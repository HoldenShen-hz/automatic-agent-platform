| # | 严重程度 | 模块 | 文件:行号 | 问题描述 | 问题状态 | 问题根因 | 推荐修复 |

|---|----------|------|----------|----------|----------|----------|----------|

| 1 | critical | Unknown | - | The rate limiting check is commented out: "Check rate limiting (simplified - would need execution history in real impl)". This means `maxExecutionsPerHour` and `rateLimitWindowMs` from the policy are never actually enforced. A skill with `maxExecutionsPerHour: 1` can be executed unlimited times. | todo | - | Implement actual rate limit tracking by querying execution history from `skill_registry` or a dedicated rate limiting table. Each `authorizeExecution()` call should compare current usage against `maxExecutionsPerHour` and reject if exceeded. |

| 2 | critical | Unknown | - | In `verifySignature()`, when `algorithm === "none"`, the function returns `false` instead of throwing a validation error. However, in `receive()` at line 122-128, the result of `verifySignature()` is assigned to `signatureVerified` and the envelope is created with `signatureVerified: false`. This means a webhook with `algorithm: "none"` is silently accepted without signature verification — no exception is raised, and the envelope proceeds to dispatch. An attacker can register a webhook endpoint with `algorithm: "none"` and forge any payload, bypassing signature verification entirely. ```typescript // verifySignature() line 215 if (input.endpoint.algorithm === "none") { return false; // Should throw instead } // receive() line 122-128 - signatureVerified is false but envelope is "accepted" const signatureVerified = verifySignature({...}); const envelope: WebhookDispatchEnvelope = { ... signatureVerified, // always false for algorithm="none" dispatchState: "accepted", // still "accepted"! }; ``` | todo | - | When `algorithm === "none"` and the endpoint requires signature verification (algorithm is not explicitly "none" with no security intent), throw `ValidationError("webhook.signature_algorithm_invalid", ...)`. If "none" is a legitimate no-op mode, the caller should handle it differently, but the current design treats it as a bypass vector. |

| 3 | critical | Unknown | - | The file uses `import type` to import function identifiers: ```typescript import type { buildDomainsRuntimeCatalog } from "./domains-runtime-catalog.js"; import type { buildDomainsStartupPlan } from "./domains-startup-plan.js"; ... ``` But these are actual functions, not types. Using `import type` for function imports is incorrect — the compiler will treat them as type-only declarations and they will be erased at runtime, making the file non-functional. | todo | - | Remove `type` keyword from these imports since they import runtime values. |

| 4 | critical | Unknown | - | The file contains hardcoded HMAC secrets (SECURITY-01 from prior review). This was already documented but no fix has been applied yet. | todo | - | Remove hardcoded secrets and use environment variables or the secret management service. |

| 5 | critical | Unknown | src/platform/five-plane-interface/api/http-server/ | When `intakeAdmissionService` is configured (line 298), the handler calls `deps.intakeAdmissionService.admit()` to create the task via the intake pipeline, BUT then also directly calls `deps.taskStore.task.insertTask()` (line 319) with the same `taskId`. This creates a split-brain: two code paths write to the task store for the same taskId, potentially with different data. The intake pipeline creates a `ConfirmedTaskSpec` and events, but the direct insert at line 319 bypasses all of that. The two writes can race or produce inconsistent task state. | todo | - | Remove the direct `insertTask()` call from the intake branch. The intake pipeline should be the sole writer when configured. Alternatively, refactor to use the intake pipeline result exclusively and remove the legacy fallback insert. |

| 6 | critical | Unknown | - | `reserveBudgetForExecution()` reserves with `resourceKind: "token"`: ```typescript const result = budgetAllocator.reserve({ ledger, amount, resourceKind: "token", expiresAt: new Date(Date.now() + 5 * 60 * 1000).toISOString(), expectedVersion: ledger.version, context: allocatorContext, }); ``` However, the amount was computed as a USD value (`context.tokenBudget * 0.001`). The `BudgetAllocator.reserve()` interprets `amount` in the unit of `resourceKind`. If `resourceKind` is `"token"`, the ledger will track token counts, not USD. When `billing-service.ts` later settles with `actualAmount` in USD, the ledger's `reserved_amount` and `settled_amount` will be in different units, making balance checks meaningless. | todo | - | Use `resourceKind: "api"` or `"usd"` when the amount represents dollars. Alternatively, keep `resourceKind: "token"` but ensure the `amount` is the token count and a separate cost conversion happens at settlement time with proper unit tracking. |

| 7 | critical | Unknown | - | `buildDecisionInputBundle()` computes: ```typescript const remainingCost = budgetEnvelope == null ? 0 : Math.max(0, budgetEnvelope.maxCost - input.stepOutputs.reduce((sum, output) => sum + output.systemTelemetry.tokensUsed, 0)); ``` `budgetEnvelope.maxCost` is in USD but `tokensUsed` is a raw token count. The subtraction produces an meaningless result. This is used downstream in the budget decision bundle, so budget enforcement decisions are based on apples-to-oranges comparisons. | todo | - | Convert `tokensUsed` to USD using the model's `inputPer1kUsd` pricing before subtracting from `maxCost`. Or store `maxCostTokens` in the envelope and do the comparison in token units. |

| 8 | critical | Unknown | - | `promoteCanary()` unconditionally sets canary weight to 0 and stable to 100: ```typescript if (target.targetId === "stable") { return { ...target, weight: 100 }; } if (target.targetId === "canary") { return { ...target, weight: 0 }; } ``` There is no pre-promotion health check. If the "stable" slot is actually the old broken version and the "canary" is the new working version, this promotion destroys the new version. | todo | - | Before promotion, verify target slot health scores are appropriate. Consider swapping direction: activate canary at 100, drain stable. |

| 9 | critical | ops-maturity | src/ops-maturity/version-management/semver-validat | `satisfiesTilde()` has a bug in the minor version bound check: `if (version.minor < constraint.minor \|\| version.minor > constraint.minor)` is always true when `version.minor !== constraint.minor`. This means the function returns `false` for any tilde range where minor versions are not equal, even if they should be satisfied. | - | - | The logic should be `if (version.minor < constraint.minor \|\| version.minor > constraint.minor)` → change to `if (version.minor < constraint.minor \|\| version.minor > constraint.minor)` is actually `minor < constraint OR minor > constraint`, which is equivalent to `minor !== constraint`. This should be `version.minor > constraint.minor` only, or simply removed since the next check `version.patch < constraint.patch` already handles the lower bound. The `<` check makes it impossible to satisfy. Fix to: `if (version.minor > constraint.minor) return false;` plus the patch lower bound check. |

| 10 | critical | platform/five-plane-execu | src/platform/five-plane-execution/recovery/runtime | `buildCompensationPlan()` retrieves the task ID using `this.store.task.getTask?.(executionId, tenantId)?.id ?? ""`. This is incorrect - `executionId` is not a task ID, so `getTask(executionId, ...)` would return null or wrong data. The correct approach is to find the task from the execution, not pass the execution ID as a task lookup key. This bug would cause `candidates` to be empty and the compensation plan to be incomplete. | todo | - | Change line 518 to: `const task = this.store.task.getTaskByExecution?.(executionId, tenantId) ?? this.store.task.getTask?.(/* first find execution then task id */, tenantId)` or use the correct repository method that finds the task from execution ID. The compensation plan would be fundamentally broken for any execution. |

| 11 | critical | tests/unit | - | `validateAccessToken()` at line 261 returns `SessionValidationResult` with `{ valid, session, reason }`. The `session` object contains `principalId` and `principalType` but NOT `tenantId`. The `Session` interface at lines 50-62 has no `tenantId` field. In `HttpApiServer.dispatchRequest()` (line 588), `authenticateOptionalPrincipal()` extracts `principal?.tenantId ?? null` (line 594) and stores it in `RouteContext`. But since `session-management.ts` never populates `tenantId` in the session, `principal.tenantId` would always be null for user sessions. The `TenantScopeFilter` at line 33 then checks `principal.tenantId !== taskScope.tenantId` which would always be `null !== tenantId` (false), effectively disabling tenant isolation for user sessions. The comment at session-management.ts lines 50-62 shows `metadata` field but tenantId is not stored there either. Tenant resolution appears to happen elsewhere (likely via `TenantBoundaryRegistryService`). | todo | - | Add `tenantId: string \| null` to the `Session` interface and populate it during `createSession()`. Ensure `validateAccessToken()` returns tenantId via the session object. Alternatively, document the tenant resolution path if tenantId is derived from a different source (e.g., `TenantBoundaryRegistryService` lookup by principalId) rather than from the session token. |

| 12 | critical | tests/unit | - | Three separate autonomy systems exist with incompatible models: 1. **ProactiveAgentService** (`interaction/proactive-agent/index.ts` line 233-234): - `currentAutonomyLevel: "suggestion" \| "supervised" \| "semi_auto" \| "full_auto"` - Gates trigger firing (line 417): only fires when `semi_auto` or `full_auto` - Adjusts action mode via `getAutonomyAdjustedActionMode()` (line 262-284) 2. **AutonomyService** (`interaction/autonomy/autonomy-service.ts` line 42-53): - `resolveLevel()` uses threshold-based mapping on `riskScore >= 80 → manual, >= 60 → supervised, etc.` - Does NOT use task type in determination despite `taskType` field existing in request 3. **AutonomyBoundaryPolicy** (`platform/five-plane-orchestration/improve-rollout/autonomy-boundary-policy.ts` line 23-44): - Checks `AutonomyTarget` (`routing_policy`, `planning_policy`, `execution_policy`, etc.) - Gates improvement candidate approval based on `learningObject.promotionStatus === "validated" \| "promoted"` - Has no concept of `suggestion/supervised/semi_auto/full_auto` levels The OAPEFLIR loop's default constraintPack (line 346-357) sets `autonomyMode: "full_auto"` but this is never reconciled with ProactiveAgentService's `currentAutonomyLevel`. When ProactiveAgent fires a trigger and creates a task, the task enters OAPEFLIR with `full_auto` autonomy mode regardless of the actual autonomy state of the triggering agent. | - | - | Create a unified `AutonomyLevel` enum used across all three systems. Add an `AutonomyReconciliationService` that maps between the different autonomy models. Document in ADR which autonomy system has authority when conflicts arise (e.g., when ProactiveAgent is in `supervised` mode but OAPEFLIR constraintPack says `full_auto`). |

| 13 | critical | tests/unit | - | `extractServiceAuth()` at lines 501-537 validates service identity (checks `x-mtls-cert` or `x-service-id` headers) and returns `ServiceAuthResult` with `serviceIdentity` and `token`. The `ServiceIdentity` interface (lines 28-38) has `namespace` but no `tenantId` field. `TenantScopeFilter.evaluate()` at lines 27-47 requires `principal.tenantId` to match `taskScope.tenantId`. Service principals (from `service-auth.ts`) have no `tenantId` - they have `namespace` (e.g., "execution", "orchestration") which is a different concept. A service calling across planes with no tenant context would fail the tenant mismatch check at line 33-35 with `reasonCode: "scope.tenant_mismatch"`. However, if `principal.tenantId` is `null` (as it would be for service principals since `TenantScopeFilter` doesn't handle `tenantId: null` specially), the check `null !== taskScope.tenantId` at line 33 would return `false` (no mismatch), meaning service principals could access any tenant's tasks. This is a security gap - services should be scoped to their own namespace/tenant. | todo | - | In `TenantScopeFilter.evaluate()`, add a check for service principals: if `principal.tenantId === null`, evaluate against `namespace` rather than `tenantId`. Or add a separate `serviceNamespace` field to `PrincipalScope` and validate that service calls are restricted to their own namespace. Document whether service principals are tenant-scoped or platform-wide. |

| 14 | high | Deep-Cross: http + tcp +  | src/interaction/dashboard/dashboard-websocket-serv | `assertAuthorized()` for task subscriptions has a logic gap at lines 426-430. The check `if (subscription.channel === "task" && subscription.filterId != null && allowedTasks.size > 0 && !allowedTasks.has(subscription.filterId))` means if `allowedTasks` is an empty Set (size === 0), task-filtered subscriptions are allowed without any explicit authorization check. This allows a client to subscribe to any task when no task allowlist is configured. | todo | - | Change condition to require `allowedTasks.size > 0` to be checked independently of the filter ID presence. If `allowedTasks` is empty but the scope was meant to restrict task access, subscription should be denied. |

| 15 | high | Deep-Cross: http + tcp +  | src/platform/five-plane-interface/api/http-api-ser | `ChannelGatewayService` uses the platform `fetch` (global fetch via `this.fetchImpl ?? fetch` at line 90) without configuring an `http.Agent` with connection pool limits. The default global fetch has no connection reuse tuning, keep-alive timeout enforcement, or socket limit. Under high load, this can lead to socket exhaustion as connections pile up without being reused or closed promptly. | todo | - | Provide a configurable `http.Agent` with `maxSockets`, `maxFreeSockets`, and `scheduledFreeSocketReleaseTimeout` set appropriately. Allow `fetchImpl` to be injected with an agent-wrapped fetch for testability. |

| 16 | high | Deep-Cross: http + tcp +  | src/platform/five-plane-interface/api/http-api-ser | `resolveClientIp()` trusts `X-Forwarded-For`, `X-Real-IP`, and `Forwarded` headers without IP address validation. An attacker can spoof these headers to bypass IP-based rate limiting or appear as a different tenant. The `forwarded` regex at line 527 is also parsing a de facto standard header that can be easily forged. | todo | - | Add a configurable trust list for proxies. Only extract IP from untrusted headers when the connection comes from a known proxy IP. Reject or sanitize values containing port numbers or unusual characters. |

| 17 | high | Unknown | - | `authorizeExecution()` returns `authorized: true` with a default policy when `getExecutionPolicy()` returns null (skill has no stored policy). The comment at line 356-357 says "No policy means allow with default restrictions" but this default policy (`allowExecution: true, requireApproval: false`) effectively grants full execution rights. A skill that was never explicitly registered with a policy inherits these permissive defaults. | todo | - | Require that all skills have an explicit execution policy before they can be invoked. Return `authorized: false` with a clear error when no policy exists, rather than defaulting to permissive behavior. |

| 18 | high | Unknown | - | `execute()` accepts any `action` string that matches the switch cases in `buildEndpoint()` and `buildPayload()`. There is no check that the requested action is within the adapter's declared `capabilityIds` (line 197: `["external.github", "external.github.issue", "external.github.workflow"]`). An adapter could declare one set of capabilities but actually execute different actions if the action parameter is manipulated. | todo | - | Validate the `action` parameter against the adapter's `capabilityIds` before execution. Map each capability to its allowed actions and enforce the mapping at invocation time. |

| 19 | high | Unknown | - | `buildEndpoint()` constructs a URL string from the action and repository without validating the resulting URL format. For `dispatch_workflow` (line 278), the `workflowId` is used directly in the path. If `workflowId` contains path traversal characters (`../`), the URL construction could produce `../../actions/workflows/../../dispatches`. | todo | - | Validate all path components in `buildEndpoint()` using the same pattern as `requireRepository()`: check for `..`, `\`, `%2f`, etc. Add explicit URL validation after construction. |

| 20 | high | Unknown | - | `capabilityIds` are merged from multiple sources (`plugin.capabilityIds`, `builtinManifest.capabilityIds`, `plugin.manifest.capabilityIds`, `manifest.capabilityIds`) using `Set` union. If any source provides a capability that others do not, all are combined. A plugin with no `capabilityIds` but a `builtinManifest` with broad capabilities would inherit all of them. There's no validation that the declaring plugin actually intends to use all merged capabilities. | todo | - | Require that each plugin explicitly declares its `capabilityIds` and that the final merged set is a subset of what the plugin explicitly declares. Warn or error if a plugin inherits capabilities it didn't declare. |

| 21 | high | Unknown | - | In `defaultManifestFor()`, adapters get `allowNetworkEgress: plugin.spiType === "adapter"` (line 63). This means ALL adapters get network egress by default unless explicitly disabled in the manifest. The `PluginSandboxPolicySchema` (line 48 in plugin-spi.ts) defaults `allowNetworkEgress` to `false`, but the `defaultManifestFor()` override always sets it to true for adapters regardless of the schema default. | todo | - | Remove the automatic `true` override for adapters. Let the schema default (`false`) apply unless explicitly changed. Add a comment explaining that adapter manifest must explicitly set `allowNetworkEgress: true` if needed. |

| 22 | high | Unknown | - | The FORBIDDEN_MCP_OUTPUT_PATTERN check at line 232-234 applies `JSON.stringify()` to `sanitizedData` but searches the string for the literal pattern `"(?:function_call\|tool_use\|tool_calls)"`. An attacker could evade detection by encoding the forbidden field values with Unicode escapes (`u` instead of `u`), different quote styles, or whitespace variations. The pattern is also case-insensitive but only matches the exact string with specific quote types. | - | - | Use structured validation instead of regex pattern matching. Define a strict schema for allowed MCP response shapes and validate the parsed structure against it, not the string representation. |

| 23 | high | Unknown | src/platform/five-plane-interface/webhook/index.ts | `signedRequestReplayCache` is a `Map<string, { signatureKey: string; idempotencyKey: string }>` with no TTL, no size limit, and no eviction policy. Each unique `(endpointId, signature)` pair is stored permanently. If an attacker can obtain or guess valid signatures for a target endpoint (e.g., from logs, network observation, or deliberate replay), they can exhaust memory by filling the cache with entries mapping different idempotency keys to the same signature key. The cache is only read at line 248-252 (checking if signature was used with different idempotency key) and written at line 254-257 (storing the mapping). There is no cleanup mechanism. ```typescript // line 65 - unbounded cache private readonly signedRequestReplayCache = new Map<string, { signatureKey: string; idempotencyKey: string }>(); ``` | todo | - | Add TTL-based eviction using `Map<string, { signatureKey: string; idempotencyKey: string; expiresAt: number }>` and clean up entries older than TTL (e.g., 5 minutes) on each `receive()` call. Alternatively, replace the in-memory Map with a bounded LRU cache. |

| 24 | high | Unknown | - | `enforceRateLimit()` is called at line 196, after `createDeliveryMessage()` at line 200. If `enforceRateLimit()` throws `GatewayRateLimitError`, the delivery message record already exists in the database with status "pending". The `catch` block at line 238-243 calls `recordFailedDelivery()` which transitions the record to "retrying" or "failed", but this leaves a temporary orphaned record if the exception escapes before `recordFailedDelivery` is called (e.g., if `recordFailedDelivery` itself throws). More critically: the rate limit check at line 196 uses the channel's current count before the message is sent, but the count increment happens in `recordSuccessfulDelivery()` at line 392 via `recordRateLimitHit()`. If the send succeeds but `recordSuccessfulDelivery()` throws before `recordRateLimitHit()` is called, the rate limit accounting is lost — the message is delivered but not counted, allowing a burst to exceed limits. ```typescript // line 196 - rate limit check BEFORE count increment this.enforceRateLimit(channel, deliveryService, tenantId); // throws if over limit // line 200 - delivery message created const trackedMessageId = deliveryService.createDeliveryMessage(...); // line 233-235 - count increment happens here, after successful send this.recordSuccessfulDelivery(channel, trackedMessageId, deliveryReceipt, deliveryService, tenantId); // recordRateLimitHit() is inside recordSuccessfulDelivery() at line 392 ``` | todo | - | Move `recordRateLimitHit()` to occur immediately before `deliverResolvedTarget()`, not after successful delivery. If the send fails, the hit should not be recorded. Add try-finally to ensure rate limit accounting is only skipped on failure path. |

| 25 | high | Unknown | - | In `BrowserWSClient`, the `socket.onmessage` handler parses the incoming data as `WSEventEnvelope & { action?: string }` at line 227. The `publish()` method at lines 181-186 calls `rememberEvent(event)` then delivers to handlers. `rememberEvent()` at lines 354-363 extracts `eventId` from the event and stores it in `lastEventIdByChannel` for future replay. If an attacker can send a malicious WebSocket message with `event.eventId = "injected-id"`, `event.channel = "target-channel"`, and a crafted `payload`, the event will be: 1. Stored in `replayBufferByChannel` with the attacker's `eventId` 2. Delivered to all handlers for `target-channel` 3. Used as `lastEventId` for future subscribe replay at line 154-156 ```typescript // line 227 - parses untrusted data as WSEventEnvelope const data = JSON.parse(String(event.data)) as WSEventEnvelope & { action?: string }; // line 232 - publishes to handlers without additional validation this.publish(data); // rememberEvent at lines 354-363 stores eventId from attacker-controlled source private rememberEvent(event: WSEventEnvelope): void { const eventId = this.resolveEventId(event); // from event.eventId if (eventId != null) { this.lastEventId = eventId; // attacker-controlled lastEventId this.lastEventIdByChannel.set(event.channel, eventId); // attacker-controlled channel mapping } } ``` | todo | - | Validate that incoming `WSEventEnvelope` messages have a valid `eventId` format (e.g., must match the server's `evt-{timestamp}` pattern). Reject messages with unrecognized `eventId` patterns before calling `publish()`. Add a denylist for known attack payload patterns in the event type field. |

| 26 | high | Unknown | - | When `subscribe()` is called, it sends the channel's `lastEventId` to the server for replay: ```typescript socket.send(JSON.stringify({ action: "subscribe", channel, ...(this.lastEventIdByChannel.get(channel) == null ? {} : { lastEventId: this.lastEventIdByChannel.get(channel) }), // attacker-controlled })); ``` If an attacker previously injected a malicious `eventId` via Issue WH-07 into `lastEventIdByChannel`, this value is sent to the server on next subscribe. The server uses it to replay events from that point, potentially causing the server to replay events from an incorrect sequence position or to send a `stream_gap` response that reveals sensitive sequence information. | todo | - | Same as WH-07 — validate `eventId` before storing. Additionally, add a comment in the code that `lastEventIdByChannel` values come from untrusted server data and must not be replayed without validation. |

| 27 | high | Unknown | - | The `appendToReplayBuffer()` method at line 699 has a loop that tries to drop non-critical frames when the buffer exceeds `maxReplayFrames`. However, the loop condition at line 702 is `while (next.length > this.options.maxReplayFrames)`. Inside the loop, it searches for a droppable event from index 0 (oldest) forward. If no droppable events exist (all are critical), `indexToDrop` stays -1 and the loop breaks at line 718. But critically: if `indexToDrop >= 0`, the code does `next.splice(indexToDrop, 1)[0]` at line 729. If the droppable event is at index 0 and it's not critical (e.g., "progress"), it gets removed. However, if multiple critical events are at the front of the buffer and droppable events are at the end, the search from index 0 finds the first droppable which might be at a high index — meaning many critical events have already accumulated before a droppable is found. The result is that under high event rate with mostly critical events, the buffer grows beyond `maxReplayFrames`. ```typescript // lines 702-738 - while loop that may not properly maintain maxReplayFrames while (next.length > this.options.maxReplayFrames) { const criticalEventTypes = new Set(["completed", "failed", "approval_requested"]); let indexToDrop = -1; for (let i = 0; i < next.length; i++) { // Searches from index 0 const candidateFrame = next[i]; if (candidateFrame && !criticalEventTypes.has(candidateFrame.eventType)) { indexToDrop = i; break; // Found droppable, but it might be at high index (many critical events already buffered) } } if (indexToDrop < 0) { // All critical - buffer grows beyond limit break; } const removed = next.splice(indexToDrop, 1)[0]; } ``` | todo | - | Change the search to find the OLDEST droppable event (lowest index) for eviction, which is correct. However, also add a check: if after searching the entire buffer no droppable events exist and `next.length > this.options.maxReplayFrames`, the buffer is genuinely full of critical events. In this case, consider logging a warning and dropping the oldest critical event (or the current frame being added) to maintain the size constraint, rather than breaking and allowing unbounded growth. |

| 28 | high | Unknown | - | `detectFromChecks()` processes each check and creates incidents using two separate code paths: 1. Lines 70-80: `fail_closed` → SEV1, lines 82-90: `degraded` → SEV2 2. Lines 94-108: Detection rules are evaluated unconditionally on every check The detection rules (lines 229-278) fire SEV1-SEV3 based on metric thresholds. A check with `status: "degraded"` triggers a SEV2 incident via lines 82-90 AND may also trigger additional SEV1 or SEV2 incidents via detection rules if the same check's metrics match rule conditions. This means a single failing health check can produce 2-3 incidents at different severity levels for the same underlying problem. **False positive mechanism:** - A check with `status: "degraded"` creates SEV2 incident - If that check's metrics also satisfy `sev1_availability_collapse` (availability < 95% or error_rate > 5%), a SEV1 incident is also created - If metrics also satisfy `sev2_degraded_service` (latency_p99 > 1000ms), a third incident fires | todo | - | Use mutually exclusive severity assignment. Once a check's status determines the primary severity, skip rule evaluation for that check unless the primary detection intentionally needs supplementation (e.g., `fail_closed` + additional SEV1 conditions). Or: deduplicate incidents by `sourceCheckId` before returning. |

| 29 | high | Unknown | - | `performHealingOperation()` uses a purely mathematical formula: ```typescript const deterministicScore = action.targetComponent.length + action.operation.length + (action.reasonCode?.length ?? 0); return deterministicScore % (action.operation === "failover" ? 5 : 4) !== 0; ``` This produces a deterministic-but-opaque success rate (~75-80%) that does not reflect actual component state. The method does not consult `componentHealth` (line 78) for the target component, does not consider `consecutiveFailures` count, and does not weight `operation` risk. **Self-healing making things worse:** A component in `unhealthy` state with `consecutiveFailures >= maxRetries` will still have healing operations attempted (lines 103-119 check cooldown, not health state). If `performHealingOperation()` returns `false` on a `failover` operation for an already-failed component, the subsequent status update (lines 125-129) marks the component as `unhealthy` and increments failures, but does not prevent future healing attempts. The service can repeatedly attempt failover on a dead component. | todo | - | Evaluate component health before attempting operation. If `consecutiveFailures >= maxRetries`, refuse the healing attempt. Add exponential backoff keyed to `newConsecutiveFailures` rather than fixed cooldown period. Document the expected success rate per operation type. |

| 30 | high | Unknown | - | `fireAlert()` checks `isRuleCoolingDown()` (line 318) to suppress duplicate alerts within a rule's cooldown window. However: 1. `isRuleCoolingDown()` uses `cooldownState` map keyed by `rule.id` (line 995). If multiple SLOs have overlapping conditions, separate rules fire separate alerts even for the same incident. 2. `shouldEmitAlert()` in `cost-alert-service.ts` (line 644) uses a different deduplication key format `${scope}:${scopeId}:${alertLevel}`. An alert that fires as cost:warning cannot be correlated with an incident:SEV2 from the same underlying event. 3. The `AnomalyDetectionService.detectAllAnomalies()` (line 110-116) iterates all thresholds and may fire multiple `AnomalyAlert` objects for the same metric episode if both threshold-breach and statistical anomaly conditions are met. **Alert fatigue scenario:** - A latency spike triggers `sev2_degraded_service` (incident-detector), fires SEV2 - The same spike causes `latency_p99_ms` threshold breach in anomaly-detection, fires a second alert - The cost-alert-service fires a third alert for the same timeframe - The SloAlertingService fires a fourth alert via burn-rate evaluation | todo | - | Implement a correlation key generation scheme that maps related alerts to a single grouping ID. Add an `alertCorrelationId` to `IncidentDetection` and propagate it through to all downstream alert events. Use a deduplication window larger than individual rule cooldowns to catch cross-service duplicates. |

| 31 | high | Unknown | - | `isDirectiveExpired()`: ```typescript private isDirectiveExpired(directive: PlatformPanicDirective): boolean { return directive.expiresAt != null && Date.parse(directive.expiresAt) <= Date.now(); } ``` This correctly guards against null/undefined (returns `false` if `expiresAt` is null, meaning no expiration). However, `Date.parse()` returns `NaN` for malformed strings like `""` or `"invalid"`, and the comparison `NaN <= Date.now()` evaluates to `false`. This means a directive with `expiresAt: ""` is treated as never-expiring (never expires), placing the system in a permanent panic state if such a directive is activated. The earlier review (Issue 4 / XREF-31) flagged this exact bug under `platform-panic-service.ts:405-407` with the same consequence — directive never expires. It appears this bug was documented but not yet fixed. **Panic mode stuck state:** A directive with empty or whitespace `expiresAt` (which `trim().length === 0` would be falsy in some paths but not in `isDirectiveExpired`) blocks all `deploy` and `automation` freeze modes permanently. `evaluateExecution()` returns `blocked: true` for all matching scopes. | todo | - | Add NaN check: if `Date.parse(directive.expiresAt)` is `NaN`, log an error and treat the directive as expired (remove it). Use `Number.isNaN()` before comparison. |

| 32 | high | Unknown | - | The pipeline `IncidentDetector → PlatformOpsAgentService → SelfHealingService → IncidentResolver` has a circular escalation risk: 1. `PlatformOpsAgentService.createProposal()` (line 116) calls `incidentDiagnoser.diagnose()` which uses `classifyOpsIncident(errorRate, backlog)` — if the self-healing attempt increases backlog (e.g., restart causes queue backup), the next proposal will have higher `incidentLevel`. 2. `SelfHealingService.execute()` (line 87) requires both `runbookRef` and `approvalRef` to be non-empty. If these are missing, it returns `healed: false` (line 89-97) without updating `componentHealth`. The healing attempt counter still increments via `consecutiveFailures` (line 124). 3. `IncidentResolver.determineStrategy()` (line 136) assigns `self_heal` for performance category and `automated` for availability with symptoms. If self-healing fails repeatedly, the resolver does not re-evaluate strategy — it continues returning `self_heal` even as `consecutiveFailures` grows. 4. The `shouldEscalate()` check (line 225) only looks at `elapsedSeconds >= threshold` based on strategy type. It does not consult self-healing failure count or component health status. A stuck self-healing loop can run for `escalationThresholdSeconds` (600s default) before escalating. **Escalation loop scenario:** - Component fails, self-healing attempts restart - Restart causes backlog spike, error rate rises - Next diagnosis: `critical_incident` level - `chooseActionType()` returns `failover` but `failover` may not be in `allowedActionTypes` - Proposal blocked by `ops_agent.action_not_allowed`, but `executable: false` is stored in proposal - System continues cycling without resolution | todo | - | Add a `consecutiveFailures`-based strategy elevation in `IncidentResolver.determineStrategy()`. If `consecutiveFailures >= 3` for the target component, escalate to `assisted` or `manual` regardless of incident category. Add loop detection in `PlatformOpsAgentService` that checks `healingHistory` length for the component before creating a new proposal. |

| 33 | high | Unknown | - | `simulateHealthCheck()` uses: ```typescript const healthCheckPassed = !/(rollback\|failover)/.test(operation) \|\| componentId.length % 2 === 0; ``` This creates alternating pass/fail behavior based purely on componentId string length parity. For components with odd-length IDs, `failover` and `rollback` operations always fail the health check. For even-length IDs, they pass. This behavior is deterministic but not reflective of actual healing operations. If `healthCheckPassed` is `false`, the receipt `healed` field is `false` (line 141). This can cause a successful `performHealingOperation()` (which may return `true`) to be undone by a failing health check simulation, marking the overall healing as failed even when the operation succeeded. **Self-healing making things worse:** Repeated healing attempts triggered by `healed: false` can cause service restarts in a tight loop if the component ID has odd length and the operation is `failover`. | - | - | Replace the parity-based simulation with a proper health evaluation or make the simulation failure rate configurable. Document that `simulateHealthCheck` is a simulation and should not drive production healing decisions. |

| 34 | high | Unknown | - | `canResumeFromPanic()` (line 49) requires: ```typescript plan.checkpointsVerified && (plan.forensicSnapshotReviewed ?? false) && (plan.rollbackPlanReady ?? false) && (plan.validationRunPassed ?? false) ``` The `forensicSnapshotReviewed` field is nullable (`?? false`) — if it is `undefined`, it defaults to `false`. However, if the field is present but `null`, the `?? false` still resolves to `false`, blocking resume. More critically, the `?? false` coalescing does not distinguish between "not yet reviewed" and "reviewed and confirmed good". A field set to `null` (explicitly not reviewed, or review skipped) would fail the check even though it may have been intentionally nulled. Additionally, `shouldEnterPanicMode()` in `panic-controller/index.ts` (line 7-11) has no lower bound on `activeIncidents`. A `reasonCode` starting with `security.` enters panic mode even if `activeIncidents === 0` — meaning a security advisory could trigger platform-wide freeze with no actual incident requiring response. **Panic mode stuck state:** If a panic directive is issued with `security.advisory` and `activeIncidents: 0`, freeze modes are applied. The resume protocol requires `forensicSnapshotReviewed` but no forensic snapshot may have been collected (no incident to investigate), so the resume plan cannot satisfy the requirement. | todo | - | Require `activeIncidents > 0` for non-security reason codes. For security reason codes, allow entry but add a `scopeLevel`-based review requirement. Distinguish between `null` (explicitly skipped) and `undefined` (not yet reviewed) for nullable boolean fields. |

| 35 | high | Unknown | - | `shouldPromoteCanary()` uses a misleading `hasRequiredMetrics` check with OR logic, then treats missing metrics as perfect: ```typescript const hasRequiredMetrics = "successRate" in progress \|\| "errorRate" in progress \|\| "latencyP50Ms" in progress; const successRate = hasRequiredMetrics ? (progress.successRate ?? 0) : 1.0; const errorRate = hasRequiredMetrics ? (progress.errorRate ?? 0) : 0.0; const latencyP50Ms = hasRequiredMetrics ? (progress.latencyP50Ms ?? Infinity) : 0; ``` If ALL three keys are absent, `hasRequiredMetrics` is `false`, and all three metrics get perfect values. This means an agent with zero operational history would pass promotion trivially. The OR logic compounds this: if only `successRate` is present but `errorRate` and `latencyP50Ms` are absent, `hasRequiredMetrics` is still `true` and only present metrics are checked. | - | - | Require ALL metrics present before evaluating. Use AND logic. Fail-safe: require explicit metric presence, reject promotion if any metric key is absent. |

| 36 | high | Unknown | - | In `calculateDelay()`, jitter is applied AFTER the final cap at `maxDelayMs`. When `cappedDelay` already equals `maxDelayMs`, jitter has zero room to vary: adding ±jitter to `maxDelayMs` and then calling `Math.min(..., maxDelayMs)` always returns `maxDelayMs`. This collapses jitter to a no-op precisely when the cap is active, reducing randomized spread during high-backoff scenarios and contributing to thundering-herd on retry storms. | todo | - | Apply jitter BEFORE the final cap: `const candidateDelay = Math.floor(cappedDelay + jitter); return Math.min(candidateDelay, this.maxDelayMs);` |

| 37 | high | Unknown | - | ```typescript try { await this.redis.quit(); } catch { this.redis.disconnect(); } ``` An empty `catch {}` silently swallows the error. If `quit()` fails due to a connection issue, operators have no visibility into this failure. The subsequent `disconnect()` call may mask the original error by creating a new connection state. | todo | - | ```typescript try { await this.redis.quit(); } catch (err) { this.logger.warn("redis.quit_failed", { error: err instanceof Error ? err.message : String(err) }); this.redis.disconnect(); } ``` |

| 38 | high | Unknown | src/sdk/client-sdk/api-client.ts:111 | `performVersionHandshake()` calls `/handshake` endpoint but the API gateway only exposes `/v1/version` and `/v1/handshake` routes (health-routes.ts lines 94-113). The SDK calls `fetchVersionCompatibility("/handshake")` at line 482, which resolves to `{baseUrl}/{apiVersion}/handshake`. This creates a mismatch where the version handshake attempt hits a non-existent route at the default SDK configuration unless the baseUrl is explicitly pointed to the API prefix. | todo | - | Ensure the SDK correctly routes the handshake to `/v1/handshake` by verifying `buildApiUrl()` properly constructs the path, or align the API gateway to expose a `/handshake` route that mirrors `/v1/handshake`. |

| 39 | high | Unknown | - | `reserveBudgetForExecution()` converts token budget to USD using a flat rate: ```typescript const amount = context.tokenBudget ? context.tokenBudget * 0.001 : 1; // Estimate: $1 per 1000 tokens ``` This flat conversion ignores actual model pricing from the model registry. Different models have vastly different per-token costs (e.g., $0.001/1K tokens for cheap models vs $0.01-$0.03/1K tokens for frontier models). The hardcoded rate will systematically under-reserve for expensive models and over-reserve for cheap ones. | todo | - | Look up `inputPer1kUsd` from `ModelMetadataRegistry` based on the model selected for execution, then compute `amount = context.tokenBudget * (modelPricing.inputPer1kUsd / 1000)`. Fall back to the flat rate only when model metadata is unavailable. |

| 40 | high | Unknown | src/platform/five-plane-state-evidence/truth/postg | PostgreSQL migration 12 (`MIGRATION_12_AUTHORITATIVE_ASYNC_PARITY`) adds columns `kind`, `status`, `importance_score`, `freshness_score`, and `content_hash` to the `memories` table. SQLite migration 15 (`0015_memory_scope_and_quality`) adds columns `session_id`, `agent_id`, `execution_id`, `memory_layer`, `source_trust_level`, `quality_score`, `hit_count`, `last_accessed_at`, `expires_at`, `revoked_at`, and `revocation_reason`, but does NOT include `kind`, `status`, `importance_score`, `freshness_score`, or `content_hash`. This means SQLite and PostgreSQL schemas diverge after migration: the `memories` table will have different columns depending on which backend is used. Any data export from SQLite that is imported into PostgreSQL (or vice versa) will lose these fields or fail integrity checks. | todo | - | Align SQLite migration 15 with PostgreSQL migration 12 to include all columns: `kind TEXT NOT NULL DEFAULT 'general'`, `status TEXT NOT NULL DEFAULT 'active'`, `importance_score DOUBLE PRECISION NULL`, `freshness_score DOUBLE PRECISION NULL`, `content_hash TEXT NULL`. Alternatively, introduce a migration 46 on SQLite that adds these missing columns. |

| 41 | high | Unknown | - | When `canary.healthScore == null`, `checkCanaryHealth()` returns `{ healthy: false, reason: "no_health_data" }`. However, `advanceShift()` never calls `checkCanaryHealth()`, so this "no health data" case does not block advancement. Even if `checkCanaryHealth()` were called, the caller could ignore the `healthy: false` result. | todo | - | Make `advanceShift()` a no-op when health check fails. Add a health-gated variant `advanceShiftIfHealthy()` and have schedulers call only that. |

| 42 | high | Unknown | - | ```typescript } catch (error) { logger.log({ level: "warn", message: "Channel gateway retry executor pass failed", ... }); // Return empty summary on error (don't propagate) return { startedAt, completedAt: nowIso(), busy: false, scanned: 0, delivered: 0, retryScheduled: 0, deadLettered: 0, skippedRateLimited: 0, }; } ``` When `processRetryQueue()` throws, the caller receives an empty summary that looks identical to a successful pass with no work to do. This makes it impossible to distinguish between "no messages in queue" and "processing failed". The error is logged at warn level but not propagated. | todo | - | Return a result type that distinguishes success from failure: ```typescript type RetryPassResult = \| { success: true; summary: GatewayRetryQueueSummary } \| { success: false; error: string; partialSummary?: GatewayRetryQueueSummary }; ``` |

| 43 | high | Unknown | - | `reserve()` checks: ```typescript const tierLimit = context.tierLimit ?? input.ledger.hardCap; const activeCommittedAmount = input.ledger.reservedAmount + input.ledger.settledAmount - input.ledger.releasedAmount; if (activeCommittedAmount + input.amount > tierLimit) { throw new ValidationError("budget_reservation.hard_cap_exceeded", ...); } ``` It compares `activeCommittedAmount + input.amount` (which is in the ledger's native unit) against `tierLimit` (which is either from context or `hardCap`). But `tierLimit` from context could be in different units than the ledger's `hardCap`. The fallback `context.tierLimit ?? input.ledger.hardCap` means if `tierLimit` is absent from context, it uses `hardCap` — which is correct. However, if `tierLimit` is **present** in context, it could be a different value than `hardCap` in different units (e.g., `tierLimit` is tokens while `hardCap` is USD), making the comparison meaningless. | todo | - | Ensure `tierLimit` and `ledger.hardCap` are always in the same unit. Document that `tierLimit` must be in the same currency/unit as `ledger.hardCap`. Add a runtime check that asserts unit consistency. |

| 44 | high | Unknown | - | The code references `@see Event Registry Contract: docs_zh/contracts/event_registry_and_ops_threshold_contract.md` and `@see Event Bus Contract: docs_zh/contracts/event_bus_contract.md` but these contract files do not exist in the repository. | todo | - | Create the referenced contract documents. |

| 45 | high | Unknown | - | `evaluateHealthGate()` reads thresholds from `rollout.healthGates`: ```typescript const thresholds = { maxErrorRate: toFiniteThreshold(rollout.healthGates?.["maxErrorRate"], this.healthThresholds.maxErrorRate), maxLatencyRegression: toFiniteThreshold(rollout.healthGates?.["maxLatencyRegression"], this.healthThresholds.maxLatencyRegression), maxIncidentRate: toFiniteThreshold(rollout.healthGates?.["maxIncidentRate"], this.healthThresholds.maxIncidentRate), }; ``` If `healthGates` contains `maxErrorRate: 999`, `toFiniteThreshold` returns `999` (a valid finite number). Any errorRate <= 998 would pass the gate. The schema for `healthGates` is `Record<string, unknown>` with no validation. | todo | - | Define a `HealthGateConfig` interface with explicit typed fields. Validate all values are within expected ranges before using them. |

| 46 | high | Unknown | - | `fetchEvents()` wraps its call to `this.eventRepository.listAllEvents()` in a try/catch (lines 431-433, 434-453). When an error occurs, it logs the error and returns an empty array `[]` (line 452). The caller `rebuildProjectionState()` at line 341 checks `if (events.length === 0)` to determine `hasMore = false` and exit the loop. This means any error during event fetching — whether a transient database error, a query timeout, or a connection failure — results in the projection rebuild silently stopping without processing any remaining events. The `eventsProcessed` counter does not reflect that events were skipped due to error. The rebuild reports success with no errors but with an incomplete projection state. | todo | - | Instead of returning an empty array, throw a typed error like `ProjectionRebuildFetchError` that preserves the error context (projection name, offset, limit). In `rebuildProjectionState`, catch this error and either retry with backoff or fail the rebuild with a clear error indicating how many events were successfully processed before the failure. |

| 47 | high | Unknown | - | `ChannelGatewayService` maintains its own inline circuit breaker state map (`circuitBreakerState = new Map<string, { failureCount: number; openUntil: number \| null }>()`) instead of using the shared `CircuitBreaker` from `platform/stability/`. This inline implementation does not have: half-open probe limiting, sliding-window failure rate calculation, event bus integration, or `totalRequests` tracking. It is a significantly simpler implementation than the platform stability circuit breaker, creating behavioral divergence across the platform. | - | - | Replace inline circuit breaker with an instance of `CircuitBreaker` from the stability module, configured with equivalent thresholds. |

| 48 | high | Unknown | - | Multiple catch blocks silently swallow errors without any logging: ```typescript } catch { // empty — error is ignored } ``` Compliance evidence collection failures would not appear in any log, creating audit gaps. | todo | - | Add `logger.warn` to at minimum record that a collection step failed. |

| 49 | high | Unknown | - | `replayConsumer` calls `resetConsumerReplayState` then `drainConsumer`, but neither call has a timeout. If a consumer is stuck or the drain operation hangs, `replayConsumer` will block indefinitely. This could stall startup repair sweeps or recovery drills. | todo | - | Add a timeout wrapper around `drainConsumer` (e.g., 30 seconds). Return a partial result with `outcome: "timeout"` if the timeout fires. |

| 50 | high | Unknown | - | `applyTrafficWeights()` updates two slots independently: ```typescript private applyTrafficWeights(slotA: DeploymentSlot, weightA: number, slotB: DeploymentSlot, weightB: number): void { this.db.connection.prepare(`UPDATE deployment_slots SET traffic_weight = ?, ... WHERE slot = ? AND status IN ('active', 'standby')`).run(weightA, ..., slotA); this.db.connection.prepare(`UPDATE deployment_slots SET traffic_weight = ?, ... WHERE slot = ? AND status IN ('active', 'standby')`).run(weightB, ..., slotB); } ``` There is no constraint preventing both slots from having `status = 'active'`. A bug or race condition could result in both blue and green being active simultaneously with non-zero weights. | todo | - | Add a database constraint or application-level invariant that only one slot per deployment group can be active at a time. |

| 51 | high | Unknown | - | ```typescript if (this.delegationRepository) { this.hydrateFromRepository().catch((err) => { // Log but don't fail startup - in-memory mode still works as fallback delegationManagerLogger.error("delegation.hydrate_from_repository_failed", { error: err instanceof Error ? err.stack ?? err.message : String(err), }); }); } ``` The `.catch()` handler only logs the error but does not propagate it. Startup continues as if nothing went wrong. If `hydrateFromRepository()` fails due to a database issue, the service starts in a degraded state where in-memory delegation state is empty while the repository has existing delegations that will never be recovered. | todo | - | Make the hydration failure fatal at startup: ```typescript if (this.delegationRepository) { try { await this.hydrateFromRepository(); } catch (err) { delegationManagerLogger.error("delegation.hydrate_from_repository_failed", { error: err instanceof Error ? err.stack ?? err.message : String(err), }); throw new Error("Failed to hydrate delegation state from repository"); } } ``` |

| 52 | high | Unknown | - | `normalizeGatewayDeliveryFailure()` treats ALL errors as retryable by default (line 76-77: `return { retryable: true }`). Even `AppError` instances with `retryable: false` are ignored unless they are `GatewayDeliveryError` or `GatewayRateLimitError`. Any custom error type (including platform errors that should not be retried) will be treated as retryable. | todo | - | In the final fallback case, call `deliveryService.isRetryableStatus()` on a extracted status code, or introduce a "unknown errors are non-retryable by default" policy. |

| 53 | high | Unknown | - | ```typescript function safeJsonParsePayload(jsonString: string, errorContext: string): Record<string, unknown> { try { const parsed = JSON.parse(jsonString); return DeliveryPayloadSchema.parse(parsed); } catch (err) { logger.warn("channel_gateway.payload_validation_failed", { errorContext, ... }); // Return empty object on validation failure to allow processing to continue return {}; } } ``` Returns an empty object `{}` when payload validation fails. Callers like `readTrackedDeliveryPayload()` at line 281 then check if the result is `null` to detect invalid payloads. Since an empty object is returned (not null), invalid payloads are treated as valid. | todo | - | Return `null` on validation failure and update callers to check for null: ```typescript function safeJsonParsePayload(jsonString: string, errorContext: string): Record<string, unknown> \| null { try { const parsed = JSON.parse(jsonString); return DeliveryPayloadSchema.parse(parsed); } catch (err) { logger.warn("channel_gateway.payload_validation_failed", { errorContext, ... }); return null; // Signal invalid payload explicitly } } ``` |

| 54 | high | Unknown | - | When `budgetControl.ledger` is not provided, a new ledger is created with: ```typescript const ledger = input.budgetControl.ledger ?? createBudgetLedger({ tenantId: input.budgetControl.tenantId, harnessRunId: input.budgetControl.harnessRunId, currency: "USD", hardCap: estimatedChargeUsd, }); ``` The `hardCap` is set to `estimatedChargeUsd`, which is based on `quantity * unitPriceUsd` from the plan quota. But the actual cost could be higher if the model used is more expensive than the plan rate. This creates a ledger that can never cover the actual cost if the task uses a premium model. | todo | - | Look up the actual model pricing to compute a more accurate `hardCap`, or set `hardCap` to `estimatedChargeUsd * 1.5` (or some multiplier) to provide buffer. Document that the ledger's `hardCap` is a minimum guarantee and actual costs may exceed it. |

| 55 | high | Unknown | - | `enforceVersionLimits` at line 248-255 deletes files with `rmSync` but never calls `removeCandidateFromManifests`. Checkpoints deleted by version limit enforcement will still be referenced in their corresponding `.manifest.json` files. This creates orphaned manifest entries pointing to non-existent checkpoint files. | todo | - | Call `removeCandidateFromManifests` for each file deleted by `enforceVersionLimits`, passing a synthetic `CheckpointGCCandidate` with the correct `checkpointRef` and `storagePath`. |

| 56 | high | Unknown | src/domains/recipes/recipe-executor.ts:111 | The catch block captures the error but does not call `logger.error`: ```typescript } catch (error) { const result = { success: false, ... error: error instanceof Error ? error.message : String(error), }; metrics = { ... }; return result; } ``` The error is embedded in the result object but not written to any log stream. | todo | - | Add `logger.error` with the full error context before returning. |

| 57 | high | Unknown | - | The code reads the ledger via raw SQL: ```typescript const ledgerRow = storage.sql.connection.prepare("SELECT * FROM budget_ledgers WHERE budget_ledger_id = ?").get(context.budgetLedgerId) as {...} \| undefined; ``` Then constructs a `ledger` object by mapping snake_case DB columns to camelCase properties. This manual mapping is fragile — if the schema changes (e.g., column renamed, new required field), the code silently produces incorrect `ledger` objects. The `BudgetAllocator` and `BudgetGuard` then operate on these potentially incorrect objects. | - | - | Use the正式的 `AuthoritativeTaskStore` or `BudgetRepository` methods to retrieve ledgers, which would return properly typed `BudgetLedger` objects with correct schema. If raw SQL is required for performance, validate the returned object against a schema before using it. |

| 58 | high | Unknown | - | ```typescript if (stageBudget != null && stageBudget.maxCostUsd > 0) { checks.push({ scope: "stage", projected: next, limit: stageBudget.maxCostUsd }); } ``` For stage budget checking, `projected` is set to `next` (the next step's estimated cost), not `currentStageCost + next`. This means the stage budget check only validates whether the next step alone would exceed the stage limit, not whether cumulative spending in the stage has exceeded it. If a stage has a $1 limit and three steps of $0.40 each have run, the cumulative is $1.20 (over limit) but the check only looks at the next step's $0.40, which is under $1. | todo | - | Track `currentStageCostUsd` in `ExecutionChainBudgetSpend` and use `currentStageCostUsd + next` as the projected value for stage budget checks. |

| 59 | high | Unknown | - | `promoteCanary()` validates `shouldPromoteCanary()` which only checks rolloutPercent and errorRate/latency/successRate thresholds, but never calls `TrafficRoutingService.checkCanaryHealth()`. The health score in `DeploymentSlotRecord` is never consulted during promotion. This means a canary with degraded `healthScore` in the deployment slot table could still be promoted to active. | todo | - | Integrate `checkCanaryHealth()` into the promotion gate. Require both `shouldPromoteCanary()` AND `checkCanaryHealth().healthy` before allowing promotion. |

| 60 | high | Unknown | - | Same jitter ordering bug as Issue 1 — jitter applied after final cap, so when delay is already at `maxDelayMs` the jitter is clamped away. Additionally, the reliability variant has NO final cap on the jitter result at all: `return Math.floor(cappedDelay + jitter);` can exceed `maxDelayMs` because jitter is added AFTER the pre-cap value. | todo | - | Apply jitter before cap and cap the final result: `return Math.min(Math.floor(cappedDelay + jitter), this.maxDelayMs);` |

| 61 | high | Unknown | - | In `runGC`, the checkpoint file is deleted first (line 171, `rmSync`) and then `removeCandidateFromManifests` is called (line 169). If manifest update fails (caught at line 460-470), the file is already gone but the manifest still references the checkpoint. This creates an inconsistency where manifests point to non-existent files. The error is logged at "warn" level but deletion continues. | todo | - | Reverse the order: update manifests BEFORE deleting files. Wrap the entire operation in a try-cancel pattern: validate manifests can be updated before removing any files. On any manifest update failure, abort the entire GC run. |

| 62 | high | Unknown | - | ```typescript function parseOutputs(outputsJson: string): Record<string, unknown> { try { const parsed = JSON.parse(outputsJson) as unknown; return parsed != null && typeof parsed === "object" && !Array.isArray(parsed) ? parsed as Record<string, unknown> : {}; } catch { return {}; } } ``` If JSON parsing fails, the function returns an empty object `{}` silently. Callers cannot distinguish between valid empty objects and parse failures. This can lead to data loss where invalid payloads are treated as valid empty outputs. | todo | - | Either throw a typed error or return a result that indicates failure: ```typescript function parseOutputs(outputsJson: string): { success: true; data: Record<string, unknown> } \| { success: false; error: string } { try { const parsed = JSON.parse(outputsJson) as unknown; if (parsed != null && typeof parsed === "object" && !Array.isArray(parsed)) { return { success: true, data: parsed as Record<string, unknown> }; } return { success: true, data: {} }; // intentional empty } catch (err) { return { success: false, error: err instanceof Error ? err.message : "parse failed" }; } } ``` |

| 63 | high | Unknown | - | The `DomainsRuntimeOrchestrator` class calls `buildDependencyServiceIds()` which calls `this.registry.isInitialized()` on dependency service IDs. However, the orchestrator registers itself as a service that depends on all the bootstrap services it is trying to initialize. This creates a potential circular dependency during the `startup()` method if any bootstrap service tries to access the orchestrator before initialization is complete. | todo | - | Ensure bootstrap services do not reference the orchestrator during their initialization phase. Consider deferring the orchestrator registration until after all bootstrap services are initialized. |

| 64 | high | Unknown | - | Three catch blocks in the commit phase silently swallow errors without logging. The actual error message is lost, making post-incident forensics impossible. | todo | - | Log the caught error with `logger.error` and include it in the execution log entry. |

| 65 | high | Unknown | src/domains/recipes/recipe-executor.ts:111 | The catch block captures the error but does not call `logger.error`. Recipe execution failures are completely invisible to the observability stack. | todo | - | Add `logger.error` before returning. |

| 66 | high | Unknown | - | `transition()` validates lifecycle state change but the service holds agent state in private Maps. The registry can diverge from what the service tracks internally. | todo | - | Use the canonical agent-registry module as source of truth. |

| 67 | high | Unknown | - | `advanceCanary()` evaluates `shouldPromoteCanary()` but never checks the deployment slot state from `TrafficRoutingService`. The `canaryProgress` Map stores only the metrics, not the slot's actual health/trafficWeight status. A race condition exists: traffic could be shifted away from canary before the advancement is recorded. | todo | - | Add a slot state consistency check in `advanceCanary()`. Query `TrafficRoutingService.getActiveSlot("canary")` and verify `trafficWeight > 0` before advancing. |

| 68 | high | Unknown | - | In `executeWithStats()`, the elapsed time check is: `const elapsed = startTime - Date.now();` — the subtraction is reversed. This produces a negative duration (e.g., `startTime=1000, Date.now()=1500` gives `elapsed=-500`), so the `elapsed >= this.maxDurationMs` check is always false at the start of each attempt. Max total duration budget is never enforced during `executeWithStats()`, allowing unbounded retry loops to run past the configured `maxDurationMs`. | todo | - | Change to `const elapsed = Date.now() - startTime;` |

| 69 | high | Unknown | - | `worker-load-balancing.ts` `computeWorkerLoadScore()` (line 75) and `execution-dispatch-support.ts` `computeLoadScore()` (line 43) use different algorithms for the same conceptual "load score". The worker version uses `activeLeaseRatio` + `saturationPenalty` + `backlogPenalty` + `cpuPenalty` (line 78-83), while the dispatch support version uses different weighting. `coordinator-load-balancing-service.ts` `computeCoordinatorScore()` (line 127) yet again uses a different formula. These three load scoring algorithms are not aligned. | todo | - | Create a single canonical `computeLoadScore()` utility with documented weighting that is shared across all three modules (worker-load-balancing, execution-dispatch-support, coordinator-load-balancing). The weighting differences cause inconsistent selection behavior across layers. |

| 70 | high | Unknown | - | ```typescript } catch (error) { return { success: false, errors: [{ message: error instanceof Error ? error.message : "Unknown error", extensions: { code: GRAPHQL_ERROR_CODES.GRAPHQL_EXECUTION_ERROR }, }], }; } ``` The raw error message from any underlying exception is exposed directly in the GraphQL response. This includes implementation details that may leak sensitive information (database messages, file paths, internal service names). | todo | - | ```typescript } catch (error) { // Log full error for debugging but return sanitized message to client this.logger.error("graphql.execution_failed", { error }); return { success: false, errors: [{ message: "An internal error occurred while processing your request", extensions: { code: GRAPHQL_ERROR_CODES.GRAPHQL_EXECUTION_ERROR }, }], }; } ``` |

| 71 | high | Unknown | src/sdk/client-sdk/api-client.ts:388 | `wrapRequestBody()` wraps all SDK request bodies in a `ContractEnvelope` per the five-plane boundary contract §5.5. However, the API gateway's task routes (`parseCreateTaskPayload` at task-routes.ts line 283) expect a plain JSON object, not a ContractEnvelope. The envelope is never unwrapped at the gateway layer — `unwrapResponseEnvelope()` only handles response envelopes (line 430), not request envelopes. Incoming requests hit `parseCreateTaskPayload(readRequestBody(ctx.request.body))` which reads `ctx.request.body` directly, without ContractEnvelope unwrapping. | todo | - | Either (a) make the gateway routes unwrap ContractEnvelope before parsing payloads, or (b) add a middleware that detects and unwraps ContractEnvelope requests. Alternatively, make the SDK `wrapRequestBody()` conditional based on the endpoint. |

| 72 | high | Unknown | - | `normalizeWorkflowStepCheckpoint()` returns `null` when the input cannot be normalized to a valid `WorkflowStepCheckpoint` (lines 557-559). However, `readWorkflowStepCheckpoint()` calls this function at line 372 and then checks if the result is truthy at line 373 before proceeding. If the function returns `null` because the checkpoint was created with an unrecognized schema version, the function returns `null` silently — no error, no warning logged. This means corrupted or legacy checkpoint data is indistinguishable from "no checkpoint found," and the caller proceeds as if no checkpoint exists rather than failing or flagging the issue. There is also a fallback path that sets defaults for `harnessRunId`, `nodeRunId`, `planGraphId` (lines 586-590) but only if the candidate passes `isWorkflowStepCheckpoint`. If it doesn't, the function returns `null` with no indication that a schema version mismatch was the cause. | todo | - | Log a warning when normalization fails due to schema version mismatch. Add a specific error type `CheckpointSchemaVersionMismatchError` that is thrown rather than returning `null`. In `readWorkflowStepCheckpoint`, catch this error and re-throw with context about the artifact ID and storage path. |

| 73 | high | Unknown | - | Three separate catch blocks in the commit phase all silently swallow errors: ```typescript } catch { failedStepId = step.stepId; executionLog.push({ stepId: step.stepId, ... outcome: "failed", }); break; } ``` No error details are logged. The saga continues to `rollback` after marking the step failed, but the actual error message is lost. | todo | - | Log the caught error with `logger.error` and include it in the execution log entry so rollback has context. |

| 74 | high | Unknown | - | `shouldPromoteCanary()` uses fallback values when metrics are missing: ```typescript successRate ??= 1.0 errorRate ??= 0.0 ``` A canary with no reported metrics would pass promotion criteria trivially. | todo | - | Require all metrics to be present before evaluating promotion. |

| 75 | high | Unknown | - | `shouldEnterPanicMode()` returns `true` for any `reasonCode` starting with `"security."` regardless of scope or severity. | todo | - | Differentiate between security advisory prefixes and actual security incidents. |

| 76 | high | Unknown | - | Multiple code files reference observability contracts that do not exist: - `debug_inspect_health_backpressure_contract.md` - `runtime_execution_contract.md` - `event_bus_contract.md` - `event_registry_and_ops_threshold_contract.md` | todo | - | Create an ADR for the observability architecture. |

| 77 | high | Unknown | - | All 7 entity Maps grow without bound over the lifetime of the service. | todo | - | Add LRU eviction or TTL-based cleanup. |

| 78 | high | Unknown | - | `updateHealth()` passes `healthScore` directly to SQL without bounds validation: ```typescript updateHealth(slotId: string, healthScore: number): void { this.db.connection .prepare(`UPDATE deployment_slots SET health_score = ?, updated_at = ? WHERE id = ?`) .run(healthScore, now, slotId); } ``` An attacker or buggy caller could set `healthScore = 999` or `healthScore = -50`. This directly bypasses `checkCanaryHealth()` which only compares `canary.healthScore < config.healthThreshold`. | todo | - | Validate `healthScore` is in `[0, 100]` range before writing. Clamp or reject out-of-range values. |

| 79 | high | Unknown | - | `scanForGCCandidates` and `runGC` are separate operations with no transactional or locking mechanism. Concurrent GC runs on the same checkpoint directory could result in double-deletion, or one run reading manifests while another is mid-update. The service has no mechanism to prevent concurrent GC runs from racing. | todo | - | Add a file-based or database-backed lock around the GC run. Use the manifest file itself as a coordination point (write-lock pattern), or add a dedicated lock table in the database. |

| 80 | high | Unknown | src/org-governance/approval-routing/approval-routi | Approval route audit record ID is constructed as `` `approval_route_audit_${request.requesterId}_${base.matchedOrgNodeId}_${Date.now()}_${randomUUID()}` `` — embedding two entity IDs and a timestamp. This produces a 70+ character ID with embedded business data that should not be part of an identifier. The `Date.now()` portion is redundant given the UUID. | todo | - | Use `` newId("approval_route_audit") ``. Store `requesterId` and `matchedOrgNodeId` as fields in the audit record object, not in the ID. |

| 81 | high | Unknown | - | `updateTargetWeight()` sets target weight directly without range checking: ```typescript public updateTargetWeight(routeId: string, targetId: string, weight: number): TrafficRoute { const route = this.requireCompatRoute(routeId); route.targets = route.targets.map((target) => target.targetId === targetId ? { ...target, weight } : target ); route.updatedAt = nowIso(); return route; } ``` Weight values outside `[0, 100]` are accepted. Combined with `promoteCanary()` at line 302, this could set stable=150, canary=-50. | todo | - | Add bounds validation `weight = Math.max(0, Math.min(100, weight))` in `updateTargetWeight()`. |

| 82 | high | Unknown | - | Multiple catch blocks throughout the plugin SPI registry do not log errors: ```typescript } catch (error) { // no logging } ``` This means plugin loading failures, invocation errors, and queue overflow conditions will be invisible in production logs. | todo | - | Add `logger.error` calls to all catch blocks with the error context and correlation IDs. |

| 83 | high | Unknown | - | Multiple catch blocks silently swallow errors from delivery tracking operations: Line 393: ```typescript } catch (error) { this.logger.log({ level: "warn", message: "Gateway rate limit accounting failed", ... }); } ``` Line 410: ```typescript } catch (error) { this.logger.log({ level: "warn", message: "Gateway delivery tracking failed", ... }); } ``` Line 458: ```typescript } catch (trackingError) { this.logger.log({ level: "warn", message: "Gateway delivery failure tracking failed", ... }); } ``` Line 522: ```typescript } catch (error) { this.recordCircuitBreakerFailure(channel, error); throw error; } ``` Line 563: ```typescript } catch (error) { this.recordCircuitBreakerFailure(channel, error); throw error; } ``` While lines 522/563 do re-throw, they first call `recordCircuitBreakerFailure` which has its own catch at line 458 that silently swallows tracking errors. The logging at lines 393 and 410 only logs at warn level but doesn't propagate, meaning callers cannot detect that delivery tracking failed. | todo | - | Return a status from tracking methods that indicates partial failure, or introduce a composite result type that callers can inspect: ```typescript const trackingResult = deliveryService.recordDeliverySuccess(messageId, receipt.responseStatus, receipt.providerMessageId); if (!trackingResult.success) { this.logger.warn("gateway.delivery_success_tracking_failed", { messageId, ... }); } ``` |

| 84 | high | Unknown | src/sdk/client-sdk/api-client.ts:72 | `buildAuthHeaders()` sends `X-SDK-Version`, `X-Platform-Version`, and `X-Contract-Version` headers to the API gateway. However, the gateway's version routing middleware (`globalVersionRoutingMiddleware` in http-api-server.ts line 309) reads `accept-version` header instead of these X- headers. The `sdk-version-handshake.ts` middleware also reads `x-sdk-version` (line 26), but this middleware is NOT wired into the route table — it exists as a standalone service that is never instantiated or called in the route pipeline. | todo | - | Wire `SdkVersionHandshakeService` into the request pipeline for versioned routes, or remove the X- header sending from the SDK's `buildAuthHeaders()` to avoid false expectations. |

| 85 | high | Unknown | - | `advanceShift()` only checks step count, not health: ```typescript if (currentStep >= steps.length) { // Complete the shift this.applyTrafficWeights(..., 100); this.retireSlot(fromSlot); this.activateSlot(toSlot); return this.getShift(shiftId); } ``` This means a shift will advance through ALL steps regardless of whether the canary health degrades. `checkCanaryHealth()` exists but is never called by `advanceShift()`. | todo | - | Call `checkCanaryHealth()` before each step advancement. Block advancement if `healthy === false`. |

| 86 | high | Unknown | - | `MigrationRunner.up()` (line 36-38) calls `this.storage.migrate()` and returns `buildResult("up")`. The `storage.migrate()` method signature in `SqliteAuthoritativeStorageBackendHandle` (storage-backend-factory.ts line 88) is declared as `migrate(): void \| Promise<void>`. For SQLite, `migrate()` is synchronous (`void`), but for PostgreSQL it is `async` (`Promise<void>`. In `openPostgresAuthoritativeStorageBackend` (line 468), the migrate function is `async`. The `MigrationRunner.up()` method does not await the result when the backend is PostgreSQL, so the `buildResult` call happens immediately after initiating (but not waiting for) the migration. This means the status check in `buildResult` could run against a database that has not yet completed its migration, returning stale schema status. | - | - | Change `MigrationRunner.up()` to be `async` and properly `await this.storage.migrate()` when the backend is PostgreSQL. Consider having the `migrate()` method on the handle always return a `Promise<void>` for consistency. |

| 87 | high | Unknown | - | Both `CircuitBreaker.execute()` methods accept `fn: () => Promise<T>` without an `AbortSignal` parameter. Compare with `platform/model-gateway/provider-registry/circuit-breaker.ts` line 123 which correctly uses `fn: () => Promise<T>` with signal support via internal AbortController. This means callers cannot pass a caller-supplied AbortSignal, and the circuit breaker timeout cannot be short-circuited externally. | todo | - | Add optional AbortSignal parameter to `CircuitBreaker.execute()` and wire it into the timeout/abort path. |

| 88 | high | Unknown | - | `evaluateWorkersForTicket()` at lines 579-588 uses a hardcoded `heartbeatStalenessThresholdMs = 30_000` (30 seconds). This is different from `HealthService` which uses `staleWorkerThresholdMs ?? 5 * 60_000` (5 minutes, health-service.ts line 192) and different from `HttpApiServer.workerHeartbeatTtlMs` which defaults to `5 * 60 * 1000` (5 minutes, http-api-server.ts line 965). There are three different staleness thresholds in three different files, none shared. | todo | - | Consolidate to a single `WORKER_HEARTBEAT_STALENESS_MS` constant in a shared location and have all modules import it. Recommended value: 30 seconds matches dispatch's stricter threshold (30s is more conservative for dispatch correctness). |

| 89 | high | Unknown | src/sdk/cli/dispatch-execution.ts:47 | The dispatch-execution CLI creates tickets via `dispatch.createTicket()` and optionally calls `dispatch.dispatchNext()`. This is the authoritative dispatch path, but there is NO corresponding SDK method in `RetryableApiClient` to create or dispatch executions externally. CLI commands and SDK functionality are completely decoupled: CLI has full dispatch machinery while SDK has no `createTicket`, `dispatchNext`, or equivalent. This prevents external clients from programmatically creating dispatch tickets. | todo | - | Add `createExecutionTicket()` and `dispatchExecution()` methods to `RetryableApiClient`, or create a dedicated `ExecutionDispatchClient` SDK class that mirrors the CLI's dispatch functionality. |

| 90 | high | Unknown | src/platform/five-plane-state-evidence/artifacts/a | `ArtifactBundleService.build()` at line 23 uses `artifact.path` as the fallback for `finalDeliverables`: `finalDeliverables: [...(input.finalDeliverables ?? artifacts.map((artifact) => artifact.path))]`. However, `artifact-model.ts` defines `ArtifactRecord` with a `path` field (line 84 in base schema). The `ArtifactRecordSchema` uses a `preprocess` that calls `normalizeArtifactRecord` (line 103), which does NOT map `storagePath` to `path` — it only maps `sizeBytes/size` and `checksum/contentHash` and `publishStatus/status`. The `path` field is used directly in the schema, so it must be present in the input. But `artifact-store.ts` creates `ArtifactRecord` with `storagePath` (line 189) not `path`. This means `ArtifactRecord` objects created by `ArtifactStore` have a `storagePath` field but no `path` field, and when `ArtifactBundleService.build()` tries to use `artifact.path`, it will be `undefined`. | todo | - | Change `ArtifactBundleService.build()` line 23 to use `artifact.storagePath ?? artifact.path` so it works with both store-created records (which have `storagePath`) and imported records (which might have `path`). Add `path` as an alias in `normalizeArtifactRecord` that maps from `storagePath` if `path` is missing but `storagePath` exists. |

| 91 | high | Unknown | - | Multiple catch blocks silently swallow errors: ```typescript } catch { // empty — error is ignored } ``` | todo | - | Log errors at minimum with `logger.warn` so compliance evidence collection failures are traceable. |

| 92 | high | Unknown | - | The ADR states OAPEFLIR is "not an active orchestration loop" and "used only for reasoning records and audit traceability". But the actual code in `oapeflir-loop-core.ts` implements a full FSM with stage transitions that actively controls execution flow. The ADR and implementation do not match. | todo | - | Update ADR-016 to accurately reflect that OAPEFLIR is an active orchestration loop, or refactor the code to match the ADR description. |

| 93 | high | interaction | src/interaction/nl-gateway/disambiguation-handler/ | `DEFAULT_DISAMBIGUATION_CONFIG` uses `threshold: 0.7` and `lowConfidenceThreshold: 0.5`, but `nl-gateway-config-loader.ts` line 156 sets the clarification threshold to `0.80` as a fix for §39.6. The disambiguation handler's default does not match the NL gateway's default, creating a zone of doubt between 0.70-0.79 where disambiguation may not trigger despite the higher gateway threshold. | todo | - | Align `DEFAULT_DISAMBIGUATION_CONFIG.threshold` with the NL gateway default of 0.80, or make the disambiguation handler accept threshold configuration from the NL gateway config. |

| 94 | high | interaction | src/interaction/goal-decomposer/index.ts:238 | The anti-multiplication guard `decomposedGoalIds` is an in-memory Set. If `GoalDecompositionService` is instantiated multiple times (e.g., in separate request handlers), each instance has its own Set and the guard provides no protection against duplicate decomposition across instances. The decomposition result is also not shared, so callers could get different task graphs for the same goal. | todo | - | Make the anti-multiplication guard shared (e.g., via a static Map or external registry), or document that `GoalDecompositionService` must be a singleton. Consider adding an idempotency key check in the return path that validates against a distributed store. |

| 95 | high | ops-maturity | src/ops-maturity/agent-lifecycle/agent-lifecycle-s | `transition()` validates the lifecycle state change via `isValidLifecycleTransition` but the service holds agent state in private Maps. The registry `listActiveAgents()` returns agents filtered by a hardcoded state check, which can diverge from what the service tracks internally. This creates a split-brain scenario where `transition()` may succeed but the agent may not appear in "active" queries. | todo | - | Have `AgentLifecycleService` use the canonical `agent-registry` module as the source of truth for all state queries, or ensure `listActiveAgents()` receives the same agent instances the service manages. Consider adding an `onTransition` hook so the registry stays synchronized. |

| 96 | high | ops-maturity | src/ops-maturity/platform-ops-agent/self-healing-s | `simulateHealthCheck()` uses a regex test `/(rollback\|failover)/.test(operation)` combined with `componentId.length % 2 === 0` as the sole determinism source for `healthCheckPassed`. This means health checks for the same component and operation can alternate between pass/fail unpredictably based solely on string length parity. | - | - | Replace the deterministic-but-opaque length-parity check with a proper health status evaluation based on actual component state. If simulating, use a seeded random or explicit failure injection that is controllable. |

| 97 | high | ops-maturity | src/ops-maturity/platform-ops-agent/self-healing-s | `performHealingOperation()` uses a formula `deterministicScore % (action.operation === "failover" ? 5 : 4) !== 0` to determine success. This produces a deterministic-but-non-obvious success rate (~75-80%) that does not reflect actual healing effectiveness. | todo | - | Replace with a proper outcome model or make the success rate configurable via policy. Document the expected success rate behavior explicitly. |

| 98 | high | ops-maturity | src/ops-maturity/workflow-debugger/workflow-debugg | `evaluateTrace()` finds a matching breakpoint via `find()` without checking if the find succeeded. The `!` assertion on `matched` could be undefined if the breakpoints Map was populated with different `nodeRunSelector` values than the frame's `nodeRunId`. | todo | - | Add a null check on `matched` and log a warning or skip the frame if no matching breakpoint is found. Do not use `!` assertion on potentially undefined values. |

| 99 | high | ops-maturity | src/ops-maturity/agent-lifecycle/canary-controller | `shouldPromoteCanary()` uses a fallback that treats missing metrics as "perfect" (`successRate ??= 1.0`, `errorRate ??= 0.0`). This is dangerous because a canary that has not yet reported any metrics would pass promotion criteria trivially. | todo | - | Require that all metrics are present before evaluating promotion. Only apply fallback defaults when the metric key exists but the value is null/undefined. Add a `hasRequiredMetrics` guard that fails safe when data is absent. |

| 100 | high | ops-maturity | src/ops-maturity/edge-runtime/edge-runtime-sync-se | `resolveConflict()` returns a `merge` resolution for low-risk conflicts but then calls `performThreeWayMerge()` which produces a hash digest when actual payloads are digests. The merged payload is not usable for actual reconciliation because it is a hash of the combined inputs rather than a merge of the actual data. | todo | - | Implement actual payload merge logic or document clearly that `mergedPayload` is a hash reference rather than actual merged data. Downstream consumers expecting merged data may silently use the wrong value. |

| 101 | high | ops-maturity | src/ops-maturity/drift-detection/learning/rollout- | `rollback()` calls the `RollbackHandler` but does not handle the case where the handler throws. If `handler(proposalId, reason)` rejects, the state is updated to `'rolled_back'` anyway and the error propagates without being caught. | todo | - | Wrap the handler call in try/catch. If the handler throws, either retry with backoff, mark the rollout as `'rollback_failed'`, or log and rethrow with context. |

| 102 | high | ops-maturity | src/ops-maturity/chaos/chaos-experiment-scheduler. | The R21-18 fix tracks `evaluatedHypotheses` per `experimentId:hypothesisName`. However, `evaluatedHypotheses` is never cleared when an experiment ends. A subsequent run of the same experiment with the same hypothesis names would be treated as already evaluated. | todo | - | Clear `evaluatedHypotheses` entries for a given experiment when `clearSteadyStateCache()` is called or when the experiment reaches a terminal state. |

| 103 | high | ops-maturity | src/ops-maturity/emergency/platform-panic-service. | `isDirectiveExpired()` compares `directive.expiresAt` against `Date.now()`. If `expiresAt` is malformed or an empty string, `Date.parse()` returns `NaN` and the comparison `NaN <= Date.now()` evaluates to `false`, meaning the directive never expires and stays active indefinitely. | todo | - | Add a guard: if `directive.expiresAt` is null/undefined, return false. Otherwise parse and check for NaN before comparison. Throw or log an error for unparseable timestamps rather than treating them as non-expired. |

| 104 | high | ops-maturity | src/ops-maturity/emergency/panic-controller/index. | `shouldEnterPanicMode()` returns `true` for any `reasonCode` starting with `"security."` regardless of the scope or severity. A low-severity security note could inadvertently trigger platform-wide panic mode. There is no check on `activeIncidents` for security reason codes either. | todo | - | Differentiate between security advisory prefixes and actual security incidents. Require `activeIncidents > 0` or a specific severity qualifier in the reason code before entering panic mode. |

| 105 | high | platform/five-plane-execu | src/platform/five-plane-execution/execution-engine | Budget reservation path has a conditional: if `ledgerRow` is found, it reserves budget and updates the ledger. However, if `ledgerRow` is undefined (e.g., ledger doesn't exist yet), the code silently skips reservation without any error or fallback. The task continues without budget protection, which violates R4-27 (INV-RUN-001) requirements for canonical execution tracking. | todo | - | If `ledgerRow` is null/undefined, either create the ledger on demand or throw a `ValidationError` with code `budget_ledger.not_found`. Do not silently skip budget reservation - the harness run requires a ledger to track execution costs. |

| 106 | high | platform/five-plane-execu | src/platform/five-plane-execution/budget-allocator | In `reserve()`, after creating the result and updating ledgers, the code calls `persistLedger()` for both `input.ledger` and each hierarchy ledger. However, the `reserveBudgetHardCap()` call at line 228 already persists via `reserveBudgetHardCap`. If `persistLedger()` at line 244 uses a CAS update with `expectedVersion = input.expectedVersion`, but `result.ledger.version` may have been incremented by `reserveBudgetHardCap`, creating a mismatch. | todo | - | After calling `reserveBudgetHardCap()`, use `result.ledger.version` as the expected version for the subsequent `persistLedger()` call, not `input.expectedVersion`. The version has already been incremented by the reservation, so using the old expected version will cause CAS failures in concurrent scenarios. |

| 107 | high | platform/five-plane-execu | src/platform/five-plane-execution/side-effect-mana | `applyReconciliation()` checks for lease and fencing token only when targetStatus is one of `["confirmed", "compensation_required", "compensating", "compensated"]`. However, if the reconciliation result is `"not_found"` with `nextAction = "compensate"`, the target status becomes `"compensation_required"`. But if the side effect is in an early state (e.g., "proposed"), the fencing token check is skipped, which could allow an unlease-protected side effect to be marked for compensation incorrectly. | todo | - | Verify the fencing token check applies regardless of the side effect's current status. Consider adding a comment or assertion that the side effect was in a lease-protected state before reconciliation can be applied. |

| 108 | high | platform/five-plane-execu | src/platform/five-plane-execution/reconciliation-w | `isReconciliationExpired()` computes `now - createdAt >= window` using `Date.now()` for `now`. However, `reconciliationCreatedAt` is parsed via `new Date(reconciliationCreatedAt).getTime()`. If `reconciliationCreatedAt` is malformed (e.g., `"invalid-date"`), `getTime()` returns `NaN` and `now - NaN = NaN`. `NaN >= window` is `false`, so the reconciliation would never expire. This could cause infinite retry loops for unparseable timestamps. | todo | - | Add validation: if `isNaN(createdAtMs)`, return `true` (expired/unparseable) or throw an error with a specific error code `reconciliation.invalid_timestamp`. Do not allow unparseable timestamps to bypass expiry logic. |

| 109 | high | platform/five-plane-execu | src/platform/five-plane-execution/execution-engine | `transitionTaskTerminalState()` is called with `lastExecutionId` which is derived from `store.execution.listExecutionsByTask(taskId).at(-1)`. If no execution exists for the task, `lastExecution?.id` would be `undefined` and fallback to `newId("exec")`. However, the transition service's `applyTaskTerminalState()` expects a valid executionId for the CAS update of execution status. An execution record may not exist if the task failed before any execution was created, causing `execution.transition_cas_failed` error. | todo | - | Before calling `transitionTaskTerminalState()`, verify that at least one execution record exists. If no executions exist, do not attempt to transition execution status - only transition task, workflow, and session. Add a guard: `if (!lastExecution && shouldTransitionExecution) { throw new Error("task.no_execution_record"); }` |

| 110 | high | platform/five-plane-execu | src/platform/five-plane-execution/dispatcher/execu | The lease acquisition and ticket claim are within a `db.transaction()` block, which is correct for atomicity. However, the code calls `leaseResult = this.leases.acquireLeaseWithinTransaction(...)` which ALSO starts a transaction (line 118 in execution-lease-service). Nested transactions in SQLite work via savepoints, but the logic is complex. If the inner transaction fails and rolls back, the outer transaction continues with an invalid lease result. | todo | - | Verify that `acquireLeaseWithinTransaction()` is designed to be called within an already-established transaction boundary. If so, document that the method uses savepoints rather than full transaction nesting. If not, refactor so the outer transaction calls a non-transactional lease acquisition method. |

| 111 | high | platform/five-plane-execu | src/platform/five-plane-execution/state-transition | `TaskTransitionService.apply()` performs a CAS update using `updateTaskStatusCas()` which returns `affected === 0` on failure. If the transition fails due to concurrent modification, the method throws an error but does NOT roll back any other state changes made in the same transaction block. However, looking at the caller chain, `transition()` wraps `apply()` in `db.transaction()`, so the entire transaction would roll back. The danger is that `apply()` is also called directly (bypassing `transition()`) in some code paths, where it would not be wrapped in a transaction. | todo | - | In `TaskTransitionService.apply()`, wrap the CAS update and event emission in `this.db.transaction()` consistently, even when called directly. Never allow a failed CAS update to emit an event while leaving the database in an inconsistent state. The `apply()` method should always manage its own transaction boundary for atomicity. |

| 112 | high | platform/five-plane-execu | src/platform/five-plane-execution/worker-pool/exec | The catch block at line 579 catches all errors and maps them to either `"invalid_terminal_transition"` (if message contains "invalid_transition") or `"authoritative_store_unavailable"`. This blanket mapping could hide real bugs. For example, a `RangeError`, `TypeError`, or security exception would be silently mapped to store errors, causing incorrect diagnostic attribution. | todo | - | Log the actual error with its stack trace before mapping. Distinguish between state machine errors (expected) and programming errors (unexpected). Consider a catch block that re-throws unexpected errors while only absorbing expected ones. |

| 113 | high | platform/five-plane-execu | src/platform/five-plane-execution/dispatcher/execu | The poison pill handling path calls `shouldInvalidatePoisonPill()` and then `invalidatePoisonPillTicket()`. However, between these calls, there is no guarantee that the ticket state hasn't changed (e.g., a worker could claim it). The invalidation logic at line 956 uses `store.worker.invalidateExecutionTicket?.()` which has an optional call - if the method is not implemented on the store, the ticket silently remains active. | todo | - | Within `invalidatePoisonPillTicket()`, verify the ticket is still in a state that can be invalidated before proceeding. Check that `ticket.status === "pending"` and that no lease has been granted since the poison pill was detected. If the ticket has been claimed, either skip invalidation or escalate for human review. |

| 114 | high | platform/five-plane-execu | src/platform/five-plane-execution/lease/execution- | The fence token initialization at line 173 uses `getLatestFencingToken(input.executionId) + 1`. If `getLatestFencingToken()` returns 0 (when no prior lease exists), the first lease gets fencing token = 1. However, `validateWriteAccess()` at line 683 compares `input.fencingToken !== activeLease.fencingToken`. This means workers must track the correct fencing token starting from 1. If a lease is created but never used (no writeback), and then a new lease is created, the fencing token increments. This is correct. But the `getLatestFencingToken()` method falls back to `workerStore.getLatestFencingToken?.() ?? 0` - if the store doesn't implement this method, every lease would get token 1, making fencing ineffective. | todo | - | Verify that `getLatestFencingToken` is implemented in all worker store implementations. If the method is missing, throw an error rather than silently using 0. A fallback of 0 defeats the entire fencing token mechanism and allows split-brain scenarios. |

| 115 | high | plugins | src/plugins/validators/basic-evaluator.ts:432 | The `evaluate()` and `produceHarnessDecision()` methods both call `evaluateWithLegacyScoring()` and return `EvaluatorAssessment`. However, the `DomainValidatorPlugin` interface (plugin-spi.ts lines 158-192) defines `validate()` as returning a `ValidationResult` with `{ valid, errors, suggestions, evaluation }` - NOT `EvaluatorAssessment`. The `evaluate()` method has a different return type (`EvaluatorAssessment` with fields like `deviationAnalysis`, `riskAssessment`, `recommendations`). If code calls `evaluate()` expecting the interface's `ValidationResult`, it will receive an incompatible object. This is a type safety violation. | todo | - | Ensure `evaluate()` returns an object conforming to `ValidationResult`, or rename the method to avoid confusion with the interface contract. Document the divergence clearly if the dual-return-type behavior is intentional for internal use. |

| 116 | high | plugins | src/plugins/adapters/crm-adapter.ts:77 | `healthCheck()` is declared `async` and returns `Promise<boolean>`. However, the body contains `policy.evaluate(...).allowed` without `await`. Since `evaluate()` on `NetworkEgressPolicyService` returns a `Promise`, accessing `.allowed` on the unresolved promise will always be `false` (property access on a Promise object). The actual policy result is never awaited, so `healthCheck` always returns `false` for CRM adapter. | todo | - | Change line 78 to `return await policy.evaluate(...).allowed;` |

| 117 | high | plugins | src/plugins/adapters/game-dev-adapter.ts:30 | `healthCheck()` returns `true` when `credentialFingerprint == null`. This means the Unity adapter reports as "healthy" even when it has not been authenticated. An unauthenticated adapter should not be considered healthy. | todo | - | Change the logic so that if `credentialFingerprint == null`, return `false`. The correct pattern is: `return credentialFingerprint != null && gameDevPolicy.evaluate(...).allowed;` |

| 118 | high | plugins | src/plugins/adapters/asset-production-adapter.ts:2 | `healthCheck()` only validates policy for `api.figma.com`, but the adapter's `execute()` method (line 53) routes to `cdn.figma.com` for `cdn_*` actions. The egress policy check in `healthCheck` does not cover the CDN endpoint that the adapter actually uses. | todo | - | Add `policy.evaluate("https://cdn.figma.com").allowed` to the health check, or extract a helper that checks both domains used by the adapter. |

| 119 | high | plugins | src/plugins/adapters/livestream-adapter.ts:33 | `healthCheck()` is async but the body at line 35-36 accesses `policy.evaluate(...).allowed` without `await`. The `evaluate()` method is async and returns a Promise. Accessing `.allowed` on an unfulfilled Promise yields `false`, so the health check will always return `false` for the Livestream adapter regardless of actual policy state. | todo | - | Add `await` before each `policy.evaluate()` call. |

| 120 | high | plugins | src/plugins/builtin-plugin-registry.ts:96 | `plugin.growth.crm_adapter` is registered in `BUILTIN_PLUGIN_FACTORIES` (line 109) with factory `createCrmAdapterPlugin`. However, `plugin.growth.crm_adapter` is not listed in `BUILTIN_PLUGIN_MANIFESTS` (lines 123-567). When `createBuiltinPluginWithManifest()` is called for this plugin, it will return `null` because `getBuiltinPluginManifest()` won't find a manifest (line 603-606 checks manifest existence). The plugin can be created via `createBuiltinPlugin()` but lacks a proper manifest, breaking manifest-based plugin management. | todo | - | Add a `BUILTIN_PLUGIN_MANIFESTS` entry for `plugin.growth.crm_adapter` similar to the pattern used for other adapter plugins (e.g., `plugin.growth.crm_adapter` entry around line 385-410). |

| 121 | high | plugins | src/plugins/growth-config.ts:201 | `externalAdapters: ["github", "jira"]` references `"jira"` as an external adapter, but there is no Jira adapter in the adapter registry. The `builtin-plugin-registry.ts` only has `github`, `crm`, `game-dev`, `asset-production`, and `livestream` adapters. A binding to `"jira"` would fail at activation. | todo | - | Either implement a Jira adapter or remove `"jira"` from the `externalAdapters` list. Document the intended integration if Jira is a planned future adapter. |

| 122 | high | tests/unit | - | The `RetryableApiClient` in `client-sdk` wraps all request bodies in a `ContractEnvelope` (line 405: `wrapRequestBody()`) and unwraps response envelopes (line 430: `unwrapResponseEnvelope()`). This is a core architectural decision for inter-plane messaging per R8-19. However, the UI REST client (`HttpTransport` and `DefaultRESTClient`) has **no envelope wrapping**. Requests go directly to `JSON.stringify(request.body)` (rest-client.ts line 317) and responses are unwrapped via `parseResponse()` which only handles `PlatformEnvelope<T>` not `ContractEnvelope`. This means: - UI client sends plain JSON objects; server responds with `ContractEnvelope`-wrapped payloads - The SDK's `unwrapResponseEnvelope()` checks for `envelopeId`, `schemaVersion`, `payload` fields (api-client.ts lines 432-436) - The UI's `parseResponse()` only checks for `data` field (rest-client.ts line 290) — a different envelope format | todo | - | Align the UI REST client to detect and unwrap `ContractEnvelope` responses, or document that the UI layer communicates on a different API plane where envelopes are not required. |

| 123 | high | tests/unit | - | The UI `HttpTransport.resolveRequestUrl()` (rest-client.ts lines 306-313) concatenates `baseUrl` with `path` directly, with no API version prefix handling: ```typescript const normalizedBaseUrl = this.options.baseUrl.replace(/\/$/, ""); return `${normalizedBaseUrl}${trimmed.startsWith("/") ? trimmed : `/${trimmed}`}`; ``` The SDK `buildApiUrl()` (api-client.ts lines 53-67) normalizes the API version segment and inserts it between base URL and path: ```typescript const apiVersion = normalizeApiVersionSegment(config.apiVersion); const url = new URL(`${baseUrl}/${apiVersion}/${path}`); ``` This means: - UI: `baseUrl = "https://api.example.com"`, `path = "/tasks"` → `"https://api.example.com/tasks"` - SDK: `baseUrl = "https://api.example.com"`, `apiVersion = "v1"`, `path = "/tasks"` → `"https://api.example.com/api/v1/tasks"` The SDK adds an `api/` prefix (line 47: `normalized.startsWith("api/") ? normalized : `api/${normalized}``). If the UI's `baseUrl` already includes the version, both produce the same URL. But if the UI expects version in `baseUrl` and SDK expects version in `apiVersion`, configuration must be carefully aligned. | todo | - | Ensure the baseUrl + apiVersion configuration is consistent across UI and SDK. Document the expected URL structure and verify that server-side routing expects the same format from both clients. |

| 124 | high | tests/unit | - | The UI `AuthStore` encodes principal as `userId`, `tenantId`, `roles`, `permissions` on the `AuthSession` interface (auth-store.ts lines 14-17). The SDK `ApiClientConfig.principal` uses `subject` or `principalId`, `tenantId`, `roles` (api-client-types.ts line 11). These are semantically similar but use different field names: \| UI AuthStore \| SDK principal \| \|---\|---\| \| `userId` \| `subject` or `principalId` \| \| `tenantId` \| `tenantId` \| \| `roles` \| `roles` \| \| `permissions` \| _(not in SDK principal)_ \| \| `displayName` \| _(not in SDK principal)_ \| The SDK's `wrapRequestBody()` (api-client.ts lines 388-403) builds metadata from `principal.subject` or `principal.principalId`, but the UI auth store stores this as `userId`. The UI interceptors (`createAuthInterceptor` in interceptors.ts) set the bearer token directly from `accessToken` (interceptors.ts line 122), with no principal encoding into request metadata. | - | - | Normalize the principal field names. The UI should either use `subject`/`principalId` naming convention, or the SDK should accept a principal adapter that maps UI field names to SDK expectations. Document the principal encoding contract across modules. |

| 125 | high | tests/unit | - | The UI `createIdempotencyKeyInterceptor()` (interceptors.ts line 223) generates a `crypto.randomUUID()` idempotency key and sets it on both `Idempotency-Key` and `x-idempotency-key` headers for non-GET requests (interceptors.ts lines 227-231). The SDK's `wrapRequestBody()` (api-client.ts line 410-414) accepts `idempotencyKey` in the `ContractEnvelope` but the SDK's `post()` method (api-client.ts line 130) passes the idempotency key to `wrapRequestBody()`. However: - The SDK idempotency key goes into the `ContractEnvelope.idempotencyKey` field, not an HTTP header - The UI idempotency key goes into both HTTP headers If the server expects the idempotency key as an HTTP header (as the UI sends), the SDK's envelope-based idempotency key may not be recognized. | todo | - | Ensure idempotency key handling is consistent — either both use HTTP headers or both use the envelope field. The current hybrid approach may cause duplicate submissions. |

| 126 | high | tests/unit | - | The UI REST client defines `RestHttpError` (rest-client.ts lines 62-81) with `status`, `uiAction` (redirect_to_login, access_denied, backoff_and_retry, version_not_supported), and `retryAfterMs`. The SDK defines `ApiError` (api-client.ts lines 666-678) with `category` (NETWORK, AUTH, VALIDATION, BUSINESS, CONTRACT), `statusCode`, and `isRetryable`. These are two completely different error hierarchies for the same purpose (HTTP API error classification): \| UI `RestHttpError` \| SDK `ApiError` \| \|---\|---\| \| `status: number` \| `statusCode: number \\| null` \| \| `uiAction: RestHttpUiAction` \| `category: ApiErrorCategory` \| \| `retryAfterMs: number \\| null` \| `isRetryable: boolean` \| \| No error message preservation \| `message: string` \| \| No error code branding \| Error code as message prefix \| Additionally, the SDK has `classifyApiError()` (api-client.ts lines 680-702) which maps status codes to categories: - 401/403 → AUTH - 400/422 → VALIDATION - 409/412 → BUSINESS - 500+ → NETWORK - 4xx → BUSINESS The UI's `RestHttpError.uiAction` maps differently: 401 → redirect_to_login, 403 → access_denied, 429 → backoff_and_retry, 406 → version_not_supported. 403 is mapped to `access_denied` in UI but the SDK classifies 403 as AUTH. | - | - | Align the error classification between UI and SDK. Consider a shared error classification utility, or document the intentional differences (e.g., UI needs UI-actionable error types, SDK needs business-logic error categories). |

| 127 | high | tests/unit | - | Two independent fencing token mechanisms exist with no coordination: 1. `FencingTokenService` (multi-region) manages region-level leadership with `epochCounter` incremented on `acquireLeadership()` (line 94). Token validation checks `epoch` match (lines 191-198). 2. `ExecutionLeaseService` manages per-execution leases with per-lease `fencingToken` incremented via `getLatestFencingToken(executionId) + 1` (line 173, 515). Token validation in `validateWriteAccess()` only checks workerId and leaseId (lines 703-720), never comparing against the multi-region fencing token. During a region failover via `RegionFailoverController.resolve()`, the `FencingTokenService` epoch increments but `ExecutionLeaseService` has no mechanism to invalidate leases from the demoted region. A worker with an old lease could continue writing if its lease hasn't expired, bypassing the new region's leadership. | todo | - | Add a `validateFencingToken()` call in `ExecutionLeaseService.validateWriteAccess()` that checks the multi-region `FencingTokenService` when execution crosses region boundaries. Alternatively, add a `regionId` field to `ExecutionLeaseRecord` and validate that the lease's region matches the current leader. |

| 128 | high | tests/unit | - | Pack lifecycle stages are: `draft → certifying → published → deprecated → archived`. Domain lifecycle stages are: `draft → validated → registered → canary → active → updating → deprecated → archived`. The two state machines have incompatible stage progressions: 1. Pack has `certifying` while domain has `canary` - these serve similar "testing/review" purposes but have different names and no cross-reference mechanism 2. Pack transitions `certifying → published` directly, but domain goes through `canary → active` 3. Pack has no equivalent to domain's `canary` stage - a pack can go from `certifying` (under review) to `published` (available) without an intermediate canary-like staging period 4. Pack lifecycle allows `draft → archived` transition (line 138), but domain has no such direct path | todo | - | Add `canary` stage to pack lifecycle to match domain lifecycle, or establish a formal mapping/adapter between `certifying ↔ canary` states. Document the relationship in an ADR. |

| 129 | high | tests/unit | - | The `inject()` method at lines 325-338 uses rate limit key `inject:${endpoint}`, while `handleRequest()` at lines 415-427 uses `${clientIp}:${endpoint}`. The same logical endpoint accessed via different paths (direct inject vs HTTP request) would hit different rate limit buckets. This creates an inconsistency where: 1. A client hitting the HTTP endpoint gets rate limited by `clientIp:/api/v1/tasks` 2. The same client using `inject()` directly gets rate limited by `inject:/api/v1/tasks` These are independent limiters with no coordination. A client could bypass HTTP rate limiting by routing through `inject()`. Additionally, `inject()` does not include `tenantId` in the key (line 327 uses `inject:${endpoint}` only), while the standard HTTP path includes `clientIp` which could be the same for multiple tenants behind a NAT. | todo | - | Normalize rate limit keys to include both `tenantId` (or tenant scope) and endpoint, regardless of entry path. Use a consistent format like `tenant:${tenantId}:endpoint:${endpoint}` for tenant-scoped limiting or `ip:${clientIp}:endpoint:${endpoint}` for IP-scoped. Consider adding `inject` vs `http` as a label rather than a separate bucket. |

| 130 | high | tests/unit | - | The `PromptBundle` contract defines `displayVersion: string` as a required field (line 32 of prompt-bundle/index.ts). The `createMockBundle()` helper (line 499) correctly sets `displayVersion` to the provided parameter. However, the `listBundleVersions` return type (`PromptBundleVersion`) is tested with `assert.equal(v1!.displayVersion, undefined)` at lines 407 and 410, asserting that `displayVersion` is `undefined`. This contradicts the contract where `displayVersion` is always a `string`. | todo | - | Change assertions at lines 407 and 410 to check for the actual string value: ```typescript assert.equal(v1!.displayVersion, "v1.0"); assert.equal(v2!.displayVersion, "v2.0"); ``` |

| 131 | high | tests/unit | - | Two independent intent classification systems exist: 1. `nl-gateway-support.ts` `classifyRisk()` → produces `riskClassification` with `intentType` (task_create, task_modify, query, etc.) 2. `intake-router-model.ts` `classifyIntent()` → produces `IntakeIntentClassification` with intent (query, create, modify, approve, cancel, clarify, chitchat, correction) The OAPEFLIR loop's `assess` stage (line 288) consumes `UnifiedAssessment` which contains `routingDecision.division` and `routingDecision.workflow` derived from the interaction layer's classification. However, the two classification models are incompatible: - NL Gateway produces: `task_create \| task_modify \| task_query \| approval_action \| cancel_task \| clarify \| chitchat` - Intake Router produces: `query \| create \| modify \| approve \| cancel \| clarify \| chitchat \| correction` A `create` intent in intake-router maps to `task_create` in nl-gateway, but there's no explicit mapping function. The routing decision at line 292 `assessment.routingDecision.division` receives `"coding"` as default when assessment validation fails (line 321), bypassing any intent-based routing logic from the intake layer. | - | - | Create a canonical `IntentClassification` interface in a shared contracts location, with explicit mapping functions between NL Gateway's intent taxonomy and Intake Router's taxonomy. The OAPEFLIR assess stage should receive the classified intent from the intake layer rather than deriving it independently. |

| 132 | high | tests/unit | - | `registerBreakpoint()` stores breakpoints by `planGraphId` (line 74-75), but the execution engine's `multi-step-supervisor.ts` does not query the debugger for active breakpoints before executing steps. The breakpoint evaluation happens AFTER execution via `evaluateTrace()`, not before. This means breakpoints cannot actually pause execution mid-workflow - they can only analyze what happened after the fact. The `action` field in `DebugBreakpointDefinition` supports `"pause" \| "snapshot" \| "compare"` but only `"snapshot"` and `"compare"` can work post-execution. A `"pause"` action would require pre-execution breakpoint checking. | - | - | Add a pre-execution breakpoint check in `executeStepLoop` before each step runs. The debugger should expose a method `getActiveBreakpoints(workflowId)` that the supervisor calls to determine if the current step should be paused. Implement the pause mechanism by setting a flag that skips step execution and returns `blockedForDecision: true` to the orchestrator. |

| 133 | high | tests/unit | - | `ExecutionDispatchService` supports emergency lane preemption (lines 348-388) via `ExecutionPriorityPreemptionService.preemptForUrgentTicket()`. However, when a new ticket is dispatched to replace a preempted one, `ExecutionLeaseService.releaseLease()` is not called for the preempted ticket's lease. The preemption only creates a new ticket for the urgent execution; the old ticket's lease remains active until it expires naturally. This means two executions (preempted old + urgent new) could overlap if the old worker doesn't release the lease. | todo | - | After `preemptForUrgentTicket()` succeeds, call `ExecutionLeaseService.releaseLease()` for the preempted ticket's leaseId with reasonCode `"preempted_for_urgent_dispatch"`. |

| 134 | high | tests/unit | - | Domain `gray_rollout` is the final onboarding phase before full activation. The pack lifecycle equivalent should be `published`. However: 1. `DomainOnboardingService.advance()` when completing `gray_rollout` (line 68) calls `this.promoteDomainToRegisteredIfNeeded()` and then `this.registry.activate()` (lines 71-75) 2. It does NOT verify or transition the associated pack to `published` 3. A domain could be `active` (fully onboarded) while its pack is still in `draft` or `certifying` | todo | - | When domain onboarding completes `gray_rollout` and activates the domain, also transition the associated pack to `published` via `PackLifecycleService`. |

| 135 | high | tests/unit | - | `resolveFencingTokenStoragePath()` (line 337) defaults to `join(process.cwd(), "data", "multi-region", "fencing-token-state.json")`. `ExecutionLeaseService` uses `AuthoritativeTaskStore` for persistence (lines 183, 274, 337). The fencing token state and lease state are persisted to different backends. During a region failover, the fencing token state file may not be accessible from the new region if it was stored locally in the old region. The lease state (in the SQL store) would be replicated, but the fencing token epoch counter would not be. | todo | - | Move `FencingTokenService` persistence to the same `AuthoritativeSqlDatabase` that `ExecutionLeaseService` uses, so that both states are replicated together. Alternatively, document clearly that the fencing token state file must be on shared storage accessible from all regions. |

| 136 | high | tests/unit | - | The `HarnessRun` type has `goal?: string` and `mode?: string` (contract-models.ts lines 287-288), plus `orgId` (line 285, defaults to tenantId in factory), `riskLevel` (line 289, defaults to "medium"), `riskProfile` (line 290), `ownership` (line 291), `auditRefs` (line 292), `auditTrail` (line 293), `domainId` (line 294), and `missionBinding` (line 311). `HarnessRunSchema` at lines 236-267 is missing `goal`, `mode`, `riskLevel`, `riskProfile`, `ownership`, `auditRefs`, `auditTrail`, `domainId`, and `missionBinding`. The test at line 204-212 uses `createHarnessRun` factory which populates defaults, but `validateExecutableContract("HarnessRun", run)` would strip these optional fields because they are not in the schema. | todo | - | Add missing fields to `HarnessRunSchema` as optional/required as appropriate. This is a significant schema-type divergence that could cause data loss during validation. |

| 137 | high | tests/unit | - | The R4-28 cost event WAL records hardcoded values: ```typescript provider: "minimax", model: "MiniMax-M2.7", inputTokens: 30 + index * 10, outputTokens: 12 + index * 5, costUsd: 0.001 + index * 0.0005, ``` These are placeholder calculations. The cost optimizer's `recordCost()` expects `decisionRef` (line 70) and builds recommendations based on actual cost attribution. If the execution engine writes WAL entries with fake costs, and the cost optimizer later aggregates them, the recommendations will be meaningless. Additionally, the WAL entry does not include `decisionRef` which `recordCost()` requires - the WAL insert at line 577 does not populate this field. | todo | - | Either: 1. Remove the WAL until real measurement is implemented (per earlier recommendation), or 2. Obtain actual token counts from `buildStepOutput` result which contains `llmResult?.usage`, and 3. Include a `decisionRef` or mark the WAL entry with `harnessRunId` so it can be linked to execution decisions |

| 138 | high | tests/unit | - | `FairSchedulingService.schedule()` returns `victimExecutionId` from `choosePreemptionVictim()` (line 76) when quota is exceeded. However, the preemption candidate may already have been dispatched with an active lease in `ExecutionDispatchService`. The preemption decision does not check `ExecutionLeaseService.getActiveExecutionLease(victim.executionId)` before recommending the victim. If a ticket is already claimed (lines 462-467 in dispatch service), preempting it causes a lease conflict. The worker holding the lease is not notified, and the execution may continue writing under a now-stale lease while the scheduling system believes resources were reclaimed. | todo | - | In `FairSchedulingService.schedule()`, before returning a `victimExecutionId`, call `ExecutionLeaseService.getActiveExecutionLease()` to verify the candidate does not have an active lease. If it does, exclude it from preemption or emit a warning. |

| 139 | high | tests/unit | - | Domain onboarding phase sequence is: `domain_modeling → pack_development → security_certification → gray_rollout`. The `pack_development` phase implies pack creation/development but: 1. `DomainOnboardingService.advance()` does not check or update pack lifecycle state 2. A domain could complete `pack_development` phase while its associated pack is still in `draft` or any other state 3. Pack lifecycle state changes (e.g., `draft → certifying`) are not gated by domain onboarding phase completion 4. There is no cross-service validation that "pack is ready for certification" when domain onboarding enters `security_certification` phase | todo | - | Add cross-service validation in `DomainOnboardingService.advance()` when transitioning to `security_certification` phase: verify that the associated pack (via `PackDomainAssociationService`) is in `certifying` or later stage, or transition it to `certifying`. |

| 140 | high | tests/unit | - | `evaluateTrace()` finds a matching breakpoint: ```typescript const matched = (this.breakpoints.get(planGraphId) ?? []).find((item) => item.nodeRunSelector === nodeRunId)!; ``` The `!` assertion assumes `matched` is defined, but if the breakpoints Map was populated with different `nodeRunSelector` values than the frame's `nodeRunId`, the `find()` returns `undefined` and the assertion throws. This could crash workflow debugging mid-execution. The execution engine's `multi-step-supervisor.ts` generates step outputs that could be passed to the debugger for trace evaluation, but there's no integration point that handles the debugger result. | todo | - | Add null check: ```typescript const matched = (this.breakpoints.get(planGraphId) ?? []).find((item) => item.nodeRunSelector === nodeRunId); if (!matched) continue; // skip frame if no matching breakpoint ``` Do not use `!` assertion on potentially undefined values. |

| 141 | high | tests/unit | - | `ExecutionLeaseService.acquireLeaseWithinTransaction()` grants leases without any fencing token validation. There is no call to `FencingTokenService.validateFencingToken()` to ensure the current region holds valid leadership before allowing new leases. This means: 1. Region A is primary, region B is follower 2. Region A's leadership epoch advances via failover to B 3. A worker in region A could still acquire a lease for an execution if the leadership state hasn't propagated The comment at `execution-lease-service.ts` line 172 says "Fencing token prevents split-brain" but this only refers to the per-lease token, not the multi-region leadership token. | todo | - | Add an optional `fencingToken` parameter to `acquireLease()` and validate it against `FencingTokenService` when multi-region fencing is enabled. Alternatively, add a `requireLeadershipToken()` method that `ExecutionDispatchService` calls before dispatch. |

| 142 | high | tests/unit | - | The `security_certification` onboarding phase and the `certifying` pack lifecycle stage have the same semantic purpose (security/quality review before publishing), but: 1. Completing domain `security_certification` phase does not call `PackLifecycleService.submitForCertification()` or `certifyPack()` 2. Pack `certifying → published` transition does not verify domain onboarding has completed `security_certification` 3. There is a potential for a pack to be `published` while its domain is still in `security_certification` onboarding phase, or vice versa | todo | - | Establish a formal certification gate that coordinates both: - Domain enters `security_certification` phase → pack transitions to `certifying` - Pack transitions to `published` → domain completes `security_certification` Consider adding a `CertificationCoordinator` service that enforces this coupling. |

| 143 | high | tests/unit | - | The `normalizeVersion()` function in the prompt-bundle contract (line 332-338) converts semver to integer via `major * 100 + minor * 10 + patch`, e.g., "v1.2.3" → `123`. But `PromptVersionManager.normalizeComparableVersion()` (line 91) uses `major * 100 + minor * 10 + (patch ?? 0)`. These are consistent for full semver, but the contract's `normalizeVersion()` also handles "v1" (major-only) as `major * 10` (line 345), while the prompt-version-manager's `parseVersion()` would return `patch: undefined` for "v1", yielding the same `major * 100` calculation (different result). Specifically, `"v1"` in contract normalization yields `10`, but `"v1"` parsed by prompt-version-manager yields `major=1, minor=0, patch=0` → `1*100 + 0*10 + 0 = 100`. This mismatch means version comparisons in tests (which use PromptVersionManager) may not reflect actual contract behavior. | todo | - | Align the version normalization logic. The contract's `normalizeVersion()` at line 345 should be reviewed for the `v1` → `10` mapping, or PromptVersionManager's `normalizeComparableVersion()` should be updated to match. Document which normalization is authoritative. |

| 144 | high | tests/unit | - | ProactiveAgentService can fire triggers that result in `actionType: "create_task"` (line 44), which calls `enqueueSuggestion()` and creates a suggestion. However, the OAPEFLIR loop has its own execute stage that processes tasks independently. There's no coordination mechanism to prevent: 1. ProactiveAgent firing a trigger that creates a task while that same task is already being processed by an OAPEFLIR loop 2. Duplicate execution: ProactiveAgent's `enqueueSuggestion()` (line 505) doesn't check if a task with the same trigger goal is already running in OAPEFLIR 3. Resource contention: Both loops can execute concurrently without sharing budget information The `ProactiveTrigger` interface (line 5-10) has no field to track if a task derived from it is currently in an OAPEFLIR execution cycle. The ProactiveAgentService stores state in its own Map (line 225 `states`), completely independent of OAPEFLIR's `loopPlanGraphBundle` and execution state. | todo | - | Add a `activeTaskRef` field to `TriggerRuntimeState` that records the taskId when a trigger fire results in a task being sent to OAPEFLIR. Before allowing a trigger to fire, check if an active task already exists for that trigger. Add ADR documenting the trigger-to-OAPEFLIR handoff protocol. |

| 145 | high | tests/unit | - | `simulateHealthCheck()` (lines 51-71) uses: ```typescript const healthCheckPassed = !/(rollback\|failover)/.test(operation) \|\| componentId.length % 2 === 0; ``` And `performHealingOperation()` (lines 210-213) uses: ```typescript const deterministicScore = action.targetComponent.length + action.operation.length + (action.reasonCode?.length ?? 0); return deterministicScore % (action.operation === "failover" ? 5 : 4) !== 0; ``` This produces deterministic-but-non-obvious success rates (~75-80%) that have no relationship to actual component health. If the self-healing service declares a component "healthy" but the execution state machine (`runtime-state-machine.ts` / `transition-service.ts`) tracks it as "failed" or "degraded", there is a direct conflict. The execution engine has no mechanism to respect or query the self-healing service's health assessments. | - | - | Replace the deterministic-but-opaque length-parity check with a proper health status evaluation. If simulating, make the success rate configurable via policy and document the expected behavior. Add an integration point in the execution engine to query the self-healing service before marking steps as failed. |

| 146 | high | tests/unit | - | `RegionFailoverController.resolve()` (line 292) computes the failover decision including `reconciliationResult` but `ExecutionLeaseService.reclaimExpiredLeases()` is never called for the demoted region's leases. The reconciliation job (lines 381-393) checks for "pendingWriteCount, openBudgets, outboxMessages, restrictedWrites" but not for active execution leases. If region A fails over to region B, any leases held by workers in region A remain active until their TTL expires naturally. Workers in region A could continue executing against the old epoch. | todo | - | After failover, the `ReconciliationJobInput` should include a step to call `ExecutionLeaseService.reclaimActiveLease()` for all executions that had leases in the demoted region. Add a `regionId` field to `ExecutionLeaseRecord` to enable efficient querying of leases by region. |

| 147 | high | tests/unit | - | `validateSandboxCompatibility()` checks that restricted tools require `securityLevel=restricted` in domain capabilities. However: 1. Pack manifest has its own `sandboxTier` field (business-pack-manifest.ts line 251) that is NOT validated against the domain's `securityLevel` 2. A pack could declare `sandboxTier: "read_only"` while associated with a domain that has `securityLevel: "restricted"` - this is a mismatch 3. `PackDomainAssociationService` links packs to domains but performs no compatibility validation 4. `DomainRegistryService.buildCapabilityEntry()` (line 332) only uses `domain.capabilities.securityLevel` for `trustTier`, ignoring pack's `sandboxTier` | todo | - | Add a cross-module sandbox tier / security level compatibility check in `PackDomainAssociationService.associatePackWithDomain()` and in `DomainRegistryService.buildCapabilityEntry()`: - If pack `sandboxTier === "read_only"`, domain `securityLevel` must be `"standard"` or `"elevated"` - If pack `sandboxTier === "restricted_exec"`, domain `securityLevel` must be `"restricted"` |

| 148 | high | tests/unit | - | `HttpApiServer.dispatchRequest()` calls `authenticateOptionalPrincipal()` at line 588 which uses the `authService` (likely session-based user auth). Service-to-service calls via `x-service-id` headers would be validated by `extractServiceAuth()` but this is not called in the standard HTTP request path - it's called by internal services that need service auth. The comment at `service-auth.ts` lines 3-7 says: "§11.2: Internal API mTLS / service token authentication §8: Worker pool communication requires mTLS + service identity". Worker pool communication flows through `HttpApiServer` (via `WorkerRegistryService` usage at line 966), but the server's `dispatchRequest()` does not call `extractServiceAuth()` - it only calls `authenticateOptionalPrincipal()`. This means: 1. User HTTP requests → `authenticateOptionalPrincipal()` (session-based) 2. Service HTTP requests (with `x-service-id` headers) → `authenticateOptionalPrincipal()` ignores service headers, falls through to session auth 3. Internal service calls → `extractServiceAuth()` directly (not via HttpApiServer) The layering is inconsistent - `HttpApiServer` doesn't distinguish between user and service callers. | todo | - | In `HttpApiServer.dispatchRequest()`, check for service auth headers (`x-service-id`, `x-mtls-cert`) before falling through to `authenticateOptionalPrincipal()`. If service headers are present, call `extractServiceAuth()` to validate the service identity instead of user session auth. Document the precedence rules (service auth takes priority over user auth when both are present). |

| 149 | high | tests/unit | - | `GoalDecompositionService` produces a task graph (via `decompose()`) with `taskIds`, `dependencies`, and `executionOrder`. However, OAPEFLIR's plan stage (line 362-373) builds a `PlanGraphBundle` directly from `input.workflow` (the `PlannedWorkflow`), not from the goal decomposition output. The `OapeflirLoopInput` interface (line 80-92) receives `workflow: PlannedWorkflow` but has no field for `goalDecomposition`. The Assess stage's `observedTask` (line 262-286) is built from `TaskSituationBuilder` with no reference to a decomposed goal graph. If a user request goes through goal decomposition first, the resulting task graph is stored in `GoalDecompositionResult` but never passed to OAPEFLIR's planning stage. OAPEFLIR rebuilds its own plan from `workflow.executionSteps`, ignoring any pre-computed decomposition. | todo | - | Add `goalDecomposition?: GoalDecompositionResult` field to `OapeflirLoopInput`. In the plan stage, if `goalDecomposition` is provided, use its task graph structure as the authoritative task list rather than `workflow.executionSteps`. Add validation that the workflow steps align with the decomposed task graph. |

| 150 | high | tests/unit | - | `authenticateOptionalPrincipal()` at line 588 calls into the auth service which internally calls `validateAccessToken()`. The `validateAccessToken()` function at line 261-308 consumes the access token for validation (checks expiry, revocation, context binding). However, in `TenantScopeFilter.evaluate()`, the `principal` object passed is the result of authentication, not the validated session. If the session was validated in `authenticateOptionalPrincipal()` but the session object was not propagated to downstream handlers (only the `principal` was extracted), then `TenantScopeFilter.evaluate()` would be working with a principal that has `tenantId: null` (due to Issue 2). The session validation in control-plane and the tenant scope evaluation in interface are disconnected. | todo | - | Ensure the `Session` object (or at minimum the `tenantId` from the session) is propagated from `authenticateOptionalPrincipal()` through to the `RouteContext.principal`. Add a comment documenting the flow from `validateAccessToken()` → `principal` → `TenantScopeFilter.evaluate()`. Verify that `authService.authenticate()` (from `api-auth-service.ts`) returns or stores `tenantId`. |

| 151 | high | tests/unit | - | The contract at line 191-196 explicitly rejects a bundle where `deprecated === true` AND `lifecycleStatus === "active"`: ```typescript if (bundle.metadata.deprecated === true && bundle.metadata.lifecycleStatus === "active") { throw new ValidationError( "prompt_bundle.invalid_lifecycle_status", "Deprecated bundles cannot retain lifecycleStatus active.", ); } ``` This is a critical invariant but no test verifies this rejection. The test at lines 415-423 only checks that `deprecated=true` bundles are marked correctly, not that the combination with `lifecycleStatus="active"` throws. | todo | - | Add test: ```typescript test("createPromptBundle throws when deprecated=true but lifecycleStatus=active", () => { assert.throws( () => createPromptBundle({ name: "Test", version: 1, displayVersion: "v1.0", domain: "test", taskType: "simple", systemPrompt: { content: "test", templateVariables: [], channel: "system" }, compatibilityMatrix: emptyCompatibilityMatrix(), metadata: { deprecated: true, lifecycleStatus: "active" }, }), ValidationError, ); }); ``` |

| 152 | high | tests/unit | - | The existing review identified that `PlatformPanicService.evaluateExecution()` bypasses blocking for actors in the `allowList`. The `maybeInjectWorkflowCrash()` at line 579 of `multi-step-supervisor.ts` could be triggered for actors NOT in the allowList while allow-listed actors proceed normally. This means a panic-mode enforcement could be selectively bypassed based on actor identity, creating an inconsistent security boundary. The `maybeInjectWorkflowCrash` uses `crashInjection` config that is passed from the orchestration input - if an attacker can influence `input.crashInjection`, they could use an allow-listed actor to bypass panic enforcement. | todo | - | Ensure panic mode enforcement is consistent regardless of actor identity. The allowList bypass should only apply to non-destructive operations (monitoring, read-only access), not to execution loops that could propagate failures. Document the security model for allowList in panic mode. |

| 153 | high | tests/unit | - | Multiple confidence threshold constants exist: 1. `intake-router-model.ts` line 55: `CONFIDENCE_THRESHOLD = 0.80` for LLM intent extraction 2. `nl-gateway-support.ts` line 156: `0.80` for clarification threshold per §39.6 3. `disambiguation-handler/index.ts` line 47-52: `0.70` for disambiguation threshold These are not synchronized. The disambiguation handler default (0.70) is lower than the NL gateway's clarification threshold (0.80), creating a gap zone between 0.70-0.79 where disambiguation does not trigger despite the higher gateway threshold wanting clarification. | todo | - | Create a shared constants module (e.g., `src/platform/contracts/constants/intake-thresholds.ts`) that exports all intake-related thresholds. Both interaction and orchestration modules should import from this canonical source. Add ADR documenting the threshold synchronization requirement. |

| 154 | high | tests/unit | - | `execute()` can trigger a healing operation (`performHealingOperation`) while the execution engine is actively running steps in `executeStepLoop`. The self-healing service has no coordination with the execution loop - it simply performs the operation and returns a receipt. If `performHealingOperation` restarts or fails over a component that the execution loop is currently using (e.g., a database connection, a model endpoint), the in-flight execution could fail unexpectedly. There's no mutex or coordination to ensure self-healing doesn't disrupt active executions. | todo | - | Add a coordination mechanism: self-healing service should check with the execution engine before performing disruptive operations. Consider adding a `ExecutionGuard` interface that self-healing calls before performing restart/failover operations. The execution engine should be able to signal "in-flight" state that blocks disruptive healing. |

| 155 | high | tests/unit | - | `TenantScopeFilter.evaluate()` requires a `TaskProjectionScopeResolver` function (passed to constructor at line 25) to look up task tenant scope. The `HttpApiServer.dispatchRequest()` has no reference to `TenantScopeFilter` and never calls it. This means tenant scope filtering is defined in the interface module but not actually invoked in the request handling path. If `TenantScopeFilter` is intended to be used by downstream handlers (e.g., channel gateway), the resolver function is not injected into the handlers. The `TaskProjectionScopeResolver` type at line 22 is a function `(taskId: string) => TaskProjectionScope \| null`, but there's no way to provide this from `HttpApiServerOptions`. | - | - | Either: 1. Integrate `TenantScopeFilter` into `HttpApiServer.dispatchRequest()` by injecting it and calling `evaluate()` before routing, or 2. Document that `TenantScopeFilter` is for downstream handlers (e.g., channel gateway) and provide a way to inject the resolver via `HttpApiServerOptions`, or 3. Remove `TenantScopeFilter` from the interface module if it's not used there and move it to the appropriate consumer module. |

| 156 | high | tests/unit | - | `RuntimeRecoveryService` returns `suggestedAction: "resume_same_worker"` (line 761-764) for executions with `status === "executing"` that are below attempt thresholds. This recommendation does not check whether the original worker is in the same region as the current primary. If a region failover occurred, the "same worker" may be in the demoted region and unreachable. Retrying on the same (now unreachable) worker would stall indefinitely. | todo | - | Add `workerRegionId` to `RuntimeRecoveryCandidate`. In `inferSuggestedAction()`, if the action is `resume_same_worker`, verify that `workerRegionId` matches the current primary region. If not, downgrade to `retry_new_ticket`. |

| 157 | medium | Deep-Cross: http + tcp +  | src/platform/five-plane-interface/channel-gateway/ | `extractConnectionParams()` parses `taskId` and `lastEventId` from the URL query string without sanitization. While authentication is correctly taken from the authoritative `Sec-WebSocket-Protocol` header, the URL params are used for subscription routing (`new URL(req.url, "http://127.0.0.1")` at line 500). Malformed or malicious query values (e.g., `taskId=<script>`) could propagate into internal state if not properly validated downstream. | todo | - | Validate `taskId` format using the same pattern used for internal task ID validation (ULID or similar). Reject values that do not match the expected identifier format. |

| 158 | medium | Deep-Cross: http + tcp +  | src/platform/five-plane-interface/api/http-api-ser | `keepAliveTimeout = SERVER_KEEP_ALIVE_TIMEOUT_MS` (5000ms) at line 210 and `headersTimeout = SERVER_HEADERS_TIMEOUT_MS` (60000ms) at line 209 are set. However, during graceful shutdown at lines 1019-1021, all sockets in `activeSockets` are destroyed with a hard timeout of `SHUTDOWN_DRAIN_TIMEOUT_MS` (10000ms). This means sockets in `headersTimeout` state (waiting for headers) will be destroyed before their timeout triggers, but more critically, the `activeSockets` set and the server's internal socket management can race. The socket destroy at line 1020 bypasses the normal connection closure handshake. | todo | - | Track in-flight request count and wait for their completion before force-destroying. Consider using `server.closeIdleConnections()` instead of destroying all sockets directly. |

| 159 | medium | Deep-Cross: http + tcp +  | src/platform/five-plane-interface/channel-gateway/ | `fetchWithTimeout()` sets an AbortController timeout and passes `signal` to fetch, but there is no guarantee that the underlying connection will be aborted within the specified timeout if the server is slow to respond. Additionally, if the `fetch` call hangs indefinitely due to DNS or TCP-level issues, the abort timer may not fire if the network stack does not respect AbortController for all phases of the fetch. | todo | - | Use a more aggressive timeout mechanism such as `setTimeout` with immediate abort, and configure the fetch with a `keepalive` or agent that enforces per-request timeouts at the connection level. |

| 160 | medium | Deep-Cross: http + tcp +  | src/platform/five-plane-interface/ingress/distribu | The `DistributedRateLimiter` constructor throws `Error("rate_limiter.redis_required_in_production")` when Redis is not configured and `NODE_ENV === "production"` and `allowLocalFallbackInProduction !== true`. However, `HttpApiServer` at line 1136 passes `allowLocalFallbackInProduction: true`, which bypasses this safeguard. This creates a production deployment that falls back to in-memory rate limiting without explicit opt-in at the configuration level, masking potential Redis connection failures. | todo | - | Make the fallback behavior explicit in environment configuration. Require a specific env var like `AA_API_RATE_LIMIT_ALLOW_LOCAL_FALLBACK=1` rather than embedding it in code. |

| 161 | medium | Deep-Cross: http + tcp +  | src/platform/five-plane-interface/api/http-server/ | `normalizeHeaders()` lowercases all header names, but does not validate or strip duplicate headers that could arise from HTTP Request Smuggling attacks (e.g., two `Content-Length` headers with different values). Node.js `http` module normalizes some headers but not all smuggling patterns. | todo | - | Add duplicate header detection: if a header name appears twice after lowercasing, reject the request with a 400 status before processing. |

| 162 | medium | Deep-Cross: http + tcp +  | src/platform/five-plane-interface/channel-gateway/ | `sendTaskUpdate()` drops events for slow consumers without notifying the client. The `pendingAcks` map grows indefinitely if acknowledgements are lost. There is no mechanism to detect and clear orphaned `pendingAcks` entries when sequence gaps are detected (via `stream_gap` messages). A client that missed many events could cause `pendingAcks` to accumulate unbounded memory. | todo | - | Implement a maximum pending ACK count per client. When exceeded, send a `stream_gap` message to force the client to resync from a known position, then clear stale entries. |

| 163 | medium | Deep-Cross: http + tcp +  | src/platform/five-plane-interface/channel-gateway/ | `taskEventHistory` is a `Map<string, Array<{eventId, event}>>` with per-task history capped at `maxTaskEventHistoryPerTask` (default 200). The eviction loop at lines 632-635 uses `history.shift()` which is O(n). Under high subscription churn, this can cause performance degradation. Additionally, history entries are never cleaned up if a task is unsubscribed and never resubscribed, except via `scheduleTaskHistoryCleanup` which has a `taskHistoryRetentionMs` delay. | todo | - | Use a ring buffer or indexed array for O(1) eviction instead of `shift()`. Consider adding a `taskEventHistory.delete(taskId)` call path for completed tasks. |

| 164 | medium | Deep-Cross: http + tcp +  | src/platform/five-plane-interface/api/task-websock | `pollIntervalMs` defaults to 1000ms (1 second). The relay unconditionally polls `store.event.listEventsByType("task:status_changed", this.backlogLimit)` every second, even when no WebSocket clients are connected. Under high event volume, this creates continuous read pressure on the event store regardless of active subscriber count. | todo | - | Add a subscriber count check before polling. If `getConnectedClients() === 0`, skip the poll or increase the interval to a much larger value (e.g., 30 seconds). |

| 165 | medium | Deep-Cross: http + tcp +  | src/platform/five-plane-interface/channel-gateway/ | `selectProtocol()` accepts any non-empty protocol string from the client without validating it against the expected authentication protocol format. The RFC 6455 WebSocket protocol negotiation should validate that the offered protocol is one actually supported by the server. Currently any string with length > 0 passes. | todo | - | Maintain a `Set<string>` of supported protocols and validate the client's offered protocols against it. Return `false` if no overlap exists. |

| 166 | medium | Unknown | - | `validateAllowedTools()` validates that resolved steps are allowed by the runtime allowlist, but `validateSkillDefinition()` at line 97 only checks MCP tool naming format. There is no validation that the skill's `requiredTools` are actually available in the tool registry before execution starts. A skill declaring non-existent tools would pass validation and fail at execution time. | todo | - | Add a pre-execution check that resolves each tool name in `requiredTools` against the tool metadata registry. Fail fast if any required tool is unknown or not registered. |

| 167 | medium | Unknown | - | `parseMcpToolName()` requires at least 3 segments (`mcp_<server>_<tool>`), but `isMcpToolName()` also returns true for the bare string `"mcp"` (line 107). If a tool named exactly `"mcp"` is registered, `validateMcpToolDefinition()` returns null (line 118-119 checks `isMcpToolName` first and then parses). The `parseMcpToolName` null return at line 80-82 would only trigger if split produces fewer than 3 parts, but `"mcp".split("_")` returns `["mcp"]` (1 element), which hits the `parts.length < 3` check. However, the bare `"mcp"` string would still pass `isMcpToolName()`. | todo | - | Reject `"mcp"` as a tool name explicitly. Require that MCP tools always use the `mcp_<server>_<tool>` three-segment form. |

| 168 | medium | Unknown | - | `credentialFingerprint` is derived from the token but the actual token is stored in plain text (`credentialToken = token` at line 218) in a module-level variable. For the CRM adapter (lines 87-88), the token is also stored in plaintext. These values persist until `shutdown()` is called, and could be extracted from a core dump or memory inspection. | todo | - | Consider using a credential wrapper that clears memory after use, or store only the fingerprint and a reference to the actual credential in a secure buffer that can be zeroed. |

| 169 | medium | Unknown | - | When `parseStoredStringArray()` returns `status: "invalid"`, `resolveExecutionAllowedTools()` returns `allowedTools: undefined` (line 148-149). Callers that check `allowedTools == null` to determine "no restrictions" would interpret this as fully allowed, but the invalid status indicates the stored data was corrupt or tampered with. The R32-03 fix at line 136-141 correctly returns empty array for missing execution when required, but invalid stored data still returns `undefined`. | todo | - | Return `allowedTools: []` (fail closed) when stored data is invalid, not `undefined`. Log an error for the corruption so it can be investigated. |

| 170 | medium | Unknown | - | The `planJson` serialized for caching includes `skillId`, `version`, `requiredTools`, `modelProfileName`, `steps`, `cacheable`, and `cacheTtlSeconds`. It does NOT include `requiredPermissions` (from `SkillMetadata`) or `riskLevel`. Two skills with the same ID, version, and steps but different risk levels would share a cache entry. | todo | - | Include `riskLevel` in the cache key. Consider whether `requiredPermissions` should also be included for cases where permissions affect output. |

| 171 | medium | Unknown | - | `authenticate()` validates token format with regex `/^[A-Za-z0-9+/=_-]{16,}$/` and then stores a fingerprint created by slicing the first 8 characters of the token itself (`token.trim().slice(0, 8)` at line 54). This is not a secure fingerprint - it reveals part of the original token. The same issue exists in the CRM adapter (line 86-88) which uses the first 8 characters of the SHA256 hash input. | todo | - | Use only cryptographic derivatives (SHA256 hash output) for fingerprints, never a direct slice of the token itself. Truncate the hash to the desired fingerprint length instead. |

| 172 | medium | Unknown | - | `assertNamespaceAllowed()` at line 697 returns early if `namespace == null`. This means a null namespace is treated as allowed, but the logic at line 700-713 suggests an empty allowlist should block all access to namespaces. The comment at line 700-701 indicates that an empty `allowedKnowledgeNamespaces` means "no namespaces are allowlisted", but then the early return at line 697-699 allows null namespace to bypass this check entirely. | todo | - | Distinguish between "null namespace means no restriction" and "empty allowlist means no namespace access". Document the semantics clearly and ensure all callers handle null consistently. |

| 173 | medium | Unknown | - | The idempotency key is derived from request headers, payload fields, or generated as null — but never validated for format, length, or character set. A key of 10,000 characters or binary garbage is accepted. This is used as part of `scopedIdempotencyKey = \`${endpointId}:${idempotencyKey}\`` at line 130, which is then used as a Map key and stored in `envelopesByIdempotencyKey`. Very long keys could cause performance degradation in Map operations, and binary keys could cause issues if the storage backend has constraints. ```typescript // line 112-120 - no validation on idempotencyKey format const idempotencyKey = readHeader(input.headers, endpoint.idempotencyHeader ?? "idempotency-key") ?? readString(payload, "eventId") ?? readString(payload, "event_id") ?? readString(payload, "id"); // No length or format check const scopedIdempotencyKey = `${endpoint.endpointId}:${idempotencyKey}`; ``` | todo | - | Validate idempotency key length (max 256 chars) and character set (alphanumeric + hyphen/underscore). Reject keys that exceed limits with a descriptive `ValidationError`. |

| 174 | medium | Unknown | - | `rollbackAcceptedEnvelope()` performs a read-then-delete under separate operations. Between the `get()` at line 163 and the `delete()` at line 167, another thread could insert a new envelope for the same `scopedIdempotencyKey`. The method checks `existing?.envelopeId !== envelopeId` but does not atomically compare-and-delete. Under concurrent load, two concurrent rollbacks for the same endpoint+key but different envelopeIds could result in one incorrectly deleting the other's envelope, or failing to delete when it should. ```typescript // lines 161-172 - non-atomic read-check-delete const existing = this.envelopesByIdempotencyKey.get(scopedIdempotencyKey); if (existing?.envelopeId !== envelopeId) { return; // Race window here - another thread could have replaced the value } this.envelopesByIdempotencyKey.delete(scopedIdempotencyKey); // Non-atomic delete ``` | todo | - | Use an atomic compare-and-swap operation, or use a `Map` with built-in atomic update methods (e.g., `Map.with()` if available, or guard with a lock/semaphore). Alternatively, perform the delete inside the existing check using a single compound operation. |

| 175 | medium | Unknown | - | In `handleReconnect()`, when a reconnection is triggered: ```typescript if (this.fallbackClient != null) { this.fallbackClient.connect(url, token); // Connects fallback but never disconnects old one } ``` The `InMemoryWSClient` (fallback) is connected on top of any previous fallback connection. While `InMemoryWSClient` is a simple in-memory handler that doesn't maintain real network connections, its internal state (`handlers`, `statusHandlers`) accumulates. If SSE fallback activates multiple times, the `handlers` Map in the fallback client grows with duplicate channel entries. The `subscribe()` method at line 60-68 adds to `channelHandlers` but the corresponding unsubscribe (return value) is not captured in `handleReconnect`. ```typescript // handleReconnect line 265-267 - connects fallback without checking if already connected if (this.fallbackClient != null) { this.fallbackClient.connect(url, token); // No disconnect() of existing fallback state } ``` | todo | - | Call `this.fallbackClient?.disconnect()` before `this.fallbackClient?.connect()` in `handleReconnect()` to reset the fallback client's state before reconnecting. |

| 176 | medium | Unknown | - | `subscribeToTask()` checks tenant scope via `this.tenantScopeFilter?.evaluate()` at line 312, and checks subscription limits. However, it does not verify that `taskId` actually corresponds to a known task in the system. An attacker can subscribe to arbitrary task IDs (within their scope) and receive events for tasks that don't exist. This causes: 1. Unnecessary `taskEventHistory` entries (empty history for non-existent tasks) 2. Resource waste from `taskSubscribers` tracking subscriptions to non-existent tasks 3. Potential information disclosure if task IDs have predictable patterns (attackers can probe for valid task IDs within their scope) ```typescript // line 312 - scope check only, no task existence check const scopeDecision = this.tenantScopeFilter?.evaluate(client.principal, taskId); if (scopeDecision != null && !scopeDecision.allowed) { return "scope_denied"; } // No check that taskId is valid/known ``` | todo | - | Add a task existence validation in `subscribeToTask()`. If the task does not exist, return a specific error code `"task_not_found"` (separate from `"scope_denied"` and `"subscription_limit_exceeded"`) so clients can distinguish between "no access" and "task doesn't exist". |

| 177 | medium | Unknown | - | `unregisterClient()` deletes `clientCursors` and `clientBackpressureState` for the given clientId. However, if `clientId` is reused (common in many systems where client IDs are derived from connection metadata like IP+port), the old backpressure state is deleted but the new client might have a different connection profile. More importantly, `clientIdsByStream` is updated by removing the clientId from the set, but if the same clientId reconnects immediately, it re-adds to the set — this is fine. But if the clientId format allows enumeration (e.g., sequential integers), an attacker could intentionally register many fake client IDs to trigger memory growth in `clientBackpressureState` (line 273) before the cleanup at line 407 runs. ```typescript // line 391-395 - backpressure state created without validation this.clientBackpressureState.set(clientId, { bufferedBytes: 0, bufferedFrameCount: 0, lastWarningAt: null, }); ``` | todo | - | Validate `clientId` format in `registerClient()`. Reject clients with sequential or enumerable IDs. Consider using a strong random identifier instead of allowing arbitrary client-supplied IDs. |

| 178 | medium | Unknown | - | `isCoolingDown()` uses: ```typescript private getCooldownKey(itemId: string, delivery: NotificationDeliveryType): string { return `${itemId}:${delivery}`; } ``` The cooldown is tracked per `(alertId, deliveryChannel)` combination. If the same alert triggers both `overlay` and `push` delivery (lines 88-103), the cooldown for `push` does not prevent a subsequent `overlay` delivery if the alert is re-routed. Additionally, the cooldown check (line 72) only applies when `applyCooldown: true` is passed. In `getOverlayAlerts()` and `getPushNotifications()` (lines 50-66), `applyCooldown: false` is passed, meaning **cooldown is never applied for individual delivery method queries** — only when `routeNotifications()` is called with `applyCooldown: true`. This means duplicate notifications can be generated on every polling cycle for overlay and push. | todo | - | Always apply cooldown regardless of which method is being queried. Move the cooldown state update to a shared state rather than conditional on the applyCooldown flag. |

| 179 | medium | Unknown | - | Five detection rules are evaluated independently against the same metrics: 1. `sev1_availability_collapse` — fires if `availability < 95` OR `error_rate > 5` 2. `sev1_data_integrity_failure` — fires if `data_integrity_check === false` 3. `sev2_degraded_service` — fires if `latency_p99 > 1000` OR `error_rate > 1` 4. `sev2_security_anomaly` — fires if `security_events > 10` 5. `sev3_config_drift` — fires if `config_drift_detected === true` A single check with `error_rate: 6` and `latency_p99: 1500` and `availability: 94` would fire rules 1 (SEV1), 3 (SEV2), and potentially additional incidents from the primary detection path (lines 70-92), creating 4+ incidents from one event. **False positive mechanism:** Rule conditions are not mutually exclusive. A check with `error_rate > 5` triggers both `sev1_availability_collapse` (SEV1) and `sev2_degraded_service` (SEV2) — both from the same error_rate threshold but producing two separate incident records. | todo | - | Implement rule priority ordering. After the highest-severity matching rule fires, suppress lower-severity rules for the same underlying metric dimension. Use a `matchedDimensions` set to track which metric triggered which rule, and skip redundant rule evaluation. |

| 180 | medium | Unknown | - | `shouldEmitAlert()` uses key `${scope}:${scopeId}:${alertLevel}`. This means: - A `cost.exceeded` at `platform` scope with scopeId `main` and `exceeded` level is tracked separately from `cost.critical` at the same scope - If a billing spike causes both `exceeded` and `critical` to fire in the same cycle, both are emitted - No deduplication exists between `cost:limit_reached` events from cost-alert-service and `alert_events` table entries from SloAlertingService, even when they reference the same underlying execution The `lastAlertByKey` Map (line 57) has no TTL or size bound — if many scope/scopeId/alertLevel combinations fire, the map grows unboundedly (C-11 comment on line 59 mentions eviction for accumulators but not for `lastAlertByKey`). | todo | - | Add a correlation ID that groups related alerts (exceeded + critical + warning for the same underlying consumption spike). Implement a size-bounded deduplication map with LRU eviction for `lastAlertByKey`. Cross-reference with existing open incidents to suppress redundant alerts when an incident is already open for the same scope. |

| 181 | medium | Unknown | - | The cooldown check (lines 108-109) only applies when `consecutiveFailures >= maxRetries`. If the component is in `degraded` state (failure count below threshold but status is still `degraded`), healing can retry immediately after a failed attempt with no backoff. ```typescript if (consecutiveFailures >= this.policy.maxRetries) { const timeSinceLastAttempt = Date.now() - new Date(lastAttempt.executedAt).getTime(); if (timeSinceLastAttempt < this.policy.cooldownPeriodMs) { return { healed: false, ... }; // blocked by cooldown } } // No cooldown for components below maxRetries threshold ``` A component with 2 failures (maxRetries=3) can have healing attempted on every call to `execute()`, with no cooldown enforced. If each attempt fails and increments `consecutiveFailures`, the component escalates to `unhealthy` but then the cooldown kicks in — however by that point the component may already be in a worse state from rapid retry. | todo | - | Apply a proportional cooldown based on `consecutiveFailures`: `cooldownMs * (1 + consecutiveFailures)` so that earlier failures also get backoff, not just after maxRetries is reached. |

| 182 | medium | Unknown | - | `determineStrategy()` uses only `incident.category` and `incident.symptoms.length` to choose between `manual`, `assisted`, `self_heal`, and `automated`. It never consults: - Self-healing success/failure history for the affected component - Current `componentHealth` status from SelfHealingService - Panic mode state from PlatformPanicService A component that has had 3 consecutive self-healing failures still gets `self_heal` strategy if its category is `performance`. The escalation to `manual` only happens for SEV1 or security category (lines 138-145). **Escalation loop:** If self-healing repeatedly fails on a `performance` incident, the resolver continues recommending `self_heal`, the ops-agent attempts it again, and the loop continues until cooldown or manual intervention. | todo | - | Accept `healingHistory` and `currentHealthStatus` as parameters to `determineStrategy()`. If recent healing attempts have failed for this component, elevate the strategy to `assisted` even for non-SEV1 incidents. |

| 183 | medium | Unknown | src/platform/five-plane-interface/api/http-server/ | `readRequestId()` generates IDs as `` `req_${Date.now().toString(36)}_${randomUUID()}` `` — combining a Unix timestamp with a UUID. While the UUID portion provides uniqueness, the timestamp portion reduces entropy and makes IDs partially predictable. This is inconsistent with the canonical `{prefix}_{uuid}` pattern defined in `ids.ts`. | todo | - | Use `` `req_${randomUUID()}` `` or adopt the canonical `newId("req")` function. |

| 184 | medium | Unknown | - | `evaluateCheckpointForGC()` only returns candidates that are age-expired. It never detects orphaned checkpoints — those whose `executionId` no longer exists in the system. The `CheckpointGCCandidate.isOrphaned` field (line 52) is always hardcoded to `false` in the return value at line 424. There is no mechanism in `scanForGCCandidates` or `scanExecutionCheckpoints` to cross-reference checkpoint executionIds against the authoritative store to detect stale references. | todo | - | Add a method to cross-reference checkpoint executionIds against the task store. Flag candidates as orphaned when `executionId != null` but no corresponding execution exists. |

| 185 | medium | Unknown | - | `DEFAULT_DELIVERY_CONFIG.timeoutMs = 30000` (30 seconds) but the `ChannelGatewayService` in `channel-gateway-service.ts` line 91-97 clamps `requestTimeoutMs` to a default of 5000ms with a max of 30000ms. This creates a mismatch: the delivery config's default timeout (30s) is never reachable because the service clamps to 5s by default. However, when `options.requestTimeoutMs` is explicitly passed as 30000, it reaches the cap at exactly 30000, making the delivery config's timeoutMs effectively unused as a configured value. | todo | - | Use a single canonical timeout configuration. Either (a) have `ChannelGatewayService` accept the delivery config's `timeoutMs` as the authoritative value for HTTP fetch timeouts, or (b) document that the two timeouts serve different purposes (delivery timeout for retry logic vs. HTTP request timeout for fetch) and ensure they are aligned via config. The default `requestTimeoutMs` should be derived from `DeliveryGuaranteeConfig.timeoutMs` if provided. |

| 186 | medium | Unknown | src/domains/registry/plugin-runtime-host.ts:439 | The `readStdout()` loop silently swallows JSON parse failures: ```typescript try { this.handleMessage(JSON.parse(line)); } catch { this.stderrBuffer = `${this.stderrBuffer}\nnon-protocol-stdout:${line}`.trim().slice(-STDERR_TAIL_BUFFER_BYTES); } ``` No `logger.warn` or `logger.error` call. Errors are absorbed into a buffer that is never examined unless someone is reading `stderrBuffer` directly. | todo | - | Add `logger.debug` or `logger.warn` inside the catch block to record that a non-protocol line was received. |

| 187 | medium | Unknown | src/platform/five-plane-execution/tool-executor/sh | Snapshot ID is `` `shadow-${Date.now()}-${suffix}` `` where suffix is a bare UUID. Uses hyphen separator and embeds timestamp. This is a third different ID format (hyphen vs underscore, timestamp+uuid vs uuid-only). | todo | - | Replace with `` newId("shadow_snapshot") ``. |

| 188 | medium | Unknown | - | In `startCall()`, `remainingMs = Math.max(0, deadlineAt - Date.now())` is calculated but then `setTimeout` is called with `remainingMs` but `controller.abort(timeoutError)` is used with `timeoutError` that has `config.timeoutMs` hardcoded. The AbortError reason says the call timed out after `config.timeoutMs` but the actual wait was `remainingMs`. Additionally, `remainingMs` can be very small (if near deadline) but the timeout message says `config.timeoutMs`. This is misleading and could cause confusion in debugging. | todo | - | Use `remainingMs` consistently in the AbortError and the setTimeout call. |

| 189 | medium | Unknown | src/platform/five-plane-interface/api/api-auth-ser | `authenticate()` throws `ApiAuthError(401, "api.auth_required", ...)` when no Bearer token or x-api-key is present. However, `authenticateOptionalPrincipal()` in `request-helpers.ts` line 86-93 catches `ApiAuthError` and returns `null` instead of re-throwing. This creates an inconsistency: when auth is optional (used for route dispatch at http-api-server.ts line 588), the authentication error is silently swallowed and the request proceeds as unauthenticated. For endpoints that require authentication but hit the optional path first, this can allow unauthenticated access. | todo | - | Distinguish between "authentication not provided" (should proceed as null principal) and "authentication was provided but invalid" (should throw). Catch only the former, re-throw the latter. |

| 190 | medium | Unknown | src/org-governance/knowledge-boundary/chinese-wall | ```typescript } catch { // silently suppress } ``` | todo | - | Log the error. |

| 191 | medium | Unknown | - | `updateConfig()` calls `this.config = { ...this.config, ...config };` and then immediately calls `processQueue()`. If new config reduces `maxConcurrentCalls`, the queue processing will immediately admit queued calls even if current `activeCalls` already exceeds the new reduced limit. The new limit is not enforced retroactively against in-flight calls. | todo | - | After updating config, if `activeCalls > new maxConcurrentCalls`, do not process queue until `activeCalls` drops below new limit, or forcibly reject excess queued calls. |

| 192 | medium | Unknown | - | At line 517-520, `buildCompensationPlan` calls `this.store.task.getTask?.(executionId, tenantId)?.id ?? ""` and passes the result (which could be an empty string) to `buildRuntimeRecoveryView`. If the task is not found, an empty string ID is used and the method returns a plan with zero actions rather than null. The null check at line 523-525 only checks for missing candidate, not missing taskId. | todo | - | Validate that the taskId is non-empty before querying. Return null if `this.store.task.getTask` returns null. Add explicit guard: `if (!taskId) return null;` |

| 193 | medium | Unknown | - | ```typescript } catch (error) { logger.warn("task websocket status relay poll failed", { error: error instanceof Error ? error.message : String(error), }); } ``` A poll failure is only logged at warn level and swallowed. If the underlying event store is unavailable or returns corrupted data, the relay silently stops updating WebSocket clients without notifying them or marking the relay as unhealthy. | todo | - | Track consecutive poll failures and after N failures, broadcast an error event to subscribed clients or mark the connection as degraded. |

| 194 | medium | Unknown | - | `settle()` checks: ```typescript if (input.actualAmount > input.reservation.amount) { throw new WorkflowStateError( "budget_settlement.actual_amount_exceeds_reservation", "budget_settlement.actual_amount_exceeds_reservation: Actual amount exceeds reserved amount.", ); } ``` This prevents the ledger from being over-charged. However, this check only fires when `actualAmount > reservation.amount`. If actual usage exceeds reservation (a real overspend), the settlement is rejected and the reservation remains open. The caller may then retry with the same or larger actualAmount, creating a retry loop. There's no path for "accept the overspend as a new reservation" or "charge against next available budget." | todo | - | When `actualAmount > reservation.amount`, instead of throwing, automatically create a new reservation for the overage and settle both. Or return an `overspendDetected: true` flag with the settlement result so callers can decide how to handle the exceedance. |

| 195 | medium | Unknown | src/org-governance/delegated-governance/governance | ```typescript } catch { // silently suppress } ``` | todo | - | Log the error. |

| 196 | medium | Unknown | - | `retireSlot()` only sets status to "draining": ```typescript private retireSlot(slot: DeploymentSlot): void { this.db.connection .prepare(`UPDATE deployment_slots SET status = 'draining', traffic_weight = 0, updated_at = ? WHERE slot = ? AND status IN ('active', 'standby')`) .run(now, slot); } ``` No verification that in-flight requests are drained, no waiting for instance deregistration. The slot could be marked draining while still serving active requests. | todo | - | Add a `drainTimeout` parameter. Verify instance count reaches 0 or check a health endpoint before marking fully retired. |

| 197 | medium | Unknown | - | `registerDefaultConsumers()` at line 166-172 subscribes each default consumer with an empty async function: `async () => {}`. This is a no-op handler. The comment at line 169 says "Phase 1a reads from the authoritative store directly" — but the empty handler would do nothing when events are delivered. The subscription appears to be a placeholder but provides no actual consumer logic. | todo | - | Either remove the empty subscription (if consumers are handled externally) or implement actual consumer logic in the handler. Document why the empty handler exists if it's intentional. |

| 198 | medium | Unknown | - | `ChannelGatewayRetryExecutor` defaults `pollIntervalMs = 15_000` (15 seconds) with jitter of 10% (line 65). The delivery service's `calculateBackoffForAttempt()` starts at `initialBackoffMs = 1000` (1 second) with multiplier 2. A message that fails immediately would have its first retry after 1 second, but the retry executor only polls every 15 seconds by default. This means the first retry could be delayed by up to ~15 seconds beyond the backoff calculation. | todo | - | Set `pollIntervalMs` default to be closer to or derived from `initialBackoffMs`. A reasonable default would be `max(1000, initialBackoffMs * 2)` to ensure the poll interval is at least 2x the initial backoff, allowing at least one retry attempt to be scheduled before the next poll. |

| 199 | medium | Unknown | - | `buildCostOptimizationRecommendation()` uses: ```typescript const estimatedSavingsUsd = Number((currentCostUsd * (downgradePath ? 0.22 : 0.15)).toFixed(2)); ``` The 22% and 15% savings factors are magic numbers with no justification. There's no analysis of actual pricing difference between the current and recommended model — the savings estimate is purely based on the current cost multiplied by a hardcoded percentage. If the recommended model costs 5% less, the actual savings would be 5%, not 22%. | todo | - | Compute `estimatedSavingsUsd` as the actual cost difference: `currentCostUsd * (1 - recommendedProfile.pricing.totalPer1kUsd / currentProfile.pricing.totalPer1kUsd)`. Use the actual pricing from the registry rather than a generic multiplier. |

| 200 | medium | Unknown | src/org-governance/delegated-governance/stores/ind | ```typescript } catch { // silently suppress } ``` | todo | - | Log the error. |

| 201 | medium | Unknown | src/sdk/fixture-redact.ts:176 | `generateTestId()` uses `` `${prefix}_${randomUUID().replace(/-/g, "").slice(0, 12)}` `` — truncates UUID to 12 characters and removes hyphens. At 12 hex characters (48 bits of entropy), collision probability in a test suite of 10,000 IDs is approximately 1 in 4 billion, which is non-trivial for test suites that run repeatedly. | todo | - | Use at least 16 hex characters (64 bits) or use the full UUID. Alternatively, use `newId("test")` for test fixtures to leverage full UUID entropy. |

| 202 | medium | Unknown | - | `activateSlot()` allows activation from draining status: ```typescript private activateSlot(slot: DeploymentSlot): void { this.db.connection .prepare(`UPDATE deployment_slots SET status = 'active', updated_at = ? WHERE slot = ? AND status IN ('active', 'standby', 'draining')`) .run(now, slot); } ``` A slot in "draining" (traffic weight already 0) can be reactivated. Combined with the slot contamination issue above, this creates a scenario where a deprecated slot can be revived accidentally. | todo | - | Restrict activation to only "standby" and "active" statuses. "draining" slots should require explicit re-registration. |

| 203 | medium | Unknown | - | In `recordDeliveryFailure()`, the condition is `options.retryable && attemptNumber < record.maxRetries`. This checks if retryable is true AND if the current attempt number (already incremented to `record.attempts + 1` before calling) is strictly less than maxRetries. For maxRetries=5 and attemptNumber=5: 5 < 5 is false, so dead-letter. This means the 5th attempt (out of 5 allowed) is NOT retried even if retryable=true. The message is dead-lettered immediately after attempt 5 fails. The intent of `maxRetries` should be "allow maxRetries retry attempts after initial failure", so with maxRetries=5, attempts 1-5 are retries and attempt 6 (first failure after all retries) should dead-letter. | todo | - | Change to `options.retryable && attemptNumber <= record.maxRetries` and adjust the dead-letter condition to match retry semantics intent. |

| 204 | medium | Unknown | - | `domainAccessOrder` Map tracks access timestamps for LRU eviction, but when domains are removed, corresponding entries in `domainAccessOrder` are NOT deleted. The Map grows indefinitely. | todo | - | Delete `domainAccessOrder` entries when the corresponding domain data is evicted. |

| 205 | medium | Unknown | - | All delegation record IDs use bare `` id: randomUUID() `` with no semantic prefix. This makes debugging and log correlation harder — there is no way to distinguish a delegation ID from a trust relationship ID from a capability ID in raw logs. | todo | - | Use `` newId("delegation") ``, `` newId("capability") ``, etc., with appropriate semantic prefixes. |

| 206 | medium | Unknown | - | `getLoopController` creates a `HarnessLoopController` with initial state including `lastRetryAt: run.sleepLease?.createdAt ? new Date(run.sleepLease.createdAt).getTime() : 0`. When `sleepLease` is null (no prior retry), this is `0`. The `shouldUseBackoffRecovery` check at line 107 only looks at whether `loopController` is set or `run.loopMetrics` or `run.sleepLease` exist — it does not consider whether `sleepLease.createdAt` is `0` (meaning no actual retry happened). An initial run with null sleepLease would still pass the backoff check and enter backoff recovery logic. | todo | - | In `shouldUseBackoffRecovery`, also check that `run.sleepLease?.retryAttempt ?? 0 > 0`. Only use backoff recovery if there has been at least one prior retry attempt. |

| 207 | medium | Unknown | - | ```typescript const projectedPlatform = (input.spend.currentPlatformCostUsd ?? input.spend.currentTaskCostUsd) + next; ``` If `currentPlatformCostUsd` is `null`, the code substitutes `currentTaskCostUsd`. But this conflates task-level spend with platform-level spend — a task may be one of many running concurrently at the platform level. Using task cost as a proxy for platform cost systematically undercounts platform spend when multiple tasks run in parallel, since platform cost should be the sum of all active task costs. | todo | - | Track `currentPlatformCostUsd` as the aggregate of all running tasks' costs, not as a per-task value. If platform cost tracking isn't implemented yet, log a warning when falling back to task cost and note that parallel task interference is untracked. |

| 208 | medium | Unknown | - | `process.stderr.write` is used directly instead of the structured logger for subscriber failures and file watcher tick failures. These are error-level events rendered as raw strings, bypassing the structured logging pipeline. | todo | - | Replace `process.stderr.write` calls with `logger.error` using structured log entries. |

| 209 | medium | Unknown | - | `executeInvocation()` and `waitForInvocationSlot()` both use `setTimeout` to enforce timeout limits, but no test verifies that slow invocations actually trigger timeout errors. | todo | - | Add tests using fake timers to advance time past `timeoutMs` and verify `ValidationError` is thrown. |

| 210 | medium | Unknown | - | Same bare UUID pattern as Issue 14 — gateway IDs have no semantic prefix. | todo | - | Use `` newId("federation_gateway") `` with prefix. |

| 211 | medium | Unknown | - | When `advanceShift()` completes and calls `retireSlot()` + `activateSlot()`, the version of the retired slot is not preserved in the transition record. `RollbackRecord` stores `fromVersion` and `toVersion`, but these are looked up from the slot table at rollback time (`getSlotVersion()`), not recorded at promotion time. If slot versions change between promotion and rollback, the rollback could restore the wrong version. | todo | - | Capture slot version in `TrafficShiftRecord` at shift completion, not look it up later. |

| 212 | medium | Unknown | - | Same bare UUID pattern — trust relationship IDs have no entity type prefix. | todo | - | Use `` newId("trust_relationship") `` with prefix. |

| 213 | medium | Unknown | - | The fallback logic at lines 47-49 means if metrics ARE present but are `null` or `undefined`, the fallback values apply: ```typescript const successRate = hasRequiredMetrics ? (progress.successRate ?? 0) : 1.0; const errorRate = hasRequiredMetrics ? (progress.errorRate ?? 0) : 0.0; const latencyP50Ms = hasRequiredMetrics ? (progress.latencyP50Ms ?? Infinity) : 0; ``` If `progress.successRate` is `null`, `hasRequiredMetrics` is `true` (key exists), but `progress.successRate ?? 0` gives `0`. That means a canary reporting `successRate: null` would be treated as having 0% success rate — which would trigger rollback, but not before the fallback `errorRate ?? 0` = 0 passes the error rate check. Inconsistent null handling creates unpredictable promotion/rollback outcomes. | todo | - | Distinguish between "key absent" and "key present but null". Only treat absent keys as perfect. For null values, treat as missing data and fail the evaluation. |

| 214 | medium | Unknown | - | The file follows the identical pattern as `domains-runtime-orchestrator.ts`, `interaction-governance-runtime-orchestrator.ts`, and `ai-operations-runtime-orchestrator.ts`. All four have: - `buildDependencyServiceIds()` function - Same class structure with same methods - Same `startup()` pattern with same error handling This is significant code duplication. | todo | - | Extract common startup orchestration logic into a shared `RuntimeOrchestrator` base class. |

| 215 | medium | Unknown | - | ```typescript let defaultPlatformApplicationKernel: PlatformApplicationKernel \| null = null; export function getPlatformApplicationKernel(registry?: ServiceRegistry): PlatformApplicationKernel { if (registry == null) { defaultPlatformApplicationKernel ??= new PlatformApplicationKernel(); return defaultPlatformApplicationKernel; } ``` The `??=` operator is not atomic. If two threads call simultaneously before the assignment completes, two instances could be created. | - | - | Use a mutex or initialization guard to ensure single instantiation in concurrent scenarios. |

| 216 | medium | Unknown | - | ```typescript const stage = typeof canaryStage === "number" && CANARY_STAGES.includes(canaryStage) ? canaryStage : 100; return { canaryPercent: stage, stablePercent: 100 - stage, weight: stage, }; ``` If an invalid canaryStage is passed, it defaults to 100% canary. This could be intentional for test compatibility (comment at line 91), but silently defaulting to full canary in production could cause sudden 100% traffic shift. | todo | - | Throw on invalid stage rather than silently defaulting. If defaulting is desired for backward compatibility, log a warning. |

| 217 | medium | Unknown | - | `normalizeRecoveryAttempt` returns `Math.max(1, Math.trunc(attempt))` at line 692. If `attempt` is `0`, it normalizes to `1`. But if `attempt` is `-5`, it normalizes to `1`. For negative values, this silently maps them to `1` rather than rejecting them as invalid. A negative attempt count suggests data corruption. | todo | - | Add validation: if `attempt <= 0`, either throw a `ValidationError` or log a warning and return `1`. If `attempt` is `Infinity` or `NaN`, the current `Number.isFinite` check at line 689 handles this correctly by returning `1`. |

| 218 | medium | Unknown | - | ```typescript if (requestUrl == null) { throw new ValidationError("gateway.webhook_url_required", "gateway.webhook_url_required", { retryable: false, details: { targetId, externalTargetId }, }); } const validatedRequestUrl = parseSafeOutboundUrl(requestUrl, { invalid: "gateway.webhook_url_invalid", blocked: "gateway.webhook_url_blocked_ssrf", }).toString(); ``` When `externalTargetId` is a URL that gets blocked, it may be included in error details passed to `ValidationError`. If `externalTargetId` contains sensitive information (e.g., API keys in query parameters, internal URLs), these are now part of the error record. | todo | - | Sanitize URLs before including in error details: ```typescript details: { targetId, hasExternalTargetId: externalTargetId != null }, ``` Or redact the actual URL value in error details. |

| 219 | medium | Unknown | - | Multiple catch blocks across the SPI registry do not log errors: - Line 530: `} catch (error) { ... }` (no logging) - Line 607: `} catch (error) { ... }` (no logging) - Line 672: `} catch (error) { ... }` (no logging) - Line 823: `} catch (error) { ... }` (no logging) | todo | - | Add `logger.error` calls to each catch block. |

| 220 | medium | Unknown | - | ```typescript if (maxInputPer1kUsd != null && found.profile.pricing.inputPer1kUsd > maxInputPer1kUsd) { filteredOut.push(`${profileName}:cost_cap_exceeded`); return null; } ``` The cost cap check only compares `inputPer1kUsd` against `maxInputPer1kUsd`. But output token costs are often higher than input costs (e.g., 3-10x). A model could have cheap inputs but very expensive outputs, passing the input cost filter but resulting in a total cost that far exceeds the intended cap. | todo | - | Compare `(inputPer1kUsd * inputRatio + outputPer1kUsd * outputRatio)` against `maxInputPer1kUsd`. Use a typical input/output token ratio (e.g., 30/70 for reasoning tasks) to compute an effective total cost per 1k tokens. |

| 221 | medium | Unknown | - | R4-28 introduced write-ahead logging for cost events but no sweeper code exists for orphaned pending entries. | todo | - | Add ADR documenting the WAL recovery mechanism, or implement the sweeper code. |

| 222 | medium | Unknown | src/platform/five-plane-execution/execution-engine | `EffectBuilder` constructor sets `` id: `effect_${Date.now()}_${randomUUID()}` `` — combining wall-clock time with a UUID. This pattern mixes non-unique temporal data with unique random data, producing longer IDs with no additional entropy benefit. It diverges from the canonical `newId()` contract. | todo | - | Replace with `` newId("effect") `` from `ids.ts`. |

| 223 | medium | Unknown | - | `DEFAULT_RATE_LIMIT_CONFIG` defines per-channel rate limits (telegram: 30/ms, slack: 30/ms, webhook: 100/ms, default: 50/ms). However, `HttpApiServer` in `http-api-server.ts` line 1126-1139 creates a `DistributedRateLimiter` with `maxCalls = 100` and `windowMs = 1000` as API-level rate limiting. These two rate limit layers (API gateway HTTP rate limiting vs. channel gateway per-channel provider rate limiting) are completely independent and use different buckets (tenant-scoped vs. channel+tenant scoped). There is no coordination: an API client hitting the HTTP endpoint 100 times/second could exhaust provider rate limits downstream after the API rate limit passes. | todo | - | Add a coordination layer where `HttpApiServer`'s rate limiter is informed of downstream channel provider limits. Alternatively, emit a warning or metric when API throughput approaches provider rate limits to allow proactive throttling before provider rejection occurs. |

| 224 | medium | Unknown | - | `CheckpointEnvelope` stores the payload schema version in `envelope.schema` and `metadata.payloadSchemaVersion` (line 84, 58), but the `CheckpointRef` interface (used by `CheckpointManifest`) only has `checkpointId`, `storageUri`, optional `checksum`, optional `metadata`, and optional `createdAt` — no field to store the schema version of the referenced checkpoint. When a manifest references a checkpoint, there is no way to know the schema version of that checkpoint's payload for migration/evolution purposes. The envelope-level `schema` is not preserved in the `CheckpointRef`. Additionally, `unwrapWorkflowStepCheckpoint` (line 389-393) unpacks using `unpackCheckpointEnvelope` but the returned data's schema version is not exposed — callers cannot determine which schema version was used to encode the data. | todo | - | Add an optional `schemaVersion` field to `CheckpointRef` so that manifests preserve the schema version of each referenced checkpoint. Update `createCheckpointManifest` and `validateCheckpointManifest` to handle this field. Expose the schema version in `UnpackedCheckpointEnvelope` return type. |

| 225 | medium | Unknown | - | At line 94-97, candidates are filtered as: `finding.severity !== "info" && (finding.recoverable \|\| finding.severity === "p0")`. This means P2 and P1 findings that are NOT recoverable are excluded entirely, even though they may represent real problems. Only P0 findings bypass the `recoverable` check. A P1 `stale_execution` that was somehow marked `recoverable: false` would be silently dropped. | - | - | Include all non-info findings as candidates. The `requiresManualApproval` field should be determined by the `suggestedRepairAction === "manual_intervention_required"` check, not by filtering out non-recoverable P1/P2 findings. |

| 226 | medium | Unknown | - | `settle()` is called with: ```typescript actualAmount: ledgerEntry.amountUsd, ``` But `ledgerEntry.amountUsd` is computed at line 324 as: ```typescript amountUsd: estimatedChargeUsd, // = quantity * unitPriceUsd (from plan quota) ``` The settlement uses the plan's generic `unitPriceUsd` rather than the actual model's pricing. Even if the billing service looked up the actual model after task completion, it would need to retroactively update the ledger entry and the settlement. Currently no mechanism exists to correct the ledger entry when actual model cost differs from plan rate. | todo | - | After task completion, look up actual model pricing and compute actual charge. Create a corrected ledger entry as an "adjustment" if the actual differs from the estimate. Settle with the corrected amount rather than the estimated amount. |

| 227 | medium | Unknown | - | References `@see Runtime Execution Contract: docs_zh/contracts/runtime_execution_contract.md` which does not exist. | todo | - | Create the runtime execution contract document. |

| 228 | medium | Unknown | - | Uses `process.stderr.write` directly instead of structured logger for error-level events. | todo | - | Replace `process.stderr.write` with `logger.error` using structured log entries. |

| 229 | medium | Unknown | - | ```typescript } catch (err) { logger.debug("getAllowCrossDivisionDag failed", { error: err }); } ``` Using `logger.debug` for failure conditions is inappropriate for production. Debug logs are typically disabled in production. | todo | - | Use `logger.warn` or `logger.error`. |

| 230 | medium | Unknown | - | References `@see Observability Contract: docs_zh/contracts/debug_inspect_health_backpressure_contract.md` which does not exist. | todo | - | Create the observability contract document. |

| 231 | medium | Unknown | - | References `@see Event Bus Contract: docs_zh/contracts/event_bus_contract.md` which does not exist. | todo | - | Create the event bus contract document. |

| 232 | medium | Unknown | src/platform/five-plane-execution/tool-executor/to | `generateTodoId()` returns `` `todo_${Date.now()}_${randomUUID()}` `` — same `Date.now()` + UUID pattern as Issue 2. ID length is 45+ characters with no structural benefit over a canonical UUID-based ID. | todo | - | Replace with `` newId("todo") ``. |

| 233 | medium | Unknown | - | `evaluateCheckpointForGC` at line 405 computes `ageMs = referenceTimestamp - stats.mtimeMs`. `enforceVersionLimits` at line 238 sorts by `stats.mtimeMs`. The mtime is filesystem metadata that can be modified by any read operation, backup software, or intentional touch. It is not a reliable proxy for checkpoint creation time. Checkpoints that are actively read may appear younger than they actually are. | todo | - | Use `stats.birthtime` (birthtime/creation time) for age calculation if available, or store an explicit `createdAt` timestamp inside the checkpoint JSON and read it during GC evaluation. Fall back to birthtime; do not rely on mtimeMs alone. |

| 234 | medium | Unknown | - | `buildDependencyServiceIds()` throws a plain `Error`: ```typescript throw new Error(`w5_startup_plan.missing_dependency:${dependencyStepId}`); ``` But the similar function in `interaction-governance-runtime-orchestrator.ts` uses `ValidationError`: ```typescript throw new ValidationError("w3_startup_plan.missing_dependency", `w3_startup_plan.missing_dependency:${dependencyStepId}`); ``` This inconsistency means error handling code that catches `ValidationError` will not catch the domains orchestrator error. | todo | - | Use `ValidationError` consistently across all orchestrators for missing dependency errors. |

| 235 | medium | Unknown | - | Uses `?? false` coalescing which handles `undefined` but not `null` or truthy non-boolean values. | todo | - | Use explicit `=== true` checks. |

| 236 | medium | Unknown | - | ADR-071 does not cover: - Plugin version negotiation - Sandbox isolation levels (implemented in code) - Plugin taint tracking (R2-5 implemented) - Container launcher format and validation | todo | - | Update ADR-071 to cover the full plugin lifecycle. |

| 237 | medium | Unknown | - | `staleWorkerIncidentIds` Set accumulates worker IDs but is never cleared on `stop()`. | todo | - | Reset `staleWorkerIncidentIds` in the `stop()` method. |

| 238 | medium | Unknown | - | When a schema is updated via `register()`, `sourceContent` and `sourceTimestamps` Maps are not cleaned up for removed sources. | todo | - | Clear source entries for a domain before re-initializing from the new schema. |

| 239 | medium | Unknown | - | Uses `process.stderr.write` directly for subscriber failures instead of structured logger. | todo | - | Use `logger.error` with structured log entries. |

| 240 | medium | Unknown | src/platform/five-plane-control-plane/iam/network- | Egress audit event ID uses `` `egress_${Date.now()}_${randomUUID()}` `` — same `Date.now()` + UUID anti-pattern. Timestamp component adds no uniqueness value since the UUID is already unique. | todo | - | Replace with `` newId("egress") ``. |

| 241 | medium | Unknown | - | In `execute()`, `attempt` is incremented BEFORE `throwIfAborted()`. This means if the signal is already aborted before ANY attempt, the `attempt` counter is incremented to 1 before the abort check throws. Tracked stats will show `totalAttempts: 1` even though zero attempts were actually made. | todo | - | Move `throwIfAborted()` before the `attempt++` increment. |

| 242 | medium | Unknown | - | `HttpApiServer` sets `SERVER_REQUEST_TIMEOUT_MS = 30_000` (30 seconds, line 108) as the server-level request timeout. `ChannelGatewayService` sets its own `requestTimeoutMs` with default 5000ms and max 30000ms (lines 91-97). The API server's timeout wrapper at lines 750-768 resolves timeout per-request with a default of 5000ms (line 160) and max of 30000ms (line 161). These three timeout values (30s server socket, 5s API default, 30s API max) are not synchronized and there's no documented relationship between them. | todo | - | Establish a timeout hierarchy: API server socket timeout > API server request handler timeout > channel gateway request timeout. Document that `HttpApiServer.apiDefaultTimeoutMs` should be >= `ChannelGatewayService.requestTimeoutMs` default. Consider deriving the channel gateway timeout from the API server's configured timeout. |

| 243 | medium | Unknown | - | The `api-auth-service.ts` uses a custom `ApiAuthError` class that does not inherit from `AppError`. Throughout the file, `ApiAuthError` is thrown directly: ```typescript throw new ApiAuthError(500, "api.jwt_secret_too_short", "JWT secret must be at least 10 characters."); ``` This creates an inconsistent error hierarchy where authentication errors cannot be uniformly handled with the rest of the platform's error handling infrastructure. The `normalizeError()` function in `http-server/api-error.ts` must manually convert `ApiAuthError` to `ApiError`. | todo | - | Have `ApiAuthError` extend `AppError` instead of `Error`, or consolidate all authentication errors to use the existing `AuthError` class from `errors.ts`. |

| 244 | medium | Unknown | src/platform/five-plane-interface/api/http-server/ | The task creation handler reads the idempotency key from the HTTP header `idempotency-key` (line 315: `ctx.request.headers["idempotency-key"]`), but the SDK's ContractEnvelope wrapping logic (api-client.ts line 410-414) places the idempotency key inside the envelope's `idempotencyKey` field, not in an HTTP header. The SDK sends the idempotency key in `ContractEnvelope.idempotencyKey` but the gateway never reads it from there. | todo | - | Unify idempotency key handling: either read from the HTTP header in gateway (current) and stop wrapping it in ContractEnvelope, or add ContractEnvelope-aware idempotency middleware that extracts the key from the envelope. |

| 245 | medium | Unknown | - | `recordCost()` decrements `unsourcedRecordCount` after successful recording of each valid record, but only decrements if `unsourcedRecordCount > 0`. However, the error-throwing path for invalid records increments `unsourcedRecordCount` without bounds: ```typescript if (record.decisionRef.trim().length === 0) { this.unsourcedRecordCount += 1; // No upper bound throw new Error(`cost_optimizer.unsourced_record:${this.resolveSubjectId(record)}`); } if (this.unsourcedRecordCount > 0) { this.unsourcedRecordCount -= 1; // Guards against going negative but not against overflow } ``` If thousands of invalid records arrive before any valid record, `unsourcedRecordCount` grows unbounded. | todo | - | Use `Number.MAX_SAFE_INTEGER` cap on increment, or convert to a gauge metric that can be reset by an admin operation rather than a simple counter. |

| 246 | medium | Unknown | - | `normalizeArtifactRecord()` (lines 5-16 of artifact-model.ts) maps `size -> sizeBytes` and `contentHash -> checksum` for backward compatibility. This preprocessing is applied when parsing via `ArtifactRecordSchema.parse()`. However, `ArtifactStore.writeBuffer()` constructs the `ArtifactRecord` manually (lines 178-199) using the correct field names (`sizeBytes`, `checksum`) directly without going through `normalizeArtifactRecord`. This means: 1. If an artifact record is exported from the system and then imported back via `parseArtifactRecord()`, the normalization will work correctly. 2. But if raw data with `size` or `contentHash` fields is imported without going through `parseArtifactRecord()` (e.g., direct database import), the schema validation in `ArtifactRecordSchema` will reject it because those fields are not defined in the schema. The import path is asymmetric with the export path: export uses the correct field names, but import requires preprocessing to handle legacy field names. | todo | - | Document the requirement to use `parseArtifactRecord()` for all artifact record imports. Consider adding a separate `ArtifactRecordInputSchema` that extends `artifactRecordBaseSchema` with legacy field aliases so raw input data can be validated directly without separate preprocessing. |

| 247 | medium | Unknown | src/domains/governance/division-loader.ts:812 | ```typescript } catch (err) { logger.debug("getAllowCrossDivisionDag failed", { error: err }); } ``` Using `logger.debug` for a failure condition is inappropriate for production. This makes debugging in production extremely difficult. | todo | - | Use `logger.warn` or `logger.error` depending on whether the condition is recoverable. |

| 248 | medium | Unknown | src/domains-runtime-orchestrator.ts:125 | ```typescript public snapshotReadiness(): DomainsReadinessSnapshot { const startupPlan = this.startupPlan ?? buildDomainsStartupPlan(); ``` The method uses `this.startupPlan` if available, otherwise calls `buildDomainsStartupPlan()` directly (not via the registry). But `buildDomainsStartupPlan()` registers services in the registry. If this method is called after `startup()` completed, it will create duplicate registrations in the global registry. The same pattern appears in `interaction-governance-runtime-orchestrator.ts:137`. | todo | - | Use `this.registry.get()` or ensure `buildDomainsStartupPlan()` is called through the registry to avoid duplicate registrations. |

| 249 | medium | Unknown | - | Console methods are monkey-patched at module load time: ```typescript console.log = ((...args: unknown[]) => writeStructuredLine("info", ...args)) as typeof console.log; ``` This global side effect runs immediately on import, making test isolation difficult. | todo | - | Consider lazy patching or making the console redirection opt-in via an explicit `install()` call. |

| 250 | medium | Unknown | - | ```typescript throw new StorageError("workflow.not_found", "workflow.not_found", { statusCode: 404, retryable: false, details: { taskId, tenantId: tenantId ?? null }, }); ``` Workflow not found is being thrown as a `StorageError`, which has `source: "storage"` and `category: "storage"`. This misclassifies workflow state errors as storage layer failures. A `WorkflowStateError` or `ValidationError` would be more appropriate. | todo | - | Use `WorkflowStateError` from `errors.ts` for workflow state violations, or create a dedicated `WorkflowNotFoundError`. |

| 251 | medium | Unknown | src/sdk/cli/pack-validate.ts:90 | `pack-validate` enforces mandatory presence of fields `sdk_semver`, `platform_min_version`, and `platform_max_version` as hard errors (lines 90-100). However, these fields are not defined in the pack manifest schema loaded by `validateBusinessPackManifest()`. This means a manifest that passes structural validation fails CLI validation for missing fields that were never declared required in the schema. | todo | - | Either add these fields to the BusinessPackManifest schema, or remove the mandatory checks from pack-validate.ts and instead emit warnings. Align with the pack-test command (line 88 comment). |

| 252 | medium | Unknown | - | `verifyManifestChecksum()` (lines 188-195) computes the combined checksum from the manifest's `checkpointRefs` and compares it to `manifest.combinedChecksum`. However, `createCheckpointManifest()` (lines 200-225) computes `combinedChecksum` at creation time using `computeCombinedChecksum(input.checkpoints)`. The `CheckpointRef.checksum` field is optional (line 39 of checkpoint-ref-validator.ts shows it can be undefined). If any `CheckpointRef` in the array has an undefined `checksum`, `computeCombinedChecksum` filters it out (line 174: `filter(Boolean)`) but still produces a hash — this means two manifests with different sets of checksums could produce the same combined checksum if one has missing checksums that were filtered. Additionally, if `computeCombinedChecksum` is called with empty checksums (all filtered out), it returns the hash of an empty string (line 178-180), which would be the same for all such manifests. | todo | - | Require `checksum` to be present when adding a checkpoint to a manifest (throw if missing). Remove the `filter(Boolean)` fallback that produces empty-string hash. Make `computeCombinedChecksum` throw if no checksums are available. |

| 253 | medium | Unknown | src/domains/governance/division-loader.ts:837 | Same issue as Issue 4: ```typescript } catch (err) { logger.debug("getDefaultDivisionRegistry failed", { error: err }); } ``` | todo | - | Upgrade to `logger.warn` or `logger.error`. |

| 254 | medium | Unknown | - | When `executeWithTimeout()` times out in the HALF_OPEN state, `recordFailure()` is called which transitions to OPEN and sets `nextAttempt = Date.now() + resetTimeout`. However, the normal HALF_OPEN->OPEN transition in `recordFailure()` also sets `nextAttempt`, but it is never used because the timeout path in `executeWithTimeout()` rejects with `CircuitBreakerTimeoutError` directly without checking the circuit state. The caller sees `CircuitBreakerTimeoutError` (extends CircuitBreakerOpenError via shared name) but with no `retryAfterMs`, making it impossible to know when the next probe will be allowed. The reliability variant's `executeWithTimeout()` does NOT have this issue because it uses a different abort mechanism. | todo | - | After timeout rejection, check if state is OPEN and attach `retryAfterMs` to the error. |

| 255 | medium | Unknown | - | The circuit breaker implementation in `ChannelGatewayService` (lines 516-553) is a hand-rolled implementation independent of the `CircuitBreaker` class in `stability/circuit-breaker.ts`. The service uses a simple failure-count + reset window approach (`circuitBreakerFailureThreshold`, `circuitBreakerResetMs`) while the platform's `CircuitBreaker` class implements the full state machine (CLOSED/OPEN/HALF_OPEN) with success thresholds and configurable timeouts. The two implementations are incompatible: if the channel gateway circuit breaker is open, there is no integration with the platform `CircuitBreaker` class for cross-plane visibility or event emission. | todo | - | Replace the hand-rolled circuit breaker in `ChannelGatewayService` with the platform `CircuitBreaker` class, or refactor to delegate to it. This ensures consistent state machine behavior and event bus integration across all planes. |

| 256 | medium | Unknown | - | `compareProfiles()` sorts by `inputPer1kUsd` only: ```typescript function compareProfiles(a: EligibleProfile, b: EligibleProfile): number { if (a.profile.pricing.inputPer1kUsd !== b.profile.pricing.inputPer1kUsd) { return a.profile.pricing.inputPer1kUsd - b.profile.pricing.inputPer1kUsd; } // ... } ``` The routing decision is driven primarily by input cost. However, output token costs can be 2-10x higher per token than input costs. A model with cheap input pricing but expensive output pricing could be selected over a model with slightly higher input but much lower output costs, resulting in higher actual bills. | todo | - | Compare `inputPer1kUsd * inputWeight + outputPer1kUsd * outputWeight` using a representative ratio (e.g., 30% input, 70% output for typical LLM workloads). Or make the comparison configurable based on workload type. |

| 257 | medium | Unknown | - | Multiple catch blocks return a result object but do not log the error: ```typescript } catch (err) { return { valid: false, error: err instanceof Error ? err.message : "jwt.validation_failed", ... }; } ``` The error is embedded in the return value but never written to a log. This makes it impossible to trace JWT validation failures without adding instrumentation at the call site. | todo | - | Add `logger.warn` with the error context before returning. |

| 258 | medium | Unknown | - | In `execute()`, when `fn()` is called and throws a non-reset error, `onFailure()` is called. Inside `onFailure()` for HALF_OPEN state, `nextAttempt` is set AND `transitionTo(OPEN)` is called. But then control returns to `execute()` which re-throws the original error. The `nextAttempt` is set but never made available to the caller via `CircuitBreakerOpenError.retryAfterMs` — the error thrown is the original `fn()` error, not a `CircuitBreakerOpenError`. The caller cannot determine when the next attempt will be permitted. | todo | - | Catch failures in HALF_OPEN and wrap in `CircuitBreakerOpenError` with correct `retryAfterMs`. |

| 259 | medium | Unknown | - | `unpackCheckpointEnvelope` validates `envelope.schema === envelope.metadata.payloadSchemaVersion` at line 189, but neither value is checked against the actual schema version stored in the uncompressed payload. A corrupted or swapped payload could pass this check while having a different internal schema version than either field indicates. | todo | - | After decompressing and parsing the payload, validate that the payload's own schema version field matches `envelope.schema`. Add a cross-field consistency check after JSON parsing. |

| 260 | medium | Unknown | - | `HealthService` has hardcoded default thresholds in its options interface (line 180-198): `staleWorkerThresholdMs: 5 * 60_000` (5 minutes), `queueStarvationThresholdSeconds: 5 * 60` (5 minutes). `ChannelGatewayDeliveryService` uses hardcoded `DEFAULT_DELIVERY_CONFIG` values. Neither of these aligns with the `workerHeartbeatTtlMs` used by `HttpApiServer` at line 965 (`workerHeartbeatTtlMs ?? 5 * 60 * 1000`). The health service marks workers stale at 5 minutes, the API server marks workers offline at 5 minutes, but there is no shared constant. The channel gateway delivery timeout of 30s is in a completely different time unit range with no documented relationship to any health threshold. | todo | - | Create a shared `health-thresholds.ts` constants module that exports all threshold defaults (`STALE_WORKER_THRESHOLD_MS`, `WORKER_HEARTBEAT_TTL_MS`, `QUEUE_STARVATION_THRESHOLD_SECONDS`) and import it in both `HealthService` and `ExecutionDispatchService`. The API gateway should use this shared constant for `workerHeartbeatTtlMs`. |

| 261 | medium | Unknown | src/platform/five-plane-interface/api/http-api-ser | ```typescript void pipeline( Readable.from([payload.body]), compress, response, ).catch(() => { response.destroy(); }); ``` The `.catch(() => {...})` swallows the error with an empty handler. While `response.destroy()` is called, the error itself is not logged. If compression fails mid-stream, operators have no visibility into why certain responses failed to reach clients. | todo | - | ```typescript .catch((err) => { this.logger.warn("http.compression_pipeline_failed", { error: err instanceof Error ? err.message : String(err) }); response.destroy(); }); ``` |

| 262 | medium | Unknown | src/interaction/nl-gateway/nl-gateway-model.ts:81 | `RiskPreview` interface defines fields in both camelCase (`overallRisk`, `riskFactors`, `sideEffects`, `approvalNeeded`) and snake_case (`overall_risk`, `risk_factors`, `side_effects`, `approval_needed`) as optional properties. The snake_case variants are documented as R5-32 additions (line 87-90), but the interfaces consuming `RiskPreview` (like `TaskBuildResult` line 81 and `ContextEnrichment`-adjacent types) only reference the camelCase variants. This creates confusion about which form is canonical and leads to potential inconsistency where one code path sets snake_case and another reads camelCase. | todo | - | Consolidate to one naming convention (camelCase per standard TypeScript conventions). Remove snake_case aliases from the interface or make them deprecated. |

| 263 | medium | Unknown | - | `remainingBudgetUsd` is computed as: ```typescript const remainingBudgetUsd = Math.max( 0, Math.min( input.policy.maxTaskCostUsd - projectedTask, input.policy.maxPackCostUsd != null && input.policy.maxPackCostUsd > 0 ? input.policy.maxPackCostUsd - projectedPack : Number.POSITIVE_INFINITY, input.policy.maxPlatformCostUsd != null && input.policy.maxPlatformCostUsd > 0 ? input.policy.maxPlatformCostUsd - projectedPlatform : Number.POSITIVE_INFINITY, input.policy.maxStepCostUsd != null && input.policy.maxStepCostUsd > 0 ? input.policy.maxStepCostUsd - projectedStep : Number.POSITIVE_INFINITY, input.policy.maxDailyCostUsd - projectedDaily, input.policy.maxMonthlyCostUsd - projectedMonthly, stageBudget != null ? stageBudget.maxCostUsd - next : Number.POSITIVE_INFINITY, ), ); ``` Using `Math.min` across all scopes means the "most restrictive" scope determines the returned remaining budget. However, the scopes have a hierarchy: platform > pack > task > step. If a task-level budget has 10 USD remaining but the pack-level has 100 USD remaining, returning `Math.min` would return 10, implying 10 USD is available when actually the pack-level is exhausted. The logic is inverted for the cascade: it should return the **minimum available** across all scopes that are relevant to the current execution context, but the warning check at line 629-636 correctly identifies which scopes are approaching limit. The remaining budget should reflect the **most restrictive active scope**, which is what `Math.min` does — but this is correct for determining how much can be spent, not for determining if a new step is allowed. The check at line 628 (`violation = checks.find((check) => check.projected > check.limit)`) correctly blocks on any violation, so the cascading remaining calculation is informational. However, the `remainingBudgetUsd` is returned in `BudgetGuardCascadeResult` which callers may use to make allow/deny decisions, not just informational display. | todo | - | Clarify in the type documentation that `remainingBudgetUsd` is the **most restrictive remaining budget** (the minimum across active scopes), not the sum or average. Callers should use `allowed: false` and `violatedScope` for deny decisions, not `remainingBudgetUsd`. |

| 264 | medium | Unknown | src/org-governance/approval-routing/approval-routi | ```typescript } catch (error) { return { eligible: false, reason: "internal_error" }; } ``` Error is caught but only a generic string is returned. The actual error is not logged. | todo | - | Log the error and include a trace ID in the returned reason. |

| 265 | medium | Unknown | src/org-governance/approval-routing/approval-routi | ```typescript } catch (error) { return { eligible: false, reason: "internal_error" }; } ``` No logging occurs, making it impossible to trace approval routing failures in production. | todo | - | Add `logger.warn` with error context and include a trace ID in the returned reason. |

| 266 | medium | Unknown | - | ```typescript export function shouldRollbackCanary(progress: CanaryProgress): boolean { return progress.errorRate > 0.05 \|\| progress.successRate < 0.90; } ``` Rollback uses errorRate > 5% and successRate < 90%, but `DEFAULT_PROMOTION_CRITERIA` uses errorRate <= 1% and successRate >= 99%. The gap between rollback and promotion creates a hysteresis: a canary could be blocked from promoting (needs 99% success) but also not rolled back (only fails at 90% success). This is exploitable: a canary at 95% success rate is neither promoted nor rolled back, yet continues receiving traffic indefinitely. | - | - | Align rollback thresholds with promotion criteria. Use the same configurable thresholds shared between both functions. |

| 267 | medium | Unknown | - | `computeCombinedChecksum` filters out empty checksums with `.filter(Boolean)` at line 174, but the concatenation at line 175 uses `\|` as a delimiter between checksum strings. If three checkpoints have checksums `abc`, `""`, `def`, the result is `"abc\|def"` which hashes identically to a case with checksums `ab`, `c\|def` (i.e., the empty string causes ambiguous boundaries). The delimiter is not present at boundaries, so empty checksums cannot be distinguished from adjacent checksum substrings. | - | - | Use a two-level approach: prefix each checksum with its length, e.g., `"3:abc\|3:def"` or `"3:abc##3:def"`. This ensures empty checksums cannot create ambiguous boundaries. Alternatively, use JSON array serialization for deterministic structure. |

| 268 | medium | Unknown | - | ```typescript class GraphQLAdapterError extends Error { public constructor( public readonly code: string, message: string, ) { super(message); this.name = "GraphQLAdapterError"; } } ``` This local error class does not extend `AppError` and is not registered in the error hierarchy. It uses `code` instead of `ErrorCode` type, and lacks `category`, `source`, `statusCode`, and other `AppError` fields. Throughout the file, `throw new GraphQLAdapterError(...)` is used instead of `AppError` subclasses. | todo | - | Either make `GraphQLAdapterError` extend `AppError` or use existing `AppError` subclasses (e.g., `ValidationError` for field resolution failures, `BusinessError` for execution failures). |

| 269 | medium | Unknown | src/org-governance/approval-routing/route-engine/i | Same pattern as Issue 7 — catch block returns `false` without logging: ```typescript } catch (error) { return false; } ``` | todo | - | Log the error and return a meaningful result. |

| 270 | medium | Unknown | - | `rollbackShift()` accepts any string as `trigger`: ```typescript rollbackShift(shiftId: string, trigger: RollbackTrigger, reason: string): RollbackRecord ``` `RollbackTrigger` is a union type defined at line 58, but TypeScript does not enforce exhaustiveness at runtime when calling from JavaScript. A caller could pass `"manual"` or `"health_check_failed"` but also any arbitrary string, polluting audit records. | todo | - | Add runtime validation of the trigger string against the union type before writing to the database. |

| 271 | medium | Unknown | - | `CircuitBreakerOpenError` in the platform/stability variant lacks `retryAfterMs` and `circuitName` fields that are present in the reliability variant (`reliability/circuit-breaker.ts` lines 377-386). Without `retryAfterMs`, callers cannot programmatically determine when to retry. Without `circuitName`, log correlation is harder. | todo | - | Add `circuitName: string` and `retryAfterMs: number \| null` to `CircuitBreakerOpenError` in `platform/stability/circuit-breaker.ts`. |

| 272 | medium | Unknown | - | The `requeueExecution` method (line 171) calls `this.store.dispatch.getExecution(action.targetId)` and if not found returns `{ applied: false, detail: "execution missing" }`. However, the subsequent logic at line 188 sets `execution.taskId` directly assuming execution exists. If `execution` is not null but `execution.taskId` is somehow malformed, the subsequent `this.store.operations.loadTaskSnapshot(execution.taskId)` at line 198 could receive an invalid ID. No validation of `taskId` format before use. | todo | - | Add a null check on `execution.taskId` before calling `loadTaskSnapshot`. Validate that taskId is a non-empty string. |

| 273 | medium | Unknown | - | `calculateBackoffForAttempt()` has a special case at line 204: `if (attemptNumber <= 0 && new Error().stack?.includes("channel-gateway-types-extended.test.ts") !== true)`. This is a test-file-specific bypass that returns `Math.min(config.initialBackoffMs, config.maxBackoffMs)` for attemptNumber <= 0 only when NOT running from a specific test file. This is a fragile pattern that silently changes behavior based on the call site's file name. If the test file is renamed or the check is inadvertently true in production, the backoff returns the capped minimum instead of computing proper exponential backoff. | todo | - | Remove the test-file-specific bypass. If attemptNumber <= 0 needs special handling for the test, use a test-specific injection mechanism (e.g., a `isTestEnvironment` flag passed via constructor or global flag) rather than checking the call stack. |

| 274 | medium | Unknown | src/platform/five-plane-interface/api/http-server/ | `buildJsonResponse()` wraps all successful responses in `{ requestId, data: payload }` shape (line 179). However, paginated responses from task routes (task-routes.ts line 105-110) return `{ tasks: page.items, nextCursor: page.nextCursor, ... }` directly without the `{ requestId, data }` wrapper, because `buildJsonResponse` is not used there — the handler returns an inline object. This means SDK paginated response parsing (`PaginatedResponse<T>` at api-client-types.ts line 35-41) expects `data: T[]` but may receive a different shape for some endpoints. | todo | - | Ensure all route handlers use `buildJsonResponse()` consistently, or define a single response envelope type that both SDK and gateway agree on. |

| 275 | medium | Unknown | - | `recordUsage()` computes: ```typescript const unitPriceUsd = quota?.unitPriceUsd ?? 0; const estimatedChargeUsd = roundCurrency(quantity * unitPriceUsd); ``` The `estimatedChargeUsd` is used to set the `hardCap` of the budget ledger (line 279) and to determine whether to reserve budget (line 274). But `estimatedChargeUsd` is based on the plan's quota `unitPriceUsd`, which may not reflect the actual model used in the task. If a task uses a more expensive model than the plan's generic rate, the reservation will be insufficient to cover the actual settlement. Conversely, if the task uses a cheaper model, the reservation is overly conservative and may unnecessarily block other tasks. | todo | - | After task completion, before calling `budgetAllocator.settle()`, look up the actual model used and compute the actual charge based on that model's `inputPer1kUsd`/`outputPer1kUsd` rather than the plan's generic `unitPriceUsd`. Use the actual cost for settlement, not the estimate. |

| 276 | medium | Unknown | - | `createCheckpointEnvelope` enforces `maxSizeBytes` (default 10MB) on the **compressed** payload before storage (lines 139-151). If compressed data exceeds the limit, it throws `CheckpointSizeExceededError`. However, when building a `CheckpointManifest`, the `totalSizeBytes` is computed as the sum of each checkpoint's `metadata.sizeBytes` (line 207-209 in checkpoint-manifest.ts), which is the **uncompressed** size (since `CheckpointEnvelopeMetadata.originalSizeBytes` tracks the uncompressed size, line 48). This means a manifest could report `totalSizeBytes` of 8MB (comfortably under 10MB per-checkpoint limit), but each compressed checkpoint might individually be 12MB (over limit) if the compression ratio is poor. The manifest validation cannot detect this because it only sums uncompressed sizes. Conversely, a manifest could show 15MB total, which seems over 10MB, but each individual checkpoint might be 5MB compressed — the actual stored size would be under the limit. | todo | - | Add `totalCompressedSizeBytes` to `CheckpointManifest` and compute it as the sum of `metadata.compressedSizeBytes`. Keep `totalSizeBytes` as uncompressed. Update validation logic to check both compressed and uncompressed totals against appropriate limits. |

| 277 | medium | interaction | src/interaction/nl-gateway/nl-gateway-support.ts:5 | `buildRiskPreview()` has a dead code block in the dry-run execution path. Lines 519-524 set `dryRunResult` with hardcoded values and `// Note: In production, this would be awaited` but the result is never actually used - the function falls through to keyword-based assessment. The R9-41 comment suggests this was intended to be async but was implemented incompletely. | todo | - | Either implement the async dry-run path fully in `buildRiskPreviewWithDryRun` (which correctly awaits), or remove the dead `dryRunResult` assignment in `buildRiskPreview` and document that keyword-based assessment is the fallback. |

| 278 | medium | interaction | src/interaction/autonomy/autonomy-service.ts:42 | `resolveLevel()` uses a simple threshold-based mapping (`riskScore >= 80 → manual`, `>= 60 → supervised`, etc.) that does not account for domain-specific risk adjustments. The `AutonomyLevelRequest` includes `taskType` which is never used in the level determination. High-risk task types like `approval_action` or `deploy` should potentially have elevated autonomy requirements beyond the generic risk score. | todo | - | Integrate `taskType` into the autonomy level determination. Add a task-type risk multiplier or domain-specific override table that adjusts the effective risk score before applying the threshold logic. |

| 279 | medium | interaction | src/interaction/autonomy/trust-scorer/index.ts:42 | `applyTrustDecay()` applies exponential decay with a fixed `decayRate` of 0.05 per day. The formula `score * Math.pow(1 - decayRate, inactiveDays)` means that after 180 days of inactivity, a score of 100 becomes approximately 0 (full decay). The TrustDecayWorker also has a 180-day threshold but these are not connected - the worker calls `applyTrustDecay` but the decay rate and threshold are independently defined. | todo | - | Connect the `NO_EXECUTION_DEMOTION_THRESHOLD_DAYS` constant to the decay rate calculation. If the threshold is 180 days, the decay rate should be calibrated so that at exactly 180 days, the score reaches the demotion threshold (e.g., suggestion level ≈ 30). Alternatively, compute the decay rate required to reach the target score at the threshold. |

| 280 | medium | interaction | src/interaction/dashboard/dashboard-projection-ser | `scheduleEmit()` sets a debounce timer but the callback only clears the timer and emits nothing. The comment says "In real implementation, this would emit to WebSocket clients / For now, deltas are consumed via consumePendingDeltas()". This means the debounce mechanism has no effect - deltas are immediately available via `consumePendingDeltas()` without waiting for the debounce period. | todo | - | Either implement the debounce emit path properly (deltas are held until the timer fires), or remove the debounce timer and `scheduleEmit()` entirely to avoid confusion. Document whether the debounce is a planned future feature or dead code. |

| 281 | medium | interaction | src/interaction/ux/conversation-history-service.ts | The `isRestricted` flag is checked at line 154 before persisting, but the session's `isRestricted` flag is captured at the time of `addTurn()`. If a turn is added when the session is not restricted, but the session later becomes restricted (e.g., user escalates to a regulated domain), subsequent calls to `completeSession()` or `abandonSession()` correctly see `session.isRestricted` but the previously-persisted turns remain in memory. | todo | - | Add a timestamp or version field to the session record so that restricted flag changes can be audited. Consider adding a cleanup pass when a session becomes restricted that explicitly removes prior non-restricted turns from the memory store if regulatory requirements demand it. |

| 282 | medium | interaction | src/interaction/ux/wizard/index.ts:78 | `goBackWizard()` pops from `history` but does not update `visitedStepIds`. The `advanceWizard()` function at line 92 correctly updates `visitedStepIds` with both current and next step IDs, but `goBackWizard` only updates `currentStepId` and `history`. This could cause `getWizardProgress()` to report inconsistent `visitedSteps` count. | todo | - | Add `visitedStepIds` update to `goBackWizard()`: when going back, the previous step is still visited, so it should remain in `visitedStepIds`. The `visitedStepIds` set only grows, never shrinks, so going back should not remove entries. |

| 283 | medium | interaction | src/interaction/ux/onboarding/index.ts:165 | `resolveMode()` and `buildOnboardingPlan()` compute domain recommendations independently using different keyword matching approaches. `resolveMode()` uses the `recommendDomains()` private method, while `buildOnboardingPlan()` also calls `recommendDomains()`. The keyword scoring signals in `recommendDomains()` include `userModeBonus` but `resolveMode()` calls it without passing a `context.userMode`, making the bonus unreachable in the initial mode detection. | todo | - | Ensure `resolveMode()` passes the `UserPortalContext` with `userMode` populated when calling `recommendDomains()`, or document that mode detection does not benefit from user mode hints. |

| 284 | medium | interaction | src/interaction/ux/workflow-builder-service.ts:624 | `validateGraph()` performs cycle detection via DFS with explicit visited/stack sets, which is correct. However, the cycle detection only runs when `validateGraph()` is called, and `validateGraph()` is called after `builder` is already constructed at line 710. The cycle check is not integrated into the graph building process itself - cycles could exist in the builder canvas before validation is invoked. | todo | - | Integrate cycle detection into `build()` before returning `WorkflowBuilderResult`, or at minimum ensure `validateGraph()` is always called and its messages are propagated to the caller even if `nextStepAllowed` is true. |

| 285 | medium | interaction | src/interaction/nl-gateway/nl-gateway-support.ts:4 | `deriveConversationState()` uses `confirmationRequired` as a signal but the logic can produce unexpected mappings. When `blockedByPolicy=true`, it returns `"Clarifying"` but this ignores the case where `blockedByPolicy` is true and `requiresClarification` is also true - the latter takes precedence. The function's truth table is not documented. | todo | - | Document the full decision matrix for conversation state transitions. Consider extracting to a lookup table or switch expression that explicitly handles all combinations of `requiresClarification`, `confirmationRequired`, and `blockedByPolicy`. |

| 286 | medium | interaction | src/interaction/goal-decomposer/index.ts:465 | `detectTemplate()` first checks `domainRecipes` (via `matchDomainRecipe`), then falls through to `domainRecipeService.matchRecipe()`, then falls through to regex patterns. The regex patterns use broad keywords like `/campaign\|marketing\|广告\|投放\|素材\|营销\|推广/i` which can match incidental mentions rather than intentional template selection. A casual mention of "marketing" in a non-marketing goal description could trigger the `marketing_campaign` template incorrectly. | - | - | Raise the specificity of regex pattern matching. Require at least 2 matching keywords from distinct categories before assigning a template, or weight the recipe/service match significantly higher than regex fallthrough. |

| 287 | medium | interaction | src/interaction/goal-decomposer/index.ts:415 | The cycle detection throws an error after `graphAnalysis` is computed, but the error message only contains `goal.goalId` and not the specific cycle path. A developer debugging a cycle would need to reconstruct the cycle from the task graph to understand which edges caused it. | todo | - | Include the cycle node IDs in the error message: `throw new Error(\`goal_decomposer.cycle_detected:${goal.goalId}:${graphAnalysis.cycleNodes.join("->")}\`);` where `cycleNodes` is derived from `topologicallySortedTaskIds.length !== taskIds.length`. |

| 288 | medium | interaction | src/interaction/proactive-agent/index.ts:417 | The autonomy level check for firing triggers requires `this.currentAutonomyLevel !== "semi_auto" && this.currentAutonomyLevel !== "full_auto"`. This means triggers only fire when autonomy is `semi_auto` or `full_auto`. But `suggestion` and `supervised` levels block firing. The logic is inverted - it reads as "if NOT suggestion AND NOT supervised", meaning "if semi_auto or full_auto". This is correct for the intended behavior but the condition is confusingly named and not documented. | todo | - | Rename the reason code to `proactive_agent.autonomy_level_insufficient` and add a comment explaining that only `semi_auto` and `full_auto` autonomy levels permit trigger firing. |

| 289 | medium | ops-maturity | src/ops-maturity/capacity-planner/capacity-plannin | `computeDynamicFailoverReserve()` returns static percentages per SLA tier (30/20/15) with no consideration of actual load, error budget burn rate, or regional constraints. In a real capacity planning context, these static values could lead to over- or under-provisioned failover reserves. | todo | - | Consider incorporating `providerQuotaLimits` and `regionFailoverReservePercent` from the capacity signal or forecast data to dynamically adjust the reserve rather than using static tier-based defaults. |

| 290 | medium | ops-maturity | src/ops-maturity/cost-optimizer/cost-optimization- | `recordCost()` throws an error if `decisionRef.trim().length === 0`. However, `unsourcedRecordCount` is decremented only after the throw check passes. If multiple invalid records arrive in sequence, the decrement can cause `unsourcedRecordCount` to go negative. The decrement logic does not guard against negative values. | todo | - | Guard the decrement with `if (this.unsourcedRecordCount > 0) { this.unsourcedRecordCount -= 1; }` or use `Math.max(0, this.unsourcedRecordCount - 1)`. |

| 291 | medium | ops-maturity | src/ops-maturity/cost-optimizer/recommendation-eng | `downgradePath` logic checks `recommendedProfile.pricing.inputPer1kUsd + recommendedProfile.pricing.outputPer1kUsd < currentProfile.pricing.inputPer1kUsd + currentProfile.pricing.outputPer1kUsd`. This uses raw price comparison without normalizing for capability or context differences. A "lower cost" model may not be appropriate for all task types. | todo | - | Add a model capability compatibility check before declaring a downgrade viable. Log a warning when a downgrade is recommended but the task type does not match the lower model's strengths. |

| 292 | medium | ops-maturity | src/ops-maturity/platform-ops-agent/platform-ops-a | `createProposal()` calls `this.chooseActionType()` before checking whether the computed `actionType` is in `this.definition.allowedActionTypes`. The `computeBlockers()` method checks this, but by then `actionType` is already selected and stored in the proposal. If the action is not allowed, it appears in `blockedBy` but the proposal summary references an action that is not executable. | todo | - | Move the allowed action type check before `chooseActionType()` so that a valid fallback action is chosen when the preferred action is not allowed. Avoid creating proposals with inherently blocked actions. |

| 293 | medium | ops-maturity | src/ops-maturity/explainability/explanation-pipeli | `generate()` throws if `depth === "L3"` and `options.forensicBudgetReservationId` is not provided, but `generate()` does not have access to actual billing or budget system to verify that the reservation is valid or has sufficient balance. The error is raised at generation time rather than at reservation confirmation time. | todo | - | Validate the reservation ID exists in the billing system before throwing. Consider deferring the error to a later pipeline stage where budget confirmation can occur. Add a pre-flight check method that `generate()` calls before attempting rendering. |

| 294 | medium | ops-maturity | src/ops-maturity/explainability/explanation-pipeli | `buildVersionLockRef()` calls `JSON.stringify()` on `rationaleWithoutLock`. If any field contains a circular reference or non-serializable value, this throws at runtime rather than producing a graceful error. | todo | - | Add a try/catch around `JSON.stringify` in `buildVersionLockRef()` and throw a descriptive error with the rationale ID if serialization fails. |

| 295 | medium | ops-maturity | src/ops-maturity/edge-runtime/edge-runtime-sync-se | The offline window check `Date.now() - createdAtMillis > profile.offlineMaxDuration!` compares milliseconds against a duration field. If `offlineMaxDuration` is meant to be in hours or minutes but is provided as milliseconds, the window check will incorrectly reject valid offline requests. | todo | - | Normalize the duration unit explicitly. Document the expected unit for `offlineMaxDuration` and add a runtime assertion or conversion to ensure consistent units. |

| 296 | medium | ops-maturity | src/ops-maturity/learning/evidence-store.ts:120 | `cloneEvidenceRecord()` uses `structuredClone()` which is not available in all Node.js versions prior to Node 17. The codebase may run on older Node versions where this would throw a `ReferenceError`. | todo | - | Verify the minimum Node.js version for this project. If Node 17+ is guaranteed, document it in the README or package.json engines field. Otherwise, replace `structuredClone` with a manual shallow clone or `JSON.parse(JSON.stringify())`. |

| 297 | medium | ops-maturity | src/ops-maturity/monitoring/anomaly-detection-serv | `ingestMetric()` stores the entire buffer array per metric name and relies on `evictExpired()` for cleanup. However, `evictExpired()` only triggers when `this.metricBuffer.size` exceeds `maxBufferEntries`. If many distinct metric names are ingested, this could cause unbounded memory growth before eviction triggers. | todo | - | Evict based on per-metric buffer size or total age rather than total distinct keys. Consider a sliding window based on timestamp rather than count-based eviction. |

| 298 | medium | ops-maturity | src/ops-maturity/agent-lifecycle/canary-controller | `shouldRollbackCanary()` is hardcoded with literal thresholds (`errorRate > 0.05`, `successRate < 0.90`) that are not configurable and differ from the promotion gate criteria. This creates inconsistent behavior between rollback and promotion logic. | todo | - | Extract these thresholds into a shared constants object or configuration passed to both functions. Align the rollback thresholds with the promotion gate criteria. |

| 299 | medium | ops-maturity | src/ops-maturity/emergency/resume-protocol/index.t | `canResumeFromPanic()` requires `plan.checkpointsVerified`, `plan.forensicSnapshotReviewed`, `plan.rollbackPlanReady`, and `plan.validationRunPassed` all to be true. However, if any of these fields is `undefined`, the `?? false` coalescing handles them correctly, but if they are `null` or a non-boolean truthy value, they bypass the check. Additionally, the `approvalCount` fallback logic does not validate that approvers themselves have actually approved. | todo | - | Use explicit `=== true` checks rather than truthiness. Add validation that `plan.approvalCount` reflects the actual count of entries in `plan.approvedBy` when it is derived. |

| 300 | medium | ops-maturity | src/ops-maturity/drift-detection/drift-detector-se | `computeFeatureDifference()` computes a symmetric set difference but normalizes by raw count rather than union cardinality. Two fingerprints with 10 features each that share 9 would show a difference of 2 (10-9+10-9=2), yielding `driftScore = 0.2` which maps to "low" severity. This is not meaningful for small feature sets. | todo | - | Normalize by the size of the union of features: `difference / union.size`. This yields a Jaccard-like metric that is more interpretable. |

| 301 | medium | platform/five-plane-execu | src/platform/five-plane-execution/lease/execution- | The TTL expiration check `activeLease.expiresAt <= occurredAt` uses `<=` (less-than-or-equal). This means a lease that expires exactly at `occurredAt` is considered expired. However, `expireActiveLeaseIfNeeded()` at line 763 uses `>` (greater-than), meaning a lease that expires exactly at the boundary is NOT auto-expired. The inconsistency could allow a just-expired lease to still be considered "active" for write validation purposes. | todo | - | Use consistent comparison operators. If `<=` is used for validation (meaning "expired if expiresAt <= now"), then `expireActiveLeaseIfNeeded()` should use the same comparison: `Date.parse(activeLease.expiresAt) <= Date.parse(occurredAt)` to expire. |

| 302 | medium | platform/five-plane-execu | src/platform/five-plane-execution/runtime-state-ma | `assertAuditRef()` is called in the transition chain but the condition `!hasAuditRef(command.auditRef)` can throw even for transitions that don't require auditing (e.g., budget reservation status updates during normal operation). The audit ref requirement is unconditional for ALL transitions. If the audit ref format changes, all aggregate types fail transitions uniformly. | todo | - | Consider making audit ref optional for non-critical transitions (e.g., budget ledger "reserved" to "settled" transitions could use an internal audit ref format). Add a configuration to the RuntimeStateMachine that controls which aggregate types require audit ref enforcement. |

| 303 | medium | platform/five-plane-execu | src/platform/five-plane-execution/execution-engine | `normalizeToolInputForHash()` sorts object keys with `Object.keys(input).sort()`. However, JSON.stringify with a custom replacer does NOT guarantee key ordering across all inputs. Some inputs may have already-sorted keys, others may not. Additionally, if the object has nested structures, only the top-level keys are sorted - nested object key order is not normalized. | todo | - | Use a deep normalization approach that sorts keys at all levels, not just top-level. Consider using `JSON.stringify()` with sorted keys or a recursive normalization function. |

| 304 | medium | platform/five-plane-execu | src/platform/five-plane-execution/compensation-man | `executeCompensationSteps()` iterates through compensation steps and catches errors per step. However, if `executeCompensationStep()` returns `false` (indicating failure), the code sets `finalStatus = "failed"` but does NOT record the error message or stack trace in `evidenceRefs`. The error is caught in the catch block (line 248), but when `success` is `false` from the step execution (not an exception), the error details are lost. | todo | - | When `executeCompensationStep()` returns `false`, capture the failure reason in the evidenceRefs. Add logging or error recording that captures the step type and targetRef that failed, not just the exception stack. |

| 305 | medium | platform/five-plane-execu | src/platform/five-plane-execution/execution-engine | R4-28 comment says "Write-ahead logging for cost events - persist BEFORE execution" and the code inserts `costEventWAL` with "pending" status before the transaction commits. However, at line 594 inside the transaction, `commitCostEventWAL(costEventId)` is called. If the transaction fails after the WAL insert but before commit (e.g., crash), the pending cost event would remain in the WAL table. The comment mentions "On crash recovery, uncommitted cost events can be detected and cleaned up" but there is no visible cleanup/recovery code in this file for orphaned WAL entries. | todo | - | Add a recovery sweep that cleans up pending WAL entries older than a threshold (e.g., entries where `created_at` is older than the longest possible transaction window). Document the recovery mechanism or point to the sweeper code that handles this. |

| 306 | medium | platform/five-plane-execu | src/platform/five-plane-execution/state-transition | `WorkflowTransitionService.apply()` has nested try-catch blocks at lines 197-214. The outer catch at line 210 catches errors and re-throws only if it's not the "unused" sentinel error. This is confusing and the indentation suggests the try-catch was added after initial implementation. The catch at line 206-208 catches `"unused"` errors from `createTier1StatusEvent`. If this sentinel is not thrown by the repository, the outer catch would silently suppress other errors. | todo | - | Replace the nested try-catch with explicit conditional logic. If `createTier1StatusEvent` should only be called in certain cases (not "unused"), add an explicit flag or check BEFORE calling it rather than catching and suppressing sentinel errors. |

| 307 | medium | platform/five-plane-execu | src/platform/five-plane-execution/state-transition | `TaskTerminalTransitionService.apply()` calls `workflowStateMachine.assertTransition()` before checking if `shouldTransitionWorkflow`. If `shouldTransitionWorkflow` is false (workflow is null), but the workflow status is "running", the assertTransition would throw before the null check prevented the update. The logic ordering is correct (assertTransition only runs when shouldTransitionWorkflow is true), but the error message for the workflow transition path in lines 496-512 could collide with the early CAS check. | todo | - | Add a debug log or comment clarifying that `assertTransition` is only called after `shouldTransitionWorkflow` is confirmed true, and that the subsequent `updateWorkflowStateCas` also validates fromStatus. This double-check is intentional but needs documentation. |

| 308 | medium | platform/five-plane-execu | src/platform/five-plane-execution/worker-pool/exec | The writeback service extracts `taskOutputJson` and `outputsJson` from either the writeback input or the existing records: `const taskOutputJson = input.taskOutputJson ?? task.outputJson ?? "{}"`. If both input and task record have null values, it defaults to `"{}"`. However, this JSON string `"{}"` is later passed to `transitionTaskTerminalState()` which expects valid JSON or null. An empty object `{}` may not be the correct default when both source values are null - it should perhaps be `null` to indicate no output was produced, or the transition should validate this case. | todo | - | Clarify the default behavior: if no output was produced (both inputs are null), use `null` rather than `"{}"`. Update the null-coalescing chain to: `input.taskOutputJson ?? task.outputJson ?? null` and ensure downstream code handles `null` correctly. |

| 309 | medium | platform/five-plane-execu | src/platform/five-plane-execution/dispatcher/execu | When backpressure blocks a ticket, two near-identical event records are emitted in the same transaction: `dispatch:backpressure_rejected` (line 276) and `dispatch.backpressure_rejected` (line 292). The event names differ only in namespace separator (`:` vs `.`). Downstream consumers processing both events would see duplicate events for the same logical occurrence, causing double-counting in metrics. | todo | - | Remove the duplicate event emission. Keep only `dispatch:backpressure_rejected` (with colon separator, matching the naming convention for tier-2 events). The `dispatch.backpressure_rejected` variant appears to be a typo or copy-paste error. |

| 310 | medium | plugins | src/plugins/validators/basic-evaluator.ts:210 | The manifest for `plugin.core.basic-evaluator` declares `spiTypes: ["validator", "evaluator"]` (line 210), indicating it implements both roles. However, `createBasicValidatorPluginInternal()` (lines 397-441) only sets `spiType: "validator"` on the returned plugin object (line 402). This creates a discrepancy between the manifest and the actual plugin's declared type. | todo | - | Either remove `"evaluator"` from the manifest's `spiTypes` array, or add an `evaluator` SPI type to the plugin object returned by the factory (which would require the plugin to implement the evaluator interface as well). |

| 311 | medium | plugins | src/plugins/presenters/coding-presenter.ts:45 | `citations` is declared as `string[]` (line 45 is the assignment: `citations: input.artifacts.map(...)`). But `input.artifacts` is typed as `ArtifactRef[]` (from plugin-spi.ts line 219). `ArtifactRef` is not necessarily a `string` - it is likely an object with fields like `artifactId`, `type`, etc. Pushing these objects into a `string[]` violates the type contract. | todo | - | Extract the string identifier from each `ArtifactRef` before pushing to citations, e.g., `citations: input.artifacts.map((ref) => ref.artifactId ?? ref.toString())`. |

| 312 | medium | plugins | src/plugins/presenters/operations-presenter.ts:70 | Same issue as Issue 11. `input.artifacts` is `ArtifactRef[]` but is pushed directly into `citations: string[]`. | todo | - | Same as Issue 11 - extract a string identifier from each `ArtifactRef`. |

| 313 | medium | plugins | src/plugins/presenters/growth-presenter.ts:78 | Same artifact-to-citation issue. The code pushes raw `artifactRef` strings to `citations` but does not verify that `ArtifactRef` is a string type. | todo | - | Same as Issue 11 - ensure `ArtifactRef` values are properly converted to strings before adding to citations. |

| 314 | medium | plugins | src/plugins/planners/basic-planner.ts:22 | `suggestWorkflow` accesses `task.assessment.complexity` and `task.assessment.approvalPolicy.required` without null checks. If `task.assessment` is `null` or `undefined`, this will throw a runtime error. The `DomainPlannerPlugin` interface (plugin-spi.ts lines 199-210) defines `assessment: UnifiedAssessment`, but the interface does not guarantee `assessment` is non-null within `UnifiedAssessment`. | todo | - | Add null checks: `if (!task.assessment) return null;` at the start of `suggestWorkflow`, or use optional chaining with a fallback. |

| 315 | medium | plugins | src/plugins/operations-config.ts:178 | `externalAdapters: ["github"]` is correct, but the `pluginBindings` at lines 179-183 reference `"plugin.operations.presenter"` etc. However, the `pluginType: "tool"` in these bindings does not match the actual `spiType: "presenter"` (for presenter) and `spiType: "retriever"` (for retriever) declared in the plugin implementations. This mismatch means the binding configuration doesn't accurately describe the plugins it targets. | todo | - | Align `pluginType` in pluginBindings with the actual `spiType` of the referenced plugin. Use `pluginType: "presenter"` for presenter bindings, `pluginType: "retriever"` for retriever bindings, etc. |

| 316 | medium | plugins | src/plugins/growth-config.ts:202 | `pluginType: "tool"` is used for all plugin bindings (retriever, presenter, validator, planner) even though the actual plugins declare different `spiType` values. For example, `growth.presenter` binding uses `pluginType: "tool"` but the plugin declares `spiType: "presenter"`. The binding role is correctly separated into `bindingRole` property, but `pluginType` still carries an incorrect value that could confuse the registry's type validation. | todo | - | Set `pluginType` to match the actual SPI type of the bound plugin (`"retriever"`, `"presenter"`, `"validator"`, `"planner"`), and use `bindingRole` for any semantic role differentiation. |

| 317 | medium | tests/unit | - | The UI `createOfflineQueueInterceptor()` (interceptors.ts lines 188-213) queues write requests when `navigator.onLine === false`. It uses `queue.enqueue()` with a principal object containing `principalId: "ui-operator"` (line 203). The SDK has no offline queue mechanism — it does not handle network offline scenarios. This creates a feature gap where: - UI can queue writes for later replay when coming back online - SDK throws errors or fails on network failure with no offline support Additionally, the hardcoded `principalId: "ui-operator"` (interceptors.ts line 203) in the offline queue entry uses a different principal naming convention than the SDK expects (`subject`/`principalId`). | todo | - | Either add offline queue support to the SDK, or document that offline resilience is a UI-only concern. If the offline queue principal (`ui-operator`) is replayed through the SDK, ensure the server can handle this mismatched principal naming. |

| 318 | medium | tests/unit | - | The UI REST client uses `DEFAULT_ACCEPT_VERSION_HEADER = "2026-04-01,2026-01-01"` (rest-client.ts line 58). The SDK `buildAuthHeaders()` function (api-client.ts line 72-81) does **not** set an `Accept-Version` header — it only sets `authorization`, `X-Platform-Version`, `X-SDK-Version`, and `X-Contract-Version`. The SDK relies on `performVersionHandshake()` (api-client.ts lines 111-113) which fetches `/handshake` explicitly to negotiate version compatibility, rather than sending version headers with every request. This creates an inconsistency: - UI sends `Accept-Version: 2026-04-01,2026-01-01` on every request - SDK sends no `Accept-Version` header by default | todo | - | Add `Accept-Version` header support to the SDK's `buildAuthHeaders()` to match the UI's default. The SDK should send the same version headers the UI sends, or document why SDK requests are version-negotiated differently. |

| 319 | medium | tests/unit | - | The UI `DefaultRESTClient` executes interceptors in this order (rest-client.ts lines 409-434): 1. `onRequest` runs forward (`for` loop, line 410-412) 2. Transport dispatch happens 3. `onResponse` runs reverse (`[...interceptors].reverse()`, line 417-420) 4. `intercept` wraps dispatch in reverse order (line 426-431) The `createAuthInterceptor` (interceptors.ts lines 90-158) implements `onRequest` (sets bearer token), `onResponse` (handles 401), and `intercept` (retries with refreshed token). The `intercept` method at line 132-155 is the primary auto-refresh mechanism. The SDK's retry logic (api-client.ts lines 580-594) is built into the `request()` method directly, not as an interceptor. It retries on retryable status codes (429, 5xx) but not via an interceptor chain. The SDK does not have an equivalent to `createAuthInterceptor`'s auto-refresh-with-retry pattern. | todo | - | Extract the UI's auth interceptor refresh logic into a reusable pattern that can be applied consistently, or document that the SDK handles retries differently (in-request vs interceptor-chain) and these two approaches must be kept in sync. |

| 320 | medium | tests/unit | - | The `createAuthInterceptor()` accepts a `TokenResolver` interface (interceptors.ts lines 24-30) with methods `getAccessToken`, `getAccessTokenWithRefresh`, `shouldRefresh`, and `handleUnauthorized`. This is a well-designed refresh token pattern. However: 1. The UI `auth-store.ts` exposes `accessToken`, `refreshToken` directly on the store state (lines 76-77) 2. The `updateTokens()` method (auth-store.ts lines 111-121) can update tokens but the store does not expose a `getAccessTokenWithRefresh()` method 3. The `TokenResolver` interface is defined in interceptors.ts but the UI auth store does not implement it This means the UI's auth interceptor cannot leverage the `TokenResolver` interface for refresh — it must be manually passed a token string or a resolver object that manually exposes the refresh method. The `createAuthInterceptor` in interceptors.ts line 90 requires either a static string or a `TokenResolver`. Since the auth store stores tokens but doesn't expose a refresh method conforming to `TokenResolver`, the UI likely passes a static token string to the interceptor, bypassing the refresh capability. | todo | - | Implement `TokenResolver` on the auth store, or create a bridge that wraps the auth store as a `TokenResolver`. This would enable the auto-refresh interceptor pattern to work with the UI's auth state. |

| 321 | medium | tests/unit | - | The SDK has a full SSE-based event subscription system (`subscribeToEvents()`, api-client.ts lines 261-375) with reconnection logic, abort controllers, and event parsing. The UI has only WebSocket clients (`BrowserWSClient`, `InMemoryWSClient`, `SharedWorkerWSClient`) with no SSE fallback path as a primary transport. The UI `ws-client.ts` line 18 defines `useSseFallback()` but this only changes the status to `sse-fallback` — it does not establish an SSE connection. The `BrowserWSClient` does not implement SSE as an actual transport. If the WebSocket connection fails and the UI calls `useSseFallback()`, the status changes but no actual SSE connection is established. The `InMemoryWSClient` is used as a fallback in-memory pub/sub but is not connected to a real SSE stream. | todo | - | Implement actual SSE fallback in `BrowserWSClient` or document that SSE fallback is intentionally stubbed and the UI relies solely on WebSocket for real-time events. |

| 322 | medium | tests/unit | tests/unit/interaction-governance/interaction-gove | Test `buildInteractionGovernanceRuntimeCatalog governance array is frozen (but not interaction)` asserts `Object.isFrozen(catalog.governance)` and comments "Note: catalog itself is NOT frozen, interaction is NOT frozen". This test comments on implementation details (which arrays are frozen) that may change. The comment describes expected behavior inconsistently - it says "frozen (but not interaction)" but the assertion only checks `governance` is frozen. This is a golden test checking internal freeze state rather than behavior. | todo | - | If the freeze behavior is a contract, document it as such and ensure it is tested through behavior rather than direct property checks. If not a contract, remove this test or convert it to verify behavior that depends on freeze immutability. |

| 323 | medium | tests/unit | - | `riskLevelForSubject()` at line 148 checks `costType === "model"` but `"model"` is not defined in the `CostSubjectType` union (line 22). This means any cost record with `costType: "model"` would fall through to the default `baseRisk` without the upgrade to "medium" that LLM costs receive. Meanwhile, the execution engine's cost event WAL (lines 560-577) records hardcoded `provider: "minimax"` and `model: "MiniMax-M2.7"` for every step, creating a systematic attribution gap where actual model names from step configuration are ignored. | todo | - | Add `"model"` to `CostSubjectType` union or remove the check from `riskLevelForSubject()`. In `multi-step-supervisor.ts`, extract actual provider/model from step configuration rather than hardcoding: ```typescript provider: step.provider ?? routing.provider ?? "minimax", model: step.model ?? routing.model ?? "MiniMax-M2.7", ``` |

| 324 | medium | tests/unit | - | `CheckpointEnvelopeMetadata` at line 46 defines `payloadSchemaVersion: string` but does not include a `domainId` field. The `DomainBaseline` interface (line 108-129) defines `domainId: VerticalDomainId` as a required field that identifies which domain owns the baseline. However, checkpoint envelopes have no domain affinity - they store arbitrary workflow step checkpoints with no reference to which domain the workflow belongs to. This creates a conflict when domain-scoped checkpoints need to be stored and retrieved. If `SessionDualStorageService` records checkpoint data for sessions that belong to specific domains, the checkpoint envelope has no field to carry that domain context. The envelope's `schema` field (line 84) is a schema version string, not a domain identifier. Conversely, `bootstrapVerticalDomainBaselines()` at line 608 registers knowledge namespaces via `stagedRegistry.registerKnowledgeNamespace(namespace, baseline.domainId)`, but these namespaces have no connection to checkpoint storage - there is no `registerCheckpointNamespace()` call or equivalent. | todo | - | Add `domainId?: string` to `CheckpointEnvelopeMetadata` so domain context can be attached to checkpoints when needed. Update `SessionDualStorageService` to extract domain context from the session record and include it in the checkpoint envelope metadata when recording session events that include checkpoint references. |

| 325 | medium | tests/unit | tests/unit/domains-runtime-orchestrator.test.ts:10 | Test `DomainsRuntimeOrchestrator.startup later steps depend on earlier rings` uses a non-null assertion `result.steps.find((s) => s.stepId === "ring2")!` without checking if the result is undefined. If the startup order changes and ring2 is not found, this would throw a cryptic error rather than a clear assertion failure. | todo | - | Add an explicit assertion that `ring2` step exists before accessing it: `const ring2 = result.steps.find((s) => s.stepId === "ring2"); assert.ok(ring2 !== undefined, "ring2 step should exist");` |

| 326 | medium | tests/unit | - | `session-management.ts` line 96 checks `process.env.NODE_ENV === "production" && process.env.AA_ALLOW_IN_MEMORY_SESSION_STORE !== "1"` to throw if in-memory session store is used in production. `service-auth.ts` line 96 has an identical check for `AA_ALLOW_IN_MEMORY_SERVICE_IDENTITY_STORE`. Both modules check environment variables independently. There's no shared enforcement mechanism. If one module is configured to allow in-memory storage but the other is not, the system could have an inconsistent security posture - session data is durable but service identity is in-memory (or vice versa). | todo | - | Create a shared utility function `assertInMemoryStorageAllowed(storeName: string)` that checks the appropriate environment variable and throws consistently. Use this in both `session-management.ts` and `service-auth.ts`. Document the expected behavior: both stores should either be in-memory or distributed together in production. |

| 327 | medium | tests/unit | - | The delegation-request module has a comment referencing "ADR-026 8-factor budget tracking" but no ADR-026 exists in the documentation. This is cross-module leakage — tests for delegation-request contracts don't cover this gap. | todo | - | Either create ADR-026 documenting the 8-factor budget tracking model, or update the code reference to point to the correct ADR number. |

| 328 | medium | tests/unit | - | `getStatistics()` returns aggregate healing metrics but these are not integrated into the execution engine's event system. The execution engine emits events like `platform.harness_run.status_changed` (line 401 in multi-step-orchestration.ts) but self-healing events are not emitted. Without events, there is no audit trail for healing operations in the context of execution runs. If a step fails, is healed, and then succeeds, there is no event linking the healing operation to the subsequent success. | todo | - | Emit healing events to the event store with `eventType: "ops_maturity.self_healing.*"` so healing operations are traceable in the execution context. Include `harnessRunId` and `executionId` in healing receipts so healing can be correlated with execution runs. |

| 329 | medium | tests/unit | - | Several cross-module relationships lack ADR documentation: 1. Relationship between pack lifecycle stages and domain lifecycle stages (Issue 1) 2. Coupling between domain onboarding phases and pack lifecycle stages (Issues 2, 3, 10) 3. Compatibility rules between `trustTier`/`securityLevel` and `sandboxTier` (Issues 4, 5) 4. Pack domain association lifecycle during domain archival/deprecation (Issue 6) | todo | - | Create ADR documenting: 1. Pack lifecycle stages and their relationship to domain lifecycle stages 2. Domain onboarding phase → pack lifecycle stage coordination rules 3. Security level / sandbox tier compatibility matrix 4. Pack-domain association behavior during domain/pack archival |

| 330 | medium | tests/unit | - | `createNodeAttempt()` at line 300 checks `attemptNo < 1` and throws `ValidationError`. The test at lines 232-238 creates an attempt with `attemptNo: 1` (valid), but there is no test for `attemptNo: 0` or negative values to verify the boundary enforcement. | todo | - | Add test: ```typescript test("createNodeAttempt throws on attemptNo < 1", () => { assert.throws( () => createNodeAttempt({ nodeRunId: "nr1", attemptNo: 0, attemptKind: "initial", executorRef: "w1", inputSnapshotRef: artifact }), ValidationError, ); }); ``` |

| 331 | medium | tests/unit | - | `simulate()` takes `CostSimulationScenarioInput[]` and computes projected costs, but this simulation is not connected to the execution engine's budget simulation. The budget allocator in `budget-allocator.ts` has no simulation method - it only reserves and settles actual costs. If an operator wants to simulate "what if we reduce cost by 20%", the cost optimizer can simulate but the execution engine has no way to run a test execution with reduced budget. There's a gap between cost simulation (what-if analysis) and execution budget enforcement (actual limits). | todo | - | Add a budget simulation mode to `BudgetAllocator` that accepts a `CostSimulationScenarioInput` and computes what the budget would look like under the scenario. Or document that cost simulation is for offline analysis only and should not affect execution budget limits without human review. |

| 332 | medium | tests/unit | - | `validateExecutableContract` is tested only for `TaskDraft`, `ConfirmedTaskSpec`, and `RequestEnvelope` (line 186-196). There is no test for validating `PromptBundle` contracts, which have their own validation logic (`validatePromptBundle`). The cross-module interaction between executable-contracts validation and prompt-bundle validation is untested at the integration point. | todo | - | Add test: ```typescript test("validateExecutableContract rejects invalid PromptBundle", () => { assert.throws( () => validateExecutableContract("PromptBundle", { bundleId: "" }), ValidationError, ); }); ``` |

| 333 | medium | tests/unit | tests/unit/root-entry-mode-consolidation.test.ts:1 | Test `PlatformStartupTargetKind is the single source of truth (issue 2002 fix)` uses a TypeScript type-level check with a conditional type (`Expected extends PlatformStartupTargetKind ? true : false`) that is then immediately discarded with `void _typeCheck`. This is a compile-time check only and provides no runtime test coverage. If the type check passes but the actual type definition is wrong at runtime, this test would not catch it. | todo | - | While type-level testing has limited options in TypeScript, add runtime assertions that validate the type actually behaves as expected (e.g., test that values of type `PlatformStartupTargetKind` can be assigned and used correctly). |

| 334 | medium | tests/unit | tests/unit/interaction-governance/interaction-gove | Test file has no `test.afterEach` or `test.beforeEach` cleanup for the singleton `ServiceRegistry.getInstance()`. The tests at lines 117, 126, 135, 144, 155, 165, 175, 184, 195 use `ServiceRegistry.getInstance()` without resetting it between tests. If tests run in non-sequential order, state from one test could leak into another. Compare with `tests/unit/domains-runtime-orchestrator.test.ts` which correctly calls `await registry.reset()` in `test.afterEach` or before each test. | todo | - | Add a `test.beforeEach` or `test.afterEach` hook that calls `await ServiceRegistry.getInstance().reset()` to ensure a clean registry state for each test. This pattern is already established in `domains-runtime-orchestrator.test.ts`. |

| 335 | medium | tests/unit | - | The `HarnessRun` interface defines `goal?: string` and `mode?: string` at lines 287-288 of contract-models.ts. However, `HarnessRunSchema` at lines 236-267 of schemas.ts does not include `goal` or `mode` fields. The test at lines 288-323 validates canonical statuses but does not validate these optional fields, and `validateExecutableContract("HarnessRun", run)` would strip these fields during schema validation. | todo | - | Add to `HarnessRunSchema`: ```typescript goal: z.string().optional(), mode: z.string().optional(), ``` |

| 336 | medium | tests/unit | - | `SessionDualStorageService` stores session events in JSONL files named after `sessionId` (line 96: `session-${safeSessionId}.jsonl`). The session record includes `taskId` and `channel` but no domain identifier. Meanwhile, `DomainRegistryService.registerKnowledgeNamespace()` at lines 336-340 tracks knowledge namespaces per domain via `knowledgeNamespacesByDomain`. There is no intersection - session storage has no domain awareness and domain registry has no session awareness. However, `DomainBaseline` (line 116) includes `knowledgeSchema: DomainKnowledgeSchema` with `namespaceIds` like `${seed.domainId}/default`. If session events reference knowledge operations, the session's taskId could be correlated with domain via `TaskRegistry` or `WorkflowCatalog`, but this correlation is not made in `SessionDualStorageService`. The session JSONL files use `sessionId` as the primary key but do not include `domainId` in the event payload (lines 171-186 record `session_created` payload without domain context). This means replaying a session's JSONL does not reveal which domain owned the session without external correlation. | todo | - | Add `domainId: string` to `SessionEvent.payload` (line 51) so session events carry domain context. When `recordSessionCreated()` is called, extract domain context from `session.domainId` (if present) or from `DomainRegistryService` by resolving `session.taskId` to a domain. This enables session replay to understand domain ownership without external lookups. |

| 337 | medium | tests/unit | tests/unit/root-entry-summary.test.ts:1 | This is a golden test with hardcoded exact counts: `totalCapabilityCount: 31`, `ring1: 8, ring2: 11, ring3: 12`, etc. Any change to the actual counts in source code would break these tests even if the changes are intentional (e.g., adding new capabilities). The test file contains no indication of when these values were frozen or why they represent the expected canonical state. | todo | - | Consider whether these exact counts should be validated against computed values from the actual catalog rather than hardcoded. If hardcoded values are intentional, add a comment explaining why these specific values represent the canonical state. Alternatively, derive expected values from the runtime catalogs dynamically. |

| 338 | medium | tests/unit | - | `createMockBundle()` (line 490-493) computes normalized version via: ```typescript const normalizedVersion = displayVersion .replace(/^v/i, "") .split(".") .reduce((accumulator, segment, index) => accumulator + Number(segment) * (index === 0 ? 100 : index === 1 ? 10 : 1), 0); ``` This yields `"v1.0"` → `100`. But the contract's `normalizeVersion()` at line 332-338 yields `"v1.0"` → `1*100 + 0*10 + 0 = 100` — consistent. However, for `"v1"` (major-only), the mock yields `1*100 = 100`, while the contract at line 345 yields `1*10 = 10`. If tests use "v1" as displayVersion, version comparison would be incorrect. | todo | - | Use `normalizeVersion()` from the actual contract in `createMockBundle()` to ensure consistency, or add a comment explaining the different normalization semantics. |

| 339 | medium | tests/unit | - | `DomainRegistryService` tracks knowledge namespaces per domain in `knowledgeNamespacesByDomain: Map<string, Set<string>>` (line 23). This is a read-only tracking structure - namespaces are registered but never queried for intersection with session data. `SessionDualStorageService` has no concept of knowledge namespaces - it treats sessions as pure event streams. When a session ends and its events are replayed via `replaySessionEvents()` (line 298), there is no integration point that would use domain knowledge namespaces to filter, redact, or interpret session content. The two modules operate completely independently: - Domain registry knows about domain boundaries and knowledge namespaces - Session storage knows about session event chronology but not domain semantics | todo | - | Add a `DomainKnowledgeIntegration` service that, given a `sessionId`, can: 1. Resolve the session's taskId to a domainId via some registry lookup 2. Query `DomainRegistryService.getKnowledgeNamespaces(domainId)` to get authorized namespaces 3. Use these namespaces to filter or annotate session events during replay |

| 340 | medium | tests/unit | - | `execute()` checks `consecutiveFailures >= this.policy.maxRetries` (line 103) and then checks `timeSinceLastAttempt < this.policy.cooldownPeriodMs` (line 109). If both conditions are true, it returns early with `healed: false`. However, the execution engine's `multi-step-supervisor.ts` has its own retry logic (lines 144+) with separate retry counts (`executionAttemptCounter`, `workflowRetryCount`). The self-healing cooldown is independent of the execution retry budget - a component could be blocked from self-healing due to cooldown while the execution engine continues to retry the same failing step, burning budget without healing occurring. | todo | - | Coordinate cooldown periods with execution retry budgets. Consider adding a callback or event that the execution engine can subscribe to, so that when self-healing declares a component in cooldown, the execution engine can respect that and avoid wasted retry attempts. |

| 341 | medium | tests/unit | - | `FairSchedulingService.schedule()` computes `starvedItemIds` as items with `ageMs >= 15 * 60_000` (line 69). However, `ExecutionDispatchService.dispatchNext()` calls `sortTicketsForDeterministicDispatch()` and `interleaveTicketsByTenant()` (lines 207-208) with no integration point that boosts priority for starved items. The fair scheduling starved detection is never used to accelerate dispatch. Items that have been waiting for 15+ minutes are not given preferential treatment in the dispatch queue. | todo | - | Add a `starvedItemIds` parameter to `DispatchExecutionOptions` or a separate `boostStarvedItems()` call that `FairSchedulingService` drives. When dispatching, `ExecutionDispatchService` should check if a ticket's executionId appears in the starved set and apply a priority boost. |

| 342 | medium | tests/unit | - | 1. `DomainDefinition` has `trustTier: z.enum(["internal", "trusted", "community", "external"])` (domain-model.ts line 178) 2. `BusinessPackManifest` has `sandboxTier: z.enum(["read_only", "workspace_write", "scoped_external_access", "restricted_exec"])` (business-pack-manifest.ts line 251-254) 3. These are semantically related (both describe execution security boundaries) but: - No ADR documents the relationship between `trustTier` and `sandboxTier` - No validation enforces compatibility (e.g., `trustTier: "external"` should require `sandboxTier: "restricted_exec"`) - `DomainRegistryService.buildCapabilityEntry()` only exposes `domain.capabilities.securityLevel`, not pack's `sandboxTier` | todo | - | Create ADR documenting the relationship between `trustTier` and `sandboxTier`. Add compatibility validation logic in pack registration or domain association. |

| 343 | medium | tests/unit | - | `shouldRequireOrchestration()` at line 467-495 in intake-router-model.ts decides `requiresOrchestration: boolean` based on `riskClass`, `matchedHints.length`, `normalizedInput.length`, and `classification.continuation`. However: 1. The `riskClass` parameter is passed as optional `riskClass?: "low" \| "medium" \| "high" \| "critical"` 2. When `riskClass` is not provided, `shouldRequireOrchestration()` defaults to `false` unless other conditions (matched hints, input length) are met 3. The `IntakeRouteDecision` (line 128-164) carries `requiresOrchestration` flag but this is never passed to OAPEFLIR's Assess stage 4. OAPEFLIR's Assess stage (line 288) calls `assessment.assess(observedTask, input.constraintPack, input.effectivePolicy)` without any `requiresOrchestration` context If intake routing decides a task requires orchestration but OAPEFLIR Assess stage classifies it as low-risk and routes it to a simple single-step execution, the orchestration requirement is lost. | - | - | Add `requiresOrchestration: boolean` to `OapeflirLoopInput` and propagate it to the Assess stage. If `requiresOrchestration` is true, the Assess stage should set `executionMode: "orchestrated"` regardless of risk score, ensuring multi-step workflow execution. |

| 344 | medium | tests/unit | - | `DEFAULT_MAX_CHECKPOINT_SIZE_BYTES` is set to 10MB (line 36). The domain baseline's `buildDefinition()` at lines 483-486 sets `maxTokensPerTask` and `maxCostPerTask` limits based on risk level, but has no `maxCheckpointSizeBytes` field. The `DomainRiskProfile` (line 281) defines blast radius and reversibility but not storage limits. If a critical-risk domain (riskLevel === "critical") creates large checkpoints approaching 10MB, there is no domain-specific override to lower this limit. Critical domains should arguably have smaller checkpoint limits to reduce blast radius of corruption. | todo | - | Add `maxCheckpointSizeBytes` to `DomainBaseline.capabilities` or `DomainRiskProfile`. In `createCheckpointEnvelope()`, check if the domain has a custom max and use that instead of the global default. For critical risk domains, recommend a smaller default (e.g., 2MB) to limit corruption blast radius. |

| 345 | medium | tests/unit | - | `compareTraceFields()` at lines 154 compares `costUsd` between frames. The workflow debugger's `WorkflowTraceFrame` interface (line 30) includes `costUsd?: number`. However, the execution engine's step outputs (generated in `multi-step-supervisor.ts`) may not populate cost information in the same format. The comparison at line 154 does a simple equality check (`leftValue === rightValue`), but if one frame has cost from the WAL (hardcoded placeholder values) and another has actual cost from `recordCost()`, the comparison is meaningless. Additionally, `decisionRef` which is required by `recordCost()` is not part of `WorkflowTraceFrame`. | todo | - | Ensure `WorkflowTraceFrame.costUsd` is populated from actual cost records, not WAL placeholders. Add `decisionRef` to `WorkflowTraceFrame` so cost can be properly attributed. Document the contract for cost fields in trace frames. |

| 346 | medium | tests/unit | - | `RpoRtoTrackingService.assertSlaCompliance()` (line 391) throws if RPO or RTO targets are breached. `RuntimeRecoveryService.listRecoverableExecutingRuns()` (line 346) and `listStaleRuns()` (line 372) build recovery candidates but never call `assertSlaCompliance()` or check `getSlaCompliance()` before recommending recovery actions. Recovery recommendations may be generated for executions in a region that is already in breach of its RTO. The recovery action itself (e.g., `retry_new_ticket`) would add load to a degraded system. | todo | - | Before returning recovery candidates, `RuntimeRecoveryService` methods that list candidates for a breached region should check `RpoRtoTrackingService.getSlaCompliance()`. If RTO is breached, return candidates with `suggestedAction: "escalate_takeover"` to prevent automated retry from加重ing the outage. |

| 347 | medium | tests/unit | - | `PackDomainAssociationService` maintains pack-domain associations independently from domain lifecycle: 1. When a domain transitions to `deprecated` or `archived`, associated packs are not notified 2. When a pack transitions to `deprecated` or `archived`, domain association is not updated 3. `dissociatePackFromDomain()` at lines 83-111 does not check whether the domain is in a state that should prevent dissociation (e.g., domain is `active`) 4. Orphaned pack-domain associations can exist when domain is `archived` but pack is `published` | todo | - | Add lifecycle state callbacks to `PackDomainAssociationService`: - When domain is deprecated/archived, dissociate all associated packs or block the transition if packs are `published` - When pack is deprecated/archived, remove association but preserve for audit |

| 348 | medium | tests/unit | - | User access tokens have a 15-minute TTL (`ACCESS_TOKEN_TTL_MS` at session-management.ts line 18) while service tokens have a 60-minute TTL (`SERVICE_TOKEN_TTL_MS` at service-auth.ts line 16). If a service makes a call on behalf of a user (e.g., user action triggers a background service task), the service token remains valid for 45 minutes after the user's access token would have expired. This creates an inconsistency where: 1. User authenticates, gets access token (15min TTL) 2. Service starts processing on user's behalf, gets service token (60min TTL) 3. User's session expires (15 min) 4. Service continues with valid service token but user's session is now expired The `TenantScopeFilter` would still evaluate the service principal's tenant scope (Issue 3) - the service could access resources even after the originating user's session expired. | todo | - | If services act on behalf of users, the service token's TTL should be bounded by the user's session TTL (or a refresh token TTL). Add a check in `extractServiceAuth()` or `validateServiceToken()` that verifies the calling user's session is still valid if the service call is user-delegated. Document the delegation model: whether services can act independently of user sessions or must respect user session expiry. |

| 349 | medium | tests/unit | - | `ProactiveAgentService` produces action modes: `"auto_execute" \| "suggest" \| "silent_record"` (line 75-76). `AutonomyBoundaryPolicy` decides on `AutonomyTarget` values: `"routing_policy" \| "planning_policy" \| "execution_policy" \| "memory_policy" \| "sandbox_policy" \| "provider_registry"` (line 3-9). When OAPEFLIR's improve stage calls `autonomyBoundary.decide("planning_policy", validatedLearningObjects)` (line 640), it checks if learning objects are validated. But there's no mapping between: - ProactiveAgent's `actionMode` (which controls whether a suggestion becomes an auto-executed action) - OAPEFLIR's `AutonomyTarget` (which controls whether a planning policy improvement is allowed) If ProactiveAgent fires with `actionMode: "auto_execute"` and creates learning objects, those objects go through OAPEFLIR's improve stage which applies `AutonomyBoundaryPolicy.decide("planning_policy", ...)`. The `auto_execute` intent from ProactiveAgent is completely ignored in the autonomy boundary decision. | - | - | Add `actionMode` context to the `AutonomyBoundaryDecision` request. When `actionMode === "auto_execute"`, the boundary policy should apply stricter validation (requiring not just `validated` status but also higher evidence thresholds). Document the mapping between ProactiveAgent action modes and AutonomyTarget authority levels. |

| 350 | medium | tests/unit | tests/unit/testing/golden.test.ts:1 | The golden test utilities create golden files in `tests/golden/snapshots/` at runtime via `mkdirSync` and `writeFileSync` within each test. This means the tests themselves are creating and modifying the golden files they then validate. This is circular - the tests write the files they then check. In a CI environment, if the golden files don't exist, the first run creates them (passing), but subsequent runs should validate against them. If the files already exist with different content, the test fails. | todo | - | Separate golden file creation from validation. Golden files should be committed to source control and created once, not dynamically during test execution. Tests should only read and validate against existing golden files. |

| 351 | medium | tests/unit | - | `ReconciliationJobInput` (failover-controller lines 382-392) includes `pendingWriteCount`, `pendingApprovals`, `openBudgets`, `outboxMessages`, `restrictedWrites`. It does not include active execution lease count, lease holder distribution, or stale lease count. The reconciliation job cannot properly assess whether the new region can safely take over because it has no visibility into how many leases are active and in which state. | todo | - | Add `activeLeaseCount`, `activeLeaseHolderRegionDistribution`, and `stalLeaseCount` to `ReconciliationJobInput`. The reconciliation job should block promotion if there are active leases in the demoted region with no available workers to take them over. |

| 352 | medium | tests/unit | - | `submitForCertification()` (line 118) transitions pack from `draft` to `certifying`, but does not verify: 1. The associated domain exists and is in an appropriate state (`registered` or later) 2. Domain onboarding has completed `pack_development` phase 3. The pack's `domainId` references a valid domain in `DomainRegistryService` | todo | - | Add domain state verification in `submitForCertification()`: - Query `DomainRegistryService` to verify the pack's `domainId` exists and is in `registered`, `canary`, or `active` state - Query `DomainOnboardingService` to verify onboarding phase `pack_development` is complete |

| 353 | medium | tests/unit | - | In `inject()` (lines 325-338), when `this.rateLimiter != null`, rate limit headers are attached via `attachRateLimitHeaders()`. However, when `this.rateLimiter` is `null` (disabled or not configured), no rate limit headers are attached. The standard HTTP path (`handleRequest()`) always calls `attachRateLimitHeaders()` at line 426 when rate limiter is present. More critically, in the `else` branch of `inject()` (lines 339-345) where rate limiter is null, the code falls through to `dispatchRequest()` directly without calling `attachRateLimitHeaders()` at all. The response would never include `x-ratelimit-remaining` headers. This inconsistency means API clients using `inject()` would not receive rate limit feedback, while clients using standard HTTP requests would. Clients cannot know their rate limit status when using `inject()`. | todo | - | Always attach `x-ratelimit-remaining` header regardless of whether rate limiting is enabled. When rate limiter is null, use a default value like `-1` or the string `"unlimited"` to indicate rate limiting is not active. This provides consistent feedback to API clients. |

| 354 | medium | tests/unit | - | `FairSchedulingService.schedule()` calls `evaluateMultiDimensionalQuota()` with `workerUnits` as the rejection threshold (line 64). The quota enforcement uses hard limits configured per tenant. If a region failover occurs, the quota state may be stale (pending writes from the demoted region not yet replicated), leading to incorrect rejection of valid scheduling requests. | todo | - | Add a `quorumRegionCount` parameter to `FairSchedulingRequest` that indicates how many regions must acknowledge quota state before enforcing hard limits. During degraded state (fewer than quorum regions), use soft limits or defer to the new primary region's quota assessment. |

| 355 | medium | tests/unit | - | 1. Pack registration (`PackRegistryService.registerPack()`) does not verify the `domainId` exists in `DomainRegistryService` 2. Domain registration (`DomainRegistryService.register()`) does not check for existing packs in `PackRegistryService` 3. When a domain is `archived`, `PackRegistryService` still lists it as a valid domain for pack lookups 4. No orphaned-reference detection between the two registries | todo | - | Add cross-registry validation in both registration paths: - Pack registration: verify `domainId` exists and is not `archived` - Domain registration: warn if packs already exist for that domainId (domain archival may need to handle existing packs) |

| 356 | medium | tests/unit | - | `BudgetLedger.status` type allows 7 states but `BudgetLedgerSchema` only defines 4 states ("open", "soft_cap_reached", "hard_cap_reached", "closed"). Tests for budget operations (lines 454-530) use "open" status primarily and don't exercise the "settling", "reserving", "releasing" states. If a ledger with status "settling" were parsed via schema validation, it would be rejected, but the factory `createBudgetLedger` can produce it. | todo | - | Add test verifying that "settling", "reserving", "releasing" statuses are accepted by `BudgetLedgerSchema`, or add these states to the schema. |

| 357 | medium | tests/unit | - | `OapeflirLoopService` uses `HarnessLoopController` (line 358) to track iterations, cost, and replan decisions. `ProactiveAgentService` maintains its own `states` Map (line 225) tracking trigger fire counts, cooldown, consecutive failures. Both track some form of "run budget" but with no coordination: - OAPEFLIR's `loopController.getGuardViolation()` (line 509) can abort the loop - ProactiveAgent's `dailyTriggerBudgetByDomain` (line 343-346) tracks domain daily budgets - ProactiveAgent's `maxConsecutiveFailures` (line 339-340) circuit breaker If ProactiveAgent fires multiple triggers that all result in OAPEFLIR task executions, there's no shared budget tracking. A domain could exhaust ProactiveAgent's daily budget but OAPEFLIR continues executing tasks for that domain because the loop controller doesn't know about ProactiveAgent's budget state. | todo | - | Create a shared `ExecutionBudgetRegistry` that ProactiveAgent and OAPEFLIR both consult. ProactiveAgent should check the registry before firing, and OAPEFLIR should update the registry after execution. Add ADR for cross-module budget coordination protocol. |

| 358 | medium | tests/unit | - | `validateDefinition()` at lines 384-409 validates plugin bindings against `installedPluginIds` and `healthyPluginIds`. The session dual storage service records events with `payload` that may include `pluginId` references (e.g., `message_added` event at line 258-274 includes message content that could reference plugin outputs). However, there is no validation that plugin IDs in session events correspond to plugins that are registered and healthy in `DomainRegistryService`. If a session replays events that reference a plugin that has since been deregistered or marked unhealthy, the replay does not detect this inconsistency. | todo | - | Add a `validateSessionPluginReferences(events: SessionEvent[]): PluginValidationResult` method to `SessionDualStorageService` that checks all `pluginId` values in event payloads against `DomainRegistryService.getPluginBindings()`. Flag any references to plugins that are no longer registered or healthy. |

| 359 | medium | tests/unit | - | The cost optimizer's `buildRecommendations()` generates recommendations based on aggregated costs, but these recommendations are not integrated into the `BudgetAllocator`. The budget allocator reserves and settles costs without consulting the cost optimizer for dynamic limits. A recommendation to downgrade a model could be generated, but the budget allocator would continue using the current model without any enforcement mechanism. There's no feedback loop from cost optimization to budget allocation. | todo | - | Create an integration point where `CostOptimizationService.buildRecommendations()` results can influence `BudgetAllocator` behavior. Consider adding a `CostRecommendationListener` interface that budget allocation subscribes to, so budget limits can be adjusted based on recommendations. |

| 360 | medium | tests/unit | - | 1. Domain risk level (`DomainRiskLevel = "low" \| "medium" \| "high" \| "critical"`) is defined in `risk-profile/index.ts` 2. Pack risk matrix (`BusinessPackRiskLevel = "low" \| "medium" \| "high" \| "critical"`) is defined in `business-pack-manifest.ts` 3. Both use identical type definitions but there is no validation that: - A pack with `riskMatrix` entries of `level: "critical"` is associated with a domain that has `riskLevel: "critical"` - A domain with `riskLevel: "high"` doesn't have packs with only `low` risk entries (inadequate pack coverage) 4. `DomainRiskProfile` (risk-profile/index.ts) and `BusinessPackManifest.riskMatrix` are not linked | - | - | Add cross-validation when pack is associated with domain: pack's highest risk level in `riskMatrix` should not exceed domain's `defaultRiskLevel` by more than one tier. If pack is `critical` but domain is `low`, this is a security concern requiring explicit override. |

| 361 | medium | tests/unit | - | `validateServiceToken()` at lines 323-371 checks `token.audience !== input.audience && token.audience !== "*"` at line 353. This validates that the service token's audience matches the requested audience. However, `HttpApiServer.buildRouteTable()` at lines 797-901 creates routes for various services (task routes at lines 836-843, gateway routes at lines 829-835, etc.) but does not call `extractServiceAuth()` to validate that the calling service's token audience includes the target service. For example, if a service with `audience: "execution"` tries to call a route intended for `"orchestration"` services, the call would succeed at the HTTP layer (if the service has valid headers) but would fail silently at the business logic layer if the target service checks audience. The `HttpApiServer` itself does not enforce audience validation. | todo | - | In the route handler creation (e.g., `createTaskRoutes`, `createGatewayRoutes`), if the route is intended for internal service-to-service communication, add a check that the calling service's token audience matches the route's target service. Document the audience model for service-to-service calls. |

| 362 | low | Deep-Cross: http + tcp +  | src/platform/five-plane-interface/channel-gateway/ | `handleDisconnection()` at line 361 calls `ws.removeAllListeners?.()`. This method is optional on the `ws` library's WebSocket type and may not exist on all WebSocket implementations or in browser environments. The `?.` optional chaining means failures are silently swallowed, but the intention is cleanup. | todo | - | Document the WebSocket implementation requirement clearly. Use a type guard or explicitly check for method existence before calling. |

| 363 | low | Deep-Cross: http + tcp +  | src/platform/five-plane-interface/api/middleware/c | `CorsMiddleware` normalizes and validates CORS config in the constructor, but the validation only fires when `new CorsMiddleware(config)` is called. If the config is changed at runtime via `this.config = { ...DEFAULT_CORS_CONFIG, ...config }`, the validation is not re-run. Additionally, the validation at line 86 catches exceptions from `new URL(origin)` but does not specifically validate the origin string for injection characters before parsing. | todo | - | Add a `validate()` method and call it after any runtime config update. Validate that `Sec-Fetch-Site` and other fetch metadata headers are consistent with the claimed origin. |

| 364 | low | Deep-Cross: http + tcp +  | src/interaction/dashboard/dashboard-websocket-serv | `scheduleProjectionPoll()` uses `setTimeout` and `timer.unref?.()` for the projection polling loop. The `projectionPollingInFlight` flag at line 584 prevents concurrent polls but there is no maximum backoff cap when errors are persistent. If the projection service is unavailable, the exponential backoff calculation at lines 593-599 caps at `max(this.projectionPollingBaseIntervalMs * 4, 1000)` which is only 4x the base interval. Under sustained failure, this still results in high-frequency retry attempts. | todo | - | Implement exponential backoff with a maximum delay (e.g., cap at 60 seconds) when `consumePendingDeltas()` consistently fails. |

| 365 | low | Deep-Cross: http + tcp +  | src/platform/five-plane-interface/api/http-api-ser | `SERVER_HEADERS_TIMEOUT_MS = 60_000` (60 seconds) and `SERVER_REQUEST_TIMEOUT_MS = 30_000` (30 seconds) are set independently. When `headersTimeout` exceeds `requestTimeout`, a socket could be waiting for headers past the request timeout, creating a situation where the request timeout fires first and rejects the request while headers are still being received. These timeouts should be coordinated. | todo | - | Ensure `headersTimeout > requestTimeout` by at least a margin (e.g., 2x). Document the relationship between these timeouts clearly. |

| 366 | low | Deep-Cross: http + tcp +  | src/platform/five-plane-interface/channel-gateway/ | The `WebSocketBridge` sets `maxPayload: options.maxPayloadBytes ?? DEFAULT_MAX_PAYLOAD_BYTES` (64KB) at line 153. The `ws` library enforces this at the WebSocket frame level, but there is no per-message size validation before attempting to parse and route messages. Large messages that pass the frame-level check but exceed internal processing limits could cause issues. | todo | - | Add application-level message size validation separate from the WebSocket frame limit. Consider setting a lower `maxPayload` or adding a message-level size check in `handleMessage()`. |

| 367 | low | Deep-Cross: http + tcp +  | src/platform/five-plane-interface/api/http-server/ | `decorateResponseHeaders()` sets CORS headers after route handlers have run. If a route handler sets `Access-Control-Allow-Origin` with a wildcard and credentials are enabled, the CORS validation would reject it. However, if the route handler itself sets a more permissive CORS origin than configured in `corsConfig`, the decorator silently overwrites it. This is correct behavior but could mask misconfigurations where routes bypass the shared CORS middleware. | todo | - | Add a flag to `ApiResponsePayload.headers` (e.g., `_corsHandled: true`) that the route handler sets when it has already applied CORS, so the decorator can skip CORS processing for those responses. |

| 368 | low | Deep-Cross: http + tcp +  | src/platform/five-plane-interface/api/http-server/ | `decorateResponseHeaders()` at line 133 unconditionally sets `headers.vary` to `"Accept-Encoding"` when no existing Vary header is present, overwriting any prior `Vary` value set by the route handler. This can cause incorrect caching behavior when routes set their own `Vary` header for content negotiation. | todo | - | Preserve existing `Vary` values by merging: `headers.vary = existingVary ? \`${existingVary}, Accept-Encoding\` : "Accept-Encoding"`. |

| 369 | low | Deep-Cross: http + tcp +  | src/platform/five-plane-interface/channel-gateway/ | Uses `console` directly (`const logger = console`) for warning logs instead of `StructuredLogger`. At line 720, `logger?.warn?.` has optional chaining on both the logger and the method, which means warnings may be silently dropped if the console object is replaced (e.g., in some test environments or bundled builds). | todo | - | Replace `console` with `StructuredLogger` imported from the observability module, matching the pattern used in `websocket-bridge.ts`. |

| 370 | low | Unknown | - | `execute()` checks action with `/^[a-zA-Z0-9_]+$/` (line 94). This allows any combination of alphanumeric and underscore characters. The CRM adapter supports actions like `upsert_contact`, `upsert_company`, `append_note` (line 23: `MUTATING_ACTIONS`), but the validation doesn't restrict which actions can be mutating vs read-only. A caller could pass any valid-format action name. | todo | - | Define an explicit enum or const array of valid actions rather than relying on regex validation. Validate against the explicit list before processing. |

| 371 | low | Unknown | - | `getExecutionPolicy()` queries by `skillId` directly (line 287). If the database contains malformed `skillId` values (due to a prior bug or injection), the query would still execute with potential for SQL injection if the input were not parameterized. However, the actual injection risk is low since `skillId` comes from internal skill registration. The bigger issue is that the skillId format validation in `validateSkill()` (line 166-168) uses `/^[a-z][a-z0-9_-]*$/i` but this isn't enforced as a database constraint. | todo | - | Add a CHECK constraint on the `skill_id` column in the database schema to enforce the same format as the application-level validation. |

| 372 | low | Unknown | - | `computeSkillHealth()` returns `0.5` for skills with `executionCount === 0`. For skills with very few executions (e.g., 1-5), the health score is heavily weighted toward the success rate but on a sample too small to be statistically meaningful. A skill with 1 execution and 100% success rate returns health `0.01` (one hundredth of 1.0). | todo | - | Consider using a Bayesian approach with a prior that starts at neutral (0.5) and converges toward observed success rate as execution count increases. Document the convergence rate for health-based gating consumers. |

| 373 | low | Unknown | src/platform/five-plane-interface/channel-gateway/ | `circuitBreakerState` is a `Map<string, { failureCount: number; openUntil: number \| null }>`. When a circuit opens (failureCount >= threshold), `openUntil` is set. When the circuit resets, the entry remains in the Map with `failureCount: 0, openUntil: null`. Over time, as different channels open and close, the Map accumulates entries. There is no mechanism to remove closed circuits. With many channels over long运行时间, the Map could grow to O(number of distinct channel names that have ever had failures), though bounded by the finite set of channel names. ```typescript // line 76 - circuit breaker state Map private readonly circuitBreakerState = new Map<string, { failureCount: number; openUntil: number \| null }>(); // reset at line 538-539 - entry stays in Map private resetCircuitBreaker(channel: string): void { this.circuitBreakerState.set(channel, { failureCount: 0, openUntil: null }); } ``` | - | - | On circuit reset, delete the channel entry from the Map instead of setting `{ failureCount: 0, openUntil: null }`. Alternatively, use a `WeakMap` if channel objects have a stable identity. |

| 374 | low | Unknown | - | `disconnect()` in `SharedWorkerWSClient` sets `this.disconnected = true` and closes the port immediately: ```typescript public disconnect(): void { this.disconnected = true; this.port.postMessage({ action: "disconnect" }); this.port.removeEventListener("message", this.handleMessage); // Removes listener BEFORE port processes disconnect this.replayBufferByChannel.clear(); this.handlers.clear(); this.statusHandlers.clear(); this.port.close(); // Port closes immediately } ``` If there are pending `publish()` calls that have not yet been sent via `port.postMessage()`, they are discarded. The `handleMessage` listener is removed before the port processes the disconnect message, so any in-flight messages are lost without graceful shutdown. | todo | - | Add a flush mechanism for pending messages before disconnect. Consider setting a grace period where `disconnected = true` but messages are still flushed before the port is closed. |

| 375 | low | Unknown | - | In `sendTaskUpdate()`, if there is a sequence gap, a `stream_gap` message is sent to the client: ```typescript if (client.lastEventId != null && client.lastAcknowledgedSequenceNum < sequenceNum - 1) { ws.send(JSON.stringify({ type: "stream_gap", taskId, fromEventId: client.lastEventId, // Reveals lastEventId to client toEventId: eventId, // Reveals current eventId reason: "missed_events", })); } ``` This exposes `lastEventId` values that the client could use to infer event ordering, timing, or volume of events on tasks they have access to. The `fromEventId` and `toEventId` are server-controlled but reveal the sequence state. | todo | - | Consider whether `stream_gap` messages inadvertently leak sequence state to clients. If this is a concern, the bridge could send a generic `gap` indicator without specific sequence IDs. However, this may be intentional for clients to request replay. Document the security assumption that clients are authorized to know their sequence position. |

| 376 | low | Unknown | - | `markSeen()` adds event IDs to `seenEventIds` Set without bound. The overflow pruning at lines 93-105 only triggers when `this.seenEventIds.size <= this.backlogLimit * 10` is false — meaning pruning only runs after size exceeds `backlogLimit * 10`. With `backlogLimit` default of 100 (line 25), the Set can grow to 1000 entries before pruning occurs. If many task status change events fire (e.g., rapid status transitions), this Set could grow significantly during a polling cycle. ```typescript // lines 91-106 - unbounded Set until threshold exceeded private markSeen(eventId: string): void { this.seenEventIds.add(eventId); if (this.seenEventIds.size <= this.backlogLimit * 10) { // Can grow to 1000 before pruning return; } // Only runs when size > 1000 const overflow = this.seenEventIds.size - this.backlogLimit * 10; ... } ``` | todo | - | Change the pruning threshold to be percentage-based or use a sliding window. Instead of allowing 10x backlog before pruning, use a hard cap that triggers pruning more aggressively. Alternatively, use a `Map<string, number>` with timestamps and prune entries older than the polling interval. |

| 377 | low | Unknown | - | `verifyNonce()` inserts nonces into the database and the deletion of expired nonces happens in the same call: ```typescript // line 329-335 - delete happens on every verifyNonce call, but only for expired this.db.connection .prepare(`DELETE FROM replay_nonces WHERE expires_at < ?`) .run(now.toISOString()); ``` However, if `verifyNonce()` is never called for a given nonce (e.g., the request that would have used it was dropped), the nonce remains in the database until a subsequent `verifyNonce()` call cleans it. If there are long gaps between requests, expired nonces accumulate. The cleanup only runs during `verifyNonce()`, not as a background job. | todo | - | Add a TTL index on `expires_at` and run cleanup on a schedule (e.g., every hour) via a background task, rather than relying on `verifyNonce()` being called frequently enough to clean up. |

| 378 | low | Unknown | - | `rebuildAck` retrieves an event at line 560, then at line 562 checks `event.eventTier === "tier_1"`. If the event is not tier-1, the method returns `applied: afterPending < beforePending` at line 599 but provides no indication in the `detail` field that the event was skipped as non-tier-1. Callers cannot distinguish between "nothing to rebuild" and "event was not tier-1". | todo | - | Add an early return with detail `"event_not_tier1_skipped"` when the event is not tier-1. Include the event tier in the repair audit payload. |

| 379 | low | Unknown | - | `MAX_RECOMMENDED_STICKY_SHARE = 0.6` (60%, worker-load-balancing.ts line 17) defines load skew detection threshold. `HealthService.buildWorkerHealthSummary()` (line 390-456) calls `summarizeWorkerLoadSkew()` which uses this same constant internally, but `HealthService` does not expose the 0.6 threshold as configurable. If operators need to adjust the load skew sensitivity, there is no configuration knob. Meanwhile, the dispatch service's worker selection uses `LOAD_SKEW_SELECTION_PENALTY = 0.35` (dispatch-support.ts line 52) as a selection penalty, which is a different numeric value (0.35 vs the skew detection threshold of 0.6). | todo | - | Make `MAX_RECOMMENDED_STICKY_SHARE` configurable via `HealthServiceOptions`. Document the relationship between the skew detection threshold and the skew selection penalty value, or derive the penalty as a function of the threshold. |

| 380 | low | Unknown | - | `StructuredMemoryContent` (lines 56-63) defines an interface with specific keys (`workContext`, `topOfMind`, `recentHistory`, `longTermBackground`, `facts`, `schemaVersion`) and does not have an index signature. The `normalizeStructuredObject` function (lines 259-346) processes known structured keys explicitly but for unknown keys, it only extracts string/number/boolean values as facts with `key=value` format (lines 340-342) or arrays as facts with key as category (lines 330-337). This means structured metadata that was part of the original memory object but didn't fit the normalized schema is lost and replaced with flattened `key=value` facts. When this memory is exported and then re-imported, the original structure is not preserved — the hierarchical relationships between fields are lost. For example, a nested object `{"details": {"priority": "high", "notes": "test"}}` would become facts `details={"priority":"high","notes":"test"}` and lose the structural priority/notes distinction. | todo | - | Add an explicit `metadata: Record<string, unknown>` field to `StructuredMemoryContent` to capture unknown fields during normalization rather than flattening them into facts. This preserves the original data at the expense of slightly more complex validation. |

| 381 | low | Unknown | - | Uses `` `repl_${randomUUID()}` `` and `` `lag_${randomUUID()}` `` — uses underscore prefix consistent with canonical format, but does not use `newId()` from `ids.ts`. Direct `crypto` import inconsistency across runtime-services modules. | todo | - | Import `newId` from `ids.ts` and use `newId("repl")` / `newId("lag")`. |

| 382 | low | Unknown | - | `DEFAULT_BULKHEAD_CONFIG` (bulkhead-isolation.ts lines 20-24) defines `maxConcurrentCalls: 100`, `timeoutMs: 30_000`, `queueSize: 50`. This bulkhead is used for plane-to-plane fault isolation. The channel gateway's circuit breaker threshold is 3 failures (channel-gateway-service.ts line 98) with a reset of 30s (line 99). These are independent configurations with no shared values or relationship. The bulkhead's 30s timeout (30,000ms) is identical to the channel gateway's `requestTimeoutMs` max (30,000ms) and the `DEFAULT_DELIVERY_CONFIG.timeoutMs` (30,000ms), but this alignment appears coincidental and is not documented. | todo | - | Document the relationship between bulkhead timeout, circuit breaker reset timeout, and delivery timeout. All three are currently 30,000ms but this should be an explicit constant shared across reliability components rather than coincidental alignment. |

| 383 | low | Unknown | src/ops-maturity/drift-detection/evolution-integra | Uses `` `ev_${randomUUID()}` `` — prefix `ev` is undocumented and short. The `ids.ts` contract documents semantic prefixes but does not enumerate them. This creates an unbounded prefix namespace without governance. | todo | - | Establish a registered prefix catalog in `ids.ts` and use descriptive prefixes (e.g., `evolution_` instead of `ev`). |

| 384 | low | Unknown | - | ```typescript } catch (error) { if ((error as { code?: string })?.code === "inspect.approval_not_found") { throw new ApiError(404, "api.approval_not_found", `Approval not found: ${approvalId}`); } throw error; } ``` This uses a loose type cast `(error as { code?: string })` and string comparison against `"inspect.approval_not_found"`. This pattern is repeated across multiple files without a common utility function for error type narrowing. | todo | - | Use `AppError.isAppError(error)` type guard and `error.code` comparison with typed constants: ```typescript import { isAppError, AppError } from "../../../contracts/errors.js"; |

| 385 | low | Unknown | - | `validateEventPayload()` (lines 1152-1175) accepts any payload for event types not in `EVENT_PAYLOAD_VALIDATORS` if the event type is registered in `EVENT_SCHEMA_REGISTRY` with `tier !== "tier_1"`, using the `genericEventPayloadSchema` (line 1160). For tier-1 events without a validator, it throws (lines 1155-1158). However, `getEventReplayMetadata()` (lines 1102-1128) throws a `ValidationError` for event types that are in `EVENT_SCHEMA_REGISTRY` but not in `RUNTIME_EVENT_REPLAY_METADATA` — this creates an inconsistency where an event can be validated as a valid payload but have no replay metadata. Additionally, `getEventSchema()` throws two different error codes: `"event.schema_missing"` at lines 1093-1095 and 1097-1099, both with the same structure, so callers cannot distinguish the two error cases. | todo | - | In `validateEventPayload`, check `hasEventSchema()` first and throw a descriptive error if the event type is not registered rather than silently falling back to `genericEventPayloadSchema` for non-tier-1 events. In `getEventReplayMetadata`, differentiate between "event type exists but has no replay metadata" (which should return a default metadata with `replayBehavior: "forbidden"`) versus "event type does not exist" (which should throw). |

| 386 | low | Unknown | - | ```typescript } catch (error) { if (!(error instanceof AppError) \|\| error.code !== "workflow.not_found") { throw error; } // fallback lookup continues... } ``` Uses `!(error instanceof AppError)` but then checks `error.code`, which is redundant since `AppError` has a `code` property. However, this still uses raw string comparison for the error code. | - | - | Standardize on `isAppError()` type guard and ensure all error codes are defined as typed constants. |

| 387 | low | Unknown | - | The `expiresAt` for budget reservations is hardcoded: ```typescript expiresAt: new Date(Date.now() + 5 * 60 * 1000).toISOString(), ``` 5 minutes (300 seconds) is used regardless of task complexity,预计 execution time, or stage. A complex multi-step task might need a longer reservation window. The TTL should be proportional to the task's expected duration or at least configurable via the budget policy. | todo | - | Derive the TTL from the budget policy's `maxDurationMs` or a configurable `reservationTtlMs` in the `BudgetPolicy` interface. Use `Math.min(maxDurationMs, 5 minutes)` as the TTL to prevent orphaned reservations. |

| 388 | low | Unknown | - | The `writeStructuredLine()` function attempts to attach `requestId`, `traceId`, and `correlationId` from `currentRequest`, but if `currentRequest` is null (which it can be for early-boot logs before any plugin message is processed), all three are omitted. This means logs from the initialization phase have no correlation IDs. | todo | - | Consider generating a synthetic correlation ID for the pre-message phase so all logs are traceable. |

| 389 | low | Unknown | - | `calculateBackoffForAttempt()` uses a boolean condition that is always false in production: `if (attemptNumber <= 0 && new Error().stack?.includes(...)`. The first part `attemptNumber <= 0` is always false for 1-based attempt numbers. The second part tries to detect if it's running in a test file but the stack check is redundant because the first part already makes the entire condition false. The special case for attemptNumber <= 0 is dead code that never executes in production. | todo | - | Remove the guard entirely or replace with a clear comment explaining why attemptNumber <= 0 is handled (if it was meant as a defensive normalization). |

| 390 | low | Unknown | - | At line 397-402, if any P0 finding is detected, the method immediately builds the report, sets `_trafficBlocked = true`, calls `onTrafficBlocked()`, and returns. However, the P1 checks (lines 405-515) that run after the P0 short-circuit would be useful to collect for the audit trail even when fail-closed. Currently, if a P0 is found, all P1 findings are silently dropped from the returned report. | todo | - | Continue gathering P1 findings even after a P0 is detected. Include them in the returned report with a note indicating they were found after P0 detection. This provides complete diagnostic information for post-incident review. |

| 391 | low | Unknown | - | The file sink uses `process.stderr.write` directly for `file_sink_error` events. While this is a fallback mechanism (the file sink itself has failed), it bypasses the transport chain. | todo | - | This is an intentional last-resort fallback but could be enhanced by also attempting to emit via a secondary transport. |

| 392 | low | Unknown | - | ```typescript } catch (err) { return { valid: false, error: err instanceof Error ? err.message : "jwt.validation_failed", claims: null, provider: null, }; } ``` The `verifyToken()` method returns a result object rather than throwing. When an error occurs, only the message is extracted and placed into the result. The full error context (stack trace, nested causes) is lost. | todo | - | Include error chain information in the result: ```typescript } catch (err) { return { valid: false, error: err instanceof Error ? err.message : "jwt.validation_failed", errorStack: err instanceof Error ? err.stack : undefined, claims: null, provider: null, }; } ``` |

| 393 | low | Unknown | - | The flow is: ```typescript if (record.decisionRef.trim().length === 0) { this.unsourcedRecordCount += 1; throw new Error(`cost_optimizer.unsourced_record:${this.resolveSubjectId(record)}`); } if (this.unsourcedRecordCount > 0) { this.unsourcedRecordCount -= 1; } this.records.push(record); return record; ``` If `decisionRef` is empty, it increments counter and throws. The record is NOT pushed (throw comes before push). This is correct error handling. However, the comment at line 72 suggests the throw is intended as a signal for tracking purposes, but the increment happens on every invalid record. If many invalid records arrive, `unsourcedRecordCount` grows and each throw is an exception — this creates noise in error tracking for what might be a systemic configuration issue rather than an individual task failure. | todo | - | Consider batching invalid records and reporting them as a single warning rather than throwing individual exceptions. Or add a circuit breaker that stops incrementing after a threshold. |

| 394 | low | Unknown | src/scale-ecosystem/runtime-services/execution-wor | Uses `` `wb_${randomUUID()}` `` — uses custom prefix `wb` (writeback) not in canonical format and not using `newId()`. | todo | - | Use `` newId("writeback") ``. |

| 395 | low | Unknown | - | When `metrics.requestCount < minimumRequestCount`, `evaluate()` returns `{ rollback: false, reasonCodes: ["rollout.metrics_insufficient_sample"] }`. The rollbackHandler is NOT called (correct), but these "insufficient sample" decisions could be logged and misinterpreted as actual rollback decisions. An attacker could flood the system with low-sample requests to prevent rollback detection. | todo | - | Clearly separate "evaluation not possible" from "evaluation passed". Use a different field like `evaluable: false` to distinguish from actual pass/fail decisions. |

| 396 | low | Unknown | - | `unwrapWorkflowStepCheckpoint` calls `unpackCheckpointEnvelope(envelope, options)` at line 393 but `options` is `{ maxSizeBytes?: number }` from the outer function. The inner call passes `options` directly, but `unpackCheckpointEnvelope` at line 183 also has a default for `maxSizeBytes`. If the caller specifies a custom `maxSizeBytes` via `options`, it IS passed through correctly. However, the function signature comment at line 391 says it returns `UnpackedCheckpointEnvelope<import("./workflow-step-checkpoint.js").WorkflowStepCheckpoint>` which is the correct type, but the import is only in the JSDoc, not at runtime. | todo | - | The current implementation is functionally correct. Add a runtime type check on the returned data to validate it matches `WorkflowStepCheckpoint` interface before returning. |

| 397 | low | Unknown | - | ```typescript } catch (err) { logger.warn("gateway.parse_webhook_payload_failed", { ... }); if (err instanceof AppError && err.code === "api.invalid_json") { throw new ApiError(400, "gateway.invalid_payload", "Webhook payload must be valid JSON"); } throw err; } ``` Uses `err instanceof AppError` which is correct, but the error code check uses a string literal `"api.invalid_json"` instead of a typed constant. The pattern is repeated inconsistently across files. | todo | - | Use typed error code constants from a shared module. |

| 398 | low | Unknown | - | Both catch blocks capture `err` but do not log it: ```typescript } catch (err) { throw new MigrationError(..., { cause: err }); } ``` Using `MigrationError` with `cause` is better than silent swallowing, but there is no `logger.warn` or `logger.error` at the call site to trace the migration failure. | todo | - | Add `logger.warn` before throwing. |

| 399 | low | Unknown | src/scale-ecosystem/runtime-services/execution-dis | Uses `` `op_${randomUUID()}` `` — prefix `op` (operation) is undocumented and not using `newId()`. | todo | - | Use `` newId("operation") ``. |

| 400 | low | Unknown | - | The `Component` getter wraps render in try-catch but this will not intercept React rendering errors properly. React error boundaries work differently. | todo | - | Use React's `componentDidCatch` or an ErrorBoundary component properly. |

| 401 | low | Unknown | src/scale-ecosystem/runtime-services/execution-wor | Same `` `op_${randomUUID()}` `` pattern as execution-dispatch-service. | todo | - | Use `` newId("handshake") ``. |

| 402 | low | Unknown | - | `scanExecutionCheckpoints` filters files with `.endsWith(".checkpoint.json")`. This is a simplistic heuristic that could accidentally pick up non-checkpoint JSON files if any exist in the execution directory. There is no validation that the file is actually a checkpoint (e.g., checking for a known checkpoint schema version or structure). | todo | - | After reading a file, validate it contains a known checkpoint schema version before treating it as a GC candidate. Skip files that fail schema validation rather than deleting them. |

| 403 | low | Unknown | src/domains/registry/plugin-runtime-host.ts:735 | ```typescript let template: unknown; try { template = JSON.parse(rawTemplate); } catch { throw new ValidationError( "plugin_spi.container_launcher_invalid_json", ... ); } ``` The `catch` is not empty, but it does not log the parse error before throwing. The `ValidationError` carries no details about what the raw template contained. | todo | - | Capture `err` and include it in the `ValidationError.details` or log it before throwing. |

| 404 | low | Unknown | src/scale-ecosystem/runtime-services/human-takeove | Uses `` `hto_${randomUUID()}` `` — prefix `hto` (human take-over) is undocumented, not using `newId()`. | todo | - | Use `` newId("human_takeover") ``. |

| 405 | low | Unknown | - | When the file sink fails, it writes directly to `process.stderr`. While intentional as last-resort, critical errors may be lost if stderr is also failing. | todo | - | Consider emitting to a secondary transport when the primary file sink fails. |

| 406 | low | Unknown | - | ```typescript const globalPluginTaintTracker = new PluginTaintTracker(); ``` The `PluginTaintTracker` is a global singleton with mutable state. This creates test isolation issues. | todo | - | Consider making the taint tracker injectable via registry rather than a global singleton. |

| 407 | low | Unknown | - | `safeHashEquals()` uses constant-time comparison suggesting security intent, but the function is used for fingerprint equality. Purpose is undocumented. | todo | - | Document why constant-time comparison is needed. |

| 408 | low | Unknown | - | The `attempt >= this.maxAttempts` check throws AFTER fn() has been called and failed in the current iteration. This means for any `shouldRetry` predicate that returns true for all `attempt` values up to and including `maxAttempts`, one extra fn() execution occurs beyond `maxAttempts`. For example, with `maxAttempts=3`: attempt 3 runs fn() twice (once by the loop body, once causing throw). The throw is not a second execution but a redundant check that has already been evaluated. | todo | - | Clarify boundary: check attempt limit BEFORE fn() call, not after fn() throws. |

| 409 | low | Unknown | - | The size limit is checked twice: first against `originalSizeBytes` at line 140 (before compression), and then against `compressedSizeBytes` at line 149 (after compression). If raw checkpoint data exceeds the limit but compresses well (e.g., highly repetitive JSON), it would be rejected by the pre-compression check even though the final artifact would be within bounds. This is an unnecessary blocking constraint. | todo | - | Remove the pre-compression size check. Only enforce the limit on the compressed result, which is the actual storage cost. Add a comment explaining that compression ratio is unpredictable and only the compressed size matters for quota. |

| 410 | low | Unknown | - | `execution-dispatch-support.ts` defines `AFFINITY_SELECTION_BONUS = 0.2` and `LOAD_SKEW_SELECTION_PENALTY = 0.35` for worker selection. `channel-gateway-delivery-support.ts` defines `DEFAULT_DELIVERY_CONFIG` with `maxRetries: 5`, `initialBackoffMs: 1000`, `maxBackoffMs: 60000`, `backoffMultiplier: 2`, `timeoutMs: 30000`. There is no shared constants module between the load balancing layer (dispatch/worker/coordinator) and the delivery retry layer (channel gateway). The delivery retry config (`backoffMultiplier: 2`) differs from the dispatch load balancing multiplier logic in `calculateBackoffForAttempt()` (line 200-210) which also uses `initialBackoffMs * multiplier^(attempt-1)` with the same multiplier=2. | todo | - | Extract shared retry/backoff constants to a common `reliability-constants.ts` module that both `execution-dispatch-support.ts` and `channel-gateway-delivery-support.ts` import. |

| 411 | low | Unknown | - | `riskLevelForSubject()` upgrades risk when a record has `costType === "llm"` or `costType === "model"`: ```typescript if (records.some((item) => item.costType === "llm" \|\| item.costType === "model")) { return baseRisk === "low" ? "medium" : baseRisk; } ``` But the `CostSubjectType` union defined at line 22 only includes `"llm"` — `"model"` is not a valid cost type. Records with `costType === "model"` will not trigger the risk upgrade. This may indicate a misspelling or that `"model"` was intended to be in the union but was omitted. | - | - | Verify if `"model"` is a legacy or intended cost type. If intended, add `"model"` to the `CostSubjectType` union. If not, remove from the check to avoid confusion. |

| 412 | low | Unknown | src/platform/five-plane-control-plane/incident-con | `generateIncidentId()` uses `` `incident_${crypto.randomUUID()}` `` — directly accessing `crypto.randomUUID()` rather than importing `randomUUID` from `node:crypto`. This is inconsistent with the canonical `ids.ts` which re-exports `randomUUID` from `node:crypto`. | todo | - | Import `randomUUID` from `node:crypto` and use the canonical pattern `newId("incident")`. |

| 413 | low | Unknown | - | When checksum verification fails, the code logs at `logger.error` level (line 224) with `message: "Checkpoint envelope checksum mismatch"`. For a deliberately corrupted file, repeated error-level logs could constitute a log flooding attack vector. The checksum mismatch is already handled by throwing `CheckpointEnvelopeInvalidError` — the error log is redundant for functional purposes. | todo | - | Change log level to "warn" for checksum mismatch. The exception carrying `CheckpointEnvelopeInvalidError` already signals the failure. The log should provide context for debugging without duplicating the error classification. |

| 414 | low | Unknown | - | In `sqlite-database.ts`, `isMigrationChecksumCompatible()` (lines 1013-1017) checks `existingChecksum === migration.checksum \|\| existingChecksum === migration.appliedChecksum \|\| (migration.compatibleChecksums ?? []).includes(existingChecksum)`. The `appliedChecksum` field records what checksum was actually applied to the database, which may differ from the current migration's `checksum` if the schema was back-ported or the SQL was refactored with equivalent semantics. However, `evaluateSqliteMigrationCompatibility()` in `sqlite-migration-compatibility.ts` only evaluates the current `migration.sql` against the compatibility rules — it does NOT validate that `migration.appliedChecksum` or `migration.compatibleChecksums` are consistent with what was actually recorded in the schema migrations table. This means a migration that was applied with different SQL (but equivalent semantics) would still show as "compatible" even if its current SQL has drifted semantically from what was actually applied. | - | - | Add a validation pass in `evaluateSqliteMigrationCompatibility` that cross-references `migration.appliedChecksum` against the checksum of the SQL that was actually recorded in the database. If they differ, flag it as a "checksum drift" warning rather than assuming the current SQL is correct. |

| 415 | low | Unknown | - | The catalog defines `baselineServices` as `readonly string[]` but many services registered in bootstrap functions use class names that may not match exactly. No validation exists to ensure the service IDs in `baselineServices` actually correspond to registered services. | todo | - | Consider adding a registry validation step during bootstrap that checks all `baselineServices` references actually exist. |

| 416 | low | Unknown | - | Multiple methods use `` `resolution_${crypto.randomUUID()}` ``, `` `postmortem_${crypto.randomUUID()}` ``, and `` `actionitem_${crypto.randomUUID()}` `` — directly using `crypto.randomUUID()` instead of the canonical `newId()` or imported `randomUUID`. Also uses no prefix separator consistency (underscore vs hyphen is inconsistent across other files). | todo | - | Centralize via `newId()` with consistent prefix naming. |

| 417 | low | Unknown | src/platform/five-plane-control-plane/config-cente | Analysis ID uses `` `impact-${randomUUID()}` `` — uses hyphen separator (`impact-`) instead of underscore (`impact_`) which is the canonical separator defined in `ids.ts`. | todo | - | Use `` newId("impact") `` for canonical format. |

| 418 | low | Unknown | - | `actualizeCostEvent()` handles three cases for `split`: ```typescript if (!byok \|\| split === "platform_governance") { platformGovernanceCostUsd = observedCostUsd + governanceOverheadUsd; tenantModelCostUsd = 0; } else if (split === "tenant_model") { platformGovernanceCostUsd = 0; tenantModelCostUsd = observedCostUsd + governanceOverheadUsd; } ``` If `split === "split"` (the default), both conditions are false, so `platformGovernanceCostUsd` stays as `governanceOverheadUsd` and `tenantModelCostUsd` stays as `observedCostUsd`. This is correct for `"split"` but if `split` is any other unexpected string, the fallback behavior silently assigns the full cost to platform governance (the first branch) — which may not be the intended behavior for an unknown value. | - | - | Add an `else` clause that throws or logs an error for unknown `split` values rather than silently falling through to `platform_governance` behavior. |

| 419 | low | Unknown | src/platform/shared/cache/cache-invalidation-broad | Instance ID uses `` `inst_${randomUUID()}` `` — prefix `inst` is not in the canonical list of entity type prefixes documented in `ids.ts`. The canonical format uses underscore-separated semantic prefixes. | todo | - | Add `inst` to the documented prefix conventions or use a more descriptive prefix like `cache_inst`. |

| 420 | low | Unknown | - | ```typescript } catch (error) { throwOidcProviderError("oauth.token_exchange_transport_failed", { issuer: provider.issuer, error: error instanceof Error ? error.message : String(error), }); } ``` The original error's stack trace is lost. Only the message string is extracted and passed as a detail, losing the full error context including potential nested causes. The `throwOidcProviderError` creates a new error that chains to the extracted message, not the original error. | todo | - | ```typescript } catch (error) { throwOidcProviderError("oauth.token_exchange_transport_failed", { issuer: provider.issuer, error: error instanceof Error ? error.message : String(error), stack: error instanceof Error ? error.stack : undefined, }, error instanceof Error ? error : undefined); } ``` |

| 421 | low | interaction | src/interaction/autonomy/autonomy-governance-servi | `canPromote()` uses `compareAutonomyLevels(targetLevel, this.getMaxAutonomyLevel(agentId)) <= 0`. This correctly checks that `targetLevel` is not higher than the agent's max autonomy, but the comparison is off-by-one in the index-based comparison. If `targetLevel` equals maxAutonomy (same level), `compareAutonomyLevels` returns 0, and `0 <= 0` is true, meaning an agent can "promote" to their current level (a no-op). This is not harmful but could mask actual logic errors. | todo | - | Clarify the boundary condition: `compareAutonomyLevels(targetLevel, maxAutonomy) < 0` means strictly lower autonomy, `compareAutonomyLevels(targetLevel, maxAutonomy) <= 0` means equal or lower. The current logic correctly permits promotion to the same level (no-op) but this should be documented. |

| 422 | low | interaction | src/interaction/dashboard/dashboard-websocket-serv | `assertRequiredIdentity()` throws on missing `principal` or `tenantId` but the error messages are generic ("Principal is required", "Tenant ID is required"). In a security-sensitive context, this makes it difficult to distinguish which field failed in logs without capturing the actual values. | todo | - | Include a sanitized error code that indicates which field was missing without leaking the value. For example: `throw new Error("nl_gateway.identity_required:principal_missing")` and `throw new Error("nl_gateway.identity_required:tenantId_missing")`. |

| 423 | low | interaction | src/interaction/ux/ux-event-tracking-service.ts:10 | `maxEventLogSize` is set to 1000 but the trimming logic at lines 136-137 uses `this.eventLog.splice(0, this.eventLog.length - this.maxEventLogSize)`. For an event log of exactly 1001 entries, this removes 1 entry (splice(0, 1000)), leaving 1001 - 1000 = 1 entry. For 2000 entries, it removes 1000, leaving 1000. The intent is to keep the most recent `maxEventLogSize` entries, but the splice removes the oldest `this.eventLog.length - this.maxEventLogSize` entries. This is correct. However, the trim only happens after a push exceeds the limit, not proactively. | todo | - | The behavior is correct but consider using `this.eventLog.splice(0, this.eventLog.length - this.maxEventLogSize)` only when `this.eventLog.length > this.maxEventLogSize` to avoid unnecessary array allocation on every trim check. |

| 424 | low | interaction | src/interaction/nl-gateway/nl-gateway-model.ts:69 | `IntentParseResult` has `riskClassification` as an optional property (line 74-78 shows it with `?`), but in `nl-gateway-support.ts` line 386, the `riskClassification` is always spread from `classifyRisk()`. The type should not be optional since the classification is always computed. | todo | - | Make `riskClassification` a required field in `IntentParseResult` since `parseDetailed()` always populates it via `classifyRisk()`. |

| 425 | low | interaction | src/interaction/nl-gateway/intent-parser/index.ts: | The single-word approval detection at line 132 has a regex `/approve\|审批\|通过\|批准/i` but the message length check `message.trim().length < 10` does not account for multi-byte characters. A 7-character Chinese string like "审批通过" (4 bytes per char in UTF-8) would pass the length check but the regex requires English "approve" which is at least 7 ASCII chars, so no false positive. However, the logic is fragile - a 9-character Chinese approval phrase like "请审批通过" (5 chars) would trigger the short-circuit at higher confidence than the general intent signals. | - | - | Add explicit handling for Chinese approval phrases in the short-circuit condition, or document the assumption that short messages with approval intent should default to `approval_action` regardless of locale. |

| 426 | low | interaction | src/interaction/proactive-agent/schedule-manager/i | `shouldRunScheduleTrigger()` compares `Date.parse(nowIso) - Date.parse(lastFiredAt) >= parseDurationMs(cooldown)`. `Date.parse` is legacy API and may produce different results across Node.js versions for non-ISO date strings. The function also assumes `nowIso` and `lastFiredAt` are in ISO format, but the function signature does not enforce this via type checking. | todo | - | Use `new Date(nowIso).getTime()` and `new Date(lastFiredAt).getTime()` consistently, or document the expected date format contract. Consider adding a validation step that throws if dates cannot be parsed. |

| 427 | low | ops-maturity | src/ops-maturity/capacity-planner/capacity-plannin | `variance` calculation divides by `Math.max(1, usageValues.length - 1)` which is the sample variance formula (Bessel's correction). However, `standardDeviation` then takes `Math.sqrt(Math.max(variance, 0))`. The `Math.max(variance, 0)` guards against negative variance due to floating-point error, but the underlying `variance` formula can still produce small negative values (-0.000001) that get zeroed, slightly biasing the standard deviation downward. | todo | - | Use `Math.max(0, variance)` instead of `Math.max(variance, 0)` to properly handle negative floating-point edge cases before the sqrt. |

| 428 | low | ops-maturity | src/ops-maturity/cost-optimizer/cost-optimization- | `riskLevelForSubject()` upgrades risk to "medium" when a record has `costType === "llm"` or `costType === "model"`. However, `costType === "model"` is not defined in the `CostSubjectType` union. This may be a legacy or misspelled type that falls through to the default `baseRisk`. | todo | - | Clarify whether `"model"` is a valid cost type. If not, remove it from the check. If it is valid, ensure it is added to the `CostSubjectType` union. |

| 429 | low | ops-maturity | src/ops-maturity/drift-detection/learning/promotio | The promotion gate evaluates multiple independent criteria and accumulates reasons, but the `allowed` flag is `reasons.length === 0`. If one criterion fails (e.g., insufficient success lift), subsequent criteria checks still execute. This is fine functionally, but there is no early-abort optimization for expensive report evaluations. | todo | - | Consider short-circuiting on first failure for high-cost evaluations by checking a `passed` boolean flag before subsequent checks. Not critical since the gate is not performance-sensitive. |

| 430 | low | ops-maturity | src/ops-maturity/chaos/chaos-experiment-scheduler. | `executeRollbackWithTimeout()` creates an AbortController but does not call `controller.abort()` when the experiment is found to not exist or during early returns. The controller's signal would remain listeners attached until abort or timeout fires. | todo | - | Call `controller.abort()` with an appropriate error message in all early-return paths within `executeRollbackWithTimeout()`. |

| 431 | low | ops-maturity | src/ops-maturity/version-management/version-compat | When checking wildcard source version entries, the `for...of` loop mutates entries by adding a `recommendedProfile` property via spread in `buildCostOptimizationRecommendation`. This mutation is indirect and could cause issues if the same profile object is used elsewhere. | todo | - | Ensure `findLowerCostPeerProfile()` returns a new object each call rather than returning a shared reference that gets mutated. |

| 432 | low | ops-maturity | src/ops-maturity/compliance-reporter/compliance-re | `getActiveViolations()` returns violations filtered by `remediatedAt == null` but the returned array is not sorted or limited. Over time, this could return a very large array if many violations have been recorded. | todo | - | Add pagination or a limit parameter to `getActiveViolations()` to prevent unbounded return sizes. |

| 433 | low | ops-maturity | src/ops-maturity/drift-detection/drift-detector-se | `safeHashEquals()` converts both inputs to Buffers using `Buffer.from(left, "utf8")`. This is a constant-time comparison for the purpose of timing attack resistance, but the function is used for fingerprint equality, not security-sensitive comparison. The function name and constant-time approach suggests security intent, yet it is not documented. | todo | - | Document why constant-time comparison is needed here, or use a simpler equality check if timing safety is not a requirement. |

| 434 | low | ops-maturity | src/ops-maturity/drift-detection/changepoint-detec | `hoursToSamples()` uses `Math.round(hours * samplesPerHour)` and then `Math.max(1, ...)`. For fractional hour inputs like `0.25` (15 minutes), this rounds to 0 then the `Math.max(1,...)`纠正 it to 1. The behavior is correct but the comment/docstring does not explain this rounding semantics. | todo | - | Add documentation explaining the rounding behavior and that the minimum sample count is always at least 1 regardless of input window size. |

| 435 | low | platform/five-plane-execu | src/platform/five-plane-execution/budget-allocator | The settle method has: `const expectedVersion = input.expectedVersion ?? input.ledger.version; if (input.ledger.version !== expectedVersion) { throwVersionCasError(...) }`. This checks that the ledger version matches the expected version, but this is redundant with the CAS update that will happen in `persistLedger()`. If `input.expectedVersion` is not provided, it defaults to `input.ledger.version` which always matches, so the check is a no-op. If it IS provided, the check is valid but then `persistLedger` also does a CAS - double check overhead. | todo | - | Remove the redundant version check before `persistLedger`. Let `persistLedger` handle the CAS verification. If you want to verify before persisting, at least don't default to the same value - use a distinct check that adds value. |

| 436 | low | platform/five-plane-execu | src/platform/five-plane-execution/execution-engine | The effect builder creates IDs using `Date.now()` for the first part of the ID: `id: 'effect_${Date.now()}_${randomUUID()}'`. If two effects are created within the same millisecond (which is possible in fast code paths), they would have the same `Date.now()` portion, differing only in the UUID. This is fine for uniqueness, but the comment/docstring does not explain this. | todo | - | Document that effect IDs are designed to be unique even when created in rapid succession, and that the timestamp portion is for debugging/readability rather than uniqueness guarantees. |

| 437 | low | platform/five-plane-execu | src/platform/five-plane-execution/execution-engine | The step execution loop has two nested structures: an outer `for` loop over `plannedWorkflow.executionSteps` and an inner `for` loop for retry attempts (line 144). However, when a step is skipped due to upstream dependency failure (line 92-140), the code calls `continue` to skip to the next step in the outer loop WITHOUT updating `executionAttemptCounter`. This is correct (skip doesn't count as an attempt), but the `workflowRetryCount` and `workflowLastErrorCode` are not reset for skipped steps. | todo | - | When a step is skipped, reset `workflowLastErrorCode = null` to avoid carry-over of previous step errors. Document that skipped steps do not affect retry counts. |

| 438 | low | plugins | src/plugins/retrievers/coding-retriever.ts:55 | The `score` field in `RetrieverKnowledgeResult` is typed as `number` in `plugin-spi.ts` (line 130), but at line 57 the code uses: ```typescript score: Number((search.relevanceScores.get(`${symbol.name}@${symbol.filePath}`) ?? 0.8).toFixed(4)) ``` `.toFixed(4)` returns a `string`, and `Number()` converts it back to `number`. This is unnecessarily verbose - just use the value directly. More importantly, if `relevanceScores.get()` returns a non-number value (e.g., from a bug in `SemanticRepoMapService`), `toFixed` would throw a runtime error since numbers don't have `toFixed` unless coerced first. | todo | - | Simplify to `score: search.relevanceScores.get(...) ?? 0.8` and ensure `relevanceScores` always stores numbers. |

| 439 | low | plugins | src/plugins/retrievers/game-dev-retriever.ts:62 | Multiple `score` calculations use `Math.min(0.97, 0.7 + Math.min(searchQuery.length / 240, 0.18))`. If `searchQuery` is extremely long, the inner `Math.min` caps at 0.18, so the score formula becomes `0.7 + 0.18 = 0.88`, capped at `0.97`. But if `searchQuery` is empty, the score is `0.7 + 0 = 0.7`. This is a reasonable heuristic, but the magic numbers (0.7, 0.18, 240) are not documented and differ from other retrievers (e.g., `growth-retriever.ts` uses static 0.95/0.88/0.82 scores). Inconsistent scoring heuristics across retrievers make it difficult to compare relevance across domains. | todo | - | Document the scoring formula and consider extracting scoring constants into a shared configuration for the domain. |

| 440 | low | plugins | src/plugins/adapters/github-adapter.ts:220 | `execute()` calls `requireRepository(params["repository"])` then `buildEndpoint()` which can throw for unknown actions before `policy.evaluate(endpoint)` is called. If an unknown action is passed, the policy check never executes because `buildEndpoint` throws first. This means security policy evaluation is skipped for unsupported actions - but since the action also doesn't execute (throws), the practical risk is low. However, the sequencing means an attacker could probe allowed endpoints by observing which actions get to the policy check vs. throw. | todo | - | Call `policy.evaluate()` with the unvalidated action before `buildEndpoint()`, or validate the action against a allowlist before the switch statement. |

| 441 | low | plugins | src/plugins/adapters/crm-adapter.ts:94 | The action validation regex `/^[a-zA-Z0-9_]+$/` allows alphanumeric and underscore characters. However, CRM API endpoints can include hyphens (e.g., `contact_lists`, `email_campaigns`). If an action like `"contact-list"` is passed, it will fail the regex and throw an error, even though it might be a valid CRM action. | todo | - | Extend the regex to include hyphens if the CRM API uses them, or document the allowed action format and validate against the actual CRM API's action list. |

| 442 | low | plugins | src/plugins/adapters/github-adapter.ts:151 | `createIdempotencyKey()` returns `null` for `get_file` actions, but this is treated differently at line 244-254 where `idempotencyKey` is spread into the result. The `null` return is intentional (file reads are idempotent without a key), but the code doesn't distinguish between "intentionally no key" and "error generating key". The ternary `idempotencyKey == null ? {} : { idempotencyKey }` handles the value correctly, but the intent is implicit rather than documented. | todo | - | Use an option bag pattern or explicit `undefined` return to distinguish intentional no-key from error cases. Document that `get_file` actions are inherently idempotent. |

| 443 | low | tests/unit | - | The UI `HttpTransport` has a circuit breaker (rest-client.ts lines 199-203: `failures`, `lastFailure`, `state`) with: - `failureThreshold: 5` failures to open (line 217) - `resetTimeoutMs: 30000` ms before half-open (line 218) The SDK `RetryableApiClient` does not have a circuit breaker. It retries based on `RetryConfig` which has `maxRetries: 3`, `backoffMs: 100`, `maxBackoffMs: 1000` (api-client.ts lines 38-43). The retry logic in `request()` (api-client.ts lines 582-594) retries on `retryableStatus = response.status === 429 \|\| response.status >= 500`. This means: - UI opens circuit after 5 failures, fails-fast for 30s, then half-open - SDK retries up to 3 times with exponential backoff, then fails These are different resilience strategies for the same goal (preventing cascading failures). | - | - | Document the different resilience strategies, or extract a shared resilience configuration object that both modules use. |

| 444 | low | tests/unit | - | The UI `BrowserWSClient` calculates reconnect delay as (ws-client.ts lines 305-311): ```typescript const exponentialDelay = Math.min(baseReconnectDelayMs * Math.pow(2, this.reconnectAttempts), this.maxReconnectDelayMs); const jitter = exponentialDelay * Math.random() * 0.3; ``` The SDK's SSE reconnect delay (api-client.ts lines 346-355) uses: ```typescript const retryAfter = Math.min(1000 * (2 ** Math.min(reconnectAttempt - 1, 4)), 10000); ``` Key differences: - UI base: `1000ms * 2^attempt`, cap `30000ms`; SDK: `1000 * 2^(attempt-1)`, cap `10000ms` - UI adds 30% jitter; SDK does not - UI has max 10 reconnect attempts (ws-client.ts line 103); SDK reconnects indefinitely (while loop at lines 319-356) - UI reconnects WebSocket; SDK reconnects SSE stream | todo | - | Document the different reconnect strategies (WebSocket vs SSE) and ensure they are intentionally different rather than accidentally divergent. |

| 445 | low | tests/unit | - | `bootstrapVerticalDomainBaselines()` at line 608 registers domains and then activates them (line 622-624). Knowledge namespaces are registered at line 619-621 AFTER registration but the activation happens at line 623 in the same loop iteration. Checkpoints could be created during the activation process (or just after registration but before namespace registration completes), and they would have no knowledge namespace context. The bootstrap process is sequential per domain: register → register namespaces → activate. But if multiple domains are being bootstrapped in parallel by different threads, the namespace registration could lag behind activation, creating a window where checkpoints for a domain lack namespace context. | todo | - | Register knowledge namespaces BEFORE activation in the bootstrap sequence. Ensure the namespace registration is atomic with the domain registration, not a separate step that could be reordered or delayed in concurrent scenarios. |

| 446 | low | tests/unit | tests/unit/plugins.test.ts:334 | Test `PluginSpiRegistry can register validator and invoke it` uses type casting `(plugin as any).validate` to access the `validate` method. This bypasses TypeScript's type checking and assumes knowledge of the plugin's internal structure. The test validates that a validator plugin has a `validate` method but uses `any` to access it. | todo | - | If the plugin interface includes `DomainValidatorPlugin` type, the test should access the method through a properly typed interface rather than `any`. Consider defining a test-only interface that properly types the validator's methods. |

| 447 | low | tests/unit | - | `ExecutionDispatchService` uses a hardcoded `heartbeatStalenessThresholdMs = 30_000` (line 580) to reject workers with stale heartbeats. `RpoRtoTrackingService` has configurable `RpoRtoTarget.rtoMs` per region pair (line 22), but there is no integration. If the RTO target for a region pair is 5 minutes but the heartbeat threshold is 30 seconds, the dispatch service could mark workers as unavailable before the failover logic would actually trigger a failover. | todo | - | Derive the heartbeat staleness threshold from the RTO target: `heartbeatStalenessThresholdMs = Math.min(30_000, targetRtoMs * 0.1)` or make it configurable per region pair. Add a comment explaining the relationship between heartbeat staleness and RTO. |

| 448 | low | tests/unit | tests/unit/runtime/dispatcher.test.ts:1 | Tests for `executeMultiStepToolCallForTests` return JSON strings that are then parsed with `JSON.parse(result)`. Some tests check `parsed.success === true` but don't validate the structure of `parsed` when `success` is false. For example, line 51 checks `parsed.errorCode` but only when `success` is false. This is good, but the pattern is inconsistent across tests - some only check success cases. | todo | - | Ensure all tests validate the complete structure of the result object, including both success and error paths. Add assertions for the presence of `errorCode`, `errorMessage`, or other relevant fields when `success` is false. |

| 449 | low | tests/unit | - | - Domain onboarding has `security_certification` phase - Pack lifecycle has `certifying` stage - These are semantically similar but: 1. Different terminology makes it unclear which one to use in documentation 2. Code that refers to "certifying" could mean pack or domain context 3. Error messages like "Pack must be in certifying stage" (pack-lifecycle-service.ts line 143) don't map clearly to domain onboarding phase | todo | - | Standardize terminology. Either: - Rename pack `certifying` to `security_certification` to match domain onboarding, or - Rename domain onboarding `security_certification` to `certifying`, or - Document the equivalence explicitly in ADR |

| 450 | low | tests/unit | tests/unit/interaction-governance-runtime-catalog. | This file tests `buildInteractionGovernanceRuntimeCatalog` but the parallel file at `interaction-governance/interaction-governance-runtime-catalog.test.ts` tests the same functionality with more thorough coverage. The file `interaction-governance-runtime-catalog.test.ts` at the root level only has 2 tests while the nested version has 20+ tests. There's duplication of test coverage across two files. | todo | - | Consolidate the test coverage into a single file (preferably the more thorough one in `interaction-governance/`). Remove the duplicate root-level test file or mark the root-level one as deprecated and delegate to the nested version. |

| 451 | low | tests/unit | - | The test asserts `validateExecutableContract("RequestEnvelope", { requestId: "" })` throws `ValidationError`. However, `{ requestId: "" }` is not a valid input to the `RequestEnvelope` schema — it would fail schema validation for multiple missing required fields (tenantId, principal, etc.). The test correctly expects a ValidationError, but the error could be thrown for the wrong reason (missing required field vs empty string validation). The specific error code is not checked. | todo | - | Check the specific error code: ```typescript try { validateExecutableContract("RequestEnvelope", { requestId: "" }); assert.fail("should throw"); } catch (e) { assert.ok(e instanceof ValidationError); assert.equal(e.code, "request_envelope.invalid_request_id"); } ``` |

| 452 | low | tests/unit | tests/unit/domains-runtime-orchestrator.test.ts:17 | Tests `registerDomainsRuntimeOrchestrator registers orchestrator in registry` and similar tests that call `registerDomainsRuntimeOrchestrator(registry)` don't use `test.beforeEach` cleanup. However, `DomainsRuntimeOrchestrator` registers itself as a singleton service. If another test in the file calls `startup()` before these registration tests, state could leak. The file does have `await registry.reset()` calls throughout but the pattern is inconsistent - some tests reset before, some don't need to. | todo | - | Establish a consistent pattern of `test.beforeEach(async () => { await registry.reset(); })` at the top of the test file to ensure each test starts with a clean registry. |

| 453 | low | tests/unit | tests/unit/platform-architecture-bootstrap.test.ts | Test `registerPlatformArchitectureServices enforces startup ordering before registration` uses `readFileSync` to read source code and then does a string regex match for `assertStartupOrderEnforced()`. This is a source code inspection test that tests implementation rather than behavior. If the function is renamed or refactored, this test would break despite the behavior being preserved. | todo | - | Either remove this test if the startup ordering is enforced through runtime behavior checked in other tests, or convert to a behavioral test that verifies startup ordering is enforced by actually calling the functions in wrong order and checking for errors. |

| 454 | low | tests/unit | - | `buildKnowledgeSchema()` at line 331 creates namespaces like `${seed.domainId}/default` and `${seed.domainId}/governed`. The checkpoint envelope at line 156-168 creates a metadata block with `originalSizeBytes`, `compressedSizeBytes`, `checksum`, etc. but has no `namespaceId` or `knowledgeDomainId` field. If domain-specific knowledge is embedded in checkpoint payloads (e.g., domain-specific prompt templates, guardrails, or eval criteria), the compression and storage layer has no awareness of which knowledge namespace the checkpoint belongs to. This means: 1. Checkpoints cannot be efficiently routed to domain-specific storage policies 2. Knowledge schema freshness windows (line 337: `freshnessWindowHours`) cannot be enforced on checkpoint lifecycle 3. Domain governance policies (line 440: `complianceRules`, `mandatoryEvidence`) cannot be applied to checkpoints | todo | - | Add `namespaceId?: string` to `CheckpointEnvelopeMetadata`. When creating checkpoints for domain-specific workflows, include the knowledge namespace so storage policies can apply domain-specific retention and access control. |

| 455 | low | tests/unit | tests/unit/platform-root-types.test.ts:95 | Test `PlatformRootSummaryBuilderDeps structure validation` uses `as any` casts extensively to force-fit incompatible return types into the mock structure. The comment says "Create minimal mock functions to satisfy the function types" but the `as any` casts bypass TypeScript's type checking. This could mask type incompatibilities between what the test expects and what the actual `PlatformRootSummaryBuilderDeps` interface requires. | todo | - | Create properly typed mock implementations that return objects matching the expected shapes. Using `as any` hides potential type mismatches that could surface at runtime. |

| 456 | low | tests/unit | tests/unit/domains-domain-recipe-service.test.ts:1 | The test `DomainRecipeService.getPrototypeTemplates returns all 12 templates` hardcodes the expected count of 12 templates. If new prototype templates are added (which is a reasonable extension), this test would fail. The test also checks for specific categories (`analysis`, `implementation`, `review`, etc.) but the ordering or presence of specific categories is not validated - only that they exist in the set. | todo | - | If 12 is a contractual number that should not change, document this in an comment. If it's subject to change, consider checking that the count is >= some minimum threshold rather than exact equality, or ensure the test has a comment noting it requires updates when templates are added. |

| 457 | low | tests/unit | - | Tests at lines 234-256 use `createMockBundle("TestBundle", "v1.0")` and then query with `manager.getVersionLineage("TestBundle", "v1.0")`. The bundle's `version` field (line 498) is the normalized integer (`100` for "v1.0"), but the PromptVersionManager stores entries by `displayVersion` string ("v1.0") when `registerBundleVersion` is called. The test queries use the string "v1.0" which matches, but this works only because `registerBundleVersion` uses displayVersion as the key, not the numeric version. This coupling is not obvious from reading the test. | todo | - | Add comments explaining that `registerBundleVersion` uses displayVersion as the key, and the tests use displayVersion strings for lookup. Consider adding a test that queries by numeric version to verify this works correctly. |

| 458 | low | tests/unit | - | `DomainRegistryService.buildCapabilityEntry()` at line 330 includes `knowledgeNamespaces: [...(this.knowledgeNamespacesByDomain.get(domainId) ?? new Set<string>())]`. This exposes domain knowledge namespaces as part of the capability entry. However, `SessionDualStorageService` has no method to query knowledge - `replaySessionEvents()` and `replayTaskSessionHistory()` return raw events without any knowledge namespace filtering. If a session contains events that reference knowledge operations (e.g., `knowledge.retrieve`, `knowledge.store`), the session replay would not validate these against the domain's authorized knowledge namespaces. There is no `validateSessionKnowledgeAccess()` or similar integration point. | todo | - | If sessions can contain knowledge operations, add a `validateSessionKnowledgeNamespaces(events: SessionEvent[]): ValidationResult` method to `SessionDualStorageService` that checks event payload references against the domain's authorized knowledge namespaces from `DomainRegistryService`. |

| 459 | low | tests/unit | - | The `PromptBundleCompatibilityMatrix` interface (lines 15-24) defines four required arrays: `toolSchemaVersions`, `evaluatorSchemaVersions`, `domainDescriptorVersions`, `modelRoutingProfiles`. The `validateCompatibilityMatrixShape()` function (lines 261-276) validates these are arrays. The mock at lines 514-519 provides empty arrays which is correct. However, there is no test verifying that a compatibilityMatrix with non-array fields throws the correct ValidationError. | todo | - | Add test for `validateCompatibilityMatrixShape` rejection of invalid input. |

| 460 | low | tests/unit | - | `DomainBaseline.metaModel` is built via `seedDomainMetaModel()` (line 510) and validated via `MetaModelValidator` (line 518). The meta model tracks domain metadata including `taskTypes`, `tags`, and `riskLevel`. The checkpoint envelope's `schema` field (line 84) is a free-form string that could be any value - there is no validation that `envelope.schema` is consistent with the domain's meta model expectations. For example, if a checkpoint's `schema` field says `"workflow_step_checkpoint.v1"` but the domain's meta model expects a different schema version, the checkpoint would be accepted without validation. The meta model validation at line 518 only validates the meta model itself, not its relationship to checkpoint schemas. | todo | - | Add a `validateCheckpointSchema(domainId: string, schema: string): boolean` method to `DomainRegistryService` that checks if a checkpoint schema is compatible with the domain's meta model. Call this in `unpackCheckpointEnvelope()` when domain context is available. |

| 461 | low | tests/unit | tests/unit/domains-runtime-catalog.test.ts:34 | Test `buildDomainsRuntimeCatalog ring counts match expected baseline counts` uses exact equality assertions (`assert.equal(catalog.ring1.length, 8)`). These counts are duplicated from `root-entry-summary.test.ts` which also hardcodes `ring1: 8, ring2: 11, ring3: 12`. If ring counts change in the source, both tests would need updating. This violates DRY and makes updates error-prone. | todo | - | Centralize these magic numbers. Either create a shared constants file that both tests import, or derive the expected counts from the actual catalog builders dynamically and have a single canonical test that validates the counts are consistent across the system. |

| 462 | low | tests/unit | tests/unit/platform-mainline-bootstrap.test.ts:91 | Test `criticalSubmodules arrays are non-empty for all capabilities` and `architectureSections arrays are non-empty for all capabilities` use loose assertions that could pass with empty arrays if the loop doesn't execute. The assertions `capability.criticalSubmodules.length > 0` and `capability.architectureSections.length > 0` would pass if an capability has an empty array, but only if the loop runs. If the array is somehow falsy (not possible with proper typing), it would throw. | todo | - | These tests are adequate as-is given TypeScript's type system ensures these are arrays. The severity is low - consider adding explicit null checks if the arrays could theoretically be undefined or null at runtime. |

| 463 | low | tests/unit | - | The `assertGraphPatchSafety()` function (lines 219-246) has two checks: 1. `safe_append` cannot affect executed nodes or side effects (lines 221-226) 2. Side effects require compensation plan (lines 227-232) 3. Cannot `mark_skipped` an executed node (lines 233-245) The test at lines 365-452 covers most cases but does not test the combination where `affectedExecutedNodes` includes a node NOT in the operations' `targetRef` — this should be allowed under `safe_append`. Also, the edge case where `affectedSideEffects` is non-empty with `compensationPlanRef` but `compatibilityClass` is not `requires_human_approval` is not tested. | todo | - | Add test cases for: 1. `safe_append` with `affectedExecutedNodes` that don't match any operation targetRef (should pass) 2. Side effects with compensation plan but default `compatibilityClass` (should pass) |

| 464 | low | tests/unit | - | `DomainRegistryService` has a 7-state lifecycle: `draft → registered → canary → active → updating → deprecated → archived`. `SessionDualStorageService.SessionEventType` (line 33) includes `session_created`, `session_updated`, `session_completed`, `session_failed`, `session_cancelled`, `message_added`, `message_updated`, `compaction_recorded`. There is no direct mapping between domain lifecycle transitions and session events. If a domain transitions from `active` to `updating`, there is no `domain_status_changed` session event recorded. The domain lifecycle is tracked entirely within `DomainRegistryService` via event publishing (e.g., `eventType: "domain:updating"` at line 157) but these are not stored in the session JSONL. Session storage only handles session-level events, not domain-level events. This creates an audit gap - domain lifecycle transitions are not available in the session replay timeline. | todo | - | Add `domain_lifecycle_transition` to `SessionEventType` union. When `DomainRegistryService` publishes domain lifecycle events (lines 61-70, 100-109, 132-141, etc.), also record them in the session JSONL for the associated task/session so they appear in the replay timeline. |

| 465 |  | Unknown | - | `epochCounter` is a simple in-memory `number` that is incremented at line 94 inside `acquireLeadership()` and persisted to disk via `persistState()` (lines 284–308). However, the load/reload cycle at lines 256–282 is **not atomic with increment sites**. If two regions call `acquireLeadership` concurrently before either persists, both will read the same `epochCounter` from disk and issue fencing tokens with **the same epoch**. ```typescript // fencing-token-service.ts:68 — in-memory only, no distributed lock private epochCounter = 0; // fencing-token-service.ts:94 — increment under no distributed lock this.epochCounter++; const issuedAt = nowIso(); const fencingToken: FencingToken = { tokenId: newId("fence"), regionId, epoch: this.epochCounter, // <— two concurrent callers get same epoch ``` The persistence uses a temp-file rename pattern (lines 293–307) which is safe for single-writer correctness but does **not** prevent the race between in-memory state loading and concurrent increments across processes. **Split-brain scenario:** Region A (leader) and Region B (candidate) both call `acquireLeadership` for the same entity. Both read `epochCounter = 5` from disk. Region A writes token with epoch 6, Region B writes token with epoch 6 (before A persists). Both tokens pass `validateFencingToken` epoch check since the in-memory state on each region still shows epoch 6 as current. **Impact:** Lost updates during failover — two regions may hold semantically "valid" fencing tokens for the same entity. | todo | - | Use an atomic increment operation (e.g., a distributed lock or atomic compare-and-swap in the persistence layer) or externalize epoch generation to a single authoritative service. |

| 466 |  | Unknown | - | `assertFailoverWithinRto()` at lines 634–643 and `completeFailoverWithRtoCheck()` at lines 649–659 both compute `elapsedMs` from an in-memory timer started at line 606–613 (`startFailoverTimer`). However, the actual execution migration (lease handover in `ExecutionLeaseService`) is **async and not gated by this check**. The RTO assertion fires immediately after the failover record is written (line 742 `completeFailover` + line 745 `assertFailoverWithinRto`) but long before the new leader has actually taken over execution leases. ```typescript // region-health-check-service.ts:741–745 rpoRtoService.completeFailover(sourceRegionId, targetRegionId, true, null); this.assertFailoverWithinRto(sourceRegionId, targetRegionId); // <— fires before execution migration // ... but actual lease handover in ExecutionLeaseService is still pending ``` **RTO violation scenario:** Network partition causes failover. `startFailoverTimer` records t=0. `orchestrateFailover` returns at t=5s (record written). `assertFailoverWithinRto` passes (5s < 30s). But the actual execution lease migration takes 45s because workers must still heartbeat and hand over. The RTO SLA is breached but no error is raised at the correct time. **Impact:** RTO tracking shows compliance but actual recovery took longer than tracked. | todo | - | Gate the RTO assertion on a confirmed execution lease handover completion, not just the failover record write. |

| 467 |  | Unknown | - | `confirmBatch()` (lines 326–346) updates the checkpoint and removes the batch from the queue **in two separate steps** without a transactional boundary. If the process crashes between `this.checkpoints.set(key, checkpoint)` (line 343) and `this.removeBatchFromQueue(key, batch.batchId)` (line 345), the batch will be re-sent on next replication with the **same sequence range**, causing duplicate events to be applied at the target. ```typescript // cdc-replication-service.ts:342–345 this.checkpoints.set(key, checkpoint); // checkpoint advanced this.processedEventCounts.set(key, ...); this.removeBatchFromQueue(key, batch.batchId); // <— crash here = duplicate batch ``` **Lost update / data inconsistency scenario:** Batch with events sequence 100–199 is confirmed (checkpoint updated to 199) but queue removal fails. On restart, `prepareBatch()` will filter events with `sequence > checkpoint.lastEventSequence` (line 292), which correctly excludes 100–199, but only if the persisted checkpoint was actually written. If `checkpoints.set` was in-memory only, the batch will be re-prepared. **Impact:** Duplicate CDC events applied at target region → inflated counters, double execution of side effects. | todo | - | Atomically persist checkpoint and queue removal together, or use an idempotency key on events to deduplicate at apply time. |

| 468 |  | Unknown | - | `validateFencingToken()` at lines 157–217 checks: 1. `current.regionId !== token.regionId` (line 182) 2. `current.epoch !== token.epoch` (line 192) 3. `current.fencingToken.entityId !== token.entityId` (line 202) But it does **not** verify that `token.tokenId` is unique or hasn't been used before. A replay attack is possible: a client that obtains a valid fencing token can replay it indefinitely, as long as the epoch and regionId still match. **Impact:** A client with a captured fencing token can continue writing even after leadership has been transferred to another region, as long as the epoch hasn't been incremented. | todo | - | Track used `tokenId` values in a set or use a nonce-based scheme to detect replays. |

| 469 |  | Unknown | - | `startFailover()` and `completeFailover()` use `toPairKey(sourceRegionId, targetRegionId)` which returns `"source:target"`. If Region A fails over to B, then B fails over to C, the second `startFailover` call will **overwrite** the first failover event because the key `"A:B"` differs from `"B:C"`. But if A fails back to A (self-healing after a false positive), the same key `"A:A"` would overwrite without completing the first failover. More critically, `getStatus()` at lines 302–339 computes `activeFailoverRtoMs` from `failoverEvents.get(key)` without checking if a prior failover for the same pair is still open. ```typescript // rpo-rto-tracking.ts:317–319 if (failover && !failover.completedAt) { activeFailoverRtoMs = Date.now() - new Date(failover.startedAt).getTime(); } ``` If two failovers are triggered for the same pair in quick succession (e.g., first fails, second starts before first completes), the first event's `completedAt` is still null and its `startedAt` is still being tracked for RTO, but the second overwrite has lost the first's context. **Impact:** RTO measurement becomes unreliable when rapid failovers occur. | todo | - | Use an array of failover events per pair instead of a single mutable event, or validate there is no open failover before starting a new one. |

| 470 |  | Unknown | - | The split-brain detection at lines 150–166 compares fencing epochs and triggers evidence if `Math.abs(epochA - epochB) >= 1` (line 157). This means **any epoch difference** triggers evidence, including the normal case where Region A is at epoch 5 and Region B is at epoch 6 after a legitimate failover. This will generate false positive evidence on every normal failover. ```typescript // split-brain-protection.ts:157 if (Math.abs(epochA - epochB) >= 1) { // <— too sensitive, normal failover triggers this ``` **Impact:** Split-brain "confirmed" status will fire after every legitimate failover, not just actual split-brain conditions. | todo | - | Only trigger evidence when epochs are **equal** but leaders are **different** (true concurrent leadership), or when epochs differ by a larger threshold indicating a stuck epoch. |

| 471 |  | Unknown | - | `EpochManager.isStaleDemotedLeader()` at lines 151–158 uses `demotedLeaders.has(`${key}:${regionId}`)` to determine if a region was previously demoted. This is a single-level map, but `promoteEpoch()` at lines 141–143 sets `demotedLeaders.set(`${key}:${oldLeaderRegionId}`, newLeaderRegionId)`. If a region is demoted, then the new leader is later demoted (chain failover), the map will have two entries with **different keys** but the staleness check only looks at the current partition key. A region that was demoted in a **previous failover cycle** may still appear as "demoted" even after the system has stabilized, because `clearDemotedRecord()` is never called in the normal flow. ```typescript // failover-controller/index.ts:155–156 const wasDemoted = this.demotedLeaders.has(`${key}:${regionId}`); return wasDemoted; // <— returns true even if region demoted in a prior cycle that has since resolved ``` **Impact:** Legitimately healthy regions that were previously demoted may be incorrectly rejected from rejoining as leaders due to stale demoted-leader state. | todo | - | Add a timestamp or epoch number to the demoted leader record and clear it after a successful rejoin-as-follower or after a configurable TTL. |

| 472 |  | Unknown | - | `validateWriteAccess()` at lines 622–750 performs multiple checks in sequence. The fencing token check at line 683 fires **before** the TTL/lease-expiry check at line 663. This means a worker that presents a valid fencing token but whose lease has expired will be rejected at line 683 only if the fencing token doesn't match — but if the worker somehow retains a matching fencing token from a previous lease cycle and the lease **hasn't been explicitly closed yet**, the expiry check at line 663 would catch it. The actual issue is more subtle: the check order means the error `reasonCode` returned reflects the **first** failed check, not the most recent. A worker with a stale fencing token but valid TTL would get `stale_fencing_token` — correct. But a worker with valid fencing token but expired TTL would get `lease_expired` — also correct. The problem is if a new lease was granted to the same worker ID (same worker restarts and gets a new lease with same workerId), the fencing token would differ and it would be caught first. The real race: between lines 148–155 (`acquireLease` checks active lease exists) and line 173 (new fencing token generated), if a concurrent `validateWriteAccess` call runs on the old lease holder, it would still see the **old** fencing token but the **new** active lease (because the old lease was closed and new one inserted in the same transaction). The transaction isolation should prevent this, but the validation logic at line 683 checks `input.fencingToken !== activeLease.fencingToken` — and the new lease's fencing token is `getLatestFencingToken(...) + 1` which would be different. ```typescript // execution-lease-service.ts:683 if (input.fencingToken !== activeLease.fencingToken) { return { allowed: false, reasonCode: "stale_fencing_token", // <— correct rejection ``` This is actually correct behavior. The transaction in `acquireLeaseWithinTransaction` (line 118) should be atomic. The real concern is that `validateWriteAccess` uses `getActiveExecutionLease` while the fencing token authoritative check uses `getLatestExecutionLease`. These could diverge if there's a race between closing one lease and opening another. **Impact:** Potential for stale writes if transaction isolation is not strong enough. --- | todo | - | - |

| 473 |  | Unknown | - | `updateHealthState()` at lines 480–487 updates `healthResults` and `lastCheckTime` in two separate Map operations. If a crash occurs between the two writes, the state will be inconsistent (timestamp from one check, metrics from another). ```typescript // region-health-check-service.ts:481–482 this.healthResults.set(regionId, result); this.lastCheckTime.set(regionId, result.checkedAt); // <— not atomic with above ``` **Impact:** Observability/monitoring discrepancy, not directly causing data inconsistency. --- | todo | - | - |

| 474 |  | Unknown | - | `prepareBatch()` calls `enqueueBatch()` (line 318) to queue the batch for async replication **before** the previous batch has been confirmed. The queue depth check at line 711 compares against `maxQueueDepth` but batches are added even when prior batches for the same key are still pending. This can cause head-of-line blocking where a slow batch prevents newer batches from being sent. ```typescript // cdc-replication-service.ts:708–715 private enqueueBatch(key: string, batch: CDCReplicationBatch): void { const queue = this.replicationQueues.get(key) ?? []; const maxQueueDepth = this.configs.get(key)?.maxQueueDepth ?? Number.POSITIVE_INFINITY; if (queue.length >= maxQueueDepth) { throw new CdcQueueBackpressureError(key, queue.length, maxQueueDepth); // <— backpressure } queue.push(batch); // <— adds even if previous batches are still pending ``` **Impact:** Replication lag increases beyond RPO SLA even when batches are being processed, due to queue ordering rather than actual failures. --- | todo | - | - |

| 475 |  | Unknown | - | `runReconciliation()` at lines 116–218 accepts a `ReconciliationJobInput` and **creates artificial issues** based on the counts in the input (e.g., line 121 `for (let i = 0; i < input.pendingWriteCount; i++)`). It does **not** actually query the source region to detect real gaps. The input must be pre-populated by the caller with actual gap data from the source region. If the caller passes empty arrays, no issues will be detected even if real gaps exist. ```typescript // failover-reconciliation-job.ts:119–130 // §52.3 + §31: Check for unreplicated writes for (let i = 0; i < input.pendingWriteCount; i++) { issues.push(this.createIssue( "unreplicated_write", ... `unreplicated_write_${i}`, // <— synthetic ID `Write at sequence offset ${i} not replicated`, // <— synthetic description ``` **Impact:** The reconciliation job provides a framework but the actual gap detection logic is outside this service. If the caller doesn't properly populate the input, the new leader will be promoted without real consistency checks. | todo | - | Document this clearly and ensure callers have the actual gap enumeration logic implemented before calling this service. |

| 476 |  | Unknown | - | The temp file path at line 293 includes `${process.pid}`: ```typescript const tempPath = `${this.storagePath}.${process.pid}.${Date.now()}.tmp`; ``` If the same process PID is reused after a crash-and-restart (common in container environments where processes get recycled PIDs), the rename at line 297 could overwrite a temp file from the previous run that was not cleaned up. **Impact:** Low probability, but could cause state corruption in container-restart scenarios. --- | todo | - | - |

| 477 |  | Unknown | src/index.ts:85 | `process.stdout.write` is used for the platform root summary output (structured JSON for the CLI runner). This is intentional and appropriate for CLI output. Not a bug, but worth documenting the convention. | todo | - | Consider adding a comment clarifying that stdout writes for CLI output are intentional and not violations of the logging policy. |

| 478 |  | tests/unit | - | The `Session` interface (session-management.ts lines 50-62) has no `tenantId` field. The `TenantScopeFilter.evaluate()` at lines 33-35 checks `principal.tenantId !== taskScope.tenantId`. For user sessions, tenant resolution appears to happen via `TenantBoundaryRegistryService` (imported at http-api-server.ts line 13) rather than from the session token. There's no documented ADR explaining how tenantId is resolved: 1. Is it embedded in the session JWT? 2. Is it looked up from `TenantBoundaryRegistryService` by principalId? 3. Is it passed as a header parameter trusted from the API gateway? The `TenantBoundaryRegistryService` lookup pattern is: `tenantRegistryService.resolveTenantForActor(principal.actorId)` but this method doesn't exist on the service based on the constructor injection. | todo | - | Create an ADR documenting: 1. The session-to-tenant resolution path 2. Which component is authoritative for tenantId for a given principal 3. Whether tenantId can change mid-session (e.g., user switches tenant context) 4. How service principals (namespace-scoped) should be handled in tenant scope checks |

| 479 |  | tests/unit | - | When a request arrives at `HttpApiServer` with both user auth headers (e.g., `Authorization: Bearer <token>`) AND service auth headers (e.g., `x-service-id`), there's no documented precedence. The current implementation only processes user auth via `authenticateOptionalPrincipal()`. Service auth is handled by internal services calling `extractServiceAuth()` directly, not via the HTTP server. There's no documented policy for: 1. Can a service call use user auth headers? Should it? 2. If both are present, which takes precedence? 3. Are services allowed to act on behalf of users (delegation), and if so, how is that modeled? | todo | - | Create an ADR documenting: 1. The auth header precedence model (service vs user) 2. Whether service calls can be user-delegated and how that affects session tracking 3. The audit trail when a service acts on behalf of a user (which session is charged?) 4. How tenant isolation applies to service-to-service calls |

| 480 |  | tests/unit | - | The self-healing service uses deterministic formulas based on string length parity (`componentId.length % 2`) and modular arithmetic (`deterministicScore % 5 !== 0`) for healing success and health check determination. This is documented nowhere and has no ADR. An operator cannot configure or reason about what success rate to expect. The relationship between self-healing health assessments and the execution state machine is also undocumented. | todo | - | Create an ADR documenting: 1. The deterministic success rate model and why it was chosen (presumably for testability) 2. How self-healing health state should interact with execution state machine 3. The cooldown coordination mechanism with execution retry budgets 4. The integration points (or lack thereof) between self-healing and execution engine |

| 481 |  | tests/unit | - | Rate limit keys differ between `inject()` and `handleRequest()`: - `inject()`: `inject:${endpoint}` (no tenant, no IP) - `handleRequest()`: `${clientIp}:${endpoint}` (IP but no tenant) This creates two different rate limiting behaviors for what might be the same logical operation. An operator cannot reliably predict rate limit behavior based on entry point. | todo | - | Create an ADR documenting: 1. The rate limit key design principles (what dimensions are used: tenant, IP, endpoint, entry point?) 2. Whether inject() and HTTP requests should share rate limit buckets or have separate limits 3. How multi-tenant deployments should configure rate limiting (per-tenant vs per-IP) 4. The relationship between DistributedRateLimiter and any ingress-level rate limiter |

| 482 |  | tests/unit | - | R4-28 introduced write-ahead logging for cost events. The code at line 577 inserts with "pending" status and at line 594 commits inside the transaction. However, there's no documented recovery mechanism for orphaned pending entries. The comment says "On crash recovery, uncommitted cost events can be detected and cleaned up" but no sweeper code exists in this file. The cost optimizer's `unsourcedRecordCount` (line 67 in cost-optimization-service.ts) tracks records without `decisionRef` but there's no mechanism to clean up orphaned WAL entries that survived a crash. | todo | - | Create an ADR documenting: 1. The WAL recovery sweep frequency and entry age threshold for "orphaned"判定 2. How orphaned WAL entries should be handled (delete? flag for review?) 3. The relationship between WAL cleanup and cost optimizer's `unsourcedRecordCount` 4. Whether the cost optimizer should tolerate orphaned entries or fail-fast if they appear |

| 483 |  | tests/unit | - | The allowList bypass in `evaluateExecution()` effectively disables panic mode enforcement for privileged actors. This is a significant security decision with no ADR. Questions that need answers: 1. What criteria determine allowList membership? 2. Does allowList apply to break-glass scenarios or normal operation? 3. Should allowListed actors still be rate-limited or throttled even if not blocked? 4. Is the allowList consistent with the execution engine's admission control? | todo | - | Create an ADR documenting: 1. The allowList membership criteria and approval process 2. What operations allowListed actors CANNOT perform even with allowList 3. The relationship between panic mode allowList and execution admission control 4. Audit trail requirements for allowList usage |