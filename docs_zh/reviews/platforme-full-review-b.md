# Platforme Full Review B - 问题汇总表

**总问题数**: 483
**日期**: 2026-05-23

---

## 统计

| 严重程度 | 数量 |
|----------|------|
| Critical | 13 |
| High | 143 |
| Medium | 205 |
| Low | 103 |

| 已有状态标注 | 0 |

---

## 问题详情表

| # | 严重程度 | 模块 | 文件:行号 | 问题描述 | 问题状态 | 问题根因 | 推荐修复 |
|---|----------|------|----------|----------|----------|----------|----------|
| 1 | critical | Unknown | - | The rate limiting check is commented out: "Check rate limiting (simplified - would need execution history in real impl)". This means `maxExecutionsPer | - | - | Implement actual rate limit tracking by querying execution history from `skill_registry` or a dedica |
| 2 | critical | Unknown | - | In `verifySignature()`, when `algorithm === "none"`, the function returns `false` instead of throwing a validation error. However, in `receive()` at l | - | - | When `algorithm === "none"` and the endpoint requires signature verification (algorithm is not expli |
| 3 | critical | Unknown | - | The file uses `import type` to import function identifiers: ```typescript import type { buildDomainsRuntimeCatalog } from "./domains-runtime-catalog.j | - | - | Remove `type` keyword from these imports since they import runtime values. |
| 4 | critical | Unknown | - | The file contains hardcoded HMAC secrets (SECURITY-01 from prior review). This was already documented but no fix has been applied yet. | - | - | Remove hardcoded secrets and use environment variables or the secret management service. |
| 5 | critical | Unknown | src/platform/five-plane-interface/api/ht | When `intakeAdmissionService` is configured (line 298), the handler calls `deps.intakeAdmissionService.admit()` to create the task via the intake pipe | - | - | Remove the direct `insertTask()` call from the intake branch. The intake pipeline should be the sole |
| 6 | critical | Unknown | - | `reserveBudgetForExecution()` reserves with `resourceKind: "token"`: ```typescript const result = budgetAllocator.reserve({ ledger, amount, resourceKi | - | - | Use `resourceKind: "api"` or `"usd"` when the amount represents dollars. Alternatively, keep `resour |
| 7 | critical | Unknown | - | `buildDecisionInputBundle()` computes: ```typescript const remainingCost = budgetEnvelope == null ? 0 : Math.max(0, budgetEnvelope.maxCost - input.ste | - | - | Convert `tokensUsed` to USD using the model's `inputPer1kUsd` pricing before subtracting from `maxCo |
| 8 | critical | Unknown | - | `promoteCanary()` unconditionally sets canary weight to 0 and stable to 100: ```typescript if (target.targetId === "stable") { return { ...target, wei | - | - | Before promotion, verify target slot health scores are appropriate. Consider swapping direction: act |
| 9 | critical | ops-maturity | src/ops-maturity/version-management/semv | `satisfiesTilde()` has a bug in the minor version bound check: `if (version.minor < constraint.minor \|\| version.minor > constraint.minor)` is always | - | - | The logic should be `if (version.minor < constraint.minor \|\| version.minor > constraint.minor)` →  |
| 10 | critical | platform/five-plane- | src/platform/five-plane-execution/recove | `buildCompensationPlan()` retrieves the task ID using `this.store.task.getTask?.(executionId, tenantId)?.id ?? ""`. This is incorrect - `executionId`  | - | - | Change line 518 to: `const task = this.store.task.getTaskByExecution?.(executionId, tenantId) ?? thi |
| 11 | critical | tests/unit | - | `validateAccessToken()` at line 261 returns `SessionValidationResult` with `{ valid, session, reason }`. The `session` object contains `principalId` a | - | - | Add `tenantId: string \| null` to the `Session` interface and populate it during `createSession()`.  |
| 12 | critical | tests/unit | - | Three separate autonomy systems exist with incompatible models: 1. **ProactiveAgentService** (`interaction/proactive-agent/index.ts` line 233-234): -  | - | - | Create a unified `AutonomyLevel` enum used across all three systems. Add an `AutonomyReconciliationS |
| 13 | critical | tests/unit | - | `extractServiceAuth()` at lines 501-537 validates service identity (checks `x-mtls-cert` or `x-service-id` headers) and returns `ServiceAuthResult` wi | - | - | In `TenantScopeFilter.evaluate()`, add a check for service principals: if `principal.tenantId === nu |
| 14 | high | Deep-Cross: http + t | src/interaction/dashboard/dashboard-webs | `assertAuthorized()` for task subscriptions has a logic gap at lines 426-430. The check `if (subscription.channel === "task" && subscription.filterId  | - | - | Change condition to require `allowedTasks.size > 0` to be checked independently of the filter ID pre |
| 15 | high | Deep-Cross: http + t | src/platform/five-plane-interface/api/ht | `ChannelGatewayService` uses the platform `fetch` (global fetch via `this.fetchImpl ?? fetch` at line 90) without configuring an `http.Agent` with con | - | - | Provide a configurable `http.Agent` with `maxSockets`, `maxFreeSockets`, and `scheduledFreeSocketRel |
| 16 | high | Deep-Cross: http + t | src/platform/five-plane-interface/api/ht | `resolveClientIp()` trusts `X-Forwarded-For`, `X-Real-IP`, and `Forwarded` headers without IP address validation. An attacker can spoof these headers  | - | - | Add a configurable trust list for proxies. Only extract IP from untrusted headers when the connectio |
| 17 | high | Unknown | - | `authorizeExecution()` returns `authorized: true` with a default policy when `getExecutionPolicy()` returns null (skill has no stored policy). The com | - | - | Require that all skills have an explicit execution policy before they can be invoked. Return `author |
| 18 | high | Unknown | - | `execute()` accepts any `action` string that matches the switch cases in `buildEndpoint()` and `buildPayload()`. There is no check that the requested  | - | - | Validate the `action` parameter against the adapter's `capabilityIds` before execution. Map each cap |
| 19 | high | Unknown | - | `buildEndpoint()` constructs a URL string from the action and repository without validating the resulting URL format. For `dispatch_workflow` (line 27 | - | - | Validate all path components in `buildEndpoint()` using the same pattern as `requireRepository()`: c |
| 20 | high | Unknown | - | `capabilityIds` are merged from multiple sources (`plugin.capabilityIds`, `builtinManifest.capabilityIds`, `plugin.manifest.capabilityIds`, `manifest. | - | - | Require that each plugin explicitly declares its `capabilityIds` and that the final merged set is a  |
| 21 | high | Unknown | - | In `defaultManifestFor()`, adapters get `allowNetworkEgress: plugin.spiType === "adapter"` (line 63). This means ALL adapters get network egress by de | - | - | Remove the automatic `true` override for adapters. Let the schema default (`false`) apply unless exp |
| 22 | high | Unknown | - | The FORBIDDEN_MCP_OUTPUT_PATTERN check at line 232-234 applies `JSON.stringify()` to `sanitizedData` but searches the string for the literal pattern ` | - | - | Use structured validation instead of regex pattern matching. Define a strict schema for allowed MCP  |
| 23 | high | Unknown | src/platform/five-plane-interface/webhoo | `signedRequestReplayCache` is a `Map<string, { signatureKey: string; idempotencyKey: string }>` with no TTL, no size limit, and no eviction policy. Ea | - | - | Add TTL-based eviction using `Map<string, { signatureKey: string; idempotencyKey: string; expiresAt: |
| 24 | high | Unknown | - | `enforceRateLimit()` is called at line 196, after `createDeliveryMessage()` at line 200. If `enforceRateLimit()` throws `GatewayRateLimitError`, the d | - | - | Move `recordRateLimitHit()` to occur immediately before `deliverResolvedTarget()`, not after success |
| 25 | high | Unknown | - | In `BrowserWSClient`, the `socket.onmessage` handler parses the incoming data as `WSEventEnvelope & { action?: string }` at line 227. The `publish()`  | - | - | Validate that incoming `WSEventEnvelope` messages have a valid `eventId` format (e.g., must match th |
| 26 | high | Unknown | - | When `subscribe()` is called, it sends the channel's `lastEventId` to the server for replay: ```typescript socket.send(JSON.stringify({ action: "subsc | - | - | Same as WH-07 — validate `eventId` before storing. Additionally, add a comment in the code that `las |
| 27 | high | Unknown | - | The `appendToReplayBuffer()` method at line 699 has a loop that tries to drop non-critical frames when the buffer exceeds `maxReplayFrames`. However,  | - | - | Change the search to find the OLDEST droppable event (lowest index) for eviction, which is correct.  |
| 28 | high | Unknown | - | `detectFromChecks()` processes each check and creates incidents using two separate code paths: 1. Lines 70-80: `fail_closed` → SEV1, lines 82-90: `deg | - | - | Use mutually exclusive severity assignment. Once a check's status determines the primary severity, s |
| 29 | high | Unknown | - | `performHealingOperation()` uses a purely mathematical formula: ```typescript const deterministicScore = action.targetComponent.length + action.operat | - | - | Evaluate component health before attempting operation. If `consecutiveFailures >= maxRetries`, refus |
| 30 | high | Unknown | - | `fireAlert()` checks `isRuleCoolingDown()` (line 318) to suppress duplicate alerts within a rule's cooldown window. However: 1. `isRuleCoolingDown()`  | - | - | Implement a correlation key generation scheme that maps related alerts to a single grouping ID. Add  |
| 31 | high | Unknown | - | `isDirectiveExpired()`: ```typescript private isDirectiveExpired(directive: PlatformPanicDirective): boolean { return directive.expiresAt != null && D | - | - | Add NaN check: if `Date.parse(directive.expiresAt)` is `NaN`, log an error and treat the directive a |
| 32 | high | Unknown | - | The pipeline `IncidentDetector → PlatformOpsAgentService → SelfHealingService → IncidentResolver` has a circular escalation risk: 1. `PlatformOpsAgent | - | - | Add a `consecutiveFailures`-based strategy elevation in `IncidentResolver.determineStrategy()`. If ` |
| 33 | high | Unknown | - | `simulateHealthCheck()` uses: ```typescript const healthCheckPassed = !/(rollback\|failover)/.test(operation) \|\| componentId.length % 2 === 0; ``` T | - | - | Replace the parity-based simulation with a proper health evaluation or make the simulation failure r |
| 34 | high | Unknown | - | `canResumeFromPanic()` (line 49) requires: ```typescript plan.checkpointsVerified && (plan.forensicSnapshotReviewed ?? false) && (plan.rollbackPlanRea | - | - | Require `activeIncidents > 0` for non-security reason codes. For security reason codes, allow entry  |
| 35 | high | Unknown | - | `shouldPromoteCanary()` uses a misleading `hasRequiredMetrics` check with OR logic, then treats missing metrics as perfect: ```typescript const hasReq | - | - | Require ALL metrics present before evaluating. Use AND logic. Fail-safe: require explicit metric pre |
| 36 | high | Unknown | - | In `calculateDelay()`, jitter is applied AFTER the final cap at `maxDelayMs`. When `cappedDelay` already equals `maxDelayMs`, jitter has zero room to  | - | - | Apply jitter BEFORE the final cap: `const candidateDelay = Math.floor(cappedDelay + jitter); return  |
| 37 | high | Unknown | - | ```typescript try { await this.redis.quit(); } catch { this.redis.disconnect(); } ``` An empty `catch {}` silently swallows the error. If `quit()` fai | - | - | ```typescript try { await this.redis.quit(); } catch (err) { this.logger.warn("redis.quit_failed", { |
| 38 | high | Unknown | src/sdk/client-sdk/api-client.ts:111 | `performVersionHandshake()` calls `/handshake` endpoint but the API gateway only exposes `/v1/version` and `/v1/handshake` routes (health-routes.ts li | - | - | Ensure the SDK correctly routes the handshake to `/v1/handshake` by verifying `buildApiUrl()` proper |
| 39 | high | Unknown | - | `reserveBudgetForExecution()` converts token budget to USD using a flat rate: ```typescript const amount = context.tokenBudget ? context.tokenBudget * | - | - | Look up `inputPer1kUsd` from `ModelMetadataRegistry` based on the model selected for execution, then |
| 40 | high | Unknown | src/platform/five-plane-state-evidence/t | PostgreSQL migration 12 (`MIGRATION_12_AUTHORITATIVE_ASYNC_PARITY`) adds columns `kind`, `status`, `importance_score`, `freshness_score`, and `content | - | - | Align SQLite migration 15 with PostgreSQL migration 12 to include all columns: `kind TEXT NOT NULL D |
| 41 | high | Unknown | - | When `canary.healthScore == null`, `checkCanaryHealth()` returns `{ healthy: false, reason: "no_health_data" }`. However, `advanceShift()` never calls | - | - | Make `advanceShift()` a no-op when health check fails. Add a health-gated variant `advanceShiftIfHea |
| 42 | high | Unknown | - | ```typescript } catch (error) { logger.log({ level: "warn", message: "Channel gateway retry executor pass failed", ... }); // Return empty summary on  | - | - | Return a result type that distinguishes success from failure: ```typescript type RetryPassResult = \ |
| 43 | high | Unknown | - | `reserve()` checks: ```typescript const tierLimit = context.tierLimit ?? input.ledger.hardCap; const activeCommittedAmount = input.ledger.reservedAmou | - | - | Ensure `tierLimit` and `ledger.hardCap` are always in the same unit. Document that `tierLimit` must  |
| 44 | high | Unknown | - | The code references `@see Event Registry Contract: docs_zh/contracts/event_registry_and_ops_threshold_contract.md` and `@see Event Bus Contract: docs_ | - | - | Create the referenced contract documents. |
| 45 | high | Unknown | - | `evaluateHealthGate()` reads thresholds from `rollout.healthGates`: ```typescript const thresholds = { maxErrorRate: toFiniteThreshold(rollout.healthG | - | - | Define a `HealthGateConfig` interface with explicit typed fields. Validate all values are within exp |
| 46 | high | Unknown | - | `fetchEvents()` wraps its call to `this.eventRepository.listAllEvents()` in a try/catch (lines 431-433, 434-453). When an error occurs, it logs the er | - | - | Instead of returning an empty array, throw a typed error like `ProjectionRebuildFetchError` that pre |
| 47 | high | Unknown | - | `ChannelGatewayService` maintains its own inline circuit breaker state map (`circuitBreakerState = new Map<string, { failureCount: number; openUntil:  | - | - | Replace inline circuit breaker with an instance of `CircuitBreaker` from the stability module, confi |
| 48 | high | Unknown | - | Multiple catch blocks silently swallow errors without any logging: ```typescript } catch { // empty — error is ignored } ``` Compliance evidence colle | - | - | Add `logger.warn` to at minimum record that a collection step failed. |
| 49 | high | Unknown | - | `replayConsumer` calls `resetConsumerReplayState` then `drainConsumer`, but neither call has a timeout. If a consumer is stuck or the drain operation  | - | - | Add a timeout wrapper around `drainConsumer` (e.g., 30 seconds). Return a partial result with `outco |
| 50 | high | Unknown | - | `applyTrafficWeights()` updates two slots independently: ```typescript private applyTrafficWeights(slotA: DeploymentSlot, weightA: number, slotB: Depl | - | - | Add a database constraint or application-level invariant that only one slot per deployment group can |
| 51 | high | Unknown | - | ```typescript if (this.delegationRepository) { this.hydrateFromRepository().catch((err) => { // Log but don't fail startup - in-memory mode still work | - | - | Make the hydration failure fatal at startup: ```typescript if (this.delegationRepository) { try { aw |
| 52 | high | Unknown | - | `normalizeGatewayDeliveryFailure()` treats ALL errors as retryable by default (line 76-77: `return { retryable: true }`). Even `AppError` instances wi | - | - | In the final fallback case, call `deliveryService.isRetryableStatus()` on a extracted status code, o |
| 53 | high | Unknown | - | ```typescript function safeJsonParsePayload(jsonString: string, errorContext: string): Record<string, unknown> { try { const parsed = JSON.parse(jsonS | - | - | Return `null` on validation failure and update callers to check for null: ```typescript function saf |
| 54 | high | Unknown | - | When `budgetControl.ledger` is not provided, a new ledger is created with: ```typescript const ledger = input.budgetControl.ledger ?? createBudgetLedg | - | - | Look up the actual model pricing to compute a more accurate `hardCap`, or set `hardCap` to `estimate |
| 55 | high | Unknown | - | `enforceVersionLimits` at line 248-255 deletes files with `rmSync` but never calls `removeCandidateFromManifests`. Checkpoints deleted by version limi | - | - | Call `removeCandidateFromManifests` for each file deleted by `enforceVersionLimits`, passing a synth |
| 56 | high | Unknown | src/domains/recipes/recipe-executor.ts:1 | The catch block captures the error but does not call `logger.error`: ```typescript } catch (error) { const result = { success: false, ... error: error | - | - | Add `logger.error` with the full error context before returning. |
| 57 | high | Unknown | - | The code reads the ledger via raw SQL: ```typescript const ledgerRow = storage.sql.connection.prepare("SELECT * FROM budget_ledgers WHERE budget_ledge | - | - | Use the正式的 `AuthoritativeTaskStore` or `BudgetRepository` methods to retrieve ledgers, which would r |
| 58 | high | Unknown | - | ```typescript if (stageBudget != null && stageBudget.maxCostUsd > 0) { checks.push({ scope: "stage", projected: next, limit: stageBudget.maxCostUsd }) | - | - | Track `currentStageCostUsd` in `ExecutionChainBudgetSpend` and use `currentStageCostUsd + next` as t |
| 59 | high | Unknown | - | `promoteCanary()` validates `shouldPromoteCanary()` which only checks rolloutPercent and errorRate/latency/successRate thresholds, but never calls `Tr | - | - | Integrate `checkCanaryHealth()` into the promotion gate. Require both `shouldPromoteCanary()` AND `c |
| 60 | high | Unknown | - | Same jitter ordering bug as Issue 1 — jitter applied after final cap, so when delay is already at `maxDelayMs` the jitter is clamped away. Additionall | - | - | Apply jitter before cap and cap the final result: `return Math.min(Math.floor(cappedDelay + jitter), |
| 61 | high | Unknown | - | In `runGC`, the checkpoint file is deleted first (line 171, `rmSync`) and then `removeCandidateFromManifests` is called (line 169). If manifest update | - | - | Reverse the order: update manifests BEFORE deleting files. Wrap the entire operation in a try-cancel |
| 62 | high | Unknown | - | ```typescript function parseOutputs(outputsJson: string): Record<string, unknown> { try { const parsed = JSON.parse(outputsJson) as unknown; return pa | - | - | Either throw a typed error or return a result that indicates failure: ```typescript function parseOu |
| 63 | high | Unknown | - | The `DomainsRuntimeOrchestrator` class calls `buildDependencyServiceIds()` which calls `this.registry.isInitialized()` on dependency service IDs. Howe | - | - | Ensure bootstrap services do not reference the orchestrator during their initialization phase. Consi |
| 64 | high | Unknown | - | Three catch blocks in the commit phase silently swallow errors without logging. The actual error message is lost, making post-incident forensics impos | - | - | Log the caught error with `logger.error` and include it in the execution log entry. |
| 65 | high | Unknown | src/domains/recipes/recipe-executor.ts:1 | The catch block captures the error but does not call `logger.error`. Recipe execution failures are completely invisible to the observability stack. | - | - | Add `logger.error` before returning. |
| 66 | high | Unknown | - | `transition()` validates lifecycle state change but the service holds agent state in private Maps. The registry can diverge from what the service trac | - | - | Use the canonical agent-registry module as source of truth. |
| 67 | high | Unknown | - | `advanceCanary()` evaluates `shouldPromoteCanary()` but never checks the deployment slot state from `TrafficRoutingService`. The `canaryProgress` Map  | - | - | Add a slot state consistency check in `advanceCanary()`. Query `TrafficRoutingService.getActiveSlot( |
| 68 | high | Unknown | - | In `executeWithStats()`, the elapsed time check is: `const elapsed = startTime - Date.now();` — the subtraction is reversed. This produces a negative  | - | - | Change to `const elapsed = Date.now() - startTime;` |
| 69 | high | Unknown | - | `worker-load-balancing.ts` `computeWorkerLoadScore()` (line 75) and `execution-dispatch-support.ts` `computeLoadScore()` (line 43) use different algor | - | - | Create a single canonical `computeLoadScore()` utility with documented weighting that is shared acro |
| 70 | high | Unknown | - | ```typescript } catch (error) { return { success: false, errors: [{ message: error instanceof Error ? error.message : "Unknown error", extensions: { c | - | - | ```typescript } catch (error) { // Log full error for debugging but return sanitized message to clie |
| 71 | high | Unknown | src/sdk/client-sdk/api-client.ts:388 | `wrapRequestBody()` wraps all SDK request bodies in a `ContractEnvelope` per the five-plane boundary contract §5.5. However, the API gateway's task ro | - | - | Either (a) make the gateway routes unwrap ContractEnvelope before parsing payloads, or (b) add a mid |
| 72 | high | Unknown | - | `normalizeWorkflowStepCheckpoint()` returns `null` when the input cannot be normalized to a valid `WorkflowStepCheckpoint` (lines 557-559). However, ` | - | - | Log a warning when normalization fails due to schema version mismatch. Add a specific error type `Ch |
| 73 | high | Unknown | - | Three separate catch blocks in the commit phase all silently swallow errors: ```typescript } catch { failedStepId = step.stepId; executionLog.push({ s | - | - | Log the caught error with `logger.error` and include it in the execution log entry so rollback has c |
| 74 | high | Unknown | - | `shouldPromoteCanary()` uses fallback values when metrics are missing: ```typescript successRate ??= 1.0 errorRate ??= 0.0 ``` A canary with no report | - | - | Require all metrics to be present before evaluating promotion. |
| 75 | high | Unknown | - | `shouldEnterPanicMode()` returns `true` for any `reasonCode` starting with `"security."` regardless of scope or severity. | - | - | Differentiate between security advisory prefixes and actual security incidents. |
| 76 | high | Unknown | - | Multiple code files reference observability contracts that do not exist: - `debug_inspect_health_backpressure_contract.md` - `runtime_execution_contra | - | - | Create an ADR for the observability architecture. |
| 77 | high | Unknown | - | All 7 entity Maps grow without bound over the lifetime of the service. | - | - | Add LRU eviction or TTL-based cleanup. |
| 78 | high | Unknown | - | `updateHealth()` passes `healthScore` directly to SQL without bounds validation: ```typescript updateHealth(slotId: string, healthScore: number): void | - | - | Validate `healthScore` is in `[0, 100]` range before writing. Clamp or reject out-of-range values. |
| 79 | high | Unknown | - | `scanForGCCandidates` and `runGC` are separate operations with no transactional or locking mechanism. Concurrent GC runs on the same checkpoint direct | - | - | Add a file-based or database-backed lock around the GC run. Use the manifest file itself as a coordi |
| 80 | high | Unknown | src/org-governance/approval-routing/appr | Approval route audit record ID is constructed as `` `approval_route_audit_${request.requesterId}_${base.matchedOrgNodeId}_${Date.now()}_${randomUUID() | - | - | Use `` newId("approval_route_audit") ``. Store `requesterId` and `matchedOrgNodeId` as fields in the |
| 81 | high | Unknown | - | `updateTargetWeight()` sets target weight directly without range checking: ```typescript public updateTargetWeight(routeId: string, targetId: string,  | - | - | Add bounds validation `weight = Math.max(0, Math.min(100, weight))` in `updateTargetWeight()`. |
| 82 | high | Unknown | - | Multiple catch blocks throughout the plugin SPI registry do not log errors: ```typescript } catch (error) { // no logging } ``` This means plugin load | - | - | Add `logger.error` calls to all catch blocks with the error context and correlation IDs. |
| 83 | high | Unknown | - | Multiple catch blocks silently swallow errors from delivery tracking operations: Line 393: ```typescript } catch (error) { this.logger.log({ level: "w | - | - | Return a status from tracking methods that indicates partial failure, or introduce a composite resul |
| 84 | high | Unknown | src/sdk/client-sdk/api-client.ts:72 | `buildAuthHeaders()` sends `X-SDK-Version`, `X-Platform-Version`, and `X-Contract-Version` headers to the API gateway. However, the gateway's version  | - | - | Wire `SdkVersionHandshakeService` into the request pipeline for versioned routes, or remove the X- h |
| 85 | high | Unknown | - | `advanceShift()` only checks step count, not health: ```typescript if (currentStep >= steps.length) { // Complete the shift this.applyTrafficWeights(. | - | - | Call `checkCanaryHealth()` before each step advancement. Block advancement if `healthy === false`. |
| 86 | high | Unknown | - | `MigrationRunner.up()` (line 36-38) calls `this.storage.migrate()` and returns `buildResult("up")`. The `storage.migrate()` method signature in `Sqlit | - | - | Change `MigrationRunner.up()` to be `async` and properly `await this.storage.migrate()` when the bac |
| 87 | high | Unknown | - | Both `CircuitBreaker.execute()` methods accept `fn: () => Promise<T>` without an `AbortSignal` parameter. Compare with `platform/model-gateway/provide | - | - | Add optional AbortSignal parameter to `CircuitBreaker.execute()` and wire it into the timeout/abort  |
| 88 | high | Unknown | - | `evaluateWorkersForTicket()` at lines 579-588 uses a hardcoded `heartbeatStalenessThresholdMs = 30_000` (30 seconds). This is different from `HealthSe | - | - | Consolidate to a single `WORKER_HEARTBEAT_STALENESS_MS` constant in a shared location and have all m |
| 89 | high | Unknown | src/sdk/cli/dispatch-execution.ts:47 | The dispatch-execution CLI creates tickets via `dispatch.createTicket()` and optionally calls `dispatch.dispatchNext()`. This is the authoritative dis | - | - | Add `createExecutionTicket()` and `dispatchExecution()` methods to `RetryableApiClient`, or create a |
| 90 | high | Unknown | src/platform/five-plane-state-evidence/a | `ArtifactBundleService.build()` at line 23 uses `artifact.path` as the fallback for `finalDeliverables`: `finalDeliverables: [...(input.finalDeliverab | - | - | Change `ArtifactBundleService.build()` line 23 to use `artifact.storagePath ?? artifact.path` so it  |
| 91 | high | Unknown | - | Multiple catch blocks silently swallow errors: ```typescript } catch { // empty — error is ignored } ``` | - | - | Log errors at minimum with `logger.warn` so compliance evidence collection failures are traceable. |
| 92 | high | Unknown | - | The ADR states OAPEFLIR is "not an active orchestration loop" and "used only for reasoning records and audit traceability". But the actual code in `oa | - | - | Update ADR-016 to accurately reflect that OAPEFLIR is an active orchestration loop, or refactor the  |
| 93 | high | interaction | src/interaction/nl-gateway/disambiguatio | `DEFAULT_DISAMBIGUATION_CONFIG` uses `threshold: 0.7` and `lowConfidenceThreshold: 0.5`, but `nl-gateway-config-loader.ts` line 156 sets the clarifica | - | - | Align `DEFAULT_DISAMBIGUATION_CONFIG.threshold` with the NL gateway default of 0.80, or make the dis |
| 94 | high | interaction | src/interaction/goal-decomposer/index.ts | The anti-multiplication guard `decomposedGoalIds` is an in-memory Set. If `GoalDecompositionService` is instantiated multiple times (e.g., in separate | - | - | Make the anti-multiplication guard shared (e.g., via a static Map or external registry), or document |
| 95 | high | ops-maturity | src/ops-maturity/agent-lifecycle/agent-l | `transition()` validates the lifecycle state change via `isValidLifecycleTransition` but the service holds agent state in private Maps. The registry ` | - | - | Have `AgentLifecycleService` use the canonical `agent-registry` module as the source of truth for al |
| 96 | high | ops-maturity | src/ops-maturity/platform-ops-agent/self | `simulateHealthCheck()` uses a regex test `/(rollback\|failover)/.test(operation)` combined with `componentId.length % 2 === 0` as the sole determinis | - | - | Replace the deterministic-but-opaque length-parity check with a proper health status evaluation base |
| 97 | high | ops-maturity | src/ops-maturity/platform-ops-agent/self | `performHealingOperation()` uses a formula `deterministicScore % (action.operation === "failover" ? 5 : 4) !== 0` to determine success. This produces  | - | - | Replace with a proper outcome model or make the success rate configurable via policy. Document the e |
| 98 | high | ops-maturity | src/ops-maturity/workflow-debugger/workf | `evaluateTrace()` finds a matching breakpoint via `find()` without checking if the find succeeded. The `!` assertion on `matched` could be undefined i | - | - | Add a null check on `matched` and log a warning or skip the frame if no matching breakpoint is found |
| 99 | high | ops-maturity | src/ops-maturity/agent-lifecycle/canary- | `shouldPromoteCanary()` uses a fallback that treats missing metrics as "perfect" (`successRate ??= 1.0`, `errorRate ??= 0.0`). This is dangerous becau | - | - | Require that all metrics are present before evaluating promotion. Only apply fallback defaults when  |
| 100 | high | ops-maturity | src/ops-maturity/edge-runtime/edge-runti | `resolveConflict()` returns a `merge` resolution for low-risk conflicts but then calls `performThreeWayMerge()` which produces a hash digest when actu | - | - | Implement actual payload merge logic or document clearly that `mergedPayload` is a hash reference ra |
| 101 | high | ops-maturity | src/ops-maturity/drift-detection/learnin | `rollback()` calls the `RollbackHandler` but does not handle the case where the handler throws. If `handler(proposalId, reason)` rejects, the state is | - | - | Wrap the handler call in try/catch. If the handler throws, either retry with backoff, mark the rollo |
| 102 | high | ops-maturity | src/ops-maturity/chaos/chaos-experiment- | The R21-18 fix tracks `evaluatedHypotheses` per `experimentId:hypothesisName`. However, `evaluatedHypotheses` is never cleared when an experiment ends | - | - | Clear `evaluatedHypotheses` entries for a given experiment when `clearSteadyStateCache()` is called  |
| 103 | high | ops-maturity | src/ops-maturity/emergency/platform-pani | `isDirectiveExpired()` compares `directive.expiresAt` against `Date.now()`. If `expiresAt` is malformed or an empty string, `Date.parse()` returns `Na | - | - | Add a guard: if `directive.expiresAt` is null/undefined, return false. Otherwise parse and check for |
| 104 | high | ops-maturity | src/ops-maturity/emergency/panic-control | `shouldEnterPanicMode()` returns `true` for any `reasonCode` starting with `"security."` regardless of the scope or severity. A low-severity security  | - | - | Differentiate between security advisory prefixes and actual security incidents. Require `activeIncid |
| 105 | high | platform/five-plane- | src/platform/five-plane-execution/execut | Budget reservation path has a conditional: if `ledgerRow` is found, it reserves budget and updates the ledger. However, if `ledgerRow` is undefined (e | - | - | If `ledgerRow` is null/undefined, either create the ledger on demand or throw a `ValidationError` wi |
| 106 | high | platform/five-plane- | src/platform/five-plane-execution/budget | In `reserve()`, after creating the result and updating ledgers, the code calls `persistLedger()` for both `input.ledger` and each hierarchy ledger. Ho | - | - | After calling `reserveBudgetHardCap()`, use `result.ledger.version` as the expected version for the  |
| 107 | high | platform/five-plane- | src/platform/five-plane-execution/side-e | `applyReconciliation()` checks for lease and fencing token only when targetStatus is one of `["confirmed", "compensation_required", "compensating", "c | - | - | Verify the fencing token check applies regardless of the side effect's current status. Consider addi |
| 108 | high | platform/five-plane- | src/platform/five-plane-execution/reconc | `isReconciliationExpired()` computes `now - createdAt >= window` using `Date.now()` for `now`. However, `reconciliationCreatedAt` is parsed via `new D | - | - | Add validation: if `isNaN(createdAtMs)`, return `true` (expired/unparseable) or throw an error with  |
| 109 | high | platform/five-plane- | src/platform/five-plane-execution/execut | `transitionTaskTerminalState()` is called with `lastExecutionId` which is derived from `store.execution.listExecutionsByTask(taskId).at(-1)`. If no ex | - | - | Before calling `transitionTaskTerminalState()`, verify that at least one execution record exists. If |
| 110 | high | platform/five-plane- | src/platform/five-plane-execution/dispat | The lease acquisition and ticket claim are within a `db.transaction()` block, which is correct for atomicity. However, the code calls `leaseResult = t | - | - | Verify that `acquireLeaseWithinTransaction()` is designed to be called within an already-established |
| 111 | high | platform/five-plane- | src/platform/five-plane-execution/state- | `TaskTransitionService.apply()` performs a CAS update using `updateTaskStatusCas()` which returns `affected === 0` on failure. If the transition fails | - | - | In `TaskTransitionService.apply()`, wrap the CAS update and event emission in `this.db.transaction() |
| 112 | high | platform/five-plane- | src/platform/five-plane-execution/worker | The catch block at line 579 catches all errors and maps them to either `"invalid_terminal_transition"` (if message contains "invalid_transition") or ` | - | - | Log the actual error with its stack trace before mapping. Distinguish between state machine errors ( |
| 113 | high | platform/five-plane- | src/platform/five-plane-execution/dispat | The poison pill handling path calls `shouldInvalidatePoisonPill()` and then `invalidatePoisonPillTicket()`. However, between these calls, there is no  | - | - | Within `invalidatePoisonPillTicket()`, verify the ticket is still in a state that can be invalidated |
| 114 | high | platform/five-plane- | src/platform/five-plane-execution/lease/ | The fence token initialization at line 173 uses `getLatestFencingToken(input.executionId) + 1`. If `getLatestFencingToken()` returns 0 (when no prior  | - | - | Verify that `getLatestFencingToken` is implemented in all worker store implementations. If the metho |
| 115 | high | plugins | src/plugins/validators/basic-evaluator.t | The `evaluate()` and `produceHarnessDecision()` methods both call `evaluateWithLegacyScoring()` and return `EvaluatorAssessment`. However, the `Domain | - | - | Ensure `evaluate()` returns an object conforming to `ValidationResult`, or rename the method to avoi |
| 116 | high | plugins | src/plugins/adapters/crm-adapter.ts:77 | `healthCheck()` is declared `async` and returns `Promise<boolean>`. However, the body contains `policy.evaluate(...).allowed` without `await`. Since ` | - | - | Change line 78 to `return await policy.evaluate(...).allowed;` |
| 117 | high | plugins | src/plugins/adapters/game-dev-adapter.ts | `healthCheck()` returns `true` when `credentialFingerprint == null`. This means the Unity adapter reports as "healthy" even when it has not been authe | - | - | Change the logic so that if `credentialFingerprint == null`, return `false`. The correct pattern is: |
| 118 | high | plugins | src/plugins/adapters/asset-production-ad | `healthCheck()` only validates policy for `api.figma.com`, but the adapter's `execute()` method (line 53) routes to `cdn.figma.com` for `cdn_*` action | - | - | Add `policy.evaluate("https://cdn.figma.com").allowed` to the health check, or extract a helper that |
| 119 | high | plugins | src/plugins/adapters/livestream-adapter. | `healthCheck()` is async but the body at line 35-36 accesses `policy.evaluate(...).allowed` without `await`. The `evaluate()` method is async and retu | - | - | Add `await` before each `policy.evaluate()` call. |
| 120 | high | plugins | src/plugins/builtin-plugin-registry.ts:9 | `plugin.growth.crm_adapter` is registered in `BUILTIN_PLUGIN_FACTORIES` (line 109) with factory `createCrmAdapterPlugin`. However, `plugin.growth.crm_ | - | - | Add a `BUILTIN_PLUGIN_MANIFESTS` entry for `plugin.growth.crm_adapter` similar to the pattern used f |
| 121 | high | plugins | src/plugins/growth-config.ts:201 | `externalAdapters: ["github", "jira"]` references `"jira"` as an external adapter, but there is no Jira adapter in the adapter registry. The `builtin- | - | - | Either implement a Jira adapter or remove `"jira"` from the `externalAdapters` list. Document the in |
| 122 | high | tests/unit | - | The `RetryableApiClient` in `client-sdk` wraps all request bodies in a `ContractEnvelope` (line 405: `wrapRequestBody()`) and unwraps response envelop | - | - | Align the UI REST client to detect and unwrap `ContractEnvelope` responses, or document that the UI  |
| 123 | high | tests/unit | - | The UI `HttpTransport.resolveRequestUrl()` (rest-client.ts lines 306-313) concatenates `baseUrl` with `path` directly, with no API version prefix hand | - | - | Ensure the baseUrl + apiVersion configuration is consistent across UI and SDK. Document the expected |
| 124 | high | tests/unit | - | The UI `AuthStore` encodes principal as `userId`, `tenantId`, `roles`, `permissions` on the `AuthSession` interface (auth-store.ts lines 14-17). The S | - | - | Normalize the principal field names. The UI should either use `subject`/`principalId` naming convent |
| 125 | high | tests/unit | - | The UI `createIdempotencyKeyInterceptor()` (interceptors.ts line 223) generates a `crypto.randomUUID()` idempotency key and sets it on both `Idempoten | - | - | Ensure idempotency key handling is consistent — either both use HTTP headers or both use the envelop |
| 126 | high | tests/unit | - | The UI REST client defines `RestHttpError` (rest-client.ts lines 62-81) with `status`, `uiAction` (redirect_to_login, access_denied, backoff_and_retry | - | - | Align the error classification between UI and SDK. Consider a shared error classification utility, o |
| 127 | high | tests/unit | - | Two independent fencing token mechanisms exist with no coordination: 1. `FencingTokenService` (multi-region) manages region-level leadership with `epo | - | - | Add a `validateFencingToken()` call in `ExecutionLeaseService.validateWriteAccess()` that checks the |
| 128 | high | tests/unit | - | Pack lifecycle stages are: `draft → certifying → published → deprecated → archived`. Domain lifecycle stages are: `draft → validated → registered → ca | - | - | Add `canary` stage to pack lifecycle to match domain lifecycle, or establish a formal mapping/adapte |
| 129 | high | tests/unit | - | The `inject()` method at lines 325-338 uses rate limit key `inject:${endpoint}`, while `handleRequest()` at lines 415-427 uses `${clientIp}:${endpoint | - | - | Normalize rate limit keys to include both `tenantId` (or tenant scope) and endpoint, regardless of e |
| 130 | high | tests/unit | - | The `PromptBundle` contract defines `displayVersion: string` as a required field (line 32 of prompt-bundle/index.ts). The `createMockBundle()` helper  | - | - | Change assertions at lines 407 and 410 to check for the actual string value: ```typescript assert.eq |
| 131 | high | tests/unit | - | Two independent intent classification systems exist: 1. `nl-gateway-support.ts` `classifyRisk()` → produces `riskClassification` with `intentType` (ta | - | - | Create a canonical `IntentClassification` interface in a shared contracts location, with explicit ma |
| 132 | high | tests/unit | - | `registerBreakpoint()` stores breakpoints by `planGraphId` (line 74-75), but the execution engine's `multi-step-supervisor.ts` does not query the debu | - | - | Add a pre-execution breakpoint check in `executeStepLoop` before each step runs. The debugger should |
| 133 | high | tests/unit | - | `ExecutionDispatchService` supports emergency lane preemption (lines 348-388) via `ExecutionPriorityPreemptionService.preemptForUrgentTicket()`. Howev | - | - | After `preemptForUrgentTicket()` succeeds, call `ExecutionLeaseService.releaseLease()` for the preem |
| 134 | high | tests/unit | - | Domain `gray_rollout` is the final onboarding phase before full activation. The pack lifecycle equivalent should be `published`. However: 1. `DomainOn | - | - | When domain onboarding completes `gray_rollout` and activates the domain, also transition the associ |
| 135 | high | tests/unit | - | `resolveFencingTokenStoragePath()` (line 337) defaults to `join(process.cwd(), "data", "multi-region", "fencing-token-state.json")`. `ExecutionLeaseSe | - | - | Move `FencingTokenService` persistence to the same `AuthoritativeSqlDatabase` that `ExecutionLeaseSe |
| 136 | high | tests/unit | - | The `HarnessRun` type has `goal?: string` and `mode?: string` (contract-models.ts lines 287-288), plus `orgId` (line 285, defaults to tenantId in fact | - | - | Add missing fields to `HarnessRunSchema` as optional/required as appropriate. This is a significant  |
| 137 | high | tests/unit | - | The R4-28 cost event WAL records hardcoded values: ```typescript provider: "minimax", model: "MiniMax-M2.7", inputTokens: 30 + index * 10, outputToken | - | - | Either: 1. Remove the WAL until real measurement is implemented (per earlier recommendation), or 2.  |
| 138 | high | tests/unit | - | `FairSchedulingService.schedule()` returns `victimExecutionId` from `choosePreemptionVictim()` (line 76) when quota is exceeded. However, the preempti | - | - | In `FairSchedulingService.schedule()`, before returning a `victimExecutionId`, call `ExecutionLeaseS |
| 139 | high | tests/unit | - | Domain onboarding phase sequence is: `domain_modeling → pack_development → security_certification → gray_rollout`. The `pack_development` phase implie | - | - | Add cross-service validation in `DomainOnboardingService.advance()` when transitioning to `security_ |
| 140 | high | tests/unit | - | `evaluateTrace()` finds a matching breakpoint: ```typescript const matched = (this.breakpoints.get(planGraphId) ?? []).find((item) => item.nodeRunSele | - | - | Add null check: ```typescript const matched = (this.breakpoints.get(planGraphId) ?? []).find((item)  |
| 141 | high | tests/unit | - | `ExecutionLeaseService.acquireLeaseWithinTransaction()` grants leases without any fencing token validation. There is no call to `FencingTokenService.v | - | - | Add an optional `fencingToken` parameter to `acquireLease()` and validate it against `FencingTokenSe |
| 142 | high | tests/unit | - | The `security_certification` onboarding phase and the `certifying` pack lifecycle stage have the same semantic purpose (security/quality review before | - | - | Establish a formal certification gate that coordinates both: - Domain enters `security_certification |
| 143 | high | tests/unit | - | The `normalizeVersion()` function in the prompt-bundle contract (line 332-338) converts semver to integer via `major * 100 + minor * 10 + patch`, e.g. | - | - | Align the version normalization logic. The contract's `normalizeVersion()` at line 345 should be rev |
| 144 | high | tests/unit | - | ProactiveAgentService can fire triggers that result in `actionType: "create_task"` (line 44), which calls `enqueueSuggestion()` and creates a suggesti | - | - | Add a `activeTaskRef` field to `TriggerRuntimeState` that records the taskId when a trigger fire res |
| 145 | high | tests/unit | - | `simulateHealthCheck()` (lines 51-71) uses: ```typescript const healthCheckPassed = !/(rollback\|failover)/.test(operation) \|\| componentId.length %  | - | - | Replace the deterministic-but-opaque length-parity check with a proper health status evaluation. If  |
| 146 | high | tests/unit | - | `RegionFailoverController.resolve()` (line 292) computes the failover decision including `reconciliationResult` but `ExecutionLeaseService.reclaimExpi | - | - | After failover, the `ReconciliationJobInput` should include a step to call `ExecutionLeaseService.re |
| 147 | high | tests/unit | - | `validateSandboxCompatibility()` checks that restricted tools require `securityLevel=restricted` in domain capabilities. However: 1. Pack manifest has | - | - | Add a cross-module sandbox tier / security level compatibility check in `PackDomainAssociationServic |
| 148 | high | tests/unit | - | `HttpApiServer.dispatchRequest()` calls `authenticateOptionalPrincipal()` at line 588 which uses the `authService` (likely session-based user auth). S | - | - | In `HttpApiServer.dispatchRequest()`, check for service auth headers (`x-service-id`, `x-mtls-cert`) |
| 149 | high | tests/unit | - | `GoalDecompositionService` produces a task graph (via `decompose()`) with `taskIds`, `dependencies`, and `executionOrder`. However, OAPEFLIR's plan st | - | - | Add `goalDecomposition?: GoalDecompositionResult` field to `OapeflirLoopInput`. In the plan stage, i |
| 150 | high | tests/unit | - | `authenticateOptionalPrincipal()` at line 588 calls into the auth service which internally calls `validateAccessToken()`. The `validateAccessToken()`  | - | - | Ensure the `Session` object (or at minimum the `tenantId` from the session) is propagated from `auth |
| 151 | high | tests/unit | - | The contract at line 191-196 explicitly rejects a bundle where `deprecated === true` AND `lifecycleStatus === "active"`: ```typescript if (bundle.meta | - | - | Add test: ```typescript test("createPromptBundle throws when deprecated=true but lifecycleStatus=act |
| 152 | high | tests/unit | - | The existing review identified that `PlatformPanicService.evaluateExecution()` bypasses blocking for actors in the `allowList`. The `maybeInjectWorkfl | - | - | Ensure panic mode enforcement is consistent regardless of actor identity. The allowList bypass shoul |
| 153 | high | tests/unit | - | Multiple confidence threshold constants exist: 1. `intake-router-model.ts` line 55: `CONFIDENCE_THRESHOLD = 0.80` for LLM intent extraction 2. `nl-gat | - | - | Create a shared constants module (e.g., `src/platform/contracts/constants/intake-thresholds.ts`) tha |
| 154 | high | tests/unit | - | `execute()` can trigger a healing operation (`performHealingOperation`) while the execution engine is actively running steps in `executeStepLoop`. The | - | - | Add a coordination mechanism: self-healing service should check with the execution engine before per |
| 155 | high | tests/unit | - | `TenantScopeFilter.evaluate()` requires a `TaskProjectionScopeResolver` function (passed to constructor at line 25) to look up task tenant scope. The  | - | - | Either: 1. Integrate `TenantScopeFilter` into `HttpApiServer.dispatchRequest()` by injecting it and  |
| 156 | high | tests/unit | - | `RuntimeRecoveryService` returns `suggestedAction: "resume_same_worker"` (line 761-764) for executions with `status === "executing"` that are below at | - | - | Add `workerRegionId` to `RuntimeRecoveryCandidate`. In `inferSuggestedAction()`, if the action is `r |
| 157 | medium | Deep-Cross: http + t | src/platform/five-plane-interface/channe | `extractConnectionParams()` parses `taskId` and `lastEventId` from the URL query string without sanitization. While authentication is correctly taken  | - | - | Validate `taskId` format using the same pattern used for internal task ID validation (ULID or simila |
| 158 | medium | Deep-Cross: http + t | src/platform/five-plane-interface/api/ht | `keepAliveTimeout = SERVER_KEEP_ALIVE_TIMEOUT_MS` (5000ms) at line 210 and `headersTimeout = SERVER_HEADERS_TIMEOUT_MS` (60000ms) at line 209 are set. | - | - | Track in-flight request count and wait for their completion before force-destroying. Consider using  |
| 159 | medium | Deep-Cross: http + t | src/platform/five-plane-interface/channe | `fetchWithTimeout()` sets an AbortController timeout and passes `signal` to fetch, but there is no guarantee that the underlying connection will be ab | - | - | Use a more aggressive timeout mechanism such as `setTimeout` with immediate abort, and configure the |
| 160 | medium | Deep-Cross: http + t | src/platform/five-plane-interface/ingres | The `DistributedRateLimiter` constructor throws `Error("rate_limiter.redis_required_in_production")` when Redis is not configured and `NODE_ENV === "p | - | - | Make the fallback behavior explicit in environment configuration. Require a specific env var like `A |
| 161 | medium | Deep-Cross: http + t | src/platform/five-plane-interface/api/ht | `normalizeHeaders()` lowercases all header names, but does not validate or strip duplicate headers that could arise from HTTP Request Smuggling attack | - | - | Add duplicate header detection: if a header name appears twice after lowercasing, reject the request |
| 162 | medium | Deep-Cross: http + t | src/platform/five-plane-interface/channe | `sendTaskUpdate()` drops events for slow consumers without notifying the client. The `pendingAcks` map grows indefinitely if acknowledgements are lost | - | - | Implement a maximum pending ACK count per client. When exceeded, send a `stream_gap` message to forc |
| 163 | medium | Deep-Cross: http + t | src/platform/five-plane-interface/channe | `taskEventHistory` is a `Map<string, Array<{eventId, event}>>` with per-task history capped at `maxTaskEventHistoryPerTask` (default 200). The evictio | - | - | Use a ring buffer or indexed array for O(1) eviction instead of `shift()`. Consider adding a `taskEv |
| 164 | medium | Deep-Cross: http + t | src/platform/five-plane-interface/api/ta | `pollIntervalMs` defaults to 1000ms (1 second). The relay unconditionally polls `store.event.listEventsByType("task:status_changed", this.backlogLimit | - | - | Add a subscriber count check before polling. If `getConnectedClients() === 0`, skip the poll or incr |
| 165 | medium | Deep-Cross: http + t | src/platform/five-plane-interface/channe | `selectProtocol()` accepts any non-empty protocol string from the client without validating it against the expected authentication protocol format. Th | - | - | Maintain a `Set<string>` of supported protocols and validate the client's offered protocols against  |
| 166 | medium | Unknown | - | `validateAllowedTools()` validates that resolved steps are allowed by the runtime allowlist, but `validateSkillDefinition()` at line 97 only checks MC | - | - | Add a pre-execution check that resolves each tool name in `requiredTools` against the tool metadata  |
| 167 | medium | Unknown | - | `parseMcpToolName()` requires at least 3 segments (`mcp_<server>_<tool>`), but `isMcpToolName()` also returns true for the bare string `"mcp"` (line 1 | - | - | Reject `"mcp"` as a tool name explicitly. Require that MCP tools always use the `mcp_<server>_<tool> |
| 168 | medium | Unknown | - | `credentialFingerprint` is derived from the token but the actual token is stored in plain text (`credentialToken = token` at line 218) in a module-lev | - | - | Consider using a credential wrapper that clears memory after use, or store only the fingerprint and  |
| 169 | medium | Unknown | - | When `parseStoredStringArray()` returns `status: "invalid"`, `resolveExecutionAllowedTools()` returns `allowedTools: undefined` (line 148-149). Caller | - | - | Return `allowedTools: []` (fail closed) when stored data is invalid, not `undefined`. Log an error f |
| 170 | medium | Unknown | - | The `planJson` serialized for caching includes `skillId`, `version`, `requiredTools`, `modelProfileName`, `steps`, `cacheable`, and `cacheTtlSeconds`. | - | - | Include `riskLevel` in the cache key. Consider whether `requiredPermissions` should also be included |
| 171 | medium | Unknown | - | `authenticate()` validates token format with regex `/^[A-Za-z0-9+/=_-]{16,}$/` and then stores a fingerprint created by slicing the first 8 characters | - | - | Use only cryptographic derivatives (SHA256 hash output) for fingerprints, never a direct slice of th |
| 172 | medium | Unknown | - | `assertNamespaceAllowed()` at line 697 returns early if `namespace == null`. This means a null namespace is treated as allowed, but the logic at line  | - | - | Distinguish between "null namespace means no restriction" and "empty allowlist means no namespace ac |
| 173 | medium | Unknown | - | The idempotency key is derived from request headers, payload fields, or generated as null — but never validated for format, length, or character set.  | - | - | Validate idempotency key length (max 256 chars) and character set (alphanumeric + hyphen/underscore) |
| 174 | medium | Unknown | - | `rollbackAcceptedEnvelope()` performs a read-then-delete under separate operations. Between the `get()` at line 163 and the `delete()` at line 167, an | - | - | Use an atomic compare-and-swap operation, or use a `Map` with built-in atomic update methods (e.g.,  |
| 175 | medium | Unknown | - | In `handleReconnect()`, when a reconnection is triggered: ```typescript if (this.fallbackClient != null) { this.fallbackClient.connect(url, token); // | - | - | Call `this.fallbackClient?.disconnect()` before `this.fallbackClient?.connect()` in `handleReconnect |
| 176 | medium | Unknown | - | `subscribeToTask()` checks tenant scope via `this.tenantScopeFilter?.evaluate()` at line 312, and checks subscription limits. However, it does not ver | - | - | Add a task existence validation in `subscribeToTask()`. If the task does not exist, return a specifi |
| 177 | medium | Unknown | - | `unregisterClient()` deletes `clientCursors` and `clientBackpressureState` for the given clientId. However, if `clientId` is reused (common in many sy | - | - | Validate `clientId` format in `registerClient()`. Reject clients with sequential or enumerable IDs.  |
| 178 | medium | Unknown | - | `isCoolingDown()` uses: ```typescript private getCooldownKey(itemId: string, delivery: NotificationDeliveryType): string { return `${itemId}:${deliver | - | - | Always apply cooldown regardless of which method is being queried. Move the cooldown state update to |
| 179 | medium | Unknown | - | Five detection rules are evaluated independently against the same metrics: 1. `sev1_availability_collapse` — fires if `availability < 95` OR `error_ra | - | - | Implement rule priority ordering. After the highest-severity matching rule fires, suppress lower-sev |
| 180 | medium | Unknown | - | `shouldEmitAlert()` uses key `${scope}:${scopeId}:${alertLevel}`. This means: - A `cost.exceeded` at `platform` scope with scopeId `main` and `exceede | - | - | Add a correlation ID that groups related alerts (exceeded + critical + warning for the same underlyi |
| 181 | medium | Unknown | - | The cooldown check (lines 108-109) only applies when `consecutiveFailures >= maxRetries`. If the component is in `degraded` state (failure count below | - | - | Apply a proportional cooldown based on `consecutiveFailures`: `cooldownMs * (1 + consecutiveFailures |
| 182 | medium | Unknown | - | `determineStrategy()` uses only `incident.category` and `incident.symptoms.length` to choose between `manual`, `assisted`, `self_heal`, and `automated | - | - | Accept `healingHistory` and `currentHealthStatus` as parameters to `determineStrategy()`. If recent  |
| 183 | medium | Unknown | src/platform/five-plane-interface/api/ht | `readRequestId()` generates IDs as `` `req_${Date.now().toString(36)}_${randomUUID()}` `` — combining a Unix timestamp with a UUID. While the UUID por | - | - | Use `` `req_${randomUUID()}` `` or adopt the canonical `newId("req")` function. |
| 184 | medium | Unknown | - | `evaluateCheckpointForGC()` only returns candidates that are age-expired. It never detects orphaned checkpoints — those whose `executionId` no longer  | - | - | Add a method to cross-reference checkpoint executionIds against the task store. Flag candidates as o |
| 185 | medium | Unknown | - | `DEFAULT_DELIVERY_CONFIG.timeoutMs = 30000` (30 seconds) but the `ChannelGatewayService` in `channel-gateway-service.ts` line 91-97 clamps `requestTim | - | - | Use a single canonical timeout configuration. Either (a) have `ChannelGatewayService` accept the del |
| 186 | medium | Unknown | src/domains/registry/plugin-runtime-host | The `readStdout()` loop silently swallows JSON parse failures: ```typescript try { this.handleMessage(JSON.parse(line)); } catch { this.stderrBuffer = | - | - | Add `logger.debug` or `logger.warn` inside the catch block to record that a non-protocol line was re |
| 187 | medium | Unknown | src/platform/five-plane-execution/tool-e | Snapshot ID is `` `shadow-${Date.now()}-${suffix}` `` where suffix is a bare UUID. Uses hyphen separator and embeds timestamp. This is a third differe | - | - | Replace with `` newId("shadow_snapshot") ``. |
| 188 | medium | Unknown | - | In `startCall()`, `remainingMs = Math.max(0, deadlineAt - Date.now())` is calculated but then `setTimeout` is called with `remainingMs` but `controlle | - | - | Use `remainingMs` consistently in the AbortError and the setTimeout call. |
| 189 | medium | Unknown | src/platform/five-plane-interface/api/ap | `authenticate()` throws `ApiAuthError(401, "api.auth_required", ...)` when no Bearer token or x-api-key is present. However, `authenticateOptionalPrin | - | - | Distinguish between "authentication not provided" (should proceed as null principal) and "authentica |
| 190 | medium | Unknown | src/org-governance/knowledge-boundary/ch | ```typescript } catch { // silently suppress } ``` | - | - | Log the error. |
| 191 | medium | Unknown | - | `updateConfig()` calls `this.config = { ...this.config, ...config };` and then immediately calls `processQueue()`. If new config reduces `maxConcurren | - | - | After updating config, if `activeCalls > new maxConcurrentCalls`, do not process queue until `active |
| 192 | medium | Unknown | - | At line 517-520, `buildCompensationPlan` calls `this.store.task.getTask?.(executionId, tenantId)?.id ?? ""` and passes the result (which could be an e | - | - | Validate that the taskId is non-empty before querying. Return null if `this.store.task.getTask` retu |
| 193 | medium | Unknown | - | ```typescript } catch (error) { logger.warn("task websocket status relay poll failed", { error: error instanceof Error ? error.message : String(error) | - | - | Track consecutive poll failures and after N failures, broadcast an error event to subscribed clients |
| 194 | medium | Unknown | - | `settle()` checks: ```typescript if (input.actualAmount > input.reservation.amount) { throw new WorkflowStateError( "budget_settlement.actual_amount_e | - | - | When `actualAmount > reservation.amount`, instead of throwing, automatically create a new reservatio |
| 195 | medium | Unknown | src/org-governance/delegated-governance/ | ```typescript } catch { // silently suppress } ``` | - | - | Log the error. |
| 196 | medium | Unknown | - | `retireSlot()` only sets status to "draining": ```typescript private retireSlot(slot: DeploymentSlot): void { this.db.connection .prepare(`UPDATE depl | - | - | Add a `drainTimeout` parameter. Verify instance count reaches 0 or check a health endpoint before ma |
| 197 | medium | Unknown | - | `registerDefaultConsumers()` at line 166-172 subscribes each default consumer with an empty async function: `async () => {}`. This is a no-op handler. | - | - | Either remove the empty subscription (if consumers are handled externally) or implement actual consu |
| 198 | medium | Unknown | - | `ChannelGatewayRetryExecutor` defaults `pollIntervalMs = 15_000` (15 seconds) with jitter of 10% (line 65). The delivery service's `calculateBackoffFo | - | - | Set `pollIntervalMs` default to be closer to or derived from `initialBackoffMs`. A reasonable defaul |
| 199 | medium | Unknown | - | `buildCostOptimizationRecommendation()` uses: ```typescript const estimatedSavingsUsd = Number((currentCostUsd * (downgradePath ? 0.22 : 0.15)).toFixe | - | - | Compute `estimatedSavingsUsd` as the actual cost difference: `currentCostUsd * (1 - recommendedProfi |
| 200 | medium | Unknown | src/org-governance/delegated-governance/ | ```typescript } catch { // silently suppress } ``` | - | - | Log the error. |
| 201 | medium | Unknown | src/sdk/fixture-redact.ts:176 | `generateTestId()` uses `` `${prefix}_${randomUUID().replace(/-/g, "").slice(0, 12)}` `` — truncates UUID to 12 characters and removes hyphens. At 12  | - | - | Use at least 16 hex characters (64 bits) or use the full UUID. Alternatively, use `newId("test")` fo |
| 202 | medium | Unknown | - | `activateSlot()` allows activation from draining status: ```typescript private activateSlot(slot: DeploymentSlot): void { this.db.connection .prepare( | - | - | Restrict activation to only "standby" and "active" statuses. "draining" slots should require explici |
| 203 | medium | Unknown | - | In `recordDeliveryFailure()`, the condition is `options.retryable && attemptNumber < record.maxRetries`. This checks if retryable is true AND if the c | - | - | Change to `options.retryable && attemptNumber <= record.maxRetries` and adjust the dead-letter condi |
| 204 | medium | Unknown | - | `domainAccessOrder` Map tracks access timestamps for LRU eviction, but when domains are removed, corresponding entries in `domainAccessOrder` are NOT  | - | - | Delete `domainAccessOrder` entries when the corresponding domain data is evicted. |
| 205 | medium | Unknown | - | All delegation record IDs use bare `` id: randomUUID() `` with no semantic prefix. This makes debugging and log correlation harder — there is no way t | - | - | Use `` newId("delegation") ``, `` newId("capability") ``, etc., with appropriate semantic prefixes. |
| 206 | medium | Unknown | - | `getLoopController` creates a `HarnessLoopController` with initial state including `lastRetryAt: run.sleepLease?.createdAt ? new Date(run.sleepLease.c | - | - | In `shouldUseBackoffRecovery`, also check that `run.sleepLease?.retryAttempt ?? 0 > 0`. Only use bac |
| 207 | medium | Unknown | - | ```typescript const projectedPlatform = (input.spend.currentPlatformCostUsd ?? input.spend.currentTaskCostUsd) + next; ``` If `currentPlatformCostUsd` | - | - | Track `currentPlatformCostUsd` as the aggregate of all running tasks' costs, not as a per-task value |
| 208 | medium | Unknown | - | `process.stderr.write` is used directly instead of the structured logger for subscriber failures and file watcher tick failures. These are error-level | - | - | Replace `process.stderr.write` calls with `logger.error` using structured log entries. |
| 209 | medium | Unknown | - | `executeInvocation()` and `waitForInvocationSlot()` both use `setTimeout` to enforce timeout limits, but no test verifies that slow invocations actual | - | - | Add tests using fake timers to advance time past `timeoutMs` and verify `ValidationError` is thrown. |
| 210 | medium | Unknown | - | Same bare UUID pattern as Issue 14 — gateway IDs have no semantic prefix. | - | - | Use `` newId("federation_gateway") `` with prefix. |
| 211 | medium | Unknown | - | When `advanceShift()` completes and calls `retireSlot()` + `activateSlot()`, the version of the retired slot is not preserved in the transition record | - | - | Capture slot version in `TrafficShiftRecord` at shift completion, not look it up later. |
| 212 | medium | Unknown | - | Same bare UUID pattern — trust relationship IDs have no entity type prefix. | - | - | Use `` newId("trust_relationship") `` with prefix. |
| 213 | medium | Unknown | - | The fallback logic at lines 47-49 means if metrics ARE present but are `null` or `undefined`, the fallback values apply: ```typescript const successRa | - | - | Distinguish between "key absent" and "key present but null". Only treat absent keys as perfect. For  |
| 214 | medium | Unknown | - | The file follows the identical pattern as `domains-runtime-orchestrator.ts`, `interaction-governance-runtime-orchestrator.ts`, and `ai-operations-runt | - | - | Extract common startup orchestration logic into a shared `RuntimeOrchestrator` base class. |
| 215 | medium | Unknown | - | ```typescript let defaultPlatformApplicationKernel: PlatformApplicationKernel \| null = null; export function getPlatformApplicationKernel(registry?:  | - | - | Use a mutex or initialization guard to ensure single instantiation in concurrent scenarios. |
| 216 | medium | Unknown | - | ```typescript const stage = typeof canaryStage === "number" && CANARY_STAGES.includes(canaryStage) ? canaryStage : 100; return { canaryPercent: stage, | - | - | Throw on invalid stage rather than silently defaulting. If defaulting is desired for backward compat |
| 217 | medium | Unknown | - | `normalizeRecoveryAttempt` returns `Math.max(1, Math.trunc(attempt))` at line 692. If `attempt` is `0`, it normalizes to `1`. But if `attempt` is `-5` | - | - | Add validation: if `attempt <= 0`, either throw a `ValidationError` or log a warning and return `1`. |
| 218 | medium | Unknown | - | ```typescript if (requestUrl == null) { throw new ValidationError("gateway.webhook_url_required", "gateway.webhook_url_required", { retryable: false,  | - | - | Sanitize URLs before including in error details: ```typescript details: { targetId, hasExternalTarge |
| 219 | medium | Unknown | - | Multiple catch blocks across the SPI registry do not log errors: - Line 530: `} catch (error) { ... }` (no logging) - Line 607: `} catch (error) { ... | - | - | Add `logger.error` calls to each catch block. |
| 220 | medium | Unknown | - | ```typescript if (maxInputPer1kUsd != null && found.profile.pricing.inputPer1kUsd > maxInputPer1kUsd) { filteredOut.push(`${profileName}:cost_cap_exce | - | - | Compare `(inputPer1kUsd * inputRatio + outputPer1kUsd * outputRatio)` against `maxInputPer1kUsd`. Us |
| 221 | medium | Unknown | - | R4-28 introduced write-ahead logging for cost events but no sweeper code exists for orphaned pending entries. | - | - | Add ADR documenting the WAL recovery mechanism, or implement the sweeper code. |
| 222 | medium | Unknown | src/platform/five-plane-execution/execut | `EffectBuilder` constructor sets `` id: `effect_${Date.now()}_${randomUUID()}` `` — combining wall-clock time with a UUID. This pattern mixes non-uniq | - | - | Replace with `` newId("effect") `` from `ids.ts`. |
| 223 | medium | Unknown | - | `DEFAULT_RATE_LIMIT_CONFIG` defines per-channel rate limits (telegram: 30/ms, slack: 30/ms, webhook: 100/ms, default: 50/ms). However, `HttpApiServer` | - | - | Add a coordination layer where `HttpApiServer`'s rate limiter is informed of downstream channel prov |
| 224 | medium | Unknown | - | `CheckpointEnvelope` stores the payload schema version in `envelope.schema` and `metadata.payloadSchemaVersion` (line 84, 58), but the `CheckpointRef` | - | - | Add an optional `schemaVersion` field to `CheckpointRef` so that manifests preserve the schema versi |
| 225 | medium | Unknown | - | At line 94-97, candidates are filtered as: `finding.severity !== "info" && (finding.recoverable \|\| finding.severity === "p0")`. This means P2 and P1 | - | - | Include all non-info findings as candidates. The `requiresManualApproval` field should be determined |
| 226 | medium | Unknown | - | `settle()` is called with: ```typescript actualAmount: ledgerEntry.amountUsd, ``` But `ledgerEntry.amountUsd` is computed at line 324 as: ```typescrip | - | - | After task completion, look up actual model pricing and compute actual charge. Create a corrected le |
| 227 | medium | Unknown | - | References `@see Runtime Execution Contract: docs_zh/contracts/runtime_execution_contract.md` which does not exist. | - | - | Create the runtime execution contract document. |
| 228 | medium | Unknown | - | Uses `process.stderr.write` directly instead of structured logger for error-level events. | - | - | Replace `process.stderr.write` with `logger.error` using structured log entries. |
| 229 | medium | Unknown | - | ```typescript } catch (err) { logger.debug("getAllowCrossDivisionDag failed", { error: err }); } ``` Using `logger.debug` for failure conditions is in | - | - | Use `logger.warn` or `logger.error`. |
| 230 | medium | Unknown | - | References `@see Observability Contract: docs_zh/contracts/debug_inspect_health_backpressure_contract.md` which does not exist. | - | - | Create the observability contract document. |
| 231 | medium | Unknown | - | References `@see Event Bus Contract: docs_zh/contracts/event_bus_contract.md` which does not exist. | - | - | Create the event bus contract document. |
| 232 | medium | Unknown | src/platform/five-plane-execution/tool-e | `generateTodoId()` returns `` `todo_${Date.now()}_${randomUUID()}` `` — same `Date.now()` + UUID pattern as Issue 2. ID length is 45+ characters with  | - | - | Replace with `` newId("todo") ``. |
| 233 | medium | Unknown | - | `evaluateCheckpointForGC` at line 405 computes `ageMs = referenceTimestamp - stats.mtimeMs`. `enforceVersionLimits` at line 238 sorts by `stats.mtimeM | - | - | Use `stats.birthtime` (birthtime/creation time) for age calculation if available, or store an explic |
| 234 | medium | Unknown | - | `buildDependencyServiceIds()` throws a plain `Error`: ```typescript throw new Error(`w5_startup_plan.missing_dependency:${dependencyStepId}`); ``` But | - | - | Use `ValidationError` consistently across all orchestrators for missing dependency errors. |
| 235 | medium | Unknown | - | Uses `?? false` coalescing which handles `undefined` but not `null` or truthy non-boolean values. | - | - | Use explicit `=== true` checks. |
| 236 | medium | Unknown | - | ADR-071 does not cover: - Plugin version negotiation - Sandbox isolation levels (implemented in code) - Plugin taint tracking (R2-5 implemented) - Con | - | - | Update ADR-071 to cover the full plugin lifecycle. |
| 237 | medium | Unknown | - | `staleWorkerIncidentIds` Set accumulates worker IDs but is never cleared on `stop()`. | - | - | Reset `staleWorkerIncidentIds` in the `stop()` method. |
| 238 | medium | Unknown | - | When a schema is updated via `register()`, `sourceContent` and `sourceTimestamps` Maps are not cleaned up for removed sources. | - | - | Clear source entries for a domain before re-initializing from the new schema. |
| 239 | medium | Unknown | - | Uses `process.stderr.write` directly for subscriber failures instead of structured logger. | - | - | Use `logger.error` with structured log entries. |
| 240 | medium | Unknown | src/platform/five-plane-control-plane/ia | Egress audit event ID uses `` `egress_${Date.now()}_${randomUUID()}` `` — same `Date.now()` + UUID anti-pattern. Timestamp component adds no uniquenes | - | - | Replace with `` newId("egress") ``. |
| 241 | medium | Unknown | - | In `execute()`, `attempt` is incremented BEFORE `throwIfAborted()`. This means if the signal is already aborted before ANY attempt, the `attempt` coun | - | - | Move `throwIfAborted()` before the `attempt++` increment. |
| 242 | medium | Unknown | - | `HttpApiServer` sets `SERVER_REQUEST_TIMEOUT_MS = 30_000` (30 seconds, line 108) as the server-level request timeout. `ChannelGatewayService` sets its | - | - | Establish a timeout hierarchy: API server socket timeout > API server request handler timeout > chan |
| 243 | medium | Unknown | - | The `api-auth-service.ts` uses a custom `ApiAuthError` class that does not inherit from `AppError`. Throughout the file, `ApiAuthError` is thrown dire | - | - | Have `ApiAuthError` extend `AppError` instead of `Error`, or consolidate all authentication errors t |
| 244 | medium | Unknown | src/platform/five-plane-interface/api/ht | The task creation handler reads the idempotency key from the HTTP header `idempotency-key` (line 315: `ctx.request.headers["idempotency-key"]`), but t | - | - | Unify idempotency key handling: either read from the HTTP header in gateway (current) and stop wrapp |
| 245 | medium | Unknown | - | `recordCost()` decrements `unsourcedRecordCount` after successful recording of each valid record, but only decrements if `unsourcedRecordCount > 0`. H | - | - | Use `Number.MAX_SAFE_INTEGER` cap on increment, or convert to a gauge metric that can be reset by an |
| 246 | medium | Unknown | - | `normalizeArtifactRecord()` (lines 5-16 of artifact-model.ts) maps `size -> sizeBytes` and `contentHash -> checksum` for backward compatibility. This  | - | - | Document the requirement to use `parseArtifactRecord()` for all artifact record imports. Consider ad |
| 247 | medium | Unknown | src/domains/governance/division-loader.t | ```typescript } catch (err) { logger.debug("getAllowCrossDivisionDag failed", { error: err }); } ``` Using `logger.debug` for a failure condition is i | - | - | Use `logger.warn` or `logger.error` depending on whether the condition is recoverable. |
| 248 | medium | Unknown | src/domains-runtime-orchestrator.ts:125 | ```typescript public snapshotReadiness(): DomainsReadinessSnapshot { const startupPlan = this.startupPlan ?? buildDomainsStartupPlan(); ``` The method | - | - | Use `this.registry.get()` or ensure `buildDomainsStartupPlan()` is called through the registry to av |
| 249 | medium | Unknown | - | Console methods are monkey-patched at module load time: ```typescript console.log = ((...args: unknown[]) => writeStructuredLine("info", ...args)) as  | - | - | Consider lazy patching or making the console redirection opt-in via an explicit `install()` call. |
| 250 | medium | Unknown | - | ```typescript throw new StorageError("workflow.not_found", "workflow.not_found", { statusCode: 404, retryable: false, details: { taskId, tenantId: ten | - | - | Use `WorkflowStateError` from `errors.ts` for workflow state violations, or create a dedicated `Work |
| 251 | medium | Unknown | src/sdk/cli/pack-validate.ts:90 | `pack-validate` enforces mandatory presence of fields `sdk_semver`, `platform_min_version`, and `platform_max_version` as hard errors (lines 90-100).  | - | - | Either add these fields to the BusinessPackManifest schema, or remove the mandatory checks from pack |
| 252 | medium | Unknown | - | `verifyManifestChecksum()` (lines 188-195) computes the combined checksum from the manifest's `checkpointRefs` and compares it to `manifest.combinedCh | - | - | Require `checksum` to be present when adding a checkpoint to a manifest (throw if missing). Remove t |
| 253 | medium | Unknown | src/domains/governance/division-loader.t | Same issue as Issue 4: ```typescript } catch (err) { logger.debug("getDefaultDivisionRegistry failed", { error: err }); } ``` | - | - | Upgrade to `logger.warn` or `logger.error`. |
| 254 | medium | Unknown | - | When `executeWithTimeout()` times out in the HALF_OPEN state, `recordFailure()` is called which transitions to OPEN and sets `nextAttempt = Date.now() | - | - | After timeout rejection, check if state is OPEN and attach `retryAfterMs` to the error. |
| 255 | medium | Unknown | - | The circuit breaker implementation in `ChannelGatewayService` (lines 516-553) is a hand-rolled implementation independent of the `CircuitBreaker` clas | - | - | Replace the hand-rolled circuit breaker in `ChannelGatewayService` with the platform `CircuitBreaker |
| 256 | medium | Unknown | - | `compareProfiles()` sorts by `inputPer1kUsd` only: ```typescript function compareProfiles(a: EligibleProfile, b: EligibleProfile): number { if (a.prof | - | - | Compare `inputPer1kUsd * inputWeight + outputPer1kUsd * outputWeight` using a representative ratio ( |
| 257 | medium | Unknown | - | Multiple catch blocks return a result object but do not log the error: ```typescript } catch (err) { return { valid: false, error: err instanceof Erro | - | - | Add `logger.warn` with the error context before returning. |
| 258 | medium | Unknown | - | In `execute()`, when `fn()` is called and throws a non-reset error, `onFailure()` is called. Inside `onFailure()` for HALF_OPEN state, `nextAttempt` i | - | - | Catch failures in HALF_OPEN and wrap in `CircuitBreakerOpenError` with correct `retryAfterMs`. |
| 259 | medium | Unknown | - | `unpackCheckpointEnvelope` validates `envelope.schema === envelope.metadata.payloadSchemaVersion` at line 189, but neither value is checked against th | - | - | After decompressing and parsing the payload, validate that the payload's own schema version field ma |
| 260 | medium | Unknown | - | `HealthService` has hardcoded default thresholds in its options interface (line 180-198): `staleWorkerThresholdMs: 5 * 60_000` (5 minutes), `queueStar | - | - | Create a shared `health-thresholds.ts` constants module that exports all threshold defaults (`STALE_ |
| 261 | medium | Unknown | src/platform/five-plane-interface/api/ht | ```typescript void pipeline( Readable.from([payload.body]), compress, response, ).catch(() => { response.destroy(); }); ``` The `.catch(() => {...})`  | - | - | ```typescript .catch((err) => { this.logger.warn("http.compression_pipeline_failed", { error: err in |
| 262 | medium | Unknown | src/interaction/nl-gateway/nl-gateway-mo | `RiskPreview` interface defines fields in both camelCase (`overallRisk`, `riskFactors`, `sideEffects`, `approvalNeeded`) and snake_case (`overall_risk | - | - | Consolidate to one naming convention (camelCase per standard TypeScript conventions). Remove snake_c |
| 263 | medium | Unknown | - | `remainingBudgetUsd` is computed as: ```typescript const remainingBudgetUsd = Math.max( 0, Math.min( input.policy.maxTaskCostUsd - projectedTask, inpu | - | - | Clarify in the type documentation that `remainingBudgetUsd` is the **most restrictive remaining budg |
| 264 | medium | Unknown | src/org-governance/approval-routing/appr | ```typescript } catch (error) { return { eligible: false, reason: "internal_error" }; } ``` Error is caught but only a generic string is returned. The | - | - | Log the error and include a trace ID in the returned reason. |
| 265 | medium | Unknown | src/org-governance/approval-routing/appr | ```typescript } catch (error) { return { eligible: false, reason: "internal_error" }; } ``` No logging occurs, making it impossible to trace approval  | - | - | Add `logger.warn` with error context and include a trace ID in the returned reason. |
| 266 | medium | Unknown | - | ```typescript export function shouldRollbackCanary(progress: CanaryProgress): boolean { return progress.errorRate > 0.05 \|\| progress.successRate < 0 | - | - | Align rollback thresholds with promotion criteria. Use the same configurable thresholds shared betwe |
| 267 | medium | Unknown | - | `computeCombinedChecksum` filters out empty checksums with `.filter(Boolean)` at line 174, but the concatenation at line 175 uses `\|` as a delimiter  | - | - | Use a two-level approach: prefix each checksum with its length, e.g., `"3:abc\|3:def"` or `"3:abc##3 |
| 268 | medium | Unknown | - | ```typescript class GraphQLAdapterError extends Error { public constructor( public readonly code: string, message: string, ) { super(message); this.na | - | - | Either make `GraphQLAdapterError` extend `AppError` or use existing `AppError` subclasses (e.g., `Va |
| 269 | medium | Unknown | src/org-governance/approval-routing/rout | Same pattern as Issue 7 — catch block returns `false` without logging: ```typescript } catch (error) { return false; } ``` | - | - | Log the error and return a meaningful result. |
| 270 | medium | Unknown | - | `rollbackShift()` accepts any string as `trigger`: ```typescript rollbackShift(shiftId: string, trigger: RollbackTrigger, reason: string): RollbackRec | - | - | Add runtime validation of the trigger string against the union type before writing to the database. |
| 271 | medium | Unknown | - | `CircuitBreakerOpenError` in the platform/stability variant lacks `retryAfterMs` and `circuitName` fields that are present in the reliability variant  | - | - | Add `circuitName: string` and `retryAfterMs: number \| null` to `CircuitBreakerOpenError` in `platfo |
| 272 | medium | Unknown | - | The `requeueExecution` method (line 171) calls `this.store.dispatch.getExecution(action.targetId)` and if not found returns `{ applied: false, detail: | - | - | Add a null check on `execution.taskId` before calling `loadTaskSnapshot`. Validate that taskId is a  |
| 273 | medium | Unknown | - | `calculateBackoffForAttempt()` has a special case at line 204: `if (attemptNumber <= 0 && new Error().stack?.includes("channel-gateway-types-extended. | - | - | Remove the test-file-specific bypass. If attemptNumber <= 0 needs special handling for the test, use |
| 274 | medium | Unknown | src/platform/five-plane-interface/api/ht | `buildJsonResponse()` wraps all successful responses in `{ requestId, data: payload }` shape (line 179). However, paginated responses from task routes | - | - | Ensure all route handlers use `buildJsonResponse()` consistently, or define a single response envelo |
| 275 | medium | Unknown | - | `recordUsage()` computes: ```typescript const unitPriceUsd = quota?.unitPriceUsd ?? 0; const estimatedChargeUsd = roundCurrency(quantity * unitPriceUs | - | - | After task completion, before calling `budgetAllocator.settle()`, look up the actual model used and  |
| 276 | medium | Unknown | - | `createCheckpointEnvelope` enforces `maxSizeBytes` (default 10MB) on the **compressed** payload before storage (lines 139-151). If compressed data exc | - | - | Add `totalCompressedSizeBytes` to `CheckpointManifest` and compute it as the sum of `metadata.compre |
| 277 | medium | interaction | src/interaction/nl-gateway/nl-gateway-su | `buildRiskPreview()` has a dead code block in the dry-run execution path. Lines 519-524 set `dryRunResult` with hardcoded values and `// Note: In prod | - | - | Either implement the async dry-run path fully in `buildRiskPreviewWithDryRun` (which correctly await |
| 278 | medium | interaction | src/interaction/autonomy/autonomy-servic | `resolveLevel()` uses a simple threshold-based mapping (`riskScore >= 80 → manual`, `>= 60 → supervised`, etc.) that does not account for domain-speci | - | - | Integrate `taskType` into the autonomy level determination. Add a task-type risk multiplier or domai |
| 279 | medium | interaction | src/interaction/autonomy/trust-scorer/in | `applyTrustDecay()` applies exponential decay with a fixed `decayRate` of 0.05 per day. The formula `score * Math.pow(1 - decayRate, inactiveDays)` me | - | - | Connect the `NO_EXECUTION_DEMOTION_THRESHOLD_DAYS` constant to the decay rate calculation. If the th |
| 280 | medium | interaction | src/interaction/dashboard/dashboard-proj | `scheduleEmit()` sets a debounce timer but the callback only clears the timer and emits nothing. The comment says "In real implementation, this would  | - | - | Either implement the debounce emit path properly (deltas are held until the timer fires), or remove  |
| 281 | medium | interaction | src/interaction/ux/conversation-history- | The `isRestricted` flag is checked at line 154 before persisting, but the session's `isRestricted` flag is captured at the time of `addTurn()`. If a t | - | - | Add a timestamp or version field to the session record so that restricted flag changes can be audite |
| 282 | medium | interaction | src/interaction/ux/wizard/index.ts:78 | `goBackWizard()` pops from `history` but does not update `visitedStepIds`. The `advanceWizard()` function at line 92 correctly updates `visitedStepIds | - | - | Add `visitedStepIds` update to `goBackWizard()`: when going back, the previous step is still visited |
| 283 | medium | interaction | src/interaction/ux/onboarding/index.ts:1 | `resolveMode()` and `buildOnboardingPlan()` compute domain recommendations independently using different keyword matching approaches. `resolveMode()`  | - | - | Ensure `resolveMode()` passes the `UserPortalContext` with `userMode` populated when calling `recomm |
| 284 | medium | interaction | src/interaction/ux/workflow-builder-serv | `validateGraph()` performs cycle detection via DFS with explicit visited/stack sets, which is correct. However, the cycle detection only runs when `va | - | - | Integrate cycle detection into `build()` before returning `WorkflowBuilderResult`, or at minimum ens |
| 285 | medium | interaction | src/interaction/nl-gateway/nl-gateway-su | `deriveConversationState()` uses `confirmationRequired` as a signal but the logic can produce unexpected mappings. When `blockedByPolicy=true`, it ret | - | - | Document the full decision matrix for conversation state transitions. Consider extracting to a looku |
| 286 | medium | interaction | src/interaction/goal-decomposer/index.ts | `detectTemplate()` first checks `domainRecipes` (via `matchDomainRecipe`), then falls through to `domainRecipeService.matchRecipe()`, then falls throu | - | - | Raise the specificity of regex pattern matching. Require at least 2 matching keywords from distinct  |
| 287 | medium | interaction | src/interaction/goal-decomposer/index.ts | The cycle detection throws an error after `graphAnalysis` is computed, but the error message only contains `goal.goalId` and not the specific cycle pa | - | - | Include the cycle node IDs in the error message: `throw new Error(\`goal_decomposer.cycle_detected:$ |
| 288 | medium | interaction | src/interaction/proactive-agent/index.ts | The autonomy level check for firing triggers requires `this.currentAutonomyLevel !== "semi_auto" && this.currentAutonomyLevel !== "full_auto"`. This m | - | - | Rename the reason code to `proactive_agent.autonomy_level_insufficient` and add a comment explaining |
| 289 | medium | ops-maturity | src/ops-maturity/capacity-planner/capaci | `computeDynamicFailoverReserve()` returns static percentages per SLA tier (30/20/15) with no consideration of actual load, error budget burn rate, or  | - | - | Consider incorporating `providerQuotaLimits` and `regionFailoverReservePercent` from the capacity si |
| 290 | medium | ops-maturity | src/ops-maturity/cost-optimizer/cost-opt | `recordCost()` throws an error if `decisionRef.trim().length === 0`. However, `unsourcedRecordCount` is decremented only after the throw check passes. | - | - | Guard the decrement with `if (this.unsourcedRecordCount > 0) { this.unsourcedRecordCount -= 1; }` or |
| 291 | medium | ops-maturity | src/ops-maturity/cost-optimizer/recommen | `downgradePath` logic checks `recommendedProfile.pricing.inputPer1kUsd + recommendedProfile.pricing.outputPer1kUsd < currentProfile.pricing.inputPer1k | - | - | Add a model capability compatibility check before declaring a downgrade viable. Log a warning when a |
| 292 | medium | ops-maturity | src/ops-maturity/platform-ops-agent/plat | `createProposal()` calls `this.chooseActionType()` before checking whether the computed `actionType` is in `this.definition.allowedActionTypes`. The ` | - | - | Move the allowed action type check before `chooseActionType()` so that a valid fallback action is ch |
| 293 | medium | ops-maturity | src/ops-maturity/explainability/explanat | `generate()` throws if `depth === "L3"` and `options.forensicBudgetReservationId` is not provided, but `generate()` does not have access to actual bil | - | - | Validate the reservation ID exists in the billing system before throwing. Consider deferring the err |
| 294 | medium | ops-maturity | src/ops-maturity/explainability/explanat | `buildVersionLockRef()` calls `JSON.stringify()` on `rationaleWithoutLock`. If any field contains a circular reference or non-serializable value, this | - | - | Add a try/catch around `JSON.stringify` in `buildVersionLockRef()` and throw a descriptive error wit |
| 295 | medium | ops-maturity | src/ops-maturity/edge-runtime/edge-runti | The offline window check `Date.now() - createdAtMillis > profile.offlineMaxDuration!` compares milliseconds against a duration field. If `offlineMaxDu | - | - | Normalize the duration unit explicitly. Document the expected unit for `offlineMaxDuration` and add  |
| 296 | medium | ops-maturity | src/ops-maturity/learning/evidence-store | `cloneEvidenceRecord()` uses `structuredClone()` which is not available in all Node.js versions prior to Node 17. The codebase may run on older Node v | - | - | Verify the minimum Node.js version for this project. If Node 17+ is guaranteed, document it in the R |
| 297 | medium | ops-maturity | src/ops-maturity/monitoring/anomaly-dete | `ingestMetric()` stores the entire buffer array per metric name and relies on `evictExpired()` for cleanup. However, `evictExpired()` only triggers wh | - | - | Evict based on per-metric buffer size or total age rather than total distinct keys. Consider a slidi |
| 298 | medium | ops-maturity | src/ops-maturity/agent-lifecycle/canary- | `shouldRollbackCanary()` is hardcoded with literal thresholds (`errorRate > 0.05`, `successRate < 0.90`) that are not configurable and differ from the | - | - | Extract these thresholds into a shared constants object or configuration passed to both functions. A |
| 299 | medium | ops-maturity | src/ops-maturity/emergency/resume-protoc | `canResumeFromPanic()` requires `plan.checkpointsVerified`, `plan.forensicSnapshotReviewed`, `plan.rollbackPlanReady`, and `plan.validationRunPassed`  | - | - | Use explicit `=== true` checks rather than truthiness. Add validation that `plan.approvalCount` refl |
| 300 | medium | ops-maturity | src/ops-maturity/drift-detection/drift-d | `computeFeatureDifference()` computes a symmetric set difference but normalizes by raw count rather than union cardinality. Two fingerprints with 10 f | - | - | Normalize by the size of the union of features: `difference / union.size`. This yields a Jaccard-lik |
| 301 | medium | platform/five-plane- | src/platform/five-plane-execution/lease/ | The TTL expiration check `activeLease.expiresAt <= occurredAt` uses `<=` (less-than-or-equal). This means a lease that expires exactly at `occurredAt` | - | - | Use consistent comparison operators. If `<=` is used for validation (meaning "expired if expiresAt < |
| 302 | medium | platform/five-plane- | src/platform/five-plane-execution/runtim | `assertAuditRef()` is called in the transition chain but the condition `!hasAuditRef(command.auditRef)` can throw even for transitions that don't requ | - | - | Consider making audit ref optional for non-critical transitions (e.g., budget ledger "reserved" to " |
| 303 | medium | platform/five-plane- | src/platform/five-plane-execution/execut | `normalizeToolInputForHash()` sorts object keys with `Object.keys(input).sort()`. However, JSON.stringify with a custom replacer does NOT guarantee ke | - | - | Use a deep normalization approach that sorts keys at all levels, not just top-level. Consider using  |
| 304 | medium | platform/five-plane- | src/platform/five-plane-execution/compen | `executeCompensationSteps()` iterates through compensation steps and catches errors per step. However, if `executeCompensationStep()` returns `false`  | - | - | When `executeCompensationStep()` returns `false`, capture the failure reason in the evidenceRefs. Ad |
| 305 | medium | platform/five-plane- | src/platform/five-plane-execution/execut | R4-28 comment says "Write-ahead logging for cost events - persist BEFORE execution" and the code inserts `costEventWAL` with "pending" status before t | - | - | Add a recovery sweep that cleans up pending WAL entries older than a threshold (e.g., entries where  |
| 306 | medium | platform/five-plane- | src/platform/five-plane-execution/state- | `WorkflowTransitionService.apply()` has nested try-catch blocks at lines 197-214. The outer catch at line 210 catches errors and re-throws only if it' | - | - | Replace the nested try-catch with explicit conditional logic. If `createTier1StatusEvent` should onl |
| 307 | medium | platform/five-plane- | src/platform/five-plane-execution/state- | `TaskTerminalTransitionService.apply()` calls `workflowStateMachine.assertTransition()` before checking if `shouldTransitionWorkflow`. If `shouldTrans | - | - | Add a debug log or comment clarifying that `assertTransition` is only called after `shouldTransition |
| 308 | medium | platform/five-plane- | src/platform/five-plane-execution/worker | The writeback service extracts `taskOutputJson` and `outputsJson` from either the writeback input or the existing records: `const taskOutputJson = inp | - | - | Clarify the default behavior: if no output was produced (both inputs are null), use `null` rather th |
| 309 | medium | platform/five-plane- | src/platform/five-plane-execution/dispat | When backpressure blocks a ticket, two near-identical event records are emitted in the same transaction: `dispatch:backpressure_rejected` (line 276) a | - | - | Remove the duplicate event emission. Keep only `dispatch:backpressure_rejected` (with colon separato |
| 310 | medium | plugins | src/plugins/validators/basic-evaluator.t | The manifest for `plugin.core.basic-evaluator` declares `spiTypes: ["validator", "evaluator"]` (line 210), indicating it implements both roles. Howeve | - | - | Either remove `"evaluator"` from the manifest's `spiTypes` array, or add an `evaluator` SPI type to  |
| 311 | medium | plugins | src/plugins/presenters/coding-presenter. | `citations` is declared as `string[]` (line 45 is the assignment: `citations: input.artifacts.map(...)`). But `input.artifacts` is typed as `ArtifactR | - | - | Extract the string identifier from each `ArtifactRef` before pushing to citations, e.g., `citations: |
| 312 | medium | plugins | src/plugins/presenters/operations-presen | Same issue as Issue 11. `input.artifacts` is `ArtifactRef[]` but is pushed directly into `citations: string[]`. | - | - | Same as Issue 11 - extract a string identifier from each `ArtifactRef`. |
| 313 | medium | plugins | src/plugins/presenters/growth-presenter. | Same artifact-to-citation issue. The code pushes raw `artifactRef` strings to `citations` but does not verify that `ArtifactRef` is a string type. | - | - | Same as Issue 11 - ensure `ArtifactRef` values are properly converted to strings before adding to ci |
| 314 | medium | plugins | src/plugins/planners/basic-planner.ts:22 | `suggestWorkflow` accesses `task.assessment.complexity` and `task.assessment.approvalPolicy.required` without null checks. If `task.assessment` is `nu | - | - | Add null checks: `if (!task.assessment) return null;` at the start of `suggestWorkflow`, or use opti |
| 315 | medium | plugins | src/plugins/operations-config.ts:178 | `externalAdapters: ["github"]` is correct, but the `pluginBindings` at lines 179-183 reference `"plugin.operations.presenter"` etc. However, the `plug | - | - | Align `pluginType` in pluginBindings with the actual `spiType` of the referenced plugin. Use `plugin |
| 316 | medium | plugins | src/plugins/growth-config.ts:202 | `pluginType: "tool"` is used for all plugin bindings (retriever, presenter, validator, planner) even though the actual plugins declare different `spiT | - | - | Set `pluginType` to match the actual SPI type of the bound plugin (`"retriever"`, `"presenter"`, `"v |
| 317 | medium | tests/unit | - | The UI `createOfflineQueueInterceptor()` (interceptors.ts lines 188-213) queues write requests when `navigator.onLine === false`. It uses `queue.enque | - | - | Either add offline queue support to the SDK, or document that offline resilience is a UI-only concer |
| 318 | medium | tests/unit | - | The UI REST client uses `DEFAULT_ACCEPT_VERSION_HEADER = "2026-04-01,2026-01-01"` (rest-client.ts line 58). The SDK `buildAuthHeaders()` function (api | - | - | Add `Accept-Version` header support to the SDK's `buildAuthHeaders()` to match the UI's default. The |
| 319 | medium | tests/unit | - | The UI `DefaultRESTClient` executes interceptors in this order (rest-client.ts lines 409-434): 1. `onRequest` runs forward (`for` loop, line 410-412)  | - | - | Extract the UI's auth interceptor refresh logic into a reusable pattern that can be applied consiste |
| 320 | medium | tests/unit | - | The `createAuthInterceptor()` accepts a `TokenResolver` interface (interceptors.ts lines 24-30) with methods `getAccessToken`, `getAccessTokenWithRefr | - | - | Implement `TokenResolver` on the auth store, or create a bridge that wraps the auth store as a `Toke |
| 321 | medium | tests/unit | - | The SDK has a full SSE-based event subscription system (`subscribeToEvents()`, api-client.ts lines 261-375) with reconnection logic, abort controllers | - | - | Implement actual SSE fallback in `BrowserWSClient` or document that SSE fallback is intentionally st |
| 322 | medium | tests/unit | tests/unit/interaction-governance/intera | Test `buildInteractionGovernanceRuntimeCatalog governance array is frozen (but not interaction)` asserts `Object.isFrozen(catalog.governance)` and com | - | - | If the freeze behavior is a contract, document it as such and ensure it is tested through behavior r |
| 323 | medium | tests/unit | - | `riskLevelForSubject()` at line 148 checks `costType === "model"` but `"model"` is not defined in the `CostSubjectType` union (line 22). This means an | - | - | Add `"model"` to `CostSubjectType` union or remove the check from `riskLevelForSubject()`. In `multi |
| 324 | medium | tests/unit | - | `CheckpointEnvelopeMetadata` at line 46 defines `payloadSchemaVersion: string` but does not include a `domainId` field. The `DomainBaseline` interface | - | - | Add `domainId?: string` to `CheckpointEnvelopeMetadata` so domain context can be attached to checkpo |
| 325 | medium | tests/unit | tests/unit/domains-runtime-orchestrator. | Test `DomainsRuntimeOrchestrator.startup later steps depend on earlier rings` uses a non-null assertion `result.steps.find((s) => s.stepId === "ring2" | - | - | Add an explicit assertion that `ring2` step exists before accessing it: `const ring2 = result.steps. |
| 326 | medium | tests/unit | - | `session-management.ts` line 96 checks `process.env.NODE_ENV === "production" && process.env.AA_ALLOW_IN_MEMORY_SESSION_STORE !== "1"` to throw if in- | - | - | Create a shared utility function `assertInMemoryStorageAllowed(storeName: string)` that checks the a |
| 327 | medium | tests/unit | - | The delegation-request module has a comment referencing "ADR-026 8-factor budget tracking" but no ADR-026 exists in the documentation. This is cross-m | - | - | Either create ADR-026 documenting the 8-factor budget tracking model, or update the code reference t |
| 328 | medium | tests/unit | - | `getStatistics()` returns aggregate healing metrics but these are not integrated into the execution engine's event system. The execution engine emits  | - | - | Emit healing events to the event store with `eventType: "ops_maturity.self_healing.*"` so healing op |
| 329 | medium | tests/unit | - | Several cross-module relationships lack ADR documentation: 1. Relationship between pack lifecycle stages and domain lifecycle stages (Issue 1) 2. Coup | - | - | Create ADR documenting: 1. Pack lifecycle stages and their relationship to domain lifecycle stages 2 |
| 330 | medium | tests/unit | - | `createNodeAttempt()` at line 300 checks `attemptNo < 1` and throws `ValidationError`. The test at lines 232-238 creates an attempt with `attemptNo: 1 | - | - | Add test: ```typescript test("createNodeAttempt throws on attemptNo < 1", () => { assert.throws( ()  |
| 331 | medium | tests/unit | - | `simulate()` takes `CostSimulationScenarioInput[]` and computes projected costs, but this simulation is not connected to the execution engine's budget | - | - | Add a budget simulation mode to `BudgetAllocator` that accepts a `CostSimulationScenarioInput` and c |
| 332 | medium | tests/unit | - | `validateExecutableContract` is tested only for `TaskDraft`, `ConfirmedTaskSpec`, and `RequestEnvelope` (line 186-196). There is no test for validatin | - | - | Add test: ```typescript test("validateExecutableContract rejects invalid PromptBundle", () => { asse |
| 333 | medium | tests/unit | tests/unit/root-entry-mode-consolidation | Test `PlatformStartupTargetKind is the single source of truth (issue 2002 fix)` uses a TypeScript type-level check with a conditional type (`Expected  | - | - | While type-level testing has limited options in TypeScript, add runtime assertions that validate the |
| 334 | medium | tests/unit | tests/unit/interaction-governance/intera | Test file has no `test.afterEach` or `test.beforeEach` cleanup for the singleton `ServiceRegistry.getInstance()`. The tests at lines 117, 126, 135, 14 | - | - | Add a `test.beforeEach` or `test.afterEach` hook that calls `await ServiceRegistry.getInstance().res |
| 335 | medium | tests/unit | - | The `HarnessRun` interface defines `goal?: string` and `mode?: string` at lines 287-288 of contract-models.ts. However, `HarnessRunSchema` at lines 23 | - | - | Add to `HarnessRunSchema`: ```typescript goal: z.string().optional(), mode: z.string().optional(), ` |
| 336 | medium | tests/unit | - | `SessionDualStorageService` stores session events in JSONL files named after `sessionId` (line 96: `session-${safeSessionId}.jsonl`). The session reco | - | - | Add `domainId: string` to `SessionEvent.payload` (line 51) so session events carry domain context. W |
| 337 | medium | tests/unit | tests/unit/root-entry-summary.test.ts:1 | This is a golden test with hardcoded exact counts: `totalCapabilityCount: 31`, `ring1: 8, ring2: 11, ring3: 12`, etc. Any change to the actual counts  | - | - | Consider whether these exact counts should be validated against computed values from the actual cata |
| 338 | medium | tests/unit | - | `createMockBundle()` (line 490-493) computes normalized version via: ```typescript const normalizedVersion = displayVersion .replace(/^v/i, "") .split | - | - | Use `normalizeVersion()` from the actual contract in `createMockBundle()` to ensure consistency, or  |
| 339 | medium | tests/unit | - | `DomainRegistryService` tracks knowledge namespaces per domain in `knowledgeNamespacesByDomain: Map<string, Set<string>>` (line 23). This is a read-on | - | - | Add a `DomainKnowledgeIntegration` service that, given a `sessionId`, can: 1. Resolve the session's  |
| 340 | medium | tests/unit | - | `execute()` checks `consecutiveFailures >= this.policy.maxRetries` (line 103) and then checks `timeSinceLastAttempt < this.policy.cooldownPeriodMs` (l | - | - | Coordinate cooldown periods with execution retry budgets. Consider adding a callback or event that t |
| 341 | medium | tests/unit | - | `FairSchedulingService.schedule()` computes `starvedItemIds` as items with `ageMs >= 15 * 60_000` (line 69). However, `ExecutionDispatchService.dispat | - | - | Add a `starvedItemIds` parameter to `DispatchExecutionOptions` or a separate `boostStarvedItems()` c |
| 342 | medium | tests/unit | - | 1. `DomainDefinition` has `trustTier: z.enum(["internal", "trusted", "community", "external"])` (domain-model.ts line 178) 2. `BusinessPackManifest` h | - | - | Create ADR documenting the relationship between `trustTier` and `sandboxTier`. Add compatibility val |
| 343 | medium | tests/unit | - | `shouldRequireOrchestration()` at line 467-495 in intake-router-model.ts decides `requiresOrchestration: boolean` based on `riskClass`, `matchedHints. | - | - | Add `requiresOrchestration: boolean` to `OapeflirLoopInput` and propagate it to the Assess stage. If |
| 344 | medium | tests/unit | - | `DEFAULT_MAX_CHECKPOINT_SIZE_BYTES` is set to 10MB (line 36). The domain baseline's `buildDefinition()` at lines 483-486 sets `maxTokensPerTask` and ` | - | - | Add `maxCheckpointSizeBytes` to `DomainBaseline.capabilities` or `DomainRiskProfile`. In `createChec |
| 345 | medium | tests/unit | - | `compareTraceFields()` at lines 154 compares `costUsd` between frames. The workflow debugger's `WorkflowTraceFrame` interface (line 30) includes `cost | - | - | Ensure `WorkflowTraceFrame.costUsd` is populated from actual cost records, not WAL placeholders. Add |
| 346 | medium | tests/unit | - | `RpoRtoTrackingService.assertSlaCompliance()` (line 391) throws if RPO or RTO targets are breached. `RuntimeRecoveryService.listRecoverableExecutingRu | - | - | Before returning recovery candidates, `RuntimeRecoveryService` methods that list candidates for a br |
| 347 | medium | tests/unit | - | `PackDomainAssociationService` maintains pack-domain associations independently from domain lifecycle: 1. When a domain transitions to `deprecated` or | - | - | Add lifecycle state callbacks to `PackDomainAssociationService`: - When domain is deprecated/archive |
| 348 | medium | tests/unit | - | User access tokens have a 15-minute TTL (`ACCESS_TOKEN_TTL_MS` at session-management.ts line 18) while service tokens have a 60-minute TTL (`SERVICE_T | - | - | If services act on behalf of users, the service token's TTL should be bounded by the user's session  |
| 349 | medium | tests/unit | - | `ProactiveAgentService` produces action modes: `"auto_execute" \| "suggest" \| "silent_record"` (line 75-76). `AutonomyBoundaryPolicy` decides on `Aut | - | - | Add `actionMode` context to the `AutonomyBoundaryDecision` request. When `actionMode === "auto_execu |
| 350 | medium | tests/unit | tests/unit/testing/golden.test.ts:1 | The golden test utilities create golden files in `tests/golden/snapshots/` at runtime via `mkdirSync` and `writeFileSync` within each test. This means | - | - | Separate golden file creation from validation. Golden files should be committed to source control an |
| 351 | medium | tests/unit | - | `ReconciliationJobInput` (failover-controller lines 382-392) includes `pendingWriteCount`, `pendingApprovals`, `openBudgets`, `outboxMessages`, `restr | - | - | Add `activeLeaseCount`, `activeLeaseHolderRegionDistribution`, and `stalLeaseCount` to `Reconciliati |
| 352 | medium | tests/unit | - | `submitForCertification()` (line 118) transitions pack from `draft` to `certifying`, but does not verify: 1. The associated domain exists and is in an | - | - | Add domain state verification in `submitForCertification()`: - Query `DomainRegistryService` to veri |
| 353 | medium | tests/unit | - | In `inject()` (lines 325-338), when `this.rateLimiter != null`, rate limit headers are attached via `attachRateLimitHeaders()`. However, when `this.ra | - | - | Always attach `x-ratelimit-remaining` header regardless of whether rate limiting is enabled. When ra |
| 354 | medium | tests/unit | - | `FairSchedulingService.schedule()` calls `evaluateMultiDimensionalQuota()` with `workerUnits` as the rejection threshold (line 64). The quota enforcem | - | - | Add a `quorumRegionCount` parameter to `FairSchedulingRequest` that indicates how many regions must  |
| 355 | medium | tests/unit | - | 1. Pack registration (`PackRegistryService.registerPack()`) does not verify the `domainId` exists in `DomainRegistryService` 2. Domain registration (` | - | - | Add cross-registry validation in both registration paths: - Pack registration: verify `domainId` exi |
| 356 | medium | tests/unit | - | `BudgetLedger.status` type allows 7 states but `BudgetLedgerSchema` only defines 4 states ("open", "soft_cap_reached", "hard_cap_reached", "closed").  | - | - | Add test verifying that "settling", "reserving", "releasing" statuses are accepted by `BudgetLedgerS |
| 357 | medium | tests/unit | - | `OapeflirLoopService` uses `HarnessLoopController` (line 358) to track iterations, cost, and replan decisions. `ProactiveAgentService` maintains its o | - | - | Create a shared `ExecutionBudgetRegistry` that ProactiveAgent and OAPEFLIR both consult. ProactiveAg |
| 358 | medium | tests/unit | - | `validateDefinition()` at lines 384-409 validates plugin bindings against `installedPluginIds` and `healthyPluginIds`. The session dual storage servic | - | - | Add a `validateSessionPluginReferences(events: SessionEvent[]): PluginValidationResult` method to `S |
| 359 | medium | tests/unit | - | The cost optimizer's `buildRecommendations()` generates recommendations based on aggregated costs, but these recommendations are not integrated into t | - | - | Create an integration point where `CostOptimizationService.buildRecommendations()` results can influ |
| 360 | medium | tests/unit | - | 1. Domain risk level (`DomainRiskLevel = "low" \| "medium" \| "high" \| "critical"`) is defined in `risk-profile/index.ts` 2. Pack risk matrix (`Busin | - | - | Add cross-validation when pack is associated with domain: pack's highest risk level in `riskMatrix`  |
| 361 | medium | tests/unit | - | `validateServiceToken()` at lines 323-371 checks `token.audience !== input.audience && token.audience !== "*"` at line 353. This validates that the se | - | - | In the route handler creation (e.g., `createTaskRoutes`, `createGatewayRoutes`), if the route is int |
| 362 | low | Deep-Cross: http + t | src/platform/five-plane-interface/channe | `handleDisconnection()` at line 361 calls `ws.removeAllListeners?.()`. This method is optional on the `ws` library's WebSocket type and may not exist  | - | - | Document the WebSocket implementation requirement clearly. Use a type guard or explicitly check for  |
| 363 | low | Deep-Cross: http + t | src/platform/five-plane-interface/api/mi | `CorsMiddleware` normalizes and validates CORS config in the constructor, but the validation only fires when `new CorsMiddleware(config)` is called. I | - | - | Add a `validate()` method and call it after any runtime config update. Validate that `Sec-Fetch-Site |
| 364 | low | Deep-Cross: http + t | src/interaction/dashboard/dashboard-webs | `scheduleProjectionPoll()` uses `setTimeout` and `timer.unref?.()` for the projection polling loop. The `projectionPollingInFlight` flag at line 584 p | - | - | Implement exponential backoff with a maximum delay (e.g., cap at 60 seconds) when `consumePendingDel |
| 365 | low | Deep-Cross: http + t | src/platform/five-plane-interface/api/ht | `SERVER_HEADERS_TIMEOUT_MS = 60_000` (60 seconds) and `SERVER_REQUEST_TIMEOUT_MS = 30_000` (30 seconds) are set independently. When `headersTimeout` e | - | - | Ensure `headersTimeout > requestTimeout` by at least a margin (e.g., 2x). Document the relationship  |
| 366 | low | Deep-Cross: http + t | src/platform/five-plane-interface/channe | The `WebSocketBridge` sets `maxPayload: options.maxPayloadBytes ?? DEFAULT_MAX_PAYLOAD_BYTES` (64KB) at line 153. The `ws` library enforces this at th | - | - | Add application-level message size validation separate from the WebSocket frame limit. Consider sett |
| 367 | low | Deep-Cross: http + t | src/platform/five-plane-interface/api/ht | `decorateResponseHeaders()` sets CORS headers after route handlers have run. If a route handler sets `Access-Control-Allow-Origin` with a wildcard and | - | - | Add a flag to `ApiResponsePayload.headers` (e.g., `_corsHandled: true`) that the route handler sets  |
| 368 | low | Deep-Cross: http + t | src/platform/five-plane-interface/api/ht | `decorateResponseHeaders()` at line 133 unconditionally sets `headers.vary` to `"Accept-Encoding"` when no existing Vary header is present, overwritin | - | - | Preserve existing `Vary` values by merging: `headers.vary = existingVary ? \`${existingVary}, Accept |
| 369 | low | Deep-Cross: http + t | src/platform/five-plane-interface/channe | Uses `console` directly (`const logger = console`) for warning logs instead of `StructuredLogger`. At line 720, `logger?.warn?.` has optional chaining | - | - | Replace `console` with `StructuredLogger` imported from the observability module, matching the patte |
| 370 | low | Unknown | - | `execute()` checks action with `/^[a-zA-Z0-9_]+$/` (line 94). This allows any combination of alphanumeric and underscore characters. The CRM adapter s | - | - | Define an explicit enum or const array of valid actions rather than relying on regex validation. Val |
| 371 | low | Unknown | - | `getExecutionPolicy()` queries by `skillId` directly (line 287). If the database contains malformed `skillId` values (due to a prior bug or injection) | - | - | Add a CHECK constraint on the `skill_id` column in the database schema to enforce the same format as |
| 372 | low | Unknown | - | `computeSkillHealth()` returns `0.5` for skills with `executionCount === 0`. For skills with very few executions (e.g., 1-5), the health score is heav | - | - | Consider using a Bayesian approach with a prior that starts at neutral (0.5) and converges toward ob |
| 373 | low | Unknown | src/platform/five-plane-interface/channe | `circuitBreakerState` is a `Map<string, { failureCount: number; openUntil: number \| null }>`. When a circuit opens (failureCount >= threshold), `open | - | - | On circuit reset, delete the channel entry from the Map instead of setting `{ failureCount: 0, openU |
| 374 | low | Unknown | - | `disconnect()` in `SharedWorkerWSClient` sets `this.disconnected = true` and closes the port immediately: ```typescript public disconnect(): void { th | - | - | Add a flush mechanism for pending messages before disconnect. Consider setting a grace period where  |
| 375 | low | Unknown | - | In `sendTaskUpdate()`, if there is a sequence gap, a `stream_gap` message is sent to the client: ```typescript if (client.lastEventId != null && clien | - | - | Consider whether `stream_gap` messages inadvertently leak sequence state to clients. If this is a co |
| 376 | low | Unknown | - | `markSeen()` adds event IDs to `seenEventIds` Set without bound. The overflow pruning at lines 93-105 only triggers when `this.seenEventIds.size <= th | - | - | Change the pruning threshold to be percentage-based or use a sliding window. Instead of allowing 10x |
| 377 | low | Unknown | - | `verifyNonce()` inserts nonces into the database and the deletion of expired nonces happens in the same call: ```typescript // line 329-335 - delete h | - | - | Add a TTL index on `expires_at` and run cleanup on a schedule (e.g., every hour) via a background ta |
| 378 | low | Unknown | - | `rebuildAck` retrieves an event at line 560, then at line 562 checks `event.eventTier === "tier_1"`. If the event is not tier-1, the method returns `a | - | - | Add an early return with detail `"event_not_tier1_skipped"` when the event is not tier-1. Include th |
| 379 | low | Unknown | - | `MAX_RECOMMENDED_STICKY_SHARE = 0.6` (60%, worker-load-balancing.ts line 17) defines load skew detection threshold. `HealthService.buildWorkerHealthSu | - | - | Make `MAX_RECOMMENDED_STICKY_SHARE` configurable via `HealthServiceOptions`. Document the relationsh |
| 380 | low | Unknown | - | `StructuredMemoryContent` (lines 56-63) defines an interface with specific keys (`workContext`, `topOfMind`, `recentHistory`, `longTermBackground`, `f | - | - | Add an explicit `metadata: Record<string, unknown>` field to `StructuredMemoryContent` to capture un |
| 381 | low | Unknown | - | Uses `` `repl_${randomUUID()}` `` and `` `lag_${randomUUID()}` `` — uses underscore prefix consistent with canonical format, but does not use `newId() | - | - | Import `newId` from `ids.ts` and use `newId("repl")` / `newId("lag")`. |
| 382 | low | Unknown | - | `DEFAULT_BULKHEAD_CONFIG` (bulkhead-isolation.ts lines 20-24) defines `maxConcurrentCalls: 100`, `timeoutMs: 30_000`, `queueSize: 50`. This bulkhead i | - | - | Document the relationship between bulkhead timeout, circuit breaker reset timeout, and delivery time |
| 383 | low | Unknown | src/ops-maturity/drift-detection/evoluti | Uses `` `ev_${randomUUID()}` `` — prefix `ev` is undocumented and short. The `ids.ts` contract documents semantic prefixes but does not enumerate them | - | - | Establish a registered prefix catalog in `ids.ts` and use descriptive prefixes (e.g., `evolution_` i |
| 384 | low | Unknown | - | ```typescript } catch (error) { if ((error as { code?: string })?.code === "inspect.approval_not_found") { throw new ApiError(404, "api.approval_not_f | - | - | Use `AppError.isAppError(error)` type guard and `error.code` comparison with typed constants: ```typ |
| 385 | low | Unknown | - | `validateEventPayload()` (lines 1152-1175) accepts any payload for event types not in `EVENT_PAYLOAD_VALIDATORS` if the event type is registered in `E | - | - | In `validateEventPayload`, check `hasEventSchema()` first and throw a descriptive error if the event |
| 386 | low | Unknown | - | ```typescript } catch (error) { if (!(error instanceof AppError) \|\| error.code !== "workflow.not_found") { throw error; } // fallback lookup continu | - | - | Standardize on `isAppError()` type guard and ensure all error codes are defined as typed constants. |
| 387 | low | Unknown | - | The `expiresAt` for budget reservations is hardcoded: ```typescript expiresAt: new Date(Date.now() + 5 * 60 * 1000).toISOString(), ``` 5 minutes (300  | - | - | Derive the TTL from the budget policy's `maxDurationMs` or a configurable `reservationTtlMs` in the  |
| 388 | low | Unknown | - | The `writeStructuredLine()` function attempts to attach `requestId`, `traceId`, and `correlationId` from `currentRequest`, but if `currentRequest` is  | - | - | Consider generating a synthetic correlation ID for the pre-message phase so all logs are traceable. |
| 389 | low | Unknown | - | `calculateBackoffForAttempt()` uses a boolean condition that is always false in production: `if (attemptNumber <= 0 && new Error().stack?.includes(... | - | - | Remove the guard entirely or replace with a clear comment explaining why attemptNumber <= 0 is handl |
| 390 | low | Unknown | - | At line 397-402, if any P0 finding is detected, the method immediately builds the report, sets `_trafficBlocked = true`, calls `onTrafficBlocked()`, a | - | - | Continue gathering P1 findings even after a P0 is detected. Include them in the returned report with |
| 391 | low | Unknown | - | The file sink uses `process.stderr.write` directly for `file_sink_error` events. While this is a fallback mechanism (the file sink itself has failed), | - | - | This is an intentional last-resort fallback but could be enhanced by also attempting to emit via a s |
| 392 | low | Unknown | - | ```typescript } catch (err) { return { valid: false, error: err instanceof Error ? err.message : "jwt.validation_failed", claims: null, provider: null | - | - | Include error chain information in the result: ```typescript } catch (err) { return { valid: false,  |
| 393 | low | Unknown | - | The flow is: ```typescript if (record.decisionRef.trim().length === 0) { this.unsourcedRecordCount += 1; throw new Error(`cost_optimizer.unsourced_rec | - | - | Consider batching invalid records and reporting them as a single warning rather than throwing indivi |
| 394 | low | Unknown | src/scale-ecosystem/runtime-services/exe | Uses `` `wb_${randomUUID()}` `` — uses custom prefix `wb` (writeback) not in canonical format and not using `newId()`. | - | - | Use `` newId("writeback") ``. |
| 395 | low | Unknown | - | When `metrics.requestCount < minimumRequestCount`, `evaluate()` returns `{ rollback: false, reasonCodes: ["rollout.metrics_insufficient_sample"] }`. T | - | - | Clearly separate "evaluation not possible" from "evaluation passed". Use a different field like `eva |
| 396 | low | Unknown | - | `unwrapWorkflowStepCheckpoint` calls `unpackCheckpointEnvelope(envelope, options)` at line 393 but `options` is `{ maxSizeBytes?: number }` from the o | - | - | The current implementation is functionally correct. Add a runtime type check on the returned data to |
| 397 | low | Unknown | - | ```typescript } catch (err) { logger.warn("gateway.parse_webhook_payload_failed", { ... }); if (err instanceof AppError && err.code === "api.invalid_j | - | - | Use typed error code constants from a shared module. |
| 398 | low | Unknown | - | Both catch blocks capture `err` but do not log it: ```typescript } catch (err) { throw new MigrationError(..., { cause: err }); } ``` Using `Migration | - | - | Add `logger.warn` before throwing. |
| 399 | low | Unknown | src/scale-ecosystem/runtime-services/exe | Uses `` `op_${randomUUID()}` `` — prefix `op` (operation) is undocumented and not using `newId()`. | - | - | Use `` newId("operation") ``. |
| 400 | low | Unknown | - | The `Component` getter wraps render in try-catch but this will not intercept React rendering errors properly. React error boundaries work differently. | - | - | Use React's `componentDidCatch` or an ErrorBoundary component properly. |
| 401 | low | Unknown | src/scale-ecosystem/runtime-services/exe | Same `` `op_${randomUUID()}` `` pattern as execution-dispatch-service. | - | - | Use `` newId("handshake") ``. |
| 402 | low | Unknown | - | `scanExecutionCheckpoints` filters files with `.endsWith(".checkpoint.json")`. This is a simplistic heuristic that could accidentally pick up non-chec | - | - | After reading a file, validate it contains a known checkpoint schema version before treating it as a |
| 403 | low | Unknown | src/domains/registry/plugin-runtime-host | ```typescript let template: unknown; try { template = JSON.parse(rawTemplate); } catch { throw new ValidationError( "plugin_spi.container_launcher_inv | - | - | Capture `err` and include it in the `ValidationError.details` or log it before throwing. |
| 404 | low | Unknown | src/scale-ecosystem/runtime-services/hum | Uses `` `hto_${randomUUID()}` `` — prefix `hto` (human take-over) is undocumented, not using `newId()`. | - | - | Use `` newId("human_takeover") ``. |
| 405 | low | Unknown | - | When the file sink fails, it writes directly to `process.stderr`. While intentional as last-resort, critical errors may be lost if stderr is also fail | - | - | Consider emitting to a secondary transport when the primary file sink fails. |
| 406 | low | Unknown | - | ```typescript const globalPluginTaintTracker = new PluginTaintTracker(); ``` The `PluginTaintTracker` is a global singleton with mutable state. This c | - | - | Consider making the taint tracker injectable via registry rather than a global singleton. |
| 407 | low | Unknown | - | `safeHashEquals()` uses constant-time comparison suggesting security intent, but the function is used for fingerprint equality. Purpose is undocumente | - | - | Document why constant-time comparison is needed. |
| 408 | low | Unknown | - | The `attempt >= this.maxAttempts` check throws AFTER fn() has been called and failed in the current iteration. This means for any `shouldRetry` predic | - | - | Clarify boundary: check attempt limit BEFORE fn() call, not after fn() throws. |
| 409 | low | Unknown | - | The size limit is checked twice: first against `originalSizeBytes` at line 140 (before compression), and then against `compressedSizeBytes` at line 14 | - | - | Remove the pre-compression size check. Only enforce the limit on the compressed result, which is the |
| 410 | low | Unknown | - | `execution-dispatch-support.ts` defines `AFFINITY_SELECTION_BONUS = 0.2` and `LOAD_SKEW_SELECTION_PENALTY = 0.35` for worker selection. `channel-gatew | - | - | Extract shared retry/backoff constants to a common `reliability-constants.ts` module that both `exec |
| 411 | low | Unknown | - | `riskLevelForSubject()` upgrades risk when a record has `costType === "llm"` or `costType === "model"`: ```typescript if (records.some((item) => item. | - | - | Verify if `"model"` is a legacy or intended cost type. If intended, add `"model"` to the `CostSubjec |
| 412 | low | Unknown | src/platform/five-plane-control-plane/in | `generateIncidentId()` uses `` `incident_${crypto.randomUUID()}` `` — directly accessing `crypto.randomUUID()` rather than importing `randomUUID` from | - | - | Import `randomUUID` from `node:crypto` and use the canonical pattern `newId("incident")`. |
| 413 | low | Unknown | - | When checksum verification fails, the code logs at `logger.error` level (line 224) with `message: "Checkpoint envelope checksum mismatch"`. For a deli | - | - | Change log level to "warn" for checksum mismatch. The exception carrying `CheckpointEnvelopeInvalidE |
| 414 | low | Unknown | - | In `sqlite-database.ts`, `isMigrationChecksumCompatible()` (lines 1013-1017) checks `existingChecksum === migration.checksum \|\| existingChecksum === | - | - | Add a validation pass in `evaluateSqliteMigrationCompatibility` that cross-references `migration.app |
| 415 | low | Unknown | - | The catalog defines `baselineServices` as `readonly string[]` but many services registered in bootstrap functions use class names that may not match e | - | - | Consider adding a registry validation step during bootstrap that checks all `baselineServices` refer |
| 416 | low | Unknown | - | Multiple methods use `` `resolution_${crypto.randomUUID()}` ``, `` `postmortem_${crypto.randomUUID()}` ``, and `` `actionitem_${crypto.randomUUID()}`  | - | - | Centralize via `newId()` with consistent prefix naming. |
| 417 | low | Unknown | src/platform/five-plane-control-plane/co | Analysis ID uses `` `impact-${randomUUID()}` `` — uses hyphen separator (`impact-`) instead of underscore (`impact_`) which is the canonical separator | - | - | Use `` newId("impact") `` for canonical format. |
| 418 | low | Unknown | - | `actualizeCostEvent()` handles three cases for `split`: ```typescript if (!byok \|\| split === "platform_governance") { platformGovernanceCostUsd = ob | - | - | Add an `else` clause that throws or logs an error for unknown `split` values rather than silently fa |
| 419 | low | Unknown | src/platform/shared/cache/cache-invalida | Instance ID uses `` `inst_${randomUUID()}` `` — prefix `inst` is not in the canonical list of entity type prefixes documented in `ids.ts`. The canonic | - | - | Add `inst` to the documented prefix conventions or use a more descriptive prefix like `cache_inst`. |
| 420 | low | Unknown | - | ```typescript } catch (error) { throwOidcProviderError("oauth.token_exchange_transport_failed", { issuer: provider.issuer, error: error instanceof Err | - | - | ```typescript } catch (error) { throwOidcProviderError("oauth.token_exchange_transport_failed", { is |
| 421 | low | interaction | src/interaction/autonomy/autonomy-govern | `canPromote()` uses `compareAutonomyLevels(targetLevel, this.getMaxAutonomyLevel(agentId)) <= 0`. This correctly checks that `targetLevel` is not high | - | - | Clarify the boundary condition: `compareAutonomyLevels(targetLevel, maxAutonomy) < 0` means strictly |
| 422 | low | interaction | src/interaction/dashboard/dashboard-webs | `assertRequiredIdentity()` throws on missing `principal` or `tenantId` but the error messages are generic ("Principal is required", "Tenant ID is requ | - | - | Include a sanitized error code that indicates which field was missing without leaking the value. For |
| 423 | low | interaction | src/interaction/ux/ux-event-tracking-ser | `maxEventLogSize` is set to 1000 but the trimming logic at lines 136-137 uses `this.eventLog.splice(0, this.eventLog.length - this.maxEventLogSize)`.  | - | - | The behavior is correct but consider using `this.eventLog.splice(0, this.eventLog.length - this.maxE |
| 424 | low | interaction | src/interaction/nl-gateway/nl-gateway-mo | `IntentParseResult` has `riskClassification` as an optional property (line 74-78 shows it with `?`), but in `nl-gateway-support.ts` line 386, the `ris | - | - | Make `riskClassification` a required field in `IntentParseResult` since `parseDetailed()` always pop |
| 425 | low | interaction | src/interaction/nl-gateway/intent-parser | The single-word approval detection at line 132 has a regex `/approve\|审批\|通过\|批准/i` but the message length check `message.trim().length < 10` does not | - | - | Add explicit handling for Chinese approval phrases in the short-circuit condition, or document the a |
| 426 | low | interaction | src/interaction/proactive-agent/schedule | `shouldRunScheduleTrigger()` compares `Date.parse(nowIso) - Date.parse(lastFiredAt) >= parseDurationMs(cooldown)`. `Date.parse` is legacy API and may  | - | - | Use `new Date(nowIso).getTime()` and `new Date(lastFiredAt).getTime()` consistently, or document the |
| 427 | low | ops-maturity | src/ops-maturity/capacity-planner/capaci | `variance` calculation divides by `Math.max(1, usageValues.length - 1)` which is the sample variance formula (Bessel's correction). However, `standard | - | - | Use `Math.max(0, variance)` instead of `Math.max(variance, 0)` to properly handle negative floating- |
| 428 | low | ops-maturity | src/ops-maturity/cost-optimizer/cost-opt | `riskLevelForSubject()` upgrades risk to "medium" when a record has `costType === "llm"` or `costType === "model"`. However, `costType === "model"` is | - | - | Clarify whether `"model"` is a valid cost type. If not, remove it from the check. If it is valid, en |
| 429 | low | ops-maturity | src/ops-maturity/drift-detection/learnin | The promotion gate evaluates multiple independent criteria and accumulates reasons, but the `allowed` flag is `reasons.length === 0`. If one criterion | - | - | Consider short-circuiting on first failure for high-cost evaluations by checking a `passed` boolean  |
| 430 | low | ops-maturity | src/ops-maturity/chaos/chaos-experiment- | `executeRollbackWithTimeout()` creates an AbortController but does not call `controller.abort()` when the experiment is found to not exist or during e | - | - | Call `controller.abort()` with an appropriate error message in all early-return paths within `execut |
| 431 | low | ops-maturity | src/ops-maturity/version-management/vers | When checking wildcard source version entries, the `for...of` loop mutates entries by adding a `recommendedProfile` property via spread in `buildCostO | - | - | Ensure `findLowerCostPeerProfile()` returns a new object each call rather than returning a shared re |
| 432 | low | ops-maturity | src/ops-maturity/compliance-reporter/com | `getActiveViolations()` returns violations filtered by `remediatedAt == null` but the returned array is not sorted or limited. Over time, this could r | - | - | Add pagination or a limit parameter to `getActiveViolations()` to prevent unbounded return sizes. |
| 433 | low | ops-maturity | src/ops-maturity/drift-detection/drift-d | `safeHashEquals()` converts both inputs to Buffers using `Buffer.from(left, "utf8")`. This is a constant-time comparison for the purpose of timing att | - | - | Document why constant-time comparison is needed here, or use a simpler equality check if timing safe |
| 434 | low | ops-maturity | src/ops-maturity/drift-detection/changep | `hoursToSamples()` uses `Math.round(hours * samplesPerHour)` and then `Math.max(1, ...)`. For fractional hour inputs like `0.25` (15 minutes), this ro | - | - | Add documentation explaining the rounding behavior and that the minimum sample count is always at le |
| 435 | low | platform/five-plane- | src/platform/five-plane-execution/budget | The settle method has: `const expectedVersion = input.expectedVersion ?? input.ledger.version; if (input.ledger.version !== expectedVersion) { throwVe | - | - | Remove the redundant version check before `persistLedger`. Let `persistLedger` handle the CAS verifi |
| 436 | low | platform/five-plane- | src/platform/five-plane-execution/execut | The effect builder creates IDs using `Date.now()` for the first part of the ID: `id: 'effect_${Date.now()}_${randomUUID()}'`. If two effects are creat | - | - | Document that effect IDs are designed to be unique even when created in rapid succession, and that t |
| 437 | low | platform/five-plane- | src/platform/five-plane-execution/execut | The step execution loop has two nested structures: an outer `for` loop over `plannedWorkflow.executionSteps` and an inner `for` loop for retry attempt | - | - | When a step is skipped, reset `workflowLastErrorCode = null` to avoid carry-over of previous step er |
| 438 | low | plugins | src/plugins/retrievers/coding-retriever. | The `score` field in `RetrieverKnowledgeResult` is typed as `number` in `plugin-spi.ts` (line 130), but at line 57 the code uses: ```typescript score: | - | - | Simplify to `score: search.relevanceScores.get(...) ?? 0.8` and ensure `relevanceScores` always stor |
| 439 | low | plugins | src/plugins/retrievers/game-dev-retrieve | Multiple `score` calculations use `Math.min(0.97, 0.7 + Math.min(searchQuery.length / 240, 0.18))`. If `searchQuery` is extremely long, the inner `Mat | - | - | Document the scoring formula and consider extracting scoring constants into a shared configuration f |
| 440 | low | plugins | src/plugins/adapters/github-adapter.ts:2 | `execute()` calls `requireRepository(params["repository"])` then `buildEndpoint()` which can throw for unknown actions before `policy.evaluate(endpoin | - | - | Call `policy.evaluate()` with the unvalidated action before `buildEndpoint()`, or validate the actio |
| 441 | low | plugins | src/plugins/adapters/crm-adapter.ts:94 | The action validation regex `/^[a-zA-Z0-9_]+$/` allows alphanumeric and underscore characters. However, CRM API endpoints can include hyphens (e.g., ` | - | - | Extend the regex to include hyphens if the CRM API uses them, or document the allowed action format  |
| 442 | low | plugins | src/plugins/adapters/github-adapter.ts:1 | `createIdempotencyKey()` returns `null` for `get_file` actions, but this is treated differently at line 244-254 where `idempotencyKey` is spread into  | - | - | Use an option bag pattern or explicit `undefined` return to distinguish intentional no-key from erro |
| 443 | low | tests/unit | - | The UI `HttpTransport` has a circuit breaker (rest-client.ts lines 199-203: `failures`, `lastFailure`, `state`) with: - `failureThreshold: 5` failures | - | - | Document the different resilience strategies, or extract a shared resilience configuration object th |
| 444 | low | tests/unit | - | The UI `BrowserWSClient` calculates reconnect delay as (ws-client.ts lines 305-311): ```typescript const exponentialDelay = Math.min(baseReconnectDela | - | - | Document the different reconnect strategies (WebSocket vs SSE) and ensure they are intentionally dif |
| 445 | low | tests/unit | - | `bootstrapVerticalDomainBaselines()` at line 608 registers domains and then activates them (line 622-624). Knowledge namespaces are registered at line | - | - | Register knowledge namespaces BEFORE activation in the bootstrap sequence. Ensure the namespace regi |
| 446 | low | tests/unit | tests/unit/plugins.test.ts:334 | Test `PluginSpiRegistry can register validator and invoke it` uses type casting `(plugin as any).validate` to access the `validate` method. This bypas | - | - | If the plugin interface includes `DomainValidatorPlugin` type, the test should access the method thr |
| 447 | low | tests/unit | - | `ExecutionDispatchService` uses a hardcoded `heartbeatStalenessThresholdMs = 30_000` (line 580) to reject workers with stale heartbeats. `RpoRtoTracki | - | - | Derive the heartbeat staleness threshold from the RTO target: `heartbeatStalenessThresholdMs = Math. |
| 448 | low | tests/unit | tests/unit/runtime/dispatcher.test.ts:1 | Tests for `executeMultiStepToolCallForTests` return JSON strings that are then parsed with `JSON.parse(result)`. Some tests check `parsed.success ===  | - | - | Ensure all tests validate the complete structure of the result object, including both success and er |
| 449 | low | tests/unit | - | - Domain onboarding has `security_certification` phase - Pack lifecycle has `certifying` stage - These are semantically similar but: 1. Different term | - | - | Standardize terminology. Either: - Rename pack `certifying` to `security_certification` to match dom |
| 450 | low | tests/unit | tests/unit/interaction-governance-runtim | This file tests `buildInteractionGovernanceRuntimeCatalog` but the parallel file at `interaction-governance/interaction-governance-runtime-catalog.tes | - | - | Consolidate the test coverage into a single file (preferably the more thorough one in `interaction-g |
| 451 | low | tests/unit | - | The test asserts `validateExecutableContract("RequestEnvelope", { requestId: "" })` throws `ValidationError`. However, `{ requestId: "" }` is not a va | - | - | Check the specific error code: ```typescript try { validateExecutableContract("RequestEnvelope", { r |
| 452 | low | tests/unit | tests/unit/domains-runtime-orchestrator. | Tests `registerDomainsRuntimeOrchestrator registers orchestrator in registry` and similar tests that call `registerDomainsRuntimeOrchestrator(registry | - | - | Establish a consistent pattern of `test.beforeEach(async () => { await registry.reset(); })` at the  |
| 453 | low | tests/unit | tests/unit/platform-architecture-bootstr | Test `registerPlatformArchitectureServices enforces startup ordering before registration` uses `readFileSync` to read source code and then does a stri | - | - | Either remove this test if the startup ordering is enforced through runtime behavior checked in othe |
| 454 | low | tests/unit | - | `buildKnowledgeSchema()` at line 331 creates namespaces like `${seed.domainId}/default` and `${seed.domainId}/governed`. The checkpoint envelope at li | - | - | Add `namespaceId?: string` to `CheckpointEnvelopeMetadata`. When creating checkpoints for domain-spe |
| 455 | low | tests/unit | tests/unit/platform-root-types.test.ts:9 | Test `PlatformRootSummaryBuilderDeps structure validation` uses `as any` casts extensively to force-fit incompatible return types into the mock struct | - | - | Create properly typed mock implementations that return objects matching the expected shapes. Using ` |
| 456 | low | tests/unit | tests/unit/domains-domain-recipe-service | The test `DomainRecipeService.getPrototypeTemplates returns all 12 templates` hardcodes the expected count of 12 templates. If new prototype templates | - | - | If 12 is a contractual number that should not change, document this in an comment. If it's subject t |
| 457 | low | tests/unit | - | Tests at lines 234-256 use `createMockBundle("TestBundle", "v1.0")` and then query with `manager.getVersionLineage("TestBundle", "v1.0")`. The bundle' | - | - | Add comments explaining that `registerBundleVersion` uses displayVersion as the key, and the tests u |
| 458 | low | tests/unit | - | `DomainRegistryService.buildCapabilityEntry()` at line 330 includes `knowledgeNamespaces: [...(this.knowledgeNamespacesByDomain.get(domainId) ?? new S | - | - | If sessions can contain knowledge operations, add a `validateSessionKnowledgeNamespaces(events: Sess |
| 459 | low | tests/unit | - | The `PromptBundleCompatibilityMatrix` interface (lines 15-24) defines four required arrays: `toolSchemaVersions`, `evaluatorSchemaVersions`, `domainDe | - | - | Add test for `validateCompatibilityMatrixShape` rejection of invalid input. |
| 460 | low | tests/unit | - | `DomainBaseline.metaModel` is built via `seedDomainMetaModel()` (line 510) and validated via `MetaModelValidator` (line 518). The meta model tracks do | - | - | Add a `validateCheckpointSchema(domainId: string, schema: string): boolean` method to `DomainRegistr |
| 461 | low | tests/unit | tests/unit/domains-runtime-catalog.test. | Test `buildDomainsRuntimeCatalog ring counts match expected baseline counts` uses exact equality assertions (`assert.equal(catalog.ring1.length, 8)`). | - | - | Centralize these magic numbers. Either create a shared constants file that both tests import, or der |
| 462 | low | tests/unit | tests/unit/platform-mainline-bootstrap.t | Test `criticalSubmodules arrays are non-empty for all capabilities` and `architectureSections arrays are non-empty for all capabilities` use loose ass | - | - | These tests are adequate as-is given TypeScript's type system ensures these are arrays. The severity |
| 463 | low | tests/unit | - | The `assertGraphPatchSafety()` function (lines 219-246) has two checks: 1. `safe_append` cannot affect executed nodes or side effects (lines 221-226)  | - | - | Add test cases for: 1. `safe_append` with `affectedExecutedNodes` that don't match any operation tar |
| 464 | low | tests/unit | - | `DomainRegistryService` has a 7-state lifecycle: `draft → registered → canary → active → updating → deprecated → archived`. `SessionDualStorageService | - | - | Add `domain_lifecycle_transition` to `SessionEventType` union. When `DomainRegistryService` publishe |
| 465 |  | Unknown | - | `epochCounter` is a simple in-memory `number` that is incremented at line 94 inside `acquireLeadership()` and persisted to disk via `persistState()` ( | - | - | Use an atomic increment operation (e.g., a distributed lock or atomic compare-and-swap in the persis |
| 466 |  | Unknown | - | `assertFailoverWithinRto()` at lines 634–643 and `completeFailoverWithRtoCheck()` at lines 649–659 both compute `elapsedMs` from an in-memory timer st | - | - | Gate the RTO assertion on a confirmed execution lease handover completion, not just the failover rec |
| 467 |  | Unknown | - | `confirmBatch()` (lines 326–346) updates the checkpoint and removes the batch from the queue **in two separate steps** without a transactional boundar | - | - | Atomically persist checkpoint and queue removal together, or use an idempotency key on events to ded |
| 468 |  | Unknown | - | `validateFencingToken()` at lines 157–217 checks: 1. `current.regionId !== token.regionId` (line 182) 2. `current.epoch !== token.epoch` (line 192) 3. | - | - | Track used `tokenId` values in a set or use a nonce-based scheme to detect replays. |
| 469 |  | Unknown | - | `startFailover()` and `completeFailover()` use `toPairKey(sourceRegionId, targetRegionId)` which returns `"source:target"`. If Region A fails over to  | - | - | Use an array of failover events per pair instead of a single mutable event, or validate there is no  |
| 470 |  | Unknown | - | The split-brain detection at lines 150–166 compares fencing epochs and triggers evidence if `Math.abs(epochA - epochB) >= 1` (line 157). This means ** | - | - | Only trigger evidence when epochs are **equal** but leaders are **different** (true concurrent leade |
| 471 |  | Unknown | - | `EpochManager.isStaleDemotedLeader()` at lines 151–158 uses `demotedLeaders.has(`${key}:${regionId}`)` to determine if a region was previously demoted | - | - | Add a timestamp or epoch number to the demoted leader record and clear it after a successful rejoin- |
| 472 |  | Unknown | - | `validateWriteAccess()` at lines 622–750 performs multiple checks in sequence. The fencing token check at line 683 fires **before** the TTL/lease-expi | - | - | - |
| 473 |  | Unknown | - | `updateHealthState()` at lines 480–487 updates `healthResults` and `lastCheckTime` in two separate Map operations. If a crash occurs between the two w | - | - | - |
| 474 |  | Unknown | - | `prepareBatch()` calls `enqueueBatch()` (line 318) to queue the batch for async replication **before** the previous batch has been confirmed. The queu | - | - | - |
| 475 |  | Unknown | - | `runReconciliation()` at lines 116–218 accepts a `ReconciliationJobInput` and **creates artificial issues** based on the counts in the input (e.g., li | - | - | Document this clearly and ensure callers have the actual gap enumeration logic implemented before ca |
| 476 |  | Unknown | - | The temp file path at line 293 includes `${process.pid}`: ```typescript const tempPath = `${this.storagePath}.${process.pid}.${Date.now()}.tmp`; ``` I | - | - | - |
| 477 |  | Unknown | src/index.ts:85 | `process.stdout.write` is used for the platform root summary output (structured JSON for the CLI runner). This is intentional and appropriate for CLI  | - | - | Consider adding a comment clarifying that stdout writes for CLI output are intentional and not viola |
| 478 |  | tests/unit | - | The `Session` interface (session-management.ts lines 50-62) has no `tenantId` field. The `TenantScopeFilter.evaluate()` at lines 33-35 checks `princip | - | - | Create an ADR documenting: 1. The session-to-tenant resolution path 2. Which component is authoritat |
| 479 |  | tests/unit | - | When a request arrives at `HttpApiServer` with both user auth headers (e.g., `Authorization: Bearer <token>`) AND service auth headers (e.g., `x-servi | - | - | Create an ADR documenting: 1. The auth header precedence model (service vs user) 2. Whether service  |
| 480 |  | tests/unit | - | The self-healing service uses deterministic formulas based on string length parity (`componentId.length % 2`) and modular arithmetic (`deterministicSc | - | - | Create an ADR documenting: 1. The deterministic success rate model and why it was chosen (presumably |
| 481 |  | tests/unit | - | Rate limit keys differ between `inject()` and `handleRequest()`: - `inject()`: `inject:${endpoint}` (no tenant, no IP) - `handleRequest()`: `${clientI | - | - | Create an ADR documenting: 1. The rate limit key design principles (what dimensions are used: tenant |
| 482 |  | tests/unit | - | R4-28 introduced write-ahead logging for cost events. The code at line 577 inserts with "pending" status and at line 594 commits inside the transactio | - | - | Create an ADR documenting: 1. The WAL recovery sweep frequency and entry age threshold for "orphaned |
| 483 |  | tests/unit | - | The allowList bypass in `evaluateExecution()` effectively disables panic mode enforcement for privileged actors. This is a significant security decisi | - | - | Create an ADR documenting: 1. The allowList membership criteria and approval process 2. What operati |