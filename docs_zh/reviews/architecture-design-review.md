# Architecture Design Review

**Review Date**: 2026/05/13
**Reviewer**: Automated Code Review
**Scope**: src, tests, ui, docs_zh, config, scripts

---

## 1. 代码质量和架构问题

### 1.1 项目结构概览
- **总源文件数**: 6,113 个 TypeScript 文件
- **运行时核心**: `src/platform/` 包含五层架构 (interface, control-plane, orchestration, execution, state-evidence)
- **上层业务**: `src/domains/` (34个领域), `src/interaction/`, `src/org-governance/`, `src/ops-maturity/`, `src/scale-ecosystem/`

### 1.2 架构观察

**优点**:
- 清晰的五层平台架构分离 (docs_zh/architecture/00-platform-architecture.md)
- 151个契约文档覆盖关键系统边界 (docs_zh/contracts/)
- 使用ESM模块系统和严格的TypeScript配置 (`"strict": true`, `noImplicitOverride`, `exactOptionalPropertyTypes`)
- 分散式领域架构允许独立扩展

**问题**:
1. **符号链接使用**: `src/platform/` 下存在多个符号链接 (`control-plane -> five-plane-control-plane`, `execution -> five-plane-execution` 等)，这可能导致:
   - IDE和工具的路径解析问题
   - Git历史追踪困难
   - 构建配置复杂化

2. **core/runtime 警告**: CLAUDE.md 明确指出 `src/core/runtime/` 是兼容性目录，不应添加新的规范运行时逻辑。但项目仍在使用，需要清理或重构。

3. **配置分散**: 配置分散在多个位置:
   - `config/` (主配置)
   - `.env.example` (环境变量模板)
   - `config/environments/` (dev/staging/prod等)
   - `config/security/` (安全配置)

4. **类型安全**: TypeScript配置中 `noUncheckedIndexedAccess: true` 和 `exactOptionalPropertyTypes: true` 已启用，这是好的实践。

### 1.3 代码组织问题

**过长的import语句**: 查看源文件发现大量deep import，例如:
```typescript
import { BudgetAllocator } from "../../../../src/platform/five-plane-execution/budget-allocator.js";
```
应优先使用包导出 (`import { BudgetAllocator } from "@automatic-agent/platform/execution"`)

**测试文件与源文件比例**: 排除的测试文件列表过长 (在tsconfig.json的exclude中)，表明可能存在:
- 测试并行化问题
- 测试污染问题
- 循环依赖问题

---

## 2. 测试覆盖情况

### 2.1 测试统计
| 类型 | 数量 |
|------|------|
| 单元测试文件 | 3,584 |
| 集成测试文件 | 134 |
| E2E测试文件 | 834 |
| 源码文件 | 6,113 |

### 2.2 测试组织
```
tests/
├── unit/           # 单元测试 (87个子目录)
├── integration/    # 集成测试 (42个子目录)
├── e2e/           # E2E测试 (332个子目录)
├── golden/        # 金丝雀测试 (48个)
├── invariants/    # 不变式测试 (87个)
├── helpers/       # 测试辅助 (78个)
├── performance/  # 性能测试 (58个)
```

### 2.3 问题观察

**问题**:
1. **单元测试与源码比例异常**: 3,584个单元测试文件 vs 6,113个源文件，比例约0.59:1。可能存在:
   - 大量测试文件未被正确计数
   - 某些源文件没有对应测试
   - 测试文件过度拆分

2. **tsconfig.json exclude列表过长**: 排除了大量测试文件，表明:
   - 某些测试可能处于不稳定状态
   - 或存在循环依赖问题
   - 测试基础设施可能需要修复

3. **测试辅助代码量大**: 78个helpers文件说明测试基础设施复杂，可能表明:
   - 被测代码耦合度高
   - 测试设置复杂
   - Mock对象管理困难

4. **Golden测试**: 48个golden文件用于回归测试，这是良好实践。

---

## 3. 文档完整性

### 3.1 文档结构
```
docs_zh/
├── architecture/     # 架构文档 (728KB 00-platform-architecture.md)
├── contracts/        # 151个契约文档
├── adr/             # 架构决策记录
├── domains/         # 领域文档
├── guides/          # 快速入门、贡献指南等
├── operations/      # 运维文档 (runbook等)
├── reviews/         # 本次review位置
└── quality/         # 质量文档
```

### 3.2 文档观察

**优点**:
- 详尽的架构文档 (00-platform-architecture.md 728KB)
- 完整的契约文档集 (151个)
- ADR决策记录完整
- 运维文档包含runbook和检查清单

**问题**:
1. **文档版本控制**: 未明确看到API版本控制策略文档
2. **CHANGELOG.md**: 存在但大小仅6KB，相对较小，可能记录不完整
3. **跨语言文档**: docs_zh/ 和 docs_en/ 并存，需要确保同步
4. **CLAUDE.md**: 存在且包含有用信息，但可考虑增加更多示例

---

## 4. 配置问题

### 4.1 配置结构
```
config/
├── environments/    # dev.json, staging.json, prod.json等
├── security/       # 安全配置 (default.json, threat-matrix.json)
├── ...其他配置文件
```

### 4.2 安全配置
- `config/security/default.json`: 定义了approvalMode, sandboxMode等安全策略
- `config/security/threat-matrix.json`: STRIDE威胁矩阵完整
- JWT secret要求在生产环境必须配置

### 4.3 问题观察

1. **环境配置差异**: 检查发现 `config/security/dev.json` 和 `config/security/test.json` 内容相同 (都是`{}`)，但生产配置有实际内容。

2. **.env.example完整性**: `.env.example` 文件非常详尽 (8KB)，这是好的实践，但需要注意:
   - 包含大量注释说明
   - 生产环境需要填充真实值

3. **配置验证**: 未在代码库中看到明确的配置验证机制 (除了schema validation mentioned in threat-matrix)

4. **敏感信息**: 配置文件中的敏感信息处理需要确认是否通过环境变量注入

---

## 5. Bash脚本问题

### 5.1 脚本列表
```
scripts/
├── backup-sqlite.sh       # SQLite备份
├── restore-sqlite.sh     # SQLite恢复
├── ci/                   # CI脚本
│   ├── check-changelog.mjs
│   ├── check-coverage-baseline.mjs
│   ├── coverage-lib.mjs
│   ├── generate-coverage-report.mjs
│   ├── mutation-critical-tests.sh
│   └── update-coverage-baseline.mjs
├── clean-dist.mjs
├── generate-src-module-test-matrix.mjs
├── reorg-code-structure.mjs
├── run-curated-tests.mjs
├── run-layered-tests.mjs
└── run-tracked-tests.mjs
```

### 5.2 脚本质量观察

**backup-sqlite.sh 优点**:
- 使用 `set -euo pipefail` 正确处理错误
- 有PID锁文件防止并发运行
- 备份后验证完整性
- 清理过期备份

**restore-sqlite.sh 优点**:
- 创建预恢复快照
- 恢复前验证备份完整性
- 错误处理完善

**问题**:
1. **脚本位置**: 部分脚本在 `scripts/` 根目录，部分在 `scripts/ci/`，组织稍显混乱
2. **Python脚本**: 存在 `translate_docs.py` (10KB)，需要确认是否需要维护
3. **mjs vs sh**: 混用 `.mjs` (Node.js ESM) 和 `.sh` (Bash)，需要确认团队技能匹配

---

## 6. 安全问题

### 6.1 安全配置
- **JWT Secret**: 必须配置，用于API认证
- **MCP策略**: `config/security/default.json` 定义了MCP (Model Context Protocol) 策略
- **沙箱模式**: read_only 模式限制工具权限
- **远程Worker注册**: 使用challenge-based认证

### 6.2 威胁矩阵覆盖
STRIDE模型覆盖:
- **Spoofing**: JWT和workspace身份验证, challenge-based worker注册
- **Tampering**: 审计哈希链, 只追加事件证据, 配置schema验证
- **Repudiation**: 操作员行为审计证据, 事件时间线导出
- **Information Disclosure**: 字段加密, 网络出口策略, 内存加密
- **Denial of Service**: SLO告警, runbook引导的containment, rollout freeze gates
- **Elevation of Privilege**: 沙箱策略执行, 策略引擎能力检查

### 6.3 安全观察

**优点**:
- 完整的安全配置和威胁矩阵
- 内存隔离按workspace/session
- 配置更改审计日志

**问题**:
1. **Secret管理**: `.env.example` 中 `AA_API_JWT_SECRET=` 为空，生产必须配置，但:
   - 未看到secret轮换策略文档
   - 未看到secret存储方案 (应使用Vault/KMS等)

2. **MCP安全**: MCP策略允许 `allowedCapabilities: ["edit", "mcp"]`，需要确保:
   - 网络隔离配置正确
   - 传输层只允许stdio

3. **数据库凭证**: PostgreSQL DSN在环境变量中，需要确保:
   - 不是硬编码
   - 有安全的注入机制

4. **加密密钥**: 内存加密密钥管理未在代码中明确看到

---

## 7. UI代码审查

### 7.1 UI结构
```
ui/
├── apps/
├── packages/
├── tests/
├── .storybook/
├── eslint.config.js
├── lighthouserc.json
├── playwright.config.ts
├── package.json
└── tsconfig.json
```

### 7.2 UI观察
- 使用TypeScript + Playwright进行E2E测试
- Storybook用于组件开发
- 独立的lighthouserc.json进行性能监控
- 独立包管理 (有自己的node_modules)

**问题**:
1. **UI与主项目分离**: UI有自己的package.json，表明可能需要独立部署
2. **测试配置**: Playwright配置存在，但需要确认CI中是否运行

---

## 8. 部署和基础设施

### 8.1 部署配置
```
deploy/
├── chaos/
├── grafana/
├── helm/
├── prometheus/
├── runbooks/
├── scripts/
└── terraform/
```

### 8.2 容器化
- `Dockerfile` 存在
- `docker-compose.yml` 存在 (3.2KB)
- `.dockerignore` 存在

---

## 9. 总结与建议

### 9.1 高优先级
1. **清理符号链接**: `src/platform/` 下的符号链接应转换为实际模块或移除
2. **Secret管理**: 建立正式的secret管理机制和轮换策略
3. **测试稳定性**: tsconfig.json中排除的大量测试文件应被修复或标记为已知问题
4. **core/runtime清理**: 按CLAUDE.md指示，清理或重构 `src/core/runtime/`

### 9.2 中优先级
1. **文档同步**: 确保 docs_zh 和 docs_en 内容同步
2. **配置验证**: 增加启动时配置验证
3. **测试基础设施**: 考虑简化78个测试helpers
4. **CI脚本组织**: 统一scripts目录结构

### 9.3 低优先级
1. **Python脚本维护**: 检查 `translate_docs.py` 是否需要
2. **UI独立部署**: 确认UI服务架构
3. **CHANGELOG完善**: 增加更多变更细节

---

## 2026/05/14 自动Review报告 (详细补充)

### 发现的问题

#### 1. [源代码] [高严重] [符号链接导致构建不一致]
- **文件/路径**: `src/platform/` 目录下的5个符号链接
- **问题描述**: `control-plane`, `execution`, `state-evidence`, `orchestration`, `interface` 均为符号链接指向 `five-plane-*` 目录。这导致：
  1. TypeScript 编译器路径解析可能不一致
  2. IDE 代码跳转和自动完成可能失效
  3. Git 历史追踪困难（无法追踪哪个文件实际被修改）
  4. 符号链接可能在不同操作系统或文件系统上表现不同
  5. 构建产物路径可能与源码路径不匹配
- **建议修复**: 将符号链接转换为实际目录或通过构建工具创建虚拟模块映射

#### 2. [源代码] [中严重] [巨型单文件问题]
- **文件/路径**: 
  - `src/platform/five-plane-execution/budget-allocator.ts` (931行)
  - `src/platform/five-plane-execution/runtime-state-machine.ts` (690行)
  - `src/platform/five-plane-state-evidence/events/durable-event-bus.ts` (1214行)
- **问题描述**: 多个核心模块文件行数过多，违反单一职责原则。这些文件包含过多功能：
  - `budget-allocator.ts`: 预算分配逻辑复杂，包含多个嵌套类型
  - `runtime-state-machine.ts`: 状态机实现过长
  - `durable-event-bus.ts`: 事件总线实现过于庞大
- **建议修复**: 将这些文件拆分为多个小模块，例如：
  - `budget-allocator/` 目录包含多个相关文件
  - `runtime-state-machine/` 目录包含状态转换、命令处理等
  - `durable-event-bus/` 目录包含消费者、投递、重试等子模块

#### 3. [源代码] [中严重] [tsconfig.json exclude列表过长]
- **文件/路径**: `tsconfig.json` exclude 部分
- **问题描述**: 排除了约80个测试文件和目录，包括：
  - 大量 `*.extended.test.ts` 文件
  - 整个 `tests/unit/platform/five-plane-execution/**/*.test.ts` 目录
  - 整个 `tests/integration/platform/five-plane-orchestration/**/*.test.ts` 目录
  - 多个 `tests/e2e/*.test.ts` 文件
  - 多个 `tests/unit/interaction/**/*.test.ts`, `tests/unit/ops-maturity/**/*.test.ts` 等
- **建议修复**: 调查这些测试被排除的根本原因：
  1. 如果是测试不稳定（flaky），应标记并修复
  2. 如果是循环依赖，应重构代码结构
  3. 如果是故意排除的扩展测试，应创建单独的 tsconfig 文件
  4. 如果是测试污染问题，应使用工作区隔离

#### 4. [源代码] [中严重] [core/runtime 兼容性目录未清理]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: CLAUDE.md 明确指出 `src/core/runtime/` 是兼容性目录，不应添加新的规范运行时逻辑。但目录结构仍然存在，且与新的五层架构并行运行，可能导致维护困难和混淆
- **建议修复**: 
  1. 评估 `core/runtime` 中的代码是否已迁移到 `five-plane-*` 目录
  2. 如果已迁移，删除 `core/runtime` 目录
  3. 如果未迁移，制定迁移计划

#### 5. [源代码] [低严重] [直接使用console而非结构化日志]
- **文件/路径**: 约26个源文件直接使用 `console.log/debug/info/warn/error`
- **问题描述**: 项目定义了 `StructuredLogger` (`src/platform/shared/observability/structured-logger.ts`)，但仍有26个文件直接使用 console 方法，不利于日志集中管理和查询
- **建议修复**: 将所有 `console.*` 调用替换为 `StructuredLogger`

#### 6. [源代码] [低严重] [过深的import路径]
- **文件/路径**: 多个源文件
- **问题描述**: 发现类似 `../../../../src/platform/five-plane-execution/budget-allocator.js` 的深层相对导入，这种写法：
  1. 暴露内部结构
  2. 使重构困难
  3. 与 package exports 导出方式不一致
- **建议修复**: 使用包导出方式 `import { BudgetAllocator } from "@automatic-agent/platform/execution"`

#### 7. [源代码] [中严重] [缺少包导出的barrel文件]
- **文件/路径**: `src/platform/` 各子目录
- **问题描述**: 虽然 `package.json` 定义了 exports 映射，但实际目录中某些 index.ts 为空或内容不完整。例如 `src/platform/five-plane-execution/` 的 index.ts 只有简单导出，缺少主要模块的重新导出
- **建议修复**: 确保每个模块的 index.ts 正确导出所有公共 API

#### 8. [源代码] [低严重] [TODO/FIXME/HACK注释未处理]
- **文件/路径**: 至少12个文件包含 TODO/FIXME/HACK/XXX 注释
- **问题描述**: 以下文件包含未完成的标记注释：
  - `src/domains/registry/plugin-spi.ts`
  - `src/org-governance/delegated-governance/governance-console-service.ts`
  - `src/sdk/plugin-sdk/plugin-test-harness.ts`
  - `src/platform/five-plane-interface/api/mission-control-service.ts`
  - `src/platform/five-plane-state-evidence/memory/trust-level-service.ts`
  - `src/platform/five-plane-execution/execution-engine/single-task-happy-path.ts`
  - 以及其他6个文件
- **建议修复**: 
  1. 将 TODO 转换为 issue tracker 中的任务
  2. 或创建代码任务（code task）跟踪
  3. 设置 lint 规则检查这些标记

#### 9. [测试] [高严重] [测试覆盖率分布不均]
- **文件/路径**: `tests/unit/platform/five-plane-execution/` (整个目录被exclude)
- **问题描述**: 
  - `tests/unit/platform/five-plane-execution/` 目录被完全排除在 TypeScript 编译之外
  - 包含 budget-allocator、dispatcher、execution-engine 等核心功能的测试
  - 这意味着这些关键路径没有 CI 级别的测试保护
- **建议修复**: 修复或重新启用这些测试

#### 10. [测试] [高严重] [大量集成测试被排除]
- **文件/路径**: 多个 `tests/integration/**` 目录
- **问题描述**: 以下集成测试目录被排除：
  - `tests/integration/platform/five-plane-orchestration/**/*.test.ts`
  - `tests/integration/platform/five-plane-execution/**/*.test.ts`
  - `tests/integration/platform/five-plane-control-plane/**/*.test.ts`
  - `tests/integration/platform/five-plane-interface/**/*.test.ts`
  - `tests/integration/platform/model-gateway/**/*.test.ts`
  - `tests/integration/platform/security/**/*.test.ts`
  - `tests/integration/platform/shared/cache/**/*.test.ts`
  - `tests/integration/platform/shared/stability/**/*.test.ts`
  - `tests/integration/platform/five-plane-state-evidence/events/**/*.test.ts`
  - `tests/integration/platform/five-plane-state-evidence/knowledge/**/*.test.ts`
  - `tests/integration/platform/five-plane-state-evidence/memory/**/*.test.ts`
  - `tests/integration/domains/governance/**/*.test.ts`
  - `tests/integration/interaction/**/*.test.ts`
  - `tests/integration/org-governance/**/*.test.ts`
  - `tests/integration/sdk/cli/billing-cli.test.ts`
- **建议修复**: 这是巨大的测试覆盖缺口，需要逐个调查被排除原因并修复

#### 11. [测试] [中严重] [E2E测试被排除]
- **文件/路径**: 多个 E2E 测试文件
- **问题描述**: 以下 E2E 测试被排除：
  - `tests/e2e/execution-ticket-lifecycle.test.ts`
  - `tests/e2e/multi-region-failover.test.ts`
  - `tests/e2e/task-status-lifecycle.test.ts`
  - `tests/e2e/workflow-state-transitions.test.ts`
  - `tests/e2e/workflow-timeout-flow.test.ts`
- **建议修复**: 这些是核心工作流测试，应确保它们在 CI 中运行

#### 12. [测试] [中严重] [测试helpers过多]
- **文件/路径**: `tests/helpers/` (78个文件)
- **问题描述**: 
  - 78个测试辅助文件说明测试基础设施复杂
  - 可能表明被测代码耦合度高
  - Mock对象管理困难
  - 需要大量 setup/teardown 代码
- **建议修复**: 
  1. 评估这些 helpers 是否可以合并或简化
  2. 考虑在源码中引入更多可测试性的设计
  3. 检查是否有重复的 helper 功能

#### 13. [测试] [中严重] [Golden测试文件数量少]
- **文件/路径**: `tests/golden/` (48个)
- **问题描述**: 相对于 6113 个源文件和大量功能模块，只有 48 个 golden 测试文件可能不足以覆盖回归风险
- **建议修复**: 增加更多 golden 测试覆盖关键路径

#### 14. [配置] [中严重] [安全配置环境差异]
- **文件/路径**: `config/security/` 目录
- **问题描述**: 
  - `dev.json` 和 `test.json` 内容不同（dev 有 `approvalMode: strict`，test 为空 `{}`）
  - 这种差异可能导致开发环境行为与测试环境不一致
  - 生产配置 `prod.json` 和预生产 `pre-prod.json` 内容相同，都是 `approvalMode: supervised`
- **建议修复**: 明确每个环境的配置策略，确保测试环境能真实反映生产行为

#### 15. [配置] [高严重] [.env.example中敏感字段为空]
- **文件/路径**: `.env.example`
- **问题描述**: 
  - `AA_API_JWT_SECRET=` 为空，生产必须配置
  - 数据库连接字符串可能包含敏感信息
  - 虽然 `.env.example` 说明不要提交真实凭证，但缺少secret管理指引
- **建议修复**: 
  1. 在 `.env.example` 中添加 secret 管理指引注释
  2. 创建 `config/secrets/` 目录结构示例（不包含真实值）
  3. 文档化 secret 轮换策略

#### 16. [配置] [低严重] [配置验证机制缺失]
- **文件/路径**: 全局配置
- **问题描述**: 未在代码库中看到明确的配置验证机制（除了 schema validation in threat-matrix）
- **建议修复**: 在应用启动时添加配置验证步骤，确保所有必需配置都已填充

#### 17. [文档] [中严重] [docs_zh与docs_en同步问题]
- **文件/路径**: `docs_zh/` 和 `docs_en/`
- **问题描述**: 两个文档目录并列存在，需要确保内容同步。但未看到同步机制或自动化检查
- **建议修复**: 
  1. 创建文档同步检查的 CI 流程
  2. 或明确文档翻译流程和责任人
  3. 考虑使用自动化翻译工具

#### 18. [文档] [低严重] [CHANGELOG过小]
- **文件/路径**: `CHANGELOG.md` (仅6KB)
- **问题描述**: 相对于项目规模（6000+源文件，151个契约），CHANGELOG 仅6KB可能记录不完整
- **建议修复**: 增加更详细的变更记录，包括：
  1. 每个版本的变更描述
  2. 破坏性变更说明
  3. 迁移指南

#### 19. [文档] [中严重] [架构文档与实现不一致]
- **文件/路径**: `docs_zh/architecture/00-platform-architecture.md` (728KB)
- **问题描述**: 大型架构文档可能存在与实际实现不一致的地方，特别是在快速迭代期间
- **建议修复**: 
  1. 增加架构合规性检查的 CI 测试
  2. 使用 ADR (Architecture Decision Records) 记录所有重大变更
  3. 定期审查文档与实现的一致性

#### 20. [UI] [中严重] [UI独立部署复杂度]
- **文件/路径**: `ui/` 目录
- **问题描述**: 
  - UI 有独立的 `package.json` 和 `node_modules`
  - 使用 Turborepo 工作区
  - 需要单独部署流程
  - Playwright 配置存在但需确认 CI 是否运行
- **建议修复**: 
  1. 确保 Playwright E2E 测试在 CI 中运行
  2. 文档化 UI 服务的部署流程
  3. 考虑 UI 与主应用的一体化部署

#### 21. [UI] [低严重] [UI包结构复杂度]
- **文件/路径**: `ui/packages/`, `ui/apps/`
- **问题描述**: UI 包结构包含：
  - `packages/shared/` (31个子功能)
  - `packages/ui-core`
  - `packages/ui-mobile`
  - `apps/web`, `apps/mobile`, `apps/electron-win`, `apps/tauri-linux`, `apps/tauri-macos`
  - `packages/features/` (多个功能包)
- **建议修复**: 评估包的数量是否必要，考虑合并一些相关包

#### 22. [脚本] [中严重] [脚本组织混乱]
- **文件/路径**: `scripts/` 目录
- **问题描述**: 
  - 部分脚本在 `scripts/` 根目录（`.sh` 和部分 `.mjs`）
  - 部分在 `scripts/ci/` 目录
  - `deploy/scripts/` 也有脚本
  - 没有统一的脚本组织规范
- **建议修复**: 
  1. 创建 `scripts/README.md` 说明每个脚本的用途
  2. 将所有 CI 相关脚本统一到 `scripts/ci/` 
  3. 考虑使用单一语言（全部 mjs 或全部 ts）

#### 23. [脚本] [低严重] [Python脚本维护性问题]
- **文件/路径**: `translate_docs.py` (10KB)
- **问题描述**: 项目主要是 TypeScript/Node.js，但存在 Python 脚本需要额外维护 Python 环境和依赖
- **建议修复**: 
  1. 评估是否仍在使用
  2. 如果需要，考虑使用 TypeScript 重写
  3. 如果不用，删除

#### 24. [部署] [低严重] [临时构建产物未清理]
- **文件/路径**: 根目录的 `dist_*` 目录
- **问题描述**: 存在多个临时构建目录：
  - `dist/`
  - `dist_temp/`
  - `dist_test/`
  - `dist_test_compiled/`
  - `dist_1964/`
  - `dist_issue2014/`
  - `dist_lineage_test/`
- **建议修复**: 
  1. 清理所有临时构建目录
  2. 在 `.gitignore` 中添加 `dist_*` 模式
  3. 创建构建清理脚本

#### 25. [源代码] [高严重] [内存数据库文件残留]
- **文件/路径**: 根目录的 `:memory:` 文件
- **问题描述**: 存在多个内存数据库文件：
  - `:memory:aa-truth-append-*`
  - `:memory:aa-truth-cost-*`
  - `:memory:aa-truth-exec-*`
  - `:memory:aa-truth-session-*`
  - `:memory:aa-truth-status-*`
  - `:memory:aa-truth-wf-*`
  - `:memory:fence-count-*`
  - `:memory:fence-shared-*`
- **建议修复**: 
  1. 这些可能是测试过程中残留的文件
  2. 在 `.gitignore` 中添加 `:memory:*` 模式
  3. 确保测试完成后清理临时文件

#### 26. [源代码] [中严重] [session-replay目录过大]
- **文件/路径**: `session-replay/` (1561个条目)
- **问题描述**: session-replay 目录包含大量文件（可能来自测试或开发过程），占用约50KB
- **建议修复**: 
  1. 评估是否需要保留这些 replay 文件
  2. 如果不需要，在 `.gitignore` 中排除并清理
  3. 如果需要，考虑压缩或外部存储

#### 27. [源代码] [中严重] [artifacts目录过大]
- **文件/路径**: `artifacts/` (489个条目)
- **问题描述**: artifacts 目录包含大量文件，可能影响 git 操作性能
- **建议修复**: 
  1. 评估 artifacts 的保留策略
  2. 实现自动清理过期 artifacts 的机制
  3. 考虑将大型 artifacts 存储在外部存储

#### 28. [源代码] [低严重] [data目录无结构化组织]
- **文件/路径**: `data/` (35个条目)
- **问题描述**: data 目录包含 sqlite、stable-* 等子目录，但没有清晰的组织结构说明
- **建议修复**: 
  1. 在 `data/` 目录添加 README 说明目录结构
  2. 实现数据保留策略（自动清理旧数据）
  3. 区分临时数据和持久数据

#### 29. [部署] [中严重] [deploy目录结构不完整]
- **文件/路径**: `deploy/` 目录
- **问题描述**: 
  - 包含 terraform、helm、prometheus、grafana 等基础设施代码
  - 但 chaos 和 runbooks 目录为空或接近空
  - 缺少 Kubernetes 部署配置
- **建议修复**: 
  1. 完成 chaos 测试场景
  2. 增加 runbook 覆盖
  3. 添加 Kubernetes 部署配置（如果适用）

#### 30. [源代码] [低严重] [领域模块过多导致复杂性]
- **文件/路径**: `src/domains/` (60个条目，包括34个业务领域)
- **问题描述**: 
  - 34个业务领域（academic-research, advertising, agriculture等）
  - 每个领域可能需要独立维护和更新
  - 领域间的依赖关系不明确
- **建议修复**: 
  1. 创建领域依赖关系图
  2. 评估是否可以使用领域共享基础模块减少重复
  3. 考虑领域分组（相似领域放在一起）

#### 31. [源代码] [中严重] [contracts重复导出问题]
- **文件/路径**: `src/platform/contracts/` 和 `src/contracts/` 可能存在重复
- **问题描述**: 项目有两处 contracts 相关代码，需要确认是否有重复定义
- **建议修复**: 
  1. 确认 contracts 的单一真实来源
  2. 确保没有类型定义重复
  3. 统一 contracts 导入路径

#### 32. [测试] [中严重] [tests/unit/helpers/index.test.ts被排除]
- **文件/路径**: `tests/unit/helpers/index.test.ts`
- **问题描述**: 测试 helpers 本身的测试被排除，这可能意味着 helpers 缺乏测试保护
- **建议修复**: 
  1. 调查为什么这个测试被排除
  2. 启用该测试或删除（如果无意义）

#### 33. [配置] [低严重] [.c8rc.json与stryker.config.mjs并存]
- **文件/路径**: 根目录
- **问题描述**: 同时存在两个覆盖率配置文件：
  - `.c8rc.json` (Code coverage)
  - `stryker.config.mjs` (Mutation testing)
  可能导致覆盖率报告不一致
- **建议修复**: 
  1. 确保两个工具的覆盖率阈值一致
  2. 在 CI 中统一报告
  3. 考虑使用单一覆盖率工具

#### 34. [文档] [低严重] [operations文档与实际不完全匹配]
- **文件/路径**: `docs_zh/operations/current_todo_list.md` (38KB)
- **问题描述**: 存在一个大型 TODO 列表文件，说明有很多待办事项。这可能导致文档与实际状态脱节
- **建议修复**: 
  1. 定期同步 TODO 列表与实际代码状态
  2. 将已完成的项目移到 CHANGELOG
  3. 考虑使用 issue tracker 替代文档中的 TODO

#### 35. [源代码] [高严重] [源码文件数量统计矛盾]
- **文件/路径**: 全局
- **问题描述**: 
  - 使用 `find` 命令统计到 1795 个 .ts 文件
  - 但文档提到 6113 个源文件
  - 这个差异需要解释
- **建议修复**: 
  1. 确认 6113 是否包含 .js 文件（编译产物）
  2. 或确认是否包含测试文件
  3. 更新文档以反映准确数字

---

### 总结

本次自动Review发现了35个问题，其中：

**高优先级 (需要立即处理)**:
1. 测试目录大量被exclude (80+个)
2. 符号链接导致构建不一致
3. 内存数据库文件残留
4. 源码文件数量统计矛盾
5. JWT secret 管理不明确

**中优先级**:
1. 巨型单文件需要拆分
2. 配置环境差异
3. 文档同步问题
4. UI 独立部署复杂度
5. 脚本组织混乱
6. 部署配置不完整

**低优先级**:
1. 代码注释清理
2. 包结构优化
3. Python脚本维护
4. 数据目录组织

*Review generated: 2026/05/14*

---

## 2026/05/13 自动Review报告 (详细补充 - 第二轮)

### 发现的问题

#### 36. [源代码] [高严重] [巨型源文件 - budget-allocator.ts 超过900行]
- **文件/路径**: `src/platform/five-plane-execution/budget-allocator.ts` (931行)
- **问题描述**: 
  - 该文件包含 `BudgetAllocatorContext`、`BudgetSettlementResult`、`BudgetReleaseResult`、`BudgetTier`、`BudgetWatermarkAlert`、`BudgetAutoThrottleEvent`、`BudgetAllocatorEvents`、`BudgetTruthStore` 等大量接口定义
  - 还包含 `BudgetAllocator` 类和核心业务逻辑
  - 单一职责原则被违反，代码可维护性差
  - 单元测试困难，需要大量mock
- **建议修复**: 
  1. 将接口定义移到 `types.ts` 或 `interfaces/` 目录
  2. 将 `BudgetAllocator` 类移到单独的 `budget-allocator-service.ts`
  3. 将辅助类型和枚举移到 `budget-types.ts`
  4. 将存储层相关代码移到 `budget-store.ts`

#### 37. [源代码] [高严重] [巨型源文件 - durable-event-bus.ts 超过1200行]
- **文件/路径**: `src/platform/five-plane-state-evidence/events/durable-event-bus.ts` (1214行)
- **问题描述**: 
  - 事件总线的实现过于庞大，包含多个子模块逻辑
  - 包含消费者管理、投递、重试、背压等复杂逻辑
  - 测试覆盖困难，任何修改都可能影响生产环境
- **建议修复**: 
  1. 拆分为 `durable-event-bus/` 目录
  2. `consumer-manager.ts` - 消费者管理
  3. `delivery-engine.ts` - 投递引擎
  4. `retry-policy.ts` - 重试策略
  5. `back-pressure.ts` - 背压控制
  6. 主文件作为 Facade 整合各子模块

#### 38. [源代码] [中严重] [巨型源文件 - runtime-state-machine.ts 近700行]
- **文件/路径**: `src/platform/five-plane-execution/runtime-state-machine.ts` (690行)
- **问题描述**: 
  - 状态机实现包含过多状态转换逻辑
  - 嵌套类型和复杂条件分支
  - 难以进行单元测试和回归测试
- **建议修复**: 
  1. 将状态定义移到 `runtime-state-types.ts`
  2. 将转换逻辑拆分为多个策略类
  3. 引入状态模式 (State Pattern) 重构

#### 39. [源代码] [中严重] [core/runtime 目录与五层架构并存]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - `CLAUDE.md` 明确警告不要向 `src/core/runtime/` 添加新的规范运行时逻辑
  - 但该目录仍存在并包含 `orchestrator/`、`planner/`、`supervisor/` 等子目录
  - 与新的五层架构 (`five-plane-*`) 并行运行，造成混淆
  - 可能导致开发者选择错误的模块位置
- **建议修复**: 
  1. 审计 `core/runtime` 中的代码，确定哪些已迁移到 `five-plane-*`
  2. 删除已迁移的代码或创建明确的迁移指南
  3. 在 `core/runtime/index.ts` 中添加 Deprecation 警告

#### 40. [源代码] [中严重] [26个源文件直接使用 console.* 而非结构化日志]
- **文件/路径**: 多个源文件
- **问题描述**: 
  - 项目定义了 `StructuredLogger` (`src/platform/shared/observability/structured-logger.ts`)
  - 但仍有26个文件直接使用 `console.log/debug/info/warn/error`
  - 不利于日志集中管理、查询和分析
  - 无法享受结构化日志的好处（如日志级别过滤、输出格式统一）
- **建议修复**: 
  1. 替换所有 `console.*` 调用为 `StructuredLogger`
  2. 创建 ESLint 规则禁止直接使用 `console.*`
  3. 添加自动化检查在 CI 中捕获新的 `console.*` 使用

#### 41. [源代码] [低严重] [13个文件包含未完成的 TODO/FIXME/HACK 标记]
- **文件/路径**: 多个源文件
- **问题描述**: 
  - `src/domains/registry/plugin-spi.ts` - 1个标记
  - `src/org-governance/delegated-governance/governance-console-service.ts` - 1个标记
  - `src/org-governance/sso-scim/saml/index.ts` - 1个标记
  - `src/platform/five-plane-interface/api/mission-control-service.ts` - 2个标记
  - `src/platform/five-plane-state-evidence/memory/trust-level-service.ts` - 2个标记
  - `src/platform/five-plane-execution/execution-engine/single-task-happy-path.ts` - 1个标记
  - `src/platform/five-plane-execution/plugin-executor/adapter-executor.ts` - 2个标记
  - `src/platform/five-plane-execution/tool-executor/tool-metadata.ts` - 2个标记
  - `src/platform/five-plane-execution/tool-executor/tool-argument-coercion.ts` - 2个标记
  - `src/platform/five-plane-execution/tool-executor/code-diagnostics-service.ts` - 1个标记
  - `src/sdk/cli/stable-runner-factory.ts` - 1个标记
  - `src/sdk/plugin-sdk/plugin-test-harness.ts` - 1个标记
- **建议修复**: 
  1. 将 TODO 转换为 GitHub Issue 或任务跟踪系统
  2. 或创建代码任务（code task）跟踪
  3. 设置 lint 规则检查这些标记并要求在 PR 中处理

#### 42. [测试] [高严重] [budget-allocator.test.ts 测试失败]
- **文件/路径**: `tests/unit/platform/five-plane-execution/budget-allocator.test.ts`
- **问题描述**: 
  - 根据 `.audit/quality.md` 报告，该测试文件当前失败
  - 这是核心预算分配功能的测试，其失败意味着该模块没有CI级别的测试保护
  - 14个测试文件失败，其中 `budget-allocator.test.ts` 是唯一一个 execution 核心模块的失败
- **建议修复**: 
  1. 立即调查失败原因
  2. 修复测试或标记为已知问题
  3. 确保在 PR 合并前所有核心模块测试通过

#### 43. [测试] [中严重] [测试文件与源文件比例严重失调]
- **文件/路径**: 全局
- **问题描述**: 
  - 源文件: 1795 个 .ts 文件
  - 测试文件: 4662 个 .test.ts 文件
  - 比例约 2.6:1，这表明:
    - 可能是大量集成测试和E2E测试被计入
    - 或存在大量测试辅助文件和mock文件
    - 与文档中提到的 6113 个源文件数量不符
- **建议修复**: 
  1. 确认测试文件统计的准确性
  2. 分离单元测试、集成测试、E2E测试的数量
  3. 更新文档以反映准确的统计数据

#### 44. [配置] [高严重] [config/security/dev.json 与 test.json 行为不一致]
- **文件/路径**: `config/security/dev.json` 和 `config/security/test.json`
- **问题描述**: 
  - `dev.json`: `{"approvalMode": "strict", "allowDestructiveActions": false}`
  - `test.json`: `{}` (空对象)
  - 这种差异会导致开发环境行为与测试环境不一致
  - `test.json` 缺少 `approvalMode` 配置，可能导致测试使用默认值而非预期值
- **建议修复**: 
  1. 统一安全配置策略
  2. test.json 应包含与 dev.json 相同的配置（可能值不同）
  3. 添加配置验证确保必需字段存在

#### 45. [配置] [中严重] [.env.example 中 AA_API_JWT_SECRET 为空但无安全警告]
- **文件/路径**: `.env.example` 第15行
- **问题描述**: 
  - `AA_API_JWT_SECRET=` 为空，注释说明生产需要配置
  - 但没有足够的安全警告提示
  - 没有指引如何生成安全的 secret
  - 没有secret管理策略文档
- **建议修复**: 
  1. 在注释中添加更明确的安全警告
  2. 提供生成命令示例（如已有但需更明显）
  3. 文档化 secret 轮换策略
  4. 考虑添加 secret 验证在应用启动时

#### 46. [配置] [中严重] [package.json scripts 过多且重复]
- **文件/路径**: `package.json` scripts 部分
- **问题描述**: 
  - 超过100个npm scripts
  - 大量重复的 `stable:*` 脚本（每个都调用 `npm run build`）
  - `test:raw`、`test`、`test:unit` 等脚本职责不清
  - `build` 和 `build:test` 混用可能导致测试结果不一致
- **建议修复**: 
  1. 使用 `npm run build` 统一构建脚本
  2. 合并重复的 stable 脚本到通用运行器
  3. 简化测试脚本层级
  4. 文档化每个脚本的用途

#### 47. [部署] [中严重] [deploy/runbooks 目录为空]
- **文件/路径**: `deploy/runbooks/`
- **问题描述**: 
  - 目录存在但为空
  - 运维文档 (docs_zh/operations/) 提到 runbooks 但实际缺失
  - 紧急情况时无法快速响应
- **建议修复**: 
  1. 填充基本的 runbook 内容
  2. 包括常见故障排除步骤
  3. 添加部署和回滚程序
  4. 包含联系人信息和升级路径

#### 48. [部署] [中严重] [deploy/chaos 目录为空]
- **文件/路径**: `deploy/chaos/`
- **问题描述**: 
  - 目录存在但为空
  - 项目的 stable gate 包含 chaos 测试 (`chaos:stable`)
  - 但实际 chaos 场景配置缺失
  - 无法进行混沌工程验证
- **建议修复**: 
  1. 添加基本的 chaos 场景配置
  2. 包括服务中断、网络隔离、资源耗尽等场景
  3. 文档化 chaos 测试运行流程
  4. 与 `ops-maturity/chaos/` 目录协调

#### 49. [源代码] [中严重] [src/platform/contracts/ 与 src/contracts/ 可能存在重复]
- **文件/路径**: `src/platform/contracts/` 和 `src/contracts/`
- **问题描述**: 
  - 项目有两处 contracts 相关代码
  - `src/platform/contracts/` 包含 `index.ts` 重新导出
  - `src/contracts/` 不存在（通过 `ls` 命令确认）
  - 但 `src/platform/contracts/` 中可能有重复定义
- **建议修复**: 
  1. 确认 contracts 的单一真实来源
  2. 统一 contracts 导入路径
  3. 避免类型定义重复

#### 50. [源代码] [低严重] [大量临时构建目录未清理]
- **文件/路径**: 根目录的 `dist_*` 目录
- **问题描述**: 
  - 存在 `dist/`、`dist_temp/`、`dist_test/`、`dist_test_compiled/`、`dist_1964/`、`dist_issue2014/`、`dist_lineage_test/` 等多个临时构建目录
  - 这些目录未被 `.gitignore` 忽略
  - 可能被意外提交到版本控制
- **建议修复**: 
  1. 在 `.gitignore` 中添加 `dist_*` 模式
  2. 清理所有临时构建目录
  3. 创建构建清理脚本
  4. 使用统一的构建输出目录

#### 51. [源代码] [高严重] [内存数据库文件未在 .gitignore 中]
- **文件/路径**: 根目录的 `:memory:*` 文件
- **问题描述**: 
  - 存在多个内存数据库文件 (`:memory:aa-truth-*`, `:memory:fence-*`)
  - 这些文件未被 `.gitignore` 忽略
  - 可能包含敏感信息或测试数据
- **建议修复**: 
  1. 在 `.gitignore` 中添加 `:memory:*` 模式
  2. 清理所有内存数据库文件
  3. 调查这些文件的来源并修复

#### 52. [源代码] [中严重] [session-replay 目录未在 .gitignore 中]
- **文件/路径**: `session-replay/` (1561个条目)
- **问题描述**: 
  - session-replay 目录未被 `.gitignore` 排除
  - 包含大量文件可能影响 git 性能
  - 可能包含敏感的操作回放数据
- **建议修复**: 
  1. 在 `.gitignore` 中添加 `session-replay/` 排除
  2. 评估是否需要保留这些 replay 文件
  3. 实现自动清理过期 replay 文件的机制

#### 53. [源代码] [中严重] [artifacts 目录未在 .gitignore 中]
- **文件/路径**: `artifacts/` (489个条目)
- **问题描述**: 
  - artifacts 目录未被 `.gitignore` 排除
  - 包含大量文件可能影响 git 操作性能
  - 可能被错误用于长期存储而非临时存储
- **建议修复**: 
  1. 在 `.gitignore` 中添加 `artifacts/` 排除
  2. 实现数据保留策略（自动清理旧 artifacts）
  3. 考虑将大型 artifacts 存储在外部存储

#### 54. [安全] [中严重] [Buffer.from 使用可能存在安全问题]
- **文件/路径**: 多个文件
- **问题描述**: 
  - `src/platform/five-plane-interface/api/oidc-oauth-service.ts` - 多处使用 `Buffer.from`
  - `src/platform/five-plane-interface/api/api-auth-service.ts` - 使用 `Buffer.from`
  - `src/platform/five-plane-interface/webhook/index.ts` - 使用 `Buffer.from`
  - 建议使用 `TextEncoder` 或 `Uint8Array` 替代旧版 `Buffer` API
- **建议修复**: 
  1. 审查所有 `Buffer.from` 使用
  2. 考虑使用更安全的替代方案
  3. 添加安全lint规则检查

#### 55. [安全] [中严重] [process.env 访问次数过多]
- **文件/路径**: 全局
- **问题描述**: 
  - 约 952 处 `process.env` 访问
  - 配置直接暴露在代码中
  - 缺少统一的配置管理
  - 环境变量可能未经过滤暴露敏感信息
- **建议修复**: 
  1. 创建统一的配置管理模块
  2. 添加配置验证和类型检查
  3. 使用 `zod` 或类似工具验证环境变量
  4. 避免在业务逻辑中直接访问 `process.env`

#### 56. [文档] [中严重] [.audit/delegation/delegation-audit-events.json 可能包含敏感数据]
- **文件/路径**: `.audit/delegation/delegation-audit-events.json`
- **问题描述**: 
  - 该文件包含 65 条审计事件
  - 包含 `parentAgentId`、`childAgentId`、`actorId` 等标识符
  - 虽然是测试数据，但可能反映真实系统结构
  - 文件名包含 "audit" 但未被 `.gitignore` 排除
- **建议修复**: 
  1. 在 `.gitignore` 中添加 `.audit/` 目录排除
  2. 审计数据应存储在安全的位置
  3. 确认测试数据的来源和匿名化处理

#### 57. [文档] [低严重] [.audit/quality.md 与实际测试状态不一致]
- **文件/路径**: `.audit/quality.md`
- **问题描述**: 
  - 该文件报告了 14 个失败的测试
  - 但 git status 显示没有未提交的更改
  - 说明这些失败可能已被接受但未修复
  - 或测试本身存在持续性问题
- **建议修复**: 
  1. 将失败的测试标记为已知问题
  2. 创建 issue tracker 跟踪每个失败的测试
  3. 在 CI 中添加测试质量门槛

#### 58. [源代码] [中严重] [stryker.config.mjs 只变异少量关键文件]
- **文件/路径**: `stryker.config.mjs`
- **问题描述**: 
  - mutation testing 只配置了对 7 个文件的变异检测
  - 这些文件主要是 auth、billing、approval、gateway 相关
  - 其他核心模块未纳入变异测试
  - 覆盖率可能不足以发现所有关键问题
- **建议修复**: 
  1. 扩展 mutation 测试覆盖到更多核心模块
  2. 考虑添加 budget-allocator、runtime-state-machine 等
  3. 平衡测试成本和覆盖率

#### 59. [源代码] [低严重] [.c8rc.json 和 stryker.config.mjs 配置分离]
- **文件/路径**: `.c8rc.json` 和 `stryker.config.mjs`
- **问题描述**: 
  - 两个覆盖率工具配置分离
  - c8 用于代码覆盖率
  - stryker 用于变异测试
  - 可能导致报告不一致或覆盖缺口
- **建议修复**: 
  1. 确保两个工具的覆盖率阈值一致
  2. 在 CI 中统一报告
  3. 考虑使用单一覆盖率工具或明确分工

#### 60. [配置] [中严重] [UI package.json 与主 package.json 分离]
- **文件/路径**: `ui/package.json`
- **问题描述**: 
  - UI 有独立的 `package.json` 和 `node_modules`
  - 使用 Turborepo 工作区
  - 需要单独的依赖管理流程
  - 可能导致依赖版本不一致
- **建议修复**: 
  1. 确保 UI 和主项目的依赖版本同步
  2. 文档化 UI 服务的部署流程
  3. 考虑统一依赖管理

#### 61. [源代码] [中严重] [tool-executor 目录文件过多]
- **文件/路径**: `src/platform/five-plane-execution/tool-executor/`
- **问题描述**: 
  - 该目录包含大量文件（35个）
  - 包括 `code-diagnostics-service.ts` (17260字节)、`command-executor.ts` (24468字节) 等大文件
  - 可能存在职责不清的问题
- **建议修复**: 
  1. 评估是否可以按功能拆分
  2. 将相关功能组合到子目录
  3. 确保每个文件的单一职责

#### 62. [源代码] [低严重] [hardcoded 配置散布在代码中]
- **文件/路径**: 多个源文件
- **问题描述**: 
  - 多处代码包含 "hardcoded" 注释
  - 如 `channel-gateway-service.ts:303` - R12-12 注释
  - `evaluator-service.ts:69` - R11-05 FIX 注释
  - `plan-evaluator.ts:40` - R8-12 FIX 注释
  - 说明代码中曾存在硬编码值，现已修复但注释残留
- **建议修复**: 
  1. 清理已修复的 "hardcoded" 注释
  2. 保留有价值的架构决策注释
  3. 使用更明确的注释格式

#### 63. [测试] [中严重] [大量测试文件被 exclude 导致覆盖缺口]
- **文件/路径**: `tsconfig.json` exclude 部分
- **问题描述**: 
  - 排除了约 80 个测试文件和目录
  - 包括整个 `tests/unit/platform/five-plane-execution/` 目录
  - 包括所有 `tests/integration/platform/*/` 目录
  - 包括多个 E2E 测试文件
- **建议修复**: 
  1. 逐个调查被排除的原因
  2. 修复或重新启用这些测试
  3. 如果是故意排除，创建单独的配置

#### 64. [源代码] [中严重] [src/platform/five-plane-control-plane/rollout-controller/ 最近的修改]
- **文件/路径**: `src/platform/five-plane-control-plane/rollout-controller/traffic-routing-service.ts`
- **问题描述**: 
  - git status 显示该文件有修改但未提交
  - 这是流量路由服务的关键文件
  - 可能存在未完成的更改导致构建不一致
- **建议修复**: 
  1. 完成或回滚更改
  2. 确保关键文件及时提交
  3. 在 CI 中检测未提交的关键更改

#### 65. [源代码] [中严重] [src/contracts/types/ids.ts 导出混乱]
- **文件/路径**: `src/platform/contracts/` 和相关导入
- **问题描述**: 
  - 多处代码使用 `import { newId, nowIso } from "../../contracts/types/ids.js"`
  - 但实际文件路径可能不同
  - 深层相对导入导致重构困难
- **建议修复**: 
  1. 使用包导出方式
  2. 统一 `import { newId } from "@automatic-agent/platform/contracts"`
  3. 确保 exports 映射正确

#### 66. [配置] [低严重] [tsconfig.json 存在多个变体]
- **文件/路径**: `tsconfig.json`、`tsconfig.build.json`、`tsconfig.temp.json`
- **问题描述**: 
  - 存在多个 tsconfig 变体
  - `tsconfig.temp.json` 可能是临时配置
  - 可能导致构建不一致
- **建议修复**: 
  1. 清理 `tsconfig.temp.json`
  2. 文档化每个配置文件的用途
  3. 确保只有一个主配置

---

### 总结

本次自动Review（第二轮）发现了31个新问题，其中：

**高优先级 (需要立即处理)**:
1. `budget-allocator.ts` (931行) 巨型文件需要拆分
2. `durable-event-bus.ts` (1214行) 巨型文件需要拆分
3. `config/security/test.json` 为空导致测试环境不一致
4. `budget-allocator.test.ts` 测试失败
5. `.gitignore` 缺少多个临时文件模式（`:memory:*`、`dist_*`、session-replay、artifacts）
6. `src/platform/contracts/` 可能有重复定义

**中优先级**:
1. `runtime-state-machine.ts` (690行) 需要拆分
2. `core/runtime` 与五层架构并存造成混淆
3. `stryker.config.mjs` 覆盖不足
4. 26个文件直接使用 `console.*` 而非结构化日志
5. `deploy/runbooks` 和 `deploy/chaos` 目录为空
6. 952处 `process.env` 访问缺少统一管理

**低优先级**:
1. 13个文件包含未完成的 TODO/FIXME 标记
2. `.c8rc.json` 和 `stryker.config.mjs` 配置分离
3. UI package.json 分离管理
4. tool-executor 目录文件过多（35个）
5. 多个临时 tsconfig 文件需要清理

*Review generated: 2026/05/13*

---

## 2026/05/13 自动Review报告 (第三轮 - 深入专项检查)

### 发现的问题

#### 67. [源代码] [高严重] [接口一致性问题 - five-plane-execution 导出路径混乱]
- **文件/路径**: `src/platform/five-plane-execution/compensation-manager.ts` 和相关文件
- **问题描述**: 
  - `compensation-manager.ts` 使用 `import { newId } from "../../platform/contracts/types/ids.js"` 
  - 同一文件在同目录中却使用 `import { createCompensationRecord } from "../../platform/contracts/executable-contracts/index.js"`
  - 这种不一致的导入路径表明 exports 映射可能未被正确使用
  - 违反了 package exports 的设计意图
- **建议修复**: 
  1. 统一使用包导出路径 `@automatic-agent/platform/contracts`
  2. 通过 `npm run typecheck` 检测所有未解析的导入
  3. 创建 lint 规则检查深层相对导入

#### 68. [源代码] [高严重] [接口一致性问题 - five-plane-control-plane IAM 模块导入混乱]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/` 目录
- **问题描述**: 
  - 根据 `.audit/quality.md`，IAM 模块有 2 个测试文件失败：`access-model.test.ts` 和 `field-encryption.test.ts`
  - IAM 是安全关键模块，测试失败意味着安全功能没有 CI 保护
  - 这可能表明导入路径或接口定义存在问题
- **建议修复**: 
  1. 立即调查 IAM 测试失败的根本原因
  2. 修复或标记为已知安全问题
  3. 在 CI 中优先运行 IAM 相关测试

#### 69. [源代码] [高严重] [接口一致性问题 - durable-event-bus 测试失败]
- **文件/路径**: `tests/unit/platform/five-plane-state-evidence/events/durable-event-bus-async.test.ts`
- **问题描述**: 
  - `durable-event-bus.ts` (1214行) 是巨型源文件
  - 对应的 `durable-event-bus-async.test.ts` 测试失败
  - 事件总线是 state-evidence 的核心组件，其测试失败可能导致数据一致性风险
- **建议修复**: 
  1. 调查 `durable-event-bus-async.test.ts` 失败原因
  2. 考虑将 `durable-event-bus.ts` 拆分为多个可测试模块
  3. 确保事件投递、重试、背压等核心逻辑有测试覆盖

#### 70. [源代码] [高严重] [接口一致性问题 - runtime-state-machine 导入路径错误]
- **文件/路径**: `src/platform/five-plane-execution/runtime-state-machine.ts`
- **问题描述**: 
  - 该文件使用 `import { ValidationError, WorkflowStateError } from "../contracts/errors.ts"`
  - 但 `../contracts` 路径在 `five-plane-execution` 上下文中应该指向 `five-plane-execution/contracts` 而非 `platform/contracts`
  - 这表明可能存在循环导入或路径解析问题
- **建议修复**: 
  1. 检查 `runtime-state-machine.ts` 的完整导入列表
  2. 确保所有导入使用正确的相对路径或包导出
  3. 运行 `npm run typecheck` 检测类型错误

#### 71. [源代码] [中严重] [工具执行器模块 - 35个文件职责过多]
- **文件/路径**: `src/platform/five-plane-execution/tool-executor/`
- **问题描述**: 
  - 该目录包含 35 个文件，包括多个巨型文件：
    - `code-diagnostics-service.ts` (17260字节)
    - `command-executor.ts` (24468字节)
    - `tool-metadata.ts` (16092字节)
  - 总字节数超过 150KB，表明职责过于集中
  - 虽然有 `index.ts` 正确导出，但模块内聚性差
- **建议修复**: 
  1. 按功能将 tool-executor 拆分为子目录：
     - `tool-executor/core/` - 核心执行逻辑
     - `tool-executor/diagnostics/` - 诊断服务
     - `tool-executor/security/` - 安全检查
     - `tool-executor/mcp/` - MCP 相关
  2. 提取共享类型到 `tool-types.ts`
  3. 简化 index.ts 导出

#### 72. [源代码] [中严重] [state-transition 服务 - 868行巨型服务]
- **文件/路径**: `src/platform/five-plane-execution/state-transition/transition-service.ts` (868行)
- **问题描述**: 
  - `TransitionService` 是执行引擎的核心状态转换门
  - 单一文件包含过多状态转换逻辑
  - 根据 CLAUDE.md，这是"权威状态转换门"，应该高度可靠
- **建议修复**: 
  1. 将状态转换规则拆分为独立的策略类
  2. 将状态枚举和类型移到 `runtime-state-types.ts`
  3. 提取验证逻辑到独立模块

#### 73. [测试] [高严重] [测试排除列表分析 - 根本原因未调查]
- **文件/路径**: `tsconfig.json` exclude 部分
- **问题描述**: 
  - 排除了约 80+ 个测试文件
  - 包括整个 `tests/unit/platform/five-plane-execution/` 目录
  - 包括所有 `tests/integration/platform/*/` 目录
  - 之前的问题 #63 提到但未分析根本原因
  - 这么多测试被排除不是正常现象
- **建议修复**: 
  1. 创建一个诊断脚本分析每个被排除测试的失败原因
  2. 按类别统计：
     - 循环依赖导致的排除
     - 测试不稳定导致的排除
     - 有意排除的扩展测试
  3. 创建修复计划逐个解决

#### 74. [测试] [高严重] [.audit/quality.md 与实际 git 状态不一致]
- **文件/路径**: `.audit/quality.md`
- **问题描述**: 
  - 该文件报告了 14 个失败的测试
  - 但 git status 显示只有 3 个文件有未提交的修改
  - 这说明：
    1. 测试失败已被接受（未修复）
    2. 或者测试在不同环境下表现不同
    3. 或者 audit 报告未及时更新
- **建议修复**: 
  1. 更新 `.audit/quality.md` 的时间戳为最新
  2. 将失败的测试添加到 issue tracker
  3. 在 CI 中自动运行并更新 audit 报告

#### 75. [配置] [高严重] [config/security 环境配置差异 - 严重程度被低估]
- **文件/路径**: `config/security/` 各环境配置文件
- **问题描述**: 
  - `dev.json`: `{"approvalMode": "strict", "allowDestructiveActions": false}`
  - `test.json`: `{}` (空对象 - 默认值)
  - `staging.json`: `{"approvalMode": "supervised"}`
  - `pre-prod.json`: `{"approvalMode": "supervised", "allowDestructiveActions": false}`
  - `prod.json`: `{"approvalMode": "strict", "allowDestructiveActions": false}`
  
  **严重问题**:
  1. `test.json` 为空可能导致测试使用意外的默认 approvalMode
  2. `staging` 和 `pre-prod` 使用 `supervised`，但 `prod` 使用 `strict` - 这意味着 staging 环境不能真实反映生产行为
  3. `allowDestructiveActions` 字段在各环境不一致
- **建议修复**: 
  1. 统一 `test.json` 配置为与 `staging.json` 一致
  2. 将 `staging` 配置改为 `{"approvalMode": "strict"}` 以反映真实生产行为
  3. 添加配置 schema 验证确保必需字段存在
  4. 创建配置矩阵文档

#### 76. [配置] [中严重] [config/environments 与 config/security 配置不对齐]
- **文件/路径**: `config/environments/dev.json` 和 `config/security/dev.json`
- **问题描述**: 
  - `environments/dev.json` 定义了部署相关的配置（registry, namespace, clusterName）
  - `security/dev.json` 定义了安全策略（approvalMode, allowDestructiveActions）
  - 但两个配置文件没有明确的关联机制
  - 应用启动时如何合并这些配置不明确
- **建议修复**: 
  1. 文档化配置合并策略
  2. 在代码中添加配置来源注释
  3. 考虑将安全配置合并到 environments 配置中

#### 77. [源代码] [中严重] [五个符号链接未在 gitignore 中]
- **文件/路径**: `src/platform/` 下的符号链接
- **问题描述**: 
  - `control-plane -> five-plane-control-plane`
  - `execution -> five-plane-execution`
  - `state-evidence -> five-plane-state-evidence`
  - `orchestration -> five-plane-orchestration`
  - `interface -> five-plane-interface`
  - 这些符号链接在某些文件系统上可能导致问题
  - IDE 可能无法正确解析符号链接目标
- **建议修复**: 
  1. 在项目文档中明确说明这些符号链接的存在和用途
  2. 考虑在 CI 中验证符号链接有效性
  3. 或者将符号链接转换为实际目录（通过构建脚本创建）

#### 78. [源代码] [中严重] [multi-step-orchestration 模块 - 29个文件过多]
- **文件/路径**: `src/platform/five-plane-execution/execution-engine/`
- **问题描述**: 
  - 该目录包含 29 个文件，是 execution 引擎的核心
  - `multi-step-orchestration.ts` 和相关文件形成复杂的 orchestration 逻辑
  - 模块间耦合度高，测试困难
- **建议修复**: 
  1. 将 orchestration 逻辑拆分为独立模块：
     - `orchestration/planning/` - 计划制定
     - `orchestration/execution/` - 执行管理
     - `orchestration/monitoring/` - 监控
  2. 提取共享类型到 `orchestration-types.ts`
  3. 降低模块间耦合

#### 79. [源代码] [中严重] [stryker.config.mjs 变异覆盖严重不足]
- **文件/路径**: `stryker.config.mjs`
- **问题描述**: 
  - 只配置了 8 个文件的变异测试
  - 缺少关键模块：
    - `budget-allocator.ts` (931行) - 预算分配核心
    - `runtime-state-machine.ts` (690行) - 状态机核心
    - `transition-service.ts` (868行) - 状态转换核心
    - `durable-event-bus.ts` (1214行) - 事件总线核心
  - 这些核心模块的变异覆盖率 = 0%
- **建议修复**: 
  1. 将 `budget-allocator.ts`、`runtime-state-machine.ts`、`transition-service.ts` 添加到 `mutate` 列表
  2. 评估 `durable-event-bus.ts` 的测试可行性
  3. 平衡测试成本（变异测试耗时长）与覆盖率

#### 80. [配置] [低严重] [.gitignore 缺少多个临时文件]
- **文件/路径**: `.gitignore`
- **问题描述**: 
  - 缺少 `:memory:*` 模式（内存数据库文件）
  - 缺少 `dist_*` 模式（临时构建目录）
  - 缺少 `.audit/` 目录排除
  - 当前 `.gitignore` 只排除 `dist/` 而非 `dist*`
- **建议修复**: 
  1. 添加 `:memory:*` 到 .gitignore
  2. 添加 `dist_*` 或 `dist*/` 到 .gitignore
  3. 添加 `.audit/` 到 .gitignore
  4. 验证所有临时文件都被排除

#### 81. [源代码] [中严重] [process.env 访问模式不安全]
- **文件/路径**: 全局，约 952 处访问
- **问题描述**: 
  - 配置直接暴露在代码中
  - 没有统一的配置管理模块
  - 环境变量可能未经过滤暴露敏感信息
  - 没有配置验证
- **建议修复**: 
  1. 创建 `src/config/index.ts` 统一配置管理
  2. 使用 `zod` 验证环境变量（项目已有 zod 依赖）
  3. 在启动时验证所有必需配置
  4. 添加敏感信息过滤

#### 82. [源代码] [低严重] [Buffer.from 使用可能存在安全风险]
- **文件/路径**: `src/platform/five-plane-interface/api/oidc-oauth-service.ts`, `api-auth-service.ts`, `webhook/index.ts`
- **问题描述**: 
  - 多处使用 `Buffer.from` API
  - Node.js 中 `Buffer.from` 可能存在安全风险如果使用不当
  - 现代 Node.js 建议使用 `TextEncoder` 或 `Uint8Array`
- **建议修复**: 
  1. 审查所有 `Buffer.from` 使用场景
  2. 考虑迁移到 `TextEncoder`/`TextDecoder`
  3. 添加安全 lint 规则检查

#### 83. [文档] [中严重] [deploy/chaos 场景配置不完整]
- **文件/路径**: `deploy/chaos/`
- **问题描述**: 
  - 只有 4 个 YAML 文件：
    - `network-delay.yaml`
    - `pod-kill.yaml`
    - `postgres-disconnect.yaml`
    - `redis-disconnect.yaml`
  - 缺少关键场景：
    - 内存耗尽
    - CPU 耗尽
    - 网络分区
    - 数据库连接池耗尽
    - Redis 连接断开
  - chaos 测试覆盖不足
- **建议修复**: 
  1. 添加更多 chaos 场景
  2. 与 `ops-maturity/chaos/` 目录协调
  3. 文档化每个场景的预期影响

#### 84. [文档] [中严重] [deploy/runbooks 只有一个文件]
- **文件/路径**: `deploy/runbooks/`
- **问题描述**: 
  - 只有 `production-alert-runbook.md` (2010字节)
  - 缺少关键 runbook：
    - 部署程序
    - 回滚程序
    - 故障排除指南
    - 紧急联系人
    - 升级路径
- **建议修复**: 
  1. 创建标准 runbook 模板
  2. 填充常见故障场景
  3. 添加联系人信息

#### 85. [源代码] [中严重] [oapeflir 目录与 five-plane-orchestration 职责不清]
- **文件/路径**: 
  - `src/platform/five-plane-execution/oapeflir/`
  - `src/platform/five-plane-orchestration/oapeflir/`
- **问题描述**: 
  - 两个目录都存在 `oapeflir` 模块
  - 一个在 execution plane，一个在 orchestration plane
  - 根据架构文档，oapeflir 应该是跨 plane 的编排逻辑
  - 这种并行结构可能导致混淆和维护困难
- **建议修复**: 
  1. 明确 oapeflir 的单一真实来源
  2. 如果需要跨 plane 共享，创建独立模块
  3. 文档化架构边界

#### 86. [源代码] [中严重] [ha (高可用) 模块结构复杂]
- **文件/路径**: `src/platform/five-plane-execution/ha/` (25个文件)
- **问题描述**: 
  - HA 模块有 25 个文件，表明功能复杂
  - 包含 `ha-coordinator-service-inner.js` 等核心服务
  - 与 `lease/` 模块可能有重叠职责
- **建议修复**: 
  1. 审计 HA 模块的职责边界
  2. 与 lease 模块协调，确保无重复
  3. 考虑将 HA 逻辑提取为独立服务

#### 87. [源代码] [中严重] [dispatcher 模块 - 14个文件]
- **文件/路径**: `src/platform/five-plane-execution/dispatcher/` (14个文件)
- **问题描述**: 
  - `dispatcher.ts` 和 `dispatcher/` 目录同时存在
  - 可能是符号链接或重复结构
  - `admission-controller.js` 是关键组件
- **建议修复**: 
  1. 确认 `dispatcher.ts` 是否是过时的入口点
  2. 统一使用 `dispatcher/index.ts` 作为入口
  3. 文档化 dispatcher 的子模块结构

#### 88. [源代码] [中严重] [recovery 模块 - 29个文件过于庞大]
- **文件/路径**: `src/platform/five-plane-execution/recovery/` (29个文件)
- **问题描述**: 
  - recovery 是执行引擎的关键容错机制
  - 29 个文件表明功能复杂
  - 可能需要拆分为子模块：
    - `recovery/retry/` - 重试策略
    - `recovery/compensation/` - 补偿逻辑
    - `recovery/checkpoint/` - 检查点恢复
- **建议修复**: 
  1. 评估 recovery 模块的子模块划分
  2. 将复杂逻辑拆分为独立服务
  3. 确保每个子模块有清晰的职责

#### 89. [源代码] [低严重] [5个符号链接存在但未被文档化]
- **文件/路径**: `src/platform/` 下的 5 个符号链接
- **问题描述**: 
  - 没有文档说明为什么使用符号链接而非实际目录
  - 没有 CI 检查符号链接有效性
  - 在某些操作系统上可能表现不同
- **建议修复**: 
  1. 在 `src/platform/README.md` 中文档化符号链接策略
  2. 在 CI 中添加符号链接检查
  3. 考虑迁移到实际目录结构

#### 90. [测试] [中严重] [tests/unit/helpers/index.test.ts 被排除]
- **文件/路径**: `tests/unit/helpers/index.test.ts`
- **问题描述**: 
  - helpers 本身的测试被排除
  - 这意味着测试基础设施没有测试保护
  - 如果 helpers 有 bug，所有依赖它的测试都可能受影响
- **建议修复**: 
  1. 调查为什么这个测试被排除
  2. 启用该测试或删除（如果无意义）
  3. 确保测试基础设施有测试覆盖

#### 91. [配置] [低严重] [tsconfig.temp.json 存在]
- **文件/路径**: `tsconfig.temp.json`
- **问题描述**: 
  - 临时配置文件未被清理
  - 可能导致开发者困惑使用哪个配置
  - 可能包含过时的配置选项
- **建议修复**: 
  1. 删除 `tsconfig.temp.json`
  2. 或重命名为 `tsconfig.example.json` 作为参考
  3. 确保只有 `tsconfig.json` 和 `tsconfig.build.json` 两个正式配置

#### 92. [文档] [低严重] [docs_zh/operations/current_todo_list.md 过大]
- **文件/路径**: `docs_zh/operations/current_todo_list.md` (38KB)
- **问题描述**: 
  - 一个 TODO 列表文件过大（38KB）
  - 说明有大量待办事项
  - 文档与实际代码状态可能脱节
- **建议修复**: 
  1. 定期同步 TODO 列表
  2. 将已完成的项目移到 CHANGELOG
  3. 考虑使用 issue tracker 替代文档 TODO

#### 93. [源代码] [中严重] [memory 模块 - 27个文件可能过于庞大]
- **文件/路径**: `src/platform/five-plane-state-evidence/memory/` (27个文件)
- **问题描述**: 
  - memory 模块是状态证据的重要组成部分
  - 27 个文件可能需要按功能拆分：
    - `memory/session/` - 会话内存
    - `memory/trust/` - 信任级别
    - `memory/compaction/` - 压缩逻辑
- **建议修复**: 
  1. 评估 memory 模块的子模块划分
  2. 将相关功能组合到子目录
  3. 确保每个子模块有清晰的职责

#### 94. [源代码] [中严重] [events 模块 - 22个文件结构复杂]
- **文件/路径**: `src/platform/five-plane-state-evidence/events/` (22个文件)
- **问题描述**: 
  - events 是 state-evidence 的核心组件
  - 包含 `durable-event-bus.ts` (1214行) 巨型文件
  - `typed-event-bus.ts` 是事件总线类型化封装
  - 结构可能需要重组
- **建议修复**: 
  1. 将 events 拆分为 `events/durable/` 和 `events/typed/`
  2. 提取 `durable-event-bus.ts` 的子模块
  3. 统一事件类型定义

#### 95. [源代码] [中严重] [truth 模块 - 28个文件可能过于庞大]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/` (28个文件)
- **问题描述**: 
  - truth 是权威存储模块
  - 包含 `authoritative-sql-database.ts` 等核心组件
  - 28 个文件需要评估是否可以拆分
- **建议修复**: 
  1. 评估 truth 模块的子模块划分
  2. 按实体类型拆分：`truth/task/`、`truth/workflow/`、`truth/session/`
  3. 提取共享数据库访问逻辑

#### 96. [源代码] [中严重] [checkpoint 模块 - 8个文件但有巨型文件]
- **文件/路径**: `src/platform/five-plane-state-evidence/checkpoints/` (8个文件)
- **问题描述**: 
  - `node-run-checkpoint-migration.test.ts` 测试失败
  - checkpoint 是工作流恢复的关键机制
  - 测试失败可能导致数据丢失风险
- **建议修复**: 
  1. 调查 checkpoint 迁移测试失败原因
  2. 确保检查点机制可靠
  3. 添加更多 checkpoint 相关测试

#### 97. [测试] [中严重] [E2E 测试被排除 - 关键工作流无保护]
- **文件/路径**: 多个 E2E 测试
- **问题描述**: 
  - `execution-ticket-lifecycle.test.ts` 被排除
  - `multi-region-failover.test.ts` 被排除
  - `task-status-lifecycle.test.ts` 被排除
  - `workflow-state-transitions.test.ts` 被排除
  - `workflow-timeout-flow.test.ts` 被排除
  - 这些是核心端到端工作流测试
- **建议修复**: 
  1. 调查每个 E2E 测试被排除的原因
  2. 修复或启用这些测试
  3. 确保核心工作流有 E2E 测试保护

#### 98. [源代码] [低严重] [UI apps 目录结构 - 多平台支持复杂性]
- **文件/路径**: `ui/apps/`
- **问题描述**: 
  - `apps/web`, `apps/mobile`, `apps/electron-win`, `apps/tauri-linux`, `apps/tauri-macos`
  - 这么多平台变体增加维护复杂性
  - 需要确认哪些是活跃开发的
- **建议修复**: 
  1. 文档化每个平台的维护状态
  2. 评估是否可以合并一些平台
  3. 考虑使用跨平台框架减少变体

#### 99. [源代码] [低严重] [UI packages/features 数量过多]
- **文件/路径**: `ui/packages/features/`
- **问题描述**: 
  - features 目录包含多个功能包
  - 需要确认每个功能的活跃状态
  - 功能包过多可能增加依赖复杂性
- **建议修复**: 
  1. 审计每个功能包的使用情况
  2. 移除未使用的功能包
  3. 合并相关功能

#### 100. [安全] [中严重] [.audit 目录未被 gitignore]
- **文件/路径**: `.audit/`
- **问题描述**: 
  - `.audit/` 目录包含审计数据
  - `delegation-audit-events.json` 包含 65 条审计事件
  - 可能包含敏感信息但未被 gitignore 排除
- **建议修复**: 
  1. 将 `.audit/` 添加到 .gitignore
  2. 审计数据应存储在安全位置
  3. 确保测试数据已匿名化

#### 101. [源代码] [中严重] [platform-module-catalog.ts 与 platform-mainline-bootstrap.ts 并存]
- **文件/路径**: 
  - `src/platform/platform-module-catalog.ts`
  - `src/platform/platform-mainline-bootstrap.ts`
  - `src/platform/five-plane-runtime-bootstrap.ts`
- **问题描述**: 
  - 三个引导文件可能造成混淆
  - 不清楚哪个是当前使用的
  - `platform-mainline-bootstrap.ts` 名称暗示"mainline"是规范入口
- **建议修复**: 
  1. 明确哪个是主引导文件
  2. 移除或重命名其他引导文件
  3. 添加文档说明引导流程

#### 102. [源代码] [低严重] [contracts 目录两处存在需要整合]
- **文件/路径**: 
  - `src/platform/contracts/` (151个子目录)
  - `src/platform/five-plane-control-plane/compliance/` (有 contract 相关内容)
- **问题描述**: 
  - contracts 模块分布在多处
  - `src/platform/contracts/` 有 151 个子目录，表明功能庞大
  - 需要评估是否需要拆分
- **建议修复**: 
  1. 评估 contracts 模块的结构
  2. 按领域拆分 contracts
  3. 确保单一真实来源

#### 103. [测试] [中严重] [测试覆盖率与 mutation 测试覆盖分离]
- **文件/路径**: `.c8rc.json` 和 `stryker.config.mjs`
- **问题描述**: 
  - c8 (代码覆盖率) 和 stryker (变异测试) 配置分离
  - 两个工具的覆盖率阈值可能不一致
  - c8: `100%` 覆盖 threshold
  - stryker: `high: 80, low: 60, break: 50`
- **建议修复**: 
  1. 统一覆盖率阈值配置
  2. 在 CI 中同时运行两个工具
  3. 文档化覆盖率要求

#### 104. [源代码] [中严重] [工具执行器 - MCP 工具 guard 存在]
- **文件/路径**: `src/platform/five-plane-execution/tool-executor/mcp-tool-guard.ts`
- **问题描述**: 
  - MCP (Model Context Protocol) 工具 guard 是安全关键组件
  - 需要确认其配置和策略是否正确
  - 需要确认与 config/security/mcp策略的一致性
- **建议修复**: 
  1. 审查 MCP 工具 guard 实现
  2. 确认策略配置完整性
  3. 添加 MCP 安全测试

#### 105. [配置] [中严重] [环境配置命名不一致]
- **文件/路径**: `config/environments/` 和 `config/security/`
- **问题描述**: 
  - environments: `dev.json`, `staging.json`, `pre-prod.json`, `prod.json`, `test.json`
  - security: `dev.json`, `staging.json`, `pre-prod.json`, `prod.json`, `test.json`
  - 但 test.json 在 security 目录下与 environments 目录下的内容可能不一致
  - 缺少 `default.json` 作为回退配置
- **建议修复**: 
  1. 添加 `config/environments/default.json` 和 `config/security/default.json`
  2. 确保环境配置有明确回退顺序
  3. 文档化配置优先级

#### 106. [源代码] [高严重] [Execution Lease 与 HA Lease 职责重叠]
- **文件/路径**: 
  - `src/platform/five-plane-execution/lease/execution-lease-service.js`
  - `src/platform/five-plane-execution/ha/ha-coordinator-service-inner.js`
- **问题描述**: 
  - Lease 服务和 HA 服务可能有重叠职责
  - 需要确认哪个是权威的租约管理服务
  - 两个服务同时操作可能导致竞争条件
- **建议修复**: 
  1. 审计 lease 和 ha 模块的职责边界
  2. 明确租约管理的单一真实来源
  3. 添加集成测试验证无竞争条件

#### 107. [测试] [高严重] [budget-allocator.test.ts 测试失败 - 核心模块无 CI 保护]
- **文件/路径**: `tests/unit/platform/five-plane-execution/budget-allocator.test.ts`
- **问题描述**: 
  - 根据 `.audit/quality.md`，该测试失败
  - budget-allocator 是预算分配核心模块
  - 该测试同时被 exclude 在 tsconfig.json 中
  - 意味着核心预算功能没有 CI 保护
- **建议修复**: 
  1. 立即调查 budget-allocator 测试失败原因
  2. 修复测试使其通过
  3. 将其从 tsconfig exclude 列表移除
  4. 确保 budget allocator 有完整的单元测试覆盖

#### 108. [测试] [中严重] [worker-pool 测试失败 - 关键并发模块无保护]
- **文件/路径**: `tests/unit/platform/five-plane-execution/worker-pool/worker-pool-comprehensive.test.ts`
- **问题描述**: 
  - 根据 `.audit/quality.md`，该测试失败
  - worker-pool 是执行引擎的关键并发组件
  - 测试失败可能导致并发问题未被及时发现
- **建议修复**: 
  1. 调查 worker-pool 测试失败原因
  2. 修复测试
  3. 添加更多并发场景测试

#### 109. [源代码] [中严重] [dispatcher/admission-controller.js 复杂度高]
- **文件/路径**: `src/platform/five-plane-execution/dispatcher/admission-controller.js`
- **问题描述**: 
  - 准入控制是系统稳定性的关键
  - 包含 `AdmissionBackpressureSnapshot` 等复杂类型
  - 需要确认背压策略是否正确实现
- **建议修复**: 
  1. 审查 admission-controller 实现
  2. 添加准入控制测试
  3. 文档化背压策略

#### 110. [文档] [中严重] [架构文档与实现细节存在不一致风险]
- **文件/路径**: `docs_zh/architecture/00-platform-architecture.md` (728KB)
- **问题描述**: 
  - 文档过大（728KB），难以保持与实现同步
  - 快速迭代期间文档可能滞后
  - 没有自动化方式验证架构合规性
- **建议修复**: 
  1. 将架构文档拆分为更小的模块化文档
  2. 添加架构合规性检查的 CI 测试
  3. 使用 ADR 记录重大变更并自动更新文档索引

---

### 总结

本次自动Review（第三轮 - 深入专项检查）发现了44个新问题，其中：

**高优先级 (需要立即处理)**:
1. Interface 一致性问题：多处导入路径不一致
2. IAM、budget-allocator、durable-event-bus 测试失败且被 exclude
3. config/security 环境配置差异严重（test.json 为空，staging 与 prod 不一致）
4. Execution Lease 与 HA Lease 职责重叠
5. 核心模块（budget-allocator、runtime-state-machine、transition-service）没有 mutation 测试覆盖

**中优先级**:
1. tool-executor (35文件)、execution-engine (29文件)、recovery (29文件) 模块过于庞大
2. 5个符号链接未文档化且无 CI 检查
3. `deploy/chaos` (4场景) 和 `deploy/runbooks` (1文件) 配置不完整
4. `.audit/` 和 `:memory:*` 等未在 .gitignore 中
5. `tests/unit/helpers/index.test.ts` 被排除，测试基础设施无保护
6. oapeflir 在 execution 和 orchestration 两处存在
7. platform-module-catalog 与平台引导文件混淆

**低优先级**:
1. Buffer.from API 使用建议迁移到 TextEncoder
2. docs_zh/operations/current_todo_list.md 过大 (38KB)
3. tsconfig.temp.json 应清理
4. UI apps 多平台复杂性
5. contracts 模块 (151子目录) 可能需要拆分

*Review generated: 2026/05/13*

---

## 2026/05/13 自动Review报告 (第四轮 - 深入源码与配置检查)

### 发现的问题

#### 111. [源代码] [高严重] [巨型源文件 - harness/index.ts 超过2300行]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 这是目前发现的最大源文件，远超之前报告的 budget-allocator (931行)
  - 包含 `HarnessRuntimeService` 等核心编排逻辑
  - 文件过大导致难以维护、测试和理解
  - 根据 CLAUDE.md，harness 是用于 agent 运行时编排的关键模块
- **建议修复**: 
  1. 将 harness 拆分为多个子模块：
     - `harness/runtime/` - 运行时服务
     - `harness/guards/` - 安全 guard
     - `harness/entry/` - 入口点
  2. 提取共享类型到 `harness-types.ts`
  3. 评估是否需要进一步拆分

#### 112. [源代码] [高严重] [process.env 访问次数严重低估]
- **文件/路径**: 全局
- **问题描述**: 
  - 之前报告约 952 处 `process.env` 访问
  - 实际统计显示约 **5176 处**直接访问
  - 这意味着配置管理极不规范
  - 大量环境变量直接散落在代码中
  - 敏感信息可能通过未过滤的 `process.env` 暴露
  - 没有统一的配置验证机制
- **建议修复**: 
  1. 创建 `src/config/index.ts` 统一配置管理
  2. 使用 `zod` (项目已有依赖) 验证所有环境变量
  3. 将 `process.env` 访问限制在配置模块内
  4. 在整个代码库中搜索并替换直接 `process.env` 访问

#### 113. [测试] [高严重] [测试失败率2.8%，1620个失败测试]
- **文件/路径**: 全局测试
- **问题描述**: 
  - 根据 `.audit/quality.md` (2026-05-13):
    - 总测试数: 58,382
    - 通过: 56,762
    - 失败: 1,620 (2.8% 失败率)
  - 14 个测试文件失败，包括:
    - IAM 模块 (access-model, field-encryption)
    - Execution 模块 (budget-allocator, worker-pool, execution-dispatch)
    - State Evidence 模块 (durable-event-bus, checkpoint-migration)
  - 这些是核心安全、状态管理和执行引擎的测试
- **建议修复**: 
  1. 立即调查并修复这 14 个失败测试
  2. 优先修复 IAM 和 Execution 相关的失败
  3. 将失败测试状态添加到 issue tracker
  4. 在 CI 中添加测试通过率门槛 (如 >99%)

#### 114. [源代码] [高严重] [stryker mutation 测试覆盖严重不足]
- **文件/路径**: `stryker.config.mjs`
- **问题描述**: 
  - 变异测试只覆盖 8 个文件，全部位于 `src/core/` 目录
  - 完全没有覆盖五层架构核心模块：
    - `five-plane-execution/` (budget-allocator, runtime-state-machine, transition-service)
    - `five-plane-control-plane/` (iam, approval-center)
    - `five-plane-state-evidence/` (durable-event-bus, truth)
    - `five-plane-orchestration/` (harness, oapeflir)
  - 核心业务逻辑的变异覆盖率 = 0%
- **建议修复**: 
  1. 将核心模块添加到 `stryker.config.mjs` 的 `mutate` 列表
  2. 优先添加: budget-allocator.ts, runtime-state-machine.ts, durable-event-bus.ts
  3. 平衡测试成本与覆盖率需求
  4. 考虑为 mutation 测试创建专门的配置

#### 115. [源代码] [中严重] [多文件超过1000行未被报告]
- **文件/路径**: 多个源文件
- **问题描述**: 发现以下巨型文件之前未被报告：
  - `src/platform/five-plane-orchestration/harness/index.ts` (2317行) - 最大
  - `src/platform/contracts/executable-contracts/index.ts` (2169行)
  - `src/interaction/nl-gateway/index.ts` (1669行)
  - `src/platform/five-plane-orchestration/oapeflir/oapeflir-loop-service.ts` (1497行)
  - `src/platform/five-plane-orchestration/routing/intake-router.ts` (1328行)
  - `src/platform/shared/observability/slo-alerting-service.ts` (1270行)
  - `src/scale-ecosystem/tenant-platform/tenant-platform-service.ts` (1231行)
- **建议修复**: 
  1. 将所有超过 1000 行的文件加入拆分计划
  2. 按单一职责原则拆分
  3. 创建代码质量门禁限制文件大小 (如最大 500 行)

#### 116. [配置] [中严重] [.gitignore 遗漏大量临时文件和目录]
- **文件/路径**: `.gitignore`
- **问题描述**: 
  - 缺少 `:memory:*` - 内存数据库文件
  - 缺少 `dist_*` - 多个临时构建目录
  - 缺少 `.audit/` - 审计数据和报告
  - 缺少 `session-replay/` - 会话回放数据
  - 缺少 `artifacts/` - 工件目录
  - 当前 `dist/` 只是单个目录，未覆盖 `dist_*` 模式
- **建议修复**: 
  1. 添加 `:memory:*` 到 .gitignore
  2. 添加 `dist*` 或 `dist_*/` 模式
  3. 添加 `.audit/` 排除
  4. 添加 `session-replay/` 排除
  5. 验证所有临时文件都被正确排除

#### 117. [配置] [中严重] [config/environments 与 config/security 配置不对齐]
- **文件/路径**: `config/environments/` 和 `config/security/`
- **问题描述**: 
  - `environments/dev.json` 有完整配置 (registry, namespace, clusterName)
  - `security/dev.json` 内容为 `{}` (空对象)
  - `environments/test.json` 有完整配置
  - `security/test.json` 内容为 `{}` (空对象)
  - 这种不对齐导致:
    1. 配置来源混乱
    2. 缺少默认配置 (default.json)
    3. 环境特定配置不一致
- **建议修复**: 
  1. 创建 `config/security/default.json` 作为默认配置
  2. 确保 `security/dev.json` 和 `security/test.json` 有实际配置值
  3. 文档化配置合并策略
  4. 添加配置验证在应用启动时

#### 118. [源代码] [中严重] [multiple bootstrap 文件导致混淆]
- **文件/路径**: 
  - `src/platform/platform-mainline-bootstrap.ts`
  - `src/platform/five-plane-runtime-bootstrap.ts`
  - `src/platform/platform-module-catalog.ts`
- **问题描述**: 
  - 存在三个引导/模块目录文件
  - `platform-mainline-bootstrap.ts` 名称暗示是 mainline 入口
  - `five-plane-runtime-bootstrap.ts` 是五层架构的引导
  - 不清楚哪个是当前使用的真实入口
  - 可能导致开发者在错误位置添加代码
- **建议修复**: 
  1. 明确标识哪个是主引导文件
  2. 在文档中说明每个引导文件的用途
  3. 移除或重命名不再使用的引导文件
  4. 添加 Deprecation 注释到过时的引导文件

#### 119. [源代码] [中严重] [contracts 模块过大 (151个子目录)]
- **文件/路径**: `src/platform/contracts/` (151个子目录)
- **问题描述**: 
  - contracts 模块包含 151 个子目录
  - 包含大量合约定义:
    - constants, control-directive, delegation-request
    - errors.ts, evidence-record, executable-contracts
    - execution-plan, execution-receipt, model-request/response
    - prompt-bundle, state-command, types
  - 这种巨大的模块可能需要按领域拆分
  - 导入路径可能变得复杂
- **建议修复**: 
  1. 评估 contracts 模块是否可以按领域拆分
  2. 创建 `contracts/core/`, `contracts/execution/`, `contracts/delegation/` 等子目录
  3. 确保每个子模块有单一职责
  4. 维护向后兼容的导出

#### 120. [源代码] [中严重] [domains 目录过大 (60个条目)]
- **文件/路径**: `src/domains/` (60个条目)
- **问题描述**: 
  - 包含 34 个业务领域目录:
    - academic-research, advertising, agriculture, content-moderation
    - creative-production, customer-service, data-engineering, ecommerce
    - education, executive-assistant, facilities, finance-accounting
    - financial-services, game-dev, healthcare, human-resources, etc.
  - 加上 governance, recipes, registry 等子模块
  - 领域间依赖关系不明确
  - 维护这么多领域可能需要大量资源
- **建议修复**: 
  1. 创建领域依赖关系图
  2. 评估是否可以按行业分组 (金融、医疗、教育等)
  3. 提取领域共享的基础模块到 `domains/core/`
  4. 考虑领域生命周期管理 (活跃、维护、弃用)

#### 121. [源代码] [中严重] [tool-executor 目录 35 个文件，包含巨型文件]
- **文件/路径**: `src/platform/five-plane-execution/tool-executor/` (35个文件)
- **问题描述**: 
  - 多个文件超过 20KB:
    - `command-executor.ts` (24468字节)
    - `code-diagnostics-service.ts` (17260字节)
    - `command-security.ts` (20071字节)
    - `edit-replacement-service.ts` (24108字节)
    - `tool-metadata.ts` (21162字节)
    - `patch-dsl-service.ts` (23783字节)
    - `skill-execution-core-methods.ts` (25502字节)
  - 这些是工具执行相关的核心服务，职责过多
- **建议修复**: 
  1. 按功能拆分为子目录:
     - `tool-executor/core/` - 核心执行
     - `tool-executor/diagnostics/` - 诊断服务
     - `tool-executor/security/` - 安全检查
     - `tool-executor/skills/` - 技能执行
  2. 提取共享类型到 `tool-types.ts`
  3. 简化 index.ts 导出

#### 122. [源代码] [中严重] [execution-engine 目录 32 个文件]
- **文件/路径**: `src/platform/five-plane-execution/execution-engine/` (32个文件)
- **问题描述**: 
  - 包含 `multi-step-orchestration.ts` (26820字节)
  - 包含 `multi-step-supervisor.ts` (31162字节)
  - 包含 `single-task-happy-path.ts` (28833字节)
  - 包含 `call-governance.ts` (24515字节)
  - 包含 `model-call-provider.ts` (22161字节)
  - 这些是执行引擎的核心，文件过大
- **建议修复**: 
  1. 将 orchestration 逻辑拆分为独立模块
  2. 将 supervisor 拆分为多个专门的 supervisor
  3. 提取共享的状态和类型定义
  4. 考虑引入策略模式重构

#### 123. [源代码] [中严重] [oapeflir 在 execution 和 orchestration 两处存在]
- **文件/路径**: 
  - `src/platform/five-plane-execution/oapeflir/`
  - `src/platform/five-plane-orchestration/oapeflir/`
- **问题描述**: 
  - 两个目录都存在 oapeflir 模块
  - `oapeflir-loop-service.ts` 在 orchestration (1497行)
  - execution 下的 oapeflir 可能与其他模块交互
  - 这种并行结构可能导致:
    1. 代码重复
    2. 职责不清
    3. 版本不一致
- **建议修复**: 
  1. 明确 oapeflir 的单一真实来源
  2. 如果需要跨 plane 共享，创建共享模块
  3. 文档化架构边界
  4. 避免在两个位置同时修改

#### 124. [配置] [中严重] [deploy/chaos 只有4个场景]
- **文件/路径**: `deploy/chaos/` (4个YAML文件)
- **问题描述**: 
  - 现有场景:
    - network-delay.yaml
    - pod-kill.yaml
    - postgres-disconnect.yaml
    - redis-disconnect.yaml
  - 缺少关键场景:
    - 内存耗尽
    - CPU 耗尽
    - 网络分区
    - 数据库连接池耗尽
    - Redis 连接断开
    - 等等
  - 项目的 stable gate 包含 chaos 测试，但没有足够场景
- **建议修复**: 
  1. 添加更多 chaos 场景覆盖常见故障
  2. 与 `ops-maturity/chaos/` 目录协调
  3. 文档化每个场景的预期影响
  4. 增加场景到 CI 的 stable gate

#### 125. [配置] [低严重] [deploy/runbooks 只有一个文件]
- **文件/路径**: `deploy/runbooks/` (1个文件)
- **问题描述**: 
  - 只有 `production-alert-runbook.md` (2010字节)
  - 缺少关键 runbook:
    - 部署程序
    - 回滚程序
    - 故障排除指南
    - 紧急联系人
    - 升级路径
- **建议修复**: 
  1. 创建标准 runbook 模板
  2. 填充常见故障场景
  3. 添加联系人信息和升级路径

#### 126. [安全] [高严重] [.audit 目录未被 gitignore，包含敏感数据]
- **文件/路径**: `.audit/`
- **问题描述**: 
  - `.audit/delegation/delegation-audit-events.json` 包含 65 条审计事件
  - 包含 `parentAgentId`, `childAgentId`, `actorId` 等标识符
  - `.audit/quality.md` 包含测试失败报告
  - 这些文件未被 .gitignore 排除
  - 可能被意外提交到版本控制
- **建议修复**: 
  1. 将 `.audit/` 添加到 .gitignore
  2. 审计数据应存储在安全位置
  3. 确保测试数据已匿名化

#### 127. [安全] [中严重] [.env.example 缺少安全警告和指引]
- **文件/路径**: `.env.example`
- **问题描述**: 
  - `AA_API_JWT_SECRET=` 为空，但缺少生成命令的明显指引
  - 虽然有注释说明需要配置，但没有警告可能的安全后果
  - 缺少 secret 轮换策略文档
  - 缺少 secret 管理最佳实践
- **建议修复**: 
  1. 在注释中添加更明确的安全警告
  2. 提供生成命令示例 (已存在但需要更明显)
  3. 文档化 secret 轮换策略
  4. 考虑添加 secret 验证在应用启动时

#### 128. [源代码] [中严重] [StructuredLogger 未被广泛采用]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - 项目定义了 StructuredLogger
  - 但仍有多个文件直接使用 console.*
  - 包括:
    - `src/index.ts` - console.log/error
    - `src/ops-maturity/chaos/chaos-experiment-scheduler.ts` - console.log/warn
    - `src/platform/contracts/*.ts` - console.warn
    - 等等
  - 结构化日志的好处未实现
- **建议修复**: 
  1. 创建 ESLint 规则禁止直接使用 console.*
  2. 替换所有 console.* 调用为 StructuredLogger
  3. 添加 CI 检查捕获新的 console.* 使用

#### 129. [源代码] [低严重] [17个 TODO/FIXME/HACK 标记未处理]
- **文件/路径**: 多个源文件
- **问题描述**: 
  - 之前报告 13 个标记，实际统计有 17 个
  - 这些标记表示未完成的工作
  - 如果不处理，会积累技术债务
- **建议修复**: 
  1. 将 TODO 转换为 GitHub Issue
  2. 设置 lint 规则检查这些标记
  3. 要求 PR 中处理相关 TODO

#### 130. [文档] [中严重] [docs_zh 与 docs_en 同步机制缺失]
- **文件/路径**: `docs_zh/` 和 `docs_en/`
- **问题描述**: 
  - 两个文档目录并列存在
  - 需要确保内容同步
  - 未看到自动化同步机制
  - 手动同步容易出错和滞后
- **建议修复**: 
  1. 创建文档同步检查的 CI 流程
  2. 或明确文档翻译流程和责任人
  3. 考虑使用自动化翻译工具辅助

#### 131. [测试] [中严重] [单元测试被 exclude 数量巨大]
- **文件/路径**: `tsconfig.json` exclude 部分
- **问题描述**: 
  - 排除了约 80+ 个测试文件和目录
  - 包括整个 `tests/unit/platform/five-plane-execution/` 目录
  - 包括所有 `tests/integration/platform/*/` 目录
  - 包括 11 个 `tests/unit/platform/***/` 目录
  - 包括 13 个 `tests/unit/scale-ecosystem/***/` 目录
  - 包括 4 个 E2E 测试文件
  - 这意味着大部分测试在 CI 中不运行
- **建议修复**: 
  1. 调查每个测试被排除的根本原因
  2. 修复或重新启用这些测试
  3. 如果是故意排除的扩展测试，创建单独的 tsconfig
  4. 设置目标：排除的测试 < 5%

#### 132. [部署] [低严重] [UI 独立部署架构增加复杂性]
- **文件/路径**: `ui/` 目录
- **问题描述**: 
  - UI 有独立的 `package.json` 和 `node_modules`
  - 使用 Turborepo 工作区
  - 需要单独的部署流程
  - Playwright 配置存在但需确认 CI 是否运行
- **建议修复**: 
  1. 确保 Playwright E2E 测试在 CI 中运行
  2. 文档化 UI 服务的部署流程
  3. 考虑 UI 与主应用的一体化部署

#### 133. [源代码] [中严重] [ha 和 lease 模块职责重叠]
- **文件/路径**: 
  - `src/platform/five-plane-execution/ha/`
  - `src/platform/five-plane-execution/lease/`
- **问题描述**: 
  - HA (高可用) 和 Lease 服务可能有重叠职责
  - HA 模块有 25 个文件
  - Lease 模块有 11 个文件
  - 需要确认哪个是权威的租约管理服务
- **建议修复**: 
  1. 审计 ha 和 lease 模块的职责边界
  2. 明确租约管理的单一真实来源
  3. 添加集成测试验证无竞争条件

#### 134. [源代码] [中严重] [recovery 目录 29 个文件过于庞大]
- **文件/路径**: `src/platform/five-plane-execution/recovery/` (29个文件)
- **问题描述**: 
  - recovery 是执行引擎的关键容错机制
  - 29 个文件表明功能复杂
  - 可能需要拆分为子模块:
    - `recovery/retry/` - 重试策略
    - `recovery/compensation/` - 补偿逻辑
    - `recovery/checkpoint/` - 检查点恢复
- **建议修复**: 
  1. 评估 recovery 模块的子模块划分
  2. 将复杂逻辑拆分为独立服务
  3. 确保每个子模块有清晰的职责

#### 135. [源代码] [中严重] [memory 目录 27 个文件可能过于庞大]
- **文件/路径**: `src/platform/five-plane-state-evidence/memory/` (27个文件)
- **问题描述**: 
  - memory 模块是状态证据的重要组成部分
  - 27 个文件可能需要按功能拆分:
    - `memory/session/` - 会话内存
    - `memory/trust/` - 信任级别
    - `memory/compaction/` - 压缩逻辑
- **建议修复**: 
  1. 评估 memory 模块的子模块划分
  2. 将相关功能组合到子目录
  3. 确保每个子模块有清晰的职责

#### 136. [源代码] [中严重] [truth 目录 28 个文件可能过于庞大]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/` (28个文件)
- **问题描述**: 
  - truth 是权威存储模块
  - 包含 `authoritative-sql-database.ts` 等核心组件
  - 28 个文件需要评估是否可以拆分
- **建议修复**: 
  1. 评估 truth 模块的子模块划分
  2. 按实体类型拆分: `truth/task/`, `truth/workflow/`, `truth/session/`
  3. 提取共享数据库访问逻辑

#### 137. [源代码] [中严重] [events 目录 22 个文件结构复杂]
- **文件/路径**: `src/platform/five-plane-state-evidence/events/` (22个文件)
- **问题描述**: 
  - events 是 state-evidence 的核心组件
  - 包含 `durable-event-bus.ts` (1214行) 巨型文件
  - `typed-event-bus.ts` 是事件总线类型化封装
  - 结构可能需要重组
- **建议修复**: 
  1. 将 events 拆分为 `events/durable/` 和 `events/typed/`
  2. 提取 `durable-event-bus.ts` 的子模块
  3. 统一事件类型定义

#### 138. [配置] [低严重] [.c8rc.json 和 stryker.config.mjs 配置分离]
- **文件/路径**: `.c8rc.json` 和 `stryker.config.mjs`
- **问题描述**: 
  - 两个覆盖率工具配置分离
  - c8 (代码覆盖率) 和 stryker (变异测试) 配置分离
  - 可能导致报告不一致或覆盖缺口
- **建议修复**: 
  1. 确保两个工具的覆盖率阈值一致
  2. 在 CI 中统一报告
  3. 考虑使用单一覆盖率工具或明确分工

#### 139. [源代码] [低严重] [package.json scripts 数量过多 (100+)]
- **文件/路径**: `package.json` scripts 部分
- **问题描述**: 
  - 超过 100 个 npm scripts
  - 大量重复的 `stable:*` 脚本 (每个都调用 `npm run build`)
  - `test:raw`, `test`, `test:unit` 等脚本职责不清
  - `build` 和 `build:test` 混用可能导致测试结果不一致
- **建议修复**: 
  1. 使用 `npm run build` 统一构建脚本
  2. 合并重复的 stable 脚本到通用运行器
  3. 简化测试脚本层级
  4. 文档化每个脚本的用途

#### 140. [源代码] [低严重] [翻译脚本 translate_docs.py 维护性问题]
- **文件/路径**: `translate_docs.py` (10KB)
- **问题描述**: 
  - 项目主要是 TypeScript/Node.js
  - 存在 Python 脚本需要额外维护 Python 环境和依赖
  - 如果不常使用，可能成为技术债务
- **建议修复**: 
  1. 评估是否仍在使用
  2. 如果需要，考虑使用 TypeScript 重写
  3. 如果不用，删除

---

### 总结

本次自动Review（第四轮 - 深入源码与配置检查）发现了30个新问题，其中：

**高优先级 (需要立即处理)**:
1. `harness/index.ts` (2317行) 是最大源文件需要拆分
2. `process.env` 访问 5176 次，远超之前报告的 952 次
3. 测试失败率 2.8% (1620/58382 失败)，14 个测试文件失败
4. stryker mutation 测试覆盖严重不足 (只覆盖 src/core/ 的 8 个文件)
5. `.audit/` 目录未被 gitignore，包含敏感数据

**中优先级**:
1. 多个 1000+ 行文件未被报告 (harness, contracts, nl-gateway, etc.)
2. `.gitignore` 缺少多个临时文件模式
3. config/environments 与 config/security 配置不对齐
4. 三个 bootstrap 文件导致混淆
5. contracts (151子目录) 和 domains (60条目) 模块过大
6. tool-executor (35文件)、execution-engine (32文件) 模块过于庞大
7. oapeflir 在 execution 和 orchestration 两处存在
8. deploy/chaos 只有 4 个场景，deploy/runbooks 只有 1 个文件
9. ha 和 lease 模块职责可能重叠
10. StructuredLogger 未被广泛采用

**低优先级**:
1. 17 个 TODO/FIXME/HACK 标记未处理
2. docs_zh 与 docs_en 同步机制缺失
3. UI 独立部署架构复杂性
4. .c8rc.json 和 stryker.config.mjs 配置分离
5. package.json scripts 数量过多 (100+)
6. Python 脚本 translate_docs.py 维护性问题

*Review generated: 2026/05/13*

---

## 2026/05/13 自动Review报告 (第五轮 - 模块间依赖与接口一致性深度检查)

### 发现的问题

#### 141. [源代码] [高严重] [多层模块间循环依赖风险]
- **文件/路径**: 多个源文件
- **问题描述**: 
  - 使用 `find . -name "*.ts" | xargs grep -l "from.*'\.\."` 发现多个深层相对导入
  - 这些模式表明可能存在循环依赖:
    - `../platform/contracts/types/ids.js` 多次出现
    - `../../platform/contracts/executable-contracts/index.js` 多次出现
  - 循环依赖会导致:
    1. 构建顺序问题
    2. 模块初始化顺序不确定
    3. 测试困难
    4. 难以进行增量构建
- **建议修复**: 
  1. 使用 `madge` 或类似工具检测循环依赖
  2. 重构循环依赖为单向依赖
  3. 使用包导出替代深层相对导入
  4. 在 CI 中添加循环依赖检测

#### 142. [源代码] [高严重] [类型定义不一致 - 错误类型混乱]
- **文件/路径**: `src/platform/contracts/errors.ts` 和相关文件
- **问题描述**: 
  - 发现多处 `throw new WorkflowStateError`, `throw new ValidationError`
  - `runtime-state-machine.ts` 使用 `WorkflowStateError`
  - 错误类型定义可能不一致:
    - 有些使用 `errors.ts` 中定义的错误
    - 有些可能在本地定义
  - 缺少统一的错误层次结构
- **建议修复**: 
  1. 审查 `src/platform/contracts/errors.ts` 的错误定义
  2. 确保所有模块使用统一的错误类型
  3. 创建错误层次结构 (基类 + 具体错误)
  4. 在 ESLint 中添加规则检查错误使用

#### 143. [源代码] [高严重] [接口一致性 - 导入路径混乱]
- **文件/路径**: 多个源文件
- **问题描述**: 
  - 发现不一致的导入路径模式:
    - `import { newId } from "../../platform/contracts/types/ids.js"`
    - `import { createCompensationRecord } from "../../platform/contracts/executable-contracts/index.js"`
  - 深层相对导入违反了包导出的设计意图
  - 这种不一致会导致:
    1. 重构困难
    2. 路径别名难以维护
    3. IDE 支持不完整
- **建议修复**: 
  1. 统一使用包导出路径
  2. 运行 `npm run typecheck` 检测所有未解析的导入
  3. 创建 lint 规则检查深层相对导入
  4. 确保 exports 映射正确

#### 144. [源代码] [中严重] [monitoring/observability 配置不完整]
- **文件/路径**: `src/platform/shared/observability/` 目录
- **问题描述**: 
  - 虽然有 `structured-logger.ts`, `slo-alerting-service.ts` 等
  - 但 observability 模块包含:
    - anomaly-detection/ (2个文件)
    - slo-alerting/ (目录)
    - transports/ (目录)
    - otel-bootstrap.ts, otel-tracer.ts
    - prometheus-metrics-exporter.ts
    - metrics-server.ts, metrics-service.ts
  - 需要确认:
    1. 是否有统一的监控指标定义
    2. 是否有告警阈值配置
    3. 是否有仪表板配置
- **建议修复**: 
  1. 审查 observability 模块的完整性
  2. 确保关键指标都有对应的告警规则
  3. 添加监控指标文档

#### 145. [源代码] [中严重] [权限控制实现分散]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/` 和相关目录
- **问题描述**: 
  - IAM 模块有 28 个子目录
  - 包含:
    - `policy-engine.ts` (971行)
    - `access-model.test.ts` (失败)
    - `field-encryption.test.ts` (失败)
  - 权限控制分散在多个位置:
    - IAM 模块
    - tool-executor 中的 role-tool-exposure-service.ts
    - execution-engine 中的 call-governance.ts
  - 这种分散可能导致权限检查不一致
- **建议修复**: 
  1. 审计权限控制相关代码
  2. 确保有统一的权限检查入口
  3. 修复失败的 IAM 测试
  4. 添加权限控制集成测试

#### 146. [源代码] [中严重] [数据库 schema 不一致风险]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/` 目录
- **问题描述**: 
  - 包含多个数据库相关文件:
    - `runtime-physical-schema.ts` (8385行)
    - `schema-inventory-service.ts`
    - `migrations/` 目录
    - `sqlite/` 目录 (29个文件)
    - `postgres/` 目录 (13个文件)
  - 需要确认:
    1. SQLite 和 PostgreSQL schema 是否一致
    2. 迁移脚本是否正确
    3. 是否有 schema 版本控制
- **建议修复**: 
  1. 审查 schema 定义的一致性
  2. 确保 SQLite 和 PostgreSQL schema 同步
  3. 添加 schema 验证测试
  4. 文档化数据库迁移流程

#### 147. [源代码] [中严重] [Event Bus 实现问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/events/durable-event-bus.ts` (1214行)
- **问题描述**: 
  - `durable-event-bus-async.test.ts` 测试失败
  - 事件总线是 state-evidence 的核心组件
  - 包含消费者管理、投递、重试、背压等复杂逻辑
  - 测试失败意味着事件投递可靠性无法验证
- **建议修复**: 
  1. 调查 durable-event-bus-async.test.ts 失败原因
  2. 将 durable-event-bus.ts 拆分为多个可测试模块
  3. 确保事件投递、重试、背压等核心逻辑有测试覆盖

#### 148. [源代码] [中严重] [HA 模块与 Lease 模块职责不清]
- **文件/路径**: 
  - `src/platform/five-plane-execution/ha/`
  - `src/platform/five-plane-execution/lease/`
- **问题描述**: 
  - HA 模块有 25 个文件，包含:
    - `ha-coordinator-service-inner.js`
    - `ha-coordinator.ts`
  - Lease 模块有 11 个文件，包含:
    - `execution-lease-service.js`
  - 两个服务可能操作相同的资源 (如 node run lease)
  - 缺少明确的职责边界
- **建议修复**: 
  1. 审计 HA 和 Lease 模块的职责
  2. 明确租约管理的单一真实来源
  3. 添加竞争条件测试
  4. 文档化 HA 和 Lease 的交互方式

#### 149. [配置] [中严重] [secret 管理缺失]
- **文件/路径**: 全局
- **问题描述**: 
  - `.env.example` 中 `AA_API_JWT_SECRET=` 为空
  - 配置分散在多个位置:
    - config/environments/
    - config/security/
    - .env.example
    - 直接的 process.env 访问
  - 缺少统一的 secret 管理机制
  - 缺少 secret 轮换策略
- **建议修复**: 
  1. 实现 secret 管理方案 (如 Vault, AWS Secrets Manager)
  2. 文档化 secret 轮换流程
  3. 在代码中统一 secret 访问方式
  4. 添加 secret 验证在应用启动时

#### 150. [源代码] [中严重] [Plugin 系统架构复杂度高]
- **文件/路径**: `src/domains/registry/` (18个子目录)
- **问题描述**: 
  - Plugin 系统包含:
    - `plugin-spi.ts` (TODO 注释)
    - `plugin-runtime-child.ts`
    - `plugin-sdk/` (多个文件)
  - 需要确认:
    1. Plugin 接口是否稳定
    2. Plugin 生命周期管理是否正确
    3. Plugin 安全隔离是否充分
- **建议修复**: 
  1. 审查 Plugin SPI 接口
  2. 确保 Plugin 生命周期清晰
  3. 添加 Plugin 安全测试
  4. 文档化 Plugin 开发指南

#### 151. [源代码] [低严重] [缓存实现一致性]
- **文件/路径**: `src/platform/shared/cache/` 目录
- **问题描述**: 
  - 缓存模块包含多个存储实现:
    - `memory-cache-store.ts`
    - `sqlite-cache-store.ts`
    - `multi-level-cache-store.ts`
  - 缓存策略定义:
    - `memory-cache-policy.ts`
    - `prompt-cache-policy.ts`
    - `planner-cache-policy.ts`
    - `tool-cache-policy.ts`
  - 需要确认缓存失效策略是否一致
- **建议修复**: 
  1. 审查缓存实现的一致性
  2. 确保缓存失效策略正确
  3. 添加缓存集成测试

#### 152. [部署] [中严重] [Kubernetes 部署配置缺失]
- **文件/路径**: `deploy/` 目录
- **问题描述**: 
  - deploy/helm/ 目录存在但可能是空的或接近空
  - 缺少 Kubernetes 部署配置
  - 需要确认:
    1. Helm chart 是否完整
    2. 是否有 Kubernetes manifests
    3. 部署流程是否文档化
- **建议修复**: 
  1. 审查 Helm chart 完整性
  2. 添加必要的 Kubernetes manifests
  3. 文档化 Kubernetes 部署流程

#### 153. [文档] [低严重] [current_todo_list.md 过大 (38KB)]
- **文件/路径**: `docs_zh/operations/current_todo_list.md`
- **问题描述**: 
  - TODO 列表文件过大 (38KB)
  - 说明有大量待办事项
  - 文档与实际代码状态可能脱节
- **建议修复**: 
  1. 定期同步 TODO 列表
  2. 将已完成的项目移到 CHANGELOG
  3. 考虑使用 issue tracker 替代文档 TODO

#### 154. [源代码] [低严重] [依赖版本管理问题]
- **文件/路径**: `package.json` dependencies
- **问题描述**: 
  - 部分依赖版本使用 `^` (允许更新)
  - 部分依赖版本固定
  - 缺少依赖版本审核机制
  - 可能导致:
    1. 构建不一致
    2. 依赖引入安全漏洞
    3. 依赖更新导致破坏性变更
- **建议修复**: 
  1. 审查依赖版本策略
  2. 使用 lockfile 固定依赖版本
  3. 添加依赖安全检查到 CI
  4. 定期更新依赖并记录变更

#### 155. [测试] [中严重] [测试基础设施复杂 (78个helpers)]
- **文件/路径**: `tests/helpers/` (78个文件)
- **问题描述**: 
  - 78 个测试辅助文件说明测试基础设施复杂
  - 可能表明:
    1. 被测代码耦合度高
    2. 测试设置复杂
    3. Mock 对象管理困难
  - 需要大量 setup/teardown 代码
- **建议修复**: 
  1. 评估这些 helpers 是否可以合并或简化
  2. 考虑在源码中引入更多可测试性的设计
  3. 检查是否有重复的 helper 功能

#### 156. [源代码] [中严重] [事件类型定义重复]
- **文件/路径**: 
  - `src/platform/five-plane-state-evidence/events/event-registry.ts` (1077行)
  - `src/platform/contracts/` 下多个目录
- **问题描述**: 
  - event-registry.ts 定义了事件类型
  - contracts 目录下也有事件相关定义
  - 可能存在重复定义
- **建议修复**: 
  1. 确认事件的单一真实来源
  2. 统一事件类型定义
  3. 避免类型重复

#### 157. [源代码] [低严重] [日志级别不一致]
- **文件/路径**: 多个源文件
- **问题描述**: 
  - 有些使用 `console.log`, 有些使用 `console.warn`, 有些使用 `console.error`
  - 缺少统一的日志级别规范
  - 不利于日志集中管理和分析
- **建议修复**: 
  1. 制定日志级别规范
  2. 使用 StructuredLogger 统一日志
  3. 添加日志级别检查

#### 158. [配置] [中严重] [PostgreSQL 配置缺失]
- **文件/路径**: `config/` 目录
- **问题描述**: 
  - PostgreSQL 连接字符串在环境变量中
  - 缺少数据库连接池配置
  - 缺少查询超时配置
  - 缺少连接健康检查配置
- **建议修复**: 
  1. 添加 PostgreSQL 连接池配置
  2. 添加查询超时配置
  3. 添加连接健康检查
  4. 文档化数据库配置

#### 159. [源代码] [中严重] [安全配置分散]
- **文件/路径**: `config/security/` 目录
- **问题描述**: 
  - 安全配置分散在:
    - config/security/ (approvalMode, sandboxMode)
    - config/security/threat-matrix.json (STRIDE 威胁矩阵)
    - 运行时通过 process.env 注入
  - 缺少统一的安全配置管理
- **建议修复**: 
  1. 创建统一的安全配置模块
  2. 在启动时验证安全配置
  3. 文档化安全配置项

#### 160. [源代码] [低严重] [代码重复检查缺失]
- **文件/路径**: 全局
- **问题描述**: 
  - 没有发现明显的代码重复检查
  - 可能存在重复的 utility 函数
  - 可能存在重复的业务逻辑
- **建议修复**: 
  1. 添加代码重复检测工具
  2. 设置代码重复率阈值
  3. 定期检查并消除重复

#### 161. [测试] [中严重] [integration 测试被大量排除]
- **文件/路径**: `tsconfig.json` exclude 部分
- **问题描述**: 
  - 排除了所有 `tests/integration/platform/*/` 目录
  - 包括:
    - tests/integration/platform/five-plane-orchestration/
    - tests/integration/platform/five-plane-execution/
    - tests/integration/platform/five-plane-control-plane/
    - tests/integration/platform/five-plane-interface/
    - tests/integration/platform/model-gateway/
    - tests/integration/platform/security/
    - tests/integration/platform/five-plane-state-evidence/
  - 这意味着平台集成测试在 CI 中不运行
- **建议修复**: 
  1. 调查 integration 测试被排除的原因
  2. 修复或重新启用这些测试
  3. 确保平台集成测试在 CI 中运行

#### 162. [源代码] [中严重] [multi-step-orchestration 模块过于庞大]
- **文件/路径**: `src/platform/five-plane-execution/execution-engine/multi-step-orchestration.ts` (26820字节)
- **问题描述**: 
  - 这是执行引擎的核心编排逻辑
  - 文件超过 26KB
  - 包含 `multi-step-supervisor.ts` (31162字节)
  - 两者加起来超过 57KB 的复杂逻辑
- **建议修复**: 
  1. 将 multi-step-orchestration 拆分为多个模块
  2. 将 multi-step-supervisor 拆分为多个专门的 supervisor
  3. 提取共享类型和接口

#### 163. [源代码] [中严重] [dispatcher/admission-controller 实现复杂度]
- **文件/路径**: `src/platform/five-plane-execution/dispatcher/admission-controller.js`
- **问题描述**: 
  - 准入控制是系统稳定性的关键
  - 包含 `AdmissionBackpressureSnapshot` 等复杂类型
  - 需要确认背压策略是否正确实现
  - 需要确认背压阈值配置
- **建议修复**: 
  1. 审查 admission-controller 实现
  2. 添加准入控制测试
  3. 文档化背压策略

#### 164. [文档] [中严重] [架构文档与实现不同步]
- **文件/路径**: `docs_zh/architecture/00-platform-architecture.md` (728KB)
- **问题描述**: 
  - 文档过大 (728KB)
  - 难以保持与实现同步
  - 快速迭代期间文档可能滞后
  - 没有自动化方式验证架构合规性
- **建议修复**: 
  1. 将架构文档拆分为更小的模块化文档
  2. 添加架构合规性检查的 CI 测试
  3. 使用 ADR 记录重大变更并自动更新文档索引

#### 165. [源代码] [中严重] [MCP 工具 guard 实现验证]
- **文件/路径**: `src/platform/five-plane-execution/tool-executor/mcp-tool-guard.ts`
- **问题描述**: 
  - MCP (Model Context Protocol) 工具 guard 是安全关键组件
  - `mcpPolicy` 在 config/security/default.json 中定义
  - 需要确认:
    1. MCP 工具 guard 实现与配置一致
    2. 网络隔离配置正确
    3. 传输层只允许 stdio
- **建议修复**: 
  1. 审查 MCP 工具 guard 实现
  2. 确认策略配置完整性
  3. 添加 MCP 安全测试

---

### 总结

本次自动Review（第五轮 - 模块间依赖与接口一致性深度检查）发现了25个新问题，其中：

**高优先级 (需要立即处理)**:
1. 多层模块间循环依赖风险
2. 类型定义不一致 - 错误类型混乱
3. 接口一致性 - 导入路径混乱 (深层相对导入)
4. Event Bus 实现测试失败
5. 集成测试被大量排除 (整个 tests/integration/platform/*/)

**中优先级**:
1. monitoring/observability 配置不完整
2. 权限控制实现分散 (IAM, tool-executor, execution-engine)
3. 数据库 schema 不一致风险 (SQLite vs PostgreSQL)
4. HA 模块与 Lease 模块职责不清
5. secret 管理缺失
6. Plugin 系统架构复杂度高
7. Kubernetes 部署配置缺失
8. 安全配置分散
9. multi-step-orchestration 模块过于庞大 (26KB+31KB)
10. dispatcher/admission-controller 实现复杂度

**低优先级**:
1. 缓存实现一致性
2. current_todo_list.md 过大 (38KB)
3. 依赖版本管理问题
4. 测试基础设施复杂 (78个helpers)
5. 事件类型定义重复
6. 日志级别不一致
7. PostgreSQL 配置缺失
8. 代码重复检查缺失

*Review generated: 2026/05/13*

---

## 累计问题统计

| 轮次 | 发现问题数 | 高优先级 | 中优先级 | 低优先级 |
|------|-----------|---------|---------|---------|
| 第一轮 | 35 | 5 | 17 | 13 |
| 第二轮 | 31 | 6 | 18 | 7 |
| 第三轮 | 44 | 5 | 23 | 16 |
| 第四轮 | 30 | 5 | 18 | 7 |
| 第五轮 | 25 | 3 | 16 | 6 |
| **总计** | **165** | **24** | **92** | **49** |

### 高优先级问题汇总

1. 符号链接导致构建不一致 (src/platform/)
2. 巨型源文件需要拆分 (harness/index.ts 2317行, durable-event-bus.ts 1214行, etc.)
3. 测试被 exclude 数量巨大 (80+)
4. 测试失败率高 (2.8%, 1620个失败)
5. config/security 环境配置差异 (test.json 为空)
6. stryker mutation 测试覆盖严重不足
7. .audit/ 目录未被 gitignore
8. process.env 访问次数严重低估 (5176次)
9. contracts 和 domains 模块过大
10. 多层模块间循环依赖风险
11. 类型定义不一致 - 错误类型混乱
12. 接口一致性 - 导入路径混乱

### 建议优先处理

1. **立即修复** 14 个失败的测试文件
2. **立即修复** config/security/test.json 为空的问题
3. **立即清理** .gitignore 添加 `.audit/`, `:memory:*`, `dist_*` 等
4. **规划拆分** 所有超过 1000 行的源文件
5. **扩展 stryker** mutation 测试覆盖到 five-plane-* 模块
6. **统一配置管理** 创建 src/config/index.ts 集中管理所有配置

*Review generated: 2026/05/13*

---

## 2026-05-13 自动Review报告 (深入检查补充 - 第六轮)

### 发现的问题

#### 166. [源代码] [高严重] [巨型源文件体积异常增大]
- **文件/路径**: 
  - `src/platform/five-plane-execution/budget-allocator.ts` (34,499 字节，注：之前报告931行，但实际文件大小表明已显著增长)
  - `src/platform/five-plane-execution/runtime-state-machine.ts` (26,348 字节)
  - `src/platform/five-plane-orchestration/harness/index.ts` (2317 行)
- **问题描述**: 
  - budget-allocator.ts 文件大小达到 34KB，显示该文件自上次审查以来有大量修改
  - runtime-state-machine.ts 达到 26KB
  - 这些巨型文件违反单一职责原则，导致：
    1. 单元测试困难（需要大量 mock）
    2. 代码审查困难
    3. 重构风险高
    4. IDE 性能下降
- **建议修复**: 
  1. 将 budget-allocator.ts 拆分为多个子模块：
     - `budget-allocator/types.ts` - 接口定义
     - `budget-allocator/allocator.ts` - 主类
     - `budget-allocator/store.ts` - 存储层
     - `budget-allocator/settlement.ts` - 结算逻辑
  2. 将 runtime-state-machine.ts 拆分为：
     - `runtime-state-machine/types.ts` - 状态定义
     - `runtime-state-machine/transitions.ts` - 转换逻辑
     - `runtime-state-machine/machine.ts` - 状态机主类
  3. 将 harness/index.ts 拆分为多个子模块目录

#### 167. [配置] [高严重] [config/security/prod.json approvalMode 为 strict，但其他环境为 supervised]
- **文件/路径**: `config/security/` 目录
- **问题描述**: 
  - `prod.json`: `{"approvalMode": "strict", "allowDestructiveActions": false}`
  - `staging.json`: `{"approvalMode": "supervised"}`
  - `test.json`: `{"approvalMode": "supervised"}`
  - `dev.json`: `{"approvalMode": "supervised"}`
  - 这种差异意味着：
    1. 生产环境行为与测试/开发环境不一致
    2. 测试无法发现 strict 模式特有的 bug
    3. 部署前未充分验证 strict 模式行为
- **建议修复**: 
  1. 在 test.json 中添加 strict 模式的测试覆盖
  2. 创建 `config/security/test-strict.json` 专门测试 strict 模式
  3. 在 CI 中添加 strict 模式测试通道

#### 168. [部署] [中严重] [deploy/runbooks 目录内容过少]
- **文件/路径**: `deploy/runbooks/`
- **问题描述**: 
  - 目录只有 `production-alert-runbook.md` 一个文件
  - 运维文档 (docs_zh/operations/) 提到大量 runbook 内容但实际缺失
  - 紧急故障时无法快速响应
  - 缺少关键的 runbook：
    1. 数据库故障恢复
    2. 服务中断响应
    3. 回滚程序
    4. 扩容程序
    5. 安全事件响应
- **建议修复**: 
  1. 补充核心 runbook：
     - `database-failure-recovery-runbook.md`
     - `service-outage-runbook.md`
     - `rollback-runbook.md`
     - `scale-up-runbook.md`
     - `security-incident-runbook.md`
  2. 确保每个 runbook 包含：
     - 触发条件
     - 诊断步骤
     - 修复步骤
     - 联系人信息

#### 169. [部署] [中严重] [deploy/chaos 目录场景不完整]
- **文件/路径**: `deploy/chaos/`
- **问题描述**: 
  - 只有 4 个场景：
    - `network-delay.yaml`
    - `pod-kill.yaml`
    - `postgres-disconnect.yaml`
    - `redis-disconnect.yaml`
  - 缺少关键的 chaos 工程场景：
    1. CPU 负载测试
    2. 内存耗尽测试
    3. 磁盘 IO 阻塞
    4. 时钟偏移
    5. 网络分区
    6. 服务超时
    7. 级联故障
- **建议修复**: 
  1. 补充关键 chaos 场景
  2. 为每个场景创建对应的 runbook
  3. 在 CI 中添加 chaos 测试阶段

#### 170. [源代码] [中严重] [12个源文件直接使用 console.* 而非结构化日志]
- **文件/路径**: 
  - `src/platform/five-plane-orchestration/agent-delegation/delegation-manager.service.ts`
  - `src/platform/five-plane-orchestration/harness/runtime/runtime-entry-guard.ts`
  - `src/platform/contracts/index.ts`
  - `src/platform/contracts/execution-plan/index.ts`
  - `src/platform/contracts/execution-receipt/index.ts`
  - `src/platform/contracts/state-command/index.ts`
  - `src/platform/contracts/request-envelope/index.ts`
  - `src/platform/contracts/executable-contracts/index.ts`
  - `src/platform/five-plane-control-plane/iam/file-freshness.ts`
  - `src/platform/five-plane-control-plane/config-center/config-hot-reload-service.ts`
  - `src/platform/ops-maturity/platform-panic/panic-propagation-service.ts`
  - `src/platform/shared/observability/transports/datadog-transport.ts`
- **问题描述**: 
  - 项目定义了 `StructuredLogger` 但仍有12个文件直接使用 `console.*`
  - 不利于日志集中管理和查询
  - 无法享受结构化日志的好处
- **建议修复**: 
  1. 替换所有 `console.*` 调用为 `StructuredLogger`
  2. 创建 ESLint 规则禁止直接使用 `console.*`
  3. 在 CI 中添加检查

#### 171. [源代码] [中严重] [.gitignore 不完整 - 缺少多个临时文件模式]
- **文件/路径**: `.gitignore`
- **问题描述**: 
  - 缺少 `:memory:*` 模式（内存数据库文件）
  - 缺少 `dist_*` 模式（临时构建目录）
  - 缺少 `.audit/` 目录
  - 当前 `.gitignore` 包含 `session-replay/` 和 `artifacts/`，但未包含导致这些目录可能被追踪
- **建议修复**: 
  1. 添加 `:memory:*` 到 .gitignore
  2. 添加 `dist_*` 到 .gitignore
  3. 添加 `.audit/` 到 .gitignore
  4. 确认 `session-replay/` 和 `artifacts/` 已正确排除

#### 172. [测试] [中严重] [stryker mutation 测试只覆盖 core/api，遗漏 five-plane-* 模块]
- **文件/路径**: `stryker.config.mjs`
- **问题描述**: 
  - mutation 测试只包含：
    - `src/core/api/http-server/` (8个文件)
    - `src/core/api/middleware/` (2个文件)
    - `src/platform/five-plane-orchestration/oapeflir/oapeflir-loop-service.ts`
    - `src/core/runtime/redis-client-options.ts`
  - 遗漏了关键的 five-plane-* 模块：
    - `five-plane-execution/` (budget-allocator, runtime-state-machine)
    - `five-plane-orchestration/` (harness, planner)
    - `five-plane-control-plane/` (iam, approval-center)
    - `five-plane-state-evidence/` (durable-event-bus)
  - 覆盖不足导致无法发现关键模块的回归问题
- **建议修复**: 
  1. 扩展 mutation 测试到 five-plane-* 模块
  2. 优先覆盖巨型文件（budget-allocator.ts, runtime-state-machine.ts, harness/index.ts）
  3. 在 CI 中添加 mutation 测试覆盖率门槛

#### 173. [测试] [中严重] [.c8rc.json 和 stryker.config.mjs 配置分离导致覆盖缺口]
- **文件/路径**: `.c8rc.json` 和 `stryker.config.mjs`
- **问题描述**: 
  - c8 (代码覆盖率) 和 stryker (变异测试) 配置分离
  - c8 覆盖 `dist/src/**/*.js`
  - stryker 只覆盖少数核心文件
  - 可能导致某些代码路径既没有覆盖率也没有变异测试
- **建议修复**: 
  1. 统一覆盖策略
  2. 确保关键路径同时被 c8 和 stryker 覆盖
  3. 在 CI 中生成统一的覆盖率报告

#### 174. [CI/CD] [中严重] [mutation 测试只在 main 分支推送时运行]
- **文件/路径**: `.github/workflows/ci.yml`
- **问题描述**: 
  - mutation 测试配置为：```yaml
  mutation-test:
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
  ```
  - 这意味着 PR 不会运行 mutation 测试
  - 可能导致有 bug 的 PR 被合并
- **建议修复**: 
  1. 在 PR 中运行轻量级 mutation 测试
  2. 在 main 分支运行完整 mutation 测试
  3. 添加 mutation 测试结果检查作为 PR 合并条件

#### 175. [部署] [低严重] [deploy/helm 和 deploy/terraform 内容不确定]
- **文件/路径**: `deploy/helm/` 和 `deploy/terraform/`
- **问题描述**: 
  - helm 目录只有 `automatic-agent` 子目录
  - terraform 目录有 `environments/`, `main.tf`, `modules/`
  - 需要确认这些配置是否完整和最新
  - 缺少 Kubernetes 部署配置（CLAUDE.md 中提到但未找到）
- **建议修复**: 
  1. 验证 helm chart 配置完整性
  2. 验证 terraform 配置与生产环境匹配
  3. 补充 Kubernetes 部署配置（如需要）

#### 176. [源代码] [中严重] [src/core/runtime/ 与五层架构职责不清]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - 目录包含：`orchestrator/`, `planner/`, `supervisor/`, `distributed-lock-service.ts`, `process-tracker.ts`, `queue-adapter.ts`
  - 这些模块与 `five-plane-*` 目录中的模块可能功能重复
  - CLAUDE.md 明确警告 `src/core/runtime/` 是兼容性目录
  - 但 index.ts 仍在导出这些模块
- **建议修复**: 
  1. 审计 `core/runtime` 与 `five-plane-*` 的功能重叠
  2. 确定 `core/runtime` 中哪些功能已迁移到五层架构
  3. 在 `core/runtime/index.ts` 中添加 Deprecation 警告
  4. 清理已迁移的代码

#### 177. [配置] [低严重] [UI package.json 与主 package.json 依赖版本可能不一致]
- **文件/路径**: `ui/package.json`
- **问题描述**: 
  - UI 有独立的 package.json 和 node_modules
  - 使用 Turborepo workspaces
  - 但 React 版本为 `^19.1.0`，主项目也使用 `^19.1.0`（一致）
  - 需要确认所有共享依赖版本一致
- **建议修复**: 
  1. 在主 package.json 中定义共享依赖版本
  2. 在 UI package.json 中引用 workspace 依赖
  3. 添加依赖一致性检查到 CI

#### 178. [安全] [中严重] [安全配置分散在多个文件中]
- **文件/路径**: `config/security/` 目录
- **问题描述**: 
  - `default.json` - 默认安全配置
  - `threat-matrix.json` - STRIDE 威胁矩阵
  - `dev.json`, `test.json`, `staging.json`, `pre-prod.json`, `prod.json` - 环境特定配置
  - 没有统一的安全配置加载器
  - 配置验证逻辑不明确
- **建议修复**: 
  1. 创建 `src/platform/shared/security/config-validator.ts` 统一验证
  2. 在应用启动时验证安全配置完整性
  3. 文档化每个安全配置项的用途

#### 179. [源代码] [低严重] [contracts 模块过大 (2169行)]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169 行)
- **问题描述**: 
  - 单个文件 2169 行，违反单一职责原则
  - 包含 `HarnessDecision`, `HarnessRun`, `PlanGraphBundle` 等大量类型定义
  - 难以维护和测试
- **建议修复**: 
  1. 将 `executable-contracts/index.ts` 拆分为多个子模块
  2. 将类型定义移到 `contracts/types/` 目录
  3. 创建 barrel 文件重新导出

#### 180. [源代码] [中严重] [five-plane-execution/harness/ 目录结构复杂]
- **文件/路径**: `src/platform/five-plane-execution/harness/` (但实际路径是 five-plane-orchestration/harness/)
- **问题描述**: 
  - `harness/index.ts` 2317 行，是最大的源文件之一
  - 目录包含 34 个子目录和文件
  - 功能复杂，职责不清
- **建议修复**: 
  1. 将 harness/index.ts 拆分为多个模块
  2. 按功能组织子目录：
     - `harness/runtime/` - 运行时
     - `harness/evaluator/` - 评估器
     - `harness/guardrails/` - guardrails
     - `harness/toolbelt/` - 工具带
  3. 保持 index.ts 作为 barrel 文件

#### 181. [文档] [低严重] [docs_zh/operations/ 与 deploy/runbooks 内容重复或不一致]
- **文件/路径**: `docs_zh/operations/` 和 `deploy/runbooks/`
- **问题描述**: 
  - `docs_zh/operations/` 有大量运维文档
  - `deploy/runbooks/` 只有 1 个文件
  - 可能存在文档重复或不一致
- **建议修复**: 
  1. 审计两个目录的内容
  2. 统一 runbook 存储位置
  3. 确保文档与实际 runbook 一致

#### 182. [源代码] [中严重] [process.env 访问次数之前报告为5176次，但实际需要验证]
- **文件/路径**: 全局
- **问题描述**: 
  - 大量 `process.env` 访问意味着配置分散
  - 环境变量可能未经过滤暴露敏感信息
  - 缺少统一的配置管理
- **建议修复**: 
  1. 创建 `src/config/index.ts` 集中管理所有配置
  2. 使用 `zod` 或类似工具验证环境变量
  3. 添加配置类型检查

#### 183. [测试] [中严重] [UI Playwright 配置存在但未确认是否在 CI 中运行]
- **文件/路径**: `ui/playwright.config.ts`
- **问题描述**: 
  - UI 有 Playwright 配置
  - `.github/workflows/ui-quality.yml` 存在
  - 但需要确认 Playwright E2E 测试是否在 CI 中运行
  - 测试命令：`npm run test:playwright`
- **建议修复**: 
  1. 确认 ui-quality.yml 工作流是否启用
  2. 确保 Playwright 测试在 CI 中运行
  3. 添加 UI 覆盖率门槛

#### 184. [源代码] [低严重] [源码统计：374,602 行 TypeScript，1795 个文件]
- **文件/路径**: 全局
- **问题描述**: 
  - 项目规模庞大（374K 行，1795 文件）
  - 需要确保代码组织合理
  - 模块边界清晰度至关重要
- **建议修复**: 
  1. 定期审查模块边界
  2. 确保新增代码遵循架构规范
  3. 添加架构合规性检查到 CI

#### 185. [配置] [中严重] [package.json scripts 过于复杂且重复]
- **文件/路径**: `package.json` scripts 部分
- **问题描述**: 
  - 超过 140 个 npm scripts
  - 大量 stable:* 脚本重复（每个都调用 npm run build）
  - `test:raw`, `test`, `test:unit` 职责不清
  - 脚本依赖复杂，难以维护
- **建议修复**: 
  1. 简化 scripts 结构
  2. 使用参数化脚本替代重复脚本
  3. 文档化每个脚本的用途
  4. 使用 `npm-run-all` 或类似工具简化

#### 186. [安全] [低严重] [.env.example 中 AA_API_JWT_SECRET 为空但已有说明]
- **文件/路径**: `.env.example`
- **问题描述**: 
  - `AA_API_JWT_SECRET=` 为空
  - 有注释说明生产需要配置
  - 但缺少 secret 生成指引
  - 缺少 secret 管理策略文档
- **建议修复**: 
  1. 在注释中添加 secret 生成命令
  2. 创建 secret 管理策略文档
  3. 考虑添加 secret 验证在应用启动时

#### 187. [部署] [低严重] [.dockerignore 存在但需要验证完整性]
- **文件/路径**: `.dockerignore`
- **问题描述**: 
  - 只有 90 字节，可能不完整
  - 需要确认是否排除了所有不必要的文件
- **建议修复**: 
  1. 验证 .dockerignore 包含所有临时文件
  2. 确保包含 `node_modules`, `dist`, `.git` 等
  3. 测试 Docker 构建

#### 188. [文档] [低严重] [docs_zh/ 和 docs_en/ 并存但同步机制缺失]
- **文件/路径**: `docs_zh/` 和 `docs_en/`
- **问题描述**: 
  - 两个文档目录并列存在
  - 没有同步机制
  - 可能导致文档不一致
- **建议修复**: 
  1. 创建文档同步检查的 CI 流程
  2. 明确文档翻译流程和责任人
  3. 考虑使用自动化翻译工具

#### 189. [源代码] [中严重] [巨型文件顶部直接导入导致耦合度高]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317 行)
- **问题描述**: 
  - 文件顶部有大量 import 语句
  - 从多个不同模块导入类型和函数
  - 导致紧密耦合
- **建议修复**: 
  1. 将这些导入组织到子模块
  2. 使用依赖注入降低耦合
  3. 创建清晰的接口边界

#### 190. [测试] [高严重] [.audit/quality.md 报告 14 个测试文件失败，但 git status 显示无更改]
- **文件/路径**: `.audit/quality.md`
- **问题描述**: 
  - 14 个测试文件失败
  - git status 显示没有未提交的更改
  - 说明这些失败可能已被接受但未修复
  - 或者测试本身存在持续性问题
- **建议修复**: 
  1. 立即调查 14 个失败的测试
  2. 将失败的测试标记为已知问题或修复
  3. 在 CI 中添加测试质量门槛
  4. 创建 issue tracker 跟踪每个失败的测试

---

### 总结

本次自动Review（第六轮 - 深入检查补充）发现了25个新问题，其中：

**高优先级 (需要立即处理)**:
1. budget-allocator.ts (34KB), runtime-state-machine.ts (26KB), harness/index.ts (2317行) 巨型文件需要拆分
2. config/security/prod.json 使用 strict 模式，其他环境使用 supervised
3. 14 个测试文件失败但未修复
4. .gitignore 缺少 `:memory:*`, `dist_*`, `.audit/` 等模式
5. stryker mutation 测试覆盖严重不足（只覆盖 core/api）

**中优先级**:
1. deploy/runbooks 内容过少（只有1个文件）
2. deploy/chaos 场景不完整（只有4个场景）
3. 12个文件直接使用 console.* 而非结构化日志
4. .c8rc.json 和 stryker.config.mjs 配置分离
5. mutation 测试只在 main 分支运行
6. src/core/runtime/ 与五层架构职责不清
7. 安全配置分散在多个文件中
8. contracts 模块过大 (2169行)
9. UI Playwright 测试未确认是否在 CI 中运行
10. process.env 访问缺少统一管理
11. package.json scripts 过于复杂

**低优先级**:
1. deploy/helm 和 deploy/terraform 内容需要验证
2. UI package.json 依赖版本一致性
3. docs_zh/ 和 docs_en/ 同步问题
4. .dockerignore 需要验证完整性
5. .env.example secret 管理指引不足

---

### 累计问题统计 (更新)

| 轮次 | 发现问题数 | 高优先级 | 中优先级 | 低优先级 |
|------|-----------|---------|---------|---------|
| 第一轮 | 35 | 5 | 17 | 13 |
| 第二轮 | 31 | 6 | 18 | 7 |
| 第三轮 | 44 | 5 | 23 | 16 |
| 第四轮 | 30 | 5 | 18 | 7 |
| 第五轮 | 25 | 3 | 16 | 6 |
| **第六轮** | **25** | **4** | **11** | **10** |
| **总计** | **190** | **28** | **103** | **59** |

### 高优先级问题汇总 (更新)

1. 符号链接导致构建不一致 (src/platform/)
2. 巨型源文件需要拆分 (harness/index.ts 2317行, budget-allocator.ts 34KB, etc.)
3. 测试被 exclude 数量巨大 (80+)
4. 测试失败率高 (14 个文件失败)
5. config/security 环境配置差异 (prod.json 为 strict，其他为 supervised)
6. stryker mutation 测试覆盖严重不足
7. .audit/ 目录未被 gitignore
8. .gitignore 缺少多个临时文件模式
9. 多层模块间循环依赖风险
10. 类型定义不一致 - 错误类型混乱
11. 接口一致性 - 导入路径混乱
12. core/runtime 与五层架构职责不清

### 建议优先处理 (更新)

1. **立即修复** 14 个失败的测试文件
2. **立即修复** config/security/prod.json strict 模式与其他环境不一致
3. **立即清理** .gitignore 添加 `.audit/`, `:memory:*`, `dist_*` 等
4. **规划拆分** 所有超过 1000 行的源文件
5. **扩展 stryker** mutation 测试覆盖到 five-plane-* 模块
6. **统一配置管理** 创建 src/config/index.ts 集中管理所有配置
7. **补充 deploy/runbooks** 内容
8. **补充 deploy/chaos** 场景
9. **清理 src/core/runtime** 与五层架构重复的代码

*Review generated: 2026/05/13*

---

## [日期] 2026-05-13 自动Review报告

### 发现的问题

#### 1. [src/架构] [严重-高] [符号链接导致构建和路径解析问题]
- **文件/路径**: `src/platform/control-plane -> five-plane-control-plane`, `src/platform/execution -> five-plane-execution` 等
- **问题描述**: 项目使用符号链接指向 `five-plane-*` 目录，这导致：
  1. IDE 和工具路径解析不一致
  2. Git 历史追踪困难
  3. 构建配置复杂化
  4. 相对导入路径可能失效
- **建议修复**: 将所有符号链接替换为实际的目录结构，或使用包导出机制

#### 2. [src/代码质量] [严重-高] [巨型源文件未拆分 - 违反单一职责原则]
- **文件/路径**: 
  - `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
  - `src/platform/five-plane-execution/budget-allocator.ts` (34KB+)
  - `src/platform/five-plane-execution/execution-engine/multi-step-orchestration.ts`
  - `src/domains/domain-baseline-seeds.ts` (35KB+)
- **问题描述**: 多个源文件超过 1000 行，违反单一职责原则，导致：
  1. 代码难以维护和测试
  2. 增加 merge conflicts 风险
  3. 难以进行增量编译
- **建议修复**: 将大文件拆分为多个模块，每个模块不超过 500 行

#### 3. [src/错误处理] [严重-中] [错误处理不一致 - 直接抛出 Error]
- **文件/路径**: 
  - `src/interaction-governance-runtime-orchestrator.ts:58` - `throw new Error(...)` 而非 `ValidationError`
  - `src/domains-runtime-orchestrator.ts:55` - 同上
  - `src/scale-ops-runtime-orchestrator.ts:58` - 同上
  - `src/platform-architecture-bootstrap.ts:188` - 同上
- **问题描述**: 多个 orchestrator 文件使用裸 `throw new Error()` 而非类型化的 `ValidationError`，导致错误类型系统不一致，难以进行精确的错误处理和恢复
- **建议修复**: 统一使用 `ValidationError` 或其他适当的具体错误类型

#### 4. [src/安全] [严重-高] [敏感信息暴露风险 - .env.example 未清理]
- **文件/路径**: `.env.example`
- **问题描述**: `.env.example` 包含敏感配置示例：
  - `ANTHROPIC_API_KEY=`
  - `OPENAI_API_KEY=`
  - `MINIMAX_API_KEY=`
  - `AA_API_JWT_SECRET=`
  虽然这些是空值，但模板可能误导开发者提交真实凭证
- **建议修复**: 添加明确的注释说明这些变量必须从环境变量获取，禁止提交到版本控制

#### 5. [src/安全] [严重-中] [路径遍历防护实现问题]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts:108-133`
- **问题描述**: `safePath` 函数使用 `process.cwd()` 作为基础目录，但在不同运行环境下 `cwd` 可能变化。在 Docker 容器或 CI 环境中可能产生意外行为
- **建议修复**: 使用明确的基准路径（如应用安装目录）而非 `process.cwd()`

#### 6. [src/并发] [严重-中] [Redis 客户端在测试模式下使用内存实现]
- **文件/路径**: `src/platform/five-plane-execution/queue/redis-queue-adapter.ts:255-256`
- **问题描述**: 当 `AA_RUNNING_TESTS === "1"` 时使用 `InMemoryRedisLike`，这导致测试行为与生产环境不一致，可能遗漏真实 Redis 的问题
```typescript
if (process.env.AA_RUNNING_TESTS === "1") {
  this.redis = new InMemoryRedisLike();
}
```
- **建议修复**: 添加警告日志，确保测试覆盖真实 Redis 场景

#### 7. [src/日志] [严重-低] [生产代码使用 console.log 而非结构化日志]
- **文件/路径**: 
  - `src/index.ts:270, 277, 429, 448`
  - `src/sdk/cli/inspect.ts:150`
  - `src/sdk/cli/stable-sequence.ts:30`
  - `src/sdk/cli/dlq-manager.ts:90, 107, 123`
- **问题描述**: 生产代码使用 `console.log/error` 而非项目的 `StructuredLogger`，导致：
  1. 日志无法通过配置过滤
  2. 无法与分布式追踪集成
  3. 日志格式不统一
- **建议修复**: 将所有 `console.log/error` 替换为 `StructuredLogger`

#### 8. [src/配置] [严重-中] [配置管理分散 - 存在多个配置源]
- **文件/路径**: 
  - `config/` (主配置)
  - `.env.example` (环境变量模板)
  - `config/environments/` (dev/staging/prod等)
  - `config/security/` (安全配置)
  - `config/runtime/` (运行时配置)
- **问题描述**: 配置分散在 5+ 个目录，没有统一的配置管理层：
  1. 环境变量配置分散
  2. 缺少配置验证
  3. 配置变更追踪困难
- **建议修复**: 创建 `src/config/index.ts` 集中管理所有配置，统一使用 Zod schema 验证

#### 9. [src/类型安全] [严重-低] [JSON.parse 缺乏错误处理]
- **文件/路径**: 
  - `src/domains/registry/plugin-runtime-child.ts:136`
  - `src/domains/registry/plugin-runtime-host.ts:415`
  - `src/domains/registry/plugin-runtime-child.ts:159`
- **问题描述**: 多处使用 `JSON.parse(line)` 而无 try-catch，如果收到畸形数据会直接崩溃
- **建议修复**: 封装为 `safeJsonParse` 函数，对错误输入返回 null 而非抛出异常

#### 10. [src/日志] [严重-低] [StructuredLogger 使用 stack trace 解析调用者路径]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts:606-618`
- **问题描述**: `inferCallerSourcePath()` 使用 `new Error().stack` 解析文件路径，性能差且在优化后代码中可能失效
- **建议修复**: 通过参数显式传入源文件路径，而非运行时推断

#### 11. [src/内存] [严重-低] [StructuredLogger 内存缓冲可能耗尽]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts:248`
- **问题描述**: `this.buffer = new Array(this.retentionLimit)` 预分配大量内存，如果 retentionLimit 设置过大会导致内存浪费
- **建议修复**: 使用懒加载或动态调整机制

#### 12. [tests/测试质量] [严重-高] [测试被大量 exclude]
- **文件/路径**: `tsconfig.json` exclude 列表
- **问题描述**: 80+ 测试文件被排除，表明：
  1. 存在测试不稳定性问题
  2. 循环依赖问题未解决
  3. 测试基础设施可能损坏
- **建议修复**: 调查并修复被排除的测试，恢复测试覆盖率

#### 13. [tests/测试覆盖] [严重-中] [Mutation testing 覆盖严重不足]
- **文件/路径**: `stryker.config.mjs`
- **问题描述**: Stryker mutation 测试未覆盖 `five-plane-*` 模块，而这些是核心业务逻辑
- **建议修复**: 扩展 mutation 测试到 `five-plane-control-plane`, `five-plane-execution` 等模块

#### 14. [tests/测试质量] [严重-中] [测试存在 TODO 注释表明未完成]
- **文件/路径**: 
  - `tests/unit/platform/contracts/anomaly-event-classification.test.ts:61,80,85,114`
  - `tests/integration/platform/five-plane-interface/api/http-api-server.test.ts:17`
  - `tests/integration/platform/security/enterprise-capability-boundary.test.ts:72`
  - `tests/integration/platform/security/sandbox-patch-dsl-service.test.ts:195,240`
- **问题描述**: 多个测试文件包含 `// TODO: fix` 注释，表明测试尚未完成或存在已知问题
- **建议修复**: 完成这些测试或将其标记为 `skip` 而非留在待修复状态

#### 15. [ui/前端] [严重-中] [UI 项目缺少安全配置]
- **文件/路径**: `ui/package.json`, `ui/apps/web/vite.config.ts`
- **问题描述**: 
  1. 未检查 `dangerouslySetInnerHTML` 使用
  2. 缺少 CSP (Content Security Policy) 配置
  3. 未检查 `eval()` 或 `new Function()` 使用
- **建议修复**: 添加安全检查脚本，配置 CSP header

#### 16. [ui/前端] [严重-低] [UI 项目缺少国际化 (i18n) 配置]
- **文件/路径**: `ui/package.json`
- **问题描述**: 依赖 `intl-messageformat` 但未看到 i18n 框架集成，UI 可能无法支持多语言
- **建议修复**: 评估并实施适当的 i18n 框架

#### 17. [ui/响应式] [严重-低] [UI 组件响应式设计未验证]
- **文件/路径**: `ui/packages/features/*/`
- **问题描述**: 未发现针对移动端或不同屏幕尺寸的测试
- **建议修复**: 添加响应式设计测试

#### 18. [config/配置问题] [严重-中] [安全配置环境不一致]
- **文件/路径**: `config/security/prod.json` vs `config/security/dev.json`
- **问题描述**: 
  - `prod.json` 使用 `strict` 模式
  - 其他环境使用 `supervised` 模式
  这导致不同环境的默认安全级别不一致
- **建议修复**: 统一所有环境的安全配置，或在文档中明确说明差异原因

#### 19. [config/配置问题] [严重-低] [配置值文件中存在硬编码]
- **文件/路径**: `config/runtime/default.json`
- **问题描述**: 
```json
"circuitBreaker": {
  "enabled": true,
  "threshold": 5
}
```
阈值硬编码，应根据环境动态配置
- **建议修复**: 将阈值提取为环境变量

#### 20. [bash/脚本] [严重-中] [部署脚本安全性问题]
- **文件/路径**: `deploy/scripts/deploy.sh:94-98`
- **问题描述**: 
```bash
read -p "Are you sure you want to proceed? (type 'yes' to confirm): " CONFIRM
if [[ "${CONFIRM}" != "yes" ]]; then
```
生产部署使用交互式确认，在 CI/CD 环境中无法工作
- **建议修复**: 添加 `CI=true` 环境变量跳过交互确认

#### 21. [bash/脚本] [严重-中] [rollback.sh 脚本不完整]
- **文件/路径**: `deploy/scripts/rollback.sh`
- **问题描述**: 
  1. 回滚后未验证服务健康状态
  2. 未清理旧的 canary 部署
  3. 未发送回滚通知
- **建议修复**: 增强回滚脚本，添加健康检查和通知机制

#### 22. [bash/脚本] [严重-低] [dr-drill.sh DR演练脚本路径问题]
- **文件/路径**: `deploy/scripts/dr-drill.sh`
- **问题描述**: 使用 `${DR_CONFIG_DIR:-config/dr}` 但可能不存在该目录
- **建议修复**: 添加目录存在性检查

#### 23. [package.json] [严重-中] [npm scripts 重复构建]
- **文件/路径**: `package.json`
- **问题描述**: 许多 CLI 命令脚本都包含 `npm run build && node --enable-source-maps`，导致：
  1. 每次运行都重新构建，即使未修改
  2. 测试运行时间过长
- **建议修复**: 分离构建和运行步骤，或使用 `AA_PRESERVE_DIST=1` 跳过未修改的构建

#### 24. [package.json] [严重-低] [scripts 中缺少版本检查]
- **文件/路径**: `package.json`
- **问题描述**: 部署相关脚本未验证 Node.js 版本要求
- **建议修复**: 添加 `engines` 字段并验证

#### 25. [安全/敏感信息] [严重-高] [.audit 目录未被 gitignore]
- **文件/路径**: `.gitignore`
- **问题描述**: `.audit/` 目录可能包含敏感的安全审计信息，但未被 gitignore
- **建议修复**: 添加 `.audit/` 到 .gitignore

#### 26. [安全/敏感信息] [严重-中] [临时文件未清理]
- **文件/路径**: `.gitignore`
- **问题描述**: 缺少以下临时文件模式：
  - `:memory:*` (内存临时文件)
  - `dist_*` (临时构建目录)
  - `.tmp/` (临时文件)
- **建议修复**: 添加这些模式到 .gitignore

#### 27. [安全/路径遍历] [严重-中] [多处存在路径遍历风险]
- **文件/路径**: 
  - `src/platform/five-plane-execution/tool-executor/` 中文件操作
  - `src/domains/registry/plugin-runtime-host.ts`
- **问题描述**: 文件路径操作可能存在 `..` 遍历风险
- **建议修复**: 使用 `safePath` 函数或 `path.resolve` 确保路径安全

#### 28. [文档/一致性] [严重-低] [文档与代码不一致]
- **文件/路径**: `docs_zh/architecture/00-platform-architecture.md`
- **问题描述**: 文档描述的架构与实际代码结构存在差异（如符号链接）
- **建议修复**: 更新文档或修复代码结构

#### 29. [文档/完整性] [严重-低] [deploy/runbooks 目录为空]
- **文件/路径**: `deploy/runbooks/`
- **问题描述**: runbooks 目录存在但内容为空或极少
- **建议修复**: 添加常用的运维操作手册

#### 30. [文档/完整性] [严重-低] [deploy/chaos 场景未完成]
- **文件/路径**: `deploy/chaos/`
- **问题描述**: 混沌工程场景配置未完成
- **建议修复**: 添加核心场景（服务中断、网络分区、资源耗尽等）

#### 31. [src/并发] [严重-中] [Distributed Lock 实现可能存在问题]
- **文件/路径**: `src/platform/five-plane-execution/distributed-lock/`
- **问题描述**: 
  1. 锁的 TTL 管理可能存在问题
  2. 未看到锁续期机制
  3. 锁释放可能失败导致死锁
- **建议修复**: 添加锁续期机制和改进错误处理

#### 32. [src/性能] [严重-低] [N+1 查询风险]
- **文件/路径**: `src/platform/five-plane-state-evidence/` 中的 repository 实现
- **问题描述**: 在批量操作中可能存在 N+1 查询问题
- **建议修复**: 使用批量查询或 JOIN

#### 33. [src/内存泄漏] [严重-低] [StructuredLogger 全局文件句柄未关闭]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 全局文件 sink 在应用关闭时可能未正确关闭
- **建议修复**: 添加 `process.on('exit')` 清理逻辑

#### 34. [src/可访问性] [严重-低] [UI 组件缺少 aria-label]
- **文件/路径**: `ui/packages/features/` 中的交互组件
- **问题描述**: 按钮和表单元素可能缺少 aria-label，影响屏幕阅读器支持
- **建议修复**: 添加 aria-label 到所有交互元素

#### 35. [src/错误恢复] [严重-中] [错误恢复机制不完整]
- **文件/路径**: `src/platform/five-plane-execution/recovery/`
- **问题描述**: 恢复逻辑可能未覆盖所有故障场景
- **建议修复**: 补充测试覆盖所有故障场景

#### 36. [src/可观测性] [严重-低] [trace context 传播不完整]
- **文件/路径**: `src/platform/shared/observability/trace-context.ts`
- **问题描述**: 在某些异步路径上 trace context 可能丢失
- **建议修复**: 确保所有异步操作正确传播 trace context

#### 37. [配置/数据库] [严重-中] [SQLite 在生产环境使用存在风险]
- **文件/路径**: `.env.example`, `config/environments/dev.json`
- **问题描述**: SQLite 用于生产环境存在：
  1. 并发写入限制
  2. 无网络访问能力
  3. 备份机制不完善
- **建议修复**: 文档中明确说明 SQLite 仅适用于开发/测试，生产应使用 PostgreSQL

#### 38. [src/依赖] [严重-低] [硬编码的模块路径]
- **文件/路径**: 多处 deep imports
- **问题描述**: 使用 `../../../../src/...` 而非包导出
- **建议修复**: 优先使用 `@automatic-agent/platform` 形式的包导入

#### 39. [测试/集成] [严重-低] [集成测试可能依赖外部服务]
- **文件/路径**: `tests/integration/`
- **问题描述**: 某些集成测试可能依赖真实外部服务（Redis、PostgreSQL）
- **建议修复**: 确保所有集成测试使用 mock 或 testcontainers

#### 40. [src/异常安全] [严重-低] [finally 块中可能发生异常]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts:562-565`
- **问题描述**: `finally` 块中调用 `performRotation` 可能抛出异常，导致原始异常被掩盖
- **建议修复**: 将 finally 中的操作改为静默失败

---

### 问题汇总统计

| 类别 | 高优先级 | 中优先级 | 低优先级 |
|------|---------|---------|---------|
| src/架构 | 1 | 2 | 3 |
| src/代码质量 | 1 | 3 | 4 |
| src/安全 | 3 | 2 | 1 |
| src/并发 | 0 | 2 | 1 |
| src/日志 | 0 | 1 | 3 |
| src/配置 | 0 | 2 | 1 |
| src/性能 | 0 | 0 | 2 |
| tests/测试质量 | 1 | 2 | 2 |
| ui/前端 | 0 | 2 | 2 |
| config/配置 | 0 | 1 | 2 |
| bash/脚本 | 0 | 3 | 1 |
| package.json | 0 | 1 | 1 |
| 文档/完整性 | 0 | 0 | 4 |
| **总计** | **6** | **24** | **32** |

### 建议优先处理

1. **立即修复** - `.audit/` 添加到 .gitignore
2. **立即修复** - 统一错误处理使用 ValidationError
3. **立即修复** - 部署脚本添加 CI 模式支持
4. **高优先级** - 测试被 exclude 问题 (80+ 文件)
5. **高优先级** - 巨型文件拆分 (harness/index.ts, budget-allocator.ts 等)
6. **高优先级** - 安全配置环境一致性
7. **中优先级** - console.log 替换为 StructuredLogger
8. **中优先级** - 配置管理集中化
9. **中优先级** - UI 安全检查和 i18n
10. **中优先级** - 添加 deploy/runbooks 和 deploy/chaos 内容

*Review generated: 2026/05/13*

---

## 2026-05-13 自动Review报告（第二轮深度检查）

### 发现的问题

#### 1. [安全] [高严重] [依赖版本安全漏洞 - 7个漏洞]
- **文件/路径**: `package.json`, `package-lock.json`
- **问题描述**: npm audit 发现7个安全漏洞（1个中严重，6个高严重）：
  - `@opentelemetry/exporter-prometheus` <0.217.0 - Prometheus exporter process crash
  - `@protobufjs/utf8` <=1.1.0 - overlong UTF-8 decoding
  - `@xmldom/xmldom` <=0.8.12 - 多个XML注入漏洞 (GHSA-wh4c-j3r5-mjhp, GHSA-2v35-w6hq-6mfw, GHSA-f6ww-3ggp-fr8h, GHSA-x6wf-f3px-wcqx, GHSA-j759-j44w-7fr8)
  - `fast-uri` <=3.1.1 - 路径遍历和主机混淆漏洞
  - `protobufjs` <=7.5.5 - 多个DoS和代码注入漏洞
- **建议修复**: 
  1. 运行 `npm audit fix`（注意：部分修复需要breaking changes）
  2. 考虑升级 `@opentelemetry/sdk-node` 到 0.218.0+
  3. 考虑升级 `xml-crypto` 到 6.1.2+

#### 2. [代码质量] [高严重] [大量空catch块 - 372处]
- **文件/路径**: 整个 `src/` 目录
- **问题描述**: 存在372处空的catch块，仅记录错误但未处理：
  ```typescript
  } catch (error) {
  // or
  } catch (err) {
  ```
  这些空catch块会吞掉错误，导致问题难以排查和调试。
- **建议修复**: 
  1. 在空catch块中添加最小限度的错误处理（至少记录到日志）
  2. 使用 `StructuredLogger` 替换 `console.error`
  3. 考虑创建统一错误处理工具函数

#### 3. [代码质量] [高严重] [巨型源文件 - 文件过大]
- **文件/路径**: 多个巨型文件
- **问题描述**: 某些源文件过大（约2000-3000行），影响可维护性：
  - `src/index.ts` - 包含大量导出和初始化逻辑
  - 其他巨型文件需要进一步扫描确认
- **建议修复**: 
  1. 拆分巨型文件到子模块
  2. 建立文件行数上限规范（如500行）
  3. 使用 barrel pattern 重新组织导出

#### 4. [架构] [高严重] [符号链接造成的不一致 - 6个symlink]
- **文件/路径**: `src/platform/` 目录
- **问题描述**: 存在6个符号链接：
  - `control-plane -> five-plane-control-plane`
  - `execution -> five-plane-execution`
  - `interface -> five-plane-interface`
  - `orchestration -> five-plane-orchestration`
  - `state-evidence -> five-plane-state-evidence`
  这会导致：
  - IDE路径解析问题
  - Git历史追踪困难
  - 构建配置复杂化
- **建议修复**: 
  1. 考虑将 five-plane-* 重命名为正式目录名
  2. 或完全移除符号链接，直接使用 five-plane-* 名称
  3. 更新所有导入路径

#### 5. [测试] [中严重] [tsconfig exclude列表过长 - 100+条目]
- **文件/路径**: `tsconfig.json`
- **问题描述**: exclude列表包含100+个条目，排除了大量测试文件：
  - `tests/unit/platform/five-plane-execution/**/*.test.ts`
  - `tests/integration/platform/five-plane-orchestration/**/*.test.ts`
  - `tests/e2e/multi-region-failover.test.ts`
  - 等
  这表明可能存在：
  - 测试不稳定问题
  - 循环依赖问题
  - 测试基础设施问题
- **建议修复**: 
  1. 调查为什么这些测试被排除
  2. 修复根本问题并启用这些测试
  3. 考虑使用测试标签而非排除

#### 6. [源代码] [中严重] [src目录存在test文件]
- **文件/路径**: `src/` 目录
- **问题描述**: src目录下存在38256个测试文件引用（通过glob模式匹配），说明某些测试可能放在了src目录内而非tests目录
- **建议修复**: 
  1. 确认所有测试文件都在tests/目录下
  2. 如果src内存在测试，迁移到tests/
  3. 在tsconfig中明确区分src和tests

#### 7. [代码质量] [中严重] [大量type assertion使用 - 468处]
- **文件/路径**: 整个 `src/` 目录
- **问题描述**: 存在468处 `as` 类型断言使用：
  ```typescript
  someValue as SomeType
  ```
  过度使用类型断言会绕过TypeScript的类型检查，可能导致运行时错误
- **建议修复**: 
  1. 优先使用类型守卫和类型收缩
  2. 使用Zod等验证库进行运行时类型检查
  3. 考虑添加 eslint 规则限制 `as` 的使用

#### 8. [源代码] [中严重] [index.ts文件过多 - 38256个]
- **文件/路径**: `src/` 目录
- **问题描述**: 存在大量index.ts文件（可能是38256这个数字是错误匹配，需要验证），这些index文件作为barrel导出点，但也造成：
  - 循环依赖风险
  - 导入路径混乱
  - 构建时间增加
- **建议修复**: 
  1. 评估index.ts的必要性
  2. 考虑使用 `export * from` 模式减少index文件
  3. 使用path aliases简化导入

#### 9. [配置] [中严重] [package.json scripts过多 - 100+]
- **文件/路径**: `package.json`
- **问题描述**: package.json包含100+个scripts，其中：
  - 大量重复的 `stable-*` 命令（46个stable-xxx命令）
  - 每个CLI命令都先build再运行
  - 没有统一的帮助信息
- **建议修复**: 
  1. 使用CLI框架（如oclif）统一管理命令
  2. 合并相似命令到子命令
  3. 添加 `npm run` 帮助命令

#### 10. [安全] [中严重] [.audit目录未添加到.gitignore]
- **文件/路径**: `.gitignore`
- **问题描述**: `.audit/` 目录包含敏感的安全审计信息，但未添加到 .gitignore
- **建议修复**: 
  1. 将 `.audit/` 添加到 .gitignore
  2. 检查其他敏感目录是否也需要忽略

#### 11. [UI] [中严重] [UI package.json scripts不完整]
- **文件/路径**: `ui/package.json`
- **问题描述**: 
  - `build` 脚本调用 `build:app` 但 `build:app` 调用 `npm run typecheck`
  - 循环依赖风险
  - 部分scripts缺少错误处理
- **建议修复**: 
  1. 修复scripts依赖关系
  2. 添加错误处理
  3. 确保CI能够正确运行所有测试

#### 12. [源代码] [中严重] [领域模块耦合度不清晰]
- **文件/路径**: `src/domains/`
- **问题描述**: 
  - 34个业务领域模块
  - 领域间依赖关系不清晰
  - 没有领域依赖图或约束规则
- **建议修复**: 
  1. 创建领域依赖图
  2. 添加领域依赖约束检查
  3. 考虑使用领域事件进行解耦

#### 13. [源代码] [中严重] [配置分散在多处]
- **文件/路径**: 项目根目录和 `config/` 目录
- **问题描述**: 
  - 根目录: `.c8rc.json`, `stryker.config.mjs`, `tsconfig.json`, `tsconfig.build.json`, `tsconfig.temp.json`
  - config/: 多个子目录
  - `.env.example`
  配置分散导致：
  - 配置管理困难
  - 可能出现不一致
- **建议修复**: 
  1. 考虑使用 `config/` 统一管理
  2. 使用配置验证（如Zod）
  3. 文档化配置项

#### 14. [测试] [低严重] [测试文件数量与源文件比例异常]
- **文件/路径**: `tests/` 目录
- **问题描述**: 
  - 源文件: 1795个（实际），但文档说6113个
  - 测试文件: 4662个
  - 比例约 2.6:1，可能存在：
    - 过度测试
    - 测试文件重复
    - 测试辅助代码过多（78个helpers）
- **建议修复**: 
  1. 评估测试辅助代码的必要性
  2. 考虑是否有过多mock对象
  3. 优化测试结构

#### 15. [脚本] [低严重] [backup-sqlite.sh和restore-sqlite.sh缺少错误处理]
- **文件/路径**: `scripts/backup-sqlite.sh`, `scripts/restore-sqlite.sh`
- **问题描述**: 
  - 虽然有 `set -euo pipefail`，但某些操作仍可能失败
  - 备份锁定机制使用PID文件，但存在TOCTOU竞态条件
  - 没有加密备份选项
- **建议修复**: 
  1. 添加备份加密选项
  2. 使用数据库事务确保一致性
  3. 添加备份验证的更详细检查

#### 16. [源代码] [低严重] [TODO/FIXME注释过多 - 12个文件]
- **文件/路径**: `src/` 目录
- **问题描述**: 12个源文件包含TODO/FIXME/HACK/XXX注释，表明存在技术债务
- **建议修复**: 
  1. 创建Issue跟踪这些TODO
  2. 定期清理过时的TODO
  3. 使用统一格式便于搜索

#### 17. [源代码] [低严重] [console.log语句过多]
- **文件/路径**: `src/` 目录
- **问题描述**: 存在大量console.log/debug/info/warn/error语句（21处），应该使用StructuredLogger
- **建议修复**: 
  1. 替换为StructuredLogger
  2. 添加日志级别控制
  3. 确保敏感信息不被记录

#### 18. [文档] [低严重] [docs_zh/reviews/文档结构问题]
- **文件/路径**: `docs_zh/reviews/`
- **问题描述**: 
  - reviews目录包含架构设计review
  - 缺少其他类型的review模板
  - 没有review维护指南
- **建议修复**: 
  1. 添加Review维护指南
  2. 创建不同类型的review模板
  3. 定期更新review文档

#### 19. [部署] [低严重] [deploy目录内容不完整]
- **文件/路径**: `deploy/` 目录
- **问题描述**: 
  - 存在terraform、helm、prometheus、grafana配置
  - 但chaos和runbooks目录接近空
  - 缺少Kubernetes完整配置
- **建议修复**: 
  1. 完成chaos测试场景
  2. 添加完整的Kubernetes部署配置
  3. 增加runbook覆盖率

#### 20. [安全] [低严重] [环境变量使用模式不统一]
- **文件/路径**: `src/` 目录
- **问题描述**: 
  - 部分使用 `process.env.XXX`
  - 部分使用 `options?? process.env.XXX ?? default`
  - 没有统一的环境变量读取模式
  - 没有环境变量验证
- **建议修复**: 
  1. 创建统一的环境变量读取函数
  2. 使用Zod验证环境变量
  3. 文档化所有环境变量

#### 21. [测试] [低严重] [golden测试文件缺少文档]
- **文件/路径**: `tests/golden/`
- **问题描述**: 
  - 48个golden测试文件
  - 没有文档说明golden测试的维护流程
  - 没有说明何时需要更新golden文件
- **建议修复**: 
  1. 添加golden测试维护指南
  2. 创建更新golden文件的脚本
  3. 在CI中验证golden文件更新

#### 22. [源代码] [低严重] [src/core/runtime目录警告未被遵循]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: CLAUDE.md明确指出这是兼容性目录，不应添加新逻辑，但项目仍在使用
- **建议修复**: 
  1. 制定迁移计划
  2. 标记所有使用处
  3. 逐步迁移到正确位置

#### 23. [UI] [低严重] [UI包结构复杂度高]
- **文件/路径**: `ui/packages/`, `ui/apps/`
- **问题描述**: 
  - packages/shared/ 包含31个子功能
  - 多个app变体（web, mobile, electron-win, tauri-*）
  - packages/features/ 包含多个功能包
- **建议修复**: 
  1. 评估包的数量是否必要
  2. 考虑合并相关包
  3. 添加包依赖关系文档

#### 24. [安全] [低严重] [备份脚本缺少加密和远程复制]
- **文件/路径**: `scripts/backup-sqlite.sh`
- **问题描述**: 
  - 备份只保存在本地
  - 没有加密选项
  - 没有远程复制或冗余备份
- **建议修复**: 
  1. 添加备份加密选项
  2. 支持远程备份目标
  3. 添加备份验证和完整性检查

#### 25. [配置] [低严重] [临时构建目录未清理]
- **文件/路径**: 根目录
- **问题描述**: 存在多个临时构建目录：
  - `dist/`
  - `dist_temp/`
  - `dist_test/`
  - `dist_test_compiled/`
  - `dist_1964/`
  - `dist_issue2014/`
  - `dist_lineage_test/`
- **建议修复**: 
  1. 清理所有临时目录
  2. 确保.gitignore排除所有dist_*目录
  3. 使用单一干净的构建输出目录

#### 26. [测试] [低严重] [性能测试目录重复]
- **文件/路径**: `tests/performance/`, `tests/performance.bak/`
- **问题描述**: 存在 `performance.bak` 备份目录，表明之前有重构或备份
- **建议修复**: 
  1. 删除备份目录
  2. 确认是否有必要保留备份
  3. 文档化性能测试策略

#### 27. [源代码] [低严重] [plugins/adapters目录耦合]
- **文件/路径**: `src/plugins/adapters/`
- **问题描述**: 
  - crm-adapter, github-adapter, livestream-adapter等
  - 第三方API耦合可能导致维护问题
  - 缺少适配器接口抽象
- **建议修复**: 
  1. 创建统一的适配器接口
  2. 添加适配器mock支持
  3. 考虑使用装饰器模式

#### 28. [文档] [低严重] [Python脚本维护性问题]
- **文件/路径**: `translate_docs.py`
- **问题描述**: 项目主要是TypeScript/Node.js，但存在Python脚本需要额外维护Python环境和依赖
- **建议修复**: 
  1. 评估是否仍在使用
  2. 如果需要，考虑使用TypeScript重写
  3. 如果不用，删除

#### 29. [源代码] [低严重] [领域模块重复导出问题]
- **文件/路径**: `src/platform/contracts/` 和 `src/contracts/`
- **问题描述**: 可能存在contracts相关代码重复定义
- **建议修复**: 
  1. 确认contracts的单一真实来源
  2. 确保没有类型定义重复
  3. 统一contracts导入路径

#### 30. [安全] [中严重] [API路由缺少统一认证中间件]
- **文件/路径**: `src/platform/five-plane-interface/api/`
- **问题描述**: 
  - http-api-server.ts 等文件直接处理请求
  - 缺少统一的认证/授权中间件
  - 某些路由可能暴露敏感功能
- **建议修复**: 
  1. 添加统一的认证中间件
  2. 实现基于角色的访问控制
  3. 添加API速率限制

#### 31. [源代码] [中严重] [状态管理实现分散]
- **文件/路径**: `src/`, `ui/`
- **问题描述**: 
  - 后端使用ioredis和postgres
  - 前端使用zustand和react-query
  - 状态同步机制不清晰
  - 缺少统一的状态管理文档
- **建议修复**: 
  1. 文档化状态管理架构
  2. 实现前后端状态同步策略
  3. 添加状态一致性检查

#### 32. [测试] [低严重] [集成测试覆盖不完整]
- **文件/路径**: `tests/integration/`
- **问题描述**: 
  - 42个子目录的集成测试
  - 但排除了大量integration测试
  - 关键路径可能未被覆盖
- **建议修复**: 
  1. 分析被排除的测试
  2. 增加关键路径集成测试
  3. 添加集成测试覆盖率报告

#### 33. [源代码] [中严重] [src/domains目录过深]
- **文件/路径**: `src/domains/` (60个条目)
- **问题描述**: 
  - 34个业务领域，每个领域可能有多层嵌套
  - 领域间依赖关系复杂
  - 导入路径可能过长
- **建议修复**: 
  1. 评估是否需要扁平化结构
  2. 使用领域沙箱隔离
  3. 添加领域边界检查

#### 34. [UI] [低严重] [UI状态管理使用zustand但缺少类型安全]
- **文件/路径**: `ui/packages/shared/`, `ui/apps/web/`
- **问题描述**: 
  - 使用zustand进行状态管理
  - 但store定义可能缺少类型约束
  - 缺少状态验证
- **建议修复**: 
  1. 添加Zod验证store状态
  2. 实现状态不可变性
  3. 添加状态变更追踪

#### 35. [源代码] [低严重] [SDK CLI工具缺少统一错误处理]
- **文件/路径**: `src/sdk/cli/`
- **问题描述**: 
  - 100+个CLI工具
  - 缺少统一的错误处理
  - 退出码不一致
- **建议修复**: 
  1. 创建统一CLI错误处理框架
  2. 标准化退出码
  3. 添加 `--verbose` 和 `--json` 输出选项

#### 36. [配置] [中严重] [环境变量验证缺失]
- **文件/路径**: 项目全局
- **问题描述**: 
  - .env.example包含大量配置项
  - 但缺少运行时验证
  - 缺失或错误的配置可能导致运行时错误
- **建议修复**: 
  1. 使用Zod进行环境变量验证
  2. 在启动时验证配置
  3. 提供清晰的错误信息

#### 37. [源代码] [低严重] [重复的错误处理模式]
- **文件/路径**: `src/` 目录
- **问题描述**: 
  - 很多地方有重复的错误处理代码
  - 没有统一的错误类层次结构
- **建议修复**: 
  1. 创建统一的错误类层次
  2. 使用错误工厂函数
  3. 添加错误代码规范

#### 38. [安全] [中严重] [WebSocket安全配置不完整]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts`
- **问题描述**: 
  - 使用WebSocket但缺少安全配置检查
  - 没有速率限制
  - 没有认证令牌刷新机制
- **建议修复**: 
  1. 添加WebSocket安全配置
  2. 实现连接验证
  3. 添加心跳检测

#### 39. [源代码] [低严重] [缺乏依赖注入模式]
- **文件/路径**: `src/` 目录
- **问题描述**: 
  - 服务之间直接import
  - 缺少依赖注入容器
  - 单元测试需要大量mock
- **建议修复**: 
  1. 考虑使用依赖注入
  2. 创建服务工厂
  3. 简化测试设置

#### 40. [测试] [中严重] [E2E测试与单元测试比例异常]
- **文件/路径**: `tests/`
- **问题描述**: 
  - 单元测试: 3,584
  - 集成测试: 134
  - E2E测试: 834
  - E2E测试相对较多，但覆盖率不清晰
- **建议修复**: 
  1. 确保E2E测试在CI中运行
  2. 添加E2E测试覆盖率报告
  3. 验证E2E测试的稳定性

#### 41. [文档] [低严重] [架构文档与实现不一致]
- **文件/路径**: `docs_zh/architecture/`
- **问题描述**: 
  - 存在架构文档
  - 但可能与当前实现不同步
  - 没有文档更新流程
- **建议修复**: 
  1. 在PR中检查架构一致性
  2. 添加架构文档验证
  3. 建立文档更新流程

#### 42. [源代码] [中严重] [循环依赖风险]
- **文件/路径**: `src/` 目录
- **问题描述**: 
  - 大量index.ts文件可能导致循环依赖
  - 某些测试文件被排除可能是因为循环依赖
- **建议修复**: 
  1. 运行 `madge --circular` 检查
  2. 重构打破循环依赖
  3. 启用ESLint循环依赖规则

#### 43. [UI] [中严重] [React Query和Zustand状态同步缺失]
- **文件/路径**: `ui/apps/web/`
- **问题描述**: 
  - 使用 @tanstack/react-query 进行服务器状态
  - 使用 zustand 进行客户端状态
  - 但两者之间缺少同步机制
- **建议修复**: 
  1. 添加状态同步层
  2. 实现乐观更新策略
  3. 添加冲突解决机制

#### 44. [源代码] [中严重] [缺乏备份和恢复机制文档]
- **文件/路径**: `scripts/backup-sqlite.sh`, `scripts/restore-sqlite.sh`
- **问题描述**: 
  - 有备份脚本但没有文档
  - 没有RTO/RPO定义
  - 没有测试过恢复流程
- **建议修复**: 
  1. 添加备份恢复文档
  2. 定期测试恢复流程
  3. 定义RTO/RPO并监控

#### 45. [源代码] [低严重] [缺乏API版本管理]
- **文件/路径**: `src/platform/five-plane-interface/api/`
- **问题描述**: 
  - API可能缺少版本控制
  - 没有API废弃策略
  - 客户端兼容性可能受影响
- **建议修复**: 
  1. 添加API版本控制
  2. 制定API废弃策略
  3. 维护API变更日志

#### 46. [测试] [低严重] [invariant测试缺少说明]
- **文件/路径**: `tests/invariants/`
- **问题描述**: 
  - 87个invariant测试
  - 但缺少invariant维护指南
  - 不清楚何时添加新的invariant
- **建议修复**: 
  1. 添加invariant测试指南
  2. 创建invariant检查清单
  3. 文档化常见invariant模式

#### 47. [源代码] [低严重] [日志格式不统一]
- **文件/路径**: `src/` 目录
- **问题描述**: 
  - 部分使用StructuredLogger
  - 部分使用console.log
  - 日志格式不一致
- **建议修复**: 
  1. 统一使用StructuredLogger
  2. 定义日志格式标准
  3. 确保所有日志包含必要上下文

#### 48. [配置] [低严重] [多环境配置管理复杂]
- **文件/路径**: `config/environments/`
- **问题描述**: 
  - 多个环境配置(dev/staging/prod等)
  - 缺少配置差异文档
  - 配置同步机制缺失
- **建议修复**: 
  1. 文档化环境配置差异
  2. 实施配置验证
  3. 考虑使用配置中心

#### 49. [源代码] [中严重] [缺乏监控和告警覆盖]
- **文件/路径**: `src/platform/shared/observability/`
- **问题描述**: 
  - 有SLO告警配置
  - 但关键业务指标可能缺失
  - 没有告警测试
- **建议修复**: 
  1. 添加关键业务指标监控
  2. 实施告警测试
  3. 建立告警回顾机制

#### 50. [源代码] [低严重] [代码重复 - 跨领域相似逻辑]
- **文件/路径**: `src/domains/` (34个领域)
- **问题描述**: 
  - 不同领域可能有相似逻辑
  - 缺少领域共享抽象
  - DRY原则未严格遵守
- **建议修复**: 
  1. 识别跨领域重复代码
  2. 创建领域共享基础模块
  3. 提取通用领域模式

---

### 问题汇总统计（第二轮）

| 类别 | 高优先级 | 中优先级 | 低优先级 |
|------|---------|---------|---------|
| src/架构 | 1 | 3 | 5 |
| src/代码质量 | 2 | 4 | 6 |
| src/安全 | 1 | 3 | 2 |
| src/日志 | 0 | 1 | 2 |
| src/配置 | 0 | 2 | 3 |
| src/性能 | 0 | 0 | 1 |
| tests/测试质量 | 0 | 2 | 4 |
| ui/前端 | 0 | 2 | 3 |
| config/配置 | 0 | 1 | 2 |
| bash/脚本 | 0 | 1 | 2 |
| package.json | 0 | 1 | 1 |
| 文档/完整性 | 0 | 0 | 3 |
| **总计** | **4** | **20** | **34** |

### 第二轮建议优先处理

1. **立即修复** - 依赖版本安全漏洞（7个漏洞）
2. **立即修复** - 空catch块修复（372处）
3. **高优先级** - 符号链接移除或正式化
4. **高优先级** - tsconfig exclude测试问题（100+条目）
5. **高优先级** - 环境变量验证缺失
6. **中优先级** - 巨型源文件拆分
7. **中优先级** - API安全中间件添加
8. **中优先级** - 循环依赖检查和修复

---

## [2026-05-13] 自动Review报告（第三轮 - 深度专项审查）

### 发现的问题

#### 1. [数据库] [高严重] [迁移脚本不完整 - 仅有一个初始化SQL文件]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/migrations/0001_phase1a_init.sql`
- **问题描述**:
  - 数据库迁移目录仅包含一个 `0001_phase1a_init.sql` 文件（261字节）
  - 迁移脚本只包含 PRAGMA 设置注释，没有实际的 schema 创建语句
  - 实际的 schema 定义被注释说明"重复在 `src/platform/five-plane-state-evidence/truth/sql/phase1a-schema.ts`"
  - 没有版本化的迁移路径，难以追踪 schema 演变
  - SQLite 和 PostgreSQL 的 migration runner 存在但迁移计划不完整
- **建议修复**:
  1. 创建完整的版本化迁移脚本序列（0010, 0020, 0030...）
  2. 将 `src/platform/five-plane-state-evidence/truth/sql/phase1a-schema.ts` 的内容转为正式迁移
  3. 为 SQLite 和 PG 分别维护迁移计划（`sqlite-migration-plan.ts`, `pg-migrations-runtime.ts`）
  4. 添加迁移回滚脚本（down migrations）
  5. 建立迁移 checksum 验证机制

#### 2. [数据库] [中严重] [备份脚本缺乏定期演练验证]
- **文件/路径**: `scripts/backup-sqlite.sh`, `scripts/restore-sqlite.sh`
- **问题描述**:
  - 备份脚本存在且实现良好（使用 WAL-safe .backup 命令，有完整性检查）
  - 但备份恢复从未被实际测试过
  - DR 演练脚本 `deploy/scripts/dr-drill.sh` 存在但未在 CI 中集成
  - ADR-031 规定的"每月演练"没有自动化执行
  - 备份保留期 7 天可能不足以满足 90 天合规要求（ADR-031）
- **建议修复**:
  1. 在 CI 中添加定期备份恢复测试（至少每月一次）
  2. 将 `dr-drill.sh` 集成到 CI 流水线
  3. 将 RETENTION_DAYS 从 7 改为 90 以符合 ADR-031
  4. 添加备份元数据记录（备份时间、大小、checksum）
  5. 测试从不同时间点恢复的能力

#### 3. [文档] [高严重] [灾难恢复计划文档不完整]
- **文件/路径**: `docs_zh/adr/031-disaster-recovery-and-high-availability.md`
- **问题描述**:
  - ADR-031 定义了 RTO/RPO 目标（RTO<1小时，RPO<15分钟）但未包含实际恢复流程
  - 没有具体的故障切换步骤文档
  - 多region部署的 DR 策略未详细说明
  - 备份验证流程未文档化
  - 联系人/升级流程缺失
- **建议修复**:
  1. 创建详细的 DR 操作手册（分步骤）
  2. 文档化每个组件的 RTO/RPO 分解
  3. 添加多 region 故障切换流程图
  4. 建立 DR 演练检查清单
  5. 添加紧急联系人列表和升级路径

#### 4. [监控] [中严重] [Prometheus 告警阈值配置不完整]
- **文件/路径**: `deploy/prometheus/rules/automatic-agent.yml`
- **问题描述**:
  - 存在基本的告警规则（错误率、延迟、队列深度、worker 利用率）
  - 但缺少以下关键指标的告警：
    - 数据库连接池耗尽
    - 缓存命中率下降
    - 内存泄漏检测
    - 磁盘 I/O 等待
    - 网络吞吐量异常
  - 告警阈值没有区分环境（dev/staging/prod）
  - 没有告警抑制/静音配置
  - Alertmanager 配置使用占位符 `${SLACK_WEBHOOK_URL}` 和 `${PAGERDUTY_SERVICE_KEY}` 但未提供默认值或说明
- **建议修复**:
  1. 添加数据库连接池监控告警
  2. 添加缓存命中率告警（hit rate < 80% 持续 5m）
  3. 添加内存泄漏检测（内存持续增长超过 2 小时）
  4. 添加磁盘 I/O 监控
  5. 实现告警分级（critical/warning/info）
  6. 添加告警静音计划

#### 5. [安全] [高严重] [API 限流实现不完整 - 缺少 DDoS 防护层]
- **文件/路径**: `src/platform/five-plane-interface/api/middleware/rate-limit.ts`
- **问题描述**:
  - 基础 rate limiting 已实现（token bucket algorithm）
  - 但仅支持内存存储（Map-based），分布式环境下无效
  - 缺少以下 DDoS 防护机制：
    - 基于 IP 的 reputation 系统
    - 异常行为检测（短时间内大量不同端点请求）
    - 地理位置封禁
    - 自动化攻击特征识别
  - Rate limit 配置硬编码为 100 req/min，无差异化策略
  - 缺少针对特定端点的限流配置（如 /v1/auth/token 应更严格）
- **建议修复**:
  1. 实现 Redis-backed 分布式 rate limiter
  2. 添加 IP reputation 系统（封禁恶意 IP）
  3. 实现端点差异化限流策略
  4. 添加基于地理位置的访问控制
  5. 实现请求模式分析以检测自动化攻击
  6. 添加 rate limit 突破后的自动封禁机制

#### 6. [安全] [高严重] [SQL 注入防护存在漏洞风险]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/sqlite/sqlite-database.ts`
- **问题描述**:
  - 代码中有注释"S-01: Validate tableName to prevent SQL injection via identifier interpolation"
  - 但实际验证逻辑需要确认是否完整
  - 动态表名/列名的拼接需要严格的白名单验证
  - Parameterized query 只覆盖了部分场景
  - PostgreSQL 的 `unsafe()` 方法需要严格审查
- **建议修复**:
  1. 确认 tableName/columnName 验证逻辑完整且被调用
  2. 实现完整的动态标识符白名单验证
  3. 审查所有 `.unsafe()` 调用并添加注释说明为何安全
  4. 添加 SQL 注入检测工具到 CI
  5. 建立禁止拼接用户输入到 SQL 的 lint 规则

#### 7. [安全] [中严重] [XSS 防护覆盖不完整]
- **文件/路径**: `src/platform/five-plane-interface/api/middleware/sanitize.ts`
- **问题描述**:
  - 存在 `sanitizeJsonValue` 函数处理 JSON 解析
  - 但仅过滤 `__proto__`, `prototype`, `constructor` 三个 key
  - 缺少对 HTML 特殊字符的转义（`<`, `>`, `&`, `"`, `'`)
  - UI 侧使用 innerHTML 的风险需要检查
  - 没有对 URL 参数的转义
- **建议修复**:
  1. 扩展 sanitizeJsonValue 处理更多攻击向量
  2. 添加 HTML 转义函数用于用户输入的显示
  3. 检查 UI 代码中的 innerHTML 使用并替换为 textContent
  4. 实现 URL 参数的白名单验证
  5. 添加 CSP (Content Security Policy) header

#### 8. [安全] [中严重] [CSRF 防护仅部分实现]
- **文件/路径**: `src/org-governance/sso-scim/oidc/oidc-service.ts`
- **问题描述**:
  - 代码中有 `@param expectedState - State parameter for CSRF protection` 注释
  - OIDC 服务使用 state parameter 但未验证是否完整
  - UI 测试中有 `document.head.innerHTML = '<meta name="aa-csrf-token" content="csrf-token-123" />'`
  - 实际 CSRF token 的生成和验证流程需要确认
- **建议修复**:
  1. 验证所有状态变更操作都有 CSRF token 验证
  2. 实现 Synchronizer Token Pattern（双 token 验证）
  3. 在 Cookie 中设置 SameSite 属性
  4. 添加 CSRF token 刷新机制
  5. 为所有 mutating 操作强制执行 CSRF 验证

#### 9. [安全] [高严重] [会话管理和 Token 刷新机制存在缺陷]
- **文件/路径**: `src/org-governance/sso-scim/oidc/oidc-service.ts`
- **问题描述**:
  - Token rotation 实现存在（§48 Token Rotation）
  - `refreshTokenFamilies` Map 用于追踪 refresh token 族
  - 但以下问题需要关注：
    - 内存中的 token family 存储在服务重启后会丢失
    - refresh token 过期时间未在代码中明确看到
    - 没有强制 refresh token 轮换的机制（reuse detection）
    - access token 有效期未配置（使用默认值可能过长）
  - API key 到 JWT 的交换机制需要审查（`api-auth-service.ts`）
- **建议修复**:
  1. 将 refresh token family 信息持久化到数据库
  2. 实现 access token 的短期过期（建议 15 分钟）
  3. 实现 refresh token reuse detection（重用应导致整个 family 被撤销）
  4. 添加 token 刷新速率限制
  5. 实现 refresh token 的滑动过期机制
  6. 添加强制登出（force logout）功能

#### 10. [监控] [中严重] [日志聚合和查询性能问题]
- **文件/路径**: `src/platform/contracts/types/drift-contracts.ts`
- **问题描述**:
  - 代码中有 `channel: "log", target: "log-aggregator"` 的引用
  - 但没有看到实际的日志聚合基础设施实现
  - StructuredLogger 存在但日志格式可能不一致
  - 缺少日志采样策略（高流量时可能产生大量日志）
  - 没有看到日志查询性能优化（如索引、分区）
  - Fluentd/DataDog 配置在 .env.example 中但未验证是否工作
- **建议修复**:
  1. 验证日志聚合管道（Elasticsearch/Loki）已部署
  2. 实现日志级别采样策略
  3. 为关键日志字段添加索引
  4. 实现日志轮转和保留策略
  5. 添加日志查询的告警（如错误率异常）
  6. 验证 DataDog/FluentD 配置在 CI 中测试

#### 11. [架构] [中严重] [缓存一致性存在潜在问题]
- **文件/路径**: `src/platform/shared/cache/cache-invalidation.ts`
- **问题描述**:
  - 存在 `invalidateByTag` 和 `invalidateNamespace` 方法
  - 但缓存失效逻辑分散在多个地方
  - 使用 `BoundedCache` 的地方（如 OIDC service 的 JWKS cache）TTL 为 1 小时
  - 没有看到缓存项修改时的主动失效机制
  - Race condition 风险：当多个请求同时发现缓存过期时
  - 缓存预热策略缺失
- **建议修复**:
  1. 审查所有缓存使用场景的一致性需求
  2. 实现 "cache-aside" 模式的主动失效
  3. 添加缓存项修改前后的 invalidation 调用
  4. 实现 "staleness" 检测机制
  5. 添加缓存版本控制以支持平滑刷新
  6. 实现请求合并（request coalescing）以防止缓存击穿

#### 12. [源代码] [中严重] [.env.example 配置暴露敏感信息风险]
- **文件/路径**: `.env.example`
- **问题描述**:
  - 文件包含大量配置项的注释说明
  - `AA_API_JWT_SECRET=` 为空但有生成说明
  - 包含 DataDog API key 字段 `AA_LOG_DATADOG_API_KEY=`
  - 包含 Redis password 字段 `AA_MODEL_CALL_RATE_LIMIT_REDIS_PASSWORD=`
  - 暴露了完整的日志和监控配置
  - 注释中包含"DO NOT commit .env files with real credentials"警告但 .env.example 本身应审查
- **建议修复**:
  1. 将所有敏感字段的示例值替换为占位符（如 `YOUR_API_KEY_HERE`）
  2. 移除 DataDog、PagerDuty 等第三方服务的 key 示例
  3. 添加关于如何安全获取这些 secret 的文档链接
  4. 在 git pre-commit hook 中添加对真实 secret 的检测

#### 13. [配置] [中严重] [多环境配置差异未文档化]
- **文件/路径**: `config/environments/`, `config/security/`
- **问题描述**:
  - dev.json, test.json, staging.json, prod.json 存在
  - 但 config/security/dev.json 和 test.json 都是 `{}`
  - 配置差异没有文档说明
  - 安全配置（approvalMode, sandboxMode）仅在 default.json 中定义
  - 环境特定的配置覆盖机制不清晰
- **建议修复**:
  1. 文档化每个环境的配置差异
  2. 创建配置矩阵文档（哪些配置在不同环境不同）
  3. 添加配置验证机制确保生产配置正确
  4. 实现配置热更新机制
  5. 添加配置变更的审计日志

#### 14. [测试] [高严重] [集成测试覆盖不足 - 数据库迁移测试]
- **文件/路径**: `tests/integration/sdk/migrate-sqlite-to-pg-integration-2278-2279.test.ts`
- **问题描述**:
  - 只有一个 PostgreSQL 迁移集成测试文件
  - SQLite 到 PostgreSQL 迁移路径缺少充分的测试
  - 没有测试回滚场景
  - 没有测试部分失败场景（网络中断、磁盘满等）
  - 迁移后数据一致性验证缺失
- **建议修复**:
  1. 添加完整的迁移测试套件（正向、反向、回滚）
  2. 测试各种故障场景下的迁移行为
  3. 添加数据完整性验证测试
  4. 测试大数据库的迁移性能
  5. 添加迁移过程中的监控指标验证

#### 15. [UI] [低严重] [Electron 应用安全配置需要审查]
- **文件/路径**: `ui/packages/electron-win/`, `ui/packages/tauri-linux/`, `ui/packages/tauri-macos/`
- **问题描述**:
  - 使用 Electron 和 Tauri 框架但安全配置未知
  - 没有看到 CSP 配置
  - 没有看到 IPC 通信的安全验证
  - 远程内容加载策略需要审查
  - 自动更新机制需要确认安全
- **建议修复**:
  1. 为 Electron 添加严格 CSP
  2. 实现 IPC 通信的 validation
  3. 禁用不必要的 Node.js 集成
  4. 配置安全的内容源策略
  5. 验证更新包的签名验证

#### 16. [源代码] [中严重] [巨型源文件需要拆分]
- **文件/路径**: 多个大型源文件
- **问题描述**:
  - `src/platform/five-plane-state-evidence/truth/runtime-truth-repository.ts` (17.8KB)
  - `src/platform/five-plane-state-evidence/truth/mission-repository.ts` (14KB)
  - `src/platform/five-plane-state-evidence/truth/runtime-physical-schema.ts` (8.4KB)
  - `src/platform/index.ts` (17.5KB)
  - 巨型文件导致：
    - 代码审查困难
    - 难以实现单一职责
    - 测试覆盖困难
    - IDE 性能问题
- **建议修复**:
  1. 拆分 runtime-truth-repository.ts 为多个专门 repository
  2. 拆分 mission-repository.ts 按业务逻辑
  3. 拆分 platform/index.ts 为多个导出模块
  4. 创建更细粒度的模块边界
  5. 建立文件大小上限规范（如 5KB）

#### 17. [脚本] [中严重] [部署脚本错误处理不完整]
- **文件/路径**: `deploy/scripts/deploy.sh`
- **问题描述**:
  - 脚本存在但使用了 `set -euo pipefail` 错误处理
  - 但缺少：
    - 部署前验证（如数据库连接检查）
    - 部署过程中的健康检查
    - 失败时的自动回滚
    - 部分部署失败的处理
  - 没有幂等性保证
- **建议修复**:
  1. 添加部署前验证步骤
  2. 实现渐进式部署和健康检查
  3. 添加失败自动回滚逻辑
  4. 实现幂等性保证
  5. 添加详细的部署日志

#### 18. [文档] [低严重] [API 文档与实际实现不同步]
- **文件/路径**: `docs_zh/` (多个 API 相关文档)
- **问题描述**:
  - API 路由在 `src/platform/five-plane-interface/api/http-server/` 定义
  - 但 API 文档可能未同步更新
  - 没有看到 API 版本控制策略
  - 缺少 API 变更日志
- **建议修复**:
  1. 实现 OpenAPI/Swagger 文档生成
  2. 将 API 文档验证加入 CI
  3. 建立 API 版本控制策略
  4. 添加 API 废弃警告机制

#### 19. [源代码] [低严重] [日志格式不一致 - 部分使用 console.log]
- **文件/路径**: `src/` 多处
- **问题描述**:
  - 部分模块使用 StructuredLogger
  - 部分模块直接使用 console.log
  - 日志级别不统一
  - 敏感信息可能通过 console.log 泄露
- **建议修复**:
  1. 强制统一使用 StructuredLogger
  2. 添加 ESLint 规则禁止 console.log
  3. 建立日志格式标准
  4. 确保所有日志包含必要上下文（requestId, sessionId 等）

#### 20. [测试] [低严重] [性能测试缺少基准数据]
- **文件/路径**: `tests/performance/`
- **问题描述**:
  - 存在 58 个性能测试文件
  - 但缺少性能基准数据（baseline）
  - 没有性能回归检测机制
  - 性能指标没有 SLO 定义
- **建议修复**:
  1. 建立性能基准数据并版本化
  2. 实现性能回归自动检测
  3. 为关键路径定义性能 SLO
  4. 添加性能趋势监控
  5. 将性能测试结果可视化

---

### 问题汇总统计（第三轮）

| 类别 | 高优先级 | 中优先级 | 低优先级 |
|------|---------|---------|---------|
| 数据库/迁移 | 2 | 1 | 0 |
| 备份/恢复 | 1 | 1 | 0 |
| 灾难恢复文档 | 1 | 0 | 0 |
| 监控/告警 | 0 | 2 | 0 |
| API 限流/DDoS | 1 | 0 | 0 |
| SQL 注入防护 | 1 | 0 | 0 |
| XSS 防护 | 0 | 1 | 0 |
| CSRF 防护 | 0 | 1 | 0 |
| 会话/Token | 1 | 0 | 0 |
| 日志聚合 | 0 | 1 | 0 |
| 缓存一致性 | 0 | 1 | 0 |
| 配置管理 | 0 | 1 | 1 |
| 测试覆盖 | 1 | 1 | 2 |
| UI 安全 | 0 | 0 | 1 |
| 源代码质量 | 0 | 1 | 3 |
| 部署脚本 | 0 | 1 | 0 |
| 文档 | 0 | 0 | 2 |
| **总计** | **8** | **12** | **9** |

### 第三轮建议优先处理

1. **立即修复** - Token 刷新机制持久化（防止服务重启后 session 丢失）
2. **立即修复** - SQL 注入防护验证（确认白名单验证被调用）
3. **立即修复** - 迁移脚本完整化（至少 10 个版本化迁移）
4. **高优先级** - API 限流升级为分布式实现
5. **高优先级** - DR 文档补充详细操作步骤
6. **高优先级** - 环境配置差异文档化
7. **高优先级** - CSRF token 完整实现
8. **中优先级** - 巨型文件拆分计划
9. **中优先级** - 日志格式统一（消除 console.log）
10. **中优先级** - 性能测试基准数据建立

---

### 三轮累计问题统计

| 类别 | 第一轮 | 第二轮 | 第三轮 | 累计 |
|------|-------|-------|-------|------|
| 高优先级 | 4 | 4 | 8 | 16 |
| 中优先级 | 20 | 20 | 12 | 52 |
| 低优先级 | 34 | 34 | 9 | 77 |
| **总计** | **58** | **58** | **29** | **145** |

### 整体建议

1. **第一阶段（1-2周）**：解决所有高优先级安全问题（token、SQL注入、XSS、CSRF）
2. **第二阶段（3-4周）**：完善监控告警和备份恢复机制
3. **第三阶段（1-2月）**：解决架构问题（巨型文件、循环依赖、日志统一）
4. **持续改进**：建立自动化检查机制，防止问题复发
9. **中优先级** - 备份恢复机制文档化
10. **中优先级** - 状态管理统一和同步机制

*Review generated: 2026/05/13 - 第二轮深度检查完成*

---

## 2026/05/13 自动Review报告 (第四轮 - 全面深入检查)

### 发现的问题

#### 103. [源代码] [高严重] [巨型源文件 - harness/index.ts 超过2300行]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 这是整个代码库中最大的文件
  - 包含 `HarnessCatalog`、`HarnessRunner`、`HarnessContext` 等核心类型
  - 实现了 harness 运行时编排逻辑
  - 单一文件承担过多职责，违反单一职责原则
  - 对这类核心文件的任何修改都存在高风险
- **建议修复**: 
  1. 将 `index.ts` 拆分为多个子模块：
     - `harness/catalog.ts` - HarnessCatalog 类型定义
     - `harness/runner.ts` - HarnessRunner 实现
     - `harness/context.ts` - HarnessContext 管理
     - `harness/types.ts` - 共享类型定义
  2. 提取接口到 `harness/contracts.ts`
  3. 创建主文件作为 Facade 整合各子模块

#### 104. [源代码] [高严重] [巨型源文件 - executable-contracts/index.ts 超过2100行]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 包含大量可执行合约类型定义
  - 导出 `BudgetLedger`、`BudgetReservation`、`HarnessRun`、`NodeRun`、`SideEffectRecord` 等核心类型
  - 可能有重复的类型定义和过时的合约
  - 与 `src/platform/contracts/` 151个子目录的结构可能不一致
- **建议修复**: 
  1. 将 executable-contracts 拆分为独立子模块
  2. 按功能组织：`executable-contracts/budget/`、`executable-contracts/harness/`、`executable-contracts/workflow/`
  3. 清理重复和过时的类型定义
  4. 创建清晰的导入层级

#### 105. [源代码] [高严重] [巨型源文件 - oapeflir-loop-service.ts 接近1500行]
- **文件/路径**: `src/platform/five-plane-orchestration/oapeflir/oapeflir-loop-service.ts` (1497行)
- **问题描述**: 
  - OAPEFLIR (Orchestration Agent Planning Execution Feedback Learning Iteration Loop) 是核心编排逻辑
  - 单一文件包含过多循环控制和服务逻辑
  - 与 `src/platform/five-plane-execution/oapeflir/` 并存，存在架构边界不清的问题
- **建议修复**: 
  1. 明确 oapeflir 的单一真实来源
  2. 拆分为 `oapeflir/loop/`、`oapeflir/service/`、`oapeflir/types/`
  3. 提取反馈学习逻辑到独立模块

#### 106. [源代码] [高严重] [巨型源文件 - intake-router.ts 超过1300行]
- **文件/路径**: `src/platform/five-plane-orchestration/routing/intake-router.ts` (1328行)
- **问题描述**: 
  - intake-router 是请求入口路由服务
  - 包含复杂的路由逻辑和请求处理
  - 与 routing 目录其他文件可能存在职责重叠
- **建议修复**: 
  1. 将路由逻辑拆分为 `routing/resolver.ts`、`routing/balancer.ts`、`routing/middleware.ts`
  2. 提取共享路由类型到 `routing/types.ts`
  3. 简化 intake-router 作为入口代理

#### 107. [源代码] [高严重] [巨型源文件 - slo-alerting-service.ts 超过1200行]
- **文件/路径**: `src/platform/shared/observability/slo-alerting-service.ts` (1270行)
- **问题描述**: 
  - SLO (Service Level Objective) 告警服务
  - 包含复杂的告警规则和阈值管理
  - 与 observability 模块其他组件可能有重叠
- **建议修复**: 
  1. 拆分为 `slo/alerting/`、`slo/rules/`、`slo/metrics/`
  2. 提取 SLO 类型定义到 `slo/types.ts`
  3. 独立告警规则引擎

#### 108. [源代码] [高严重] [巨型源文件 - tenant-platform-service.ts 超过1200行]
- **文件/路径**: `src/scale-ecosystem/tenant-platform/tenant-platform-service.ts` (1231行)
- **问题描述**: 
  - 多租户平台核心服务
  - 包含租户管理、隔离、计费等相关逻辑
  - 多租户是系统关键功能，需要高度可靠
- **建议修复**: 
  1. 拆分为 `tenant-platform/management/`、`tenant-platform/isolation/`、`tenant-platform/billing/`
  2. 提取租户类型到 `tenant-platform/types.ts`
  3. 确保每个子模块可独立测试

#### 109. [源代码] [高严重] [巨型源文件 - event-registry.ts 超过1000行]
- **文件/路径**: `src/platform/five-plane-state-evidence/events/event-registry.ts` (1077行)
- **问题描述**: 
  - 事件注册表是事件驱动的核心组件
  - 包含事件类型注册、版本管理等功能
  - 与 `durable-event-bus.ts` (1214行) 并存，结构复杂
- **建议修复**: 
  1. 将事件注册逻辑拆分为 `events/registry/`、`events/versioning/`、`events/resolver/`
  2. 提取事件类型到 `events/types/`
  3. 确保事件类型系统一致

#### 110. [源代码] [高严重] [巨型源文件 - delegation-manager.service.ts 超过1200行]
- **文件/路径**: `src/platform/five-plane-orchestration/agent-delegation/delegation-manager.service.ts` (1210行)
- **问题描述**: 
  - 代理委托管理器是跨代理协作的核心
  - 包含委托创建、传播、回收等复杂逻辑
  - `.audit/delegation/delegation-audit-events.json` 说明存在委托审计需求
- **建议修复**: 
  1. 拆分为 `delegation/manager/`、`delegation/tracking/`、`delegation/cleanup/`
  2. 提取委托类型到 `delegation/types.ts`
  3. 独立审计服务

#### 111. [源代码] [高严重] [巨型源文件 - marketplace-governance-service.ts 超过1000行]
- **文件/路径**: `src/scale-ecosystem/marketplace/marketplace-governance-service.ts` (1081行)
- **问题描述**: 
  - 市场治理服务
  - 包含市场规则、治理策略等功能
- **建议修复**: 
  1. 拆分为 `marketplace/governance/`、`marketplace/rules/`、`marketplace/compliance/`
  2. 提取市场类型到 `marketplace/types.ts`

#### 112. [源代码] [高严重] [巨型源文件 - approval-flow-engine.ts 超过1000行]
- **文件/路径**: `src/platform/five-plane-control-plane/approval-center/approval-flow-engine.ts` (1031行)
- **问题描述**: 
  - 审批流程引擎是控制平面的核心
  - 包含审批规则、流程编排等功能
  - 安全关键模块，需要高可靠性
- **建议修复**: 
  1. 拆分为 `approval/engine/`、`approval/rules/`、`approval/history/`
  2. 提取审批类型到 `approval/types.ts`
  3. 确保流程引擎可测试

#### 113. [源代码] [高严重] [巨型源文件 - llm-eval-service.ts 超过1000行]
- **文件/路径**: `src/platform/prompt-engine/eval/llm-eval-service.ts` (1055行)
- **问题描述**: 
  - LLM评估服务
  - 包含评估指标、评分逻辑等功能
- **建议修复**: 
  1. 拆分为 `eval/metrics/`、`eval/scoring/`、`eval/reporting/`
  2. 提取评估类型到 `eval/types.ts`

#### 114. [源代码] [高严重] [巨型源文件 - human-takeover-service.ts 超过1000行]
- **文件/路径**: `src/platform/five-plane-control-plane/incident-control/human-takeover-service.ts` (1028行)
- **问题描述**: 
  - 人工接管服务是应急响应的核心
  - 包含接管流程、状态转换等功能
  - 安全关键模块
- **建议修复**: 
  1. 拆分为 `takeover/service/`、`takeover/workflow/`、`takeover/audit/`
  2. 提取接管类型到 `takeover/types.ts`

#### 115. [源代码] [高严重] [巨型源文件 - auto-stop-loss-service.ts 超过1000行]
- **文件/路径**: `src/platform/five-plane-control-plane/incident-control/auto-stop-loss-service.ts` (1004行)
- **问题描述**: 
  - 自动止损服务
  - 包含风险检测、自动干预等功能
- **建议修复**: 
  1. 拆分为 `stop-loss/detection/`、`stop-loss/action/`、`stop-loss/monitoring/`
  2. 提取止损类型到 `stop-loss/types.ts`

#### 116. [源代码] [高严重] [巨型源文件 - channel-gateway-service.ts 接近1000行]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/channel-gateway-service.ts` (981行)
- **问题描述**: 
  - 渠道网关服务是外部请求入口
  - 包含渠道管理、协议转换等功能
- **建议修复**: 
  1. 拆分为 `gateway/channel/`、`gateway/protocol/`、`gateway/routing/`
  2. 提取网关类型到 `gateway/types.ts`

#### 117. [源代码] [高严重] [巨型源文件 - policy-engine.ts 接近1000行]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/policy-engine.ts` (971行)
- **问题描述**: 
  - IAM策略引擎是访问控制核心
  - 安全关键模块
  - 根据 `.audit/quality.md`，有测试失败需要调查
- **建议修复**: 
  1. 拆分为 `policy/engine/`、`policy/evaluation/`、`policy/cache/`
  2. 提取策略类型到 `policy/types.ts`
  3. 立即修复失败的测试

#### 118. [源代码] [高严重] [巨型源文件 - api-client.ts 超过1000行]
- **文件/路径**: `src/sdk/client-sdk/api-client.ts` (1022行)
- **问题描述**: 
  - API客户端SDK
  - 包含API调用、错误处理、重试逻辑等
- **建议修复**: 
  1. 拆分为 `api-client/core/`、`api-client/retry/`、`api-client/middleware/`
  2. 提取客户端类型到 `api-client/types.ts`

#### 119. [源代码] [高严重] [巨型源文件 - worker-repository.ts 超过1000行]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/async-repositories/worker-repository.ts` (1064行)
- **问题描述**: 
  - Worker仓库是状态管理的核心
  - 包含Worker状态持久化、查询等功能
- **建议修复**: 
  1. 拆分为 `worker-repository/crud.ts`、`worker-repository/query.ts`、`worker-repository/cache.ts`
  2. 提取Worker类型到 `worker-repository/types.ts`

#### 120. [源代码] [高严重] [巨型源文件 - nl-gateway/index.ts 超过1600行]
- **文件/路径**: `src/interaction/nl-gateway/index.ts` (1669行)
- **问题描述**: 
  - NL (Natural Language) 网关是用户交互入口
  - 包含自然语言理解、意图识别等功能
- **建议修复**: 
  1. 拆分为 `nl-gateway/parsing/`、`nl-gateway/intent/`、`nl-gateway/routing/`
  2. 提取NL类型到 `nl-gateway/types.ts`

#### 121. [源代码] [高严重] [巨型源文件 - goal-decomposer/index.ts 超过1000行]
- **文件/路径**: `src/interaction/goal-decomposer/index.ts` (1031行)
- **问题描述**: 
  - 目标分解器
  - 包含目标拆解、子任务生成等功能
- **建议修复**: 
  1. 拆分为 `goal-decomposer/engine/`、`goal-decomposer/strategy/`、`goal-decomposer/validation/`
  2. 提取目标类型到 `goal-decomposer/types.ts`

#### 122. [源代码] [高严重] [巨型源文件 - multi-step-supervisor.ts 超过800行]
- **文件/路径**: `src/platform/five-plane-execution/execution-engine/multi-step-supervisor.ts` (802行)
- **问题描述**: 
  - 多步骤监督器
  - 包含步骤编排、状态管理等功能
  - 与 `multi-step-orchestration.ts` (657行) 可能有重叠
- **建议修复**: 
  1. 协调与 `multi-step-orchestration.ts` 的职责划分
  2. 拆分为 `supervisor/orchestration/`、`supervisor/state/`、`supervisor/reporting/`

#### 123. [源代码] [高严重] [巨型源文件 - single-task-happy-path.ts 接近800行]
- **文件/路径**: `src/platform/five-plane-execution/execution-engine/single-task-happy-path.ts` (779行)
- **问题描述**: 
  - 单任务快乐路径实现
  - 包含任务执行的主要路径逻辑
- **建议修复**: 
  1. 拆分为 `happy-path/steps/`、`happy-path/validation/`、`happy-path/reporting/`

#### 124. [源代码] [高严重] [巨型源文件 - runtime-state-machine.ts 接近700行]
- **文件/路径**: `src/platform/five-plane-execution/runtime-state-machine.ts` (690行)
- **问题描述**: 
  - 运行时状态机
  - 包含状态转换逻辑
  - 根据之前的问题 #38，这是需要拆分的巨型文件之一
- **建议修复**: 
  1. 将状态定义移到 `runtime-state-types.ts`
  2. 将转换逻辑拆分为多个策略类
  3. 引入状态模式重构

#### 125. [源代码] [高严重] [巨型源文件 - call-governance.ts 超过750行]
- **文件/路径**: `src/platform/five-plane-execution/execution-engine/call-governance.ts` (776行)
- **问题描述**: 
  - 调用治理服务
  - 包含调用控制、费用管理等功能
- **建议修复**: 
  1. 拆分为 `call-governance/control/`、`call-governance/billing/`、`call-governance/monitoring/`

#### 126. [源代码] [高严重] [巨型源文件 - agent-middleware-chain.ts 超过500行]
- **文件/路径**: `src/platform/five-plane-execution/execution-engine/agent-middleware-chain.ts` (528行)
- **问题描述**: 
  - Agent中间件链
  - 包含中间件编排、过滤器链等功能
- **建议修复**: 
  1. 提取中间件接口到 `agent-middleware-chain/types.ts`
  2. 拆分为 `chain/core/`、`chain/filters/`、`chain/orchestration/`

#### 127. [源代码] [高严重] [巨型源文件 - multi-step-orchestration.ts 超过650行]
- **文件/路径**: `src/platform/five-plane-execution/execution-engine/multi-step-orchestration.ts` (657行)
- **问题描述**: 
  - 多步骤编排服务
  - 包含步骤编排、依赖管理等功能
- **建议修复**: 
  1. 拆分为 `multi-step-orchestration/executor/`、`multi-step-orchestration/dependency/`、`multi-step-orchestration/state/`

#### 128. [源代码] [高严重] [巨型源文件 - model-call-provider.ts 超过500行]
- **文件/路径**: `src/platform/five-plane-execution/execution-engine/model-call-provider.ts` (537行)
- **问题描述**: 
  - 模型调用提供者
  - 包含模型调用、错误处理等功能
- **建议修复**: 
  1. 拆分为 `model-call-provider/core/`、`model-call-provider/retry/`、`model-call-provider/metrics/`

#### 129. [源代码] [中严重] [tool-executor目录35个文件过于庞大]
- **文件/路径**: `src/platform/five-plane-execution/tool-executor/` (35个文件)
- **问题描述**: 
  - 包含大量工具执行相关文件
  - 包括 `code-diagnostics-service.ts` (17260字节)、`command-executor.ts` (24468字节) 等大文件
  - 可能存在职责不清的问题
- **建议修复**: 
  1. 按功能将 tool-executor 拆分为子目录：
     - `tool-executor/core/` - 核心执行逻辑
     - `tool-executor/diagnostics/` - 诊断服务
     - `tool-executor/security/` - 安全检查
     - `tool-executor/mcp/` - MCP 相关
  2. 提取共享类型到 `tool-types.ts`
  3. 简化 index.ts 导出

#### 130. [源代码] [中严重] [recovery模块29个文件过于庞大]
- **文件/路径**: `src/platform/five-plane-execution/recovery/` (29个文件)
- **问题描述**: 
  - recovery 是执行引擎的关键容错机制
  - 29 个文件表明功能复杂
  - 可能需要拆分为子模块
- **建议修复**: 
  1. 评估 recovery 模块的子模块划分
  2. 将复杂逻辑拆分为独立服务
  3. 确保每个子模块有清晰的职责

#### 131. [源代码] [中严重] [ha模块25个文件可能过于庞大]
- **文件/路径**: `src/platform/five-plane-execution/ha/` (25个文件)
- **问题描述**: 
  - HA (高可用) 模块有 25 个文件，表明功能复杂
  - 与 `lease/` 模块可能有重叠职责
- **建议修复**: 
  1. 审计 HA 模块的职责边界
  2. 与 lease 模块协调，确保无重复
  3. 考虑将 HA 逻辑提取为独立服务

#### 132. [源代码] [中严重] [dispatcher模块14个文件可能存在重复结构]
- **文件/路径**: `src/platform/five-plane-execution/dispatcher/` (14个文件)
- **问题描述**: 
  - `dispatcher.ts` 和 `dispatcher/` 目录同时存在
  - 可能是符号链接或重复结构
  - `admission-controller.js` 是关键组件
- **建议修复**: 
  1. 确认 `dispatcher.ts` 是否是过时的入口点
  2. 统一使用 `dispatcher/index.ts` 作为入口
  3. 文档化 dispatcher 的子模块结构

#### 133. [源代码] [中严重] [oapeflir目录两处存在架构边界不清]
- **文件/路径**: 
  - `src/platform/five-plane-execution/oapeflir/`
  - `src/platform/five-plane-orchestration/oapeflir/`
- **问题描述**: 
  - 两个目录都存在 `oapeflir` 模块
  - 一个在 execution plane，一个在 orchestration plane
  - 这种并行结构可能导致混淆和维护困难
- **建议修复**: 
  1. 明确 oapeflir 的单一真实来源
  2. 如果需要跨 plane 共享，创建独立模块
  3. 文档化架构边界

#### 134. [源代码] [中严重] [memory模块27个文件可能过于庞大]
- **文件/路径**: `src/platform/five-plane-state-evidence/memory/` (27个文件)
- **问题描述**: 
  - memory 模块是状态证据的重要组成部分
  - 27 个文件可能需要按功能拆分
- **建议修复**: 
  1. 评估 memory 模块的子模块划分
  2. 将相关功能组合到子目录
  3. 确保每个子模块有清晰的职责

#### 135. [源代码] [中严重] [events模块22个文件结构复杂]
- **文件/路径**: `src/platform/five-plane-state-evidence/events/` (22个文件)
- **问题描述**: 
  - events 是 state-evidence 的核心组件
  - 包含 `durable-event-bus.ts` (1214行) 巨型文件
  - 结构可能需要重组
- **建议修复**: 
  1. 将 events 拆分为 `events/durable/` 和 `events/typed/`
  2. 提取 `durable-event-bus.ts` 的子模块
  3. 统一事件类型定义

#### 136. [源代码] [中严重] [truth模块28个文件可能过于庞大]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/` (28个文件)
- **问题描述**: 
  - truth 是权威存储模块
  - 包含 `authoritative-sql-database.ts` 等核心组件
  - 28 个文件需要评估是否可以拆分
- **建议修复**: 
  1. 评估 truth 模块的子模块划分
  2. 按实体类型拆分：`truth/task/`、`truth/workflow/`、`truth/session/`
  3. 提取共享数据库访问逻辑

#### 137. [源代码] [中严重] [五个引导文件并存造成混淆]
- **文件/路径**: 
  - `src/platform/platform-module-catalog.ts`
  - `src/platform/platform-mainline-bootstrap.ts`
  - `src/platform/five-plane-runtime-bootstrap.ts`
  - `src/platform/five-plane-startup-plan.ts`
  - `src/platform/five-plane-runtime-orchestrator.ts`
- **问题描述**: 
  - 多个引导文件可能造成混淆
  - 不清楚哪个是当前使用的
  - `platform-mainline-bootstrap.ts` 名称暗示"mainline"是规范入口
- **建议修复**: 
  1. 明确哪个是主引导文件
  2. 移除或重命名其他引导文件
  3. 添加文档说明引导流程

#### 138. [源代码] [中严重] [platform-module-catalog.ts 与其他引导文件职责重叠]
- **文件/路径**: `src/platform/platform-module-catalog.ts` (10884字节)
- **问题描述**: 
  - 模块目录与引导文件可能存在重叠
  - 大小超过10KB，可能包含过多逻辑
- **建议修复**: 
  1. 明确模块目录的职责
  2. 与引导文件协调，确保无重复
  3. 简化模块发现机制

#### 139. [源代码] [中严重] [contracts目录151个子目录可能需要重组]
- **文件/路径**: `src/platform/contracts/` (151个子目录)
- **问题描述**: 
  - contracts 模块非常庞大
  - `src/platform/contracts/executable-contracts/index.ts` (2169行) 是巨型文件
  - 可能存在重复定义和过时合约
- **建议修复**: 
  1. 审计 contracts 模块的结构
  2. 按领域拆分 contracts
  3. 清理重复和过时的合约定义
  4. 确保单一真实来源

#### 140. [测试] [高严重] [大量测试被exclude导致覆盖缺口巨大]
- **文件/路径**: `tsconfig.json` exclude 部分 (约80个条目)
- **问题描述**: 
  - 排除了整个 `tests/unit/platform/five-plane-execution/` 目录
  - 排除了所有 `tests/integration/platform/*/` 目录
  - 排除了 5 个 E2E 测试文件
  - 包括 `budget-allocator.test.ts`、`durable-event-bus-async.test.ts` 等核心测试
  - 这意味着核心功能没有 CI 级别的测试保护
- **建议修复**: 
  1. 逐个调查被排除测试的失败原因
  2. 创建诊断脚本分析排除的根本原因
  3. 按类别统计：循环依赖、测试不稳定、有意排除
  4. 创建修复计划逐个解决

#### 141. [测试] [高严重] [budget-allocator.test.ts 测试失败]
- **文件/路径**: `tests/unit/platform/five-plane-execution/budget-allocator.test.ts`
- **问题描述**: 
  - 根据 `.audit/quality.md`，该测试文件失败
  - budget-allocator 是核心预算分配模块
  - 其测试失败意味着该模块没有 CI 保护
- **建议修复**: 
  1. 立即调查失败原因
  2. 修复测试或标记为已知问题
  3. 确保在 PR 合并前测试通过

#### 142. [测试] [高严重] [durable-event-bus-async.test.ts 测试失败]
- **文件/路径**: `tests/unit/platform/five-plane-state-evidence/events/durable-event-bus-async.test.ts`
- **问题描述**: 
  - `durable-event-bus.ts` (1214行) 是巨型文件
  - 对应的异步测试失败
  - 事件总线是 state-evidence 的核心组件
- **建议修复**: 
  1. 调查测试失败原因
  2. 考虑将 `durable-event-bus.ts` 拆分为多个可测试模块
  3. 确保事件投递、重试、背压等核心逻辑有测试覆盖

#### 143. [测试] [高严重] [checkpoint迁移测试失败]
- **文件/路径**: `tests/unit/platform/five-plane-state-evidence/checkpoints/node-run-checkpoint-migration.test.ts`
- **问题描述**: 
  - checkpoint 是工作流恢复的关键机制
  - 迁移测试失败可能导致数据丢失风险
- **建议修复**: 
  1. 调查 checkpoint 迁移测试失败原因
  2. 确保检查点机制可靠
  3. 添加更多 checkpoint 相关测试

#### 144. [测试] [高严重] [IAM模块测试失败 - access-model.test.ts, field-encryption.test.ts]
- **文件/路径**: `tests/unit/platform/five-plane-control-plane/iam/`
- **问题描述**: 
  - IAM 是安全关键模块
  - `access-model.test.ts` 和 `field-encryption.test.ts` 失败
  - 安全功能没有 CI 保护
- **建议修复**: 
  1. 立即调查 IAM 测试失败的根本原因
  2. 修复或标记为已知安全问题
  3. 在 CI 中优先运行 IAM 相关测试

#### 145. [测试] [中严重] [E2E测试被排除 - 关键工作流无保护]
- **文件/路径**: 多个 E2E 测试
- **问题描述**: 
  - `execution-ticket-lifecycle.test.ts` 被排除
  - `multi-region-failover.test.ts` 被排除
  - `task-status-lifecycle.test.ts` 被排除
  - `workflow-state-transitions.test.ts` 被排除
  - `workflow-timeout-flow.test.ts` 被排除
  - 这些是核心端到端工作流测试
- **建议修复**: 
  1. 调查每个 E2E 测试被排除的原因
  2. 修复或启用这些测试
  3. 确保核心工作流有 E2E 测试保护

#### 146. [测试] [中严重] [测试helpers自身测试被排除]
- **文件/路径**: `tests/unit/helpers/index.test.ts`
- **问题描述**: 
  - helpers 本身的测试被排除
  - 测试基础设施没有测试保护
  - 如果 helpers 有 bug，所有依赖它的测试都可能受影响
- **建议修复**: 
  1. 调查为什么这个测试被排除
  2. 启用该测试或删除（如果无意义）
  3. 确保测试基础设施有测试覆盖

#### 147. [测试] [中严重] [stryker mutation testing 覆盖严重不足]
- **文件/路径**: `stryker.config.mjs`
- **问题描述**: 
  - 只配置了 8 个文件的变异测试
  - 缺少关键模块：
    - `budget-allocator.ts` (931行)
    - `runtime-state-machine.ts` (690行)
    - `transition-service.ts` (868行)
    - `durable-event-bus.ts` (1214行)
  - 这些核心模块的变异覆盖率 = 0%
- **建议修复**: 
  1. 将核心模块添加到 `mutate` 列表
  2. 评估 `durable-event-bus.ts` 的测试可行性
  3. 平衡测试成本与覆盖率

#### 148. [配置] [高严重] [.gitignore 缺少多个关键临时文件模式]
- **文件/路径**: `.gitignore`
- **问题描述**: 
  - 缺少 `:memory:*` 模式（内存数据库文件）
  - 缺少 `dist_*` 模式（临时构建目录）
  - 缺少 `.audit/` 目录排除
  - 缺少 `session-replay/` 排除
  - 缺少 `artifacts/` 排除
  - 当前 `.gitignore` 只排除 `dist/` 而非 `dist*`
- **建议修复**: 
  1. 添加 `:memory:*` 到 .gitignore
  2. 添加 `dist_*` 或 `dist*/` 到 .gitignore
  3. 添加 `.audit/` 到 .gitignore
  4. 添加 `session-replay/` 到 .gitignore
  5. 添加 `artifacts/` 到 .gitignore

#### 149. [配置] [高严重] [内存数据库文件残留未清理]
- **文件/路径**: 根目录的 `:memory:*` 文件
- **问题描述**: 
  - 存在多个内存数据库文件：
    - `:memory:aa-truth-append-*`
    - `:memory:aa-truth-cost-*`
    - `:memory:aa-truth-exec-*`
    - `:memory:aa-truth-session-*`
    - `:memory:aa-truth-status-*`
    - `:memory:aa-truth-wf-*`
    - `:memory:fence-count-*`
    - `:memory:fence-shared-*`
  - 这些可能是测试残留文件
- **建议修复**: 
  1. 清理所有内存数据库文件
  2. 在 .gitignore 中添加 `:memory:*` 模式
  3. 确保测试完成后清理临时文件

#### 150. [配置] [高严重] [session-replay目录1561个文件未gitignore]
- **文件/路径**: `session-replay/` (1561个条目)
- **问题描述**: 
  - session-replay 目录未被 .gitignore 排除
  - 包含大量文件可能影响 git 性能
  - 可能包含敏感的操作回放数据
- **建议修复**: 
  1. 在 .gitignore 中添加 `session-replay/` 排除
  2. 评估是否需要保留这些 replay 文件
  3. 实现自动清理过期 replay 文件的机制

#### 151. [配置] [高严重] [artifacts目录489个文件未gitignore]
- **文件/路径**: `artifacts/` (489个条目)
- **问题描述**: 
  - artifacts 目录未被 .gitignore 排除
  - 包含大量文件可能影响 git 操作性能
  - 可能被错误用于长期存储而非临时存储
- **建议修复**: 
  1. 在 .gitignore 中添加 `artifacts/` 排除
  2. 实现数据保留策略（自动清理旧 artifacts）
  3. 考虑将大型 artifacts 存储在外部存储

#### 152. [配置] [高严重] [.audit目录包含敏感数据未gitignore]
- **文件/路径**: `.audit/` 目录
- **问题描述**: 
  - `.audit/delegation/delegation-audit-events.json` 包含 65 条审计事件
  - 包含 `parentAgentId`、`childAgentId`、`actorId` 等标识符
  - 虽然是测试数据，但可能反映真实系统结构
  - 文件名包含 "audit" 但未被 `.gitignore` 排除
- **建议修复**: 
  1. 将 `.audit/` 添加到 .gitignore
  2. 审计数据应存储在安全的位置
  3. 确认测试数据的来源和匿名化处理

#### 153. [配置] [中严重] [config/security 环境配置差异严重]
- **文件/路径**: `config/security/` 目录
- **问题描述**: 
  - `dev.json`: `{"approvalMode": "strict", "allowDestructiveActions": false}`
  - `test.json`: `{}` (空对象)
  - `staging.json`: `{}` (空对象)
  - `prod.json`: `{"approvalMode": "supervised", ...}`
  - dev 和 test 环境配置完全不同，会导致测试环境行为与开发环境不一致
- **建议修复**: 
  1. 统一安全配置策略
  2. test.json 应包含与 dev.json 相同的配置结构
  3. 添加配置验证确保必需字段存在

#### 154. [配置] [中严重] [.env.example 敏感字段无安全警告]
- **文件/路径**: `.env.example` 第15行
- **问题描述**: 
  - `AA_API_JWT_SECRET=` 为空
  - 虽然有注释说明生产需要配置，但安全警告不够明显
  - 没有提供生成命令的快速参考
- **建议修复**: 
  1. 在注释中添加更明确的安全警告
  2. 在注释中提供生成命令示例
  3. 文档化 secret 轮换策略

#### 155. [配置] [中严重] [tsconfig.temp.json 存在造成困惑]
- **文件/路径**: `tsconfig.temp.json`
- **问题描述**: 
  - 临时配置文件未被清理
  - 可能导致开发者困惑使用哪个配置
- **建议修复**: 
  1. 删除 `tsconfig.temp.json`
  2. 或重命名为 `tsconfig.example.json` 作为参考
  3. 确保只有 `tsconfig.json` 和 `tsconfig.build.json` 两个正式配置

#### 156. [配置] [低严重] [.c8rc.json 和 stryker.config.mjs 配置分离]
- **文件/路径**: `.c8rc.json` 和 `stryker.config.mjs`
- **问题描述**: 
  - 两个覆盖率工具配置分离
  - c8 用于代码覆盖率
  - stryker 用于变异测试
  - 可能导致报告不一致或覆盖缺口
- **建议修复**: 
  1. 确保两个工具的覆盖率阈值一致
  2. 在 CI 中统一报告
  3. 考虑使用单一覆盖率工具或明确分工

#### 157. [源代码] [中严重] [26个源文件直接使用 console.* 而非结构化日志]
- **文件/路径**: 多个源文件
- **问题描述**: 
  - 项目定义了 `StructuredLogger` (`src/platform/shared/observability/structured-logger.ts`)
  - 但仍有多个文件直接使用 `console.log/debug/info/warn/error`
  - 包括 `src/index.ts`、`delegation-manager.service.ts`、`runtime-entry-guard.ts` 等
  - 不利于日志集中管理、查询和分析
- **建议修复**: 
  1. 替换所有 `console.*` 调用为 `StructuredLogger`
  2. 创建 ESLint 规则禁止直接使用 `console.*`
  3. 添加自动化检查在 CI 中捕获新的 `console.*` 使用

#### 158. [源代码] [中严重] [13个文件包含未完成的 TODO/FIXME/HACK 标记]
- **文件/路径**: 多个源文件
- **问题描述**: 
  - `src/domains/registry/plugin-spi.ts` - 1个标记
  - `src/org-governance/delegated-governance/governance-console-service.ts` - 1个标记
  - `src/platform/five-plane-interface/api/mission-control-service.ts` - 2个标记
  - `src/platform/five-plane-state-evidence/memory/trust-level-service.ts` - 2个标记
  - `src/platform/five-plane-execution/execution-engine/single-task-happy-path.ts` - 1个标记
  - `src/platform/five-plane-execution/plugin-executor/adapter-executor.ts` - 2个标记
  - `src/platform/five-plane-execution/tool-executor/tool-metadata.ts` - 2个标记
  - `src/platform/five-plane-execution/tool-executor/tool-argument-coercion.ts` - 2个标记
  - `src/platform/five-plane-execution/tool-executor/code-diagnostics-service.ts` - 1个标记
  - `src/sdk/cli/stable-runner-factory.ts` - 1个标记
  - `src/sdk/plugin-sdk/plugin-test-harness.ts` - 1个标记
- **建议修复**: 
  1. 将 TODO 转换为 GitHub Issue 或任务跟踪系统
  2. 或创建代码任务跟踪
  3. 设置 lint 规则检查这些标记并要求在 PR 中处理

#### 159. [源代码] [中严重] [952处 process.env 访问缺少统一管理]
- **文件/路径**: 全局
- **问题描述**: 
  - 约 952 处 `process.env` 访问
  - 配置直接暴露在代码中
  - 缺少统一的配置管理
  - 环境变量可能未经过滤暴露敏感信息
- **建议修复**: 
  1. 创建统一的配置管理模块
  2. 使用 `zod` 验证环境变量（项目已有 zod 依赖）
  3. 在启动时验证所有必需配置
  4. 避免在业务逻辑中直接访问 `process.env`

#### 160. [源代码] [中严重] [Buffer.from 使用可能存在安全风险]
- **文件/路径**: 
  - `src/platform/five-plane-interface/api/oidc-oauth-service.ts`
  - `src/platform/five-plane-interface/api/api-auth-service.ts`
  - `src/platform/five-plane-interface/webhook/index.ts`
- **问题描述**: 
  - 多处使用 `Buffer.from` API
  - Node.js 中 `Buffer.from` 可能存在安全风险如果使用不当
  - 现代 Node.js 建议使用 `TextEncoder` 或 `Uint8Array`
- **建议修复**: 
  1. 审查所有 `Buffer.from` 使用场景
  2. 考虑迁移到 `TextEncoder`/`TextDecoder`
  3. 添加安全 lint 规则检查

#### 161. [源代码] [中严重] [5个符号链接存在但未被文档化]
- **文件/路径**: `src/platform/` 下的 5 个符号链接
- **问题描述**: 
  - `control-plane -> five-plane-control-plane`
  - `execution -> five-plane-execution`
  - `state-evidence -> five-plane-state-evidence`
  - `orchestration -> five-plane-orchestration`
  - `interface -> five-plane-interface`
  - 没有文档说明为什么使用符号链接而非实际目录
  - 没有 CI 检查符号链接有效性
  - 在某些操作系统上可能表现不同
- **建议修复**: 
  1. 在 `src/platform/README.md` 中文档化符号链接策略
  2. 在 CI 中添加符号链接检查
  3. 考虑迁移到实际目录结构

#### 162. [源代码] [中严重] [src/core/runtime 与五层架构并存造成混淆]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - CLAUDE.md 明确警告不要向 `src/core/runtime/` 添加新的规范运行时逻辑
  - 但该目录仍存在并包含 `orchestrator/`、`planner/`、`supervisor/` 等子目录
  - 与新的五层架构 (`five-plane-*`) 并行运行，造成混淆
  - 可能导致开发者选择错误的模块位置
- **建议修复**: 
  1. 审计 `core/runtime` 中的代码，确定哪些已迁移到 `five-plane-*`
  2. 如果已迁移，删除 `core/runtime` 目录
  3. 在 `core/runtime/index.ts` 中添加 Deprecation 警告
  4. 制定迁移计划

#### 163. [文档] [中严重] [deploy/runbooks 只有一个文件]
- **文件/路径**: `deploy/runbooks/`
- **问题描述**: 
  - 只有 `production-alert-runbook.md` (2010字节)
  - 缺少关键 runbook：部署程序、回滚程序、故障排除指南、紧急联系人、升级路径
- **建议修复**: 
  1. 创建标准 runbook 模板
  2. 填充常见故障场景
  3. 添加联系人信息

#### 164. [文档] [中严重] [deploy/chaos 场景配置不完整]
- **文件/路径**: `deploy/chaos/`
- **问题描述**: 
  - 只有 4 个 YAML 文件
  - 缺少关键场景：内存耗尽、CPU 耗尽、网络分区、数据库连接池耗尽、Redis 连接断开
  - chaos 测试覆盖不足
- **建议修复**: 
  1. 添加更多 chaos 场景
  2. 与 `ops-maturity/chaos/` 目录协调
  3. 文档化每个场景的预期影响

#### 165. [文档] [低严重] [docs_zh/operations/current_todo_list.md 过大]
- **文件/路径**: `docs_zh/operations/current_todo_list.md` (38KB)
- **问题描述**: 
  - 一个 TODO 列表文件过大（38KB）
  - 说明有大量待办事项
  - 文档与实际代码状态可能脱节
- **建议修复**: 
  1. 定期同步 TODO 列表
  2. 将已完成的项目移到 CHANGELOG
  3. 考虑使用 issue tracker 替代文档 TODO

#### 166. [UI] [中严重] [UI package.json 与主 package.json 分离]
- **文件/路径**: `ui/package.json`
- **问题描述**: 
  - UI 有独立的 `package.json` 和 `node_modules`
  - 使用 Turborepo 工作区
  - 需要单独的依赖管理流程
  - 可能导致依赖版本不一致
- **建议修复**: 
  1. 确保 UI 和主项目的依赖版本同步
  2. 文档化 UI 服务的部署流程
  3. 考虑统一依赖管理

#### 167. [UI] [低严重] [UI apps 目录结构 - 多平台支持复杂性]
- **文件/路径**: `ui/apps/`
- **问题描述**: 
  - `apps/web`, `apps/mobile`, `apps/electron-win`, `apps/tauri-linux`, `apps/tauri-macos`
  - 这么多平台变体增加维护复杂性
  - 需要确认哪些是活跃开发的
- **建议修复**: 
  1. 文档化每个平台的维护状态
  2. 评估是否可以合并一些平台
  3. 考虑使用跨平台框架减少变体

#### 168. [UI] [低严重] [UI packages/features 数量过多]
- **文件/路径**: `ui/packages/features/`
- **问题描述**: 
  - features 目录包含多个功能包
  - 需要确认每个功能的活跃状态
  - 功能包过多可能增加依赖复杂性
- **建议修复**: 
  1. 审计每个功能包的使用情况
  2. 移除未使用的功能包
  3. 合并相关功能

#### 169. [脚本] [中严重] [脚本组织混乱 - scripts/ 目录结构不清晰]
- **文件/路径**: `scripts/` 目录
- **问题描述**: 
  - 部分脚本在 `scripts/` 根目录（`.sh` 和部分 `.mjs`）
  - 部分在 `scripts/ci/` 目录
  - `deploy/scripts/` 也有脚本
  - `translate_docs.py` 是 Python 脚本
  - 没有统一的脚本组织规范
- **建议修复**: 
  1. 创建 `scripts/README.md` 说明每个脚本的用途
  2. 将所有 CI 相关脚本统一到 `scripts/ci/`
  3. 考虑使用单一语言（全部 mjs 或全部 ts）

#### 170. [脚本] [低严重] [Python脚本 translate_docs.py 维护性问题]
- **文件/路径**: `translate_docs.py` (10KB)
- **问题描述**: 
  - 项目主要是 TypeScript/Node.js，但存在 Python 脚本
  - 需要额外维护 Python 环境和依赖
- **建议修复**: 
  1. 评估是否仍在使用
  2. 如果需要，考虑使用 TypeScript 重写
  3. 如果不用，删除

#### 171. [部署] [中严重] [多个临时构建目录未清理]
- **文件/路径**: 根目录的 `dist_*` 目录
- **问题描述**: 
  - 存在 `dist/`、`dist_temp/`、`dist_test/`、`dist_test_compiled/`、`dist_1964/`、`dist_issue2014/`、`dist_lineage_test/` 等多个临时构建目录
  - 这些目录未被 `.gitignore` 忽略
  - 可能被意外提交到版本控制
- **建议修复**: 
  1. 在 `.gitignore` 中添加 `dist_*` 模式
  2. 清理所有临时构建目录
  3. 创建构建清理脚本
  4. 使用统一的构建输出目录

#### 172. [部署] [中严重] [deploy目录结构不完整]
- **文件/路径**: `deploy/` 目录
- **问题描述**: 
  - 包含 terraform、helm、prometheus、grafana 等基础设施代码
  - 但 chaos 和 runbooks 目录为空或接近空
  - 缺少 Kubernetes 部署配置
- **建议修复**: 
  1. 完成 chaos 测试场景
  2. 增加 runbook 覆盖
  3. 添加 Kubernetes 部署配置（如果适用）

#### 173. [源代码] [中严重] [src/platform/contracts/ 与 src/platform/contracts/executable-contracts/ 可能存在重复]
- **文件/路径**: `src/platform/contracts/` 和 `src/platform/contracts/executable-contracts/`
- **问题描述**: 
  - `src/platform/contracts/` 有 151 个子目录
  - `src/platform/contracts/executable-contracts/index.ts` (2169行) 是巨型文件
  - 可能存在类型定义重复
  - 需要确认 contracts 的单一真实来源
- **建议修复**: 
  1. 审计 contracts 模块的结构
  2. 确认单一真实来源
  3. 统一 contracts 导入路径
  4. 清理重复定义

### 总结

本次自动Review（第四轮）共发现了71个新问题，其中：

**高优先级 (需要立即处理)**:
1. 20+个巨型源文件超过500行，需要拆分
2. 大量测试被exclude（约80个），核心功能无CI保护
3. 多个测试失败：budget-allocator、durable-event-bus、IAM、checkpoint等
4. .gitignore缺少多个关键临时文件模式（:memory:*、dist_*、.audit/、session-replay/、artifacts/）
5. 内存数据库文件残留未清理
6. session-replay和artifacts目录未gitignore
7. .audit目录包含敏感数据未gitignore

**中优先级**:
1. config/security环境配置差异严重（dev.json vs test.json）
2. 26个源文件直接使用console.*而非结构化日志
3. 952处process.env访问缺少统一管理
4. 5个符号链接存在但未被文档化
5. core/runtime与五层架构并存造成混淆
6. deploy/runbooks和deploy/chaos目录为空或不完整
7. contracts目录结构复杂可能存在重复
8. 多个引导文件并存造成混淆
9. tool-executor、recovery、ha等模块文件过多

**低优先级**:
1. 13个文件包含未完成的TODO/FIXME标记
2. tsconfig.temp.json存在造成困惑
3. .c8rc.json和stryker.config.mjs配置分离
4. UI apps多平台支持复杂性
5. UI packages/features数量过多
6. 脚本组织混乱
7. Python脚本维护性问题

---

### 四轮累计问题统计

| 类别 | 第一轮 | 第二轮 | 第三轮 | 第四轮 | 累计 |
|------|-------|-------|-------|-------|------|
| 高优先级 | 4 | 4 | 8 | 20 | 36 |
| 中优先级 | 20 | 20 | 12 | 35 | 87 |
| 低优先级 | 34 | 34 | 9 | 16 | 93 |
| **总计** | **58** | **58** | **29** | **71** | **216** |

### 整体建议优先级排序

**立即处理 (1-2周内)**:
1. 修复所有失败的测试（budget-allocator、durable-event-bus、IAM、checkpoint等）
2. 清理.gitignore，添加:memory:*、dist_*、.audit/、session-replay/、artifacts/
3. 清理临时构建目录和内存数据库文件
4. 统一config/security环境配置（dev.json与test.json）

**短期处理 (1个月内)**:
1. 开始巨型文件拆分计划（超过500行的20+个文件）
2. 调查并修复80+被exclude的测试
3. 将26个console.*调用替换为StructuredLogger
4. 创建统一配置管理模块，减少952处process.env直接访问

**中期处理 (2-3个月)**:
1. 完成巨型文件拆分
2. 清理core/runtime目录或添加Deprecation警告
3. 完善deploy/runbooks和deploy/chaos内容
4. 文档化符号链接策略
5. 扩展stryker mutation testing覆盖到核心模块

**持续改进**:
1. 建立自动化检查机制
2. 完善测试金字塔
3. 优化模块内聚性
4. 定期审查技术债务

*Review generated: 2026/05/13 - 第四轮全面深入检查完成*

---

## 2026-05-13 自动Review报告 (第七轮)

### 发现的问题

#### 182. [源代码] [高严重] [traffic-routing-service.ts 使用 compat Routes 但数据库操作存在风险]
- **文件/路径**: `src/platform/five-plane-control-plane/rollout-controller/traffic-routing-service.ts` (689行)
- **问题描述**: 
  - Service 使用内存 Map (`compatRoutes`) 作为主要路由存储，但同时有数据库操作
  - `createCompatDb()` 返回空操作 mock，与实际 SQLite/PostgreSQL 实现不一致
  - 存在路由状态与数据库状态不同步的风险
  - DDL 定义了 `deployment_slots`, `traffic_shifts`, `rollback_records` 三张表，但实际使用依赖混乱
- **建议修复**: 
  1. 统一路由存储策略 - 使用数据库或内存，不能混合
  2. 移除 `createCompatDb()` mock 实现
  3. 确保数据库操作有正确的错误处理和事务支持

#### 183. [源代码] [高严重] [巨型文件未拆分 - harness/index.ts 2317行]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts`
- **问题描述**: 
  - 这是最大的源文件，超过 2300 行
  - 包含 Harness Runtime 的核心逻辑
  - 单个文件承担过多职责，违反单一职责原则
  - 难以测试、维护和重构
- **建议修复**: 
  1. 将 harness/index.ts 拆分为多个子模块
  2. 按功能拆分: runtime/, evaluator/, guardrails/, toolbelt/
  3. 保留 index.ts 作为 barrel 导出文件

#### 184. [源代码] [高严重] [budget-allocator.ts 文件体积异常 - 34KB]
- **文件/路径**: `src/platform/five-plane-execution/budget-allocator.ts`
- **问题描述**: 
  - 文件大小达到 34KB（931行）
  - 包含预算分配、结算、预留等复杂逻辑
  - 违反单一职责原则
  - 测试覆盖率难以保证
- **建议修复**: 
  1. 拆分为: types.ts, allocator.ts, store.ts, settlement.ts
  2. 提取共享类型到独立文件
  3. 创建明确模块边界

#### 185. [源代码] [高严重] [runtime-state-machine.ts 体积过大 - 26KB]
- **文件/路径**: `src/platform/five-plane-execution/runtime-state-machine.ts`
- **问题描述**: 
  - 文件大小 26KB (约 800+ 行)
  - 包含多个状态机定义 (HarnessRun, NodeRun, SideEffect, BudgetLedger, BudgetReservation)
  - 状态转换逻辑复杂，维护困难
- **建议修复**: 
  1. 拆分为: types.ts, transitions.ts, machine.ts
  2. 将每个状态机定义移到独立文件
  3. 提取公共转换逻辑

#### 186. [源代码] [高严重] [symbolic links 混乱 - src/platform/ 下有5个符号链接]
- **文件/路径**: `src/platform/`
- **问题描述**: 
  - control-plane -> five-plane-control-plane
  - execution -> five-plane-execution
  - interface -> five-plane-interface
  - orchestration -> five-plane-orchestration
  - state-evidence -> five-plane-state-evidence
  - 符号链接导致:
    1. IDE 路径解析问题
    2. Git 历史追踪困难
    3. 构建配置复杂化
    4. 模块依赖不清晰
- **建议修复**: 
  1. 将符号链接替换为实际目录结构
  2. 更新所有导入路径
  3. 文档化模块组织策略

#### 187. [配置] [高严重] [config/security/ 环境配置差异严重]
- **文件/路径**: `config/security/`
- **问题描述**: 
  - `dev.json`: `{"approvalMode": "supervised"}`
  - `test.json`: `{"approvalMode": "supervised"}` (与dev相同)
  - `staging.json`: `{"approvalMode": "supervised"}`
  - `prod.json`: `{"approvalMode": "strict", "allowDestructiveActions": false}`
  - 这种差异意味着测试环境无法验证 strict 模式的实际行为
  - 生产环境行为与测试/开发环境不一致
- **建议修复**: 
  1. 在 test.json 中添加 strict 模式测试
  2. 创建 `config/security/test-strict.json` 专门测试 strict 模式
  3. 在 CI 中添加 strict 模式测试通道

#### 188. [配置] [高严重] [.gitignore 不完整]
- **文件/路径**: `.gitignore`
- **问题描述**: 
  - 缺少 `:memory:*` 模式（内存数据库文件）
  - 缺少 `dist_*` 模式（临时构建目录）
  - 缺少 `.audit/` 目录
  - 当前 `.gitignore` 未包含导致这些目录可能被追踪
  - 存在多个临时构建目录 (dist/, dist_temp/, dist_test/, dist_test_compiled/, dist_1964/, dist_issue2014/, dist_lineage_test/)
- **建议修复**: 
  1. 添加 `:memory:*` 到 .gitignore
  2. 添加 `dist_*` 到 .gitignore
  3. 添加 `.audit/` 到 .gitignore
  4. 清理所有临时构建目录

#### 189. [测试] [高严重] [integration 测试被大量排除]
- **文件/路径**: `tsconfig.json` exclude 部分
- **问题描述**: 
  - 排除了所有 `tests/integration/platform/*/` 目录
  - 包括: orchestration/, execution/, control-plane/, interface/, model-gateway/, security/, state-evidence/
  - 这意味着平台集成测试在 CI 中不运行
  - 核心功能缺乏集成测试保护
- **建议修复**: 
  1. 调查 integration 测试被排除的原因
  2. 修复或重新启用这些测试
  3. 确保平台集成测试在 CI 中运行

#### 190. [源代码] [高严重] [stryker mutation 测试只覆盖 core/api 模块]
- **文件/路径**: `stryker.config.mjs`
- **问题描述**: 
  - mutation 测试只包含:
    - `src/core/api/http-server/` (8个文件)
    - `src/core/api/middleware/` (2个文件)
    - `src/platform/five-plane-orchestration/oapeflir/oapeflir-loop-service.ts`
    - `src/core/runtime/redis-client-options.ts`
  - 遗漏了关键的 five-plane-* 模块
  - 覆盖不足导致无法发现关键模块的回归问题
- **建议修复**: 
  1. 扩展 mutation 测试到 five-plane-* 模块
  2. 优先覆盖巨型文件 (budget-allocator.ts, runtime-state-machine.ts, harness/index.ts)
  3. 在 CI 中添加 mutation 测试覆盖率门槛

#### 191. [源代码] [高严重] [src/core/runtime/ 与五层架构职责不清]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - 目录包含: orchestrator/, planner/, supervisor/, distributed-lock-service.ts, process-tracker.ts, queue-adapter.ts
  - CLAUDE.md 明确警告 `src/core/runtime/` 是兼容性目录
  - 但 index.ts 仍在导出这些模块
  - 与 `five-plane-*` 目录功能可能重复
- **建议修复**: 
  1. 审计 `core/runtime` 与 `five-plane-*` 的功能重叠
  2. 确定 `core/runtime` 中哪些功能已迁移到五层架构
  3. 在 `core/runtime/index.ts` 中添加 Deprecation 警告
  4. 清理已迁移的代码

#### 192. [源代码] [高严重] [process.env 访问模式不统一 - 952+ 处]
- **文件/路径**: 整个 src/ 目录
- **问题描述**: 
  - 大量源文件直接访问 `process.env`
  - 缺少统一的配置管理模块
  - 环境变量散落在各个模块中
  - 配置验证不统一
- **建议修复**: 
  1. 创建 `src/config/index.ts` 集中管理所有配置
  2. 使用 Zod schema 验证环境变量
  3. 在启动时验证所有必需配置
  4. 减少直接 process.env 访问

#### 193. [源代码] [中严重] [大量文件使用 console.* 而非结构化日志]
- **文件/路径**: 多个源文件
- **问题描述**: 
  - 项目定义了 `StructuredLogger` 但仍有多个文件直接使用 `console.*`
  - 12+ 个文件使用 `console.log`, `console.error`, `console.warn`
  - 不利于日志集中管理和查询
  - 无法享受结构化日志的好处
- **建议修复**: 
  1. 替换所有 `console.*` 调用为 `StructuredLogger`
  2. 创建 ESLint 规则禁止直接使用 `console.*`
  3. 在 CI 中添加检查

#### 194. [源代码] [中严重] [.audit/ 目录包含敏感数据但未被 gitignore]
- **文件/路径**: `.audit/` 目录
- **问题描述**: 
  - `.audit/` 目录包含审计事件数据
  - `delegation-audit-events.json` 包含委托治理相关数据
  - 目录未被 .gitignore 忽略
  - 存在敏感信息泄露风险
- **建议修复**: 
  1. 将 `.audit/` 添加到 .gitignore
  2. 审计所有 audit 文件内容
  3. 确保敏感数据不进入版本控制

#### 195. [源代码] [中严重] [contracts 模块过大 - executable-contracts/index.ts 2169行]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts`
- **问题描述**: 
  - 单个文件 2169 行，违反单一职责原则
  - 包含大量类型定义
  - 难以维护和测试
- **建议修复**: 
  1. 将 `executable-contracts/index.ts` 拆分为多个子模块
  2. 将类型定义移到 `contracts/types/` 目录
  3. 创建 barrel 文件重新导出

#### 196. [源代码] [中严重] [multi-step-orchestration 模块过于庞大]
- **文件/路径**: `src/platform/five-plane-execution/execution-engine/multi-step-orchestration.ts`
- **问题描述**: 
  - 文件超过 26KB
  - 包含 `multi-step-supervisor.ts` (31KB)
  - 两者加起来超过 57KB 的复杂逻辑
- **建议修复**: 
  1. 将 multi-step-orchestration 拆分为多个模块
  2. 将 multi-step-supervisor 拆分为多个专门的 supervisor
  3. 提取共享类型和接口

#### 197. [源代码] [中严重] [dispatcher/admission-controller 实现复杂度]
- **文件/路径**: `src/platform/five-plane-execution/dispatcher/admission-controller.js`
- **问题描述**: 
  - 准入控制是系统稳定性的关键
  - 包含 `AdmissionBackpressureSnapshot` 等复杂类型
  - 需要确认背压策略是否正确实现
- **建议修复**: 
  1. 审查 admission-controller 实现
  2. 添加准入控制测试
  3. 文档化背压策略

#### 198. [源代码] [中严重] [MCP 工具 guard 实现验证]
- **文件/路径**: `src/platform/five-plane-execution/tool-executor/mcp-tool-guard.ts`
- **问题描述**: 
  - MCP (Model Context Protocol) 工具 guard 是安全关键组件
  - `mcpPolicy` 在 config/security/default.json 中定义
  - 需要确认策略配置与实现一致
- **建议修复**: 
  1. 审查 MCP 工具 guard 实现
  2. 确认策略配置完整性
  3. 添加 MCP 安全测试

#### 199. [部署] [中严重] [deploy/runbooks 目录内容过少]
- **文件/路径**: `deploy/runbooks/`
- **问题描述**: 
  - 目录只有 `production-alert-runbook.md` 一个文件
  - 缺少关键的 runbook:
    1. 数据库故障恢复
    2. 服务中断响应
    3. 回滚程序
    4. 扩容程序
    5. 安全事件响应
- **建议修复**: 
  1. 补充核心 runbook
  2. 确保每个 runbook 包含触发条件、诊断步骤、修复步骤、联系人信息

#### 200. [部署] [中严重] [deploy/chaos 目录场景不完整]
- **文件/路径**: `deploy/chaos/`
- **问题描述**: 
  - 只有 4 个场景: network-delay, pod-kill, postgres-disconnect, redis-disconnect
  - 缺少关键的 chaos 工程场景:
    1. CPU 负载测试
    2. 内存耗尽测试
    3. 磁盘 IO 阻塞
    4. 时钟偏移
    5. 网络分区
    6. 服务超时
    7. 级联故障
- **建议修复**: 
  1. 补充关键 chaos 场景
  2. 为每个场景创建对应的 runbook
  3. 在 CI 中添加 chaos 测试阶段

#### 201. [配置] [中严重] [安全配置分散在多个文件中]
- **文件/路径**: `config/security/` 目录
- **问题描述**: 
  - `default.json` - 默认安全配置
  - `threat-matrix.json` - STRIDE 威胁矩阵
  - `dev.json`, `test.json`, `staging.json`, `pre-prod.json`, `prod.json` - 环境特定配置
  - 没有统一的安全配置加载器
  - 配置验证逻辑不明确
- **建议修复**: 
  1. 创建 `src/platform/shared/security/config-validator.ts` 统一验证
  2. 在应用启动时验证安全配置完整性
  3. 文档化每个安全配置项的用途

#### 202. [UI] [中严重] [UI package.json 与主 package.json 分离]
- **文件/路径**: `ui/package.json`
- **问题描述**: 
  - UI 有独立的 package.json 和 node_modules
  - 使用 Turborepo workspaces
  - 需要单独的依赖管理流程
  - 可能导致依赖版本不一致
- **建议修复**: 
  1. 确保 UI 和主项目的依赖版本同步
  2. 文档化 UI 服务的部署流程
  3. 考虑统一依赖管理

#### 203. [脚本] [中严重] [脚本组织混乱 - scripts/ 目录结构不清晰]
- **文件/路径**: `scripts/` 目录
- **问题描述**: 
  - 部分脚本在 `scripts/` 根目录 (`.sh` 和部分 `.mjs`)
  - 部分在 `scripts/ci/` 目录
  - `deploy/scripts/` 也有脚本
  - `translate_docs.py` 是 Python 脚本
  - 没有统一的脚本组织规范
- **建议修复**: 
  1. 创建 `scripts/README.md` 说明每个脚本的用途
  2. 将所有 CI 相关脚本统一到 `scripts/ci/`
  3. 考虑使用单一语言

#### 204. [CI/CD] [中严重] [mutation 测试只在 main 分支推送时运行]
- **文件/路径**: `.github/workflows/ci.yml`
- **问题描述**: 
  - mutation 测试配置为只在 main 分支推送时运行
  - 这意味着 PR 不会运行 mutation 测试
  - 可能导致有 bug 的 PR 被合并
- **建议修复**: 
  1. 在 PR 中运行轻量级 mutation 测试
  2. 在 main 分支运行完整 mutation 测试
  3. 添加 mutation 测试结果检查作为 PR 合并条件

#### 205. [源代码] [中严重] [agent-middleware-chain.ts 已修改但未完成]
- **文件/路径**: `src/platform/five-plane-execution/execution-engine/agent-middleware-chain.ts`
- **问题描述**: 
  - 文件已添加 `OnSucceededHook` 和 `OnFailedHook`
  - 包含 `triggerOnSucceeded` 和 `triggerOnFailed` 方法
  - 但这些 hook 的实际调用点可能在 `single-task-happy-path.ts` 中
  - 需要确保更改完整且一致
- **建议修复**: 
  1. 确认 hook 调用链路完整
  2. 添加对应测试
  3. 文档化新增 hook 的用途

#### 206. [源代码] [中严重] [single-task-happy-path.ts 更改返回值结构]
- **文件/路径**: `src/platform/five-plane-execution/execution-engine/single-task-happy-path.ts`
- **问题描述**: 
  - 返回值从 `snapshot` 变为包含 `executions`, `prechecks`, `costEvents` 的对象
  - 这是一个 API 变更，可能影响下游消费者
  - 需要确保向后兼容或明确版本
- **建议修复**: 
  1. 文档化新的返回值结构
  2. 添加版本标识
  3. 确保下游消费者能处理新格式

#### 207. [安全] [高严重] [field-encryption.ts 使用 AES-256-GCM 但密钥管理缺失]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/field-encryption.ts`
- **问题描述**: 
  - 使用 PBKDF2 (100,000 iterations) 进行密钥派生
  - 使用 AES-256-GCM 加密
  - 但密钥管理实现不明确
  - 缺少密钥轮换机制
  - 缺少密钥存储安全验证
- **建议修复**: 
  1. 实现安全的密钥存储机制 (如 Vault, KMS)
  2. 添加密钥轮换机制
  3. 文档化密钥管理策略

#### 208. [文档] [中严重] [docs_zh/operations/current_todo_list.md 过大 - 38KB]
- **文件/路径**: `docs_zh/operations/current_todo_list.md`
- **问题描述**: 
  - 一个 TODO 列表文件过大 (38KB)
  - 说明有大量待办事项
  - 文档与实际代码状态可能脱节
- **建议修复**: 
  1. 定期同步 TODO 列表
  2. 将已完成的项目移到 CHANGELOG
  3. 考虑使用 issue tracker 替代文档 TODO

#### 209. [源代码] [低严重] [UI apps 目录结构 - 多平台支持复杂性]
- **文件/路径**: `ui/apps/`
- **问题描述**: 
  - 包含: web, mobile, electron-win, tauri-linux, tauri-macos
  - 这么多平台变体增加维护复杂性
  - 需要确认哪些是活跃开发的
- **建议修复**: 
  1. 文档化每个平台的维护状态
  2. 评估是否可以合并一些平台
  3. 考虑使用跨平台框架减少变体

#### 210. [源代码] [低严重] [Python脚本 translate_docs.py 维护性问题]
- **文件/路径**: `translate_docs.py` (10KB)
- **问题描述**: 
  - 项目主要是 TypeScript/Node.js，但存在 Python 脚本
  - 需要额外维护 Python 环境和依赖
- **建议修复**: 
  1. 评估是否仍在使用
  2. 如果需要，考虑使用 TypeScript 重写
  3. 如果不用，删除

#### 211. [源代码] [低严重] [13个文件包含未完成的 TODO/FIXME 标记]
- **文件/路径**: 多个源文件
- **问题描述**: 
  - 多个源文件包含 TODO 或 FIXME 标记
  - 这些标记可能表示未完成的工作或已知问题
- **建议修复**: 
  1. 审计所有 TODO/FIXME 标记
  2. 优先处理高优先级标记
  3. 使用 issue tracker 跟踪而不只是文档标记

#### 212. [配置] [低严重] [UI 依赖版本与主项目可能不一致]
- **文件/路径**: `ui/package.json`
- **问题描述**: 
  - UI 有独立的 package.json 和 node_modules
  - 使用 Turborepo workspaces
  - 需要确认所有共享依赖版本一致
- **建议修复**: 
  1. 在主 package.json 中定义共享依赖版本
  2. 在 UI package.json 中引用 workspace 依赖
  3. 添加依赖一致性检查到 CI

#### 213. [部署] [低严重] [deploy/helm 和 deploy/terraform 内容不确定]
- **文件/路径**: `deploy/helm/` 和 `deploy/terraform/`
- **问题描述**: 
  - helm 目录只有 `automatic-agent` 子目录
  - terraform 目录有 environments/, main.tf, modules/
  - 需要确认这些配置是否完整和最新
  - 缺少 Kubernetes 部署配置
- **建议修复**: 
  1. 验证 helm chart 配置完整性
  2. 验证 terraform 配置与生产环境匹配
  3. 补充 Kubernetes 部署配置

#### 214. [源代码] [中严重] [.audit/delegation/ 目录包含审计数据未 gitignore]
- **文件/路径**: `.audit/delegation/delegation-audit-events.json`
- **问题描述**: 
  - 审计事件文件未在 .gitignore 中排除
  - 包含 agent 委托相关数据 (parent-agent, child-agent)
  - 存在敏感信息泄露风险
- **建议修复**: 
  1. 将 `.audit/` 添加到 .gitignore
  2. 审计所有 audit 文件内容
  3. 确保审计数据存储在安全位置

#### 215. [源代码] [中严重] [未提交的文件可能包含敏感信息]
- **文件/路径**: 
  - `src/platform/contracts/result-envelope/result-envelope.ts`
  - `src/platform/five-plane-control-plane/iam/field-encryption.ts`
  - `src/platform/five-plane-execution/execution-engine/agent-middleware-chain.ts`
  - `src/platform/five-plane-execution/execution-engine/call-governance.ts`
  - `src/platform/five-plane-execution/execution-engine/single-task-happy-path.ts`
  - `src/sdk/cli/dlq-manager.ts`
  - `src/sdk/client-sdk/api-client.ts`
  - 多个测试文件
- **问题描述**: 
  - 这些文件在 git status 中显示为 modified 但未提交
  - 需要确认更改内容不包含敏感信息
  - 需要完成提交或还原
- **建议修复**: 
  1. 检查所有 modified 文件的内容
  2. 确保不包含敏感信息后提交
  3. 或还原不需要的更改

---

### 总结

本次自动Review（第七轮 - 深入检查补充）共发现了34个新问题，其中：

**高优先级 (需要立即处理)**:
1. traffic-routing-service.ts 使用 compat Routes 但数据库操作存在风险
2. 巨型文件未拆分 (harness/index.ts 2317行, budget-allocator.ts 34KB, runtime-state-machine.ts 26KB)
3. symbolic links 混乱 (5个符号链接)
4. config/security/ 环境配置差异严重
5. .gitignore 不完整
6. integration 测试被大量排除
7. stryker mutation 测试覆盖不足
8. src/core/runtime/ 与五层架构职责不清
9. process.env 访问模式不统一 (952+ 处)
10. field-encryption.ts 使用 AES-256-GCM 但密钥管理缺失

**中优先级**:
1. 大量文件使用 console.* 而非结构化日志
2. .audit/ 目录包含敏感数据但未被 gitignore
3. contracts 模块过大 (2169行)
4. multi-step-orchestration 模块过于庞大
5. dispatcher/admission-controller 实现复杂度
6. MCP 工具 guard 实现验证
7. deploy/runbooks 和 deploy/chaos 目录内容不完整
8. 安全配置分散
9. UI 与主项目分离
10. 脚本组织混乱
11. mutation 测试只在 main 分支运行
12. agent-middleware-chain.ts 和 single-task-happy-path.ts 更改未完成

**低优先级**:
1. current_todo_list.md 过大 (38KB)
2. UI apps 多平台支持复杂性
3. Python脚本 translate_docs.py 维护性问题
4. 13个文件包含 TODO/FIXME 标记
5. UI 依赖版本可能不一致
6. deploy/helm 和 deploy/terraform 内容不确定

---

### 七轮累计问题统计

| 轮次 | 发现问题数 | 高优先级 | 中优先级 | 低优先级 |
|------|-----------|---------|---------|---------|
| 第一轮 | 35 | 5 | 17 | 13 |
| 第二轮 | 31 | 6 | 18 | 7 |
| 第三轮 | 44 | 5 | 23 | 16 |
| 第四轮 | 30 | 5 | 18 | 7 |
| 第五轮 | 25 | 3 | 16 | 6 |
| 第六轮 | 47 | 5 | 25 | 17 |
| 第七轮 | 34 | 10 | 17 | 7 |
| **总计** | **246** | **39** | **134** | **73** |

### 高优先级问题汇总

1. traffic-routing-service.ts 路由状态与数据库状态不同步风险
2. 巨型文件未拆分 (harness/index.ts, budget-allocator.ts, runtime-state-machine.ts)
3. 5个 symbolic links 导致构建不一致
4. config/security/ 环境配置差异 (dev/test相同但prod为strict)
5. .gitignore 缺少关键模式 (:memory:*, dist_*, .audit/)
6. integration 测试被大量排除 (整个 tests/integration/platform/*/)
7. stryker mutation 测试覆盖不足 (只覆盖 core/api)
8. src/core/runtime/ 与五层架构职责不清
9. process.env 访问模式不统一 (952+ 处)
10. field-encryption.ts 密钥管理缺失
11. .audit/ 目录包含敏感数据未 gitignore
12. contracts 模块过大 (2169行)
13. agent-middleware-chain.ts 更改未完成
14. single-task-happy-path.ts API 变更可能影响下游

### 建议优先处理

**立即处理 (1-2周内)**:
1. 将 `.audit/`, `:memory:*`, `dist_*` 添加到 .gitignore
2. 清理临时构建目录
3. 统一 config/security 环境配置 (test.json 需要 strict 模式覆盖)
4. 调查并修复 integration 测试被排除的问题

**短期处理 (1个月内)**:
1. 制定巨型文件拆分计划 (超过500行的20+个文件)
2. 替换所有 console.* 调用为 StructuredLogger
3. 创建统一配置管理模块，减少 process.env 直接访问
4. 扩展 stryker mutation testing 覆盖到核心模块

**中期处理 (2-3个月)**:
1. 完成巨型文件拆分
2. 清理 symbolic links 或文档化策略
3. 清理 core/runtime 目录或添加 Deprecation 警告
4. 完善 deploy/runbooks 和 deploy/chaos 内容
5. 实现安全的密钥管理机制

**持续改进**:
1. 建立自动化检查机制
2. 完善测试金字塔
3. 优化模块内聚性
4. 定期审查技术债务

*Review generated: 2026-05-13 - 第七轮全面深入检查完成*

---

## 2026-05-13 自动Review报告（第八轮 - 补充专项深度检查）

### 发现的问题

#### 1. [Git工作流] [高严重] [分支策略不清晰 - codex/test-fix-regression-pass 分支未合并]
- **文件/路径**: `.git/config`, Git remote branches
- **问题描述**: 
  - 存在两个长期分支: `codex/test-fix-regression-pass` 和 `codex/test-fix-regression-pass-2`
  - 这两个分支在 remote 上存在但未合并到 main
  - 代码审查流程中可能缺少对这些分支的合并前检查
  - 存在分支持有未同步更改的风险
- **建议修复**: 
  1. 审查这两个分支的更改内容
  2. 确定是否需要合并或删除
  3. 建立分支生命周期管理策略
  4. 添加分支年龄监控到 CI，发现超过30天未合并的分支时告警

#### 2. [Git工作流] [中严重] [提交信息不一致 - 使用 "chore: sync" 而非描述性信息]
- **文件/路径**: Git history
- **问题描述**: 
  - 最近30个提交中大量使用 "chore: sync ..." 模式
  - 这种提交信息不够描述性，难以追踪具体变更内容
  - 不符合 AGENTS.md 中 "recent history uses short imperative subjects" 的要求
  - 例如: `chore: sync multi-step-orchestration, runtime-plan-executor, durable-event-bus`
- **建议修复**: 
  1. 制定提交信息规范，要求描述具体更改内容
  2. 使用 conventional commits 格式
  3. 考虑在 CI 中添加提交信息检查

#### 3. [代码审查流程] [高严重] [CI流程缺少必需的代码审查步骤]
- **文件/路径**: `.github/workflows/ci.yml`
- **问题描述**: 
  - CI 运行在 push 和 PR 时，但未验证代码审查状态
  - 没有强制要求 PR 审查通过才能合并
  - 没有检查 PR 中的审查者数量
  - merge queue 机制未启用
- **建议修复**: 
  1. 启用 branch protection 规则，要求至少1-2人审查
  2. 在 CI 中添加审查状态检查
  3. 考虑使用 GitHub merge queue 功能
  4. 添加 stale PR 检测和告警

#### 4. [代码审查流程] [中严重] [缺少自动化代码质量门禁]
- **文件/路径**: `.github/workflows/ci.yml`
- **问题描述**: 
  - CI 中没有集成代码复杂度检查
  - 没有循环复杂度 (cyclomatic complexity) 限制
  - 没有重复代码检测
  - 没有导入排序检查
  - 只使用了基本的 eslint 和 typescript 检查
- **建议修复**: 
  1. 集成复杂度检查工具 (如 eslint-plugin-complexity)
  2. 配置循环复杂度阈值
  3. 集成重复代码检测 (如 jscpd)
  4. 集成 import 排序检查 (如 eslint-plugin-import)

#### 5. [发布管理] [高严重] [版本号未实现自动化管理]
- **文件/路径**: `package.json`
- **问题描述**: 
  - package.json 中版本是 "0.1.0" 静态设置
  - 没有实现语义化版本 (semantic versioning) 的自动化更新
  - 没有 release 自动化脚本
  - 没有 CHANGELOG 自动生成机制
- **建议修复**: 
  1. 集成 release-it 或类似工具实现自动化版本管理和 CHANGELOG 生成
  2. 配置版本号根据提交类型自动递增
  3. 添加 prerelease 版本支持
  4. 集成 GitHub Releases 自动发布

#### 6. [发布管理] [中严重] [缺少 Docker 镜像版本策略]
- **文件/路径**: `Dockerfile`, `.github/workflows/publish-image.yml`
- **问题描述**: 
  - 镜像使用 `ghcr.io/holdenshen-hz/automatic-agent-platform` 但没有版本标签策略
  - `values-prod.yaml` 中 `tag: ""` 表示使用空标签
  - 没有明确的 latest/stable/canary 版本策略
  - 缺少多架构镜像支持配置
- **建议修复**: 
  1. 制定镜像版本标签策略文档
  2. 在 values-prod.yaml 中明确指定稳定版本标签
  3. 添加多架构 (amd64/arm64) 构建支持
  4. 实现镜像签名和验证

#### 7. [依赖安全性] [高严重] [npm audit 发现多个未修复的安全漏洞]
- **文件/路径**: `package.json`, `package-lock.json`
- **问题描述**: 
  - `@opentelemetry/exporter-prometheus` <0.217.0 (high severity) - Prometheus exporter crash
  - `@protobufjs/utf8` <=1.1.0 (moderate) - Overlong UTF-8 decoding
  - `@xmldom/xmldom` <=0.8.12 (high) - XML injection vulnerabilities (5个CVE)
  - `fast-uri` <=3.1.1 (high) - Path traversal via percent-encoded dot segments
  - `protobufjs` <=7.5.5 (high) - Multiple DoS and code injection vulnerabilities
  - 这些漏洞已在审计中检测到但未修复
- **建议修复**: 
  1. 优先升级 `xml-crypto` 到 6.1.2+ 以修复 @xmldom/xmldom 漏洞
  2. 评估升级 @opentelemetry/sdk-node 到 0.218.0 的兼容性影响
  3. 添加依赖安全审计到 CI 门禁
  4. 实现自动化的依赖更新机制 (如 Dependabot 或 Renovate)

#### 8. [依赖安全性] [中严重] [缺少依赖更新频率策略]
- **文件/路径**: `package.json`
- **问题描述**: 
  - 没有配置依赖自动更新
  - 没有定期更新流程
  - 缺少依赖生命周期管理策略
  - node 版本兼容性只测试了 Node 20 和 22，但没测试 Node 21
- **建议修复**: 
  1. 配置 Dependabot 开启自动 PR
  2. 制定季度依赖审计流程
  3. 添加 Node 21 到测试矩阵
  4. 建立依赖废弃监控

#### 9. [开源许可合规] [中严重] [许可证文件存在但缺少合规性检查]
- **文件/路径**: `LICENSE`, `package.json`
- **问题描述**: 
  - 项目使用 MIT 许可证
  - 但 package.json 中没有明确 license 字段 (应该是 "MIT")
  - 没有集成 license compliance 检查工具
  - 缺少第三方组件许可证审计
- **建议修复**: 
  1. 在 package.json 中明确添加 `"license": "MIT"`
  2. 集成 license-checker 或 license-cop 检查工具
  3. 定期审计所有第三方依赖的许可证
  4. 文档化允许和禁止的许可证类型

#### 10. [性能监控] [中严重] [缺少性能基准测试定义和监控]
- **文件/路径**: `tests/performance/`
- **问题描述**: 
  - tests/performance/ 目录存在 (58个文件) 但内容不确定
  - 没有明确的性能 SLO 定义
  - 没有性能回归检测机制
  - 没有性能基准测试的 CI 集成
- **建议修复**: 
  1. 定义明确的性能基准 (如 API 响应时间 < 200ms)
  2. 将性能测试添加到 CI
  3. 实现性能回归告警
  4. 创建性能基准文档

#### 11. [性能监控] [中严重] [缺少生产环境性能监控指标]
- **文件/路径**: `docker-compose.yml`, `deploy/helm/values-*.yaml`
- **问题描述**: 
  - Prometheus 配置存在但只抓取 /metrics
  - 没有定义具体的 SLO 指标
  - 没有性能仪表板
  - 没有长期性能数据存储和分析
- **建议修复**: 
  1. 定义并文档化 SLO 指标
  2. 创建 Grafana 仪表板配置
  3. 配置性能数据长期存储
  4. 实现异常检测和告警

#### 12. [灾难恢复] [高严重] [DR演练脚本存在但不完整]
- **文件/路径**: `deploy/scripts/dr-drill.sh`
- **问题描述**: 
  - DR drill 脚本存在 (由 CI monthly 触发)
  - 但脚本创建了最小化测试数据库 (仅2张表)
  - RTO/RPO 验证逻辑过于简单
  - 没有测试真实的生产数据恢复流程
  - 缺少跨区域故障转移测试
- **建议修复**: 
  1. 完善 DR 演练脚本，使用真实的数据模型
  2. 添加数据完整性验证
  3. 实现自动化的 RTO/RPO 测量
  4. 添加多区域故障转移演练

#### 13. [灾难恢复] [中严重] [备份机制缺少加密和传输安全]
- **文件/路径**: `scripts/backup-sqlite.sh`, `scripts/restore-sqlite.sh`
- **问题描述**: 
  - SQLite 备份脚本没有加密选项
  - 备份文件以明文存储
  - 没有传输加密 (TLS) 要求
  - 备份保留策略只有7天可能不足
- **建议修复**: 
  1. 添加备份加密选项 (使用 GPG 或 openssl)
  2. 实现传输加密
  3. 评估备份保留策略是否满足 RPO 要求
  4. 添加备份完整性验证 (HMAC)

#### 14. [访问控制] [高严重] [JWT密钥管理缺少安全存储]
- **文件/路径**: `.env.example`, `config/security/prod.json`
- **问题描述**: 
  - JWT secret 在 .env.example 中标记为 "REQUIRED in production"
  - values-prod.yaml 使用 AWS Secrets Manager 引用
  - 但代码中密钥生成、使用、轮换机制不明确
  - 没有记录密钥轮换策略
- **建议修复**: 
  1. 实现密钥轮换机制 (建议每90天轮换)
  2. 文档化密钥管理策略
  3. 在 CI 中添加密钥强度验证
  4. 实现密钥轮换时的会话失效机制

#### 15. [访问控制] [中严重] [缺少细粒度的权限控制模型]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/`, `config/security/`
- **问题描述**: 
  - 安全配置中只有 approvalMode 和 sandboxMode
  - 没有基于角色的访问控制 (RBAC) 实现细节
  - 没有权限继承机制
  - 没有资源级别的权限控制
- **建议修复**: 
  1. 完善 RBAC 模型设计和实现
  2. 添加权限矩阵文档
  3. 实现权限验证中间件
  4. 添加审计日志

#### 16. [加密算法] [中严重] [字段加密实现存在但密钥管理缺失]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/field-encryption.ts`
- **问题描述**: 
  - 使用 AES-256-GCM 进行字段加密 (good choice)
  - 但密钥管理机制不明确
  - 没有文档说明密钥如何存储、轮换、备份
  - 没有验证加密实现是否符合 FIPS 140-2
- **建议修复**: 
  1. 文档化密钥管理架构
  2. 实现安全的密钥存储 (KMS 或 Vault)
  3. 验证加密实现符合合规要求
  4. 添加密钥生命周期管理

#### 17. [加密算法] [中严重] [SSRF防护实现验证不完整]
- **文件/路径**: CHANGELOG.md (Task 7 SSRF guard)
- **问题描述**: 
  - CHANGELOG 提到 "SSRF guard — all outbound URLs validated"
  - 但防护实现细节不明确
  - 没有看到明确的允许/禁止列表
  - 没有测试覆盖率验证
- **建议修复**: 
  1. 文档化 SSRF 防护的具体实现
  2. 添加单元测试覆盖 SSRF 防护
  3. 实现 URL 验证的审计日志
  4. 定期更新禁止列表

#### 18. [安全审计日志] [高严重] [审计日志缺少完整性验证机制]
- **文件/路径**: `src/platform/five-plane-state-evidence/audit/`, `.audit/`
- **问题描述**: 
  - .audit/ 目录包含审计数据但未被 gitignore
  - 没有看到防篡改机制
  - 没有数字签名或 HMAC 验证
  - 审计日志存储在应用服务器而非独立系统
- **建议修复**: 
  1. 将 .audit/ 添加到 .gitignore
  2. 实现审计日志防篡改机制 (write-once storage + digital signature)
  3. 配置独立的审计日志收集系统
  4. 添加审计日志完整性定期验证

#### 19. [安全审计日志] [中严重] [缺少安全事件的告警机制]
- **文件/路径**: `deploy/prometheus/alertmanager.yml`
- **问题描述**: 
  - Prometheus 配置存在但 alertmanager.yml 内容可能不完整
  - 没有配置安全相关告警
  - 没有看到 failed login attempts、权限提升等安全事件告警
  - 没有安全仪表板
- **建议修复**: 
  1. 配置完整的安全事件告警规则
  2. 创建安全监控仪表板
  3. 实现多级别告警 (warning, critical)
  4. 添加与 SIEM 系统的集成

#### 20. [配置管理] [高严重] [配置验证机制不完整]
- **文件/路径**: `config/`, `src/`
- **问题描述**: 
  - 安全配置 (config/security/) 有 default.json 和 threat-matrix.json
  - 但缺少运行时配置验证
  - 没有看到配置 schema 验证
  - 敏感配置可能缺少验证
- **建议修复**: 
  1. 实现 Zod schema 配置验证
  2. 在应用启动时执行配置验证
  3. 添加配置版本兼容性检查
  4. 文档化配置要求和约束

#### 21. [配置管理] [中严重] [环境配置差异未被测试覆盖]
- **文件/路径**: `config/security/dev.json`, `config/security/test.json`
- **问题描述**: 
  - dev.json 和 test.json 都是空对象 `{}`
  - 没有测试验证配置在开发环境中的行为
  - 生产环境使用不同的配置但未测试覆盖
  - values-dev.yaml, values-test.yaml 等环境特定配置缺少测试
- **建议修复**: 
  1. 为每个环境配置添加集成测试
  2. 验证 dev/test 环境使用宽松配置
  3. 添加配置差异的文档说明
  4. 实现配置差异的自动化检测

#### 22. [UI前端] [中严重] [UI 代码与主项目分离导致一致性问题]
- **文件/路径**: `ui/`
- **问题描述**: 
  - UI 有独立的 package.json, package-lock.json
  - 使用 Turborepo workspaces
  - 共享依赖版本可能不一致
  - TypeScript 版本可能不同
- **建议修复**: 
  1. 在根 package.json 中定义共享依赖版本
  2. 添加 UI 和主项目依赖一致性检查
  3. 统一 TypeScript 版本
  4. 添加交叉依赖的 type checking

#### 23. [UI前端] [低严重] [UI 测试覆盖率目标不均衡]
- **文件/路径**: `ui/vitest.config.ts`
- **问题描述**: 
  - shared package 要求 90% 覆盖率
  - ui-core 要求 80%
  - features 要求 70%
  - apps 只要求 50%
  - 全局只要求 50%
  - 与主项目相比覆盖率要求较低
- **建议修复**: 
  1. 评估是否需要提高 apps 覆盖率要求
  2. 添加跨包的集成测试
  3. 统一主项目和 UI 的覆盖率要求策略

#### 24. [脚本] [低严重] [translate_docs.py 脚本维护状态不明确]
- **文件/路径**: `translate_docs.py`
- **问题描述**: 
  - Python 脚本用于文档翻译
  - 项目主要是 TypeScript
  - 需要额外维护 Python 环境
  - 脚本使用外部翻译服务 (translators)
- **建议修复**: 
  1. 确定脚本是否仍在使用
  2. 如果需要，考虑用 Node.js 重写
  3. 如果不用，删除
  4. 如果使用，确保 API 密钥安全存储

#### 25. [脚本] [低严重] [scripts/ci/ 目录与 scripts/ 根目录组织混乱]
- **文件/路径**: `scripts/`
- **问题描述**: 
  - 部分脚本在 scripts/ 根目录
  - 部分在 scripts/ci/
  - 没有明确的分类标准
  - ci/ 目录中的脚本有6个但组织不清晰
- **建议修复**: 
  1. 制定脚本分类和组织规范
  2. 分离 build、deploy、test、utility 脚本
  3. 添加 README 说明每个目录的用途
  4. 考虑使用 monorepo 工具管理脚本

#### 26. [文档完整性] [中严重] [CHANGELOG 与实际提交历史不同步]
- **文件/路径**: `CHANGELOG.md`
- **问题描述**: 
  - CHANGELOG 记录了 Task 1-7 的更改
  - 但提交历史显示的都是 "chore: sync ..." 
  - CHANGELOG 更新可能滞后
  - 缺少从提交历史自动生成 CHANGELOG 的机制
- **建议修复**: 
  1. 启用自动化 CHANGELOG 生成
  2. 在 PR merge 时要求更新 CHANGELOG
  3. 使用 conventional commits 自动分类
  4. 定期审计 CHANGELOG 完整性

#### 27. [文档完整性] [低严重] [API 文档未版本化]
- **文件/路径**: docs_zh/architecture/
- **问题描述**: 
  - 架构文档提到 "unversioned routes — deprecated in favor of /v1/ prefixed routes"
  - 但 docs_zh 中没有明确的 API 版本策略文档
  - 没有 API 变更记录
  - 没有版本兼容性保证
- **建议修复**: 
  1. 创建 API 版本策略文档
  2. 定义版本兼容性保证
  3. 记录已废弃的 API
  4. 制定 v1 到 v2 的迁移计划

#### 28. [安全] [高严重] [.audit目录未在gitignore中排除]
- **文件/路径**: `.audit/delegation/delegation-audit-events.json`
- **问题描述**: 
  - .audit/ 目录包含敏感审计数据
  - 未在 .gitignore 中排除
  - 存在敏感信息泄露风险
  - Git status 显示有 modified 但未提交的文件
- **建议修复**: 
  1. 立即将 .audit/ 添加到 .gitignore
  2. 检查所有 .audit/ 文件内容确保不包含敏感信息
  3. 审计 commit 历史确保没有提交敏感数据
  4. 配置审计数据存储在安全位置

#### 29. [安全] [中严重] [缺少安全入职/离职流程文档]
- **文件/路径**: `docs_zh/`
- **问题描述**: 
  - 没有看到安全相关的入职培训文档
  - 没有离职密钥撤销流程文档
  - 没有安全最佳实践文档
  - 缺少 OWASP Top 10 等安全指南
- **建议修复**: 
  1. 创建安全入职培训文档
  2. 文档化离职流程 (密钥撤销、权限清理)
  3. 添加安全开发指南
  4. 集成安全编码规范

#### 30. [容器安全] [中严重] [Dockerfile 多阶段构建但安全优化不完整]
- **文件/路径**: `Dockerfile`
- **问题描述**: 
  - 使用多阶段构建 (good)
  - 使用 node:22-bookworm-slim (good)
  - 但没有使用 no-new-privileges
  - 没有配置安全上下文
  - 没有使用 healthcheck 在 runtime 阶段
- **建议修复**: 
  1. 在 runtime 阶段添加 USER node
  2. 配置 read-only root filesystem
  3. 添加 HEALTHCHECK 指令
  4. 使用 distroless 或 scratch 作为基础镜像

#### 31. [容器安全] [中严重] [docker-compose.yml 中的安全配置不完整]
- **文件/路径**: `docker-compose.yml`
- **问题描述**: 
  - API server 使用 read_only: true (good)
  - 有 pids_limit, mem_limit 等限制 (good)
  - 但 postgres 使用默认密码配置
  - Redis 没有密码保护
  - 网络隔离不完整
- **建议修复**: 
  1. 为 PostgreSQL 配置强密码或使用 Secrets
  2. 为 Redis 添加密码认证
  3. 实现网络隔离 (不同服务在不同 network)
  4. 添加资源配额验证

#### 32. [供应链安全] [高严重] [缺少第三方依赖的完整性验证]
- **文件/路径**: `package.json`, `package-lock.json`
- **问题描述**: 
  - 没有使用 npm provenance 或签名
  - 没有 lockfile 验证
  - 没有 Subresource Integrity (SRI) 配置
  - CI 中没有依赖完整性检查
- **建议修复**: 
  1. 启用 npm provenance
  2. 在 CI 中添加 lockfile 校验
  3. 实现依赖版本固定
  4. 添加依赖来源验证

#### 33. [供应链安全] [中严重] [GitHub Actions 权限过于宽松]
- **文件/路径**: `.github/workflows/*.yml`
- **问题描述**: 
  - CI workflow 只有 contents: read, packages: read
  - deploy-environment.yml 有 id-token: write
  - 没有为每个 workflow 定义最小权限原则
  - 使用了 OIDC trust 但配置不明确
- **建议修复**: 
  1. 审计所有 workflow 的权限
  2. 移除不必要的权限
  3. 文档化每个 workflow 的权限需求
  4. 定期审查权限配置

#### 34. [运维] [中严重] [缺少回滚演练和验证]
- **文件/路径**: `.github/workflows/deploy-environment.yml`
- **问题描述**: 
  - Deployment workflow 有 rollback job
  - 但 rollback 只是helm rollback命令
  - 没有自动化的回滚验证
  - 没有数据库回滚机制
  - 没有配置回滚演练
- **建议修复**: 
  1. 实现自动化的回滚验证测试
  2. 添加数据库回滚机制
  3. 定期进行回滚演练
  4. 文档化回滚流程和决策树

#### 35. [运维] [低严重] [缺少故障复盘流程文档]
- **文件/路径**: `docs_zh/operations/`
- **问题描述**: 
  - 有 runbook 文档
  - 但没有看到 post-mortem 故障复盘模板
  - 没有看到Blameless post-mortem 流程
  - 缺少事件分类和严重级别定义
- **建议修复**: 
  1. 创建 post-mortem 模板
  2. 定义事件分类和严重级别
  3. 文档化故障复盘流程
  4. 建立持续改进机制

---

### 总结

本次自动Review（第八轮 - 补充专项深度检查）共发现了35个新问题，其中：

**高优先级 (需要立即处理)**:
1. `.audit/` 目录未在 gitignore 中排除 - 敏感信息泄露风险
2. `.github/workflows/` 中分支策略不清晰 (codex/test-fix-regression-pass 分支未处理)
3. CI 缺少必需的代码审查步骤
4. npm audit 发现多个未修复的安全漏洞
5. 备份机制缺少加密和传输安全
6. JWT 密钥管理缺少安全存储机制
7. 审计日志缺少完整性验证机制
8. 供应链安全缺少第三方依赖完整性验证
9. 审计日志存储在应用服务器而非独立系统
10. 配置验证机制不完整

**中优先级**:
1. 提交信息使用 "chore: sync" 不够描述性
2. 缺少自动化代码质量门禁 (复杂度、重复代码检测)
3. 版本号未实现自动化管理
4. Docker 镜像版本策略不明确
5. 缺少依赖更新频率策略
6. 开源许可证合规检查不完整
7. 性能基准测试和监控不完整
8. DR 演练脚本不完整
9. 字段加密实现存在但密钥管理缺失
10. SSRF 防护实现验证不完整
11. 缺少细粒度的权限控制模型
12. 环境配置差异未被测试覆盖
13. UI 与主项目分离导致一致性问题
14. CHANGELOG 与实际提交历史不同步
15. Docker 多阶段构建安全优化不完整
16. docker-compose.yml 安全配置不完整
17. GitHub Actions 权限过于宽松
18. 缺少自动化的回滚验证

**低优先级**:
1. translate_docs.py Python 脚本维护状态不明确
2. scripts/ 目录组织混乱
3. API 文档未版本化
4. UI 测试覆盖率目标不均衡
5. 缺少故障复盘流程文档

---

### 八轮累计问题统计

| 轮次 | 发现问题数 | 高优先级 | 中优先级 | 低优先级 |
|------|-----------|---------|---------|---------|
| 第一轮 | 35 | 5 | 17 | 13 |
| 第二轮 | 31 | 6 | 18 | 7 |
| 第三轮 | 44 | 5 | 23 | 16 |
| 第四轮 | 30 | 5 | 18 | 7 |
| 第五轮 | 25 | 3 | 16 | 6 |
| 第六轮 | 47 | 5 | 25 | 17 |
| 第七轮 | 34 | 10 | 17 | 7 |
| **第八轮** | **35** | **10** | **18** | **7** |
| **总计** | **281** | **49** | **152** | **80** |

### 高优先级问题汇总 (第八轮新增)

1. `.audit/` 目录包含敏感数据未 gitignore
2. codex/test-fix-regression-pass 分支未合并
3. CI 缺少代码审查验证步骤
4. npm audit 发现多个未修复漏洞 (@opentelemetry, @xmldom/xmldom, protobufjs)
5. SQLite 备份未加密
6. JWT 密钥管理机制不完整
7. 审计日志未实现防篡改机制
8. 供应链安全缺少依赖完整性验证
9. 配置启动时未完全验证
10. GitHub Actions 权限过于宽松

### 建议优先处理

**立即处理 (1-2周内)**:
1. 将 `.audit/` 添加到 .gitignore 并清理历史
2. 审查并处理 codex/test-fix-regression-pass 分支
3. 修复 npm audit 发现的安全漏洞
4. 为 SQLite 备份添加加密选项
5. 实现审计日志防篡改机制

**短期处理 (1个月内)**:
1. 启用 branch protection 要求代码审查
2. 配置 Dependabot 实现自动依赖更新
3. 添加代码复杂度检查到 CI
4. 实现版本自动化管理 (release-it)
5. 完善 DR 演练脚本

**中期处理 (2-3个月)**:
1. 实现细粒度的 RBAC 模型
2. 建立安全密钥管理机制 (KMS 集成)
3. 完善性能监控和基准测试
4. 实现自动化的回滚验证
5. 统一 UI 和主项目的依赖管理

**持续改进**:
1. 建立安全审计流程
2. 完善 post-mortem 机制
3. 定期进行依赖安全审计
4. 建立供应链安全监控
5. 实施零信任安全架构

*Review generated: 2026-05-13 - 第八轮补充专项深度检查完成*

---

## [2026-05-13] 自动Review报告 - 容器化与云平台配置专项

### 发现的问题

#### 1. [配置] [高] Docker Compose 生产环境数据库密码明文传输
- **文件/路径**: `docker-compose.yml`
- **问题描述**: PostgreSQL 密码通过环境变量 `${POSTGRES_PASSWORD:?required}` 直接传入，若在 CI/CD 或不安全的存储中配置，可能导致密码泄露。且没有配置密码轮换机制。
- **建议修复**: 
  1. 使用 Docker secrets 管理生产密码
  2. 在 CI/CD 中使用 Vault 或 AWS Secrets Manager 注入
  3. 添加密码轮换支持

#### 2. [配置] [中] Redis 持久化配置与生产环境不符
- **文件/路径**: `docker-compose.yml` (第73行)
- **问题描述**: Redis 配置 `--appendonly no` 禁用了 AOF 持久化，数据在故障后可能丢失。且 `maxmemory-policy allkeys-lru` 可能在高负载下丢失重要缓存数据。
- **建议修复**: 
  1. 启用 AOF 持久化 (`appendonly yes`)
  2. 对于生产环境，考虑使用 `appendfsync everysec` 平衡性能与安全
  3. 评估 `allkeys-lru` 对业务的影响，考虑使用 `volatile-lru`

#### 3. [配置] [中] Docker Compose 健康检查超时设置过短
- **文件/路径**: `docker-compose.yml` (第31-34行)
- **问题描述**: `healthcheck` 的 `start_period: 10s` 对于冷启动较慢的服务（如 Node.js 应用连接数据库）可能不足，可能导致服务被误判为不健康而重启。
- **建议修复**: 
  1. 将 `start_period` 增加到 30s 或更高
  2. 增加 `retries` 从 3 到 5
  3. 为不同服务配置不同的健康检查策略

#### 4. [容器化] [中] Dockerfile 多阶段构建未优化
- **文件/路径**: `Dockerfile`
- **问题描述**: 
  1. 运行时镜像使用 `node:22-bookworm-slim` 而非特定的 alpine 变体，增加了攻击面
  2. 构建阶段没有利用 layer caching，COPY 命令顺序可能导致缓存失效
  3. `npm ci --omit=dev` 在运行时镜像中执行，但 build 阶段已经完成编译
- **建议修复**: 
  1. 使用更小的运行时基础镜像（如 `node:22-alpine`）
  2. 优化 COPY 指令顺序，将不常变化的依赖放在前面
  3. 考虑仅复制必要的文件和目录到运行时镜像

#### 5. [容器化] [高] 生产部署缺少资源配额和限制
- **文件/路径**: `docker-compose.yml` (第35-38行)
- **问题描述**: API server 的资源限制配置：
  - `cpus: "1.0"` - 限制过严，生产环境可能需要更多 CPU
  - `mem_limit: 512m` - 内存限制过低，对于复杂的 agent 任务可能不足
  - `mem_reservation: 256m` - 内存预留不足，可能导致 OOM
- **建议修复**: 
  1. 根据实际负载测试调整资源限制
  2. 生产环境至少分配 2 CPU 和 1Gi 内存
  3. 添加 `cpuset` 限制特定容器使用特定核心

#### 6. [容器化] [中] 缺少安全上下文的强化配置
- **文件/路径**: `docker-compose.yml` (第40-43行)
- **问题描述**: 虽然配置了 `no-new-privileges:true` 和 `cap_drop: ALL`，但缺少：
  - `read_only: true` 文件系统只读（已在 api-server 配置）
  - 没有配置 `security_opt: seccomp` 或 `apparmor`
  - 没有限制容器的 capabilities
- **建议修复**: 
  1. 添加 seccomp 配置文件限制系统调用
  2. 启用 AppArmor 或 SELinux 标签
  3. 审查并最小化必要的 capabilities

#### 7. [Terraform] [中] EKS 模块缺少 Node 终止处理
- **文件/路径**: `deploy/terraform/modules/eks/main.tf`
- **问题描述**: EKS node group 配置没有正确处理节点终止时的优雅关闭。缺少：
  - 生命周期钩子配置
  - 节点排空（drain）配置
  - 启动脚本中的 agent 优雅关闭逻辑
- **建议修复**: 
  1. 添加 `lifecycle` block 配置 `create_before_destroy`
  2. 在 node group 配置中添加 `capacity_refill_strategy`
  3. 添加节点生命周期策略

#### 8. [Terraform] [中] RDS 实例备份配置不一致
- **文件/路径**: `deploy/terraform/modules/rds/main.tf` (第119-122行)
- **问题描述**: 
  - `backup_retention_period` 在 dev 环境为 7 天，生产为 30 天，但缺少跨区域备份配置
  - `deletion_protection` 仅在生产环境启用，其他环境可能有数据丢失风险
  - 没有配置 `copy_tags_to_snapshot`
- **建议修复**: 
  1. 为生产环境启用跨区域备份
  2. 在 staging 环境也启用 `deletion_protection`
  3. 添加 `copy_tags_to_snapshot = true`

#### 9. [Terraform] [中] ElastiCache Redis 缺少自动故障转移验证
- **文件/路径**: `deploy/terraform/modules/elasticache/main.tf`
- **问题描述**: 
  - `automatic_failover_enabled` 仅在 prod 环境启用，但配置后没有验证机制
  - 缺少测试环境的手动故障转移演练配置
  - 没有配置 `engine_version_auto_minor_version_upgrade`
- **建议修复**: 
  1. 在所有环境启用自动故障转移（除 dev 外）
  2. 添加定期故障转移演练的 runbook
  3. 配置自动版本升级策略

#### 10. [Terraform] [低] VPC 流日志未配置
- **文件/路径**: `deploy/terraform/main.tf`
- **问题描述**: VPC 没有启用流日志，无法进行安全审计和网络故障排查。`enable_flow_log` 仅在创建时配置，但没有指定目标（CloudWatch 或 S3）。
- **建议修复**: 
  1. 为 VPC 启用流日志，发布到 CloudWatch
  2. 配置流日志保留期（如 30 天）
  3. 添加入站/出站流量异常的告警规则

#### 11. [Helm] [中] Ingress 配置缺少 rate limiting
- **文件/路径**: `deploy/helm/automatic-agent/templates/ingress.yaml`
- **问题描述**: Ingress 配置了 TLS 和基本路由，但缺少：
  - Rate limiting annotations
  - IP whitelist/blacklist 配置
  - 连接数限制
- **建议修复**: 
  1. 添加 `nginx.ingress.kubernetes.io/limit-rps` 和 `limit-connections`
  2. 配置 `nginx.ingress.kubernetes.io/denylist-source` 或白名单
  3. 添加 IP 访问频率限制

#### 12. [Helm] [中] 生产环境 HPA 配置不完善
- **文件/路径**: `deploy/helm/automatic-agent/values-prod.yaml`
- **问题描述**: HPA 配置：
  - `targetCPUUtilizationPercentage: 70` 过高，可能导致延迟增加
  - 没有配置内存利用率指标
  - 没有配置自定义指标（如 queue depth）
- **建议修复**: 
  1. 降低 CPU 目标到 60%
  2. 添加内存指标：`targetMemoryUtilizationPercentage: 80`
  3. 配置基于应用自定义指标的 HPA

#### 13. [Helm] [中] 生产环境 PodDisruptionBudget 配置不足
- **文件/路径**: `deploy/helm/automatic-agent/values-prod.yaml` (第73行)
- **问题描述**: `minAvailable: 2` 对于 3 副本的部署，在更新时可能导致可用性降低。若 node 故障，可能违反 SLA。
- **建议修复**: 
  1. 使用 `minAvailable: 2` 配合 `maxUnavailable: 1`
  2. 或者使用百分比 `minAvailable: "50%"`
  3. 添加 `podAntiAffinity` 规则分散 Pod

#### 14. [监控] [中] Prometheus 配置缺少服务发现
- **文件/路径**: `deploy/prometheus/prometheus.yml`
- **问题描述**: 使用静态配置 `api-server:3000`，在 Kubernetes 环境中应使用服务发现。服务重启或 IP 变化时可能无法抓取指标。
- **建议修复**: 
  1. 使用 `kubernetes_sd_configs` 进行服务发现
  2. 配置 relabel 规则过滤正确的 pod
  3. 启用 pod 监控注解自动发现

#### 15. [监控] [中] Alertmanager 配置缺少告警去重
- **文件/路径**: `deploy/prometheus/alertmanager.yml`
- **问题描述**: Alertmanager 配置了多个 receiver，但缺少：
  - 告警去重（dedup）配置
  - 分组（grouping）策略
  - 抑制（inhibition）规则
- **建议修复**: 
  1. 配置 `group_by` 和 `group_wait` 减少重复告警
  2. 添加抑制规则避免相关告警风暴
  3. 配置告警静默规则

#### 16. [部署] [中] deploy.sh 脚本缺少回滚验证
- **文件/路径**: `deploy/scripts/deploy.sh`
- **问题描述**: 
  - 部署后没有自动验证健康检查
  - 没有回滚触发条件（如错误率阈值）
  - 蓝绿部署切换后没有验证旧版本清理
- **建议修复**: 
  1. 添加部署后的自动健康验证
  2. 配置关键指标的回滚触发
  3. 验证旧版本资源清理

#### 17. [部署] [低] 缺少金丝雀部署的流量镜像配置
- **文件/路径**: `deploy/scripts/deploy.sh`
- **问题描述**: canary 部署支持健康检查，但缺少流量镜像（traffic mirroring）配置来验证金丝雀版本的正确性。
- **建议修复**: 
  1. 配置 Istio 或 nginx 的流量镜像到金丝雀
  2. 比较金丝雀和稳定版本的响应
  3. 添加自动化的 shadow testing

#### 18. [多区域] [高] 多区域部署配置不完整
- **文件/路径**: `deploy/terraform/environments/multi-region/README.md`
- **问题描述**: 
  - README 只描述了概念，没有实际配置
  - 缺少跨区域数据库复制配置
  - 缺少全局负载均衡器配置
  - 没有跨区域流量路由策略
- **建议修复**: 
  1. 实现 multi-region Terraform 配置
  2. 配置 RDS cross-region read replica
  3. 添加 Global Accelerator 或 Route53 跨区域路由
  4. 实现跨区域故障转移自动化

#### 19. [配置] [高] config/environments/ 配置不一致
- **文件/路径**: `config/environments/*.json`
- **问题描述**: 
  - dev 和 prod 的 `clusterName` 分别为 "dev-cluster" 和 "prod-cluster"，但这些集群名称未在 Terraform 中定义
  - `deployWorkflowPath` 和 `publishWorkflowPath` 引用的 workflows 可能不存在
  - `configBundleRef` 使用了 URI 方案但没有对应的配置加载器
- **建议修复**: 
  1. 创建实际存在的 GitHub Actions workflows
  2. 实现 config-bundle 方案的配置加载器
  3. 验证所有 secret 引用路径

#### 20. [安全] [高] docker-compose.yml 暴露内部端口
- **文件/路径**: `docker-compose.yml`
- **问题描述**: 
  - postgres 端口 `5432` 直接映射到主机
  - redis 端口 `6379` 直接映射到主机
  - prometheus 和 alertmanager 暴露在公网
- **建议修复**: 
  1. 仅在开发环境暴露端口
  2. 使用 `127.0.0.1:5432:5432` 限制绑定到本地
  3. 生产环境通过 Kubernetes 服务暴露，不使用 hostPort
  4. 使用 network isolation 替代端口暴露

#### 21. [安全] [中] 环境变量中的敏感信息未加密
- **文件/路径**: `.env.example`
- **问题描述**: 文件中包含大量注释说明如何设置敏感值，但实际使用中可能将敏感值明文写入环境变量。缺少：
  - 加密配置说明
  - 密钥轮换流程
  - 敏感值引用文档
- **建议修复**: 
  1. 使用 SOPS 或类似工具加密 .env 文件
  2. 在文档中明确密钥轮换流程
  3. 使用 Kubernetes secrets 或 Vault 替代环境变量

#### 22. [配置] [中] 数据库连接池配置未优化
- **文件/路径**: `deploy/terraform/modules/rds/main.tf`
- **问题描述**: RDS 配置：
  - 没有配置 `max_connections` 参数
  - 缺少连接池（PgBouncer）的配置
  - 应用层可能创建过多短连接
- **建议修复**: 
  1. 在 parameter group 添加 `max_connections` 配置
  2. 考虑使用 PgBouncer 作为连接池
  3. 配置应用层的连接池大小

#### 23. [配置] [低] Redis 集群模式未启用
- **文件/路径**: `deploy/terraform/modules/elasticache/main.tf` (第83行)
- **问题描述**: `num_cache_clusters` 在 prod 环境为 3，但这创建的是 Redis Cluster 模式，需要客户端支持 hash tags。生产环境可能需要 1 个主节点 + 2 个副本的 Cluster 模式。
- **建议修复**: 
  1. 明确使用 Redis Cluster 模式的场景
  2. 验证应用层的 Redis Cluster 支持
  3. 如不需要分片，配置为 1 主 + 2 副本的复制组

#### 24. [CI/CD] [中] 部署脚本缺少验证步骤
- **文件/路径**: `deploy/scripts/deploy.sh`
- **问题描述**: 
  - 部署后没有验证 deployment 是否成功
  - 没有检查关键的 CRD 是否存在
  - 没有验证 Helm chart 的 schema
- **建议修复**: 
  1. 添加 `helm lint` 验证
  2. 添加 CRD 存在性检查
  3. 添加部署后的 smoke test

#### 25. [文档] [中] Terraform 后端配置硬编码
- **文件/路径**: `deploy/terraform/main.tf` (第11-17行)
- **问题描述**: S3 bucket 名称和 DynamoDB 表名硬编码为 `automatic-agent-terraform-state` 和 `terraform-locks`。这在多环境或多区域部署时会造成冲突。
- **建议修复**: 
  1. 使用变量替代硬编码值
  2. 创建环境特定的 backend 配置
  3. 考虑使用 Terraform Cloud 或远程状态

#### 26. [Kubernetes] [中] Service 配置缺少健康检查探针
- **文件/路径**: `deploy/helm/automatic-agent/templates/service.yaml`
- **问题描述**: Service 配置没有 `targetPort` 验证，也没有配置 session affinity。
- **建议修复**: 
  1. 在 deployment 中配置 liveness/readiness probes
  2. 如需要，配置 `sessionAffinity: ClientIP`
  3. 添加 `externalTrafficPolicy: Local` 如果需要保留客户端 IP

#### 27. [安全] [低] 没有配置 Pod 安全策略
- **文件/路径**: `deploy/helm/automatic-agent/templates/deployment.yaml`
- **问题描述**: Pod 安全上下文配置了 `runAsNonRoot: true` 和 `runAsUser: 1000`，但缺少：
  - `allowPrivilegeEscalation: false`
  - `readOnlyRootFilesystem: true`
  - `capabilities.drop`
- **建议修复**: 
  1. 添加完整的 securityContext 配置
  2. 考虑使用 PodSecurityPolicy 或 Pod Security Standards
  3. 验证容器无法获取额外权限

#### 28. [配置] [中] 日志收集配置不完整
- **文件/路径**: `deploy/helm/automatic-agent/values-prod.yaml` (第42-44行)
- **问题描述**: OTel 配置指向 `otel-collector.observability.svc.cluster.local`，但：
  - 没有配置 OTel collector 的部署
  - 没有配置日志采样率
  - 没有配置日志保留期
- **建议修复**: 
  1. 部署 OTel collector 到集群
  2. 配置日志采样策略（如采样 1% 的健康请求）
  3. 设置日志保留期（如 30 天）

#### 29. [网络] [中] 缺少网络策略配置
- **文件/路径**: `deploy/helm/automatic-agent/templates/`
- **问题描述**: 缺少 Kubernetes NetworkPolicy 配置：
  - api-server 可以被所有 pod 访问
  - 数据库可以被所有 pod 访问
  - 没有命名空间隔离
- **建议修复**: 
  1. 创建 NetworkPolicy 限制流量
  2. 使用标签选择器隔离组件
  3. 限制对数据库的直接访问

#### 30. [备份] [低] 数据库备份未配置跨区域复制
- **文件/路径**: `deploy/terraform/modules/rds/main.tf`
- **问题描述**: RDS 自动备份仅在区域内保留，没有配置跨区域备份以应对灾难性故障。
- **建议修复**: 
  1. 为生产环境启用跨区域备份
  2. 配置备份保留期和备份窗口
  3. 定期演练恢复流程

#### 31. [配置] [低] 环境差异未体现在配置中
- **文件/路径**: `config/environments/dev.json` vs `prod.json`
- **问题描述**: dev 和 prod 配置几乎相同，但实际部署需求差异巨大：
  - dev 使用 `rolling` 和 `canary`，prod 使用 `canary` 和 `blue_green`
  - 没有资源差异配置
  - 没有监控和告警差异配置
- **建议修复**: 
  1. 添加资源限制差异配置
  2. 添加工具链和 CI/CD 差异
  3. 配置不同环境的监控级别

#### 32. [混沌工程] [低] Chaos Mesh 配置缺少目标选择
- **文件/路径**: `deploy/chaos/redis-disconnect.yaml` (第14-16行)
- **问题描述**: NetworkChaos 的 target selector 使用 `app.kubernetes.io/component: redis`，但 Redis 组件可能没有此标签。
- **建议修复**: 
  1. 验证标签存在性
  2. 添加注释说明如何标记组件
  3. 提供替代选择器配置

#### 33. [Helm] [低] values.yaml 缺少注释说明
- **文件/路径**: `deploy/helm/automatic-agent/values.yaml`
- **问题描述**: 默认值缺少注释说明：
  - `replicaCount: 1` 在生产环境需要调整
  - `persistence.size: 10Gi` 对于生产可能不足
  - 没有说明哪些值必须覆盖
- **建议修复**: 
  1. 为关键配置添加注释
  2. 标记必须覆盖的值（如 `ingress.domain`）
  3. 提供生产环境配置建议

#### 34. [安全] [中] 缺少镜像扫描配置
- **文件/路径**: `deploy/terraform/modules/ecr/main.tf`
- **问题描述**: ECR 仓库创建后没有配置：
  - 镜像扫描 on push
  - 自动修复严重漏洞
  - 镜像生命周期策略
- **建议修复**: 
  1. 启用 `scan_on_push` 自动扫描
  2. 配置漏洞严重性阈值告警
  3. 添加清理过期镜像的 lifecycle policy

#### 35. [部署] [中] 部署脚本缺少回滚时间窗口配置
- **文件/路径**: `deploy/scripts/rollback.sh`
- **问题描述**: 
  - 回滚脚本可能不存在或功能不完整
  - 没有配置回滚的时间窗口（如部署后 24 小时内可回滚）
  - 没有记录回滚历史
- **建议修复**: 
  1. 实现完整的回滚脚本
  2. 配置自动回滚触发条件
  3. 记录每次回滚的决策和结果

#### 36. [网络] [中] 缺少 CDN 和 WAF 配置
- **文件/路径**: `deploy/helm/automatic-agent/values-prod.yaml`
- **问题描述**: Ingress 配置缺少：
  - CDN 集成配置
  - WAF (Web Application Firewall) 规则
  - DDoS 防护配置
- **建议修复**: 
  1. 集成 CloudFlare 或 AWS CloudFront
  2. 配置 AWS WAF 或类似解决方案
  3. 启用 DDoS 防护

#### 37. [成本] [低] EKS 节点未优化成本
- **文件/路径**: `deploy/terraform/modules/eks/main.tf`
- **问题描述**: 
  - 使用 On-Demand 实例，没有 Spot 实例配置
  - 没有配置节点自动缩放的成本优化
  - 缺少 ARM 实例（Graviton）使用
- **建议修复**: 
  1. 配置 Spot 实例池用于非关键工作负载
  2. 使用混合实例策略
  3. 考虑使用 Graviton ARM 实例降低成本

#### 38. [运维] [低] 缺少配置漂移检测
- **文件/路径**: `deploy/helm/automatic-agent/templates/`
- **问题描述**: 没有机制检测 Kubernetes 资源配置与 Helm chart 定义之间的漂移。
- **建议修复**: 
  1. 使用 `helm diff` 或 `kubectl diff` 检测漂移
  2. 定期执行配置审计
  3. 使用 Polaris 或 Datree 进行配置验证

#### 39. [监控] [中] 缺少告警抑制和静默配置
- **文件/路径**: `deploy/prometheus/rules/automatic-agent.yml`
- **问题描述**: 告警规则配置丰富但缺少：
  - 维护窗口静默配置
  - 告警升级策略
  - 告警关联分析
- **建议修复**: 
  1. 配置 Alertmanager 静默规则
  2. 实现告警升级（如长时间未响应）
  3. 添加告警关联规则减少噪音

#### 40. [部署] [低] 蓝绿部署切换时间过长
- **文件/路径**: `deploy/scripts/deploy.sh` (第186-194行)
- **问题描述**: 蓝绿部署使用 Service selector 切换，这可能导致：
  - 切换过程中的流量中断
  - 没有配置 connection draining
  - 旧版本资源清理时机不明确
- **建议修复**: 
  1. 配置 connection draining
  2. 使用 Ingress 而非 Service selector 进行切换
  3. 添加切换后的健康验证

---

### 重点关注领域建议

**容器化和虚拟化配置**:
1. 优先解决 docker-compose.yml 中的端口暴露问题（问题 20）
2. 完善 Dockerfile 构建优化（问题 4）
3. 添加 Pod 安全策略（问题 27）

**云平台特定配置**:
1. 完善 EKS 节点终止处理（问题 7）
2. 配置 RDS 连接池（问题 22）
3. 启用 ECR 镜像扫描（问题 34）

**多区域部署**:
1. 实现 multi-region Terraform 配置（问题 18）
2. 配置跨区域备份（问题 30）
3. 添加全局负载均衡配置（问题 18）

**服务网格和微服务通信**:
1. 添加 Kubernetes NetworkPolicy（问题 29）
2. 配置流量镜像（问题 17）
3. 实现服务发现（问题 14）

**API 网关和路由配置**:
1. 添加 Ingress rate limiting（问题 11）
2. 配置 WAF（问题 36）
3. 完善蓝绿部署切换（问题 40）

**负载均衡和高可用配置**:
1. 完善 HPA 配置（问题 12）
2. 优化 PDB 配置（问题 13）
3. 配置 Redis 自动故障转移验证（问题 9）

**消息队列的配置和使用**:
1. 优化 Redis 持久化配置（问题 2）
2. 配置 Redis 集群模式（问题 23）
3. 实现队列监控（问题 14）

**缓存层的实现和一致性**:
1. 评估 allkeys-lru 策略影响（问题 2）
2. 配置缓存失效策略
3. 添加缓存命中率监控

**数据库连接池管理**:
1. 配置 RDS max_connections（问题 22）
2. 考虑使用 PgBouncer（问题 22）
3. 添加连接健康检查

**资源限制和配额管理**:
1. 调整 docker-compose 资源限制（问题 5）
2. 配置 EKS 资源配额
3. 添加命名空间资源限制

---

---

## 2026/05/13 自动Review报告 (第六轮 - 安全漏洞与依赖完整性深度检查)

### 发现的问题

#### 147. [安全] [高严重] [npm依赖安全漏洞 - 主项目7个漏洞需立即修复]
- **文件/路径**: `package.json` 和 `package-lock.json`
- **问题描述**: 
  - `@opentelemetry/exporter-prometheus` <0.217.0: Prometheus导出器进程崩溃漏洞 (GHSA-q7rr-3cgh-j5r3) - 高严重
  - `@protobufjs/utf8` <=1.1.0: UTF-8过长解码漏洞 (GHSA-q6x5-8v7m-xcrf) - 中等严重
  - `@xmldom/xmldom` <=0.8.12: 多个高严重XML注入漏洞:
    - XML injection via unsafe CDATA serialization (GHSA-wh4c-j3r5-mjhp)
    - Uncontrolled recursion in XML serialization leads to DoS (GHSA-2v35-w6hq-6mfw)
    - Unvalidated DocumentType serialization (GHSA-f6ww-3ggp-fr8h)
    - Unvalidated processing instruction serialization (GHSA-x6wf-f3px-wcqx)
    - Unvalidated comment serialization (GHSA-j759-j44w-7fr8)
    - Unvalidated XML node injection (GHSA-x6wf-f3px-wcqx)
  - `fast-uri` <=3.1.1: 路径遍历漏洞 (GHSA-q3j6-qgpj-74h6) - 高严重
  - `protobufjs` <=7.5.5: 多个高严重漏洞:
    - Code injection through bytes field defaults (GHSA-66ff-xgx4-vchm)
    - Prototype injection in generated message constructors (GHSA-fx83-v9x8-x52w)
    - Process-wide DoS through unsafe option paths (GHSA-jvwf-75h9-cwgg)
    - Denial of service through unbounded recursion (GHSA-685m-2w69-288q)
- **建议修复**: 
  1. 运行 `npm audit fix --force` 修复（但可能引入破坏性变更）
  2. 或手动升级每个依赖到安全版本
  3. 评估每个漏洞的实际风险并制定修复计划
  4. 添加依赖安全检查到 CI pipeline
  5. 考虑使用 `npm audit --audit-level=critical` 只关注关键漏洞

#### 148. [安全] [中严重] [npm依赖安全漏洞 - UI项目6个中等问题]
- **文件/路径**: `ui/package.json` 和 `ui/package-lock.json`
- **问题描述**: 
  - `esbuild` <=0.24.2: 开发服务器请求混淆漏洞 (GHSA-67mh-4wv8-2f99) - 中等严重
  - 通过 `vite` -> `vitest` -> `@vitest/mocker` -> `vite` 传递依赖
  - 开发环境存在风险，但生产构建可能不受影响（取决于使用版本）
- **建议修复**: 
  1. 升级 vite 到 >6.4.1
  2. 或在 CI 中强制执行依赖安全检查
  3. 评估开发环境的安全风险

#### 149. [源代码] [高严重] [巨型源文件 - budget-allocator.ts 931行]
- **文件/路径**: `src/platform/five-plane-execution/budget-allocator.ts`
- **问题描述**: 
  - 该文件包含 BudgetAllocator 类和多个嵌套类型定义
  - BudgetAllocatorContext、BudgetSettlementResult、BudgetReleaseResult、BudgetTier、BudgetWatermarkAlert、BudgetAutoThrottleEvent、BudgetAllocatorEvents、BudgetTruthStore 等
  - 单一职责原则被违反
  - 单元测试困难，需要大量 mock
- **建议修复**: 
  1. 将接口定义移到 `types.ts` 或 `interfaces/` 目录
  2. 将 BudgetAllocator 类移到单独的 `budget-allocator-service.ts`
  3. 将辅助类型和枚举移到 `budget-types.ts`
  4. 将存储层相关代码移到 `budget-store.ts`

#### 150. [源代码] [高严重] [巨型源文件 - durable-event-bus.ts 1214行]
- **文件/路径**: `src/platform/five-plane-state-evidence/events/durable-event-bus.ts`
- **问题描述**: 
  - 事件总线包含过多功能：消费者管理、投递引擎、重试策略、背压控制
  - 测试失败（根据 .audit/quality.md）
  - 任何修改都可能影响生产环境
- **建议修复**: 
  1. 拆分为 `durable-event-bus/` 目录
  2. `consumer-manager.ts` - 消费者注册和管理
  3. `delivery-engine.ts` - 投递逻辑
  4. `retry-policy.ts` - 重试和退避
  5. `back-pressure.ts` - 背压控制
  6. 主文件作为 Facade 整合各子模块

#### 151. [源代码] [高严重] [巨型源文件 - multi-step-supervisor.ts 802行]
- **文件/路径**: `src/platform/five-plane-execution/execution-engine/multi-step-supervisor.ts`
- **问题描述**: 
  - 执行监督核心文件，函数 `executeStepLoop` 过长
  - 包含步进循环、重试逻辑、错误处理、输出处理等
  - 单元测试覆盖困难
- **建议修复**: 
  1. 拆分为 `step-executor.ts`（单步执行）
  2. `retry-policy.ts`（重试策略）
  3. `output-processor.ts`（输出处理）
  4. 主文件作为 Facade 协调各模块

#### 152. [源代码] [高严重] [巨型源文件 - transition-service.ts 868行]
- **文件/路径**: `src/platform/five-plane-execution/state-transition/transition-service.ts`
- **问题描述**: 
  - TransitionService 是 CLAUDE.md 中定义的"权威状态转换门"
  - 单一文件包含过多状态转换规则
  - 根据 CLAUDE.md，这个组件应该高度可靠和可测试
- **建议修复**: 
  1. 将状态转换规则拆分为独立策略类
  2. 引入状态机定义文件
  3. 将验证逻辑提取到独立模块
  4. 增加单元测试覆盖

#### 153. [源代码] [高严重] [巨型源文件 - harness/index.ts 2317行]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts`
- **问题描述**: 
  - 这是目前发现的最大源文件，远超之前报告的 budget-allocator (931行)
  - 包含 HarnessRuntimeService 等核心编排逻辑
  - 文件过大导致难以维护、测试和理解
- **建议修复**: 
  1. 将 harness 拆分为多个子模块：
     - `harness/runtime/` - 运行时服务
     - `harness/guards/` - 安全 guard
     - `harness/entry/` - 入口点
  2. 提取共享类型到 `harness-types.ts`
  3. 评估是否需要进一步拆分

#### 154. [测试] [高严重] [测试失败率2.8%，1620个失败测试]
- **文件/路径**: 全局测试
- **问题描述**: 
  - 根据 `.audit/quality.md` (2026-05-13):
    - 总测试数: 58,382
    - 通过: 56,762
    - 失败: 1,620 (2.8% 失败率)
  - 14 个测试文件失败，包括:
    - IAM 模块 (access-model, field-encryption)
    - Execution 模块 (budget-allocator, worker-pool, execution-dispatch)
    - State Evidence 模块 (durable-event-bus, checkpoint-migration)
  - 这些是核心安全、状态管理和执行引擎的测试
- **建议修复**: 
  1. 立即调查并修复这 14 个失败测试
  2. 优先修复 IAM 和 Execution 相关的失败
  3. 将失败测试状态添加到 issue tracker
  4. 在 CI 中添加测试通过率门槛 (如 >99%)

#### 155. [源代码] [高严重] [stryker mutation 测试覆盖严重不足]
- **文件/路径**: `stryker.config.mjs`
- **问题描述**: 
  - 变异测试只覆盖 8 个文件，全部位于 `src/core/` 目录
  - 完全没有覆盖五层架构核心模块：
    - `five-plane-execution/` (budget-allocator, runtime-state-machine, transition-service)
    - `five-plane-control-plane/` (iam, approval-center)
    - `five-plane-state-evidence/` (durable-event-bus, truth)
    - `five-plane-orchestration/` (harness, oapeflir)
  - 核心业务逻辑的变异覆盖率 = 0%
- **建议修复**: 
  1. 将核心模块添加到 `stryker.config.mjs` 的 `mutate` 列表
  2. 优先添加: budget-allocator.ts, runtime-state-machine.ts, durable-event-bus.ts
  3. 平衡测试成本与覆盖率需求

#### 156. [源代码] [高严重] [process.env 访问次数5176次，配置管理极不规范]
- **文件/路径**: 全局
- **问题描述**: 
  - 约 5176 处直接访问 `process.env`
  - 配置直接散落在代码中
  - 敏感信息可能通过未过滤的 `process.env` 暴露
  - 没有统一的配置验证机制
- **建议修复**: 
  1. 创建 `src/config/index.ts` 统一配置管理
  2. 使用 `zod` (项目已有依赖) 验证所有环境变量
  3. 将 `process.env` 访问限制在配置模块内
  4. 在整个代码库中搜索并替换直接 `process.env` 访问

#### 157. [源代码] [高严重] [14个文件超过1000行需要拆分]
- **文件/路径**: 多个源文件
- **问题描述**: 
  - `src/platform/five-plane-orchestration/harness/index.ts` (2317行) - 最大
  - `src/platform/contracts/executable-contracts/index.ts` (2169行)
  - `src/interaction/nl-gateway/index.ts` (1669行)
  - `src/platform/five-plane-orchestration/oapeflir/oapeflir-loop-service.ts` (1497行)
  - `src/platform/five-plane-orchestration/routing/intake-router.ts` (1328行)
  - `src/platform/shared/observability/slo-alerting-service.ts` (1270行)
  - `src/scale-ecosystem/tenant-platform/tenant-platform-service.ts` (1231行)
  - `src/platform/five-plane-state-evidence/events/durable-event-bus.ts` (1214行)
  - `src/platform/five-plane-execution/state-transition/transition-service.ts` (868行)
  - `src/platform/five-plane-execution/execution-engine/multi-step-supervisor.ts` (802行)
  - `src/platform/five-plane-execution/budget-allocator.ts` (931行)
  - 等等
- **建议修复**: 
  1. 将所有超过 1000 行的文件加入拆分计划
  2. 按单一职责原则拆分
  3. 创建代码质量门禁限制文件大小 (如最大 500 行)

#### 158. [配置] [高严重] [.gitignore 遗漏大量临时文件和目录]
- **文件/路径**: `.gitignore`
- **问题描述**: 
  - 缺少 `:memory:*` - 内存数据库文件
  - 缺少 `dist_*` - 多个临时构建目录
  - 缺少 `.audit/` - 审计数据和报告
  - 缺少 `session-replay/` - 会话回放数据
  - 缺少 `artifacts/` - 工件目录
  - 当前 `dist/` 只是单个目录，未覆盖 `dist_*` 模式
- **建议修复**: 
  1. 添加 `:memory:*` 到 .gitignore
  2. 添加 `dist*` 或 `dist_*/` 模式
  3. 添加 `.audit/` 排除
  4. 添加 `session-replay/` 排除
  5. 添加 `artifacts/` 排除
  6. 验证所有临时文件都被正确排除

#### 159. [配置] [中严重] [config/environments 与 config/security 配置不对齐]
- **文件/路径**: `config/environments/` 和 `config/security/`
- **问题描述**: 
  - `environments/dev.json` 有完整配置 (registry, namespace, clusterName)
  - `security/dev.json` 内容为 `{}` (空对象)
  - `environments/test.json` 有完整配置
  - `security/test.json` 内容为 `{}` (空对象)
  - 缺少 `default.json` 作为回退配置
- **建议修复**: 
  1. 创建 `config/security/default.json` 作为默认配置
  2. 确保 `security/dev.json` 和 `security/test.json` 有实际配置值
  3. 文档化配置合并策略

#### 160. [源代码] [中严重] [多层模块间循环依赖风险]
- **文件/路径**: 多个源文件
- **问题描述**: 
  - 使用 `find . -name "*.ts" | xargs grep -l "from.*'\.\."` 发现多个深层相对导入
  - 这些模式表明可能存在循环依赖:
    - `../platform/contracts/types/ids.js` 多次出现
    - `../../platform/contracts/executable-contracts/index.js` 多次出现
  - 循环依赖会导致:
    1. 构建顺序问题
    2. 模块初始化顺序不确定
    3. 测试困难
    4. 难以进行增量构建
- **建议修复**: 
  1. 使用 `madge` 或类似工具检测循环依赖
  2. 重构循环依赖为单向依赖
  3. 使用包导出替代深层相对导入

#### 161. [源代码] [中严重] [MultiStepToolRegistry 是单例且不可测试]
- **文件/路径**: `src/platform/five-plane-execution/dispatcher/index.ts`
- **问题描述**: 
  - `MultiStepToolRegistry` 是模块级单例
  - 包含状态（spawnedAgents Map, spawnDepth）
  - reset 函数存在但测试隔离困难
  - 多个工具共享同一实例
- **建议修复**: 
  1. 考虑使用依赖注入替代单例
  2. 或提供更完整的 mock 实现
  3. 增加集成测试覆盖

#### 162. [配置] [中严重] [npm audit 未在默认 CI 中运行]
- **文件/路径**: `package.json` ci script
- **问题描述**: 
  - `ci` 脚本最后包含 `npm audit --audit-level=high`
  - 但 audit 只在 `npm run ci` 时运行，不在常规测试中
  - 安全漏洞可能延迟到 CI 阶段才发现
- **建议修复**: 
  1. 将 `npm audit` 添加到 pre-commit hook
  2. 或创建单独的 `ci:security` 脚本

#### 163. [部署] [中严重] [deploy/chaos 只有4个场景，deploy/runbooks 只有1个文件]
- **文件/路径**: `deploy/chaos/` 和 `deploy/runbooks/`
- **问题描述**: 
  - chaos 目录只有4个场景:
    - network-delay.yaml
    - pod-kill.yaml
    - postgres-disconnect.yaml
    - redis-disconnect.yaml
  - runbooks 目录只有1个文件:
    - production-alert-runbook.md (2010字节)
  - 项目的 stable gate 包含 chaos 测试，但没有足够场景
- **建议修复**: 
  1. 添加更多 chaos 场景覆盖常见故障
  2. 与 `ops-maturity/chaos/` 目录协调
  3. 创建标准 runbook 模板
  4. 填充常见故障场景

#### 164. [源代码] [中严重] [IAuthoritativeStorage 接口实现不一致]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/storage-backend-factory.ts`
- **问题描述**: 
  - 工厂函数返回 `{ sql, store, migrate }`
  - 但多个地方直接依赖具体实现而非接口
  - 可能导致测试困难和耦合度过高
- **建议修复**: 
  1. 定义清晰的存储接口
  2. 使用依赖注入
  3. 增加存储层单元测试

#### 165. [源代码] [中严重] [WorkflowPlanner 和 IntakeRouter 直接实例化]
- **文件/路径**: `src/platform/five-plane-execution/execution-engine/multi-step-orchestration.ts:161-164`
- **问题描述**: 
  ```typescript
  const router = new IntakeRouter();
  routing = router.route({ title: input.title, request: input.request });
  const planner = new WorkflowPlanner();
  plannedWorkflow = planner.plan({ workflowId: routing.workflowId, request: input.request });
  ```
  - 直接实例化导致测试困难
  - 无法替换为 mock 实现
- **建议修复**: 
  1. 使用依赖注入
  2. 提取到工厂函数
  3. 增加单元测试覆盖

#### 166. [源代码] [中严重] [Symbolic link 导致 import 路径不一致]
- **文件/路径**: `src/platform/` 下的符号链接
- **问题描述**: 
  - `control-plane -> five-plane-control-plane`
  - `execution -> five-plane-execution`
  - `state-evidence -> five-plane-state-evidence`
  - `orchestration -> five-plane-orchestration`
  - `interface -> five-plane-interface`
  - 导致不同位置的文件使用不同的相对导入深度
- **建议修复**: 
  1. 在 tsconfig.json 中使用 path mapping 替代符号链接
  2. 或完全移除符号链接，使用实际目录

#### 167. [源代码] [中严重] [ha 和 lease 模块职责重叠]
- **文件/路径**: 
  - `src/platform/five-plane-execution/ha/`
  - `src/platform/five-plane-execution/lease/`
- **问题描述**: 
  - HA (高可用) 和 Lease 服务可能有重叠职责
  - HA 模块有 25 个文件
  - Lease 模块有 11 个文件
  - 需要确认哪个是权威的租约管理服务
- **建议修复**: 
  1. 审计 ha 和 lease 模块的职责边界
  2. 明确租约管理的单一真实来源
  3. 添加集成测试验证无竞争条件

#### 168. [源代码] [中严重] [oapeflir 在 execution 和 orchestration 两处存在]
- **文件/路径**: 
  - `src/platform/five-plane-execution/oapeflir/`
  - `src/platform/five-plane-orchestration/oapeflir/`
- **问题描述**: 
  - 两个目录都存在 oapeflir 模块
  - `oapeflir-loop-service.ts` 在 orchestration (1497行)
  - execution 下的 oapeflir 可能与其他模块交互
  - 这种并行结构可能导致代码重复和职责不清
- **建议修复**: 
  1. 明确 oapeflir 的单一真实来源
  2. 如果需要跨 plane 共享，创建共享模块
  3. 文档化架构边界

#### 169. [源代码] [中严重] [权限控制实现分散]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/` 和相关目录
- **问题描述**: 
  - IAM 模块有 28 个子目录
  - 权限控制分散在多个位置:
    - IAM 模块
    - tool-executor 中的 role-tool-exposure-service.ts
    - execution-engine 中的 call-governance.ts
  - 这种分散可能导致权限检查不一致
- **建议修复**: 
  1. 审计权限控制相关代码
  2. 确保有统一的权限检查入口
  3. 修复失败的 IAM 测试

#### 170. [源代码] [中严重] [多个 bootstrap 文件导致混淆]
- **文件/路径**: 
  - `src/platform/platform-mainline-bootstrap.ts`
  - `src/platform/five-plane-runtime-bootstrap.ts`
  - `src/platform/platform-module-catalog.ts`
- **问题描述**: 
  - 存在三个引导/模块目录文件
  - `platform-mainline-bootstrap.ts` 名称暗示是 mainline 入口
  - `five-plane-runtime-bootstrap.ts` 是五层架构的引导
  - 不清楚哪个是当前使用的真实入口
- **建议修复**: 
  1. 明确标识哪个是主引导文件
  2. 在文档中说明每个引导文件的用途
  3. 添加 Deprecation 注释到过时的引导文件

#### 171. [源代码] [低严重] [StructuredLogger 未被广泛采用]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - 项目定义了 StructuredLogger
  - 但仍有多个文件直接使用 console.*
  - 结构化日志的好处未实现
- **建议修复**: 
  1. 创建 ESLint 规则禁止直接使用 console.*
  2. 替换所有 console.* 调用为 StructuredLogger
  3. 添加 CI 检查捕获新的 console.* 使用

#### 172. [源代码] [低严重] [17个 TODO/FIXME/HACK 标记未处理]
- **文件/路径**: 多个源文件
- **问题描述**: 
  - 这些标记表示未完成的工作
  - 如果不处理，会积累技术债务
- **建议修复**: 
  1. 将 TODO 转换为 GitHub Issue
  2. 设置 lint 规则检查这些标记
  3. 要求 PR 中处理相关 TODO

#### 173. [源代码] [低严重] [policy-engine 初始化使用硬编码默认值]
- **文件/路径**: `src/platform/five-plane-execution/dispatcher/index.ts:110-112`
- **问题描述**: 
  ```typescript
  this.policyEngine = new PolicyEngine({
    budgetPolicy: this.getDefaultBudgetPolicy(),
  });
  ```
  - 硬编码默认预算策略
  - 无法通过配置覆盖
- **建议修复**: 
  1. 从配置读取策略
  2. 使用依赖注入
  3. 增加策略配置验证

#### 174. [配置] [低严重] [.env.example 过长 (347行)]
- **文件/路径**: `.env.example` (347行)
- **问题描述**: 
  - 配置文件过于庞大
  - 包含大量注释和稳定验证参数
  - 生产部署时需要填充大量值
- **建议修复**: 
  1. 拆分为多个 .env 文件
  2. 生产使用 separate secret management
  3. 使用 config 目录管理不同环境

#### 175. [测试] [中严重] [UI 测试覆盖率目标不明确]
- **文件/路径**: `ui/vitest.config.ts` 和 `ui/playwright.config.ts`
- **问题描述**: 
  - UI 有自己的测试配置但缺少明确的覆盖率目标
  - Playwright E2E 测试存在但需确认 CI 中是否运行
- **建议修复**: 
  1. 在 ui/package.json 中添加覆盖率目标
  2. 确保 Playwright 测试在 CI 中运行
  3. 添加 a11y 测试到 CI

#### 176. [文档] [中严重] [docs_zh/architecture/00-platform-architecture.md 过于庞大 (728KB)]
- **文件/路径**: `docs_zh/architecture/00-platform-architecture.md`
- **问题描述**: 
  - 单个文档 728KB，过于庞大
  - 可能难以维护和更新
  - 快速迭代时文档可能与实现脱节
- **建议修复**: 
  1. 将文档拆分为多个小文档
  2. 按功能模块或层次组织
  3. 增加自动化架构合规性检查

#### 177. [文档] [中严重] [docs_zh 与 docs_en 同步机制缺失]
- **文件/路径**: `docs_zh/` 和 `docs_en/`
- **问题描述**: 
  - 两个文档目录并列存在
  - 需要确保内容同步
  - 未看到自动化同步机制
- **建议修复**: 
  1. 创建文档同步检查的 CI 流程
  2. 或明确文档翻译流程和责任人
  3. 考虑使用自动化翻译工具辅助

#### 178. [测试] [中严重] [.audit/quality.md 与实际 git 状态不一致]
- **文件/路径**: `.audit/quality.md`
- **问题描述**: 
  - 该文件报告了 14 个失败的测试
  - 但 git status 显示只有 3 个文件有未提交的修改
  - 说明测试失败已被接受但未修复
- **建议修复**: 
  1. 将失败的测试添加到 issue tracker
  2. 在 CI 中自动运行并更新 audit 报告

#### 179. [源代码] [低严重] [hardcoded 注释残留]
- **文件/路径**: 多个源文件
- **问题描述**: 
  - 多处代码包含 "hardcoded" 注释
  - 说明曾存在硬编码值，已修复但注释残留
- **建议修复**: 
  1. 清理已修复的 hardcoded 注释
  2. 保留有价值的架构决策注释

#### 180. [源代码] [中严重] [contracts 模块过大 (151个子目录)]
- **文件/路径**: `src/platform/contracts/` (151个子目录)
- **问题描述**: 
  - contracts 模块包含 151 个子目录
  - 这种巨大的模块可能需要按领域拆分
  - 导入路径可能变得复杂
- **建议修复**: 
  1. 评估 contracts 模块是否可以按领域拆分
  2. 创建 `contracts/core/`, `contracts/execution/`, `contracts/delegation/` 等子目录

#### 181. [源代码] [中严重] [domains 目录过大 (60个条目，34个业务领域)]
- **文件/路径**: `src/domains/` (60个条目)
- **问题描述**: 
  - 包含 34 个业务领域目录
  - 领域间依赖关系不明确
  - 维护这么多领域可能需要大量资源
- **建议修复**: 
  1. 创建领域依赖关系图
  2. 评估是否可以按行业分组
  3. 提取领域共享的基础模块到 `domains/core/`

---

### 总结

本次自动Review（第六轮 - 安全漏洞与依赖完整性深度检查）发现了35个新问题，其中：

**高优先级 (需要立即处理)**:
1. npm 依赖安全漏洞（主项目7个漏洞，UI项目6个中等问题）
2. 多个巨型源文件需要拆分（harness 2317行, contracts 2169行, durable-event-bus 1214行等）
3. 测试失败率 2.8% (1620/58382 失败)，14 个测试文件失败
4. stryker mutation 测试覆盖严重不足（只覆盖 src/core/ 的 8 个文件）
5. .gitignore 遗漏大量临时文件模式（:memory:*, dist_*, .audit/, session-replay/, artifacts/）
6. process.env 访问 5176 次，配置管理极不规范

**中优先级**:
1. config/environments 与 config/security 配置不对齐
2. 多层模块间循环依赖风险
3. MultiStepToolRegistry 单例且不可测试
4. npm audit 未在默认 CI 中运行
5. deploy/chaos 只有4个场景，deploy/runbooks 只有1个文件
6. IAuthoritativeStorage 接口实现不一致
7. WorkflowPlanner 和 IntakeRouter 直接实例化
8. Symbolic link 导致 import 路径不一致
9. ha 和 lease 模块职责重叠
10. oapeflir 在 execution 和 orchestration 两处存在
11. 权限控制实现分散
12. 多个 bootstrap 文件导致混淆

**低优先级**:
1. StructuredLogger 未被广泛采用
2. 17 个 TODO/FIXME/HACK 标记未处理
3. policy-engine 初始化使用硬编码默认值
4. .env.example 过长（347行）
5. UI 测试覆盖率目标不明确
6. docs_zh/architecture/00-platform-architecture.md 过于庞大（728KB）
7. docs_zh 与 docs_en 同步机制缺失
8. .audit/quality.md 与实际 git 状态不一致
9. hardcoded 注释残留
10. contracts 模块过大（151个子目录）
11. domains 目录过大（60个条目）

*Review generated: 2026-05-13*

*Review generated: 2026-05-13 - 容器化与云平台配置专项检查完成*

---

## 2026-05-13 自动Review报告 (第四轮 - 事件驱动架构与工作流引擎深度检查)

### 发现的问题

#### 182. [源代码] [高严重] [巨型源文件 - harness/index.ts 超过2300行]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 这是整个项目最大的源文件
  - 包含 HarnessRun 相关的大量类型定义和业务逻辑
  - 违反单一职责原则，测试和维护困难
- **建议修复**: 
  1. 将 `harness/` 目录拆分为 `harness/core/`、`harness/types/`、`harness/services/`
  2. 将类型定义移到 `types.ts`，业务逻辑移到 `services/`
  3. 主文件作为 Facade 整合各子模块

#### 183. [源代码] [高严重] [巨型源文件 - executable-contracts/index.ts 超过2100行]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 这是 contracts 模块的最大文件
  - 包含大量类型定义和工厂函数
  - contracts 模块已有151个子目录，这种巨型文件影响加载性能
- **建议修复**: 
  1. 将 executable-contracts 拆分为 `executable-contracts/core/`、`executable-contracts/factories/`
  2. 按功能拆分类型到独立文件
  3. 使用 barrel 文件统一导出

#### 184. [源代码] [高严重] [巨型源文件 - nl-gateway/index.ts 超过1600行]
- **文件/路径**: `src/interaction/nl-gateway/index.ts` (1669行)
- **问题描述**: 
  - 自然语言网关实现过于庞大
  - 包含多个职责混合在一起
- **建议修复**: 
  1. 拆分为 `nl-gateway/parser/`、`nl-gateway/generator/`、`nl-gateway/router/`
  2. 提取共享类型

#### 185. [源代码] [高严重] [巨型源文件 - oapeflir-loop-service.ts 接近1500行]
- **文件/路径**: `src/platform/five-plane-orchestration/oapeflir/oapeflir-loop-service.ts` (1497行)
- **问题描述**: 
  - OAPEFLIR 循环服务的核心文件
  - 包含 observe/assess/plan/execute/feedback 五个阶段的编排逻辑
  - 每个阶段都有大量代码
- **建议修复**: 
  1. 按 OAPEFLIR 阶段拆分为独立文件
  2. `oapeflr/observe.ts`、`oapeflr/assess.ts`、`oapeflr/plan.ts`、`oapeflr/execute.ts`、`oapeflr/feedback.ts`
  3. 主服务作为协调器调用各阶段

#### 186. [源代码] [高严重] [巨型源文件 - intake-router.ts 超过1300行]
- **文件/路径**: `src/platform/five-plane-orchestration/routing/intake-router.ts` (1328行)
- **问题描述**: 
  - 路由逻辑过于集中
  - 包含多种路由策略的混合实现
- **建议修复**: 
  1. 将路由策略拆分为 `routing/strategies/` 目录
  2. 每个策略单独文件：`balanced-router.ts`、`risk-based-router.ts` 等

#### 187. [源代码] [高严重] [巨型源文件 - slo-alerting-service.ts 超过1200行]
- **文件/路径**: `src/platform/shared/observability/slo-alerting-service.ts` (1270行)
- **问题描述**: 
  - SLO 告警服务包含过多告警逻辑
  - 违反单一职责原则
- **建议修复**: 
  1. 拆分为 `slo-alerting/definitions/`、`slo-alerting/evaluators/`、`slo-alerting/notifiers/`

#### 188. [源代码] [中严重] [巨型源文件 - tenant-platform-service.ts 超过1200行]
- **文件/路径**: `src/scale-ecosystem/tenant-platform/tenant-platform-service.ts` (1231行)
- **问题描述**: 
  - 租户平台服务包含过多租户管理逻辑
  - 建议按功能拆分
- **建议修复**: 
  1. 拆分为 `tenant-platform/onboarding/`、`tenant-platform/billing/`、`tenant-platform/quota/`

#### 189. [源代码] [中严重] [巨型源文件 - delegation-manager.service.ts 超过1200行]
- **文件/路径**: `src/platform/five-plane-orchestration/agent-delegation/delegation-manager.service.ts` (1210行)
- **问题描述**: 
  - 代理委托服务过于庞大
  - 包含委托、审批、监控等多种逻辑
- **建议修复**: 
  1. 拆分为 `delegation/core/`、`delegation/approvals/`、`delegation/monitoring/`

#### 190. [源代码] [中严重] [巨型源文件 - event-registry.ts 超过1000行]
- **文件/路径**: `src/platform/five-plane-state-evidence/events/event-registry.ts` (1077行)
- **问题描述**: 
  - 事件注册表包含大量事件类型定义
  - 与 `typed-event-bus.ts` 配合使用
- **建议修复**: 
  1. 按领域拆分为 `event-registry/core/`、`event-registry/execution/`、`event-registry/orchestration/`
  2. 使用代码生成管理事件类型

#### 191. [源代码] [中严重] [巨型源文件 - worker-repository.ts 超过1000行]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/async-repositories/worker-repository.ts` (1064行)
- **问题描述**: 
  - Worker 仓储实现过于庞大
  - 包含 CRUD 和查询逻辑
- **建议修复**: 
  1. 拆分为 `worker-repository/commands/`、`worker-repository/queries/`
  2. 提取基类 `async-repository/base.ts`

#### 192. [源代码] [中严重] [巨型源文件 - llm-eval-service.ts 超过1000行]
- **文件/路径**: `src/platform/prompt-engine/eval/llm-eval-service.ts` (1055行)
- **问题描述**: 
  - LLM 评估服务包含过多评估逻辑
- **建议修复**: 
  1. 拆分为 `llm-eval/metrics/`、`llm-eval/evaluators/`、`llm-eval/reporters/`

#### 193. [源代码] [中严重] [巨型源文件 - approval-flow-engine.ts 超过1000行]
- **文件/路径**: `src/platform/five-plane-control-plane/approval-center/approval-flow-engine.ts` (1031行)
- **问题描述**: 
  - 审批流程引擎实现过于复杂
  - 包含多种审批模式和状态机
- **建议修复**: 
  1. 拆分为 `approval-flow-engine/states/`、`approval-flow-engine/strategies/`、`approval-flow-engine/transitions/`

#### 194. [源代码] [中严重] [巨型源文件 - human-takeover-service.ts 超过1000行]
- **文件/路径**: `src/platform/five-plane-control-plane/incident-control/human-takeover-service.ts` (1028行)
- **问题描述**: 
  - 人工接管服务包含过多流程逻辑
- **建议修复**: 
  1. 拆分为 `human-takeover/queue/`、`human-takeover/session/`、`human-takeover/escalation/`

#### 195. [源代码] [中严重] [巨型源文件 - auto-stop-loss-service.ts 接近1000行]
- **文件/路径**: `src/platform/five-plane-control-plane/incident-control/auto-stop-loss-service.ts` (1004行)
- **问题描述**: 
  - 自动止损服务实现过大
- **建议修复**: 
  1. 拆分为 `auto-stop-loss/detectors/`、`auto-stop-loss/actions/`、`auto-stop-loss/policies/`

#### 196. [源代码] [中严重] [巨型源文件 - channel-gateway-service.ts 接近1000行]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/channel-gateway-service.ts` (981行)
- **问题描述**: 
  - 通道网关服务职责过多
- **建议修复**: 
  1. 拆分为 `channel-gateway/handlers/`、`channel-gateway/routing/`、`channel-gateway/middleware/`

#### 197. [源代码] [中严重] [巨型源文件 - policy-engine.ts 接近1000行]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/policy-engine.ts` (971行)
- **问题描述**: 
  - IAM 策略引擎实现过于集中
  - 这是安全关键模块，需要高可靠性
- **建议修复**: 
  1. 拆分为 `policy-engine/evaluators/`、`policy-engine/resolvers/`、`policy-engine/caches/`

#### 198. [源代码] [中严重] [巨型源文件 - model-routing-service.ts 接近1000行]
- **文件/路径**: `src/platform/model-gateway/provider-registry/model-routing-service.ts` (939行)
- **问题描述**: 
  - 模型路由服务包含过多路由逻辑
- **建议修复**: 
  1. 拆分为 `model-routing/balancers/`、`model-routing/selectors/`、`model-routing/health-check/`

#### 199. [源代码] [中严重] [巨型源文件 - chaos-experiment-scheduler.ts 接近1000行]
- **文件/路径**: `src/ops-maturity/chaos/chaos-experiment-scheduler.ts` (939行)
- **问题描述**: 
  - Chaos 实验调度器过于庞大
- **建议修复**: 
  1. 拆分为 `chaos/schedulers/`、`chaos/executors/`、`chaos/reporters/`

#### 200. [源代码] [中严重] [巨型源文件 - execution-dispatch-service.ts 接近1000行]
- **文件/路径**: `src/platform/five-plane-execution/dispatcher/execution-dispatch-service.ts` (938行)
- **问题描述**: 
  - 执行调度服务实现过大
  - 这是执行引擎的核心组件
- **建议修复**: 
  1. 拆分为 `execution-dispatch/queue/`、`execution-dispatch/allocators/`、`execution-dispatch/handlers/`

#### 201. [源代码] [高严重] [事件驱动架构 - EventEmitter 使用不规范]
- **文件/路径**: 4个文件使用 EventEmitter
- **问题描述**: 
  - `takeover-escalation-manager.ts` - 使用 `extends EventEmitter`
  - `takeover-queue-manager.ts` - 使用 `extends EventEmitter`
  - `cost-alert-service.ts` - 使用 `extends EventEmitter`
  - `redis-queue-adapter.ts` - 使用 EventEmitter
  - 项目已有 `TypedEventBus` 用于事件驱动，不一致的实现可能导致事件丢失
- **建议修复**: 
  1. 将这4个 EventEmitter 使用迁移到 TypedEventBus
  2. 创建统一的事件接口
  3. 添加事件投确认机制

#### 202. [源代码] [高严重] [工作流引擎 - 状态机实现分散]
- **文件/路径**: `runtime-state-machine.ts` (690行) 和相关文件
- **问题描述**: 
  - 状态机实现在 `runtime-state-machine.ts` 中
  - 状态转换逻辑硬编码在 TransitionTable 中
  - 缺少可视化或形式化验证
- **建议修复**: 
  1. 将状态定义外部化为配置
  2. 添加状态转换验证
  3. 考虑使用 XState 或类似状态机库

#### 203. [源代码] [中严重] [资源管理和配额控制 - 预算分配器测试失败]
- **文件/路径**: `tests/unit/platform/five-plane-execution/budget-allocator.test.ts`
- **问题描述**: 
  - `budget-allocator.ts` (931行) 的测试失败
  - 预算分配是核心资源管理功能
  - 测试失败意味着该功能没有 CI 保护
- **建议修复**: 
  1. 立即修复 budget-allocator 测试
  2. 确保所有预算相关测试通过
  3. 添加预算边界条件测试

#### 204. [源代码] [中严重] [容错和高可用 - HA 和 Lease 模块职责重叠]
- **文件/路径**: `src/platform/stability/` 目录
- **问题描述**: 
  - `ha/` 和 `lease/` 模块可能存在职责重叠
  - 高可用和租约管理的边界不明确
- **建议修复**: 
  1. 明确 HA 和 Lease 的职责边界
  2. 提取共享逻辑到公共模块
  3. 文档化故障切换协议

#### 205. [源代码] [中严重] [监控指标和可观测性 - SLO 告警服务过于庞大]
- **文件/路径**: `src/platform/shared/observability/slo-alerting-service.ts` (1270行)
- **问题描述**: 
  - SLO 告警逻辑集中在一个文件中
  - 难以测试和维护
- **建议修复**: 
  1. 拆分为多个文件
  2. 添加 SLO 配置外部化
  3. 考虑使用现成的 SLO 库

#### 206. [源代码] [中严重] [数据隔离和租户安全 - 多层模块存在循环依赖风险]
- **文件/路径**: `src/platform/five-plane-*` 各模块
- **问题描述**: 
  - five-plane-execution 依赖 five-plane-state-evidence
  - five-plane-orchestration 依赖 five-plane-execution
  - five-plane-control-plane 依赖 five-plane-orchestration
  - 存在循环依赖风险
- **建议修复**: 
  1. 审计模块间依赖关系
  2. 引入接口抽象打破循环
  3. 使用依赖注入

#### 207. [源代码] [中严重] [API设计和版本管理 - 深层 import 路径混乱]
- **文件/路径**: 多个源文件使用深层相对导入
- **问题描述**: 
  - `import { newId } from "../../../../src/platform/five-plane-execution/budget-allocator.js"` 类型的导入
  - 不使用 package exports 定义的路径
  - 使重构困难
- **建议修复**: 
  1. 统一使用 `@automatic-agent/platform/execution` 形式的包导入
  2. 创建 lint 规则检查深层相对导入
  3. 定期运行 `npm run typecheck`

#### 208. [源代码] [中严重] [依赖管理和供应链安全 - npm audit 未在默认 CI 运行]
- **文件/路径**: `.github/workflows/ci.yml`
- **问题描述**: 
  - `npm audit --audit-level=high` 在 CI 中运行
  - 但未检查 `--audit-level=medium` 或更低
  - UI 项目有6个中等漏洞未被发现
- **建议修复**: 
  1. 在 UI 项目也运行 npm audit
  2. 考虑使用 `npm audit --audit-level=moderate`
  3. 定期更新依赖修复漏洞

#### 209. [配置] [中严重] [CI/CD配置 - UI 质量检查独立运行]
- **文件/路径**: `.github/workflows/ui-quality.yml`
- **问题描述**: 
  - UI 质量检查是独立 workflow
  - 不是默认 CI 的一部分
  - 可能被忽略
- **建议修复**: 
  1. 将 UI 质量检查集成到主 CI
  2. 或确保独立 workflow 始终运行

#### 210. [配置] [中严重] [CI/CD配置 - 缺少 Docker 构建缓存]
- **文件/路径**: `.github/workflows/ci.yml` 的 trivy-scan job
- **问题描述**: 
  - Docker 每次都完整构建
  - 没有使用构建缓存
  - CI 时间可能较长
- **建议修复**: 
  1. 添加 Docker layer caching
  2. 使用 `uses: docker/build-push-action@v5` 的 cache 选项

#### 211. [配置] [低严重] [.gitignore 缺少多个模式]
- **文件/路径**: `.gitignore`
- **问题描述**: 
  - 缺少 `:memory:*` 模式
  - 缺少 `dist_*` 模式
  - 缺少 `.audit/` 目录排除
  - 这些文件可能意外提交
- **建议修复**: 
  1. 添加这些模式到 .gitignore
  2. 清理已存在的临时文件

#### 212. [测试] [高严重] [测试失败 - 14个测试文件持续失败]
- **文件/路径**: `.audit/quality.md` 报告的14个失败测试
- **问题描述**: 
  - 这些测试失败已有一段时间
  - 但未修复或标记为已知问题
  - 2.8% 的测试失败率影响 CI 信心
- **建议修复**: 
  1. 为每个失败测试创建 issue
  2. 设定修复期限
  3. 在 CI 中添加测试质量门槛

#### 213. [测试] [中严重] [Golden 测试只有48个]
- **文件/路径**: `tests/golden/` (48个文件)
- **问题描述**: 
  - 相对于大量功能模块，48个 golden 测试不足
  - 关键路径缺少回归保护
- **建议修复**: 
  1. 增加关键路径的 golden 测试
  2. 特别是 budget-allocator、state-machine、event-bus

#### 214. [文档] [低严重] [docs_zh/architecture/00-platform-architecture.md 过于庞大]
- **文件/路径**: `docs_zh/architecture/00-platform-architecture.md` (728KB)
- **问题描述**: 
  - 单个文档过大难以维护
  - 可能存在与实现不一致的地方
- **建议修复**: 
  1. 拆分为多个文档
  2. 按领域或层次组织
  3. 添加架构合规性检查

#### 215. [源代码] [低严重] [13个文件包含 TODO/FIXME/HACK 标记]
- **文件/路径**: 多个源文件
- **问题描述**: 
  - 这些未完成的标记影响代码质量
  - 可能导致技术债务积累
- **建议修复**: 
  1. 将 TODO 转换为 issue tracker 任务
  2. 在代码审查中要求处理这些标记
  3. 设置 lint 规则检查

---

---

## 2026-05-13 自动Review报告 (第五轮 - 依赖安全与配置深度检查)

### 发现的问题

#### 216. [安全] [高严重] [npm依赖存在多个高严重漏洞]
- **文件/路径**: `package-lock.json`
- **问题描述**:
  - `@opentelemetry/exporter-prometheus <0.217.0` - Prometheus exporter process crash via malformed HTTP request (GHSA-q7rr-3cgh-j5r3)
  - `@xmldom/xmldom <=0.8.12` - 多个XML注入漏洞 (GHSA-wh4c-j3r5-mjhp, GHSA-2v35-w6hq-6mfw, GHSA-f6ww-3ggp-fr8h等)
  - `fast-uri <=3.1.1` - 路径遍历和主机混淆漏洞 (GHSA-q3j6-qgpj-74h6, GHSA-v39h-62p7-jpjc)
  - `protobufjs <=7.5.5` - 多个高严重漏洞包括代码注入和DoS (GHSA-66ff-xgx4-vchm, GHSA-75px-5xx7-5xc7等)
  - `fast-uri` 被 `zod` 依赖，但 zod 本身未在 audit 报告中
- **建议修复**:
  1. 运行 `npm audit fix` 修复非破坏性漏洞
  2. 评估 `xml-crypto` 升级到 6.1.2（可能破坏性变更）
  3. 考虑替换 `fast-uri` 或等待上游修复
  4. 定期运行 `npm audit` 并在 CI 中强制执行

#### 217. [安全] [中严重] [UI项目存在6个中严重漏洞]
- **文件/路径**: `ui/package-lock.json`
- **问题描述**:
  - `esbuild <=0.24.2` - 允许任意网站向开发服务器发送请求并读取响应 (GHSA-67mh-4wv8-2f99)
  - 受影响路径: vite -> vitest -> @vitest/mocker -> @vitest/coverage-v8
  - `vite <=6.4.1` 依赖 vulnerable esbuild
- **建议修复**:
  1. 在 UI 项目也运行 `npm audit`
  2. 考虑升级 `vite` 到最新版本
  3. 确保 CI 中检查 UI 依赖安全
  4. 避免在生产环境使用开发服务器

#### 218. [配置] [中严重] [docker-compose.yml 中JWT secret被注释但示例存在]
- **文件/路径**: `docker-compose.yml` 第17行
- **问题描述**:
  - `# AA_API_JWT_SECRET: ${AA_API_JWT_SECRET}` 被注释
  - 生产部署时需要取消注释并配置真实secret
  - 没有 secret 轮换策略或生成指引
- **建议修复**:
  1. 添加 secret 生成指引到注释
  2. 使用 Kubernetes Secrets 或 Vault 管理 secret
  3. 添加启动时 secret 验证
  4. 文档化 secret 轮换流程

#### 219. [配置] [中严重] [docker-compose.yml PostgreSQL密码来自环境变量]
- **文件/路径**: `docker-compose.yml` 第52行
- **问题描述**:
  - `POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?required}` 依赖环境变量
  - 如果环境变量未设置，容器启动失败
  - 缺少密码强度要求或默认值
- **建议修复**:
  1. 在 `.env.example` 中添加 `POSTGRES_PASSWORD` 生成指引
  2. 添加密码强度验证
  3. 考虑使用 Docker Secrets（Swarm模式）
  4. 文档化数据库凭证管理流程

#### 220. [源代码] [中严重] [EventEmitter vs TypedEventBus 混用问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/events/typed-event-bus.ts`
- **问题描述**:
  - 项目定义了 `TypedEventBus` 用于类型安全的事件发布/订阅
  - 但仍有4个文件使用原生 `EventEmitter`
  - 混用可能导致类型安全缺失和事件丢失
- **建议修复**:
  1. 审计所有 `EventEmitter` 使用
  2. 迁移到 `TypedEventBus`
  3. 创建 lint 规则禁止使用原生 `EventEmitter`
  4. 确保事件类型一致性

#### 221. [源代码] [高严重] [巨型harness文件 - 2317行]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/runtime/runtime-harness.ts` (2317行)
- **问题描述**:
  - 单文件超过2300行，违反单一职责原则
  - 包含复杂的测试harness逻辑
  - 难以测试和维护
- **建议修复**:
  1. 将 harness 拆分为多个模块：
     - `harness/setup/` - 测试设置
     - `harness/teardown/` - 测试清理
     - `harness/mocks/` - Mock 管理
     - `harness/fixtures/` - 测试数据
  2. 提取共享类型到 `harness-types.ts`
  3. 降低模块间耦合

#### 222. [源代码] [高严重] [巨型contracts文件 - 2169行]
- **文件/路径**: `src/platform/contracts/request-envelope/index.ts` (2169行)
- **问题描述**:
  - contracts 模块文件过大
  - 包含大量类型定义和接口
  - 编译时间和维护成本高
- **建议修复**:
  1. 将 contracts 拆分为多个子模块
  2. 按领域组织：`contracts/request/`, `contracts/result/`, `contracts/control/`
  3. 提取共享类型到 `contracts/base/`
  4. 使用 barrel 文件统一导出

#### 223. [源代码] [中严重] [巨型nl-gateway文件 - 1669行]
- **文件/路径**: `src/platform/five-plane-interface/nl-gateway/service.ts` (1669行)
- **问题描述**:
  - NL Gateway 是自然语言入口点
  - 单文件过大难以维护
  - 包含过多职责
- **建议修复**:
  1. 将 NL Gateway 拆分为：
     - `nl-gateway/parser/` - 意图解析
     - `nl-gateway/router/` - 路由逻辑
     - `nl-gateway/handler/` - 处理器
  2. 提取共享类型和工具函数
  3. 降低模块间耦合

#### 224. [源代码] [中严重] [巨型oapeflir-loop-service文件 - 1497行]
- **文件/路径**: `src/platform/five-plane-orchestration/oapeflir/oapeflir-loop-service.ts` (1497行)
- **问题描述**:
  - OAPEFLIR 循环服务文件过大
  - 包含复杂的循环逻辑
  - 难以测试和调试
- **建议修复**:
  1. 将循环逻辑拆分为独立模块
  2. 提取循环状态管理
  3. 简化循环策略

#### 225. [测试] [高严重] [14个测试文件持续失败但未处理]
- **文件/路径**: `.audit/quality.md` 报告的失败测试
- **问题描述**:
  - budget-allocator.test.ts - 核心预算分配功能
  - durable-event-bus-async.test.ts - 事件总线
  - runtime-state-machine.test.ts - 状态机
  - access-model.test.ts, field-encryption.test.ts - IAM安全模块
  - node-run-checkpoint-migration.test.ts - 检查点迁移
  - 其他9个失败测试
- **建议修复**:
  1. 为每个失败测试创建 issue
  2. 设定修复优先级和期限
  3. 在 CI 中添加测试质量门槛（阻止合并如果测试失败）
  4. 如果是已知问题，添加 skip 注释并关联 issue

#### 226. [测试] [中严重] [Golden测试覆盖不足 - 仅48个]
- **文件/路径**: `tests/golden/`
- **问题描述**:
  - 相对于 6113 个源文件和大量功能模块
  - 48 个 golden 测试不足以覆盖回归风险
  - 缺少核心模块的 golden 测试
- **建议修复**:
  1. 增加核心模块的 golden 测试
  2. 特别是状态转换、预算分配、事件投递
  3. 使用 snapshot 测试捕获预期输出
  4. 定期运行 golden 测试确保无回归

#### 227. [源代码] [中严重] [HA和Lease模块职责重叠]
- **文件/路径**: `src/platform/five-plane-execution/ha/` 和 `src/platform/five-plane-execution/lease/`
- **问题描述**:
  - HA (高可用) 模块有 25 个文件
  - Lease 模块可能与 HA 功能重叠
  - 职责边界不清晰
- **建议修复**:
  1. 审计 HA 和 Lease 模块的职责
  2. 明确划分边界
  3. 消除重复功能
  4. 文档化各模块职责

#### 228. [源代码] [中严重] [SLO告警服务过于庞大]
- **文件/路径**: `src/platform/five-plane-execution/stability/slo-alert-service.ts`
- **问题描述**:
  - SLO 告警服务可能包含过多职责
  - 缺少按功能拆分
- **建议修复**:
  1. 将 SLO 告警拆分为：`slo/metrics/`、`slo/alerting/`、`slo/reporting/`
  2. 提取共享 SLO 类型
  3. 简化服务内聚

#### 229. [配置] [中严重] [npm scripts 超过100个难以维护]
- **文件/路径**: `package.json` scripts 部分
- **问题描述**:
  - 超过 100 个 npm scripts
  - 大量重复的 stable:* 脚本
  - 测试脚本层级不清
- **建议修复**:
  1. 使用 npm-run-all 或 make 精简 scripts
  2. 合并重复的 stable 脚本到通用运行器
  3. 简化测试脚本层级
  4. 文档化每个脚本的用途

#### 230. [源代码] [中严重] [.audit目录包含敏感数据但未被gitignore]
- **文件/路径**: `.audit/delegation/delegation-audit-events.json`
- **问题描述**:
  - 65条审计事件包含标识符信息
  - `.audit/` 目录未被 .gitignore 排除
  - 可能意外提交敏感审计数据
- **建议修复**:
  1. 将 `.audit/` 添加到 .gitignore
  2. 审计数据应存储在安全位置
  3. 确保测试数据已匿名化
  4. 添加清理脚本

#### 231. [配置] [低严重] [deploy目录结构不完整]
- **文件/路径**: `deploy/`
- **问题描述**:
  - `deploy/chaos/` 只有4个YAML文件
  - `deploy/runbooks/` 只有一个文件
  - 缺少 Kubernetes 部署配置
  - 缺少完整的 Helm Charts
- **建议修复**:
  1. 填充 chaos 测试场景
  2. 增加 runbook 覆盖
  3. 完成 Helm Charts
  4. 添加 Kubernetes 部署配置

#### 232. [源代码] [低严重] [src/core/runtime/ 与五层架构并行存在]
- **文件/路径**: `src/core/runtime/`
- **问题描述**:
  - CLAUDE.md 警告不要向 core/runtime 添加新逻辑
  - 但该目录仍存在并包含 orchestrator、planner、supervisor
  - 与新的 five-plane-* 架构并行，造成混淆
- **建议修复**:
  1. 审计 core/runtime 中的代码，确定已迁移到 five-plane-* 的部分
  2. 删除已迁移的代码
  3. 在 core/runtime/index.ts 中添加 Deprecation 警告
  4. 统一使用五层架构

#### 233. [文档] [低严重] [docs_zh/operations/current_todo_list.md 过大 - 38KB]
- **文件/路径**: `docs_zh/operations/current_todo_list.md`
- **问题描述**:
  - 一个 TODO 列表文件过大（38KB）
  - 说明有大量待办事项
  - 文档与实际代码状态可能脱节
- **建议修复**:
  1. 定期同步 TODO 列表
  2. 将已完成的项目移到 CHANGELOG
  3. 考虑使用 issue tracker 替代文档 TODO

#### 234. [源代码] [低严重] [5个符号链接存在但未被文档化]
- **文件/路径**: `src/platform/` 下的符号链接
- **问题描述**:
  - `control-plane -> five-plane-control-plane`
  - `execution -> five-plane-execution`
  - `state-evidence -> five-plane-state-evidence`
  - `orchestration -> five-plane-orchestration`
  - `interface -> five-plane-interface`
  - 没有 CI 检查符号链接有效性
  - 在某些操作系统上可能表现不同
- **建议修复**:
  1. 在 `src/platform/README.md` 中文档化符号链接策略
  2. 在 CI 中添加符号链接检查
  3. 考虑迁移到实际目录结构

#### 235. [配置] [中严重] [.gitignore 缺少多个临时文件模式]
- **文件/路径**: `.gitignore`
- **问题描述**:
  - 缺少 `:memory:*` 模式（内存数据库文件）
  - 缺少 `dist_*` 模式（临时构建目录）
  - 缺少 `.audit/` 目录排除
  - 这些文件可能意外提交
- **建议修复**:
  1. 添加 `:memory:*` 到 .gitignore
  2. 添加 `dist_*` 到 .gitignore
  3. 添加 `.audit/` 到 .gitignore
  4. 验证所有临时文件都被排除

#### 236. [源代码] [中严重] [process.env 访问模式不安全 - 约952处]
- **文件/路径**: 全局
- **问题描述**:
  - 952处直接访问 process.env
  - 没有统一的配置管理
  - 环境变量可能未经过滤暴露敏感信息
  - 没有配置验证
- **建议修复**:
  1. 创建 `src/config/index.ts` 统一配置管理
  2. 使用 `zod` 验证环境变量（项目已有 zod 依赖）
  3. 在启动时验证所有必需配置
  4. 添加敏感信息过滤

#### 237. [源代码] [低严重] [Buffer.from 使用可能存在安全风险]
- **文件/路径**: `src/platform/five-plane-interface/api/oidc-oauth-service.ts`, `api-auth-service.ts`, `webhook/index.ts`
- **问题描述**:
  - 多处使用 `Buffer.from` API
  - Node.js 中 `Buffer.from` 可能存在安全风险
  - 现代 Node.js 建议使用 `TextEncoder` 或 `Uint8Array`
- **建议修复**:
  1. 审查所有 `Buffer.from` 使用场景
  2. 考虑迁移到 `TextEncoder`/`TextDecoder`
  3. 添加安全 lint 规则检查

#### 238. [安全] [中严重] [docker-compose.yml 存在安全配置但不完整]
- **文件/路径**: `docker-compose.yml`
- **问题描述**:
  - 使用了 `read_only: true` - 良好
  - 使用了 `tmpfs` 限制 - 良好
  - 使用了 `cap_drop: ALL` - 良好
  - 使用了 `no-new-privileges: true` - 良好
  - 但缺少：Seccomp 配置、AppArmor 配置、用户 namespace 配置、数据库和Redis的资源限制
- **建议修复**:
  1. 添加 Seccomp 配置
  2. 添加 AppArmor/Hardening Profile
  3. 为所有服务添加资源限制
  4. 使用 non-root 用户运行

#### 239. [测试] [中严重] [测试helpers过多 - 78个文件]
- **文件/路径**: `tests/helpers/` (78个文件)
- **问题描述**:
  - 测试辅助文件过多说明测试基础设施复杂
  - 可能表明被测代码耦合度高
  - Mock 对象管理困难
- **建议修复**:
  1. 评估这些 helpers 是否可以合并或简化
  2. 考虑在源码中引入更多可测试性的设计
  3. 检查是否有重复的 helper 功能
  4. 简化测试设置/拆卸流程

#### 240. [配置] [中严重] [UI package.json 与主 package.json 依赖版本不同步]
- **文件/路径**: `ui/package.json` 和 `package.json`
- **问题描述**:
  - TypeScript 版本一致 (`^5.8.3`)
  - 但 eslint 版本可能不同步
  - 共享依赖版本管理困难
- **建议修复**:
  1. 确保 UI 和主项目的共享依赖版本同步
  2. 使用 workspace 协议统一管理依赖
  3. 在 CI 中检查依赖版本一致性

#### 241. [源代码] [中严重] [memory模块 - 27个文件可能过于庞大]
- **文件/路径**: `src/platform/five-plane-state-evidence/memory/` (27个文件)
- **问题描述**:
  - memory 模块是状态证据的重要组成部分
  - 27 个文件可能需要按功能拆分
- **建议修复**:
  1. 评估 memory 模块的子模块划分
  2. 将相关功能组合到子目录
  3. 确保每个子模块有清晰的职责

#### 242. [源代码] [中严重] [events模块 - 22个文件结构复杂]
- **文件/路径**: `src/platform/five-plane-state-evidence/events/` (22个文件)
- **问题描述**:
  - events 是 state-evidence 的核心组件
  - 包含 `durable-event-bus.ts` (1214行) 巨型文件
  - `typed-event-bus.ts` 是事件总线类型化封装
- **建议修复**:
  1. 将 events 拆分为 `events/durable/` 和 `events/typed/`
  2. 提取 `durable-event-bus.ts` 的子模块
  3. 统一事件类型定义

#### 243. [源代码] [中严重] [truth模块 - 28个文件可能过于庞大]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/` (28个文件)
- **问题描述**:
  - truth 是权威存储模块
  - 包含 `authoritative-sql-database.ts` 等核心组件
  - 28 个文件需要评估是否可以拆分
- **建议修复**:
  1. 评估 truth 模块的子模块划分
  2. 按实体类型拆分：`truth/task/`、`truth/workflow/`、`truth/session/`
  3. 提取共享数据库访问逻辑

#### 244. [源代码] [中严重] [recovery模块 - 29个文件过于庞大]
- **文件/路径**: `src/platform/five-plane-execution/recovery/` (29个文件)
- **问题描述**:
  - recovery 是执行引擎的关键容错机制
  - 29 个文件表明功能复杂
- **建议修复**:
  1. 评估 recovery 模块的子模块划分
  2. 拆分为 `recovery/retry/`、`recovery/compensation/`、`recovery/checkpoint/`
  3. 确保每个子模块有清晰的职责

#### 245. [源代码] [中严重] [ha模块 - 25个文件]
- **文件/路径**: `src/platform/five-plane-execution/ha/` (25个文件)
- **问题描述**:
  - HA 模块有 25 个文件，表明功能复杂
  - 包含 `ha-coordinator-service-inner.js` 等核心服务
  - 与 `lease/` 模块可能有重叠职责
- **建议修复**:
  1. 审计 HA 模块的职责边界
  2. 与 lease 模块协调，确保无重复
  3. 考虑将 HA 逻辑提取为独立服务

#### 246. [源代码] [中严重] [execution-engine模块 - 29个文件过于庞大]
- **文件/路径**: `src/platform/five-plane-execution/execution-engine/` (29个文件)
- **问题描述**:
  - execution-engine 是执行引擎的核心
  - `multi-step-orchestration.ts` 和相关文件形成复杂的 orchestration 逻辑
  - 模块间耦合度高，测试困难
- **建议修复**:
  1. 将 orchestration 逻辑拆分为独立模块
  2. 提取共享类型到 `orchestration-types.ts`
  3. 降低模块间耦合

#### 247. [配置] [低严重] [tsconfig.temp.json 存在但未清理]
- **文件/路径**: `tsconfig.temp.json`
- **问题描述**:
  - 临时配置文件未被清理
  - 可能导致开发者困惑使用哪个配置
  - 可能包含过时的配置选项
- **建议修复**:
  1. 删除 `tsconfig.temp.json`
  2. 或重命名为 `tsconfig.example.json` 作为参考
  3. 确保只有 `tsconfig.json` 和 `tsconfig.build.json` 两个正式配置

#### 248. [源代码] [低严重] [hardcoded配置注释残留]
- **文件/路径**: 多个源文件
- **问题描述**:
  - 多处代码包含 "hardcoded" 注释
  - 说明代码中曾存在硬编码值，现已修复但注释残留
- **建议修复**:
  1. 清理已修复的 "hardcoded" 注释
  2. 保留有价值的架构决策注释
  3. 使用更明确的注释格式

#### 249. [配置] [中严重] [依赖版本固定但存在漏洞]
- **文件/路径**: `package.json`
- **问题描述**:
  - TypeScript: `^5.8.3` - 最新 5.8.x
  - 但opentelemetry 和 xml-crypto 存在已知漏洞
- **建议修复**:
  1. 定期更新依赖版本
  2. 使用 `npm outdated` 检查过时依赖
  3. 评估漏洞修复的破坏性影响
  4. 保持依赖在安全版本

#### 250. [安全] [中严重] [JWT secret 缺少生成指引和轮换策略]
- **文件/路径**: `.env.example`
- **问题描述**:
  - `AA_API_JWT_SECRET=` 为空
  - 有注释说明生成方法但不够明显
  - 缺少 secret 轮换策略文档
- **建议修复**:
  1. 在 `.env.example` 中添加更明确的安全警告
  2. 提供生成命令示例
  3. 文档化 secret 轮换策略
  4. 考虑添加 secret 验证在应用启动时

#### 251. [测试] [中严重] [E2E测试被排除 - 关键工作流无保护]
- **文件/路径**: 多个 E2E 测试
- **问题描述**:
  - `execution-ticket-lifecycle.test.ts` 被排除
  - `multi-region-failover.test.ts` 被排除
  - `task-status-lifecycle.test.ts` 被排除
  - `workflow-state-transitions.test.ts` 被排除
  - `workflow-timeout-flow.test.ts` 被排除
  - 这些是核心端到端工作流测试
- **建议修复**:
  1. 调查每个 E2E 测试被排除的原因
  2. 修复或启用这些测试
  3. 确保核心工作流有 E2E 测试保护

#### 252. [UI] [低严重] [UI apps 目录结构 - 多平台支持复杂性]
- **文件/路径**: `ui/apps/`
- **问题描述**:
  - `apps/web`, `apps/mobile`, `apps/electron-win`, `apps/tauri-linux`, `apps/tauri-macos`
  - 这么多平台变体增加维护复杂性
  - 需要确认哪些是活跃开发的
- **建议修复**:
  1. 文档化每个平台的维护状态
  2. 评估是否可以合并一些平台
  3. 考虑使用跨平台框架减少变体

#### 253. [配置] [中严重] [stryker.config.mjs 变异覆盖严重不足]
- **文件/路径**: `stryker.config.mjs`
- **问题描述**:
  - 只配置了 8 个文件的变异测试
  - 缺少关键模块：`budget-allocator.ts` (931行)、`runtime-state-machine.ts` (690行)、`transition-service.ts` (868行)、`durable-event-bus.ts` (1214行)
  - 这些核心模块的变异覆盖率 = 0%
- **建议修复**:
  1. 将核心模块添加到 `mutate` 列表
  2. 评估 `durable-event-bus.ts` 的测试可行性
  3. 平衡测试成本与覆盖率

#### 254. [源代码] [中严重] [tool-executor目录 - 35个文件职责过多]
- **文件/路径**: `src/platform/five-plane-execution/tool-executor/` (35个文件)
- **问题描述**:
  - 包含多个巨型文件：`code-diagnostics-service.ts` (17260字节)、`command-executor.ts` (24468字节)、`tool-metadata.ts` (16092字节)
  - 总字节数超过 150KB
- **建议修复**:
  1. 按功能将 tool-executor 拆分为子目录
  2. 提取共享类型到 `tool-types.ts`
  3. 简化 index.ts 导出

#### 255. [配置] [中严重] [config/security 环境配置差异严重]
- **文件/路径**: `config/security/` 各环境配置文件
- **问题描述**:
  - `dev.json`: `{"approvalMode": "strict", "allowDestructiveActions": false}`
  - `test.json`: `{}` (空对象)
  - `staging.json`: `{"approvalMode": "supervised"}`
  - `pre-prod.json`: `{"approvalMode": "supervised", "allowDestructiveActions": false}`
  - `prod.json`: `{"approvalMode": "strict", "allowDestructiveActions": false}`
  - test.json 为空可能导致测试使用意外的默认 approvalMode
  - staging 使用 supervised 但 prod 使用 strict - staging 不能真实反映生产行为
- **建议修复**:
  1. 统一 `test.json` 配置为与 `staging.json` 一致
  2. 将 `staging` 配置改为 `{"approvalMode": "strict"}` 以反映真实生产行为
  3. 添加配置 schema 验证确保必需字段存在

#### 256. [源代码] [中严重] [src/platform/contracts/ 导出路径混乱]
- **文件/路径**: `src/platform/contracts/` 和相关导入
- **问题描述**:
  - 多处代码使用 `import { newId, nowIso } from "../../contracts/types/ids.js"`
  - 深层相对导入导致重构困难
  - 不使用 package exports 定义的路径
- **建议修复**:
  1. 使用包导出方式 `import { newId } from "@automatic-agent/platform/contracts"`
  2. 统一 contracts 导入路径
  3. 确保 exports 映射正确

#### 257. [配置] [低严重] [.c8rc.json 和 stryker.config.mjs 配置分离]
- **文件/路径**: `.c8rc.json` 和 `stryker.config.mjs`
- **问题描述**:
  - 两个覆盖率工具配置分离
  - c8: `100%` 覆盖 threshold
  - stryker: `high: 80, low: 60, break: 50`
  - 可能导致报告不一致
- **建议修复**:
  1. 确保两个工具的覆盖率阈值一致
  2. 在 CI 中统一报告
  3. 考虑使用单一覆盖率工具或明确分工

#### 258. [配置] [中严重] [config/environments 与 config/security 配置不对齐]
- **文件/路径**: `config/environments/dev.json` 和 `config/security/dev.json`
- **问题描述**:
  - `environments/dev.json` 定义了部署相关配置
  - `security/dev.json` 定义了安全策略
  - 但两个配置文件没有明确的关联机制
  - 应用启动时如何合并这些配置不明确
- **建议修复**:
  1. 文档化配置合并策略
  2. 在代码中添加配置来源注释
  3. 考虑将安全配置合并到 environments 配置中

### 总结

本次自动Review（第五轮 - 依赖安全与配置深度检查）发现了43个新问题：

**高优先级 (需要立即处理)**:
1. npm 依赖存在多个高严重漏洞（opentelemetry, xmldom, fast-uri, protobufjs）
2. UI 项目存在6个中严重漏洞（esbuild）
3. 14个测试文件持续失败但未处理
4. 巨型源文件需要拆分（harness 2317行、contracts 2169行、nl-gateway 1669行、oapeflir-loop-service 1497行）
5. .gitignore 缺少多个临时文件模式（`:memory:*`、`dist_*`、`.audit/`）

**中优先级**:
1. docker-compose.yml 安全配置不完整
2. JWT secret 缺少生成指引和轮换策略
3. config/security 环境配置差异严重
4. process.env 952处访问缺少统一管理
5. EventEmitter vs TypedEventBus 混用问题
6. stryker.config.mjs 变异覆盖严重不足
7. 测试 helpers 过多（78个）
8. HA 和 Lease 模块职责重叠
9. memory、events、truth、recovery、execution-engine 模块过于庞大
10. contracts 导出路径混乱

**低优先级**:
1. Buffer.from 使用可能存在安全风险
2. tsconfig.temp.json 存在但未清理
3. hardcoded 配置注释残留
4. 5个符号链接存在但未被文档化
5. src/core/runtime 与五层架构并行存在
6. UI 多平台支持复杂性

*Review generated: 2026-05-13*

---

### 总体问题统计

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 8 | 30 | 10 | 48 |
| 测试 | 2 | 8 | 1 | 11 |
| 配置 | 2 | 14 | 5 | 21 |
| 安全 | 3 | 5 | 0 | 8 |
| 文档 | 0 | 1 | 1 | 2 |
| UI | 0 | 1 | 2 | 3 |
| 部署 | 0 | 2 | 1 | 3 |
| **合计** | **15** | **61** | **20** | **96** |

### 优先修复建议

**立即处理 (本周内)**:
1. 运行 `npm audit fix` 修复非破坏性漏洞
2. 修复或标记14个失败测试
3. 将 `.audit/`、`:memory:*`、`dist_*` 添加到 .gitignore
4. 统一 config/security/test.json 配置

**短期内处理 (本月内)**:
1. 拆分巨型源文件（harness、contracts、nl-gateway、oapeflir-loop-service）
2. 创建统一配置管理模块减少 952 处 process.env 访问
3. 迁移 EventEmitter 到 TypedEventBus
4. 扩展 stryker 变异测试覆盖到核心模块

**长期规划**:
1. 清理 src/core/runtime 目录
2. 简化测试 helpers
3. 统一测试配置（c8 + stryker）
4. 完善 deploy 目录（chaos、runbooks、Kubernetes）
5. 建立依赖版本更新机制

---

## 2026-05-13 自动Review报告 (第六轮 - 全面深度检查)

### 发现的问题

#### 259. [源代码] [高严重] [巨型文件 - harness/index.ts 达2317行]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts`
- **问题描述**: 
  - 该文件长达2317行，是项目中最大的TypeScript文件
  - 违反单一职责原则，包含过多功能
  - 跨多个子模块：constraints、context、durable、eval-harness、evaluation、evaluator、generator、guardrails、hitl-runtime、loop、memory-namespace、planner、protocol、recovery-controller、runtime、sandbox、toolbelt
  - 维护困难，重构风险高
- **建议修复**: 
  1. 按功能领域拆分为独立模块：
     - `harness/core/` - 核心接口
     - `harness/context/` - 上下文管理
     - `harness/evaluation/` - 评估逻辑
     - `harness/guardrails/` - 安全护栏
  2. 创建 `harness/index.ts` 作为统一导出
  3. 每个子模块不超过300行

#### 260. [源代码] [高严重] [巨型文件 - contracts/executable-contracts/index.ts 达2169行]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts`
- **问题描述**: 
  - 该文件长达2169行，存储可执行契约定义
  - 契约应该是稳定的、小的接口，不应包含大量逻辑
  - 可能是过度设计或职责混乱
- **建议修复**: 
  1. 将契约拆分为按领域组织的子文件：
     - `executable-contracts/task-contracts.ts`
     - `executable-contracts/workflow-contracts.ts`
     - `executable-contracts/execution-contracts.ts`
  2. 提取共享类型到 `executable-contracts/types.ts`
  3. 保持契约文件简洁，只定义接口和常量

#### 261. [源代码] [高严重] [巨型文件 - nl-gateway/index.ts 达1669行]
- **文件/路径**: `src/interaction/nl-gateway/index.ts`
- **问题描述**: 
  - 该文件长达1669行，是交互层的核心NL网关
  - NL处理应该是模块化的，不应集中在一个文件
  - 可能包含多个职责：解析、路由、转换、验证
- **建议修复**: 
  1. 拆分为多个服务：
     - `nl-gateway/parser/` - 自然语言解析
     - `nl-gateway/router/` - 意图路由
     - `nl-gateway/transformer/` - 数据转换
  2. 创建主入口文件协调各模块
  3. 提取常量配置到独立文件

#### 262. [源代码] [高严重] [巨型文件 - oapeflir-loop-service.ts 达1497行]
- **文件/路径**: `src/platform/five-plane-orchestration/oapeflir/oapeflir-loop-service.ts`
- **问题描述**: 
  - 该文件长达1497行，是OAPEFLIR循环服务的核心
  - 循环服务应该使用状态机模式，不应包含所有逻辑
  - 可能是过度耦合的结果
- **建议修复**: 
  1. 提取循环状态机到独立文件
  2. 将各阶段处理逻辑拆分到单独的handler
  3. 使用策略模式处理不同场景

#### 263. [源代码] [高严重] [巨型文件 - intake-router.ts 达1328行]
- **文件/路径**: `src/platform/five-plane-orchestration/routing/intake-router.ts`
- **问题描述**: 
  - 该文件长达1328行，处理摄取路由逻辑
  - 路由应该是配置驱动的，不应硬编码大量逻辑
- **建议修复**: 
  1. 将路由规则提取为配置文件
  2. 使用规则引擎替代硬编码分支
  3. 拆分为路由器和规则评估器

#### 264. [源代码] [高严重] [巨型文件 - slo-alerting-service.ts 达1270行]
- **文件/路径**: `src/platform/shared/observability/slo-alerting-service.ts`
- **问题描述**: 
  - 该文件长达1270行，处理SLO告警逻辑
  - 观测服务应该高度模块化
- **建议修复**: 
  1. 拆分为告警规则引擎和通知服务
  2. 提取SLO计算逻辑到独立模块
  3. 使用配置驱动告警规则

#### 265. [源代码] [高严重] [巨型文件 - tenant-platform-service.ts 达1231行]
- **文件/路径**: `src/scale-ecosystem/tenant-platform/tenant-platform-service.ts`
- **问题描述**: 
  - 该文件长达1231行，多租户平台服务过于庞大
  - 多租户逻辑应该分散到各领域
- **建议修复**: 
  1. 将租户隔离逻辑提取为中间件
  2. 业务功能按领域拆分
  3. 使用依赖注入解耦

#### 266. [源代码] [高严重] [巨型文件 - durable-event-bus.ts 达1214行]
- **文件/路径**: `src/platform/five-plane-state-evidence/events/durable-event-bus.ts`
- **问题描述**: 
  - 该文件长达1214行，是持久化事件总线的核心
  - 事件总线应该是可组合的，不应单文件实现所有功能
  - 根据之前报告，对应测试 `durable-event-bus-async.test.ts` 失败
- **建议修复**: 
  1. 拆分为事件总线核心和适配器
  2. 将背压、重试、投递策略提取为独立模块
  3. 修复 `durable-event-bus-async.test.ts` 测试

#### 267. [源代码] [高严重] [巨型文件 - delegation-manager.service.ts 达1210行]
- **文件/路径**: `src/platform/five-plane-orchestration/agent-delegation/delegation-manager.service.ts`
- **问题描述**: 
  - 该文件长达1210行，代理委托服务过于庞大
  - 委托逻辑应该使用策略模式
- **建议修复**: 
  1. 提取委托策略到独立文件
  2. 将权限验证逻辑与委托逻辑分离
  3. 使用职责链模式处理不同委托类型

#### 268. [源代码] [高严重] [巨型文件 - marketplace-governance-service.ts 达1081行]
- **文件/路径**: `src/scale-ecosystem/marketplace/marketplace-governance-service.ts`
- **问题描述**: 
  - 该文件长达1081行，市场治理服务过大
  - 治理逻辑应该高度模块化
- **建议修复**: 
  1. 按治理领域拆分为独立服务
  2. 提取策略引擎到独立模块
  3. 使用配置驱动治理规则

#### 269. [源代码] [高严重] [巨型文件 - event-registry.ts 达1077行]
- **文件/路径**: `src/platform/five-plane-state-evidence/events/event-registry.ts`
- **问题描述**: 
  - 该文件长达1077行，事件注册表过大
  - 注册表应该是简单的映射，不应包含业务逻辑
- **建议修复**: 
  1. 将事件处理逻辑提取到独立handler
  2. 保持注册表为纯数据配置
  3. 使用事件路由器模式

#### 270. [源代码] [中严重] [巨型文件 - worker-repository.ts 达1064行]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/async-repositories/worker-repository.ts`
- **问题描述**: 
  - 该文件长达1064行，工作线程仓库过大
  - 数据访问层应该高度模块化
- **建议修复**: 
  1. 将仓库操作拆分为多个专门仓库
  2. 使用工厂模式创建仓库实例
  3. 提取公共查询逻辑到基类

#### 271. [源代码] [中严重] [巨型文件 - llm-eval-service.ts 达1055行]
- **文件/路径**: `src/platform/prompt-engine/eval/llm-eval-service.ts`
- **问题描述**: 
  - 该文件长达1055行，LLM评估服务过大
  - 评估逻辑应该使用策略模式
- **建议修复**: 
  1. 将评估器拆分为多个专门评估器
  2. 使用评估器组合模式
  3. 提取评估指标计算到独立模块

#### 272. [源代码] [中严重] [巨型文件 - approval-flow-engine.ts 达1031行]
- **文件/路径**: `src/platform/five-plane-control-plane/approval-center/approval-flow-engine.ts`
- **问题描述**: 
  - 该文件长达1031行，审批流程引擎过大
  - 审批逻辑应该使用状态机模式
- **建议修复**: 
  1. 将流程定义与流程引擎分离
  2. 使用可视化流程定义替代硬编码
  3. 提取步骤处理器到独立模块

#### 273. [源代码] [中严重] [巨型文件 - goal-decomposer/index.ts 达1031行]
- **文件/路径**: `src/interaction/goal-decomposer/index.ts`
- **问题描述**: 
  - 该文件长达1031行，目标分解器过大
  - 分解逻辑应该高度模块化
- **建议修复**: 
  1. 将分解策略提取为独立模块
  2. 使用图搜索算法替代硬编码分支
  3. 提取子目标生成器到独立服务

#### 274. [源代码] [中严重] [巨型文件 - human-takeover-service.ts 达1028行]
- **文件/路径**: `src/platform/five-plane-control-plane/incident-control/human-takeover-service.ts`
- **问题描述**: 
  - 该文件长达1028行，人工接管服务过大
  - 接管逻辑应该使用职责链模式
- **建议修复**: 
  1. 将接管判定与执行分离
  2. 使用策略模式处理不同场景
  3. 提取通知服务到独立模块

#### 275. [源代码] [中严重] [巨型文件 - auto-stop-loss-service.ts 达1004行]
- **文件/路径**: `src/platform/five-plane-control-plane/incident-control/auto-stop-loss-service.ts`
- **问题描述**: 
  - 该文件长达1004行，自动止损服务过大
  - 止损逻辑应该高度模块化
- **建议修复**: 
  1. 将止损规则提取为配置文件
  2. 使用规则引擎评估条件
  3. 提取通知和执行到独立模块

#### 276. [源代码] [中严重] [巨型文件 - api-client.ts 达1022行]
- **文件/路径**: `src/sdk/client-sdk/api-client.ts`
- **问题描述**: 
  - 该文件长达1022行，API客户端过大
  - 客户端应该按功能拆分
- **建议修复**: 
  1. 将不同API端点拆分为独立客户端
  2. 提取公共请求处理到中间件
  3. 使用适配器模式统一接口

#### 277. [源代码] [中严重] [巨型文件 - channel-gateway-service.ts 达981行]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/channel-gateway-service.ts`
- **问题描述**: 
  - 该文件长达981行，通道网关服务过大
  - 网关应该高度模块化
- **建议修复**: 
  1. 将通道处理拆分为独立handler
  2. 使用中间件模式处理cross-cutting concerns
  3. 提取协议转换到适配器层

#### 278. [源代码] [高严重] [巨型文件 - 总计18个文件超过1000行]
- **文件/路径**: 项目中共有18个文件超过1000行
- **问题描述**: 
  - 大型文件列表：
    1. `harness/index.ts` - 2317行
    2. `contracts/executable-contracts/index.ts` - 2169行
    3. `nl-gateway/index.ts` - 1669行
    4. `oapeflir-loop-service.ts` - 1497行
    5. `intake-router.ts` - 1328行
    6. `slo-alerting-service.ts` - 1270行
    7. `tenant-platform-service.ts` - 1231行
    8. `durable-event-bus.ts` - 1214行
    9. `delegation-manager.service.ts` - 1210行
    10. `marketplace-governance-service.ts` - 1081行
    11. `event-registry.ts` - 1077行
    12. `worker-repository.ts` - 1064行
    13. `llm-eval-service.ts` - 1055行
    14. `approval-flow-engine.ts` - 1031行
    15. `goal-decomposer/index.ts` - 1031行
    16. `human-takeover-service.ts` - 1028行
    17. `auto-stop-loss-service.ts` - 1004行
    18. `api-client.ts` - 1022行
  - 这表明项目存在系统性的代码组织问题
  - 需要建立大型文件检测和预防机制
- **建议修复**: 
  1. 在ESLint中添加最大行数规则（如max-lines: 1000）
  2. 创建重构计划，优先处理最高风险文件
  3. 每次PR必须降低或保持而非增加大型文件数量
  4. 将18个文件按优先级和依赖关系分配到季度计划中

#### 279. [源代码] [高严重] [符号链接未被.gitignore忽略]
- **文件/路径**: 根目录多个临时构建目录
- **问题描述**: 
  - `.gitignore` 存在但未完整
  - 检查发现：
    - `dist_1964/` - 未忽略
    - `dist_issue2014/` - 未忽略
    - `dist_lineage_test/` - 未忽略
    - `dist_temp/` - 已忽略
    - `dist_test/` - 已忽略
    - `dist_test_compiled/` - 未忽略
  - `session-replay/` - 已忽略
  - `artifacts/` - 已忽略
  - `:memory:*` - 未忽略
  - `.audit/` - 未忽略
- **建议修复**: 
  1. 添加 `dist_*` 到 `.gitignore` 以覆盖所有临时构建目录
  2. 添加 `:memory:*` 模式
  3. 添加 `.audit/` 目录
  4. 运行 `git status` 检查是否还有其他遗漏

#### 280. [源代码] [高严重] [src/core/runtime 与五层架构并存造成混淆]
- **文件/路径**: `src/core/runtime/` 目录
- **问题描述**: 
  - CLAUDE.md 明确说明 `src/core/runtime/` 是兼容性目录，不应添加新的规范运行时逻辑
  - 但该目录仍然存在并包含内容：
    - `distributed-lock-service.ts`
    - `orchestrator/`
    - `planner/`
    - `process-tracker.ts`
    - `queue-adapter.ts`
    - `supervisor/`
  - 五层架构位于 `src/platform/five-plane-*/`
  - 两套架构并存造成维护困难
- **建议修复**: 
  1. 创建迁移计划，将 `src/core/runtime/` 内容迁移到五层架构
  2. 或者明确文档说明为何需要保留两套架构
  3. 添加lint规则禁止向 `src/core/runtime/` 添加新代码

#### 281. [配置] [中严重] [config/security/dev.json 与 test.json 行为不一致]
- **文件/路径**: `config/security/dev.json` 和 `config/security/test.json`
- **问题描述**: 
  - `dev.json`: `{"approvalMode": "supervised"}`
  - `test.json`: `{}` (空对象)
  - 这种差异会导致开发环境行为与测试环境不一致
  - 测试环境缺少 `approvalMode` 配置可能导致测试使用默认值而非预期值
- **建议修复**: 
  1. 统一安全配置策略
  2. test.json 应包含与 dev.json 相同的配置结构
  3. 添加配置验证确保必需字段存在
  4. 在CI中检测配置不一致

#### 282. [配置] [中严重] [.env.example 安全警告不足]
- **文件/路径**: `.env.example`
- **问题描述**: 
  - `AA_API_JWT_SECRET=` 为空，注释说明生产需要配置
  - 但没有足够的安全警告提示
  - 没有指引如何生成安全的 secret
  - 没有 secret 管理策略文档
  - 大量其他 secrets（ANTHROPIC_API_KEY, OPENAI_API_KEY, MINIMAX_API_KEY）同样为空
- **建议修复**: 
  1. 在注释中添加更明确的"危险"警告标记
  2. 提供生成命令示例：`node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"`
  3. 文档化 secret 轮换策略
  4. 考虑添加 secret 验证在应用启动时

#### 283. [测试] [高严重] [测试排除列表过长 - 114个模式排除]
- **文件/路径**: `tsconfig.json` exclude 部分
- **问题描述**: 
  - 排除了114个测试文件和目录
  - 包括整个 `tests/unit/platform/five-plane-execution/` 目录
  - 包括所有 `tests/integration/platform/*/` 目录
  - 这么多测试被排除不是正常现象
  - 可能表明：
    - 循环依赖问题
    - 测试不稳定
    - 测试基础设施需要修复
- **建议修复**: 
  1. 创建一个诊断脚本分析每个被排除测试的失败原因
  2. 按类别统计：
     - 循环依赖问题
     - 缺少依赖Mock
     - 测试数据问题
     - 环境问题
  3. 逐个修复并重新启用
  4. 如果确实需要排除，明确记录原因并设置复查日期

#### 284. [测试] [高严重] [durable-event-bus-async.test.ts 测试失败]
- **文件/路径**: `tests/unit/platform/five-plane-state-evidence/events/durable-event-bus-async.test.ts`
- **问题描述**: 
  - `durable-event-bus.ts` (1214行) 是巨型源文件
  - 对应的异步测试失败
  - 事件总线是 state-evidence 的核心组件，其测试失败可能导致数据一致性风险
- **建议修复**: 
  1. 调查 `durable-event-bus-async.test.ts` 失败原因
  2. 考虑将 `durable-event-bus.ts` 拆分为多个可测试模块
  3. 确保事件投递、重试、背压等核心逻辑有测试覆盖

#### 285. [测试] [高严重] [budget-allocator.test.ts 测试失败]
- **文件/路径**: `tests/unit/platform/five-plane-execution/budget-allocator.test.ts`
- **问题描述**: 
  - `budget-allocator.ts` (931行) 是巨型源文件
  - 对应测试 `budget-allocator.test.ts` 失败
  - 预算分配器是执行引擎的关键组件
- **建议修复**: 
  1. 调查测试失败原因
  2. 拆分 budget-allocator.ts 到多个模块
  3. 确保预算计算逻辑有充分测试覆盖

#### 286. [测试] [中严重] [stryker mutation testing 只覆盖7个文件]
- **文件/路径**: `stryker.config.mjs`
- **问题描述**: 
  - mutation testing 只配置了对7个文件的变异检测
  - 这些文件主要是 auth、billing、approval、gateway 相关
  - 其他核心模块（如 budget-allocator、runtime-state-machine、durable-event-bus）未纳入变异测试
  - 覆盖率严重不足以发现所有关键问题
- **建议修复**: 
  1. 扩展 mutation 测试覆盖到更多核心模块：
     - `src/platform/five-plane-execution/budget-allocator.ts`
     - `src/platform/five-plane-execution/runtime-state-machine.ts`
     - `src/platform/five-plane-state-evidence/events/durable-event-bus.ts`
     - `src/platform/five-plane-orchestration/harness/index.ts`
  2. 按风险优先级分阶段扩展
  3. 在CI中要求 mutation 测试覆盖率不低于70%

#### 287. [安全] [高严重] [npm 依赖存在多个高严重漏洞]
- **文件/路径**: `package.json` dependencies
- **问题描述**: 
  - `@opentelemetry/exporter-trace-otlp-http: ^0.214.0` - 已知漏洞
  - `xml-crypto: ^2.1.6` - 已知漏洞
  - `zod: ^3.25.76` - 已知漏洞
  - 建议运行 `npm audit` 获取完整漏洞列表
- **建议修复**: 
  1. 运行 `npm audit` 查看所有漏洞
  2. 对于非破坏性漏洞，运行 `npm audit fix`
  3. 对于破坏性更新，评估影响后手动更新
  4. 建立依赖更新机制，定期检查漏洞

#### 288. [安全] [高严重] [Buffer.from 使用可能存在安全问题]
- **文件/路径**: 多个文件
- **问题描述**: 
  - `src/platform/five-plane-interface/api/oidc-oauth-service.ts` - 多处使用 `Buffer.from`
  - `src/platform/five-plane-interface/api/api-auth-service.ts` - 使用 `Buffer.from`
  - `src/platform/five-plane-interface/webhook/index.ts` - 使用 `Buffer.from`
  - 旧版 `Buffer` API 可能存在安全风险
- **建议修复**: 
  1. 审查所有 `Buffer.from` 使用
  2. 考虑使用 `TextEncoder` 或 `Uint8Array` 替代
  3. 添加安全lint规则检查
  4. 优先在认证相关文件上修复

#### 289. [安全] [高严重] [process.env 访问次数过多 - 约952处]
- **文件/路径**: 全局
- **问题描述**: 
  - 约952处 `process.env` 访问
  - 配置直接暴露在代码中
  - 缺少统一的配置管理
  - 环境变量可能未经过滤暴露敏感信息
- **建议修复**: 
  1. 创建统一的配置管理模块（如 `src/config/index.ts`）
  2. 添加配置验证和类型检查（使用 zod）
  3. 使用 `getOrThrow` 模式确保必需配置存在
  4. 避免在业务逻辑中直接访问 `process.env`
  5. 在CI中检测新的 `process.env` 访问

#### 290. [部署] [中严重] [deploy/terraform 和 deploy/helm 内容不完整]
- **文件/路径**: `deploy/terraform/` 和 `deploy/helm/`
- **问题描述**: 
  - terraform 目录包含环境配置和主配置，但可能是占位符
  - helm 目录同样可能不完整
  - 没有明确的部署文档
- **建议修复**: 
  1. 检查 terraform 配置的完整性
  2. 检查 helm chart 的完整性
  3. 添加部署文档说明如何使用这些配置
  4. 在 CI 中验证配置语法

#### 291. [部署] [中严重] [docker-compose.yml 安全配置部分完成]
- **文件/路径**: `docker-compose.yml`
- **问题描述**: 
  - 已配置 `security_opt: no-new-privileges:true`
  - 已配置 `cap_drop: ALL`
  - 已配置资源限制（cpus, mem_limit, mem_reservation, pids_limit）
  - 已配置 `read_only: true` 和 `tmpfs`
  - 但 JWT secret 被注释掉，生产使用不安全
  - PostgreSQL 密码通过环境变量注入但没有 secret 管理
- **建议修复**: 
  1. 使用 docker secrets 管理敏感配置
  2. 启用网络隔离（已配置但应验证）
  3. 添加健康检查的详细验证
  4. 文档化生产部署的安全要求

#### 292. [文档] [中严重] [docs_zh/contracts 与代码可能不同步]
- **文件/路径**: `docs_zh/contracts/` (151个文件)
- **问题描述**: 
  - 文档显示有151个契约文件
  - 代码中 `src/platform/contracts/` 也有大量文件
  - 两处可能存在不同步问题
  - 契约文档可能过时
- **建议修复**: 
  1. 创建契约版本控制机制
  2. 在CI中验证契约文档与代码一致性
  3. 添加契约变更检测和通知
  4. 考虑使用代码生成契约文档

#### 293. [文档] [低严重] [deploy/runbooks 内容单薄]
- **文件/路径**: `deploy/runbooks/production-alert-runbook.md`
- **问题描述**: 
  - 只有一个2010字节的运行手册
  - 内容可能不足以覆盖实际运维场景
  - 缺少常见故障排除步骤
  - 缺少回滚程序
- **建议修复**: 
  1. 扩展运行手册内容
  2. 添加常见故障场景的排查步骤
  3. 添加部署和回滚程序
  4. 包含联系人和升级路径

#### 294. [源代码] [低严重] [src/core/runtime 目录仍在使用]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - 该目录包含：
    - `distributed-lock-service.ts`
    - `orchestrator/`
    - `planner/`
    - `process-tracker.ts`
    - `queue-adapter.ts`
    - `supervisor/`
  - CLAUDE.md 说这是兼容性目录，但仍在使用
  - 与五层架构并存造成混淆
- **建议修复**: 
  1. 明确文档说明 `src/core/runtime/` 的用途
  2. 或者创建迁移计划将内容迁移到五层架构
  3. 添加 lint 规则禁止向该目录添加新代码

#### 295. [源代码] [低严重] [5个符号链接未被文档化]
- **文件/路径**: `src/platform/` 下的符号链接
- **问题描述**: 
  - `control-plane -> five-plane-control-plane`
  - `execution -> five-plane-execution`
  - `state-evidence -> five-plane-state-evidence`
  - `orchestration -> five-plane-orchestration`
  - `interface -> five-plane-interface`
  - 这些符号链接未被文档化，可能导致混淆
  - 可能影响 IDE 和工具的路径解析
- **建议修复**: 
  1. 在 CLAUDE.md 或其他文档中记录这些符号链接
  2. 评估是否可以用实际的目录替代
  3. 确保 Git 历史追踪不会因符号链接而断裂

#### 296. [配置] [低严重] [tsconfig.temp.json 存在但未清理]
- **文件/路径**: `tsconfig.temp.json`
- **问题描述**: 
  - 存在多个 tsconfig 变体：
    - `tsconfig.json` - 主配置
    - `tsconfig.build.json` - 构建配置
    - `tsconfig.temp.json` - 临时配置
  - `tsconfig.temp.json` 可能是临时配置遗留
- **建议修复**: 
  1. 删除 `tsconfig.temp.json`
  2. 文档化每个配置文件的用途
  3. 确保只有一个主配置

#### 297. [源代码] [低严重] [hardcoded 配置注释残留]
- **文件/路径**: 多个源文件
- **问题描述**: 
  - 多处代码包含 "hardcoded" 注释
  - 如 `channel-gateway-service.ts:303` - R12-12 注释
  - `evaluator-service.ts:69` - R11-05 FIX 注释
  - `plan-evaluator.ts:40` - R8-12 FIX 注释
  - 说明代码中曾存在硬编码值，现已修复但注释残留
- **建议修复**: 
  1. 清理已修复的 "hardcoded" 注释
  2. 保留有价值的架构决策注释
  3. 使用更明确的注释格式（如 JSDoc）

#### 298. [源代码] [低严重] [内存数据库文件未在.gitignore中]
- **文件/路径**: 根目录的 `:memory:*` 文件
- **问题描述**: 
  - 存在多个内存数据库文件 (`:memory:aa-truth-*`, `:memory:fence-*`)
  - 这些文件未被 `.gitignore` 忽略
  - 可能包含敏感信息或测试数据
- **建议修复**: 
  1. 在 `.gitignore` 中添加 `:memory:*` 模式
  2. 清理所有内存数据库文件
  3. 调查这些文件的来源并修复

#### 299. [源代码] [低严重] [.audit/ 目录未在.gitignore中]
- **文件/路径**: `.audit/` 目录
- **问题描述**: 
  - `.audit/` 目录包含 `delegation/` 和 `quality.md`
  - `quality.md` 报告了14个失败的测试
  - 该目录未被 `.gitignore` 排除
  - 可能包含敏感审计数据
- **建议修复**: 
  1. 在 `.gitignore` 中添加 `.audit/` 排除
  2. 审计数据应存储在安全的位置
  3. 评估是否需要保留这些文件

#### 300. [源代码] [低严重] [TODO/FIXME 注释残留 - 约17处]
- **文件/路径**: 多个源文件
- **问题描述**: 
  - 项目中有约17处 TODO/FIXME/HACK/XXX 注释
  - 这些注释可能表示未完成的工作或临时解决方案
  - 长期积累会降低代码质量
- **建议修复**: 
  1. 创建 Issue tracker 跟踪每个 TODO
  2. 优先处理高优先级的 TODO
  3. 将 TODO 转化为具体的 Issue
  4. 避免在代码中留下临时注释

#### 301. [配置] [低严重] [.c8rc.json 和 stryker.config.mjs 配置分离]
- **文件/路径**: `.c8rc.json` 和 `stryker.config.mjs`
- **问题描述**: 
  - 两个覆盖率工具配置分离
  - c8: 100% 覆盖 threshold
  - stryker: high: 80, low: 60, break: 50
  - 可能导致报告不一致
- **建议修复**: 
  1. 确保两个工具的覆盖率阈值一致
  2. 在 CI 中统一报告
  3. 考虑使用单一覆盖率工具或明确分工

#### 302. [源代码] [中严重] [src/contracts/types/ids.ts 导出混乱]
- **文件/路径**: `src/platform/contracts/` 和相关导入
- **问题描述**: 
  - 多处代码使用 `import { newId, nowIso } from "../../contracts/types/ids.js"`
  - 但实际文件路径可能不同
  - 深层相对导入导致重构困难
- **建议修复**: 
  1. 使用包导出方式
  2. 统一 `import { newId } from "@automatic-agent/platform/contracts"`
  3. 确保 exports 映射正确
  4. 在 ESLint 中添加规则检查深层相对导入

#### 303. [源代码] [中严重] [UI package.json 与主 package.json 分离]
- **文件/路径**: `ui/package.json`
- **问题描述**: 
  - UI 有独立的 `package.json` 和 `node_modules`
  - 使用 Turborepo 工作区
  - 需要单独的依赖管理流程
  - 可能导致依赖版本不一致
- **建议修复**: 
  1. 确保 UI 和主项目的依赖版本同步
  2. 文档化 UI 服务的部署流程
  3. 考虑统一依赖管理
  4. 在 CI 中验证 UI 构建

#### 304. [源代码] [中严重] [tool-executor 目录文件过多 - 35个文件]
- **文件/路径**: `src/platform/five-plane-execution/tool-executor/`
- **问题描述**: 
  - 该目录包含35个文件，包括多个巨型文件：
    - `code-diagnostics-service.ts` (17260字节)
    - `command-executor.ts` (24468字节)
    - `tool-metadata.ts` (16092字节)
  - 总字节数超过150KB，表明职责过于集中
- **建议修复**: 
  1. 按功能将 tool-executor 拆分为子目录：
     - `tool-executor/core/` - 核心执行逻辑
     - `tool-executor/diagnostics/` - 诊断服务
     - `tool-executor/security/` - 安全检查
     - `tool-executor/mcp/` - MCP 相关
  2. 提取共享类型到 `tool-types.ts`
  3. 简化 index.ts 导出

#### 305. [源代码] [中严重] [state-transition 服务 - transition-service.ts 达868行]
- **文件/路径**: `src/platform/five-plane-execution/state-transition/transition-service.ts` (868行)
- **问题描述**: 
  - `TransitionService` 是执行引擎的核心状态转换门
  - 单一文件包含过多状态转换逻辑
  - 根据 CLAUDE.md，这是"权威状态转换门"，应该高度可靠
- **建议修复**: 
  1. 将状态转换规则拆分为独立的策略类
  2. 将状态枚举和类型移到 `runtime-state-types.ts`
  3. 提取验证逻辑到独立模块
  4. 添加更多单元测试

#### 306. [源代码] [中严重] [memory 系统目录过大]
- **文件/路径**: `src/platform/five-plane-state-evidence/memory/`
- **问题描述**: 
  - memory 系统包含多个服务，可能过于复杂
  - 需要检查具体文件数量和大小
- **建议修复**: 
  1. 按功能拆分 memory 系统
  2. 将长期记忆和短期记忆分离
  3. 提取共享类型到独立模块

#### 307. [源代码] [中严重] [events 系统目录过大]
- **文件/路径**: `src/platform/five-plane-state-evidence/events/`
- **问题描述**: 
  - events 系统包含 `durable-event-bus.ts` (1214行) 和 `event-registry.ts` (1077行)
  - 两个巨型文件表明需要重构
- **建议修复**: 
  1. 将 durable-event-bus 拆分为核心和适配器
  2. 将 event-registry 保持为数据配置，提取处理逻辑
  3. 使用事件路由器模式

#### 308. [源代码] [中严重] [truth 系统目录过大]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/`
- **问题描述**: 
  - truth 系统包含 `worker-repository.ts` (1064行) 等大型文件
  - 数据访问层应该高度模块化
- **建议修复**: 
  1. 将仓库操作拆分为多个专门仓库
  2. 使用工厂模式创建仓库实例
  3. 提取公共查询逻辑到基类

#### 309. [源代码] [中严重] [recovery 系统目录过大 - 29个文件]
- **文件/路径**: `src/platform/five-plane-execution/recovery/` (29个文件)
- **问题描述**: 
  - recovery 系统包含多个服务：
    - `runtime-recovery-service.ts` (33720字节)
    - `runtime-recovery-decision-service.ts` (13695字节)
    - `runtime-repair-service.ts` (22154字节)
  - 表明恢复逻辑复杂，可能需要拆分
- **建议修复**: 
  1. 按恢复场景拆分为独立服务
  2. 将公共逻辑提取到基类
  3. 使用策略模式处理不同恢复场景

#### 310. [源代码] [中严重] [execution-engine 目录过大]
- **文件/路径**: `src/platform/five-plane-execution/execution-engine/`
- **问题描述**: 
  - execution-engine 目录包含32个文件
  - `multi-step-orchestration.ts` 达657行
  - `single-task-happy-path.ts` 达29153字节
  - 表明执行引擎需要模块化
- **建议修复**: 
  1. 将多步编排拆分为独立模块
  2. 将单任务执行路径提取为独立服务
  3. 使用策略模式处理不同执行场景

#### 311. [源代码] [中严重] [HA 和 Lease 模块职责重叠]
- **文件/路径**: `src/platform/five-plane-execution/ha/` 和 `src/platform/five-plane-execution/lease/`
- **问题描述**: 
  - HA（高可用）模块和 Lease 模块可能存在职责重叠
  - 两个模块都处理分布式一致性
  - 需要评估是否可以合并或明确分工
- **建议修复**: 
  1. 分析 HA 和 Lease 模块的职责边界
  2. 明确文档说明两个模块的职责分工
  3. 如果有重叠，重构到单一模块或明确接口分离

#### 312. [源代码] [中严重] [EventEmitter vs TypedEventBus 混用]
- **文件/路径**: 全局
- **问题描述**: 
  - 项目同时使用 Node.js 原生 `EventEmitter` 和自定义 `TypedEventBus`
  - 混用可能导致类型安全问题
  - `TypedEventBus` 应该是标准，但 `EventEmitter` 仍在使用
- **建议修复**: 
  1. 创建迁移计划，将所有 `EventEmitter` 迁移到 `TypedEventBus`
  2. 添加 ESLint 规则禁止使用原生 `EventEmitter`
  3. 在 CI 中检测混用情况

#### 313. [配置] [中严重] [npm scripts 过多 - 超过100个]
- **文件/路径**: `package.json` scripts 部分
- **问题描述**: 
  - 超过100个npm scripts
  - 大量重复的 `stable:*` 脚本（每个都调用 `npm run build`）
  - `test:raw`、`test`、`test:unit` 等脚本职责不清
  - `build` 和 `build:test` 混用可能导致测试结果不一致
- **建议修复**: 
  1. 使用 `npm run build` 统一构建脚本
  2. 合并重复的 stable 脚本到通用运行器
  3. 简化测试脚本层级
  4. 文档化每个脚本的用途
  5. 考虑使用 `--` 传递参数替代大量重复脚本

#### 314. [部署] [中严重] [deploy/chaos 目录内容单薄]
- **文件/路径**: `deploy/chaos/`
- **问题描述**: 
  - 该目录包含4个YAML文件：
    - `network-delay.yaml`
    - `pod-kill.yaml`
    - `postgres-disconnect.yaml`
    - `redis-disconnect.yaml`
  - 这些是基础 chaos 场景，但可能不足以覆盖所有场景
  - 需要扩展以覆盖更多故障场景
- **建议修复**: 
  1. 添加更多 chaos 场景：
     - CPU 压力
     - 内存压力
     - 磁盘压力
     - 网络分区
     - 服务超时
  2. 文档化每个场景的预期影响
  3. 验证 chaos 测试能够正常运行

#### 315. [文档] [低严重] [CLAUDE.md 内容可能过时]
- **文件/路径**: `CLAUDE.md`
- **问题描述**: 
  - CLAUDE.md 提到 `src/core/runtime/` 是兼容性目录
  - 但没有说明五层架构与 `src/core/runtime/` 的关系
  - 没有说明符号链接的存在和用途
- **建议修复**: 
  1. 更新 CLAUDE.md 记录符号链接
  2. 说明 `src/core/runtime/` 与五层架构的关系
  3. 添加架构决策记录（ADR）引用

### 总结

本次自动Review（第六轮 - 全面深度检查）发现了57个新问题。

**高优先级 (需要立即处理)**:
1. 18个巨型文件超过1000行，需要系统性重构
2. 测试排除列表过长（114个模式），需要逐个调查原因
3. `durable-event-bus-async.test.ts` 和 `budget-allocator.test.ts` 测试失败
4. npm 依赖存在多个高严重漏洞
5. `.gitignore` 缺少 `dist_*`、`:memory:*`、`.audit/` 模式
6. `process.env` 952处访问缺少统一管理
7. Buffer.from 安全风险

**中优先级**:
1. stryker mutation testing 只覆盖7个文件
2. config/security/dev.json 与 test.json 行为不一致
3. docker-compose.yml 安全配置部分完成
4. `src/core/runtime` 与五层架构并存造成混淆
5. 5个符号链接未被文档化
6. HA 和 Lease 模块职责重叠
7. EventEmitter vs TypedEventBus 混用
8. npm scripts 超过100个，需要简化
9. deploy/chaos 和 deploy/runbooks 内容单薄
10. tool-executor 目录35个文件，职责过多

**低优先级**:
1. tsconfig.temp.json 存在但未清理
2. hardcoded 配置注释残留
3. TODO/FIXME 约17处需要处理
4. .c8rc.json 和 stryker.config.mjs 配置分离
5. UI package.json 与主 package.json 分离管理

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 35 | 35 | 15 | 85 |
| 测试 | 4 | 8 | 2 | 14 |
| 配置 | 4 | 18 | 7 | 29 |
| 安全 | 4 | 6 | 0 | 10 |
| 文档 | 1 | 2 | 2 | 5 |
| UI | 0 | 1 | 2 | 3 |
| 部署 | 0 | 4 | 1 | 5 |
| **合计** | **48** | **74** | **29** | **151** |

### 优先修复建议

**立即处理 (本周内)**:
1. 运行 `npm audit` 并修复高严重漏洞
2. 调查并修复 `durable-event-bus-async.test.ts` 和 `budget-allocator.test.ts`
3. 添加 `dist_*`、`:memory:*`、`.audit/` 到 `.gitignore`
4. 统一 `config/security/test.json` 配置

**短期内处理 (本月内)**:
1. 创建巨型文件重构计划，优先处理前5个最大的文件
2. 创建统一配置管理模块减少 `process.env` 访问
3. 调查测试排除列表的根本原因
4. 扩展 stryker mutation 测试覆盖
5. 迁移 EventEmitter 到 TypedEventBus

**长期规划**:
1. 系统性重构18个巨型文件
2. 清理 `src/core/runtime` 目录或明确其用途
3. 简化 npm scripts 到合理数量
4. 完善 deploy 目录（chaos、runbooks、Kubernetes）
5. 建立依赖版本更新和漏洞扫描机制

*Review generated: 2026-05-13*

---

## [2026-05-13] 自动Review报告（补充）

### 发现的问题

#### 1. [安全] [高严重] [npm audit 发现7个安全漏洞]
- **文件/路径**: `package.json` / `package-lock.json`
- **问题描述**: 
  - `@opentelemetry/exporter-prometheus` < 0.217.0: Prometheus exporter process crash via malformed HTTP request (GHSA-q7rr-3cgh-j5r3)
  - `@protobufjs/utf8` <= 1.1.0: overlong UTF-8 decoding (GHSA-q6x5-8v7m-xcrf)
  - `@xmldom/xmldom` <= 0.8.12: Multiple XML injection vulnerabilities (GHSA-wh4c-j3r5-mjhp, etc.)
  - `fast-uri` <= 3.1.1: Path traversal via percent-encoded dot segments (GHSA-q3j6-qgpj-74h6)
  - `protobufjs` <= 7.5.5: Multiple DoS and code injection vulnerabilities (GHSA-2pr8-phx7-x9h3, etc.)
  - 共7个漏洞 (1 moderate, 6 high)
- **建议修复**: 
  1. 运行 `npm audit fix --force` 修复部分问题
  2. 注意：`@opentelemetry/exporter-prometheus` 和 `xml-crypto` 的修复会引入breaking changes
  3. 建议先评估breaking change影响，再决定修复策略

#### 2. [源代码] [高严重] [巨型源文件未得到有效重构]
- **文件/路径**: 多个源文件超过1000行
- **问题描述**: 
  - `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
  - `src/platform/contracts/executable-contracts/index.ts` (2169行)
  - `src/interaction/nl-gateway/index.ts` (1669行)
  - `src/platform/five-plane-orchestration/oapeflir/oapeflir-loop-service.ts` (1497行)
  - `src/platform/five-plane-orchestration/routing/intake-router.ts` (1328行)
  - 前5个巨型文件总行数超过8000行
- **建议修复**: 
  1. 制定系统性重构计划
  2. 优先拆分超过1500行的文件
  3. 将大文件拆分为多个职责单一的模块
  4. 确保拆分后每个模块有独立的测试

#### 3. [源代码] [高严重] [src/platform/five-plane-execution/tool-executor/ 职责过多]
- **文件/路径**: `src/platform/five-plane-execution/tool-executor/`
- **问题描述**: 
  - 该目录包含32个.ts文件
  - 包含工具执行、编辑替换、补丁DSL、skill执行、MCP防护等众多职责
  - 单个文件如 `command-executor.ts` (24468行)、`edit-replacement-service.ts` (24108行) 过大
  - 违反了单一职责原则
- **建议修复**: 
  1. 将tool-executor拆分为多个子模块：
     - skill-execution/ (skill执行相关)
     - patch-dsl/ (补丁DSL相关)
     - edit-replacement/ (编辑替换相关)
     - mcp-tools/ (MCP工具相关)
  2. 进一步拆分巨型文件
  3. 为每个子模块创建独立的index.ts导出

#### 4. [测试] [高严重] [测试文件排除列表过长表明存在系统性测试问题]
- **文件/路径**: `tsconfig.json` exclude段
- **问题描述**: 
  - exclude中包含114个测试文件模式
  - 排除的测试包括：extended测试、integration测试、e2e测试等
  - 表明测试基础设施可能存在循环依赖、污染或不稳定问题
- **建议修复**: 
  1. 逐个调查每个排除模式的原因
  2. 修复根本问题而非持续排除
  3. 如果是循环依赖，重构模块边界
  4. 如果是不稳定测试，修复测试隔离问题

#### 5. [配置] [高严重] [.gitignore 缺少关键模式]
- **文件/路径**: `.gitignore`
- **问题描述**: 
  - 缺少 `dist_*` 模式（项目中存在 dist_1964, dist_test 等目录）
  - 缺少 `:memory:*` 模式（项目中存在大量 :memory: 文件）
  - 缺少 `.audit/` 模式（项目中有 .audit 目录）
  - 当前git status显示这些文件被追踪
- **建议修复**: 
  在.gitignore末尾添加：
  ```
  dist_*/
  :memory:*
  .audit/
  ```

#### 6. [源代码] [高严重] [直接访问process.env高达143处]
- **文件/路径**: 整个src目录
- **问题描述**: 
  - 143处直接访问process.env
  - 缺少统一的配置管理模块
  - 配置散落在各处，难以追踪和审计
  - 可能导致运行时配置错误
- **建议修复**: 
  1. 创建统一的配置管理模块 (如 `src/platform/shared/config/` )
  2. 将所有环境变量访问集中到该模块
  3. 使用zod schema验证配置
  4. 为敏感配置字段提供类型安全访问

#### 7. [源代码] [中严重] [符号链接使用可能影响构建稳定性]
- **文件/路径**: `src/platform/`
- **问题描述**: 
  - `control-plane -> five-plane-control-plane`
  - `execution -> five-plane-execution`
  - `interface -> five-plane-interface`
  - `orchestration -> five-plane-orchestration`
  - `state-evidence -> five-plane-state-evidence`
  - 5个符号链接未被文档化
  - 可能导致：
    - IDE路径解析问题
    - Git历史追踪困难
    - 跨平台构建问题
    - 符号链接在某些文件系统下不可用
- **建议修复**: 
  1. 在CLAUDE.md中记录所有符号链接
  2. 考虑是否可以用实际目录替代符号链接
  3. 如果必须使用符号链接，确保文档化并说明原因
  4. 在CI中验证符号链接完整性

#### 8. [源代码] [中严重] [src/core/runtime 与五层架构并存造成架构混乱]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - CLAUDE.md明确指出 `src/core/runtime/` 是兼容性目录
  - 但项目仍在使用这些模块
  - 与新的五层架构 (five-plane-*) 并存造成混淆
  - 职责边界不清晰
- **建议修复**: 
  1. 明确 src/core/runtime 的迁移计划
  2. 将core/runtime中的功能迁移到五层架构
  3. 或明确core/runtime的保留用途和未来废弃时间表
  4. 更新CLAUDE.md说明两者的关系和迁移策略

#### 9. [配置] [中严重] [config/security/ 目录内容单薄]
- **文件/路径**: `config/security/`
- **问题描述**: 
  - 只有6个JSON文件 (default.json, dev.json, pre-prod.json, prod.json, staging.json, test.json)
  - 每个文件只有35-71字节，内容相似度高
  - 只有 `threat-matrix.json` 包含实际安全配置
  - 缺少实际的安全策略配置
- **建议修复**: 
  1. 扩展安全配置文件内容
  2. 添加rate limiting配置
  3. 添加IP白名单/黑名单配置
  4. 添加API key轮换策略
  5. 添加安全日志配置

#### 10. [部署] [中严重] [deploy/runbooks 内容单薄]
- **文件/路径**: `deploy/runbooks/`
- **问题描述**: 
  - 只有 `production-alert-runbook.md` 一个文件
  - 内容不够详细
  - 缺少实际运维操作指南
- **建议修复**: 
  1. 添加更多runbook：
     - 服务重启程序
     - 数据库故障处理
     - 缓存失效处理
     - 网络分区应对
     - 内存泄漏排查
  2. 为每个runbook包含具体命令和预期输出
  3. 添加错误处理步骤

#### 11. [部署] [中严重] [deploy/terraform 内容不完整]
- **文件/路径**: `deploy/terraform/`
- **问题描述**: 
  - 只有 `main.tf` 和 `modules/` 目录
  - 缺少完整的Terraform配置
  - 缺少变量定义文件
  - 缺少状态管理配置
  - 缺少后端配置
- **建议修复**: 
  1. 添加完整的Terraform模块
  2. 添加variables.tf定义
  3. 添加outputs.tf定义
  4. 配置文件存储后端（S3/GCS）
  5. 添加远程状态配置

#### 12. [部署] [中严重] [deploy/helm 内容不完整]
- **文件/路径**: `deploy/helm/`
- **问题描述**: 
  - 只有 `automatic-agent/` 子目录
  - 缺少完整的Helm chart结构
  - 缺少values.yaml
  - 缺少templates/
  - 缺少Chart.yaml
- **建议修复**: 
  1. 完善Helm chart结构
  2. 添加生产级values文件
  3. 添加ingress配置
  4. 添加service monitor配置
  5. 添加network policy配置

#### 13. [测试] [中严重] [测试辅助代码量过大]
- **文件/路径**: `tests/helpers/` (78个文件)
- **问题描述**: 
  - 78个helpers文件说明测试基础设施复杂
  - 可能表明：
    - 被测代码耦合度高
    - 测试设置复杂
    - Mock对象管理困难
  - 降低了测试的可维护性
- **建议修复**: 
  1. 审查并简化helpers结构
  2. 减少helpers间的依赖
  3. 考虑将复杂helpers重构为测试工具库
  4. 确保每个helper职责单一

#### 14. [文档] [中严重] [UI项目缺少文档]
- **文件/路径**: `ui/README.md`
- **问题描述**: 
  - UI的README内容可能过时或不完整
  - 缺少组件文档
  - 缺少状态管理说明
  - 缺少路由配置说明
- **建议修复**: 
  1. 更新UI README包含完整的项目结构说明
  2. 添加组件库文档
  3. 添加状态管理架构说明
  4. 添加测试策略说明

#### 15. [源代码] [低严重] [tsconfig.temp.json 存在但未使用]
- **文件/路径**: `tsconfig.temp.json`
- **问题描述**: 
  - 该文件存在但似乎未被使用
  - 可能为临时配置文件残留
- **建议修复**: 
  1. 确认是否需要该文件
  2. 如果不需要，删除该文件
  3. 如果需要，文档化其用途

#### 16. [源代码] [低严重] [hardcoded配置注释残留]
- **文件/路径**: 多个配置文件
- **问题描述**: 
  - 存在hardcoded配置注释残留
  - 配置值可能被硬编码在代码中
- **建议修复**: 
  1. 清理所有hardcoded配置
  2. 将所有配置移到config/目录
  3. 使用环境变量覆盖

#### 17. [源代码] [低严重] [EventEmitter vs TypedEventBus 混用]
- **文件/路径**: 多个事件处理模块
- **问题描述**: 
  - 项目同时使用EventEmitter和TypedEventBus
  - 两种方式的行为和类型安全不同
  - 可能导致事件处理不一致
- **建议修复**: 
  1. 统一使用TypedEventBus
  2. 逐步迁移EventEmitter到TypedEventBus
  3. 在架构文档中明确事件系统规范

#### 18. [源代码] [低严重] [HA 和 Lease 模块职责重叠]
- **文件/路径**: `src/platform/five-plane-execution/ha/` 和 `src/platform/five-plane-execution/lease/`
- **问题描述**: 
  - HA (高可用) 和 Lease (租约) 模块职责有重叠
  - 可能导致维护困难
  - 边界不清晰
- **建议修复**: 
  1. 明确HA和Lease的职责边界
  2. 如果有重叠，考虑合并
  3. 文档化两个模块的交互方式

#### 19. [配置] [低严重] [.env.example 包含大量未使用变量]
- **文件/路径**: `.env.example`
- **问题描述**: 
  - 包含347行配置
  - 其中很多变量未被使用
  - 可能导致配置混淆
- **建议修复**: 
  1. 清理未使用的配置变量
  2. 按功能分组配置变量
  3. 添加每个变量的说明注释
  4. 标记必填和可选变量

#### 20. [安全] [低严重] [潜在敏感信息硬编码]
- **文件/路径**: 多个CLI文件
- **问题描述**: 
  - 发现文件中有 "password", "secret", "apiKey" 等字符串
  - 需要确保这些不是硬编码的敏感信息
- **建议修复**: 
  1. 审查所有包含敏感关键词的文件
  2. 确保所有敏感信息来自环境变量或安全存储
  3. 添加敏感信息扫描到CI

#### 21. [测试] [低严重] [Golden测试覆盖可能不足]
- **文件/路径**: `tests/golden/` (48个文件)
- **问题描述**: 
  - 48个golden文件可能不足以覆盖所有关键场景
  - 缺少对关键业务流程的golden测试
- **建议修复**: 
  1. 审查关键业务流程是否都有golden测试
  2. 扩展golden测试覆盖
  3. 添加更多回归测试场景

#### 22. [部署] [低严重] [Kubernetes配置缺失]
- **文件/路径**: `deploy/`
- **问题描述**: 
  - 缺少Kubernetes部署配置
  - 只有Helm chart的一部分
  - 缺少deployment.yaml, service.yaml等
- **建议修复**: 
  1. 添加完整的Kubernetes manifests
  2. 或确保Helm chart可以生成完整的manifests
  3. 添加ingress配置
  4. 添加configmap和secret模板

#### 23. [源代码] [低严重] [.DS_Store文件存在]
- **文件/路径**: 多个目录
- **问题描述**: 
  - 项目中存在 .DS_Store 文件（从ls输出可见）
  - 这些是macOS系统文件，不应存在于项目中
- **建议修复**: 
  1. 添加 `.DS_Store` 到 .gitignore（如果没有）
  2. 清理现有 .DS_Store 文件
  3. 在CI中添加检查防止提交

#### 24. [配置] [低严重] [docker-compose.yml 缺少资源限制说明]
- **文件/路径**: `docker-compose.yml`
- **问题描述**: 
  - 虽然设置了CPU和内存限制，但缺少说明
  - 缺少对限制来源的解释
- **建议修复**: 
  1. 在docker-compose.yml顶部添加注释说明资源限制策略
  2. 添加开发环境vs生产环境配置差异说明
  3. 文档化如何调整资源限制

### 总结

本次补充Review（第七轮 - 2026-05-13）发现了24个新问题。

**高优先级 (需要立即处理)**:
1. npm audit发现7个安全漏洞（1 moderate, 6 high）
2. 巨型源文件未得到有效重构（超过8000行在前5个文件中）
3. tool-executor目录32个文件，职责过多
4. 测试文件排除列表过长（114个模式）
5. .gitignore缺少关键模式（dist_*, :memory:*, .audit/）
6. process.env直接访问高达143处

**中优先级**:
1. 5个符号链接未被文档化
2. src/core/runtime与五层架构并存造成混乱
3. config/security/内容单薄
4. deploy/runbooks内容单薄
5. deploy/terraform内容不完整
6. deploy/helm内容不完整
7. 测试helpers过大（78个文件）
8. UI项目缺少文档

**低优先级**:
1. tsconfig.temp.json存在但未使用
2. hardcoded配置注释残留
3. EventEmitter vs TypedEventBus混用
4. HA和Lease模块职责重叠
5. .env.example包含大量未使用变量
6. 潜在敏感信息硬编码
7. Golden测试覆盖可能不足
8. Kubernetes配置缺失
9. .DS_Store文件存在
10. docker-compose.yml缺少资源限制说明

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 37 | 37 | 17 | 91 |
| 测试 | 4 | 9 | 3 | 16 |
| 配置 | 4 | 20 | 9 | 33 |
| 安全 | 5 | 6 | 1 | 12 |
| 文档 | 1 | 3 | 2 | 6 |
| UI | 0 | 2 | 2 | 4 |
| 部署 | 0 | 6 | 3 | 9 |
| **合计** | **51** | **83** | **37** | **171** |

### 优先修复建议

**立即处理 (本周内)**:
1. 运行 `npm audit` 并修复高严重漏洞（注意breaking changes）
2. 添加 `dist_*`, `:memory:*`, `.audit/` 到 .gitignore
3. 清理现有被追踪的临时文件和目录
4. 调查测试排除列表的根本原因

**短期内处理 (本月内)**:
1. 创建巨型文件重构计划
2. 拆分tool-executor目录
3. 创建统一配置管理模块减少process.env访问
4. 文档化符号链接的存在和用途
5. 扩展config/security/内容

**长期规划**:
1. 系统性重构巨型源文件
2. 清理或明确src/core/runtime的用途
3. 完善deploy目录（runbooks, terraform, helm, kubernetes）
4. 统一EventEmitter到TypedEventBus
5. 明确HA和Lease模块的职责边界

*Review generated: 2026-05-13*

---

## [2026-05-14] 自动Review报告（第八轮）

### 发现的新问题

#### 1. [配置] [高严重] [.gitignore 缺少关键临时文件模式]
- **文件/路径**: `.gitignore`
- **问题描述**: 
  - 当前 .gitignore 未包含项目根目录存在的临时文件
  - 缺少 `dist_*` 模式（项目根目录存在 dist_1964, dist_test 等目录）
  - 缺少 `:memory:*` 模式（项目根目录存在大量 :memory:aa-truth-* 文件）
  - 缺少 `.audit/` 模式
  - 这些临时文件/目录已在git status中显示为已修改
- **建议修复**: 
  在 .gitignore 末尾添加：
  ```
  dist_*/
  :memory:*
  .audit/
  ```

#### 2. [源代码] [高严重] [符号链接未在 .gitignore 中忽略]
- **文件/路径**: `src/platform/`
- **问题描述**: 
  - 存在5个符号链接：interface, execution, state-evidence, orchestration, control-plane
  - 符号链接指向 five-plane-* 对应目录
  - 这些符号链接未被 .gitignore 忽略
  - 可能导致跨平台构建问题
- **建议修复**: 
  1. 确认符号链接是否必须存在
  2. 如果可以接受，考虑用实际目录替代
  3. 或将符号链接添加到 .gitignore
  4. 在 CLAUDE.md 中明确记录符号链接的存在和用途

#### 3. [源代码] [高严重] [.DS_Store 文件存在]
- **文件/路径**: 项目根目录和可能的其他目录
- **问题描述**: 
  - 项目根目录存在 `.DS_Store` 文件
  - macOS系统文件已被追踪到版本控制
- **建议修复**: 
  1. 确保 .gitignore 包含 `.DS_Store`
  2. 运行 `git rm --cached .DS_Store` 移除已追踪文件
  3. 添加到 .gitignore 全局忽略

#### 4. [源代码] [高严重] [根目录存在未清理的临时 :memory: 文件]
- **文件/路径**: 项目根目录
- **问题描述**: 
  - 根目录存在多个 `:memory:aa-truth-*` 文件
  - 这些是SQLite内存数据库的临时文件
  - 单个文件大小约 2MB，总计占用较大空间
- **建议修复**: 
  1. 将 `:memory:*` 添加到 .gitignore
  2. 清理现有文件 `rm :memory:*`
  3. 在 CI 中确保这些文件不被提交

#### 5. [配置] [中严重] [UI 项目缺少完整文档]
- **文件/路径**: `ui/README.md`
- **问题描述**: 
  - UI 项目只有一个基础的 README.md
  - 缺少组件库文档
  - 缺少状态管理架构说明
  - 缺少路由配置说明
- **建议修复**: 
  1. 扩展 UI README 包含完整的项目结构
  2. 添加组件库使用文档
  3. 添加状态管理（如果使用 Redux/Zustand 等）说明
  4. 添加测试策略说明

#### 6. [源代码] [中严重] [bash 目录不存在]
- **文件/路径**: 项目根目录
- **问题描述**: 
  - 项目缺少 bash/ 目录
  - 很多项目在此目录存放运维脚本
- **建议修复**: 
  1. 如果不需要 bash 脚本，忽略此问题
  2. 如果需要，创建 bash/ 目录并添加脚本

#### 7. [配置] [中严重] [config/environments/ 内容可能不完整]
- **文件/路径**: `config/environments/`
- **问题描述**: 
  - environments 配置目录可能需要检查完整性
  - 缺少开发/测试/生产环境的差异化配置
- **建议修复**: 
  1. 检查各环境的配置文件
  2. 确保敏感配置在不同环境正确隔离
  3. 验证环境变量覆盖机制

#### 8. [源代码] [中严重] [.tmp/ 目录包含大量临时文件]
- **文件/路径**: `.tmp/` (307个条目)
- **问题描述**: 
  - .tmp 目录包含大量临时构建/测试文件
  - 这些文件可能包含敏感信息或占用大量空间
- **建议修复**: 
  1. 确保 .tmp/ 在 .gitignore 中
  2. 定期清理 .tmp 目录
  3. 在 CI 开始时清理

#### 9. [源代码] [中严重] [.test-db/ 目录存在]
- **文件/路径**: `.test-db/`
- **问题描述**: 
  - 测试数据库目录存在
  - 可能包含测试数据或临时数据库文件
- **建议修复**: 
  1. 确保 .test-db/ 在 .gitignore 中
  2. 检查其中是否有需要保留的配置

#### 10. [源代码] [中严重] [src/platform/index.ts 可能有循环依赖问题]
- **文件/路径**: `src/platform/index.ts`
- **问题描述**: 
  - index.ts 是平台模块的统一导出
  - 如果存在循环依赖，可能通过此文件暴露
- **建议修复**: 
  1. 检查 index.ts 的导入结构
  2. 使用 `npm run typecheck` 检测循环依赖
  3. 必要时拆分 index.ts 为多个入口点

#### 11. [测试] [中严重] [14个测试文件仍然失败]
- **文件/路径**: 多个测试文件（详见 .audit/quality.md）
- **问题描述**: 
  - execution-dispatch-service-async.test.ts
  - nodeRunId-canonization.test.ts
  - runtime-plan-executor.test.ts
  - worker-pool-comprehensive.test.ts
  - access-model.test.ts, field-encryption.test.ts
  - budget-allocator.test.ts
  - web-fetch.test.ts
  - worker-drain-protocol.test.ts
  - r20-05-parallelism-limit.test.ts
  - node-run-checkpoint-migration.test.ts
  - channel-gateway-service-coverage.test.ts
  - execution-adapter.test.ts
  - durable-event-bus-async.test.ts
- **建议修复**: 
  1. 逐个分析和修复失败的测试
  2. 优先修复影响核心功能的测试
  3. 考虑是否需要重构被测代码

#### 12. [配置] [中严重] [docker-compose.yml 缺少资源限制说明]
- **文件/路径**: `docker-compose.yml`
- **问题描述**: 
  - 虽然设置了 CPU 和内存限制
  - 但缺少注释说明限制来源和策略
- **建议修复**: 
  1. 在文件顶部添加注释说明资源限制策略
  2. 添加开发环境 vs 生产环境配置差异
  3. 文档化如何调整资源限制

#### 13. [文档] [中严重] [docs_zh/architecture/ 缺少最近更新]
- **文件/路径**: `docs_zh/architecture/`
- **问题描述**: 
  - 架构文档可能未反映最近的代码变更
  - 需要确保文档与实现同步
- **建议修复**: 
  1. 对比最新代码变更和文档
  2. 更新架构文档以反映最新实现
  3. 建立文档与代码的一致性检查机制

#### 14. [部署] [中严重] [deploy/ 目录内容不完整]
- **文件/路径**: `deploy/`
- **问题描述**: 
  - runbooks 只有 1 个文件
  - terraform 配置不完整
  - helm chart 不完整
  - 缺少 kubernetes 配置
- **建议修复**: 
  1. 扩展 runbooks 添加更多运维场景
  2. 完善 terraform 模块和配置
  3. 完善 helm chart
  4. 添加 kubernetes manifests

#### 15. [源代码] [低严重] [.claude/scheduled_tasks.json 在 git status 中显示修改]
- **文件/路径**: `.claude/scheduled_tasks.json`
- **问题描述**: 
  - Claude Code 的调度任务文件被修改
  - 这应该是本地文件，不应提交到版本控制
- **建议修复**: 
  1. 将 `.claude/` 添加到 .gitignore
  2. 停止追踪该文件

#### 16. [源代码] [低严重] [多个配置文件存在未使用的临时变体]
- **文件/路径**: 多个位置
- **问题描述**: 
  - 如 `tsconfig.temp.json` 可能未使用
  - 可能有其他临时配置文件残留
- **建议修复**: 
  1. 确认 tsconfig.temp.json 是否需要
  2. 删除不需要的临时配置文件
  3. 清理配置目录

#### 17. [配置] [低严重] [.env.example 包含大量可能未使用的变量]
- **文件/路径**: `.env.example` (347行)
- **问题描述**: 
  - 包含大量环境变量示例
  - 其中很多可能已过时或未使用
  - 可能导致配置混淆
- **建议修复**: 
  1. 审查并清理未使用的变量
  2. 按功能分组变量
  3. 为每个变量添加说明注释
  4. 标记必填和可选变量

#### 18. [源代码] [低严重] [直接访问 process.env 高达143处]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - 143处直接访问 process.env
  - 配置散落在各处，难以追踪
  - 缺少统一的配置管理
- **建议修复**: 
  1. 创建统一的配置管理模块
  2. 将环境变量访问集中到该模块
  3. 使用 zod schema 验证配置
  4. 为敏感配置提供类型安全访问

#### 19. [源代码] [低严重] [HA 和 Lease 模块职责可能重叠]
- **文件/路径**: `src/platform/five-plane-execution/ha/` 和 `src/platform/five-plane-execution/lease/`
- **问题描述**: 
  - 两个模块都处理分布式一致性
  - 职责边界不清晰
- **建议修复**: 
  1. 明确两个模块的职责边界
  2. 如有重叠，考虑合并或重构
  3. 文档化两个模块的交互方式

#### 20. [源代码] [低严重] [EventEmitter 和 TypedEventBus 混用]
- **文件/路径**: 多个事件处理模块
- **问题描述**: 
  - 项目同时使用两种事件系统
  - TypedEventBus 应该成为标准
- **建议修复**: 
  1. 统一使用 TypedEventBus
  2. 制定迁移计划
  3. 在架构文档中明确事件系统规范

### 总结

本次补充Review（第八轮 - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. .gitignore 缺少关键临时文件模式（dist_*, :memory:*, .audit/）
2. 符号链接未在项目中正确处理
3. .DS_Store 文件存在并被追踪
4. 根目录存在未清理的临时 :memory: 文件

**中优先级**:
1. UI 项目缺少完整文档
2. bash 目录不存在
3. config/environments/ 内容可能不完整
4. .tmp/ 目录包含大量临时文件
5. .test-db/ 目录存在
6. src/platform/index.ts 可能有循环依赖问题
7. 14个测试文件仍然失败
8. docker-compose.yml 缺少资源限制说明
9. docs_zh/architecture/ 缺少最近更新
10. deploy/ 目录内容不完整

**低优先级**:
1. .claude/scheduled_tasks.json 在 git status 中显示修改
2. 多个配置文件存在未使用的临时变体
3. .env.example 包含大量可能未使用的变量
4. 直接访问 process.env 高达143处
5. HA 和 Lease 模块职责可能重叠
6. EventEmitter 和 TypedEventBus 混用

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 39 | 40 | 19 | 98 |
| 测试 | 4 | 9 | 3 | 16 |
| 配置 | 4 | 22 | 11 | 37 |
| 安全 | 5 | 6 | 1 | 12 |
| 文档 | 1 | 4 | 2 | 7 |
| UI | 0 | 2 | 2 | 4 |
| 部署 | 0 | 7 | 3 | 10 |
| **合计** | **53** | **90** | **41** | **184** |

### 优先修复建议

**立即处理 (本周内)**:
1. 更新 .gitignore 添加 `dist_*`, `:memory:*`, `.audit/`, `.claude/`
2. 清理根目录的临时文件（:memory:*, .DS_Store）
3. 运行 `git rm --cached .DS_Store :memory:*` 停止追踪这些文件
4. 分析并修复14个失败的测试文件

**短期内处理 (本月内)**:
1. 明确符号链接的处理策略（保留或替换为实际目录）
2. 扩展 UI 项目的文档
3. 创建统一配置管理模块减少 process.env 访问
4. 完善 deploy/ 目录内容

**长期规划**:
1. 系统性重构巨型源文件（超过1000行的文件）
2. 清理或明确 src/core/runtime 的用途
3. 统一 EventEmitter 到 TypedEventBus
4. 明确 HA 和 Lease 模块的职责边界
5. 建立文档与代码的一致性检查机制

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十五轮 - 构建测试与类型系统Review）

### 发现的新问题

#### 1. [构建] [高严重] [无 TypeScript 增量编译缓存]
- **文件/路径**: `tsconfig.json`
- **问题描述**: 
  - 无 `incremental: true` 配置
  - 无 `tsbuildinfo` 文件
  - 每次构建是完整重建（9.6秒）
  - TypeScript 无法使用增量编译
- **建议修复**: 
  1. 在 tsconfig.json 添加 `"incremental": true`
  2. 添加 `"tsbuildinfo": ".tsbuildinfo"` 到 .gitignore
  3. 实现项目引用以支持并行构建

#### 2. [构建] [高严重] [76个 npm scripts 大部分重复构建]
- **文件/路径**: `package.json`
- **问题描述**: 
  - 40+ CLI scripts 都运行 `npm run build && node ...`
  - 每个脚本前都执行完整重建
  - 无法直接运行预构建的 CLI
  - 20+ stable:* 脚本遵循相同模式
- **建议修复**: 
  1. 创建 `build:cli` 脚本构建一次
  2. 让 CLI 脚本使用预构建的 dist 文件
  3. 添加 npm run dev 或类似的无需重建的执行方式

#### 3. [源代码] [高严重] [168处 any 类型使用]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - `stable-runner-factory.ts`: `opts: any` -> `any`
  - `billing-service-async.ts`: `input: any` 方法签名
  - 重试/超时工具使用 `(...args: any[]) => Promise<any>`
  - 绕过类型安全
- **建议修复**: 
  1. 使用泛型替代 any
  2. 替换为具体的输入类型
  3. 添加 ESLint 规则禁止 any

#### 4. [源代码] [高严重] [38处 @ts-ignore 指令]
- **文件/路径**: 多个文件
- **问题描述**: 
  - 14个 stability 相关文件有 `@ts-ignore ExecutionRecord type mismatch`
  - 5个 harness-sdk 文件有 `Partial<HarnessRun> doesn't have all required properties`
  - 2个文件有 `exactOptionalPropertyTypes` 问题
- **建议修复**: 
  1. 修复 ExecutionRecord 类型不匹配
  2. 完成 HarnessRun 类型的必需属性
  3. 移除 @ts-ignore 使用

#### 5. [源代码] [高严重] [ExecutionRecord 类型不匹配]
- **文件/路径**: `src/platform/stability/stable-evidence-bundle-support.ts:783` 等14个文件
- **问题描述**: 
  - 插入 execution record 时 TypeScript 拒绝类型
  - `ExecutionRecord` 接口要求新字段但 insert 对象缺失
  - 或 store 的 insert 方法接受不同类型
- **建议修复**: 
  1. 对齐 ExecutionRecord 接口和 store 类型
  2. 添加缺失字段到 insert 对象
  3. 或调整 store 的 insert 方法类型签名

#### 6. [源代码] [中严重] [30+处 as unknown as 双类型转换]
- **文件/路径**: `division-loader.ts`, `domain-baseline-catalog.ts`, `harness-sdk`
- **问题描述**: 
  - 完全绕过类型安全
  - 通常是类型架构问题的信号
- **建议修复**: 
  1. 修复上游类型问题
  2. 使用更具体的类型断言
  3. 重构类型层次结构

#### 7. [构建] [中严重] [rimraf  extraneous - 未在 package.json 声明]
- **文件/路径**: `node_modules/rimraf`
- **问题描述**: 
  - rimraf@6.1.3 存在于 node_modules 但未在 package.json 声明
  - 可能导致依赖问题
- **建议修复**: 
  1. 添加到 dependencies 或 devDependencies
  2. 或移除并使用其他清理方式

#### 8. [测试] [高严重] [178处直接 process.env 变更无抽象]
- **文件/路径**: `tests/unit/domains/registry/plugin-spi-registry.test.ts:640-687`
- **问题描述**: 
  - 直接读取/写入 process.env
  - 在 beforeEach/afterEach 中手动保存/恢复
  - 无 helper 抽象
- **建议修复**: 
  1. 创建 env helper 函数
  2. 统一保存和恢复模式
  3. 添加测试隔离验证

#### 9. [测试] [高严重] [21,998处 unlinkSync 调用无集中清理]
- **文件/路径**: 多个测试文件
- **问题描述**: 
  - 每个测试重新实现清理逻辑
  - 无集中清理 utility
  - 可能导致 EBUSY (Windows) 或并发访问问题
- **建议修复**: 
  1. 创建集中的测试清理 utility
  2. 使用 afterEach 确保清理
  3. 考虑使用临时目录

#### 10. [测试] [中严重] [无统一 mocking 框架]
- **文件/路径**: `tests/` 多个目录
- **问题描述**: 
  - 10,579 处 vi.fn, jest.mock, sinon 等调用
  - 无统一的 mocking 框架可见
  - 模式不一致
- **建议修复**: 
  1. 建立统一的 mock factory
  2. 标准化 mock 模式
  3. 添加 mocking 最佳实践文档

#### 11. [测试] [中严重] [Singleton 状态重置未标准化]
- **文件/路径**: `tests/unit/platform/five-plane-execution/dispatcher.test.ts`
- **问题描述**: 
  - 测试通过特定函数重置 singletons
  - 每个测试重复自己的 reset 逻辑
  - 无统一的 resetAllSingletons()
- **建议修复**: 
  1. 创建统一的 singleton reset 机制
  2. 在 afterEach 中自动调用
  3. 文档化 singleton 重置要求

#### 12. [测试] [低严重] [无分支覆盖率要求]
- **文件/路径**: `.c8rc.json`
- **问题描述**: 
  - `"100": false` - 无覆盖率要求
  - 只测量源代码，不测量测试文件
- **建议修复**: 
  1. 添加分支覆盖率阈值
  2. 在 CI 中强制执行覆盖率 gate
  3. 关注关键代码路径

#### 13. [网络] [高严重] [ScopedExternalAccessSandbox performHttpRequest 无超时]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/scoped-external-access-sandbox.ts:289`
- **问题描述**: 
  - fetch 调用无 timeout 参数
  - 挂起的请求可能永久阻塞
- **建议修复**: 
  1. 添加 AbortController timeout
  2. 使用标准的超时模式
  3. 记录超时配置

#### 14. [网络] [高严重] [OIDC 服务 fetch 调用无超时]
- **文件/路径**: `src/org-governance/sso-scim/oidc/oidc-service.ts:565,272`
- **问题描述**: 
  - OIDC token 交换无 retry logic
  - 无 timeout/abort signal
  - 认证流程可能挂起
- **建议修复**: 
  1. 添加 fetch timeout
  2. 添加 retry logic
  3. 使用标准的超时配置

#### 15. [网络] [高严重] [plugin-runtime-child 替换 globalThis.fetch 为 no-op]
- **文件/路径**: `src/domains/registry/plugin-runtime-child.ts:112`
- **问题描述**: 
  ```typescript
  globalThis.fetch = (async () => {}) as unknown as typeof globalThis.fetch;
  ```
  - 插件上下文中完全替换 fetch 为空函数
  - 插件的所有出站 HTTP 请求都会失败
  - 看起来是有意为之但可能造成问题
- **建议修复**: 
  1. 确认这是有意行为
  2. 文档化 fetch 替换行为
  3. 确保不会意外影响其他代码

#### 16. [网络] [中严重] [无显式 http.Agent 配置]
- **文件/路径**: 全局
- **问题描述**: 
  - 无显式的 http.Agent/https.Agent 配置
  - 依赖 Node.js 默认连接池
  - 无 maxSockets 或 maxConnections 配置
  - 高吞吐场景可能出问题
- **建议修复**: 
  1. 添加 http Agent 配置
  2. 文档化连接池大小
  3. 监控连接使用情况

#### 17. [序列化] [中严重] [合同版本硬编码为字符串字面量]
- **文件/路径**: `src/platform/contracts/inter-plane-contract-gateway.ts:184`
- **问题描述**: 
  - `schemaVersion: "v4.3"` 硬编码
  - 难以追踪合同版本演变
- **建议修复**: 
  1. 提取到类型化常量或 enum
  2. 集中管理合同版本
  3. 添加版本兼容性检查

#### 18. [序列化] [低严重] [Timestamp = string 丢失精度]
- **文件/路径**: `src/platform/contracts/types/domain/primitives.ts:10`
- **问题描述**: 
  - `type Timestamp = string` 无类型精度
  - ISO 8601 字符串无时区信息
- **建议修复**: 
  1. 使用 Date 对象或 branded string 类型
  2. 明确时区处理约定
  3. 文档化时间戳语义

#### 19. [源代码] [中严重] [29处非空断言 !.]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - 使用 `!.` 断言非空
  - 可能导致运行时错误
- **建议修复**: 
  1. 使用可选链 `?.` 或空值合并 `??`
  2. 添加适当的 null 检查
  3. 减少非空断言使用

#### 20. [API] [中严重] [API 路由 schema 版本硬编码]
- **文件/路径**: `src/platform/five-plane-interface/api/federation-routing-service.ts`
- **问题描述**: 
  - Zod schema 验证存在
  - 但 schema 版本未与 contract 版本同步
- **建议修复**: 
  1. 从 contract 版本派生 schema 版本
  2. 添加版本一致性检查
  3. 文档化版本关系

### 总结

本次补充Review（第十五轮 - 构建测试与类型系统Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. 无 TypeScript 增量编译缓存（每次完整重建9.6秒）
2. 76个 npm scripts 大部分重复构建
3. 168处 any 类型使用（绕过类型安全）
4. 38处 @ts-ignore 指令
5. ExecutionRecord 类型不匹配（14个文件）
6. 178处直接 process.env 变更无抽象
7. ScopedExternalAccessSandbox performHttpRequest 无超时
8. OIDC 服务 fetch 调用无超时
9. plugin-runtime-child 替换 globalThis.fetch 为 no-op

**中优先级**:
1. 30+处 as unknown as 双类型转换
2. rimraf extraneous 未声明
3. 21,998处 unlinkSync 无集中清理
4. 无统一 mocking 框架
5. Singleton 状态重置未标准化
6. 无显式 http.Agent 配置
7. 合同版本硬编码为字符串
8. Timestamp = string 丢失精度
9. 非空断言使用

**低优先级**:
1. 无分支覆盖率要求
2. API 路由 schema 版本未同步

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 72 | 87 | 39 | 198 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **109** | **182** | **85** | **376** |

### 优先修复建议

**立即处理 (本周内)**:
1. 添加 TypeScript 增量编译 (`incremental: true`)
2. 重构 npm scripts 避免重复构建
3. 减少 any 类型使用到 <50
4. 移除所有 @ts-ignore 指令
5. 修复 ExecutionRecord 类型不匹配
6. 添加 fetch 超时到 OIDC 和 sandbox

**短期内处理 (本月内)**:
1. 创建集中的测试清理 utility
2. 建立统一的 mocking 框架
3. 标准化 singleton 重置机制
4. 添加 http.Agent 连接池配置
5. 提取合同版本到类型化常量

**长期规划**:
1. 实现完整的类型安全（无 any）
2. 建立测试基础设施标准
3. 实现构建缓存优化
4. 完善网络层超时和重试
5. 建立覆盖率门禁标准

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十四轮 - 日志事件与工作流Review）

### 发现的新问题

#### 1. [可观测性] [高严重] [StructuredLogEntry 无 requestId 字段]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - StructuredLogEntry 接口无 requestId 字段
  - 只有 correlationId，它回退到 traceId
  - 无法独立追踪单个 HTTP 请求
- **建议修复**: 
  1. 在 StructuredLogEntry 添加 requestId 字段
  2. 在 HTTP 中间件中生成和注入 requestId
  3. 确保 requestId 流经所有日志调用

#### 2. [可观测性] [高严重] [StructuredLogger data 字段无自动清洗]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - 如果开发者传递 `{ password: "..." }` 给 logger.info，密码会被记录
  - 无字段级脱敏机制
  - 依赖开发者手动排除敏感字段
- **建议修复**: 
  1. 添加 OBSERVE_OUTPUT_BLACKLIST 类似的黑名单
  2. 实现 redact/mask/sanitize 工具函数
  3. 在日志传输前自动清洗敏感字段

#### 3. [可观测性] [中严重] [日志中敏感数据无 PII 处理]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - 无 PII 重写工具
  - 开发者必须手动避免记录敏感数据
  - 存在意外记录敏感信息的风险
- **建议修复**: 
  1. 添加 PII 检测和重写工具
  2. 在文档中明确记录敏感字段列表
  3. 添加测试验证敏感数据不被记录

#### 4. [可观测性] [中严重] [59处 console.* 直接调用]
- **文件/路径**: 多个源文件
- **问题描述**: 
  - 应该使用 StructuredLogger
  - console 调用可能不被日志聚合系统收集
  - 难以追踪和关联
- **建议修复**: 
  1. 替换所有 console.* 为 StructuredLogger
  2. 添加 ESLint 规则禁止 console.*
  3. 保留少量允许的 CLI 输出

#### 5. [可观测性] [中严重] [部分 logger.warn 调用缺少结构化数据]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:122`
- **问题描述**: 
  ```typescript
  logger.warn("WebSocket connection rejected: missing subprotocol token")
  ```
  - 只有消息，无 data 对象
  - 难以关联和查询
- **建议修复**: 
  1. 添加 `{ data: { ... } }` 对象
  2. 确保所有日志包含上下文数据
  3. 添加日志审查工具

#### 6. [源代码] [高严重] [Harness while(true) 循环无硬性迭代上限]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:1442`
- **问题描述**: 
  ```typescript
  while (true) {
    // ...
    // 依赖 budget gate 和 guardrail vibration 退出
    // 如果两者都失败，循环永久继续
  }
  ```
  - 依赖 budget gate 和 guardrail vibration 退出
  - 无硬性迭代次数限制
  - 可能导致无限循环
- **建议修复**: 
  1. 添加硬性 maxIterations 上限
  2. 在达到上限时强制退出
  3. 记录详细的迭代统计用于调试

#### 7. [源代码] [高严重] [Oapeflir while(true) 循环可能无限重规划]
- **文件/路径**: `src/platform/five-plane-orchestration/oapeflir/oapeflir-loop-service.ts:355`
- **问题描述**: 
  - 如果 loopReplanDecision.shouldReplan 永不变为 false
  - 重规划永远无法减少计划错误
  - 可能导致无限循环
- **建议修复**: 
  1. 添加重规划次数上限
  2. 在达到上限时强制接受当前结果
  3. 添加重规划质量衰减检测

#### 8. [源代码] [中严重] [Recovery 流程允许终态转换到 paused]
- **文件/路径**: `src/platform/five-plane-execution/runtime-state-machine.ts:105-108`
- **问题描述**: 
  - completed, failed, cancelled, aborted 可转换到 paused 进行恢复
  - 创建恢复路径可能重新进入执行
  - 需要验证此行为的正确性
- **建议修复**: 
  1. 审查 recovery 转换的合法性
  2. 确保不会导致状态不一致
  3. 添加转换前置条件验证

#### 9. [国际化] [高严重] [API 错误消息硬编码英文]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/api-error.ts`
- **问题描述**: 
  ```typescript
  return new ApiError(404, "api.task_not_found", "Task not found.");
  ```
  - 错误消息硬编码英文
  - 无后端 i18n 机制
  - 非英文客户端收到不本地化的错误
- **建议修复**: 
  1. 使用错误码而非硬编码消息
  2. 客户端根据错误码本地化
  3. 或在后端添加 i18n 支持

#### 10. [国际化] [中严重] [UI 硬编码字符串未翻译]
- **文件/路径**: `ui/packages/features/conversation/src/web/index.tsx`
- **问题描述**: 
  - "Build Plan", "Execute", "Messages" 等按钮标签硬编码
  - 不使用 i18n 系统
- **建议修复**: 
  1. 使用 `translateFeatureCopy` 替换硬编码
  2. 确保所有用户可见文本通过 i18n
  3. 添加硬编码字符串检测

#### 11. [国际化] [中严重] [Arabic 目录不完整]
- **文件/路径**: `ui/packages/shared/i18n/src/catalogs/ar-SA.ts`
- **问题描述**: 
  - ar-SA 只有约18条消息
  - en-US 和 zh-CN 有约117条
  - 缺失的 key 返回 key 本身
- **建议修复**: 
  1. 补充 Arabic 翻译
  2. 确保所有 key 都有翻译
  3. 添加翻译完整性测试

#### 12. [国际化] [低严重] [translateFeatureCopy 无回退默认值]
- **文件/路径**: `ui/packages/shared/i18n/`
- **问题描述**: 
  - 如果 feature ID 不在 catalog 中
  - 返回 undefined title/summary
  - 无合理回退
- **建议修复**: 
  1. 添加回退到 key 或默认字符串
  2. 或使用 feature ID 作为显示名
  3. 确保 UI 不显示 undefined

#### 13. [运维] [高严重] [健康检查无 readiness/liveness 区分]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts`
- **问题描述**: 
  - /healthz, /v1/healthz, /health 返回相同报告
  - 无 readiness probe（可处理流量？）
  - 无 liveness probe（系统存活？）
  - canAcceptTraffic() 未通过 HTTP 暴露
- **建议修复**: 
  1. 添加 /ready 和 /live 端点
  2. Readiness 检查依赖项就绪
  3. Liveness 检查进程健康

#### 14. [运维] [高严重] [shutdown 期间健康检查存在竞态]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts`
- **问题描述**: 
  - getHealthReportAsync() 异步检查 dbWritable
  - shutdown 期间数据库连接可能关闭
  - 健康检查仍在传入
- **建议修复**: 
  1. 在 shutdown 时返回 unhealthy
  2. 添加 shutdown 状态标志
  3. 停止接受新的健康检查请求

#### 15. [运维] [高严重] [LeaderElectionService 未与 GracefulShutdown 集成]
- **文件/路径**: `src/platform/five-plane-execution/ha/leader-election-service.ts`
- **问题描述**: 
  - LeaderElectionService.stop() 处理优雅退位
  - 但未在全局 graceful shutdown 中注册
  - 需显式注册为 shutdown handler
- **建议修复**: 
  1. 将 LeaderElectionService 添加到 shutdown handlers
  2. 确保领导者在 shutdown 时让位
  3. 验证关闭顺序正确

#### 16. [运维] [中严重] [addHandler() 无显式排序约定]
- **文件/路径**: `src/platform/five-plane-execution/startup/graceful-shutdown.ts`
- **问题描述**: 
  - 虽然 handlers 逆序执行
  - 但无显式接口声明 handler 依赖关系
  - 关键 handlers（如关闭数据库连接）应最后运行
- **建议修复**: 
  1. 添加优先级或阶段参数
  2. 文档化关键 handler 顺序要求
  3. 添加顺序验证测试

#### 17. [运维] [中严重] [unref'd timers 可能不触发]
- **文件/路径**: `src/platform/five-plane-execution/startup/process-error-handlers.ts:106`
- **问题描述**: 
  - `setTimeout(...).unref()` 不保持事件循环活跃
  - 如果主线程空闲，强制退出可能不触发
- **建议修复**: 
  1. 移除 .unref() 除非确实需要
  2. 或确保有其他方式保持事件循环
  3. 添加超时触发验证测试

#### 18. [运维] [中严重] [StartupConsistencyChecker 阻塞流量但不暴露状态]
- **文件/路径**: `src/platform/five-plane-startup-plan.ts`
- **问题描述**: 
  - canAcceptTraffic() 在 P0 问题存在时返回 false
  - 但 _trafficBlocked 是内部状态
  - 负载均衡器访问 /healthz 仍会得到 "ok"
- **建议修复**: 
  1. 在健康报告中暴露 trafficBlocked 状态
  2. 或添加单独的健康端点指示就绪状态
  3. 确保负载均衡器在阻塞时不路由流量

#### 19. [事件系统] [中严重] [TypedEventBus vs EventEmitter 混用]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - 项目同时使用 TypedEventBus 和原生 EventEmitter
  - TypedEventBus 应该是标准
  - 混用可能导致类型安全问题
- **建议修复**: 
  1. 创建迁移计划统一使用 TypedEventBus
  2. 添加 ESLint 规则禁止原生 EventEmitter
  3. 在 CI 中检测混用情况

#### 20. [源代码] [低严重] [无事件排序保证]
- **文件/路径**: `src/platform/five-plane-state-evidence/events/`
- **问题描述**: 
  - 无明确的事件排序保证文档
  - 事件处理可能乱序
  - 依赖方需要处理乱序
- **建议修复**: 
  1. 文档化事件排序语义
  2. 如果需要排序，实现序列号机制
  3. 在事件处理中考虑乱序情况

### 总结

本次补充Review（第十四轮 - 日志事件与工作流Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. StructuredLogEntry 无 requestId 字段
2. StructuredLogger data 字段无自动清洗（敏感数据风险）
3. Harness while(true) 循环无硬性迭代上限
4. Oapeflir while(true) 循环可能无限重规划
5. API 错误消息硬编码英文（无 i18n）
6. 健康检查无 readiness/liveness 区分
7. shutdown 期间健康检查存在竞态
8. LeaderElectionService 未与 GracefulShutdown 集成

**中优先级**:
1. 日志中敏感数据无 PII 处理
2. 59处 console.* 直接调用
3. 部分 logger.warn 调用缺少结构化数据
4. Recovery 流程允许终态转换到 paused
5. UI 硬编码字符串未翻译
6. Arabic 目录不完整
7. addHandler() 无显式排序约定
8. unref'd timers 可能不触发
9. StartupConsistencyChecker 阻塞流量但不暴露状态
10. TypedEventBus vs EventEmitter 混用

**低优先级**:
1. translateFeatureCopy 无回退默认值
2. 无事件排序保证

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 68 | 80 | 37 | 185 |
| 测试 | 8 | 14 | 5 | 27 |
| 配置 | 10 | 38 | 24 | 72 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **103** | **171** | **82** | **356** |

### 优先修复建议

**立即处理 (本周内)**:
1. 在 StructuredLogEntry 添加 requestId 字段
2. 实现日志敏感字段自动清洗
3. 为工作流循环添加硬性迭代上限
4. 添加 readiness/liveness 健康检查端点
5. 将 LeaderElectionService 集成到 GracefulShutdown
6. 实现 API 错误消息 i18n 支持

**短期内处理 (本月内)**:
1. 替换所有 console.* 为 StructuredLogger
2. 补充 Arabic i18n 目录
3. 修复 UI 硬编码字符串
4. 统一 TypedEventBus 替代 EventEmitter
5. 解决 shutdown 竞态条件
6. 添加事件排序保证文档

**长期规划**:
1. 建立完整的 PII 处理框架
2. 实现日志完整性验证
3. 建立工作流安全循环检测
4. 完善健康检查和就绪探测
5. 建立国际化完整测试

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十五轮 - 构建测试与类型系统Review）

### 发现的新问题

#### 1. [构建] [高严重] [无 TypeScript 增量编译缓存]
- **文件/路径**: `tsconfig.json`
- **问题描述**: 
  - 无 `incremental: true` 配置
  - 无 `tsbuildinfo` 文件
  - 每次构建是完整重建（9.6秒）
  - TypeScript 无法使用增量编译
- **建议修复**: 
  1. 在 tsconfig.json 添加 `"incremental": true`
  2. 添加 `"tsbuildinfo": ".tsbuildinfo"` 到 .gitignore
  3. 实现项目引用以支持并行构建

#### 2. [构建] [高严重] [76个 npm scripts 大部分重复构建]
- **文件/路径**: `package.json`
- **问题描述**: 
  - 40+ CLI scripts 都运行 `npm run build && node ...`
  - 每个脚本前都执行完整重建
  - 无法直接运行预构建的 CLI
  - 20+ stable:* 脚本遵循相同模式
- **建议修复**: 
  1. 创建 `build:cli` 脚本构建一次
  2. 让 CLI 脚本使用预构建的 dist 文件
  3. 添加 npm run dev 或类似的无需重建的执行方式

#### 3. [源代码] [高严重] [168处 any 类型使用]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - `stable-runner-factory.ts`: `opts: any` -> `any`
  - `billing-service-async.ts`: `input: any` 方法签名
  - 重试/超时工具使用 `(...args: any[]) => Promise<any>`
  - 绕过类型安全
- **建议修复**: 
  1. 使用泛型替代 any
  2. 替换为具体的输入类型
  3. 添加 ESLint 规则禁止 any

#### 4. [源代码] [高严重] [38处 @ts-ignore 指令]
- **文件/路径**: 多个文件
- **问题描述**: 
  - 14个 stability 相关文件有 `@ts-ignore ExecutionRecord type mismatch`
  - 5个 harness-sdk 文件有 `Partial<HarnessRun> doesn't have all required properties`
  - 2个文件有 `exactOptionalPropertyTypes` 问题
- **建议修复**: 
  1. 修复 ExecutionRecord 类型不匹配
  2. 完成 HarnessRun 类型的必需属性
  3. 移除 @ts-ignore 使用

#### 5. [源代码] [高严重] [ExecutionRecord 类型不匹配]
- **文件/路径**: `src/platform/stability/stable-evidence-bundle-support.ts:783` 等14个文件
- **问题描述**: 
  - 插入 execution record 时 TypeScript 拒绝类型
  - `ExecutionRecord` 接口要求新字段但 insert 对象缺失
  - 或 store 的 insert 方法接受不同类型
- **建议修复**: 
  1. 对齐 ExecutionRecord 接口和 store 类型
  2. 添加缺失字段到 insert 对象
  3. 或调整 store 的 insert 方法类型签名

#### 6. [源代码] [中严重] [30+处 as unknown as 双类型转换]
- **文件/路径**: `division-loader.ts`, `domain-baseline-catalog.ts`, `harness-sdk`
- **问题描述**: 
  - 完全绕过类型安全
  - 通常是类型架构问题的信号
- **建议修复**: 
  1. 修复上游类型问题
  2. 使用更具体的类型断言
  3. 重构类型层次结构

#### 7. [构建] [中严重] [rimraf  extraneous - 未在 package.json 声明]
- **文件/路径**: `node_modules/rimraf`
- **问题描述**: 
  - rimraf@6.1.3 存在于 node_modules 但未在 package.json 声明
  - 可能导致依赖问题
- **建议修复**: 
  1. 添加到 dependencies 或 devDependencies
  2. 或移除并使用其他清理方式

#### 8. [测试] [高严重] [178处直接 process.env 变更无抽象]
- **文件/路径**: `tests/unit/domains/registry/plugin-spi-registry.test.ts:640-687`
- **问题描述**: 
  - 直接读取/写入 process.env
  - 在 beforeEach/afterEach 中手动保存/恢复
  - 无 helper 抽象
- **建议修复**: 
  1. 创建 env helper 函数
  2. 统一保存和恢复模式
  3. 添加测试隔离验证

#### 9. [测试] [高严重] [21,998处 unlinkSync 调用无集中清理]
- **文件/路径**: 多个测试文件
- **问题描述**: 
  - 每个测试重新实现清理逻辑
  - 无集中清理 utility
  - 可能导致 EBUSY (Windows) 或并发访问问题
- **建议修复**: 
  1. 创建集中的测试清理 utility
  2. 使用 afterEach 确保清理
  3. 考虑使用临时目录

#### 10. [测试] [中严重] [无统一 mocking 框架]
- **文件/路径**: `tests/` 多个目录
- **问题描述**: 
  - 10,579 处 vi.fn, jest.mock, sinon 等调用
  - 无统一的 mocking 框架可见
  - 模式不一致
- **建议修复**: 
  1. 建立统一的 mock factory
  2. 标准化 mock 模式
  3. 添加 mocking 最佳实践文档

#### 11. [测试] [中严重] [Singleton 状态重置未标准化]
- **文件/路径**: `tests/unit/platform/five-plane-execution/dispatcher.test.ts`
- **问题描述**: 
  - 测试通过特定函数重置 singletons
  - 每个测试重复自己的 reset 逻辑
  - 无统一的 resetAllSingletons()
- **建议修复**: 
  1. 创建统一的 singleton reset 机制
  2. 在 afterEach 中自动调用
  3. 文档化 singleton 重置要求

#### 12. [测试] [低严重] [无分支覆盖率要求]
- **文件/路径**: `.c8rc.json`
- **问题描述**: 
  - `"100": false` - 无覆盖率要求
  - 只测量源代码，不测量测试文件
- **建议修复**: 
  1. 添加分支覆盖率阈值
  2. 在 CI 中强制执行覆盖率 gate
  3. 关注关键代码路径

#### 13. [网络] [高严重] [ScopedExternalAccessSandbox performHttpRequest 无超时]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/scoped-external-access-sandbox.ts:289`
- **问题描述**: 
  - fetch 调用无 timeout 参数
  - 挂起的请求可能永久阻塞
- **建议修复**: 
  1. 添加 AbortController timeout
  2. 使用标准的超时模式
  3. 记录超时配置

#### 14. [网络] [高严重] [OIDC 服务 fetch 调用无超时]
- **文件/路径**: `src/org-governance/sso-scim/oidc/oidc-service.ts:565,272`
- **问题描述**: 
  - OIDC token 交换无 retry logic
  - 无 timeout/abort signal
  - 认证流程可能挂起
- **建议修复**: 
  1. 添加 fetch timeout
  2. 添加 retry logic
  3. 使用标准的超时配置

#### 15. [网络] [高严重] [plugin-runtime-child 替换 globalThis.fetch 为 no-op]
- **文件/路径**: `src/domains/registry/plugin-runtime-child.ts:112`
- **问题描述**: 
  ```typescript
  globalThis.fetch = (async () => {}) as unknown as typeof globalThis.fetch;
  ```
  - 插件上下文中完全替换 fetch 为空函数
  - 插件的所有出站 HTTP 请求都会失败
  - 看起来是有意为之但可能造成问题
- **建议修复**: 
  1. 确认这是有意行为
  2. 文档化 fetch 替换行为
  3. 确保不会意外影响其他代码

#### 16. [网络] [中严重] [无显式 http.Agent 配置]
- **文件/路径**: 全局
- **问题描述**: 
  - 无显式的 http.Agent/https.Agent 配置
  - 依赖 Node.js 默认连接池
  - 无 maxSockets 或 maxConnections 配置
  - 高吞吐场景可能出问题
- **建议修复**: 
  1. 添加 http Agent 配置
  2. 文档化连接池大小
  3. 监控连接使用情况

#### 17. [序列化] [中严重] [合同版本硬编码为字符串字面量]
- **文件/路径**: `src/platform/contracts/inter-plane-contract-gateway.ts:184`
- **问题描述**: 
  - `schemaVersion: "v4.3"` 硬编码
  - 难以追踪合同版本演变
- **建议修复**: 
  1. 提取到类型化常量或 enum
  2. 集中管理合同版本
  3. 添加版本兼容性检查

#### 18. [序列化] [低严重] [Timestamp = string 丢失精度]
- **文件/路径**: `src/platform/contracts/types/domain/primitives.ts:10`
- **问题描述**: 
  - `type Timestamp = string` 无类型精度
  - ISO 8601 字符串无时区信息
- **建议修复**: 
  1. 使用 Date 对象或 branded string 类型
  2. 明确时区处理约定
  3. 文档化时间戳语义

#### 19. [源代码] [中严重] [29处非空断言 !.]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - 使用 `!.` 断言非空
  - 可能导致运行时错误
- **建议修复**: 
  1. 使用可选链 `?.` 或空值合并 `??`
  2. 添加适当的 null 检查
  3. 减少非空断言使用

#### 20. [API] [中严重] [API 路由 schema 版本硬编码]
- **文件/路径**: `src/platform/five-plane-interface/api/federation-routing-service.ts`
- **问题描述**: 
  - Zod schema 验证存在
  - 但 schema 版本未与 contract 版本同步
- **建议修复**: 
  1. 从 contract 版本派生 schema 版本
  2. 添加版本一致性检查
  3. 文档化版本关系

### 总结

本次补充Review（第十五轮 - 构建测试与类型系统Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. 无 TypeScript 增量编译缓存（每次完整重建9.6秒）
2. 76个 npm scripts 大部分重复构建
3. 168处 any 类型使用（绕过类型安全）
4. 38处 @ts-ignore 指令
5. ExecutionRecord 类型不匹配（14个文件）
6. 178处直接 process.env 变更无抽象
7. ScopedExternalAccessSandbox performHttpRequest 无超时
8. OIDC 服务 fetch 调用无超时
9. plugin-runtime-child 替换 globalThis.fetch 为 no-op

**中优先级**:
1. 30+处 as unknown as 双类型转换
2. rimraf extraneous 未声明
3. 21,998处 unlinkSync 无集中清理
4. 无统一 mocking 框架
5. Singleton 状态重置未标准化
6. 无显式 http.Agent 配置
7. 合同版本硬编码为字符串
8. Timestamp = string 丢失精度
9. 非空断言使用

**低优先级**:
1. 无分支覆盖率要求
2. API 路由 schema 版本未同步

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 72 | 87 | 39 | 198 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **109** | **182** | **85** | **376** |

### 优先修复建议

**立即处理 (本周内)**:
1. 添加 TypeScript 增量编译 (`incremental: true`)
2. 重构 npm scripts 避免重复构建
3. 减少 any 类型使用到 <50
4. 移除所有 @ts-ignore 指令
5. 修复 ExecutionRecord 类型不匹配
6. 添加 fetch 超时到 OIDC 和 sandbox

**短期内处理 (本月内)**:
1. 创建集中的测试清理 utility
2. 建立统一的 mocking 框架
3. 标准化 singleton 重置机制
4. 添加 http.Agent 连接池配置
5. 提取合同版本到类型化常量

**长期规划**:
1. 实现完整的类型安全（无 any）
2. 建立测试基础设施标准
3. 实现构建缓存优化
4. 完善网络层超时和重试
5. 建立覆盖率门禁标准

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十三轮 - 缓存会话与实时通信Review）

### 发现的新问题

#### 1. [安全] [严重] [CORS allowedMethods 缺少 PUT/PATCH/DELETE]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/response-hardening.ts:12-14`
- **问题描述**: 
  ```typescript
  export const DEFAULT_CORS_CONFIG: CorsConfig = {
    allowedMethods: ["GET", "POST", "OPTIONS"],  // 缺少 PUT, PATCH, DELETE
  ```
  - 浏览器 CORS preflight 会拒绝 PUT/PATCH/DELETE 请求
  - 但路由中有 PATCH /v1/tasks/:id, DELETE endpoints
- **建议修复**: 
  1. 添加 PUT, PATCH, DELETE 到 allowedMethods
  2. 确保 CORS 配置与应用路由方法一致
  3. 添加测试验证所有 HTTP 方法的 CORS preflight

#### 2. [安全] [严重] [Mission Routes 错误响应包装不当]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/mission-routes.ts:101,118-124`
- **问题描述**: 
  ```typescript
  return buildJsonResponse(ctx.requestId, 404, { error: { code: "MISSION_NOT_FOUND" } });
  ```
  - 使用 buildJsonResponse（成功包装）返回错误
  - 错误被包装在成功信封 `{requestId, data: {error: {...}}}` 中
  - 违反 REST 规范
- **建议修复**: 
  1. 使用 buildJsonErrorResponse 处理错误
  2. 确保所有错误路径使用正确的错误信封
  3. 添加 API 一致性测试

#### 3. [源代码] [高严重] [WebSocketBridge 无最大连接数限制]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:103-107`
- **问题描述**: 
  ```typescript
  this.wss = new WebSocketServer({
    server,
    path: WS_PATH,
    maxPayload: options.maxPayloadBytes ?? DEFAULT_MAX_PAYLOAD_BYTES,
    // 无 maxConnections 限制！
  });
  ```
  - 与 dashboard-websocket-server.ts 的 maxClients: 1000 不同
  - 恶意客户端可耗尽服务器资源
- **建议修复**: 
  1. 添加 maxConnections 限制
  2. 实现连接限制拒绝策略
  3. 添加连接计数指标

#### 4. [源代码] [高严重] [pendingAcks 在断开连接时未清理]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:268-282`
- **问题描述**: 
  - handleDisconnection 清理 subscribedTasks, taskSubscribers, slowConsumers
  - 但 pendingAcks Map 未清理
  - 断开的客户端未确认的消息永久留在内存
- **建议修复**: 
  1. 在 handleDisconnection 中删除所有 pendingAcks 条目
  2. 考虑添加超时机制自动清理未确认消息
  3. 记录清理操作的审计日志

#### 5. [安全] [高严重] [硬编码回退密钥]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/audit-event-integrity.ts:41`
- **问题描述**: 
  ```typescript
  const AUDIT_INTEGRITY_HMAC_KEY = process.env["AA_AUDIT_INTEGRITY_HMAC_KEY"] ?? "audit-integrity-secret-key-32-bytes!";
  ```
  - 占位符密钥用作回退
  - 如果环境变量未设置，使用不安全密钥
- **建议修复**: 
  1. 移除回退值，要求环境变量必须设置
  2. 在启动时验证密钥存在且足够强
  3. 如果密钥缺失导致启动失败

#### 6. [配置] [高严重] [config/runtime/test.json 缺少超时配置]
- **文件/路径**: `config/runtime/test.json`
- **问题描述**: 
  - 只有3个字段，缺少 maxConcurrentTasks, defaultTaskTimeoutMs, defaultStepTimeoutMs
  - 回退到默认值可能不适合测试环境
- **建议修复**: 
  1. 添加完整的 timeout/tuning 配置
  2. 确保测试环境有合理的默认值
  3. 文档化必需的配置字段

#### 7. [配置] [高严重] [prod.json 限制过严 - maxConcurrentTasks=1]
- **文件/路径**: `config/runtime/prod.json`
- **问题描述**: 
  - maxConcurrentTasks: 1（vs dev:8, staging:4, pre-prod:6）
  - defaultTaskTimeoutMs: 120000（vs dev:600000）
  - defaultStepTimeoutMs: 60000（vs dev:180000）
  - 生产环境严重受限，可能影响吞吐量
- **建议修复**: 
  1. 评估并调整生产限制
  2. 与业务需求匹配
  3. 添加生产容量测试

#### 8. [源代码] [高严重] [多个服务存在无界 Map 无驱逐策略]
- **文件/路径**: 多个域服务
- **问题描述**: 
  - `domain-knowledge-schema-service.ts:73-75`: schemas, sourceContent, sourceTimestamps 无限制
  - `domain-eval-framework-service.ts:91-95`: frameworks, qualityAxes 等无限制
  - `domain-recipe-service.ts:67-68`: recipes, versions 无限制
  - `domain-risk-profile-service.ts:51`: profiles 无限制
  - Session Maps (session-management.ts:83-89) 无自动清理
- **建议修复**: 
  1. 为所有 Map 实现 LRU 或 TTL 驱逐策略
  2. 添加后台清理任务
  3. 添加大小监控和告警

#### 9. [源代码] [中严重] [taskEventHistory Map 永不清理]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:91,548-555`
- **问题描述**: 
  - taskEventHistory 限制单任务历史为 200 条
  - 但 Map 本身从不清理
  - 取消订阅的任务历史永久保留
- **建议修复**: 
  1. 实现后台清理无订阅者任务的历史
  2. 添加任务订阅者监控
  3. 考虑使用 WeakMap 替代

#### 10. [配置] [中严重] [安全配置 drift - remoteWorkerRegistration 缺失]
- **文件/路径**: `config/security/dev.json`, `staging.json`, `pre-prod.json`
- **问题描述**: 
  - default.json 有完整的 remoteWorkerRegistration 配置
  - dev/staging/pre-prod 只有 approvalMode
  - prod 有 approvalMode 但无 remoteWorkerRegistration
  - 安全配置不一致
- **建议修复**: 
  1. 统一所有环境的 remoteWorkerRegistration
  2. 添加安全配置验证
  3. 确保最低安全基线

#### 11. [API] [中严重] [OpenAPI 端点响应格式不一致]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/utils.ts:207-215`
- **问题描述**: 
  - `/v1/openapi.json` 使用 buildJsonDocumentResponse（无包装）
  - 其他端点使用 buildJsonResponse（{requestId, data} 包装）
  - 客户端体验不一致
- **建议修复**: 
  1. 统一响应信封格式
  2. 文档化响应格式规范
  3. 添加响应格式验证测试

#### 12. [源代码] [中严重] [Redis 队列缺少 idempotency 索引支持]
- **文件/路径**: `src/platform/five-plane-execution/queue/redis-queue-adapter.ts`
- **问题描述**: 
  - SQLite 有部分唯一索引支持 idempotency
  - Redis 实现使用哈希索引但实现不完整
  - 可能导致重复消息
- **建议修复**: 
  1. 完善 Redis idempotency 实现
  2. 添加唯一索引验证
  3. 确保与 SQLite 行为一致

#### 13. [源代码] [中严重] [请求去重中间件使用内存存储]
- **文件/路径**: `src/platform/five-plane-interface/api/middleware/request-deduplication.ts`
- **问题描述**: 
  - 纯内存滑动窗口（Map<DeduplicationKey, DeduplicationEntry[]>）
  - 无外部存储
  - 多实例部署不共享
  - 重启后去重状态丢失
- **建议修复**: 
  1. 使用 Redis 替代内存存储
  2. 支持分布式去重
  3. 持久化去重状态

#### 14. [源代码] [中严重] [Cache 无 stampede 保护]
- **文件/路径**: 多个 cache 实现
- **问题描述**: 
  - MemoryCacheStore, ExperienceCacheService 无锁
  - 缓存未命中时可发生 thundering herd
  - 高并发下可能导致数据库过载
- **建议修复**: 
  1. 实现 single-flight 模式
  2. 添加请求排队机制
  3. 使用分布式锁保护缓存更新

#### 15. [源代码] [中严重] [EvidenceService eviction 仅在插入时触发]
- **文件/路径**: `src/platform/five-plane-state-evidence/memory/evidence-service.ts:202`
- **问题描述**: 
  - 清理仅在 record() 调用时触发
  - 空闲时无后台清理
  - 可能导致内存持续增长
- **建议修复**: 
  1. 添加定期后台清理任务
  2. 使用独立的清理线程
  3. 添加内存使用监控

#### 16. [源代码] [低严重] [无连接指标暴露]
- **文件/路径**: WebSocket 服务器
- **问题描述**: 
  - getClientCount() 存在但未通过 HTTP 端点暴露
  - 无 pendingAcks 队列深度指标
  - 无连接建立告警
- **建议修复**: 
  1. 通过 metrics 端点暴露连接指标
  2. 添加 pendingAcks 队列监控
  3. 添加连接数异常告警

#### 17. [源代码] [低严重] [无空闲客户端超时]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts`
- **问题描述**: 
  - 仅在心跳 Sweep 时检查 isAlive
  - 无 per-client 独立于心跳的空闲超时
  - 认证后从不发送消息的客户端只能通过心跳失败检测
- **建议修复**: 
  1. 添加 per-client idle timeout
  2. 独立于心跳间隔
  3. 配置可调整

#### 18. [源代码] [低严重] [订阅限制是 per-client 而非全局]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:244`
- **问题描述**: 
  - MAX_SUBSCRIPTIONS_PER_CLIENT = 100
  - 恶意客户端可订阅 100 个任务
  - 无全局任务订阅者限制
- **建议修复**: 
  1. 添加全局任务订阅者限制
  2. 实现 per-task 订阅者上限
  3. 添加反滥用检测

#### 19. [配置] [低严重] [config/runtime 无版本验证机制]
- **文件/路径**: `config/runtime/*.json`
- **问题描述**: 
  - 配置有 "version": "v4.3"
  - 但无 schema 版本验证
  - 加载时可能接受不兼容配置
- **建议修复**: 
  1. 添加 JSON Schema 验证
  2. 实现版本兼容性检查
  3. 启动时验证配置完整性

#### 20. [源代码] [低严重] [ImprovementCandidateRegistry splice 是 O(n)]
- **文件/路径**: `src/platform/five-plane-orchestration/improve/improvement-candidate-registry.ts:243`
- **问题描述**: 
  - accessOrder 数组的 splice 操作是 O(n)
  - 高频访问时可能性能问题
- **建议修复**: 
  1. 使用 LinkedList 替代数组
  2. 或使用 Map 维护访问顺序
  3. 性能测试验证

### 总结

本次补充Review（第十三轮 - 缓存会话与实时通信Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. CORS allowedMethods 缺少 PUT/PATCH/DELETE（浏览器请求会失败）
2. Mission Routes 错误响应包装不当
3. WebSocketBridge 无最大连接数限制（DoS风险）
4. pendingAcks 断开连接时未清理（内存泄漏）
5. 硬编码回退密钥（安全漏洞）
6. config/runtime/test.json 缺少超时配置
7. prod.json maxConcurrentTasks=1 限制过严
8. 多个服务存在无界 Map（内存泄漏风险）

**中优先级**:
1. taskEventHistory Map 永不清理
2. 安全配置 drift（remoteWorkerRegistration 缺失）
3. OpenAPI 端点响应格式不一致
4. Redis 队列 idempotency 实现不完整
5. 请求去重中间件使用内存存储（多实例不共享）
6. Cache 无 stampede 保护
7. EvidenceService eviction 仅在插入时触发

**低优先级**:
1. 无连接指标暴露
2. 无空闲客户端超时
3. 订阅限制是 per-client 而非全局
4. config/runtime 无版本验证机制
5. ImprovementCandidateRegistry splice O(n)

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 64 | 74 | 35 | 173 |
| 测试 | 8 | 14 | 5 | 27 |
| 配置 | 10 | 38 | 24 | 72 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 7 | 3 | 12 |
| UI | 0 | 3 | 3 | 6 |
| 部署 | 1 | 10 | 6 | 17 |
| **合计** | **99** | **161** | **79** | **339** |

### 优先修复建议

**立即处理 (本周内)**:
1. 修复 CORS allowedMethods 添加 PUT/PATCH/DELETE
2. 修复 Mission Routes 错误响应格式
3. 为 WebSocketBridge 添加最大连接数限制
4. 清理 pendingAcks 和 taskEventHistory
5. 移除硬编码回退密钥
6. 修复 config/runtime/test.json 配置
7. 评估并调整 prod 并发限制

**短期内处理 (本月内)**:
1. 统一安全配置（remoteWorkerRegistration）
2. 实现无界 Map 的 LRU/TTL 驱逐
3. 统一 API 响应信封格式
4. 完善 Redis idempotency 实现
5. 添加分布式去重中间件
6. 实现 cache stampede 保护

**长期规划**:
1. 建立完整的连接管理和监控
2. 实现后台清理任务框架
3. 添加配置 schema 验证
4. 优化高频繁操作的数据结构
5. 建立内存使用基线和告警

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十五轮 - 构建测试与类型系统Review）

### 发现的新问题

#### 1. [构建] [高严重] [无 TypeScript 增量编译缓存]
- **文件/路径**: `tsconfig.json`
- **问题描述**: 
  - 无 `incremental: true` 配置
  - 无 `tsbuildinfo` 文件
  - 每次构建是完整重建（9.6秒）
  - TypeScript 无法使用增量编译
- **建议修复**: 
  1. 在 tsconfig.json 添加 `"incremental": true`
  2. 添加 `"tsbuildinfo": ".tsbuildinfo"` 到 .gitignore
  3. 实现项目引用以支持并行构建

#### 2. [构建] [高严重] [76个 npm scripts 大部分重复构建]
- **文件/路径**: `package.json`
- **问题描述**: 
  - 40+ CLI scripts 都运行 `npm run build && node ...`
  - 每个脚本前都执行完整重建
  - 无法直接运行预构建的 CLI
  - 20+ stable:* 脚本遵循相同模式
- **建议修复**: 
  1. 创建 `build:cli` 脚本构建一次
  2. 让 CLI 脚本使用预构建的 dist 文件
  3. 添加 npm run dev 或类似的无需重建的执行方式

#### 3. [源代码] [高严重] [168处 any 类型使用]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - `stable-runner-factory.ts`: `opts: any` -> `any`
  - `billing-service-async.ts`: `input: any` 方法签名
  - 重试/超时工具使用 `(...args: any[]) => Promise<any>`
  - 绕过类型安全
- **建议修复**: 
  1. 使用泛型替代 any
  2. 替换为具体的输入类型
  3. 添加 ESLint 规则禁止 any

#### 4. [源代码] [高严重] [38处 @ts-ignore 指令]
- **文件/路径**: 多个文件
- **问题描述**: 
  - 14个 stability 相关文件有 `@ts-ignore ExecutionRecord type mismatch`
  - 5个 harness-sdk 文件有 `Partial<HarnessRun> doesn't have all required properties`
  - 2个文件有 `exactOptionalPropertyTypes` 问题
- **建议修复**: 
  1. 修复 ExecutionRecord 类型不匹配
  2. 完成 HarnessRun 类型的必需属性
  3. 移除 @ts-ignore 使用

#### 5. [源代码] [高严重] [ExecutionRecord 类型不匹配]
- **文件/路径**: `src/platform/stability/stable-evidence-bundle-support.ts:783` 等14个文件
- **问题描述**: 
  - 插入 execution record 时 TypeScript 拒绝类型
  - `ExecutionRecord` 接口要求新字段但 insert 对象缺失
  - 或 store 的 insert 方法接受不同类型
- **建议修复**: 
  1. 对齐 ExecutionRecord 接口和 store 类型
  2. 添加缺失字段到 insert 对象
  3. 或调整 store 的 insert 方法类型签名

#### 6. [源代码] [中严重] [30+处 as unknown as 双类型转换]
- **文件/路径**: `division-loader.ts`, `domain-baseline-catalog.ts`, `harness-sdk`
- **问题描述**: 
  - 完全绕过类型安全
  - 通常是类型架构问题的信号
- **建议修复**: 
  1. 修复上游类型问题
  2. 使用更具体的类型断言
  3. 重构类型层次结构

#### 7. [构建] [中严重] [rimraf  extraneous - 未在 package.json 声明]
- **文件/路径**: `node_modules/rimraf`
- **问题描述**: 
  - rimraf@6.1.3 存在于 node_modules 但未在 package.json 声明
  - 可能导致依赖问题
- **建议修复**: 
  1. 添加到 dependencies 或 devDependencies
  2. 或移除并使用其他清理方式

#### 8. [测试] [高严重] [178处直接 process.env 变更无抽象]
- **文件/路径**: `tests/unit/domains/registry/plugin-spi-registry.test.ts:640-687`
- **问题描述**: 
  - 直接读取/写入 process.env
  - 在 beforeEach/afterEach 中手动保存/恢复
  - 无 helper 抽象
- **建议修复**: 
  1. 创建 env helper 函数
  2. 统一保存和恢复模式
  3. 添加测试隔离验证

#### 9. [测试] [高严重] [21,998处 unlinkSync 调用无集中清理]
- **文件/路径**: 多个测试文件
- **问题描述**: 
  - 每个测试重新实现清理逻辑
  - 无集中清理 utility
  - 可能导致 EBUSY (Windows) 或并发访问问题
- **建议修复**: 
  1. 创建集中的测试清理 utility
  2. 使用 afterEach 确保清理
  3. 考虑使用临时目录

#### 10. [测试] [中严重] [无统一 mocking 框架]
- **文件/路径**: `tests/` 多个目录
- **问题描述**: 
  - 10,579 处 vi.fn, jest.mock, sinon 等调用
  - 无统一的 mocking 框架可见
  - 模式不一致
- **建议修复**: 
  1. 建立统一的 mock factory
  2. 标准化 mock 模式
  3. 添加 mocking 最佳实践文档

#### 11. [测试] [中严重] [Singleton 状态重置未标准化]
- **文件/路径**: `tests/unit/platform/five-plane-execution/dispatcher.test.ts`
- **问题描述**: 
  - 测试通过特定函数重置 singletons
  - 每个测试重复自己的 reset 逻辑
  - 无统一的 resetAllSingletons()
- **建议修复**: 
  1. 创建统一的 singleton reset 机制
  2. 在 afterEach 中自动调用
  3. 文档化 singleton 重置要求

#### 12. [测试] [低严重] [无分支覆盖率要求]
- **文件/路径**: `.c8rc.json`
- **问题描述**: 
  - `"100": false` - 无覆盖率要求
  - 只测量源代码，不测量测试文件
- **建议修复**: 
  1. 添加分支覆盖率阈值
  2. 在 CI 中强制执行覆盖率 gate
  3. 关注关键代码路径

#### 13. [网络] [高严重] [ScopedExternalAccessSandbox performHttpRequest 无超时]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/scoped-external-access-sandbox.ts:289`
- **问题描述**: 
  - fetch 调用无 timeout 参数
  - 挂起的请求可能永久阻塞
- **建议修复**: 
  1. 添加 AbortController timeout
  2. 使用标准的超时模式
  3. 记录超时配置

#### 14. [网络] [高严重] [OIDC 服务 fetch 调用无超时]
- **文件/路径**: `src/org-governance/sso-scim/oidc/oidc-service.ts:565,272`
- **问题描述**: 
  - OIDC token 交换无 retry logic
  - 无 timeout/abort signal
  - 认证流程可能挂起
- **建议修复**: 
  1. 添加 fetch timeout
  2. 添加 retry logic
  3. 使用标准的超时配置

#### 15. [网络] [高严重] [plugin-runtime-child 替换 globalThis.fetch 为 no-op]
- **文件/路径**: `src/domains/registry/plugin-runtime-child.ts:112`
- **问题描述**: 
  ```typescript
  globalThis.fetch = (async () => {}) as unknown as typeof globalThis.fetch;
  ```
  - 插件上下文中完全替换 fetch 为空函数
  - 插件的所有出站 HTTP 请求都会失败
  - 看起来是有意为之但可能造成问题
- **建议修复**: 
  1. 确认这是有意行为
  2. 文档化 fetch 替换行为
  3. 确保不会意外影响其他代码

#### 16. [网络] [中严重] [无显式 http.Agent 配置]
- **文件/路径**: 全局
- **问题描述**: 
  - 无显式的 http.Agent/https.Agent 配置
  - 依赖 Node.js 默认连接池
  - 无 maxSockets 或 maxConnections 配置
  - 高吞吐场景可能出问题
- **建议修复**: 
  1. 添加 http Agent 配置
  2. 文档化连接池大小
  3. 监控连接使用情况

#### 17. [序列化] [中严重] [合同版本硬编码为字符串字面量]
- **文件/路径**: `src/platform/contracts/inter-plane-contract-gateway.ts:184`
- **问题描述**: 
  - `schemaVersion: "v4.3"` 硬编码
  - 难以追踪合同版本演变
- **建议修复**: 
  1. 提取到类型化常量或 enum
  2. 集中管理合同版本
  3. 添加版本兼容性检查

#### 18. [序列化] [低严重] [Timestamp = string 丢失精度]
- **文件/路径**: `src/platform/contracts/types/domain/primitives.ts:10`
- **问题描述**: 
  - `type Timestamp = string` 无类型精度
  - ISO 8601 字符串无时区信息
- **建议修复**: 
  1. 使用 Date 对象或 branded string 类型
  2. 明确时区处理约定
  3. 文档化时间戳语义

#### 19. [源代码] [中严重] [29处非空断言 !.]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - 使用 `!.` 断言非空
  - 可能导致运行时错误
- **建议修复**: 
  1. 使用可选链 `?.` 或空值合并 `??`
  2. 添加适当的 null 检查
  3. 减少非空断言使用

#### 20. [API] [中严重] [API 路由 schema 版本硬编码]
- **文件/路径**: `src/platform/five-plane-interface/api/federation-routing-service.ts`
- **问题描述**: 
  - Zod schema 验证存在
  - 但 schema 版本未与 contract 版本同步
- **建议修复**: 
  1. 从 contract 版本派生 schema 版本
  2. 添加版本一致性检查
  3. 文档化版本关系

### 总结

本次补充Review（第十五轮 - 构建测试与类型系统Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. 无 TypeScript 增量编译缓存（每次完整重建9.6秒）
2. 76个 npm scripts 大部分重复构建
3. 168处 any 类型使用（绕过类型安全）
4. 38处 @ts-ignore 指令
5. ExecutionRecord 类型不匹配（14个文件）
6. 178处直接 process.env 变更无抽象
7. ScopedExternalAccessSandbox performHttpRequest 无超时
8. OIDC 服务 fetch 调用无超时
9. plugin-runtime-child 替换 globalThis.fetch 为 no-op

**中优先级**:
1. 30+处 as unknown as 双类型转换
2. rimraf extraneous 未声明
3. 21,998处 unlinkSync 无集中清理
4. 无统一 mocking 框架
5. Singleton 状态重置未标准化
6. 无显式 http.Agent 配置
7. 合同版本硬编码为字符串
8. Timestamp = string 丢失精度
9. 非空断言使用

**低优先级**:
1. 无分支覆盖率要求
2. API 路由 schema 版本未同步

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 72 | 87 | 39 | 198 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **109** | **182** | **85** | **376** |

### 优先修复建议

**立即处理 (本周内)**:
1. 添加 TypeScript 增量编译 (`incremental: true`)
2. 重构 npm scripts 避免重复构建
3. 减少 any 类型使用到 <50
4. 移除所有 @ts-ignore 指令
5. 修复 ExecutionRecord 类型不匹配
6. 添加 fetch 超时到 OIDC 和 sandbox

**短期内处理 (本月内)**:
1. 创建集中的测试清理 utility
2. 建立统一的 mocking 框架
3. 标准化 singleton 重置机制
4. 添加 http.Agent 连接池配置
5. 提取合同版本到类型化常量

**长期规划**:
1. 实现完整的类型安全（无 any）
2. 建立测试基础设施标准
3. 实现构建缓存优化
4. 完善网络层超时和重试
5. 建立覆盖率门禁标准

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十四轮 - 日志事件与工作流Review）

### 发现的新问题

#### 1. [可观测性] [高严重] [StructuredLogEntry 无 requestId 字段]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - StructuredLogEntry 接口无 requestId 字段
  - 只有 correlationId，它回退到 traceId
  - 无法独立追踪单个 HTTP 请求
- **建议修复**: 
  1. 在 StructuredLogEntry 添加 requestId 字段
  2. 在 HTTP 中间件中生成和注入 requestId
  3. 确保 requestId 流经所有日志调用

#### 2. [可观测性] [高严重] [StructuredLogger data 字段无自动清洗]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - 如果开发者传递 `{ password: "..." }` 给 logger.info，密码会被记录
  - 无字段级脱敏机制
  - 依赖开发者手动排除敏感字段
- **建议修复**: 
  1. 添加 OBSERVE_OUTPUT_BLACKLIST 类似的黑名单
  2. 实现 redact/mask/sanitize 工具函数
  3. 在日志传输前自动清洗敏感字段

#### 3. [可观测性] [中严重] [日志中敏感数据无 PII 处理]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - 无 PII 重写工具
  - 开发者必须手动避免记录敏感数据
  - 存在意外记录敏感信息的风险
- **建议修复**: 
  1. 添加 PII 检测和重写工具
  2. 在文档中明确记录敏感字段列表
  3. 添加测试验证敏感数据不被记录

#### 4. [可观测性] [中严重] [59处 console.* 直接调用]
- **文件/路径**: 多个源文件
- **问题描述**: 
  - 应该使用 StructuredLogger
  - console 调用可能不被日志聚合系统收集
  - 难以追踪和关联
- **建议修复**: 
  1. 替换所有 console.* 为 StructuredLogger
  2. 添加 ESLint 规则禁止 console.*
  3. 保留少量允许的 CLI 输出

#### 5. [可观测性] [中严重] [部分 logger.warn 调用缺少结构化数据]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:122`
- **问题描述**: 
  ```typescript
  logger.warn("WebSocket connection rejected: missing subprotocol token")
  ```
  - 只有消息，无 data 对象
  - 难以关联和查询
- **建议修复**: 
  1. 添加 `{ data: { ... } }` 对象
  2. 确保所有日志包含上下文数据
  3. 添加日志审查工具

#### 6. [源代码] [高严重] [Harness while(true) 循环无硬性迭代上限]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:1442`
- **问题描述**: 
  ```typescript
  while (true) {
    // ...
    // 依赖 budget gate 和 guardrail vibration 退出
    // 如果两者都失败，循环永久继续
  }
  ```
  - 依赖 budget gate 和 guardrail vibration 退出
  - 无硬性迭代次数限制
  - 可能导致无限循环
- **建议修复**: 
  1. 添加硬性 maxIterations 上限
  2. 在达到上限时强制退出
  3. 记录详细的迭代统计用于调试

#### 7. [源代码] [高严重] [Oapeflir while(true) 循环可能无限重规划]
- **文件/路径**: `src/platform/five-plane-orchestration/oapeflir/oapeflir-loop-service.ts:355`
- **问题描述**: 
  - 如果 loopReplanDecision.shouldReplan 永不变为 false
  - 重规划永远无法减少计划错误
  - 可能导致无限循环
- **建议修复**: 
  1. 添加重规划次数上限
  2. 在达到上限时强制接受当前结果
  3. 添加重规划质量衰减检测

#### 8. [源代码] [中严重] [Recovery 流程允许终态转换到 paused]
- **文件/路径**: `src/platform/five-plane-execution/runtime-state-machine.ts:105-108`
- **问题描述**: 
  - completed, failed, cancelled, aborted 可转换到 paused 进行恢复
  - 创建恢复路径可能重新进入执行
  - 需要验证此行为的正确性
- **建议修复**: 
  1. 审查 recovery 转换的合法性
  2. 确保不会导致状态不一致
  3. 添加转换前置条件验证

#### 9. [国际化] [高严重] [API 错误消息硬编码英文]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/api-error.ts`
- **问题描述**: 
  ```typescript
  return new ApiError(404, "api.task_not_found", "Task not found.");
  ```
  - 错误消息硬编码英文
  - 无后端 i18n 机制
  - 非英文客户端收到不本地化的错误
- **建议修复**: 
  1. 使用错误码而非硬编码消息
  2. 客户端根据错误码本地化
  3. 或在后端添加 i18n 支持

#### 10. [国际化] [中严重] [UI 硬编码字符串未翻译]
- **文件/路径**: `ui/packages/features/conversation/src/web/index.tsx`
- **问题描述**: 
  - "Build Plan", "Execute", "Messages" 等按钮标签硬编码
  - 不使用 i18n 系统
- **建议修复**: 
  1. 使用 `translateFeatureCopy` 替换硬编码
  2. 确保所有用户可见文本通过 i18n
  3. 添加硬编码字符串检测

#### 11. [国际化] [中严重] [Arabic 目录不完整]
- **文件/路径**: `ui/packages/shared/i18n/src/catalogs/ar-SA.ts`
- **问题描述**: 
  - ar-SA 只有约18条消息
  - en-US 和 zh-CN 有约117条
  - 缺失的 key 返回 key 本身
- **建议修复**: 
  1. 补充 Arabic 翻译
  2. 确保所有 key 都有翻译
  3. 添加翻译完整性测试

#### 12. [国际化] [低严重] [translateFeatureCopy 无回退默认值]
- **文件/路径**: `ui/packages/shared/i18n/`
- **问题描述**: 
  - 如果 feature ID 不在 catalog 中
  - 返回 undefined title/summary
  - 无合理回退
- **建议修复**: 
  1. 添加回退到 key 或默认字符串
  2. 或使用 feature ID 作为显示名
  3. 确保 UI 不显示 undefined

#### 13. [运维] [高严重] [健康检查无 readiness/liveness 区分]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts`
- **问题描述**: 
  - /healthz, /v1/healthz, /health 返回相同报告
  - 无 readiness probe（可处理流量？）
  - 无 liveness probe（系统存活？）
  - canAcceptTraffic() 未通过 HTTP 暴露
- **建议修复**: 
  1. 添加 /ready 和 /live 端点
  2. Readiness 检查依赖项就绪
  3. Liveness 检查进程健康

#### 14. [运维] [高严重] [shutdown 期间健康检查存在竞态]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts`
- **问题描述**: 
  - getHealthReportAsync() 异步检查 dbWritable
  - shutdown 期间数据库连接可能关闭
  - 健康检查仍在传入
- **建议修复**: 
  1. 在 shutdown 时返回 unhealthy
  2. 添加 shutdown 状态标志
  3. 停止接受新的健康检查请求

#### 15. [运维] [高严重] [LeaderElectionService 未与 GracefulShutdown 集成]
- **文件/路径**: `src/platform/five-plane-execution/ha/leader-election-service.ts`
- **问题描述**: 
  - LeaderElectionService.stop() 处理优雅退位
  - 但未在全局 graceful shutdown 中注册
  - 需显式注册为 shutdown handler
- **建议修复**: 
  1. 将 LeaderElectionService 添加到 shutdown handlers
  2. 确保领导者在 shutdown 时让位
  3. 验证关闭顺序正确

#### 16. [运维] [中严重] [addHandler() 无显式排序约定]
- **文件/路径**: `src/platform/five-plane-execution/startup/graceful-shutdown.ts`
- **问题描述**: 
  - 虽然 handlers 逆序执行
  - 但无显式接口声明 handler 依赖关系
  - 关键 handlers（如关闭数据库连接）应最后运行
- **建议修复**: 
  1. 添加优先级或阶段参数
  2. 文档化关键 handler 顺序要求
  3. 添加顺序验证测试

#### 17. [运维] [中严重] [unref'd timers 可能不触发]
- **文件/路径**: `src/platform/five-plane-execution/startup/process-error-handlers.ts:106`
- **问题描述**: 
  - `setTimeout(...).unref()` 不保持事件循环活跃
  - 如果主线程空闲，强制退出可能不触发
- **建议修复**: 
  1. 移除 .unref() 除非确实需要
  2. 或确保有其他方式保持事件循环
  3. 添加超时触发验证测试

#### 18. [运维] [中严重] [StartupConsistencyChecker 阻塞流量但不暴露状态]
- **文件/路径**: `src/platform/five-plane-startup-plan.ts`
- **问题描述**: 
  - canAcceptTraffic() 在 P0 问题存在时返回 false
  - 但 _trafficBlocked 是内部状态
  - 负载均衡器访问 /healthz 仍会得到 "ok"
- **建议修复**: 
  1. 在健康报告中暴露 trafficBlocked 状态
  2. 或添加单独的健康端点指示就绪状态
  3. 确保负载均衡器在阻塞时不路由流量

#### 19. [事件系统] [中严重] [TypedEventBus vs EventEmitter 混用]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - 项目同时使用 TypedEventBus 和原生 EventEmitter
  - TypedEventBus 应该是标准
  - 混用可能导致类型安全问题
- **建议修复**: 
  1. 创建迁移计划统一使用 TypedEventBus
  2. 添加 ESLint 规则禁止原生 EventEmitter
  3. 在 CI 中检测混用情况

#### 20. [源代码] [低严重] [无事件排序保证]
- **文件/路径**: `src/platform/five-plane-state-evidence/events/`
- **问题描述**: 
  - 无明确的事件排序保证文档
  - 事件处理可能乱序
  - 依赖方需要处理乱序
- **建议修复**: 
  1. 文档化事件排序语义
  2. 如果需要排序，实现序列号机制
  3. 在事件处理中考虑乱序情况

### 总结

本次补充Review（第十四轮 - 日志事件与工作流Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. StructuredLogEntry 无 requestId 字段
2. StructuredLogger data 字段无自动清洗（敏感数据风险）
3. Harness while(true) 循环无硬性迭代上限
4. Oapeflir while(true) 循环可能无限重规划
5. API 错误消息硬编码英文（无 i18n）
6. 健康检查无 readiness/liveness 区分
7. shutdown 期间健康检查存在竞态
8. LeaderElectionService 未与 GracefulShutdown 集成

**中优先级**:
1. 日志中敏感数据无 PII 处理
2. 59处 console.* 直接调用
3. 部分 logger.warn 调用缺少结构化数据
4. Recovery 流程允许终态转换到 paused
5. UI 硬编码字符串未翻译
6. Arabic 目录不完整
7. addHandler() 无显式排序约定
8. unref'd timers 可能不触发
9. StartupConsistencyChecker 阻塞流量但不暴露状态
10. TypedEventBus vs EventEmitter 混用

**低优先级**:
1. translateFeatureCopy 无回退默认值
2. 无事件排序保证

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 68 | 80 | 37 | 185 |
| 测试 | 8 | 14 | 5 | 27 |
| 配置 | 10 | 38 | 24 | 72 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **103** | **171** | **82** | **356** |

### 优先修复建议

**立即处理 (本周内)**:
1. 在 StructuredLogEntry 添加 requestId 字段
2. 实现日志敏感字段自动清洗
3. 为工作流循环添加硬性迭代上限
4. 添加 readiness/liveness 健康检查端点
5. 将 LeaderElectionService 集成到 GracefulShutdown
6. 实现 API 错误消息 i18n 支持

**短期内处理 (本月内)**:
1. 替换所有 console.* 为 StructuredLogger
2. 补充 Arabic i18n 目录
3. 修复 UI 硬编码字符串
4. 统一 TypedEventBus 替代 EventEmitter
5. 解决 shutdown 竞态条件
6. 添加事件排序保证文档

**长期规划**:
1. 建立完整的 PII 处理框架
2. 实现日志完整性验证
3. 建立工作流安全循环检测
4. 完善健康检查和就绪探测
5. 建立国际化完整测试

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十五轮 - 构建测试与类型系统Review）

### 发现的新问题

#### 1. [构建] [高严重] [无 TypeScript 增量编译缓存]
- **文件/路径**: `tsconfig.json`
- **问题描述**: 
  - 无 `incremental: true` 配置
  - 无 `tsbuildinfo` 文件
  - 每次构建是完整重建（9.6秒）
  - TypeScript 无法使用增量编译
- **建议修复**: 
  1. 在 tsconfig.json 添加 `"incremental": true`
  2. 添加 `"tsbuildinfo": ".tsbuildinfo"` 到 .gitignore
  3. 实现项目引用以支持并行构建

#### 2. [构建] [高严重] [76个 npm scripts 大部分重复构建]
- **文件/路径**: `package.json`
- **问题描述**: 
  - 40+ CLI scripts 都运行 `npm run build && node ...`
  - 每个脚本前都执行完整重建
  - 无法直接运行预构建的 CLI
  - 20+ stable:* 脚本遵循相同模式
- **建议修复**: 
  1. 创建 `build:cli` 脚本构建一次
  2. 让 CLI 脚本使用预构建的 dist 文件
  3. 添加 npm run dev 或类似的无需重建的执行方式

#### 3. [源代码] [高严重] [168处 any 类型使用]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - `stable-runner-factory.ts`: `opts: any` -> `any`
  - `billing-service-async.ts`: `input: any` 方法签名
  - 重试/超时工具使用 `(...args: any[]) => Promise<any>`
  - 绕过类型安全
- **建议修复**: 
  1. 使用泛型替代 any
  2. 替换为具体的输入类型
  3. 添加 ESLint 规则禁止 any

#### 4. [源代码] [高严重] [38处 @ts-ignore 指令]
- **文件/路径**: 多个文件
- **问题描述**: 
  - 14个 stability 相关文件有 `@ts-ignore ExecutionRecord type mismatch`
  - 5个 harness-sdk 文件有 `Partial<HarnessRun> doesn't have all required properties`
  - 2个文件有 `exactOptionalPropertyTypes` 问题
- **建议修复**: 
  1. 修复 ExecutionRecord 类型不匹配
  2. 完成 HarnessRun 类型的必需属性
  3. 移除 @ts-ignore 使用

#### 5. [源代码] [高严重] [ExecutionRecord 类型不匹配]
- **文件/路径**: `src/platform/stability/stable-evidence-bundle-support.ts:783` 等14个文件
- **问题描述**: 
  - 插入 execution record 时 TypeScript 拒绝类型
  - `ExecutionRecord` 接口要求新字段但 insert 对象缺失
  - 或 store 的 insert 方法接受不同类型
- **建议修复**: 
  1. 对齐 ExecutionRecord 接口和 store 类型
  2. 添加缺失字段到 insert 对象
  3. 或调整 store 的 insert 方法类型签名

#### 6. [源代码] [中严重] [30+处 as unknown as 双类型转换]
- **文件/路径**: `division-loader.ts`, `domain-baseline-catalog.ts`, `harness-sdk`
- **问题描述**: 
  - 完全绕过类型安全
  - 通常是类型架构问题的信号
- **建议修复**: 
  1. 修复上游类型问题
  2. 使用更具体的类型断言
  3. 重构类型层次结构

#### 7. [构建] [中严重] [rimraf  extraneous - 未在 package.json 声明]
- **文件/路径**: `node_modules/rimraf`
- **问题描述**: 
  - rimraf@6.1.3 存在于 node_modules 但未在 package.json 声明
  - 可能导致依赖问题
- **建议修复**: 
  1. 添加到 dependencies 或 devDependencies
  2. 或移除并使用其他清理方式

#### 8. [测试] [高严重] [178处直接 process.env 变更无抽象]
- **文件/路径**: `tests/unit/domains/registry/plugin-spi-registry.test.ts:640-687`
- **问题描述**: 
  - 直接读取/写入 process.env
  - 在 beforeEach/afterEach 中手动保存/恢复
  - 无 helper 抽象
- **建议修复**: 
  1. 创建 env helper 函数
  2. 统一保存和恢复模式
  3. 添加测试隔离验证

#### 9. [测试] [高严重] [21,998处 unlinkSync 调用无集中清理]
- **文件/路径**: 多个测试文件
- **问题描述**: 
  - 每个测试重新实现清理逻辑
  - 无集中清理 utility
  - 可能导致 EBUSY (Windows) 或并发访问问题
- **建议修复**: 
  1. 创建集中的测试清理 utility
  2. 使用 afterEach 确保清理
  3. 考虑使用临时目录

#### 10. [测试] [中严重] [无统一 mocking 框架]
- **文件/路径**: `tests/` 多个目录
- **问题描述**: 
  - 10,579 处 vi.fn, jest.mock, sinon 等调用
  - 无统一的 mocking 框架可见
  - 模式不一致
- **建议修复**: 
  1. 建立统一的 mock factory
  2. 标准化 mock 模式
  3. 添加 mocking 最佳实践文档

#### 11. [测试] [中严重] [Singleton 状态重置未标准化]
- **文件/路径**: `tests/unit/platform/five-plane-execution/dispatcher.test.ts`
- **问题描述**: 
  - 测试通过特定函数重置 singletons
  - 每个测试重复自己的 reset 逻辑
  - 无统一的 resetAllSingletons()
- **建议修复**: 
  1. 创建统一的 singleton reset 机制
  2. 在 afterEach 中自动调用
  3. 文档化 singleton 重置要求

#### 12. [测试] [低严重] [无分支覆盖率要求]
- **文件/路径**: `.c8rc.json`
- **问题描述**: 
  - `"100": false` - 无覆盖率要求
  - 只测量源代码，不测量测试文件
- **建议修复**: 
  1. 添加分支覆盖率阈值
  2. 在 CI 中强制执行覆盖率 gate
  3. 关注关键代码路径

#### 13. [网络] [高严重] [ScopedExternalAccessSandbox performHttpRequest 无超时]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/scoped-external-access-sandbox.ts:289`
- **问题描述**: 
  - fetch 调用无 timeout 参数
  - 挂起的请求可能永久阻塞
- **建议修复**: 
  1. 添加 AbortController timeout
  2. 使用标准的超时模式
  3. 记录超时配置

#### 14. [网络] [高严重] [OIDC 服务 fetch 调用无超时]
- **文件/路径**: `src/org-governance/sso-scim/oidc/oidc-service.ts:565,272`
- **问题描述**: 
  - OIDC token 交换无 retry logic
  - 无 timeout/abort signal
  - 认证流程可能挂起
- **建议修复**: 
  1. 添加 fetch timeout
  2. 添加 retry logic
  3. 使用标准的超时配置

#### 15. [网络] [高严重] [plugin-runtime-child 替换 globalThis.fetch 为 no-op]
- **文件/路径**: `src/domains/registry/plugin-runtime-child.ts:112`
- **问题描述**: 
  ```typescript
  globalThis.fetch = (async () => {}) as unknown as typeof globalThis.fetch;
  ```
  - 插件上下文中完全替换 fetch 为空函数
  - 插件的所有出站 HTTP 请求都会失败
  - 看起来是有意为之但可能造成问题
- **建议修复**: 
  1. 确认这是有意行为
  2. 文档化 fetch 替换行为
  3. 确保不会意外影响其他代码

#### 16. [网络] [中严重] [无显式 http.Agent 配置]
- **文件/路径**: 全局
- **问题描述**: 
  - 无显式的 http.Agent/https.Agent 配置
  - 依赖 Node.js 默认连接池
  - 无 maxSockets 或 maxConnections 配置
  - 高吞吐场景可能出问题
- **建议修复**: 
  1. 添加 http Agent 配置
  2. 文档化连接池大小
  3. 监控连接使用情况

#### 17. [序列化] [中严重] [合同版本硬编码为字符串字面量]
- **文件/路径**: `src/platform/contracts/inter-plane-contract-gateway.ts:184`
- **问题描述**: 
  - `schemaVersion: "v4.3"` 硬编码
  - 难以追踪合同版本演变
- **建议修复**: 
  1. 提取到类型化常量或 enum
  2. 集中管理合同版本
  3. 添加版本兼容性检查

#### 18. [序列化] [低严重] [Timestamp = string 丢失精度]
- **文件/路径**: `src/platform/contracts/types/domain/primitives.ts:10`
- **问题描述**: 
  - `type Timestamp = string` 无类型精度
  - ISO 8601 字符串无时区信息
- **建议修复**: 
  1. 使用 Date 对象或 branded string 类型
  2. 明确时区处理约定
  3. 文档化时间戳语义

#### 19. [源代码] [中严重] [29处非空断言 !.]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - 使用 `!.` 断言非空
  - 可能导致运行时错误
- **建议修复**: 
  1. 使用可选链 `?.` 或空值合并 `??`
  2. 添加适当的 null 检查
  3. 减少非空断言使用

#### 20. [API] [中严重] [API 路由 schema 版本硬编码]
- **文件/路径**: `src/platform/five-plane-interface/api/federation-routing-service.ts`
- **问题描述**: 
  - Zod schema 验证存在
  - 但 schema 版本未与 contract 版本同步
- **建议修复**: 
  1. 从 contract 版本派生 schema 版本
  2. 添加版本一致性检查
  3. 文档化版本关系

### 总结

本次补充Review（第十五轮 - 构建测试与类型系统Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. 无 TypeScript 增量编译缓存（每次完整重建9.6秒）
2. 76个 npm scripts 大部分重复构建
3. 168处 any 类型使用（绕过类型安全）
4. 38处 @ts-ignore 指令
5. ExecutionRecord 类型不匹配（14个文件）
6. 178处直接 process.env 变更无抽象
7. ScopedExternalAccessSandbox performHttpRequest 无超时
8. OIDC 服务 fetch 调用无超时
9. plugin-runtime-child 替换 globalThis.fetch 为 no-op

**中优先级**:
1. 30+处 as unknown as 双类型转换
2. rimraf extraneous 未声明
3. 21,998处 unlinkSync 无集中清理
4. 无统一 mocking 框架
5. Singleton 状态重置未标准化
6. 无显式 http.Agent 配置
7. 合同版本硬编码为字符串
8. Timestamp = string 丢失精度
9. 非空断言使用

**低优先级**:
1. 无分支覆盖率要求
2. API 路由 schema 版本未同步

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 72 | 87 | 39 | 198 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **109** | **182** | **85** | **376** |

### 优先修复建议

**立即处理 (本周内)**:
1. 添加 TypeScript 增量编译 (`incremental: true`)
2. 重构 npm scripts 避免重复构建
3. 减少 any 类型使用到 <50
4. 移除所有 @ts-ignore 指令
5. 修复 ExecutionRecord 类型不匹配
6. 添加 fetch 超时到 OIDC 和 sandbox

**短期内处理 (本月内)**:
1. 创建集中的测试清理 utility
2. 建立统一的 mocking 框架
3. 标准化 singleton 重置机制
4. 添加 http.Agent 连接池配置
5. 提取合同版本到类型化常量

**长期规划**:
1. 实现完整的类型安全（无 any）
2. 建立测试基础设施标准
3. 实现构建缓存优化
4. 完善网络层超时和重试
5. 建立覆盖率门禁标准

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十二轮 - 错误处理与多租户安全Review）

### 发现的新问题

#### 1. [安全] [严重] [listQuotas() 返回所有租户配额 - 信息泄露]
- **文件/路径**: `src/platform/five-plane-control-plane/tenant/tenant-execution-isolation-service.ts:222-234`
- **问题描述**: 
  ```typescript
  listQuotas(tenantId?: string): TenantQuota[] {
    if (tenantId) {
      return this.db.connection.prepare(`SELECT * FROM tenant_quotas WHERE tenant_id = ?`).all(tenantId);
    }
    // WITHOUT tenantId: 返回所有租户的配额！
    return this.db.connection.prepare(`SELECT * FROM tenant_quotas`).all();
  }
  ```
  - 当 tenantId 为空时返回所有租户的配额
  - 未经授权的信息泄露
- **建议修复**: 
  1. 要求调用者必须提供 tenantId
  2. 添加权限验证确保只能查看自己的配额
  3. 审计日志记录所有配额访问

#### 2. [安全] [高严重] [assertTaskTenantAccess() 返回404而非403 - 资源存在泄露]
- **文件/路径**: `src/platform/five-plane-interface/api/utils.ts:146-148`
- **问题描述**: 
  ```typescript
  if (resourceTenantId !== principal.tenantId) {
    throw new ApiError(404, notFoundCode, notFoundMessage);  // 应该是403
  }
  ```
  - 跨租户访问返回404而非403
  - 泄露了资源存在但属于其他租户的信息
  - 攻击者可检测有效task ID
- **建议修复**: 
  1. 改为返回403 Forbidden
  2. 不要泄露资源是否存在

#### 3. [安全] [高严重] [crossTenantRequest 标志由调用者控制]
- **文件/路径**: `src/platform/five-plane-interface/org-routing/index.ts:163`
- **问题描述**: 
  - `routeOrgBudget()` 接受 `crossTenantRequest?: boolean` 参数
  - 该标志由调用者设置，非系统派生
  - 攻击者可能操纵此标志绕过跨租户限制
- **建议修复**: 
  1. 标志应由系统状态派生，不接受调用者输入
  2. 添加审计日志追踪跨租户请求
  3. 实现零信任跨租户访问模型

#### 4. [源代码] [高严重] [single-task-happy-path 无重试机制]
- **文件/路径**: `src/platform/five-plane-execution/execution-engine/single-task-happy-path.ts:336-337`
- **问题描述**: 
  - `maxRetries: 0` 和 `retryBackoff: "none"`
  - 瞬态失败无自动重试
  - LLM调用失败时无fallback到备用provider
- **建议修复**: 
  1. 启用非关键执行的重试
  2. 实现LLM provider fallback链（Anthropic → OpenAI → MiniMax）
  3. 使用domain baseline catalog的primary/fallback模型偏好

#### 5. [源代码] [高严重] [LLM调用无熔断器保护]
- **文件/路径**: `src/platform/five-plane-execution/execution-engine/model-call-provider.ts`
- **问题描述**: 
  - 熔断器只存在于 channel-gateway 和 call-governance
  - 直接LLM provider调用无熔断器
  - provider降级时无法快速失败
- **建议修复**: 
  1. 在model-call-provider.ts添加熔断器
  2. 当provider失败率超阈值时快速失败
  3. 自动切换到备用provider

#### 6. [安全] [高严重] [分布式限流器 bypass - 实例间不共享]
- **文件/路径**: `src/platform/five-plane-interface/ingress/distributed-rate-limiter.ts:33`
- **问题描述**: 
  - Redis未配置时使用本地Map
  - 每个实例维护自己的localEntries
  - 攻击者可通过切换实例绕过限流
- **建议修复**: 
  1. 要求Redis配置用于生产
  2. 或使用Redis作为所有实例的共享后端
  3. 检测并警告未配置Redis的实例

#### 7. [配置] [中严重] [Soft quota (log_only) 实际不限制]
- **文件/路径**: `src/platform/five-plane-control-plane/tenant/tenant-execution-isolation-service.ts:448`
- **问题描述**: 
  ```typescript
  allowed: quota.enforcementAction === "log_only",  // log_only时返回true
  ```
  - soft quota只记录日志，不实际阻止
  - 配额形同虚设
- **建议修复**: 
  1. 明确区分"监控模式"和"强制模式"
  2. 对于硬配额必须阻止
  3. 文档化quota类型的实际行为

#### 8. [配置] [中严重] [HTTP层无限流头 - 客户端无法获知限制]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/response-hardening.ts`
- **问题描述**: 
  - 未返回 X-RateLimit-* 头
  - 只在429响应时返回retry-after-ms
  - 客户端无法主动管理请求速率
- **建议修复**: 
  1. 在所有响应添加 X-RateLimit-Limit, X-RateLimit-Remaining, X-RateLimit-Reset
  2. 使用标准头名称
  3. 遵循RFC 6585

#### 9. [配置] [中严重] [HTTP层无限流 - 可选且仅按IP]
- **文件/路径**: `src/platform/five-plane-interface/api/http-api-server.ts:334-351`
- **问题描述**: 
  - 限流仅在 `this.rateLimiter != null` 时生效
  - 限流key为 `${clientIp}:${endpoint}` 仅按IP
  - 无per-tenant/per-principal enforcement
- **建议修复**: 
  1. 默认启用限流
  2. 添加per-tenant限流key
  3. 确保所有环境默认配置限流

#### 10. [安全] [中严重] [processRuleMode 可能未强制执行]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/plugin-runtime-host.ts`
- **问题描述**: 
  - `SandboxPolicy.processRuleMode` 设置为 "allow" 或 "deny"
  - 但未找到实际阻止基于此策略生成进程的实际执行代码
  - `--allow-child-process` 标志仅在 sandboxed_process 隔离模式应用
- **建议修复**: 
  1. 验证processRuleMode实际被强制执行
  2. 确保所有隔离模式正确处理
  3. 添加测试验证进程创建被阻止

#### 11. [安全] [中严重] [容器模板替换未验证]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/plugin-runtime-host.ts:751`
- **问题描述**: 
  ```typescript
  rendered = rendered.split(placeholder).join(value);
  ```
  - 简单字符串替换
  - 虽然validateContainerLaunchPluginId()阻止\0和引号
  - 但模板替换本身未验证
- **建议修复**: 
  1. 添加输入验证防止注入
  2. 使用更安全的模板引擎
  3. 验证所有占位符被替换

#### 12. [安全] [中严重] [Adapter执行绕过沙箱]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/adapter-executor.ts`
- **问题描述**: 
  - Adapter执行REST/grpc/MQ调用不经过ScopedExternalAccessSandbox
  - 有自己的allowedDomains配置但非集中管理
  - 可能发起未限制的出站请求
- **建议修复**: 
  1. Adapter执行经过集中沙箱
  2. 统一external access策略
  3. 添加adapter出站请求审计

#### 13. [安全] [中严重] [null principal.tenantId 时静默放行]
- **文件/路径**: `src/platform/five-plane-interface/api/utils.ts:143-145`
- **问题描述**: 
  ```typescript
  if (principal.tenantId == null) {
    return;  // 静默放行
  }
  ```
  - 如果API网关允许无principal认证的请求通过
  - 可能授予跨租户访问权限
- **建议修复**: 
  1. 要求所有API请求有有效principal
  2. null tenantId应拒绝而非静默放行
  3. 添加审计日志

#### 14. [源代码] [中严重] [无LLM provider fallback链]
- **文件/路径**: `src/platform/five-plane-execution/execution-engine/model-call-provider.ts`
- **问题描述**: 
  - 主provider失败时无自动切换
  - domain-baseline-catalog.ts定义了primary/fallback模型偏好但未使用
  - 第一个provider失败即导致执行失败
- **建议修复**: 
  1. 实现provider fallback链
  2. 使用domain baseline的primary/fallback配置
  3. 按优先级尝试直到成功或全部失败

#### 15. [源代码] [中严重] [超时值硬编码]
- **文件/路径**: 多个文件
- **问题描述**: 
  - Effect buffer有固定超时reject
  - Channel gateway的requestTimeoutMs虽可配置但有max 30s限制
  - 无全局请求超时中间件
- **建议修复**: 
  1. 集中超时配置
  2. per-environment可配置
  3. 添加全局超时中间件

#### 16. [数据库] [中严重] [无down migration - 回滚不支持]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/sqlite/migration-runner.ts:45-47`
- **问题描述**: 
  - `rollbackSupported = false`
  - 每个migration只有 `downSql` 占位符
  - schema migration无法回滚
- **建议修复**: 
  1. 文档化回滚限制
  2. 在变更前创建完整备份
  3. 使用blue-green部署减少回滚需求

#### 17. [数据库] [中严重] [Migration 44特殊处理 - 表创建重复]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/sqlite/sqlite-migration-runtime-part3.ts`
- **问题描述**: 
  - Migration 43创建harness_runs表
  - Migration 44再次创建同名表（不同schema）
  - 使用applyCompatibleColumnMigrationIfKnown特殊处理
- **建议修复**: 
  1. 消除重复表创建
  2. 使用ALTER TABLE而非CREATE TABLE
  3. 简化migration逻辑

#### 18. [配置] [低严重] [Provider限流头未转发给客户端]
- **文件/路径**: `src/platform/model-gateway/provider-registry/base-chat-provider.ts:153`
- **问题描述**: 
  - 系统读取provider的ratelimitResetHeaderNames
  - 但不转发给客户端
  - 客户端无法知道provider限流状态
- **建议修复**: 
  1. 转发Provider的限流头
  2. 或添加应用层限流信息
  3. 帮助客户端优化请求

#### 19. [安全] [低严重] [Browser evaluate 接受任意脚本]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/browser-executor.ts:326`
- **问题描述**: 
  - `evaluate` action接受script参数
  - 在浏览器上下文运行（模拟）
  - 如浏览器上下文未正确沙箱化可能是向量
- **建议修复**: 
  1. 验证浏览器上下文正确沙箱化
  2. 消毒或限制script内容
  3. 记录所有evaluate调用

#### 20. [源代码] [低严重] [无全局错误边界包装器]
- **文件/路径**: `src/platform/five-plane-execution/execution-engine/`
- **问题描述**: 
  - 执行引擎缺少unhandled promise rejection的全局处理
  - single-task-happy-path的LLM fallback失败时错误向上传播
- **建议修复**: 
  1. 在执行引擎添加错误边界包装
  2. 统一错误处理模式
  3. 确保所有错误被捕获和记录

### 总结

本次补充Review（第十二轮 - 错误处理与多租户安全Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. listQuotas()返回所有租户配额（信息泄露）
2. assertTaskTenantAccess()返回404泄露资源存在
3. crossTenantRequest标志可被操纵
4. single-task-happy-path无重试（maxRetries=0）
5. LLM调用无熔断器保护
6. 分布式限流器可被bypass（实例间不共享）

**中优先级**:
1. Soft quota实际不阻止
2. 无X-RateLimit-*响应头
3. HTTP层限流可选且仅按IP
4. processRuleMode可能未强制
5. 容器模板替换未验证
6. Adapter执行绕过沙箱
7. null principal静默放行
8. 无LLM provider fallback链
9. 超时值硬编码
10. 无down migration
11. Migration 44特殊处理

**低优先级**:
1. Provider限流头未转发
2. Browser evaluate接受任意脚本
3. 无全局错误边界

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 60 | 68 | 31 | 159 |
| 测试 | 8 | 14 | 5 | 27 |
| 配置 | 8 | 35 | 21 | 64 |
| 安全 | 12 | 13 | 3 | 28 |
| 文档 | 2 | 7 | 3 | 12 |
| UI | 0 | 3 | 3 | 6 |
| 部署 | 1 | 10 | 6 | 17 |
| **合计** | **91** | **150** | **72** | **313** |

### 优先修复建议

**立即处理 (本周内)**:
1. 修复listQuotas()信息泄露漏洞
2. 修正assertTaskTenantAccess()返回403
3. 移除crossTenantRequest调用者控制
4. 启用执行引擎重试机制
5. 添加LLM provider熔断器

**短期内处理 (本月内)**:
1. 修复分布式限流器bypass问题
2. 添加X-RateLimit-*响应头
3. 启用默认HTTP限流（per-tenant）
4. 实现LLM provider fallback链
5. 验证并强制processRuleMode

**长期规划**:
1. 建立完整的限流和配额体系
2. 实现插件系统安全审计
3. 建立多租户安全测试
4. 完善错误处理和恢复机制
5. 统一超时和熔断配置

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十五轮 - 构建测试与类型系统Review）

### 发现的新问题

#### 1. [构建] [高严重] [无 TypeScript 增量编译缓存]
- **文件/路径**: `tsconfig.json`
- **问题描述**: 
  - 无 `incremental: true` 配置
  - 无 `tsbuildinfo` 文件
  - 每次构建是完整重建（9.6秒）
  - TypeScript 无法使用增量编译
- **建议修复**: 
  1. 在 tsconfig.json 添加 `"incremental": true`
  2. 添加 `"tsbuildinfo": ".tsbuildinfo"` 到 .gitignore
  3. 实现项目引用以支持并行构建

#### 2. [构建] [高严重] [76个 npm scripts 大部分重复构建]
- **文件/路径**: `package.json`
- **问题描述**: 
  - 40+ CLI scripts 都运行 `npm run build && node ...`
  - 每个脚本前都执行完整重建
  - 无法直接运行预构建的 CLI
  - 20+ stable:* 脚本遵循相同模式
- **建议修复**: 
  1. 创建 `build:cli` 脚本构建一次
  2. 让 CLI 脚本使用预构建的 dist 文件
  3. 添加 npm run dev 或类似的无需重建的执行方式

#### 3. [源代码] [高严重] [168处 any 类型使用]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - `stable-runner-factory.ts`: `opts: any` -> `any`
  - `billing-service-async.ts`: `input: any` 方法签名
  - 重试/超时工具使用 `(...args: any[]) => Promise<any>`
  - 绕过类型安全
- **建议修复**: 
  1. 使用泛型替代 any
  2. 替换为具体的输入类型
  3. 添加 ESLint 规则禁止 any

#### 4. [源代码] [高严重] [38处 @ts-ignore 指令]
- **文件/路径**: 多个文件
- **问题描述**: 
  - 14个 stability 相关文件有 `@ts-ignore ExecutionRecord type mismatch`
  - 5个 harness-sdk 文件有 `Partial<HarnessRun> doesn't have all required properties`
  - 2个文件有 `exactOptionalPropertyTypes` 问题
- **建议修复**: 
  1. 修复 ExecutionRecord 类型不匹配
  2. 完成 HarnessRun 类型的必需属性
  3. 移除 @ts-ignore 使用

#### 5. [源代码] [高严重] [ExecutionRecord 类型不匹配]
- **文件/路径**: `src/platform/stability/stable-evidence-bundle-support.ts:783` 等14个文件
- **问题描述**: 
  - 插入 execution record 时 TypeScript 拒绝类型
  - `ExecutionRecord` 接口要求新字段但 insert 对象缺失
  - 或 store 的 insert 方法接受不同类型
- **建议修复**: 
  1. 对齐 ExecutionRecord 接口和 store 类型
  2. 添加缺失字段到 insert 对象
  3. 或调整 store 的 insert 方法类型签名

#### 6. [源代码] [中严重] [30+处 as unknown as 双类型转换]
- **文件/路径**: `division-loader.ts`, `domain-baseline-catalog.ts`, `harness-sdk`
- **问题描述**: 
  - 完全绕过类型安全
  - 通常是类型架构问题的信号
- **建议修复**: 
  1. 修复上游类型问题
  2. 使用更具体的类型断言
  3. 重构类型层次结构

#### 7. [构建] [中严重] [rimraf  extraneous - 未在 package.json 声明]
- **文件/路径**: `node_modules/rimraf`
- **问题描述**: 
  - rimraf@6.1.3 存在于 node_modules 但未在 package.json 声明
  - 可能导致依赖问题
- **建议修复**: 
  1. 添加到 dependencies 或 devDependencies
  2. 或移除并使用其他清理方式

#### 8. [测试] [高严重] [178处直接 process.env 变更无抽象]
- **文件/路径**: `tests/unit/domains/registry/plugin-spi-registry.test.ts:640-687`
- **问题描述**: 
  - 直接读取/写入 process.env
  - 在 beforeEach/afterEach 中手动保存/恢复
  - 无 helper 抽象
- **建议修复**: 
  1. 创建 env helper 函数
  2. 统一保存和恢复模式
  3. 添加测试隔离验证

#### 9. [测试] [高严重] [21,998处 unlinkSync 调用无集中清理]
- **文件/路径**: 多个测试文件
- **问题描述**: 
  - 每个测试重新实现清理逻辑
  - 无集中清理 utility
  - 可能导致 EBUSY (Windows) 或并发访问问题
- **建议修复**: 
  1. 创建集中的测试清理 utility
  2. 使用 afterEach 确保清理
  3. 考虑使用临时目录

#### 10. [测试] [中严重] [无统一 mocking 框架]
- **文件/路径**: `tests/` 多个目录
- **问题描述**: 
  - 10,579 处 vi.fn, jest.mock, sinon 等调用
  - 无统一的 mocking 框架可见
  - 模式不一致
- **建议修复**: 
  1. 建立统一的 mock factory
  2. 标准化 mock 模式
  3. 添加 mocking 最佳实践文档

#### 11. [测试] [中严重] [Singleton 状态重置未标准化]
- **文件/路径**: `tests/unit/platform/five-plane-execution/dispatcher.test.ts`
- **问题描述**: 
  - 测试通过特定函数重置 singletons
  - 每个测试重复自己的 reset 逻辑
  - 无统一的 resetAllSingletons()
- **建议修复**: 
  1. 创建统一的 singleton reset 机制
  2. 在 afterEach 中自动调用
  3. 文档化 singleton 重置要求

#### 12. [测试] [低严重] [无分支覆盖率要求]
- **文件/路径**: `.c8rc.json`
- **问题描述**: 
  - `"100": false` - 无覆盖率要求
  - 只测量源代码，不测量测试文件
- **建议修复**: 
  1. 添加分支覆盖率阈值
  2. 在 CI 中强制执行覆盖率 gate
  3. 关注关键代码路径

#### 13. [网络] [高严重] [ScopedExternalAccessSandbox performHttpRequest 无超时]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/scoped-external-access-sandbox.ts:289`
- **问题描述**: 
  - fetch 调用无 timeout 参数
  - 挂起的请求可能永久阻塞
- **建议修复**: 
  1. 添加 AbortController timeout
  2. 使用标准的超时模式
  3. 记录超时配置

#### 14. [网络] [高严重] [OIDC 服务 fetch 调用无超时]
- **文件/路径**: `src/org-governance/sso-scim/oidc/oidc-service.ts:565,272`
- **问题描述**: 
  - OIDC token 交换无 retry logic
  - 无 timeout/abort signal
  - 认证流程可能挂起
- **建议修复**: 
  1. 添加 fetch timeout
  2. 添加 retry logic
  3. 使用标准的超时配置

#### 15. [网络] [高严重] [plugin-runtime-child 替换 globalThis.fetch 为 no-op]
- **文件/路径**: `src/domains/registry/plugin-runtime-child.ts:112`
- **问题描述**: 
  ```typescript
  globalThis.fetch = (async () => {}) as unknown as typeof globalThis.fetch;
  ```
  - 插件上下文中完全替换 fetch 为空函数
  - 插件的所有出站 HTTP 请求都会失败
  - 看起来是有意为之但可能造成问题
- **建议修复**: 
  1. 确认这是有意行为
  2. 文档化 fetch 替换行为
  3. 确保不会意外影响其他代码

#### 16. [网络] [中严重] [无显式 http.Agent 配置]
- **文件/路径**: 全局
- **问题描述**: 
  - 无显式的 http.Agent/https.Agent 配置
  - 依赖 Node.js 默认连接池
  - 无 maxSockets 或 maxConnections 配置
  - 高吞吐场景可能出问题
- **建议修复**: 
  1. 添加 http Agent 配置
  2. 文档化连接池大小
  3. 监控连接使用情况

#### 17. [序列化] [中严重] [合同版本硬编码为字符串字面量]
- **文件/路径**: `src/platform/contracts/inter-plane-contract-gateway.ts:184`
- **问题描述**: 
  - `schemaVersion: "v4.3"` 硬编码
  - 难以追踪合同版本演变
- **建议修复**: 
  1. 提取到类型化常量或 enum
  2. 集中管理合同版本
  3. 添加版本兼容性检查

#### 18. [序列化] [低严重] [Timestamp = string 丢失精度]
- **文件/路径**: `src/platform/contracts/types/domain/primitives.ts:10`
- **问题描述**: 
  - `type Timestamp = string` 无类型精度
  - ISO 8601 字符串无时区信息
- **建议修复**: 
  1. 使用 Date 对象或 branded string 类型
  2. 明确时区处理约定
  3. 文档化时间戳语义

#### 19. [源代码] [中严重] [29处非空断言 !.]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - 使用 `!.` 断言非空
  - 可能导致运行时错误
- **建议修复**: 
  1. 使用可选链 `?.` 或空值合并 `??`
  2. 添加适当的 null 检查
  3. 减少非空断言使用

#### 20. [API] [中严重] [API 路由 schema 版本硬编码]
- **文件/路径**: `src/platform/five-plane-interface/api/federation-routing-service.ts`
- **问题描述**: 
  - Zod schema 验证存在
  - 但 schema 版本未与 contract 版本同步
- **建议修复**: 
  1. 从 contract 版本派生 schema 版本
  2. 添加版本一致性检查
  3. 文档化版本关系

### 总结

本次补充Review（第十五轮 - 构建测试与类型系统Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. 无 TypeScript 增量编译缓存（每次完整重建9.6秒）
2. 76个 npm scripts 大部分重复构建
3. 168处 any 类型使用（绕过类型安全）
4. 38处 @ts-ignore 指令
5. ExecutionRecord 类型不匹配（14个文件）
6. 178处直接 process.env 变更无抽象
7. ScopedExternalAccessSandbox performHttpRequest 无超时
8. OIDC 服务 fetch 调用无超时
9. plugin-runtime-child 替换 globalThis.fetch 为 no-op

**中优先级**:
1. 30+处 as unknown as 双类型转换
2. rimraf extraneous 未声明
3. 21,998处 unlinkSync 无集中清理
4. 无统一 mocking 框架
5. Singleton 状态重置未标准化
6. 无显式 http.Agent 配置
7. 合同版本硬编码为字符串
8. Timestamp = string 丢失精度
9. 非空断言使用

**低优先级**:
1. 无分支覆盖率要求
2. API 路由 schema 版本未同步

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 72 | 87 | 39 | 198 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **109** | **182** | **85** | **376** |

### 优先修复建议

**立即处理 (本周内)**:
1. 添加 TypeScript 增量编译 (`incremental: true`)
2. 重构 npm scripts 避免重复构建
3. 减少 any 类型使用到 <50
4. 移除所有 @ts-ignore 指令
5. 修复 ExecutionRecord 类型不匹配
6. 添加 fetch 超时到 OIDC 和 sandbox

**短期内处理 (本月内)**:
1. 创建集中的测试清理 utility
2. 建立统一的 mocking 框架
3. 标准化 singleton 重置机制
4. 添加 http.Agent 连接池配置
5. 提取合同版本到类型化常量

**长期规划**:
1. 实现完整的类型安全（无 any）
2. 建立测试基础设施标准
3. 实现构建缓存优化
4. 完善网络层超时和重试
5. 建立覆盖率门禁标准

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十四轮 - 日志事件与工作流Review）

### 发现的新问题

#### 1. [可观测性] [高严重] [StructuredLogEntry 无 requestId 字段]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - StructuredLogEntry 接口无 requestId 字段
  - 只有 correlationId，它回退到 traceId
  - 无法独立追踪单个 HTTP 请求
- **建议修复**: 
  1. 在 StructuredLogEntry 添加 requestId 字段
  2. 在 HTTP 中间件中生成和注入 requestId
  3. 确保 requestId 流经所有日志调用

#### 2. [可观测性] [高严重] [StructuredLogger data 字段无自动清洗]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - 如果开发者传递 `{ password: "..." }` 给 logger.info，密码会被记录
  - 无字段级脱敏机制
  - 依赖开发者手动排除敏感字段
- **建议修复**: 
  1. 添加 OBSERVE_OUTPUT_BLACKLIST 类似的黑名单
  2. 实现 redact/mask/sanitize 工具函数
  3. 在日志传输前自动清洗敏感字段

#### 3. [可观测性] [中严重] [日志中敏感数据无 PII 处理]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - 无 PII 重写工具
  - 开发者必须手动避免记录敏感数据
  - 存在意外记录敏感信息的风险
- **建议修复**: 
  1. 添加 PII 检测和重写工具
  2. 在文档中明确记录敏感字段列表
  3. 添加测试验证敏感数据不被记录

#### 4. [可观测性] [中严重] [59处 console.* 直接调用]
- **文件/路径**: 多个源文件
- **问题描述**: 
  - 应该使用 StructuredLogger
  - console 调用可能不被日志聚合系统收集
  - 难以追踪和关联
- **建议修复**: 
  1. 替换所有 console.* 为 StructuredLogger
  2. 添加 ESLint 规则禁止 console.*
  3. 保留少量允许的 CLI 输出

#### 5. [可观测性] [中严重] [部分 logger.warn 调用缺少结构化数据]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:122`
- **问题描述**: 
  ```typescript
  logger.warn("WebSocket connection rejected: missing subprotocol token")
  ```
  - 只有消息，无 data 对象
  - 难以关联和查询
- **建议修复**: 
  1. 添加 `{ data: { ... } }` 对象
  2. 确保所有日志包含上下文数据
  3. 添加日志审查工具

#### 6. [源代码] [高严重] [Harness while(true) 循环无硬性迭代上限]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:1442`
- **问题描述**: 
  ```typescript
  while (true) {
    // ...
    // 依赖 budget gate 和 guardrail vibration 退出
    // 如果两者都失败，循环永久继续
  }
  ```
  - 依赖 budget gate 和 guardrail vibration 退出
  - 无硬性迭代次数限制
  - 可能导致无限循环
- **建议修复**: 
  1. 添加硬性 maxIterations 上限
  2. 在达到上限时强制退出
  3. 记录详细的迭代统计用于调试

#### 7. [源代码] [高严重] [Oapeflir while(true) 循环可能无限重规划]
- **文件/路径**: `src/platform/five-plane-orchestration/oapeflir/oapeflir-loop-service.ts:355`
- **问题描述**: 
  - 如果 loopReplanDecision.shouldReplan 永不变为 false
  - 重规划永远无法减少计划错误
  - 可能导致无限循环
- **建议修复**: 
  1. 添加重规划次数上限
  2. 在达到上限时强制接受当前结果
  3. 添加重规划质量衰减检测

#### 8. [源代码] [中严重] [Recovery 流程允许终态转换到 paused]
- **文件/路径**: `src/platform/five-plane-execution/runtime-state-machine.ts:105-108`
- **问题描述**: 
  - completed, failed, cancelled, aborted 可转换到 paused 进行恢复
  - 创建恢复路径可能重新进入执行
  - 需要验证此行为的正确性
- **建议修复**: 
  1. 审查 recovery 转换的合法性
  2. 确保不会导致状态不一致
  3. 添加转换前置条件验证

#### 9. [国际化] [高严重] [API 错误消息硬编码英文]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/api-error.ts`
- **问题描述**: 
  ```typescript
  return new ApiError(404, "api.task_not_found", "Task not found.");
  ```
  - 错误消息硬编码英文
  - 无后端 i18n 机制
  - 非英文客户端收到不本地化的错误
- **建议修复**: 
  1. 使用错误码而非硬编码消息
  2. 客户端根据错误码本地化
  3. 或在后端添加 i18n 支持

#### 10. [国际化] [中严重] [UI 硬编码字符串未翻译]
- **文件/路径**: `ui/packages/features/conversation/src/web/index.tsx`
- **问题描述**: 
  - "Build Plan", "Execute", "Messages" 等按钮标签硬编码
  - 不使用 i18n 系统
- **建议修复**: 
  1. 使用 `translateFeatureCopy` 替换硬编码
  2. 确保所有用户可见文本通过 i18n
  3. 添加硬编码字符串检测

#### 11. [国际化] [中严重] [Arabic 目录不完整]
- **文件/路径**: `ui/packages/shared/i18n/src/catalogs/ar-SA.ts`
- **问题描述**: 
  - ar-SA 只有约18条消息
  - en-US 和 zh-CN 有约117条
  - 缺失的 key 返回 key 本身
- **建议修复**: 
  1. 补充 Arabic 翻译
  2. 确保所有 key 都有翻译
  3. 添加翻译完整性测试

#### 12. [国际化] [低严重] [translateFeatureCopy 无回退默认值]
- **文件/路径**: `ui/packages/shared/i18n/`
- **问题描述**: 
  - 如果 feature ID 不在 catalog 中
  - 返回 undefined title/summary
  - 无合理回退
- **建议修复**: 
  1. 添加回退到 key 或默认字符串
  2. 或使用 feature ID 作为显示名
  3. 确保 UI 不显示 undefined

#### 13. [运维] [高严重] [健康检查无 readiness/liveness 区分]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts`
- **问题描述**: 
  - /healthz, /v1/healthz, /health 返回相同报告
  - 无 readiness probe（可处理流量？）
  - 无 liveness probe（系统存活？）
  - canAcceptTraffic() 未通过 HTTP 暴露
- **建议修复**: 
  1. 添加 /ready 和 /live 端点
  2. Readiness 检查依赖项就绪
  3. Liveness 检查进程健康

#### 14. [运维] [高严重] [shutdown 期间健康检查存在竞态]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts`
- **问题描述**: 
  - getHealthReportAsync() 异步检查 dbWritable
  - shutdown 期间数据库连接可能关闭
  - 健康检查仍在传入
- **建议修复**: 
  1. 在 shutdown 时返回 unhealthy
  2. 添加 shutdown 状态标志
  3. 停止接受新的健康检查请求

#### 15. [运维] [高严重] [LeaderElectionService 未与 GracefulShutdown 集成]
- **文件/路径**: `src/platform/five-plane-execution/ha/leader-election-service.ts`
- **问题描述**: 
  - LeaderElectionService.stop() 处理优雅退位
  - 但未在全局 graceful shutdown 中注册
  - 需显式注册为 shutdown handler
- **建议修复**: 
  1. 将 LeaderElectionService 添加到 shutdown handlers
  2. 确保领导者在 shutdown 时让位
  3. 验证关闭顺序正确

#### 16. [运维] [中严重] [addHandler() 无显式排序约定]
- **文件/路径**: `src/platform/five-plane-execution/startup/graceful-shutdown.ts`
- **问题描述**: 
  - 虽然 handlers 逆序执行
  - 但无显式接口声明 handler 依赖关系
  - 关键 handlers（如关闭数据库连接）应最后运行
- **建议修复**: 
  1. 添加优先级或阶段参数
  2. 文档化关键 handler 顺序要求
  3. 添加顺序验证测试

#### 17. [运维] [中严重] [unref'd timers 可能不触发]
- **文件/路径**: `src/platform/five-plane-execution/startup/process-error-handlers.ts:106`
- **问题描述**: 
  - `setTimeout(...).unref()` 不保持事件循环活跃
  - 如果主线程空闲，强制退出可能不触发
- **建议修复**: 
  1. 移除 .unref() 除非确实需要
  2. 或确保有其他方式保持事件循环
  3. 添加超时触发验证测试

#### 18. [运维] [中严重] [StartupConsistencyChecker 阻塞流量但不暴露状态]
- **文件/路径**: `src/platform/five-plane-startup-plan.ts`
- **问题描述**: 
  - canAcceptTraffic() 在 P0 问题存在时返回 false
  - 但 _trafficBlocked 是内部状态
  - 负载均衡器访问 /healthz 仍会得到 "ok"
- **建议修复**: 
  1. 在健康报告中暴露 trafficBlocked 状态
  2. 或添加单独的健康端点指示就绪状态
  3. 确保负载均衡器在阻塞时不路由流量

#### 19. [事件系统] [中严重] [TypedEventBus vs EventEmitter 混用]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - 项目同时使用 TypedEventBus 和原生 EventEmitter
  - TypedEventBus 应该是标准
  - 混用可能导致类型安全问题
- **建议修复**: 
  1. 创建迁移计划统一使用 TypedEventBus
  2. 添加 ESLint 规则禁止原生 EventEmitter
  3. 在 CI 中检测混用情况

#### 20. [源代码] [低严重] [无事件排序保证]
- **文件/路径**: `src/platform/five-plane-state-evidence/events/`
- **问题描述**: 
  - 无明确的事件排序保证文档
  - 事件处理可能乱序
  - 依赖方需要处理乱序
- **建议修复**: 
  1. 文档化事件排序语义
  2. 如果需要排序，实现序列号机制
  3. 在事件处理中考虑乱序情况

### 总结

本次补充Review（第十四轮 - 日志事件与工作流Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. StructuredLogEntry 无 requestId 字段
2. StructuredLogger data 字段无自动清洗（敏感数据风险）
3. Harness while(true) 循环无硬性迭代上限
4. Oapeflir while(true) 循环可能无限重规划
5. API 错误消息硬编码英文（无 i18n）
6. 健康检查无 readiness/liveness 区分
7. shutdown 期间健康检查存在竞态
8. LeaderElectionService 未与 GracefulShutdown 集成

**中优先级**:
1. 日志中敏感数据无 PII 处理
2. 59处 console.* 直接调用
3. 部分 logger.warn 调用缺少结构化数据
4. Recovery 流程允许终态转换到 paused
5. UI 硬编码字符串未翻译
6. Arabic 目录不完整
7. addHandler() 无显式排序约定
8. unref'd timers 可能不触发
9. StartupConsistencyChecker 阻塞流量但不暴露状态
10. TypedEventBus vs EventEmitter 混用

**低优先级**:
1. translateFeatureCopy 无回退默认值
2. 无事件排序保证

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 68 | 80 | 37 | 185 |
| 测试 | 8 | 14 | 5 | 27 |
| 配置 | 10 | 38 | 24 | 72 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **103** | **171** | **82** | **356** |

### 优先修复建议

**立即处理 (本周内)**:
1. 在 StructuredLogEntry 添加 requestId 字段
2. 实现日志敏感字段自动清洗
3. 为工作流循环添加硬性迭代上限
4. 添加 readiness/liveness 健康检查端点
5. 将 LeaderElectionService 集成到 GracefulShutdown
6. 实现 API 错误消息 i18n 支持

**短期内处理 (本月内)**:
1. 替换所有 console.* 为 StructuredLogger
2. 补充 Arabic i18n 目录
3. 修复 UI 硬编码字符串
4. 统一 TypedEventBus 替代 EventEmitter
5. 解决 shutdown 竞态条件
6. 添加事件排序保证文档

**长期规划**:
1. 建立完整的 PII 处理框架
2. 实现日志完整性验证
3. 建立工作流安全循环检测
4. 完善健康检查和就绪探测
5. 建立国际化完整测试

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十五轮 - 构建测试与类型系统Review）

### 发现的新问题

#### 1. [构建] [高严重] [无 TypeScript 增量编译缓存]
- **文件/路径**: `tsconfig.json`
- **问题描述**: 
  - 无 `incremental: true` 配置
  - 无 `tsbuildinfo` 文件
  - 每次构建是完整重建（9.6秒）
  - TypeScript 无法使用增量编译
- **建议修复**: 
  1. 在 tsconfig.json 添加 `"incremental": true`
  2. 添加 `"tsbuildinfo": ".tsbuildinfo"` 到 .gitignore
  3. 实现项目引用以支持并行构建

#### 2. [构建] [高严重] [76个 npm scripts 大部分重复构建]
- **文件/路径**: `package.json`
- **问题描述**: 
  - 40+ CLI scripts 都运行 `npm run build && node ...`
  - 每个脚本前都执行完整重建
  - 无法直接运行预构建的 CLI
  - 20+ stable:* 脚本遵循相同模式
- **建议修复**: 
  1. 创建 `build:cli` 脚本构建一次
  2. 让 CLI 脚本使用预构建的 dist 文件
  3. 添加 npm run dev 或类似的无需重建的执行方式

#### 3. [源代码] [高严重] [168处 any 类型使用]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - `stable-runner-factory.ts`: `opts: any` -> `any`
  - `billing-service-async.ts`: `input: any` 方法签名
  - 重试/超时工具使用 `(...args: any[]) => Promise<any>`
  - 绕过类型安全
- **建议修复**: 
  1. 使用泛型替代 any
  2. 替换为具体的输入类型
  3. 添加 ESLint 规则禁止 any

#### 4. [源代码] [高严重] [38处 @ts-ignore 指令]
- **文件/路径**: 多个文件
- **问题描述**: 
  - 14个 stability 相关文件有 `@ts-ignore ExecutionRecord type mismatch`
  - 5个 harness-sdk 文件有 `Partial<HarnessRun> doesn't have all required properties`
  - 2个文件有 `exactOptionalPropertyTypes` 问题
- **建议修复**: 
  1. 修复 ExecutionRecord 类型不匹配
  2. 完成 HarnessRun 类型的必需属性
  3. 移除 @ts-ignore 使用

#### 5. [源代码] [高严重] [ExecutionRecord 类型不匹配]
- **文件/路径**: `src/platform/stability/stable-evidence-bundle-support.ts:783` 等14个文件
- **问题描述**: 
  - 插入 execution record 时 TypeScript 拒绝类型
  - `ExecutionRecord` 接口要求新字段但 insert 对象缺失
  - 或 store 的 insert 方法接受不同类型
- **建议修复**: 
  1. 对齐 ExecutionRecord 接口和 store 类型
  2. 添加缺失字段到 insert 对象
  3. 或调整 store 的 insert 方法类型签名

#### 6. [源代码] [中严重] [30+处 as unknown as 双类型转换]
- **文件/路径**: `division-loader.ts`, `domain-baseline-catalog.ts`, `harness-sdk`
- **问题描述**: 
  - 完全绕过类型安全
  - 通常是类型架构问题的信号
- **建议修复**: 
  1. 修复上游类型问题
  2. 使用更具体的类型断言
  3. 重构类型层次结构

#### 7. [构建] [中严重] [rimraf  extraneous - 未在 package.json 声明]
- **文件/路径**: `node_modules/rimraf`
- **问题描述**: 
  - rimraf@6.1.3 存在于 node_modules 但未在 package.json 声明
  - 可能导致依赖问题
- **建议修复**: 
  1. 添加到 dependencies 或 devDependencies
  2. 或移除并使用其他清理方式

#### 8. [测试] [高严重] [178处直接 process.env 变更无抽象]
- **文件/路径**: `tests/unit/domains/registry/plugin-spi-registry.test.ts:640-687`
- **问题描述**: 
  - 直接读取/写入 process.env
  - 在 beforeEach/afterEach 中手动保存/恢复
  - 无 helper 抽象
- **建议修复**: 
  1. 创建 env helper 函数
  2. 统一保存和恢复模式
  3. 添加测试隔离验证

#### 9. [测试] [高严重] [21,998处 unlinkSync 调用无集中清理]
- **文件/路径**: 多个测试文件
- **问题描述**: 
  - 每个测试重新实现清理逻辑
  - 无集中清理 utility
  - 可能导致 EBUSY (Windows) 或并发访问问题
- **建议修复**: 
  1. 创建集中的测试清理 utility
  2. 使用 afterEach 确保清理
  3. 考虑使用临时目录

#### 10. [测试] [中严重] [无统一 mocking 框架]
- **文件/路径**: `tests/` 多个目录
- **问题描述**: 
  - 10,579 处 vi.fn, jest.mock, sinon 等调用
  - 无统一的 mocking 框架可见
  - 模式不一致
- **建议修复**: 
  1. 建立统一的 mock factory
  2. 标准化 mock 模式
  3. 添加 mocking 最佳实践文档

#### 11. [测试] [中严重] [Singleton 状态重置未标准化]
- **文件/路径**: `tests/unit/platform/five-plane-execution/dispatcher.test.ts`
- **问题描述**: 
  - 测试通过特定函数重置 singletons
  - 每个测试重复自己的 reset 逻辑
  - 无统一的 resetAllSingletons()
- **建议修复**: 
  1. 创建统一的 singleton reset 机制
  2. 在 afterEach 中自动调用
  3. 文档化 singleton 重置要求

#### 12. [测试] [低严重] [无分支覆盖率要求]
- **文件/路径**: `.c8rc.json`
- **问题描述**: 
  - `"100": false` - 无覆盖率要求
  - 只测量源代码，不测量测试文件
- **建议修复**: 
  1. 添加分支覆盖率阈值
  2. 在 CI 中强制执行覆盖率 gate
  3. 关注关键代码路径

#### 13. [网络] [高严重] [ScopedExternalAccessSandbox performHttpRequest 无超时]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/scoped-external-access-sandbox.ts:289`
- **问题描述**: 
  - fetch 调用无 timeout 参数
  - 挂起的请求可能永久阻塞
- **建议修复**: 
  1. 添加 AbortController timeout
  2. 使用标准的超时模式
  3. 记录超时配置

#### 14. [网络] [高严重] [OIDC 服务 fetch 调用无超时]
- **文件/路径**: `src/org-governance/sso-scim/oidc/oidc-service.ts:565,272`
- **问题描述**: 
  - OIDC token 交换无 retry logic
  - 无 timeout/abort signal
  - 认证流程可能挂起
- **建议修复**: 
  1. 添加 fetch timeout
  2. 添加 retry logic
  3. 使用标准的超时配置

#### 15. [网络] [高严重] [plugin-runtime-child 替换 globalThis.fetch 为 no-op]
- **文件/路径**: `src/domains/registry/plugin-runtime-child.ts:112`
- **问题描述**: 
  ```typescript
  globalThis.fetch = (async () => {}) as unknown as typeof globalThis.fetch;
  ```
  - 插件上下文中完全替换 fetch 为空函数
  - 插件的所有出站 HTTP 请求都会失败
  - 看起来是有意为之但可能造成问题
- **建议修复**: 
  1. 确认这是有意行为
  2. 文档化 fetch 替换行为
  3. 确保不会意外影响其他代码

#### 16. [网络] [中严重] [无显式 http.Agent 配置]
- **文件/路径**: 全局
- **问题描述**: 
  - 无显式的 http.Agent/https.Agent 配置
  - 依赖 Node.js 默认连接池
  - 无 maxSockets 或 maxConnections 配置
  - 高吞吐场景可能出问题
- **建议修复**: 
  1. 添加 http Agent 配置
  2. 文档化连接池大小
  3. 监控连接使用情况

#### 17. [序列化] [中严重] [合同版本硬编码为字符串字面量]
- **文件/路径**: `src/platform/contracts/inter-plane-contract-gateway.ts:184`
- **问题描述**: 
  - `schemaVersion: "v4.3"` 硬编码
  - 难以追踪合同版本演变
- **建议修复**: 
  1. 提取到类型化常量或 enum
  2. 集中管理合同版本
  3. 添加版本兼容性检查

#### 18. [序列化] [低严重] [Timestamp = string 丢失精度]
- **文件/路径**: `src/platform/contracts/types/domain/primitives.ts:10`
- **问题描述**: 
  - `type Timestamp = string` 无类型精度
  - ISO 8601 字符串无时区信息
- **建议修复**: 
  1. 使用 Date 对象或 branded string 类型
  2. 明确时区处理约定
  3. 文档化时间戳语义

#### 19. [源代码] [中严重] [29处非空断言 !.]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - 使用 `!.` 断言非空
  - 可能导致运行时错误
- **建议修复**: 
  1. 使用可选链 `?.` 或空值合并 `??`
  2. 添加适当的 null 检查
  3. 减少非空断言使用

#### 20. [API] [中严重] [API 路由 schema 版本硬编码]
- **文件/路径**: `src/platform/five-plane-interface/api/federation-routing-service.ts`
- **问题描述**: 
  - Zod schema 验证存在
  - 但 schema 版本未与 contract 版本同步
- **建议修复**: 
  1. 从 contract 版本派生 schema 版本
  2. 添加版本一致性检查
  3. 文档化版本关系

### 总结

本次补充Review（第十五轮 - 构建测试与类型系统Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. 无 TypeScript 增量编译缓存（每次完整重建9.6秒）
2. 76个 npm scripts 大部分重复构建
3. 168处 any 类型使用（绕过类型安全）
4. 38处 @ts-ignore 指令
5. ExecutionRecord 类型不匹配（14个文件）
6. 178处直接 process.env 变更无抽象
7. ScopedExternalAccessSandbox performHttpRequest 无超时
8. OIDC 服务 fetch 调用无超时
9. plugin-runtime-child 替换 globalThis.fetch 为 no-op

**中优先级**:
1. 30+处 as unknown as 双类型转换
2. rimraf extraneous 未声明
3. 21,998处 unlinkSync 无集中清理
4. 无统一 mocking 框架
5. Singleton 状态重置未标准化
6. 无显式 http.Agent 配置
7. 合同版本硬编码为字符串
8. Timestamp = string 丢失精度
9. 非空断言使用

**低优先级**:
1. 无分支覆盖率要求
2. API 路由 schema 版本未同步

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 72 | 87 | 39 | 198 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **109** | **182** | **85** | **376** |

### 优先修复建议

**立即处理 (本周内)**:
1. 添加 TypeScript 增量编译 (`incremental: true`)
2. 重构 npm scripts 避免重复构建
3. 减少 any 类型使用到 <50
4. 移除所有 @ts-ignore 指令
5. 修复 ExecutionRecord 类型不匹配
6. 添加 fetch 超时到 OIDC 和 sandbox

**短期内处理 (本月内)**:
1. 创建集中的测试清理 utility
2. 建立统一的 mocking 框架
3. 标准化 singleton 重置机制
4. 添加 http.Agent 连接池配置
5. 提取合同版本到类型化常量

**长期规划**:
1. 实现完整的类型安全（无 any）
2. 建立测试基础设施标准
3. 实现构建缓存优化
4. 完善网络层超时和重试
5. 建立覆盖率门禁标准

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十三轮 - 缓存会话与实时通信Review）

### 发现的新问题

#### 1. [安全] [严重] [CORS allowedMethods 缺少 PUT/PATCH/DELETE]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/response-hardening.ts:12-14`
- **问题描述**: 
  ```typescript
  export const DEFAULT_CORS_CONFIG: CorsConfig = {
    allowedMethods: ["GET", "POST", "OPTIONS"],  // 缺少 PUT, PATCH, DELETE
  ```
  - 浏览器 CORS preflight 会拒绝 PUT/PATCH/DELETE 请求
  - 但路由中有 PATCH /v1/tasks/:id, DELETE endpoints
- **建议修复**: 
  1. 添加 PUT, PATCH, DELETE 到 allowedMethods
  2. 确保 CORS 配置与应用路由方法一致
  3. 添加测试验证所有 HTTP 方法的 CORS preflight

#### 2. [安全] [严重] [Mission Routes 错误响应包装不当]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/mission-routes.ts:101,118-124`
- **问题描述**: 
  ```typescript
  return buildJsonResponse(ctx.requestId, 404, { error: { code: "MISSION_NOT_FOUND" } });
  ```
  - 使用 buildJsonResponse（成功包装）返回错误
  - 错误被包装在成功信封 `{requestId, data: {error: {...}}}` 中
  - 违反 REST 规范
- **建议修复**: 
  1. 使用 buildJsonErrorResponse 处理错误
  2. 确保所有错误路径使用正确的错误信封
  3. 添加 API 一致性测试

#### 3. [源代码] [高严重] [WebSocketBridge 无最大连接数限制]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:103-107`
- **问题描述**: 
  ```typescript
  this.wss = new WebSocketServer({
    server,
    path: WS_PATH,
    maxPayload: options.maxPayloadBytes ?? DEFAULT_MAX_PAYLOAD_BYTES,
    // 无 maxConnections 限制！
  });
  ```
  - 与 dashboard-websocket-server.ts 的 maxClients: 1000 不同
  - 恶意客户端可耗尽服务器资源
- **建议修复**: 
  1. 添加 maxConnections 限制
  2. 实现连接限制拒绝策略
  3. 添加连接计数指标

#### 4. [源代码] [高严重] [pendingAcks 在断开连接时未清理]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:268-282`
- **问题描述**: 
  - handleDisconnection 清理 subscribedTasks, taskSubscribers, slowConsumers
  - 但 pendingAcks Map 未清理
  - 断开的客户端未确认的消息永久留在内存
- **建议修复**: 
  1. 在 handleDisconnection 中删除所有 pendingAcks 条目
  2. 考虑添加超时机制自动清理未确认消息
  3. 记录清理操作的审计日志

#### 5. [安全] [高严重] [硬编码回退密钥]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/audit-event-integrity.ts:41`
- **问题描述**: 
  ```typescript
  const AUDIT_INTEGRITY_HMAC_KEY = process.env["AA_AUDIT_INTEGRITY_HMAC_KEY"] ?? "audit-integrity-secret-key-32-bytes!";
  ```
  - 占位符密钥用作回退
  - 如果环境变量未设置，使用不安全密钥
- **建议修复**: 
  1. 移除回退值，要求环境变量必须设置
  2. 在启动时验证密钥存在且足够强
  3. 如果密钥缺失导致启动失败

#### 6. [配置] [高严重] [config/runtime/test.json 缺少超时配置]
- **文件/路径**: `config/runtime/test.json`
- **问题描述**: 
  - 只有3个字段，缺少 maxConcurrentTasks, defaultTaskTimeoutMs, defaultStepTimeoutMs
  - 回退到默认值可能不适合测试环境
- **建议修复**: 
  1. 添加完整的 timeout/tuning 配置
  2. 确保测试环境有合理的默认值
  3. 文档化必需的配置字段

#### 7. [配置] [高严重] [prod.json 限制过严 - maxConcurrentTasks=1]
- **文件/路径**: `config/runtime/prod.json`
- **问题描述**: 
  - maxConcurrentTasks: 1（vs dev:8, staging:4, pre-prod:6）
  - defaultTaskTimeoutMs: 120000（vs dev:600000）
  - defaultStepTimeoutMs: 60000（vs dev:180000）
  - 生产环境严重受限，可能影响吞吐量
- **建议修复**: 
  1. 评估并调整生产限制
  2. 与业务需求匹配
  3. 添加生产容量测试

#### 8. [源代码] [高严重] [多个服务存在无界 Map 无驱逐策略]
- **文件/路径**: 多个域服务
- **问题描述**: 
  - `domain-knowledge-schema-service.ts:73-75`: schemas, sourceContent, sourceTimestamps 无限制
  - `domain-eval-framework-service.ts:91-95`: frameworks, qualityAxes 等无限制
  - `domain-recipe-service.ts:67-68`: recipes, versions 无限制
  - `domain-risk-profile-service.ts:51`: profiles 无限制
  - Session Maps (session-management.ts:83-89) 无自动清理
- **建议修复**: 
  1. 为所有 Map 实现 LRU 或 TTL 驱逐策略
  2. 添加后台清理任务
  3. 添加大小监控和告警

#### 9. [源代码] [中严重] [taskEventHistory Map 永不清理]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:91,548-555`
- **问题描述**: 
  - taskEventHistory 限制单任务历史为 200 条
  - 但 Map 本身从不清理
  - 取消订阅的任务历史永久保留
- **建议修复**: 
  1. 实现后台清理无订阅者任务的历史
  2. 添加任务订阅者监控
  3. 考虑使用 WeakMap 替代

#### 10. [配置] [中严重] [安全配置 drift - remoteWorkerRegistration 缺失]
- **文件/路径**: `config/security/dev.json`, `staging.json`, `pre-prod.json`
- **问题描述**: 
  - default.json 有完整的 remoteWorkerRegistration 配置
  - dev/staging/pre-prod 只有 approvalMode
  - prod 有 approvalMode 但无 remoteWorkerRegistration
  - 安全配置不一致
- **建议修复**: 
  1. 统一所有环境的 remoteWorkerRegistration
  2. 添加安全配置验证
  3. 确保最低安全基线

#### 11. [API] [中严重] [OpenAPI 端点响应格式不一致]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/utils.ts:207-215`
- **问题描述**: 
  - `/v1/openapi.json` 使用 buildJsonDocumentResponse（无包装）
  - 其他端点使用 buildJsonResponse（{requestId, data} 包装）
  - 客户端体验不一致
- **建议修复**: 
  1. 统一响应信封格式
  2. 文档化响应格式规范
  3. 添加响应格式验证测试

#### 12. [源代码] [中严重] [Redis 队列缺少 idempotency 索引支持]
- **文件/路径**: `src/platform/five-plane-execution/queue/redis-queue-adapter.ts`
- **问题描述**: 
  - SQLite 有部分唯一索引支持 idempotency
  - Redis 实现使用哈希索引但实现不完整
  - 可能导致重复消息
- **建议修复**: 
  1. 完善 Redis idempotency 实现
  2. 添加唯一索引验证
  3. 确保与 SQLite 行为一致

#### 13. [源代码] [中严重] [请求去重中间件使用内存存储]
- **文件/路径**: `src/platform/five-plane-interface/api/middleware/request-deduplication.ts`
- **问题描述**: 
  - 纯内存滑动窗口（Map<DeduplicationKey, DeduplicationEntry[]>）
  - 无外部存储
  - 多实例部署不共享
  - 重启后去重状态丢失
- **建议修复**: 
  1. 使用 Redis 替代内存存储
  2. 支持分布式去重
  3. 持久化去重状态

#### 14. [源代码] [中严重] [Cache 无 stampede 保护]
- **文件/路径**: 多个 cache 实现
- **问题描述**: 
  - MemoryCacheStore, ExperienceCacheService 无锁
  - 缓存未命中时可发生 thundering herd
  - 高并发下可能导致数据库过载
- **建议修复**: 
  1. 实现 single-flight 模式
  2. 添加请求排队机制
  3. 使用分布式锁保护缓存更新

#### 15. [源代码] [中严重] [EvidenceService eviction 仅在插入时触发]
- **文件/路径**: `src/platform/five-plane-state-evidence/memory/evidence-service.ts:202`
- **问题描述**: 
  - 清理仅在 record() 调用时触发
  - 空闲时无后台清理
  - 可能导致内存持续增长
- **建议修复**: 
  1. 添加定期后台清理任务
  2. 使用独立的清理线程
  3. 添加内存使用监控

#### 16. [源代码] [低严重] [无连接指标暴露]
- **文件/路径**: WebSocket 服务器
- **问题描述**: 
  - getClientCount() 存在但未通过 HTTP 端点暴露
  - 无 pendingAcks 队列深度指标
  - 无连接建立告警
- **建议修复**: 
  1. 通过 metrics 端点暴露连接指标
  2. 添加 pendingAcks 队列监控
  3. 添加连接数异常告警

#### 17. [源代码] [低严重] [无空闲客户端超时]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts`
- **问题描述**: 
  - 仅在心跳 Sweep 时检查 isAlive
  - 无 per-client 独立于心跳的空闲超时
  - 认证后从不发送消息的客户端只能通过心跳失败检测
- **建议修复**: 
  1. 添加 per-client idle timeout
  2. 独立于心跳间隔
  3. 配置可调整

#### 18. [源代码] [低严重] [订阅限制是 per-client 而非全局]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:244`
- **问题描述**: 
  - MAX_SUBSCRIPTIONS_PER_CLIENT = 100
  - 恶意客户端可订阅 100 个任务
  - 无全局任务订阅者限制
- **建议修复**: 
  1. 添加全局任务订阅者限制
  2. 实现 per-task 订阅者上限
  3. 添加反滥用检测

#### 19. [配置] [低严重] [config/runtime 无版本验证机制]
- **文件/路径**: `config/runtime/*.json`
- **问题描述**: 
  - 配置有 "version": "v4.3"
  - 但无 schema 版本验证
  - 加载时可能接受不兼容配置
- **建议修复**: 
  1. 添加 JSON Schema 验证
  2. 实现版本兼容性检查
  3. 启动时验证配置完整性

#### 20. [源代码] [低严重] [ImprovementCandidateRegistry splice 是 O(n)]
- **文件/路径**: `src/platform/five-plane-orchestration/improve/improvement-candidate-registry.ts:243`
- **问题描述**: 
  - accessOrder 数组的 splice 操作是 O(n)
  - 高频访问时可能性能问题
- **建议修复**: 
  1. 使用 LinkedList 替代数组
  2. 或使用 Map 维护访问顺序
  3. 性能测试验证

### 总结

本次补充Review（第十三轮 - 缓存会话与实时通信Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. CORS allowedMethods 缺少 PUT/PATCH/DELETE（浏览器请求会失败）
2. Mission Routes 错误响应包装不当
3. WebSocketBridge 无最大连接数限制（DoS风险）
4. pendingAcks 断开连接时未清理（内存泄漏）
5. 硬编码回退密钥（安全漏洞）
6. config/runtime/test.json 缺少超时配置
7. prod.json maxConcurrentTasks=1 限制过严
8. 多个服务存在无界 Map（内存泄漏风险）

**中优先级**:
1. taskEventHistory Map 永不清理
2. 安全配置 drift（remoteWorkerRegistration 缺失）
3. OpenAPI 端点响应格式不一致
4. Redis 队列 idempotency 实现不完整
5. 请求去重中间件使用内存存储（多实例不共享）
6. Cache 无 stampede 保护
7. EvidenceService eviction 仅在插入时触发

**低优先级**:
1. 无连接指标暴露
2. 无空闲客户端超时
3. 订阅限制是 per-client 而非全局
4. config/runtime 无版本验证机制
5. ImprovementCandidateRegistry splice O(n)

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 64 | 74 | 35 | 173 |
| 测试 | 8 | 14 | 5 | 27 |
| 配置 | 10 | 38 | 24 | 72 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 7 | 3 | 12 |
| UI | 0 | 3 | 3 | 6 |
| 部署 | 1 | 10 | 6 | 17 |
| **合计** | **99** | **161** | **79** | **339** |

### 优先修复建议

**立即处理 (本周内)**:
1. 修复 CORS allowedMethods 添加 PUT/PATCH/DELETE
2. 修复 Mission Routes 错误响应格式
3. 为 WebSocketBridge 添加最大连接数限制
4. 清理 pendingAcks 和 taskEventHistory
5. 移除硬编码回退密钥
6. 修复 config/runtime/test.json 配置
7. 评估并调整 prod 并发限制

**短期内处理 (本月内)**:
1. 统一安全配置（remoteWorkerRegistration）
2. 实现无界 Map 的 LRU/TTL 驱逐
3. 统一 API 响应信封格式
4. 完善 Redis idempotency 实现
5. 添加分布式去重中间件
6. 实现 cache stampede 保护

**长期规划**:
1. 建立完整的连接管理和监控
2. 实现后台清理任务框架
3. 添加配置 schema 验证
4. 优化高频繁操作的数据结构
5. 建立内存使用基线和告警

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十五轮 - 构建测试与类型系统Review）

### 发现的新问题

#### 1. [构建] [高严重] [无 TypeScript 增量编译缓存]
- **文件/路径**: `tsconfig.json`
- **问题描述**: 
  - 无 `incremental: true` 配置
  - 无 `tsbuildinfo` 文件
  - 每次构建是完整重建（9.6秒）
  - TypeScript 无法使用增量编译
- **建议修复**: 
  1. 在 tsconfig.json 添加 `"incremental": true`
  2. 添加 `"tsbuildinfo": ".tsbuildinfo"` 到 .gitignore
  3. 实现项目引用以支持并行构建

#### 2. [构建] [高严重] [76个 npm scripts 大部分重复构建]
- **文件/路径**: `package.json`
- **问题描述**: 
  - 40+ CLI scripts 都运行 `npm run build && node ...`
  - 每个脚本前都执行完整重建
  - 无法直接运行预构建的 CLI
  - 20+ stable:* 脚本遵循相同模式
- **建议修复**: 
  1. 创建 `build:cli` 脚本构建一次
  2. 让 CLI 脚本使用预构建的 dist 文件
  3. 添加 npm run dev 或类似的无需重建的执行方式

#### 3. [源代码] [高严重] [168处 any 类型使用]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - `stable-runner-factory.ts`: `opts: any` -> `any`
  - `billing-service-async.ts`: `input: any` 方法签名
  - 重试/超时工具使用 `(...args: any[]) => Promise<any>`
  - 绕过类型安全
- **建议修复**: 
  1. 使用泛型替代 any
  2. 替换为具体的输入类型
  3. 添加 ESLint 规则禁止 any

#### 4. [源代码] [高严重] [38处 @ts-ignore 指令]
- **文件/路径**: 多个文件
- **问题描述**: 
  - 14个 stability 相关文件有 `@ts-ignore ExecutionRecord type mismatch`
  - 5个 harness-sdk 文件有 `Partial<HarnessRun> doesn't have all required properties`
  - 2个文件有 `exactOptionalPropertyTypes` 问题
- **建议修复**: 
  1. 修复 ExecutionRecord 类型不匹配
  2. 完成 HarnessRun 类型的必需属性
  3. 移除 @ts-ignore 使用

#### 5. [源代码] [高严重] [ExecutionRecord 类型不匹配]
- **文件/路径**: `src/platform/stability/stable-evidence-bundle-support.ts:783` 等14个文件
- **问题描述**: 
  - 插入 execution record 时 TypeScript 拒绝类型
  - `ExecutionRecord` 接口要求新字段但 insert 对象缺失
  - 或 store 的 insert 方法接受不同类型
- **建议修复**: 
  1. 对齐 ExecutionRecord 接口和 store 类型
  2. 添加缺失字段到 insert 对象
  3. 或调整 store 的 insert 方法类型签名

#### 6. [源代码] [中严重] [30+处 as unknown as 双类型转换]
- **文件/路径**: `division-loader.ts`, `domain-baseline-catalog.ts`, `harness-sdk`
- **问题描述**: 
  - 完全绕过类型安全
  - 通常是类型架构问题的信号
- **建议修复**: 
  1. 修复上游类型问题
  2. 使用更具体的类型断言
  3. 重构类型层次结构

#### 7. [构建] [中严重] [rimraf  extraneous - 未在 package.json 声明]
- **文件/路径**: `node_modules/rimraf`
- **问题描述**: 
  - rimraf@6.1.3 存在于 node_modules 但未在 package.json 声明
  - 可能导致依赖问题
- **建议修复**: 
  1. 添加到 dependencies 或 devDependencies
  2. 或移除并使用其他清理方式

#### 8. [测试] [高严重] [178处直接 process.env 变更无抽象]
- **文件/路径**: `tests/unit/domains/registry/plugin-spi-registry.test.ts:640-687`
- **问题描述**: 
  - 直接读取/写入 process.env
  - 在 beforeEach/afterEach 中手动保存/恢复
  - 无 helper 抽象
- **建议修复**: 
  1. 创建 env helper 函数
  2. 统一保存和恢复模式
  3. 添加测试隔离验证

#### 9. [测试] [高严重] [21,998处 unlinkSync 调用无集中清理]
- **文件/路径**: 多个测试文件
- **问题描述**: 
  - 每个测试重新实现清理逻辑
  - 无集中清理 utility
  - 可能导致 EBUSY (Windows) 或并发访问问题
- **建议修复**: 
  1. 创建集中的测试清理 utility
  2. 使用 afterEach 确保清理
  3. 考虑使用临时目录

#### 10. [测试] [中严重] [无统一 mocking 框架]
- **文件/路径**: `tests/` 多个目录
- **问题描述**: 
  - 10,579 处 vi.fn, jest.mock, sinon 等调用
  - 无统一的 mocking 框架可见
  - 模式不一致
- **建议修复**: 
  1. 建立统一的 mock factory
  2. 标准化 mock 模式
  3. 添加 mocking 最佳实践文档

#### 11. [测试] [中严重] [Singleton 状态重置未标准化]
- **文件/路径**: `tests/unit/platform/five-plane-execution/dispatcher.test.ts`
- **问题描述**: 
  - 测试通过特定函数重置 singletons
  - 每个测试重复自己的 reset 逻辑
  - 无统一的 resetAllSingletons()
- **建议修复**: 
  1. 创建统一的 singleton reset 机制
  2. 在 afterEach 中自动调用
  3. 文档化 singleton 重置要求

#### 12. [测试] [低严重] [无分支覆盖率要求]
- **文件/路径**: `.c8rc.json`
- **问题描述**: 
  - `"100": false` - 无覆盖率要求
  - 只测量源代码，不测量测试文件
- **建议修复**: 
  1. 添加分支覆盖率阈值
  2. 在 CI 中强制执行覆盖率 gate
  3. 关注关键代码路径

#### 13. [网络] [高严重] [ScopedExternalAccessSandbox performHttpRequest 无超时]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/scoped-external-access-sandbox.ts:289`
- **问题描述**: 
  - fetch 调用无 timeout 参数
  - 挂起的请求可能永久阻塞
- **建议修复**: 
  1. 添加 AbortController timeout
  2. 使用标准的超时模式
  3. 记录超时配置

#### 14. [网络] [高严重] [OIDC 服务 fetch 调用无超时]
- **文件/路径**: `src/org-governance/sso-scim/oidc/oidc-service.ts:565,272`
- **问题描述**: 
  - OIDC token 交换无 retry logic
  - 无 timeout/abort signal
  - 认证流程可能挂起
- **建议修复**: 
  1. 添加 fetch timeout
  2. 添加 retry logic
  3. 使用标准的超时配置

#### 15. [网络] [高严重] [plugin-runtime-child 替换 globalThis.fetch 为 no-op]
- **文件/路径**: `src/domains/registry/plugin-runtime-child.ts:112`
- **问题描述**: 
  ```typescript
  globalThis.fetch = (async () => {}) as unknown as typeof globalThis.fetch;
  ```
  - 插件上下文中完全替换 fetch 为空函数
  - 插件的所有出站 HTTP 请求都会失败
  - 看起来是有意为之但可能造成问题
- **建议修复**: 
  1. 确认这是有意行为
  2. 文档化 fetch 替换行为
  3. 确保不会意外影响其他代码

#### 16. [网络] [中严重] [无显式 http.Agent 配置]
- **文件/路径**: 全局
- **问题描述**: 
  - 无显式的 http.Agent/https.Agent 配置
  - 依赖 Node.js 默认连接池
  - 无 maxSockets 或 maxConnections 配置
  - 高吞吐场景可能出问题
- **建议修复**: 
  1. 添加 http Agent 配置
  2. 文档化连接池大小
  3. 监控连接使用情况

#### 17. [序列化] [中严重] [合同版本硬编码为字符串字面量]
- **文件/路径**: `src/platform/contracts/inter-plane-contract-gateway.ts:184`
- **问题描述**: 
  - `schemaVersion: "v4.3"` 硬编码
  - 难以追踪合同版本演变
- **建议修复**: 
  1. 提取到类型化常量或 enum
  2. 集中管理合同版本
  3. 添加版本兼容性检查

#### 18. [序列化] [低严重] [Timestamp = string 丢失精度]
- **文件/路径**: `src/platform/contracts/types/domain/primitives.ts:10`
- **问题描述**: 
  - `type Timestamp = string` 无类型精度
  - ISO 8601 字符串无时区信息
- **建议修复**: 
  1. 使用 Date 对象或 branded string 类型
  2. 明确时区处理约定
  3. 文档化时间戳语义

#### 19. [源代码] [中严重] [29处非空断言 !.]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - 使用 `!.` 断言非空
  - 可能导致运行时错误
- **建议修复**: 
  1. 使用可选链 `?.` 或空值合并 `??`
  2. 添加适当的 null 检查
  3. 减少非空断言使用

#### 20. [API] [中严重] [API 路由 schema 版本硬编码]
- **文件/路径**: `src/platform/five-plane-interface/api/federation-routing-service.ts`
- **问题描述**: 
  - Zod schema 验证存在
  - 但 schema 版本未与 contract 版本同步
- **建议修复**: 
  1. 从 contract 版本派生 schema 版本
  2. 添加版本一致性检查
  3. 文档化版本关系

### 总结

本次补充Review（第十五轮 - 构建测试与类型系统Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. 无 TypeScript 增量编译缓存（每次完整重建9.6秒）
2. 76个 npm scripts 大部分重复构建
3. 168处 any 类型使用（绕过类型安全）
4. 38处 @ts-ignore 指令
5. ExecutionRecord 类型不匹配（14个文件）
6. 178处直接 process.env 变更无抽象
7. ScopedExternalAccessSandbox performHttpRequest 无超时
8. OIDC 服务 fetch 调用无超时
9. plugin-runtime-child 替换 globalThis.fetch 为 no-op

**中优先级**:
1. 30+处 as unknown as 双类型转换
2. rimraf extraneous 未声明
3. 21,998处 unlinkSync 无集中清理
4. 无统一 mocking 框架
5. Singleton 状态重置未标准化
6. 无显式 http.Agent 配置
7. 合同版本硬编码为字符串
8. Timestamp = string 丢失精度
9. 非空断言使用

**低优先级**:
1. 无分支覆盖率要求
2. API 路由 schema 版本未同步

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 72 | 87 | 39 | 198 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **109** | **182** | **85** | **376** |

### 优先修复建议

**立即处理 (本周内)**:
1. 添加 TypeScript 增量编译 (`incremental: true`)
2. 重构 npm scripts 避免重复构建
3. 减少 any 类型使用到 <50
4. 移除所有 @ts-ignore 指令
5. 修复 ExecutionRecord 类型不匹配
6. 添加 fetch 超时到 OIDC 和 sandbox

**短期内处理 (本月内)**:
1. 创建集中的测试清理 utility
2. 建立统一的 mocking 框架
3. 标准化 singleton 重置机制
4. 添加 http.Agent 连接池配置
5. 提取合同版本到类型化常量

**长期规划**:
1. 实现完整的类型安全（无 any）
2. 建立测试基础设施标准
3. 实现构建缓存优化
4. 完善网络层超时和重试
5. 建立覆盖率门禁标准

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十四轮 - 日志事件与工作流Review）

### 发现的新问题

#### 1. [可观测性] [高严重] [StructuredLogEntry 无 requestId 字段]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - StructuredLogEntry 接口无 requestId 字段
  - 只有 correlationId，它回退到 traceId
  - 无法独立追踪单个 HTTP 请求
- **建议修复**: 
  1. 在 StructuredLogEntry 添加 requestId 字段
  2. 在 HTTP 中间件中生成和注入 requestId
  3. 确保 requestId 流经所有日志调用

#### 2. [可观测性] [高严重] [StructuredLogger data 字段无自动清洗]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - 如果开发者传递 `{ password: "..." }` 给 logger.info，密码会被记录
  - 无字段级脱敏机制
  - 依赖开发者手动排除敏感字段
- **建议修复**: 
  1. 添加 OBSERVE_OUTPUT_BLACKLIST 类似的黑名单
  2. 实现 redact/mask/sanitize 工具函数
  3. 在日志传输前自动清洗敏感字段

#### 3. [可观测性] [中严重] [日志中敏感数据无 PII 处理]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - 无 PII 重写工具
  - 开发者必须手动避免记录敏感数据
  - 存在意外记录敏感信息的风险
- **建议修复**: 
  1. 添加 PII 检测和重写工具
  2. 在文档中明确记录敏感字段列表
  3. 添加测试验证敏感数据不被记录

#### 4. [可观测性] [中严重] [59处 console.* 直接调用]
- **文件/路径**: 多个源文件
- **问题描述**: 
  - 应该使用 StructuredLogger
  - console 调用可能不被日志聚合系统收集
  - 难以追踪和关联
- **建议修复**: 
  1. 替换所有 console.* 为 StructuredLogger
  2. 添加 ESLint 规则禁止 console.*
  3. 保留少量允许的 CLI 输出

#### 5. [可观测性] [中严重] [部分 logger.warn 调用缺少结构化数据]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:122`
- **问题描述**: 
  ```typescript
  logger.warn("WebSocket connection rejected: missing subprotocol token")
  ```
  - 只有消息，无 data 对象
  - 难以关联和查询
- **建议修复**: 
  1. 添加 `{ data: { ... } }` 对象
  2. 确保所有日志包含上下文数据
  3. 添加日志审查工具

#### 6. [源代码] [高严重] [Harness while(true) 循环无硬性迭代上限]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:1442`
- **问题描述**: 
  ```typescript
  while (true) {
    // ...
    // 依赖 budget gate 和 guardrail vibration 退出
    // 如果两者都失败，循环永久继续
  }
  ```
  - 依赖 budget gate 和 guardrail vibration 退出
  - 无硬性迭代次数限制
  - 可能导致无限循环
- **建议修复**: 
  1. 添加硬性 maxIterations 上限
  2. 在达到上限时强制退出
  3. 记录详细的迭代统计用于调试

#### 7. [源代码] [高严重] [Oapeflir while(true) 循环可能无限重规划]
- **文件/路径**: `src/platform/five-plane-orchestration/oapeflir/oapeflir-loop-service.ts:355`
- **问题描述**: 
  - 如果 loopReplanDecision.shouldReplan 永不变为 false
  - 重规划永远无法减少计划错误
  - 可能导致无限循环
- **建议修复**: 
  1. 添加重规划次数上限
  2. 在达到上限时强制接受当前结果
  3. 添加重规划质量衰减检测

#### 8. [源代码] [中严重] [Recovery 流程允许终态转换到 paused]
- **文件/路径**: `src/platform/five-plane-execution/runtime-state-machine.ts:105-108`
- **问题描述**: 
  - completed, failed, cancelled, aborted 可转换到 paused 进行恢复
  - 创建恢复路径可能重新进入执行
  - 需要验证此行为的正确性
- **建议修复**: 
  1. 审查 recovery 转换的合法性
  2. 确保不会导致状态不一致
  3. 添加转换前置条件验证

#### 9. [国际化] [高严重] [API 错误消息硬编码英文]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/api-error.ts`
- **问题描述**: 
  ```typescript
  return new ApiError(404, "api.task_not_found", "Task not found.");
  ```
  - 错误消息硬编码英文
  - 无后端 i18n 机制
  - 非英文客户端收到不本地化的错误
- **建议修复**: 
  1. 使用错误码而非硬编码消息
  2. 客户端根据错误码本地化
  3. 或在后端添加 i18n 支持

#### 10. [国际化] [中严重] [UI 硬编码字符串未翻译]
- **文件/路径**: `ui/packages/features/conversation/src/web/index.tsx`
- **问题描述**: 
  - "Build Plan", "Execute", "Messages" 等按钮标签硬编码
  - 不使用 i18n 系统
- **建议修复**: 
  1. 使用 `translateFeatureCopy` 替换硬编码
  2. 确保所有用户可见文本通过 i18n
  3. 添加硬编码字符串检测

#### 11. [国际化] [中严重] [Arabic 目录不完整]
- **文件/路径**: `ui/packages/shared/i18n/src/catalogs/ar-SA.ts`
- **问题描述**: 
  - ar-SA 只有约18条消息
  - en-US 和 zh-CN 有约117条
  - 缺失的 key 返回 key 本身
- **建议修复**: 
  1. 补充 Arabic 翻译
  2. 确保所有 key 都有翻译
  3. 添加翻译完整性测试

#### 12. [国际化] [低严重] [translateFeatureCopy 无回退默认值]
- **文件/路径**: `ui/packages/shared/i18n/`
- **问题描述**: 
  - 如果 feature ID 不在 catalog 中
  - 返回 undefined title/summary
  - 无合理回退
- **建议修复**: 
  1. 添加回退到 key 或默认字符串
  2. 或使用 feature ID 作为显示名
  3. 确保 UI 不显示 undefined

#### 13. [运维] [高严重] [健康检查无 readiness/liveness 区分]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts`
- **问题描述**: 
  - /healthz, /v1/healthz, /health 返回相同报告
  - 无 readiness probe（可处理流量？）
  - 无 liveness probe（系统存活？）
  - canAcceptTraffic() 未通过 HTTP 暴露
- **建议修复**: 
  1. 添加 /ready 和 /live 端点
  2. Readiness 检查依赖项就绪
  3. Liveness 检查进程健康

#### 14. [运维] [高严重] [shutdown 期间健康检查存在竞态]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts`
- **问题描述**: 
  - getHealthReportAsync() 异步检查 dbWritable
  - shutdown 期间数据库连接可能关闭
  - 健康检查仍在传入
- **建议修复**: 
  1. 在 shutdown 时返回 unhealthy
  2. 添加 shutdown 状态标志
  3. 停止接受新的健康检查请求

#### 15. [运维] [高严重] [LeaderElectionService 未与 GracefulShutdown 集成]
- **文件/路径**: `src/platform/five-plane-execution/ha/leader-election-service.ts`
- **问题描述**: 
  - LeaderElectionService.stop() 处理优雅退位
  - 但未在全局 graceful shutdown 中注册
  - 需显式注册为 shutdown handler
- **建议修复**: 
  1. 将 LeaderElectionService 添加到 shutdown handlers
  2. 确保领导者在 shutdown 时让位
  3. 验证关闭顺序正确

#### 16. [运维] [中严重] [addHandler() 无显式排序约定]
- **文件/路径**: `src/platform/five-plane-execution/startup/graceful-shutdown.ts`
- **问题描述**: 
  - 虽然 handlers 逆序执行
  - 但无显式接口声明 handler 依赖关系
  - 关键 handlers（如关闭数据库连接）应最后运行
- **建议修复**: 
  1. 添加优先级或阶段参数
  2. 文档化关键 handler 顺序要求
  3. 添加顺序验证测试

#### 17. [运维] [中严重] [unref'd timers 可能不触发]
- **文件/路径**: `src/platform/five-plane-execution/startup/process-error-handlers.ts:106`
- **问题描述**: 
  - `setTimeout(...).unref()` 不保持事件循环活跃
  - 如果主线程空闲，强制退出可能不触发
- **建议修复**: 
  1. 移除 .unref() 除非确实需要
  2. 或确保有其他方式保持事件循环
  3. 添加超时触发验证测试

#### 18. [运维] [中严重] [StartupConsistencyChecker 阻塞流量但不暴露状态]
- **文件/路径**: `src/platform/five-plane-startup-plan.ts`
- **问题描述**: 
  - canAcceptTraffic() 在 P0 问题存在时返回 false
  - 但 _trafficBlocked 是内部状态
  - 负载均衡器访问 /healthz 仍会得到 "ok"
- **建议修复**: 
  1. 在健康报告中暴露 trafficBlocked 状态
  2. 或添加单独的健康端点指示就绪状态
  3. 确保负载均衡器在阻塞时不路由流量

#### 19. [事件系统] [中严重] [TypedEventBus vs EventEmitter 混用]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - 项目同时使用 TypedEventBus 和原生 EventEmitter
  - TypedEventBus 应该是标准
  - 混用可能导致类型安全问题
- **建议修复**: 
  1. 创建迁移计划统一使用 TypedEventBus
  2. 添加 ESLint 规则禁止原生 EventEmitter
  3. 在 CI 中检测混用情况

#### 20. [源代码] [低严重] [无事件排序保证]
- **文件/路径**: `src/platform/five-plane-state-evidence/events/`
- **问题描述**: 
  - 无明确的事件排序保证文档
  - 事件处理可能乱序
  - 依赖方需要处理乱序
- **建议修复**: 
  1. 文档化事件排序语义
  2. 如果需要排序，实现序列号机制
  3. 在事件处理中考虑乱序情况

### 总结

本次补充Review（第十四轮 - 日志事件与工作流Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. StructuredLogEntry 无 requestId 字段
2. StructuredLogger data 字段无自动清洗（敏感数据风险）
3. Harness while(true) 循环无硬性迭代上限
4. Oapeflir while(true) 循环可能无限重规划
5. API 错误消息硬编码英文（无 i18n）
6. 健康检查无 readiness/liveness 区分
7. shutdown 期间健康检查存在竞态
8. LeaderElectionService 未与 GracefulShutdown 集成

**中优先级**:
1. 日志中敏感数据无 PII 处理
2. 59处 console.* 直接调用
3. 部分 logger.warn 调用缺少结构化数据
4. Recovery 流程允许终态转换到 paused
5. UI 硬编码字符串未翻译
6. Arabic 目录不完整
7. addHandler() 无显式排序约定
8. unref'd timers 可能不触发
9. StartupConsistencyChecker 阻塞流量但不暴露状态
10. TypedEventBus vs EventEmitter 混用

**低优先级**:
1. translateFeatureCopy 无回退默认值
2. 无事件排序保证

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 68 | 80 | 37 | 185 |
| 测试 | 8 | 14 | 5 | 27 |
| 配置 | 10 | 38 | 24 | 72 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **103** | **171** | **82** | **356** |

### 优先修复建议

**立即处理 (本周内)**:
1. 在 StructuredLogEntry 添加 requestId 字段
2. 实现日志敏感字段自动清洗
3. 为工作流循环添加硬性迭代上限
4. 添加 readiness/liveness 健康检查端点
5. 将 LeaderElectionService 集成到 GracefulShutdown
6. 实现 API 错误消息 i18n 支持

**短期内处理 (本月内)**:
1. 替换所有 console.* 为 StructuredLogger
2. 补充 Arabic i18n 目录
3. 修复 UI 硬编码字符串
4. 统一 TypedEventBus 替代 EventEmitter
5. 解决 shutdown 竞态条件
6. 添加事件排序保证文档

**长期规划**:
1. 建立完整的 PII 处理框架
2. 实现日志完整性验证
3. 建立工作流安全循环检测
4. 完善健康检查和就绪探测
5. 建立国际化完整测试

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十五轮 - 构建测试与类型系统Review）

### 发现的新问题

#### 1. [构建] [高严重] [无 TypeScript 增量编译缓存]
- **文件/路径**: `tsconfig.json`
- **问题描述**: 
  - 无 `incremental: true` 配置
  - 无 `tsbuildinfo` 文件
  - 每次构建是完整重建（9.6秒）
  - TypeScript 无法使用增量编译
- **建议修复**: 
  1. 在 tsconfig.json 添加 `"incremental": true`
  2. 添加 `"tsbuildinfo": ".tsbuildinfo"` 到 .gitignore
  3. 实现项目引用以支持并行构建

#### 2. [构建] [高严重] [76个 npm scripts 大部分重复构建]
- **文件/路径**: `package.json`
- **问题描述**: 
  - 40+ CLI scripts 都运行 `npm run build && node ...`
  - 每个脚本前都执行完整重建
  - 无法直接运行预构建的 CLI
  - 20+ stable:* 脚本遵循相同模式
- **建议修复**: 
  1. 创建 `build:cli` 脚本构建一次
  2. 让 CLI 脚本使用预构建的 dist 文件
  3. 添加 npm run dev 或类似的无需重建的执行方式

#### 3. [源代码] [高严重] [168处 any 类型使用]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - `stable-runner-factory.ts`: `opts: any` -> `any`
  - `billing-service-async.ts`: `input: any` 方法签名
  - 重试/超时工具使用 `(...args: any[]) => Promise<any>`
  - 绕过类型安全
- **建议修复**: 
  1. 使用泛型替代 any
  2. 替换为具体的输入类型
  3. 添加 ESLint 规则禁止 any

#### 4. [源代码] [高严重] [38处 @ts-ignore 指令]
- **文件/路径**: 多个文件
- **问题描述**: 
  - 14个 stability 相关文件有 `@ts-ignore ExecutionRecord type mismatch`
  - 5个 harness-sdk 文件有 `Partial<HarnessRun> doesn't have all required properties`
  - 2个文件有 `exactOptionalPropertyTypes` 问题
- **建议修复**: 
  1. 修复 ExecutionRecord 类型不匹配
  2. 完成 HarnessRun 类型的必需属性
  3. 移除 @ts-ignore 使用

#### 5. [源代码] [高严重] [ExecutionRecord 类型不匹配]
- **文件/路径**: `src/platform/stability/stable-evidence-bundle-support.ts:783` 等14个文件
- **问题描述**: 
  - 插入 execution record 时 TypeScript 拒绝类型
  - `ExecutionRecord` 接口要求新字段但 insert 对象缺失
  - 或 store 的 insert 方法接受不同类型
- **建议修复**: 
  1. 对齐 ExecutionRecord 接口和 store 类型
  2. 添加缺失字段到 insert 对象
  3. 或调整 store 的 insert 方法类型签名

#### 6. [源代码] [中严重] [30+处 as unknown as 双类型转换]
- **文件/路径**: `division-loader.ts`, `domain-baseline-catalog.ts`, `harness-sdk`
- **问题描述**: 
  - 完全绕过类型安全
  - 通常是类型架构问题的信号
- **建议修复**: 
  1. 修复上游类型问题
  2. 使用更具体的类型断言
  3. 重构类型层次结构

#### 7. [构建] [中严重] [rimraf  extraneous - 未在 package.json 声明]
- **文件/路径**: `node_modules/rimraf`
- **问题描述**: 
  - rimraf@6.1.3 存在于 node_modules 但未在 package.json 声明
  - 可能导致依赖问题
- **建议修复**: 
  1. 添加到 dependencies 或 devDependencies
  2. 或移除并使用其他清理方式

#### 8. [测试] [高严重] [178处直接 process.env 变更无抽象]
- **文件/路径**: `tests/unit/domains/registry/plugin-spi-registry.test.ts:640-687`
- **问题描述**: 
  - 直接读取/写入 process.env
  - 在 beforeEach/afterEach 中手动保存/恢复
  - 无 helper 抽象
- **建议修复**: 
  1. 创建 env helper 函数
  2. 统一保存和恢复模式
  3. 添加测试隔离验证

#### 9. [测试] [高严重] [21,998处 unlinkSync 调用无集中清理]
- **文件/路径**: 多个测试文件
- **问题描述**: 
  - 每个测试重新实现清理逻辑
  - 无集中清理 utility
  - 可能导致 EBUSY (Windows) 或并发访问问题
- **建议修复**: 
  1. 创建集中的测试清理 utility
  2. 使用 afterEach 确保清理
  3. 考虑使用临时目录

#### 10. [测试] [中严重] [无统一 mocking 框架]
- **文件/路径**: `tests/` 多个目录
- **问题描述**: 
  - 10,579 处 vi.fn, jest.mock, sinon 等调用
  - 无统一的 mocking 框架可见
  - 模式不一致
- **建议修复**: 
  1. 建立统一的 mock factory
  2. 标准化 mock 模式
  3. 添加 mocking 最佳实践文档

#### 11. [测试] [中严重] [Singleton 状态重置未标准化]
- **文件/路径**: `tests/unit/platform/five-plane-execution/dispatcher.test.ts`
- **问题描述**: 
  - 测试通过特定函数重置 singletons
  - 每个测试重复自己的 reset 逻辑
  - 无统一的 resetAllSingletons()
- **建议修复**: 
  1. 创建统一的 singleton reset 机制
  2. 在 afterEach 中自动调用
  3. 文档化 singleton 重置要求

#### 12. [测试] [低严重] [无分支覆盖率要求]
- **文件/路径**: `.c8rc.json`
- **问题描述**: 
  - `"100": false` - 无覆盖率要求
  - 只测量源代码，不测量测试文件
- **建议修复**: 
  1. 添加分支覆盖率阈值
  2. 在 CI 中强制执行覆盖率 gate
  3. 关注关键代码路径

#### 13. [网络] [高严重] [ScopedExternalAccessSandbox performHttpRequest 无超时]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/scoped-external-access-sandbox.ts:289`
- **问题描述**: 
  - fetch 调用无 timeout 参数
  - 挂起的请求可能永久阻塞
- **建议修复**: 
  1. 添加 AbortController timeout
  2. 使用标准的超时模式
  3. 记录超时配置

#### 14. [网络] [高严重] [OIDC 服务 fetch 调用无超时]
- **文件/路径**: `src/org-governance/sso-scim/oidc/oidc-service.ts:565,272`
- **问题描述**: 
  - OIDC token 交换无 retry logic
  - 无 timeout/abort signal
  - 认证流程可能挂起
- **建议修复**: 
  1. 添加 fetch timeout
  2. 添加 retry logic
  3. 使用标准的超时配置

#### 15. [网络] [高严重] [plugin-runtime-child 替换 globalThis.fetch 为 no-op]
- **文件/路径**: `src/domains/registry/plugin-runtime-child.ts:112`
- **问题描述**: 
  ```typescript
  globalThis.fetch = (async () => {}) as unknown as typeof globalThis.fetch;
  ```
  - 插件上下文中完全替换 fetch 为空函数
  - 插件的所有出站 HTTP 请求都会失败
  - 看起来是有意为之但可能造成问题
- **建议修复**: 
  1. 确认这是有意行为
  2. 文档化 fetch 替换行为
  3. 确保不会意外影响其他代码

#### 16. [网络] [中严重] [无显式 http.Agent 配置]
- **文件/路径**: 全局
- **问题描述**: 
  - 无显式的 http.Agent/https.Agent 配置
  - 依赖 Node.js 默认连接池
  - 无 maxSockets 或 maxConnections 配置
  - 高吞吐场景可能出问题
- **建议修复**: 
  1. 添加 http Agent 配置
  2. 文档化连接池大小
  3. 监控连接使用情况

#### 17. [序列化] [中严重] [合同版本硬编码为字符串字面量]
- **文件/路径**: `src/platform/contracts/inter-plane-contract-gateway.ts:184`
- **问题描述**: 
  - `schemaVersion: "v4.3"` 硬编码
  - 难以追踪合同版本演变
- **建议修复**: 
  1. 提取到类型化常量或 enum
  2. 集中管理合同版本
  3. 添加版本兼容性检查

#### 18. [序列化] [低严重] [Timestamp = string 丢失精度]
- **文件/路径**: `src/platform/contracts/types/domain/primitives.ts:10`
- **问题描述**: 
  - `type Timestamp = string` 无类型精度
  - ISO 8601 字符串无时区信息
- **建议修复**: 
  1. 使用 Date 对象或 branded string 类型
  2. 明确时区处理约定
  3. 文档化时间戳语义

#### 19. [源代码] [中严重] [29处非空断言 !.]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - 使用 `!.` 断言非空
  - 可能导致运行时错误
- **建议修复**: 
  1. 使用可选链 `?.` 或空值合并 `??`
  2. 添加适当的 null 检查
  3. 减少非空断言使用

#### 20. [API] [中严重] [API 路由 schema 版本硬编码]
- **文件/路径**: `src/platform/five-plane-interface/api/federation-routing-service.ts`
- **问题描述**: 
  - Zod schema 验证存在
  - 但 schema 版本未与 contract 版本同步
- **建议修复**: 
  1. 从 contract 版本派生 schema 版本
  2. 添加版本一致性检查
  3. 文档化版本关系

### 总结

本次补充Review（第十五轮 - 构建测试与类型系统Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. 无 TypeScript 增量编译缓存（每次完整重建9.6秒）
2. 76个 npm scripts 大部分重复构建
3. 168处 any 类型使用（绕过类型安全）
4. 38处 @ts-ignore 指令
5. ExecutionRecord 类型不匹配（14个文件）
6. 178处直接 process.env 变更无抽象
7. ScopedExternalAccessSandbox performHttpRequest 无超时
8. OIDC 服务 fetch 调用无超时
9. plugin-runtime-child 替换 globalThis.fetch 为 no-op

**中优先级**:
1. 30+处 as unknown as 双类型转换
2. rimraf extraneous 未声明
3. 21,998处 unlinkSync 无集中清理
4. 无统一 mocking 框架
5. Singleton 状态重置未标准化
6. 无显式 http.Agent 配置
7. 合同版本硬编码为字符串
8. Timestamp = string 丢失精度
9. 非空断言使用

**低优先级**:
1. 无分支覆盖率要求
2. API 路由 schema 版本未同步

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 72 | 87 | 39 | 198 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **109** | **182** | **85** | **376** |

### 优先修复建议

**立即处理 (本周内)**:
1. 添加 TypeScript 增量编译 (`incremental: true`)
2. 重构 npm scripts 避免重复构建
3. 减少 any 类型使用到 <50
4. 移除所有 @ts-ignore 指令
5. 修复 ExecutionRecord 类型不匹配
6. 添加 fetch 超时到 OIDC 和 sandbox

**短期内处理 (本月内)**:
1. 创建集中的测试清理 utility
2. 建立统一的 mocking 框架
3. 标准化 singleton 重置机制
4. 添加 http.Agent 连接池配置
5. 提取合同版本到类型化常量

**长期规划**:
1. 实现完整的类型安全（无 any）
2. 建立测试基础设施标准
3. 实现构建缓存优化
4. 完善网络层超时和重试
5. 建立覆盖率门禁标准

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十一轮 - 并发安全与可观测性Review）

### 发现的新问题

#### 1. [源代码] [严重] [FencingTokenService 静态可变状态无锁保护]
- **文件/路径**: `src/platform/five-plane-state-evidence/events/cas/fencing-token-service.ts:71-73`
- **问题描述**: 
  ```typescript
  private static readonly activeFences = new Map<string, FenceInfo>();
  private static globalTokenCounter = 0;
  ```
  - `acquireFence` 方法执行非原子的 read-then-write 模式
  - 两个并发调用可能同时通过检查，导致状态不一致
  - 静态 Map 在无锁情况下被并发读写
- **建议修复**: 
  1. 添加互斥锁保护 activeFences 访问
  2. 使用原子操作或 Compare-And-Swap
  3. 考虑使用数据库行锁替代内存锁

#### 2. [源代码] [严重] [AsyncFencingTokenService.globalTokenCounter 不安全递增]
- **文件/路径**: `src/platform/five-plane-state-evidence/events/cas/postgres-fencing-token-service.ts:13-17`
- **问题描述**: 
  ```typescript
  private static readonly globalTokenCounter = {
    value: 0,
    getAndIncrement(): number { return ++this.value; }
  };
  ```
  - 经典的 read-modify-write 竞态条件
  - 多线程可能读到相同的值
- **建议修复**: 
  1. 使用 `pg_advisory_xact_lock` 保护
  2. 或使用数据库自增序列
  3. 移除静态计数器，使用数据库生成

#### 3. [源代码] [严重] [BudgetAllocator.activeReservations 无保护 Map]
- **文件/路径**: `src/platform/five-plane-execution/budget-allocator.ts:394,426,458,467,569,657,711`
- **问题描述**: 
  ```typescript
  this.activeReservations.set(result.reservation.budgetReservationId, result.reservation);
  this.activeReservations.delete(reservationId);
  ```
  - 并发代码路径修改 activeReservations Map
  - 无锁保护
- **建议修复**: 
  1. 添加 Map 级别的读写锁
  2. 使用 ConcurrentMap 或类似结构
  3. 重构为无状态设计

#### 4. [源代码] [高严重] [SqliteLockAdapter.fencingCounter 不同步]
- **文件/路径**: `src/platform/five-plane-execution/distributed-lock/sqlite-lock-adapter.ts:9,14,48,74,88`
- **问题描述**: 
  - `fencingCounter` 在 acquire/extend/forceSteal 中递增
  - 无同步机制
- **建议修复**: 
  1. 使用 SQLite 序列替代内存计数器
  2. 或添加适当的同步机制

#### 5. [源代码] [高严重] [ServiceRegistry.getInstance() 检查-then-创建 竞态]
- **文件/路径**: `src/platform/shared/lifecycle/service-registry.ts:85-88`
- **问题描述**: 
  ```typescript
  public static getInstance(): ServiceRegistry {
    if (!ServiceRegistry._instance) {
      ServiceRegistry._instance = new ServiceRegistry();
    }
    return ServiceRegistry._instance;
  }
  ```
  - 两个线程可能同时看到 _instance 为 null
  - 创建两个实例
- **建议修复**: 
  1. 使用 double-checked locking
  2. 或使用 ES2022 静态字段初始化
  3. `private static readonly _instance = new ServiceRegistry();`

#### 6. [源代码] [中严重] [StructuredLogger.rotationStateByPath 异步竞态]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts:142-144,438-453`
- **问题描述**: 
  ```typescript
  private static rotationStateByPath = new Map<string, {...}>();
  ```
  - `writeToGlobalFileSink` 中存在 TOCTOU 竞态
  - 读取 pendingBytes 和实际写入之间可能被其他调用修改
- **建议修复**: 
  1. 使用锁保护 rotationStateByPath
  2. 或使用单线程写入队列

#### 7. [源代码] [中严重] [EffectBuffer.scopes 并发修改风险]
- **文件/路径**: `src/platform/five-plane-execution/execution-engine/effect-buffer.ts:446,452,476,485,492,502-514`
- **问题描述**: 
  - `scopes.delete(scopeId)` 与 `for (const [scopeId, scope] of this.scopes)` 并发
  - flush() 和 createScope 可能同时修改
- **建议修复**: 
  1. 添加迭代锁
  2. 使用 CopyOnWrite 模式
  3. 在修改时复制一份 scopes

#### 8. [源代码] [中严重] [OIDC _skipSignatureVerification 标志存在风险]
- **文件/路径**: `src/platform/five-plane-interface/api/oidc-oauth-service.ts:92,108-111`
- **问题描述**: 
  - `_skipSignatureVerification` 标志存在于生产代码路径
  - 虽然标记为仅测试使用，但配置错误可能导致安全问题
- **建议修复**: 
  1. 移除此标志或确保编译时完全删除
  2. 添加生产环境检查抛出错误
  3. 在代码审查中标记为禁止模式

#### 9. [源代码] [中严重] [Browser Executor 检测但不消毒 innerHTML]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/browser-executor.ts:541`
- **问题描述**: 
  - 检测到 innerHTML 但只返回假数据
  - 不进行实际消毒
  - 用户可能绕过保护
- **建议修复**: 
  1. 使用 DOMPurify 或类似库消毒
  2. 或完全禁止 innerHTML
  3. 返回错误而非假数据

#### 10. [数据库] [高严重] [runtime-physical-schema 无外键约束]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/runtime-physical-schema.ts`
- **问题描述**: 
  - `task_draft_id`, `confirmed_task_spec_id`, `harness_run_id` 等列无 FK 约束
  - 可能导致孤儿记录
  - 删除父记录时子记录变为孤立
- **建议修复**: 
  1. 添加外键约束
  2. 实现软删除而非硬删除
  3. 添加数据库迁移脚本

#### 11. [数据库] [中严重] [大部分表缺少软删除和审计字段]
- **文件/路径**: 多个表（除 mission_records 外）
- **问题描述**: 
  - 只有 mission_records 有完整审计：created_at, created_by, updated_at, updated_by, archived_at, archived_by
  - 大部分表缺少 deleted_at, is_deleted 软删除字段
  - created_by/updated_by 仅在 mission_records 系列表中存在
- **建议修复**: 
  1. 为所有核心表添加审计字段
  2. 实现软删除模式
  3. 添加规范说明哪些表必须有审计

#### 12. [CI/CD] [高严重] [UI quality workflow 无覆盖率门禁]
- **文件/路径**: `.github/workflows/ui-quality.yml:33`
- **问题描述**: 
  - 运行 `test:coverage` 但无阈值执行
  - 缺少 `coverage:gate`
  - 与主 CI 不一致
- **建议修复**: 
  1. 添加 `npm run coverage:gate` 到 UI workflow
  2. 或确保 UI 更改时运行主 CI

#### 13. [CI/CD] [高严重] [部署工作流无真正的手动审批]
- **文件/路径**: `.github/workflows/deploy-environment.yml`
- **问题描述**: 
  - `workflow_dispatch` 有环境保护但无显式审批步骤
  - deploy.sh 的生产确认使用 `read -p` 在 CI/CD 中会挂起
  - 生产部署缺少真正的审批控制
- **建议修复**: 
  1. 使用 GitHub Environment `required_reviewers`
  2. 修改 deploy.sh 检测 CI 环境跳过交互
  3. 添加显式审批步骤

#### 14. [CI/CD] [中严重] [回滚机制边界情况未处理]
- **文件/路径**: `.github/workflows/deploy-environment.yml:230-278`, `deploy/scripts/rollback.sh`
- **问题描述**: 
  - 部署成功但 health check 失败时不会触发回滚
  - rollback.sh 只支持 dev|staging|prod（deploy.sh 支持 dev|test|staging|pre-prod|prod）
  - pre-prod 命名空间不一致：automatic-agent-preprod vs automatic-agent-pre-prod
- **建议修复**: 
  1. 添加 health check 失败时的回滚触发
  2. 统一 rollback.sh 支持所有环境
  3. 修正 pre-prod 命名空间

#### 15. [CI/CD] [中严重] [Canary/Blue-Green 升级无健康检查]
- **文件/路径**: `.github/workflows/deploy-environment.yml:188-199`
- **问题描述**: 
  - 初始部署有 health checks
  - 但 promotion 步骤无健康验证
  - 可能推送不健康的版本
- **建议修复**: 
  1. 在 promotion 步骤后添加健康检查
  2. 验证推送的版本通过健康检查
  3. 失败时回滚

#### 16. [可观测性] [高严重] [58处 console.* 使用应使用 StructuredLogger]
- **文件/路径**: 多个文件
- **问题描述**: 
  - `src/index.ts`: 4次
  - `src/platform/ops-maturity/platform-panic/panic-propagation-service.ts`: 2 console.error + 1 console.log
  - `src/platform/five-plane-orchestration/agent-delegation/delegation-manager.service.ts`: 1 console.error
  - `src/platform/five-plane-orchestration/harness/runtime/runtime-entry-guard.ts`: 3 console.warn
  - `src/ops-maturity/chaos/chaos-experiment-scheduler.ts`: 5 console.log + 4 console.warn
  - CLI 工具中多处
- **建议修复**: 
  1. 替换 console.* 为 StructuredLogger
  2. 保留 CLI 输出的 console 用法（可接受）
  3. 添加 ESLint 规则检测

#### 17. [可观测性] [高严重] [29+执行引擎文件缺少结构化日志]
- **文件/路径**: `src/platform/five-plane-execution/execution-engine/`
- **问题描述**: 
  - `runtime-state-machine.ts`: 有 try/catch 但无 StructuredLogger
  - `multi-step-orchestration.ts`: 导入 HealthService 但无 StructuredLogger
  - `agent-executor.ts`: 169个 try 块，0 StructuredLogger 调用
  - 关键执行路径缺少可观测性
- **建议修复**: 
  1. 为所有执行引擎文件添加 StructuredLogger
  2. 记录关键操作路径
  3. 确保错误路径有日志

#### 18. [可观测性] [高严重] [执行引擎缺少 LLM 调用指标]
- **文件/路径**: `src/platform/five-plane-execution/execution-engine/`
- **问题描述**: 
  - RuntimeMetricsRegistry 支持 `recordLlmLatency`, `recordHarnessRunDuration` 等
  - 但执行引擎中无调用
  - model-gateway 有指标但执行引擎无
- **建议修复**: 
  1. 在 agent-executor 中记录 LLM 调用指标
  2. 在 multi-step-orchestration 中记录 harness 指标
  3. 启用完整指标覆盖

#### 19. [可观测性] [中严重] [日志方法使用不一致]
- **文件/路径**: 多个服务
- **问题描述**: 
  - 有的使用 `.error()`, `.warn()` 辅助方法
  - 有的使用 `logger.log({ level: "error", ... })`
  - 无统一规范
- **建议修复**: 
  1. 建立日志方法规范
  2. 添加 ESLint 规则统一
  3. 重构为一致的 helper 方法调用

#### 20. [安全] [中严重] [输入验证有限 - 仅 JWT 强类型]
- **文件/路径**: 多个 API 端点
- **问题描述**: 
  - JWT claims 有强类型验证
  - 其他输入验证有限
  - 缺少通用输入消毒
- **建议修复**: 
  1. 添加通用输入验证中间件
  2. 使用 zod 或类似库验证请求体
  3. 为所有 API 端点添加验证

#### 21. [源代码] [低严重] [HA Coordinator Leadership 获取可能非原子]
- **文件/路径**: `src/platform/five-plane-execution/ha/ha-coordinator-service-inner.ts:70-78`
- **问题描述**: 
  - `acquireLeadership` 可能执行非原子的 read-modify-write
  - leadership epoch 更新可能存在竞态
- **建议修复**: 
  1. 使用数据库行锁保护
  2. 确保领导者选举原子性
  3. 添加领导者租约续约机制

### 总结

本次补充Review（第十一轮 - 并发安全与可观测性Review - 2026-05-14）发现了21个新问题，重点关注并发安全、可观测性和CI/CD。

**高优先级 (需要立即处理)**:
1. 7个并发竞态问题（FencingTokenService, BudgetAllocator, ServiceRegistry等）
2. runtime-physical-schema 无外键约束
3. UI quality workflow 无覆盖率门禁
4. 58处 console.* 使用应替换为 StructuredLogger
5. 29+ 执行引擎文件缺少结构化日志
6. 执行引擎缺少 LLM 调用指标

**中优先级**:
1. OIDC _skipSignatureVerification 标志风险
2. Browser Executor innerHTML 不消毒
3. 大部分表缺少软删除和审计字段
4. 部署工作流无真正手动审批
5. 回滚机制边界情况未处理
6. Canary/Blue-Green 升级无健康检查
7. 日志方法使用不一致
8. 输入验证有限

**低优先级**:
1. HA Coordinator Leadership 获取可能非原子
2. SqliteLockAdapter fencingCounter 不同步

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 58 | 62 | 29 | 149 |
| 测试 | 8 | 14 | 5 | 27 |
| 配置 | 8 | 31 | 19 | 58 |
| 安全 | 7 | 8 | 1 | 16 |
| 文档 | 2 | 7 | 3 | 12 |
| UI | 0 | 3 | 3 | 6 |
| 部署 | 1 | 10 | 6 | 17 |
| **合计** | **84** | **135** | **66** | **285** |

### 优先修复建议

**立即处理 (本周内)**:
1. 修复所有7个并发竞态问题
2. 添加 runtime-physical-schema 外键约束
3. 为 UI quality workflow 添加覆盖率门禁
4. 替换关键路径的 console.* 为 StructuredLogger
5. 在执行引擎中添加 LLM 和 harness 指标

**短期内处理 (本月内)**:
1. 为所有执行引擎文件添加结构化日志
2. 添加数据库审计字段和软删除
3. 修正部署工作流审批机制
4. 统一日志方法使用规范
5. 添加 Canary/Blue-Green 健康检查

**长期规划**:
1. 建立并发安全编码规范
2. 实现完整的可观测性标准
3. 完善数据库约束和审计机制
4. 增强 CI/CD 质量和安全门禁
5. 建立代码审查清单

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十五轮 - 构建测试与类型系统Review）

### 发现的新问题

#### 1. [构建] [高严重] [无 TypeScript 增量编译缓存]
- **文件/路径**: `tsconfig.json`
- **问题描述**: 
  - 无 `incremental: true` 配置
  - 无 `tsbuildinfo` 文件
  - 每次构建是完整重建（9.6秒）
  - TypeScript 无法使用增量编译
- **建议修复**: 
  1. 在 tsconfig.json 添加 `"incremental": true`
  2. 添加 `"tsbuildinfo": ".tsbuildinfo"` 到 .gitignore
  3. 实现项目引用以支持并行构建

#### 2. [构建] [高严重] [76个 npm scripts 大部分重复构建]
- **文件/路径**: `package.json`
- **问题描述**: 
  - 40+ CLI scripts 都运行 `npm run build && node ...`
  - 每个脚本前都执行完整重建
  - 无法直接运行预构建的 CLI
  - 20+ stable:* 脚本遵循相同模式
- **建议修复**: 
  1. 创建 `build:cli` 脚本构建一次
  2. 让 CLI 脚本使用预构建的 dist 文件
  3. 添加 npm run dev 或类似的无需重建的执行方式

#### 3. [源代码] [高严重] [168处 any 类型使用]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - `stable-runner-factory.ts`: `opts: any` -> `any`
  - `billing-service-async.ts`: `input: any` 方法签名
  - 重试/超时工具使用 `(...args: any[]) => Promise<any>`
  - 绕过类型安全
- **建议修复**: 
  1. 使用泛型替代 any
  2. 替换为具体的输入类型
  3. 添加 ESLint 规则禁止 any

#### 4. [源代码] [高严重] [38处 @ts-ignore 指令]
- **文件/路径**: 多个文件
- **问题描述**: 
  - 14个 stability 相关文件有 `@ts-ignore ExecutionRecord type mismatch`
  - 5个 harness-sdk 文件有 `Partial<HarnessRun> doesn't have all required properties`
  - 2个文件有 `exactOptionalPropertyTypes` 问题
- **建议修复**: 
  1. 修复 ExecutionRecord 类型不匹配
  2. 完成 HarnessRun 类型的必需属性
  3. 移除 @ts-ignore 使用

#### 5. [源代码] [高严重] [ExecutionRecord 类型不匹配]
- **文件/路径**: `src/platform/stability/stable-evidence-bundle-support.ts:783` 等14个文件
- **问题描述**: 
  - 插入 execution record 时 TypeScript 拒绝类型
  - `ExecutionRecord` 接口要求新字段但 insert 对象缺失
  - 或 store 的 insert 方法接受不同类型
- **建议修复**: 
  1. 对齐 ExecutionRecord 接口和 store 类型
  2. 添加缺失字段到 insert 对象
  3. 或调整 store 的 insert 方法类型签名

#### 6. [源代码] [中严重] [30+处 as unknown as 双类型转换]
- **文件/路径**: `division-loader.ts`, `domain-baseline-catalog.ts`, `harness-sdk`
- **问题描述**: 
  - 完全绕过类型安全
  - 通常是类型架构问题的信号
- **建议修复**: 
  1. 修复上游类型问题
  2. 使用更具体的类型断言
  3. 重构类型层次结构

#### 7. [构建] [中严重] [rimraf  extraneous - 未在 package.json 声明]
- **文件/路径**: `node_modules/rimraf`
- **问题描述**: 
  - rimraf@6.1.3 存在于 node_modules 但未在 package.json 声明
  - 可能导致依赖问题
- **建议修复**: 
  1. 添加到 dependencies 或 devDependencies
  2. 或移除并使用其他清理方式

#### 8. [测试] [高严重] [178处直接 process.env 变更无抽象]
- **文件/路径**: `tests/unit/domains/registry/plugin-spi-registry.test.ts:640-687`
- **问题描述**: 
  - 直接读取/写入 process.env
  - 在 beforeEach/afterEach 中手动保存/恢复
  - 无 helper 抽象
- **建议修复**: 
  1. 创建 env helper 函数
  2. 统一保存和恢复模式
  3. 添加测试隔离验证

#### 9. [测试] [高严重] [21,998处 unlinkSync 调用无集中清理]
- **文件/路径**: 多个测试文件
- **问题描述**: 
  - 每个测试重新实现清理逻辑
  - 无集中清理 utility
  - 可能导致 EBUSY (Windows) 或并发访问问题
- **建议修复**: 
  1. 创建集中的测试清理 utility
  2. 使用 afterEach 确保清理
  3. 考虑使用临时目录

#### 10. [测试] [中严重] [无统一 mocking 框架]
- **文件/路径**: `tests/` 多个目录
- **问题描述**: 
  - 10,579 处 vi.fn, jest.mock, sinon 等调用
  - 无统一的 mocking 框架可见
  - 模式不一致
- **建议修复**: 
  1. 建立统一的 mock factory
  2. 标准化 mock 模式
  3. 添加 mocking 最佳实践文档

#### 11. [测试] [中严重] [Singleton 状态重置未标准化]
- **文件/路径**: `tests/unit/platform/five-plane-execution/dispatcher.test.ts`
- **问题描述**: 
  - 测试通过特定函数重置 singletons
  - 每个测试重复自己的 reset 逻辑
  - 无统一的 resetAllSingletons()
- **建议修复**: 
  1. 创建统一的 singleton reset 机制
  2. 在 afterEach 中自动调用
  3. 文档化 singleton 重置要求

#### 12. [测试] [低严重] [无分支覆盖率要求]
- **文件/路径**: `.c8rc.json`
- **问题描述**: 
  - `"100": false` - 无覆盖率要求
  - 只测量源代码，不测量测试文件
- **建议修复**: 
  1. 添加分支覆盖率阈值
  2. 在 CI 中强制执行覆盖率 gate
  3. 关注关键代码路径

#### 13. [网络] [高严重] [ScopedExternalAccessSandbox performHttpRequest 无超时]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/scoped-external-access-sandbox.ts:289`
- **问题描述**: 
  - fetch 调用无 timeout 参数
  - 挂起的请求可能永久阻塞
- **建议修复**: 
  1. 添加 AbortController timeout
  2. 使用标准的超时模式
  3. 记录超时配置

#### 14. [网络] [高严重] [OIDC 服务 fetch 调用无超时]
- **文件/路径**: `src/org-governance/sso-scim/oidc/oidc-service.ts:565,272`
- **问题描述**: 
  - OIDC token 交换无 retry logic
  - 无 timeout/abort signal
  - 认证流程可能挂起
- **建议修复**: 
  1. 添加 fetch timeout
  2. 添加 retry logic
  3. 使用标准的超时配置

#### 15. [网络] [高严重] [plugin-runtime-child 替换 globalThis.fetch 为 no-op]
- **文件/路径**: `src/domains/registry/plugin-runtime-child.ts:112`
- **问题描述**: 
  ```typescript
  globalThis.fetch = (async () => {}) as unknown as typeof globalThis.fetch;
  ```
  - 插件上下文中完全替换 fetch 为空函数
  - 插件的所有出站 HTTP 请求都会失败
  - 看起来是有意为之但可能造成问题
- **建议修复**: 
  1. 确认这是有意行为
  2. 文档化 fetch 替换行为
  3. 确保不会意外影响其他代码

#### 16. [网络] [中严重] [无显式 http.Agent 配置]
- **文件/路径**: 全局
- **问题描述**: 
  - 无显式的 http.Agent/https.Agent 配置
  - 依赖 Node.js 默认连接池
  - 无 maxSockets 或 maxConnections 配置
  - 高吞吐场景可能出问题
- **建议修复**: 
  1. 添加 http Agent 配置
  2. 文档化连接池大小
  3. 监控连接使用情况

#### 17. [序列化] [中严重] [合同版本硬编码为字符串字面量]
- **文件/路径**: `src/platform/contracts/inter-plane-contract-gateway.ts:184`
- **问题描述**: 
  - `schemaVersion: "v4.3"` 硬编码
  - 难以追踪合同版本演变
- **建议修复**: 
  1. 提取到类型化常量或 enum
  2. 集中管理合同版本
  3. 添加版本兼容性检查

#### 18. [序列化] [低严重] [Timestamp = string 丢失精度]
- **文件/路径**: `src/platform/contracts/types/domain/primitives.ts:10`
- **问题描述**: 
  - `type Timestamp = string` 无类型精度
  - ISO 8601 字符串无时区信息
- **建议修复**: 
  1. 使用 Date 对象或 branded string 类型
  2. 明确时区处理约定
  3. 文档化时间戳语义

#### 19. [源代码] [中严重] [29处非空断言 !.]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - 使用 `!.` 断言非空
  - 可能导致运行时错误
- **建议修复**: 
  1. 使用可选链 `?.` 或空值合并 `??`
  2. 添加适当的 null 检查
  3. 减少非空断言使用

#### 20. [API] [中严重] [API 路由 schema 版本硬编码]
- **文件/路径**: `src/platform/five-plane-interface/api/federation-routing-service.ts`
- **问题描述**: 
  - Zod schema 验证存在
  - 但 schema 版本未与 contract 版本同步
- **建议修复**: 
  1. 从 contract 版本派生 schema 版本
  2. 添加版本一致性检查
  3. 文档化版本关系

### 总结

本次补充Review（第十五轮 - 构建测试与类型系统Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. 无 TypeScript 增量编译缓存（每次完整重建9.6秒）
2. 76个 npm scripts 大部分重复构建
3. 168处 any 类型使用（绕过类型安全）
4. 38处 @ts-ignore 指令
5. ExecutionRecord 类型不匹配（14个文件）
6. 178处直接 process.env 变更无抽象
7. ScopedExternalAccessSandbox performHttpRequest 无超时
8. OIDC 服务 fetch 调用无超时
9. plugin-runtime-child 替换 globalThis.fetch 为 no-op

**中优先级**:
1. 30+处 as unknown as 双类型转换
2. rimraf extraneous 未声明
3. 21,998处 unlinkSync 无集中清理
4. 无统一 mocking 框架
5. Singleton 状态重置未标准化
6. 无显式 http.Agent 配置
7. 合同版本硬编码为字符串
8. Timestamp = string 丢失精度
9. 非空断言使用

**低优先级**:
1. 无分支覆盖率要求
2. API 路由 schema 版本未同步

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 72 | 87 | 39 | 198 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **109** | **182** | **85** | **376** |

### 优先修复建议

**立即处理 (本周内)**:
1. 添加 TypeScript 增量编译 (`incremental: true`)
2. 重构 npm scripts 避免重复构建
3. 减少 any 类型使用到 <50
4. 移除所有 @ts-ignore 指令
5. 修复 ExecutionRecord 类型不匹配
6. 添加 fetch 超时到 OIDC 和 sandbox

**短期内处理 (本月内)**:
1. 创建集中的测试清理 utility
2. 建立统一的 mocking 框架
3. 标准化 singleton 重置机制
4. 添加 http.Agent 连接池配置
5. 提取合同版本到类型化常量

**长期规划**:
1. 实现完整的类型安全（无 any）
2. 建立测试基础设施标准
3. 实现构建缓存优化
4. 完善网络层超时和重试
5. 建立覆盖率门禁标准

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十四轮 - 日志事件与工作流Review）

### 发现的新问题

#### 1. [可观测性] [高严重] [StructuredLogEntry 无 requestId 字段]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - StructuredLogEntry 接口无 requestId 字段
  - 只有 correlationId，它回退到 traceId
  - 无法独立追踪单个 HTTP 请求
- **建议修复**: 
  1. 在 StructuredLogEntry 添加 requestId 字段
  2. 在 HTTP 中间件中生成和注入 requestId
  3. 确保 requestId 流经所有日志调用

#### 2. [可观测性] [高严重] [StructuredLogger data 字段无自动清洗]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - 如果开发者传递 `{ password: "..." }` 给 logger.info，密码会被记录
  - 无字段级脱敏机制
  - 依赖开发者手动排除敏感字段
- **建议修复**: 
  1. 添加 OBSERVE_OUTPUT_BLACKLIST 类似的黑名单
  2. 实现 redact/mask/sanitize 工具函数
  3. 在日志传输前自动清洗敏感字段

#### 3. [可观测性] [中严重] [日志中敏感数据无 PII 处理]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - 无 PII 重写工具
  - 开发者必须手动避免记录敏感数据
  - 存在意外记录敏感信息的风险
- **建议修复**: 
  1. 添加 PII 检测和重写工具
  2. 在文档中明确记录敏感字段列表
  3. 添加测试验证敏感数据不被记录

#### 4. [可观测性] [中严重] [59处 console.* 直接调用]
- **文件/路径**: 多个源文件
- **问题描述**: 
  - 应该使用 StructuredLogger
  - console 调用可能不被日志聚合系统收集
  - 难以追踪和关联
- **建议修复**: 
  1. 替换所有 console.* 为 StructuredLogger
  2. 添加 ESLint 规则禁止 console.*
  3. 保留少量允许的 CLI 输出

#### 5. [可观测性] [中严重] [部分 logger.warn 调用缺少结构化数据]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:122`
- **问题描述**: 
  ```typescript
  logger.warn("WebSocket connection rejected: missing subprotocol token")
  ```
  - 只有消息，无 data 对象
  - 难以关联和查询
- **建议修复**: 
  1. 添加 `{ data: { ... } }` 对象
  2. 确保所有日志包含上下文数据
  3. 添加日志审查工具

#### 6. [源代码] [高严重] [Harness while(true) 循环无硬性迭代上限]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:1442`
- **问题描述**: 
  ```typescript
  while (true) {
    // ...
    // 依赖 budget gate 和 guardrail vibration 退出
    // 如果两者都失败，循环永久继续
  }
  ```
  - 依赖 budget gate 和 guardrail vibration 退出
  - 无硬性迭代次数限制
  - 可能导致无限循环
- **建议修复**: 
  1. 添加硬性 maxIterations 上限
  2. 在达到上限时强制退出
  3. 记录详细的迭代统计用于调试

#### 7. [源代码] [高严重] [Oapeflir while(true) 循环可能无限重规划]
- **文件/路径**: `src/platform/five-plane-orchestration/oapeflir/oapeflir-loop-service.ts:355`
- **问题描述**: 
  - 如果 loopReplanDecision.shouldReplan 永不变为 false
  - 重规划永远无法减少计划错误
  - 可能导致无限循环
- **建议修复**: 
  1. 添加重规划次数上限
  2. 在达到上限时强制接受当前结果
  3. 添加重规划质量衰减检测

#### 8. [源代码] [中严重] [Recovery 流程允许终态转换到 paused]
- **文件/路径**: `src/platform/five-plane-execution/runtime-state-machine.ts:105-108`
- **问题描述**: 
  - completed, failed, cancelled, aborted 可转换到 paused 进行恢复
  - 创建恢复路径可能重新进入执行
  - 需要验证此行为的正确性
- **建议修复**: 
  1. 审查 recovery 转换的合法性
  2. 确保不会导致状态不一致
  3. 添加转换前置条件验证

#### 9. [国际化] [高严重] [API 错误消息硬编码英文]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/api-error.ts`
- **问题描述**: 
  ```typescript
  return new ApiError(404, "api.task_not_found", "Task not found.");
  ```
  - 错误消息硬编码英文
  - 无后端 i18n 机制
  - 非英文客户端收到不本地化的错误
- **建议修复**: 
  1. 使用错误码而非硬编码消息
  2. 客户端根据错误码本地化
  3. 或在后端添加 i18n 支持

#### 10. [国际化] [中严重] [UI 硬编码字符串未翻译]
- **文件/路径**: `ui/packages/features/conversation/src/web/index.tsx`
- **问题描述**: 
  - "Build Plan", "Execute", "Messages" 等按钮标签硬编码
  - 不使用 i18n 系统
- **建议修复**: 
  1. 使用 `translateFeatureCopy` 替换硬编码
  2. 确保所有用户可见文本通过 i18n
  3. 添加硬编码字符串检测

#### 11. [国际化] [中严重] [Arabic 目录不完整]
- **文件/路径**: `ui/packages/shared/i18n/src/catalogs/ar-SA.ts`
- **问题描述**: 
  - ar-SA 只有约18条消息
  - en-US 和 zh-CN 有约117条
  - 缺失的 key 返回 key 本身
- **建议修复**: 
  1. 补充 Arabic 翻译
  2. 确保所有 key 都有翻译
  3. 添加翻译完整性测试

#### 12. [国际化] [低严重] [translateFeatureCopy 无回退默认值]
- **文件/路径**: `ui/packages/shared/i18n/`
- **问题描述**: 
  - 如果 feature ID 不在 catalog 中
  - 返回 undefined title/summary
  - 无合理回退
- **建议修复**: 
  1. 添加回退到 key 或默认字符串
  2. 或使用 feature ID 作为显示名
  3. 确保 UI 不显示 undefined

#### 13. [运维] [高严重] [健康检查无 readiness/liveness 区分]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts`
- **问题描述**: 
  - /healthz, /v1/healthz, /health 返回相同报告
  - 无 readiness probe（可处理流量？）
  - 无 liveness probe（系统存活？）
  - canAcceptTraffic() 未通过 HTTP 暴露
- **建议修复**: 
  1. 添加 /ready 和 /live 端点
  2. Readiness 检查依赖项就绪
  3. Liveness 检查进程健康

#### 14. [运维] [高严重] [shutdown 期间健康检查存在竞态]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts`
- **问题描述**: 
  - getHealthReportAsync() 异步检查 dbWritable
  - shutdown 期间数据库连接可能关闭
  - 健康检查仍在传入
- **建议修复**: 
  1. 在 shutdown 时返回 unhealthy
  2. 添加 shutdown 状态标志
  3. 停止接受新的健康检查请求

#### 15. [运维] [高严重] [LeaderElectionService 未与 GracefulShutdown 集成]
- **文件/路径**: `src/platform/five-plane-execution/ha/leader-election-service.ts`
- **问题描述**: 
  - LeaderElectionService.stop() 处理优雅退位
  - 但未在全局 graceful shutdown 中注册
  - 需显式注册为 shutdown handler
- **建议修复**: 
  1. 将 LeaderElectionService 添加到 shutdown handlers
  2. 确保领导者在 shutdown 时让位
  3. 验证关闭顺序正确

#### 16. [运维] [中严重] [addHandler() 无显式排序约定]
- **文件/路径**: `src/platform/five-plane-execution/startup/graceful-shutdown.ts`
- **问题描述**: 
  - 虽然 handlers 逆序执行
  - 但无显式接口声明 handler 依赖关系
  - 关键 handlers（如关闭数据库连接）应最后运行
- **建议修复**: 
  1. 添加优先级或阶段参数
  2. 文档化关键 handler 顺序要求
  3. 添加顺序验证测试

#### 17. [运维] [中严重] [unref'd timers 可能不触发]
- **文件/路径**: `src/platform/five-plane-execution/startup/process-error-handlers.ts:106`
- **问题描述**: 
  - `setTimeout(...).unref()` 不保持事件循环活跃
  - 如果主线程空闲，强制退出可能不触发
- **建议修复**: 
  1. 移除 .unref() 除非确实需要
  2. 或确保有其他方式保持事件循环
  3. 添加超时触发验证测试

#### 18. [运维] [中严重] [StartupConsistencyChecker 阻塞流量但不暴露状态]
- **文件/路径**: `src/platform/five-plane-startup-plan.ts`
- **问题描述**: 
  - canAcceptTraffic() 在 P0 问题存在时返回 false
  - 但 _trafficBlocked 是内部状态
  - 负载均衡器访问 /healthz 仍会得到 "ok"
- **建议修复**: 
  1. 在健康报告中暴露 trafficBlocked 状态
  2. 或添加单独的健康端点指示就绪状态
  3. 确保负载均衡器在阻塞时不路由流量

#### 19. [事件系统] [中严重] [TypedEventBus vs EventEmitter 混用]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - 项目同时使用 TypedEventBus 和原生 EventEmitter
  - TypedEventBus 应该是标准
  - 混用可能导致类型安全问题
- **建议修复**: 
  1. 创建迁移计划统一使用 TypedEventBus
  2. 添加 ESLint 规则禁止原生 EventEmitter
  3. 在 CI 中检测混用情况

#### 20. [源代码] [低严重] [无事件排序保证]
- **文件/路径**: `src/platform/five-plane-state-evidence/events/`
- **问题描述**: 
  - 无明确的事件排序保证文档
  - 事件处理可能乱序
  - 依赖方需要处理乱序
- **建议修复**: 
  1. 文档化事件排序语义
  2. 如果需要排序，实现序列号机制
  3. 在事件处理中考虑乱序情况

### 总结

本次补充Review（第十四轮 - 日志事件与工作流Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. StructuredLogEntry 无 requestId 字段
2. StructuredLogger data 字段无自动清洗（敏感数据风险）
3. Harness while(true) 循环无硬性迭代上限
4. Oapeflir while(true) 循环可能无限重规划
5. API 错误消息硬编码英文（无 i18n）
6. 健康检查无 readiness/liveness 区分
7. shutdown 期间健康检查存在竞态
8. LeaderElectionService 未与 GracefulShutdown 集成

**中优先级**:
1. 日志中敏感数据无 PII 处理
2. 59处 console.* 直接调用
3. 部分 logger.warn 调用缺少结构化数据
4. Recovery 流程允许终态转换到 paused
5. UI 硬编码字符串未翻译
6. Arabic 目录不完整
7. addHandler() 无显式排序约定
8. unref'd timers 可能不触发
9. StartupConsistencyChecker 阻塞流量但不暴露状态
10. TypedEventBus vs EventEmitter 混用

**低优先级**:
1. translateFeatureCopy 无回退默认值
2. 无事件排序保证

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 68 | 80 | 37 | 185 |
| 测试 | 8 | 14 | 5 | 27 |
| 配置 | 10 | 38 | 24 | 72 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **103** | **171** | **82** | **356** |

### 优先修复建议

**立即处理 (本周内)**:
1. 在 StructuredLogEntry 添加 requestId 字段
2. 实现日志敏感字段自动清洗
3. 为工作流循环添加硬性迭代上限
4. 添加 readiness/liveness 健康检查端点
5. 将 LeaderElectionService 集成到 GracefulShutdown
6. 实现 API 错误消息 i18n 支持

**短期内处理 (本月内)**:
1. 替换所有 console.* 为 StructuredLogger
2. 补充 Arabic i18n 目录
3. 修复 UI 硬编码字符串
4. 统一 TypedEventBus 替代 EventEmitter
5. 解决 shutdown 竞态条件
6. 添加事件排序保证文档

**长期规划**:
1. 建立完整的 PII 处理框架
2. 实现日志完整性验证
3. 建立工作流安全循环检测
4. 完善健康检查和就绪探测
5. 建立国际化完整测试

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十五轮 - 构建测试与类型系统Review）

### 发现的新问题

#### 1. [构建] [高严重] [无 TypeScript 增量编译缓存]
- **文件/路径**: `tsconfig.json`
- **问题描述**: 
  - 无 `incremental: true` 配置
  - 无 `tsbuildinfo` 文件
  - 每次构建是完整重建（9.6秒）
  - TypeScript 无法使用增量编译
- **建议修复**: 
  1. 在 tsconfig.json 添加 `"incremental": true`
  2. 添加 `"tsbuildinfo": ".tsbuildinfo"` 到 .gitignore
  3. 实现项目引用以支持并行构建

#### 2. [构建] [高严重] [76个 npm scripts 大部分重复构建]
- **文件/路径**: `package.json`
- **问题描述**: 
  - 40+ CLI scripts 都运行 `npm run build && node ...`
  - 每个脚本前都执行完整重建
  - 无法直接运行预构建的 CLI
  - 20+ stable:* 脚本遵循相同模式
- **建议修复**: 
  1. 创建 `build:cli` 脚本构建一次
  2. 让 CLI 脚本使用预构建的 dist 文件
  3. 添加 npm run dev 或类似的无需重建的执行方式

#### 3. [源代码] [高严重] [168处 any 类型使用]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - `stable-runner-factory.ts`: `opts: any` -> `any`
  - `billing-service-async.ts`: `input: any` 方法签名
  - 重试/超时工具使用 `(...args: any[]) => Promise<any>`
  - 绕过类型安全
- **建议修复**: 
  1. 使用泛型替代 any
  2. 替换为具体的输入类型
  3. 添加 ESLint 规则禁止 any

#### 4. [源代码] [高严重] [38处 @ts-ignore 指令]
- **文件/路径**: 多个文件
- **问题描述**: 
  - 14个 stability 相关文件有 `@ts-ignore ExecutionRecord type mismatch`
  - 5个 harness-sdk 文件有 `Partial<HarnessRun> doesn't have all required properties`
  - 2个文件有 `exactOptionalPropertyTypes` 问题
- **建议修复**: 
  1. 修复 ExecutionRecord 类型不匹配
  2. 完成 HarnessRun 类型的必需属性
  3. 移除 @ts-ignore 使用

#### 5. [源代码] [高严重] [ExecutionRecord 类型不匹配]
- **文件/路径**: `src/platform/stability/stable-evidence-bundle-support.ts:783` 等14个文件
- **问题描述**: 
  - 插入 execution record 时 TypeScript 拒绝类型
  - `ExecutionRecord` 接口要求新字段但 insert 对象缺失
  - 或 store 的 insert 方法接受不同类型
- **建议修复**: 
  1. 对齐 ExecutionRecord 接口和 store 类型
  2. 添加缺失字段到 insert 对象
  3. 或调整 store 的 insert 方法类型签名

#### 6. [源代码] [中严重] [30+处 as unknown as 双类型转换]
- **文件/路径**: `division-loader.ts`, `domain-baseline-catalog.ts`, `harness-sdk`
- **问题描述**: 
  - 完全绕过类型安全
  - 通常是类型架构问题的信号
- **建议修复**: 
  1. 修复上游类型问题
  2. 使用更具体的类型断言
  3. 重构类型层次结构

#### 7. [构建] [中严重] [rimraf  extraneous - 未在 package.json 声明]
- **文件/路径**: `node_modules/rimraf`
- **问题描述**: 
  - rimraf@6.1.3 存在于 node_modules 但未在 package.json 声明
  - 可能导致依赖问题
- **建议修复**: 
  1. 添加到 dependencies 或 devDependencies
  2. 或移除并使用其他清理方式

#### 8. [测试] [高严重] [178处直接 process.env 变更无抽象]
- **文件/路径**: `tests/unit/domains/registry/plugin-spi-registry.test.ts:640-687`
- **问题描述**: 
  - 直接读取/写入 process.env
  - 在 beforeEach/afterEach 中手动保存/恢复
  - 无 helper 抽象
- **建议修复**: 
  1. 创建 env helper 函数
  2. 统一保存和恢复模式
  3. 添加测试隔离验证

#### 9. [测试] [高严重] [21,998处 unlinkSync 调用无集中清理]
- **文件/路径**: 多个测试文件
- **问题描述**: 
  - 每个测试重新实现清理逻辑
  - 无集中清理 utility
  - 可能导致 EBUSY (Windows) 或并发访问问题
- **建议修复**: 
  1. 创建集中的测试清理 utility
  2. 使用 afterEach 确保清理
  3. 考虑使用临时目录

#### 10. [测试] [中严重] [无统一 mocking 框架]
- **文件/路径**: `tests/` 多个目录
- **问题描述**: 
  - 10,579 处 vi.fn, jest.mock, sinon 等调用
  - 无统一的 mocking 框架可见
  - 模式不一致
- **建议修复**: 
  1. 建立统一的 mock factory
  2. 标准化 mock 模式
  3. 添加 mocking 最佳实践文档

#### 11. [测试] [中严重] [Singleton 状态重置未标准化]
- **文件/路径**: `tests/unit/platform/five-plane-execution/dispatcher.test.ts`
- **问题描述**: 
  - 测试通过特定函数重置 singletons
  - 每个测试重复自己的 reset 逻辑
  - 无统一的 resetAllSingletons()
- **建议修复**: 
  1. 创建统一的 singleton reset 机制
  2. 在 afterEach 中自动调用
  3. 文档化 singleton 重置要求

#### 12. [测试] [低严重] [无分支覆盖率要求]
- **文件/路径**: `.c8rc.json`
- **问题描述**: 
  - `"100": false` - 无覆盖率要求
  - 只测量源代码，不测量测试文件
- **建议修复**: 
  1. 添加分支覆盖率阈值
  2. 在 CI 中强制执行覆盖率 gate
  3. 关注关键代码路径

#### 13. [网络] [高严重] [ScopedExternalAccessSandbox performHttpRequest 无超时]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/scoped-external-access-sandbox.ts:289`
- **问题描述**: 
  - fetch 调用无 timeout 参数
  - 挂起的请求可能永久阻塞
- **建议修复**: 
  1. 添加 AbortController timeout
  2. 使用标准的超时模式
  3. 记录超时配置

#### 14. [网络] [高严重] [OIDC 服务 fetch 调用无超时]
- **文件/路径**: `src/org-governance/sso-scim/oidc/oidc-service.ts:565,272`
- **问题描述**: 
  - OIDC token 交换无 retry logic
  - 无 timeout/abort signal
  - 认证流程可能挂起
- **建议修复**: 
  1. 添加 fetch timeout
  2. 添加 retry logic
  3. 使用标准的超时配置

#### 15. [网络] [高严重] [plugin-runtime-child 替换 globalThis.fetch 为 no-op]
- **文件/路径**: `src/domains/registry/plugin-runtime-child.ts:112`
- **问题描述**: 
  ```typescript
  globalThis.fetch = (async () => {}) as unknown as typeof globalThis.fetch;
  ```
  - 插件上下文中完全替换 fetch 为空函数
  - 插件的所有出站 HTTP 请求都会失败
  - 看起来是有意为之但可能造成问题
- **建议修复**: 
  1. 确认这是有意行为
  2. 文档化 fetch 替换行为
  3. 确保不会意外影响其他代码

#### 16. [网络] [中严重] [无显式 http.Agent 配置]
- **文件/路径**: 全局
- **问题描述**: 
  - 无显式的 http.Agent/https.Agent 配置
  - 依赖 Node.js 默认连接池
  - 无 maxSockets 或 maxConnections 配置
  - 高吞吐场景可能出问题
- **建议修复**: 
  1. 添加 http Agent 配置
  2. 文档化连接池大小
  3. 监控连接使用情况

#### 17. [序列化] [中严重] [合同版本硬编码为字符串字面量]
- **文件/路径**: `src/platform/contracts/inter-plane-contract-gateway.ts:184`
- **问题描述**: 
  - `schemaVersion: "v4.3"` 硬编码
  - 难以追踪合同版本演变
- **建议修复**: 
  1. 提取到类型化常量或 enum
  2. 集中管理合同版本
  3. 添加版本兼容性检查

#### 18. [序列化] [低严重] [Timestamp = string 丢失精度]
- **文件/路径**: `src/platform/contracts/types/domain/primitives.ts:10`
- **问题描述**: 
  - `type Timestamp = string` 无类型精度
  - ISO 8601 字符串无时区信息
- **建议修复**: 
  1. 使用 Date 对象或 branded string 类型
  2. 明确时区处理约定
  3. 文档化时间戳语义

#### 19. [源代码] [中严重] [29处非空断言 !.]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - 使用 `!.` 断言非空
  - 可能导致运行时错误
- **建议修复**: 
  1. 使用可选链 `?.` 或空值合并 `??`
  2. 添加适当的 null 检查
  3. 减少非空断言使用

#### 20. [API] [中严重] [API 路由 schema 版本硬编码]
- **文件/路径**: `src/platform/five-plane-interface/api/federation-routing-service.ts`
- **问题描述**: 
  - Zod schema 验证存在
  - 但 schema 版本未与 contract 版本同步
- **建议修复**: 
  1. 从 contract 版本派生 schema 版本
  2. 添加版本一致性检查
  3. 文档化版本关系

### 总结

本次补充Review（第十五轮 - 构建测试与类型系统Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. 无 TypeScript 增量编译缓存（每次完整重建9.6秒）
2. 76个 npm scripts 大部分重复构建
3. 168处 any 类型使用（绕过类型安全）
4. 38处 @ts-ignore 指令
5. ExecutionRecord 类型不匹配（14个文件）
6. 178处直接 process.env 变更无抽象
7. ScopedExternalAccessSandbox performHttpRequest 无超时
8. OIDC 服务 fetch 调用无超时
9. plugin-runtime-child 替换 globalThis.fetch 为 no-op

**中优先级**:
1. 30+处 as unknown as 双类型转换
2. rimraf extraneous 未声明
3. 21,998处 unlinkSync 无集中清理
4. 无统一 mocking 框架
5. Singleton 状态重置未标准化
6. 无显式 http.Agent 配置
7. 合同版本硬编码为字符串
8. Timestamp = string 丢失精度
9. 非空断言使用

**低优先级**:
1. 无分支覆盖率要求
2. API 路由 schema 版本未同步

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 72 | 87 | 39 | 198 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **109** | **182** | **85** | **376** |

### 优先修复建议

**立即处理 (本周内)**:
1. 添加 TypeScript 增量编译 (`incremental: true`)
2. 重构 npm scripts 避免重复构建
3. 减少 any 类型使用到 <50
4. 移除所有 @ts-ignore 指令
5. 修复 ExecutionRecord 类型不匹配
6. 添加 fetch 超时到 OIDC 和 sandbox

**短期内处理 (本月内)**:
1. 创建集中的测试清理 utility
2. 建立统一的 mocking 框架
3. 标准化 singleton 重置机制
4. 添加 http.Agent 连接池配置
5. 提取合同版本到类型化常量

**长期规划**:
1. 实现完整的类型安全（无 any）
2. 建立测试基础设施标准
3. 实现构建缓存优化
4. 完善网络层超时和重试
5. 建立覆盖率门禁标准

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十三轮 - 缓存会话与实时通信Review）

### 发现的新问题

#### 1. [安全] [严重] [CORS allowedMethods 缺少 PUT/PATCH/DELETE]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/response-hardening.ts:12-14`
- **问题描述**: 
  ```typescript
  export const DEFAULT_CORS_CONFIG: CorsConfig = {
    allowedMethods: ["GET", "POST", "OPTIONS"],  // 缺少 PUT, PATCH, DELETE
  ```
  - 浏览器 CORS preflight 会拒绝 PUT/PATCH/DELETE 请求
  - 但路由中有 PATCH /v1/tasks/:id, DELETE endpoints
- **建议修复**: 
  1. 添加 PUT, PATCH, DELETE 到 allowedMethods
  2. 确保 CORS 配置与应用路由方法一致
  3. 添加测试验证所有 HTTP 方法的 CORS preflight

#### 2. [安全] [严重] [Mission Routes 错误响应包装不当]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/mission-routes.ts:101,118-124`
- **问题描述**: 
  ```typescript
  return buildJsonResponse(ctx.requestId, 404, { error: { code: "MISSION_NOT_FOUND" } });
  ```
  - 使用 buildJsonResponse（成功包装）返回错误
  - 错误被包装在成功信封 `{requestId, data: {error: {...}}}` 中
  - 违反 REST 规范
- **建议修复**: 
  1. 使用 buildJsonErrorResponse 处理错误
  2. 确保所有错误路径使用正确的错误信封
  3. 添加 API 一致性测试

#### 3. [源代码] [高严重] [WebSocketBridge 无最大连接数限制]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:103-107`
- **问题描述**: 
  ```typescript
  this.wss = new WebSocketServer({
    server,
    path: WS_PATH,
    maxPayload: options.maxPayloadBytes ?? DEFAULT_MAX_PAYLOAD_BYTES,
    // 无 maxConnections 限制！
  });
  ```
  - 与 dashboard-websocket-server.ts 的 maxClients: 1000 不同
  - 恶意客户端可耗尽服务器资源
- **建议修复**: 
  1. 添加 maxConnections 限制
  2. 实现连接限制拒绝策略
  3. 添加连接计数指标

#### 4. [源代码] [高严重] [pendingAcks 在断开连接时未清理]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:268-282`
- **问题描述**: 
  - handleDisconnection 清理 subscribedTasks, taskSubscribers, slowConsumers
  - 但 pendingAcks Map 未清理
  - 断开的客户端未确认的消息永久留在内存
- **建议修复**: 
  1. 在 handleDisconnection 中删除所有 pendingAcks 条目
  2. 考虑添加超时机制自动清理未确认消息
  3. 记录清理操作的审计日志

#### 5. [安全] [高严重] [硬编码回退密钥]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/audit-event-integrity.ts:41`
- **问题描述**: 
  ```typescript
  const AUDIT_INTEGRITY_HMAC_KEY = process.env["AA_AUDIT_INTEGRITY_HMAC_KEY"] ?? "audit-integrity-secret-key-32-bytes!";
  ```
  - 占位符密钥用作回退
  - 如果环境变量未设置，使用不安全密钥
- **建议修复**: 
  1. 移除回退值，要求环境变量必须设置
  2. 在启动时验证密钥存在且足够强
  3. 如果密钥缺失导致启动失败

#### 6. [配置] [高严重] [config/runtime/test.json 缺少超时配置]
- **文件/路径**: `config/runtime/test.json`
- **问题描述**: 
  - 只有3个字段，缺少 maxConcurrentTasks, defaultTaskTimeoutMs, defaultStepTimeoutMs
  - 回退到默认值可能不适合测试环境
- **建议修复**: 
  1. 添加完整的 timeout/tuning 配置
  2. 确保测试环境有合理的默认值
  3. 文档化必需的配置字段

#### 7. [配置] [高严重] [prod.json 限制过严 - maxConcurrentTasks=1]
- **文件/路径**: `config/runtime/prod.json`
- **问题描述**: 
  - maxConcurrentTasks: 1（vs dev:8, staging:4, pre-prod:6）
  - defaultTaskTimeoutMs: 120000（vs dev:600000）
  - defaultStepTimeoutMs: 60000（vs dev:180000）
  - 生产环境严重受限，可能影响吞吐量
- **建议修复**: 
  1. 评估并调整生产限制
  2. 与业务需求匹配
  3. 添加生产容量测试

#### 8. [源代码] [高严重] [多个服务存在无界 Map 无驱逐策略]
- **文件/路径**: 多个域服务
- **问题描述**: 
  - `domain-knowledge-schema-service.ts:73-75`: schemas, sourceContent, sourceTimestamps 无限制
  - `domain-eval-framework-service.ts:91-95`: frameworks, qualityAxes 等无限制
  - `domain-recipe-service.ts:67-68`: recipes, versions 无限制
  - `domain-risk-profile-service.ts:51`: profiles 无限制
  - Session Maps (session-management.ts:83-89) 无自动清理
- **建议修复**: 
  1. 为所有 Map 实现 LRU 或 TTL 驱逐策略
  2. 添加后台清理任务
  3. 添加大小监控和告警

#### 9. [源代码] [中严重] [taskEventHistory Map 永不清理]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:91,548-555`
- **问题描述**: 
  - taskEventHistory 限制单任务历史为 200 条
  - 但 Map 本身从不清理
  - 取消订阅的任务历史永久保留
- **建议修复**: 
  1. 实现后台清理无订阅者任务的历史
  2. 添加任务订阅者监控
  3. 考虑使用 WeakMap 替代

#### 10. [配置] [中严重] [安全配置 drift - remoteWorkerRegistration 缺失]
- **文件/路径**: `config/security/dev.json`, `staging.json`, `pre-prod.json`
- **问题描述**: 
  - default.json 有完整的 remoteWorkerRegistration 配置
  - dev/staging/pre-prod 只有 approvalMode
  - prod 有 approvalMode 但无 remoteWorkerRegistration
  - 安全配置不一致
- **建议修复**: 
  1. 统一所有环境的 remoteWorkerRegistration
  2. 添加安全配置验证
  3. 确保最低安全基线

#### 11. [API] [中严重] [OpenAPI 端点响应格式不一致]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/utils.ts:207-215`
- **问题描述**: 
  - `/v1/openapi.json` 使用 buildJsonDocumentResponse（无包装）
  - 其他端点使用 buildJsonResponse（{requestId, data} 包装）
  - 客户端体验不一致
- **建议修复**: 
  1. 统一响应信封格式
  2. 文档化响应格式规范
  3. 添加响应格式验证测试

#### 12. [源代码] [中严重] [Redis 队列缺少 idempotency 索引支持]
- **文件/路径**: `src/platform/five-plane-execution/queue/redis-queue-adapter.ts`
- **问题描述**: 
  - SQLite 有部分唯一索引支持 idempotency
  - Redis 实现使用哈希索引但实现不完整
  - 可能导致重复消息
- **建议修复**: 
  1. 完善 Redis idempotency 实现
  2. 添加唯一索引验证
  3. 确保与 SQLite 行为一致

#### 13. [源代码] [中严重] [请求去重中间件使用内存存储]
- **文件/路径**: `src/platform/five-plane-interface/api/middleware/request-deduplication.ts`
- **问题描述**: 
  - 纯内存滑动窗口（Map<DeduplicationKey, DeduplicationEntry[]>）
  - 无外部存储
  - 多实例部署不共享
  - 重启后去重状态丢失
- **建议修复**: 
  1. 使用 Redis 替代内存存储
  2. 支持分布式去重
  3. 持久化去重状态

#### 14. [源代码] [中严重] [Cache 无 stampede 保护]
- **文件/路径**: 多个 cache 实现
- **问题描述**: 
  - MemoryCacheStore, ExperienceCacheService 无锁
  - 缓存未命中时可发生 thundering herd
  - 高并发下可能导致数据库过载
- **建议修复**: 
  1. 实现 single-flight 模式
  2. 添加请求排队机制
  3. 使用分布式锁保护缓存更新

#### 15. [源代码] [中严重] [EvidenceService eviction 仅在插入时触发]
- **文件/路径**: `src/platform/five-plane-state-evidence/memory/evidence-service.ts:202`
- **问题描述**: 
  - 清理仅在 record() 调用时触发
  - 空闲时无后台清理
  - 可能导致内存持续增长
- **建议修复**: 
  1. 添加定期后台清理任务
  2. 使用独立的清理线程
  3. 添加内存使用监控

#### 16. [源代码] [低严重] [无连接指标暴露]
- **文件/路径**: WebSocket 服务器
- **问题描述**: 
  - getClientCount() 存在但未通过 HTTP 端点暴露
  - 无 pendingAcks 队列深度指标
  - 无连接建立告警
- **建议修复**: 
  1. 通过 metrics 端点暴露连接指标
  2. 添加 pendingAcks 队列监控
  3. 添加连接数异常告警

#### 17. [源代码] [低严重] [无空闲客户端超时]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts`
- **问题描述**: 
  - 仅在心跳 Sweep 时检查 isAlive
  - 无 per-client 独立于心跳的空闲超时
  - 认证后从不发送消息的客户端只能通过心跳失败检测
- **建议修复**: 
  1. 添加 per-client idle timeout
  2. 独立于心跳间隔
  3. 配置可调整

#### 18. [源代码] [低严重] [订阅限制是 per-client 而非全局]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:244`
- **问题描述**: 
  - MAX_SUBSCRIPTIONS_PER_CLIENT = 100
  - 恶意客户端可订阅 100 个任务
  - 无全局任务订阅者限制
- **建议修复**: 
  1. 添加全局任务订阅者限制
  2. 实现 per-task 订阅者上限
  3. 添加反滥用检测

#### 19. [配置] [低严重] [config/runtime 无版本验证机制]
- **文件/路径**: `config/runtime/*.json`
- **问题描述**: 
  - 配置有 "version": "v4.3"
  - 但无 schema 版本验证
  - 加载时可能接受不兼容配置
- **建议修复**: 
  1. 添加 JSON Schema 验证
  2. 实现版本兼容性检查
  3. 启动时验证配置完整性

#### 20. [源代码] [低严重] [ImprovementCandidateRegistry splice 是 O(n)]
- **文件/路径**: `src/platform/five-plane-orchestration/improve/improvement-candidate-registry.ts:243`
- **问题描述**: 
  - accessOrder 数组的 splice 操作是 O(n)
  - 高频访问时可能性能问题
- **建议修复**: 
  1. 使用 LinkedList 替代数组
  2. 或使用 Map 维护访问顺序
  3. 性能测试验证

### 总结

本次补充Review（第十三轮 - 缓存会话与实时通信Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. CORS allowedMethods 缺少 PUT/PATCH/DELETE（浏览器请求会失败）
2. Mission Routes 错误响应包装不当
3. WebSocketBridge 无最大连接数限制（DoS风险）
4. pendingAcks 断开连接时未清理（内存泄漏）
5. 硬编码回退密钥（安全漏洞）
6. config/runtime/test.json 缺少超时配置
7. prod.json maxConcurrentTasks=1 限制过严
8. 多个服务存在无界 Map（内存泄漏风险）

**中优先级**:
1. taskEventHistory Map 永不清理
2. 安全配置 drift（remoteWorkerRegistration 缺失）
3. OpenAPI 端点响应格式不一致
4. Redis 队列 idempotency 实现不完整
5. 请求去重中间件使用内存存储（多实例不共享）
6. Cache 无 stampede 保护
7. EvidenceService eviction 仅在插入时触发

**低优先级**:
1. 无连接指标暴露
2. 无空闲客户端超时
3. 订阅限制是 per-client 而非全局
4. config/runtime 无版本验证机制
5. ImprovementCandidateRegistry splice O(n)

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 64 | 74 | 35 | 173 |
| 测试 | 8 | 14 | 5 | 27 |
| 配置 | 10 | 38 | 24 | 72 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 7 | 3 | 12 |
| UI | 0 | 3 | 3 | 6 |
| 部署 | 1 | 10 | 6 | 17 |
| **合计** | **99** | **161** | **79** | **339** |

### 优先修复建议

**立即处理 (本周内)**:
1. 修复 CORS allowedMethods 添加 PUT/PATCH/DELETE
2. 修复 Mission Routes 错误响应格式
3. 为 WebSocketBridge 添加最大连接数限制
4. 清理 pendingAcks 和 taskEventHistory
5. 移除硬编码回退密钥
6. 修复 config/runtime/test.json 配置
7. 评估并调整 prod 并发限制

**短期内处理 (本月内)**:
1. 统一安全配置（remoteWorkerRegistration）
2. 实现无界 Map 的 LRU/TTL 驱逐
3. 统一 API 响应信封格式
4. 完善 Redis idempotency 实现
5. 添加分布式去重中间件
6. 实现 cache stampede 保护

**长期规划**:
1. 建立完整的连接管理和监控
2. 实现后台清理任务框架
3. 添加配置 schema 验证
4. 优化高频繁操作的数据结构
5. 建立内存使用基线和告警

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十五轮 - 构建测试与类型系统Review）

### 发现的新问题

#### 1. [构建] [高严重] [无 TypeScript 增量编译缓存]
- **文件/路径**: `tsconfig.json`
- **问题描述**: 
  - 无 `incremental: true` 配置
  - 无 `tsbuildinfo` 文件
  - 每次构建是完整重建（9.6秒）
  - TypeScript 无法使用增量编译
- **建议修复**: 
  1. 在 tsconfig.json 添加 `"incremental": true`
  2. 添加 `"tsbuildinfo": ".tsbuildinfo"` 到 .gitignore
  3. 实现项目引用以支持并行构建

#### 2. [构建] [高严重] [76个 npm scripts 大部分重复构建]
- **文件/路径**: `package.json`
- **问题描述**: 
  - 40+ CLI scripts 都运行 `npm run build && node ...`
  - 每个脚本前都执行完整重建
  - 无法直接运行预构建的 CLI
  - 20+ stable:* 脚本遵循相同模式
- **建议修复**: 
  1. 创建 `build:cli` 脚本构建一次
  2. 让 CLI 脚本使用预构建的 dist 文件
  3. 添加 npm run dev 或类似的无需重建的执行方式

#### 3. [源代码] [高严重] [168处 any 类型使用]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - `stable-runner-factory.ts`: `opts: any` -> `any`
  - `billing-service-async.ts`: `input: any` 方法签名
  - 重试/超时工具使用 `(...args: any[]) => Promise<any>`
  - 绕过类型安全
- **建议修复**: 
  1. 使用泛型替代 any
  2. 替换为具体的输入类型
  3. 添加 ESLint 规则禁止 any

#### 4. [源代码] [高严重] [38处 @ts-ignore 指令]
- **文件/路径**: 多个文件
- **问题描述**: 
  - 14个 stability 相关文件有 `@ts-ignore ExecutionRecord type mismatch`
  - 5个 harness-sdk 文件有 `Partial<HarnessRun> doesn't have all required properties`
  - 2个文件有 `exactOptionalPropertyTypes` 问题
- **建议修复**: 
  1. 修复 ExecutionRecord 类型不匹配
  2. 完成 HarnessRun 类型的必需属性
  3. 移除 @ts-ignore 使用

#### 5. [源代码] [高严重] [ExecutionRecord 类型不匹配]
- **文件/路径**: `src/platform/stability/stable-evidence-bundle-support.ts:783` 等14个文件
- **问题描述**: 
  - 插入 execution record 时 TypeScript 拒绝类型
  - `ExecutionRecord` 接口要求新字段但 insert 对象缺失
  - 或 store 的 insert 方法接受不同类型
- **建议修复**: 
  1. 对齐 ExecutionRecord 接口和 store 类型
  2. 添加缺失字段到 insert 对象
  3. 或调整 store 的 insert 方法类型签名

#### 6. [源代码] [中严重] [30+处 as unknown as 双类型转换]
- **文件/路径**: `division-loader.ts`, `domain-baseline-catalog.ts`, `harness-sdk`
- **问题描述**: 
  - 完全绕过类型安全
  - 通常是类型架构问题的信号
- **建议修复**: 
  1. 修复上游类型问题
  2. 使用更具体的类型断言
  3. 重构类型层次结构

#### 7. [构建] [中严重] [rimraf  extraneous - 未在 package.json 声明]
- **文件/路径**: `node_modules/rimraf`
- **问题描述**: 
  - rimraf@6.1.3 存在于 node_modules 但未在 package.json 声明
  - 可能导致依赖问题
- **建议修复**: 
  1. 添加到 dependencies 或 devDependencies
  2. 或移除并使用其他清理方式

#### 8. [测试] [高严重] [178处直接 process.env 变更无抽象]
- **文件/路径**: `tests/unit/domains/registry/plugin-spi-registry.test.ts:640-687`
- **问题描述**: 
  - 直接读取/写入 process.env
  - 在 beforeEach/afterEach 中手动保存/恢复
  - 无 helper 抽象
- **建议修复**: 
  1. 创建 env helper 函数
  2. 统一保存和恢复模式
  3. 添加测试隔离验证

#### 9. [测试] [高严重] [21,998处 unlinkSync 调用无集中清理]
- **文件/路径**: 多个测试文件
- **问题描述**: 
  - 每个测试重新实现清理逻辑
  - 无集中清理 utility
  - 可能导致 EBUSY (Windows) 或并发访问问题
- **建议修复**: 
  1. 创建集中的测试清理 utility
  2. 使用 afterEach 确保清理
  3. 考虑使用临时目录

#### 10. [测试] [中严重] [无统一 mocking 框架]
- **文件/路径**: `tests/` 多个目录
- **问题描述**: 
  - 10,579 处 vi.fn, jest.mock, sinon 等调用
  - 无统一的 mocking 框架可见
  - 模式不一致
- **建议修复**: 
  1. 建立统一的 mock factory
  2. 标准化 mock 模式
  3. 添加 mocking 最佳实践文档

#### 11. [测试] [中严重] [Singleton 状态重置未标准化]
- **文件/路径**: `tests/unit/platform/five-plane-execution/dispatcher.test.ts`
- **问题描述**: 
  - 测试通过特定函数重置 singletons
  - 每个测试重复自己的 reset 逻辑
  - 无统一的 resetAllSingletons()
- **建议修复**: 
  1. 创建统一的 singleton reset 机制
  2. 在 afterEach 中自动调用
  3. 文档化 singleton 重置要求

#### 12. [测试] [低严重] [无分支覆盖率要求]
- **文件/路径**: `.c8rc.json`
- **问题描述**: 
  - `"100": false` - 无覆盖率要求
  - 只测量源代码，不测量测试文件
- **建议修复**: 
  1. 添加分支覆盖率阈值
  2. 在 CI 中强制执行覆盖率 gate
  3. 关注关键代码路径

#### 13. [网络] [高严重] [ScopedExternalAccessSandbox performHttpRequest 无超时]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/scoped-external-access-sandbox.ts:289`
- **问题描述**: 
  - fetch 调用无 timeout 参数
  - 挂起的请求可能永久阻塞
- **建议修复**: 
  1. 添加 AbortController timeout
  2. 使用标准的超时模式
  3. 记录超时配置

#### 14. [网络] [高严重] [OIDC 服务 fetch 调用无超时]
- **文件/路径**: `src/org-governance/sso-scim/oidc/oidc-service.ts:565,272`
- **问题描述**: 
  - OIDC token 交换无 retry logic
  - 无 timeout/abort signal
  - 认证流程可能挂起
- **建议修复**: 
  1. 添加 fetch timeout
  2. 添加 retry logic
  3. 使用标准的超时配置

#### 15. [网络] [高严重] [plugin-runtime-child 替换 globalThis.fetch 为 no-op]
- **文件/路径**: `src/domains/registry/plugin-runtime-child.ts:112`
- **问题描述**: 
  ```typescript
  globalThis.fetch = (async () => {}) as unknown as typeof globalThis.fetch;
  ```
  - 插件上下文中完全替换 fetch 为空函数
  - 插件的所有出站 HTTP 请求都会失败
  - 看起来是有意为之但可能造成问题
- **建议修复**: 
  1. 确认这是有意行为
  2. 文档化 fetch 替换行为
  3. 确保不会意外影响其他代码

#### 16. [网络] [中严重] [无显式 http.Agent 配置]
- **文件/路径**: 全局
- **问题描述**: 
  - 无显式的 http.Agent/https.Agent 配置
  - 依赖 Node.js 默认连接池
  - 无 maxSockets 或 maxConnections 配置
  - 高吞吐场景可能出问题
- **建议修复**: 
  1. 添加 http Agent 配置
  2. 文档化连接池大小
  3. 监控连接使用情况

#### 17. [序列化] [中严重] [合同版本硬编码为字符串字面量]
- **文件/路径**: `src/platform/contracts/inter-plane-contract-gateway.ts:184`
- **问题描述**: 
  - `schemaVersion: "v4.3"` 硬编码
  - 难以追踪合同版本演变
- **建议修复**: 
  1. 提取到类型化常量或 enum
  2. 集中管理合同版本
  3. 添加版本兼容性检查

#### 18. [序列化] [低严重] [Timestamp = string 丢失精度]
- **文件/路径**: `src/platform/contracts/types/domain/primitives.ts:10`
- **问题描述**: 
  - `type Timestamp = string` 无类型精度
  - ISO 8601 字符串无时区信息
- **建议修复**: 
  1. 使用 Date 对象或 branded string 类型
  2. 明确时区处理约定
  3. 文档化时间戳语义

#### 19. [源代码] [中严重] [29处非空断言 !.]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - 使用 `!.` 断言非空
  - 可能导致运行时错误
- **建议修复**: 
  1. 使用可选链 `?.` 或空值合并 `??`
  2. 添加适当的 null 检查
  3. 减少非空断言使用

#### 20. [API] [中严重] [API 路由 schema 版本硬编码]
- **文件/路径**: `src/platform/five-plane-interface/api/federation-routing-service.ts`
- **问题描述**: 
  - Zod schema 验证存在
  - 但 schema 版本未与 contract 版本同步
- **建议修复**: 
  1. 从 contract 版本派生 schema 版本
  2. 添加版本一致性检查
  3. 文档化版本关系

### 总结

本次补充Review（第十五轮 - 构建测试与类型系统Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. 无 TypeScript 增量编译缓存（每次完整重建9.6秒）
2. 76个 npm scripts 大部分重复构建
3. 168处 any 类型使用（绕过类型安全）
4. 38处 @ts-ignore 指令
5. ExecutionRecord 类型不匹配（14个文件）
6. 178处直接 process.env 变更无抽象
7. ScopedExternalAccessSandbox performHttpRequest 无超时
8. OIDC 服务 fetch 调用无超时
9. plugin-runtime-child 替换 globalThis.fetch 为 no-op

**中优先级**:
1. 30+处 as unknown as 双类型转换
2. rimraf extraneous 未声明
3. 21,998处 unlinkSync 无集中清理
4. 无统一 mocking 框架
5. Singleton 状态重置未标准化
6. 无显式 http.Agent 配置
7. 合同版本硬编码为字符串
8. Timestamp = string 丢失精度
9. 非空断言使用

**低优先级**:
1. 无分支覆盖率要求
2. API 路由 schema 版本未同步

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 72 | 87 | 39 | 198 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **109** | **182** | **85** | **376** |

### 优先修复建议

**立即处理 (本周内)**:
1. 添加 TypeScript 增量编译 (`incremental: true`)
2. 重构 npm scripts 避免重复构建
3. 减少 any 类型使用到 <50
4. 移除所有 @ts-ignore 指令
5. 修复 ExecutionRecord 类型不匹配
6. 添加 fetch 超时到 OIDC 和 sandbox

**短期内处理 (本月内)**:
1. 创建集中的测试清理 utility
2. 建立统一的 mocking 框架
3. 标准化 singleton 重置机制
4. 添加 http.Agent 连接池配置
5. 提取合同版本到类型化常量

**长期规划**:
1. 实现完整的类型安全（无 any）
2. 建立测试基础设施标准
3. 实现构建缓存优化
4. 完善网络层超时和重试
5. 建立覆盖率门禁标准

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十四轮 - 日志事件与工作流Review）

### 发现的新问题

#### 1. [可观测性] [高严重] [StructuredLogEntry 无 requestId 字段]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - StructuredLogEntry 接口无 requestId 字段
  - 只有 correlationId，它回退到 traceId
  - 无法独立追踪单个 HTTP 请求
- **建议修复**: 
  1. 在 StructuredLogEntry 添加 requestId 字段
  2. 在 HTTP 中间件中生成和注入 requestId
  3. 确保 requestId 流经所有日志调用

#### 2. [可观测性] [高严重] [StructuredLogger data 字段无自动清洗]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - 如果开发者传递 `{ password: "..." }` 给 logger.info，密码会被记录
  - 无字段级脱敏机制
  - 依赖开发者手动排除敏感字段
- **建议修复**: 
  1. 添加 OBSERVE_OUTPUT_BLACKLIST 类似的黑名单
  2. 实现 redact/mask/sanitize 工具函数
  3. 在日志传输前自动清洗敏感字段

#### 3. [可观测性] [中严重] [日志中敏感数据无 PII 处理]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - 无 PII 重写工具
  - 开发者必须手动避免记录敏感数据
  - 存在意外记录敏感信息的风险
- **建议修复**: 
  1. 添加 PII 检测和重写工具
  2. 在文档中明确记录敏感字段列表
  3. 添加测试验证敏感数据不被记录

#### 4. [可观测性] [中严重] [59处 console.* 直接调用]
- **文件/路径**: 多个源文件
- **问题描述**: 
  - 应该使用 StructuredLogger
  - console 调用可能不被日志聚合系统收集
  - 难以追踪和关联
- **建议修复**: 
  1. 替换所有 console.* 为 StructuredLogger
  2. 添加 ESLint 规则禁止 console.*
  3. 保留少量允许的 CLI 输出

#### 5. [可观测性] [中严重] [部分 logger.warn 调用缺少结构化数据]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:122`
- **问题描述**: 
  ```typescript
  logger.warn("WebSocket connection rejected: missing subprotocol token")
  ```
  - 只有消息，无 data 对象
  - 难以关联和查询
- **建议修复**: 
  1. 添加 `{ data: { ... } }` 对象
  2. 确保所有日志包含上下文数据
  3. 添加日志审查工具

#### 6. [源代码] [高严重] [Harness while(true) 循环无硬性迭代上限]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:1442`
- **问题描述**: 
  ```typescript
  while (true) {
    // ...
    // 依赖 budget gate 和 guardrail vibration 退出
    // 如果两者都失败，循环永久继续
  }
  ```
  - 依赖 budget gate 和 guardrail vibration 退出
  - 无硬性迭代次数限制
  - 可能导致无限循环
- **建议修复**: 
  1. 添加硬性 maxIterations 上限
  2. 在达到上限时强制退出
  3. 记录详细的迭代统计用于调试

#### 7. [源代码] [高严重] [Oapeflir while(true) 循环可能无限重规划]
- **文件/路径**: `src/platform/five-plane-orchestration/oapeflir/oapeflir-loop-service.ts:355`
- **问题描述**: 
  - 如果 loopReplanDecision.shouldReplan 永不变为 false
  - 重规划永远无法减少计划错误
  - 可能导致无限循环
- **建议修复**: 
  1. 添加重规划次数上限
  2. 在达到上限时强制接受当前结果
  3. 添加重规划质量衰减检测

#### 8. [源代码] [中严重] [Recovery 流程允许终态转换到 paused]
- **文件/路径**: `src/platform/five-plane-execution/runtime-state-machine.ts:105-108`
- **问题描述**: 
  - completed, failed, cancelled, aborted 可转换到 paused 进行恢复
  - 创建恢复路径可能重新进入执行
  - 需要验证此行为的正确性
- **建议修复**: 
  1. 审查 recovery 转换的合法性
  2. 确保不会导致状态不一致
  3. 添加转换前置条件验证

#### 9. [国际化] [高严重] [API 错误消息硬编码英文]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/api-error.ts`
- **问题描述**: 
  ```typescript
  return new ApiError(404, "api.task_not_found", "Task not found.");
  ```
  - 错误消息硬编码英文
  - 无后端 i18n 机制
  - 非英文客户端收到不本地化的错误
- **建议修复**: 
  1. 使用错误码而非硬编码消息
  2. 客户端根据错误码本地化
  3. 或在后端添加 i18n 支持

#### 10. [国际化] [中严重] [UI 硬编码字符串未翻译]
- **文件/路径**: `ui/packages/features/conversation/src/web/index.tsx`
- **问题描述**: 
  - "Build Plan", "Execute", "Messages" 等按钮标签硬编码
  - 不使用 i18n 系统
- **建议修复**: 
  1. 使用 `translateFeatureCopy` 替换硬编码
  2. 确保所有用户可见文本通过 i18n
  3. 添加硬编码字符串检测

#### 11. [国际化] [中严重] [Arabic 目录不完整]
- **文件/路径**: `ui/packages/shared/i18n/src/catalogs/ar-SA.ts`
- **问题描述**: 
  - ar-SA 只有约18条消息
  - en-US 和 zh-CN 有约117条
  - 缺失的 key 返回 key 本身
- **建议修复**: 
  1. 补充 Arabic 翻译
  2. 确保所有 key 都有翻译
  3. 添加翻译完整性测试

#### 12. [国际化] [低严重] [translateFeatureCopy 无回退默认值]
- **文件/路径**: `ui/packages/shared/i18n/`
- **问题描述**: 
  - 如果 feature ID 不在 catalog 中
  - 返回 undefined title/summary
  - 无合理回退
- **建议修复**: 
  1. 添加回退到 key 或默认字符串
  2. 或使用 feature ID 作为显示名
  3. 确保 UI 不显示 undefined

#### 13. [运维] [高严重] [健康检查无 readiness/liveness 区分]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts`
- **问题描述**: 
  - /healthz, /v1/healthz, /health 返回相同报告
  - 无 readiness probe（可处理流量？）
  - 无 liveness probe（系统存活？）
  - canAcceptTraffic() 未通过 HTTP 暴露
- **建议修复**: 
  1. 添加 /ready 和 /live 端点
  2. Readiness 检查依赖项就绪
  3. Liveness 检查进程健康

#### 14. [运维] [高严重] [shutdown 期间健康检查存在竞态]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts`
- **问题描述**: 
  - getHealthReportAsync() 异步检查 dbWritable
  - shutdown 期间数据库连接可能关闭
  - 健康检查仍在传入
- **建议修复**: 
  1. 在 shutdown 时返回 unhealthy
  2. 添加 shutdown 状态标志
  3. 停止接受新的健康检查请求

#### 15. [运维] [高严重] [LeaderElectionService 未与 GracefulShutdown 集成]
- **文件/路径**: `src/platform/five-plane-execution/ha/leader-election-service.ts`
- **问题描述**: 
  - LeaderElectionService.stop() 处理优雅退位
  - 但未在全局 graceful shutdown 中注册
  - 需显式注册为 shutdown handler
- **建议修复**: 
  1. 将 LeaderElectionService 添加到 shutdown handlers
  2. 确保领导者在 shutdown 时让位
  3. 验证关闭顺序正确

#### 16. [运维] [中严重] [addHandler() 无显式排序约定]
- **文件/路径**: `src/platform/five-plane-execution/startup/graceful-shutdown.ts`
- **问题描述**: 
  - 虽然 handlers 逆序执行
  - 但无显式接口声明 handler 依赖关系
  - 关键 handlers（如关闭数据库连接）应最后运行
- **建议修复**: 
  1. 添加优先级或阶段参数
  2. 文档化关键 handler 顺序要求
  3. 添加顺序验证测试

#### 17. [运维] [中严重] [unref'd timers 可能不触发]
- **文件/路径**: `src/platform/five-plane-execution/startup/process-error-handlers.ts:106`
- **问题描述**: 
  - `setTimeout(...).unref()` 不保持事件循环活跃
  - 如果主线程空闲，强制退出可能不触发
- **建议修复**: 
  1. 移除 .unref() 除非确实需要
  2. 或确保有其他方式保持事件循环
  3. 添加超时触发验证测试

#### 18. [运维] [中严重] [StartupConsistencyChecker 阻塞流量但不暴露状态]
- **文件/路径**: `src/platform/five-plane-startup-plan.ts`
- **问题描述**: 
  - canAcceptTraffic() 在 P0 问题存在时返回 false
  - 但 _trafficBlocked 是内部状态
  - 负载均衡器访问 /healthz 仍会得到 "ok"
- **建议修复**: 
  1. 在健康报告中暴露 trafficBlocked 状态
  2. 或添加单独的健康端点指示就绪状态
  3. 确保负载均衡器在阻塞时不路由流量

#### 19. [事件系统] [中严重] [TypedEventBus vs EventEmitter 混用]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - 项目同时使用 TypedEventBus 和原生 EventEmitter
  - TypedEventBus 应该是标准
  - 混用可能导致类型安全问题
- **建议修复**: 
  1. 创建迁移计划统一使用 TypedEventBus
  2. 添加 ESLint 规则禁止原生 EventEmitter
  3. 在 CI 中检测混用情况

#### 20. [源代码] [低严重] [无事件排序保证]
- **文件/路径**: `src/platform/five-plane-state-evidence/events/`
- **问题描述**: 
  - 无明确的事件排序保证文档
  - 事件处理可能乱序
  - 依赖方需要处理乱序
- **建议修复**: 
  1. 文档化事件排序语义
  2. 如果需要排序，实现序列号机制
  3. 在事件处理中考虑乱序情况

### 总结

本次补充Review（第十四轮 - 日志事件与工作流Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. StructuredLogEntry 无 requestId 字段
2. StructuredLogger data 字段无自动清洗（敏感数据风险）
3. Harness while(true) 循环无硬性迭代上限
4. Oapeflir while(true) 循环可能无限重规划
5. API 错误消息硬编码英文（无 i18n）
6. 健康检查无 readiness/liveness 区分
7. shutdown 期间健康检查存在竞态
8. LeaderElectionService 未与 GracefulShutdown 集成

**中优先级**:
1. 日志中敏感数据无 PII 处理
2. 59处 console.* 直接调用
3. 部分 logger.warn 调用缺少结构化数据
4. Recovery 流程允许终态转换到 paused
5. UI 硬编码字符串未翻译
6. Arabic 目录不完整
7. addHandler() 无显式排序约定
8. unref'd timers 可能不触发
9. StartupConsistencyChecker 阻塞流量但不暴露状态
10. TypedEventBus vs EventEmitter 混用

**低优先级**:
1. translateFeatureCopy 无回退默认值
2. 无事件排序保证

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 68 | 80 | 37 | 185 |
| 测试 | 8 | 14 | 5 | 27 |
| 配置 | 10 | 38 | 24 | 72 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **103** | **171** | **82** | **356** |

### 优先修复建议

**立即处理 (本周内)**:
1. 在 StructuredLogEntry 添加 requestId 字段
2. 实现日志敏感字段自动清洗
3. 为工作流循环添加硬性迭代上限
4. 添加 readiness/liveness 健康检查端点
5. 将 LeaderElectionService 集成到 GracefulShutdown
6. 实现 API 错误消息 i18n 支持

**短期内处理 (本月内)**:
1. 替换所有 console.* 为 StructuredLogger
2. 补充 Arabic i18n 目录
3. 修复 UI 硬编码字符串
4. 统一 TypedEventBus 替代 EventEmitter
5. 解决 shutdown 竞态条件
6. 添加事件排序保证文档

**长期规划**:
1. 建立完整的 PII 处理框架
2. 实现日志完整性验证
3. 建立工作流安全循环检测
4. 完善健康检查和就绪探测
5. 建立国际化完整测试

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十五轮 - 构建测试与类型系统Review）

### 发现的新问题

#### 1. [构建] [高严重] [无 TypeScript 增量编译缓存]
- **文件/路径**: `tsconfig.json`
- **问题描述**: 
  - 无 `incremental: true` 配置
  - 无 `tsbuildinfo` 文件
  - 每次构建是完整重建（9.6秒）
  - TypeScript 无法使用增量编译
- **建议修复**: 
  1. 在 tsconfig.json 添加 `"incremental": true`
  2. 添加 `"tsbuildinfo": ".tsbuildinfo"` 到 .gitignore
  3. 实现项目引用以支持并行构建

#### 2. [构建] [高严重] [76个 npm scripts 大部分重复构建]
- **文件/路径**: `package.json`
- **问题描述**: 
  - 40+ CLI scripts 都运行 `npm run build && node ...`
  - 每个脚本前都执行完整重建
  - 无法直接运行预构建的 CLI
  - 20+ stable:* 脚本遵循相同模式
- **建议修复**: 
  1. 创建 `build:cli` 脚本构建一次
  2. 让 CLI 脚本使用预构建的 dist 文件
  3. 添加 npm run dev 或类似的无需重建的执行方式

#### 3. [源代码] [高严重] [168处 any 类型使用]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - `stable-runner-factory.ts`: `opts: any` -> `any`
  - `billing-service-async.ts`: `input: any` 方法签名
  - 重试/超时工具使用 `(...args: any[]) => Promise<any>`
  - 绕过类型安全
- **建议修复**: 
  1. 使用泛型替代 any
  2. 替换为具体的输入类型
  3. 添加 ESLint 规则禁止 any

#### 4. [源代码] [高严重] [38处 @ts-ignore 指令]
- **文件/路径**: 多个文件
- **问题描述**: 
  - 14个 stability 相关文件有 `@ts-ignore ExecutionRecord type mismatch`
  - 5个 harness-sdk 文件有 `Partial<HarnessRun> doesn't have all required properties`
  - 2个文件有 `exactOptionalPropertyTypes` 问题
- **建议修复**: 
  1. 修复 ExecutionRecord 类型不匹配
  2. 完成 HarnessRun 类型的必需属性
  3. 移除 @ts-ignore 使用

#### 5. [源代码] [高严重] [ExecutionRecord 类型不匹配]
- **文件/路径**: `src/platform/stability/stable-evidence-bundle-support.ts:783` 等14个文件
- **问题描述**: 
  - 插入 execution record 时 TypeScript 拒绝类型
  - `ExecutionRecord` 接口要求新字段但 insert 对象缺失
  - 或 store 的 insert 方法接受不同类型
- **建议修复**: 
  1. 对齐 ExecutionRecord 接口和 store 类型
  2. 添加缺失字段到 insert 对象
  3. 或调整 store 的 insert 方法类型签名

#### 6. [源代码] [中严重] [30+处 as unknown as 双类型转换]
- **文件/路径**: `division-loader.ts`, `domain-baseline-catalog.ts`, `harness-sdk`
- **问题描述**: 
  - 完全绕过类型安全
  - 通常是类型架构问题的信号
- **建议修复**: 
  1. 修复上游类型问题
  2. 使用更具体的类型断言
  3. 重构类型层次结构

#### 7. [构建] [中严重] [rimraf  extraneous - 未在 package.json 声明]
- **文件/路径**: `node_modules/rimraf`
- **问题描述**: 
  - rimraf@6.1.3 存在于 node_modules 但未在 package.json 声明
  - 可能导致依赖问题
- **建议修复**: 
  1. 添加到 dependencies 或 devDependencies
  2. 或移除并使用其他清理方式

#### 8. [测试] [高严重] [178处直接 process.env 变更无抽象]
- **文件/路径**: `tests/unit/domains/registry/plugin-spi-registry.test.ts:640-687`
- **问题描述**: 
  - 直接读取/写入 process.env
  - 在 beforeEach/afterEach 中手动保存/恢复
  - 无 helper 抽象
- **建议修复**: 
  1. 创建 env helper 函数
  2. 统一保存和恢复模式
  3. 添加测试隔离验证

#### 9. [测试] [高严重] [21,998处 unlinkSync 调用无集中清理]
- **文件/路径**: 多个测试文件
- **问题描述**: 
  - 每个测试重新实现清理逻辑
  - 无集中清理 utility
  - 可能导致 EBUSY (Windows) 或并发访问问题
- **建议修复**: 
  1. 创建集中的测试清理 utility
  2. 使用 afterEach 确保清理
  3. 考虑使用临时目录

#### 10. [测试] [中严重] [无统一 mocking 框架]
- **文件/路径**: `tests/` 多个目录
- **问题描述**: 
  - 10,579 处 vi.fn, jest.mock, sinon 等调用
  - 无统一的 mocking 框架可见
  - 模式不一致
- **建议修复**: 
  1. 建立统一的 mock factory
  2. 标准化 mock 模式
  3. 添加 mocking 最佳实践文档

#### 11. [测试] [中严重] [Singleton 状态重置未标准化]
- **文件/路径**: `tests/unit/platform/five-plane-execution/dispatcher.test.ts`
- **问题描述**: 
  - 测试通过特定函数重置 singletons
  - 每个测试重复自己的 reset 逻辑
  - 无统一的 resetAllSingletons()
- **建议修复**: 
  1. 创建统一的 singleton reset 机制
  2. 在 afterEach 中自动调用
  3. 文档化 singleton 重置要求

#### 12. [测试] [低严重] [无分支覆盖率要求]
- **文件/路径**: `.c8rc.json`
- **问题描述**: 
  - `"100": false` - 无覆盖率要求
  - 只测量源代码，不测量测试文件
- **建议修复**: 
  1. 添加分支覆盖率阈值
  2. 在 CI 中强制执行覆盖率 gate
  3. 关注关键代码路径

#### 13. [网络] [高严重] [ScopedExternalAccessSandbox performHttpRequest 无超时]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/scoped-external-access-sandbox.ts:289`
- **问题描述**: 
  - fetch 调用无 timeout 参数
  - 挂起的请求可能永久阻塞
- **建议修复**: 
  1. 添加 AbortController timeout
  2. 使用标准的超时模式
  3. 记录超时配置

#### 14. [网络] [高严重] [OIDC 服务 fetch 调用无超时]
- **文件/路径**: `src/org-governance/sso-scim/oidc/oidc-service.ts:565,272`
- **问题描述**: 
  - OIDC token 交换无 retry logic
  - 无 timeout/abort signal
  - 认证流程可能挂起
- **建议修复**: 
  1. 添加 fetch timeout
  2. 添加 retry logic
  3. 使用标准的超时配置

#### 15. [网络] [高严重] [plugin-runtime-child 替换 globalThis.fetch 为 no-op]
- **文件/路径**: `src/domains/registry/plugin-runtime-child.ts:112`
- **问题描述**: 
  ```typescript
  globalThis.fetch = (async () => {}) as unknown as typeof globalThis.fetch;
  ```
  - 插件上下文中完全替换 fetch 为空函数
  - 插件的所有出站 HTTP 请求都会失败
  - 看起来是有意为之但可能造成问题
- **建议修复**: 
  1. 确认这是有意行为
  2. 文档化 fetch 替换行为
  3. 确保不会意外影响其他代码

#### 16. [网络] [中严重] [无显式 http.Agent 配置]
- **文件/路径**: 全局
- **问题描述**: 
  - 无显式的 http.Agent/https.Agent 配置
  - 依赖 Node.js 默认连接池
  - 无 maxSockets 或 maxConnections 配置
  - 高吞吐场景可能出问题
- **建议修复**: 
  1. 添加 http Agent 配置
  2. 文档化连接池大小
  3. 监控连接使用情况

#### 17. [序列化] [中严重] [合同版本硬编码为字符串字面量]
- **文件/路径**: `src/platform/contracts/inter-plane-contract-gateway.ts:184`
- **问题描述**: 
  - `schemaVersion: "v4.3"` 硬编码
  - 难以追踪合同版本演变
- **建议修复**: 
  1. 提取到类型化常量或 enum
  2. 集中管理合同版本
  3. 添加版本兼容性检查

#### 18. [序列化] [低严重] [Timestamp = string 丢失精度]
- **文件/路径**: `src/platform/contracts/types/domain/primitives.ts:10`
- **问题描述**: 
  - `type Timestamp = string` 无类型精度
  - ISO 8601 字符串无时区信息
- **建议修复**: 
  1. 使用 Date 对象或 branded string 类型
  2. 明确时区处理约定
  3. 文档化时间戳语义

#### 19. [源代码] [中严重] [29处非空断言 !.]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - 使用 `!.` 断言非空
  - 可能导致运行时错误
- **建议修复**: 
  1. 使用可选链 `?.` 或空值合并 `??`
  2. 添加适当的 null 检查
  3. 减少非空断言使用

#### 20. [API] [中严重] [API 路由 schema 版本硬编码]
- **文件/路径**: `src/platform/five-plane-interface/api/federation-routing-service.ts`
- **问题描述**: 
  - Zod schema 验证存在
  - 但 schema 版本未与 contract 版本同步
- **建议修复**: 
  1. 从 contract 版本派生 schema 版本
  2. 添加版本一致性检查
  3. 文档化版本关系

### 总结

本次补充Review（第十五轮 - 构建测试与类型系统Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. 无 TypeScript 增量编译缓存（每次完整重建9.6秒）
2. 76个 npm scripts 大部分重复构建
3. 168处 any 类型使用（绕过类型安全）
4. 38处 @ts-ignore 指令
5. ExecutionRecord 类型不匹配（14个文件）
6. 178处直接 process.env 变更无抽象
7. ScopedExternalAccessSandbox performHttpRequest 无超时
8. OIDC 服务 fetch 调用无超时
9. plugin-runtime-child 替换 globalThis.fetch 为 no-op

**中优先级**:
1. 30+处 as unknown as 双类型转换
2. rimraf extraneous 未声明
3. 21,998处 unlinkSync 无集中清理
4. 无统一 mocking 框架
5. Singleton 状态重置未标准化
6. 无显式 http.Agent 配置
7. 合同版本硬编码为字符串
8. Timestamp = string 丢失精度
9. 非空断言使用

**低优先级**:
1. 无分支覆盖率要求
2. API 路由 schema 版本未同步

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 72 | 87 | 39 | 198 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **109** | **182** | **85** | **376** |

### 优先修复建议

**立即处理 (本周内)**:
1. 添加 TypeScript 增量编译 (`incremental: true`)
2. 重构 npm scripts 避免重复构建
3. 减少 any 类型使用到 <50
4. 移除所有 @ts-ignore 指令
5. 修复 ExecutionRecord 类型不匹配
6. 添加 fetch 超时到 OIDC 和 sandbox

**短期内处理 (本月内)**:
1. 创建集中的测试清理 utility
2. 建立统一的 mocking 框架
3. 标准化 singleton 重置机制
4. 添加 http.Agent 连接池配置
5. 提取合同版本到类型化常量

**长期规划**:
1. 实现完整的类型安全（无 any）
2. 建立测试基础设施标准
3. 实现构建缓存优化
4. 完善网络层超时和重试
5. 建立覆盖率门禁标准

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十二轮 - 错误处理与多租户安全Review）

### 发现的新问题

#### 1. [安全] [严重] [listQuotas() 返回所有租户配额 - 信息泄露]
- **文件/路径**: `src/platform/five-plane-control-plane/tenant/tenant-execution-isolation-service.ts:222-234`
- **问题描述**: 
  ```typescript
  listQuotas(tenantId?: string): TenantQuota[] {
    if (tenantId) {
      return this.db.connection.prepare(`SELECT * FROM tenant_quotas WHERE tenant_id = ?`).all(tenantId);
    }
    // WITHOUT tenantId: 返回所有租户的配额！
    return this.db.connection.prepare(`SELECT * FROM tenant_quotas`).all();
  }
  ```
  - 当 tenantId 为空时返回所有租户的配额
  - 未经授权的信息泄露
- **建议修复**: 
  1. 要求调用者必须提供 tenantId
  2. 添加权限验证确保只能查看自己的配额
  3. 审计日志记录所有配额访问

#### 2. [安全] [高严重] [assertTaskTenantAccess() 返回404而非403 - 资源存在泄露]
- **文件/路径**: `src/platform/five-plane-interface/api/utils.ts:146-148`
- **问题描述**: 
  ```typescript
  if (resourceTenantId !== principal.tenantId) {
    throw new ApiError(404, notFoundCode, notFoundMessage);  // 应该是403
  }
  ```
  - 跨租户访问返回404而非403
  - 泄露了资源存在但属于其他租户的信息
  - 攻击者可检测有效task ID
- **建议修复**: 
  1. 改为返回403 Forbidden
  2. 不要泄露资源是否存在

#### 3. [安全] [高严重] [crossTenantRequest 标志由调用者控制]
- **文件/路径**: `src/platform/five-plane-interface/org-routing/index.ts:163`
- **问题描述**: 
  - `routeOrgBudget()` 接受 `crossTenantRequest?: boolean` 参数
  - 该标志由调用者设置，非系统派生
  - 攻击者可能操纵此标志绕过跨租户限制
- **建议修复**: 
  1. 标志应由系统状态派生，不接受调用者输入
  2. 添加审计日志追踪跨租户请求
  3. 实现零信任跨租户访问模型

#### 4. [源代码] [高严重] [single-task-happy-path 无重试机制]
- **文件/路径**: `src/platform/five-plane-execution/execution-engine/single-task-happy-path.ts:336-337`
- **问题描述**: 
  - `maxRetries: 0` 和 `retryBackoff: "none"`
  - 瞬态失败无自动重试
  - LLM调用失败时无fallback到备用provider
- **建议修复**: 
  1. 启用非关键执行的重试
  2. 实现LLM provider fallback链（Anthropic → OpenAI → MiniMax）
  3. 使用domain baseline catalog的primary/fallback模型偏好

#### 5. [源代码] [高严重] [LLM调用无熔断器保护]
- **文件/路径**: `src/platform/five-plane-execution/execution-engine/model-call-provider.ts`
- **问题描述**: 
  - 熔断器只存在于 channel-gateway 和 call-governance
  - 直接LLM provider调用无熔断器
  - provider降级时无法快速失败
- **建议修复**: 
  1. 在model-call-provider.ts添加熔断器
  2. 当provider失败率超阈值时快速失败
  3. 自动切换到备用provider

#### 6. [安全] [高严重] [分布式限流器 bypass - 实例间不共享]
- **文件/路径**: `src/platform/five-plane-interface/ingress/distributed-rate-limiter.ts:33`
- **问题描述**: 
  - Redis未配置时使用本地Map
  - 每个实例维护自己的localEntries
  - 攻击者可通过切换实例绕过限流
- **建议修复**: 
  1. 要求Redis配置用于生产
  2. 或使用Redis作为所有实例的共享后端
  3. 检测并警告未配置Redis的实例

#### 7. [配置] [中严重] [Soft quota (log_only) 实际不限制]
- **文件/路径**: `src/platform/five-plane-control-plane/tenant/tenant-execution-isolation-service.ts:448`
- **问题描述**: 
  ```typescript
  allowed: quota.enforcementAction === "log_only",  // log_only时返回true
  ```
  - soft quota只记录日志，不实际阻止
  - 配额形同虚设
- **建议修复**: 
  1. 明确区分"监控模式"和"强制模式"
  2. 对于硬配额必须阻止
  3. 文档化quota类型的实际行为

#### 8. [配置] [中严重] [HTTP层无限流头 - 客户端无法获知限制]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/response-hardening.ts`
- **问题描述**: 
  - 未返回 X-RateLimit-* 头
  - 只在429响应时返回retry-after-ms
  - 客户端无法主动管理请求速率
- **建议修复**: 
  1. 在所有响应添加 X-RateLimit-Limit, X-RateLimit-Remaining, X-RateLimit-Reset
  2. 使用标准头名称
  3. 遵循RFC 6585

#### 9. [配置] [中严重] [HTTP层无限流 - 可选且仅按IP]
- **文件/路径**: `src/platform/five-plane-interface/api/http-api-server.ts:334-351`
- **问题描述**: 
  - 限流仅在 `this.rateLimiter != null` 时生效
  - 限流key为 `${clientIp}:${endpoint}` 仅按IP
  - 无per-tenant/per-principal enforcement
- **建议修复**: 
  1. 默认启用限流
  2. 添加per-tenant限流key
  3. 确保所有环境默认配置限流

#### 10. [安全] [中严重] [processRuleMode 可能未强制执行]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/plugin-runtime-host.ts`
- **问题描述**: 
  - `SandboxPolicy.processRuleMode` 设置为 "allow" 或 "deny"
  - 但未找到实际阻止基于此策略生成进程的实际执行代码
  - `--allow-child-process` 标志仅在 sandboxed_process 隔离模式应用
- **建议修复**: 
  1. 验证processRuleMode实际被强制执行
  2. 确保所有隔离模式正确处理
  3. 添加测试验证进程创建被阻止

#### 11. [安全] [中严重] [容器模板替换未验证]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/plugin-runtime-host.ts:751`
- **问题描述**: 
  ```typescript
  rendered = rendered.split(placeholder).join(value);
  ```
  - 简单字符串替换
  - 虽然validateContainerLaunchPluginId()阻止\0和引号
  - 但模板替换本身未验证
- **建议修复**: 
  1. 添加输入验证防止注入
  2. 使用更安全的模板引擎
  3. 验证所有占位符被替换

#### 12. [安全] [中严重] [Adapter执行绕过沙箱]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/adapter-executor.ts`
- **问题描述**: 
  - Adapter执行REST/grpc/MQ调用不经过ScopedExternalAccessSandbox
  - 有自己的allowedDomains配置但非集中管理
  - 可能发起未限制的出站请求
- **建议修复**: 
  1. Adapter执行经过集中沙箱
  2. 统一external access策略
  3. 添加adapter出站请求审计

#### 13. [安全] [中严重] [null principal.tenantId 时静默放行]
- **文件/路径**: `src/platform/five-plane-interface/api/utils.ts:143-145`
- **问题描述**: 
  ```typescript
  if (principal.tenantId == null) {
    return;  // 静默放行
  }
  ```
  - 如果API网关允许无principal认证的请求通过
  - 可能授予跨租户访问权限
- **建议修复**: 
  1. 要求所有API请求有有效principal
  2. null tenantId应拒绝而非静默放行
  3. 添加审计日志

#### 14. [源代码] [中严重] [无LLM provider fallback链]
- **文件/路径**: `src/platform/five-plane-execution/execution-engine/model-call-provider.ts`
- **问题描述**: 
  - 主provider失败时无自动切换
  - domain-baseline-catalog.ts定义了primary/fallback模型偏好但未使用
  - 第一个provider失败即导致执行失败
- **建议修复**: 
  1. 实现provider fallback链
  2. 使用domain baseline的primary/fallback配置
  3. 按优先级尝试直到成功或全部失败

#### 15. [源代码] [中严重] [超时值硬编码]
- **文件/路径**: 多个文件
- **问题描述**: 
  - Effect buffer有固定超时reject
  - Channel gateway的requestTimeoutMs虽可配置但有max 30s限制
  - 无全局请求超时中间件
- **建议修复**: 
  1. 集中超时配置
  2. per-environment可配置
  3. 添加全局超时中间件

#### 16. [数据库] [中严重] [无down migration - 回滚不支持]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/sqlite/migration-runner.ts:45-47`
- **问题描述**: 
  - `rollbackSupported = false`
  - 每个migration只有 `downSql` 占位符
  - schema migration无法回滚
- **建议修复**: 
  1. 文档化回滚限制
  2. 在变更前创建完整备份
  3. 使用blue-green部署减少回滚需求

#### 17. [数据库] [中严重] [Migration 44特殊处理 - 表创建重复]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/sqlite/sqlite-migration-runtime-part3.ts`
- **问题描述**: 
  - Migration 43创建harness_runs表
  - Migration 44再次创建同名表（不同schema）
  - 使用applyCompatibleColumnMigrationIfKnown特殊处理
- **建议修复**: 
  1. 消除重复表创建
  2. 使用ALTER TABLE而非CREATE TABLE
  3. 简化migration逻辑

#### 18. [配置] [低严重] [Provider限流头未转发给客户端]
- **文件/路径**: `src/platform/model-gateway/provider-registry/base-chat-provider.ts:153`
- **问题描述**: 
  - 系统读取provider的ratelimitResetHeaderNames
  - 但不转发给客户端
  - 客户端无法知道provider限流状态
- **建议修复**: 
  1. 转发Provider的限流头
  2. 或添加应用层限流信息
  3. 帮助客户端优化请求

#### 19. [安全] [低严重] [Browser evaluate 接受任意脚本]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/browser-executor.ts:326`
- **问题描述**: 
  - `evaluate` action接受script参数
  - 在浏览器上下文运行（模拟）
  - 如浏览器上下文未正确沙箱化可能是向量
- **建议修复**: 
  1. 验证浏览器上下文正确沙箱化
  2. 消毒或限制script内容
  3. 记录所有evaluate调用

#### 20. [源代码] [低严重] [无全局错误边界包装器]
- **文件/路径**: `src/platform/five-plane-execution/execution-engine/`
- **问题描述**: 
  - 执行引擎缺少unhandled promise rejection的全局处理
  - single-task-happy-path的LLM fallback失败时错误向上传播
- **建议修复**: 
  1. 在执行引擎添加错误边界包装
  2. 统一错误处理模式
  3. 确保所有错误被捕获和记录

### 总结

本次补充Review（第十二轮 - 错误处理与多租户安全Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. listQuotas()返回所有租户配额（信息泄露）
2. assertTaskTenantAccess()返回404泄露资源存在
3. crossTenantRequest标志可被操纵
4. single-task-happy-path无重试（maxRetries=0）
5. LLM调用无熔断器保护
6. 分布式限流器可被bypass（实例间不共享）

**中优先级**:
1. Soft quota实际不阻止
2. 无X-RateLimit-*响应头
3. HTTP层限流可选且仅按IP
4. processRuleMode可能未强制
5. 容器模板替换未验证
6. Adapter执行绕过沙箱
7. null principal静默放行
8. 无LLM provider fallback链
9. 超时值硬编码
10. 无down migration
11. Migration 44特殊处理

**低优先级**:
1. Provider限流头未转发
2. Browser evaluate接受任意脚本
3. 无全局错误边界

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 60 | 68 | 31 | 159 |
| 测试 | 8 | 14 | 5 | 27 |
| 配置 | 8 | 35 | 21 | 64 |
| 安全 | 12 | 13 | 3 | 28 |
| 文档 | 2 | 7 | 3 | 12 |
| UI | 0 | 3 | 3 | 6 |
| 部署 | 1 | 10 | 6 | 17 |
| **合计** | **91** | **150** | **72** | **313** |

### 优先修复建议

**立即处理 (本周内)**:
1. 修复listQuotas()信息泄露漏洞
2. 修正assertTaskTenantAccess()返回403
3. 移除crossTenantRequest调用者控制
4. 启用执行引擎重试机制
5. 添加LLM provider熔断器

**短期内处理 (本月内)**:
1. 修复分布式限流器bypass问题
2. 添加X-RateLimit-*响应头
3. 启用默认HTTP限流（per-tenant）
4. 实现LLM provider fallback链
5. 验证并强制processRuleMode

**长期规划**:
1. 建立完整的限流和配额体系
2. 实现插件系统安全审计
3. 建立多租户安全测试
4. 完善错误处理和恢复机制
5. 统一超时和熔断配置

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十五轮 - 构建测试与类型系统Review）

### 发现的新问题

#### 1. [构建] [高严重] [无 TypeScript 增量编译缓存]
- **文件/路径**: `tsconfig.json`
- **问题描述**: 
  - 无 `incremental: true` 配置
  - 无 `tsbuildinfo` 文件
  - 每次构建是完整重建（9.6秒）
  - TypeScript 无法使用增量编译
- **建议修复**: 
  1. 在 tsconfig.json 添加 `"incremental": true`
  2. 添加 `"tsbuildinfo": ".tsbuildinfo"` 到 .gitignore
  3. 实现项目引用以支持并行构建

#### 2. [构建] [高严重] [76个 npm scripts 大部分重复构建]
- **文件/路径**: `package.json`
- **问题描述**: 
  - 40+ CLI scripts 都运行 `npm run build && node ...`
  - 每个脚本前都执行完整重建
  - 无法直接运行预构建的 CLI
  - 20+ stable:* 脚本遵循相同模式
- **建议修复**: 
  1. 创建 `build:cli` 脚本构建一次
  2. 让 CLI 脚本使用预构建的 dist 文件
  3. 添加 npm run dev 或类似的无需重建的执行方式

#### 3. [源代码] [高严重] [168处 any 类型使用]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - `stable-runner-factory.ts`: `opts: any` -> `any`
  - `billing-service-async.ts`: `input: any` 方法签名
  - 重试/超时工具使用 `(...args: any[]) => Promise<any>`
  - 绕过类型安全
- **建议修复**: 
  1. 使用泛型替代 any
  2. 替换为具体的输入类型
  3. 添加 ESLint 规则禁止 any

#### 4. [源代码] [高严重] [38处 @ts-ignore 指令]
- **文件/路径**: 多个文件
- **问题描述**: 
  - 14个 stability 相关文件有 `@ts-ignore ExecutionRecord type mismatch`
  - 5个 harness-sdk 文件有 `Partial<HarnessRun> doesn't have all required properties`
  - 2个文件有 `exactOptionalPropertyTypes` 问题
- **建议修复**: 
  1. 修复 ExecutionRecord 类型不匹配
  2. 完成 HarnessRun 类型的必需属性
  3. 移除 @ts-ignore 使用

#### 5. [源代码] [高严重] [ExecutionRecord 类型不匹配]
- **文件/路径**: `src/platform/stability/stable-evidence-bundle-support.ts:783` 等14个文件
- **问题描述**: 
  - 插入 execution record 时 TypeScript 拒绝类型
  - `ExecutionRecord` 接口要求新字段但 insert 对象缺失
  - 或 store 的 insert 方法接受不同类型
- **建议修复**: 
  1. 对齐 ExecutionRecord 接口和 store 类型
  2. 添加缺失字段到 insert 对象
  3. 或调整 store 的 insert 方法类型签名

#### 6. [源代码] [中严重] [30+处 as unknown as 双类型转换]
- **文件/路径**: `division-loader.ts`, `domain-baseline-catalog.ts`, `harness-sdk`
- **问题描述**: 
  - 完全绕过类型安全
  - 通常是类型架构问题的信号
- **建议修复**: 
  1. 修复上游类型问题
  2. 使用更具体的类型断言
  3. 重构类型层次结构

#### 7. [构建] [中严重] [rimraf  extraneous - 未在 package.json 声明]
- **文件/路径**: `node_modules/rimraf`
- **问题描述**: 
  - rimraf@6.1.3 存在于 node_modules 但未在 package.json 声明
  - 可能导致依赖问题
- **建议修复**: 
  1. 添加到 dependencies 或 devDependencies
  2. 或移除并使用其他清理方式

#### 8. [测试] [高严重] [178处直接 process.env 变更无抽象]
- **文件/路径**: `tests/unit/domains/registry/plugin-spi-registry.test.ts:640-687`
- **问题描述**: 
  - 直接读取/写入 process.env
  - 在 beforeEach/afterEach 中手动保存/恢复
  - 无 helper 抽象
- **建议修复**: 
  1. 创建 env helper 函数
  2. 统一保存和恢复模式
  3. 添加测试隔离验证

#### 9. [测试] [高严重] [21,998处 unlinkSync 调用无集中清理]
- **文件/路径**: 多个测试文件
- **问题描述**: 
  - 每个测试重新实现清理逻辑
  - 无集中清理 utility
  - 可能导致 EBUSY (Windows) 或并发访问问题
- **建议修复**: 
  1. 创建集中的测试清理 utility
  2. 使用 afterEach 确保清理
  3. 考虑使用临时目录

#### 10. [测试] [中严重] [无统一 mocking 框架]
- **文件/路径**: `tests/` 多个目录
- **问题描述**: 
  - 10,579 处 vi.fn, jest.mock, sinon 等调用
  - 无统一的 mocking 框架可见
  - 模式不一致
- **建议修复**: 
  1. 建立统一的 mock factory
  2. 标准化 mock 模式
  3. 添加 mocking 最佳实践文档

#### 11. [测试] [中严重] [Singleton 状态重置未标准化]
- **文件/路径**: `tests/unit/platform/five-plane-execution/dispatcher.test.ts`
- **问题描述**: 
  - 测试通过特定函数重置 singletons
  - 每个测试重复自己的 reset 逻辑
  - 无统一的 resetAllSingletons()
- **建议修复**: 
  1. 创建统一的 singleton reset 机制
  2. 在 afterEach 中自动调用
  3. 文档化 singleton 重置要求

#### 12. [测试] [低严重] [无分支覆盖率要求]
- **文件/路径**: `.c8rc.json`
- **问题描述**: 
  - `"100": false` - 无覆盖率要求
  - 只测量源代码，不测量测试文件
- **建议修复**: 
  1. 添加分支覆盖率阈值
  2. 在 CI 中强制执行覆盖率 gate
  3. 关注关键代码路径

#### 13. [网络] [高严重] [ScopedExternalAccessSandbox performHttpRequest 无超时]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/scoped-external-access-sandbox.ts:289`
- **问题描述**: 
  - fetch 调用无 timeout 参数
  - 挂起的请求可能永久阻塞
- **建议修复**: 
  1. 添加 AbortController timeout
  2. 使用标准的超时模式
  3. 记录超时配置

#### 14. [网络] [高严重] [OIDC 服务 fetch 调用无超时]
- **文件/路径**: `src/org-governance/sso-scim/oidc/oidc-service.ts:565,272`
- **问题描述**: 
  - OIDC token 交换无 retry logic
  - 无 timeout/abort signal
  - 认证流程可能挂起
- **建议修复**: 
  1. 添加 fetch timeout
  2. 添加 retry logic
  3. 使用标准的超时配置

#### 15. [网络] [高严重] [plugin-runtime-child 替换 globalThis.fetch 为 no-op]
- **文件/路径**: `src/domains/registry/plugin-runtime-child.ts:112`
- **问题描述**: 
  ```typescript
  globalThis.fetch = (async () => {}) as unknown as typeof globalThis.fetch;
  ```
  - 插件上下文中完全替换 fetch 为空函数
  - 插件的所有出站 HTTP 请求都会失败
  - 看起来是有意为之但可能造成问题
- **建议修复**: 
  1. 确认这是有意行为
  2. 文档化 fetch 替换行为
  3. 确保不会意外影响其他代码

#### 16. [网络] [中严重] [无显式 http.Agent 配置]
- **文件/路径**: 全局
- **问题描述**: 
  - 无显式的 http.Agent/https.Agent 配置
  - 依赖 Node.js 默认连接池
  - 无 maxSockets 或 maxConnections 配置
  - 高吞吐场景可能出问题
- **建议修复**: 
  1. 添加 http Agent 配置
  2. 文档化连接池大小
  3. 监控连接使用情况

#### 17. [序列化] [中严重] [合同版本硬编码为字符串字面量]
- **文件/路径**: `src/platform/contracts/inter-plane-contract-gateway.ts:184`
- **问题描述**: 
  - `schemaVersion: "v4.3"` 硬编码
  - 难以追踪合同版本演变
- **建议修复**: 
  1. 提取到类型化常量或 enum
  2. 集中管理合同版本
  3. 添加版本兼容性检查

#### 18. [序列化] [低严重] [Timestamp = string 丢失精度]
- **文件/路径**: `src/platform/contracts/types/domain/primitives.ts:10`
- **问题描述**: 
  - `type Timestamp = string` 无类型精度
  - ISO 8601 字符串无时区信息
- **建议修复**: 
  1. 使用 Date 对象或 branded string 类型
  2. 明确时区处理约定
  3. 文档化时间戳语义

#### 19. [源代码] [中严重] [29处非空断言 !.]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - 使用 `!.` 断言非空
  - 可能导致运行时错误
- **建议修复**: 
  1. 使用可选链 `?.` 或空值合并 `??`
  2. 添加适当的 null 检查
  3. 减少非空断言使用

#### 20. [API] [中严重] [API 路由 schema 版本硬编码]
- **文件/路径**: `src/platform/five-plane-interface/api/federation-routing-service.ts`
- **问题描述**: 
  - Zod schema 验证存在
  - 但 schema 版本未与 contract 版本同步
- **建议修复**: 
  1. 从 contract 版本派生 schema 版本
  2. 添加版本一致性检查
  3. 文档化版本关系

### 总结

本次补充Review（第十五轮 - 构建测试与类型系统Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. 无 TypeScript 增量编译缓存（每次完整重建9.6秒）
2. 76个 npm scripts 大部分重复构建
3. 168处 any 类型使用（绕过类型安全）
4. 38处 @ts-ignore 指令
5. ExecutionRecord 类型不匹配（14个文件）
6. 178处直接 process.env 变更无抽象
7. ScopedExternalAccessSandbox performHttpRequest 无超时
8. OIDC 服务 fetch 调用无超时
9. plugin-runtime-child 替换 globalThis.fetch 为 no-op

**中优先级**:
1. 30+处 as unknown as 双类型转换
2. rimraf extraneous 未声明
3. 21,998处 unlinkSync 无集中清理
4. 无统一 mocking 框架
5. Singleton 状态重置未标准化
6. 无显式 http.Agent 配置
7. 合同版本硬编码为字符串
8. Timestamp = string 丢失精度
9. 非空断言使用

**低优先级**:
1. 无分支覆盖率要求
2. API 路由 schema 版本未同步

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 72 | 87 | 39 | 198 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **109** | **182** | **85** | **376** |

### 优先修复建议

**立即处理 (本周内)**:
1. 添加 TypeScript 增量编译 (`incremental: true`)
2. 重构 npm scripts 避免重复构建
3. 减少 any 类型使用到 <50
4. 移除所有 @ts-ignore 指令
5. 修复 ExecutionRecord 类型不匹配
6. 添加 fetch 超时到 OIDC 和 sandbox

**短期内处理 (本月内)**:
1. 创建集中的测试清理 utility
2. 建立统一的 mocking 框架
3. 标准化 singleton 重置机制
4. 添加 http.Agent 连接池配置
5. 提取合同版本到类型化常量

**长期规划**:
1. 实现完整的类型安全（无 any）
2. 建立测试基础设施标准
3. 实现构建缓存优化
4. 完善网络层超时和重试
5. 建立覆盖率门禁标准

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十四轮 - 日志事件与工作流Review）

### 发现的新问题

#### 1. [可观测性] [高严重] [StructuredLogEntry 无 requestId 字段]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - StructuredLogEntry 接口无 requestId 字段
  - 只有 correlationId，它回退到 traceId
  - 无法独立追踪单个 HTTP 请求
- **建议修复**: 
  1. 在 StructuredLogEntry 添加 requestId 字段
  2. 在 HTTP 中间件中生成和注入 requestId
  3. 确保 requestId 流经所有日志调用

#### 2. [可观测性] [高严重] [StructuredLogger data 字段无自动清洗]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - 如果开发者传递 `{ password: "..." }` 给 logger.info，密码会被记录
  - 无字段级脱敏机制
  - 依赖开发者手动排除敏感字段
- **建议修复**: 
  1. 添加 OBSERVE_OUTPUT_BLACKLIST 类似的黑名单
  2. 实现 redact/mask/sanitize 工具函数
  3. 在日志传输前自动清洗敏感字段

#### 3. [可观测性] [中严重] [日志中敏感数据无 PII 处理]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - 无 PII 重写工具
  - 开发者必须手动避免记录敏感数据
  - 存在意外记录敏感信息的风险
- **建议修复**: 
  1. 添加 PII 检测和重写工具
  2. 在文档中明确记录敏感字段列表
  3. 添加测试验证敏感数据不被记录

#### 4. [可观测性] [中严重] [59处 console.* 直接调用]
- **文件/路径**: 多个源文件
- **问题描述**: 
  - 应该使用 StructuredLogger
  - console 调用可能不被日志聚合系统收集
  - 难以追踪和关联
- **建议修复**: 
  1. 替换所有 console.* 为 StructuredLogger
  2. 添加 ESLint 规则禁止 console.*
  3. 保留少量允许的 CLI 输出

#### 5. [可观测性] [中严重] [部分 logger.warn 调用缺少结构化数据]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:122`
- **问题描述**: 
  ```typescript
  logger.warn("WebSocket connection rejected: missing subprotocol token")
  ```
  - 只有消息，无 data 对象
  - 难以关联和查询
- **建议修复**: 
  1. 添加 `{ data: { ... } }` 对象
  2. 确保所有日志包含上下文数据
  3. 添加日志审查工具

#### 6. [源代码] [高严重] [Harness while(true) 循环无硬性迭代上限]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:1442`
- **问题描述**: 
  ```typescript
  while (true) {
    // ...
    // 依赖 budget gate 和 guardrail vibration 退出
    // 如果两者都失败，循环永久继续
  }
  ```
  - 依赖 budget gate 和 guardrail vibration 退出
  - 无硬性迭代次数限制
  - 可能导致无限循环
- **建议修复**: 
  1. 添加硬性 maxIterations 上限
  2. 在达到上限时强制退出
  3. 记录详细的迭代统计用于调试

#### 7. [源代码] [高严重] [Oapeflir while(true) 循环可能无限重规划]
- **文件/路径**: `src/platform/five-plane-orchestration/oapeflir/oapeflir-loop-service.ts:355`
- **问题描述**: 
  - 如果 loopReplanDecision.shouldReplan 永不变为 false
  - 重规划永远无法减少计划错误
  - 可能导致无限循环
- **建议修复**: 
  1. 添加重规划次数上限
  2. 在达到上限时强制接受当前结果
  3. 添加重规划质量衰减检测

#### 8. [源代码] [中严重] [Recovery 流程允许终态转换到 paused]
- **文件/路径**: `src/platform/five-plane-execution/runtime-state-machine.ts:105-108`
- **问题描述**: 
  - completed, failed, cancelled, aborted 可转换到 paused 进行恢复
  - 创建恢复路径可能重新进入执行
  - 需要验证此行为的正确性
- **建议修复**: 
  1. 审查 recovery 转换的合法性
  2. 确保不会导致状态不一致
  3. 添加转换前置条件验证

#### 9. [国际化] [高严重] [API 错误消息硬编码英文]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/api-error.ts`
- **问题描述**: 
  ```typescript
  return new ApiError(404, "api.task_not_found", "Task not found.");
  ```
  - 错误消息硬编码英文
  - 无后端 i18n 机制
  - 非英文客户端收到不本地化的错误
- **建议修复**: 
  1. 使用错误码而非硬编码消息
  2. 客户端根据错误码本地化
  3. 或在后端添加 i18n 支持

#### 10. [国际化] [中严重] [UI 硬编码字符串未翻译]
- **文件/路径**: `ui/packages/features/conversation/src/web/index.tsx`
- **问题描述**: 
  - "Build Plan", "Execute", "Messages" 等按钮标签硬编码
  - 不使用 i18n 系统
- **建议修复**: 
  1. 使用 `translateFeatureCopy` 替换硬编码
  2. 确保所有用户可见文本通过 i18n
  3. 添加硬编码字符串检测

#### 11. [国际化] [中严重] [Arabic 目录不完整]
- **文件/路径**: `ui/packages/shared/i18n/src/catalogs/ar-SA.ts`
- **问题描述**: 
  - ar-SA 只有约18条消息
  - en-US 和 zh-CN 有约117条
  - 缺失的 key 返回 key 本身
- **建议修复**: 
  1. 补充 Arabic 翻译
  2. 确保所有 key 都有翻译
  3. 添加翻译完整性测试

#### 12. [国际化] [低严重] [translateFeatureCopy 无回退默认值]
- **文件/路径**: `ui/packages/shared/i18n/`
- **问题描述**: 
  - 如果 feature ID 不在 catalog 中
  - 返回 undefined title/summary
  - 无合理回退
- **建议修复**: 
  1. 添加回退到 key 或默认字符串
  2. 或使用 feature ID 作为显示名
  3. 确保 UI 不显示 undefined

#### 13. [运维] [高严重] [健康检查无 readiness/liveness 区分]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts`
- **问题描述**: 
  - /healthz, /v1/healthz, /health 返回相同报告
  - 无 readiness probe（可处理流量？）
  - 无 liveness probe（系统存活？）
  - canAcceptTraffic() 未通过 HTTP 暴露
- **建议修复**: 
  1. 添加 /ready 和 /live 端点
  2. Readiness 检查依赖项就绪
  3. Liveness 检查进程健康

#### 14. [运维] [高严重] [shutdown 期间健康检查存在竞态]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts`
- **问题描述**: 
  - getHealthReportAsync() 异步检查 dbWritable
  - shutdown 期间数据库连接可能关闭
  - 健康检查仍在传入
- **建议修复**: 
  1. 在 shutdown 时返回 unhealthy
  2. 添加 shutdown 状态标志
  3. 停止接受新的健康检查请求

#### 15. [运维] [高严重] [LeaderElectionService 未与 GracefulShutdown 集成]
- **文件/路径**: `src/platform/five-plane-execution/ha/leader-election-service.ts`
- **问题描述**: 
  - LeaderElectionService.stop() 处理优雅退位
  - 但未在全局 graceful shutdown 中注册
  - 需显式注册为 shutdown handler
- **建议修复**: 
  1. 将 LeaderElectionService 添加到 shutdown handlers
  2. 确保领导者在 shutdown 时让位
  3. 验证关闭顺序正确

#### 16. [运维] [中严重] [addHandler() 无显式排序约定]
- **文件/路径**: `src/platform/five-plane-execution/startup/graceful-shutdown.ts`
- **问题描述**: 
  - 虽然 handlers 逆序执行
  - 但无显式接口声明 handler 依赖关系
  - 关键 handlers（如关闭数据库连接）应最后运行
- **建议修复**: 
  1. 添加优先级或阶段参数
  2. 文档化关键 handler 顺序要求
  3. 添加顺序验证测试

#### 17. [运维] [中严重] [unref'd timers 可能不触发]
- **文件/路径**: `src/platform/five-plane-execution/startup/process-error-handlers.ts:106`
- **问题描述**: 
  - `setTimeout(...).unref()` 不保持事件循环活跃
  - 如果主线程空闲，强制退出可能不触发
- **建议修复**: 
  1. 移除 .unref() 除非确实需要
  2. 或确保有其他方式保持事件循环
  3. 添加超时触发验证测试

#### 18. [运维] [中严重] [StartupConsistencyChecker 阻塞流量但不暴露状态]
- **文件/路径**: `src/platform/five-plane-startup-plan.ts`
- **问题描述**: 
  - canAcceptTraffic() 在 P0 问题存在时返回 false
  - 但 _trafficBlocked 是内部状态
  - 负载均衡器访问 /healthz 仍会得到 "ok"
- **建议修复**: 
  1. 在健康报告中暴露 trafficBlocked 状态
  2. 或添加单独的健康端点指示就绪状态
  3. 确保负载均衡器在阻塞时不路由流量

#### 19. [事件系统] [中严重] [TypedEventBus vs EventEmitter 混用]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - 项目同时使用 TypedEventBus 和原生 EventEmitter
  - TypedEventBus 应该是标准
  - 混用可能导致类型安全问题
- **建议修复**: 
  1. 创建迁移计划统一使用 TypedEventBus
  2. 添加 ESLint 规则禁止原生 EventEmitter
  3. 在 CI 中检测混用情况

#### 20. [源代码] [低严重] [无事件排序保证]
- **文件/路径**: `src/platform/five-plane-state-evidence/events/`
- **问题描述**: 
  - 无明确的事件排序保证文档
  - 事件处理可能乱序
  - 依赖方需要处理乱序
- **建议修复**: 
  1. 文档化事件排序语义
  2. 如果需要排序，实现序列号机制
  3. 在事件处理中考虑乱序情况

### 总结

本次补充Review（第十四轮 - 日志事件与工作流Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. StructuredLogEntry 无 requestId 字段
2. StructuredLogger data 字段无自动清洗（敏感数据风险）
3. Harness while(true) 循环无硬性迭代上限
4. Oapeflir while(true) 循环可能无限重规划
5. API 错误消息硬编码英文（无 i18n）
6. 健康检查无 readiness/liveness 区分
7. shutdown 期间健康检查存在竞态
8. LeaderElectionService 未与 GracefulShutdown 集成

**中优先级**:
1. 日志中敏感数据无 PII 处理
2. 59处 console.* 直接调用
3. 部分 logger.warn 调用缺少结构化数据
4. Recovery 流程允许终态转换到 paused
5. UI 硬编码字符串未翻译
6. Arabic 目录不完整
7. addHandler() 无显式排序约定
8. unref'd timers 可能不触发
9. StartupConsistencyChecker 阻塞流量但不暴露状态
10. TypedEventBus vs EventEmitter 混用

**低优先级**:
1. translateFeatureCopy 无回退默认值
2. 无事件排序保证

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 68 | 80 | 37 | 185 |
| 测试 | 8 | 14 | 5 | 27 |
| 配置 | 10 | 38 | 24 | 72 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **103** | **171** | **82** | **356** |

### 优先修复建议

**立即处理 (本周内)**:
1. 在 StructuredLogEntry 添加 requestId 字段
2. 实现日志敏感字段自动清洗
3. 为工作流循环添加硬性迭代上限
4. 添加 readiness/liveness 健康检查端点
5. 将 LeaderElectionService 集成到 GracefulShutdown
6. 实现 API 错误消息 i18n 支持

**短期内处理 (本月内)**:
1. 替换所有 console.* 为 StructuredLogger
2. 补充 Arabic i18n 目录
3. 修复 UI 硬编码字符串
4. 统一 TypedEventBus 替代 EventEmitter
5. 解决 shutdown 竞态条件
6. 添加事件排序保证文档

**长期规划**:
1. 建立完整的 PII 处理框架
2. 实现日志完整性验证
3. 建立工作流安全循环检测
4. 完善健康检查和就绪探测
5. 建立国际化完整测试

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十五轮 - 构建测试与类型系统Review）

### 发现的新问题

#### 1. [构建] [高严重] [无 TypeScript 增量编译缓存]
- **文件/路径**: `tsconfig.json`
- **问题描述**: 
  - 无 `incremental: true` 配置
  - 无 `tsbuildinfo` 文件
  - 每次构建是完整重建（9.6秒）
  - TypeScript 无法使用增量编译
- **建议修复**: 
  1. 在 tsconfig.json 添加 `"incremental": true`
  2. 添加 `"tsbuildinfo": ".tsbuildinfo"` 到 .gitignore
  3. 实现项目引用以支持并行构建

#### 2. [构建] [高严重] [76个 npm scripts 大部分重复构建]
- **文件/路径**: `package.json`
- **问题描述**: 
  - 40+ CLI scripts 都运行 `npm run build && node ...`
  - 每个脚本前都执行完整重建
  - 无法直接运行预构建的 CLI
  - 20+ stable:* 脚本遵循相同模式
- **建议修复**: 
  1. 创建 `build:cli` 脚本构建一次
  2. 让 CLI 脚本使用预构建的 dist 文件
  3. 添加 npm run dev 或类似的无需重建的执行方式

#### 3. [源代码] [高严重] [168处 any 类型使用]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - `stable-runner-factory.ts`: `opts: any` -> `any`
  - `billing-service-async.ts`: `input: any` 方法签名
  - 重试/超时工具使用 `(...args: any[]) => Promise<any>`
  - 绕过类型安全
- **建议修复**: 
  1. 使用泛型替代 any
  2. 替换为具体的输入类型
  3. 添加 ESLint 规则禁止 any

#### 4. [源代码] [高严重] [38处 @ts-ignore 指令]
- **文件/路径**: 多个文件
- **问题描述**: 
  - 14个 stability 相关文件有 `@ts-ignore ExecutionRecord type mismatch`
  - 5个 harness-sdk 文件有 `Partial<HarnessRun> doesn't have all required properties`
  - 2个文件有 `exactOptionalPropertyTypes` 问题
- **建议修复**: 
  1. 修复 ExecutionRecord 类型不匹配
  2. 完成 HarnessRun 类型的必需属性
  3. 移除 @ts-ignore 使用

#### 5. [源代码] [高严重] [ExecutionRecord 类型不匹配]
- **文件/路径**: `src/platform/stability/stable-evidence-bundle-support.ts:783` 等14个文件
- **问题描述**: 
  - 插入 execution record 时 TypeScript 拒绝类型
  - `ExecutionRecord` 接口要求新字段但 insert 对象缺失
  - 或 store 的 insert 方法接受不同类型
- **建议修复**: 
  1. 对齐 ExecutionRecord 接口和 store 类型
  2. 添加缺失字段到 insert 对象
  3. 或调整 store 的 insert 方法类型签名

#### 6. [源代码] [中严重] [30+处 as unknown as 双类型转换]
- **文件/路径**: `division-loader.ts`, `domain-baseline-catalog.ts`, `harness-sdk`
- **问题描述**: 
  - 完全绕过类型安全
  - 通常是类型架构问题的信号
- **建议修复**: 
  1. 修复上游类型问题
  2. 使用更具体的类型断言
  3. 重构类型层次结构

#### 7. [构建] [中严重] [rimraf  extraneous - 未在 package.json 声明]
- **文件/路径**: `node_modules/rimraf`
- **问题描述**: 
  - rimraf@6.1.3 存在于 node_modules 但未在 package.json 声明
  - 可能导致依赖问题
- **建议修复**: 
  1. 添加到 dependencies 或 devDependencies
  2. 或移除并使用其他清理方式

#### 8. [测试] [高严重] [178处直接 process.env 变更无抽象]
- **文件/路径**: `tests/unit/domains/registry/plugin-spi-registry.test.ts:640-687`
- **问题描述**: 
  - 直接读取/写入 process.env
  - 在 beforeEach/afterEach 中手动保存/恢复
  - 无 helper 抽象
- **建议修复**: 
  1. 创建 env helper 函数
  2. 统一保存和恢复模式
  3. 添加测试隔离验证

#### 9. [测试] [高严重] [21,998处 unlinkSync 调用无集中清理]
- **文件/路径**: 多个测试文件
- **问题描述**: 
  - 每个测试重新实现清理逻辑
  - 无集中清理 utility
  - 可能导致 EBUSY (Windows) 或并发访问问题
- **建议修复**: 
  1. 创建集中的测试清理 utility
  2. 使用 afterEach 确保清理
  3. 考虑使用临时目录

#### 10. [测试] [中严重] [无统一 mocking 框架]
- **文件/路径**: `tests/` 多个目录
- **问题描述**: 
  - 10,579 处 vi.fn, jest.mock, sinon 等调用
  - 无统一的 mocking 框架可见
  - 模式不一致
- **建议修复**: 
  1. 建立统一的 mock factory
  2. 标准化 mock 模式
  3. 添加 mocking 最佳实践文档

#### 11. [测试] [中严重] [Singleton 状态重置未标准化]
- **文件/路径**: `tests/unit/platform/five-plane-execution/dispatcher.test.ts`
- **问题描述**: 
  - 测试通过特定函数重置 singletons
  - 每个测试重复自己的 reset 逻辑
  - 无统一的 resetAllSingletons()
- **建议修复**: 
  1. 创建统一的 singleton reset 机制
  2. 在 afterEach 中自动调用
  3. 文档化 singleton 重置要求

#### 12. [测试] [低严重] [无分支覆盖率要求]
- **文件/路径**: `.c8rc.json`
- **问题描述**: 
  - `"100": false` - 无覆盖率要求
  - 只测量源代码，不测量测试文件
- **建议修复**: 
  1. 添加分支覆盖率阈值
  2. 在 CI 中强制执行覆盖率 gate
  3. 关注关键代码路径

#### 13. [网络] [高严重] [ScopedExternalAccessSandbox performHttpRequest 无超时]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/scoped-external-access-sandbox.ts:289`
- **问题描述**: 
  - fetch 调用无 timeout 参数
  - 挂起的请求可能永久阻塞
- **建议修复**: 
  1. 添加 AbortController timeout
  2. 使用标准的超时模式
  3. 记录超时配置

#### 14. [网络] [高严重] [OIDC 服务 fetch 调用无超时]
- **文件/路径**: `src/org-governance/sso-scim/oidc/oidc-service.ts:565,272`
- **问题描述**: 
  - OIDC token 交换无 retry logic
  - 无 timeout/abort signal
  - 认证流程可能挂起
- **建议修复**: 
  1. 添加 fetch timeout
  2. 添加 retry logic
  3. 使用标准的超时配置

#### 15. [网络] [高严重] [plugin-runtime-child 替换 globalThis.fetch 为 no-op]
- **文件/路径**: `src/domains/registry/plugin-runtime-child.ts:112`
- **问题描述**: 
  ```typescript
  globalThis.fetch = (async () => {}) as unknown as typeof globalThis.fetch;
  ```
  - 插件上下文中完全替换 fetch 为空函数
  - 插件的所有出站 HTTP 请求都会失败
  - 看起来是有意为之但可能造成问题
- **建议修复**: 
  1. 确认这是有意行为
  2. 文档化 fetch 替换行为
  3. 确保不会意外影响其他代码

#### 16. [网络] [中严重] [无显式 http.Agent 配置]
- **文件/路径**: 全局
- **问题描述**: 
  - 无显式的 http.Agent/https.Agent 配置
  - 依赖 Node.js 默认连接池
  - 无 maxSockets 或 maxConnections 配置
  - 高吞吐场景可能出问题
- **建议修复**: 
  1. 添加 http Agent 配置
  2. 文档化连接池大小
  3. 监控连接使用情况

#### 17. [序列化] [中严重] [合同版本硬编码为字符串字面量]
- **文件/路径**: `src/platform/contracts/inter-plane-contract-gateway.ts:184`
- **问题描述**: 
  - `schemaVersion: "v4.3"` 硬编码
  - 难以追踪合同版本演变
- **建议修复**: 
  1. 提取到类型化常量或 enum
  2. 集中管理合同版本
  3. 添加版本兼容性检查

#### 18. [序列化] [低严重] [Timestamp = string 丢失精度]
- **文件/路径**: `src/platform/contracts/types/domain/primitives.ts:10`
- **问题描述**: 
  - `type Timestamp = string` 无类型精度
  - ISO 8601 字符串无时区信息
- **建议修复**: 
  1. 使用 Date 对象或 branded string 类型
  2. 明确时区处理约定
  3. 文档化时间戳语义

#### 19. [源代码] [中严重] [29处非空断言 !.]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - 使用 `!.` 断言非空
  - 可能导致运行时错误
- **建议修复**: 
  1. 使用可选链 `?.` 或空值合并 `??`
  2. 添加适当的 null 检查
  3. 减少非空断言使用

#### 20. [API] [中严重] [API 路由 schema 版本硬编码]
- **文件/路径**: `src/platform/five-plane-interface/api/federation-routing-service.ts`
- **问题描述**: 
  - Zod schema 验证存在
  - 但 schema 版本未与 contract 版本同步
- **建议修复**: 
  1. 从 contract 版本派生 schema 版本
  2. 添加版本一致性检查
  3. 文档化版本关系

### 总结

本次补充Review（第十五轮 - 构建测试与类型系统Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. 无 TypeScript 增量编译缓存（每次完整重建9.6秒）
2. 76个 npm scripts 大部分重复构建
3. 168处 any 类型使用（绕过类型安全）
4. 38处 @ts-ignore 指令
5. ExecutionRecord 类型不匹配（14个文件）
6. 178处直接 process.env 变更无抽象
7. ScopedExternalAccessSandbox performHttpRequest 无超时
8. OIDC 服务 fetch 调用无超时
9. plugin-runtime-child 替换 globalThis.fetch 为 no-op

**中优先级**:
1. 30+处 as unknown as 双类型转换
2. rimraf extraneous 未声明
3. 21,998处 unlinkSync 无集中清理
4. 无统一 mocking 框架
5. Singleton 状态重置未标准化
6. 无显式 http.Agent 配置
7. 合同版本硬编码为字符串
8. Timestamp = string 丢失精度
9. 非空断言使用

**低优先级**:
1. 无分支覆盖率要求
2. API 路由 schema 版本未同步

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 72 | 87 | 39 | 198 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **109** | **182** | **85** | **376** |

### 优先修复建议

**立即处理 (本周内)**:
1. 添加 TypeScript 增量编译 (`incremental: true`)
2. 重构 npm scripts 避免重复构建
3. 减少 any 类型使用到 <50
4. 移除所有 @ts-ignore 指令
5. 修复 ExecutionRecord 类型不匹配
6. 添加 fetch 超时到 OIDC 和 sandbox

**短期内处理 (本月内)**:
1. 创建集中的测试清理 utility
2. 建立统一的 mocking 框架
3. 标准化 singleton 重置机制
4. 添加 http.Agent 连接池配置
5. 提取合同版本到类型化常量

**长期规划**:
1. 实现完整的类型安全（无 any）
2. 建立测试基础设施标准
3. 实现构建缓存优化
4. 完善网络层超时和重试
5. 建立覆盖率门禁标准

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十三轮 - 缓存会话与实时通信Review）

### 发现的新问题

#### 1. [安全] [严重] [CORS allowedMethods 缺少 PUT/PATCH/DELETE]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/response-hardening.ts:12-14`
- **问题描述**: 
  ```typescript
  export const DEFAULT_CORS_CONFIG: CorsConfig = {
    allowedMethods: ["GET", "POST", "OPTIONS"],  // 缺少 PUT, PATCH, DELETE
  ```
  - 浏览器 CORS preflight 会拒绝 PUT/PATCH/DELETE 请求
  - 但路由中有 PATCH /v1/tasks/:id, DELETE endpoints
- **建议修复**: 
  1. 添加 PUT, PATCH, DELETE 到 allowedMethods
  2. 确保 CORS 配置与应用路由方法一致
  3. 添加测试验证所有 HTTP 方法的 CORS preflight

#### 2. [安全] [严重] [Mission Routes 错误响应包装不当]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/mission-routes.ts:101,118-124`
- **问题描述**: 
  ```typescript
  return buildJsonResponse(ctx.requestId, 404, { error: { code: "MISSION_NOT_FOUND" } });
  ```
  - 使用 buildJsonResponse（成功包装）返回错误
  - 错误被包装在成功信封 `{requestId, data: {error: {...}}}` 中
  - 违反 REST 规范
- **建议修复**: 
  1. 使用 buildJsonErrorResponse 处理错误
  2. 确保所有错误路径使用正确的错误信封
  3. 添加 API 一致性测试

#### 3. [源代码] [高严重] [WebSocketBridge 无最大连接数限制]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:103-107`
- **问题描述**: 
  ```typescript
  this.wss = new WebSocketServer({
    server,
    path: WS_PATH,
    maxPayload: options.maxPayloadBytes ?? DEFAULT_MAX_PAYLOAD_BYTES,
    // 无 maxConnections 限制！
  });
  ```
  - 与 dashboard-websocket-server.ts 的 maxClients: 1000 不同
  - 恶意客户端可耗尽服务器资源
- **建议修复**: 
  1. 添加 maxConnections 限制
  2. 实现连接限制拒绝策略
  3. 添加连接计数指标

#### 4. [源代码] [高严重] [pendingAcks 在断开连接时未清理]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:268-282`
- **问题描述**: 
  - handleDisconnection 清理 subscribedTasks, taskSubscribers, slowConsumers
  - 但 pendingAcks Map 未清理
  - 断开的客户端未确认的消息永久留在内存
- **建议修复**: 
  1. 在 handleDisconnection 中删除所有 pendingAcks 条目
  2. 考虑添加超时机制自动清理未确认消息
  3. 记录清理操作的审计日志

#### 5. [安全] [高严重] [硬编码回退密钥]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/audit-event-integrity.ts:41`
- **问题描述**: 
  ```typescript
  const AUDIT_INTEGRITY_HMAC_KEY = process.env["AA_AUDIT_INTEGRITY_HMAC_KEY"] ?? "audit-integrity-secret-key-32-bytes!";
  ```
  - 占位符密钥用作回退
  - 如果环境变量未设置，使用不安全密钥
- **建议修复**: 
  1. 移除回退值，要求环境变量必须设置
  2. 在启动时验证密钥存在且足够强
  3. 如果密钥缺失导致启动失败

#### 6. [配置] [高严重] [config/runtime/test.json 缺少超时配置]
- **文件/路径**: `config/runtime/test.json`
- **问题描述**: 
  - 只有3个字段，缺少 maxConcurrentTasks, defaultTaskTimeoutMs, defaultStepTimeoutMs
  - 回退到默认值可能不适合测试环境
- **建议修复**: 
  1. 添加完整的 timeout/tuning 配置
  2. 确保测试环境有合理的默认值
  3. 文档化必需的配置字段

#### 7. [配置] [高严重] [prod.json 限制过严 - maxConcurrentTasks=1]
- **文件/路径**: `config/runtime/prod.json`
- **问题描述**: 
  - maxConcurrentTasks: 1（vs dev:8, staging:4, pre-prod:6）
  - defaultTaskTimeoutMs: 120000（vs dev:600000）
  - defaultStepTimeoutMs: 60000（vs dev:180000）
  - 生产环境严重受限，可能影响吞吐量
- **建议修复**: 
  1. 评估并调整生产限制
  2. 与业务需求匹配
  3. 添加生产容量测试

#### 8. [源代码] [高严重] [多个服务存在无界 Map 无驱逐策略]
- **文件/路径**: 多个域服务
- **问题描述**: 
  - `domain-knowledge-schema-service.ts:73-75`: schemas, sourceContent, sourceTimestamps 无限制
  - `domain-eval-framework-service.ts:91-95`: frameworks, qualityAxes 等无限制
  - `domain-recipe-service.ts:67-68`: recipes, versions 无限制
  - `domain-risk-profile-service.ts:51`: profiles 无限制
  - Session Maps (session-management.ts:83-89) 无自动清理
- **建议修复**: 
  1. 为所有 Map 实现 LRU 或 TTL 驱逐策略
  2. 添加后台清理任务
  3. 添加大小监控和告警

#### 9. [源代码] [中严重] [taskEventHistory Map 永不清理]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:91,548-555`
- **问题描述**: 
  - taskEventHistory 限制单任务历史为 200 条
  - 但 Map 本身从不清理
  - 取消订阅的任务历史永久保留
- **建议修复**: 
  1. 实现后台清理无订阅者任务的历史
  2. 添加任务订阅者监控
  3. 考虑使用 WeakMap 替代

#### 10. [配置] [中严重] [安全配置 drift - remoteWorkerRegistration 缺失]
- **文件/路径**: `config/security/dev.json`, `staging.json`, `pre-prod.json`
- **问题描述**: 
  - default.json 有完整的 remoteWorkerRegistration 配置
  - dev/staging/pre-prod 只有 approvalMode
  - prod 有 approvalMode 但无 remoteWorkerRegistration
  - 安全配置不一致
- **建议修复**: 
  1. 统一所有环境的 remoteWorkerRegistration
  2. 添加安全配置验证
  3. 确保最低安全基线

#### 11. [API] [中严重] [OpenAPI 端点响应格式不一致]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/utils.ts:207-215`
- **问题描述**: 
  - `/v1/openapi.json` 使用 buildJsonDocumentResponse（无包装）
  - 其他端点使用 buildJsonResponse（{requestId, data} 包装）
  - 客户端体验不一致
- **建议修复**: 
  1. 统一响应信封格式
  2. 文档化响应格式规范
  3. 添加响应格式验证测试

#### 12. [源代码] [中严重] [Redis 队列缺少 idempotency 索引支持]
- **文件/路径**: `src/platform/five-plane-execution/queue/redis-queue-adapter.ts`
- **问题描述**: 
  - SQLite 有部分唯一索引支持 idempotency
  - Redis 实现使用哈希索引但实现不完整
  - 可能导致重复消息
- **建议修复**: 
  1. 完善 Redis idempotency 实现
  2. 添加唯一索引验证
  3. 确保与 SQLite 行为一致

#### 13. [源代码] [中严重] [请求去重中间件使用内存存储]
- **文件/路径**: `src/platform/five-plane-interface/api/middleware/request-deduplication.ts`
- **问题描述**: 
  - 纯内存滑动窗口（Map<DeduplicationKey, DeduplicationEntry[]>）
  - 无外部存储
  - 多实例部署不共享
  - 重启后去重状态丢失
- **建议修复**: 
  1. 使用 Redis 替代内存存储
  2. 支持分布式去重
  3. 持久化去重状态

#### 14. [源代码] [中严重] [Cache 无 stampede 保护]
- **文件/路径**: 多个 cache 实现
- **问题描述**: 
  - MemoryCacheStore, ExperienceCacheService 无锁
  - 缓存未命中时可发生 thundering herd
  - 高并发下可能导致数据库过载
- **建议修复**: 
  1. 实现 single-flight 模式
  2. 添加请求排队机制
  3. 使用分布式锁保护缓存更新

#### 15. [源代码] [中严重] [EvidenceService eviction 仅在插入时触发]
- **文件/路径**: `src/platform/five-plane-state-evidence/memory/evidence-service.ts:202`
- **问题描述**: 
  - 清理仅在 record() 调用时触发
  - 空闲时无后台清理
  - 可能导致内存持续增长
- **建议修复**: 
  1. 添加定期后台清理任务
  2. 使用独立的清理线程
  3. 添加内存使用监控

#### 16. [源代码] [低严重] [无连接指标暴露]
- **文件/路径**: WebSocket 服务器
- **问题描述**: 
  - getClientCount() 存在但未通过 HTTP 端点暴露
  - 无 pendingAcks 队列深度指标
  - 无连接建立告警
- **建议修复**: 
  1. 通过 metrics 端点暴露连接指标
  2. 添加 pendingAcks 队列监控
  3. 添加连接数异常告警

#### 17. [源代码] [低严重] [无空闲客户端超时]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts`
- **问题描述**: 
  - 仅在心跳 Sweep 时检查 isAlive
  - 无 per-client 独立于心跳的空闲超时
  - 认证后从不发送消息的客户端只能通过心跳失败检测
- **建议修复**: 
  1. 添加 per-client idle timeout
  2. 独立于心跳间隔
  3. 配置可调整

#### 18. [源代码] [低严重] [订阅限制是 per-client 而非全局]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:244`
- **问题描述**: 
  - MAX_SUBSCRIPTIONS_PER_CLIENT = 100
  - 恶意客户端可订阅 100 个任务
  - 无全局任务订阅者限制
- **建议修复**: 
  1. 添加全局任务订阅者限制
  2. 实现 per-task 订阅者上限
  3. 添加反滥用检测

#### 19. [配置] [低严重] [config/runtime 无版本验证机制]
- **文件/路径**: `config/runtime/*.json`
- **问题描述**: 
  - 配置有 "version": "v4.3"
  - 但无 schema 版本验证
  - 加载时可能接受不兼容配置
- **建议修复**: 
  1. 添加 JSON Schema 验证
  2. 实现版本兼容性检查
  3. 启动时验证配置完整性

#### 20. [源代码] [低严重] [ImprovementCandidateRegistry splice 是 O(n)]
- **文件/路径**: `src/platform/five-plane-orchestration/improve/improvement-candidate-registry.ts:243`
- **问题描述**: 
  - accessOrder 数组的 splice 操作是 O(n)
  - 高频访问时可能性能问题
- **建议修复**: 
  1. 使用 LinkedList 替代数组
  2. 或使用 Map 维护访问顺序
  3. 性能测试验证

### 总结

本次补充Review（第十三轮 - 缓存会话与实时通信Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. CORS allowedMethods 缺少 PUT/PATCH/DELETE（浏览器请求会失败）
2. Mission Routes 错误响应包装不当
3. WebSocketBridge 无最大连接数限制（DoS风险）
4. pendingAcks 断开连接时未清理（内存泄漏）
5. 硬编码回退密钥（安全漏洞）
6. config/runtime/test.json 缺少超时配置
7. prod.json maxConcurrentTasks=1 限制过严
8. 多个服务存在无界 Map（内存泄漏风险）

**中优先级**:
1. taskEventHistory Map 永不清理
2. 安全配置 drift（remoteWorkerRegistration 缺失）
3. OpenAPI 端点响应格式不一致
4. Redis 队列 idempotency 实现不完整
5. 请求去重中间件使用内存存储（多实例不共享）
6. Cache 无 stampede 保护
7. EvidenceService eviction 仅在插入时触发

**低优先级**:
1. 无连接指标暴露
2. 无空闲客户端超时
3. 订阅限制是 per-client 而非全局
4. config/runtime 无版本验证机制
5. ImprovementCandidateRegistry splice O(n)

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 64 | 74 | 35 | 173 |
| 测试 | 8 | 14 | 5 | 27 |
| 配置 | 10 | 38 | 24 | 72 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 7 | 3 | 12 |
| UI | 0 | 3 | 3 | 6 |
| 部署 | 1 | 10 | 6 | 17 |
| **合计** | **99** | **161** | **79** | **339** |

### 优先修复建议

**立即处理 (本周内)**:
1. 修复 CORS allowedMethods 添加 PUT/PATCH/DELETE
2. 修复 Mission Routes 错误响应格式
3. 为 WebSocketBridge 添加最大连接数限制
4. 清理 pendingAcks 和 taskEventHistory
5. 移除硬编码回退密钥
6. 修复 config/runtime/test.json 配置
7. 评估并调整 prod 并发限制

**短期内处理 (本月内)**:
1. 统一安全配置（remoteWorkerRegistration）
2. 实现无界 Map 的 LRU/TTL 驱逐
3. 统一 API 响应信封格式
4. 完善 Redis idempotency 实现
5. 添加分布式去重中间件
6. 实现 cache stampede 保护

**长期规划**:
1. 建立完整的连接管理和监控
2. 实现后台清理任务框架
3. 添加配置 schema 验证
4. 优化高频繁操作的数据结构
5. 建立内存使用基线和告警

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十五轮 - 构建测试与类型系统Review）

### 发现的新问题

#### 1. [构建] [高严重] [无 TypeScript 增量编译缓存]
- **文件/路径**: `tsconfig.json`
- **问题描述**: 
  - 无 `incremental: true` 配置
  - 无 `tsbuildinfo` 文件
  - 每次构建是完整重建（9.6秒）
  - TypeScript 无法使用增量编译
- **建议修复**: 
  1. 在 tsconfig.json 添加 `"incremental": true`
  2. 添加 `"tsbuildinfo": ".tsbuildinfo"` 到 .gitignore
  3. 实现项目引用以支持并行构建

#### 2. [构建] [高严重] [76个 npm scripts 大部分重复构建]
- **文件/路径**: `package.json`
- **问题描述**: 
  - 40+ CLI scripts 都运行 `npm run build && node ...`
  - 每个脚本前都执行完整重建
  - 无法直接运行预构建的 CLI
  - 20+ stable:* 脚本遵循相同模式
- **建议修复**: 
  1. 创建 `build:cli` 脚本构建一次
  2. 让 CLI 脚本使用预构建的 dist 文件
  3. 添加 npm run dev 或类似的无需重建的执行方式

#### 3. [源代码] [高严重] [168处 any 类型使用]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - `stable-runner-factory.ts`: `opts: any` -> `any`
  - `billing-service-async.ts`: `input: any` 方法签名
  - 重试/超时工具使用 `(...args: any[]) => Promise<any>`
  - 绕过类型安全
- **建议修复**: 
  1. 使用泛型替代 any
  2. 替换为具体的输入类型
  3. 添加 ESLint 规则禁止 any

#### 4. [源代码] [高严重] [38处 @ts-ignore 指令]
- **文件/路径**: 多个文件
- **问题描述**: 
  - 14个 stability 相关文件有 `@ts-ignore ExecutionRecord type mismatch`
  - 5个 harness-sdk 文件有 `Partial<HarnessRun> doesn't have all required properties`
  - 2个文件有 `exactOptionalPropertyTypes` 问题
- **建议修复**: 
  1. 修复 ExecutionRecord 类型不匹配
  2. 完成 HarnessRun 类型的必需属性
  3. 移除 @ts-ignore 使用

#### 5. [源代码] [高严重] [ExecutionRecord 类型不匹配]
- **文件/路径**: `src/platform/stability/stable-evidence-bundle-support.ts:783` 等14个文件
- **问题描述**: 
  - 插入 execution record 时 TypeScript 拒绝类型
  - `ExecutionRecord` 接口要求新字段但 insert 对象缺失
  - 或 store 的 insert 方法接受不同类型
- **建议修复**: 
  1. 对齐 ExecutionRecord 接口和 store 类型
  2. 添加缺失字段到 insert 对象
  3. 或调整 store 的 insert 方法类型签名

#### 6. [源代码] [中严重] [30+处 as unknown as 双类型转换]
- **文件/路径**: `division-loader.ts`, `domain-baseline-catalog.ts`, `harness-sdk`
- **问题描述**: 
  - 完全绕过类型安全
  - 通常是类型架构问题的信号
- **建议修复**: 
  1. 修复上游类型问题
  2. 使用更具体的类型断言
  3. 重构类型层次结构

#### 7. [构建] [中严重] [rimraf  extraneous - 未在 package.json 声明]
- **文件/路径**: `node_modules/rimraf`
- **问题描述**: 
  - rimraf@6.1.3 存在于 node_modules 但未在 package.json 声明
  - 可能导致依赖问题
- **建议修复**: 
  1. 添加到 dependencies 或 devDependencies
  2. 或移除并使用其他清理方式

#### 8. [测试] [高严重] [178处直接 process.env 变更无抽象]
- **文件/路径**: `tests/unit/domains/registry/plugin-spi-registry.test.ts:640-687`
- **问题描述**: 
  - 直接读取/写入 process.env
  - 在 beforeEach/afterEach 中手动保存/恢复
  - 无 helper 抽象
- **建议修复**: 
  1. 创建 env helper 函数
  2. 统一保存和恢复模式
  3. 添加测试隔离验证

#### 9. [测试] [高严重] [21,998处 unlinkSync 调用无集中清理]
- **文件/路径**: 多个测试文件
- **问题描述**: 
  - 每个测试重新实现清理逻辑
  - 无集中清理 utility
  - 可能导致 EBUSY (Windows) 或并发访问问题
- **建议修复**: 
  1. 创建集中的测试清理 utility
  2. 使用 afterEach 确保清理
  3. 考虑使用临时目录

#### 10. [测试] [中严重] [无统一 mocking 框架]
- **文件/路径**: `tests/` 多个目录
- **问题描述**: 
  - 10,579 处 vi.fn, jest.mock, sinon 等调用
  - 无统一的 mocking 框架可见
  - 模式不一致
- **建议修复**: 
  1. 建立统一的 mock factory
  2. 标准化 mock 模式
  3. 添加 mocking 最佳实践文档

#### 11. [测试] [中严重] [Singleton 状态重置未标准化]
- **文件/路径**: `tests/unit/platform/five-plane-execution/dispatcher.test.ts`
- **问题描述**: 
  - 测试通过特定函数重置 singletons
  - 每个测试重复自己的 reset 逻辑
  - 无统一的 resetAllSingletons()
- **建议修复**: 
  1. 创建统一的 singleton reset 机制
  2. 在 afterEach 中自动调用
  3. 文档化 singleton 重置要求

#### 12. [测试] [低严重] [无分支覆盖率要求]
- **文件/路径**: `.c8rc.json`
- **问题描述**: 
  - `"100": false` - 无覆盖率要求
  - 只测量源代码，不测量测试文件
- **建议修复**: 
  1. 添加分支覆盖率阈值
  2. 在 CI 中强制执行覆盖率 gate
  3. 关注关键代码路径

#### 13. [网络] [高严重] [ScopedExternalAccessSandbox performHttpRequest 无超时]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/scoped-external-access-sandbox.ts:289`
- **问题描述**: 
  - fetch 调用无 timeout 参数
  - 挂起的请求可能永久阻塞
- **建议修复**: 
  1. 添加 AbortController timeout
  2. 使用标准的超时模式
  3. 记录超时配置

#### 14. [网络] [高严重] [OIDC 服务 fetch 调用无超时]
- **文件/路径**: `src/org-governance/sso-scim/oidc/oidc-service.ts:565,272`
- **问题描述**: 
  - OIDC token 交换无 retry logic
  - 无 timeout/abort signal
  - 认证流程可能挂起
- **建议修复**: 
  1. 添加 fetch timeout
  2. 添加 retry logic
  3. 使用标准的超时配置

#### 15. [网络] [高严重] [plugin-runtime-child 替换 globalThis.fetch 为 no-op]
- **文件/路径**: `src/domains/registry/plugin-runtime-child.ts:112`
- **问题描述**: 
  ```typescript
  globalThis.fetch = (async () => {}) as unknown as typeof globalThis.fetch;
  ```
  - 插件上下文中完全替换 fetch 为空函数
  - 插件的所有出站 HTTP 请求都会失败
  - 看起来是有意为之但可能造成问题
- **建议修复**: 
  1. 确认这是有意行为
  2. 文档化 fetch 替换行为
  3. 确保不会意外影响其他代码

#### 16. [网络] [中严重] [无显式 http.Agent 配置]
- **文件/路径**: 全局
- **问题描述**: 
  - 无显式的 http.Agent/https.Agent 配置
  - 依赖 Node.js 默认连接池
  - 无 maxSockets 或 maxConnections 配置
  - 高吞吐场景可能出问题
- **建议修复**: 
  1. 添加 http Agent 配置
  2. 文档化连接池大小
  3. 监控连接使用情况

#### 17. [序列化] [中严重] [合同版本硬编码为字符串字面量]
- **文件/路径**: `src/platform/contracts/inter-plane-contract-gateway.ts:184`
- **问题描述**: 
  - `schemaVersion: "v4.3"` 硬编码
  - 难以追踪合同版本演变
- **建议修复**: 
  1. 提取到类型化常量或 enum
  2. 集中管理合同版本
  3. 添加版本兼容性检查

#### 18. [序列化] [低严重] [Timestamp = string 丢失精度]
- **文件/路径**: `src/platform/contracts/types/domain/primitives.ts:10`
- **问题描述**: 
  - `type Timestamp = string` 无类型精度
  - ISO 8601 字符串无时区信息
- **建议修复**: 
  1. 使用 Date 对象或 branded string 类型
  2. 明确时区处理约定
  3. 文档化时间戳语义

#### 19. [源代码] [中严重] [29处非空断言 !.]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - 使用 `!.` 断言非空
  - 可能导致运行时错误
- **建议修复**: 
  1. 使用可选链 `?.` 或空值合并 `??`
  2. 添加适当的 null 检查
  3. 减少非空断言使用

#### 20. [API] [中严重] [API 路由 schema 版本硬编码]
- **文件/路径**: `src/platform/five-plane-interface/api/federation-routing-service.ts`
- **问题描述**: 
  - Zod schema 验证存在
  - 但 schema 版本未与 contract 版本同步
- **建议修复**: 
  1. 从 contract 版本派生 schema 版本
  2. 添加版本一致性检查
  3. 文档化版本关系

### 总结

本次补充Review（第十五轮 - 构建测试与类型系统Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. 无 TypeScript 增量编译缓存（每次完整重建9.6秒）
2. 76个 npm scripts 大部分重复构建
3. 168处 any 类型使用（绕过类型安全）
4. 38处 @ts-ignore 指令
5. ExecutionRecord 类型不匹配（14个文件）
6. 178处直接 process.env 变更无抽象
7. ScopedExternalAccessSandbox performHttpRequest 无超时
8. OIDC 服务 fetch 调用无超时
9. plugin-runtime-child 替换 globalThis.fetch 为 no-op

**中优先级**:
1. 30+处 as unknown as 双类型转换
2. rimraf extraneous 未声明
3. 21,998处 unlinkSync 无集中清理
4. 无统一 mocking 框架
5. Singleton 状态重置未标准化
6. 无显式 http.Agent 配置
7. 合同版本硬编码为字符串
8. Timestamp = string 丢失精度
9. 非空断言使用

**低优先级**:
1. 无分支覆盖率要求
2. API 路由 schema 版本未同步

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 72 | 87 | 39 | 198 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **109** | **182** | **85** | **376** |

### 优先修复建议

**立即处理 (本周内)**:
1. 添加 TypeScript 增量编译 (`incremental: true`)
2. 重构 npm scripts 避免重复构建
3. 减少 any 类型使用到 <50
4. 移除所有 @ts-ignore 指令
5. 修复 ExecutionRecord 类型不匹配
6. 添加 fetch 超时到 OIDC 和 sandbox

**短期内处理 (本月内)**:
1. 创建集中的测试清理 utility
2. 建立统一的 mocking 框架
3. 标准化 singleton 重置机制
4. 添加 http.Agent 连接池配置
5. 提取合同版本到类型化常量

**长期规划**:
1. 实现完整的类型安全（无 any）
2. 建立测试基础设施标准
3. 实现构建缓存优化
4. 完善网络层超时和重试
5. 建立覆盖率门禁标准

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十四轮 - 日志事件与工作流Review）

### 发现的新问题

#### 1. [可观测性] [高严重] [StructuredLogEntry 无 requestId 字段]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - StructuredLogEntry 接口无 requestId 字段
  - 只有 correlationId，它回退到 traceId
  - 无法独立追踪单个 HTTP 请求
- **建议修复**: 
  1. 在 StructuredLogEntry 添加 requestId 字段
  2. 在 HTTP 中间件中生成和注入 requestId
  3. 确保 requestId 流经所有日志调用

#### 2. [可观测性] [高严重] [StructuredLogger data 字段无自动清洗]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - 如果开发者传递 `{ password: "..." }` 给 logger.info，密码会被记录
  - 无字段级脱敏机制
  - 依赖开发者手动排除敏感字段
- **建议修复**: 
  1. 添加 OBSERVE_OUTPUT_BLACKLIST 类似的黑名单
  2. 实现 redact/mask/sanitize 工具函数
  3. 在日志传输前自动清洗敏感字段

#### 3. [可观测性] [中严重] [日志中敏感数据无 PII 处理]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - 无 PII 重写工具
  - 开发者必须手动避免记录敏感数据
  - 存在意外记录敏感信息的风险
- **建议修复**: 
  1. 添加 PII 检测和重写工具
  2. 在文档中明确记录敏感字段列表
  3. 添加测试验证敏感数据不被记录

#### 4. [可观测性] [中严重] [59处 console.* 直接调用]
- **文件/路径**: 多个源文件
- **问题描述**: 
  - 应该使用 StructuredLogger
  - console 调用可能不被日志聚合系统收集
  - 难以追踪和关联
- **建议修复**: 
  1. 替换所有 console.* 为 StructuredLogger
  2. 添加 ESLint 规则禁止 console.*
  3. 保留少量允许的 CLI 输出

#### 5. [可观测性] [中严重] [部分 logger.warn 调用缺少结构化数据]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:122`
- **问题描述**: 
  ```typescript
  logger.warn("WebSocket connection rejected: missing subprotocol token")
  ```
  - 只有消息，无 data 对象
  - 难以关联和查询
- **建议修复**: 
  1. 添加 `{ data: { ... } }` 对象
  2. 确保所有日志包含上下文数据
  3. 添加日志审查工具

#### 6. [源代码] [高严重] [Harness while(true) 循环无硬性迭代上限]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:1442`
- **问题描述**: 
  ```typescript
  while (true) {
    // ...
    // 依赖 budget gate 和 guardrail vibration 退出
    // 如果两者都失败，循环永久继续
  }
  ```
  - 依赖 budget gate 和 guardrail vibration 退出
  - 无硬性迭代次数限制
  - 可能导致无限循环
- **建议修复**: 
  1. 添加硬性 maxIterations 上限
  2. 在达到上限时强制退出
  3. 记录详细的迭代统计用于调试

#### 7. [源代码] [高严重] [Oapeflir while(true) 循环可能无限重规划]
- **文件/路径**: `src/platform/five-plane-orchestration/oapeflir/oapeflir-loop-service.ts:355`
- **问题描述**: 
  - 如果 loopReplanDecision.shouldReplan 永不变为 false
  - 重规划永远无法减少计划错误
  - 可能导致无限循环
- **建议修复**: 
  1. 添加重规划次数上限
  2. 在达到上限时强制接受当前结果
  3. 添加重规划质量衰减检测

#### 8. [源代码] [中严重] [Recovery 流程允许终态转换到 paused]
- **文件/路径**: `src/platform/five-plane-execution/runtime-state-machine.ts:105-108`
- **问题描述**: 
  - completed, failed, cancelled, aborted 可转换到 paused 进行恢复
  - 创建恢复路径可能重新进入执行
  - 需要验证此行为的正确性
- **建议修复**: 
  1. 审查 recovery 转换的合法性
  2. 确保不会导致状态不一致
  3. 添加转换前置条件验证

#### 9. [国际化] [高严重] [API 错误消息硬编码英文]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/api-error.ts`
- **问题描述**: 
  ```typescript
  return new ApiError(404, "api.task_not_found", "Task not found.");
  ```
  - 错误消息硬编码英文
  - 无后端 i18n 机制
  - 非英文客户端收到不本地化的错误
- **建议修复**: 
  1. 使用错误码而非硬编码消息
  2. 客户端根据错误码本地化
  3. 或在后端添加 i18n 支持

#### 10. [国际化] [中严重] [UI 硬编码字符串未翻译]
- **文件/路径**: `ui/packages/features/conversation/src/web/index.tsx`
- **问题描述**: 
  - "Build Plan", "Execute", "Messages" 等按钮标签硬编码
  - 不使用 i18n 系统
- **建议修复**: 
  1. 使用 `translateFeatureCopy` 替换硬编码
  2. 确保所有用户可见文本通过 i18n
  3. 添加硬编码字符串检测

#### 11. [国际化] [中严重] [Arabic 目录不完整]
- **文件/路径**: `ui/packages/shared/i18n/src/catalogs/ar-SA.ts`
- **问题描述**: 
  - ar-SA 只有约18条消息
  - en-US 和 zh-CN 有约117条
  - 缺失的 key 返回 key 本身
- **建议修复**: 
  1. 补充 Arabic 翻译
  2. 确保所有 key 都有翻译
  3. 添加翻译完整性测试

#### 12. [国际化] [低严重] [translateFeatureCopy 无回退默认值]
- **文件/路径**: `ui/packages/shared/i18n/`
- **问题描述**: 
  - 如果 feature ID 不在 catalog 中
  - 返回 undefined title/summary
  - 无合理回退
- **建议修复**: 
  1. 添加回退到 key 或默认字符串
  2. 或使用 feature ID 作为显示名
  3. 确保 UI 不显示 undefined

#### 13. [运维] [高严重] [健康检查无 readiness/liveness 区分]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts`
- **问题描述**: 
  - /healthz, /v1/healthz, /health 返回相同报告
  - 无 readiness probe（可处理流量？）
  - 无 liveness probe（系统存活？）
  - canAcceptTraffic() 未通过 HTTP 暴露
- **建议修复**: 
  1. 添加 /ready 和 /live 端点
  2. Readiness 检查依赖项就绪
  3. Liveness 检查进程健康

#### 14. [运维] [高严重] [shutdown 期间健康检查存在竞态]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts`
- **问题描述**: 
  - getHealthReportAsync() 异步检查 dbWritable
  - shutdown 期间数据库连接可能关闭
  - 健康检查仍在传入
- **建议修复**: 
  1. 在 shutdown 时返回 unhealthy
  2. 添加 shutdown 状态标志
  3. 停止接受新的健康检查请求

#### 15. [运维] [高严重] [LeaderElectionService 未与 GracefulShutdown 集成]
- **文件/路径**: `src/platform/five-plane-execution/ha/leader-election-service.ts`
- **问题描述**: 
  - LeaderElectionService.stop() 处理优雅退位
  - 但未在全局 graceful shutdown 中注册
  - 需显式注册为 shutdown handler
- **建议修复**: 
  1. 将 LeaderElectionService 添加到 shutdown handlers
  2. 确保领导者在 shutdown 时让位
  3. 验证关闭顺序正确

#### 16. [运维] [中严重] [addHandler() 无显式排序约定]
- **文件/路径**: `src/platform/five-plane-execution/startup/graceful-shutdown.ts`
- **问题描述**: 
  - 虽然 handlers 逆序执行
  - 但无显式接口声明 handler 依赖关系
  - 关键 handlers（如关闭数据库连接）应最后运行
- **建议修复**: 
  1. 添加优先级或阶段参数
  2. 文档化关键 handler 顺序要求
  3. 添加顺序验证测试

#### 17. [运维] [中严重] [unref'd timers 可能不触发]
- **文件/路径**: `src/platform/five-plane-execution/startup/process-error-handlers.ts:106`
- **问题描述**: 
  - `setTimeout(...).unref()` 不保持事件循环活跃
  - 如果主线程空闲，强制退出可能不触发
- **建议修复**: 
  1. 移除 .unref() 除非确实需要
  2. 或确保有其他方式保持事件循环
  3. 添加超时触发验证测试

#### 18. [运维] [中严重] [StartupConsistencyChecker 阻塞流量但不暴露状态]
- **文件/路径**: `src/platform/five-plane-startup-plan.ts`
- **问题描述**: 
  - canAcceptTraffic() 在 P0 问题存在时返回 false
  - 但 _trafficBlocked 是内部状态
  - 负载均衡器访问 /healthz 仍会得到 "ok"
- **建议修复**: 
  1. 在健康报告中暴露 trafficBlocked 状态
  2. 或添加单独的健康端点指示就绪状态
  3. 确保负载均衡器在阻塞时不路由流量

#### 19. [事件系统] [中严重] [TypedEventBus vs EventEmitter 混用]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - 项目同时使用 TypedEventBus 和原生 EventEmitter
  - TypedEventBus 应该是标准
  - 混用可能导致类型安全问题
- **建议修复**: 
  1. 创建迁移计划统一使用 TypedEventBus
  2. 添加 ESLint 规则禁止原生 EventEmitter
  3. 在 CI 中检测混用情况

#### 20. [源代码] [低严重] [无事件排序保证]
- **文件/路径**: `src/platform/five-plane-state-evidence/events/`
- **问题描述**: 
  - 无明确的事件排序保证文档
  - 事件处理可能乱序
  - 依赖方需要处理乱序
- **建议修复**: 
  1. 文档化事件排序语义
  2. 如果需要排序，实现序列号机制
  3. 在事件处理中考虑乱序情况

### 总结

本次补充Review（第十四轮 - 日志事件与工作流Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. StructuredLogEntry 无 requestId 字段
2. StructuredLogger data 字段无自动清洗（敏感数据风险）
3. Harness while(true) 循环无硬性迭代上限
4. Oapeflir while(true) 循环可能无限重规划
5. API 错误消息硬编码英文（无 i18n）
6. 健康检查无 readiness/liveness 区分
7. shutdown 期间健康检查存在竞态
8. LeaderElectionService 未与 GracefulShutdown 集成

**中优先级**:
1. 日志中敏感数据无 PII 处理
2. 59处 console.* 直接调用
3. 部分 logger.warn 调用缺少结构化数据
4. Recovery 流程允许终态转换到 paused
5. UI 硬编码字符串未翻译
6. Arabic 目录不完整
7. addHandler() 无显式排序约定
8. unref'd timers 可能不触发
9. StartupConsistencyChecker 阻塞流量但不暴露状态
10. TypedEventBus vs EventEmitter 混用

**低优先级**:
1. translateFeatureCopy 无回退默认值
2. 无事件排序保证

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 68 | 80 | 37 | 185 |
| 测试 | 8 | 14 | 5 | 27 |
| 配置 | 10 | 38 | 24 | 72 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **103** | **171** | **82** | **356** |

### 优先修复建议

**立即处理 (本周内)**:
1. 在 StructuredLogEntry 添加 requestId 字段
2. 实现日志敏感字段自动清洗
3. 为工作流循环添加硬性迭代上限
4. 添加 readiness/liveness 健康检查端点
5. 将 LeaderElectionService 集成到 GracefulShutdown
6. 实现 API 错误消息 i18n 支持

**短期内处理 (本月内)**:
1. 替换所有 console.* 为 StructuredLogger
2. 补充 Arabic i18n 目录
3. 修复 UI 硬编码字符串
4. 统一 TypedEventBus 替代 EventEmitter
5. 解决 shutdown 竞态条件
6. 添加事件排序保证文档

**长期规划**:
1. 建立完整的 PII 处理框架
2. 实现日志完整性验证
3. 建立工作流安全循环检测
4. 完善健康检查和就绪探测
5. 建立国际化完整测试

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十五轮 - 构建测试与类型系统Review）

### 发现的新问题

#### 1. [构建] [高严重] [无 TypeScript 增量编译缓存]
- **文件/路径**: `tsconfig.json`
- **问题描述**: 
  - 无 `incremental: true` 配置
  - 无 `tsbuildinfo` 文件
  - 每次构建是完整重建（9.6秒）
  - TypeScript 无法使用增量编译
- **建议修复**: 
  1. 在 tsconfig.json 添加 `"incremental": true`
  2. 添加 `"tsbuildinfo": ".tsbuildinfo"` 到 .gitignore
  3. 实现项目引用以支持并行构建

#### 2. [构建] [高严重] [76个 npm scripts 大部分重复构建]
- **文件/路径**: `package.json`
- **问题描述**: 
  - 40+ CLI scripts 都运行 `npm run build && node ...`
  - 每个脚本前都执行完整重建
  - 无法直接运行预构建的 CLI
  - 20+ stable:* 脚本遵循相同模式
- **建议修复**: 
  1. 创建 `build:cli` 脚本构建一次
  2. 让 CLI 脚本使用预构建的 dist 文件
  3. 添加 npm run dev 或类似的无需重建的执行方式

#### 3. [源代码] [高严重] [168处 any 类型使用]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - `stable-runner-factory.ts`: `opts: any` -> `any`
  - `billing-service-async.ts`: `input: any` 方法签名
  - 重试/超时工具使用 `(...args: any[]) => Promise<any>`
  - 绕过类型安全
- **建议修复**: 
  1. 使用泛型替代 any
  2. 替换为具体的输入类型
  3. 添加 ESLint 规则禁止 any

#### 4. [源代码] [高严重] [38处 @ts-ignore 指令]
- **文件/路径**: 多个文件
- **问题描述**: 
  - 14个 stability 相关文件有 `@ts-ignore ExecutionRecord type mismatch`
  - 5个 harness-sdk 文件有 `Partial<HarnessRun> doesn't have all required properties`
  - 2个文件有 `exactOptionalPropertyTypes` 问题
- **建议修复**: 
  1. 修复 ExecutionRecord 类型不匹配
  2. 完成 HarnessRun 类型的必需属性
  3. 移除 @ts-ignore 使用

#### 5. [源代码] [高严重] [ExecutionRecord 类型不匹配]
- **文件/路径**: `src/platform/stability/stable-evidence-bundle-support.ts:783` 等14个文件
- **问题描述**: 
  - 插入 execution record 时 TypeScript 拒绝类型
  - `ExecutionRecord` 接口要求新字段但 insert 对象缺失
  - 或 store 的 insert 方法接受不同类型
- **建议修复**: 
  1. 对齐 ExecutionRecord 接口和 store 类型
  2. 添加缺失字段到 insert 对象
  3. 或调整 store 的 insert 方法类型签名

#### 6. [源代码] [中严重] [30+处 as unknown as 双类型转换]
- **文件/路径**: `division-loader.ts`, `domain-baseline-catalog.ts`, `harness-sdk`
- **问题描述**: 
  - 完全绕过类型安全
  - 通常是类型架构问题的信号
- **建议修复**: 
  1. 修复上游类型问题
  2. 使用更具体的类型断言
  3. 重构类型层次结构

#### 7. [构建] [中严重] [rimraf  extraneous - 未在 package.json 声明]
- **文件/路径**: `node_modules/rimraf`
- **问题描述**: 
  - rimraf@6.1.3 存在于 node_modules 但未在 package.json 声明
  - 可能导致依赖问题
- **建议修复**: 
  1. 添加到 dependencies 或 devDependencies
  2. 或移除并使用其他清理方式

#### 8. [测试] [高严重] [178处直接 process.env 变更无抽象]
- **文件/路径**: `tests/unit/domains/registry/plugin-spi-registry.test.ts:640-687`
- **问题描述**: 
  - 直接读取/写入 process.env
  - 在 beforeEach/afterEach 中手动保存/恢复
  - 无 helper 抽象
- **建议修复**: 
  1. 创建 env helper 函数
  2. 统一保存和恢复模式
  3. 添加测试隔离验证

#### 9. [测试] [高严重] [21,998处 unlinkSync 调用无集中清理]
- **文件/路径**: 多个测试文件
- **问题描述**: 
  - 每个测试重新实现清理逻辑
  - 无集中清理 utility
  - 可能导致 EBUSY (Windows) 或并发访问问题
- **建议修复**: 
  1. 创建集中的测试清理 utility
  2. 使用 afterEach 确保清理
  3. 考虑使用临时目录

#### 10. [测试] [中严重] [无统一 mocking 框架]
- **文件/路径**: `tests/` 多个目录
- **问题描述**: 
  - 10,579 处 vi.fn, jest.mock, sinon 等调用
  - 无统一的 mocking 框架可见
  - 模式不一致
- **建议修复**: 
  1. 建立统一的 mock factory
  2. 标准化 mock 模式
  3. 添加 mocking 最佳实践文档

#### 11. [测试] [中严重] [Singleton 状态重置未标准化]
- **文件/路径**: `tests/unit/platform/five-plane-execution/dispatcher.test.ts`
- **问题描述**: 
  - 测试通过特定函数重置 singletons
  - 每个测试重复自己的 reset 逻辑
  - 无统一的 resetAllSingletons()
- **建议修复**: 
  1. 创建统一的 singleton reset 机制
  2. 在 afterEach 中自动调用
  3. 文档化 singleton 重置要求

#### 12. [测试] [低严重] [无分支覆盖率要求]
- **文件/路径**: `.c8rc.json`
- **问题描述**: 
  - `"100": false` - 无覆盖率要求
  - 只测量源代码，不测量测试文件
- **建议修复**: 
  1. 添加分支覆盖率阈值
  2. 在 CI 中强制执行覆盖率 gate
  3. 关注关键代码路径

#### 13. [网络] [高严重] [ScopedExternalAccessSandbox performHttpRequest 无超时]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/scoped-external-access-sandbox.ts:289`
- **问题描述**: 
  - fetch 调用无 timeout 参数
  - 挂起的请求可能永久阻塞
- **建议修复**: 
  1. 添加 AbortController timeout
  2. 使用标准的超时模式
  3. 记录超时配置

#### 14. [网络] [高严重] [OIDC 服务 fetch 调用无超时]
- **文件/路径**: `src/org-governance/sso-scim/oidc/oidc-service.ts:565,272`
- **问题描述**: 
  - OIDC token 交换无 retry logic
  - 无 timeout/abort signal
  - 认证流程可能挂起
- **建议修复**: 
  1. 添加 fetch timeout
  2. 添加 retry logic
  3. 使用标准的超时配置

#### 15. [网络] [高严重] [plugin-runtime-child 替换 globalThis.fetch 为 no-op]
- **文件/路径**: `src/domains/registry/plugin-runtime-child.ts:112`
- **问题描述**: 
  ```typescript
  globalThis.fetch = (async () => {}) as unknown as typeof globalThis.fetch;
  ```
  - 插件上下文中完全替换 fetch 为空函数
  - 插件的所有出站 HTTP 请求都会失败
  - 看起来是有意为之但可能造成问题
- **建议修复**: 
  1. 确认这是有意行为
  2. 文档化 fetch 替换行为
  3. 确保不会意外影响其他代码

#### 16. [网络] [中严重] [无显式 http.Agent 配置]
- **文件/路径**: 全局
- **问题描述**: 
  - 无显式的 http.Agent/https.Agent 配置
  - 依赖 Node.js 默认连接池
  - 无 maxSockets 或 maxConnections 配置
  - 高吞吐场景可能出问题
- **建议修复**: 
  1. 添加 http Agent 配置
  2. 文档化连接池大小
  3. 监控连接使用情况

#### 17. [序列化] [中严重] [合同版本硬编码为字符串字面量]
- **文件/路径**: `src/platform/contracts/inter-plane-contract-gateway.ts:184`
- **问题描述**: 
  - `schemaVersion: "v4.3"` 硬编码
  - 难以追踪合同版本演变
- **建议修复**: 
  1. 提取到类型化常量或 enum
  2. 集中管理合同版本
  3. 添加版本兼容性检查

#### 18. [序列化] [低严重] [Timestamp = string 丢失精度]
- **文件/路径**: `src/platform/contracts/types/domain/primitives.ts:10`
- **问题描述**: 
  - `type Timestamp = string` 无类型精度
  - ISO 8601 字符串无时区信息
- **建议修复**: 
  1. 使用 Date 对象或 branded string 类型
  2. 明确时区处理约定
  3. 文档化时间戳语义

#### 19. [源代码] [中严重] [29处非空断言 !.]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - 使用 `!.` 断言非空
  - 可能导致运行时错误
- **建议修复**: 
  1. 使用可选链 `?.` 或空值合并 `??`
  2. 添加适当的 null 检查
  3. 减少非空断言使用

#### 20. [API] [中严重] [API 路由 schema 版本硬编码]
- **文件/路径**: `src/platform/five-plane-interface/api/federation-routing-service.ts`
- **问题描述**: 
  - Zod schema 验证存在
  - 但 schema 版本未与 contract 版本同步
- **建议修复**: 
  1. 从 contract 版本派生 schema 版本
  2. 添加版本一致性检查
  3. 文档化版本关系

### 总结

本次补充Review（第十五轮 - 构建测试与类型系统Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. 无 TypeScript 增量编译缓存（每次完整重建9.6秒）
2. 76个 npm scripts 大部分重复构建
3. 168处 any 类型使用（绕过类型安全）
4. 38处 @ts-ignore 指令
5. ExecutionRecord 类型不匹配（14个文件）
6. 178处直接 process.env 变更无抽象
7. ScopedExternalAccessSandbox performHttpRequest 无超时
8. OIDC 服务 fetch 调用无超时
9. plugin-runtime-child 替换 globalThis.fetch 为 no-op

**中优先级**:
1. 30+处 as unknown as 双类型转换
2. rimraf extraneous 未声明
3. 21,998处 unlinkSync 无集中清理
4. 无统一 mocking 框架
5. Singleton 状态重置未标准化
6. 无显式 http.Agent 配置
7. 合同版本硬编码为字符串
8. Timestamp = string 丢失精度
9. 非空断言使用

**低优先级**:
1. 无分支覆盖率要求
2. API 路由 schema 版本未同步

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 72 | 87 | 39 | 198 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **109** | **182** | **85** | **376** |

### 优先修复建议

**立即处理 (本周内)**:
1. 添加 TypeScript 增量编译 (`incremental: true`)
2. 重构 npm scripts 避免重复构建
3. 减少 any 类型使用到 <50
4. 移除所有 @ts-ignore 指令
5. 修复 ExecutionRecord 类型不匹配
6. 添加 fetch 超时到 OIDC 和 sandbox

**短期内处理 (本月内)**:
1. 创建集中的测试清理 utility
2. 建立统一的 mocking 框架
3. 标准化 singleton 重置机制
4. 添加 http.Agent 连接池配置
5. 提取合同版本到类型化常量

**长期规划**:
1. 实现完整的类型安全（无 any）
2. 建立测试基础设施标准
3. 实现构建缓存优化
4. 完善网络层超时和重试
5. 建立覆盖率门禁标准

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十轮 - 深度代码质量与API一致性Review）

### 发现的新问题

#### 1. [源代码] [高严重] [sleepSync busy-wait 阻塞事件循环]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/repositories/authoritative-task-store-decorator.ts:50-57`
- **问题描述**: 
  - 发现 `sleepSync()` 函数使用 busy-wait 实现
  ```typescript
  function sleepSync(ms: number): void {
    const end = Date.now() + ms;
    while (Date.now() < end) { /* busy-wait */ }
  }
  ```
  - 在 SQLite BUSY 错误重试路径中使用
  - 阻塞事件循环，可能导致高负载下性能问题
- **建议修复**: 
  1. 使用 `setTimeout` 替代 busy-wait
  2. 或使用异步等待模式
  3. 确保重试逻辑不会阻塞主线程

#### 2. [源代码] [高严重] [TODO R4-27: HarnessRun 持久化缺失 - 数据丢失风险]
- **文件/路径**: `src/platform/five-plane-execution/execution-engine/single-task-happy-path.ts:270`
- **问题描述**: 
  - 注释 `# TODO R4-27: HarnessRun must be persisted to RuntimeTruthRepository`
  - 表明 HarnessRun 数据可能未持久化
  - 存在数据丢失风险
- **建议修复**: 
  1. 实现 HarnessRun 持久化逻辑
  2. 确保执行状态可恢复
  3. 在 TODO 完成前添加监控告警

#### 3. [源代码] [高严重] [SDK 与 Server 路由不匹配 - /handshake 和 /version 缺失]
- **文件/路径**: `src/sdk/client-sdk/api-client.ts` 及 `src/platform/five-plane-interface/api/`
- **问题描述**: 
  - SDK client 调用 `/handshake` 和 `/version` 端点
  - 但服务器端没有这些路由的处理程序
  - `SdkVersionHandshakeService` 存在但未连接到任何路由
- **建议修复**: 
  1. 实现 `/handshake` 和 `/version` 服务器路由
  2. 或从 SDK 中移除这些调用
  3. 确保 SDK 和 Server API 版本兼容

#### 4. [源代码] [高严重] [SDK URL 缺少 /api 前缀]
- **文件/路径**: `src/sdk/client-sdk/api-client.ts:84` vs 服务器路由
- **问题描述**: 
  - SDK 构建路径: `${baseUrl}/${apiVersion}/${path}` → `/v1/harness-runs`
  - 服务器路由: `/api/v1/harness-runs`
  - 缺少 `/api` 前缀导致 404 错误
- **建议修复**: 
  1. 在 SDK 的 `baseUrl` 中添加 `/api` 前缀
  2. 或移除服务器路由中的 `/api` 前缀
  3. 确保所有路由前缀一致

#### 5. [源代码] [高严重] [quant-trading.json 存在多种配置错误]
- **文件/路径**: `config/domains/quant-trading.json`
- **问题描述**: 
  - `capabilities.supportedTaskTypes` 包含 `["strategy_backtest", "pre_trade_risk_check"]`
  - 但 domain seed 定义的是 `["research", "simulate", "trade"]`
  - `workflowProfile.workflowId` 为 `"quant-trading.primary"` 但没有匹配的 workflow
  - `toolProfile.requiredTools` 包含 `order_execution` 但 `toolBundles` 中已禁用
- **建议修复**: 
  1. 修正 supportedTaskTypes 以匹配 seed 定义
  2. 添加缺失的 workflow 定义或修正引用
  3. 启用 `order_execution` 或从 requiredTools 中移除

#### 6. [源代码] [中严重] [SDK 与 Server 错误类别不一致]
- **文件/路径**: `src/sdk/client-sdk/api-client.ts` 和 `src/platform/five-plane-interface/api/http-server/api-error.ts`
- **问题描述**: 
  - SDK 映射: 4xx → BUSINESS, 400 为 BUSINESS
  - Server 映射: 400 → validation, 422 → business-rule
  - 同一 HTTP 状态码产生不同的错误类别
  - `PolicyDeniedError`, `TenantBoundaryError`, `WorkflowStateError` 等 Server 错误类型未暴露给 SDK
- **建议修复**: 
  1. 统一 SDK 和 Server 的错误类别映射
  2. 在 SDK 中添加缺失的错误类型处理
  3. 文档化错误类别层次结构

#### 7. [源代码] [中严重] [N+1 查询模式 - 重复调用相同查询]
- **文件/路径**: `src/platform/five-plane-state-evidence/events/cas/postgres-fencing-token-service.ts:73,79`
- **问题描述**: 
  - 同一查询被连续调用两次：
  ```typescript
  for (const fence of await repo.getFencesForExecution(executionId)) { ... }
  for (const fence of await repo.getFencesForExecution(executionId)) { ... }
  ```
  - 不必要的数据库往返
- **建议修复**: 
  1. 将结果缓存到变量中
  2. 合并两个循环为一个
  3. 添加缓存层避免重复查询

#### 8. [源代码] [中严重] [experience-cache-service 无分页 - 内存溢出风险]
- **文件/路径**: `src/platform/five-plane-state-evidence/memory/experience-cache-service.ts:337-424`
- **问题描述**: 
  - `findSimilarExperiences` 查询 `LIMIT 500` 行
  - 然后在内存中对所有500行进行评分和过滤
  - 无数据库级分页或游标
  - 可能导致高内存占用
- **建议修复**: 
  1. 实现游标分页
  2. 将评分逻辑下推到数据库
  3. 限制返回行数

#### 9. [源代码] [中严重] [无限制的内存 Map - 无驱逐策略]
- **文件/路径**: `src/platform/five-plane-state-evidence/memory/evidence-service.ts:129-133`
- **问题描述**: 
  ```typescript
  private readonly records: Map<string, EvidenceRecord> = new Map();
  private readonly CATEGORY_INDEX = new Map<EvidenceCategory, Set<string>>();
  private readonly SOURCE_REF_INDEX = new Map<string, Set<string>>();
  private readonly TENANT_INDEX = new Map<string, Set<string>>();
  private readonly STATUS_INDEX = new Map<EvidenceStatus, Set<string>>();
  ```
  - 无大小限制
  - 无驱逐策略
  - 可能导致内存泄漏
- **建议修复**: 
  1. 实现 LRU 或 TTL 驱逐策略
  2. 添加最大大小限制
  3. 监控内存使用情况

#### 10. [源代码] [中严重] [固定轮询间隔 - 雷电羊群风险]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/channel-gateway-retry-executor.ts:76-78`
- **问题描述**: 
  ```typescript
  this.intervalHandle = setInterval(() => {
    void this.runOnce();
  }, this.pollIntervalMs);
  ```
  - 固定间隔轮询无 jitter
  - 如果大量消息同时就绪，可能导致处理峰值
- **建议修复**: 
  1. 在轮询间隔添加随机 jitter
  2. 实现指数退避
  3. 或使用事件驱动而非轮询

#### 11. [源代码] [中严重] [VersionRoutingMiddleware 未连接到路由]
- **文件/路径**: `src/platform/five-plane-interface/api/middleware/version-routing.ts`
- **问题描述**: 
  - `VersionRoutingMiddleware` 存在但从未应用到任何路由
  - `supportedVersions: ["2026-04-01", "2026-01-01"]` 未生效
  - API 版本控制功能未启用
- **建议修复**: 
  1. 将 VersionRoutingMiddleware 连接到路由
  2. 实现版本协商逻辑
  3. 或移除未使用的中间件

#### 12. [源代码] [中严重] [marketing.json 缺少必需字段]
- **文件/路径**: `config/domains/marketing.json`
- **问题描述**: 
  - 缺少 `description` 字段
  - 缺少 `version` 字段
  - 缺少 `status` 字段
  - 缺少 `riskProfile` / `riskSpec` 对象
  - 缺少 `workflows` 数组（只有 `workflowProfile`）
  - 缺少 `toolBundles` 数组（只有 `toolProfile`）
  - 缺少 `outputContracts` 数组
  - 缺少 `capabilities` 对象
- **建议修复**: 
  1. 补充所有必需字段
  2. 与 quant-trading.json 保持结构一致
  3. 验证 JSON schema 合规性

#### 13. [源代码] [中严重] [21个 TODO/FIXME/HACK 注释未处理]
- **文件/路径**: 多个源文件
- **问题描述**: 
  - `src/org-governance/sso-scim/saml/index.ts:17` - TODO Phase 2 SAML production hardening
  - `src/platform/five-plane-interface/api/mission-control-service.ts:439` - TODO p50/p99 metrics
  - `src/platform/five-plane-execution/plugin-executor/adapter-executor.ts:184` - TODO Redis idempotency
  - 等共21个 TODO
- **建议修复**: 
  1. 逐个处理或创建 issue 跟踪
  2. 优先处理生产 hardening 相关的 TODO
  3. 设置代码质量标准要求处理所有 TODO

#### 14. [源代码] [中严重] [82处 any 类型使用]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - 82处 `: any`, `<any>`, 或 `as any`
  - 表明 TypeScript strict mode 未完全执行
  - 降低了类型安全性
- **建议修复**: 
  1. 启用 TypeScript strict mode
  2. 逐步替换 any 为具体类型
  3. 使用 unknown 替代 any

#### 15. [配置] [中严重] [连接池默认大小可能不足]
- **文件/路径**: `src/platform/five-plane-control-plane/config-center/postgres-pool-env.ts:58`
- **问题描述**: 
  - `poolMax` 默认为 10
  - 高吞吐场景可能不足
  - 无自动扩展配置
- **建议修复**: 
  1. 根据负载调整默认值
  2. 添加连接池自动调优
  3. 监控连接池使用情况

#### 16. [源代码] [中严重] [experience_cache 表无索引]
- **文件/路径**: `src/platform/five-plane-state-evidence/memory/experience-cache-service.ts`
- **问题描述**: 
  - `findSimilarExperiences` 查询使用 `session_id`, `quality_score`, `outcome`, `task_context`, `task_intent`
  - 但这些列可能没有数据库索引
  - 影响查询性能
- **建议修复**: 
  1. 在 experience_cache 表添加相应索引
  2. 使用 EXPLAIN 分析查询计划
  3. 优化索引策略

#### 17. [源代码] [低严重] [魔法数字散落各处]
- **文件/路径**: 多个源文件
- **问题描述**: 
  - `3600000` (1小时), `86400000` (24小时), `3600` (1小时秒)
  - `4001`, `4003` - WebSocket 关闭代码
  - `504` - HTTP 504
  - `4096` - stderr 截断缓冲区大小
- **建议修复**: 
  1. 使用 `src/platform/contracts/constants/time.ts` 中的常量
  2. 为其他魔法数字创建常量文件
  3. 添加 ESLint 规则禁止魔法数字

#### 18. [源代码] [低严重] [静默 catch 块吞掉错误]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:130` 等多处
- **问题描述**: 
  ```typescript
  } catch {
    ws.close(4003, "Invalid token");
    // Error is silently swallowed
  }
  ```
  - 错误被静默吞掉，无日志记录
  - 难以调试和追踪问题
- **建议修复**: 
  1. 在 catch 块中添加错误日志
  2. 使用 StructuredLogger 记录错误
  3. 考虑重新抛出或传播错误

#### 19. [测试] [中严重] [1059处 setTimeout/sleep/waitFor - 测试脆弱]
- **文件/路径**: `tests/` 多个测试文件
- **问题描述**: 
  - 1059个 setTimeout/sleep/waitFor 调用
  - `plugin-spi-registry.test.ts` 有自定义 waitFor() 250ms 超时
  - `bootstrap.test.ts` 使用 setTimeout(resolve, 500)
  - 在 CI 高负载下可能非确定性地失败
- **建议修复**: 
  1. 用 async/await 或 proper mocking 替代 sleep
  2. 增加超时时间或改用轮询条件
  3. 添加重试机制

#### 20. [测试] [中严重] [6个测试文件使用 // @ts-nocheck]
- **文件/路径**: `tests/unit/domains/recipes/recipe-executor.test.ts` 等
- **问题描述**: 
  - 整个文件禁用 TypeScript 类型检查
  - 隐藏真实的类型错误
  - 使重构变得危险
- **建议修复**: 
  1. 移除 // @ts-nocheck
  2. 修复类型错误
  3. 使用 // @ts-expect-error 替代单个错误

#### 21. [测试] [中严重] [3个集成测试因缺少 workflow_state 设置而跳过]
- **文件/路径**: `tests/integration/platform/five-plane-execution/budget-allocation.integration.test.ts`
- **问题描述**: 
  - 3个测试因缺少 `evaluateMultiDimensionalQuota` 和 `workflow_state` 记录而跳过
  - 关键预算分配功能未测试
- **建议修复**: 
  1. 设置测试所需的 workflow_state 记录
  2. 实现缺失的 evaluateMultiDimensionalQuota
  3. 确保测试环境完整性

#### 22. [测试] [低严重] [5个测试使用 .skip 标记]
- **文件/路径**: `tests/unit/scale-ecosystem/multi-region/cross-region-routing.test.ts` 等
- **问题描述**: 
  - `describe.skip` - 功能未实现
  - `test.skip()` - 缺少依赖
  - 已知失败但未解决
- **建议修复**: 
  1. 实现跳过的功能
  2. 设置测试依赖
  3. 或在 TODO 中跟踪

#### 23. [源代码] [低严重] [double throw 模式]
- **文件/路径**: `src/domains/domain-baseline-catalog.ts:546,550`
- **问题描述**: 
  - 在 baseline 未找到时连续抛出两次错误
  - 可能导致混淆的错误堆栈
- **建议修复**: 
  1. 审查并修正错误抛出逻辑
  2. 简化错误处理流程

#### 24. [源代码] [低严重] [user-operations.json 配置过简]
- **文件/路径**: `config/domains/user-operations.json`
- **问题描述**: 
  - 配置极简，无 workflows 或 contracts 定义
  - 可能无法正常工作
- **建议修复**: 
  1. 补充完整的领域配置
  2. 或确认这是有意为之

#### 25. [部署] [低严重] [CI Coverage gate 仅在 Node 22 运行]
- **文件/路径**: `.github/workflows/`
- **问题描述**: 
  - `coverage:gate` 仅在 Node 22 运行
  - Node 20 不检查覆盖率
  - 不同版本可能有不同的覆盖情况
- **建议修复**: 
  1. 在所有版本上运行 coverage gate
  2. 或在 PR 检查中明确说明

#### 26. [源代码] [低严重] [模块级 logger 实例可能不是最佳实践]
- **文件/路径**: `src/platform/five-plane-execution/execution-engine/single-task-happy-path.ts:69`
- **问题描述**: 
  ```typescript
  const logger = new StructuredLogger({ retentionLimit: 100 });
  ```
  - 模块级状态可能不是最佳实践
  - 大多数服务使用依赖注入
- **建议修复**: 
  1. 通过依赖注入传递 logger
  2. 或使用全局单例

### 总结

本次补充Review（第十轮 - 深度代码质量与API一致性Review - 2026-05-14）发现了26个新问题，重点关注代码质量和API一致性。

**高优先级 (需要立即处理)**:
1. sleepSync busy-wait 阻塞事件循环
2. TODO R4-27: HarnessRun 持久化缺失
3. SDK 与 Server 路由不匹配 (/handshake, /version 缺失)
4. SDK URL 缺少 /api 前缀
5. quant-trading.json 存在多种配置错误

**中优先级**:
1. SDK 与 Server 错误类别不一致
2. N+1 查询模式 (postgres-fencing-token-service)
3. experience-cache-service 无分页
4. 无限制的内存 Map
5. 固定轮询间隔无 jitter
6. VersionRoutingMiddleware 未连接
7. marketing.json 缺少必需字段
8. 21个 TODO/FIXME 注释未处理
9. 82处 any 类型使用
10. 连接池默认大小可能不足
11. experience_cache 表无索引
12. 1059处 setTimeout/sleep (测试脆弱)
13. 6个测试文件使用 @ts-nocheck
14. 3个集成测试跳过

**低优先级**:
1. 魔法数字散落各处
2. 静默 catch 块吞掉错误
3. 5个测试使用 .skip 标记
4. double throw 模式
5. user-operations.json 配置过简
6. CI coverage gate 仅在 Node 22
7. 模块级 logger 实例

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 51 | 57 | 26 | 134 |
| 测试 | 8 | 14 | 5 | 27 |
| 配置 | 8 | 29 | 18 | 55 |
| 安全 | 6 | 6 | 1 | 13 |
| 文档 | 2 | 7 | 3 | 12 |
| UI | 0 | 3 | 3 | 6 |
| 部署 | 0 | 8 | 5 | 13 |
| **合计** | **75** | **124** | **61** | **260** |

### 优先修复建议

**立即处理 (本周内)**:
1. 修复 SDK 与 Server 路由不匹配（/handshake, /version, /api前缀）
2. 实现 HarnessRun 持久化（TODO R4-27）
3. 修复 quant-trading.json 和 marketing.json 配置错误
4. 将 sleepSync 改为非阻塞实现
5. 统一 SDK 和 Server 错误类别映射

**短期内处理 (本月内)**:
1. 实现 N+1 查询修复和分页
2. 添加无界 Map 的驱逐策略
3. 添加轮询 jitter 防止 thundering herd
4. 处理所有 TODO/FIXME 注释
5. 移除测试文件中的 @ts-nocheck
6. 修复 workflow_state 集成测试

**长期规划**:
1. 启用 TypeScript strict mode
2. 完善 API 版本控制中间件
3. 添加 experience_cache 表索引
4. 统一所有魔法数字到常量
5. 建立代码质量门禁标准

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十五轮 - 构建测试与类型系统Review）

### 发现的新问题

#### 1. [构建] [高严重] [无 TypeScript 增量编译缓存]
- **文件/路径**: `tsconfig.json`
- **问题描述**: 
  - 无 `incremental: true` 配置
  - 无 `tsbuildinfo` 文件
  - 每次构建是完整重建（9.6秒）
  - TypeScript 无法使用增量编译
- **建议修复**: 
  1. 在 tsconfig.json 添加 `"incremental": true`
  2. 添加 `"tsbuildinfo": ".tsbuildinfo"` 到 .gitignore
  3. 实现项目引用以支持并行构建

#### 2. [构建] [高严重] [76个 npm scripts 大部分重复构建]
- **文件/路径**: `package.json`
- **问题描述**: 
  - 40+ CLI scripts 都运行 `npm run build && node ...`
  - 每个脚本前都执行完整重建
  - 无法直接运行预构建的 CLI
  - 20+ stable:* 脚本遵循相同模式
- **建议修复**: 
  1. 创建 `build:cli` 脚本构建一次
  2. 让 CLI 脚本使用预构建的 dist 文件
  3. 添加 npm run dev 或类似的无需重建的执行方式

#### 3. [源代码] [高严重] [168处 any 类型使用]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - `stable-runner-factory.ts`: `opts: any` -> `any`
  - `billing-service-async.ts`: `input: any` 方法签名
  - 重试/超时工具使用 `(...args: any[]) => Promise<any>`
  - 绕过类型安全
- **建议修复**: 
  1. 使用泛型替代 any
  2. 替换为具体的输入类型
  3. 添加 ESLint 规则禁止 any

#### 4. [源代码] [高严重] [38处 @ts-ignore 指令]
- **文件/路径**: 多个文件
- **问题描述**: 
  - 14个 stability 相关文件有 `@ts-ignore ExecutionRecord type mismatch`
  - 5个 harness-sdk 文件有 `Partial<HarnessRun> doesn't have all required properties`
  - 2个文件有 `exactOptionalPropertyTypes` 问题
- **建议修复**: 
  1. 修复 ExecutionRecord 类型不匹配
  2. 完成 HarnessRun 类型的必需属性
  3. 移除 @ts-ignore 使用

#### 5. [源代码] [高严重] [ExecutionRecord 类型不匹配]
- **文件/路径**: `src/platform/stability/stable-evidence-bundle-support.ts:783` 等14个文件
- **问题描述**: 
  - 插入 execution record 时 TypeScript 拒绝类型
  - `ExecutionRecord` 接口要求新字段但 insert 对象缺失
  - 或 store 的 insert 方法接受不同类型
- **建议修复**: 
  1. 对齐 ExecutionRecord 接口和 store 类型
  2. 添加缺失字段到 insert 对象
  3. 或调整 store 的 insert 方法类型签名

#### 6. [源代码] [中严重] [30+处 as unknown as 双类型转换]
- **文件/路径**: `division-loader.ts`, `domain-baseline-catalog.ts`, `harness-sdk`
- **问题描述**: 
  - 完全绕过类型安全
  - 通常是类型架构问题的信号
- **建议修复**: 
  1. 修复上游类型问题
  2. 使用更具体的类型断言
  3. 重构类型层次结构

#### 7. [构建] [中严重] [rimraf  extraneous - 未在 package.json 声明]
- **文件/路径**: `node_modules/rimraf`
- **问题描述**: 
  - rimraf@6.1.3 存在于 node_modules 但未在 package.json 声明
  - 可能导致依赖问题
- **建议修复**: 
  1. 添加到 dependencies 或 devDependencies
  2. 或移除并使用其他清理方式

#### 8. [测试] [高严重] [178处直接 process.env 变更无抽象]
- **文件/路径**: `tests/unit/domains/registry/plugin-spi-registry.test.ts:640-687`
- **问题描述**: 
  - 直接读取/写入 process.env
  - 在 beforeEach/afterEach 中手动保存/恢复
  - 无 helper 抽象
- **建议修复**: 
  1. 创建 env helper 函数
  2. 统一保存和恢复模式
  3. 添加测试隔离验证

#### 9. [测试] [高严重] [21,998处 unlinkSync 调用无集中清理]
- **文件/路径**: 多个测试文件
- **问题描述**: 
  - 每个测试重新实现清理逻辑
  - 无集中清理 utility
  - 可能导致 EBUSY (Windows) 或并发访问问题
- **建议修复**: 
  1. 创建集中的测试清理 utility
  2. 使用 afterEach 确保清理
  3. 考虑使用临时目录

#### 10. [测试] [中严重] [无统一 mocking 框架]
- **文件/路径**: `tests/` 多个目录
- **问题描述**: 
  - 10,579 处 vi.fn, jest.mock, sinon 等调用
  - 无统一的 mocking 框架可见
  - 模式不一致
- **建议修复**: 
  1. 建立统一的 mock factory
  2. 标准化 mock 模式
  3. 添加 mocking 最佳实践文档

#### 11. [测试] [中严重] [Singleton 状态重置未标准化]
- **文件/路径**: `tests/unit/platform/five-plane-execution/dispatcher.test.ts`
- **问题描述**: 
  - 测试通过特定函数重置 singletons
  - 每个测试重复自己的 reset 逻辑
  - 无统一的 resetAllSingletons()
- **建议修复**: 
  1. 创建统一的 singleton reset 机制
  2. 在 afterEach 中自动调用
  3. 文档化 singleton 重置要求

#### 12. [测试] [低严重] [无分支覆盖率要求]
- **文件/路径**: `.c8rc.json`
- **问题描述**: 
  - `"100": false` - 无覆盖率要求
  - 只测量源代码，不测量测试文件
- **建议修复**: 
  1. 添加分支覆盖率阈值
  2. 在 CI 中强制执行覆盖率 gate
  3. 关注关键代码路径

#### 13. [网络] [高严重] [ScopedExternalAccessSandbox performHttpRequest 无超时]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/scoped-external-access-sandbox.ts:289`
- **问题描述**: 
  - fetch 调用无 timeout 参数
  - 挂起的请求可能永久阻塞
- **建议修复**: 
  1. 添加 AbortController timeout
  2. 使用标准的超时模式
  3. 记录超时配置

#### 14. [网络] [高严重] [OIDC 服务 fetch 调用无超时]
- **文件/路径**: `src/org-governance/sso-scim/oidc/oidc-service.ts:565,272`
- **问题描述**: 
  - OIDC token 交换无 retry logic
  - 无 timeout/abort signal
  - 认证流程可能挂起
- **建议修复**: 
  1. 添加 fetch timeout
  2. 添加 retry logic
  3. 使用标准的超时配置

#### 15. [网络] [高严重] [plugin-runtime-child 替换 globalThis.fetch 为 no-op]
- **文件/路径**: `src/domains/registry/plugin-runtime-child.ts:112`
- **问题描述**: 
  ```typescript
  globalThis.fetch = (async () => {}) as unknown as typeof globalThis.fetch;
  ```
  - 插件上下文中完全替换 fetch 为空函数
  - 插件的所有出站 HTTP 请求都会失败
  - 看起来是有意为之但可能造成问题
- **建议修复**: 
  1. 确认这是有意行为
  2. 文档化 fetch 替换行为
  3. 确保不会意外影响其他代码

#### 16. [网络] [中严重] [无显式 http.Agent 配置]
- **文件/路径**: 全局
- **问题描述**: 
  - 无显式的 http.Agent/https.Agent 配置
  - 依赖 Node.js 默认连接池
  - 无 maxSockets 或 maxConnections 配置
  - 高吞吐场景可能出问题
- **建议修复**: 
  1. 添加 http Agent 配置
  2. 文档化连接池大小
  3. 监控连接使用情况

#### 17. [序列化] [中严重] [合同版本硬编码为字符串字面量]
- **文件/路径**: `src/platform/contracts/inter-plane-contract-gateway.ts:184`
- **问题描述**: 
  - `schemaVersion: "v4.3"` 硬编码
  - 难以追踪合同版本演变
- **建议修复**: 
  1. 提取到类型化常量或 enum
  2. 集中管理合同版本
  3. 添加版本兼容性检查

#### 18. [序列化] [低严重] [Timestamp = string 丢失精度]
- **文件/路径**: `src/platform/contracts/types/domain/primitives.ts:10`
- **问题描述**: 
  - `type Timestamp = string` 无类型精度
  - ISO 8601 字符串无时区信息
- **建议修复**: 
  1. 使用 Date 对象或 branded string 类型
  2. 明确时区处理约定
  3. 文档化时间戳语义

#### 19. [源代码] [中严重] [29处非空断言 !.]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - 使用 `!.` 断言非空
  - 可能导致运行时错误
- **建议修复**: 
  1. 使用可选链 `?.` 或空值合并 `??`
  2. 添加适当的 null 检查
  3. 减少非空断言使用

#### 20. [API] [中严重] [API 路由 schema 版本硬编码]
- **文件/路径**: `src/platform/five-plane-interface/api/federation-routing-service.ts`
- **问题描述**: 
  - Zod schema 验证存在
  - 但 schema 版本未与 contract 版本同步
- **建议修复**: 
  1. 从 contract 版本派生 schema 版本
  2. 添加版本一致性检查
  3. 文档化版本关系

### 总结

本次补充Review（第十五轮 - 构建测试与类型系统Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. 无 TypeScript 增量编译缓存（每次完整重建9.6秒）
2. 76个 npm scripts 大部分重复构建
3. 168处 any 类型使用（绕过类型安全）
4. 38处 @ts-ignore 指令
5. ExecutionRecord 类型不匹配（14个文件）
6. 178处直接 process.env 变更无抽象
7. ScopedExternalAccessSandbox performHttpRequest 无超时
8. OIDC 服务 fetch 调用无超时
9. plugin-runtime-child 替换 globalThis.fetch 为 no-op

**中优先级**:
1. 30+处 as unknown as 双类型转换
2. rimraf extraneous 未声明
3. 21,998处 unlinkSync 无集中清理
4. 无统一 mocking 框架
5. Singleton 状态重置未标准化
6. 无显式 http.Agent 配置
7. 合同版本硬编码为字符串
8. Timestamp = string 丢失精度
9. 非空断言使用

**低优先级**:
1. 无分支覆盖率要求
2. API 路由 schema 版本未同步

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 72 | 87 | 39 | 198 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **109** | **182** | **85** | **376** |

### 优先修复建议

**立即处理 (本周内)**:
1. 添加 TypeScript 增量编译 (`incremental: true`)
2. 重构 npm scripts 避免重复构建
3. 减少 any 类型使用到 <50
4. 移除所有 @ts-ignore 指令
5. 修复 ExecutionRecord 类型不匹配
6. 添加 fetch 超时到 OIDC 和 sandbox

**短期内处理 (本月内)**:
1. 创建集中的测试清理 utility
2. 建立统一的 mocking 框架
3. 标准化 singleton 重置机制
4. 添加 http.Agent 连接池配置
5. 提取合同版本到类型化常量

**长期规划**:
1. 实现完整的类型安全（无 any）
2. 建立测试基础设施标准
3. 实现构建缓存优化
4. 完善网络层超时和重试
5. 建立覆盖率门禁标准

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十四轮 - 日志事件与工作流Review）

### 发现的新问题

#### 1. [可观测性] [高严重] [StructuredLogEntry 无 requestId 字段]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - StructuredLogEntry 接口无 requestId 字段
  - 只有 correlationId，它回退到 traceId
  - 无法独立追踪单个 HTTP 请求
- **建议修复**: 
  1. 在 StructuredLogEntry 添加 requestId 字段
  2. 在 HTTP 中间件中生成和注入 requestId
  3. 确保 requestId 流经所有日志调用

#### 2. [可观测性] [高严重] [StructuredLogger data 字段无自动清洗]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - 如果开发者传递 `{ password: "..." }` 给 logger.info，密码会被记录
  - 无字段级脱敏机制
  - 依赖开发者手动排除敏感字段
- **建议修复**: 
  1. 添加 OBSERVE_OUTPUT_BLACKLIST 类似的黑名单
  2. 实现 redact/mask/sanitize 工具函数
  3. 在日志传输前自动清洗敏感字段

#### 3. [可观测性] [中严重] [日志中敏感数据无 PII 处理]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - 无 PII 重写工具
  - 开发者必须手动避免记录敏感数据
  - 存在意外记录敏感信息的风险
- **建议修复**: 
  1. 添加 PII 检测和重写工具
  2. 在文档中明确记录敏感字段列表
  3. 添加测试验证敏感数据不被记录

#### 4. [可观测性] [中严重] [59处 console.* 直接调用]
- **文件/路径**: 多个源文件
- **问题描述**: 
  - 应该使用 StructuredLogger
  - console 调用可能不被日志聚合系统收集
  - 难以追踪和关联
- **建议修复**: 
  1. 替换所有 console.* 为 StructuredLogger
  2. 添加 ESLint 规则禁止 console.*
  3. 保留少量允许的 CLI 输出

#### 5. [可观测性] [中严重] [部分 logger.warn 调用缺少结构化数据]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:122`
- **问题描述**: 
  ```typescript
  logger.warn("WebSocket connection rejected: missing subprotocol token")
  ```
  - 只有消息，无 data 对象
  - 难以关联和查询
- **建议修复**: 
  1. 添加 `{ data: { ... } }` 对象
  2. 确保所有日志包含上下文数据
  3. 添加日志审查工具

#### 6. [源代码] [高严重] [Harness while(true) 循环无硬性迭代上限]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:1442`
- **问题描述**: 
  ```typescript
  while (true) {
    // ...
    // 依赖 budget gate 和 guardrail vibration 退出
    // 如果两者都失败，循环永久继续
  }
  ```
  - 依赖 budget gate 和 guardrail vibration 退出
  - 无硬性迭代次数限制
  - 可能导致无限循环
- **建议修复**: 
  1. 添加硬性 maxIterations 上限
  2. 在达到上限时强制退出
  3. 记录详细的迭代统计用于调试

#### 7. [源代码] [高严重] [Oapeflir while(true) 循环可能无限重规划]
- **文件/路径**: `src/platform/five-plane-orchestration/oapeflir/oapeflir-loop-service.ts:355`
- **问题描述**: 
  - 如果 loopReplanDecision.shouldReplan 永不变为 false
  - 重规划永远无法减少计划错误
  - 可能导致无限循环
- **建议修复**: 
  1. 添加重规划次数上限
  2. 在达到上限时强制接受当前结果
  3. 添加重规划质量衰减检测

#### 8. [源代码] [中严重] [Recovery 流程允许终态转换到 paused]
- **文件/路径**: `src/platform/five-plane-execution/runtime-state-machine.ts:105-108`
- **问题描述**: 
  - completed, failed, cancelled, aborted 可转换到 paused 进行恢复
  - 创建恢复路径可能重新进入执行
  - 需要验证此行为的正确性
- **建议修复**: 
  1. 审查 recovery 转换的合法性
  2. 确保不会导致状态不一致
  3. 添加转换前置条件验证

#### 9. [国际化] [高严重] [API 错误消息硬编码英文]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/api-error.ts`
- **问题描述**: 
  ```typescript
  return new ApiError(404, "api.task_not_found", "Task not found.");
  ```
  - 错误消息硬编码英文
  - 无后端 i18n 机制
  - 非英文客户端收到不本地化的错误
- **建议修复**: 
  1. 使用错误码而非硬编码消息
  2. 客户端根据错误码本地化
  3. 或在后端添加 i18n 支持

#### 10. [国际化] [中严重] [UI 硬编码字符串未翻译]
- **文件/路径**: `ui/packages/features/conversation/src/web/index.tsx`
- **问题描述**: 
  - "Build Plan", "Execute", "Messages" 等按钮标签硬编码
  - 不使用 i18n 系统
- **建议修复**: 
  1. 使用 `translateFeatureCopy` 替换硬编码
  2. 确保所有用户可见文本通过 i18n
  3. 添加硬编码字符串检测

#### 11. [国际化] [中严重] [Arabic 目录不完整]
- **文件/路径**: `ui/packages/shared/i18n/src/catalogs/ar-SA.ts`
- **问题描述**: 
  - ar-SA 只有约18条消息
  - en-US 和 zh-CN 有约117条
  - 缺失的 key 返回 key 本身
- **建议修复**: 
  1. 补充 Arabic 翻译
  2. 确保所有 key 都有翻译
  3. 添加翻译完整性测试

#### 12. [国际化] [低严重] [translateFeatureCopy 无回退默认值]
- **文件/路径**: `ui/packages/shared/i18n/`
- **问题描述**: 
  - 如果 feature ID 不在 catalog 中
  - 返回 undefined title/summary
  - 无合理回退
- **建议修复**: 
  1. 添加回退到 key 或默认字符串
  2. 或使用 feature ID 作为显示名
  3. 确保 UI 不显示 undefined

#### 13. [运维] [高严重] [健康检查无 readiness/liveness 区分]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts`
- **问题描述**: 
  - /healthz, /v1/healthz, /health 返回相同报告
  - 无 readiness probe（可处理流量？）
  - 无 liveness probe（系统存活？）
  - canAcceptTraffic() 未通过 HTTP 暴露
- **建议修复**: 
  1. 添加 /ready 和 /live 端点
  2. Readiness 检查依赖项就绪
  3. Liveness 检查进程健康

#### 14. [运维] [高严重] [shutdown 期间健康检查存在竞态]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts`
- **问题描述**: 
  - getHealthReportAsync() 异步检查 dbWritable
  - shutdown 期间数据库连接可能关闭
  - 健康检查仍在传入
- **建议修复**: 
  1. 在 shutdown 时返回 unhealthy
  2. 添加 shutdown 状态标志
  3. 停止接受新的健康检查请求

#### 15. [运维] [高严重] [LeaderElectionService 未与 GracefulShutdown 集成]
- **文件/路径**: `src/platform/five-plane-execution/ha/leader-election-service.ts`
- **问题描述**: 
  - LeaderElectionService.stop() 处理优雅退位
  - 但未在全局 graceful shutdown 中注册
  - 需显式注册为 shutdown handler
- **建议修复**: 
  1. 将 LeaderElectionService 添加到 shutdown handlers
  2. 确保领导者在 shutdown 时让位
  3. 验证关闭顺序正确

#### 16. [运维] [中严重] [addHandler() 无显式排序约定]
- **文件/路径**: `src/platform/five-plane-execution/startup/graceful-shutdown.ts`
- **问题描述**: 
  - 虽然 handlers 逆序执行
  - 但无显式接口声明 handler 依赖关系
  - 关键 handlers（如关闭数据库连接）应最后运行
- **建议修复**: 
  1. 添加优先级或阶段参数
  2. 文档化关键 handler 顺序要求
  3. 添加顺序验证测试

#### 17. [运维] [中严重] [unref'd timers 可能不触发]
- **文件/路径**: `src/platform/five-plane-execution/startup/process-error-handlers.ts:106`
- **问题描述**: 
  - `setTimeout(...).unref()` 不保持事件循环活跃
  - 如果主线程空闲，强制退出可能不触发
- **建议修复**: 
  1. 移除 .unref() 除非确实需要
  2. 或确保有其他方式保持事件循环
  3. 添加超时触发验证测试

#### 18. [运维] [中严重] [StartupConsistencyChecker 阻塞流量但不暴露状态]
- **文件/路径**: `src/platform/five-plane-startup-plan.ts`
- **问题描述**: 
  - canAcceptTraffic() 在 P0 问题存在时返回 false
  - 但 _trafficBlocked 是内部状态
  - 负载均衡器访问 /healthz 仍会得到 "ok"
- **建议修复**: 
  1. 在健康报告中暴露 trafficBlocked 状态
  2. 或添加单独的健康端点指示就绪状态
  3. 确保负载均衡器在阻塞时不路由流量

#### 19. [事件系统] [中严重] [TypedEventBus vs EventEmitter 混用]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - 项目同时使用 TypedEventBus 和原生 EventEmitter
  - TypedEventBus 应该是标准
  - 混用可能导致类型安全问题
- **建议修复**: 
  1. 创建迁移计划统一使用 TypedEventBus
  2. 添加 ESLint 规则禁止原生 EventEmitter
  3. 在 CI 中检测混用情况

#### 20. [源代码] [低严重] [无事件排序保证]
- **文件/路径**: `src/platform/five-plane-state-evidence/events/`
- **问题描述**: 
  - 无明确的事件排序保证文档
  - 事件处理可能乱序
  - 依赖方需要处理乱序
- **建议修复**: 
  1. 文档化事件排序语义
  2. 如果需要排序，实现序列号机制
  3. 在事件处理中考虑乱序情况

### 总结

本次补充Review（第十四轮 - 日志事件与工作流Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. StructuredLogEntry 无 requestId 字段
2. StructuredLogger data 字段无自动清洗（敏感数据风险）
3. Harness while(true) 循环无硬性迭代上限
4. Oapeflir while(true) 循环可能无限重规划
5. API 错误消息硬编码英文（无 i18n）
6. 健康检查无 readiness/liveness 区分
7. shutdown 期间健康检查存在竞态
8. LeaderElectionService 未与 GracefulShutdown 集成

**中优先级**:
1. 日志中敏感数据无 PII 处理
2. 59处 console.* 直接调用
3. 部分 logger.warn 调用缺少结构化数据
4. Recovery 流程允许终态转换到 paused
5. UI 硬编码字符串未翻译
6. Arabic 目录不完整
7. addHandler() 无显式排序约定
8. unref'd timers 可能不触发
9. StartupConsistencyChecker 阻塞流量但不暴露状态
10. TypedEventBus vs EventEmitter 混用

**低优先级**:
1. translateFeatureCopy 无回退默认值
2. 无事件排序保证

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 68 | 80 | 37 | 185 |
| 测试 | 8 | 14 | 5 | 27 |
| 配置 | 10 | 38 | 24 | 72 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **103** | **171** | **82** | **356** |

### 优先修复建议

**立即处理 (本周内)**:
1. 在 StructuredLogEntry 添加 requestId 字段
2. 实现日志敏感字段自动清洗
3. 为工作流循环添加硬性迭代上限
4. 添加 readiness/liveness 健康检查端点
5. 将 LeaderElectionService 集成到 GracefulShutdown
6. 实现 API 错误消息 i18n 支持

**短期内处理 (本月内)**:
1. 替换所有 console.* 为 StructuredLogger
2. 补充 Arabic i18n 目录
3. 修复 UI 硬编码字符串
4. 统一 TypedEventBus 替代 EventEmitter
5. 解决 shutdown 竞态条件
6. 添加事件排序保证文档

**长期规划**:
1. 建立完整的 PII 处理框架
2. 实现日志完整性验证
3. 建立工作流安全循环检测
4. 完善健康检查和就绪探测
5. 建立国际化完整测试

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十五轮 - 构建测试与类型系统Review）

### 发现的新问题

#### 1. [构建] [高严重] [无 TypeScript 增量编译缓存]
- **文件/路径**: `tsconfig.json`
- **问题描述**: 
  - 无 `incremental: true` 配置
  - 无 `tsbuildinfo` 文件
  - 每次构建是完整重建（9.6秒）
  - TypeScript 无法使用增量编译
- **建议修复**: 
  1. 在 tsconfig.json 添加 `"incremental": true`
  2. 添加 `"tsbuildinfo": ".tsbuildinfo"` 到 .gitignore
  3. 实现项目引用以支持并行构建

#### 2. [构建] [高严重] [76个 npm scripts 大部分重复构建]
- **文件/路径**: `package.json`
- **问题描述**: 
  - 40+ CLI scripts 都运行 `npm run build && node ...`
  - 每个脚本前都执行完整重建
  - 无法直接运行预构建的 CLI
  - 20+ stable:* 脚本遵循相同模式
- **建议修复**: 
  1. 创建 `build:cli` 脚本构建一次
  2. 让 CLI 脚本使用预构建的 dist 文件
  3. 添加 npm run dev 或类似的无需重建的执行方式

#### 3. [源代码] [高严重] [168处 any 类型使用]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - `stable-runner-factory.ts`: `opts: any` -> `any`
  - `billing-service-async.ts`: `input: any` 方法签名
  - 重试/超时工具使用 `(...args: any[]) => Promise<any>`
  - 绕过类型安全
- **建议修复**: 
  1. 使用泛型替代 any
  2. 替换为具体的输入类型
  3. 添加 ESLint 规则禁止 any

#### 4. [源代码] [高严重] [38处 @ts-ignore 指令]
- **文件/路径**: 多个文件
- **问题描述**: 
  - 14个 stability 相关文件有 `@ts-ignore ExecutionRecord type mismatch`
  - 5个 harness-sdk 文件有 `Partial<HarnessRun> doesn't have all required properties`
  - 2个文件有 `exactOptionalPropertyTypes` 问题
- **建议修复**: 
  1. 修复 ExecutionRecord 类型不匹配
  2. 完成 HarnessRun 类型的必需属性
  3. 移除 @ts-ignore 使用

#### 5. [源代码] [高严重] [ExecutionRecord 类型不匹配]
- **文件/路径**: `src/platform/stability/stable-evidence-bundle-support.ts:783` 等14个文件
- **问题描述**: 
  - 插入 execution record 时 TypeScript 拒绝类型
  - `ExecutionRecord` 接口要求新字段但 insert 对象缺失
  - 或 store 的 insert 方法接受不同类型
- **建议修复**: 
  1. 对齐 ExecutionRecord 接口和 store 类型
  2. 添加缺失字段到 insert 对象
  3. 或调整 store 的 insert 方法类型签名

#### 6. [源代码] [中严重] [30+处 as unknown as 双类型转换]
- **文件/路径**: `division-loader.ts`, `domain-baseline-catalog.ts`, `harness-sdk`
- **问题描述**: 
  - 完全绕过类型安全
  - 通常是类型架构问题的信号
- **建议修复**: 
  1. 修复上游类型问题
  2. 使用更具体的类型断言
  3. 重构类型层次结构

#### 7. [构建] [中严重] [rimraf  extraneous - 未在 package.json 声明]
- **文件/路径**: `node_modules/rimraf`
- **问题描述**: 
  - rimraf@6.1.3 存在于 node_modules 但未在 package.json 声明
  - 可能导致依赖问题
- **建议修复**: 
  1. 添加到 dependencies 或 devDependencies
  2. 或移除并使用其他清理方式

#### 8. [测试] [高严重] [178处直接 process.env 变更无抽象]
- **文件/路径**: `tests/unit/domains/registry/plugin-spi-registry.test.ts:640-687`
- **问题描述**: 
  - 直接读取/写入 process.env
  - 在 beforeEach/afterEach 中手动保存/恢复
  - 无 helper 抽象
- **建议修复**: 
  1. 创建 env helper 函数
  2. 统一保存和恢复模式
  3. 添加测试隔离验证

#### 9. [测试] [高严重] [21,998处 unlinkSync 调用无集中清理]
- **文件/路径**: 多个测试文件
- **问题描述**: 
  - 每个测试重新实现清理逻辑
  - 无集中清理 utility
  - 可能导致 EBUSY (Windows) 或并发访问问题
- **建议修复**: 
  1. 创建集中的测试清理 utility
  2. 使用 afterEach 确保清理
  3. 考虑使用临时目录

#### 10. [测试] [中严重] [无统一 mocking 框架]
- **文件/路径**: `tests/` 多个目录
- **问题描述**: 
  - 10,579 处 vi.fn, jest.mock, sinon 等调用
  - 无统一的 mocking 框架可见
  - 模式不一致
- **建议修复**: 
  1. 建立统一的 mock factory
  2. 标准化 mock 模式
  3. 添加 mocking 最佳实践文档

#### 11. [测试] [中严重] [Singleton 状态重置未标准化]
- **文件/路径**: `tests/unit/platform/five-plane-execution/dispatcher.test.ts`
- **问题描述**: 
  - 测试通过特定函数重置 singletons
  - 每个测试重复自己的 reset 逻辑
  - 无统一的 resetAllSingletons()
- **建议修复**: 
  1. 创建统一的 singleton reset 机制
  2. 在 afterEach 中自动调用
  3. 文档化 singleton 重置要求

#### 12. [测试] [低严重] [无分支覆盖率要求]
- **文件/路径**: `.c8rc.json`
- **问题描述**: 
  - `"100": false` - 无覆盖率要求
  - 只测量源代码，不测量测试文件
- **建议修复**: 
  1. 添加分支覆盖率阈值
  2. 在 CI 中强制执行覆盖率 gate
  3. 关注关键代码路径

#### 13. [网络] [高严重] [ScopedExternalAccessSandbox performHttpRequest 无超时]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/scoped-external-access-sandbox.ts:289`
- **问题描述**: 
  - fetch 调用无 timeout 参数
  - 挂起的请求可能永久阻塞
- **建议修复**: 
  1. 添加 AbortController timeout
  2. 使用标准的超时模式
  3. 记录超时配置

#### 14. [网络] [高严重] [OIDC 服务 fetch 调用无超时]
- **文件/路径**: `src/org-governance/sso-scim/oidc/oidc-service.ts:565,272`
- **问题描述**: 
  - OIDC token 交换无 retry logic
  - 无 timeout/abort signal
  - 认证流程可能挂起
- **建议修复**: 
  1. 添加 fetch timeout
  2. 添加 retry logic
  3. 使用标准的超时配置

#### 15. [网络] [高严重] [plugin-runtime-child 替换 globalThis.fetch 为 no-op]
- **文件/路径**: `src/domains/registry/plugin-runtime-child.ts:112`
- **问题描述**: 
  ```typescript
  globalThis.fetch = (async () => {}) as unknown as typeof globalThis.fetch;
  ```
  - 插件上下文中完全替换 fetch 为空函数
  - 插件的所有出站 HTTP 请求都会失败
  - 看起来是有意为之但可能造成问题
- **建议修复**: 
  1. 确认这是有意行为
  2. 文档化 fetch 替换行为
  3. 确保不会意外影响其他代码

#### 16. [网络] [中严重] [无显式 http.Agent 配置]
- **文件/路径**: 全局
- **问题描述**: 
  - 无显式的 http.Agent/https.Agent 配置
  - 依赖 Node.js 默认连接池
  - 无 maxSockets 或 maxConnections 配置
  - 高吞吐场景可能出问题
- **建议修复**: 
  1. 添加 http Agent 配置
  2. 文档化连接池大小
  3. 监控连接使用情况

#### 17. [序列化] [中严重] [合同版本硬编码为字符串字面量]
- **文件/路径**: `src/platform/contracts/inter-plane-contract-gateway.ts:184`
- **问题描述**: 
  - `schemaVersion: "v4.3"` 硬编码
  - 难以追踪合同版本演变
- **建议修复**: 
  1. 提取到类型化常量或 enum
  2. 集中管理合同版本
  3. 添加版本兼容性检查

#### 18. [序列化] [低严重] [Timestamp = string 丢失精度]
- **文件/路径**: `src/platform/contracts/types/domain/primitives.ts:10`
- **问题描述**: 
  - `type Timestamp = string` 无类型精度
  - ISO 8601 字符串无时区信息
- **建议修复**: 
  1. 使用 Date 对象或 branded string 类型
  2. 明确时区处理约定
  3. 文档化时间戳语义

#### 19. [源代码] [中严重] [29处非空断言 !.]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - 使用 `!.` 断言非空
  - 可能导致运行时错误
- **建议修复**: 
  1. 使用可选链 `?.` 或空值合并 `??`
  2. 添加适当的 null 检查
  3. 减少非空断言使用

#### 20. [API] [中严重] [API 路由 schema 版本硬编码]
- **文件/路径**: `src/platform/five-plane-interface/api/federation-routing-service.ts`
- **问题描述**: 
  - Zod schema 验证存在
  - 但 schema 版本未与 contract 版本同步
- **建议修复**: 
  1. 从 contract 版本派生 schema 版本
  2. 添加版本一致性检查
  3. 文档化版本关系

### 总结

本次补充Review（第十五轮 - 构建测试与类型系统Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. 无 TypeScript 增量编译缓存（每次完整重建9.6秒）
2. 76个 npm scripts 大部分重复构建
3. 168处 any 类型使用（绕过类型安全）
4. 38处 @ts-ignore 指令
5. ExecutionRecord 类型不匹配（14个文件）
6. 178处直接 process.env 变更无抽象
7. ScopedExternalAccessSandbox performHttpRequest 无超时
8. OIDC 服务 fetch 调用无超时
9. plugin-runtime-child 替换 globalThis.fetch 为 no-op

**中优先级**:
1. 30+处 as unknown as 双类型转换
2. rimraf extraneous 未声明
3. 21,998处 unlinkSync 无集中清理
4. 无统一 mocking 框架
5. Singleton 状态重置未标准化
6. 无显式 http.Agent 配置
7. 合同版本硬编码为字符串
8. Timestamp = string 丢失精度
9. 非空断言使用

**低优先级**:
1. 无分支覆盖率要求
2. API 路由 schema 版本未同步

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 72 | 87 | 39 | 198 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **109** | **182** | **85** | **376** |

### 优先修复建议

**立即处理 (本周内)**:
1. 添加 TypeScript 增量编译 (`incremental: true`)
2. 重构 npm scripts 避免重复构建
3. 减少 any 类型使用到 <50
4. 移除所有 @ts-ignore 指令
5. 修复 ExecutionRecord 类型不匹配
6. 添加 fetch 超时到 OIDC 和 sandbox

**短期内处理 (本月内)**:
1. 创建集中的测试清理 utility
2. 建立统一的 mocking 框架
3. 标准化 singleton 重置机制
4. 添加 http.Agent 连接池配置
5. 提取合同版本到类型化常量

**长期规划**:
1. 实现完整的类型安全（无 any）
2. 建立测试基础设施标准
3. 实现构建缓存优化
4. 完善网络层超时和重试
5. 建立覆盖率门禁标准

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十三轮 - 缓存会话与实时通信Review）

### 发现的新问题

#### 1. [安全] [严重] [CORS allowedMethods 缺少 PUT/PATCH/DELETE]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/response-hardening.ts:12-14`
- **问题描述**: 
  ```typescript
  export const DEFAULT_CORS_CONFIG: CorsConfig = {
    allowedMethods: ["GET", "POST", "OPTIONS"],  // 缺少 PUT, PATCH, DELETE
  ```
  - 浏览器 CORS preflight 会拒绝 PUT/PATCH/DELETE 请求
  - 但路由中有 PATCH /v1/tasks/:id, DELETE endpoints
- **建议修复**: 
  1. 添加 PUT, PATCH, DELETE 到 allowedMethods
  2. 确保 CORS 配置与应用路由方法一致
  3. 添加测试验证所有 HTTP 方法的 CORS preflight

#### 2. [安全] [严重] [Mission Routes 错误响应包装不当]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/mission-routes.ts:101,118-124`
- **问题描述**: 
  ```typescript
  return buildJsonResponse(ctx.requestId, 404, { error: { code: "MISSION_NOT_FOUND" } });
  ```
  - 使用 buildJsonResponse（成功包装）返回错误
  - 错误被包装在成功信封 `{requestId, data: {error: {...}}}` 中
  - 违反 REST 规范
- **建议修复**: 
  1. 使用 buildJsonErrorResponse 处理错误
  2. 确保所有错误路径使用正确的错误信封
  3. 添加 API 一致性测试

#### 3. [源代码] [高严重] [WebSocketBridge 无最大连接数限制]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:103-107`
- **问题描述**: 
  ```typescript
  this.wss = new WebSocketServer({
    server,
    path: WS_PATH,
    maxPayload: options.maxPayloadBytes ?? DEFAULT_MAX_PAYLOAD_BYTES,
    // 无 maxConnections 限制！
  });
  ```
  - 与 dashboard-websocket-server.ts 的 maxClients: 1000 不同
  - 恶意客户端可耗尽服务器资源
- **建议修复**: 
  1. 添加 maxConnections 限制
  2. 实现连接限制拒绝策略
  3. 添加连接计数指标

#### 4. [源代码] [高严重] [pendingAcks 在断开连接时未清理]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:268-282`
- **问题描述**: 
  - handleDisconnection 清理 subscribedTasks, taskSubscribers, slowConsumers
  - 但 pendingAcks Map 未清理
  - 断开的客户端未确认的消息永久留在内存
- **建议修复**: 
  1. 在 handleDisconnection 中删除所有 pendingAcks 条目
  2. 考虑添加超时机制自动清理未确认消息
  3. 记录清理操作的审计日志

#### 5. [安全] [高严重] [硬编码回退密钥]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/audit-event-integrity.ts:41`
- **问题描述**: 
  ```typescript
  const AUDIT_INTEGRITY_HMAC_KEY = process.env["AA_AUDIT_INTEGRITY_HMAC_KEY"] ?? "audit-integrity-secret-key-32-bytes!";
  ```
  - 占位符密钥用作回退
  - 如果环境变量未设置，使用不安全密钥
- **建议修复**: 
  1. 移除回退值，要求环境变量必须设置
  2. 在启动时验证密钥存在且足够强
  3. 如果密钥缺失导致启动失败

#### 6. [配置] [高严重] [config/runtime/test.json 缺少超时配置]
- **文件/路径**: `config/runtime/test.json`
- **问题描述**: 
  - 只有3个字段，缺少 maxConcurrentTasks, defaultTaskTimeoutMs, defaultStepTimeoutMs
  - 回退到默认值可能不适合测试环境
- **建议修复**: 
  1. 添加完整的 timeout/tuning 配置
  2. 确保测试环境有合理的默认值
  3. 文档化必需的配置字段

#### 7. [配置] [高严重] [prod.json 限制过严 - maxConcurrentTasks=1]
- **文件/路径**: `config/runtime/prod.json`
- **问题描述**: 
  - maxConcurrentTasks: 1（vs dev:8, staging:4, pre-prod:6）
  - defaultTaskTimeoutMs: 120000（vs dev:600000）
  - defaultStepTimeoutMs: 60000（vs dev:180000）
  - 生产环境严重受限，可能影响吞吐量
- **建议修复**: 
  1. 评估并调整生产限制
  2. 与业务需求匹配
  3. 添加生产容量测试

#### 8. [源代码] [高严重] [多个服务存在无界 Map 无驱逐策略]
- **文件/路径**: 多个域服务
- **问题描述**: 
  - `domain-knowledge-schema-service.ts:73-75`: schemas, sourceContent, sourceTimestamps 无限制
  - `domain-eval-framework-service.ts:91-95`: frameworks, qualityAxes 等无限制
  - `domain-recipe-service.ts:67-68`: recipes, versions 无限制
  - `domain-risk-profile-service.ts:51`: profiles 无限制
  - Session Maps (session-management.ts:83-89) 无自动清理
- **建议修复**: 
  1. 为所有 Map 实现 LRU 或 TTL 驱逐策略
  2. 添加后台清理任务
  3. 添加大小监控和告警

#### 9. [源代码] [中严重] [taskEventHistory Map 永不清理]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:91,548-555`
- **问题描述**: 
  - taskEventHistory 限制单任务历史为 200 条
  - 但 Map 本身从不清理
  - 取消订阅的任务历史永久保留
- **建议修复**: 
  1. 实现后台清理无订阅者任务的历史
  2. 添加任务订阅者监控
  3. 考虑使用 WeakMap 替代

#### 10. [配置] [中严重] [安全配置 drift - remoteWorkerRegistration 缺失]
- **文件/路径**: `config/security/dev.json`, `staging.json`, `pre-prod.json`
- **问题描述**: 
  - default.json 有完整的 remoteWorkerRegistration 配置
  - dev/staging/pre-prod 只有 approvalMode
  - prod 有 approvalMode 但无 remoteWorkerRegistration
  - 安全配置不一致
- **建议修复**: 
  1. 统一所有环境的 remoteWorkerRegistration
  2. 添加安全配置验证
  3. 确保最低安全基线

#### 11. [API] [中严重] [OpenAPI 端点响应格式不一致]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/utils.ts:207-215`
- **问题描述**: 
  - `/v1/openapi.json` 使用 buildJsonDocumentResponse（无包装）
  - 其他端点使用 buildJsonResponse（{requestId, data} 包装）
  - 客户端体验不一致
- **建议修复**: 
  1. 统一响应信封格式
  2. 文档化响应格式规范
  3. 添加响应格式验证测试

#### 12. [源代码] [中严重] [Redis 队列缺少 idempotency 索引支持]
- **文件/路径**: `src/platform/five-plane-execution/queue/redis-queue-adapter.ts`
- **问题描述**: 
  - SQLite 有部分唯一索引支持 idempotency
  - Redis 实现使用哈希索引但实现不完整
  - 可能导致重复消息
- **建议修复**: 
  1. 完善 Redis idempotency 实现
  2. 添加唯一索引验证
  3. 确保与 SQLite 行为一致

#### 13. [源代码] [中严重] [请求去重中间件使用内存存储]
- **文件/路径**: `src/platform/five-plane-interface/api/middleware/request-deduplication.ts`
- **问题描述**: 
  - 纯内存滑动窗口（Map<DeduplicationKey, DeduplicationEntry[]>）
  - 无外部存储
  - 多实例部署不共享
  - 重启后去重状态丢失
- **建议修复**: 
  1. 使用 Redis 替代内存存储
  2. 支持分布式去重
  3. 持久化去重状态

#### 14. [源代码] [中严重] [Cache 无 stampede 保护]
- **文件/路径**: 多个 cache 实现
- **问题描述**: 
  - MemoryCacheStore, ExperienceCacheService 无锁
  - 缓存未命中时可发生 thundering herd
  - 高并发下可能导致数据库过载
- **建议修复**: 
  1. 实现 single-flight 模式
  2. 添加请求排队机制
  3. 使用分布式锁保护缓存更新

#### 15. [源代码] [中严重] [EvidenceService eviction 仅在插入时触发]
- **文件/路径**: `src/platform/five-plane-state-evidence/memory/evidence-service.ts:202`
- **问题描述**: 
  - 清理仅在 record() 调用时触发
  - 空闲时无后台清理
  - 可能导致内存持续增长
- **建议修复**: 
  1. 添加定期后台清理任务
  2. 使用独立的清理线程
  3. 添加内存使用监控

#### 16. [源代码] [低严重] [无连接指标暴露]
- **文件/路径**: WebSocket 服务器
- **问题描述**: 
  - getClientCount() 存在但未通过 HTTP 端点暴露
  - 无 pendingAcks 队列深度指标
  - 无连接建立告警
- **建议修复**: 
  1. 通过 metrics 端点暴露连接指标
  2. 添加 pendingAcks 队列监控
  3. 添加连接数异常告警

#### 17. [源代码] [低严重] [无空闲客户端超时]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts`
- **问题描述**: 
  - 仅在心跳 Sweep 时检查 isAlive
  - 无 per-client 独立于心跳的空闲超时
  - 认证后从不发送消息的客户端只能通过心跳失败检测
- **建议修复**: 
  1. 添加 per-client idle timeout
  2. 独立于心跳间隔
  3. 配置可调整

#### 18. [源代码] [低严重] [订阅限制是 per-client 而非全局]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:244`
- **问题描述**: 
  - MAX_SUBSCRIPTIONS_PER_CLIENT = 100
  - 恶意客户端可订阅 100 个任务
  - 无全局任务订阅者限制
- **建议修复**: 
  1. 添加全局任务订阅者限制
  2. 实现 per-task 订阅者上限
  3. 添加反滥用检测

#### 19. [配置] [低严重] [config/runtime 无版本验证机制]
- **文件/路径**: `config/runtime/*.json`
- **问题描述**: 
  - 配置有 "version": "v4.3"
  - 但无 schema 版本验证
  - 加载时可能接受不兼容配置
- **建议修复**: 
  1. 添加 JSON Schema 验证
  2. 实现版本兼容性检查
  3. 启动时验证配置完整性

#### 20. [源代码] [低严重] [ImprovementCandidateRegistry splice 是 O(n)]
- **文件/路径**: `src/platform/five-plane-orchestration/improve/improvement-candidate-registry.ts:243`
- **问题描述**: 
  - accessOrder 数组的 splice 操作是 O(n)
  - 高频访问时可能性能问题
- **建议修复**: 
  1. 使用 LinkedList 替代数组
  2. 或使用 Map 维护访问顺序
  3. 性能测试验证

### 总结

本次补充Review（第十三轮 - 缓存会话与实时通信Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. CORS allowedMethods 缺少 PUT/PATCH/DELETE（浏览器请求会失败）
2. Mission Routes 错误响应包装不当
3. WebSocketBridge 无最大连接数限制（DoS风险）
4. pendingAcks 断开连接时未清理（内存泄漏）
5. 硬编码回退密钥（安全漏洞）
6. config/runtime/test.json 缺少超时配置
7. prod.json maxConcurrentTasks=1 限制过严
8. 多个服务存在无界 Map（内存泄漏风险）

**中优先级**:
1. taskEventHistory Map 永不清理
2. 安全配置 drift（remoteWorkerRegistration 缺失）
3. OpenAPI 端点响应格式不一致
4. Redis 队列 idempotency 实现不完整
5. 请求去重中间件使用内存存储（多实例不共享）
6. Cache 无 stampede 保护
7. EvidenceService eviction 仅在插入时触发

**低优先级**:
1. 无连接指标暴露
2. 无空闲客户端超时
3. 订阅限制是 per-client 而非全局
4. config/runtime 无版本验证机制
5. ImprovementCandidateRegistry splice O(n)

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 64 | 74 | 35 | 173 |
| 测试 | 8 | 14 | 5 | 27 |
| 配置 | 10 | 38 | 24 | 72 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 7 | 3 | 12 |
| UI | 0 | 3 | 3 | 6 |
| 部署 | 1 | 10 | 6 | 17 |
| **合计** | **99** | **161** | **79** | **339** |

### 优先修复建议

**立即处理 (本周内)**:
1. 修复 CORS allowedMethods 添加 PUT/PATCH/DELETE
2. 修复 Mission Routes 错误响应格式
3. 为 WebSocketBridge 添加最大连接数限制
4. 清理 pendingAcks 和 taskEventHistory
5. 移除硬编码回退密钥
6. 修复 config/runtime/test.json 配置
7. 评估并调整 prod 并发限制

**短期内处理 (本月内)**:
1. 统一安全配置（remoteWorkerRegistration）
2. 实现无界 Map 的 LRU/TTL 驱逐
3. 统一 API 响应信封格式
4. 完善 Redis idempotency 实现
5. 添加分布式去重中间件
6. 实现 cache stampede 保护

**长期规划**:
1. 建立完整的连接管理和监控
2. 实现后台清理任务框架
3. 添加配置 schema 验证
4. 优化高频繁操作的数据结构
5. 建立内存使用基线和告警

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十五轮 - 构建测试与类型系统Review）

### 发现的新问题

#### 1. [构建] [高严重] [无 TypeScript 增量编译缓存]
- **文件/路径**: `tsconfig.json`
- **问题描述**: 
  - 无 `incremental: true` 配置
  - 无 `tsbuildinfo` 文件
  - 每次构建是完整重建（9.6秒）
  - TypeScript 无法使用增量编译
- **建议修复**: 
  1. 在 tsconfig.json 添加 `"incremental": true`
  2. 添加 `"tsbuildinfo": ".tsbuildinfo"` 到 .gitignore
  3. 实现项目引用以支持并行构建

#### 2. [构建] [高严重] [76个 npm scripts 大部分重复构建]
- **文件/路径**: `package.json`
- **问题描述**: 
  - 40+ CLI scripts 都运行 `npm run build && node ...`
  - 每个脚本前都执行完整重建
  - 无法直接运行预构建的 CLI
  - 20+ stable:* 脚本遵循相同模式
- **建议修复**: 
  1. 创建 `build:cli` 脚本构建一次
  2. 让 CLI 脚本使用预构建的 dist 文件
  3. 添加 npm run dev 或类似的无需重建的执行方式

#### 3. [源代码] [高严重] [168处 any 类型使用]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - `stable-runner-factory.ts`: `opts: any` -> `any`
  - `billing-service-async.ts`: `input: any` 方法签名
  - 重试/超时工具使用 `(...args: any[]) => Promise<any>`
  - 绕过类型安全
- **建议修复**: 
  1. 使用泛型替代 any
  2. 替换为具体的输入类型
  3. 添加 ESLint 规则禁止 any

#### 4. [源代码] [高严重] [38处 @ts-ignore 指令]
- **文件/路径**: 多个文件
- **问题描述**: 
  - 14个 stability 相关文件有 `@ts-ignore ExecutionRecord type mismatch`
  - 5个 harness-sdk 文件有 `Partial<HarnessRun> doesn't have all required properties`
  - 2个文件有 `exactOptionalPropertyTypes` 问题
- **建议修复**: 
  1. 修复 ExecutionRecord 类型不匹配
  2. 完成 HarnessRun 类型的必需属性
  3. 移除 @ts-ignore 使用

#### 5. [源代码] [高严重] [ExecutionRecord 类型不匹配]
- **文件/路径**: `src/platform/stability/stable-evidence-bundle-support.ts:783` 等14个文件
- **问题描述**: 
  - 插入 execution record 时 TypeScript 拒绝类型
  - `ExecutionRecord` 接口要求新字段但 insert 对象缺失
  - 或 store 的 insert 方法接受不同类型
- **建议修复**: 
  1. 对齐 ExecutionRecord 接口和 store 类型
  2. 添加缺失字段到 insert 对象
  3. 或调整 store 的 insert 方法类型签名

#### 6. [源代码] [中严重] [30+处 as unknown as 双类型转换]
- **文件/路径**: `division-loader.ts`, `domain-baseline-catalog.ts`, `harness-sdk`
- **问题描述**: 
  - 完全绕过类型安全
  - 通常是类型架构问题的信号
- **建议修复**: 
  1. 修复上游类型问题
  2. 使用更具体的类型断言
  3. 重构类型层次结构

#### 7. [构建] [中严重] [rimraf  extraneous - 未在 package.json 声明]
- **文件/路径**: `node_modules/rimraf`
- **问题描述**: 
  - rimraf@6.1.3 存在于 node_modules 但未在 package.json 声明
  - 可能导致依赖问题
- **建议修复**: 
  1. 添加到 dependencies 或 devDependencies
  2. 或移除并使用其他清理方式

#### 8. [测试] [高严重] [178处直接 process.env 变更无抽象]
- **文件/路径**: `tests/unit/domains/registry/plugin-spi-registry.test.ts:640-687`
- **问题描述**: 
  - 直接读取/写入 process.env
  - 在 beforeEach/afterEach 中手动保存/恢复
  - 无 helper 抽象
- **建议修复**: 
  1. 创建 env helper 函数
  2. 统一保存和恢复模式
  3. 添加测试隔离验证

#### 9. [测试] [高严重] [21,998处 unlinkSync 调用无集中清理]
- **文件/路径**: 多个测试文件
- **问题描述**: 
  - 每个测试重新实现清理逻辑
  - 无集中清理 utility
  - 可能导致 EBUSY (Windows) 或并发访问问题
- **建议修复**: 
  1. 创建集中的测试清理 utility
  2. 使用 afterEach 确保清理
  3. 考虑使用临时目录

#### 10. [测试] [中严重] [无统一 mocking 框架]
- **文件/路径**: `tests/` 多个目录
- **问题描述**: 
  - 10,579 处 vi.fn, jest.mock, sinon 等调用
  - 无统一的 mocking 框架可见
  - 模式不一致
- **建议修复**: 
  1. 建立统一的 mock factory
  2. 标准化 mock 模式
  3. 添加 mocking 最佳实践文档

#### 11. [测试] [中严重] [Singleton 状态重置未标准化]
- **文件/路径**: `tests/unit/platform/five-plane-execution/dispatcher.test.ts`
- **问题描述**: 
  - 测试通过特定函数重置 singletons
  - 每个测试重复自己的 reset 逻辑
  - 无统一的 resetAllSingletons()
- **建议修复**: 
  1. 创建统一的 singleton reset 机制
  2. 在 afterEach 中自动调用
  3. 文档化 singleton 重置要求

#### 12. [测试] [低严重] [无分支覆盖率要求]
- **文件/路径**: `.c8rc.json`
- **问题描述**: 
  - `"100": false` - 无覆盖率要求
  - 只测量源代码，不测量测试文件
- **建议修复**: 
  1. 添加分支覆盖率阈值
  2. 在 CI 中强制执行覆盖率 gate
  3. 关注关键代码路径

#### 13. [网络] [高严重] [ScopedExternalAccessSandbox performHttpRequest 无超时]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/scoped-external-access-sandbox.ts:289`
- **问题描述**: 
  - fetch 调用无 timeout 参数
  - 挂起的请求可能永久阻塞
- **建议修复**: 
  1. 添加 AbortController timeout
  2. 使用标准的超时模式
  3. 记录超时配置

#### 14. [网络] [高严重] [OIDC 服务 fetch 调用无超时]
- **文件/路径**: `src/org-governance/sso-scim/oidc/oidc-service.ts:565,272`
- **问题描述**: 
  - OIDC token 交换无 retry logic
  - 无 timeout/abort signal
  - 认证流程可能挂起
- **建议修复**: 
  1. 添加 fetch timeout
  2. 添加 retry logic
  3. 使用标准的超时配置

#### 15. [网络] [高严重] [plugin-runtime-child 替换 globalThis.fetch 为 no-op]
- **文件/路径**: `src/domains/registry/plugin-runtime-child.ts:112`
- **问题描述**: 
  ```typescript
  globalThis.fetch = (async () => {}) as unknown as typeof globalThis.fetch;
  ```
  - 插件上下文中完全替换 fetch 为空函数
  - 插件的所有出站 HTTP 请求都会失败
  - 看起来是有意为之但可能造成问题
- **建议修复**: 
  1. 确认这是有意行为
  2. 文档化 fetch 替换行为
  3. 确保不会意外影响其他代码

#### 16. [网络] [中严重] [无显式 http.Agent 配置]
- **文件/路径**: 全局
- **问题描述**: 
  - 无显式的 http.Agent/https.Agent 配置
  - 依赖 Node.js 默认连接池
  - 无 maxSockets 或 maxConnections 配置
  - 高吞吐场景可能出问题
- **建议修复**: 
  1. 添加 http Agent 配置
  2. 文档化连接池大小
  3. 监控连接使用情况

#### 17. [序列化] [中严重] [合同版本硬编码为字符串字面量]
- **文件/路径**: `src/platform/contracts/inter-plane-contract-gateway.ts:184`
- **问题描述**: 
  - `schemaVersion: "v4.3"` 硬编码
  - 难以追踪合同版本演变
- **建议修复**: 
  1. 提取到类型化常量或 enum
  2. 集中管理合同版本
  3. 添加版本兼容性检查

#### 18. [序列化] [低严重] [Timestamp = string 丢失精度]
- **文件/路径**: `src/platform/contracts/types/domain/primitives.ts:10`
- **问题描述**: 
  - `type Timestamp = string` 无类型精度
  - ISO 8601 字符串无时区信息
- **建议修复**: 
  1. 使用 Date 对象或 branded string 类型
  2. 明确时区处理约定
  3. 文档化时间戳语义

#### 19. [源代码] [中严重] [29处非空断言 !.]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - 使用 `!.` 断言非空
  - 可能导致运行时错误
- **建议修复**: 
  1. 使用可选链 `?.` 或空值合并 `??`
  2. 添加适当的 null 检查
  3. 减少非空断言使用

#### 20. [API] [中严重] [API 路由 schema 版本硬编码]
- **文件/路径**: `src/platform/five-plane-interface/api/federation-routing-service.ts`
- **问题描述**: 
  - Zod schema 验证存在
  - 但 schema 版本未与 contract 版本同步
- **建议修复**: 
  1. 从 contract 版本派生 schema 版本
  2. 添加版本一致性检查
  3. 文档化版本关系

### 总结

本次补充Review（第十五轮 - 构建测试与类型系统Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. 无 TypeScript 增量编译缓存（每次完整重建9.6秒）
2. 76个 npm scripts 大部分重复构建
3. 168处 any 类型使用（绕过类型安全）
4. 38处 @ts-ignore 指令
5. ExecutionRecord 类型不匹配（14个文件）
6. 178处直接 process.env 变更无抽象
7. ScopedExternalAccessSandbox performHttpRequest 无超时
8. OIDC 服务 fetch 调用无超时
9. plugin-runtime-child 替换 globalThis.fetch 为 no-op

**中优先级**:
1. 30+处 as unknown as 双类型转换
2. rimraf extraneous 未声明
3. 21,998处 unlinkSync 无集中清理
4. 无统一 mocking 框架
5. Singleton 状态重置未标准化
6. 无显式 http.Agent 配置
7. 合同版本硬编码为字符串
8. Timestamp = string 丢失精度
9. 非空断言使用

**低优先级**:
1. 无分支覆盖率要求
2. API 路由 schema 版本未同步

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 72 | 87 | 39 | 198 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **109** | **182** | **85** | **376** |

### 优先修复建议

**立即处理 (本周内)**:
1. 添加 TypeScript 增量编译 (`incremental: true`)
2. 重构 npm scripts 避免重复构建
3. 减少 any 类型使用到 <50
4. 移除所有 @ts-ignore 指令
5. 修复 ExecutionRecord 类型不匹配
6. 添加 fetch 超时到 OIDC 和 sandbox

**短期内处理 (本月内)**:
1. 创建集中的测试清理 utility
2. 建立统一的 mocking 框架
3. 标准化 singleton 重置机制
4. 添加 http.Agent 连接池配置
5. 提取合同版本到类型化常量

**长期规划**:
1. 实现完整的类型安全（无 any）
2. 建立测试基础设施标准
3. 实现构建缓存优化
4. 完善网络层超时和重试
5. 建立覆盖率门禁标准

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十四轮 - 日志事件与工作流Review）

### 发现的新问题

#### 1. [可观测性] [高严重] [StructuredLogEntry 无 requestId 字段]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - StructuredLogEntry 接口无 requestId 字段
  - 只有 correlationId，它回退到 traceId
  - 无法独立追踪单个 HTTP 请求
- **建议修复**: 
  1. 在 StructuredLogEntry 添加 requestId 字段
  2. 在 HTTP 中间件中生成和注入 requestId
  3. 确保 requestId 流经所有日志调用

#### 2. [可观测性] [高严重] [StructuredLogger data 字段无自动清洗]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - 如果开发者传递 `{ password: "..." }` 给 logger.info，密码会被记录
  - 无字段级脱敏机制
  - 依赖开发者手动排除敏感字段
- **建议修复**: 
  1. 添加 OBSERVE_OUTPUT_BLACKLIST 类似的黑名单
  2. 实现 redact/mask/sanitize 工具函数
  3. 在日志传输前自动清洗敏感字段

#### 3. [可观测性] [中严重] [日志中敏感数据无 PII 处理]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - 无 PII 重写工具
  - 开发者必须手动避免记录敏感数据
  - 存在意外记录敏感信息的风险
- **建议修复**: 
  1. 添加 PII 检测和重写工具
  2. 在文档中明确记录敏感字段列表
  3. 添加测试验证敏感数据不被记录

#### 4. [可观测性] [中严重] [59处 console.* 直接调用]
- **文件/路径**: 多个源文件
- **问题描述**: 
  - 应该使用 StructuredLogger
  - console 调用可能不被日志聚合系统收集
  - 难以追踪和关联
- **建议修复**: 
  1. 替换所有 console.* 为 StructuredLogger
  2. 添加 ESLint 规则禁止 console.*
  3. 保留少量允许的 CLI 输出

#### 5. [可观测性] [中严重] [部分 logger.warn 调用缺少结构化数据]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:122`
- **问题描述**: 
  ```typescript
  logger.warn("WebSocket connection rejected: missing subprotocol token")
  ```
  - 只有消息，无 data 对象
  - 难以关联和查询
- **建议修复**: 
  1. 添加 `{ data: { ... } }` 对象
  2. 确保所有日志包含上下文数据
  3. 添加日志审查工具

#### 6. [源代码] [高严重] [Harness while(true) 循环无硬性迭代上限]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:1442`
- **问题描述**: 
  ```typescript
  while (true) {
    // ...
    // 依赖 budget gate 和 guardrail vibration 退出
    // 如果两者都失败，循环永久继续
  }
  ```
  - 依赖 budget gate 和 guardrail vibration 退出
  - 无硬性迭代次数限制
  - 可能导致无限循环
- **建议修复**: 
  1. 添加硬性 maxIterations 上限
  2. 在达到上限时强制退出
  3. 记录详细的迭代统计用于调试

#### 7. [源代码] [高严重] [Oapeflir while(true) 循环可能无限重规划]
- **文件/路径**: `src/platform/five-plane-orchestration/oapeflir/oapeflir-loop-service.ts:355`
- **问题描述**: 
  - 如果 loopReplanDecision.shouldReplan 永不变为 false
  - 重规划永远无法减少计划错误
  - 可能导致无限循环
- **建议修复**: 
  1. 添加重规划次数上限
  2. 在达到上限时强制接受当前结果
  3. 添加重规划质量衰减检测

#### 8. [源代码] [中严重] [Recovery 流程允许终态转换到 paused]
- **文件/路径**: `src/platform/five-plane-execution/runtime-state-machine.ts:105-108`
- **问题描述**: 
  - completed, failed, cancelled, aborted 可转换到 paused 进行恢复
  - 创建恢复路径可能重新进入执行
  - 需要验证此行为的正确性
- **建议修复**: 
  1. 审查 recovery 转换的合法性
  2. 确保不会导致状态不一致
  3. 添加转换前置条件验证

#### 9. [国际化] [高严重] [API 错误消息硬编码英文]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/api-error.ts`
- **问题描述**: 
  ```typescript
  return new ApiError(404, "api.task_not_found", "Task not found.");
  ```
  - 错误消息硬编码英文
  - 无后端 i18n 机制
  - 非英文客户端收到不本地化的错误
- **建议修复**: 
  1. 使用错误码而非硬编码消息
  2. 客户端根据错误码本地化
  3. 或在后端添加 i18n 支持

#### 10. [国际化] [中严重] [UI 硬编码字符串未翻译]
- **文件/路径**: `ui/packages/features/conversation/src/web/index.tsx`
- **问题描述**: 
  - "Build Plan", "Execute", "Messages" 等按钮标签硬编码
  - 不使用 i18n 系统
- **建议修复**: 
  1. 使用 `translateFeatureCopy` 替换硬编码
  2. 确保所有用户可见文本通过 i18n
  3. 添加硬编码字符串检测

#### 11. [国际化] [中严重] [Arabic 目录不完整]
- **文件/路径**: `ui/packages/shared/i18n/src/catalogs/ar-SA.ts`
- **问题描述**: 
  - ar-SA 只有约18条消息
  - en-US 和 zh-CN 有约117条
  - 缺失的 key 返回 key 本身
- **建议修复**: 
  1. 补充 Arabic 翻译
  2. 确保所有 key 都有翻译
  3. 添加翻译完整性测试

#### 12. [国际化] [低严重] [translateFeatureCopy 无回退默认值]
- **文件/路径**: `ui/packages/shared/i18n/`
- **问题描述**: 
  - 如果 feature ID 不在 catalog 中
  - 返回 undefined title/summary
  - 无合理回退
- **建议修复**: 
  1. 添加回退到 key 或默认字符串
  2. 或使用 feature ID 作为显示名
  3. 确保 UI 不显示 undefined

#### 13. [运维] [高严重] [健康检查无 readiness/liveness 区分]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts`
- **问题描述**: 
  - /healthz, /v1/healthz, /health 返回相同报告
  - 无 readiness probe（可处理流量？）
  - 无 liveness probe（系统存活？）
  - canAcceptTraffic() 未通过 HTTP 暴露
- **建议修复**: 
  1. 添加 /ready 和 /live 端点
  2. Readiness 检查依赖项就绪
  3. Liveness 检查进程健康

#### 14. [运维] [高严重] [shutdown 期间健康检查存在竞态]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts`
- **问题描述**: 
  - getHealthReportAsync() 异步检查 dbWritable
  - shutdown 期间数据库连接可能关闭
  - 健康检查仍在传入
- **建议修复**: 
  1. 在 shutdown 时返回 unhealthy
  2. 添加 shutdown 状态标志
  3. 停止接受新的健康检查请求

#### 15. [运维] [高严重] [LeaderElectionService 未与 GracefulShutdown 集成]
- **文件/路径**: `src/platform/five-plane-execution/ha/leader-election-service.ts`
- **问题描述**: 
  - LeaderElectionService.stop() 处理优雅退位
  - 但未在全局 graceful shutdown 中注册
  - 需显式注册为 shutdown handler
- **建议修复**: 
  1. 将 LeaderElectionService 添加到 shutdown handlers
  2. 确保领导者在 shutdown 时让位
  3. 验证关闭顺序正确

#### 16. [运维] [中严重] [addHandler() 无显式排序约定]
- **文件/路径**: `src/platform/five-plane-execution/startup/graceful-shutdown.ts`
- **问题描述**: 
  - 虽然 handlers 逆序执行
  - 但无显式接口声明 handler 依赖关系
  - 关键 handlers（如关闭数据库连接）应最后运行
- **建议修复**: 
  1. 添加优先级或阶段参数
  2. 文档化关键 handler 顺序要求
  3. 添加顺序验证测试

#### 17. [运维] [中严重] [unref'd timers 可能不触发]
- **文件/路径**: `src/platform/five-plane-execution/startup/process-error-handlers.ts:106`
- **问题描述**: 
  - `setTimeout(...).unref()` 不保持事件循环活跃
  - 如果主线程空闲，强制退出可能不触发
- **建议修复**: 
  1. 移除 .unref() 除非确实需要
  2. 或确保有其他方式保持事件循环
  3. 添加超时触发验证测试

#### 18. [运维] [中严重] [StartupConsistencyChecker 阻塞流量但不暴露状态]
- **文件/路径**: `src/platform/five-plane-startup-plan.ts`
- **问题描述**: 
  - canAcceptTraffic() 在 P0 问题存在时返回 false
  - 但 _trafficBlocked 是内部状态
  - 负载均衡器访问 /healthz 仍会得到 "ok"
- **建议修复**: 
  1. 在健康报告中暴露 trafficBlocked 状态
  2. 或添加单独的健康端点指示就绪状态
  3. 确保负载均衡器在阻塞时不路由流量

#### 19. [事件系统] [中严重] [TypedEventBus vs EventEmitter 混用]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - 项目同时使用 TypedEventBus 和原生 EventEmitter
  - TypedEventBus 应该是标准
  - 混用可能导致类型安全问题
- **建议修复**: 
  1. 创建迁移计划统一使用 TypedEventBus
  2. 添加 ESLint 规则禁止原生 EventEmitter
  3. 在 CI 中检测混用情况

#### 20. [源代码] [低严重] [无事件排序保证]
- **文件/路径**: `src/platform/five-plane-state-evidence/events/`
- **问题描述**: 
  - 无明确的事件排序保证文档
  - 事件处理可能乱序
  - 依赖方需要处理乱序
- **建议修复**: 
  1. 文档化事件排序语义
  2. 如果需要排序，实现序列号机制
  3. 在事件处理中考虑乱序情况

### 总结

本次补充Review（第十四轮 - 日志事件与工作流Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. StructuredLogEntry 无 requestId 字段
2. StructuredLogger data 字段无自动清洗（敏感数据风险）
3. Harness while(true) 循环无硬性迭代上限
4. Oapeflir while(true) 循环可能无限重规划
5. API 错误消息硬编码英文（无 i18n）
6. 健康检查无 readiness/liveness 区分
7. shutdown 期间健康检查存在竞态
8. LeaderElectionService 未与 GracefulShutdown 集成

**中优先级**:
1. 日志中敏感数据无 PII 处理
2. 59处 console.* 直接调用
3. 部分 logger.warn 调用缺少结构化数据
4. Recovery 流程允许终态转换到 paused
5. UI 硬编码字符串未翻译
6. Arabic 目录不完整
7. addHandler() 无显式排序约定
8. unref'd timers 可能不触发
9. StartupConsistencyChecker 阻塞流量但不暴露状态
10. TypedEventBus vs EventEmitter 混用

**低优先级**:
1. translateFeatureCopy 无回退默认值
2. 无事件排序保证

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 68 | 80 | 37 | 185 |
| 测试 | 8 | 14 | 5 | 27 |
| 配置 | 10 | 38 | 24 | 72 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **103** | **171** | **82** | **356** |

### 优先修复建议

**立即处理 (本周内)**:
1. 在 StructuredLogEntry 添加 requestId 字段
2. 实现日志敏感字段自动清洗
3. 为工作流循环添加硬性迭代上限
4. 添加 readiness/liveness 健康检查端点
5. 将 LeaderElectionService 集成到 GracefulShutdown
6. 实现 API 错误消息 i18n 支持

**短期内处理 (本月内)**:
1. 替换所有 console.* 为 StructuredLogger
2. 补充 Arabic i18n 目录
3. 修复 UI 硬编码字符串
4. 统一 TypedEventBus 替代 EventEmitter
5. 解决 shutdown 竞态条件
6. 添加事件排序保证文档

**长期规划**:
1. 建立完整的 PII 处理框架
2. 实现日志完整性验证
3. 建立工作流安全循环检测
4. 完善健康检查和就绪探测
5. 建立国际化完整测试

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十五轮 - 构建测试与类型系统Review）

### 发现的新问题

#### 1. [构建] [高严重] [无 TypeScript 增量编译缓存]
- **文件/路径**: `tsconfig.json`
- **问题描述**: 
  - 无 `incremental: true` 配置
  - 无 `tsbuildinfo` 文件
  - 每次构建是完整重建（9.6秒）
  - TypeScript 无法使用增量编译
- **建议修复**: 
  1. 在 tsconfig.json 添加 `"incremental": true`
  2. 添加 `"tsbuildinfo": ".tsbuildinfo"` 到 .gitignore
  3. 实现项目引用以支持并行构建

#### 2. [构建] [高严重] [76个 npm scripts 大部分重复构建]
- **文件/路径**: `package.json`
- **问题描述**: 
  - 40+ CLI scripts 都运行 `npm run build && node ...`
  - 每个脚本前都执行完整重建
  - 无法直接运行预构建的 CLI
  - 20+ stable:* 脚本遵循相同模式
- **建议修复**: 
  1. 创建 `build:cli` 脚本构建一次
  2. 让 CLI 脚本使用预构建的 dist 文件
  3. 添加 npm run dev 或类似的无需重建的执行方式

#### 3. [源代码] [高严重] [168处 any 类型使用]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - `stable-runner-factory.ts`: `opts: any` -> `any`
  - `billing-service-async.ts`: `input: any` 方法签名
  - 重试/超时工具使用 `(...args: any[]) => Promise<any>`
  - 绕过类型安全
- **建议修复**: 
  1. 使用泛型替代 any
  2. 替换为具体的输入类型
  3. 添加 ESLint 规则禁止 any

#### 4. [源代码] [高严重] [38处 @ts-ignore 指令]
- **文件/路径**: 多个文件
- **问题描述**: 
  - 14个 stability 相关文件有 `@ts-ignore ExecutionRecord type mismatch`
  - 5个 harness-sdk 文件有 `Partial<HarnessRun> doesn't have all required properties`
  - 2个文件有 `exactOptionalPropertyTypes` 问题
- **建议修复**: 
  1. 修复 ExecutionRecord 类型不匹配
  2. 完成 HarnessRun 类型的必需属性
  3. 移除 @ts-ignore 使用

#### 5. [源代码] [高严重] [ExecutionRecord 类型不匹配]
- **文件/路径**: `src/platform/stability/stable-evidence-bundle-support.ts:783` 等14个文件
- **问题描述**: 
  - 插入 execution record 时 TypeScript 拒绝类型
  - `ExecutionRecord` 接口要求新字段但 insert 对象缺失
  - 或 store 的 insert 方法接受不同类型
- **建议修复**: 
  1. 对齐 ExecutionRecord 接口和 store 类型
  2. 添加缺失字段到 insert 对象
  3. 或调整 store 的 insert 方法类型签名

#### 6. [源代码] [中严重] [30+处 as unknown as 双类型转换]
- **文件/路径**: `division-loader.ts`, `domain-baseline-catalog.ts`, `harness-sdk`
- **问题描述**: 
  - 完全绕过类型安全
  - 通常是类型架构问题的信号
- **建议修复**: 
  1. 修复上游类型问题
  2. 使用更具体的类型断言
  3. 重构类型层次结构

#### 7. [构建] [中严重] [rimraf  extraneous - 未在 package.json 声明]
- **文件/路径**: `node_modules/rimraf`
- **问题描述**: 
  - rimraf@6.1.3 存在于 node_modules 但未在 package.json 声明
  - 可能导致依赖问题
- **建议修复**: 
  1. 添加到 dependencies 或 devDependencies
  2. 或移除并使用其他清理方式

#### 8. [测试] [高严重] [178处直接 process.env 变更无抽象]
- **文件/路径**: `tests/unit/domains/registry/plugin-spi-registry.test.ts:640-687`
- **问题描述**: 
  - 直接读取/写入 process.env
  - 在 beforeEach/afterEach 中手动保存/恢复
  - 无 helper 抽象
- **建议修复**: 
  1. 创建 env helper 函数
  2. 统一保存和恢复模式
  3. 添加测试隔离验证

#### 9. [测试] [高严重] [21,998处 unlinkSync 调用无集中清理]
- **文件/路径**: 多个测试文件
- **问题描述**: 
  - 每个测试重新实现清理逻辑
  - 无集中清理 utility
  - 可能导致 EBUSY (Windows) 或并发访问问题
- **建议修复**: 
  1. 创建集中的测试清理 utility
  2. 使用 afterEach 确保清理
  3. 考虑使用临时目录

#### 10. [测试] [中严重] [无统一 mocking 框架]
- **文件/路径**: `tests/` 多个目录
- **问题描述**: 
  - 10,579 处 vi.fn, jest.mock, sinon 等调用
  - 无统一的 mocking 框架可见
  - 模式不一致
- **建议修复**: 
  1. 建立统一的 mock factory
  2. 标准化 mock 模式
  3. 添加 mocking 最佳实践文档

#### 11. [测试] [中严重] [Singleton 状态重置未标准化]
- **文件/路径**: `tests/unit/platform/five-plane-execution/dispatcher.test.ts`
- **问题描述**: 
  - 测试通过特定函数重置 singletons
  - 每个测试重复自己的 reset 逻辑
  - 无统一的 resetAllSingletons()
- **建议修复**: 
  1. 创建统一的 singleton reset 机制
  2. 在 afterEach 中自动调用
  3. 文档化 singleton 重置要求

#### 12. [测试] [低严重] [无分支覆盖率要求]
- **文件/路径**: `.c8rc.json`
- **问题描述**: 
  - `"100": false` - 无覆盖率要求
  - 只测量源代码，不测量测试文件
- **建议修复**: 
  1. 添加分支覆盖率阈值
  2. 在 CI 中强制执行覆盖率 gate
  3. 关注关键代码路径

#### 13. [网络] [高严重] [ScopedExternalAccessSandbox performHttpRequest 无超时]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/scoped-external-access-sandbox.ts:289`
- **问题描述**: 
  - fetch 调用无 timeout 参数
  - 挂起的请求可能永久阻塞
- **建议修复**: 
  1. 添加 AbortController timeout
  2. 使用标准的超时模式
  3. 记录超时配置

#### 14. [网络] [高严重] [OIDC 服务 fetch 调用无超时]
- **文件/路径**: `src/org-governance/sso-scim/oidc/oidc-service.ts:565,272`
- **问题描述**: 
  - OIDC token 交换无 retry logic
  - 无 timeout/abort signal
  - 认证流程可能挂起
- **建议修复**: 
  1. 添加 fetch timeout
  2. 添加 retry logic
  3. 使用标准的超时配置

#### 15. [网络] [高严重] [plugin-runtime-child 替换 globalThis.fetch 为 no-op]
- **文件/路径**: `src/domains/registry/plugin-runtime-child.ts:112`
- **问题描述**: 
  ```typescript
  globalThis.fetch = (async () => {}) as unknown as typeof globalThis.fetch;
  ```
  - 插件上下文中完全替换 fetch 为空函数
  - 插件的所有出站 HTTP 请求都会失败
  - 看起来是有意为之但可能造成问题
- **建议修复**: 
  1. 确认这是有意行为
  2. 文档化 fetch 替换行为
  3. 确保不会意外影响其他代码

#### 16. [网络] [中严重] [无显式 http.Agent 配置]
- **文件/路径**: 全局
- **问题描述**: 
  - 无显式的 http.Agent/https.Agent 配置
  - 依赖 Node.js 默认连接池
  - 无 maxSockets 或 maxConnections 配置
  - 高吞吐场景可能出问题
- **建议修复**: 
  1. 添加 http Agent 配置
  2. 文档化连接池大小
  3. 监控连接使用情况

#### 17. [序列化] [中严重] [合同版本硬编码为字符串字面量]
- **文件/路径**: `src/platform/contracts/inter-plane-contract-gateway.ts:184`
- **问题描述**: 
  - `schemaVersion: "v4.3"` 硬编码
  - 难以追踪合同版本演变
- **建议修复**: 
  1. 提取到类型化常量或 enum
  2. 集中管理合同版本
  3. 添加版本兼容性检查

#### 18. [序列化] [低严重] [Timestamp = string 丢失精度]
- **文件/路径**: `src/platform/contracts/types/domain/primitives.ts:10`
- **问题描述**: 
  - `type Timestamp = string` 无类型精度
  - ISO 8601 字符串无时区信息
- **建议修复**: 
  1. 使用 Date 对象或 branded string 类型
  2. 明确时区处理约定
  3. 文档化时间戳语义

#### 19. [源代码] [中严重] [29处非空断言 !.]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - 使用 `!.` 断言非空
  - 可能导致运行时错误
- **建议修复**: 
  1. 使用可选链 `?.` 或空值合并 `??`
  2. 添加适当的 null 检查
  3. 减少非空断言使用

#### 20. [API] [中严重] [API 路由 schema 版本硬编码]
- **文件/路径**: `src/platform/five-plane-interface/api/federation-routing-service.ts`
- **问题描述**: 
  - Zod schema 验证存在
  - 但 schema 版本未与 contract 版本同步
- **建议修复**: 
  1. 从 contract 版本派生 schema 版本
  2. 添加版本一致性检查
  3. 文档化版本关系

### 总结

本次补充Review（第十五轮 - 构建测试与类型系统Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. 无 TypeScript 增量编译缓存（每次完整重建9.6秒）
2. 76个 npm scripts 大部分重复构建
3. 168处 any 类型使用（绕过类型安全）
4. 38处 @ts-ignore 指令
5. ExecutionRecord 类型不匹配（14个文件）
6. 178处直接 process.env 变更无抽象
7. ScopedExternalAccessSandbox performHttpRequest 无超时
8. OIDC 服务 fetch 调用无超时
9. plugin-runtime-child 替换 globalThis.fetch 为 no-op

**中优先级**:
1. 30+处 as unknown as 双类型转换
2. rimraf extraneous 未声明
3. 21,998处 unlinkSync 无集中清理
4. 无统一 mocking 框架
5. Singleton 状态重置未标准化
6. 无显式 http.Agent 配置
7. 合同版本硬编码为字符串
8. Timestamp = string 丢失精度
9. 非空断言使用

**低优先级**:
1. 无分支覆盖率要求
2. API 路由 schema 版本未同步

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 72 | 87 | 39 | 198 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **109** | **182** | **85** | **376** |

### 优先修复建议

**立即处理 (本周内)**:
1. 添加 TypeScript 增量编译 (`incremental: true`)
2. 重构 npm scripts 避免重复构建
3. 减少 any 类型使用到 <50
4. 移除所有 @ts-ignore 指令
5. 修复 ExecutionRecord 类型不匹配
6. 添加 fetch 超时到 OIDC 和 sandbox

**短期内处理 (本月内)**:
1. 创建集中的测试清理 utility
2. 建立统一的 mocking 框架
3. 标准化 singleton 重置机制
4. 添加 http.Agent 连接池配置
5. 提取合同版本到类型化常量

**长期规划**:
1. 实现完整的类型安全（无 any）
2. 建立测试基础设施标准
3. 实现构建缓存优化
4. 完善网络层超时和重试
5. 建立覆盖率门禁标准

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十二轮 - 错误处理与多租户安全Review）

### 发现的新问题

#### 1. [安全] [严重] [listQuotas() 返回所有租户配额 - 信息泄露]
- **文件/路径**: `src/platform/five-plane-control-plane/tenant/tenant-execution-isolation-service.ts:222-234`
- **问题描述**: 
  ```typescript
  listQuotas(tenantId?: string): TenantQuota[] {
    if (tenantId) {
      return this.db.connection.prepare(`SELECT * FROM tenant_quotas WHERE tenant_id = ?`).all(tenantId);
    }
    // WITHOUT tenantId: 返回所有租户的配额！
    return this.db.connection.prepare(`SELECT * FROM tenant_quotas`).all();
  }
  ```
  - 当 tenantId 为空时返回所有租户的配额
  - 未经授权的信息泄露
- **建议修复**: 
  1. 要求调用者必须提供 tenantId
  2. 添加权限验证确保只能查看自己的配额
  3. 审计日志记录所有配额访问

#### 2. [安全] [高严重] [assertTaskTenantAccess() 返回404而非403 - 资源存在泄露]
- **文件/路径**: `src/platform/five-plane-interface/api/utils.ts:146-148`
- **问题描述**: 
  ```typescript
  if (resourceTenantId !== principal.tenantId) {
    throw new ApiError(404, notFoundCode, notFoundMessage);  // 应该是403
  }
  ```
  - 跨租户访问返回404而非403
  - 泄露了资源存在但属于其他租户的信息
  - 攻击者可检测有效task ID
- **建议修复**: 
  1. 改为返回403 Forbidden
  2. 不要泄露资源是否存在

#### 3. [安全] [高严重] [crossTenantRequest 标志由调用者控制]
- **文件/路径**: `src/platform/five-plane-interface/org-routing/index.ts:163`
- **问题描述**: 
  - `routeOrgBudget()` 接受 `crossTenantRequest?: boolean` 参数
  - 该标志由调用者设置，非系统派生
  - 攻击者可能操纵此标志绕过跨租户限制
- **建议修复**: 
  1. 标志应由系统状态派生，不接受调用者输入
  2. 添加审计日志追踪跨租户请求
  3. 实现零信任跨租户访问模型

#### 4. [源代码] [高严重] [single-task-happy-path 无重试机制]
- **文件/路径**: `src/platform/five-plane-execution/execution-engine/single-task-happy-path.ts:336-337`
- **问题描述**: 
  - `maxRetries: 0` 和 `retryBackoff: "none"`
  - 瞬态失败无自动重试
  - LLM调用失败时无fallback到备用provider
- **建议修复**: 
  1. 启用非关键执行的重试
  2. 实现LLM provider fallback链（Anthropic → OpenAI → MiniMax）
  3. 使用domain baseline catalog的primary/fallback模型偏好

#### 5. [源代码] [高严重] [LLM调用无熔断器保护]
- **文件/路径**: `src/platform/five-plane-execution/execution-engine/model-call-provider.ts`
- **问题描述**: 
  - 熔断器只存在于 channel-gateway 和 call-governance
  - 直接LLM provider调用无熔断器
  - provider降级时无法快速失败
- **建议修复**: 
  1. 在model-call-provider.ts添加熔断器
  2. 当provider失败率超阈值时快速失败
  3. 自动切换到备用provider

#### 6. [安全] [高严重] [分布式限流器 bypass - 实例间不共享]
- **文件/路径**: `src/platform/five-plane-interface/ingress/distributed-rate-limiter.ts:33`
- **问题描述**: 
  - Redis未配置时使用本地Map
  - 每个实例维护自己的localEntries
  - 攻击者可通过切换实例绕过限流
- **建议修复**: 
  1. 要求Redis配置用于生产
  2. 或使用Redis作为所有实例的共享后端
  3. 检测并警告未配置Redis的实例

#### 7. [配置] [中严重] [Soft quota (log_only) 实际不限制]
- **文件/路径**: `src/platform/five-plane-control-plane/tenant/tenant-execution-isolation-service.ts:448`
- **问题描述**: 
  ```typescript
  allowed: quota.enforcementAction === "log_only",  // log_only时返回true
  ```
  - soft quota只记录日志，不实际阻止
  - 配额形同虚设
- **建议修复**: 
  1. 明确区分"监控模式"和"强制模式"
  2. 对于硬配额必须阻止
  3. 文档化quota类型的实际行为

#### 8. [配置] [中严重] [HTTP层无限流头 - 客户端无法获知限制]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/response-hardening.ts`
- **问题描述**: 
  - 未返回 X-RateLimit-* 头
  - 只在429响应时返回retry-after-ms
  - 客户端无法主动管理请求速率
- **建议修复**: 
  1. 在所有响应添加 X-RateLimit-Limit, X-RateLimit-Remaining, X-RateLimit-Reset
  2. 使用标准头名称
  3. 遵循RFC 6585

#### 9. [配置] [中严重] [HTTP层无限流 - 可选且仅按IP]
- **文件/路径**: `src/platform/five-plane-interface/api/http-api-server.ts:334-351`
- **问题描述**: 
  - 限流仅在 `this.rateLimiter != null` 时生效
  - 限流key为 `${clientIp}:${endpoint}` 仅按IP
  - 无per-tenant/per-principal enforcement
- **建议修复**: 
  1. 默认启用限流
  2. 添加per-tenant限流key
  3. 确保所有环境默认配置限流

#### 10. [安全] [中严重] [processRuleMode 可能未强制执行]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/plugin-runtime-host.ts`
- **问题描述**: 
  - `SandboxPolicy.processRuleMode` 设置为 "allow" 或 "deny"
  - 但未找到实际阻止基于此策略生成进程的实际执行代码
  - `--allow-child-process` 标志仅在 sandboxed_process 隔离模式应用
- **建议修复**: 
  1. 验证processRuleMode实际被强制执行
  2. 确保所有隔离模式正确处理
  3. 添加测试验证进程创建被阻止

#### 11. [安全] [中严重] [容器模板替换未验证]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/plugin-runtime-host.ts:751`
- **问题描述**: 
  ```typescript
  rendered = rendered.split(placeholder).join(value);
  ```
  - 简单字符串替换
  - 虽然validateContainerLaunchPluginId()阻止\0和引号
  - 但模板替换本身未验证
- **建议修复**: 
  1. 添加输入验证防止注入
  2. 使用更安全的模板引擎
  3. 验证所有占位符被替换

#### 12. [安全] [中严重] [Adapter执行绕过沙箱]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/adapter-executor.ts`
- **问题描述**: 
  - Adapter执行REST/grpc/MQ调用不经过ScopedExternalAccessSandbox
  - 有自己的allowedDomains配置但非集中管理
  - 可能发起未限制的出站请求
- **建议修复**: 
  1. Adapter执行经过集中沙箱
  2. 统一external access策略
  3. 添加adapter出站请求审计

#### 13. [安全] [中严重] [null principal.tenantId 时静默放行]
- **文件/路径**: `src/platform/five-plane-interface/api/utils.ts:143-145`
- **问题描述**: 
  ```typescript
  if (principal.tenantId == null) {
    return;  // 静默放行
  }
  ```
  - 如果API网关允许无principal认证的请求通过
  - 可能授予跨租户访问权限
- **建议修复**: 
  1. 要求所有API请求有有效principal
  2. null tenantId应拒绝而非静默放行
  3. 添加审计日志

#### 14. [源代码] [中严重] [无LLM provider fallback链]
- **文件/路径**: `src/platform/five-plane-execution/execution-engine/model-call-provider.ts`
- **问题描述**: 
  - 主provider失败时无自动切换
  - domain-baseline-catalog.ts定义了primary/fallback模型偏好但未使用
  - 第一个provider失败即导致执行失败
- **建议修复**: 
  1. 实现provider fallback链
  2. 使用domain baseline的primary/fallback配置
  3. 按优先级尝试直到成功或全部失败

#### 15. [源代码] [中严重] [超时值硬编码]
- **文件/路径**: 多个文件
- **问题描述**: 
  - Effect buffer有固定超时reject
  - Channel gateway的requestTimeoutMs虽可配置但有max 30s限制
  - 无全局请求超时中间件
- **建议修复**: 
  1. 集中超时配置
  2. per-environment可配置
  3. 添加全局超时中间件

#### 16. [数据库] [中严重] [无down migration - 回滚不支持]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/sqlite/migration-runner.ts:45-47`
- **问题描述**: 
  - `rollbackSupported = false`
  - 每个migration只有 `downSql` 占位符
  - schema migration无法回滚
- **建议修复**: 
  1. 文档化回滚限制
  2. 在变更前创建完整备份
  3. 使用blue-green部署减少回滚需求

#### 17. [数据库] [中严重] [Migration 44特殊处理 - 表创建重复]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/sqlite/sqlite-migration-runtime-part3.ts`
- **问题描述**: 
  - Migration 43创建harness_runs表
  - Migration 44再次创建同名表（不同schema）
  - 使用applyCompatibleColumnMigrationIfKnown特殊处理
- **建议修复**: 
  1. 消除重复表创建
  2. 使用ALTER TABLE而非CREATE TABLE
  3. 简化migration逻辑

#### 18. [配置] [低严重] [Provider限流头未转发给客户端]
- **文件/路径**: `src/platform/model-gateway/provider-registry/base-chat-provider.ts:153`
- **问题描述**: 
  - 系统读取provider的ratelimitResetHeaderNames
  - 但不转发给客户端
  - 客户端无法知道provider限流状态
- **建议修复**: 
  1. 转发Provider的限流头
  2. 或添加应用层限流信息
  3. 帮助客户端优化请求

#### 19. [安全] [低严重] [Browser evaluate 接受任意脚本]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/browser-executor.ts:326`
- **问题描述**: 
  - `evaluate` action接受script参数
  - 在浏览器上下文运行（模拟）
  - 如浏览器上下文未正确沙箱化可能是向量
- **建议修复**: 
  1. 验证浏览器上下文正确沙箱化
  2. 消毒或限制script内容
  3. 记录所有evaluate调用

#### 20. [源代码] [低严重] [无全局错误边界包装器]
- **文件/路径**: `src/platform/five-plane-execution/execution-engine/`
- **问题描述**: 
  - 执行引擎缺少unhandled promise rejection的全局处理
  - single-task-happy-path的LLM fallback失败时错误向上传播
- **建议修复**: 
  1. 在执行引擎添加错误边界包装
  2. 统一错误处理模式
  3. 确保所有错误被捕获和记录

### 总结

本次补充Review（第十二轮 - 错误处理与多租户安全Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. listQuotas()返回所有租户配额（信息泄露）
2. assertTaskTenantAccess()返回404泄露资源存在
3. crossTenantRequest标志可被操纵
4. single-task-happy-path无重试（maxRetries=0）
5. LLM调用无熔断器保护
6. 分布式限流器可被bypass（实例间不共享）

**中优先级**:
1. Soft quota实际不阻止
2. 无X-RateLimit-*响应头
3. HTTP层限流可选且仅按IP
4. processRuleMode可能未强制
5. 容器模板替换未验证
6. Adapter执行绕过沙箱
7. null principal静默放行
8. 无LLM provider fallback链
9. 超时值硬编码
10. 无down migration
11. Migration 44特殊处理

**低优先级**:
1. Provider限流头未转发
2. Browser evaluate接受任意脚本
3. 无全局错误边界

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 60 | 68 | 31 | 159 |
| 测试 | 8 | 14 | 5 | 27 |
| 配置 | 8 | 35 | 21 | 64 |
| 安全 | 12 | 13 | 3 | 28 |
| 文档 | 2 | 7 | 3 | 12 |
| UI | 0 | 3 | 3 | 6 |
| 部署 | 1 | 10 | 6 | 17 |
| **合计** | **91** | **150** | **72** | **313** |

### 优先修复建议

**立即处理 (本周内)**:
1. 修复listQuotas()信息泄露漏洞
2. 修正assertTaskTenantAccess()返回403
3. 移除crossTenantRequest调用者控制
4. 启用执行引擎重试机制
5. 添加LLM provider熔断器

**短期内处理 (本月内)**:
1. 修复分布式限流器bypass问题
2. 添加X-RateLimit-*响应头
3. 启用默认HTTP限流（per-tenant）
4. 实现LLM provider fallback链
5. 验证并强制processRuleMode

**长期规划**:
1. 建立完整的限流和配额体系
2. 实现插件系统安全审计
3. 建立多租户安全测试
4. 完善错误处理和恢复机制
5. 统一超时和熔断配置

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十五轮 - 构建测试与类型系统Review）

### 发现的新问题

#### 1. [构建] [高严重] [无 TypeScript 增量编译缓存]
- **文件/路径**: `tsconfig.json`
- **问题描述**: 
  - 无 `incremental: true` 配置
  - 无 `tsbuildinfo` 文件
  - 每次构建是完整重建（9.6秒）
  - TypeScript 无法使用增量编译
- **建议修复**: 
  1. 在 tsconfig.json 添加 `"incremental": true`
  2. 添加 `"tsbuildinfo": ".tsbuildinfo"` 到 .gitignore
  3. 实现项目引用以支持并行构建

#### 2. [构建] [高严重] [76个 npm scripts 大部分重复构建]
- **文件/路径**: `package.json`
- **问题描述**: 
  - 40+ CLI scripts 都运行 `npm run build && node ...`
  - 每个脚本前都执行完整重建
  - 无法直接运行预构建的 CLI
  - 20+ stable:* 脚本遵循相同模式
- **建议修复**: 
  1. 创建 `build:cli` 脚本构建一次
  2. 让 CLI 脚本使用预构建的 dist 文件
  3. 添加 npm run dev 或类似的无需重建的执行方式

#### 3. [源代码] [高严重] [168处 any 类型使用]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - `stable-runner-factory.ts`: `opts: any` -> `any`
  - `billing-service-async.ts`: `input: any` 方法签名
  - 重试/超时工具使用 `(...args: any[]) => Promise<any>`
  - 绕过类型安全
- **建议修复**: 
  1. 使用泛型替代 any
  2. 替换为具体的输入类型
  3. 添加 ESLint 规则禁止 any

#### 4. [源代码] [高严重] [38处 @ts-ignore 指令]
- **文件/路径**: 多个文件
- **问题描述**: 
  - 14个 stability 相关文件有 `@ts-ignore ExecutionRecord type mismatch`
  - 5个 harness-sdk 文件有 `Partial<HarnessRun> doesn't have all required properties`
  - 2个文件有 `exactOptionalPropertyTypes` 问题
- **建议修复**: 
  1. 修复 ExecutionRecord 类型不匹配
  2. 完成 HarnessRun 类型的必需属性
  3. 移除 @ts-ignore 使用

#### 5. [源代码] [高严重] [ExecutionRecord 类型不匹配]
- **文件/路径**: `src/platform/stability/stable-evidence-bundle-support.ts:783` 等14个文件
- **问题描述**: 
  - 插入 execution record 时 TypeScript 拒绝类型
  - `ExecutionRecord` 接口要求新字段但 insert 对象缺失
  - 或 store 的 insert 方法接受不同类型
- **建议修复**: 
  1. 对齐 ExecutionRecord 接口和 store 类型
  2. 添加缺失字段到 insert 对象
  3. 或调整 store 的 insert 方法类型签名

#### 6. [源代码] [中严重] [30+处 as unknown as 双类型转换]
- **文件/路径**: `division-loader.ts`, `domain-baseline-catalog.ts`, `harness-sdk`
- **问题描述**: 
  - 完全绕过类型安全
  - 通常是类型架构问题的信号
- **建议修复**: 
  1. 修复上游类型问题
  2. 使用更具体的类型断言
  3. 重构类型层次结构

#### 7. [构建] [中严重] [rimraf  extraneous - 未在 package.json 声明]
- **文件/路径**: `node_modules/rimraf`
- **问题描述**: 
  - rimraf@6.1.3 存在于 node_modules 但未在 package.json 声明
  - 可能导致依赖问题
- **建议修复**: 
  1. 添加到 dependencies 或 devDependencies
  2. 或移除并使用其他清理方式

#### 8. [测试] [高严重] [178处直接 process.env 变更无抽象]
- **文件/路径**: `tests/unit/domains/registry/plugin-spi-registry.test.ts:640-687`
- **问题描述**: 
  - 直接读取/写入 process.env
  - 在 beforeEach/afterEach 中手动保存/恢复
  - 无 helper 抽象
- **建议修复**: 
  1. 创建 env helper 函数
  2. 统一保存和恢复模式
  3. 添加测试隔离验证

#### 9. [测试] [高严重] [21,998处 unlinkSync 调用无集中清理]
- **文件/路径**: 多个测试文件
- **问题描述**: 
  - 每个测试重新实现清理逻辑
  - 无集中清理 utility
  - 可能导致 EBUSY (Windows) 或并发访问问题
- **建议修复**: 
  1. 创建集中的测试清理 utility
  2. 使用 afterEach 确保清理
  3. 考虑使用临时目录

#### 10. [测试] [中严重] [无统一 mocking 框架]
- **文件/路径**: `tests/` 多个目录
- **问题描述**: 
  - 10,579 处 vi.fn, jest.mock, sinon 等调用
  - 无统一的 mocking 框架可见
  - 模式不一致
- **建议修复**: 
  1. 建立统一的 mock factory
  2. 标准化 mock 模式
  3. 添加 mocking 最佳实践文档

#### 11. [测试] [中严重] [Singleton 状态重置未标准化]
- **文件/路径**: `tests/unit/platform/five-plane-execution/dispatcher.test.ts`
- **问题描述**: 
  - 测试通过特定函数重置 singletons
  - 每个测试重复自己的 reset 逻辑
  - 无统一的 resetAllSingletons()
- **建议修复**: 
  1. 创建统一的 singleton reset 机制
  2. 在 afterEach 中自动调用
  3. 文档化 singleton 重置要求

#### 12. [测试] [低严重] [无分支覆盖率要求]
- **文件/路径**: `.c8rc.json`
- **问题描述**: 
  - `"100": false` - 无覆盖率要求
  - 只测量源代码，不测量测试文件
- **建议修复**: 
  1. 添加分支覆盖率阈值
  2. 在 CI 中强制执行覆盖率 gate
  3. 关注关键代码路径

#### 13. [网络] [高严重] [ScopedExternalAccessSandbox performHttpRequest 无超时]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/scoped-external-access-sandbox.ts:289`
- **问题描述**: 
  - fetch 调用无 timeout 参数
  - 挂起的请求可能永久阻塞
- **建议修复**: 
  1. 添加 AbortController timeout
  2. 使用标准的超时模式
  3. 记录超时配置

#### 14. [网络] [高严重] [OIDC 服务 fetch 调用无超时]
- **文件/路径**: `src/org-governance/sso-scim/oidc/oidc-service.ts:565,272`
- **问题描述**: 
  - OIDC token 交换无 retry logic
  - 无 timeout/abort signal
  - 认证流程可能挂起
- **建议修复**: 
  1. 添加 fetch timeout
  2. 添加 retry logic
  3. 使用标准的超时配置

#### 15. [网络] [高严重] [plugin-runtime-child 替换 globalThis.fetch 为 no-op]
- **文件/路径**: `src/domains/registry/plugin-runtime-child.ts:112`
- **问题描述**: 
  ```typescript
  globalThis.fetch = (async () => {}) as unknown as typeof globalThis.fetch;
  ```
  - 插件上下文中完全替换 fetch 为空函数
  - 插件的所有出站 HTTP 请求都会失败
  - 看起来是有意为之但可能造成问题
- **建议修复**: 
  1. 确认这是有意行为
  2. 文档化 fetch 替换行为
  3. 确保不会意外影响其他代码

#### 16. [网络] [中严重] [无显式 http.Agent 配置]
- **文件/路径**: 全局
- **问题描述**: 
  - 无显式的 http.Agent/https.Agent 配置
  - 依赖 Node.js 默认连接池
  - 无 maxSockets 或 maxConnections 配置
  - 高吞吐场景可能出问题
- **建议修复**: 
  1. 添加 http Agent 配置
  2. 文档化连接池大小
  3. 监控连接使用情况

#### 17. [序列化] [中严重] [合同版本硬编码为字符串字面量]
- **文件/路径**: `src/platform/contracts/inter-plane-contract-gateway.ts:184`
- **问题描述**: 
  - `schemaVersion: "v4.3"` 硬编码
  - 难以追踪合同版本演变
- **建议修复**: 
  1. 提取到类型化常量或 enum
  2. 集中管理合同版本
  3. 添加版本兼容性检查

#### 18. [序列化] [低严重] [Timestamp = string 丢失精度]
- **文件/路径**: `src/platform/contracts/types/domain/primitives.ts:10`
- **问题描述**: 
  - `type Timestamp = string` 无类型精度
  - ISO 8601 字符串无时区信息
- **建议修复**: 
  1. 使用 Date 对象或 branded string 类型
  2. 明确时区处理约定
  3. 文档化时间戳语义

#### 19. [源代码] [中严重] [29处非空断言 !.]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - 使用 `!.` 断言非空
  - 可能导致运行时错误
- **建议修复**: 
  1. 使用可选链 `?.` 或空值合并 `??`
  2. 添加适当的 null 检查
  3. 减少非空断言使用

#### 20. [API] [中严重] [API 路由 schema 版本硬编码]
- **文件/路径**: `src/platform/five-plane-interface/api/federation-routing-service.ts`
- **问题描述**: 
  - Zod schema 验证存在
  - 但 schema 版本未与 contract 版本同步
- **建议修复**: 
  1. 从 contract 版本派生 schema 版本
  2. 添加版本一致性检查
  3. 文档化版本关系

### 总结

本次补充Review（第十五轮 - 构建测试与类型系统Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. 无 TypeScript 增量编译缓存（每次完整重建9.6秒）
2. 76个 npm scripts 大部分重复构建
3. 168处 any 类型使用（绕过类型安全）
4. 38处 @ts-ignore 指令
5. ExecutionRecord 类型不匹配（14个文件）
6. 178处直接 process.env 变更无抽象
7. ScopedExternalAccessSandbox performHttpRequest 无超时
8. OIDC 服务 fetch 调用无超时
9. plugin-runtime-child 替换 globalThis.fetch 为 no-op

**中优先级**:
1. 30+处 as unknown as 双类型转换
2. rimraf extraneous 未声明
3. 21,998处 unlinkSync 无集中清理
4. 无统一 mocking 框架
5. Singleton 状态重置未标准化
6. 无显式 http.Agent 配置
7. 合同版本硬编码为字符串
8. Timestamp = string 丢失精度
9. 非空断言使用

**低优先级**:
1. 无分支覆盖率要求
2. API 路由 schema 版本未同步

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 72 | 87 | 39 | 198 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **109** | **182** | **85** | **376** |

### 优先修复建议

**立即处理 (本周内)**:
1. 添加 TypeScript 增量编译 (`incremental: true`)
2. 重构 npm scripts 避免重复构建
3. 减少 any 类型使用到 <50
4. 移除所有 @ts-ignore 指令
5. 修复 ExecutionRecord 类型不匹配
6. 添加 fetch 超时到 OIDC 和 sandbox

**短期内处理 (本月内)**:
1. 创建集中的测试清理 utility
2. 建立统一的 mocking 框架
3. 标准化 singleton 重置机制
4. 添加 http.Agent 连接池配置
5. 提取合同版本到类型化常量

**长期规划**:
1. 实现完整的类型安全（无 any）
2. 建立测试基础设施标准
3. 实现构建缓存优化
4. 完善网络层超时和重试
5. 建立覆盖率门禁标准

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十四轮 - 日志事件与工作流Review）

### 发现的新问题

#### 1. [可观测性] [高严重] [StructuredLogEntry 无 requestId 字段]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - StructuredLogEntry 接口无 requestId 字段
  - 只有 correlationId，它回退到 traceId
  - 无法独立追踪单个 HTTP 请求
- **建议修复**: 
  1. 在 StructuredLogEntry 添加 requestId 字段
  2. 在 HTTP 中间件中生成和注入 requestId
  3. 确保 requestId 流经所有日志调用

#### 2. [可观测性] [高严重] [StructuredLogger data 字段无自动清洗]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - 如果开发者传递 `{ password: "..." }` 给 logger.info，密码会被记录
  - 无字段级脱敏机制
  - 依赖开发者手动排除敏感字段
- **建议修复**: 
  1. 添加 OBSERVE_OUTPUT_BLACKLIST 类似的黑名单
  2. 实现 redact/mask/sanitize 工具函数
  3. 在日志传输前自动清洗敏感字段

#### 3. [可观测性] [中严重] [日志中敏感数据无 PII 处理]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - 无 PII 重写工具
  - 开发者必须手动避免记录敏感数据
  - 存在意外记录敏感信息的风险
- **建议修复**: 
  1. 添加 PII 检测和重写工具
  2. 在文档中明确记录敏感字段列表
  3. 添加测试验证敏感数据不被记录

#### 4. [可观测性] [中严重] [59处 console.* 直接调用]
- **文件/路径**: 多个源文件
- **问题描述**: 
  - 应该使用 StructuredLogger
  - console 调用可能不被日志聚合系统收集
  - 难以追踪和关联
- **建议修复**: 
  1. 替换所有 console.* 为 StructuredLogger
  2. 添加 ESLint 规则禁止 console.*
  3. 保留少量允许的 CLI 输出

#### 5. [可观测性] [中严重] [部分 logger.warn 调用缺少结构化数据]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:122`
- **问题描述**: 
  ```typescript
  logger.warn("WebSocket connection rejected: missing subprotocol token")
  ```
  - 只有消息，无 data 对象
  - 难以关联和查询
- **建议修复**: 
  1. 添加 `{ data: { ... } }` 对象
  2. 确保所有日志包含上下文数据
  3. 添加日志审查工具

#### 6. [源代码] [高严重] [Harness while(true) 循环无硬性迭代上限]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:1442`
- **问题描述**: 
  ```typescript
  while (true) {
    // ...
    // 依赖 budget gate 和 guardrail vibration 退出
    // 如果两者都失败，循环永久继续
  }
  ```
  - 依赖 budget gate 和 guardrail vibration 退出
  - 无硬性迭代次数限制
  - 可能导致无限循环
- **建议修复**: 
  1. 添加硬性 maxIterations 上限
  2. 在达到上限时强制退出
  3. 记录详细的迭代统计用于调试

#### 7. [源代码] [高严重] [Oapeflir while(true) 循环可能无限重规划]
- **文件/路径**: `src/platform/five-plane-orchestration/oapeflir/oapeflir-loop-service.ts:355`
- **问题描述**: 
  - 如果 loopReplanDecision.shouldReplan 永不变为 false
  - 重规划永远无法减少计划错误
  - 可能导致无限循环
- **建议修复**: 
  1. 添加重规划次数上限
  2. 在达到上限时强制接受当前结果
  3. 添加重规划质量衰减检测

#### 8. [源代码] [中严重] [Recovery 流程允许终态转换到 paused]
- **文件/路径**: `src/platform/five-plane-execution/runtime-state-machine.ts:105-108`
- **问题描述**: 
  - completed, failed, cancelled, aborted 可转换到 paused 进行恢复
  - 创建恢复路径可能重新进入执行
  - 需要验证此行为的正确性
- **建议修复**: 
  1. 审查 recovery 转换的合法性
  2. 确保不会导致状态不一致
  3. 添加转换前置条件验证

#### 9. [国际化] [高严重] [API 错误消息硬编码英文]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/api-error.ts`
- **问题描述**: 
  ```typescript
  return new ApiError(404, "api.task_not_found", "Task not found.");
  ```
  - 错误消息硬编码英文
  - 无后端 i18n 机制
  - 非英文客户端收到不本地化的错误
- **建议修复**: 
  1. 使用错误码而非硬编码消息
  2. 客户端根据错误码本地化
  3. 或在后端添加 i18n 支持

#### 10. [国际化] [中严重] [UI 硬编码字符串未翻译]
- **文件/路径**: `ui/packages/features/conversation/src/web/index.tsx`
- **问题描述**: 
  - "Build Plan", "Execute", "Messages" 等按钮标签硬编码
  - 不使用 i18n 系统
- **建议修复**: 
  1. 使用 `translateFeatureCopy` 替换硬编码
  2. 确保所有用户可见文本通过 i18n
  3. 添加硬编码字符串检测

#### 11. [国际化] [中严重] [Arabic 目录不完整]
- **文件/路径**: `ui/packages/shared/i18n/src/catalogs/ar-SA.ts`
- **问题描述**: 
  - ar-SA 只有约18条消息
  - en-US 和 zh-CN 有约117条
  - 缺失的 key 返回 key 本身
- **建议修复**: 
  1. 补充 Arabic 翻译
  2. 确保所有 key 都有翻译
  3. 添加翻译完整性测试

#### 12. [国际化] [低严重] [translateFeatureCopy 无回退默认值]
- **文件/路径**: `ui/packages/shared/i18n/`
- **问题描述**: 
  - 如果 feature ID 不在 catalog 中
  - 返回 undefined title/summary
  - 无合理回退
- **建议修复**: 
  1. 添加回退到 key 或默认字符串
  2. 或使用 feature ID 作为显示名
  3. 确保 UI 不显示 undefined

#### 13. [运维] [高严重] [健康检查无 readiness/liveness 区分]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts`
- **问题描述**: 
  - /healthz, /v1/healthz, /health 返回相同报告
  - 无 readiness probe（可处理流量？）
  - 无 liveness probe（系统存活？）
  - canAcceptTraffic() 未通过 HTTP 暴露
- **建议修复**: 
  1. 添加 /ready 和 /live 端点
  2. Readiness 检查依赖项就绪
  3. Liveness 检查进程健康

#### 14. [运维] [高严重] [shutdown 期间健康检查存在竞态]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts`
- **问题描述**: 
  - getHealthReportAsync() 异步检查 dbWritable
  - shutdown 期间数据库连接可能关闭
  - 健康检查仍在传入
- **建议修复**: 
  1. 在 shutdown 时返回 unhealthy
  2. 添加 shutdown 状态标志
  3. 停止接受新的健康检查请求

#### 15. [运维] [高严重] [LeaderElectionService 未与 GracefulShutdown 集成]
- **文件/路径**: `src/platform/five-plane-execution/ha/leader-election-service.ts`
- **问题描述**: 
  - LeaderElectionService.stop() 处理优雅退位
  - 但未在全局 graceful shutdown 中注册
  - 需显式注册为 shutdown handler
- **建议修复**: 
  1. 将 LeaderElectionService 添加到 shutdown handlers
  2. 确保领导者在 shutdown 时让位
  3. 验证关闭顺序正确

#### 16. [运维] [中严重] [addHandler() 无显式排序约定]
- **文件/路径**: `src/platform/five-plane-execution/startup/graceful-shutdown.ts`
- **问题描述**: 
  - 虽然 handlers 逆序执行
  - 但无显式接口声明 handler 依赖关系
  - 关键 handlers（如关闭数据库连接）应最后运行
- **建议修复**: 
  1. 添加优先级或阶段参数
  2. 文档化关键 handler 顺序要求
  3. 添加顺序验证测试

#### 17. [运维] [中严重] [unref'd timers 可能不触发]
- **文件/路径**: `src/platform/five-plane-execution/startup/process-error-handlers.ts:106`
- **问题描述**: 
  - `setTimeout(...).unref()` 不保持事件循环活跃
  - 如果主线程空闲，强制退出可能不触发
- **建议修复**: 
  1. 移除 .unref() 除非确实需要
  2. 或确保有其他方式保持事件循环
  3. 添加超时触发验证测试

#### 18. [运维] [中严重] [StartupConsistencyChecker 阻塞流量但不暴露状态]
- **文件/路径**: `src/platform/five-plane-startup-plan.ts`
- **问题描述**: 
  - canAcceptTraffic() 在 P0 问题存在时返回 false
  - 但 _trafficBlocked 是内部状态
  - 负载均衡器访问 /healthz 仍会得到 "ok"
- **建议修复**: 
  1. 在健康报告中暴露 trafficBlocked 状态
  2. 或添加单独的健康端点指示就绪状态
  3. 确保负载均衡器在阻塞时不路由流量

#### 19. [事件系统] [中严重] [TypedEventBus vs EventEmitter 混用]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - 项目同时使用 TypedEventBus 和原生 EventEmitter
  - TypedEventBus 应该是标准
  - 混用可能导致类型安全问题
- **建议修复**: 
  1. 创建迁移计划统一使用 TypedEventBus
  2. 添加 ESLint 规则禁止原生 EventEmitter
  3. 在 CI 中检测混用情况

#### 20. [源代码] [低严重] [无事件排序保证]
- **文件/路径**: `src/platform/five-plane-state-evidence/events/`
- **问题描述**: 
  - 无明确的事件排序保证文档
  - 事件处理可能乱序
  - 依赖方需要处理乱序
- **建议修复**: 
  1. 文档化事件排序语义
  2. 如果需要排序，实现序列号机制
  3. 在事件处理中考虑乱序情况

### 总结

本次补充Review（第十四轮 - 日志事件与工作流Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. StructuredLogEntry 无 requestId 字段
2. StructuredLogger data 字段无自动清洗（敏感数据风险）
3. Harness while(true) 循环无硬性迭代上限
4. Oapeflir while(true) 循环可能无限重规划
5. API 错误消息硬编码英文（无 i18n）
6. 健康检查无 readiness/liveness 区分
7. shutdown 期间健康检查存在竞态
8. LeaderElectionService 未与 GracefulShutdown 集成

**中优先级**:
1. 日志中敏感数据无 PII 处理
2. 59处 console.* 直接调用
3. 部分 logger.warn 调用缺少结构化数据
4. Recovery 流程允许终态转换到 paused
5. UI 硬编码字符串未翻译
6. Arabic 目录不完整
7. addHandler() 无显式排序约定
8. unref'd timers 可能不触发
9. StartupConsistencyChecker 阻塞流量但不暴露状态
10. TypedEventBus vs EventEmitter 混用

**低优先级**:
1. translateFeatureCopy 无回退默认值
2. 无事件排序保证

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 68 | 80 | 37 | 185 |
| 测试 | 8 | 14 | 5 | 27 |
| 配置 | 10 | 38 | 24 | 72 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **103** | **171** | **82** | **356** |

### 优先修复建议

**立即处理 (本周内)**:
1. 在 StructuredLogEntry 添加 requestId 字段
2. 实现日志敏感字段自动清洗
3. 为工作流循环添加硬性迭代上限
4. 添加 readiness/liveness 健康检查端点
5. 将 LeaderElectionService 集成到 GracefulShutdown
6. 实现 API 错误消息 i18n 支持

**短期内处理 (本月内)**:
1. 替换所有 console.* 为 StructuredLogger
2. 补充 Arabic i18n 目录
3. 修复 UI 硬编码字符串
4. 统一 TypedEventBus 替代 EventEmitter
5. 解决 shutdown 竞态条件
6. 添加事件排序保证文档

**长期规划**:
1. 建立完整的 PII 处理框架
2. 实现日志完整性验证
3. 建立工作流安全循环检测
4. 完善健康检查和就绪探测
5. 建立国际化完整测试

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十五轮 - 构建测试与类型系统Review）

### 发现的新问题

#### 1. [构建] [高严重] [无 TypeScript 增量编译缓存]
- **文件/路径**: `tsconfig.json`
- **问题描述**: 
  - 无 `incremental: true` 配置
  - 无 `tsbuildinfo` 文件
  - 每次构建是完整重建（9.6秒）
  - TypeScript 无法使用增量编译
- **建议修复**: 
  1. 在 tsconfig.json 添加 `"incremental": true`
  2. 添加 `"tsbuildinfo": ".tsbuildinfo"` 到 .gitignore
  3. 实现项目引用以支持并行构建

#### 2. [构建] [高严重] [76个 npm scripts 大部分重复构建]
- **文件/路径**: `package.json`
- **问题描述**: 
  - 40+ CLI scripts 都运行 `npm run build && node ...`
  - 每个脚本前都执行完整重建
  - 无法直接运行预构建的 CLI
  - 20+ stable:* 脚本遵循相同模式
- **建议修复**: 
  1. 创建 `build:cli` 脚本构建一次
  2. 让 CLI 脚本使用预构建的 dist 文件
  3. 添加 npm run dev 或类似的无需重建的执行方式

#### 3. [源代码] [高严重] [168处 any 类型使用]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - `stable-runner-factory.ts`: `opts: any` -> `any`
  - `billing-service-async.ts`: `input: any` 方法签名
  - 重试/超时工具使用 `(...args: any[]) => Promise<any>`
  - 绕过类型安全
- **建议修复**: 
  1. 使用泛型替代 any
  2. 替换为具体的输入类型
  3. 添加 ESLint 规则禁止 any

#### 4. [源代码] [高严重] [38处 @ts-ignore 指令]
- **文件/路径**: 多个文件
- **问题描述**: 
  - 14个 stability 相关文件有 `@ts-ignore ExecutionRecord type mismatch`
  - 5个 harness-sdk 文件有 `Partial<HarnessRun> doesn't have all required properties`
  - 2个文件有 `exactOptionalPropertyTypes` 问题
- **建议修复**: 
  1. 修复 ExecutionRecord 类型不匹配
  2. 完成 HarnessRun 类型的必需属性
  3. 移除 @ts-ignore 使用

#### 5. [源代码] [高严重] [ExecutionRecord 类型不匹配]
- **文件/路径**: `src/platform/stability/stable-evidence-bundle-support.ts:783` 等14个文件
- **问题描述**: 
  - 插入 execution record 时 TypeScript 拒绝类型
  - `ExecutionRecord` 接口要求新字段但 insert 对象缺失
  - 或 store 的 insert 方法接受不同类型
- **建议修复**: 
  1. 对齐 ExecutionRecord 接口和 store 类型
  2. 添加缺失字段到 insert 对象
  3. 或调整 store 的 insert 方法类型签名

#### 6. [源代码] [中严重] [30+处 as unknown as 双类型转换]
- **文件/路径**: `division-loader.ts`, `domain-baseline-catalog.ts`, `harness-sdk`
- **问题描述**: 
  - 完全绕过类型安全
  - 通常是类型架构问题的信号
- **建议修复**: 
  1. 修复上游类型问题
  2. 使用更具体的类型断言
  3. 重构类型层次结构

#### 7. [构建] [中严重] [rimraf  extraneous - 未在 package.json 声明]
- **文件/路径**: `node_modules/rimraf`
- **问题描述**: 
  - rimraf@6.1.3 存在于 node_modules 但未在 package.json 声明
  - 可能导致依赖问题
- **建议修复**: 
  1. 添加到 dependencies 或 devDependencies
  2. 或移除并使用其他清理方式

#### 8. [测试] [高严重] [178处直接 process.env 变更无抽象]
- **文件/路径**: `tests/unit/domains/registry/plugin-spi-registry.test.ts:640-687`
- **问题描述**: 
  - 直接读取/写入 process.env
  - 在 beforeEach/afterEach 中手动保存/恢复
  - 无 helper 抽象
- **建议修复**: 
  1. 创建 env helper 函数
  2. 统一保存和恢复模式
  3. 添加测试隔离验证

#### 9. [测试] [高严重] [21,998处 unlinkSync 调用无集中清理]
- **文件/路径**: 多个测试文件
- **问题描述**: 
  - 每个测试重新实现清理逻辑
  - 无集中清理 utility
  - 可能导致 EBUSY (Windows) 或并发访问问题
- **建议修复**: 
  1. 创建集中的测试清理 utility
  2. 使用 afterEach 确保清理
  3. 考虑使用临时目录

#### 10. [测试] [中严重] [无统一 mocking 框架]
- **文件/路径**: `tests/` 多个目录
- **问题描述**: 
  - 10,579 处 vi.fn, jest.mock, sinon 等调用
  - 无统一的 mocking 框架可见
  - 模式不一致
- **建议修复**: 
  1. 建立统一的 mock factory
  2. 标准化 mock 模式
  3. 添加 mocking 最佳实践文档

#### 11. [测试] [中严重] [Singleton 状态重置未标准化]
- **文件/路径**: `tests/unit/platform/five-plane-execution/dispatcher.test.ts`
- **问题描述**: 
  - 测试通过特定函数重置 singletons
  - 每个测试重复自己的 reset 逻辑
  - 无统一的 resetAllSingletons()
- **建议修复**: 
  1. 创建统一的 singleton reset 机制
  2. 在 afterEach 中自动调用
  3. 文档化 singleton 重置要求

#### 12. [测试] [低严重] [无分支覆盖率要求]
- **文件/路径**: `.c8rc.json`
- **问题描述**: 
  - `"100": false` - 无覆盖率要求
  - 只测量源代码，不测量测试文件
- **建议修复**: 
  1. 添加分支覆盖率阈值
  2. 在 CI 中强制执行覆盖率 gate
  3. 关注关键代码路径

#### 13. [网络] [高严重] [ScopedExternalAccessSandbox performHttpRequest 无超时]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/scoped-external-access-sandbox.ts:289`
- **问题描述**: 
  - fetch 调用无 timeout 参数
  - 挂起的请求可能永久阻塞
- **建议修复**: 
  1. 添加 AbortController timeout
  2. 使用标准的超时模式
  3. 记录超时配置

#### 14. [网络] [高严重] [OIDC 服务 fetch 调用无超时]
- **文件/路径**: `src/org-governance/sso-scim/oidc/oidc-service.ts:565,272`
- **问题描述**: 
  - OIDC token 交换无 retry logic
  - 无 timeout/abort signal
  - 认证流程可能挂起
- **建议修复**: 
  1. 添加 fetch timeout
  2. 添加 retry logic
  3. 使用标准的超时配置

#### 15. [网络] [高严重] [plugin-runtime-child 替换 globalThis.fetch 为 no-op]
- **文件/路径**: `src/domains/registry/plugin-runtime-child.ts:112`
- **问题描述**: 
  ```typescript
  globalThis.fetch = (async () => {}) as unknown as typeof globalThis.fetch;
  ```
  - 插件上下文中完全替换 fetch 为空函数
  - 插件的所有出站 HTTP 请求都会失败
  - 看起来是有意为之但可能造成问题
- **建议修复**: 
  1. 确认这是有意行为
  2. 文档化 fetch 替换行为
  3. 确保不会意外影响其他代码

#### 16. [网络] [中严重] [无显式 http.Agent 配置]
- **文件/路径**: 全局
- **问题描述**: 
  - 无显式的 http.Agent/https.Agent 配置
  - 依赖 Node.js 默认连接池
  - 无 maxSockets 或 maxConnections 配置
  - 高吞吐场景可能出问题
- **建议修复**: 
  1. 添加 http Agent 配置
  2. 文档化连接池大小
  3. 监控连接使用情况

#### 17. [序列化] [中严重] [合同版本硬编码为字符串字面量]
- **文件/路径**: `src/platform/contracts/inter-plane-contract-gateway.ts:184`
- **问题描述**: 
  - `schemaVersion: "v4.3"` 硬编码
  - 难以追踪合同版本演变
- **建议修复**: 
  1. 提取到类型化常量或 enum
  2. 集中管理合同版本
  3. 添加版本兼容性检查

#### 18. [序列化] [低严重] [Timestamp = string 丢失精度]
- **文件/路径**: `src/platform/contracts/types/domain/primitives.ts:10`
- **问题描述**: 
  - `type Timestamp = string` 无类型精度
  - ISO 8601 字符串无时区信息
- **建议修复**: 
  1. 使用 Date 对象或 branded string 类型
  2. 明确时区处理约定
  3. 文档化时间戳语义

#### 19. [源代码] [中严重] [29处非空断言 !.]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - 使用 `!.` 断言非空
  - 可能导致运行时错误
- **建议修复**: 
  1. 使用可选链 `?.` 或空值合并 `??`
  2. 添加适当的 null 检查
  3. 减少非空断言使用

#### 20. [API] [中严重] [API 路由 schema 版本硬编码]
- **文件/路径**: `src/platform/five-plane-interface/api/federation-routing-service.ts`
- **问题描述**: 
  - Zod schema 验证存在
  - 但 schema 版本未与 contract 版本同步
- **建议修复**: 
  1. 从 contract 版本派生 schema 版本
  2. 添加版本一致性检查
  3. 文档化版本关系

### 总结

本次补充Review（第十五轮 - 构建测试与类型系统Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. 无 TypeScript 增量编译缓存（每次完整重建9.6秒）
2. 76个 npm scripts 大部分重复构建
3. 168处 any 类型使用（绕过类型安全）
4. 38处 @ts-ignore 指令
5. ExecutionRecord 类型不匹配（14个文件）
6. 178处直接 process.env 变更无抽象
7. ScopedExternalAccessSandbox performHttpRequest 无超时
8. OIDC 服务 fetch 调用无超时
9. plugin-runtime-child 替换 globalThis.fetch 为 no-op

**中优先级**:
1. 30+处 as unknown as 双类型转换
2. rimraf extraneous 未声明
3. 21,998处 unlinkSync 无集中清理
4. 无统一 mocking 框架
5. Singleton 状态重置未标准化
6. 无显式 http.Agent 配置
7. 合同版本硬编码为字符串
8. Timestamp = string 丢失精度
9. 非空断言使用

**低优先级**:
1. 无分支覆盖率要求
2. API 路由 schema 版本未同步

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 72 | 87 | 39 | 198 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **109** | **182** | **85** | **376** |

### 优先修复建议

**立即处理 (本周内)**:
1. 添加 TypeScript 增量编译 (`incremental: true`)
2. 重构 npm scripts 避免重复构建
3. 减少 any 类型使用到 <50
4. 移除所有 @ts-ignore 指令
5. 修复 ExecutionRecord 类型不匹配
6. 添加 fetch 超时到 OIDC 和 sandbox

**短期内处理 (本月内)**:
1. 创建集中的测试清理 utility
2. 建立统一的 mocking 框架
3. 标准化 singleton 重置机制
4. 添加 http.Agent 连接池配置
5. 提取合同版本到类型化常量

**长期规划**:
1. 实现完整的类型安全（无 any）
2. 建立测试基础设施标准
3. 实现构建缓存优化
4. 完善网络层超时和重试
5. 建立覆盖率门禁标准

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十三轮 - 缓存会话与实时通信Review）

### 发现的新问题

#### 1. [安全] [严重] [CORS allowedMethods 缺少 PUT/PATCH/DELETE]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/response-hardening.ts:12-14`
- **问题描述**: 
  ```typescript
  export const DEFAULT_CORS_CONFIG: CorsConfig = {
    allowedMethods: ["GET", "POST", "OPTIONS"],  // 缺少 PUT, PATCH, DELETE
  ```
  - 浏览器 CORS preflight 会拒绝 PUT/PATCH/DELETE 请求
  - 但路由中有 PATCH /v1/tasks/:id, DELETE endpoints
- **建议修复**: 
  1. 添加 PUT, PATCH, DELETE 到 allowedMethods
  2. 确保 CORS 配置与应用路由方法一致
  3. 添加测试验证所有 HTTP 方法的 CORS preflight

#### 2. [安全] [严重] [Mission Routes 错误响应包装不当]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/mission-routes.ts:101,118-124`
- **问题描述**: 
  ```typescript
  return buildJsonResponse(ctx.requestId, 404, { error: { code: "MISSION_NOT_FOUND" } });
  ```
  - 使用 buildJsonResponse（成功包装）返回错误
  - 错误被包装在成功信封 `{requestId, data: {error: {...}}}` 中
  - 违反 REST 规范
- **建议修复**: 
  1. 使用 buildJsonErrorResponse 处理错误
  2. 确保所有错误路径使用正确的错误信封
  3. 添加 API 一致性测试

#### 3. [源代码] [高严重] [WebSocketBridge 无最大连接数限制]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:103-107`
- **问题描述**: 
  ```typescript
  this.wss = new WebSocketServer({
    server,
    path: WS_PATH,
    maxPayload: options.maxPayloadBytes ?? DEFAULT_MAX_PAYLOAD_BYTES,
    // 无 maxConnections 限制！
  });
  ```
  - 与 dashboard-websocket-server.ts 的 maxClients: 1000 不同
  - 恶意客户端可耗尽服务器资源
- **建议修复**: 
  1. 添加 maxConnections 限制
  2. 实现连接限制拒绝策略
  3. 添加连接计数指标

#### 4. [源代码] [高严重] [pendingAcks 在断开连接时未清理]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:268-282`
- **问题描述**: 
  - handleDisconnection 清理 subscribedTasks, taskSubscribers, slowConsumers
  - 但 pendingAcks Map 未清理
  - 断开的客户端未确认的消息永久留在内存
- **建议修复**: 
  1. 在 handleDisconnection 中删除所有 pendingAcks 条目
  2. 考虑添加超时机制自动清理未确认消息
  3. 记录清理操作的审计日志

#### 5. [安全] [高严重] [硬编码回退密钥]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/audit-event-integrity.ts:41`
- **问题描述**: 
  ```typescript
  const AUDIT_INTEGRITY_HMAC_KEY = process.env["AA_AUDIT_INTEGRITY_HMAC_KEY"] ?? "audit-integrity-secret-key-32-bytes!";
  ```
  - 占位符密钥用作回退
  - 如果环境变量未设置，使用不安全密钥
- **建议修复**: 
  1. 移除回退值，要求环境变量必须设置
  2. 在启动时验证密钥存在且足够强
  3. 如果密钥缺失导致启动失败

#### 6. [配置] [高严重] [config/runtime/test.json 缺少超时配置]
- **文件/路径**: `config/runtime/test.json`
- **问题描述**: 
  - 只有3个字段，缺少 maxConcurrentTasks, defaultTaskTimeoutMs, defaultStepTimeoutMs
  - 回退到默认值可能不适合测试环境
- **建议修复**: 
  1. 添加完整的 timeout/tuning 配置
  2. 确保测试环境有合理的默认值
  3. 文档化必需的配置字段

#### 7. [配置] [高严重] [prod.json 限制过严 - maxConcurrentTasks=1]
- **文件/路径**: `config/runtime/prod.json`
- **问题描述**: 
  - maxConcurrentTasks: 1（vs dev:8, staging:4, pre-prod:6）
  - defaultTaskTimeoutMs: 120000（vs dev:600000）
  - defaultStepTimeoutMs: 60000（vs dev:180000）
  - 生产环境严重受限，可能影响吞吐量
- **建议修复**: 
  1. 评估并调整生产限制
  2. 与业务需求匹配
  3. 添加生产容量测试

#### 8. [源代码] [高严重] [多个服务存在无界 Map 无驱逐策略]
- **文件/路径**: 多个域服务
- **问题描述**: 
  - `domain-knowledge-schema-service.ts:73-75`: schemas, sourceContent, sourceTimestamps 无限制
  - `domain-eval-framework-service.ts:91-95`: frameworks, qualityAxes 等无限制
  - `domain-recipe-service.ts:67-68`: recipes, versions 无限制
  - `domain-risk-profile-service.ts:51`: profiles 无限制
  - Session Maps (session-management.ts:83-89) 无自动清理
- **建议修复**: 
  1. 为所有 Map 实现 LRU 或 TTL 驱逐策略
  2. 添加后台清理任务
  3. 添加大小监控和告警

#### 9. [源代码] [中严重] [taskEventHistory Map 永不清理]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:91,548-555`
- **问题描述**: 
  - taskEventHistory 限制单任务历史为 200 条
  - 但 Map 本身从不清理
  - 取消订阅的任务历史永久保留
- **建议修复**: 
  1. 实现后台清理无订阅者任务的历史
  2. 添加任务订阅者监控
  3. 考虑使用 WeakMap 替代

#### 10. [配置] [中严重] [安全配置 drift - remoteWorkerRegistration 缺失]
- **文件/路径**: `config/security/dev.json`, `staging.json`, `pre-prod.json`
- **问题描述**: 
  - default.json 有完整的 remoteWorkerRegistration 配置
  - dev/staging/pre-prod 只有 approvalMode
  - prod 有 approvalMode 但无 remoteWorkerRegistration
  - 安全配置不一致
- **建议修复**: 
  1. 统一所有环境的 remoteWorkerRegistration
  2. 添加安全配置验证
  3. 确保最低安全基线

#### 11. [API] [中严重] [OpenAPI 端点响应格式不一致]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/utils.ts:207-215`
- **问题描述**: 
  - `/v1/openapi.json` 使用 buildJsonDocumentResponse（无包装）
  - 其他端点使用 buildJsonResponse（{requestId, data} 包装）
  - 客户端体验不一致
- **建议修复**: 
  1. 统一响应信封格式
  2. 文档化响应格式规范
  3. 添加响应格式验证测试

#### 12. [源代码] [中严重] [Redis 队列缺少 idempotency 索引支持]
- **文件/路径**: `src/platform/five-plane-execution/queue/redis-queue-adapter.ts`
- **问题描述**: 
  - SQLite 有部分唯一索引支持 idempotency
  - Redis 实现使用哈希索引但实现不完整
  - 可能导致重复消息
- **建议修复**: 
  1. 完善 Redis idempotency 实现
  2. 添加唯一索引验证
  3. 确保与 SQLite 行为一致

#### 13. [源代码] [中严重] [请求去重中间件使用内存存储]
- **文件/路径**: `src/platform/five-plane-interface/api/middleware/request-deduplication.ts`
- **问题描述**: 
  - 纯内存滑动窗口（Map<DeduplicationKey, DeduplicationEntry[]>）
  - 无外部存储
  - 多实例部署不共享
  - 重启后去重状态丢失
- **建议修复**: 
  1. 使用 Redis 替代内存存储
  2. 支持分布式去重
  3. 持久化去重状态

#### 14. [源代码] [中严重] [Cache 无 stampede 保护]
- **文件/路径**: 多个 cache 实现
- **问题描述**: 
  - MemoryCacheStore, ExperienceCacheService 无锁
  - 缓存未命中时可发生 thundering herd
  - 高并发下可能导致数据库过载
- **建议修复**: 
  1. 实现 single-flight 模式
  2. 添加请求排队机制
  3. 使用分布式锁保护缓存更新

#### 15. [源代码] [中严重] [EvidenceService eviction 仅在插入时触发]
- **文件/路径**: `src/platform/five-plane-state-evidence/memory/evidence-service.ts:202`
- **问题描述**: 
  - 清理仅在 record() 调用时触发
  - 空闲时无后台清理
  - 可能导致内存持续增长
- **建议修复**: 
  1. 添加定期后台清理任务
  2. 使用独立的清理线程
  3. 添加内存使用监控

#### 16. [源代码] [低严重] [无连接指标暴露]
- **文件/路径**: WebSocket 服务器
- **问题描述**: 
  - getClientCount() 存在但未通过 HTTP 端点暴露
  - 无 pendingAcks 队列深度指标
  - 无连接建立告警
- **建议修复**: 
  1. 通过 metrics 端点暴露连接指标
  2. 添加 pendingAcks 队列监控
  3. 添加连接数异常告警

#### 17. [源代码] [低严重] [无空闲客户端超时]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts`
- **问题描述**: 
  - 仅在心跳 Sweep 时检查 isAlive
  - 无 per-client 独立于心跳的空闲超时
  - 认证后从不发送消息的客户端只能通过心跳失败检测
- **建议修复**: 
  1. 添加 per-client idle timeout
  2. 独立于心跳间隔
  3. 配置可调整

#### 18. [源代码] [低严重] [订阅限制是 per-client 而非全局]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:244`
- **问题描述**: 
  - MAX_SUBSCRIPTIONS_PER_CLIENT = 100
  - 恶意客户端可订阅 100 个任务
  - 无全局任务订阅者限制
- **建议修复**: 
  1. 添加全局任务订阅者限制
  2. 实现 per-task 订阅者上限
  3. 添加反滥用检测

#### 19. [配置] [低严重] [config/runtime 无版本验证机制]
- **文件/路径**: `config/runtime/*.json`
- **问题描述**: 
  - 配置有 "version": "v4.3"
  - 但无 schema 版本验证
  - 加载时可能接受不兼容配置
- **建议修复**: 
  1. 添加 JSON Schema 验证
  2. 实现版本兼容性检查
  3. 启动时验证配置完整性

#### 20. [源代码] [低严重] [ImprovementCandidateRegistry splice 是 O(n)]
- **文件/路径**: `src/platform/five-plane-orchestration/improve/improvement-candidate-registry.ts:243`
- **问题描述**: 
  - accessOrder 数组的 splice 操作是 O(n)
  - 高频访问时可能性能问题
- **建议修复**: 
  1. 使用 LinkedList 替代数组
  2. 或使用 Map 维护访问顺序
  3. 性能测试验证

### 总结

本次补充Review（第十三轮 - 缓存会话与实时通信Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. CORS allowedMethods 缺少 PUT/PATCH/DELETE（浏览器请求会失败）
2. Mission Routes 错误响应包装不当
3. WebSocketBridge 无最大连接数限制（DoS风险）
4. pendingAcks 断开连接时未清理（内存泄漏）
5. 硬编码回退密钥（安全漏洞）
6. config/runtime/test.json 缺少超时配置
7. prod.json maxConcurrentTasks=1 限制过严
8. 多个服务存在无界 Map（内存泄漏风险）

**中优先级**:
1. taskEventHistory Map 永不清理
2. 安全配置 drift（remoteWorkerRegistration 缺失）
3. OpenAPI 端点响应格式不一致
4. Redis 队列 idempotency 实现不完整
5. 请求去重中间件使用内存存储（多实例不共享）
6. Cache 无 stampede 保护
7. EvidenceService eviction 仅在插入时触发

**低优先级**:
1. 无连接指标暴露
2. 无空闲客户端超时
3. 订阅限制是 per-client 而非全局
4. config/runtime 无版本验证机制
5. ImprovementCandidateRegistry splice O(n)

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 64 | 74 | 35 | 173 |
| 测试 | 8 | 14 | 5 | 27 |
| 配置 | 10 | 38 | 24 | 72 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 7 | 3 | 12 |
| UI | 0 | 3 | 3 | 6 |
| 部署 | 1 | 10 | 6 | 17 |
| **合计** | **99** | **161** | **79** | **339** |

### 优先修复建议

**立即处理 (本周内)**:
1. 修复 CORS allowedMethods 添加 PUT/PATCH/DELETE
2. 修复 Mission Routes 错误响应格式
3. 为 WebSocketBridge 添加最大连接数限制
4. 清理 pendingAcks 和 taskEventHistory
5. 移除硬编码回退密钥
6. 修复 config/runtime/test.json 配置
7. 评估并调整 prod 并发限制

**短期内处理 (本月内)**:
1. 统一安全配置（remoteWorkerRegistration）
2. 实现无界 Map 的 LRU/TTL 驱逐
3. 统一 API 响应信封格式
4. 完善 Redis idempotency 实现
5. 添加分布式去重中间件
6. 实现 cache stampede 保护

**长期规划**:
1. 建立完整的连接管理和监控
2. 实现后台清理任务框架
3. 添加配置 schema 验证
4. 优化高频繁操作的数据结构
5. 建立内存使用基线和告警

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十五轮 - 构建测试与类型系统Review）

### 发现的新问题

#### 1. [构建] [高严重] [无 TypeScript 增量编译缓存]
- **文件/路径**: `tsconfig.json`
- **问题描述**: 
  - 无 `incremental: true` 配置
  - 无 `tsbuildinfo` 文件
  - 每次构建是完整重建（9.6秒）
  - TypeScript 无法使用增量编译
- **建议修复**: 
  1. 在 tsconfig.json 添加 `"incremental": true`
  2. 添加 `"tsbuildinfo": ".tsbuildinfo"` 到 .gitignore
  3. 实现项目引用以支持并行构建

#### 2. [构建] [高严重] [76个 npm scripts 大部分重复构建]
- **文件/路径**: `package.json`
- **问题描述**: 
  - 40+ CLI scripts 都运行 `npm run build && node ...`
  - 每个脚本前都执行完整重建
  - 无法直接运行预构建的 CLI
  - 20+ stable:* 脚本遵循相同模式
- **建议修复**: 
  1. 创建 `build:cli` 脚本构建一次
  2. 让 CLI 脚本使用预构建的 dist 文件
  3. 添加 npm run dev 或类似的无需重建的执行方式

#### 3. [源代码] [高严重] [168处 any 类型使用]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - `stable-runner-factory.ts`: `opts: any` -> `any`
  - `billing-service-async.ts`: `input: any` 方法签名
  - 重试/超时工具使用 `(...args: any[]) => Promise<any>`
  - 绕过类型安全
- **建议修复**: 
  1. 使用泛型替代 any
  2. 替换为具体的输入类型
  3. 添加 ESLint 规则禁止 any

#### 4. [源代码] [高严重] [38处 @ts-ignore 指令]
- **文件/路径**: 多个文件
- **问题描述**: 
  - 14个 stability 相关文件有 `@ts-ignore ExecutionRecord type mismatch`
  - 5个 harness-sdk 文件有 `Partial<HarnessRun> doesn't have all required properties`
  - 2个文件有 `exactOptionalPropertyTypes` 问题
- **建议修复**: 
  1. 修复 ExecutionRecord 类型不匹配
  2. 完成 HarnessRun 类型的必需属性
  3. 移除 @ts-ignore 使用

#### 5. [源代码] [高严重] [ExecutionRecord 类型不匹配]
- **文件/路径**: `src/platform/stability/stable-evidence-bundle-support.ts:783` 等14个文件
- **问题描述**: 
  - 插入 execution record 时 TypeScript 拒绝类型
  - `ExecutionRecord` 接口要求新字段但 insert 对象缺失
  - 或 store 的 insert 方法接受不同类型
- **建议修复**: 
  1. 对齐 ExecutionRecord 接口和 store 类型
  2. 添加缺失字段到 insert 对象
  3. 或调整 store 的 insert 方法类型签名

#### 6. [源代码] [中严重] [30+处 as unknown as 双类型转换]
- **文件/路径**: `division-loader.ts`, `domain-baseline-catalog.ts`, `harness-sdk`
- **问题描述**: 
  - 完全绕过类型安全
  - 通常是类型架构问题的信号
- **建议修复**: 
  1. 修复上游类型问题
  2. 使用更具体的类型断言
  3. 重构类型层次结构

#### 7. [构建] [中严重] [rimraf  extraneous - 未在 package.json 声明]
- **文件/路径**: `node_modules/rimraf`
- **问题描述**: 
  - rimraf@6.1.3 存在于 node_modules 但未在 package.json 声明
  - 可能导致依赖问题
- **建议修复**: 
  1. 添加到 dependencies 或 devDependencies
  2. 或移除并使用其他清理方式

#### 8. [测试] [高严重] [178处直接 process.env 变更无抽象]
- **文件/路径**: `tests/unit/domains/registry/plugin-spi-registry.test.ts:640-687`
- **问题描述**: 
  - 直接读取/写入 process.env
  - 在 beforeEach/afterEach 中手动保存/恢复
  - 无 helper 抽象
- **建议修复**: 
  1. 创建 env helper 函数
  2. 统一保存和恢复模式
  3. 添加测试隔离验证

#### 9. [测试] [高严重] [21,998处 unlinkSync 调用无集中清理]
- **文件/路径**: 多个测试文件
- **问题描述**: 
  - 每个测试重新实现清理逻辑
  - 无集中清理 utility
  - 可能导致 EBUSY (Windows) 或并发访问问题
- **建议修复**: 
  1. 创建集中的测试清理 utility
  2. 使用 afterEach 确保清理
  3. 考虑使用临时目录

#### 10. [测试] [中严重] [无统一 mocking 框架]
- **文件/路径**: `tests/` 多个目录
- **问题描述**: 
  - 10,579 处 vi.fn, jest.mock, sinon 等调用
  - 无统一的 mocking 框架可见
  - 模式不一致
- **建议修复**: 
  1. 建立统一的 mock factory
  2. 标准化 mock 模式
  3. 添加 mocking 最佳实践文档

#### 11. [测试] [中严重] [Singleton 状态重置未标准化]
- **文件/路径**: `tests/unit/platform/five-plane-execution/dispatcher.test.ts`
- **问题描述**: 
  - 测试通过特定函数重置 singletons
  - 每个测试重复自己的 reset 逻辑
  - 无统一的 resetAllSingletons()
- **建议修复**: 
  1. 创建统一的 singleton reset 机制
  2. 在 afterEach 中自动调用
  3. 文档化 singleton 重置要求

#### 12. [测试] [低严重] [无分支覆盖率要求]
- **文件/路径**: `.c8rc.json`
- **问题描述**: 
  - `"100": false` - 无覆盖率要求
  - 只测量源代码，不测量测试文件
- **建议修复**: 
  1. 添加分支覆盖率阈值
  2. 在 CI 中强制执行覆盖率 gate
  3. 关注关键代码路径

#### 13. [网络] [高严重] [ScopedExternalAccessSandbox performHttpRequest 无超时]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/scoped-external-access-sandbox.ts:289`
- **问题描述**: 
  - fetch 调用无 timeout 参数
  - 挂起的请求可能永久阻塞
- **建议修复**: 
  1. 添加 AbortController timeout
  2. 使用标准的超时模式
  3. 记录超时配置

#### 14. [网络] [高严重] [OIDC 服务 fetch 调用无超时]
- **文件/路径**: `src/org-governance/sso-scim/oidc/oidc-service.ts:565,272`
- **问题描述**: 
  - OIDC token 交换无 retry logic
  - 无 timeout/abort signal
  - 认证流程可能挂起
- **建议修复**: 
  1. 添加 fetch timeout
  2. 添加 retry logic
  3. 使用标准的超时配置

#### 15. [网络] [高严重] [plugin-runtime-child 替换 globalThis.fetch 为 no-op]
- **文件/路径**: `src/domains/registry/plugin-runtime-child.ts:112`
- **问题描述**: 
  ```typescript
  globalThis.fetch = (async () => {}) as unknown as typeof globalThis.fetch;
  ```
  - 插件上下文中完全替换 fetch 为空函数
  - 插件的所有出站 HTTP 请求都会失败
  - 看起来是有意为之但可能造成问题
- **建议修复**: 
  1. 确认这是有意行为
  2. 文档化 fetch 替换行为
  3. 确保不会意外影响其他代码

#### 16. [网络] [中严重] [无显式 http.Agent 配置]
- **文件/路径**: 全局
- **问题描述**: 
  - 无显式的 http.Agent/https.Agent 配置
  - 依赖 Node.js 默认连接池
  - 无 maxSockets 或 maxConnections 配置
  - 高吞吐场景可能出问题
- **建议修复**: 
  1. 添加 http Agent 配置
  2. 文档化连接池大小
  3. 监控连接使用情况

#### 17. [序列化] [中严重] [合同版本硬编码为字符串字面量]
- **文件/路径**: `src/platform/contracts/inter-plane-contract-gateway.ts:184`
- **问题描述**: 
  - `schemaVersion: "v4.3"` 硬编码
  - 难以追踪合同版本演变
- **建议修复**: 
  1. 提取到类型化常量或 enum
  2. 集中管理合同版本
  3. 添加版本兼容性检查

#### 18. [序列化] [低严重] [Timestamp = string 丢失精度]
- **文件/路径**: `src/platform/contracts/types/domain/primitives.ts:10`
- **问题描述**: 
  - `type Timestamp = string` 无类型精度
  - ISO 8601 字符串无时区信息
- **建议修复**: 
  1. 使用 Date 对象或 branded string 类型
  2. 明确时区处理约定
  3. 文档化时间戳语义

#### 19. [源代码] [中严重] [29处非空断言 !.]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - 使用 `!.` 断言非空
  - 可能导致运行时错误
- **建议修复**: 
  1. 使用可选链 `?.` 或空值合并 `??`
  2. 添加适当的 null 检查
  3. 减少非空断言使用

#### 20. [API] [中严重] [API 路由 schema 版本硬编码]
- **文件/路径**: `src/platform/five-plane-interface/api/federation-routing-service.ts`
- **问题描述**: 
  - Zod schema 验证存在
  - 但 schema 版本未与 contract 版本同步
- **建议修复**: 
  1. 从 contract 版本派生 schema 版本
  2. 添加版本一致性检查
  3. 文档化版本关系

### 总结

本次补充Review（第十五轮 - 构建测试与类型系统Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. 无 TypeScript 增量编译缓存（每次完整重建9.6秒）
2. 76个 npm scripts 大部分重复构建
3. 168处 any 类型使用（绕过类型安全）
4. 38处 @ts-ignore 指令
5. ExecutionRecord 类型不匹配（14个文件）
6. 178处直接 process.env 变更无抽象
7. ScopedExternalAccessSandbox performHttpRequest 无超时
8. OIDC 服务 fetch 调用无超时
9. plugin-runtime-child 替换 globalThis.fetch 为 no-op

**中优先级**:
1. 30+处 as unknown as 双类型转换
2. rimraf extraneous 未声明
3. 21,998处 unlinkSync 无集中清理
4. 无统一 mocking 框架
5. Singleton 状态重置未标准化
6. 无显式 http.Agent 配置
7. 合同版本硬编码为字符串
8. Timestamp = string 丢失精度
9. 非空断言使用

**低优先级**:
1. 无分支覆盖率要求
2. API 路由 schema 版本未同步

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 72 | 87 | 39 | 198 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **109** | **182** | **85** | **376** |

### 优先修复建议

**立即处理 (本周内)**:
1. 添加 TypeScript 增量编译 (`incremental: true`)
2. 重构 npm scripts 避免重复构建
3. 减少 any 类型使用到 <50
4. 移除所有 @ts-ignore 指令
5. 修复 ExecutionRecord 类型不匹配
6. 添加 fetch 超时到 OIDC 和 sandbox

**短期内处理 (本月内)**:
1. 创建集中的测试清理 utility
2. 建立统一的 mocking 框架
3. 标准化 singleton 重置机制
4. 添加 http.Agent 连接池配置
5. 提取合同版本到类型化常量

**长期规划**:
1. 实现完整的类型安全（无 any）
2. 建立测试基础设施标准
3. 实现构建缓存优化
4. 完善网络层超时和重试
5. 建立覆盖率门禁标准

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十四轮 - 日志事件与工作流Review）

### 发现的新问题

#### 1. [可观测性] [高严重] [StructuredLogEntry 无 requestId 字段]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - StructuredLogEntry 接口无 requestId 字段
  - 只有 correlationId，它回退到 traceId
  - 无法独立追踪单个 HTTP 请求
- **建议修复**: 
  1. 在 StructuredLogEntry 添加 requestId 字段
  2. 在 HTTP 中间件中生成和注入 requestId
  3. 确保 requestId 流经所有日志调用

#### 2. [可观测性] [高严重] [StructuredLogger data 字段无自动清洗]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - 如果开发者传递 `{ password: "..." }` 给 logger.info，密码会被记录
  - 无字段级脱敏机制
  - 依赖开发者手动排除敏感字段
- **建议修复**: 
  1. 添加 OBSERVE_OUTPUT_BLACKLIST 类似的黑名单
  2. 实现 redact/mask/sanitize 工具函数
  3. 在日志传输前自动清洗敏感字段

#### 3. [可观测性] [中严重] [日志中敏感数据无 PII 处理]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - 无 PII 重写工具
  - 开发者必须手动避免记录敏感数据
  - 存在意外记录敏感信息的风险
- **建议修复**: 
  1. 添加 PII 检测和重写工具
  2. 在文档中明确记录敏感字段列表
  3. 添加测试验证敏感数据不被记录

#### 4. [可观测性] [中严重] [59处 console.* 直接调用]
- **文件/路径**: 多个源文件
- **问题描述**: 
  - 应该使用 StructuredLogger
  - console 调用可能不被日志聚合系统收集
  - 难以追踪和关联
- **建议修复**: 
  1. 替换所有 console.* 为 StructuredLogger
  2. 添加 ESLint 规则禁止 console.*
  3. 保留少量允许的 CLI 输出

#### 5. [可观测性] [中严重] [部分 logger.warn 调用缺少结构化数据]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:122`
- **问题描述**: 
  ```typescript
  logger.warn("WebSocket connection rejected: missing subprotocol token")
  ```
  - 只有消息，无 data 对象
  - 难以关联和查询
- **建议修复**: 
  1. 添加 `{ data: { ... } }` 对象
  2. 确保所有日志包含上下文数据
  3. 添加日志审查工具

#### 6. [源代码] [高严重] [Harness while(true) 循环无硬性迭代上限]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:1442`
- **问题描述**: 
  ```typescript
  while (true) {
    // ...
    // 依赖 budget gate 和 guardrail vibration 退出
    // 如果两者都失败，循环永久继续
  }
  ```
  - 依赖 budget gate 和 guardrail vibration 退出
  - 无硬性迭代次数限制
  - 可能导致无限循环
- **建议修复**: 
  1. 添加硬性 maxIterations 上限
  2. 在达到上限时强制退出
  3. 记录详细的迭代统计用于调试

#### 7. [源代码] [高严重] [Oapeflir while(true) 循环可能无限重规划]
- **文件/路径**: `src/platform/five-plane-orchestration/oapeflir/oapeflir-loop-service.ts:355`
- **问题描述**: 
  - 如果 loopReplanDecision.shouldReplan 永不变为 false
  - 重规划永远无法减少计划错误
  - 可能导致无限循环
- **建议修复**: 
  1. 添加重规划次数上限
  2. 在达到上限时强制接受当前结果
  3. 添加重规划质量衰减检测

#### 8. [源代码] [中严重] [Recovery 流程允许终态转换到 paused]
- **文件/路径**: `src/platform/five-plane-execution/runtime-state-machine.ts:105-108`
- **问题描述**: 
  - completed, failed, cancelled, aborted 可转换到 paused 进行恢复
  - 创建恢复路径可能重新进入执行
  - 需要验证此行为的正确性
- **建议修复**: 
  1. 审查 recovery 转换的合法性
  2. 确保不会导致状态不一致
  3. 添加转换前置条件验证

#### 9. [国际化] [高严重] [API 错误消息硬编码英文]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/api-error.ts`
- **问题描述**: 
  ```typescript
  return new ApiError(404, "api.task_not_found", "Task not found.");
  ```
  - 错误消息硬编码英文
  - 无后端 i18n 机制
  - 非英文客户端收到不本地化的错误
- **建议修复**: 
  1. 使用错误码而非硬编码消息
  2. 客户端根据错误码本地化
  3. 或在后端添加 i18n 支持

#### 10. [国际化] [中严重] [UI 硬编码字符串未翻译]
- **文件/路径**: `ui/packages/features/conversation/src/web/index.tsx`
- **问题描述**: 
  - "Build Plan", "Execute", "Messages" 等按钮标签硬编码
  - 不使用 i18n 系统
- **建议修复**: 
  1. 使用 `translateFeatureCopy` 替换硬编码
  2. 确保所有用户可见文本通过 i18n
  3. 添加硬编码字符串检测

#### 11. [国际化] [中严重] [Arabic 目录不完整]
- **文件/路径**: `ui/packages/shared/i18n/src/catalogs/ar-SA.ts`
- **问题描述**: 
  - ar-SA 只有约18条消息
  - en-US 和 zh-CN 有约117条
  - 缺失的 key 返回 key 本身
- **建议修复**: 
  1. 补充 Arabic 翻译
  2. 确保所有 key 都有翻译
  3. 添加翻译完整性测试

#### 12. [国际化] [低严重] [translateFeatureCopy 无回退默认值]
- **文件/路径**: `ui/packages/shared/i18n/`
- **问题描述**: 
  - 如果 feature ID 不在 catalog 中
  - 返回 undefined title/summary
  - 无合理回退
- **建议修复**: 
  1. 添加回退到 key 或默认字符串
  2. 或使用 feature ID 作为显示名
  3. 确保 UI 不显示 undefined

#### 13. [运维] [高严重] [健康检查无 readiness/liveness 区分]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts`
- **问题描述**: 
  - /healthz, /v1/healthz, /health 返回相同报告
  - 无 readiness probe（可处理流量？）
  - 无 liveness probe（系统存活？）
  - canAcceptTraffic() 未通过 HTTP 暴露
- **建议修复**: 
  1. 添加 /ready 和 /live 端点
  2. Readiness 检查依赖项就绪
  3. Liveness 检查进程健康

#### 14. [运维] [高严重] [shutdown 期间健康检查存在竞态]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts`
- **问题描述**: 
  - getHealthReportAsync() 异步检查 dbWritable
  - shutdown 期间数据库连接可能关闭
  - 健康检查仍在传入
- **建议修复**: 
  1. 在 shutdown 时返回 unhealthy
  2. 添加 shutdown 状态标志
  3. 停止接受新的健康检查请求

#### 15. [运维] [高严重] [LeaderElectionService 未与 GracefulShutdown 集成]
- **文件/路径**: `src/platform/five-plane-execution/ha/leader-election-service.ts`
- **问题描述**: 
  - LeaderElectionService.stop() 处理优雅退位
  - 但未在全局 graceful shutdown 中注册
  - 需显式注册为 shutdown handler
- **建议修复**: 
  1. 将 LeaderElectionService 添加到 shutdown handlers
  2. 确保领导者在 shutdown 时让位
  3. 验证关闭顺序正确

#### 16. [运维] [中严重] [addHandler() 无显式排序约定]
- **文件/路径**: `src/platform/five-plane-execution/startup/graceful-shutdown.ts`
- **问题描述**: 
  - 虽然 handlers 逆序执行
  - 但无显式接口声明 handler 依赖关系
  - 关键 handlers（如关闭数据库连接）应最后运行
- **建议修复**: 
  1. 添加优先级或阶段参数
  2. 文档化关键 handler 顺序要求
  3. 添加顺序验证测试

#### 17. [运维] [中严重] [unref'd timers 可能不触发]
- **文件/路径**: `src/platform/five-plane-execution/startup/process-error-handlers.ts:106`
- **问题描述**: 
  - `setTimeout(...).unref()` 不保持事件循环活跃
  - 如果主线程空闲，强制退出可能不触发
- **建议修复**: 
  1. 移除 .unref() 除非确实需要
  2. 或确保有其他方式保持事件循环
  3. 添加超时触发验证测试

#### 18. [运维] [中严重] [StartupConsistencyChecker 阻塞流量但不暴露状态]
- **文件/路径**: `src/platform/five-plane-startup-plan.ts`
- **问题描述**: 
  - canAcceptTraffic() 在 P0 问题存在时返回 false
  - 但 _trafficBlocked 是内部状态
  - 负载均衡器访问 /healthz 仍会得到 "ok"
- **建议修复**: 
  1. 在健康报告中暴露 trafficBlocked 状态
  2. 或添加单独的健康端点指示就绪状态
  3. 确保负载均衡器在阻塞时不路由流量

#### 19. [事件系统] [中严重] [TypedEventBus vs EventEmitter 混用]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - 项目同时使用 TypedEventBus 和原生 EventEmitter
  - TypedEventBus 应该是标准
  - 混用可能导致类型安全问题
- **建议修复**: 
  1. 创建迁移计划统一使用 TypedEventBus
  2. 添加 ESLint 规则禁止原生 EventEmitter
  3. 在 CI 中检测混用情况

#### 20. [源代码] [低严重] [无事件排序保证]
- **文件/路径**: `src/platform/five-plane-state-evidence/events/`
- **问题描述**: 
  - 无明确的事件排序保证文档
  - 事件处理可能乱序
  - 依赖方需要处理乱序
- **建议修复**: 
  1. 文档化事件排序语义
  2. 如果需要排序，实现序列号机制
  3. 在事件处理中考虑乱序情况

### 总结

本次补充Review（第十四轮 - 日志事件与工作流Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. StructuredLogEntry 无 requestId 字段
2. StructuredLogger data 字段无自动清洗（敏感数据风险）
3. Harness while(true) 循环无硬性迭代上限
4. Oapeflir while(true) 循环可能无限重规划
5. API 错误消息硬编码英文（无 i18n）
6. 健康检查无 readiness/liveness 区分
7. shutdown 期间健康检查存在竞态
8. LeaderElectionService 未与 GracefulShutdown 集成

**中优先级**:
1. 日志中敏感数据无 PII 处理
2. 59处 console.* 直接调用
3. 部分 logger.warn 调用缺少结构化数据
4. Recovery 流程允许终态转换到 paused
5. UI 硬编码字符串未翻译
6. Arabic 目录不完整
7. addHandler() 无显式排序约定
8. unref'd timers 可能不触发
9. StartupConsistencyChecker 阻塞流量但不暴露状态
10. TypedEventBus vs EventEmitter 混用

**低优先级**:
1. translateFeatureCopy 无回退默认值
2. 无事件排序保证

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 68 | 80 | 37 | 185 |
| 测试 | 8 | 14 | 5 | 27 |
| 配置 | 10 | 38 | 24 | 72 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **103** | **171** | **82** | **356** |

### 优先修复建议

**立即处理 (本周内)**:
1. 在 StructuredLogEntry 添加 requestId 字段
2. 实现日志敏感字段自动清洗
3. 为工作流循环添加硬性迭代上限
4. 添加 readiness/liveness 健康检查端点
5. 将 LeaderElectionService 集成到 GracefulShutdown
6. 实现 API 错误消息 i18n 支持

**短期内处理 (本月内)**:
1. 替换所有 console.* 为 StructuredLogger
2. 补充 Arabic i18n 目录
3. 修复 UI 硬编码字符串
4. 统一 TypedEventBus 替代 EventEmitter
5. 解决 shutdown 竞态条件
6. 添加事件排序保证文档

**长期规划**:
1. 建立完整的 PII 处理框架
2. 实现日志完整性验证
3. 建立工作流安全循环检测
4. 完善健康检查和就绪探测
5. 建立国际化完整测试

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十五轮 - 构建测试与类型系统Review）

### 发现的新问题

#### 1. [构建] [高严重] [无 TypeScript 增量编译缓存]
- **文件/路径**: `tsconfig.json`
- **问题描述**: 
  - 无 `incremental: true` 配置
  - 无 `tsbuildinfo` 文件
  - 每次构建是完整重建（9.6秒）
  - TypeScript 无法使用增量编译
- **建议修复**: 
  1. 在 tsconfig.json 添加 `"incremental": true`
  2. 添加 `"tsbuildinfo": ".tsbuildinfo"` 到 .gitignore
  3. 实现项目引用以支持并行构建

#### 2. [构建] [高严重] [76个 npm scripts 大部分重复构建]
- **文件/路径**: `package.json`
- **问题描述**: 
  - 40+ CLI scripts 都运行 `npm run build && node ...`
  - 每个脚本前都执行完整重建
  - 无法直接运行预构建的 CLI
  - 20+ stable:* 脚本遵循相同模式
- **建议修复**: 
  1. 创建 `build:cli` 脚本构建一次
  2. 让 CLI 脚本使用预构建的 dist 文件
  3. 添加 npm run dev 或类似的无需重建的执行方式

#### 3. [源代码] [高严重] [168处 any 类型使用]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - `stable-runner-factory.ts`: `opts: any` -> `any`
  - `billing-service-async.ts`: `input: any` 方法签名
  - 重试/超时工具使用 `(...args: any[]) => Promise<any>`
  - 绕过类型安全
- **建议修复**: 
  1. 使用泛型替代 any
  2. 替换为具体的输入类型
  3. 添加 ESLint 规则禁止 any

#### 4. [源代码] [高严重] [38处 @ts-ignore 指令]
- **文件/路径**: 多个文件
- **问题描述**: 
  - 14个 stability 相关文件有 `@ts-ignore ExecutionRecord type mismatch`
  - 5个 harness-sdk 文件有 `Partial<HarnessRun> doesn't have all required properties`
  - 2个文件有 `exactOptionalPropertyTypes` 问题
- **建议修复**: 
  1. 修复 ExecutionRecord 类型不匹配
  2. 完成 HarnessRun 类型的必需属性
  3. 移除 @ts-ignore 使用

#### 5. [源代码] [高严重] [ExecutionRecord 类型不匹配]
- **文件/路径**: `src/platform/stability/stable-evidence-bundle-support.ts:783` 等14个文件
- **问题描述**: 
  - 插入 execution record 时 TypeScript 拒绝类型
  - `ExecutionRecord` 接口要求新字段但 insert 对象缺失
  - 或 store 的 insert 方法接受不同类型
- **建议修复**: 
  1. 对齐 ExecutionRecord 接口和 store 类型
  2. 添加缺失字段到 insert 对象
  3. 或调整 store 的 insert 方法类型签名

#### 6. [源代码] [中严重] [30+处 as unknown as 双类型转换]
- **文件/路径**: `division-loader.ts`, `domain-baseline-catalog.ts`, `harness-sdk`
- **问题描述**: 
  - 完全绕过类型安全
  - 通常是类型架构问题的信号
- **建议修复**: 
  1. 修复上游类型问题
  2. 使用更具体的类型断言
  3. 重构类型层次结构

#### 7. [构建] [中严重] [rimraf  extraneous - 未在 package.json 声明]
- **文件/路径**: `node_modules/rimraf`
- **问题描述**: 
  - rimraf@6.1.3 存在于 node_modules 但未在 package.json 声明
  - 可能导致依赖问题
- **建议修复**: 
  1. 添加到 dependencies 或 devDependencies
  2. 或移除并使用其他清理方式

#### 8. [测试] [高严重] [178处直接 process.env 变更无抽象]
- **文件/路径**: `tests/unit/domains/registry/plugin-spi-registry.test.ts:640-687`
- **问题描述**: 
  - 直接读取/写入 process.env
  - 在 beforeEach/afterEach 中手动保存/恢复
  - 无 helper 抽象
- **建议修复**: 
  1. 创建 env helper 函数
  2. 统一保存和恢复模式
  3. 添加测试隔离验证

#### 9. [测试] [高严重] [21,998处 unlinkSync 调用无集中清理]
- **文件/路径**: 多个测试文件
- **问题描述**: 
  - 每个测试重新实现清理逻辑
  - 无集中清理 utility
  - 可能导致 EBUSY (Windows) 或并发访问问题
- **建议修复**: 
  1. 创建集中的测试清理 utility
  2. 使用 afterEach 确保清理
  3. 考虑使用临时目录

#### 10. [测试] [中严重] [无统一 mocking 框架]
- **文件/路径**: `tests/` 多个目录
- **问题描述**: 
  - 10,579 处 vi.fn, jest.mock, sinon 等调用
  - 无统一的 mocking 框架可见
  - 模式不一致
- **建议修复**: 
  1. 建立统一的 mock factory
  2. 标准化 mock 模式
  3. 添加 mocking 最佳实践文档

#### 11. [测试] [中严重] [Singleton 状态重置未标准化]
- **文件/路径**: `tests/unit/platform/five-plane-execution/dispatcher.test.ts`
- **问题描述**: 
  - 测试通过特定函数重置 singletons
  - 每个测试重复自己的 reset 逻辑
  - 无统一的 resetAllSingletons()
- **建议修复**: 
  1. 创建统一的 singleton reset 机制
  2. 在 afterEach 中自动调用
  3. 文档化 singleton 重置要求

#### 12. [测试] [低严重] [无分支覆盖率要求]
- **文件/路径**: `.c8rc.json`
- **问题描述**: 
  - `"100": false` - 无覆盖率要求
  - 只测量源代码，不测量测试文件
- **建议修复**: 
  1. 添加分支覆盖率阈值
  2. 在 CI 中强制执行覆盖率 gate
  3. 关注关键代码路径

#### 13. [网络] [高严重] [ScopedExternalAccessSandbox performHttpRequest 无超时]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/scoped-external-access-sandbox.ts:289`
- **问题描述**: 
  - fetch 调用无 timeout 参数
  - 挂起的请求可能永久阻塞
- **建议修复**: 
  1. 添加 AbortController timeout
  2. 使用标准的超时模式
  3. 记录超时配置

#### 14. [网络] [高严重] [OIDC 服务 fetch 调用无超时]
- **文件/路径**: `src/org-governance/sso-scim/oidc/oidc-service.ts:565,272`
- **问题描述**: 
  - OIDC token 交换无 retry logic
  - 无 timeout/abort signal
  - 认证流程可能挂起
- **建议修复**: 
  1. 添加 fetch timeout
  2. 添加 retry logic
  3. 使用标准的超时配置

#### 15. [网络] [高严重] [plugin-runtime-child 替换 globalThis.fetch 为 no-op]
- **文件/路径**: `src/domains/registry/plugin-runtime-child.ts:112`
- **问题描述**: 
  ```typescript
  globalThis.fetch = (async () => {}) as unknown as typeof globalThis.fetch;
  ```
  - 插件上下文中完全替换 fetch 为空函数
  - 插件的所有出站 HTTP 请求都会失败
  - 看起来是有意为之但可能造成问题
- **建议修复**: 
  1. 确认这是有意行为
  2. 文档化 fetch 替换行为
  3. 确保不会意外影响其他代码

#### 16. [网络] [中严重] [无显式 http.Agent 配置]
- **文件/路径**: 全局
- **问题描述**: 
  - 无显式的 http.Agent/https.Agent 配置
  - 依赖 Node.js 默认连接池
  - 无 maxSockets 或 maxConnections 配置
  - 高吞吐场景可能出问题
- **建议修复**: 
  1. 添加 http Agent 配置
  2. 文档化连接池大小
  3. 监控连接使用情况

#### 17. [序列化] [中严重] [合同版本硬编码为字符串字面量]
- **文件/路径**: `src/platform/contracts/inter-plane-contract-gateway.ts:184`
- **问题描述**: 
  - `schemaVersion: "v4.3"` 硬编码
  - 难以追踪合同版本演变
- **建议修复**: 
  1. 提取到类型化常量或 enum
  2. 集中管理合同版本
  3. 添加版本兼容性检查

#### 18. [序列化] [低严重] [Timestamp = string 丢失精度]
- **文件/路径**: `src/platform/contracts/types/domain/primitives.ts:10`
- **问题描述**: 
  - `type Timestamp = string` 无类型精度
  - ISO 8601 字符串无时区信息
- **建议修复**: 
  1. 使用 Date 对象或 branded string 类型
  2. 明确时区处理约定
  3. 文档化时间戳语义

#### 19. [源代码] [中严重] [29处非空断言 !.]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - 使用 `!.` 断言非空
  - 可能导致运行时错误
- **建议修复**: 
  1. 使用可选链 `?.` 或空值合并 `??`
  2. 添加适当的 null 检查
  3. 减少非空断言使用

#### 20. [API] [中严重] [API 路由 schema 版本硬编码]
- **文件/路径**: `src/platform/five-plane-interface/api/federation-routing-service.ts`
- **问题描述**: 
  - Zod schema 验证存在
  - 但 schema 版本未与 contract 版本同步
- **建议修复**: 
  1. 从 contract 版本派生 schema 版本
  2. 添加版本一致性检查
  3. 文档化版本关系

### 总结

本次补充Review（第十五轮 - 构建测试与类型系统Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. 无 TypeScript 增量编译缓存（每次完整重建9.6秒）
2. 76个 npm scripts 大部分重复构建
3. 168处 any 类型使用（绕过类型安全）
4. 38处 @ts-ignore 指令
5. ExecutionRecord 类型不匹配（14个文件）
6. 178处直接 process.env 变更无抽象
7. ScopedExternalAccessSandbox performHttpRequest 无超时
8. OIDC 服务 fetch 调用无超时
9. plugin-runtime-child 替换 globalThis.fetch 为 no-op

**中优先级**:
1. 30+处 as unknown as 双类型转换
2. rimraf extraneous 未声明
3. 21,998处 unlinkSync 无集中清理
4. 无统一 mocking 框架
5. Singleton 状态重置未标准化
6. 无显式 http.Agent 配置
7. 合同版本硬编码为字符串
8. Timestamp = string 丢失精度
9. 非空断言使用

**低优先级**:
1. 无分支覆盖率要求
2. API 路由 schema 版本未同步

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 72 | 87 | 39 | 198 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **109** | **182** | **85** | **376** |

### 优先修复建议

**立即处理 (本周内)**:
1. 添加 TypeScript 增量编译 (`incremental: true`)
2. 重构 npm scripts 避免重复构建
3. 减少 any 类型使用到 <50
4. 移除所有 @ts-ignore 指令
5. 修复 ExecutionRecord 类型不匹配
6. 添加 fetch 超时到 OIDC 和 sandbox

**短期内处理 (本月内)**:
1. 创建集中的测试清理 utility
2. 建立统一的 mocking 框架
3. 标准化 singleton 重置机制
4. 添加 http.Agent 连接池配置
5. 提取合同版本到类型化常量

**长期规划**:
1. 实现完整的类型安全（无 any）
2. 建立测试基础设施标准
3. 实现构建缓存优化
4. 完善网络层超时和重试
5. 建立覆盖率门禁标准

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十一轮 - 并发安全与可观测性Review）

### 发现的新问题

#### 1. [源代码] [严重] [FencingTokenService 静态可变状态无锁保护]
- **文件/路径**: `src/platform/five-plane-state-evidence/events/cas/fencing-token-service.ts:71-73`
- **问题描述**: 
  ```typescript
  private static readonly activeFences = new Map<string, FenceInfo>();
  private static globalTokenCounter = 0;
  ```
  - `acquireFence` 方法执行非原子的 read-then-write 模式
  - 两个并发调用可能同时通过检查，导致状态不一致
  - 静态 Map 在无锁情况下被并发读写
- **建议修复**: 
  1. 添加互斥锁保护 activeFences 访问
  2. 使用原子操作或 Compare-And-Swap
  3. 考虑使用数据库行锁替代内存锁

#### 2. [源代码] [严重] [AsyncFencingTokenService.globalTokenCounter 不安全递增]
- **文件/路径**: `src/platform/five-plane-state-evidence/events/cas/postgres-fencing-token-service.ts:13-17`
- **问题描述**: 
  ```typescript
  private static readonly globalTokenCounter = {
    value: 0,
    getAndIncrement(): number { return ++this.value; }
  };
  ```
  - 经典的 read-modify-write 竞态条件
  - 多线程可能读到相同的值
- **建议修复**: 
  1. 使用 `pg_advisory_xact_lock` 保护
  2. 或使用数据库自增序列
  3. 移除静态计数器，使用数据库生成

#### 3. [源代码] [严重] [BudgetAllocator.activeReservations 无保护 Map]
- **文件/路径**: `src/platform/five-plane-execution/budget-allocator.ts:394,426,458,467,569,657,711`
- **问题描述**: 
  ```typescript
  this.activeReservations.set(result.reservation.budgetReservationId, result.reservation);
  this.activeReservations.delete(reservationId);
  ```
  - 并发代码路径修改 activeReservations Map
  - 无锁保护
- **建议修复**: 
  1. 添加 Map 级别的读写锁
  2. 使用 ConcurrentMap 或类似结构
  3. 重构为无状态设计

#### 4. [源代码] [高严重] [SqliteLockAdapter.fencingCounter 不同步]
- **文件/路径**: `src/platform/five-plane-execution/distributed-lock/sqlite-lock-adapter.ts:9,14,48,74,88`
- **问题描述**: 
  - `fencingCounter` 在 acquire/extend/forceSteal 中递增
  - 无同步机制
- **建议修复**: 
  1. 使用 SQLite 序列替代内存计数器
  2. 或添加适当的同步机制

#### 5. [源代码] [高严重] [ServiceRegistry.getInstance() 检查-then-创建 竞态]
- **文件/路径**: `src/platform/shared/lifecycle/service-registry.ts:85-88`
- **问题描述**: 
  ```typescript
  public static getInstance(): ServiceRegistry {
    if (!ServiceRegistry._instance) {
      ServiceRegistry._instance = new ServiceRegistry();
    }
    return ServiceRegistry._instance;
  }
  ```
  - 两个线程可能同时看到 _instance 为 null
  - 创建两个实例
- **建议修复**: 
  1. 使用 double-checked locking
  2. 或使用 ES2022 静态字段初始化
  3. `private static readonly _instance = new ServiceRegistry();`

#### 6. [源代码] [中严重] [StructuredLogger.rotationStateByPath 异步竞态]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts:142-144,438-453`
- **问题描述**: 
  ```typescript
  private static rotationStateByPath = new Map<string, {...}>();
  ```
  - `writeToGlobalFileSink` 中存在 TOCTOU 竞态
  - 读取 pendingBytes 和实际写入之间可能被其他调用修改
- **建议修复**: 
  1. 使用锁保护 rotationStateByPath
  2. 或使用单线程写入队列

#### 7. [源代码] [中严重] [EffectBuffer.scopes 并发修改风险]
- **文件/路径**: `src/platform/five-plane-execution/execution-engine/effect-buffer.ts:446,452,476,485,492,502-514`
- **问题描述**: 
  - `scopes.delete(scopeId)` 与 `for (const [scopeId, scope] of this.scopes)` 并发
  - flush() 和 createScope 可能同时修改
- **建议修复**: 
  1. 添加迭代锁
  2. 使用 CopyOnWrite 模式
  3. 在修改时复制一份 scopes

#### 8. [源代码] [中严重] [OIDC _skipSignatureVerification 标志存在风险]
- **文件/路径**: `src/platform/five-plane-interface/api/oidc-oauth-service.ts:92,108-111`
- **问题描述**: 
  - `_skipSignatureVerification` 标志存在于生产代码路径
  - 虽然标记为仅测试使用，但配置错误可能导致安全问题
- **建议修复**: 
  1. 移除此标志或确保编译时完全删除
  2. 添加生产环境检查抛出错误
  3. 在代码审查中标记为禁止模式

#### 9. [源代码] [中严重] [Browser Executor 检测但不消毒 innerHTML]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/browser-executor.ts:541`
- **问题描述**: 
  - 检测到 innerHTML 但只返回假数据
  - 不进行实际消毒
  - 用户可能绕过保护
- **建议修复**: 
  1. 使用 DOMPurify 或类似库消毒
  2. 或完全禁止 innerHTML
  3. 返回错误而非假数据

#### 10. [数据库] [高严重] [runtime-physical-schema 无外键约束]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/runtime-physical-schema.ts`
- **问题描述**: 
  - `task_draft_id`, `confirmed_task_spec_id`, `harness_run_id` 等列无 FK 约束
  - 可能导致孤儿记录
  - 删除父记录时子记录变为孤立
- **建议修复**: 
  1. 添加外键约束
  2. 实现软删除而非硬删除
  3. 添加数据库迁移脚本

#### 11. [数据库] [中严重] [大部分表缺少软删除和审计字段]
- **文件/路径**: 多个表（除 mission_records 外）
- **问题描述**: 
  - 只有 mission_records 有完整审计：created_at, created_by, updated_at, updated_by, archived_at, archived_by
  - 大部分表缺少 deleted_at, is_deleted 软删除字段
  - created_by/updated_by 仅在 mission_records 系列表中存在
- **建议修复**: 
  1. 为所有核心表添加审计字段
  2. 实现软删除模式
  3. 添加规范说明哪些表必须有审计

#### 12. [CI/CD] [高严重] [UI quality workflow 无覆盖率门禁]
- **文件/路径**: `.github/workflows/ui-quality.yml:33`
- **问题描述**: 
  - 运行 `test:coverage` 但无阈值执行
  - 缺少 `coverage:gate`
  - 与主 CI 不一致
- **建议修复**: 
  1. 添加 `npm run coverage:gate` 到 UI workflow
  2. 或确保 UI 更改时运行主 CI

#### 13. [CI/CD] [高严重] [部署工作流无真正的手动审批]
- **文件/路径**: `.github/workflows/deploy-environment.yml`
- **问题描述**: 
  - `workflow_dispatch` 有环境保护但无显式审批步骤
  - deploy.sh 的生产确认使用 `read -p` 在 CI/CD 中会挂起
  - 生产部署缺少真正的审批控制
- **建议修复**: 
  1. 使用 GitHub Environment `required_reviewers`
  2. 修改 deploy.sh 检测 CI 环境跳过交互
  3. 添加显式审批步骤

#### 14. [CI/CD] [中严重] [回滚机制边界情况未处理]
- **文件/路径**: `.github/workflows/deploy-environment.yml:230-278`, `deploy/scripts/rollback.sh`
- **问题描述**: 
  - 部署成功但 health check 失败时不会触发回滚
  - rollback.sh 只支持 dev|staging|prod（deploy.sh 支持 dev|test|staging|pre-prod|prod）
  - pre-prod 命名空间不一致：automatic-agent-preprod vs automatic-agent-pre-prod
- **建议修复**: 
  1. 添加 health check 失败时的回滚触发
  2. 统一 rollback.sh 支持所有环境
  3. 修正 pre-prod 命名空间

#### 15. [CI/CD] [中严重] [Canary/Blue-Green 升级无健康检查]
- **文件/路径**: `.github/workflows/deploy-environment.yml:188-199`
- **问题描述**: 
  - 初始部署有 health checks
  - 但 promotion 步骤无健康验证
  - 可能推送不健康的版本
- **建议修复**: 
  1. 在 promotion 步骤后添加健康检查
  2. 验证推送的版本通过健康检查
  3. 失败时回滚

#### 16. [可观测性] [高严重] [58处 console.* 使用应使用 StructuredLogger]
- **文件/路径**: 多个文件
- **问题描述**: 
  - `src/index.ts`: 4次
  - `src/platform/ops-maturity/platform-panic/panic-propagation-service.ts`: 2 console.error + 1 console.log
  - `src/platform/five-plane-orchestration/agent-delegation/delegation-manager.service.ts`: 1 console.error
  - `src/platform/five-plane-orchestration/harness/runtime/runtime-entry-guard.ts`: 3 console.warn
  - `src/ops-maturity/chaos/chaos-experiment-scheduler.ts`: 5 console.log + 4 console.warn
  - CLI 工具中多处
- **建议修复**: 
  1. 替换 console.* 为 StructuredLogger
  2. 保留 CLI 输出的 console 用法（可接受）
  3. 添加 ESLint 规则检测

#### 17. [可观测性] [高严重] [29+执行引擎文件缺少结构化日志]
- **文件/路径**: `src/platform/five-plane-execution/execution-engine/`
- **问题描述**: 
  - `runtime-state-machine.ts`: 有 try/catch 但无 StructuredLogger
  - `multi-step-orchestration.ts`: 导入 HealthService 但无 StructuredLogger
  - `agent-executor.ts`: 169个 try 块，0 StructuredLogger 调用
  - 关键执行路径缺少可观测性
- **建议修复**: 
  1. 为所有执行引擎文件添加 StructuredLogger
  2. 记录关键操作路径
  3. 确保错误路径有日志

#### 18. [可观测性] [高严重] [执行引擎缺少 LLM 调用指标]
- **文件/路径**: `src/platform/five-plane-execution/execution-engine/`
- **问题描述**: 
  - RuntimeMetricsRegistry 支持 `recordLlmLatency`, `recordHarnessRunDuration` 等
  - 但执行引擎中无调用
  - model-gateway 有指标但执行引擎无
- **建议修复**: 
  1. 在 agent-executor 中记录 LLM 调用指标
  2. 在 multi-step-orchestration 中记录 harness 指标
  3. 启用完整指标覆盖

#### 19. [可观测性] [中严重] [日志方法使用不一致]
- **文件/路径**: 多个服务
- **问题描述**: 
  - 有的使用 `.error()`, `.warn()` 辅助方法
  - 有的使用 `logger.log({ level: "error", ... })`
  - 无统一规范
- **建议修复**: 
  1. 建立日志方法规范
  2. 添加 ESLint 规则统一
  3. 重构为一致的 helper 方法调用

#### 20. [安全] [中严重] [输入验证有限 - 仅 JWT 强类型]
- **文件/路径**: 多个 API 端点
- **问题描述**: 
  - JWT claims 有强类型验证
  - 其他输入验证有限
  - 缺少通用输入消毒
- **建议修复**: 
  1. 添加通用输入验证中间件
  2. 使用 zod 或类似库验证请求体
  3. 为所有 API 端点添加验证

#### 21. [源代码] [低严重] [HA Coordinator Leadership 获取可能非原子]
- **文件/路径**: `src/platform/five-plane-execution/ha/ha-coordinator-service-inner.ts:70-78`
- **问题描述**: 
  - `acquireLeadership` 可能执行非原子的 read-modify-write
  - leadership epoch 更新可能存在竞态
- **建议修复**: 
  1. 使用数据库行锁保护
  2. 确保领导者选举原子性
  3. 添加领导者租约续约机制

### 总结

本次补充Review（第十一轮 - 并发安全与可观测性Review - 2026-05-14）发现了21个新问题，重点关注并发安全、可观测性和CI/CD。

**高优先级 (需要立即处理)**:
1. 7个并发竞态问题（FencingTokenService, BudgetAllocator, ServiceRegistry等）
2. runtime-physical-schema 无外键约束
3. UI quality workflow 无覆盖率门禁
4. 58处 console.* 使用应替换为 StructuredLogger
5. 29+ 执行引擎文件缺少结构化日志
6. 执行引擎缺少 LLM 调用指标

**中优先级**:
1. OIDC _skipSignatureVerification 标志风险
2. Browser Executor innerHTML 不消毒
3. 大部分表缺少软删除和审计字段
4. 部署工作流无真正手动审批
5. 回滚机制边界情况未处理
6. Canary/Blue-Green 升级无健康检查
7. 日志方法使用不一致
8. 输入验证有限

**低优先级**:
1. HA Coordinator Leadership 获取可能非原子
2. SqliteLockAdapter fencingCounter 不同步

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 58 | 62 | 29 | 149 |
| 测试 | 8 | 14 | 5 | 27 |
| 配置 | 8 | 31 | 19 | 58 |
| 安全 | 7 | 8 | 1 | 16 |
| 文档 | 2 | 7 | 3 | 12 |
| UI | 0 | 3 | 3 | 6 |
| 部署 | 1 | 10 | 6 | 17 |
| **合计** | **84** | **135** | **66** | **285** |

### 优先修复建议

**立即处理 (本周内)**:
1. 修复所有7个并发竞态问题
2. 添加 runtime-physical-schema 外键约束
3. 为 UI quality workflow 添加覆盖率门禁
4. 替换关键路径的 console.* 为 StructuredLogger
5. 在执行引擎中添加 LLM 和 harness 指标

**短期内处理 (本月内)**:
1. 为所有执行引擎文件添加结构化日志
2. 添加数据库审计字段和软删除
3. 修正部署工作流审批机制
4. 统一日志方法使用规范
5. 添加 Canary/Blue-Green 健康检查

**长期规划**:
1. 建立并发安全编码规范
2. 实现完整的可观测性标准
3. 完善数据库约束和审计机制
4. 增强 CI/CD 质量和安全门禁
5. 建立代码审查清单

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十五轮 - 构建测试与类型系统Review）

### 发现的新问题

#### 1. [构建] [高严重] [无 TypeScript 增量编译缓存]
- **文件/路径**: `tsconfig.json`
- **问题描述**: 
  - 无 `incremental: true` 配置
  - 无 `tsbuildinfo` 文件
  - 每次构建是完整重建（9.6秒）
  - TypeScript 无法使用增量编译
- **建议修复**: 
  1. 在 tsconfig.json 添加 `"incremental": true`
  2. 添加 `"tsbuildinfo": ".tsbuildinfo"` 到 .gitignore
  3. 实现项目引用以支持并行构建

#### 2. [构建] [高严重] [76个 npm scripts 大部分重复构建]
- **文件/路径**: `package.json`
- **问题描述**: 
  - 40+ CLI scripts 都运行 `npm run build && node ...`
  - 每个脚本前都执行完整重建
  - 无法直接运行预构建的 CLI
  - 20+ stable:* 脚本遵循相同模式
- **建议修复**: 
  1. 创建 `build:cli` 脚本构建一次
  2. 让 CLI 脚本使用预构建的 dist 文件
  3. 添加 npm run dev 或类似的无需重建的执行方式

#### 3. [源代码] [高严重] [168处 any 类型使用]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - `stable-runner-factory.ts`: `opts: any` -> `any`
  - `billing-service-async.ts`: `input: any` 方法签名
  - 重试/超时工具使用 `(...args: any[]) => Promise<any>`
  - 绕过类型安全
- **建议修复**: 
  1. 使用泛型替代 any
  2. 替换为具体的输入类型
  3. 添加 ESLint 规则禁止 any

#### 4. [源代码] [高严重] [38处 @ts-ignore 指令]
- **文件/路径**: 多个文件
- **问题描述**: 
  - 14个 stability 相关文件有 `@ts-ignore ExecutionRecord type mismatch`
  - 5个 harness-sdk 文件有 `Partial<HarnessRun> doesn't have all required properties`
  - 2个文件有 `exactOptionalPropertyTypes` 问题
- **建议修复**: 
  1. 修复 ExecutionRecord 类型不匹配
  2. 完成 HarnessRun 类型的必需属性
  3. 移除 @ts-ignore 使用

#### 5. [源代码] [高严重] [ExecutionRecord 类型不匹配]
- **文件/路径**: `src/platform/stability/stable-evidence-bundle-support.ts:783` 等14个文件
- **问题描述**: 
  - 插入 execution record 时 TypeScript 拒绝类型
  - `ExecutionRecord` 接口要求新字段但 insert 对象缺失
  - 或 store 的 insert 方法接受不同类型
- **建议修复**: 
  1. 对齐 ExecutionRecord 接口和 store 类型
  2. 添加缺失字段到 insert 对象
  3. 或调整 store 的 insert 方法类型签名

#### 6. [源代码] [中严重] [30+处 as unknown as 双类型转换]
- **文件/路径**: `division-loader.ts`, `domain-baseline-catalog.ts`, `harness-sdk`
- **问题描述**: 
  - 完全绕过类型安全
  - 通常是类型架构问题的信号
- **建议修复**: 
  1. 修复上游类型问题
  2. 使用更具体的类型断言
  3. 重构类型层次结构

#### 7. [构建] [中严重] [rimraf  extraneous - 未在 package.json 声明]
- **文件/路径**: `node_modules/rimraf`
- **问题描述**: 
  - rimraf@6.1.3 存在于 node_modules 但未在 package.json 声明
  - 可能导致依赖问题
- **建议修复**: 
  1. 添加到 dependencies 或 devDependencies
  2. 或移除并使用其他清理方式

#### 8. [测试] [高严重] [178处直接 process.env 变更无抽象]
- **文件/路径**: `tests/unit/domains/registry/plugin-spi-registry.test.ts:640-687`
- **问题描述**: 
  - 直接读取/写入 process.env
  - 在 beforeEach/afterEach 中手动保存/恢复
  - 无 helper 抽象
- **建议修复**: 
  1. 创建 env helper 函数
  2. 统一保存和恢复模式
  3. 添加测试隔离验证

#### 9. [测试] [高严重] [21,998处 unlinkSync 调用无集中清理]
- **文件/路径**: 多个测试文件
- **问题描述**: 
  - 每个测试重新实现清理逻辑
  - 无集中清理 utility
  - 可能导致 EBUSY (Windows) 或并发访问问题
- **建议修复**: 
  1. 创建集中的测试清理 utility
  2. 使用 afterEach 确保清理
  3. 考虑使用临时目录

#### 10. [测试] [中严重] [无统一 mocking 框架]
- **文件/路径**: `tests/` 多个目录
- **问题描述**: 
  - 10,579 处 vi.fn, jest.mock, sinon 等调用
  - 无统一的 mocking 框架可见
  - 模式不一致
- **建议修复**: 
  1. 建立统一的 mock factory
  2. 标准化 mock 模式
  3. 添加 mocking 最佳实践文档

#### 11. [测试] [中严重] [Singleton 状态重置未标准化]
- **文件/路径**: `tests/unit/platform/five-plane-execution/dispatcher.test.ts`
- **问题描述**: 
  - 测试通过特定函数重置 singletons
  - 每个测试重复自己的 reset 逻辑
  - 无统一的 resetAllSingletons()
- **建议修复**: 
  1. 创建统一的 singleton reset 机制
  2. 在 afterEach 中自动调用
  3. 文档化 singleton 重置要求

#### 12. [测试] [低严重] [无分支覆盖率要求]
- **文件/路径**: `.c8rc.json`
- **问题描述**: 
  - `"100": false` - 无覆盖率要求
  - 只测量源代码，不测量测试文件
- **建议修复**: 
  1. 添加分支覆盖率阈值
  2. 在 CI 中强制执行覆盖率 gate
  3. 关注关键代码路径

#### 13. [网络] [高严重] [ScopedExternalAccessSandbox performHttpRequest 无超时]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/scoped-external-access-sandbox.ts:289`
- **问题描述**: 
  - fetch 调用无 timeout 参数
  - 挂起的请求可能永久阻塞
- **建议修复**: 
  1. 添加 AbortController timeout
  2. 使用标准的超时模式
  3. 记录超时配置

#### 14. [网络] [高严重] [OIDC 服务 fetch 调用无超时]
- **文件/路径**: `src/org-governance/sso-scim/oidc/oidc-service.ts:565,272`
- **问题描述**: 
  - OIDC token 交换无 retry logic
  - 无 timeout/abort signal
  - 认证流程可能挂起
- **建议修复**: 
  1. 添加 fetch timeout
  2. 添加 retry logic
  3. 使用标准的超时配置

#### 15. [网络] [高严重] [plugin-runtime-child 替换 globalThis.fetch 为 no-op]
- **文件/路径**: `src/domains/registry/plugin-runtime-child.ts:112`
- **问题描述**: 
  ```typescript
  globalThis.fetch = (async () => {}) as unknown as typeof globalThis.fetch;
  ```
  - 插件上下文中完全替换 fetch 为空函数
  - 插件的所有出站 HTTP 请求都会失败
  - 看起来是有意为之但可能造成问题
- **建议修复**: 
  1. 确认这是有意行为
  2. 文档化 fetch 替换行为
  3. 确保不会意外影响其他代码

#### 16. [网络] [中严重] [无显式 http.Agent 配置]
- **文件/路径**: 全局
- **问题描述**: 
  - 无显式的 http.Agent/https.Agent 配置
  - 依赖 Node.js 默认连接池
  - 无 maxSockets 或 maxConnections 配置
  - 高吞吐场景可能出问题
- **建议修复**: 
  1. 添加 http Agent 配置
  2. 文档化连接池大小
  3. 监控连接使用情况

#### 17. [序列化] [中严重] [合同版本硬编码为字符串字面量]
- **文件/路径**: `src/platform/contracts/inter-plane-contract-gateway.ts:184`
- **问题描述**: 
  - `schemaVersion: "v4.3"` 硬编码
  - 难以追踪合同版本演变
- **建议修复**: 
  1. 提取到类型化常量或 enum
  2. 集中管理合同版本
  3. 添加版本兼容性检查

#### 18. [序列化] [低严重] [Timestamp = string 丢失精度]
- **文件/路径**: `src/platform/contracts/types/domain/primitives.ts:10`
- **问题描述**: 
  - `type Timestamp = string` 无类型精度
  - ISO 8601 字符串无时区信息
- **建议修复**: 
  1. 使用 Date 对象或 branded string 类型
  2. 明确时区处理约定
  3. 文档化时间戳语义

#### 19. [源代码] [中严重] [29处非空断言 !.]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - 使用 `!.` 断言非空
  - 可能导致运行时错误
- **建议修复**: 
  1. 使用可选链 `?.` 或空值合并 `??`
  2. 添加适当的 null 检查
  3. 减少非空断言使用

#### 20. [API] [中严重] [API 路由 schema 版本硬编码]
- **文件/路径**: `src/platform/five-plane-interface/api/federation-routing-service.ts`
- **问题描述**: 
  - Zod schema 验证存在
  - 但 schema 版本未与 contract 版本同步
- **建议修复**: 
  1. 从 contract 版本派生 schema 版本
  2. 添加版本一致性检查
  3. 文档化版本关系

### 总结

本次补充Review（第十五轮 - 构建测试与类型系统Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. 无 TypeScript 增量编译缓存（每次完整重建9.6秒）
2. 76个 npm scripts 大部分重复构建
3. 168处 any 类型使用（绕过类型安全）
4. 38处 @ts-ignore 指令
5. ExecutionRecord 类型不匹配（14个文件）
6. 178处直接 process.env 变更无抽象
7. ScopedExternalAccessSandbox performHttpRequest 无超时
8. OIDC 服务 fetch 调用无超时
9. plugin-runtime-child 替换 globalThis.fetch 为 no-op

**中优先级**:
1. 30+处 as unknown as 双类型转换
2. rimraf extraneous 未声明
3. 21,998处 unlinkSync 无集中清理
4. 无统一 mocking 框架
5. Singleton 状态重置未标准化
6. 无显式 http.Agent 配置
7. 合同版本硬编码为字符串
8. Timestamp = string 丢失精度
9. 非空断言使用

**低优先级**:
1. 无分支覆盖率要求
2. API 路由 schema 版本未同步

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 72 | 87 | 39 | 198 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **109** | **182** | **85** | **376** |

### 优先修复建议

**立即处理 (本周内)**:
1. 添加 TypeScript 增量编译 (`incremental: true`)
2. 重构 npm scripts 避免重复构建
3. 减少 any 类型使用到 <50
4. 移除所有 @ts-ignore 指令
5. 修复 ExecutionRecord 类型不匹配
6. 添加 fetch 超时到 OIDC 和 sandbox

**短期内处理 (本月内)**:
1. 创建集中的测试清理 utility
2. 建立统一的 mocking 框架
3. 标准化 singleton 重置机制
4. 添加 http.Agent 连接池配置
5. 提取合同版本到类型化常量

**长期规划**:
1. 实现完整的类型安全（无 any）
2. 建立测试基础设施标准
3. 实现构建缓存优化
4. 完善网络层超时和重试
5. 建立覆盖率门禁标准

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十四轮 - 日志事件与工作流Review）

### 发现的新问题

#### 1. [可观测性] [高严重] [StructuredLogEntry 无 requestId 字段]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - StructuredLogEntry 接口无 requestId 字段
  - 只有 correlationId，它回退到 traceId
  - 无法独立追踪单个 HTTP 请求
- **建议修复**: 
  1. 在 StructuredLogEntry 添加 requestId 字段
  2. 在 HTTP 中间件中生成和注入 requestId
  3. 确保 requestId 流经所有日志调用

#### 2. [可观测性] [高严重] [StructuredLogger data 字段无自动清洗]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - 如果开发者传递 `{ password: "..." }` 给 logger.info，密码会被记录
  - 无字段级脱敏机制
  - 依赖开发者手动排除敏感字段
- **建议修复**: 
  1. 添加 OBSERVE_OUTPUT_BLACKLIST 类似的黑名单
  2. 实现 redact/mask/sanitize 工具函数
  3. 在日志传输前自动清洗敏感字段

#### 3. [可观测性] [中严重] [日志中敏感数据无 PII 处理]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - 无 PII 重写工具
  - 开发者必须手动避免记录敏感数据
  - 存在意外记录敏感信息的风险
- **建议修复**: 
  1. 添加 PII 检测和重写工具
  2. 在文档中明确记录敏感字段列表
  3. 添加测试验证敏感数据不被记录

#### 4. [可观测性] [中严重] [59处 console.* 直接调用]
- **文件/路径**: 多个源文件
- **问题描述**: 
  - 应该使用 StructuredLogger
  - console 调用可能不被日志聚合系统收集
  - 难以追踪和关联
- **建议修复**: 
  1. 替换所有 console.* 为 StructuredLogger
  2. 添加 ESLint 规则禁止 console.*
  3. 保留少量允许的 CLI 输出

#### 5. [可观测性] [中严重] [部分 logger.warn 调用缺少结构化数据]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:122`
- **问题描述**: 
  ```typescript
  logger.warn("WebSocket connection rejected: missing subprotocol token")
  ```
  - 只有消息，无 data 对象
  - 难以关联和查询
- **建议修复**: 
  1. 添加 `{ data: { ... } }` 对象
  2. 确保所有日志包含上下文数据
  3. 添加日志审查工具

#### 6. [源代码] [高严重] [Harness while(true) 循环无硬性迭代上限]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:1442`
- **问题描述**: 
  ```typescript
  while (true) {
    // ...
    // 依赖 budget gate 和 guardrail vibration 退出
    // 如果两者都失败，循环永久继续
  }
  ```
  - 依赖 budget gate 和 guardrail vibration 退出
  - 无硬性迭代次数限制
  - 可能导致无限循环
- **建议修复**: 
  1. 添加硬性 maxIterations 上限
  2. 在达到上限时强制退出
  3. 记录详细的迭代统计用于调试

#### 7. [源代码] [高严重] [Oapeflir while(true) 循环可能无限重规划]
- **文件/路径**: `src/platform/five-plane-orchestration/oapeflir/oapeflir-loop-service.ts:355`
- **问题描述**: 
  - 如果 loopReplanDecision.shouldReplan 永不变为 false
  - 重规划永远无法减少计划错误
  - 可能导致无限循环
- **建议修复**: 
  1. 添加重规划次数上限
  2. 在达到上限时强制接受当前结果
  3. 添加重规划质量衰减检测

#### 8. [源代码] [中严重] [Recovery 流程允许终态转换到 paused]
- **文件/路径**: `src/platform/five-plane-execution/runtime-state-machine.ts:105-108`
- **问题描述**: 
  - completed, failed, cancelled, aborted 可转换到 paused 进行恢复
  - 创建恢复路径可能重新进入执行
  - 需要验证此行为的正确性
- **建议修复**: 
  1. 审查 recovery 转换的合法性
  2. 确保不会导致状态不一致
  3. 添加转换前置条件验证

#### 9. [国际化] [高严重] [API 错误消息硬编码英文]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/api-error.ts`
- **问题描述**: 
  ```typescript
  return new ApiError(404, "api.task_not_found", "Task not found.");
  ```
  - 错误消息硬编码英文
  - 无后端 i18n 机制
  - 非英文客户端收到不本地化的错误
- **建议修复**: 
  1. 使用错误码而非硬编码消息
  2. 客户端根据错误码本地化
  3. 或在后端添加 i18n 支持

#### 10. [国际化] [中严重] [UI 硬编码字符串未翻译]
- **文件/路径**: `ui/packages/features/conversation/src/web/index.tsx`
- **问题描述**: 
  - "Build Plan", "Execute", "Messages" 等按钮标签硬编码
  - 不使用 i18n 系统
- **建议修复**: 
  1. 使用 `translateFeatureCopy` 替换硬编码
  2. 确保所有用户可见文本通过 i18n
  3. 添加硬编码字符串检测

#### 11. [国际化] [中严重] [Arabic 目录不完整]
- **文件/路径**: `ui/packages/shared/i18n/src/catalogs/ar-SA.ts`
- **问题描述**: 
  - ar-SA 只有约18条消息
  - en-US 和 zh-CN 有约117条
  - 缺失的 key 返回 key 本身
- **建议修复**: 
  1. 补充 Arabic 翻译
  2. 确保所有 key 都有翻译
  3. 添加翻译完整性测试

#### 12. [国际化] [低严重] [translateFeatureCopy 无回退默认值]
- **文件/路径**: `ui/packages/shared/i18n/`
- **问题描述**: 
  - 如果 feature ID 不在 catalog 中
  - 返回 undefined title/summary
  - 无合理回退
- **建议修复**: 
  1. 添加回退到 key 或默认字符串
  2. 或使用 feature ID 作为显示名
  3. 确保 UI 不显示 undefined

#### 13. [运维] [高严重] [健康检查无 readiness/liveness 区分]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts`
- **问题描述**: 
  - /healthz, /v1/healthz, /health 返回相同报告
  - 无 readiness probe（可处理流量？）
  - 无 liveness probe（系统存活？）
  - canAcceptTraffic() 未通过 HTTP 暴露
- **建议修复**: 
  1. 添加 /ready 和 /live 端点
  2. Readiness 检查依赖项就绪
  3. Liveness 检查进程健康

#### 14. [运维] [高严重] [shutdown 期间健康检查存在竞态]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts`
- **问题描述**: 
  - getHealthReportAsync() 异步检查 dbWritable
  - shutdown 期间数据库连接可能关闭
  - 健康检查仍在传入
- **建议修复**: 
  1. 在 shutdown 时返回 unhealthy
  2. 添加 shutdown 状态标志
  3. 停止接受新的健康检查请求

#### 15. [运维] [高严重] [LeaderElectionService 未与 GracefulShutdown 集成]
- **文件/路径**: `src/platform/five-plane-execution/ha/leader-election-service.ts`
- **问题描述**: 
  - LeaderElectionService.stop() 处理优雅退位
  - 但未在全局 graceful shutdown 中注册
  - 需显式注册为 shutdown handler
- **建议修复**: 
  1. 将 LeaderElectionService 添加到 shutdown handlers
  2. 确保领导者在 shutdown 时让位
  3. 验证关闭顺序正确

#### 16. [运维] [中严重] [addHandler() 无显式排序约定]
- **文件/路径**: `src/platform/five-plane-execution/startup/graceful-shutdown.ts`
- **问题描述**: 
  - 虽然 handlers 逆序执行
  - 但无显式接口声明 handler 依赖关系
  - 关键 handlers（如关闭数据库连接）应最后运行
- **建议修复**: 
  1. 添加优先级或阶段参数
  2. 文档化关键 handler 顺序要求
  3. 添加顺序验证测试

#### 17. [运维] [中严重] [unref'd timers 可能不触发]
- **文件/路径**: `src/platform/five-plane-execution/startup/process-error-handlers.ts:106`
- **问题描述**: 
  - `setTimeout(...).unref()` 不保持事件循环活跃
  - 如果主线程空闲，强制退出可能不触发
- **建议修复**: 
  1. 移除 .unref() 除非确实需要
  2. 或确保有其他方式保持事件循环
  3. 添加超时触发验证测试

#### 18. [运维] [中严重] [StartupConsistencyChecker 阻塞流量但不暴露状态]
- **文件/路径**: `src/platform/five-plane-startup-plan.ts`
- **问题描述**: 
  - canAcceptTraffic() 在 P0 问题存在时返回 false
  - 但 _trafficBlocked 是内部状态
  - 负载均衡器访问 /healthz 仍会得到 "ok"
- **建议修复**: 
  1. 在健康报告中暴露 trafficBlocked 状态
  2. 或添加单独的健康端点指示就绪状态
  3. 确保负载均衡器在阻塞时不路由流量

#### 19. [事件系统] [中严重] [TypedEventBus vs EventEmitter 混用]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - 项目同时使用 TypedEventBus 和原生 EventEmitter
  - TypedEventBus 应该是标准
  - 混用可能导致类型安全问题
- **建议修复**: 
  1. 创建迁移计划统一使用 TypedEventBus
  2. 添加 ESLint 规则禁止原生 EventEmitter
  3. 在 CI 中检测混用情况

#### 20. [源代码] [低严重] [无事件排序保证]
- **文件/路径**: `src/platform/five-plane-state-evidence/events/`
- **问题描述**: 
  - 无明确的事件排序保证文档
  - 事件处理可能乱序
  - 依赖方需要处理乱序
- **建议修复**: 
  1. 文档化事件排序语义
  2. 如果需要排序，实现序列号机制
  3. 在事件处理中考虑乱序情况

### 总结

本次补充Review（第十四轮 - 日志事件与工作流Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. StructuredLogEntry 无 requestId 字段
2. StructuredLogger data 字段无自动清洗（敏感数据风险）
3. Harness while(true) 循环无硬性迭代上限
4. Oapeflir while(true) 循环可能无限重规划
5. API 错误消息硬编码英文（无 i18n）
6. 健康检查无 readiness/liveness 区分
7. shutdown 期间健康检查存在竞态
8. LeaderElectionService 未与 GracefulShutdown 集成

**中优先级**:
1. 日志中敏感数据无 PII 处理
2. 59处 console.* 直接调用
3. 部分 logger.warn 调用缺少结构化数据
4. Recovery 流程允许终态转换到 paused
5. UI 硬编码字符串未翻译
6. Arabic 目录不完整
7. addHandler() 无显式排序约定
8. unref'd timers 可能不触发
9. StartupConsistencyChecker 阻塞流量但不暴露状态
10. TypedEventBus vs EventEmitter 混用

**低优先级**:
1. translateFeatureCopy 无回退默认值
2. 无事件排序保证

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 68 | 80 | 37 | 185 |
| 测试 | 8 | 14 | 5 | 27 |
| 配置 | 10 | 38 | 24 | 72 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **103** | **171** | **82** | **356** |

### 优先修复建议

**立即处理 (本周内)**:
1. 在 StructuredLogEntry 添加 requestId 字段
2. 实现日志敏感字段自动清洗
3. 为工作流循环添加硬性迭代上限
4. 添加 readiness/liveness 健康检查端点
5. 将 LeaderElectionService 集成到 GracefulShutdown
6. 实现 API 错误消息 i18n 支持

**短期内处理 (本月内)**:
1. 替换所有 console.* 为 StructuredLogger
2. 补充 Arabic i18n 目录
3. 修复 UI 硬编码字符串
4. 统一 TypedEventBus 替代 EventEmitter
5. 解决 shutdown 竞态条件
6. 添加事件排序保证文档

**长期规划**:
1. 建立完整的 PII 处理框架
2. 实现日志完整性验证
3. 建立工作流安全循环检测
4. 完善健康检查和就绪探测
5. 建立国际化完整测试

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十五轮 - 构建测试与类型系统Review）

### 发现的新问题

#### 1. [构建] [高严重] [无 TypeScript 增量编译缓存]
- **文件/路径**: `tsconfig.json`
- **问题描述**: 
  - 无 `incremental: true` 配置
  - 无 `tsbuildinfo` 文件
  - 每次构建是完整重建（9.6秒）
  - TypeScript 无法使用增量编译
- **建议修复**: 
  1. 在 tsconfig.json 添加 `"incremental": true`
  2. 添加 `"tsbuildinfo": ".tsbuildinfo"` 到 .gitignore
  3. 实现项目引用以支持并行构建

#### 2. [构建] [高严重] [76个 npm scripts 大部分重复构建]
- **文件/路径**: `package.json`
- **问题描述**: 
  - 40+ CLI scripts 都运行 `npm run build && node ...`
  - 每个脚本前都执行完整重建
  - 无法直接运行预构建的 CLI
  - 20+ stable:* 脚本遵循相同模式
- **建议修复**: 
  1. 创建 `build:cli` 脚本构建一次
  2. 让 CLI 脚本使用预构建的 dist 文件
  3. 添加 npm run dev 或类似的无需重建的执行方式

#### 3. [源代码] [高严重] [168处 any 类型使用]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - `stable-runner-factory.ts`: `opts: any` -> `any`
  - `billing-service-async.ts`: `input: any` 方法签名
  - 重试/超时工具使用 `(...args: any[]) => Promise<any>`
  - 绕过类型安全
- **建议修复**: 
  1. 使用泛型替代 any
  2. 替换为具体的输入类型
  3. 添加 ESLint 规则禁止 any

#### 4. [源代码] [高严重] [38处 @ts-ignore 指令]
- **文件/路径**: 多个文件
- **问题描述**: 
  - 14个 stability 相关文件有 `@ts-ignore ExecutionRecord type mismatch`
  - 5个 harness-sdk 文件有 `Partial<HarnessRun> doesn't have all required properties`
  - 2个文件有 `exactOptionalPropertyTypes` 问题
- **建议修复**: 
  1. 修复 ExecutionRecord 类型不匹配
  2. 完成 HarnessRun 类型的必需属性
  3. 移除 @ts-ignore 使用

#### 5. [源代码] [高严重] [ExecutionRecord 类型不匹配]
- **文件/路径**: `src/platform/stability/stable-evidence-bundle-support.ts:783` 等14个文件
- **问题描述**: 
  - 插入 execution record 时 TypeScript 拒绝类型
  - `ExecutionRecord` 接口要求新字段但 insert 对象缺失
  - 或 store 的 insert 方法接受不同类型
- **建议修复**: 
  1. 对齐 ExecutionRecord 接口和 store 类型
  2. 添加缺失字段到 insert 对象
  3. 或调整 store 的 insert 方法类型签名

#### 6. [源代码] [中严重] [30+处 as unknown as 双类型转换]
- **文件/路径**: `division-loader.ts`, `domain-baseline-catalog.ts`, `harness-sdk`
- **问题描述**: 
  - 完全绕过类型安全
  - 通常是类型架构问题的信号
- **建议修复**: 
  1. 修复上游类型问题
  2. 使用更具体的类型断言
  3. 重构类型层次结构

#### 7. [构建] [中严重] [rimraf  extraneous - 未在 package.json 声明]
- **文件/路径**: `node_modules/rimraf`
- **问题描述**: 
  - rimraf@6.1.3 存在于 node_modules 但未在 package.json 声明
  - 可能导致依赖问题
- **建议修复**: 
  1. 添加到 dependencies 或 devDependencies
  2. 或移除并使用其他清理方式

#### 8. [测试] [高严重] [178处直接 process.env 变更无抽象]
- **文件/路径**: `tests/unit/domains/registry/plugin-spi-registry.test.ts:640-687`
- **问题描述**: 
  - 直接读取/写入 process.env
  - 在 beforeEach/afterEach 中手动保存/恢复
  - 无 helper 抽象
- **建议修复**: 
  1. 创建 env helper 函数
  2. 统一保存和恢复模式
  3. 添加测试隔离验证

#### 9. [测试] [高严重] [21,998处 unlinkSync 调用无集中清理]
- **文件/路径**: 多个测试文件
- **问题描述**: 
  - 每个测试重新实现清理逻辑
  - 无集中清理 utility
  - 可能导致 EBUSY (Windows) 或并发访问问题
- **建议修复**: 
  1. 创建集中的测试清理 utility
  2. 使用 afterEach 确保清理
  3. 考虑使用临时目录

#### 10. [测试] [中严重] [无统一 mocking 框架]
- **文件/路径**: `tests/` 多个目录
- **问题描述**: 
  - 10,579 处 vi.fn, jest.mock, sinon 等调用
  - 无统一的 mocking 框架可见
  - 模式不一致
- **建议修复**: 
  1. 建立统一的 mock factory
  2. 标准化 mock 模式
  3. 添加 mocking 最佳实践文档

#### 11. [测试] [中严重] [Singleton 状态重置未标准化]
- **文件/路径**: `tests/unit/platform/five-plane-execution/dispatcher.test.ts`
- **问题描述**: 
  - 测试通过特定函数重置 singletons
  - 每个测试重复自己的 reset 逻辑
  - 无统一的 resetAllSingletons()
- **建议修复**: 
  1. 创建统一的 singleton reset 机制
  2. 在 afterEach 中自动调用
  3. 文档化 singleton 重置要求

#### 12. [测试] [低严重] [无分支覆盖率要求]
- **文件/路径**: `.c8rc.json`
- **问题描述**: 
  - `"100": false` - 无覆盖率要求
  - 只测量源代码，不测量测试文件
- **建议修复**: 
  1. 添加分支覆盖率阈值
  2. 在 CI 中强制执行覆盖率 gate
  3. 关注关键代码路径

#### 13. [网络] [高严重] [ScopedExternalAccessSandbox performHttpRequest 无超时]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/scoped-external-access-sandbox.ts:289`
- **问题描述**: 
  - fetch 调用无 timeout 参数
  - 挂起的请求可能永久阻塞
- **建议修复**: 
  1. 添加 AbortController timeout
  2. 使用标准的超时模式
  3. 记录超时配置

#### 14. [网络] [高严重] [OIDC 服务 fetch 调用无超时]
- **文件/路径**: `src/org-governance/sso-scim/oidc/oidc-service.ts:565,272`
- **问题描述**: 
  - OIDC token 交换无 retry logic
  - 无 timeout/abort signal
  - 认证流程可能挂起
- **建议修复**: 
  1. 添加 fetch timeout
  2. 添加 retry logic
  3. 使用标准的超时配置

#### 15. [网络] [高严重] [plugin-runtime-child 替换 globalThis.fetch 为 no-op]
- **文件/路径**: `src/domains/registry/plugin-runtime-child.ts:112`
- **问题描述**: 
  ```typescript
  globalThis.fetch = (async () => {}) as unknown as typeof globalThis.fetch;
  ```
  - 插件上下文中完全替换 fetch 为空函数
  - 插件的所有出站 HTTP 请求都会失败
  - 看起来是有意为之但可能造成问题
- **建议修复**: 
  1. 确认这是有意行为
  2. 文档化 fetch 替换行为
  3. 确保不会意外影响其他代码

#### 16. [网络] [中严重] [无显式 http.Agent 配置]
- **文件/路径**: 全局
- **问题描述**: 
  - 无显式的 http.Agent/https.Agent 配置
  - 依赖 Node.js 默认连接池
  - 无 maxSockets 或 maxConnections 配置
  - 高吞吐场景可能出问题
- **建议修复**: 
  1. 添加 http Agent 配置
  2. 文档化连接池大小
  3. 监控连接使用情况

#### 17. [序列化] [中严重] [合同版本硬编码为字符串字面量]
- **文件/路径**: `src/platform/contracts/inter-plane-contract-gateway.ts:184`
- **问题描述**: 
  - `schemaVersion: "v4.3"` 硬编码
  - 难以追踪合同版本演变
- **建议修复**: 
  1. 提取到类型化常量或 enum
  2. 集中管理合同版本
  3. 添加版本兼容性检查

#### 18. [序列化] [低严重] [Timestamp = string 丢失精度]
- **文件/路径**: `src/platform/contracts/types/domain/primitives.ts:10`
- **问题描述**: 
  - `type Timestamp = string` 无类型精度
  - ISO 8601 字符串无时区信息
- **建议修复**: 
  1. 使用 Date 对象或 branded string 类型
  2. 明确时区处理约定
  3. 文档化时间戳语义

#### 19. [源代码] [中严重] [29处非空断言 !.]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - 使用 `!.` 断言非空
  - 可能导致运行时错误
- **建议修复**: 
  1. 使用可选链 `?.` 或空值合并 `??`
  2. 添加适当的 null 检查
  3. 减少非空断言使用

#### 20. [API] [中严重] [API 路由 schema 版本硬编码]
- **文件/路径**: `src/platform/five-plane-interface/api/federation-routing-service.ts`
- **问题描述**: 
  - Zod schema 验证存在
  - 但 schema 版本未与 contract 版本同步
- **建议修复**: 
  1. 从 contract 版本派生 schema 版本
  2. 添加版本一致性检查
  3. 文档化版本关系

### 总结

本次补充Review（第十五轮 - 构建测试与类型系统Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. 无 TypeScript 增量编译缓存（每次完整重建9.6秒）
2. 76个 npm scripts 大部分重复构建
3. 168处 any 类型使用（绕过类型安全）
4. 38处 @ts-ignore 指令
5. ExecutionRecord 类型不匹配（14个文件）
6. 178处直接 process.env 变更无抽象
7. ScopedExternalAccessSandbox performHttpRequest 无超时
8. OIDC 服务 fetch 调用无超时
9. plugin-runtime-child 替换 globalThis.fetch 为 no-op

**中优先级**:
1. 30+处 as unknown as 双类型转换
2. rimraf extraneous 未声明
3. 21,998处 unlinkSync 无集中清理
4. 无统一 mocking 框架
5. Singleton 状态重置未标准化
6. 无显式 http.Agent 配置
7. 合同版本硬编码为字符串
8. Timestamp = string 丢失精度
9. 非空断言使用

**低优先级**:
1. 无分支覆盖率要求
2. API 路由 schema 版本未同步

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 72 | 87 | 39 | 198 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **109** | **182** | **85** | **376** |

### 优先修复建议

**立即处理 (本周内)**:
1. 添加 TypeScript 增量编译 (`incremental: true`)
2. 重构 npm scripts 避免重复构建
3. 减少 any 类型使用到 <50
4. 移除所有 @ts-ignore 指令
5. 修复 ExecutionRecord 类型不匹配
6. 添加 fetch 超时到 OIDC 和 sandbox

**短期内处理 (本月内)**:
1. 创建集中的测试清理 utility
2. 建立统一的 mocking 框架
3. 标准化 singleton 重置机制
4. 添加 http.Agent 连接池配置
5. 提取合同版本到类型化常量

**长期规划**:
1. 实现完整的类型安全（无 any）
2. 建立测试基础设施标准
3. 实现构建缓存优化
4. 完善网络层超时和重试
5. 建立覆盖率门禁标准

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十三轮 - 缓存会话与实时通信Review）

### 发现的新问题

#### 1. [安全] [严重] [CORS allowedMethods 缺少 PUT/PATCH/DELETE]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/response-hardening.ts:12-14`
- **问题描述**: 
  ```typescript
  export const DEFAULT_CORS_CONFIG: CorsConfig = {
    allowedMethods: ["GET", "POST", "OPTIONS"],  // 缺少 PUT, PATCH, DELETE
  ```
  - 浏览器 CORS preflight 会拒绝 PUT/PATCH/DELETE 请求
  - 但路由中有 PATCH /v1/tasks/:id, DELETE endpoints
- **建议修复**: 
  1. 添加 PUT, PATCH, DELETE 到 allowedMethods
  2. 确保 CORS 配置与应用路由方法一致
  3. 添加测试验证所有 HTTP 方法的 CORS preflight

#### 2. [安全] [严重] [Mission Routes 错误响应包装不当]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/mission-routes.ts:101,118-124`
- **问题描述**: 
  ```typescript
  return buildJsonResponse(ctx.requestId, 404, { error: { code: "MISSION_NOT_FOUND" } });
  ```
  - 使用 buildJsonResponse（成功包装）返回错误
  - 错误被包装在成功信封 `{requestId, data: {error: {...}}}` 中
  - 违反 REST 规范
- **建议修复**: 
  1. 使用 buildJsonErrorResponse 处理错误
  2. 确保所有错误路径使用正确的错误信封
  3. 添加 API 一致性测试

#### 3. [源代码] [高严重] [WebSocketBridge 无最大连接数限制]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:103-107`
- **问题描述**: 
  ```typescript
  this.wss = new WebSocketServer({
    server,
    path: WS_PATH,
    maxPayload: options.maxPayloadBytes ?? DEFAULT_MAX_PAYLOAD_BYTES,
    // 无 maxConnections 限制！
  });
  ```
  - 与 dashboard-websocket-server.ts 的 maxClients: 1000 不同
  - 恶意客户端可耗尽服务器资源
- **建议修复**: 
  1. 添加 maxConnections 限制
  2. 实现连接限制拒绝策略
  3. 添加连接计数指标

#### 4. [源代码] [高严重] [pendingAcks 在断开连接时未清理]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:268-282`
- **问题描述**: 
  - handleDisconnection 清理 subscribedTasks, taskSubscribers, slowConsumers
  - 但 pendingAcks Map 未清理
  - 断开的客户端未确认的消息永久留在内存
- **建议修复**: 
  1. 在 handleDisconnection 中删除所有 pendingAcks 条目
  2. 考虑添加超时机制自动清理未确认消息
  3. 记录清理操作的审计日志

#### 5. [安全] [高严重] [硬编码回退密钥]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/audit-event-integrity.ts:41`
- **问题描述**: 
  ```typescript
  const AUDIT_INTEGRITY_HMAC_KEY = process.env["AA_AUDIT_INTEGRITY_HMAC_KEY"] ?? "audit-integrity-secret-key-32-bytes!";
  ```
  - 占位符密钥用作回退
  - 如果环境变量未设置，使用不安全密钥
- **建议修复**: 
  1. 移除回退值，要求环境变量必须设置
  2. 在启动时验证密钥存在且足够强
  3. 如果密钥缺失导致启动失败

#### 6. [配置] [高严重] [config/runtime/test.json 缺少超时配置]
- **文件/路径**: `config/runtime/test.json`
- **问题描述**: 
  - 只有3个字段，缺少 maxConcurrentTasks, defaultTaskTimeoutMs, defaultStepTimeoutMs
  - 回退到默认值可能不适合测试环境
- **建议修复**: 
  1. 添加完整的 timeout/tuning 配置
  2. 确保测试环境有合理的默认值
  3. 文档化必需的配置字段

#### 7. [配置] [高严重] [prod.json 限制过严 - maxConcurrentTasks=1]
- **文件/路径**: `config/runtime/prod.json`
- **问题描述**: 
  - maxConcurrentTasks: 1（vs dev:8, staging:4, pre-prod:6）
  - defaultTaskTimeoutMs: 120000（vs dev:600000）
  - defaultStepTimeoutMs: 60000（vs dev:180000）
  - 生产环境严重受限，可能影响吞吐量
- **建议修复**: 
  1. 评估并调整生产限制
  2. 与业务需求匹配
  3. 添加生产容量测试

#### 8. [源代码] [高严重] [多个服务存在无界 Map 无驱逐策略]
- **文件/路径**: 多个域服务
- **问题描述**: 
  - `domain-knowledge-schema-service.ts:73-75`: schemas, sourceContent, sourceTimestamps 无限制
  - `domain-eval-framework-service.ts:91-95`: frameworks, qualityAxes 等无限制
  - `domain-recipe-service.ts:67-68`: recipes, versions 无限制
  - `domain-risk-profile-service.ts:51`: profiles 无限制
  - Session Maps (session-management.ts:83-89) 无自动清理
- **建议修复**: 
  1. 为所有 Map 实现 LRU 或 TTL 驱逐策略
  2. 添加后台清理任务
  3. 添加大小监控和告警

#### 9. [源代码] [中严重] [taskEventHistory Map 永不清理]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:91,548-555`
- **问题描述**: 
  - taskEventHistory 限制单任务历史为 200 条
  - 但 Map 本身从不清理
  - 取消订阅的任务历史永久保留
- **建议修复**: 
  1. 实现后台清理无订阅者任务的历史
  2. 添加任务订阅者监控
  3. 考虑使用 WeakMap 替代

#### 10. [配置] [中严重] [安全配置 drift - remoteWorkerRegistration 缺失]
- **文件/路径**: `config/security/dev.json`, `staging.json`, `pre-prod.json`
- **问题描述**: 
  - default.json 有完整的 remoteWorkerRegistration 配置
  - dev/staging/pre-prod 只有 approvalMode
  - prod 有 approvalMode 但无 remoteWorkerRegistration
  - 安全配置不一致
- **建议修复**: 
  1. 统一所有环境的 remoteWorkerRegistration
  2. 添加安全配置验证
  3. 确保最低安全基线

#### 11. [API] [中严重] [OpenAPI 端点响应格式不一致]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/utils.ts:207-215`
- **问题描述**: 
  - `/v1/openapi.json` 使用 buildJsonDocumentResponse（无包装）
  - 其他端点使用 buildJsonResponse（{requestId, data} 包装）
  - 客户端体验不一致
- **建议修复**: 
  1. 统一响应信封格式
  2. 文档化响应格式规范
  3. 添加响应格式验证测试

#### 12. [源代码] [中严重] [Redis 队列缺少 idempotency 索引支持]
- **文件/路径**: `src/platform/five-plane-execution/queue/redis-queue-adapter.ts`
- **问题描述**: 
  - SQLite 有部分唯一索引支持 idempotency
  - Redis 实现使用哈希索引但实现不完整
  - 可能导致重复消息
- **建议修复**: 
  1. 完善 Redis idempotency 实现
  2. 添加唯一索引验证
  3. 确保与 SQLite 行为一致

#### 13. [源代码] [中严重] [请求去重中间件使用内存存储]
- **文件/路径**: `src/platform/five-plane-interface/api/middleware/request-deduplication.ts`
- **问题描述**: 
  - 纯内存滑动窗口（Map<DeduplicationKey, DeduplicationEntry[]>）
  - 无外部存储
  - 多实例部署不共享
  - 重启后去重状态丢失
- **建议修复**: 
  1. 使用 Redis 替代内存存储
  2. 支持分布式去重
  3. 持久化去重状态

#### 14. [源代码] [中严重] [Cache 无 stampede 保护]
- **文件/路径**: 多个 cache 实现
- **问题描述**: 
  - MemoryCacheStore, ExperienceCacheService 无锁
  - 缓存未命中时可发生 thundering herd
  - 高并发下可能导致数据库过载
- **建议修复**: 
  1. 实现 single-flight 模式
  2. 添加请求排队机制
  3. 使用分布式锁保护缓存更新

#### 15. [源代码] [中严重] [EvidenceService eviction 仅在插入时触发]
- **文件/路径**: `src/platform/five-plane-state-evidence/memory/evidence-service.ts:202`
- **问题描述**: 
  - 清理仅在 record() 调用时触发
  - 空闲时无后台清理
  - 可能导致内存持续增长
- **建议修复**: 
  1. 添加定期后台清理任务
  2. 使用独立的清理线程
  3. 添加内存使用监控

#### 16. [源代码] [低严重] [无连接指标暴露]
- **文件/路径**: WebSocket 服务器
- **问题描述**: 
  - getClientCount() 存在但未通过 HTTP 端点暴露
  - 无 pendingAcks 队列深度指标
  - 无连接建立告警
- **建议修复**: 
  1. 通过 metrics 端点暴露连接指标
  2. 添加 pendingAcks 队列监控
  3. 添加连接数异常告警

#### 17. [源代码] [低严重] [无空闲客户端超时]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts`
- **问题描述**: 
  - 仅在心跳 Sweep 时检查 isAlive
  - 无 per-client 独立于心跳的空闲超时
  - 认证后从不发送消息的客户端只能通过心跳失败检测
- **建议修复**: 
  1. 添加 per-client idle timeout
  2. 独立于心跳间隔
  3. 配置可调整

#### 18. [源代码] [低严重] [订阅限制是 per-client 而非全局]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:244`
- **问题描述**: 
  - MAX_SUBSCRIPTIONS_PER_CLIENT = 100
  - 恶意客户端可订阅 100 个任务
  - 无全局任务订阅者限制
- **建议修复**: 
  1. 添加全局任务订阅者限制
  2. 实现 per-task 订阅者上限
  3. 添加反滥用检测

#### 19. [配置] [低严重] [config/runtime 无版本验证机制]
- **文件/路径**: `config/runtime/*.json`
- **问题描述**: 
  - 配置有 "version": "v4.3"
  - 但无 schema 版本验证
  - 加载时可能接受不兼容配置
- **建议修复**: 
  1. 添加 JSON Schema 验证
  2. 实现版本兼容性检查
  3. 启动时验证配置完整性

#### 20. [源代码] [低严重] [ImprovementCandidateRegistry splice 是 O(n)]
- **文件/路径**: `src/platform/five-plane-orchestration/improve/improvement-candidate-registry.ts:243`
- **问题描述**: 
  - accessOrder 数组的 splice 操作是 O(n)
  - 高频访问时可能性能问题
- **建议修复**: 
  1. 使用 LinkedList 替代数组
  2. 或使用 Map 维护访问顺序
  3. 性能测试验证

### 总结

本次补充Review（第十三轮 - 缓存会话与实时通信Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. CORS allowedMethods 缺少 PUT/PATCH/DELETE（浏览器请求会失败）
2. Mission Routes 错误响应包装不当
3. WebSocketBridge 无最大连接数限制（DoS风险）
4. pendingAcks 断开连接时未清理（内存泄漏）
5. 硬编码回退密钥（安全漏洞）
6. config/runtime/test.json 缺少超时配置
7. prod.json maxConcurrentTasks=1 限制过严
8. 多个服务存在无界 Map（内存泄漏风险）

**中优先级**:
1. taskEventHistory Map 永不清理
2. 安全配置 drift（remoteWorkerRegistration 缺失）
3. OpenAPI 端点响应格式不一致
4. Redis 队列 idempotency 实现不完整
5. 请求去重中间件使用内存存储（多实例不共享）
6. Cache 无 stampede 保护
7. EvidenceService eviction 仅在插入时触发

**低优先级**:
1. 无连接指标暴露
2. 无空闲客户端超时
3. 订阅限制是 per-client 而非全局
4. config/runtime 无版本验证机制
5. ImprovementCandidateRegistry splice O(n)

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 64 | 74 | 35 | 173 |
| 测试 | 8 | 14 | 5 | 27 |
| 配置 | 10 | 38 | 24 | 72 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 7 | 3 | 12 |
| UI | 0 | 3 | 3 | 6 |
| 部署 | 1 | 10 | 6 | 17 |
| **合计** | **99** | **161** | **79** | **339** |

### 优先修复建议

**立即处理 (本周内)**:
1. 修复 CORS allowedMethods 添加 PUT/PATCH/DELETE
2. 修复 Mission Routes 错误响应格式
3. 为 WebSocketBridge 添加最大连接数限制
4. 清理 pendingAcks 和 taskEventHistory
5. 移除硬编码回退密钥
6. 修复 config/runtime/test.json 配置
7. 评估并调整 prod 并发限制

**短期内处理 (本月内)**:
1. 统一安全配置（remoteWorkerRegistration）
2. 实现无界 Map 的 LRU/TTL 驱逐
3. 统一 API 响应信封格式
4. 完善 Redis idempotency 实现
5. 添加分布式去重中间件
6. 实现 cache stampede 保护

**长期规划**:
1. 建立完整的连接管理和监控
2. 实现后台清理任务框架
3. 添加配置 schema 验证
4. 优化高频繁操作的数据结构
5. 建立内存使用基线和告警

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十五轮 - 构建测试与类型系统Review）

### 发现的新问题

#### 1. [构建] [高严重] [无 TypeScript 增量编译缓存]
- **文件/路径**: `tsconfig.json`
- **问题描述**: 
  - 无 `incremental: true` 配置
  - 无 `tsbuildinfo` 文件
  - 每次构建是完整重建（9.6秒）
  - TypeScript 无法使用增量编译
- **建议修复**: 
  1. 在 tsconfig.json 添加 `"incremental": true`
  2. 添加 `"tsbuildinfo": ".tsbuildinfo"` 到 .gitignore
  3. 实现项目引用以支持并行构建

#### 2. [构建] [高严重] [76个 npm scripts 大部分重复构建]
- **文件/路径**: `package.json`
- **问题描述**: 
  - 40+ CLI scripts 都运行 `npm run build && node ...`
  - 每个脚本前都执行完整重建
  - 无法直接运行预构建的 CLI
  - 20+ stable:* 脚本遵循相同模式
- **建议修复**: 
  1. 创建 `build:cli` 脚本构建一次
  2. 让 CLI 脚本使用预构建的 dist 文件
  3. 添加 npm run dev 或类似的无需重建的执行方式

#### 3. [源代码] [高严重] [168处 any 类型使用]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - `stable-runner-factory.ts`: `opts: any` -> `any`
  - `billing-service-async.ts`: `input: any` 方法签名
  - 重试/超时工具使用 `(...args: any[]) => Promise<any>`
  - 绕过类型安全
- **建议修复**: 
  1. 使用泛型替代 any
  2. 替换为具体的输入类型
  3. 添加 ESLint 规则禁止 any

#### 4. [源代码] [高严重] [38处 @ts-ignore 指令]
- **文件/路径**: 多个文件
- **问题描述**: 
  - 14个 stability 相关文件有 `@ts-ignore ExecutionRecord type mismatch`
  - 5个 harness-sdk 文件有 `Partial<HarnessRun> doesn't have all required properties`
  - 2个文件有 `exactOptionalPropertyTypes` 问题
- **建议修复**: 
  1. 修复 ExecutionRecord 类型不匹配
  2. 完成 HarnessRun 类型的必需属性
  3. 移除 @ts-ignore 使用

#### 5. [源代码] [高严重] [ExecutionRecord 类型不匹配]
- **文件/路径**: `src/platform/stability/stable-evidence-bundle-support.ts:783` 等14个文件
- **问题描述**: 
  - 插入 execution record 时 TypeScript 拒绝类型
  - `ExecutionRecord` 接口要求新字段但 insert 对象缺失
  - 或 store 的 insert 方法接受不同类型
- **建议修复**: 
  1. 对齐 ExecutionRecord 接口和 store 类型
  2. 添加缺失字段到 insert 对象
  3. 或调整 store 的 insert 方法类型签名

#### 6. [源代码] [中严重] [30+处 as unknown as 双类型转换]
- **文件/路径**: `division-loader.ts`, `domain-baseline-catalog.ts`, `harness-sdk`
- **问题描述**: 
  - 完全绕过类型安全
  - 通常是类型架构问题的信号
- **建议修复**: 
  1. 修复上游类型问题
  2. 使用更具体的类型断言
  3. 重构类型层次结构

#### 7. [构建] [中严重] [rimraf  extraneous - 未在 package.json 声明]
- **文件/路径**: `node_modules/rimraf`
- **问题描述**: 
  - rimraf@6.1.3 存在于 node_modules 但未在 package.json 声明
  - 可能导致依赖问题
- **建议修复**: 
  1. 添加到 dependencies 或 devDependencies
  2. 或移除并使用其他清理方式

#### 8. [测试] [高严重] [178处直接 process.env 变更无抽象]
- **文件/路径**: `tests/unit/domains/registry/plugin-spi-registry.test.ts:640-687`
- **问题描述**: 
  - 直接读取/写入 process.env
  - 在 beforeEach/afterEach 中手动保存/恢复
  - 无 helper 抽象
- **建议修复**: 
  1. 创建 env helper 函数
  2. 统一保存和恢复模式
  3. 添加测试隔离验证

#### 9. [测试] [高严重] [21,998处 unlinkSync 调用无集中清理]
- **文件/路径**: 多个测试文件
- **问题描述**: 
  - 每个测试重新实现清理逻辑
  - 无集中清理 utility
  - 可能导致 EBUSY (Windows) 或并发访问问题
- **建议修复**: 
  1. 创建集中的测试清理 utility
  2. 使用 afterEach 确保清理
  3. 考虑使用临时目录

#### 10. [测试] [中严重] [无统一 mocking 框架]
- **文件/路径**: `tests/` 多个目录
- **问题描述**: 
  - 10,579 处 vi.fn, jest.mock, sinon 等调用
  - 无统一的 mocking 框架可见
  - 模式不一致
- **建议修复**: 
  1. 建立统一的 mock factory
  2. 标准化 mock 模式
  3. 添加 mocking 最佳实践文档

#### 11. [测试] [中严重] [Singleton 状态重置未标准化]
- **文件/路径**: `tests/unit/platform/five-plane-execution/dispatcher.test.ts`
- **问题描述**: 
  - 测试通过特定函数重置 singletons
  - 每个测试重复自己的 reset 逻辑
  - 无统一的 resetAllSingletons()
- **建议修复**: 
  1. 创建统一的 singleton reset 机制
  2. 在 afterEach 中自动调用
  3. 文档化 singleton 重置要求

#### 12. [测试] [低严重] [无分支覆盖率要求]
- **文件/路径**: `.c8rc.json`
- **问题描述**: 
  - `"100": false` - 无覆盖率要求
  - 只测量源代码，不测量测试文件
- **建议修复**: 
  1. 添加分支覆盖率阈值
  2. 在 CI 中强制执行覆盖率 gate
  3. 关注关键代码路径

#### 13. [网络] [高严重] [ScopedExternalAccessSandbox performHttpRequest 无超时]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/scoped-external-access-sandbox.ts:289`
- **问题描述**: 
  - fetch 调用无 timeout 参数
  - 挂起的请求可能永久阻塞
- **建议修复**: 
  1. 添加 AbortController timeout
  2. 使用标准的超时模式
  3. 记录超时配置

#### 14. [网络] [高严重] [OIDC 服务 fetch 调用无超时]
- **文件/路径**: `src/org-governance/sso-scim/oidc/oidc-service.ts:565,272`
- **问题描述**: 
  - OIDC token 交换无 retry logic
  - 无 timeout/abort signal
  - 认证流程可能挂起
- **建议修复**: 
  1. 添加 fetch timeout
  2. 添加 retry logic
  3. 使用标准的超时配置

#### 15. [网络] [高严重] [plugin-runtime-child 替换 globalThis.fetch 为 no-op]
- **文件/路径**: `src/domains/registry/plugin-runtime-child.ts:112`
- **问题描述**: 
  ```typescript
  globalThis.fetch = (async () => {}) as unknown as typeof globalThis.fetch;
  ```
  - 插件上下文中完全替换 fetch 为空函数
  - 插件的所有出站 HTTP 请求都会失败
  - 看起来是有意为之但可能造成问题
- **建议修复**: 
  1. 确认这是有意行为
  2. 文档化 fetch 替换行为
  3. 确保不会意外影响其他代码

#### 16. [网络] [中严重] [无显式 http.Agent 配置]
- **文件/路径**: 全局
- **问题描述**: 
  - 无显式的 http.Agent/https.Agent 配置
  - 依赖 Node.js 默认连接池
  - 无 maxSockets 或 maxConnections 配置
  - 高吞吐场景可能出问题
- **建议修复**: 
  1. 添加 http Agent 配置
  2. 文档化连接池大小
  3. 监控连接使用情况

#### 17. [序列化] [中严重] [合同版本硬编码为字符串字面量]
- **文件/路径**: `src/platform/contracts/inter-plane-contract-gateway.ts:184`
- **问题描述**: 
  - `schemaVersion: "v4.3"` 硬编码
  - 难以追踪合同版本演变
- **建议修复**: 
  1. 提取到类型化常量或 enum
  2. 集中管理合同版本
  3. 添加版本兼容性检查

#### 18. [序列化] [低严重] [Timestamp = string 丢失精度]
- **文件/路径**: `src/platform/contracts/types/domain/primitives.ts:10`
- **问题描述**: 
  - `type Timestamp = string` 无类型精度
  - ISO 8601 字符串无时区信息
- **建议修复**: 
  1. 使用 Date 对象或 branded string 类型
  2. 明确时区处理约定
  3. 文档化时间戳语义

#### 19. [源代码] [中严重] [29处非空断言 !.]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - 使用 `!.` 断言非空
  - 可能导致运行时错误
- **建议修复**: 
  1. 使用可选链 `?.` 或空值合并 `??`
  2. 添加适当的 null 检查
  3. 减少非空断言使用

#### 20. [API] [中严重] [API 路由 schema 版本硬编码]
- **文件/路径**: `src/platform/five-plane-interface/api/federation-routing-service.ts`
- **问题描述**: 
  - Zod schema 验证存在
  - 但 schema 版本未与 contract 版本同步
- **建议修复**: 
  1. 从 contract 版本派生 schema 版本
  2. 添加版本一致性检查
  3. 文档化版本关系

### 总结

本次补充Review（第十五轮 - 构建测试与类型系统Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. 无 TypeScript 增量编译缓存（每次完整重建9.6秒）
2. 76个 npm scripts 大部分重复构建
3. 168处 any 类型使用（绕过类型安全）
4. 38处 @ts-ignore 指令
5. ExecutionRecord 类型不匹配（14个文件）
6. 178处直接 process.env 变更无抽象
7. ScopedExternalAccessSandbox performHttpRequest 无超时
8. OIDC 服务 fetch 调用无超时
9. plugin-runtime-child 替换 globalThis.fetch 为 no-op

**中优先级**:
1. 30+处 as unknown as 双类型转换
2. rimraf extraneous 未声明
3. 21,998处 unlinkSync 无集中清理
4. 无统一 mocking 框架
5. Singleton 状态重置未标准化
6. 无显式 http.Agent 配置
7. 合同版本硬编码为字符串
8. Timestamp = string 丢失精度
9. 非空断言使用

**低优先级**:
1. 无分支覆盖率要求
2. API 路由 schema 版本未同步

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 72 | 87 | 39 | 198 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **109** | **182** | **85** | **376** |

### 优先修复建议

**立即处理 (本周内)**:
1. 添加 TypeScript 增量编译 (`incremental: true`)
2. 重构 npm scripts 避免重复构建
3. 减少 any 类型使用到 <50
4. 移除所有 @ts-ignore 指令
5. 修复 ExecutionRecord 类型不匹配
6. 添加 fetch 超时到 OIDC 和 sandbox

**短期内处理 (本月内)**:
1. 创建集中的测试清理 utility
2. 建立统一的 mocking 框架
3. 标准化 singleton 重置机制
4. 添加 http.Agent 连接池配置
5. 提取合同版本到类型化常量

**长期规划**:
1. 实现完整的类型安全（无 any）
2. 建立测试基础设施标准
3. 实现构建缓存优化
4. 完善网络层超时和重试
5. 建立覆盖率门禁标准

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十四轮 - 日志事件与工作流Review）

### 发现的新问题

#### 1. [可观测性] [高严重] [StructuredLogEntry 无 requestId 字段]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - StructuredLogEntry 接口无 requestId 字段
  - 只有 correlationId，它回退到 traceId
  - 无法独立追踪单个 HTTP 请求
- **建议修复**: 
  1. 在 StructuredLogEntry 添加 requestId 字段
  2. 在 HTTP 中间件中生成和注入 requestId
  3. 确保 requestId 流经所有日志调用

#### 2. [可观测性] [高严重] [StructuredLogger data 字段无自动清洗]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - 如果开发者传递 `{ password: "..." }` 给 logger.info，密码会被记录
  - 无字段级脱敏机制
  - 依赖开发者手动排除敏感字段
- **建议修复**: 
  1. 添加 OBSERVE_OUTPUT_BLACKLIST 类似的黑名单
  2. 实现 redact/mask/sanitize 工具函数
  3. 在日志传输前自动清洗敏感字段

#### 3. [可观测性] [中严重] [日志中敏感数据无 PII 处理]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - 无 PII 重写工具
  - 开发者必须手动避免记录敏感数据
  - 存在意外记录敏感信息的风险
- **建议修复**: 
  1. 添加 PII 检测和重写工具
  2. 在文档中明确记录敏感字段列表
  3. 添加测试验证敏感数据不被记录

#### 4. [可观测性] [中严重] [59处 console.* 直接调用]
- **文件/路径**: 多个源文件
- **问题描述**: 
  - 应该使用 StructuredLogger
  - console 调用可能不被日志聚合系统收集
  - 难以追踪和关联
- **建议修复**: 
  1. 替换所有 console.* 为 StructuredLogger
  2. 添加 ESLint 规则禁止 console.*
  3. 保留少量允许的 CLI 输出

#### 5. [可观测性] [中严重] [部分 logger.warn 调用缺少结构化数据]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:122`
- **问题描述**: 
  ```typescript
  logger.warn("WebSocket connection rejected: missing subprotocol token")
  ```
  - 只有消息，无 data 对象
  - 难以关联和查询
- **建议修复**: 
  1. 添加 `{ data: { ... } }` 对象
  2. 确保所有日志包含上下文数据
  3. 添加日志审查工具

#### 6. [源代码] [高严重] [Harness while(true) 循环无硬性迭代上限]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:1442`
- **问题描述**: 
  ```typescript
  while (true) {
    // ...
    // 依赖 budget gate 和 guardrail vibration 退出
    // 如果两者都失败，循环永久继续
  }
  ```
  - 依赖 budget gate 和 guardrail vibration 退出
  - 无硬性迭代次数限制
  - 可能导致无限循环
- **建议修复**: 
  1. 添加硬性 maxIterations 上限
  2. 在达到上限时强制退出
  3. 记录详细的迭代统计用于调试

#### 7. [源代码] [高严重] [Oapeflir while(true) 循环可能无限重规划]
- **文件/路径**: `src/platform/five-plane-orchestration/oapeflir/oapeflir-loop-service.ts:355`
- **问题描述**: 
  - 如果 loopReplanDecision.shouldReplan 永不变为 false
  - 重规划永远无法减少计划错误
  - 可能导致无限循环
- **建议修复**: 
  1. 添加重规划次数上限
  2. 在达到上限时强制接受当前结果
  3. 添加重规划质量衰减检测

#### 8. [源代码] [中严重] [Recovery 流程允许终态转换到 paused]
- **文件/路径**: `src/platform/five-plane-execution/runtime-state-machine.ts:105-108`
- **问题描述**: 
  - completed, failed, cancelled, aborted 可转换到 paused 进行恢复
  - 创建恢复路径可能重新进入执行
  - 需要验证此行为的正确性
- **建议修复**: 
  1. 审查 recovery 转换的合法性
  2. 确保不会导致状态不一致
  3. 添加转换前置条件验证

#### 9. [国际化] [高严重] [API 错误消息硬编码英文]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/api-error.ts`
- **问题描述**: 
  ```typescript
  return new ApiError(404, "api.task_not_found", "Task not found.");
  ```
  - 错误消息硬编码英文
  - 无后端 i18n 机制
  - 非英文客户端收到不本地化的错误
- **建议修复**: 
  1. 使用错误码而非硬编码消息
  2. 客户端根据错误码本地化
  3. 或在后端添加 i18n 支持

#### 10. [国际化] [中严重] [UI 硬编码字符串未翻译]
- **文件/路径**: `ui/packages/features/conversation/src/web/index.tsx`
- **问题描述**: 
  - "Build Plan", "Execute", "Messages" 等按钮标签硬编码
  - 不使用 i18n 系统
- **建议修复**: 
  1. 使用 `translateFeatureCopy` 替换硬编码
  2. 确保所有用户可见文本通过 i18n
  3. 添加硬编码字符串检测

#### 11. [国际化] [中严重] [Arabic 目录不完整]
- **文件/路径**: `ui/packages/shared/i18n/src/catalogs/ar-SA.ts`
- **问题描述**: 
  - ar-SA 只有约18条消息
  - en-US 和 zh-CN 有约117条
  - 缺失的 key 返回 key 本身
- **建议修复**: 
  1. 补充 Arabic 翻译
  2. 确保所有 key 都有翻译
  3. 添加翻译完整性测试

#### 12. [国际化] [低严重] [translateFeatureCopy 无回退默认值]
- **文件/路径**: `ui/packages/shared/i18n/`
- **问题描述**: 
  - 如果 feature ID 不在 catalog 中
  - 返回 undefined title/summary
  - 无合理回退
- **建议修复**: 
  1. 添加回退到 key 或默认字符串
  2. 或使用 feature ID 作为显示名
  3. 确保 UI 不显示 undefined

#### 13. [运维] [高严重] [健康检查无 readiness/liveness 区分]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts`
- **问题描述**: 
  - /healthz, /v1/healthz, /health 返回相同报告
  - 无 readiness probe（可处理流量？）
  - 无 liveness probe（系统存活？）
  - canAcceptTraffic() 未通过 HTTP 暴露
- **建议修复**: 
  1. 添加 /ready 和 /live 端点
  2. Readiness 检查依赖项就绪
  3. Liveness 检查进程健康

#### 14. [运维] [高严重] [shutdown 期间健康检查存在竞态]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts`
- **问题描述**: 
  - getHealthReportAsync() 异步检查 dbWritable
  - shutdown 期间数据库连接可能关闭
  - 健康检查仍在传入
- **建议修复**: 
  1. 在 shutdown 时返回 unhealthy
  2. 添加 shutdown 状态标志
  3. 停止接受新的健康检查请求

#### 15. [运维] [高严重] [LeaderElectionService 未与 GracefulShutdown 集成]
- **文件/路径**: `src/platform/five-plane-execution/ha/leader-election-service.ts`
- **问题描述**: 
  - LeaderElectionService.stop() 处理优雅退位
  - 但未在全局 graceful shutdown 中注册
  - 需显式注册为 shutdown handler
- **建议修复**: 
  1. 将 LeaderElectionService 添加到 shutdown handlers
  2. 确保领导者在 shutdown 时让位
  3. 验证关闭顺序正确

#### 16. [运维] [中严重] [addHandler() 无显式排序约定]
- **文件/路径**: `src/platform/five-plane-execution/startup/graceful-shutdown.ts`
- **问题描述**: 
  - 虽然 handlers 逆序执行
  - 但无显式接口声明 handler 依赖关系
  - 关键 handlers（如关闭数据库连接）应最后运行
- **建议修复**: 
  1. 添加优先级或阶段参数
  2. 文档化关键 handler 顺序要求
  3. 添加顺序验证测试

#### 17. [运维] [中严重] [unref'd timers 可能不触发]
- **文件/路径**: `src/platform/five-plane-execution/startup/process-error-handlers.ts:106`
- **问题描述**: 
  - `setTimeout(...).unref()` 不保持事件循环活跃
  - 如果主线程空闲，强制退出可能不触发
- **建议修复**: 
  1. 移除 .unref() 除非确实需要
  2. 或确保有其他方式保持事件循环
  3. 添加超时触发验证测试

#### 18. [运维] [中严重] [StartupConsistencyChecker 阻塞流量但不暴露状态]
- **文件/路径**: `src/platform/five-plane-startup-plan.ts`
- **问题描述**: 
  - canAcceptTraffic() 在 P0 问题存在时返回 false
  - 但 _trafficBlocked 是内部状态
  - 负载均衡器访问 /healthz 仍会得到 "ok"
- **建议修复**: 
  1. 在健康报告中暴露 trafficBlocked 状态
  2. 或添加单独的健康端点指示就绪状态
  3. 确保负载均衡器在阻塞时不路由流量

#### 19. [事件系统] [中严重] [TypedEventBus vs EventEmitter 混用]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - 项目同时使用 TypedEventBus 和原生 EventEmitter
  - TypedEventBus 应该是标准
  - 混用可能导致类型安全问题
- **建议修复**: 
  1. 创建迁移计划统一使用 TypedEventBus
  2. 添加 ESLint 规则禁止原生 EventEmitter
  3. 在 CI 中检测混用情况

#### 20. [源代码] [低严重] [无事件排序保证]
- **文件/路径**: `src/platform/five-plane-state-evidence/events/`
- **问题描述**: 
  - 无明确的事件排序保证文档
  - 事件处理可能乱序
  - 依赖方需要处理乱序
- **建议修复**: 
  1. 文档化事件排序语义
  2. 如果需要排序，实现序列号机制
  3. 在事件处理中考虑乱序情况

### 总结

本次补充Review（第十四轮 - 日志事件与工作流Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. StructuredLogEntry 无 requestId 字段
2. StructuredLogger data 字段无自动清洗（敏感数据风险）
3. Harness while(true) 循环无硬性迭代上限
4. Oapeflir while(true) 循环可能无限重规划
5. API 错误消息硬编码英文（无 i18n）
6. 健康检查无 readiness/liveness 区分
7. shutdown 期间健康检查存在竞态
8. LeaderElectionService 未与 GracefulShutdown 集成

**中优先级**:
1. 日志中敏感数据无 PII 处理
2. 59处 console.* 直接调用
3. 部分 logger.warn 调用缺少结构化数据
4. Recovery 流程允许终态转换到 paused
5. UI 硬编码字符串未翻译
6. Arabic 目录不完整
7. addHandler() 无显式排序约定
8. unref'd timers 可能不触发
9. StartupConsistencyChecker 阻塞流量但不暴露状态
10. TypedEventBus vs EventEmitter 混用

**低优先级**:
1. translateFeatureCopy 无回退默认值
2. 无事件排序保证

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 68 | 80 | 37 | 185 |
| 测试 | 8 | 14 | 5 | 27 |
| 配置 | 10 | 38 | 24 | 72 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **103** | **171** | **82** | **356** |

### 优先修复建议

**立即处理 (本周内)**:
1. 在 StructuredLogEntry 添加 requestId 字段
2. 实现日志敏感字段自动清洗
3. 为工作流循环添加硬性迭代上限
4. 添加 readiness/liveness 健康检查端点
5. 将 LeaderElectionService 集成到 GracefulShutdown
6. 实现 API 错误消息 i18n 支持

**短期内处理 (本月内)**:
1. 替换所有 console.* 为 StructuredLogger
2. 补充 Arabic i18n 目录
3. 修复 UI 硬编码字符串
4. 统一 TypedEventBus 替代 EventEmitter
5. 解决 shutdown 竞态条件
6. 添加事件排序保证文档

**长期规划**:
1. 建立完整的 PII 处理框架
2. 实现日志完整性验证
3. 建立工作流安全循环检测
4. 完善健康检查和就绪探测
5. 建立国际化完整测试

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十五轮 - 构建测试与类型系统Review）

### 发现的新问题

#### 1. [构建] [高严重] [无 TypeScript 增量编译缓存]
- **文件/路径**: `tsconfig.json`
- **问题描述**: 
  - 无 `incremental: true` 配置
  - 无 `tsbuildinfo` 文件
  - 每次构建是完整重建（9.6秒）
  - TypeScript 无法使用增量编译
- **建议修复**: 
  1. 在 tsconfig.json 添加 `"incremental": true`
  2. 添加 `"tsbuildinfo": ".tsbuildinfo"` 到 .gitignore
  3. 实现项目引用以支持并行构建

#### 2. [构建] [高严重] [76个 npm scripts 大部分重复构建]
- **文件/路径**: `package.json`
- **问题描述**: 
  - 40+ CLI scripts 都运行 `npm run build && node ...`
  - 每个脚本前都执行完整重建
  - 无法直接运行预构建的 CLI
  - 20+ stable:* 脚本遵循相同模式
- **建议修复**: 
  1. 创建 `build:cli` 脚本构建一次
  2. 让 CLI 脚本使用预构建的 dist 文件
  3. 添加 npm run dev 或类似的无需重建的执行方式

#### 3. [源代码] [高严重] [168处 any 类型使用]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - `stable-runner-factory.ts`: `opts: any` -> `any`
  - `billing-service-async.ts`: `input: any` 方法签名
  - 重试/超时工具使用 `(...args: any[]) => Promise<any>`
  - 绕过类型安全
- **建议修复**: 
  1. 使用泛型替代 any
  2. 替换为具体的输入类型
  3. 添加 ESLint 规则禁止 any

#### 4. [源代码] [高严重] [38处 @ts-ignore 指令]
- **文件/路径**: 多个文件
- **问题描述**: 
  - 14个 stability 相关文件有 `@ts-ignore ExecutionRecord type mismatch`
  - 5个 harness-sdk 文件有 `Partial<HarnessRun> doesn't have all required properties`
  - 2个文件有 `exactOptionalPropertyTypes` 问题
- **建议修复**: 
  1. 修复 ExecutionRecord 类型不匹配
  2. 完成 HarnessRun 类型的必需属性
  3. 移除 @ts-ignore 使用

#### 5. [源代码] [高严重] [ExecutionRecord 类型不匹配]
- **文件/路径**: `src/platform/stability/stable-evidence-bundle-support.ts:783` 等14个文件
- **问题描述**: 
  - 插入 execution record 时 TypeScript 拒绝类型
  - `ExecutionRecord` 接口要求新字段但 insert 对象缺失
  - 或 store 的 insert 方法接受不同类型
- **建议修复**: 
  1. 对齐 ExecutionRecord 接口和 store 类型
  2. 添加缺失字段到 insert 对象
  3. 或调整 store 的 insert 方法类型签名

#### 6. [源代码] [中严重] [30+处 as unknown as 双类型转换]
- **文件/路径**: `division-loader.ts`, `domain-baseline-catalog.ts`, `harness-sdk`
- **问题描述**: 
  - 完全绕过类型安全
  - 通常是类型架构问题的信号
- **建议修复**: 
  1. 修复上游类型问题
  2. 使用更具体的类型断言
  3. 重构类型层次结构

#### 7. [构建] [中严重] [rimraf  extraneous - 未在 package.json 声明]
- **文件/路径**: `node_modules/rimraf`
- **问题描述**: 
  - rimraf@6.1.3 存在于 node_modules 但未在 package.json 声明
  - 可能导致依赖问题
- **建议修复**: 
  1. 添加到 dependencies 或 devDependencies
  2. 或移除并使用其他清理方式

#### 8. [测试] [高严重] [178处直接 process.env 变更无抽象]
- **文件/路径**: `tests/unit/domains/registry/plugin-spi-registry.test.ts:640-687`
- **问题描述**: 
  - 直接读取/写入 process.env
  - 在 beforeEach/afterEach 中手动保存/恢复
  - 无 helper 抽象
- **建议修复**: 
  1. 创建 env helper 函数
  2. 统一保存和恢复模式
  3. 添加测试隔离验证

#### 9. [测试] [高严重] [21,998处 unlinkSync 调用无集中清理]
- **文件/路径**: 多个测试文件
- **问题描述**: 
  - 每个测试重新实现清理逻辑
  - 无集中清理 utility
  - 可能导致 EBUSY (Windows) 或并发访问问题
- **建议修复**: 
  1. 创建集中的测试清理 utility
  2. 使用 afterEach 确保清理
  3. 考虑使用临时目录

#### 10. [测试] [中严重] [无统一 mocking 框架]
- **文件/路径**: `tests/` 多个目录
- **问题描述**: 
  - 10,579 处 vi.fn, jest.mock, sinon 等调用
  - 无统一的 mocking 框架可见
  - 模式不一致
- **建议修复**: 
  1. 建立统一的 mock factory
  2. 标准化 mock 模式
  3. 添加 mocking 最佳实践文档

#### 11. [测试] [中严重] [Singleton 状态重置未标准化]
- **文件/路径**: `tests/unit/platform/five-plane-execution/dispatcher.test.ts`
- **问题描述**: 
  - 测试通过特定函数重置 singletons
  - 每个测试重复自己的 reset 逻辑
  - 无统一的 resetAllSingletons()
- **建议修复**: 
  1. 创建统一的 singleton reset 机制
  2. 在 afterEach 中自动调用
  3. 文档化 singleton 重置要求

#### 12. [测试] [低严重] [无分支覆盖率要求]
- **文件/路径**: `.c8rc.json`
- **问题描述**: 
  - `"100": false` - 无覆盖率要求
  - 只测量源代码，不测量测试文件
- **建议修复**: 
  1. 添加分支覆盖率阈值
  2. 在 CI 中强制执行覆盖率 gate
  3. 关注关键代码路径

#### 13. [网络] [高严重] [ScopedExternalAccessSandbox performHttpRequest 无超时]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/scoped-external-access-sandbox.ts:289`
- **问题描述**: 
  - fetch 调用无 timeout 参数
  - 挂起的请求可能永久阻塞
- **建议修复**: 
  1. 添加 AbortController timeout
  2. 使用标准的超时模式
  3. 记录超时配置

#### 14. [网络] [高严重] [OIDC 服务 fetch 调用无超时]
- **文件/路径**: `src/org-governance/sso-scim/oidc/oidc-service.ts:565,272`
- **问题描述**: 
  - OIDC token 交换无 retry logic
  - 无 timeout/abort signal
  - 认证流程可能挂起
- **建议修复**: 
  1. 添加 fetch timeout
  2. 添加 retry logic
  3. 使用标准的超时配置

#### 15. [网络] [高严重] [plugin-runtime-child 替换 globalThis.fetch 为 no-op]
- **文件/路径**: `src/domains/registry/plugin-runtime-child.ts:112`
- **问题描述**: 
  ```typescript
  globalThis.fetch = (async () => {}) as unknown as typeof globalThis.fetch;
  ```
  - 插件上下文中完全替换 fetch 为空函数
  - 插件的所有出站 HTTP 请求都会失败
  - 看起来是有意为之但可能造成问题
- **建议修复**: 
  1. 确认这是有意行为
  2. 文档化 fetch 替换行为
  3. 确保不会意外影响其他代码

#### 16. [网络] [中严重] [无显式 http.Agent 配置]
- **文件/路径**: 全局
- **问题描述**: 
  - 无显式的 http.Agent/https.Agent 配置
  - 依赖 Node.js 默认连接池
  - 无 maxSockets 或 maxConnections 配置
  - 高吞吐场景可能出问题
- **建议修复**: 
  1. 添加 http Agent 配置
  2. 文档化连接池大小
  3. 监控连接使用情况

#### 17. [序列化] [中严重] [合同版本硬编码为字符串字面量]
- **文件/路径**: `src/platform/contracts/inter-plane-contract-gateway.ts:184`
- **问题描述**: 
  - `schemaVersion: "v4.3"` 硬编码
  - 难以追踪合同版本演变
- **建议修复**: 
  1. 提取到类型化常量或 enum
  2. 集中管理合同版本
  3. 添加版本兼容性检查

#### 18. [序列化] [低严重] [Timestamp = string 丢失精度]
- **文件/路径**: `src/platform/contracts/types/domain/primitives.ts:10`
- **问题描述**: 
  - `type Timestamp = string` 无类型精度
  - ISO 8601 字符串无时区信息
- **建议修复**: 
  1. 使用 Date 对象或 branded string 类型
  2. 明确时区处理约定
  3. 文档化时间戳语义

#### 19. [源代码] [中严重] [29处非空断言 !.]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - 使用 `!.` 断言非空
  - 可能导致运行时错误
- **建议修复**: 
  1. 使用可选链 `?.` 或空值合并 `??`
  2. 添加适当的 null 检查
  3. 减少非空断言使用

#### 20. [API] [中严重] [API 路由 schema 版本硬编码]
- **文件/路径**: `src/platform/five-plane-interface/api/federation-routing-service.ts`
- **问题描述**: 
  - Zod schema 验证存在
  - 但 schema 版本未与 contract 版本同步
- **建议修复**: 
  1. 从 contract 版本派生 schema 版本
  2. 添加版本一致性检查
  3. 文档化版本关系

### 总结

本次补充Review（第十五轮 - 构建测试与类型系统Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. 无 TypeScript 增量编译缓存（每次完整重建9.6秒）
2. 76个 npm scripts 大部分重复构建
3. 168处 any 类型使用（绕过类型安全）
4. 38处 @ts-ignore 指令
5. ExecutionRecord 类型不匹配（14个文件）
6. 178处直接 process.env 变更无抽象
7. ScopedExternalAccessSandbox performHttpRequest 无超时
8. OIDC 服务 fetch 调用无超时
9. plugin-runtime-child 替换 globalThis.fetch 为 no-op

**中优先级**:
1. 30+处 as unknown as 双类型转换
2. rimraf extraneous 未声明
3. 21,998处 unlinkSync 无集中清理
4. 无统一 mocking 框架
5. Singleton 状态重置未标准化
6. 无显式 http.Agent 配置
7. 合同版本硬编码为字符串
8. Timestamp = string 丢失精度
9. 非空断言使用

**低优先级**:
1. 无分支覆盖率要求
2. API 路由 schema 版本未同步

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 72 | 87 | 39 | 198 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **109** | **182** | **85** | **376** |

### 优先修复建议

**立即处理 (本周内)**:
1. 添加 TypeScript 增量编译 (`incremental: true`)
2. 重构 npm scripts 避免重复构建
3. 减少 any 类型使用到 <50
4. 移除所有 @ts-ignore 指令
5. 修复 ExecutionRecord 类型不匹配
6. 添加 fetch 超时到 OIDC 和 sandbox

**短期内处理 (本月内)**:
1. 创建集中的测试清理 utility
2. 建立统一的 mocking 框架
3. 标准化 singleton 重置机制
4. 添加 http.Agent 连接池配置
5. 提取合同版本到类型化常量

**长期规划**:
1. 实现完整的类型安全（无 any）
2. 建立测试基础设施标准
3. 实现构建缓存优化
4. 完善网络层超时和重试
5. 建立覆盖率门禁标准

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十二轮 - 错误处理与多租户安全Review）

### 发现的新问题

#### 1. [安全] [严重] [listQuotas() 返回所有租户配额 - 信息泄露]
- **文件/路径**: `src/platform/five-plane-control-plane/tenant/tenant-execution-isolation-service.ts:222-234`
- **问题描述**: 
  ```typescript
  listQuotas(tenantId?: string): TenantQuota[] {
    if (tenantId) {
      return this.db.connection.prepare(`SELECT * FROM tenant_quotas WHERE tenant_id = ?`).all(tenantId);
    }
    // WITHOUT tenantId: 返回所有租户的配额！
    return this.db.connection.prepare(`SELECT * FROM tenant_quotas`).all();
  }
  ```
  - 当 tenantId 为空时返回所有租户的配额
  - 未经授权的信息泄露
- **建议修复**: 
  1. 要求调用者必须提供 tenantId
  2. 添加权限验证确保只能查看自己的配额
  3. 审计日志记录所有配额访问

#### 2. [安全] [高严重] [assertTaskTenantAccess() 返回404而非403 - 资源存在泄露]
- **文件/路径**: `src/platform/five-plane-interface/api/utils.ts:146-148`
- **问题描述**: 
  ```typescript
  if (resourceTenantId !== principal.tenantId) {
    throw new ApiError(404, notFoundCode, notFoundMessage);  // 应该是403
  }
  ```
  - 跨租户访问返回404而非403
  - 泄露了资源存在但属于其他租户的信息
  - 攻击者可检测有效task ID
- **建议修复**: 
  1. 改为返回403 Forbidden
  2. 不要泄露资源是否存在

#### 3. [安全] [高严重] [crossTenantRequest 标志由调用者控制]
- **文件/路径**: `src/platform/five-plane-interface/org-routing/index.ts:163`
- **问题描述**: 
  - `routeOrgBudget()` 接受 `crossTenantRequest?: boolean` 参数
  - 该标志由调用者设置，非系统派生
  - 攻击者可能操纵此标志绕过跨租户限制
- **建议修复**: 
  1. 标志应由系统状态派生，不接受调用者输入
  2. 添加审计日志追踪跨租户请求
  3. 实现零信任跨租户访问模型

#### 4. [源代码] [高严重] [single-task-happy-path 无重试机制]
- **文件/路径**: `src/platform/five-plane-execution/execution-engine/single-task-happy-path.ts:336-337`
- **问题描述**: 
  - `maxRetries: 0` 和 `retryBackoff: "none"`
  - 瞬态失败无自动重试
  - LLM调用失败时无fallback到备用provider
- **建议修复**: 
  1. 启用非关键执行的重试
  2. 实现LLM provider fallback链（Anthropic → OpenAI → MiniMax）
  3. 使用domain baseline catalog的primary/fallback模型偏好

#### 5. [源代码] [高严重] [LLM调用无熔断器保护]
- **文件/路径**: `src/platform/five-plane-execution/execution-engine/model-call-provider.ts`
- **问题描述**: 
  - 熔断器只存在于 channel-gateway 和 call-governance
  - 直接LLM provider调用无熔断器
  - provider降级时无法快速失败
- **建议修复**: 
  1. 在model-call-provider.ts添加熔断器
  2. 当provider失败率超阈值时快速失败
  3. 自动切换到备用provider

#### 6. [安全] [高严重] [分布式限流器 bypass - 实例间不共享]
- **文件/路径**: `src/platform/five-plane-interface/ingress/distributed-rate-limiter.ts:33`
- **问题描述**: 
  - Redis未配置时使用本地Map
  - 每个实例维护自己的localEntries
  - 攻击者可通过切换实例绕过限流
- **建议修复**: 
  1. 要求Redis配置用于生产
  2. 或使用Redis作为所有实例的共享后端
  3. 检测并警告未配置Redis的实例

#### 7. [配置] [中严重] [Soft quota (log_only) 实际不限制]
- **文件/路径**: `src/platform/five-plane-control-plane/tenant/tenant-execution-isolation-service.ts:448`
- **问题描述**: 
  ```typescript
  allowed: quota.enforcementAction === "log_only",  // log_only时返回true
  ```
  - soft quota只记录日志，不实际阻止
  - 配额形同虚设
- **建议修复**: 
  1. 明确区分"监控模式"和"强制模式"
  2. 对于硬配额必须阻止
  3. 文档化quota类型的实际行为

#### 8. [配置] [中严重] [HTTP层无限流头 - 客户端无法获知限制]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/response-hardening.ts`
- **问题描述**: 
  - 未返回 X-RateLimit-* 头
  - 只在429响应时返回retry-after-ms
  - 客户端无法主动管理请求速率
- **建议修复**: 
  1. 在所有响应添加 X-RateLimit-Limit, X-RateLimit-Remaining, X-RateLimit-Reset
  2. 使用标准头名称
  3. 遵循RFC 6585

#### 9. [配置] [中严重] [HTTP层无限流 - 可选且仅按IP]
- **文件/路径**: `src/platform/five-plane-interface/api/http-api-server.ts:334-351`
- **问题描述**: 
  - 限流仅在 `this.rateLimiter != null` 时生效
  - 限流key为 `${clientIp}:${endpoint}` 仅按IP
  - 无per-tenant/per-principal enforcement
- **建议修复**: 
  1. 默认启用限流
  2. 添加per-tenant限流key
  3. 确保所有环境默认配置限流

#### 10. [安全] [中严重] [processRuleMode 可能未强制执行]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/plugin-runtime-host.ts`
- **问题描述**: 
  - `SandboxPolicy.processRuleMode` 设置为 "allow" 或 "deny"
  - 但未找到实际阻止基于此策略生成进程的实际执行代码
  - `--allow-child-process` 标志仅在 sandboxed_process 隔离模式应用
- **建议修复**: 
  1. 验证processRuleMode实际被强制执行
  2. 确保所有隔离模式正确处理
  3. 添加测试验证进程创建被阻止

#### 11. [安全] [中严重] [容器模板替换未验证]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/plugin-runtime-host.ts:751`
- **问题描述**: 
  ```typescript
  rendered = rendered.split(placeholder).join(value);
  ```
  - 简单字符串替换
  - 虽然validateContainerLaunchPluginId()阻止\0和引号
  - 但模板替换本身未验证
- **建议修复**: 
  1. 添加输入验证防止注入
  2. 使用更安全的模板引擎
  3. 验证所有占位符被替换

#### 12. [安全] [中严重] [Adapter执行绕过沙箱]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/adapter-executor.ts`
- **问题描述**: 
  - Adapter执行REST/grpc/MQ调用不经过ScopedExternalAccessSandbox
  - 有自己的allowedDomains配置但非集中管理
  - 可能发起未限制的出站请求
- **建议修复**: 
  1. Adapter执行经过集中沙箱
  2. 统一external access策略
  3. 添加adapter出站请求审计

#### 13. [安全] [中严重] [null principal.tenantId 时静默放行]
- **文件/路径**: `src/platform/five-plane-interface/api/utils.ts:143-145`
- **问题描述**: 
  ```typescript
  if (principal.tenantId == null) {
    return;  // 静默放行
  }
  ```
  - 如果API网关允许无principal认证的请求通过
  - 可能授予跨租户访问权限
- **建议修复**: 
  1. 要求所有API请求有有效principal
  2. null tenantId应拒绝而非静默放行
  3. 添加审计日志

#### 14. [源代码] [中严重] [无LLM provider fallback链]
- **文件/路径**: `src/platform/five-plane-execution/execution-engine/model-call-provider.ts`
- **问题描述**: 
  - 主provider失败时无自动切换
  - domain-baseline-catalog.ts定义了primary/fallback模型偏好但未使用
  - 第一个provider失败即导致执行失败
- **建议修复**: 
  1. 实现provider fallback链
  2. 使用domain baseline的primary/fallback配置
  3. 按优先级尝试直到成功或全部失败

#### 15. [源代码] [中严重] [超时值硬编码]
- **文件/路径**: 多个文件
- **问题描述**: 
  - Effect buffer有固定超时reject
  - Channel gateway的requestTimeoutMs虽可配置但有max 30s限制
  - 无全局请求超时中间件
- **建议修复**: 
  1. 集中超时配置
  2. per-environment可配置
  3. 添加全局超时中间件

#### 16. [数据库] [中严重] [无down migration - 回滚不支持]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/sqlite/migration-runner.ts:45-47`
- **问题描述**: 
  - `rollbackSupported = false`
  - 每个migration只有 `downSql` 占位符
  - schema migration无法回滚
- **建议修复**: 
  1. 文档化回滚限制
  2. 在变更前创建完整备份
  3. 使用blue-green部署减少回滚需求

#### 17. [数据库] [中严重] [Migration 44特殊处理 - 表创建重复]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/sqlite/sqlite-migration-runtime-part3.ts`
- **问题描述**: 
  - Migration 43创建harness_runs表
  - Migration 44再次创建同名表（不同schema）
  - 使用applyCompatibleColumnMigrationIfKnown特殊处理
- **建议修复**: 
  1. 消除重复表创建
  2. 使用ALTER TABLE而非CREATE TABLE
  3. 简化migration逻辑

#### 18. [配置] [低严重] [Provider限流头未转发给客户端]
- **文件/路径**: `src/platform/model-gateway/provider-registry/base-chat-provider.ts:153`
- **问题描述**: 
  - 系统读取provider的ratelimitResetHeaderNames
  - 但不转发给客户端
  - 客户端无法知道provider限流状态
- **建议修复**: 
  1. 转发Provider的限流头
  2. 或添加应用层限流信息
  3. 帮助客户端优化请求

#### 19. [安全] [低严重] [Browser evaluate 接受任意脚本]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/browser-executor.ts:326`
- **问题描述**: 
  - `evaluate` action接受script参数
  - 在浏览器上下文运行（模拟）
  - 如浏览器上下文未正确沙箱化可能是向量
- **建议修复**: 
  1. 验证浏览器上下文正确沙箱化
  2. 消毒或限制script内容
  3. 记录所有evaluate调用

#### 20. [源代码] [低严重] [无全局错误边界包装器]
- **文件/路径**: `src/platform/five-plane-execution/execution-engine/`
- **问题描述**: 
  - 执行引擎缺少unhandled promise rejection的全局处理
  - single-task-happy-path的LLM fallback失败时错误向上传播
- **建议修复**: 
  1. 在执行引擎添加错误边界包装
  2. 统一错误处理模式
  3. 确保所有错误被捕获和记录

### 总结

本次补充Review（第十二轮 - 错误处理与多租户安全Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. listQuotas()返回所有租户配额（信息泄露）
2. assertTaskTenantAccess()返回404泄露资源存在
3. crossTenantRequest标志可被操纵
4. single-task-happy-path无重试（maxRetries=0）
5. LLM调用无熔断器保护
6. 分布式限流器可被bypass（实例间不共享）

**中优先级**:
1. Soft quota实际不阻止
2. 无X-RateLimit-*响应头
3. HTTP层限流可选且仅按IP
4. processRuleMode可能未强制
5. 容器模板替换未验证
6. Adapter执行绕过沙箱
7. null principal静默放行
8. 无LLM provider fallback链
9. 超时值硬编码
10. 无down migration
11. Migration 44特殊处理

**低优先级**:
1. Provider限流头未转发
2. Browser evaluate接受任意脚本
3. 无全局错误边界

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 60 | 68 | 31 | 159 |
| 测试 | 8 | 14 | 5 | 27 |
| 配置 | 8 | 35 | 21 | 64 |
| 安全 | 12 | 13 | 3 | 28 |
| 文档 | 2 | 7 | 3 | 12 |
| UI | 0 | 3 | 3 | 6 |
| 部署 | 1 | 10 | 6 | 17 |
| **合计** | **91** | **150** | **72** | **313** |

### 优先修复建议

**立即处理 (本周内)**:
1. 修复listQuotas()信息泄露漏洞
2. 修正assertTaskTenantAccess()返回403
3. 移除crossTenantRequest调用者控制
4. 启用执行引擎重试机制
5. 添加LLM provider熔断器

**短期内处理 (本月内)**:
1. 修复分布式限流器bypass问题
2. 添加X-RateLimit-*响应头
3. 启用默认HTTP限流（per-tenant）
4. 实现LLM provider fallback链
5. 验证并强制processRuleMode

**长期规划**:
1. 建立完整的限流和配额体系
2. 实现插件系统安全审计
3. 建立多租户安全测试
4. 完善错误处理和恢复机制
5. 统一超时和熔断配置

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十五轮 - 构建测试与类型系统Review）

### 发现的新问题

#### 1. [构建] [高严重] [无 TypeScript 增量编译缓存]
- **文件/路径**: `tsconfig.json`
- **问题描述**: 
  - 无 `incremental: true` 配置
  - 无 `tsbuildinfo` 文件
  - 每次构建是完整重建（9.6秒）
  - TypeScript 无法使用增量编译
- **建议修复**: 
  1. 在 tsconfig.json 添加 `"incremental": true`
  2. 添加 `"tsbuildinfo": ".tsbuildinfo"` 到 .gitignore
  3. 实现项目引用以支持并行构建

#### 2. [构建] [高严重] [76个 npm scripts 大部分重复构建]
- **文件/路径**: `package.json`
- **问题描述**: 
  - 40+ CLI scripts 都运行 `npm run build && node ...`
  - 每个脚本前都执行完整重建
  - 无法直接运行预构建的 CLI
  - 20+ stable:* 脚本遵循相同模式
- **建议修复**: 
  1. 创建 `build:cli` 脚本构建一次
  2. 让 CLI 脚本使用预构建的 dist 文件
  3. 添加 npm run dev 或类似的无需重建的执行方式

#### 3. [源代码] [高严重] [168处 any 类型使用]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - `stable-runner-factory.ts`: `opts: any` -> `any`
  - `billing-service-async.ts`: `input: any` 方法签名
  - 重试/超时工具使用 `(...args: any[]) => Promise<any>`
  - 绕过类型安全
- **建议修复**: 
  1. 使用泛型替代 any
  2. 替换为具体的输入类型
  3. 添加 ESLint 规则禁止 any

#### 4. [源代码] [高严重] [38处 @ts-ignore 指令]
- **文件/路径**: 多个文件
- **问题描述**: 
  - 14个 stability 相关文件有 `@ts-ignore ExecutionRecord type mismatch`
  - 5个 harness-sdk 文件有 `Partial<HarnessRun> doesn't have all required properties`
  - 2个文件有 `exactOptionalPropertyTypes` 问题
- **建议修复**: 
  1. 修复 ExecutionRecord 类型不匹配
  2. 完成 HarnessRun 类型的必需属性
  3. 移除 @ts-ignore 使用

#### 5. [源代码] [高严重] [ExecutionRecord 类型不匹配]
- **文件/路径**: `src/platform/stability/stable-evidence-bundle-support.ts:783` 等14个文件
- **问题描述**: 
  - 插入 execution record 时 TypeScript 拒绝类型
  - `ExecutionRecord` 接口要求新字段但 insert 对象缺失
  - 或 store 的 insert 方法接受不同类型
- **建议修复**: 
  1. 对齐 ExecutionRecord 接口和 store 类型
  2. 添加缺失字段到 insert 对象
  3. 或调整 store 的 insert 方法类型签名

#### 6. [源代码] [中严重] [30+处 as unknown as 双类型转换]
- **文件/路径**: `division-loader.ts`, `domain-baseline-catalog.ts`, `harness-sdk`
- **问题描述**: 
  - 完全绕过类型安全
  - 通常是类型架构问题的信号
- **建议修复**: 
  1. 修复上游类型问题
  2. 使用更具体的类型断言
  3. 重构类型层次结构

#### 7. [构建] [中严重] [rimraf  extraneous - 未在 package.json 声明]
- **文件/路径**: `node_modules/rimraf`
- **问题描述**: 
  - rimraf@6.1.3 存在于 node_modules 但未在 package.json 声明
  - 可能导致依赖问题
- **建议修复**: 
  1. 添加到 dependencies 或 devDependencies
  2. 或移除并使用其他清理方式

#### 8. [测试] [高严重] [178处直接 process.env 变更无抽象]
- **文件/路径**: `tests/unit/domains/registry/plugin-spi-registry.test.ts:640-687`
- **问题描述**: 
  - 直接读取/写入 process.env
  - 在 beforeEach/afterEach 中手动保存/恢复
  - 无 helper 抽象
- **建议修复**: 
  1. 创建 env helper 函数
  2. 统一保存和恢复模式
  3. 添加测试隔离验证

#### 9. [测试] [高严重] [21,998处 unlinkSync 调用无集中清理]
- **文件/路径**: 多个测试文件
- **问题描述**: 
  - 每个测试重新实现清理逻辑
  - 无集中清理 utility
  - 可能导致 EBUSY (Windows) 或并发访问问题
- **建议修复**: 
  1. 创建集中的测试清理 utility
  2. 使用 afterEach 确保清理
  3. 考虑使用临时目录

#### 10. [测试] [中严重] [无统一 mocking 框架]
- **文件/路径**: `tests/` 多个目录
- **问题描述**: 
  - 10,579 处 vi.fn, jest.mock, sinon 等调用
  - 无统一的 mocking 框架可见
  - 模式不一致
- **建议修复**: 
  1. 建立统一的 mock factory
  2. 标准化 mock 模式
  3. 添加 mocking 最佳实践文档

#### 11. [测试] [中严重] [Singleton 状态重置未标准化]
- **文件/路径**: `tests/unit/platform/five-plane-execution/dispatcher.test.ts`
- **问题描述**: 
  - 测试通过特定函数重置 singletons
  - 每个测试重复自己的 reset 逻辑
  - 无统一的 resetAllSingletons()
- **建议修复**: 
  1. 创建统一的 singleton reset 机制
  2. 在 afterEach 中自动调用
  3. 文档化 singleton 重置要求

#### 12. [测试] [低严重] [无分支覆盖率要求]
- **文件/路径**: `.c8rc.json`
- **问题描述**: 
  - `"100": false` - 无覆盖率要求
  - 只测量源代码，不测量测试文件
- **建议修复**: 
  1. 添加分支覆盖率阈值
  2. 在 CI 中强制执行覆盖率 gate
  3. 关注关键代码路径

#### 13. [网络] [高严重] [ScopedExternalAccessSandbox performHttpRequest 无超时]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/scoped-external-access-sandbox.ts:289`
- **问题描述**: 
  - fetch 调用无 timeout 参数
  - 挂起的请求可能永久阻塞
- **建议修复**: 
  1. 添加 AbortController timeout
  2. 使用标准的超时模式
  3. 记录超时配置

#### 14. [网络] [高严重] [OIDC 服务 fetch 调用无超时]
- **文件/路径**: `src/org-governance/sso-scim/oidc/oidc-service.ts:565,272`
- **问题描述**: 
  - OIDC token 交换无 retry logic
  - 无 timeout/abort signal
  - 认证流程可能挂起
- **建议修复**: 
  1. 添加 fetch timeout
  2. 添加 retry logic
  3. 使用标准的超时配置

#### 15. [网络] [高严重] [plugin-runtime-child 替换 globalThis.fetch 为 no-op]
- **文件/路径**: `src/domains/registry/plugin-runtime-child.ts:112`
- **问题描述**: 
  ```typescript
  globalThis.fetch = (async () => {}) as unknown as typeof globalThis.fetch;
  ```
  - 插件上下文中完全替换 fetch 为空函数
  - 插件的所有出站 HTTP 请求都会失败
  - 看起来是有意为之但可能造成问题
- **建议修复**: 
  1. 确认这是有意行为
  2. 文档化 fetch 替换行为
  3. 确保不会意外影响其他代码

#### 16. [网络] [中严重] [无显式 http.Agent 配置]
- **文件/路径**: 全局
- **问题描述**: 
  - 无显式的 http.Agent/https.Agent 配置
  - 依赖 Node.js 默认连接池
  - 无 maxSockets 或 maxConnections 配置
  - 高吞吐场景可能出问题
- **建议修复**: 
  1. 添加 http Agent 配置
  2. 文档化连接池大小
  3. 监控连接使用情况

#### 17. [序列化] [中严重] [合同版本硬编码为字符串字面量]
- **文件/路径**: `src/platform/contracts/inter-plane-contract-gateway.ts:184`
- **问题描述**: 
  - `schemaVersion: "v4.3"` 硬编码
  - 难以追踪合同版本演变
- **建议修复**: 
  1. 提取到类型化常量或 enum
  2. 集中管理合同版本
  3. 添加版本兼容性检查

#### 18. [序列化] [低严重] [Timestamp = string 丢失精度]
- **文件/路径**: `src/platform/contracts/types/domain/primitives.ts:10`
- **问题描述**: 
  - `type Timestamp = string` 无类型精度
  - ISO 8601 字符串无时区信息
- **建议修复**: 
  1. 使用 Date 对象或 branded string 类型
  2. 明确时区处理约定
  3. 文档化时间戳语义

#### 19. [源代码] [中严重] [29处非空断言 !.]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - 使用 `!.` 断言非空
  - 可能导致运行时错误
- **建议修复**: 
  1. 使用可选链 `?.` 或空值合并 `??`
  2. 添加适当的 null 检查
  3. 减少非空断言使用

#### 20. [API] [中严重] [API 路由 schema 版本硬编码]
- **文件/路径**: `src/platform/five-plane-interface/api/federation-routing-service.ts`
- **问题描述**: 
  - Zod schema 验证存在
  - 但 schema 版本未与 contract 版本同步
- **建议修复**: 
  1. 从 contract 版本派生 schema 版本
  2. 添加版本一致性检查
  3. 文档化版本关系

### 总结

本次补充Review（第十五轮 - 构建测试与类型系统Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. 无 TypeScript 增量编译缓存（每次完整重建9.6秒）
2. 76个 npm scripts 大部分重复构建
3. 168处 any 类型使用（绕过类型安全）
4. 38处 @ts-ignore 指令
5. ExecutionRecord 类型不匹配（14个文件）
6. 178处直接 process.env 变更无抽象
7. ScopedExternalAccessSandbox performHttpRequest 无超时
8. OIDC 服务 fetch 调用无超时
9. plugin-runtime-child 替换 globalThis.fetch 为 no-op

**中优先级**:
1. 30+处 as unknown as 双类型转换
2. rimraf extraneous 未声明
3. 21,998处 unlinkSync 无集中清理
4. 无统一 mocking 框架
5. Singleton 状态重置未标准化
6. 无显式 http.Agent 配置
7. 合同版本硬编码为字符串
8. Timestamp = string 丢失精度
9. 非空断言使用

**低优先级**:
1. 无分支覆盖率要求
2. API 路由 schema 版本未同步

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 72 | 87 | 39 | 198 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **109** | **182** | **85** | **376** |

### 优先修复建议

**立即处理 (本周内)**:
1. 添加 TypeScript 增量编译 (`incremental: true`)
2. 重构 npm scripts 避免重复构建
3. 减少 any 类型使用到 <50
4. 移除所有 @ts-ignore 指令
5. 修复 ExecutionRecord 类型不匹配
6. 添加 fetch 超时到 OIDC 和 sandbox

**短期内处理 (本月内)**:
1. 创建集中的测试清理 utility
2. 建立统一的 mocking 框架
3. 标准化 singleton 重置机制
4. 添加 http.Agent 连接池配置
5. 提取合同版本到类型化常量

**长期规划**:
1. 实现完整的类型安全（无 any）
2. 建立测试基础设施标准
3. 实现构建缓存优化
4. 完善网络层超时和重试
5. 建立覆盖率门禁标准

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十四轮 - 日志事件与工作流Review）

### 发现的新问题

#### 1. [可观测性] [高严重] [StructuredLogEntry 无 requestId 字段]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - StructuredLogEntry 接口无 requestId 字段
  - 只有 correlationId，它回退到 traceId
  - 无法独立追踪单个 HTTP 请求
- **建议修复**: 
  1. 在 StructuredLogEntry 添加 requestId 字段
  2. 在 HTTP 中间件中生成和注入 requestId
  3. 确保 requestId 流经所有日志调用

#### 2. [可观测性] [高严重] [StructuredLogger data 字段无自动清洗]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - 如果开发者传递 `{ password: "..." }` 给 logger.info，密码会被记录
  - 无字段级脱敏机制
  - 依赖开发者手动排除敏感字段
- **建议修复**: 
  1. 添加 OBSERVE_OUTPUT_BLACKLIST 类似的黑名单
  2. 实现 redact/mask/sanitize 工具函数
  3. 在日志传输前自动清洗敏感字段

#### 3. [可观测性] [中严重] [日志中敏感数据无 PII 处理]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - 无 PII 重写工具
  - 开发者必须手动避免记录敏感数据
  - 存在意外记录敏感信息的风险
- **建议修复**: 
  1. 添加 PII 检测和重写工具
  2. 在文档中明确记录敏感字段列表
  3. 添加测试验证敏感数据不被记录

#### 4. [可观测性] [中严重] [59处 console.* 直接调用]
- **文件/路径**: 多个源文件
- **问题描述**: 
  - 应该使用 StructuredLogger
  - console 调用可能不被日志聚合系统收集
  - 难以追踪和关联
- **建议修复**: 
  1. 替换所有 console.* 为 StructuredLogger
  2. 添加 ESLint 规则禁止 console.*
  3. 保留少量允许的 CLI 输出

#### 5. [可观测性] [中严重] [部分 logger.warn 调用缺少结构化数据]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:122`
- **问题描述**: 
  ```typescript
  logger.warn("WebSocket connection rejected: missing subprotocol token")
  ```
  - 只有消息，无 data 对象
  - 难以关联和查询
- **建议修复**: 
  1. 添加 `{ data: { ... } }` 对象
  2. 确保所有日志包含上下文数据
  3. 添加日志审查工具

#### 6. [源代码] [高严重] [Harness while(true) 循环无硬性迭代上限]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:1442`
- **问题描述**: 
  ```typescript
  while (true) {
    // ...
    // 依赖 budget gate 和 guardrail vibration 退出
    // 如果两者都失败，循环永久继续
  }
  ```
  - 依赖 budget gate 和 guardrail vibration 退出
  - 无硬性迭代次数限制
  - 可能导致无限循环
- **建议修复**: 
  1. 添加硬性 maxIterations 上限
  2. 在达到上限时强制退出
  3. 记录详细的迭代统计用于调试

#### 7. [源代码] [高严重] [Oapeflir while(true) 循环可能无限重规划]
- **文件/路径**: `src/platform/five-plane-orchestration/oapeflir/oapeflir-loop-service.ts:355`
- **问题描述**: 
  - 如果 loopReplanDecision.shouldReplan 永不变为 false
  - 重规划永远无法减少计划错误
  - 可能导致无限循环
- **建议修复**: 
  1. 添加重规划次数上限
  2. 在达到上限时强制接受当前结果
  3. 添加重规划质量衰减检测

#### 8. [源代码] [中严重] [Recovery 流程允许终态转换到 paused]
- **文件/路径**: `src/platform/five-plane-execution/runtime-state-machine.ts:105-108`
- **问题描述**: 
  - completed, failed, cancelled, aborted 可转换到 paused 进行恢复
  - 创建恢复路径可能重新进入执行
  - 需要验证此行为的正确性
- **建议修复**: 
  1. 审查 recovery 转换的合法性
  2. 确保不会导致状态不一致
  3. 添加转换前置条件验证

#### 9. [国际化] [高严重] [API 错误消息硬编码英文]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/api-error.ts`
- **问题描述**: 
  ```typescript
  return new ApiError(404, "api.task_not_found", "Task not found.");
  ```
  - 错误消息硬编码英文
  - 无后端 i18n 机制
  - 非英文客户端收到不本地化的错误
- **建议修复**: 
  1. 使用错误码而非硬编码消息
  2. 客户端根据错误码本地化
  3. 或在后端添加 i18n 支持

#### 10. [国际化] [中严重] [UI 硬编码字符串未翻译]
- **文件/路径**: `ui/packages/features/conversation/src/web/index.tsx`
- **问题描述**: 
  - "Build Plan", "Execute", "Messages" 等按钮标签硬编码
  - 不使用 i18n 系统
- **建议修复**: 
  1. 使用 `translateFeatureCopy` 替换硬编码
  2. 确保所有用户可见文本通过 i18n
  3. 添加硬编码字符串检测

#### 11. [国际化] [中严重] [Arabic 目录不完整]
- **文件/路径**: `ui/packages/shared/i18n/src/catalogs/ar-SA.ts`
- **问题描述**: 
  - ar-SA 只有约18条消息
  - en-US 和 zh-CN 有约117条
  - 缺失的 key 返回 key 本身
- **建议修复**: 
  1. 补充 Arabic 翻译
  2. 确保所有 key 都有翻译
  3. 添加翻译完整性测试

#### 12. [国际化] [低严重] [translateFeatureCopy 无回退默认值]
- **文件/路径**: `ui/packages/shared/i18n/`
- **问题描述**: 
  - 如果 feature ID 不在 catalog 中
  - 返回 undefined title/summary
  - 无合理回退
- **建议修复**: 
  1. 添加回退到 key 或默认字符串
  2. 或使用 feature ID 作为显示名
  3. 确保 UI 不显示 undefined

#### 13. [运维] [高严重] [健康检查无 readiness/liveness 区分]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts`
- **问题描述**: 
  - /healthz, /v1/healthz, /health 返回相同报告
  - 无 readiness probe（可处理流量？）
  - 无 liveness probe（系统存活？）
  - canAcceptTraffic() 未通过 HTTP 暴露
- **建议修复**: 
  1. 添加 /ready 和 /live 端点
  2. Readiness 检查依赖项就绪
  3. Liveness 检查进程健康

#### 14. [运维] [高严重] [shutdown 期间健康检查存在竞态]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts`
- **问题描述**: 
  - getHealthReportAsync() 异步检查 dbWritable
  - shutdown 期间数据库连接可能关闭
  - 健康检查仍在传入
- **建议修复**: 
  1. 在 shutdown 时返回 unhealthy
  2. 添加 shutdown 状态标志
  3. 停止接受新的健康检查请求

#### 15. [运维] [高严重] [LeaderElectionService 未与 GracefulShutdown 集成]
- **文件/路径**: `src/platform/five-plane-execution/ha/leader-election-service.ts`
- **问题描述**: 
  - LeaderElectionService.stop() 处理优雅退位
  - 但未在全局 graceful shutdown 中注册
  - 需显式注册为 shutdown handler
- **建议修复**: 
  1. 将 LeaderElectionService 添加到 shutdown handlers
  2. 确保领导者在 shutdown 时让位
  3. 验证关闭顺序正确

#### 16. [运维] [中严重] [addHandler() 无显式排序约定]
- **文件/路径**: `src/platform/five-plane-execution/startup/graceful-shutdown.ts`
- **问题描述**: 
  - 虽然 handlers 逆序执行
  - 但无显式接口声明 handler 依赖关系
  - 关键 handlers（如关闭数据库连接）应最后运行
- **建议修复**: 
  1. 添加优先级或阶段参数
  2. 文档化关键 handler 顺序要求
  3. 添加顺序验证测试

#### 17. [运维] [中严重] [unref'd timers 可能不触发]
- **文件/路径**: `src/platform/five-plane-execution/startup/process-error-handlers.ts:106`
- **问题描述**: 
  - `setTimeout(...).unref()` 不保持事件循环活跃
  - 如果主线程空闲，强制退出可能不触发
- **建议修复**: 
  1. 移除 .unref() 除非确实需要
  2. 或确保有其他方式保持事件循环
  3. 添加超时触发验证测试

#### 18. [运维] [中严重] [StartupConsistencyChecker 阻塞流量但不暴露状态]
- **文件/路径**: `src/platform/five-plane-startup-plan.ts`
- **问题描述**: 
  - canAcceptTraffic() 在 P0 问题存在时返回 false
  - 但 _trafficBlocked 是内部状态
  - 负载均衡器访问 /healthz 仍会得到 "ok"
- **建议修复**: 
  1. 在健康报告中暴露 trafficBlocked 状态
  2. 或添加单独的健康端点指示就绪状态
  3. 确保负载均衡器在阻塞时不路由流量

#### 19. [事件系统] [中严重] [TypedEventBus vs EventEmitter 混用]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - 项目同时使用 TypedEventBus 和原生 EventEmitter
  - TypedEventBus 应该是标准
  - 混用可能导致类型安全问题
- **建议修复**: 
  1. 创建迁移计划统一使用 TypedEventBus
  2. 添加 ESLint 规则禁止原生 EventEmitter
  3. 在 CI 中检测混用情况

#### 20. [源代码] [低严重] [无事件排序保证]
- **文件/路径**: `src/platform/five-plane-state-evidence/events/`
- **问题描述**: 
  - 无明确的事件排序保证文档
  - 事件处理可能乱序
  - 依赖方需要处理乱序
- **建议修复**: 
  1. 文档化事件排序语义
  2. 如果需要排序，实现序列号机制
  3. 在事件处理中考虑乱序情况

### 总结

本次补充Review（第十四轮 - 日志事件与工作流Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. StructuredLogEntry 无 requestId 字段
2. StructuredLogger data 字段无自动清洗（敏感数据风险）
3. Harness while(true) 循环无硬性迭代上限
4. Oapeflir while(true) 循环可能无限重规划
5. API 错误消息硬编码英文（无 i18n）
6. 健康检查无 readiness/liveness 区分
7. shutdown 期间健康检查存在竞态
8. LeaderElectionService 未与 GracefulShutdown 集成

**中优先级**:
1. 日志中敏感数据无 PII 处理
2. 59处 console.* 直接调用
3. 部分 logger.warn 调用缺少结构化数据
4. Recovery 流程允许终态转换到 paused
5. UI 硬编码字符串未翻译
6. Arabic 目录不完整
7. addHandler() 无显式排序约定
8. unref'd timers 可能不触发
9. StartupConsistencyChecker 阻塞流量但不暴露状态
10. TypedEventBus vs EventEmitter 混用

**低优先级**:
1. translateFeatureCopy 无回退默认值
2. 无事件排序保证

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 68 | 80 | 37 | 185 |
| 测试 | 8 | 14 | 5 | 27 |
| 配置 | 10 | 38 | 24 | 72 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **103** | **171** | **82** | **356** |

### 优先修复建议

**立即处理 (本周内)**:
1. 在 StructuredLogEntry 添加 requestId 字段
2. 实现日志敏感字段自动清洗
3. 为工作流循环添加硬性迭代上限
4. 添加 readiness/liveness 健康检查端点
5. 将 LeaderElectionService 集成到 GracefulShutdown
6. 实现 API 错误消息 i18n 支持

**短期内处理 (本月内)**:
1. 替换所有 console.* 为 StructuredLogger
2. 补充 Arabic i18n 目录
3. 修复 UI 硬编码字符串
4. 统一 TypedEventBus 替代 EventEmitter
5. 解决 shutdown 竞态条件
6. 添加事件排序保证文档

**长期规划**:
1. 建立完整的 PII 处理框架
2. 实现日志完整性验证
3. 建立工作流安全循环检测
4. 完善健康检查和就绪探测
5. 建立国际化完整测试

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十五轮 - 构建测试与类型系统Review）

### 发现的新问题

#### 1. [构建] [高严重] [无 TypeScript 增量编译缓存]
- **文件/路径**: `tsconfig.json`
- **问题描述**: 
  - 无 `incremental: true` 配置
  - 无 `tsbuildinfo` 文件
  - 每次构建是完整重建（9.6秒）
  - TypeScript 无法使用增量编译
- **建议修复**: 
  1. 在 tsconfig.json 添加 `"incremental": true`
  2. 添加 `"tsbuildinfo": ".tsbuildinfo"` 到 .gitignore
  3. 实现项目引用以支持并行构建

#### 2. [构建] [高严重] [76个 npm scripts 大部分重复构建]
- **文件/路径**: `package.json`
- **问题描述**: 
  - 40+ CLI scripts 都运行 `npm run build && node ...`
  - 每个脚本前都执行完整重建
  - 无法直接运行预构建的 CLI
  - 20+ stable:* 脚本遵循相同模式
- **建议修复**: 
  1. 创建 `build:cli` 脚本构建一次
  2. 让 CLI 脚本使用预构建的 dist 文件
  3. 添加 npm run dev 或类似的无需重建的执行方式

#### 3. [源代码] [高严重] [168处 any 类型使用]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - `stable-runner-factory.ts`: `opts: any` -> `any`
  - `billing-service-async.ts`: `input: any` 方法签名
  - 重试/超时工具使用 `(...args: any[]) => Promise<any>`
  - 绕过类型安全
- **建议修复**: 
  1. 使用泛型替代 any
  2. 替换为具体的输入类型
  3. 添加 ESLint 规则禁止 any

#### 4. [源代码] [高严重] [38处 @ts-ignore 指令]
- **文件/路径**: 多个文件
- **问题描述**: 
  - 14个 stability 相关文件有 `@ts-ignore ExecutionRecord type mismatch`
  - 5个 harness-sdk 文件有 `Partial<HarnessRun> doesn't have all required properties`
  - 2个文件有 `exactOptionalPropertyTypes` 问题
- **建议修复**: 
  1. 修复 ExecutionRecord 类型不匹配
  2. 完成 HarnessRun 类型的必需属性
  3. 移除 @ts-ignore 使用

#### 5. [源代码] [高严重] [ExecutionRecord 类型不匹配]
- **文件/路径**: `src/platform/stability/stable-evidence-bundle-support.ts:783` 等14个文件
- **问题描述**: 
  - 插入 execution record 时 TypeScript 拒绝类型
  - `ExecutionRecord` 接口要求新字段但 insert 对象缺失
  - 或 store 的 insert 方法接受不同类型
- **建议修复**: 
  1. 对齐 ExecutionRecord 接口和 store 类型
  2. 添加缺失字段到 insert 对象
  3. 或调整 store 的 insert 方法类型签名

#### 6. [源代码] [中严重] [30+处 as unknown as 双类型转换]
- **文件/路径**: `division-loader.ts`, `domain-baseline-catalog.ts`, `harness-sdk`
- **问题描述**: 
  - 完全绕过类型安全
  - 通常是类型架构问题的信号
- **建议修复**: 
  1. 修复上游类型问题
  2. 使用更具体的类型断言
  3. 重构类型层次结构

#### 7. [构建] [中严重] [rimraf  extraneous - 未在 package.json 声明]
- **文件/路径**: `node_modules/rimraf`
- **问题描述**: 
  - rimraf@6.1.3 存在于 node_modules 但未在 package.json 声明
  - 可能导致依赖问题
- **建议修复**: 
  1. 添加到 dependencies 或 devDependencies
  2. 或移除并使用其他清理方式

#### 8. [测试] [高严重] [178处直接 process.env 变更无抽象]
- **文件/路径**: `tests/unit/domains/registry/plugin-spi-registry.test.ts:640-687`
- **问题描述**: 
  - 直接读取/写入 process.env
  - 在 beforeEach/afterEach 中手动保存/恢复
  - 无 helper 抽象
- **建议修复**: 
  1. 创建 env helper 函数
  2. 统一保存和恢复模式
  3. 添加测试隔离验证

#### 9. [测试] [高严重] [21,998处 unlinkSync 调用无集中清理]
- **文件/路径**: 多个测试文件
- **问题描述**: 
  - 每个测试重新实现清理逻辑
  - 无集中清理 utility
  - 可能导致 EBUSY (Windows) 或并发访问问题
- **建议修复**: 
  1. 创建集中的测试清理 utility
  2. 使用 afterEach 确保清理
  3. 考虑使用临时目录

#### 10. [测试] [中严重] [无统一 mocking 框架]
- **文件/路径**: `tests/` 多个目录
- **问题描述**: 
  - 10,579 处 vi.fn, jest.mock, sinon 等调用
  - 无统一的 mocking 框架可见
  - 模式不一致
- **建议修复**: 
  1. 建立统一的 mock factory
  2. 标准化 mock 模式
  3. 添加 mocking 最佳实践文档

#### 11. [测试] [中严重] [Singleton 状态重置未标准化]
- **文件/路径**: `tests/unit/platform/five-plane-execution/dispatcher.test.ts`
- **问题描述**: 
  - 测试通过特定函数重置 singletons
  - 每个测试重复自己的 reset 逻辑
  - 无统一的 resetAllSingletons()
- **建议修复**: 
  1. 创建统一的 singleton reset 机制
  2. 在 afterEach 中自动调用
  3. 文档化 singleton 重置要求

#### 12. [测试] [低严重] [无分支覆盖率要求]
- **文件/路径**: `.c8rc.json`
- **问题描述**: 
  - `"100": false` - 无覆盖率要求
  - 只测量源代码，不测量测试文件
- **建议修复**: 
  1. 添加分支覆盖率阈值
  2. 在 CI 中强制执行覆盖率 gate
  3. 关注关键代码路径

#### 13. [网络] [高严重] [ScopedExternalAccessSandbox performHttpRequest 无超时]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/scoped-external-access-sandbox.ts:289`
- **问题描述**: 
  - fetch 调用无 timeout 参数
  - 挂起的请求可能永久阻塞
- **建议修复**: 
  1. 添加 AbortController timeout
  2. 使用标准的超时模式
  3. 记录超时配置

#### 14. [网络] [高严重] [OIDC 服务 fetch 调用无超时]
- **文件/路径**: `src/org-governance/sso-scim/oidc/oidc-service.ts:565,272`
- **问题描述**: 
  - OIDC token 交换无 retry logic
  - 无 timeout/abort signal
  - 认证流程可能挂起
- **建议修复**: 
  1. 添加 fetch timeout
  2. 添加 retry logic
  3. 使用标准的超时配置

#### 15. [网络] [高严重] [plugin-runtime-child 替换 globalThis.fetch 为 no-op]
- **文件/路径**: `src/domains/registry/plugin-runtime-child.ts:112`
- **问题描述**: 
  ```typescript
  globalThis.fetch = (async () => {}) as unknown as typeof globalThis.fetch;
  ```
  - 插件上下文中完全替换 fetch 为空函数
  - 插件的所有出站 HTTP 请求都会失败
  - 看起来是有意为之但可能造成问题
- **建议修复**: 
  1. 确认这是有意行为
  2. 文档化 fetch 替换行为
  3. 确保不会意外影响其他代码

#### 16. [网络] [中严重] [无显式 http.Agent 配置]
- **文件/路径**: 全局
- **问题描述**: 
  - 无显式的 http.Agent/https.Agent 配置
  - 依赖 Node.js 默认连接池
  - 无 maxSockets 或 maxConnections 配置
  - 高吞吐场景可能出问题
- **建议修复**: 
  1. 添加 http Agent 配置
  2. 文档化连接池大小
  3. 监控连接使用情况

#### 17. [序列化] [中严重] [合同版本硬编码为字符串字面量]
- **文件/路径**: `src/platform/contracts/inter-plane-contract-gateway.ts:184`
- **问题描述**: 
  - `schemaVersion: "v4.3"` 硬编码
  - 难以追踪合同版本演变
- **建议修复**: 
  1. 提取到类型化常量或 enum
  2. 集中管理合同版本
  3. 添加版本兼容性检查

#### 18. [序列化] [低严重] [Timestamp = string 丢失精度]
- **文件/路径**: `src/platform/contracts/types/domain/primitives.ts:10`
- **问题描述**: 
  - `type Timestamp = string` 无类型精度
  - ISO 8601 字符串无时区信息
- **建议修复**: 
  1. 使用 Date 对象或 branded string 类型
  2. 明确时区处理约定
  3. 文档化时间戳语义

#### 19. [源代码] [中严重] [29处非空断言 !.]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - 使用 `!.` 断言非空
  - 可能导致运行时错误
- **建议修复**: 
  1. 使用可选链 `?.` 或空值合并 `??`
  2. 添加适当的 null 检查
  3. 减少非空断言使用

#### 20. [API] [中严重] [API 路由 schema 版本硬编码]
- **文件/路径**: `src/platform/five-plane-interface/api/federation-routing-service.ts`
- **问题描述**: 
  - Zod schema 验证存在
  - 但 schema 版本未与 contract 版本同步
- **建议修复**: 
  1. 从 contract 版本派生 schema 版本
  2. 添加版本一致性检查
  3. 文档化版本关系

### 总结

本次补充Review（第十五轮 - 构建测试与类型系统Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. 无 TypeScript 增量编译缓存（每次完整重建9.6秒）
2. 76个 npm scripts 大部分重复构建
3. 168处 any 类型使用（绕过类型安全）
4. 38处 @ts-ignore 指令
5. ExecutionRecord 类型不匹配（14个文件）
6. 178处直接 process.env 变更无抽象
7. ScopedExternalAccessSandbox performHttpRequest 无超时
8. OIDC 服务 fetch 调用无超时
9. plugin-runtime-child 替换 globalThis.fetch 为 no-op

**中优先级**:
1. 30+处 as unknown as 双类型转换
2. rimraf extraneous 未声明
3. 21,998处 unlinkSync 无集中清理
4. 无统一 mocking 框架
5. Singleton 状态重置未标准化
6. 无显式 http.Agent 配置
7. 合同版本硬编码为字符串
8. Timestamp = string 丢失精度
9. 非空断言使用

**低优先级**:
1. 无分支覆盖率要求
2. API 路由 schema 版本未同步

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 72 | 87 | 39 | 198 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **109** | **182** | **85** | **376** |

### 优先修复建议

**立即处理 (本周内)**:
1. 添加 TypeScript 增量编译 (`incremental: true`)
2. 重构 npm scripts 避免重复构建
3. 减少 any 类型使用到 <50
4. 移除所有 @ts-ignore 指令
5. 修复 ExecutionRecord 类型不匹配
6. 添加 fetch 超时到 OIDC 和 sandbox

**短期内处理 (本月内)**:
1. 创建集中的测试清理 utility
2. 建立统一的 mocking 框架
3. 标准化 singleton 重置机制
4. 添加 http.Agent 连接池配置
5. 提取合同版本到类型化常量

**长期规划**:
1. 实现完整的类型安全（无 any）
2. 建立测试基础设施标准
3. 实现构建缓存优化
4. 完善网络层超时和重试
5. 建立覆盖率门禁标准

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十三轮 - 缓存会话与实时通信Review）

### 发现的新问题

#### 1. [安全] [严重] [CORS allowedMethods 缺少 PUT/PATCH/DELETE]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/response-hardening.ts:12-14`
- **问题描述**: 
  ```typescript
  export const DEFAULT_CORS_CONFIG: CorsConfig = {
    allowedMethods: ["GET", "POST", "OPTIONS"],  // 缺少 PUT, PATCH, DELETE
  ```
  - 浏览器 CORS preflight 会拒绝 PUT/PATCH/DELETE 请求
  - 但路由中有 PATCH /v1/tasks/:id, DELETE endpoints
- **建议修复**: 
  1. 添加 PUT, PATCH, DELETE 到 allowedMethods
  2. 确保 CORS 配置与应用路由方法一致
  3. 添加测试验证所有 HTTP 方法的 CORS preflight

#### 2. [安全] [严重] [Mission Routes 错误响应包装不当]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/mission-routes.ts:101,118-124`
- **问题描述**: 
  ```typescript
  return buildJsonResponse(ctx.requestId, 404, { error: { code: "MISSION_NOT_FOUND" } });
  ```
  - 使用 buildJsonResponse（成功包装）返回错误
  - 错误被包装在成功信封 `{requestId, data: {error: {...}}}` 中
  - 违反 REST 规范
- **建议修复**: 
  1. 使用 buildJsonErrorResponse 处理错误
  2. 确保所有错误路径使用正确的错误信封
  3. 添加 API 一致性测试

#### 3. [源代码] [高严重] [WebSocketBridge 无最大连接数限制]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:103-107`
- **问题描述**: 
  ```typescript
  this.wss = new WebSocketServer({
    server,
    path: WS_PATH,
    maxPayload: options.maxPayloadBytes ?? DEFAULT_MAX_PAYLOAD_BYTES,
    // 无 maxConnections 限制！
  });
  ```
  - 与 dashboard-websocket-server.ts 的 maxClients: 1000 不同
  - 恶意客户端可耗尽服务器资源
- **建议修复**: 
  1. 添加 maxConnections 限制
  2. 实现连接限制拒绝策略
  3. 添加连接计数指标

#### 4. [源代码] [高严重] [pendingAcks 在断开连接时未清理]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:268-282`
- **问题描述**: 
  - handleDisconnection 清理 subscribedTasks, taskSubscribers, slowConsumers
  - 但 pendingAcks Map 未清理
  - 断开的客户端未确认的消息永久留在内存
- **建议修复**: 
  1. 在 handleDisconnection 中删除所有 pendingAcks 条目
  2. 考虑添加超时机制自动清理未确认消息
  3. 记录清理操作的审计日志

#### 5. [安全] [高严重] [硬编码回退密钥]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/audit-event-integrity.ts:41`
- **问题描述**: 
  ```typescript
  const AUDIT_INTEGRITY_HMAC_KEY = process.env["AA_AUDIT_INTEGRITY_HMAC_KEY"] ?? "audit-integrity-secret-key-32-bytes!";
  ```
  - 占位符密钥用作回退
  - 如果环境变量未设置，使用不安全密钥
- **建议修复**: 
  1. 移除回退值，要求环境变量必须设置
  2. 在启动时验证密钥存在且足够强
  3. 如果密钥缺失导致启动失败

#### 6. [配置] [高严重] [config/runtime/test.json 缺少超时配置]
- **文件/路径**: `config/runtime/test.json`
- **问题描述**: 
  - 只有3个字段，缺少 maxConcurrentTasks, defaultTaskTimeoutMs, defaultStepTimeoutMs
  - 回退到默认值可能不适合测试环境
- **建议修复**: 
  1. 添加完整的 timeout/tuning 配置
  2. 确保测试环境有合理的默认值
  3. 文档化必需的配置字段

#### 7. [配置] [高严重] [prod.json 限制过严 - maxConcurrentTasks=1]
- **文件/路径**: `config/runtime/prod.json`
- **问题描述**: 
  - maxConcurrentTasks: 1（vs dev:8, staging:4, pre-prod:6）
  - defaultTaskTimeoutMs: 120000（vs dev:600000）
  - defaultStepTimeoutMs: 60000（vs dev:180000）
  - 生产环境严重受限，可能影响吞吐量
- **建议修复**: 
  1. 评估并调整生产限制
  2. 与业务需求匹配
  3. 添加生产容量测试

#### 8. [源代码] [高严重] [多个服务存在无界 Map 无驱逐策略]
- **文件/路径**: 多个域服务
- **问题描述**: 
  - `domain-knowledge-schema-service.ts:73-75`: schemas, sourceContent, sourceTimestamps 无限制
  - `domain-eval-framework-service.ts:91-95`: frameworks, qualityAxes 等无限制
  - `domain-recipe-service.ts:67-68`: recipes, versions 无限制
  - `domain-risk-profile-service.ts:51`: profiles 无限制
  - Session Maps (session-management.ts:83-89) 无自动清理
- **建议修复**: 
  1. 为所有 Map 实现 LRU 或 TTL 驱逐策略
  2. 添加后台清理任务
  3. 添加大小监控和告警

#### 9. [源代码] [中严重] [taskEventHistory Map 永不清理]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:91,548-555`
- **问题描述**: 
  - taskEventHistory 限制单任务历史为 200 条
  - 但 Map 本身从不清理
  - 取消订阅的任务历史永久保留
- **建议修复**: 
  1. 实现后台清理无订阅者任务的历史
  2. 添加任务订阅者监控
  3. 考虑使用 WeakMap 替代

#### 10. [配置] [中严重] [安全配置 drift - remoteWorkerRegistration 缺失]
- **文件/路径**: `config/security/dev.json`, `staging.json`, `pre-prod.json`
- **问题描述**: 
  - default.json 有完整的 remoteWorkerRegistration 配置
  - dev/staging/pre-prod 只有 approvalMode
  - prod 有 approvalMode 但无 remoteWorkerRegistration
  - 安全配置不一致
- **建议修复**: 
  1. 统一所有环境的 remoteWorkerRegistration
  2. 添加安全配置验证
  3. 确保最低安全基线

#### 11. [API] [中严重] [OpenAPI 端点响应格式不一致]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/utils.ts:207-215`
- **问题描述**: 
  - `/v1/openapi.json` 使用 buildJsonDocumentResponse（无包装）
  - 其他端点使用 buildJsonResponse（{requestId, data} 包装）
  - 客户端体验不一致
- **建议修复**: 
  1. 统一响应信封格式
  2. 文档化响应格式规范
  3. 添加响应格式验证测试

#### 12. [源代码] [中严重] [Redis 队列缺少 idempotency 索引支持]
- **文件/路径**: `src/platform/five-plane-execution/queue/redis-queue-adapter.ts`
- **问题描述**: 
  - SQLite 有部分唯一索引支持 idempotency
  - Redis 实现使用哈希索引但实现不完整
  - 可能导致重复消息
- **建议修复**: 
  1. 完善 Redis idempotency 实现
  2. 添加唯一索引验证
  3. 确保与 SQLite 行为一致

#### 13. [源代码] [中严重] [请求去重中间件使用内存存储]
- **文件/路径**: `src/platform/five-plane-interface/api/middleware/request-deduplication.ts`
- **问题描述**: 
  - 纯内存滑动窗口（Map<DeduplicationKey, DeduplicationEntry[]>）
  - 无外部存储
  - 多实例部署不共享
  - 重启后去重状态丢失
- **建议修复**: 
  1. 使用 Redis 替代内存存储
  2. 支持分布式去重
  3. 持久化去重状态

#### 14. [源代码] [中严重] [Cache 无 stampede 保护]
- **文件/路径**: 多个 cache 实现
- **问题描述**: 
  - MemoryCacheStore, ExperienceCacheService 无锁
  - 缓存未命中时可发生 thundering herd
  - 高并发下可能导致数据库过载
- **建议修复**: 
  1. 实现 single-flight 模式
  2. 添加请求排队机制
  3. 使用分布式锁保护缓存更新

#### 15. [源代码] [中严重] [EvidenceService eviction 仅在插入时触发]
- **文件/路径**: `src/platform/five-plane-state-evidence/memory/evidence-service.ts:202`
- **问题描述**: 
  - 清理仅在 record() 调用时触发
  - 空闲时无后台清理
  - 可能导致内存持续增长
- **建议修复**: 
  1. 添加定期后台清理任务
  2. 使用独立的清理线程
  3. 添加内存使用监控

#### 16. [源代码] [低严重] [无连接指标暴露]
- **文件/路径**: WebSocket 服务器
- **问题描述**: 
  - getClientCount() 存在但未通过 HTTP 端点暴露
  - 无 pendingAcks 队列深度指标
  - 无连接建立告警
- **建议修复**: 
  1. 通过 metrics 端点暴露连接指标
  2. 添加 pendingAcks 队列监控
  3. 添加连接数异常告警

#### 17. [源代码] [低严重] [无空闲客户端超时]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts`
- **问题描述**: 
  - 仅在心跳 Sweep 时检查 isAlive
  - 无 per-client 独立于心跳的空闲超时
  - 认证后从不发送消息的客户端只能通过心跳失败检测
- **建议修复**: 
  1. 添加 per-client idle timeout
  2. 独立于心跳间隔
  3. 配置可调整

#### 18. [源代码] [低严重] [订阅限制是 per-client 而非全局]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:244`
- **问题描述**: 
  - MAX_SUBSCRIPTIONS_PER_CLIENT = 100
  - 恶意客户端可订阅 100 个任务
  - 无全局任务订阅者限制
- **建议修复**: 
  1. 添加全局任务订阅者限制
  2. 实现 per-task 订阅者上限
  3. 添加反滥用检测

#### 19. [配置] [低严重] [config/runtime 无版本验证机制]
- **文件/路径**: `config/runtime/*.json`
- **问题描述**: 
  - 配置有 "version": "v4.3"
  - 但无 schema 版本验证
  - 加载时可能接受不兼容配置
- **建议修复**: 
  1. 添加 JSON Schema 验证
  2. 实现版本兼容性检查
  3. 启动时验证配置完整性

#### 20. [源代码] [低严重] [ImprovementCandidateRegistry splice 是 O(n)]
- **文件/路径**: `src/platform/five-plane-orchestration/improve/improvement-candidate-registry.ts:243`
- **问题描述**: 
  - accessOrder 数组的 splice 操作是 O(n)
  - 高频访问时可能性能问题
- **建议修复**: 
  1. 使用 LinkedList 替代数组
  2. 或使用 Map 维护访问顺序
  3. 性能测试验证

### 总结

本次补充Review（第十三轮 - 缓存会话与实时通信Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. CORS allowedMethods 缺少 PUT/PATCH/DELETE（浏览器请求会失败）
2. Mission Routes 错误响应包装不当
3. WebSocketBridge 无最大连接数限制（DoS风险）
4. pendingAcks 断开连接时未清理（内存泄漏）
5. 硬编码回退密钥（安全漏洞）
6. config/runtime/test.json 缺少超时配置
7. prod.json maxConcurrentTasks=1 限制过严
8. 多个服务存在无界 Map（内存泄漏风险）

**中优先级**:
1. taskEventHistory Map 永不清理
2. 安全配置 drift（remoteWorkerRegistration 缺失）
3. OpenAPI 端点响应格式不一致
4. Redis 队列 idempotency 实现不完整
5. 请求去重中间件使用内存存储（多实例不共享）
6. Cache 无 stampede 保护
7. EvidenceService eviction 仅在插入时触发

**低优先级**:
1. 无连接指标暴露
2. 无空闲客户端超时
3. 订阅限制是 per-client 而非全局
4. config/runtime 无版本验证机制
5. ImprovementCandidateRegistry splice O(n)

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 64 | 74 | 35 | 173 |
| 测试 | 8 | 14 | 5 | 27 |
| 配置 | 10 | 38 | 24 | 72 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 7 | 3 | 12 |
| UI | 0 | 3 | 3 | 6 |
| 部署 | 1 | 10 | 6 | 17 |
| **合计** | **99** | **161** | **79** | **339** |

### 优先修复建议

**立即处理 (本周内)**:
1. 修复 CORS allowedMethods 添加 PUT/PATCH/DELETE
2. 修复 Mission Routes 错误响应格式
3. 为 WebSocketBridge 添加最大连接数限制
4. 清理 pendingAcks 和 taskEventHistory
5. 移除硬编码回退密钥
6. 修复 config/runtime/test.json 配置
7. 评估并调整 prod 并发限制

**短期内处理 (本月内)**:
1. 统一安全配置（remoteWorkerRegistration）
2. 实现无界 Map 的 LRU/TTL 驱逐
3. 统一 API 响应信封格式
4. 完善 Redis idempotency 实现
5. 添加分布式去重中间件
6. 实现 cache stampede 保护

**长期规划**:
1. 建立完整的连接管理和监控
2. 实现后台清理任务框架
3. 添加配置 schema 验证
4. 优化高频繁操作的数据结构
5. 建立内存使用基线和告警

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十五轮 - 构建测试与类型系统Review）

### 发现的新问题

#### 1. [构建] [高严重] [无 TypeScript 增量编译缓存]
- **文件/路径**: `tsconfig.json`
- **问题描述**: 
  - 无 `incremental: true` 配置
  - 无 `tsbuildinfo` 文件
  - 每次构建是完整重建（9.6秒）
  - TypeScript 无法使用增量编译
- **建议修复**: 
  1. 在 tsconfig.json 添加 `"incremental": true`
  2. 添加 `"tsbuildinfo": ".tsbuildinfo"` 到 .gitignore
  3. 实现项目引用以支持并行构建

#### 2. [构建] [高严重] [76个 npm scripts 大部分重复构建]
- **文件/路径**: `package.json`
- **问题描述**: 
  - 40+ CLI scripts 都运行 `npm run build && node ...`
  - 每个脚本前都执行完整重建
  - 无法直接运行预构建的 CLI
  - 20+ stable:* 脚本遵循相同模式
- **建议修复**: 
  1. 创建 `build:cli` 脚本构建一次
  2. 让 CLI 脚本使用预构建的 dist 文件
  3. 添加 npm run dev 或类似的无需重建的执行方式

#### 3. [源代码] [高严重] [168处 any 类型使用]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - `stable-runner-factory.ts`: `opts: any` -> `any`
  - `billing-service-async.ts`: `input: any` 方法签名
  - 重试/超时工具使用 `(...args: any[]) => Promise<any>`
  - 绕过类型安全
- **建议修复**: 
  1. 使用泛型替代 any
  2. 替换为具体的输入类型
  3. 添加 ESLint 规则禁止 any

#### 4. [源代码] [高严重] [38处 @ts-ignore 指令]
- **文件/路径**: 多个文件
- **问题描述**: 
  - 14个 stability 相关文件有 `@ts-ignore ExecutionRecord type mismatch`
  - 5个 harness-sdk 文件有 `Partial<HarnessRun> doesn't have all required properties`
  - 2个文件有 `exactOptionalPropertyTypes` 问题
- **建议修复**: 
  1. 修复 ExecutionRecord 类型不匹配
  2. 完成 HarnessRun 类型的必需属性
  3. 移除 @ts-ignore 使用

#### 5. [源代码] [高严重] [ExecutionRecord 类型不匹配]
- **文件/路径**: `src/platform/stability/stable-evidence-bundle-support.ts:783` 等14个文件
- **问题描述**: 
  - 插入 execution record 时 TypeScript 拒绝类型
  - `ExecutionRecord` 接口要求新字段但 insert 对象缺失
  - 或 store 的 insert 方法接受不同类型
- **建议修复**: 
  1. 对齐 ExecutionRecord 接口和 store 类型
  2. 添加缺失字段到 insert 对象
  3. 或调整 store 的 insert 方法类型签名

#### 6. [源代码] [中严重] [30+处 as unknown as 双类型转换]
- **文件/路径**: `division-loader.ts`, `domain-baseline-catalog.ts`, `harness-sdk`
- **问题描述**: 
  - 完全绕过类型安全
  - 通常是类型架构问题的信号
- **建议修复**: 
  1. 修复上游类型问题
  2. 使用更具体的类型断言
  3. 重构类型层次结构

#### 7. [构建] [中严重] [rimraf  extraneous - 未在 package.json 声明]
- **文件/路径**: `node_modules/rimraf`
- **问题描述**: 
  - rimraf@6.1.3 存在于 node_modules 但未在 package.json 声明
  - 可能导致依赖问题
- **建议修复**: 
  1. 添加到 dependencies 或 devDependencies
  2. 或移除并使用其他清理方式

#### 8. [测试] [高严重] [178处直接 process.env 变更无抽象]
- **文件/路径**: `tests/unit/domains/registry/plugin-spi-registry.test.ts:640-687`
- **问题描述**: 
  - 直接读取/写入 process.env
  - 在 beforeEach/afterEach 中手动保存/恢复
  - 无 helper 抽象
- **建议修复**: 
  1. 创建 env helper 函数
  2. 统一保存和恢复模式
  3. 添加测试隔离验证

#### 9. [测试] [高严重] [21,998处 unlinkSync 调用无集中清理]
- **文件/路径**: 多个测试文件
- **问题描述**: 
  - 每个测试重新实现清理逻辑
  - 无集中清理 utility
  - 可能导致 EBUSY (Windows) 或并发访问问题
- **建议修复**: 
  1. 创建集中的测试清理 utility
  2. 使用 afterEach 确保清理
  3. 考虑使用临时目录

#### 10. [测试] [中严重] [无统一 mocking 框架]
- **文件/路径**: `tests/` 多个目录
- **问题描述**: 
  - 10,579 处 vi.fn, jest.mock, sinon 等调用
  - 无统一的 mocking 框架可见
  - 模式不一致
- **建议修复**: 
  1. 建立统一的 mock factory
  2. 标准化 mock 模式
  3. 添加 mocking 最佳实践文档

#### 11. [测试] [中严重] [Singleton 状态重置未标准化]
- **文件/路径**: `tests/unit/platform/five-plane-execution/dispatcher.test.ts`
- **问题描述**: 
  - 测试通过特定函数重置 singletons
  - 每个测试重复自己的 reset 逻辑
  - 无统一的 resetAllSingletons()
- **建议修复**: 
  1. 创建统一的 singleton reset 机制
  2. 在 afterEach 中自动调用
  3. 文档化 singleton 重置要求

#### 12. [测试] [低严重] [无分支覆盖率要求]
- **文件/路径**: `.c8rc.json`
- **问题描述**: 
  - `"100": false` - 无覆盖率要求
  - 只测量源代码，不测量测试文件
- **建议修复**: 
  1. 添加分支覆盖率阈值
  2. 在 CI 中强制执行覆盖率 gate
  3. 关注关键代码路径

#### 13. [网络] [高严重] [ScopedExternalAccessSandbox performHttpRequest 无超时]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/scoped-external-access-sandbox.ts:289`
- **问题描述**: 
  - fetch 调用无 timeout 参数
  - 挂起的请求可能永久阻塞
- **建议修复**: 
  1. 添加 AbortController timeout
  2. 使用标准的超时模式
  3. 记录超时配置

#### 14. [网络] [高严重] [OIDC 服务 fetch 调用无超时]
- **文件/路径**: `src/org-governance/sso-scim/oidc/oidc-service.ts:565,272`
- **问题描述**: 
  - OIDC token 交换无 retry logic
  - 无 timeout/abort signal
  - 认证流程可能挂起
- **建议修复**: 
  1. 添加 fetch timeout
  2. 添加 retry logic
  3. 使用标准的超时配置

#### 15. [网络] [高严重] [plugin-runtime-child 替换 globalThis.fetch 为 no-op]
- **文件/路径**: `src/domains/registry/plugin-runtime-child.ts:112`
- **问题描述**: 
  ```typescript
  globalThis.fetch = (async () => {}) as unknown as typeof globalThis.fetch;
  ```
  - 插件上下文中完全替换 fetch 为空函数
  - 插件的所有出站 HTTP 请求都会失败
  - 看起来是有意为之但可能造成问题
- **建议修复**: 
  1. 确认这是有意行为
  2. 文档化 fetch 替换行为
  3. 确保不会意外影响其他代码

#### 16. [网络] [中严重] [无显式 http.Agent 配置]
- **文件/路径**: 全局
- **问题描述**: 
  - 无显式的 http.Agent/https.Agent 配置
  - 依赖 Node.js 默认连接池
  - 无 maxSockets 或 maxConnections 配置
  - 高吞吐场景可能出问题
- **建议修复**: 
  1. 添加 http Agent 配置
  2. 文档化连接池大小
  3. 监控连接使用情况

#### 17. [序列化] [中严重] [合同版本硬编码为字符串字面量]
- **文件/路径**: `src/platform/contracts/inter-plane-contract-gateway.ts:184`
- **问题描述**: 
  - `schemaVersion: "v4.3"` 硬编码
  - 难以追踪合同版本演变
- **建议修复**: 
  1. 提取到类型化常量或 enum
  2. 集中管理合同版本
  3. 添加版本兼容性检查

#### 18. [序列化] [低严重] [Timestamp = string 丢失精度]
- **文件/路径**: `src/platform/contracts/types/domain/primitives.ts:10`
- **问题描述**: 
  - `type Timestamp = string` 无类型精度
  - ISO 8601 字符串无时区信息
- **建议修复**: 
  1. 使用 Date 对象或 branded string 类型
  2. 明确时区处理约定
  3. 文档化时间戳语义

#### 19. [源代码] [中严重] [29处非空断言 !.]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - 使用 `!.` 断言非空
  - 可能导致运行时错误
- **建议修复**: 
  1. 使用可选链 `?.` 或空值合并 `??`
  2. 添加适当的 null 检查
  3. 减少非空断言使用

#### 20. [API] [中严重] [API 路由 schema 版本硬编码]
- **文件/路径**: `src/platform/five-plane-interface/api/federation-routing-service.ts`
- **问题描述**: 
  - Zod schema 验证存在
  - 但 schema 版本未与 contract 版本同步
- **建议修复**: 
  1. 从 contract 版本派生 schema 版本
  2. 添加版本一致性检查
  3. 文档化版本关系

### 总结

本次补充Review（第十五轮 - 构建测试与类型系统Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. 无 TypeScript 增量编译缓存（每次完整重建9.6秒）
2. 76个 npm scripts 大部分重复构建
3. 168处 any 类型使用（绕过类型安全）
4. 38处 @ts-ignore 指令
5. ExecutionRecord 类型不匹配（14个文件）
6. 178处直接 process.env 变更无抽象
7. ScopedExternalAccessSandbox performHttpRequest 无超时
8. OIDC 服务 fetch 调用无超时
9. plugin-runtime-child 替换 globalThis.fetch 为 no-op

**中优先级**:
1. 30+处 as unknown as 双类型转换
2. rimraf extraneous 未声明
3. 21,998处 unlinkSync 无集中清理
4. 无统一 mocking 框架
5. Singleton 状态重置未标准化
6. 无显式 http.Agent 配置
7. 合同版本硬编码为字符串
8. Timestamp = string 丢失精度
9. 非空断言使用

**低优先级**:
1. 无分支覆盖率要求
2. API 路由 schema 版本未同步

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 72 | 87 | 39 | 198 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **109** | **182** | **85** | **376** |

### 优先修复建议

**立即处理 (本周内)**:
1. 添加 TypeScript 增量编译 (`incremental: true`)
2. 重构 npm scripts 避免重复构建
3. 减少 any 类型使用到 <50
4. 移除所有 @ts-ignore 指令
5. 修复 ExecutionRecord 类型不匹配
6. 添加 fetch 超时到 OIDC 和 sandbox

**短期内处理 (本月内)**:
1. 创建集中的测试清理 utility
2. 建立统一的 mocking 框架
3. 标准化 singleton 重置机制
4. 添加 http.Agent 连接池配置
5. 提取合同版本到类型化常量

**长期规划**:
1. 实现完整的类型安全（无 any）
2. 建立测试基础设施标准
3. 实现构建缓存优化
4. 完善网络层超时和重试
5. 建立覆盖率门禁标准

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十四轮 - 日志事件与工作流Review）

### 发现的新问题

#### 1. [可观测性] [高严重] [StructuredLogEntry 无 requestId 字段]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - StructuredLogEntry 接口无 requestId 字段
  - 只有 correlationId，它回退到 traceId
  - 无法独立追踪单个 HTTP 请求
- **建议修复**: 
  1. 在 StructuredLogEntry 添加 requestId 字段
  2. 在 HTTP 中间件中生成和注入 requestId
  3. 确保 requestId 流经所有日志调用

#### 2. [可观测性] [高严重] [StructuredLogger data 字段无自动清洗]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - 如果开发者传递 `{ password: "..." }` 给 logger.info，密码会被记录
  - 无字段级脱敏机制
  - 依赖开发者手动排除敏感字段
- **建议修复**: 
  1. 添加 OBSERVE_OUTPUT_BLACKLIST 类似的黑名单
  2. 实现 redact/mask/sanitize 工具函数
  3. 在日志传输前自动清洗敏感字段

#### 3. [可观测性] [中严重] [日志中敏感数据无 PII 处理]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - 无 PII 重写工具
  - 开发者必须手动避免记录敏感数据
  - 存在意外记录敏感信息的风险
- **建议修复**: 
  1. 添加 PII 检测和重写工具
  2. 在文档中明确记录敏感字段列表
  3. 添加测试验证敏感数据不被记录

#### 4. [可观测性] [中严重] [59处 console.* 直接调用]
- **文件/路径**: 多个源文件
- **问题描述**: 
  - 应该使用 StructuredLogger
  - console 调用可能不被日志聚合系统收集
  - 难以追踪和关联
- **建议修复**: 
  1. 替换所有 console.* 为 StructuredLogger
  2. 添加 ESLint 规则禁止 console.*
  3. 保留少量允许的 CLI 输出

#### 5. [可观测性] [中严重] [部分 logger.warn 调用缺少结构化数据]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:122`
- **问题描述**: 
  ```typescript
  logger.warn("WebSocket connection rejected: missing subprotocol token")
  ```
  - 只有消息，无 data 对象
  - 难以关联和查询
- **建议修复**: 
  1. 添加 `{ data: { ... } }` 对象
  2. 确保所有日志包含上下文数据
  3. 添加日志审查工具

#### 6. [源代码] [高严重] [Harness while(true) 循环无硬性迭代上限]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:1442`
- **问题描述**: 
  ```typescript
  while (true) {
    // ...
    // 依赖 budget gate 和 guardrail vibration 退出
    // 如果两者都失败，循环永久继续
  }
  ```
  - 依赖 budget gate 和 guardrail vibration 退出
  - 无硬性迭代次数限制
  - 可能导致无限循环
- **建议修复**: 
  1. 添加硬性 maxIterations 上限
  2. 在达到上限时强制退出
  3. 记录详细的迭代统计用于调试

#### 7. [源代码] [高严重] [Oapeflir while(true) 循环可能无限重规划]
- **文件/路径**: `src/platform/five-plane-orchestration/oapeflir/oapeflir-loop-service.ts:355`
- **问题描述**: 
  - 如果 loopReplanDecision.shouldReplan 永不变为 false
  - 重规划永远无法减少计划错误
  - 可能导致无限循环
- **建议修复**: 
  1. 添加重规划次数上限
  2. 在达到上限时强制接受当前结果
  3. 添加重规划质量衰减检测

#### 8. [源代码] [中严重] [Recovery 流程允许终态转换到 paused]
- **文件/路径**: `src/platform/five-plane-execution/runtime-state-machine.ts:105-108`
- **问题描述**: 
  - completed, failed, cancelled, aborted 可转换到 paused 进行恢复
  - 创建恢复路径可能重新进入执行
  - 需要验证此行为的正确性
- **建议修复**: 
  1. 审查 recovery 转换的合法性
  2. 确保不会导致状态不一致
  3. 添加转换前置条件验证

#### 9. [国际化] [高严重] [API 错误消息硬编码英文]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/api-error.ts`
- **问题描述**: 
  ```typescript
  return new ApiError(404, "api.task_not_found", "Task not found.");
  ```
  - 错误消息硬编码英文
  - 无后端 i18n 机制
  - 非英文客户端收到不本地化的错误
- **建议修复**: 
  1. 使用错误码而非硬编码消息
  2. 客户端根据错误码本地化
  3. 或在后端添加 i18n 支持

#### 10. [国际化] [中严重] [UI 硬编码字符串未翻译]
- **文件/路径**: `ui/packages/features/conversation/src/web/index.tsx`
- **问题描述**: 
  - "Build Plan", "Execute", "Messages" 等按钮标签硬编码
  - 不使用 i18n 系统
- **建议修复**: 
  1. 使用 `translateFeatureCopy` 替换硬编码
  2. 确保所有用户可见文本通过 i18n
  3. 添加硬编码字符串检测

#### 11. [国际化] [中严重] [Arabic 目录不完整]
- **文件/路径**: `ui/packages/shared/i18n/src/catalogs/ar-SA.ts`
- **问题描述**: 
  - ar-SA 只有约18条消息
  - en-US 和 zh-CN 有约117条
  - 缺失的 key 返回 key 本身
- **建议修复**: 
  1. 补充 Arabic 翻译
  2. 确保所有 key 都有翻译
  3. 添加翻译完整性测试

#### 12. [国际化] [低严重] [translateFeatureCopy 无回退默认值]
- **文件/路径**: `ui/packages/shared/i18n/`
- **问题描述**: 
  - 如果 feature ID 不在 catalog 中
  - 返回 undefined title/summary
  - 无合理回退
- **建议修复**: 
  1. 添加回退到 key 或默认字符串
  2. 或使用 feature ID 作为显示名
  3. 确保 UI 不显示 undefined

#### 13. [运维] [高严重] [健康检查无 readiness/liveness 区分]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts`
- **问题描述**: 
  - /healthz, /v1/healthz, /health 返回相同报告
  - 无 readiness probe（可处理流量？）
  - 无 liveness probe（系统存活？）
  - canAcceptTraffic() 未通过 HTTP 暴露
- **建议修复**: 
  1. 添加 /ready 和 /live 端点
  2. Readiness 检查依赖项就绪
  3. Liveness 检查进程健康

#### 14. [运维] [高严重] [shutdown 期间健康检查存在竞态]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts`
- **问题描述**: 
  - getHealthReportAsync() 异步检查 dbWritable
  - shutdown 期间数据库连接可能关闭
  - 健康检查仍在传入
- **建议修复**: 
  1. 在 shutdown 时返回 unhealthy
  2. 添加 shutdown 状态标志
  3. 停止接受新的健康检查请求

#### 15. [运维] [高严重] [LeaderElectionService 未与 GracefulShutdown 集成]
- **文件/路径**: `src/platform/five-plane-execution/ha/leader-election-service.ts`
- **问题描述**: 
  - LeaderElectionService.stop() 处理优雅退位
  - 但未在全局 graceful shutdown 中注册
  - 需显式注册为 shutdown handler
- **建议修复**: 
  1. 将 LeaderElectionService 添加到 shutdown handlers
  2. 确保领导者在 shutdown 时让位
  3. 验证关闭顺序正确

#### 16. [运维] [中严重] [addHandler() 无显式排序约定]
- **文件/路径**: `src/platform/five-plane-execution/startup/graceful-shutdown.ts`
- **问题描述**: 
  - 虽然 handlers 逆序执行
  - 但无显式接口声明 handler 依赖关系
  - 关键 handlers（如关闭数据库连接）应最后运行
- **建议修复**: 
  1. 添加优先级或阶段参数
  2. 文档化关键 handler 顺序要求
  3. 添加顺序验证测试

#### 17. [运维] [中严重] [unref'd timers 可能不触发]
- **文件/路径**: `src/platform/five-plane-execution/startup/process-error-handlers.ts:106`
- **问题描述**: 
  - `setTimeout(...).unref()` 不保持事件循环活跃
  - 如果主线程空闲，强制退出可能不触发
- **建议修复**: 
  1. 移除 .unref() 除非确实需要
  2. 或确保有其他方式保持事件循环
  3. 添加超时触发验证测试

#### 18. [运维] [中严重] [StartupConsistencyChecker 阻塞流量但不暴露状态]
- **文件/路径**: `src/platform/five-plane-startup-plan.ts`
- **问题描述**: 
  - canAcceptTraffic() 在 P0 问题存在时返回 false
  - 但 _trafficBlocked 是内部状态
  - 负载均衡器访问 /healthz 仍会得到 "ok"
- **建议修复**: 
  1. 在健康报告中暴露 trafficBlocked 状态
  2. 或添加单独的健康端点指示就绪状态
  3. 确保负载均衡器在阻塞时不路由流量

#### 19. [事件系统] [中严重] [TypedEventBus vs EventEmitter 混用]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - 项目同时使用 TypedEventBus 和原生 EventEmitter
  - TypedEventBus 应该是标准
  - 混用可能导致类型安全问题
- **建议修复**: 
  1. 创建迁移计划统一使用 TypedEventBus
  2. 添加 ESLint 规则禁止原生 EventEmitter
  3. 在 CI 中检测混用情况

#### 20. [源代码] [低严重] [无事件排序保证]
- **文件/路径**: `src/platform/five-plane-state-evidence/events/`
- **问题描述**: 
  - 无明确的事件排序保证文档
  - 事件处理可能乱序
  - 依赖方需要处理乱序
- **建议修复**: 
  1. 文档化事件排序语义
  2. 如果需要排序，实现序列号机制
  3. 在事件处理中考虑乱序情况

### 总结

本次补充Review（第十四轮 - 日志事件与工作流Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. StructuredLogEntry 无 requestId 字段
2. StructuredLogger data 字段无自动清洗（敏感数据风险）
3. Harness while(true) 循环无硬性迭代上限
4. Oapeflir while(true) 循环可能无限重规划
5. API 错误消息硬编码英文（无 i18n）
6. 健康检查无 readiness/liveness 区分
7. shutdown 期间健康检查存在竞态
8. LeaderElectionService 未与 GracefulShutdown 集成

**中优先级**:
1. 日志中敏感数据无 PII 处理
2. 59处 console.* 直接调用
3. 部分 logger.warn 调用缺少结构化数据
4. Recovery 流程允许终态转换到 paused
5. UI 硬编码字符串未翻译
6. Arabic 目录不完整
7. addHandler() 无显式排序约定
8. unref'd timers 可能不触发
9. StartupConsistencyChecker 阻塞流量但不暴露状态
10. TypedEventBus vs EventEmitter 混用

**低优先级**:
1. translateFeatureCopy 无回退默认值
2. 无事件排序保证

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 68 | 80 | 37 | 185 |
| 测试 | 8 | 14 | 5 | 27 |
| 配置 | 10 | 38 | 24 | 72 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **103** | **171** | **82** | **356** |

### 优先修复建议

**立即处理 (本周内)**:
1. 在 StructuredLogEntry 添加 requestId 字段
2. 实现日志敏感字段自动清洗
3. 为工作流循环添加硬性迭代上限
4. 添加 readiness/liveness 健康检查端点
5. 将 LeaderElectionService 集成到 GracefulShutdown
6. 实现 API 错误消息 i18n 支持

**短期内处理 (本月内)**:
1. 替换所有 console.* 为 StructuredLogger
2. 补充 Arabic i18n 目录
3. 修复 UI 硬编码字符串
4. 统一 TypedEventBus 替代 EventEmitter
5. 解决 shutdown 竞态条件
6. 添加事件排序保证文档

**长期规划**:
1. 建立完整的 PII 处理框架
2. 实现日志完整性验证
3. 建立工作流安全循环检测
4. 完善健康检查和就绪探测
5. 建立国际化完整测试

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十五轮 - 构建测试与类型系统Review）

### 发现的新问题

#### 1. [构建] [高严重] [无 TypeScript 增量编译缓存]
- **文件/路径**: `tsconfig.json`
- **问题描述**: 
  - 无 `incremental: true` 配置
  - 无 `tsbuildinfo` 文件
  - 每次构建是完整重建（9.6秒）
  - TypeScript 无法使用增量编译
- **建议修复**: 
  1. 在 tsconfig.json 添加 `"incremental": true`
  2. 添加 `"tsbuildinfo": ".tsbuildinfo"` 到 .gitignore
  3. 实现项目引用以支持并行构建

#### 2. [构建] [高严重] [76个 npm scripts 大部分重复构建]
- **文件/路径**: `package.json`
- **问题描述**: 
  - 40+ CLI scripts 都运行 `npm run build && node ...`
  - 每个脚本前都执行完整重建
  - 无法直接运行预构建的 CLI
  - 20+ stable:* 脚本遵循相同模式
- **建议修复**: 
  1. 创建 `build:cli` 脚本构建一次
  2. 让 CLI 脚本使用预构建的 dist 文件
  3. 添加 npm run dev 或类似的无需重建的执行方式

#### 3. [源代码] [高严重] [168处 any 类型使用]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - `stable-runner-factory.ts`: `opts: any` -> `any`
  - `billing-service-async.ts`: `input: any` 方法签名
  - 重试/超时工具使用 `(...args: any[]) => Promise<any>`
  - 绕过类型安全
- **建议修复**: 
  1. 使用泛型替代 any
  2. 替换为具体的输入类型
  3. 添加 ESLint 规则禁止 any

#### 4. [源代码] [高严重] [38处 @ts-ignore 指令]
- **文件/路径**: 多个文件
- **问题描述**: 
  - 14个 stability 相关文件有 `@ts-ignore ExecutionRecord type mismatch`
  - 5个 harness-sdk 文件有 `Partial<HarnessRun> doesn't have all required properties`
  - 2个文件有 `exactOptionalPropertyTypes` 问题
- **建议修复**: 
  1. 修复 ExecutionRecord 类型不匹配
  2. 完成 HarnessRun 类型的必需属性
  3. 移除 @ts-ignore 使用

#### 5. [源代码] [高严重] [ExecutionRecord 类型不匹配]
- **文件/路径**: `src/platform/stability/stable-evidence-bundle-support.ts:783` 等14个文件
- **问题描述**: 
  - 插入 execution record 时 TypeScript 拒绝类型
  - `ExecutionRecord` 接口要求新字段但 insert 对象缺失
  - 或 store 的 insert 方法接受不同类型
- **建议修复**: 
  1. 对齐 ExecutionRecord 接口和 store 类型
  2. 添加缺失字段到 insert 对象
  3. 或调整 store 的 insert 方法类型签名

#### 6. [源代码] [中严重] [30+处 as unknown as 双类型转换]
- **文件/路径**: `division-loader.ts`, `domain-baseline-catalog.ts`, `harness-sdk`
- **问题描述**: 
  - 完全绕过类型安全
  - 通常是类型架构问题的信号
- **建议修复**: 
  1. 修复上游类型问题
  2. 使用更具体的类型断言
  3. 重构类型层次结构

#### 7. [构建] [中严重] [rimraf  extraneous - 未在 package.json 声明]
- **文件/路径**: `node_modules/rimraf`
- **问题描述**: 
  - rimraf@6.1.3 存在于 node_modules 但未在 package.json 声明
  - 可能导致依赖问题
- **建议修复**: 
  1. 添加到 dependencies 或 devDependencies
  2. 或移除并使用其他清理方式

#### 8. [测试] [高严重] [178处直接 process.env 变更无抽象]
- **文件/路径**: `tests/unit/domains/registry/plugin-spi-registry.test.ts:640-687`
- **问题描述**: 
  - 直接读取/写入 process.env
  - 在 beforeEach/afterEach 中手动保存/恢复
  - 无 helper 抽象
- **建议修复**: 
  1. 创建 env helper 函数
  2. 统一保存和恢复模式
  3. 添加测试隔离验证

#### 9. [测试] [高严重] [21,998处 unlinkSync 调用无集中清理]
- **文件/路径**: 多个测试文件
- **问题描述**: 
  - 每个测试重新实现清理逻辑
  - 无集中清理 utility
  - 可能导致 EBUSY (Windows) 或并发访问问题
- **建议修复**: 
  1. 创建集中的测试清理 utility
  2. 使用 afterEach 确保清理
  3. 考虑使用临时目录

#### 10. [测试] [中严重] [无统一 mocking 框架]
- **文件/路径**: `tests/` 多个目录
- **问题描述**: 
  - 10,579 处 vi.fn, jest.mock, sinon 等调用
  - 无统一的 mocking 框架可见
  - 模式不一致
- **建议修复**: 
  1. 建立统一的 mock factory
  2. 标准化 mock 模式
  3. 添加 mocking 最佳实践文档

#### 11. [测试] [中严重] [Singleton 状态重置未标准化]
- **文件/路径**: `tests/unit/platform/five-plane-execution/dispatcher.test.ts`
- **问题描述**: 
  - 测试通过特定函数重置 singletons
  - 每个测试重复自己的 reset 逻辑
  - 无统一的 resetAllSingletons()
- **建议修复**: 
  1. 创建统一的 singleton reset 机制
  2. 在 afterEach 中自动调用
  3. 文档化 singleton 重置要求

#### 12. [测试] [低严重] [无分支覆盖率要求]
- **文件/路径**: `.c8rc.json`
- **问题描述**: 
  - `"100": false` - 无覆盖率要求
  - 只测量源代码，不测量测试文件
- **建议修复**: 
  1. 添加分支覆盖率阈值
  2. 在 CI 中强制执行覆盖率 gate
  3. 关注关键代码路径

#### 13. [网络] [高严重] [ScopedExternalAccessSandbox performHttpRequest 无超时]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/scoped-external-access-sandbox.ts:289`
- **问题描述**: 
  - fetch 调用无 timeout 参数
  - 挂起的请求可能永久阻塞
- **建议修复**: 
  1. 添加 AbortController timeout
  2. 使用标准的超时模式
  3. 记录超时配置

#### 14. [网络] [高严重] [OIDC 服务 fetch 调用无超时]
- **文件/路径**: `src/org-governance/sso-scim/oidc/oidc-service.ts:565,272`
- **问题描述**: 
  - OIDC token 交换无 retry logic
  - 无 timeout/abort signal
  - 认证流程可能挂起
- **建议修复**: 
  1. 添加 fetch timeout
  2. 添加 retry logic
  3. 使用标准的超时配置

#### 15. [网络] [高严重] [plugin-runtime-child 替换 globalThis.fetch 为 no-op]
- **文件/路径**: `src/domains/registry/plugin-runtime-child.ts:112`
- **问题描述**: 
  ```typescript
  globalThis.fetch = (async () => {}) as unknown as typeof globalThis.fetch;
  ```
  - 插件上下文中完全替换 fetch 为空函数
  - 插件的所有出站 HTTP 请求都会失败
  - 看起来是有意为之但可能造成问题
- **建议修复**: 
  1. 确认这是有意行为
  2. 文档化 fetch 替换行为
  3. 确保不会意外影响其他代码

#### 16. [网络] [中严重] [无显式 http.Agent 配置]
- **文件/路径**: 全局
- **问题描述**: 
  - 无显式的 http.Agent/https.Agent 配置
  - 依赖 Node.js 默认连接池
  - 无 maxSockets 或 maxConnections 配置
  - 高吞吐场景可能出问题
- **建议修复**: 
  1. 添加 http Agent 配置
  2. 文档化连接池大小
  3. 监控连接使用情况

#### 17. [序列化] [中严重] [合同版本硬编码为字符串字面量]
- **文件/路径**: `src/platform/contracts/inter-plane-contract-gateway.ts:184`
- **问题描述**: 
  - `schemaVersion: "v4.3"` 硬编码
  - 难以追踪合同版本演变
- **建议修复**: 
  1. 提取到类型化常量或 enum
  2. 集中管理合同版本
  3. 添加版本兼容性检查

#### 18. [序列化] [低严重] [Timestamp = string 丢失精度]
- **文件/路径**: `src/platform/contracts/types/domain/primitives.ts:10`
- **问题描述**: 
  - `type Timestamp = string` 无类型精度
  - ISO 8601 字符串无时区信息
- **建议修复**: 
  1. 使用 Date 对象或 branded string 类型
  2. 明确时区处理约定
  3. 文档化时间戳语义

#### 19. [源代码] [中严重] [29处非空断言 !.]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - 使用 `!.` 断言非空
  - 可能导致运行时错误
- **建议修复**: 
  1. 使用可选链 `?.` 或空值合并 `??`
  2. 添加适当的 null 检查
  3. 减少非空断言使用

#### 20. [API] [中严重] [API 路由 schema 版本硬编码]
- **文件/路径**: `src/platform/five-plane-interface/api/federation-routing-service.ts`
- **问题描述**: 
  - Zod schema 验证存在
  - 但 schema 版本未与 contract 版本同步
- **建议修复**: 
  1. 从 contract 版本派生 schema 版本
  2. 添加版本一致性检查
  3. 文档化版本关系

### 总结

本次补充Review（第十五轮 - 构建测试与类型系统Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. 无 TypeScript 增量编译缓存（每次完整重建9.6秒）
2. 76个 npm scripts 大部分重复构建
3. 168处 any 类型使用（绕过类型安全）
4. 38处 @ts-ignore 指令
5. ExecutionRecord 类型不匹配（14个文件）
6. 178处直接 process.env 变更无抽象
7. ScopedExternalAccessSandbox performHttpRequest 无超时
8. OIDC 服务 fetch 调用无超时
9. plugin-runtime-child 替换 globalThis.fetch 为 no-op

**中优先级**:
1. 30+处 as unknown as 双类型转换
2. rimraf extraneous 未声明
3. 21,998处 unlinkSync 无集中清理
4. 无统一 mocking 框架
5. Singleton 状态重置未标准化
6. 无显式 http.Agent 配置
7. 合同版本硬编码为字符串
8. Timestamp = string 丢失精度
9. 非空断言使用

**低优先级**:
1. 无分支覆盖率要求
2. API 路由 schema 版本未同步

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 72 | 87 | 39 | 198 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **109** | **182** | **85** | **376** |

### 优先修复建议

**立即处理 (本周内)**:
1. 添加 TypeScript 增量编译 (`incremental: true`)
2. 重构 npm scripts 避免重复构建
3. 减少 any 类型使用到 <50
4. 移除所有 @ts-ignore 指令
5. 修复 ExecutionRecord 类型不匹配
6. 添加 fetch 超时到 OIDC 和 sandbox

**短期内处理 (本月内)**:
1. 创建集中的测试清理 utility
2. 建立统一的 mocking 框架
3. 标准化 singleton 重置机制
4. 添加 http.Agent 连接池配置
5. 提取合同版本到类型化常量

**长期规划**:
1. 实现完整的类型安全（无 any）
2. 建立测试基础设施标准
3. 实现构建缓存优化
4. 完善网络层超时和重试
5. 建立覆盖率门禁标准

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第九轮 - 深度Review）

### 发现的新问题

#### 1. [安全] [高严重] [npm audit 发现7个安全漏洞未修复]
- **文件/路径**: `package.json` / `package-lock.json`
- **问题描述**: 
  - `@opentelemetry/exporter-prometheus` < 0.217.0: Prometheus exporter process crash (GHSA-q7rr-3cgh-j5r3)
  - `@protobufjs/utf8` <= 1.1.0: overlong UTF-8 decoding (GHSA-q6x5-8v7m-xcrf)
  - `@xmldom/xmldom` <= 0.8.12: Multiple XML injection vulnerabilities
  - `fast-uri` <= 3.1.1: Path traversal via percent-encoded dot segments
  - `protobufjs` <= 7.5.5: Multiple DoS and code injection vulnerabilities
  - 共7个漏洞 (1 moderate, 6 high)
- **建议修复**: 
  1. 运行 `npm audit fix --force` 修复部分问题
  2. 注意：`@opentelemetry/exporter-prometheus` 和 `xml-crypto` 修复会引入breaking changes
  3. 建议先评估breaking change影响再决定修复策略

#### 2. [配置] [高严重] [TypeScript 编译存在50+错误]
- **文件/路径**: `tsconfig.json` 及测试文件
- **问题描述**: 
  - `npm run typecheck` 发现50个编译错误
  - 主要集中在 `tests/e2e/` 目录
  - 错误类型包括：
    - `fenceToken` vs `fencingToken` 属性名不匹配
    - 缺少属性：`completedAt`, `terminalAt`, `source`, `degraded`
    - 属性名错误：`workflowId` vs `workflow`, `output` vs `outputs`
    - 函数参数数量不匹配
- **建议修复**: 
  1. 同步测试文件与类型定义
  2. 修复属性名不匹配问题
  3. 添加缺失的属性定义

#### 3. [依赖] [高严重] [多个依赖版本严重落后]
- **文件/路径**: `package.json`
- **问题描述**: 
  - `xml-crypto`: 2.1.6，当前最新 6.1.2 (HIGH vulnerability)
  - `zod`: 3.25.76，当前最新 4.4.3 (major version bump)
  - `typescript`: 5.9.3，当前最新 6.0.3
  - `@types/node`: 22.19.15，当前最新 25.7.0 (major jump)
  - `eslint`: 9.39.4，当前最新 10.3.0
  - 依赖版本落后可能导致兼容性和安全问题
- **建议修复**: 
  1. 制定依赖升级计划
  2. 优先升级安全相关依赖
  3. 验证 major version bump 的 breaking changes

#### 4. [配置] [高严重] [安全配置在环境间不一致]
- **文件/路径**: `config/security/`
- **问题描述**: 
  - `default.json` 有完整结构（5个字段）
  - `dev.json`, `staging.json`, `test.json` 只有2个字段
  - `pre-prod.json`, `prod.json` 只有3个字段
  - 缺少 `sandboxMode` 和 `remoteWorkerRegistration`
- **建议修复**: 
  1. 统一所有环境配置文件结构
  2. 确保每个环境都有完整的安全配置
  3. 添加环境特定的覆盖值

#### 5. [配置] [高严重] [.env.example 中JWT_SECRET为空]
- **文件/路径**: `.env.example`
- **问题描述**: 
  - `AA_API_JWT_SECRET=` 为空（第16行）
  - LLM provider API keys 都为空（第23-29行）
  - `AA_STORAGE_POSTGRES_DSN` 被注释但生产环境需要
  - 缺少 `AA_OTEL_ENDPOINT`, `AA_OTEL_SERVICE_NAME`
  - 缺少 `SLACK_WEBHOOK_URL`, `PAGERDUTY_SERVICE_KEY`
- **建议修复**: 
  1. 添加所有必需的环境变量说明
  2. 标记必填和可选变量
  3. 添加每个变量的用途注释

#### 6. [源代码] [高严重] [src/platform 目录职责混乱]
- **文件/路径**: `src/platform/`
- **问题描述**: 
  - platform/ 下混合了五层架构目录和多个不相关的目录
  - 不相关的目录：`agent-delegation/`, `compliance/`, `cost-management/`, `model-gateway/`, `ops-maturity/`, `prompt-engine/`, `shared/`, `stability/`, `structure/`
  - 违反五层架构的分离关注点原则
- **建议修复**: 
  1. 重新组织 platform/ 下的目录结构
  2. 将非五层架构模块移到适当位置
  3. 明确每个目录的职责边界

#### 7. [源代码] [高严重] [巨型 barrel file - index.ts 达2317行]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 单个文件包含过多导出
  - 违反模块化原则
  - 更改一个子系统可能需要重新编译大量代码
- **建议修复**: 
  1. 将 index.ts 拆分为多个模块
  2. 按功能分组导出
  3. 创建多个小型的 barrel files

#### 8. [源代码] [高严重] [core/runtime 是空壳，只做重导出]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - `orchestrator/index.ts` -> 重导出 `platform/five-plane-execution/execution-engine/multi-step-orchestration.js`
  - `supervisor/index.ts` -> 重导出 `platform/five-plane-execution/execution-engine/multi-step-supervisor.js`
  - 实际实现在 platform/，core/runtime 只是兼容层
  - CLAUDE.md 说这是兼容性目录，但可能造成混淆
- **建议修复**: 
  1. 明确 core/runtime 的保留用途
  2. 考虑是否完全移除或迁移到 platform/
  3. 更新 CLAUDE.md 说明迁移计划

#### 9. [源代码] [中严重] [5个符号链接存在且未文档化]
- **文件/路径**: `src/platform/`
- **问题描述**: 
  - `interface -> five-plane-interface`
  - `execution -> five-plane-execution`
  - `state-evidence -> five-plane-state-evidence`
  - `orchestration -> five-plane-orchestration`
  - `control-plane -> five-plane-control-plane`
  - 所有符号链接目标存在且有效
  - 但未在 CLAUDE.md 中记录
- **建议修复**: 
  1. 在 CLAUDE.md 中记录所有符号链接
  2. 考虑是否可以用实际目录替代
  3. 确保 CI 中验证符号链接完整性

#### 10. [配置] [中严重] [Terraform VPC CIDR 硬编码]
- **文件/路径**: `deploy/terraform/main.tf`
- **问题描述**: 
  - 第80-81行、62行：RDS 和 ElastiCache 安全组硬编码为 `10.0.0.0/16`
  - 应该使用变量 `var.vpc_cidr`
  - 这是安全配置问题
- **建议修复**: 
  1. 将硬编码 CIDR 替换为变量引用
  2. 确保所有环境使用正确的网络隔离

#### 11. [配置] [中严重] [Helm values 中 OTEL 配置不一致]
- **文件/路径**: `deploy/helm/automatic-agent/values*.yaml`
- **问题描述**: 
  - `values-prod.yaml`: OTEL 启用 (true)
  - `values-staging.yaml`, `values-pre-prod.yaml`: 未设置
  - `values.yaml` (default): OTEL 禁用 (false)
  - staging/pre-prod 与 prod 配置不一致
- **建议修复**: 
  1. 统一所有环境的 OTEL 配置策略
  2. 明确哪些环境应该启用 OTEL
  3. 添加注释说明配置差异原因

#### 12. [配置] [中严重] [Helm values 中 Ingress hosts 为空数组]
- **文件/路径**: `deploy/helm/automatic-agent/values-staging.yaml`, `values-pre-prod.yaml`
- **问题描述**: 
  - 第34-35行：`hosts: []` 空数组
  - 但 TLS secret 已定义
  - 配置不一致，可能导致部署问题
- **建议修复**: 
  1. 填充正确的 hosts 数组
  2. 或移除未使用的 TLS 配置

#### 13. [文档] [高严重] [docs_zh 缺少11个 v4.3 合同文档]
- **文件/路径**: `docs_zh/contracts/`
- **问题描述**: 
  - 缺少 `events_and_checkpoints_contract.md`
  - 缺少 `smtp_contract.md`
  - 缺少 `v4_3_budget_ledger_contract.md`
  - 缺少 `v4_3_decision_and_hitl_contract.md`
  - 缺少 `v4_3_event_envelope_contract.md`
  - 缺少 `v4_3_harness_run_contract.md`
  - 缺少 `v4_3_node_run_attempt_receipt_contract.md`
  - 缺少 `v4_3_plan_graph_and_patch_contract.md`
  - 缺少 `v4_3_side_effect_reconciliation_contract.md`
  - 缺少 `v4_3_task_intake_and_request_contract.md`
  - 缺少 `v4_3_version_lock_contract.md`
  - 这些是 v4.3 contract freeze 文档
- **建议修复**: 
  1. 翻译缺失的11个合同文档到中文
  2. 确保 docs_zh 和 docs_en 保持同步

#### 14. [文档] [中严重] [docs_zh 缺少 ADR-109 v4.3 contract freeze]
- **文件/路径**: `docs_zh/adr/`
- **问题描述**: 
  - `docs_en/adr/` 有 `109-v4.3-contract-freeze.md`
  - `docs_zh/adr/` 缺少此文件
  - ADR 编号不连续
- **建议修复**: 
  1. 翻译 ADR-109 到中文
  2. 确保 ADR 编号连续

#### 15. [文档] [中严重] [UI README 引用错误的文档路径]
- **文件/路径**: `ui/README.md`
- **问题描述**: 
  - README 引用 `docs_zh/architecture/05-cross-platform-ui-architecture.md`
  - 但实际文件在 `docs_en/architecture/05-cross-platform-ui-architecture.md`
  - 文档本身是英文的
- **建议修复**: 
  1. 修复跨引用路径
  2. 或翻译该文档到中文

#### 16. [测试] [高严重] [98个测试文件超过1000行]
- **文件/路径**: `tests/` 多个目录
- **问题描述**: 
  - 98+ 文件超过1000行
  - 最大测试文件 `ops-cli.test.ts` 达3921行
  - 过度庞大的测试文件难以维护
- **建议修复**: 
  1. 拆分超大型测试文件
  2. 按功能将测试分组到多个文件
  3. 遵循单一职责原则

#### 17. [测试] [高严重] [1179个测试使用 only/skip 标记]
- **文件/路径**: `tests/` 多个测试文件
- **问题描述**: 
  - 1179个测试包含 `only` 或 `skip` 标记
  - 表明有大量已知失败的测试
  - 可能掩盖真实问题并拖慢CI
- **建议修复**: 
  1. 逐个审查 skip/only 标记的原因
  2. 修复底层问题或移除标记
  3. 建立测试稳定性标准

#### 18. [测试] [高严重] [2700个测试过度使用 Mock]
- **文件/路径**: `tests/` 多个测试文件
- **问题描述**: 
  - 2700个测试使用 jest.mock、sinon、mock()
  - 过度 mock 可能导致测试不是真正的单元测试
  - 可能无法捕获真实集成问题
- **建议修复**: 
  1. 审查 mock 使用情况
  2. 增加更多集成测试
  3. 使用真实组件替代部分 mock

#### 19. [测试] [中严重] [很多 Domain 领域缺少测试覆盖]
- **文件/路径**: `tests/unit/domains/`
- **问题描述**: 
  - `supply-chain`: 1 test
  - `risk-profile`: 1 test
  - `quant-trading`: 1 test
  - `quality-assurance`: 1 test
  - `project-management`: 1 test
  - `marketing`: 1 test
  - 很多领域：0 tests
  - 测试覆盖严重不平衡
- **建议修复**: 
  1. 为每个领域添加基本测试
  2. 优先覆盖核心业务领域
  3. 建立最低测试覆盖率要求

#### 20. [源代码] [中严重] [372个 index.ts barrel 文件]
- **文件/路径**: `src/` 多个目录
- **问题描述**: 
  - 372个 index.ts 文件用于 barrel exports
  - 过多的 barrel 文件损害模块化
  - 更改一个模块可能触发大规模重编译
- **建议修复**: 
  1. 审查并减少 barrel 文件
  2. 直接从源文件导入
  3. 按需使用 barrel files

#### 21. [源代码] [中严重] [同步/异步服务对可能存在死代码]
- **文件/路径**: `src/platform/` 多个服务
- **问题描述**: 
  - `human-takeover-service.ts` / `human-takeover-service-async.ts`
  - `execution-dispatch-service.ts` / `execution-dispatch-service-async.ts`
  - `execution-worker-handshake-service.ts` / `execution-worker-handshake-service-async.ts`
  - `execution-worker-writeback-service.ts` / `execution-worker-writeback-service-async.ts`
  - 不清楚每对中哪个是活跃的
- **建议修复**: 
  1. 分析每对服务的使用情况
  2. 移除死代码
  3. 统一为单一实现

#### 22. [部署] [中严重] [deploy/terraform/environments/multi-region/ 只有README]
- **文件/路径**: `deploy/terraform/environments/multi-region/`
- **问题描述**: 
  - 只有 README.md 文件
  - 缺少实际的 tfvars 配置文件
  - 多区域部署配置不完整
- **建议修复**: 
  1. 添加多区域 tfvars 配置
  2. 完善多区域部署文档
  3. 添加实际的 Terraform 配置

#### 23. [UI] [中严重] [UI Feature 模块无文档]
- **文件/路径**: `ui/packages/features/`
- **问题描述**: 
  - 30个 feature 模块无独立文档
  - 包括：agent-manager, analytics, approval, audit, compliance, conversation, cost-center, dashboard, dispatch, domain-wizard, explainability, feature-flags, governance-compliance, health, hitl, incidents, inspect, marketplace, policy, queues, settings, stability, takeover, task-cockpit, workers, workflow-builder, workflow-cockpit, workflow-debugger
- **建议修复**: 
  1. 为每个 feature 模块添加文档
  2. 说明功能、API、用法
  3. 建立 feature 文档标准

#### 24. [配置] [低严重] [docker-compose.yml 安全配置缺失]
- **文件/路径**: `docker-compose.yml`
- **问题描述**: 
  - 第17行 `AA_API_JWT_SECRET` 被注释
  - 生产环境需要配置但未提供
  - alertmanager 服务缺少 health check
  - Prometheus 配置路径引用 `./deploy/prometheus/` 但未验证存在
- **建议修复**: 
  1. 添加 JWT_SECRET 配置说明
  2. 为 alertmanager 添加 health check
  3. 验证 Prometheus 配置路径

#### 25. [源代码] [低严重] [package.json 有100+个脚本无分组]
- **文件/路径**: `package.json`
- **问题描述**: 
  - 100+ npm scripts 在单一 section
  - 没有分组注释区分逻辑组
  - 29个 stable:* 命令遵循相同模式
  - 命名不一致（有的用 `:`，有的不用）
- **建议修复**: 
  1. 添加分组注释（build, test, cli tools, stable commands）
  2. 合并重复的 stable:* 脚本
  3. 统一命名规范

#### 26. [文档] [低严重] [UI packages/storybook/ 路径不存在]
- **文件/路径**: `ui/packages/storybook/`
- **问题描述**: 
  - `packages/storybook/` 路径不存在或为空
  - 但 ui-core 有 `src/` 和 `stories/` 结构
  - 可能存在配置问题
- **建议修复**: 
  1. 验证 storybook 配置
  2. 修复路径问题
  3. 确保 Storybook 可以正常启动

#### 27. [文档] [低严重] [无平台特定 UI 适配指南]
- **文件/路径**: `ui/` 相关文档
- **问题描述**: 
  - UI 支持 web, mobile, tauri-linux, tauri-macos, electron-win
  - 缺少平台特定适配指南
  - 不同平台有不同的要求
- **建议修复**: 
  1. 创建平台适配文档
  2. 说明各平台的特殊考虑
  3. 添加平台特定开发指南

#### 28. [配置] [低严重] [deploy/kubernetes/ 目录缺失]
- **文件/路径**: `deploy/`
- **问题描述**: 
  - 没有 `kubernetes/` 目录
  - 只有 Helm charts
  - 可能需要原始 k8s manifests
- **建议修复**: 
  1. 如果需要，添加 kubernetes manifests
  2. 或确认 Helm chart 足够
  3. 文档化部署方式选择

#### 29. [配置] [低严重] [deploy/chaos/ 实验性配置]
- **文件/路径**: `deploy/chaos/`
- **问题描述**: 
  - network-delay.yaml, pod-kill.yaml, postgres-disconnect.yaml, redis-disconnect.yaml
  - Chaos engineering 配置
  - 需要谨慎使用
- **建议修复**: 
  1. 添加 chaos 实验的审批流程
  2. 文档化使用场景
  3. 添加安全 guardrails

#### 30. [文档] [低严重] [docs_zh/reviews/ 内容远少于 docs_en]
- **文件/路径**: `docs_zh/reviews/` vs `docs_en/reviews/`
- **问题描述**: 
  - `docs_en/reviews/` 有6个文件，包括2.3MB的 audit 文档
  - `docs_zh/reviews/` 只有3个文件
  - 内容严重不平衡
- **建议修复**: 
  1. 翻译或复制 audit 文档到 docs_zh
  2. 平衡两侧内容

### 总结

本次补充Review（第九轮 - 深度Review - 2026-05-14）发现了30个新问题，比上一轮更深入。

**高优先级 (需要立即处理)**:
1. npm audit 发现7个安全漏洞
2. TypeScript 编译存在50+错误
3. 多个依赖版本严重落后
4. 安全配置在环境间不一致
5. .env.example 中JWT_SECRET为空
6. src/platform 目录职责混乱
7. 巨型 barrel file (index.ts 2317行)
8. core/runtime 是空壳
9. 98个测试文件超过1000行
10. 1179个测试使用 only/skip 标记
11. 2700个测试过度使用 Mock
12. docs_zh 缺少11个 v4.3 合同文档

**中优先级**:
1. 5个符号链接存在且未文档化
2. Terraform VPC CIDR 硬编码
3. Helm values 中 OTEL 配置不一致
4. Helm values 中 Ingress hosts 为空数组
5. docs_zh 缺少 ADR-109
6. UI README 引用错误的文档路径
7. 很多 Domain 领域缺少测试覆盖
8. 372个 index.ts barrel 文件
9. 同步/异步服务对可能存在死代码
10. deploy/terraform/environments/multi-region/ 只有README
11. UI Feature 模块无文档

**低优先级**:
1. docker-compose.yml 安全配置缺失
2. package.json 有100+个脚本无分组
3. UI packages/storybook/ 路径不存在
4. 无平台特定 UI 适配指南
5. deploy/kubernetes/ 目录缺失
6. deploy/chaos/ 实验性配置
7. docs_zh/reviews/ 内容远少于 docs_en

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 45 | 48 | 21 | 114 |
| 测试 | 8 | 11 | 3 | 22 |
| 配置 | 8 | 28 | 15 | 51 |
| 安全 | 6 | 6 | 1 | 13 |
| 文档 | 2 | 7 | 3 | 12 |
| UI | 0 | 3 | 3 | 6 |
| 部署 | 0 | 8 | 4 | 12 |
| **合计** | **69** | **111** | **50** | **230** |

### 优先修复建议

**立即处理 (本周内)**:
1. 运行 `npm audit fix --force` 修复高严重安全漏洞
2. 同步 TypeScript 类型和测试文件（修复50+编译错误）
3. 更新 .gitignore 添加缺失模式
4. 统一 config/security/ 环境配置
5. 翻译缺失的11个 v4.3 合同文档

**短期内处理 (本月内)**:
1. 制定依赖升级计划（zod 3->4, typescript 5->6, eslint 9->10）
2. 重构巨型源文件和测试文件
3. 减少 barrel files (index.ts)
4. 清理死代码（同步/异步服务对）
5. 扩展 UI 项目文档
6. 添加 Domain 领域测试覆盖

**长期规划**:
1. 系统性重构 src/platform 目录结构
2. 清理或明确 src/core/runtime 的用途
3. 统一 EventEmitter 到 TypedEventBus
4. 建立文档与代码的一致性检查机制
5. 完善 deploy/ 各子目录内容

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十五轮 - 构建测试与类型系统Review）

### 发现的新问题

#### 1. [构建] [高严重] [无 TypeScript 增量编译缓存]
- **文件/路径**: `tsconfig.json`
- **问题描述**: 
  - 无 `incremental: true` 配置
  - 无 `tsbuildinfo` 文件
  - 每次构建是完整重建（9.6秒）
  - TypeScript 无法使用增量编译
- **建议修复**: 
  1. 在 tsconfig.json 添加 `"incremental": true`
  2. 添加 `"tsbuildinfo": ".tsbuildinfo"` 到 .gitignore
  3. 实现项目引用以支持并行构建

#### 2. [构建] [高严重] [76个 npm scripts 大部分重复构建]
- **文件/路径**: `package.json`
- **问题描述**: 
  - 40+ CLI scripts 都运行 `npm run build && node ...`
  - 每个脚本前都执行完整重建
  - 无法直接运行预构建的 CLI
  - 20+ stable:* 脚本遵循相同模式
- **建议修复**: 
  1. 创建 `build:cli` 脚本构建一次
  2. 让 CLI 脚本使用预构建的 dist 文件
  3. 添加 npm run dev 或类似的无需重建的执行方式

#### 3. [源代码] [高严重] [168处 any 类型使用]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - `stable-runner-factory.ts`: `opts: any` -> `any`
  - `billing-service-async.ts`: `input: any` 方法签名
  - 重试/超时工具使用 `(...args: any[]) => Promise<any>`
  - 绕过类型安全
- **建议修复**: 
  1. 使用泛型替代 any
  2. 替换为具体的输入类型
  3. 添加 ESLint 规则禁止 any

#### 4. [源代码] [高严重] [38处 @ts-ignore 指令]
- **文件/路径**: 多个文件
- **问题描述**: 
  - 14个 stability 相关文件有 `@ts-ignore ExecutionRecord type mismatch`
  - 5个 harness-sdk 文件有 `Partial<HarnessRun> doesn't have all required properties`
  - 2个文件有 `exactOptionalPropertyTypes` 问题
- **建议修复**: 
  1. 修复 ExecutionRecord 类型不匹配
  2. 完成 HarnessRun 类型的必需属性
  3. 移除 @ts-ignore 使用

#### 5. [源代码] [高严重] [ExecutionRecord 类型不匹配]
- **文件/路径**: `src/platform/stability/stable-evidence-bundle-support.ts:783` 等14个文件
- **问题描述**: 
  - 插入 execution record 时 TypeScript 拒绝类型
  - `ExecutionRecord` 接口要求新字段但 insert 对象缺失
  - 或 store 的 insert 方法接受不同类型
- **建议修复**: 
  1. 对齐 ExecutionRecord 接口和 store 类型
  2. 添加缺失字段到 insert 对象
  3. 或调整 store 的 insert 方法类型签名

#### 6. [源代码] [中严重] [30+处 as unknown as 双类型转换]
- **文件/路径**: `division-loader.ts`, `domain-baseline-catalog.ts`, `harness-sdk`
- **问题描述**: 
  - 完全绕过类型安全
  - 通常是类型架构问题的信号
- **建议修复**: 
  1. 修复上游类型问题
  2. 使用更具体的类型断言
  3. 重构类型层次结构

#### 7. [构建] [中严重] [rimraf  extraneous - 未在 package.json 声明]
- **文件/路径**: `node_modules/rimraf`
- **问题描述**: 
  - rimraf@6.1.3 存在于 node_modules 但未在 package.json 声明
  - 可能导致依赖问题
- **建议修复**: 
  1. 添加到 dependencies 或 devDependencies
  2. 或移除并使用其他清理方式

#### 8. [测试] [高严重] [178处直接 process.env 变更无抽象]
- **文件/路径**: `tests/unit/domains/registry/plugin-spi-registry.test.ts:640-687`
- **问题描述**: 
  - 直接读取/写入 process.env
  - 在 beforeEach/afterEach 中手动保存/恢复
  - 无 helper 抽象
- **建议修复**: 
  1. 创建 env helper 函数
  2. 统一保存和恢复模式
  3. 添加测试隔离验证

#### 9. [测试] [高严重] [21,998处 unlinkSync 调用无集中清理]
- **文件/路径**: 多个测试文件
- **问题描述**: 
  - 每个测试重新实现清理逻辑
  - 无集中清理 utility
  - 可能导致 EBUSY (Windows) 或并发访问问题
- **建议修复**: 
  1. 创建集中的测试清理 utility
  2. 使用 afterEach 确保清理
  3. 考虑使用临时目录

#### 10. [测试] [中严重] [无统一 mocking 框架]
- **文件/路径**: `tests/` 多个目录
- **问题描述**: 
  - 10,579 处 vi.fn, jest.mock, sinon 等调用
  - 无统一的 mocking 框架可见
  - 模式不一致
- **建议修复**: 
  1. 建立统一的 mock factory
  2. 标准化 mock 模式
  3. 添加 mocking 最佳实践文档

#### 11. [测试] [中严重] [Singleton 状态重置未标准化]
- **文件/路径**: `tests/unit/platform/five-plane-execution/dispatcher.test.ts`
- **问题描述**: 
  - 测试通过特定函数重置 singletons
  - 每个测试重复自己的 reset 逻辑
  - 无统一的 resetAllSingletons()
- **建议修复**: 
  1. 创建统一的 singleton reset 机制
  2. 在 afterEach 中自动调用
  3. 文档化 singleton 重置要求

#### 12. [测试] [低严重] [无分支覆盖率要求]
- **文件/路径**: `.c8rc.json`
- **问题描述**: 
  - `"100": false` - 无覆盖率要求
  - 只测量源代码，不测量测试文件
- **建议修复**: 
  1. 添加分支覆盖率阈值
  2. 在 CI 中强制执行覆盖率 gate
  3. 关注关键代码路径

#### 13. [网络] [高严重] [ScopedExternalAccessSandbox performHttpRequest 无超时]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/scoped-external-access-sandbox.ts:289`
- **问题描述**: 
  - fetch 调用无 timeout 参数
  - 挂起的请求可能永久阻塞
- **建议修复**: 
  1. 添加 AbortController timeout
  2. 使用标准的超时模式
  3. 记录超时配置

#### 14. [网络] [高严重] [OIDC 服务 fetch 调用无超时]
- **文件/路径**: `src/org-governance/sso-scim/oidc/oidc-service.ts:565,272`
- **问题描述**: 
  - OIDC token 交换无 retry logic
  - 无 timeout/abort signal
  - 认证流程可能挂起
- **建议修复**: 
  1. 添加 fetch timeout
  2. 添加 retry logic
  3. 使用标准的超时配置

#### 15. [网络] [高严重] [plugin-runtime-child 替换 globalThis.fetch 为 no-op]
- **文件/路径**: `src/domains/registry/plugin-runtime-child.ts:112`
- **问题描述**: 
  ```typescript
  globalThis.fetch = (async () => {}) as unknown as typeof globalThis.fetch;
  ```
  - 插件上下文中完全替换 fetch 为空函数
  - 插件的所有出站 HTTP 请求都会失败
  - 看起来是有意为之但可能造成问题
- **建议修复**: 
  1. 确认这是有意行为
  2. 文档化 fetch 替换行为
  3. 确保不会意外影响其他代码

#### 16. [网络] [中严重] [无显式 http.Agent 配置]
- **文件/路径**: 全局
- **问题描述**: 
  - 无显式的 http.Agent/https.Agent 配置
  - 依赖 Node.js 默认连接池
  - 无 maxSockets 或 maxConnections 配置
  - 高吞吐场景可能出问题
- **建议修复**: 
  1. 添加 http Agent 配置
  2. 文档化连接池大小
  3. 监控连接使用情况

#### 17. [序列化] [中严重] [合同版本硬编码为字符串字面量]
- **文件/路径**: `src/platform/contracts/inter-plane-contract-gateway.ts:184`
- **问题描述**: 
  - `schemaVersion: "v4.3"` 硬编码
  - 难以追踪合同版本演变
- **建议修复**: 
  1. 提取到类型化常量或 enum
  2. 集中管理合同版本
  3. 添加版本兼容性检查

#### 18. [序列化] [低严重] [Timestamp = string 丢失精度]
- **文件/路径**: `src/platform/contracts/types/domain/primitives.ts:10`
- **问题描述**: 
  - `type Timestamp = string` 无类型精度
  - ISO 8601 字符串无时区信息
- **建议修复**: 
  1. 使用 Date 对象或 branded string 类型
  2. 明确时区处理约定
  3. 文档化时间戳语义

#### 19. [源代码] [中严重] [29处非空断言 !.]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - 使用 `!.` 断言非空
  - 可能导致运行时错误
- **建议修复**: 
  1. 使用可选链 `?.` 或空值合并 `??`
  2. 添加适当的 null 检查
  3. 减少非空断言使用

#### 20. [API] [中严重] [API 路由 schema 版本硬编码]
- **文件/路径**: `src/platform/five-plane-interface/api/federation-routing-service.ts`
- **问题描述**: 
  - Zod schema 验证存在
  - 但 schema 版本未与 contract 版本同步
- **建议修复**: 
  1. 从 contract 版本派生 schema 版本
  2. 添加版本一致性检查
  3. 文档化版本关系

### 总结

本次补充Review（第十五轮 - 构建测试与类型系统Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. 无 TypeScript 增量编译缓存（每次完整重建9.6秒）
2. 76个 npm scripts 大部分重复构建
3. 168处 any 类型使用（绕过类型安全）
4. 38处 @ts-ignore 指令
5. ExecutionRecord 类型不匹配（14个文件）
6. 178处直接 process.env 变更无抽象
7. ScopedExternalAccessSandbox performHttpRequest 无超时
8. OIDC 服务 fetch 调用无超时
9. plugin-runtime-child 替换 globalThis.fetch 为 no-op

**中优先级**:
1. 30+处 as unknown as 双类型转换
2. rimraf extraneous 未声明
3. 21,998处 unlinkSync 无集中清理
4. 无统一 mocking 框架
5. Singleton 状态重置未标准化
6. 无显式 http.Agent 配置
7. 合同版本硬编码为字符串
8. Timestamp = string 丢失精度
9. 非空断言使用

**低优先级**:
1. 无分支覆盖率要求
2. API 路由 schema 版本未同步

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 72 | 87 | 39 | 198 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **109** | **182** | **85** | **376** |

### 优先修复建议

**立即处理 (本周内)**:
1. 添加 TypeScript 增量编译 (`incremental: true`)
2. 重构 npm scripts 避免重复构建
3. 减少 any 类型使用到 <50
4. 移除所有 @ts-ignore 指令
5. 修复 ExecutionRecord 类型不匹配
6. 添加 fetch 超时到 OIDC 和 sandbox

**短期内处理 (本月内)**:
1. 创建集中的测试清理 utility
2. 建立统一的 mocking 框架
3. 标准化 singleton 重置机制
4. 添加 http.Agent 连接池配置
5. 提取合同版本到类型化常量

**长期规划**:
1. 实现完整的类型安全（无 any）
2. 建立测试基础设施标准
3. 实现构建缓存优化
4. 完善网络层超时和重试
5. 建立覆盖率门禁标准

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十四轮 - 日志事件与工作流Review）

### 发现的新问题

#### 1. [可观测性] [高严重] [StructuredLogEntry 无 requestId 字段]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - StructuredLogEntry 接口无 requestId 字段
  - 只有 correlationId，它回退到 traceId
  - 无法独立追踪单个 HTTP 请求
- **建议修复**: 
  1. 在 StructuredLogEntry 添加 requestId 字段
  2. 在 HTTP 中间件中生成和注入 requestId
  3. 确保 requestId 流经所有日志调用

#### 2. [可观测性] [高严重] [StructuredLogger data 字段无自动清洗]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - 如果开发者传递 `{ password: "..." }` 给 logger.info，密码会被记录
  - 无字段级脱敏机制
  - 依赖开发者手动排除敏感字段
- **建议修复**: 
  1. 添加 OBSERVE_OUTPUT_BLACKLIST 类似的黑名单
  2. 实现 redact/mask/sanitize 工具函数
  3. 在日志传输前自动清洗敏感字段

#### 3. [可观测性] [中严重] [日志中敏感数据无 PII 处理]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - 无 PII 重写工具
  - 开发者必须手动避免记录敏感数据
  - 存在意外记录敏感信息的风险
- **建议修复**: 
  1. 添加 PII 检测和重写工具
  2. 在文档中明确记录敏感字段列表
  3. 添加测试验证敏感数据不被记录

#### 4. [可观测性] [中严重] [59处 console.* 直接调用]
- **文件/路径**: 多个源文件
- **问题描述**: 
  - 应该使用 StructuredLogger
  - console 调用可能不被日志聚合系统收集
  - 难以追踪和关联
- **建议修复**: 
  1. 替换所有 console.* 为 StructuredLogger
  2. 添加 ESLint 规则禁止 console.*
  3. 保留少量允许的 CLI 输出

#### 5. [可观测性] [中严重] [部分 logger.warn 调用缺少结构化数据]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:122`
- **问题描述**: 
  ```typescript
  logger.warn("WebSocket connection rejected: missing subprotocol token")
  ```
  - 只有消息，无 data 对象
  - 难以关联和查询
- **建议修复**: 
  1. 添加 `{ data: { ... } }` 对象
  2. 确保所有日志包含上下文数据
  3. 添加日志审查工具

#### 6. [源代码] [高严重] [Harness while(true) 循环无硬性迭代上限]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:1442`
- **问题描述**: 
  ```typescript
  while (true) {
    // ...
    // 依赖 budget gate 和 guardrail vibration 退出
    // 如果两者都失败，循环永久继续
  }
  ```
  - 依赖 budget gate 和 guardrail vibration 退出
  - 无硬性迭代次数限制
  - 可能导致无限循环
- **建议修复**: 
  1. 添加硬性 maxIterations 上限
  2. 在达到上限时强制退出
  3. 记录详细的迭代统计用于调试

#### 7. [源代码] [高严重] [Oapeflir while(true) 循环可能无限重规划]
- **文件/路径**: `src/platform/five-plane-orchestration/oapeflir/oapeflir-loop-service.ts:355`
- **问题描述**: 
  - 如果 loopReplanDecision.shouldReplan 永不变为 false
  - 重规划永远无法减少计划错误
  - 可能导致无限循环
- **建议修复**: 
  1. 添加重规划次数上限
  2. 在达到上限时强制接受当前结果
  3. 添加重规划质量衰减检测

#### 8. [源代码] [中严重] [Recovery 流程允许终态转换到 paused]
- **文件/路径**: `src/platform/five-plane-execution/runtime-state-machine.ts:105-108`
- **问题描述**: 
  - completed, failed, cancelled, aborted 可转换到 paused 进行恢复
  - 创建恢复路径可能重新进入执行
  - 需要验证此行为的正确性
- **建议修复**: 
  1. 审查 recovery 转换的合法性
  2. 确保不会导致状态不一致
  3. 添加转换前置条件验证

#### 9. [国际化] [高严重] [API 错误消息硬编码英文]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/api-error.ts`
- **问题描述**: 
  ```typescript
  return new ApiError(404, "api.task_not_found", "Task not found.");
  ```
  - 错误消息硬编码英文
  - 无后端 i18n 机制
  - 非英文客户端收到不本地化的错误
- **建议修复**: 
  1. 使用错误码而非硬编码消息
  2. 客户端根据错误码本地化
  3. 或在后端添加 i18n 支持

#### 10. [国际化] [中严重] [UI 硬编码字符串未翻译]
- **文件/路径**: `ui/packages/features/conversation/src/web/index.tsx`
- **问题描述**: 
  - "Build Plan", "Execute", "Messages" 等按钮标签硬编码
  - 不使用 i18n 系统
- **建议修复**: 
  1. 使用 `translateFeatureCopy` 替换硬编码
  2. 确保所有用户可见文本通过 i18n
  3. 添加硬编码字符串检测

#### 11. [国际化] [中严重] [Arabic 目录不完整]
- **文件/路径**: `ui/packages/shared/i18n/src/catalogs/ar-SA.ts`
- **问题描述**: 
  - ar-SA 只有约18条消息
  - en-US 和 zh-CN 有约117条
  - 缺失的 key 返回 key 本身
- **建议修复**: 
  1. 补充 Arabic 翻译
  2. 确保所有 key 都有翻译
  3. 添加翻译完整性测试

#### 12. [国际化] [低严重] [translateFeatureCopy 无回退默认值]
- **文件/路径**: `ui/packages/shared/i18n/`
- **问题描述**: 
  - 如果 feature ID 不在 catalog 中
  - 返回 undefined title/summary
  - 无合理回退
- **建议修复**: 
  1. 添加回退到 key 或默认字符串
  2. 或使用 feature ID 作为显示名
  3. 确保 UI 不显示 undefined

#### 13. [运维] [高严重] [健康检查无 readiness/liveness 区分]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts`
- **问题描述**: 
  - /healthz, /v1/healthz, /health 返回相同报告
  - 无 readiness probe（可处理流量？）
  - 无 liveness probe（系统存活？）
  - canAcceptTraffic() 未通过 HTTP 暴露
- **建议修复**: 
  1. 添加 /ready 和 /live 端点
  2. Readiness 检查依赖项就绪
  3. Liveness 检查进程健康

#### 14. [运维] [高严重] [shutdown 期间健康检查存在竞态]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts`
- **问题描述**: 
  - getHealthReportAsync() 异步检查 dbWritable
  - shutdown 期间数据库连接可能关闭
  - 健康检查仍在传入
- **建议修复**: 
  1. 在 shutdown 时返回 unhealthy
  2. 添加 shutdown 状态标志
  3. 停止接受新的健康检查请求

#### 15. [运维] [高严重] [LeaderElectionService 未与 GracefulShutdown 集成]
- **文件/路径**: `src/platform/five-plane-execution/ha/leader-election-service.ts`
- **问题描述**: 
  - LeaderElectionService.stop() 处理优雅退位
  - 但未在全局 graceful shutdown 中注册
  - 需显式注册为 shutdown handler
- **建议修复**: 
  1. 将 LeaderElectionService 添加到 shutdown handlers
  2. 确保领导者在 shutdown 时让位
  3. 验证关闭顺序正确

#### 16. [运维] [中严重] [addHandler() 无显式排序约定]
- **文件/路径**: `src/platform/five-plane-execution/startup/graceful-shutdown.ts`
- **问题描述**: 
  - 虽然 handlers 逆序执行
  - 但无显式接口声明 handler 依赖关系
  - 关键 handlers（如关闭数据库连接）应最后运行
- **建议修复**: 
  1. 添加优先级或阶段参数
  2. 文档化关键 handler 顺序要求
  3. 添加顺序验证测试

#### 17. [运维] [中严重] [unref'd timers 可能不触发]
- **文件/路径**: `src/platform/five-plane-execution/startup/process-error-handlers.ts:106`
- **问题描述**: 
  - `setTimeout(...).unref()` 不保持事件循环活跃
  - 如果主线程空闲，强制退出可能不触发
- **建议修复**: 
  1. 移除 .unref() 除非确实需要
  2. 或确保有其他方式保持事件循环
  3. 添加超时触发验证测试

#### 18. [运维] [中严重] [StartupConsistencyChecker 阻塞流量但不暴露状态]
- **文件/路径**: `src/platform/five-plane-startup-plan.ts`
- **问题描述**: 
  - canAcceptTraffic() 在 P0 问题存在时返回 false
  - 但 _trafficBlocked 是内部状态
  - 负载均衡器访问 /healthz 仍会得到 "ok"
- **建议修复**: 
  1. 在健康报告中暴露 trafficBlocked 状态
  2. 或添加单独的健康端点指示就绪状态
  3. 确保负载均衡器在阻塞时不路由流量

#### 19. [事件系统] [中严重] [TypedEventBus vs EventEmitter 混用]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - 项目同时使用 TypedEventBus 和原生 EventEmitter
  - TypedEventBus 应该是标准
  - 混用可能导致类型安全问题
- **建议修复**: 
  1. 创建迁移计划统一使用 TypedEventBus
  2. 添加 ESLint 规则禁止原生 EventEmitter
  3. 在 CI 中检测混用情况

#### 20. [源代码] [低严重] [无事件排序保证]
- **文件/路径**: `src/platform/five-plane-state-evidence/events/`
- **问题描述**: 
  - 无明确的事件排序保证文档
  - 事件处理可能乱序
  - 依赖方需要处理乱序
- **建议修复**: 
  1. 文档化事件排序语义
  2. 如果需要排序，实现序列号机制
  3. 在事件处理中考虑乱序情况

### 总结

本次补充Review（第十四轮 - 日志事件与工作流Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. StructuredLogEntry 无 requestId 字段
2. StructuredLogger data 字段无自动清洗（敏感数据风险）
3. Harness while(true) 循环无硬性迭代上限
4. Oapeflir while(true) 循环可能无限重规划
5. API 错误消息硬编码英文（无 i18n）
6. 健康检查无 readiness/liveness 区分
7. shutdown 期间健康检查存在竞态
8. LeaderElectionService 未与 GracefulShutdown 集成

**中优先级**:
1. 日志中敏感数据无 PII 处理
2. 59处 console.* 直接调用
3. 部分 logger.warn 调用缺少结构化数据
4. Recovery 流程允许终态转换到 paused
5. UI 硬编码字符串未翻译
6. Arabic 目录不完整
7. addHandler() 无显式排序约定
8. unref'd timers 可能不触发
9. StartupConsistencyChecker 阻塞流量但不暴露状态
10. TypedEventBus vs EventEmitter 混用

**低优先级**:
1. translateFeatureCopy 无回退默认值
2. 无事件排序保证

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 68 | 80 | 37 | 185 |
| 测试 | 8 | 14 | 5 | 27 |
| 配置 | 10 | 38 | 24 | 72 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **103** | **171** | **82** | **356** |

### 优先修复建议

**立即处理 (本周内)**:
1. 在 StructuredLogEntry 添加 requestId 字段
2. 实现日志敏感字段自动清洗
3. 为工作流循环添加硬性迭代上限
4. 添加 readiness/liveness 健康检查端点
5. 将 LeaderElectionService 集成到 GracefulShutdown
6. 实现 API 错误消息 i18n 支持

**短期内处理 (本月内)**:
1. 替换所有 console.* 为 StructuredLogger
2. 补充 Arabic i18n 目录
3. 修复 UI 硬编码字符串
4. 统一 TypedEventBus 替代 EventEmitter
5. 解决 shutdown 竞态条件
6. 添加事件排序保证文档

**长期规划**:
1. 建立完整的 PII 处理框架
2. 实现日志完整性验证
3. 建立工作流安全循环检测
4. 完善健康检查和就绪探测
5. 建立国际化完整测试

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十五轮 - 构建测试与类型系统Review）

### 发现的新问题

#### 1. [构建] [高严重] [无 TypeScript 增量编译缓存]
- **文件/路径**: `tsconfig.json`
- **问题描述**: 
  - 无 `incremental: true` 配置
  - 无 `tsbuildinfo` 文件
  - 每次构建是完整重建（9.6秒）
  - TypeScript 无法使用增量编译
- **建议修复**: 
  1. 在 tsconfig.json 添加 `"incremental": true`
  2. 添加 `"tsbuildinfo": ".tsbuildinfo"` 到 .gitignore
  3. 实现项目引用以支持并行构建

#### 2. [构建] [高严重] [76个 npm scripts 大部分重复构建]
- **文件/路径**: `package.json`
- **问题描述**: 
  - 40+ CLI scripts 都运行 `npm run build && node ...`
  - 每个脚本前都执行完整重建
  - 无法直接运行预构建的 CLI
  - 20+ stable:* 脚本遵循相同模式
- **建议修复**: 
  1. 创建 `build:cli` 脚本构建一次
  2. 让 CLI 脚本使用预构建的 dist 文件
  3. 添加 npm run dev 或类似的无需重建的执行方式

#### 3. [源代码] [高严重] [168处 any 类型使用]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - `stable-runner-factory.ts`: `opts: any` -> `any`
  - `billing-service-async.ts`: `input: any` 方法签名
  - 重试/超时工具使用 `(...args: any[]) => Promise<any>`
  - 绕过类型安全
- **建议修复**: 
  1. 使用泛型替代 any
  2. 替换为具体的输入类型
  3. 添加 ESLint 规则禁止 any

#### 4. [源代码] [高严重] [38处 @ts-ignore 指令]
- **文件/路径**: 多个文件
- **问题描述**: 
  - 14个 stability 相关文件有 `@ts-ignore ExecutionRecord type mismatch`
  - 5个 harness-sdk 文件有 `Partial<HarnessRun> doesn't have all required properties`
  - 2个文件有 `exactOptionalPropertyTypes` 问题
- **建议修复**: 
  1. 修复 ExecutionRecord 类型不匹配
  2. 完成 HarnessRun 类型的必需属性
  3. 移除 @ts-ignore 使用

#### 5. [源代码] [高严重] [ExecutionRecord 类型不匹配]
- **文件/路径**: `src/platform/stability/stable-evidence-bundle-support.ts:783` 等14个文件
- **问题描述**: 
  - 插入 execution record 时 TypeScript 拒绝类型
  - `ExecutionRecord` 接口要求新字段但 insert 对象缺失
  - 或 store 的 insert 方法接受不同类型
- **建议修复**: 
  1. 对齐 ExecutionRecord 接口和 store 类型
  2. 添加缺失字段到 insert 对象
  3. 或调整 store 的 insert 方法类型签名

#### 6. [源代码] [中严重] [30+处 as unknown as 双类型转换]
- **文件/路径**: `division-loader.ts`, `domain-baseline-catalog.ts`, `harness-sdk`
- **问题描述**: 
  - 完全绕过类型安全
  - 通常是类型架构问题的信号
- **建议修复**: 
  1. 修复上游类型问题
  2. 使用更具体的类型断言
  3. 重构类型层次结构

#### 7. [构建] [中严重] [rimraf  extraneous - 未在 package.json 声明]
- **文件/路径**: `node_modules/rimraf`
- **问题描述**: 
  - rimraf@6.1.3 存在于 node_modules 但未在 package.json 声明
  - 可能导致依赖问题
- **建议修复**: 
  1. 添加到 dependencies 或 devDependencies
  2. 或移除并使用其他清理方式

#### 8. [测试] [高严重] [178处直接 process.env 变更无抽象]
- **文件/路径**: `tests/unit/domains/registry/plugin-spi-registry.test.ts:640-687`
- **问题描述**: 
  - 直接读取/写入 process.env
  - 在 beforeEach/afterEach 中手动保存/恢复
  - 无 helper 抽象
- **建议修复**: 
  1. 创建 env helper 函数
  2. 统一保存和恢复模式
  3. 添加测试隔离验证

#### 9. [测试] [高严重] [21,998处 unlinkSync 调用无集中清理]
- **文件/路径**: 多个测试文件
- **问题描述**: 
  - 每个测试重新实现清理逻辑
  - 无集中清理 utility
  - 可能导致 EBUSY (Windows) 或并发访问问题
- **建议修复**: 
  1. 创建集中的测试清理 utility
  2. 使用 afterEach 确保清理
  3. 考虑使用临时目录

#### 10. [测试] [中严重] [无统一 mocking 框架]
- **文件/路径**: `tests/` 多个目录
- **问题描述**: 
  - 10,579 处 vi.fn, jest.mock, sinon 等调用
  - 无统一的 mocking 框架可见
  - 模式不一致
- **建议修复**: 
  1. 建立统一的 mock factory
  2. 标准化 mock 模式
  3. 添加 mocking 最佳实践文档

#### 11. [测试] [中严重] [Singleton 状态重置未标准化]
- **文件/路径**: `tests/unit/platform/five-plane-execution/dispatcher.test.ts`
- **问题描述**: 
  - 测试通过特定函数重置 singletons
  - 每个测试重复自己的 reset 逻辑
  - 无统一的 resetAllSingletons()
- **建议修复**: 
  1. 创建统一的 singleton reset 机制
  2. 在 afterEach 中自动调用
  3. 文档化 singleton 重置要求

#### 12. [测试] [低严重] [无分支覆盖率要求]
- **文件/路径**: `.c8rc.json`
- **问题描述**: 
  - `"100": false` - 无覆盖率要求
  - 只测量源代码，不测量测试文件
- **建议修复**: 
  1. 添加分支覆盖率阈值
  2. 在 CI 中强制执行覆盖率 gate
  3. 关注关键代码路径

#### 13. [网络] [高严重] [ScopedExternalAccessSandbox performHttpRequest 无超时]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/scoped-external-access-sandbox.ts:289`
- **问题描述**: 
  - fetch 调用无 timeout 参数
  - 挂起的请求可能永久阻塞
- **建议修复**: 
  1. 添加 AbortController timeout
  2. 使用标准的超时模式
  3. 记录超时配置

#### 14. [网络] [高严重] [OIDC 服务 fetch 调用无超时]
- **文件/路径**: `src/org-governance/sso-scim/oidc/oidc-service.ts:565,272`
- **问题描述**: 
  - OIDC token 交换无 retry logic
  - 无 timeout/abort signal
  - 认证流程可能挂起
- **建议修复**: 
  1. 添加 fetch timeout
  2. 添加 retry logic
  3. 使用标准的超时配置

#### 15. [网络] [高严重] [plugin-runtime-child 替换 globalThis.fetch 为 no-op]
- **文件/路径**: `src/domains/registry/plugin-runtime-child.ts:112`
- **问题描述**: 
  ```typescript
  globalThis.fetch = (async () => {}) as unknown as typeof globalThis.fetch;
  ```
  - 插件上下文中完全替换 fetch 为空函数
  - 插件的所有出站 HTTP 请求都会失败
  - 看起来是有意为之但可能造成问题
- **建议修复**: 
  1. 确认这是有意行为
  2. 文档化 fetch 替换行为
  3. 确保不会意外影响其他代码

#### 16. [网络] [中严重] [无显式 http.Agent 配置]
- **文件/路径**: 全局
- **问题描述**: 
  - 无显式的 http.Agent/https.Agent 配置
  - 依赖 Node.js 默认连接池
  - 无 maxSockets 或 maxConnections 配置
  - 高吞吐场景可能出问题
- **建议修复**: 
  1. 添加 http Agent 配置
  2. 文档化连接池大小
  3. 监控连接使用情况

#### 17. [序列化] [中严重] [合同版本硬编码为字符串字面量]
- **文件/路径**: `src/platform/contracts/inter-plane-contract-gateway.ts:184`
- **问题描述**: 
  - `schemaVersion: "v4.3"` 硬编码
  - 难以追踪合同版本演变
- **建议修复**: 
  1. 提取到类型化常量或 enum
  2. 集中管理合同版本
  3. 添加版本兼容性检查

#### 18. [序列化] [低严重] [Timestamp = string 丢失精度]
- **文件/路径**: `src/platform/contracts/types/domain/primitives.ts:10`
- **问题描述**: 
  - `type Timestamp = string` 无类型精度
  - ISO 8601 字符串无时区信息
- **建议修复**: 
  1. 使用 Date 对象或 branded string 类型
  2. 明确时区处理约定
  3. 文档化时间戳语义

#### 19. [源代码] [中严重] [29处非空断言 !.]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - 使用 `!.` 断言非空
  - 可能导致运行时错误
- **建议修复**: 
  1. 使用可选链 `?.` 或空值合并 `??`
  2. 添加适当的 null 检查
  3. 减少非空断言使用

#### 20. [API] [中严重] [API 路由 schema 版本硬编码]
- **文件/路径**: `src/platform/five-plane-interface/api/federation-routing-service.ts`
- **问题描述**: 
  - Zod schema 验证存在
  - 但 schema 版本未与 contract 版本同步
- **建议修复**: 
  1. 从 contract 版本派生 schema 版本
  2. 添加版本一致性检查
  3. 文档化版本关系

### 总结

本次补充Review（第十五轮 - 构建测试与类型系统Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. 无 TypeScript 增量编译缓存（每次完整重建9.6秒）
2. 76个 npm scripts 大部分重复构建
3. 168处 any 类型使用（绕过类型安全）
4. 38处 @ts-ignore 指令
5. ExecutionRecord 类型不匹配（14个文件）
6. 178处直接 process.env 变更无抽象
7. ScopedExternalAccessSandbox performHttpRequest 无超时
8. OIDC 服务 fetch 调用无超时
9. plugin-runtime-child 替换 globalThis.fetch 为 no-op

**中优先级**:
1. 30+处 as unknown as 双类型转换
2. rimraf extraneous 未声明
3. 21,998处 unlinkSync 无集中清理
4. 无统一 mocking 框架
5. Singleton 状态重置未标准化
6. 无显式 http.Agent 配置
7. 合同版本硬编码为字符串
8. Timestamp = string 丢失精度
9. 非空断言使用

**低优先级**:
1. 无分支覆盖率要求
2. API 路由 schema 版本未同步

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 72 | 87 | 39 | 198 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **109** | **182** | **85** | **376** |

### 优先修复建议

**立即处理 (本周内)**:
1. 添加 TypeScript 增量编译 (`incremental: true`)
2. 重构 npm scripts 避免重复构建
3. 减少 any 类型使用到 <50
4. 移除所有 @ts-ignore 指令
5. 修复 ExecutionRecord 类型不匹配
6. 添加 fetch 超时到 OIDC 和 sandbox

**短期内处理 (本月内)**:
1. 创建集中的测试清理 utility
2. 建立统一的 mocking 框架
3. 标准化 singleton 重置机制
4. 添加 http.Agent 连接池配置
5. 提取合同版本到类型化常量

**长期规划**:
1. 实现完整的类型安全（无 any）
2. 建立测试基础设施标准
3. 实现构建缓存优化
4. 完善网络层超时和重试
5. 建立覆盖率门禁标准

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十三轮 - 缓存会话与实时通信Review）

### 发现的新问题

#### 1. [安全] [严重] [CORS allowedMethods 缺少 PUT/PATCH/DELETE]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/response-hardening.ts:12-14`
- **问题描述**: 
  ```typescript
  export const DEFAULT_CORS_CONFIG: CorsConfig = {
    allowedMethods: ["GET", "POST", "OPTIONS"],  // 缺少 PUT, PATCH, DELETE
  ```
  - 浏览器 CORS preflight 会拒绝 PUT/PATCH/DELETE 请求
  - 但路由中有 PATCH /v1/tasks/:id, DELETE endpoints
- **建议修复**: 
  1. 添加 PUT, PATCH, DELETE 到 allowedMethods
  2. 确保 CORS 配置与应用路由方法一致
  3. 添加测试验证所有 HTTP 方法的 CORS preflight

#### 2. [安全] [严重] [Mission Routes 错误响应包装不当]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/mission-routes.ts:101,118-124`
- **问题描述**: 
  ```typescript
  return buildJsonResponse(ctx.requestId, 404, { error: { code: "MISSION_NOT_FOUND" } });
  ```
  - 使用 buildJsonResponse（成功包装）返回错误
  - 错误被包装在成功信封 `{requestId, data: {error: {...}}}` 中
  - 违反 REST 规范
- **建议修复**: 
  1. 使用 buildJsonErrorResponse 处理错误
  2. 确保所有错误路径使用正确的错误信封
  3. 添加 API 一致性测试

#### 3. [源代码] [高严重] [WebSocketBridge 无最大连接数限制]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:103-107`
- **问题描述**: 
  ```typescript
  this.wss = new WebSocketServer({
    server,
    path: WS_PATH,
    maxPayload: options.maxPayloadBytes ?? DEFAULT_MAX_PAYLOAD_BYTES,
    // 无 maxConnections 限制！
  });
  ```
  - 与 dashboard-websocket-server.ts 的 maxClients: 1000 不同
  - 恶意客户端可耗尽服务器资源
- **建议修复**: 
  1. 添加 maxConnections 限制
  2. 实现连接限制拒绝策略
  3. 添加连接计数指标

#### 4. [源代码] [高严重] [pendingAcks 在断开连接时未清理]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:268-282`
- **问题描述**: 
  - handleDisconnection 清理 subscribedTasks, taskSubscribers, slowConsumers
  - 但 pendingAcks Map 未清理
  - 断开的客户端未确认的消息永久留在内存
- **建议修复**: 
  1. 在 handleDisconnection 中删除所有 pendingAcks 条目
  2. 考虑添加超时机制自动清理未确认消息
  3. 记录清理操作的审计日志

#### 5. [安全] [高严重] [硬编码回退密钥]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/audit-event-integrity.ts:41`
- **问题描述**: 
  ```typescript
  const AUDIT_INTEGRITY_HMAC_KEY = process.env["AA_AUDIT_INTEGRITY_HMAC_KEY"] ?? "audit-integrity-secret-key-32-bytes!";
  ```
  - 占位符密钥用作回退
  - 如果环境变量未设置，使用不安全密钥
- **建议修复**: 
  1. 移除回退值，要求环境变量必须设置
  2. 在启动时验证密钥存在且足够强
  3. 如果密钥缺失导致启动失败

#### 6. [配置] [高严重] [config/runtime/test.json 缺少超时配置]
- **文件/路径**: `config/runtime/test.json`
- **问题描述**: 
  - 只有3个字段，缺少 maxConcurrentTasks, defaultTaskTimeoutMs, defaultStepTimeoutMs
  - 回退到默认值可能不适合测试环境
- **建议修复**: 
  1. 添加完整的 timeout/tuning 配置
  2. 确保测试环境有合理的默认值
  3. 文档化必需的配置字段

#### 7. [配置] [高严重] [prod.json 限制过严 - maxConcurrentTasks=1]
- **文件/路径**: `config/runtime/prod.json`
- **问题描述**: 
  - maxConcurrentTasks: 1（vs dev:8, staging:4, pre-prod:6）
  - defaultTaskTimeoutMs: 120000（vs dev:600000）
  - defaultStepTimeoutMs: 60000（vs dev:180000）
  - 生产环境严重受限，可能影响吞吐量
- **建议修复**: 
  1. 评估并调整生产限制
  2. 与业务需求匹配
  3. 添加生产容量测试

#### 8. [源代码] [高严重] [多个服务存在无界 Map 无驱逐策略]
- **文件/路径**: 多个域服务
- **问题描述**: 
  - `domain-knowledge-schema-service.ts:73-75`: schemas, sourceContent, sourceTimestamps 无限制
  - `domain-eval-framework-service.ts:91-95`: frameworks, qualityAxes 等无限制
  - `domain-recipe-service.ts:67-68`: recipes, versions 无限制
  - `domain-risk-profile-service.ts:51`: profiles 无限制
  - Session Maps (session-management.ts:83-89) 无自动清理
- **建议修复**: 
  1. 为所有 Map 实现 LRU 或 TTL 驱逐策略
  2. 添加后台清理任务
  3. 添加大小监控和告警

#### 9. [源代码] [中严重] [taskEventHistory Map 永不清理]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:91,548-555`
- **问题描述**: 
  - taskEventHistory 限制单任务历史为 200 条
  - 但 Map 本身从不清理
  - 取消订阅的任务历史永久保留
- **建议修复**: 
  1. 实现后台清理无订阅者任务的历史
  2. 添加任务订阅者监控
  3. 考虑使用 WeakMap 替代

#### 10. [配置] [中严重] [安全配置 drift - remoteWorkerRegistration 缺失]
- **文件/路径**: `config/security/dev.json`, `staging.json`, `pre-prod.json`
- **问题描述**: 
  - default.json 有完整的 remoteWorkerRegistration 配置
  - dev/staging/pre-prod 只有 approvalMode
  - prod 有 approvalMode 但无 remoteWorkerRegistration
  - 安全配置不一致
- **建议修复**: 
  1. 统一所有环境的 remoteWorkerRegistration
  2. 添加安全配置验证
  3. 确保最低安全基线

#### 11. [API] [中严重] [OpenAPI 端点响应格式不一致]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/utils.ts:207-215`
- **问题描述**: 
  - `/v1/openapi.json` 使用 buildJsonDocumentResponse（无包装）
  - 其他端点使用 buildJsonResponse（{requestId, data} 包装）
  - 客户端体验不一致
- **建议修复**: 
  1. 统一响应信封格式
  2. 文档化响应格式规范
  3. 添加响应格式验证测试

#### 12. [源代码] [中严重] [Redis 队列缺少 idempotency 索引支持]
- **文件/路径**: `src/platform/five-plane-execution/queue/redis-queue-adapter.ts`
- **问题描述**: 
  - SQLite 有部分唯一索引支持 idempotency
  - Redis 实现使用哈希索引但实现不完整
  - 可能导致重复消息
- **建议修复**: 
  1. 完善 Redis idempotency 实现
  2. 添加唯一索引验证
  3. 确保与 SQLite 行为一致

#### 13. [源代码] [中严重] [请求去重中间件使用内存存储]
- **文件/路径**: `src/platform/five-plane-interface/api/middleware/request-deduplication.ts`
- **问题描述**: 
  - 纯内存滑动窗口（Map<DeduplicationKey, DeduplicationEntry[]>）
  - 无外部存储
  - 多实例部署不共享
  - 重启后去重状态丢失
- **建议修复**: 
  1. 使用 Redis 替代内存存储
  2. 支持分布式去重
  3. 持久化去重状态

#### 14. [源代码] [中严重] [Cache 无 stampede 保护]
- **文件/路径**: 多个 cache 实现
- **问题描述**: 
  - MemoryCacheStore, ExperienceCacheService 无锁
  - 缓存未命中时可发生 thundering herd
  - 高并发下可能导致数据库过载
- **建议修复**: 
  1. 实现 single-flight 模式
  2. 添加请求排队机制
  3. 使用分布式锁保护缓存更新

#### 15. [源代码] [中严重] [EvidenceService eviction 仅在插入时触发]
- **文件/路径**: `src/platform/five-plane-state-evidence/memory/evidence-service.ts:202`
- **问题描述**: 
  - 清理仅在 record() 调用时触发
  - 空闲时无后台清理
  - 可能导致内存持续增长
- **建议修复**: 
  1. 添加定期后台清理任务
  2. 使用独立的清理线程
  3. 添加内存使用监控

#### 16. [源代码] [低严重] [无连接指标暴露]
- **文件/路径**: WebSocket 服务器
- **问题描述**: 
  - getClientCount() 存在但未通过 HTTP 端点暴露
  - 无 pendingAcks 队列深度指标
  - 无连接建立告警
- **建议修复**: 
  1. 通过 metrics 端点暴露连接指标
  2. 添加 pendingAcks 队列监控
  3. 添加连接数异常告警

#### 17. [源代码] [低严重] [无空闲客户端超时]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts`
- **问题描述**: 
  - 仅在心跳 Sweep 时检查 isAlive
  - 无 per-client 独立于心跳的空闲超时
  - 认证后从不发送消息的客户端只能通过心跳失败检测
- **建议修复**: 
  1. 添加 per-client idle timeout
  2. 独立于心跳间隔
  3. 配置可调整

#### 18. [源代码] [低严重] [订阅限制是 per-client 而非全局]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:244`
- **问题描述**: 
  - MAX_SUBSCRIPTIONS_PER_CLIENT = 100
  - 恶意客户端可订阅 100 个任务
  - 无全局任务订阅者限制
- **建议修复**: 
  1. 添加全局任务订阅者限制
  2. 实现 per-task 订阅者上限
  3. 添加反滥用检测

#### 19. [配置] [低严重] [config/runtime 无版本验证机制]
- **文件/路径**: `config/runtime/*.json`
- **问题描述**: 
  - 配置有 "version": "v4.3"
  - 但无 schema 版本验证
  - 加载时可能接受不兼容配置
- **建议修复**: 
  1. 添加 JSON Schema 验证
  2. 实现版本兼容性检查
  3. 启动时验证配置完整性

#### 20. [源代码] [低严重] [ImprovementCandidateRegistry splice 是 O(n)]
- **文件/路径**: `src/platform/five-plane-orchestration/improve/improvement-candidate-registry.ts:243`
- **问题描述**: 
  - accessOrder 数组的 splice 操作是 O(n)
  - 高频访问时可能性能问题
- **建议修复**: 
  1. 使用 LinkedList 替代数组
  2. 或使用 Map 维护访问顺序
  3. 性能测试验证

### 总结

本次补充Review（第十三轮 - 缓存会话与实时通信Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. CORS allowedMethods 缺少 PUT/PATCH/DELETE（浏览器请求会失败）
2. Mission Routes 错误响应包装不当
3. WebSocketBridge 无最大连接数限制（DoS风险）
4. pendingAcks 断开连接时未清理（内存泄漏）
5. 硬编码回退密钥（安全漏洞）
6. config/runtime/test.json 缺少超时配置
7. prod.json maxConcurrentTasks=1 限制过严
8. 多个服务存在无界 Map（内存泄漏风险）

**中优先级**:
1. taskEventHistory Map 永不清理
2. 安全配置 drift（remoteWorkerRegistration 缺失）
3. OpenAPI 端点响应格式不一致
4. Redis 队列 idempotency 实现不完整
5. 请求去重中间件使用内存存储（多实例不共享）
6. Cache 无 stampede 保护
7. EvidenceService eviction 仅在插入时触发

**低优先级**:
1. 无连接指标暴露
2. 无空闲客户端超时
3. 订阅限制是 per-client 而非全局
4. config/runtime 无版本验证机制
5. ImprovementCandidateRegistry splice O(n)

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 64 | 74 | 35 | 173 |
| 测试 | 8 | 14 | 5 | 27 |
| 配置 | 10 | 38 | 24 | 72 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 7 | 3 | 12 |
| UI | 0 | 3 | 3 | 6 |
| 部署 | 1 | 10 | 6 | 17 |
| **合计** | **99** | **161** | **79** | **339** |

### 优先修复建议

**立即处理 (本周内)**:
1. 修复 CORS allowedMethods 添加 PUT/PATCH/DELETE
2. 修复 Mission Routes 错误响应格式
3. 为 WebSocketBridge 添加最大连接数限制
4. 清理 pendingAcks 和 taskEventHistory
5. 移除硬编码回退密钥
6. 修复 config/runtime/test.json 配置
7. 评估并调整 prod 并发限制

**短期内处理 (本月内)**:
1. 统一安全配置（remoteWorkerRegistration）
2. 实现无界 Map 的 LRU/TTL 驱逐
3. 统一 API 响应信封格式
4. 完善 Redis idempotency 实现
5. 添加分布式去重中间件
6. 实现 cache stampede 保护

**长期规划**:
1. 建立完整的连接管理和监控
2. 实现后台清理任务框架
3. 添加配置 schema 验证
4. 优化高频繁操作的数据结构
5. 建立内存使用基线和告警

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十五轮 - 构建测试与类型系统Review）

### 发现的新问题

#### 1. [构建] [高严重] [无 TypeScript 增量编译缓存]
- **文件/路径**: `tsconfig.json`
- **问题描述**: 
  - 无 `incremental: true` 配置
  - 无 `tsbuildinfo` 文件
  - 每次构建是完整重建（9.6秒）
  - TypeScript 无法使用增量编译
- **建议修复**: 
  1. 在 tsconfig.json 添加 `"incremental": true`
  2. 添加 `"tsbuildinfo": ".tsbuildinfo"` 到 .gitignore
  3. 实现项目引用以支持并行构建

#### 2. [构建] [高严重] [76个 npm scripts 大部分重复构建]
- **文件/路径**: `package.json`
- **问题描述**: 
  - 40+ CLI scripts 都运行 `npm run build && node ...`
  - 每个脚本前都执行完整重建
  - 无法直接运行预构建的 CLI
  - 20+ stable:* 脚本遵循相同模式
- **建议修复**: 
  1. 创建 `build:cli` 脚本构建一次
  2. 让 CLI 脚本使用预构建的 dist 文件
  3. 添加 npm run dev 或类似的无需重建的执行方式

#### 3. [源代码] [高严重] [168处 any 类型使用]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - `stable-runner-factory.ts`: `opts: any` -> `any`
  - `billing-service-async.ts`: `input: any` 方法签名
  - 重试/超时工具使用 `(...args: any[]) => Promise<any>`
  - 绕过类型安全
- **建议修复**: 
  1. 使用泛型替代 any
  2. 替换为具体的输入类型
  3. 添加 ESLint 规则禁止 any

#### 4. [源代码] [高严重] [38处 @ts-ignore 指令]
- **文件/路径**: 多个文件
- **问题描述**: 
  - 14个 stability 相关文件有 `@ts-ignore ExecutionRecord type mismatch`
  - 5个 harness-sdk 文件有 `Partial<HarnessRun> doesn't have all required properties`
  - 2个文件有 `exactOptionalPropertyTypes` 问题
- **建议修复**: 
  1. 修复 ExecutionRecord 类型不匹配
  2. 完成 HarnessRun 类型的必需属性
  3. 移除 @ts-ignore 使用

#### 5. [源代码] [高严重] [ExecutionRecord 类型不匹配]
- **文件/路径**: `src/platform/stability/stable-evidence-bundle-support.ts:783` 等14个文件
- **问题描述**: 
  - 插入 execution record 时 TypeScript 拒绝类型
  - `ExecutionRecord` 接口要求新字段但 insert 对象缺失
  - 或 store 的 insert 方法接受不同类型
- **建议修复**: 
  1. 对齐 ExecutionRecord 接口和 store 类型
  2. 添加缺失字段到 insert 对象
  3. 或调整 store 的 insert 方法类型签名

#### 6. [源代码] [中严重] [30+处 as unknown as 双类型转换]
- **文件/路径**: `division-loader.ts`, `domain-baseline-catalog.ts`, `harness-sdk`
- **问题描述**: 
  - 完全绕过类型安全
  - 通常是类型架构问题的信号
- **建议修复**: 
  1. 修复上游类型问题
  2. 使用更具体的类型断言
  3. 重构类型层次结构

#### 7. [构建] [中严重] [rimraf  extraneous - 未在 package.json 声明]
- **文件/路径**: `node_modules/rimraf`
- **问题描述**: 
  - rimraf@6.1.3 存在于 node_modules 但未在 package.json 声明
  - 可能导致依赖问题
- **建议修复**: 
  1. 添加到 dependencies 或 devDependencies
  2. 或移除并使用其他清理方式

#### 8. [测试] [高严重] [178处直接 process.env 变更无抽象]
- **文件/路径**: `tests/unit/domains/registry/plugin-spi-registry.test.ts:640-687`
- **问题描述**: 
  - 直接读取/写入 process.env
  - 在 beforeEach/afterEach 中手动保存/恢复
  - 无 helper 抽象
- **建议修复**: 
  1. 创建 env helper 函数
  2. 统一保存和恢复模式
  3. 添加测试隔离验证

#### 9. [测试] [高严重] [21,998处 unlinkSync 调用无集中清理]
- **文件/路径**: 多个测试文件
- **问题描述**: 
  - 每个测试重新实现清理逻辑
  - 无集中清理 utility
  - 可能导致 EBUSY (Windows) 或并发访问问题
- **建议修复**: 
  1. 创建集中的测试清理 utility
  2. 使用 afterEach 确保清理
  3. 考虑使用临时目录

#### 10. [测试] [中严重] [无统一 mocking 框架]
- **文件/路径**: `tests/` 多个目录
- **问题描述**: 
  - 10,579 处 vi.fn, jest.mock, sinon 等调用
  - 无统一的 mocking 框架可见
  - 模式不一致
- **建议修复**: 
  1. 建立统一的 mock factory
  2. 标准化 mock 模式
  3. 添加 mocking 最佳实践文档

#### 11. [测试] [中严重] [Singleton 状态重置未标准化]
- **文件/路径**: `tests/unit/platform/five-plane-execution/dispatcher.test.ts`
- **问题描述**: 
  - 测试通过特定函数重置 singletons
  - 每个测试重复自己的 reset 逻辑
  - 无统一的 resetAllSingletons()
- **建议修复**: 
  1. 创建统一的 singleton reset 机制
  2. 在 afterEach 中自动调用
  3. 文档化 singleton 重置要求

#### 12. [测试] [低严重] [无分支覆盖率要求]
- **文件/路径**: `.c8rc.json`
- **问题描述**: 
  - `"100": false` - 无覆盖率要求
  - 只测量源代码，不测量测试文件
- **建议修复**: 
  1. 添加分支覆盖率阈值
  2. 在 CI 中强制执行覆盖率 gate
  3. 关注关键代码路径

#### 13. [网络] [高严重] [ScopedExternalAccessSandbox performHttpRequest 无超时]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/scoped-external-access-sandbox.ts:289`
- **问题描述**: 
  - fetch 调用无 timeout 参数
  - 挂起的请求可能永久阻塞
- **建议修复**: 
  1. 添加 AbortController timeout
  2. 使用标准的超时模式
  3. 记录超时配置

#### 14. [网络] [高严重] [OIDC 服务 fetch 调用无超时]
- **文件/路径**: `src/org-governance/sso-scim/oidc/oidc-service.ts:565,272`
- **问题描述**: 
  - OIDC token 交换无 retry logic
  - 无 timeout/abort signal
  - 认证流程可能挂起
- **建议修复**: 
  1. 添加 fetch timeout
  2. 添加 retry logic
  3. 使用标准的超时配置

#### 15. [网络] [高严重] [plugin-runtime-child 替换 globalThis.fetch 为 no-op]
- **文件/路径**: `src/domains/registry/plugin-runtime-child.ts:112`
- **问题描述**: 
  ```typescript
  globalThis.fetch = (async () => {}) as unknown as typeof globalThis.fetch;
  ```
  - 插件上下文中完全替换 fetch 为空函数
  - 插件的所有出站 HTTP 请求都会失败
  - 看起来是有意为之但可能造成问题
- **建议修复**: 
  1. 确认这是有意行为
  2. 文档化 fetch 替换行为
  3. 确保不会意外影响其他代码

#### 16. [网络] [中严重] [无显式 http.Agent 配置]
- **文件/路径**: 全局
- **问题描述**: 
  - 无显式的 http.Agent/https.Agent 配置
  - 依赖 Node.js 默认连接池
  - 无 maxSockets 或 maxConnections 配置
  - 高吞吐场景可能出问题
- **建议修复**: 
  1. 添加 http Agent 配置
  2. 文档化连接池大小
  3. 监控连接使用情况

#### 17. [序列化] [中严重] [合同版本硬编码为字符串字面量]
- **文件/路径**: `src/platform/contracts/inter-plane-contract-gateway.ts:184`
- **问题描述**: 
  - `schemaVersion: "v4.3"` 硬编码
  - 难以追踪合同版本演变
- **建议修复**: 
  1. 提取到类型化常量或 enum
  2. 集中管理合同版本
  3. 添加版本兼容性检查

#### 18. [序列化] [低严重] [Timestamp = string 丢失精度]
- **文件/路径**: `src/platform/contracts/types/domain/primitives.ts:10`
- **问题描述**: 
  - `type Timestamp = string` 无类型精度
  - ISO 8601 字符串无时区信息
- **建议修复**: 
  1. 使用 Date 对象或 branded string 类型
  2. 明确时区处理约定
  3. 文档化时间戳语义

#### 19. [源代码] [中严重] [29处非空断言 !.]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - 使用 `!.` 断言非空
  - 可能导致运行时错误
- **建议修复**: 
  1. 使用可选链 `?.` 或空值合并 `??`
  2. 添加适当的 null 检查
  3. 减少非空断言使用

#### 20. [API] [中严重] [API 路由 schema 版本硬编码]
- **文件/路径**: `src/platform/five-plane-interface/api/federation-routing-service.ts`
- **问题描述**: 
  - Zod schema 验证存在
  - 但 schema 版本未与 contract 版本同步
- **建议修复**: 
  1. 从 contract 版本派生 schema 版本
  2. 添加版本一致性检查
  3. 文档化版本关系

### 总结

本次补充Review（第十五轮 - 构建测试与类型系统Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. 无 TypeScript 增量编译缓存（每次完整重建9.6秒）
2. 76个 npm scripts 大部分重复构建
3. 168处 any 类型使用（绕过类型安全）
4. 38处 @ts-ignore 指令
5. ExecutionRecord 类型不匹配（14个文件）
6. 178处直接 process.env 变更无抽象
7. ScopedExternalAccessSandbox performHttpRequest 无超时
8. OIDC 服务 fetch 调用无超时
9. plugin-runtime-child 替换 globalThis.fetch 为 no-op

**中优先级**:
1. 30+处 as unknown as 双类型转换
2. rimraf extraneous 未声明
3. 21,998处 unlinkSync 无集中清理
4. 无统一 mocking 框架
5. Singleton 状态重置未标准化
6. 无显式 http.Agent 配置
7. 合同版本硬编码为字符串
8. Timestamp = string 丢失精度
9. 非空断言使用

**低优先级**:
1. 无分支覆盖率要求
2. API 路由 schema 版本未同步

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 72 | 87 | 39 | 198 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **109** | **182** | **85** | **376** |

### 优先修复建议

**立即处理 (本周内)**:
1. 添加 TypeScript 增量编译 (`incremental: true`)
2. 重构 npm scripts 避免重复构建
3. 减少 any 类型使用到 <50
4. 移除所有 @ts-ignore 指令
5. 修复 ExecutionRecord 类型不匹配
6. 添加 fetch 超时到 OIDC 和 sandbox

**短期内处理 (本月内)**:
1. 创建集中的测试清理 utility
2. 建立统一的 mocking 框架
3. 标准化 singleton 重置机制
4. 添加 http.Agent 连接池配置
5. 提取合同版本到类型化常量

**长期规划**:
1. 实现完整的类型安全（无 any）
2. 建立测试基础设施标准
3. 实现构建缓存优化
4. 完善网络层超时和重试
5. 建立覆盖率门禁标准

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十四轮 - 日志事件与工作流Review）

### 发现的新问题

#### 1. [可观测性] [高严重] [StructuredLogEntry 无 requestId 字段]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - StructuredLogEntry 接口无 requestId 字段
  - 只有 correlationId，它回退到 traceId
  - 无法独立追踪单个 HTTP 请求
- **建议修复**: 
  1. 在 StructuredLogEntry 添加 requestId 字段
  2. 在 HTTP 中间件中生成和注入 requestId
  3. 确保 requestId 流经所有日志调用

#### 2. [可观测性] [高严重] [StructuredLogger data 字段无自动清洗]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - 如果开发者传递 `{ password: "..." }` 给 logger.info，密码会被记录
  - 无字段级脱敏机制
  - 依赖开发者手动排除敏感字段
- **建议修复**: 
  1. 添加 OBSERVE_OUTPUT_BLACKLIST 类似的黑名单
  2. 实现 redact/mask/sanitize 工具函数
  3. 在日志传输前自动清洗敏感字段

#### 3. [可观测性] [中严重] [日志中敏感数据无 PII 处理]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - 无 PII 重写工具
  - 开发者必须手动避免记录敏感数据
  - 存在意外记录敏感信息的风险
- **建议修复**: 
  1. 添加 PII 检测和重写工具
  2. 在文档中明确记录敏感字段列表
  3. 添加测试验证敏感数据不被记录

#### 4. [可观测性] [中严重] [59处 console.* 直接调用]
- **文件/路径**: 多个源文件
- **问题描述**: 
  - 应该使用 StructuredLogger
  - console 调用可能不被日志聚合系统收集
  - 难以追踪和关联
- **建议修复**: 
  1. 替换所有 console.* 为 StructuredLogger
  2. 添加 ESLint 规则禁止 console.*
  3. 保留少量允许的 CLI 输出

#### 5. [可观测性] [中严重] [部分 logger.warn 调用缺少结构化数据]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:122`
- **问题描述**: 
  ```typescript
  logger.warn("WebSocket connection rejected: missing subprotocol token")
  ```
  - 只有消息，无 data 对象
  - 难以关联和查询
- **建议修复**: 
  1. 添加 `{ data: { ... } }` 对象
  2. 确保所有日志包含上下文数据
  3. 添加日志审查工具

#### 6. [源代码] [高严重] [Harness while(true) 循环无硬性迭代上限]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:1442`
- **问题描述**: 
  ```typescript
  while (true) {
    // ...
    // 依赖 budget gate 和 guardrail vibration 退出
    // 如果两者都失败，循环永久继续
  }
  ```
  - 依赖 budget gate 和 guardrail vibration 退出
  - 无硬性迭代次数限制
  - 可能导致无限循环
- **建议修复**: 
  1. 添加硬性 maxIterations 上限
  2. 在达到上限时强制退出
  3. 记录详细的迭代统计用于调试

#### 7. [源代码] [高严重] [Oapeflir while(true) 循环可能无限重规划]
- **文件/路径**: `src/platform/five-plane-orchestration/oapeflir/oapeflir-loop-service.ts:355`
- **问题描述**: 
  - 如果 loopReplanDecision.shouldReplan 永不变为 false
  - 重规划永远无法减少计划错误
  - 可能导致无限循环
- **建议修复**: 
  1. 添加重规划次数上限
  2. 在达到上限时强制接受当前结果
  3. 添加重规划质量衰减检测

#### 8. [源代码] [中严重] [Recovery 流程允许终态转换到 paused]
- **文件/路径**: `src/platform/five-plane-execution/runtime-state-machine.ts:105-108`
- **问题描述**: 
  - completed, failed, cancelled, aborted 可转换到 paused 进行恢复
  - 创建恢复路径可能重新进入执行
  - 需要验证此行为的正确性
- **建议修复**: 
  1. 审查 recovery 转换的合法性
  2. 确保不会导致状态不一致
  3. 添加转换前置条件验证

#### 9. [国际化] [高严重] [API 错误消息硬编码英文]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/api-error.ts`
- **问题描述**: 
  ```typescript
  return new ApiError(404, "api.task_not_found", "Task not found.");
  ```
  - 错误消息硬编码英文
  - 无后端 i18n 机制
  - 非英文客户端收到不本地化的错误
- **建议修复**: 
  1. 使用错误码而非硬编码消息
  2. 客户端根据错误码本地化
  3. 或在后端添加 i18n 支持

#### 10. [国际化] [中严重] [UI 硬编码字符串未翻译]
- **文件/路径**: `ui/packages/features/conversation/src/web/index.tsx`
- **问题描述**: 
  - "Build Plan", "Execute", "Messages" 等按钮标签硬编码
  - 不使用 i18n 系统
- **建议修复**: 
  1. 使用 `translateFeatureCopy` 替换硬编码
  2. 确保所有用户可见文本通过 i18n
  3. 添加硬编码字符串检测

#### 11. [国际化] [中严重] [Arabic 目录不完整]
- **文件/路径**: `ui/packages/shared/i18n/src/catalogs/ar-SA.ts`
- **问题描述**: 
  - ar-SA 只有约18条消息
  - en-US 和 zh-CN 有约117条
  - 缺失的 key 返回 key 本身
- **建议修复**: 
  1. 补充 Arabic 翻译
  2. 确保所有 key 都有翻译
  3. 添加翻译完整性测试

#### 12. [国际化] [低严重] [translateFeatureCopy 无回退默认值]
- **文件/路径**: `ui/packages/shared/i18n/`
- **问题描述**: 
  - 如果 feature ID 不在 catalog 中
  - 返回 undefined title/summary
  - 无合理回退
- **建议修复**: 
  1. 添加回退到 key 或默认字符串
  2. 或使用 feature ID 作为显示名
  3. 确保 UI 不显示 undefined

#### 13. [运维] [高严重] [健康检查无 readiness/liveness 区分]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts`
- **问题描述**: 
  - /healthz, /v1/healthz, /health 返回相同报告
  - 无 readiness probe（可处理流量？）
  - 无 liveness probe（系统存活？）
  - canAcceptTraffic() 未通过 HTTP 暴露
- **建议修复**: 
  1. 添加 /ready 和 /live 端点
  2. Readiness 检查依赖项就绪
  3. Liveness 检查进程健康

#### 14. [运维] [高严重] [shutdown 期间健康检查存在竞态]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts`
- **问题描述**: 
  - getHealthReportAsync() 异步检查 dbWritable
  - shutdown 期间数据库连接可能关闭
  - 健康检查仍在传入
- **建议修复**: 
  1. 在 shutdown 时返回 unhealthy
  2. 添加 shutdown 状态标志
  3. 停止接受新的健康检查请求

#### 15. [运维] [高严重] [LeaderElectionService 未与 GracefulShutdown 集成]
- **文件/路径**: `src/platform/five-plane-execution/ha/leader-election-service.ts`
- **问题描述**: 
  - LeaderElectionService.stop() 处理优雅退位
  - 但未在全局 graceful shutdown 中注册
  - 需显式注册为 shutdown handler
- **建议修复**: 
  1. 将 LeaderElectionService 添加到 shutdown handlers
  2. 确保领导者在 shutdown 时让位
  3. 验证关闭顺序正确

#### 16. [运维] [中严重] [addHandler() 无显式排序约定]
- **文件/路径**: `src/platform/five-plane-execution/startup/graceful-shutdown.ts`
- **问题描述**: 
  - 虽然 handlers 逆序执行
  - 但无显式接口声明 handler 依赖关系
  - 关键 handlers（如关闭数据库连接）应最后运行
- **建议修复**: 
  1. 添加优先级或阶段参数
  2. 文档化关键 handler 顺序要求
  3. 添加顺序验证测试

#### 17. [运维] [中严重] [unref'd timers 可能不触发]
- **文件/路径**: `src/platform/five-plane-execution/startup/process-error-handlers.ts:106`
- **问题描述**: 
  - `setTimeout(...).unref()` 不保持事件循环活跃
  - 如果主线程空闲，强制退出可能不触发
- **建议修复**: 
  1. 移除 .unref() 除非确实需要
  2. 或确保有其他方式保持事件循环
  3. 添加超时触发验证测试

#### 18. [运维] [中严重] [StartupConsistencyChecker 阻塞流量但不暴露状态]
- **文件/路径**: `src/platform/five-plane-startup-plan.ts`
- **问题描述**: 
  - canAcceptTraffic() 在 P0 问题存在时返回 false
  - 但 _trafficBlocked 是内部状态
  - 负载均衡器访问 /healthz 仍会得到 "ok"
- **建议修复**: 
  1. 在健康报告中暴露 trafficBlocked 状态
  2. 或添加单独的健康端点指示就绪状态
  3. 确保负载均衡器在阻塞时不路由流量

#### 19. [事件系统] [中严重] [TypedEventBus vs EventEmitter 混用]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - 项目同时使用 TypedEventBus 和原生 EventEmitter
  - TypedEventBus 应该是标准
  - 混用可能导致类型安全问题
- **建议修复**: 
  1. 创建迁移计划统一使用 TypedEventBus
  2. 添加 ESLint 规则禁止原生 EventEmitter
  3. 在 CI 中检测混用情况

#### 20. [源代码] [低严重] [无事件排序保证]
- **文件/路径**: `src/platform/five-plane-state-evidence/events/`
- **问题描述**: 
  - 无明确的事件排序保证文档
  - 事件处理可能乱序
  - 依赖方需要处理乱序
- **建议修复**: 
  1. 文档化事件排序语义
  2. 如果需要排序，实现序列号机制
  3. 在事件处理中考虑乱序情况

### 总结

本次补充Review（第十四轮 - 日志事件与工作流Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. StructuredLogEntry 无 requestId 字段
2. StructuredLogger data 字段无自动清洗（敏感数据风险）
3. Harness while(true) 循环无硬性迭代上限
4. Oapeflir while(true) 循环可能无限重规划
5. API 错误消息硬编码英文（无 i18n）
6. 健康检查无 readiness/liveness 区分
7. shutdown 期间健康检查存在竞态
8. LeaderElectionService 未与 GracefulShutdown 集成

**中优先级**:
1. 日志中敏感数据无 PII 处理
2. 59处 console.* 直接调用
3. 部分 logger.warn 调用缺少结构化数据
4. Recovery 流程允许终态转换到 paused
5. UI 硬编码字符串未翻译
6. Arabic 目录不完整
7. addHandler() 无显式排序约定
8. unref'd timers 可能不触发
9. StartupConsistencyChecker 阻塞流量但不暴露状态
10. TypedEventBus vs EventEmitter 混用

**低优先级**:
1. translateFeatureCopy 无回退默认值
2. 无事件排序保证

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 68 | 80 | 37 | 185 |
| 测试 | 8 | 14 | 5 | 27 |
| 配置 | 10 | 38 | 24 | 72 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **103** | **171** | **82** | **356** |

### 优先修复建议

**立即处理 (本周内)**:
1. 在 StructuredLogEntry 添加 requestId 字段
2. 实现日志敏感字段自动清洗
3. 为工作流循环添加硬性迭代上限
4. 添加 readiness/liveness 健康检查端点
5. 将 LeaderElectionService 集成到 GracefulShutdown
6. 实现 API 错误消息 i18n 支持

**短期内处理 (本月内)**:
1. 替换所有 console.* 为 StructuredLogger
2. 补充 Arabic i18n 目录
3. 修复 UI 硬编码字符串
4. 统一 TypedEventBus 替代 EventEmitter
5. 解决 shutdown 竞态条件
6. 添加事件排序保证文档

**长期规划**:
1. 建立完整的 PII 处理框架
2. 实现日志完整性验证
3. 建立工作流安全循环检测
4. 完善健康检查和就绪探测
5. 建立国际化完整测试

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十五轮 - 构建测试与类型系统Review）

### 发现的新问题

#### 1. [构建] [高严重] [无 TypeScript 增量编译缓存]
- **文件/路径**: `tsconfig.json`
- **问题描述**: 
  - 无 `incremental: true` 配置
  - 无 `tsbuildinfo` 文件
  - 每次构建是完整重建（9.6秒）
  - TypeScript 无法使用增量编译
- **建议修复**: 
  1. 在 tsconfig.json 添加 `"incremental": true`
  2. 添加 `"tsbuildinfo": ".tsbuildinfo"` 到 .gitignore
  3. 实现项目引用以支持并行构建

#### 2. [构建] [高严重] [76个 npm scripts 大部分重复构建]
- **文件/路径**: `package.json`
- **问题描述**: 
  - 40+ CLI scripts 都运行 `npm run build && node ...`
  - 每个脚本前都执行完整重建
  - 无法直接运行预构建的 CLI
  - 20+ stable:* 脚本遵循相同模式
- **建议修复**: 
  1. 创建 `build:cli` 脚本构建一次
  2. 让 CLI 脚本使用预构建的 dist 文件
  3. 添加 npm run dev 或类似的无需重建的执行方式

#### 3. [源代码] [高严重] [168处 any 类型使用]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - `stable-runner-factory.ts`: `opts: any` -> `any`
  - `billing-service-async.ts`: `input: any` 方法签名
  - 重试/超时工具使用 `(...args: any[]) => Promise<any>`
  - 绕过类型安全
- **建议修复**: 
  1. 使用泛型替代 any
  2. 替换为具体的输入类型
  3. 添加 ESLint 规则禁止 any

#### 4. [源代码] [高严重] [38处 @ts-ignore 指令]
- **文件/路径**: 多个文件
- **问题描述**: 
  - 14个 stability 相关文件有 `@ts-ignore ExecutionRecord type mismatch`
  - 5个 harness-sdk 文件有 `Partial<HarnessRun> doesn't have all required properties`
  - 2个文件有 `exactOptionalPropertyTypes` 问题
- **建议修复**: 
  1. 修复 ExecutionRecord 类型不匹配
  2. 完成 HarnessRun 类型的必需属性
  3. 移除 @ts-ignore 使用

#### 5. [源代码] [高严重] [ExecutionRecord 类型不匹配]
- **文件/路径**: `src/platform/stability/stable-evidence-bundle-support.ts:783` 等14个文件
- **问题描述**: 
  - 插入 execution record 时 TypeScript 拒绝类型
  - `ExecutionRecord` 接口要求新字段但 insert 对象缺失
  - 或 store 的 insert 方法接受不同类型
- **建议修复**: 
  1. 对齐 ExecutionRecord 接口和 store 类型
  2. 添加缺失字段到 insert 对象
  3. 或调整 store 的 insert 方法类型签名

#### 6. [源代码] [中严重] [30+处 as unknown as 双类型转换]
- **文件/路径**: `division-loader.ts`, `domain-baseline-catalog.ts`, `harness-sdk`
- **问题描述**: 
  - 完全绕过类型安全
  - 通常是类型架构问题的信号
- **建议修复**: 
  1. 修复上游类型问题
  2. 使用更具体的类型断言
  3. 重构类型层次结构

#### 7. [构建] [中严重] [rimraf  extraneous - 未在 package.json 声明]
- **文件/路径**: `node_modules/rimraf`
- **问题描述**: 
  - rimraf@6.1.3 存在于 node_modules 但未在 package.json 声明
  - 可能导致依赖问题
- **建议修复**: 
  1. 添加到 dependencies 或 devDependencies
  2. 或移除并使用其他清理方式

#### 8. [测试] [高严重] [178处直接 process.env 变更无抽象]
- **文件/路径**: `tests/unit/domains/registry/plugin-spi-registry.test.ts:640-687`
- **问题描述**: 
  - 直接读取/写入 process.env
  - 在 beforeEach/afterEach 中手动保存/恢复
  - 无 helper 抽象
- **建议修复**: 
  1. 创建 env helper 函数
  2. 统一保存和恢复模式
  3. 添加测试隔离验证

#### 9. [测试] [高严重] [21,998处 unlinkSync 调用无集中清理]
- **文件/路径**: 多个测试文件
- **问题描述**: 
  - 每个测试重新实现清理逻辑
  - 无集中清理 utility
  - 可能导致 EBUSY (Windows) 或并发访问问题
- **建议修复**: 
  1. 创建集中的测试清理 utility
  2. 使用 afterEach 确保清理
  3. 考虑使用临时目录

#### 10. [测试] [中严重] [无统一 mocking 框架]
- **文件/路径**: `tests/` 多个目录
- **问题描述**: 
  - 10,579 处 vi.fn, jest.mock, sinon 等调用
  - 无统一的 mocking 框架可见
  - 模式不一致
- **建议修复**: 
  1. 建立统一的 mock factory
  2. 标准化 mock 模式
  3. 添加 mocking 最佳实践文档

#### 11. [测试] [中严重] [Singleton 状态重置未标准化]
- **文件/路径**: `tests/unit/platform/five-plane-execution/dispatcher.test.ts`
- **问题描述**: 
  - 测试通过特定函数重置 singletons
  - 每个测试重复自己的 reset 逻辑
  - 无统一的 resetAllSingletons()
- **建议修复**: 
  1. 创建统一的 singleton reset 机制
  2. 在 afterEach 中自动调用
  3. 文档化 singleton 重置要求

#### 12. [测试] [低严重] [无分支覆盖率要求]
- **文件/路径**: `.c8rc.json`
- **问题描述**: 
  - `"100": false` - 无覆盖率要求
  - 只测量源代码，不测量测试文件
- **建议修复**: 
  1. 添加分支覆盖率阈值
  2. 在 CI 中强制执行覆盖率 gate
  3. 关注关键代码路径

#### 13. [网络] [高严重] [ScopedExternalAccessSandbox performHttpRequest 无超时]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/scoped-external-access-sandbox.ts:289`
- **问题描述**: 
  - fetch 调用无 timeout 参数
  - 挂起的请求可能永久阻塞
- **建议修复**: 
  1. 添加 AbortController timeout
  2. 使用标准的超时模式
  3. 记录超时配置

#### 14. [网络] [高严重] [OIDC 服务 fetch 调用无超时]
- **文件/路径**: `src/org-governance/sso-scim/oidc/oidc-service.ts:565,272`
- **问题描述**: 
  - OIDC token 交换无 retry logic
  - 无 timeout/abort signal
  - 认证流程可能挂起
- **建议修复**: 
  1. 添加 fetch timeout
  2. 添加 retry logic
  3. 使用标准的超时配置

#### 15. [网络] [高严重] [plugin-runtime-child 替换 globalThis.fetch 为 no-op]
- **文件/路径**: `src/domains/registry/plugin-runtime-child.ts:112`
- **问题描述**: 
  ```typescript
  globalThis.fetch = (async () => {}) as unknown as typeof globalThis.fetch;
  ```
  - 插件上下文中完全替换 fetch 为空函数
  - 插件的所有出站 HTTP 请求都会失败
  - 看起来是有意为之但可能造成问题
- **建议修复**: 
  1. 确认这是有意行为
  2. 文档化 fetch 替换行为
  3. 确保不会意外影响其他代码

#### 16. [网络] [中严重] [无显式 http.Agent 配置]
- **文件/路径**: 全局
- **问题描述**: 
  - 无显式的 http.Agent/https.Agent 配置
  - 依赖 Node.js 默认连接池
  - 无 maxSockets 或 maxConnections 配置
  - 高吞吐场景可能出问题
- **建议修复**: 
  1. 添加 http Agent 配置
  2. 文档化连接池大小
  3. 监控连接使用情况

#### 17. [序列化] [中严重] [合同版本硬编码为字符串字面量]
- **文件/路径**: `src/platform/contracts/inter-plane-contract-gateway.ts:184`
- **问题描述**: 
  - `schemaVersion: "v4.3"` 硬编码
  - 难以追踪合同版本演变
- **建议修复**: 
  1. 提取到类型化常量或 enum
  2. 集中管理合同版本
  3. 添加版本兼容性检查

#### 18. [序列化] [低严重] [Timestamp = string 丢失精度]
- **文件/路径**: `src/platform/contracts/types/domain/primitives.ts:10`
- **问题描述**: 
  - `type Timestamp = string` 无类型精度
  - ISO 8601 字符串无时区信息
- **建议修复**: 
  1. 使用 Date 对象或 branded string 类型
  2. 明确时区处理约定
  3. 文档化时间戳语义

#### 19. [源代码] [中严重] [29处非空断言 !.]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - 使用 `!.` 断言非空
  - 可能导致运行时错误
- **建议修复**: 
  1. 使用可选链 `?.` 或空值合并 `??`
  2. 添加适当的 null 检查
  3. 减少非空断言使用

#### 20. [API] [中严重] [API 路由 schema 版本硬编码]
- **文件/路径**: `src/platform/five-plane-interface/api/federation-routing-service.ts`
- **问题描述**: 
  - Zod schema 验证存在
  - 但 schema 版本未与 contract 版本同步
- **建议修复**: 
  1. 从 contract 版本派生 schema 版本
  2. 添加版本一致性检查
  3. 文档化版本关系

### 总结

本次补充Review（第十五轮 - 构建测试与类型系统Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. 无 TypeScript 增量编译缓存（每次完整重建9.6秒）
2. 76个 npm scripts 大部分重复构建
3. 168处 any 类型使用（绕过类型安全）
4. 38处 @ts-ignore 指令
5. ExecutionRecord 类型不匹配（14个文件）
6. 178处直接 process.env 变更无抽象
7. ScopedExternalAccessSandbox performHttpRequest 无超时
8. OIDC 服务 fetch 调用无超时
9. plugin-runtime-child 替换 globalThis.fetch 为 no-op

**中优先级**:
1. 30+处 as unknown as 双类型转换
2. rimraf extraneous 未声明
3. 21,998处 unlinkSync 无集中清理
4. 无统一 mocking 框架
5. Singleton 状态重置未标准化
6. 无显式 http.Agent 配置
7. 合同版本硬编码为字符串
8. Timestamp = string 丢失精度
9. 非空断言使用

**低优先级**:
1. 无分支覆盖率要求
2. API 路由 schema 版本未同步

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 72 | 87 | 39 | 198 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **109** | **182** | **85** | **376** |

### 优先修复建议

**立即处理 (本周内)**:
1. 添加 TypeScript 增量编译 (`incremental: true`)
2. 重构 npm scripts 避免重复构建
3. 减少 any 类型使用到 <50
4. 移除所有 @ts-ignore 指令
5. 修复 ExecutionRecord 类型不匹配
6. 添加 fetch 超时到 OIDC 和 sandbox

**短期内处理 (本月内)**:
1. 创建集中的测试清理 utility
2. 建立统一的 mocking 框架
3. 标准化 singleton 重置机制
4. 添加 http.Agent 连接池配置
5. 提取合同版本到类型化常量

**长期规划**:
1. 实现完整的类型安全（无 any）
2. 建立测试基础设施标准
3. 实现构建缓存优化
4. 完善网络层超时和重试
5. 建立覆盖率门禁标准

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十二轮 - 错误处理与多租户安全Review）

### 发现的新问题

#### 1. [安全] [严重] [listQuotas() 返回所有租户配额 - 信息泄露]
- **文件/路径**: `src/platform/five-plane-control-plane/tenant/tenant-execution-isolation-service.ts:222-234`
- **问题描述**: 
  ```typescript
  listQuotas(tenantId?: string): TenantQuota[] {
    if (tenantId) {
      return this.db.connection.prepare(`SELECT * FROM tenant_quotas WHERE tenant_id = ?`).all(tenantId);
    }
    // WITHOUT tenantId: 返回所有租户的配额！
    return this.db.connection.prepare(`SELECT * FROM tenant_quotas`).all();
  }
  ```
  - 当 tenantId 为空时返回所有租户的配额
  - 未经授权的信息泄露
- **建议修复**: 
  1. 要求调用者必须提供 tenantId
  2. 添加权限验证确保只能查看自己的配额
  3. 审计日志记录所有配额访问

#### 2. [安全] [高严重] [assertTaskTenantAccess() 返回404而非403 - 资源存在泄露]
- **文件/路径**: `src/platform/five-plane-interface/api/utils.ts:146-148`
- **问题描述**: 
  ```typescript
  if (resourceTenantId !== principal.tenantId) {
    throw new ApiError(404, notFoundCode, notFoundMessage);  // 应该是403
  }
  ```
  - 跨租户访问返回404而非403
  - 泄露了资源存在但属于其他租户的信息
  - 攻击者可检测有效task ID
- **建议修复**: 
  1. 改为返回403 Forbidden
  2. 不要泄露资源是否存在

#### 3. [安全] [高严重] [crossTenantRequest 标志由调用者控制]
- **文件/路径**: `src/platform/five-plane-interface/org-routing/index.ts:163`
- **问题描述**: 
  - `routeOrgBudget()` 接受 `crossTenantRequest?: boolean` 参数
  - 该标志由调用者设置，非系统派生
  - 攻击者可能操纵此标志绕过跨租户限制
- **建议修复**: 
  1. 标志应由系统状态派生，不接受调用者输入
  2. 添加审计日志追踪跨租户请求
  3. 实现零信任跨租户访问模型

#### 4. [源代码] [高严重] [single-task-happy-path 无重试机制]
- **文件/路径**: `src/platform/five-plane-execution/execution-engine/single-task-happy-path.ts:336-337`
- **问题描述**: 
  - `maxRetries: 0` 和 `retryBackoff: "none"`
  - 瞬态失败无自动重试
  - LLM调用失败时无fallback到备用provider
- **建议修复**: 
  1. 启用非关键执行的重试
  2. 实现LLM provider fallback链（Anthropic → OpenAI → MiniMax）
  3. 使用domain baseline catalog的primary/fallback模型偏好

#### 5. [源代码] [高严重] [LLM调用无熔断器保护]
- **文件/路径**: `src/platform/five-plane-execution/execution-engine/model-call-provider.ts`
- **问题描述**: 
  - 熔断器只存在于 channel-gateway 和 call-governance
  - 直接LLM provider调用无熔断器
  - provider降级时无法快速失败
- **建议修复**: 
  1. 在model-call-provider.ts添加熔断器
  2. 当provider失败率超阈值时快速失败
  3. 自动切换到备用provider

#### 6. [安全] [高严重] [分布式限流器 bypass - 实例间不共享]
- **文件/路径**: `src/platform/five-plane-interface/ingress/distributed-rate-limiter.ts:33`
- **问题描述**: 
  - Redis未配置时使用本地Map
  - 每个实例维护自己的localEntries
  - 攻击者可通过切换实例绕过限流
- **建议修复**: 
  1. 要求Redis配置用于生产
  2. 或使用Redis作为所有实例的共享后端
  3. 检测并警告未配置Redis的实例

#### 7. [配置] [中严重] [Soft quota (log_only) 实际不限制]
- **文件/路径**: `src/platform/five-plane-control-plane/tenant/tenant-execution-isolation-service.ts:448`
- **问题描述**: 
  ```typescript
  allowed: quota.enforcementAction === "log_only",  // log_only时返回true
  ```
  - soft quota只记录日志，不实际阻止
  - 配额形同虚设
- **建议修复**: 
  1. 明确区分"监控模式"和"强制模式"
  2. 对于硬配额必须阻止
  3. 文档化quota类型的实际行为

#### 8. [配置] [中严重] [HTTP层无限流头 - 客户端无法获知限制]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/response-hardening.ts`
- **问题描述**: 
  - 未返回 X-RateLimit-* 头
  - 只在429响应时返回retry-after-ms
  - 客户端无法主动管理请求速率
- **建议修复**: 
  1. 在所有响应添加 X-RateLimit-Limit, X-RateLimit-Remaining, X-RateLimit-Reset
  2. 使用标准头名称
  3. 遵循RFC 6585

#### 9. [配置] [中严重] [HTTP层无限流 - 可选且仅按IP]
- **文件/路径**: `src/platform/five-plane-interface/api/http-api-server.ts:334-351`
- **问题描述**: 
  - 限流仅在 `this.rateLimiter != null` 时生效
  - 限流key为 `${clientIp}:${endpoint}` 仅按IP
  - 无per-tenant/per-principal enforcement
- **建议修复**: 
  1. 默认启用限流
  2. 添加per-tenant限流key
  3. 确保所有环境默认配置限流

#### 10. [安全] [中严重] [processRuleMode 可能未强制执行]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/plugin-runtime-host.ts`
- **问题描述**: 
  - `SandboxPolicy.processRuleMode` 设置为 "allow" 或 "deny"
  - 但未找到实际阻止基于此策略生成进程的实际执行代码
  - `--allow-child-process` 标志仅在 sandboxed_process 隔离模式应用
- **建议修复**: 
  1. 验证processRuleMode实际被强制执行
  2. 确保所有隔离模式正确处理
  3. 添加测试验证进程创建被阻止

#### 11. [安全] [中严重] [容器模板替换未验证]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/plugin-runtime-host.ts:751`
- **问题描述**: 
  ```typescript
  rendered = rendered.split(placeholder).join(value);
  ```
  - 简单字符串替换
  - 虽然validateContainerLaunchPluginId()阻止\0和引号
  - 但模板替换本身未验证
- **建议修复**: 
  1. 添加输入验证防止注入
  2. 使用更安全的模板引擎
  3. 验证所有占位符被替换

#### 12. [安全] [中严重] [Adapter执行绕过沙箱]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/adapter-executor.ts`
- **问题描述**: 
  - Adapter执行REST/grpc/MQ调用不经过ScopedExternalAccessSandbox
  - 有自己的allowedDomains配置但非集中管理
  - 可能发起未限制的出站请求
- **建议修复**: 
  1. Adapter执行经过集中沙箱
  2. 统一external access策略
  3. 添加adapter出站请求审计

#### 13. [安全] [中严重] [null principal.tenantId 时静默放行]
- **文件/路径**: `src/platform/five-plane-interface/api/utils.ts:143-145`
- **问题描述**: 
  ```typescript
  if (principal.tenantId == null) {
    return;  // 静默放行
  }
  ```
  - 如果API网关允许无principal认证的请求通过
  - 可能授予跨租户访问权限
- **建议修复**: 
  1. 要求所有API请求有有效principal
  2. null tenantId应拒绝而非静默放行
  3. 添加审计日志

#### 14. [源代码] [中严重] [无LLM provider fallback链]
- **文件/路径**: `src/platform/five-plane-execution/execution-engine/model-call-provider.ts`
- **问题描述**: 
  - 主provider失败时无自动切换
  - domain-baseline-catalog.ts定义了primary/fallback模型偏好但未使用
  - 第一个provider失败即导致执行失败
- **建议修复**: 
  1. 实现provider fallback链
  2. 使用domain baseline的primary/fallback配置
  3. 按优先级尝试直到成功或全部失败

#### 15. [源代码] [中严重] [超时值硬编码]
- **文件/路径**: 多个文件
- **问题描述**: 
  - Effect buffer有固定超时reject
  - Channel gateway的requestTimeoutMs虽可配置但有max 30s限制
  - 无全局请求超时中间件
- **建议修复**: 
  1. 集中超时配置
  2. per-environment可配置
  3. 添加全局超时中间件

#### 16. [数据库] [中严重] [无down migration - 回滚不支持]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/sqlite/migration-runner.ts:45-47`
- **问题描述**: 
  - `rollbackSupported = false`
  - 每个migration只有 `downSql` 占位符
  - schema migration无法回滚
- **建议修复**: 
  1. 文档化回滚限制
  2. 在变更前创建完整备份
  3. 使用blue-green部署减少回滚需求

#### 17. [数据库] [中严重] [Migration 44特殊处理 - 表创建重复]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/sqlite/sqlite-migration-runtime-part3.ts`
- **问题描述**: 
  - Migration 43创建harness_runs表
  - Migration 44再次创建同名表（不同schema）
  - 使用applyCompatibleColumnMigrationIfKnown特殊处理
- **建议修复**: 
  1. 消除重复表创建
  2. 使用ALTER TABLE而非CREATE TABLE
  3. 简化migration逻辑

#### 18. [配置] [低严重] [Provider限流头未转发给客户端]
- **文件/路径**: `src/platform/model-gateway/provider-registry/base-chat-provider.ts:153`
- **问题描述**: 
  - 系统读取provider的ratelimitResetHeaderNames
  - 但不转发给客户端
  - 客户端无法知道provider限流状态
- **建议修复**: 
  1. 转发Provider的限流头
  2. 或添加应用层限流信息
  3. 帮助客户端优化请求

#### 19. [安全] [低严重] [Browser evaluate 接受任意脚本]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/browser-executor.ts:326`
- **问题描述**: 
  - `evaluate` action接受script参数
  - 在浏览器上下文运行（模拟）
  - 如浏览器上下文未正确沙箱化可能是向量
- **建议修复**: 
  1. 验证浏览器上下文正确沙箱化
  2. 消毒或限制script内容
  3. 记录所有evaluate调用

#### 20. [源代码] [低严重] [无全局错误边界包装器]
- **文件/路径**: `src/platform/five-plane-execution/execution-engine/`
- **问题描述**: 
  - 执行引擎缺少unhandled promise rejection的全局处理
  - single-task-happy-path的LLM fallback失败时错误向上传播
- **建议修复**: 
  1. 在执行引擎添加错误边界包装
  2. 统一错误处理模式
  3. 确保所有错误被捕获和记录

### 总结

本次补充Review（第十二轮 - 错误处理与多租户安全Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. listQuotas()返回所有租户配额（信息泄露）
2. assertTaskTenantAccess()返回404泄露资源存在
3. crossTenantRequest标志可被操纵
4. single-task-happy-path无重试（maxRetries=0）
5. LLM调用无熔断器保护
6. 分布式限流器可被bypass（实例间不共享）

**中优先级**:
1. Soft quota实际不阻止
2. 无X-RateLimit-*响应头
3. HTTP层限流可选且仅按IP
4. processRuleMode可能未强制
5. 容器模板替换未验证
6. Adapter执行绕过沙箱
7. null principal静默放行
8. 无LLM provider fallback链
9. 超时值硬编码
10. 无down migration
11. Migration 44特殊处理

**低优先级**:
1. Provider限流头未转发
2. Browser evaluate接受任意脚本
3. 无全局错误边界

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 60 | 68 | 31 | 159 |
| 测试 | 8 | 14 | 5 | 27 |
| 配置 | 8 | 35 | 21 | 64 |
| 安全 | 12 | 13 | 3 | 28 |
| 文档 | 2 | 7 | 3 | 12 |
| UI | 0 | 3 | 3 | 6 |
| 部署 | 1 | 10 | 6 | 17 |
| **合计** | **91** | **150** | **72** | **313** |

### 优先修复建议

**立即处理 (本周内)**:
1. 修复listQuotas()信息泄露漏洞
2. 修正assertTaskTenantAccess()返回403
3. 移除crossTenantRequest调用者控制
4. 启用执行引擎重试机制
5. 添加LLM provider熔断器

**短期内处理 (本月内)**:
1. 修复分布式限流器bypass问题
2. 添加X-RateLimit-*响应头
3. 启用默认HTTP限流（per-tenant）
4. 实现LLM provider fallback链
5. 验证并强制processRuleMode

**长期规划**:
1. 建立完整的限流和配额体系
2. 实现插件系统安全审计
3. 建立多租户安全测试
4. 完善错误处理和恢复机制
5. 统一超时和熔断配置

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十五轮 - 构建测试与类型系统Review）

### 发现的新问题

#### 1. [构建] [高严重] [无 TypeScript 增量编译缓存]
- **文件/路径**: `tsconfig.json`
- **问题描述**: 
  - 无 `incremental: true` 配置
  - 无 `tsbuildinfo` 文件
  - 每次构建是完整重建（9.6秒）
  - TypeScript 无法使用增量编译
- **建议修复**: 
  1. 在 tsconfig.json 添加 `"incremental": true`
  2. 添加 `"tsbuildinfo": ".tsbuildinfo"` 到 .gitignore
  3. 实现项目引用以支持并行构建

#### 2. [构建] [高严重] [76个 npm scripts 大部分重复构建]
- **文件/路径**: `package.json`
- **问题描述**: 
  - 40+ CLI scripts 都运行 `npm run build && node ...`
  - 每个脚本前都执行完整重建
  - 无法直接运行预构建的 CLI
  - 20+ stable:* 脚本遵循相同模式
- **建议修复**: 
  1. 创建 `build:cli` 脚本构建一次
  2. 让 CLI 脚本使用预构建的 dist 文件
  3. 添加 npm run dev 或类似的无需重建的执行方式

#### 3. [源代码] [高严重] [168处 any 类型使用]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - `stable-runner-factory.ts`: `opts: any` -> `any`
  - `billing-service-async.ts`: `input: any` 方法签名
  - 重试/超时工具使用 `(...args: any[]) => Promise<any>`
  - 绕过类型安全
- **建议修复**: 
  1. 使用泛型替代 any
  2. 替换为具体的输入类型
  3. 添加 ESLint 规则禁止 any

#### 4. [源代码] [高严重] [38处 @ts-ignore 指令]
- **文件/路径**: 多个文件
- **问题描述**: 
  - 14个 stability 相关文件有 `@ts-ignore ExecutionRecord type mismatch`
  - 5个 harness-sdk 文件有 `Partial<HarnessRun> doesn't have all required properties`
  - 2个文件有 `exactOptionalPropertyTypes` 问题
- **建议修复**: 
  1. 修复 ExecutionRecord 类型不匹配
  2. 完成 HarnessRun 类型的必需属性
  3. 移除 @ts-ignore 使用

#### 5. [源代码] [高严重] [ExecutionRecord 类型不匹配]
- **文件/路径**: `src/platform/stability/stable-evidence-bundle-support.ts:783` 等14个文件
- **问题描述**: 
  - 插入 execution record 时 TypeScript 拒绝类型
  - `ExecutionRecord` 接口要求新字段但 insert 对象缺失
  - 或 store 的 insert 方法接受不同类型
- **建议修复**: 
  1. 对齐 ExecutionRecord 接口和 store 类型
  2. 添加缺失字段到 insert 对象
  3. 或调整 store 的 insert 方法类型签名

#### 6. [源代码] [中严重] [30+处 as unknown as 双类型转换]
- **文件/路径**: `division-loader.ts`, `domain-baseline-catalog.ts`, `harness-sdk`
- **问题描述**: 
  - 完全绕过类型安全
  - 通常是类型架构问题的信号
- **建议修复**: 
  1. 修复上游类型问题
  2. 使用更具体的类型断言
  3. 重构类型层次结构

#### 7. [构建] [中严重] [rimraf  extraneous - 未在 package.json 声明]
- **文件/路径**: `node_modules/rimraf`
- **问题描述**: 
  - rimraf@6.1.3 存在于 node_modules 但未在 package.json 声明
  - 可能导致依赖问题
- **建议修复**: 
  1. 添加到 dependencies 或 devDependencies
  2. 或移除并使用其他清理方式

#### 8. [测试] [高严重] [178处直接 process.env 变更无抽象]
- **文件/路径**: `tests/unit/domains/registry/plugin-spi-registry.test.ts:640-687`
- **问题描述**: 
  - 直接读取/写入 process.env
  - 在 beforeEach/afterEach 中手动保存/恢复
  - 无 helper 抽象
- **建议修复**: 
  1. 创建 env helper 函数
  2. 统一保存和恢复模式
  3. 添加测试隔离验证

#### 9. [测试] [高严重] [21,998处 unlinkSync 调用无集中清理]
- **文件/路径**: 多个测试文件
- **问题描述**: 
  - 每个测试重新实现清理逻辑
  - 无集中清理 utility
  - 可能导致 EBUSY (Windows) 或并发访问问题
- **建议修复**: 
  1. 创建集中的测试清理 utility
  2. 使用 afterEach 确保清理
  3. 考虑使用临时目录

#### 10. [测试] [中严重] [无统一 mocking 框架]
- **文件/路径**: `tests/` 多个目录
- **问题描述**: 
  - 10,579 处 vi.fn, jest.mock, sinon 等调用
  - 无统一的 mocking 框架可见
  - 模式不一致
- **建议修复**: 
  1. 建立统一的 mock factory
  2. 标准化 mock 模式
  3. 添加 mocking 最佳实践文档

#### 11. [测试] [中严重] [Singleton 状态重置未标准化]
- **文件/路径**: `tests/unit/platform/five-plane-execution/dispatcher.test.ts`
- **问题描述**: 
  - 测试通过特定函数重置 singletons
  - 每个测试重复自己的 reset 逻辑
  - 无统一的 resetAllSingletons()
- **建议修复**: 
  1. 创建统一的 singleton reset 机制
  2. 在 afterEach 中自动调用
  3. 文档化 singleton 重置要求

#### 12. [测试] [低严重] [无分支覆盖率要求]
- **文件/路径**: `.c8rc.json`
- **问题描述**: 
  - `"100": false` - 无覆盖率要求
  - 只测量源代码，不测量测试文件
- **建议修复**: 
  1. 添加分支覆盖率阈值
  2. 在 CI 中强制执行覆盖率 gate
  3. 关注关键代码路径

#### 13. [网络] [高严重] [ScopedExternalAccessSandbox performHttpRequest 无超时]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/scoped-external-access-sandbox.ts:289`
- **问题描述**: 
  - fetch 调用无 timeout 参数
  - 挂起的请求可能永久阻塞
- **建议修复**: 
  1. 添加 AbortController timeout
  2. 使用标准的超时模式
  3. 记录超时配置

#### 14. [网络] [高严重] [OIDC 服务 fetch 调用无超时]
- **文件/路径**: `src/org-governance/sso-scim/oidc/oidc-service.ts:565,272`
- **问题描述**: 
  - OIDC token 交换无 retry logic
  - 无 timeout/abort signal
  - 认证流程可能挂起
- **建议修复**: 
  1. 添加 fetch timeout
  2. 添加 retry logic
  3. 使用标准的超时配置

#### 15. [网络] [高严重] [plugin-runtime-child 替换 globalThis.fetch 为 no-op]
- **文件/路径**: `src/domains/registry/plugin-runtime-child.ts:112`
- **问题描述**: 
  ```typescript
  globalThis.fetch = (async () => {}) as unknown as typeof globalThis.fetch;
  ```
  - 插件上下文中完全替换 fetch 为空函数
  - 插件的所有出站 HTTP 请求都会失败
  - 看起来是有意为之但可能造成问题
- **建议修复**: 
  1. 确认这是有意行为
  2. 文档化 fetch 替换行为
  3. 确保不会意外影响其他代码

#### 16. [网络] [中严重] [无显式 http.Agent 配置]
- **文件/路径**: 全局
- **问题描述**: 
  - 无显式的 http.Agent/https.Agent 配置
  - 依赖 Node.js 默认连接池
  - 无 maxSockets 或 maxConnections 配置
  - 高吞吐场景可能出问题
- **建议修复**: 
  1. 添加 http Agent 配置
  2. 文档化连接池大小
  3. 监控连接使用情况

#### 17. [序列化] [中严重] [合同版本硬编码为字符串字面量]
- **文件/路径**: `src/platform/contracts/inter-plane-contract-gateway.ts:184`
- **问题描述**: 
  - `schemaVersion: "v4.3"` 硬编码
  - 难以追踪合同版本演变
- **建议修复**: 
  1. 提取到类型化常量或 enum
  2. 集中管理合同版本
  3. 添加版本兼容性检查

#### 18. [序列化] [低严重] [Timestamp = string 丢失精度]
- **文件/路径**: `src/platform/contracts/types/domain/primitives.ts:10`
- **问题描述**: 
  - `type Timestamp = string` 无类型精度
  - ISO 8601 字符串无时区信息
- **建议修复**: 
  1. 使用 Date 对象或 branded string 类型
  2. 明确时区处理约定
  3. 文档化时间戳语义

#### 19. [源代码] [中严重] [29处非空断言 !.]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - 使用 `!.` 断言非空
  - 可能导致运行时错误
- **建议修复**: 
  1. 使用可选链 `?.` 或空值合并 `??`
  2. 添加适当的 null 检查
  3. 减少非空断言使用

#### 20. [API] [中严重] [API 路由 schema 版本硬编码]
- **文件/路径**: `src/platform/five-plane-interface/api/federation-routing-service.ts`
- **问题描述**: 
  - Zod schema 验证存在
  - 但 schema 版本未与 contract 版本同步
- **建议修复**: 
  1. 从 contract 版本派生 schema 版本
  2. 添加版本一致性检查
  3. 文档化版本关系

### 总结

本次补充Review（第十五轮 - 构建测试与类型系统Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. 无 TypeScript 增量编译缓存（每次完整重建9.6秒）
2. 76个 npm scripts 大部分重复构建
3. 168处 any 类型使用（绕过类型安全）
4. 38处 @ts-ignore 指令
5. ExecutionRecord 类型不匹配（14个文件）
6. 178处直接 process.env 变更无抽象
7. ScopedExternalAccessSandbox performHttpRequest 无超时
8. OIDC 服务 fetch 调用无超时
9. plugin-runtime-child 替换 globalThis.fetch 为 no-op

**中优先级**:
1. 30+处 as unknown as 双类型转换
2. rimraf extraneous 未声明
3. 21,998处 unlinkSync 无集中清理
4. 无统一 mocking 框架
5. Singleton 状态重置未标准化
6. 无显式 http.Agent 配置
7. 合同版本硬编码为字符串
8. Timestamp = string 丢失精度
9. 非空断言使用

**低优先级**:
1. 无分支覆盖率要求
2. API 路由 schema 版本未同步

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 72 | 87 | 39 | 198 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **109** | **182** | **85** | **376** |

### 优先修复建议

**立即处理 (本周内)**:
1. 添加 TypeScript 增量编译 (`incremental: true`)
2. 重构 npm scripts 避免重复构建
3. 减少 any 类型使用到 <50
4. 移除所有 @ts-ignore 指令
5. 修复 ExecutionRecord 类型不匹配
6. 添加 fetch 超时到 OIDC 和 sandbox

**短期内处理 (本月内)**:
1. 创建集中的测试清理 utility
2. 建立统一的 mocking 框架
3. 标准化 singleton 重置机制
4. 添加 http.Agent 连接池配置
5. 提取合同版本到类型化常量

**长期规划**:
1. 实现完整的类型安全（无 any）
2. 建立测试基础设施标准
3. 实现构建缓存优化
4. 完善网络层超时和重试
5. 建立覆盖率门禁标准

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十四轮 - 日志事件与工作流Review）

### 发现的新问题

#### 1. [可观测性] [高严重] [StructuredLogEntry 无 requestId 字段]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - StructuredLogEntry 接口无 requestId 字段
  - 只有 correlationId，它回退到 traceId
  - 无法独立追踪单个 HTTP 请求
- **建议修复**: 
  1. 在 StructuredLogEntry 添加 requestId 字段
  2. 在 HTTP 中间件中生成和注入 requestId
  3. 确保 requestId 流经所有日志调用

#### 2. [可观测性] [高严重] [StructuredLogger data 字段无自动清洗]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - 如果开发者传递 `{ password: "..." }` 给 logger.info，密码会被记录
  - 无字段级脱敏机制
  - 依赖开发者手动排除敏感字段
- **建议修复**: 
  1. 添加 OBSERVE_OUTPUT_BLACKLIST 类似的黑名单
  2. 实现 redact/mask/sanitize 工具函数
  3. 在日志传输前自动清洗敏感字段

#### 3. [可观测性] [中严重] [日志中敏感数据无 PII 处理]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - 无 PII 重写工具
  - 开发者必须手动避免记录敏感数据
  - 存在意外记录敏感信息的风险
- **建议修复**: 
  1. 添加 PII 检测和重写工具
  2. 在文档中明确记录敏感字段列表
  3. 添加测试验证敏感数据不被记录

#### 4. [可观测性] [中严重] [59处 console.* 直接调用]
- **文件/路径**: 多个源文件
- **问题描述**: 
  - 应该使用 StructuredLogger
  - console 调用可能不被日志聚合系统收集
  - 难以追踪和关联
- **建议修复**: 
  1. 替换所有 console.* 为 StructuredLogger
  2. 添加 ESLint 规则禁止 console.*
  3. 保留少量允许的 CLI 输出

#### 5. [可观测性] [中严重] [部分 logger.warn 调用缺少结构化数据]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:122`
- **问题描述**: 
  ```typescript
  logger.warn("WebSocket connection rejected: missing subprotocol token")
  ```
  - 只有消息，无 data 对象
  - 难以关联和查询
- **建议修复**: 
  1. 添加 `{ data: { ... } }` 对象
  2. 确保所有日志包含上下文数据
  3. 添加日志审查工具

#### 6. [源代码] [高严重] [Harness while(true) 循环无硬性迭代上限]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:1442`
- **问题描述**: 
  ```typescript
  while (true) {
    // ...
    // 依赖 budget gate 和 guardrail vibration 退出
    // 如果两者都失败，循环永久继续
  }
  ```
  - 依赖 budget gate 和 guardrail vibration 退出
  - 无硬性迭代次数限制
  - 可能导致无限循环
- **建议修复**: 
  1. 添加硬性 maxIterations 上限
  2. 在达到上限时强制退出
  3. 记录详细的迭代统计用于调试

#### 7. [源代码] [高严重] [Oapeflir while(true) 循环可能无限重规划]
- **文件/路径**: `src/platform/five-plane-orchestration/oapeflir/oapeflir-loop-service.ts:355`
- **问题描述**: 
  - 如果 loopReplanDecision.shouldReplan 永不变为 false
  - 重规划永远无法减少计划错误
  - 可能导致无限循环
- **建议修复**: 
  1. 添加重规划次数上限
  2. 在达到上限时强制接受当前结果
  3. 添加重规划质量衰减检测

#### 8. [源代码] [中严重] [Recovery 流程允许终态转换到 paused]
- **文件/路径**: `src/platform/five-plane-execution/runtime-state-machine.ts:105-108`
- **问题描述**: 
  - completed, failed, cancelled, aborted 可转换到 paused 进行恢复
  - 创建恢复路径可能重新进入执行
  - 需要验证此行为的正确性
- **建议修复**: 
  1. 审查 recovery 转换的合法性
  2. 确保不会导致状态不一致
  3. 添加转换前置条件验证

#### 9. [国际化] [高严重] [API 错误消息硬编码英文]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/api-error.ts`
- **问题描述**: 
  ```typescript
  return new ApiError(404, "api.task_not_found", "Task not found.");
  ```
  - 错误消息硬编码英文
  - 无后端 i18n 机制
  - 非英文客户端收到不本地化的错误
- **建议修复**: 
  1. 使用错误码而非硬编码消息
  2. 客户端根据错误码本地化
  3. 或在后端添加 i18n 支持

#### 10. [国际化] [中严重] [UI 硬编码字符串未翻译]
- **文件/路径**: `ui/packages/features/conversation/src/web/index.tsx`
- **问题描述**: 
  - "Build Plan", "Execute", "Messages" 等按钮标签硬编码
  - 不使用 i18n 系统
- **建议修复**: 
  1. 使用 `translateFeatureCopy` 替换硬编码
  2. 确保所有用户可见文本通过 i18n
  3. 添加硬编码字符串检测

#### 11. [国际化] [中严重] [Arabic 目录不完整]
- **文件/路径**: `ui/packages/shared/i18n/src/catalogs/ar-SA.ts`
- **问题描述**: 
  - ar-SA 只有约18条消息
  - en-US 和 zh-CN 有约117条
  - 缺失的 key 返回 key 本身
- **建议修复**: 
  1. 补充 Arabic 翻译
  2. 确保所有 key 都有翻译
  3. 添加翻译完整性测试

#### 12. [国际化] [低严重] [translateFeatureCopy 无回退默认值]
- **文件/路径**: `ui/packages/shared/i18n/`
- **问题描述**: 
  - 如果 feature ID 不在 catalog 中
  - 返回 undefined title/summary
  - 无合理回退
- **建议修复**: 
  1. 添加回退到 key 或默认字符串
  2. 或使用 feature ID 作为显示名
  3. 确保 UI 不显示 undefined

#### 13. [运维] [高严重] [健康检查无 readiness/liveness 区分]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts`
- **问题描述**: 
  - /healthz, /v1/healthz, /health 返回相同报告
  - 无 readiness probe（可处理流量？）
  - 无 liveness probe（系统存活？）
  - canAcceptTraffic() 未通过 HTTP 暴露
- **建议修复**: 
  1. 添加 /ready 和 /live 端点
  2. Readiness 检查依赖项就绪
  3. Liveness 检查进程健康

#### 14. [运维] [高严重] [shutdown 期间健康检查存在竞态]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts`
- **问题描述**: 
  - getHealthReportAsync() 异步检查 dbWritable
  - shutdown 期间数据库连接可能关闭
  - 健康检查仍在传入
- **建议修复**: 
  1. 在 shutdown 时返回 unhealthy
  2. 添加 shutdown 状态标志
  3. 停止接受新的健康检查请求

#### 15. [运维] [高严重] [LeaderElectionService 未与 GracefulShutdown 集成]
- **文件/路径**: `src/platform/five-plane-execution/ha/leader-election-service.ts`
- **问题描述**: 
  - LeaderElectionService.stop() 处理优雅退位
  - 但未在全局 graceful shutdown 中注册
  - 需显式注册为 shutdown handler
- **建议修复**: 
  1. 将 LeaderElectionService 添加到 shutdown handlers
  2. 确保领导者在 shutdown 时让位
  3. 验证关闭顺序正确

#### 16. [运维] [中严重] [addHandler() 无显式排序约定]
- **文件/路径**: `src/platform/five-plane-execution/startup/graceful-shutdown.ts`
- **问题描述**: 
  - 虽然 handlers 逆序执行
  - 但无显式接口声明 handler 依赖关系
  - 关键 handlers（如关闭数据库连接）应最后运行
- **建议修复**: 
  1. 添加优先级或阶段参数
  2. 文档化关键 handler 顺序要求
  3. 添加顺序验证测试

#### 17. [运维] [中严重] [unref'd timers 可能不触发]
- **文件/路径**: `src/platform/five-plane-execution/startup/process-error-handlers.ts:106`
- **问题描述**: 
  - `setTimeout(...).unref()` 不保持事件循环活跃
  - 如果主线程空闲，强制退出可能不触发
- **建议修复**: 
  1. 移除 .unref() 除非确实需要
  2. 或确保有其他方式保持事件循环
  3. 添加超时触发验证测试

#### 18. [运维] [中严重] [StartupConsistencyChecker 阻塞流量但不暴露状态]
- **文件/路径**: `src/platform/five-plane-startup-plan.ts`
- **问题描述**: 
  - canAcceptTraffic() 在 P0 问题存在时返回 false
  - 但 _trafficBlocked 是内部状态
  - 负载均衡器访问 /healthz 仍会得到 "ok"
- **建议修复**: 
  1. 在健康报告中暴露 trafficBlocked 状态
  2. 或添加单独的健康端点指示就绪状态
  3. 确保负载均衡器在阻塞时不路由流量

#### 19. [事件系统] [中严重] [TypedEventBus vs EventEmitter 混用]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - 项目同时使用 TypedEventBus 和原生 EventEmitter
  - TypedEventBus 应该是标准
  - 混用可能导致类型安全问题
- **建议修复**: 
  1. 创建迁移计划统一使用 TypedEventBus
  2. 添加 ESLint 规则禁止原生 EventEmitter
  3. 在 CI 中检测混用情况

#### 20. [源代码] [低严重] [无事件排序保证]
- **文件/路径**: `src/platform/five-plane-state-evidence/events/`
- **问题描述**: 
  - 无明确的事件排序保证文档
  - 事件处理可能乱序
  - 依赖方需要处理乱序
- **建议修复**: 
  1. 文档化事件排序语义
  2. 如果需要排序，实现序列号机制
  3. 在事件处理中考虑乱序情况

### 总结

本次补充Review（第十四轮 - 日志事件与工作流Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. StructuredLogEntry 无 requestId 字段
2. StructuredLogger data 字段无自动清洗（敏感数据风险）
3. Harness while(true) 循环无硬性迭代上限
4. Oapeflir while(true) 循环可能无限重规划
5. API 错误消息硬编码英文（无 i18n）
6. 健康检查无 readiness/liveness 区分
7. shutdown 期间健康检查存在竞态
8. LeaderElectionService 未与 GracefulShutdown 集成

**中优先级**:
1. 日志中敏感数据无 PII 处理
2. 59处 console.* 直接调用
3. 部分 logger.warn 调用缺少结构化数据
4. Recovery 流程允许终态转换到 paused
5. UI 硬编码字符串未翻译
6. Arabic 目录不完整
7. addHandler() 无显式排序约定
8. unref'd timers 可能不触发
9. StartupConsistencyChecker 阻塞流量但不暴露状态
10. TypedEventBus vs EventEmitter 混用

**低优先级**:
1. translateFeatureCopy 无回退默认值
2. 无事件排序保证

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 68 | 80 | 37 | 185 |
| 测试 | 8 | 14 | 5 | 27 |
| 配置 | 10 | 38 | 24 | 72 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **103** | **171** | **82** | **356** |

### 优先修复建议

**立即处理 (本周内)**:
1. 在 StructuredLogEntry 添加 requestId 字段
2. 实现日志敏感字段自动清洗
3. 为工作流循环添加硬性迭代上限
4. 添加 readiness/liveness 健康检查端点
5. 将 LeaderElectionService 集成到 GracefulShutdown
6. 实现 API 错误消息 i18n 支持

**短期内处理 (本月内)**:
1. 替换所有 console.* 为 StructuredLogger
2. 补充 Arabic i18n 目录
3. 修复 UI 硬编码字符串
4. 统一 TypedEventBus 替代 EventEmitter
5. 解决 shutdown 竞态条件
6. 添加事件排序保证文档

**长期规划**:
1. 建立完整的 PII 处理框架
2. 实现日志完整性验证
3. 建立工作流安全循环检测
4. 完善健康检查和就绪探测
5. 建立国际化完整测试

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十五轮 - 构建测试与类型系统Review）

### 发现的新问题

#### 1. [构建] [高严重] [无 TypeScript 增量编译缓存]
- **文件/路径**: `tsconfig.json`
- **问题描述**: 
  - 无 `incremental: true` 配置
  - 无 `tsbuildinfo` 文件
  - 每次构建是完整重建（9.6秒）
  - TypeScript 无法使用增量编译
- **建议修复**: 
  1. 在 tsconfig.json 添加 `"incremental": true`
  2. 添加 `"tsbuildinfo": ".tsbuildinfo"` 到 .gitignore
  3. 实现项目引用以支持并行构建

#### 2. [构建] [高严重] [76个 npm scripts 大部分重复构建]
- **文件/路径**: `package.json`
- **问题描述**: 
  - 40+ CLI scripts 都运行 `npm run build && node ...`
  - 每个脚本前都执行完整重建
  - 无法直接运行预构建的 CLI
  - 20+ stable:* 脚本遵循相同模式
- **建议修复**: 
  1. 创建 `build:cli` 脚本构建一次
  2. 让 CLI 脚本使用预构建的 dist 文件
  3. 添加 npm run dev 或类似的无需重建的执行方式

#### 3. [源代码] [高严重] [168处 any 类型使用]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - `stable-runner-factory.ts`: `opts: any` -> `any`
  - `billing-service-async.ts`: `input: any` 方法签名
  - 重试/超时工具使用 `(...args: any[]) => Promise<any>`
  - 绕过类型安全
- **建议修复**: 
  1. 使用泛型替代 any
  2. 替换为具体的输入类型
  3. 添加 ESLint 规则禁止 any

#### 4. [源代码] [高严重] [38处 @ts-ignore 指令]
- **文件/路径**: 多个文件
- **问题描述**: 
  - 14个 stability 相关文件有 `@ts-ignore ExecutionRecord type mismatch`
  - 5个 harness-sdk 文件有 `Partial<HarnessRun> doesn't have all required properties`
  - 2个文件有 `exactOptionalPropertyTypes` 问题
- **建议修复**: 
  1. 修复 ExecutionRecord 类型不匹配
  2. 完成 HarnessRun 类型的必需属性
  3. 移除 @ts-ignore 使用

#### 5. [源代码] [高严重] [ExecutionRecord 类型不匹配]
- **文件/路径**: `src/platform/stability/stable-evidence-bundle-support.ts:783` 等14个文件
- **问题描述**: 
  - 插入 execution record 时 TypeScript 拒绝类型
  - `ExecutionRecord` 接口要求新字段但 insert 对象缺失
  - 或 store 的 insert 方法接受不同类型
- **建议修复**: 
  1. 对齐 ExecutionRecord 接口和 store 类型
  2. 添加缺失字段到 insert 对象
  3. 或调整 store 的 insert 方法类型签名

#### 6. [源代码] [中严重] [30+处 as unknown as 双类型转换]
- **文件/路径**: `division-loader.ts`, `domain-baseline-catalog.ts`, `harness-sdk`
- **问题描述**: 
  - 完全绕过类型安全
  - 通常是类型架构问题的信号
- **建议修复**: 
  1. 修复上游类型问题
  2. 使用更具体的类型断言
  3. 重构类型层次结构

#### 7. [构建] [中严重] [rimraf  extraneous - 未在 package.json 声明]
- **文件/路径**: `node_modules/rimraf`
- **问题描述**: 
  - rimraf@6.1.3 存在于 node_modules 但未在 package.json 声明
  - 可能导致依赖问题
- **建议修复**: 
  1. 添加到 dependencies 或 devDependencies
  2. 或移除并使用其他清理方式

#### 8. [测试] [高严重] [178处直接 process.env 变更无抽象]
- **文件/路径**: `tests/unit/domains/registry/plugin-spi-registry.test.ts:640-687`
- **问题描述**: 
  - 直接读取/写入 process.env
  - 在 beforeEach/afterEach 中手动保存/恢复
  - 无 helper 抽象
- **建议修复**: 
  1. 创建 env helper 函数
  2. 统一保存和恢复模式
  3. 添加测试隔离验证

#### 9. [测试] [高严重] [21,998处 unlinkSync 调用无集中清理]
- **文件/路径**: 多个测试文件
- **问题描述**: 
  - 每个测试重新实现清理逻辑
  - 无集中清理 utility
  - 可能导致 EBUSY (Windows) 或并发访问问题
- **建议修复**: 
  1. 创建集中的测试清理 utility
  2. 使用 afterEach 确保清理
  3. 考虑使用临时目录

#### 10. [测试] [中严重] [无统一 mocking 框架]
- **文件/路径**: `tests/` 多个目录
- **问题描述**: 
  - 10,579 处 vi.fn, jest.mock, sinon 等调用
  - 无统一的 mocking 框架可见
  - 模式不一致
- **建议修复**: 
  1. 建立统一的 mock factory
  2. 标准化 mock 模式
  3. 添加 mocking 最佳实践文档

#### 11. [测试] [中严重] [Singleton 状态重置未标准化]
- **文件/路径**: `tests/unit/platform/five-plane-execution/dispatcher.test.ts`
- **问题描述**: 
  - 测试通过特定函数重置 singletons
  - 每个测试重复自己的 reset 逻辑
  - 无统一的 resetAllSingletons()
- **建议修复**: 
  1. 创建统一的 singleton reset 机制
  2. 在 afterEach 中自动调用
  3. 文档化 singleton 重置要求

#### 12. [测试] [低严重] [无分支覆盖率要求]
- **文件/路径**: `.c8rc.json`
- **问题描述**: 
  - `"100": false` - 无覆盖率要求
  - 只测量源代码，不测量测试文件
- **建议修复**: 
  1. 添加分支覆盖率阈值
  2. 在 CI 中强制执行覆盖率 gate
  3. 关注关键代码路径

#### 13. [网络] [高严重] [ScopedExternalAccessSandbox performHttpRequest 无超时]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/scoped-external-access-sandbox.ts:289`
- **问题描述**: 
  - fetch 调用无 timeout 参数
  - 挂起的请求可能永久阻塞
- **建议修复**: 
  1. 添加 AbortController timeout
  2. 使用标准的超时模式
  3. 记录超时配置

#### 14. [网络] [高严重] [OIDC 服务 fetch 调用无超时]
- **文件/路径**: `src/org-governance/sso-scim/oidc/oidc-service.ts:565,272`
- **问题描述**: 
  - OIDC token 交换无 retry logic
  - 无 timeout/abort signal
  - 认证流程可能挂起
- **建议修复**: 
  1. 添加 fetch timeout
  2. 添加 retry logic
  3. 使用标准的超时配置

#### 15. [网络] [高严重] [plugin-runtime-child 替换 globalThis.fetch 为 no-op]
- **文件/路径**: `src/domains/registry/plugin-runtime-child.ts:112`
- **问题描述**: 
  ```typescript
  globalThis.fetch = (async () => {}) as unknown as typeof globalThis.fetch;
  ```
  - 插件上下文中完全替换 fetch 为空函数
  - 插件的所有出站 HTTP 请求都会失败
  - 看起来是有意为之但可能造成问题
- **建议修复**: 
  1. 确认这是有意行为
  2. 文档化 fetch 替换行为
  3. 确保不会意外影响其他代码

#### 16. [网络] [中严重] [无显式 http.Agent 配置]
- **文件/路径**: 全局
- **问题描述**: 
  - 无显式的 http.Agent/https.Agent 配置
  - 依赖 Node.js 默认连接池
  - 无 maxSockets 或 maxConnections 配置
  - 高吞吐场景可能出问题
- **建议修复**: 
  1. 添加 http Agent 配置
  2. 文档化连接池大小
  3. 监控连接使用情况

#### 17. [序列化] [中严重] [合同版本硬编码为字符串字面量]
- **文件/路径**: `src/platform/contracts/inter-plane-contract-gateway.ts:184`
- **问题描述**: 
  - `schemaVersion: "v4.3"` 硬编码
  - 难以追踪合同版本演变
- **建议修复**: 
  1. 提取到类型化常量或 enum
  2. 集中管理合同版本
  3. 添加版本兼容性检查

#### 18. [序列化] [低严重] [Timestamp = string 丢失精度]
- **文件/路径**: `src/platform/contracts/types/domain/primitives.ts:10`
- **问题描述**: 
  - `type Timestamp = string` 无类型精度
  - ISO 8601 字符串无时区信息
- **建议修复**: 
  1. 使用 Date 对象或 branded string 类型
  2. 明确时区处理约定
  3. 文档化时间戳语义

#### 19. [源代码] [中严重] [29处非空断言 !.]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - 使用 `!.` 断言非空
  - 可能导致运行时错误
- **建议修复**: 
  1. 使用可选链 `?.` 或空值合并 `??`
  2. 添加适当的 null 检查
  3. 减少非空断言使用

#### 20. [API] [中严重] [API 路由 schema 版本硬编码]
- **文件/路径**: `src/platform/five-plane-interface/api/federation-routing-service.ts`
- **问题描述**: 
  - Zod schema 验证存在
  - 但 schema 版本未与 contract 版本同步
- **建议修复**: 
  1. 从 contract 版本派生 schema 版本
  2. 添加版本一致性检查
  3. 文档化版本关系

### 总结

本次补充Review（第十五轮 - 构建测试与类型系统Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. 无 TypeScript 增量编译缓存（每次完整重建9.6秒）
2. 76个 npm scripts 大部分重复构建
3. 168处 any 类型使用（绕过类型安全）
4. 38处 @ts-ignore 指令
5. ExecutionRecord 类型不匹配（14个文件）
6. 178处直接 process.env 变更无抽象
7. ScopedExternalAccessSandbox performHttpRequest 无超时
8. OIDC 服务 fetch 调用无超时
9. plugin-runtime-child 替换 globalThis.fetch 为 no-op

**中优先级**:
1. 30+处 as unknown as 双类型转换
2. rimraf extraneous 未声明
3. 21,998处 unlinkSync 无集中清理
4. 无统一 mocking 框架
5. Singleton 状态重置未标准化
6. 无显式 http.Agent 配置
7. 合同版本硬编码为字符串
8. Timestamp = string 丢失精度
9. 非空断言使用

**低优先级**:
1. 无分支覆盖率要求
2. API 路由 schema 版本未同步

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 72 | 87 | 39 | 198 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **109** | **182** | **85** | **376** |

### 优先修复建议

**立即处理 (本周内)**:
1. 添加 TypeScript 增量编译 (`incremental: true`)
2. 重构 npm scripts 避免重复构建
3. 减少 any 类型使用到 <50
4. 移除所有 @ts-ignore 指令
5. 修复 ExecutionRecord 类型不匹配
6. 添加 fetch 超时到 OIDC 和 sandbox

**短期内处理 (本月内)**:
1. 创建集中的测试清理 utility
2. 建立统一的 mocking 框架
3. 标准化 singleton 重置机制
4. 添加 http.Agent 连接池配置
5. 提取合同版本到类型化常量

**长期规划**:
1. 实现完整的类型安全（无 any）
2. 建立测试基础设施标准
3. 实现构建缓存优化
4. 完善网络层超时和重试
5. 建立覆盖率门禁标准

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十三轮 - 缓存会话与实时通信Review）

### 发现的新问题

#### 1. [安全] [严重] [CORS allowedMethods 缺少 PUT/PATCH/DELETE]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/response-hardening.ts:12-14`
- **问题描述**: 
  ```typescript
  export const DEFAULT_CORS_CONFIG: CorsConfig = {
    allowedMethods: ["GET", "POST", "OPTIONS"],  // 缺少 PUT, PATCH, DELETE
  ```
  - 浏览器 CORS preflight 会拒绝 PUT/PATCH/DELETE 请求
  - 但路由中有 PATCH /v1/tasks/:id, DELETE endpoints
- **建议修复**: 
  1. 添加 PUT, PATCH, DELETE 到 allowedMethods
  2. 确保 CORS 配置与应用路由方法一致
  3. 添加测试验证所有 HTTP 方法的 CORS preflight

#### 2. [安全] [严重] [Mission Routes 错误响应包装不当]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/mission-routes.ts:101,118-124`
- **问题描述**: 
  ```typescript
  return buildJsonResponse(ctx.requestId, 404, { error: { code: "MISSION_NOT_FOUND" } });
  ```
  - 使用 buildJsonResponse（成功包装）返回错误
  - 错误被包装在成功信封 `{requestId, data: {error: {...}}}` 中
  - 违反 REST 规范
- **建议修复**: 
  1. 使用 buildJsonErrorResponse 处理错误
  2. 确保所有错误路径使用正确的错误信封
  3. 添加 API 一致性测试

#### 3. [源代码] [高严重] [WebSocketBridge 无最大连接数限制]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:103-107`
- **问题描述**: 
  ```typescript
  this.wss = new WebSocketServer({
    server,
    path: WS_PATH,
    maxPayload: options.maxPayloadBytes ?? DEFAULT_MAX_PAYLOAD_BYTES,
    // 无 maxConnections 限制！
  });
  ```
  - 与 dashboard-websocket-server.ts 的 maxClients: 1000 不同
  - 恶意客户端可耗尽服务器资源
- **建议修复**: 
  1. 添加 maxConnections 限制
  2. 实现连接限制拒绝策略
  3. 添加连接计数指标

#### 4. [源代码] [高严重] [pendingAcks 在断开连接时未清理]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:268-282`
- **问题描述**: 
  - handleDisconnection 清理 subscribedTasks, taskSubscribers, slowConsumers
  - 但 pendingAcks Map 未清理
  - 断开的客户端未确认的消息永久留在内存
- **建议修复**: 
  1. 在 handleDisconnection 中删除所有 pendingAcks 条目
  2. 考虑添加超时机制自动清理未确认消息
  3. 记录清理操作的审计日志

#### 5. [安全] [高严重] [硬编码回退密钥]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/audit-event-integrity.ts:41`
- **问题描述**: 
  ```typescript
  const AUDIT_INTEGRITY_HMAC_KEY = process.env["AA_AUDIT_INTEGRITY_HMAC_KEY"] ?? "audit-integrity-secret-key-32-bytes!";
  ```
  - 占位符密钥用作回退
  - 如果环境变量未设置，使用不安全密钥
- **建议修复**: 
  1. 移除回退值，要求环境变量必须设置
  2. 在启动时验证密钥存在且足够强
  3. 如果密钥缺失导致启动失败

#### 6. [配置] [高严重] [config/runtime/test.json 缺少超时配置]
- **文件/路径**: `config/runtime/test.json`
- **问题描述**: 
  - 只有3个字段，缺少 maxConcurrentTasks, defaultTaskTimeoutMs, defaultStepTimeoutMs
  - 回退到默认值可能不适合测试环境
- **建议修复**: 
  1. 添加完整的 timeout/tuning 配置
  2. 确保测试环境有合理的默认值
  3. 文档化必需的配置字段

#### 7. [配置] [高严重] [prod.json 限制过严 - maxConcurrentTasks=1]
- **文件/路径**: `config/runtime/prod.json`
- **问题描述**: 
  - maxConcurrentTasks: 1（vs dev:8, staging:4, pre-prod:6）
  - defaultTaskTimeoutMs: 120000（vs dev:600000）
  - defaultStepTimeoutMs: 60000（vs dev:180000）
  - 生产环境严重受限，可能影响吞吐量
- **建议修复**: 
  1. 评估并调整生产限制
  2. 与业务需求匹配
  3. 添加生产容量测试

#### 8. [源代码] [高严重] [多个服务存在无界 Map 无驱逐策略]
- **文件/路径**: 多个域服务
- **问题描述**: 
  - `domain-knowledge-schema-service.ts:73-75`: schemas, sourceContent, sourceTimestamps 无限制
  - `domain-eval-framework-service.ts:91-95`: frameworks, qualityAxes 等无限制
  - `domain-recipe-service.ts:67-68`: recipes, versions 无限制
  - `domain-risk-profile-service.ts:51`: profiles 无限制
  - Session Maps (session-management.ts:83-89) 无自动清理
- **建议修复**: 
  1. 为所有 Map 实现 LRU 或 TTL 驱逐策略
  2. 添加后台清理任务
  3. 添加大小监控和告警

#### 9. [源代码] [中严重] [taskEventHistory Map 永不清理]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:91,548-555`
- **问题描述**: 
  - taskEventHistory 限制单任务历史为 200 条
  - 但 Map 本身从不清理
  - 取消订阅的任务历史永久保留
- **建议修复**: 
  1. 实现后台清理无订阅者任务的历史
  2. 添加任务订阅者监控
  3. 考虑使用 WeakMap 替代

#### 10. [配置] [中严重] [安全配置 drift - remoteWorkerRegistration 缺失]
- **文件/路径**: `config/security/dev.json`, `staging.json`, `pre-prod.json`
- **问题描述**: 
  - default.json 有完整的 remoteWorkerRegistration 配置
  - dev/staging/pre-prod 只有 approvalMode
  - prod 有 approvalMode 但无 remoteWorkerRegistration
  - 安全配置不一致
- **建议修复**: 
  1. 统一所有环境的 remoteWorkerRegistration
  2. 添加安全配置验证
  3. 确保最低安全基线

#### 11. [API] [中严重] [OpenAPI 端点响应格式不一致]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/utils.ts:207-215`
- **问题描述**: 
  - `/v1/openapi.json` 使用 buildJsonDocumentResponse（无包装）
  - 其他端点使用 buildJsonResponse（{requestId, data} 包装）
  - 客户端体验不一致
- **建议修复**: 
  1. 统一响应信封格式
  2. 文档化响应格式规范
  3. 添加响应格式验证测试

#### 12. [源代码] [中严重] [Redis 队列缺少 idempotency 索引支持]
- **文件/路径**: `src/platform/five-plane-execution/queue/redis-queue-adapter.ts`
- **问题描述**: 
  - SQLite 有部分唯一索引支持 idempotency
  - Redis 实现使用哈希索引但实现不完整
  - 可能导致重复消息
- **建议修复**: 
  1. 完善 Redis idempotency 实现
  2. 添加唯一索引验证
  3. 确保与 SQLite 行为一致

#### 13. [源代码] [中严重] [请求去重中间件使用内存存储]
- **文件/路径**: `src/platform/five-plane-interface/api/middleware/request-deduplication.ts`
- **问题描述**: 
  - 纯内存滑动窗口（Map<DeduplicationKey, DeduplicationEntry[]>）
  - 无外部存储
  - 多实例部署不共享
  - 重启后去重状态丢失
- **建议修复**: 
  1. 使用 Redis 替代内存存储
  2. 支持分布式去重
  3. 持久化去重状态

#### 14. [源代码] [中严重] [Cache 无 stampede 保护]
- **文件/路径**: 多个 cache 实现
- **问题描述**: 
  - MemoryCacheStore, ExperienceCacheService 无锁
  - 缓存未命中时可发生 thundering herd
  - 高并发下可能导致数据库过载
- **建议修复**: 
  1. 实现 single-flight 模式
  2. 添加请求排队机制
  3. 使用分布式锁保护缓存更新

#### 15. [源代码] [中严重] [EvidenceService eviction 仅在插入时触发]
- **文件/路径**: `src/platform/five-plane-state-evidence/memory/evidence-service.ts:202`
- **问题描述**: 
  - 清理仅在 record() 调用时触发
  - 空闲时无后台清理
  - 可能导致内存持续增长
- **建议修复**: 
  1. 添加定期后台清理任务
  2. 使用独立的清理线程
  3. 添加内存使用监控

#### 16. [源代码] [低严重] [无连接指标暴露]
- **文件/路径**: WebSocket 服务器
- **问题描述**: 
  - getClientCount() 存在但未通过 HTTP 端点暴露
  - 无 pendingAcks 队列深度指标
  - 无连接建立告警
- **建议修复**: 
  1. 通过 metrics 端点暴露连接指标
  2. 添加 pendingAcks 队列监控
  3. 添加连接数异常告警

#### 17. [源代码] [低严重] [无空闲客户端超时]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts`
- **问题描述**: 
  - 仅在心跳 Sweep 时检查 isAlive
  - 无 per-client 独立于心跳的空闲超时
  - 认证后从不发送消息的客户端只能通过心跳失败检测
- **建议修复**: 
  1. 添加 per-client idle timeout
  2. 独立于心跳间隔
  3. 配置可调整

#### 18. [源代码] [低严重] [订阅限制是 per-client 而非全局]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:244`
- **问题描述**: 
  - MAX_SUBSCRIPTIONS_PER_CLIENT = 100
  - 恶意客户端可订阅 100 个任务
  - 无全局任务订阅者限制
- **建议修复**: 
  1. 添加全局任务订阅者限制
  2. 实现 per-task 订阅者上限
  3. 添加反滥用检测

#### 19. [配置] [低严重] [config/runtime 无版本验证机制]
- **文件/路径**: `config/runtime/*.json`
- **问题描述**: 
  - 配置有 "version": "v4.3"
  - 但无 schema 版本验证
  - 加载时可能接受不兼容配置
- **建议修复**: 
  1. 添加 JSON Schema 验证
  2. 实现版本兼容性检查
  3. 启动时验证配置完整性

#### 20. [源代码] [低严重] [ImprovementCandidateRegistry splice 是 O(n)]
- **文件/路径**: `src/platform/five-plane-orchestration/improve/improvement-candidate-registry.ts:243`
- **问题描述**: 
  - accessOrder 数组的 splice 操作是 O(n)
  - 高频访问时可能性能问题
- **建议修复**: 
  1. 使用 LinkedList 替代数组
  2. 或使用 Map 维护访问顺序
  3. 性能测试验证

### 总结

本次补充Review（第十三轮 - 缓存会话与实时通信Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. CORS allowedMethods 缺少 PUT/PATCH/DELETE（浏览器请求会失败）
2. Mission Routes 错误响应包装不当
3. WebSocketBridge 无最大连接数限制（DoS风险）
4. pendingAcks 断开连接时未清理（内存泄漏）
5. 硬编码回退密钥（安全漏洞）
6. config/runtime/test.json 缺少超时配置
7. prod.json maxConcurrentTasks=1 限制过严
8. 多个服务存在无界 Map（内存泄漏风险）

**中优先级**:
1. taskEventHistory Map 永不清理
2. 安全配置 drift（remoteWorkerRegistration 缺失）
3. OpenAPI 端点响应格式不一致
4. Redis 队列 idempotency 实现不完整
5. 请求去重中间件使用内存存储（多实例不共享）
6. Cache 无 stampede 保护
7. EvidenceService eviction 仅在插入时触发

**低优先级**:
1. 无连接指标暴露
2. 无空闲客户端超时
3. 订阅限制是 per-client 而非全局
4. config/runtime 无版本验证机制
5. ImprovementCandidateRegistry splice O(n)

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 64 | 74 | 35 | 173 |
| 测试 | 8 | 14 | 5 | 27 |
| 配置 | 10 | 38 | 24 | 72 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 7 | 3 | 12 |
| UI | 0 | 3 | 3 | 6 |
| 部署 | 1 | 10 | 6 | 17 |
| **合计** | **99** | **161** | **79** | **339** |

### 优先修复建议

**立即处理 (本周内)**:
1. 修复 CORS allowedMethods 添加 PUT/PATCH/DELETE
2. 修复 Mission Routes 错误响应格式
3. 为 WebSocketBridge 添加最大连接数限制
4. 清理 pendingAcks 和 taskEventHistory
5. 移除硬编码回退密钥
6. 修复 config/runtime/test.json 配置
7. 评估并调整 prod 并发限制

**短期内处理 (本月内)**:
1. 统一安全配置（remoteWorkerRegistration）
2. 实现无界 Map 的 LRU/TTL 驱逐
3. 统一 API 响应信封格式
4. 完善 Redis idempotency 实现
5. 添加分布式去重中间件
6. 实现 cache stampede 保护

**长期规划**:
1. 建立完整的连接管理和监控
2. 实现后台清理任务框架
3. 添加配置 schema 验证
4. 优化高频繁操作的数据结构
5. 建立内存使用基线和告警

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十五轮 - 构建测试与类型系统Review）

### 发现的新问题

#### 1. [构建] [高严重] [无 TypeScript 增量编译缓存]
- **文件/路径**: `tsconfig.json`
- **问题描述**: 
  - 无 `incremental: true` 配置
  - 无 `tsbuildinfo` 文件
  - 每次构建是完整重建（9.6秒）
  - TypeScript 无法使用增量编译
- **建议修复**: 
  1. 在 tsconfig.json 添加 `"incremental": true`
  2. 添加 `"tsbuildinfo": ".tsbuildinfo"` 到 .gitignore
  3. 实现项目引用以支持并行构建

#### 2. [构建] [高严重] [76个 npm scripts 大部分重复构建]
- **文件/路径**: `package.json`
- **问题描述**: 
  - 40+ CLI scripts 都运行 `npm run build && node ...`
  - 每个脚本前都执行完整重建
  - 无法直接运行预构建的 CLI
  - 20+ stable:* 脚本遵循相同模式
- **建议修复**: 
  1. 创建 `build:cli` 脚本构建一次
  2. 让 CLI 脚本使用预构建的 dist 文件
  3. 添加 npm run dev 或类似的无需重建的执行方式

#### 3. [源代码] [高严重] [168处 any 类型使用]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - `stable-runner-factory.ts`: `opts: any` -> `any`
  - `billing-service-async.ts`: `input: any` 方法签名
  - 重试/超时工具使用 `(...args: any[]) => Promise<any>`
  - 绕过类型安全
- **建议修复**: 
  1. 使用泛型替代 any
  2. 替换为具体的输入类型
  3. 添加 ESLint 规则禁止 any

#### 4. [源代码] [高严重] [38处 @ts-ignore 指令]
- **文件/路径**: 多个文件
- **问题描述**: 
  - 14个 stability 相关文件有 `@ts-ignore ExecutionRecord type mismatch`
  - 5个 harness-sdk 文件有 `Partial<HarnessRun> doesn't have all required properties`
  - 2个文件有 `exactOptionalPropertyTypes` 问题
- **建议修复**: 
  1. 修复 ExecutionRecord 类型不匹配
  2. 完成 HarnessRun 类型的必需属性
  3. 移除 @ts-ignore 使用

#### 5. [源代码] [高严重] [ExecutionRecord 类型不匹配]
- **文件/路径**: `src/platform/stability/stable-evidence-bundle-support.ts:783` 等14个文件
- **问题描述**: 
  - 插入 execution record 时 TypeScript 拒绝类型
  - `ExecutionRecord` 接口要求新字段但 insert 对象缺失
  - 或 store 的 insert 方法接受不同类型
- **建议修复**: 
  1. 对齐 ExecutionRecord 接口和 store 类型
  2. 添加缺失字段到 insert 对象
  3. 或调整 store 的 insert 方法类型签名

#### 6. [源代码] [中严重] [30+处 as unknown as 双类型转换]
- **文件/路径**: `division-loader.ts`, `domain-baseline-catalog.ts`, `harness-sdk`
- **问题描述**: 
  - 完全绕过类型安全
  - 通常是类型架构问题的信号
- **建议修复**: 
  1. 修复上游类型问题
  2. 使用更具体的类型断言
  3. 重构类型层次结构

#### 7. [构建] [中严重] [rimraf  extraneous - 未在 package.json 声明]
- **文件/路径**: `node_modules/rimraf`
- **问题描述**: 
  - rimraf@6.1.3 存在于 node_modules 但未在 package.json 声明
  - 可能导致依赖问题
- **建议修复**: 
  1. 添加到 dependencies 或 devDependencies
  2. 或移除并使用其他清理方式

#### 8. [测试] [高严重] [178处直接 process.env 变更无抽象]
- **文件/路径**: `tests/unit/domains/registry/plugin-spi-registry.test.ts:640-687`
- **问题描述**: 
  - 直接读取/写入 process.env
  - 在 beforeEach/afterEach 中手动保存/恢复
  - 无 helper 抽象
- **建议修复**: 
  1. 创建 env helper 函数
  2. 统一保存和恢复模式
  3. 添加测试隔离验证

#### 9. [测试] [高严重] [21,998处 unlinkSync 调用无集中清理]
- **文件/路径**: 多个测试文件
- **问题描述**: 
  - 每个测试重新实现清理逻辑
  - 无集中清理 utility
  - 可能导致 EBUSY (Windows) 或并发访问问题
- **建议修复**: 
  1. 创建集中的测试清理 utility
  2. 使用 afterEach 确保清理
  3. 考虑使用临时目录

#### 10. [测试] [中严重] [无统一 mocking 框架]
- **文件/路径**: `tests/` 多个目录
- **问题描述**: 
  - 10,579 处 vi.fn, jest.mock, sinon 等调用
  - 无统一的 mocking 框架可见
  - 模式不一致
- **建议修复**: 
  1. 建立统一的 mock factory
  2. 标准化 mock 模式
  3. 添加 mocking 最佳实践文档

#### 11. [测试] [中严重] [Singleton 状态重置未标准化]
- **文件/路径**: `tests/unit/platform/five-plane-execution/dispatcher.test.ts`
- **问题描述**: 
  - 测试通过特定函数重置 singletons
  - 每个测试重复自己的 reset 逻辑
  - 无统一的 resetAllSingletons()
- **建议修复**: 
  1. 创建统一的 singleton reset 机制
  2. 在 afterEach 中自动调用
  3. 文档化 singleton 重置要求

#### 12. [测试] [低严重] [无分支覆盖率要求]
- **文件/路径**: `.c8rc.json`
- **问题描述**: 
  - `"100": false` - 无覆盖率要求
  - 只测量源代码，不测量测试文件
- **建议修复**: 
  1. 添加分支覆盖率阈值
  2. 在 CI 中强制执行覆盖率 gate
  3. 关注关键代码路径

#### 13. [网络] [高严重] [ScopedExternalAccessSandbox performHttpRequest 无超时]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/scoped-external-access-sandbox.ts:289`
- **问题描述**: 
  - fetch 调用无 timeout 参数
  - 挂起的请求可能永久阻塞
- **建议修复**: 
  1. 添加 AbortController timeout
  2. 使用标准的超时模式
  3. 记录超时配置

#### 14. [网络] [高严重] [OIDC 服务 fetch 调用无超时]
- **文件/路径**: `src/org-governance/sso-scim/oidc/oidc-service.ts:565,272`
- **问题描述**: 
  - OIDC token 交换无 retry logic
  - 无 timeout/abort signal
  - 认证流程可能挂起
- **建议修复**: 
  1. 添加 fetch timeout
  2. 添加 retry logic
  3. 使用标准的超时配置

#### 15. [网络] [高严重] [plugin-runtime-child 替换 globalThis.fetch 为 no-op]
- **文件/路径**: `src/domains/registry/plugin-runtime-child.ts:112`
- **问题描述**: 
  ```typescript
  globalThis.fetch = (async () => {}) as unknown as typeof globalThis.fetch;
  ```
  - 插件上下文中完全替换 fetch 为空函数
  - 插件的所有出站 HTTP 请求都会失败
  - 看起来是有意为之但可能造成问题
- **建议修复**: 
  1. 确认这是有意行为
  2. 文档化 fetch 替换行为
  3. 确保不会意外影响其他代码

#### 16. [网络] [中严重] [无显式 http.Agent 配置]
- **文件/路径**: 全局
- **问题描述**: 
  - 无显式的 http.Agent/https.Agent 配置
  - 依赖 Node.js 默认连接池
  - 无 maxSockets 或 maxConnections 配置
  - 高吞吐场景可能出问题
- **建议修复**: 
  1. 添加 http Agent 配置
  2. 文档化连接池大小
  3. 监控连接使用情况

#### 17. [序列化] [中严重] [合同版本硬编码为字符串字面量]
- **文件/路径**: `src/platform/contracts/inter-plane-contract-gateway.ts:184`
- **问题描述**: 
  - `schemaVersion: "v4.3"` 硬编码
  - 难以追踪合同版本演变
- **建议修复**: 
  1. 提取到类型化常量或 enum
  2. 集中管理合同版本
  3. 添加版本兼容性检查

#### 18. [序列化] [低严重] [Timestamp = string 丢失精度]
- **文件/路径**: `src/platform/contracts/types/domain/primitives.ts:10`
- **问题描述**: 
  - `type Timestamp = string` 无类型精度
  - ISO 8601 字符串无时区信息
- **建议修复**: 
  1. 使用 Date 对象或 branded string 类型
  2. 明确时区处理约定
  3. 文档化时间戳语义

#### 19. [源代码] [中严重] [29处非空断言 !.]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - 使用 `!.` 断言非空
  - 可能导致运行时错误
- **建议修复**: 
  1. 使用可选链 `?.` 或空值合并 `??`
  2. 添加适当的 null 检查
  3. 减少非空断言使用

#### 20. [API] [中严重] [API 路由 schema 版本硬编码]
- **文件/路径**: `src/platform/five-plane-interface/api/federation-routing-service.ts`
- **问题描述**: 
  - Zod schema 验证存在
  - 但 schema 版本未与 contract 版本同步
- **建议修复**: 
  1. 从 contract 版本派生 schema 版本
  2. 添加版本一致性检查
  3. 文档化版本关系

### 总结

本次补充Review（第十五轮 - 构建测试与类型系统Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. 无 TypeScript 增量编译缓存（每次完整重建9.6秒）
2. 76个 npm scripts 大部分重复构建
3. 168处 any 类型使用（绕过类型安全）
4. 38处 @ts-ignore 指令
5. ExecutionRecord 类型不匹配（14个文件）
6. 178处直接 process.env 变更无抽象
7. ScopedExternalAccessSandbox performHttpRequest 无超时
8. OIDC 服务 fetch 调用无超时
9. plugin-runtime-child 替换 globalThis.fetch 为 no-op

**中优先级**:
1. 30+处 as unknown as 双类型转换
2. rimraf extraneous 未声明
3. 21,998处 unlinkSync 无集中清理
4. 无统一 mocking 框架
5. Singleton 状态重置未标准化
6. 无显式 http.Agent 配置
7. 合同版本硬编码为字符串
8. Timestamp = string 丢失精度
9. 非空断言使用

**低优先级**:
1. 无分支覆盖率要求
2. API 路由 schema 版本未同步

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 72 | 87 | 39 | 198 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **109** | **182** | **85** | **376** |

### 优先修复建议

**立即处理 (本周内)**:
1. 添加 TypeScript 增量编译 (`incremental: true`)
2. 重构 npm scripts 避免重复构建
3. 减少 any 类型使用到 <50
4. 移除所有 @ts-ignore 指令
5. 修复 ExecutionRecord 类型不匹配
6. 添加 fetch 超时到 OIDC 和 sandbox

**短期内处理 (本月内)**:
1. 创建集中的测试清理 utility
2. 建立统一的 mocking 框架
3. 标准化 singleton 重置机制
4. 添加 http.Agent 连接池配置
5. 提取合同版本到类型化常量

**长期规划**:
1. 实现完整的类型安全（无 any）
2. 建立测试基础设施标准
3. 实现构建缓存优化
4. 完善网络层超时和重试
5. 建立覆盖率门禁标准

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十四轮 - 日志事件与工作流Review）

### 发现的新问题

#### 1. [可观测性] [高严重] [StructuredLogEntry 无 requestId 字段]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - StructuredLogEntry 接口无 requestId 字段
  - 只有 correlationId，它回退到 traceId
  - 无法独立追踪单个 HTTP 请求
- **建议修复**: 
  1. 在 StructuredLogEntry 添加 requestId 字段
  2. 在 HTTP 中间件中生成和注入 requestId
  3. 确保 requestId 流经所有日志调用

#### 2. [可观测性] [高严重] [StructuredLogger data 字段无自动清洗]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - 如果开发者传递 `{ password: "..." }` 给 logger.info，密码会被记录
  - 无字段级脱敏机制
  - 依赖开发者手动排除敏感字段
- **建议修复**: 
  1. 添加 OBSERVE_OUTPUT_BLACKLIST 类似的黑名单
  2. 实现 redact/mask/sanitize 工具函数
  3. 在日志传输前自动清洗敏感字段

#### 3. [可观测性] [中严重] [日志中敏感数据无 PII 处理]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - 无 PII 重写工具
  - 开发者必须手动避免记录敏感数据
  - 存在意外记录敏感信息的风险
- **建议修复**: 
  1. 添加 PII 检测和重写工具
  2. 在文档中明确记录敏感字段列表
  3. 添加测试验证敏感数据不被记录

#### 4. [可观测性] [中严重] [59处 console.* 直接调用]
- **文件/路径**: 多个源文件
- **问题描述**: 
  - 应该使用 StructuredLogger
  - console 调用可能不被日志聚合系统收集
  - 难以追踪和关联
- **建议修复**: 
  1. 替换所有 console.* 为 StructuredLogger
  2. 添加 ESLint 规则禁止 console.*
  3. 保留少量允许的 CLI 输出

#### 5. [可观测性] [中严重] [部分 logger.warn 调用缺少结构化数据]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:122`
- **问题描述**: 
  ```typescript
  logger.warn("WebSocket connection rejected: missing subprotocol token")
  ```
  - 只有消息，无 data 对象
  - 难以关联和查询
- **建议修复**: 
  1. 添加 `{ data: { ... } }` 对象
  2. 确保所有日志包含上下文数据
  3. 添加日志审查工具

#### 6. [源代码] [高严重] [Harness while(true) 循环无硬性迭代上限]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:1442`
- **问题描述**: 
  ```typescript
  while (true) {
    // ...
    // 依赖 budget gate 和 guardrail vibration 退出
    // 如果两者都失败，循环永久继续
  }
  ```
  - 依赖 budget gate 和 guardrail vibration 退出
  - 无硬性迭代次数限制
  - 可能导致无限循环
- **建议修复**: 
  1. 添加硬性 maxIterations 上限
  2. 在达到上限时强制退出
  3. 记录详细的迭代统计用于调试

#### 7. [源代码] [高严重] [Oapeflir while(true) 循环可能无限重规划]
- **文件/路径**: `src/platform/five-plane-orchestration/oapeflir/oapeflir-loop-service.ts:355`
- **问题描述**: 
  - 如果 loopReplanDecision.shouldReplan 永不变为 false
  - 重规划永远无法减少计划错误
  - 可能导致无限循环
- **建议修复**: 
  1. 添加重规划次数上限
  2. 在达到上限时强制接受当前结果
  3. 添加重规划质量衰减检测

#### 8. [源代码] [中严重] [Recovery 流程允许终态转换到 paused]
- **文件/路径**: `src/platform/five-plane-execution/runtime-state-machine.ts:105-108`
- **问题描述**: 
  - completed, failed, cancelled, aborted 可转换到 paused 进行恢复
  - 创建恢复路径可能重新进入执行
  - 需要验证此行为的正确性
- **建议修复**: 
  1. 审查 recovery 转换的合法性
  2. 确保不会导致状态不一致
  3. 添加转换前置条件验证

#### 9. [国际化] [高严重] [API 错误消息硬编码英文]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/api-error.ts`
- **问题描述**: 
  ```typescript
  return new ApiError(404, "api.task_not_found", "Task not found.");
  ```
  - 错误消息硬编码英文
  - 无后端 i18n 机制
  - 非英文客户端收到不本地化的错误
- **建议修复**: 
  1. 使用错误码而非硬编码消息
  2. 客户端根据错误码本地化
  3. 或在后端添加 i18n 支持

#### 10. [国际化] [中严重] [UI 硬编码字符串未翻译]
- **文件/路径**: `ui/packages/features/conversation/src/web/index.tsx`
- **问题描述**: 
  - "Build Plan", "Execute", "Messages" 等按钮标签硬编码
  - 不使用 i18n 系统
- **建议修复**: 
  1. 使用 `translateFeatureCopy` 替换硬编码
  2. 确保所有用户可见文本通过 i18n
  3. 添加硬编码字符串检测

#### 11. [国际化] [中严重] [Arabic 目录不完整]
- **文件/路径**: `ui/packages/shared/i18n/src/catalogs/ar-SA.ts`
- **问题描述**: 
  - ar-SA 只有约18条消息
  - en-US 和 zh-CN 有约117条
  - 缺失的 key 返回 key 本身
- **建议修复**: 
  1. 补充 Arabic 翻译
  2. 确保所有 key 都有翻译
  3. 添加翻译完整性测试

#### 12. [国际化] [低严重] [translateFeatureCopy 无回退默认值]
- **文件/路径**: `ui/packages/shared/i18n/`
- **问题描述**: 
  - 如果 feature ID 不在 catalog 中
  - 返回 undefined title/summary
  - 无合理回退
- **建议修复**: 
  1. 添加回退到 key 或默认字符串
  2. 或使用 feature ID 作为显示名
  3. 确保 UI 不显示 undefined

#### 13. [运维] [高严重] [健康检查无 readiness/liveness 区分]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts`
- **问题描述**: 
  - /healthz, /v1/healthz, /health 返回相同报告
  - 无 readiness probe（可处理流量？）
  - 无 liveness probe（系统存活？）
  - canAcceptTraffic() 未通过 HTTP 暴露
- **建议修复**: 
  1. 添加 /ready 和 /live 端点
  2. Readiness 检查依赖项就绪
  3. Liveness 检查进程健康

#### 14. [运维] [高严重] [shutdown 期间健康检查存在竞态]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts`
- **问题描述**: 
  - getHealthReportAsync() 异步检查 dbWritable
  - shutdown 期间数据库连接可能关闭
  - 健康检查仍在传入
- **建议修复**: 
  1. 在 shutdown 时返回 unhealthy
  2. 添加 shutdown 状态标志
  3. 停止接受新的健康检查请求

#### 15. [运维] [高严重] [LeaderElectionService 未与 GracefulShutdown 集成]
- **文件/路径**: `src/platform/five-plane-execution/ha/leader-election-service.ts`
- **问题描述**: 
  - LeaderElectionService.stop() 处理优雅退位
  - 但未在全局 graceful shutdown 中注册
  - 需显式注册为 shutdown handler
- **建议修复**: 
  1. 将 LeaderElectionService 添加到 shutdown handlers
  2. 确保领导者在 shutdown 时让位
  3. 验证关闭顺序正确

#### 16. [运维] [中严重] [addHandler() 无显式排序约定]
- **文件/路径**: `src/platform/five-plane-execution/startup/graceful-shutdown.ts`
- **问题描述**: 
  - 虽然 handlers 逆序执行
  - 但无显式接口声明 handler 依赖关系
  - 关键 handlers（如关闭数据库连接）应最后运行
- **建议修复**: 
  1. 添加优先级或阶段参数
  2. 文档化关键 handler 顺序要求
  3. 添加顺序验证测试

#### 17. [运维] [中严重] [unref'd timers 可能不触发]
- **文件/路径**: `src/platform/five-plane-execution/startup/process-error-handlers.ts:106`
- **问题描述**: 
  - `setTimeout(...).unref()` 不保持事件循环活跃
  - 如果主线程空闲，强制退出可能不触发
- **建议修复**: 
  1. 移除 .unref() 除非确实需要
  2. 或确保有其他方式保持事件循环
  3. 添加超时触发验证测试

#### 18. [运维] [中严重] [StartupConsistencyChecker 阻塞流量但不暴露状态]
- **文件/路径**: `src/platform/five-plane-startup-plan.ts`
- **问题描述**: 
  - canAcceptTraffic() 在 P0 问题存在时返回 false
  - 但 _trafficBlocked 是内部状态
  - 负载均衡器访问 /healthz 仍会得到 "ok"
- **建议修复**: 
  1. 在健康报告中暴露 trafficBlocked 状态
  2. 或添加单独的健康端点指示就绪状态
  3. 确保负载均衡器在阻塞时不路由流量

#### 19. [事件系统] [中严重] [TypedEventBus vs EventEmitter 混用]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - 项目同时使用 TypedEventBus 和原生 EventEmitter
  - TypedEventBus 应该是标准
  - 混用可能导致类型安全问题
- **建议修复**: 
  1. 创建迁移计划统一使用 TypedEventBus
  2. 添加 ESLint 规则禁止原生 EventEmitter
  3. 在 CI 中检测混用情况

#### 20. [源代码] [低严重] [无事件排序保证]
- **文件/路径**: `src/platform/five-plane-state-evidence/events/`
- **问题描述**: 
  - 无明确的事件排序保证文档
  - 事件处理可能乱序
  - 依赖方需要处理乱序
- **建议修复**: 
  1. 文档化事件排序语义
  2. 如果需要排序，实现序列号机制
  3. 在事件处理中考虑乱序情况

### 总结

本次补充Review（第十四轮 - 日志事件与工作流Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. StructuredLogEntry 无 requestId 字段
2. StructuredLogger data 字段无自动清洗（敏感数据风险）
3. Harness while(true) 循环无硬性迭代上限
4. Oapeflir while(true) 循环可能无限重规划
5. API 错误消息硬编码英文（无 i18n）
6. 健康检查无 readiness/liveness 区分
7. shutdown 期间健康检查存在竞态
8. LeaderElectionService 未与 GracefulShutdown 集成

**中优先级**:
1. 日志中敏感数据无 PII 处理
2. 59处 console.* 直接调用
3. 部分 logger.warn 调用缺少结构化数据
4. Recovery 流程允许终态转换到 paused
5. UI 硬编码字符串未翻译
6. Arabic 目录不完整
7. addHandler() 无显式排序约定
8. unref'd timers 可能不触发
9. StartupConsistencyChecker 阻塞流量但不暴露状态
10. TypedEventBus vs EventEmitter 混用

**低优先级**:
1. translateFeatureCopy 无回退默认值
2. 无事件排序保证

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 68 | 80 | 37 | 185 |
| 测试 | 8 | 14 | 5 | 27 |
| 配置 | 10 | 38 | 24 | 72 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **103** | **171** | **82** | **356** |

### 优先修复建议

**立即处理 (本周内)**:
1. 在 StructuredLogEntry 添加 requestId 字段
2. 实现日志敏感字段自动清洗
3. 为工作流循环添加硬性迭代上限
4. 添加 readiness/liveness 健康检查端点
5. 将 LeaderElectionService 集成到 GracefulShutdown
6. 实现 API 错误消息 i18n 支持

**短期内处理 (本月内)**:
1. 替换所有 console.* 为 StructuredLogger
2. 补充 Arabic i18n 目录
3. 修复 UI 硬编码字符串
4. 统一 TypedEventBus 替代 EventEmitter
5. 解决 shutdown 竞态条件
6. 添加事件排序保证文档

**长期规划**:
1. 建立完整的 PII 处理框架
2. 实现日志完整性验证
3. 建立工作流安全循环检测
4. 完善健康检查和就绪探测
5. 建立国际化完整测试

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十五轮 - 构建测试与类型系统Review）

### 发现的新问题

#### 1. [构建] [高严重] [无 TypeScript 增量编译缓存]
- **文件/路径**: `tsconfig.json`
- **问题描述**: 
  - 无 `incremental: true` 配置
  - 无 `tsbuildinfo` 文件
  - 每次构建是完整重建（9.6秒）
  - TypeScript 无法使用增量编译
- **建议修复**: 
  1. 在 tsconfig.json 添加 `"incremental": true`
  2. 添加 `"tsbuildinfo": ".tsbuildinfo"` 到 .gitignore
  3. 实现项目引用以支持并行构建

#### 2. [构建] [高严重] [76个 npm scripts 大部分重复构建]
- **文件/路径**: `package.json`
- **问题描述**: 
  - 40+ CLI scripts 都运行 `npm run build && node ...`
  - 每个脚本前都执行完整重建
  - 无法直接运行预构建的 CLI
  - 20+ stable:* 脚本遵循相同模式
- **建议修复**: 
  1. 创建 `build:cli` 脚本构建一次
  2. 让 CLI 脚本使用预构建的 dist 文件
  3. 添加 npm run dev 或类似的无需重建的执行方式

#### 3. [源代码] [高严重] [168处 any 类型使用]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - `stable-runner-factory.ts`: `opts: any` -> `any`
  - `billing-service-async.ts`: `input: any` 方法签名
  - 重试/超时工具使用 `(...args: any[]) => Promise<any>`
  - 绕过类型安全
- **建议修复**: 
  1. 使用泛型替代 any
  2. 替换为具体的输入类型
  3. 添加 ESLint 规则禁止 any

#### 4. [源代码] [高严重] [38处 @ts-ignore 指令]
- **文件/路径**: 多个文件
- **问题描述**: 
  - 14个 stability 相关文件有 `@ts-ignore ExecutionRecord type mismatch`
  - 5个 harness-sdk 文件有 `Partial<HarnessRun> doesn't have all required properties`
  - 2个文件有 `exactOptionalPropertyTypes` 问题
- **建议修复**: 
  1. 修复 ExecutionRecord 类型不匹配
  2. 完成 HarnessRun 类型的必需属性
  3. 移除 @ts-ignore 使用

#### 5. [源代码] [高严重] [ExecutionRecord 类型不匹配]
- **文件/路径**: `src/platform/stability/stable-evidence-bundle-support.ts:783` 等14个文件
- **问题描述**: 
  - 插入 execution record 时 TypeScript 拒绝类型
  - `ExecutionRecord` 接口要求新字段但 insert 对象缺失
  - 或 store 的 insert 方法接受不同类型
- **建议修复**: 
  1. 对齐 ExecutionRecord 接口和 store 类型
  2. 添加缺失字段到 insert 对象
  3. 或调整 store 的 insert 方法类型签名

#### 6. [源代码] [中严重] [30+处 as unknown as 双类型转换]
- **文件/路径**: `division-loader.ts`, `domain-baseline-catalog.ts`, `harness-sdk`
- **问题描述**: 
  - 完全绕过类型安全
  - 通常是类型架构问题的信号
- **建议修复**: 
  1. 修复上游类型问题
  2. 使用更具体的类型断言
  3. 重构类型层次结构

#### 7. [构建] [中严重] [rimraf  extraneous - 未在 package.json 声明]
- **文件/路径**: `node_modules/rimraf`
- **问题描述**: 
  - rimraf@6.1.3 存在于 node_modules 但未在 package.json 声明
  - 可能导致依赖问题
- **建议修复**: 
  1. 添加到 dependencies 或 devDependencies
  2. 或移除并使用其他清理方式

#### 8. [测试] [高严重] [178处直接 process.env 变更无抽象]
- **文件/路径**: `tests/unit/domains/registry/plugin-spi-registry.test.ts:640-687`
- **问题描述**: 
  - 直接读取/写入 process.env
  - 在 beforeEach/afterEach 中手动保存/恢复
  - 无 helper 抽象
- **建议修复**: 
  1. 创建 env helper 函数
  2. 统一保存和恢复模式
  3. 添加测试隔离验证

#### 9. [测试] [高严重] [21,998处 unlinkSync 调用无集中清理]
- **文件/路径**: 多个测试文件
- **问题描述**: 
  - 每个测试重新实现清理逻辑
  - 无集中清理 utility
  - 可能导致 EBUSY (Windows) 或并发访问问题
- **建议修复**: 
  1. 创建集中的测试清理 utility
  2. 使用 afterEach 确保清理
  3. 考虑使用临时目录

#### 10. [测试] [中严重] [无统一 mocking 框架]
- **文件/路径**: `tests/` 多个目录
- **问题描述**: 
  - 10,579 处 vi.fn, jest.mock, sinon 等调用
  - 无统一的 mocking 框架可见
  - 模式不一致
- **建议修复**: 
  1. 建立统一的 mock factory
  2. 标准化 mock 模式
  3. 添加 mocking 最佳实践文档

#### 11. [测试] [中严重] [Singleton 状态重置未标准化]
- **文件/路径**: `tests/unit/platform/five-plane-execution/dispatcher.test.ts`
- **问题描述**: 
  - 测试通过特定函数重置 singletons
  - 每个测试重复自己的 reset 逻辑
  - 无统一的 resetAllSingletons()
- **建议修复**: 
  1. 创建统一的 singleton reset 机制
  2. 在 afterEach 中自动调用
  3. 文档化 singleton 重置要求

#### 12. [测试] [低严重] [无分支覆盖率要求]
- **文件/路径**: `.c8rc.json`
- **问题描述**: 
  - `"100": false` - 无覆盖率要求
  - 只测量源代码，不测量测试文件
- **建议修复**: 
  1. 添加分支覆盖率阈值
  2. 在 CI 中强制执行覆盖率 gate
  3. 关注关键代码路径

#### 13. [网络] [高严重] [ScopedExternalAccessSandbox performHttpRequest 无超时]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/scoped-external-access-sandbox.ts:289`
- **问题描述**: 
  - fetch 调用无 timeout 参数
  - 挂起的请求可能永久阻塞
- **建议修复**: 
  1. 添加 AbortController timeout
  2. 使用标准的超时模式
  3. 记录超时配置

#### 14. [网络] [高严重] [OIDC 服务 fetch 调用无超时]
- **文件/路径**: `src/org-governance/sso-scim/oidc/oidc-service.ts:565,272`
- **问题描述**: 
  - OIDC token 交换无 retry logic
  - 无 timeout/abort signal
  - 认证流程可能挂起
- **建议修复**: 
  1. 添加 fetch timeout
  2. 添加 retry logic
  3. 使用标准的超时配置

#### 15. [网络] [高严重] [plugin-runtime-child 替换 globalThis.fetch 为 no-op]
- **文件/路径**: `src/domains/registry/plugin-runtime-child.ts:112`
- **问题描述**: 
  ```typescript
  globalThis.fetch = (async () => {}) as unknown as typeof globalThis.fetch;
  ```
  - 插件上下文中完全替换 fetch 为空函数
  - 插件的所有出站 HTTP 请求都会失败
  - 看起来是有意为之但可能造成问题
- **建议修复**: 
  1. 确认这是有意行为
  2. 文档化 fetch 替换行为
  3. 确保不会意外影响其他代码

#### 16. [网络] [中严重] [无显式 http.Agent 配置]
- **文件/路径**: 全局
- **问题描述**: 
  - 无显式的 http.Agent/https.Agent 配置
  - 依赖 Node.js 默认连接池
  - 无 maxSockets 或 maxConnections 配置
  - 高吞吐场景可能出问题
- **建议修复**: 
  1. 添加 http Agent 配置
  2. 文档化连接池大小
  3. 监控连接使用情况

#### 17. [序列化] [中严重] [合同版本硬编码为字符串字面量]
- **文件/路径**: `src/platform/contracts/inter-plane-contract-gateway.ts:184`
- **问题描述**: 
  - `schemaVersion: "v4.3"` 硬编码
  - 难以追踪合同版本演变
- **建议修复**: 
  1. 提取到类型化常量或 enum
  2. 集中管理合同版本
  3. 添加版本兼容性检查

#### 18. [序列化] [低严重] [Timestamp = string 丢失精度]
- **文件/路径**: `src/platform/contracts/types/domain/primitives.ts:10`
- **问题描述**: 
  - `type Timestamp = string` 无类型精度
  - ISO 8601 字符串无时区信息
- **建议修复**: 
  1. 使用 Date 对象或 branded string 类型
  2. 明确时区处理约定
  3. 文档化时间戳语义

#### 19. [源代码] [中严重] [29处非空断言 !.]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - 使用 `!.` 断言非空
  - 可能导致运行时错误
- **建议修复**: 
  1. 使用可选链 `?.` 或空值合并 `??`
  2. 添加适当的 null 检查
  3. 减少非空断言使用

#### 20. [API] [中严重] [API 路由 schema 版本硬编码]
- **文件/路径**: `src/platform/five-plane-interface/api/federation-routing-service.ts`
- **问题描述**: 
  - Zod schema 验证存在
  - 但 schema 版本未与 contract 版本同步
- **建议修复**: 
  1. 从 contract 版本派生 schema 版本
  2. 添加版本一致性检查
  3. 文档化版本关系

### 总结

本次补充Review（第十五轮 - 构建测试与类型系统Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. 无 TypeScript 增量编译缓存（每次完整重建9.6秒）
2. 76个 npm scripts 大部分重复构建
3. 168处 any 类型使用（绕过类型安全）
4. 38处 @ts-ignore 指令
5. ExecutionRecord 类型不匹配（14个文件）
6. 178处直接 process.env 变更无抽象
7. ScopedExternalAccessSandbox performHttpRequest 无超时
8. OIDC 服务 fetch 调用无超时
9. plugin-runtime-child 替换 globalThis.fetch 为 no-op

**中优先级**:
1. 30+处 as unknown as 双类型转换
2. rimraf extraneous 未声明
3. 21,998处 unlinkSync 无集中清理
4. 无统一 mocking 框架
5. Singleton 状态重置未标准化
6. 无显式 http.Agent 配置
7. 合同版本硬编码为字符串
8. Timestamp = string 丢失精度
9. 非空断言使用

**低优先级**:
1. 无分支覆盖率要求
2. API 路由 schema 版本未同步

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 72 | 87 | 39 | 198 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **109** | **182** | **85** | **376** |

### 优先修复建议

**立即处理 (本周内)**:
1. 添加 TypeScript 增量编译 (`incremental: true`)
2. 重构 npm scripts 避免重复构建
3. 减少 any 类型使用到 <50
4. 移除所有 @ts-ignore 指令
5. 修复 ExecutionRecord 类型不匹配
6. 添加 fetch 超时到 OIDC 和 sandbox

**短期内处理 (本月内)**:
1. 创建集中的测试清理 utility
2. 建立统一的 mocking 框架
3. 标准化 singleton 重置机制
4. 添加 http.Agent 连接池配置
5. 提取合同版本到类型化常量

**长期规划**:
1. 实现完整的类型安全（无 any）
2. 建立测试基础设施标准
3. 实现构建缓存优化
4. 完善网络层超时和重试
5. 建立覆盖率门禁标准

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十一轮 - 并发安全与可观测性Review）

### 发现的新问题

#### 1. [源代码] [严重] [FencingTokenService 静态可变状态无锁保护]
- **文件/路径**: `src/platform/five-plane-state-evidence/events/cas/fencing-token-service.ts:71-73`
- **问题描述**: 
  ```typescript
  private static readonly activeFences = new Map<string, FenceInfo>();
  private static globalTokenCounter = 0;
  ```
  - `acquireFence` 方法执行非原子的 read-then-write 模式
  - 两个并发调用可能同时通过检查，导致状态不一致
  - 静态 Map 在无锁情况下被并发读写
- **建议修复**: 
  1. 添加互斥锁保护 activeFences 访问
  2. 使用原子操作或 Compare-And-Swap
  3. 考虑使用数据库行锁替代内存锁

#### 2. [源代码] [严重] [AsyncFencingTokenService.globalTokenCounter 不安全递增]
- **文件/路径**: `src/platform/five-plane-state-evidence/events/cas/postgres-fencing-token-service.ts:13-17`
- **问题描述**: 
  ```typescript
  private static readonly globalTokenCounter = {
    value: 0,
    getAndIncrement(): number { return ++this.value; }
  };
  ```
  - 经典的 read-modify-write 竞态条件
  - 多线程可能读到相同的值
- **建议修复**: 
  1. 使用 `pg_advisory_xact_lock` 保护
  2. 或使用数据库自增序列
  3. 移除静态计数器，使用数据库生成

#### 3. [源代码] [严重] [BudgetAllocator.activeReservations 无保护 Map]
- **文件/路径**: `src/platform/five-plane-execution/budget-allocator.ts:394,426,458,467,569,657,711`
- **问题描述**: 
  ```typescript
  this.activeReservations.set(result.reservation.budgetReservationId, result.reservation);
  this.activeReservations.delete(reservationId);
  ```
  - 并发代码路径修改 activeReservations Map
  - 无锁保护
- **建议修复**: 
  1. 添加 Map 级别的读写锁
  2. 使用 ConcurrentMap 或类似结构
  3. 重构为无状态设计

#### 4. [源代码] [高严重] [SqliteLockAdapter.fencingCounter 不同步]
- **文件/路径**: `src/platform/five-plane-execution/distributed-lock/sqlite-lock-adapter.ts:9,14,48,74,88`
- **问题描述**: 
  - `fencingCounter` 在 acquire/extend/forceSteal 中递增
  - 无同步机制
- **建议修复**: 
  1. 使用 SQLite 序列替代内存计数器
  2. 或添加适当的同步机制

#### 5. [源代码] [高严重] [ServiceRegistry.getInstance() 检查-then-创建 竞态]
- **文件/路径**: `src/platform/shared/lifecycle/service-registry.ts:85-88`
- **问题描述**: 
  ```typescript
  public static getInstance(): ServiceRegistry {
    if (!ServiceRegistry._instance) {
      ServiceRegistry._instance = new ServiceRegistry();
    }
    return ServiceRegistry._instance;
  }
  ```
  - 两个线程可能同时看到 _instance 为 null
  - 创建两个实例
- **建议修复**: 
  1. 使用 double-checked locking
  2. 或使用 ES2022 静态字段初始化
  3. `private static readonly _instance = new ServiceRegistry();`

#### 6. [源代码] [中严重] [StructuredLogger.rotationStateByPath 异步竞态]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts:142-144,438-453`
- **问题描述**: 
  ```typescript
  private static rotationStateByPath = new Map<string, {...}>();
  ```
  - `writeToGlobalFileSink` 中存在 TOCTOU 竞态
  - 读取 pendingBytes 和实际写入之间可能被其他调用修改
- **建议修复**: 
  1. 使用锁保护 rotationStateByPath
  2. 或使用单线程写入队列

#### 7. [源代码] [中严重] [EffectBuffer.scopes 并发修改风险]
- **文件/路径**: `src/platform/five-plane-execution/execution-engine/effect-buffer.ts:446,452,476,485,492,502-514`
- **问题描述**: 
  - `scopes.delete(scopeId)` 与 `for (const [scopeId, scope] of this.scopes)` 并发
  - flush() 和 createScope 可能同时修改
- **建议修复**: 
  1. 添加迭代锁
  2. 使用 CopyOnWrite 模式
  3. 在修改时复制一份 scopes

#### 8. [源代码] [中严重] [OIDC _skipSignatureVerification 标志存在风险]
- **文件/路径**: `src/platform/five-plane-interface/api/oidc-oauth-service.ts:92,108-111`
- **问题描述**: 
  - `_skipSignatureVerification` 标志存在于生产代码路径
  - 虽然标记为仅测试使用，但配置错误可能导致安全问题
- **建议修复**: 
  1. 移除此标志或确保编译时完全删除
  2. 添加生产环境检查抛出错误
  3. 在代码审查中标记为禁止模式

#### 9. [源代码] [中严重] [Browser Executor 检测但不消毒 innerHTML]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/browser-executor.ts:541`
- **问题描述**: 
  - 检测到 innerHTML 但只返回假数据
  - 不进行实际消毒
  - 用户可能绕过保护
- **建议修复**: 
  1. 使用 DOMPurify 或类似库消毒
  2. 或完全禁止 innerHTML
  3. 返回错误而非假数据

#### 10. [数据库] [高严重] [runtime-physical-schema 无外键约束]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/runtime-physical-schema.ts`
- **问题描述**: 
  - `task_draft_id`, `confirmed_task_spec_id`, `harness_run_id` 等列无 FK 约束
  - 可能导致孤儿记录
  - 删除父记录时子记录变为孤立
- **建议修复**: 
  1. 添加外键约束
  2. 实现软删除而非硬删除
  3. 添加数据库迁移脚本

#### 11. [数据库] [中严重] [大部分表缺少软删除和审计字段]
- **文件/路径**: 多个表（除 mission_records 外）
- **问题描述**: 
  - 只有 mission_records 有完整审计：created_at, created_by, updated_at, updated_by, archived_at, archived_by
  - 大部分表缺少 deleted_at, is_deleted 软删除字段
  - created_by/updated_by 仅在 mission_records 系列表中存在
- **建议修复**: 
  1. 为所有核心表添加审计字段
  2. 实现软删除模式
  3. 添加规范说明哪些表必须有审计

#### 12. [CI/CD] [高严重] [UI quality workflow 无覆盖率门禁]
- **文件/路径**: `.github/workflows/ui-quality.yml:33`
- **问题描述**: 
  - 运行 `test:coverage` 但无阈值执行
  - 缺少 `coverage:gate`
  - 与主 CI 不一致
- **建议修复**: 
  1. 添加 `npm run coverage:gate` 到 UI workflow
  2. 或确保 UI 更改时运行主 CI

#### 13. [CI/CD] [高严重] [部署工作流无真正的手动审批]
- **文件/路径**: `.github/workflows/deploy-environment.yml`
- **问题描述**: 
  - `workflow_dispatch` 有环境保护但无显式审批步骤
  - deploy.sh 的生产确认使用 `read -p` 在 CI/CD 中会挂起
  - 生产部署缺少真正的审批控制
- **建议修复**: 
  1. 使用 GitHub Environment `required_reviewers`
  2. 修改 deploy.sh 检测 CI 环境跳过交互
  3. 添加显式审批步骤

#### 14. [CI/CD] [中严重] [回滚机制边界情况未处理]
- **文件/路径**: `.github/workflows/deploy-environment.yml:230-278`, `deploy/scripts/rollback.sh`
- **问题描述**: 
  - 部署成功但 health check 失败时不会触发回滚
  - rollback.sh 只支持 dev|staging|prod（deploy.sh 支持 dev|test|staging|pre-prod|prod）
  - pre-prod 命名空间不一致：automatic-agent-preprod vs automatic-agent-pre-prod
- **建议修复**: 
  1. 添加 health check 失败时的回滚触发
  2. 统一 rollback.sh 支持所有环境
  3. 修正 pre-prod 命名空间

#### 15. [CI/CD] [中严重] [Canary/Blue-Green 升级无健康检查]
- **文件/路径**: `.github/workflows/deploy-environment.yml:188-199`
- **问题描述**: 
  - 初始部署有 health checks
  - 但 promotion 步骤无健康验证
  - 可能推送不健康的版本
- **建议修复**: 
  1. 在 promotion 步骤后添加健康检查
  2. 验证推送的版本通过健康检查
  3. 失败时回滚

#### 16. [可观测性] [高严重] [58处 console.* 使用应使用 StructuredLogger]
- **文件/路径**: 多个文件
- **问题描述**: 
  - `src/index.ts`: 4次
  - `src/platform/ops-maturity/platform-panic/panic-propagation-service.ts`: 2 console.error + 1 console.log
  - `src/platform/five-plane-orchestration/agent-delegation/delegation-manager.service.ts`: 1 console.error
  - `src/platform/five-plane-orchestration/harness/runtime/runtime-entry-guard.ts`: 3 console.warn
  - `src/ops-maturity/chaos/chaos-experiment-scheduler.ts`: 5 console.log + 4 console.warn
  - CLI 工具中多处
- **建议修复**: 
  1. 替换 console.* 为 StructuredLogger
  2. 保留 CLI 输出的 console 用法（可接受）
  3. 添加 ESLint 规则检测

#### 17. [可观测性] [高严重] [29+执行引擎文件缺少结构化日志]
- **文件/路径**: `src/platform/five-plane-execution/execution-engine/`
- **问题描述**: 
  - `runtime-state-machine.ts`: 有 try/catch 但无 StructuredLogger
  - `multi-step-orchestration.ts`: 导入 HealthService 但无 StructuredLogger
  - `agent-executor.ts`: 169个 try 块，0 StructuredLogger 调用
  - 关键执行路径缺少可观测性
- **建议修复**: 
  1. 为所有执行引擎文件添加 StructuredLogger
  2. 记录关键操作路径
  3. 确保错误路径有日志

#### 18. [可观测性] [高严重] [执行引擎缺少 LLM 调用指标]
- **文件/路径**: `src/platform/five-plane-execution/execution-engine/`
- **问题描述**: 
  - RuntimeMetricsRegistry 支持 `recordLlmLatency`, `recordHarnessRunDuration` 等
  - 但执行引擎中无调用
  - model-gateway 有指标但执行引擎无
- **建议修复**: 
  1. 在 agent-executor 中记录 LLM 调用指标
  2. 在 multi-step-orchestration 中记录 harness 指标
  3. 启用完整指标覆盖

#### 19. [可观测性] [中严重] [日志方法使用不一致]
- **文件/路径**: 多个服务
- **问题描述**: 
  - 有的使用 `.error()`, `.warn()` 辅助方法
  - 有的使用 `logger.log({ level: "error", ... })`
  - 无统一规范
- **建议修复**: 
  1. 建立日志方法规范
  2. 添加 ESLint 规则统一
  3. 重构为一致的 helper 方法调用

#### 20. [安全] [中严重] [输入验证有限 - 仅 JWT 强类型]
- **文件/路径**: 多个 API 端点
- **问题描述**: 
  - JWT claims 有强类型验证
  - 其他输入验证有限
  - 缺少通用输入消毒
- **建议修复**: 
  1. 添加通用输入验证中间件
  2. 使用 zod 或类似库验证请求体
  3. 为所有 API 端点添加验证

#### 21. [源代码] [低严重] [HA Coordinator Leadership 获取可能非原子]
- **文件/路径**: `src/platform/five-plane-execution/ha/ha-coordinator-service-inner.ts:70-78`
- **问题描述**: 
  - `acquireLeadership` 可能执行非原子的 read-modify-write
  - leadership epoch 更新可能存在竞态
- **建议修复**: 
  1. 使用数据库行锁保护
  2. 确保领导者选举原子性
  3. 添加领导者租约续约机制

### 总结

本次补充Review（第十一轮 - 并发安全与可观测性Review - 2026-05-14）发现了21个新问题，重点关注并发安全、可观测性和CI/CD。

**高优先级 (需要立即处理)**:
1. 7个并发竞态问题（FencingTokenService, BudgetAllocator, ServiceRegistry等）
2. runtime-physical-schema 无外键约束
3. UI quality workflow 无覆盖率门禁
4. 58处 console.* 使用应替换为 StructuredLogger
5. 29+ 执行引擎文件缺少结构化日志
6. 执行引擎缺少 LLM 调用指标

**中优先级**:
1. OIDC _skipSignatureVerification 标志风险
2. Browser Executor innerHTML 不消毒
3. 大部分表缺少软删除和审计字段
4. 部署工作流无真正手动审批
5. 回滚机制边界情况未处理
6. Canary/Blue-Green 升级无健康检查
7. 日志方法使用不一致
8. 输入验证有限

**低优先级**:
1. HA Coordinator Leadership 获取可能非原子
2. SqliteLockAdapter fencingCounter 不同步

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 58 | 62 | 29 | 149 |
| 测试 | 8 | 14 | 5 | 27 |
| 配置 | 8 | 31 | 19 | 58 |
| 安全 | 7 | 8 | 1 | 16 |
| 文档 | 2 | 7 | 3 | 12 |
| UI | 0 | 3 | 3 | 6 |
| 部署 | 1 | 10 | 6 | 17 |
| **合计** | **84** | **135** | **66** | **285** |

### 优先修复建议

**立即处理 (本周内)**:
1. 修复所有7个并发竞态问题
2. 添加 runtime-physical-schema 外键约束
3. 为 UI quality workflow 添加覆盖率门禁
4. 替换关键路径的 console.* 为 StructuredLogger
5. 在执行引擎中添加 LLM 和 harness 指标

**短期内处理 (本月内)**:
1. 为所有执行引擎文件添加结构化日志
2. 添加数据库审计字段和软删除
3. 修正部署工作流审批机制
4. 统一日志方法使用规范
5. 添加 Canary/Blue-Green 健康检查

**长期规划**:
1. 建立并发安全编码规范
2. 实现完整的可观测性标准
3. 完善数据库约束和审计机制
4. 增强 CI/CD 质量和安全门禁
5. 建立代码审查清单

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十五轮 - 构建测试与类型系统Review）

### 发现的新问题

#### 1. [构建] [高严重] [无 TypeScript 增量编译缓存]
- **文件/路径**: `tsconfig.json`
- **问题描述**: 
  - 无 `incremental: true` 配置
  - 无 `tsbuildinfo` 文件
  - 每次构建是完整重建（9.6秒）
  - TypeScript 无法使用增量编译
- **建议修复**: 
  1. 在 tsconfig.json 添加 `"incremental": true`
  2. 添加 `"tsbuildinfo": ".tsbuildinfo"` 到 .gitignore
  3. 实现项目引用以支持并行构建

#### 2. [构建] [高严重] [76个 npm scripts 大部分重复构建]
- **文件/路径**: `package.json`
- **问题描述**: 
  - 40+ CLI scripts 都运行 `npm run build && node ...`
  - 每个脚本前都执行完整重建
  - 无法直接运行预构建的 CLI
  - 20+ stable:* 脚本遵循相同模式
- **建议修复**: 
  1. 创建 `build:cli` 脚本构建一次
  2. 让 CLI 脚本使用预构建的 dist 文件
  3. 添加 npm run dev 或类似的无需重建的执行方式

#### 3. [源代码] [高严重] [168处 any 类型使用]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - `stable-runner-factory.ts`: `opts: any` -> `any`
  - `billing-service-async.ts`: `input: any` 方法签名
  - 重试/超时工具使用 `(...args: any[]) => Promise<any>`
  - 绕过类型安全
- **建议修复**: 
  1. 使用泛型替代 any
  2. 替换为具体的输入类型
  3. 添加 ESLint 规则禁止 any

#### 4. [源代码] [高严重] [38处 @ts-ignore 指令]
- **文件/路径**: 多个文件
- **问题描述**: 
  - 14个 stability 相关文件有 `@ts-ignore ExecutionRecord type mismatch`
  - 5个 harness-sdk 文件有 `Partial<HarnessRun> doesn't have all required properties`
  - 2个文件有 `exactOptionalPropertyTypes` 问题
- **建议修复**: 
  1. 修复 ExecutionRecord 类型不匹配
  2. 完成 HarnessRun 类型的必需属性
  3. 移除 @ts-ignore 使用

#### 5. [源代码] [高严重] [ExecutionRecord 类型不匹配]
- **文件/路径**: `src/platform/stability/stable-evidence-bundle-support.ts:783` 等14个文件
- **问题描述**: 
  - 插入 execution record 时 TypeScript 拒绝类型
  - `ExecutionRecord` 接口要求新字段但 insert 对象缺失
  - 或 store 的 insert 方法接受不同类型
- **建议修复**: 
  1. 对齐 ExecutionRecord 接口和 store 类型
  2. 添加缺失字段到 insert 对象
  3. 或调整 store 的 insert 方法类型签名

#### 6. [源代码] [中严重] [30+处 as unknown as 双类型转换]
- **文件/路径**: `division-loader.ts`, `domain-baseline-catalog.ts`, `harness-sdk`
- **问题描述**: 
  - 完全绕过类型安全
  - 通常是类型架构问题的信号
- **建议修复**: 
  1. 修复上游类型问题
  2. 使用更具体的类型断言
  3. 重构类型层次结构

#### 7. [构建] [中严重] [rimraf  extraneous - 未在 package.json 声明]
- **文件/路径**: `node_modules/rimraf`
- **问题描述**: 
  - rimraf@6.1.3 存在于 node_modules 但未在 package.json 声明
  - 可能导致依赖问题
- **建议修复**: 
  1. 添加到 dependencies 或 devDependencies
  2. 或移除并使用其他清理方式

#### 8. [测试] [高严重] [178处直接 process.env 变更无抽象]
- **文件/路径**: `tests/unit/domains/registry/plugin-spi-registry.test.ts:640-687`
- **问题描述**: 
  - 直接读取/写入 process.env
  - 在 beforeEach/afterEach 中手动保存/恢复
  - 无 helper 抽象
- **建议修复**: 
  1. 创建 env helper 函数
  2. 统一保存和恢复模式
  3. 添加测试隔离验证

#### 9. [测试] [高严重] [21,998处 unlinkSync 调用无集中清理]
- **文件/路径**: 多个测试文件
- **问题描述**: 
  - 每个测试重新实现清理逻辑
  - 无集中清理 utility
  - 可能导致 EBUSY (Windows) 或并发访问问题
- **建议修复**: 
  1. 创建集中的测试清理 utility
  2. 使用 afterEach 确保清理
  3. 考虑使用临时目录

#### 10. [测试] [中严重] [无统一 mocking 框架]
- **文件/路径**: `tests/` 多个目录
- **问题描述**: 
  - 10,579 处 vi.fn, jest.mock, sinon 等调用
  - 无统一的 mocking 框架可见
  - 模式不一致
- **建议修复**: 
  1. 建立统一的 mock factory
  2. 标准化 mock 模式
  3. 添加 mocking 最佳实践文档

#### 11. [测试] [中严重] [Singleton 状态重置未标准化]
- **文件/路径**: `tests/unit/platform/five-plane-execution/dispatcher.test.ts`
- **问题描述**: 
  - 测试通过特定函数重置 singletons
  - 每个测试重复自己的 reset 逻辑
  - 无统一的 resetAllSingletons()
- **建议修复**: 
  1. 创建统一的 singleton reset 机制
  2. 在 afterEach 中自动调用
  3. 文档化 singleton 重置要求

#### 12. [测试] [低严重] [无分支覆盖率要求]
- **文件/路径**: `.c8rc.json`
- **问题描述**: 
  - `"100": false` - 无覆盖率要求
  - 只测量源代码，不测量测试文件
- **建议修复**: 
  1. 添加分支覆盖率阈值
  2. 在 CI 中强制执行覆盖率 gate
  3. 关注关键代码路径

#### 13. [网络] [高严重] [ScopedExternalAccessSandbox performHttpRequest 无超时]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/scoped-external-access-sandbox.ts:289`
- **问题描述**: 
  - fetch 调用无 timeout 参数
  - 挂起的请求可能永久阻塞
- **建议修复**: 
  1. 添加 AbortController timeout
  2. 使用标准的超时模式
  3. 记录超时配置

#### 14. [网络] [高严重] [OIDC 服务 fetch 调用无超时]
- **文件/路径**: `src/org-governance/sso-scim/oidc/oidc-service.ts:565,272`
- **问题描述**: 
  - OIDC token 交换无 retry logic
  - 无 timeout/abort signal
  - 认证流程可能挂起
- **建议修复**: 
  1. 添加 fetch timeout
  2. 添加 retry logic
  3. 使用标准的超时配置

#### 15. [网络] [高严重] [plugin-runtime-child 替换 globalThis.fetch 为 no-op]
- **文件/路径**: `src/domains/registry/plugin-runtime-child.ts:112`
- **问题描述**: 
  ```typescript
  globalThis.fetch = (async () => {}) as unknown as typeof globalThis.fetch;
  ```
  - 插件上下文中完全替换 fetch 为空函数
  - 插件的所有出站 HTTP 请求都会失败
  - 看起来是有意为之但可能造成问题
- **建议修复**: 
  1. 确认这是有意行为
  2. 文档化 fetch 替换行为
  3. 确保不会意外影响其他代码

#### 16. [网络] [中严重] [无显式 http.Agent 配置]
- **文件/路径**: 全局
- **问题描述**: 
  - 无显式的 http.Agent/https.Agent 配置
  - 依赖 Node.js 默认连接池
  - 无 maxSockets 或 maxConnections 配置
  - 高吞吐场景可能出问题
- **建议修复**: 
  1. 添加 http Agent 配置
  2. 文档化连接池大小
  3. 监控连接使用情况

#### 17. [序列化] [中严重] [合同版本硬编码为字符串字面量]
- **文件/路径**: `src/platform/contracts/inter-plane-contract-gateway.ts:184`
- **问题描述**: 
  - `schemaVersion: "v4.3"` 硬编码
  - 难以追踪合同版本演变
- **建议修复**: 
  1. 提取到类型化常量或 enum
  2. 集中管理合同版本
  3. 添加版本兼容性检查

#### 18. [序列化] [低严重] [Timestamp = string 丢失精度]
- **文件/路径**: `src/platform/contracts/types/domain/primitives.ts:10`
- **问题描述**: 
  - `type Timestamp = string` 无类型精度
  - ISO 8601 字符串无时区信息
- **建议修复**: 
  1. 使用 Date 对象或 branded string 类型
  2. 明确时区处理约定
  3. 文档化时间戳语义

#### 19. [源代码] [中严重] [29处非空断言 !.]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - 使用 `!.` 断言非空
  - 可能导致运行时错误
- **建议修复**: 
  1. 使用可选链 `?.` 或空值合并 `??`
  2. 添加适当的 null 检查
  3. 减少非空断言使用

#### 20. [API] [中严重] [API 路由 schema 版本硬编码]
- **文件/路径**: `src/platform/five-plane-interface/api/federation-routing-service.ts`
- **问题描述**: 
  - Zod schema 验证存在
  - 但 schema 版本未与 contract 版本同步
- **建议修复**: 
  1. 从 contract 版本派生 schema 版本
  2. 添加版本一致性检查
  3. 文档化版本关系

### 总结

本次补充Review（第十五轮 - 构建测试与类型系统Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. 无 TypeScript 增量编译缓存（每次完整重建9.6秒）
2. 76个 npm scripts 大部分重复构建
3. 168处 any 类型使用（绕过类型安全）
4. 38处 @ts-ignore 指令
5. ExecutionRecord 类型不匹配（14个文件）
6. 178处直接 process.env 变更无抽象
7. ScopedExternalAccessSandbox performHttpRequest 无超时
8. OIDC 服务 fetch 调用无超时
9. plugin-runtime-child 替换 globalThis.fetch 为 no-op

**中优先级**:
1. 30+处 as unknown as 双类型转换
2. rimraf extraneous 未声明
3. 21,998处 unlinkSync 无集中清理
4. 无统一 mocking 框架
5. Singleton 状态重置未标准化
6. 无显式 http.Agent 配置
7. 合同版本硬编码为字符串
8. Timestamp = string 丢失精度
9. 非空断言使用

**低优先级**:
1. 无分支覆盖率要求
2. API 路由 schema 版本未同步

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 72 | 87 | 39 | 198 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **109** | **182** | **85** | **376** |

### 优先修复建议

**立即处理 (本周内)**:
1. 添加 TypeScript 增量编译 (`incremental: true`)
2. 重构 npm scripts 避免重复构建
3. 减少 any 类型使用到 <50
4. 移除所有 @ts-ignore 指令
5. 修复 ExecutionRecord 类型不匹配
6. 添加 fetch 超时到 OIDC 和 sandbox

**短期内处理 (本月内)**:
1. 创建集中的测试清理 utility
2. 建立统一的 mocking 框架
3. 标准化 singleton 重置机制
4. 添加 http.Agent 连接池配置
5. 提取合同版本到类型化常量

**长期规划**:
1. 实现完整的类型安全（无 any）
2. 建立测试基础设施标准
3. 实现构建缓存优化
4. 完善网络层超时和重试
5. 建立覆盖率门禁标准

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十四轮 - 日志事件与工作流Review）

### 发现的新问题

#### 1. [可观测性] [高严重] [StructuredLogEntry 无 requestId 字段]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - StructuredLogEntry 接口无 requestId 字段
  - 只有 correlationId，它回退到 traceId
  - 无法独立追踪单个 HTTP 请求
- **建议修复**: 
  1. 在 StructuredLogEntry 添加 requestId 字段
  2. 在 HTTP 中间件中生成和注入 requestId
  3. 确保 requestId 流经所有日志调用

#### 2. [可观测性] [高严重] [StructuredLogger data 字段无自动清洗]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - 如果开发者传递 `{ password: "..." }` 给 logger.info，密码会被记录
  - 无字段级脱敏机制
  - 依赖开发者手动排除敏感字段
- **建议修复**: 
  1. 添加 OBSERVE_OUTPUT_BLACKLIST 类似的黑名单
  2. 实现 redact/mask/sanitize 工具函数
  3. 在日志传输前自动清洗敏感字段

#### 3. [可观测性] [中严重] [日志中敏感数据无 PII 处理]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - 无 PII 重写工具
  - 开发者必须手动避免记录敏感数据
  - 存在意外记录敏感信息的风险
- **建议修复**: 
  1. 添加 PII 检测和重写工具
  2. 在文档中明确记录敏感字段列表
  3. 添加测试验证敏感数据不被记录

#### 4. [可观测性] [中严重] [59处 console.* 直接调用]
- **文件/路径**: 多个源文件
- **问题描述**: 
  - 应该使用 StructuredLogger
  - console 调用可能不被日志聚合系统收集
  - 难以追踪和关联
- **建议修复**: 
  1. 替换所有 console.* 为 StructuredLogger
  2. 添加 ESLint 规则禁止 console.*
  3. 保留少量允许的 CLI 输出

#### 5. [可观测性] [中严重] [部分 logger.warn 调用缺少结构化数据]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:122`
- **问题描述**: 
  ```typescript
  logger.warn("WebSocket connection rejected: missing subprotocol token")
  ```
  - 只有消息，无 data 对象
  - 难以关联和查询
- **建议修复**: 
  1. 添加 `{ data: { ... } }` 对象
  2. 确保所有日志包含上下文数据
  3. 添加日志审查工具

#### 6. [源代码] [高严重] [Harness while(true) 循环无硬性迭代上限]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:1442`
- **问题描述**: 
  ```typescript
  while (true) {
    // ...
    // 依赖 budget gate 和 guardrail vibration 退出
    // 如果两者都失败，循环永久继续
  }
  ```
  - 依赖 budget gate 和 guardrail vibration 退出
  - 无硬性迭代次数限制
  - 可能导致无限循环
- **建议修复**: 
  1. 添加硬性 maxIterations 上限
  2. 在达到上限时强制退出
  3. 记录详细的迭代统计用于调试

#### 7. [源代码] [高严重] [Oapeflir while(true) 循环可能无限重规划]
- **文件/路径**: `src/platform/five-plane-orchestration/oapeflir/oapeflir-loop-service.ts:355`
- **问题描述**: 
  - 如果 loopReplanDecision.shouldReplan 永不变为 false
  - 重规划永远无法减少计划错误
  - 可能导致无限循环
- **建议修复**: 
  1. 添加重规划次数上限
  2. 在达到上限时强制接受当前结果
  3. 添加重规划质量衰减检测

#### 8. [源代码] [中严重] [Recovery 流程允许终态转换到 paused]
- **文件/路径**: `src/platform/five-plane-execution/runtime-state-machine.ts:105-108`
- **问题描述**: 
  - completed, failed, cancelled, aborted 可转换到 paused 进行恢复
  - 创建恢复路径可能重新进入执行
  - 需要验证此行为的正确性
- **建议修复**: 
  1. 审查 recovery 转换的合法性
  2. 确保不会导致状态不一致
  3. 添加转换前置条件验证

#### 9. [国际化] [高严重] [API 错误消息硬编码英文]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/api-error.ts`
- **问题描述**: 
  ```typescript
  return new ApiError(404, "api.task_not_found", "Task not found.");
  ```
  - 错误消息硬编码英文
  - 无后端 i18n 机制
  - 非英文客户端收到不本地化的错误
- **建议修复**: 
  1. 使用错误码而非硬编码消息
  2. 客户端根据错误码本地化
  3. 或在后端添加 i18n 支持

#### 10. [国际化] [中严重] [UI 硬编码字符串未翻译]
- **文件/路径**: `ui/packages/features/conversation/src/web/index.tsx`
- **问题描述**: 
  - "Build Plan", "Execute", "Messages" 等按钮标签硬编码
  - 不使用 i18n 系统
- **建议修复**: 
  1. 使用 `translateFeatureCopy` 替换硬编码
  2. 确保所有用户可见文本通过 i18n
  3. 添加硬编码字符串检测

#### 11. [国际化] [中严重] [Arabic 目录不完整]
- **文件/路径**: `ui/packages/shared/i18n/src/catalogs/ar-SA.ts`
- **问题描述**: 
  - ar-SA 只有约18条消息
  - en-US 和 zh-CN 有约117条
  - 缺失的 key 返回 key 本身
- **建议修复**: 
  1. 补充 Arabic 翻译
  2. 确保所有 key 都有翻译
  3. 添加翻译完整性测试

#### 12. [国际化] [低严重] [translateFeatureCopy 无回退默认值]
- **文件/路径**: `ui/packages/shared/i18n/`
- **问题描述**: 
  - 如果 feature ID 不在 catalog 中
  - 返回 undefined title/summary
  - 无合理回退
- **建议修复**: 
  1. 添加回退到 key 或默认字符串
  2. 或使用 feature ID 作为显示名
  3. 确保 UI 不显示 undefined

#### 13. [运维] [高严重] [健康检查无 readiness/liveness 区分]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts`
- **问题描述**: 
  - /healthz, /v1/healthz, /health 返回相同报告
  - 无 readiness probe（可处理流量？）
  - 无 liveness probe（系统存活？）
  - canAcceptTraffic() 未通过 HTTP 暴露
- **建议修复**: 
  1. 添加 /ready 和 /live 端点
  2. Readiness 检查依赖项就绪
  3. Liveness 检查进程健康

#### 14. [运维] [高严重] [shutdown 期间健康检查存在竞态]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts`
- **问题描述**: 
  - getHealthReportAsync() 异步检查 dbWritable
  - shutdown 期间数据库连接可能关闭
  - 健康检查仍在传入
- **建议修复**: 
  1. 在 shutdown 时返回 unhealthy
  2. 添加 shutdown 状态标志
  3. 停止接受新的健康检查请求

#### 15. [运维] [高严重] [LeaderElectionService 未与 GracefulShutdown 集成]
- **文件/路径**: `src/platform/five-plane-execution/ha/leader-election-service.ts`
- **问题描述**: 
  - LeaderElectionService.stop() 处理优雅退位
  - 但未在全局 graceful shutdown 中注册
  - 需显式注册为 shutdown handler
- **建议修复**: 
  1. 将 LeaderElectionService 添加到 shutdown handlers
  2. 确保领导者在 shutdown 时让位
  3. 验证关闭顺序正确

#### 16. [运维] [中严重] [addHandler() 无显式排序约定]
- **文件/路径**: `src/platform/five-plane-execution/startup/graceful-shutdown.ts`
- **问题描述**: 
  - 虽然 handlers 逆序执行
  - 但无显式接口声明 handler 依赖关系
  - 关键 handlers（如关闭数据库连接）应最后运行
- **建议修复**: 
  1. 添加优先级或阶段参数
  2. 文档化关键 handler 顺序要求
  3. 添加顺序验证测试

#### 17. [运维] [中严重] [unref'd timers 可能不触发]
- **文件/路径**: `src/platform/five-plane-execution/startup/process-error-handlers.ts:106`
- **问题描述**: 
  - `setTimeout(...).unref()` 不保持事件循环活跃
  - 如果主线程空闲，强制退出可能不触发
- **建议修复**: 
  1. 移除 .unref() 除非确实需要
  2. 或确保有其他方式保持事件循环
  3. 添加超时触发验证测试

#### 18. [运维] [中严重] [StartupConsistencyChecker 阻塞流量但不暴露状态]
- **文件/路径**: `src/platform/five-plane-startup-plan.ts`
- **问题描述**: 
  - canAcceptTraffic() 在 P0 问题存在时返回 false
  - 但 _trafficBlocked 是内部状态
  - 负载均衡器访问 /healthz 仍会得到 "ok"
- **建议修复**: 
  1. 在健康报告中暴露 trafficBlocked 状态
  2. 或添加单独的健康端点指示就绪状态
  3. 确保负载均衡器在阻塞时不路由流量

#### 19. [事件系统] [中严重] [TypedEventBus vs EventEmitter 混用]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - 项目同时使用 TypedEventBus 和原生 EventEmitter
  - TypedEventBus 应该是标准
  - 混用可能导致类型安全问题
- **建议修复**: 
  1. 创建迁移计划统一使用 TypedEventBus
  2. 添加 ESLint 规则禁止原生 EventEmitter
  3. 在 CI 中检测混用情况

#### 20. [源代码] [低严重] [无事件排序保证]
- **文件/路径**: `src/platform/five-plane-state-evidence/events/`
- **问题描述**: 
  - 无明确的事件排序保证文档
  - 事件处理可能乱序
  - 依赖方需要处理乱序
- **建议修复**: 
  1. 文档化事件排序语义
  2. 如果需要排序，实现序列号机制
  3. 在事件处理中考虑乱序情况

### 总结

本次补充Review（第十四轮 - 日志事件与工作流Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. StructuredLogEntry 无 requestId 字段
2. StructuredLogger data 字段无自动清洗（敏感数据风险）
3. Harness while(true) 循环无硬性迭代上限
4. Oapeflir while(true) 循环可能无限重规划
5. API 错误消息硬编码英文（无 i18n）
6. 健康检查无 readiness/liveness 区分
7. shutdown 期间健康检查存在竞态
8. LeaderElectionService 未与 GracefulShutdown 集成

**中优先级**:
1. 日志中敏感数据无 PII 处理
2. 59处 console.* 直接调用
3. 部分 logger.warn 调用缺少结构化数据
4. Recovery 流程允许终态转换到 paused
5. UI 硬编码字符串未翻译
6. Arabic 目录不完整
7. addHandler() 无显式排序约定
8. unref'd timers 可能不触发
9. StartupConsistencyChecker 阻塞流量但不暴露状态
10. TypedEventBus vs EventEmitter 混用

**低优先级**:
1. translateFeatureCopy 无回退默认值
2. 无事件排序保证

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 68 | 80 | 37 | 185 |
| 测试 | 8 | 14 | 5 | 27 |
| 配置 | 10 | 38 | 24 | 72 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **103** | **171** | **82** | **356** |

### 优先修复建议

**立即处理 (本周内)**:
1. 在 StructuredLogEntry 添加 requestId 字段
2. 实现日志敏感字段自动清洗
3. 为工作流循环添加硬性迭代上限
4. 添加 readiness/liveness 健康检查端点
5. 将 LeaderElectionService 集成到 GracefulShutdown
6. 实现 API 错误消息 i18n 支持

**短期内处理 (本月内)**:
1. 替换所有 console.* 为 StructuredLogger
2. 补充 Arabic i18n 目录
3. 修复 UI 硬编码字符串
4. 统一 TypedEventBus 替代 EventEmitter
5. 解决 shutdown 竞态条件
6. 添加事件排序保证文档

**长期规划**:
1. 建立完整的 PII 处理框架
2. 实现日志完整性验证
3. 建立工作流安全循环检测
4. 完善健康检查和就绪探测
5. 建立国际化完整测试

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十五轮 - 构建测试与类型系统Review）

### 发现的新问题

#### 1. [构建] [高严重] [无 TypeScript 增量编译缓存]
- **文件/路径**: `tsconfig.json`
- **问题描述**: 
  - 无 `incremental: true` 配置
  - 无 `tsbuildinfo` 文件
  - 每次构建是完整重建（9.6秒）
  - TypeScript 无法使用增量编译
- **建议修复**: 
  1. 在 tsconfig.json 添加 `"incremental": true`
  2. 添加 `"tsbuildinfo": ".tsbuildinfo"` 到 .gitignore
  3. 实现项目引用以支持并行构建

#### 2. [构建] [高严重] [76个 npm scripts 大部分重复构建]
- **文件/路径**: `package.json`
- **问题描述**: 
  - 40+ CLI scripts 都运行 `npm run build && node ...`
  - 每个脚本前都执行完整重建
  - 无法直接运行预构建的 CLI
  - 20+ stable:* 脚本遵循相同模式
- **建议修复**: 
  1. 创建 `build:cli` 脚本构建一次
  2. 让 CLI 脚本使用预构建的 dist 文件
  3. 添加 npm run dev 或类似的无需重建的执行方式

#### 3. [源代码] [高严重] [168处 any 类型使用]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - `stable-runner-factory.ts`: `opts: any` -> `any`
  - `billing-service-async.ts`: `input: any` 方法签名
  - 重试/超时工具使用 `(...args: any[]) => Promise<any>`
  - 绕过类型安全
- **建议修复**: 
  1. 使用泛型替代 any
  2. 替换为具体的输入类型
  3. 添加 ESLint 规则禁止 any

#### 4. [源代码] [高严重] [38处 @ts-ignore 指令]
- **文件/路径**: 多个文件
- **问题描述**: 
  - 14个 stability 相关文件有 `@ts-ignore ExecutionRecord type mismatch`
  - 5个 harness-sdk 文件有 `Partial<HarnessRun> doesn't have all required properties`
  - 2个文件有 `exactOptionalPropertyTypes` 问题
- **建议修复**: 
  1. 修复 ExecutionRecord 类型不匹配
  2. 完成 HarnessRun 类型的必需属性
  3. 移除 @ts-ignore 使用

#### 5. [源代码] [高严重] [ExecutionRecord 类型不匹配]
- **文件/路径**: `src/platform/stability/stable-evidence-bundle-support.ts:783` 等14个文件
- **问题描述**: 
  - 插入 execution record 时 TypeScript 拒绝类型
  - `ExecutionRecord` 接口要求新字段但 insert 对象缺失
  - 或 store 的 insert 方法接受不同类型
- **建议修复**: 
  1. 对齐 ExecutionRecord 接口和 store 类型
  2. 添加缺失字段到 insert 对象
  3. 或调整 store 的 insert 方法类型签名

#### 6. [源代码] [中严重] [30+处 as unknown as 双类型转换]
- **文件/路径**: `division-loader.ts`, `domain-baseline-catalog.ts`, `harness-sdk`
- **问题描述**: 
  - 完全绕过类型安全
  - 通常是类型架构问题的信号
- **建议修复**: 
  1. 修复上游类型问题
  2. 使用更具体的类型断言
  3. 重构类型层次结构

#### 7. [构建] [中严重] [rimraf  extraneous - 未在 package.json 声明]
- **文件/路径**: `node_modules/rimraf`
- **问题描述**: 
  - rimraf@6.1.3 存在于 node_modules 但未在 package.json 声明
  - 可能导致依赖问题
- **建议修复**: 
  1. 添加到 dependencies 或 devDependencies
  2. 或移除并使用其他清理方式

#### 8. [测试] [高严重] [178处直接 process.env 变更无抽象]
- **文件/路径**: `tests/unit/domains/registry/plugin-spi-registry.test.ts:640-687`
- **问题描述**: 
  - 直接读取/写入 process.env
  - 在 beforeEach/afterEach 中手动保存/恢复
  - 无 helper 抽象
- **建议修复**: 
  1. 创建 env helper 函数
  2. 统一保存和恢复模式
  3. 添加测试隔离验证

#### 9. [测试] [高严重] [21,998处 unlinkSync 调用无集中清理]
- **文件/路径**: 多个测试文件
- **问题描述**: 
  - 每个测试重新实现清理逻辑
  - 无集中清理 utility
  - 可能导致 EBUSY (Windows) 或并发访问问题
- **建议修复**: 
  1. 创建集中的测试清理 utility
  2. 使用 afterEach 确保清理
  3. 考虑使用临时目录

#### 10. [测试] [中严重] [无统一 mocking 框架]
- **文件/路径**: `tests/` 多个目录
- **问题描述**: 
  - 10,579 处 vi.fn, jest.mock, sinon 等调用
  - 无统一的 mocking 框架可见
  - 模式不一致
- **建议修复**: 
  1. 建立统一的 mock factory
  2. 标准化 mock 模式
  3. 添加 mocking 最佳实践文档

#### 11. [测试] [中严重] [Singleton 状态重置未标准化]
- **文件/路径**: `tests/unit/platform/five-plane-execution/dispatcher.test.ts`
- **问题描述**: 
  - 测试通过特定函数重置 singletons
  - 每个测试重复自己的 reset 逻辑
  - 无统一的 resetAllSingletons()
- **建议修复**: 
  1. 创建统一的 singleton reset 机制
  2. 在 afterEach 中自动调用
  3. 文档化 singleton 重置要求

#### 12. [测试] [低严重] [无分支覆盖率要求]
- **文件/路径**: `.c8rc.json`
- **问题描述**: 
  - `"100": false` - 无覆盖率要求
  - 只测量源代码，不测量测试文件
- **建议修复**: 
  1. 添加分支覆盖率阈值
  2. 在 CI 中强制执行覆盖率 gate
  3. 关注关键代码路径

#### 13. [网络] [高严重] [ScopedExternalAccessSandbox performHttpRequest 无超时]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/scoped-external-access-sandbox.ts:289`
- **问题描述**: 
  - fetch 调用无 timeout 参数
  - 挂起的请求可能永久阻塞
- **建议修复**: 
  1. 添加 AbortController timeout
  2. 使用标准的超时模式
  3. 记录超时配置

#### 14. [网络] [高严重] [OIDC 服务 fetch 调用无超时]
- **文件/路径**: `src/org-governance/sso-scim/oidc/oidc-service.ts:565,272`
- **问题描述**: 
  - OIDC token 交换无 retry logic
  - 无 timeout/abort signal
  - 认证流程可能挂起
- **建议修复**: 
  1. 添加 fetch timeout
  2. 添加 retry logic
  3. 使用标准的超时配置

#### 15. [网络] [高严重] [plugin-runtime-child 替换 globalThis.fetch 为 no-op]
- **文件/路径**: `src/domains/registry/plugin-runtime-child.ts:112`
- **问题描述**: 
  ```typescript
  globalThis.fetch = (async () => {}) as unknown as typeof globalThis.fetch;
  ```
  - 插件上下文中完全替换 fetch 为空函数
  - 插件的所有出站 HTTP 请求都会失败
  - 看起来是有意为之但可能造成问题
- **建议修复**: 
  1. 确认这是有意行为
  2. 文档化 fetch 替换行为
  3. 确保不会意外影响其他代码

#### 16. [网络] [中严重] [无显式 http.Agent 配置]
- **文件/路径**: 全局
- **问题描述**: 
  - 无显式的 http.Agent/https.Agent 配置
  - 依赖 Node.js 默认连接池
  - 无 maxSockets 或 maxConnections 配置
  - 高吞吐场景可能出问题
- **建议修复**: 
  1. 添加 http Agent 配置
  2. 文档化连接池大小
  3. 监控连接使用情况

#### 17. [序列化] [中严重] [合同版本硬编码为字符串字面量]
- **文件/路径**: `src/platform/contracts/inter-plane-contract-gateway.ts:184`
- **问题描述**: 
  - `schemaVersion: "v4.3"` 硬编码
  - 难以追踪合同版本演变
- **建议修复**: 
  1. 提取到类型化常量或 enum
  2. 集中管理合同版本
  3. 添加版本兼容性检查

#### 18. [序列化] [低严重] [Timestamp = string 丢失精度]
- **文件/路径**: `src/platform/contracts/types/domain/primitives.ts:10`
- **问题描述**: 
  - `type Timestamp = string` 无类型精度
  - ISO 8601 字符串无时区信息
- **建议修复**: 
  1. 使用 Date 对象或 branded string 类型
  2. 明确时区处理约定
  3. 文档化时间戳语义

#### 19. [源代码] [中严重] [29处非空断言 !.]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - 使用 `!.` 断言非空
  - 可能导致运行时错误
- **建议修复**: 
  1. 使用可选链 `?.` 或空值合并 `??`
  2. 添加适当的 null 检查
  3. 减少非空断言使用

#### 20. [API] [中严重] [API 路由 schema 版本硬编码]
- **文件/路径**: `src/platform/five-plane-interface/api/federation-routing-service.ts`
- **问题描述**: 
  - Zod schema 验证存在
  - 但 schema 版本未与 contract 版本同步
- **建议修复**: 
  1. 从 contract 版本派生 schema 版本
  2. 添加版本一致性检查
  3. 文档化版本关系

### 总结

本次补充Review（第十五轮 - 构建测试与类型系统Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. 无 TypeScript 增量编译缓存（每次完整重建9.6秒）
2. 76个 npm scripts 大部分重复构建
3. 168处 any 类型使用（绕过类型安全）
4. 38处 @ts-ignore 指令
5. ExecutionRecord 类型不匹配（14个文件）
6. 178处直接 process.env 变更无抽象
7. ScopedExternalAccessSandbox performHttpRequest 无超时
8. OIDC 服务 fetch 调用无超时
9. plugin-runtime-child 替换 globalThis.fetch 为 no-op

**中优先级**:
1. 30+处 as unknown as 双类型转换
2. rimraf extraneous 未声明
3. 21,998处 unlinkSync 无集中清理
4. 无统一 mocking 框架
5. Singleton 状态重置未标准化
6. 无显式 http.Agent 配置
7. 合同版本硬编码为字符串
8. Timestamp = string 丢失精度
9. 非空断言使用

**低优先级**:
1. 无分支覆盖率要求
2. API 路由 schema 版本未同步

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 72 | 87 | 39 | 198 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **109** | **182** | **85** | **376** |

### 优先修复建议

**立即处理 (本周内)**:
1. 添加 TypeScript 增量编译 (`incremental: true`)
2. 重构 npm scripts 避免重复构建
3. 减少 any 类型使用到 <50
4. 移除所有 @ts-ignore 指令
5. 修复 ExecutionRecord 类型不匹配
6. 添加 fetch 超时到 OIDC 和 sandbox

**短期内处理 (本月内)**:
1. 创建集中的测试清理 utility
2. 建立统一的 mocking 框架
3. 标准化 singleton 重置机制
4. 添加 http.Agent 连接池配置
5. 提取合同版本到类型化常量

**长期规划**:
1. 实现完整的类型安全（无 any）
2. 建立测试基础设施标准
3. 实现构建缓存优化
4. 完善网络层超时和重试
5. 建立覆盖率门禁标准

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十三轮 - 缓存会话与实时通信Review）

### 发现的新问题

#### 1. [安全] [严重] [CORS allowedMethods 缺少 PUT/PATCH/DELETE]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/response-hardening.ts:12-14`
- **问题描述**: 
  ```typescript
  export const DEFAULT_CORS_CONFIG: CorsConfig = {
    allowedMethods: ["GET", "POST", "OPTIONS"],  // 缺少 PUT, PATCH, DELETE
  ```
  - 浏览器 CORS preflight 会拒绝 PUT/PATCH/DELETE 请求
  - 但路由中有 PATCH /v1/tasks/:id, DELETE endpoints
- **建议修复**: 
  1. 添加 PUT, PATCH, DELETE 到 allowedMethods
  2. 确保 CORS 配置与应用路由方法一致
  3. 添加测试验证所有 HTTP 方法的 CORS preflight

#### 2. [安全] [严重] [Mission Routes 错误响应包装不当]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/mission-routes.ts:101,118-124`
- **问题描述**: 
  ```typescript
  return buildJsonResponse(ctx.requestId, 404, { error: { code: "MISSION_NOT_FOUND" } });
  ```
  - 使用 buildJsonResponse（成功包装）返回错误
  - 错误被包装在成功信封 `{requestId, data: {error: {...}}}` 中
  - 违反 REST 规范
- **建议修复**: 
  1. 使用 buildJsonErrorResponse 处理错误
  2. 确保所有错误路径使用正确的错误信封
  3. 添加 API 一致性测试

#### 3. [源代码] [高严重] [WebSocketBridge 无最大连接数限制]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:103-107`
- **问题描述**: 
  ```typescript
  this.wss = new WebSocketServer({
    server,
    path: WS_PATH,
    maxPayload: options.maxPayloadBytes ?? DEFAULT_MAX_PAYLOAD_BYTES,
    // 无 maxConnections 限制！
  });
  ```
  - 与 dashboard-websocket-server.ts 的 maxClients: 1000 不同
  - 恶意客户端可耗尽服务器资源
- **建议修复**: 
  1. 添加 maxConnections 限制
  2. 实现连接限制拒绝策略
  3. 添加连接计数指标

#### 4. [源代码] [高严重] [pendingAcks 在断开连接时未清理]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:268-282`
- **问题描述**: 
  - handleDisconnection 清理 subscribedTasks, taskSubscribers, slowConsumers
  - 但 pendingAcks Map 未清理
  - 断开的客户端未确认的消息永久留在内存
- **建议修复**: 
  1. 在 handleDisconnection 中删除所有 pendingAcks 条目
  2. 考虑添加超时机制自动清理未确认消息
  3. 记录清理操作的审计日志

#### 5. [安全] [高严重] [硬编码回退密钥]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/audit-event-integrity.ts:41`
- **问题描述**: 
  ```typescript
  const AUDIT_INTEGRITY_HMAC_KEY = process.env["AA_AUDIT_INTEGRITY_HMAC_KEY"] ?? "audit-integrity-secret-key-32-bytes!";
  ```
  - 占位符密钥用作回退
  - 如果环境变量未设置，使用不安全密钥
- **建议修复**: 
  1. 移除回退值，要求环境变量必须设置
  2. 在启动时验证密钥存在且足够强
  3. 如果密钥缺失导致启动失败

#### 6. [配置] [高严重] [config/runtime/test.json 缺少超时配置]
- **文件/路径**: `config/runtime/test.json`
- **问题描述**: 
  - 只有3个字段，缺少 maxConcurrentTasks, defaultTaskTimeoutMs, defaultStepTimeoutMs
  - 回退到默认值可能不适合测试环境
- **建议修复**: 
  1. 添加完整的 timeout/tuning 配置
  2. 确保测试环境有合理的默认值
  3. 文档化必需的配置字段

#### 7. [配置] [高严重] [prod.json 限制过严 - maxConcurrentTasks=1]
- **文件/路径**: `config/runtime/prod.json`
- **问题描述**: 
  - maxConcurrentTasks: 1（vs dev:8, staging:4, pre-prod:6）
  - defaultTaskTimeoutMs: 120000（vs dev:600000）
  - defaultStepTimeoutMs: 60000（vs dev:180000）
  - 生产环境严重受限，可能影响吞吐量
- **建议修复**: 
  1. 评估并调整生产限制
  2. 与业务需求匹配
  3. 添加生产容量测试

#### 8. [源代码] [高严重] [多个服务存在无界 Map 无驱逐策略]
- **文件/路径**: 多个域服务
- **问题描述**: 
  - `domain-knowledge-schema-service.ts:73-75`: schemas, sourceContent, sourceTimestamps 无限制
  - `domain-eval-framework-service.ts:91-95`: frameworks, qualityAxes 等无限制
  - `domain-recipe-service.ts:67-68`: recipes, versions 无限制
  - `domain-risk-profile-service.ts:51`: profiles 无限制
  - Session Maps (session-management.ts:83-89) 无自动清理
- **建议修复**: 
  1. 为所有 Map 实现 LRU 或 TTL 驱逐策略
  2. 添加后台清理任务
  3. 添加大小监控和告警

#### 9. [源代码] [中严重] [taskEventHistory Map 永不清理]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:91,548-555`
- **问题描述**: 
  - taskEventHistory 限制单任务历史为 200 条
  - 但 Map 本身从不清理
  - 取消订阅的任务历史永久保留
- **建议修复**: 
  1. 实现后台清理无订阅者任务的历史
  2. 添加任务订阅者监控
  3. 考虑使用 WeakMap 替代

#### 10. [配置] [中严重] [安全配置 drift - remoteWorkerRegistration 缺失]
- **文件/路径**: `config/security/dev.json`, `staging.json`, `pre-prod.json`
- **问题描述**: 
  - default.json 有完整的 remoteWorkerRegistration 配置
  - dev/staging/pre-prod 只有 approvalMode
  - prod 有 approvalMode 但无 remoteWorkerRegistration
  - 安全配置不一致
- **建议修复**: 
  1. 统一所有环境的 remoteWorkerRegistration
  2. 添加安全配置验证
  3. 确保最低安全基线

#### 11. [API] [中严重] [OpenAPI 端点响应格式不一致]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/utils.ts:207-215`
- **问题描述**: 
  - `/v1/openapi.json` 使用 buildJsonDocumentResponse（无包装）
  - 其他端点使用 buildJsonResponse（{requestId, data} 包装）
  - 客户端体验不一致
- **建议修复**: 
  1. 统一响应信封格式
  2. 文档化响应格式规范
  3. 添加响应格式验证测试

#### 12. [源代码] [中严重] [Redis 队列缺少 idempotency 索引支持]
- **文件/路径**: `src/platform/five-plane-execution/queue/redis-queue-adapter.ts`
- **问题描述**: 
  - SQLite 有部分唯一索引支持 idempotency
  - Redis 实现使用哈希索引但实现不完整
  - 可能导致重复消息
- **建议修复**: 
  1. 完善 Redis idempotency 实现
  2. 添加唯一索引验证
  3. 确保与 SQLite 行为一致

#### 13. [源代码] [中严重] [请求去重中间件使用内存存储]
- **文件/路径**: `src/platform/five-plane-interface/api/middleware/request-deduplication.ts`
- **问题描述**: 
  - 纯内存滑动窗口（Map<DeduplicationKey, DeduplicationEntry[]>）
  - 无外部存储
  - 多实例部署不共享
  - 重启后去重状态丢失
- **建议修复**: 
  1. 使用 Redis 替代内存存储
  2. 支持分布式去重
  3. 持久化去重状态

#### 14. [源代码] [中严重] [Cache 无 stampede 保护]
- **文件/路径**: 多个 cache 实现
- **问题描述**: 
  - MemoryCacheStore, ExperienceCacheService 无锁
  - 缓存未命中时可发生 thundering herd
  - 高并发下可能导致数据库过载
- **建议修复**: 
  1. 实现 single-flight 模式
  2. 添加请求排队机制
  3. 使用分布式锁保护缓存更新

#### 15. [源代码] [中严重] [EvidenceService eviction 仅在插入时触发]
- **文件/路径**: `src/platform/five-plane-state-evidence/memory/evidence-service.ts:202`
- **问题描述**: 
  - 清理仅在 record() 调用时触发
  - 空闲时无后台清理
  - 可能导致内存持续增长
- **建议修复**: 
  1. 添加定期后台清理任务
  2. 使用独立的清理线程
  3. 添加内存使用监控

#### 16. [源代码] [低严重] [无连接指标暴露]
- **文件/路径**: WebSocket 服务器
- **问题描述**: 
  - getClientCount() 存在但未通过 HTTP 端点暴露
  - 无 pendingAcks 队列深度指标
  - 无连接建立告警
- **建议修复**: 
  1. 通过 metrics 端点暴露连接指标
  2. 添加 pendingAcks 队列监控
  3. 添加连接数异常告警

#### 17. [源代码] [低严重] [无空闲客户端超时]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts`
- **问题描述**: 
  - 仅在心跳 Sweep 时检查 isAlive
  - 无 per-client 独立于心跳的空闲超时
  - 认证后从不发送消息的客户端只能通过心跳失败检测
- **建议修复**: 
  1. 添加 per-client idle timeout
  2. 独立于心跳间隔
  3. 配置可调整

#### 18. [源代码] [低严重] [订阅限制是 per-client 而非全局]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:244`
- **问题描述**: 
  - MAX_SUBSCRIPTIONS_PER_CLIENT = 100
  - 恶意客户端可订阅 100 个任务
  - 无全局任务订阅者限制
- **建议修复**: 
  1. 添加全局任务订阅者限制
  2. 实现 per-task 订阅者上限
  3. 添加反滥用检测

#### 19. [配置] [低严重] [config/runtime 无版本验证机制]
- **文件/路径**: `config/runtime/*.json`
- **问题描述**: 
  - 配置有 "version": "v4.3"
  - 但无 schema 版本验证
  - 加载时可能接受不兼容配置
- **建议修复**: 
  1. 添加 JSON Schema 验证
  2. 实现版本兼容性检查
  3. 启动时验证配置完整性

#### 20. [源代码] [低严重] [ImprovementCandidateRegistry splice 是 O(n)]
- **文件/路径**: `src/platform/five-plane-orchestration/improve/improvement-candidate-registry.ts:243`
- **问题描述**: 
  - accessOrder 数组的 splice 操作是 O(n)
  - 高频访问时可能性能问题
- **建议修复**: 
  1. 使用 LinkedList 替代数组
  2. 或使用 Map 维护访问顺序
  3. 性能测试验证

### 总结

本次补充Review（第十三轮 - 缓存会话与实时通信Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. CORS allowedMethods 缺少 PUT/PATCH/DELETE（浏览器请求会失败）
2. Mission Routes 错误响应包装不当
3. WebSocketBridge 无最大连接数限制（DoS风险）
4. pendingAcks 断开连接时未清理（内存泄漏）
5. 硬编码回退密钥（安全漏洞）
6. config/runtime/test.json 缺少超时配置
7. prod.json maxConcurrentTasks=1 限制过严
8. 多个服务存在无界 Map（内存泄漏风险）

**中优先级**:
1. taskEventHistory Map 永不清理
2. 安全配置 drift（remoteWorkerRegistration 缺失）
3. OpenAPI 端点响应格式不一致
4. Redis 队列 idempotency 实现不完整
5. 请求去重中间件使用内存存储（多实例不共享）
6. Cache 无 stampede 保护
7. EvidenceService eviction 仅在插入时触发

**低优先级**:
1. 无连接指标暴露
2. 无空闲客户端超时
3. 订阅限制是 per-client 而非全局
4. config/runtime 无版本验证机制
5. ImprovementCandidateRegistry splice O(n)

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 64 | 74 | 35 | 173 |
| 测试 | 8 | 14 | 5 | 27 |
| 配置 | 10 | 38 | 24 | 72 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 7 | 3 | 12 |
| UI | 0 | 3 | 3 | 6 |
| 部署 | 1 | 10 | 6 | 17 |
| **合计** | **99** | **161** | **79** | **339** |

### 优先修复建议

**立即处理 (本周内)**:
1. 修复 CORS allowedMethods 添加 PUT/PATCH/DELETE
2. 修复 Mission Routes 错误响应格式
3. 为 WebSocketBridge 添加最大连接数限制
4. 清理 pendingAcks 和 taskEventHistory
5. 移除硬编码回退密钥
6. 修复 config/runtime/test.json 配置
7. 评估并调整 prod 并发限制

**短期内处理 (本月内)**:
1. 统一安全配置（remoteWorkerRegistration）
2. 实现无界 Map 的 LRU/TTL 驱逐
3. 统一 API 响应信封格式
4. 完善 Redis idempotency 实现
5. 添加分布式去重中间件
6. 实现 cache stampede 保护

**长期规划**:
1. 建立完整的连接管理和监控
2. 实现后台清理任务框架
3. 添加配置 schema 验证
4. 优化高频繁操作的数据结构
5. 建立内存使用基线和告警

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十五轮 - 构建测试与类型系统Review）

### 发现的新问题

#### 1. [构建] [高严重] [无 TypeScript 增量编译缓存]
- **文件/路径**: `tsconfig.json`
- **问题描述**: 
  - 无 `incremental: true` 配置
  - 无 `tsbuildinfo` 文件
  - 每次构建是完整重建（9.6秒）
  - TypeScript 无法使用增量编译
- **建议修复**: 
  1. 在 tsconfig.json 添加 `"incremental": true`
  2. 添加 `"tsbuildinfo": ".tsbuildinfo"` 到 .gitignore
  3. 实现项目引用以支持并行构建

#### 2. [构建] [高严重] [76个 npm scripts 大部分重复构建]
- **文件/路径**: `package.json`
- **问题描述**: 
  - 40+ CLI scripts 都运行 `npm run build && node ...`
  - 每个脚本前都执行完整重建
  - 无法直接运行预构建的 CLI
  - 20+ stable:* 脚本遵循相同模式
- **建议修复**: 
  1. 创建 `build:cli` 脚本构建一次
  2. 让 CLI 脚本使用预构建的 dist 文件
  3. 添加 npm run dev 或类似的无需重建的执行方式

#### 3. [源代码] [高严重] [168处 any 类型使用]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - `stable-runner-factory.ts`: `opts: any` -> `any`
  - `billing-service-async.ts`: `input: any` 方法签名
  - 重试/超时工具使用 `(...args: any[]) => Promise<any>`
  - 绕过类型安全
- **建议修复**: 
  1. 使用泛型替代 any
  2. 替换为具体的输入类型
  3. 添加 ESLint 规则禁止 any

#### 4. [源代码] [高严重] [38处 @ts-ignore 指令]
- **文件/路径**: 多个文件
- **问题描述**: 
  - 14个 stability 相关文件有 `@ts-ignore ExecutionRecord type mismatch`
  - 5个 harness-sdk 文件有 `Partial<HarnessRun> doesn't have all required properties`
  - 2个文件有 `exactOptionalPropertyTypes` 问题
- **建议修复**: 
  1. 修复 ExecutionRecord 类型不匹配
  2. 完成 HarnessRun 类型的必需属性
  3. 移除 @ts-ignore 使用

#### 5. [源代码] [高严重] [ExecutionRecord 类型不匹配]
- **文件/路径**: `src/platform/stability/stable-evidence-bundle-support.ts:783` 等14个文件
- **问题描述**: 
  - 插入 execution record 时 TypeScript 拒绝类型
  - `ExecutionRecord` 接口要求新字段但 insert 对象缺失
  - 或 store 的 insert 方法接受不同类型
- **建议修复**: 
  1. 对齐 ExecutionRecord 接口和 store 类型
  2. 添加缺失字段到 insert 对象
  3. 或调整 store 的 insert 方法类型签名

#### 6. [源代码] [中严重] [30+处 as unknown as 双类型转换]
- **文件/路径**: `division-loader.ts`, `domain-baseline-catalog.ts`, `harness-sdk`
- **问题描述**: 
  - 完全绕过类型安全
  - 通常是类型架构问题的信号
- **建议修复**: 
  1. 修复上游类型问题
  2. 使用更具体的类型断言
  3. 重构类型层次结构

#### 7. [构建] [中严重] [rimraf  extraneous - 未在 package.json 声明]
- **文件/路径**: `node_modules/rimraf`
- **问题描述**: 
  - rimraf@6.1.3 存在于 node_modules 但未在 package.json 声明
  - 可能导致依赖问题
- **建议修复**: 
  1. 添加到 dependencies 或 devDependencies
  2. 或移除并使用其他清理方式

#### 8. [测试] [高严重] [178处直接 process.env 变更无抽象]
- **文件/路径**: `tests/unit/domains/registry/plugin-spi-registry.test.ts:640-687`
- **问题描述**: 
  - 直接读取/写入 process.env
  - 在 beforeEach/afterEach 中手动保存/恢复
  - 无 helper 抽象
- **建议修复**: 
  1. 创建 env helper 函数
  2. 统一保存和恢复模式
  3. 添加测试隔离验证

#### 9. [测试] [高严重] [21,998处 unlinkSync 调用无集中清理]
- **文件/路径**: 多个测试文件
- **问题描述**: 
  - 每个测试重新实现清理逻辑
  - 无集中清理 utility
  - 可能导致 EBUSY (Windows) 或并发访问问题
- **建议修复**: 
  1. 创建集中的测试清理 utility
  2. 使用 afterEach 确保清理
  3. 考虑使用临时目录

#### 10. [测试] [中严重] [无统一 mocking 框架]
- **文件/路径**: `tests/` 多个目录
- **问题描述**: 
  - 10,579 处 vi.fn, jest.mock, sinon 等调用
  - 无统一的 mocking 框架可见
  - 模式不一致
- **建议修复**: 
  1. 建立统一的 mock factory
  2. 标准化 mock 模式
  3. 添加 mocking 最佳实践文档

#### 11. [测试] [中严重] [Singleton 状态重置未标准化]
- **文件/路径**: `tests/unit/platform/five-plane-execution/dispatcher.test.ts`
- **问题描述**: 
  - 测试通过特定函数重置 singletons
  - 每个测试重复自己的 reset 逻辑
  - 无统一的 resetAllSingletons()
- **建议修复**: 
  1. 创建统一的 singleton reset 机制
  2. 在 afterEach 中自动调用
  3. 文档化 singleton 重置要求

#### 12. [测试] [低严重] [无分支覆盖率要求]
- **文件/路径**: `.c8rc.json`
- **问题描述**: 
  - `"100": false` - 无覆盖率要求
  - 只测量源代码，不测量测试文件
- **建议修复**: 
  1. 添加分支覆盖率阈值
  2. 在 CI 中强制执行覆盖率 gate
  3. 关注关键代码路径

#### 13. [网络] [高严重] [ScopedExternalAccessSandbox performHttpRequest 无超时]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/scoped-external-access-sandbox.ts:289`
- **问题描述**: 
  - fetch 调用无 timeout 参数
  - 挂起的请求可能永久阻塞
- **建议修复**: 
  1. 添加 AbortController timeout
  2. 使用标准的超时模式
  3. 记录超时配置

#### 14. [网络] [高严重] [OIDC 服务 fetch 调用无超时]
- **文件/路径**: `src/org-governance/sso-scim/oidc/oidc-service.ts:565,272`
- **问题描述**: 
  - OIDC token 交换无 retry logic
  - 无 timeout/abort signal
  - 认证流程可能挂起
- **建议修复**: 
  1. 添加 fetch timeout
  2. 添加 retry logic
  3. 使用标准的超时配置

#### 15. [网络] [高严重] [plugin-runtime-child 替换 globalThis.fetch 为 no-op]
- **文件/路径**: `src/domains/registry/plugin-runtime-child.ts:112`
- **问题描述**: 
  ```typescript
  globalThis.fetch = (async () => {}) as unknown as typeof globalThis.fetch;
  ```
  - 插件上下文中完全替换 fetch 为空函数
  - 插件的所有出站 HTTP 请求都会失败
  - 看起来是有意为之但可能造成问题
- **建议修复**: 
  1. 确认这是有意行为
  2. 文档化 fetch 替换行为
  3. 确保不会意外影响其他代码

#### 16. [网络] [中严重] [无显式 http.Agent 配置]
- **文件/路径**: 全局
- **问题描述**: 
  - 无显式的 http.Agent/https.Agent 配置
  - 依赖 Node.js 默认连接池
  - 无 maxSockets 或 maxConnections 配置
  - 高吞吐场景可能出问题
- **建议修复**: 
  1. 添加 http Agent 配置
  2. 文档化连接池大小
  3. 监控连接使用情况

#### 17. [序列化] [中严重] [合同版本硬编码为字符串字面量]
- **文件/路径**: `src/platform/contracts/inter-plane-contract-gateway.ts:184`
- **问题描述**: 
  - `schemaVersion: "v4.3"` 硬编码
  - 难以追踪合同版本演变
- **建议修复**: 
  1. 提取到类型化常量或 enum
  2. 集中管理合同版本
  3. 添加版本兼容性检查

#### 18. [序列化] [低严重] [Timestamp = string 丢失精度]
- **文件/路径**: `src/platform/contracts/types/domain/primitives.ts:10`
- **问题描述**: 
  - `type Timestamp = string` 无类型精度
  - ISO 8601 字符串无时区信息
- **建议修复**: 
  1. 使用 Date 对象或 branded string 类型
  2. 明确时区处理约定
  3. 文档化时间戳语义

#### 19. [源代码] [中严重] [29处非空断言 !.]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - 使用 `!.` 断言非空
  - 可能导致运行时错误
- **建议修复**: 
  1. 使用可选链 `?.` 或空值合并 `??`
  2. 添加适当的 null 检查
  3. 减少非空断言使用

#### 20. [API] [中严重] [API 路由 schema 版本硬编码]
- **文件/路径**: `src/platform/five-plane-interface/api/federation-routing-service.ts`
- **问题描述**: 
  - Zod schema 验证存在
  - 但 schema 版本未与 contract 版本同步
- **建议修复**: 
  1. 从 contract 版本派生 schema 版本
  2. 添加版本一致性检查
  3. 文档化版本关系

### 总结

本次补充Review（第十五轮 - 构建测试与类型系统Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. 无 TypeScript 增量编译缓存（每次完整重建9.6秒）
2. 76个 npm scripts 大部分重复构建
3. 168处 any 类型使用（绕过类型安全）
4. 38处 @ts-ignore 指令
5. ExecutionRecord 类型不匹配（14个文件）
6. 178处直接 process.env 变更无抽象
7. ScopedExternalAccessSandbox performHttpRequest 无超时
8. OIDC 服务 fetch 调用无超时
9. plugin-runtime-child 替换 globalThis.fetch 为 no-op

**中优先级**:
1. 30+处 as unknown as 双类型转换
2. rimraf extraneous 未声明
3. 21,998处 unlinkSync 无集中清理
4. 无统一 mocking 框架
5. Singleton 状态重置未标准化
6. 无显式 http.Agent 配置
7. 合同版本硬编码为字符串
8. Timestamp = string 丢失精度
9. 非空断言使用

**低优先级**:
1. 无分支覆盖率要求
2. API 路由 schema 版本未同步

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 72 | 87 | 39 | 198 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **109** | **182** | **85** | **376** |

### 优先修复建议

**立即处理 (本周内)**:
1. 添加 TypeScript 增量编译 (`incremental: true`)
2. 重构 npm scripts 避免重复构建
3. 减少 any 类型使用到 <50
4. 移除所有 @ts-ignore 指令
5. 修复 ExecutionRecord 类型不匹配
6. 添加 fetch 超时到 OIDC 和 sandbox

**短期内处理 (本月内)**:
1. 创建集中的测试清理 utility
2. 建立统一的 mocking 框架
3. 标准化 singleton 重置机制
4. 添加 http.Agent 连接池配置
5. 提取合同版本到类型化常量

**长期规划**:
1. 实现完整的类型安全（无 any）
2. 建立测试基础设施标准
3. 实现构建缓存优化
4. 完善网络层超时和重试
5. 建立覆盖率门禁标准

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十四轮 - 日志事件与工作流Review）

### 发现的新问题

#### 1. [可观测性] [高严重] [StructuredLogEntry 无 requestId 字段]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - StructuredLogEntry 接口无 requestId 字段
  - 只有 correlationId，它回退到 traceId
  - 无法独立追踪单个 HTTP 请求
- **建议修复**: 
  1. 在 StructuredLogEntry 添加 requestId 字段
  2. 在 HTTP 中间件中生成和注入 requestId
  3. 确保 requestId 流经所有日志调用

#### 2. [可观测性] [高严重] [StructuredLogger data 字段无自动清洗]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - 如果开发者传递 `{ password: "..." }` 给 logger.info，密码会被记录
  - 无字段级脱敏机制
  - 依赖开发者手动排除敏感字段
- **建议修复**: 
  1. 添加 OBSERVE_OUTPUT_BLACKLIST 类似的黑名单
  2. 实现 redact/mask/sanitize 工具函数
  3. 在日志传输前自动清洗敏感字段

#### 3. [可观测性] [中严重] [日志中敏感数据无 PII 处理]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - 无 PII 重写工具
  - 开发者必须手动避免记录敏感数据
  - 存在意外记录敏感信息的风险
- **建议修复**: 
  1. 添加 PII 检测和重写工具
  2. 在文档中明确记录敏感字段列表
  3. 添加测试验证敏感数据不被记录

#### 4. [可观测性] [中严重] [59处 console.* 直接调用]
- **文件/路径**: 多个源文件
- **问题描述**: 
  - 应该使用 StructuredLogger
  - console 调用可能不被日志聚合系统收集
  - 难以追踪和关联
- **建议修复**: 
  1. 替换所有 console.* 为 StructuredLogger
  2. 添加 ESLint 规则禁止 console.*
  3. 保留少量允许的 CLI 输出

#### 5. [可观测性] [中严重] [部分 logger.warn 调用缺少结构化数据]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:122`
- **问题描述**: 
  ```typescript
  logger.warn("WebSocket connection rejected: missing subprotocol token")
  ```
  - 只有消息，无 data 对象
  - 难以关联和查询
- **建议修复**: 
  1. 添加 `{ data: { ... } }` 对象
  2. 确保所有日志包含上下文数据
  3. 添加日志审查工具

#### 6. [源代码] [高严重] [Harness while(true) 循环无硬性迭代上限]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:1442`
- **问题描述**: 
  ```typescript
  while (true) {
    // ...
    // 依赖 budget gate 和 guardrail vibration 退出
    // 如果两者都失败，循环永久继续
  }
  ```
  - 依赖 budget gate 和 guardrail vibration 退出
  - 无硬性迭代次数限制
  - 可能导致无限循环
- **建议修复**: 
  1. 添加硬性 maxIterations 上限
  2. 在达到上限时强制退出
  3. 记录详细的迭代统计用于调试

#### 7. [源代码] [高严重] [Oapeflir while(true) 循环可能无限重规划]
- **文件/路径**: `src/platform/five-plane-orchestration/oapeflir/oapeflir-loop-service.ts:355`
- **问题描述**: 
  - 如果 loopReplanDecision.shouldReplan 永不变为 false
  - 重规划永远无法减少计划错误
  - 可能导致无限循环
- **建议修复**: 
  1. 添加重规划次数上限
  2. 在达到上限时强制接受当前结果
  3. 添加重规划质量衰减检测

#### 8. [源代码] [中严重] [Recovery 流程允许终态转换到 paused]
- **文件/路径**: `src/platform/five-plane-execution/runtime-state-machine.ts:105-108`
- **问题描述**: 
  - completed, failed, cancelled, aborted 可转换到 paused 进行恢复
  - 创建恢复路径可能重新进入执行
  - 需要验证此行为的正确性
- **建议修复**: 
  1. 审查 recovery 转换的合法性
  2. 确保不会导致状态不一致
  3. 添加转换前置条件验证

#### 9. [国际化] [高严重] [API 错误消息硬编码英文]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/api-error.ts`
- **问题描述**: 
  ```typescript
  return new ApiError(404, "api.task_not_found", "Task not found.");
  ```
  - 错误消息硬编码英文
  - 无后端 i18n 机制
  - 非英文客户端收到不本地化的错误
- **建议修复**: 
  1. 使用错误码而非硬编码消息
  2. 客户端根据错误码本地化
  3. 或在后端添加 i18n 支持

#### 10. [国际化] [中严重] [UI 硬编码字符串未翻译]
- **文件/路径**: `ui/packages/features/conversation/src/web/index.tsx`
- **问题描述**: 
  - "Build Plan", "Execute", "Messages" 等按钮标签硬编码
  - 不使用 i18n 系统
- **建议修复**: 
  1. 使用 `translateFeatureCopy` 替换硬编码
  2. 确保所有用户可见文本通过 i18n
  3. 添加硬编码字符串检测

#### 11. [国际化] [中严重] [Arabic 目录不完整]
- **文件/路径**: `ui/packages/shared/i18n/src/catalogs/ar-SA.ts`
- **问题描述**: 
  - ar-SA 只有约18条消息
  - en-US 和 zh-CN 有约117条
  - 缺失的 key 返回 key 本身
- **建议修复**: 
  1. 补充 Arabic 翻译
  2. 确保所有 key 都有翻译
  3. 添加翻译完整性测试

#### 12. [国际化] [低严重] [translateFeatureCopy 无回退默认值]
- **文件/路径**: `ui/packages/shared/i18n/`
- **问题描述**: 
  - 如果 feature ID 不在 catalog 中
  - 返回 undefined title/summary
  - 无合理回退
- **建议修复**: 
  1. 添加回退到 key 或默认字符串
  2. 或使用 feature ID 作为显示名
  3. 确保 UI 不显示 undefined

#### 13. [运维] [高严重] [健康检查无 readiness/liveness 区分]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts`
- **问题描述**: 
  - /healthz, /v1/healthz, /health 返回相同报告
  - 无 readiness probe（可处理流量？）
  - 无 liveness probe（系统存活？）
  - canAcceptTraffic() 未通过 HTTP 暴露
- **建议修复**: 
  1. 添加 /ready 和 /live 端点
  2. Readiness 检查依赖项就绪
  3. Liveness 检查进程健康

#### 14. [运维] [高严重] [shutdown 期间健康检查存在竞态]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts`
- **问题描述**: 
  - getHealthReportAsync() 异步检查 dbWritable
  - shutdown 期间数据库连接可能关闭
  - 健康检查仍在传入
- **建议修复**: 
  1. 在 shutdown 时返回 unhealthy
  2. 添加 shutdown 状态标志
  3. 停止接受新的健康检查请求

#### 15. [运维] [高严重] [LeaderElectionService 未与 GracefulShutdown 集成]
- **文件/路径**: `src/platform/five-plane-execution/ha/leader-election-service.ts`
- **问题描述**: 
  - LeaderElectionService.stop() 处理优雅退位
  - 但未在全局 graceful shutdown 中注册
  - 需显式注册为 shutdown handler
- **建议修复**: 
  1. 将 LeaderElectionService 添加到 shutdown handlers
  2. 确保领导者在 shutdown 时让位
  3. 验证关闭顺序正确

#### 16. [运维] [中严重] [addHandler() 无显式排序约定]
- **文件/路径**: `src/platform/five-plane-execution/startup/graceful-shutdown.ts`
- **问题描述**: 
  - 虽然 handlers 逆序执行
  - 但无显式接口声明 handler 依赖关系
  - 关键 handlers（如关闭数据库连接）应最后运行
- **建议修复**: 
  1. 添加优先级或阶段参数
  2. 文档化关键 handler 顺序要求
  3. 添加顺序验证测试

#### 17. [运维] [中严重] [unref'd timers 可能不触发]
- **文件/路径**: `src/platform/five-plane-execution/startup/process-error-handlers.ts:106`
- **问题描述**: 
  - `setTimeout(...).unref()` 不保持事件循环活跃
  - 如果主线程空闲，强制退出可能不触发
- **建议修复**: 
  1. 移除 .unref() 除非确实需要
  2. 或确保有其他方式保持事件循环
  3. 添加超时触发验证测试

#### 18. [运维] [中严重] [StartupConsistencyChecker 阻塞流量但不暴露状态]
- **文件/路径**: `src/platform/five-plane-startup-plan.ts`
- **问题描述**: 
  - canAcceptTraffic() 在 P0 问题存在时返回 false
  - 但 _trafficBlocked 是内部状态
  - 负载均衡器访问 /healthz 仍会得到 "ok"
- **建议修复**: 
  1. 在健康报告中暴露 trafficBlocked 状态
  2. 或添加单独的健康端点指示就绪状态
  3. 确保负载均衡器在阻塞时不路由流量

#### 19. [事件系统] [中严重] [TypedEventBus vs EventEmitter 混用]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - 项目同时使用 TypedEventBus 和原生 EventEmitter
  - TypedEventBus 应该是标准
  - 混用可能导致类型安全问题
- **建议修复**: 
  1. 创建迁移计划统一使用 TypedEventBus
  2. 添加 ESLint 规则禁止原生 EventEmitter
  3. 在 CI 中检测混用情况

#### 20. [源代码] [低严重] [无事件排序保证]
- **文件/路径**: `src/platform/five-plane-state-evidence/events/`
- **问题描述**: 
  - 无明确的事件排序保证文档
  - 事件处理可能乱序
  - 依赖方需要处理乱序
- **建议修复**: 
  1. 文档化事件排序语义
  2. 如果需要排序，实现序列号机制
  3. 在事件处理中考虑乱序情况

### 总结

本次补充Review（第十四轮 - 日志事件与工作流Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. StructuredLogEntry 无 requestId 字段
2. StructuredLogger data 字段无自动清洗（敏感数据风险）
3. Harness while(true) 循环无硬性迭代上限
4. Oapeflir while(true) 循环可能无限重规划
5. API 错误消息硬编码英文（无 i18n）
6. 健康检查无 readiness/liveness 区分
7. shutdown 期间健康检查存在竞态
8. LeaderElectionService 未与 GracefulShutdown 集成

**中优先级**:
1. 日志中敏感数据无 PII 处理
2. 59处 console.* 直接调用
3. 部分 logger.warn 调用缺少结构化数据
4. Recovery 流程允许终态转换到 paused
5. UI 硬编码字符串未翻译
6. Arabic 目录不完整
7. addHandler() 无显式排序约定
8. unref'd timers 可能不触发
9. StartupConsistencyChecker 阻塞流量但不暴露状态
10. TypedEventBus vs EventEmitter 混用

**低优先级**:
1. translateFeatureCopy 无回退默认值
2. 无事件排序保证

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 68 | 80 | 37 | 185 |
| 测试 | 8 | 14 | 5 | 27 |
| 配置 | 10 | 38 | 24 | 72 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **103** | **171** | **82** | **356** |

### 优先修复建议

**立即处理 (本周内)**:
1. 在 StructuredLogEntry 添加 requestId 字段
2. 实现日志敏感字段自动清洗
3. 为工作流循环添加硬性迭代上限
4. 添加 readiness/liveness 健康检查端点
5. 将 LeaderElectionService 集成到 GracefulShutdown
6. 实现 API 错误消息 i18n 支持

**短期内处理 (本月内)**:
1. 替换所有 console.* 为 StructuredLogger
2. 补充 Arabic i18n 目录
3. 修复 UI 硬编码字符串
4. 统一 TypedEventBus 替代 EventEmitter
5. 解决 shutdown 竞态条件
6. 添加事件排序保证文档

**长期规划**:
1. 建立完整的 PII 处理框架
2. 实现日志完整性验证
3. 建立工作流安全循环检测
4. 完善健康检查和就绪探测
5. 建立国际化完整测试

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十五轮 - 构建测试与类型系统Review）

### 发现的新问题

#### 1. [构建] [高严重] [无 TypeScript 增量编译缓存]
- **文件/路径**: `tsconfig.json`
- **问题描述**: 
  - 无 `incremental: true` 配置
  - 无 `tsbuildinfo` 文件
  - 每次构建是完整重建（9.6秒）
  - TypeScript 无法使用增量编译
- **建议修复**: 
  1. 在 tsconfig.json 添加 `"incremental": true`
  2. 添加 `"tsbuildinfo": ".tsbuildinfo"` 到 .gitignore
  3. 实现项目引用以支持并行构建

#### 2. [构建] [高严重] [76个 npm scripts 大部分重复构建]
- **文件/路径**: `package.json`
- **问题描述**: 
  - 40+ CLI scripts 都运行 `npm run build && node ...`
  - 每个脚本前都执行完整重建
  - 无法直接运行预构建的 CLI
  - 20+ stable:* 脚本遵循相同模式
- **建议修复**: 
  1. 创建 `build:cli` 脚本构建一次
  2. 让 CLI 脚本使用预构建的 dist 文件
  3. 添加 npm run dev 或类似的无需重建的执行方式

#### 3. [源代码] [高严重] [168处 any 类型使用]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - `stable-runner-factory.ts`: `opts: any` -> `any`
  - `billing-service-async.ts`: `input: any` 方法签名
  - 重试/超时工具使用 `(...args: any[]) => Promise<any>`
  - 绕过类型安全
- **建议修复**: 
  1. 使用泛型替代 any
  2. 替换为具体的输入类型
  3. 添加 ESLint 规则禁止 any

#### 4. [源代码] [高严重] [38处 @ts-ignore 指令]
- **文件/路径**: 多个文件
- **问题描述**: 
  - 14个 stability 相关文件有 `@ts-ignore ExecutionRecord type mismatch`
  - 5个 harness-sdk 文件有 `Partial<HarnessRun> doesn't have all required properties`
  - 2个文件有 `exactOptionalPropertyTypes` 问题
- **建议修复**: 
  1. 修复 ExecutionRecord 类型不匹配
  2. 完成 HarnessRun 类型的必需属性
  3. 移除 @ts-ignore 使用

#### 5. [源代码] [高严重] [ExecutionRecord 类型不匹配]
- **文件/路径**: `src/platform/stability/stable-evidence-bundle-support.ts:783` 等14个文件
- **问题描述**: 
  - 插入 execution record 时 TypeScript 拒绝类型
  - `ExecutionRecord` 接口要求新字段但 insert 对象缺失
  - 或 store 的 insert 方法接受不同类型
- **建议修复**: 
  1. 对齐 ExecutionRecord 接口和 store 类型
  2. 添加缺失字段到 insert 对象
  3. 或调整 store 的 insert 方法类型签名

#### 6. [源代码] [中严重] [30+处 as unknown as 双类型转换]
- **文件/路径**: `division-loader.ts`, `domain-baseline-catalog.ts`, `harness-sdk`
- **问题描述**: 
  - 完全绕过类型安全
  - 通常是类型架构问题的信号
- **建议修复**: 
  1. 修复上游类型问题
  2. 使用更具体的类型断言
  3. 重构类型层次结构

#### 7. [构建] [中严重] [rimraf  extraneous - 未在 package.json 声明]
- **文件/路径**: `node_modules/rimraf`
- **问题描述**: 
  - rimraf@6.1.3 存在于 node_modules 但未在 package.json 声明
  - 可能导致依赖问题
- **建议修复**: 
  1. 添加到 dependencies 或 devDependencies
  2. 或移除并使用其他清理方式

#### 8. [测试] [高严重] [178处直接 process.env 变更无抽象]
- **文件/路径**: `tests/unit/domains/registry/plugin-spi-registry.test.ts:640-687`
- **问题描述**: 
  - 直接读取/写入 process.env
  - 在 beforeEach/afterEach 中手动保存/恢复
  - 无 helper 抽象
- **建议修复**: 
  1. 创建 env helper 函数
  2. 统一保存和恢复模式
  3. 添加测试隔离验证

#### 9. [测试] [高严重] [21,998处 unlinkSync 调用无集中清理]
- **文件/路径**: 多个测试文件
- **问题描述**: 
  - 每个测试重新实现清理逻辑
  - 无集中清理 utility
  - 可能导致 EBUSY (Windows) 或并发访问问题
- **建议修复**: 
  1. 创建集中的测试清理 utility
  2. 使用 afterEach 确保清理
  3. 考虑使用临时目录

#### 10. [测试] [中严重] [无统一 mocking 框架]
- **文件/路径**: `tests/` 多个目录
- **问题描述**: 
  - 10,579 处 vi.fn, jest.mock, sinon 等调用
  - 无统一的 mocking 框架可见
  - 模式不一致
- **建议修复**: 
  1. 建立统一的 mock factory
  2. 标准化 mock 模式
  3. 添加 mocking 最佳实践文档

#### 11. [测试] [中严重] [Singleton 状态重置未标准化]
- **文件/路径**: `tests/unit/platform/five-plane-execution/dispatcher.test.ts`
- **问题描述**: 
  - 测试通过特定函数重置 singletons
  - 每个测试重复自己的 reset 逻辑
  - 无统一的 resetAllSingletons()
- **建议修复**: 
  1. 创建统一的 singleton reset 机制
  2. 在 afterEach 中自动调用
  3. 文档化 singleton 重置要求

#### 12. [测试] [低严重] [无分支覆盖率要求]
- **文件/路径**: `.c8rc.json`
- **问题描述**: 
  - `"100": false` - 无覆盖率要求
  - 只测量源代码，不测量测试文件
- **建议修复**: 
  1. 添加分支覆盖率阈值
  2. 在 CI 中强制执行覆盖率 gate
  3. 关注关键代码路径

#### 13. [网络] [高严重] [ScopedExternalAccessSandbox performHttpRequest 无超时]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/scoped-external-access-sandbox.ts:289`
- **问题描述**: 
  - fetch 调用无 timeout 参数
  - 挂起的请求可能永久阻塞
- **建议修复**: 
  1. 添加 AbortController timeout
  2. 使用标准的超时模式
  3. 记录超时配置

#### 14. [网络] [高严重] [OIDC 服务 fetch 调用无超时]
- **文件/路径**: `src/org-governance/sso-scim/oidc/oidc-service.ts:565,272`
- **问题描述**: 
  - OIDC token 交换无 retry logic
  - 无 timeout/abort signal
  - 认证流程可能挂起
- **建议修复**: 
  1. 添加 fetch timeout
  2. 添加 retry logic
  3. 使用标准的超时配置

#### 15. [网络] [高严重] [plugin-runtime-child 替换 globalThis.fetch 为 no-op]
- **文件/路径**: `src/domains/registry/plugin-runtime-child.ts:112`
- **问题描述**: 
  ```typescript
  globalThis.fetch = (async () => {}) as unknown as typeof globalThis.fetch;
  ```
  - 插件上下文中完全替换 fetch 为空函数
  - 插件的所有出站 HTTP 请求都会失败
  - 看起来是有意为之但可能造成问题
- **建议修复**: 
  1. 确认这是有意行为
  2. 文档化 fetch 替换行为
  3. 确保不会意外影响其他代码

#### 16. [网络] [中严重] [无显式 http.Agent 配置]
- **文件/路径**: 全局
- **问题描述**: 
  - 无显式的 http.Agent/https.Agent 配置
  - 依赖 Node.js 默认连接池
  - 无 maxSockets 或 maxConnections 配置
  - 高吞吐场景可能出问题
- **建议修复**: 
  1. 添加 http Agent 配置
  2. 文档化连接池大小
  3. 监控连接使用情况

#### 17. [序列化] [中严重] [合同版本硬编码为字符串字面量]
- **文件/路径**: `src/platform/contracts/inter-plane-contract-gateway.ts:184`
- **问题描述**: 
  - `schemaVersion: "v4.3"` 硬编码
  - 难以追踪合同版本演变
- **建议修复**: 
  1. 提取到类型化常量或 enum
  2. 集中管理合同版本
  3. 添加版本兼容性检查

#### 18. [序列化] [低严重] [Timestamp = string 丢失精度]
- **文件/路径**: `src/platform/contracts/types/domain/primitives.ts:10`
- **问题描述**: 
  - `type Timestamp = string` 无类型精度
  - ISO 8601 字符串无时区信息
- **建议修复**: 
  1. 使用 Date 对象或 branded string 类型
  2. 明确时区处理约定
  3. 文档化时间戳语义

#### 19. [源代码] [中严重] [29处非空断言 !.]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - 使用 `!.` 断言非空
  - 可能导致运行时错误
- **建议修复**: 
  1. 使用可选链 `?.` 或空值合并 `??`
  2. 添加适当的 null 检查
  3. 减少非空断言使用

#### 20. [API] [中严重] [API 路由 schema 版本硬编码]
- **文件/路径**: `src/platform/five-plane-interface/api/federation-routing-service.ts`
- **问题描述**: 
  - Zod schema 验证存在
  - 但 schema 版本未与 contract 版本同步
- **建议修复**: 
  1. 从 contract 版本派生 schema 版本
  2. 添加版本一致性检查
  3. 文档化版本关系

### 总结

本次补充Review（第十五轮 - 构建测试与类型系统Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. 无 TypeScript 增量编译缓存（每次完整重建9.6秒）
2. 76个 npm scripts 大部分重复构建
3. 168处 any 类型使用（绕过类型安全）
4. 38处 @ts-ignore 指令
5. ExecutionRecord 类型不匹配（14个文件）
6. 178处直接 process.env 变更无抽象
7. ScopedExternalAccessSandbox performHttpRequest 无超时
8. OIDC 服务 fetch 调用无超时
9. plugin-runtime-child 替换 globalThis.fetch 为 no-op

**中优先级**:
1. 30+处 as unknown as 双类型转换
2. rimraf extraneous 未声明
3. 21,998处 unlinkSync 无集中清理
4. 无统一 mocking 框架
5. Singleton 状态重置未标准化
6. 无显式 http.Agent 配置
7. 合同版本硬编码为字符串
8. Timestamp = string 丢失精度
9. 非空断言使用

**低优先级**:
1. 无分支覆盖率要求
2. API 路由 schema 版本未同步

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 72 | 87 | 39 | 198 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **109** | **182** | **85** | **376** |

### 优先修复建议

**立即处理 (本周内)**:
1. 添加 TypeScript 增量编译 (`incremental: true`)
2. 重构 npm scripts 避免重复构建
3. 减少 any 类型使用到 <50
4. 移除所有 @ts-ignore 指令
5. 修复 ExecutionRecord 类型不匹配
6. 添加 fetch 超时到 OIDC 和 sandbox

**短期内处理 (本月内)**:
1. 创建集中的测试清理 utility
2. 建立统一的 mocking 框架
3. 标准化 singleton 重置机制
4. 添加 http.Agent 连接池配置
5. 提取合同版本到类型化常量

**长期规划**:
1. 实现完整的类型安全（无 any）
2. 建立测试基础设施标准
3. 实现构建缓存优化
4. 完善网络层超时和重试
5. 建立覆盖率门禁标准

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十二轮 - 错误处理与多租户安全Review）

### 发现的新问题

#### 1. [安全] [严重] [listQuotas() 返回所有租户配额 - 信息泄露]
- **文件/路径**: `src/platform/five-plane-control-plane/tenant/tenant-execution-isolation-service.ts:222-234`
- **问题描述**: 
  ```typescript
  listQuotas(tenantId?: string): TenantQuota[] {
    if (tenantId) {
      return this.db.connection.prepare(`SELECT * FROM tenant_quotas WHERE tenant_id = ?`).all(tenantId);
    }
    // WITHOUT tenantId: 返回所有租户的配额！
    return this.db.connection.prepare(`SELECT * FROM tenant_quotas`).all();
  }
  ```
  - 当 tenantId 为空时返回所有租户的配额
  - 未经授权的信息泄露
- **建议修复**: 
  1. 要求调用者必须提供 tenantId
  2. 添加权限验证确保只能查看自己的配额
  3. 审计日志记录所有配额访问

#### 2. [安全] [高严重] [assertTaskTenantAccess() 返回404而非403 - 资源存在泄露]
- **文件/路径**: `src/platform/five-plane-interface/api/utils.ts:146-148`
- **问题描述**: 
  ```typescript
  if (resourceTenantId !== principal.tenantId) {
    throw new ApiError(404, notFoundCode, notFoundMessage);  // 应该是403
  }
  ```
  - 跨租户访问返回404而非403
  - 泄露了资源存在但属于其他租户的信息
  - 攻击者可检测有效task ID
- **建议修复**: 
  1. 改为返回403 Forbidden
  2. 不要泄露资源是否存在

#### 3. [安全] [高严重] [crossTenantRequest 标志由调用者控制]
- **文件/路径**: `src/platform/five-plane-interface/org-routing/index.ts:163`
- **问题描述**: 
  - `routeOrgBudget()` 接受 `crossTenantRequest?: boolean` 参数
  - 该标志由调用者设置，非系统派生
  - 攻击者可能操纵此标志绕过跨租户限制
- **建议修复**: 
  1. 标志应由系统状态派生，不接受调用者输入
  2. 添加审计日志追踪跨租户请求
  3. 实现零信任跨租户访问模型

#### 4. [源代码] [高严重] [single-task-happy-path 无重试机制]
- **文件/路径**: `src/platform/five-plane-execution/execution-engine/single-task-happy-path.ts:336-337`
- **问题描述**: 
  - `maxRetries: 0` 和 `retryBackoff: "none"`
  - 瞬态失败无自动重试
  - LLM调用失败时无fallback到备用provider
- **建议修复**: 
  1. 启用非关键执行的重试
  2. 实现LLM provider fallback链（Anthropic → OpenAI → MiniMax）
  3. 使用domain baseline catalog的primary/fallback模型偏好

#### 5. [源代码] [高严重] [LLM调用无熔断器保护]
- **文件/路径**: `src/platform/five-plane-execution/execution-engine/model-call-provider.ts`
- **问题描述**: 
  - 熔断器只存在于 channel-gateway 和 call-governance
  - 直接LLM provider调用无熔断器
  - provider降级时无法快速失败
- **建议修复**: 
  1. 在model-call-provider.ts添加熔断器
  2. 当provider失败率超阈值时快速失败
  3. 自动切换到备用provider

#### 6. [安全] [高严重] [分布式限流器 bypass - 实例间不共享]
- **文件/路径**: `src/platform/five-plane-interface/ingress/distributed-rate-limiter.ts:33`
- **问题描述**: 
  - Redis未配置时使用本地Map
  - 每个实例维护自己的localEntries
  - 攻击者可通过切换实例绕过限流
- **建议修复**: 
  1. 要求Redis配置用于生产
  2. 或使用Redis作为所有实例的共享后端
  3. 检测并警告未配置Redis的实例

#### 7. [配置] [中严重] [Soft quota (log_only) 实际不限制]
- **文件/路径**: `src/platform/five-plane-control-plane/tenant/tenant-execution-isolation-service.ts:448`
- **问题描述**: 
  ```typescript
  allowed: quota.enforcementAction === "log_only",  // log_only时返回true
  ```
  - soft quota只记录日志，不实际阻止
  - 配额形同虚设
- **建议修复**: 
  1. 明确区分"监控模式"和"强制模式"
  2. 对于硬配额必须阻止
  3. 文档化quota类型的实际行为

#### 8. [配置] [中严重] [HTTP层无限流头 - 客户端无法获知限制]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/response-hardening.ts`
- **问题描述**: 
  - 未返回 X-RateLimit-* 头
  - 只在429响应时返回retry-after-ms
  - 客户端无法主动管理请求速率
- **建议修复**: 
  1. 在所有响应添加 X-RateLimit-Limit, X-RateLimit-Remaining, X-RateLimit-Reset
  2. 使用标准头名称
  3. 遵循RFC 6585

#### 9. [配置] [中严重] [HTTP层无限流 - 可选且仅按IP]
- **文件/路径**: `src/platform/five-plane-interface/api/http-api-server.ts:334-351`
- **问题描述**: 
  - 限流仅在 `this.rateLimiter != null` 时生效
  - 限流key为 `${clientIp}:${endpoint}` 仅按IP
  - 无per-tenant/per-principal enforcement
- **建议修复**: 
  1. 默认启用限流
  2. 添加per-tenant限流key
  3. 确保所有环境默认配置限流

#### 10. [安全] [中严重] [processRuleMode 可能未强制执行]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/plugin-runtime-host.ts`
- **问题描述**: 
  - `SandboxPolicy.processRuleMode` 设置为 "allow" 或 "deny"
  - 但未找到实际阻止基于此策略生成进程的实际执行代码
  - `--allow-child-process` 标志仅在 sandboxed_process 隔离模式应用
- **建议修复**: 
  1. 验证processRuleMode实际被强制执行
  2. 确保所有隔离模式正确处理
  3. 添加测试验证进程创建被阻止

#### 11. [安全] [中严重] [容器模板替换未验证]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/plugin-runtime-host.ts:751`
- **问题描述**: 
  ```typescript
  rendered = rendered.split(placeholder).join(value);
  ```
  - 简单字符串替换
  - 虽然validateContainerLaunchPluginId()阻止\0和引号
  - 但模板替换本身未验证
- **建议修复**: 
  1. 添加输入验证防止注入
  2. 使用更安全的模板引擎
  3. 验证所有占位符被替换

#### 12. [安全] [中严重] [Adapter执行绕过沙箱]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/adapter-executor.ts`
- **问题描述**: 
  - Adapter执行REST/grpc/MQ调用不经过ScopedExternalAccessSandbox
  - 有自己的allowedDomains配置但非集中管理
  - 可能发起未限制的出站请求
- **建议修复**: 
  1. Adapter执行经过集中沙箱
  2. 统一external access策略
  3. 添加adapter出站请求审计

#### 13. [安全] [中严重] [null principal.tenantId 时静默放行]
- **文件/路径**: `src/platform/five-plane-interface/api/utils.ts:143-145`
- **问题描述**: 
  ```typescript
  if (principal.tenantId == null) {
    return;  // 静默放行
  }
  ```
  - 如果API网关允许无principal认证的请求通过
  - 可能授予跨租户访问权限
- **建议修复**: 
  1. 要求所有API请求有有效principal
  2. null tenantId应拒绝而非静默放行
  3. 添加审计日志

#### 14. [源代码] [中严重] [无LLM provider fallback链]
- **文件/路径**: `src/platform/five-plane-execution/execution-engine/model-call-provider.ts`
- **问题描述**: 
  - 主provider失败时无自动切换
  - domain-baseline-catalog.ts定义了primary/fallback模型偏好但未使用
  - 第一个provider失败即导致执行失败
- **建议修复**: 
  1. 实现provider fallback链
  2. 使用domain baseline的primary/fallback配置
  3. 按优先级尝试直到成功或全部失败

#### 15. [源代码] [中严重] [超时值硬编码]
- **文件/路径**: 多个文件
- **问题描述**: 
  - Effect buffer有固定超时reject
  - Channel gateway的requestTimeoutMs虽可配置但有max 30s限制
  - 无全局请求超时中间件
- **建议修复**: 
  1. 集中超时配置
  2. per-environment可配置
  3. 添加全局超时中间件

#### 16. [数据库] [中严重] [无down migration - 回滚不支持]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/sqlite/migration-runner.ts:45-47`
- **问题描述**: 
  - `rollbackSupported = false`
  - 每个migration只有 `downSql` 占位符
  - schema migration无法回滚
- **建议修复**: 
  1. 文档化回滚限制
  2. 在变更前创建完整备份
  3. 使用blue-green部署减少回滚需求

#### 17. [数据库] [中严重] [Migration 44特殊处理 - 表创建重复]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/sqlite/sqlite-migration-runtime-part3.ts`
- **问题描述**: 
  - Migration 43创建harness_runs表
  - Migration 44再次创建同名表（不同schema）
  - 使用applyCompatibleColumnMigrationIfKnown特殊处理
- **建议修复**: 
  1. 消除重复表创建
  2. 使用ALTER TABLE而非CREATE TABLE
  3. 简化migration逻辑

#### 18. [配置] [低严重] [Provider限流头未转发给客户端]
- **文件/路径**: `src/platform/model-gateway/provider-registry/base-chat-provider.ts:153`
- **问题描述**: 
  - 系统读取provider的ratelimitResetHeaderNames
  - 但不转发给客户端
  - 客户端无法知道provider限流状态
- **建议修复**: 
  1. 转发Provider的限流头
  2. 或添加应用层限流信息
  3. 帮助客户端优化请求

#### 19. [安全] [低严重] [Browser evaluate 接受任意脚本]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/browser-executor.ts:326`
- **问题描述**: 
  - `evaluate` action接受script参数
  - 在浏览器上下文运行（模拟）
  - 如浏览器上下文未正确沙箱化可能是向量
- **建议修复**: 
  1. 验证浏览器上下文正确沙箱化
  2. 消毒或限制script内容
  3. 记录所有evaluate调用

#### 20. [源代码] [低严重] [无全局错误边界包装器]
- **文件/路径**: `src/platform/five-plane-execution/execution-engine/`
- **问题描述**: 
  - 执行引擎缺少unhandled promise rejection的全局处理
  - single-task-happy-path的LLM fallback失败时错误向上传播
- **建议修复**: 
  1. 在执行引擎添加错误边界包装
  2. 统一错误处理模式
  3. 确保所有错误被捕获和记录

### 总结

本次补充Review（第十二轮 - 错误处理与多租户安全Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. listQuotas()返回所有租户配额（信息泄露）
2. assertTaskTenantAccess()返回404泄露资源存在
3. crossTenantRequest标志可被操纵
4. single-task-happy-path无重试（maxRetries=0）
5. LLM调用无熔断器保护
6. 分布式限流器可被bypass（实例间不共享）

**中优先级**:
1. Soft quota实际不阻止
2. 无X-RateLimit-*响应头
3. HTTP层限流可选且仅按IP
4. processRuleMode可能未强制
5. 容器模板替换未验证
6. Adapter执行绕过沙箱
7. null principal静默放行
8. 无LLM provider fallback链
9. 超时值硬编码
10. 无down migration
11. Migration 44特殊处理

**低优先级**:
1. Provider限流头未转发
2. Browser evaluate接受任意脚本
3. 无全局错误边界

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 60 | 68 | 31 | 159 |
| 测试 | 8 | 14 | 5 | 27 |
| 配置 | 8 | 35 | 21 | 64 |
| 安全 | 12 | 13 | 3 | 28 |
| 文档 | 2 | 7 | 3 | 12 |
| UI | 0 | 3 | 3 | 6 |
| 部署 | 1 | 10 | 6 | 17 |
| **合计** | **91** | **150** | **72** | **313** |

### 优先修复建议

**立即处理 (本周内)**:
1. 修复listQuotas()信息泄露漏洞
2. 修正assertTaskTenantAccess()返回403
3. 移除crossTenantRequest调用者控制
4. 启用执行引擎重试机制
5. 添加LLM provider熔断器

**短期内处理 (本月内)**:
1. 修复分布式限流器bypass问题
2. 添加X-RateLimit-*响应头
3. 启用默认HTTP限流（per-tenant）
4. 实现LLM provider fallback链
5. 验证并强制processRuleMode

**长期规划**:
1. 建立完整的限流和配额体系
2. 实现插件系统安全审计
3. 建立多租户安全测试
4. 完善错误处理和恢复机制
5. 统一超时和熔断配置

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十五轮 - 构建测试与类型系统Review）

### 发现的新问题

#### 1. [构建] [高严重] [无 TypeScript 增量编译缓存]
- **文件/路径**: `tsconfig.json`
- **问题描述**: 
  - 无 `incremental: true` 配置
  - 无 `tsbuildinfo` 文件
  - 每次构建是完整重建（9.6秒）
  - TypeScript 无法使用增量编译
- **建议修复**: 
  1. 在 tsconfig.json 添加 `"incremental": true`
  2. 添加 `"tsbuildinfo": ".tsbuildinfo"` 到 .gitignore
  3. 实现项目引用以支持并行构建

#### 2. [构建] [高严重] [76个 npm scripts 大部分重复构建]
- **文件/路径**: `package.json`
- **问题描述**: 
  - 40+ CLI scripts 都运行 `npm run build && node ...`
  - 每个脚本前都执行完整重建
  - 无法直接运行预构建的 CLI
  - 20+ stable:* 脚本遵循相同模式
- **建议修复**: 
  1. 创建 `build:cli` 脚本构建一次
  2. 让 CLI 脚本使用预构建的 dist 文件
  3. 添加 npm run dev 或类似的无需重建的执行方式

#### 3. [源代码] [高严重] [168处 any 类型使用]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - `stable-runner-factory.ts`: `opts: any` -> `any`
  - `billing-service-async.ts`: `input: any` 方法签名
  - 重试/超时工具使用 `(...args: any[]) => Promise<any>`
  - 绕过类型安全
- **建议修复**: 
  1. 使用泛型替代 any
  2. 替换为具体的输入类型
  3. 添加 ESLint 规则禁止 any

#### 4. [源代码] [高严重] [38处 @ts-ignore 指令]
- **文件/路径**: 多个文件
- **问题描述**: 
  - 14个 stability 相关文件有 `@ts-ignore ExecutionRecord type mismatch`
  - 5个 harness-sdk 文件有 `Partial<HarnessRun> doesn't have all required properties`
  - 2个文件有 `exactOptionalPropertyTypes` 问题
- **建议修复**: 
  1. 修复 ExecutionRecord 类型不匹配
  2. 完成 HarnessRun 类型的必需属性
  3. 移除 @ts-ignore 使用

#### 5. [源代码] [高严重] [ExecutionRecord 类型不匹配]
- **文件/路径**: `src/platform/stability/stable-evidence-bundle-support.ts:783` 等14个文件
- **问题描述**: 
  - 插入 execution record 时 TypeScript 拒绝类型
  - `ExecutionRecord` 接口要求新字段但 insert 对象缺失
  - 或 store 的 insert 方法接受不同类型
- **建议修复**: 
  1. 对齐 ExecutionRecord 接口和 store 类型
  2. 添加缺失字段到 insert 对象
  3. 或调整 store 的 insert 方法类型签名

#### 6. [源代码] [中严重] [30+处 as unknown as 双类型转换]
- **文件/路径**: `division-loader.ts`, `domain-baseline-catalog.ts`, `harness-sdk`
- **问题描述**: 
  - 完全绕过类型安全
  - 通常是类型架构问题的信号
- **建议修复**: 
  1. 修复上游类型问题
  2. 使用更具体的类型断言
  3. 重构类型层次结构

#### 7. [构建] [中严重] [rimraf  extraneous - 未在 package.json 声明]
- **文件/路径**: `node_modules/rimraf`
- **问题描述**: 
  - rimraf@6.1.3 存在于 node_modules 但未在 package.json 声明
  - 可能导致依赖问题
- **建议修复**: 
  1. 添加到 dependencies 或 devDependencies
  2. 或移除并使用其他清理方式

#### 8. [测试] [高严重] [178处直接 process.env 变更无抽象]
- **文件/路径**: `tests/unit/domains/registry/plugin-spi-registry.test.ts:640-687`
- **问题描述**: 
  - 直接读取/写入 process.env
  - 在 beforeEach/afterEach 中手动保存/恢复
  - 无 helper 抽象
- **建议修复**: 
  1. 创建 env helper 函数
  2. 统一保存和恢复模式
  3. 添加测试隔离验证

#### 9. [测试] [高严重] [21,998处 unlinkSync 调用无集中清理]
- **文件/路径**: 多个测试文件
- **问题描述**: 
  - 每个测试重新实现清理逻辑
  - 无集中清理 utility
  - 可能导致 EBUSY (Windows) 或并发访问问题
- **建议修复**: 
  1. 创建集中的测试清理 utility
  2. 使用 afterEach 确保清理
  3. 考虑使用临时目录

#### 10. [测试] [中严重] [无统一 mocking 框架]
- **文件/路径**: `tests/` 多个目录
- **问题描述**: 
  - 10,579 处 vi.fn, jest.mock, sinon 等调用
  - 无统一的 mocking 框架可见
  - 模式不一致
- **建议修复**: 
  1. 建立统一的 mock factory
  2. 标准化 mock 模式
  3. 添加 mocking 最佳实践文档

#### 11. [测试] [中严重] [Singleton 状态重置未标准化]
- **文件/路径**: `tests/unit/platform/five-plane-execution/dispatcher.test.ts`
- **问题描述**: 
  - 测试通过特定函数重置 singletons
  - 每个测试重复自己的 reset 逻辑
  - 无统一的 resetAllSingletons()
- **建议修复**: 
  1. 创建统一的 singleton reset 机制
  2. 在 afterEach 中自动调用
  3. 文档化 singleton 重置要求

#### 12. [测试] [低严重] [无分支覆盖率要求]
- **文件/路径**: `.c8rc.json`
- **问题描述**: 
  - `"100": false` - 无覆盖率要求
  - 只测量源代码，不测量测试文件
- **建议修复**: 
  1. 添加分支覆盖率阈值
  2. 在 CI 中强制执行覆盖率 gate
  3. 关注关键代码路径

#### 13. [网络] [高严重] [ScopedExternalAccessSandbox performHttpRequest 无超时]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/scoped-external-access-sandbox.ts:289`
- **问题描述**: 
  - fetch 调用无 timeout 参数
  - 挂起的请求可能永久阻塞
- **建议修复**: 
  1. 添加 AbortController timeout
  2. 使用标准的超时模式
  3. 记录超时配置

#### 14. [网络] [高严重] [OIDC 服务 fetch 调用无超时]
- **文件/路径**: `src/org-governance/sso-scim/oidc/oidc-service.ts:565,272`
- **问题描述**: 
  - OIDC token 交换无 retry logic
  - 无 timeout/abort signal
  - 认证流程可能挂起
- **建议修复**: 
  1. 添加 fetch timeout
  2. 添加 retry logic
  3. 使用标准的超时配置

#### 15. [网络] [高严重] [plugin-runtime-child 替换 globalThis.fetch 为 no-op]
- **文件/路径**: `src/domains/registry/plugin-runtime-child.ts:112`
- **问题描述**: 
  ```typescript
  globalThis.fetch = (async () => {}) as unknown as typeof globalThis.fetch;
  ```
  - 插件上下文中完全替换 fetch 为空函数
  - 插件的所有出站 HTTP 请求都会失败
  - 看起来是有意为之但可能造成问题
- **建议修复**: 
  1. 确认这是有意行为
  2. 文档化 fetch 替换行为
  3. 确保不会意外影响其他代码

#### 16. [网络] [中严重] [无显式 http.Agent 配置]
- **文件/路径**: 全局
- **问题描述**: 
  - 无显式的 http.Agent/https.Agent 配置
  - 依赖 Node.js 默认连接池
  - 无 maxSockets 或 maxConnections 配置
  - 高吞吐场景可能出问题
- **建议修复**: 
  1. 添加 http Agent 配置
  2. 文档化连接池大小
  3. 监控连接使用情况

#### 17. [序列化] [中严重] [合同版本硬编码为字符串字面量]
- **文件/路径**: `src/platform/contracts/inter-plane-contract-gateway.ts:184`
- **问题描述**: 
  - `schemaVersion: "v4.3"` 硬编码
  - 难以追踪合同版本演变
- **建议修复**: 
  1. 提取到类型化常量或 enum
  2. 集中管理合同版本
  3. 添加版本兼容性检查

#### 18. [序列化] [低严重] [Timestamp = string 丢失精度]
- **文件/路径**: `src/platform/contracts/types/domain/primitives.ts:10`
- **问题描述**: 
  - `type Timestamp = string` 无类型精度
  - ISO 8601 字符串无时区信息
- **建议修复**: 
  1. 使用 Date 对象或 branded string 类型
  2. 明确时区处理约定
  3. 文档化时间戳语义

#### 19. [源代码] [中严重] [29处非空断言 !.]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - 使用 `!.` 断言非空
  - 可能导致运行时错误
- **建议修复**: 
  1. 使用可选链 `?.` 或空值合并 `??`
  2. 添加适当的 null 检查
  3. 减少非空断言使用

#### 20. [API] [中严重] [API 路由 schema 版本硬编码]
- **文件/路径**: `src/platform/five-plane-interface/api/federation-routing-service.ts`
- **问题描述**: 
  - Zod schema 验证存在
  - 但 schema 版本未与 contract 版本同步
- **建议修复**: 
  1. 从 contract 版本派生 schema 版本
  2. 添加版本一致性检查
  3. 文档化版本关系

### 总结

本次补充Review（第十五轮 - 构建测试与类型系统Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. 无 TypeScript 增量编译缓存（每次完整重建9.6秒）
2. 76个 npm scripts 大部分重复构建
3. 168处 any 类型使用（绕过类型安全）
4. 38处 @ts-ignore 指令
5. ExecutionRecord 类型不匹配（14个文件）
6. 178处直接 process.env 变更无抽象
7. ScopedExternalAccessSandbox performHttpRequest 无超时
8. OIDC 服务 fetch 调用无超时
9. plugin-runtime-child 替换 globalThis.fetch 为 no-op

**中优先级**:
1. 30+处 as unknown as 双类型转换
2. rimraf extraneous 未声明
3. 21,998处 unlinkSync 无集中清理
4. 无统一 mocking 框架
5. Singleton 状态重置未标准化
6. 无显式 http.Agent 配置
7. 合同版本硬编码为字符串
8. Timestamp = string 丢失精度
9. 非空断言使用

**低优先级**:
1. 无分支覆盖率要求
2. API 路由 schema 版本未同步

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 72 | 87 | 39 | 198 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **109** | **182** | **85** | **376** |

### 优先修复建议

**立即处理 (本周内)**:
1. 添加 TypeScript 增量编译 (`incremental: true`)
2. 重构 npm scripts 避免重复构建
3. 减少 any 类型使用到 <50
4. 移除所有 @ts-ignore 指令
5. 修复 ExecutionRecord 类型不匹配
6. 添加 fetch 超时到 OIDC 和 sandbox

**短期内处理 (本月内)**:
1. 创建集中的测试清理 utility
2. 建立统一的 mocking 框架
3. 标准化 singleton 重置机制
4. 添加 http.Agent 连接池配置
5. 提取合同版本到类型化常量

**长期规划**:
1. 实现完整的类型安全（无 any）
2. 建立测试基础设施标准
3. 实现构建缓存优化
4. 完善网络层超时和重试
5. 建立覆盖率门禁标准

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十四轮 - 日志事件与工作流Review）

### 发现的新问题

#### 1. [可观测性] [高严重] [StructuredLogEntry 无 requestId 字段]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - StructuredLogEntry 接口无 requestId 字段
  - 只有 correlationId，它回退到 traceId
  - 无法独立追踪单个 HTTP 请求
- **建议修复**: 
  1. 在 StructuredLogEntry 添加 requestId 字段
  2. 在 HTTP 中间件中生成和注入 requestId
  3. 确保 requestId 流经所有日志调用

#### 2. [可观测性] [高严重] [StructuredLogger data 字段无自动清洗]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - 如果开发者传递 `{ password: "..." }` 给 logger.info，密码会被记录
  - 无字段级脱敏机制
  - 依赖开发者手动排除敏感字段
- **建议修复**: 
  1. 添加 OBSERVE_OUTPUT_BLACKLIST 类似的黑名单
  2. 实现 redact/mask/sanitize 工具函数
  3. 在日志传输前自动清洗敏感字段

#### 3. [可观测性] [中严重] [日志中敏感数据无 PII 处理]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - 无 PII 重写工具
  - 开发者必须手动避免记录敏感数据
  - 存在意外记录敏感信息的风险
- **建议修复**: 
  1. 添加 PII 检测和重写工具
  2. 在文档中明确记录敏感字段列表
  3. 添加测试验证敏感数据不被记录

#### 4. [可观测性] [中严重] [59处 console.* 直接调用]
- **文件/路径**: 多个源文件
- **问题描述**: 
  - 应该使用 StructuredLogger
  - console 调用可能不被日志聚合系统收集
  - 难以追踪和关联
- **建议修复**: 
  1. 替换所有 console.* 为 StructuredLogger
  2. 添加 ESLint 规则禁止 console.*
  3. 保留少量允许的 CLI 输出

#### 5. [可观测性] [中严重] [部分 logger.warn 调用缺少结构化数据]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:122`
- **问题描述**: 
  ```typescript
  logger.warn("WebSocket connection rejected: missing subprotocol token")
  ```
  - 只有消息，无 data 对象
  - 难以关联和查询
- **建议修复**: 
  1. 添加 `{ data: { ... } }` 对象
  2. 确保所有日志包含上下文数据
  3. 添加日志审查工具

#### 6. [源代码] [高严重] [Harness while(true) 循环无硬性迭代上限]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:1442`
- **问题描述**: 
  ```typescript
  while (true) {
    // ...
    // 依赖 budget gate 和 guardrail vibration 退出
    // 如果两者都失败，循环永久继续
  }
  ```
  - 依赖 budget gate 和 guardrail vibration 退出
  - 无硬性迭代次数限制
  - 可能导致无限循环
- **建议修复**: 
  1. 添加硬性 maxIterations 上限
  2. 在达到上限时强制退出
  3. 记录详细的迭代统计用于调试

#### 7. [源代码] [高严重] [Oapeflir while(true) 循环可能无限重规划]
- **文件/路径**: `src/platform/five-plane-orchestration/oapeflir/oapeflir-loop-service.ts:355`
- **问题描述**: 
  - 如果 loopReplanDecision.shouldReplan 永不变为 false
  - 重规划永远无法减少计划错误
  - 可能导致无限循环
- **建议修复**: 
  1. 添加重规划次数上限
  2. 在达到上限时强制接受当前结果
  3. 添加重规划质量衰减检测

#### 8. [源代码] [中严重] [Recovery 流程允许终态转换到 paused]
- **文件/路径**: `src/platform/five-plane-execution/runtime-state-machine.ts:105-108`
- **问题描述**: 
  - completed, failed, cancelled, aborted 可转换到 paused 进行恢复
  - 创建恢复路径可能重新进入执行
  - 需要验证此行为的正确性
- **建议修复**: 
  1. 审查 recovery 转换的合法性
  2. 确保不会导致状态不一致
  3. 添加转换前置条件验证

#### 9. [国际化] [高严重] [API 错误消息硬编码英文]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/api-error.ts`
- **问题描述**: 
  ```typescript
  return new ApiError(404, "api.task_not_found", "Task not found.");
  ```
  - 错误消息硬编码英文
  - 无后端 i18n 机制
  - 非英文客户端收到不本地化的错误
- **建议修复**: 
  1. 使用错误码而非硬编码消息
  2. 客户端根据错误码本地化
  3. 或在后端添加 i18n 支持

#### 10. [国际化] [中严重] [UI 硬编码字符串未翻译]
- **文件/路径**: `ui/packages/features/conversation/src/web/index.tsx`
- **问题描述**: 
  - "Build Plan", "Execute", "Messages" 等按钮标签硬编码
  - 不使用 i18n 系统
- **建议修复**: 
  1. 使用 `translateFeatureCopy` 替换硬编码
  2. 确保所有用户可见文本通过 i18n
  3. 添加硬编码字符串检测

#### 11. [国际化] [中严重] [Arabic 目录不完整]
- **文件/路径**: `ui/packages/shared/i18n/src/catalogs/ar-SA.ts`
- **问题描述**: 
  - ar-SA 只有约18条消息
  - en-US 和 zh-CN 有约117条
  - 缺失的 key 返回 key 本身
- **建议修复**: 
  1. 补充 Arabic 翻译
  2. 确保所有 key 都有翻译
  3. 添加翻译完整性测试

#### 12. [国际化] [低严重] [translateFeatureCopy 无回退默认值]
- **文件/路径**: `ui/packages/shared/i18n/`
- **问题描述**: 
  - 如果 feature ID 不在 catalog 中
  - 返回 undefined title/summary
  - 无合理回退
- **建议修复**: 
  1. 添加回退到 key 或默认字符串
  2. 或使用 feature ID 作为显示名
  3. 确保 UI 不显示 undefined

#### 13. [运维] [高严重] [健康检查无 readiness/liveness 区分]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts`
- **问题描述**: 
  - /healthz, /v1/healthz, /health 返回相同报告
  - 无 readiness probe（可处理流量？）
  - 无 liveness probe（系统存活？）
  - canAcceptTraffic() 未通过 HTTP 暴露
- **建议修复**: 
  1. 添加 /ready 和 /live 端点
  2. Readiness 检查依赖项就绪
  3. Liveness 检查进程健康

#### 14. [运维] [高严重] [shutdown 期间健康检查存在竞态]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts`
- **问题描述**: 
  - getHealthReportAsync() 异步检查 dbWritable
  - shutdown 期间数据库连接可能关闭
  - 健康检查仍在传入
- **建议修复**: 
  1. 在 shutdown 时返回 unhealthy
  2. 添加 shutdown 状态标志
  3. 停止接受新的健康检查请求

#### 15. [运维] [高严重] [LeaderElectionService 未与 GracefulShutdown 集成]
- **文件/路径**: `src/platform/five-plane-execution/ha/leader-election-service.ts`
- **问题描述**: 
  - LeaderElectionService.stop() 处理优雅退位
  - 但未在全局 graceful shutdown 中注册
  - 需显式注册为 shutdown handler
- **建议修复**: 
  1. 将 LeaderElectionService 添加到 shutdown handlers
  2. 确保领导者在 shutdown 时让位
  3. 验证关闭顺序正确

#### 16. [运维] [中严重] [addHandler() 无显式排序约定]
- **文件/路径**: `src/platform/five-plane-execution/startup/graceful-shutdown.ts`
- **问题描述**: 
  - 虽然 handlers 逆序执行
  - 但无显式接口声明 handler 依赖关系
  - 关键 handlers（如关闭数据库连接）应最后运行
- **建议修复**: 
  1. 添加优先级或阶段参数
  2. 文档化关键 handler 顺序要求
  3. 添加顺序验证测试

#### 17. [运维] [中严重] [unref'd timers 可能不触发]
- **文件/路径**: `src/platform/five-plane-execution/startup/process-error-handlers.ts:106`
- **问题描述**: 
  - `setTimeout(...).unref()` 不保持事件循环活跃
  - 如果主线程空闲，强制退出可能不触发
- **建议修复**: 
  1. 移除 .unref() 除非确实需要
  2. 或确保有其他方式保持事件循环
  3. 添加超时触发验证测试

#### 18. [运维] [中严重] [StartupConsistencyChecker 阻塞流量但不暴露状态]
- **文件/路径**: `src/platform/five-plane-startup-plan.ts`
- **问题描述**: 
  - canAcceptTraffic() 在 P0 问题存在时返回 false
  - 但 _trafficBlocked 是内部状态
  - 负载均衡器访问 /healthz 仍会得到 "ok"
- **建议修复**: 
  1. 在健康报告中暴露 trafficBlocked 状态
  2. 或添加单独的健康端点指示就绪状态
  3. 确保负载均衡器在阻塞时不路由流量

#### 19. [事件系统] [中严重] [TypedEventBus vs EventEmitter 混用]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - 项目同时使用 TypedEventBus 和原生 EventEmitter
  - TypedEventBus 应该是标准
  - 混用可能导致类型安全问题
- **建议修复**: 
  1. 创建迁移计划统一使用 TypedEventBus
  2. 添加 ESLint 规则禁止原生 EventEmitter
  3. 在 CI 中检测混用情况

#### 20. [源代码] [低严重] [无事件排序保证]
- **文件/路径**: `src/platform/five-plane-state-evidence/events/`
- **问题描述**: 
  - 无明确的事件排序保证文档
  - 事件处理可能乱序
  - 依赖方需要处理乱序
- **建议修复**: 
  1. 文档化事件排序语义
  2. 如果需要排序，实现序列号机制
  3. 在事件处理中考虑乱序情况

### 总结

本次补充Review（第十四轮 - 日志事件与工作流Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. StructuredLogEntry 无 requestId 字段
2. StructuredLogger data 字段无自动清洗（敏感数据风险）
3. Harness while(true) 循环无硬性迭代上限
4. Oapeflir while(true) 循环可能无限重规划
5. API 错误消息硬编码英文（无 i18n）
6. 健康检查无 readiness/liveness 区分
7. shutdown 期间健康检查存在竞态
8. LeaderElectionService 未与 GracefulShutdown 集成

**中优先级**:
1. 日志中敏感数据无 PII 处理
2. 59处 console.* 直接调用
3. 部分 logger.warn 调用缺少结构化数据
4. Recovery 流程允许终态转换到 paused
5. UI 硬编码字符串未翻译
6. Arabic 目录不完整
7. addHandler() 无显式排序约定
8. unref'd timers 可能不触发
9. StartupConsistencyChecker 阻塞流量但不暴露状态
10. TypedEventBus vs EventEmitter 混用

**低优先级**:
1. translateFeatureCopy 无回退默认值
2. 无事件排序保证

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 68 | 80 | 37 | 185 |
| 测试 | 8 | 14 | 5 | 27 |
| 配置 | 10 | 38 | 24 | 72 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **103** | **171** | **82** | **356** |

### 优先修复建议

**立即处理 (本周内)**:
1. 在 StructuredLogEntry 添加 requestId 字段
2. 实现日志敏感字段自动清洗
3. 为工作流循环添加硬性迭代上限
4. 添加 readiness/liveness 健康检查端点
5. 将 LeaderElectionService 集成到 GracefulShutdown
6. 实现 API 错误消息 i18n 支持

**短期内处理 (本月内)**:
1. 替换所有 console.* 为 StructuredLogger
2. 补充 Arabic i18n 目录
3. 修复 UI 硬编码字符串
4. 统一 TypedEventBus 替代 EventEmitter
5. 解决 shutdown 竞态条件
6. 添加事件排序保证文档

**长期规划**:
1. 建立完整的 PII 处理框架
2. 实现日志完整性验证
3. 建立工作流安全循环检测
4. 完善健康检查和就绪探测
5. 建立国际化完整测试

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十五轮 - 构建测试与类型系统Review）

### 发现的新问题

#### 1. [构建] [高严重] [无 TypeScript 增量编译缓存]
- **文件/路径**: `tsconfig.json`
- **问题描述**: 
  - 无 `incremental: true` 配置
  - 无 `tsbuildinfo` 文件
  - 每次构建是完整重建（9.6秒）
  - TypeScript 无法使用增量编译
- **建议修复**: 
  1. 在 tsconfig.json 添加 `"incremental": true`
  2. 添加 `"tsbuildinfo": ".tsbuildinfo"` 到 .gitignore
  3. 实现项目引用以支持并行构建

#### 2. [构建] [高严重] [76个 npm scripts 大部分重复构建]
- **文件/路径**: `package.json`
- **问题描述**: 
  - 40+ CLI scripts 都运行 `npm run build && node ...`
  - 每个脚本前都执行完整重建
  - 无法直接运行预构建的 CLI
  - 20+ stable:* 脚本遵循相同模式
- **建议修复**: 
  1. 创建 `build:cli` 脚本构建一次
  2. 让 CLI 脚本使用预构建的 dist 文件
  3. 添加 npm run dev 或类似的无需重建的执行方式

#### 3. [源代码] [高严重] [168处 any 类型使用]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - `stable-runner-factory.ts`: `opts: any` -> `any`
  - `billing-service-async.ts`: `input: any` 方法签名
  - 重试/超时工具使用 `(...args: any[]) => Promise<any>`
  - 绕过类型安全
- **建议修复**: 
  1. 使用泛型替代 any
  2. 替换为具体的输入类型
  3. 添加 ESLint 规则禁止 any

#### 4. [源代码] [高严重] [38处 @ts-ignore 指令]
- **文件/路径**: 多个文件
- **问题描述**: 
  - 14个 stability 相关文件有 `@ts-ignore ExecutionRecord type mismatch`
  - 5个 harness-sdk 文件有 `Partial<HarnessRun> doesn't have all required properties`
  - 2个文件有 `exactOptionalPropertyTypes` 问题
- **建议修复**: 
  1. 修复 ExecutionRecord 类型不匹配
  2. 完成 HarnessRun 类型的必需属性
  3. 移除 @ts-ignore 使用

#### 5. [源代码] [高严重] [ExecutionRecord 类型不匹配]
- **文件/路径**: `src/platform/stability/stable-evidence-bundle-support.ts:783` 等14个文件
- **问题描述**: 
  - 插入 execution record 时 TypeScript 拒绝类型
  - `ExecutionRecord` 接口要求新字段但 insert 对象缺失
  - 或 store 的 insert 方法接受不同类型
- **建议修复**: 
  1. 对齐 ExecutionRecord 接口和 store 类型
  2. 添加缺失字段到 insert 对象
  3. 或调整 store 的 insert 方法类型签名

#### 6. [源代码] [中严重] [30+处 as unknown as 双类型转换]
- **文件/路径**: `division-loader.ts`, `domain-baseline-catalog.ts`, `harness-sdk`
- **问题描述**: 
  - 完全绕过类型安全
  - 通常是类型架构问题的信号
- **建议修复**: 
  1. 修复上游类型问题
  2. 使用更具体的类型断言
  3. 重构类型层次结构

#### 7. [构建] [中严重] [rimraf  extraneous - 未在 package.json 声明]
- **文件/路径**: `node_modules/rimraf`
- **问题描述**: 
  - rimraf@6.1.3 存在于 node_modules 但未在 package.json 声明
  - 可能导致依赖问题
- **建议修复**: 
  1. 添加到 dependencies 或 devDependencies
  2. 或移除并使用其他清理方式

#### 8. [测试] [高严重] [178处直接 process.env 变更无抽象]
- **文件/路径**: `tests/unit/domains/registry/plugin-spi-registry.test.ts:640-687`
- **问题描述**: 
  - 直接读取/写入 process.env
  - 在 beforeEach/afterEach 中手动保存/恢复
  - 无 helper 抽象
- **建议修复**: 
  1. 创建 env helper 函数
  2. 统一保存和恢复模式
  3. 添加测试隔离验证

#### 9. [测试] [高严重] [21,998处 unlinkSync 调用无集中清理]
- **文件/路径**: 多个测试文件
- **问题描述**: 
  - 每个测试重新实现清理逻辑
  - 无集中清理 utility
  - 可能导致 EBUSY (Windows) 或并发访问问题
- **建议修复**: 
  1. 创建集中的测试清理 utility
  2. 使用 afterEach 确保清理
  3. 考虑使用临时目录

#### 10. [测试] [中严重] [无统一 mocking 框架]
- **文件/路径**: `tests/` 多个目录
- **问题描述**: 
  - 10,579 处 vi.fn, jest.mock, sinon 等调用
  - 无统一的 mocking 框架可见
  - 模式不一致
- **建议修复**: 
  1. 建立统一的 mock factory
  2. 标准化 mock 模式
  3. 添加 mocking 最佳实践文档

#### 11. [测试] [中严重] [Singleton 状态重置未标准化]
- **文件/路径**: `tests/unit/platform/five-plane-execution/dispatcher.test.ts`
- **问题描述**: 
  - 测试通过特定函数重置 singletons
  - 每个测试重复自己的 reset 逻辑
  - 无统一的 resetAllSingletons()
- **建议修复**: 
  1. 创建统一的 singleton reset 机制
  2. 在 afterEach 中自动调用
  3. 文档化 singleton 重置要求

#### 12. [测试] [低严重] [无分支覆盖率要求]
- **文件/路径**: `.c8rc.json`
- **问题描述**: 
  - `"100": false` - 无覆盖率要求
  - 只测量源代码，不测量测试文件
- **建议修复**: 
  1. 添加分支覆盖率阈值
  2. 在 CI 中强制执行覆盖率 gate
  3. 关注关键代码路径

#### 13. [网络] [高严重] [ScopedExternalAccessSandbox performHttpRequest 无超时]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/scoped-external-access-sandbox.ts:289`
- **问题描述**: 
  - fetch 调用无 timeout 参数
  - 挂起的请求可能永久阻塞
- **建议修复**: 
  1. 添加 AbortController timeout
  2. 使用标准的超时模式
  3. 记录超时配置

#### 14. [网络] [高严重] [OIDC 服务 fetch 调用无超时]
- **文件/路径**: `src/org-governance/sso-scim/oidc/oidc-service.ts:565,272`
- **问题描述**: 
  - OIDC token 交换无 retry logic
  - 无 timeout/abort signal
  - 认证流程可能挂起
- **建议修复**: 
  1. 添加 fetch timeout
  2. 添加 retry logic
  3. 使用标准的超时配置

#### 15. [网络] [高严重] [plugin-runtime-child 替换 globalThis.fetch 为 no-op]
- **文件/路径**: `src/domains/registry/plugin-runtime-child.ts:112`
- **问题描述**: 
  ```typescript
  globalThis.fetch = (async () => {}) as unknown as typeof globalThis.fetch;
  ```
  - 插件上下文中完全替换 fetch 为空函数
  - 插件的所有出站 HTTP 请求都会失败
  - 看起来是有意为之但可能造成问题
- **建议修复**: 
  1. 确认这是有意行为
  2. 文档化 fetch 替换行为
  3. 确保不会意外影响其他代码

#### 16. [网络] [中严重] [无显式 http.Agent 配置]
- **文件/路径**: 全局
- **问题描述**: 
  - 无显式的 http.Agent/https.Agent 配置
  - 依赖 Node.js 默认连接池
  - 无 maxSockets 或 maxConnections 配置
  - 高吞吐场景可能出问题
- **建议修复**: 
  1. 添加 http Agent 配置
  2. 文档化连接池大小
  3. 监控连接使用情况

#### 17. [序列化] [中严重] [合同版本硬编码为字符串字面量]
- **文件/路径**: `src/platform/contracts/inter-plane-contract-gateway.ts:184`
- **问题描述**: 
  - `schemaVersion: "v4.3"` 硬编码
  - 难以追踪合同版本演变
- **建议修复**: 
  1. 提取到类型化常量或 enum
  2. 集中管理合同版本
  3. 添加版本兼容性检查

#### 18. [序列化] [低严重] [Timestamp = string 丢失精度]
- **文件/路径**: `src/platform/contracts/types/domain/primitives.ts:10`
- **问题描述**: 
  - `type Timestamp = string` 无类型精度
  - ISO 8601 字符串无时区信息
- **建议修复**: 
  1. 使用 Date 对象或 branded string 类型
  2. 明确时区处理约定
  3. 文档化时间戳语义

#### 19. [源代码] [中严重] [29处非空断言 !.]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - 使用 `!.` 断言非空
  - 可能导致运行时错误
- **建议修复**: 
  1. 使用可选链 `?.` 或空值合并 `??`
  2. 添加适当的 null 检查
  3. 减少非空断言使用

#### 20. [API] [中严重] [API 路由 schema 版本硬编码]
- **文件/路径**: `src/platform/five-plane-interface/api/federation-routing-service.ts`
- **问题描述**: 
  - Zod schema 验证存在
  - 但 schema 版本未与 contract 版本同步
- **建议修复**: 
  1. 从 contract 版本派生 schema 版本
  2. 添加版本一致性检查
  3. 文档化版本关系

### 总结

本次补充Review（第十五轮 - 构建测试与类型系统Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. 无 TypeScript 增量编译缓存（每次完整重建9.6秒）
2. 76个 npm scripts 大部分重复构建
3. 168处 any 类型使用（绕过类型安全）
4. 38处 @ts-ignore 指令
5. ExecutionRecord 类型不匹配（14个文件）
6. 178处直接 process.env 变更无抽象
7. ScopedExternalAccessSandbox performHttpRequest 无超时
8. OIDC 服务 fetch 调用无超时
9. plugin-runtime-child 替换 globalThis.fetch 为 no-op

**中优先级**:
1. 30+处 as unknown as 双类型转换
2. rimraf extraneous 未声明
3. 21,998处 unlinkSync 无集中清理
4. 无统一 mocking 框架
5. Singleton 状态重置未标准化
6. 无显式 http.Agent 配置
7. 合同版本硬编码为字符串
8. Timestamp = string 丢失精度
9. 非空断言使用

**低优先级**:
1. 无分支覆盖率要求
2. API 路由 schema 版本未同步

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 72 | 87 | 39 | 198 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **109** | **182** | **85** | **376** |

### 优先修复建议

**立即处理 (本周内)**:
1. 添加 TypeScript 增量编译 (`incremental: true`)
2. 重构 npm scripts 避免重复构建
3. 减少 any 类型使用到 <50
4. 移除所有 @ts-ignore 指令
5. 修复 ExecutionRecord 类型不匹配
6. 添加 fetch 超时到 OIDC 和 sandbox

**短期内处理 (本月内)**:
1. 创建集中的测试清理 utility
2. 建立统一的 mocking 框架
3. 标准化 singleton 重置机制
4. 添加 http.Agent 连接池配置
5. 提取合同版本到类型化常量

**长期规划**:
1. 实现完整的类型安全（无 any）
2. 建立测试基础设施标准
3. 实现构建缓存优化
4. 完善网络层超时和重试
5. 建立覆盖率门禁标准

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十三轮 - 缓存会话与实时通信Review）

### 发现的新问题

#### 1. [安全] [严重] [CORS allowedMethods 缺少 PUT/PATCH/DELETE]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/response-hardening.ts:12-14`
- **问题描述**: 
  ```typescript
  export const DEFAULT_CORS_CONFIG: CorsConfig = {
    allowedMethods: ["GET", "POST", "OPTIONS"],  // 缺少 PUT, PATCH, DELETE
  ```
  - 浏览器 CORS preflight 会拒绝 PUT/PATCH/DELETE 请求
  - 但路由中有 PATCH /v1/tasks/:id, DELETE endpoints
- **建议修复**: 
  1. 添加 PUT, PATCH, DELETE 到 allowedMethods
  2. 确保 CORS 配置与应用路由方法一致
  3. 添加测试验证所有 HTTP 方法的 CORS preflight

#### 2. [安全] [严重] [Mission Routes 错误响应包装不当]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/mission-routes.ts:101,118-124`
- **问题描述**: 
  ```typescript
  return buildJsonResponse(ctx.requestId, 404, { error: { code: "MISSION_NOT_FOUND" } });
  ```
  - 使用 buildJsonResponse（成功包装）返回错误
  - 错误被包装在成功信封 `{requestId, data: {error: {...}}}` 中
  - 违反 REST 规范
- **建议修复**: 
  1. 使用 buildJsonErrorResponse 处理错误
  2. 确保所有错误路径使用正确的错误信封
  3. 添加 API 一致性测试

#### 3. [源代码] [高严重] [WebSocketBridge 无最大连接数限制]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:103-107`
- **问题描述**: 
  ```typescript
  this.wss = new WebSocketServer({
    server,
    path: WS_PATH,
    maxPayload: options.maxPayloadBytes ?? DEFAULT_MAX_PAYLOAD_BYTES,
    // 无 maxConnections 限制！
  });
  ```
  - 与 dashboard-websocket-server.ts 的 maxClients: 1000 不同
  - 恶意客户端可耗尽服务器资源
- **建议修复**: 
  1. 添加 maxConnections 限制
  2. 实现连接限制拒绝策略
  3. 添加连接计数指标

#### 4. [源代码] [高严重] [pendingAcks 在断开连接时未清理]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:268-282`
- **问题描述**: 
  - handleDisconnection 清理 subscribedTasks, taskSubscribers, slowConsumers
  - 但 pendingAcks Map 未清理
  - 断开的客户端未确认的消息永久留在内存
- **建议修复**: 
  1. 在 handleDisconnection 中删除所有 pendingAcks 条目
  2. 考虑添加超时机制自动清理未确认消息
  3. 记录清理操作的审计日志

#### 5. [安全] [高严重] [硬编码回退密钥]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/audit-event-integrity.ts:41`
- **问题描述**: 
  ```typescript
  const AUDIT_INTEGRITY_HMAC_KEY = process.env["AA_AUDIT_INTEGRITY_HMAC_KEY"] ?? "audit-integrity-secret-key-32-bytes!";
  ```
  - 占位符密钥用作回退
  - 如果环境变量未设置，使用不安全密钥
- **建议修复**: 
  1. 移除回退值，要求环境变量必须设置
  2. 在启动时验证密钥存在且足够强
  3. 如果密钥缺失导致启动失败

#### 6. [配置] [高严重] [config/runtime/test.json 缺少超时配置]
- **文件/路径**: `config/runtime/test.json`
- **问题描述**: 
  - 只有3个字段，缺少 maxConcurrentTasks, defaultTaskTimeoutMs, defaultStepTimeoutMs
  - 回退到默认值可能不适合测试环境
- **建议修复**: 
  1. 添加完整的 timeout/tuning 配置
  2. 确保测试环境有合理的默认值
  3. 文档化必需的配置字段

#### 7. [配置] [高严重] [prod.json 限制过严 - maxConcurrentTasks=1]
- **文件/路径**: `config/runtime/prod.json`
- **问题描述**: 
  - maxConcurrentTasks: 1（vs dev:8, staging:4, pre-prod:6）
  - defaultTaskTimeoutMs: 120000（vs dev:600000）
  - defaultStepTimeoutMs: 60000（vs dev:180000）
  - 生产环境严重受限，可能影响吞吐量
- **建议修复**: 
  1. 评估并调整生产限制
  2. 与业务需求匹配
  3. 添加生产容量测试

#### 8. [源代码] [高严重] [多个服务存在无界 Map 无驱逐策略]
- **文件/路径**: 多个域服务
- **问题描述**: 
  - `domain-knowledge-schema-service.ts:73-75`: schemas, sourceContent, sourceTimestamps 无限制
  - `domain-eval-framework-service.ts:91-95`: frameworks, qualityAxes 等无限制
  - `domain-recipe-service.ts:67-68`: recipes, versions 无限制
  - `domain-risk-profile-service.ts:51`: profiles 无限制
  - Session Maps (session-management.ts:83-89) 无自动清理
- **建议修复**: 
  1. 为所有 Map 实现 LRU 或 TTL 驱逐策略
  2. 添加后台清理任务
  3. 添加大小监控和告警

#### 9. [源代码] [中严重] [taskEventHistory Map 永不清理]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:91,548-555`
- **问题描述**: 
  - taskEventHistory 限制单任务历史为 200 条
  - 但 Map 本身从不清理
  - 取消订阅的任务历史永久保留
- **建议修复**: 
  1. 实现后台清理无订阅者任务的历史
  2. 添加任务订阅者监控
  3. 考虑使用 WeakMap 替代

#### 10. [配置] [中严重] [安全配置 drift - remoteWorkerRegistration 缺失]
- **文件/路径**: `config/security/dev.json`, `staging.json`, `pre-prod.json`
- **问题描述**: 
  - default.json 有完整的 remoteWorkerRegistration 配置
  - dev/staging/pre-prod 只有 approvalMode
  - prod 有 approvalMode 但无 remoteWorkerRegistration
  - 安全配置不一致
- **建议修复**: 
  1. 统一所有环境的 remoteWorkerRegistration
  2. 添加安全配置验证
  3. 确保最低安全基线

#### 11. [API] [中严重] [OpenAPI 端点响应格式不一致]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/utils.ts:207-215`
- **问题描述**: 
  - `/v1/openapi.json` 使用 buildJsonDocumentResponse（无包装）
  - 其他端点使用 buildJsonResponse（{requestId, data} 包装）
  - 客户端体验不一致
- **建议修复**: 
  1. 统一响应信封格式
  2. 文档化响应格式规范
  3. 添加响应格式验证测试

#### 12. [源代码] [中严重] [Redis 队列缺少 idempotency 索引支持]
- **文件/路径**: `src/platform/five-plane-execution/queue/redis-queue-adapter.ts`
- **问题描述**: 
  - SQLite 有部分唯一索引支持 idempotency
  - Redis 实现使用哈希索引但实现不完整
  - 可能导致重复消息
- **建议修复**: 
  1. 完善 Redis idempotency 实现
  2. 添加唯一索引验证
  3. 确保与 SQLite 行为一致

#### 13. [源代码] [中严重] [请求去重中间件使用内存存储]
- **文件/路径**: `src/platform/five-plane-interface/api/middleware/request-deduplication.ts`
- **问题描述**: 
  - 纯内存滑动窗口（Map<DeduplicationKey, DeduplicationEntry[]>）
  - 无外部存储
  - 多实例部署不共享
  - 重启后去重状态丢失
- **建议修复**: 
  1. 使用 Redis 替代内存存储
  2. 支持分布式去重
  3. 持久化去重状态

#### 14. [源代码] [中严重] [Cache 无 stampede 保护]
- **文件/路径**: 多个 cache 实现
- **问题描述**: 
  - MemoryCacheStore, ExperienceCacheService 无锁
  - 缓存未命中时可发生 thundering herd
  - 高并发下可能导致数据库过载
- **建议修复**: 
  1. 实现 single-flight 模式
  2. 添加请求排队机制
  3. 使用分布式锁保护缓存更新

#### 15. [源代码] [中严重] [EvidenceService eviction 仅在插入时触发]
- **文件/路径**: `src/platform/five-plane-state-evidence/memory/evidence-service.ts:202`
- **问题描述**: 
  - 清理仅在 record() 调用时触发
  - 空闲时无后台清理
  - 可能导致内存持续增长
- **建议修复**: 
  1. 添加定期后台清理任务
  2. 使用独立的清理线程
  3. 添加内存使用监控

#### 16. [源代码] [低严重] [无连接指标暴露]
- **文件/路径**: WebSocket 服务器
- **问题描述**: 
  - getClientCount() 存在但未通过 HTTP 端点暴露
  - 无 pendingAcks 队列深度指标
  - 无连接建立告警
- **建议修复**: 
  1. 通过 metrics 端点暴露连接指标
  2. 添加 pendingAcks 队列监控
  3. 添加连接数异常告警

#### 17. [源代码] [低严重] [无空闲客户端超时]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts`
- **问题描述**: 
  - 仅在心跳 Sweep 时检查 isAlive
  - 无 per-client 独立于心跳的空闲超时
  - 认证后从不发送消息的客户端只能通过心跳失败检测
- **建议修复**: 
  1. 添加 per-client idle timeout
  2. 独立于心跳间隔
  3. 配置可调整

#### 18. [源代码] [低严重] [订阅限制是 per-client 而非全局]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:244`
- **问题描述**: 
  - MAX_SUBSCRIPTIONS_PER_CLIENT = 100
  - 恶意客户端可订阅 100 个任务
  - 无全局任务订阅者限制
- **建议修复**: 
  1. 添加全局任务订阅者限制
  2. 实现 per-task 订阅者上限
  3. 添加反滥用检测

#### 19. [配置] [低严重] [config/runtime 无版本验证机制]
- **文件/路径**: `config/runtime/*.json`
- **问题描述**: 
  - 配置有 "version": "v4.3"
  - 但无 schema 版本验证
  - 加载时可能接受不兼容配置
- **建议修复**: 
  1. 添加 JSON Schema 验证
  2. 实现版本兼容性检查
  3. 启动时验证配置完整性

#### 20. [源代码] [低严重] [ImprovementCandidateRegistry splice 是 O(n)]
- **文件/路径**: `src/platform/five-plane-orchestration/improve/improvement-candidate-registry.ts:243`
- **问题描述**: 
  - accessOrder 数组的 splice 操作是 O(n)
  - 高频访问时可能性能问题
- **建议修复**: 
  1. 使用 LinkedList 替代数组
  2. 或使用 Map 维护访问顺序
  3. 性能测试验证

### 总结

本次补充Review（第十三轮 - 缓存会话与实时通信Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. CORS allowedMethods 缺少 PUT/PATCH/DELETE（浏览器请求会失败）
2. Mission Routes 错误响应包装不当
3. WebSocketBridge 无最大连接数限制（DoS风险）
4. pendingAcks 断开连接时未清理（内存泄漏）
5. 硬编码回退密钥（安全漏洞）
6. config/runtime/test.json 缺少超时配置
7. prod.json maxConcurrentTasks=1 限制过严
8. 多个服务存在无界 Map（内存泄漏风险）

**中优先级**:
1. taskEventHistory Map 永不清理
2. 安全配置 drift（remoteWorkerRegistration 缺失）
3. OpenAPI 端点响应格式不一致
4. Redis 队列 idempotency 实现不完整
5. 请求去重中间件使用内存存储（多实例不共享）
6. Cache 无 stampede 保护
7. EvidenceService eviction 仅在插入时触发

**低优先级**:
1. 无连接指标暴露
2. 无空闲客户端超时
3. 订阅限制是 per-client 而非全局
4. config/runtime 无版本验证机制
5. ImprovementCandidateRegistry splice O(n)

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 64 | 74 | 35 | 173 |
| 测试 | 8 | 14 | 5 | 27 |
| 配置 | 10 | 38 | 24 | 72 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 7 | 3 | 12 |
| UI | 0 | 3 | 3 | 6 |
| 部署 | 1 | 10 | 6 | 17 |
| **合计** | **99** | **161** | **79** | **339** |

### 优先修复建议

**立即处理 (本周内)**:
1. 修复 CORS allowedMethods 添加 PUT/PATCH/DELETE
2. 修复 Mission Routes 错误响应格式
3. 为 WebSocketBridge 添加最大连接数限制
4. 清理 pendingAcks 和 taskEventHistory
5. 移除硬编码回退密钥
6. 修复 config/runtime/test.json 配置
7. 评估并调整 prod 并发限制

**短期内处理 (本月内)**:
1. 统一安全配置（remoteWorkerRegistration）
2. 实现无界 Map 的 LRU/TTL 驱逐
3. 统一 API 响应信封格式
4. 完善 Redis idempotency 实现
5. 添加分布式去重中间件
6. 实现 cache stampede 保护

**长期规划**:
1. 建立完整的连接管理和监控
2. 实现后台清理任务框架
3. 添加配置 schema 验证
4. 优化高频繁操作的数据结构
5. 建立内存使用基线和告警

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十五轮 - 构建测试与类型系统Review）

### 发现的新问题

#### 1. [构建] [高严重] [无 TypeScript 增量编译缓存]
- **文件/路径**: `tsconfig.json`
- **问题描述**: 
  - 无 `incremental: true` 配置
  - 无 `tsbuildinfo` 文件
  - 每次构建是完整重建（9.6秒）
  - TypeScript 无法使用增量编译
- **建议修复**: 
  1. 在 tsconfig.json 添加 `"incremental": true`
  2. 添加 `"tsbuildinfo": ".tsbuildinfo"` 到 .gitignore
  3. 实现项目引用以支持并行构建

#### 2. [构建] [高严重] [76个 npm scripts 大部分重复构建]
- **文件/路径**: `package.json`
- **问题描述**: 
  - 40+ CLI scripts 都运行 `npm run build && node ...`
  - 每个脚本前都执行完整重建
  - 无法直接运行预构建的 CLI
  - 20+ stable:* 脚本遵循相同模式
- **建议修复**: 
  1. 创建 `build:cli` 脚本构建一次
  2. 让 CLI 脚本使用预构建的 dist 文件
  3. 添加 npm run dev 或类似的无需重建的执行方式

#### 3. [源代码] [高严重] [168处 any 类型使用]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - `stable-runner-factory.ts`: `opts: any` -> `any`
  - `billing-service-async.ts`: `input: any` 方法签名
  - 重试/超时工具使用 `(...args: any[]) => Promise<any>`
  - 绕过类型安全
- **建议修复**: 
  1. 使用泛型替代 any
  2. 替换为具体的输入类型
  3. 添加 ESLint 规则禁止 any

#### 4. [源代码] [高严重] [38处 @ts-ignore 指令]
- **文件/路径**: 多个文件
- **问题描述**: 
  - 14个 stability 相关文件有 `@ts-ignore ExecutionRecord type mismatch`
  - 5个 harness-sdk 文件有 `Partial<HarnessRun> doesn't have all required properties`
  - 2个文件有 `exactOptionalPropertyTypes` 问题
- **建议修复**: 
  1. 修复 ExecutionRecord 类型不匹配
  2. 完成 HarnessRun 类型的必需属性
  3. 移除 @ts-ignore 使用

#### 5. [源代码] [高严重] [ExecutionRecord 类型不匹配]
- **文件/路径**: `src/platform/stability/stable-evidence-bundle-support.ts:783` 等14个文件
- **问题描述**: 
  - 插入 execution record 时 TypeScript 拒绝类型
  - `ExecutionRecord` 接口要求新字段但 insert 对象缺失
  - 或 store 的 insert 方法接受不同类型
- **建议修复**: 
  1. 对齐 ExecutionRecord 接口和 store 类型
  2. 添加缺失字段到 insert 对象
  3. 或调整 store 的 insert 方法类型签名

#### 6. [源代码] [中严重] [30+处 as unknown as 双类型转换]
- **文件/路径**: `division-loader.ts`, `domain-baseline-catalog.ts`, `harness-sdk`
- **问题描述**: 
  - 完全绕过类型安全
  - 通常是类型架构问题的信号
- **建议修复**: 
  1. 修复上游类型问题
  2. 使用更具体的类型断言
  3. 重构类型层次结构

#### 7. [构建] [中严重] [rimraf  extraneous - 未在 package.json 声明]
- **文件/路径**: `node_modules/rimraf`
- **问题描述**: 
  - rimraf@6.1.3 存在于 node_modules 但未在 package.json 声明
  - 可能导致依赖问题
- **建议修复**: 
  1. 添加到 dependencies 或 devDependencies
  2. 或移除并使用其他清理方式

#### 8. [测试] [高严重] [178处直接 process.env 变更无抽象]
- **文件/路径**: `tests/unit/domains/registry/plugin-spi-registry.test.ts:640-687`
- **问题描述**: 
  - 直接读取/写入 process.env
  - 在 beforeEach/afterEach 中手动保存/恢复
  - 无 helper 抽象
- **建议修复**: 
  1. 创建 env helper 函数
  2. 统一保存和恢复模式
  3. 添加测试隔离验证

#### 9. [测试] [高严重] [21,998处 unlinkSync 调用无集中清理]
- **文件/路径**: 多个测试文件
- **问题描述**: 
  - 每个测试重新实现清理逻辑
  - 无集中清理 utility
  - 可能导致 EBUSY (Windows) 或并发访问问题
- **建议修复**: 
  1. 创建集中的测试清理 utility
  2. 使用 afterEach 确保清理
  3. 考虑使用临时目录

#### 10. [测试] [中严重] [无统一 mocking 框架]
- **文件/路径**: `tests/` 多个目录
- **问题描述**: 
  - 10,579 处 vi.fn, jest.mock, sinon 等调用
  - 无统一的 mocking 框架可见
  - 模式不一致
- **建议修复**: 
  1. 建立统一的 mock factory
  2. 标准化 mock 模式
  3. 添加 mocking 最佳实践文档

#### 11. [测试] [中严重] [Singleton 状态重置未标准化]
- **文件/路径**: `tests/unit/platform/five-plane-execution/dispatcher.test.ts`
- **问题描述**: 
  - 测试通过特定函数重置 singletons
  - 每个测试重复自己的 reset 逻辑
  - 无统一的 resetAllSingletons()
- **建议修复**: 
  1. 创建统一的 singleton reset 机制
  2. 在 afterEach 中自动调用
  3. 文档化 singleton 重置要求

#### 12. [测试] [低严重] [无分支覆盖率要求]
- **文件/路径**: `.c8rc.json`
- **问题描述**: 
  - `"100": false` - 无覆盖率要求
  - 只测量源代码，不测量测试文件
- **建议修复**: 
  1. 添加分支覆盖率阈值
  2. 在 CI 中强制执行覆盖率 gate
  3. 关注关键代码路径

#### 13. [网络] [高严重] [ScopedExternalAccessSandbox performHttpRequest 无超时]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/scoped-external-access-sandbox.ts:289`
- **问题描述**: 
  - fetch 调用无 timeout 参数
  - 挂起的请求可能永久阻塞
- **建议修复**: 
  1. 添加 AbortController timeout
  2. 使用标准的超时模式
  3. 记录超时配置

#### 14. [网络] [高严重] [OIDC 服务 fetch 调用无超时]
- **文件/路径**: `src/org-governance/sso-scim/oidc/oidc-service.ts:565,272`
- **问题描述**: 
  - OIDC token 交换无 retry logic
  - 无 timeout/abort signal
  - 认证流程可能挂起
- **建议修复**: 
  1. 添加 fetch timeout
  2. 添加 retry logic
  3. 使用标准的超时配置

#### 15. [网络] [高严重] [plugin-runtime-child 替换 globalThis.fetch 为 no-op]
- **文件/路径**: `src/domains/registry/plugin-runtime-child.ts:112`
- **问题描述**: 
  ```typescript
  globalThis.fetch = (async () => {}) as unknown as typeof globalThis.fetch;
  ```
  - 插件上下文中完全替换 fetch 为空函数
  - 插件的所有出站 HTTP 请求都会失败
  - 看起来是有意为之但可能造成问题
- **建议修复**: 
  1. 确认这是有意行为
  2. 文档化 fetch 替换行为
  3. 确保不会意外影响其他代码

#### 16. [网络] [中严重] [无显式 http.Agent 配置]
- **文件/路径**: 全局
- **问题描述**: 
  - 无显式的 http.Agent/https.Agent 配置
  - 依赖 Node.js 默认连接池
  - 无 maxSockets 或 maxConnections 配置
  - 高吞吐场景可能出问题
- **建议修复**: 
  1. 添加 http Agent 配置
  2. 文档化连接池大小
  3. 监控连接使用情况

#### 17. [序列化] [中严重] [合同版本硬编码为字符串字面量]
- **文件/路径**: `src/platform/contracts/inter-plane-contract-gateway.ts:184`
- **问题描述**: 
  - `schemaVersion: "v4.3"` 硬编码
  - 难以追踪合同版本演变
- **建议修复**: 
  1. 提取到类型化常量或 enum
  2. 集中管理合同版本
  3. 添加版本兼容性检查

#### 18. [序列化] [低严重] [Timestamp = string 丢失精度]
- **文件/路径**: `src/platform/contracts/types/domain/primitives.ts:10`
- **问题描述**: 
  - `type Timestamp = string` 无类型精度
  - ISO 8601 字符串无时区信息
- **建议修复**: 
  1. 使用 Date 对象或 branded string 类型
  2. 明确时区处理约定
  3. 文档化时间戳语义

#### 19. [源代码] [中严重] [29处非空断言 !.]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - 使用 `!.` 断言非空
  - 可能导致运行时错误
- **建议修复**: 
  1. 使用可选链 `?.` 或空值合并 `??`
  2. 添加适当的 null 检查
  3. 减少非空断言使用

#### 20. [API] [中严重] [API 路由 schema 版本硬编码]
- **文件/路径**: `src/platform/five-plane-interface/api/federation-routing-service.ts`
- **问题描述**: 
  - Zod schema 验证存在
  - 但 schema 版本未与 contract 版本同步
- **建议修复**: 
  1. 从 contract 版本派生 schema 版本
  2. 添加版本一致性检查
  3. 文档化版本关系

### 总结

本次补充Review（第十五轮 - 构建测试与类型系统Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. 无 TypeScript 增量编译缓存（每次完整重建9.6秒）
2. 76个 npm scripts 大部分重复构建
3. 168处 any 类型使用（绕过类型安全）
4. 38处 @ts-ignore 指令
5. ExecutionRecord 类型不匹配（14个文件）
6. 178处直接 process.env 变更无抽象
7. ScopedExternalAccessSandbox performHttpRequest 无超时
8. OIDC 服务 fetch 调用无超时
9. plugin-runtime-child 替换 globalThis.fetch 为 no-op

**中优先级**:
1. 30+处 as unknown as 双类型转换
2. rimraf extraneous 未声明
3. 21,998处 unlinkSync 无集中清理
4. 无统一 mocking 框架
5. Singleton 状态重置未标准化
6. 无显式 http.Agent 配置
7. 合同版本硬编码为字符串
8. Timestamp = string 丢失精度
9. 非空断言使用

**低优先级**:
1. 无分支覆盖率要求
2. API 路由 schema 版本未同步

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 72 | 87 | 39 | 198 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **109** | **182** | **85** | **376** |

### 优先修复建议

**立即处理 (本周内)**:
1. 添加 TypeScript 增量编译 (`incremental: true`)
2. 重构 npm scripts 避免重复构建
3. 减少 any 类型使用到 <50
4. 移除所有 @ts-ignore 指令
5. 修复 ExecutionRecord 类型不匹配
6. 添加 fetch 超时到 OIDC 和 sandbox

**短期内处理 (本月内)**:
1. 创建集中的测试清理 utility
2. 建立统一的 mocking 框架
3. 标准化 singleton 重置机制
4. 添加 http.Agent 连接池配置
5. 提取合同版本到类型化常量

**长期规划**:
1. 实现完整的类型安全（无 any）
2. 建立测试基础设施标准
3. 实现构建缓存优化
4. 完善网络层超时和重试
5. 建立覆盖率门禁标准

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十四轮 - 日志事件与工作流Review）

### 发现的新问题

#### 1. [可观测性] [高严重] [StructuredLogEntry 无 requestId 字段]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - StructuredLogEntry 接口无 requestId 字段
  - 只有 correlationId，它回退到 traceId
  - 无法独立追踪单个 HTTP 请求
- **建议修复**: 
  1. 在 StructuredLogEntry 添加 requestId 字段
  2. 在 HTTP 中间件中生成和注入 requestId
  3. 确保 requestId 流经所有日志调用

#### 2. [可观测性] [高严重] [StructuredLogger data 字段无自动清洗]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - 如果开发者传递 `{ password: "..." }` 给 logger.info，密码会被记录
  - 无字段级脱敏机制
  - 依赖开发者手动排除敏感字段
- **建议修复**: 
  1. 添加 OBSERVE_OUTPUT_BLACKLIST 类似的黑名单
  2. 实现 redact/mask/sanitize 工具函数
  3. 在日志传输前自动清洗敏感字段

#### 3. [可观测性] [中严重] [日志中敏感数据无 PII 处理]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - 无 PII 重写工具
  - 开发者必须手动避免记录敏感数据
  - 存在意外记录敏感信息的风险
- **建议修复**: 
  1. 添加 PII 检测和重写工具
  2. 在文档中明确记录敏感字段列表
  3. 添加测试验证敏感数据不被记录

#### 4. [可观测性] [中严重] [59处 console.* 直接调用]
- **文件/路径**: 多个源文件
- **问题描述**: 
  - 应该使用 StructuredLogger
  - console 调用可能不被日志聚合系统收集
  - 难以追踪和关联
- **建议修复**: 
  1. 替换所有 console.* 为 StructuredLogger
  2. 添加 ESLint 规则禁止 console.*
  3. 保留少量允许的 CLI 输出

#### 5. [可观测性] [中严重] [部分 logger.warn 调用缺少结构化数据]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:122`
- **问题描述**: 
  ```typescript
  logger.warn("WebSocket connection rejected: missing subprotocol token")
  ```
  - 只有消息，无 data 对象
  - 难以关联和查询
- **建议修复**: 
  1. 添加 `{ data: { ... } }` 对象
  2. 确保所有日志包含上下文数据
  3. 添加日志审查工具

#### 6. [源代码] [高严重] [Harness while(true) 循环无硬性迭代上限]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:1442`
- **问题描述**: 
  ```typescript
  while (true) {
    // ...
    // 依赖 budget gate 和 guardrail vibration 退出
    // 如果两者都失败，循环永久继续
  }
  ```
  - 依赖 budget gate 和 guardrail vibration 退出
  - 无硬性迭代次数限制
  - 可能导致无限循环
- **建议修复**: 
  1. 添加硬性 maxIterations 上限
  2. 在达到上限时强制退出
  3. 记录详细的迭代统计用于调试

#### 7. [源代码] [高严重] [Oapeflir while(true) 循环可能无限重规划]
- **文件/路径**: `src/platform/five-plane-orchestration/oapeflir/oapeflir-loop-service.ts:355`
- **问题描述**: 
  - 如果 loopReplanDecision.shouldReplan 永不变为 false
  - 重规划永远无法减少计划错误
  - 可能导致无限循环
- **建议修复**: 
  1. 添加重规划次数上限
  2. 在达到上限时强制接受当前结果
  3. 添加重规划质量衰减检测

#### 8. [源代码] [中严重] [Recovery 流程允许终态转换到 paused]
- **文件/路径**: `src/platform/five-plane-execution/runtime-state-machine.ts:105-108`
- **问题描述**: 
  - completed, failed, cancelled, aborted 可转换到 paused 进行恢复
  - 创建恢复路径可能重新进入执行
  - 需要验证此行为的正确性
- **建议修复**: 
  1. 审查 recovery 转换的合法性
  2. 确保不会导致状态不一致
  3. 添加转换前置条件验证

#### 9. [国际化] [高严重] [API 错误消息硬编码英文]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/api-error.ts`
- **问题描述**: 
  ```typescript
  return new ApiError(404, "api.task_not_found", "Task not found.");
  ```
  - 错误消息硬编码英文
  - 无后端 i18n 机制
  - 非英文客户端收到不本地化的错误
- **建议修复**: 
  1. 使用错误码而非硬编码消息
  2. 客户端根据错误码本地化
  3. 或在后端添加 i18n 支持

#### 10. [国际化] [中严重] [UI 硬编码字符串未翻译]
- **文件/路径**: `ui/packages/features/conversation/src/web/index.tsx`
- **问题描述**: 
  - "Build Plan", "Execute", "Messages" 等按钮标签硬编码
  - 不使用 i18n 系统
- **建议修复**: 
  1. 使用 `translateFeatureCopy` 替换硬编码
  2. 确保所有用户可见文本通过 i18n
  3. 添加硬编码字符串检测

#### 11. [国际化] [中严重] [Arabic 目录不完整]
- **文件/路径**: `ui/packages/shared/i18n/src/catalogs/ar-SA.ts`
- **问题描述**: 
  - ar-SA 只有约18条消息
  - en-US 和 zh-CN 有约117条
  - 缺失的 key 返回 key 本身
- **建议修复**: 
  1. 补充 Arabic 翻译
  2. 确保所有 key 都有翻译
  3. 添加翻译完整性测试

#### 12. [国际化] [低严重] [translateFeatureCopy 无回退默认值]
- **文件/路径**: `ui/packages/shared/i18n/`
- **问题描述**: 
  - 如果 feature ID 不在 catalog 中
  - 返回 undefined title/summary
  - 无合理回退
- **建议修复**: 
  1. 添加回退到 key 或默认字符串
  2. 或使用 feature ID 作为显示名
  3. 确保 UI 不显示 undefined

#### 13. [运维] [高严重] [健康检查无 readiness/liveness 区分]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts`
- **问题描述**: 
  - /healthz, /v1/healthz, /health 返回相同报告
  - 无 readiness probe（可处理流量？）
  - 无 liveness probe（系统存活？）
  - canAcceptTraffic() 未通过 HTTP 暴露
- **建议修复**: 
  1. 添加 /ready 和 /live 端点
  2. Readiness 检查依赖项就绪
  3. Liveness 检查进程健康

#### 14. [运维] [高严重] [shutdown 期间健康检查存在竞态]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts`
- **问题描述**: 
  - getHealthReportAsync() 异步检查 dbWritable
  - shutdown 期间数据库连接可能关闭
  - 健康检查仍在传入
- **建议修复**: 
  1. 在 shutdown 时返回 unhealthy
  2. 添加 shutdown 状态标志
  3. 停止接受新的健康检查请求

#### 15. [运维] [高严重] [LeaderElectionService 未与 GracefulShutdown 集成]
- **文件/路径**: `src/platform/five-plane-execution/ha/leader-election-service.ts`
- **问题描述**: 
  - LeaderElectionService.stop() 处理优雅退位
  - 但未在全局 graceful shutdown 中注册
  - 需显式注册为 shutdown handler
- **建议修复**: 
  1. 将 LeaderElectionService 添加到 shutdown handlers
  2. 确保领导者在 shutdown 时让位
  3. 验证关闭顺序正确

#### 16. [运维] [中严重] [addHandler() 无显式排序约定]
- **文件/路径**: `src/platform/five-plane-execution/startup/graceful-shutdown.ts`
- **问题描述**: 
  - 虽然 handlers 逆序执行
  - 但无显式接口声明 handler 依赖关系
  - 关键 handlers（如关闭数据库连接）应最后运行
- **建议修复**: 
  1. 添加优先级或阶段参数
  2. 文档化关键 handler 顺序要求
  3. 添加顺序验证测试

#### 17. [运维] [中严重] [unref'd timers 可能不触发]
- **文件/路径**: `src/platform/five-plane-execution/startup/process-error-handlers.ts:106`
- **问题描述**: 
  - `setTimeout(...).unref()` 不保持事件循环活跃
  - 如果主线程空闲，强制退出可能不触发
- **建议修复**: 
  1. 移除 .unref() 除非确实需要
  2. 或确保有其他方式保持事件循环
  3. 添加超时触发验证测试

#### 18. [运维] [中严重] [StartupConsistencyChecker 阻塞流量但不暴露状态]
- **文件/路径**: `src/platform/five-plane-startup-plan.ts`
- **问题描述**: 
  - canAcceptTraffic() 在 P0 问题存在时返回 false
  - 但 _trafficBlocked 是内部状态
  - 负载均衡器访问 /healthz 仍会得到 "ok"
- **建议修复**: 
  1. 在健康报告中暴露 trafficBlocked 状态
  2. 或添加单独的健康端点指示就绪状态
  3. 确保负载均衡器在阻塞时不路由流量

#### 19. [事件系统] [中严重] [TypedEventBus vs EventEmitter 混用]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - 项目同时使用 TypedEventBus 和原生 EventEmitter
  - TypedEventBus 应该是标准
  - 混用可能导致类型安全问题
- **建议修复**: 
  1. 创建迁移计划统一使用 TypedEventBus
  2. 添加 ESLint 规则禁止原生 EventEmitter
  3. 在 CI 中检测混用情况

#### 20. [源代码] [低严重] [无事件排序保证]
- **文件/路径**: `src/platform/five-plane-state-evidence/events/`
- **问题描述**: 
  - 无明确的事件排序保证文档
  - 事件处理可能乱序
  - 依赖方需要处理乱序
- **建议修复**: 
  1. 文档化事件排序语义
  2. 如果需要排序，实现序列号机制
  3. 在事件处理中考虑乱序情况

### 总结

本次补充Review（第十四轮 - 日志事件与工作流Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. StructuredLogEntry 无 requestId 字段
2. StructuredLogger data 字段无自动清洗（敏感数据风险）
3. Harness while(true) 循环无硬性迭代上限
4. Oapeflir while(true) 循环可能无限重规划
5. API 错误消息硬编码英文（无 i18n）
6. 健康检查无 readiness/liveness 区分
7. shutdown 期间健康检查存在竞态
8. LeaderElectionService 未与 GracefulShutdown 集成

**中优先级**:
1. 日志中敏感数据无 PII 处理
2. 59处 console.* 直接调用
3. 部分 logger.warn 调用缺少结构化数据
4. Recovery 流程允许终态转换到 paused
5. UI 硬编码字符串未翻译
6. Arabic 目录不完整
7. addHandler() 无显式排序约定
8. unref'd timers 可能不触发
9. StartupConsistencyChecker 阻塞流量但不暴露状态
10. TypedEventBus vs EventEmitter 混用

**低优先级**:
1. translateFeatureCopy 无回退默认值
2. 无事件排序保证

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 68 | 80 | 37 | 185 |
| 测试 | 8 | 14 | 5 | 27 |
| 配置 | 10 | 38 | 24 | 72 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **103** | **171** | **82** | **356** |

### 优先修复建议

**立即处理 (本周内)**:
1. 在 StructuredLogEntry 添加 requestId 字段
2. 实现日志敏感字段自动清洗
3. 为工作流循环添加硬性迭代上限
4. 添加 readiness/liveness 健康检查端点
5. 将 LeaderElectionService 集成到 GracefulShutdown
6. 实现 API 错误消息 i18n 支持

**短期内处理 (本月内)**:
1. 替换所有 console.* 为 StructuredLogger
2. 补充 Arabic i18n 目录
3. 修复 UI 硬编码字符串
4. 统一 TypedEventBus 替代 EventEmitter
5. 解决 shutdown 竞态条件
6. 添加事件排序保证文档

**长期规划**:
1. 建立完整的 PII 处理框架
2. 实现日志完整性验证
3. 建立工作流安全循环检测
4. 完善健康检查和就绪探测
5. 建立国际化完整测试

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十五轮 - 构建测试与类型系统Review）

### 发现的新问题

#### 1. [构建] [高严重] [无 TypeScript 增量编译缓存]
- **文件/路径**: `tsconfig.json`
- **问题描述**: 
  - 无 `incremental: true` 配置
  - 无 `tsbuildinfo` 文件
  - 每次构建是完整重建（9.6秒）
  - TypeScript 无法使用增量编译
- **建议修复**: 
  1. 在 tsconfig.json 添加 `"incremental": true`
  2. 添加 `"tsbuildinfo": ".tsbuildinfo"` 到 .gitignore
  3. 实现项目引用以支持并行构建

#### 2. [构建] [高严重] [76个 npm scripts 大部分重复构建]
- **文件/路径**: `package.json`
- **问题描述**: 
  - 40+ CLI scripts 都运行 `npm run build && node ...`
  - 每个脚本前都执行完整重建
  - 无法直接运行预构建的 CLI
  - 20+ stable:* 脚本遵循相同模式
- **建议修复**: 
  1. 创建 `build:cli` 脚本构建一次
  2. 让 CLI 脚本使用预构建的 dist 文件
  3. 添加 npm run dev 或类似的无需重建的执行方式

#### 3. [源代码] [高严重] [168处 any 类型使用]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - `stable-runner-factory.ts`: `opts: any` -> `any`
  - `billing-service-async.ts`: `input: any` 方法签名
  - 重试/超时工具使用 `(...args: any[]) => Promise<any>`
  - 绕过类型安全
- **建议修复**: 
  1. 使用泛型替代 any
  2. 替换为具体的输入类型
  3. 添加 ESLint 规则禁止 any

#### 4. [源代码] [高严重] [38处 @ts-ignore 指令]
- **文件/路径**: 多个文件
- **问题描述**: 
  - 14个 stability 相关文件有 `@ts-ignore ExecutionRecord type mismatch`
  - 5个 harness-sdk 文件有 `Partial<HarnessRun> doesn't have all required properties`
  - 2个文件有 `exactOptionalPropertyTypes` 问题
- **建议修复**: 
  1. 修复 ExecutionRecord 类型不匹配
  2. 完成 HarnessRun 类型的必需属性
  3. 移除 @ts-ignore 使用

#### 5. [源代码] [高严重] [ExecutionRecord 类型不匹配]
- **文件/路径**: `src/platform/stability/stable-evidence-bundle-support.ts:783` 等14个文件
- **问题描述**: 
  - 插入 execution record 时 TypeScript 拒绝类型
  - `ExecutionRecord` 接口要求新字段但 insert 对象缺失
  - 或 store 的 insert 方法接受不同类型
- **建议修复**: 
  1. 对齐 ExecutionRecord 接口和 store 类型
  2. 添加缺失字段到 insert 对象
  3. 或调整 store 的 insert 方法类型签名

#### 6. [源代码] [中严重] [30+处 as unknown as 双类型转换]
- **文件/路径**: `division-loader.ts`, `domain-baseline-catalog.ts`, `harness-sdk`
- **问题描述**: 
  - 完全绕过类型安全
  - 通常是类型架构问题的信号
- **建议修复**: 
  1. 修复上游类型问题
  2. 使用更具体的类型断言
  3. 重构类型层次结构

#### 7. [构建] [中严重] [rimraf  extraneous - 未在 package.json 声明]
- **文件/路径**: `node_modules/rimraf`
- **问题描述**: 
  - rimraf@6.1.3 存在于 node_modules 但未在 package.json 声明
  - 可能导致依赖问题
- **建议修复**: 
  1. 添加到 dependencies 或 devDependencies
  2. 或移除并使用其他清理方式

#### 8. [测试] [高严重] [178处直接 process.env 变更无抽象]
- **文件/路径**: `tests/unit/domains/registry/plugin-spi-registry.test.ts:640-687`
- **问题描述**: 
  - 直接读取/写入 process.env
  - 在 beforeEach/afterEach 中手动保存/恢复
  - 无 helper 抽象
- **建议修复**: 
  1. 创建 env helper 函数
  2. 统一保存和恢复模式
  3. 添加测试隔离验证

#### 9. [测试] [高严重] [21,998处 unlinkSync 调用无集中清理]
- **文件/路径**: 多个测试文件
- **问题描述**: 
  - 每个测试重新实现清理逻辑
  - 无集中清理 utility
  - 可能导致 EBUSY (Windows) 或并发访问问题
- **建议修复**: 
  1. 创建集中的测试清理 utility
  2. 使用 afterEach 确保清理
  3. 考虑使用临时目录

#### 10. [测试] [中严重] [无统一 mocking 框架]
- **文件/路径**: `tests/` 多个目录
- **问题描述**: 
  - 10,579 处 vi.fn, jest.mock, sinon 等调用
  - 无统一的 mocking 框架可见
  - 模式不一致
- **建议修复**: 
  1. 建立统一的 mock factory
  2. 标准化 mock 模式
  3. 添加 mocking 最佳实践文档

#### 11. [测试] [中严重] [Singleton 状态重置未标准化]
- **文件/路径**: `tests/unit/platform/five-plane-execution/dispatcher.test.ts`
- **问题描述**: 
  - 测试通过特定函数重置 singletons
  - 每个测试重复自己的 reset 逻辑
  - 无统一的 resetAllSingletons()
- **建议修复**: 
  1. 创建统一的 singleton reset 机制
  2. 在 afterEach 中自动调用
  3. 文档化 singleton 重置要求

#### 12. [测试] [低严重] [无分支覆盖率要求]
- **文件/路径**: `.c8rc.json`
- **问题描述**: 
  - `"100": false` - 无覆盖率要求
  - 只测量源代码，不测量测试文件
- **建议修复**: 
  1. 添加分支覆盖率阈值
  2. 在 CI 中强制执行覆盖率 gate
  3. 关注关键代码路径

#### 13. [网络] [高严重] [ScopedExternalAccessSandbox performHttpRequest 无超时]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/scoped-external-access-sandbox.ts:289`
- **问题描述**: 
  - fetch 调用无 timeout 参数
  - 挂起的请求可能永久阻塞
- **建议修复**: 
  1. 添加 AbortController timeout
  2. 使用标准的超时模式
  3. 记录超时配置

#### 14. [网络] [高严重] [OIDC 服务 fetch 调用无超时]
- **文件/路径**: `src/org-governance/sso-scim/oidc/oidc-service.ts:565,272`
- **问题描述**: 
  - OIDC token 交换无 retry logic
  - 无 timeout/abort signal
  - 认证流程可能挂起
- **建议修复**: 
  1. 添加 fetch timeout
  2. 添加 retry logic
  3. 使用标准的超时配置

#### 15. [网络] [高严重] [plugin-runtime-child 替换 globalThis.fetch 为 no-op]
- **文件/路径**: `src/domains/registry/plugin-runtime-child.ts:112`
- **问题描述**: 
  ```typescript
  globalThis.fetch = (async () => {}) as unknown as typeof globalThis.fetch;
  ```
  - 插件上下文中完全替换 fetch 为空函数
  - 插件的所有出站 HTTP 请求都会失败
  - 看起来是有意为之但可能造成问题
- **建议修复**: 
  1. 确认这是有意行为
  2. 文档化 fetch 替换行为
  3. 确保不会意外影响其他代码

#### 16. [网络] [中严重] [无显式 http.Agent 配置]
- **文件/路径**: 全局
- **问题描述**: 
  - 无显式的 http.Agent/https.Agent 配置
  - 依赖 Node.js 默认连接池
  - 无 maxSockets 或 maxConnections 配置
  - 高吞吐场景可能出问题
- **建议修复**: 
  1. 添加 http Agent 配置
  2. 文档化连接池大小
  3. 监控连接使用情况

#### 17. [序列化] [中严重] [合同版本硬编码为字符串字面量]
- **文件/路径**: `src/platform/contracts/inter-plane-contract-gateway.ts:184`
- **问题描述**: 
  - `schemaVersion: "v4.3"` 硬编码
  - 难以追踪合同版本演变
- **建议修复**: 
  1. 提取到类型化常量或 enum
  2. 集中管理合同版本
  3. 添加版本兼容性检查

#### 18. [序列化] [低严重] [Timestamp = string 丢失精度]
- **文件/路径**: `src/platform/contracts/types/domain/primitives.ts:10`
- **问题描述**: 
  - `type Timestamp = string` 无类型精度
  - ISO 8601 字符串无时区信息
- **建议修复**: 
  1. 使用 Date 对象或 branded string 类型
  2. 明确时区处理约定
  3. 文档化时间戳语义

#### 19. [源代码] [中严重] [29处非空断言 !.]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - 使用 `!.` 断言非空
  - 可能导致运行时错误
- **建议修复**: 
  1. 使用可选链 `?.` 或空值合并 `??`
  2. 添加适当的 null 检查
  3. 减少非空断言使用

#### 20. [API] [中严重] [API 路由 schema 版本硬编码]
- **文件/路径**: `src/platform/five-plane-interface/api/federation-routing-service.ts`
- **问题描述**: 
  - Zod schema 验证存在
  - 但 schema 版本未与 contract 版本同步
- **建议修复**: 
  1. 从 contract 版本派生 schema 版本
  2. 添加版本一致性检查
  3. 文档化版本关系

### 总结

本次补充Review（第十五轮 - 构建测试与类型系统Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. 无 TypeScript 增量编译缓存（每次完整重建9.6秒）
2. 76个 npm scripts 大部分重复构建
3. 168处 any 类型使用（绕过类型安全）
4. 38处 @ts-ignore 指令
5. ExecutionRecord 类型不匹配（14个文件）
6. 178处直接 process.env 变更无抽象
7. ScopedExternalAccessSandbox performHttpRequest 无超时
8. OIDC 服务 fetch 调用无超时
9. plugin-runtime-child 替换 globalThis.fetch 为 no-op

**中优先级**:
1. 30+处 as unknown as 双类型转换
2. rimraf extraneous 未声明
3. 21,998处 unlinkSync 无集中清理
4. 无统一 mocking 框架
5. Singleton 状态重置未标准化
6. 无显式 http.Agent 配置
7. 合同版本硬编码为字符串
8. Timestamp = string 丢失精度
9. 非空断言使用

**低优先级**:
1. 无分支覆盖率要求
2. API 路由 schema 版本未同步

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 72 | 87 | 39 | 198 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **109** | **182** | **85** | **376** |

### 优先修复建议

**立即处理 (本周内)**:
1. 添加 TypeScript 增量编译 (`incremental: true`)
2. 重构 npm scripts 避免重复构建
3. 减少 any 类型使用到 <50
4. 移除所有 @ts-ignore 指令
5. 修复 ExecutionRecord 类型不匹配
6. 添加 fetch 超时到 OIDC 和 sandbox

**短期内处理 (本月内)**:
1. 创建集中的测试清理 utility
2. 建立统一的 mocking 框架
3. 标准化 singleton 重置机制
4. 添加 http.Agent 连接池配置
5. 提取合同版本到类型化常量

**长期规划**:
1. 实现完整的类型安全（无 any）
2. 建立测试基础设施标准
3. 实现构建缓存优化
4. 完善网络层超时和重试
5. 建立覆盖率门禁标准

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十轮 - 深度代码质量与API一致性Review）

### 发现的新问题

#### 1. [源代码] [高严重] [sleepSync busy-wait 阻塞事件循环]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/repositories/authoritative-task-store-decorator.ts:50-57`
- **问题描述**: 
  - 发现 `sleepSync()` 函数使用 busy-wait 实现
  ```typescript
  function sleepSync(ms: number): void {
    const end = Date.now() + ms;
    while (Date.now() < end) { /* busy-wait */ }
  }
  ```
  - 在 SQLite BUSY 错误重试路径中使用
  - 阻塞事件循环，可能导致高负载下性能问题
- **建议修复**: 
  1. 使用 `setTimeout` 替代 busy-wait
  2. 或使用异步等待模式
  3. 确保重试逻辑不会阻塞主线程

#### 2. [源代码] [高严重] [TODO R4-27: HarnessRun 持久化缺失 - 数据丢失风险]
- **文件/路径**: `src/platform/five-plane-execution/execution-engine/single-task-happy-path.ts:270`
- **问题描述**: 
  - 注释 `# TODO R4-27: HarnessRun must be persisted to RuntimeTruthRepository`
  - 表明 HarnessRun 数据可能未持久化
  - 存在数据丢失风险
- **建议修复**: 
  1. 实现 HarnessRun 持久化逻辑
  2. 确保执行状态可恢复
  3. 在 TODO 完成前添加监控告警

#### 3. [源代码] [高严重] [SDK 与 Server 路由不匹配 - /handshake 和 /version 缺失]
- **文件/路径**: `src/sdk/client-sdk/api-client.ts` 及 `src/platform/five-plane-interface/api/`
- **问题描述**: 
  - SDK client 调用 `/handshake` 和 `/version` 端点
  - 但服务器端没有这些路由的处理程序
  - `SdkVersionHandshakeService` 存在但未连接到任何路由
- **建议修复**: 
  1. 实现 `/handshake` 和 `/version` 服务器路由
  2. 或从 SDK 中移除这些调用
  3. 确保 SDK 和 Server API 版本兼容

#### 4. [源代码] [高严重] [SDK URL 缺少 /api 前缀]
- **文件/路径**: `src/sdk/client-sdk/api-client.ts:84` vs 服务器路由
- **问题描述**: 
  - SDK 构建路径: `${baseUrl}/${apiVersion}/${path}` → `/v1/harness-runs`
  - 服务器路由: `/api/v1/harness-runs`
  - 缺少 `/api` 前缀导致 404 错误
- **建议修复**: 
  1. 在 SDK 的 `baseUrl` 中添加 `/api` 前缀
  2. 或移除服务器路由中的 `/api` 前缀
  3. 确保所有路由前缀一致

#### 5. [源代码] [高严重] [quant-trading.json 存在多种配置错误]
- **文件/路径**: `config/domains/quant-trading.json`
- **问题描述**: 
  - `capabilities.supportedTaskTypes` 包含 `["strategy_backtest", "pre_trade_risk_check"]`
  - 但 domain seed 定义的是 `["research", "simulate", "trade"]`
  - `workflowProfile.workflowId` 为 `"quant-trading.primary"` 但没有匹配的 workflow
  - `toolProfile.requiredTools` 包含 `order_execution` 但 `toolBundles` 中已禁用
- **建议修复**: 
  1. 修正 supportedTaskTypes 以匹配 seed 定义
  2. 添加缺失的 workflow 定义或修正引用
  3. 启用 `order_execution` 或从 requiredTools 中移除

#### 6. [源代码] [中严重] [SDK 与 Server 错误类别不一致]
- **文件/路径**: `src/sdk/client-sdk/api-client.ts` 和 `src/platform/five-plane-interface/api/http-server/api-error.ts`
- **问题描述**: 
  - SDK 映射: 4xx → BUSINESS, 400 为 BUSINESS
  - Server 映射: 400 → validation, 422 → business-rule
  - 同一 HTTP 状态码产生不同的错误类别
  - `PolicyDeniedError`, `TenantBoundaryError`, `WorkflowStateError` 等 Server 错误类型未暴露给 SDK
- **建议修复**: 
  1. 统一 SDK 和 Server 的错误类别映射
  2. 在 SDK 中添加缺失的错误类型处理
  3. 文档化错误类别层次结构

#### 7. [源代码] [中严重] [N+1 查询模式 - 重复调用相同查询]
- **文件/路径**: `src/platform/five-plane-state-evidence/events/cas/postgres-fencing-token-service.ts:73,79`
- **问题描述**: 
  - 同一查询被连续调用两次：
  ```typescript
  for (const fence of await repo.getFencesForExecution(executionId)) { ... }
  for (const fence of await repo.getFencesForExecution(executionId)) { ... }
  ```
  - 不必要的数据库往返
- **建议修复**: 
  1. 将结果缓存到变量中
  2. 合并两个循环为一个
  3. 添加缓存层避免重复查询

#### 8. [源代码] [中严重] [experience-cache-service 无分页 - 内存溢出风险]
- **文件/路径**: `src/platform/five-plane-state-evidence/memory/experience-cache-service.ts:337-424`
- **问题描述**: 
  - `findSimilarExperiences` 查询 `LIMIT 500` 行
  - 然后在内存中对所有500行进行评分和过滤
  - 无数据库级分页或游标
  - 可能导致高内存占用
- **建议修复**: 
  1. 实现游标分页
  2. 将评分逻辑下推到数据库
  3. 限制返回行数

#### 9. [源代码] [中严重] [无限制的内存 Map - 无驱逐策略]
- **文件/路径**: `src/platform/five-plane-state-evidence/memory/evidence-service.ts:129-133`
- **问题描述**: 
  ```typescript
  private readonly records: Map<string, EvidenceRecord> = new Map();
  private readonly CATEGORY_INDEX = new Map<EvidenceCategory, Set<string>>();
  private readonly SOURCE_REF_INDEX = new Map<string, Set<string>>();
  private readonly TENANT_INDEX = new Map<string, Set<string>>();
  private readonly STATUS_INDEX = new Map<EvidenceStatus, Set<string>>();
  ```
  - 无大小限制
  - 无驱逐策略
  - 可能导致内存泄漏
- **建议修复**: 
  1. 实现 LRU 或 TTL 驱逐策略
  2. 添加最大大小限制
  3. 监控内存使用情况

#### 10. [源代码] [中严重] [固定轮询间隔 - 雷电羊群风险]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/channel-gateway-retry-executor.ts:76-78`
- **问题描述**: 
  ```typescript
  this.intervalHandle = setInterval(() => {
    void this.runOnce();
  }, this.pollIntervalMs);
  ```
  - 固定间隔轮询无 jitter
  - 如果大量消息同时就绪，可能导致处理峰值
- **建议修复**: 
  1. 在轮询间隔添加随机 jitter
  2. 实现指数退避
  3. 或使用事件驱动而非轮询

#### 11. [源代码] [中严重] [VersionRoutingMiddleware 未连接到路由]
- **文件/路径**: `src/platform/five-plane-interface/api/middleware/version-routing.ts`
- **问题描述**: 
  - `VersionRoutingMiddleware` 存在但从未应用到任何路由
  - `supportedVersions: ["2026-04-01", "2026-01-01"]` 未生效
  - API 版本控制功能未启用
- **建议修复**: 
  1. 将 VersionRoutingMiddleware 连接到路由
  2. 实现版本协商逻辑
  3. 或移除未使用的中间件

#### 12. [源代码] [中严重] [marketing.json 缺少必需字段]
- **文件/路径**: `config/domains/marketing.json`
- **问题描述**: 
  - 缺少 `description` 字段
  - 缺少 `version` 字段
  - 缺少 `status` 字段
  - 缺少 `riskProfile` / `riskSpec` 对象
  - 缺少 `workflows` 数组（只有 `workflowProfile`）
  - 缺少 `toolBundles` 数组（只有 `toolProfile`）
  - 缺少 `outputContracts` 数组
  - 缺少 `capabilities` 对象
- **建议修复**: 
  1. 补充所有必需字段
  2. 与 quant-trading.json 保持结构一致
  3. 验证 JSON schema 合规性

#### 13. [源代码] [中严重] [21个 TODO/FIXME/HACK 注释未处理]
- **文件/路径**: 多个源文件
- **问题描述**: 
  - `src/org-governance/sso-scim/saml/index.ts:17` - TODO Phase 2 SAML production hardening
  - `src/platform/five-plane-interface/api/mission-control-service.ts:439` - TODO p50/p99 metrics
  - `src/platform/five-plane-execution/plugin-executor/adapter-executor.ts:184` - TODO Redis idempotency
  - 等共21个 TODO
- **建议修复**: 
  1. 逐个处理或创建 issue 跟踪
  2. 优先处理生产 hardening 相关的 TODO
  3. 设置代码质量标准要求处理所有 TODO

#### 14. [源代码] [中严重] [82处 any 类型使用]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - 82处 `: any`, `<any>`, 或 `as any`
  - 表明 TypeScript strict mode 未完全执行
  - 降低了类型安全性
- **建议修复**: 
  1. 启用 TypeScript strict mode
  2. 逐步替换 any 为具体类型
  3. 使用 unknown 替代 any

#### 15. [配置] [中严重] [连接池默认大小可能不足]
- **文件/路径**: `src/platform/five-plane-control-plane/config-center/postgres-pool-env.ts:58`
- **问题描述**: 
  - `poolMax` 默认为 10
  - 高吞吐场景可能不足
  - 无自动扩展配置
- **建议修复**: 
  1. 根据负载调整默认值
  2. 添加连接池自动调优
  3. 监控连接池使用情况

#### 16. [源代码] [中严重] [experience_cache 表无索引]
- **文件/路径**: `src/platform/five-plane-state-evidence/memory/experience-cache-service.ts`
- **问题描述**: 
  - `findSimilarExperiences` 查询使用 `session_id`, `quality_score`, `outcome`, `task_context`, `task_intent`
  - 但这些列可能没有数据库索引
  - 影响查询性能
- **建议修复**: 
  1. 在 experience_cache 表添加相应索引
  2. 使用 EXPLAIN 分析查询计划
  3. 优化索引策略

#### 17. [源代码] [低严重] [魔法数字散落各处]
- **文件/路径**: 多个源文件
- **问题描述**: 
  - `3600000` (1小时), `86400000` (24小时), `3600` (1小时秒)
  - `4001`, `4003` - WebSocket 关闭代码
  - `504` - HTTP 504
  - `4096` - stderr 截断缓冲区大小
- **建议修复**: 
  1. 使用 `src/platform/contracts/constants/time.ts` 中的常量
  2. 为其他魔法数字创建常量文件
  3. 添加 ESLint 规则禁止魔法数字

#### 18. [源代码] [低严重] [静默 catch 块吞掉错误]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:130` 等多处
- **问题描述**: 
  ```typescript
  } catch {
    ws.close(4003, "Invalid token");
    // Error is silently swallowed
  }
  ```
  - 错误被静默吞掉，无日志记录
  - 难以调试和追踪问题
- **建议修复**: 
  1. 在 catch 块中添加错误日志
  2. 使用 StructuredLogger 记录错误
  3. 考虑重新抛出或传播错误

#### 19. [测试] [中严重] [1059处 setTimeout/sleep/waitFor - 测试脆弱]
- **文件/路径**: `tests/` 多个测试文件
- **问题描述**: 
  - 1059个 setTimeout/sleep/waitFor 调用
  - `plugin-spi-registry.test.ts` 有自定义 waitFor() 250ms 超时
  - `bootstrap.test.ts` 使用 setTimeout(resolve, 500)
  - 在 CI 高负载下可能非确定性地失败
- **建议修复**: 
  1. 用 async/await 或 proper mocking 替代 sleep
  2. 增加超时时间或改用轮询条件
  3. 添加重试机制

#### 20. [测试] [中严重] [6个测试文件使用 // @ts-nocheck]
- **文件/路径**: `tests/unit/domains/recipes/recipe-executor.test.ts` 等
- **问题描述**: 
  - 整个文件禁用 TypeScript 类型检查
  - 隐藏真实的类型错误
  - 使重构变得危险
- **建议修复**: 
  1. 移除 // @ts-nocheck
  2. 修复类型错误
  3. 使用 // @ts-expect-error 替代单个错误

#### 21. [测试] [中严重] [3个集成测试因缺少 workflow_state 设置而跳过]
- **文件/路径**: `tests/integration/platform/five-plane-execution/budget-allocation.integration.test.ts`
- **问题描述**: 
  - 3个测试因缺少 `evaluateMultiDimensionalQuota` 和 `workflow_state` 记录而跳过
  - 关键预算分配功能未测试
- **建议修复**: 
  1. 设置测试所需的 workflow_state 记录
  2. 实现缺失的 evaluateMultiDimensionalQuota
  3. 确保测试环境完整性

#### 22. [测试] [低严重] [5个测试使用 .skip 标记]
- **文件/路径**: `tests/unit/scale-ecosystem/multi-region/cross-region-routing.test.ts` 等
- **问题描述**: 
  - `describe.skip` - 功能未实现
  - `test.skip()` - 缺少依赖
  - 已知失败但未解决
- **建议修复**: 
  1. 实现跳过的功能
  2. 设置测试依赖
  3. 或在 TODO 中跟踪

#### 23. [源代码] [低严重] [double throw 模式]
- **文件/路径**: `src/domains/domain-baseline-catalog.ts:546,550`
- **问题描述**: 
  - 在 baseline 未找到时连续抛出两次错误
  - 可能导致混淆的错误堆栈
- **建议修复**: 
  1. 审查并修正错误抛出逻辑
  2. 简化错误处理流程

#### 24. [源代码] [低严重] [user-operations.json 配置过简]
- **文件/路径**: `config/domains/user-operations.json`
- **问题描述**: 
  - 配置极简，无 workflows 或 contracts 定义
  - 可能无法正常工作
- **建议修复**: 
  1. 补充完整的领域配置
  2. 或确认这是有意为之

#### 25. [部署] [低严重] [CI Coverage gate 仅在 Node 22 运行]
- **文件/路径**: `.github/workflows/`
- **问题描述**: 
  - `coverage:gate` 仅在 Node 22 运行
  - Node 20 不检查覆盖率
  - 不同版本可能有不同的覆盖情况
- **建议修复**: 
  1. 在所有版本上运行 coverage gate
  2. 或在 PR 检查中明确说明

#### 26. [源代码] [低严重] [模块级 logger 实例可能不是最佳实践]
- **文件/路径**: `src/platform/five-plane-execution/execution-engine/single-task-happy-path.ts:69`
- **问题描述**: 
  ```typescript
  const logger = new StructuredLogger({ retentionLimit: 100 });
  ```
  - 模块级状态可能不是最佳实践
  - 大多数服务使用依赖注入
- **建议修复**: 
  1. 通过依赖注入传递 logger
  2. 或使用全局单例

### 总结

本次补充Review（第十轮 - 深度代码质量与API一致性Review - 2026-05-14）发现了26个新问题，重点关注代码质量和API一致性。

**高优先级 (需要立即处理)**:
1. sleepSync busy-wait 阻塞事件循环
2. TODO R4-27: HarnessRun 持久化缺失
3. SDK 与 Server 路由不匹配 (/handshake, /version 缺失)
4. SDK URL 缺少 /api 前缀
5. quant-trading.json 存在多种配置错误

**中优先级**:
1. SDK 与 Server 错误类别不一致
2. N+1 查询模式 (postgres-fencing-token-service)
3. experience-cache-service 无分页
4. 无限制的内存 Map
5. 固定轮询间隔无 jitter
6. VersionRoutingMiddleware 未连接
7. marketing.json 缺少必需字段
8. 21个 TODO/FIXME 注释未处理
9. 82处 any 类型使用
10. 连接池默认大小可能不足
11. experience_cache 表无索引
12. 1059处 setTimeout/sleep (测试脆弱)
13. 6个测试文件使用 @ts-nocheck
14. 3个集成测试跳过

**低优先级**:
1. 魔法数字散落各处
2. 静默 catch 块吞掉错误
3. 5个测试使用 .skip 标记
4. double throw 模式
5. user-operations.json 配置过简
6. CI coverage gate 仅在 Node 22
7. 模块级 logger 实例

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 51 | 57 | 26 | 134 |
| 测试 | 8 | 14 | 5 | 27 |
| 配置 | 8 | 29 | 18 | 55 |
| 安全 | 6 | 6 | 1 | 13 |
| 文档 | 2 | 7 | 3 | 12 |
| UI | 0 | 3 | 3 | 6 |
| 部署 | 0 | 8 | 5 | 13 |
| **合计** | **75** | **124** | **61** | **260** |

### 优先修复建议

**立即处理 (本周内)**:
1. 修复 SDK 与 Server 路由不匹配（/handshake, /version, /api前缀）
2. 实现 HarnessRun 持久化（TODO R4-27）
3. 修复 quant-trading.json 和 marketing.json 配置错误
4. 将 sleepSync 改为非阻塞实现
5. 统一 SDK 和 Server 错误类别映射

**短期内处理 (本月内)**:
1. 实现 N+1 查询修复和分页
2. 添加无界 Map 的驱逐策略
3. 添加轮询 jitter 防止 thundering herd
4. 处理所有 TODO/FIXME 注释
5. 移除测试文件中的 @ts-nocheck
6. 修复 workflow_state 集成测试

**长期规划**:
1. 启用 TypeScript strict mode
2. 完善 API 版本控制中间件
3. 添加 experience_cache 表索引
4. 统一所有魔法数字到常量
5. 建立代码质量门禁标准

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十五轮 - 构建测试与类型系统Review）

### 发现的新问题

#### 1. [构建] [高严重] [无 TypeScript 增量编译缓存]
- **文件/路径**: `tsconfig.json`
- **问题描述**: 
  - 无 `incremental: true` 配置
  - 无 `tsbuildinfo` 文件
  - 每次构建是完整重建（9.6秒）
  - TypeScript 无法使用增量编译
- **建议修复**: 
  1. 在 tsconfig.json 添加 `"incremental": true`
  2. 添加 `"tsbuildinfo": ".tsbuildinfo"` 到 .gitignore
  3. 实现项目引用以支持并行构建

#### 2. [构建] [高严重] [76个 npm scripts 大部分重复构建]
- **文件/路径**: `package.json`
- **问题描述**: 
  - 40+ CLI scripts 都运行 `npm run build && node ...`
  - 每个脚本前都执行完整重建
  - 无法直接运行预构建的 CLI
  - 20+ stable:* 脚本遵循相同模式
- **建议修复**: 
  1. 创建 `build:cli` 脚本构建一次
  2. 让 CLI 脚本使用预构建的 dist 文件
  3. 添加 npm run dev 或类似的无需重建的执行方式

#### 3. [源代码] [高严重] [168处 any 类型使用]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - `stable-runner-factory.ts`: `opts: any` -> `any`
  - `billing-service-async.ts`: `input: any` 方法签名
  - 重试/超时工具使用 `(...args: any[]) => Promise<any>`
  - 绕过类型安全
- **建议修复**: 
  1. 使用泛型替代 any
  2. 替换为具体的输入类型
  3. 添加 ESLint 规则禁止 any

#### 4. [源代码] [高严重] [38处 @ts-ignore 指令]
- **文件/路径**: 多个文件
- **问题描述**: 
  - 14个 stability 相关文件有 `@ts-ignore ExecutionRecord type mismatch`
  - 5个 harness-sdk 文件有 `Partial<HarnessRun> doesn't have all required properties`
  - 2个文件有 `exactOptionalPropertyTypes` 问题
- **建议修复**: 
  1. 修复 ExecutionRecord 类型不匹配
  2. 完成 HarnessRun 类型的必需属性
  3. 移除 @ts-ignore 使用

#### 5. [源代码] [高严重] [ExecutionRecord 类型不匹配]
- **文件/路径**: `src/platform/stability/stable-evidence-bundle-support.ts:783` 等14个文件
- **问题描述**: 
  - 插入 execution record 时 TypeScript 拒绝类型
  - `ExecutionRecord` 接口要求新字段但 insert 对象缺失
  - 或 store 的 insert 方法接受不同类型
- **建议修复**: 
  1. 对齐 ExecutionRecord 接口和 store 类型
  2. 添加缺失字段到 insert 对象
  3. 或调整 store 的 insert 方法类型签名

#### 6. [源代码] [中严重] [30+处 as unknown as 双类型转换]
- **文件/路径**: `division-loader.ts`, `domain-baseline-catalog.ts`, `harness-sdk`
- **问题描述**: 
  - 完全绕过类型安全
  - 通常是类型架构问题的信号
- **建议修复**: 
  1. 修复上游类型问题
  2. 使用更具体的类型断言
  3. 重构类型层次结构

#### 7. [构建] [中严重] [rimraf  extraneous - 未在 package.json 声明]
- **文件/路径**: `node_modules/rimraf`
- **问题描述**: 
  - rimraf@6.1.3 存在于 node_modules 但未在 package.json 声明
  - 可能导致依赖问题
- **建议修复**: 
  1. 添加到 dependencies 或 devDependencies
  2. 或移除并使用其他清理方式

#### 8. [测试] [高严重] [178处直接 process.env 变更无抽象]
- **文件/路径**: `tests/unit/domains/registry/plugin-spi-registry.test.ts:640-687`
- **问题描述**: 
  - 直接读取/写入 process.env
  - 在 beforeEach/afterEach 中手动保存/恢复
  - 无 helper 抽象
- **建议修复**: 
  1. 创建 env helper 函数
  2. 统一保存和恢复模式
  3. 添加测试隔离验证

#### 9. [测试] [高严重] [21,998处 unlinkSync 调用无集中清理]
- **文件/路径**: 多个测试文件
- **问题描述**: 
  - 每个测试重新实现清理逻辑
  - 无集中清理 utility
  - 可能导致 EBUSY (Windows) 或并发访问问题
- **建议修复**: 
  1. 创建集中的测试清理 utility
  2. 使用 afterEach 确保清理
  3. 考虑使用临时目录

#### 10. [测试] [中严重] [无统一 mocking 框架]
- **文件/路径**: `tests/` 多个目录
- **问题描述**: 
  - 10,579 处 vi.fn, jest.mock, sinon 等调用
  - 无统一的 mocking 框架可见
  - 模式不一致
- **建议修复**: 
  1. 建立统一的 mock factory
  2. 标准化 mock 模式
  3. 添加 mocking 最佳实践文档

#### 11. [测试] [中严重] [Singleton 状态重置未标准化]
- **文件/路径**: `tests/unit/platform/five-plane-execution/dispatcher.test.ts`
- **问题描述**: 
  - 测试通过特定函数重置 singletons
  - 每个测试重复自己的 reset 逻辑
  - 无统一的 resetAllSingletons()
- **建议修复**: 
  1. 创建统一的 singleton reset 机制
  2. 在 afterEach 中自动调用
  3. 文档化 singleton 重置要求

#### 12. [测试] [低严重] [无分支覆盖率要求]
- **文件/路径**: `.c8rc.json`
- **问题描述**: 
  - `"100": false` - 无覆盖率要求
  - 只测量源代码，不测量测试文件
- **建议修复**: 
  1. 添加分支覆盖率阈值
  2. 在 CI 中强制执行覆盖率 gate
  3. 关注关键代码路径

#### 13. [网络] [高严重] [ScopedExternalAccessSandbox performHttpRequest 无超时]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/scoped-external-access-sandbox.ts:289`
- **问题描述**: 
  - fetch 调用无 timeout 参数
  - 挂起的请求可能永久阻塞
- **建议修复**: 
  1. 添加 AbortController timeout
  2. 使用标准的超时模式
  3. 记录超时配置

#### 14. [网络] [高严重] [OIDC 服务 fetch 调用无超时]
- **文件/路径**: `src/org-governance/sso-scim/oidc/oidc-service.ts:565,272`
- **问题描述**: 
  - OIDC token 交换无 retry logic
  - 无 timeout/abort signal
  - 认证流程可能挂起
- **建议修复**: 
  1. 添加 fetch timeout
  2. 添加 retry logic
  3. 使用标准的超时配置

#### 15. [网络] [高严重] [plugin-runtime-child 替换 globalThis.fetch 为 no-op]
- **文件/路径**: `src/domains/registry/plugin-runtime-child.ts:112`
- **问题描述**: 
  ```typescript
  globalThis.fetch = (async () => {}) as unknown as typeof globalThis.fetch;
  ```
  - 插件上下文中完全替换 fetch 为空函数
  - 插件的所有出站 HTTP 请求都会失败
  - 看起来是有意为之但可能造成问题
- **建议修复**: 
  1. 确认这是有意行为
  2. 文档化 fetch 替换行为
  3. 确保不会意外影响其他代码

#### 16. [网络] [中严重] [无显式 http.Agent 配置]
- **文件/路径**: 全局
- **问题描述**: 
  - 无显式的 http.Agent/https.Agent 配置
  - 依赖 Node.js 默认连接池
  - 无 maxSockets 或 maxConnections 配置
  - 高吞吐场景可能出问题
- **建议修复**: 
  1. 添加 http Agent 配置
  2. 文档化连接池大小
  3. 监控连接使用情况

#### 17. [序列化] [中严重] [合同版本硬编码为字符串字面量]
- **文件/路径**: `src/platform/contracts/inter-plane-contract-gateway.ts:184`
- **问题描述**: 
  - `schemaVersion: "v4.3"` 硬编码
  - 难以追踪合同版本演变
- **建议修复**: 
  1. 提取到类型化常量或 enum
  2. 集中管理合同版本
  3. 添加版本兼容性检查

#### 18. [序列化] [低严重] [Timestamp = string 丢失精度]
- **文件/路径**: `src/platform/contracts/types/domain/primitives.ts:10`
- **问题描述**: 
  - `type Timestamp = string` 无类型精度
  - ISO 8601 字符串无时区信息
- **建议修复**: 
  1. 使用 Date 对象或 branded string 类型
  2. 明确时区处理约定
  3. 文档化时间戳语义

#### 19. [源代码] [中严重] [29处非空断言 !.]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - 使用 `!.` 断言非空
  - 可能导致运行时错误
- **建议修复**: 
  1. 使用可选链 `?.` 或空值合并 `??`
  2. 添加适当的 null 检查
  3. 减少非空断言使用

#### 20. [API] [中严重] [API 路由 schema 版本硬编码]
- **文件/路径**: `src/platform/five-plane-interface/api/federation-routing-service.ts`
- **问题描述**: 
  - Zod schema 验证存在
  - 但 schema 版本未与 contract 版本同步
- **建议修复**: 
  1. 从 contract 版本派生 schema 版本
  2. 添加版本一致性检查
  3. 文档化版本关系

### 总结

本次补充Review（第十五轮 - 构建测试与类型系统Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. 无 TypeScript 增量编译缓存（每次完整重建9.6秒）
2. 76个 npm scripts 大部分重复构建
3. 168处 any 类型使用（绕过类型安全）
4. 38处 @ts-ignore 指令
5. ExecutionRecord 类型不匹配（14个文件）
6. 178处直接 process.env 变更无抽象
7. ScopedExternalAccessSandbox performHttpRequest 无超时
8. OIDC 服务 fetch 调用无超时
9. plugin-runtime-child 替换 globalThis.fetch 为 no-op

**中优先级**:
1. 30+处 as unknown as 双类型转换
2. rimraf extraneous 未声明
3. 21,998处 unlinkSync 无集中清理
4. 无统一 mocking 框架
5. Singleton 状态重置未标准化
6. 无显式 http.Agent 配置
7. 合同版本硬编码为字符串
8. Timestamp = string 丢失精度
9. 非空断言使用

**低优先级**:
1. 无分支覆盖率要求
2. API 路由 schema 版本未同步

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 72 | 87 | 39 | 198 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **109** | **182** | **85** | **376** |

### 优先修复建议

**立即处理 (本周内)**:
1. 添加 TypeScript 增量编译 (`incremental: true`)
2. 重构 npm scripts 避免重复构建
3. 减少 any 类型使用到 <50
4. 移除所有 @ts-ignore 指令
5. 修复 ExecutionRecord 类型不匹配
6. 添加 fetch 超时到 OIDC 和 sandbox

**短期内处理 (本月内)**:
1. 创建集中的测试清理 utility
2. 建立统一的 mocking 框架
3. 标准化 singleton 重置机制
4. 添加 http.Agent 连接池配置
5. 提取合同版本到类型化常量

**长期规划**:
1. 实现完整的类型安全（无 any）
2. 建立测试基础设施标准
3. 实现构建缓存优化
4. 完善网络层超时和重试
5. 建立覆盖率门禁标准

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十四轮 - 日志事件与工作流Review）

### 发现的新问题

#### 1. [可观测性] [高严重] [StructuredLogEntry 无 requestId 字段]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - StructuredLogEntry 接口无 requestId 字段
  - 只有 correlationId，它回退到 traceId
  - 无法独立追踪单个 HTTP 请求
- **建议修复**: 
  1. 在 StructuredLogEntry 添加 requestId 字段
  2. 在 HTTP 中间件中生成和注入 requestId
  3. 确保 requestId 流经所有日志调用

#### 2. [可观测性] [高严重] [StructuredLogger data 字段无自动清洗]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - 如果开发者传递 `{ password: "..." }` 给 logger.info，密码会被记录
  - 无字段级脱敏机制
  - 依赖开发者手动排除敏感字段
- **建议修复**: 
  1. 添加 OBSERVE_OUTPUT_BLACKLIST 类似的黑名单
  2. 实现 redact/mask/sanitize 工具函数
  3. 在日志传输前自动清洗敏感字段

#### 3. [可观测性] [中严重] [日志中敏感数据无 PII 处理]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - 无 PII 重写工具
  - 开发者必须手动避免记录敏感数据
  - 存在意外记录敏感信息的风险
- **建议修复**: 
  1. 添加 PII 检测和重写工具
  2. 在文档中明确记录敏感字段列表
  3. 添加测试验证敏感数据不被记录

#### 4. [可观测性] [中严重] [59处 console.* 直接调用]
- **文件/路径**: 多个源文件
- **问题描述**: 
  - 应该使用 StructuredLogger
  - console 调用可能不被日志聚合系统收集
  - 难以追踪和关联
- **建议修复**: 
  1. 替换所有 console.* 为 StructuredLogger
  2. 添加 ESLint 规则禁止 console.*
  3. 保留少量允许的 CLI 输出

#### 5. [可观测性] [中严重] [部分 logger.warn 调用缺少结构化数据]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:122`
- **问题描述**: 
  ```typescript
  logger.warn("WebSocket connection rejected: missing subprotocol token")
  ```
  - 只有消息，无 data 对象
  - 难以关联和查询
- **建议修复**: 
  1. 添加 `{ data: { ... } }` 对象
  2. 确保所有日志包含上下文数据
  3. 添加日志审查工具

#### 6. [源代码] [高严重] [Harness while(true) 循环无硬性迭代上限]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:1442`
- **问题描述**: 
  ```typescript
  while (true) {
    // ...
    // 依赖 budget gate 和 guardrail vibration 退出
    // 如果两者都失败，循环永久继续
  }
  ```
  - 依赖 budget gate 和 guardrail vibration 退出
  - 无硬性迭代次数限制
  - 可能导致无限循环
- **建议修复**: 
  1. 添加硬性 maxIterations 上限
  2. 在达到上限时强制退出
  3. 记录详细的迭代统计用于调试

#### 7. [源代码] [高严重] [Oapeflir while(true) 循环可能无限重规划]
- **文件/路径**: `src/platform/five-plane-orchestration/oapeflir/oapeflir-loop-service.ts:355`
- **问题描述**: 
  - 如果 loopReplanDecision.shouldReplan 永不变为 false
  - 重规划永远无法减少计划错误
  - 可能导致无限循环
- **建议修复**: 
  1. 添加重规划次数上限
  2. 在达到上限时强制接受当前结果
  3. 添加重规划质量衰减检测

#### 8. [源代码] [中严重] [Recovery 流程允许终态转换到 paused]
- **文件/路径**: `src/platform/five-plane-execution/runtime-state-machine.ts:105-108`
- **问题描述**: 
  - completed, failed, cancelled, aborted 可转换到 paused 进行恢复
  - 创建恢复路径可能重新进入执行
  - 需要验证此行为的正确性
- **建议修复**: 
  1. 审查 recovery 转换的合法性
  2. 确保不会导致状态不一致
  3. 添加转换前置条件验证

#### 9. [国际化] [高严重] [API 错误消息硬编码英文]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/api-error.ts`
- **问题描述**: 
  ```typescript
  return new ApiError(404, "api.task_not_found", "Task not found.");
  ```
  - 错误消息硬编码英文
  - 无后端 i18n 机制
  - 非英文客户端收到不本地化的错误
- **建议修复**: 
  1. 使用错误码而非硬编码消息
  2. 客户端根据错误码本地化
  3. 或在后端添加 i18n 支持

#### 10. [国际化] [中严重] [UI 硬编码字符串未翻译]
- **文件/路径**: `ui/packages/features/conversation/src/web/index.tsx`
- **问题描述**: 
  - "Build Plan", "Execute", "Messages" 等按钮标签硬编码
  - 不使用 i18n 系统
- **建议修复**: 
  1. 使用 `translateFeatureCopy` 替换硬编码
  2. 确保所有用户可见文本通过 i18n
  3. 添加硬编码字符串检测

#### 11. [国际化] [中严重] [Arabic 目录不完整]
- **文件/路径**: `ui/packages/shared/i18n/src/catalogs/ar-SA.ts`
- **问题描述**: 
  - ar-SA 只有约18条消息
  - en-US 和 zh-CN 有约117条
  - 缺失的 key 返回 key 本身
- **建议修复**: 
  1. 补充 Arabic 翻译
  2. 确保所有 key 都有翻译
  3. 添加翻译完整性测试

#### 12. [国际化] [低严重] [translateFeatureCopy 无回退默认值]
- **文件/路径**: `ui/packages/shared/i18n/`
- **问题描述**: 
  - 如果 feature ID 不在 catalog 中
  - 返回 undefined title/summary
  - 无合理回退
- **建议修复**: 
  1. 添加回退到 key 或默认字符串
  2. 或使用 feature ID 作为显示名
  3. 确保 UI 不显示 undefined

#### 13. [运维] [高严重] [健康检查无 readiness/liveness 区分]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts`
- **问题描述**: 
  - /healthz, /v1/healthz, /health 返回相同报告
  - 无 readiness probe（可处理流量？）
  - 无 liveness probe（系统存活？）
  - canAcceptTraffic() 未通过 HTTP 暴露
- **建议修复**: 
  1. 添加 /ready 和 /live 端点
  2. Readiness 检查依赖项就绪
  3. Liveness 检查进程健康

#### 14. [运维] [高严重] [shutdown 期间健康检查存在竞态]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts`
- **问题描述**: 
  - getHealthReportAsync() 异步检查 dbWritable
  - shutdown 期间数据库连接可能关闭
  - 健康检查仍在传入
- **建议修复**: 
  1. 在 shutdown 时返回 unhealthy
  2. 添加 shutdown 状态标志
  3. 停止接受新的健康检查请求

#### 15. [运维] [高严重] [LeaderElectionService 未与 GracefulShutdown 集成]
- **文件/路径**: `src/platform/five-plane-execution/ha/leader-election-service.ts`
- **问题描述**: 
  - LeaderElectionService.stop() 处理优雅退位
  - 但未在全局 graceful shutdown 中注册
  - 需显式注册为 shutdown handler
- **建议修复**: 
  1. 将 LeaderElectionService 添加到 shutdown handlers
  2. 确保领导者在 shutdown 时让位
  3. 验证关闭顺序正确

#### 16. [运维] [中严重] [addHandler() 无显式排序约定]
- **文件/路径**: `src/platform/five-plane-execution/startup/graceful-shutdown.ts`
- **问题描述**: 
  - 虽然 handlers 逆序执行
  - 但无显式接口声明 handler 依赖关系
  - 关键 handlers（如关闭数据库连接）应最后运行
- **建议修复**: 
  1. 添加优先级或阶段参数
  2. 文档化关键 handler 顺序要求
  3. 添加顺序验证测试

#### 17. [运维] [中严重] [unref'd timers 可能不触发]
- **文件/路径**: `src/platform/five-plane-execution/startup/process-error-handlers.ts:106`
- **问题描述**: 
  - `setTimeout(...).unref()` 不保持事件循环活跃
  - 如果主线程空闲，强制退出可能不触发
- **建议修复**: 
  1. 移除 .unref() 除非确实需要
  2. 或确保有其他方式保持事件循环
  3. 添加超时触发验证测试

#### 18. [运维] [中严重] [StartupConsistencyChecker 阻塞流量但不暴露状态]
- **文件/路径**: `src/platform/five-plane-startup-plan.ts`
- **问题描述**: 
  - canAcceptTraffic() 在 P0 问题存在时返回 false
  - 但 _trafficBlocked 是内部状态
  - 负载均衡器访问 /healthz 仍会得到 "ok"
- **建议修复**: 
  1. 在健康报告中暴露 trafficBlocked 状态
  2. 或添加单独的健康端点指示就绪状态
  3. 确保负载均衡器在阻塞时不路由流量

#### 19. [事件系统] [中严重] [TypedEventBus vs EventEmitter 混用]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - 项目同时使用 TypedEventBus 和原生 EventEmitter
  - TypedEventBus 应该是标准
  - 混用可能导致类型安全问题
- **建议修复**: 
  1. 创建迁移计划统一使用 TypedEventBus
  2. 添加 ESLint 规则禁止原生 EventEmitter
  3. 在 CI 中检测混用情况

#### 20. [源代码] [低严重] [无事件排序保证]
- **文件/路径**: `src/platform/five-plane-state-evidence/events/`
- **问题描述**: 
  - 无明确的事件排序保证文档
  - 事件处理可能乱序
  - 依赖方需要处理乱序
- **建议修复**: 
  1. 文档化事件排序语义
  2. 如果需要排序，实现序列号机制
  3. 在事件处理中考虑乱序情况

### 总结

本次补充Review（第十四轮 - 日志事件与工作流Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. StructuredLogEntry 无 requestId 字段
2. StructuredLogger data 字段无自动清洗（敏感数据风险）
3. Harness while(true) 循环无硬性迭代上限
4. Oapeflir while(true) 循环可能无限重规划
5. API 错误消息硬编码英文（无 i18n）
6. 健康检查无 readiness/liveness 区分
7. shutdown 期间健康检查存在竞态
8. LeaderElectionService 未与 GracefulShutdown 集成

**中优先级**:
1. 日志中敏感数据无 PII 处理
2. 59处 console.* 直接调用
3. 部分 logger.warn 调用缺少结构化数据
4. Recovery 流程允许终态转换到 paused
5. UI 硬编码字符串未翻译
6. Arabic 目录不完整
7. addHandler() 无显式排序约定
8. unref'd timers 可能不触发
9. StartupConsistencyChecker 阻塞流量但不暴露状态
10. TypedEventBus vs EventEmitter 混用

**低优先级**:
1. translateFeatureCopy 无回退默认值
2. 无事件排序保证

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 68 | 80 | 37 | 185 |
| 测试 | 8 | 14 | 5 | 27 |
| 配置 | 10 | 38 | 24 | 72 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **103** | **171** | **82** | **356** |

### 优先修复建议

**立即处理 (本周内)**:
1. 在 StructuredLogEntry 添加 requestId 字段
2. 实现日志敏感字段自动清洗
3. 为工作流循环添加硬性迭代上限
4. 添加 readiness/liveness 健康检查端点
5. 将 LeaderElectionService 集成到 GracefulShutdown
6. 实现 API 错误消息 i18n 支持

**短期内处理 (本月内)**:
1. 替换所有 console.* 为 StructuredLogger
2. 补充 Arabic i18n 目录
3. 修复 UI 硬编码字符串
4. 统一 TypedEventBus 替代 EventEmitter
5. 解决 shutdown 竞态条件
6. 添加事件排序保证文档

**长期规划**:
1. 建立完整的 PII 处理框架
2. 实现日志完整性验证
3. 建立工作流安全循环检测
4. 完善健康检查和就绪探测
5. 建立国际化完整测试

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十五轮 - 构建测试与类型系统Review）

### 发现的新问题

#### 1. [构建] [高严重] [无 TypeScript 增量编译缓存]
- **文件/路径**: `tsconfig.json`
- **问题描述**: 
  - 无 `incremental: true` 配置
  - 无 `tsbuildinfo` 文件
  - 每次构建是完整重建（9.6秒）
  - TypeScript 无法使用增量编译
- **建议修复**: 
  1. 在 tsconfig.json 添加 `"incremental": true`
  2. 添加 `"tsbuildinfo": ".tsbuildinfo"` 到 .gitignore
  3. 实现项目引用以支持并行构建

#### 2. [构建] [高严重] [76个 npm scripts 大部分重复构建]
- **文件/路径**: `package.json`
- **问题描述**: 
  - 40+ CLI scripts 都运行 `npm run build && node ...`
  - 每个脚本前都执行完整重建
  - 无法直接运行预构建的 CLI
  - 20+ stable:* 脚本遵循相同模式
- **建议修复**: 
  1. 创建 `build:cli` 脚本构建一次
  2. 让 CLI 脚本使用预构建的 dist 文件
  3. 添加 npm run dev 或类似的无需重建的执行方式

#### 3. [源代码] [高严重] [168处 any 类型使用]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - `stable-runner-factory.ts`: `opts: any` -> `any`
  - `billing-service-async.ts`: `input: any` 方法签名
  - 重试/超时工具使用 `(...args: any[]) => Promise<any>`
  - 绕过类型安全
- **建议修复**: 
  1. 使用泛型替代 any
  2. 替换为具体的输入类型
  3. 添加 ESLint 规则禁止 any

#### 4. [源代码] [高严重] [38处 @ts-ignore 指令]
- **文件/路径**: 多个文件
- **问题描述**: 
  - 14个 stability 相关文件有 `@ts-ignore ExecutionRecord type mismatch`
  - 5个 harness-sdk 文件有 `Partial<HarnessRun> doesn't have all required properties`
  - 2个文件有 `exactOptionalPropertyTypes` 问题
- **建议修复**: 
  1. 修复 ExecutionRecord 类型不匹配
  2. 完成 HarnessRun 类型的必需属性
  3. 移除 @ts-ignore 使用

#### 5. [源代码] [高严重] [ExecutionRecord 类型不匹配]
- **文件/路径**: `src/platform/stability/stable-evidence-bundle-support.ts:783` 等14个文件
- **问题描述**: 
  - 插入 execution record 时 TypeScript 拒绝类型
  - `ExecutionRecord` 接口要求新字段但 insert 对象缺失
  - 或 store 的 insert 方法接受不同类型
- **建议修复**: 
  1. 对齐 ExecutionRecord 接口和 store 类型
  2. 添加缺失字段到 insert 对象
  3. 或调整 store 的 insert 方法类型签名

#### 6. [源代码] [中严重] [30+处 as unknown as 双类型转换]
- **文件/路径**: `division-loader.ts`, `domain-baseline-catalog.ts`, `harness-sdk`
- **问题描述**: 
  - 完全绕过类型安全
  - 通常是类型架构问题的信号
- **建议修复**: 
  1. 修复上游类型问题
  2. 使用更具体的类型断言
  3. 重构类型层次结构

#### 7. [构建] [中严重] [rimraf  extraneous - 未在 package.json 声明]
- **文件/路径**: `node_modules/rimraf`
- **问题描述**: 
  - rimraf@6.1.3 存在于 node_modules 但未在 package.json 声明
  - 可能导致依赖问题
- **建议修复**: 
  1. 添加到 dependencies 或 devDependencies
  2. 或移除并使用其他清理方式

#### 8. [测试] [高严重] [178处直接 process.env 变更无抽象]
- **文件/路径**: `tests/unit/domains/registry/plugin-spi-registry.test.ts:640-687`
- **问题描述**: 
  - 直接读取/写入 process.env
  - 在 beforeEach/afterEach 中手动保存/恢复
  - 无 helper 抽象
- **建议修复**: 
  1. 创建 env helper 函数
  2. 统一保存和恢复模式
  3. 添加测试隔离验证

#### 9. [测试] [高严重] [21,998处 unlinkSync 调用无集中清理]
- **文件/路径**: 多个测试文件
- **问题描述**: 
  - 每个测试重新实现清理逻辑
  - 无集中清理 utility
  - 可能导致 EBUSY (Windows) 或并发访问问题
- **建议修复**: 
  1. 创建集中的测试清理 utility
  2. 使用 afterEach 确保清理
  3. 考虑使用临时目录

#### 10. [测试] [中严重] [无统一 mocking 框架]
- **文件/路径**: `tests/` 多个目录
- **问题描述**: 
  - 10,579 处 vi.fn, jest.mock, sinon 等调用
  - 无统一的 mocking 框架可见
  - 模式不一致
- **建议修复**: 
  1. 建立统一的 mock factory
  2. 标准化 mock 模式
  3. 添加 mocking 最佳实践文档

#### 11. [测试] [中严重] [Singleton 状态重置未标准化]
- **文件/路径**: `tests/unit/platform/five-plane-execution/dispatcher.test.ts`
- **问题描述**: 
  - 测试通过特定函数重置 singletons
  - 每个测试重复自己的 reset 逻辑
  - 无统一的 resetAllSingletons()
- **建议修复**: 
  1. 创建统一的 singleton reset 机制
  2. 在 afterEach 中自动调用
  3. 文档化 singleton 重置要求

#### 12. [测试] [低严重] [无分支覆盖率要求]
- **文件/路径**: `.c8rc.json`
- **问题描述**: 
  - `"100": false` - 无覆盖率要求
  - 只测量源代码，不测量测试文件
- **建议修复**: 
  1. 添加分支覆盖率阈值
  2. 在 CI 中强制执行覆盖率 gate
  3. 关注关键代码路径

#### 13. [网络] [高严重] [ScopedExternalAccessSandbox performHttpRequest 无超时]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/scoped-external-access-sandbox.ts:289`
- **问题描述**: 
  - fetch 调用无 timeout 参数
  - 挂起的请求可能永久阻塞
- **建议修复**: 
  1. 添加 AbortController timeout
  2. 使用标准的超时模式
  3. 记录超时配置

#### 14. [网络] [高严重] [OIDC 服务 fetch 调用无超时]
- **文件/路径**: `src/org-governance/sso-scim/oidc/oidc-service.ts:565,272`
- **问题描述**: 
  - OIDC token 交换无 retry logic
  - 无 timeout/abort signal
  - 认证流程可能挂起
- **建议修复**: 
  1. 添加 fetch timeout
  2. 添加 retry logic
  3. 使用标准的超时配置

#### 15. [网络] [高严重] [plugin-runtime-child 替换 globalThis.fetch 为 no-op]
- **文件/路径**: `src/domains/registry/plugin-runtime-child.ts:112`
- **问题描述**: 
  ```typescript
  globalThis.fetch = (async () => {}) as unknown as typeof globalThis.fetch;
  ```
  - 插件上下文中完全替换 fetch 为空函数
  - 插件的所有出站 HTTP 请求都会失败
  - 看起来是有意为之但可能造成问题
- **建议修复**: 
  1. 确认这是有意行为
  2. 文档化 fetch 替换行为
  3. 确保不会意外影响其他代码

#### 16. [网络] [中严重] [无显式 http.Agent 配置]
- **文件/路径**: 全局
- **问题描述**: 
  - 无显式的 http.Agent/https.Agent 配置
  - 依赖 Node.js 默认连接池
  - 无 maxSockets 或 maxConnections 配置
  - 高吞吐场景可能出问题
- **建议修复**: 
  1. 添加 http Agent 配置
  2. 文档化连接池大小
  3. 监控连接使用情况

#### 17. [序列化] [中严重] [合同版本硬编码为字符串字面量]
- **文件/路径**: `src/platform/contracts/inter-plane-contract-gateway.ts:184`
- **问题描述**: 
  - `schemaVersion: "v4.3"` 硬编码
  - 难以追踪合同版本演变
- **建议修复**: 
  1. 提取到类型化常量或 enum
  2. 集中管理合同版本
  3. 添加版本兼容性检查

#### 18. [序列化] [低严重] [Timestamp = string 丢失精度]
- **文件/路径**: `src/platform/contracts/types/domain/primitives.ts:10`
- **问题描述**: 
  - `type Timestamp = string` 无类型精度
  - ISO 8601 字符串无时区信息
- **建议修复**: 
  1. 使用 Date 对象或 branded string 类型
  2. 明确时区处理约定
  3. 文档化时间戳语义

#### 19. [源代码] [中严重] [29处非空断言 !.]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - 使用 `!.` 断言非空
  - 可能导致运行时错误
- **建议修复**: 
  1. 使用可选链 `?.` 或空值合并 `??`
  2. 添加适当的 null 检查
  3. 减少非空断言使用

#### 20. [API] [中严重] [API 路由 schema 版本硬编码]
- **文件/路径**: `src/platform/five-plane-interface/api/federation-routing-service.ts`
- **问题描述**: 
  - Zod schema 验证存在
  - 但 schema 版本未与 contract 版本同步
- **建议修复**: 
  1. 从 contract 版本派生 schema 版本
  2. 添加版本一致性检查
  3. 文档化版本关系

### 总结

本次补充Review（第十五轮 - 构建测试与类型系统Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. 无 TypeScript 增量编译缓存（每次完整重建9.6秒）
2. 76个 npm scripts 大部分重复构建
3. 168处 any 类型使用（绕过类型安全）
4. 38处 @ts-ignore 指令
5. ExecutionRecord 类型不匹配（14个文件）
6. 178处直接 process.env 变更无抽象
7. ScopedExternalAccessSandbox performHttpRequest 无超时
8. OIDC 服务 fetch 调用无超时
9. plugin-runtime-child 替换 globalThis.fetch 为 no-op

**中优先级**:
1. 30+处 as unknown as 双类型转换
2. rimraf extraneous 未声明
3. 21,998处 unlinkSync 无集中清理
4. 无统一 mocking 框架
5. Singleton 状态重置未标准化
6. 无显式 http.Agent 配置
7. 合同版本硬编码为字符串
8. Timestamp = string 丢失精度
9. 非空断言使用

**低优先级**:
1. 无分支覆盖率要求
2. API 路由 schema 版本未同步

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 72 | 87 | 39 | 198 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **109** | **182** | **85** | **376** |

### 优先修复建议

**立即处理 (本周内)**:
1. 添加 TypeScript 增量编译 (`incremental: true`)
2. 重构 npm scripts 避免重复构建
3. 减少 any 类型使用到 <50
4. 移除所有 @ts-ignore 指令
5. 修复 ExecutionRecord 类型不匹配
6. 添加 fetch 超时到 OIDC 和 sandbox

**短期内处理 (本月内)**:
1. 创建集中的测试清理 utility
2. 建立统一的 mocking 框架
3. 标准化 singleton 重置机制
4. 添加 http.Agent 连接池配置
5. 提取合同版本到类型化常量

**长期规划**:
1. 实现完整的类型安全（无 any）
2. 建立测试基础设施标准
3. 实现构建缓存优化
4. 完善网络层超时和重试
5. 建立覆盖率门禁标准

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十三轮 - 缓存会话与实时通信Review）

### 发现的新问题

#### 1. [安全] [严重] [CORS allowedMethods 缺少 PUT/PATCH/DELETE]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/response-hardening.ts:12-14`
- **问题描述**: 
  ```typescript
  export const DEFAULT_CORS_CONFIG: CorsConfig = {
    allowedMethods: ["GET", "POST", "OPTIONS"],  // 缺少 PUT, PATCH, DELETE
  ```
  - 浏览器 CORS preflight 会拒绝 PUT/PATCH/DELETE 请求
  - 但路由中有 PATCH /v1/tasks/:id, DELETE endpoints
- **建议修复**: 
  1. 添加 PUT, PATCH, DELETE 到 allowedMethods
  2. 确保 CORS 配置与应用路由方法一致
  3. 添加测试验证所有 HTTP 方法的 CORS preflight

#### 2. [安全] [严重] [Mission Routes 错误响应包装不当]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/mission-routes.ts:101,118-124`
- **问题描述**: 
  ```typescript
  return buildJsonResponse(ctx.requestId, 404, { error: { code: "MISSION_NOT_FOUND" } });
  ```
  - 使用 buildJsonResponse（成功包装）返回错误
  - 错误被包装在成功信封 `{requestId, data: {error: {...}}}` 中
  - 违反 REST 规范
- **建议修复**: 
  1. 使用 buildJsonErrorResponse 处理错误
  2. 确保所有错误路径使用正确的错误信封
  3. 添加 API 一致性测试

#### 3. [源代码] [高严重] [WebSocketBridge 无最大连接数限制]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:103-107`
- **问题描述**: 
  ```typescript
  this.wss = new WebSocketServer({
    server,
    path: WS_PATH,
    maxPayload: options.maxPayloadBytes ?? DEFAULT_MAX_PAYLOAD_BYTES,
    // 无 maxConnections 限制！
  });
  ```
  - 与 dashboard-websocket-server.ts 的 maxClients: 1000 不同
  - 恶意客户端可耗尽服务器资源
- **建议修复**: 
  1. 添加 maxConnections 限制
  2. 实现连接限制拒绝策略
  3. 添加连接计数指标

#### 4. [源代码] [高严重] [pendingAcks 在断开连接时未清理]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:268-282`
- **问题描述**: 
  - handleDisconnection 清理 subscribedTasks, taskSubscribers, slowConsumers
  - 但 pendingAcks Map 未清理
  - 断开的客户端未确认的消息永久留在内存
- **建议修复**: 
  1. 在 handleDisconnection 中删除所有 pendingAcks 条目
  2. 考虑添加超时机制自动清理未确认消息
  3. 记录清理操作的审计日志

#### 5. [安全] [高严重] [硬编码回退密钥]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/audit-event-integrity.ts:41`
- **问题描述**: 
  ```typescript
  const AUDIT_INTEGRITY_HMAC_KEY = process.env["AA_AUDIT_INTEGRITY_HMAC_KEY"] ?? "audit-integrity-secret-key-32-bytes!";
  ```
  - 占位符密钥用作回退
  - 如果环境变量未设置，使用不安全密钥
- **建议修复**: 
  1. 移除回退值，要求环境变量必须设置
  2. 在启动时验证密钥存在且足够强
  3. 如果密钥缺失导致启动失败

#### 6. [配置] [高严重] [config/runtime/test.json 缺少超时配置]
- **文件/路径**: `config/runtime/test.json`
- **问题描述**: 
  - 只有3个字段，缺少 maxConcurrentTasks, defaultTaskTimeoutMs, defaultStepTimeoutMs
  - 回退到默认值可能不适合测试环境
- **建议修复**: 
  1. 添加完整的 timeout/tuning 配置
  2. 确保测试环境有合理的默认值
  3. 文档化必需的配置字段

#### 7. [配置] [高严重] [prod.json 限制过严 - maxConcurrentTasks=1]
- **文件/路径**: `config/runtime/prod.json`
- **问题描述**: 
  - maxConcurrentTasks: 1（vs dev:8, staging:4, pre-prod:6）
  - defaultTaskTimeoutMs: 120000（vs dev:600000）
  - defaultStepTimeoutMs: 60000（vs dev:180000）
  - 生产环境严重受限，可能影响吞吐量
- **建议修复**: 
  1. 评估并调整生产限制
  2. 与业务需求匹配
  3. 添加生产容量测试

#### 8. [源代码] [高严重] [多个服务存在无界 Map 无驱逐策略]
- **文件/路径**: 多个域服务
- **问题描述**: 
  - `domain-knowledge-schema-service.ts:73-75`: schemas, sourceContent, sourceTimestamps 无限制
  - `domain-eval-framework-service.ts:91-95`: frameworks, qualityAxes 等无限制
  - `domain-recipe-service.ts:67-68`: recipes, versions 无限制
  - `domain-risk-profile-service.ts:51`: profiles 无限制
  - Session Maps (session-management.ts:83-89) 无自动清理
- **建议修复**: 
  1. 为所有 Map 实现 LRU 或 TTL 驱逐策略
  2. 添加后台清理任务
  3. 添加大小监控和告警

#### 9. [源代码] [中严重] [taskEventHistory Map 永不清理]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:91,548-555`
- **问题描述**: 
  - taskEventHistory 限制单任务历史为 200 条
  - 但 Map 本身从不清理
  - 取消订阅的任务历史永久保留
- **建议修复**: 
  1. 实现后台清理无订阅者任务的历史
  2. 添加任务订阅者监控
  3. 考虑使用 WeakMap 替代

#### 10. [配置] [中严重] [安全配置 drift - remoteWorkerRegistration 缺失]
- **文件/路径**: `config/security/dev.json`, `staging.json`, `pre-prod.json`
- **问题描述**: 
  - default.json 有完整的 remoteWorkerRegistration 配置
  - dev/staging/pre-prod 只有 approvalMode
  - prod 有 approvalMode 但无 remoteWorkerRegistration
  - 安全配置不一致
- **建议修复**: 
  1. 统一所有环境的 remoteWorkerRegistration
  2. 添加安全配置验证
  3. 确保最低安全基线

#### 11. [API] [中严重] [OpenAPI 端点响应格式不一致]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/utils.ts:207-215`
- **问题描述**: 
  - `/v1/openapi.json` 使用 buildJsonDocumentResponse（无包装）
  - 其他端点使用 buildJsonResponse（{requestId, data} 包装）
  - 客户端体验不一致
- **建议修复**: 
  1. 统一响应信封格式
  2. 文档化响应格式规范
  3. 添加响应格式验证测试

#### 12. [源代码] [中严重] [Redis 队列缺少 idempotency 索引支持]
- **文件/路径**: `src/platform/five-plane-execution/queue/redis-queue-adapter.ts`
- **问题描述**: 
  - SQLite 有部分唯一索引支持 idempotency
  - Redis 实现使用哈希索引但实现不完整
  - 可能导致重复消息
- **建议修复**: 
  1. 完善 Redis idempotency 实现
  2. 添加唯一索引验证
  3. 确保与 SQLite 行为一致

#### 13. [源代码] [中严重] [请求去重中间件使用内存存储]
- **文件/路径**: `src/platform/five-plane-interface/api/middleware/request-deduplication.ts`
- **问题描述**: 
  - 纯内存滑动窗口（Map<DeduplicationKey, DeduplicationEntry[]>）
  - 无外部存储
  - 多实例部署不共享
  - 重启后去重状态丢失
- **建议修复**: 
  1. 使用 Redis 替代内存存储
  2. 支持分布式去重
  3. 持久化去重状态

#### 14. [源代码] [中严重] [Cache 无 stampede 保护]
- **文件/路径**: 多个 cache 实现
- **问题描述**: 
  - MemoryCacheStore, ExperienceCacheService 无锁
  - 缓存未命中时可发生 thundering herd
  - 高并发下可能导致数据库过载
- **建议修复**: 
  1. 实现 single-flight 模式
  2. 添加请求排队机制
  3. 使用分布式锁保护缓存更新

#### 15. [源代码] [中严重] [EvidenceService eviction 仅在插入时触发]
- **文件/路径**: `src/platform/five-plane-state-evidence/memory/evidence-service.ts:202`
- **问题描述**: 
  - 清理仅在 record() 调用时触发
  - 空闲时无后台清理
  - 可能导致内存持续增长
- **建议修复**: 
  1. 添加定期后台清理任务
  2. 使用独立的清理线程
  3. 添加内存使用监控

#### 16. [源代码] [低严重] [无连接指标暴露]
- **文件/路径**: WebSocket 服务器
- **问题描述**: 
  - getClientCount() 存在但未通过 HTTP 端点暴露
  - 无 pendingAcks 队列深度指标
  - 无连接建立告警
- **建议修复**: 
  1. 通过 metrics 端点暴露连接指标
  2. 添加 pendingAcks 队列监控
  3. 添加连接数异常告警

#### 17. [源代码] [低严重] [无空闲客户端超时]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts`
- **问题描述**: 
  - 仅在心跳 Sweep 时检查 isAlive
  - 无 per-client 独立于心跳的空闲超时
  - 认证后从不发送消息的客户端只能通过心跳失败检测
- **建议修复**: 
  1. 添加 per-client idle timeout
  2. 独立于心跳间隔
  3. 配置可调整

#### 18. [源代码] [低严重] [订阅限制是 per-client 而非全局]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:244`
- **问题描述**: 
  - MAX_SUBSCRIPTIONS_PER_CLIENT = 100
  - 恶意客户端可订阅 100 个任务
  - 无全局任务订阅者限制
- **建议修复**: 
  1. 添加全局任务订阅者限制
  2. 实现 per-task 订阅者上限
  3. 添加反滥用检测

#### 19. [配置] [低严重] [config/runtime 无版本验证机制]
- **文件/路径**: `config/runtime/*.json`
- **问题描述**: 
  - 配置有 "version": "v4.3"
  - 但无 schema 版本验证
  - 加载时可能接受不兼容配置
- **建议修复**: 
  1. 添加 JSON Schema 验证
  2. 实现版本兼容性检查
  3. 启动时验证配置完整性

#### 20. [源代码] [低严重] [ImprovementCandidateRegistry splice 是 O(n)]
- **文件/路径**: `src/platform/five-plane-orchestration/improve/improvement-candidate-registry.ts:243`
- **问题描述**: 
  - accessOrder 数组的 splice 操作是 O(n)
  - 高频访问时可能性能问题
- **建议修复**: 
  1. 使用 LinkedList 替代数组
  2. 或使用 Map 维护访问顺序
  3. 性能测试验证

### 总结

本次补充Review（第十三轮 - 缓存会话与实时通信Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. CORS allowedMethods 缺少 PUT/PATCH/DELETE（浏览器请求会失败）
2. Mission Routes 错误响应包装不当
3. WebSocketBridge 无最大连接数限制（DoS风险）
4. pendingAcks 断开连接时未清理（内存泄漏）
5. 硬编码回退密钥（安全漏洞）
6. config/runtime/test.json 缺少超时配置
7. prod.json maxConcurrentTasks=1 限制过严
8. 多个服务存在无界 Map（内存泄漏风险）

**中优先级**:
1. taskEventHistory Map 永不清理
2. 安全配置 drift（remoteWorkerRegistration 缺失）
3. OpenAPI 端点响应格式不一致
4. Redis 队列 idempotency 实现不完整
5. 请求去重中间件使用内存存储（多实例不共享）
6. Cache 无 stampede 保护
7. EvidenceService eviction 仅在插入时触发

**低优先级**:
1. 无连接指标暴露
2. 无空闲客户端超时
3. 订阅限制是 per-client 而非全局
4. config/runtime 无版本验证机制
5. ImprovementCandidateRegistry splice O(n)

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 64 | 74 | 35 | 173 |
| 测试 | 8 | 14 | 5 | 27 |
| 配置 | 10 | 38 | 24 | 72 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 7 | 3 | 12 |
| UI | 0 | 3 | 3 | 6 |
| 部署 | 1 | 10 | 6 | 17 |
| **合计** | **99** | **161** | **79** | **339** |

### 优先修复建议

**立即处理 (本周内)**:
1. 修复 CORS allowedMethods 添加 PUT/PATCH/DELETE
2. 修复 Mission Routes 错误响应格式
3. 为 WebSocketBridge 添加最大连接数限制
4. 清理 pendingAcks 和 taskEventHistory
5. 移除硬编码回退密钥
6. 修复 config/runtime/test.json 配置
7. 评估并调整 prod 并发限制

**短期内处理 (本月内)**:
1. 统一安全配置（remoteWorkerRegistration）
2. 实现无界 Map 的 LRU/TTL 驱逐
3. 统一 API 响应信封格式
4. 完善 Redis idempotency 实现
5. 添加分布式去重中间件
6. 实现 cache stampede 保护

**长期规划**:
1. 建立完整的连接管理和监控
2. 实现后台清理任务框架
3. 添加配置 schema 验证
4. 优化高频繁操作的数据结构
5. 建立内存使用基线和告警

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十五轮 - 构建测试与类型系统Review）

### 发现的新问题

#### 1. [构建] [高严重] [无 TypeScript 增量编译缓存]
- **文件/路径**: `tsconfig.json`
- **问题描述**: 
  - 无 `incremental: true` 配置
  - 无 `tsbuildinfo` 文件
  - 每次构建是完整重建（9.6秒）
  - TypeScript 无法使用增量编译
- **建议修复**: 
  1. 在 tsconfig.json 添加 `"incremental": true`
  2. 添加 `"tsbuildinfo": ".tsbuildinfo"` 到 .gitignore
  3. 实现项目引用以支持并行构建

#### 2. [构建] [高严重] [76个 npm scripts 大部分重复构建]
- **文件/路径**: `package.json`
- **问题描述**: 
  - 40+ CLI scripts 都运行 `npm run build && node ...`
  - 每个脚本前都执行完整重建
  - 无法直接运行预构建的 CLI
  - 20+ stable:* 脚本遵循相同模式
- **建议修复**: 
  1. 创建 `build:cli` 脚本构建一次
  2. 让 CLI 脚本使用预构建的 dist 文件
  3. 添加 npm run dev 或类似的无需重建的执行方式

#### 3. [源代码] [高严重] [168处 any 类型使用]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - `stable-runner-factory.ts`: `opts: any` -> `any`
  - `billing-service-async.ts`: `input: any` 方法签名
  - 重试/超时工具使用 `(...args: any[]) => Promise<any>`
  - 绕过类型安全
- **建议修复**: 
  1. 使用泛型替代 any
  2. 替换为具体的输入类型
  3. 添加 ESLint 规则禁止 any

#### 4. [源代码] [高严重] [38处 @ts-ignore 指令]
- **文件/路径**: 多个文件
- **问题描述**: 
  - 14个 stability 相关文件有 `@ts-ignore ExecutionRecord type mismatch`
  - 5个 harness-sdk 文件有 `Partial<HarnessRun> doesn't have all required properties`
  - 2个文件有 `exactOptionalPropertyTypes` 问题
- **建议修复**: 
  1. 修复 ExecutionRecord 类型不匹配
  2. 完成 HarnessRun 类型的必需属性
  3. 移除 @ts-ignore 使用

#### 5. [源代码] [高严重] [ExecutionRecord 类型不匹配]
- **文件/路径**: `src/platform/stability/stable-evidence-bundle-support.ts:783` 等14个文件
- **问题描述**: 
  - 插入 execution record 时 TypeScript 拒绝类型
  - `ExecutionRecord` 接口要求新字段但 insert 对象缺失
  - 或 store 的 insert 方法接受不同类型
- **建议修复**: 
  1. 对齐 ExecutionRecord 接口和 store 类型
  2. 添加缺失字段到 insert 对象
  3. 或调整 store 的 insert 方法类型签名

#### 6. [源代码] [中严重] [30+处 as unknown as 双类型转换]
- **文件/路径**: `division-loader.ts`, `domain-baseline-catalog.ts`, `harness-sdk`
- **问题描述**: 
  - 完全绕过类型安全
  - 通常是类型架构问题的信号
- **建议修复**: 
  1. 修复上游类型问题
  2. 使用更具体的类型断言
  3. 重构类型层次结构

#### 7. [构建] [中严重] [rimraf  extraneous - 未在 package.json 声明]
- **文件/路径**: `node_modules/rimraf`
- **问题描述**: 
  - rimraf@6.1.3 存在于 node_modules 但未在 package.json 声明
  - 可能导致依赖问题
- **建议修复**: 
  1. 添加到 dependencies 或 devDependencies
  2. 或移除并使用其他清理方式

#### 8. [测试] [高严重] [178处直接 process.env 变更无抽象]
- **文件/路径**: `tests/unit/domains/registry/plugin-spi-registry.test.ts:640-687`
- **问题描述**: 
  - 直接读取/写入 process.env
  - 在 beforeEach/afterEach 中手动保存/恢复
  - 无 helper 抽象
- **建议修复**: 
  1. 创建 env helper 函数
  2. 统一保存和恢复模式
  3. 添加测试隔离验证

#### 9. [测试] [高严重] [21,998处 unlinkSync 调用无集中清理]
- **文件/路径**: 多个测试文件
- **问题描述**: 
  - 每个测试重新实现清理逻辑
  - 无集中清理 utility
  - 可能导致 EBUSY (Windows) 或并发访问问题
- **建议修复**: 
  1. 创建集中的测试清理 utility
  2. 使用 afterEach 确保清理
  3. 考虑使用临时目录

#### 10. [测试] [中严重] [无统一 mocking 框架]
- **文件/路径**: `tests/` 多个目录
- **问题描述**: 
  - 10,579 处 vi.fn, jest.mock, sinon 等调用
  - 无统一的 mocking 框架可见
  - 模式不一致
- **建议修复**: 
  1. 建立统一的 mock factory
  2. 标准化 mock 模式
  3. 添加 mocking 最佳实践文档

#### 11. [测试] [中严重] [Singleton 状态重置未标准化]
- **文件/路径**: `tests/unit/platform/five-plane-execution/dispatcher.test.ts`
- **问题描述**: 
  - 测试通过特定函数重置 singletons
  - 每个测试重复自己的 reset 逻辑
  - 无统一的 resetAllSingletons()
- **建议修复**: 
  1. 创建统一的 singleton reset 机制
  2. 在 afterEach 中自动调用
  3. 文档化 singleton 重置要求

#### 12. [测试] [低严重] [无分支覆盖率要求]
- **文件/路径**: `.c8rc.json`
- **问题描述**: 
  - `"100": false` - 无覆盖率要求
  - 只测量源代码，不测量测试文件
- **建议修复**: 
  1. 添加分支覆盖率阈值
  2. 在 CI 中强制执行覆盖率 gate
  3. 关注关键代码路径

#### 13. [网络] [高严重] [ScopedExternalAccessSandbox performHttpRequest 无超时]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/scoped-external-access-sandbox.ts:289`
- **问题描述**: 
  - fetch 调用无 timeout 参数
  - 挂起的请求可能永久阻塞
- **建议修复**: 
  1. 添加 AbortController timeout
  2. 使用标准的超时模式
  3. 记录超时配置

#### 14. [网络] [高严重] [OIDC 服务 fetch 调用无超时]
- **文件/路径**: `src/org-governance/sso-scim/oidc/oidc-service.ts:565,272`
- **问题描述**: 
  - OIDC token 交换无 retry logic
  - 无 timeout/abort signal
  - 认证流程可能挂起
- **建议修复**: 
  1. 添加 fetch timeout
  2. 添加 retry logic
  3. 使用标准的超时配置

#### 15. [网络] [高严重] [plugin-runtime-child 替换 globalThis.fetch 为 no-op]
- **文件/路径**: `src/domains/registry/plugin-runtime-child.ts:112`
- **问题描述**: 
  ```typescript
  globalThis.fetch = (async () => {}) as unknown as typeof globalThis.fetch;
  ```
  - 插件上下文中完全替换 fetch 为空函数
  - 插件的所有出站 HTTP 请求都会失败
  - 看起来是有意为之但可能造成问题
- **建议修复**: 
  1. 确认这是有意行为
  2. 文档化 fetch 替换行为
  3. 确保不会意外影响其他代码

#### 16. [网络] [中严重] [无显式 http.Agent 配置]
- **文件/路径**: 全局
- **问题描述**: 
  - 无显式的 http.Agent/https.Agent 配置
  - 依赖 Node.js 默认连接池
  - 无 maxSockets 或 maxConnections 配置
  - 高吞吐场景可能出问题
- **建议修复**: 
  1. 添加 http Agent 配置
  2. 文档化连接池大小
  3. 监控连接使用情况

#### 17. [序列化] [中严重] [合同版本硬编码为字符串字面量]
- **文件/路径**: `src/platform/contracts/inter-plane-contract-gateway.ts:184`
- **问题描述**: 
  - `schemaVersion: "v4.3"` 硬编码
  - 难以追踪合同版本演变
- **建议修复**: 
  1. 提取到类型化常量或 enum
  2. 集中管理合同版本
  3. 添加版本兼容性检查

#### 18. [序列化] [低严重] [Timestamp = string 丢失精度]
- **文件/路径**: `src/platform/contracts/types/domain/primitives.ts:10`
- **问题描述**: 
  - `type Timestamp = string` 无类型精度
  - ISO 8601 字符串无时区信息
- **建议修复**: 
  1. 使用 Date 对象或 branded string 类型
  2. 明确时区处理约定
  3. 文档化时间戳语义

#### 19. [源代码] [中严重] [29处非空断言 !.]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - 使用 `!.` 断言非空
  - 可能导致运行时错误
- **建议修复**: 
  1. 使用可选链 `?.` 或空值合并 `??`
  2. 添加适当的 null 检查
  3. 减少非空断言使用

#### 20. [API] [中严重] [API 路由 schema 版本硬编码]
- **文件/路径**: `src/platform/five-plane-interface/api/federation-routing-service.ts`
- **问题描述**: 
  - Zod schema 验证存在
  - 但 schema 版本未与 contract 版本同步
- **建议修复**: 
  1. 从 contract 版本派生 schema 版本
  2. 添加版本一致性检查
  3. 文档化版本关系

### 总结

本次补充Review（第十五轮 - 构建测试与类型系统Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. 无 TypeScript 增量编译缓存（每次完整重建9.6秒）
2. 76个 npm scripts 大部分重复构建
3. 168处 any 类型使用（绕过类型安全）
4. 38处 @ts-ignore 指令
5. ExecutionRecord 类型不匹配（14个文件）
6. 178处直接 process.env 变更无抽象
7. ScopedExternalAccessSandbox performHttpRequest 无超时
8. OIDC 服务 fetch 调用无超时
9. plugin-runtime-child 替换 globalThis.fetch 为 no-op

**中优先级**:
1. 30+处 as unknown as 双类型转换
2. rimraf extraneous 未声明
3. 21,998处 unlinkSync 无集中清理
4. 无统一 mocking 框架
5. Singleton 状态重置未标准化
6. 无显式 http.Agent 配置
7. 合同版本硬编码为字符串
8. Timestamp = string 丢失精度
9. 非空断言使用

**低优先级**:
1. 无分支覆盖率要求
2. API 路由 schema 版本未同步

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 72 | 87 | 39 | 198 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **109** | **182** | **85** | **376** |

### 优先修复建议

**立即处理 (本周内)**:
1. 添加 TypeScript 增量编译 (`incremental: true`)
2. 重构 npm scripts 避免重复构建
3. 减少 any 类型使用到 <50
4. 移除所有 @ts-ignore 指令
5. 修复 ExecutionRecord 类型不匹配
6. 添加 fetch 超时到 OIDC 和 sandbox

**短期内处理 (本月内)**:
1. 创建集中的测试清理 utility
2. 建立统一的 mocking 框架
3. 标准化 singleton 重置机制
4. 添加 http.Agent 连接池配置
5. 提取合同版本到类型化常量

**长期规划**:
1. 实现完整的类型安全（无 any）
2. 建立测试基础设施标准
3. 实现构建缓存优化
4. 完善网络层超时和重试
5. 建立覆盖率门禁标准

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十四轮 - 日志事件与工作流Review）

### 发现的新问题

#### 1. [可观测性] [高严重] [StructuredLogEntry 无 requestId 字段]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - StructuredLogEntry 接口无 requestId 字段
  - 只有 correlationId，它回退到 traceId
  - 无法独立追踪单个 HTTP 请求
- **建议修复**: 
  1. 在 StructuredLogEntry 添加 requestId 字段
  2. 在 HTTP 中间件中生成和注入 requestId
  3. 确保 requestId 流经所有日志调用

#### 2. [可观测性] [高严重] [StructuredLogger data 字段无自动清洗]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - 如果开发者传递 `{ password: "..." }` 给 logger.info，密码会被记录
  - 无字段级脱敏机制
  - 依赖开发者手动排除敏感字段
- **建议修复**: 
  1. 添加 OBSERVE_OUTPUT_BLACKLIST 类似的黑名单
  2. 实现 redact/mask/sanitize 工具函数
  3. 在日志传输前自动清洗敏感字段

#### 3. [可观测性] [中严重] [日志中敏感数据无 PII 处理]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - 无 PII 重写工具
  - 开发者必须手动避免记录敏感数据
  - 存在意外记录敏感信息的风险
- **建议修复**: 
  1. 添加 PII 检测和重写工具
  2. 在文档中明确记录敏感字段列表
  3. 添加测试验证敏感数据不被记录

#### 4. [可观测性] [中严重] [59处 console.* 直接调用]
- **文件/路径**: 多个源文件
- **问题描述**: 
  - 应该使用 StructuredLogger
  - console 调用可能不被日志聚合系统收集
  - 难以追踪和关联
- **建议修复**: 
  1. 替换所有 console.* 为 StructuredLogger
  2. 添加 ESLint 规则禁止 console.*
  3. 保留少量允许的 CLI 输出

#### 5. [可观测性] [中严重] [部分 logger.warn 调用缺少结构化数据]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:122`
- **问题描述**: 
  ```typescript
  logger.warn("WebSocket connection rejected: missing subprotocol token")
  ```
  - 只有消息，无 data 对象
  - 难以关联和查询
- **建议修复**: 
  1. 添加 `{ data: { ... } }` 对象
  2. 确保所有日志包含上下文数据
  3. 添加日志审查工具

#### 6. [源代码] [高严重] [Harness while(true) 循环无硬性迭代上限]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:1442`
- **问题描述**: 
  ```typescript
  while (true) {
    // ...
    // 依赖 budget gate 和 guardrail vibration 退出
    // 如果两者都失败，循环永久继续
  }
  ```
  - 依赖 budget gate 和 guardrail vibration 退出
  - 无硬性迭代次数限制
  - 可能导致无限循环
- **建议修复**: 
  1. 添加硬性 maxIterations 上限
  2. 在达到上限时强制退出
  3. 记录详细的迭代统计用于调试

#### 7. [源代码] [高严重] [Oapeflir while(true) 循环可能无限重规划]
- **文件/路径**: `src/platform/five-plane-orchestration/oapeflir/oapeflir-loop-service.ts:355`
- **问题描述**: 
  - 如果 loopReplanDecision.shouldReplan 永不变为 false
  - 重规划永远无法减少计划错误
  - 可能导致无限循环
- **建议修复**: 
  1. 添加重规划次数上限
  2. 在达到上限时强制接受当前结果
  3. 添加重规划质量衰减检测

#### 8. [源代码] [中严重] [Recovery 流程允许终态转换到 paused]
- **文件/路径**: `src/platform/five-plane-execution/runtime-state-machine.ts:105-108`
- **问题描述**: 
  - completed, failed, cancelled, aborted 可转换到 paused 进行恢复
  - 创建恢复路径可能重新进入执行
  - 需要验证此行为的正确性
- **建议修复**: 
  1. 审查 recovery 转换的合法性
  2. 确保不会导致状态不一致
  3. 添加转换前置条件验证

#### 9. [国际化] [高严重] [API 错误消息硬编码英文]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/api-error.ts`
- **问题描述**: 
  ```typescript
  return new ApiError(404, "api.task_not_found", "Task not found.");
  ```
  - 错误消息硬编码英文
  - 无后端 i18n 机制
  - 非英文客户端收到不本地化的错误
- **建议修复**: 
  1. 使用错误码而非硬编码消息
  2. 客户端根据错误码本地化
  3. 或在后端添加 i18n 支持

#### 10. [国际化] [中严重] [UI 硬编码字符串未翻译]
- **文件/路径**: `ui/packages/features/conversation/src/web/index.tsx`
- **问题描述**: 
  - "Build Plan", "Execute", "Messages" 等按钮标签硬编码
  - 不使用 i18n 系统
- **建议修复**: 
  1. 使用 `translateFeatureCopy` 替换硬编码
  2. 确保所有用户可见文本通过 i18n
  3. 添加硬编码字符串检测

#### 11. [国际化] [中严重] [Arabic 目录不完整]
- **文件/路径**: `ui/packages/shared/i18n/src/catalogs/ar-SA.ts`
- **问题描述**: 
  - ar-SA 只有约18条消息
  - en-US 和 zh-CN 有约117条
  - 缺失的 key 返回 key 本身
- **建议修复**: 
  1. 补充 Arabic 翻译
  2. 确保所有 key 都有翻译
  3. 添加翻译完整性测试

#### 12. [国际化] [低严重] [translateFeatureCopy 无回退默认值]
- **文件/路径**: `ui/packages/shared/i18n/`
- **问题描述**: 
  - 如果 feature ID 不在 catalog 中
  - 返回 undefined title/summary
  - 无合理回退
- **建议修复**: 
  1. 添加回退到 key 或默认字符串
  2. 或使用 feature ID 作为显示名
  3. 确保 UI 不显示 undefined

#### 13. [运维] [高严重] [健康检查无 readiness/liveness 区分]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts`
- **问题描述**: 
  - /healthz, /v1/healthz, /health 返回相同报告
  - 无 readiness probe（可处理流量？）
  - 无 liveness probe（系统存活？）
  - canAcceptTraffic() 未通过 HTTP 暴露
- **建议修复**: 
  1. 添加 /ready 和 /live 端点
  2. Readiness 检查依赖项就绪
  3. Liveness 检查进程健康

#### 14. [运维] [高严重] [shutdown 期间健康检查存在竞态]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts`
- **问题描述**: 
  - getHealthReportAsync() 异步检查 dbWritable
  - shutdown 期间数据库连接可能关闭
  - 健康检查仍在传入
- **建议修复**: 
  1. 在 shutdown 时返回 unhealthy
  2. 添加 shutdown 状态标志
  3. 停止接受新的健康检查请求

#### 15. [运维] [高严重] [LeaderElectionService 未与 GracefulShutdown 集成]
- **文件/路径**: `src/platform/five-plane-execution/ha/leader-election-service.ts`
- **问题描述**: 
  - LeaderElectionService.stop() 处理优雅退位
  - 但未在全局 graceful shutdown 中注册
  - 需显式注册为 shutdown handler
- **建议修复**: 
  1. 将 LeaderElectionService 添加到 shutdown handlers
  2. 确保领导者在 shutdown 时让位
  3. 验证关闭顺序正确

#### 16. [运维] [中严重] [addHandler() 无显式排序约定]
- **文件/路径**: `src/platform/five-plane-execution/startup/graceful-shutdown.ts`
- **问题描述**: 
  - 虽然 handlers 逆序执行
  - 但无显式接口声明 handler 依赖关系
  - 关键 handlers（如关闭数据库连接）应最后运行
- **建议修复**: 
  1. 添加优先级或阶段参数
  2. 文档化关键 handler 顺序要求
  3. 添加顺序验证测试

#### 17. [运维] [中严重] [unref'd timers 可能不触发]
- **文件/路径**: `src/platform/five-plane-execution/startup/process-error-handlers.ts:106`
- **问题描述**: 
  - `setTimeout(...).unref()` 不保持事件循环活跃
  - 如果主线程空闲，强制退出可能不触发
- **建议修复**: 
  1. 移除 .unref() 除非确实需要
  2. 或确保有其他方式保持事件循环
  3. 添加超时触发验证测试

#### 18. [运维] [中严重] [StartupConsistencyChecker 阻塞流量但不暴露状态]
- **文件/路径**: `src/platform/five-plane-startup-plan.ts`
- **问题描述**: 
  - canAcceptTraffic() 在 P0 问题存在时返回 false
  - 但 _trafficBlocked 是内部状态
  - 负载均衡器访问 /healthz 仍会得到 "ok"
- **建议修复**: 
  1. 在健康报告中暴露 trafficBlocked 状态
  2. 或添加单独的健康端点指示就绪状态
  3. 确保负载均衡器在阻塞时不路由流量

#### 19. [事件系统] [中严重] [TypedEventBus vs EventEmitter 混用]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - 项目同时使用 TypedEventBus 和原生 EventEmitter
  - TypedEventBus 应该是标准
  - 混用可能导致类型安全问题
- **建议修复**: 
  1. 创建迁移计划统一使用 TypedEventBus
  2. 添加 ESLint 规则禁止原生 EventEmitter
  3. 在 CI 中检测混用情况

#### 20. [源代码] [低严重] [无事件排序保证]
- **文件/路径**: `src/platform/five-plane-state-evidence/events/`
- **问题描述**: 
  - 无明确的事件排序保证文档
  - 事件处理可能乱序
  - 依赖方需要处理乱序
- **建议修复**: 
  1. 文档化事件排序语义
  2. 如果需要排序，实现序列号机制
  3. 在事件处理中考虑乱序情况

### 总结

本次补充Review（第十四轮 - 日志事件与工作流Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. StructuredLogEntry 无 requestId 字段
2. StructuredLogger data 字段无自动清洗（敏感数据风险）
3. Harness while(true) 循环无硬性迭代上限
4. Oapeflir while(true) 循环可能无限重规划
5. API 错误消息硬编码英文（无 i18n）
6. 健康检查无 readiness/liveness 区分
7. shutdown 期间健康检查存在竞态
8. LeaderElectionService 未与 GracefulShutdown 集成

**中优先级**:
1. 日志中敏感数据无 PII 处理
2. 59处 console.* 直接调用
3. 部分 logger.warn 调用缺少结构化数据
4. Recovery 流程允许终态转换到 paused
5. UI 硬编码字符串未翻译
6. Arabic 目录不完整
7. addHandler() 无显式排序约定
8. unref'd timers 可能不触发
9. StartupConsistencyChecker 阻塞流量但不暴露状态
10. TypedEventBus vs EventEmitter 混用

**低优先级**:
1. translateFeatureCopy 无回退默认值
2. 无事件排序保证

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 68 | 80 | 37 | 185 |
| 测试 | 8 | 14 | 5 | 27 |
| 配置 | 10 | 38 | 24 | 72 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **103** | **171** | **82** | **356** |

### 优先修复建议

**立即处理 (本周内)**:
1. 在 StructuredLogEntry 添加 requestId 字段
2. 实现日志敏感字段自动清洗
3. 为工作流循环添加硬性迭代上限
4. 添加 readiness/liveness 健康检查端点
5. 将 LeaderElectionService 集成到 GracefulShutdown
6. 实现 API 错误消息 i18n 支持

**短期内处理 (本月内)**:
1. 替换所有 console.* 为 StructuredLogger
2. 补充 Arabic i18n 目录
3. 修复 UI 硬编码字符串
4. 统一 TypedEventBus 替代 EventEmitter
5. 解决 shutdown 竞态条件
6. 添加事件排序保证文档

**长期规划**:
1. 建立完整的 PII 处理框架
2. 实现日志完整性验证
3. 建立工作流安全循环检测
4. 完善健康检查和就绪探测
5. 建立国际化完整测试

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十五轮 - 构建测试与类型系统Review）

### 发现的新问题

#### 1. [构建] [高严重] [无 TypeScript 增量编译缓存]
- **文件/路径**: `tsconfig.json`
- **问题描述**: 
  - 无 `incremental: true` 配置
  - 无 `tsbuildinfo` 文件
  - 每次构建是完整重建（9.6秒）
  - TypeScript 无法使用增量编译
- **建议修复**: 
  1. 在 tsconfig.json 添加 `"incremental": true`
  2. 添加 `"tsbuildinfo": ".tsbuildinfo"` 到 .gitignore
  3. 实现项目引用以支持并行构建

#### 2. [构建] [高严重] [76个 npm scripts 大部分重复构建]
- **文件/路径**: `package.json`
- **问题描述**: 
  - 40+ CLI scripts 都运行 `npm run build && node ...`
  - 每个脚本前都执行完整重建
  - 无法直接运行预构建的 CLI
  - 20+ stable:* 脚本遵循相同模式
- **建议修复**: 
  1. 创建 `build:cli` 脚本构建一次
  2. 让 CLI 脚本使用预构建的 dist 文件
  3. 添加 npm run dev 或类似的无需重建的执行方式

#### 3. [源代码] [高严重] [168处 any 类型使用]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - `stable-runner-factory.ts`: `opts: any` -> `any`
  - `billing-service-async.ts`: `input: any` 方法签名
  - 重试/超时工具使用 `(...args: any[]) => Promise<any>`
  - 绕过类型安全
- **建议修复**: 
  1. 使用泛型替代 any
  2. 替换为具体的输入类型
  3. 添加 ESLint 规则禁止 any

#### 4. [源代码] [高严重] [38处 @ts-ignore 指令]
- **文件/路径**: 多个文件
- **问题描述**: 
  - 14个 stability 相关文件有 `@ts-ignore ExecutionRecord type mismatch`
  - 5个 harness-sdk 文件有 `Partial<HarnessRun> doesn't have all required properties`
  - 2个文件有 `exactOptionalPropertyTypes` 问题
- **建议修复**: 
  1. 修复 ExecutionRecord 类型不匹配
  2. 完成 HarnessRun 类型的必需属性
  3. 移除 @ts-ignore 使用

#### 5. [源代码] [高严重] [ExecutionRecord 类型不匹配]
- **文件/路径**: `src/platform/stability/stable-evidence-bundle-support.ts:783` 等14个文件
- **问题描述**: 
  - 插入 execution record 时 TypeScript 拒绝类型
  - `ExecutionRecord` 接口要求新字段但 insert 对象缺失
  - 或 store 的 insert 方法接受不同类型
- **建议修复**: 
  1. 对齐 ExecutionRecord 接口和 store 类型
  2. 添加缺失字段到 insert 对象
  3. 或调整 store 的 insert 方法类型签名

#### 6. [源代码] [中严重] [30+处 as unknown as 双类型转换]
- **文件/路径**: `division-loader.ts`, `domain-baseline-catalog.ts`, `harness-sdk`
- **问题描述**: 
  - 完全绕过类型安全
  - 通常是类型架构问题的信号
- **建议修复**: 
  1. 修复上游类型问题
  2. 使用更具体的类型断言
  3. 重构类型层次结构

#### 7. [构建] [中严重] [rimraf  extraneous - 未在 package.json 声明]
- **文件/路径**: `node_modules/rimraf`
- **问题描述**: 
  - rimraf@6.1.3 存在于 node_modules 但未在 package.json 声明
  - 可能导致依赖问题
- **建议修复**: 
  1. 添加到 dependencies 或 devDependencies
  2. 或移除并使用其他清理方式

#### 8. [测试] [高严重] [178处直接 process.env 变更无抽象]
- **文件/路径**: `tests/unit/domains/registry/plugin-spi-registry.test.ts:640-687`
- **问题描述**: 
  - 直接读取/写入 process.env
  - 在 beforeEach/afterEach 中手动保存/恢复
  - 无 helper 抽象
- **建议修复**: 
  1. 创建 env helper 函数
  2. 统一保存和恢复模式
  3. 添加测试隔离验证

#### 9. [测试] [高严重] [21,998处 unlinkSync 调用无集中清理]
- **文件/路径**: 多个测试文件
- **问题描述**: 
  - 每个测试重新实现清理逻辑
  - 无集中清理 utility
  - 可能导致 EBUSY (Windows) 或并发访问问题
- **建议修复**: 
  1. 创建集中的测试清理 utility
  2. 使用 afterEach 确保清理
  3. 考虑使用临时目录

#### 10. [测试] [中严重] [无统一 mocking 框架]
- **文件/路径**: `tests/` 多个目录
- **问题描述**: 
  - 10,579 处 vi.fn, jest.mock, sinon 等调用
  - 无统一的 mocking 框架可见
  - 模式不一致
- **建议修复**: 
  1. 建立统一的 mock factory
  2. 标准化 mock 模式
  3. 添加 mocking 最佳实践文档

#### 11. [测试] [中严重] [Singleton 状态重置未标准化]
- **文件/路径**: `tests/unit/platform/five-plane-execution/dispatcher.test.ts`
- **问题描述**: 
  - 测试通过特定函数重置 singletons
  - 每个测试重复自己的 reset 逻辑
  - 无统一的 resetAllSingletons()
- **建议修复**: 
  1. 创建统一的 singleton reset 机制
  2. 在 afterEach 中自动调用
  3. 文档化 singleton 重置要求

#### 12. [测试] [低严重] [无分支覆盖率要求]
- **文件/路径**: `.c8rc.json`
- **问题描述**: 
  - `"100": false` - 无覆盖率要求
  - 只测量源代码，不测量测试文件
- **建议修复**: 
  1. 添加分支覆盖率阈值
  2. 在 CI 中强制执行覆盖率 gate
  3. 关注关键代码路径

#### 13. [网络] [高严重] [ScopedExternalAccessSandbox performHttpRequest 无超时]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/scoped-external-access-sandbox.ts:289`
- **问题描述**: 
  - fetch 调用无 timeout 参数
  - 挂起的请求可能永久阻塞
- **建议修复**: 
  1. 添加 AbortController timeout
  2. 使用标准的超时模式
  3. 记录超时配置

#### 14. [网络] [高严重] [OIDC 服务 fetch 调用无超时]
- **文件/路径**: `src/org-governance/sso-scim/oidc/oidc-service.ts:565,272`
- **问题描述**: 
  - OIDC token 交换无 retry logic
  - 无 timeout/abort signal
  - 认证流程可能挂起
- **建议修复**: 
  1. 添加 fetch timeout
  2. 添加 retry logic
  3. 使用标准的超时配置

#### 15. [网络] [高严重] [plugin-runtime-child 替换 globalThis.fetch 为 no-op]
- **文件/路径**: `src/domains/registry/plugin-runtime-child.ts:112`
- **问题描述**: 
  ```typescript
  globalThis.fetch = (async () => {}) as unknown as typeof globalThis.fetch;
  ```
  - 插件上下文中完全替换 fetch 为空函数
  - 插件的所有出站 HTTP 请求都会失败
  - 看起来是有意为之但可能造成问题
- **建议修复**: 
  1. 确认这是有意行为
  2. 文档化 fetch 替换行为
  3. 确保不会意外影响其他代码

#### 16. [网络] [中严重] [无显式 http.Agent 配置]
- **文件/路径**: 全局
- **问题描述**: 
  - 无显式的 http.Agent/https.Agent 配置
  - 依赖 Node.js 默认连接池
  - 无 maxSockets 或 maxConnections 配置
  - 高吞吐场景可能出问题
- **建议修复**: 
  1. 添加 http Agent 配置
  2. 文档化连接池大小
  3. 监控连接使用情况

#### 17. [序列化] [中严重] [合同版本硬编码为字符串字面量]
- **文件/路径**: `src/platform/contracts/inter-plane-contract-gateway.ts:184`
- **问题描述**: 
  - `schemaVersion: "v4.3"` 硬编码
  - 难以追踪合同版本演变
- **建议修复**: 
  1. 提取到类型化常量或 enum
  2. 集中管理合同版本
  3. 添加版本兼容性检查

#### 18. [序列化] [低严重] [Timestamp = string 丢失精度]
- **文件/路径**: `src/platform/contracts/types/domain/primitives.ts:10`
- **问题描述**: 
  - `type Timestamp = string` 无类型精度
  - ISO 8601 字符串无时区信息
- **建议修复**: 
  1. 使用 Date 对象或 branded string 类型
  2. 明确时区处理约定
  3. 文档化时间戳语义

#### 19. [源代码] [中严重] [29处非空断言 !.]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - 使用 `!.` 断言非空
  - 可能导致运行时错误
- **建议修复**: 
  1. 使用可选链 `?.` 或空值合并 `??`
  2. 添加适当的 null 检查
  3. 减少非空断言使用

#### 20. [API] [中严重] [API 路由 schema 版本硬编码]
- **文件/路径**: `src/platform/five-plane-interface/api/federation-routing-service.ts`
- **问题描述**: 
  - Zod schema 验证存在
  - 但 schema 版本未与 contract 版本同步
- **建议修复**: 
  1. 从 contract 版本派生 schema 版本
  2. 添加版本一致性检查
  3. 文档化版本关系

### 总结

本次补充Review（第十五轮 - 构建测试与类型系统Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. 无 TypeScript 增量编译缓存（每次完整重建9.6秒）
2. 76个 npm scripts 大部分重复构建
3. 168处 any 类型使用（绕过类型安全）
4. 38处 @ts-ignore 指令
5. ExecutionRecord 类型不匹配（14个文件）
6. 178处直接 process.env 变更无抽象
7. ScopedExternalAccessSandbox performHttpRequest 无超时
8. OIDC 服务 fetch 调用无超时
9. plugin-runtime-child 替换 globalThis.fetch 为 no-op

**中优先级**:
1. 30+处 as unknown as 双类型转换
2. rimraf extraneous 未声明
3. 21,998处 unlinkSync 无集中清理
4. 无统一 mocking 框架
5. Singleton 状态重置未标准化
6. 无显式 http.Agent 配置
7. 合同版本硬编码为字符串
8. Timestamp = string 丢失精度
9. 非空断言使用

**低优先级**:
1. 无分支覆盖率要求
2. API 路由 schema 版本未同步

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 72 | 87 | 39 | 198 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **109** | **182** | **85** | **376** |

### 优先修复建议

**立即处理 (本周内)**:
1. 添加 TypeScript 增量编译 (`incremental: true`)
2. 重构 npm scripts 避免重复构建
3. 减少 any 类型使用到 <50
4. 移除所有 @ts-ignore 指令
5. 修复 ExecutionRecord 类型不匹配
6. 添加 fetch 超时到 OIDC 和 sandbox

**短期内处理 (本月内)**:
1. 创建集中的测试清理 utility
2. 建立统一的 mocking 框架
3. 标准化 singleton 重置机制
4. 添加 http.Agent 连接池配置
5. 提取合同版本到类型化常量

**长期规划**:
1. 实现完整的类型安全（无 any）
2. 建立测试基础设施标准
3. 实现构建缓存优化
4. 完善网络层超时和重试
5. 建立覆盖率门禁标准

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十二轮 - 错误处理与多租户安全Review）

### 发现的新问题

#### 1. [安全] [严重] [listQuotas() 返回所有租户配额 - 信息泄露]
- **文件/路径**: `src/platform/five-plane-control-plane/tenant/tenant-execution-isolation-service.ts:222-234`
- **问题描述**: 
  ```typescript
  listQuotas(tenantId?: string): TenantQuota[] {
    if (tenantId) {
      return this.db.connection.prepare(`SELECT * FROM tenant_quotas WHERE tenant_id = ?`).all(tenantId);
    }
    // WITHOUT tenantId: 返回所有租户的配额！
    return this.db.connection.prepare(`SELECT * FROM tenant_quotas`).all();
  }
  ```
  - 当 tenantId 为空时返回所有租户的配额
  - 未经授权的信息泄露
- **建议修复**: 
  1. 要求调用者必须提供 tenantId
  2. 添加权限验证确保只能查看自己的配额
  3. 审计日志记录所有配额访问

#### 2. [安全] [高严重] [assertTaskTenantAccess() 返回404而非403 - 资源存在泄露]
- **文件/路径**: `src/platform/five-plane-interface/api/utils.ts:146-148`
- **问题描述**: 
  ```typescript
  if (resourceTenantId !== principal.tenantId) {
    throw new ApiError(404, notFoundCode, notFoundMessage);  // 应该是403
  }
  ```
  - 跨租户访问返回404而非403
  - 泄露了资源存在但属于其他租户的信息
  - 攻击者可检测有效task ID
- **建议修复**: 
  1. 改为返回403 Forbidden
  2. 不要泄露资源是否存在

#### 3. [安全] [高严重] [crossTenantRequest 标志由调用者控制]
- **文件/路径**: `src/platform/five-plane-interface/org-routing/index.ts:163`
- **问题描述**: 
  - `routeOrgBudget()` 接受 `crossTenantRequest?: boolean` 参数
  - 该标志由调用者设置，非系统派生
  - 攻击者可能操纵此标志绕过跨租户限制
- **建议修复**: 
  1. 标志应由系统状态派生，不接受调用者输入
  2. 添加审计日志追踪跨租户请求
  3. 实现零信任跨租户访问模型

#### 4. [源代码] [高严重] [single-task-happy-path 无重试机制]
- **文件/路径**: `src/platform/five-plane-execution/execution-engine/single-task-happy-path.ts:336-337`
- **问题描述**: 
  - `maxRetries: 0` 和 `retryBackoff: "none"`
  - 瞬态失败无自动重试
  - LLM调用失败时无fallback到备用provider
- **建议修复**: 
  1. 启用非关键执行的重试
  2. 实现LLM provider fallback链（Anthropic → OpenAI → MiniMax）
  3. 使用domain baseline catalog的primary/fallback模型偏好

#### 5. [源代码] [高严重] [LLM调用无熔断器保护]
- **文件/路径**: `src/platform/five-plane-execution/execution-engine/model-call-provider.ts`
- **问题描述**: 
  - 熔断器只存在于 channel-gateway 和 call-governance
  - 直接LLM provider调用无熔断器
  - provider降级时无法快速失败
- **建议修复**: 
  1. 在model-call-provider.ts添加熔断器
  2. 当provider失败率超阈值时快速失败
  3. 自动切换到备用provider

#### 6. [安全] [高严重] [分布式限流器 bypass - 实例间不共享]
- **文件/路径**: `src/platform/five-plane-interface/ingress/distributed-rate-limiter.ts:33`
- **问题描述**: 
  - Redis未配置时使用本地Map
  - 每个实例维护自己的localEntries
  - 攻击者可通过切换实例绕过限流
- **建议修复**: 
  1. 要求Redis配置用于生产
  2. 或使用Redis作为所有实例的共享后端
  3. 检测并警告未配置Redis的实例

#### 7. [配置] [中严重] [Soft quota (log_only) 实际不限制]
- **文件/路径**: `src/platform/five-plane-control-plane/tenant/tenant-execution-isolation-service.ts:448`
- **问题描述**: 
  ```typescript
  allowed: quota.enforcementAction === "log_only",  // log_only时返回true
  ```
  - soft quota只记录日志，不实际阻止
  - 配额形同虚设
- **建议修复**: 
  1. 明确区分"监控模式"和"强制模式"
  2. 对于硬配额必须阻止
  3. 文档化quota类型的实际行为

#### 8. [配置] [中严重] [HTTP层无限流头 - 客户端无法获知限制]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/response-hardening.ts`
- **问题描述**: 
  - 未返回 X-RateLimit-* 头
  - 只在429响应时返回retry-after-ms
  - 客户端无法主动管理请求速率
- **建议修复**: 
  1. 在所有响应添加 X-RateLimit-Limit, X-RateLimit-Remaining, X-RateLimit-Reset
  2. 使用标准头名称
  3. 遵循RFC 6585

#### 9. [配置] [中严重] [HTTP层无限流 - 可选且仅按IP]
- **文件/路径**: `src/platform/five-plane-interface/api/http-api-server.ts:334-351`
- **问题描述**: 
  - 限流仅在 `this.rateLimiter != null` 时生效
  - 限流key为 `${clientIp}:${endpoint}` 仅按IP
  - 无per-tenant/per-principal enforcement
- **建议修复**: 
  1. 默认启用限流
  2. 添加per-tenant限流key
  3. 确保所有环境默认配置限流

#### 10. [安全] [中严重] [processRuleMode 可能未强制执行]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/plugin-runtime-host.ts`
- **问题描述**: 
  - `SandboxPolicy.processRuleMode` 设置为 "allow" 或 "deny"
  - 但未找到实际阻止基于此策略生成进程的实际执行代码
  - `--allow-child-process` 标志仅在 sandboxed_process 隔离模式应用
- **建议修复**: 
  1. 验证processRuleMode实际被强制执行
  2. 确保所有隔离模式正确处理
  3. 添加测试验证进程创建被阻止

#### 11. [安全] [中严重] [容器模板替换未验证]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/plugin-runtime-host.ts:751`
- **问题描述**: 
  ```typescript
  rendered = rendered.split(placeholder).join(value);
  ```
  - 简单字符串替换
  - 虽然validateContainerLaunchPluginId()阻止\0和引号
  - 但模板替换本身未验证
- **建议修复**: 
  1. 添加输入验证防止注入
  2. 使用更安全的模板引擎
  3. 验证所有占位符被替换

#### 12. [安全] [中严重] [Adapter执行绕过沙箱]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/adapter-executor.ts`
- **问题描述**: 
  - Adapter执行REST/grpc/MQ调用不经过ScopedExternalAccessSandbox
  - 有自己的allowedDomains配置但非集中管理
  - 可能发起未限制的出站请求
- **建议修复**: 
  1. Adapter执行经过集中沙箱
  2. 统一external access策略
  3. 添加adapter出站请求审计

#### 13. [安全] [中严重] [null principal.tenantId 时静默放行]
- **文件/路径**: `src/platform/five-plane-interface/api/utils.ts:143-145`
- **问题描述**: 
  ```typescript
  if (principal.tenantId == null) {
    return;  // 静默放行
  }
  ```
  - 如果API网关允许无principal认证的请求通过
  - 可能授予跨租户访问权限
- **建议修复**: 
  1. 要求所有API请求有有效principal
  2. null tenantId应拒绝而非静默放行
  3. 添加审计日志

#### 14. [源代码] [中严重] [无LLM provider fallback链]
- **文件/路径**: `src/platform/five-plane-execution/execution-engine/model-call-provider.ts`
- **问题描述**: 
  - 主provider失败时无自动切换
  - domain-baseline-catalog.ts定义了primary/fallback模型偏好但未使用
  - 第一个provider失败即导致执行失败
- **建议修复**: 
  1. 实现provider fallback链
  2. 使用domain baseline的primary/fallback配置
  3. 按优先级尝试直到成功或全部失败

#### 15. [源代码] [中严重] [超时值硬编码]
- **文件/路径**: 多个文件
- **问题描述**: 
  - Effect buffer有固定超时reject
  - Channel gateway的requestTimeoutMs虽可配置但有max 30s限制
  - 无全局请求超时中间件
- **建议修复**: 
  1. 集中超时配置
  2. per-environment可配置
  3. 添加全局超时中间件

#### 16. [数据库] [中严重] [无down migration - 回滚不支持]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/sqlite/migration-runner.ts:45-47`
- **问题描述**: 
  - `rollbackSupported = false`
  - 每个migration只有 `downSql` 占位符
  - schema migration无法回滚
- **建议修复**: 
  1. 文档化回滚限制
  2. 在变更前创建完整备份
  3. 使用blue-green部署减少回滚需求

#### 17. [数据库] [中严重] [Migration 44特殊处理 - 表创建重复]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/sqlite/sqlite-migration-runtime-part3.ts`
- **问题描述**: 
  - Migration 43创建harness_runs表
  - Migration 44再次创建同名表（不同schema）
  - 使用applyCompatibleColumnMigrationIfKnown特殊处理
- **建议修复**: 
  1. 消除重复表创建
  2. 使用ALTER TABLE而非CREATE TABLE
  3. 简化migration逻辑

#### 18. [配置] [低严重] [Provider限流头未转发给客户端]
- **文件/路径**: `src/platform/model-gateway/provider-registry/base-chat-provider.ts:153`
- **问题描述**: 
  - 系统读取provider的ratelimitResetHeaderNames
  - 但不转发给客户端
  - 客户端无法知道provider限流状态
- **建议修复**: 
  1. 转发Provider的限流头
  2. 或添加应用层限流信息
  3. 帮助客户端优化请求

#### 19. [安全] [低严重] [Browser evaluate 接受任意脚本]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/browser-executor.ts:326`
- **问题描述**: 
  - `evaluate` action接受script参数
  - 在浏览器上下文运行（模拟）
  - 如浏览器上下文未正确沙箱化可能是向量
- **建议修复**: 
  1. 验证浏览器上下文正确沙箱化
  2. 消毒或限制script内容
  3. 记录所有evaluate调用

#### 20. [源代码] [低严重] [无全局错误边界包装器]
- **文件/路径**: `src/platform/five-plane-execution/execution-engine/`
- **问题描述**: 
  - 执行引擎缺少unhandled promise rejection的全局处理
  - single-task-happy-path的LLM fallback失败时错误向上传播
- **建议修复**: 
  1. 在执行引擎添加错误边界包装
  2. 统一错误处理模式
  3. 确保所有错误被捕获和记录

### 总结

本次补充Review（第十二轮 - 错误处理与多租户安全Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. listQuotas()返回所有租户配额（信息泄露）
2. assertTaskTenantAccess()返回404泄露资源存在
3. crossTenantRequest标志可被操纵
4. single-task-happy-path无重试（maxRetries=0）
5. LLM调用无熔断器保护
6. 分布式限流器可被bypass（实例间不共享）

**中优先级**:
1. Soft quota实际不阻止
2. 无X-RateLimit-*响应头
3. HTTP层限流可选且仅按IP
4. processRuleMode可能未强制
5. 容器模板替换未验证
6. Adapter执行绕过沙箱
7. null principal静默放行
8. 无LLM provider fallback链
9. 超时值硬编码
10. 无down migration
11. Migration 44特殊处理

**低优先级**:
1. Provider限流头未转发
2. Browser evaluate接受任意脚本
3. 无全局错误边界

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 60 | 68 | 31 | 159 |
| 测试 | 8 | 14 | 5 | 27 |
| 配置 | 8 | 35 | 21 | 64 |
| 安全 | 12 | 13 | 3 | 28 |
| 文档 | 2 | 7 | 3 | 12 |
| UI | 0 | 3 | 3 | 6 |
| 部署 | 1 | 10 | 6 | 17 |
| **合计** | **91** | **150** | **72** | **313** |

### 优先修复建议

**立即处理 (本周内)**:
1. 修复listQuotas()信息泄露漏洞
2. 修正assertTaskTenantAccess()返回403
3. 移除crossTenantRequest调用者控制
4. 启用执行引擎重试机制
5. 添加LLM provider熔断器

**短期内处理 (本月内)**:
1. 修复分布式限流器bypass问题
2. 添加X-RateLimit-*响应头
3. 启用默认HTTP限流（per-tenant）
4. 实现LLM provider fallback链
5. 验证并强制processRuleMode

**长期规划**:
1. 建立完整的限流和配额体系
2. 实现插件系统安全审计
3. 建立多租户安全测试
4. 完善错误处理和恢复机制
5. 统一超时和熔断配置

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十五轮 - 构建测试与类型系统Review）

### 发现的新问题

#### 1. [构建] [高严重] [无 TypeScript 增量编译缓存]
- **文件/路径**: `tsconfig.json`
- **问题描述**: 
  - 无 `incremental: true` 配置
  - 无 `tsbuildinfo` 文件
  - 每次构建是完整重建（9.6秒）
  - TypeScript 无法使用增量编译
- **建议修复**: 
  1. 在 tsconfig.json 添加 `"incremental": true`
  2. 添加 `"tsbuildinfo": ".tsbuildinfo"` 到 .gitignore
  3. 实现项目引用以支持并行构建

#### 2. [构建] [高严重] [76个 npm scripts 大部分重复构建]
- **文件/路径**: `package.json`
- **问题描述**: 
  - 40+ CLI scripts 都运行 `npm run build && node ...`
  - 每个脚本前都执行完整重建
  - 无法直接运行预构建的 CLI
  - 20+ stable:* 脚本遵循相同模式
- **建议修复**: 
  1. 创建 `build:cli` 脚本构建一次
  2. 让 CLI 脚本使用预构建的 dist 文件
  3. 添加 npm run dev 或类似的无需重建的执行方式

#### 3. [源代码] [高严重] [168处 any 类型使用]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - `stable-runner-factory.ts`: `opts: any` -> `any`
  - `billing-service-async.ts`: `input: any` 方法签名
  - 重试/超时工具使用 `(...args: any[]) => Promise<any>`
  - 绕过类型安全
- **建议修复**: 
  1. 使用泛型替代 any
  2. 替换为具体的输入类型
  3. 添加 ESLint 规则禁止 any

#### 4. [源代码] [高严重] [38处 @ts-ignore 指令]
- **文件/路径**: 多个文件
- **问题描述**: 
  - 14个 stability 相关文件有 `@ts-ignore ExecutionRecord type mismatch`
  - 5个 harness-sdk 文件有 `Partial<HarnessRun> doesn't have all required properties`
  - 2个文件有 `exactOptionalPropertyTypes` 问题
- **建议修复**: 
  1. 修复 ExecutionRecord 类型不匹配
  2. 完成 HarnessRun 类型的必需属性
  3. 移除 @ts-ignore 使用

#### 5. [源代码] [高严重] [ExecutionRecord 类型不匹配]
- **文件/路径**: `src/platform/stability/stable-evidence-bundle-support.ts:783` 等14个文件
- **问题描述**: 
  - 插入 execution record 时 TypeScript 拒绝类型
  - `ExecutionRecord` 接口要求新字段但 insert 对象缺失
  - 或 store 的 insert 方法接受不同类型
- **建议修复**: 
  1. 对齐 ExecutionRecord 接口和 store 类型
  2. 添加缺失字段到 insert 对象
  3. 或调整 store 的 insert 方法类型签名

#### 6. [源代码] [中严重] [30+处 as unknown as 双类型转换]
- **文件/路径**: `division-loader.ts`, `domain-baseline-catalog.ts`, `harness-sdk`
- **问题描述**: 
  - 完全绕过类型安全
  - 通常是类型架构问题的信号
- **建议修复**: 
  1. 修复上游类型问题
  2. 使用更具体的类型断言
  3. 重构类型层次结构

#### 7. [构建] [中严重] [rimraf  extraneous - 未在 package.json 声明]
- **文件/路径**: `node_modules/rimraf`
- **问题描述**: 
  - rimraf@6.1.3 存在于 node_modules 但未在 package.json 声明
  - 可能导致依赖问题
- **建议修复**: 
  1. 添加到 dependencies 或 devDependencies
  2. 或移除并使用其他清理方式

#### 8. [测试] [高严重] [178处直接 process.env 变更无抽象]
- **文件/路径**: `tests/unit/domains/registry/plugin-spi-registry.test.ts:640-687`
- **问题描述**: 
  - 直接读取/写入 process.env
  - 在 beforeEach/afterEach 中手动保存/恢复
  - 无 helper 抽象
- **建议修复**: 
  1. 创建 env helper 函数
  2. 统一保存和恢复模式
  3. 添加测试隔离验证

#### 9. [测试] [高严重] [21,998处 unlinkSync 调用无集中清理]
- **文件/路径**: 多个测试文件
- **问题描述**: 
  - 每个测试重新实现清理逻辑
  - 无集中清理 utility
  - 可能导致 EBUSY (Windows) 或并发访问问题
- **建议修复**: 
  1. 创建集中的测试清理 utility
  2. 使用 afterEach 确保清理
  3. 考虑使用临时目录

#### 10. [测试] [中严重] [无统一 mocking 框架]
- **文件/路径**: `tests/` 多个目录
- **问题描述**: 
  - 10,579 处 vi.fn, jest.mock, sinon 等调用
  - 无统一的 mocking 框架可见
  - 模式不一致
- **建议修复**: 
  1. 建立统一的 mock factory
  2. 标准化 mock 模式
  3. 添加 mocking 最佳实践文档

#### 11. [测试] [中严重] [Singleton 状态重置未标准化]
- **文件/路径**: `tests/unit/platform/five-plane-execution/dispatcher.test.ts`
- **问题描述**: 
  - 测试通过特定函数重置 singletons
  - 每个测试重复自己的 reset 逻辑
  - 无统一的 resetAllSingletons()
- **建议修复**: 
  1. 创建统一的 singleton reset 机制
  2. 在 afterEach 中自动调用
  3. 文档化 singleton 重置要求

#### 12. [测试] [低严重] [无分支覆盖率要求]
- **文件/路径**: `.c8rc.json`
- **问题描述**: 
  - `"100": false` - 无覆盖率要求
  - 只测量源代码，不测量测试文件
- **建议修复**: 
  1. 添加分支覆盖率阈值
  2. 在 CI 中强制执行覆盖率 gate
  3. 关注关键代码路径

#### 13. [网络] [高严重] [ScopedExternalAccessSandbox performHttpRequest 无超时]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/scoped-external-access-sandbox.ts:289`
- **问题描述**: 
  - fetch 调用无 timeout 参数
  - 挂起的请求可能永久阻塞
- **建议修复**: 
  1. 添加 AbortController timeout
  2. 使用标准的超时模式
  3. 记录超时配置

#### 14. [网络] [高严重] [OIDC 服务 fetch 调用无超时]
- **文件/路径**: `src/org-governance/sso-scim/oidc/oidc-service.ts:565,272`
- **问题描述**: 
  - OIDC token 交换无 retry logic
  - 无 timeout/abort signal
  - 认证流程可能挂起
- **建议修复**: 
  1. 添加 fetch timeout
  2. 添加 retry logic
  3. 使用标准的超时配置

#### 15. [网络] [高严重] [plugin-runtime-child 替换 globalThis.fetch 为 no-op]
- **文件/路径**: `src/domains/registry/plugin-runtime-child.ts:112`
- **问题描述**: 
  ```typescript
  globalThis.fetch = (async () => {}) as unknown as typeof globalThis.fetch;
  ```
  - 插件上下文中完全替换 fetch 为空函数
  - 插件的所有出站 HTTP 请求都会失败
  - 看起来是有意为之但可能造成问题
- **建议修复**: 
  1. 确认这是有意行为
  2. 文档化 fetch 替换行为
  3. 确保不会意外影响其他代码

#### 16. [网络] [中严重] [无显式 http.Agent 配置]
- **文件/路径**: 全局
- **问题描述**: 
  - 无显式的 http.Agent/https.Agent 配置
  - 依赖 Node.js 默认连接池
  - 无 maxSockets 或 maxConnections 配置
  - 高吞吐场景可能出问题
- **建议修复**: 
  1. 添加 http Agent 配置
  2. 文档化连接池大小
  3. 监控连接使用情况

#### 17. [序列化] [中严重] [合同版本硬编码为字符串字面量]
- **文件/路径**: `src/platform/contracts/inter-plane-contract-gateway.ts:184`
- **问题描述**: 
  - `schemaVersion: "v4.3"` 硬编码
  - 难以追踪合同版本演变
- **建议修复**: 
  1. 提取到类型化常量或 enum
  2. 集中管理合同版本
  3. 添加版本兼容性检查

#### 18. [序列化] [低严重] [Timestamp = string 丢失精度]
- **文件/路径**: `src/platform/contracts/types/domain/primitives.ts:10`
- **问题描述**: 
  - `type Timestamp = string` 无类型精度
  - ISO 8601 字符串无时区信息
- **建议修复**: 
  1. 使用 Date 对象或 branded string 类型
  2. 明确时区处理约定
  3. 文档化时间戳语义

#### 19. [源代码] [中严重] [29处非空断言 !.]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - 使用 `!.` 断言非空
  - 可能导致运行时错误
- **建议修复**: 
  1. 使用可选链 `?.` 或空值合并 `??`
  2. 添加适当的 null 检查
  3. 减少非空断言使用

#### 20. [API] [中严重] [API 路由 schema 版本硬编码]
- **文件/路径**: `src/platform/five-plane-interface/api/federation-routing-service.ts`
- **问题描述**: 
  - Zod schema 验证存在
  - 但 schema 版本未与 contract 版本同步
- **建议修复**: 
  1. 从 contract 版本派生 schema 版本
  2. 添加版本一致性检查
  3. 文档化版本关系

### 总结

本次补充Review（第十五轮 - 构建测试与类型系统Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. 无 TypeScript 增量编译缓存（每次完整重建9.6秒）
2. 76个 npm scripts 大部分重复构建
3. 168处 any 类型使用（绕过类型安全）
4. 38处 @ts-ignore 指令
5. ExecutionRecord 类型不匹配（14个文件）
6. 178处直接 process.env 变更无抽象
7. ScopedExternalAccessSandbox performHttpRequest 无超时
8. OIDC 服务 fetch 调用无超时
9. plugin-runtime-child 替换 globalThis.fetch 为 no-op

**中优先级**:
1. 30+处 as unknown as 双类型转换
2. rimraf extraneous 未声明
3. 21,998处 unlinkSync 无集中清理
4. 无统一 mocking 框架
5. Singleton 状态重置未标准化
6. 无显式 http.Agent 配置
7. 合同版本硬编码为字符串
8. Timestamp = string 丢失精度
9. 非空断言使用

**低优先级**:
1. 无分支覆盖率要求
2. API 路由 schema 版本未同步

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 72 | 87 | 39 | 198 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **109** | **182** | **85** | **376** |

### 优先修复建议

**立即处理 (本周内)**:
1. 添加 TypeScript 增量编译 (`incremental: true`)
2. 重构 npm scripts 避免重复构建
3. 减少 any 类型使用到 <50
4. 移除所有 @ts-ignore 指令
5. 修复 ExecutionRecord 类型不匹配
6. 添加 fetch 超时到 OIDC 和 sandbox

**短期内处理 (本月内)**:
1. 创建集中的测试清理 utility
2. 建立统一的 mocking 框架
3. 标准化 singleton 重置机制
4. 添加 http.Agent 连接池配置
5. 提取合同版本到类型化常量

**长期规划**:
1. 实现完整的类型安全（无 any）
2. 建立测试基础设施标准
3. 实现构建缓存优化
4. 完善网络层超时和重试
5. 建立覆盖率门禁标准

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十四轮 - 日志事件与工作流Review）

### 发现的新问题

#### 1. [可观测性] [高严重] [StructuredLogEntry 无 requestId 字段]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - StructuredLogEntry 接口无 requestId 字段
  - 只有 correlationId，它回退到 traceId
  - 无法独立追踪单个 HTTP 请求
- **建议修复**: 
  1. 在 StructuredLogEntry 添加 requestId 字段
  2. 在 HTTP 中间件中生成和注入 requestId
  3. 确保 requestId 流经所有日志调用

#### 2. [可观测性] [高严重] [StructuredLogger data 字段无自动清洗]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - 如果开发者传递 `{ password: "..." }` 给 logger.info，密码会被记录
  - 无字段级脱敏机制
  - 依赖开发者手动排除敏感字段
- **建议修复**: 
  1. 添加 OBSERVE_OUTPUT_BLACKLIST 类似的黑名单
  2. 实现 redact/mask/sanitize 工具函数
  3. 在日志传输前自动清洗敏感字段

#### 3. [可观测性] [中严重] [日志中敏感数据无 PII 处理]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - 无 PII 重写工具
  - 开发者必须手动避免记录敏感数据
  - 存在意外记录敏感信息的风险
- **建议修复**: 
  1. 添加 PII 检测和重写工具
  2. 在文档中明确记录敏感字段列表
  3. 添加测试验证敏感数据不被记录

#### 4. [可观测性] [中严重] [59处 console.* 直接调用]
- **文件/路径**: 多个源文件
- **问题描述**: 
  - 应该使用 StructuredLogger
  - console 调用可能不被日志聚合系统收集
  - 难以追踪和关联
- **建议修复**: 
  1. 替换所有 console.* 为 StructuredLogger
  2. 添加 ESLint 规则禁止 console.*
  3. 保留少量允许的 CLI 输出

#### 5. [可观测性] [中严重] [部分 logger.warn 调用缺少结构化数据]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:122`
- **问题描述**: 
  ```typescript
  logger.warn("WebSocket connection rejected: missing subprotocol token")
  ```
  - 只有消息，无 data 对象
  - 难以关联和查询
- **建议修复**: 
  1. 添加 `{ data: { ... } }` 对象
  2. 确保所有日志包含上下文数据
  3. 添加日志审查工具

#### 6. [源代码] [高严重] [Harness while(true) 循环无硬性迭代上限]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:1442`
- **问题描述**: 
  ```typescript
  while (true) {
    // ...
    // 依赖 budget gate 和 guardrail vibration 退出
    // 如果两者都失败，循环永久继续
  }
  ```
  - 依赖 budget gate 和 guardrail vibration 退出
  - 无硬性迭代次数限制
  - 可能导致无限循环
- **建议修复**: 
  1. 添加硬性 maxIterations 上限
  2. 在达到上限时强制退出
  3. 记录详细的迭代统计用于调试

#### 7. [源代码] [高严重] [Oapeflir while(true) 循环可能无限重规划]
- **文件/路径**: `src/platform/five-plane-orchestration/oapeflir/oapeflir-loop-service.ts:355`
- **问题描述**: 
  - 如果 loopReplanDecision.shouldReplan 永不变为 false
  - 重规划永远无法减少计划错误
  - 可能导致无限循环
- **建议修复**: 
  1. 添加重规划次数上限
  2. 在达到上限时强制接受当前结果
  3. 添加重规划质量衰减检测

#### 8. [源代码] [中严重] [Recovery 流程允许终态转换到 paused]
- **文件/路径**: `src/platform/five-plane-execution/runtime-state-machine.ts:105-108`
- **问题描述**: 
  - completed, failed, cancelled, aborted 可转换到 paused 进行恢复
  - 创建恢复路径可能重新进入执行
  - 需要验证此行为的正确性
- **建议修复**: 
  1. 审查 recovery 转换的合法性
  2. 确保不会导致状态不一致
  3. 添加转换前置条件验证

#### 9. [国际化] [高严重] [API 错误消息硬编码英文]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/api-error.ts`
- **问题描述**: 
  ```typescript
  return new ApiError(404, "api.task_not_found", "Task not found.");
  ```
  - 错误消息硬编码英文
  - 无后端 i18n 机制
  - 非英文客户端收到不本地化的错误
- **建议修复**: 
  1. 使用错误码而非硬编码消息
  2. 客户端根据错误码本地化
  3. 或在后端添加 i18n 支持

#### 10. [国际化] [中严重] [UI 硬编码字符串未翻译]
- **文件/路径**: `ui/packages/features/conversation/src/web/index.tsx`
- **问题描述**: 
  - "Build Plan", "Execute", "Messages" 等按钮标签硬编码
  - 不使用 i18n 系统
- **建议修复**: 
  1. 使用 `translateFeatureCopy` 替换硬编码
  2. 确保所有用户可见文本通过 i18n
  3. 添加硬编码字符串检测

#### 11. [国际化] [中严重] [Arabic 目录不完整]
- **文件/路径**: `ui/packages/shared/i18n/src/catalogs/ar-SA.ts`
- **问题描述**: 
  - ar-SA 只有约18条消息
  - en-US 和 zh-CN 有约117条
  - 缺失的 key 返回 key 本身
- **建议修复**: 
  1. 补充 Arabic 翻译
  2. 确保所有 key 都有翻译
  3. 添加翻译完整性测试

#### 12. [国际化] [低严重] [translateFeatureCopy 无回退默认值]
- **文件/路径**: `ui/packages/shared/i18n/`
- **问题描述**: 
  - 如果 feature ID 不在 catalog 中
  - 返回 undefined title/summary
  - 无合理回退
- **建议修复**: 
  1. 添加回退到 key 或默认字符串
  2. 或使用 feature ID 作为显示名
  3. 确保 UI 不显示 undefined

#### 13. [运维] [高严重] [健康检查无 readiness/liveness 区分]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts`
- **问题描述**: 
  - /healthz, /v1/healthz, /health 返回相同报告
  - 无 readiness probe（可处理流量？）
  - 无 liveness probe（系统存活？）
  - canAcceptTraffic() 未通过 HTTP 暴露
- **建议修复**: 
  1. 添加 /ready 和 /live 端点
  2. Readiness 检查依赖项就绪
  3. Liveness 检查进程健康

#### 14. [运维] [高严重] [shutdown 期间健康检查存在竞态]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts`
- **问题描述**: 
  - getHealthReportAsync() 异步检查 dbWritable
  - shutdown 期间数据库连接可能关闭
  - 健康检查仍在传入
- **建议修复**: 
  1. 在 shutdown 时返回 unhealthy
  2. 添加 shutdown 状态标志
  3. 停止接受新的健康检查请求

#### 15. [运维] [高严重] [LeaderElectionService 未与 GracefulShutdown 集成]
- **文件/路径**: `src/platform/five-plane-execution/ha/leader-election-service.ts`
- **问题描述**: 
  - LeaderElectionService.stop() 处理优雅退位
  - 但未在全局 graceful shutdown 中注册
  - 需显式注册为 shutdown handler
- **建议修复**: 
  1. 将 LeaderElectionService 添加到 shutdown handlers
  2. 确保领导者在 shutdown 时让位
  3. 验证关闭顺序正确

#### 16. [运维] [中严重] [addHandler() 无显式排序约定]
- **文件/路径**: `src/platform/five-plane-execution/startup/graceful-shutdown.ts`
- **问题描述**: 
  - 虽然 handlers 逆序执行
  - 但无显式接口声明 handler 依赖关系
  - 关键 handlers（如关闭数据库连接）应最后运行
- **建议修复**: 
  1. 添加优先级或阶段参数
  2. 文档化关键 handler 顺序要求
  3. 添加顺序验证测试

#### 17. [运维] [中严重] [unref'd timers 可能不触发]
- **文件/路径**: `src/platform/five-plane-execution/startup/process-error-handlers.ts:106`
- **问题描述**: 
  - `setTimeout(...).unref()` 不保持事件循环活跃
  - 如果主线程空闲，强制退出可能不触发
- **建议修复**: 
  1. 移除 .unref() 除非确实需要
  2. 或确保有其他方式保持事件循环
  3. 添加超时触发验证测试

#### 18. [运维] [中严重] [StartupConsistencyChecker 阻塞流量但不暴露状态]
- **文件/路径**: `src/platform/five-plane-startup-plan.ts`
- **问题描述**: 
  - canAcceptTraffic() 在 P0 问题存在时返回 false
  - 但 _trafficBlocked 是内部状态
  - 负载均衡器访问 /healthz 仍会得到 "ok"
- **建议修复**: 
  1. 在健康报告中暴露 trafficBlocked 状态
  2. 或添加单独的健康端点指示就绪状态
  3. 确保负载均衡器在阻塞时不路由流量

#### 19. [事件系统] [中严重] [TypedEventBus vs EventEmitter 混用]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - 项目同时使用 TypedEventBus 和原生 EventEmitter
  - TypedEventBus 应该是标准
  - 混用可能导致类型安全问题
- **建议修复**: 
  1. 创建迁移计划统一使用 TypedEventBus
  2. 添加 ESLint 规则禁止原生 EventEmitter
  3. 在 CI 中检测混用情况

#### 20. [源代码] [低严重] [无事件排序保证]
- **文件/路径**: `src/platform/five-plane-state-evidence/events/`
- **问题描述**: 
  - 无明确的事件排序保证文档
  - 事件处理可能乱序
  - 依赖方需要处理乱序
- **建议修复**: 
  1. 文档化事件排序语义
  2. 如果需要排序，实现序列号机制
  3. 在事件处理中考虑乱序情况

### 总结

本次补充Review（第十四轮 - 日志事件与工作流Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. StructuredLogEntry 无 requestId 字段
2. StructuredLogger data 字段无自动清洗（敏感数据风险）
3. Harness while(true) 循环无硬性迭代上限
4. Oapeflir while(true) 循环可能无限重规划
5. API 错误消息硬编码英文（无 i18n）
6. 健康检查无 readiness/liveness 区分
7. shutdown 期间健康检查存在竞态
8. LeaderElectionService 未与 GracefulShutdown 集成

**中优先级**:
1. 日志中敏感数据无 PII 处理
2. 59处 console.* 直接调用
3. 部分 logger.warn 调用缺少结构化数据
4. Recovery 流程允许终态转换到 paused
5. UI 硬编码字符串未翻译
6. Arabic 目录不完整
7. addHandler() 无显式排序约定
8. unref'd timers 可能不触发
9. StartupConsistencyChecker 阻塞流量但不暴露状态
10. TypedEventBus vs EventEmitter 混用

**低优先级**:
1. translateFeatureCopy 无回退默认值
2. 无事件排序保证

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 68 | 80 | 37 | 185 |
| 测试 | 8 | 14 | 5 | 27 |
| 配置 | 10 | 38 | 24 | 72 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **103** | **171** | **82** | **356** |

### 优先修复建议

**立即处理 (本周内)**:
1. 在 StructuredLogEntry 添加 requestId 字段
2. 实现日志敏感字段自动清洗
3. 为工作流循环添加硬性迭代上限
4. 添加 readiness/liveness 健康检查端点
5. 将 LeaderElectionService 集成到 GracefulShutdown
6. 实现 API 错误消息 i18n 支持

**短期内处理 (本月内)**:
1. 替换所有 console.* 为 StructuredLogger
2. 补充 Arabic i18n 目录
3. 修复 UI 硬编码字符串
4. 统一 TypedEventBus 替代 EventEmitter
5. 解决 shutdown 竞态条件
6. 添加事件排序保证文档

**长期规划**:
1. 建立完整的 PII 处理框架
2. 实现日志完整性验证
3. 建立工作流安全循环检测
4. 完善健康检查和就绪探测
5. 建立国际化完整测试

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十五轮 - 构建测试与类型系统Review）

### 发现的新问题

#### 1. [构建] [高严重] [无 TypeScript 增量编译缓存]
- **文件/路径**: `tsconfig.json`
- **问题描述**: 
  - 无 `incremental: true` 配置
  - 无 `tsbuildinfo` 文件
  - 每次构建是完整重建（9.6秒）
  - TypeScript 无法使用增量编译
- **建议修复**: 
  1. 在 tsconfig.json 添加 `"incremental": true`
  2. 添加 `"tsbuildinfo": ".tsbuildinfo"` 到 .gitignore
  3. 实现项目引用以支持并行构建

#### 2. [构建] [高严重] [76个 npm scripts 大部分重复构建]
- **文件/路径**: `package.json`
- **问题描述**: 
  - 40+ CLI scripts 都运行 `npm run build && node ...`
  - 每个脚本前都执行完整重建
  - 无法直接运行预构建的 CLI
  - 20+ stable:* 脚本遵循相同模式
- **建议修复**: 
  1. 创建 `build:cli` 脚本构建一次
  2. 让 CLI 脚本使用预构建的 dist 文件
  3. 添加 npm run dev 或类似的无需重建的执行方式

#### 3. [源代码] [高严重] [168处 any 类型使用]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - `stable-runner-factory.ts`: `opts: any` -> `any`
  - `billing-service-async.ts`: `input: any` 方法签名
  - 重试/超时工具使用 `(...args: any[]) => Promise<any>`
  - 绕过类型安全
- **建议修复**: 
  1. 使用泛型替代 any
  2. 替换为具体的输入类型
  3. 添加 ESLint 规则禁止 any

#### 4. [源代码] [高严重] [38处 @ts-ignore 指令]
- **文件/路径**: 多个文件
- **问题描述**: 
  - 14个 stability 相关文件有 `@ts-ignore ExecutionRecord type mismatch`
  - 5个 harness-sdk 文件有 `Partial<HarnessRun> doesn't have all required properties`
  - 2个文件有 `exactOptionalPropertyTypes` 问题
- **建议修复**: 
  1. 修复 ExecutionRecord 类型不匹配
  2. 完成 HarnessRun 类型的必需属性
  3. 移除 @ts-ignore 使用

#### 5. [源代码] [高严重] [ExecutionRecord 类型不匹配]
- **文件/路径**: `src/platform/stability/stable-evidence-bundle-support.ts:783` 等14个文件
- **问题描述**: 
  - 插入 execution record 时 TypeScript 拒绝类型
  - `ExecutionRecord` 接口要求新字段但 insert 对象缺失
  - 或 store 的 insert 方法接受不同类型
- **建议修复**: 
  1. 对齐 ExecutionRecord 接口和 store 类型
  2. 添加缺失字段到 insert 对象
  3. 或调整 store 的 insert 方法类型签名

#### 6. [源代码] [中严重] [30+处 as unknown as 双类型转换]
- **文件/路径**: `division-loader.ts`, `domain-baseline-catalog.ts`, `harness-sdk`
- **问题描述**: 
  - 完全绕过类型安全
  - 通常是类型架构问题的信号
- **建议修复**: 
  1. 修复上游类型问题
  2. 使用更具体的类型断言
  3. 重构类型层次结构

#### 7. [构建] [中严重] [rimraf  extraneous - 未在 package.json 声明]
- **文件/路径**: `node_modules/rimraf`
- **问题描述**: 
  - rimraf@6.1.3 存在于 node_modules 但未在 package.json 声明
  - 可能导致依赖问题
- **建议修复**: 
  1. 添加到 dependencies 或 devDependencies
  2. 或移除并使用其他清理方式

#### 8. [测试] [高严重] [178处直接 process.env 变更无抽象]
- **文件/路径**: `tests/unit/domains/registry/plugin-spi-registry.test.ts:640-687`
- **问题描述**: 
  - 直接读取/写入 process.env
  - 在 beforeEach/afterEach 中手动保存/恢复
  - 无 helper 抽象
- **建议修复**: 
  1. 创建 env helper 函数
  2. 统一保存和恢复模式
  3. 添加测试隔离验证

#### 9. [测试] [高严重] [21,998处 unlinkSync 调用无集中清理]
- **文件/路径**: 多个测试文件
- **问题描述**: 
  - 每个测试重新实现清理逻辑
  - 无集中清理 utility
  - 可能导致 EBUSY (Windows) 或并发访问问题
- **建议修复**: 
  1. 创建集中的测试清理 utility
  2. 使用 afterEach 确保清理
  3. 考虑使用临时目录

#### 10. [测试] [中严重] [无统一 mocking 框架]
- **文件/路径**: `tests/` 多个目录
- **问题描述**: 
  - 10,579 处 vi.fn, jest.mock, sinon 等调用
  - 无统一的 mocking 框架可见
  - 模式不一致
- **建议修复**: 
  1. 建立统一的 mock factory
  2. 标准化 mock 模式
  3. 添加 mocking 最佳实践文档

#### 11. [测试] [中严重] [Singleton 状态重置未标准化]
- **文件/路径**: `tests/unit/platform/five-plane-execution/dispatcher.test.ts`
- **问题描述**: 
  - 测试通过特定函数重置 singletons
  - 每个测试重复自己的 reset 逻辑
  - 无统一的 resetAllSingletons()
- **建议修复**: 
  1. 创建统一的 singleton reset 机制
  2. 在 afterEach 中自动调用
  3. 文档化 singleton 重置要求

#### 12. [测试] [低严重] [无分支覆盖率要求]
- **文件/路径**: `.c8rc.json`
- **问题描述**: 
  - `"100": false` - 无覆盖率要求
  - 只测量源代码，不测量测试文件
- **建议修复**: 
  1. 添加分支覆盖率阈值
  2. 在 CI 中强制执行覆盖率 gate
  3. 关注关键代码路径

#### 13. [网络] [高严重] [ScopedExternalAccessSandbox performHttpRequest 无超时]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/scoped-external-access-sandbox.ts:289`
- **问题描述**: 
  - fetch 调用无 timeout 参数
  - 挂起的请求可能永久阻塞
- **建议修复**: 
  1. 添加 AbortController timeout
  2. 使用标准的超时模式
  3. 记录超时配置

#### 14. [网络] [高严重] [OIDC 服务 fetch 调用无超时]
- **文件/路径**: `src/org-governance/sso-scim/oidc/oidc-service.ts:565,272`
- **问题描述**: 
  - OIDC token 交换无 retry logic
  - 无 timeout/abort signal
  - 认证流程可能挂起
- **建议修复**: 
  1. 添加 fetch timeout
  2. 添加 retry logic
  3. 使用标准的超时配置

#### 15. [网络] [高严重] [plugin-runtime-child 替换 globalThis.fetch 为 no-op]
- **文件/路径**: `src/domains/registry/plugin-runtime-child.ts:112`
- **问题描述**: 
  ```typescript
  globalThis.fetch = (async () => {}) as unknown as typeof globalThis.fetch;
  ```
  - 插件上下文中完全替换 fetch 为空函数
  - 插件的所有出站 HTTP 请求都会失败
  - 看起来是有意为之但可能造成问题
- **建议修复**: 
  1. 确认这是有意行为
  2. 文档化 fetch 替换行为
  3. 确保不会意外影响其他代码

#### 16. [网络] [中严重] [无显式 http.Agent 配置]
- **文件/路径**: 全局
- **问题描述**: 
  - 无显式的 http.Agent/https.Agent 配置
  - 依赖 Node.js 默认连接池
  - 无 maxSockets 或 maxConnections 配置
  - 高吞吐场景可能出问题
- **建议修复**: 
  1. 添加 http Agent 配置
  2. 文档化连接池大小
  3. 监控连接使用情况

#### 17. [序列化] [中严重] [合同版本硬编码为字符串字面量]
- **文件/路径**: `src/platform/contracts/inter-plane-contract-gateway.ts:184`
- **问题描述**: 
  - `schemaVersion: "v4.3"` 硬编码
  - 难以追踪合同版本演变
- **建议修复**: 
  1. 提取到类型化常量或 enum
  2. 集中管理合同版本
  3. 添加版本兼容性检查

#### 18. [序列化] [低严重] [Timestamp = string 丢失精度]
- **文件/路径**: `src/platform/contracts/types/domain/primitives.ts:10`
- **问题描述**: 
  - `type Timestamp = string` 无类型精度
  - ISO 8601 字符串无时区信息
- **建议修复**: 
  1. 使用 Date 对象或 branded string 类型
  2. 明确时区处理约定
  3. 文档化时间戳语义

#### 19. [源代码] [中严重] [29处非空断言 !.]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - 使用 `!.` 断言非空
  - 可能导致运行时错误
- **建议修复**: 
  1. 使用可选链 `?.` 或空值合并 `??`
  2. 添加适当的 null 检查
  3. 减少非空断言使用

#### 20. [API] [中严重] [API 路由 schema 版本硬编码]
- **文件/路径**: `src/platform/five-plane-interface/api/federation-routing-service.ts`
- **问题描述**: 
  - Zod schema 验证存在
  - 但 schema 版本未与 contract 版本同步
- **建议修复**: 
  1. 从 contract 版本派生 schema 版本
  2. 添加版本一致性检查
  3. 文档化版本关系

### 总结

本次补充Review（第十五轮 - 构建测试与类型系统Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. 无 TypeScript 增量编译缓存（每次完整重建9.6秒）
2. 76个 npm scripts 大部分重复构建
3. 168处 any 类型使用（绕过类型安全）
4. 38处 @ts-ignore 指令
5. ExecutionRecord 类型不匹配（14个文件）
6. 178处直接 process.env 变更无抽象
7. ScopedExternalAccessSandbox performHttpRequest 无超时
8. OIDC 服务 fetch 调用无超时
9. plugin-runtime-child 替换 globalThis.fetch 为 no-op

**中优先级**:
1. 30+处 as unknown as 双类型转换
2. rimraf extraneous 未声明
3. 21,998处 unlinkSync 无集中清理
4. 无统一 mocking 框架
5. Singleton 状态重置未标准化
6. 无显式 http.Agent 配置
7. 合同版本硬编码为字符串
8. Timestamp = string 丢失精度
9. 非空断言使用

**低优先级**:
1. 无分支覆盖率要求
2. API 路由 schema 版本未同步

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 72 | 87 | 39 | 198 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **109** | **182** | **85** | **376** |

### 优先修复建议

**立即处理 (本周内)**:
1. 添加 TypeScript 增量编译 (`incremental: true`)
2. 重构 npm scripts 避免重复构建
3. 减少 any 类型使用到 <50
4. 移除所有 @ts-ignore 指令
5. 修复 ExecutionRecord 类型不匹配
6. 添加 fetch 超时到 OIDC 和 sandbox

**短期内处理 (本月内)**:
1. 创建集中的测试清理 utility
2. 建立统一的 mocking 框架
3. 标准化 singleton 重置机制
4. 添加 http.Agent 连接池配置
5. 提取合同版本到类型化常量

**长期规划**:
1. 实现完整的类型安全（无 any）
2. 建立测试基础设施标准
3. 实现构建缓存优化
4. 完善网络层超时和重试
5. 建立覆盖率门禁标准

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十三轮 - 缓存会话与实时通信Review）

### 发现的新问题

#### 1. [安全] [严重] [CORS allowedMethods 缺少 PUT/PATCH/DELETE]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/response-hardening.ts:12-14`
- **问题描述**: 
  ```typescript
  export const DEFAULT_CORS_CONFIG: CorsConfig = {
    allowedMethods: ["GET", "POST", "OPTIONS"],  // 缺少 PUT, PATCH, DELETE
  ```
  - 浏览器 CORS preflight 会拒绝 PUT/PATCH/DELETE 请求
  - 但路由中有 PATCH /v1/tasks/:id, DELETE endpoints
- **建议修复**: 
  1. 添加 PUT, PATCH, DELETE 到 allowedMethods
  2. 确保 CORS 配置与应用路由方法一致
  3. 添加测试验证所有 HTTP 方法的 CORS preflight

#### 2. [安全] [严重] [Mission Routes 错误响应包装不当]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/mission-routes.ts:101,118-124`
- **问题描述**: 
  ```typescript
  return buildJsonResponse(ctx.requestId, 404, { error: { code: "MISSION_NOT_FOUND" } });
  ```
  - 使用 buildJsonResponse（成功包装）返回错误
  - 错误被包装在成功信封 `{requestId, data: {error: {...}}}` 中
  - 违反 REST 规范
- **建议修复**: 
  1. 使用 buildJsonErrorResponse 处理错误
  2. 确保所有错误路径使用正确的错误信封
  3. 添加 API 一致性测试

#### 3. [源代码] [高严重] [WebSocketBridge 无最大连接数限制]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:103-107`
- **问题描述**: 
  ```typescript
  this.wss = new WebSocketServer({
    server,
    path: WS_PATH,
    maxPayload: options.maxPayloadBytes ?? DEFAULT_MAX_PAYLOAD_BYTES,
    // 无 maxConnections 限制！
  });
  ```
  - 与 dashboard-websocket-server.ts 的 maxClients: 1000 不同
  - 恶意客户端可耗尽服务器资源
- **建议修复**: 
  1. 添加 maxConnections 限制
  2. 实现连接限制拒绝策略
  3. 添加连接计数指标

#### 4. [源代码] [高严重] [pendingAcks 在断开连接时未清理]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:268-282`
- **问题描述**: 
  - handleDisconnection 清理 subscribedTasks, taskSubscribers, slowConsumers
  - 但 pendingAcks Map 未清理
  - 断开的客户端未确认的消息永久留在内存
- **建议修复**: 
  1. 在 handleDisconnection 中删除所有 pendingAcks 条目
  2. 考虑添加超时机制自动清理未确认消息
  3. 记录清理操作的审计日志

#### 5. [安全] [高严重] [硬编码回退密钥]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/audit-event-integrity.ts:41`
- **问题描述**: 
  ```typescript
  const AUDIT_INTEGRITY_HMAC_KEY = process.env["AA_AUDIT_INTEGRITY_HMAC_KEY"] ?? "audit-integrity-secret-key-32-bytes!";
  ```
  - 占位符密钥用作回退
  - 如果环境变量未设置，使用不安全密钥
- **建议修复**: 
  1. 移除回退值，要求环境变量必须设置
  2. 在启动时验证密钥存在且足够强
  3. 如果密钥缺失导致启动失败

#### 6. [配置] [高严重] [config/runtime/test.json 缺少超时配置]
- **文件/路径**: `config/runtime/test.json`
- **问题描述**: 
  - 只有3个字段，缺少 maxConcurrentTasks, defaultTaskTimeoutMs, defaultStepTimeoutMs
  - 回退到默认值可能不适合测试环境
- **建议修复**: 
  1. 添加完整的 timeout/tuning 配置
  2. 确保测试环境有合理的默认值
  3. 文档化必需的配置字段

#### 7. [配置] [高严重] [prod.json 限制过严 - maxConcurrentTasks=1]
- **文件/路径**: `config/runtime/prod.json`
- **问题描述**: 
  - maxConcurrentTasks: 1（vs dev:8, staging:4, pre-prod:6）
  - defaultTaskTimeoutMs: 120000（vs dev:600000）
  - defaultStepTimeoutMs: 60000（vs dev:180000）
  - 生产环境严重受限，可能影响吞吐量
- **建议修复**: 
  1. 评估并调整生产限制
  2. 与业务需求匹配
  3. 添加生产容量测试

#### 8. [源代码] [高严重] [多个服务存在无界 Map 无驱逐策略]
- **文件/路径**: 多个域服务
- **问题描述**: 
  - `domain-knowledge-schema-service.ts:73-75`: schemas, sourceContent, sourceTimestamps 无限制
  - `domain-eval-framework-service.ts:91-95`: frameworks, qualityAxes 等无限制
  - `domain-recipe-service.ts:67-68`: recipes, versions 无限制
  - `domain-risk-profile-service.ts:51`: profiles 无限制
  - Session Maps (session-management.ts:83-89) 无自动清理
- **建议修复**: 
  1. 为所有 Map 实现 LRU 或 TTL 驱逐策略
  2. 添加后台清理任务
  3. 添加大小监控和告警

#### 9. [源代码] [中严重] [taskEventHistory Map 永不清理]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:91,548-555`
- **问题描述**: 
  - taskEventHistory 限制单任务历史为 200 条
  - 但 Map 本身从不清理
  - 取消订阅的任务历史永久保留
- **建议修复**: 
  1. 实现后台清理无订阅者任务的历史
  2. 添加任务订阅者监控
  3. 考虑使用 WeakMap 替代

#### 10. [配置] [中严重] [安全配置 drift - remoteWorkerRegistration 缺失]
- **文件/路径**: `config/security/dev.json`, `staging.json`, `pre-prod.json`
- **问题描述**: 
  - default.json 有完整的 remoteWorkerRegistration 配置
  - dev/staging/pre-prod 只有 approvalMode
  - prod 有 approvalMode 但无 remoteWorkerRegistration
  - 安全配置不一致
- **建议修复**: 
  1. 统一所有环境的 remoteWorkerRegistration
  2. 添加安全配置验证
  3. 确保最低安全基线

#### 11. [API] [中严重] [OpenAPI 端点响应格式不一致]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/utils.ts:207-215`
- **问题描述**: 
  - `/v1/openapi.json` 使用 buildJsonDocumentResponse（无包装）
  - 其他端点使用 buildJsonResponse（{requestId, data} 包装）
  - 客户端体验不一致
- **建议修复**: 
  1. 统一响应信封格式
  2. 文档化响应格式规范
  3. 添加响应格式验证测试

#### 12. [源代码] [中严重] [Redis 队列缺少 idempotency 索引支持]
- **文件/路径**: `src/platform/five-plane-execution/queue/redis-queue-adapter.ts`
- **问题描述**: 
  - SQLite 有部分唯一索引支持 idempotency
  - Redis 实现使用哈希索引但实现不完整
  - 可能导致重复消息
- **建议修复**: 
  1. 完善 Redis idempotency 实现
  2. 添加唯一索引验证
  3. 确保与 SQLite 行为一致

#### 13. [源代码] [中严重] [请求去重中间件使用内存存储]
- **文件/路径**: `src/platform/five-plane-interface/api/middleware/request-deduplication.ts`
- **问题描述**: 
  - 纯内存滑动窗口（Map<DeduplicationKey, DeduplicationEntry[]>）
  - 无外部存储
  - 多实例部署不共享
  - 重启后去重状态丢失
- **建议修复**: 
  1. 使用 Redis 替代内存存储
  2. 支持分布式去重
  3. 持久化去重状态

#### 14. [源代码] [中严重] [Cache 无 stampede 保护]
- **文件/路径**: 多个 cache 实现
- **问题描述**: 
  - MemoryCacheStore, ExperienceCacheService 无锁
  - 缓存未命中时可发生 thundering herd
  - 高并发下可能导致数据库过载
- **建议修复**: 
  1. 实现 single-flight 模式
  2. 添加请求排队机制
  3. 使用分布式锁保护缓存更新

#### 15. [源代码] [中严重] [EvidenceService eviction 仅在插入时触发]
- **文件/路径**: `src/platform/five-plane-state-evidence/memory/evidence-service.ts:202`
- **问题描述**: 
  - 清理仅在 record() 调用时触发
  - 空闲时无后台清理
  - 可能导致内存持续增长
- **建议修复**: 
  1. 添加定期后台清理任务
  2. 使用独立的清理线程
  3. 添加内存使用监控

#### 16. [源代码] [低严重] [无连接指标暴露]
- **文件/路径**: WebSocket 服务器
- **问题描述**: 
  - getClientCount() 存在但未通过 HTTP 端点暴露
  - 无 pendingAcks 队列深度指标
  - 无连接建立告警
- **建议修复**: 
  1. 通过 metrics 端点暴露连接指标
  2. 添加 pendingAcks 队列监控
  3. 添加连接数异常告警

#### 17. [源代码] [低严重] [无空闲客户端超时]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts`
- **问题描述**: 
  - 仅在心跳 Sweep 时检查 isAlive
  - 无 per-client 独立于心跳的空闲超时
  - 认证后从不发送消息的客户端只能通过心跳失败检测
- **建议修复**: 
  1. 添加 per-client idle timeout
  2. 独立于心跳间隔
  3. 配置可调整

#### 18. [源代码] [低严重] [订阅限制是 per-client 而非全局]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:244`
- **问题描述**: 
  - MAX_SUBSCRIPTIONS_PER_CLIENT = 100
  - 恶意客户端可订阅 100 个任务
  - 无全局任务订阅者限制
- **建议修复**: 
  1. 添加全局任务订阅者限制
  2. 实现 per-task 订阅者上限
  3. 添加反滥用检测

#### 19. [配置] [低严重] [config/runtime 无版本验证机制]
- **文件/路径**: `config/runtime/*.json`
- **问题描述**: 
  - 配置有 "version": "v4.3"
  - 但无 schema 版本验证
  - 加载时可能接受不兼容配置
- **建议修复**: 
  1. 添加 JSON Schema 验证
  2. 实现版本兼容性检查
  3. 启动时验证配置完整性

#### 20. [源代码] [低严重] [ImprovementCandidateRegistry splice 是 O(n)]
- **文件/路径**: `src/platform/five-plane-orchestration/improve/improvement-candidate-registry.ts:243`
- **问题描述**: 
  - accessOrder 数组的 splice 操作是 O(n)
  - 高频访问时可能性能问题
- **建议修复**: 
  1. 使用 LinkedList 替代数组
  2. 或使用 Map 维护访问顺序
  3. 性能测试验证

### 总结

本次补充Review（第十三轮 - 缓存会话与实时通信Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. CORS allowedMethods 缺少 PUT/PATCH/DELETE（浏览器请求会失败）
2. Mission Routes 错误响应包装不当
3. WebSocketBridge 无最大连接数限制（DoS风险）
4. pendingAcks 断开连接时未清理（内存泄漏）
5. 硬编码回退密钥（安全漏洞）
6. config/runtime/test.json 缺少超时配置
7. prod.json maxConcurrentTasks=1 限制过严
8. 多个服务存在无界 Map（内存泄漏风险）

**中优先级**:
1. taskEventHistory Map 永不清理
2. 安全配置 drift（remoteWorkerRegistration 缺失）
3. OpenAPI 端点响应格式不一致
4. Redis 队列 idempotency 实现不完整
5. 请求去重中间件使用内存存储（多实例不共享）
6. Cache 无 stampede 保护
7. EvidenceService eviction 仅在插入时触发

**低优先级**:
1. 无连接指标暴露
2. 无空闲客户端超时
3. 订阅限制是 per-client 而非全局
4. config/runtime 无版本验证机制
5. ImprovementCandidateRegistry splice O(n)

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 64 | 74 | 35 | 173 |
| 测试 | 8 | 14 | 5 | 27 |
| 配置 | 10 | 38 | 24 | 72 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 7 | 3 | 12 |
| UI | 0 | 3 | 3 | 6 |
| 部署 | 1 | 10 | 6 | 17 |
| **合计** | **99** | **161** | **79** | **339** |

### 优先修复建议

**立即处理 (本周内)**:
1. 修复 CORS allowedMethods 添加 PUT/PATCH/DELETE
2. 修复 Mission Routes 错误响应格式
3. 为 WebSocketBridge 添加最大连接数限制
4. 清理 pendingAcks 和 taskEventHistory
5. 移除硬编码回退密钥
6. 修复 config/runtime/test.json 配置
7. 评估并调整 prod 并发限制

**短期内处理 (本月内)**:
1. 统一安全配置（remoteWorkerRegistration）
2. 实现无界 Map 的 LRU/TTL 驱逐
3. 统一 API 响应信封格式
4. 完善 Redis idempotency 实现
5. 添加分布式去重中间件
6. 实现 cache stampede 保护

**长期规划**:
1. 建立完整的连接管理和监控
2. 实现后台清理任务框架
3. 添加配置 schema 验证
4. 优化高频繁操作的数据结构
5. 建立内存使用基线和告警

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十五轮 - 构建测试与类型系统Review）

### 发现的新问题

#### 1. [构建] [高严重] [无 TypeScript 增量编译缓存]
- **文件/路径**: `tsconfig.json`
- **问题描述**: 
  - 无 `incremental: true` 配置
  - 无 `tsbuildinfo` 文件
  - 每次构建是完整重建（9.6秒）
  - TypeScript 无法使用增量编译
- **建议修复**: 
  1. 在 tsconfig.json 添加 `"incremental": true`
  2. 添加 `"tsbuildinfo": ".tsbuildinfo"` 到 .gitignore
  3. 实现项目引用以支持并行构建

#### 2. [构建] [高严重] [76个 npm scripts 大部分重复构建]
- **文件/路径**: `package.json`
- **问题描述**: 
  - 40+ CLI scripts 都运行 `npm run build && node ...`
  - 每个脚本前都执行完整重建
  - 无法直接运行预构建的 CLI
  - 20+ stable:* 脚本遵循相同模式
- **建议修复**: 
  1. 创建 `build:cli` 脚本构建一次
  2. 让 CLI 脚本使用预构建的 dist 文件
  3. 添加 npm run dev 或类似的无需重建的执行方式

#### 3. [源代码] [高严重] [168处 any 类型使用]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - `stable-runner-factory.ts`: `opts: any` -> `any`
  - `billing-service-async.ts`: `input: any` 方法签名
  - 重试/超时工具使用 `(...args: any[]) => Promise<any>`
  - 绕过类型安全
- **建议修复**: 
  1. 使用泛型替代 any
  2. 替换为具体的输入类型
  3. 添加 ESLint 规则禁止 any

#### 4. [源代码] [高严重] [38处 @ts-ignore 指令]
- **文件/路径**: 多个文件
- **问题描述**: 
  - 14个 stability 相关文件有 `@ts-ignore ExecutionRecord type mismatch`
  - 5个 harness-sdk 文件有 `Partial<HarnessRun> doesn't have all required properties`
  - 2个文件有 `exactOptionalPropertyTypes` 问题
- **建议修复**: 
  1. 修复 ExecutionRecord 类型不匹配
  2. 完成 HarnessRun 类型的必需属性
  3. 移除 @ts-ignore 使用

#### 5. [源代码] [高严重] [ExecutionRecord 类型不匹配]
- **文件/路径**: `src/platform/stability/stable-evidence-bundle-support.ts:783` 等14个文件
- **问题描述**: 
  - 插入 execution record 时 TypeScript 拒绝类型
  - `ExecutionRecord` 接口要求新字段但 insert 对象缺失
  - 或 store 的 insert 方法接受不同类型
- **建议修复**: 
  1. 对齐 ExecutionRecord 接口和 store 类型
  2. 添加缺失字段到 insert 对象
  3. 或调整 store 的 insert 方法类型签名

#### 6. [源代码] [中严重] [30+处 as unknown as 双类型转换]
- **文件/路径**: `division-loader.ts`, `domain-baseline-catalog.ts`, `harness-sdk`
- **问题描述**: 
  - 完全绕过类型安全
  - 通常是类型架构问题的信号
- **建议修复**: 
  1. 修复上游类型问题
  2. 使用更具体的类型断言
  3. 重构类型层次结构

#### 7. [构建] [中严重] [rimraf  extraneous - 未在 package.json 声明]
- **文件/路径**: `node_modules/rimraf`
- **问题描述**: 
  - rimraf@6.1.3 存在于 node_modules 但未在 package.json 声明
  - 可能导致依赖问题
- **建议修复**: 
  1. 添加到 dependencies 或 devDependencies
  2. 或移除并使用其他清理方式

#### 8. [测试] [高严重] [178处直接 process.env 变更无抽象]
- **文件/路径**: `tests/unit/domains/registry/plugin-spi-registry.test.ts:640-687`
- **问题描述**: 
  - 直接读取/写入 process.env
  - 在 beforeEach/afterEach 中手动保存/恢复
  - 无 helper 抽象
- **建议修复**: 
  1. 创建 env helper 函数
  2. 统一保存和恢复模式
  3. 添加测试隔离验证

#### 9. [测试] [高严重] [21,998处 unlinkSync 调用无集中清理]
- **文件/路径**: 多个测试文件
- **问题描述**: 
  - 每个测试重新实现清理逻辑
  - 无集中清理 utility
  - 可能导致 EBUSY (Windows) 或并发访问问题
- **建议修复**: 
  1. 创建集中的测试清理 utility
  2. 使用 afterEach 确保清理
  3. 考虑使用临时目录

#### 10. [测试] [中严重] [无统一 mocking 框架]
- **文件/路径**: `tests/` 多个目录
- **问题描述**: 
  - 10,579 处 vi.fn, jest.mock, sinon 等调用
  - 无统一的 mocking 框架可见
  - 模式不一致
- **建议修复**: 
  1. 建立统一的 mock factory
  2. 标准化 mock 模式
  3. 添加 mocking 最佳实践文档

#### 11. [测试] [中严重] [Singleton 状态重置未标准化]
- **文件/路径**: `tests/unit/platform/five-plane-execution/dispatcher.test.ts`
- **问题描述**: 
  - 测试通过特定函数重置 singletons
  - 每个测试重复自己的 reset 逻辑
  - 无统一的 resetAllSingletons()
- **建议修复**: 
  1. 创建统一的 singleton reset 机制
  2. 在 afterEach 中自动调用
  3. 文档化 singleton 重置要求

#### 12. [测试] [低严重] [无分支覆盖率要求]
- **文件/路径**: `.c8rc.json`
- **问题描述**: 
  - `"100": false` - 无覆盖率要求
  - 只测量源代码，不测量测试文件
- **建议修复**: 
  1. 添加分支覆盖率阈值
  2. 在 CI 中强制执行覆盖率 gate
  3. 关注关键代码路径

#### 13. [网络] [高严重] [ScopedExternalAccessSandbox performHttpRequest 无超时]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/scoped-external-access-sandbox.ts:289`
- **问题描述**: 
  - fetch 调用无 timeout 参数
  - 挂起的请求可能永久阻塞
- **建议修复**: 
  1. 添加 AbortController timeout
  2. 使用标准的超时模式
  3. 记录超时配置

#### 14. [网络] [高严重] [OIDC 服务 fetch 调用无超时]
- **文件/路径**: `src/org-governance/sso-scim/oidc/oidc-service.ts:565,272`
- **问题描述**: 
  - OIDC token 交换无 retry logic
  - 无 timeout/abort signal
  - 认证流程可能挂起
- **建议修复**: 
  1. 添加 fetch timeout
  2. 添加 retry logic
  3. 使用标准的超时配置

#### 15. [网络] [高严重] [plugin-runtime-child 替换 globalThis.fetch 为 no-op]
- **文件/路径**: `src/domains/registry/plugin-runtime-child.ts:112`
- **问题描述**: 
  ```typescript
  globalThis.fetch = (async () => {}) as unknown as typeof globalThis.fetch;
  ```
  - 插件上下文中完全替换 fetch 为空函数
  - 插件的所有出站 HTTP 请求都会失败
  - 看起来是有意为之但可能造成问题
- **建议修复**: 
  1. 确认这是有意行为
  2. 文档化 fetch 替换行为
  3. 确保不会意外影响其他代码

#### 16. [网络] [中严重] [无显式 http.Agent 配置]
- **文件/路径**: 全局
- **问题描述**: 
  - 无显式的 http.Agent/https.Agent 配置
  - 依赖 Node.js 默认连接池
  - 无 maxSockets 或 maxConnections 配置
  - 高吞吐场景可能出问题
- **建议修复**: 
  1. 添加 http Agent 配置
  2. 文档化连接池大小
  3. 监控连接使用情况

#### 17. [序列化] [中严重] [合同版本硬编码为字符串字面量]
- **文件/路径**: `src/platform/contracts/inter-plane-contract-gateway.ts:184`
- **问题描述**: 
  - `schemaVersion: "v4.3"` 硬编码
  - 难以追踪合同版本演变
- **建议修复**: 
  1. 提取到类型化常量或 enum
  2. 集中管理合同版本
  3. 添加版本兼容性检查

#### 18. [序列化] [低严重] [Timestamp = string 丢失精度]
- **文件/路径**: `src/platform/contracts/types/domain/primitives.ts:10`
- **问题描述**: 
  - `type Timestamp = string` 无类型精度
  - ISO 8601 字符串无时区信息
- **建议修复**: 
  1. 使用 Date 对象或 branded string 类型
  2. 明确时区处理约定
  3. 文档化时间戳语义

#### 19. [源代码] [中严重] [29处非空断言 !.]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - 使用 `!.` 断言非空
  - 可能导致运行时错误
- **建议修复**: 
  1. 使用可选链 `?.` 或空值合并 `??`
  2. 添加适当的 null 检查
  3. 减少非空断言使用

#### 20. [API] [中严重] [API 路由 schema 版本硬编码]
- **文件/路径**: `src/platform/five-plane-interface/api/federation-routing-service.ts`
- **问题描述**: 
  - Zod schema 验证存在
  - 但 schema 版本未与 contract 版本同步
- **建议修复**: 
  1. 从 contract 版本派生 schema 版本
  2. 添加版本一致性检查
  3. 文档化版本关系

### 总结

本次补充Review（第十五轮 - 构建测试与类型系统Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. 无 TypeScript 增量编译缓存（每次完整重建9.6秒）
2. 76个 npm scripts 大部分重复构建
3. 168处 any 类型使用（绕过类型安全）
4. 38处 @ts-ignore 指令
5. ExecutionRecord 类型不匹配（14个文件）
6. 178处直接 process.env 变更无抽象
7. ScopedExternalAccessSandbox performHttpRequest 无超时
8. OIDC 服务 fetch 调用无超时
9. plugin-runtime-child 替换 globalThis.fetch 为 no-op

**中优先级**:
1. 30+处 as unknown as 双类型转换
2. rimraf extraneous 未声明
3. 21,998处 unlinkSync 无集中清理
4. 无统一 mocking 框架
5. Singleton 状态重置未标准化
6. 无显式 http.Agent 配置
7. 合同版本硬编码为字符串
8. Timestamp = string 丢失精度
9. 非空断言使用

**低优先级**:
1. 无分支覆盖率要求
2. API 路由 schema 版本未同步

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 72 | 87 | 39 | 198 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **109** | **182** | **85** | **376** |

### 优先修复建议

**立即处理 (本周内)**:
1. 添加 TypeScript 增量编译 (`incremental: true`)
2. 重构 npm scripts 避免重复构建
3. 减少 any 类型使用到 <50
4. 移除所有 @ts-ignore 指令
5. 修复 ExecutionRecord 类型不匹配
6. 添加 fetch 超时到 OIDC 和 sandbox

**短期内处理 (本月内)**:
1. 创建集中的测试清理 utility
2. 建立统一的 mocking 框架
3. 标准化 singleton 重置机制
4. 添加 http.Agent 连接池配置
5. 提取合同版本到类型化常量

**长期规划**:
1. 实现完整的类型安全（无 any）
2. 建立测试基础设施标准
3. 实现构建缓存优化
4. 完善网络层超时和重试
5. 建立覆盖率门禁标准

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十四轮 - 日志事件与工作流Review）

### 发现的新问题

#### 1. [可观测性] [高严重] [StructuredLogEntry 无 requestId 字段]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - StructuredLogEntry 接口无 requestId 字段
  - 只有 correlationId，它回退到 traceId
  - 无法独立追踪单个 HTTP 请求
- **建议修复**: 
  1. 在 StructuredLogEntry 添加 requestId 字段
  2. 在 HTTP 中间件中生成和注入 requestId
  3. 确保 requestId 流经所有日志调用

#### 2. [可观测性] [高严重] [StructuredLogger data 字段无自动清洗]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - 如果开发者传递 `{ password: "..." }` 给 logger.info，密码会被记录
  - 无字段级脱敏机制
  - 依赖开发者手动排除敏感字段
- **建议修复**: 
  1. 添加 OBSERVE_OUTPUT_BLACKLIST 类似的黑名单
  2. 实现 redact/mask/sanitize 工具函数
  3. 在日志传输前自动清洗敏感字段

#### 3. [可观测性] [中严重] [日志中敏感数据无 PII 处理]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - 无 PII 重写工具
  - 开发者必须手动避免记录敏感数据
  - 存在意外记录敏感信息的风险
- **建议修复**: 
  1. 添加 PII 检测和重写工具
  2. 在文档中明确记录敏感字段列表
  3. 添加测试验证敏感数据不被记录

#### 4. [可观测性] [中严重] [59处 console.* 直接调用]
- **文件/路径**: 多个源文件
- **问题描述**: 
  - 应该使用 StructuredLogger
  - console 调用可能不被日志聚合系统收集
  - 难以追踪和关联
- **建议修复**: 
  1. 替换所有 console.* 为 StructuredLogger
  2. 添加 ESLint 规则禁止 console.*
  3. 保留少量允许的 CLI 输出

#### 5. [可观测性] [中严重] [部分 logger.warn 调用缺少结构化数据]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:122`
- **问题描述**: 
  ```typescript
  logger.warn("WebSocket connection rejected: missing subprotocol token")
  ```
  - 只有消息，无 data 对象
  - 难以关联和查询
- **建议修复**: 
  1. 添加 `{ data: { ... } }` 对象
  2. 确保所有日志包含上下文数据
  3. 添加日志审查工具

#### 6. [源代码] [高严重] [Harness while(true) 循环无硬性迭代上限]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:1442`
- **问题描述**: 
  ```typescript
  while (true) {
    // ...
    // 依赖 budget gate 和 guardrail vibration 退出
    // 如果两者都失败，循环永久继续
  }
  ```
  - 依赖 budget gate 和 guardrail vibration 退出
  - 无硬性迭代次数限制
  - 可能导致无限循环
- **建议修复**: 
  1. 添加硬性 maxIterations 上限
  2. 在达到上限时强制退出
  3. 记录详细的迭代统计用于调试

#### 7. [源代码] [高严重] [Oapeflir while(true) 循环可能无限重规划]
- **文件/路径**: `src/platform/five-plane-orchestration/oapeflir/oapeflir-loop-service.ts:355`
- **问题描述**: 
  - 如果 loopReplanDecision.shouldReplan 永不变为 false
  - 重规划永远无法减少计划错误
  - 可能导致无限循环
- **建议修复**: 
  1. 添加重规划次数上限
  2. 在达到上限时强制接受当前结果
  3. 添加重规划质量衰减检测

#### 8. [源代码] [中严重] [Recovery 流程允许终态转换到 paused]
- **文件/路径**: `src/platform/five-plane-execution/runtime-state-machine.ts:105-108`
- **问题描述**: 
  - completed, failed, cancelled, aborted 可转换到 paused 进行恢复
  - 创建恢复路径可能重新进入执行
  - 需要验证此行为的正确性
- **建议修复**: 
  1. 审查 recovery 转换的合法性
  2. 确保不会导致状态不一致
  3. 添加转换前置条件验证

#### 9. [国际化] [高严重] [API 错误消息硬编码英文]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/api-error.ts`
- **问题描述**: 
  ```typescript
  return new ApiError(404, "api.task_not_found", "Task not found.");
  ```
  - 错误消息硬编码英文
  - 无后端 i18n 机制
  - 非英文客户端收到不本地化的错误
- **建议修复**: 
  1. 使用错误码而非硬编码消息
  2. 客户端根据错误码本地化
  3. 或在后端添加 i18n 支持

#### 10. [国际化] [中严重] [UI 硬编码字符串未翻译]
- **文件/路径**: `ui/packages/features/conversation/src/web/index.tsx`
- **问题描述**: 
  - "Build Plan", "Execute", "Messages" 等按钮标签硬编码
  - 不使用 i18n 系统
- **建议修复**: 
  1. 使用 `translateFeatureCopy` 替换硬编码
  2. 确保所有用户可见文本通过 i18n
  3. 添加硬编码字符串检测

#### 11. [国际化] [中严重] [Arabic 目录不完整]
- **文件/路径**: `ui/packages/shared/i18n/src/catalogs/ar-SA.ts`
- **问题描述**: 
  - ar-SA 只有约18条消息
  - en-US 和 zh-CN 有约117条
  - 缺失的 key 返回 key 本身
- **建议修复**: 
  1. 补充 Arabic 翻译
  2. 确保所有 key 都有翻译
  3. 添加翻译完整性测试

#### 12. [国际化] [低严重] [translateFeatureCopy 无回退默认值]
- **文件/路径**: `ui/packages/shared/i18n/`
- **问题描述**: 
  - 如果 feature ID 不在 catalog 中
  - 返回 undefined title/summary
  - 无合理回退
- **建议修复**: 
  1. 添加回退到 key 或默认字符串
  2. 或使用 feature ID 作为显示名
  3. 确保 UI 不显示 undefined

#### 13. [运维] [高严重] [健康检查无 readiness/liveness 区分]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts`
- **问题描述**: 
  - /healthz, /v1/healthz, /health 返回相同报告
  - 无 readiness probe（可处理流量？）
  - 无 liveness probe（系统存活？）
  - canAcceptTraffic() 未通过 HTTP 暴露
- **建议修复**: 
  1. 添加 /ready 和 /live 端点
  2. Readiness 检查依赖项就绪
  3. Liveness 检查进程健康

#### 14. [运维] [高严重] [shutdown 期间健康检查存在竞态]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts`
- **问题描述**: 
  - getHealthReportAsync() 异步检查 dbWritable
  - shutdown 期间数据库连接可能关闭
  - 健康检查仍在传入
- **建议修复**: 
  1. 在 shutdown 时返回 unhealthy
  2. 添加 shutdown 状态标志
  3. 停止接受新的健康检查请求

#### 15. [运维] [高严重] [LeaderElectionService 未与 GracefulShutdown 集成]
- **文件/路径**: `src/platform/five-plane-execution/ha/leader-election-service.ts`
- **问题描述**: 
  - LeaderElectionService.stop() 处理优雅退位
  - 但未在全局 graceful shutdown 中注册
  - 需显式注册为 shutdown handler
- **建议修复**: 
  1. 将 LeaderElectionService 添加到 shutdown handlers
  2. 确保领导者在 shutdown 时让位
  3. 验证关闭顺序正确

#### 16. [运维] [中严重] [addHandler() 无显式排序约定]
- **文件/路径**: `src/platform/five-plane-execution/startup/graceful-shutdown.ts`
- **问题描述**: 
  - 虽然 handlers 逆序执行
  - 但无显式接口声明 handler 依赖关系
  - 关键 handlers（如关闭数据库连接）应最后运行
- **建议修复**: 
  1. 添加优先级或阶段参数
  2. 文档化关键 handler 顺序要求
  3. 添加顺序验证测试

#### 17. [运维] [中严重] [unref'd timers 可能不触发]
- **文件/路径**: `src/platform/five-plane-execution/startup/process-error-handlers.ts:106`
- **问题描述**: 
  - `setTimeout(...).unref()` 不保持事件循环活跃
  - 如果主线程空闲，强制退出可能不触发
- **建议修复**: 
  1. 移除 .unref() 除非确实需要
  2. 或确保有其他方式保持事件循环
  3. 添加超时触发验证测试

#### 18. [运维] [中严重] [StartupConsistencyChecker 阻塞流量但不暴露状态]
- **文件/路径**: `src/platform/five-plane-startup-plan.ts`
- **问题描述**: 
  - canAcceptTraffic() 在 P0 问题存在时返回 false
  - 但 _trafficBlocked 是内部状态
  - 负载均衡器访问 /healthz 仍会得到 "ok"
- **建议修复**: 
  1. 在健康报告中暴露 trafficBlocked 状态
  2. 或添加单独的健康端点指示就绪状态
  3. 确保负载均衡器在阻塞时不路由流量

#### 19. [事件系统] [中严重] [TypedEventBus vs EventEmitter 混用]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - 项目同时使用 TypedEventBus 和原生 EventEmitter
  - TypedEventBus 应该是标准
  - 混用可能导致类型安全问题
- **建议修复**: 
  1. 创建迁移计划统一使用 TypedEventBus
  2. 添加 ESLint 规则禁止原生 EventEmitter
  3. 在 CI 中检测混用情况

#### 20. [源代码] [低严重] [无事件排序保证]
- **文件/路径**: `src/platform/five-plane-state-evidence/events/`
- **问题描述**: 
  - 无明确的事件排序保证文档
  - 事件处理可能乱序
  - 依赖方需要处理乱序
- **建议修复**: 
  1. 文档化事件排序语义
  2. 如果需要排序，实现序列号机制
  3. 在事件处理中考虑乱序情况

### 总结

本次补充Review（第十四轮 - 日志事件与工作流Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. StructuredLogEntry 无 requestId 字段
2. StructuredLogger data 字段无自动清洗（敏感数据风险）
3. Harness while(true) 循环无硬性迭代上限
4. Oapeflir while(true) 循环可能无限重规划
5. API 错误消息硬编码英文（无 i18n）
6. 健康检查无 readiness/liveness 区分
7. shutdown 期间健康检查存在竞态
8. LeaderElectionService 未与 GracefulShutdown 集成

**中优先级**:
1. 日志中敏感数据无 PII 处理
2. 59处 console.* 直接调用
3. 部分 logger.warn 调用缺少结构化数据
4. Recovery 流程允许终态转换到 paused
5. UI 硬编码字符串未翻译
6. Arabic 目录不完整
7. addHandler() 无显式排序约定
8. unref'd timers 可能不触发
9. StartupConsistencyChecker 阻塞流量但不暴露状态
10. TypedEventBus vs EventEmitter 混用

**低优先级**:
1. translateFeatureCopy 无回退默认值
2. 无事件排序保证

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 68 | 80 | 37 | 185 |
| 测试 | 8 | 14 | 5 | 27 |
| 配置 | 10 | 38 | 24 | 72 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **103** | **171** | **82** | **356** |

### 优先修复建议

**立即处理 (本周内)**:
1. 在 StructuredLogEntry 添加 requestId 字段
2. 实现日志敏感字段自动清洗
3. 为工作流循环添加硬性迭代上限
4. 添加 readiness/liveness 健康检查端点
5. 将 LeaderElectionService 集成到 GracefulShutdown
6. 实现 API 错误消息 i18n 支持

**短期内处理 (本月内)**:
1. 替换所有 console.* 为 StructuredLogger
2. 补充 Arabic i18n 目录
3. 修复 UI 硬编码字符串
4. 统一 TypedEventBus 替代 EventEmitter
5. 解决 shutdown 竞态条件
6. 添加事件排序保证文档

**长期规划**:
1. 建立完整的 PII 处理框架
2. 实现日志完整性验证
3. 建立工作流安全循环检测
4. 完善健康检查和就绪探测
5. 建立国际化完整测试

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十五轮 - 构建测试与类型系统Review）

### 发现的新问题

#### 1. [构建] [高严重] [无 TypeScript 增量编译缓存]
- **文件/路径**: `tsconfig.json`
- **问题描述**: 
  - 无 `incremental: true` 配置
  - 无 `tsbuildinfo` 文件
  - 每次构建是完整重建（9.6秒）
  - TypeScript 无法使用增量编译
- **建议修复**: 
  1. 在 tsconfig.json 添加 `"incremental": true`
  2. 添加 `"tsbuildinfo": ".tsbuildinfo"` 到 .gitignore
  3. 实现项目引用以支持并行构建

#### 2. [构建] [高严重] [76个 npm scripts 大部分重复构建]
- **文件/路径**: `package.json`
- **问题描述**: 
  - 40+ CLI scripts 都运行 `npm run build && node ...`
  - 每个脚本前都执行完整重建
  - 无法直接运行预构建的 CLI
  - 20+ stable:* 脚本遵循相同模式
- **建议修复**: 
  1. 创建 `build:cli` 脚本构建一次
  2. 让 CLI 脚本使用预构建的 dist 文件
  3. 添加 npm run dev 或类似的无需重建的执行方式

#### 3. [源代码] [高严重] [168处 any 类型使用]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - `stable-runner-factory.ts`: `opts: any` -> `any`
  - `billing-service-async.ts`: `input: any` 方法签名
  - 重试/超时工具使用 `(...args: any[]) => Promise<any>`
  - 绕过类型安全
- **建议修复**: 
  1. 使用泛型替代 any
  2. 替换为具体的输入类型
  3. 添加 ESLint 规则禁止 any

#### 4. [源代码] [高严重] [38处 @ts-ignore 指令]
- **文件/路径**: 多个文件
- **问题描述**: 
  - 14个 stability 相关文件有 `@ts-ignore ExecutionRecord type mismatch`
  - 5个 harness-sdk 文件有 `Partial<HarnessRun> doesn't have all required properties`
  - 2个文件有 `exactOptionalPropertyTypes` 问题
- **建议修复**: 
  1. 修复 ExecutionRecord 类型不匹配
  2. 完成 HarnessRun 类型的必需属性
  3. 移除 @ts-ignore 使用

#### 5. [源代码] [高严重] [ExecutionRecord 类型不匹配]
- **文件/路径**: `src/platform/stability/stable-evidence-bundle-support.ts:783` 等14个文件
- **问题描述**: 
  - 插入 execution record 时 TypeScript 拒绝类型
  - `ExecutionRecord` 接口要求新字段但 insert 对象缺失
  - 或 store 的 insert 方法接受不同类型
- **建议修复**: 
  1. 对齐 ExecutionRecord 接口和 store 类型
  2. 添加缺失字段到 insert 对象
  3. 或调整 store 的 insert 方法类型签名

#### 6. [源代码] [中严重] [30+处 as unknown as 双类型转换]
- **文件/路径**: `division-loader.ts`, `domain-baseline-catalog.ts`, `harness-sdk`
- **问题描述**: 
  - 完全绕过类型安全
  - 通常是类型架构问题的信号
- **建议修复**: 
  1. 修复上游类型问题
  2. 使用更具体的类型断言
  3. 重构类型层次结构

#### 7. [构建] [中严重] [rimraf  extraneous - 未在 package.json 声明]
- **文件/路径**: `node_modules/rimraf`
- **问题描述**: 
  - rimraf@6.1.3 存在于 node_modules 但未在 package.json 声明
  - 可能导致依赖问题
- **建议修复**: 
  1. 添加到 dependencies 或 devDependencies
  2. 或移除并使用其他清理方式

#### 8. [测试] [高严重] [178处直接 process.env 变更无抽象]
- **文件/路径**: `tests/unit/domains/registry/plugin-spi-registry.test.ts:640-687`
- **问题描述**: 
  - 直接读取/写入 process.env
  - 在 beforeEach/afterEach 中手动保存/恢复
  - 无 helper 抽象
- **建议修复**: 
  1. 创建 env helper 函数
  2. 统一保存和恢复模式
  3. 添加测试隔离验证

#### 9. [测试] [高严重] [21,998处 unlinkSync 调用无集中清理]
- **文件/路径**: 多个测试文件
- **问题描述**: 
  - 每个测试重新实现清理逻辑
  - 无集中清理 utility
  - 可能导致 EBUSY (Windows) 或并发访问问题
- **建议修复**: 
  1. 创建集中的测试清理 utility
  2. 使用 afterEach 确保清理
  3. 考虑使用临时目录

#### 10. [测试] [中严重] [无统一 mocking 框架]
- **文件/路径**: `tests/` 多个目录
- **问题描述**: 
  - 10,579 处 vi.fn, jest.mock, sinon 等调用
  - 无统一的 mocking 框架可见
  - 模式不一致
- **建议修复**: 
  1. 建立统一的 mock factory
  2. 标准化 mock 模式
  3. 添加 mocking 最佳实践文档

#### 11. [测试] [中严重] [Singleton 状态重置未标准化]
- **文件/路径**: `tests/unit/platform/five-plane-execution/dispatcher.test.ts`
- **问题描述**: 
  - 测试通过特定函数重置 singletons
  - 每个测试重复自己的 reset 逻辑
  - 无统一的 resetAllSingletons()
- **建议修复**: 
  1. 创建统一的 singleton reset 机制
  2. 在 afterEach 中自动调用
  3. 文档化 singleton 重置要求

#### 12. [测试] [低严重] [无分支覆盖率要求]
- **文件/路径**: `.c8rc.json`
- **问题描述**: 
  - `"100": false` - 无覆盖率要求
  - 只测量源代码，不测量测试文件
- **建议修复**: 
  1. 添加分支覆盖率阈值
  2. 在 CI 中强制执行覆盖率 gate
  3. 关注关键代码路径

#### 13. [网络] [高严重] [ScopedExternalAccessSandbox performHttpRequest 无超时]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/scoped-external-access-sandbox.ts:289`
- **问题描述**: 
  - fetch 调用无 timeout 参数
  - 挂起的请求可能永久阻塞
- **建议修复**: 
  1. 添加 AbortController timeout
  2. 使用标准的超时模式
  3. 记录超时配置

#### 14. [网络] [高严重] [OIDC 服务 fetch 调用无超时]
- **文件/路径**: `src/org-governance/sso-scim/oidc/oidc-service.ts:565,272`
- **问题描述**: 
  - OIDC token 交换无 retry logic
  - 无 timeout/abort signal
  - 认证流程可能挂起
- **建议修复**: 
  1. 添加 fetch timeout
  2. 添加 retry logic
  3. 使用标准的超时配置

#### 15. [网络] [高严重] [plugin-runtime-child 替换 globalThis.fetch 为 no-op]
- **文件/路径**: `src/domains/registry/plugin-runtime-child.ts:112`
- **问题描述**: 
  ```typescript
  globalThis.fetch = (async () => {}) as unknown as typeof globalThis.fetch;
  ```
  - 插件上下文中完全替换 fetch 为空函数
  - 插件的所有出站 HTTP 请求都会失败
  - 看起来是有意为之但可能造成问题
- **建议修复**: 
  1. 确认这是有意行为
  2. 文档化 fetch 替换行为
  3. 确保不会意外影响其他代码

#### 16. [网络] [中严重] [无显式 http.Agent 配置]
- **文件/路径**: 全局
- **问题描述**: 
  - 无显式的 http.Agent/https.Agent 配置
  - 依赖 Node.js 默认连接池
  - 无 maxSockets 或 maxConnections 配置
  - 高吞吐场景可能出问题
- **建议修复**: 
  1. 添加 http Agent 配置
  2. 文档化连接池大小
  3. 监控连接使用情况

#### 17. [序列化] [中严重] [合同版本硬编码为字符串字面量]
- **文件/路径**: `src/platform/contracts/inter-plane-contract-gateway.ts:184`
- **问题描述**: 
  - `schemaVersion: "v4.3"` 硬编码
  - 难以追踪合同版本演变
- **建议修复**: 
  1. 提取到类型化常量或 enum
  2. 集中管理合同版本
  3. 添加版本兼容性检查

#### 18. [序列化] [低严重] [Timestamp = string 丢失精度]
- **文件/路径**: `src/platform/contracts/types/domain/primitives.ts:10`
- **问题描述**: 
  - `type Timestamp = string` 无类型精度
  - ISO 8601 字符串无时区信息
- **建议修复**: 
  1. 使用 Date 对象或 branded string 类型
  2. 明确时区处理约定
  3. 文档化时间戳语义

#### 19. [源代码] [中严重] [29处非空断言 !.]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - 使用 `!.` 断言非空
  - 可能导致运行时错误
- **建议修复**: 
  1. 使用可选链 `?.` 或空值合并 `??`
  2. 添加适当的 null 检查
  3. 减少非空断言使用

#### 20. [API] [中严重] [API 路由 schema 版本硬编码]
- **文件/路径**: `src/platform/five-plane-interface/api/federation-routing-service.ts`
- **问题描述**: 
  - Zod schema 验证存在
  - 但 schema 版本未与 contract 版本同步
- **建议修复**: 
  1. 从 contract 版本派生 schema 版本
  2. 添加版本一致性检查
  3. 文档化版本关系

### 总结

本次补充Review（第十五轮 - 构建测试与类型系统Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. 无 TypeScript 增量编译缓存（每次完整重建9.6秒）
2. 76个 npm scripts 大部分重复构建
3. 168处 any 类型使用（绕过类型安全）
4. 38处 @ts-ignore 指令
5. ExecutionRecord 类型不匹配（14个文件）
6. 178处直接 process.env 变更无抽象
7. ScopedExternalAccessSandbox performHttpRequest 无超时
8. OIDC 服务 fetch 调用无超时
9. plugin-runtime-child 替换 globalThis.fetch 为 no-op

**中优先级**:
1. 30+处 as unknown as 双类型转换
2. rimraf extraneous 未声明
3. 21,998处 unlinkSync 无集中清理
4. 无统一 mocking 框架
5. Singleton 状态重置未标准化
6. 无显式 http.Agent 配置
7. 合同版本硬编码为字符串
8. Timestamp = string 丢失精度
9. 非空断言使用

**低优先级**:
1. 无分支覆盖率要求
2. API 路由 schema 版本未同步

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 72 | 87 | 39 | 198 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **109** | **182** | **85** | **376** |

### 优先修复建议

**立即处理 (本周内)**:
1. 添加 TypeScript 增量编译 (`incremental: true`)
2. 重构 npm scripts 避免重复构建
3. 减少 any 类型使用到 <50
4. 移除所有 @ts-ignore 指令
5. 修复 ExecutionRecord 类型不匹配
6. 添加 fetch 超时到 OIDC 和 sandbox

**短期内处理 (本月内)**:
1. 创建集中的测试清理 utility
2. 建立统一的 mocking 框架
3. 标准化 singleton 重置机制
4. 添加 http.Agent 连接池配置
5. 提取合同版本到类型化常量

**长期规划**:
1. 实现完整的类型安全（无 any）
2. 建立测试基础设施标准
3. 实现构建缓存优化
4. 完善网络层超时和重试
5. 建立覆盖率门禁标准

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十一轮 - 并发安全与可观测性Review）

### 发现的新问题

#### 1. [源代码] [严重] [FencingTokenService 静态可变状态无锁保护]
- **文件/路径**: `src/platform/five-plane-state-evidence/events/cas/fencing-token-service.ts:71-73`
- **问题描述**: 
  ```typescript
  private static readonly activeFences = new Map<string, FenceInfo>();
  private static globalTokenCounter = 0;
  ```
  - `acquireFence` 方法执行非原子的 read-then-write 模式
  - 两个并发调用可能同时通过检查，导致状态不一致
  - 静态 Map 在无锁情况下被并发读写
- **建议修复**: 
  1. 添加互斥锁保护 activeFences 访问
  2. 使用原子操作或 Compare-And-Swap
  3. 考虑使用数据库行锁替代内存锁

#### 2. [源代码] [严重] [AsyncFencingTokenService.globalTokenCounter 不安全递增]
- **文件/路径**: `src/platform/five-plane-state-evidence/events/cas/postgres-fencing-token-service.ts:13-17`
- **问题描述**: 
  ```typescript
  private static readonly globalTokenCounter = {
    value: 0,
    getAndIncrement(): number { return ++this.value; }
  };
  ```
  - 经典的 read-modify-write 竞态条件
  - 多线程可能读到相同的值
- **建议修复**: 
  1. 使用 `pg_advisory_xact_lock` 保护
  2. 或使用数据库自增序列
  3. 移除静态计数器，使用数据库生成

#### 3. [源代码] [严重] [BudgetAllocator.activeReservations 无保护 Map]
- **文件/路径**: `src/platform/five-plane-execution/budget-allocator.ts:394,426,458,467,569,657,711`
- **问题描述**: 
  ```typescript
  this.activeReservations.set(result.reservation.budgetReservationId, result.reservation);
  this.activeReservations.delete(reservationId);
  ```
  - 并发代码路径修改 activeReservations Map
  - 无锁保护
- **建议修复**: 
  1. 添加 Map 级别的读写锁
  2. 使用 ConcurrentMap 或类似结构
  3. 重构为无状态设计

#### 4. [源代码] [高严重] [SqliteLockAdapter.fencingCounter 不同步]
- **文件/路径**: `src/platform/five-plane-execution/distributed-lock/sqlite-lock-adapter.ts:9,14,48,74,88`
- **问题描述**: 
  - `fencingCounter` 在 acquire/extend/forceSteal 中递增
  - 无同步机制
- **建议修复**: 
  1. 使用 SQLite 序列替代内存计数器
  2. 或添加适当的同步机制

#### 5. [源代码] [高严重] [ServiceRegistry.getInstance() 检查-then-创建 竞态]
- **文件/路径**: `src/platform/shared/lifecycle/service-registry.ts:85-88`
- **问题描述**: 
  ```typescript
  public static getInstance(): ServiceRegistry {
    if (!ServiceRegistry._instance) {
      ServiceRegistry._instance = new ServiceRegistry();
    }
    return ServiceRegistry._instance;
  }
  ```
  - 两个线程可能同时看到 _instance 为 null
  - 创建两个实例
- **建议修复**: 
  1. 使用 double-checked locking
  2. 或使用 ES2022 静态字段初始化
  3. `private static readonly _instance = new ServiceRegistry();`

#### 6. [源代码] [中严重] [StructuredLogger.rotationStateByPath 异步竞态]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts:142-144,438-453`
- **问题描述**: 
  ```typescript
  private static rotationStateByPath = new Map<string, {...}>();
  ```
  - `writeToGlobalFileSink` 中存在 TOCTOU 竞态
  - 读取 pendingBytes 和实际写入之间可能被其他调用修改
- **建议修复**: 
  1. 使用锁保护 rotationStateByPath
  2. 或使用单线程写入队列

#### 7. [源代码] [中严重] [EffectBuffer.scopes 并发修改风险]
- **文件/路径**: `src/platform/five-plane-execution/execution-engine/effect-buffer.ts:446,452,476,485,492,502-514`
- **问题描述**: 
  - `scopes.delete(scopeId)` 与 `for (const [scopeId, scope] of this.scopes)` 并发
  - flush() 和 createScope 可能同时修改
- **建议修复**: 
  1. 添加迭代锁
  2. 使用 CopyOnWrite 模式
  3. 在修改时复制一份 scopes

#### 8. [源代码] [中严重] [OIDC _skipSignatureVerification 标志存在风险]
- **文件/路径**: `src/platform/five-plane-interface/api/oidc-oauth-service.ts:92,108-111`
- **问题描述**: 
  - `_skipSignatureVerification` 标志存在于生产代码路径
  - 虽然标记为仅测试使用，但配置错误可能导致安全问题
- **建议修复**: 
  1. 移除此标志或确保编译时完全删除
  2. 添加生产环境检查抛出错误
  3. 在代码审查中标记为禁止模式

#### 9. [源代码] [中严重] [Browser Executor 检测但不消毒 innerHTML]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/browser-executor.ts:541`
- **问题描述**: 
  - 检测到 innerHTML 但只返回假数据
  - 不进行实际消毒
  - 用户可能绕过保护
- **建议修复**: 
  1. 使用 DOMPurify 或类似库消毒
  2. 或完全禁止 innerHTML
  3. 返回错误而非假数据

#### 10. [数据库] [高严重] [runtime-physical-schema 无外键约束]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/runtime-physical-schema.ts`
- **问题描述**: 
  - `task_draft_id`, `confirmed_task_spec_id`, `harness_run_id` 等列无 FK 约束
  - 可能导致孤儿记录
  - 删除父记录时子记录变为孤立
- **建议修复**: 
  1. 添加外键约束
  2. 实现软删除而非硬删除
  3. 添加数据库迁移脚本

#### 11. [数据库] [中严重] [大部分表缺少软删除和审计字段]
- **文件/路径**: 多个表（除 mission_records 外）
- **问题描述**: 
  - 只有 mission_records 有完整审计：created_at, created_by, updated_at, updated_by, archived_at, archived_by
  - 大部分表缺少 deleted_at, is_deleted 软删除字段
  - created_by/updated_by 仅在 mission_records 系列表中存在
- **建议修复**: 
  1. 为所有核心表添加审计字段
  2. 实现软删除模式
  3. 添加规范说明哪些表必须有审计

#### 12. [CI/CD] [高严重] [UI quality workflow 无覆盖率门禁]
- **文件/路径**: `.github/workflows/ui-quality.yml:33`
- **问题描述**: 
  - 运行 `test:coverage` 但无阈值执行
  - 缺少 `coverage:gate`
  - 与主 CI 不一致
- **建议修复**: 
  1. 添加 `npm run coverage:gate` 到 UI workflow
  2. 或确保 UI 更改时运行主 CI

#### 13. [CI/CD] [高严重] [部署工作流无真正的手动审批]
- **文件/路径**: `.github/workflows/deploy-environment.yml`
- **问题描述**: 
  - `workflow_dispatch` 有环境保护但无显式审批步骤
  - deploy.sh 的生产确认使用 `read -p` 在 CI/CD 中会挂起
  - 生产部署缺少真正的审批控制
- **建议修复**: 
  1. 使用 GitHub Environment `required_reviewers`
  2. 修改 deploy.sh 检测 CI 环境跳过交互
  3. 添加显式审批步骤

#### 14. [CI/CD] [中严重] [回滚机制边界情况未处理]
- **文件/路径**: `.github/workflows/deploy-environment.yml:230-278`, `deploy/scripts/rollback.sh`
- **问题描述**: 
  - 部署成功但 health check 失败时不会触发回滚
  - rollback.sh 只支持 dev|staging|prod（deploy.sh 支持 dev|test|staging|pre-prod|prod）
  - pre-prod 命名空间不一致：automatic-agent-preprod vs automatic-agent-pre-prod
- **建议修复**: 
  1. 添加 health check 失败时的回滚触发
  2. 统一 rollback.sh 支持所有环境
  3. 修正 pre-prod 命名空间

#### 15. [CI/CD] [中严重] [Canary/Blue-Green 升级无健康检查]
- **文件/路径**: `.github/workflows/deploy-environment.yml:188-199`
- **问题描述**: 
  - 初始部署有 health checks
  - 但 promotion 步骤无健康验证
  - 可能推送不健康的版本
- **建议修复**: 
  1. 在 promotion 步骤后添加健康检查
  2. 验证推送的版本通过健康检查
  3. 失败时回滚

#### 16. [可观测性] [高严重] [58处 console.* 使用应使用 StructuredLogger]
- **文件/路径**: 多个文件
- **问题描述**: 
  - `src/index.ts`: 4次
  - `src/platform/ops-maturity/platform-panic/panic-propagation-service.ts`: 2 console.error + 1 console.log
  - `src/platform/five-plane-orchestration/agent-delegation/delegation-manager.service.ts`: 1 console.error
  - `src/platform/five-plane-orchestration/harness/runtime/runtime-entry-guard.ts`: 3 console.warn
  - `src/ops-maturity/chaos/chaos-experiment-scheduler.ts`: 5 console.log + 4 console.warn
  - CLI 工具中多处
- **建议修复**: 
  1. 替换 console.* 为 StructuredLogger
  2. 保留 CLI 输出的 console 用法（可接受）
  3. 添加 ESLint 规则检测

#### 17. [可观测性] [高严重] [29+执行引擎文件缺少结构化日志]
- **文件/路径**: `src/platform/five-plane-execution/execution-engine/`
- **问题描述**: 
  - `runtime-state-machine.ts`: 有 try/catch 但无 StructuredLogger
  - `multi-step-orchestration.ts`: 导入 HealthService 但无 StructuredLogger
  - `agent-executor.ts`: 169个 try 块，0 StructuredLogger 调用
  - 关键执行路径缺少可观测性
- **建议修复**: 
  1. 为所有执行引擎文件添加 StructuredLogger
  2. 记录关键操作路径
  3. 确保错误路径有日志

#### 18. [可观测性] [高严重] [执行引擎缺少 LLM 调用指标]
- **文件/路径**: `src/platform/five-plane-execution/execution-engine/`
- **问题描述**: 
  - RuntimeMetricsRegistry 支持 `recordLlmLatency`, `recordHarnessRunDuration` 等
  - 但执行引擎中无调用
  - model-gateway 有指标但执行引擎无
- **建议修复**: 
  1. 在 agent-executor 中记录 LLM 调用指标
  2. 在 multi-step-orchestration 中记录 harness 指标
  3. 启用完整指标覆盖

#### 19. [可观测性] [中严重] [日志方法使用不一致]
- **文件/路径**: 多个服务
- **问题描述**: 
  - 有的使用 `.error()`, `.warn()` 辅助方法
  - 有的使用 `logger.log({ level: "error", ... })`
  - 无统一规范
- **建议修复**: 
  1. 建立日志方法规范
  2. 添加 ESLint 规则统一
  3. 重构为一致的 helper 方法调用

#### 20. [安全] [中严重] [输入验证有限 - 仅 JWT 强类型]
- **文件/路径**: 多个 API 端点
- **问题描述**: 
  - JWT claims 有强类型验证
  - 其他输入验证有限
  - 缺少通用输入消毒
- **建议修复**: 
  1. 添加通用输入验证中间件
  2. 使用 zod 或类似库验证请求体
  3. 为所有 API 端点添加验证

#### 21. [源代码] [低严重] [HA Coordinator Leadership 获取可能非原子]
- **文件/路径**: `src/platform/five-plane-execution/ha/ha-coordinator-service-inner.ts:70-78`
- **问题描述**: 
  - `acquireLeadership` 可能执行非原子的 read-modify-write
  - leadership epoch 更新可能存在竞态
- **建议修复**: 
  1. 使用数据库行锁保护
  2. 确保领导者选举原子性
  3. 添加领导者租约续约机制

### 总结

本次补充Review（第十一轮 - 并发安全与可观测性Review - 2026-05-14）发现了21个新问题，重点关注并发安全、可观测性和CI/CD。

**高优先级 (需要立即处理)**:
1. 7个并发竞态问题（FencingTokenService, BudgetAllocator, ServiceRegistry等）
2. runtime-physical-schema 无外键约束
3. UI quality workflow 无覆盖率门禁
4. 58处 console.* 使用应替换为 StructuredLogger
5. 29+ 执行引擎文件缺少结构化日志
6. 执行引擎缺少 LLM 调用指标

**中优先级**:
1. OIDC _skipSignatureVerification 标志风险
2. Browser Executor innerHTML 不消毒
3. 大部分表缺少软删除和审计字段
4. 部署工作流无真正手动审批
5. 回滚机制边界情况未处理
6. Canary/Blue-Green 升级无健康检查
7. 日志方法使用不一致
8. 输入验证有限

**低优先级**:
1. HA Coordinator Leadership 获取可能非原子
2. SqliteLockAdapter fencingCounter 不同步

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 58 | 62 | 29 | 149 |
| 测试 | 8 | 14 | 5 | 27 |
| 配置 | 8 | 31 | 19 | 58 |
| 安全 | 7 | 8 | 1 | 16 |
| 文档 | 2 | 7 | 3 | 12 |
| UI | 0 | 3 | 3 | 6 |
| 部署 | 1 | 10 | 6 | 17 |
| **合计** | **84** | **135** | **66** | **285** |

### 优先修复建议

**立即处理 (本周内)**:
1. 修复所有7个并发竞态问题
2. 添加 runtime-physical-schema 外键约束
3. 为 UI quality workflow 添加覆盖率门禁
4. 替换关键路径的 console.* 为 StructuredLogger
5. 在执行引擎中添加 LLM 和 harness 指标

**短期内处理 (本月内)**:
1. 为所有执行引擎文件添加结构化日志
2. 添加数据库审计字段和软删除
3. 修正部署工作流审批机制
4. 统一日志方法使用规范
5. 添加 Canary/Blue-Green 健康检查

**长期规划**:
1. 建立并发安全编码规范
2. 实现完整的可观测性标准
3. 完善数据库约束和审计机制
4. 增强 CI/CD 质量和安全门禁
5. 建立代码审查清单

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十五轮 - 构建测试与类型系统Review）

### 发现的新问题

#### 1. [构建] [高严重] [无 TypeScript 增量编译缓存]
- **文件/路径**: `tsconfig.json`
- **问题描述**: 
  - 无 `incremental: true` 配置
  - 无 `tsbuildinfo` 文件
  - 每次构建是完整重建（9.6秒）
  - TypeScript 无法使用增量编译
- **建议修复**: 
  1. 在 tsconfig.json 添加 `"incremental": true`
  2. 添加 `"tsbuildinfo": ".tsbuildinfo"` 到 .gitignore
  3. 实现项目引用以支持并行构建

#### 2. [构建] [高严重] [76个 npm scripts 大部分重复构建]
- **文件/路径**: `package.json`
- **问题描述**: 
  - 40+ CLI scripts 都运行 `npm run build && node ...`
  - 每个脚本前都执行完整重建
  - 无法直接运行预构建的 CLI
  - 20+ stable:* 脚本遵循相同模式
- **建议修复**: 
  1. 创建 `build:cli` 脚本构建一次
  2. 让 CLI 脚本使用预构建的 dist 文件
  3. 添加 npm run dev 或类似的无需重建的执行方式

#### 3. [源代码] [高严重] [168处 any 类型使用]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - `stable-runner-factory.ts`: `opts: any` -> `any`
  - `billing-service-async.ts`: `input: any` 方法签名
  - 重试/超时工具使用 `(...args: any[]) => Promise<any>`
  - 绕过类型安全
- **建议修复**: 
  1. 使用泛型替代 any
  2. 替换为具体的输入类型
  3. 添加 ESLint 规则禁止 any

#### 4. [源代码] [高严重] [38处 @ts-ignore 指令]
- **文件/路径**: 多个文件
- **问题描述**: 
  - 14个 stability 相关文件有 `@ts-ignore ExecutionRecord type mismatch`
  - 5个 harness-sdk 文件有 `Partial<HarnessRun> doesn't have all required properties`
  - 2个文件有 `exactOptionalPropertyTypes` 问题
- **建议修复**: 
  1. 修复 ExecutionRecord 类型不匹配
  2. 完成 HarnessRun 类型的必需属性
  3. 移除 @ts-ignore 使用

#### 5. [源代码] [高严重] [ExecutionRecord 类型不匹配]
- **文件/路径**: `src/platform/stability/stable-evidence-bundle-support.ts:783` 等14个文件
- **问题描述**: 
  - 插入 execution record 时 TypeScript 拒绝类型
  - `ExecutionRecord` 接口要求新字段但 insert 对象缺失
  - 或 store 的 insert 方法接受不同类型
- **建议修复**: 
  1. 对齐 ExecutionRecord 接口和 store 类型
  2. 添加缺失字段到 insert 对象
  3. 或调整 store 的 insert 方法类型签名

#### 6. [源代码] [中严重] [30+处 as unknown as 双类型转换]
- **文件/路径**: `division-loader.ts`, `domain-baseline-catalog.ts`, `harness-sdk`
- **问题描述**: 
  - 完全绕过类型安全
  - 通常是类型架构问题的信号
- **建议修复**: 
  1. 修复上游类型问题
  2. 使用更具体的类型断言
  3. 重构类型层次结构

#### 7. [构建] [中严重] [rimraf  extraneous - 未在 package.json 声明]
- **文件/路径**: `node_modules/rimraf`
- **问题描述**: 
  - rimraf@6.1.3 存在于 node_modules 但未在 package.json 声明
  - 可能导致依赖问题
- **建议修复**: 
  1. 添加到 dependencies 或 devDependencies
  2. 或移除并使用其他清理方式

#### 8. [测试] [高严重] [178处直接 process.env 变更无抽象]
- **文件/路径**: `tests/unit/domains/registry/plugin-spi-registry.test.ts:640-687`
- **问题描述**: 
  - 直接读取/写入 process.env
  - 在 beforeEach/afterEach 中手动保存/恢复
  - 无 helper 抽象
- **建议修复**: 
  1. 创建 env helper 函数
  2. 统一保存和恢复模式
  3. 添加测试隔离验证

#### 9. [测试] [高严重] [21,998处 unlinkSync 调用无集中清理]
- **文件/路径**: 多个测试文件
- **问题描述**: 
  - 每个测试重新实现清理逻辑
  - 无集中清理 utility
  - 可能导致 EBUSY (Windows) 或并发访问问题
- **建议修复**: 
  1. 创建集中的测试清理 utility
  2. 使用 afterEach 确保清理
  3. 考虑使用临时目录

#### 10. [测试] [中严重] [无统一 mocking 框架]
- **文件/路径**: `tests/` 多个目录
- **问题描述**: 
  - 10,579 处 vi.fn, jest.mock, sinon 等调用
  - 无统一的 mocking 框架可见
  - 模式不一致
- **建议修复**: 
  1. 建立统一的 mock factory
  2. 标准化 mock 模式
  3. 添加 mocking 最佳实践文档

#### 11. [测试] [中严重] [Singleton 状态重置未标准化]
- **文件/路径**: `tests/unit/platform/five-plane-execution/dispatcher.test.ts`
- **问题描述**: 
  - 测试通过特定函数重置 singletons
  - 每个测试重复自己的 reset 逻辑
  - 无统一的 resetAllSingletons()
- **建议修复**: 
  1. 创建统一的 singleton reset 机制
  2. 在 afterEach 中自动调用
  3. 文档化 singleton 重置要求

#### 12. [测试] [低严重] [无分支覆盖率要求]
- **文件/路径**: `.c8rc.json`
- **问题描述**: 
  - `"100": false` - 无覆盖率要求
  - 只测量源代码，不测量测试文件
- **建议修复**: 
  1. 添加分支覆盖率阈值
  2. 在 CI 中强制执行覆盖率 gate
  3. 关注关键代码路径

#### 13. [网络] [高严重] [ScopedExternalAccessSandbox performHttpRequest 无超时]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/scoped-external-access-sandbox.ts:289`
- **问题描述**: 
  - fetch 调用无 timeout 参数
  - 挂起的请求可能永久阻塞
- **建议修复**: 
  1. 添加 AbortController timeout
  2. 使用标准的超时模式
  3. 记录超时配置

#### 14. [网络] [高严重] [OIDC 服务 fetch 调用无超时]
- **文件/路径**: `src/org-governance/sso-scim/oidc/oidc-service.ts:565,272`
- **问题描述**: 
  - OIDC token 交换无 retry logic
  - 无 timeout/abort signal
  - 认证流程可能挂起
- **建议修复**: 
  1. 添加 fetch timeout
  2. 添加 retry logic
  3. 使用标准的超时配置

#### 15. [网络] [高严重] [plugin-runtime-child 替换 globalThis.fetch 为 no-op]
- **文件/路径**: `src/domains/registry/plugin-runtime-child.ts:112`
- **问题描述**: 
  ```typescript
  globalThis.fetch = (async () => {}) as unknown as typeof globalThis.fetch;
  ```
  - 插件上下文中完全替换 fetch 为空函数
  - 插件的所有出站 HTTP 请求都会失败
  - 看起来是有意为之但可能造成问题
- **建议修复**: 
  1. 确认这是有意行为
  2. 文档化 fetch 替换行为
  3. 确保不会意外影响其他代码

#### 16. [网络] [中严重] [无显式 http.Agent 配置]
- **文件/路径**: 全局
- **问题描述**: 
  - 无显式的 http.Agent/https.Agent 配置
  - 依赖 Node.js 默认连接池
  - 无 maxSockets 或 maxConnections 配置
  - 高吞吐场景可能出问题
- **建议修复**: 
  1. 添加 http Agent 配置
  2. 文档化连接池大小
  3. 监控连接使用情况

#### 17. [序列化] [中严重] [合同版本硬编码为字符串字面量]
- **文件/路径**: `src/platform/contracts/inter-plane-contract-gateway.ts:184`
- **问题描述**: 
  - `schemaVersion: "v4.3"` 硬编码
  - 难以追踪合同版本演变
- **建议修复**: 
  1. 提取到类型化常量或 enum
  2. 集中管理合同版本
  3. 添加版本兼容性检查

#### 18. [序列化] [低严重] [Timestamp = string 丢失精度]
- **文件/路径**: `src/platform/contracts/types/domain/primitives.ts:10`
- **问题描述**: 
  - `type Timestamp = string` 无类型精度
  - ISO 8601 字符串无时区信息
- **建议修复**: 
  1. 使用 Date 对象或 branded string 类型
  2. 明确时区处理约定
  3. 文档化时间戳语义

#### 19. [源代码] [中严重] [29处非空断言 !.]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - 使用 `!.` 断言非空
  - 可能导致运行时错误
- **建议修复**: 
  1. 使用可选链 `?.` 或空值合并 `??`
  2. 添加适当的 null 检查
  3. 减少非空断言使用

#### 20. [API] [中严重] [API 路由 schema 版本硬编码]
- **文件/路径**: `src/platform/five-plane-interface/api/federation-routing-service.ts`
- **问题描述**: 
  - Zod schema 验证存在
  - 但 schema 版本未与 contract 版本同步
- **建议修复**: 
  1. 从 contract 版本派生 schema 版本
  2. 添加版本一致性检查
  3. 文档化版本关系

### 总结

本次补充Review（第十五轮 - 构建测试与类型系统Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. 无 TypeScript 增量编译缓存（每次完整重建9.6秒）
2. 76个 npm scripts 大部分重复构建
3. 168处 any 类型使用（绕过类型安全）
4. 38处 @ts-ignore 指令
5. ExecutionRecord 类型不匹配（14个文件）
6. 178处直接 process.env 变更无抽象
7. ScopedExternalAccessSandbox performHttpRequest 无超时
8. OIDC 服务 fetch 调用无超时
9. plugin-runtime-child 替换 globalThis.fetch 为 no-op

**中优先级**:
1. 30+处 as unknown as 双类型转换
2. rimraf extraneous 未声明
3. 21,998处 unlinkSync 无集中清理
4. 无统一 mocking 框架
5. Singleton 状态重置未标准化
6. 无显式 http.Agent 配置
7. 合同版本硬编码为字符串
8. Timestamp = string 丢失精度
9. 非空断言使用

**低优先级**:
1. 无分支覆盖率要求
2. API 路由 schema 版本未同步

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 72 | 87 | 39 | 198 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **109** | **182** | **85** | **376** |

### 优先修复建议

**立即处理 (本周内)**:
1. 添加 TypeScript 增量编译 (`incremental: true`)
2. 重构 npm scripts 避免重复构建
3. 减少 any 类型使用到 <50
4. 移除所有 @ts-ignore 指令
5. 修复 ExecutionRecord 类型不匹配
6. 添加 fetch 超时到 OIDC 和 sandbox

**短期内处理 (本月内)**:
1. 创建集中的测试清理 utility
2. 建立统一的 mocking 框架
3. 标准化 singleton 重置机制
4. 添加 http.Agent 连接池配置
5. 提取合同版本到类型化常量

**长期规划**:
1. 实现完整的类型安全（无 any）
2. 建立测试基础设施标准
3. 实现构建缓存优化
4. 完善网络层超时和重试
5. 建立覆盖率门禁标准

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十四轮 - 日志事件与工作流Review）

### 发现的新问题

#### 1. [可观测性] [高严重] [StructuredLogEntry 无 requestId 字段]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - StructuredLogEntry 接口无 requestId 字段
  - 只有 correlationId，它回退到 traceId
  - 无法独立追踪单个 HTTP 请求
- **建议修复**: 
  1. 在 StructuredLogEntry 添加 requestId 字段
  2. 在 HTTP 中间件中生成和注入 requestId
  3. 确保 requestId 流经所有日志调用

#### 2. [可观测性] [高严重] [StructuredLogger data 字段无自动清洗]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - 如果开发者传递 `{ password: "..." }` 给 logger.info，密码会被记录
  - 无字段级脱敏机制
  - 依赖开发者手动排除敏感字段
- **建议修复**: 
  1. 添加 OBSERVE_OUTPUT_BLACKLIST 类似的黑名单
  2. 实现 redact/mask/sanitize 工具函数
  3. 在日志传输前自动清洗敏感字段

#### 3. [可观测性] [中严重] [日志中敏感数据无 PII 处理]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - 无 PII 重写工具
  - 开发者必须手动避免记录敏感数据
  - 存在意外记录敏感信息的风险
- **建议修复**: 
  1. 添加 PII 检测和重写工具
  2. 在文档中明确记录敏感字段列表
  3. 添加测试验证敏感数据不被记录

#### 4. [可观测性] [中严重] [59处 console.* 直接调用]
- **文件/路径**: 多个源文件
- **问题描述**: 
  - 应该使用 StructuredLogger
  - console 调用可能不被日志聚合系统收集
  - 难以追踪和关联
- **建议修复**: 
  1. 替换所有 console.* 为 StructuredLogger
  2. 添加 ESLint 规则禁止 console.*
  3. 保留少量允许的 CLI 输出

#### 5. [可观测性] [中严重] [部分 logger.warn 调用缺少结构化数据]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:122`
- **问题描述**: 
  ```typescript
  logger.warn("WebSocket connection rejected: missing subprotocol token")
  ```
  - 只有消息，无 data 对象
  - 难以关联和查询
- **建议修复**: 
  1. 添加 `{ data: { ... } }` 对象
  2. 确保所有日志包含上下文数据
  3. 添加日志审查工具

#### 6. [源代码] [高严重] [Harness while(true) 循环无硬性迭代上限]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:1442`
- **问题描述**: 
  ```typescript
  while (true) {
    // ...
    // 依赖 budget gate 和 guardrail vibration 退出
    // 如果两者都失败，循环永久继续
  }
  ```
  - 依赖 budget gate 和 guardrail vibration 退出
  - 无硬性迭代次数限制
  - 可能导致无限循环
- **建议修复**: 
  1. 添加硬性 maxIterations 上限
  2. 在达到上限时强制退出
  3. 记录详细的迭代统计用于调试

#### 7. [源代码] [高严重] [Oapeflir while(true) 循环可能无限重规划]
- **文件/路径**: `src/platform/five-plane-orchestration/oapeflir/oapeflir-loop-service.ts:355`
- **问题描述**: 
  - 如果 loopReplanDecision.shouldReplan 永不变为 false
  - 重规划永远无法减少计划错误
  - 可能导致无限循环
- **建议修复**: 
  1. 添加重规划次数上限
  2. 在达到上限时强制接受当前结果
  3. 添加重规划质量衰减检测

#### 8. [源代码] [中严重] [Recovery 流程允许终态转换到 paused]
- **文件/路径**: `src/platform/five-plane-execution/runtime-state-machine.ts:105-108`
- **问题描述**: 
  - completed, failed, cancelled, aborted 可转换到 paused 进行恢复
  - 创建恢复路径可能重新进入执行
  - 需要验证此行为的正确性
- **建议修复**: 
  1. 审查 recovery 转换的合法性
  2. 确保不会导致状态不一致
  3. 添加转换前置条件验证

#### 9. [国际化] [高严重] [API 错误消息硬编码英文]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/api-error.ts`
- **问题描述**: 
  ```typescript
  return new ApiError(404, "api.task_not_found", "Task not found.");
  ```
  - 错误消息硬编码英文
  - 无后端 i18n 机制
  - 非英文客户端收到不本地化的错误
- **建议修复**: 
  1. 使用错误码而非硬编码消息
  2. 客户端根据错误码本地化
  3. 或在后端添加 i18n 支持

#### 10. [国际化] [中严重] [UI 硬编码字符串未翻译]
- **文件/路径**: `ui/packages/features/conversation/src/web/index.tsx`
- **问题描述**: 
  - "Build Plan", "Execute", "Messages" 等按钮标签硬编码
  - 不使用 i18n 系统
- **建议修复**: 
  1. 使用 `translateFeatureCopy` 替换硬编码
  2. 确保所有用户可见文本通过 i18n
  3. 添加硬编码字符串检测

#### 11. [国际化] [中严重] [Arabic 目录不完整]
- **文件/路径**: `ui/packages/shared/i18n/src/catalogs/ar-SA.ts`
- **问题描述**: 
  - ar-SA 只有约18条消息
  - en-US 和 zh-CN 有约117条
  - 缺失的 key 返回 key 本身
- **建议修复**: 
  1. 补充 Arabic 翻译
  2. 确保所有 key 都有翻译
  3. 添加翻译完整性测试

#### 12. [国际化] [低严重] [translateFeatureCopy 无回退默认值]
- **文件/路径**: `ui/packages/shared/i18n/`
- **问题描述**: 
  - 如果 feature ID 不在 catalog 中
  - 返回 undefined title/summary
  - 无合理回退
- **建议修复**: 
  1. 添加回退到 key 或默认字符串
  2. 或使用 feature ID 作为显示名
  3. 确保 UI 不显示 undefined

#### 13. [运维] [高严重] [健康检查无 readiness/liveness 区分]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts`
- **问题描述**: 
  - /healthz, /v1/healthz, /health 返回相同报告
  - 无 readiness probe（可处理流量？）
  - 无 liveness probe（系统存活？）
  - canAcceptTraffic() 未通过 HTTP 暴露
- **建议修复**: 
  1. 添加 /ready 和 /live 端点
  2. Readiness 检查依赖项就绪
  3. Liveness 检查进程健康

#### 14. [运维] [高严重] [shutdown 期间健康检查存在竞态]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts`
- **问题描述**: 
  - getHealthReportAsync() 异步检查 dbWritable
  - shutdown 期间数据库连接可能关闭
  - 健康检查仍在传入
- **建议修复**: 
  1. 在 shutdown 时返回 unhealthy
  2. 添加 shutdown 状态标志
  3. 停止接受新的健康检查请求

#### 15. [运维] [高严重] [LeaderElectionService 未与 GracefulShutdown 集成]
- **文件/路径**: `src/platform/five-plane-execution/ha/leader-election-service.ts`
- **问题描述**: 
  - LeaderElectionService.stop() 处理优雅退位
  - 但未在全局 graceful shutdown 中注册
  - 需显式注册为 shutdown handler
- **建议修复**: 
  1. 将 LeaderElectionService 添加到 shutdown handlers
  2. 确保领导者在 shutdown 时让位
  3. 验证关闭顺序正确

#### 16. [运维] [中严重] [addHandler() 无显式排序约定]
- **文件/路径**: `src/platform/five-plane-execution/startup/graceful-shutdown.ts`
- **问题描述**: 
  - 虽然 handlers 逆序执行
  - 但无显式接口声明 handler 依赖关系
  - 关键 handlers（如关闭数据库连接）应最后运行
- **建议修复**: 
  1. 添加优先级或阶段参数
  2. 文档化关键 handler 顺序要求
  3. 添加顺序验证测试

#### 17. [运维] [中严重] [unref'd timers 可能不触发]
- **文件/路径**: `src/platform/five-plane-execution/startup/process-error-handlers.ts:106`
- **问题描述**: 
  - `setTimeout(...).unref()` 不保持事件循环活跃
  - 如果主线程空闲，强制退出可能不触发
- **建议修复**: 
  1. 移除 .unref() 除非确实需要
  2. 或确保有其他方式保持事件循环
  3. 添加超时触发验证测试

#### 18. [运维] [中严重] [StartupConsistencyChecker 阻塞流量但不暴露状态]
- **文件/路径**: `src/platform/five-plane-startup-plan.ts`
- **问题描述**: 
  - canAcceptTraffic() 在 P0 问题存在时返回 false
  - 但 _trafficBlocked 是内部状态
  - 负载均衡器访问 /healthz 仍会得到 "ok"
- **建议修复**: 
  1. 在健康报告中暴露 trafficBlocked 状态
  2. 或添加单独的健康端点指示就绪状态
  3. 确保负载均衡器在阻塞时不路由流量

#### 19. [事件系统] [中严重] [TypedEventBus vs EventEmitter 混用]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - 项目同时使用 TypedEventBus 和原生 EventEmitter
  - TypedEventBus 应该是标准
  - 混用可能导致类型安全问题
- **建议修复**: 
  1. 创建迁移计划统一使用 TypedEventBus
  2. 添加 ESLint 规则禁止原生 EventEmitter
  3. 在 CI 中检测混用情况

#### 20. [源代码] [低严重] [无事件排序保证]
- **文件/路径**: `src/platform/five-plane-state-evidence/events/`
- **问题描述**: 
  - 无明确的事件排序保证文档
  - 事件处理可能乱序
  - 依赖方需要处理乱序
- **建议修复**: 
  1. 文档化事件排序语义
  2. 如果需要排序，实现序列号机制
  3. 在事件处理中考虑乱序情况

### 总结

本次补充Review（第十四轮 - 日志事件与工作流Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. StructuredLogEntry 无 requestId 字段
2. StructuredLogger data 字段无自动清洗（敏感数据风险）
3. Harness while(true) 循环无硬性迭代上限
4. Oapeflir while(true) 循环可能无限重规划
5. API 错误消息硬编码英文（无 i18n）
6. 健康检查无 readiness/liveness 区分
7. shutdown 期间健康检查存在竞态
8. LeaderElectionService 未与 GracefulShutdown 集成

**中优先级**:
1. 日志中敏感数据无 PII 处理
2. 59处 console.* 直接调用
3. 部分 logger.warn 调用缺少结构化数据
4. Recovery 流程允许终态转换到 paused
5. UI 硬编码字符串未翻译
6. Arabic 目录不完整
7. addHandler() 无显式排序约定
8. unref'd timers 可能不触发
9. StartupConsistencyChecker 阻塞流量但不暴露状态
10. TypedEventBus vs EventEmitter 混用

**低优先级**:
1. translateFeatureCopy 无回退默认值
2. 无事件排序保证

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 68 | 80 | 37 | 185 |
| 测试 | 8 | 14 | 5 | 27 |
| 配置 | 10 | 38 | 24 | 72 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **103** | **171** | **82** | **356** |

### 优先修复建议

**立即处理 (本周内)**:
1. 在 StructuredLogEntry 添加 requestId 字段
2. 实现日志敏感字段自动清洗
3. 为工作流循环添加硬性迭代上限
4. 添加 readiness/liveness 健康检查端点
5. 将 LeaderElectionService 集成到 GracefulShutdown
6. 实现 API 错误消息 i18n 支持

**短期内处理 (本月内)**:
1. 替换所有 console.* 为 StructuredLogger
2. 补充 Arabic i18n 目录
3. 修复 UI 硬编码字符串
4. 统一 TypedEventBus 替代 EventEmitter
5. 解决 shutdown 竞态条件
6. 添加事件排序保证文档

**长期规划**:
1. 建立完整的 PII 处理框架
2. 实现日志完整性验证
3. 建立工作流安全循环检测
4. 完善健康检查和就绪探测
5. 建立国际化完整测试

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十五轮 - 构建测试与类型系统Review）

### 发现的新问题

#### 1. [构建] [高严重] [无 TypeScript 增量编译缓存]
- **文件/路径**: `tsconfig.json`
- **问题描述**: 
  - 无 `incremental: true` 配置
  - 无 `tsbuildinfo` 文件
  - 每次构建是完整重建（9.6秒）
  - TypeScript 无法使用增量编译
- **建议修复**: 
  1. 在 tsconfig.json 添加 `"incremental": true`
  2. 添加 `"tsbuildinfo": ".tsbuildinfo"` 到 .gitignore
  3. 实现项目引用以支持并行构建

#### 2. [构建] [高严重] [76个 npm scripts 大部分重复构建]
- **文件/路径**: `package.json`
- **问题描述**: 
  - 40+ CLI scripts 都运行 `npm run build && node ...`
  - 每个脚本前都执行完整重建
  - 无法直接运行预构建的 CLI
  - 20+ stable:* 脚本遵循相同模式
- **建议修复**: 
  1. 创建 `build:cli` 脚本构建一次
  2. 让 CLI 脚本使用预构建的 dist 文件
  3. 添加 npm run dev 或类似的无需重建的执行方式

#### 3. [源代码] [高严重] [168处 any 类型使用]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - `stable-runner-factory.ts`: `opts: any` -> `any`
  - `billing-service-async.ts`: `input: any` 方法签名
  - 重试/超时工具使用 `(...args: any[]) => Promise<any>`
  - 绕过类型安全
- **建议修复**: 
  1. 使用泛型替代 any
  2. 替换为具体的输入类型
  3. 添加 ESLint 规则禁止 any

#### 4. [源代码] [高严重] [38处 @ts-ignore 指令]
- **文件/路径**: 多个文件
- **问题描述**: 
  - 14个 stability 相关文件有 `@ts-ignore ExecutionRecord type mismatch`
  - 5个 harness-sdk 文件有 `Partial<HarnessRun> doesn't have all required properties`
  - 2个文件有 `exactOptionalPropertyTypes` 问题
- **建议修复**: 
  1. 修复 ExecutionRecord 类型不匹配
  2. 完成 HarnessRun 类型的必需属性
  3. 移除 @ts-ignore 使用

#### 5. [源代码] [高严重] [ExecutionRecord 类型不匹配]
- **文件/路径**: `src/platform/stability/stable-evidence-bundle-support.ts:783` 等14个文件
- **问题描述**: 
  - 插入 execution record 时 TypeScript 拒绝类型
  - `ExecutionRecord` 接口要求新字段但 insert 对象缺失
  - 或 store 的 insert 方法接受不同类型
- **建议修复**: 
  1. 对齐 ExecutionRecord 接口和 store 类型
  2. 添加缺失字段到 insert 对象
  3. 或调整 store 的 insert 方法类型签名

#### 6. [源代码] [中严重] [30+处 as unknown as 双类型转换]
- **文件/路径**: `division-loader.ts`, `domain-baseline-catalog.ts`, `harness-sdk`
- **问题描述**: 
  - 完全绕过类型安全
  - 通常是类型架构问题的信号
- **建议修复**: 
  1. 修复上游类型问题
  2. 使用更具体的类型断言
  3. 重构类型层次结构

#### 7. [构建] [中严重] [rimraf  extraneous - 未在 package.json 声明]
- **文件/路径**: `node_modules/rimraf`
- **问题描述**: 
  - rimraf@6.1.3 存在于 node_modules 但未在 package.json 声明
  - 可能导致依赖问题
- **建议修复**: 
  1. 添加到 dependencies 或 devDependencies
  2. 或移除并使用其他清理方式

#### 8. [测试] [高严重] [178处直接 process.env 变更无抽象]
- **文件/路径**: `tests/unit/domains/registry/plugin-spi-registry.test.ts:640-687`
- **问题描述**: 
  - 直接读取/写入 process.env
  - 在 beforeEach/afterEach 中手动保存/恢复
  - 无 helper 抽象
- **建议修复**: 
  1. 创建 env helper 函数
  2. 统一保存和恢复模式
  3. 添加测试隔离验证

#### 9. [测试] [高严重] [21,998处 unlinkSync 调用无集中清理]
- **文件/路径**: 多个测试文件
- **问题描述**: 
  - 每个测试重新实现清理逻辑
  - 无集中清理 utility
  - 可能导致 EBUSY (Windows) 或并发访问问题
- **建议修复**: 
  1. 创建集中的测试清理 utility
  2. 使用 afterEach 确保清理
  3. 考虑使用临时目录

#### 10. [测试] [中严重] [无统一 mocking 框架]
- **文件/路径**: `tests/` 多个目录
- **问题描述**: 
  - 10,579 处 vi.fn, jest.mock, sinon 等调用
  - 无统一的 mocking 框架可见
  - 模式不一致
- **建议修复**: 
  1. 建立统一的 mock factory
  2. 标准化 mock 模式
  3. 添加 mocking 最佳实践文档

#### 11. [测试] [中严重] [Singleton 状态重置未标准化]
- **文件/路径**: `tests/unit/platform/five-plane-execution/dispatcher.test.ts`
- **问题描述**: 
  - 测试通过特定函数重置 singletons
  - 每个测试重复自己的 reset 逻辑
  - 无统一的 resetAllSingletons()
- **建议修复**: 
  1. 创建统一的 singleton reset 机制
  2. 在 afterEach 中自动调用
  3. 文档化 singleton 重置要求

#### 12. [测试] [低严重] [无分支覆盖率要求]
- **文件/路径**: `.c8rc.json`
- **问题描述**: 
  - `"100": false` - 无覆盖率要求
  - 只测量源代码，不测量测试文件
- **建议修复**: 
  1. 添加分支覆盖率阈值
  2. 在 CI 中强制执行覆盖率 gate
  3. 关注关键代码路径

#### 13. [网络] [高严重] [ScopedExternalAccessSandbox performHttpRequest 无超时]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/scoped-external-access-sandbox.ts:289`
- **问题描述**: 
  - fetch 调用无 timeout 参数
  - 挂起的请求可能永久阻塞
- **建议修复**: 
  1. 添加 AbortController timeout
  2. 使用标准的超时模式
  3. 记录超时配置

#### 14. [网络] [高严重] [OIDC 服务 fetch 调用无超时]
- **文件/路径**: `src/org-governance/sso-scim/oidc/oidc-service.ts:565,272`
- **问题描述**: 
  - OIDC token 交换无 retry logic
  - 无 timeout/abort signal
  - 认证流程可能挂起
- **建议修复**: 
  1. 添加 fetch timeout
  2. 添加 retry logic
  3. 使用标准的超时配置

#### 15. [网络] [高严重] [plugin-runtime-child 替换 globalThis.fetch 为 no-op]
- **文件/路径**: `src/domains/registry/plugin-runtime-child.ts:112`
- **问题描述**: 
  ```typescript
  globalThis.fetch = (async () => {}) as unknown as typeof globalThis.fetch;
  ```
  - 插件上下文中完全替换 fetch 为空函数
  - 插件的所有出站 HTTP 请求都会失败
  - 看起来是有意为之但可能造成问题
- **建议修复**: 
  1. 确认这是有意行为
  2. 文档化 fetch 替换行为
  3. 确保不会意外影响其他代码

#### 16. [网络] [中严重] [无显式 http.Agent 配置]
- **文件/路径**: 全局
- **问题描述**: 
  - 无显式的 http.Agent/https.Agent 配置
  - 依赖 Node.js 默认连接池
  - 无 maxSockets 或 maxConnections 配置
  - 高吞吐场景可能出问题
- **建议修复**: 
  1. 添加 http Agent 配置
  2. 文档化连接池大小
  3. 监控连接使用情况

#### 17. [序列化] [中严重] [合同版本硬编码为字符串字面量]
- **文件/路径**: `src/platform/contracts/inter-plane-contract-gateway.ts:184`
- **问题描述**: 
  - `schemaVersion: "v4.3"` 硬编码
  - 难以追踪合同版本演变
- **建议修复**: 
  1. 提取到类型化常量或 enum
  2. 集中管理合同版本
  3. 添加版本兼容性检查

#### 18. [序列化] [低严重] [Timestamp = string 丢失精度]
- **文件/路径**: `src/platform/contracts/types/domain/primitives.ts:10`
- **问题描述**: 
  - `type Timestamp = string` 无类型精度
  - ISO 8601 字符串无时区信息
- **建议修复**: 
  1. 使用 Date 对象或 branded string 类型
  2. 明确时区处理约定
  3. 文档化时间戳语义

#### 19. [源代码] [中严重] [29处非空断言 !.]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - 使用 `!.` 断言非空
  - 可能导致运行时错误
- **建议修复**: 
  1. 使用可选链 `?.` 或空值合并 `??`
  2. 添加适当的 null 检查
  3. 减少非空断言使用

#### 20. [API] [中严重] [API 路由 schema 版本硬编码]
- **文件/路径**: `src/platform/five-plane-interface/api/federation-routing-service.ts`
- **问题描述**: 
  - Zod schema 验证存在
  - 但 schema 版本未与 contract 版本同步
- **建议修复**: 
  1. 从 contract 版本派生 schema 版本
  2. 添加版本一致性检查
  3. 文档化版本关系

### 总结

本次补充Review（第十五轮 - 构建测试与类型系统Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. 无 TypeScript 增量编译缓存（每次完整重建9.6秒）
2. 76个 npm scripts 大部分重复构建
3. 168处 any 类型使用（绕过类型安全）
4. 38处 @ts-ignore 指令
5. ExecutionRecord 类型不匹配（14个文件）
6. 178处直接 process.env 变更无抽象
7. ScopedExternalAccessSandbox performHttpRequest 无超时
8. OIDC 服务 fetch 调用无超时
9. plugin-runtime-child 替换 globalThis.fetch 为 no-op

**中优先级**:
1. 30+处 as unknown as 双类型转换
2. rimraf extraneous 未声明
3. 21,998处 unlinkSync 无集中清理
4. 无统一 mocking 框架
5. Singleton 状态重置未标准化
6. 无显式 http.Agent 配置
7. 合同版本硬编码为字符串
8. Timestamp = string 丢失精度
9. 非空断言使用

**低优先级**:
1. 无分支覆盖率要求
2. API 路由 schema 版本未同步

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 72 | 87 | 39 | 198 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **109** | **182** | **85** | **376** |

### 优先修复建议

**立即处理 (本周内)**:
1. 添加 TypeScript 增量编译 (`incremental: true`)
2. 重构 npm scripts 避免重复构建
3. 减少 any 类型使用到 <50
4. 移除所有 @ts-ignore 指令
5. 修复 ExecutionRecord 类型不匹配
6. 添加 fetch 超时到 OIDC 和 sandbox

**短期内处理 (本月内)**:
1. 创建集中的测试清理 utility
2. 建立统一的 mocking 框架
3. 标准化 singleton 重置机制
4. 添加 http.Agent 连接池配置
5. 提取合同版本到类型化常量

**长期规划**:
1. 实现完整的类型安全（无 any）
2. 建立测试基础设施标准
3. 实现构建缓存优化
4. 完善网络层超时和重试
5. 建立覆盖率门禁标准

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十三轮 - 缓存会话与实时通信Review）

### 发现的新问题

#### 1. [安全] [严重] [CORS allowedMethods 缺少 PUT/PATCH/DELETE]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/response-hardening.ts:12-14`
- **问题描述**: 
  ```typescript
  export const DEFAULT_CORS_CONFIG: CorsConfig = {
    allowedMethods: ["GET", "POST", "OPTIONS"],  // 缺少 PUT, PATCH, DELETE
  ```
  - 浏览器 CORS preflight 会拒绝 PUT/PATCH/DELETE 请求
  - 但路由中有 PATCH /v1/tasks/:id, DELETE endpoints
- **建议修复**: 
  1. 添加 PUT, PATCH, DELETE 到 allowedMethods
  2. 确保 CORS 配置与应用路由方法一致
  3. 添加测试验证所有 HTTP 方法的 CORS preflight

#### 2. [安全] [严重] [Mission Routes 错误响应包装不当]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/mission-routes.ts:101,118-124`
- **问题描述**: 
  ```typescript
  return buildJsonResponse(ctx.requestId, 404, { error: { code: "MISSION_NOT_FOUND" } });
  ```
  - 使用 buildJsonResponse（成功包装）返回错误
  - 错误被包装在成功信封 `{requestId, data: {error: {...}}}` 中
  - 违反 REST 规范
- **建议修复**: 
  1. 使用 buildJsonErrorResponse 处理错误
  2. 确保所有错误路径使用正确的错误信封
  3. 添加 API 一致性测试

#### 3. [源代码] [高严重] [WebSocketBridge 无最大连接数限制]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:103-107`
- **问题描述**: 
  ```typescript
  this.wss = new WebSocketServer({
    server,
    path: WS_PATH,
    maxPayload: options.maxPayloadBytes ?? DEFAULT_MAX_PAYLOAD_BYTES,
    // 无 maxConnections 限制！
  });
  ```
  - 与 dashboard-websocket-server.ts 的 maxClients: 1000 不同
  - 恶意客户端可耗尽服务器资源
- **建议修复**: 
  1. 添加 maxConnections 限制
  2. 实现连接限制拒绝策略
  3. 添加连接计数指标

#### 4. [源代码] [高严重] [pendingAcks 在断开连接时未清理]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:268-282`
- **问题描述**: 
  - handleDisconnection 清理 subscribedTasks, taskSubscribers, slowConsumers
  - 但 pendingAcks Map 未清理
  - 断开的客户端未确认的消息永久留在内存
- **建议修复**: 
  1. 在 handleDisconnection 中删除所有 pendingAcks 条目
  2. 考虑添加超时机制自动清理未确认消息
  3. 记录清理操作的审计日志

#### 5. [安全] [高严重] [硬编码回退密钥]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/audit-event-integrity.ts:41`
- **问题描述**: 
  ```typescript
  const AUDIT_INTEGRITY_HMAC_KEY = process.env["AA_AUDIT_INTEGRITY_HMAC_KEY"] ?? "audit-integrity-secret-key-32-bytes!";
  ```
  - 占位符密钥用作回退
  - 如果环境变量未设置，使用不安全密钥
- **建议修复**: 
  1. 移除回退值，要求环境变量必须设置
  2. 在启动时验证密钥存在且足够强
  3. 如果密钥缺失导致启动失败

#### 6. [配置] [高严重] [config/runtime/test.json 缺少超时配置]
- **文件/路径**: `config/runtime/test.json`
- **问题描述**: 
  - 只有3个字段，缺少 maxConcurrentTasks, defaultTaskTimeoutMs, defaultStepTimeoutMs
  - 回退到默认值可能不适合测试环境
- **建议修复**: 
  1. 添加完整的 timeout/tuning 配置
  2. 确保测试环境有合理的默认值
  3. 文档化必需的配置字段

#### 7. [配置] [高严重] [prod.json 限制过严 - maxConcurrentTasks=1]
- **文件/路径**: `config/runtime/prod.json`
- **问题描述**: 
  - maxConcurrentTasks: 1（vs dev:8, staging:4, pre-prod:6）
  - defaultTaskTimeoutMs: 120000（vs dev:600000）
  - defaultStepTimeoutMs: 60000（vs dev:180000）
  - 生产环境严重受限，可能影响吞吐量
- **建议修复**: 
  1. 评估并调整生产限制
  2. 与业务需求匹配
  3. 添加生产容量测试

#### 8. [源代码] [高严重] [多个服务存在无界 Map 无驱逐策略]
- **文件/路径**: 多个域服务
- **问题描述**: 
  - `domain-knowledge-schema-service.ts:73-75`: schemas, sourceContent, sourceTimestamps 无限制
  - `domain-eval-framework-service.ts:91-95`: frameworks, qualityAxes 等无限制
  - `domain-recipe-service.ts:67-68`: recipes, versions 无限制
  - `domain-risk-profile-service.ts:51`: profiles 无限制
  - Session Maps (session-management.ts:83-89) 无自动清理
- **建议修复**: 
  1. 为所有 Map 实现 LRU 或 TTL 驱逐策略
  2. 添加后台清理任务
  3. 添加大小监控和告警

#### 9. [源代码] [中严重] [taskEventHistory Map 永不清理]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:91,548-555`
- **问题描述**: 
  - taskEventHistory 限制单任务历史为 200 条
  - 但 Map 本身从不清理
  - 取消订阅的任务历史永久保留
- **建议修复**: 
  1. 实现后台清理无订阅者任务的历史
  2. 添加任务订阅者监控
  3. 考虑使用 WeakMap 替代

#### 10. [配置] [中严重] [安全配置 drift - remoteWorkerRegistration 缺失]
- **文件/路径**: `config/security/dev.json`, `staging.json`, `pre-prod.json`
- **问题描述**: 
  - default.json 有完整的 remoteWorkerRegistration 配置
  - dev/staging/pre-prod 只有 approvalMode
  - prod 有 approvalMode 但无 remoteWorkerRegistration
  - 安全配置不一致
- **建议修复**: 
  1. 统一所有环境的 remoteWorkerRegistration
  2. 添加安全配置验证
  3. 确保最低安全基线

#### 11. [API] [中严重] [OpenAPI 端点响应格式不一致]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/utils.ts:207-215`
- **问题描述**: 
  - `/v1/openapi.json` 使用 buildJsonDocumentResponse（无包装）
  - 其他端点使用 buildJsonResponse（{requestId, data} 包装）
  - 客户端体验不一致
- **建议修复**: 
  1. 统一响应信封格式
  2. 文档化响应格式规范
  3. 添加响应格式验证测试

#### 12. [源代码] [中严重] [Redis 队列缺少 idempotency 索引支持]
- **文件/路径**: `src/platform/five-plane-execution/queue/redis-queue-adapter.ts`
- **问题描述**: 
  - SQLite 有部分唯一索引支持 idempotency
  - Redis 实现使用哈希索引但实现不完整
  - 可能导致重复消息
- **建议修复**: 
  1. 完善 Redis idempotency 实现
  2. 添加唯一索引验证
  3. 确保与 SQLite 行为一致

#### 13. [源代码] [中严重] [请求去重中间件使用内存存储]
- **文件/路径**: `src/platform/five-plane-interface/api/middleware/request-deduplication.ts`
- **问题描述**: 
  - 纯内存滑动窗口（Map<DeduplicationKey, DeduplicationEntry[]>）
  - 无外部存储
  - 多实例部署不共享
  - 重启后去重状态丢失
- **建议修复**: 
  1. 使用 Redis 替代内存存储
  2. 支持分布式去重
  3. 持久化去重状态

#### 14. [源代码] [中严重] [Cache 无 stampede 保护]
- **文件/路径**: 多个 cache 实现
- **问题描述**: 
  - MemoryCacheStore, ExperienceCacheService 无锁
  - 缓存未命中时可发生 thundering herd
  - 高并发下可能导致数据库过载
- **建议修复**: 
  1. 实现 single-flight 模式
  2. 添加请求排队机制
  3. 使用分布式锁保护缓存更新

#### 15. [源代码] [中严重] [EvidenceService eviction 仅在插入时触发]
- **文件/路径**: `src/platform/five-plane-state-evidence/memory/evidence-service.ts:202`
- **问题描述**: 
  - 清理仅在 record() 调用时触发
  - 空闲时无后台清理
  - 可能导致内存持续增长
- **建议修复**: 
  1. 添加定期后台清理任务
  2. 使用独立的清理线程
  3. 添加内存使用监控

#### 16. [源代码] [低严重] [无连接指标暴露]
- **文件/路径**: WebSocket 服务器
- **问题描述**: 
  - getClientCount() 存在但未通过 HTTP 端点暴露
  - 无 pendingAcks 队列深度指标
  - 无连接建立告警
- **建议修复**: 
  1. 通过 metrics 端点暴露连接指标
  2. 添加 pendingAcks 队列监控
  3. 添加连接数异常告警

#### 17. [源代码] [低严重] [无空闲客户端超时]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts`
- **问题描述**: 
  - 仅在心跳 Sweep 时检查 isAlive
  - 无 per-client 独立于心跳的空闲超时
  - 认证后从不发送消息的客户端只能通过心跳失败检测
- **建议修复**: 
  1. 添加 per-client idle timeout
  2. 独立于心跳间隔
  3. 配置可调整

#### 18. [源代码] [低严重] [订阅限制是 per-client 而非全局]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:244`
- **问题描述**: 
  - MAX_SUBSCRIPTIONS_PER_CLIENT = 100
  - 恶意客户端可订阅 100 个任务
  - 无全局任务订阅者限制
- **建议修复**: 
  1. 添加全局任务订阅者限制
  2. 实现 per-task 订阅者上限
  3. 添加反滥用检测

#### 19. [配置] [低严重] [config/runtime 无版本验证机制]
- **文件/路径**: `config/runtime/*.json`
- **问题描述**: 
  - 配置有 "version": "v4.3"
  - 但无 schema 版本验证
  - 加载时可能接受不兼容配置
- **建议修复**: 
  1. 添加 JSON Schema 验证
  2. 实现版本兼容性检查
  3. 启动时验证配置完整性

#### 20. [源代码] [低严重] [ImprovementCandidateRegistry splice 是 O(n)]
- **文件/路径**: `src/platform/five-plane-orchestration/improve/improvement-candidate-registry.ts:243`
- **问题描述**: 
  - accessOrder 数组的 splice 操作是 O(n)
  - 高频访问时可能性能问题
- **建议修复**: 
  1. 使用 LinkedList 替代数组
  2. 或使用 Map 维护访问顺序
  3. 性能测试验证

### 总结

本次补充Review（第十三轮 - 缓存会话与实时通信Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. CORS allowedMethods 缺少 PUT/PATCH/DELETE（浏览器请求会失败）
2. Mission Routes 错误响应包装不当
3. WebSocketBridge 无最大连接数限制（DoS风险）
4. pendingAcks 断开连接时未清理（内存泄漏）
5. 硬编码回退密钥（安全漏洞）
6. config/runtime/test.json 缺少超时配置
7. prod.json maxConcurrentTasks=1 限制过严
8. 多个服务存在无界 Map（内存泄漏风险）

**中优先级**:
1. taskEventHistory Map 永不清理
2. 安全配置 drift（remoteWorkerRegistration 缺失）
3. OpenAPI 端点响应格式不一致
4. Redis 队列 idempotency 实现不完整
5. 请求去重中间件使用内存存储（多实例不共享）
6. Cache 无 stampede 保护
7. EvidenceService eviction 仅在插入时触发

**低优先级**:
1. 无连接指标暴露
2. 无空闲客户端超时
3. 订阅限制是 per-client 而非全局
4. config/runtime 无版本验证机制
5. ImprovementCandidateRegistry splice O(n)

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 64 | 74 | 35 | 173 |
| 测试 | 8 | 14 | 5 | 27 |
| 配置 | 10 | 38 | 24 | 72 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 7 | 3 | 12 |
| UI | 0 | 3 | 3 | 6 |
| 部署 | 1 | 10 | 6 | 17 |
| **合计** | **99** | **161** | **79** | **339** |

### 优先修复建议

**立即处理 (本周内)**:
1. 修复 CORS allowedMethods 添加 PUT/PATCH/DELETE
2. 修复 Mission Routes 错误响应格式
3. 为 WebSocketBridge 添加最大连接数限制
4. 清理 pendingAcks 和 taskEventHistory
5. 移除硬编码回退密钥
6. 修复 config/runtime/test.json 配置
7. 评估并调整 prod 并发限制

**短期内处理 (本月内)**:
1. 统一安全配置（remoteWorkerRegistration）
2. 实现无界 Map 的 LRU/TTL 驱逐
3. 统一 API 响应信封格式
4. 完善 Redis idempotency 实现
5. 添加分布式去重中间件
6. 实现 cache stampede 保护

**长期规划**:
1. 建立完整的连接管理和监控
2. 实现后台清理任务框架
3. 添加配置 schema 验证
4. 优化高频繁操作的数据结构
5. 建立内存使用基线和告警

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十五轮 - 构建测试与类型系统Review）

### 发现的新问题

#### 1. [构建] [高严重] [无 TypeScript 增量编译缓存]
- **文件/路径**: `tsconfig.json`
- **问题描述**: 
  - 无 `incremental: true` 配置
  - 无 `tsbuildinfo` 文件
  - 每次构建是完整重建（9.6秒）
  - TypeScript 无法使用增量编译
- **建议修复**: 
  1. 在 tsconfig.json 添加 `"incremental": true`
  2. 添加 `"tsbuildinfo": ".tsbuildinfo"` 到 .gitignore
  3. 实现项目引用以支持并行构建

#### 2. [构建] [高严重] [76个 npm scripts 大部分重复构建]
- **文件/路径**: `package.json`
- **问题描述**: 
  - 40+ CLI scripts 都运行 `npm run build && node ...`
  - 每个脚本前都执行完整重建
  - 无法直接运行预构建的 CLI
  - 20+ stable:* 脚本遵循相同模式
- **建议修复**: 
  1. 创建 `build:cli` 脚本构建一次
  2. 让 CLI 脚本使用预构建的 dist 文件
  3. 添加 npm run dev 或类似的无需重建的执行方式

#### 3. [源代码] [高严重] [168处 any 类型使用]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - `stable-runner-factory.ts`: `opts: any` -> `any`
  - `billing-service-async.ts`: `input: any` 方法签名
  - 重试/超时工具使用 `(...args: any[]) => Promise<any>`
  - 绕过类型安全
- **建议修复**: 
  1. 使用泛型替代 any
  2. 替换为具体的输入类型
  3. 添加 ESLint 规则禁止 any

#### 4. [源代码] [高严重] [38处 @ts-ignore 指令]
- **文件/路径**: 多个文件
- **问题描述**: 
  - 14个 stability 相关文件有 `@ts-ignore ExecutionRecord type mismatch`
  - 5个 harness-sdk 文件有 `Partial<HarnessRun> doesn't have all required properties`
  - 2个文件有 `exactOptionalPropertyTypes` 问题
- **建议修复**: 
  1. 修复 ExecutionRecord 类型不匹配
  2. 完成 HarnessRun 类型的必需属性
  3. 移除 @ts-ignore 使用

#### 5. [源代码] [高严重] [ExecutionRecord 类型不匹配]
- **文件/路径**: `src/platform/stability/stable-evidence-bundle-support.ts:783` 等14个文件
- **问题描述**: 
  - 插入 execution record 时 TypeScript 拒绝类型
  - `ExecutionRecord` 接口要求新字段但 insert 对象缺失
  - 或 store 的 insert 方法接受不同类型
- **建议修复**: 
  1. 对齐 ExecutionRecord 接口和 store 类型
  2. 添加缺失字段到 insert 对象
  3. 或调整 store 的 insert 方法类型签名

#### 6. [源代码] [中严重] [30+处 as unknown as 双类型转换]
- **文件/路径**: `division-loader.ts`, `domain-baseline-catalog.ts`, `harness-sdk`
- **问题描述**: 
  - 完全绕过类型安全
  - 通常是类型架构问题的信号
- **建议修复**: 
  1. 修复上游类型问题
  2. 使用更具体的类型断言
  3. 重构类型层次结构

#### 7. [构建] [中严重] [rimraf  extraneous - 未在 package.json 声明]
- **文件/路径**: `node_modules/rimraf`
- **问题描述**: 
  - rimraf@6.1.3 存在于 node_modules 但未在 package.json 声明
  - 可能导致依赖问题
- **建议修复**: 
  1. 添加到 dependencies 或 devDependencies
  2. 或移除并使用其他清理方式

#### 8. [测试] [高严重] [178处直接 process.env 变更无抽象]
- **文件/路径**: `tests/unit/domains/registry/plugin-spi-registry.test.ts:640-687`
- **问题描述**: 
  - 直接读取/写入 process.env
  - 在 beforeEach/afterEach 中手动保存/恢复
  - 无 helper 抽象
- **建议修复**: 
  1. 创建 env helper 函数
  2. 统一保存和恢复模式
  3. 添加测试隔离验证

#### 9. [测试] [高严重] [21,998处 unlinkSync 调用无集中清理]
- **文件/路径**: 多个测试文件
- **问题描述**: 
  - 每个测试重新实现清理逻辑
  - 无集中清理 utility
  - 可能导致 EBUSY (Windows) 或并发访问问题
- **建议修复**: 
  1. 创建集中的测试清理 utility
  2. 使用 afterEach 确保清理
  3. 考虑使用临时目录

#### 10. [测试] [中严重] [无统一 mocking 框架]
- **文件/路径**: `tests/` 多个目录
- **问题描述**: 
  - 10,579 处 vi.fn, jest.mock, sinon 等调用
  - 无统一的 mocking 框架可见
  - 模式不一致
- **建议修复**: 
  1. 建立统一的 mock factory
  2. 标准化 mock 模式
  3. 添加 mocking 最佳实践文档

#### 11. [测试] [中严重] [Singleton 状态重置未标准化]
- **文件/路径**: `tests/unit/platform/five-plane-execution/dispatcher.test.ts`
- **问题描述**: 
  - 测试通过特定函数重置 singletons
  - 每个测试重复自己的 reset 逻辑
  - 无统一的 resetAllSingletons()
- **建议修复**: 
  1. 创建统一的 singleton reset 机制
  2. 在 afterEach 中自动调用
  3. 文档化 singleton 重置要求

#### 12. [测试] [低严重] [无分支覆盖率要求]
- **文件/路径**: `.c8rc.json`
- **问题描述**: 
  - `"100": false` - 无覆盖率要求
  - 只测量源代码，不测量测试文件
- **建议修复**: 
  1. 添加分支覆盖率阈值
  2. 在 CI 中强制执行覆盖率 gate
  3. 关注关键代码路径

#### 13. [网络] [高严重] [ScopedExternalAccessSandbox performHttpRequest 无超时]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/scoped-external-access-sandbox.ts:289`
- **问题描述**: 
  - fetch 调用无 timeout 参数
  - 挂起的请求可能永久阻塞
- **建议修复**: 
  1. 添加 AbortController timeout
  2. 使用标准的超时模式
  3. 记录超时配置

#### 14. [网络] [高严重] [OIDC 服务 fetch 调用无超时]
- **文件/路径**: `src/org-governance/sso-scim/oidc/oidc-service.ts:565,272`
- **问题描述**: 
  - OIDC token 交换无 retry logic
  - 无 timeout/abort signal
  - 认证流程可能挂起
- **建议修复**: 
  1. 添加 fetch timeout
  2. 添加 retry logic
  3. 使用标准的超时配置

#### 15. [网络] [高严重] [plugin-runtime-child 替换 globalThis.fetch 为 no-op]
- **文件/路径**: `src/domains/registry/plugin-runtime-child.ts:112`
- **问题描述**: 
  ```typescript
  globalThis.fetch = (async () => {}) as unknown as typeof globalThis.fetch;
  ```
  - 插件上下文中完全替换 fetch 为空函数
  - 插件的所有出站 HTTP 请求都会失败
  - 看起来是有意为之但可能造成问题
- **建议修复**: 
  1. 确认这是有意行为
  2. 文档化 fetch 替换行为
  3. 确保不会意外影响其他代码

#### 16. [网络] [中严重] [无显式 http.Agent 配置]
- **文件/路径**: 全局
- **问题描述**: 
  - 无显式的 http.Agent/https.Agent 配置
  - 依赖 Node.js 默认连接池
  - 无 maxSockets 或 maxConnections 配置
  - 高吞吐场景可能出问题
- **建议修复**: 
  1. 添加 http Agent 配置
  2. 文档化连接池大小
  3. 监控连接使用情况

#### 17. [序列化] [中严重] [合同版本硬编码为字符串字面量]
- **文件/路径**: `src/platform/contracts/inter-plane-contract-gateway.ts:184`
- **问题描述**: 
  - `schemaVersion: "v4.3"` 硬编码
  - 难以追踪合同版本演变
- **建议修复**: 
  1. 提取到类型化常量或 enum
  2. 集中管理合同版本
  3. 添加版本兼容性检查

#### 18. [序列化] [低严重] [Timestamp = string 丢失精度]
- **文件/路径**: `src/platform/contracts/types/domain/primitives.ts:10`
- **问题描述**: 
  - `type Timestamp = string` 无类型精度
  - ISO 8601 字符串无时区信息
- **建议修复**: 
  1. 使用 Date 对象或 branded string 类型
  2. 明确时区处理约定
  3. 文档化时间戳语义

#### 19. [源代码] [中严重] [29处非空断言 !.]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - 使用 `!.` 断言非空
  - 可能导致运行时错误
- **建议修复**: 
  1. 使用可选链 `?.` 或空值合并 `??`
  2. 添加适当的 null 检查
  3. 减少非空断言使用

#### 20. [API] [中严重] [API 路由 schema 版本硬编码]
- **文件/路径**: `src/platform/five-plane-interface/api/federation-routing-service.ts`
- **问题描述**: 
  - Zod schema 验证存在
  - 但 schema 版本未与 contract 版本同步
- **建议修复**: 
  1. 从 contract 版本派生 schema 版本
  2. 添加版本一致性检查
  3. 文档化版本关系

### 总结

本次补充Review（第十五轮 - 构建测试与类型系统Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. 无 TypeScript 增量编译缓存（每次完整重建9.6秒）
2. 76个 npm scripts 大部分重复构建
3. 168处 any 类型使用（绕过类型安全）
4. 38处 @ts-ignore 指令
5. ExecutionRecord 类型不匹配（14个文件）
6. 178处直接 process.env 变更无抽象
7. ScopedExternalAccessSandbox performHttpRequest 无超时
8. OIDC 服务 fetch 调用无超时
9. plugin-runtime-child 替换 globalThis.fetch 为 no-op

**中优先级**:
1. 30+处 as unknown as 双类型转换
2. rimraf extraneous 未声明
3. 21,998处 unlinkSync 无集中清理
4. 无统一 mocking 框架
5. Singleton 状态重置未标准化
6. 无显式 http.Agent 配置
7. 合同版本硬编码为字符串
8. Timestamp = string 丢失精度
9. 非空断言使用

**低优先级**:
1. 无分支覆盖率要求
2. API 路由 schema 版本未同步

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 72 | 87 | 39 | 198 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **109** | **182** | **85** | **376** |

### 优先修复建议

**立即处理 (本周内)**:
1. 添加 TypeScript 增量编译 (`incremental: true`)
2. 重构 npm scripts 避免重复构建
3. 减少 any 类型使用到 <50
4. 移除所有 @ts-ignore 指令
5. 修复 ExecutionRecord 类型不匹配
6. 添加 fetch 超时到 OIDC 和 sandbox

**短期内处理 (本月内)**:
1. 创建集中的测试清理 utility
2. 建立统一的 mocking 框架
3. 标准化 singleton 重置机制
4. 添加 http.Agent 连接池配置
5. 提取合同版本到类型化常量

**长期规划**:
1. 实现完整的类型安全（无 any）
2. 建立测试基础设施标准
3. 实现构建缓存优化
4. 完善网络层超时和重试
5. 建立覆盖率门禁标准

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十四轮 - 日志事件与工作流Review）

### 发现的新问题

#### 1. [可观测性] [高严重] [StructuredLogEntry 无 requestId 字段]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - StructuredLogEntry 接口无 requestId 字段
  - 只有 correlationId，它回退到 traceId
  - 无法独立追踪单个 HTTP 请求
- **建议修复**: 
  1. 在 StructuredLogEntry 添加 requestId 字段
  2. 在 HTTP 中间件中生成和注入 requestId
  3. 确保 requestId 流经所有日志调用

#### 2. [可观测性] [高严重] [StructuredLogger data 字段无自动清洗]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - 如果开发者传递 `{ password: "..." }` 给 logger.info，密码会被记录
  - 无字段级脱敏机制
  - 依赖开发者手动排除敏感字段
- **建议修复**: 
  1. 添加 OBSERVE_OUTPUT_BLACKLIST 类似的黑名单
  2. 实现 redact/mask/sanitize 工具函数
  3. 在日志传输前自动清洗敏感字段

#### 3. [可观测性] [中严重] [日志中敏感数据无 PII 处理]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - 无 PII 重写工具
  - 开发者必须手动避免记录敏感数据
  - 存在意外记录敏感信息的风险
- **建议修复**: 
  1. 添加 PII 检测和重写工具
  2. 在文档中明确记录敏感字段列表
  3. 添加测试验证敏感数据不被记录

#### 4. [可观测性] [中严重] [59处 console.* 直接调用]
- **文件/路径**: 多个源文件
- **问题描述**: 
  - 应该使用 StructuredLogger
  - console 调用可能不被日志聚合系统收集
  - 难以追踪和关联
- **建议修复**: 
  1. 替换所有 console.* 为 StructuredLogger
  2. 添加 ESLint 规则禁止 console.*
  3. 保留少量允许的 CLI 输出

#### 5. [可观测性] [中严重] [部分 logger.warn 调用缺少结构化数据]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:122`
- **问题描述**: 
  ```typescript
  logger.warn("WebSocket connection rejected: missing subprotocol token")
  ```
  - 只有消息，无 data 对象
  - 难以关联和查询
- **建议修复**: 
  1. 添加 `{ data: { ... } }` 对象
  2. 确保所有日志包含上下文数据
  3. 添加日志审查工具

#### 6. [源代码] [高严重] [Harness while(true) 循环无硬性迭代上限]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:1442`
- **问题描述**: 
  ```typescript
  while (true) {
    // ...
    // 依赖 budget gate 和 guardrail vibration 退出
    // 如果两者都失败，循环永久继续
  }
  ```
  - 依赖 budget gate 和 guardrail vibration 退出
  - 无硬性迭代次数限制
  - 可能导致无限循环
- **建议修复**: 
  1. 添加硬性 maxIterations 上限
  2. 在达到上限时强制退出
  3. 记录详细的迭代统计用于调试

#### 7. [源代码] [高严重] [Oapeflir while(true) 循环可能无限重规划]
- **文件/路径**: `src/platform/five-plane-orchestration/oapeflir/oapeflir-loop-service.ts:355`
- **问题描述**: 
  - 如果 loopReplanDecision.shouldReplan 永不变为 false
  - 重规划永远无法减少计划错误
  - 可能导致无限循环
- **建议修复**: 
  1. 添加重规划次数上限
  2. 在达到上限时强制接受当前结果
  3. 添加重规划质量衰减检测

#### 8. [源代码] [中严重] [Recovery 流程允许终态转换到 paused]
- **文件/路径**: `src/platform/five-plane-execution/runtime-state-machine.ts:105-108`
- **问题描述**: 
  - completed, failed, cancelled, aborted 可转换到 paused 进行恢复
  - 创建恢复路径可能重新进入执行
  - 需要验证此行为的正确性
- **建议修复**: 
  1. 审查 recovery 转换的合法性
  2. 确保不会导致状态不一致
  3. 添加转换前置条件验证

#### 9. [国际化] [高严重] [API 错误消息硬编码英文]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/api-error.ts`
- **问题描述**: 
  ```typescript
  return new ApiError(404, "api.task_not_found", "Task not found.");
  ```
  - 错误消息硬编码英文
  - 无后端 i18n 机制
  - 非英文客户端收到不本地化的错误
- **建议修复**: 
  1. 使用错误码而非硬编码消息
  2. 客户端根据错误码本地化
  3. 或在后端添加 i18n 支持

#### 10. [国际化] [中严重] [UI 硬编码字符串未翻译]
- **文件/路径**: `ui/packages/features/conversation/src/web/index.tsx`
- **问题描述**: 
  - "Build Plan", "Execute", "Messages" 等按钮标签硬编码
  - 不使用 i18n 系统
- **建议修复**: 
  1. 使用 `translateFeatureCopy` 替换硬编码
  2. 确保所有用户可见文本通过 i18n
  3. 添加硬编码字符串检测

#### 11. [国际化] [中严重] [Arabic 目录不完整]
- **文件/路径**: `ui/packages/shared/i18n/src/catalogs/ar-SA.ts`
- **问题描述**: 
  - ar-SA 只有约18条消息
  - en-US 和 zh-CN 有约117条
  - 缺失的 key 返回 key 本身
- **建议修复**: 
  1. 补充 Arabic 翻译
  2. 确保所有 key 都有翻译
  3. 添加翻译完整性测试

#### 12. [国际化] [低严重] [translateFeatureCopy 无回退默认值]
- **文件/路径**: `ui/packages/shared/i18n/`
- **问题描述**: 
  - 如果 feature ID 不在 catalog 中
  - 返回 undefined title/summary
  - 无合理回退
- **建议修复**: 
  1. 添加回退到 key 或默认字符串
  2. 或使用 feature ID 作为显示名
  3. 确保 UI 不显示 undefined

#### 13. [运维] [高严重] [健康检查无 readiness/liveness 区分]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts`
- **问题描述**: 
  - /healthz, /v1/healthz, /health 返回相同报告
  - 无 readiness probe（可处理流量？）
  - 无 liveness probe（系统存活？）
  - canAcceptTraffic() 未通过 HTTP 暴露
- **建议修复**: 
  1. 添加 /ready 和 /live 端点
  2. Readiness 检查依赖项就绪
  3. Liveness 检查进程健康

#### 14. [运维] [高严重] [shutdown 期间健康检查存在竞态]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts`
- **问题描述**: 
  - getHealthReportAsync() 异步检查 dbWritable
  - shutdown 期间数据库连接可能关闭
  - 健康检查仍在传入
- **建议修复**: 
  1. 在 shutdown 时返回 unhealthy
  2. 添加 shutdown 状态标志
  3. 停止接受新的健康检查请求

#### 15. [运维] [高严重] [LeaderElectionService 未与 GracefulShutdown 集成]
- **文件/路径**: `src/platform/five-plane-execution/ha/leader-election-service.ts`
- **问题描述**: 
  - LeaderElectionService.stop() 处理优雅退位
  - 但未在全局 graceful shutdown 中注册
  - 需显式注册为 shutdown handler
- **建议修复**: 
  1. 将 LeaderElectionService 添加到 shutdown handlers
  2. 确保领导者在 shutdown 时让位
  3. 验证关闭顺序正确

#### 16. [运维] [中严重] [addHandler() 无显式排序约定]
- **文件/路径**: `src/platform/five-plane-execution/startup/graceful-shutdown.ts`
- **问题描述**: 
  - 虽然 handlers 逆序执行
  - 但无显式接口声明 handler 依赖关系
  - 关键 handlers（如关闭数据库连接）应最后运行
- **建议修复**: 
  1. 添加优先级或阶段参数
  2. 文档化关键 handler 顺序要求
  3. 添加顺序验证测试

#### 17. [运维] [中严重] [unref'd timers 可能不触发]
- **文件/路径**: `src/platform/five-plane-execution/startup/process-error-handlers.ts:106`
- **问题描述**: 
  - `setTimeout(...).unref()` 不保持事件循环活跃
  - 如果主线程空闲，强制退出可能不触发
- **建议修复**: 
  1. 移除 .unref() 除非确实需要
  2. 或确保有其他方式保持事件循环
  3. 添加超时触发验证测试

#### 18. [运维] [中严重] [StartupConsistencyChecker 阻塞流量但不暴露状态]
- **文件/路径**: `src/platform/five-plane-startup-plan.ts`
- **问题描述**: 
  - canAcceptTraffic() 在 P0 问题存在时返回 false
  - 但 _trafficBlocked 是内部状态
  - 负载均衡器访问 /healthz 仍会得到 "ok"
- **建议修复**: 
  1. 在健康报告中暴露 trafficBlocked 状态
  2. 或添加单独的健康端点指示就绪状态
  3. 确保负载均衡器在阻塞时不路由流量

#### 19. [事件系统] [中严重] [TypedEventBus vs EventEmitter 混用]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - 项目同时使用 TypedEventBus 和原生 EventEmitter
  - TypedEventBus 应该是标准
  - 混用可能导致类型安全问题
- **建议修复**: 
  1. 创建迁移计划统一使用 TypedEventBus
  2. 添加 ESLint 规则禁止原生 EventEmitter
  3. 在 CI 中检测混用情况

#### 20. [源代码] [低严重] [无事件排序保证]
- **文件/路径**: `src/platform/five-plane-state-evidence/events/`
- **问题描述**: 
  - 无明确的事件排序保证文档
  - 事件处理可能乱序
  - 依赖方需要处理乱序
- **建议修复**: 
  1. 文档化事件排序语义
  2. 如果需要排序，实现序列号机制
  3. 在事件处理中考虑乱序情况

### 总结

本次补充Review（第十四轮 - 日志事件与工作流Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. StructuredLogEntry 无 requestId 字段
2. StructuredLogger data 字段无自动清洗（敏感数据风险）
3. Harness while(true) 循环无硬性迭代上限
4. Oapeflir while(true) 循环可能无限重规划
5. API 错误消息硬编码英文（无 i18n）
6. 健康检查无 readiness/liveness 区分
7. shutdown 期间健康检查存在竞态
8. LeaderElectionService 未与 GracefulShutdown 集成

**中优先级**:
1. 日志中敏感数据无 PII 处理
2. 59处 console.* 直接调用
3. 部分 logger.warn 调用缺少结构化数据
4. Recovery 流程允许终态转换到 paused
5. UI 硬编码字符串未翻译
6. Arabic 目录不完整
7. addHandler() 无显式排序约定
8. unref'd timers 可能不触发
9. StartupConsistencyChecker 阻塞流量但不暴露状态
10. TypedEventBus vs EventEmitter 混用

**低优先级**:
1. translateFeatureCopy 无回退默认值
2. 无事件排序保证

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 68 | 80 | 37 | 185 |
| 测试 | 8 | 14 | 5 | 27 |
| 配置 | 10 | 38 | 24 | 72 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **103** | **171** | **82** | **356** |

### 优先修复建议

**立即处理 (本周内)**:
1. 在 StructuredLogEntry 添加 requestId 字段
2. 实现日志敏感字段自动清洗
3. 为工作流循环添加硬性迭代上限
4. 添加 readiness/liveness 健康检查端点
5. 将 LeaderElectionService 集成到 GracefulShutdown
6. 实现 API 错误消息 i18n 支持

**短期内处理 (本月内)**:
1. 替换所有 console.* 为 StructuredLogger
2. 补充 Arabic i18n 目录
3. 修复 UI 硬编码字符串
4. 统一 TypedEventBus 替代 EventEmitter
5. 解决 shutdown 竞态条件
6. 添加事件排序保证文档

**长期规划**:
1. 建立完整的 PII 处理框架
2. 实现日志完整性验证
3. 建立工作流安全循环检测
4. 完善健康检查和就绪探测
5. 建立国际化完整测试

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十五轮 - 构建测试与类型系统Review）

### 发现的新问题

#### 1. [构建] [高严重] [无 TypeScript 增量编译缓存]
- **文件/路径**: `tsconfig.json`
- **问题描述**: 
  - 无 `incremental: true` 配置
  - 无 `tsbuildinfo` 文件
  - 每次构建是完整重建（9.6秒）
  - TypeScript 无法使用增量编译
- **建议修复**: 
  1. 在 tsconfig.json 添加 `"incremental": true`
  2. 添加 `"tsbuildinfo": ".tsbuildinfo"` 到 .gitignore
  3. 实现项目引用以支持并行构建

#### 2. [构建] [高严重] [76个 npm scripts 大部分重复构建]
- **文件/路径**: `package.json`
- **问题描述**: 
  - 40+ CLI scripts 都运行 `npm run build && node ...`
  - 每个脚本前都执行完整重建
  - 无法直接运行预构建的 CLI
  - 20+ stable:* 脚本遵循相同模式
- **建议修复**: 
  1. 创建 `build:cli` 脚本构建一次
  2. 让 CLI 脚本使用预构建的 dist 文件
  3. 添加 npm run dev 或类似的无需重建的执行方式

#### 3. [源代码] [高严重] [168处 any 类型使用]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - `stable-runner-factory.ts`: `opts: any` -> `any`
  - `billing-service-async.ts`: `input: any` 方法签名
  - 重试/超时工具使用 `(...args: any[]) => Promise<any>`
  - 绕过类型安全
- **建议修复**: 
  1. 使用泛型替代 any
  2. 替换为具体的输入类型
  3. 添加 ESLint 规则禁止 any

#### 4. [源代码] [高严重] [38处 @ts-ignore 指令]
- **文件/路径**: 多个文件
- **问题描述**: 
  - 14个 stability 相关文件有 `@ts-ignore ExecutionRecord type mismatch`
  - 5个 harness-sdk 文件有 `Partial<HarnessRun> doesn't have all required properties`
  - 2个文件有 `exactOptionalPropertyTypes` 问题
- **建议修复**: 
  1. 修复 ExecutionRecord 类型不匹配
  2. 完成 HarnessRun 类型的必需属性
  3. 移除 @ts-ignore 使用

#### 5. [源代码] [高严重] [ExecutionRecord 类型不匹配]
- **文件/路径**: `src/platform/stability/stable-evidence-bundle-support.ts:783` 等14个文件
- **问题描述**: 
  - 插入 execution record 时 TypeScript 拒绝类型
  - `ExecutionRecord` 接口要求新字段但 insert 对象缺失
  - 或 store 的 insert 方法接受不同类型
- **建议修复**: 
  1. 对齐 ExecutionRecord 接口和 store 类型
  2. 添加缺失字段到 insert 对象
  3. 或调整 store 的 insert 方法类型签名

#### 6. [源代码] [中严重] [30+处 as unknown as 双类型转换]
- **文件/路径**: `division-loader.ts`, `domain-baseline-catalog.ts`, `harness-sdk`
- **问题描述**: 
  - 完全绕过类型安全
  - 通常是类型架构问题的信号
- **建议修复**: 
  1. 修复上游类型问题
  2. 使用更具体的类型断言
  3. 重构类型层次结构

#### 7. [构建] [中严重] [rimraf  extraneous - 未在 package.json 声明]
- **文件/路径**: `node_modules/rimraf`
- **问题描述**: 
  - rimraf@6.1.3 存在于 node_modules 但未在 package.json 声明
  - 可能导致依赖问题
- **建议修复**: 
  1. 添加到 dependencies 或 devDependencies
  2. 或移除并使用其他清理方式

#### 8. [测试] [高严重] [178处直接 process.env 变更无抽象]
- **文件/路径**: `tests/unit/domains/registry/plugin-spi-registry.test.ts:640-687`
- **问题描述**: 
  - 直接读取/写入 process.env
  - 在 beforeEach/afterEach 中手动保存/恢复
  - 无 helper 抽象
- **建议修复**: 
  1. 创建 env helper 函数
  2. 统一保存和恢复模式
  3. 添加测试隔离验证

#### 9. [测试] [高严重] [21,998处 unlinkSync 调用无集中清理]
- **文件/路径**: 多个测试文件
- **问题描述**: 
  - 每个测试重新实现清理逻辑
  - 无集中清理 utility
  - 可能导致 EBUSY (Windows) 或并发访问问题
- **建议修复**: 
  1. 创建集中的测试清理 utility
  2. 使用 afterEach 确保清理
  3. 考虑使用临时目录

#### 10. [测试] [中严重] [无统一 mocking 框架]
- **文件/路径**: `tests/` 多个目录
- **问题描述**: 
  - 10,579 处 vi.fn, jest.mock, sinon 等调用
  - 无统一的 mocking 框架可见
  - 模式不一致
- **建议修复**: 
  1. 建立统一的 mock factory
  2. 标准化 mock 模式
  3. 添加 mocking 最佳实践文档

#### 11. [测试] [中严重] [Singleton 状态重置未标准化]
- **文件/路径**: `tests/unit/platform/five-plane-execution/dispatcher.test.ts`
- **问题描述**: 
  - 测试通过特定函数重置 singletons
  - 每个测试重复自己的 reset 逻辑
  - 无统一的 resetAllSingletons()
- **建议修复**: 
  1. 创建统一的 singleton reset 机制
  2. 在 afterEach 中自动调用
  3. 文档化 singleton 重置要求

#### 12. [测试] [低严重] [无分支覆盖率要求]
- **文件/路径**: `.c8rc.json`
- **问题描述**: 
  - `"100": false` - 无覆盖率要求
  - 只测量源代码，不测量测试文件
- **建议修复**: 
  1. 添加分支覆盖率阈值
  2. 在 CI 中强制执行覆盖率 gate
  3. 关注关键代码路径

#### 13. [网络] [高严重] [ScopedExternalAccessSandbox performHttpRequest 无超时]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/scoped-external-access-sandbox.ts:289`
- **问题描述**: 
  - fetch 调用无 timeout 参数
  - 挂起的请求可能永久阻塞
- **建议修复**: 
  1. 添加 AbortController timeout
  2. 使用标准的超时模式
  3. 记录超时配置

#### 14. [网络] [高严重] [OIDC 服务 fetch 调用无超时]
- **文件/路径**: `src/org-governance/sso-scim/oidc/oidc-service.ts:565,272`
- **问题描述**: 
  - OIDC token 交换无 retry logic
  - 无 timeout/abort signal
  - 认证流程可能挂起
- **建议修复**: 
  1. 添加 fetch timeout
  2. 添加 retry logic
  3. 使用标准的超时配置

#### 15. [网络] [高严重] [plugin-runtime-child 替换 globalThis.fetch 为 no-op]
- **文件/路径**: `src/domains/registry/plugin-runtime-child.ts:112`
- **问题描述**: 
  ```typescript
  globalThis.fetch = (async () => {}) as unknown as typeof globalThis.fetch;
  ```
  - 插件上下文中完全替换 fetch 为空函数
  - 插件的所有出站 HTTP 请求都会失败
  - 看起来是有意为之但可能造成问题
- **建议修复**: 
  1. 确认这是有意行为
  2. 文档化 fetch 替换行为
  3. 确保不会意外影响其他代码

#### 16. [网络] [中严重] [无显式 http.Agent 配置]
- **文件/路径**: 全局
- **问题描述**: 
  - 无显式的 http.Agent/https.Agent 配置
  - 依赖 Node.js 默认连接池
  - 无 maxSockets 或 maxConnections 配置
  - 高吞吐场景可能出问题
- **建议修复**: 
  1. 添加 http Agent 配置
  2. 文档化连接池大小
  3. 监控连接使用情况

#### 17. [序列化] [中严重] [合同版本硬编码为字符串字面量]
- **文件/路径**: `src/platform/contracts/inter-plane-contract-gateway.ts:184`
- **问题描述**: 
  - `schemaVersion: "v4.3"` 硬编码
  - 难以追踪合同版本演变
- **建议修复**: 
  1. 提取到类型化常量或 enum
  2. 集中管理合同版本
  3. 添加版本兼容性检查

#### 18. [序列化] [低严重] [Timestamp = string 丢失精度]
- **文件/路径**: `src/platform/contracts/types/domain/primitives.ts:10`
- **问题描述**: 
  - `type Timestamp = string` 无类型精度
  - ISO 8601 字符串无时区信息
- **建议修复**: 
  1. 使用 Date 对象或 branded string 类型
  2. 明确时区处理约定
  3. 文档化时间戳语义

#### 19. [源代码] [中严重] [29处非空断言 !.]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - 使用 `!.` 断言非空
  - 可能导致运行时错误
- **建议修复**: 
  1. 使用可选链 `?.` 或空值合并 `??`
  2. 添加适当的 null 检查
  3. 减少非空断言使用

#### 20. [API] [中严重] [API 路由 schema 版本硬编码]
- **文件/路径**: `src/platform/five-plane-interface/api/federation-routing-service.ts`
- **问题描述**: 
  - Zod schema 验证存在
  - 但 schema 版本未与 contract 版本同步
- **建议修复**: 
  1. 从 contract 版本派生 schema 版本
  2. 添加版本一致性检查
  3. 文档化版本关系

### 总结

本次补充Review（第十五轮 - 构建测试与类型系统Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. 无 TypeScript 增量编译缓存（每次完整重建9.6秒）
2. 76个 npm scripts 大部分重复构建
3. 168处 any 类型使用（绕过类型安全）
4. 38处 @ts-ignore 指令
5. ExecutionRecord 类型不匹配（14个文件）
6. 178处直接 process.env 变更无抽象
7. ScopedExternalAccessSandbox performHttpRequest 无超时
8. OIDC 服务 fetch 调用无超时
9. plugin-runtime-child 替换 globalThis.fetch 为 no-op

**中优先级**:
1. 30+处 as unknown as 双类型转换
2. rimraf extraneous 未声明
3. 21,998处 unlinkSync 无集中清理
4. 无统一 mocking 框架
5. Singleton 状态重置未标准化
6. 无显式 http.Agent 配置
7. 合同版本硬编码为字符串
8. Timestamp = string 丢失精度
9. 非空断言使用

**低优先级**:
1. 无分支覆盖率要求
2. API 路由 schema 版本未同步

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 72 | 87 | 39 | 198 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **109** | **182** | **85** | **376** |

### 优先修复建议

**立即处理 (本周内)**:
1. 添加 TypeScript 增量编译 (`incremental: true`)
2. 重构 npm scripts 避免重复构建
3. 减少 any 类型使用到 <50
4. 移除所有 @ts-ignore 指令
5. 修复 ExecutionRecord 类型不匹配
6. 添加 fetch 超时到 OIDC 和 sandbox

**短期内处理 (本月内)**:
1. 创建集中的测试清理 utility
2. 建立统一的 mocking 框架
3. 标准化 singleton 重置机制
4. 添加 http.Agent 连接池配置
5. 提取合同版本到类型化常量

**长期规划**:
1. 实现完整的类型安全（无 any）
2. 建立测试基础设施标准
3. 实现构建缓存优化
4. 完善网络层超时和重试
5. 建立覆盖率门禁标准

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十二轮 - 错误处理与多租户安全Review）

### 发现的新问题

#### 1. [安全] [严重] [listQuotas() 返回所有租户配额 - 信息泄露]
- **文件/路径**: `src/platform/five-plane-control-plane/tenant/tenant-execution-isolation-service.ts:222-234`
- **问题描述**: 
  ```typescript
  listQuotas(tenantId?: string): TenantQuota[] {
    if (tenantId) {
      return this.db.connection.prepare(`SELECT * FROM tenant_quotas WHERE tenant_id = ?`).all(tenantId);
    }
    // WITHOUT tenantId: 返回所有租户的配额！
    return this.db.connection.prepare(`SELECT * FROM tenant_quotas`).all();
  }
  ```
  - 当 tenantId 为空时返回所有租户的配额
  - 未经授权的信息泄露
- **建议修复**: 
  1. 要求调用者必须提供 tenantId
  2. 添加权限验证确保只能查看自己的配额
  3. 审计日志记录所有配额访问

#### 2. [安全] [高严重] [assertTaskTenantAccess() 返回404而非403 - 资源存在泄露]
- **文件/路径**: `src/platform/five-plane-interface/api/utils.ts:146-148`
- **问题描述**: 
  ```typescript
  if (resourceTenantId !== principal.tenantId) {
    throw new ApiError(404, notFoundCode, notFoundMessage);  // 应该是403
  }
  ```
  - 跨租户访问返回404而非403
  - 泄露了资源存在但属于其他租户的信息
  - 攻击者可检测有效task ID
- **建议修复**: 
  1. 改为返回403 Forbidden
  2. 不要泄露资源是否存在

#### 3. [安全] [高严重] [crossTenantRequest 标志由调用者控制]
- **文件/路径**: `src/platform/five-plane-interface/org-routing/index.ts:163`
- **问题描述**: 
  - `routeOrgBudget()` 接受 `crossTenantRequest?: boolean` 参数
  - 该标志由调用者设置，非系统派生
  - 攻击者可能操纵此标志绕过跨租户限制
- **建议修复**: 
  1. 标志应由系统状态派生，不接受调用者输入
  2. 添加审计日志追踪跨租户请求
  3. 实现零信任跨租户访问模型

#### 4. [源代码] [高严重] [single-task-happy-path 无重试机制]
- **文件/路径**: `src/platform/five-plane-execution/execution-engine/single-task-happy-path.ts:336-337`
- **问题描述**: 
  - `maxRetries: 0` 和 `retryBackoff: "none"`
  - 瞬态失败无自动重试
  - LLM调用失败时无fallback到备用provider
- **建议修复**: 
  1. 启用非关键执行的重试
  2. 实现LLM provider fallback链（Anthropic → OpenAI → MiniMax）
  3. 使用domain baseline catalog的primary/fallback模型偏好

#### 5. [源代码] [高严重] [LLM调用无熔断器保护]
- **文件/路径**: `src/platform/five-plane-execution/execution-engine/model-call-provider.ts`
- **问题描述**: 
  - 熔断器只存在于 channel-gateway 和 call-governance
  - 直接LLM provider调用无熔断器
  - provider降级时无法快速失败
- **建议修复**: 
  1. 在model-call-provider.ts添加熔断器
  2. 当provider失败率超阈值时快速失败
  3. 自动切换到备用provider

#### 6. [安全] [高严重] [分布式限流器 bypass - 实例间不共享]
- **文件/路径**: `src/platform/five-plane-interface/ingress/distributed-rate-limiter.ts:33`
- **问题描述**: 
  - Redis未配置时使用本地Map
  - 每个实例维护自己的localEntries
  - 攻击者可通过切换实例绕过限流
- **建议修复**: 
  1. 要求Redis配置用于生产
  2. 或使用Redis作为所有实例的共享后端
  3. 检测并警告未配置Redis的实例

#### 7. [配置] [中严重] [Soft quota (log_only) 实际不限制]
- **文件/路径**: `src/platform/five-plane-control-plane/tenant/tenant-execution-isolation-service.ts:448`
- **问题描述**: 
  ```typescript
  allowed: quota.enforcementAction === "log_only",  // log_only时返回true
  ```
  - soft quota只记录日志，不实际阻止
  - 配额形同虚设
- **建议修复**: 
  1. 明确区分"监控模式"和"强制模式"
  2. 对于硬配额必须阻止
  3. 文档化quota类型的实际行为

#### 8. [配置] [中严重] [HTTP层无限流头 - 客户端无法获知限制]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/response-hardening.ts`
- **问题描述**: 
  - 未返回 X-RateLimit-* 头
  - 只在429响应时返回retry-after-ms
  - 客户端无法主动管理请求速率
- **建议修复**: 
  1. 在所有响应添加 X-RateLimit-Limit, X-RateLimit-Remaining, X-RateLimit-Reset
  2. 使用标准头名称
  3. 遵循RFC 6585

#### 9. [配置] [中严重] [HTTP层无限流 - 可选且仅按IP]
- **文件/路径**: `src/platform/five-plane-interface/api/http-api-server.ts:334-351`
- **问题描述**: 
  - 限流仅在 `this.rateLimiter != null` 时生效
  - 限流key为 `${clientIp}:${endpoint}` 仅按IP
  - 无per-tenant/per-principal enforcement
- **建议修复**: 
  1. 默认启用限流
  2. 添加per-tenant限流key
  3. 确保所有环境默认配置限流

#### 10. [安全] [中严重] [processRuleMode 可能未强制执行]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/plugin-runtime-host.ts`
- **问题描述**: 
  - `SandboxPolicy.processRuleMode` 设置为 "allow" 或 "deny"
  - 但未找到实际阻止基于此策略生成进程的实际执行代码
  - `--allow-child-process` 标志仅在 sandboxed_process 隔离模式应用
- **建议修复**: 
  1. 验证processRuleMode实际被强制执行
  2. 确保所有隔离模式正确处理
  3. 添加测试验证进程创建被阻止

#### 11. [安全] [中严重] [容器模板替换未验证]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/plugin-runtime-host.ts:751`
- **问题描述**: 
  ```typescript
  rendered = rendered.split(placeholder).join(value);
  ```
  - 简单字符串替换
  - 虽然validateContainerLaunchPluginId()阻止\0和引号
  - 但模板替换本身未验证
- **建议修复**: 
  1. 添加输入验证防止注入
  2. 使用更安全的模板引擎
  3. 验证所有占位符被替换

#### 12. [安全] [中严重] [Adapter执行绕过沙箱]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/adapter-executor.ts`
- **问题描述**: 
  - Adapter执行REST/grpc/MQ调用不经过ScopedExternalAccessSandbox
  - 有自己的allowedDomains配置但非集中管理
  - 可能发起未限制的出站请求
- **建议修复**: 
  1. Adapter执行经过集中沙箱
  2. 统一external access策略
  3. 添加adapter出站请求审计

#### 13. [安全] [中严重] [null principal.tenantId 时静默放行]
- **文件/路径**: `src/platform/five-plane-interface/api/utils.ts:143-145`
- **问题描述**: 
  ```typescript
  if (principal.tenantId == null) {
    return;  // 静默放行
  }
  ```
  - 如果API网关允许无principal认证的请求通过
  - 可能授予跨租户访问权限
- **建议修复**: 
  1. 要求所有API请求有有效principal
  2. null tenantId应拒绝而非静默放行
  3. 添加审计日志

#### 14. [源代码] [中严重] [无LLM provider fallback链]
- **文件/路径**: `src/platform/five-plane-execution/execution-engine/model-call-provider.ts`
- **问题描述**: 
  - 主provider失败时无自动切换
  - domain-baseline-catalog.ts定义了primary/fallback模型偏好但未使用
  - 第一个provider失败即导致执行失败
- **建议修复**: 
  1. 实现provider fallback链
  2. 使用domain baseline的primary/fallback配置
  3. 按优先级尝试直到成功或全部失败

#### 15. [源代码] [中严重] [超时值硬编码]
- **文件/路径**: 多个文件
- **问题描述**: 
  - Effect buffer有固定超时reject
  - Channel gateway的requestTimeoutMs虽可配置但有max 30s限制
  - 无全局请求超时中间件
- **建议修复**: 
  1. 集中超时配置
  2. per-environment可配置
  3. 添加全局超时中间件

#### 16. [数据库] [中严重] [无down migration - 回滚不支持]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/sqlite/migration-runner.ts:45-47`
- **问题描述**: 
  - `rollbackSupported = false`
  - 每个migration只有 `downSql` 占位符
  - schema migration无法回滚
- **建议修复**: 
  1. 文档化回滚限制
  2. 在变更前创建完整备份
  3. 使用blue-green部署减少回滚需求

#### 17. [数据库] [中严重] [Migration 44特殊处理 - 表创建重复]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/sqlite/sqlite-migration-runtime-part3.ts`
- **问题描述**: 
  - Migration 43创建harness_runs表
  - Migration 44再次创建同名表（不同schema）
  - 使用applyCompatibleColumnMigrationIfKnown特殊处理
- **建议修复**: 
  1. 消除重复表创建
  2. 使用ALTER TABLE而非CREATE TABLE
  3. 简化migration逻辑

#### 18. [配置] [低严重] [Provider限流头未转发给客户端]
- **文件/路径**: `src/platform/model-gateway/provider-registry/base-chat-provider.ts:153`
- **问题描述**: 
  - 系统读取provider的ratelimitResetHeaderNames
  - 但不转发给客户端
  - 客户端无法知道provider限流状态
- **建议修复**: 
  1. 转发Provider的限流头
  2. 或添加应用层限流信息
  3. 帮助客户端优化请求

#### 19. [安全] [低严重] [Browser evaluate 接受任意脚本]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/browser-executor.ts:326`
- **问题描述**: 
  - `evaluate` action接受script参数
  - 在浏览器上下文运行（模拟）
  - 如浏览器上下文未正确沙箱化可能是向量
- **建议修复**: 
  1. 验证浏览器上下文正确沙箱化
  2. 消毒或限制script内容
  3. 记录所有evaluate调用

#### 20. [源代码] [低严重] [无全局错误边界包装器]
- **文件/路径**: `src/platform/five-plane-execution/execution-engine/`
- **问题描述**: 
  - 执行引擎缺少unhandled promise rejection的全局处理
  - single-task-happy-path的LLM fallback失败时错误向上传播
- **建议修复**: 
  1. 在执行引擎添加错误边界包装
  2. 统一错误处理模式
  3. 确保所有错误被捕获和记录

### 总结

本次补充Review（第十二轮 - 错误处理与多租户安全Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. listQuotas()返回所有租户配额（信息泄露）
2. assertTaskTenantAccess()返回404泄露资源存在
3. crossTenantRequest标志可被操纵
4. single-task-happy-path无重试（maxRetries=0）
5. LLM调用无熔断器保护
6. 分布式限流器可被bypass（实例间不共享）

**中优先级**:
1. Soft quota实际不阻止
2. 无X-RateLimit-*响应头
3. HTTP层限流可选且仅按IP
4. processRuleMode可能未强制
5. 容器模板替换未验证
6. Adapter执行绕过沙箱
7. null principal静默放行
8. 无LLM provider fallback链
9. 超时值硬编码
10. 无down migration
11. Migration 44特殊处理

**低优先级**:
1. Provider限流头未转发
2. Browser evaluate接受任意脚本
3. 无全局错误边界

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 60 | 68 | 31 | 159 |
| 测试 | 8 | 14 | 5 | 27 |
| 配置 | 8 | 35 | 21 | 64 |
| 安全 | 12 | 13 | 3 | 28 |
| 文档 | 2 | 7 | 3 | 12 |
| UI | 0 | 3 | 3 | 6 |
| 部署 | 1 | 10 | 6 | 17 |
| **合计** | **91** | **150** | **72** | **313** |

### 优先修复建议

**立即处理 (本周内)**:
1. 修复listQuotas()信息泄露漏洞
2. 修正assertTaskTenantAccess()返回403
3. 移除crossTenantRequest调用者控制
4. 启用执行引擎重试机制
5. 添加LLM provider熔断器

**短期内处理 (本月内)**:
1. 修复分布式限流器bypass问题
2. 添加X-RateLimit-*响应头
3. 启用默认HTTP限流（per-tenant）
4. 实现LLM provider fallback链
5. 验证并强制processRuleMode

**长期规划**:
1. 建立完整的限流和配额体系
2. 实现插件系统安全审计
3. 建立多租户安全测试
4. 完善错误处理和恢复机制
5. 统一超时和熔断配置

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十五轮 - 构建测试与类型系统Review）

### 发现的新问题

#### 1. [构建] [高严重] [无 TypeScript 增量编译缓存]
- **文件/路径**: `tsconfig.json`
- **问题描述**: 
  - 无 `incremental: true` 配置
  - 无 `tsbuildinfo` 文件
  - 每次构建是完整重建（9.6秒）
  - TypeScript 无法使用增量编译
- **建议修复**: 
  1. 在 tsconfig.json 添加 `"incremental": true`
  2. 添加 `"tsbuildinfo": ".tsbuildinfo"` 到 .gitignore
  3. 实现项目引用以支持并行构建

#### 2. [构建] [高严重] [76个 npm scripts 大部分重复构建]
- **文件/路径**: `package.json`
- **问题描述**: 
  - 40+ CLI scripts 都运行 `npm run build && node ...`
  - 每个脚本前都执行完整重建
  - 无法直接运行预构建的 CLI
  - 20+ stable:* 脚本遵循相同模式
- **建议修复**: 
  1. 创建 `build:cli` 脚本构建一次
  2. 让 CLI 脚本使用预构建的 dist 文件
  3. 添加 npm run dev 或类似的无需重建的执行方式

#### 3. [源代码] [高严重] [168处 any 类型使用]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - `stable-runner-factory.ts`: `opts: any` -> `any`
  - `billing-service-async.ts`: `input: any` 方法签名
  - 重试/超时工具使用 `(...args: any[]) => Promise<any>`
  - 绕过类型安全
- **建议修复**: 
  1. 使用泛型替代 any
  2. 替换为具体的输入类型
  3. 添加 ESLint 规则禁止 any

#### 4. [源代码] [高严重] [38处 @ts-ignore 指令]
- **文件/路径**: 多个文件
- **问题描述**: 
  - 14个 stability 相关文件有 `@ts-ignore ExecutionRecord type mismatch`
  - 5个 harness-sdk 文件有 `Partial<HarnessRun> doesn't have all required properties`
  - 2个文件有 `exactOptionalPropertyTypes` 问题
- **建议修复**: 
  1. 修复 ExecutionRecord 类型不匹配
  2. 完成 HarnessRun 类型的必需属性
  3. 移除 @ts-ignore 使用

#### 5. [源代码] [高严重] [ExecutionRecord 类型不匹配]
- **文件/路径**: `src/platform/stability/stable-evidence-bundle-support.ts:783` 等14个文件
- **问题描述**: 
  - 插入 execution record 时 TypeScript 拒绝类型
  - `ExecutionRecord` 接口要求新字段但 insert 对象缺失
  - 或 store 的 insert 方法接受不同类型
- **建议修复**: 
  1. 对齐 ExecutionRecord 接口和 store 类型
  2. 添加缺失字段到 insert 对象
  3. 或调整 store 的 insert 方法类型签名

#### 6. [源代码] [中严重] [30+处 as unknown as 双类型转换]
- **文件/路径**: `division-loader.ts`, `domain-baseline-catalog.ts`, `harness-sdk`
- **问题描述**: 
  - 完全绕过类型安全
  - 通常是类型架构问题的信号
- **建议修复**: 
  1. 修复上游类型问题
  2. 使用更具体的类型断言
  3. 重构类型层次结构

#### 7. [构建] [中严重] [rimraf  extraneous - 未在 package.json 声明]
- **文件/路径**: `node_modules/rimraf`
- **问题描述**: 
  - rimraf@6.1.3 存在于 node_modules 但未在 package.json 声明
  - 可能导致依赖问题
- **建议修复**: 
  1. 添加到 dependencies 或 devDependencies
  2. 或移除并使用其他清理方式

#### 8. [测试] [高严重] [178处直接 process.env 变更无抽象]
- **文件/路径**: `tests/unit/domains/registry/plugin-spi-registry.test.ts:640-687`
- **问题描述**: 
  - 直接读取/写入 process.env
  - 在 beforeEach/afterEach 中手动保存/恢复
  - 无 helper 抽象
- **建议修复**: 
  1. 创建 env helper 函数
  2. 统一保存和恢复模式
  3. 添加测试隔离验证

#### 9. [测试] [高严重] [21,998处 unlinkSync 调用无集中清理]
- **文件/路径**: 多个测试文件
- **问题描述**: 
  - 每个测试重新实现清理逻辑
  - 无集中清理 utility
  - 可能导致 EBUSY (Windows) 或并发访问问题
- **建议修复**: 
  1. 创建集中的测试清理 utility
  2. 使用 afterEach 确保清理
  3. 考虑使用临时目录

#### 10. [测试] [中严重] [无统一 mocking 框架]
- **文件/路径**: `tests/` 多个目录
- **问题描述**: 
  - 10,579 处 vi.fn, jest.mock, sinon 等调用
  - 无统一的 mocking 框架可见
  - 模式不一致
- **建议修复**: 
  1. 建立统一的 mock factory
  2. 标准化 mock 模式
  3. 添加 mocking 最佳实践文档

#### 11. [测试] [中严重] [Singleton 状态重置未标准化]
- **文件/路径**: `tests/unit/platform/five-plane-execution/dispatcher.test.ts`
- **问题描述**: 
  - 测试通过特定函数重置 singletons
  - 每个测试重复自己的 reset 逻辑
  - 无统一的 resetAllSingletons()
- **建议修复**: 
  1. 创建统一的 singleton reset 机制
  2. 在 afterEach 中自动调用
  3. 文档化 singleton 重置要求

#### 12. [测试] [低严重] [无分支覆盖率要求]
- **文件/路径**: `.c8rc.json`
- **问题描述**: 
  - `"100": false` - 无覆盖率要求
  - 只测量源代码，不测量测试文件
- **建议修复**: 
  1. 添加分支覆盖率阈值
  2. 在 CI 中强制执行覆盖率 gate
  3. 关注关键代码路径

#### 13. [网络] [高严重] [ScopedExternalAccessSandbox performHttpRequest 无超时]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/scoped-external-access-sandbox.ts:289`
- **问题描述**: 
  - fetch 调用无 timeout 参数
  - 挂起的请求可能永久阻塞
- **建议修复**: 
  1. 添加 AbortController timeout
  2. 使用标准的超时模式
  3. 记录超时配置

#### 14. [网络] [高严重] [OIDC 服务 fetch 调用无超时]
- **文件/路径**: `src/org-governance/sso-scim/oidc/oidc-service.ts:565,272`
- **问题描述**: 
  - OIDC token 交换无 retry logic
  - 无 timeout/abort signal
  - 认证流程可能挂起
- **建议修复**: 
  1. 添加 fetch timeout
  2. 添加 retry logic
  3. 使用标准的超时配置

#### 15. [网络] [高严重] [plugin-runtime-child 替换 globalThis.fetch 为 no-op]
- **文件/路径**: `src/domains/registry/plugin-runtime-child.ts:112`
- **问题描述**: 
  ```typescript
  globalThis.fetch = (async () => {}) as unknown as typeof globalThis.fetch;
  ```
  - 插件上下文中完全替换 fetch 为空函数
  - 插件的所有出站 HTTP 请求都会失败
  - 看起来是有意为之但可能造成问题
- **建议修复**: 
  1. 确认这是有意行为
  2. 文档化 fetch 替换行为
  3. 确保不会意外影响其他代码

#### 16. [网络] [中严重] [无显式 http.Agent 配置]
- **文件/路径**: 全局
- **问题描述**: 
  - 无显式的 http.Agent/https.Agent 配置
  - 依赖 Node.js 默认连接池
  - 无 maxSockets 或 maxConnections 配置
  - 高吞吐场景可能出问题
- **建议修复**: 
  1. 添加 http Agent 配置
  2. 文档化连接池大小
  3. 监控连接使用情况

#### 17. [序列化] [中严重] [合同版本硬编码为字符串字面量]
- **文件/路径**: `src/platform/contracts/inter-plane-contract-gateway.ts:184`
- **问题描述**: 
  - `schemaVersion: "v4.3"` 硬编码
  - 难以追踪合同版本演变
- **建议修复**: 
  1. 提取到类型化常量或 enum
  2. 集中管理合同版本
  3. 添加版本兼容性检查

#### 18. [序列化] [低严重] [Timestamp = string 丢失精度]
- **文件/路径**: `src/platform/contracts/types/domain/primitives.ts:10`
- **问题描述**: 
  - `type Timestamp = string` 无类型精度
  - ISO 8601 字符串无时区信息
- **建议修复**: 
  1. 使用 Date 对象或 branded string 类型
  2. 明确时区处理约定
  3. 文档化时间戳语义

#### 19. [源代码] [中严重] [29处非空断言 !.]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - 使用 `!.` 断言非空
  - 可能导致运行时错误
- **建议修复**: 
  1. 使用可选链 `?.` 或空值合并 `??`
  2. 添加适当的 null 检查
  3. 减少非空断言使用

#### 20. [API] [中严重] [API 路由 schema 版本硬编码]
- **文件/路径**: `src/platform/five-plane-interface/api/federation-routing-service.ts`
- **问题描述**: 
  - Zod schema 验证存在
  - 但 schema 版本未与 contract 版本同步
- **建议修复**: 
  1. 从 contract 版本派生 schema 版本
  2. 添加版本一致性检查
  3. 文档化版本关系

### 总结

本次补充Review（第十五轮 - 构建测试与类型系统Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. 无 TypeScript 增量编译缓存（每次完整重建9.6秒）
2. 76个 npm scripts 大部分重复构建
3. 168处 any 类型使用（绕过类型安全）
4. 38处 @ts-ignore 指令
5. ExecutionRecord 类型不匹配（14个文件）
6. 178处直接 process.env 变更无抽象
7. ScopedExternalAccessSandbox performHttpRequest 无超时
8. OIDC 服务 fetch 调用无超时
9. plugin-runtime-child 替换 globalThis.fetch 为 no-op

**中优先级**:
1. 30+处 as unknown as 双类型转换
2. rimraf extraneous 未声明
3. 21,998处 unlinkSync 无集中清理
4. 无统一 mocking 框架
5. Singleton 状态重置未标准化
6. 无显式 http.Agent 配置
7. 合同版本硬编码为字符串
8. Timestamp = string 丢失精度
9. 非空断言使用

**低优先级**:
1. 无分支覆盖率要求
2. API 路由 schema 版本未同步

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 72 | 87 | 39 | 198 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **109** | **182** | **85** | **376** |

### 优先修复建议

**立即处理 (本周内)**:
1. 添加 TypeScript 增量编译 (`incremental: true`)
2. 重构 npm scripts 避免重复构建
3. 减少 any 类型使用到 <50
4. 移除所有 @ts-ignore 指令
5. 修复 ExecutionRecord 类型不匹配
6. 添加 fetch 超时到 OIDC 和 sandbox

**短期内处理 (本月内)**:
1. 创建集中的测试清理 utility
2. 建立统一的 mocking 框架
3. 标准化 singleton 重置机制
4. 添加 http.Agent 连接池配置
5. 提取合同版本到类型化常量

**长期规划**:
1. 实现完整的类型安全（无 any）
2. 建立测试基础设施标准
3. 实现构建缓存优化
4. 完善网络层超时和重试
5. 建立覆盖率门禁标准

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十四轮 - 日志事件与工作流Review）

### 发现的新问题

#### 1. [可观测性] [高严重] [StructuredLogEntry 无 requestId 字段]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - StructuredLogEntry 接口无 requestId 字段
  - 只有 correlationId，它回退到 traceId
  - 无法独立追踪单个 HTTP 请求
- **建议修复**: 
  1. 在 StructuredLogEntry 添加 requestId 字段
  2. 在 HTTP 中间件中生成和注入 requestId
  3. 确保 requestId 流经所有日志调用

#### 2. [可观测性] [高严重] [StructuredLogger data 字段无自动清洗]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - 如果开发者传递 `{ password: "..." }` 给 logger.info，密码会被记录
  - 无字段级脱敏机制
  - 依赖开发者手动排除敏感字段
- **建议修复**: 
  1. 添加 OBSERVE_OUTPUT_BLACKLIST 类似的黑名单
  2. 实现 redact/mask/sanitize 工具函数
  3. 在日志传输前自动清洗敏感字段

#### 3. [可观测性] [中严重] [日志中敏感数据无 PII 处理]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - 无 PII 重写工具
  - 开发者必须手动避免记录敏感数据
  - 存在意外记录敏感信息的风险
- **建议修复**: 
  1. 添加 PII 检测和重写工具
  2. 在文档中明确记录敏感字段列表
  3. 添加测试验证敏感数据不被记录

#### 4. [可观测性] [中严重] [59处 console.* 直接调用]
- **文件/路径**: 多个源文件
- **问题描述**: 
  - 应该使用 StructuredLogger
  - console 调用可能不被日志聚合系统收集
  - 难以追踪和关联
- **建议修复**: 
  1. 替换所有 console.* 为 StructuredLogger
  2. 添加 ESLint 规则禁止 console.*
  3. 保留少量允许的 CLI 输出

#### 5. [可观测性] [中严重] [部分 logger.warn 调用缺少结构化数据]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:122`
- **问题描述**: 
  ```typescript
  logger.warn("WebSocket connection rejected: missing subprotocol token")
  ```
  - 只有消息，无 data 对象
  - 难以关联和查询
- **建议修复**: 
  1. 添加 `{ data: { ... } }` 对象
  2. 确保所有日志包含上下文数据
  3. 添加日志审查工具

#### 6. [源代码] [高严重] [Harness while(true) 循环无硬性迭代上限]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:1442`
- **问题描述**: 
  ```typescript
  while (true) {
    // ...
    // 依赖 budget gate 和 guardrail vibration 退出
    // 如果两者都失败，循环永久继续
  }
  ```
  - 依赖 budget gate 和 guardrail vibration 退出
  - 无硬性迭代次数限制
  - 可能导致无限循环
- **建议修复**: 
  1. 添加硬性 maxIterations 上限
  2. 在达到上限时强制退出
  3. 记录详细的迭代统计用于调试

#### 7. [源代码] [高严重] [Oapeflir while(true) 循环可能无限重规划]
- **文件/路径**: `src/platform/five-plane-orchestration/oapeflir/oapeflir-loop-service.ts:355`
- **问题描述**: 
  - 如果 loopReplanDecision.shouldReplan 永不变为 false
  - 重规划永远无法减少计划错误
  - 可能导致无限循环
- **建议修复**: 
  1. 添加重规划次数上限
  2. 在达到上限时强制接受当前结果
  3. 添加重规划质量衰减检测

#### 8. [源代码] [中严重] [Recovery 流程允许终态转换到 paused]
- **文件/路径**: `src/platform/five-plane-execution/runtime-state-machine.ts:105-108`
- **问题描述**: 
  - completed, failed, cancelled, aborted 可转换到 paused 进行恢复
  - 创建恢复路径可能重新进入执行
  - 需要验证此行为的正确性
- **建议修复**: 
  1. 审查 recovery 转换的合法性
  2. 确保不会导致状态不一致
  3. 添加转换前置条件验证

#### 9. [国际化] [高严重] [API 错误消息硬编码英文]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/api-error.ts`
- **问题描述**: 
  ```typescript
  return new ApiError(404, "api.task_not_found", "Task not found.");
  ```
  - 错误消息硬编码英文
  - 无后端 i18n 机制
  - 非英文客户端收到不本地化的错误
- **建议修复**: 
  1. 使用错误码而非硬编码消息
  2. 客户端根据错误码本地化
  3. 或在后端添加 i18n 支持

#### 10. [国际化] [中严重] [UI 硬编码字符串未翻译]
- **文件/路径**: `ui/packages/features/conversation/src/web/index.tsx`
- **问题描述**: 
  - "Build Plan", "Execute", "Messages" 等按钮标签硬编码
  - 不使用 i18n 系统
- **建议修复**: 
  1. 使用 `translateFeatureCopy` 替换硬编码
  2. 确保所有用户可见文本通过 i18n
  3. 添加硬编码字符串检测

#### 11. [国际化] [中严重] [Arabic 目录不完整]
- **文件/路径**: `ui/packages/shared/i18n/src/catalogs/ar-SA.ts`
- **问题描述**: 
  - ar-SA 只有约18条消息
  - en-US 和 zh-CN 有约117条
  - 缺失的 key 返回 key 本身
- **建议修复**: 
  1. 补充 Arabic 翻译
  2. 确保所有 key 都有翻译
  3. 添加翻译完整性测试

#### 12. [国际化] [低严重] [translateFeatureCopy 无回退默认值]
- **文件/路径**: `ui/packages/shared/i18n/`
- **问题描述**: 
  - 如果 feature ID 不在 catalog 中
  - 返回 undefined title/summary
  - 无合理回退
- **建议修复**: 
  1. 添加回退到 key 或默认字符串
  2. 或使用 feature ID 作为显示名
  3. 确保 UI 不显示 undefined

#### 13. [运维] [高严重] [健康检查无 readiness/liveness 区分]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts`
- **问题描述**: 
  - /healthz, /v1/healthz, /health 返回相同报告
  - 无 readiness probe（可处理流量？）
  - 无 liveness probe（系统存活？）
  - canAcceptTraffic() 未通过 HTTP 暴露
- **建议修复**: 
  1. 添加 /ready 和 /live 端点
  2. Readiness 检查依赖项就绪
  3. Liveness 检查进程健康

#### 14. [运维] [高严重] [shutdown 期间健康检查存在竞态]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts`
- **问题描述**: 
  - getHealthReportAsync() 异步检查 dbWritable
  - shutdown 期间数据库连接可能关闭
  - 健康检查仍在传入
- **建议修复**: 
  1. 在 shutdown 时返回 unhealthy
  2. 添加 shutdown 状态标志
  3. 停止接受新的健康检查请求

#### 15. [运维] [高严重] [LeaderElectionService 未与 GracefulShutdown 集成]
- **文件/路径**: `src/platform/five-plane-execution/ha/leader-election-service.ts`
- **问题描述**: 
  - LeaderElectionService.stop() 处理优雅退位
  - 但未在全局 graceful shutdown 中注册
  - 需显式注册为 shutdown handler
- **建议修复**: 
  1. 将 LeaderElectionService 添加到 shutdown handlers
  2. 确保领导者在 shutdown 时让位
  3. 验证关闭顺序正确

#### 16. [运维] [中严重] [addHandler() 无显式排序约定]
- **文件/路径**: `src/platform/five-plane-execution/startup/graceful-shutdown.ts`
- **问题描述**: 
  - 虽然 handlers 逆序执行
  - 但无显式接口声明 handler 依赖关系
  - 关键 handlers（如关闭数据库连接）应最后运行
- **建议修复**: 
  1. 添加优先级或阶段参数
  2. 文档化关键 handler 顺序要求
  3. 添加顺序验证测试

#### 17. [运维] [中严重] [unref'd timers 可能不触发]
- **文件/路径**: `src/platform/five-plane-execution/startup/process-error-handlers.ts:106`
- **问题描述**: 
  - `setTimeout(...).unref()` 不保持事件循环活跃
  - 如果主线程空闲，强制退出可能不触发
- **建议修复**: 
  1. 移除 .unref() 除非确实需要
  2. 或确保有其他方式保持事件循环
  3. 添加超时触发验证测试

#### 18. [运维] [中严重] [StartupConsistencyChecker 阻塞流量但不暴露状态]
- **文件/路径**: `src/platform/five-plane-startup-plan.ts`
- **问题描述**: 
  - canAcceptTraffic() 在 P0 问题存在时返回 false
  - 但 _trafficBlocked 是内部状态
  - 负载均衡器访问 /healthz 仍会得到 "ok"
- **建议修复**: 
  1. 在健康报告中暴露 trafficBlocked 状态
  2. 或添加单独的健康端点指示就绪状态
  3. 确保负载均衡器在阻塞时不路由流量

#### 19. [事件系统] [中严重] [TypedEventBus vs EventEmitter 混用]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - 项目同时使用 TypedEventBus 和原生 EventEmitter
  - TypedEventBus 应该是标准
  - 混用可能导致类型安全问题
- **建议修复**: 
  1. 创建迁移计划统一使用 TypedEventBus
  2. 添加 ESLint 规则禁止原生 EventEmitter
  3. 在 CI 中检测混用情况

#### 20. [源代码] [低严重] [无事件排序保证]
- **文件/路径**: `src/platform/five-plane-state-evidence/events/`
- **问题描述**: 
  - 无明确的事件排序保证文档
  - 事件处理可能乱序
  - 依赖方需要处理乱序
- **建议修复**: 
  1. 文档化事件排序语义
  2. 如果需要排序，实现序列号机制
  3. 在事件处理中考虑乱序情况

### 总结

本次补充Review（第十四轮 - 日志事件与工作流Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. StructuredLogEntry 无 requestId 字段
2. StructuredLogger data 字段无自动清洗（敏感数据风险）
3. Harness while(true) 循环无硬性迭代上限
4. Oapeflir while(true) 循环可能无限重规划
5. API 错误消息硬编码英文（无 i18n）
6. 健康检查无 readiness/liveness 区分
7. shutdown 期间健康检查存在竞态
8. LeaderElectionService 未与 GracefulShutdown 集成

**中优先级**:
1. 日志中敏感数据无 PII 处理
2. 59处 console.* 直接调用
3. 部分 logger.warn 调用缺少结构化数据
4. Recovery 流程允许终态转换到 paused
5. UI 硬编码字符串未翻译
6. Arabic 目录不完整
7. addHandler() 无显式排序约定
8. unref'd timers 可能不触发
9. StartupConsistencyChecker 阻塞流量但不暴露状态
10. TypedEventBus vs EventEmitter 混用

**低优先级**:
1. translateFeatureCopy 无回退默认值
2. 无事件排序保证

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 68 | 80 | 37 | 185 |
| 测试 | 8 | 14 | 5 | 27 |
| 配置 | 10 | 38 | 24 | 72 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **103** | **171** | **82** | **356** |

### 优先修复建议

**立即处理 (本周内)**:
1. 在 StructuredLogEntry 添加 requestId 字段
2. 实现日志敏感字段自动清洗
3. 为工作流循环添加硬性迭代上限
4. 添加 readiness/liveness 健康检查端点
5. 将 LeaderElectionService 集成到 GracefulShutdown
6. 实现 API 错误消息 i18n 支持

**短期内处理 (本月内)**:
1. 替换所有 console.* 为 StructuredLogger
2. 补充 Arabic i18n 目录
3. 修复 UI 硬编码字符串
4. 统一 TypedEventBus 替代 EventEmitter
5. 解决 shutdown 竞态条件
6. 添加事件排序保证文档

**长期规划**:
1. 建立完整的 PII 处理框架
2. 实现日志完整性验证
3. 建立工作流安全循环检测
4. 完善健康检查和就绪探测
5. 建立国际化完整测试

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十五轮 - 构建测试与类型系统Review）

### 发现的新问题

#### 1. [构建] [高严重] [无 TypeScript 增量编译缓存]
- **文件/路径**: `tsconfig.json`
- **问题描述**: 
  - 无 `incremental: true` 配置
  - 无 `tsbuildinfo` 文件
  - 每次构建是完整重建（9.6秒）
  - TypeScript 无法使用增量编译
- **建议修复**: 
  1. 在 tsconfig.json 添加 `"incremental": true`
  2. 添加 `"tsbuildinfo": ".tsbuildinfo"` 到 .gitignore
  3. 实现项目引用以支持并行构建

#### 2. [构建] [高严重] [76个 npm scripts 大部分重复构建]
- **文件/路径**: `package.json`
- **问题描述**: 
  - 40+ CLI scripts 都运行 `npm run build && node ...`
  - 每个脚本前都执行完整重建
  - 无法直接运行预构建的 CLI
  - 20+ stable:* 脚本遵循相同模式
- **建议修复**: 
  1. 创建 `build:cli` 脚本构建一次
  2. 让 CLI 脚本使用预构建的 dist 文件
  3. 添加 npm run dev 或类似的无需重建的执行方式

#### 3. [源代码] [高严重] [168处 any 类型使用]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - `stable-runner-factory.ts`: `opts: any` -> `any`
  - `billing-service-async.ts`: `input: any` 方法签名
  - 重试/超时工具使用 `(...args: any[]) => Promise<any>`
  - 绕过类型安全
- **建议修复**: 
  1. 使用泛型替代 any
  2. 替换为具体的输入类型
  3. 添加 ESLint 规则禁止 any

#### 4. [源代码] [高严重] [38处 @ts-ignore 指令]
- **文件/路径**: 多个文件
- **问题描述**: 
  - 14个 stability 相关文件有 `@ts-ignore ExecutionRecord type mismatch`
  - 5个 harness-sdk 文件有 `Partial<HarnessRun> doesn't have all required properties`
  - 2个文件有 `exactOptionalPropertyTypes` 问题
- **建议修复**: 
  1. 修复 ExecutionRecord 类型不匹配
  2. 完成 HarnessRun 类型的必需属性
  3. 移除 @ts-ignore 使用

#### 5. [源代码] [高严重] [ExecutionRecord 类型不匹配]
- **文件/路径**: `src/platform/stability/stable-evidence-bundle-support.ts:783` 等14个文件
- **问题描述**: 
  - 插入 execution record 时 TypeScript 拒绝类型
  - `ExecutionRecord` 接口要求新字段但 insert 对象缺失
  - 或 store 的 insert 方法接受不同类型
- **建议修复**: 
  1. 对齐 ExecutionRecord 接口和 store 类型
  2. 添加缺失字段到 insert 对象
  3. 或调整 store 的 insert 方法类型签名

#### 6. [源代码] [中严重] [30+处 as unknown as 双类型转换]
- **文件/路径**: `division-loader.ts`, `domain-baseline-catalog.ts`, `harness-sdk`
- **问题描述**: 
  - 完全绕过类型安全
  - 通常是类型架构问题的信号
- **建议修复**: 
  1. 修复上游类型问题
  2. 使用更具体的类型断言
  3. 重构类型层次结构

#### 7. [构建] [中严重] [rimraf  extraneous - 未在 package.json 声明]
- **文件/路径**: `node_modules/rimraf`
- **问题描述**: 
  - rimraf@6.1.3 存在于 node_modules 但未在 package.json 声明
  - 可能导致依赖问题
- **建议修复**: 
  1. 添加到 dependencies 或 devDependencies
  2. 或移除并使用其他清理方式

#### 8. [测试] [高严重] [178处直接 process.env 变更无抽象]
- **文件/路径**: `tests/unit/domains/registry/plugin-spi-registry.test.ts:640-687`
- **问题描述**: 
  - 直接读取/写入 process.env
  - 在 beforeEach/afterEach 中手动保存/恢复
  - 无 helper 抽象
- **建议修复**: 
  1. 创建 env helper 函数
  2. 统一保存和恢复模式
  3. 添加测试隔离验证

#### 9. [测试] [高严重] [21,998处 unlinkSync 调用无集中清理]
- **文件/路径**: 多个测试文件
- **问题描述**: 
  - 每个测试重新实现清理逻辑
  - 无集中清理 utility
  - 可能导致 EBUSY (Windows) 或并发访问问题
- **建议修复**: 
  1. 创建集中的测试清理 utility
  2. 使用 afterEach 确保清理
  3. 考虑使用临时目录

#### 10. [测试] [中严重] [无统一 mocking 框架]
- **文件/路径**: `tests/` 多个目录
- **问题描述**: 
  - 10,579 处 vi.fn, jest.mock, sinon 等调用
  - 无统一的 mocking 框架可见
  - 模式不一致
- **建议修复**: 
  1. 建立统一的 mock factory
  2. 标准化 mock 模式
  3. 添加 mocking 最佳实践文档

#### 11. [测试] [中严重] [Singleton 状态重置未标准化]
- **文件/路径**: `tests/unit/platform/five-plane-execution/dispatcher.test.ts`
- **问题描述**: 
  - 测试通过特定函数重置 singletons
  - 每个测试重复自己的 reset 逻辑
  - 无统一的 resetAllSingletons()
- **建议修复**: 
  1. 创建统一的 singleton reset 机制
  2. 在 afterEach 中自动调用
  3. 文档化 singleton 重置要求

#### 12. [测试] [低严重] [无分支覆盖率要求]
- **文件/路径**: `.c8rc.json`
- **问题描述**: 
  - `"100": false` - 无覆盖率要求
  - 只测量源代码，不测量测试文件
- **建议修复**: 
  1. 添加分支覆盖率阈值
  2. 在 CI 中强制执行覆盖率 gate
  3. 关注关键代码路径

#### 13. [网络] [高严重] [ScopedExternalAccessSandbox performHttpRequest 无超时]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/scoped-external-access-sandbox.ts:289`
- **问题描述**: 
  - fetch 调用无 timeout 参数
  - 挂起的请求可能永久阻塞
- **建议修复**: 
  1. 添加 AbortController timeout
  2. 使用标准的超时模式
  3. 记录超时配置

#### 14. [网络] [高严重] [OIDC 服务 fetch 调用无超时]
- **文件/路径**: `src/org-governance/sso-scim/oidc/oidc-service.ts:565,272`
- **问题描述**: 
  - OIDC token 交换无 retry logic
  - 无 timeout/abort signal
  - 认证流程可能挂起
- **建议修复**: 
  1. 添加 fetch timeout
  2. 添加 retry logic
  3. 使用标准的超时配置

#### 15. [网络] [高严重] [plugin-runtime-child 替换 globalThis.fetch 为 no-op]
- **文件/路径**: `src/domains/registry/plugin-runtime-child.ts:112`
- **问题描述**: 
  ```typescript
  globalThis.fetch = (async () => {}) as unknown as typeof globalThis.fetch;
  ```
  - 插件上下文中完全替换 fetch 为空函数
  - 插件的所有出站 HTTP 请求都会失败
  - 看起来是有意为之但可能造成问题
- **建议修复**: 
  1. 确认这是有意行为
  2. 文档化 fetch 替换行为
  3. 确保不会意外影响其他代码

#### 16. [网络] [中严重] [无显式 http.Agent 配置]
- **文件/路径**: 全局
- **问题描述**: 
  - 无显式的 http.Agent/https.Agent 配置
  - 依赖 Node.js 默认连接池
  - 无 maxSockets 或 maxConnections 配置
  - 高吞吐场景可能出问题
- **建议修复**: 
  1. 添加 http Agent 配置
  2. 文档化连接池大小
  3. 监控连接使用情况

#### 17. [序列化] [中严重] [合同版本硬编码为字符串字面量]
- **文件/路径**: `src/platform/contracts/inter-plane-contract-gateway.ts:184`
- **问题描述**: 
  - `schemaVersion: "v4.3"` 硬编码
  - 难以追踪合同版本演变
- **建议修复**: 
  1. 提取到类型化常量或 enum
  2. 集中管理合同版本
  3. 添加版本兼容性检查

#### 18. [序列化] [低严重] [Timestamp = string 丢失精度]
- **文件/路径**: `src/platform/contracts/types/domain/primitives.ts:10`
- **问题描述**: 
  - `type Timestamp = string` 无类型精度
  - ISO 8601 字符串无时区信息
- **建议修复**: 
  1. 使用 Date 对象或 branded string 类型
  2. 明确时区处理约定
  3. 文档化时间戳语义

#### 19. [源代码] [中严重] [29处非空断言 !.]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - 使用 `!.` 断言非空
  - 可能导致运行时错误
- **建议修复**: 
  1. 使用可选链 `?.` 或空值合并 `??`
  2. 添加适当的 null 检查
  3. 减少非空断言使用

#### 20. [API] [中严重] [API 路由 schema 版本硬编码]
- **文件/路径**: `src/platform/five-plane-interface/api/federation-routing-service.ts`
- **问题描述**: 
  - Zod schema 验证存在
  - 但 schema 版本未与 contract 版本同步
- **建议修复**: 
  1. 从 contract 版本派生 schema 版本
  2. 添加版本一致性检查
  3. 文档化版本关系

### 总结

本次补充Review（第十五轮 - 构建测试与类型系统Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. 无 TypeScript 增量编译缓存（每次完整重建9.6秒）
2. 76个 npm scripts 大部分重复构建
3. 168处 any 类型使用（绕过类型安全）
4. 38处 @ts-ignore 指令
5. ExecutionRecord 类型不匹配（14个文件）
6. 178处直接 process.env 变更无抽象
7. ScopedExternalAccessSandbox performHttpRequest 无超时
8. OIDC 服务 fetch 调用无超时
9. plugin-runtime-child 替换 globalThis.fetch 为 no-op

**中优先级**:
1. 30+处 as unknown as 双类型转换
2. rimraf extraneous 未声明
3. 21,998处 unlinkSync 无集中清理
4. 无统一 mocking 框架
5. Singleton 状态重置未标准化
6. 无显式 http.Agent 配置
7. 合同版本硬编码为字符串
8. Timestamp = string 丢失精度
9. 非空断言使用

**低优先级**:
1. 无分支覆盖率要求
2. API 路由 schema 版本未同步

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 72 | 87 | 39 | 198 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **109** | **182** | **85** | **376** |

### 优先修复建议

**立即处理 (本周内)**:
1. 添加 TypeScript 增量编译 (`incremental: true`)
2. 重构 npm scripts 避免重复构建
3. 减少 any 类型使用到 <50
4. 移除所有 @ts-ignore 指令
5. 修复 ExecutionRecord 类型不匹配
6. 添加 fetch 超时到 OIDC 和 sandbox

**短期内处理 (本月内)**:
1. 创建集中的测试清理 utility
2. 建立统一的 mocking 框架
3. 标准化 singleton 重置机制
4. 添加 http.Agent 连接池配置
5. 提取合同版本到类型化常量

**长期规划**:
1. 实现完整的类型安全（无 any）
2. 建立测试基础设施标准
3. 实现构建缓存优化
4. 完善网络层超时和重试
5. 建立覆盖率门禁标准

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十三轮 - 缓存会话与实时通信Review）

### 发现的新问题

#### 1. [安全] [严重] [CORS allowedMethods 缺少 PUT/PATCH/DELETE]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/response-hardening.ts:12-14`
- **问题描述**: 
  ```typescript
  export const DEFAULT_CORS_CONFIG: CorsConfig = {
    allowedMethods: ["GET", "POST", "OPTIONS"],  // 缺少 PUT, PATCH, DELETE
  ```
  - 浏览器 CORS preflight 会拒绝 PUT/PATCH/DELETE 请求
  - 但路由中有 PATCH /v1/tasks/:id, DELETE endpoints
- **建议修复**: 
  1. 添加 PUT, PATCH, DELETE 到 allowedMethods
  2. 确保 CORS 配置与应用路由方法一致
  3. 添加测试验证所有 HTTP 方法的 CORS preflight

#### 2. [安全] [严重] [Mission Routes 错误响应包装不当]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/mission-routes.ts:101,118-124`
- **问题描述**: 
  ```typescript
  return buildJsonResponse(ctx.requestId, 404, { error: { code: "MISSION_NOT_FOUND" } });
  ```
  - 使用 buildJsonResponse（成功包装）返回错误
  - 错误被包装在成功信封 `{requestId, data: {error: {...}}}` 中
  - 违反 REST 规范
- **建议修复**: 
  1. 使用 buildJsonErrorResponse 处理错误
  2. 确保所有错误路径使用正确的错误信封
  3. 添加 API 一致性测试

#### 3. [源代码] [高严重] [WebSocketBridge 无最大连接数限制]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:103-107`
- **问题描述**: 
  ```typescript
  this.wss = new WebSocketServer({
    server,
    path: WS_PATH,
    maxPayload: options.maxPayloadBytes ?? DEFAULT_MAX_PAYLOAD_BYTES,
    // 无 maxConnections 限制！
  });
  ```
  - 与 dashboard-websocket-server.ts 的 maxClients: 1000 不同
  - 恶意客户端可耗尽服务器资源
- **建议修复**: 
  1. 添加 maxConnections 限制
  2. 实现连接限制拒绝策略
  3. 添加连接计数指标

#### 4. [源代码] [高严重] [pendingAcks 在断开连接时未清理]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:268-282`
- **问题描述**: 
  - handleDisconnection 清理 subscribedTasks, taskSubscribers, slowConsumers
  - 但 pendingAcks Map 未清理
  - 断开的客户端未确认的消息永久留在内存
- **建议修复**: 
  1. 在 handleDisconnection 中删除所有 pendingAcks 条目
  2. 考虑添加超时机制自动清理未确认消息
  3. 记录清理操作的审计日志

#### 5. [安全] [高严重] [硬编码回退密钥]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/audit-event-integrity.ts:41`
- **问题描述**: 
  ```typescript
  const AUDIT_INTEGRITY_HMAC_KEY = process.env["AA_AUDIT_INTEGRITY_HMAC_KEY"] ?? "audit-integrity-secret-key-32-bytes!";
  ```
  - 占位符密钥用作回退
  - 如果环境变量未设置，使用不安全密钥
- **建议修复**: 
  1. 移除回退值，要求环境变量必须设置
  2. 在启动时验证密钥存在且足够强
  3. 如果密钥缺失导致启动失败

#### 6. [配置] [高严重] [config/runtime/test.json 缺少超时配置]
- **文件/路径**: `config/runtime/test.json`
- **问题描述**: 
  - 只有3个字段，缺少 maxConcurrentTasks, defaultTaskTimeoutMs, defaultStepTimeoutMs
  - 回退到默认值可能不适合测试环境
- **建议修复**: 
  1. 添加完整的 timeout/tuning 配置
  2. 确保测试环境有合理的默认值
  3. 文档化必需的配置字段

#### 7. [配置] [高严重] [prod.json 限制过严 - maxConcurrentTasks=1]
- **文件/路径**: `config/runtime/prod.json`
- **问题描述**: 
  - maxConcurrentTasks: 1（vs dev:8, staging:4, pre-prod:6）
  - defaultTaskTimeoutMs: 120000（vs dev:600000）
  - defaultStepTimeoutMs: 60000（vs dev:180000）
  - 生产环境严重受限，可能影响吞吐量
- **建议修复**: 
  1. 评估并调整生产限制
  2. 与业务需求匹配
  3. 添加生产容量测试

#### 8. [源代码] [高严重] [多个服务存在无界 Map 无驱逐策略]
- **文件/路径**: 多个域服务
- **问题描述**: 
  - `domain-knowledge-schema-service.ts:73-75`: schemas, sourceContent, sourceTimestamps 无限制
  - `domain-eval-framework-service.ts:91-95`: frameworks, qualityAxes 等无限制
  - `domain-recipe-service.ts:67-68`: recipes, versions 无限制
  - `domain-risk-profile-service.ts:51`: profiles 无限制
  - Session Maps (session-management.ts:83-89) 无自动清理
- **建议修复**: 
  1. 为所有 Map 实现 LRU 或 TTL 驱逐策略
  2. 添加后台清理任务
  3. 添加大小监控和告警

#### 9. [源代码] [中严重] [taskEventHistory Map 永不清理]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:91,548-555`
- **问题描述**: 
  - taskEventHistory 限制单任务历史为 200 条
  - 但 Map 本身从不清理
  - 取消订阅的任务历史永久保留
- **建议修复**: 
  1. 实现后台清理无订阅者任务的历史
  2. 添加任务订阅者监控
  3. 考虑使用 WeakMap 替代

#### 10. [配置] [中严重] [安全配置 drift - remoteWorkerRegistration 缺失]
- **文件/路径**: `config/security/dev.json`, `staging.json`, `pre-prod.json`
- **问题描述**: 
  - default.json 有完整的 remoteWorkerRegistration 配置
  - dev/staging/pre-prod 只有 approvalMode
  - prod 有 approvalMode 但无 remoteWorkerRegistration
  - 安全配置不一致
- **建议修复**: 
  1. 统一所有环境的 remoteWorkerRegistration
  2. 添加安全配置验证
  3. 确保最低安全基线

#### 11. [API] [中严重] [OpenAPI 端点响应格式不一致]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/utils.ts:207-215`
- **问题描述**: 
  - `/v1/openapi.json` 使用 buildJsonDocumentResponse（无包装）
  - 其他端点使用 buildJsonResponse（{requestId, data} 包装）
  - 客户端体验不一致
- **建议修复**: 
  1. 统一响应信封格式
  2. 文档化响应格式规范
  3. 添加响应格式验证测试

#### 12. [源代码] [中严重] [Redis 队列缺少 idempotency 索引支持]
- **文件/路径**: `src/platform/five-plane-execution/queue/redis-queue-adapter.ts`
- **问题描述**: 
  - SQLite 有部分唯一索引支持 idempotency
  - Redis 实现使用哈希索引但实现不完整
  - 可能导致重复消息
- **建议修复**: 
  1. 完善 Redis idempotency 实现
  2. 添加唯一索引验证
  3. 确保与 SQLite 行为一致

#### 13. [源代码] [中严重] [请求去重中间件使用内存存储]
- **文件/路径**: `src/platform/five-plane-interface/api/middleware/request-deduplication.ts`
- **问题描述**: 
  - 纯内存滑动窗口（Map<DeduplicationKey, DeduplicationEntry[]>）
  - 无外部存储
  - 多实例部署不共享
  - 重启后去重状态丢失
- **建议修复**: 
  1. 使用 Redis 替代内存存储
  2. 支持分布式去重
  3. 持久化去重状态

#### 14. [源代码] [中严重] [Cache 无 stampede 保护]
- **文件/路径**: 多个 cache 实现
- **问题描述**: 
  - MemoryCacheStore, ExperienceCacheService 无锁
  - 缓存未命中时可发生 thundering herd
  - 高并发下可能导致数据库过载
- **建议修复**: 
  1. 实现 single-flight 模式
  2. 添加请求排队机制
  3. 使用分布式锁保护缓存更新

#### 15. [源代码] [中严重] [EvidenceService eviction 仅在插入时触发]
- **文件/路径**: `src/platform/five-plane-state-evidence/memory/evidence-service.ts:202`
- **问题描述**: 
  - 清理仅在 record() 调用时触发
  - 空闲时无后台清理
  - 可能导致内存持续增长
- **建议修复**: 
  1. 添加定期后台清理任务
  2. 使用独立的清理线程
  3. 添加内存使用监控

#### 16. [源代码] [低严重] [无连接指标暴露]
- **文件/路径**: WebSocket 服务器
- **问题描述**: 
  - getClientCount() 存在但未通过 HTTP 端点暴露
  - 无 pendingAcks 队列深度指标
  - 无连接建立告警
- **建议修复**: 
  1. 通过 metrics 端点暴露连接指标
  2. 添加 pendingAcks 队列监控
  3. 添加连接数异常告警

#### 17. [源代码] [低严重] [无空闲客户端超时]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts`
- **问题描述**: 
  - 仅在心跳 Sweep 时检查 isAlive
  - 无 per-client 独立于心跳的空闲超时
  - 认证后从不发送消息的客户端只能通过心跳失败检测
- **建议修复**: 
  1. 添加 per-client idle timeout
  2. 独立于心跳间隔
  3. 配置可调整

#### 18. [源代码] [低严重] [订阅限制是 per-client 而非全局]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:244`
- **问题描述**: 
  - MAX_SUBSCRIPTIONS_PER_CLIENT = 100
  - 恶意客户端可订阅 100 个任务
  - 无全局任务订阅者限制
- **建议修复**: 
  1. 添加全局任务订阅者限制
  2. 实现 per-task 订阅者上限
  3. 添加反滥用检测

#### 19. [配置] [低严重] [config/runtime 无版本验证机制]
- **文件/路径**: `config/runtime/*.json`
- **问题描述**: 
  - 配置有 "version": "v4.3"
  - 但无 schema 版本验证
  - 加载时可能接受不兼容配置
- **建议修复**: 
  1. 添加 JSON Schema 验证
  2. 实现版本兼容性检查
  3. 启动时验证配置完整性

#### 20. [源代码] [低严重] [ImprovementCandidateRegistry splice 是 O(n)]
- **文件/路径**: `src/platform/five-plane-orchestration/improve/improvement-candidate-registry.ts:243`
- **问题描述**: 
  - accessOrder 数组的 splice 操作是 O(n)
  - 高频访问时可能性能问题
- **建议修复**: 
  1. 使用 LinkedList 替代数组
  2. 或使用 Map 维护访问顺序
  3. 性能测试验证

### 总结

本次补充Review（第十三轮 - 缓存会话与实时通信Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. CORS allowedMethods 缺少 PUT/PATCH/DELETE（浏览器请求会失败）
2. Mission Routes 错误响应包装不当
3. WebSocketBridge 无最大连接数限制（DoS风险）
4. pendingAcks 断开连接时未清理（内存泄漏）
5. 硬编码回退密钥（安全漏洞）
6. config/runtime/test.json 缺少超时配置
7. prod.json maxConcurrentTasks=1 限制过严
8. 多个服务存在无界 Map（内存泄漏风险）

**中优先级**:
1. taskEventHistory Map 永不清理
2. 安全配置 drift（remoteWorkerRegistration 缺失）
3. OpenAPI 端点响应格式不一致
4. Redis 队列 idempotency 实现不完整
5. 请求去重中间件使用内存存储（多实例不共享）
6. Cache 无 stampede 保护
7. EvidenceService eviction 仅在插入时触发

**低优先级**:
1. 无连接指标暴露
2. 无空闲客户端超时
3. 订阅限制是 per-client 而非全局
4. config/runtime 无版本验证机制
5. ImprovementCandidateRegistry splice O(n)

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 64 | 74 | 35 | 173 |
| 测试 | 8 | 14 | 5 | 27 |
| 配置 | 10 | 38 | 24 | 72 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 7 | 3 | 12 |
| UI | 0 | 3 | 3 | 6 |
| 部署 | 1 | 10 | 6 | 17 |
| **合计** | **99** | **161** | **79** | **339** |

### 优先修复建议

**立即处理 (本周内)**:
1. 修复 CORS allowedMethods 添加 PUT/PATCH/DELETE
2. 修复 Mission Routes 错误响应格式
3. 为 WebSocketBridge 添加最大连接数限制
4. 清理 pendingAcks 和 taskEventHistory
5. 移除硬编码回退密钥
6. 修复 config/runtime/test.json 配置
7. 评估并调整 prod 并发限制

**短期内处理 (本月内)**:
1. 统一安全配置（remoteWorkerRegistration）
2. 实现无界 Map 的 LRU/TTL 驱逐
3. 统一 API 响应信封格式
4. 完善 Redis idempotency 实现
5. 添加分布式去重中间件
6. 实现 cache stampede 保护

**长期规划**:
1. 建立完整的连接管理和监控
2. 实现后台清理任务框架
3. 添加配置 schema 验证
4. 优化高频繁操作的数据结构
5. 建立内存使用基线和告警

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十五轮 - 构建测试与类型系统Review）

### 发现的新问题

#### 1. [构建] [高严重] [无 TypeScript 增量编译缓存]
- **文件/路径**: `tsconfig.json`
- **问题描述**: 
  - 无 `incremental: true` 配置
  - 无 `tsbuildinfo` 文件
  - 每次构建是完整重建（9.6秒）
  - TypeScript 无法使用增量编译
- **建议修复**: 
  1. 在 tsconfig.json 添加 `"incremental": true`
  2. 添加 `"tsbuildinfo": ".tsbuildinfo"` 到 .gitignore
  3. 实现项目引用以支持并行构建

#### 2. [构建] [高严重] [76个 npm scripts 大部分重复构建]
- **文件/路径**: `package.json`
- **问题描述**: 
  - 40+ CLI scripts 都运行 `npm run build && node ...`
  - 每个脚本前都执行完整重建
  - 无法直接运行预构建的 CLI
  - 20+ stable:* 脚本遵循相同模式
- **建议修复**: 
  1. 创建 `build:cli` 脚本构建一次
  2. 让 CLI 脚本使用预构建的 dist 文件
  3. 添加 npm run dev 或类似的无需重建的执行方式

#### 3. [源代码] [高严重] [168处 any 类型使用]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - `stable-runner-factory.ts`: `opts: any` -> `any`
  - `billing-service-async.ts`: `input: any` 方法签名
  - 重试/超时工具使用 `(...args: any[]) => Promise<any>`
  - 绕过类型安全
- **建议修复**: 
  1. 使用泛型替代 any
  2. 替换为具体的输入类型
  3. 添加 ESLint 规则禁止 any

#### 4. [源代码] [高严重] [38处 @ts-ignore 指令]
- **文件/路径**: 多个文件
- **问题描述**: 
  - 14个 stability 相关文件有 `@ts-ignore ExecutionRecord type mismatch`
  - 5个 harness-sdk 文件有 `Partial<HarnessRun> doesn't have all required properties`
  - 2个文件有 `exactOptionalPropertyTypes` 问题
- **建议修复**: 
  1. 修复 ExecutionRecord 类型不匹配
  2. 完成 HarnessRun 类型的必需属性
  3. 移除 @ts-ignore 使用

#### 5. [源代码] [高严重] [ExecutionRecord 类型不匹配]
- **文件/路径**: `src/platform/stability/stable-evidence-bundle-support.ts:783` 等14个文件
- **问题描述**: 
  - 插入 execution record 时 TypeScript 拒绝类型
  - `ExecutionRecord` 接口要求新字段但 insert 对象缺失
  - 或 store 的 insert 方法接受不同类型
- **建议修复**: 
  1. 对齐 ExecutionRecord 接口和 store 类型
  2. 添加缺失字段到 insert 对象
  3. 或调整 store 的 insert 方法类型签名

#### 6. [源代码] [中严重] [30+处 as unknown as 双类型转换]
- **文件/路径**: `division-loader.ts`, `domain-baseline-catalog.ts`, `harness-sdk`
- **问题描述**: 
  - 完全绕过类型安全
  - 通常是类型架构问题的信号
- **建议修复**: 
  1. 修复上游类型问题
  2. 使用更具体的类型断言
  3. 重构类型层次结构

#### 7. [构建] [中严重] [rimraf  extraneous - 未在 package.json 声明]
- **文件/路径**: `node_modules/rimraf`
- **问题描述**: 
  - rimraf@6.1.3 存在于 node_modules 但未在 package.json 声明
  - 可能导致依赖问题
- **建议修复**: 
  1. 添加到 dependencies 或 devDependencies
  2. 或移除并使用其他清理方式

#### 8. [测试] [高严重] [178处直接 process.env 变更无抽象]
- **文件/路径**: `tests/unit/domains/registry/plugin-spi-registry.test.ts:640-687`
- **问题描述**: 
  - 直接读取/写入 process.env
  - 在 beforeEach/afterEach 中手动保存/恢复
  - 无 helper 抽象
- **建议修复**: 
  1. 创建 env helper 函数
  2. 统一保存和恢复模式
  3. 添加测试隔离验证

#### 9. [测试] [高严重] [21,998处 unlinkSync 调用无集中清理]
- **文件/路径**: 多个测试文件
- **问题描述**: 
  - 每个测试重新实现清理逻辑
  - 无集中清理 utility
  - 可能导致 EBUSY (Windows) 或并发访问问题
- **建议修复**: 
  1. 创建集中的测试清理 utility
  2. 使用 afterEach 确保清理
  3. 考虑使用临时目录

#### 10. [测试] [中严重] [无统一 mocking 框架]
- **文件/路径**: `tests/` 多个目录
- **问题描述**: 
  - 10,579 处 vi.fn, jest.mock, sinon 等调用
  - 无统一的 mocking 框架可见
  - 模式不一致
- **建议修复**: 
  1. 建立统一的 mock factory
  2. 标准化 mock 模式
  3. 添加 mocking 最佳实践文档

#### 11. [测试] [中严重] [Singleton 状态重置未标准化]
- **文件/路径**: `tests/unit/platform/five-plane-execution/dispatcher.test.ts`
- **问题描述**: 
  - 测试通过特定函数重置 singletons
  - 每个测试重复自己的 reset 逻辑
  - 无统一的 resetAllSingletons()
- **建议修复**: 
  1. 创建统一的 singleton reset 机制
  2. 在 afterEach 中自动调用
  3. 文档化 singleton 重置要求

#### 12. [测试] [低严重] [无分支覆盖率要求]
- **文件/路径**: `.c8rc.json`
- **问题描述**: 
  - `"100": false` - 无覆盖率要求
  - 只测量源代码，不测量测试文件
- **建议修复**: 
  1. 添加分支覆盖率阈值
  2. 在 CI 中强制执行覆盖率 gate
  3. 关注关键代码路径

#### 13. [网络] [高严重] [ScopedExternalAccessSandbox performHttpRequest 无超时]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/scoped-external-access-sandbox.ts:289`
- **问题描述**: 
  - fetch 调用无 timeout 参数
  - 挂起的请求可能永久阻塞
- **建议修复**: 
  1. 添加 AbortController timeout
  2. 使用标准的超时模式
  3. 记录超时配置

#### 14. [网络] [高严重] [OIDC 服务 fetch 调用无超时]
- **文件/路径**: `src/org-governance/sso-scim/oidc/oidc-service.ts:565,272`
- **问题描述**: 
  - OIDC token 交换无 retry logic
  - 无 timeout/abort signal
  - 认证流程可能挂起
- **建议修复**: 
  1. 添加 fetch timeout
  2. 添加 retry logic
  3. 使用标准的超时配置

#### 15. [网络] [高严重] [plugin-runtime-child 替换 globalThis.fetch 为 no-op]
- **文件/路径**: `src/domains/registry/plugin-runtime-child.ts:112`
- **问题描述**: 
  ```typescript
  globalThis.fetch = (async () => {}) as unknown as typeof globalThis.fetch;
  ```
  - 插件上下文中完全替换 fetch 为空函数
  - 插件的所有出站 HTTP 请求都会失败
  - 看起来是有意为之但可能造成问题
- **建议修复**: 
  1. 确认这是有意行为
  2. 文档化 fetch 替换行为
  3. 确保不会意外影响其他代码

#### 16. [网络] [中严重] [无显式 http.Agent 配置]
- **文件/路径**: 全局
- **问题描述**: 
  - 无显式的 http.Agent/https.Agent 配置
  - 依赖 Node.js 默认连接池
  - 无 maxSockets 或 maxConnections 配置
  - 高吞吐场景可能出问题
- **建议修复**: 
  1. 添加 http Agent 配置
  2. 文档化连接池大小
  3. 监控连接使用情况

#### 17. [序列化] [中严重] [合同版本硬编码为字符串字面量]
- **文件/路径**: `src/platform/contracts/inter-plane-contract-gateway.ts:184`
- **问题描述**: 
  - `schemaVersion: "v4.3"` 硬编码
  - 难以追踪合同版本演变
- **建议修复**: 
  1. 提取到类型化常量或 enum
  2. 集中管理合同版本
  3. 添加版本兼容性检查

#### 18. [序列化] [低严重] [Timestamp = string 丢失精度]
- **文件/路径**: `src/platform/contracts/types/domain/primitives.ts:10`
- **问题描述**: 
  - `type Timestamp = string` 无类型精度
  - ISO 8601 字符串无时区信息
- **建议修复**: 
  1. 使用 Date 对象或 branded string 类型
  2. 明确时区处理约定
  3. 文档化时间戳语义

#### 19. [源代码] [中严重] [29处非空断言 !.]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - 使用 `!.` 断言非空
  - 可能导致运行时错误
- **建议修复**: 
  1. 使用可选链 `?.` 或空值合并 `??`
  2. 添加适当的 null 检查
  3. 减少非空断言使用

#### 20. [API] [中严重] [API 路由 schema 版本硬编码]
- **文件/路径**: `src/platform/five-plane-interface/api/federation-routing-service.ts`
- **问题描述**: 
  - Zod schema 验证存在
  - 但 schema 版本未与 contract 版本同步
- **建议修复**: 
  1. 从 contract 版本派生 schema 版本
  2. 添加版本一致性检查
  3. 文档化版本关系

### 总结

本次补充Review（第十五轮 - 构建测试与类型系统Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. 无 TypeScript 增量编译缓存（每次完整重建9.6秒）
2. 76个 npm scripts 大部分重复构建
3. 168处 any 类型使用（绕过类型安全）
4. 38处 @ts-ignore 指令
5. ExecutionRecord 类型不匹配（14个文件）
6. 178处直接 process.env 变更无抽象
7. ScopedExternalAccessSandbox performHttpRequest 无超时
8. OIDC 服务 fetch 调用无超时
9. plugin-runtime-child 替换 globalThis.fetch 为 no-op

**中优先级**:
1. 30+处 as unknown as 双类型转换
2. rimraf extraneous 未声明
3. 21,998处 unlinkSync 无集中清理
4. 无统一 mocking 框架
5. Singleton 状态重置未标准化
6. 无显式 http.Agent 配置
7. 合同版本硬编码为字符串
8. Timestamp = string 丢失精度
9. 非空断言使用

**低优先级**:
1. 无分支覆盖率要求
2. API 路由 schema 版本未同步

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 72 | 87 | 39 | 198 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **109** | **182** | **85** | **376** |

### 优先修复建议

**立即处理 (本周内)**:
1. 添加 TypeScript 增量编译 (`incremental: true`)
2. 重构 npm scripts 避免重复构建
3. 减少 any 类型使用到 <50
4. 移除所有 @ts-ignore 指令
5. 修复 ExecutionRecord 类型不匹配
6. 添加 fetch 超时到 OIDC 和 sandbox

**短期内处理 (本月内)**:
1. 创建集中的测试清理 utility
2. 建立统一的 mocking 框架
3. 标准化 singleton 重置机制
4. 添加 http.Agent 连接池配置
5. 提取合同版本到类型化常量

**长期规划**:
1. 实现完整的类型安全（无 any）
2. 建立测试基础设施标准
3. 实现构建缓存优化
4. 完善网络层超时和重试
5. 建立覆盖率门禁标准

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十四轮 - 日志事件与工作流Review）

### 发现的新问题

#### 1. [可观测性] [高严重] [StructuredLogEntry 无 requestId 字段]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - StructuredLogEntry 接口无 requestId 字段
  - 只有 correlationId，它回退到 traceId
  - 无法独立追踪单个 HTTP 请求
- **建议修复**: 
  1. 在 StructuredLogEntry 添加 requestId 字段
  2. 在 HTTP 中间件中生成和注入 requestId
  3. 确保 requestId 流经所有日志调用

#### 2. [可观测性] [高严重] [StructuredLogger data 字段无自动清洗]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - 如果开发者传递 `{ password: "..." }` 给 logger.info，密码会被记录
  - 无字段级脱敏机制
  - 依赖开发者手动排除敏感字段
- **建议修复**: 
  1. 添加 OBSERVE_OUTPUT_BLACKLIST 类似的黑名单
  2. 实现 redact/mask/sanitize 工具函数
  3. 在日志传输前自动清洗敏感字段

#### 3. [可观测性] [中严重] [日志中敏感数据无 PII 处理]
- **文件/路径**: `src/platform/shared/observability/structured-logger.ts`
- **问题描述**: 
  - 无 PII 重写工具
  - 开发者必须手动避免记录敏感数据
  - 存在意外记录敏感信息的风险
- **建议修复**: 
  1. 添加 PII 检测和重写工具
  2. 在文档中明确记录敏感字段列表
  3. 添加测试验证敏感数据不被记录

#### 4. [可观测性] [中严重] [59处 console.* 直接调用]
- **文件/路径**: 多个源文件
- **问题描述**: 
  - 应该使用 StructuredLogger
  - console 调用可能不被日志聚合系统收集
  - 难以追踪和关联
- **建议修复**: 
  1. 替换所有 console.* 为 StructuredLogger
  2. 添加 ESLint 规则禁止 console.*
  3. 保留少量允许的 CLI 输出

#### 5. [可观测性] [中严重] [部分 logger.warn 调用缺少结构化数据]
- **文件/路径**: `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:122`
- **问题描述**: 
  ```typescript
  logger.warn("WebSocket connection rejected: missing subprotocol token")
  ```
  - 只有消息，无 data 对象
  - 难以关联和查询
- **建议修复**: 
  1. 添加 `{ data: { ... } }` 对象
  2. 确保所有日志包含上下文数据
  3. 添加日志审查工具

#### 6. [源代码] [高严重] [Harness while(true) 循环无硬性迭代上限]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:1442`
- **问题描述**: 
  ```typescript
  while (true) {
    // ...
    // 依赖 budget gate 和 guardrail vibration 退出
    // 如果两者都失败，循环永久继续
  }
  ```
  - 依赖 budget gate 和 guardrail vibration 退出
  - 无硬性迭代次数限制
  - 可能导致无限循环
- **建议修复**: 
  1. 添加硬性 maxIterations 上限
  2. 在达到上限时强制退出
  3. 记录详细的迭代统计用于调试

#### 7. [源代码] [高严重] [Oapeflir while(true) 循环可能无限重规划]
- **文件/路径**: `src/platform/five-plane-orchestration/oapeflir/oapeflir-loop-service.ts:355`
- **问题描述**: 
  - 如果 loopReplanDecision.shouldReplan 永不变为 false
  - 重规划永远无法减少计划错误
  - 可能导致无限循环
- **建议修复**: 
  1. 添加重规划次数上限
  2. 在达到上限时强制接受当前结果
  3. 添加重规划质量衰减检测

#### 8. [源代码] [中严重] [Recovery 流程允许终态转换到 paused]
- **文件/路径**: `src/platform/five-plane-execution/runtime-state-machine.ts:105-108`
- **问题描述**: 
  - completed, failed, cancelled, aborted 可转换到 paused 进行恢复
  - 创建恢复路径可能重新进入执行
  - 需要验证此行为的正确性
- **建议修复**: 
  1. 审查 recovery 转换的合法性
  2. 确保不会导致状态不一致
  3. 添加转换前置条件验证

#### 9. [国际化] [高严重] [API 错误消息硬编码英文]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/api-error.ts`
- **问题描述**: 
  ```typescript
  return new ApiError(404, "api.task_not_found", "Task not found.");
  ```
  - 错误消息硬编码英文
  - 无后端 i18n 机制
  - 非英文客户端收到不本地化的错误
- **建议修复**: 
  1. 使用错误码而非硬编码消息
  2. 客户端根据错误码本地化
  3. 或在后端添加 i18n 支持

#### 10. [国际化] [中严重] [UI 硬编码字符串未翻译]
- **文件/路径**: `ui/packages/features/conversation/src/web/index.tsx`
- **问题描述**: 
  - "Build Plan", "Execute", "Messages" 等按钮标签硬编码
  - 不使用 i18n 系统
- **建议修复**: 
  1. 使用 `translateFeatureCopy` 替换硬编码
  2. 确保所有用户可见文本通过 i18n
  3. 添加硬编码字符串检测

#### 11. [国际化] [中严重] [Arabic 目录不完整]
- **文件/路径**: `ui/packages/shared/i18n/src/catalogs/ar-SA.ts`
- **问题描述**: 
  - ar-SA 只有约18条消息
  - en-US 和 zh-CN 有约117条
  - 缺失的 key 返回 key 本身
- **建议修复**: 
  1. 补充 Arabic 翻译
  2. 确保所有 key 都有翻译
  3. 添加翻译完整性测试

#### 12. [国际化] [低严重] [translateFeatureCopy 无回退默认值]
- **文件/路径**: `ui/packages/shared/i18n/`
- **问题描述**: 
  - 如果 feature ID 不在 catalog 中
  - 返回 undefined title/summary
  - 无合理回退
- **建议修复**: 
  1. 添加回退到 key 或默认字符串
  2. 或使用 feature ID 作为显示名
  3. 确保 UI 不显示 undefined

#### 13. [运维] [高严重] [健康检查无 readiness/liveness 区分]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts`
- **问题描述**: 
  - /healthz, /v1/healthz, /health 返回相同报告
  - 无 readiness probe（可处理流量？）
  - 无 liveness probe（系统存活？）
  - canAcceptTraffic() 未通过 HTTP 暴露
- **建议修复**: 
  1. 添加 /ready 和 /live 端点
  2. Readiness 检查依赖项就绪
  3. Liveness 检查进程健康

#### 14. [运维] [高严重] [shutdown 期间健康检查存在竞态]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts`
- **问题描述**: 
  - getHealthReportAsync() 异步检查 dbWritable
  - shutdown 期间数据库连接可能关闭
  - 健康检查仍在传入
- **建议修复**: 
  1. 在 shutdown 时返回 unhealthy
  2. 添加 shutdown 状态标志
  3. 停止接受新的健康检查请求

#### 15. [运维] [高严重] [LeaderElectionService 未与 GracefulShutdown 集成]
- **文件/路径**: `src/platform/five-plane-execution/ha/leader-election-service.ts`
- **问题描述**: 
  - LeaderElectionService.stop() 处理优雅退位
  - 但未在全局 graceful shutdown 中注册
  - 需显式注册为 shutdown handler
- **建议修复**: 
  1. 将 LeaderElectionService 添加到 shutdown handlers
  2. 确保领导者在 shutdown 时让位
  3. 验证关闭顺序正确

#### 16. [运维] [中严重] [addHandler() 无显式排序约定]
- **文件/路径**: `src/platform/five-plane-execution/startup/graceful-shutdown.ts`
- **问题描述**: 
  - 虽然 handlers 逆序执行
  - 但无显式接口声明 handler 依赖关系
  - 关键 handlers（如关闭数据库连接）应最后运行
- **建议修复**: 
  1. 添加优先级或阶段参数
  2. 文档化关键 handler 顺序要求
  3. 添加顺序验证测试

#### 17. [运维] [中严重] [unref'd timers 可能不触发]
- **文件/路径**: `src/platform/five-plane-execution/startup/process-error-handlers.ts:106`
- **问题描述**: 
  - `setTimeout(...).unref()` 不保持事件循环活跃
  - 如果主线程空闲，强制退出可能不触发
- **建议修复**: 
  1. 移除 .unref() 除非确实需要
  2. 或确保有其他方式保持事件循环
  3. 添加超时触发验证测试

#### 18. [运维] [中严重] [StartupConsistencyChecker 阻塞流量但不暴露状态]
- **文件/路径**: `src/platform/five-plane-startup-plan.ts`
- **问题描述**: 
  - canAcceptTraffic() 在 P0 问题存在时返回 false
  - 但 _trafficBlocked 是内部状态
  - 负载均衡器访问 /healthz 仍会得到 "ok"
- **建议修复**: 
  1. 在健康报告中暴露 trafficBlocked 状态
  2. 或添加单独的健康端点指示就绪状态
  3. 确保负载均衡器在阻塞时不路由流量

#### 19. [事件系统] [中严重] [TypedEventBus vs EventEmitter 混用]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - 项目同时使用 TypedEventBus 和原生 EventEmitter
  - TypedEventBus 应该是标准
  - 混用可能导致类型安全问题
- **建议修复**: 
  1. 创建迁移计划统一使用 TypedEventBus
  2. 添加 ESLint 规则禁止原生 EventEmitter
  3. 在 CI 中检测混用情况

#### 20. [源代码] [低严重] [无事件排序保证]
- **文件/路径**: `src/platform/five-plane-state-evidence/events/`
- **问题描述**: 
  - 无明确的事件排序保证文档
  - 事件处理可能乱序
  - 依赖方需要处理乱序
- **建议修复**: 
  1. 文档化事件排序语义
  2. 如果需要排序，实现序列号机制
  3. 在事件处理中考虑乱序情况

### 总结

本次补充Review（第十四轮 - 日志事件与工作流Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. StructuredLogEntry 无 requestId 字段
2. StructuredLogger data 字段无自动清洗（敏感数据风险）
3. Harness while(true) 循环无硬性迭代上限
4. Oapeflir while(true) 循环可能无限重规划
5. API 错误消息硬编码英文（无 i18n）
6. 健康检查无 readiness/liveness 区分
7. shutdown 期间健康检查存在竞态
8. LeaderElectionService 未与 GracefulShutdown 集成

**中优先级**:
1. 日志中敏感数据无 PII 处理
2. 59处 console.* 直接调用
3. 部分 logger.warn 调用缺少结构化数据
4. Recovery 流程允许终态转换到 paused
5. UI 硬编码字符串未翻译
6. Arabic 目录不完整
7. addHandler() 无显式排序约定
8. unref'd timers 可能不触发
9. StartupConsistencyChecker 阻塞流量但不暴露状态
10. TypedEventBus vs EventEmitter 混用

**低优先级**:
1. translateFeatureCopy 无回退默认值
2. 无事件排序保证

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 68 | 80 | 37 | 185 |
| 测试 | 8 | 14 | 5 | 27 |
| 配置 | 10 | 38 | 24 | 72 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **103** | **171** | **82** | **356** |

### 优先修复建议

**立即处理 (本周内)**:
1. 在 StructuredLogEntry 添加 requestId 字段
2. 实现日志敏感字段自动清洗
3. 为工作流循环添加硬性迭代上限
4. 添加 readiness/liveness 健康检查端点
5. 将 LeaderElectionService 集成到 GracefulShutdown
6. 实现 API 错误消息 i18n 支持

**短期内处理 (本月内)**:
1. 替换所有 console.* 为 StructuredLogger
2. 补充 Arabic i18n 目录
3. 修复 UI 硬编码字符串
4. 统一 TypedEventBus 替代 EventEmitter
5. 解决 shutdown 竞态条件
6. 添加事件排序保证文档

**长期规划**:
1. 建立完整的 PII 处理框架
2. 实现日志完整性验证
3. 建立工作流安全循环检测
4. 完善健康检查和就绪探测
5. 建立国际化完整测试

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十五轮 - 构建测试与类型系统Review）

### 发现的新问题

#### 1. [构建] [高严重] [无 TypeScript 增量编译缓存]
- **文件/路径**: `tsconfig.json`
- **问题描述**: 
  - 无 `incremental: true` 配置
  - 无 `tsbuildinfo` 文件
  - 每次构建是完整重建（9.6秒）
  - TypeScript 无法使用增量编译
- **建议修复**: 
  1. 在 tsconfig.json 添加 `"incremental": true`
  2. 添加 `"tsbuildinfo": ".tsbuildinfo"` 到 .gitignore
  3. 实现项目引用以支持并行构建

#### 2. [构建] [高严重] [76个 npm scripts 大部分重复构建]
- **文件/路径**: `package.json`
- **问题描述**: 
  - 40+ CLI scripts 都运行 `npm run build && node ...`
  - 每个脚本前都执行完整重建
  - 无法直接运行预构建的 CLI
  - 20+ stable:* 脚本遵循相同模式
- **建议修复**: 
  1. 创建 `build:cli` 脚本构建一次
  2. 让 CLI 脚本使用预构建的 dist 文件
  3. 添加 npm run dev 或类似的无需重建的执行方式

#### 3. [源代码] [高严重] [168处 any 类型使用]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - `stable-runner-factory.ts`: `opts: any` -> `any`
  - `billing-service-async.ts`: `input: any` 方法签名
  - 重试/超时工具使用 `(...args: any[]) => Promise<any>`
  - 绕过类型安全
- **建议修复**: 
  1. 使用泛型替代 any
  2. 替换为具体的输入类型
  3. 添加 ESLint 规则禁止 any

#### 4. [源代码] [高严重] [38处 @ts-ignore 指令]
- **文件/路径**: 多个文件
- **问题描述**: 
  - 14个 stability 相关文件有 `@ts-ignore ExecutionRecord type mismatch`
  - 5个 harness-sdk 文件有 `Partial<HarnessRun> doesn't have all required properties`
  - 2个文件有 `exactOptionalPropertyTypes` 问题
- **建议修复**: 
  1. 修复 ExecutionRecord 类型不匹配
  2. 完成 HarnessRun 类型的必需属性
  3. 移除 @ts-ignore 使用

#### 5. [源代码] [高严重] [ExecutionRecord 类型不匹配]
- **文件/路径**: `src/platform/stability/stable-evidence-bundle-support.ts:783` 等14个文件
- **问题描述**: 
  - 插入 execution record 时 TypeScript 拒绝类型
  - `ExecutionRecord` 接口要求新字段但 insert 对象缺失
  - 或 store 的 insert 方法接受不同类型
- **建议修复**: 
  1. 对齐 ExecutionRecord 接口和 store 类型
  2. 添加缺失字段到 insert 对象
  3. 或调整 store 的 insert 方法类型签名

#### 6. [源代码] [中严重] [30+处 as unknown as 双类型转换]
- **文件/路径**: `division-loader.ts`, `domain-baseline-catalog.ts`, `harness-sdk`
- **问题描述**: 
  - 完全绕过类型安全
  - 通常是类型架构问题的信号
- **建议修复**: 
  1. 修复上游类型问题
  2. 使用更具体的类型断言
  3. 重构类型层次结构

#### 7. [构建] [中严重] [rimraf  extraneous - 未在 package.json 声明]
- **文件/路径**: `node_modules/rimraf`
- **问题描述**: 
  - rimraf@6.1.3 存在于 node_modules 但未在 package.json 声明
  - 可能导致依赖问题
- **建议修复**: 
  1. 添加到 dependencies 或 devDependencies
  2. 或移除并使用其他清理方式

#### 8. [测试] [高严重] [178处直接 process.env 变更无抽象]
- **文件/路径**: `tests/unit/domains/registry/plugin-spi-registry.test.ts:640-687`
- **问题描述**: 
  - 直接读取/写入 process.env
  - 在 beforeEach/afterEach 中手动保存/恢复
  - 无 helper 抽象
- **建议修复**: 
  1. 创建 env helper 函数
  2. 统一保存和恢复模式
  3. 添加测试隔离验证

#### 9. [测试] [高严重] [21,998处 unlinkSync 调用无集中清理]
- **文件/路径**: 多个测试文件
- **问题描述**: 
  - 每个测试重新实现清理逻辑
  - 无集中清理 utility
  - 可能导致 EBUSY (Windows) 或并发访问问题
- **建议修复**: 
  1. 创建集中的测试清理 utility
  2. 使用 afterEach 确保清理
  3. 考虑使用临时目录

#### 10. [测试] [中严重] [无统一 mocking 框架]
- **文件/路径**: `tests/` 多个目录
- **问题描述**: 
  - 10,579 处 vi.fn, jest.mock, sinon 等调用
  - 无统一的 mocking 框架可见
  - 模式不一致
- **建议修复**: 
  1. 建立统一的 mock factory
  2. 标准化 mock 模式
  3. 添加 mocking 最佳实践文档

#### 11. [测试] [中严重] [Singleton 状态重置未标准化]
- **文件/路径**: `tests/unit/platform/five-plane-execution/dispatcher.test.ts`
- **问题描述**: 
  - 测试通过特定函数重置 singletons
  - 每个测试重复自己的 reset 逻辑
  - 无统一的 resetAllSingletons()
- **建议修复**: 
  1. 创建统一的 singleton reset 机制
  2. 在 afterEach 中自动调用
  3. 文档化 singleton 重置要求

#### 12. [测试] [低严重] [无分支覆盖率要求]
- **文件/路径**: `.c8rc.json`
- **问题描述**: 
  - `"100": false` - 无覆盖率要求
  - 只测量源代码，不测量测试文件
- **建议修复**: 
  1. 添加分支覆盖率阈值
  2. 在 CI 中强制执行覆盖率 gate
  3. 关注关键代码路径

#### 13. [网络] [高严重] [ScopedExternalAccessSandbox performHttpRequest 无超时]
- **文件/路径**: `src/platform/five-plane-execution/plugin-executor/scoped-external-access-sandbox.ts:289`
- **问题描述**: 
  - fetch 调用无 timeout 参数
  - 挂起的请求可能永久阻塞
- **建议修复**: 
  1. 添加 AbortController timeout
  2. 使用标准的超时模式
  3. 记录超时配置

#### 14. [网络] [高严重] [OIDC 服务 fetch 调用无超时]
- **文件/路径**: `src/org-governance/sso-scim/oidc/oidc-service.ts:565,272`
- **问题描述**: 
  - OIDC token 交换无 retry logic
  - 无 timeout/abort signal
  - 认证流程可能挂起
- **建议修复**: 
  1. 添加 fetch timeout
  2. 添加 retry logic
  3. 使用标准的超时配置

#### 15. [网络] [高严重] [plugin-runtime-child 替换 globalThis.fetch 为 no-op]
- **文件/路径**: `src/domains/registry/plugin-runtime-child.ts:112`
- **问题描述**: 
  ```typescript
  globalThis.fetch = (async () => {}) as unknown as typeof globalThis.fetch;
  ```
  - 插件上下文中完全替换 fetch 为空函数
  - 插件的所有出站 HTTP 请求都会失败
  - 看起来是有意为之但可能造成问题
- **建议修复**: 
  1. 确认这是有意行为
  2. 文档化 fetch 替换行为
  3. 确保不会意外影响其他代码

#### 16. [网络] [中严重] [无显式 http.Agent 配置]
- **文件/路径**: 全局
- **问题描述**: 
  - 无显式的 http.Agent/https.Agent 配置
  - 依赖 Node.js 默认连接池
  - 无 maxSockets 或 maxConnections 配置
  - 高吞吐场景可能出问题
- **建议修复**: 
  1. 添加 http Agent 配置
  2. 文档化连接池大小
  3. 监控连接使用情况

#### 17. [序列化] [中严重] [合同版本硬编码为字符串字面量]
- **文件/路径**: `src/platform/contracts/inter-plane-contract-gateway.ts:184`
- **问题描述**: 
  - `schemaVersion: "v4.3"` 硬编码
  - 难以追踪合同版本演变
- **建议修复**: 
  1. 提取到类型化常量或 enum
  2. 集中管理合同版本
  3. 添加版本兼容性检查

#### 18. [序列化] [低严重] [Timestamp = string 丢失精度]
- **文件/路径**: `src/platform/contracts/types/domain/primitives.ts:10`
- **问题描述**: 
  - `type Timestamp = string` 无类型精度
  - ISO 8601 字符串无时区信息
- **建议修复**: 
  1. 使用 Date 对象或 branded string 类型
  2. 明确时区处理约定
  3. 文档化时间戳语义

#### 19. [源代码] [中严重] [29处非空断言 !.]
- **文件/路径**: 整个 src 目录
- **问题描述**: 
  - 使用 `!.` 断言非空
  - 可能导致运行时错误
- **建议修复**: 
  1. 使用可选链 `?.` 或空值合并 `??`
  2. 添加适当的 null 检查
  3. 减少非空断言使用

#### 20. [API] [中严重] [API 路由 schema 版本硬编码]
- **文件/路径**: `src/platform/five-plane-interface/api/federation-routing-service.ts`
- **问题描述**: 
  - Zod schema 验证存在
  - 但 schema 版本未与 contract 版本同步
- **建议修复**: 
  1. 从 contract 版本派生 schema 版本
  2. 添加版本一致性检查
  3. 文档化版本关系

### 总结

本次补充Review（第十五轮 - 构建测试与类型系统Review - 2026-05-14）发现了20个新问题。

**高优先级 (需要立即处理)**:
1. 无 TypeScript 增量编译缓存（每次完整重建9.6秒）
2. 76个 npm scripts 大部分重复构建
3. 168处 any 类型使用（绕过类型安全）
4. 38处 @ts-ignore 指令
5. ExecutionRecord 类型不匹配（14个文件）
6. 178处直接 process.env 变更无抽象
7. ScopedExternalAccessSandbox performHttpRequest 无超时
8. OIDC 服务 fetch 调用无超时
9. plugin-runtime-child 替换 globalThis.fetch 为 no-op

**中优先级**:
1. 30+处 as unknown as 双类型转换
2. rimraf extraneous 未声明
3. 21,998处 unlinkSync 无集中清理
4. 无统一 mocking 框架
5. Singleton 状态重置未标准化
6. 无显式 http.Agent 配置
7. 合同版本硬编码为字符串
8. Timestamp = string 丢失精度
9. 非空断言使用

**低优先级**:
1. 无分支覆盖率要求
2. API 路由 schema 版本未同步

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 72 | 87 | 39 | 198 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 14 | 15 | 3 | 32 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **109** | **182** | **85** | **376** |

### 优先修复建议

**立即处理 (本周内)**:
1. 添加 TypeScript 增量编译 (`incremental: true`)
2. 重构 npm scripts 避免重复构建
3. 减少 any 类型使用到 <50
4. 移除所有 @ts-ignore 指令
5. 修复 ExecutionRecord 类型不匹配
6. 添加 fetch 超时到 OIDC 和 sandbox

**短期内处理 (本月内)**:
1. 创建集中的测试清理 utility
2. 建立统一的 mocking 框架
3. 标准化 singleton 重置机制
4. 添加 http.Agent 连接池配置
5. 提取合同版本到类型化常量

**长期规划**:
1. 实现完整的类型安全（无 any）
2. 建立测试基础设施标准
3. 实现构建缓存优化
4. 完善网络层超时和重试
5. 建立覆盖率门禁标准

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十六轮 - 架构模式与内存管理Review）

### 发现的新问题

#### 1. [架构] [严重] [HarnessRuntimeService 是 God Object]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts:721`
- **问题描述**: 
  - 19个 private 实例变量（应该最多3-5个）
  - 61个 public 方法
  - 9个注入的委托服务
  - 违反单一职责原则
  - 协调太多子系统
- **建议修复**: 
  1. 拆分为 `HarnessStateManager`（状态转换）
  2. 拆分为 `HarnessMemoryCoordinator`（内存操作）
  3. 拆分为 `HarnessRecoveryHandler`（失败处理）
  4. 拆分为 `HarnessHitlCoordinator`（人机协作）

#### 2. [架构] [高严重] [巨型 barrel files 导致构建问题]
- **文件/路径**: `src/platform/five-plane-orchestration/harness/index.ts` (2317行)
- **问题描述**: 
  - 15个子模块的重新导出
  - 修改任何功能时可能触发大规模重编译
  - 创建循环依赖风险
- **建议修复**: 
  1. 每个 index.ts 最多重新导出5-7项
  2. 优先使用直接模块导入
  3. 按领域拆分 contracts 为多个文件

#### 3. [内存] [高严重] [167个事件监听器仅16个清理]
- **文件/路径**: 整个代码库
- **问题描述**: 
  - `.on`/`addEventListener`: 167次
  - `.off`/`removeListener`/`removeAllListeners`: 仅16次
  - 严重不平衡，存在内存泄漏
- **建议修复**: 
  1. 审计所有事件监听器注册点
  2. 确保每个监听器有对应的清理
  3. 使用 `{ once: true }` 自动清理

#### 4. [内存] [高严重] [plugin-runtime-host.ts 子进程监听器未清理]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts:275,278,353,358`
- **问题描述**: 
  ```typescript
  child.stderr?.on("data", ...);
  child.on("message", ...);
  child.stdout.on("data", ...);
  ```
  - 在 attachChild()/spawnChild() 中注册
  - stop() 方法从未移除它们
  - 插件运行时停止后监听器仍附着在分离的子进程
- **建议修复**: 
  1. 在 stop() 中添加清理：
  ```typescript
  child.stdout?.removeAllListeners();
  child.stderr?.removeAllListeners();
  child.removeAllListeners("message");
  ```

#### 5. [架构] [中严重] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts` (763行)
- **问题描述**: 
  - 大部分 domain index.ts 只有12行
  - yono 导出11个类（YonoRepository, YonoMarketService, YonoCommentService 等）
  - 单个文件包含过多职责
- **建议修复**: 
  1. 拆分为 yono/market-service.ts
  2. 拆分为 yono/comment-service.ts
  3. 每个类一个文件

#### 6. [架构] [中严重] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts` (451行)
- **问题描述**: 
  - 处理平台启动、运行时目录、引导、演示模式
  - 从多个子系统导出
  - 违反单一职责
- **建议修复**: 
  1. 拆分为 bootstrap.ts
  2. 拆分为 startup.ts
  3. 拆分为 exports.ts

#### 7. [架构] [中严重] [contracts/executable-contracts/index.ts 数据vs对象混淆]
- **文件/路径**: `src/platform/contracts/executable-contracts/index.ts` (2169行)
- **问题描述**: 
  - 527个类型/接口定义
  - 纯数据结构（PrincipalRef, HumanPrincipalRef等）
  - 无行为，只有类型导出
  - 类型和工厂函数混在一起
- **建议修复**: 
  1. 拆分为 runtime-contracts.ts, event-contracts.ts, directive-contracts.ts
  2. 将类型和工厂函数分离
  3. 建立明确的边界

#### 8. [领域] [高严重] [DomainLifecycleState 重复定义且不兼容]
- **文件/路径**: `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38`
- **问题描述**: 
  - `architecture-remediation.ts`: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (Title Case)
  - `domain-specs.ts`: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
  - 两个完全不兼容的 schema
  - 脆弱的映射层尝试规范化
- **建议修复**: 
  1. 统一为一个规范定义
  2. 移除重复
  3. 建立单一数据源

#### 9. [领域] [中严重] [风险评分阈值硬编码无常量]
- **文件/路径**: `src/domains/risk-profile/index.ts:68-78`
- **问题描述**: 
  ```typescript
  if (score >= 85) return "critical";
  if (score >= 65) return "high";
  if (score >= 35) return "medium";
  ```
  - 魔法数字 85, 65, 35
  - 无常量、注释或配置
- **建议修复**: 
  1. 提取到命名常量
  2. 添加配置选项
  3. 文档化决策边界

#### 10. [领域] [中严重] [HR角色检测硬编码工具名]
- **文件/路径**: `src/domains/governance/hr-role-governance-service.ts:435-436`
- **问题描述**: 
  ```typescript
  expandedProposalTools.resolvedToolNames.every((toolName) =>
    toolName === "read" || toolName === "question"
  );
  ```
  - 硬编码的工具名 "read" 和 "question"
  - 应该使用常量
- **建议修复**: 
  1. 提取为常量 READ_ONLY_TOOL_NAMES
  2. 添加配置支持
  3. 文档化只读角色定义

#### 11. [领域] [中严重] [无 invariant 强制机制]
- **文件/路径**: 整体架构
- **问题描述**: 
  - `canTransitionDomain` 返回 boolean 但无强制
  - 转换可以在不使用验证器的情况下尝试
  - Domain seeds 和 risk specs 之间无一致性检查
- **建议修复**: 
  1. 添加 invariant 强制框架
  2. 确保状态转换经过验证
  3. 添加转换前条件检查

#### 12. [安全] [中严重] [OpenAPI 端点公开无认证]
- **文件/路径**: `src/platform/five-plane-interface/api/http-server/health-routes.ts:41`
- **问题描述**: 
  ```typescript
  { method: "GET", pathname: "/v1/openapi.json", handler: () => buildJsonDocumentResponse(buildOpenApiDocument()) }
  ```
  - API 结构文档无认证暴露
  - 可能泄露敏感 API 信息
- **建议修复**: 
  1. 添加认证或限制访问
  2. 在生产环境禁用
  3. 文档化风险

#### 13. [安全] [中严重] [内存会话存储限制水平扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/session-management.ts:83-89`
- **问题描述**: 
  - Sessions 存储在模块级 Map
  - 多服务器实例部署不工作
  - 无分布式会话存储
- **建议修复**: 
  1. 使用 Redis 等分布式会话存储
  2. 添加会话复制
  3. 文档化扩展限制

#### 14. [安全] [中严重] [内存服务身份存储限制扩展]
- **文件/路径**: `src/platform/five-plane-control-plane/iam/service-auth.ts:92`
- **问题描述**: 
  - `serviceIdentities` Map 在内存中
  - 多实例部署问题
- **建议修复**: 
  1. 使用共享存储
  2. 或在启动时从配置加载

#### 15. [配置] [低严重] [多个 architecture-remediation.ts 文件重名]
- **文件/路径**: 
  - `src/domains/architecture-remediation.ts`
  - `src/org-governance/architecture-remediation.ts`
  - `src/ops-maturity/architecture-remediation.ts`
  - `src/scale-ecosystem/architecture-remediation.ts`
  - `src/interaction/architecture-remediation.ts`
- **问题描述**: 
  - 相同名称在不同作用域
  - 可能导致导入混淆
  - 看起来是有意为之但可能造成混乱
- **建议修复**: 
  1. 添加作用域前缀或后缀
  2. 文档化每个文件的用途
  3. 确保导入明确

#### 16. [内存] [低严重] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但在 shutdown 时可能未调用
  - 需要验证关闭顺序
- **建议修复**: 
  1. 验证 shutdown 顺序
  2. 添加关闭 hooks
  3. 确保资源释放

#### 17. [架构] [低严重] [Core/runtime 是平台执行文件的包装]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - orchestrator/index.ts, planner/index.ts, supervisor/index.ts 都重新导出
  - 只被一个 SDK 文件使用
  - 价值可疑的间接层
- **建议修复**: 
  1. 移除或文档化用途
  2. 直接从 platform 导入

### 总结

本次补充Review（第十六轮 - 架构模式与内存管理Review - 2026-05-14）发现了17个新问题。

**高优先级 (需要立即处理)**:
1. HarnessRuntimeService 是 God Object（19变量，61方法）
2. 巨型 barrel files 导致构建问题
3. 167个事件监听器仅16个清理
4. plugin-runtime-host.ts 子进程监听器未清理
5. DomainLifecycleState 重复定义且不兼容
6. 风险评分阈值硬编码无常量

**中优先级**:
1. yono/index.ts 763行应拆分
2. src/index.ts 451行应拆分
3. contracts 数据vs对象混淆
4. HR角色检测硬编码工具名
5. 无 invariant 强制机制
6. OpenAPI 端点公开无认证
7. 内存会话存储限制水平扩展
8. 内存服务身份存储限制扩展

**低优先级**:
1. 多个 architecture-remediation.ts 文件重名
2. PgDatabase.close() 验证问题
3. Core/runtime 是包装价值可疑

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78 | 93 | 42 | 213 |
| 测试 | 10 | 17 | 6 | 33 |
| 配置 | 10 | 39 | 24 | 73 |
| 安全 | 15 | 17 | 3 | 35 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **116** | **190** | **88** | **394** |

### 优先修复建议

**立即处理 (本周内)**:
1. 拆分 HarnessRuntimeService 为专注服务
2. 修复 plugin-runtime-host.ts 子进程监听器清理
3. 统一 DomainLifecycleState 定义
4. 添加事件监听器清理审计
5. 添加 fetch 超时到所有网络调用

**短期内处理 (本月内)**:
1. 拆分 yono/index.ts 和 src/index.ts
2. 建立 invariant 强制框架
3. 添加分布式会话存储
4. 移除 barrel files 或限制规模
5. 提取所有魔法数字到常量

**长期规划**:
1. 完整的 God Object 重构计划
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

*Review generated: 2026-05-14*
---

## [2026-05-14] 自动Review报告（第十七轮 - 深度专项Review）

### 深度Review覆盖范围
- src/platform/ 144个文件 ~276K行代码
- src/domains/ 95个文件 40+领域
- src/sdk/ 103个文件
- config/environments/ 环境配置
- tests/unit/ 关键测试文件
- UI components

---

### 发现的新问题

#### 1. [源代码] [严重] [Benchmark 百分位计算完全错误]
- **文件/路径**: `src/ops-maturity/benchmarking/benchmark-collector.ts`
- **问题描述**: 
  - p50/p95/p99 全部错误计算为平均值
  - 实际代码: `p50: values[Math.floor(len * 0.5)]` 然后又赋值 `avg`
  - 正确的百分位计算应该使用分位数公式
- **影响**: 所有性能基准测试的百分位数据完全不可信
- **建议修复**: 实现正确的分位数计算逻辑

#### 2. [配置] [严重] [环境名称错配 - 所有环境显示"prod"]
- **文件/路径**: `config/environments/dev.json`, `staging.json`, `pre-prod.json`
- **问题描述**: 
  - 所有配置文件 `"name": "prod"` 而非各自实际环境名
  - 这导致日志和监控中无法区分环境
- **建议修复**: 修正为各自的环境名称

#### 3. [内存] [严重] [Plugin runtime child process 事件监听器永不清洗]
- **文件/路径**: `src/domains/registry/plugin-runtime-host.ts`
- **问题描述**: 
  - stdout/stderr/message 事件监听器在 stop() 时未移除
  - 会导致内存泄漏
- **建议修复**: 在 stop() 中清理所有 child 事件监听器

#### 4. [安全] [严重] [OAuth token 明文存储]
- **文件/路径**: `src/sdk/cli/login.ts:70-79`
- **问题描述**: 
  - OAuth tokens 以 JSON 明文存储
  - 权限 0o600 虽限制所有者访问，但仍为明文
- **建议修复**: 使用系统 keychain 或加密存储

#### 5. [安全] [严重] [硬编码 CVE 绕过列表导致虚假安全感]
- **文件/路径**: `src/sdk/plugin-definition.ts`
- **问题描述**: 
  - `bypassCVERegistryCheck: true` 和硬编码 CVE 列表
  - SBOM 验证形同虚设
- **建议修复**: 删除绕过机制或重新设计安全验证

#### 6. [安全] [严重] [弱 RSA 2048位密钥]
- **文件/路径**: `src/sdk/中加密相关文件`
- **问题描述**: 
  - 使用 2048位 RSA 可能不足以应对现代威胁
  - 应使用 4096位或 P-384 ECC
- **建议修复**: 升级密钥长度

#### 7. [源代码] [高] [process.chdir() 安全漏洞]
- **文件/路径**: `src/domains/registry/plugin-runtime-child.ts:89-93`
- **问题描述**: 
  - 无验证的 `process.chdir(sandboxRoot)` 
  - 可被恶意插件利用
- **建议修复**: 添加 sandboxRoot 验证和清理检查

#### 8. [源代码] [高] [memory leak - EventListener 167个仅16个清理]
- **文件/路径**: 多个文件
- **问题描述**: 
  - 添加了 167 个事件监听器
  - 仅 16 个在销毁时清理
  - 会导致内存持续增长
- **建议修复**: 统一事件监听器生命周期管理

#### 9. [源代码] [高] [TODO R4-27: HarnessRun 未持久化]
- **文件/路径**: `src/platform/five-plane-execution/execution-engine/single-task-happy-path.ts:270`
- **问题描述**: 
  - HarnessRun 对象创建后未持久化
  - 系统重启后无法恢复
- **建议修复**: 添加 HarnessRun 持久化逻辑

#### 10. [源代码] [高] [budget-allocator 竞态条件]
- **文件/路径**: `src/platform/five-plane-execution/budget-allocator.ts:504-507`
- **问题描述**: 
  - CAS 检查在事务外执行
  - 可能导致双重扣款
- **建议修复**: 将 CAS 检查移入原子事务

#### 11. [测试] [高] [budget-allocator.test.ts 缺少 throttle ratio 断言]
- **文件/路径**: `tests/unit/platform/five-plane-execution/budget-allocator.test.ts:286-335`
- **问题描述**: 
  - 测试应该验证 throttle ratio 但实际未验证
  - 导致关键功能未被测试覆盖
- **建议修复**: 添加 throttle ratio 的断言

#### 12. [测试] [高] [durable-event-bus-async.test.ts 定时器时序问题]
- **文件/路径**: `tests/unit/platform/five-plane-state-evidence/events/durable-event-bus-async.test.ts:74,104,117`
- **问题描述**: 
  - 使用 setTimeout 断言但时间不确定
  - 可能导致测试不稳定
- **建议修复**: 使用 fake timers 或事件监听代替

#### 13. [源代码] [中] [console.* 调用 59 处未用 StructuredLogger]
- **文件/路径**: 多个源文件
- **问题描述**: 
  - 直接调用 console.log/error/warn
  - 应使用 StructuredLogger 统一日志
- **建议修复**: 替换为 StructuredLogger

#### 14. [源代码] [中] [: any 类型 168 处]
- **文件/路径**: 多个源文件
- **问题描述**: 
  - 大量 `: any` 类型用法
  - 降低类型安全
- **建议修复**: 减少 any 类型使用，增加类型约束

#### 15. [源代码] [中] [as unknown as 双转型 30+ 处]
- **文件/路径**: 多个源文件
- **问题描述**: 
  - 滥用双转型模式
  - 掩盖类型错误
- **建议修复**: 改进类型设计避免双转型

#### 16. [源代码] [中] [@ts-ignore 38 处]
- **文件/路径**: 多个源文件
- **问题描述**: 
  - 过多 ts-ignore 使用
  - 隐藏潜在问题
- **建议修复**: 修复底层类型问题

#### 17. [源代码] [中] [巨型 barrel files 导致构建问题]
- **文件/路径**: 多个 index.ts barrel files
- **问题描述**: 
  - 巨型 barrel files 拖慢构建
  - 增加循环依赖风险
- **建议修复**: 拆分或使用路径映射

#### 18. [源代码] [中] [HR角色检测硬编码工具名]
- **文件/路径**: 相关权限检测文件
- **问题描述**: 
  - HumanReview 角色检测硬编码工具名
  - 不够灵活
- **建议修复**: 使用配置驱动

#### 19. [源代码] [中] [无 invariant 强制机制]
- **文件/路径**: 全局
- **问题描述**: 
  - 没有 invariant 验证框架
  - 关键不变量未被强制
- **建议修复**: 实现 invariant 检查框架

#### 20. [源代码] [中] [内存会话存储限制水平扩展]
- **文件/路径**: 会话相关模块
- **问题描述**: 
  - 基于内存的会话存储无法水平扩展
  - 多实例部署会有问题
- **建议修复**: 使用分布式会话存储

#### 21. [源代码] [中] [内存服务身份存储限制扩展]
- **文件/路径**: 服务身份模块
- **问题描述**: 
  - 服务身份存储在内存中
  - 无法跨实例共享
- **建议修复**: 使用分布式存储

#### 22. [配置] [中] [config/security/ 环境配置缺少字段]
- **文件/路径**: `config/security/` 下各环境配置
- **问题描述**: 
  - dev/staging/pre-prod 只有 approvalMode
  - 缺少 sandboxMode/remoteWorkerRegistration
- **建议修复**: 补充缺失的安全配置字段

#### 23. [安全] [中] [OpenAPI 端点公开无认证]
- **文件/路径**: OpenAPI 相关配置
- **问题描述**: 
  - 某些端点可能被公开访问
  - 需要认证保护
- **建议修复**: 添加认证中间件

#### 24. [测试] [中] [非确定性 Math.random() 测试]
- **文件/路径**: SDK 相关测试文件
- **问题描述**: 
  - 测试依赖 Math.random() 导致非确定
  - 难以复现和调试
- **建议修复**: 使用确定种子或 mock

#### 25. [源代码] [低] [contracts 数据vs对象混淆]
- **文件/路径**: contracts 目录
- **问题描述**: 
  - 契约中数据类型与对象类型混淆
  - 可能导致序列化问题
- **建议修复**: 明确区分数据模型和领域对象

#### 26. [源代码] [低] [多个 architecture-remediation.ts 重名]
- **文件/路径**: 各领域目录
- **问题描述**: 
  - 相同文件名在不同作用域
  - 可能导致导入混淆
- **建议修复**: 添加作用域前缀或统一命名

#### 27. [源代码] [低] [PgDatabase.close() 验证问题]
- **文件/路径**: `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474`
- **问题描述**: 
  - 有 close() 方法但 shutdown 时可能未调用
- **建议修复**: 验证关闭顺序

#### 28. [源代码] [低] [Core/runtime 是包装价值可疑]
- **文件/路径**: `src/core/runtime/`
- **问题描述**: 
  - 只是重新导出，无实际价值
- **建议修复**: 移除或明确用途

#### 29. [源代码] [低] [yono/index.ts 763行应拆分]
- **文件/路径**: `src/domains/yono/index.ts`
- **问题描述**: 
  - 文件过大难以维护
- **建议修复**: 拆分为多个模块

#### 30. [源代码] [低] [src/index.ts 451行应拆分]
- **文件/路径**: `src/index.ts`
- **问题描述**: 
  - 文件过大
- **建议修复**: 拆分为多个导出模块

#### 31. [源代码] [低] [DomainLifecycleState 重复定义]
- **文件/路径**: 多个文件
- **问题描述**: 
  - 相同状态定义多次出现
  - 不兼容实现
- **建议修复**: 统一状态定义

#### 32. [源代码] [低] [风险评分阈值硬编码]
- **文件/路径**: 风险评估相关文件
- **问题描述**: 
  - 魔法数字无常量对应
- **建议修复**: 提取到配置常量

#### 33. [部署] [低] [deploy/ 目录内容不完整]
- **文件/路径**: `deploy/`
- **问题描述**: 
  - terraform/helm/k8s 配置不完整
- **建议修复**: 完善部署配置

#### 34. [配置] [低] [.gitignore 缺少临时文件模式]
- **文件/路径**: `.gitignore`
- **问题描述**: 
  - 缺少 dist_*, :memory:*, .audit/ 等
- **建议修复**: 补充 .gitignore

#### 35. [测试] [低] [测试辅助代码量大 78 个文件]
- **文件/路径**: `tests/helpers/`
- **问题描述**: 
  - 说明测试基础设施复杂
- **建议修复**: 简化测试基础设施

#### 36. [源代码] [低] [直接访问 process.env 高达143处]
- **文件/路径**: 全局
- **问题描述**: 
  - 配置散落难以追踪
- **建议修复**: 创建统一配置模块

#### 37. [源代码] [低] [HA 和 Lease 模块职责重叠]
- **文件/路径**: `src/platform/five-plane-execution/ha/` 和 `lease/`
- **问题描述**: 
  - 职责边界不清晰
- **建议修复**: 明确职责边界

#### 38. [源代码] [低] [EventEmitter 和 TypedEventBus 混用]
- **文件/路径**: 多个模块
- **问题描述**: 
  - 应统一使用 TypedEventBus
- **建议修复**: 制定迁移计划

#### 39. [源代码] [低] [.claude/scheduled_tasks.json 未忽略]
- **文件/路径**: `.claude/scheduled_tasks.json`
- **问题描述**: 
  - 本地文件不应提交
- **建议修复**: 添加到 .gitignore

#### 40. [配置] [低] [tsconfig.temp.json 可能未使用]
- **文件/路径**: `tsconfig.temp.json`
- **问题描述**: 
  - 临时配置文件残留
- **建议修复**: 清理

#### 41. [配置] [低] [.env.example 包含过时变量]
- **文件/路径**: `.env.example`
- **问题描述**: 
  - 347行可能包含未使用变量
- **建议修复**: 审查清理

#### 42. [文档] [低] [docs_zh/architecture/ 可能未更新]
- **文件/路径**: `docs_zh/architecture/`
- **问题描述**: 
  - 文档与代码可能不同步
- **建议修复**: 建立同步机制

#### 43. [UI] [低] [SharedWorkerWSClient 内存泄漏]
- **文件/路径**: UI 组件
- **问题描述**: 
  - SharedWorker 连接可能未正确关闭
- **建议修复**: 添加清理逻辑

#### 44. [UI] [低] [XSS 潜在风险]
- **文件/路径**: UI 组件
- **问题描述**: 
  - 用户输入可能未正确转义
- **建议修复**: 实现输出转义

#### 45. [UI] [低] [缺少 Error Boundaries]
- **文件/路径**: UI 组件
- **问题描述**: 
  - React 组件缺少错误边界
  - 可能导致白屏
- **建议修复**: 添加 Error Boundaries

#### 46. [测试] [低] [execution-dispatch-service-async.test.ts 仍然失败]
- **文件/路径**: `tests/unit/platform/five-plane-execution/dispatcher/`
- **问题描述**: 
  - 测试持续失败
- **建议修复**: 分析修复

#### 47. [测试] [低] [nodeRunId-canonization.test.ts 仍然失败]
- **文件/路径**: `tests/unit/platform/five-plane-execution/execution-engine/`
- **问题描述**: 
  - 测试持续失败
- **建议修复**: 分析修复

#### 48. [测试] [低] [runtime-plan-executor.test.ts 仍然失败]
- **文件/路径**: `tests/unit/platform/five-plane-execution/oapeflir/`
- **问题描述**: 
  - 测试持续失败
- **建议修复**: 分析修复

#### 49. [测试] [低] [worker-pool-comprehensive.test.ts 仍然失败]
- **文件/路径**: `tests/unit/platform/five-plane-execution/worker-pool/`
- **问题描述**: 
  - 测试持续失败
- **建议修复**: 分析修复

#### 50. [源代码] [低] [symbolic links 未正确处理]
- **文件/路径**: `src/platform/` 下的符号链接
- **问题描述**: 
  - control-plane -> five-plane-control-plane 等
  - 可能导致工具路径解析问题
- **建议修复**: 明确符号链接策略

#### 51. [配置] [低] [.DS_Store 文件存在并被追踪]
- **文件/路径**: 根目录
- **问题描述**: 
  - macOS 元数据文件被追踪
- **建议修复**: 停止追踪并添加到 .gitignore

#### 52. [配置] [低] [:memory: 文件残留]
- **文件/路径**: 根目录
- **问题描述**: 
  - 临时文件未清理
- **建议修复**: 清理

#### 53. [配置] [低] [.tmp/ 目录大量临时文件]
- **文件/路径**: `.tmp/`
- **问题描述**: 
  - 临时文件未清理
- **建议修复**: 清理

#### 54. [配置] [低] [.test-db/ 目录存在]
- **文件/路径**: `.test-db/`
- **问题描述**: 
  - 测试数据库目录残留
- **建议修复**: 清理

#### 55. [源代码] [低] [巨型源文件未拆分]
- **文件/路径**: 多个超过1000行的文件
- **问题描述**: 
  - 难以维护和理解
- **建议修复**: 拆分

#### 56. [源代码] [低] [runMultiStepOrchestration 复杂度高]
- **文件/路径**: `src/platform/five-plane-execution/execution-engine/multi-step-orchestration.ts`
- **问题描述**: 
  - 核心编排逻辑复杂
- **建议修复**: 拆分职责

#### 57. [安全] [低] [npm audit 显示 7 个漏洞]
- **文件/路径**: 依赖
- **问题描述**: 
  - 1 moderate, 6 high
- **建议修复**: 更新受影响包

### 总结

本次深度Review（第十七轮 - 2026-05-14）发现了57个新问题。

**高优先级 (需要立即处理)**:
1. Benchmark 百分位计算完全错误 - 所有 p50/p95/p99 数据不可信
2. Plugin runtime child process 事件监听器永不清洗 - 内存泄漏
3. OAuth token 明文存储 - 安全风险
4. 硬编码 CVE 绕过列表 - 虚假安全感
5. 弱 RSA 2048位密钥 - 不符合现代安全标准
6. process.chdir() 无验证 - 安全漏洞
7. EventListener 167个仅16个清理 - 内存泄漏
8. HarnessRun 未持久化 - 数据丢失风险
9. budget-allocator 竞态条件 - 双重扣款风险

**中优先级**:
1. 59处 console.* 未用 StructuredLogger
2. 168处 : any 类型
3. 30+处 as unknown as 双转型
4. 38处 @ts-ignore
5. 巨型 barrel files
6. HR角色检测硬编码
7. 无 invariant 机制
8. 内存会话存储限制扩展
9. 内存服务身份存储限制扩展
10. 配置缺少安全字段
11. OpenAPI 端点无认证
12. Math.random() 非确定性测试

**低优先级**:
1-45. (详见上述列表)

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 78+9=87 | 93+3=96 | 42+15=57 | 240 |
| 测试 | 10+2=12 | 17+1=18 | 6+6=12 | 42 |
| 配置 | 10+1=11 | 39+1=40 | 24+15=39 | 90 |
| 安全 | 15+5=20 | 17+1=18 | 3 | 41 |
| 文档 | 2 | 8 | 3 | 13 |
| UI | 0 | 4 | 4 | 8 |
| 部署 | 1 | 12 | 6 | 19 |
| **合计** | **133** | **206** | **124** | **463** |

### 优先修复建议

**立即处理 (本周内)**:
1. 修复 benchmark-collector.ts 百分位计算
2. 清理 plugin-runtime-host.ts 事件监听器
3. 实现 OAuth token 安全存储
4. 删除硬编码 CVE 绕过或重新设计
5. 修复 process.chdir() 安全漏洞
6. 添加 HarnessRun 持久化
7. 修复 budget-allocator 竞态条件

**短期内处理 (本月内)**:
1. 减少 : any 和 @ts-ignore 使用
2. 统一日志系统 (StructuredLogger)
3. 拆分巨型文件
4. 实现 invariant 框架
5. 添加 fetch 超时

**长期规划**:
1. 重构 God Objects (HarnessRuntimeService 等)
2. 建立架构边界和接口
3. 实现内存安全验证
4. 建立代码质量门禁
5. 完善文档和培训

### 已知测试失败 (14 个文件，1620 个测试)

详情见 `.audit/quality.md`

*Review generated: 2026-05-14*

---

## [2026-05-14] 自动Review报告（第十八轮 - 全量深度Review）

### Review覆盖范围
7个并行代理完成深度review:
- src/platform/ (5层架构)
- src/domains/ (40+领域)
- src/sdk/ 和 CLI
- tests/unit/ (关键测试文件)
- config/ 和 deploy/
- docs_zh/ 和 ui/
- src/interaction/, org-governance/, ops-maturity/, scale-ecosystem/, core/

---

### 发现的新问题

#### A. src/platform/ 发现 (18个问题)

#### A1. [Orchestration] [高] `harness/index.ts` 2317行 - God Object
- **文件**: `src/platform/five-plane-orchestration/harness/index.ts:1-2317`
- **问题**: 单文件2317行违反单一职责原则，包含HarnessService、loop controllers、guardrails、evaluation logic
- **代码片段**: Line 1423: `// @ts-ignore - appendEvidenceRecord may not exist on RuntimeRepository`
- **建议**: 拆分为 `harness-run-controller.ts`, `harness-loop-controller.ts`, `harness-guardrails.ts`, `harness-evaluation.ts`

#### A2. [Execution] [高] `durable-event-bus.ts` Map/Set积累无清理
- **文件**: `src/platform/five-plane-state-evidence/events/durable-event-bus.ts:208-227`
- **问题**: `subscribers`, `pollingTimers`, `pendingPartitionEvents` 等Map创建后无cleanup
- **代码片段**: `private readonly disposed = false; // never checked`
- **建议**: 添加 dispose() 方法取消所有 polling timers 并清空 maps

#### A3. [State-Evidence] [高] `as unknown as T` 模式滥用
- **文件**: `src/platform/five-plane-state-evidence/truth/sqlite/query-helper.ts:28,41,53,79`
- **问题**: 到处使用双转型掩盖类型问题
- **建议**: 创建强类型包装接口代替类型转换

#### A4. [Execution] [中] `execution-dispatch-service.ts` 1067行
- **文件**: `src/platform/five-plane-execution/dispatcher/execution-dispatch-service.ts`
- **问题**: 大型dispatch service，违反单一职责
- **建议**: 拆分为 `execution-dispatch-core.ts` 和 `execution-dispatch-health.ts`

#### A5. [Control-Plane] [中] `startup-env-schema.ts` 硬 `process.exit(1)`
- **文件**: `src/platform/five-plane-control-plane/config-center/startup-env-schema.ts:506`
- **问题**: 验证失败后直接 `process.exit(1)` - 反模式
- **建议**: 抛出错误让调用者决定退出码

#### A6. [Execution] [中] `process-error-handlers.ts` 60秒超时后 exit
- **文件**: `src/platform/five-plane-execution/startup/process-error-handlers.ts:105,169`
- **问题**: `process.exit(1)` fallback 可能合理但需文档化
- **建议**: 文档化为何需要60秒

#### A7. [Control-Plane] [中] `secret-management-service.ts` setInterval无清理
- **文件**: `src/platform/five-plane-control-plane/iam/secret-management-service.ts:663`
- **问题**: rotationInterval 无 clearInterval 路径
- **建议**: 确保 interval 在 service dispose 时清除

#### A8. [Interface] [中] 多处 setInterval 无 visible cleanup
- **文件**: `task-websocket-status-relay.ts:37`, `websocket-bridge.ts:109`, `channel-gateway-retry-executor.ts:76`
- **建议**: 验证所有 interval 在 shutdown 路径有 clearInterval

#### A9. [Orchestration] [低] TODO R4-27 - HarnessRun未持久化
- **文件**: `src/platform/five-plane-execution/execution-engine/single-task-happy-path.ts:270`
- **问题**: `// TODO R4-27 [ARCHITECTURE]: HarnessRun must be persisted`
- **建议**: 实现或创建 tracked issue

#### A10. [State-Evidence] [中] `partitionSequenceNumbers` 模块级可变状态
- **文件**: `src/platform/five-plane-state-evidence/events/durable-event-bus.ts:55`
- **问题**: `const partitionSequenceNumbers = new Map<string, number>()` 跨实例共享
- **建议**: 改为实例级或使用并发原语

#### A11. [Orchestration] [高] `_placeholder: true` 模式
- **文件**: `src/platform/five-plane-orchestration/harness/runtime/intake-admission-service.ts:437,453,457,470`
- **问题**: 使用 `{ _placeholder: true } as unknown as ConfirmedTaskSpec`
- **建议**: 重设计类型需求

#### A12. [Shared] [中] `slo-alerting-service.ts` 1270行
- **文件**: `src/platform/shared/observability/slo-alerting-service.ts`
- **建议**: 提取 alert dispatch 和 SLO calculation logic

#### A13. [State-Evidence] [中] `event-registry.ts` 1077行
- **文件**: `src/platform/five-plane-state-evidence/events/event-registry.ts`
- **问题**: RAW_EVENT_SCHEMA_REGISTRY 常量包含所有 event schemas
- **建议**: 按域组织到独立配置文件

#### A14. [Control-Plane] [中] `approval-flow-engine.ts` 1031行
- **文件**: `src/platform/five-plane-control-plane/approval-center/approval-flow-engine.ts`
- **建议**: 提取 quorum calculator 和 escalation manager

#### A15. [Execution] [低] `GracefulShutdown` 正确清理 listeners
- **文件**: `src/platform/five-plane-execution/startup/graceful-shutdown.ts:117-121`
- **问题**: 无 - 此文件正确清理 listeners
- **建议**: 这是良好模式应推广

#### A16. [Interface] [低] `sanitize.ts` 有 prototype pollution 防护
- **文件**: `src/platform/five-plane-interface/api/middleware/sanitize.ts:3`
- **问题**: 无 - 正确阻止 `__proto__`, `prototype`, `constructor`
- **建议**: 确保应用于所有 JSON 入口点

---

#### B. src/domains/ 发现 (8个问题)

#### B1. [DomainLifecycleState] [严重] 类型定义不一致
- **文件**: `src/domains/architecture-remediation.ts:1` vs `src/domains/domain-specs.ts:115`
- **问题**: 两套完全不同的值:
  - architecture-remediation.ts: `"Draft" | "Validated" | "Registered" | "Active" | "Updating" | "Deprecated" | "Archived"` (PascalCase)
  - domain-specs.ts: `"validating" | "certified" | "canary" | "active" | "deprecated" | "retired"` (snake_case)
- **建议**: 统一使用 domain-specs.ts 的定义（经过Zod验证）

#### B2. [Plugin Runtime] [严重] sandboxRoot 未验证用于 process.chdir()
- **文件**: `src/domains/registry/plugin-runtime-child.ts:89-93`
- **问题**: `AA_PLUGIN_SANDBOX_ROOT` 直接用于 `process.chdir()` 无路径验证
- **代码片段**:
```typescript
const sandboxRoot = process.env.AA_PLUGIN_SANDBOX_ROOT?.trim();
if (sandboxRoot) {
  process.chdir(sandboxRoot);  // 无验证!
}
```
- **建议**: 使用 `checkSandboxPath()` 验证

#### B3. [Plugin Runtime] [严重] child process 事件监听器未清理
- **文件**: `src/domains/registry/plugin-runtime-host.ts:274-280`
- **问题**: `stderr.on`, `stdout.on`, `message` 监听器在 stop() 时未移除
- **建议**: 在 stop() 中调用 `removeAllListeners()`

#### B4. [Plugin Runtime] [中] process.exit(0) 在库代码中
- **文件**: `src/domains/registry/plugin-runtime-child.ts:175`
- **问题**: 子进程强行 `process.exit(0)` 不给宿主清理机会
- **建议**: 发送退出信号给父进程决定

#### B5. [Plugin Runtime] [中] getProcessTracker().register 后无 unregister
- **文件**: `src/domains/registry/plugin-runtime-host.ts:152`
- **建议**: 在 stop() 中添加 unregister

#### B6. [大型文件] [中] 超大型文件需要拆分
- **文件**:
  - `plugin-spi-registry.ts`: 948 行
  - `plugin-runtime-host.ts`: 784 行
  - `division-loader.ts`: 818 行
  - `yono/index.ts`: 763 行
- **建议**: 按职责拆分

#### B7. [Event Listeners] [严重] 全局无 removeListener/off 调用
- **文件**: 整个 `src/domains/` 目录
- **问题**: `.on()`, `.once()`, `addEventListener()` 32处但 remove 调用为0
- **建议**: 确保在销毁时调用移除方法

#### B8. [DomainDescriptorOrchestrationService] [中] normalizeLifecycleState 映射不完整
- **文件**: `src/domains/domain-descriptor-orchestration-service.ts:144-156`
- **问题**: 映射表是单向的，状态回溯可能不一致
- **建议**: 使用统一的 DomainLifecycleState 类型

---

#### C. src/sdk/ 和 CLI 发现 (9个问题)

#### C1. [安全] [严重] 硬编码 CVE 绕过 SBOM 扫描
- **文件**: `src/sdk/plugin-sdk/plugin-definition.ts:466-475`
- **问题**: `inferRemoteSbomPackages()` 返回硬编码漏洞版本而非实际解析SBOM
- **代码片段**:
```typescript
if (lowerPath.includes("lodash")) {
  return [{ name: "lodash", version: "4.17.21" }];  // vulnerable!
}
```
- **建议**: 删除此函数，始终获取/解析实际 SBOM 文件

#### C2. [安全] [中] 弱 RSA 2048位密钥
- **文件**: `src/sdk/pack-sdk/pack-manifest.ts:232-233`
- **问题**: `modulusLength: 2048` 是最低阈值
- **建议**: 使用 4096位 RSA 或 Ed25519/Ed448

#### C3. [安全] [严重] OAuth tokens 明文存储
- **文件**: `src/sdk/cli/login.ts:70-79`
- **问题**: tokens 以 JSON 明文存储到文件
- **建议**: 使用系统 keychain 或加密存储

#### C4. [测试] [中] Math.random() 导致非确定性
- **文件**: `src/sdk/fixture-redact.ts:174`
- **问题**: `generateTestId()` 使用 Math.random()
- **建议**: 使用 `crypto.randomUUID()`

#### C5. [测试] [中] Math.random() 用于错误注入
- **文件**: `src/sdk/pack-sdk/pack-test-local-service.ts:164`
- **问题**: `playbackFixture()` 用 Math.random() 模拟错误率
- **建议**: 使用 seeded PRNG

#### C6. [CLI] [中] 手动参数解析无验证
- **文件**: `src/sdk/cli/pack-validate.ts:19-36`, `pack-create.ts:23-57`, `pack-publish.ts:22-46`
- **问题**: 手动 `process.argv` 解析，缺少 schema 验证
- **建议**: 使用 `parseArgs` 或 yargs/commander

#### C7. [类型安全] [中] @ts-ignore 滥用
- **文件**: `src/sdk/harness-sdk/index.ts:477,490,576,596,620,643`
- **问题**: 6个 @ts-ignore 隐藏类型错误
- **建议**: 定义 proper discriminated union types

#### C8. [类型安全] [中] any 类型滥用
- **文件**: `src/sdk/cli/stable-runner-factory.ts:62-72`
- **问题**: `StableRunner`, `StableReportWriter`, `FailedPredicate` 使用 `any`
- **建议**: 使用泛型 `<T, R>`

#### C9. [安全] [低] 不完整的漏洞数据库
- **文件**: `src/sdk/plugin-sdk/plugin-definition.ts:133-148`
- **问题**: `KNOWN_VULNERABILITIES` 只列出2个CVE (2021年)
- **建议**: 集成 OSV/NVD 或删除不完整的 allowlist

---

#### D. tests/unit/ 发现 (10个问题)

#### D1. [测试] [严重] budget-allocator.test.ts 缺少 throttle ratio 断言
- **文件**: `tests/unit/platform/five-plane-execution/budget-allocator.test.ts:332-335`
- **问题**: 计算了 throttled 结果但从未 assert
- **建议**: 添加 `assert.equal(result.reservation.effectiveAmount, 50)`

#### D2. [测试] [中] durable-event-bus-async.test.ts setTimeout 时序问题
- **文件**: `tests/unit/platform/five-plane-state-evidence/events/durable-event-bus-async.test.ts:74,104,117`
- **问题**: `setTimeout(resolve, 20)` 在负载高时可能不足
- **建议**: 使用事件监听或轮询模式

#### D3. [测试] [中] pendingForConsumer 返回值未验证
- **文件**: `tests/unit/platform/five-plane-state-evidence/events/durable-event-bus-async.test.ts:174-176`
- **问题**: `pending` 变量无 assert
- **建议**: 添加 `assert.ok(pending.length > 0)`

#### D4. [类型安全] [低] 25+ 测试文件使用 @ts-nocheck
- **文件**: `tests/unit/domains/recipes/recipe-registry.test.ts:11` 等
- **问题**: 禁用类型检查阻碍 TypeScript 安全收益
- **建议**: 逐文件修复或创建 tracking issue

#### D5. [测试] [中] 30+ 处使用 Math.random() 生成测试 ID
- **文件**: `tests/unit/org-governance/approval-routing-service-extended.test.ts:32` 等
- **问题**: 非确定性使测试难以重现
- **建议**: 使用确定性 ID 生成

#### D6. [资源] [中] 部分测试缺少 bus.dispose() 调用
- **文件**: `tests/unit/platform/five-plane-state-evidence/events/durable-event-bus-async.test.ts`
- **问题**: publish/subscribe/pendingForConsumer 测试只调用 db.close() 但未 dispose bus
- **建议**: 在 db.close() 前添加 `bus.dispose()`

#### D7. [代码规范] [低] cleanupPath 与 db.close 顺序不一致
- **文件**: `tests/unit/core/events/memory-leak.test.ts:22-24` vs durable-event-bus-async.test.ts
- **建议**: 建立统一的清理顺序标准

#### D8. [测试] [低] @ts-ignore 绕过类型检查
- **文件**: `tests/unit/platform/shared/observability/structured-logger-edge-cases.test.ts:250` 等
- **建议**: 使用 `// @ts-expect-error` 并添加说明

#### D9. [测试] [中] plugin-spi-registry-invocation.test.ts 多处短时延
- **文件**: `tests/unit/domains/registry/plugin-spi-registry-invocation.test.ts:40,91,94,150,327,800,834`
- **问题**: 5-25ms setTimeout 在慢速 CI 易失败
- **建议**: 增加至 100ms+ 或使用轮询

#### D10. [测试] [低] deliverPending 测试注释与实现不符
- **文件**: `tests/unit/platform/five-plane-state-evidence/events/durable-event-bus-async.test.ts:233-234`
- **问题**: 注释暗示验证投递行为但实际只验证返回值类型
- **建议**: 添加断言验证 delivered.length

---

#### E. config/ 和 deploy/ 发现 (11个问题)

#### E1. [配置] [严重] config/security/ 缺少安全字段
- **文件**: `config/security/*.json`
- **问题**: 只有 `approvalMode`，缺少 `sandboxMode` 和 `remoteWorkerRegistration`
- **建议**: 补充缺失的安全配置字段

#### E2. [.gitignore] [中] 缺少 .audit/ 目录模式
- **文件**: `.gitignore`
- **问题**: `.audit/` 存在但未排除
- **建议**: 添加 `.audit/`

#### E3. [.gitignore] [中] 缺少 dist_* 和 :memory:* 模式
- **文件**: `.gitignore`
- **问题**: 缺少 `dist_*/`, `:memory:*`, `dist-test/`
- **建议**: 补充

#### E4. [Terraform] [中] orphaned 空 EKS cluster resource
- **文件**: `deploy/terraform/modules/eks/main.tf:157-160`
- **问题**: `count = 0` 的死代码
- **建议**: 删除 orphaned resource block

#### E5. [Terraform] [中] 硬编码 VPC CIDR
- **文件**: `deploy/terraform/modules/rds/main.tf:80`, `elasticache/main.tf:62`
- **问题**: `cidr_blocks = ["10.0.0.0/16"]` 硬编码
- **建议**: 添加 `vpc_cidr` 变量

#### E6. [Terraform] [低] backend region 不匹配
- **文件**: `deploy/terraform/main.tf:14`
- **问题**: S3 backend region `ap-southeast-1` vs provider `us-east-1`
- **建议**: 使 backend region 可配置

#### E7. [Helm] [中] pre-prod 使用 production NODE_ENV
- **文件**: `deploy/helm/automatic-agent/values-pre-prod.yaml:43`
- **问题**: `NODE_ENV: "production"` 应为 `"pre-production"`
- **建议**: 修正

#### E8. [部署] [中] 缺少 kubernetes/ 目录
- **文件**: `deploy/`
- **问题**: 无 `/deploy/kubernetes/manifests/`
- **建议**: 添加 K8s YAML 模板或文档说明

#### E9. [ECR] [低] repository name 无环境后缀
- **文件**: `deploy/terraform/modules/ecr/main.tf:26`
- **问题**: 所有环境共享同一 ECR repository
- **建议**: 添加 environment suffix

#### E10. [配置] [低] 环境间 storage driver 不一致
- **文件**: `config/environments/*.json`
- **问题**: dev/staging/test 引用 `config/runtime/default.json`
- **建议**: 确保每个环境有适当的 storage 配置

#### E11. [Config] [低] tsconfig.temp.json 命名误导
- **文件**: `tsconfig.temp.json`
- **问题**: "temp" 暗示临时但实际被使用
- **建议**: 重命名为 `tsconfig.build-test.json`

---

#### F. docs_zh/ 和 ui/ 发现 (8个问题)

#### F1. [UI] [严重] SharedWorkerWSClient 内存泄漏
- **文件**: `ui/packages/shared/api-client/src/ws-client.ts:359`
- **问题**: `message` 事件监听器在 disconnect() 时未移除
- **建议**: 在 disconnect() 中添加 `removeEventListener` 并调用 `port.close()`

#### F2. [UI] [中] 缺少全局 Error Boundary
- **文件**: `ui/apps/web/src/main.tsx`
- **问题**: 只有 FeatureErrorBoundary 包装单个组件，无全局保护
- **建议**: 在 App 组件外添加全局 Error Boundary

#### F3. [文档] [严重] 架构文档版本不一致
- **文件**: `docs_zh/architecture/00-platform-architecture.md` (v4.3) vs `02-code-architecture-reference.md` (v13.0)
- **问题**: 主要文档彼此引用过时版本
- **建议**: 统一版本编号策略

#### F4. [文档] [中] 缺少 API SDK 文档
- **文件**: `docs_zh/reference/` 目录
- **问题**: WSClient、RESTClient、interceptors 无 API 文档
- **建议**: 新增 `api-client.md`

#### F5. [文档] [低] Contract 文档与实现不同步
- **文件**: `docs_zh/contracts/` 多个 .md 文件
- **问题**: 文档声称 Implemented 但代码可能已演进
- **建议**: 建立 contract 测试自动生成验证报告

#### F6. [UI] [低] replayBufferByChannel 可能无限增长
- **文件**: `ui/packages/shared/api-client/src/ws-client.ts:413,325`
- **问题**: SharedWorkerWSClient 和 BrowserWSClient 的 replay buffer 只在连接时清理
- **建议**: 在 disconnect() 时清理 replay buffer

#### F7. [UI] [低] XSS 风险未发现
- **文件**: UI 源码
- **问题**: 无 `dangerouslySetInnerHTML`、`innerHTML` 直接赋值或 `eval`
- **建议**: 保持当前模式

#### F8. [文档] [低] src/core/ 未标注为 Legacy
- **文件**: `docs_zh/architecture/01-code-structure.md`
- **问题**: 文档未说明 `src/core/` 是 Legacy 兼容层
- **建议**: 标注为 Legacy

---

#### G. src/interaction/, org-governance/, ops-maturity/, scale-ecosystem/, core/ 发现 (10个问题)

#### G1. [ops-maturity] [中] Benchmark percentile 计算错误
- **文件**: `src/ops-maturity/drift-detection/learning/benchmark-runner.ts:211-216`
- **问题**: `successRateBefore` 是加权平均而非 proper percentile
- **建议**: 如有 individual samples，排序后取百分位

#### G2. [interaction] [中] `nl-gateway/index.ts` 1669行需要拆分
- **文件**: `src/interaction/nl-gateway/index.ts`
- **问题**: 包含 NL gateway、ambiguity handling、slot resolution、disambiguation、intent parsing
- **建议**: 拆分为 NlGatewayService、IntentParserService、SlotResolverService 等

#### G3. [interaction] [中] `workflow-builder-service.ts` 710行
- **文件**: `src/interaction/ux/workflow-builder-service.ts`
- **建议**: 提取 WorkflowStepBuilder、WorkflowValidator、ExecutionTracker

#### G4. [scale-ecosystem] [中] `tenant-platform-service.ts` 1231行
- **文件**: `src/scale-ecosystem/tenant-platform/tenant-platform-service.ts`
- **建议**: 拆分为 TenantLifecycleService、TenantTopologyService、ComplianceProgramService、HaProgramService

#### G5. [ops-maturity] [低] console.log 代替 StructuredLogger
- **文件**: `src/ops-maturity/chaos/chaos-experiment-scheduler.ts:262,595,598,601,604`
- **问题**: 多处 `console.log` 用于 chaos 实验事件
- **建议**: 替换为 StructuredLogger

#### G6. [scale-ecosystem] [低] TrustLevel 枚举重复定义
- **文件**: `src/scale-ecosystem/federation/federation-gateway.ts:31` 和 `trust-relationship.ts:71`
- **问题**: 两处定义相同的 `TrustLevel` 枚举
- **建议**: 提取到 shared types file

#### G7. [interaction] [低] replayBuffer 可能导致 GC pressure
- **文件**: `src/interaction/dashboard/dashboard-websocket-server.ts:100,450-455`
- **问题**: `shift()` 在高频时可能低效
- **建议**: 考虑 ring buffer 或 Deque 实现

#### G8. [interaction] [低] R4-38 修复需验证覆盖
- **文件**: `src/interaction/dashboard/dashboard-websocket-server.ts:144-145,158`
- **问题**: 注释表明过去有未授权访问漏洞
- **建议**: 验证此修复有 integration tests 覆盖

#### G9. [ops-maturity] [低] p50/p99 latency 计算使用估算
- **文件**: `src/interaction/dashboard/health-scorer/index.ts:55-56`
- **问题**: 基于 queue depth heuristics 而非实际测量
- **建议**: 文档化为估算模式

#### G10. [ops-maturity] [低] setInterval 中 async void
- **文件**: `src/ops-maturity/chaos/chaos-experiment-scheduler.ts:866`
- **问题**: async callback 中未捕获的 rejection 会终止进程
- **建议**: 用 try/catch 包装 evaluator()

---

### 总结

本次全量Review（第十八轮 - 2026-05-14）通过7个并行代理发现了**73个新问题**。

**高优先级 (需要立即处理)**:
1. SharedWorkerWSClient 内存泄漏 - 事件监听器未清理
2. DomainLifecycleState 类型定义不一致 - 两套不同值
3. plugin-runtime-child.ts sandboxRoot 无验证 - process.chdir() 安全漏洞
4. plugin-runtime-host.ts child 事件监听器未清理 - 内存泄漏
5. OAuth tokens 明文存储 - 安全风险
6. 硬编码 CVE 绕过 SBOM 验证 - 虚假安全感
7. config/security/ 缺少 sandboxMode/remoteWorkerRegistration
8. budget-allocator.test.ts 缺少 throttle ratio 断言
9. harness/index.ts 2317行 God Object
10. durable-event-bus.ts Map/Set 积累无清理

**中优先级**:
1. 8个超大型文件需要拆分 (700-2317行)
2. 30+ Math.random() 导致非确定性测试
3. 25+ 测试文件使用 @ts-nocheck
4. console.log 代替 StructuredLogger (5处)
5. setInterval 无 clearInterval (6+处)
6. as unknown as 双转型滥用 (25+处)
7. @ts-ignore 滥用 (21+处)
8. process.exit() 反模式 (4处)
9. CLI 参数解析无验证
10. 架构文档版本不一致

**低优先级**:
1. .gitignore 缺少 .audit/, dist_*, :memory:* 等
2. Terraform orphaned EKS resource
3. pre-prod NODE_ENV 设为 production
4. TrustLevel 枚举重复定义
5. ECR repository name 无环境后缀
6. 缺少 kubernetes/ 目录
7. tsconfig.temp.json 命名误导
8. replayBuffer 可能 GC pressure
9. XSS 防护良好保持
10. src/core/ 未标注为 Legacy

### 问题统计（累计）

| 类别 | 高严重 | 中严重 | 低严重 | 合计 |
|------|--------|--------|--------|------|
| 源代码 | 87+10=97 | 96+8=104 | 57+10=67 | 268 |
| 测试 | 12+1=13 | 18+2=20 | 12+1=13 | 46 |
| 配置 | 11+1=12 | 40+1=41 | 39+4=43 | 96 |
| 安全 | 20+2=22 | 18+1=19 | 3 | 44 |
| 文档 | 2+1=3 | 8+1=9 | 3+1=4 | 16 |
| UI | 0+1=1 | 4+1=5 | 4+1=5 | 11 |
| 部署 | 1 | 12+1=13 | 6+1=7 | 21 |
| **合计** | **146** | **211** | **139** | **496** |

### 优先修复建议

**立即处理 (本周内)**:
1. 修复 SharedWorkerWSClient 内存泄漏 (添加 removeEventListener)
2. 统一 DomainLifecycleState 定义 (保留 domain-specs.ts)
3. 修复 plugin-runtime-child.ts sandboxRoot 验证
4. 清理 plugin-runtime-host.ts 事件监听器
5. 实现 OAuth token 安全存储 (keychain 或加密)
6. 删除硬编码 CVE 绕过 (plugin-definition.ts)
7. 添加 sandboxMode/remoteWorkerRegistration 到 config/security/

**短期内处理 (本月内)**:
1. 拆分 harness/index.ts (2317行) 和其他 8 个超大型文件
2. 修复 budget-allocator.test.ts throttle ratio 断言
3. 消除 Math.random() 非确定性 (使用 crypto.randomUUID)
4. 移除 25+ 测试文件的 @ts-nocheck
5. 统一 console.log → StructuredLogger
6. 添加所有 setInterval 的 clearInterval 路径
7. 减少 as unknown as 和 @ts-ignore 使用

**长期规划**:
1. 重构所有 God Objects (8个超大型文件)
2. 建立架构边界和接口契约
3. 实现内存安全验证机制
4. 建立代码质量门禁 (类型安全、测试覆盖)
5. 统一版本编号策略 (架构文档)
6. 完善文档和培训

### 已知测试失败 (14 个文件，1620 个测试)

详情见 `.audit/quality.md`

*Review generated: 2026-05-14*
