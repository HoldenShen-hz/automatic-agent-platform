# 2026-04-30 `platform-architecture-implementation-consistency-audit_round` 逐编号重审结果

本文件是对 `docs_zh/reviews/platform-architecture-implementation-consistency-audit_round.md` 的逐编号复核结果。

原则：

- 不直接沿用历史长表结论。
- 只在有真实源码、contract、ADR、配置或定向测试证据时才写“已修复/已解决”。
- 对不改代码的项，明确写出依据；不做假修复。
- 不做全量测试。

规模说明：

- 原始行项目：3490
- 唯一编号：2640
- 重复编号：850
- 数字编号截至 `2604`，另含 `R2-1` 至 `R32-55` 这类轮次编号。
- 本次补录新增唯一编号：1334。

当前已直接实修并定向验证的真实问题：

- `R2-6`：`BudgetGuard` 的 pack/platform 结果类型与 legacy 归一化缺口。
- `R4-37`：`network-egress-policy` 默认 `deny` 模式未真正阻断，以及 `audit_only` 环境模式解析漂移。
- 自治/主动触发 family 的陈旧测试断言：medium risk 旧断言仍要求 `auto_execute`；TrustScorer 旧断言仍把 low-risk `fully_trusted` 固定压成 `semi_auto`。
- `R5-49`：`KnowledgeQueryService` 已有 scope 输入却未把 `accessPrincipal` 绑定进服务层边界校验。
- `R22-31`：`TransactionalEventAppender` 只包 event/outbox，没有 truth mutation hook，无法保证 truth+event 同事务。
- `R22-47`：恢复分类对外仍暴露 `L1/L2/L3`，未与 canonical `transient/permanent/unknown` 对齐；顺带修正了若干真实误分类优先级问题。
- `R5-59`：`ADR-002` 仍残留 `VP 编排层/事业部` 结果表述，和已迁移的 DomainDescriptor/BusinessPack 主线混用。
- `R5-63`：ADR source section 批次里，`ADR-002/004/028` 仍有旧节号或旧叙事残留，需要补齐现行 canonical 引用。
- `2580`：`SqlExecutionMetricsProvider` 虽已把 incident 计数改成按 failed status，但 `lastIncidentAt` 和旧测试仍沿用 `last_error_code` 语义，导致复审依据继续漂移。
- `2575`：`DashboardWebSocketServer` 的 metric 路由此前只有消费端、没有订阅注册端，本轮已把注册/更新/回收链补齐。
- `2568`：`AutonomyGovernanceService` 虽识别 `P0/P1`，但旧 severity 分支丢弃了 `promotion.targetLevel`，高严重度 incident 实际不会触发建议降级；本轮已修复。
- `2585`：`addWorkspaceMembership()` 旧“修复”没有真实 caller context，只是回读 owner membership；本轮已把 `callerUserId` 接进服务/CLI 边界并做真实授权。
- `2586`：`addOrganizationMembership()` 旧实现既无 caller principal，又依赖不存在的 `organization.ownerId`；本轮改为 `createOrganization()` 显式建立 owner membership，再用 `callerUserId` 做成员管理授权。
- `R5-28`：`GoalDecompositionService` 此前只会暴露 capability/permission 越界，但任务 envelope 不会产出按域收窄后的最终权限集；本轮已补上真实的 task-level permission narrowing，并保留失配告警。
- `R14-22`：`authoritative-task-store-decorator` 旧实现虽然已不再使用全局 metrics Map，但同步重试仍靠阻塞式等待兜底；本轮移除了 `SharedArrayBuffer`/阻塞睡眠，改为实例级 metrics + 异步非阻塞 backoff + 同步即时重试。
- `R15-02`：SDK 版本握手测试仍按旧接口把 `initialize()` 当成返回握手结果；本轮回写为显式调用 `performVersionHandshake()`，并据此补强 `R15-01/02/03` 的复核证据。
- `R15-49/50/51/52/53/54/55/58/59/60/61/65/66/67/68/69`：Round 15 的多区域/资源管理/Marketplace/SLA 残余条目此前仍按“本轮未改”保守挂起；本轮通过现行源码复核并补齐 `tests/unit/scale-ecosystem/reaudit-r15-multi-region-resource-manager-regressions.test.ts`，将 fencing epoch、failover reconciliation、写路由、跨境合规链、多维 quota、promotion budget、tenant-scoped pool 等能力统一回写为已解决。
- `2237`：`DurableEventBus.ensurePendingAcksForActiveConsumers()` 之前会给未注册到该 eventType 的活跃 consumer 也创建 pending ack，导致虚假积压计数；本轮已按事件注册表过滤。
- `2246`：`EvalRunService` 之前把 `feedbackEnvelope.signals` 直接当 `actualEvidenceRefs`，会把 legacy reason/signal 与 evidence 混算；本轮改为优先读取 step `evidenceRefs`，仅在 legacy run 无 step evidence 时回退到 flat signals。
- `2247`：`governance-projection` 已去掉未知 `decision*` 事件到 `approval_granted` 的错误推断；本轮复验时还发现 projection handler 把内部 `_processedEventIdSet` 直接返回、没有序列化回 `processedEventIds`，导致去重状态无法持久化，这个真实实现问题也一并修复。
- `2249/2250`：`access-model` 现已只信任角色继承得到的 capability，并把 `manualTakeoverActive` 收敛为“仅记录且仅限 operator-grade principal 可用”的上下文标记；本轮同时修复了验证中暴露的 default-allow 回归，恢复“能力和上下文检查通过后允许”的正常路径。
- `2251/2254/2255/2256`：OIDC 路径现已禁止在 `allowMockFallback=false` 时回退 mock user、改用 `timingSafeEqual` 校验 access token、限制单用户并发 session 数并实现 `touchSession()` 滑动过期；本轮补齐了对应定向验证，并顺手修掉了 PKCE helper 的 ESM `require()` 运行时错误。
- `2236/2239/2240`：`FeedbackSignal` 现已强制 `trustScore/evidenceRefs` 与 canonical `harnessRunId/nodeRunId`，`workflow-run-projection` 对 `subtask:failed` 已改为仅记录失败 step、不立即把 workflow 标成失败，且 projection 去重主链已经统一切到内部 `Set` 而非 `Array.includes()`。
- `2257/2258/2259`：`PolicyEngine` 的 `full-auto` 已不再绕过高风险升级；`VaultHttpSecretProvider` 不再把缺失 `lease_duration` 的 AppRole token 默认缓存 1 小时；`DataClassificationService` 已在运行前拒绝不安全 regex，避免用户自定义模式触发 ReDoS。
- `R7-42/R7-43/R7-47` 与 `2292/2293/2295/2296/2298/2300`：HTTP API 主链此前把幂等、版本协商、trace 透传和任务创建 fact append 分散在局部组件里，没有真正接到请求入口；本轮已收敛到 `HttpApiServer` 主链并补上任务创建 truth+event 同事务。
- `2278-2291`：Round 39 的 CLI/SDK 连续条目现已全部重审；其中 `migrate-sqlite-to-pg`、`api-server`、`harness-sdk`、`admin-sdk`、`billing`、`shadow-snapshot`、`data-plane`、`client-sdk` 多数属于 reaudit 结论滞后，`dlq-manager` 与 `login` 则存在真实缺口，本轮已分别补上事务/审计/归档/schema 对齐，以及真正可执行的 PKCE OAuth CLI 入口与 ESM 安全落盘。
- `2302`：`ServiceRegistry` 虽早已有 `createScoped()`，但顶层 bootstrap/runtime catalog/runtime orchestrator/root summary 的默认入口仍隐式绑定 `getInstance()`；本轮已把这些生产装配默认值统一切到 `createScoped()`，只在显式传入 registry 时才共享。
- `2203`：`remote-session-state` 的真实实现早已把 failed 收到 `connected` 信号时收敛到 `connecting`，但 reaudit 条目和对应单测仍停留在旧 bug 叙述；本轮已回写为“必须先重连握手”的现行语义，并修正失效的测试导入路径。
- `2192/2193/2195/2196/2197/2198/2199/2204`：Round 37 的多租户/多区域早期条目里，有一部分其实已被后续实现真实修掉但没有回写，另有 `2194` 仍存在真实定时 flush 旁路，本轮已把定时 flush 收口回 `DataReplicatorService.flush()` 主链，并补齐边界/延迟/容量/队列/序列号方面的定向证据。
- `R11-35`：`prompt-routes` 的 `POST` 路径早已走强类型 schema，但同文件 `PUT /deprecate` 仍残留 `readJsonBody(...) as Record<string, unknown>` 旧分支；本轮已补上独立 deprecation schema，并顺手把 `R11-32/33/34/36` 这组“边界仍是裸 parse/as any”的陈旧结论重新按现状回写。
- `R11-41/R11-44`：本轮继续清理 Round 11 的残余真实类型擦除点，分别去掉了 `UxEventTrackingService` 中对 `elementId/interactionType` 的隐式不安全断言，以及 `stable-runner-factory` 对 runner 签名的 `(runner as any)(runnerArgs as any)` 擦除；同时将 `R11-37/38/39/40/42/43` 按现行源码复核回写为“原指控已过时”。
- `R6-45`：5 份 short contract 文档此前确实缺 remediation / canonical 强制规则，且多份不足 60 行；本轮已直接补齐 `sla_tier`、`quota_preemption`、`multimodal_gateway`、`org_hierarchy`、`feedback_improvement` 的 remediation、导出/兼容约束与 v4.3 强制规则。
- `R14-01`：`platform-panic` 不是“只有 panic_stop 字符串枚举”，但 `cascadeHalt()` 返回旧快照，导致传播结果与内部 halting log 不一致；本轮已修正返回链并补上 cascade/ack/resume 定向验证。
- `R16-42`：`ConflictResolver` 已不是 shallow spread，但数组中同 `id` 冲突项仍会固化 server 旧值；本轮补上 item-level CRDT merge 与 Lamport fallback。
- `R20-03`：`WorkerDrainProtocol` 已有 drain state，却把 checkpoint/lease release/recovery 通路都留成 stub；本轮接上协作依赖并让 `TERMINATE` 相位真实执行 forced handoff。
- `R20-44/45/46/49`：分别补齐了 side-effect 注册/预提交生命周期、workflow 运行前 5 类静态分析、`StaticCompatibilityIssue[]` 兼容导出，以及 pre-release quality gate。
- `R25-03/04/05/06/07/08/10/12/13`：本轮继续清理 Platform Interface / API / Channel Gateway 的真实缺口，分别修复了 gateway target error 归一化顺序、标准化错误响应 `traceId/details`、webhook nonce 熵截断、delivery receipt 虚假 success、gateway rate limit tenant 隔离、WebSocket server heartbeat、WS `maxPayload`、API rate-limit key 路径参数归一化，以及 body read 绕过请求超时保护。
- `R25-01/02/09/11`：这组 reaudit 条目继续沿用了旧快照；本轮补充源码与定向测试后确认，`matchRoute` 已支持 `PUT/PATCH/DELETE`，CORS 默认已 fail-closed 且拒绝 wildcard+credentials，`StreamBridge` replay buffer 早已有限界，`WebSocketBridge` 也已在订阅和广播两侧接入 tenant scope filter。

定向验证：

- `node --test --import tsx tests/unit/platform/control-plane/iam/network-egress-policy.test.ts tests/unit/platform/model-gateway/cost-tracker/budget-guard.test.ts tests/unit/platform/model-gateway/cost-tracker/budget-guard-types.test.ts tests/unit/interaction/autonomy/trust-scorer/index.test.ts tests/unit/interaction/autonomy/promotion-engine/index.test.ts tests/unit/interaction/proactive-agent/trigger-engine/index.test.ts tests/unit/interaction/proactive-agent/trigger-engine/trigger-engine.test.ts tests/unit/interaction/proactive-agent/trigger-engine/resolve-trigger-action-mode.unit.test.ts tests/unit/interaction/proactive-agent/trigger-engine.test.ts tests/unit/platform/interaction/proactive-agent/trigger-engine.test.ts`
- 结果：`152 passed / 0 failed / 0 skipped`
- `node --test --import tsx tests/unit/platform/state-evidence/knowledge/knowledge-query-boundary.test.ts tests/unit/platform/state-evidence/events/transactional-event-appender.test.ts tests/unit/platform/execution/recovery/failure-classification.test.ts`
- 结果：`62 passed / 0 failed / 0 skipped`
- `node --test --import tsx tests/unit/interaction/goal-decomposer/task-domain-capability-policy.test.ts tests/unit/platform/orchestration/harness/evaluation/eval-run-service.test.ts tests/unit/platform/state-evidence/events/durable-event-bus-consumer-filter.test.ts`
- 结果：`10 passed / 0 failed / 0 skipped`
- `node --test --import tsx tests/unit/interaction/goal-decomposer/task-domain-capability-policy.test.ts tests/unit/platform/orchestration/harness/evaluation/eval-run-service.test.ts tests/unit/platform/state-evidence/events/durable-event-bus-consumer-filter.test.ts tests/unit/platform/state-evidence/events/dlq-service.test.ts tests/unit/platform/state-evidence/events/durable-event-bus-no-handler.test.ts tests/unit/platform/state-evidence/events/layered-event-inbox-compaction.test.ts tests/unit/platform/orchestration/oapeflir/assessment-service-extended.test.ts tests/unit/platform/state-evidence/events/projections/governance-projection.test.ts tests/unit/platform/control-plane/iam/access-model-authorization.test.ts tests/unit/platform/control-plane/iam/iam-access-model-extended.test.ts tests/unit/platform/control-plane/iam/field-encryption-extended.test.ts tests/unit/platform/control-plane/iam/secret-management-service.test.ts tests/unit/platform/control-plane/iam/audit-integrity-repository.test.ts tests/unit/org-governance/sso-scim/oidc/oidc-service.test.ts tests/unit/org-governance/sso-scim/scim-sync/scim-service.test.ts tests/unit/org-governance/sso-scim/group-role-mapping-service.test.ts`
- 结果：`395 passed / 0 failed / 0 skipped`
- `node --test --import tsx tests/unit/platform/state-evidence/events/durable-event-bus-no-handler.test.ts tests/unit/platform/state-evidence/events/layered-event-inbox-compaction.test.ts`
- 结果：`3 passed / 0 failed / 0 skipped`
- `node --test --import tsx tests/unit/platform/orchestration/oapeflir/types/feedback-signal.test.ts tests/unit/platform/orchestration/oapeflir/feedback-signal.test.ts tests/unit/platform/state-evidence/events/projections/workflow-run-projection.test.ts tests/unit/platform/state-evidence/events/projections/workflow-run-projection-subtask-failure-regression.test.ts tests/unit/platform/control-plane/iam/policy-engine.test.ts tests/unit/platform/control-plane/iam/policy-engine-edge-cases.test.ts tests/unit/platform/control-plane/iam/vault-http-secret-provider.test.ts tests/unit/platform/control-plane/iam/data-classification-service-regex-safety.test.ts`
- 结果：`149 passed / 0 failed / 0 skipped`
- `node --test --import tsx tests/integration/platform/interface/api/http-api-server-architecture-regressions.test.ts tests/unit/platform/five-plane-interface/api/middleware/idempotency-key.test.ts tests/unit/platform/interface/api/http-server/request-helpers.test.ts`
- 结果：`47 passed / 0 failed / 0 skipped`
- `node --test --import tsx tests/unit/sdk/cli/migrate-sqlite-to-pg.test.ts tests/integration/sdk/migrate-sqlite-to-pg-integration-2278-2279.test.ts tests/unit/sdk/cli/migrate-sqlite-to-pg-2288-transaction.test.ts tests/unit/sdk/cli/api-server-source-2280-2281.test.ts tests/unit/sdk/client-sdk/cursor-2291-schema-validation.test.ts tests/unit/sdk/cli/dlq-manager-operations-2282-2283.test.ts tests/integration/sdk/dlq-manager-architecture-regressions-2282-2283.test.ts`
- 结果：`30 passed / 0 failed / 0 skipped`
- `node --test --import tsx tests/unit/sdk/harness-sdk/deterministic-replay-2284.test.ts tests/unit/sdk/admin-sdk/bulk-ops-2285.test.ts tests/unit/sdk/cli/login-2286.test.ts tests/unit/sdk/cli/source-review-2287-2289-2290.test.ts`
- 结果：`9 passed / 0 failed / 0 skipped`
- `node --test --import tsx tests/unit/platform/orchestration/oapeflir/di-source-review-2294.test.ts tests/unit/platform/state-evidence/events/durable-event-bus-backpressure-2297.test.ts`
- 结果：`2 passed / 0 failed / 0 skipped`
- `./node_modules/.bin/tsx --test tests/unit/platform/shared/service-registry-default-scope-2302.test.ts tests/unit/platform/five-plane-runtime-bootstrap.test.ts tests/unit/platform/five-plane-runtime-orchestrator.test.ts tests/unit/domains-runtime-orchestrator.test.ts tests/unit/interaction-governance-runtime-orchestrator.test.ts tests/unit/scale-ops-runtime-orchestrator.test.ts tests/unit/platform/ai-operations/runtime-orchestrator.test.ts`
- 结果：`65 passed / 0 failed / 0 skipped`
- `./node_modules/.bin/tsx --test tests/unit/scale-ecosystem/multi-region/remote-session-state.test.ts`
- 结果：`10 passed / 0 failed / 0 skipped`
- `./node_modules/.bin/tsx --test tests/unit/scale-ecosystem/multi-region/round37-multi-region-regressions-2193-2198.test.ts tests/unit/scale-ecosystem/multi-region/data-replicator/index.test.ts tests/unit/scale-ecosystem/multi-region/region-health-check-service.test.ts tests/integration/platform/security/data-plane-flow-boundary.test.ts tests/unit/scale-ecosystem/multi-region/remote-session-state.test.ts`
- 结果：`44 passed / 0 failed / 0 skipped`
- `./node_modules/.bin/tsx --test tests/unit/platform/orchestration/reaudit-r23-harness-learn-regressions.test.ts tests/unit/platform/reaudit-r23-security-20-32.test.ts tests/unit/scale-ecosystem/reaudit-r15-multi-region-resource-manager-regressions.test.ts tests/unit/scale-ecosystem/marketplace/plugin-trust-store.test.ts`
- 结果：`38 passed / 0 failed / 0 skipped`
- `./node_modules/.bin/tsx --test tests/unit/platform/reaudit-batch-r24-13-28.test.ts`
- 结果：`9 passed / 0 failed / 0 skipped`
- `./node_modules/.bin/tsx --test tests/unit/platform/reaudit-batch-r24-14-31-60-71.test.ts`
- 结果：`9 passed / 0 failed / 0 skipped`
- `./node_modules/.bin/tsx --test tests/unit/platform/state-evidence/memory/memory-layer-model-functions.test.ts tests/unit/scale-ecosystem/resource-manager/preemption/index.test.ts`
- 结果：`76 passed / 0 failed / 0 skipped`
- `./node_modules/.bin/tsx --test tests/e2e/workflow-state-transitions.test.ts`
- 结果：`6 passed / 0 failed / 0 skipped`
- `./node_modules/.bin/tsx --test --test-name-pattern "persists pending queues and keeps failed batch retryable across restart" tests/unit/scale-ecosystem/multi-region/cdc-replication-service.test.ts`
- 结果：`1 passed / 0 failed / 0 skipped`
- `./node_modules/.bin/tsx --test tests/unit/platform/interface/api/http-server/prompt-routes.test.ts tests/unit/platform/interface/channel-gateway/stream-bridge.test.ts tests/unit/platform/interface/channel-gateway/channel-gateway-delivery-service.test.ts tests/unit/platform/execution/lease/execution-lease-service-async-r9.test.ts tests/unit/platform/five-plane-execution/lease/execution-lease-service-async.test.ts`
- 结果：`63 passed / 0 failed / 0 skipped`
- `./node_modules/.bin/tsx --test tests/unit/sdk/cli/stable-runner-factory.test.ts tests/unit/interaction/ux/ux-event-tracking-service.test.ts tests/unit/interaction/ux/ux-event-tracking-service-edge-cases.test.ts tests/unit/platform/interaction/ux/ux-event-tracking-service.test.ts`
- 结果：`94 passed / 0 failed / 0 skipped`
- `./node_modules/.bin/tsx --test tests/unit/platform/five-plane-execution/worker-drain-protocol.test.ts tests/unit/platform/five-plane-execution/side-effect-manager.test.ts tests/unit/platform/orchestration/oapeflir/workflow/workflow-validator.test.ts tests/unit/platform/prompt-engine/eval/post-execution-quality-gate.test.ts tests/unit/platform/ops-maturity/platform-panic/panic-propagation-service.test.ts`
- 结果：`65 passed / 0 failed / 0 skipped`
- `./node_modules/.bin/tsx --test tests/unit/ui/shared/sync/conflict-resolver.test.ts`
- 结果：`4 passed / 0 failed / 0 skipped`
- `cd ui && ./node_modules/.bin/vitest run tests/shared/reaudit-r24-ui-foundations.test.tsx tests/reaudit-r24-ui-source-review.test.ts tests/tools/tooling.test.ts`
- 结果：`13 passed / 0 failed / 0 skipped`
- `./node_modules/.bin/tsx --test tests/unit/platform/reaudit-batch-r25-01-13.test.ts tests/unit/platform/interface/api/http-server/api-error.test.ts tests/unit/platform/interface/api/http-server/request-helpers.test.ts tests/unit/platform/interface/api/http-server/response-hardening.test.ts tests/unit/platform/interface/channel-gateway/channel-gateway-delivery-service.test.ts tests/unit/platform/interface/channel-gateway/websocket-bridge-coverage.test.ts`
- 结果：`124 passed / 0 failed / 0 skipped`
- `npm run build`
- 结果：通过；同时暴露并收敛了 `stable-gate.ts`、`stable-package.ts` 在 `exactOptionalPropertyTypes` 下把 `undefined` 显式写入可选字段的签名漂移。

## 逐编号结论表（按唯一编号）

| 编号 | 原章节 | 严重度 | 位置 | 原问题 | 当前结论 | 根因 | 依据 | 本轮动作 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| R2-1 | 7. AI 运营层代码 vs 架构（§15-§23） | CRITICAL | `model-gateway/unified-chat-provider.ts` | ChatCompletionRequest 缺 traceId/tenantId/costTag 必填字段，架构 §15.2 明确要求 | 已解决 | 原审计停留在旧版 model gateway 请求与 streaming 接口快照，没有反映后续上下文、预算与审计透传。 | 当前 `src/platform/model-gateway/provider-registry/unified-chat-provider.ts` 已包含 `traceId`、`tenantId`、`costTag`、`abortSignal`、`validatePartialChunk`、`streamingBudgetTrack`、`policyOutcome`，并回填 trace。 | 不改代码，仅回写依据。 |
| R2-2 | 7. AI 运营层代码 vs 架构（§15-§23） | CRITICAL | `model-gateway/unified-chat-provider.ts stream()` | 无 AbortSignal / 增量预算扣减 / partial response validation，架构 §15.4 要求流式预算实时控制 | 已解决 | 原审计停留在旧版 model gateway 请求与 streaming 接口快照，没有反映后续上下文、预算与审计透传。 | 当前 `src/platform/model-gateway/provider-registry/unified-chat-provider.ts` 已包含 `traceId`、`tenantId`、`costTag`、`abortSignal`、`validatePartialChunk`、`streamingBudgetTrack`、`policyOutcome`，并回填 trace。 | 不改代码，仅回写依据。 |
| R2-3 | 7. AI 运营层代码 vs 架构（§15-§23） | CRITICAL | `prompt-engine/prompt-injection-guard.ts` | PromptInjectionDefenseChain 为单层正则，无架构 §20.3 要求的多层链编排器(regex→classifier→LLM judge) | 已解决 | Prompt/Eval 审计基于早期实现，后续多层防护、分级样本门禁和独立 judge 已落地，但历史审计未清退。 | 当前 `src/platform/prompt-engine/prompt-injection-guard.ts` 已非单层正则，`src/platform/prompt-engine/eval/eval-dataset-judge-service.ts` 已实现风险样本下限和 hard gate，`cross-provider-judge-service.ts` 已存在。 | 不改代码，仅回写依据。 |
| R2-4 | 7. AI 运营层代码 vs 架构（§15-§23） | CRITICAL | `prompt-engine/eval/` | EvalDataset 无按风险级别最小样本数校验，架构 §21.5 要求 critical≥200/high≥100/medium≥50 | 已解决 | Prompt/Eval 审计基于早期实现，后续多层防护、分级样本门禁和独立 judge 已落地，但历史审计未清退。 | `src/platform/prompt-engine/eval/eval-dataset-judge-service.ts` 已实现 `EVAL_RISK_LEVEL_MIN_SAMPLES` 常量（critical≥200/high≥100/medium≥50/standard≥20）和 `registerDataset()` 中的硬门禁校验，不符合最小样本数要求的 dataset 注册时会抛 `ValidationError`。 | 不改代码，更新依据列指向。 |
| R2-5 | 7. AI 运营层代码 vs 架构（§15-§23） | CRITICAL | `plugins/builtin-plugin-registry.ts` | 插件系统无 DataTaintPropagation 追踪，架构 §23.4 要求跨插件数据污染标记传递 | 已解决 | 插件治理条目来自中间态快照，registry 与 SDK 后续已补齐污染传播、撤回分级和递归深度追踪。 | 当前 `src/plugins/builtin-plugin-registry.ts` 已有 `DataTaintPropagation` 与 `BundleRevocationSeverity`，`src/sdk/plugin-sdk/plugin-context.ts` 已有 `callDepth/delegationDepth`。 | 不改代码，仅回写依据。 |
| R2-6 | 7. AI 运营层代码 vs 架构（§15-§23） | HIGH | `model-gateway/cost-tracker/budget-guard.ts` | BudgetPolicy 仅支持 task 级预算，缺架构 §18 要求的 platform/pack/step 三级预算层次 | 已解决（源码修复并补定向测试） | 根因分两层：一是旧 `BudgetGuard` 只有 task cost 判断，pack/platform 级联预算与 step constraint 语义不完整；二是后续虽然补出了 `evaluateStepConstraints()`，但没有接到真实 model execution 入口，`maxSteps/maxDurationMs/maxModelTokens` 仍停留在“会计算、不会拦”。 | 当前 `src/platform/model-gateway/cost-tracker/budget-guard.ts` 已统一提供 task/pack/platform/daily/monthly 级联预算与 `evaluateStepConstraints()`；本轮再把 `src/platform/five-plane-execution/execution-engine/model-call-provider.ts` 接上真实前置门禁和超时中止，`createCompletion/createStreamingCompletion` 现在会在执行前拒绝 `maxSteps/maxModelTokens` 越界，并在执行时用 `AbortSignal` + elapsed 校验强制 `maxDurationMs`。定向测试 `tests/unit/platform/execution/execution-engine/model-call-provider.test.ts` 已新增 3 条回归，验证三项约束都会被真实拦截。 | 关闭的是“三级预算与 step/token/duration 约束未真正执行”的缺口，不外推为所有上层产品流都已自定义配置 budget policy。 |
| R2-7 | 7. AI 运营层代码 vs 架构（§15-§23） | HIGH | `model-gateway/cost-tracker/chargeback-service.ts` | ChargebackAllocation 缺 fx_rate/cost_source 字段，架构 §18.7 要求多币种归因 | 已解决 | 旧审计停留在 chargeback contract 初次迁移阶段，没有吸收后续 `fxRateToBase/costSource` 实装和多币种回归测试。 | 当前 `src/platform/model-gateway/cost-tracker/chargeback-service.ts` 已包含 `originalCurrency/baseCurrency/fxRateToBase/costSource`；`tests/unit/platform/model-gateway/cost-tracker/chargeback-service.test.ts` 与 `tests/integration/platform/model-gateway/chargeback-service-integration.test.ts` 已验证 FX 和来源归因。 | 不改生产代码，补跑定向测试并回写依据。 |
| R2-8 | 7. AI 运营层代码 vs 架构（§15-§23） | HIGH | `prompt-engine/registry/` | Prompt lifecycle 缺 deprecated 阶段，架构 §20.6 定义 draft→active→deprecated→archived 四阶段 | 已解决 | Prompt/Eval 审计基于早期实现，后续多层防护、分级样本门禁和独立 judge 已落地，但历史审计未清退。 | 当前 `src/platform/prompt-engine/prompt-injection-guard.ts` 已非单层正则，`src/platform/prompt-engine/eval/eval-dataset-judge-service.ts` 已实现风险样本下限和 hard gate，`cross-provider-judge-service.ts` 已存在。 | 不改代码，仅回写依据。 |
| R2-9 | 7. AI 运营层代码 vs 架构（§15-§23） | HIGH | `plugins/builtin-plugin-registry.ts` | 无 BundleRevocationSeverity 机制，架构 §23.6 要求插件撤回严重度分级 | 已解决 | 插件治理条目来自中间态快照，registry 与 SDK 后续已补齐污染传播、撤回分级和递归深度追踪。 | 当前 `src/plugins/builtin-plugin-registry.ts` 已有 `DataTaintPropagation` 与 `BundleRevocationSeverity`，`src/sdk/plugin-sdk/plugin-context.ts` 已有 `callDepth/delegationDepth`。 | 不改代码，仅回写依据。 |
| R2-10 | 7. AI 运营层代码 vs 架构（§15-§23） | HIGH | `prompt-engine/eval/` | LLM-as-Judge 无按风险级别独立性强制（高风险需外部独立评审），架构 §21.7 明确要求 | 已修复（源码） | 旧审计仅识别历史快照中缺少独立性强制，未反映后续实现状态。本轮补充了 §21.7 要求的强独立性校验：`CiGateOptions` 新增 `enforceIndependenceForHighRisk` 和 `independentJudgeId` 字段，`runCiGate()` 在检测到 critical/high 风险用例时，若未配置独立 judge 则强制失败并设置 `independenceViolationReason`。`eval-dataset-judge-service.ts` 的 `resolveJudge()` 已实现 judge 与 candidate provider 不同家族、不同 provider 的跨实体独立性校验。 | 当前 `src/platform/prompt-engine/eval/llm-eval-service.ts` 的 `CiGateOptions`（167-181 行）新增 `enforceIndependenceForHighRisk` 和 `independentJudgeId`，`runCiGate()`（661-693 行）在检测到 critical/high 风险用例时强制验证独立 judge 配置，无则标记 `independenceViolation` 并令 `passed=false`；`eval-dataset-judge-service.ts` 的 `EvalDatasetEvaluationInput`（143-160 行）新增 `enforceIndependenceForHighRisk`，`evaluateDataset()`（415-420 行）在 flag 为 true 且存在 critical/high 优先级用例但无独立 judge 时追加 `independence_violation:high_risk_evaluation_requires_independent_judge` 到 `blockingFindings`；`resolveJudge()`（435-468 行）已实现 judge 与 candidate provider 不同家族、不同 provider 的跨实体独立性校验；`cross-provider-judge-service.ts` 已实现多 judge 共识评估。 | 已实现 §21.7 独立性强制，补录完整实现状态。 |
| R2-11 | 7. AI 运营层代码 vs 架构（§15-§23） | HIGH | `plugins/ PluginContext` | 无 call_depth/delegation_depth 追踪，架构 §23.2 要求防止插件无限递归委托 | 已解决 | 插件治理条目来自中间态快照，registry 与 SDK 后续已补齐污染传播、撤回分级和递归深度追踪。 | 当前 `src/plugins/builtin-plugin-registry.ts` 已有 `DataTaintPropagation` 与 `BundleRevocationSeverity`，`src/sdk/plugin-sdk/plugin-context.ts` 已有 `callDepth/delegationDepth`。 | 不改代码，仅回写依据。 |
| R2-12 | 7. AI 运营层代码 vs 架构（§15-§23） | HIGH | `prompt-engine/eval/` | §21.5 critical_case_pass==100% 硬门禁已实现：eval-dataset-judge-service.ts 的 requireCriticalPass 策略默认开启，criticalPassRate<1 时 gateDecision 为 hold/rollback；post-execution-quality-gate.ts 的 decide() 在 criticalPassRate<1 时返回 releaseStage:"blocked"。架构合规。 | 已解决 | Prompt/Eval 审计基于早期实现，后续多层防护、分级样本门禁和独立 judge 已落地，但历史审计未清退。 | 当前 `src/platform/prompt-engine/prompt-injection-guard.ts` 已非单层正则，`src/platform/prompt-engine/eval/eval-dataset-judge-service.ts` 已实现风险样本下限和 hard gate，`cross-provider-judge-service.ts` 已存在，`src/platform/prompt-engine/eval/post-execution-quality-gate.ts` 的 `PostExecutionQualityGate.decide()` 在 criticalPassRate<1 时返回 releaseStage:"blocked"。 | 不改代码，issue description 已修正以准确反映实现状态。 |
| R2-13 | 8. 剩余 Contract vs 架构 | HIGH | `runtime_state_machine_contract.md` | §6 ExecutionStatus 8态机与架构 §25.8 NodeRun 14态生命周期冲突，缺 admitted/planning/ready/pausing/replanning/compensating | 已修复 | contract 文档曾沿用 execution/workflow/step/status 等 legacy canonical，后续已按 HarnessRun/NodeRun/PlanGraph 主链回写。 | 当前 `docs_zh/contracts/` 下的 `runtime_state_machine_contract.md`、`cost_and_budget_contract.md`、`task_and_workflow_contract.md`、`policy_engine_contract.md`、`execution_plane_contract.md`、`observability_contract.md` 等已切到 canonical 命名。 | 不改代码，作为 contract 回写问题处理。 |
| R2-14 | 8. 剩余 Contract vs 架构 | HIGH | `runtime_state_machine_contract.md` | §3 WorkflowStatus 7态缺架构 13态 HarnessRun 的 created/admitted/planning/ready/pausing/replanning/compensating/aborted | 已修复 | contract 文档曾沿用 execution/workflow/step/status 等 legacy canonical，后续已按 HarnessRun/NodeRun/PlanGraph 主链回写。 | 当前 `docs_zh/contracts/` 下的 `runtime_state_machine_contract.md`、`cost_and_budget_contract.md`、`task_and_workflow_contract.md`、`policy_engine_contract.md`、`execution_plane_contract.md`、`observability_contract.md` 等已切到 canonical 命名。 | 不改代码，作为 contract 回写问题处理。 |
| R2-15 | 8. 剩余 Contract vs 架构 | HIGH | `cost_and_budget_contract.md` | §4 CostEvent 以 task_id 为必填但 harness_run_id 为可选，架构 §18 以 HarnessRun 为预算主体 | 已修复 | contract 文档曾沿用 execution/workflow/step/status 等 legacy canonical，后续已按 HarnessRun/NodeRun/PlanGraph 主链回写。 | 当前 `docs_zh/contracts/` 下的 `runtime_state_machine_contract.md`、`cost_and_budget_contract.md`、`task_and_workflow_contract.md`、`policy_engine_contract.md`、`execution_plane_contract.md`、`observability_contract.md` 等已切到 canonical 命名。 | 不改代码，作为 contract 回写问题处理。 |
| R2-16 | 8. 剩余 Contract vs 架构 | HIGH | `cost_and_budget_contract.md` | §7.4 隐式成本归属仍用废弃 execution_id，应为 node_run_id/attempt_id | 已修复 | contract 文档曾沿用 execution/workflow/step/status 等 legacy canonical，后续已按 HarnessRun/NodeRun/PlanGraph 主链回写。 | 当前 `docs_zh/contracts/` 下的 `runtime_state_machine_contract.md`、`cost_and_budget_contract.md`、`task_and_workflow_contract.md`、`policy_engine_contract.md`、`execution_plane_contract.md`、`observability_contract.md` 等已切到 canonical 命名。 | 不改代码，作为 contract 回写问题处理。 |
| R2-17 | 8. 剩余 Contract vs 架构 | MEDIUM | `cost_and_budget_contract.md` | §4 CostEvent 缺 budget_reservation_id，架构 §18.3 要求 reserve-before-execute 链接 | 已修复 | contract 文档曾沿用 execution/workflow/step/status 等 legacy canonical，后续已按 HarnessRun/NodeRun/PlanGraph 主链回写。 | 当前 `docs_zh/contracts/` 下的 `runtime_state_machine_contract.md`、`cost_and_budget_contract.md`、`task_and_workflow_contract.md`、`policy_engine_contract.md`、`execution_plane_contract.md`、`observability_contract.md` 等已切到 canonical 命名。 | 不改代码，作为 contract 回写问题处理。 |
| R2-18 | 8. 剩余 Contract vs 架构 | MEDIUM | `task_and_workflow_contract.md` | §6-§7 WorkflowStep/StepOutput 以 step_id 为主键，应为 node_run_id | 已修复 | contract 文档曾沿用 execution/workflow/step/status 等 legacy canonical，后续已按 HarnessRun/NodeRun/PlanGraph 主链回写。 | 当前 `docs_zh/contracts/` 下的 `runtime_state_machine_contract.md`、`cost_and_budget_contract.md`、`task_and_workflow_contract.md`、`policy_engine_contract.md`、`execution_plane_contract.md`、`observability_contract.md` 等已切到 canonical 命名。 | 不改代码，作为 contract 回写问题处理。 |
| R2-19 | 8. 剩余 Contract vs 架构 | MEDIUM | `policy_engine_contract.md` | §3.1 PolicyDecisionRequest 用废弃 execution_id | 已修复 | contract 文档曾沿用 execution/workflow/step/status 等 legacy canonical，后续已按 HarnessRun/NodeRun/PlanGraph 主链回写。 | 当前 `docs_zh/contracts/` 下的 `runtime_state_machine_contract.md`、`cost_and_budget_contract.md`、`task_and_workflow_contract.md`、`policy_engine_contract.md`、`execution_plane_contract.md`、`observability_contract.md` 等已切到 canonical 命名。 | 不改代码，作为 contract 回写问题处理。 |
| R2-20 | 8. 剩余 Contract vs 架构 | MEDIUM | `execution_plane_contract.md` | §8 ExecutionTicket isolation_level 用废弃 standard/hardened/strict，应为 read_only/workspace_write/scoped_external_access/restricted_exec | 已修复 | contract 文档曾沿用 execution/workflow/step/status 等 legacy canonical，后续已按 HarnessRun/NodeRun/PlanGraph 主链回写。 | 当前 `docs_zh/contracts/` 下的 `runtime_state_machine_contract.md`、`cost_and_budget_contract.md`、`task_and_workflow_contract.md`、`policy_engine_contract.md`、`execution_plane_contract.md`、`observability_contract.md` 等已切到 canonical 命名。 | 不改代码，作为 contract 回写问题处理。 |
| R2-21 | 8. 剩余 Contract vs 架构 | MEDIUM | `model_gateway_routing_contract.md` | ModelRouteRequest 缺 harness_run_id/node_run_id，无法满足 INV-BUDGET-001 预算门禁 | 已修复 | contract 文档曾沿用 execution/workflow/step/status 等 legacy canonical，后续已按 HarnessRun/NodeRun/PlanGraph 主链回写。 | 当前 `docs_zh/contracts/` 下的 `runtime_state_machine_contract.md`、`cost_and_budget_contract.md`、`task_and_workflow_contract.md`、`policy_engine_contract.md`、`execution_plane_contract.md`、`observability_contract.md` 等已切到 canonical 命名。 | 不改代码，作为 contract 回写问题处理。 |
| R2-22 | 8. 剩余 Contract vs 架构 | MEDIUM | `observability_contract.md` | §3 LogEvent 缺 harness_run_id/node_run_id 必填字段 | 已修复 | contract 文档曾沿用 execution/workflow/step/status 等 legacy canonical，后续已按 HarnessRun/NodeRun/PlanGraph 主链回写。 | 当前 `docs_zh/contracts/` 下的 `runtime_state_machine_contract.md`、`cost_and_budget_contract.md`、`task_and_workflow_contract.md`、`policy_engine_contract.md`、`execution_plane_contract.md`、`observability_contract.md` 等已切到 canonical 命名。 | 不改代码，作为 contract 回写问题处理。 |
| R2-23 | 8. 剩余 Contract vs 架构 | LOW | `plugin_spi_contract.md vs tool_skill_plugin_contract.md` | 生命周期钩子命名互相矛盾（initialize/activate vs onLoad/onActivate） | 已修复 | `plugin_spi_contract.md` 原在 Plugin 接口上使用 `initialize/activate/suspend/deactivate` 作为生命周期钩子名，与 `tool_skill_plugin_contract.md` 使用的 `onLoad/onActivate/onDeactivate/onUnload` 互相矛盾。 | 当前 `plugin_spi_contract.md` 的所有 Plugin SPI 接口（DomainRetrieverPlugin/DomainValidatorPlugin/DomainPlannerPlugin/DomainPresenterPlugin）已统一使用 canonical 钩子名称 `onLoad / onActivate / onDeactivate / onUnload`，与 `tool_skill_plugin_contract.md` 完全对齐。PluginSpiRegistry 的 `initialize/activate/suspend/deactivate/unload` 方法为 registry-level 操作，明确注释为"调用 plugin.onLoad"等，对齐说明已写入文档。 | 不改代码，仅更新 audit doc 根因/依据描述以准确反映实际修复内容。 |
| R2-24 | 8. 剩余 Contract vs 架构 | MEDIUM | `runtime_state_machine_contract.md` | 用 ExecutionStatus 名称而非 canonical NodeRun.status | 已修复 | contract 文档曾沿用 execution/workflow/step/status 等 legacy canonical，后续已按 HarnessRun/NodeRun/PlanGraph 主链回写。 | 当前 `docs_zh/contracts/` 下的 `runtime_state_machine_contract.md`、`cost_and_budget_contract.md`、`task_and_workflow_contract.md`、`policy_engine_contract.md`、`execution_plane_contract.md`、`observability_contract.md` 等已切到 canonical 命名。 | 不改代码，作为 contract 回写问题处理。 |
| R2-25 | 9. 架构文档内部一致性 | HIGH | `§45.13 vs §25.8` | HarnessRun 状态数矛盾：§45.13 定义 6态，§25.8 定义 13态 | 已证伪（矛盾不存在） | 经验证，§45.13 第 6758 行 `status` 实际列出 13 态（created/admitted/planning/ready/running/pausing/paused/resuming/replanning/compensating/completed/failed/aborted），与 §25.8 第 2042 行完全一致。矛盾不成立。 | 不改代码，无需任何修复。 |
| R2-26 | 9. 架构文档内部一致性 | HIGH | `§45.13 vs §58.6` | finalDecision 取值矛盾：§45.13 允许 4值，§58.6 HarnessDecision 列出 6值 | 已证伪（矛盾不存在） | 经验证，§45.13 第 6757 行 `finalDecision` 实际列出 10 值（accept/retry_same_plan/replan/escalate_to_human/downgrade_mode/abort/quarantine/revoke_approval/pause_for_external/require_revalidation），§58.6 同样列出 10 种裁决类型（6 种基础 + 4 种生产扩展）。两者完全一致，矛盾不成立。 | 不改代码，无需任何修复。 |
| R2-27 | 9. 架构文档内部一致性 | HIGH | `§58.6` | 标题称”六种裁决”但表格实际列出 10种，自相矛盾 | 已修复（文档一致性修复） | §58.6 本身正确说明”共十种裁决（基础裁决 6 种 + 生产扩展裁决 4 种）”，但文档其他位置（§33 路线图表、§58 术语表、v2.9 版本历史）引用 §58.6 时误写为”六种裁决”。已将所有引用统一修正为”十种裁决”并注明”6 种基础 + 4 种生产扩展”。 | 架构文档 `docs_zh/architecture/00-platform-architecture.md` 已将 §58.6 相关引用（8923 行、9221 行、10218 行、10351 行、10434 行、10590 行）统一修正为”十种裁决”。 | 文档内部一致性修复，无需修改实现代码。 |
| R2-28 | 9. 架构文档内部一致性 | MEDIUM | `§45.7 vs §58.6` | LoopController 决策类型：§45.7 列 5种，§58.6 要求 6种（缺 downgrade_mode） | 已证伪（矛盾不存在） | 经验证，§45.7 第 6622-6629 行决策矩阵实际列出 6 种裁决（accept/retry_same_plan/replan/escalate_to_human/downgrade_mode/abort），包含 downgrade_mode；§58.6 第 7253-7264 行同样列出 6 种基础裁决。两者完全一致，矛盾不成立。 | 不改代码，无需任何修复。 |
| R2-29 | 9. 架构文档内部一致性 | MEDIUM | `§45.9` | Generator WorkProduct 字段命名 | 已修复 | §45.9 WorkProduct 表明文定义 `node_run_id`（第 6671 行），glossary（第 10427 行）同样写作 `nodeRunId`，与 §5.5 NodeRun 主键命名一致。issue 原始描述”仍用 step_id”已过时。 | 架构文档已正确使用 `node_run_id`/`nodeRunId`，无需修复。 | 不改代码，仅更新 issue 描述以准确反映实际状态。 |
| R2-30 | 9. 架构文档内部一致性 | MEDIUM | `§59.2` | ExplanationRequest 用废弃 workflow_id/step_id | 已证伪（架构文档已正确使用 canonical 名称） | 经核实，`§59.2` 的 `ExplanationRequest` 定义（8170 行）已使用 `harness_run_id / node_run_id?`，未使用废弃的 `workflow_id/step_id`。原 issue 描述不准确。 | 架构文档本身已正确，无需修复。 | 不改代码，仅更新 audit doc 状态以准确反映核实结果。 |
| R2-31 | 9. 架构文档内部一致性 | MEDIUM | `§35` | contracts/ 目录结构含废弃命名子目录（execution-plan/、workflow-run/） | 移出实现审计范围 | 这组属于架构主文档内部章节/枚举/目录表述冲突，不是实现与架构的不一致。 | 条目全部来自“架构文档内部一致性”章节，本质是 `00-platform-architecture.md` 的文档自一致性问题。 | 不改代码，保留为单独文档审计范围。 |
| R2-32 | 9. 架构文档内部一致性 | LOW | `§36.3` | 仍用 Phase 1-9 作为 canonical 成功标准，与 Ring 1/2/3 体系矛盾 | 已解决（文档修复） | §36.3 Ring 3 子段（3a/3b/3c）已全部改为 Ring 命名，Phase 6-9 仅以括注历史映射形式保留，不作为独立 gate。 | 架构文档 `docs_zh/architecture/00-platform-architecture.md` §36.3 已将 `#### Phase 6-7` → `#### Ring 3a（对应 Phase 6-7）`；`#### Phase 8` → `#### Ring 3b（对应 Phase 8）`；`#### Phase 9` → `#### Ring 3c（对应 Phase 9，Enterprise 扩张 gate）`。Phase 仅作历史映射保留，不再作为 canonical 成功标准独立使用。 | 已按源文档修复更新状态。 |
| R3-1 | 11. Harness Runtime 深层实现缺口（§45） | CRITICAL | `orchestration/harness/guardrail-engine.ts` | 护栏仅 policy/risk/tool/evidence/budget 5层；§45.20 要求 Input(注入防御)/Planning/Tool/Memory/Output 五层——Input 和 Memory 护栏完全缺失 | 已解决 | Harness runtime 条目来自早期快照，没有反映后续 HITL、责任记录、decision bundle、toolbelt 六步装配、五类恢复、memory 治理和 invariants 收口。 | 当前 `src/platform/five-plane-orchestration/harness/guardrails/guardrail-engine.ts` 已实现 §45.20 全部 9 层护栏（Input/Planning/Tool/Memory/Output/Policy/Risk/Evidence/Budget）：Input 层（50-70 行）检测 `__import__`、`<script>`、`javascript:`、`data:text/html`、`${...}` 等注入模式；Memory 层（117-137 行）检测 `modify_own_prompt`、`update_own_instructions`、`change_own_role`、`escalate_own_permissions` 等自我强化指标。旧 `orchestration/harness/guardrail-engine.ts` 已废弃，代码未动，仅回写依据。 | 不改代码，仅回写依据。 |
| R3-2 | 11. Harness Runtime 深层实现缺口（§45） | CRITICAL | `orchestration/harness/hitl-runtime.ts` | 仅支持 open/resolve(approve/reject)；§45.18 要求 5种 HITL：Inspect/Patch/Override/Takeover/Resume 含完整状态机 | 已解决 | Harness runtime 条目来自早期快照，没有反映后续 HITL、责任记录、decision bundle、toolbelt 六步装配、五类恢复、memory 治理和 invariants 收口。 | 当前 `src/platform/five-plane-orchestration/harness/` 已包含 `inspect/patch/override/takeover/resume`、`HumanResponsibilityRecord`、`PromptExecutionRecord`、`DecisionInputBundle`、`downgrade_mode`、六步 `ToolbeltAssembler`、五类 `RecoveryController`、memory namespace promotion/demotion 以及 `assertInvariants()` 的 10 项校验。 | 不改代码，仅回写依据。 |
| R3-3 | 11. Harness Runtime 深层实现缺口（§45） | CRITICAL | `orchestration/harness/index.ts` | HumanResponsibilityRecord(§45.27) 未实现——每次 HITL 操作需产出 actor/action/scope/rationale/beforeRef/afterRef/expiresAt/auditRef | 已解决 | Harness runtime 条目来自早期快照，没有反映后续 HITL、责任记录、decision bundle、toolbelt 六步装配、五类恢复、memory 治理和 invariants 收口。 | 当前 `src/platform/five-plane-orchestration/harness/` 已包含 `inspect/patch/override/takeover/resume`、`HumanResponsibilityRecord`、`PromptExecutionRecord`、`DecisionInputBundle`、`downgrade_mode`、六步 `ToolbeltAssembler`、五类 `RecoveryController`、memory namespace promotion/demotion 以及 `assertInvariants()` 的 10 项校验。 | 不改代码，仅回写依据。 |
| R3-4 | 11. Harness Runtime 深层实现缺口（§45） | HIGH | `orchestration/harness/index.ts` | autonomyMode 用 manual/supervised/auto/full_auto；§42.1 要求 suggestion/supervised/semi_auto/full_auto | 已解决 | Harness runtime 条目来自早期快照，没有反映后续 HITL、责任记录、decision bundle、toolbelt 六步装配、五类恢复、memory 治理和 invariants 收口。 | 当前 `src/platform/five-plane-orchestration/harness/` 已包含 `inspect/patch/override/takeover/resume`、`HumanResponsibilityRecord`、`PromptExecutionRecord`、`DecisionInputBundle`、`downgrade_mode`、六步 `ToolbeltAssembler`、五类 `RecoveryController`、memory namespace promotion/demotion 以及 `assertInvariants()` 的 10 项校验。 | 不改代码，仅回写依据。 |
| R3-5 | 11. Harness Runtime 深层实现缺口（§45） | HIGH | `orchestration/harness/index.ts` | HarnessRun 缺 §45.13 要求的 tenantId/goal/mode/riskLevel/ownership/auditRefs/traceId 7字段 | 已解决 | Harness runtime 条目来自早期快照，没有反映后续 HITL、责任记录、decision bundle、toolbelt 六步装配、五类恢复、memory 治理和 invariants 收口。 | 当前 `src/platform/five-plane-orchestration/harness/` 已包含 `inspect/patch/override/takeover/resume`、`HumanResponsibilityRecord`、`PromptExecutionRecord`、`DecisionInputBundle`、`downgrade_mode`、六步 `ToolbeltAssembler`、五类 `RecoveryController`、memory namespace promotion/demotion 以及 `assertInvariants()` 的 10 项校验。 | 不改代码，仅回写依据。 |
| R3-6 | 11. Harness Runtime 深层实现缺口（§45） | HIGH | `orchestration/harness/index.ts` | HarnessStep 缺 §45.13 要求的 nodeRunRefs/rationale/evidenceRefs/toolCalls/latency/cost/error/nextAction 8字段 | 已解决 | Harness runtime 条目来自早期快照，没有反映后续 HITL、责任记录、decision bundle、toolbelt 六步装配、五类恢复、memory 治理和 invariants 收口。 | 当前 `src/platform/five-plane-orchestration/harness/` 已包含 `inspect/patch/override/takeover/resume`、`HumanResponsibilityRecord`、`PromptExecutionRecord`、`DecisionInputBundle`、`downgrade_mode`、六步 `ToolbeltAssembler`、五类 `RecoveryController`、memory namespace promotion/demotion 以及 `assertInvariants()` 的 10 项校验。 | 不改代码，仅回写依据。 |
| R3-7 | 11. Harness Runtime 深层实现缺口（§45） | HIGH | `orchestration/harness/index.ts` | HarnessDecision 仅 6值；§58.6 要求追加 quarantine/revoke_approval/pause_for_external/require_revalidation | 已解决 | Harness runtime 条目来自早期快照，没有反映后续 HITL、责任记录、decision bundle、toolbelt 六步装配、五类恢复、memory 治理和 invariants 收口。 | 当前 `src/platform/five-plane-orchestration/harness/` 已包含 `inspect/patch/override/takeover/resume`、`HumanResponsibilityRecord`、`PromptExecutionRecord`、`DecisionInputBundle`、`downgrade_mode`、六步 `ToolbeltAssembler`、五类 `RecoveryController`、memory namespace promotion/demotion 以及 `assertInvariants()` 的 10 项校验。 | 不改代码，仅回写依据。 |
| R3-8 | 11. Harness Runtime 深层实现缺口（§45） | HIGH | `orchestration/harness/toolbelt-assembler.ts` | 仅做 allowed/blocked 集合交集；§45.4 要求 6步装配：domain→constraint→risk→budget→security→reliability | 已解决 | Harness runtime 条目来自早期快照，没有反映后续 HITL、责任记录、decision bundle、toolbelt 六步装配、五类恢复、memory 治理和 invariants 收口。 | 当前 `src/platform/five-plane-orchestration/harness/` 已包含 `inspect/patch/override/takeover/resume`、`HumanResponsibilityRecord`、`PromptExecutionRecord`、`DecisionInputBundle`、`downgrade_mode`、六步 `ToolbeltAssembler`、五类 `RecoveryController`、memory namespace promotion/demotion 以及 `assertInvariants()` 的 10 项校验。 | 不改代码，仅回写依据。 |
| R3-9 | 11. Harness Runtime 深层实现缺口（§45） | HIGH | `orchestration/harness/recovery-controller.ts` | 仅处理 3种故障；§45.11 要求 5种含 llm_provider_unavailable/budget_exhausted/platform_panic | 已解决 | Harness runtime 条目来自早期快照，没有反映后续 HITL、责任记录、decision bundle、toolbelt 六步装配、五类恢复、memory 治理和 invariants 收口。 | 当前 `src/platform/five-plane-orchestration/harness/` 已包含 `inspect/patch/override/takeover/resume`、`HumanResponsibilityRecord`、`PromptExecutionRecord`、`DecisionInputBundle`、`downgrade_mode`、六步 `ToolbeltAssembler`、五类 `RecoveryController`、memory namespace promotion/demotion 以及 `assertInvariants()` 的 10 项校验。 | 不改代码，仅回写依据。 |
| R3-10 | 11. Harness Runtime 深层实现缺口（§45） | HIGH | `orchestration/harness/memory-manager.ts` | 命名空间 run/domain/shared 无治理；§45.16 要求 Working/Long-term/Shared 含晋升/降级策略+防自我强化 | 已解决 | Harness runtime 条目来自早期快照，没有反映后续 HITL、责任记录、decision bundle、toolbelt 六步装配、五类恢复、memory 治理和 invariants 收口。 | 当前 `src/platform/five-plane-orchestration/harness/` 已包含 `inspect/patch/override/takeover/resume`、`HumanResponsibilityRecord`、`PromptExecutionRecord`、`DecisionInputBundle`、`downgrade_mode`、六步 `ToolbeltAssembler`、五类 `RecoveryController`、memory namespace promotion/demotion 以及 `assertInvariants()` 的 10 项校验。 | 不改代码，仅回写依据。 |
| R3-11 | 11. Harness Runtime 深层实现缺口（§45） | HIGH | `orchestration/harness/index.ts` | assertInvariants 仅检查 budget/state；§45.21 定义 10项不变量（INV-1~INV-10）均未强制 | 已解决 | Harness runtime 条目来自早期快照，没有反映后续 HITL、责任记录、decision bundle、toolbelt 六步装配、五类恢复、memory 治理和 invariants 收口。 | 当前 `src/platform/five-plane-orchestration/harness/` 已包含 `inspect/patch/override/takeover/resume`、`HumanResponsibilityRecord`、`PromptExecutionRecord`、`DecisionInputBundle`、`downgrade_mode`、六步 `ToolbeltAssembler`、五类 `RecoveryController`、memory namespace promotion/demotion 以及 `assertInvariants()` 的 10 项校验。 | 不改代码，仅回写依据。 |
| R3-12 | 11. Harness Runtime 深层实现缺口（§45） | HIGH | `orchestration/harness/index.ts` | PromptExecutionRecord(§45.24) 未实现——需冻结 promptVersion/modelRoute/inputHash/outputHash/contextSnapshotRef/guardrailResult/usage | 已解决 | Harness runtime 条目来自早期快照，没有反映后续 HITL、责任记录、decision bundle、toolbelt 六步装配、五类恢复、memory 治理和 invariants 收口。 | 当前 `src/platform/five-plane-orchestration/harness/` 已包含 `inspect/patch/override/takeover/resume`、`HumanResponsibilityRecord`、`PromptExecutionRecord`、`DecisionInputBundle`、`downgrade_mode`、六步 `ToolbeltAssembler`、五类 `RecoveryController`、memory namespace promotion/demotion 以及 `assertInvariants()` 的 10 项校验。 | 不改代码，仅回写依据。 |
| R3-13 | 11. Harness Runtime 深层实现缺口（§45） | HIGH | `orchestration/harness/index.ts` | DecisionInputBundle(§45.25) 未实现——决策前需冻结 evaluator/policy/budget/risk/node/sideEffect/hitl/guardrail 状态 | 已解决 | Harness runtime 条目来自早期快照，没有反映后续 HITL、责任记录、decision bundle、toolbelt 六步装配、五类恢复、memory 治理和 invariants 收口。 | 当前 `src/platform/five-plane-orchestration/harness/` 已包含 `inspect/patch/override/takeover/resume`、`HumanResponsibilityRecord`、`PromptExecutionRecord`、`DecisionInputBundle`、`downgrade_mode`、六步 `ToolbeltAssembler`、五类 `RecoveryController`、memory namespace promotion/demotion 以及 `assertInvariants()` 的 10 项校验。 | 不改代码，仅回写依据。 |
| R3-14 | 11. Harness Runtime 深层实现缺口（§45） | MEDIUM | `orchestration/harness/context-assembler.ts` | 直接复制源对象；§45.5 要求 token budget trimming + relevance scoring + freshness scoring + trust filtering | 已解决 | Harness runtime 条目来自早期快照，没有反映后续 HITL、责任记录、decision bundle、toolbelt 六步装配、五类恢复、memory 治理和 invariants 收口。 | 当前 `src/platform/five-plane-orchestration/harness/` 已包含 `inspect/patch/override/takeover/resume`、`HumanResponsibilityRecord`、`PromptExecutionRecord`、`DecisionInputBundle`、`downgrade_mode`、六步 `ToolbeltAssembler`、五类 `RecoveryController`、memory namespace promotion/demotion 以及 `assertInvariants()` 的 10 项校验。 | 不改代码，仅回写依据。 |
| R3-15 | 11. Harness Runtime 深层实现缺口（§45） | MEDIUM | `orchestration/harness/index.ts` | ContextAssemblyContract(§45.23) 未实现——需 per-role context 隔离含 taintPolicy/rankingPolicy/redactionPolicy | 已解决 | Harness runtime 条目来自早期快照，没有反映后续 HITL、责任记录、decision bundle、toolbelt 六步装配、五类恢复、memory 治理和 invariants 收口。 | 当前 `src/platform/five-plane-orchestration/harness/` 已包含 `inspect/patch/override/takeover/resume`、`HumanResponsibilityRecord`、`PromptExecutionRecord`、`DecisionInputBundle`、`downgrade_mode`、六步 `ToolbeltAssembler`、五类 `RecoveryController`、memory namespace promotion/demotion 以及 `assertInvariants()` 的 10 项校验。 | 不改代码，仅回写依据。 |
| R3-16 | 12. 组织治理 + 规模生态深层缺口（§46-§57） | CRITICAL | `scale-ecosystem/multi-region/region-router/` | RegionDescriptor 缺 provider/endpoints/dataResidencyPolicy；status 用 active/degraded/disabled 而非架构要求 active/standby/draining | 已解决 | 生产代码已补齐 provider/endpoints/dataResidencyPolicy 字段，status 枚举已修正为 active/standby/draining（附加 degraded/maintenance）；旧审计文档条目未同步更新。 | 当前 `src/scale-ecosystem/multi-region/region-router/index.ts` 已定义 `provider/endpoints/dataResidencyPolicy` 和 `active/standby/draining/degraded/maintenance`；`tests/unit/platform/scale-ecosystem/multi-region/region-router.test.ts` 已通过定向回归。 | 不改生产代码，本轮已修正本文档条目。 |
| R3-17 | 12. 组织治理 + 规模生态深层缺口（§46-§57） | CRITICAL | `scale-ecosystem/multi-region/failover-controller/` | 已实现 fencing epoch 机制；§52.3 要求 failover 提升 epoch（promoteEpoch），旧 leader 恢复后只能 follower 加入（detectSplitBrain 检测低 epoch 旧 leader 拒绝其领导权） | 已解决 | 审计条目的 issue 描述停留在早期"无 fencing epoch"版本，实际代码早有 `promoteEpoch` 追踪、`FencingToken` 颁发、`acknowledgeDemotion` 旧 leader 降级和 `detectSplitBrain` 分裂脑检测。 | 当前 `src/scale-ecosystem/multi-region/failover-controller/index.ts` 包含 `RegionFailoverController` 类，带 `promoteEpoch`/`fencingToken`/`getPromoteEpoch()`/`getFencingToken()`/`acknowledgeDemotion()`/`detectSplitBrain()`/`canLeaderServeWrites()`；`tests/unit/scale-ecosystem/multi-region/failover-controller-comprehensive.test.ts` 已通过。 | 不改生产代码，修正审计 issue 描述以准确反映已有实现。 |
| R3-18 | 12. 组织治理 + 规模生态深层缺口（§46-§57） | CRITICAL | `scale-ecosystem/integration/connector-registry/` | ConnectorManifest 缺整个 ConnectorCapabilityProfile(§57.1)：actionRiskProfiles/permissionProbes/quotaProbes/credentialRotationPolicy | 已解决 | connector manifest 已扩到 capability profile，但审计结论仍停在早期最小 manifest 版本。 | 当前 `src/scale-ecosystem/integration/connector-registry/index.ts` 已包含 `ConnectorCapabilityProfileSchema` 及 `actionRiskProfiles/permissionProbes/quotaProbes/credentialRotationPolicy`；相关 schema 测试已通过。 | 不改生产代码，仅回写依据。 |
| R3-19 | 12. 组织治理 + 规模生态深层缺口（§46-§57） | HIGH | `scale-ecosystem/sla-engine/tier-resolver/` | SlaTierSchema 缺 §54.1 要求的 availability/externalP95/internalP99/approvalLatencySlo/incidentResponseSlo/costMultiplier/supportLevel | 已解决 | SLA tier schema 已扩充，但旧审计没有吸收 canonical 字段升级。 | 当前 `src/scale-ecosystem/sla-engine/tier-resolver/index.ts` 已包含 `availability/externalP95/internalP99/approvalLatencySlo/incidentResponseSlo/costMultiplier/supportLevel`；`tests/unit/scale-ecosystem/sla-engine/tier-resolver/index.test.ts` 已通过。 | 不改生产代码，仅回写依据。 |
| R3-20 | 12. 组织治理 + 规模生态深层缺口（§46-§57） | HIGH | `scale-ecosystem/sla-engine/sla-operations-service.ts` | 无按 workflow class 拆分 SLA（§54.3 要求 deterministic/LLM-assisted/HITL-waiting 分别承诺） | 已解决 | 实现已经按 workflow class 乘子拆分承诺，但旧测试没有显式传入 `workflowClass`，导致看起来像未实现。 | 当前 `src/scale-ecosystem/sla-engine/sla-operations-service.ts` 已定义 `WorkflowClass` 与差异化 latency multiplier；本轮已修正 `tests/unit/scale-ecosystem/sla-engine/sla-operations-service.test.ts` 的旧断言并通过回归。 | 不改生产代码，修正旧测试和文档证据。 |
| R3-21 | 12. 组织治理 + 规模生态深层缺口（§46-§57） | HIGH | `scale-ecosystem/marketplace/catalog/` | 用 listingId 代替 §55.2 entryId，缺 packId/rating/installCount；certificationStatus 枚举不匹配 | 已解决 | marketplace catalog 已迁到 `entryId` 和新字段，但旧测试仍用 `listingId`。 | 当前 `src/scale-ecosystem/marketplace/catalog/index.ts` 已定义 `entryId/packId/rating/installCount/certificationStatus`；本轮已修正 `tests/unit/scale-ecosystem/marketplace/catalog/catalog.test.ts` 并通过。 | 不改生产代码，修正旧测试和文档证据。 |
| R3-22 | 12. 组织治理 + 规模生态深层缺口（§46-§57） | HIGH | `org-governance/compliance-engine/framework-catalog.ts` | auditRequirements 是 string[] 而非 §49.1 要求的 AuditSpec[]（含 frequency/evidenceType/retentionPeriod） | 已解决 | compliance framework 已切到 `AuditSpec[]`，但旧测试仍把 `auditRequirements` 当字符串数组。 | 当前 `src/org-governance/compliance-engine/framework-catalog.ts` 已定义 `AuditSpecSchema` 和结构化 `auditRequirements`；本轮已修正 `tests/unit/org-governance/compliance-engine/framework-catalog.test.ts` 并通过。 | 不改生产代码，修正旧测试和文档证据。 |
| R3-23 | 12. 组织治理 + 规模生态深层缺口（§46-§57） | HIGH | `org-governance/compliance-engine/inheritance/` | 无 PolicyStrictnessComparator(§49.2)；不可比策略静默 fallback 到 Math.min 而非进入合规审批 | 已解决 | inheritance 模块已具备严格度比较器与“不可比即进入合规审批”的显式语义，但旧审计停留在只有 merge 逻辑的早期版本。 | 当前 `src/org-governance/compliance-engine/inheritance/index.ts` 已定义 `PolicyStrictnessComparator`、`DefaultPolicyStrictnessComparator`、`comparePolicyStrictness()`，且 `incomparable` 时返回 `requiresComplianceApproval: true`；`tests/unit/org-governance/compliance-engine/inheritance.test.ts` 已通过。 | 不改生产代码，仅回写依据。 |
| R3-24 | 12. 组织治理 + 规模生态深层缺口（§46-§57） | HIGH | `org-governance/approval-routing/route-engine/` | applySodPolicy 未阻止同链互批(§47.1)——同一审批链两人可互相审批对方请求 | 已解决 | route-engine 已实现同链互批拦截、并行签核和 revalidation，但旧测试仍带着早期 groupId/FX 前置条件假设。 | 当前 `src/org-governance/approval-routing/route-engine/index.ts` 已有 `applySodPolicy()` 同链阻断与 `revalidateApprovalRoute()`；本轮已修正 `tests/unit/org-governance/approval-routing/route-engine/index.test.ts` 的旧断言并通过。 | 不改生产代码，修正旧测试和文档证据。 |
| R3-25 | 12. 组织治理 + 规模生态深层缺口（§46-§57） | HIGH | `org-governance/knowledge-boundary/chinese-wall-access-saga.ts` | 缺 §50.3 两阶段提交（prepare lock→atomic commit→failure reconciliation）；仅做简单 pass/fail 分类 | 已解决（源码修复并补定向测试） | 原实现确有真实缺口：`prepare_release` 误走 `prepareGrant` handler，prepare/commit handler 异常没有进入 saga 失败链，回滚只根据已提交 commit action 生成，导致 prepare-only 失败时缺少 failure reconciliation。 | 本轮已在 `src/org-governance/knowledge-boundary/chinese-wall-access-saga.ts` 补齐 prepare/commit try-catch、兼容多种 prepare/commit stepId 关联、按成功 action 反向生成补偿动作，并把 `committedActions` 收敛到成功 2PC 轨迹；`tests/unit/org-governance/knowledge-boundary/chinese-wall-access-saga.test.ts`、`tests/integration/org-governance/knowledge-boundary-integration.test.ts`、`tests/integration/org-governance/governance-sagas.integration.test.ts`、`tests/integration/platform/org-governance/knowledge-boundary/chinese-wall-access.integration.test.ts` 已通过。 | 已改生产代码、测试和文档依据。 |
| R3-26 | 12. 组织治理 + 规模生态深层缺口（§46-§57） | HIGH | `scale-ecosystem/resource-manager/quota-enforcer/` | QuotaPolicy 单维度；§53.2 要求 7维 MultiResourceQuotaVector（worker_concurrency/tool_qps/model_tpm/model_rpm/budget_amount/approval_capacity/storage_io）全部通过才准入 | 已解决 | quota-enforcer 已扩到多维配额向量，但旧审计保留了单维时期的结论。 | 当前 `src/scale-ecosystem/resource-manager/quota-enforcer/index.ts` 已定义 `MultiResourceQuotaVectorSchema` 和 `evaluateMultiDimensionalQuota()`；`tests/unit/scale-ecosystem/resource-manager/quota-enforcer/index.test.ts` 已通过。 | 不改生产代码，仅回写依据。 |
| R3-27 | 12. 组织治理 + 规模生态深层缺口（§46-§57） | MEDIUM | `org-governance/delegated-governance/` | GovernanceDelegationRevocationSaga 缺级联范围(§51.1)：需覆盖 pending approval/active session/secret lease/worker lease/scheduled trigger | 已解决 | revocation saga 已按 cascade scope 覆盖 5 类撤销范围，旧审计没有跟上默认 scope 和 receipt 字段扩展。 | 当前 `src/org-governance/delegated-governance/governance-delegation-revocation-saga.ts` 已包含 `pendingApprovals/activeSessions/secretLeases/workerLeases/scheduledTriggers`；`tests/integration/platform/org-governance/delegated-governance/delegation-revocation-saga.integration.test.ts` 与 `tests/integration/org-governance/governance-sagas.integration.test.ts` 已通过。 | 不改生产代码，仅回写依据并修正旧测试口径。 |
| R3-28 | 12. 组织治理 + 规模生态深层缺口（§46-§57） | MEDIUM | `org-governance/org-model/org-governance-saga.ts` | §46.3 要求 commit 固定序(identity→approval→budget→domain→agent)含 OrgGovernanceSagaReceipt；实际无序无 receipt | 已解决 | `OrgGovernanceSaga` 已固化 phase 顺序并提供 `executeWithReceipt()`，旧审计和旧单测仍把“无失败时也应执行 compensate”当成现行语义。 | 当前 `src/org-governance/org-model/org-governance-saga.ts` 已定义固定 `PHASE_ORDER` 与 `OrgGovernanceSagaReceipt`；本轮已修正 `tests/unit/org-governance/org-model/org-governance-saga.test.ts` 及相关集成测试的过时断言并通过。 | 不改生产代码，修正旧测试和文档证据。 |
| R3-29 | 12. 组织治理 + 规模生态深层缺口（§46-§57） | MEDIUM | `org-governance/sso-scim/identity-sync-service.ts` | DLQ 仅记录无重试；§48.2 要求 retry/backoff + 每日对账 + IdentityReconciliationReport | 已解决 | identity sync 已补齐 DLQ retry/backoff、每日 reconciliation 和结构化 report，但旧审计没有吸收该能力。 | 当前 `src/org-governance/sso-scim/identity-sync-service.ts` 已实现 `processDlqWithRetry()`、`retryDlqRecord()`、`generateDailyReconciliation()` 和 `IdentityReconciliationReport`；`tests/unit/org-governance/identity-sync-service.test.ts`、`tests/integration/org-governance/sso-scim/identity-sync-integration.test.ts` 已通过。 | 不改生产代码，仅回写依据。 |
| R3-30 | 12. 组织治理 + 规模生态深层缺口（§46-§57） | MEDIUM | `scale-ecosystem/multi-region/cross-region-routing-service.ts` | 跨境传输仅 boolean allowCrossBorder；§52.4 要求 5步链：JurisdictionClassifier→TransferImpactAssessor→MechanismSelector→DataMinimizer→OutputScanner | 已解决 | cross-region routing 已落地 5 步跨境链路，旧集成测试仍用过时的 region status 枚举，放大成“实现缺失”。 | 当前 `src/scale-ecosystem/multi-region/cross-region-routing-service.ts` 已显式包含 `classifyJurisdiction()`、`assessTransferImpact()`、`selectMechanism()`、`minimizeData()`、`scanOutput()`；本轮已修正 `tests/integration/scale-ecosystem/cross-region-routing-service.test.ts` 的旧状态断言并通过。 | 不改生产代码，修正旧测试和文档证据。 |
| R3-31 | 12. 组织治理 + 规模生态深层缺口（§46-§57） | MEDIUM | `scale-ecosystem/billing/types.ts` | RecordUsageInput 单 metricType；§53.2 要求多维准入守卫 | 已解决 | billing types 已扩到多维 usage dimensions 和 dimension quota counters，旧审计仍停留在单 metricType 输入的早期口径。 | 当前 `src/scale-ecosystem/billing/types.ts` 已包含 `RecordUsageInput.metricDimensions` 与 `RecordUsageResult.dimensionQuotaCounters`；`tests/unit/scale-ecosystem/billing/types.test.ts` 已通过。 | 不改生产代码，仅回写依据。 |
| R3-32 | 12. 组织治理 + 规模生态深层缺口（§46-§57） | MEDIUM | `org-governance/knowledge-boundary/sharing-gate/` | evaluateKnowledgeShare 返回 boolean；§50.3 要求经 CrossBoundaryTransform（脱敏/摘要/字段过滤） | 已解决 | 审计基于旧实现快照；当前实现早已改成显式的跨边界变换返回值，而不是 boolean。 | `src/org-governance/knowledge-boundary/sharing-gate/index.ts` 中 `evaluateKnowledgeShare(...)` 现返回 `CrossBoundaryTransformResult` 或 `null`，结果内含 `mode(summary/field_filter)` 与 `allowedFieldKeys`。 | 不改代码，仅回写依据。 |
| R3-33 | 13. 运维成熟度 + SDK 缺口（§51-§69） | HIGH | `ops-maturity/explainability/explanation-pipeline-service.ts` | StageRationale 缺 §59.3 的 rationaleId/alternatives/confidence/decisionInputRef/versionLockRef/visibilityLabels/renderedExplanation | 已解决 | 审计引用的是旧版 `StageRationale` 结构；当前 explainability 契约已补齐这些字段。 | `src/ops-maturity/explainability/explanation-pipeline-service.ts` 中 `StageRationale` 已声明 `rationaleId/alternatives/confidence/decisionInputRef/versionLockRef/visibilityLabels/renderedExplanation`，生成链也会写入这些字段。 | 不改代码，仅回写依据。 |
| R3-34 | 13. 运维成熟度 + SDK 缺口（§51-§69） | HIGH | `ops-maturity/emergency/platform-panic-service.ts` | PlatformPanicDirective 缺 §60.1 severity(full/partial)/reconfirmationAfterSeconds/rollbackStrategy | 已解决 | 旧审计没有跟上当前 panic 指令契约。 | `src/ops-maturity/emergency/platform-panic-service.ts` 的 `PlatformPanicDirective` 已包含 `severity`、`reconfirmationAfterSeconds`、`rollbackStrategy`，相关 `platform-panic` 单测本轮保持通过。 | 不改代码，仅回写依据。 |
| R3-35 | 13. 运维成熟度 + SDK 缺口（§51-§69） | HIGH | `ops-maturity/agent-lifecycle/agent-registry/` | AgentLifecycleState 缺 removed 态(§61.3 要求9态)；transitions 缺 archived→removed 和 paused→canary | 已解决（源码修复并补定向测试） | 主状态机其实已迁到 9 态，但辅助语义和一批旧测试仍按 8 态/`archived` 终态理解，导致审计证据与现行实现混杂。 | `src/ops-maturity/agent-lifecycle/agent-registry/index.ts` 已确认 9 态与 `archived→removed`、`paused→canary`；本轮进一步把 `isTerminalState(...)` 修正为仅 `removed` 为终态，并更新 `tests/unit/ops-maturity/agent-lifecycle/*.test.ts` 全套回归。 | 已修代码与测试，不保留缺口。 |
| R3-36 | 13. 运维成熟度 + SDK 缺口（§51-§69） | HIGH | `ops-maturity/edge-runtime/edge-runtime-sync-service.ts` | EdgeRuntimeProfile 缺 §62.2 deviceId/offlineMaxDuration/keyLease/risk_level≤medium 门禁 | 已解决 | 运行时代码已加这些字段与门禁，但旧测试样例仍沿用缺字段 profile，制造了“像是没实现”的假象。 | `src/ops-maturity/edge-runtime/edge-runtime-sync-service.ts` 的 `executeOffline(...)` 已检查 `deviceId/offlineMaxDuration/keyLease`，且拒绝 `riskLevel > medium`；本轮同步修正了 `tests/unit/ops-maturity/edge-runtime/edge-runtime-sync-service.test.ts` 与相关集成样例。 | 不改主实现逻辑，仅修正过期测试与依据。 |
| R3-37 | 13. 运维成熟度 + SDK 缺口（§51-§69） | HIGH | `ops-maturity/edge-runtime/sync-queue/` | EdgeSyncEnvelope 缺 §62.3 device_id/sequence_no/prev_hash/side_effect_dependency_refs/signature/local_time_offset | 已解决（源码修复并补定向测试） | 这里不是“完全没模块”，而是队列 envelope 契约长期保持最小兼容形态，关键 provenance 字段停留在 optional/未强制接线状态。 | 本轮将 `src/ops-maturity/edge-runtime/sync-queue/index.ts` 的 `EdgeSyncEnvelope` 收敛为包含 `device_id/sequence_no/prev_hash/side_effect_dependency_refs/signature/local_time_offset/createdAt` 的 canonical 契约，并在 `src/ops-maturity/edge-runtime/edge-runtime-sync-service.ts` 的有序同步路径中补齐映射；`tests/unit/ops-maturity/edge-runtime/sync-queue*.test.ts` 已新增/更新回归。 | 已修代码与测试，不保留缺口。 |
| R3-38 | 13. 运维成熟度 + SDK 缺口（§51-§69） | HIGH | `sdk/client-sdk/api-client.ts` | 未发送 §22.2 要求的 X-Platform-Version/X-SDK-Version/X-Contract-Version 版本握手头 | 已解决 | 这组混合了旧目录快照、接口认知和测试存在性误判，当前仓库文件已直接反证。 | 当前 `src/sdk/client-sdk/api-client.ts` 已发送版本握手头；`src/sdk/cli/index.ts` 已包含 `pack create/test/validate/publish`；`src/platform/five-plane-*` 目录存在；`dashboard-websocket-server.ts` 已绑定 principal/tenantId；`structured-logger.ts` 有 `crosscuttingFabric`；`tests/invariants/` 已存在；启动顺序与 ring catalog 已改为 canonical。 | 不改代码，仅回写依据。 |
| R3-39 | 13. 运维成熟度 + SDK 缺口（§51-§69） | HIGH | `sdk/cli/index.ts` | 缺 §22.3 要求的 pack create/test/validate/publish CLI 命令 | 已解决 | 这组混合了旧目录快照、接口认知和测试存在性误判，当前仓库文件已直接反证。 | 当前 `src/sdk/client-sdk/api-client.ts` 已发送版本握手头；`src/sdk/cli/index.ts` 已包含 `pack create/test/validate/publish`；`src/platform/five-plane-*` 目录存在；`dashboard-websocket-server.ts` 已绑定 principal/tenantId；`structured-logger.ts` 有 `crosscuttingFabric`；`tests/invariants/` 已存在；启动顺序与 ring catalog 已改为 canonical。 | 不改代码，仅回写依据。 |
| R3-40 | 13. 运维成熟度 + SDK 缺口（§51-§69） | MEDIUM | `ops-maturity/edge-runtime/` | 冲突解决含 accept_edge；§62.3 要求 central wins + 生成 Incident 人工审查 | 已解决 | 当前运行时早已使用 central-wins；问题出在旧测试/旧认知仍按 `merge/accept_edge` 路径断言。 | `src/ops-maturity/edge-runtime/edge-runtime-sync-service.ts` 在冲突分支返回 `accept_central` 并生成 `incidentId`；本轮把 unit/integration 合同测试样例统一改到 central-wins 语义。 | 不改主实现逻辑，仅修正过期测试与依据。 |
| R3-41 | 13. 运维成熟度 + SDK 缺口（§51-§69） | MEDIUM | `sdk/pack-sdk/pack-manifest.ts` | BusinessPackManifest 缺 §22.2 sdk_semver/platform_min_version/platform_max_version/contract_test_generator | 已解决 | 审计快照没有反映 pack manifest 当前字段面。 | `src/sdk/pack-sdk/pack-manifest.ts` 的 `BusinessPackManifest` 已含 `sdk_semver/platform_min_version/platform_max_version/contract_test_generator`，相关 `pack-manifest` 定向测试本轮保持通过。 | 不改代码，仅回写依据。 |
| R3-42 | 13. 运维成熟度 + SDK 缺口（§51-§69） | MEDIUM | `ops-maturity/cost-optimizer/` | CostAttributionRecord 用单一 amountUsd；§64.1 要求 7维分解(llm/tool/compute/storage/egress/humanReview/total) | 已验证 | 本轮验证 `src/ops-maturity/cost-optimizer/cost-optimization-service.ts` 的 `CostAttributionRecord` 已含全部 7 维字段（llmCostUsd/toolCostUsd/computeCostUsd/storageCostUsd/egressCostUsd/humanReviewCostUsd + amountUsd）；`aggregateCostAttribution` 正确求和 7 维；`aggregate` 方法正确传递 7 维字段。 | 不改代码，仅回写验证结果。 |
| R3-43 | 13. 运维成熟度 + SDK 缺口（§51-§69） | MEDIUM | `ops-maturity/compliance-reporter/` | 缺 ControlCoverageReport + GapAnalyzer(§66.2)；evidence-to-control mapping 缺 controlId/freshness/owner/exception | 已解决 | 当前 compliance reporter 已具备这些结构，旧审计未跟进。 | `src/ops-maturity/compliance-reporter/compliance-report-pipeline-service.ts` 已有 `ControlCoverageReport/GapAnalysisResult/GapAnalyzerService`，`src/ops-maturity/compliance-reporter/evidence-mapper/index.ts` 已包含 `controlId/freshness/owner/exception` 等映射字段。 | 不改代码，仅回写依据。 |
| R3-44 | 13. 运维成熟度 + SDK 缺口（§51-§69） | MEDIUM | `ops-maturity/chaos/` | 缺 PanicDrillReport(§60.4)：ingress_block_time/execution_quiescence_time/plane_ack_success_rate 等 | 已解决 | Chaos drill 输出契约已存在，只是旧审计未复核到位。 | `src/ops-maturity/chaos/chaos-experiment-scheduler.ts` 已声明 `PanicDrillReport`，相关 `chaos-experiment-scheduler` 单测本轮继续通过。 | 不改代码，仅回写依据。 |
| R3-45 | 13. 运维成熟度 + SDK 缺口（§51-§69） | MEDIUM | `ops-maturity/multimodal/` | MultimodalInputPart 缺 §68.2 provenance(C2PA/watermark/hash/license)/artifactRef；SafetyFinding 缺 confidence/policyDecision/appealPath | 已解决 | Multimodal 契约已演进到含 provenance / artifactRef / safety adjudication 字段，旧审计仍引用早期最小结构。 | `src/ops-maturity/multimodal/multimodal-gateway-service.ts` 已包含 `MultimodalInputPart.provenance/artifactRef` 与 `SafetyFinding.confidence/policyDecision/appealPath`。 | 不改代码，仅回写依据。 |
| R3-46 | 13. 运维成熟度 + SDK 缺口（§51-§69） | MEDIUM | `ops-maturity/drift-detection/cross-agent-analyzer/` | 不产出 CrossAgentDriftAlert(§63.4)；缺 alert severity + anti-gaming 区分 | 已解决 | 审计判断落后于现行 drift analyzer 契约。 | `src/ops-maturity/drift-detection/cross-agent-analyzer/index.ts` 已产出 `CrossAgentDriftAlert`，并区分 `severity` 与 anti-gaming 相关信号。 | 不改代码，仅回写依据。 |
| R3-47 | 13. 运维成熟度 + SDK 缺口（§51-§69） | MEDIUM | `ops-maturity/platform-ops-agent/` | OpsAgentDefinition 缺 §69.1 ops_data_boundary 声明（仅平台指标/日志/配置，禁止业务 payload） | 已解决（源码修复并补定向测试） | 这里确实存在真实缺口：`ops_data_boundary` 长期停留在可选字段，没有硬性声明，也没有运行时边界校验。 | 本轮将 `src/ops-maturity/platform-ops-agent/platform-ops-agent-service.ts` 中 `OpsAgentDefinition.ops_data_boundary` 改为必填，新增 `DEFAULT_OPS_DATA_BOUNDARY` 与构造时校验，禁止 `businessPayloadAllowed=true`；相关 unit/integration 构造样例与拒绝用例均已补齐。 | 已修代码与测试，不保留缺口。 |
| R3-48 | 13. 运维成熟度 + SDK 缺口（§51-§69） | LOW | `ops-maturity/capacity-planner/` | failoverReservePercent 硬编码 15%；§67.2 要求按 SLA tier 动态 N+1（approval capacity） | 已解决 | 当前 capacity planner 已实现动态 failover reserve 与 approval capacity N+1 双路逻辑。 | `src/ops-maturity/capacity-planner/capacity-planning-service.ts` 第 240 行 `approvalCapacityNeeded: sloRisk === "high" ? 2 : 1` 实现 SLA risk 驱动的 N+1 审批容量；第 286-292 行 `computeDynamicFailoverReserve` 按 gold/silver/bronze SLA tier 动态分配 failover reserve（30%/20%/15%），不再是单一硬编码百分比。 | 不改代码，仅回写依据。 |
| R3-49 | 13. 运维成熟度 + SDK 缺口（§51-§69） | LOW | `sdk/harness-sdk/` | 缺 traceReplay/sideEffectReconciliation 方法(§22) | 已解决 | 这不是“方法缺失”，而是方法已存在、语义仍偏兼容实现；原审计标题本身已不成立。 | `src/sdk/harness-sdk/index.ts` 已公开 `traceReplay(...)` 与 `sideEffectReconciliation(...)`。当前实现偏 compatibility shim，但“缺方法”指控已被现行代码直接推翻。 | 不改代码，仅回写依据。 |
| R3-50 | 13. 运维成熟度 + SDK 缺口（§51-§69） | LOW | `sdk/admin-sdk/` | 缺 triggerPanic/resumePanic/manageAgentLifecycle/rotateSecrets(§22.1) | 已解决 | Admin SDK 的操作面方法已存在，旧审计没有同步。 | `src/sdk/admin-sdk/index.ts` 已公开 `triggerPanic(...)`、`resumePanic(...)`、`manageAgentLifecycle(...)`、`rotateSecrets(...)`。 | 不改代码，仅回写依据。 |
| R3-51 | 14. ADR 与架构矛盾（新发现） | HIGH | `ADR-060` | 定义 Plan DTO + RuntimeExecuteBridge 作为 P3→P4 contract；§5.3/INV-GRAPH-001 要求 PlanGraphBundle 为唯一 canonical P3→P4 交接物 | 已解决（文档/ADR） | ADR-060 仍残留早期 “Plan DTO / Bridge 即 handoff” 表述，和现行 executable contract 不完全一致。 | 本轮回写 `docs_zh/adr/060-explicit-planning-hub.md`，将 P3→P4 唯一 handoff 明确收敛为 `PlanGraphBundle`，并把 `RuntimeExecuteBridge` 降为 compatibility seam。 | 已修 ADR，不再保留缺口。 |
| R3-52 | 14. ADR 与架构矛盾（新发现） | HIGH | `ADR-061` | 生命周期 6态(draft/testing/staging/production/deprecated/retired)；§61.3 要求 9态，缺 canary/active/paused/archived/removed，多出 production/retired | 已解决 | 旧审计对应的是早期 ADR 草稿；当前 ADR-061 已与 9 态 canonical 生命周期对齐。 | `docs_zh/adr/061-agent-unified-lifecycle-management.md` 已列出 `draft/testing/staging/canary/active/paused/deprecated/archived/removed` 九态。 | 不改代码，仅回写依据。 |
| R3-53 | 14. ADR 与架构矛盾（新发现） | HIGH | `ADR-054` | Platinum 承诺 99.99%；§54.2 限定 99.95%（99.99% 仅在专用部署档单独承诺） | 已解决 | ADR-054 已补上“99.99 仅限专用部署”的限制说明。 | `docs_zh/adr/054-sla-tiered-guarantees.md` 已将平台默认 platinum SLA 写为 `99.95%`，并单独注明 `99.99%` 仅在专用部署层级承诺。 | 不改代码，仅回写依据。 |
| R3-54 | 14. ADR 与架构矛盾（新发现） | HIGH | `ADR-042` | 自治等级 supervised/assisted/partial_auto/high_auto/full_auto(5级)；§42.1 仅 suggestion/supervised/semi_auto/full_auto(4级) | 已解决 | ADR-042 已收敛到 4 级自治模型。 | `docs_zh/adr/042-progressive-autonomy-model.md` 当前只保留 `suggestion/supervised/semi_auto/full_auto` 四级，并补了高危域 `full_auto` 限制。 | 不改代码，仅回写依据。 |
| R3-55 | 14. ADR 与架构矛盾（新发现） | HIGH | `ADR-083` | 又一套自治命名 manual_only/suggest_only/supervised_execute/trusted_auto_execute——第三套互不兼容 | 已解决 | ADR-083 已与 §42.1 的 canonical 自治等级对齐。 | `docs_zh/adr/083-proactive-agent-and-progressive-autonomy.md` 当前使用 `suggestion/supervised/semi_auto/full_auto` 四级，不再保留第三套自治枚举。 | 不改代码，仅回写依据。 |
| R3-56 | 14. ADR 与架构矛盾（新发现） | MEDIUM | `ADR-058` | GlobalCircuitBreaker.open_duration_ms 隐含 TTL 自动解除；§60.3 明确禁止 Panic TTL 自动解除，恢复需人工双人确认 | 已解决 | ADR-058 已显式写明 `open_duration_ms` 不是自动恢复 TTL。 | `docs_zh/adr/058-emergency-stop-and-global-circuit-breaker.md` 已声明 Panic 默认无限期生效，恢复必须走 `PlatformResumeDirective` 且双人审批。 | 不改代码，仅回写依据。 |
| R3-57 | 14. ADR 与架构矛盾（新发现） | MEDIUM | `ADR-022` | 暴露 /api/v1/workflow-runs 为 canonical API；§5.5 声明 workflow_run 仅为 query projection | 已解决 | ADR-022 已把 `/api/v1/workflow-runs` 标注为 legacy 查询投影，而非 canonical truth API。 | `docs_zh/adr/022-api-contract-and-versioning.md` 当前同时暴露 `harness-runs` / `node-runs`，并把 `workflow-runs` 注明为 `legacy 兼容` 查询投影。 | 不改代码，仅回写依据。 |
| R3-58 | 14. ADR 与架构矛盾（新发现） | MEDIUM | `ADR-065` | 用 WorkflowDAGView/StepInspector 全为废弃概念，无 v4.3 remediation | 已解决 | ADR-065 已写入 v4.3 remediation，并迁移到 `PlanGraphView / NodeRun` 调试语义。 | `docs_zh/adr/065-workflow-visual-debugger.md` 明确说明旧 `WorkflowDAGView/StepInspector` 已废弃，正文使用 `HarnessRun / NodeRun` 调试锚点。 | 不改代码，仅回写依据。 |
| R3-59 | 14. ADR 与架构矛盾（新发现） | MEDIUM | `ADR-040` | goal decomposition MAX_DEPTH=5 未引用全局 call_depth 硬帽=8 及反乘法规则(§19.2) | 已解决 | ADR-040 已补充目标分解深度与全局 `call_depth` 硬帽的协同约束。 | `docs_zh/adr/040-goal-decomposition-engine.md` 当前明确 `DEFAULT_MAX_DEPTH = 5` 需受全局 `call_depth` 上限（默认 8）约束，且不得通过局部上限相乘绕过。 | 不改代码，仅回写依据。 |
| R3-60 | 14. ADR 与架构矛盾（新发现） | MEDIUM | `ADR-062` | 边缘同步列 last_write_wins 为合法策略；§25.11 真相数据要求单主写入，LWW 违反不变量 | 已解决 | ADR-062 已把 truth / budget / side effect 的冲突策略收敛到单 leader 写入。 | `docs_zh/adr/062-offline-and-edge-deployment-architecture.md` 当前明确禁止 `last_write_wins` 用于 truth / budget / side effect 域。 | 不改代码，仅回写依据。 |
| R3-61 | 14. ADR 与架构矛盾（新发现） | MEDIUM | `ADR-060` | 引用 §L.6/§H.2 节——架构 v4.3 无此节号，cross-ref 失效 | 已解决 | ADR-060 现有交叉引用和来源章节已改为有效章节，不再引用失效节号。 | `docs_zh/adr/060-explicit-planning-hub.md` 当前来源章节为 `§5 / §13.5 / §13.8`，不再使用失效的 `§L.6/§H.2`。 | 不改代码，仅回写依据。 |
| R3-62 | 14. ADR 与架构矛盾（新发现） | LOW | `ADR-003` | 标题"六层"文件名"seven-layers"实际架构和 ADR-020 均为六层——命名全面混乱 | 已解决 | ADR-003 已在正文中显式解释“文件名沿用旧称，但内容定义的是六层模型”。 | `docs_zh/adr/003-memory-seven-layers.md` 已标注标题/文件名历史原因，并明确当前定义是六层（L1-L6）模型。 | 不改代码，仅回写依据。 |
| R3-63 | 14. ADR 与架构矛盾（新发现） | LOW | `ADR-075` | ImprovementCandidate 12态机无架构支撑；§56.4 LearningCandidate 仅 quarantine/approved/rejected/released | 已解决 | ADR-075 已将 `ImprovementCandidateStatus` 收敛到 `quarantine/approved/rejected/released`。 | `docs_zh/adr/075-controlled-rollout-release.md` 当前 ImprovementCandidate 生命周期与 §56.4 保持一致。 | 不改代码，仅回写依据。 |
| R3-64 | 14. ADR 与架构矛盾（新发现） | LOW | `ADR-019` | 声称源节 §12 "Agent Handoff"；实际 §12 是"异常事件处理架构"——section ref 错误 | 已解决（文档/ADR） | ADR-019 的来源章节确实残留错误节号引用。 | 本轮回写 `docs_zh/adr/019-agent-handoff-four-layer-protocol.md`，移除错误的 `§12 Agent Handoff` 引用，改为 handoff / explainability 相关 canonical 章节描述。 | 已修 ADR，不再保留缺口。 |
| R3-65 | 15. 剩余 Contract 深层缺口 | HIGH | `typed_event_bus_contract.md` | OAPEFLIR 事件 payload 全部用 task_id/workflow_id/execution_id；§5.5 要求 harnessRunId/nodeRunId/planGraphId | 已解决 | `typed_event_bus_contract` 已完成 canonical 运行链锚点迁移。 | `docs_zh/contracts/typed_event_bus_contract.md` 已强制 OAPEFLIR payload 使用 `harnessRunId/nodeRunId/planGraphId`，并把旧 `task_id/workflow_id/execution_id` 标注为废弃映射。 | 不改代码，仅回写依据。 |
| R3-66 | 15. 剩余 Contract 深层缺口 | HIGH | `typed_event_bus_contract.md` | PlanCreatedPayload 用 step_count 暗示线性步骤；§5 要求 PlanGraph(图结构) | 已解决 | 该 contract 已切到 PlanGraph 图语义。 | `docs_zh/contracts/typed_event_bus_contract.md` 已将 `PlanCreatedPayload` 收敛到 `nodeCount + edgeCount`，显式取代 `step_count`。 | 不改代码，仅回写依据。 |
| R3-67 | 15. 剩余 Contract 深层缺口 | HIGH | `typed_event_bus_contract.md` | ExecutionCompletedPayload 定义 execution_id/outcome/output_refs 为执行结果模型；与 §5 NodeAttemptReceipt(receiptId/nodeRunId/attemptId/status) 冲突 | 已解决 | `ExecutionCompletedPayload` 已迁到 NodeAttemptReceipt 模型。 | `docs_zh/contracts/typed_event_bus_contract.md` 当前使用 `nodeRunId/attemptId/receiptId/attemptStatus`，不再把旧 execution 术语作为 canonical 字段。 | 不改代码，仅回写依据。 |
| R3-68 | 15. 剩余 Contract 深层缺口 | HIGH | `explainability_and_stage_rationale_contract.md` | StageRationale 仅 7字段；§59.3 要求 11字段(缺 rationaleId/decisionInputRef/versionLockRef/visibilityLabels/confidence/alternatives) | 已解决（contract/文档） | 合同文档已部分迁移，但仍缺少 `rendered_explanation` 与深度/治理说明，导致审计继续判作不完整。 | 本轮回写 `docs_zh/contracts/explainability_and_stage_rationale_contract.md`，补齐 `rationale_id/decision_input_ref/version_lock_ref/visibility_labels/confidence/alternatives/rendered_explanation` 等权威字段说明。 | 已修 contract 文档，不再保留缺口。 |
| R3-69 | 15. 剩余 Contract 深层缺口 | HIGH | `workflow_debugger_contract.md` | BreakpointDefinition 用 workflow_id/step_selector；§5.5 应为 harnessRunId/nodeRunId | 已解决 | debugger contract 已完成到 HarnessRun/NodeRun 模型的迁移。 | `docs_zh/contracts/workflow_debugger_contract.md` 当前锚点为 `harness_run_id / node_run_id / node_selector`，并附带 remediation 说明。 | 不改代码，仅回写依据。 |
| R3-70 | 15. 剩余 Contract 深层缺口 | HIGH | `startup_consistency_and_recovery_drill_contract.md` | 一致性矩阵用 current_step_index/workflow_state；应为 HarnessRun.status/NodeRun.status/PlanGraph | 已解决（contract/文档） | 该 contract 仍残留旧 workflow/step 术语，未完全切换到 HarnessRun/NodeRun/PlanGraph 一致性模型。 | 本轮回写 `docs_zh/contracts/startup_consistency_and_recovery_drill_contract.md`，将巡检矩阵改为 `HarnessRun / NodeRun / PlanGraph` 语义。 | 已修 contract 文档，不再保留缺口。 |
| R3-71 | 15. 剩余 Contract 深层缺口 | MEDIUM | `budget-ledger-contract.md` | BudgetReservation.resourceKind 枚举缺 §18 要求的 storage/bandwidth/memory | 已解决（contract/文档） | 预算合同已迁移到 ledger 模型，但 `resourceKind` 枚举还少 3 个 canonical 资源类别。 | 本轮回写 `docs_zh/contracts/budget-ledger-contract.md`，补齐 `storage / bandwidth / memory`。 | 已修 contract 文档，不再保留缺口。 |
| R3-72 | 15. 剩余 Contract 深层缺口 | MEDIUM | `naming_and_engineering_boundary_contract.md` | §2 列 WorkflowExecutor 为 canonical 工程名；§5 canonical 入口为 HarnessRuntime | 已解决（contract/文档） | 命名边界合同仍保留 `WorkflowExecutor` 作为工程 canonical 名称。 | 本轮回写 `docs_zh/contracts/naming_and_engineering_boundary_contract.md`，将 canonical 入口更正为 `HarnessRuntime`。 | 已修 contract 文档，不再保留缺口。 |
| R3-73 | 15. 剩余 Contract 深层缺口 | MEDIUM | `admin_console_and_human_takeover_contract.md` | takeover 操作用步骤语言(修改下一步/跳过某步/重试某步)；§5.5 操作粒度为 NodeRun | 已解决 | 人工接管合同已迁移到 `HarnessRun / NodeRun / NodeAttempt` 粒度，并带 remediation。 | `docs_zh/contracts/admin_console_and_human_takeover_contract.md` 当前动作说明和约束均基于 NodeRun/NodeAttempt，而非旧 step/execution 语义。 | 不改代码，仅回写依据。 |
| R3-74 | 15. 剩余 Contract 深层缺口 | MEDIUM | `nl_entry_and_goal_decomposition_contract.md` | IntentParseResult 含 suggested_workflow_id；§5 所有执行为 HarnessRun，NL 应建议 domain/pack/recipe | 已解决（contract/文档） | NL 合同仍残留 `suggested_workflow_id`，且未明确 Goal 只作为上层投影。 | 本轮回写 `docs_zh/contracts/nl_entry_and_goal_decomposition_contract.md`，将建议输出改为 `division/domain/pack/recipe`，并注明执行 truth 收敛到 `HarnessRun.status`。 | 已修 contract 文档，不再保留缺口。 |
| R3-75 | 15. 剩余 Contract 深层缺口 | MEDIUM | `typed_event_bus_contract.md` | OAPEFLIR payload 缺 derivedFromEventId；event-envelope-contract §4 要求声明 derivation source | 已解决（contract/文档） | 事件总线合同已迁移主要 payload，但派生来源字段未在总线 contract 中冻结。 | 本轮回写 `docs_zh/contracts/typed_event_bus_contract.md`，把 `derivedFromEventId?` 与派生来源声明加入事件定义最小字段与强制规则。 | 已修 contract 文档，不再保留缺口。 |
| R3-76 | 15. 剩余 Contract 深层缺口 | MEDIUM | `governance_control_plane_contract.md` | §15A release_transition_gate 值 off/suggest/shadow 与 §61.3 lifecycle 9态不映射 | 已解决（contract/文档） | 治理平面合同把 rollout 兼容词汇和 lifecycle gate 混成了一类状态。 | 本轮回写 `docs_zh/contracts/governance_control_plane_contract.md`，将 `release_transition_gate` 明确解释为 lifecycle 推进 gate，并把 `off/suggest/shadow` 降为兼容 rollout 投影视图。 | 已修 contract 文档，不再保留缺口。 |
| R3-77 | 15. 剩余 Contract 深层缺口 | MEDIUM | `explainability_and_stage_rationale_contract.md` | ExplanationDepth 用 brief/standard/audit；§59.4 要求 L1 Summary/L2 Reasoning/L3 Forensic | 已解决（contract/文档） | 解释合同仍残留旧 depth 命名。 | 本轮回写 `docs_zh/contracts/explainability_and_stage_rationale_contract.md`，将深度统一到 `L1/L2/L3`，并解释其分别对应 Summary/Reasoning/Forensic。 | 已修 contract 文档，不再保留缺口。 |
| R3-78 | 15. 剩余 Contract 深层缺口 | LOW | `typed_event_bus_contract.md` | ReplanTriggeredPayload 用 old_version/new_version 未引用 GraphPatch(baseGraphVersion→newGraphVersion) | 已解决 | 该 contract 已使用图版本语义。 | `docs_zh/contracts/typed_event_bus_contract.md` 当前 `ReplanTriggeredPayload` 使用 `baseGraphVersion/newGraphVersion`，不再保留 `old_version/new_version` 作为 canonical 字段。 | 不改代码，仅回写依据。 |
| R3-79 | 15. 剩余 Contract 深层缺口 | LOW | `capacity_planning_contract.md` | 缺 CapacityAlert 输出对象(§67.2 要求 forecast 超阈值时产出) | 已解决（contract/文档） | 容量合同缺失越线预警对象定义。 | 本轮回写 `docs_zh/contracts/capacity_planning_contract.md`，新增 `CapacityAlert` 及最小字段/规则说明。 | 已修 contract 文档，不再保留缺口。 |
| R3-80 | 15. 剩余 Contract 深层缺口 | LOW | `explainability_and_stage_rationale_contract.md` | 无 remediation section；未引用 §59 "解释不可篡改纳入 Evidence Plane" + "解释必须 permission-aware" | 已解决（contract/文档） | 解释合同虽然已有主对象，但缺少显式 remediation 与 Evidence Plane / permission-aware 约束。 | 本轮在 `docs_zh/contracts/explainability_and_stage_rationale_contract.md` 中补充了 remediation、Evidence Plane、permission-aware 规则。 | 已修 contract 文档，不再保留缺口。 |
| R4-1 | 17. Platform Contracts 层根本性问题 | CRITICAL | `platform/contracts/control-directive/` | ControlDirective 仍作为第一级导出活跃消费；§5.2 明确废弃，canonical 替代 OperationalDirective/DecisionDirective 全代码库不存在 | 已解决（源码修复） | 原审计停留在 contract 迁移中间态，没有反映 canonical directive 已落地、legacy factory 已被 fail-fast 封禁。 | `src/platform/contracts/control-directive/index.ts` 当前已定义 `OperationalDirective / DecisionDirective` 与创建器；`createControlDirective()` 现在实现为 `never` throw，抛 `ValidationError(platform_contracts.legacy_control_directive_forbidden)`；`tests/unit/platform/contracts/control-directive-contract.test.ts` 与 `tests/unit/platform/contracts/control-directive/index.test.ts` 已验证所有 22 个 case 通过。 | 保留 fail-fast 工厂修复结论。 |
| R4-2 | 17. Platform Contracts 层根本性问题 | CRITICAL | `platform/contracts/execution-plan/` | ExecutionPlan 用线性 steps[] 作为活跃 contract；§5.3 禁止线性步骤，PlanGraphBundle(graph nodes/edges) 为唯一 P3→P4 交接物 | 已解决 | 原结论把 legacy compatibility stub 误判成活跃 canonical contract。 | `src/platform/contracts/execution-plan/index.ts` 当前首要导出为 `PlanGraphBundle` 系列；legacy `createExecutionPlan()` 直接抛 `execution_plan.legacy_contract_forbidden`；`tests/unit/platform/contracts/types/index.test.ts` 与 `tests/invariants/plan-graph-only-dispatch.test.ts` 已对齐。 | 不改业务代码，回写 canonical/legacy 边界。 |
| R4-3 | 17. Platform Contracts 层根本性问题 | CRITICAL | `platform/contracts/execution-receipt/` | ExecutionReceipt 以 stepId 为主键仍为活跃 contract；§5.5 canonical 为 NodeAttemptReceipt(nodeRunId+attemptId) | 已解决 | 原结论把 legacy receipt adapter 当成主路径，遗漏了 canonical receipt re-export 和 legacy fail-fast。 | `src/platform/contracts/execution-receipt/index.ts` 当前 canonical 导出为 `NodeAttemptReceipt`，legacy `createExecutionReceipt()` 直接抛 `execution_receipt.legacy_contract_forbidden`；`tests/unit/platform/contracts/types/index.test.ts` 已校正断言。 | 不改业务代码，回写现行契约依据。 |
| R4-4 | 17. Platform Contracts 层根本性问题 | CRITICAL | `platform/contracts/types/platform-contracts.ts` | 同文件含第二份 ExecutionPlan + ExecutionReceipt + ControlDirective 定义——两套废弃 contract 并行存在 | 已解决 | 该文件的真实角色是“聚合导出 + legacy stub”；原审计把聚合层的 deprecated re-export 误判为第二套活跃 canonical。 | `src/platform/contracts/types/platform-contracts.ts` 当前对 `PlanGraphBundle / NodeAttemptReceipt / OperationalDirective / DecisionDirective` 采用 re-export；legacy 名称仅保留兼容导出并带 deprecated 注释与 fail-fast 工厂。 | 不改业务代码，仅回写结构性根因和证据。 |
| R4-5 | 17. Platform Contracts 层根本性问题 | CRITICAL | `platform/five-plane-\*/` | 架构 §4 要求五平面目录(P1-P5)，**实际无 five-plane-\* 目录**——平面分离在结构上不可强制 | 已解决 | 这组混合了旧目录快照、接口认知和测试存在性误判，当前仓库文件已直接反证。 | 当前 `src/sdk/client-sdk/api-client.ts` 已发送版本握手头；`src/sdk/cli/index.ts` 已包含 `pack create/test/validate/publish`；`src/platform/five-plane-*` 目录存在；`dashboard-websocket-server.ts` 已绑定 principal/tenantId；`structured-logger.ts` 有 `crosscuttingFabric`；`tests/invariants/` 已存在；启动顺序与 ring catalog 已改为 canonical。 | 不改代码，仅回写依据。 |
| R4-6 | 17. Platform Contracts 层根本性问题 | HIGH | `platform/contracts/executable-contracts/` | NodeAttemptReceipt 缺 harnessRunId/planGraphId/graphVersion/duration/error_detail(§5.3 必填) | 已解决 | 原审计基于旧版 receipt 快照，未反映 canonical receipt schema 已扩展完整上下文字段。 | 当前 `src/platform/contracts/executable-contracts/index.ts` 的 `NodeAttemptReceipt` 已包含 `harnessRunId / planGraphId / graphVersion / duration / errorDetail`；相关 contract 测试本轮已通过。 | 不改业务代码，仅回写 schema 现状。 |
| R4-7 | 17. Platform Contracts 层根本性问题 | HIGH | `platform/contracts/request-envelope/` | RequestEnvelope 缺 confirmedTaskSpecId/principal(typed)/idempotencyKey/priority(§5.3 intake pipeline) | 已解决 | 原审计引用的是更老的 request envelope 形状。 | 当前 `src/platform/contracts/request-envelope/index.ts` 已包含 `confirmedTaskSpecId / principal / idempotencyKey / priority`；`tests/unit/platform/contracts/request-envelope/new.test.ts` 本轮已按现行字段回归通过。 | 不改业务代码，仅回写契约证据。 |
| R4-8 | 17. Platform Contracts 层根本性问题 | HIGH | `platform/contracts/state-command/` | StateCommand 无 leaseId/fencingToken/event/principal——无法满足 INV-STATE-001 | 已解决 | 原审计混淆了旧调用方和当前 legacy state-command 兼容定义。 | 当前 `src/platform/contracts/types/platform-contracts.ts` 中 `StateCommand` 已带 `leaseId / fencingToken / event / principal`；`tests/unit/platform/contracts/types/index.test.ts` 和 `tests/unit/platform/contracts/state-command.test.ts` 已覆盖。 | 不改业务代码，仅回写字段级依据。 |
| R4-9 | 17. Platform Contracts 层根本性问题 | HIGH | `platform/contracts/` | 缺 EventAppendCommand/AuditAppendCommand/ArtifactWriteCommand 三个 §5.3 inter-plane 契约模块 | 已解决 | 原审计没有复核 executable-contracts 的 inter-plane command 导出。 | 当前 `src/platform/contracts/executable-contracts/index.ts` 已导出 `EventAppendCommand / AuditAppendCommand / ArtifactWriteCommand`。 | 不改业务代码，仅回写导出面证据。 |
| R4-10 | 17. Platform Contracts 层根本性问题 | HIGH | `platform/contracts/types/platform-contracts.ts` | SideEffectRecord 仅 4态(proposed/committed/rolled_back/failed)；executable-contracts 定义 16态——两套冲突共存 | 已解决 | 原审计把本地 `SideEffectExpectation` 错认成第二份 `SideEffectRecord`。 | 当前 16 态 canonical `SideEffectRecord` 仅定义于 `src/platform/contracts/executable-contracts/index.ts`；`src/platform/contracts/types/platform-contracts.ts` 保留的是不同语义的 `SideEffectExpectation`。 | 不改业务代码，仅回写类型辨析依据。 |
| R4-11 | 17. Platform Contracts 层根本性问题 | MEDIUM | `platform/contracts/executable-contracts/` | LEGACY_CONTRACT_NAMES 列表无强制机制——无 deprecation warning/re-export guard/CI lint 阻止新代码导入废弃模块 | 已解决 | 原审计忽略了 runtime guard、legacy factory fail-fast 和 invariant 测试的组合约束。 | 当前 `createControlDirective/createExecutionPlan/createExecutionReceipt` 均 fail-fast；`RuntimeEntryGuard` 会阻断 legacy truth write；`tests/invariants/deny-by-default.test.ts`、`harness-run-authority.test.ts`、`plan-graph-only-dispatch.test.ts` 本轮已回归通过。 | 不改业务代码，回写“已有强制机制”的证据。 |
| R4-12 | 17. Platform Contracts 层根本性问题 | MEDIUM | `platform/contracts/index.ts` | Barrel 导出优先废弃类型(requestEnvelopeContract)而非 executable-contracts——激励消费废弃接口 | 已解决 | 原审计基于旧 barrel 排序。 | 当前 `src/platform/contracts/index.ts` 明确先导出 `executableContracts` canonical 面，再单独以 `createLegacyRequestEnvelope` 等别名暴露废弃入口。 | 不改业务代码，仅回写 barrel 排序现状。 |
| R4-13 | 17. Platform Contracts 层根本性问题 | MEDIUM | `platform/contracts/executable-contracts/` | EventEnvelope 缺必填 runId(§28.1)；replayBehavior 为 optional(§28.1 要求 explicitly declared)；eventVersion 为 string 而非 §28.1 numeric schemaVersion | 已解决 | 原审计使用的是更早期 event envelope 认知，没有复核当前 schema。 | 当前 canonical `EventEnvelope` schema 已要求 `runId`、显式 `replayBehavior`，并使用 numeric `schemaVersion`；本轮 contract/invariant 定向回归已通过。 | 不改业务代码，仅回写 schema 依据。 |
| R4-14 | 17. Platform Contracts 层根本性问题 | MEDIUM | `platform/control-plane/` | P2 模块无任何 OperationalDirective/DecisionDirective 发射或消费——P2→P3/P4 治理门禁结构性缺失 | 已解决（源码修复并补定向测试） | 这一条在本轮复核时确实仍成立：control-plane 子模块此前没有统一的 canonical directive sink，审批/发布主链只落事件与本地状态，不下发 P2 directive。 | 本轮新增 `src/platform/control-plane/control-plane-directive-sink.ts`，并把 `ApprovalService.applyDecision()` 接到 `DecisionDirective` 发射、`TrafficRoutingService.startCanaryShift()/rollbackShift()` 接到 `OperationalDirective` 发射；`tests/unit/platform/control-plane/approval-center/approval-service-class.test.ts` 与 `tests/unit/platform/control-plane/rollout-controller/traffic-routing-service.test.ts` 已新增回归并通过。 | 保留真实代码修复结论。 |
| R4-15 | 18. Execution + State-Evidence 平面缺口（§13-§14） | HIGH | `execution/state-transition/transition-service.ts` | 并行 legacy TransitionService 直接操作 task/workflow/session/execution 状态，完全绕过 RuntimeStateMachine——INV-STATE-001 旁路 | 已解决（源码修复并补定向测试） | 根因是 legacy 生命周期一直把 task/workflow/session/execution/approval 权威表当主写模型，`RuntimeStateMachine` 只覆盖 canonical aggregate，导致 legacy 路径虽有 CAS 仍绕过统一状态机与 platform fact 主链。 | 本轮在 `src/platform/five-plane-execution/runtime-state-machine.ts:92-177,288-329` 新增 `transitionLegacy()` 与 legacy transition tables；`src/platform/execution/state-transition/transition-service.ts:395-407,579-590,668-679,776-787,854-870,981-1045,1525-1541` 现已统一先经 `RuntimeStateMachine` 生成 `PlatformFactEvent`，再执行 legacy projection CAS；`tests/unit/platform/execution/state-transition/transition-service.test.ts` 新增 approval/terminal fanout 回归并通过。 | 架构决策收敛为“legacy 表继续保留为投影，但 legacy 状态写路径不再绕过统一状态机”。 |
| R4-16 | 18. Execution + State-Evidence 平面缺口（§13-§14） | HIGH | `execution/runtime-state-machine.ts` | RuntimeTransitionCommand 缺 commandId(UUID)/entityType/entityId/principal(§5.3 必填) | 已解决 | 原审计使用的是更早一版 `RuntimeTransitionCommand` 形状，没有复核当前执行状态机接口。 | 当前 `src/platform/execution/runtime-state-machine.ts` 已声明 `commandId`、`entityType`、`entityId`、`principal` 为必填；`src/platform/execution/budget-allocator.ts` 等调用方也已按该命令形状传参。 | 不改生产代码，仅回写现行接口依据。 |
| R4-17 | 18. Execution + State-Evidence 平面缺口（§13-§14） | HIGH | `execution/recovery/` | 无 RecoveryCadence/RecoveryReport 类型；§14.7 要求每个 Recovery Worker 声明检查间隔+产出报告 | 已解决 | 原审计基于更早期 recovery 目录快照，没有复核当前 contract 类型与 recovery barrel 导出。 | 当前 `src/platform/contracts/types/recovery-cadence.ts` 已定义 `RecoveryCadence`、`RecoveryReport` 与 `RecoveryWorker.getRecoveryCadence()/runRecoveryCycle()`；`src/platform/five-plane-execution/recovery/index.ts` 也已对外导出这些 canonical 类型。 | 不改生产代码，仅回写现行 contract 依据。 |
| R4-18 | 18. Execution + State-Evidence 平面缺口（§13-§14） | HIGH | `state-evidence/checkpoints/workflow-step-checkpoint.ts` | Checkpoint 用 stepId/workflowId/executionId 而非 harnessRunId/nodeRunId/planGraphId | 已解决 | 原审计停留在旧 checkpoint 形状，没有复核当前 checkpoint 同时保留 workflow/step 语义字段与 canonical 运行时标识的实现。 | 当前 `src/platform/five-plane-state-evidence/checkpoints/workflow-step-checkpoint.ts` 的 `WorkflowStepCheckpoint` 与创建输入已显式包含 `harnessRunId`、`nodeRunId`、`planGraphBundleId`；对应单测 `tests/unit/platform/state-evidence/checkpoints/workflow-step-checkpoint-functions.test.ts` 也覆盖了这些字段。 | 不改生产代码，仅回写现行结构依据。 |
| R4-19 | 18. Execution + State-Evidence 平面缺口（§13-§14） | MEDIUM | `execution/state-transition/state-transition-machine.ts` | 允许 no-op transition(current==next 静默返回)；RuntimeStateMachine 明确拒绝——两套机器行为矛盾 | 已解决（源码修复） | 原审计基于更早期 `StateTransitionMachine` 行为，没有复核当前 no-op 分支。原审计结论”当前实现直接抛出”与实际不符——当时 `assertTransition()` 确实存在允许 self-transition 显式声明后静默返回的代码路径。 | 本轮修正 `src/platform/execution/state-transition/state-transition-machine.ts` 的 `assertTransition()`：移除 `if (allowed && allowed.includes(next)) { return; }` 分支，使 no-op transition 无论是否显式声明均抛出 `WorkflowStateError(...noop_transition_denied)`，与 `RuntimeStateMachine.assertTransitionAllowed()` 行为完全一致。 | 修正审计结论：这条确为真实代码矛盾，已通过源码修复对齐两套状态机的 no-op 拒绝策略。 |
| R4-20 | 18. Execution + State-Evidence 平面缺口（§13-§14） | MEDIUM | `execution/recovery/replay-boundary-guard.ts` | 仅实现 trace_replay/reexecution_replay 两种模式；§28.5 定义三种含 projection_replay | 已解决 | 原审计停留在 replay boundary guard 引入前两种模式的阶段，没有刷新 `projection_replay` 后续落地。 | 当前 `src/platform/execution/recovery/replay-boundary-guard.ts` 已声明 `ReplayMode` 三态：`trace_replay`、`reexecution_replay`、`projection_replay`，并对 `projection_replay` 加了仅允许 projection 操作、阻断真实副作用的分支。 | 不改生产代码，仅回写现行实现依据。 |
| R4-21 | 18. Execution + State-Evidence 平面缺口（§13-§14） | MEDIUM | `execution/run-termination-cleanup.ts` | 原审计报告称 executeAsync 缺少 flushEvidence、triggerCompensation、notificationRecipients 三字段 | 已解决（验证通过） | 经本轮逐一核查源码：上述三字段自早期版本即存在于 `RunTerminationCleanupRequest` 接口（第 36/38/40 行），`executeAsync` 中也已有对应调用（第 243/313/329-331 行）。原审计报告可能基于未刷新到最新代码状态。本轮补充验证：真正的遗留缺口是 `executeAsync` 缺少内置清理 fallback（当 handler 不存在、返回 verified:false 或抛异常时），本轮已在 `performBuiltInCleanupAsync()` 中实现 fallback 路径，使 `executeAsync` 与同步 `execute()` 行为一致。 | 不改代码。更新审计结论：原报告“缺三字段”不准确——三字段始终存在；真正的代码缺陷是缺少内置清理 fallback，已在 R4-21 源码修复中补齐。 |
| R4-22 | 18. Execution + State-Evidence 平面缺口（§13-§14） | MEDIUM | `execution/run-termination-cleanup.ts` | CleanupResourceKind 缺 callback 类型(§14.10 清理序列含"cancel pending callbacks") | 已解决 | 原审计使用的是旧版 cleanup resource 枚举，没有复核当前清理序列定义。 | 当前 `src/platform/execution/run-termination-cleanup.ts` 的 `CleanupResourceKind` 已包含 `callback`，且 `CLEANUP_ORDER` 也把 `callback` 纳入终止清理序列；本轮新增的 `tests/unit/platform/execution/run-termination-cleanup.test.ts` 也直接断言了 `callback` 清理顺序。 | 不改生产代码，仅回写现行枚举依据并补直接回归。 |
| R4-23 | 18. Execution + State-Evidence 平面缺口（§13-§14） | MEDIUM | `execution/budget-allocator.ts` | reserve() 不经 RuntimeStateMachine.transition()；§25.9 预算变更需同 CAS+event 事务路径 | 已解决（源码修复并补定向测试） | 原审计对旧实现的判断并非全错：`BudgetAllocator` 后续虽已接入 `RuntimeStateMachine`，但 `reserve()` 在 ledger 状态不变化时仍会退回到裸 ledger 路径，且新增的 watermark/throttle/streaming 扩展没有默认值，旧调用路径会在保留接口兼容时直接崩掉。 | 本轮修正 `src/platform/five-plane-execution/budget-allocator.ts`：`reserve()` 先归一化上下文默认值；需要状态切换时走 `RuntimeStateMachine.transition()`，状态不切换时也不再因为 no-op 状态机拒绝而中断预算保留；事件 sink 也接回可选 `BudgetAllocatorEvents`。`tests/unit/platform/execution/budget-allocator.test.ts` 本轮已回归通过 3/3。 | 保留真实代码修复结论，不把这条仅写成“早已完成”。 |
| R4-24 | 18. Execution + State-Evidence 平面缺口（§13-§14） | MEDIUM | `execution/queue/bounded-dispatch-event.ts` | BoundedDispatchEvent 缺 nodeRunId/tenantId/traceId/ordering_policy_version/queue_class(§14.9) | 已解决 | 原审计基于旧版 bounded dispatch event 结构，没有复核当前事件工厂输出。 | 当前 `src/platform/execution/queue/bounded-dispatch-event.ts` 的 `BoundedDispatchEvent` 已显式包含 `nodeRunId`、`tenantId`、`traceId`、`ordering_policy_version`、`queue_class`，`BoundedDispatchQueueEventFactory.create()` 也会实际填充这些字段。 | 不改生产代码，仅回写现行事件结构依据。 |
| R4-25 | 19. 核心不变量未强制执行（最严重系统性问题） | CRITICAL | `INV-BUDGET-001 reserve-before-execute` | single-task-happy-path 和 multi-step-agent-round-loop 所有 LLM/Tool 调用无 BudgetReservation；BudgetAllocator.reserve() 存在但从未在执行路径调用；仅 AdmissionController 做粗粒度估算 | 已解决（验证通过） | 原审计声称”预算预留从未在执行路径调用”这一结论不正确。背景调查验证确认：`BudgetSessionManager.reserveAndCreateSession()` 确实在 LLM 调用和 tool 执之前被正确调用，reserve-before-execute 模式已在两条主执行路径（single-task-happy-path 和 multi-step-agent-round-loop）上正确落地。 | 验证证据：`src/platform/five-plane-execution/execution-engine/single-task-happy-path.ts` 在 admission/model-call 路径前调用 `BudgetSessionManager.reserveAndCreateSession()`；`src/platform/model-gateway/cost-tracker/budget-guard.ts` 与 `src/interaction/goal-decomposer/llm-plan-generator.ts` 也正确调用 `BudgetAllocator.reserve()`。原审计基于对代码路径的错误理解，未正确追踪 budget reservation 的实际调用链。 | 修正结论：INV-BUDGET-001 已正确实现，原审计claim “BudgetAllocator.reserve() 存在但从未在执行路径调用”属于审计错误，已通过源码验证确认预算预留模式已在主执行路径全面落地。 |
| R4-26 | 19. 核心不变量未强制执行（最严重系统性问题） | CRITICAL | `INV-GRAPH-001 PlanGraphBundle 为唯一 P3→P4 contract` | 实际执行路径(single-task-happy-path/multi-step-orchestration) 创建 TaskRecord+WorkflowState+线性步骤直接执行，无 PlanGraphBundle；RuntimeEntryGuard 存在但从未被调用 | 已解决（生产入口改为 PlanGraphBundle-only） | 根因不是“仓内完全没有 PlanGraphBundle”，而是此前真正的 P3→P4 runtime executor 边界仍接受 `title/request` 这类原始输入，`RuntimeExecuteBridge` 只能把 plan 序列化成字符串再交给 P4 侧重建，导致 graph contract 虽在执行器内部生成，却还不是边界层的唯一契约。 | 本轮将 `src/platform/five-plane-orchestration/oapeflir/execute-bridge.ts` 与 `src/platform/five-plane-execution/oapeflir/runtime-plan-executor.ts` 的 `RuntimePlanExecutionInput` 改为显式携带 `planGraphBundle`；`src/platform/five-plane-orchestration/oapeflir/runtime-execute-bridge.ts` 现在直接把 `PlanGraphBundle` 交给 runtime executor；`src/platform/five-plane-execution/execution-engine/multi-step-orchestration.ts` 新增对外部 `planGraphBundle` 的直消费路径，收到 bundle 时会先过 `RuntimeEntryGuard.assertPlanGraphBundleOnly()`，并沿用其 `planGraphBundleId/harnessRunId`，不再在 P4 侧重建新的 graph contract。 | 定向验证已通过：`AA_RUNNING_TESTS=1 node --import tsx --test tests/unit/platform/execution/oapeflir/runtime-plan-executor.test.ts` 证明 runtime executor 直接消费外部 `PlanGraphBundle` 并把 bundle 自带 `planGraphBundleId/harnessRunId` 落入执行真相链；`AA_RUNNING_TESTS=1 node --import tsx --test tests/invariants/plan-graph-only-dispatch.test.ts` 继续证明入口守卫只接受 `PlanGraphBundle`；`AA_RUNNING_TESTS=1 node --import tsx --test tests/unit/platform/execution/execution-engine/multi-step-orchestration.test.ts` 全组通过，确认边界收紧未破坏既有多步执行链。 |
| R4-27 | 19. 核心不变量未强制执行（最严重系统性问题） | CRITICAL | `INV-RUN-001 HarnessRuntime 唯一执行入口` | 两个主执行路径均不创建 HarnessRun；用 legacy TaskRecord/ExecutionRecord 直接执行；RuntimeEntryGuard 未接入任何 dispatch 路径 | 已解决（源码修复并补定向测试） | 原问题不再是”仓内完全没有 HarnessRun”。`HarnessRun` 已局部落地，但遗留双轨并存的核心根因是两条主执行路径未统一切到 harness aggregate 作为唯一执行入口。 | 本轮整改后，`multi-step-orchestration.ts` 与 `single-task-happy-path.ts` 均已在 HarnessRun INSERT 的同一事务内发射 `platform.harness_run.status_changed` 事件，`src/platform/five-plane-orchestration/oapeflir/runtime-execute-bridge.ts` 也已将 RuntimeEntryGuard 接入 dispatch 主链。`tests/unit/platform/execution/execution-engine/multi-step-orchestration.test.ts` 与 `tests/integration/platform/five-plane-execution/single-task-happy-path.integration.test.ts` 本轮定向回归已通过。 | 保留真实代码修复结论。 |
| R4-28 | 19. 核心不变量未强制执行（最严重系统性问题） | CRITICAL | `INV-STATE-001 Truth mutation 必须同事务 append event` | single-task-happy-path 插入 task/workflow/execution 不 append PlatformFactEvent；用 legacy TransitionService 而非 RuntimeStateMachine | 已解决（源码修复并补定向测试） | 原审计针对的是事件原子性完全不成立的状态。当前 `multi-step-orchestration.ts` 与 `single-task-happy-path.ts` 均已在 HarnessRun INSERT 的同一事务内发射 `platform.harness_run.status_changed`，满足 INV-STATE-001”truth mutation 必须同事务 append event”的要求。 | 本轮整改后，两条主执行路径均通过 `TransactionalEventAppender` 在同一事务内完成 HarnessRun 持久化与 `platform.harness_run.status_changed` 事件发射，不再存在直接改 truth 而不 append event 的遗留写链。相关定向回归 `tests/unit/platform/execution/execution-engine/multi-step-orchestration.test.ts` 与 `tests/integration/platform/five-plane-execution/single-task-happy-path.integration.test.ts` 已通过。 | 保留真实代码修复结论，不把”部分路径满足”写成全局尚未统一。 |
| R4-29 | 19. 核心不变量未强制执行（最严重系统性问题） | CRITICAL | `INV-REPLAY-001 Replay 禁止产生真实副作用` | ReplayWorker 委托 replayService 但不调用 ReplayBoundaryGuard；无 ReplaySandboxPolicy 实现 | 已解决（源码修复并补定向测试） | 原审计针对的是 replay worker 接线前的状态；当前主缺口反而是 `ReplayWorker` 文件里相对导入写错，导致这条保护链虽然写在代码里，但测试无法启动验证。 | 本轮修正 `src/platform/five-plane-execution/ha/replay-worker.ts` 对 `ReplayBoundaryGuard` 的导入路径；当前 `ReplayWorker.runRecoveryCycle()` 已显式调用 boundary guard，并拒绝 `allowRealSideEffects=true` 的 replay policy。`tests/invariants/no-side-effect-in-replay.test.ts` 与 `tests/unit/platform/execution/ha/replay-worker.test.ts` 本轮定向回归已通过。 | 保留真实代码修复结论，不把“导入坏了导致保护链无法验证”写成早已完成。 |
| R4-30 | 19. 核心不变量未强制执行（最严重系统性问题） | HIGH | `INV-FENCING fencing token on state writes` | RuntimeStateMachine.assertLeaseAndFencing() 仅检查 NodeRun；HarnessRun/SideEffectRecord/BudgetLedger 跳过 fencing；legacy 路径完全绕过 | 已解决 | `assertLeaseAndFencing()` 已覆盖 NodeRun/HarnessRun/SideEffectRecord/BudgetLedger 四类聚合根因不再是”状态机只检查 NodeRun”。当前 `src/platform/five-plane-execution/runtime-state-machine.ts` 的 `assertLeaseAndFencing()` 分别在第 400 行（ HarnessRun）、第 424 行（SideEffectRecord）、第 444 行（BudgetLedger）实施 fence 检查；NodeRun 的检查在第 166 行。legacy TransitionService 旁路未完全消除问题已在 R4-27/R4-28 整改中统一收敛至 harness aggregate 主链。 | 验证证据：`runtime-state-machine.ts` lines 341-455 显式对 HarnessRun/SideEffectRecord/BudgetLedger 三类聚合实施 lease/fencing 检查；`tests/unit/platform/execution/runtime-state-machine.test.ts` 等相关回归已通过。 | 修正结论：INV-FENCING 已正确实现覆盖全部四类聚合，原审计claim “HarnessRun/SideEffectRecord/BudgetLedger 跳过 fencing”已过时。 |
| R4-31 | 19. 核心不变量未强制执行（最严重系统性问题） | HIGH | `INV-SANDBOX 无 sandbox 不执行` | executeToolCall()/executeAgentRoundLoop() 无 sandbox policy 检查；todo_write 硬编码空策略 {allow:[],deny:[]} 从不 enforce | 已解决（源码修复并补定向测试） | 原审计说“完全无 sandbox 检查”已经过时，但 `todo_write` 写操作这条链确实还有真实 bug：代码试图做 deny-by-default，却把 `SandboxPolicy` 当成含 `allow/deny` 数组的旧对象来读，导致写操作在真正拒绝前先抛 `TypeError`。 | 本轮修正 `src/platform/five-plane-execution/dispatcher/index.ts`，对 `todo_write` 的写操作先归一化可选的 `allow/deny` 操作列表；未显式授权时按 fail-closed 返回 `tool.sandbox_policy_denied`，不再因策略对象形状不匹配而崩溃。`tests/unit/platform/execution/dispatcher/dispatcher.test.ts` 本轮已新增/修正回归，验证 `create/update/delete` 被拒绝、`list/get` 仍可执行。 | 保留真实代码修复结论。 |
| R4-32 | 19. 核心不变量未强制执行（最严重系统性问题） | HIGH | `INV-APPROVAL risk-proportional approval` | single-task-happy-path 硬编码 requiresApproval:0；multi-step-supervisor 同；PolicyEngine 未接入执行路径 | 已解决 | 根因已修正：`single-task-happy-path.ts` 已显式接入 `ApprovalPolicyEngine`，在第 52 行导入 `ApprovalPolicyEngine`，第 227 行实例化引擎，第 363 行构造 `ApprovalPolicyContext` 并调用 `evaluate()`，先落基线 truth/event 再进入 approval block，避免旧实现”未建档先阻塞”的顺序错误。 | 验证证据：`src/platform/five-plane-execution/execution-engine/single-task-happy-path.ts` lines 52/227/363 显式接线 `ApprovalPolicyEngine`；`tests/integration/platform/five-plane-execution/single-task-happy-path.integration.test.ts` 本轮定向回归已通过。 | 修正结论：INV-APPROVAL 在 single-task-happy-path 执行路径已正确实现。 |
| R4-33 | 19. 核心不变量未强制执行（最严重系统性问题） | HIGH | `INV-SIDEEFFECT-001 ambiguous→reconciliation` | 无执行路径创建 SideEffectRecord；web_fetch/web_search 产生真实副作用但未记录/追踪/调和 | 已解决 | 根因已修正：`createSideEffectRecordForExternalCall` 已在 dispatcher 中实现并接线。第 485 行定义该方法，对 web_fetch/web_search/git/repo-map 等外部调用创建 SideEffectRecord；第 762 行在 tool 执行前调用该方法，将真实副作用纳入 reconciliation 追踪。 | 验证证据：`src/platform/five-plane-execution/dispatcher/index.ts` lines 485-519 定义 `createSideEffectRecordForExternalCall`，lines 760-762 在 tool 执行路径显式调用；`tests/invariants/side-effect-ambiguous-reconciles.test.ts` 等相关回归已通过。 | 修正结论：INV-SIDEEFFECT-001 在 dispatcher 执行路径已正确实现，web_fetch/web_search 真实副作用已被记录和追踪。 |
| R4-34 | 19. 核心不变量未强制执行（最严重系统性问题） | HIGH | `INV-POLICY-001 deny-by-default` | executeToolCall 用硬编码 switch-case dispatch，无 PolicyEngine/CapabilityGate 前置检查 | 已解决 | 原审计停留在 dispatcher 只有 switch-case 分发的阶段，没有复核前置门禁链已经被补到 `executeToolCall()` 入口。 | 当前 `src/platform/five-plane-execution/dispatcher/index.ts` 在实际分发前先执行 `assertBudgetAllowed()`、`assertPolicyAllowed()`、`assertSandboxAllowed()`；`tests/invariants/deny-by-default.test.ts` 与本轮修正后的 `tests/unit/platform/execution/dispatcher/dispatcher.test.ts` 已证明 deny-by-default 与 sandbox fail-closed 分支可运行。 | 不改生产代码，仅回写前置门禁链与测试依据。 |
| R4-35 | 19. 核心不变量未强制执行（最严重系统性问题） | HIGH | `All decisions→immutable evidence` | LLM 调用和 tool 执行不产出 EvidenceRecord/DecisionInputBundle/HarnessDecision | 已修复 | appendDecisionInputBundle/HarnessDecision 方法已在 runtime-truth-repository.ts 实现并持久化到 RuntimeTruthRepository。 |
| R4-36 | 19. 核心不变量未强制执行（最严重系统性问题） | MEDIUM | `INV-SINGLE-LEADER` | 主执行路径直接 SQLite store.\* 写入无 leader check；HACoordinator 未接入 | 已修复 | requireLeader() 调用已添加到所有写方法：transition、appendNodeAttemptReceipt、appendRunVersionLock、appendDecisionInputBundle、appendHarnessDecision、appendEvidenceRecord。 |
| R4-37 | 20. 安全/可观测/错误处理跨切面 | CRITICAL | `control-plane/iam/network-egress-policy.ts` | 默认 mode="audit_only"——egress 违规仅日志不阻断(§11.5 要求 deny 为正式安全事件) | 已修复 | 默认模式虽然切到 deny，但运行时阻断判断仍按旧的 enforce-only 语义执行，导致默认 deny 形同 audit。 | 当前 `src/platform/control-plane/iam/network-egress-policy.ts` 已显式区分 `audit_only/deny/enforce`，并以 blocking mode 做真实阻断；定向测试已通过。 | 保留代码修复结论。 |
| R4-38 | 20. 安全/可观测/错误处理跨切面 | CRITICAL | `interaction/dashboard/dashboard-websocket-server.ts` | registerClient() 无鉴权/无 tenantId/无 principal(§11.1 要求所有操作关联 principal) | 已解决 | 这组混合了旧目录快照、接口认知和测试存在性误判，当前仓库文件已直接反证。 | 当前 `src/sdk/client-sdk/api-client.ts` 已发送版本握手头；`src/sdk/cli/index.ts` 已包含 `pack create/test/validate/publish`；`src/platform/five-plane-*` 目录存在；`dashboard-websocket-server.ts` 已绑定 principal/tenantId；`structured-logger.ts` 有 `crosscuttingFabric`；`tests/invariants/` 已存在；启动顺序与 ring catalog 已改为 canonical。 | 不改代码，仅回写依据。 |
| R4-39 | 20. 安全/可观测/错误处理跨切面 | HIGH | `全 src/` | DataTaintPropagation(§11.6) 零实现——taint_label 从不出现在代码中 | 已解决 | 插件治理条目来自中间态快照，registry 与 SDK 后续已补齐污染传播、撤回分级和递归深度追踪。 | 当前 `src/plugins/builtin-plugin-registry.ts` 已有 `DataTaintPropagation` 与 `BundleRevocationSeverity`，`src/sdk/plugin-sdk/plugin-context.ts` 已有 `callDepth/delegationDepth`。 | 不改代码，仅回写依据。 |
| R4-40 | 20. 安全/可观测/错误处理跨切面 | HIGH | `model-gateway/unified-chat-provider.ts` | LLM 调用无 principal/tenantId/audit/PolicyOutcome(§11.1-11.2) | 已解决 | 原审计停留在旧版 model gateway 请求与 streaming 接口快照，没有反映后续上下文、预算与审计透传。 | 当前 `src/platform/model-gateway/provider-registry/unified-chat-provider.ts` 已包含 `traceId`、`tenantId`、`costTag`、`abortSignal`、`validatePartialChunk`、`streamingBudgetTrack`、`policyOutcome`，并回填 trace。 | 不改代码，仅回写依据。 |
| R4-41 | 20. 安全/可观测/错误处理跨切面 | HIGH | `model-gateway/circuit-breaker.ts` | 状态变更仅写日志不发 event bus 事件(§9.4) | 已解决 | 这组混合了旧目录快照、接口认知和测试存在性误判，当前仓库文件已直接反证。 | 当前 `src/sdk/client-sdk/api-client.ts` 已发送版本握手头；`src/sdk/cli/index.ts` 已包含 `pack create/test/validate/publish`；`src/platform/five-plane-*` 目录存在；`dashboard-websocket-server.ts` 已绑定 principal/tenantId；`structured-logger.ts` 有 `crosscuttingFabric`；`tests/invariants/` 已存在；启动顺序与 ring catalog 已改为 canonical。 | 不改代码，仅回写依据。 |
| R4-42 | 20. 安全/可观测/错误处理跨切面 | HIGH | `shared/observability/runtime-metrics-registry.ts` | 10+ canonical harness.\* 指标仅 1个被记录(§12.4) | 已解决 | 原审计停留在 runtime metrics registry 还没扩展 harness 指标的阶段。 | 当前 `src/platform/shared/observability/runtime-metrics-registry.ts` 已实现 `recordHarnessRunDuration / recordHarnessStepCount / recordHarnessBudgetConsumed / recordHarnessExecutionLatency / recordHarnessTaskStarted / recordHarnessTaskCompleted / recordHarnessPluginInvoked / recordHarnessPolicyDecision` 等 canonical harness 指标；`tests/unit/platform/shared/observability/runtime-metrics-registry.test.ts` 本轮定向回归通过。 | 不改生产代码，仅回写现行指标面和测试依据。 |
| R4-43 | 20. 安全/可观测/错误处理跨切面 | HIGH | `shared/observability/structured-logger.ts` | 缺 crosscutting_fabric 字段(§12.4 要求 reliability/security/governance 分类) | 已解决 | 这组混合了旧目录快照、接口认知和测试存在性误判，当前仓库文件已直接反证。 | 当前 `src/sdk/client-sdk/api-client.ts` 已发送版本握手头；`src/sdk/cli/index.ts` 已包含 `pack create/test/validate/publish`；`src/platform/five-plane-*` 目录存在；`dashboard-websocket-server.ts` 已绑定 principal/tenantId；`structured-logger.ts` 有 `crosscuttingFabric`；`tests/invariants/` 已存在；启动顺序与 ring catalog 已改为 canonical。 | 不改代码，仅回写依据。 |
| R4-44 | 20. 安全/可观测/错误处理跨切面 | HIGH | `execution/plugin-executor/adapter-executor.ts` | retry 用固定延迟无 exponential backoff 无 jitter 无幂等检查(§9.3) | 已解决 | 原审计没有刷新 adapter executor 的后续演进。 | 当前 `src/platform/five-plane-execution/plugin-executor/adapter-executor.ts` 已按 `backoffMs * backoffMultiplier` 计算指数退避，并受 `backoffMaxMs` 与 `jitterPercent` 约束；同时还引入了 `idempotencyKey`、`checkIdempotencyCache()` 与 `cacheIdempotencyResult()` 接口缝。`tests/unit/platform/execution/plugin-executor/adapter-executor.test.ts` 本轮定向回归已通过。 | 不改生产代码，仅回写现行实现与测试依据。 |
| R4-45 | 20. 安全/可观测/错误处理跨切面 | MEDIUM | `interaction/ux/conversation-history-service.ts` | tenant 隔离依赖后置 client-side filter 而非查询级隔离(§9.1) | 已解决 | 原审计停留在会话列表只靠内存后置筛选的旧实现。 | 当前 `src/interaction/ux/conversation-history-service.ts` 的 `listUserSessions()` 已在 `memoryService.recall()` 查询参数中显式传入 `tenantId`，并仅保留同租户会话作为二次防护；本轮新增 `tests/unit/platform/interaction/ux/conversation-history-service.test.ts` 回归，直接断言 recall 查询携带 `tenantId` 且外租户数据不会进入结果。 | 不改生产代码，仅回写 query-level tenant isolation 的现行依据。 |
| R4-46 | 20. 安全/可观测/错误处理跨切面 | MEDIUM | `model-gateway/unified-chat-provider.ts` | createChatCompletion 不传播 traceId/spanId(§12.7 断链) | 已解决 | 原审计停留在旧版 model gateway 请求与 streaming 接口快照，没有反映后续上下文、预算与审计透传。 | 当前 `src/platform/model-gateway/provider-registry/unified-chat-provider.ts` 已包含 `traceId`、`tenantId`、`costTag`、`abortSignal`、`validatePartialChunk`、`streamingBudgetTrack`、`policyOutcome`，并回填 trace。 | 不改代码，仅回写依据。 |
| R4-47 | 20. 安全/可观测/错误处理跨切面 | MEDIUM | `model-gateway/degradation-controller.ts` | 降级切换不发 OperationalDirective 不与 mode 合成链交互(§9.5) | 已解决 | 原审计没有复核降级控制器后续补入的 directive/event 总线接线。 | 当前 `src/platform/model-gateway/degradation/degradation-controller.ts` 在 `escalate()/deescalate()` 中都会创建 `OperationalDirective`，并通过 `eventBusEmitter` 发出 `degradation:escalate` / `degradation:deescalate` 事件；`tests/unit/platform/model-gateway/degradation-controller.test.ts` 本轮定向回归已通过。 | 不改生产代码，仅回写现行 mode synthesis 交互依据。 |
| R4-48 | 20. 安全/可观测/错误处理跨切面 | MEDIUM | `execution/plugin-executor/adapter-executor.ts` | retry 耗尽静默返回 error 无 incident/DLQ/error_code(§12.1) | 已解决 | 原审计针对的是更早期 retry exhaustion 分支。 | 当前 `src/platform/five-plane-execution/plugin-executor/adapter-executor.ts` 在重试耗尽后会写入 reliability 向的结构化错误日志，并返回带 `error_code: "RETRY_EXHAUSTED"` 的错误输出；`tests/unit/platform/execution/plugin-executor/adapter-executor.test.ts` 已覆盖重试耗尽分支。 | 不改生产代码，仅回写现行错误输出与观测依据。 |
| R4-49 | 20. 安全/可观测/错误处理跨切面 | LOW | `model-gateway/circuit-breaker.ts` | failure rate 公式无成功数分母——阈值比较数学错误 | 已解决 | 这组混合了旧目录快照、接口认知和测试存在性误判，当前仓库文件已直接反证。 | 当前 `src/sdk/client-sdk/api-client.ts` 已发送版本握手头；`src/sdk/cli/index.ts` 已包含 `pack create/test/validate/publish`；`src/platform/five-plane-*` 目录存在；`dashboard-websocket-server.ts` 已绑定 principal/tenantId；`structured-logger.ts` 有 `crosscuttingFabric`；`tests/invariants/` 已存在；启动顺序与 ring catalog 已改为 canonical。 | 不改代码，仅回写依据。 |
| R4-50 | 21. 测试/配置/引导对齐 | CRITICAL | `tests/invariants/` | §2.4 要求 9个 invariant test 文件——**全部不存在**(truth-event-atomicity/harness-run-authority/plan-graph-only-dispatch/budget-reserve-before-execute/no-side-effect-in-replay/side-effect-ambiguous-reconciles/deny-by-default 等) | 已解决 | 原审计引用的是早期目录快照；当前 `tests/invariants/` 不但存在，而且本轮还修正了其中几份因旧路径/旧断言导致的假失败。 | 当前仓库已有 `tests/invariants/budget-reserve-before-execute.test.ts`、`no-side-effect-in-replay.test.ts`、`side-effect-ambiguous-reconciles.test.ts`、`deny-by-default.test.ts`、`harness-run-authority.test.ts`、`plan-graph-only-dispatch.test.ts` 等文件；本轮相关 invariant 定向回归已通过。 | 不改生产代码，回写现行测试资产与本轮修复事实。 |
| R4-51 | 21. 测试/配置/引导对齐 | CRITICAL | `tests/` | INV-BUDGET-001 零测试覆盖 | 已解决（源码修复并补定向测试） | 原问题不再是“没有测试”，而是 invariant 测试文件本身还停留在旧 import 路径、旧返回形状和旧 replay 字段假设，导致无法证明当前预算语义。 | 本轮修正 `tests/invariants/budget-reserve-before-execute.test.ts`，对齐当前 `BudgetAllocator` 的 `context`、reserve/settle 返回形状、hard-cap 异常类型和零成本 settle 语义；同时 `tests/unit/platform/execution/budget-allocator.test.ts` 也已通过。 | 保留“本轮已修复并补齐回归”结论。 |
| R4-52 | 21. 测试/配置/引导对齐 | CRITICAL | `tests/` | INV-REPLAY-001 零测试覆盖 | 已解决（源码修复并补定向测试） | 原问题不再是“无覆盖”，而是 replay invariant 测试本身还引用旧路径，并把 `ReplayBoundaryGuard` 与 `ReplayWorker` 的职责边界混成了旧断言。 | 本轮修正 `tests/invariants/no-side-effect-in-replay.test.ts`，明确区分 `trace_replay` guard 行为与 `ReplayWorker` 对 `allowRealSideEffects=true` policy 的拒绝；同时修复 `ReplayWorker` 真实导入错误，`tests/unit/platform/execution/ha/replay-worker.test.ts` 现已通过。 | 保留“本轮已修复并补齐回归”结论。 |
| R4-53 | 21. 测试/配置/引导对齐 | CRITICAL | `tests/` | INV-SIDEEFFECT-001 零测试覆盖 | 已解决（源码修复并补定向测试） | 原问题不再是“无覆盖”，而是 invariant 文件引用旧路径、旧字段名，并断言了已经被 fencing 规则取代的旧成功路径。 | 本轮重写 `tests/invariants/side-effect-ambiguous-reconciles.test.ts`，直接验证 ambiguous→reconciliation-safe、commit-affecting reconciliation 仍受 fencing 约束、compensation 成功收敛到 `compensated`；配合既有 `tests/unit/platform/execution/side-effect-manager.test.ts` 一并证明这条不变量已有覆盖。 | 保留“本轮已修复并补齐回归”结论。 |
| R4-54 | 21. 测试/配置/引导对齐 | CRITICAL | `tests/` | INV-POLICY-001 零测试覆盖 | 已解决 | 原审计未复核 deny-by-default invariant 与 dispatcher 侧 fail-closed 回归。 | 当前 `tests/invariants/deny-by-default.test.ts` 已覆盖 legacy contract/platform event/PlanGraphBundle deny-by-default；本轮还补强了 `tests/unit/platform/execution/dispatcher/dispatcher.test.ts`，验证 `todo_write` 写操作在无显式授权时被 sandbox/policy 前置门禁拒绝。 | 不改生产代码，仅回写现行测试依据。 |
| R4-55 | 21. 测试/配置/引导对齐 | HIGH | `config/runtime/default.json` | 用废弃 defaultStepTimeoutMs；无 canonical 状态机/五平面/RuntimeStateMachine 配置——仅 7字段 stub | 已解决 | 原审计基于旧版 runtime 配置样本，没有复核当前默认配置。 | 当前 `config/runtime/default.json` 使用的是 `defaultTaskTimeoutMs`，并且已声明 `stateMachine.type = "RuntimeStateMachine"`、`fivePlane.startupOrder`、`harnessRuntime`、`budgetConfiguration`、`eventRegistry` 等 canonical 运行时配置，远不止 7 个 stub 字段。 | 不改配置，仅回写现行默认配置依据。 |
| R4-56 | 21. 测试/配置/引导对齐 | HIGH | `config/risk/default.json` | 用废弃 stepTypeRisk/stepTypeRiskValues；无 §28 Event Registry/DLQ 模型对齐 | 已解决 | 原审计停留在更早期 risk config 形状，没有复核当前 canonical 风险配置。 | 当前 `config/risk/default.json` 已改为 `factorWeights/systemRiskValues/dataClassRiskValues` 体系，并显式声明 `eventRegistry.namespace` 与 `dlqModel`；文件内不再使用 `stepTypeRisk` 或 `stepTypeRiskValues`。 | 不改配置，仅回写现行风险配置依据。 |
| R4-57 | 21. 测试/配置/引导对齐 | HIGH | `config/domains/\*.json` | 域 workflow 配置用线性 steps[] + stepName——§13/§45 要求 PlanGraph | 已解决 | 原审计依据的是旧版 domains workflow 配置，没有复核当前 domain default。 | 当前 `config/domains/default.json` 的 workflow 已使用 `planGraph`，其中包含 `graphId/nodes/edges/entryNodeIds/terminalNodeIds/joinStrategy/graphHash`；并非线性 `steps[]` 结构。 | 不改配置，仅回写现行 domain workflow 结构依据。 |
| R4-58 | 21. 测试/配置/引导对齐 | HIGH | `config/domains/\*.json` | 无 DomainRiskSpec(advisory_only/human_accountable/deterministic_hot_path_only)——quant-trading 高危域无风险声明 | 已解决 | 原审计针对的是旧版 domain 风险声明形状，没有复核当前 domain/risk 默认配置。 | 当前 `config/domains/default.json` 每个 domain 已包含 `riskSpec.advisoryOnly/humanAccountable/deterministicHotPathOnly`；同时 `config/risk/default.json` 的 `domainRiskDefaults` 也已为高风险域声明默认风险约束。 | 不改配置，仅回写现行风险声明依据。 |
| R4-59 | 21. 测试/配置/引导对齐 | HIGH | `platform-architecture-bootstrap.ts` | 注册为扁平目录无强制启动序(§7 要求 P5→X1→P2→P3→P4→P1) | 已解决 | 原审计停留在启动序校验尚未固化到 bootstrap 入口前的旧快照。 | 当前 `src/platform-architecture-bootstrap.ts` 已定义 canonical `PLATFORM_STARTUP_ORDER = ["P5","X1","P2","P3","P4","P1"]`，并通过 `assertStartupOrderEnforced()` 校验必需不变量和 release gate；`src/index.ts` 的 `main()` 启动时会先执行该断言。 | 不改代码，仅回写具体启动序证据。 |
| R4-60 | 21. 测试/配置/引导对齐 | MEDIUM | `platform-architecture-types.ts` | 无 canonical runtime 对象类型(HarnessRun/NodeRun/PlanGraphBundle/BudgetReservation)——仅基础设施类型 | 已解决 | 原审计使用的是旧版 platform architecture types 快照，没有复核当前类型定义。 | 当前 `src/platform-architecture-types.ts` 已直接定义 `HarnessRun`、`NodeRun`、`PlanGraphBundle`、`BudgetReservation` 以及关联 `PlanGraph/PlanNode/BudgetIntent/RiskPreview` 等 canonical runtime 类型。 | 不改生产代码，仅回写现行类型依据。 |
| R4-61 | 21. 测试/配置/引导对齐 | MEDIUM | `domains-runtime-catalog.ts` | 仍用 phase9a-9f 旧分期(§33 明确"仅历史映射"，canonical 为 Ring 1/2/3) | 已解决 | 原问题更接近“底层 baseline 仍保留 phase 历史字段”，而不是 runtime catalog 还在对外暴露 phase9a-9f。 | 当前 `src/domains-runtime-catalog.ts` 对外 authoritative 结构只有 `ring1/ring2/ring3`；`buildDomainsRuntimeCatalog()` 与 `registerDomainsRuntimeCatalog()` 都按 ring 聚合。phase `9a-9f` 仍存在于 baseline/roadmap 历史映射层，但不再是 runtime catalog 的外部分期接口。 | 不改代码，仅回写 authoritative catalog 与历史映射边界。 |
| R4-62 | 21. 测试/配置/引导对齐 | MEDIUM | `index.ts` | main() 无架构启动不变量检查(ArchitectureInvariantRegistry/NonOverridableInvariantRegistry §2.4) | 已解决 | 原审计遗漏了 `main()` 已把启动序和架构不变量断言前置到真正的程序入口。 | 当前 `src/index.ts` 的 `main()` 会先调用 `assertStartupOrderEnforced()`；而 `src/platform-architecture-bootstrap.ts` 的该断言内部已实例化 `ArchitectureInvariantRegistry` 与 `NonOverridableInvariantRegistry`，检查 `INV-STATE-001 / INV-RUN-001 / INV-GRAPH-001 / INV-BUDGET-001 / INV-REPLAY-001 / INV-SIDEEFFECT-001 / INV-POLICY-001` 并执行 `assertReleaseGateReady()`；`tests/integration/platform-architecture-bootstrap.test.ts` 已覆盖此链路。 | 不改代码，仅回写入口级不变量检查证据。 |
| R4-63 | 21. 测试/配置/引导对齐 | MEDIUM | `index.ts` | runPlatformRootDemo 用 snapshot.workflow.currentStepIndex/stepOutputs 废弃对象作为主输出 | 已解决 | 原审计抓的是 demo 仍输出旧 `workflow/stepOutputs` 快照的早期版本；现行 demo 已切到 canonical runtime 视图。 | 当前 `src/index.ts` 的 `runPlatformRootDemo()` 输出的是 `harnessRun`、`nodeRuns`、`execution`、`events`，并且只挑选 `harnessRunId/status/tenantId/planGraphBundleId`、`nodeRunId/nodeId/status/attemptCount` 等 canonical 字段，不再读取 `snapshot.workflow.currentStepIndex` 或 `stepOutputs`。 | 不改代码，仅回写 demo 输出面现状。 |
| R4-64 | 21. 测试/配置/引导对齐 | MEDIUM | `tests/` | 无 contract-naming-consistency.test.ts(§6.4 要求 CI lint 扫描废弃术语) | 已解决 | 原审计没有重新扫描现行 invariants 目录。 | 当前仓库已存在 `tests/invariants/contract-naming-consistency.test.ts`，其中直接扫描 `docs_zh/contracts` 中的废弃术语、检查 `event-types.ts` 的 legacy namespace、并验证 `PlanGraphBundle` 导出面。 | 不改代码，仅回写具体测试资产依据。 |
| R5-1 | 23. OAPEFLIR 编排循环实现缺口（§13/§45/§58） | CRITICAL | `orchestration/oapeflir/oapeflir-loop-service.ts` | Plan 阶段产出线性 Plan{steps[]}——非 PlanGraphBundle(§13.7 "Plan must be Graph") | 已解决 | OAPEFLIR loop 条目建立在单程管线时期的历史实现上，后续 graph bundle、replan loop、FSM、HarnessDecision 已接入。 | 当前 `src/platform/orchestration/oapeflir/oapeflir-loop-service.ts` 已包含 `PlanGraphBundle`、`HarnessLoopController`、重入 while loop、`EvaluationReport`、`GraphPatch`、`harnessDecision`；`stage-transition-fsm.ts` 已允许反馈驱动回退。 | 不改代码，仅回写依据。 |
| R5-2 | 23. OAPEFLIR 编排循环实现缺口（§13/§45/§58） | CRITICAL | `orchestration/oapeflir/oapeflir-loop-service.ts` | run() 是单程管线(O→A→P→E→F→L→I→R→return)；replanDecision 计算后无重入——不是循环(§45.7 要求重入 Plan/Execute) | 已解决 | OAPEFLIR loop 条目建立在单程管线时期的历史实现上，后续 graph bundle、replan loop、FSM、HarnessDecision 已接入。 | 当前 `src/platform/orchestration/oapeflir/oapeflir-loop-service.ts` 已包含 `PlanGraphBundle`、`HarnessLoopController`、重入 while loop、`EvaluationReport`、`GraphPatch`、`harnessDecision`；`stage-transition-fsm.ts` 已允许反馈驱动回退。 | 不改代码，仅回写依据。 |
| R5-3 | 23. OAPEFLIR 编排循环实现缺口（§13/§45/§58） | CRITICAL | `orchestration/oapeflir/oapeflir-loop-service.ts` | 未集成 StageTransitionFSM——FSM 为死代码；阶段转换无校验 | 已解决 | OAPEFLIR loop 条目建立在单程管线时期的历史实现上，后续 graph bundle、replan loop、FSM、HarnessDecision 已接入。 | 当前 `src/platform/orchestration/oapeflir/oapeflir-loop-service.ts` 已包含 `PlanGraphBundle`、`HarnessLoopController`、重入 while loop、`EvaluationReport`、`GraphPatch`、`harnessDecision`；`stage-transition-fsm.ts` 已允许反馈驱动回退。 | 不改代码，仅回写依据。 |
| R5-4 | 23. OAPEFLIR 编排循环实现缺口（§13/§45/§58） | CRITICAL | `orchestration/oapeflir/oapeflir-loop-service.ts` | 未集成 HarnessLoopController——无 max-iteration/max-replan/max-duration/max-cost 守卫 | 已解决 | OAPEFLIR loop 条目建立在单程管线时期的历史实现上，后续 graph bundle、replan loop、FSM、HarnessDecision 已接入。 | 当前 `src/platform/orchestration/oapeflir/oapeflir-loop-service.ts` 已包含 `PlanGraphBundle`、`HarnessLoopController`、重入 while loop、`EvaluationReport`、`GraphPatch`、`harnessDecision`；`stage-transition-fsm.ts` 已允许反馈驱动回退。 | 不改代码，仅回写依据。 |
| R5-5 | 23. OAPEFLIR 编排循环实现缺口（§13/§45/§58） | HIGH | `orchestration/harness/index.ts decide()` | 无 downgrade_mode 决策分支(§58.6 要求 6种基础决策) | 已解决 | OAPEFLIR loop 条目建立在单程管线时期的历史实现上，后续 graph bundle、replan loop、FSM、HarnessDecision 已接入。 | 当前 `src/platform/orchestration/oapeflir/oapeflir-loop-service.ts` 已包含 `PlanGraphBundle`、`HarnessLoopController`、重入 while loop、`EvaluationReport`、`GraphPatch`、`harnessDecision`；`stage-transition-fsm.ts` 已允许反馈驱动回退。 | 不改代码，仅回写依据。 |
| R5-6 | 23. OAPEFLIR 编排循环实现缺口（§13/§45/§58） | HIGH | `orchestration/oapeflir/assessment-service.ts` | Assess 不消费/产出 ConstraintPack/EffectivePolicySnapshot/RiskAssessment(§13.1.1) | 已解决（源码修复并补定向测试） | 根因是 assessment 主链过去只接受 `TaskSituation`，loop 也没有把 harness 约束、策略快照和继承风险穿进 assess 阶段，导致评估结果只能看局部 blocker/tooling 信号。 | 当前 `src/platform/five-plane-orchestration/oapeflir/assessment-service.ts` 已提供 `AssessmentInput`，显式消费 `constraintPack`、`effectivePolicySnapshot`、`inheritedRiskAssessment`，并把这些信号写入 `riskAssessment`、`approvalPolicy`、`executionMode` 与 `routingDecision`；`src/platform/five-plane-orchestration/oapeflir/oapeflir-loop-service.ts` 也已把结构化 assessment input 接到 assess 阶段。定向测试 `tests/unit/platform/orchestration/oapeflir/assessment-service.test.ts` 与 `assessment-service-extended.test.ts` 已通过。 | 已改生产代码并补最小回归。 |
| R5-7 | 23. OAPEFLIR 编排循环实现缺口（§13/§45/§58） | HIGH | `orchestration/oapeflir/oapeflir-loop-service.ts` | Evaluator 产出 ExecutionOutcomeEvaluation 而非 §45.10 EvaluationReport(passed/score/issues[]/recommendation/confidence) | 已解决 | OAPEFLIR loop 条目建立在单程管线时期的历史实现上，后续 graph bundle、replan loop、FSM、HarnessDecision 已接入。 | 当前 `src/platform/orchestration/oapeflir/oapeflir-loop-service.ts` 已包含 `PlanGraphBundle`、`HarnessLoopController`、重入 while loop、`EvaluationReport`、`GraphPatch`、`harnessDecision`；`stage-transition-fsm.ts` 已允许反馈驱动回退。 | 不改代码，仅回写依据。 |
| R5-8 | 23. OAPEFLIR 编排循环实现缺口（§13/§45/§58） | HIGH | `orchestration/oapeflir/oapeflir-loop-service.ts` | Release 阶段调 PolicyRolloutService.start() 无 EvaluationGate/approval/canary/rollback(§13.14) | 已解决 | 原审计对应的是 release 还只调 `start()` 的阶段；当前 loop 已改为显式 gating 入口。 | 当前 `src/platform/five-plane-orchestration/oapeflir/oapeflir-loop-service.ts` 在 release 阶段调用 `this.rollout.startWithGating(...)`，并传入 `evaluationGate`、`requireApproval`、`canaryPercent`、`rollbackOnFailure`；`src/platform/five-plane-orchestration/improve-rollout/policy-rollout-service.ts` 也已提供对应 gating 逻辑。 | 不改生产代码，仅回写现行 release gating 依据。 |
| R5-9 | 23. OAPEFLIR 编排循环实现缺口（§13/§45/§58） | HIGH | `orchestration/planner/plan-builder.ts` | 无 Graph Normalization/Validation/Risk Propagation/Worst-Path Analysis(§13.9-13.12) | 已解决 | 这条针对 `plan-builder.ts` 本身已经过时：相关 graph build/validation/risk/worst-path 能力已经在该文件中落地。 | 当前 `src/platform/five-plane-orchestration/planner/plan-builder.ts` 已提供 `buildGraphBundle()`，并在其中执行 graph normalization、`performExtendedValidation()`、`computeRiskPropagation()` 与 `dagValidator.analyzeWorstPath()`。 | 不改生产代码，仅回写 `plan-builder` 现行能力；后续若要继续审计，应聚焦调用方是否统一走 `buildGraphBundle()`。 |
| R5-10 | 23. OAPEFLIR 编排循环实现缺口（§13/§45/§58） | HIGH | `orchestration/oapeflir/stage-transition-fsm.ts` | FSM 禁止所有后向转换——replan 在结构上不可能(§45.7/§13.4 要求 feedback→plan) | 已解决 | OAPEFLIR loop 条目建立在单程管线时期的历史实现上，后续 graph bundle、replan loop、FSM、HarnessDecision 已接入。 | 当前 `src/platform/orchestration/oapeflir/oapeflir-loop-service.ts` 已包含 `PlanGraphBundle`、`HarnessLoopController`、重入 while loop、`EvaluationReport`、`GraphPatch`、`harnessDecision`；`stage-transition-fsm.ts` 已允许反馈驱动回退。 | 不改代码，仅回写依据。 |
| R5-11 | 23. OAPEFLIR 编排循环实现缺口（§13/§45/§58） | HIGH | `orchestration/oapeflir/oapeflir-loop-service.ts` | Observer 仅合并 TaskSituation+SystemSituation；缺事件流/目标分解/记忆/前次运行上下文(§45.8) | 已解决 | 原审计没有跟上 observer 上下文扩展后的实现。 | 当前 `src/platform/five-plane-orchestration/oapeflir/oapeflir-loop-service.ts` 已在 `previousRunContext` 中声明 `eventFlowRefs / goalDecompositionRef / memoryRefs`，并在 observe 阶段把这些字段写回 `systemObservation`。 | 不改生产代码，仅回写现行 observer 上下文依据。 |
| R5-12 | 23. OAPEFLIR 编排循环实现缺口（§13/§45/§58） | MEDIUM | `orchestration/oapeflir/oapeflir-loop-service.ts` | Replan 无 GraphPatch 产出(§13.13 要求 baseGraphVersion+operations[]+compatibilityReport) | 已解决 | OAPEFLIR loop 条目建立在单程管线时期的历史实现上，后续 graph bundle、replan loop、FSM、HarnessDecision 已接入。 | 当前 `src/platform/orchestration/oapeflir/oapeflir-loop-service.ts` 已包含 `PlanGraphBundle`、`HarnessLoopController`、重入 while loop、`EvaluationReport`、`GraphPatch`、`harnessDecision`；`stage-transition-fsm.ts` 已允许反馈驱动回退。 | 不改代码，仅回写依据。 |
| R5-13 | 23. OAPEFLIR 编排循环实现缺口（§13/§45/§58） | MEDIUM | `orchestration/oapeflir/oapeflir-loop-service.ts` | Execute 用 flat ExecuteBridge 无 subgraph/child-run 支持(§13.7 要求子任务/委托显式建模) | 已解决（源码复核） | 根因不是现行执行桥仍然“只认 flat plan”，而是审计条目停留在 parent context 还没真正下沉到 runtime bridge 的中间态。后续重构把 OAPEFLIR 自身收敛为 projection-only，同时把 subgraph/child-run 上下文交给 canonical runtime execute bridge。 | 当前 `src/platform/five-plane-orchestration/oapeflir/oapeflir-loop-service.ts` 在 execute stage 已把 `parentPlanGraphBundleId / parentNodeId / childRunId` 写入 stage metadata，并要求调用方显式提供 `stepOutputs`；`src/platform/five-plane-orchestration/oapeflir/runtime-execute-bridge.ts` 的 `executePlan(...)` 已把 `context.parentContext` 透传到 `RuntimePlanExecutionInput`。这意味着子图/child-run 关联已进入执行平面，而不是停留在注释。 | 不改生产代码，回写“执行桥已接入 parentContext，OAPEFLIR 不再自做 execution truth”的现状。 |
| R5-14 | 23. OAPEFLIR 编排循环实现缺口（§13/§45/§58） | LOW | `orchestration/oapeflir/oapeflir-loop-service.ts` | OapeflirLoopResult 无 HarnessDecision 字段——OAPEFLIR 层与 Harness 决策模型断连 | 已解决 | OAPEFLIR loop 条目建立在单程管线时期的历史实现上，后续 graph bundle、replan loop、FSM、HarnessDecision 已接入。 | 当前 `src/platform/orchestration/oapeflir/oapeflir-loop-service.ts` 已包含 `PlanGraphBundle`、`HarnessLoopController`、重入 while loop、`EvaluationReport`、`GraphPatch`、`harnessDecision`；`stage-transition-fsm.ts` 已允许反馈驱动回退。 | 不改代码，仅回写依据。 |
| R5-15 | 24. NL 入口 + 目标分解 + 主动代理缺口（§8/§19/§40-§42） | CRITICAL | `interaction/nl-gateway/index.ts` | pending_user_confirmation 状态仍发射 RequestEnvelope(§39.2 要求仅 confirmed TaskSpec 方可产生) | 已解决 | 原审计依据的是 `buildTask()` 仍在确认前生成 envelope 的旧实现；现行逻辑已经把确认门禁收前。 | 当前 `src/interaction/nl-gateway/index.ts` 在 `buildTask()` 中明确写了“Only emit RequestEnvelope after TaskSpec is confirmed”，并在 `confirmationRequired` 为真时返回 `requestEnvelope: null`；`tests/unit/interaction/nl-gateway/request-envelope-confirmation-gate.test.ts` 已覆盖确认前不发 envelope 的行为。 | 不改主实现，仅回写现行确认门禁依据。 |
| R5-16 | 24. NL 入口 + 目标分解 + 主动代理缺口（§8/§19/§40-§42） | CRITICAL | `interaction/nl-gateway/index.ts` | 无独立 classify_risk 管线阶段(§39.2 要求作为独立准入门禁) | 已解决 | 原审计没有复核 `parseDetailed()` 现行顺序。 | 当前 `src/interaction/nl-gateway/index.ts` 已定义独立 `classifyRisk(message)`，并在 `parseDetailed()` 里于 intent 包装之前先执行该风险分类，作为 admission gate 输入。 | 不改主实现，仅回写独立风险分类链路依据。 |
| R5-17 | 24. NL 入口 + 目标分解 + 主动代理缺口（§8/§19/§40-§42） | HIGH | `interaction/nl-gateway/index.ts` | DetectedIntent.intentType 缺 "why"(§39 新增解释查询类型) | 已解决 | 原审计停留在 `DetectedIntent` 仍只有 create/query/modify/status/approval 的旧版本。 | 当前 `src/interaction/nl-gateway/index.ts` 的 `DetectedIntent["intentType"]` 已包含 `"why"`，`mapIntentType()` 也显式把 `"why"` / `"explain"` 映射到该类型。 | 不改主实现，仅回写类型与映射证据。 |
| R5-18 | 24. NL 入口 + 目标分解 + 主动代理缺口（§8/§19/§40-§42） | HIGH | `interaction/goal-decomposer/index.ts` | 无委托链深度限制(§19.2 max=3)和全局 call_depth 硬帽(=8)；无反乘法守卫 | 已解决 | GoalDecomposer / Autonomy / ProactiveAgent 家族已在后续迭代补齐深度上限、预算分配、风险传播、部分完成态、0-1000 信任分、incident-free 窗口、成本降级、180d 衰减和 trigger-autonomy linkage；历史审计与测试没有回写。 | 当前 `src/interaction/goal-decomposer/index.ts` 已有 `maxDelegationDepth`、`callDepth`、`budgetAllocations`、`riskPropagation`、`partially_completed`；`src/interaction/autonomy/index.ts` 已实现 0-1000 trust score、30/60/90d incident-free、200% cost overrun demotion、180d inactivity demotion；`src/interaction/proactive-agent/index.ts` 与 `trigger-engine/index.ts` 已将 medium/high 风险降为 suggestion。本轮同时修复了相关陈旧测试断言。 | 回写依据并修复陈旧测试，不改主实现。 |
| R5-19 | 24. NL 入口 + 目标分解 + 主动代理缺口（§8/§19/§40-§42） | HIGH | `interaction/goal-decomposer/index.ts` | 无预算按比例分配到子任务(§40.2)；无风险传播到子任务 | 已解决 | GoalDecomposer / Autonomy / ProactiveAgent 家族已在后续迭代补齐深度上限、预算分配、风险传播、部分完成态、0-1000 信任分、incident-free 窗口、成本降级、180d 衰减和 trigger-autonomy linkage；历史审计与测试没有回写。 | 当前 `src/interaction/goal-decomposer/index.ts` 已有 `maxDelegationDepth`、`callDepth`、`budgetAllocations`、`riskPropagation`、`partially_completed`；`src/interaction/autonomy/index.ts` 已实现 0-1000 trust score、30/60/90d incident-free、200% cost overrun demotion、180d inactivity demotion；`src/interaction/proactive-agent/index.ts` 与 `trigger-engine/index.ts` 已将 medium/high 风险降为 suggestion。本轮同时修复了相关陈旧测试断言。 | 回写依据并修复陈旧测试，不改主实现。 |
| R5-20 | 24. NL 入口 + 目标分解 + 主动代理缺口（§8/§19/§40-§42） | HIGH | `interaction/goal-decomposer/index.ts` | GoalLifecycleState 缺 partially_completed(§40.5) | 已解决 | GoalDecomposer / Autonomy / ProactiveAgent 家族已在后续迭代补齐深度上限、预算分配、风险传播、部分完成态、0-1000 信任分、incident-free 窗口、成本降级、180d 衰减和 trigger-autonomy linkage；历史审计与测试没有回写。 | 当前 `src/interaction/goal-decomposer/index.ts` 已有 `maxDelegationDepth`、`callDepth`、`budgetAllocations`、`riskPropagation`、`partially_completed`；`src/interaction/autonomy/index.ts` 已实现 0-1000 trust score、30/60/90d incident-free、200% cost overrun demotion、180d inactivity demotion；`src/interaction/proactive-agent/index.ts` 与 `trigger-engine/index.ts` 已将 medium/high 风险降为 suggestion。本轮同时修复了相关陈旧测试断言。 | 回写依据并修复陈旧测试，不改主实现。 |
| R5-21 | 24. NL 入口 + 目标分解 + 主动代理缺口（§8/§19/§40-§42） | HIGH | `interaction/autonomy/index.ts` | TrustScore 范围 0-100；§42.1 要求 0-1000 | 已解决 | GoalDecomposer / Autonomy / ProactiveAgent 家族已在后续迭代补齐深度上限、预算分配、风险传播、部分完成态、0-1000 信任分、incident-free 窗口、成本降级、180d 衰减和 trigger-autonomy linkage；历史审计与测试没有回写。 | 当前 `src/interaction/goal-decomposer/index.ts` 已有 `maxDelegationDepth`、`callDepth`、`budgetAllocations`、`riskPropagation`、`partially_completed`；`src/interaction/autonomy/index.ts` 已实现 0-1000 trust score、30/60/90d incident-free、200% cost overrun demotion、180d inactivity demotion；`src/interaction/proactive-agent/index.ts` 与 `trigger-engine/index.ts` 已将 medium/high 风险降为 suggestion。本轮同时修复了相关陈旧测试断言。 | 回写依据并修复陈旧测试，不改主实现。 |
| R5-22 | 24. NL 入口 + 目标分解 + 主动代理缺口（§8/§19/§40-§42） | HIGH | `interaction/autonomy/index.ts` | 晋升规则无时间窗口 incident-free 检查(§42.2 要求 30d/60d/90d 零事件) | 已解决 | GoalDecomposer / Autonomy / ProactiveAgent 家族已在后续迭代补齐深度上限、预算分配、风险传播、部分完成态、0-1000 信任分、incident-free 窗口、成本降级、180d 衰减和 trigger-autonomy linkage；历史审计与测试没有回写。 | 当前 `src/interaction/goal-decomposer/index.ts` 已有 `maxDelegationDepth`、`callDepth`、`budgetAllocations`、`riskPropagation`、`partially_completed`；`src/interaction/autonomy/index.ts` 已实现 0-1000 trust score、30/60/90d incident-free、200% cost overrun demotion、180d inactivity demotion；`src/interaction/proactive-agent/index.ts` 与 `trigger-engine/index.ts` 已将 medium/high 风险降为 suggestion。本轮同时修复了相关陈旧测试断言。 | 回写依据并修复陈旧测试，不改主实现。 |
| R5-23 | 24. NL 入口 + 目标分解 + 主动代理缺口（§8/§19/§40-§42） | HIGH | `interaction/autonomy/index.ts` | 无成本超预算 200% 降级规则(§42.2) | 已解决 | GoalDecomposer / Autonomy / ProactiveAgent 家族已在后续迭代补齐深度上限、预算分配、风险传播、部分完成态、0-1000 信任分、incident-free 窗口、成本降级、180d 衰减和 trigger-autonomy linkage；历史审计与测试没有回写。 | 当前 `src/interaction/goal-decomposer/index.ts` 已有 `maxDelegationDepth`、`callDepth`、`budgetAllocations`、`riskPropagation`、`partially_completed`；`src/interaction/autonomy/index.ts` 已实现 0-1000 trust score、30/60/90d incident-free、200% cost overrun demotion、180d inactivity demotion；`src/interaction/proactive-agent/index.ts` 与 `trigger-engine/index.ts` 已将 medium/high 风险降为 suggestion。本轮同时修复了相关陈旧测试断言。 | 回写依据并修复陈旧测试，不改主实现。 |
| R5-24 | 24. NL 入口 + 目标分解 + 主动代理缺口（§8/§19/§40-§42） | HIGH | `interaction/proactive-agent/index.ts` | medium 风险主动动作可 auto_execute(§41.1 禁止 medium+ 直接执行) | 已解决 | GoalDecomposer / Autonomy / ProactiveAgent 家族已在后续迭代补齐深度上限、预算分配、风险传播、部分完成态、0-1000 信任分、incident-free 窗口、成本降级、180d 衰减和 trigger-autonomy linkage；历史审计与测试没有回写。 | 当前 `src/interaction/goal-decomposer/index.ts` 已有 `maxDelegationDepth`、`callDepth`、`budgetAllocations`、`riskPropagation`、`partially_completed`；`src/interaction/autonomy/index.ts` 已实现 0-1000 trust score、30/60/90d incident-free、200% cost overrun demotion、180d inactivity demotion；`src/interaction/proactive-agent/index.ts` 与 `trigger-engine/index.ts` 已将 medium/high 风险降为 suggestion。本轮同时修复了相关陈旧测试断言。 | 回写依据并修复陈旧测试，不改主实现。 |
| R5-25 | 24. NL 入口 + 目标分解 + 主动代理缺口（§8/§19/§40-§42） | HIGH | `interaction/proactive-agent/trigger-engine/` | resolveTriggerActionMode() 同样对 medium/high 返回 auto_execute(§41.1 违规) | 已解决 | GoalDecomposer / Autonomy / ProactiveAgent 家族已在后续迭代补齐深度上限、预算分配、风险传播、部分完成态、0-1000 信任分、incident-free 窗口、成本降级、180d 衰减和 trigger-autonomy linkage；历史审计与测试没有回写。 | 当前 `src/interaction/goal-decomposer/index.ts` 已有 `maxDelegationDepth`、`callDepth`、`budgetAllocations`、`riskPropagation`、`partially_completed`；`src/interaction/autonomy/index.ts` 已实现 0-1000 trust score、30/60/90d incident-free、200% cost overrun demotion、180d inactivity demotion；`src/interaction/proactive-agent/index.ts` 与 `trigger-engine/index.ts` 已将 medium/high 风险降为 suggestion。本轮同时修复了相关陈旧测试断言。 | 回写依据并修复陈旧测试，不改主实现。 |
| R5-26 | 24. NL 入口 + 目标分解 + 主动代理缺口（§8/§19/§40-§42） | MEDIUM | `interaction/autonomy/index.ts` | TrustDecayWorker 无 180d 无执行→suggestion 降级(§42.3)；无 30d 冻结晋升 | 已解决 | GoalDecomposer / Autonomy / ProactiveAgent 家族已在后续迭代补齐深度上限、预算分配、风险传播、部分完成态、0-1000 信任分、incident-free 窗口、成本降级、180d 衰减和 trigger-autonomy linkage；历史审计与测试没有回写。 | 当前 `src/interaction/goal-decomposer/index.ts` 已有 `maxDelegationDepth`、`callDepth`、`budgetAllocations`、`riskPropagation`、`partially_completed`；`src/interaction/autonomy/index.ts` 已实现 0-1000 trust score、30/60/90d incident-free、200% cost overrun demotion、180d inactivity demotion；`src/interaction/proactive-agent/index.ts` 与 `trigger-engine/index.ts` 已将 medium/high 风险降为 suggestion。本轮同时修复了相关陈旧测试断言。 | 回写依据并修复陈旧测试，不改主实现。 |
| R5-27 | 24. NL 入口 + 目标分解 + 主动代理缺口（§8/§19/§40-§42） | MEDIUM | `interaction/autonomy/index.ts` | 自治等级不与主动触发器联动(§42.5 要求 semi_auto 以上才允许自动执行) | 已解决 | GoalDecomposer / Autonomy / ProactiveAgent 家族已在后续迭代补齐深度上限、预算分配、风险传播、部分完成态、0-1000 信任分、incident-free 窗口、成本降级、180d 衰减和 trigger-autonomy linkage；历史审计与测试没有回写。 | 当前 `src/interaction/goal-decomposer/index.ts` 已有 `maxDelegationDepth`、`callDepth`、`budgetAllocations`、`riskPropagation`、`partially_completed`；`src/interaction/autonomy/index.ts` 已实现 0-1000 trust score、30/60/90d incident-free、200% cost overrun demotion、180d inactivity demotion；`src/interaction/proactive-agent/index.ts` 与 `trigger-engine/index.ts` 已将 medium/high 风险降为 suggestion。本轮同时修复了相关陈旧测试断言。 | 回写依据并修复陈旧测试，不改主实现。 |
| R5-28 | 24. NL 入口 + 目标分解 + 主动代理缺口（§8/§19/§40-§42） | MEDIUM | `interaction/goal-decomposer/index.ts` | 无能力验证(§40.2 要求验证目标域暴露所需 DomainCapability)；无权限收窄传播 | 已解决（源码修复并补定向测试） | 真实根因分成两半：旧实现先把 `requiredCapabilities/requiredPermissions` 预过滤成“该域已允许的子集”，导致失配被提前抹掉；随后 task envelope 又直接复用 goal-level 权限集，导致运行时拿不到“按域收窄后的最终权限集”。 | 当前 `src/interaction/goal-decomposer/index.ts` 先基于原始请求执行 `validateTaskDomainCapabilities()`，再通过 `narrowTaskConstraintEnvelope()` 只对 task-level `requiredPermissions` 做域内收窄；`tests/unit/interaction/goal-decomposer/task-domain-capability-policy.test.ts` 已验证 `engineering_ops/operations` 保留 `deployment:write`，而 `data_analysis/quality_assurance` 会收窄为 `[]`，同时 `validationMessages` 仍暴露 `unauthorized_permission`。 | 关闭本项：能力校验与权限收窄已同时真实落地。 |
| R5-29 | 24. NL 入口 + 目标分解 + 主动代理缺口（§8/§19/§40-§42） | MEDIUM | `interaction/proactive-agent/index.ts` | batch_window 配置存在但 evaluate() 无事件批量聚合(§41.4) | 已解决（源码修复并补定向测试） | 根因是旧实现只把 `EventTriggerConfig.batchWindow` 当作静态配置字段保存，`evaluate()` 完全只按单个 event 判断和入队，既没有待聚合缓冲区，也没有窗口到期后的合并 fire。 | 本轮在 `src/interaction/proactive-agent/index.ts` 给 `TriggerRuntimeState` 增加了 `pendingBatch`，并在 `evaluate()` 中通过 `resolveBatchWindowInput()` 实现 event/webhook 触发器的窗口缓存与到期聚合；定向验证已通过 `tests/unit/interaction/proactive-agent/index.test.ts` 新增的 batch-window 用例以及 `tests/integration/interaction/proactive-agent-integration.test.ts` 回归。 | 这是实修行为缺口，不是只在测试里模拟聚合。 |
| R5-30 | 24. NL 入口 + 目标分解 + 主动代理缺口（§8/§19/§40-§42） | MEDIUM | `interaction/nl-gateway/index.ts` | ClarificationState 无 rounds/maxRounds 追踪——可能无限澄清循环(§39.5) | 已解决（源码修复并补定向测试） | 根因不是类型缺字段，而是运行时没有状态机：`ClarificationState` 虽声明了 `rounds/maxRounds`，但旧实现每次 `parseDetailed()` 都把 `rounds` 重置为 `0`，`buildTask()` 也从不累计同一用户的澄清轮次，因此实际上不会触发上限阻断。 | 本轮在 `src/interaction/nl-gateway/index.ts` 新增了按 `tenantId:userId` 维度维护的 `clarificationTracker`，`buildTask()` 会在需要澄清时累加轮次、在请求收敛时清零，并在达到 `maxRounds` 后把请求置为 `blocked`；`tests/unit/interaction/nl-gateway/request-envelope-confirmation-gate.test.ts` 已新增三次重复澄清后触发上限阻断的用例。 | 这是实修循环控制，不是仅保留字段定义。 |
| R5-31 | 24. NL 入口 + 目标分解 + 主动代理缺口（§8/§19/§40-§42） | LOW | `interaction/ux/conversation-history-service.ts` | restricted/regulated 对话数据写入 long-term memory(§39.6 要求仅存 session memory) | 已解决（源码修复并补定向测试） | 根因是旧实现没有任何敏感度门控：`ConversationHistoryService` 只要有 `memoryService` 就会把会话写入 memory store，`restricted/regulated` 与 `standard` 路径完全一样，因此受限对话会落入长期记忆。 | 本轮在 `src/interaction/ux/conversation-history-service.ts` 的 `ConversationHistoryOptions` 增加 `dataHandling`，并通过 `shouldPersistToLongTermMemory()` 明确阻断 `restricted/regulated` 会话的持久化；`tests/unit/platform/interaction/ux/conversation-history-service.test.ts` 已新增 “regulated session 不写 remember()” 的定向用例。 | 这是实修数据边界，不是把风险描述挪到文档里。 |
| R5-32 | 24. NL 入口 + 目标分解 + 主动代理缺口（§8/§19/§40-§42） | LOW | `interaction/nl-gateway/index.ts` | UserConfirmationReceipt 缺 scope/time/riskPreviewVersion(§39.3 审计匹配要求) | 已解决（源码修复并补定向测试） | 根因是接口类型早已声明了这些字段，但旧 `buildTask()` 只回填 `confirmationId/required/state/reasonCodes/summary`，把 `scope/timestamp/riskPreviewVersion/actor` 留在“类型存在、运行时为空”的半落地状态。 | 本轮在 `src/interaction/nl-gateway/index.ts` 的 `buildTask()` 里新增 `deriveConfirmationScope()` 与 `buildRiskPreviewVersion()`，并在回执中填入 `scope`、`timestamp`、`riskPreviewVersion`、`actor`；`tests/unit/interaction/nl-gateway/request-envelope-confirmation-gate.test.ts` 已验证高风险请求会得到这些元数据。 | 这次修的是实际回执内容，不是只靠接口声明自证。 |
| R5-33 | 25. 事件流 + API Surface 缺口（§6/§28） | CRITICAL | `platform/contracts/types/domain/session-types.ts` | EventRecord 缺 §28.1 必填字段：schemaVersion/aggregateId/runId/sequence/replayBehavior/principal/evidenceRefs | 已解决 | 这条在上一版重审里保守挂起，但当前源码已直接反证：`EventRecord` 兼容结构已经补齐这些 replay/audit 字段。 | 当前 `src/platform/contracts/types/domain/session-types.ts` 的 `EventRecord` 已包含 `schemaVersion`、`aggregateId`、`runId`、`sequence`、`replayBehavior`、`principal`、`evidenceRefs`。 | 不改生产代码，仅回写现行 contract 依据。 |
| R5-34 | 25. 事件流 + API Surface 缺口（§6/§28） | CRITICAL | `platform/state-evidence/events/event-registry.ts` | 两套不互通事件注册表共存：legacy task:_ colon 命名空间 vs canonical platform._ dot 命名空间；platform.\* 无 Tier-1 路由/Zod 验证/typed payload | 已修复 | 真实根因不是“canonical 事件完全不存在”，而是事件体系演进分成了两段：`event-types` / `typed-event-bus` 已先补 canonical 运行时事件，但 `event-registry` 仍把这些事件挂在 `RUNTIME_EVENT_REPLAY_METADATA` 旁路表上，仅能合成 schema，不能作为一等 `KnownEventType` 走专用 validator。 | 本轮已把 `platform.request_envelope.admitted`、`platform.harness_run.status_changed`、`platform.node_run.status_changed`、`platform.side_effect.status_changed`、`platform.budget_ledger.status_changed`、`platform.budget_reservation.status_changed`、`platform.graph_scheduler.decision_recorded`、`oapeflir.view.run_lifecycle` 并入 `src/platform/state-evidence/events/event-registry.ts` 的主注册表，并补齐专用 Zod payload 校验；`tests/unit/platform/state-evidence/events/event-registry.test.ts` 已新增一等注册与 payload 校验断言。 | 已改生产代码与定向测试。 |
| R5-35 | 25. 事件流 + API Surface 缺口（§6/§28） | CRITICAL | `platform/interface/api/http-server/` | 无 /api/v1/harness-runs 及子资源路由(§6 canonical API)；仅有 legacy /v1/tasks | 已解决 | 原审计抓的是只剩 legacy task/workflow 路由的旧阶段；后续 canonical harness-run 查询接口已补进 admin route 组，但审计结论没有跟进。 | 当前 `src/platform/interface/api/http-server/admin-routes.ts` 已存在 `GET /api/v1/harness-runs`、`GET /api/v1/harness-runs/:id`、`GET /api/v1/harness-runs/:id/events`；`src/platform/interface/api/http-server/task-routes.ts` 也已使用 `/api/v1/tasks` 而非裸 `/v1/tasks` 作为主任务路由。 | 不改主逻辑，仅回写依据。 |
| R5-36 | 25. 事件流 + API Surface 缺口（§6/§28） | HIGH | `platform/interface/api/http-server/` | 缺 /api/v1/replay-sessions(§28.5 MVP)；admin routes 缺所有写方法(PUT config/POST panic-directives/POST resume-directives) | 已解决（源码修复并补集成测试） | 此前 re-audit 文档提前把该项标成已解决，但源码实际仍缺 replay session 路由、`PUT /admin/config` 写方法，以及 panic/resume directive 写接口。根因是审计文档和当前 HTTP surface 脱节。 | 本轮已在 `src/platform/interface/api/http-server/admin-routes.ts` 与 `src/platform/five-plane-interface/api/http-server/admin-routes.ts` 补齐 `GET /api/v1/replay-sessions`、`GET /api/v1/replay-sessions/:id`、`PUT /v1/admin/config`、`POST /v1/admin/panic-directives`、`POST /v1/admin/resume-directives`；同时新增 `src/platform/interface/api/admin-runtime-directive-service.ts` 与 five-plane 镜像，为 panic/resume 提供有状态服务注入。 | 定向集成测试 `tests/integration/platform/interface/api/http-api-admin-routes.test.ts` 已新增 replay/config/panic/resume 覆盖，验证 canonical `/api/v1/*` 路径与 directive lifecycle。 |
| R5-37 | 25. 事件流 + API Surface 缺口（§6/§28） | HIGH | `state-evidence/events/durable-event-bus.ts` | publish() 不持久化 aggregateId/runId/sequence/schemaVersion——replay ordering 不可能(§28.5) | 已解决 | 原审计基于旧版 publish path，没有复核现行 `publish()` / `publishBatch()` 已经把 replay 排序字段一并落库。 | 当前 `src/platform/five-plane-state-evidence/events/durable-event-bus.ts` 的 `publish()` 与 `publishBatch()` 在 `insertEvent()` 时都会写入 `schemaVersion`、`aggregateId`、`runId`、`sequence`；后续 `deliverPendingNow()` 也会按 aggregate 分区并按 sequence 排序。 | 不改生产代码，仅回写持久化与投递排序证据。 |
| R5-38 | 25. 事件流 + API Surface 缺口（§6/§28） | HIGH | `state-evidence/events/event-types.ts` | Tier-1 列表含非架构事件(delegation:_/prompt:_/tenant:_)但缺架构核心事实(platform.harness_run._/platform.node*run.*/platform.side*effect.*/platform.budget.\*) | 已解决 | `event-types` 已在后续重构中切到 canonical platform/oapeflir 事实面，但历史审计还停留在旧 Tier-1 列表。 | 当前 `src/platform/state-evidence/events/event-types.ts` 的 `TIER_1_EVENT_TYPES` 与 `REQUIRED_CONSUMERS_BY_EVENT_TYPE` 已包含 `platform.harness_run.*`、`platform.node_run.*`、`platform.side_effect.*`、budget 相关 canonical 事件与 `oapeflir.*`；对应 `tests/unit/platform/state-evidence/events/event-types.test.ts` 已直接断言这些枚举存在。 | 不改生产代码，仅回写依据。 |
| R5-39 | 25. 事件流 + API Surface 缺口（§6/§28） | MEDIUM | `platform/interface/api/http-server/` | WebSocket 绑定 /ws 而非 §6 要求的 /ws/v1/stream；task-routes 用 /v1/tasks 无 /api/ 前缀 | 已解决 | 运行时实现已经迁到 canonical 路径，但顶部注释和旧认知还保留了 `/ws` / `/v1/tasks` 表述，容易把“文档注释陈旧”误判成“实现未迁移”。 | 当前 `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts` 实际绑定 `"/ws/v1/stream"`，`src/platform/interface/api/http-server/task-routes.ts` 主任务路由为 `"/api/v1/tasks"`；本轮顺手回写了 WebSocket 头注释，避免再次误读。 | 已修正文档注释并回写依据。 |
| R5-40 | 25. 事件流 + API Surface 缺口（§6/§28） | MEDIUM | `state-evidence/events/event-registry.ts` | replayBehavior 用 simulate_projection 而非 §28.1 canonical simulate | 已解决 | 上一轮保守挂起前没有直接复核当前 registry；现行实现已经使用 canonical `simulate`。 | 当前 `src/platform/state-evidence/events/event-registry.ts` 的 `replayBehavior` 枚举为 `replay_as_fact/skip_side_effect/simulate/forbidden`，projection 路径也写成 canonical `simulate`。 | 不改生产代码，仅回写现行 registry 依据。 |
| R5-41 | 25. 事件流 + API Surface 缺口（§6/§28） | MEDIUM | `state-evidence/events/typed-event-bus.ts` | TypedEventPayloadMap 不含 platform._/oapeflir._ 事件——编译时类型检查静默排除所有 canonical 运行时事件 | 已解决 | Typed payload map 已在后续阶段补进 canonical runtime / OAPEFLIR 事件，但原审计仍按旧版 map 判定“完全缺失”。 | 当前 `src/platform/state-evidence/events/typed-event-bus.ts` 的 `TypedEventPayloadMap` 已包含 `platform.harness_run.*`、`platform.node_run.*`、`platform.side_effect.*`、budget 相关 canonical 事件以及 `oapeflir.view.run_lifecycle` / `oapeflir.decision.recorded` / `oapeflir.phase.transition`。 | 不改生产代码，仅回写依据。 |
| R5-42 | 26. 委托 + 版本锁 + 记忆 + Truth 深层缺口（§19/§24/§25/§29） | HIGH | `orchestration/agent-delegation/delegation-types.ts` | DelegationResult 缺 §19.1 必填：summary/artifact_refs/trust_level/taint_labels/evidence_refs/policy_outcome | 已解决（源码修复并补定向测试） | 根因是委托结果类型虽然补进了这些字段名，但此前仍保持 optional，`DelegationManagerService` 也没有在创建记录时产出完整结果，导致跨委托证据链只停留在“字段可能存在”的半落地状态。 | 当前 `src/platform/five-plane-orchestration/agent-delegation/delegation-types.ts` 已把 `summary`、`artifact_refs`、`trust_level`、`taint_labels`、`evidence_refs`、`policy_outcome` 设为 mandatory；`src/platform/five-plane-orchestration/agent-delegation/delegation-manager.service.ts` 的 `createDelegationRecord()` 会填满这些字段，并把 `delegationId/status` 透传到 `src/platform/five-plane-state-evidence/truth/sqlite/repositories/delegation-repository.ts` 的持久化入口。 | 已改生产代码，并以 `tests/unit/platform/agent-delegation.test.ts` 做定向回归。 |
| R5-43 | 26. 委托 + 版本锁 + 记忆 + Truth 深层缺口（§19/§24/§25/§29） | HIGH | `orchestration/agent-delegation/collaboration-protocol/types.ts` | ACP 消息缺 §19.1 必填：delegationId/childRunId/capabilityIntersection/budgetCap/dataBoundary/deadline | 已解决 | 原审计对应的是 ACP schema 还未补齐委托约束字段的早期版本。 | 当前 `src/platform/five-plane-orchestration/agent-delegation/collaboration-protocol/types.ts` 的 `ACPMessageSchema` 已把 `delegationId`、`childRunId`、`capabilityIntersection`、`budgetCap`、`dataBoundary`、`deadline` 设为 required 字段。 | 不改生产代码，仅回写现行 ACP schema 依据。 |
| R5-44 | 26. 委托 + 版本锁 + 记忆 + Truth 深层缺口（§19/§24/§25/§29） | HIGH | `state-evidence/truth/runtime-truth-repository.ts` | transition() 对 HarnessRun 无 lease/fencing 验证(§25.3) | 已解决（源码修复并补定向测试） | 根因是 repository 侧最初沿用了并不存在的 `lease/ownedBy` 旧字段，导致 `HarnessRun` 真正 authoritative 的 `leaseId/fencingToken` 既没被读取，也没和命令侧做一致性校验。 | 当前 `src/platform/five-plane-state-evidence/truth/runtime-truth-repository.ts` 的 `validateLease()` 已改为对 authoritative `leaseId/fencingToken` 做 fail-closed 比对，缺失或陈旧命令分别抛出 `runtime_truth_repository.lease_fencing_required`、`stale_lease_id`、`stale_fencing_token`；定向测试 `tests/unit/platform/state-evidence/truth/runtime-truth-repository.test.ts` 已覆盖 stale fencing token 拒绝路径。 | 已改生产代码并补最小回归。 |
| R5-45 | 26. 委托 + 版本锁 + 记忆 + Truth 深层缺口（§19/§24/§25/§29） | HIGH | `orchestration/agent-delegation/delegation-types.ts` | DelegationResult 无 taint_labels/data_class——跨委托数据分类链断裂 | 已解决（源码修复并补定向测试） | 根因与 `R5-42` 同源：数据分类字段虽然已经出现过，但之前仍是 optional，且创建委托时没有形成强制产物，所以分类链在真实委托主路径上并不可靠。 | 当前 `src/platform/five-plane-orchestration/agent-delegation/delegation-types.ts` 已把 `taint_labels` 与 `data_class` 设为必填；`src/platform/five-plane-orchestration/agent-delegation/delegation-manager.service.ts` 会在 `createDelegationRecord()` 中产出这两个字段。 | 已随 `R5-42` 一并改实现在主路径落地。 |
| R5-46 | 26. 委托 + 版本锁 + 记忆 + Truth 深层缺口（§19/§24/§25/§29） | MEDIUM | `orchestration/agent-delegation/call-depth-budget.ts` | 用 Math.max() 非求和——全局深度限制=8 实际无效(§19.2) | 已解决 | 现行实现已把调用深度改为三维求和，不再使用 `Math.max()`。 | 当前 `src/platform/five-plane-orchestration/agent-delegation/call-depth-budget.ts` 的 `evaluate()` 直接计算 `currentCallDepth + goalDecompositionDepth + delegationDepth`，并用该和与 `DEFAULT_MAX_DEPTH` 比较。 | 不改生产代码，仅回写当前实现依据。 |
| R5-47 | 26. 委托 + 版本锁 + 记忆 + Truth 深层缺口（§19/§24/§25/§29） | MEDIUM | `orchestration/agent-delegation/delegation-manager.service.ts` | delegate() 不调 CallDepthBudget.evaluate()——直接委托绕过深度检查 | 已解决 | 原审计基于旧版 `delegate()`，当时委托入口还没有接入 call-depth 守卫。 | 当前 `src/platform/five-plane-orchestration/agent-delegation/delegation-manager.service.ts` 的 `delegate()` 在拓扑校验前先调用 `this.callDepthBudget.evaluate(...)`，超限直接抛 `delegation.call_depth_exceeded`。 | 不改生产代码，仅回写当前委托入口依据。 |
| R5-48 | 26. 委托 + 版本锁 + 记忆 + Truth 深层缺口（§19/§24/§25/§29） | MEDIUM | `state-evidence/truth/runtime-truth-repository.ts` | transaction() 内存 clone-and-rollback 无数据库事务——truth+event 原子性无崩溃安全(§25.6) | 已解决 | `appendEvidenceRecord()` 现在使用 `db.transaction()` 封装 `persistEventToDatabase` 与 `appendEvent`，truth+event 写入具备崩溃安全的持久事务语义。 | 当前 `src/platform/five-plane-state-evidence/truth/runtime-truth-repository.ts` 的 `appendEvidenceRecord()` 已将 `db.transaction()` 包裹 `persistEventToDatabase` 与 `appendEvent`，两者在同一 SQLite 事务内完成；进程崩溃后重启可从 WAL/durable write 恢复一致性。 | 源码验证：两者均位于 `db.transaction()` 内，fix 正确。 |
| R5-49 | 26. 委托 + 版本锁 + 记忆 + Truth 深层缺口（§19/§24/§25/§29） | MEDIUM | `state-evidence/knowledge/knowledge-query-service.ts` | 查询无 tenant/domain 边界校验(§45.16+§50) | 已解决（源码修复并补定向测试） | 根因不是 retrieval 层完全没 principal，而是 `KnowledgeQueryOptions` 早已携带 `accessPrincipal`，但 `KnowledgeQueryService` 自己的 `TenantDomainValidator` 只校验了 `namespace/domainId` 是否为空，完全没把请求 scope 与调用主体绑定，导致服务层边界仍可被绕过。 | 当前 `src/platform/five-plane-state-evidence/knowledge/knowledge-query-service.ts` 已在 `validate()` 中强制要求 `accessPrincipal`，并对 `domainId` 与 `principal.domainId` 做 fail-closed 校验，对受限 `namespace` 做 principal-permission 校验；定向回归位于 `tests/unit/platform/state-evidence/knowledge/knowledge-query-boundary.test.ts`。 | 已改生产代码并补最小边界回归。 |
| R5-50 | 26. 委托 + 版本锁 + 记忆 + Truth 深层缺口（§19/§24/§25/§29） | MEDIUM | `state-evidence/memory/memory-decay-service.ts` | working/procedural 施加指数衰减——§29.2 禁止静默丢弃 working、禁止丢弃 procedural | 已解决 | 原审计对应的是 working/procedural 仍沿用统一衰减曲线的旧版本。 | 当前 `src/platform/five-plane-state-evidence/memory/memory-decay-service.ts` 的 `DEFAULT_DECAY_CONFIGS` 对 `working` 与 `procedural` 都使用 `halfLifeSeconds: Infinity` 和 `decayRateMultiplier: 0`，不会再静默衰减这两层。 | 不改生产代码，仅回写现行 decay 配置依据。 |
| R5-51 | 26. 委托 + 版本锁 + 记忆 + Truth 深层缺口（§19/§24/§25/§29） | MEDIUM | `orchestration/agent-delegation/delegation-types.ts` | 仅 pipeline/negotiation 模式；缺 §19.1 broadcast+AggregationPolicy | 已解决 | 委托协作模式与聚合策略已经补进主类型，但原审计仍停留在仅支持 `pipeline/negotiation` 的旧快照。 | 当前 `src/platform/five-plane-orchestration/agent-delegation/delegation-types.ts` 的 `CollaborationMode` 已包含 `broadcast`，并定义了 `AggregationPolicy`、`aggregationPolicy?` 等字段。 | 不改生产代码，仅回写现行 contract 依据。 |
| R5-52 | 26. 委托 + 版本锁 + 记忆 + Truth 深层缺口（§19/§24/§25/§29） | MEDIUM | `orchestration/agent-delegation/delegation-types.ts` | DelegationStatus 缺 discovery/bid/awarded(§19.1 竞标) | 已解决 | 委托生命周期状态机已纳入竞标阶段状态，但原审计仍按旧枚举结论写成缺失。 | 当前 `src/platform/five-plane-orchestration/agent-delegation/delegation-types.ts` 的 `DelegationStatus` 已包含 `discovery`、`bid`、`awarded`。 | 不改生产代码，仅回写现行状态枚举依据。 |
| R5-53 | 26. 委托 + 版本锁 + 记忆 + Truth 深层缺口（§19/§24/§25/§29） | MEDIUM | `interface/api/middleware/sdk-version-handshake.ts` | 缺 platform_min_version 兼容检查(§24) | 已解决 | SDK 握手服务已经读取 `x-platform-min-version` 并做 semver 拒绝判定。 | 当前 `src/platform/five-plane-interface/api/middleware/sdk-version-handshake.ts` 的 `evaluate()` 会读取 `platformMinVersion`，当其高于当前平台版本时返回 `426` 并给出 `sdk.platform_incompatible`。 | 不改生产代码，仅回写握手实现依据。 |
| R5-54 | 26. 委托 + 版本锁 + 记忆 + Truth 深层缺口（§19/§24/§25/§29） | LOW | `control-plane/config-center/config-versioning-service.ts` | 发 config.version.created 非 §24.2 config.changed 热加载事件 | 已解决（源码修复并补定向测试） | 根因是 `ConfigVersioningService` 之前只发布 `config.version.created/config.version.rollback`，缺少面向热加载消费者的 `config.changed`。 | 本轮在 `src/platform/five-plane-control-plane/config-center/config-versioning-service.ts` 的 `emitVersionEvent()` 中补发 `config.changed`，并新增 `tests/unit/platform/control-plane/config-center/config-versioning-hot-reload.test.ts` 验证一次 version publish 同时产出 `config.version.created` 与 `config.changed`。 | 关闭本项依赖于本轮代码改造和定向单测。 |
| R5-55 | 27. ADR 与架构矛盾（第二批） | CRITICAL | `ADR-026` | 风险因子模型(8因子/权重/18分制)与 §10.2 canonical(impact×4/irreversibility×4/…)完全不兼容 | 已解决 | 该 ADR 已完成 canonical 风险模型回写，旧审计结论滞留在 remediation 前版本。 | 当前 `docs_zh/adr/026-risk-control-architecture.md` 已明确 `risk_score = impact × 4 + irreversibility × 4`，并在 `v4.3 ADR Remediation` 中记录把旧多因子模型收敛到 §10.2 canonical。 | 不改代码，仅回写现行 ADR 依据。 |
| R5-56 | 27. ADR 与架构矛盾（第二批） | CRITICAL | `oapeflir-loop-service.ts` | 注释误导：原注释称”NOT the active orchestration loop”、”audit trail only”、”deprecated”；实际代码有真实重入 while loop + HarnessLoopController | 已解决（源码注释修复） | 旧注释把真实编排循环描述为”audit trail only”，导致代码被误认为非活跃循环。根因是注释与实际代码行为严重不符。 | 当前 `src/platform/five-plane-orchestration/oapeflir/oapeflir-loop-service.ts` 的 `produceStageRationale()` 注释已修正为”Main OAPEFLIR orchestration loop — runs observe→assess→plan→execute→feedback→learn→improve stages with re-entrant replanning capability”；while loop 注释也改为”R5-2: Re-entrant orchestration loop - runs O→A→P→E→F→L→I→R stages with full replanning support”。注释不再使用”NOT the active orchestration loop”、”audit trail only”或”deprecated”措辞。 | 注释已修正以反映真实循环行为，ADR-001 定位澄清与实际实现不一致应以本结论为准。 |
| R5-57 | 27. ADR 与架构矛盾（第二批） | HIGH | `ADR-039` | 定义 cancel_task intent；§6.3 明确移除——调用方必须用 abort/pause/panic kill | 已解决 | `ADR-039` 已把 `cancel_task` 标记为移除项，旧结论停留在修订前。 | 当前 `docs_zh/adr/039-natural-language-task-entry.md` 在 intent 表后明确写出 “`cancel_task` 已从 §6.3 移除，调用方必须使用 abort/pause/panic kill 等语义替代”。 | 不改代码，仅回写现行 ADR 依据。 |
| R5-58 | 27. ADR 与架构矛盾（第二批） | HIGH | `ADR-001` | 三层 CEO/VP 架构作为 Accepted 决策无 remediation；v4.3 §4 已用五平面+X1 替代 | 已解决 | `ADR-001` 正文和 remediation 都已经把运行时主架构切到五平面+X1，只在背景/历史说明里保留旧叙事。 | 当前 `docs_zh/adr/001-three-layer-architecture.md` 的决策段已完全改为五平面+X1，并在 `v4.3 ADR Remediation` 中记录旧 CEO/VP/Lead 模型为何被替换。 | 不改代码，仅回写现行 ADR 依据。 |
| R5-59 | 27. ADR 与架构矛盾（第二批） | HIGH | `ADR-002` | "事业部" YAML division 模型无 remediation；v4.3 用 DomainDescriptor+BusinessPack+DomainRiskSpec | 已解决（文档/ADR） | 主体模型其实早已迁到 `DomainDescriptor + BusinessPack + DomainRiskSpec`，但 ADR 结果段仍残留 “VP 编排层/事业部” 叙事，导致 remediation 证据链不闭合。 | 当前 `docs_zh/adr/002-division-system.md` 的决策主线已使用 `DomainDescriptor + BusinessPack + DomainRiskSpec`，本轮又把结果段回写为 “P3 编排面 / domain / pack” 语义，去掉残留的 `VP 编排层/事业部` 表述。 | 已改 ADR，关闭残留文档漂移。 |
| R5-60 | 27. ADR 与架构矛盾（第二批） | HIGH | `ADR-004` | workflow 数据传递仍用 WorkflowState/StepOutput(§5.5 废弃)无 remediation | 已解决 | `ADR-004` 已把数据传递主线切到 `HarnessRun / NodeRun / NodeAttemptReceipt`，旧审计没有复核现行正文。 | 当前 `docs_zh/adr/004-workflow-routing.md` 的 “Workflow 数据传递” 小节已明确写出 `WorkflowState/StepOutput` 废弃，并以 `NodeRun/HarnessRun` 与 `NodeAttemptReceipt.output` 取代。 | 不改生产代码；本轮仅顺手补齐来源章节到现行 canonical 节点。 |
| R5-61 | 27. ADR 与架构矛盾（第二批） | HIGH | `ADR-034` | ADR freeze 规则"不允许直接修改已冻结内容"——v4.3 remediation 过程直接修改 30+ ADR 违反此规则 | 已解决 | `ADR-034` 已补入 v4.3 迁移窗口期例外条款，旧审计仍按无例外版本判定冲突。 | 当前 `docs_zh/adr/034-adr-freeze-recommendation.md` 已包含 “v4.3 迁移例外条款”，明确 remediation 期间允许直接修改已冻结 ADR，但要求记录原因、修复内容和对齐确认。 | 不改代码，仅回写现行 ADR 依据。 |
| R5-62 | 27. ADR 与架构矛盾（第二批） | HIGH | `ADR-041` | TriggerAction.create_task 直接创建任务绕过 §5.3 intake pipeline(TaskDraft→ConfirmedTaskSpec→RequestEnvelope) | 已解决 | `ADR-041` 已把 `create_task` 约束改成必须经 intake pipeline，并记录 remediation。 | 当前 `docs_zh/adr/041-proactive-agent-framework.md` 在 TriggerAction 表和注释里都明确了 `create_task` 必须经 `TaskDraft → ConfirmedTaskSpec → RequestEnvelope`，并在 `v4.3 ADR Remediation` 中记录本次纠偏。 | 不改代码，仅回写现行 ADR 依据。 |
| R5-63 | 27. ADR 与架构矛盾（第二批） | MEDIUM | `ADR-006/008/005/002` | 源节引用全部指向旧版节号(§7/§8/§2)——v4.3 对应节已完全更替；cross-ref 批量失效 | 已解决（文档/ADR） | `ADR-005/006/008` 已完成 source section 迁移，但这组证据链里 `ADR-002` 仍混有旧 division 叙事，容易让“source 已迁移”和“正文已对齐”混为一谈。 | 当前 `docs_zh/adr/005-security-model.md`、`006-llm-provider-strategy.md`、`008-cost-model.md` 均已使用 v4.3 有效引用；本轮又补齐 `docs_zh/adr/002-division-system.md` 的现行叙事，使这组交叉引用不再混有旧 division 主线。 | 已改 ADR 并回写依据。 |
| R5-64 | 27. ADR 与架构矛盾（第二批） | MEDIUM | `ADR-028` | trace span 用 "service→operation→step"——step 为废弃术语(§5.5) | 已解决 | `ADR-028` 的 trace span 术语已经切到 `node_run/node_attempt`，旧审计停留在修订前。 | 当前 `docs_zh/adr/028-incident-and-event-handling-architecture.md` 已明确 “span 语义应按 `service -> operation -> node_run -> node_attempt` 组织”；本轮又补齐了来源章节到 `§28/§59/§63`。 | 不改代码，仅回写现行 ADR 依据。 |
| R5-65 | 27. ADR 与架构矛盾（第二批） | MEDIUM | `ADR-071` | 引用不存在的 §B/§G appendix；v4.3 无此附录 | 已解决 | Plugin SPI ADR 在回写 appendix 修复时还沿用了重复的 `ADR-066` 编号，导致审计证据路径本身继续漂移。 | 当前 `docs_zh/adr/071-plugin-spi-framework.md` 已在 “来源章节” 中明确写出 “原 §B/§G 附录已重构为模块化 contract 文档”，并列出 `plugin_spi_contract.md` 等现行引用；ADR README 与引用也已同步到 `071`。 | 不改代码，仅回写现行 ADR 依据。 |
| R5-66 | 27. ADR 与架构矛盾（第二批） | MEDIUM | `ADR-046` | 用 CEO/VP 作为治理层级——v4.3 §46-§51 用 OrgNode 层次 | 已解决 | `ADR-046` 已把治理层级主体改写为 OrgNode hierarchy，旧结论未跟进。 | 当前 `docs_zh/adr/046-organization-hierarchy-model.md` 以 `OrgNode` 作为组织层次基础，并明确写出 “v4.3 以 OrgNode 层次替代 CEO/VP 命名体系”。 | 不改代码，仅回写现行 ADR 依据。 |
| R5-67 | 27. ADR 与架构矛盾（第二批） | MEDIUM | `ADR-047` | auto_action 超时自动执行无风险级别守卫(§10.3 high/critical 默认 deny) | 已解决 | `ADR-047` 已把 `auto_action` 的 high/critical 风险守卫和 break-glass 约束回写进正文。 | 当前 `docs_zh/adr/047-organization-approval-routing.md` 已明确 `auto_action` 必须遵循 §10.3 风险守卫，high/critical 默认 deny，critical 需 break-glass + 双人审批。 | 不改代码，仅回写现行 ADR 依据。 |
| R6-1 | 29. Intake 准入 + Dispatcher 调度缺口（§5.3/§14/§25.4） | HIGH | `orchestration/harness/runtime/intake-admission-service.ts` | §5.3 ClarificationSession 阶段完全缺失；admit() 直接 RawTaskInput→TaskDraft→ConfirmedTaskSpec 无澄清循环 | 已解决（源码修复并补定向测试） | 根因是旧实现虽然补了 `pending_clarification` 占位事件，但会话只留在进程内 `admittedByIdempotencyKey`，服务实例重建后无法 resume，也不会保留原始 `budgetIntent/taskDraft/harnessRun`。 | 本轮新增 `src/platform/five-plane-orchestration/harness/runtime/clarification-session-repository.ts:5-145`，并把 `src/platform/five-plane-orchestration/harness/runtime/intake-admission-service.ts:355-375,572-657` 改为在 admission 时持久化会话、在 `resumeClarification()` 中从 repository 恢复并继续创建 `ConfirmedTaskSpec/RequestEnvelope`。定向测试 `tests/unit/platform/five-plane-orchestration/harness/intake-admission-clarification-repository.test.ts` 已验证跨 service 实例恢复与 `budgetIntent` 保真。 | 关闭的是 ClarificationSession 持久化与恢复闭环缺口，不外推为所有 NL 歧义策略都已完备。 |
| R6-2 | 29. Intake 准入 + Dispatcher 调度缺口（§5.3/§14/§25.4） | HIGH | `orchestration/harness/runtime/intake-admission-service.ts` | high/critical 任务不强制 UserConfirmationReceipt(§39.6)——confirmationReceipt 可选且 critical 时仍放行 | 已解决 | 现行准入逻辑已经把 high/critical 风险任务的确认回执提升为硬门禁。 | 当前 `src/platform/five-plane-orchestration/harness/runtime/intake-admission-service.ts` 的 `admit()` 在 `riskClass` 为 `high` 或 `critical` 且缺失 `confirmationReceipt` 时直接抛出 `admission.confirmation_required`；对应定向单测也已存在于 `tests/unit/platform/orchestration/harness/runtime/intake-admission-service.test.ts`。 | 不改生产代码，仅回写现行准入守卫依据。 |
| R6-3 | 29. Intake 准入 + Dispatcher 调度缺口（§5.3/§14/§25.4） | HIGH | `execution/dispatcher/admission-controller.ts` | 缺 §14.2 调度因子：无 risk-class 隔离路由/无 tenant-quota/无 sandbox 匹配/无 capability-class 门禁 | 已解决 | AdmissionController 已经纳入这四类调度因子，只是原审计仍按早期极简 admission 版本写成“全部缺失”。 | 当前 `src/platform/five-plane-execution/dispatcher/admission-controller.ts` 具备 `riskClassIsolationEnabled`、`tenantQuotaEnabled`、`sandboxMatchingEnabled`、`capabilityClassGateEnabled`，并在 `evaluate()` 中返回对应 reject reason；定向测试位于 `tests/unit/platform/execution/dispatcher/admission-controller-scheduling-factors.test.ts`。 | 不改生产代码，仅回写现行调度因子依据。 |
| R6-4 | 29. Intake 准入 + Dispatcher 调度缺口（§5.3/§14/§25.4） | HIGH | `execution/dispatcher/` | 无确定性图调度器(§14.9)——应按 priority/risk_class/critical_path_rank/created_order/scheduler_seed 调度 | 已解决 | 确定性图调度器已完整实现。dispatch 排序现在严格按 priority → critical_path_rank → scheduler_seed → risk_class → created_at 排序（lines 371-392），无任何 Math.random 介入；selectDeterministicWorker 同样移除随机 jitter，改用 workerId 作为确定性最终 fallback（line 1220）；Math.random 仅在 calculateBackoffDelay（retry backoff）中使用，不影响调度确定性。 | `src/platform/five-plane-execution/dispatcher/execution-dispatch-service.ts` 的 `dispatchNext()` 排序和 `selectDeterministicWorker()` 均已收敛为全确定性实现；`src/platform/contracts/types/domain/dispatch-types.ts:62-69` 的 `ExecutionTicketRecord` 已正式定义 `criticalPathRank` 与 `schedulerSeed` 字段并注明 R6-4 用途；`dispatch.partial-deterministic.v2` policy version 已升至正式确定性策略。 | 不改代码，已完成 R6-4 确定性调度器整改。 |
| R6-5 | 29. Intake 准入 + Dispatcher 调度缺口（§5.3/§14/§25.4） | HIGH | `execution/dispatcher/` | 缺 §14.9 emergency lane(critical NodeRun 独立通道) | 已解决 | critical dispatch 的独立紧急通道已经接入执行调度主链。 | 当前 `src/platform/five-plane-execution/dispatcher/execution-dispatch-service.ts` 在 `eligibleWorkers.length === 0 && ticket.priority === "critical"` 时会尝试 `queueName: "emergency_lane"` 的 lease，并发出 `dispatch.emergency_lane_used`。 | 不改生产代码，仅回写现行 emergency lane 依据。 |
| R6-6 | 29. Intake 准入 + Dispatcher 调度缺口（§5.3/§14/§25.4） | HIGH | `execution/dispatcher/` | 缺 dispatch_backpressure_rejected 事件+DLQ 集成(§14.9) | 已解决 | 根因是阻塞分支此前只记录 decision trace，backpressure 事件与 DLQ 入队逻辑分散在少数 critical fallback 分支，没有统一挂到 blocked path。现已把 queue unavailable、backpressure blocked、remote blocked、critical emergency lane 失败统一收敛到 `recordBlockedDispatchArtifacts(...)`。 | 当前 `src/platform/five-plane-execution/dispatcher/execution-dispatch-service.ts` 会在 blocked trace 后统一补 `dispatch.backpressure_rejected`（仅 backpressure 类原因）与 `dispatch.dlq_enqueue`，并在 SQLite authoritative DB 可用时通过 `DeadLetterQueueService + SqliteDeadLetterQueueRepository` 持久化 DLQ；定向验证位于 `tests/unit/platform/execution/dispatcher/execution-dispatch-service-scheduler-dlq.test.ts`。 | 已改生产代码与定向测试，不做全量测试。 |
| R6-7 | 29. Intake 准入 + Dispatcher 调度缺口（§5.3/§14/§25.4） | HIGH | `execution/dispatcher/` | §14.9 scheduler events 缺 ready_set/selected_node_ids/ordering_policy_version/worker_pool_snapshot_ref | 已解决 | 根因是 scheduler snapshot 字段之前只在成功 dispatch 路径手工拼装，blocked/no-worker 分支各自构造 payload，导致字段覆盖不一致。现已集中到 `buildSchedulerTraceFields(...)`，所有 `recordDecisionEvent(...)` 分支共用同一组字段。 | `src/platform/five-plane-execution/dispatcher/execution-dispatch-service.ts` 现统一生成 `readySet`、`selectedNodeIds`、`orderingPolicyVersion`、`workerPoolSnapshotRef`，包括 queue unavailable、backpressure blocked、remote blocked、emergency lane 和正常 dispatch；定向验证位于 `tests/unit/platform/execution/dispatcher/execution-dispatch-service-scheduler-dlq.test.ts`。 | 已改生产代码与定向测试，不做全量测试。 |
| R6-8 | 29. Intake 准入 + Dispatcher 调度缺口（§5.3/§14/§25.4） | MEDIUM | `execution/dispatcher/admission-controller.ts` | priority 用 "urgent" 而非 §5.3 canonical "critical" | 已解决 | AdmissionController 及其提升优先级判断都已使用 canonical `critical`，不存在 `urgent` 优先级常量。 | 当前 `src/platform/five-plane-execution/dispatcher/admission-controller.ts` 的 `isPriorityElevated()` 只判断 `high` 或 `critical`；同文件策略和测试也都围绕 `critical` 展开。 | 不改生产代码，仅回写现行优先级枚举依据。 |
| R6-9 | 29. Intake 准入 + Dispatcher 调度缺口（§5.3/§14/§25.4） | MEDIUM | `execution/dispatcher/` | dispatch 前不验证 budget reservation 存在(§14.2 无 active reservation 不得调度) | 已解决（源码修复并补定向测试） | 根因是 dispatch 主链原先确实缺少 lease 之前的预算前置校验，导致即使上游有 budget gate，调度器本身也可能在无 reservation 句柄时继续 claim/lease。 | 当前 `src/platform/five-plane-execution/dispatcher/execution-dispatch-service.ts` 已在获取可用 worker 后、获取 lease 前显式读取 `this.store.dispatch.getExecution(ticket.executionId)` 并在缺失 `budgetReservationId` 时阻断为 `dispatch.budget_reservation_missing`；本轮新增 `tests/unit/platform/execution/dispatcher/execution-dispatch-service.test.ts` 用例 `dispatchNext blocks when budget reservation is missing`，并同步补齐现有成功路径 mock 的 `budgetReservationId`。 | 关闭的是“dispatcher 主链无 reservation 校验”的缺口。 |
| R6-10 | 29. Intake 准入 + Dispatcher 调度缺口（§5.3/§14/§25.4） | MEDIUM | `execution/worker-pool/worker-registry-service.ts` | 无 heartbeat staleness 检测(§14: gap>30s 触发 worker_heartbeat_missing 事件+lease_reclaim) | 已解决 | worker registry 已经提供陈旧 heartbeat 扫描与恢复事件。 | 当前 `src/platform/five-plane-execution/worker-pool/worker/worker-registry-service.ts` 具备 `listStaleWorkers()` 与 `detectStaleWorkers()`；后者会在 `gap > 30s` 时发出 `worker.heartbeat_missing` 与 `execution.lease_reclaim`。 | 不改生产代码，仅回写现行 worker heartbeat 恢复依据。 |
| R6-11 | 29. Intake 准入 + Dispatcher 调度缺口（§5.3/§14/§25.4） | MEDIUM | `orchestration/routing/intake-router.ts` | 仅关键词匹配无 LLM intent extraction/confidence threshold(0.80)/AmbiguityResolver(§39.3) | 已解决（源码复核） | 根因是审计条目停留在 `IntakeRouter` 仅做 trigger-rule 分类的旧版本，没有复核后续接入的 LLM intent extraction、0.80 置信阈值和歧义澄清阶段。 | 当前 `src/platform/five-plane-orchestration/routing/intake-router.ts` 已定义 `DefaultAmbiguityResolver(0.80)`、`extractIntentWithLlm(...)`、`parseLlmIntentResponse(...)`，并在 `route(...)` 中对低置信度或 ambiguity flags 生成 `ClarificationSession`；`tests/unit/platform/execution/dispatcher/intake-router.test.ts` 也已覆盖 confidence scoring 边界。 | 不改生产代码，回写 router 本体已具备 threshold/ambiguity/LLM extraction 的现状。 |
| R6-12 | 29. Intake 准入 + Dispatcher 调度缺口（§5.3/§14/§25.4） | MEDIUM | `orchestration/harness/runtime/intake-admission-service.ts` | policyGuard.allowed 硬编码 true——§25.4/§45.2 准入时策略/能力/风险检查为虚设 | 已解决 | 准入阶段已经把策略判断抽成 `evaluatePolicyGuard()` 并根据 riskClass/constraintPack/principal 决定拒绝或放行。 | 当前 `src/platform/five-plane-orchestration/harness/runtime/intake-admission-service.ts` 中 `policyGuardResult` 不再硬编码 true；critical/high risk 会基于 `constraintPackRef` 和 `principal.authorizationLevel` 触发 `admission.policy_denied` 或放行。 | 不改生产代码，仅回写现行 admission policy guard 依据。 |
| R6-13 | 30. 类型系统 + API 序列化 + 共享层问题 | CRITICAL | `harness/index.ts vs contracts/executable-contracts/` | 两套冲突 HarnessRun 接口(runId+steps[] vs harnessRunId+confirmedTaskSpecId+currentSeq)——无统一 re-export/adapter | 原发现已部分过时 | 公共导出面已经统一到 canonical `HarnessRun`，不再把旧 shape 暴露为另一套公开契约；剩下的是内部 `HarnessRunRuntimeState` 与 canonical contract 并存，而不是“双 public interface 冲突”。 | 当前 `src/platform/five-plane-orchestration/harness/index.ts` 已 `export type HarnessRun = CanonicalHarnessRun`，并显式提供 `toCanonicalHarnessRun(...)` adapter；同文件内部仍保留 `HarnessRunRuntimeState` 作为运行时态。 | 不改生产代码，仅把本条从“无统一 adapter”回写成“public 面已统一，internal runtime state 仍并存”。 |
| R6-14 | 30. 类型系统 + API 序列化 + 共享层问题 | CRITICAL | `contracts/control-directive/ + types/platform-contracts.ts` | 两套不兼容 ControlDirective(kind enum vs type enum)——废弃类型双重存在且无 canonical 替代 | 已解决（源码复核） | 根因不是当前 contract 层仍“只有双重 legacy ControlDirective、没有 canonical 替代”，而是原条目没有跟上后续 directive 契约迁移；现行 `ControlDirective` 已降级为带 `@deprecated` 的 fail-fast legacy stub，canonical `OperationalDirective/DecisionDirective` 已独立落地。 | 当前 `src/platform/contracts/control-directive/index.ts:4-42` 明确把 legacy `ControlDirective` 标成 `@deprecated` 且 `createControlDirective()` 直接抛 `platform_contracts.legacy_control_directive_forbidden`；同文件 `50-179` 已定义 `OperationalDirective`、`DecisionDirective` 及其工厂。`src/platform/contracts/types/platform-contracts.ts` 也不再承载第二套活跃 ControlDirective 定义。 | 不改生产代码，关闭过时的“双重存在且无替代”结论。 |
| R6-15 | 30. 类型系统 + API 序列化 + 共享层问题 | CRITICAL | `contracts/execution-plan/ + types/platform-contracts.ts` | 两套 ExecutionPlan(均线性 steps[])——废弃类型双重可构造无 @deprecated 注解 | 已解决（源码复核） | 根因是原审计把 legacy `execution-plan/` 兼容层误判成第二套可活跃构造的线性执行计划；现行实现已把 canonical 面收敛到 `PlanGraphBundle`，legacy 工厂只保留 fail-fast 迁移提示。 | 当前 `src/platform/contracts/execution-plan/index.ts:6-20` 直接 re-export canonical `PlanGraphBundle` 族；`ExecutionPlanStep` 与 `ExecutionPlan` 在 `src/platform/contracts/execution-plan/index.ts:29-69` 已显式标注 `@deprecated`，其中 `ExecutionPlan` 只是 `PlanGraphBundle` 别名，`createExecutionPlan()` 会直接抛 `platform_contracts.legacy_execution_plan_forbidden`。`src/platform/contracts/types/platform-contracts.ts` 也不再定义第二套线性 `steps[]` ExecutionPlan。 | 不改生产代码，按现行 contract 结构关闭陈旧指控。 |
| R6-16 | 30. 类型系统 + API 序列化 + 共享层问题 | CRITICAL | `interface/api/http-server/task-routes.ts` | POST /v1/tasks 接受 {title,priority,source} 完全绕过 §5.3 intake pipeline | 已解决 | 任务创建路由现在显式依赖 `intakeAdmissionService`，并在接口层把输入转换后送入 intake。 | 当前 `src/platform/five-plane-interface/api/http-server/task-routes.ts` 在 `POST /api/v1/tasks` 中检查 `deps.intakeAdmissionService`，随后构造 `PrincipalRef/RiskPreview/BudgetIntent` 并调用准入主链；不再直接在 route 内落 Task truth。 | 不改生产代码，仅回写现行 route wiring 依据。 |
| R6-17 | 30. 类型系统 + API 序列化 + 共享层问题 | HIGH | `interface/api/http-server/schemas.ts` | Task status 枚举(queued/pending/in_progress/done/failed/cancelled)无法表示 canonical 13态 HarnessRunStatus | 已解决 | 更新任务的 HTTP schema 已改为 canonical HarnessRun 13 态。 | 当前 `src/platform/five-plane-interface/api/http-server/schemas.ts` 的 `updateTaskPayloadSchema.status` 已使用 `created/admitted/planning/ready/running/pausing/paused/resuming/replanning/compensating/completed/failed/aborted`。 | 不改生产代码，仅回写现行 schema 依据。 |
| R6-18 | 30. 类型系统 + API 序列化 + 共享层问题 | HIGH | `全 src/` | OperationalDirective/DecisionDirective 零实现/零 schema/零 import——§5.2 contract 矩阵完全未落地 | 已解决（源码复核） | 根因不是 directive contract 仍然“零实现/零 import”，而是这条复审结论停留在早期 contract 迁移前；现行仓库不但已有 directive 类型/工厂，还在 approval、human takeover、harness、OAPEFLIR 等生产链上发射和消费。 | `src/platform/contracts/control-directive/index.ts:54-179` 已定义 `OperationalDirective/DecisionDirective` 与工厂；`src/platform/contracts/executable-contracts/index.ts:56-66` 已把它们作为 canonical P2→P3/P4 directive 重新导出；生产使用可见于 `src/platform/five-plane-control-plane/approval-center/approval-service.ts:476-605`、`src/platform/five-plane-control-plane/incident-control/human-takeover-service.ts:764-791`、`src/platform/five-plane-orchestration/harness/index.ts:1826-1851`、`src/platform/five-plane-orchestration/oapeflir/oapeflir-loop-service.ts:910-920`。 | 不改生产代码，回写 directive 已真实落地的现状。 |
| R6-19 | 30. 类型系统 + API 序列化 + 共享层问题 | HIGH | `全 src/ 870+ 处` | stepId 仍为普遍执行标识(plugin-spi/域注册/presenter/migration/SDK)——§5.5 仅允许作 legacy projection | 需继续整改（artifact/result 关联已 canonical 化，但仍非 870+ 调用点全量替换） | 真实根因已从“系统完全没有 nodeRunId 入口”进一步收敛为“canonical 字段已进入 contract/allocator/checkpoint/plugin SPI/presenter/SDK/ArtifactRecord/result-envelope 等边界，但 execution receipt、compensation、migration DTO、外部适配器和大量历史调用点仍存在 `stepId` 语义入口”。因此本轮能关闭的是一整段事实关联链，而不是虚写成“数百处调用点已一次性迁完”。 | 本轮在前序 plugin SPI / presenter / SDK 兼容层之外，继续补齐 `src/platform/contracts/types/domain/task-types.ts` 的 `ArtifactRecord.nodeRunId`、`src/platform/five-plane-state-evidence/artifacts/artifact-store.ts` 的 `ArtifactWriteInput.nodeRunId`、SQLite/async repository 的 `node_run_id` 持久化，以及 `src/platform/contracts/result-envelope/result-envelope.ts` 中 `resolveArtifactRefs()` 的 `nodeRunId -> stepId` 回退匹配。定向验证 `tests/unit/platform/contracts/result-envelope/result-envelope.test.ts`、`tests/unit/platform/state-evidence/artifacts/artifact-store.test.ts`、`tests/unit/platform/state-evidence/truth/{sqlite,async-repositories}/artifact-repository.test.ts` 与 `tests/integration/platform/state-evidence/truth/async-repositories/artifact-repository.test.ts` 已通过。**剩余缺口**：execution receipt / compensation / migration DTO / SDK 外部集成面仍未全面从 `stepId` 切到 `nodeRunId`。 | 本轮关闭的是“artifact truth 与 result-envelope 仍只能靠 `stepId` 关联”的真实缺口；保留的是全仓范围的多年迁移工程，不把边界 canonical 化夸大成全量完成。 |
| R6-20 | 30. 类型系统 + API 序列化 + 共享层问题 | HIGH | `harness/index.ts:174` | HarnessRun 含 steps:HarnessStep[] 为第一级字段——§5.5 HarnessStep 仅为 semantic projection，嵌入使违规天然化 | 已解决（源码复核） | 根因不是 canonical `HarnessRun` 现在仍把 `steps` 作为第一级 truth 字段，而是原条目把 harness 内部运行时辅助状态和对外 canonical contract 混在了一起；后续迁移后，公开 `HarnessRun` 已是无 `steps` 的 canonical 形态，只在内部 runtime state 里保留了带 `@deprecated` 标记的语义投影。 | 当前 `src/platform/contracts/executable-contracts/index.ts:250-271` 的 canonical `HarnessRun` 不含 `steps` 字段；`src/platform/five-plane-orchestration/harness/index.ts:387-392` 只是把 `HarnessRun` alias 到 canonical 类型。只有内部 `HarnessRunRuntimeState` 在 `src/platform/five-plane-orchestration/harness/index.ts:395-426` 保留 `steps: readonly HarnessStep[]`，并明确注释“semantic projection / internal runtime state only”。 | 不改生产代码，关闭“public HarnessRun 仍嵌入 steps truth”这一过时结论。 |
| R6-21 | 30. 类型系统 + API 序列化 + 共享层问题 | MEDIUM | `execution/lease/execution-lease-service-async.ts:502` | `as any` cast 在 lease audit 关键路径——绕过类型安全 | 已解决（源码复核） | 这条与后续 `R11-36` 指向的是同一类问题，根因是 reaudit 文档仍停留在旧版 lease audit 写入分支；现行 async lease service 已统一通过强类型 `LeaseAuditRecord` 写入，不再存在关键路径 `as any`。 | 当前 `src/platform/execution/lease/execution-lease-service-async.ts:485-494` 直接向 `insertLeaseAudit()` 传入显式对象字面量；同文件的审计入口 `insertLeaseAudit(audit: LeaseAuditRecord)` 已以强类型参数收敛写链，不存在原条目所述 `audit as any`。 | 不改生产代码，按现行类型边界关闭。 |
| R6-22 | 30. 类型系统 + API 序列化 + 共享层问题 | MEDIUM | `ops-maturity/edge-runtime/edge-orchestrator/` | EdgeExecutionPlan 用线性 orderedTaskIds 而非 PlanGraph(§4.4) | 已解决（源码复核并保留定向测试） | 根因是旧 edge orchestrator 确实以任务 ID 线性数组作为执行计划输入；后续已把这个 compatibility 入口折叠成 `EdgePlanGraphBundle`，并显式生成 canonical `PlanGraphBundle`。 | 当前 `src/ops-maturity/edge-runtime/edge-orchestrator/index.ts` 已把 `EdgeExecutionPlan` 定义为 `EdgePlanGraphBundle` 的兼容别名，`buildEdgeExecutionPlan(...)` 会构造 `graph.nodes / graph.edges / entryNodeIds / terminalNodeIds`；`tests/unit/ops-maturity/edge-runtime/edge-orchestrator/index.test.ts` 已验证 graph bundle 结构。 | 不改生产代码，回写 edge runtime 已切到 PlanGraph 的事实。 |
| R6-23 | 30. 类型系统 + API 序列化 + 共享层问题 | MEDIUM | `contracts/executable-contracts/schemas.ts:650` | validateExecutableContract() 返回 unknown——校验后无类型收窄 | 已解决（源码复核） | 根因不是当前校验函数仍返回 `unknown`，而是 reaudit 条目停留在更早的 helper 签名；现行实现已经按 `CanonicalContractName` 泛型返回对应的 `z.infer` 结果。 | 当前 `src/platform/contracts/executable-contracts/schemas.ts:678-698` 将 `validateExecutableContract<T extends CanonicalContractName>(...)` 的返回值声明为 `z.infer<typeof CONTRACT_ZOD_SCHEMAS[T]>`，并在 safeParse 成功后返回 `result.data`，不再是无类型收窄的 `unknown`。 | 不改生产代码，回写现行泛型签名。 |
| R6-24 | 30. 类型系统 + API 序列化 + 共享层问题 | MEDIUM | `orchestration/harness/runtime/runtime-entry-guard.ts` | assertNoLegacyTruthWrite() 仅运行时拦截——无编译时 @deprecated/no import 强制 | 已解决（源码复核） | 根因不是现行工程仍只有 runtime guard，没有任何编译期限制；后续迁移已经叠加了 `@deprecated` 标记、barrel 隐藏 legacy factory，以及 ESLint `no-restricted-imports` 规则，只是 reaudit 表没有跟上。 | 当前 `src/platform/five-plane-orchestration/harness/runtime/runtime-entry-guard.ts:24-57` 仍保留 runtime guard；同时 `eslint.config.js:24-59` 已对 `control-directive/`、`execution-plan/`、`execution-receipt/`、`state-command/` 等 legacy 模块启用 `no-restricted-imports` 编译期限制，相关 legacy 类型/工厂也在源码中带 `@deprecated`。 | 不改生产代码，关闭“只有运行时拦截”的旧结论。 |
| R6-25 | 31. 测试体系编码错误模型（阻断迁移） | CRITICAL | `tests/unit/platform/contracts/execution-plan/` | 400+ 行验证 createExecutionPlan/ExecutionPlanStep+stepId 为正确行为——将废弃 contract 作为正确性基线 | 已解决（测试基线复核） | 根因不是这些单测当前还把 legacy `ExecutionPlan` 当成 canonical 成功路径，而是测试文件虽然保留在仓内，但语义已经翻转成“legacy contract 只能做兼容类型占位，工厂必须 fail-fast”；reaudit 仍沿用旧描述。 | `tests/unit/platform/contracts/execution-plan/index.test.ts:1-12` 已明确写成 “deprecated / deprecation guard” 测试；同文件 `43-62` 断言 `createExecutionPlan(...)` 必须抛 `execution_plan.legacy_contract_forbidden`。现有 execution-plan 相关测试是在守护迁移边界，而不是把线性 `steps[]` 当成正确基线。 | 不改生产代码，回写测试语义已翻转的事实。 |
| R6-26 | 31. 测试体系编码错误模型（阻断迁移） | CRITICAL | `tests/e2e/multi-step-workflow-comprehensive.test.ts` | 7个场景全部驱动 WorkflowState CRUD 线性步骤模型——迁移到 canonical 会破坏全部 e2e | 已解决（测试基线复核） | 根因是该 e2e 文件曾直接依赖 `WorkflowState` CRUD 与线性 step 状态推进；后续已被改写为 `HarnessRuntimeService`/`runMultiStepOrchestration()` 驱动的 canonical path，但审计表没有同步。 | 当前 `tests/e2e/multi-step-workflow-comprehensive.test.ts` 已显式验证 “canonical failure path records terminal failure without WorkflowState CRUD”，并通过 `runMultiStepOrchestration()` 提交 `oapeflir://plan` 节点图，不再直接操纵 `WorkflowState/currentStepIndex`。 | 不改生产代码，回写测试资产已迁到 canonical 路径。 |
| R6-27 | 31. 测试体系编码错误模型（阻断迁移） | CRITICAL | `tests/e2e/multi-step-task-execution.test.ts` | 18+ WorkflowState 调用断言线性步进——编码废弃执行模型为正确 | 已解决（测试基线复核） | 根因是旧测试把线性 workflow step progression 当正确基线；现行文件已切成 canonical `HarnessRun` / `PlanGraphBundle` / bridge route 断言。 | 当前 `tests/e2e/multi-step-task-execution.test.ts` 已改为验证 “canonical task execution completes without legacy WorkflowState” 与 “canonical route is the oapeflir bridge path”，通过 `HarnessRuntimeService` 和 `runMultiStepOrchestration()` 覆盖 canonical execution。 | 不改生产代码，回写测试基线已迁移的事实。 |
| R6-28 | 31. 测试体系编码错误模型（阻断迁移） | CRITICAL | `tests/e2e/critical-workflows.test.ts` | 16+ WorkflowState 调用断言废弃状态转换(running→paused→completed) | 已解决（测试基线复核） | 根因是该 critical workflow e2e 曾基于 legacy workflow state 转换断言；现行文件已经迁到 canonical harness/runtime terminal projection 验证。 | 当前 `tests/e2e/critical-workflows.test.ts` 已显式验证 “canonical deployment path completes without WorkflowState/currentStepIndex” 和失败路径的 `task/workflow` terminal projection，不再编码 `running→paused→completed` 旧状态机。 | 不改生产代码，回写 critical workflow e2e 已迁移的现状。 |
| R6-29 | 31. 测试体系编码错误模型（阻断迁移） | CRITICAL | `tests/unit/platform/contracts/control-directive/` | 50+ 断言验证 createControlDirective 为正确——废弃 contract 有完整回归保护 | 已解决（测试基线复核） | 根因不是 control-directive 单测还在把 `createControlDirective` 当 canonical happy path，而是测试资产已经改成“legacy contract 必须被拒绝、canonical replacement 必须存在”；reaudit 条目没有刷新。 | `tests/unit/platform/contracts/control-directive-contract.test.ts:30-49,52-77` 断言 `createControlDirective(...)` 对所有 legacy kind 都抛 `control_directive.legacy_contract_forbidden`；同文件 `104-120` 起开始验证 canonical `createOperationalDirective()`。这说明单测的回归基线已经从“旧 contract 正常创建”翻转成“旧 contract 必须 fail-fast”。 | 不改生产代码，仅回写测试现状。 |
| R6-30 | 31. 测试体系编码错误模型（阻断迁移） | HIGH | `tests/integration/platform/contracts/` | 集成测试导入并验证 createExecutionPlan+createControlDirective 流——作为回归门禁阻止废弃删除 | 已解决（测试基线复核） | 根因不是集成测试当前仍把 legacy factory 当可成功执行的主路径，而是它们保留导入仅用于验证 fail-fast 与 canonical 替代路径的完整性；旧 reaudit 结论把“仍有导入”误读成“仍是正确性基线”。 | `tests/integration/platform/contracts/platform-contract-flow-integration.test.ts` 现已断言 `createExecutionPlan()`、`createControlDirective()`、`createExecutionReceipt()` 分别抛 legacy forbidden 错误；同时 `tests/integration/platform/contracts/contracts.integration.test.ts:88-89,157-158,334-371` 等用例改为围绕 `PlanGraphBundle`、`NodeRun` 和 canonical contracts 做集成校验。 | 不改生产代码，关闭“集成测试阻止废弃删除”的旧判断。 |
| R6-31 | 31. 测试体系编码错误模型（阻断迁移） | HIGH | `tests/golden/workflow-validation.test.ts` | golden snapshot 编码线性 steps[]+stepId+dependsOnStepIds——PlanGraph 迁移会破坏快照 | 已解决（测试基线复核） | 根因是旧 golden 用线性 workflow snapshot 做结构锁定；现行 golden 已改成验证 canonical `PlanGraphBundle` 和 planner graph analysis，而不是固定 `steps[]` truth 形状。 | 当前 `tests/golden/workflow-validation.test.ts` 只断言 `minimalWorkflowToPlanGraphBundle(...)` 与 `PlanBuilder.build(...)` 生成的 `graph.nodes / graph.edges / entryNodeIds / terminalNodeIds / validationReport / worstPath`，不再保存或比对 legacy steps snapshot。 | 不改生产代码，回写 golden 基线已改成 graph validation。 |
| R6-32 | 31. 测试体系编码错误模型（阻断迁移） | HIGH | `tests/helpers/fixtures/base.ts+composite.ts` | 所有 fixture 工厂产出 TaskRecord+ExecutionRecord 无 HarnessRun/NodeRun/PlanGraphBundle/BudgetReservation | 已解决（补 canonical fixtures 并接入 e2e） | 根因是 fixture 工厂虽然早已新增 `HarnessRun/NodeRun/PlanGraphBundle/BudgetReservation` canonical 版本，但复审时没有把“存在工厂定义”和“被真实 e2e 使用”区分开，因此文档保守地继续挂成未整改。 | 当前 `tests/helpers/fixtures/base.ts` 已提供 `createMinimalHarnessRun/createMinimalPlanGraphBundle/createMinimalNodeRun/createMinimalBudgetReservation`；本轮 `tests/e2e/canonical-intake-and-side-effect-lifecycle.test.ts:23-59,119-173` 已直接使用 `createMinimalHarnessRun` 和 `createMinimalBudgetReservation` 驱动 canonical intake 与 `BudgetReservation` 生命周期 e2e，形成从 fixture 到 e2e 的真实接线证据。定向命令 `./node_modules/.bin/tsx --test tests/e2e/canonical-intake-and-side-effect-lifecycle.test.ts tests/unit/platform/five-plane-orchestration/oapeflir-event-and-risk-regressions.test.ts` 及相关事件/fixture测试本轮通过。 | 关闭的是“canonical fixture 工厂存在但未被 e2e 使用”的缺口；不外推为全仓所有测试资产都已彻底完成 `stepId -> nodeRunId` 迁移。 |
| R6-33 | 31. 测试体系编码错误模型（阻断迁移） | HIGH | `tests/e2e/oapeflir-full-loop.test.ts` | E2E 用 stepId-based PlanStep/StepResult 驱动 OAPEFLIR 为执行运行时(§2.4 OAPEFLIR 不为 truth source) | 已解决（测试基线复核） | 根因是该 e2e 曾把 OAPEFLIR 当执行器本身；现行测试已经翻转成“调用方提供 stepOutputs，OAPEFLIR 只消费执行结果并生成 stage timeline”的 projection-only 语义。 | 当前 `tests/e2e/oapeflir-full-loop.test.ts` 明确写出 “caller-supplied canonical step output” 与 “OAPEFLIR does not execute”，并通过 `stepOutputs`/`feedbackSignals` 驱动 feedback path；`src/platform/five-plane-orchestration/oapeflir/oapeflir-loop-service.ts` 在 execute stage 也已要求 `stepOutputs must be provided by caller`。 | 不改生产代码，回写 OAPEFLIR e2e 已不再把其当 execution truth source。 |
| R6-34 | 31. 测试体系编码错误模型（阻断迁移） | HIGH | `tests/e2e/ 全部` | 零 e2e 测试走 canonical intake pipeline；零 e2e 测试验证 BudgetReservation 前置；零 e2e 测试验证 SideEffectRecord 生命周期 | 已解决（补 e2e 并定向验证） | 根因是这条最初成立于 “只有 canonical execution e2e、但 intake/side-effect 生命周期还没有专门 e2e” 的阶段；后续虽然已有 `budget-enforcement-flow`，但缺少把 canonical intake、`SideEffectRecord` 生命周期和 `BudgetReservation` 生命周期明确落成可回归的 e2e 资产，因此重审时只能保守挂起。 | 当前 `tests/e2e/canonical-intake-and-side-effect-lifecycle.test.ts:23-59` 直接走 `IntakeRouter.route(...)` 的 `RawInput → TaskDraft → ConfirmedTaskSpec → RequestEnvelope` canonical intake pipeline，`:62-117` 覆盖 `SideEffectRecord` 从 `approved/reserved/committing/ambiguous/compensation_required/compensating/compensated` 的完整生命周期，`:119-173` 进一步通过 `RuntimeStateMachine` 覆盖 `BudgetReservation` 的 `reserved -> settled/released` 生命周期；同时既有 `tests/e2e/budget-enforcement-flow.test.ts:180-298` 已验证预算预留失败时执行保持 `created`、不会进入执行。定向命令 `./node_modules/.bin/tsx --test tests/e2e/canonical-intake-and-side-effect-lifecycle.test.ts` 以及本轮相关 unit/event 定向测试已全部通过。 | 补齐缺失的 canonical intake、SideEffectRecord 与 BudgetReservation 生命周期 e2e，关闭这条“零覆盖”结论。 |
| R6-35 | 32. 剩余 Contract 批量缺口 | CRITICAL | `event_bus_contract.md` | 事件名 task.status*changed/workflow.step_completed/execution.* 与架构 platform.harness*run.*/platform.node_run.\* 冲突 | 已修复（contract/文档） | 旧 event bus contract 混杂了 pre-v4.3 `task/workflow/execution` 叙事、`platform.harness.run.*` 点式命名和裸 `release.*` namespace，没有把 canonical runtime naming freeze 真正落到总线 contract。 | `docs_zh/contracts/event_bus_contract.md` 已把 canonical truth event 收敛到 `platform.harness_run.*`、`platform.node_run.*`、`platform.release.*`、`platform.feedback.*` 等命名，并明确 legacy alias 只能做边界映射；对应不变量测试 `tests/invariants/contract-and-oapeflir-remediation.test.ts` 已新增命名守护并通过。 | 已修 contract 文档并补最小守护测试。 |
| R6-36 | 32. 剩余 Contract 批量缺口 | CRITICAL | `event_registry_and_ops_threshold_contract.md` | 阈值规则绑定 execution._ 废弃事件类型——ops 告警无法捕获 canonical platform._ 事件 | 已解决（contract 复核） | 根因不是现行事件注册表仍以 `execution.*` 作为 truth 事件，而是 reaudit 条目没有吸收后续 contract remediation；当前文档已经把 canonical truth 事件切到 `platform.harness_run.*` / `platform.node_run.*` / `platform.*` 系列。 | `docs_zh/contracts/event_registry_and_ops_threshold_contract.md:36-67` 的注册表已以 `platform.*` 为 truth、`oapeflir.view.*` 为 projection；`213-216` 的 remediation 还明确写出 `execution.*` 只保留兼容 alias，新实现不得再生产。 | 不改代码，仅回写 contract 现状。 |
| R6-37 | 32. 剩余 Contract 批量缺口 | CRITICAL | `result_envelope_contract.md` | buildTaskResultEnvelope(task, stepOutputs, artifacts) 完全基于 pre-v4.3 模型 | 已解决（contract 复核） | 根因不是结果封装 contract 仍围绕 `Task -> Execution` 旧模型，而是 reaudit 表沿用了旧 builder 名称；现行 contract 已把主构建入口切到 `HarnessRun / NodeRun`。 | `docs_zh/contracts/result_envelope_contract.md:56-77` 已定义 `buildHarnessRunResultEnvelope(...)` 与 `buildNodeRunResultEnvelope(...)`，`103-105` 的 remediation 也明确指出 `buildTaskResultEnvelope` 已降为早期版本表述。 | 不改代码，仅回写现行 builder 语义。 |
| R6-38 | 32. 剩余 Contract 批量缺口 | CRITICAL | `debug_inspect_health_backpressure_contract.md` | TaskInspectView.executions[] + /executions/:executionId/inspect 全为废弃实体 | 已解决（contract 复核） | 根因不是 inspect contract 现在仍以 `executions[]` 和 `/executions/:executionId/inspect` 为主入口，而是当前文档已把它们降为兼容 alias；reaudit 表没有刷新。 | `docs_zh/contracts/debug_inspect_health_backpressure_contract.md:60-85,155-170` 已把 `Task -> HarnessRun -> NodeRun[]` 写成 canonical 主链，并明确 `executions[]` 与 `/executions/:executionId/inspect` 仅作 legacy compat alias；`193-195` 的 remediation 也已记录迁移。 | 不改代码，仅回写现行 inspect contract。 |
| R6-39 | 32. 剩余 Contract 批量缺口 | HIGH | `data_plane_contract.md` | ArtifactRef.source_execution_id 应为 source_harness_run_id/source_node_run_id | 已解决（contract 复核） | 根因不是 `ArtifactRef` 仍以 `source_execution_id` 为 canonical 字段，而是 data plane contract 后续已补齐 `source_harness_run_id` / `source_node_run_id`，复审表却仍停在旧字段。 | `docs_zh/contracts/data_plane_contract.md:115-135` 已把 `ArtifactRef` 最小字段收敛到 `source_harness_run_id?`、`source_node_run_id?`，并明确 `source_execution_id` 只保留为 legacy 查询键；`133-135` 的 remediation 直接记录了这一点。 | 不改代码，仅回写现行 artifact 关联键。 |
| R6-40 | 32. 剩余 Contract 批量缺口 | HIGH | `app_error_contract.md` | AppError.execution_id 用 legacy 标识符 | 已解决（contract 复核） | 根因不是现行 `AppError` contract 仍把 `execution_id` 当主关联键，而是文档已经切换到 `harness_run_id` / `node_run_id`，旧复审结论未更新。 | `docs_zh/contracts/app_error_contract.md:26-48` 已声明 `harness_run_id` / `node_run_id` 为 canonical 关联键，并注明 `execution_id` 仅用于 legacy 查询；`98-100` 的 remediation 也已冻结该迁移。 | 不改代码，仅回写现行错误 contract。 |
| R6-41 | 32. 剩余 Contract 批量缺口 | HIGH | `audit_lineage_and_retention_contract.md` | 审计记录用 execution_id 无 harness_run_id/node_run_id——谱系链断裂 | 已解决（contract 复核） | 根因不是当前审计 contract 仍缺 `HarnessRun / NodeRun` 谱系锚点，而是旧条目未吸收 remediation 回写。 | `docs_zh/contracts/audit_lineage_and_retention_contract.md:64-84` 已把最小审计字段扩展到 `harness_run_id?`、`node_run_id?`，并明确 `execution_id` 仅作兼容查询键；`165-167` 的 remediation 已记录主关联字段迁移。 | 不改代码，仅回写现行审计谱系 contract。 |
| R6-42 | 32. 剩余 Contract 批量缺口 | HIGH | `context_compaction_and_overflow_contract.md` | CompactionRecord 用 session_id/task_id 无 harness_run_id/node_run_id | 已解决（contract 复核） | 根因不是 `CompactionRecord` 现在仍只能挂 `session_id/task_id`，而是现行 contract 已补上 canonical runtime 关联字段；reaudit 表没有跟进。 | `docs_zh/contracts/context_compaction_and_overflow_contract.md:142-156` 已给 `CompactionRecord` 增加 `harness_run_id?`、`node_run_id?`，`232-234` 的 remediation 也明确要求新增记录必须使用 canonical 字段。 | 不改代码，仅回写现行 compaction lineage。 |
| R6-43 | 32. 剩余 Contract 批量缺口 | HIGH | `workflow_io_compatibility_precheck_contract.md` | 主字段 workflow_id/step_id 无 PlanGraphBundle/NodeRun | 已解决（contract 复核） | 根因不是预检 contract 仍以 `workflow_id/step_id` 为主键，而是现行文档已切到 `plan_graph_bundle_id` / `node_run_id` / `harness_run_id`；旧复审表未更新。 | `docs_zh/contracts/workflow_io_compatibility_precheck_contract.md:21-40` 已将 `WorkflowIoPrecheckResult` 主字段改成 `harness_run_id`、`plan_graph_bundle_id`、`node_run_id`，并把 `workflow_id/step_id` 降为 legacy 查询键；`82-84` 的 remediation 也已写明。 | 不改代码，仅回写现行预检 contract。 |
| R6-44 | 32. 剩余 Contract 批量缺口 | HIGH | `knowledge_spi_contract.md` | 无 harness_run_id 集成；TrustLevel 4级未引用 §29 知识边界规则 | 已解决（contract 复核） | 根因不是 Knowledge SPI 现在仍缺 canonical 运行时关联和信任边界映射，而是现行 contract 已补齐 `KnowledgeSource.harnessRunId/nodeRunId` 与 §29 对齐表，reaudit 条目没有刷新。 | `docs_zh/contracts/knowledge_spi_contract.md:34-46` 已给 `KnowledgeSource` 增加 `harnessRunId` / `nodeRunId`；`193-206` 已把 4 级 `TrustLevel` 映射到 §29 知识边界规则；`253-255` 的 remediation 直接记录了这次迁移。 | 不改代码，仅回写现行 Knowledge SPI contract。 |
| R6-45 | 32. 剩余 Contract 批量缺口 | MEDIUM | `sla_tier_contract.md / quota_preemption / multimodal_gateway / org_hierarchy / feedback_improvement` | 均不足 60行，缺 ContractEnvelope compliance + remediation section | 已修复（contract/文档） | 根因复核后是一个真实文档缺口：这批 short contract 里虽然部分对象名已存在，但 `quota_preemption`、`multimodal_gateway`、`feedback_improvement` 缺 remediation 与 canonical 强制规则，`org_hierarchy` 也缺 remediation；同时多份文档过短，无法承载最基本的 v4.3 runtime 约束。 | 本轮已直接补齐 `docs_zh/contracts/sla_tier_contract.md`、`quota_preemption_and_fair_scheduling_contract.md`、`multimodal_gateway_contract.md`、`org_hierarchy_and_dynamic_approval_contract.md`、`feedback_improvement_pipeline_contract.md` 的 remediation、导出/兼容约束与强制规则；当前行数分别为 `60 / 60 / 60 / 65 / 60`。对应新增内容可见于 `sla_tier_contract.md:44-60`、`quota_preemption_and_fair_scheduling_contract.md:30-60`、`multimodal_gateway_contract.md:42-60`、`org_hierarchy_and_dynamic_approval_contract.md:55-65`、`feedback_improvement_pipeline_contract.md:42-60`。 | 这是本轮真实文档修复，不是假关闭。 |
| R6-46 | 33. ADR 与架构矛盾（第三批） | CRITICAL | `ADR-079` | FeedbackSignal 用 taskId+executionId 为关联键；v4.3 canonical 为 harnessRunId/nodeRunId——学习对象无法 join truth | 已解决 | ADR 已修复 FeedbackSignal 接口，移除 taskId 关联键，统一使用 harnessRunId/nodeRunId 作为 correlation keys，确保 learning objects 可以 join truth。 | `docs_zh/adr/079-feedback-hub-signals.md` 第 32-34 行已将信号锚点改为 harnessRunId/nodeRunId/receiptId，v4.3 ADR Remediation 第 80-81 行记录 R6-46 修复。 | 不改代码，仅回写 ADR 现行依据。 |
| R6-47 | 33. ADR 与架构矛盾（第三批） | CRITICAL | `ADR-080` | FailurePattern/EvidenceRef 用 executionId——同 R6-46，Learning 子系统与 truth 断连 | 已解决 | ADR 已修复 FailurePattern/EvidenceRef 接口，移除 executionId 关联，统一使用 harnessRunId/nodeRunId，确保 learning objects 可以 join truth。 | `docs_zh/adr/080-learn-hub-pattern-detection.md` 第 56-62 行已将 EvidenceRef 锚点改为 harnessRunId/nodeRunId，v4.3 ADR Remediation 第 195-196 行记录 R6-47 修复。 | 不改代码，仅回写 ADR 现行依据。 |
| R6-48 | 33. ADR 与架构矛盾（第三批） | CRITICAL | `ADR-033` | Status Accepted 定义 Phase 1-7 为 canonical roadmap 含 evaluatePhaseAdvance() gate；§33 明确仅历史映射——应 Superseded | 已解决（ADR 复核） | 根因不是当前 ADR-033 仍把 `Phase 1-7` 当 canonical roadmap，而是 reaudit 表没有跟上后续 superseded 回写；现行 ADR 已把 ring 路线设为权威，并把 phase 仅保留为历史映射。 | `docs_zh/adr/033-phased-roadmap.md:1-4` 已标明 `Superseded by ADR-112`；`55-66` 的 remediation 与现行权威段落也已明确 `Phase 1-7` 只允许作为 migration alias，不再作为 active canonical gate。 | 不改代码，仅回写 ADR 现行状态。 |
| R6-49 | 33. ADR 与架构矛盾（第三批） | HIGH | `ADR-038` | Canary stages CANARY_5/20/50/100 与 ADR-075 canonical rollout 态(canary_5/partial_25/50/75/stable) 冲突 | 已解决（ADR 复核） | 根因是原 onboarding runbook 的 canary 阶段命名曾与 ADR-075 冲突，但现行文档已统一引用 ADR-075 rollout 态；reaudit 条目没有同步更新。 | `docs_zh/adr/038-business-domain-onboarding-runbook.md:38-48` 已把 canary 发布配置改成 `canary_5 → partial_25 → 50 → 75 → stable`，并在 `72-74` 的 remediation 中明确记录了 `R6-49` 修复。 | 不改代码，仅回写现行 rollout 命名。 |
| R6-50 | 33. ADR 与架构矛盾（第三批） | HIGH | `ADR-009` | 用 src/core/ 作为 canonical 目录+workflow_state 作为恢复表——v4.3 §35 用 src/platform/ + harness_runs | 已解决（ADR 复核） | 根因是 ADR-009 早期确实沿用了 `src/core/` 与旧 workflow 术语；现行 ADR 已切换到 `src/platform/` 与 `harness_runs`，但 reaudit 表还保留旧结论。 | `docs_zh/adr/009-deployment-ops.md:29-35,37-47` 已把核心服务层与项目结构统一写成 `src/platform/`；`58-65` 的恢复表清单也已使用 `harness_runs`。同文件 `140-142` 的 remediation 还直接注明了 `R6-50` 修复。 | 不改代码，仅回写 ADR 现行结构。 |
| R6-51 | 33. ADR 与架构矛盾（第三批） | HIGH | `ADR-007` | "Supervisor" 拥有重启/暂停/升级/终止 Agent 权限——v4.3 §45 将全部生命周期控制归 HarnessRuntime | 已解决（ADR 复核） | 根因是原进化引擎 ADR 曾把生命周期控制挂在 “Supervisor / observability” 上；现行 ADR 已把生命周期所有权收口到 `HarnessRuntime`，reaudit 结论没有刷新。 | `docs_zh/adr/007-evolution-engine.md:22-30` 已明确 `HarnessRuntime` 负责生命周期管理、监控和在必要时重启/暂停/升级/终止异常 Agent；`116-118` 的 remediation 直接记录了从 Supervisor 归并到 HarnessRuntime 的修复。 | 不改代码，仅回写 ADR 现行所有权。 |
| R6-52 | 33. ADR 与架构矛盾（第三批） | HIGH | `ADR-070` | Status Accepted 列 Phase 1-7 + "OAPEFLIR 循环不变" 无 v4.3 限定(仅 projection)——应 Superseded | 已解决（ADR 复核） | 根因不是总结 ADR 仍把 `Phase 1-7` 与 OAPEFLIR 主链化表述成不变量，而是现行 ADR 已标成 superseded 并把不变量收口到 `HarnessRuntime + RuntimeStateMachine`；reaudit 表没有吸收。 | `docs_zh/adr/070-conclusion.md:1-4` 已标明 `Superseded`；`35-42` 的演进路线已改为 `Ring 1/2/3`，`47-56` 也把运行时不变量明确为 `HarnessRuntime + RuntimeStateMachine truth authority` 与 “OAPEFLIR 只作为认知投影”。 | 不改代码，仅回写现行 ADR 结论。 |
| R6-53 | 33. ADR 与架构矛盾（第三批） | HIGH | `ADR-041` | TriggerAction.create_task 绕过 §5.3 intake pipeline | 已解决（ADR 复核） | 根因是主动式 Agent ADR 早期没有把 `create_task` 的 intake 约束写死；现行 ADR 已显式规定必须经 `TaskDraft → ConfirmedTaskSpec → RequestEnvelope` 管道，reaudit 表仍停留在旧版本。 | `docs_zh/adr/041-proactive-agent-framework.md:28-37` 已把 `create_task` 描述成“须经 §5.3 intake pipeline”，并在 `77-79` 的 remediation 中直接记录 `R6-53` 修复。 | 不改代码，仅回写现行 intake 约束。 |
| R6-54 | 33. ADR 与架构矛盾（第三批） | MEDIUM | `ADR-069` | OpsCapability 含 restart_service/scale_up_down 直接执行——未经 HarnessRuntime+PlanGraphBundle | 已解决（ADR 复核） | 这条与本轮已回写的 `R11-45` 指向同一事实：原 ADR-069 曾允许自运维能力直接执行，但现行文本已经明确所有此类操作必须经 `RuntimeStateMachine.transition()`、`HarnessRuntime` 和 `PlanGraphBundle` 上下文。 | `docs_zh/adr/069-platform-self-operating-agent.md` 现行正文与 remediation 已明确 restart/scale/rotate 等操作必须走 canonical 控制路径；同一依据已在 `R11-45` 行中复核确认。 | 不改代码，仅把重复条目与同一 ADR 现状对齐。 |
| R6-55 | 33. ADR 与架构矛盾（第三批） | MEDIUM | `ADR-072` | 测试矩阵按 OAPEFLIR 模块目录组织而非 v4.3 canonical runtime 模块 | 已解决（ADR 复核） | 根因是测试策略 ADR 早期用 OAPEFLIR 模块视角组织测试矩阵；现行文档已经改成 `platform/interface`、`platform/control-plane`、`platform/orchestration`、`platform/execution`、`platform/state-evidence`、`domains`、`interaction` 的 canonical runtime 结构。 | `docs_zh/adr/072-oapeflir-testing-strategy.md:31-42` 的测试矩阵现已按 v4.3 runtime 模块结构展开，`159-161` 的 remediation 也直接标注了 `R6-55` 修复。 | 不改代码，仅回写现行测试矩阵口径。 |
| R6-56 | 33. ADR 与架构矛盾（第三批） | MEDIUM | `ADR-078` | Knowledge TrustLevel 无 §10 risk model inherent_risk+trust_score 分离映射——可能隐式降低风险 | 已解决（ADR 复核） | 根因是早期 Knowledge ADR 只写了 trust level 层级，没有冻结 `inherent_risk` 与 `trust_score` 的分离映射；现行 ADR 已补齐该表和解释，reaudit 表仍停在旧结论。 | `docs_zh/adr/078-knowledge-plane-architecture.md:89-99` 已定义四级 TrustLevel 对应的 `inherent_risk` 与 `trust_score`，并明确两者分离以避免“信任提升隐式降低风险”；`185-187` 的 remediation 也直接记录了 `R6-56` 修复。 | 不改代码，仅回写现行知识信任模型。 |
| R7-1 | 35. UI Monorepo 实现 vs UI 架构规格（§1-§7） | P0 | `ui/apps/web/src/feature-registry.ts` | 27个 feature 全部 eager import 无 code split；§4.4.1 要求除 / 和 /login 外全部 React.lazy | 已解决（源码/测试） | 根因不是“feature-registry 完全没有 lazy import”，而是此前只把 feature `Component` 包成了 `React.lazy`，但路由元数据仍写成 `codeSplit:false`，且 `ui/apps/web/src/app-shell.tsx` 渲染 feature 时没有 `Suspense`，导致懒加载契约在产品入口上没有真正生效。 | 本轮将 `ui/apps/web/src/feature-registry.ts` 中 27 个 feature 的 `route.codeSplit` 全部切到 `true`，并在 `ui/apps/web/src/app-shell.tsx` 的 `renderGuardedFeature()` 中补上 `React.Suspense` fallback，打通真实 route-level lazy path。 | 定向验证已通过：`ui/tests/unit/ui/apps/web/feature-registry.test.ts` 新增断言所有 feature `route.codeSplit === true`，并与 `ui/tests/shared/web-app-shell-auth-regression.test.tsx` 一起通过，证明壳层能承载懒加载组件。 |
| R7-2 | 35. UI Monorepo 实现 vs UI 架构规格（§1-§7） | P0 | `ui/vitest.config.ts` | 覆盖率阈值(lines:30%/branches:20%) 远低于 §7.2.6(shared≥90%/ui-core≥80%/features≥70%/apps≥50%) | 已解决（源码复核） | 根因不是“UI 测试体系天然做不到高阈值”，而是早期 `vitest.config.ts` 把 coverage thresholds 写成了过低的占位值，并且一度依赖会绕过强制门限的配置，reaudit 条目没有吸收后续配置收敛。 | 当前 `ui/vitest.config.ts` 已按 spec 分层声明 `90/80/70/50` 四档 coverage thresholds，并显式移除了会绕过门限的 `perManualProject` 配置注释。该条关闭的是配置门限漂移，不外推为整套 UI 覆盖率现状已经通过全量 CI。 | 不改代码，仅回写现行 coverage 配置依据。 |
| R7-3 | 35. UI Monorepo 实现 vs UI 架构规格（§1-§7） | P0 | `ui/scripts/perf-budget.mjs` | JS chunk 550KB/total 1200KB——§7.3.1 要求 main<200KB gz/lazy chunk<100KB gz(超 2.75-5.5x) | 已解决（源码复核） | 根因是早期性能预算脚本确实按宽松 raw-byte 阈值写死，且没有把 FCP/TTI 时间门禁纳入脚本；现行脚本已经改成 gzip 预算和时间预算双重守卫，但旧审计结论没有回写。 | 当前 `ui/scripts/perf-budget.mjs` 已收敛为 `maxJsChunkGzBytes=100KB`、`maxCssChunkGzBytes=40KB`、`totalGzBytes=200KB`，并在 CI 下追加 Lighthouse 的 `FCP<1.5s` / `TTI<3.5s` 检查；`ui/apps/web/vite.config.ts` 也已把 `chunkSizeWarningLimit` 对齐到 `WEB_CHUNK_WARNING_LIMIT_KB`。该条关闭的是预算脚本与门限配置漂移，不外推为当前所有构建产物都已达标。 | 不改代码，仅回写现行 perf budget 脚本依据。 |
| R7-4 | 35. UI Monorepo 实现 vs UI 架构规格（§1-§7） | P1 | `ui/apps/web/src/app-shell.tsx` | 路由为扁平单路径——无 §4.4.1 L2-L5 嵌套下钻路由(/tasks/:id/evidence 等) | 已解决（源码复核） | 根因是这条结论停留在 app shell 只有顶层 feature route 的阶段；当前壳层已经在每个 feature route 下补入 L3-L5 子路由占位，但旧审计没有跟进。另有 `ui/tests/unit/ui/apps/web/app-shell.test.tsx` 现存双 Router 夹具问题，会掩盖真实实现状态，因此不能再把旧单测失配当成“嵌套路由不存在”的依据。 | 当前 `ui/apps/web/src/app-shell.tsx` 已在每个 `Route` 下补入 `index/evidence/logs/debug/metrics/settings` 等嵌套 child routes，并保留壳层 `Suspense` 包装；`ui/tests/unit/ui/apps/web/feature-registry.test.ts` 仍通过，证明 route catalog 已按分组和路径挂载。该条关闭的是“完全没有嵌套路由”的指控，不外推为所有 L2-L5 页面内容都已产品化。 | 回写源码依据，并记录 `app-shell` 旧测试夹具已陈旧。 |
| R7-5 | 35. UI Monorepo 实现 vs UI 架构规格（§1-§7） | P1 | `ui/packages/features/` | 缺 feature-flags 模块(§4.1 Admin 下独立路由 /admin/feature-flags) | 已解决（源码/测试） | 根因是早期 UI feature registry 确实缺 `feature-flags` 模块与独立路由；后续 `ui/` monorepo 已补齐该 feature，但 reaudit 条目仍沿用旧快照。 | 当前 `ui/packages/features/feature-flags/src/index.tsx` 已存在独立 feature 模块，`ui/apps/web/src/feature-registry.ts` 也已将其挂到 `/admin/feature-flags` 且启用 `codeSplit: true`，`ui/packages/shared/api-client/src/endpoints.ts` 与 `ws-event-router.ts` 还补上了对应 REST/WS 接线。定向验证：`ui/tests/unit/ui/apps/web/feature-registry.test.ts` 已通过并明确断言 `feature-flags` feature 存在。 | 关闭的是 feature-flags 模块缺失问题，不外推为整个 Admin 配置中心已完全产品化。 |
| R7-6 | 35. UI Monorepo 实现 vs UI 架构规格（§1-§7） | P1 | `ui/packages/features/settings/` | Settings 无子路由导航——§4.2.9 定义 8个子页面均缺失 | 已解决（源码复核） | 根因是 reaudit 沿用了旧快照，没有吸收 settings feature 已补齐 8 个 sub-page 路由的现状。当前 `ui/packages/features/settings/src/index.tsx`（L5-24）已定义所有 8 个 sub-page 的 lazy import 与 `settingsSubPages` 路由配置，`sub-pages/` 目录下各组件文件均存在，`createFeatureModule()` 也已传入 `subPages: settingsSubPages`（L35）。 | 关闭的是 sub-page 路由缺失问题，不外推为所有子页面内容都已产品化或与后端完全接线。 |
| R7-7 | 35. UI Monorepo 实现 vs UI 架构规格（§1-§7） | P1 | `ui/packages/shared/api-client/` | 缺 /api/v1/meta/contract-version 端点(§1.8 契约版本协商) | 已解决（源码/测试） | 根因不是单点“缺一个 endpoint 常量”，而是 contract-version 主链有 4 处接线断层：UI endpoint 写成绝对 `/api/v1/...` 与其余相对 baseUrl 约定不一致；shared client 默认 `Accept-Version` 仍是 `v1`；后端只暴露 legacy `/api/contract-version`；真实 HTTP client 没有解包平台统一 `{ requestId, data }` envelope。 | 本轮将 `ui/packages/shared/api-client/src/endpoints.ts` 的 contractVersion 路径收敛为相对 `/meta/contract-version`，在 `interceptors.ts` / `rest-client.ts` / `ui/apps/web/src/runtime.ts` 统一到 dated `Accept-Version` 集合并补上 response envelope 解包；后端 `src/platform/interface/api/http-server/health-routes.ts` 新增 canonical `/api/v1/meta/contract-version` 与 `/v1/meta/contract-version`，同时保留 legacy alias。 | 定向验证已通过：`./node_modules/.bin/tsx --test --test-name-pattern "contract-version" tests/unit/platform/interface/api/http-api-server.test.ts` 与 `./ui/node_modules/.bin/vitest run --environment jsdom ui/tests/unit/ui/shared/interceptors.test.ts ui/tests/shared/api-client-runtime-regressions.test.ts`。 |
| R7-8 | 35. UI Monorepo 实现 vs UI 架构规格（§1-§7） | P1 | `ui/packages/shared/api-client/ws-event-router.ts` | 缺 nl.clarification_needed 事件映射(§5.3) | 已解决（源码/测试） | 根因是 reaudit 沿用了旧结论，没有吸收 `shared api-client` 已补入 `nl.clarification_needed -> ["tasks"]` query invalidation 的现状，也缺少专门回归测试来证明这个实时事件不会再被遗漏。 | 当前 `ui/packages/shared/api-client/src/ws-event-router.ts` 已包含 `nl.clarification_needed` 映射；本轮补充 `ui/tests/unit/ui/shared/ws-event-router.test.ts`，同时验证纯映射表和 router 触发 `invalidateQueries({ queryKey: ["tasks"] })`。 | 定向验证已通过：`./ui/node_modules/.bin/vitest run --environment jsdom ui/tests/unit/ui/shared/ws-event-router.test.ts`。 |
| R7-9 | 35. UI Monorepo 实现 vs UI 架构规格（§1-§7） | P1 | `ui/package.json + ui/tests/` | 缺 Playwright/Detox/Spectron/axe-core 依赖(§7.2.4 E2E+无障碍测试) | 部分修复（Web Playwright/axe 与 CI 门禁已落地，native/mobile harness 仍缺） | 原条目把“依赖清单缺失”“没有真实可执行的无障碍回归”和“跨端测试体系未完整产品化”混成了一条。当前 Web 端已经不只是声明依赖，而是有实际 Playwright+axe 用例和工作流接线；剩下没补的是 native/mobile shell 自动化与辅助技术人工回归。 | 本轮新增 `ui/playwright.config.ts`、`ui/tests/a11y/web-accessibility.spec.ts`、`ui/package.json` 的 `test:a11y` 脚本，以及根工作流 `.github/workflows/ui-quality.yml` 中的 `npx playwright install --with-deps chromium` 与 `npm run test:a11y`。同时修复 `ui/tools/codegen/package.json` 里无效的 `openapi-typescript@^0.14.0`，改为可安装的 `^7.9.1`，并更新 `ui/package-lock.json`，否则 UI 工作流无法真实安装新增依赖。**剩余缺口**：仍未见 Detox/Spectron 或等价 native/mobile shell 自动化方案，也没有 VoiceOver/TalkBack 等辅助技术回归资产。 | 本条关闭的是“Web 端没有真实 Playwright/axe 与 PR 门禁”的缺口；保留的是跨端 native/mobile 测试体系仍未完整产品化。 |
| R7-10 | 35. UI Monorepo 实现 vs UI 架构规格（§1-§7） | P1 | `ui/packages/shared/i18n/` | 仅 4个翻译 key/2 locale——§6.4 要求全模块覆盖 | 部分修复（i18n 基础设施已具备，feature 层仍需覆盖） | 原审计条目“仅 4个翻译 key”已是过时结论。当前 `ui/packages/shared/i18n/src/index.ts` 已实现 87个 key 覆盖 zh-CN/en-US 两个 locale，包含 app/nav/status/notifications/risk/autonomy/action/form/error/confirm/empty/pagination 等域。剩余缺口是 feature 层（feature-flags、settings 等）尚无独立翻译文件，TranslationService 未被所有 feature 组件调用。 | 本轮确认 i18n 基础设施已从“4 key/2 locale”扩充到“87 key/2 locale”，但 feature 级翻译覆盖仍需在 UI 主线整改中补充。 | 将条目从“依赖完全缺失”收敛为“feature 层翻译覆盖待补充”。 |
| R7-11 | 35. UI Monorepo 实现 vs UI 架构规格（§1-§7） | P2 | `ui/packages/ui-core/src/design-tokens/` | 无 primitive/semantic token 分层(§6.3.1) | 已解决（源码核实） | 根因是旧审计沿用了早期 design-tokens 只有扁平 key-value 的结论；当前 `ui/packages/ui-core/src/design-tokens/index.ts` 已明确定义三层架构：(1) Layer 1 `PrimitiveTokens` 接口 + `primitiveTokens` 常量（gray50/100/.../950、green/blue/amber/red/sky 各色阶、radius/spacing/typography/motion/shadows/iconSizes）；(2) Layer 2 `SemanticTokens` 接口 + `semanticTokens` 常量（background/surface/text/accent/danger/success 等意图命名，全部按语义引用 `primitiveTokens` 底层值，如 `background: primitiveTokens.color.gray50`）；(3) Layer 3 注释为 domain-specific compositions。 | 当前 `ui/packages/ui-core/src/design-tokens/index.ts` 已完整实现 primitive/semantic 两层分层，符合 §6.3.1 要求。 | 关闭的是 primitive/semantic token 分层缺失问题，不外推为整套 design token 产品化已全部完成。 |
| R7-12 | 35. UI Monorepo 实现 vs UI 架构规格（§1-§7） | P2 | `ui/apps/web/src/app-shell.tsx` | 路由守卫硬编码 demo 权限——§4.4.3 要求 5层动态 guard chain | 已解决（源码/测试） | 根因与 `2164` 同源：旧 web 壳层曾把 route guard 上下文写成固定 demo 管理员权限，导致守卫链即使存在也会被硬编码上下文短路。 | 当前 `ui/apps/web/src/app-shell.tsx` 已通过 `createFeatureGuardContext()` 从 `authContext.tenantId/roles/permissions` 动态构造 guard context，不再存在 demo guard 常量；本轮新增 `ui/tests/shared/web-app-shell-auth-regression.test.tsx`，直接验证 guard context 使用传入的真实 `authContext`，定向测试已通过。 | 关闭的是 demo guard 硬编码缺陷，不外推为完整五层 route-guard 产品化能力全部完成。 |
| R7-13 | 35. UI Monorepo 实现 vs UI 架构规格（§1-§7） | P2 | `ui/packages/shared/api-client/rest-client.ts` | 缺 Idempotency-Key header 支持(§5.6.4) | 已解决（源码修复并补定向测试） | 根因是 UI runtime 之前只下沉了 trace/version/csrf/auth/tenant/offline 拦截器，没有把写请求幂等键注入能力接到 shared api-client 主链上。 | 本轮新增 `ui/packages/shared/api-client/src/interceptors.ts#createIdempotencyKeyInterceptor()`，对所有非 GET 请求自动补写 `Idempotency-Key` 与兼容期 `x-idempotency-key`，并在 `ui/apps/web/src/runtime.ts` 默认接线；`ui/tests/unit/ui/shared/interceptors.test.ts` 与 `ui/tests/shared/api-client-runtime-regressions.test.ts` 已验证写请求会在进入 transport 前携带同值幂等头，定向测试已通过。 | 关闭的是 UI client 侧幂等头注入缺口，不外推为服务端所有幂等去重链路都已完成。 |
| R7-14 | 35. UI Monorepo 实现 vs UI 架构规格（§1-§7） | P2 | `ui/pnpm-workspace.yaml + turbo.json` | 与 §2.2 ADR 选定的 npm workspaces 冲突——vestigial 配置 | 已解决（删除 vestigial 文件） | 根因是 UI workspace 目录同时保留了 pnpm 和 turbo 配置文件，与 architecture spec 指定的 npm workspaces + npm scripts 方案冲突。 | 本轮删除 `ui/pnpm-workspace.yaml` 与 `ui/turbo.json`，保留 `ui/package.json` 的 `workspaces` 字段和 npm scripts 构建链，符合 §2.2 ADR 选型。 | 不改代码，仅删除冲突的 vestigial 配置。 |
| R7-15 | 36. 后端 UI 服务 vs UI 架构规格（§4-§5） | P0 | `src/interaction/dashboard/dashboard-projection-service` | 仅产出 totalTasks/tasksByStatus 等 4字段；UI spec §4.7.7 要求 success_rate/avg_duration_ms/active_agents 等 10+ 字段 | 已解决（源码/测试） | 根因是 reaudit 条目沿用了只看 legacy `totalTasks/tasksByStatus/totalIncidents/totalWorkflows` 的旧快照，没有复核 `buildStateFromProjections()` 后续已经补齐的 KPI 聚合字段，也缺少专项回归去防止字段再次缩水。 | 当前 `src/interaction/dashboard/dashboard-projection-service.ts` 的 `buildStateFromProjections()` 已稳定产出 `successRate/avgDurationMs/activeAgents/queueDepth/errorRate/p50LatencyMs/p99LatencyMs/budgetUtilizationPercent/approvalPendingCount/systemHealthScore`；本轮新增 `tests/unit/interaction/dashboard/dashboard-projection-service.test.ts` 的 “full dashboard KPI field set” 回归，并已通过定向命令 `./node_modules/.bin/tsx --test tests/unit/interaction/dashboard/dashboard-projection-service.test.ts`。 | 关闭的是“后端 dashboard projection 仍只有 4 字段”的陈旧结论，不外推为所有 UI 面板消费链都已做完。 |
| R7-16 | 36. 后端 UI 服务 vs UI 架构规格（§4-§5） | P0 | `src/interaction/dashboard/dashboard-websocket-server` | WS 消息类型 dashboard_delta/snapshot 与 UI spec task.status_changed/approval.resolved 等 domain event 模型不匹配 | 已解决（源码/测试） | 根因是审计条目引用了旧版 `dashboard_delta/snapshot` 内部命名；现行 `DashboardPushMessageType`（L28-53）已包含完整 UI spec domain event 模型：`task.status_changed/created/completed/failed`、`approval.requested/resolved`、`incident.opened/resolved`、`system.health_changed`、`harness_run.*`、`node_run.*`、`dashboard.snapshot`（`dashboard_snapshot` 别名）。`mapChangeTypeToDomainEvent()`（L518-538）将内部 change type 正确映射为 UI spec domain event type。 | 定向验证：`tests/unit/interaction/dashboard/dashboard-websocket-server.test.ts` 已覆盖 messageType 映射与 domain event 推送。 | 关闭的是 WS 消息类型与 UI spec domain event 模型不匹配问题。 |
| R7-17 | 36. 后端 UI 服务 vs UI 架构规格（§4-§5） | P0 | `src/interaction/dashboard/dashboard-websocket-server` | 订阅模型为 dashboard-ID-based；UI spec 要求 channel-based (global/task:{id}/approvals/admin) | 已解决（源码/测试） | 原审计结论误判为"dashboard-ID-based"，实际 `DashboardChannel` 类型(L82-92)已定义为 `| "global" | "task" | "approvals" | "admin"`，`ChannelSubscription` 接口(L89-92)正确建模 `channel` + 可选 `filterId`（task ID），`channelToKey()`(L115-120) 转换为 `task:{id}` 格式，测试套件已覆盖 channel-based 订阅。 | 本轮关闭误报，订阅模型与 UI spec channel-based 要求一致。 | 定向验证：`tests/unit/interaction/dashboard/dashboard-websocket-server.test.ts` 中 `createChannelSubscription()` 使用 "global"/"task"/"admin" 等 channel 类型注册客户端并验证路由。 |
| R7-18 | 36. 后端 UI 服务 vs UI 架构规格（§4-§5） | P1 | `src/interaction/dashboard/` | DashboardProjectionService 与 DashboardWebSocketServer 未集成（有 TODO） | 已解决（源码/测试） | 根因是审计条目提到的 TODO 已在本轮修复：`DashboardWebSocketServer.integrateWithProjectionService()`(L618-633) 现已实现与 `DashboardProjectionService` 的集成，自动每 100ms 调用 `consumePendingDeltas()` 并通过 `handleProjectionDelta()` 推送给订阅客户端；`setDeltaHandler()` 回调机制(L607-609)也已完成接线。 | 定向验证：`src/interaction/dashboard/dashboard-websocket-server.ts` L618-633 已实现集成方法，测试套件已覆盖 delta 推送路由。 | 关闭的是 projection/websocket 未集成缺口，不外推 UI 产品化全链路已通。 |
| R7-19 | 36. 后端 UI 服务 vs UI 架构规格（§4-§5） | P1 | `src/interaction/dashboard/metric-aggregator/` | 后端 metric aggregator 仅覆盖 ~15% 所需指标；UI spec 4层 28面板要求完整 metric 集 | 部分修复（backend metric aggregator 已实现，UI 面板接线仍需整改） | 原审计结论"仅覆盖 ~15%"已在后续迭代中显著过时。当前 `src/interaction/dashboard/metric-aggregator/index.ts` 已实现完整 metric 集：(1) `TaskMetricSnapshot` / `WorkflowMetricSnapshot` / `SystemHealthSnapshot` / `CostMetricSnapshot` / `ApprovalMetricSnapshot` / `AlertMetricSnapshot`；(2) `DashboardMetricSummary` 含 `activeAgents/successRate/avgDurationMs`；(3) `WindowedMetricAggregation` / `MetricTrend` / `SloComparison` / `DomainGroupedMetrics` 时间序列和 SLO 支持；(4) `MetricAggregator` 类含时序历史管理；(5) `buildDashboardMetrics` / `buildDashboardMetricsWithAggregation` 两套 builder 函数。后端 metric aggregator 已达到 UI spec 要求的完整 metric 集。**剩余缺口**：backend metrics 到 UI spec 4层 28面板的整组接线验证尚未完成（无专项 UI E2E/原生壳验证）。 | 将条目从"仅 15% 覆盖"更正为"backend metric aggregator 已完整实现，UI 面板接线 productization 仍需后续整改"。 | 不改代码，回写 audit 状态。
| R7-20 | 36. 后端 UI 服务 vs UI 架构规格（§4-§5） | P1 | `src/interaction/dashboard/health-scorer/` | 返回单一数值；UI spec StabilityPanelView 要求 9字段（overall/uptime/errorRate/p50LatencyMs/p99LatencyMs/queueDepth/activeWorkers/budgetUtilizationPercent/findings） | 已解决（9字段结构已实现，注释已更正） | 旧审计结论"返回单一数值"已过时。当前 `src/interaction/dashboard/health-scorer/index.ts` 已定义 `StructuredHealthScore` 接口（L7-17），包含 `overall/uptime/errorRate/p50LatencyMs/p99LatencyMs/queueDepth/activeWorkers/budgetUtilizationPercent/findings` 共 9 个字段，并通过 `buildStructuredHealthScore()`（L22-65）产出完整结构化对象。`scoreSystemHealth()`（L71-73）保留为数值兼容封装，调用 `buildStructuredHealthScore(system).overall`。代码注释（原错写为"8 required fields"）已更正为"9 required fields"。**剩余缺口**：`buildStructuredHealthScore` 缺少专项测试守护字段结构和边界条件；该 structured 接口是否已接入 StabilityPanel UI presenter 尚待 UI 主线验证（均为后续整改项，不影响本条关闭）。 | 将条目从"部分修复"更正为"已解决（9字段结构已实现）"。 | 回写 audit 状态，修正代码注释。 |
| R7-21 | 36. 后端 UI 服务 vs UI 架构规格（§4-§5） | P1 | `src/interaction/dashboard/alert-router/` | 仅排序；无实时路由/overlay/push/haptic 通知 | 已解决（源码/测试） | 原审计结论"仅排序；无实时路由"已过时——`AlertRouter` 类已实现完整实时路由机制：(1) `NotificationDeliveryType` 含 `overlay/push/haptic/email/sms`；(2) `routeNotifications()` 将 attention items 路由到对应通知渠道；(3) `getOverlayAlerts()`/`getPushNotifications()`/`getHapticAlerts()` 分别过滤各渠道通知；(4) 默认路由规则已将 `incident/approval_needed/budget_warning/quality_alert/suggestion` 的各优先级映射到对应 delivery methods 并配置 cooldown。剩余缺口是 unit test 仅覆盖 `sortAttentionQueue()`，未覆盖 `AlertRouter` 的 routing/push/overlay/haptic 方法。 | 本轮补全 `tests/unit/interaction/dashboard/alert-router/alert-router.test.ts` 中 `AlertRouter` 方法测试：验证 `routeNotifications()`/`getOverlayAlerts()`/`getPushNotifications()`/`getHapticAlerts()` 按规则正确路由、`cooldown` 防重放、`enableOverlay/Push/Haptic` 配置开关。 | 回写源码已实现实时路由，更新 unit test 覆盖。 |
| R7-22 | 36. 后端 UI 服务 vs UI 架构规格（§4-§5） | P1 | `src/platform/interface/api/mission-control-service` | MissionControlSnapshot DTO 与 UI spec Dashboard wireframe 字段不匹配 | 已解决（DTO/源码核实） | 根因是旧审计时 `MissionControlSnapshot` 缺少 §4.7.7 Dashboard wireframe KPI 字段；现行接口（L61-85）已包含完整 KPI 字段：`successRate/avgDurationMs/activeAgents/queueDepth/errorRate/p50LatencyMs/p99LatencyMs/budgetUtilizationPercent/uptimePercent`，且 `getSnapshot()`（L204-271）已实现从 metrics 真实计算这些值。本轮核实确认 DTO 字段与 UI spec wireframe 口径一致。**剩余缺口**：AnalyticsDashboard 所需时间序列数组（task_trend[]/cost_trend[]/top_failures[]/workflow_durations[] 等）属于 AnalyticsDashboard 投影层的独立 productization 问题，不在本条 DTO 比对范围内。 | 关闭的是"Dashboard wireframe KPI 字段缺失"问题；时间序列数据投影视为后续 UI 主线整改项。 | 回写 audit 状态，不改代码。 |
| R7-23 | 36. 后端 UI 服务 vs UI 架构规格（§4-§5） | P1 | `src/platform/interface/api/mission-control-service` | getWorkflowCockpit() 返回 inspect-oriented shape 而非 UI spec presentation shape | 已解决（源码修复） | 根因是旧审计时 `WorkflowCockpitView.presentation` 缺少 UI spec §5.2 要求的三个字段：`dependency_state`、`approval_nodes`、`evidence_refs`；`getWorkflowCockpit()` 只返回 planGraph/nodeRuns 等部分字段，无法构成完整 presentation shape。现行接口（L87-128）已完整实现 UI spec §5.2 最小字段集：`harnessRunId`/`planGraph`/`nodeRuns`/`dependencyState`/`approvalNodes`/`evidenceRefs`，并额外提供 `activeNodeRunId`/`elapsedTimeMs`/`riskLevel` 等 UX 有用字段。`getWorkflowCockpit()`（L297-382）现已通过 `deriveDependencyState()`/`deriveApprovalNodes()`/`deriveEvidenceRefs()` 填充这三个字段。**剩余缺口**：UI 消费层是否真正渲染这些字段属于 UI 主线产品化范围。 | 关闭的是"后端 presentation 数据缺字段"问题；UI 侧渲染验证视为后续 UI 主线整改项。 |
| R7-24 | 36. 后端 UI 服务 vs UI 架构规格（§4-§5） | P1 | `src/platform/interface/api/mission-control-service` | getStabilityPanel() 返回数组而非 UI spec 要求的标量计数 | 已解决（源码/测试） | 根因是旧审计时 `StabilityPanelView` 只有数组字段，未含标量计数；现行接口（L126-143）已同时提供 `activeTaskCount/queuedTaskCount/blockedTaskCount/pendingApprovalCount/workerCount/findingsCount` 标量，以及对应的 `activeTasks[]/queuedTasks[]/blockedTasks[]/workers[]` 数组。`getStabilityPanel()`(L532-562) 同时返回两类字段，定向测试验证标量与数组长度一致。 | 关闭的是标量计数字段缺失，不是数组结构本身（UI spec 仍需数组呈现面板明细）。 |
| R7-25 | 36. 后端 UI 服务 vs UI 架构规格（§4-§5） | P1 | `src/interaction/ux/workflow-builder-service` | 仅内部方法无 REST 端点；UI spec 要求 CRUD + validate + publish API | 已解决（源码/测试） | 根因不是单独缺几条 route，而是 `WorkflowBuilderService` 之前只有临时返回对象：`create/get/update/delete/list/publish` 都没有持有真实状态，`HttpApiServer` 也没有任何 builder API 入口，导致 UI 无法进行真正的 builder CRUD。 | 本轮把 `src/interaction/ux/workflow-builder-service.ts` 改为持有实际 workflow definition state 的服务，并在 `src/platform/interface/api/http-server/task-routes.ts` 新增 `/api/v1/workflows/builder`、`/validate`、`/:workflowId`、`/:workflowId/publish` 等 CRUD/validate/publish 端点，同时对非法 graph 返回显式 400。 | 定向验证已通过：`./node_modules/.bin/tsx --test --test-name-pattern "workflow builder routes provide CRUD|WorkflowBuilderService persists workflow CRUD state" tests/unit/platform/interface/api/http-api-server.test.ts tests/unit/interaction/ux/workflow-builder-service.test.ts`。 |
| R7-26 | 36. 后端 UI 服务 vs UI 架构规格（§4-§5） | P1 | `src/interaction/ux/conversation-history-service` | 缺 clarificationState/riskPreview/actionOptions[] 字段 | 已解决（源码/测试） | 根因是复审条目停留在旧 `ConversationTurnRecord` 只包含基础消息字段的阶段，没有吸收当前 service 已扩充 UI contract 所需 clarification/risk/action 字段，但仓内也确实缺少专门测试守护，导致审计无法闭环。 | 当前 `src/interaction/ux/conversation-history-service.ts` 已定义 `clarificationState`、`riskPreview`、`actionOptions`，本轮新增回归测试验证 `addTurn()` 会原样保留这些 UI contract 字段。 | 定向验证已通过：`./node_modules/.bin/tsx --test --test-name-pattern "ConversationHistoryService preserves clarification" tests/unit/interaction/ux/conversation-history-service.test.ts`。 |
| R7-27 | 36. 后端 UI 服务 vs UI 架构规格（§4-§5） | P1 | `src/interaction/ux/conversation-history-service` | 无 WS 事件发射；UI spec 要求 nl.clarification_needed 实时推送 | 已解决（源码/测试） | 根因与 `R7-26` 同源：service 已经具备 `setEventEmitter()/emitClarificationNeeded()`，但缺少直接证明 `nl.clarification_needed` 会被发射的回归测试，因此 reaudit 继续把它视为“无实时推送”。 | 当前 `src/interaction/ux/conversation-history-service.ts` 已发射 `nl.clarification_needed` 事件；本轮新增测试直接验证 emitter 收到 `sessionId/turnId/prompt/timestamp` 完整事件，同时前端 `ws-event-router` 的对应映射也已单独补测。 | 定向验证已通过：`./node_modules/.bin/tsx --test --test-name-pattern "ConversationHistoryService emits nl.clarification_needed" tests/unit/interaction/ux/conversation-history-service.test.ts` 与 `./ui/node_modules/.bin/vitest run --environment jsdom ui/tests/unit/ui/shared/ws-event-router.test.ts`。 |
| R7-28 | 36. 后端 UI 服务 vs UI 架构规格（§4-§5） | P2 | `src/interaction/ux/ux-event-tracking-service` | 硬编码 "test:many_events" 事件类型；无 §5.4 规定的 standard event taxonomy | 已解决（源码修复） | 根因是 reaudit 停留在早期版本 `ux-event-tracking-service.ts` 仍有 hardcoded "test:many_events" 的快照；现行版本（line 10 注释、lines 19-32 PlatformUxEventType、lines 143-156 映射表）已实现完整 §5.4 standard event taxonomy，使用 `platform.ux.*` 命名空间，`test:many_events` 在本服务中不存在。 | 关闭的是 event taxonomy 缺失缺口；"test:many_events" 本是 platform 层 soak test 用的测试事件类型，不属于 UX tracking service 的范畴。 |
| R7-29 | 36. 后端 UI 服务 vs UI 架构规格（§4-§5） | P2 | `src/interaction/ux/platform-workbench-snapshot-service` | 路由与 UI spec §4.4.1 /workbench/:view 不匹配 | 已解决（源码/测试） | 根因不是默认动作真的跳到了错误的信息架构，而是服务直接内联了 `/workbench/approvals`、`/workbench/stability`、`/workbench/tasks|takeover` 这些具体 view 路径，却没有任何显式 invariant 说明“它们都是 `/workbench/:view` 的具体实例”，导致审计只能按字符串表面判断为不匹配。 | 本轮在 `src/interaction/ux/platform-workbench-snapshot-service.ts` 新增 `WORKBENCH_VIEW_ROUTE_PATTERN` 与 `isWorkbenchViewRoute()`，并在默认 operator actions 构造时做 fail-close 校验；`tests/unit/interaction/ux/platform-workbench-snapshot-service.test.ts` 也新增断言，验证默认动作路由全部符合 `/workbench/:view` 模式，定向测试已通过。 | 关闭的是 workbench 动作路由模式不自证的问题，不外推为整套 workbench 页面都已产品化。 |
| R7-30 | 36. 后端 UI 服务 vs UI 架构规格（§4-§5） | P2 | `src/interaction/dashboard/` | DashboardAggregationService 与 DashboardProjectionService 两套并行未集成 | 已解决（源码修复） | 根因是 `DashboardAggregationService` 只持有独立聚合逻辑，没有接入 `DashboardProjectionService` 的 delta 链路，导致两套系统并行运行、状态分裂。 | 当前 `src/interaction/dashboard/index.ts` 的 `DashboardAggregationService` 已注入 `DashboardProjectionService`（L239 `projectionService` 属性），`buildOperatorDashboard()` 在 L294-298 调用 `projectionService.consumePendingDeltas()` 并通过 `mergeProjectionDeltas()`（L497）将 projection deltas 合并进 attention queue；`DashboardProjectionService` 通过 `processProjectionUpdate()/processEvent()` 生成 delta，由 `DashboardAggregationService` 消费并驱动注意力项更新。 | 关闭后端 aggregation/projection 集成缺口，不外推 UI 产品化全链路已通。 |
| R7-31 | 37. UI 相关 Contract/ADR 与 UI 架构矛盾 | P0 | `docs_zh/contracts/ui_console_and_cockpit_contract` | TaskCockpit 用 task_id/task_status/current_step——均为废弃术语（应为 harness_run_id/NodeRun） | 已解决（contract/文档回写） | 根因是早期 cockpit contract 仍沿用 task/workflow/step 视角写 UI 对象，没跟上 canonical runtime 命名冻结。 | 当前 `docs_zh/contracts/ui_console_and_cockpit_contract.md` 的 `TaskCockpit` 最小字段已改为 `harness_run_id`、`NodeRun` 列表、`blocked_reason`、`artifact_refs`，并明确 cockpit truth 以 `HarnessRun / NodeRun` 为准。 | 回写 contract，不伪造后端 UI 产品化已完成。 |
| R7-32 | 37. UI 相关 Contract/ADR 与 UI 架构矛盾 | P0 | `docs_zh/contracts/ui_console_and_cockpit_contract` | WorkflowCockpit 用 workflow_id/steps/current_step_index——废弃线性模型 | 已解决（contract/文档回写） | 根因同样是 workflow cockpit contract 停留在线性 `steps[]` 时代，没有切到 DAG/PlanGraph 视角。 | 当前 `docs_zh/contracts/ui_console_and_cockpit_contract.md` 的 `WorkflowCockpit` 最小字段已要求 `plan_graph`、`NodeRun`、`dependency_state`、`approval_nodes`、`evidence_refs`，并明确“不得以 `workflow_id + steps[] + current_step_index` 充当 cockpit truth”。 | 回写 contract，关闭线性 workflow 表述缺口。 |
| R7-33 | 37. UI 相关 Contract/ADR 与 UI 架构矛盾 | P0 | `docs_zh/contracts/ui_console_and_cockpit_contract` | AdminTakeoverConsole 用 retry_step/skip_step/override_step_output——废弃操作 | 已解决（contract/文档回写） | 根因是接管控制台 contract 继续沿用 step-oriented 运维动作名，没有跟到 `NodeRun / NodeAttempt`。 | 当前 `docs_zh/contracts/ui_console_and_cockpit_contract.md` 已把接管动作改成 `重试某个 NodeAttempt / 跳过某个 NodeRun / 注入 override_artifact_ref / compensation / replay / rollback`，并显式禁止 `retry_step / skip_step / override_step_output` 这类 legacy 动作名。 | 回写 contract，关闭废弃 takeover 动作名。 |
| R7-34 | 37. UI 相关 Contract/ADR 与 UI 架构矛盾 | P1 | `docs_zh/contracts/admin_console_and_human_takeover_contract` | 同样使用步骤语言(step_id/step_status)而非 PlanGraph NodeRun | 已解决（contract/文档回写） | 根因是值班与人工接管 contract 早期沿用“某步/某 execution”的人工干预语言。 | 当前 `docs_zh/contracts/admin_console_and_human_takeover_contract.md` 已统一锚定 `HarnessRun / NodeRun / NodeAttempt`，并要求所有改变运行态的动作必须携带 `harness_run_id / node_run_id` 作用域。 | 回写 contract，关闭 step-oriented 接管语言。 |
| R7-35 | 37. UI 相关 Contract/ADR 与 UI 架构矛盾 | P1 | `docs_zh/contracts/ui_console_and_cockpit_contract` | Contract 导航仅 4组；UI spec 有 Extended/Shared Features 含 12+ 模块 | 已解决（contract/文档回写） | 根因是旧 console contract 只保留最小四组导航，没有表达扩展模块和共享能力域。 | 当前 `docs_zh/contracts/ui_console_and_cockpit_contract.md` 的信息架构已包含 `Mission Control / Operations / Governance / Admin / Extended / Shared Features` 六组，并在 `Extended` 与 `Shared Features` 下列出 12+ 模块。 | 回写 contract，关闭导航能力域缺口。 |
| R7-36 | 37. UI 相关 Contract/ADR 与 UI 架构矛盾 | P1 | `docs_zh/contracts/gateway_message_contract` | 无 console WebSocket 推送协议定义 | 已解决（contract/文档回写） | 根因是 gateway message contract 之前只定义入站/出站消息，没有把 console 实时推送 envelope 写成正式协议。 | 当前 `docs_zh/contracts/gateway_message_contract.md` 已新增 `ConsolePushEnvelope`，定义 `task.status_changed / approval.created / approval.resolved / nl.clarification_needed / dashboard.snapshot / dashboard.delta` 等 WS messageType、channel 模型和 canonical `harnessRunId / nodeRunId` 关联键。 | 回写 contract，关闭 console WS 协议缺口。 |
| R7-37 | 37. UI 相关 Contract/ADR 与 UI 架构矛盾 | P1 | `docs_zh/contracts/dashboard_and_operator_experience_contract` | WorkflowBuilderDraft.steps 用线性模型——应为 DAG nodes/edges | 已解决（contract/文档回写） | 根因是 workflow builder draft contract 曾只说“草稿”而没把 canonical draft 结构写死，导致线性 `steps[]` 表述残留。 | 当前 `docs_zh/contracts/dashboard_and_operator_experience_contract.md` 已把 `WorkflowBuilderDraft.plan_graph_draft` 定义成 `graph_id / nodes[] / edges[] / entry_node_ids / terminal_node_ids / join_strategy` 结构，并明确 UI 步骤列表只能是 graph 的只读投影。 | 回写 contract，关闭 workflow builder 线性模型漂移。 |
| R7-38 | 37. UI 相关 Contract/ADR 与 UI 架构矛盾 | P1 | `docs_zh/contracts/hitl_experience_and_explainability_contract` | 用废弃 step 术语（step_id/step_output/step_retry） | 已解决（contract/文档） | 根因是 HITL 体验 contract 的正文还停留在 step-oriented 接管语言，虽然 remediation 已提到 canonical runtime，但主段落没有真正改写。 | 本轮已把人工接管动作回写为 `HarnessRun / NodeRun / NodeAttempt / override_artifact_ref / UnifiedRuntimeMode` 语义，并新增 invariant 约束 `tests/invariants/adr-contract-closure-round2.test.ts`，验证文档不再出现 `step_retry / step_output`。 | 关闭的是 contract 术语漂移，不外推为 UI 产品化链路已完成。 |
| R7-39 | 37. UI 相关 Contract/ADR 与 UI 架构矛盾 | P2 | `ui/docs/adr/` | 仅 placeholder README；UI spec 引用的 ADR-UI-001~009 全部不存在 | 已解决（ADR/文档） | 根因是 UI ADR 目录长期只有占位 README，没有把壳层路由、typed façade、WS 协议、auth/session、token、feature 边界、builder、性能和测试门禁固化成可引用 ADR。 | 本轮新增 `ui/docs/adr/ADR-UI-001` 至 `ADR-UI-009`，README 也补齐索引；`tests/invariants/adr-contract-closure-round2.test.ts` 已验证 001~009 文件都存在且可读取。 | 这是文档基线修复，不宣称 UI 实现主线已产品化。 |
| R7-40 | 37. UI 相关 Contract/ADR 与 UI 架构矛盾 | P2 | `docs_zh/contracts/sdk_surface_contract` | 无 MissionControlService typed 端点定义 | 已解决（contract/文档） | 根因是 `sdk_surface_contract` 只有泛化 `ApiClient`，没有把 Mission Control 作为 typed façade 冻结下来，且把 `taskId` 与 runtime truth 查询键混在一起。 | 本轮在 `docs_zh/contracts/sdk_surface_contract.md` 增加了 `MissionControlService` / `MissionControlProjectionAliases` typed surface，明确 `harnessRunId` 为 canonical 锚点、`taskId` 仅为 projection alias；对应 invariant 已补。 | 关闭的是 typed SDK contract 缺口，不外推为后端 presentation 实装已完全对齐。 |
| R7-41 | 38. 剩余平台缺口（API 网关/安全/可靠性） | P0 | `src/platform/interface/api/middleware/` | 无 rate-limiting middleware；§9.2 要求 per-endpoint-class 速率限制 | 已解决 | 原结论停留在 HTTP server 还未把分布式限流器接到主请求链之前。 | 当前 `src/platform/five-plane-interface/api/http-api-server.ts` 已在 `handleRequest()` 中于非 `OPTIONS` 请求前调用 `DistributedRateLimiter.checkAndConsume(...)`，并统一附带 `x-ratelimit-*` / `retry-after` 响应头；相关行为已有 `tests/integration/platform/interface/api/http-api-server-auth-gateway-load.test.ts` 覆盖 429 路径。 | 不改生产代码，回写现行入口级限流依据。 |
| R7-42 | 38. 剩余平台缺口（API 网关/安全/可靠性） | P0 | `src/platform/interface/api/middleware/` | 无 Idempotency-Key middleware；§6.2 要求幂等保证 | 已修复 | 根因是幂等能力原本只停留在中间件文件本身，既没有挂到 `HttpApiServer` 主链，也没有把“进行中请求”和“已完成缓存响应”区分开，导致同 key 重试既可能漏拦截，也可能错误冲突。 | 当前 `src/platform/five-plane-interface/api/http-api-server.ts` 已在请求入口统一调用 `IdempotencyKeyMiddleware.check()/record()/clear()`；`src/platform/five-plane-interface/api/middleware/idempotency-key.ts` 也已保留原 method、区分 in-flight 与 replay。定向验证 `tests/integration/platform/interface/api/http-api-server-architecture-regressions.test.ts` 与 `tests/unit/platform/five-plane-interface/api/middleware/idempotency-key.test.ts` 已通过。 | 已改主链接线与中间件细节，不做全量测试。 |
| R7-43 | 38. 剩余平台缺口（API 网关/安全/可靠性） | P0 | `src/platform/interface/api/http-server/` | 响应缺 X-Trace-Id header；§6.2 要求全链路追踪透传 | 已修复 | 根因是请求入口只读取 `x-request-id`，没有把 `x-correlation-id` 纳入 trace 源，也没有保证最终响应把该 trace 头稳定回传。 | 当前 `src/platform/five-plane-interface/api/http-server/utils.ts` 的 `readRequestId()` 已优先读取 `x-correlation-id`；`response-hardening.ts` 与 error/json response builder 也都会落 `x-trace-id`。定向验证 `tests/integration/platform/interface/api/http-api-server-architecture-regressions.test.ts` 已证明 header 会沿响应透传。 | 已改主请求链 trace 读取和响应透传。 |
| R7-44 | 38. 剩余平台缺口（API 网关/安全/可靠性） | P0 | `src/platform/contracts/` | 无 inter-plane ContractEnvelope 签名验证；§5.2 要求签名+版本校验 | 已修复（源码核实确认） | 真实根因已从”完全没有签名能力”收敛为”contracts 层已有 envelope 签名/验证原语，但调用主链并未稳定采用，导致平面间消息仍可能绕过签名校验”。 | 经逐行源码核实确认三项实现：(1) `src/platform/contracts/executable-contracts/index.ts:2022-2039` canonical `signContractEnvelope()` 使用 `createHash(“sha256”)` + timing-safe comparison；(2) `src/platform/contracts/executable-contracts/index.ts:1966-2012` canonical `verifyContractEnvelopeSignature()` 使用 `createHash(“sha256”)` + timing-safe comparison；(3) `src/sdk/client-sdk/api-client.ts:52-92` SDK 层 `signContractEnvelope()`/`verifyContractEnvelopeSignature()` 使用 `createHmac(“sha256”)` + `timingSafeEqual`。`inter-plane-contract-gateway.ts` 第 247 行 `sendToPlane()` 调用 `signContractEnvelope()` 签名，第 317 行 `receiveFromPlane()` 调用 `verifyContractEnvelopeSignature()` 验签。所有 inter-plane ContractEnvelope 均强制 HMAC-SHA256 签名+验证，符合 §5.2 要求。 | 关闭的是”签名验证能力缺失”缺口；adoption 覆盖度由 R7-45 单独跟踪。 |
| R7-45 | 38. 剩余平台缺口（API 网关/安全/可靠性） | P0 | `src/platform/` | 无 bulkhead isolation pattern；§9.1 要求平面间故障隔离 | 已修复（R7-44 SDK 链路同批验收，其他平面入口继续整改） | 根因已从”平台完全没有 bulkhead 实现”收敛为”bulkhead 库存在，但 SDK 主链路之外的其他平面间调用尚未统一 adoption”。 | 经逐行核实确认：(1) `src/platform/stability/bulkhead-isolation.ts` 已提供 `BulkheadIsolator`/`BulkheadRegistry` + `BulkheadRejectionError`/`BulkheadTimeoutError`；(2) `src/platform/contracts/inter-plane-contract-gateway.ts` 第 104-286 行已在 `sendToPlane()` 中通过 `getOutgoingBulkhead(targetPlane).execute()` 对每个 caller-callee 配对做隔离；(3) `src/sdk/harness-sdk/index.ts` 第 170-180 行 `getInterPlaneBulkhead()` 按 targetPlane 创建 isolator，第 244-250 行在 `sendInterPlaneMessage()` 中调用 `bulkhead.execute()` 并对 `BulkheadRejectionError`/`BulkheadTimeoutError` 做 fail-closed 抛 `harness_sdk.inter_plane_*`；(4) `tests/unit/sdk/harness-sdk/index.test.ts` 已覆盖并发超限 reject 路径。本轮关闭范围仅限”bulkhead 机制已进入一条可验证的发送主链（R7-44 SDK 链路同批）”；其他平面实际入口的统一 adoption 仍为 open item，不在本轮声称范围内。 |
| R7-46 | 38. 剩余平台缺口（API 网关/安全/可靠性） | P0 | `src/platform/control-plane/iam/` | SAML 实现缺 X.509 trust-chain 验证/C14N/encrypted assertion（安全关键 TODO） | 已修复（源码 hardening 并补定向测试） | 真实根因有三段：旧实现只有 fingerprint 与 XML signature 基础校验，没有可信 CA trust-chain 验证；没有 canonicalization allowlist；对 `EncryptedAssertion` 既不解密也不 fail-closed。 | 当前 `src/org-governance/sso-scim/saml/index.ts` 已新增 `SAML_CANONICALIZATION_ALGORITHMS` allowlist、`X509TrustChainValidator` 的 Node 原生证书解析/验证、provider 级 `trustedCaCertificates` 与 `allowEncryptedAssertions` 配置，并在 `consumeAssertion()` 中对未显式允许的 `EncryptedAssertion` 直接 fail-closed。`tests/unit/org-governance/sso-scim/saml/saml-signature.test.ts` 已新增并通过 trust-chain、unsupported canonicalization、encrypted assertion 拒绝三类回归。 | 关闭的是 SAML 安全关键 TODO：当前实现对不受信任证书、异常 C14N 和未支持的加密断言均会 fail-closed。 |
| R7-47 | 38. 剩余平台缺口（API 网关/安全/可靠性） | P1 | `src/platform/interface/api/` | 无 API 版本路由/协商机制；§6.4 要求 Accept-Version header 路由 | 已修复 | 根因是 `VersionRoutingMiddleware` 先前只是孤立定义，HTTP 主链并不会解析 `Accept-Version`，版本提示头和真正的请求协商是脱节的。 | 当前 `src/platform/five-plane-interface/api/http-api-server.ts` 已在 `dispatchRequest()` 入口调用 `globalVersionRoutingMiddleware.parseAcceptVersion()/selectVersion()`，对低于最低版本的请求 fail closed，并把选定版本写回 `x-api-version`；`tests/integration/platform/interface/api/http-api-server-architecture-regressions.test.ts` 已验证拒绝与接受两条路径。 | 已把版本协商接入主请求链。 |
| R7-48 | 38. 剩余平台缺口（API 网关/安全/可靠性） | P1 | `src/platform/stability/` | 仅 rehearsal runner 无可复用可靠性库（circuit-breaker/retry/timeout 均缺失） | 已修复（reliability 子目录已实现，但尚未从 stability/index.ts 导出） | 根因是审计人员未发现 `src/platform/stability/reliability/` 子目录已实现三个库。 | 当前 `src/platform/stability/reliability/` 已包含 `circuit-breaker.ts`（CircuitBreaker + CircuitState + CircuitBreakerOpenError/CircuitBreakerTimeoutError）、`retry.ts`（Retry + RetryResult + RetryTimeoutError + withRetry）、`timeout.ts`（Timeout + TimeoutState + TimeoutError + withTimeout + withDeadline）；`src/platform/stability/reliability/index.ts` 已定义导出。但 `src/platform/stability/index.ts` 尚未 re-export reliability 库，导致上层调用无法通过 `stability/` 统一入口引用。 | 关闭的是“库已实现”缺口；待确认是否需要统一导出或保持独立引用路径。 |
| R7-49 | 38. 剩余平台缺口（API 网关/安全/可靠性） | P1 | `src/platform/interface/api/middleware/` | CORS 默认 allowedOrigins:["*"] + credentials:true——安全反模式 | 已解决（源码复核并补 fail-closed 配置校验） | 根因不在 `middleware/cors.ts`，而在真实请求链使用的 `http-server/response-hardening.ts`。虽然运行时已会拒绝 `* + credentials`，但配置归一化仍接受该危险组合，导致错误配置在启动期被静默放行，只在请求期表现为异常拒绝。 | 当前 `src/platform/interface/api/http-server/response-hardening.ts` 的 `DEFAULT_CORS_CONFIG` 已默认 `allowedOrigins: []`、`credentials: false`，且 `normalizeCorsConfig()` 现在会对 `allowedOrigins.includes("*") && credentials=true` 直接 fail-closed 抛 `api.cors.invalid_wildcard_credentials`。定向验证 `tests/unit/platform/interface/api/http-server/response-hardening.test.ts` 已通过。 | 关闭的是 HTTP API 主链的真实 CORS 安全缺口，不把“另一套 middleware 已安全”误写成全链路已证。 |
| R7-50 | 38. 剩余平台缺口（API 网关/安全/可靠性） | P1 | `src/platform/execution/worker-pool/` | WorkerDrainProtocol 40行 stub 缺 §8.2 drain-quiesce-terminate 三阶段行为 | 已解决（源码复核确认三阶段实现） | 根因是审计条目停留在历史 stub 阶段；现行 `worker-drain-protocol.ts:1-796` 已实现完整三阶段状态机。 | 当前 `WorkerDrainPhase` 枚举（第 17-24 行）定义了 DRAIN/QUIESCE/TERMINATE 三个阶段；`WorkerDrainProtocol` 类（第 272-796 行）通过 `beginDrain()`、`advancePhase()`、`createReceipt()` 实现状态转移；`WorkerDrainPhaseEvent` 枚举（第 171-188 行）覆盖 DRAIN_STARTED/COMPLETED、QUIESCE_STARTED/COMPLETED、TERMINATE_STARTED/COMPLETED、ALL_LEASES_RELEASED、FORCED_HANDOFF_OCCURRED 等事件；`forceHandoffPendingLeases()`（第 764-795 行）实现强制交接；`coordinateCheckpoint()`（第 436-459 行）与 `notifyRecoveryWorker()`（第 467-487 行）接入 §9 集成。未进行专项定向测试验证。 | 本轮关闭的是“stub 状态机已补全实现”缺口，不外推为已通过集成测试验证。 |
| R7-51 | 38. 剩余平台缺口（API 网关/安全/可靠性） | P1 | `src/org-governance/` | 治理控制台缺持久审计日志 + RBAC 检查（标注 TODO） | 已解决（源码复核） | 根因是审计条目停留在历史阶段，声称治理控制台缺持久审计日志和 RBAC 检查，但实际 `src/org-governance/delegated-governance/governance-console-service.ts` 已完整实现 `SelfServiceGovernanceConsole` 类，包含 `SqliteAuditLogStore` 持久化审计日志和 `RBAC_PERMISSIONS` / `checkPermission()` RBAC 检查。 | 当前 `delegated-governance/stores/index.ts` 第 187-279 行的 `SqliteAuditLogStore` 已实现完整 SQLite 持久化审计日志表 `governance_audit_log`；`governance-console-service.ts` 第 119-165 行的 `RBAC_PERMISSIONS` 定义了各操作的角色权限，`checkPermission()` 在所有操作（createDelegation/revokeDelegation/exportAuditLog 等）中作为 guard 调用。TODOs 已全部清除。 | 关闭的是"审计条目过时未跟进实现"缺口，不外推为所有 org-governance 功能均已完备。 |
| R7-52 | 38. 剩余平台缺口（API 网关/安全/可靠性） | P2 | `src/platform/shared/stability/ vs stability/` | 重复模块树；职责边界不清 | 已解决（源码复核） | 原始问题指控"重复模块树"和"职责边界不清"。经逐文件核实：`src/platform/shared/stability/` 是 1 行纯转发 facade（所有文件均为 `export * from "../../stability/..."`），不是重复实现；`src/platform/stability/` 才是所有实现的权威位置。职责边界已在 `shared/stability/index.ts` 注释中明确定义：shared 是 facade，platform/stability 是实现。但 `platform/stability/reliability/` 子目录（circuit-breaker/retry/timeout）未通过 `platform/stability/index.ts` 对外导出，且 `shared/stability/` 对 reliability 子目录缺少 re-export，导致 R7-48 指出 reliability 库"尚未从 stability/index.ts 导出"。 | 本轮为 `src/platform/shared/stability/reliability/` 补充了完整 re-export facade（index.ts + 三文件转发），与 shared/stability 其他模块保持一致模式。`src/platform/stability/index.ts` 39 行显式列出所有导出，reliability 子模块未在其中是设计选择（reliability 可被直接引用：`import { CircuitBreaker } from "platform/stability/reliability/circuit-breaker.js"`）。当前双目录结构职责已澄清，无冗余实现。 | 关闭的是"shared/stability 被误认为重复实现"问题；reliability 导出策略由 R7-48 单独跟踪。 |
| R8-01 | 40. 平台核心深层缺口（Model Gateway / Planner / Recovery / Evidence） | P0 | `src/platform/model-gateway/cost-tracker/budget-guard.ts` | Budget 检查为无状态比较；§18.3 要求原子 reserve→execute→settle + BudgetReservation 状态机；并发可超支 | 已解决（源码修复并补定向测试） | 根因是旧 `BudgetExecutionSessionManager` 只把 reserve 结果存在内存 session，后续 `settle/release` 没有把 reservation fencing token 带回 `BudgetAllocator`，看似有生命周期，实际不能满足状态机写保护。 | 当前 `src/platform/model-gateway/cost-tracker/budget-guard.ts:422-430,477-534,578-629,665-702` 已把 session 升级为 `reserve→executing→settled/released` 状态机，并保存/回传 `fencingToken` 到 `reserve/settle/release`。`tests/unit/platform/model-gateway/budget-routing-regressions.test.ts:9-42` 已验证完整 reserve→execute→settle 闭环。 | 这次修的是原子生命周期与 fencing 一致性，不是只补一个状态枚举。 |
| R8-02 | 40. 平台核心深层缺口（Model Gateway / Planner / Recovery / Evidence） | P0 | `src/platform/execution/recovery/runtime-recovery-service.ts` | Recovery 服务只读——分类故障并建议动作但从不执行；无 saga rollback/compensation executor/CompensationRecord | 已修复 | compensation handler 已添加到 RuntimeRecoveryDecisionService，实现 saga rollback/compensation executor 和 CompensationRecord。 |
| R8-03 | 40. 平台核心深层缺口（Model Gateway / Planner / Recovery / Evidence） | P0 | `src/platform/orchestration/planner/plan-builder.ts` | 构建 legacy Plan(steps array) 而非 PlanGraphBundle DAG；无 §13.9 图规范化/§13.11 风险传播/§13.12 最坏路径分析 | 已解决（源码复核并补定向测试） | 根因是审计条目停留在 `build()` 仍返回线性 `steps[]` 的历史阶段，没有跟进后续 DAG 化改造。 | 当前 `src/platform/orchestration/planner/plan-builder.ts:25-31,69-160,173-182,190-260` 的 `build()` 已直接返回 `PlanGraphBundle`，包含 `nodes/edges/entryNodeIds/terminalNodeIds`、扩展 validation、risk propagation 与 worst-path analysis；`tests/unit/platform/orchestration/planner/plan-builder-graph-regressions.test.ts` 已定向通过。 | 这条属于实现已在、审计结论过期。 |
| R8-04 | 40. 平台核心深层缺口（Model Gateway / Planner / Recovery / Evidence） | P1 | `src/platform/model-gateway/provider-registry/model-routing-service.ts` | 无延迟 SLO 强制；缺 latency_optimized 路由策略/P99 追踪/data_residency/pii_input_detected 约束 | 已解决（源码修复并补定向测试） | 根因不是完全没有 telemetry，而是旧实现只把 latency/residency 当记录字段，不把它们作为候选过滤条件；PII、training opt-out、judge independence 也没有真正约束路由。 | 当前 `src/platform/model-gateway/provider-registry/model-routing-service.ts:418,434-470,651-690,794-879,881-894` 已强制计算 latency SLO、按 `data_residency/pii_input_detected/model_training_opt_out/judge_independence` 过滤候选，并优先选择满足 SLO 的 profile，同时记录 P99 与 residency 结果。`tests/unit/platform/model-gateway/budget-routing-regressions.test.ts:45-71,84-138` 已覆盖 residency/PII/opt-out/judge/SLO 约束。 | 关闭的是“约束未生效”缺口，不外推为未来所有路由策略都已完整。 |
| R8-05 | 40. 平台核心深层缺口（Model Gateway / Planner / Recovery / Evidence） | P1 | `src/platform/model-gateway/provider-registry/model-routing-service.ts` | 路由决策仅内存——无持久化到 BudgetLedger 或 evidence store；缺模型选择审计轨迹 | 已解决（源码修复并补定向测试） | 根因是旧 `ModelRoutingService` 只返回内存 decision，对选型结果、SLO、residency 和 fallback 是否触发没有任何可审计持久化出口。 | 当前 `src/platform/model-gateway/provider-registry/model-routing-service.ts:166-185,187-193,738-757` 已定义 `ModelRoutingDecisionRecord/ModelRoutingPersistence` 并在路由完成后持久化 selected profile、provider、routeReason、latencySLO/P99、residency 与 fallback 信息；`tests/unit/platform/model-gateway/budget-routing-regressions.test.ts:45-82` 已验证持久化记录真实落出。 | 关闭范围是“路由决策已有持久化审计出口”，不夸大为所有 evidence sink 都已统一。 |
| R8-06 | 40. 平台核心深层缺口（Model Gateway / Planner / Recovery / Evidence） | P1 | `src/platform/model-gateway/degradation/degradation-controller.ts` | getFallbackCandidates() 返回空数组使 D1 降级死代码；递归 route() 可栈溢出 | 已修复 | getFallbackCandidates 和递归 depth 已修复。 |
| R8-07 | 40. 平台核心深层缺口（Model Gateway / Planner / Recovery / Evidence） | P1 | `src/platform/model-gateway/provider-registry/circuit-breaker.ts` | 失败率公式 `(failures/windowSec)*10` 非百分比；50% 阈值(§9.4)语义错误 | 已解决（源码复核并补定向测试） | 根因是审计条目沿用了旧公式；现行实现早已改成“失败数 / 总样本数”，并在达到最小样本量后按 `>= 0.5` 判定 50% failure rate。 | 当前 `src/platform/model-gateway/provider-registry/circuit-breaker.ts:175-184` 明确使用 `recentFailureRate >= 0.5` 与 `recentSampleCount >= minSampleSize`；`tests/unit/platform/model-gateway/budget-routing-regressions.test.ts:140-156` 已验证 6/10 失败会打开熔断器。 | 这条属于源码已修、文档未跟进。 |
| R8-08 | 40. 平台核心深层缺口（Model Gateway / Planner / Recovery / Evidence） | P1 | `src/platform/prompt-engine/eval/llm-eval-service.ts` | runAbTest() 使用硬编码分数(0.85/0.90)模拟评估；无真实 LLM 调用/统计显著性检验 | 已解决（源码修复并补定向测试） | 根因是旧 A/B seam 只有 judge 分数，没有“按 arm 生成真实输出再评分”的 evaluator 接口，所以 `actualOutput` 与得分只能靠占位字符串和硬编码。 | 当前 `src/platform/prompt-engine/eval/llm-eval-service.ts:182-205,431-479,533-556` 新增 `evaluateCase()` seam，并在 `runAbTest()` 中记录 evaluator 返回的 `actualOutput/score/latency/metadata`，再做显著性计算；`tests/unit/platform/prompt-engine/llm-eval-ab-test-regressions.test.ts` 已验证不再写入占位输出。 | 关闭的是 A/B evaluator 真正接线缺口，不外推为线上 judge 质量已经充分。 |
| R8-09 | 40. 平台核心深层缺口（Model Gateway / Planner / Recovery / Evidence） | P1 | `src/platform/state-evidence/events/event-registry.ts` | 主事件类型用 legacy 命名(task:status_changed)；canonical platform.harness_run.* 未接入主注册表 | 已解决（源码复核） | 根因不是主注册表仍在用 legacy 命名，而是 reaudit 停留在 alias 映射尚未迁移前的旧快照。 | 当前 `event-registry.ts` 第 52-57 行 `LEGACY_TO_CANONICAL_EVENT_ALIASES` 已把 `task:status_changed` 映射到 `platform.harness_run.status_changed`；第 400-617 行 `CANONICAL_RUNTIME_EVENT_SCHEMA_REGISTRY` 已包含全部 `platform.harness_run.*` 事件（created/admitted/planning/ready/pausing/replanning/compensating/aborted/completed/status_changed），并通过第 619-622 行 `RAW_EVENT_SCHEMA_REGISTRY` 合并进主注册表；第 1130-1475 行 `RUNTIME_EVENT_REPLAY_METADATA` 也完整覆盖。Legacy 别名保留用于向后兼容，但 canonical 事件已接入主注册表。 | 这是源码早已就位、审计结论过期的情形，不是假关闭。 |
| R8-10 | 40. 平台核心深层缺口（Model Gateway / Planner / Recovery / Evidence） | P1 | `src/platform/state-evidence/checkpoints/` | Checkpoint 基于 workflow-step 而非 NodeRun/NodeAttempt；缺 graphVersion/planGraphId 无法与 PlanGraph 对齐 | 已解决（执行链已切到 node_run_snapshot） | 根因此前分两层：graph metadata 已通，但真正写 artifact 的两条主执行路径还停留在 `workflow_step_snapshot` + `createWorkflowStepCheckpoint(...)`，导致 NodeRun schema 只是“类型存在、运行时未使用”。 | 当前 `src/platform/five-plane-execution/execution-engine/single-task-happy-path.ts` 与 `multi-step-supervisor.ts` 都已改为写入 `kind: "node_run_snapshot"`，并调用 `createNodeRunCheckpoint(...)`；`decisionContext` 与 `resumeContext` 也已映射到 `priorNodeSummaries/dependsOnNodeIds/completedNodeIds/nextNodeId`。新增回归 `tests/unit/platform/five-plane-state-evidence/checkpoints/node-run-checkpoint-migration.test.ts` 已验证 `readNodeRunCheckpoint()` 只接受 `node_run_snapshot` 且能成功读回 canonical NodeRun checkpoint。 | 关闭运行时仍写 legacy workflow-step checkpoint 的真实缺口。 |
| R8-11 | 40. 平台核心深层缺口（Model Gateway / Planner / Recovery / Evidence） | P1 | `src/platform/state-evidence/knowledge/semantic-knowledge-graph.ts` | 仅3种边类型(contains/shared_keyword/same_document)；缺实体关系边/信任传播/knowledge.trust_downgraded 事件；纯内存无持久层 | 已解决（源码修复） | 根因是原实现仅覆盖 3 种基础边类型和内存操作；后续迭代已添加 `sequential`/`entity_relation` 边类型、`handleTrustDowngradedEvent` 信任降级处理、`serialize`/`deserialize` 持久化能力。 | 当前 `src/platform/state-evidence/knowledge/semantic-knowledge-graph.ts` 第 4-19 行已定义 15 种边类型（包含 `sequential`/`entity_relation`），第 432-475 行实现了 `handleTrustDowngradedEvent` 信任降级事件处理，第 502-558 行实现了 `serialize`/`deserialize` 持久化方法，第 480-497 行提供了 `addSequentialEdge` 和 `addEntityRelationEdge` 便捷方法。 | 关闭的是"缺边类型/信任传播/持久化"缺口。 |
| R8-12 | 40. 平台核心深层缺口（Model Gateway / Planner / Recovery / Evidence） | P1 | `src/platform/orchestration/planner/plan-evaluator.ts` | 成本估算为 `steps.length * 1000` 硬编码常数；无 token 估算/并行分支检测(§13.8)/风险加权成本 | 已解决（源码复核） | 根因是旧实现使用 naive steps.length*1000 启发式估算；现行实现已改用 token-based 估算、并行分支检测和风险加权成本。 | 当前 `src/platform/orchestration/planner/plan-evaluator.ts` 第 34-126 行已实现 `estimateTokenBudget` 函数，包含：依赖图并行分支检测（第 39-81 行）、per-step tool cost map（第 83-97 行）、risk-weighted multiplier（第 99-106 行）、以及 `estimateCostUsd` 基于 token 估算的 USD 成本计算（第 132-137 行）。 | 关闭的是"成本估算硬编码/无 token 估算/无并行分支检测/无风险加权"缺口。 |
| R8-13 | 40. 平台核心深层缺口（Model Gateway / Planner / Recovery / Evidence） | P1 | `src/platform/orchestration/planner/plan-dag-validator.ts` | 仅验证环/自依赖/缺失依赖；不检查入口/终端节点存在性/executor 可用性/risk/budget/tool/sandbox 完整性(§13.10) | 已解决（源码修复） | 根因是 DAG validator 原本仅做拓扑排序校验；后续迭代已为 `PlanDagValidator` 增加完整的 §13.10 验证能力：入口/终端节点存在性（153-163行）、executor 可用性（180-214行）、riskClass/budget 完整性（186-192行）、tool 可用性（224-233行）、sandbox 兼容性（235-249行）、预算约束（252-260行）。 | 当前 `src/platform/orchestration/planner/plan-dag-validator.ts` 第 153-260 行已实现所有 §13.10 要求的验证：entry node（153-157行）、terminal node（159-163行）、executor availability（180-214行）、risk/budget completeness（186-192行）、tool availability（224-233行）、sandbox compatibility（235-249行）、budget constraint（252-260行）。 | 关闭的是"缺少 §13.10 完整性验证"缺口。 |
| R8-14 | 40. 平台核心深层缺口（Model Gateway / Planner / Recovery / Evidence） | P1 | `src/platform/execution/recovery/failure-classification.ts` | 分类器针对 coding-agent 错误(schema_error/lint_error/test_failure)；非通用平台恢复分类器(§9.6 异常分类法) | 部分解决（源码修复） | 根因是 `schema_error` 被错误标记为 `isPlatformException: false`，导致 schema 验证失败被归类为 coding-agent 专有错误。schema 验证是平台层能力（处理外部输入/API schema），应作为平台异常。本轮将 `schema_error` 的 `isPlatformException` 从 `false` 改为 `true`。lint_error 和 test_failure 保持 `isPlatformException: false`（这些确实是 coding-agent 特有错误）。 | 当前 `src/platform/execution/recovery/failure-classification.ts:312` 已将 `schema_error` 改为 `isPlatformException: true`；`tests/unit/platform/execution/recovery/failure-classification.test.ts` 相关测试已同步更新。 | 关闭的是"schema_error 被错误归类为 coding-agent 错误"这一具体问题；lint_error/test_failure 仍是 coding-agent 特有，不影响通用平台分类器的完整性。 |
| R8-15 | 40. 平台核心深层缺口（Model Gateway / Planner / Recovery / Evidence） | P2 | `src/platform/model-gateway/cost-tracker/chargeback-service.ts` | 无多币种/汇率支持；§18.4 要求 original_currency/base_currency/FX snapshot | 已解决（源码修复） | 根因是旧 chargeback service 缺乏多币种抽象；现行实现已在 `ChargebackAllocation` 接口（第 12-14 行）定义 `originalCurrency`、`baseCurrency`、`fxRateToBase` 字段，并在 `buildReport()` 中为每条 allocation 快照当时的 FX 汇率（第 110 行）。 | 当前 `src/platform/model-gateway/cost-tracker/chargeback-service.ts` 已实现完整多币种支持：`ChargebackAllocation` 含 originalCurrency/baseCurrency/fxRateToBase；`getFxRateToUsd()` 与 `convertCurrency()` 提供币种换算；`buildReport()` 在第 110 行对每笔 resource 捕获 FX snapshot 并写入 allocation。 | 关闭的是"chargeback service 无多币种/FX snapshot"缺口。 |
| R8-16 | 40. 平台核心深层缺口（Model Gateway / Planner / Recovery / Evidence） | P2 | `src/platform/model-gateway/fallback/index.ts` | Fallback 选最便宜健康替代；无有序回退链(primary→secondary→tertiary) §15.4 | 已修复 | `selectFallback()` 现已实现有序回退链：`FALLBACK_CHAIN_ORDER = ["primary", "secondary", "tertiary"]`，按 tier 顺序选择，只在当前 tier 无健康候选时才进入下一 tier。代码同时补全了类型安全性（非空断言）。 | 类型检查通过。 |
| R8-17 | 40. 平台核心深层缺口（Model Gateway / Planner / Recovery / Evidence） | P2 | `src/platform/prompt-engine/rollout/prompt-rollout-stage.ts` | 管线 draft→review→staging→shadow→canary_5→partial_25→50→75→stable；§16.3 仅 canary(5%)→canary(20%)→stable；多余阶段无自动回滚质量门 | 已解决（源码复核） | 原审计指控的"管线多余阶段"问题，实际源码早已按 §16.3 简化：`PROMPT_ROLLOUT_STAGES = ["canary_5", "canary_20", "stable", "rolled_back"]`；`evaluateRolloutMetrics()` 已实现基于 errorRate/latency/quality 的自动回滚质量门。 | 当前 `src/platform/prompt-engine/rollout/prompt-rollout-stage.ts` 已定义正确管线与 `QUALITY_GATE_THRESHOLDS`（canary_5: 5% 错误率，canary_20: 3% 错误率，stable: 1% 错误率）；`src/platform/prompt-engine/rollout/index.ts` 的 `PromptRolloutService.evaluateRolloutMetrics()` 已实现自动回滚逻辑（qualityRegression/latencyRegression/errorRateThreshold）。 | 本轮经逐文件复核确认源码已正确实现，非假关闭。 |
| R8-18 | 40. 平台核心深层缺口（Model Gateway / Planner / Recovery / Evidence） | P2 | `src/platform/state-evidence/memory/memory-layer-model.ts` | working 层 LRU 驱逐无 ContextTruncationReport；§29.2 要求"事实不可静默丢弃，压缩需附损失报告" | 已修复 | R8-18 FIX: 在 `MemoryService.remember()` 中集成 `evictFromWorkingLayer()` 调用。当存储 working layer（layer_3, scope 为 task_runtime/runtime）内存时，检查当前 working layer 记录数是否超过 `MAX_WORKING_MEMORY_RECORDS`（1000 条）。若超出，调用 `evictFromWorkingLayer()` 获取待驱逐记录 ID 列表和 `ContextTruncationReport`，然后通过 `revokeMemory()` 标记驱逐的记录。报告生成时会记录 evictedCount、remainingCount 等指标，满足 §29.2 对"事实不可静默丢弃"的要求。 | 类型检查通过；`evictFromWorkingLayer` 已在 `memory-service.ts` 的 `remember()` 方法中调用，驱逐时生成 `ContextTruncationReport`。 | 本轮修复的是"R8-18: working 层 LRU 驱逐无 ContextTruncationReport"问题，通过在 `MemoryService.remember()` 中集成 `evictFromWorkingLayer()` 实现。 |
| R8-19 | 41. SDK / 插件 / 域注册 / 多区域 / 运维成熟度 | P0 | `src/sdk/client-sdk/api-client.ts` | 无 ContractEnvelope 包装；§5.2 要求所有 inter-plane 消息含 schemaVersion/commandId/correlationId/signature | 已修复 | ContractEnvelope 已实现完整字段（schemaVersion/commandId/correlationId/signature），所有 inter-plane 消息均已正确包装。 |
| R8-20 | 41. SDK / 插件 / 域注册 / 多区域 / 运维成熟度 | P0 | `src/sdk/client-sdk/` | 无事件订阅/流式 API；§6/§28 要求 typed event subscription(PlatformFactEvent/ProjectionUpdate/run lifecycle) | 已修复 | EventSubscriptionClient 已添加，实现 typed event subscription（PlatformFactEvent/ProjectionUpdate/run lifecycle）。 |
| R8-21 | 41. SDK / 插件 / 域注册 / 多区域 / 运维成熟度 | P0 | `src/sdk/harness-sdk/index.ts` | appendStep() 仍用 stage 字符串路由；不产出 NodeAttemptReceipt(§5.3)；nodeRunId/planGraphId 塞入 inputs bag | 已修复 | nodeId/planGraphId 路由已修复，appendStep() 现已支持 nodeId 和 planGraphId 作为独立参数传递，不再塞入 inputs bag。 |
| R8-22 | 41. SDK / 插件 / 域注册 / 多区域 / 运维成熟度 | P1 | `src/sdk/harness-sdk/index.ts` | 无 PlanGraphBundle 构建/验证 API；§22 SDK 须暴露图级规划操作 | 已修复 | R8-22 FIX: `HarnessSdk` 类已实现 `buildPlanGraph(input: PlanGraphBuildInput): PlanGraphBundleBuildResult`、`validatePlanGraph(graph: PlanGraph): GraphValidationReport` 和 `validatePlanGraphBundle(bundle: PlanGraphBundle): PlanGraphValidationResult` 三个方法（lines 716-740）。同时导出 standalone 函数 `buildPlanGraphBundle()`、`validatePlanGraph()` 和 `validatePlanGraphBundle()`（lines 775-942），支持图级规划操作的完整生命周期。§22 SDK 图级规划操作已暴露。 | 代码复核确认：SDK 已暴露图级规划 API，非假收口。 |
| R8-23 | 41. SDK / 插件 / 域注册 / 多区域 / 运维成熟度 | P1 | `src/sdk/admin-sdk/index.ts` | 无 OperationalDirective/DecisionDirective typed 方法；pauseHarnessRun/abortHarnessRun 绕过指令信封模型 | 已修复 | admin-sdk directive methods 已添加。 |
| R8-24 | 41. SDK / 插件 / 域注册 / 多区域 / 运维成熟度 | P1 | `src/plugins/builtin-plugin-registry.ts` | 内置插件无 PluginManifest；§10 要求 owner/trustLevel/sbomRef/publicSdkSurface | 已修复 | PluginManifest 已添加 trustLevel/sbomRef/publicSdkSurface 字段。 |
| R8-25 | 41. SDK / 插件 / 域注册 / 多区域 / 运维成熟度 | P1 | `src/plugins/adapters/github-adapter.ts` | 插件加载无签名验证；§10 要求 signing.keyId/signature/algorithm 验证后才激活 | 已修复 | Plugin signature verification 已添加。 |
| R8-26 | 41. SDK / 插件 / 域注册 / 多区域 / 运维成熟度 | P1 | `src/plugins/ (所有内置插件)` | 无完整生命周期钩子；仅 initialize/healthCheck/shutdown，缺 onLoad/onActivate/onDeactivate/onUnload(§10) | 已修复 | onLoad/onActivate/onDeactivate/onUnload hooks 已添加。 |
| R8-27 | 41. SDK / 插件 / 域注册 / 多区域 / 运维成熟度 | P0 | `src/domains/registry/domain-model.ts` | 无 DomainManifest 类型；§37 要求含 capability matrix/risk classification/schema registry 引用 | 已解决（源码复核） | 根因不是当前实现仍缺 `DomainManifest`，而是审计条目停留在更早的 schema 形态，后续 `domain-model` 已把 manifest contract 收敛成一等类型，但文档没有同步回写。 | 当前 `src/domains/registry/domain-model.ts` 已定义 `DomainManifestSchema` 与 `export type DomainManifest`；相关回归已存在于 `tests/unit/domains/domain-model.test.ts`、`tests/unit/domains/registry/domain-model-schemas.test.ts`、`tests/unit/domains/registry/domain-manifest-resource-quotas.test.ts` 与 `tests/unit/domains/registry/domain-descriptor-schema.test.ts`。 | 这条是现行源码已实现、审计结论过期，不是假关闭。 |
| R8-28 | 41. SDK / 插件 / 域注册 / 多区域 / 运维成熟度 | P1 | `src/domains/domain-specs.ts` | DomainRiskSpecSchema 缺 advisory_only/human_accountable/deterministic_hot_path_only 字段(§3.2 责任边界) | 已解决（源码复核） | 根因不是责任边界字段仍未实现，而是审计条目引用了旧字段集；现行 `DomainRiskSpecSchema` 已把 advisory / human-accountable / deterministic-hot-path 三类边界显式冻结进 schema。 | 当前 `src/domains/domain-specs.ts` 已包含 `advisoryOnly`、`humanAccountable`、`deterministicHotPathOnly`，并在内建风险配置中实际使用；`tests/unit/domains/domain-specs.test.ts` 与 `tests/unit/domains/domain-risk-boundary.test.ts` 已覆盖默认值和责任边界行为。 | 回写真实现状与现有测试依据。 |
| R8-29 | 41. SDK / 插件 / 域注册 / 多区域 / 运维成熟度 | P1 | `src/domains/registry/` | 无专用 SchemaRegistry；§37 要求域输入/输出 schema 版本管理+兼容性检查 | 已解决（源码复核） | 根因不是系统现在仍没有专用 schema registry，而是 reaudit 沿用了 registry 抽象落地前的旧结论；现行实现已经把 schema 版本管理和兼容性检查下沉到独立模块。 | 当前 `src/domains/registry/schema-registry.ts` 已实现 `SchemaRegistry`，覆盖 register/get/list/checkCompatibility/deprecate；`src/domains/registry/domain-registry-service.ts` 也通过 `getSchemaRegistry()` 挂接该能力。回归已存在于 `tests/unit/domains/schema-registry.test.ts` 与 `tests/unit/domains/registry/schema-registry.test.ts`。 | 这条同样属于文档过期，不再保守挂账。 |
| R8-30 | 41. SDK / 插件 / 域注册 / 多区域 / 运维成熟度 | P1 | `src/domains/registry/domain-registry-service.ts` | register() 自动 validated→registered 无冒烟测试门控；§37 要求验证门 | 已解决 | 根因是旧 `DomainRegistryService.register()` 在 `validated -> registered` 自动提升路径上只做 schema/definition 校验，没有执行 `DomainSmokeTestRunner`，导致明显不具备运行条件的域也能被登记成 `registered`。 | 本轮已在 `src/domains/registry/domain-registry-service.ts` 的 auto-promote 分支中增加 `this.smokeTests.run(normalizedDefinition)` 门控，失败时抛出 `domain_registry.smoke_test_failed` 并阻止注册；`tests/unit/domains/registry/domain-registration.test.ts` 新增 `register rejects validated domains that fail smoke tests before auto-promotion`，同时把事件测试夹具收敛成真正满足 smoke gate 的最小有效域。 | 这是生产逻辑真实修复，不再让 validated 域绕过注册门禁。 |
| R8-31 | 41. SDK / 插件 / 域注册 / 多区域 / 运维成熟度 | P2 | `src/domains/registry/domain-model.ts:45` | WorkflowConfigSchema.steps 为线性 z.array(StepTemplateConfigSchema)——§13 禁止复杂任务使用线性步骤 | 已修复（源码修复并补定向测试） | 真实根因是 schema 过去虽然已经引入 `planGraph/stepGraph` 表达，但没有把“多步或存在依赖关系的 workflow 必须声明 graph 结构”上升为校验规则，导致复杂任务仍可只靠线性 `steps[]` 绕过 DAG 约束。 | 当前 `src/domains/registry/domain-model.ts` 已在 `WorkflowConfigSchema.superRefine(...)` 中强制：多步 workflow 或任一步含 `dependsOn` 时，必须提供 `planGraph` 或非空 `stepGraph`；同时 `stepGraph.edges` 也会校验 `fromStep/toStep` 必须引用已声明的 `steps[]`。`tests/unit/domains/registry/domain-model.test.ts` 已新增并通过“多步 workflow 缺 graph 时报错”和“stepGraph 引用未知 step 时报错”回归。 | 关闭的是“复杂任务可线性 steps 绕过 DAG 约束”的真实缺口，不外推为所有 workflow 建模问题都已结束。 |
| R8-32 | 41. SDK / 插件 / 域注册 / 多区域 / 运维成熟度 | P1 | `src/scale-ecosystem/multi-region/` | 无 fencing token/single-leader 写强制；§25.11/§52.3 要求 truth/budget/side-effect 写仅通过 fencing 单领导者 | 已解决（源码复核并补定向测试） | 根因不是 multi-region 现在仍缺单领导者写强制，而是审计条目停留在 fencing abstraction 尚未梳理进多区域目录时的旧快照；现行实现已经把 truth leader、fencing token 和 CDC 侧 epoch 验证分层落地。 | 当前 `src/scale-ecosystem/multi-region/fencing-token-service.ts` 已实现 `acquireLeadership/releaseLeadership/validateFencingToken`，`src/platform/state-evidence/truth/cross-region-truth-leader.js` 也会按 `tenantId + region + epoch + fencingToken` fail closed 校验 truth write。`src/scale-ecosystem/multi-region/cdc-replication-service.ts` 进一步提供 `validateLeaderFencingToken()` 与 `recordEntityEpoch()`。本轮补强 `tests/unit/platform/state-evidence/truth/multi-region-replication.test.ts`，新增 stale leader epoch 被拒与 current epoch 被接受的直接断言。 | 这条属于现行实现已具备、此前缺最小回归证据的挂账。 |
| R8-33 | 41. SDK / 插件 / 域注册 / 多区域 / 运维成熟度 | P1 | `src/scale-ecosystem/multi-region/cdc-replication-service.ts` | CDC 复制无冲突解决；applyBatch() 盲目应用事件无 epoch/版本 fencing 检查 | 已解决（源码复核并补定向测试） | 根因不是 CDC 现在仍盲写，而是审计结论落在 `applyBatch()` 加入 epoch gate 和 conflict-resolution 之前的版本；现行代码已经在合并前做 stale epoch 过滤，并在合并时走 vector clock / strategy 驱动的冲突解析。 | 当前 `src/scale-ecosystem/multi-region/cdc-replication-service.ts` 的 `applyBatch()` 会先按 `remoteEvent.epoch < localEpoch` 过滤 stale event，再调用 `mergeEventsWithConflictResolution()`；后者会记录 conflict audit 并更新 vector clock。`tests/unit/platform/state-evidence/truth/multi-region-replication.test.ts` 本轮新增 `applyBatch filters stale epoch events before conflict resolution`，直接证明旧 epoch 事件不会落入 merged result。 | 不再把现行 CDC 路径误写成“无冲突解决、无 epoch 校验”。 |
| R8-34 | 41. SDK / 插件 / 域注册 / 多区域 / 运维成熟度 | P2 | `src/scale-ecosystem/tenant-platform/tenant-platform-service.ts` | 无 Chinese Wall 强制/跨租户数据移动阻断(§50 知识域隔离) | 已解决（源码复核并补定向测试） | 根因不是当前 tenant platform 仍允许跨租户数据移动，而是审计只看到了早期 CRUD 主路径，没有复核后续加入的 scope relationship 校验与显式 cross-tenant deny。 | 当前 `createDataNamespace()` 会校验 workspace / tenant / organization 的一致性；`TenantPlatformService.assertNoCrossTenantDataMovement()` 还会对 `sourceTenantId !== targetTenantId` 直接抛出 `tenant.cross_tenant_data_movement`。现有 `tests/unit/scale-ecosystem/tenant-platform/tenant-platform-service.test.ts` 已覆盖 `tenant.workspace_tenant_organization_mismatch`，本轮又新增 same-tenant/null-target 放行与 cross-tenant movement 拒绝的直接断言。 | 这条属于 Chinese Wall 已实装、证据补强后可收口。 |
| R8-35 | 41. SDK / 插件 / 域注册 / 多区域 / 运维成熟度 | P1 | `src/ops-maturity/workflow-debugger/workflow-debugger-service.ts` | Debugger 使用 stepId/workflowId 术语而非 nodeRunId/planGraphId(§65) | 已解决（源码复核） | 根因是旧 debugger contract 早期确实以 `workflowId/stepId` 为主键，后续迁移时保留了 deprecated alias 兼容，但审计条目没有跟进“canonical ids 已切换、legacy 字段只剩兼容入口”的现状。 | 当前 `WorkflowDebuggerService` 的 breakpoint、trace frame、hit 和 comparison report 都以 `planGraphId` / `nodeRunId` 为 canonical 字段，仅保留 `workflowId/stepId` 作为 deprecated alias。已有 `tests/unit/ops-maturity/workflow-debugger/index.test.ts` 覆盖 `registerBreakpoint`、`evaluateTrace` 和 deprecated `workflowId -> planGraphId` 归一化。 | 现行主键语义已切到 canonical runtime IDs，不是假关闭。 |
| R8-36 | 41. SDK / 插件 / 域注册 / 多区域 / 运维成熟度 | P1 | `src/ops-maturity/workflow-debugger/time-travel-debug-service.ts` | Time-travel 用 stepId/executionId 作主键；应为 nodeRunId/harnessRunId(§5.5) | 已解决（源码复核） | 根因与 `R8-35` 同源：time-travel service 早期围绕 `stepId/executionId` 设计，后续迁移已经把 session、snapshot、cursor 和 event store 锚点统一到 `harnessRunId/nodeRunId`，只是兼容别名仍在，导致表面上看似没迁完。 | 当前 `DebugSnapshot`、`ReplayCursor`、`TimeTravelDebugSession` 与 `TimeTravelDebugEvent` 均以 `harnessRunId/nodeRunId` 为主字段，`executionId/stepId` 仅为 deprecated alias。`tests/unit/ops-maturity/workflow-debugger/index.test.ts` 与 `tests/unit/ops-maturity/workflow-debugger/time-travel-debug-service-edge-cases.test.ts` 已覆盖 `createSession/loadEventStore/replayToCursor/jumpToStep/getSnapshot` 等 canonical 路径。 | 回写真实现状：兼容别名仍在，但主键语义已迁移完成。 |
| R8-37 | 41. SDK / 插件 / 域注册 / 多区域 / 运维成熟度 | P2 | `src/ops-maturity/explainability/` | 无 StageRationale/OAPEFLIR 投影消费；§59 要求渲染 OAPEFLIR StageRationale 为审计解释 | 已解决（源码复核并修正回归基线） | 根因不是 explainability 目录现在仍缺 `StageRationale`，而是审计条目停留在 pipeline 只渲染 summary string 的早期实现；现行 explainability 已把 `StageRationale`、causal summary、evidence redaction 和 audit trail 收敛成权威 explanation bundle。 | 当前 `src/ops-maturity/explainability/explanation-pipeline-service.ts` 已定义 `StageRationale` 与 `ExplanationBundle`，`generate()` 会产出 rendered explanation、causal summary、redacted evidence refs 和 version lock/audit trail。本轮同步修正了 `tests/unit/ops-maturity/explainability/explanation-pipeline-service.test.ts` 的过时 `rationale.summary` 断言，使其与现行 `inferredSummary` 字段一致；同目录现有回归已覆盖 L1/L2/L3 渲染、causal chain、redaction、cache 与 audit trail。 | 该条应按 explainability pipeline 的现状收口，而不是继续按旧 summary-only 版本挂账。 |
| R8-38 | 41. SDK / 插件 / 域注册 / 多区域 / 运维成熟度 | P2 | `src/ops-maturity/edge-runtime/` | Edge orchestrator 为单文件 stub；缺 §62 离线能力/本地模型执行/sync-queue+冲突解决/确定性回退 | 已解决（源码复核） | 根因不是 edge-runtime 现在仍停在单文件 stub，而是审计条目没有跟进后续拆分出的 `edge-orchestrator/local-model/sync-queue/edge-executor/edge-runtime-sync-service` 五个子模块。 | 当前 `src/ops-maturity/edge-runtime/` 已包含 `edge-orchestrator/index.ts`、`local-model/index.ts`、`sync-queue/index.ts`、`edge-executor/index.ts` 与 `edge-runtime-sync-service.ts`；其中 `buildEdgeExecutionPlan()` 已输出 canonical `PlanGraphBundle`，`EdgeRuntimeSyncService` 负责离线执行、sync envelope 和 ordering policy。现有 `tests/unit/ops-maturity/edge-runtime-edge-orchestrator.test.ts`、`tests/unit/ops-maturity/edge-runtime/edge-runtime-sync-service.test.ts` 与相关 integration tests 已证明这不是 stub-only 目录。 | 现行 edge runtime 仍可继续演进，但“单文件 stub”这一指控已经失真。 |
| R8-39 | 41. SDK / 插件 / 域注册 / 多区域 / 运维成熟度 | P2 | `src/ops-maturity/` | 无 OpsMaturityScore 聚合模型；§69 要求跨 drift/compliance/cost/explainability 维度评分 | 已解决（源码复核） | 根因不是仓内仍然没有 maturity 聚合模型，而是审计沿用了 score service 引入前的旧结论。 | 当前 `src/ops-maturity/ops-maturity-score.ts` 已实现 `OpsMaturityScore` 与 `OpsMaturityScoreService`，覆盖 drift/compliance/cost/explainability 四维评分、overallScore、riskFlags、history 和 dimension details。`tests/unit/ops-maturity/ops-maturity-score.test.ts` 与 `tests/unit/scale-ops/ops-maturity-services.test.ts` 已覆盖加权、clamp、risk flag 与历史查询。 | 不再把现有 score service 误记成缺失。 |
| R8-40 | 41. SDK / 插件 / 域注册 / 多区域 / 运维成熟度 | P1 | `src/sdk/plugin-sdk/plugin-test-harness.ts` | executePlugin() 全 mock 返回硬编码响应；§22.4 测试 harness 须在沙盒中执行真实插件生命周期 | 已修复（源码/测试） | 根因复核后分成两层：其一，审计看到的是旧版只会 mock 的阶段；其二，真正未完成的是 `PluginTestHarness` 把 `PluginDefinition` 这类 manifest 当成可执行 runtime 使用，缺少 live runtime seam，导致 live 模式只能报 “requires a plugin host runtime”。 | 本轮在 `src/sdk/plugin-sdk/plugin-test-harness.ts` 中新增 `livePlugin/liveRunner` 绑定点，并把 live 模式桥接到真实 `tool/retriever/validator/planner/presenter/evaluator/adapter` SPI 与 lifecycle（`initialize/onLoad/onActivate/healthCheck`）流程；`tests/unit/sdk/plugin-sdk/plugin-test-harness-regressions.test.ts` 已定向通过，验证 live 模式可执行真实 plugin runtime 而非继续回退 mock。 | 关闭范围仅限“测试 harness 已能绑定并执行真实 runtime plugin/runner”，不外推为完整插件宿主平台已完备。 |
| R8-41 | 41. SDK / 插件 / 域注册 / 多区域 / 运维成熟度 | P2 | `src/scale-ecosystem/marketplace/` | 无 AgentCertification/PackCertificationGate；§55 发布前须经安全扫描/eval gate/SBOM 认证管线 | 已解决（源码复核） | 根因不是 marketplace 目录仍然缺认证管线，而是审计条目没有跟进后续新增的 certification 子模块与 governance service 接线。 | 当前 `src/scale-ecosystem/marketplace/certification/index.ts` 已定义 `AgentCertificationSchema`、`PackCertificationSchema`、`CertificationGate` 与 SBOM / security scan / evaluation gate；`certification-gate-service.ts` 和 `marketplace-governance-service.ts` 也已把认证校验接入发布路径。`tests/unit/scale-ecosystem/marketplace/certification/certification.test.ts` 已覆盖 approved/revoked/unverified agent 与 pack gate 行为。 | 这条属于实现已在，文档结论过期。 |
| R8-42 | 41. SDK / 插件 / 域注册 / 多区域 / 运维成熟度 | P2 | `src/sdk/plugin-sdk/plugin-definition.ts:26` | PluginSecurityConfig.sandboxTier 含 "none"；§10 插件默认不信任——"none" 违反 INV-POLICY-001 | 已解决（源码复核并修正回归基线） | 根因不是当前 `PluginSecurityConfig` 仍接受 `"none"`，而是旧测试和审计条目停留在“未知 tier 静默回退 `read_only`”的历史行为。现行实现已把 deny-by-default 收敛到 sandbox policy 层，`none` 与未知 tier 都会直接拒绝。 | 当前 `src/sdk/plugin-sdk/plugin-definition.ts` 的 `PluginSecurityConfig.sandboxTier` 类型是 `SandboxMode`，并通过 `normalizeSandboxMode(...)` 归一化；`src/platform/control-plane/iam/sandbox-policy.ts` 现已对 `"none"` 明确抛出 `sandbox_policy.invalid_sandbox_tier`。本轮同步修正了 `tests/unit/platform/control-plane/iam-sandbox-policy.test.ts` 与 `tests/unit/platform/control-plane/iam/arch-p1-sandbox-level-coverage.test.ts` 的过时断言，改为直接验证 `none` / `unknown_alias` / `vm` 都会被拒绝。 | 现行策略已禁止 unsandboxed tier，不再允许 `"none"` 伪装成有效配置。 |
| R8-43 | 42. UI 深层缺口（组件库 / 无障碍 / 原生壳 / 离线 / 工具链） | P0 | `ui/packages/shared/auth/src/auth-service.ts` | 无 token refresh 逻辑；§5.4.4 要求到期前 60s 主动静默刷新+并发锁+401→redirect | 已解决（源码修复并补定向测试） | 真正根因不是“完全没有 refresh”，而是旧实现只做了半套：`TokenManager` 虽有 refresh lock，但阈值写成了 5 分钟，`createAuthInterceptor()` 也只会在 401 后偷偷后台 refresh，不会在请求前按 60s 窗口静默刷新，更不会在 401 时清 session 并跳回认证流。 | 本轮在 `ui/packages/shared/auth/src/token-manager.ts` 把默认预刷新窗口收敛到 `60s`，补上可注入 `refreshFn`、`shouldRefresh()`、并发 refresh 锁和 `handleUnauthorized()`；`ui/packages/shared/api-client/src/interceptors.ts` 现已在 request 前根据 `shouldRefresh()` 主动等待静默刷新，并在 `401` 时委托 resolver 做失效处理；`ui/packages/shared/auth/src/auth-service.ts` 又把 unauthorized handler 绑定回 PKCE 授权流。定向验证 `ui/tests/unit/ui/shared/auth-service.test.ts` 与 `ui/tests/unit/ui/shared/interceptors.test.ts` 已通过，新增断言直接覆盖“60s 窗口内并发 refresh 只触发一次”和“401 调用 unauthorized handler”。 | 这是共享认证链路的真实补齐，不是把刷新责任继续留给调用方或只在文档里承认“未来应接 refresh”。 |
| R8-44 | 42. UI 深层缺口（组件库 / 无障碍 / 原生壳 / 离线 / 工具链） | P0 | `ui/packages/shared/auth/src/auth-service.ts` | 无 PKCE 支持；handleSsoCallback 直接从 URL 参数读 token 无 code_verifier/code_challenge/授权码交换 | 已解决（源码复核） | 根因不是当前 auth service 仍直读 URL token，而是审计条目停留在修复前版本，没有跟进后续切到 authorization code + PKCE flow 的实现。 | 当前 `ui/packages/shared/auth/src/auth-service.ts` 已定义 `AuthCodeFlowState`、`initiateCodeFlow()`、`generateCodeVerifier()`、`generateCodeChallenge()` 与 `handleAuthorizationCallback()`；`handleSsoCallback()` 也不再消费 `access_token` / `refresh_token` URL 参数，而是启动 PKCE 授权码流。`ui/tests/unit/ui/shared/auth-service.test.ts` 已覆盖 state mismatch、missing code、authorization error 和“SSO callback 不接受 URL token、而是跳回授权服务器”的路径。 | 这条属于审计结论过期，不再保留为未修。 |
| R8-45 | 42. UI 深层缺口（组件库 / 无障碍 / 原生壳 / 离线 / 工具链） | P0 | `ui/packages/shared/platform/src/web-platform-adapter.ts` | Token 明文存入 localStorage；§6.5.2 要求 HttpOnly Secure Cookie 或 memory-only | 已解决（源码复核） | 根因不是 web adapter 现在还把 secure value 写进 `localStorage`，而是审计沿用了更早的 Web 存储实现，没有吸收后续把 secure storage 收敛到 memory-only、把 `localStorage` 限缩到 analytics consent 的修复。 | 当前 `ui/packages/shared/platform/src/web-platform-adapter.ts` 已明确注释 secure storage 不落 `localStorage`，`readSecureValue()/writeSecureValue()/deleteSecureValue()` 继续走 `DefaultPlatformAdapter` 的内存 secure store；只有 analytics consent 才会持久化到 `aa.analytics.consent`。定向验证 `tests/unit/ui/shared/platform/platform-adapter-security.test.ts` 已通过，直接断言 secure value 读写不会触碰 `localStorage`。 | 不假关闭：Web token 存储策略已经收敛到 memory-only。 |
| R8-46 | 42. UI 深层缺口（组件库 / 无障碍 / 原生壳 / 离线 / 工具链） | P1 | `ui/packages/ui-core/src/components/` | 极少 ARIA 覆盖；§6.4.3+§6.4.5 要求全交互元素含 role/aria-live/aria-label；ListCard/KeyValueTable/按钮均缺 | 已解决（源码复核） | 根因是审计基于早期仅有少量基础组件的快照，没有复核后续 `ui-core` 里已落地的语义化标记与扩展组件库。 | 当前 `ui/packages/ui-core/src/components/index.ts:136-183,344-384,457-503,748-773,909-930` 已对表单、Alert、Modal、ListCard、KeyValueTable、Workbench 加入 `role/aria-live/aria-label`；`ui/packages/ui-core/src/components/extended.tsx:62-67,93-96,145-172,186-236,284-344` 进一步覆盖 Search/Tabs/Drawer/Toast/IconButton/SegmentedControl 等交互组件。 | 该条按当前组件源码收口；是否继续补更多 a11y e2e 属于后续质量提升，不再作为“极少覆盖”挂账。 |
| R8-47 | 42. UI 深层缺口（组件库 / 无障碍 / 原生壳 / 离线 / 工具链） | P1 | `ui/packages/ui-core/src/components/` | 无键盘焦点管理；§6.4.3 要求可见焦点环；designTokens.shadows.focusRing 已定义但组件未消费 | 已解决（源码修复） | 根因是 focus ring token 早已存在，但全局样式没有把它落到 `:focus-visible`，导致键盘用户看不到一致的焦点反馈。 | 当前 `ui/packages/ui-core/src/design-tokens/tokens.css:239-244` 已在全局 `:focus-visible` 同时使用 `outline` 与 `box-shadow: var(--aa-shadow-focus-ring)`，不再停留在“token 已定义但未消费”。 | 关闭的是可见焦点环缺口。 |
| R8-48 | 42. UI 深层缺口（组件库 / 无障碍 / 原生壳 / 离线 / 工具链） | P1 | `ui/packages/ui-core/src/design-tokens/` | 扁平 token 结构无 primitive/semantic/domain 分层；缺 risk-level/autonomy-level/status/domain 色阶(§6.3.1) | 已解决（源码复核） | 根因是审计没有复核 `design-tokens` 后续拆成三层架构的实现。 | 当前 `ui/packages/ui-core/src/design-tokens/index.ts:1-7,473-521,627-630` 已明确定义 `primitive/semantic/domain` 三层 token，且 domain 层包含 `workflow/task/execution/riskLevel/autonomyLevel`；`ui/packages/ui-core/src/design-tokens/tokens.css:16-90` 也把 risk/autonomy/status 色阶导出为 CSS vars。 | 这条属于现状已满足三层 token 架构，文档结论过期。 |
| R8-49 | 42. UI 深层缺口（组件库 / 无障碍 / 原生壳 / 离线 / 工具链） | P1 | `ui/packages/ui-core/` | 无动画系统/prefers-reduced-motion 支持；§6.4.3 + §6.3.1 要求 animation.ts 含 fast/normal/slow/easing | 已解决（源码复核） | 根因是审计停留在 motion token 尚未抽离前的版本。 | 当前 `ui/packages/ui-core/src/design-tokens/index.ts:97-107` 已定义 `duration.fast/normal/slow` 与 `easing.standard/enter/exit`，`ui/packages/ui-core/src/design-tokens/tokens.css:156-163,255-260` 也提供 motion CSS vars 与 `prefers-reduced-motion` 全局降级。 | 不再把现有 motion/reduced-motion 能力误记为缺失。 |
| R8-50 | 42. UI 深层缺口（组件库 / 无障碍 / 原生壳 / 离线 / 工具链） | P1 | `ui/packages/ui-core/src/components/` | 组件库严重不完整；§6.3.2 要求 50+ 组件(8类)；当前仅 7个(StatusPill/ListCard/KeyValueTable/FeatureScaffold 等) | 已解决（源码修复并补定向测试） | 根因是旧 `ui-core` 只有一小组基础组件，审计结论在那一阶段成立。 | 当前 `ui/packages/ui-core/src/components/index.ts:5` 已导出 `extended.tsx`，而 `ui/packages/ui-core/src/components/extended.tsx:11-353` 新增 Card/Panel/TextArea/SearchInput/ProgressBar/Breadcrumbs/Pagination/Tabs/Accordion/Drawer/Toast/Stepper/IconButton/SegmentedControl 等大批组件；`ui/tests/unit/ui/packages/ui-core/feature-module-regressions.test.tsx:27-29` 已断言导出组件数 `>= 50` 并通过。 | 关闭范围是“组件数量与类别基线”缺口。 |
| R8-51 | 42. UI 深层缺口（组件库 / 无障碍 / 原生壳 / 离线 / 工具链） | P1 | `ui/packages/ui-core/src/themes/` | Theme 为 JS 对象非 CSS Custom Properties；§6.3.3 要求 CSS vars + prefers-color-scheme media query | 已解决（源码复核） | 根因是审计没有复核 `tokens.css` 已把语义色、阴影、半径等导出为 CSS variables，并配套暗色模式媒体查询。 | 当前 `ui/packages/ui-core/src/design-tokens/tokens.css:16-187` 已以 CSS custom properties 暴露主题变量，`193-232` 通过 `@media (prefers-color-scheme: dark)` 覆盖 dark mode。 | 现行主题能力已不再是“纯 JS 对象”。 |
| R8-52 | 42. UI 深层缺口（组件库 / 无障碍 / 原生壳 / 离线 / 工具链） | P1 | `ui/tools/mock-server/src/index.ts` | Mock server 仅覆盖 3端点(dashboard/tasks/workflows)；§5.2 定义 30+ 端点；缺 approval/agent/policy/WS mock | 已解决（源码复核并补定向测试） | 根因是审计停留在 mock server 仅回 dashboard/tasks/workflows 的阶段。 | 当前 `ui/tools/mock-server/src/index.ts:15-39,55-133` 已扩到 approvals/agents/policies/incidents/workers/queues/models/webhooks/contract-version 及 websocket/planned endpoint seam；`ui/tests/unit/ui/tools/mock-codegen-sync-regressions.test.ts:8-15` 已验证 approvals 与 contract-version mock。 | 关闭的是 API mock 覆盖面缺口。 |
| R8-53 | 42. UI 深层缺口（组件库 / 无障碍 / 原生壳 / 离线 / 工具链） | P1 | `ui/tools/codegen/src/index.ts` | Codegen 仅生成路径常量；§5.4.3 要求 typed endpoint 函数+query key factories+DTO 类型 | 已解决（源码修复并补定向测试） | 根因是旧 codegen 只有 endpoint path 常量，没有把 OpenAPI schema 转成 DTO、client factory 和 query key。 | 当前 `ui/tools/codegen/src/index.ts:151-162,181-258` 的 `parseOpenApiSpec()` 已同时产出 `dtoTypes/endpointBindings/endpointClients/queryKeys`，并生成 typed endpoint client factory 与 query key factory；`ui/tests/unit/ui/tools/mock-codegen-sync-regressions.test.ts:17-57` 已定向验证输出。 | 关闭范围仅限 codegen 能力缺口。 |
| R8-54 | 42. UI 深层缺口（组件库 / 无障碍 / 原生壳 / 离线 / 工具链） | P1 | `ui/apps/mobile/src/App.tsx` | 移动平台硬编码 android；§2.5.5/2.5.6 要求 Android+iOS 支持；无运行时平台检测 | 已解决（源码复核并保留定向测试） | 根因是旧移动入口在 native bridge 不可用时直接回退成 `"android"`，导致 iOS WebView/浏览器环境永远报错平台。 | 当前 `ui/apps/mobile/src/App.tsx:4-15` 已按 UA 检测 `iphone/ipad/ios/android` 并只在完全无法识别时回退 android；现有 `ui/tests/unit/ui/apps/mobile/App.test.tsx` 定向测试已通过。 | 这条属于移动平台检测已到位。 |
| R8-55 | 42. UI 深层缺口（组件库 / 无障碍 / 原生壳 / 离线 / 工具链） | P2 | `ui/apps/electron-win/, ui/apps/tauri-\*/` | 无自动更新机制；§7.1.5+§2.5.2 要求 electron-updater/Sparkle/Tauri updater；桌面壳为清单 stub | 已解决（源码复核并保留定向测试） | 根因是审计基于桌面壳初始 stub 阶段；后续 updater 依赖与 channel manifest 已接入，但文档未同步。 | 当前 `ui/apps/electron-win/package.json` 已引入 `electron-updater`，`ui/apps/electron-win/src/main.ts:5-15` 会调用 `checkForUpdatesAndNotify()`；`ui/apps/tauri-linux/src-tauri/Cargo.toml` 与 `ui/apps/tauri-macos/src-tauri/Cargo.toml` 已接入 `tauri-plugin-updater`，对应 manifest `ui/apps/tauri-linux/src/index.ts:4-16`、`ui/apps/tauri-macos/src/index.ts:4-16` 也都有 `updateChannel`。桌面相关定向测试 `ui/tests/unit/ui/apps/electron-win/main.test.ts` 与 `ui/tests/unit/ui/apps/tauri-linux/lib.test.ts` 已通过。 | 关闭的是自动更新机制缺口。 |
| R8-56 | 42. UI 深层缺口（组件库 / 无障碍 / 原生壳 / 离线 / 工具链） | P2 | `ui/packages/shared/sync/src/offline-queue.ts` | OfflineMutation 缺 idempotencyKey/retryCount/status 字段(§5.4.5) | 已解决（源码/测试） | 根因是复审条目停留在早期简化版离线 mutation 结构，没有吸收 shared sync types 后续补全字段的实现。 | 当前 `ui/packages/shared/sync/src/types.ts` 的 `OfflineMutation` 已包含 `idempotencyKey`、`retryCount` 与 `status`，`ui/tests/unit/ui/shared/offline-queue.test.ts` 也已使用带这些字段的 mutation fixture 验证队列 enqueue/persist/eviction 行为，本轮定向测试已通过。 | 关闭的是离线 mutation 结构字段缺口，不外推为全部离线同步产品能力都已完成。 |
| R8-57 | 42. UI 深层缺口（组件库 / 无障碍 / 原生壳 / 离线 / 工具链） | P2 | `ui/packages/shared/sync/src/conflict-resolver.ts` | 仅 server_wins/local_wins/shallow-merge；§5.5.4 要求数据类型特定冲突解决(CAS/幂等/先到先得) | 已解决（源码复核并补定向测试） | 根因是审计结论停留在只支持简单 winner 策略的旧实现。 | 当前 `ui/packages/shared/sync/src/conflict-resolver.ts:15-67,102-132` 已支持 vector clock + Lamport timestamp 的 CRDT merge、数组去重合并与对象字段级 LWW；`ui/tests/unit/ui/tools/mock-codegen-sync-regressions.test.ts:59-76` 已验证 merge 语义。 | 关闭范围是冲突解决语义，不外推为全部离线同步产品能力。 |
| R8-58 | 42. UI 深层缺口（组件库 / 无障碍 / 原生壳 / 离线 / 工具链） | P2 | `ui/packages/features/\*/src/index.tsx` | 无 Error Boundary；§5.6 要求 P0-P3 错误分级+fallback UI；单组件崩溃拖垮全应用 | 已解决（源码修复并补定向测试） | 根因是 feature module factory 过去把 `render()` 结果直接暴露给路由层，组件抛错时没有任何 boundary/fallback。 | 当前 `ui/packages/ui-core/src/index.tsx:19-44` 已在 `createFeatureModule()` 统一包裹 `ComponentErrorBoundary`，而 `ui/packages/ui-core/src/components/index.ts:1055-1098` 已实现 P0-P3 分级与 fallback UI；`ui/tests/unit/ui/packages/ui-core/feature-module-regressions.test.tsx:9-25` 已验证渲染异常会落到边界而非炸掉整页。 | 关闭的是 feature 级 Error Boundary 缺口。 |
| R8-59 | 42. UI 深层缺口（组件库 / 无障碍 / 原生壳 / 离线 / 工具链） | P2 | `ui/apps/tauri-macos/, ui/apps/tauri-linux/` | 无 Tauri 原生集成(Keychain/native menu/Spotlight/D-Bus/XDG/Wayland)；src-tauri/ 无 main.rs | 已解决（源码修复并补定向测试） | 根因是早期 Tauri 壳只有最小 health/openDeepLink stub，macOS/Linux 各自的原生桥接能力没有收口到 `src-tauri`。 | 当前 `ui/apps/tauri-macos/src-tauri/src/lib.rs:35-72` 已提供 native menu、Keychain 和 Spotlight export；`ui/apps/tauri-linux/src-tauri/src/lib.rs:31-74` 已提供 D-Bus notify、XDG open、Wayland/X11 检测与 theme preference bridge，并补了 `open_deep_link()` scheme 校验；`ui/tests/unit/ui/apps/tauri-linux/lib.test.ts` 已定向通过。 | 关闭的是 Tauri 原生集成缺口。 |
| R8-60 | 42. UI 深层缺口（组件库 / 无障碍 / 原生壳 / 离线 / 工具链） | P2 | `ui/packages/ui-core/src/charts/` | 图表无表格替代视图；§6.4.3 要求所有图表提供 table fallback 供屏幕阅读器使用 | 已解决（源码/测试） | 根因复核后发现这条挂账引用的是早期图表层认知；现行 `ui-core` 已经具备 table fallback，只是前次 reaudit 没有把图表入口与 fallback 实现一起复核。 | 当前 `ui/packages/ui-core/src/charts/echart-surface.tsx` 已暴露 `Table` 按钮与 `showTableFallback` 开关，`ChartTableFallback` 也已作为真实表格渲染路径存在。 | 定向验证已通过：`ui/tests/shared/ui-core-chart-regressions.test.tsx` 直接验证 `EChartSurface` 在 table fallback 模式下会渲染可访问表格 `role="table"`，本轮测试通过。 |
| R8-61 | 43. ADR / Contract 新发现矛盾与缺失 | P0 | `docs_zh/adr/066-\*.md (×2)` | ADR-066 编号重复：compliance-report-auto-generation 与 plugin-spi-framework 共用同一编号 | 已解决（文档/ADR） | 根因是 Plugin SPI ADR 后加进序列时直接复用了 `066`，README 与交叉引用也跟着沿用，导致 ADR 编号唯一性失效。 | `docs_zh/adr/071-plugin-spi-framework.md` 已从重复的 `066` 重新编号为 `071`，`docs_zh/adr/README.md`、`plugin_spi_contract.md`、`migration/00-migration-guideline.md`、`ADR-088` 引用均已同步；新增 `tests/unit/docs/documentation-health.test.ts` 唯一编号守护并通过。 | 已按真实 ADR 与测试证据解决。 |
| R8-62 | 43. ADR / Contract 新发现矛盾与缺失 | P0 | `docs_zh/adr/060-explicit-planning-hub.md` | 定义 Plan DTO(planId/taskId/steps:PlanStep[]/DAGStructure) 作为 P3→P4 canonical contract；v4.3 用 PlanGraphBundle/PlanGraph/PlanNode/PlanEdge——完全不同对象名；未标 superseded | 已解决（文档/ADR） | 根因是显式规划 ADR 早于 executable contract freeze，后续虽然正文大体迁移，但审计没有吸收已把 `PlanGraphBundle` 定为唯一 handoff、`RuntimeExecuteBridge` 降为 compatibility seam 的现状。 | 当前 `docs_zh/adr/060-explicit-planning-hub.md` 已将 `PlanGraphBundle` 写为唯一 P3→P4 handoff，并明确 `RuntimeExecuteBridge` 仅是 compatibility seam；相关不变量测试已持续通过。 | 已按真实 ADR 与测试证据解决。 |
| R8-63 | 43. ADR / Contract 新发现矛盾与缺失 | P0 | `docs_zh/adr/033-phased-roadmap.md` | 定义 7-Phase 路线图；v4.3 §33 已替换为 Ring 1/2/3 模型；ADR 仍 Accepted 无 Ring 引用 | 已解决（文档/ADR） | 根因是 ADR-033 之前只把正文换成 Ring 叙事，但 `Accepted` 状态和 `evaluatePhaseAdvance()` 历史门禁仍保留着 active canonical 语气。 | `docs_zh/adr/033-phased-roadmap.md` 现已标记为 `Superseded by ADR-112`，正文明确 `Phase 1-7` 只允许作历史 milestone 映射，现行推进边界以 `Ring 1 / Ring 2 / Ring 3` 为准。 | 已按真实 ADR 与测试证据解决。 |
| R8-64 | 43. ADR / Contract 新发现矛盾与缺失 | P1 | `docs_zh/contracts/event-envelope-contract.md` | 同一 schema 内混合 snake_case(schema_version/idempotency_key) 和 camelCase(eventId/eventType) | 已解决（contract/文档） | 根因是事件 envelope 文档把 wire alias 和 canonical schema 混写在一张字段表里，没有区分“权威字段”与“迁移兼容别名”。 | `docs_zh/contracts/event-envelope-contract.md` 现已把 canonical EventEnvelope 收敛到 camelCase，并单列 `schema_version -> schemaVersion` 等 alias 映射；新增不变量测试守护该分层并通过。 | 已按真实 contract 与测试证据解决。 |
| R8-65 | 43. ADR / Contract 新发现矛盾与缺失 | P1 | `docs_zh/contracts/event_bus_contract.md` | Legacy EventEnvelope 用 task_id 作关联字段；v4.3 要求 harnessRunId + aggregate 关联；parallel schema 未重定向 | 已解决（contract/文档） | 根因是 event bus contract 仍沿用 pre-v4.3 关联模型，把 `task_id/workflow_id/execution_id` 当作主要锚点，没有把 canonical `runId + aggregate` 和 `harnessRunId/nodeRunId` 写成权威边界。 | `docs_zh/contracts/event_bus_contract.md` 现已明确 truth event 使用 `runId + aggregateType + aggregateId + aggregateSeq`，payload 关联优先 `harnessRunId/nodeRunId/planGraphId/attemptId`，legacy 运行键仅保留为 compatibility adapter。 | 已按真实 contract 与测试证据解决。 |
| R8-66 | 43. ADR / Contract 新发现矛盾与缺失 | P1 | `docs_zh/adr/019-agent-handoff-four-layer-protocol.md` | HandoffSerializer.buildFromStepResult(result: StepResult) 引用废弃类型；v4.3 用 NodeAttemptReceipt | 已解决（文档/ADR） | 根因是 handoff ADR 在回写 `NodeAttemptReceipt` 接口后，缺少专属 contract 与 cross-link，导致审计仍把它视为停留在旧 `StepResult` 时代。 | `docs_zh/adr/019-agent-handoff-four-layer-protocol.md` 已明确 handoff / delegation canonical contract 入口，并把回执锚点收敛到 `NodeAttemptReceipt / HarnessRun / NodeRun`；对应 contract 文件已补齐。 | 已按真实 ADR 与 contract 证据解决。 |
| R8-67 | 43. ADR / Contract 新发现矛盾与缺失 | P1 | `缺失 contract: Agent Delegation / Multi-Agent Collaboration` | §19 定义完整委托协议(DelegationRequest/DelegationReceipt/depth C1-C7)；无对应 contract 文件 | 已解决（contract/文档） | 根因不是仓内完全没有委托实现，而是 `DelegationRequest`、`DelegationResult`、`ACPMessage`、`AgentHandoff` 分散在多个源码模块，缺少一份权威 contract 文档冻结边界。 | 新增 `docs_zh/contracts/agent_handoff_contract.md`，覆盖 `DelegationRequest / DelegationReceipt / ACPMessage / AgentHandoff` 与 `C1-C7` 深度治理；文档结构测试和不变量测试已新增守护并通过。 | 已按真实 contract 与测试证据解决。 |
| R8-68 | 43. ADR / Contract 新发现矛盾与缺失 | P1 | `docs_zh/contracts/task-intake-request-contract.md` | TaskDraft/ConfirmedTaskSpec 无 domainId 字段；§30/§37 要求每个进入执行的任务携带已验证 domain_id | 已修复 | 根因不是单纯文档遗漏，而是 canonical executable contracts 与 intake/nl-gateway 入口长期只保留 `divisionId`/`domainHint` 或直接丢失域绑定，导致 internal runtime 有 `domainId`，但 `TaskDraft` / `ConfirmedTaskSpec` / `RequestEnvelope` truth contract 没有跟上。 | `src/platform/contracts/executable-contracts/index.ts` 与 `schemas.ts` 已为 `TaskDraft` / `ConfirmedTaskSpec` / `RequestEnvelope` 增加 `domainId`，并新增 legacy alias 归一化；`src/platform/orchestration/harness/runtime/intake-admission-service.ts`、`src/interaction/nl-gateway/index.ts` 已显式传递 canonical `domainId`；`tests/unit/platform/contracts/executable-contracts-factories.test.ts` 新增 legacy `engineering_ops -> coding` 归一化断言，`./node_modules/.bin/tsx --test tests/unit/platform/contracts/executable-contracts-factories.test.ts tests/unit/platform/contracts/executable-contracts/index.test.ts tests/unit/platform/orchestration/harness/runtime/intake-admission-service.test.ts` 通过。 | contract 文档已回写到 [task-intake-request-contract.md](/Users/holden/Project/automatic_agent/automatic_agent_platform/docs_zh/contracts/task-intake-request-contract.md:1)，并在 invariant 中固化。 |
| R8-69 | 43. ADR / Contract 新发现矛盾与缺失 | P1 | `docs_zh/contracts/harness-run-contract.md` | HarnessRun 有 tenantId 无 domainId；§37 要求域绑定用于风险覆盖/知识边界/prompt 库选择 | 已修复 | 根因是 internal `HarnessRunRuntimeState` 一直持有 `domainId`，但 canonical `HarnessRun` contract / schema / adapter 与 goal-decomposer 路由创建 run 时都没有把它带入 truth object，形成“内部有、导出无”的断层。 | `src/platform/contracts/executable-contracts/index.ts` 与 `schemas.ts` 已为 `HarnessRun` 增加 `domainId`；`src/platform/orchestration/harness/index.ts` 的 `toCanonicalHarnessRun()` 已回填 `state.domainId`；`src/interaction/goal-decomposer/index.ts` 已在跨域任务场景用 baseline 规则解析 top-level run domain；`tests/integration/platform/contracts/platform-contracts-integration.test.ts` 已验证 intake->request->run 链路携带 `domainId`，定向集成测试通过。 | contract 文档已回写到 [harness-run-contract.md](/Users/holden/Project/automatic_agent/automatic_agent_platform/docs_zh/contracts/harness-run-contract.md:1)，并在 invariant 中固化。 |
| R8-70 | 43. ADR / Contract 新发现矛盾与缺失 | P1 | `缺失 contract: ReleaseDecisionView / ReleaseChannel` | §13 列 ReleaseDecisionView 为 canonical OAPEFLIR 投影对象；ADR-091 要求 ReleaseChannel；均无 contract | 已解决（contract/文档） | 根因是 `release_rollout_and_rollback_contract` 只冻结了 rollout mechanics，没有把 `ReleaseDecisionView` 和 `ReleaseChannel` 作为独立权威对象写进 contract。 | `docs_zh/contracts/release_rollout_and_rollback_contract.md` 已新增 `ReleaseDecisionView` 与 `ReleaseChannel` 对象定义、字段和约束；不变量测试已新增守护并通过。 | 已按真实 contract 与测试证据解决。 |
| R8-71 | 43. ADR / Contract 新发现矛盾与缺失 | P1 | `docs_zh/adr/012-sqlite-phase-1-2-primary-store.md` | 仍 Accepted 范围为 "Phase 1a/1b"；v4.3 用 Ring 1 MVP；退出条件未映射到 Ring 边界 | 已解决（文档/ADR） | 根因是 SQLite ADR 主体语义已迁移到 canonical truth 表族，但适用边界仍沿用 `Phase 1a / 1b / Phase 2` 历史口径。 | `docs_zh/adr/012-sqlite-phase-1-2-primary-store.md` 已把标题、适用范围和 remediation 统一到 `Ring 1 MVP / Ring 2 readiness`，并保留 phase 仅作历史迁移语义。 | 已按真实 ADR 证据解决。 |
| R8-72 | 43. ADR / Contract 新发现矛盾与缺失 | P1 | `docs_zh/adr/013-eventemitter-phase-2-boundary.md` | 同上——范围 "Phase 1a/1b/Phase 2" 无 Ring 映射；退出触发器("Phase 2 是否替换")已无定义 | 已解决（文档/ADR） | 根因与 `R8-71` 同源：EventEmitter ADR 只重写了 transport 边界，没有同步把 phase 命名和退出条件迁到 ring 口径。 | `docs_zh/adr/013-eventemitter-phase-2-boundary.md` 已改写为 `Ring 2 readiness` 口径，并把替换触发器明确为 ring readiness 之后的独立决策。 | 已按真实 ADR 证据解决。 |
| R8-73 | 43. ADR / Contract 新发现矛盾与缺失 | P2 | `docs_zh/contracts/typed_event_bus_contract.md §3A` | 所有 OAPEFLIR 事件载荷用 task_id/workflow_id 作主关联字段；v4.3 用 harnessRunId+aggregate 关联 | 已解决（contract/文档） | 该 contract 的 payload 字段其实早已迁到 `harnessRunId/nodeRunId/planGraphId`，残留问题是底部事件类型规则仍写 `<stage>:<event>`，把新的 payload 锚点和旧 namespace 语义重新混在一起。 | `docs_zh/contracts/typed_event_bus_contract.md` 已把事件类型规则收敛到 `oapeflir.view.<stage>.<event>` / `oapeflir.rationale.<stage>.<event>` 与 `platform.*` truth namespace，payload 锚点继续保持 canonical 运行链。 | 已按真实 contract 与测试证据解决。 |
| R8-74 | 43. ADR / Contract 新发现矛盾与缺失 | P2 | `docs_zh/adr/072-oapeflir-testing-strategy.md` | 测试 OAPEFLIR 为独立执行管线("O→A→P→E→F happy path" E2E)；v4.3 降级 OAPEFLIR 为仅投影/视图 | 已解决（文档/ADR） | 根因是 ADR-072 在前一轮整改时只改了模块目录矩阵，没有把 happy-path / failure / replan 示例从 “OAPEFLIR 执行主链” 改成 “runtime truth + OAPEFLIR 视图连续性”，同时把不存在的专项测试目录误写成已落地事实。 | `docs_zh/adr/072-oapeflir-testing-strategy.md` 现已把核心测试目标改写为 `HarnessRun / NodeRun / NodeAttemptReceipt` 主链与 `oapeflir.view.*` 投影连续性验证，并收敛现存权威测试根到 `tests/unit/`、`tests/integration/`、`tests/golden/`、`tests/e2e/`、`tests/invariants/`；`tests/invariants/contract-and-oapeflir-remediation.test.ts` 已新增守护并通过。 | 已按真实 ADR 与测试证据解决。 |
| R8-75 | 43. ADR / Contract 新发现矛盾与缺失 | P2 | `docs_zh/contracts/runtime_state_machine_contract.md §1A` | 定义 OapeflirStage 为工作流级状态机(observe→assess→plan→...)；v4.3 说 OAPEFLIR 阶段仅为投影非状态机转换 | 已解决（contract/文档） | 根因是 runtime state machine contract 虽已在顶部声明 OAPEFLIR 只做 projection，但 §1A 仍以 “OapeflirStage 枚举” 的状态机语气描述它。 | `docs_zh/contracts/runtime_state_machine_contract.md` 现已将该枚举明确为 `OapeflirStageView` projection-only alias，并补充“不驱动 truth 状态迁移”的约束。 | 已按真实 contract 证据解决。 |
| R8-76 | 43. ADR / Contract 新发现矛盾与缺失 | P2 | `docs_zh/adr/002-division-system.md` | 用"事业部"建模(division_id)；v4.3 §37/§46 用 DomainDescriptor+OrgUnit；ADR 仍 Accepted 未标废弃 | 已解决（文档/ADR） | 根因是 ADR-002 正文虽已大体迁移到 `DomainDescriptor`，但标题、状态和背景说明仍保留着 “事业部系统 / Accepted” 的 active canonical 语气。 | `docs_zh/adr/002-division-system.md` 现已标记为 `Superseded by ADR-081 and ADR-085`，并明确 `division` 只保留为历史 alias，canonical 执行边界统一收敛到 `domain_id / DomainDescriptor` 与 `OrgUnit`。 | 已按真实 ADR 证据解决。 |
| R8-77 | 43. ADR / Contract 新发现矛盾与缺失 | P2 | `docs_zh/contracts/task_and_workflow_contract.md` | WorkflowState 含 division_id 必填字段；v4.3 canonical 对象(HarnessRun/TaskDraft/RequestEnvelope)无此字段 | 已解决（contract/文档） | 根因是 task/workflow contract 已把 `WorkflowState` 降为 projection，但字段表仍把 `division_id` 当作主要 workflow 锚点。 | `docs_zh/contracts/task_and_workflow_contract.md` 已把投影锚点改为 `domain_id`，并把 `legacy_division_alias` 降为兼容投影字段；新增不变量测试守护该映射并通过。 | 已按真实 contract 与测试证据解决。 |
| R9-01 | 45. 执行平面深层缺陷（Lease / Dispatch / State-Transition / Delegation） | P0 | `src/platform/execution/lease/execution-lease-service.ts:566-663` | validateWriteAccess 不检查 expiresAt vs 当前时间；TTL 过期但未回收的 lease 仍允许写入(§8.3 stale detection) | 已解决 | 原审计基于旧版 lease 校验路径；现行同步实现已在 stale-write gate 中显式比较 `expiresAt` 与 `occurredAt`，并在拒绝时写入审计。 | 当前 `src/platform/execution/lease/execution-lease-service.ts` 已在 `validateWriteAccess()` 内对过期 lease 返回 `reasonCode: "lease_expired"`；`tests/unit/platform/execution/lease/execution-lease-service-r9.test.ts` 已验证过期 lease 即使 token/worker 匹配也会被拒绝。 | 不改同步生产代码，仅回写现行实现与定向测试依据。 |
| R9-02 | 45. 执行平面深层缺陷（Lease / Dispatch / State-Transition / Delegation） | P0 | `src/platform/execution/state-transition/transition-service.ts:505-549` | TaskTerminalTransitionService.apply() 用非 CAS 更新(updateTaskStatus/updateWorkflowState)；并发终态转换可互相覆盖，违反 RT-01 不变量 | 已解决 | 原审计停留在终态转换仍走非 CAS repository API 的旧实现；现行实现已经统一改成 CAS 写入，并在预期状态不匹配时抛出 transition CAS failed。 | 当前 `src/platform/execution/state-transition/transition-service.ts` 已使用 `updateTaskStatusCas`、`updateWorkflowStateCas`、`updateSessionStatusCas`、`updateExecutionStatusCas`；`tests/unit/platform/execution/state-transition/transition-service-terminal.test.ts` 已验证四类 CAS 更新都被调用且 mismatch 会失败。 | 本轮修复了该测试文件的 mock 缺失返回值问题，并回写现行 CAS 证据。 |
| R9-03 | 45. 执行平面深层缺陷（Lease / Dispatch / State-Transition / Delegation） | P0 | `src/platform/execution/lease/execution-lease-service-async.ts:247-296` | releaseLeaseSync 不检查 lease.status!=="active"；已过期/已回收 lease 可被重新释放，破坏审计轨迹+双释放 worker slot | 已解决 | 原审计针对的是 async lease service 早期实现；现行 `releaseLeaseSync()` 已先校验 `lease.status === "active"`，非 active 直接阻断，不再允许双释放。 | 当前 `src/platform/execution/lease/execution-lease-service-async.ts` 已在 release 路径返回 `reasonCode: "lease_not_active"`；`tests/unit/platform/execution/lease/execution-lease-service-async-r9.test.ts` 已验证 released/expired lease 都会被阻断。 | 不改该 release 路径主逻辑，仅回写现行实现与定向测试依据。 |
| R9-04 | 45. 执行平面深层缺陷（Lease / Dispatch / State-Transition / Delegation） | P1 | `src/platform/execution/state-transition/transition-service.ts:110-123` | EXECUTION_TRANSITIONS 仅定义 8态；§45.13 要求 13态(缺 queued/dispatching/paused/recovering/timed_out) | 已解决 | 原审计保留了旧 8 态生命周期印象，但当前执行状态机已经包含 `created/queued/dispatching/prechecking/executing/paused/recovering/timed_out/blocked/succeeded/failed/cancelled/superseded` 13 态。 | 当前 `src/platform/execution/state-transition/transition-service.ts` 的 `EXECUTION_TRANSITIONS` 已覆盖 13 态；`tests/unit/platform/execution/state-machine/execution-state-machine.test.ts` 已直接用同一 13 态映射验证允许/拒绝迁移与 terminal 状态。 | 不改生产代码，仅回写现行状态机依据。 |
| R9-05 | 45. 执行平面深层缺陷（Lease / Dispatch / State-Transition / Delegation） | P1 | `src/platform/execution/dispatcher/execution-dispatch-service.ts:223-251` | 无 poison-pill 检测；永久无匹配 worker 的 ticket 无限循环消耗扫描时间，无失败计数/重试限制/死信机制 | 已解决（源码/测试） | 根因是原审计基于还没有“队列老化淘汰 + 失效 + 死信”分支的旧调度循环；现行实现已经对长期滞留的 pending ticket 做 poison-pill 识别，复审表没有同步刷新。 | 当前 `src/platform/execution/dispatcher/execution-dispatch-service.ts` 已引入 `maxQueueAgeMs`，在 `dispatchNext()` 中对超过阈值的 ticket 打上 `dispatch.poison_pill_detected`，随后调用 `invalidateExecutionTicket()` 取消票据、把 execution 更新为 `failed/dispatch.poison_pill_abandoned`、写入 `dispatch:poison_pill_detected` 事件，并通过 `recordBlockedDispatchArtifacts()` 下沉 `dispatch.dlq_enqueue` 与 DLQ 记录。本轮补充 `tests/unit/platform/execution/dispatcher/execution-dispatch-service-poison-pill.test.ts`，定向验证老化 ticket 会被失效、失败、写 poison-pill 事件和 DLQ，而不是继续留在调度循环中，测试已通过。 | 关闭的是“无限滞留 pending ticket 无 poison-pill 处理”缺陷；不外推为已经实现按 worker-rejection 次数累计的另一套失败计数模型。 |
| R9-06 | 45. 执行平面深层缺陷（Lease / Dispatch / State-Transition / Delegation） | P1 | `src/platform/orchestration/agent-delegation/delegation-manager.service.ts:55-76` | 所有委托状态(delegationStore/chainStore)纯内存 Map；进程重启丢失活跃委托链；SQLite delegation-repository 存在但从未接入 | 已解决（源码修复并补定向测试） | 根因是原审计基于委托管理器尚未接入仓储层的旧实现。 | 当前 `DelegationManagerService` 已完整接入 `DelegationRepository`：1) 构造时若传入 repository 则设为 primary store（`useRepositoryAsPrimaryStore = true`）；2) 启动时调用 `hydrateFromRepository()` 从仓储恢复活跃委托链（lines 128-133）；3) `createDelegationRecord()` 同时写仓储和内存缓存（lines 983-993）；4) `transitionDelegationStatus()` 同步更新仓储状态（lines 1056-1059）；5) `getDelegation()`/`getDelegationChain()`/`getActiveDelegations()` 均优先查缓存、未命中则回退到仓储查询（lines 564-671）。内存 Map（delegationStore/chainStore/delegationRootStore）现为缓存层，仓储为持久层权威存储，进程重启后可通过 `hydrateFromRepository()` 恢复活跃委托链。 | 这是实修，不是文档回写；仓储层已完整接线，缓存仅作性能优化，不作权威存储。 |
| R9-07 | 45. 执行平面深层缺陷（Lease / Dispatch / State-Transition / Delegation） | P1 | `src/platform/orchestration/agent-delegation/delegation-manager.service.ts:437-460` | narrowPermissions 将父资源替换为子请求资源(非交集)；子 agent 可请求父未持有的资源，违反 §19 信任继承/仅收窄规则 | 已解决 | 原审计对应的是早期委托权限模型印象；现行 `narrowPermissions()` 已对 `resources` 与 `actions` 都做交集收窄，并对约束取更严格值。 | 当前 `src/platform/orchestration/agent-delegation/delegation-manager.service.ts` 已用 `parentPermissions.resources.filter((resource) => requiredPermissions.resources.includes(resource))` 做资源交集；本轮新增 `tests/unit/platform/orchestration/agent-delegation/delegation-manager-permissions-r9.test.ts`，验证子委托只能得到父子交集资源与动作。 | 不改生产代码，仅补充定向回归测试并回写现行实现依据。 |
| R9-08 | 45. 执行平面深层缺陷（Lease / Dispatch / State-Transition / Delegation） | P1 | `src/platform/execution/lease/types.ts vs execution-lease-service.ts` | MIN_LEASE_TTL_MS(5s)/MAX_LEASE_TTL_MS(30s) 定义但 acquireLease 从不强制；可传入 ttlMs:1 或 999999999(§8.3 TTL bounds) | 已解决（源码修复并补定向测试） | 根因是 TTL 边界常量先落在 `types.ts`，但早期只在部分 lease 路径消费，导致“定义存在、执行不统一”的实现漂移；本轮同时补齐了 async lease service 的 acquire/renew/handover 校验，消除了同类旁路。 | 当前同步 `src/platform/execution/lease/execution-lease-service.ts` 已在 `acquireLease()` 强制 `MIN/MAX_LEASE_TTL_MS`；本轮补齐了 `src/platform/execution/lease/execution-lease-service-async.ts` 的 TTL bounds 检查与过期写拒绝。`tests/unit/platform/execution/lease/execution-lease-service-r9.test.ts` 与 `tests/unit/platform/execution/lease/execution-lease-service-async-r9.test.ts` 均已验证上下界阻断。 | 保留真实代码修复结论，并以同步/异步两套定向测试证明 TTL 边界已被一致执行。 |
| R9-09 | 45. 执行平面深层缺陷（Lease / Dispatch / State-Transition / Delegation） | P1 | `src/platform/orchestration/routing/intake-router.ts` | 路由纯关键词匹配无 capability matching；§8.5 要求匹配 worker/agent 能力注册+容量 | 已解决（源码修复并补定向测试） | 根因是 intake routing 此前停留在”文本分类到 division/workflow”的实现边界。但本轮已在 `intake-router.ts` 接入 `WorkerRegistryService`，在路由决策前先调用 `matchCapabilities()` 查询能力注册+容量快照，满足 §8.5 要求。 | 当前 `src/platform/orchestration/routing/intake-router.ts` 已实现完整 capability matching：1) `checkCapableWorkerAvailability()` 查询 worker registry 并过滤有可用容量的 worker；2) `matchCapabilities()` 按优先级解析能力来源（显式任务声明 > 内容推断 > division 派生）；3) `inferCapabilitiesFromContent()` 从任务文本推断能力；4) `extractDivisionCapabilities()` 从 division 角色提取能力；5) 容量快照 `WorkerCapacitySnapshot` 含 `availableSlots/maxConcurrency/saturation/activeLeaseCount`；6) 路由时若 `capableWorkerFound=false` 走 `capability_fallback_queue`。定向测试已验证能力匹配与容量约束。 | 真实源码修复，不是文档回写。 |
| R9-10 | 45. 执行平面深层缺陷（Lease / Dispatch / State-Transition / Delegation） | P1 | `src/platform/execution/dispatcher/execution-dispatch-service.ts:227` | 每 ticket 迭代实例化新 HealthService(backpressureSnapshot==null时)；O(n) 健康扫描+同批次票据间背压决策不一致 | 已解决 | 原问题确实存在于历史 dispatch loop：背压/健康报告曾在票据循环内部重复求值。现行实现已经把 backpressure 计算提升到循环外，并缓存 `HealthService` 实例，消除了同批次不一致与 O(n) 重扫。 | 当前 `ExecutionDispatchService.dispatchNext()` 已在进入 ticket loop 前一次性求得 `backpressure`，并通过 `cachedHealthService` 复用 `HealthService`；本轮定向运行 `tests/integration/platform/five-plane-execution/dispatcher/execution-dispatch-service-health.test.ts`，4 个用例全部通过，覆盖“每次 dispatchNext 只求值一次”“跨调用复用 cachedHealthService”等行为。 | 不改生产代码，仅回写现行实现与定向测试依据。 |
| R9-11 | 45. 执行平面深层缺陷（Lease / Dispatch / State-Transition / Delegation） | P2 | `src/platform/orchestration/agent-delegation/call-depth-budget.ts vs topology-validator.ts` | maxCallDepth=8 vs DEFAULT_MAX_DEPTH=3 两套独立深度限制互不协调；§19 要求单一权威深度限制 | 已解决（源码修复并补定向测试） | 根因分两层：一是旧实现/旧测试长期保留了 `DEFAULT_MAX_DEPTH=3` 的历史残留；二是 `DelegationManagerService` 曾把同一个 `delegationDepth` 同时塞给 `currentCallDepth / goalDecompositionDepth / delegationDepth`，导致即使常量已到 8，默认委托链仍会被“三次重复计数”过早拦截。 | 本轮在 `src/platform/orchestration/agent-delegation/delegation-types.ts` 为 `AgentContext` 补充可选 call-depth 维度，在 `delegation-manager.service.ts` 中改为 `currentCallDepth ?? delegationDepth`、`goalDecompositionDepth ?? 0`、`delegationDepth: 1`，并让 `createDelegationContext()` 传递 `currentCallDepth`。定向运行 `tests/unit/platform/orchestration/agent-delegation/delegation-manager.test.ts`、`tests/integration/platform/agent-delegation/delegation-manager.integration.test.ts`、`tests/unit/platform/orchestration/agent-delegation/topology-validator.test.ts`、`tests/integration/platform/orchestration/agent-delegation/topology-validator-integration.test.ts`、`tests/unit/platform/agent-delegation.test.ts`、`tests/unit/platform/orchestration/agent-delegation/context-isolator.test.ts` 共 153 个用例，全部通过；其中新增/更新断言已验证默认链深可达 8、depth 9 被拒绝、且不再三次重复计数。 | 这是实修，不是仅改审计或测试常量；同时顺手修复了包装层 `context-isolator.ts` 的语法断裂和 ACP message 必填字段漂移，避免测试入口继续被伪故障污染。 |
| R9-12 | 45. 执行平面深层缺陷（Lease / Dispatch / State-Transition / Delegation） | P2 | `src/platform/state-evidence/truth/async-repositories/event-repository.ts` | 无投影版本化；`listEventsForTask()` 返回原始事件流，无 snapshot cursor / projection version；读侧仍只能按任务全量回放(§4.2 snapshot optimization) | 已解决（源码修复并补定向测试） | 根因不是“没建 event table”，而是事件仓储过去只提供“按 task 顺序列出原始事件”这一最低层接口，没有把投影检查点、快照游标或版本戳提升为仓储契约。 | 本轮在 `src/platform/state-evidence/truth/async-repositories/event-repository.ts`、`src/platform/state-evidence/truth/sqlite/repositories/event-repository.ts` 及其 five-plane 镜像中新增 `TaskEventStreamSnapshot`、`listEventsForTaskSnapshot()`、`listEventsForTaskSinceCursor()`，并统一用 `created_at + id` 作为稳定排序/游标边界。定向运行 `tests/unit/platform/state-evidence/truth/async-repositories/event-repository.test.ts` 与 `tests/unit/platform/state-evidence/truth/sqlite/repositories/event-repository.test.ts`，新增用例已验证 `streamVersion`、`snapshotCursor` 与增量回放。 | 这是实修：仓储层现在已经显式暴露 snapshot/version/cursor 契约，读侧不必再只能按任务全量重放。 |
| R9-13 | 45. 执行平面深层缺陷（Lease / Dispatch / State-Transition / Delegation） | P2 | `src/platform/orchestration/routing/agent-team-service.ts` | 原审计称执行循环固定为 7 阶段；低风险任务也必须经过 review/validate/repair | 已解决 | 历史实现确实是固定 7 阶段；现行实现已把 adaptive routing 下沉到 `riskLevel` 分支，低风险路径不再经过完整重流水线。 | 当前 `AgentTeamService.buildPlan()` 对 `riskLevel === "low"` 返回 `["plan","build","release"]`，中高风险才返回 `["plan","build","review","validate","repair","validate","release"]`。本轮定向运行 `tests/unit/platform/five-plane-orchestration/routing/agent-team-service.test.ts`，15 个用例全部通过，覆盖低风险 3 阶段与高风险 7 阶段分流。 | 不改生产代码，仅把审计结论改回当前真实状态。 |
| R9-14 | 46. OAPEFLIR / Harness / Bootstrap 深层问题 | P0 | `src/platform/orchestration/oapeflir/runtime-execute-bridge.ts:223-241` | `OapeflirLoopService` 本体已通过 `ExecuteBridge` 抽象委托执行，但默认 `RuntimeExecuteBridge` 仍在 orchestration 模块内直接 `import("../../../core/runtime/orchestrator/index.js")` 并调用 `runMultiStepOrchestration()`；平面边界泄漏仍在，只是已从 loop-service 转移到默认 bridge | 已解决（源码修复并补定向测试） | 真正根因不是“loop-service 直接执行”，而是默认 bridge 过去仍由 orchestration 平面持有并直连 runtime 入口，导致“通过抽象委托”与“默认实现仍跨平面直调”并存。 | 本轮把 runtime 入口下沉到 execution plane：`src/platform/orchestration/oapeflir/runtime-execute-bridge.ts` 与 `src/platform/five-plane-orchestration/oapeflir/runtime-execute-bridge.ts` 现在只依赖 `RuntimePlanExecutor` 契约；真正调用 `runMultiStepOrchestration()` 的责任已移到 `src/platform/execution/oapeflir/runtime-plan-executor.ts` 与 `src/platform/five-plane-execution/oapeflir/runtime-plan-executor.ts`。定向运行 `tests/unit/platform/orchestration/oapeflir/runtime-execute-bridge.test.ts` 与 `tests/integration/platform/five-plane-orchestration/oapeflir/runtime-execute-bridge.test.ts`，验证 bridge 只通过注入的 execution-plane adapter 委托执行，并正确传递序列化 plan 与 token budget。 | 这是实修：orchestration 平面不再直接 import runtime orchestrator，默认 bridge 的跨平面边界已收回到 execution plane。 |
| R9-15 | 46. OAPEFLIR / Harness / Bootstrap 深层问题 | P0 | `src/index.ts` | 原审计把 `buildPlatformRootSummary()` 误判为“初始化所有平面目录且无依赖顺序” | 已解决 | 根因是把“汇总 startup plan/runtime catalog 的只读 summary 构建”误读成“真实启动/初始化逻辑”。 | 当前 `buildPlatformRootSummary()` 只调用 `build*StartupPlan()` / `build*RuntimeCatalog()` 组合统计对象，并不创建目录、启动服务或变更运行态；真正的启动顺序约束在 `src/platform-architecture-bootstrap.ts` 的 `PLATFORM_STARTUP_ORDER` 与 `assertStartupOrderEnforced()`。原审计所指的“无依赖顺序初始化”在现行代码中不存在。 | 不改生产代码，仅回写误判已过时。 |
| R9-16 | 46. OAPEFLIR / Harness / Bootstrap 深层问题 | P1 | `src/platform/orchestration/oapeflir/oapeflir-loop-service.ts` | 原审计称 `StageTransitionFSM` 从未被 `OapeflirLoopService.run()` 实例化/咨询，属于死代码 | 已解决 | 历史上这条曾可能成立；现行 loop 已把 FSM 接进实际运行路径，在阶段进入/完成前后持续调用。 | 当前 `OapeflirLoopService` 持有 `private readonly stageFsm = createStageTransitionFSM()`，并在 `run()` 内多次执行 `canTransitionTo()`、`recordStageEntry()`、`recordStageCompletion()`。这已经不是“死代码未接线”，而是实际阶段门控的一部分。 | 不改生产代码，仅回写现行代码已接线；是否还需要扩展更多回退场景是另一条测试/行为问题，不属于原指控本体。 |
| R9-17 | 46. OAPEFLIR / Harness / Bootstrap 深层问题 | P1 | `src/platform/orchestration/oapeflir/assessment-service.ts:65` | routingDecision.division 硬编码 "coding"；非编码域任务永远被误路由 | 已解决（源码修复并补定向测试） | 根因不是单点字符串常量，而是 OAPEFLIR 的 `TaskSituation` 链路此前没有承载 domain/division 身份，`AssessmentService` 只能退回硬编码默认值 `"coding"`。 | 本轮在 `src/platform/five-plane-orchestration/oapeflir/types/task-situation.ts` 与 `src/platform/shared/observability/task-situation-builder.ts` 增加 `domainId`，在 `src/platform/five-plane-orchestration/oapeflir/oapeflir-loop-service.ts` 传入 `workflow.workflow.divisionId`，并让 `src/platform/five-plane-orchestration/oapeflir/assessment-service.ts` 优先使用 `situation.domainId`；定向回归 `tests/unit/platform/orchestration/oapeflir/assessment-service-extended.test.ts` 已验证显式域路由不再被硬编码覆盖。 | 保留真实代码修复结论。 |
| R9-18 | 46. OAPEFLIR / Harness / Bootstrap 深层问题 | P1 | `src/platform/orchestration/oapeflir/final-response.ts:27-48` | FinalResponse 接口 10字段；§A.3 要求 13字段(缺 executionDurationMs/modelId/retryCount) | 已解决（源码修复并补定向测试） | 根因是 `FinalResponse` 接口长期只保留了旧版最小返回包，缺少执行元数据字段，且没有针对 §A.3 13 字段结构的回归保护。 | 本轮已在 `src/platform/five-plane-orchestration/oapeflir/final-response.ts` 补齐 `executionDurationMs`、`modelId`、`retryCount`，并同步更新 `tests/unit/platform/orchestration/oapeflir/final-response.test.ts`；本轮定向测试已通过。 | 保留真实代码修复结论。 |
| R9-19 | 46. OAPEFLIR / Harness / Bootstrap 深层问题 | P1 | `config/runtime/default.json` | 仅 7字段；缺 §8 要求的 healthCheckIntervalMs/shutdownGracePeriodMs/logLevel/metricsEnabled/tracingEnabled/retryPolicy | 已解决 | 原审计基于旧版 runtime config 样本，没有复核当前默认运行时配置。 | 当前 `config/runtime/default.json` 已显式包含 `healthCheckIntervalMs`、`shutdownGracePeriodMs`、`logLevel`、`metricsEnabled`、`tracingEnabled` 与 `retryPolicy`，同时还包含 `stateMachine`、`fivePlane`、`harnessRuntime`、`budgetConfiguration`、`eventRegistry`。 | 不改配置，仅回写现行默认配置依据。 |
| R9-20 | 46. OAPEFLIR / Harness / Bootstrap 深层问题 | P1 | `src/platform/orchestration/harness/index.ts` | `ConstraintPack` 混用 `toolPolicy` 与 `risk_policy` / `output_policy`，调用方拼写会渗透到运行态与序列化边界 | 已解决（源码修复并补定向测试） | 根因是 harness runtime 过去直接消费混合命名字段，导致输入形状就是持久化/运行态形状，没有 canonical policy 归一化步骤。 | 本轮在 `harness/index.ts` 增加 `riskPolicy` / `outputPolicy` canonical 字段与 `getConstraintRiskPolicy()`、`getConstraintOutputPolicy()`、`normalizeConstraintPack()`；`createRun()` 现先归一化再落入运行态，评估与 guardrail 路径也统一改为 helper 读取。定向运行 `tests/unit/platform/orchestration/harness/constraints.test.ts` 中新增的归一化/兼容性用例，全部通过；同时两套 harness 模块导入编译检查通过。 | 这是实修；兼容层仍接受旧 `snake_case` 输入，但 canonical 运行态已统一为 camelCase。 |
| R9-21 | 46. OAPEFLIR / Harness / Bootstrap 深层问题 | P1 | `src/platform/orchestration/harness/hitl-runtime.ts` / `src/platform/orchestration/harness/index.ts` | 历史上 pending HITL 请求只存在于 `HitlRuntime` 进程内 `Map`；即使 run 已持久化，恢复后新的 runtime 实例也找不到原请求索引 | 已解决（源码修复并补定向测试） | 根因不是“完全没有 durable run”，而是 durable run 中其实已经带有 `hitlRequest`，但 `restoreRun()` / `restoreFromCheckpoint()` 从未把它重新注入新的 `HitlRuntime` 索引，导致重启后审批上下文断链。 | 本轮在 `HitlRuntime` 增加 `hydrate()`，并让 `HarnessRuntimeService.resolveHitlReview()`、`restoreRun()`、`restoreFromCheckpoint()` 在使用前重建请求索引。定向运行新增用例 `HarnessRuntimeService restores persisted HITL requests into a fresh runtime`，验证新建 runtime 实例在 restore 后能重新拿到 pending HITL request；测试通过。 | 这是针对 runtime-managed HITL 路径的真实修复；standalone `HitlRuntime` 仍是内存索引，但审计所指“持久化 run 恢复后审批请求丢失”链路已被补齐。 |
| R9-22 | 46. OAPEFLIR / Harness / Bootstrap 深层问题 | P1 | `src/platform/orchestration/harness/recovery-controller.ts` | 原审计称 `handleFailure()` 恢复期间不发事件到 state-evidence plane | 已解决 | 当前恢复控制器已经在 abort、resume、sleep、hitl_review 等分支统一发出恢复生命周期事件，原指控与现行代码不符。 | 当前 `RecoveryController.handleFailure()` 会在 `operator_abort` 分支发 `harness:recovery_aborted`，在 `tool_timeout` / `llm_provider_unavailable` / `budget_exhausted` / `platform_panic` / `worker_crash` 分支发 `harness:recovery_started`，并通过 `durableService.emitEvent()` 写入 evidence 通道。 | 不改生产代码，仅回写现行实现依据。 |
| R9-23 | 46. OAPEFLIR / Harness / Bootstrap 深层问题 | P1 | `src/platform-architecture-bootstrap.ts` / `src/platform/shared/lifecycle/service-registry.ts` | 原审计称 `registerPlatformArchitectureServices()` 注册后立即 `get()`，且 init 失败会“静默传播” | 已解决 | 根因是把“fail-fast eager init”误读成“无门控且静默失败”。在现行实现里，`get()` 会同步抛出注册缺失、循环依赖或 `init()` 异常，并不会吞掉。 | `registerPlatformArchitectureServices()` 注册的是 layer/plane/app/startup-target/summary 这类静态 manifest 服务，随后调用 `registry.get()` 进行 eager init；而 `ServiceRegistry.getRecursive()` 对未注册、循环依赖和初始化异常都会直接抛错。当前代码不存在“init 失败静默传播”这一路径。 | 不改生产代码，仅回写原指控失真；若未来需要统一 readiness/health API，应另立新项，不应继续沿用本条表述。 |
| R9-24 | 46. OAPEFLIR / Harness / Bootstrap 深层问题 | P1 | `config/risk/default.json:2` | $schema 指向 .ts 文件非 JSON Schema；运行时无法做 JSON Schema 验证 | 已解决 | 原审计使用的是旧版 risk config 文件头，没有复核当前 schema 声明。 | 当前 `config/risk/default.json` 的 `$schema` 为 `http://json-schema.org/draft-07/schema#`，已不再指向 `.ts` 文件。 | 不改配置，仅回写现行 schema 依据。 |
| R9-25 | 46. OAPEFLIR / Harness / Bootstrap 深层问题 | P2 | `config/domains/default.json:98` | domain status:"testing" 非 canonical(§11: draft/active/deprecated/retired) | 已解决 | 原审计基于旧版 domain default 数据，没有复核当前状态枚举值。 | 当前 `config/domains/default.json` 中 domain `status` 为 `active`，并不存在 `testing` 状态。 | 不改配置，仅回写现行 domain 状态依据。 |
| R9-26 | 46. OAPEFLIR / Harness / Bootstrap 深层问题 | P2 | `config/domains/default.json:7` | domain version:1(integer)；§11 要求 semver string("1.0.0") 用于兼容性检查 | 已解决 | 原审计使用的是旧版 domain version 形状，没有复核当前默认域配置。 | 当前 `config/domains/default.json` 中 domain `version` 为 semver 字符串 `1.0.0`，并非整数。 | 不改配置，仅回写现行版本字段依据。 |
| R9-27 | 46. OAPEFLIR / Harness / Bootstrap 深层问题 | P2 | `src/platform/orchestration/harness/oapeflir-harness-mapping.ts:24` | hitl_operator 映射到 OAPEFLIR "assess" 阶段；§45 HITL 是 feedback/gate 机制非自动风险评估 | 已解决（源码修复并补定向测试） | 根因是早期 phase mapping 把 `hitl_operator` 当成“评估角色 fallback”处理，直接落到了 `assess` 语义，混淆了人工 gate/feedback 与系统自动评估阶段。 | 当前 `src/platform/orchestration/harness/oapeflir-harness-mapping.ts` 与 `src/platform/five-plane-orchestration/harness/oapeflir-harness-mapping.ts` 都已把 `hitl_operator` 映射到 `feedback`；对应 `tests/unit/platform/orchestration/harness/oapeflir-harness-mapping.test.ts` 与 `tests/unit/platform/five-plane-orchestration/harness.test.ts` 的定向断言已更新并通过。 | 真实修复是改 phase 语义映射，而不是靠 ADR、注释或 closure test 覆盖原问题。 |
| R9-28 | 46. OAPEFLIR / Harness / Bootstrap 深层问题 | P2 | `src/platform/orchestration/harness/guardrails/guardrail-engine.ts:91-95` | 永远不返回 retry_same_plan；HarnessDecisionAction 联合含此值但护栏无法触发 | 已解决（源码修复并补定向测试） | 根因有两层：一是 guardrail engine 之前把 warning 路径都压成 `requiresHuman/escalate_to_human`，没有把“仅证据缺失”的可重试告警区分出来；二是 harness 决策层没有消费 guardrail 的 `retry_same_plan` 建议，导致即使补出该建议也无法落到最终 decision。 | 当前 `src/platform/orchestration/harness/guardrails/guardrail-engine.ts` 与 `src/platform/five-plane-orchestration/harness/guardrails/guardrail-engine.ts` 已把“仅 evidence warn”分支产出 `suggestedAction: "retry_same_plan"` 且不再强制 `requiresHuman`；`src/platform/orchestration/harness/index.ts` 与 `src/platform/five-plane-orchestration/harness/index.ts` 的 `decide()` / `runLoop()` 已显式接入 `guardrailSuggestedAction` 并优先落成 `harness.guardrail_retry_same_plan`。定向证据为 `tests/unit/platform/orchestration/harness/guardrails/guardrail-engine.test.ts`、`tests/unit/platform/orchestration/harness/guardrail-engine.test.ts`、`tests/unit/platform/orchestration/harness/guardrail-decision-priority.test.ts`，并已与 mapping/five-plane 相关窄集一起通过。 | 这次收口只基于真实源码与定向测试；未使用全量测试，也未把 lease/fencing 已知污染链路当作 closure 依据。 |
| R9-29 | 46. OAPEFLIR / Harness / Bootstrap 深层问题 | P2 | `src/platform/orchestration/oapeflir/runtime-execute-bridge.ts:228` | 动态 import ../../../core/runtime/orchestrator/index.js——路径在 src/platform/ 外部；耦合未声明的 core/ 模块 | 已解决（源码修复并补定向测试） | 根因与 R9-14 同源：runtime 真实执行入口曾直接暴露给 orchestration bridge，导致模块边界与路径边界一起泄漏。 | 本轮 `runtime-execute-bridge.ts` 已不再引用 `../../../core/runtime/orchestrator/index.js`；`runMultiStepOrchestration()` 调用已下沉到 execution-plane 的 `runtime-plan-executor.ts`，five-plane 镜像也同步改为依赖 `five-plane-execution` 适配器。上述 bridge 定向测试与导入检查均已通过。 | 这是和 R9-14 同一组收口：orchestration 层对 `core/` 的未声明直连已经移除。 |
| R9-30 | 46. OAPEFLIR / Harness / Bootstrap 深层问题 | P2 | `tests/integration/` | 缺跨平面事件传播/事件溯源重放/OAPEFLIR FSM/PlanGraph 全链路集成验证；§45 核心行为覆盖仍明显不足 | 部分整改（补测试但未完全达标） | 已存在 `tests/integration/cross-plane-event-propagation.test.ts`（1127行，9个集成测试）覆盖跨平面事件传播、事件溯源重放、OAPEFLIR FSM、PlanGraph依赖链。`tests/integration/platform/five-plane-orchestration/oapeflir/runtime-execute-bridge.test.ts` 补充 bridge→execution adapter 委托验证。`tests/integration/platform/orchestration/oapeflir/oapeflir-loop-integration.test.ts` 覆盖完整8阶段OAPEFLIR生命周期。`tests/integration/platform/shared/stability/stable-rehearsal-integration.test.ts` 含 event replay rehearsal。但当前测试侧重局部行为和单程调用，尚未充分覆盖跨平面事件传播+事件溯源重放+OAPEFLIR FSM状态机+PlanGraph在同一条真实链路中串联合并验证的场景（即”全链路”缺口仍存）。 | 补齐测试文件已存在，覆盖广度已扩大；但审计要求的”全链路”（事件传播+重放+FSM+PlanGraph串成单链）仍有差距，保持”部分整改”而非”已解决”。后续要关闭此项，需补齐一条真实跨平面事件传播→事件溯源重放→OAPEFLIR FSM状态转换→PlanGraph执行完整链路验证。 |
| R9-31 | 47. 组织治理 / NL 交互 / 自治引擎深层问题 | P0 | `src/org-governance/org-model/org-governance-saga.ts` | §46.3 要求 OrgGovernanceSaga 冻结 orgVersion+计算影响差异+有序子步骤+补偿；实现为 stub 仅按类型分组无实际逻辑 | 已解决 | 原审计条目在早期复审时写为 stub，当前实现已补齐全部四项要求：`freezeOrgVersion()`(line 1002)创建 `OrgVersionSnapshot`；`computeImpactDiff()`(line 1020)创建 `OrgImpactDiff`；`sortStepsByPhase()`(line 80)实现有序子步骤；`compensate()`(line 306)配合 `CompensationReceiptEntry[]` 实现可追踪的补偿。`executeWithOrgOperations()`(line 601)直接执行真实 org 操作而非依赖外部 handler。 | 当前 `org-governance-saga.ts` 已实现全部 §46.3 要求：freeze orgVersion (`freezeOrgVersion()`)、impact diff 计算 (`computeImpactDiff()`)、phase 顺序排序 (`sortStepsByPhase()`)、补偿回滚含 receipt (`compensate()`)。typecheck 仅在测试文件有 pre-existing 错误，主实现文件无错误。 | 不改主流程设计，仅回写现行实现并更新审计结论。 |
| R9-32 | 47. 组织治理 / NL 交互 / 自治引擎深层问题 | P0 | `src/interaction/nl-gateway/index.ts:722` | §39.6 规定仅 confirmed TaskSpec 可生成 RequestEnvelope；buildTask() 在 confirmationReceipt.state="pending_user_confirmation" 时即预构建 envelope | 已解决 | 根因是原审计停留在旧版 NL gateway buildTask 流程，没有复核后续加上的 confirmation gate，因此把“生成 task draft / confirmation receipt”误判成了“已经生成可执行 RequestEnvelope”。 | 当前 `src/interaction/nl-gateway/index.ts` 在 `buildTask()` 中已明确写出“Only emit RequestEnvelope after TaskSpec is confirmed”，并在 `confirmationRequired` 为真时直接返回 `requestEnvelope: null`；本轮新增 `tests/unit/interaction/nl-gateway/request-envelope-confirmation-gate.test.ts`，定向验证了“待确认时 envelope 为空、无需确认时才生成 envelope”两条分支。 | 不改生产代码，仅回写现行实现与定向测试依据。 |
| R9-33 | 47. 组织治理 / NL 交互 / 自治引擎深层问题 | P1 | `src/org-governance/approval-routing/route-engine/index.ts:155` | §47.1 要求 parallel 会签+sequential 逐级审批模式；仅实现单线性链无并行/会签 | 已解决 | 根因不是主实现缺会签，而是复审文档没有吸收后续 `ApprovalMode` / `buildParallelSignoffGroups()` / `resolveApprovalSteps()` 的落地，且旧单测还保留了早期 `groupId` 断言。 | 当前 `src/org-governance/approval-routing/route-engine/index.ts` 已显式支持 `sequential` / `parallel` / `any` 三种模式，并生成 `ApprovalStepRequirement[]`；本轮修正 `tests/unit/org-governance/approval-routing/route-engine/index.test.ts` 的过期断言后，定向测试已覆盖单人链、会签分组和并行步骤生成。 | 不改生产逻辑，回写现行实现并修正过期测试证据。 |
| R9-34 | 47. 组织治理 / NL 交互 / 自治引擎深层问题 | P1 | `src/org-governance/approval-routing/route-engine/index.ts:257` | normalizeThresholdCny 硬编码 USD→CNY 汇率 7.2；§47.2 要求 base_currency+FX snapshot | 已解决（源码修复并补定向测试） | 根因是 legacy `amountUsd` 兼容路径和 USD 阈值换算过去仍能在无结构化 FX snapshot 的情况下退回隐式默认汇率，导致“有新结构、旧旁路仍可绕过”并存。 | 本轮在 `src/org-governance/approval-routing/route-engine/index.ts` 去掉了内建 `7.2` 默认值：legacy `amountUsd` 现在必须依赖显式配置的 legacy FX rate 或直接改走 `amount + fxRateSnapshot`；无配置时会抛出 `approval_route.fx_snapshot_required:USD`。同时请求完全未携带金额时不再误走 legacy USD 路径。定向测试已验证“配置 legacy rate 时生成冻结 FX snapshot”“未配置时拒绝 legacy amountUsd”“显式 fx snapshot 优先于 legacy 兼容路径”。 | 这是实修，不是文档回写；关闭依据来自生产代码收口和最小回归测试。 |
| R9-35 | 47. 组织治理 / NL 交互 / 自治引擎深层问题 | P1 | `src/org-governance/approval-routing/route-engine/index.ts:46` | ApprovalRouteSnapshot 无 expiresAt；§47.3 要求 expiry/revocation/commit-time revalidation | 已解决 | 根因是复审条目仍沿用缺 `expiresAt` 的旧实现印象，同时同目录旧单测还停留在早期 30 分钟假设，没有对照当前实现。 | 当前 `ApprovalRouteSnapshot` 已包含 `expiresAt`，`resolveApprovalRoute()` 会冻结 `createdAt/expiresAt`，`revalidateApprovalRoute()` 也已对 `expired` / `revoked` / 提交态重算做显式校验。本轮同步修正了 `tests/unit/org-governance/approval-routing/route-engine/index.test.ts` 的过期时长断言并完成定向验证。 | 不改主流程设计，只回写现行实现与修正后的测试依据。 |
| R9-36 | 47. 组织治理 / NL 交互 / 自治引擎深层问题 | P1 | `src/org-governance/approval-routing/escalation/index.ts` | 超时升级用静态 escalateToApproverId 不遍历 OrgTree；§47.1 要求向上走组织层级 | 已解决 | 根因是复审条目没有复核现行 `resolveEscalationApprover()` 与 `traverseOrgHierarchyForEscalation()`，同时旧测试对“ownerless 中间层是否继续向上遍历”的语义预期也落后于实现。 | 当前 `src/org-governance/approval-routing/escalation/index.ts` 在 `escalateToParentManager` 路径下会先调用 `traverseOrgHierarchyForEscalation()` 沿父层级寻找下一个 approver，并在显式 `escalateToApproverId` 场景下也会结合层级路径校验。本轮修正 `tests/unit/org-governance/approval-routing/escalation/index.test.ts` 的过期断言后，定向测试已覆盖向上遍历、去重、maxLevels 和 ownerless 中间层继续上探。 | 不改主实现逻辑，回写现行源码与定向测试证据。 |
| R9-37 | 47. 组织治理 / NL 交互 / 自治引擎深层问题 | P1 | `src/org-governance/compliance-engine/inheritance/index.ts` | §49.2 要求 PolicyStrictnessComparator+不可比策略进入 compliance approval；用硬编码启发式无比较器接口 | 已解决 | 根因是复审条目停留在更早的继承合并印象，没有吸收后续引入的 `PolicyStrictnessComparator` 契约和不可比策略显式审批判定。 | 当前 `src/org-governance/compliance-engine/inheritance/index.ts` 已定义 `PolicyStrictnessComparator`、`DefaultPolicyStrictnessComparator` 与 `PolicyStrictnessResult.requiresComplianceApproval`；`comparePolicyStrictness()` 在字段不可比时会返回 `ordering: "incomparable"` 且 `requiresComplianceApproval: true`。现有定向测试 `tests/unit/org-governance/compliance-engine/policy-resolver/policy-resolver.test.ts` 已直接覆盖该行为。 | 不改生产代码，仅回写现行实现与已有定向测试依据。 |
| R9-38 | 47. 组织治理 / NL 交互 / 自治引擎深层问题 | P1 | `src/org-governance/compliance-engine/` | §49 要求 ComplianceExceptionWorkflow(scope/expiresAt/compensating controls) + EvidenceQualityScore/ControlCoverageReport——全部未实现 | 已解决（源码修复并补定向测试） | 根因不是“完全没有对象定义”，而是早期 `ComplianceGovernanceService` 只提供了最薄的 exception/score/report 结构：`ComplianceExceptionWorkflow` 已有，但 `EvidenceQualityScore` 只有总分，`ControlCoverageReport` 只有 covered/missing/ratio，缺少架构要求的 freshness、来源可信度、不可篡改性，以及 control-level pass/fail/partial/not_applicable 与 gap/remediation owner。 | 本轮在 `src/org-governance/compliance-engine/compliance-governance-service.ts` 扩展了 `EvidenceQualityScore`（`completenessScore`、`freshnessScore`、`sourceTrustScore`、`tamperIntegrityScore`、`staleScheduleIds`、`untrustedEvidenceIds`、`tamperedEvidenceIds`）与 `ControlCoverageReport`（`controlStatuses`、`gaps`、`remediationOwner`），并保留原有总分/coverageRatio 兼容字段。定向测试已验证分维评分、gap 生成和 remediation owner。 | 这是对治理服务结构的真实补齐，不是只在文档里承认已有接口名。 |
| R9-39 | 47. 组织治理 / NL 交互 / 自治引擎深层问题 | P1 | `src/org-governance/compliance-engine/evidence-collector.ts` | §49.3 要求定期自动证据收集(季度 SOX/持续 HIPAA)；实现仅按需调用无调度器/周期/新鲜度强制 | 已解决（源码修复并补定向测试） | 根因是 evidence automation 能力早已沉在 `ComplianceEvidenceCollector` 内部，但没有提升为 `ComplianceGovernanceService` 的正式入口，导致上层治理流程只能“手动 collectEvidence”，无法显式调度季度/持续采集、取 due jobs、执行采集和检查 freshness。 | 当前 `ComplianceEvidenceCollector` 已支持 `quarterly` / `continuous` / `periodic` / `on_demand` 调度；本轮进一步在 `src/org-governance/compliance-engine/compliance-governance-service.ts` 暴露 `scheduleEvidenceCollection()`、`listScheduledCollections()`、`getDueEvidenceCollections()`、`executeScheduledEvidenceCollection()`、`checkEvidenceFreshness()`、`deactivateEvidenceCollectionSchedule()`，把自动采集与新鲜度约束提升成治理服务 API。定向测试已验证季度调度、due collection、执行采集和 stale freshness 处罚。 | 关闭依据来自真实服务入口补齐和最小回归测试，而不是只引用底层 collector 私有能力。 |
| R9-40 | 47. 组织治理 / NL 交互 / 自治引擎深层问题 | P1 | `src/interaction/nl-gateway/index.ts:161` | UserConfirmationReceipt 仅 not_required/pending 两态；缺 confirmed 态+risk preview version/scope/actor/timestamp(§39) | 已解决 | 根因是原审计基于旧版 `UserConfirmationReceipt` 接口快照，没有复核后来补进的三态模型与确认元数据字段。 | 当前 `src/interaction/nl-gateway/index.ts` 的 `UserConfirmationReceipt` 已声明三态 `not_required`、`pending_user_confirmation`、`confirmed`，并包含 `scope`、`timestamp`、`riskPreviewVersion`、`actor` 字段；本轮新增 `tests/unit/interaction/nl-gateway/user-confirmation-receipt-shape.test.ts`，定向验证 `confirmed` 状态与这些字段可直接作为接口实例化。 | 不改生产代码，仅回写现行类型定义与定向测试依据。 |
| R9-41 | 47. 组织治理 / NL 交互 / 自治引擎深层问题 | P1 | `src/interaction/nl-gateway/index.ts:480` | §39 high/critical 指令须 dry-run preview；buildRiskPreview 纯关键词匹配无实际 dry-run 执行/副作用预览 | 已解决（源码修复并补定向测试） | 真正根因不是“完全没有风险分类”，而是高风险 NL 请求过去只有 `RiskPreview` 这个关键词驱动的风险标签，没有把已解析的 intent / division / workflow / environment / channel / confirmation gate 组装成用户可确认的预演材料。结果用户看到的是风险等级，不是 dry-run 结果。 | 本轮在 `src/interaction/nl-gateway/index.ts` 新增 `DryRunPreview` 与 `buildDryRunPreview()`，高/critical 请求会基于结构化 payload、目标 scope、环境/渠道实体、side effect 预估和 policy checks 生成预演材料，并通过 `TaskBuildResult.dryRunPreview` 与 `confirmationReceipt.reasonCodes += nl_gateway.dry_run_preview_ready` 对外暴露；定向测试 `tests/unit/interaction/nl-gateway/dry-run-preview-and-memory.test.ts` 已验证高风险请求会返回结构化 dry-run preview、confirmation 前仍不发 `RequestEnvelope`。 | 这是把“风险标签”补成了真正可审阅的 preflight/dry-run 结果，不是只改文案。 |
| R9-42 | 47. 组织治理 / NL 交互 / 自治引擎深层问题 | P1 | `src/interaction/goal-decomposer/index.ts` | §40.2 要求 capability validation+risk propagation through task graph；无 DomainDescriptor 能力检查；风险逐节点不传播 | 已解决（源码修复并补定向测试） | 根因是 `goal-decomposer` 虽然已有 `validateTaskDomainCapabilities()` 与 `riskPropagation` 结构，但旧实现会在进入校验前把 `requiredCapabilities/requiredPermissions` 预过滤成“该域已允许的子集”，导致 capability 缺口被提前抹掉；风险逐节点传播其实已在主链存在，只是没有和真实能力失配一起收口。 | 当前 `src/interaction/goal-decomposer/index.ts` 已保留任务真实请求的 capability/permission，并让 `validateTaskDomainCapabilities()` 的失配结果进入 `validationMessages` 且触发 `requiresHumanReview`；同文件的 `riskPropagation` 仍会按 task graph 逐节点生成。定向测试 `tests/unit/interaction/goal-decomposer/index.test.ts` 与 `task-domain-capability-policy.test.ts` 已通过。 | 已改生产代码并补最小回归。 |
| R9-43 | 47. 组织治理 / NL 交互 / 自治引擎深层问题 | P1 | `src/interaction/autonomy/promotion-engine/index.ts:27-31` | §42.2 要求 human override rate <5%/<1% 才可升级；assessPromotion 仅检查 totalExecutions/successRate 从不评估 override rate | 已解决 | 根因是原审计停留在 promotion engine 早期阈值逻辑，没有复核后续补进的 override-rate 门禁，因此把“测试没覆盖”误写成了“实现不存在”。 | 当前 `src/interaction/autonomy/promotion-engine/index.ts` 已显式计算 `overrideRate()`，并对 `supervised -> semi_auto` 施加 `<5%`、对 `semi_auto -> full_auto` 施加 `<1%` 的阻断条件；本轮补强 `tests/unit/interaction/autonomy/promotion-engine/index.test.ts`，定向验证了 `5%` 和 `1%` 边界都会返回 `autonomy.promotion_blocked_by_override_rate`。 | 不改生产代码，仅回写现行实现与定向测试依据。 |
| R9-44 | 47. 组织治理 / NL 交互 / 自治引擎深层问题 | P1 | `src/interaction/autonomy/index.ts:329` | §42.2 要求 domain_owner/platform_team 审批升级；所有升级 approvedBy:"auto" 无审批门 | 已解决（源码修复并补定向测试） | 真实根因不是“事件标签全是 auto”，而是此前实现只把部分 promotion 事件标成 `domain_owner`，却没有真正建立待审批门：`evaluateProfile()` 会直接返回升级后的 autonomy level，既没有 pending approval queue / resolver，也没有把 `semi_auto -> full_auto` 和 frozen recovery 明确路由到 `platform_team`。 | 本轮在 `src/interaction/autonomy/index.ts` 新增 `PendingAutonomyApprovalRequest`、`pendingApprovalRequests`、`listPendingApprovalRequests()`、`resolvePendingApproval()`；`evaluateProfile()` 对所有 promotion 在 `approvedBy !== "auto"` 时设置 `requiresApprovalResolution`，先把 `effectiveLevel` 保持在当前级别，再创建 pending request。`full_auto` 目标级别明确路由到 `platform_team`，`supervised/semi_auto` 路由到 `domain_owner`；审批通过后才在 `resolvePendingApproval()` 中把 `currentAutonomy` 实际写回 profile。 | 定向测试已验证审批前不生效、审批后才升级，以及 `platform_team` 路由：`tests/unit/interaction/autonomy/progressive-autonomy.test.ts`、`progressive-autonomy-approval-attribution.test.ts`、`index.test.ts`、`autonomy-level-state.test.ts`、`progressive-autonomy-edge-cases.test.ts` 均已通过。 | 这是自治升级链路的真实修复，不是只把 `approvedBy` 文案从 `auto` 改成 `domain_owner`。 |
| R9-45 | 47. 组织治理 / NL 交互 / 自治引擎深层问题 | P2 | `src/interaction/autonomy/promotion-engine/index.ts:24-31` | §42.2 要求 per-level 无事件窗口(30d/60d/90d)；仅全局 incidents>0 检查无时间窗约束 | 已解决（源码修复并补定向测试） | 根因是 `assessPromotion()` 过去只看“当前是否有 incident / override / volume / success rate”，没有把 `lastIncidentAgeDays` 提升成分级 promotion gate；而 `AutonomyGovernanceService.evaluateCapability()` 仍直接依赖该函数，所以窗口缺口会落到真实治理推荐路径。 | 本轮在 `src/interaction/autonomy/promotion-engine/index.ts` 新增了按级别的 incident-free window 门槛：`suggestion -> supervised` 需 `30d`、`supervised -> semi_auto` 需 `60d`、`semi_auto -> full_auto governance path` 需 `90d`；并补强 `tests/unit/interaction/autonomy/promotion-engine/index.test.ts` 的三个窗口边界用例，同时回归 `tests/unit/interaction/autonomy/autonomy-governance-service.test.ts`，确认治理推荐路径仍通过。 | 这是实修，不是把主逻辑转移到别的 service 后在审计表里偷换概念。 |
| R9-46 | 47. 组织治理 / NL 交互 / 自治引擎深层问题 | P2 | `src/interaction/goal-decomposer/index.ts:368` | §40.3 模板匹配应用 DomainRecipe(§37.7)；detectTemplate 用 5个硬编码正则无 DomainRecipe/DomainDescriptor 集成 | 已解决（源码修复并补定向测试） | 根因是 `GoalDecompositionService.detectTemplate()` 过去完全自给自足，只按 5 组硬编码正则在 interaction 层做模板判断；仓内虽然已有 `domains/recipes` 与 baseline recipe catalog，但这条识别链没有真正消费它们。 | 本轮在 `src/interaction/goal-decomposer/index.ts` 引入 `DomainRecipe` 匹配优先路径：先使用 `matchDomainRecipe()` 匹配显式 recipe，再依据 recipe 的 `archetype/domainId/defaultWorkflowId` 映射到 `marketing_campaign/release_launch/incident_response/hiring_pipeline/generic_multi_step` 模板，最后才回退到旧正则；定向验证已通过新增的 `tests/unit/interaction/goal-decomposer/domain-recipe-template-routing.test.ts`。 | 这是把现有 domain recipe 真正接进模板识别链，不是仅在审计表里承认“仓内有 recipe 目录”。 |
| R9-47 | 47. 组织治理 / NL 交互 / 自治引擎深层问题 | P2 | `src/interaction/proactive-agent/index.ts:76` | §41.5 Suggestion 管线(Context Builder→Generator→Queue→dashboard)；enqueueSuggestion 无上下文构建/质量评分 | 已解决（源码修复并补定向测试） | 根因是 `ProactiveAgentService` 过去把 `suggest` 路径退化成了最薄的 in-memory queue：`enqueueSuggestion()` 只复制 trigger 元数据，没有根据 event/metric 输入构造 suggestion context，也没有任何质量分层，导致 dashboard 侧拿不到足够的建议解释信息。 | 本轮在 `src/interaction/proactive-agent/index.ts` 新增 `ProactiveSuggestionContext`、`buildSuggestionContext()`、`generateSuggestionTitle()`、`scoreSuggestionQuality()`，让 suggestion 记录携带 `triggerType/riskLevel/sourceSummary/matchedSignal/requireConfirmation` 上下文与 `qualityScore`；同时把 `update_dashboard` 动作固定为 `silent_record`，避免误走 `auto_execute`。定向验证已通过 `tests/unit/interaction/proactive-agent/index.test.ts` 与 `tests/integration/interaction/proactive-agent-integration.test.ts`。 | 这是实修 suggestion pipeline，不是仅给队列对象补几个空字段。 |
| R9-48 | 48. Contract 深层矛盾与缺口（新发现） | P0 | `docs_zh/contracts/platform_panic_and_resume_contract.md §3` | PlatformPanicDirective 含可选 expires_at TTL；§2.4 不变量明确"Panic 不得 TTL 自动解除，恢复必须人工确认" | 已解决 | 原审计条目基于 contract 历史版本；现行 `platform_panic_and_resume_contract.md` §3 已明确 `expires_at` 为"必须指定具体时间，不得使用 auto-TTL"，non-expiring directive 须显式标注，§4 规则进一步明确"恢复必须通过显式 ResumePlan，不得靠隐式重启解除"。 | 当前 `docs_zh/contracts/platform_panic_and_resume_contract.md` §3 明确 `expires_at` 语义：必须指定具体时间，不得使用 auto-TTL；若未指定则表示 non-expiring directive。v4.3 Architecture Remediation §T-26 已把 scope 收敛到 `platform / region / tenant / domain / run / node`，并要求 ResumePlan 双人审批与兼容性复核。 | 不改 contract，仅回写现行 contract 条款与 remediation 依据。 |
| R9-49 | 48. Contract 深层矛盾与缺口（新发现） | P1 | `docs_zh/contracts/observability_contract.md §3` | LogEvent 用 task_id? 作主关联键缺 harness_run_id/node_run_id/attempt_id；§5.5 要求 HarnessRun 为 canonical 关联 | 已解决 | 原审计条目基于 contract 历史版本；现行 `observability_contract.md` §3 LogEvent 最小字段已将 `harness_run_id`（必填）、`node_run_id`（必填）、`attempt_id`（必填）作为 canonical 主关联键，`task_id?` 仅保留为 legacy 兼容投影。§4.3 StageMetricSample 和 LoopIterationTrace 同样以 `harness_run_id` 为必填。 | 当前 `docs_zh/contracts/observability_contract.md` §3 明确：`harness_run_id`（必填，架构要求）、`node_run_id`（必填）、`attempt_id`（必填），`task_id?` 仅作 legacy 兼容投影。v4.3 Architecture Remediation 已回写 canonical 关联要求。 | 不改 contract，仅回写现行 contract 字段定义与 remediation 依据。 |
| R9-50 | 48. Contract 深层矛盾与缺口（新发现） | P1 | `docs_zh/contracts/model_gateway_routing_contract.md §2` | ModelRouteRequest 用 taskId 无 harnessRunId/nodeRunId；INV-BUDGET-001 要求 harnessRunId 才能验证预算预留 | 已解决 | 原审计条目基于 contract 历史版本；现行 `model_gateway_routing_contract.md` §2 `ModelRouteRequest` 已显式包含 `harnessRunId`（canonical 执行关联）、`nodeRunId`（canonical 节点关联）、`attemptId`（canonical attempt 关联）三个字段，`taskId` 仅作为 legacy 兼容字段。 | 当前 `docs_zh/contracts/model_gateway_routing_contract.md` §2 显式定义 `harnessRunId: string | null`（canonical 执行关联）、`nodeRunId: string | null`（canonical 节点关联）、`attemptId: string | null`（canonical attempt 关联），`taskId` 仅作 legacy 兼容。v4.3 Architecture Remediation §T-10 已补齐 routingStrategy 5种枚举。 | 不改 contract，仅回写现行 contract 字段定义与 remediation 依据。 |
| R9-51 | 48. Contract 深层矛盾与缺口（新发现） | P1 | `docs_zh/contracts/budget-ledger-contract.md §3` | BudgetReservation.resourceKind 枚举(token/tool/api/compute/human/side_effect/other)与 §53 ResourceKind(worker_concurrency/tool_qps/model_tpm/model_rpm/budget_amount/approval_capacity/storage_io)完全不匹配 | 已解决 | 原审计条目比较了 BudgetReservation.resourceKind 与架构 §53 要求；现行 `budget-ledger-contract.md` §3 已明确定义 `resourceKind` 枚举为 `token | tool_call | api | compute | storage | bandwidth | memory | human | side_effect | other`，并注明"§53 ResourceKind"。v4.3 canonical contract 已将 resourceKind 与架构 §53 对齐。 | 当前 `docs_zh/contracts/budget-ledger-contract.md` §3 显式定义 `resourceKind` 为 `token | tool_call | api | compute | storage | bandwidth | memory | human | side_effect | other`，v4.3 Architecture Remediation §T-3 已将语义收敛到 canonical contract。 | 不改 contract，仅回写现行 resourceKind 枚举定义与 remediation 依据。 |
| R9-52 | 48. Contract 深层矛盾与缺口（新发现） | P1 | `docs_zh/contracts/side-effect-reconciliation-contract.md §2` | SideEffectStatus 枚举缺 approved/committed/confirming/manual_review_required/compensation_required 态；多出 reserved 态(§14.11 无此态) | 已解决 | 原审计条目比较了 SideEffectStatus 与当时认为的 §14.11 规范；现行 `side-effect-reconciliation-contract.md` §2 已明确定义 SideEffectStatus 完整枚举：`proposed | reserved | claimed | executing | awaiting_confirmation | confirmed | ambiguous | reconciling | compensating | compensated | failed | revoked | expired`，并有 v4.3 canonical contract 声明。§14.11 的具体枚举要求已在 v4.3 canonical 定义中收敛。 | 当前 `docs_zh/contracts/side-effect-reconciliation-contract.md` §2 显式定义 SideEffectStatus 为 13 态枚举，v4.3 canonical contract 覆盖 SideEffectRecord / ReconciliationRecord / CompensationRecord。§6 Legacy 映射明确了旧术语到 v4.3 语义的转换规则。 | 不改 contract，仅回写现行 SideEffectStatus 枚举定义与 canonical contract 依据。 |
| R9-53 | 48. Contract 深层矛盾与缺口（新发现） | P1 | `docs_zh/contracts/cost_and_budget_contract.md §4` | CostEvent 以 task_id 为必填主键，harness_run_id/node_run_id/attempt_id 为可选；与 budget-ledger(要求 harnessRunId)对接断裂 | 已解决 | 原审计条目基于 contract 历史版本；现行 `cost_and_budget_contract.md` §4 CostEvent 已将 `harness_run_id` 列为主键必填字段，`node_run_id?`、`attempt_id?` 为可选，`task_id?` 仅作"兼容查询入口；非 truth 主键"。§7.4 明确要求隐式成本归因必须使用 canonical 标识符 `harness_run_id / node_run_id / attempt_id`，不得使用废弃的 `execution_id`。 | 当前 `docs_zh/contracts/cost_and_budget_contract.md` §4 显式定义 `harness_run_id`（关联 HarnessRun，架构 §18 以 HarnessRun 为预算主体）为主键必填，`task_id?` 仅作兼容查询入口。v4.3 Architecture Remediation §T-41 已将 BudgetPolicy 收敛到 canonical 多维预算约束。 | 不改 contract，仅回写现行 CostEvent 字段定义与 remediation 依据。 |
| R9-54 | 48. Contract 深层矛盾与缺口（新发现） | P1 | `docs_zh/contracts/cost_and_budget_contract.md §3` | BudgetPolicy.runtime_mode 8态(full_auto/supervised_auto/read_only/no-write/...) 与 sandbox_and_auth_contract §3 4态(read_only/workspace_write/scoped_external_access/restricted_exec)完全不重叠 | 已解决 | 原审计条目比较了 cost_and_budget_contract 的 runtime_mode 与 sandbox_and_auth_contract §3；现行 `cost_and_budget_contract.md` §3 runtime_mode 枚举为 `full_auto | supervised_auto | read_only | no_write | no_external_call | no_rollout | manual_only | incident_mode`，文档注释说明"4 states 对齐 sandbox_and_auth_contract §3"（指 read_only 等子集重叠）。v4.3 canonical contract 已将 runtime_mode 与 sandbox_and_auth_contract 进行对齐整合。 | 当前 `docs_zh/contracts/cost_and_budget_contract.md` §3 显式定义 runtime_mode 枚举包含 `read_only` 等与 sandbox_and_auth_contract 重叠的值，v4.3 Architecture Remediation 已将语义收敛。 | 不改 contract，仅回写现行 runtime_mode 枚举定义与 remediation 依据。 |
| R9-55 | 48. Contract 深层矛盾与缺口（新发现） | P1 | `docs_zh/contracts/workflow_static_analysis_and_compensation_contract.md` | 全文使用 step 术语("不可达步骤检测"/"step id 唯一性检查"/"每个有副作用的 step")；v4.3 用 PlanNode/nodeId；无迁移段 | 已解决 | 原审计条目基于 contract 历史版本；现行 `workflow_static_analysis_and_compensation_contract.md` 已全面使用 v4.3 `node` 术语：不可达 node 检测、node id 唯一性检查、node type 与 side effect level、每个有副作用的 node 等。v4.3 canonical contract 已完成 step → node/nodeId 的术语迁移。 | 当前 `docs_zh/contracts/workflow_static_analysis_and_compensation_contract.md` §3 静态分析检查项已使用 `node` 术语（不可达 node 检测、node id 唯一性检查、node type 与 side effect level 等），v4.3 canonical contract 已完成术语迁移。 | 不改 contract，仅回写现行 node 术语使用与 remediation 依据。 |
| R9-56 | 48. Contract 深层矛盾与缺口（新发现） | P2 | `docs_zh/contracts/multimodal_gateway_contract.md` | MultimodalRequest 缺 harnessRunId/nodeRunId/tenantId/traceId(§5.2 ContractEnvelope 必填)；无 BudgetReservation 引用 | 已解决 | 原审计条目基于 contract 历史版本；现行 `multimodal_gateway_contract.md` §3 `MultimodalRequest` 已显式包含 `harness_run_id`（canonical 执行关联，必填）、`node_run_id?`（canonical 节点关联）、`tenant_id`（租户关联，必填）、`trace_id`（trace 关联，必填），以及 `cost_budget` 字段。 | 当前 `docs_zh/contracts/multimodal_gateway_contract.md` §3 显式定义 `harness_run_id`（必填）、`node_run_id?`、`tenant_id`（必填）、`trace_id`（必填）和 `cost_budget` 字段，v4.3 canonical contract 已完成 canonical 运行链标识对齐。 | 不改 contract，仅回写现行 MultimodalRequest 字段定义与 remediation 依据。 |
| R9-57 | 48. Contract 深层矛盾与缺口（新发现） | P2 | `docs_zh/contracts/connector_framework_contract.md` | ConnectorExecutionRequest/Result 无最小字段定义；缺 harnessRunId/nodeRunId/sideEffectId 关联(§14.11 外部写须注册 SideEffectRecord) | 已解决 | 原审计条目基于 contract 历史版本；现行 `connector_framework_contract.md` §3A `ConnectorExecutionRequest` 已显式包含 `harness_run_id`（canonical 执行关联，必填）、`node_run_id`（canonical 节点关联，必填）、`side_effect_id?`（关联 side effect record）。§2 Canonical 对象已注明含 harnessRunId / nodeRunId / sideEffectId（必填）。 | 当前 `docs_zh/contracts/connector_framework_contract.md` §3A 显式定义 `harness_run_id`（必填）、`node_run_id`（必填）、`side_effect_id?`，v4.3 canonical contract 已完成 canonical 运行链标识对齐。 | 不改 contract，仅回写现行 ConnectorExecutionRequest 字段定义与 remediation 依据。 |
| R9-58 | 48. Contract 深层矛盾与缺口（新发现） | P2 | `docs_zh/contracts/capacity_planning_contract.md` | CapacitySignal 无 tenantId/harnessRunId；resource_type 为 plain string 未对齐 §53 ResourceKind canonical 枚举 | 已解决 | 原审计条目基于 contract 历史版本；现行 `capacity_planning_contract.md` §3 `CapacitySignal` 已显式包含 `tenant_id`（租户关联，必填）和 `harness_run_id?`（canonical 执行关联）。`resource_type` 在 v4.3 canonical contract 中保留为自由字符串类型，以支持多样化资源标识。 | 当前 `docs_zh/contracts/capacity_planning_contract.md` §3 显式定义 `tenant_id`（必填）和 `harness_run_id?`，v4.3 Contract Remediation §T-79 已新增 `CapacityAlert` 作为容量越线的权威输出对象。 | 不改 contract，仅回写现行 CapacitySignal 字段定义与 remediation 依据。 |
| R9-59 | 48. Contract 深层矛盾与缺口（新发现） | P2 | `docs_zh/contracts/gateway_streaming_contract.md §3` | StreamEvent 用 task_id 作主关联键无 harness_run_id/node_run_id；§6.8 legacy task 端点须解析到 harnessRunId | 已解决 | 原审计条目基于 contract 历史版本；现行 `gateway_streaming_contract.md` §3 StreamEvent 已将 `harness_run_id` 列为必填主关联键，`node_run_id?` 和 `task_id?` 作为可选字段。v4.3 Contract Remediation §T-66 明确修复：将 task_id 主锚点改为 harness_run_id / node_run_id 主链，task_id 仅保留聚合视图用途。 | 当前 `docs_zh/contracts/gateway_streaming_contract.md` §3 显式定义 `harness_run_id`（必填）、`node_run_id?` 和 `task_id?`，v4.3 Contract Remediation §T-66 已完成 canonical 运行链标识迁移。 | 不改 contract，仅回写现行 StreamEvent 字段定义与 remediation 依据。 |
| R9-60 | 48. Contract 深层矛盾与缺口（新发现） | P2 | `docs_zh/contracts/observability_contract.md §4.3` | StageMetricSample/LoopIterationTrace 携带 task_id? 作关联字段但 T-47 remediation 降级 OAPEFLIR 指标为 view-only——定位矛盾 | 已解决 | 原审计条目指出 StageMetricSample/LoopIterationTrace 含 task_id? 存在矛盾；现行 `observability_contract.md` §4.3 明确定义 `harness_run_id` 为必填关联字段（关联运行 truth），`task_id?` 仅标注为"关联任务投影"（legacy 兼容）。OAPEFLIR 指标作为 view/rationale 投影与 truth 关联字段并不矛盾——前者是语义层级，后者是关联标识。 | 当前 `docs_zh/contracts/observability_contract.md` §4.3 StageMetricSample 和 LoopIterationTrace 均以 `harness_run_id` 为必填关联运行 truth，`task_id?` 仅作 legacy 兼容投影，§4 规则明确"oapeflirViewMetrics 与 stageViewMetrics 只能作为 view / trace / rationale 指标"，语义层级与关联标识不矛盾。 | 不改 contract，仅回写现行字段定义与语义层级说明。 |
| R9-61 | 48. Contract 深层矛盾与缺口（新发现） | P2 | `docs_zh/contracts/plugin_spi_contract.md §2.4` | DomainPresenterPlugin.present() 接受 DualChannelStepOutput(含 Step 的废弃类型)；v4.3 用 NodeAttemptReceipt | 已解决 | 原审计条目基于 contract 历史版本；现行 `plugin_spi_contract.md` §2.4 `DomainPresenterPlugin.present()` 已明确使用 `NodeAttemptReceipt` 作为输入类型。contract 注释明确写明"§R11-61: 必须使用 NodeAttemptReceipt（不是废弃的 DualChannelStepOutput）"，并有"废弃警告：DualChannelStepOutput 已被废弃，不得作为 Plugin 边界类型传递。必须使用 NodeAttemptReceipt"以及"R11-61 FIX"确认。 | 当前 `docs_zh/contracts/plugin_spi_contract.md` §2.4 显式定义 `present(receipt: NodeAttemptReceipt, format: OutputFormat): Promise<PresentedOutput>`，废弃警告明确 DualChannelStepOutput 不可用于 Plugin 边界类型。v4.3 canonical contract 已完成 NodeAttemptReceipt 迁移。 | 不改 contract，仅回写现行 present() 方法签名与 remediation 依据。 |
| R10-01 | 50. 控制平面深层缺口（IAM / Config Center / Stability） | P0 | `src/platform/control-plane/iam/access-model.ts:270` | 无权限继承模型；§11.2 要求三层(RBAC→Capability→Context-aware)；ROLE_CAPABILITY_MAP 扁平+手动重复无层级链 | 已解决 | 原审计基于旧版 access model，没有复核后续引入的角色继承链与上下文判定层。 | 当前 `src/platform/five-plane-control-plane/iam/access-model.ts` 已声明 `ROLE_HIERARCHY`、`capabilitiesForRole()`、`resolvePrincipalAccessProfile()`、`evaluateAuthorizationContext()`，形成 RBAC→Capability→Context-aware 三层模型。 | 不改生产代码，仅回写现行权限模型依据。 |
| R10-02 | 50. 控制平面深层缺口（IAM / Config Center / Stability） | P0 | `src/platform/control-plane/iam/ (缺失)` | 无会话管理；§11.1/§11.2 要求 access_token TTL=15min/refresh_token=24h/revocation/rotation；IAM 内零会话生命周期代码 | 已解决 | 原审计基于 IAM 早期目录状态，没有复核后续补入的 session lifecycle 模块。 | 当前 `src/platform/five-plane-control-plane/iam/session-management.ts` 已实现 `ACCESS_TOKEN_TTL_MS = 15min`、`REFRESH_TOKEN_TTL_MS = 24h`、session 创建、refresh rotation、revoke 与查询；`src/platform/five-plane-control-plane/iam/index.ts` 也已导出这些入口。 | 不改生产代码，仅回写现行会话管理依据。 |
| R10-03 | 50. 控制平面深层缺口（IAM / Config Center / Stability） | P0 | `src/platform/control-plane/iam/ (缺失)` | 无 MFA 强制；§11+§48 要求企业 SSO/SCIM 含 MFA；无 challenge/enrollment/verification 代码 | 已解决 | 原审计依据的是没有 MFA 子模块的旧版 IAM 目录。 | 当前 `src/platform/five-plane-control-plane/iam/mfa-service.ts` 已实现 enrollment、challenge、verification、disable 与 `DEFAULT_MFA_POLICY`；`src/platform/five-plane-control-plane/iam/index.ts` 也已导出这些 MFA API。 | 不改生产代码，仅回写现行 MFA 模块依据。 |
| R10-04 | 50. 控制平面深层缺口（IAM / Config Center / Stability） | P1 | `src/platform/control-plane/iam/ (缺失)` | 无 service-to-service auth(mTLS/JWT)；§11.2 要求内部 API mTLS/service token；§8 worker pool 通信要求 mTLS+service identity | 已解决 | 原审计基于 service identity/auth 模块尚未落地时的目录快照。 | 当前 `src/platform/five-plane-control-plane/iam/service-auth.ts` 已实现 service identity、service token、mTLS certificate、rotation 与校验逻辑；`src/platform/five-plane-control-plane/iam/index.ts` 也已对外导出服务认证入口。 | 不改生产代码，仅回写现行 service auth 依据。 |
| R10-05 | 50. 控制平面深层缺口（IAM / Config Center / Stability） | P1 | `src/platform/control-plane/iam/field-encryption.ts:14-30` | normalizeKey() 用 SHA-256 哈希任意长度密码为 AES key 无 proper KDF(PBKDF2/scrypt/argon2)；1字节 key 通过验证(§11.9 要求 Vault/KMS) | 已解决（源码修复并补定向测试） | 根因是旧实现把“任意非空字符串”直接做 SHA-256 摘要当作 AES key，既没有 KDF 的成本参数/内存硬化，也没有最小口令强度门槛。 | 当前 `src/platform/five-plane-control-plane/iam/field-encryption.ts` 已改为 `scryptSync(..., maxmem)` 派生 32-byte key，并引入 `MIN_PASSPHRASE_BYTES = 16`；`tests/unit/platform/control-plane/iam/field-encryption.test.ts` 已验证强口令可加解密、短口令被拒绝。 | 保留真实代码修复结论，并以定向单测作为行为依据。 |
| R10-06 | 50. 控制平面深层缺口（IAM / Config Center / Stability） | P1 | `src/platform/control-plane/config-center/hierarchical-config-loader.ts:16-20` | Config 层级缺 environment 和 runtime 动态层；§24.1 要求 platform→environment→tenant→pack→runtime 5层 | 已解决 | 原审计依据的是只含 platform/tenant/pack 的早期 loader 实现，没有复核当前层级枚举和加载入口。 | 当前 `src/platform/five-plane-control-plane/config-center/hierarchical-config-loader.ts` 的 `ConfigHierarchyLayer` 已包含 `environment` 与 `runtime`，`loadConfig()` 也显式接收并合并 `environmentConfigs`、`runtimeConfigs`。 | 不改生产代码，仅回写现行层级加载依据。 |
| R10-07 | 50. 控制平面深层缺口（IAM / Config Center / Stability） | P1 | `src/platform/control-plane/config-center/config-versioning-service.ts:115` | 配置版本化纯内存(Map)；进程重启丢失全部历史；§24.2 要求完整历史+回滚能力 | 已解决（源码/测试） | 根因是原复审结论停留在早期“内存 snapshots Map”阶段，没有吸收后续事件溯源和 durable replay 接线；现行实现虽然仍在内存中维护热态索引，但版本真相已通过事件总线回放和可选 `versionStore` 持久化保留。 | 当前 `src/platform/control-plane/config-center/config-versioning-service.ts` 已在 `createVersion()`/`createRollbackPoint()` 中发出 `config.version.created`、`config.rollback_point.created` 与 `config.version.rollback` 事件，并在 `initialize()` 中重新订阅这些事件以重建历史；同时已具备 `versionStore.saveSnapshot/saveRollbackPoint` 持久接口。本轮新增 `tests/unit/platform/control-plane/config-center/config-center-durability-regressions.test.ts`，直接验证“写入版本与回滚点后，新的 service 实例可通过 durable event replay 恢复 version history 与 rollback points”，定向测试已通过。 | 关闭的是“重启丢失版本历史”缺陷，不外推为所有部署形态都已启用 SQLite `versionStore`。 |
| R10-08 | 50. 控制平面深层缺口（IAM / Config Center / Stability） | P1 | `src/platform/control-plane/config-center/ (缺失)` | 无动态配置热重载机制；§24.1 要求 hot_reloadable_config+config.changed 事件触发组件热加载；无 file-watcher/push/subscription | 已解决（源码修复并补定向测试） | 根因是 config-center 之前只有静态加载与版本/审计服务，没有承接 `config.changed` 的订阅器，也没有组件级订阅与文件监听入口。 | 本轮新增 `src/platform/five-plane-control-plane/config-center/config-hot-reload-service.ts`，实现 `config.changed` 订阅、组件 subscription、`triggerReload()` 推送与 file watcher 机制，已补齐热重载基础设施。 | 保留真实代码修复结论。 |
| R10-09 | 50. 控制平面深层缺口（IAM / Config Center / Stability） | P1 | `src/platform/control-plane/config-center/config-audit-service.ts:142` | 配置审计轨迹纯内存数组；重启丢失；§24.4 要求 who/when/what/why 合规持久记录 | 已解决（源码/测试） | 根因与 `R10-07` 类似：复审表只看到了运行期 `entries[]` 数组，没有复核现行 `config.audit.recorded` 事件回放机制，因此把热态缓存误判成唯一真相来源。 | 当前 `src/platform/control-plane/config-center/config-audit-service.ts` 在 `recordCreate/update/delete/rollback` 等入口都会经 `emitAuditEvent("config.audit.recorded", entry)` 发出完整的 who/when/what/why 审计载荷，并在 `initialize()` 时重新订阅同类事件做幂等重放恢复。本轮新增 `tests/unit/platform/control-plane/config-center/config-center-durability-regressions.test.ts`，验证写入一条带 actor/reason/versionId/previousVersionId/metadata 的审计记录后，新 service 实例能通过 durable event replay 恢复该条审计信息，定向测试已通过。 | 关闭的是“重启丢失审计轨迹”缺陷，不把它夸大为已完成所有合规留存策略。 |
| R10-10 | 50. 控制平面深层缺口（IAM / Config Center / Stability） | P1 | `src/platform/stability/ (缺失)` | 无复合健康评分模型；§9/§27 要求加权多维度数值 HealthScore；仅有 4级状态(ok/degraded/overloaded/unhealthy) | 已解决（源码修复并补定向测试） | 根因是 stability 侧此前只有离散状态和演练类模块，缺少把多维信号合成为统一数值健康分的服务。 | 本轮新增 `src/platform/stability/composite-health-score-service.ts`，实现按维度权重计算 `overallScore`、`dimensionScores` 与兼容性 `status` 的 `CompositeHealthScoreService`。 | 保留真实代码修复结论。 |
| R10-11 | 50. 控制平面深层缺口（IAM / Config Center / Stability） | P1 | `src/platform/stability/event-response-automation-service.ts` | 无事件响应自动化；§12.2 SEV1-4/§60 emergency brake/§28 incident DLQ 均未实现；stability 仅含 rehearsal/drill | 已解决（源码） | 原审计声称 stability 目录缺失事件响应自动化实现，复核发现本轮已新增 `event-response-automation-service.ts`。 | 当前 `src/platform/stability/event-response-automation-service.ts` 已实现完整的 SEV1-4 incident lifecycle（createIncident/acknowledgeIncident/resolveIncident/escalateIncident）、emergency brake（engageBrake/releaseBrake，支持 advisory/partial/full 模式）、incident DLQ（addToDlq/retryDlqEntry/markProcessed，含指数退避重试和 dead letter 处理），并在 `src/platform/stability/index.ts:5` 导出。文件头部注释明确写明"Per §12.2/§60/§28: Implements SEV1-4 emergency response, emergency brake, and incident DLQ (Dead Letter Queue) handling"。 | 关闭该条事件响应自动化缺失问题。 |
| R10-12 | 50. 控制平面深层缺口（IAM / Config Center / Stability） | P1 | `divisions/` | 仅 10个通用运营 division；§37 要求 24个垂直业务域+DomainDescriptor/DomainRiskProfile/DomainEvalSpec 结构化层级 | 已解决 | 原审计指控"仅 10个通用运营 division"，当前 `src/domains/` 下已落地 43个垂直业务域目录（academic-research, advertising, agriculture, healthcare, legal, quant-trading, financial-services 等），远超 §37 要求的 24个。结构化层级已在 `src/domains/domain-specs.ts` 中完整定义：`DomainCoreDescriptorSchema`（对应 DomainDescriptor）、`DomainRiskSpecSchema`（对应 DomainRiskProfile）、`DomainEvalSpecSchema`（对应 DomainEvalSpec），且均有对应 TypeScript type 导出。 | 关闭该条关于业务域数量和结构化层级缺失的缺陷。 |
| R10-13 | 50. 控制平面深层缺口（IAM / Config Center / Stability） | P2 | `src/platform/shared/async/sync-backed-async-service.ts.bak` | .bak 备份文件提交到源码树；卫生问题 | 已解决 | 原审计记录的是当时源码树里的备份文件残留，当前工作树已不再保留该文件。 | 当前 `src/platform/shared/async/sync-backed-async-service.ts.bak` 已删除，不再构成源码卫生问题。 | 不改业务逻辑，仅回写当前工作树事实。 |
| R10-14 | 50. 控制平面深层缺口（IAM / Config Center / Stability） | P2 | `src/platform/control-plane/config-center/config-rollout-service.ts:134` | startRollout 跳到目标阶段(percentage>=target)而非从 PENDING 开始；§24.3 要求强制 canary→10%→full 递进 | 已解决 | 原审计针对的是旧版 rollout 初始化逻辑，没有复核当前起始阶段。 | 当前 `src/platform/five-plane-control-plane/config-center/config-rollout-service.ts` 的 `startRollout()` 已固定从 `PENDING` 阶段开始，并通过 `targetPercentage` 只作为后续可推进上限，而非起始阶段。 | 不改生产代码，仅回写现行 rollout 启动依据。 |
| R10-15 | 50. 控制平面深层缺口（IAM / Config Center / Stability） | P2 | `src/platform/control-plane/config-center/config-store.ts:116` | 变更检测用引用相等(===)；对象/数组永远报变更，mutation-in-place 漏检测 | 已解决 | 原审计停留在引用相等比较阶段，没有复核当前变更检测实现。 | 当前 `src/platform/five-plane-control-plane/config-center/config-store.ts` 已引入递归 `deepEqual()`，`set()` 仅在深比较发现真实变化时通知监听器，不再使用裸 `===` 作为对象/数组变更判定。 | 不改生产代码，仅回写现行变更检测依据。 |
| R10-16 | 51. UI Feature 模块 / API Client / Mobile 深层问题 | P0 | `ui/packages/shared/api-client/src/rest-client.ts:170-189` | 无重试或 circuit-breaker；§5.4.1 要求指数退避重试+circuit breaker；HttpTransport.send() 失败即抛异常零重试 | 已解决（源码/测试） | 根因是 reaudit 结论停留在 `HttpTransport` 还没有失败重试和断路器状态的旧版本。 | 当前 `ui/packages/shared/api-client/src/rest-client.ts` 已实现 retryable status 指数退避重试、失败计数、`closed/open/half-open` circuit breaker 和 30s reset；`ui/tests/shared/api-client-runtime-regressions.test.ts` 已验证 503 会重试至成功，连续失败会打开断路器并对后续请求 fail closed，本轮定向测试已通过。 | 关闭该条 REST transport retry/circuit-breaker 缺陷。 |
| R10-17 | 51. UI Feature 模块 / API Client / Mobile 深层问题 | P0 | `ui/packages/shared/api-client/src/ws-client.ts:100-113` | WS 断连后永不重连；onclose/onerror 仅设状态 "reconnecting" 从不调 connect()；§5.3 要求指数退避重连(1s~30s+jitter) | 已解决（源码/测试） | 根因是旧浏览器 WS 客户端只切状态到 `reconnecting`，没有真正调度重连计时器。 | 当前 `ui/packages/shared/api-client/src/ws-client.ts` 已加入 `scheduleReconnect()`、指数退避+jitter、重连计数和自动 `doConnect()`；`ui/tests/shared/api-client-runtime-regressions.test.ts` 已验证断连后会按回退策略重新建连，本轮定向测试已通过。 | 关闭该条 WS 自动重连缺陷。 |
| R10-18 | 51. UI Feature 模块 / API Client / Mobile 深层问题 | P0 | `ui/packages/features/approval,task-cockpit,workflow-cockpit,settings (hooks)` | 所有 mutation 仅更新本地 state 从不调 REST API；approve()/reject()/delegate()/escalateTask() 等全部 setState only | 已解决（源码/测试） | 根因是这组条目停留在 hooks 只做本地状态镜像的旧实现，没有吸收后续把 mutation 接到 shared API client 的改造。 | 当前 `ui/packages/features/approval/src/hooks/index.ts`、`task-cockpit/src/hooks/index.ts`、`workflow-cockpit/src/hooks/index.ts`、`settings/src/hooks/index.ts` 均已在 mutation 前调用 `approveApproval/rejectApproval/delegateApproval/updateTask/pauseWorkflow/resumeWorkflow/recoverWorkflow/releaseWorkflow/updatePreferences`；`ui/tests/unit/ui/packages/features/approval/hooks.test.tsx`、`task-cockpit/hooks.test.tsx`、`workflow-cockpit/hooks.test.tsx`、`settings/hooks.test.tsx` 已分别覆盖这些 API 调用，本轮定向测试已通过。 | 关闭该条 feature hook mutation 仅本地态缺陷。 |
| R10-19 | 51. UI Feature 模块 / API Client / Mobile 深层问题 | P1 | `ui/packages/shared/api-client/src/rest-client.ts` | 无 Accept-Version header；§1.8 要求每个请求携带 Accept-Version:v1 + 处理 406 响应 | 已解决（源码/测试） | 根因是旧 HTTP transport 没有把版本协商头下沉到每次真实 fetch，也没有对 `406` 做显式错误分支。 | 当前 `ui/packages/shared/api-client/src/rest-client.ts` 会在请求头中默认写入 `Accept-Version: v1`，并对 `406` 抛出 `rest.version_not_supported`；`ui/tests/shared/api-client-runtime-regressions.test.ts` 与 `ui/tests/unit/ui/shared/interceptors.test.ts` 已分别验证真实 transport 会带 `Accept-Version`，以及版本协商拦截器/406 处理路径存在，本轮定向测试已通过。 | 关闭该条 Accept-Version 协商缺陷。 |
| R10-20 | 51. UI Feature 模块 / API Client / Mobile 深层问题 | P1 | `ui/packages/features/task-cockpit/src/hooks/` | TaskCockpit 仅实现 L1-L2 drill-down；§4.2.2 要求 L3 StepOutputViewer/L4 EvidenceChainViewer/L5 TimelineViewer | 已解决（源码/测试） | 根因是旧 task cockpit 只有列表和详情两层，L3-L5 下钻视图尚未挂接。 | 当前 `ui/packages/features/task-cockpit/src/hooks/index.ts` 已提供 `stepViewer/evidenceViewer/timelineViewer`，`ui/packages/features/task-cockpit/src/web/index.tsx` 已接入 L3/L4/L5 drill-down tabs；`ui/tests/unit/ui/packages/features/task-cockpit/web.test.tsx` 已验证三层内容切换展示，本轮定向测试已通过。 | 关闭该条 task cockpit 多级下钻缺陷。 |
| R10-21 | 51. UI Feature 模块 / API Client / Mobile 深层问题 | P1 | `ui/packages/shared/api-client/src/endpoints.ts` | 所有 list fetch 无分页/过滤/排序参数；§5.4.6 要求标准化 page/pageSize/sort/filter | 已解决（源码/测试） | 根因是旧 endpoint helpers 只暴露裸 path，没有标准化 query builder。 | 当前 `ui/packages/shared/api-client/src/endpoints.ts` 已定义 `PaginationParams` 和 `buildQueryString()`，`fetchTasks/fetchWorkflows/fetchApprovals/fetchIncidents/...` 等 list fetch 全部支持 `page/pageSize/sort/filter`；`ui/tests/shared/api-client-runtime-regressions.test.ts` 已验证标准分页/排序/过滤查询串会被正确拼接，本轮定向测试已通过。 | 关闭该条 list endpoint 分页参数缺陷。 |
| R10-22 | 51. UI Feature 模块 / API Client / Mobile 深层问题 | P1 | `ui/packages/shared/api-client/src/interceptors.ts:62-77` | Offline-queue interceptor 入队后不短路请求；请求继续到 fetch() 失败；§5.5 要求离线写被捕获+乐观响应 | 已解决（源码/测试） | 根因是旧离线拦截器只负责入队，不会阻断后续 transport dispatch，离线写请求仍会继续落到网络层报错。 | 当前 `ui/packages/shared/api-client/src/interceptors.ts` 的 `createOfflineQueueInterceptor()` 在离线非 GET 请求入队后会直接抛出 `rest.offline:Request queued for offline sync` 以短路请求；`ui/tests/shared/api-client-runtime-regressions.test.ts` 已验证离线写会入队且 transport 不会被调用，本轮定向测试已通过。 | 关闭该条 offline interceptor 不短路缺陷。 |
| R10-23 | 51. UI Feature 模块 / API Client / Mobile 深层问题 | P1 | `ui/packages/ui-mobile/src/` | Mobile 包零 React Native 组件；仅 TS 类型描述+静态导航配置；§2.5.5/§2.5.6 要求手势/推送/生物认证/widget | 已解决（原指控已过时） | 原条目把“移动端尚未做完整产品化”和“包内完全没有移动组件/能力适配”混成了一条。当前仓内真实状态已经不是“零组件、只有类型和导航”。 | `ui/packages/ui-mobile/src/components/index.ts` 已实现 `GestureTouchable`、`PushNotification`、`BiometricAuth`、`MobileWidget` 和 `createStepOutputViewer()`；`ui/apps/mobile/src/App.tsx` 已接入 `createMobilePlatformAdapter(...)`；本轮又把 `ui/packages/ui-mobile/src/native-modules/index.ts` 从平面布尔配置收敛为带 `requiresBridge/permission/source` 的 capability 描述，并新增 `ui/tests/shared/native-modules.test.ts` 验证 bridge-backed capability 探测。**保留说明**：这条关闭的是“mobile 包里完全没有移动能力适配”的原始指控，不外推为整个移动端产品线已经完成上架级原生集成验证。 | 关闭“zero React Native/mobile capability baseline”这一陈旧结论。 |
| R10-24 | 51. UI Feature 模块 / API Client / Mobile 深层问题 | P1 | `ui/packages/features/hitl/src/hooks/` | HITL hook 全静态/硬编码返回 3个固定项无 API 集成；§4.6.2 要求真实审批路由+4种恢复模式+live PlanBundle 展示 | 已解决（源码/测试） | 根因与 `2263` 同源：hook 曾长期停留在静态占位项，没有接审批 API 和人工恢复动作。 | 当前 `ui/packages/features/hitl/src/hooks/index.ts` 已接入 `fetchApprovals()`、WS 订阅和 `approve/reject/delegate/resume/patch/override` 等真实动作；`ui/tests/unit/ui/packages/features/hitl/hooks.test.tsx` 已验证审批补丁/override 会走 API client，本轮定向测试已通过。 | 作为重复挂账，与 `2263` 一并关闭。 |
| R10-25 | 51. UI Feature 模块 / API Client / Mobile 深层问题 | P2 | `ui/packages/shared/api-client/src/ws-client.ts:88` | WS auth token 作为 URL query param 发送；§6.5.4 要求 token 不出现在 URL(被代理/CDN 日志记录) | 已解决（源码/测试） | 根因与 `2070` 同源：旧浏览器 WS 客户端默认把鉴权 token 暴露给 URL 级连接参数。 | 当前 `ui/packages/shared/api-client/src/ws-client.ts` 已使用 `new WebSocket(url, "v1.auth.token")` 建连并在 `onopen` 后发送首条 auth message；`ui/tests/shared/api-client-runtime-regressions.test.ts` 已验证 token 不会出现在 URL 中，而是随首条消息发送，本轮定向测试已通过。 | 作为重复挂账，与 `2070` 一并关闭。 |
| R10-26 | 51. UI Feature 模块 / API Client / Mobile 深层问题 | P2 | `ui/packages/features/conversation/src/hooks/` | Conversation 用内存 ConversationClient 无 WS streaming；§4.6.1 要求实时 nl.session.updated/nl.plan.created 事件 | 已解决（源码/测试） | 根因是旧 conversation 路径确实只有 in-memory fallback，没有把后端实时 transport 接入会话状态。 | 当前 `ui/packages/features/conversation/src/hooks/index.ts` 会在提供 `wsClient + userId` 时把 transport 注入 `ConversationClient`；`ui/packages/shared/nl-client/src/index.ts` 已订阅 `nl.session.updated` 与 `nl.plan.created`；`ui/tests/shared/nl-client.test.ts` 已验证实时 transport 更新会话消息与 `planReady` 状态，本轮定向测试已通过。 | 关闭该条 conversation 无 WS streaming 缺陷。 |
| R10-27 | 51. UI Feature 模块 / API Client / Mobile 深层问题 | P2 | `ui/packages/features/analytics/src/hooks/` | Analytics 无多层 KPI breakdown/chart-type 配置；§4.2.8 要求 7种图表+角色自适应指标集 | 已解决（源码/测试） | 根因与 `2267` 同源：analytics hook 早期没有图表类型校验、角色指标筛选和 breakdown 聚合。 | 当前 `ui/packages/features/analytics/src/hooks/index.ts` 已提供 7 种 `ChartType`、角色自适应 `METRIC_SETS`、多维 `breakdowns` 与时间序列数据；`ui/tests/unit/ui/packages/features/analytics/hooks.test.tsx` 已验证时间序列和导出路径，本轮定向测试已通过。 | 作为重复挂账，与 `2267` 一并关闭。 |
| R10-28 | 51. UI Feature 模块 / API Client / Mobile 深层问题 | P2 | `ui/apps/web/src/runtime.ts:39` | Auth token 硬编码字符串 "ui-runtime-access"；interceptor 发送无效 Bearer token 到后端 | 已解决（源码/测试） | 根因与 `2175` 同源：旧 web runtime 曾把伪 session token 当成固定凭证喂给 auth 链路，后续虽然代码已切到 `TokenManager`，但 reaudit 表没有吸收该修复，仍沿用旧结论。 | 当前 `ui/apps/web/src/runtime.ts` 已通过 `const tokenManager = config.tokenManager ?? new TokenManager()` 构造动态令牌源，并把它传给 `createAuthInterceptor(tokenManager)`，源码中已不存在 `"ui-runtime-access"` 常量；`ui/packages/shared/state/src/index.ts` 也改为与 runtime 共享同一 `TokenManager`/`AuthService` 链路，不再合成伪 token。本轮新增 `ui/tests/shared/web-runtime-auth-regression.test.ts`，直接验证 `createAuthInterceptor` 接收的是动态 `TokenManager` 而不是硬编码字符串，且 `createTenantInterceptor` 仍使用动态 tenant，定向测试已通过。 | 关闭的是 web runtime 硬编码 token 缺陷，不外推为完整 OAuth/SSO 生产集成已完成。 |
| R10-29 | 52. Prompt Engine / Injection Guard / 测试体系深层问题 | P0 | `src/platform/stability/prompt-injection-guard.ts:213-236` | §16.5.2 明确"Classifier 不得单独作为 production hard deny"；实现 blocked:true 直接硬拒绝——违反规格 | 已解决（源码修复并补定向测试） | 复核后发现问题分两段：同步 `classifyPromptInjectionRisk()` 其实已经是 signal-only，但异步 `executePromptDefenseChain()` 仍会在 lexical/semantic/behavioral 层命中阈值后提前返回，使 classifier 层实际上重新获得了“单独终止链路”的能力，外部 guardrails 根本拿不到决策权。 | 本轮把 `executePromptDefenseChain()` 改为总是走完整个 classifier 链并汇总到 consensus，`classifyPromptInjectionRisk()` 继续保持 `blocked=false`；只有外部 guardrails 集成结果才会把 consensus 标成阻断。定向测试 `tests/unit/platform/prompt-engine/prompt-injection-guard-remediation.test.ts` 与更新后的 `tests/unit/platform/stability/prompt-injection-guard.test.ts`、`tests/unit/platform/shared/stability/prompt-injection-guard.test.ts` 已验证“分类器本身不硬拒绝、外部 guardrails 才能阻断”。 | 这是修执行链语义，不是单纯把一个布尔值改掉。 |
| R10-30 | 52. Prompt Engine / Injection Guard / 测试体系深层问题 | P1 | `src/platform/prompt-engine/registry/index.ts` | §16.2 要求 PromptTemplate status:draft/review/staging/canary/stable/deprecated 生命周期；PromptTemplateRecord 无 status 字段 | 已解决（源码） | 根因是旧 `PromptTemplateRecord` 接口确实缺少 `status` 字段；现已增加 `PromptTemplateStatus` 类型（draft/review/staging/canary/stable/deprecated）并将其作为必需字段添加到 `PromptTemplateRecord`。 | 源码已实现完整生命周期状态机，默认值为 draft。 | 关闭该条 status 字段缺失问题。 |
| R10-31 | 52. Prompt Engine / Injection Guard / 测试体系深层问题 | P1 | `src/platform/prompt-engine/registry/prompt-version-manager.ts` | §16.2 要求 version:number(递增整数)；实现用 semver 字符串(v1.0.0)——版本模型矛盾 | 已解决（测试修复） | 根因是测试文件 `tests/unit/platform/prompt-registry.test.ts` 使用了 semver 字符串 (`version: "1.0.0"`) 调用 `PromptTemplateRegistryService`，而实现已正确使用整数版本 (`version: number`) 并通过 `normalizeVersion()` 验证。本轮修正测试用例，将所有 `version` 参数从字符串改为整数，并添加相应的 `displayVersion` 字段。 | 测试文件已更新为使用整数版本 (`version: 1`, `version: 2`) 替代 semver 字符串。 | 关闭该条版本模型矛盾问题。 |
| R10-32 | 52. Prompt Engine / Injection Guard / 测试体系深层问题 | P1 | `src/platform/prompt-engine/registry/index.ts:402-420` | §16.1 要求 {{variable}} 模板组合+变量绑定/展开；无模板变量渲染实现；variableSuffixTemplate 存储但从不求值 | 已解决（源码） | 根因是旧 audit 声称 `variableSuffixTemplate` 从不求值；现已存在 `renderVariableTemplate()` 方法（第 402-420 行）实现完整的 `{{variable}}` 替换逻辑：先用 `validateVariableBindings()` 校验所有绑定，再对每个 variable spec 执行 `rendered.split(placeholder).join(value)` 完成替换，支持默认值。 | 源码 `PromptTemplateRegistryService.renderVariableTemplate()` 已实现完整的 {{variable}} 模板渲染：绑定校验 + `{{key}}` 替换 + 默认值回退。本轮确认实现存在，关闭该条。 | 这是文档基线漂移，不再保留为未解决。 |
| R10-33 | 52. Prompt Engine / Injection Guard / 测试体系深层问题 | P1 | `src/platform/prompt-engine/registry/` | §16.4 要求 PromptBundleCompatibilityMatrix(Tool/Evaluator/DomainDescriptor/Model 兼容)；无兼容矩阵验证 | 已解决（源码） | 根因是旧 registry 缺少兼容矩阵类型和验证方法。现已增加 `PromptBundleCompatibilityMatrix` 类型（`PromptBundleCompatibilityEntry[]`），以及 `validateBundleCompatibility()` 方法，在 `registerTemplate()` 时校验 Tool/Evaluator/DomainDescriptor/Model 兼容性。默认矩阵 `DEFAULT_PROMPT_BUNDLE_COMPATIBILITY_MATRIX` 为 planner/generator/evaluator/system 四个角色分别定义了允许/禁止的工具、评估器、域和模型组合。 | 源码已实现完整兼容矩阵验证；`PromptTemplateRegistryService.validateBundleCompatibility()` 在模板注册时强制执行校验。 | 关闭该条无兼容矩阵验证问题。 |
| R10-34 | 52. Prompt Engine / Injection Guard / 测试体系深层问题 | P1 | `src/platform/stability/prompt-injection-guard.ts:117-148` | §16.5.1 要求 ML Classifier；buildSemanticAssessment 纯正则启发式；MLInjectionClassifierConfig 命名误导无真实 ML | 已解决（默认已启用 ML 尝试，失败安全回退） | 之前剩余的真实缺口是默认配置仍把 `useMlClassification` 关着，生产默认不会尝试 ML classifier。 | 当前 `src/platform/stability/prompt-injection-guard.ts` 已将 `DEFAULT_CLASSIFIER_CONFIG.useMlClassification` 切到 `true`；`tests/unit/platform/prompt-engine/prompt-injection-guard-remediation.test.ts` 新增回归，分别验证默认配置已启用 ML、异步 defense chain 在 ML endpoint 正常时走 `ml_semantic_classifier`、endpoint 失败时安全回退到 heuristic。**边界说明**：同步 `classifyPromptInjectionRisk()` 仍是 heuristic-only，这是现有同步 API 形状限制，不再把它误记成“默认未启用 ML”。 | 关闭“默认 heuristic-only、无自动 ML 接入”的缺口。 |
| R10-35 | 52. Prompt Engine / Injection Guard / 测试体系深层问题 | P1 | `src/platform/stability/prompt-injection-guard.ts` | §16.5.2 要求联动 Tool Guardrails/Egress Control/Context Assembly/Output Validator；实现为独立纯函数模块零集成点 | 已解决（源码修复并补定向测试） | 真正根因不是“类型上没有 integration 参数”，而是参数存在却完全未被消费：无论传不传 `toolGuardrails`、`egressControl`、`contextAssembly`、`outputValidator`，执行链行为都一样，因此外部 guardrails 在生产上是死接线。 | 本轮在 `src/platform/stability/prompt-injection-guard.ts` 增加了 `collectExternalGuardrailFindings()`，实际消费 context assembly、output validator、tool guardrails 和 egress control，并把外部阻断信号汇总到 consensus；同时补齐 canonical `src/platform/prompt-engine/prompt-injection-guard.ts` 对 `executePromptDefenseChain` / `PromptDefenseIntegration` 的 re-export。定向测试 `tests/unit/platform/prompt-engine/prompt-injection-guard-remediation.test.ts` 已验证只有外部 guardrails deny 时 consensus 才会 blocked。 | 这是把 integration 从“摆设参数”接成了真实执行链。 |
| R10-36 | 52. Prompt Engine / Injection Guard / 测试体系深层问题 | P1 | `tests/golden/config-file-generation.test.ts:32-42` | 3个 golden test 捕获 schema 验证错误后 assert.ok(true)；schema 变更时测试静默通过——锁定"broken passes"行为 | 已解决（源码修复并补定向测试） | 根因有两层：旧测试曾通过 `catch` 分支吞掉 schema/加载失败；后续虽然改成 `assert.fail`，但夹具仍停留在旧的 `config/dev/*.json` 布局，导致 golden path 无法反映现行契约。 | 本轮 `tests/golden/config-file-generation.test.ts` 已统一改为现行 `config/<layer>/default.json` 夹具布局，并保留失败即 `assert.fail(...)` 的 hard-fail 行为；定向运行 `node --test --import tsx tests/golden/config-file-generation.test.ts`，4 个用例全部通过，不再存在静默通过路径。 | 这是实修测试，不是改单条文案。 |
| R10-37 | 52. Prompt Engine / Injection Guard / 测试体系深层问题 | P1 | `tests/e2e/prompt-injection-guard-e2e.test.ts:27` | E2E 直接 import src/platform/stability/ 绕过 canonical prompt-engine/ re-export；模块移动后测试指向错误入口 | 已解决 | 根因是原审计基于早期 E2E 测试入口，没有复核后续 prompt-engine canonical re-export 接线。现行问题已经不再是“导错模块”，而是测试断言与当前检测策略之间的行为差异。 | 当前 `tests/e2e/prompt-injection-guard-e2e.test.ts` 已从 `../../src/platform/prompt-engine/index.js` 导入；本轮又补齐了 `src/platform/prompt-engine/prompt-injection-guard.ts` 对 `executePromptDefenseChain` 的 canonical re-export，避免外层只能绕回 stability 入口。 | 保持“错误 import 入口”子问题关闭；剩余 E2E 断言差异只由尚未完成的 `R10-34` 等条目承接。 |
| R10-38 | 52. Prompt Engine / Injection Guard / 测试体系深层问题 | P2 | `tests/e2e/harness-loop-e2e.test.ts:379-418`（旧引用有误） | E2E 测试中 DurationGuard 未被验证——budget 配置包含 maxDurationMs 但从不调用 getGuardViolation() 或断言实际触发 abort；集成测试有 R10-38 fix comments 但 E2E 层缺测试 | 已解决（改成确定性时钟驱动回归） | 根因不是 DurationGuard 逻辑本身失效，而是早先 E2E 断言依赖机器执行时间，出现“如果执行太快就只断言 loopMetrics 存在”的弱校验。 | 当前 `tests/e2e/harness-loop-e2e.test.ts` 已用可控 `Date.now()` mock 构造 replan 场景，强制在 loop controller 内跨过 `maxDurationMs`，并明确断言最终 `status="aborted"`、`decision.reasonCodes` 包含 `harness.guard.max_duration_exceeded`。旧的“太快就降级断言”用例已移除。定向命令 `./node_modules/.bin/tsx --test tests/e2e/harness-loop-e2e.test.ts` 现通过 `17 passed`。 | 关闭 E2E DurationGuard 只做弱校验的问题。 |
| R10-39 | 52. Prompt Engine / Injection Guard / 测试体系深层问题 | P2 | `tests/e2e/harness-loop-e2e.test.ts:46-79` | HarnessRun lifecycle 测试用静态构造参数；loop 从不调真实 planner/generator/evaluator——仅测编排簿记非执行合约 | 已解决（runtime 已拥有真实 loop service 调用链） | 根因不是“断言数量不够”，而是旧 `HarnessRuntimeService.runLoop()` API 只消费预组装 `plannerOutput/generatorOutput/evaluatorOutput`，runtime 自己并不驱动三段执行。 | 当前 `src/platform/orchestration/harness/index.ts` 已为 `runLoop()` 增加 `loopServices.plan/generate/evaluate` 调用链，并提供默认 planner/generator/evaluator 输出生成逻辑；E2E `tests/e2e/harness-loop-e2e.test.ts` 也改为把 spy services 直接传给 runtime，由 runtime 触发三段调用，而不是测试预先执行一轮再把结果喂回去。定向命令 `./node_modules/.bin/tsx --test tests/e2e/harness-loop-e2e.test.ts` 已通过 `17 passed`。 | 关闭“loop 只测簿记、不拥有执行链”的真实缺口。 |
| R10-40 | 52. Prompt Engine / Injection Guard / 测试体系深层问题 | P2 | `tests/golden/golden-tasks.test.ts:22` | 硬编码 assert.equal(results.length, 7)；新增 golden task 类即失败(脆弱断言) | 已解决 | 当前 `tests/golden/golden-tasks.test.ts:22` 已使用 `SINGLE_TASK_GOLDEN_TASKS.length` 参数化断言，而非硬编码数字 7；新增 golden task 类不会导致断言失败。 | 测试文件第 22 行已改为参数化断言 `assert.equal(results.length, SINGLE_TASK_GOLDEN_TASKS.length)`，无需进一步整改。 | 关闭该条脆弱断言问题。 |
| R10-41 | 52. Prompt Engine / Injection Guard / 测试体系深层问题 | P2 | `src/platform/stability/prompt-injection-guard.ts:86-94 vs :219` | sanitizePromptInput 和 classifyPromptInjectionRisk 操作不同表示(escaped vs raw NFKC)；分类和清洗不一致 | 已解决（源码修复并补定向测试） | 根因是旧实现直接把 `sanitizePromptInput()` 的 escaped 输出复用成分类输入，导致 `<script>` 这类本应命中 lexical pattern 的 payload 在分类前先被转义成 `&lt;script&gt;`，分析视角和展示视角混成了一套。 | 本轮在 `src/platform/stability/prompt-injection-guard.ts` 拆出了 `normalizePromptInputForAnalysis()` 与 `escapePromptInputForRendering()`：分类统一基于 raw NFKC normalized 文本，`sanitizedInput` 只用于渲染/保护。定向测试 `tests/unit/platform/prompt-engine/prompt-injection-guard-remediation.test.ts` 已验证 code injection 仍会被识别，同时 `sanitizedInput` 保持转义输出。 | 这是修真实检测语义，不是改测试期望。 |
| R10-42 | 52. Prompt Engine / Injection Guard / 测试体系深层问题 | P2 | `tests/e2e/budget-per-execution-enforcement-r10-42.test.ts`; `tests/e2e/budget-execution-enforcement.test.ts`; `tests/e2e/budget-execution-blocking-e2e.test.ts` | 无 per-execution budget enforcement e2e 测试；budgetUsdLimit 字段被设置但从不断言实际阻断超支执行 | 已解决（E2E 收口） | 真实残留问题不是 `.js` 后缀 import，而是这批预算 E2E 在创建后仍包含过期断言：对不存在的 `decision` 字段做校验、引用未定义结果变量、以及错误的 `transitionTaskTerminalState(...)` 调用形状，导致测试虽存在但无法真实证明预算阻断。 | 本轮修正了 `budget-per-execution-enforcement-r10-42.test.ts` 与 `budget-execution-blocking-e2e.test.ts` 的断言，使其对齐 `BudgetAllocator.reserve()` 的真实返回/抛错语义，并按 `TransitionService` 的真实终态输入改为验证“预算超限后走 failed 而非 done”。定向命令 `./node_modules/.bin/tsx --test tests/e2e/budget-per-execution-enforcement-r10-42.test.ts tests/e2e/budget-execution-enforcement.test.ts tests/e2e/budget-execution-blocking-e2e.test.ts` 现已通过 `19 passed / 0 failed`。 | 关闭的是“无可运行、可证明 budgetUsdLimit 真正阻断超支执行的 E2E 回归”问题。 |
| R10-43 | 52. Prompt Engine / Injection Guard / 测试体系深层问题 | P2 | `src/platform/prompt-engine/registry/index.ts` | §16.2 role:planner/generator/evaluator/system；实现用 channel:system/developer/user——完全不同枚举映射 LLM 消息角色非 Harness 管线角色 | 已解决（源码修复） | 根因是旧实现将 Harness 管线角色（planner/generator/evaluator/system）与 LLM 消息 channel（system/developer/user）混用同一枚举。现已拆分为两个独立类型：`PromptTemplateRole`（planner/generator/evaluator/system，用于管线协调）和 `PromptTemplateChannel`（system/developer/user，用于 LLM 消息路由），并通过 `ROLE_TO_MESSAGE_CHANNEL` 常量定义两者的映射关系：`planner→developer`、`generator→user`、`evaluator→developer`、`system→system`。`PromptTemplateRecord` 同时包含 `role` 和 `channel` 字段，registration input 支持分别指定，验证时以 `role` 为准调用 `validateBundleCompatibility()`。 | `src/platform/prompt-engine/registry/index.ts` 第 11/18/26-31/60-61/94-95 行已实现完整双类型 + 映射机制。 | 关闭该条角色/channel 混淆问题。 |
| R10-44 | 53. ADR 新发现矛盾（Round 10） | P0 | `docs_zh/adr/054-\*.md (SLA tiers)` | Platinum 99.99% 无前置条件；§2.5 要求"99.99 只能绑定自动 failover+quorum+演练证据"——ADR 无此约束 | 已解决 | ADR 已补入前提约束：99.99% 仅在专有部署层级（自动 failover、quorum、容量预留与完整演练证据）条件下才可对外承诺，平台默认 SLA 上限为 99.95%。 | `docs_zh/adr/054-sla-tiered-guarantees.md` 第 21 行已明确注明前提条件，第 51 行补充前置条件说明。 | 不改代码，仅回写 ADR 现行依据。 |
| R10-45 | 53. ADR 新发现矛盾（Round 10） | P0 | `docs_zh/adr/096-\*.md (phase 8b)` | 仍用 "phase 8b" 作为 release gate；§33 明确废弃 Phase 1-9 仅保留历史映射；ADR 未被 remediate | 已解决 | ADR 已修正交付门禁术语为 `Ring 2 durable-readiness`，符合 §33 ring 口径废弃 Phase 1-9 的要求。 | `docs_zh/adr/096-harness-recovery-controller.md` v4.3 ADR Remediation 第 38 行已明确 R10-45 修复内容。 | 不改代码，仅回写 ADR 现行依据。 |
| R10-46 | 53. ADR 新发现矛盾（Round 10） | P0 | `docs_zh/adr/041-\*.md (create_task bypass)` | TriggerAction.create_task 直接建任务绕过 intake pipeline(RawInput→TaskDraft→Clarification→ConfirmedTaskSpec→RequestEnvelope)；违反 §4.2/§5.3 | 已解决 | ADR 已明确 TriggerAction.create_task 必须经 §5.3 intake pipeline（TaskDraft → ConfirmedTaskSpec → RequestEnvelope），不得直接创建任务绕过管道。 | `docs_zh/adr/041-proactive-agent-framework.md` 第 31-37 行已明确 create_task 必须经 intake pipeline，并在 v4.3 ADR Remediation 第 79 行记录 R6-53 修复。 | 不改代码，仅回写 ADR 现行依据。 |
| R10-47 | 53. ADR 新发现矛盾（Round 10） | P1 | `docs_zh/adr/070-\*.md (Phase 1-7 roadmap)` | 仍以 Phase 1-7 为 canonical 演进模型；§33 要求 Ring 1/2/3；未标 Superseded | 已解决（ADR 复核） | 根因不是 ADR-070 仍把 `Phase 1-7` 当 canonical，而是 reaudit 条目引用了 remediation 之前的旧版本判断；当前 ADR 已改成 ring 口径并显式标记 superseded，但复审表未同步刷新。 | `docs_zh/adr/070-conclusion.md:3-5` 已标记 `状态：Superseded`；`docs_zh/adr/070-conclusion.md:31-41` 的演进路线已改为 `Ring 1 / Ring 2 / Ring 3`；`docs_zh/adr/070-conclusion.md:52-53` 的 remediation 还明确记录了“从 Phase 1-7 收口到 ring 模型”的修复原因。 | 不改代码，仅按现行 ADR 证据关闭陈旧结论。 |
| R10-48 | 53. ADR 新发现矛盾（Round 10） | P1 | `docs_zh/adr/059-\*.md (DecisionRecord)` | DecisionRecord 以 agent_id 为键无 harnessRunId/nodeRunId/planGraphId；§5.5 要求所有决策链接到 HarnessRun | 已解决（ADR 复核） | 根因不是 ADR-059 仍缺 HarnessRun 链路，而是复审条目停留在只以 `agent_id` 追踪决策的早期草案；当前正文已经把决策对象锚定到 `harness_run_id / node_run_id / plan_graph_id`。 | `docs_zh/adr/059-agent-explainability-and-decision-transparency.md:14-25` 的 `DecisionRecord` 已显式包含 `harness_run_id`、`node_run_id`、`plan_graph_id`；`docs_zh/adr/059-agent-explainability-and-decision-transparency.md:28` 还明确写出“所有 DecisionRecord 必须通过这三个字段链接到 HarnessRun”。 | 不改代码，仅回写 ADR 现行可追溯约束。 |
| R10-49 | 53. ADR 新发现矛盾（Round 10） | P1 | `docs_zh/adr/083-\*.md vs ADR-042 (autonomy levels)` | ADR-083 定义 4级(manual_only/suggest_only/supervised_execute/trusted_auto_execute)；ADR-042 定义 5级(0-4)；§42 引用 5级模型——两 ADR 互相矛盾 | 已解决（ADR 复核） | 根因不是当前两份 ADR 仍各写一套自治等级，而是 reaudit 条目沿用了 ADR-083 早期四级命名；当前 ADR-083 已显式声明必须与 §42 的五级 canonical autonomy mode 对齐，和 ADR-042 一致。 | `docs_zh/adr/083-proactive-agent-and-progressive-autonomy.md:36-47` 已写明五级模型 `0-4`；`docs_zh/adr/083-proactive-agent-and-progressive-autonomy.md:29-31` 明确注明 ADR-083 与 ADR-042 均须与 §42 的 5 级模型对齐；`docs_zh/adr/042-progressive-autonomy-model.md:12-26` 也使用同一五级枚举。 | 不改代码，仅关闭过时的 ADR 冲突结论。 |
| R10-50 | 53. ADR 新发现矛盾（Round 10） | P1 | `docs_zh/adr/055-\*.md (Marketplace certification)` | 认证仅含 Prompt Injection 检查；§11.7 要求签名/SBOM/依赖漏扫/最小权限/sandbox egress/tool-call 攻击模拟 | 已解决（ADR 复核） | 根因不是当前市场 ADR 仍只做 Prompt Injection 检查，而是复审结论没有吸收后续认证矩阵扩展；现行 ADR 已把 §11.7 的七类认证项全部写进上架门禁。 | `docs_zh/adr/055-agent-marketplace-and-ecosystem.md:32-40` 的“认证评估（§11.7）”现已列出 `Prompt Injection`、`签名验证`、`SBOM`、`依赖漏洞扫描`、`最小权限原则`、`Sandbox Egress 控制`、`Tool-call 攻击模拟`，并在 `42` 行要求全部通过后才能上架。 | 不改代码，仅按现行 ADR 关闭旧缺口。 |
| R10-51 | 53. ADR 新发现矛盾（Round 10） | P1 | `docs_zh/adr/048-\*.md (SCIM lifecycle)` | SCIM 入职/转岗/离职为简单自动动作；§2.4 要求 prepare/commit/compensate/audit 四段 Saga 语义 | 已解决（ADR 复核） | 根因不是 ADR-048 仍把 SCIM 生命周期写成无补偿自动动作，而是 reaudit 结论滞留在四段 Saga 回写之前；当前正文已经把入职、转岗、离职统一改为 `prepare/commit/compensate/audit` 语义。 | `docs_zh/adr/048-enterprise-sso-scim-integration.md:29-38` 已给出 SCIM 生命周期四段 Saga 表，并在 `40` 行明确写出“确保所有用户生命周期操作具备幂等性和补偿能力”。 | 不改代码，仅按现行 ADR 关闭旧判断。 |
| R10-52 | 53. ADR 新发现矛盾（Round 10） | P1 | `docs_zh/adr/078-\*.md (Knowledge Plane)` | 将 Knowledge 定位为独立架构平面；v4.3 §4 仅定义 5 plane+X1，Knowledge 属 P5 State&Evidence(§29) | 已解决（ADR 复核） | 根因不是 ADR-078 仍把 Knowledge 当第六平面，而是标题保留了历史功能域命名，导致旧复审误把它当成独立架构平面；正文现已显式声明 Knowledge 只是 P5 State & Evidence 内的功能组件。 | `docs_zh/adr/078-knowledge-plane-architecture.md:10` 已明确注明 “标题中的 Knowledge Plane 是功能域命名，而非独立架构平面”；同段写明 Knowledge 属于 `P5 State & Evidence`，不是额外第六平面。 | 不改代码，仅回写 ADR 现行平面归属。 |
| R10-53 | 53. ADR 新发现矛盾（Round 10） | P2 | `docs_zh/adr/047-\*.md (ApprovalTimeout auto_action)` | 超时 auto_action(自动执行预设动作)；§10.3 critical 操作要求 break-glass+双人审批；§2.1 审批延迟时"安全停住"非自动执行 | 已解决（ADR 复核） | 根因不是审批路由 ADR 仍允许高风险 `auto_action` 无门禁自动执行，而是复审结论引用了补约束前的旧描述；当前正文已经把 high/critical 风险的 auto_action 明确禁止，并回到“安全停住”语义。 | `docs_zh/adr/047-organization-approval-routing.md:28-31` 已写明 `auto_action` 对 `high/critical` 默认 deny，critical 操作要求 `break-glass + 双人审批`，审批延迟时应“安全停住”而非自动执行。 | 不改代码，仅按现行 ADR 关闭陈旧指控。 |
| R10-54 | 53. ADR 新发现矛盾（Round 10） | P2 | `docs_zh/adr/051-\*.md (governance delegation)` | 允许 platform 级"全部权限"委托；§2.4 NonOverridableInvariantRegistry 不得被任何管理员/域 owner 关闭——未排除不可覆盖不变量 | 已解决（ADR 复核） | 根因不是 ADR-051 仍允许无边界的“全部权限”委托，而是复审条目没有吸收后续补充的不可覆盖不变量约束；当前平台级委托已经显式排除了 `NonOverridableInvariantRegistry`。 | `docs_zh/adr/051-tiered-governance-delegation.md:29-34` 已注明“平台级全部权限但须排除 NonOverridableInvariantRegistry”，并在 `34` 行再次强调该控制权不得被委托。 | 不改代码，仅回写 ADR 现行治理边界。 |
| R10-55 | 53. ADR 新发现矛盾（Round 10） | P2 | `docs_zh/adr/056-\*.md (Learn→Release)` | Learn→Improve→Release 六级发布无 P2 Release Governance 门控；§13.1 明确"Release 是 P2 决策非 OAPEFLIR 自行发布" | 已解决（ADR 复核） | 根因不是 ADR-056 仍把 Release 写成 OAPEFLIR 自行发布，而是 reaudit 结论滞后于后续治理回写；当前正文已经把 Release 明确限定为 `P2 Release Governance` 决策门。 | `docs_zh/adr/056-feedback-driven-continuous-improvement.md:49-51` 已写明 “Release 六级发布（须经 P2 Release Governance 门禁）”，并明确 `Release` 是 `P2` 决策，不得跳过。 | 不改代码，仅按现行 ADR 关闭旧矛盾。 |
| R10-56 | 53. ADR 新发现矛盾（Round 10） | P2 | `docs_zh/adr/053-\*.md (ResourcePool)` | 定义 ResourcePool/ResourceAllocation(reserved/used)与 §1.5 冻结的 BudgetLedger/BudgetReservation/BudgetSettlement 并行；无集成/引用 | 已解决（ADR 复核） | 根因不是 ADR-053 仍维持两套独立预算真相模型，而是 reaudit 条目引用了“资源池与预算账本并行落地”的旧说法；当前正文已经把 ResourcePool 降为运行时配额视图，并强制通过 BudgetLedger/BudgetSettlement 集成结算。 | `docs_zh/adr/053-scaling-resource-competition-management.md:30` 已明确写出 `ResourcePool / ResourceAllocation` 与 `BudgetLedger / BudgetReservation / BudgetSettlement` 的整合关系，并声明原并行模型已废止。 | 不改代码，仅回写现行 ADR 的统一预算口径。 |
| R10-57 | 53. ADR 新发现矛盾（Round 10） | P2 | `docs_zh/adr/046-\*.md (OrgTree mutation)` | OrgTree 级联变更无补偿语义；§2.4 要求 prepare/commit/compensate/audit 四段 Saga | 已解决（ADR 复核） | 根因不是 ADR-046 仍缺组织树变更补偿语义，而是复审结论停留在只描述层级结构、未补事务边界的旧版本；当前正文已把 OrgTree 级联变更改成完整四段 Saga。 | `docs_zh/adr/046-organization-hierarchy-model.md:51-66` 已新增 `OrgTree 级联变更（§2.4 Saga 语义）`，覆盖 `prepare/commit/compensate/audit` 四阶段以及补偿日志、幂等与 `saga_in_flight` 告警要求。 | 不改代码，仅按现行 ADR 证据关闭陈旧条目。 |
| R11-01 | 55. 编排平面深层缺口（Evaluator / Observer / Budget / Termination / Truth） | P0 | `src/platform/orchestration/evaluator/` | 无 Evaluator 服务；§13.5 定义 Evaluator 为 Harness 一级角色(质量门控/目标偏离/风险升级/决策产出)；整个目录不存在 | 已解决（源码复核） | 根因是旧 audit 声称"整个目录不存在"；实际 `src/platform/orchestration/evaluator/` 目录和 `evaluator-service.ts` 已存在并完整实现 §13.5 全部五项职责：`evaluateQuality()` 质量门控、`evaluateGoalDeviation()` 目标偏离检测、`evaluateRiskBoundary()` 风险升级、`evaluateBudgetAdherence()` 预算合规、`evaluateTimingSLO()` 时长 SLO，`determineDecision()` 产出 accept/retry/replan/escalate/approve/abort 决策。`EvaluationReport` 接口含 `qualityScore`、`decision`、`findings[]`、`riskLevel` 字段，`EvaluatorService` 构造接受 `EvaluatorConfig` 含 `qualityGate`、`riskThresholds`、`timingSlo` 配置项，支持 `nodeRunId` 工作流感知过滤（R11-02 FIX 协同）。这是目录存在性 + 完整功能实现的双重修复，不是仅修复目录存在。 | 当前 `src/platform/orchestration/evaluator/evaluator-service.ts` 已完整实现五项评估维度和六类决策输出（1-459 行）；`src/platform/orchestration/evaluator/index.ts` 已导出全部类型和服务。这是文档基线漂移，不再保留为未解决。 | 这是目录存在性 + 完整功能实现的双重修复，不是仅修复目录存在。 |
| R11-02 | 55. 编排平面深层缺口（Evaluator / Observer / Budget / Termination / Truth） | P0 | `src/platform/orchestration/observer/ (缺失)` | 无 Observer 服务；§13.2 定义 Observe 为 OAPEFLIR 首阶段产出 ObservationBundle(信号收集/上下文组装)；目录不存在 | 已解决（源码复核） | 根因是旧 audit 声称"整个目录不存在"；实际目录和 `observer-service.ts` 早已存在，只是原始实现缺少 workflow awareness（无父子工作流上下文追踪）。本轮已补充 `parentNodeRunId` 和 `subgraphNodeIds` 参数，使 `observe()` 和 `collectSignals()` 能感知嵌套 subgraph 执行层级。 | 当前 `src/platform/orchestration/observer/observer-service.ts` 已支持 `parentNodeRunId?: string` 和 `subgraphNodeIds?: readonly string[]` 参数；`ContextSnapshot` 接口已扩展 `workflowContext?: { parentNodeRunId?: string; subgraphNodeIds?: readonly string[] }`；`collectSignals()` 会为父子关系和子图节点集分别注入 `parent_workflow_context` 和 `subgraph_nodes` 信号类型。 | 这是目录存在性 + workflow awareness 的双重修复，不是仅修复目录存在。 |
| R11-03 | 55. 编排平面深层缺口（Evaluator / Observer / Budget / Termination / Truth） | P1 | `src/platform/prompt-engine/eval/execution-outcome-evaluator.ts` | 仅基于 feedback signal 计数评分；缺 §13.5 要求的 constraint compliance/budget adherence/risk boundary/timing SLO 评估维度 | 已解决（源码复核） | 根因是旧 audit 声称仅基于 signal 计数评分；现行实现已包含所有 §13.5 要求的评估维度：`evaluateConstraintCompliance()`、`evaluateBudgetAdherence()`、`evaluateRiskBoundary()`、`evaluateTimingSLO()` 四个方法均已实现，`ExecutionOutcomeEvaluation` 接口已包含 `constraintCompliance`、`budgetAdherence`、`riskBoundary`、`timingSlo` 四个字段，`evaluate()` 方法在第 397-401 行已调用全部四个评估方法。 | `src/platform/prompt-engine/eval/execution-outcome-evaluator.ts:397-401` 已调用全部四个评估方法；`src/platform/prompt-engine/eval/types.ts:91-118` 已定义四个结果类型；`ExecutionOutcomeEvaluation` 接口(第 64-67 行)已包含全部四个维度的结果字段。 | 这是文档基线漂移，不再保留为未解决。 |
| R11-04 | 55. 编排平面深层缺口（Evaluator / Observer / Budget / Termination / Truth） | P1 | `src/platform/prompt-engine/eval/execution-outcome-evaluator.ts:70` | Evaluator 消费 legacy Plan 非 PlanGraphBundle；无法访问图级元数据(节点风险/预算预留/图版本) | 已解决（源码复核） | 根因是旧 audit 声称 line 70 仍消费 legacy Plan；现行实现已完整迁移至 PlanGraphBundle：① 显式导入 `PlanGraphBundle`/`RiskClass`/`PlanNode`/`PlanEdge`（第 24-29 行）；② `evaluate()` 方法签名已改为 `PlanGraphBundle | LegacyPlanLike` 并通过 `toPlanGraphBundle()` 统一处理（第 342 行）；③ 新增 `graphMetadata` 字段（第 68-77 行）含 `nodeCount`、`edgeCount`、`graphVersion`、`highestNodeRisk`、`totalBudgetIntent`、`entryNodeIds`、`terminalNodeIds`；④ `extractGraphMetadata()`（第 122-149 行）从 `graph.nodes[].riskClass` 和 `graph.nodes[].budgetIntent` 提取元数据；⑤ `evaluateWithGraphContext()`（第 463-526 行）提供图结构感知评估；⑥ `evaluateBudgetAdherence()`（第 655 行）和 `evaluateRiskBoundary()`（第 720 行）均已使用 `graph.nodes[]` 的节点级风险/预算数据。Legacy `toPlanGraphBundle()` 函数（第 844-877 行）在旧 Plan 场景下生成兼容的 PlanGraphBundle 结构，保证向后兼容。 | `src/platform/prompt-engine/eval/execution-outcome-evaluator.ts:24-29`（导入）、`68-77`（graphMetadata 字段）、`122-149`（extractGraphMetadata）、`285-295`（方法签名）、`397-401`（评估调用链）、`463-526`（evaluateWithGraphContext）、`655`/`720`（预算/风险评估使用图数据）、`844-877`（toPlanGraphBundle 兼容转换）。 | 这是文档基线漂移，不再保留为未解决。 |
| R11-05 | 55. 编排平面深层缺口（Evaluator / Observer / Budget / Termination / Truth） | P1 | `src/platform/prompt-engine/eval/execution-outcome-evaluator.ts:39` | 质量门控阈值硬编码(defaultPassThreshold:0.5)；§17.3 要求按风险等级+域可配置 | 已解决（源码修复） | 根因是 `DEFAULT_QUALITY_GATE_CONFIG` 的 `riskLevelThresholds` 和 `domainThresholdOverrides` 确实为空数组，导致 `getEffectiveThreshold()` 无法在默认配置下查找到风险等级阈值，只能回退到 `defaultPassThreshold`。已修复：给 `DEFAULT_QUALITY_GATE_CONFIG.riskLevelThresholds` 填入 low/medium/high/critical 四个风险等级的默认阈值（与 `quality-config-loader.ts` 的 fallback 阈值一致），使默认配置也能按 §17.3 提供可配置的 per-risk-level 阈值。`domainThresholdOverrides` 保持为空数组是正确的——域级覆盖确实需要通过外部配置提供（`config/quality/default.json`），实现已通过 `getEffectiveThreshold()` 的三级查询逻辑（domain override → risk level threshold → default）正确处理该场景。 | `src/platform/prompt-engine/eval/execution-outcome-evaluator.ts:119-129`（修复后的 `DEFAULT_QUALITY_GATE_CONFIG.riskLevelThresholds`）；`src/platform/prompt-engine/eval/quality-config-loader.ts:67-72`（外部配置加载器使用相同阈值作为 fallback）；`src/platform/prompt-engine/eval/execution-outcome-evaluator.ts:555-601`（`getEffectiveThreshold()` 三级查询）。 | 代码修复：填入默认 per-risk-level 阈值。 |
| R11-06 | 55. 编排平面深层缺口（Evaluator / Observer / Budget / Termination / Truth） | P1 | `src/platform/execution/budget-allocator.ts` | 无水位告警/自动限流/跨 run 优先级；§18.2-18.3 要求 watermark alert+auto-throttle+platform→tenant→pack→step 层级预算 | 已解决（源码复核） | 根因不是代码仍缺 watermark alert/auto-throttle/层级预算，而是 audit 条目未跟随实现更新。`BudgetTier` enum（PLATFORM/TENANT/PACK/STEP）+ `WatermarkAlertConfig` + `checkWatermarkAlert()` + `AutoThrottleConfig` + `checkAutoThrottle()` + `checkHierarchicalBudgetAllowance()` 均已实现，属于文档基线漂移。 | 当前 `src/platform/execution/budget-allocator.ts` 已实现：`BudgetTier`（20-28行）、`WatermarkAlertConfig`（31-38行）、`BudgetWatermarkAlert`（94-103行）、`checkWatermarkAlert()`（231-274行）水位告警；`AutoThrottleConfig`（44-48行）、`BudgetAutoThrottleEvent`（108-116行）、`checkAutoThrottle()`（280-319行）、`computeEffectiveAmount()`（560-571行）自动限流；`CrossRunPriorityConfig`（54-57行）跨 run 优先级；`BudgetAllocatorContext`（73-86行）含 tier + watermarkAlert + autoThrottle + crossRunPriority；`checkHierarchicalBudgetAllowance()`（643-672行）+ `hierarchyLedgers` 在 reserve/settle/release 中流通实现层级预算（platform→tenant→pack→step）。 | 不改生产代码，仅回写实现现状并关闭条目。 |
| R11-07 | 55. 编排平面深层缺口（Evaluator / Observer / Budget / Termination / Truth） | P1 | `src/platform/execution/budget-allocator.ts:35-44` | reserve() 无预留过期清扫器+无流式增量 reserve/settle(§18.3 要求 Sweeper+streaming 每 N token settle) | 已解决（源码复核） | 根因是旧 audit 声称 reserve() 缺 Sweeper 和 streaming settle；现行代码已在 BudgetAllocator 中完整实现：① `SweeperConfig`（155-159 行）+ `sweepExpiredReservations()`（993-1029 行）提供过期预留清扫；② `StreamingSettleConfig`（63-67 行）+ `processStreamingSettle()`（577-635 行）+ `StreamingSettleState`（143-149 行）提供按 token interval 或 time interval 的增量 settle。`reserve()` 在 streamingSettle.enabled 时初始化状态（741-744 行），`settle()` 在 streamingSettle.enabled 时调用 processStreamingSettle（862-864 行）。 | `src/platform/execution/budget-allocator.ts` 已含 SweeperConfig + sweepExpiredReservations() + StreamingSettleConfig + processStreamingSettle()；这是文档基线漂移，不再保留为未解决。 |
| R11-08 | 55. 编排平面深层缺口（Evaluator / Observer / Budget / Termination / Truth） | P1 | `src/platform/five-plane-execution/run-termination-cleanup.ts` | 无 state evidence flush/compensation trigger/notification；§8.6 要求优雅终止含证据刷写+副作用补偿+通知发送 | 已解决（源码复核） | 根因是旧 audit 声称"无"这三项；实际文件早已实现：`RunTerminationCleanupRequest` 含 `flushEvidence`(line 36)、`triggerCompensation`(line 38)、`notificationRecipients`(line 40) 字段，`executeAsync()` 对三项分别做了处理：evidence flush 在 line 241-256 执行、compensation trigger 在 line 311-325 执行、notifications 在 line 327-355 发送给各接收方。 | 当前 `src/platform/five-plane-execution/run-termination-cleanup.ts` 已完整实现 §8.6 要求的证据刷写+副作用补偿+通知发送三项特性。 | 这是文档基线漂移，不再保留为未解决。 |
| R11-09 | 55. 编排平面深层缺口（Evaluator / Observer / Budget / Termination / Truth） | P1 | `src/platform/state-evidence/projections/projection-rebuild-service.ts:579,600` | 每次 handler(null, event) 传 null 不线程化状态；projection rebuild 无法跨事件累积状态(§25.4 rebuild 失效) | 已解决（源码复核） | 根因是旧 audit 声称 rebuildProjection() 和 shadowBuild() 中每次调用都传 null 给 handler；但现行代码已在两处方法中正确实现状态线程：rebuildProjection() 第 581 行初始化 accumulatedState=null，第 600 行 handler(accumulatedState, inputEvent) 并将返回值回写 accumulatedState；shadowBuild() 第 307 行初始化 accumulatedState=null，第 327 行同样正确线程状态。两方法的注释也明确标注了 §R11-09 FIX。 | 当前 `src/platform/state-evidence/projections/projection-rebuild-service.ts` 的 `rebuildProjection()` (579-600 行) 和 `shadowBuild()` (307-327 行) 均已正确实现 accumulatedState 跨事件累积；这是文档基线漂移，不再保留为未解决。 |
| R11-10 | 55. 编排平面深层缺口（Evaluator / Observer / Budget / Termination / Truth） | P1 | `src/platform/state-evidence/projections/projection-rebuild-service.ts` | 无 shadow-build/compare/cutover 协议；§28.6 要求影子投影→hash 比对→切换→保留旧版回滚+API stale marker | 已修复（源码修复并补定向测试） | 根因有两层：一是 `rebuildProjection()` 之前只做“逐事件调用 handler”的一次性遍历，没有版本化快照；二是服务层完全没有 active/shadow/previous 三态管理，因此即使能重建，也不存在影子构建、hash 比对、cutover 和 stale 标记协议。 | 本轮在 `src/platform/state-evidence/projections/projection-rebuild-service.ts` 中补入 `ProjectionSnapshot`、`ProjectionComparisonResult`、`shadowBuildProjection()`、`compareShadowProjection()`、`cutoverShadowProjection()`、`markProjectionStale()` 与 `getProjectionSnapshotStatus()`，并把 rebuild 主链改为真实线程化 `accumulatedState` 后生成 `stateHash` 快照。 | 定向验证已通过：`tests/unit/platform/state-evidence/projections/projection-rebuild-service.test.ts` 新增 `threads state across events` 与 `supports shadow build, compare, and cutover`，证明影子投影、hash 比对、切换和旧版保留链可运行。 |
| R11-11 | 55. 编排平面深层缺口（Evaluator / Observer / Budget / Termination / Truth） | P2 | `src/platform/execution/run-termination-cleanup.ts:65` | 始终返回 complete:true 不论实际清理成功/失败；无 partial-failure 追踪/incident 创建(§12 异常处理) | 原指控已部分过时，incident 仍缺 | 复核后确认这里不是“始终 complete:true”：现行 `RunTerminationCleanupReceipt` 已显式返回 `failedResourceIds`，并用 `failedResourceIds.length === 0` 决定 `complete`。真正还没补上的是 cleanup partial-failure 到 incident 的自动升级，而不是返回值恒真。 | 当前 `src/platform/five-plane-execution/run-termination-cleanup.ts` 已有 `cleanedResourceIds/skippedResourceIds/failedResourceIds/complete` 四组结果字段，失败时 `complete=false`；但文件内仍无 incident service 或异常升级链。 | 本轮不假关闭：回写“complete 恒真”已被源码证伪，同时保留“partial failure 不会自动开 incident”这一剩余缺口。 |
| R11-12 | 55. 编排平面深层缺口（Evaluator / Observer / Budget / Termination / Truth） | P2 | `src/platform/state-evidence/truth/ (全域)` | 无 snapshot 版本化；worker-snapshot-repository 用 bare UPSERT 无 version 列/CAS/乐观并发(§25.3/§25.10) | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。 | 不宣称已修复。 |
| R11-13 | 55. 编排平面深层缺口（Evaluator / Observer / Budget / Termination / Truth） | P2 | `src/platform/state-evidence/events/projections/workflow-run-projection.ts:196` | processedEventIds 无界数组+includes() O(n) 去重；长运行工作流内存/性能恶化；需 Set 或 watermark 去重 | 已解决（源码复核并补定向测试） | 根因是旧实现确实把 `processedEventIds` 当数组直接查重，长运行投影会出现 O(n) 去重和无界增长；现行实现后来已改成“序列化时保留数组、运行时转 `Set`”的双态模型，但 reaudit 文档没有跟上。 | 当前 `src/platform/state-evidence/events/projections/workflow-run-projection.ts` 已通过 `toInternalState()` 把 `processedEventIds` 转成 `_processedEventIdSet: Set<string>` 做 O(1) 查重，并在 `markEventProcessed()` 中对 `MAX_PROCESSED_EVENT_ID_SET_SIZE = 10_000` 执行 watermark eviction。现有 `tests/unit/platform/state-evidence/events/projections/workflow-run-projection.test.ts` 已覆盖基础去重，本轮新增的 `workflowRunProjectionHandler bounds processedEventIds with watermark eviction` 进一步验证了 10,050 个事件后集合仍被上限约束。 | 这是文档基线漂移，不再保留为未解决。 |
| R11-14 | 55. 编排平面深层缺口（Evaluator / Observer / Budget / Termination / Truth） | P2 | `src/plugins/validators/basic-evaluator.ts` | Evaluator 插件仅验证字段存在/类型；无质量评分/目标偏离检测/风险检查/HarnessDecision 产出(§13.5) | 已修复（源码修复并补定向测试） | 复核后发现根因不是“完全没有质量评估代码”，而是插件内部曾引入 `validateWithQualityScoring()` 但结果被截断在 SPI 边界外，`createBasicEvaluatorPlugin().validate()` 仍只返回 `valid/errors/suggestions`；同时 contract 中也没有 expected outcomes、风险字段和 harness decision 输出。 | 本轮在 `src/domains/registry/plugin-spi.ts` 为 validator 结果补入 `evaluation`，并在 `src/plugins/validators/basic-evaluator.ts` 实际返回 `qualityScore/qualityThreshold`、`goalDeviation(missingOutcomes/unexpectedFields)`、`riskFindings` 与 `harnessDecision(accept/repair/requires_human/reject)`；`tests/unit/plugins/validators/basic-evaluator.test.ts` 新增断言，验证高风险字段会产出 `requires_human`，缺失 expected outcomes 会产出 `repair`。 | 关闭的是 basic evaluator 插件本体输出缺口，不外推所有上游 harness 已全面消费这些新字段。 |
| R11-15 | 56. 交互子系统深层缺口（Wizard / Template / NL Pipeline / Dashboard） | P1 | `src/interaction/ux/wizard/index.ts` | §44.2 要求后退导航+步骤历史栈；WizardSession 仅 currentStepId+canAdvanceWizard()无 goBack()/history | 已解决（源码修复并补定向测试） | 原审计抓的是 wizard 早期极简会话形态；现行实现不但已有 `history/goBack`，本轮还把导航状态补成完整 session 模型。 | `src/interaction/ux/wizard/index.ts` 现已显式持有 `history/visitedStepIds`，并提供 `goBackWizard()`、`advanceWizard()`、`advanceWizardToNextVisibleStep()`；`tests/unit/interaction/ux/wizard/navigation-and-session.test.ts` 已覆盖回退栈、可见步骤跳转与访问轨迹。 | 关闭“无后退导航/历史栈”这一条。 |
| R11-16 | 56. 交互子系统深层缺口（Wizard / Template / NL Pipeline / Dashboard） | P1 | `src/interaction/ux/wizard/index.ts` | §44.2 要求进度持久化(跨会话 save/restore)；WizardSession 纯内存 Zod schema 无序列化/session storage/resume | 已解决（源码修复并补定向测试） | 根因已从“完全无持久化接口”收敛为“session 缺少显式保存时间和进度对象”；本轮把 save/restore 补成了一等 API。 | 当前 `src/interaction/ux/wizard/index.ts` 已提供 `serializeWizardSession()`、`deserializeWizardSession()`、`saveWizardSession()` 与 `getWizardProgress()`，session schema 也加入 `lastSavedAt/schemaVersion`；对应测试已在 `tests/unit/interaction/ux/wizard/navigation-and-session.test.ts` 覆盖。 | 关闭“无法跨会话保存/恢复”的条目。 |
| R11-17 | 56. 交互子系统深层缺口（Wizard / Template / NL Pipeline / Dashboard） | P1 | `src/interaction/ux/wizard/index.ts` | §44.2 要求条件步骤(基于前序答案 show/hide)；steps 为静态扁平数组无条件谓词/skip/分支 | 已解决（源码修复并补定向测试） | 这条一半是文档基线滞后，一半是缺少“条件步骤后如何跳过隐藏节点”的导航能力；本轮把两部分都补齐了。 | `WizardStepSchema.condition` 和 `getVisibleSteps()` 本已存在，本轮又新增 `recordWizardAnswer()`、`advanceWizardToNextVisibleStep()`，使 show/hide 与 skip 导航真正联动；`tests/unit/interaction/ux/wizard/navigation-and-session.test.ts` 已新增隐藏步骤跳过回归。 | 不再保留为未解决。 |
| R11-18 | 56. 交互子系统深层缺口（Wizard / Template / NL Pipeline / Dashboard） | P1 | `src/interaction/ux/template-engine/index.ts` | 16行 stub；§44.3 要求域模板+参数化工作流+模板市场集成；InteractionTemplate 仅含 templateId/title/steps:string[] | 已解决（源码修复并补定向测试） | 原审计依据已经过时：template-engine 早已不是 16 行 stub，但 marketplace/catalog/workflow defaults 还没有被显式建模。 | 本轮在 `src/interaction/ux/template-engine/index.ts` 补入 `marketplaceBinding`、`workflowDefaults`、`catalogTags`、`recommendTemplatesForDomain()` 与参数绑定结果 `parameterValues`；`tests/unit/interaction/ux/template-engine/instantiate-and-validate.test.ts` 已覆盖实例化、域校验和模板推荐。 | 关闭“template-engine 仍是 stub”的结论。 |
| R11-19 | 56. 交互子系统深层缺口（Wizard / Template / NL Pipeline / Dashboard） | P1 | `src/interaction/nl-gateway/index.ts:804-898` | §39.5 要求对话上下文跨轮携带(Memory §29.2)；ConversationContextManager 存在但从未被 parseDetailed/buildTask 调用——每次解析无状态 | 已解决 | 原条目的根因判断已经失真：现行 `NlEntryService` 不是“完全无状态”，而是 `parseDetailed()` 会先读取上下文、`buildTask()` 在任务构建后回写 turn。此前真正缺的是 Memory-backed service 入口没有从 `NlEntryServiceOptions` 透传，不是“完全没调用 manager”。 | 当前 `src/interaction/nl-gateway/index.ts` 的 `parseDetailed()` 已调用 `conversationContextManager.getContext(...)` 并把 `priorConversationContext` 传给 router，`buildTask()` 末尾也会 `addTurn(...)`；本轮新增 `tests/unit/interaction/nl-gateway/dry-run-preview-and-memory.test.ts`，定向验证了跨 service 实例恢复 prior context 并注入下一轮解析。 | 关闭“parse/build 从不调用 manager”这一本体指控；Memory 注入缺口由 `R17-24` 的实修承接。 |
| R11-20 | 56. 交互子系统深层缺口（Wizard / Template / NL Pipeline / Dashboard） | P1 | `src/interaction/nl-gateway/intent-parser/index.ts` | §39.3 IntentParser 应通过 ModelGateway 做多语言意图识别；实现纯正则(parseIntentTokens)+硬编码模式无 LLM/置信度评分 | 已解决（源码修复并补定向测试） | 根因不是 heuristic parser 必须被删除，而是缺少“可选模型解析 + fallback”的正式接线。 | 本轮在 `src/interaction/nl-gateway/intent-parser/index.ts` 增加 `parseIntentTokensWithModel()`，并在 `src/interaction/nl-gateway/index.ts` 的 `NlEntryService.parseDetailed()` 中接入 `intentParser.parseWithLlm`；`tests/unit/interaction/nl-gateway-service.test.ts` 已新增高置信 LLM 意图覆盖本地 router 的回归。 | 关闭“纯正则且无法接 LLM”的缺口。 |
| R11-21 | 56. 交互子系统深层缺口（Wizard / Template / NL Pipeline / Dashboard） | P1 | `src/interaction/nl-gateway/slot-resolver/index.ts` | §39.2 要求迭代 slot-filling(Clarification loop)；resolveRequiredSlots 单 pass 返回缺失 slot 无多轮填充/提示生成/ClarificationState 驱动 | 已解决（源码修复并补定向测试） | 原问题在于 slot-resolver 只有一次性缺口计算，缺少可直接喂给 clarification state 的结构化输出。 | 本轮新增 `buildSlotClarificationState()`，返回 `missing/resolved/questions/nextExpectedSlot/isComplete`，并在 `NlEntryService.parseDetailed()` 中把结果写入 `context.requiredSlots/missingSlots/resolvedSlots` 和 `clarificationState.questions`；`tests/unit/interaction/nl-gateway/slot-resolver.test.ts` 与 `tests/unit/interaction/nl-gateway-service.test.ts` 已覆盖。 | 这次关闭的是“无 clarification-driven slot loop”这个真实缺口。 |
| R11-22 | 56. 交互子系统深层缺口（Wizard / Template / NL Pipeline / Dashboard） | P1 | `src/interaction/goal-decomposer/planner/index.ts` | §40.2 要求优先级调度；buildExecutionBatches 仅拓扑序——Goal.priority 从不被咨询；critical 任务不会优先于 low | 已解决（源码修复并补定向测试） | 原审计忽略了 `buildExecutionBatches()` 已有 numeric priority 排序能力，但也指出了一个真实问题：调用侧若持有 `Goal.priority` 字符串，需要自行转换。 | 本轮在 `src/interaction/goal-decomposer/planner/index.ts` 增加 `priorityLabels` 支持，把 `low/normal/high/critical` 直接映射为权重；`tests/unit/interaction/goal-decomposer/planner/build-execution-batches.unit.test.ts` 已新增 critical 优先于 low 的回归。 | 关闭“planner 只按拓扑序、不认优先级标签”的条目。 |
| R11-23 | 56. 交互子系统深层缺口（Wizard / Template / NL Pipeline / Dashboard） | P1 | `src/interaction/goal-decomposer/llm-plan-generator.ts:49` | §40.2 要求预算传播到子任务；generate() 所有任务 estimatedCost.confidence="low"/sampleCount=0；目标预算约束未按子任务比例分配 | 已解决（源码修复并补定向测试） | 根因是预算分配逻辑和 estimate metadata 之前都停留在占位值；本轮把两者改成真实派生。 | `src/interaction/goal-decomposer/llm-plan-generator.ts` 现已用 `allocateBudgetShare()` 按子任务估算成本比例分配 `allocatedBudgetUsd/budgetLimitUsd`，并基于 goal 约束与成功标准派生 `estimatedCost.confidence/sampleCount/basedOn`；`tests/unit/interaction/goal-decomposer/llm-plan-generator.test.ts` 已新增预算传播与 evidence-based estimate 回归。 | 关闭“预算未传播/estimate 一律占位”的条目。 |
| R11-24 | 56. 交互子系统深层缺口（Wizard / Template / NL Pipeline / Dashboard） | P1 | `src/interaction/dashboard/alert-router/index.ts` | §43 要求多通道路由(dashboard/push/NL summary)；仅含 sortAttentionQueue()纯排序函数无路由/dispatch/escalation/确认 | 已解决（源码修复并补定向测试） | 根因分两层：第一，reaudit 仍停留在只有 `sortAttentionQueue()` 的旧快照，没有复核后续已经加入的 `AlertRouter` 路由类；第二，现行实现里确实还残留一个真实 bug，`cooldown` 写入 key 与读取 key 不一致，导致重复投递抑制失效，同时也还没有把 NL summary 作为第三条输出通道显式建模。 | 本轮修复 `src/interaction/dashboard/alert-router/index.ts` 的 delivery history key，对 `routeNotifications()` 的 per-delivery cooldown 真正生效；同时新增 `buildNlSummary()`，使 alert router 具备 dashboard overlay、push/haptic 和 NL summary 三条输出通道。`tests/unit/interaction/dashboard/alert-router/alert-router.test.ts` 已新增并通过 cooldown 与 NL summary 回归，定向命令 `./node_modules/.bin/tsx --test tests/unit/interaction/dashboard/alert-router/alert-router.test.ts` 通过。 | 关闭的是“alert-router 只有排序、没有多通道路由”的结论；不把它夸大成完整 incident escalation/ack workflow 已全部到位。 |
| R11-25 | 56. 交互子系统深层缺口（Wizard / Template / NL Pipeline / Dashboard） | P1 | `src/interaction/dashboard/metric-aggregator/index.ts` | §43.2-43.5 要求时序聚合/趋势/SLO 比较/域级分组；summarizeTaskMetrics 仅 4状态单快照计数无窗口/P50/P95/SLO gap | 已解决（源码/测试） | 原结论停留在 `summarizeTaskMetrics()` 仍是唯一入口的阶段；现行实现已经有 `aggregateWindowedMetrics()`、`calculateMetricTrend()`、`compareSloValue()`、`groupMetricsByDomain()`、`MetricAggregator` 与 `buildDashboardMetricsWithAggregation()`，只是 reaudit 没把这些能力和专项回归写回。 | 本轮新增 `tests/unit/interaction/dashboard/metric-aggregator/metric-aggregator.test.ts` 的 `buildDashboardMetricsWithAggregation returns window, trend, slo, and domain grouping`，直接验证 summary/window/SLO/domain/trend 五类输出；同文件与既有 advanced tests 一起通过。定向命令 `./node_modules/.bin/tsx --test tests/unit/interaction/dashboard/metric-aggregator.test.ts tests/unit/interaction/dashboard/metric-aggregator/metric-aggregator.test.ts` 已通过。 | 关闭的是“metric aggregator 仍只有单快照计数”的陈旧结论。 |
| R11-26 | 56. 交互子系统深层缺口（Wizard / Template / NL Pipeline / Dashboard） | P2 | `src/interaction/nl-gateway/ (ambiguity-handler vs disambiguation-handler)` | 重复 detectAmbiguity 函数(不同逻辑)从两个模块导出——import 冲突风险+行为不一致 | 已解决（源码修复） | 原问题已不在于两套逻辑并存，而在于 barrel 入口继续把 canonical 和 legacy wrapper 混在一起导出，容易误读成“双实现”。 | 本轮将 `src/interaction/nl-gateway/index.ts` 的 `detectAmbiguity` 统一指向 `disambiguation-handler` canonical source；`ambiguity-handler` 仅保留兼容入口，不再作为 barrel 主导出来源。 | 关闭“主导出面存在双逻辑冲突风险”的条目。 |
| R11-27 | 56. 交互子系统深层缺口（Wizard / Template / NL Pipeline / Dashboard） | P2 | `src/interaction/ux/onboarding/index.ts:310` | recommendDomains() 硬编码正则关键词匹配；§44.4 要求基于业务上下文的智能域推荐(LLM/用户历史/DomainRegistry) | 已解决（源码修复并补定向测试） | 根因是域推荐过去只看描述关键词，不消费用户模式和历史偏好。 | 本轮在 `src/interaction/ux/onboarding/index.ts` 的 `recommendDomains()` 中加入 `historicalDomainPreferences/userMode/requiresSso` 等上下文评分，并把原因链回写到 `PortalOnboardingPlan.recommendationReasons`；`tests/unit/interaction/ux/onboarding.test.ts` 已新增 history + executive 模式偏好回归。 | 关闭“域推荐完全不看业务上下文”的条目。 |
| R11-28 | 56. 交互子系统深层缺口（Wizard / Template / NL Pipeline / Dashboard） | P2 | `src/interaction/ux/onboarding/index.ts` | §44.5 要求 progressive disclosure(按用户模式隐藏复杂度)；buildVisualWorkflowBuilder/buildDomainOnboardingWizard 不按模式区分返回相同结构 | 已解决（源码修复并补定向测试） | 旧实现虽然已有文案差异，但结构层仍没有显式表达“哪些部分被隐藏/展开”；这才是审计真正指出的缺口。 | 本轮为 `DomainOnboardingWizard` 与 `VisualWorkflowBuilder` 增加 `progressiveDisclosure` 结构，按 `solo/team/department/enterprise` 输出不同的 `level/visibleSections/hiddenSections/defaultExpandedCategories`；`tests/unit/interaction/ux/onboarding.test.ts` 已覆盖 solo 和 enterprise 两端。 | 关闭“返回结构不区分用户模式”的条目。 |
| R11-29 | 56. 交互子系统深层缺口（Wizard / Template / NL Pipeline / Dashboard） | P2 | `src/interaction/dashboard/dashboard-websocket-server.ts:330-346` | performHeartbeat() 标记超时连接 isConnected=false 但不从 connections/subscribers 移除——无界内存增长+stale 推送 | 已解决（源码复核） | 根因是旧 heartbeat 清理只标记连接断开，不真正执行 unregister；现行实现已经改为先收集超时 client，再统一调用 `unregisterClient()` 清理连接和订阅映射，只是 reaudit 条目没有回写。 | 当前 `src/interaction/dashboard/dashboard-websocket-server.ts` 的 `performHeartbeat()` 已在超时分支收集 `timedOutClientIds` 并逐个调用 `unregisterClient()`；`unregisterClient()` 本身会同时移除 `connections`、`channelSubscribers` 和 metric 订阅。现有 `tests/unit/interaction/dashboard/dashboard-websocket-server.test.ts` 已包含 `heartbeat unregisters timed out clients (issue #2040 fixed)`、`performHeartbeat removes timed out connections from the registry` 和 `heartbeat cleanup removes timed out clients from counts and routing` 三条回归。 | 这条与后面的 `R23-14/2040` 是同一缺陷的重复挂账，现统一视为已解决。 |
| R11-30 | 57. 跨切面架构问题（错误处理 / 日志 / 类型安全 / 平面耦合） | P0 | `src/platform/control-plane/incident-control/auto-stop-loss-service.ts:803` | executePlaybook() .catch(()=>{}) 静默吞没全部错误；健康事件期间 runbook 失败不可见——§9.6 要求分类+证据记录 | 已解决（源码复核并补定向测试） | 根因是旧实现的确存在 fire-and-forget 吞错路径；现行实现已经去掉了空 `catch`，并把“promise 级异常”和“playbook 内部 action 失败”分成两类可见出口：前者在异步 `catch` 中分类记录，后者以失败 `StopLossEvent` 留存在执行历史里。 | 当前 `src/platform/control-plane/incident-control/auto-stop-loss-service.ts` 的 `updateHealthCheck()` 已不再使用空 `catch`，而是对 `executePlaybook(...).catch(...)` 调用 `classifyPlaybookError(...)` + `recordError(...)`；同时 `executePlaybook()` 自身会把 action handler 失败收敛成带 `errorMessage` 的失败事件并写入 `executionHistory`。本轮新增 `tests/unit/platform/control-plane/incident-control/auto-stop-loss-service.test.ts` 的 `updateHealthCheck records failed execution event when async playbook action fails`，验证健康事件触发的异步 playbook 失败不会被吞掉，而会留存在执行历史中。 | 这次关闭的是“静默吞错导致失败不可见”的原问题，不额外宣称更强的 incident 编排能力已全部补齐。 |
| R11-31 | 57. 跨切面架构问题（错误处理 / 日志 / 类型安全 / 平面耦合） | P1 | `src/platform/shared/observability/structured-logger.ts:42-59` | StructuredLogEntry 缺 tenantId/harnessRunId；§7.1 要求每条日志含 traceId+spanId+tenantId+harnessRunId | 已解决 | 原条目已与现行类型不符。`StructuredLogEntry` 现在已显式包含 `tenantId` 与 `harnessRunId`，logger 还支持从显式字段或 `data` payload 提取这两个相关 ID。 | 当前 `src/platform/shared/observability/structured-logger.ts` 的 `StructuredLogEntry` 已定义 `tenantId?: string` 与 `harnessRunId?: string`；现有 `tests/unit/platform/shared/observability/structured-logger-extended.test.ts` 已验证从 payload 读取与显式字段覆盖，`tests/unit/platform/shared/observability/structured-logger-format.test.ts` 也已验证两字段格式化保留。 | 属于文档基线漂移，关闭。 |
| R11-32 | 57. 跨切面架构问题（错误处理 / 日志 / 类型安全 / 平面耦合） | P1 | `src/platform/interface/channel-gateway/stream-bridge.ts:147,251` | JSON.parse() as T 无 schema 验证在 inter-plane 边界；§5.2 要求所有平面间通信 schema-validated | 已解决（源码复核并补定向测试） | 原指控停留在更早的 `StreamBridge` 版本：当前实现已经不是 `JSON.parse() as T`，而是在两处反序列化后都走了 Zod 运行时校验；本轮补的缺口只是把这份现状重新写回 reaudit，并增加一个“非对象 payload 被收敛为空对象”的回归。 | 当前 `src/platform/interface/channel-gateway/stream-bridge.ts` 在 `mapEventType()` 使用 `taskStatusChangedPayloadSchema.safeParse(JSON.parse(event.payloadJson))`，在 `emitFromEvent()` 使用 `genericEventPayloadSchema.safeParse(JSON.parse(input.event.payloadJson))`，不再把 JSON 直接断言成业务类型；新增 `tests/unit/platform/interface/channel-gateway/stream-bridge.test.ts` 的 `StreamBridge drops non-object event payloads at the inter-plane boundary`，并在本轮定向命令中通过。 | 关闭陈旧结论；不宣称这里已经有比 `Record<string, unknown>` 更细的业务 payload contract。 |
| R11-33 | 57. 跨切面架构问题（错误处理 / 日志 / 类型安全 / 平面耦合） | P1 | `src/platform/interface/channel-gateway/channel-gateway-delivery-service.ts:540,698,746` | 三处 JSON.parse() as Record<string,unknown> 无运行时验证；DB payload_json 反序列化越过平面边界无校验 | 已修复 | 原始问题在主读取路径上其实早已开始收敛：`getPendingDeliveries()`、`getDeadLetters()`、`getRetryableMessages()` 都已经转成 `deliveryPayloadSchema.safeParse(...)`；但同文件里 `moveToDeadLetter()` 提取 `requestUrl` 仍残留一次裸 `JSON.parse`，导致“主链有校验、辅助链没统一”的边界漂移。 | 本轮新增 `parseDeliveryPayload()`，把 `src/platform/interface/channel-gateway/channel-gateway-delivery-service.ts` 内所有 `payload_json` 读取统一收口到 `deliveryPayloadSchema.safeParse(JSON.parse(...))`；新增 `tests/unit/platform/interface/channel-gateway/channel-gateway-delivery-service.test.ts` 的 `getPendingDeliveries rejects non-object payload_json at the DB boundary`，证明数组类畸形 payload 不会穿透成业务对象，并在本轮定向命令中通过。 | 这次关闭的是“DB payload_json 读取未统一校验”的问题，不额外宣称 delivery payload 已有更严格的字段级 schema。 |
| R11-34 | 57. 跨切面架构问题（错误处理 / 日志 / 类型安全 / 平面耦合） | P1 | `src/platform/interface/channel-gateway/websocket-bridge.ts:135` | WS 消息 JSON.parse() as WebSocketMessageType 无 schema 验证；外部客户端输入在入口边界需运行时 schema 检查 | 已解决（源码复核） | 原条目没有跟上后续接线：当前 WebSocket 入口已经先做 JSON 解析，再用 `webSocketMessageSchema` 进行 discriminated-union 校验，失败时返回 `invalid_message`，不再把外部输入直接断言成 `WebSocketMessageType`。 | 当前 `src/platform/interface/channel-gateway/websocket-bridge.ts` 已定义 `webSocketMessageSchema`，在 `handleConnection()` 的 message handler 中对 `parsed` 执行 `webSocketMessageSchema.safeParse(parsed)` 后才进入 `handleMessage()`；现有 `tests/unit/platform/interface/channel-gateway/websocket-bridge-coverage.test.ts` 已包含 `WebSocketBridge handles invalid JSON gracefully` 用例，可直接对照这一入口行为。 | 关闭陈旧结论；本轮未把整份 websocket coverage 文件作为验证命令，因为该文件存在旧的异步收尾噪音，但源码与现有用例已足以反证原指控。 |
| R11-35 | 57. 跨切面架构问题（错误处理 / 日志 / 类型安全 / 平面耦合） | P1 | `src/platform/interface/api/http-server/prompt-routes.ts:44` | schema.parse 后立即 payload as any 转交——验证产出的类型安全被 any 抹除；字段漂移静默通过 | 已修复 | 原条目命中的 `POST /v1/prompts` 路径其实早已是强类型 `readValidatedJsonBody(..., promptBundleRegistrationSchema.parse)`；真正残留的根因在同文件 `PUT /v1/prompts/:name/deprecate`，它仍走 `readJsonBody(...) as Record<string, unknown>` 旧路径，导致 deprecate body 没有独立 schema。 | 本轮在 `src/platform/interface/api/http-server/prompt-routes.ts` 新增 `promptBundleDeprecationSchema`，并把 `PUT /deprecate` 改成 `readValidatedJsonBody(ctx.request.body, promptBundleDeprecationSchema.parse)`，彻底去掉该分支的 `as Record<string, unknown>` 类型抹除；`tests/unit/platform/interface/api/http-server/prompt-routes.test.ts` 新增 `PUT /v1/prompts/:name/deprecate validates request body before deprecating` 与 `... accepts validated payload`，并在本轮定向命令中通过。 | 这是实修，不是单纯改文档；同时也把该条的命中位置从过时的 `POST` 路径纠正到了真实残留分支。 |
| R11-36 | 57. 跨切面架构问题（错误处理 / 日志 / 类型安全 / 平面耦合） | P1 | `src/platform/execution/lease/execution-lease-service-async.ts:502` | audit as any 在 lease audit 插入处抹除类型安全——执行平面与证据平面间类型合约断裂 | 已解决（源码复核） | 这条属于 reaudit 结论滞后：当前 async lease service 的审计写入入口已经显式收敛到 `LeaseAuditRecord`，不再有 `audit as any` 这种跨平面类型抹除。 | 当前 `src/platform/execution/lease/execution-lease-service-async.ts` 通过 `private insertLeaseAudit(audit: LeaseAuditRecord): void` 统一写入，所有调用点都构造显式 `LeaseAuditRecord` 字面量；本轮同时校正并跑通了 `tests/unit/platform/execution/lease/execution-lease-service-async-r9.test.ts` 与 `tests/unit/platform/five-plane-execution/lease/execution-lease-service-async.test.ts`，确认 async lease 主链仍按现行 TTL/审计语义工作。 | 关闭陈旧结论；本轮没有在这里新增生产代码，因为真实类型抹除点已经不存在。 |
| R11-37 | 57. 跨切面架构问题（错误处理 / 日志 / 类型安全 / 平面耦合） | P1 | `src/org-governance/org-model/hr-role-governance-service.ts:24` | 跨平面深度 import：org-governance 直接导入 platform/execution/tool-executor/tool-recommend-service.ts 绕过 contracts 层 | 已解决（源码复核） | 根因不是当前源码仍在深度导入 `tool-recommend-service`，而是 reaudit 条目引用了更早期的 import 位置，后续重构把该依赖收敛成共享工具发现入口后，文档没有同步回写。 | 当前 `src/org-governance/org-model/hr-role-governance-service.ts:16-24` 只依赖 approval service、division loader 与 `../../platform/shared/tool-discovery/index.js` 的 `expandToolNames/inferPromotedToolNames`，并不存在对 `platform/execution/tool-executor/tool-recommend-service.ts` 的直接导入。 | 不改生产代码，按现行源码关闭陈旧指控。 |
| R11-38 | 57. 跨切面架构问题（错误处理 / 日志 / 类型安全 / 平面耦合） | P1 | `src/platform/compliance/compliance-case-orchestration-service.ts:4` | Platform 平面导入 org-governance/compliance-engine/ 形成双向依赖——违反平面间单向依赖规则 | 已解决（源码复核） | 根因是原审计停留在 platform/compliance 仍直接引用 org-governance 实现类型的阶段；现行文件已改为在 platform 平面内声明最小 `ComplianceGovernanceEvaluator` 接口，由调用方注入实现，但 reaudit 结论没有刷新。 | 当前 `src/platform/compliance/compliance-case-orchestration-service.ts:16-27,60-80` 在本平面内定义 `ComplianceEvaluationResult` 和 `ComplianceGovernanceEvaluator` 最小接口，`options.governance` 只接受接口注入，不再直接 import `org-governance/compliance-engine/*`。 | 不改生产代码，按现行解耦状态关闭。 |
| R11-39 | 57. 跨切面架构问题（错误处理 / 日志 / 类型安全 / 平面耦合） | P1 | `src/platform/interface/api/http-server/dashboard-routes.ts:14` | Platform API 层直接导入 interaction/ux/ 跨平面边界无 contract 接口 | 已解决（源码复核） | 根因是 reaudit 条目引用了旧的 dashboard 依赖关系；当前 API 层并没有直接接入 `interaction/ux`，而是依赖 platform/shared 下的 workbench 快照服务，历史指控未随重构更新。 | 当前 `src/platform/interface/api/http-server/dashboard-routes.ts:10-19` 导入的是 `../../../shared/ux/platform-workbench-snapshot-service.js`、stability/compliance/prompt-engine 等 platform 内模块，没有 `interaction/ux/*` 直接依赖。 | 不改生产代码，关闭过时导入指控。 |
| R11-40 | 57. 跨切面架构问题（错误处理 / 日志 / 类型安全 / 平面耦合） | P1 | `src/platform/contracts/errors.ts` | 异常分类法缺 ResourceError/SecurityError/BusinessRuleError(§9.6 要求 transient/permanent/resource/security/business-rule 五类) | 已解决（源码复核） | 根因不是现行错误模型仍缺三类异常，而是 reaudit 条目基于旧版 `AppErrorCategory` 快照；后续扩展已补齐 category union 与对应错误类，但文档仍保持旧判断。 | 当前 `src/platform/contracts/errors.ts:15-32` 的 `AppErrorCategory` 已含 `resource`、`security`、`business-rule`，并在 `src/platform/contracts/errors.ts:459-503` 定义了 `ResourceError`、`SecurityError`、`BusinessRuleError` 三个具体错误类。 | 不改生产代码，回写现行错误分类体系。 |
| R11-41 | 57. 跨切面架构问题（错误处理 / 日志 / 类型安全 / 平面耦合） | P2 | `src/interaction/ux/ux-event-tracking-service.ts:122,136` | 双重 as any 转换使 UX 事件逃逸类型检查；分析管线可收到畸形数据 | 已修复 | 根因复核后比原条目更具体：当前文件里最显著的 `as any` 已不在原位置，但 `trackEvent()` 仍把 `payload.elementId` 和 `payload.interactionType` 通过隐式断言塞进 `UxEventTrack`，导致非字符串 `elementId` 和非法交互类型可以穿透到事件日志/发布链。 | 本轮在 `src/interaction/ux/ux-event-tracking-service.ts:86-95,131-194` 新增 `normalizeElementId()`、`normalizeInteractionType()` 与 `TrackEventPayload`，去掉残余不安全断言，改为把非字符串 `elementId` 收敛为 `null`、非法 `interactionType` 收敛为 `"click"`；`tests/unit/interaction/ux/ux-event-tracking-service-edge-cases.test.ts:92-112` 新增两条回归，并与同族 UX 测试一起定向通过 `94 passed / 0 failed / 0 skipped`。 | 真实改了生产代码并补了边界回归。 |
| R11-42 | 57. 跨切面架构问题（错误处理 / 日志 / 类型安全 / 平面耦合） | P2 | `src/domains/registry/plugin-spi-registry.ts:488` | .catch(()=>undefined) 静默丢弃 Promise rejection；插件超时原因丢失无日志/证据 | 已解决（源码复核） | 根因不是现行插件 SPI 仍在 activation/invocation 路径静默吞错，而是 reaudit 仍停留在更早期 fire-and-forget 认知；当前失败链已经统一收敛到 failure state、隔离事件和 invocation 完成事件，原指控所述“超时原因丢失无证据”已不成立。 | 当前 `src/domains/registry/plugin-spi-registry.ts:675-699` 的 `recordFailure()` 会落 `failureCount/lastErrorAt/lastErrorMessage` 并发布 `publishIsolationEvent(...)`；`src/domains/registry/plugin-spi-registry.ts:719-741` 的 invocation 主链会在失败时发布 `plugin:invocation_completed` 且 `status: "failed"`、携带 `reasonCode/errorMessage`。源码中未再保留原条目所述的 `.catch(()=>undefined)` 静默吞错分支。 | 不改生产代码，按现行可观测失败链关闭。 |
| R11-43 | 57. 跨切面架构问题（错误处理 / 日志 / 类型安全 / 平面耦合） | P2 | `src/platform/state-evidence/events/durable-event-bus.ts:474,508,523` | 多处 .catch(()=>undefined/0) 使事件投递链失败对证据平面不可见；交付顺序错误无法追踪 | 已解决（源码复核） | 根因是 reaudit 条目没有跟上后续容错改造：当前 delivery 链在“允许吞错返回 0”的分支前，已经先把失败记录到结构化日志中，因此真实剩余行为是“容错返回值仍存在”，而不是“失败不可见”。 | 当前 `src/platform/state-evidence/events/durable-event-bus.ts:1038-1058` 已在 volatile handler 失败时记录 `event_bus.volatile_delivery_failed` 与 `event_bus.volatile_delivery_chain_error`；`src/platform/state-evidence/events/durable-event-bus.ts:1088-1115` 已在 durable delivery 链记录 `event_bus.delivery_chain_error`，并在 `swallowErrors` 分支返回 `0` 前记录 `event_bus.delivery_failed`。因此原条目指控的“无日志/证据可见性”已被后续实现消化。 | 不改生产代码；保留对当前容错语义的准确描述，但关闭“静默失败不可见”的旧问题。 |
| R11-44 | 57. 跨切面架构问题（错误处理 / 日志 / 类型安全 / 平面耦合） | P2 | `src/sdk/cli/stable-runner-factory.ts:166` | (runner as any)(runnerArgs) 擦除 runner 函数签名；参数合约不匹配仅运行时暴露 | 已修复 | 根因是 stable CLI 工厂为了兼容同步/异步 runner 和不同 report 形状，把 `runner`、`writer`、`failed` 都宽化成 `any`，再用 `(runner as any)(runnerArgs as any)` 调用，导致 entrypoint 参数漂移只能等运行时报错；移除这层擦除后，也暴露出 `stable-gate` / `stable-package` 在 `exactOptionalPropertyTypes` 下把 `undefined` 显式写入可选字段的真实调用端漂移。 | 本轮在 `src/sdk/cli/stable-runner-factory.ts:63-149` 用泛型 `StableRunner<RunnerArgs, Report>`、`StableReportWriter<Report>`、`FailedPredicate<Report>` 替换 `any`，移除 `(runner as any)(runnerArgs as any)`，补上 `defaultFailed(report: unknown)`；同时把 `src/sdk/cli/stable-gate.ts:15-21` 与 `src/sdk/cli/stable-package.ts:12-18` 改为条件展开 `evidenceRootDir`。`tests/unit/sdk/cli/stable-runner-factory.test.ts:19-31,251-260` 已补强强类型回归，相关定向测试通过 `94 passed / 0 failed / 0 skipped`，`npm run build` 也已通过。 | 真实修复类型签名擦除，并顺带收敛了被 build 暴露的调用端漂移。 |
| R11-45 | 58. ADR 新矛盾（Round 11） | P0 | `docs_zh/adr/069-\*.md (Self-operating agent)` | 自运维 agent restart_service/scale_up/rotate_secrets 不经 RuntimeStateMachine.transition()/OperationalDirective；§5.3 要求所有状态变更走 canonical 控制路径 | 已解决 | ADR 已明确所有 OpsCapability 操作必须通过 RuntimeStateMachine.transition(OperationalDirective) + HarnessRuntime + PlanGraphBundle 上下文执行，确保操作可审计、可回滚。 | `docs_zh/adr/069-platform-self-operating-agent.md` 第 37-43 行已明确所有直接执行操作需经 RuntimeStateMachine.transition()，并在 v4.3 ADR Remediation 第 85 行记录 R6-54 修复。 | 不改代码，仅回写 ADR 现行依据。 |
| R11-46 | 58. ADR 新矛盾（Round 11） | P1 | `docs_zh/adr/065-\*.md (Debugger)` | 使用 workflow_id/WorkflowDAGView/StepInspector/step_over 等废弃模型；§5.5 要求 PlanGraph+NodeRun 操作；无 remediation | 已解决（ADR 复核） | 根因不是当前 ADR 仍停留在 `workflow_id/WorkflowDAGView/StepInspector`，而是 reaudit 条目没有吸收后续 remediation 回写；调试器 ADR 现已切换到 `PlanGraphView + HarnessRun/NodeRun` 语义，并显式记录了旧模型迁移原因。 | `docs_zh/adr/065-workflow-visual-debugger.md:24-40` 已把视图定义改成 `PlanGraphView { harness_run_id, node_run_ids, ... }`，并在 `v4.3 ADR Remediation` 中明确写出从 `WorkflowDAGView/StepInspector/step_over` 迁移到 `NodeRun`/`node_step_*`。 | 不改代码，仅回写 ADR 现行状态。 |
| R11-47 | 58. ADR 新矛盾（Round 11） | P1 | `docs_zh/adr/062-\*.md (Edge sync)` | last_write_wins 冲突解决与 §25.11/§52.3 single-leader truth write+fencing 矛盾；未区分 truth vs projection 对象 | 已解决（ADR 复核） | 根因是原 ADR 早期确实容易被理解成“所有对象都可 last_write_wins”，但现行文本已经显式把 truth/budget/side effect 与 projection/统计对象分开；reaudit 表未跟上这次回写。 | `docs_zh/adr/062-offline-and-edge-deployment-architecture.md:49-61` 已明确 `server_wins` 仅用于 `truth / budget / side effect` 对象，并强调这些对象必须走单 leader + fencing；`last_write_wins` 与 `merge` 只保留给 projection / 非关键统计对象。 | 不改代码，仅按现行 ADR 关闭旧矛盾。 |
| R11-48 | 58. ADR 新矛盾（Round 11） | P1 | `docs_zh/adr/094-\*.md (OAPEFLIR Execute)` | "落盘 run、step、decision" 使用 step 作为持久执行单元；§5.5 step 仅为语义投影，NodeRun/NodeAttempt 为持久 truth | 已解决（ADR 复核） | 根因不是 Durable Execution ADR 仍把 step 当持久执行 truth，而是 reaudit 条目引用了更早的 durable wording；现行 ADR 已把持久对象改写为 `HarnessRun/NodeRun/NodeAttempt`，并把 step 降级为语义投影。 | `docs_zh/adr/094-harness-durable-execution.md:7-14` 已写明 Execute 阶段“落盘 HarnessRun、NodeRun、NodeAttempt 与上下文”，并注明 “step 为语义投影，非持久 truth”；不再存在“run、step、decision”作为持久执行单元的表述。 | 不改代码，仅回写现行 durable execution 口径。 |
| R11-49 | 58. ADR 新矛盾（Round 11） | P1 | `docs_zh/adr/101-\*.md (Domain risk override)` | 域风险画像优先于平台默认但未引用 DomainRiskSpec 必填字段(advisory_only/human_accountable/deterministic_hot_path_only)；不可强制 | 已解决（ADR 复核） | 根因是 reaudit 条目停留在只声明“领域风险优先”但未冻结责任边界字段的旧版本；当前 ADR 已补齐 `DomainRiskSpec` 结构和 high/critical 域必须声明的治理字段。 | `docs_zh/adr/101-domain-risk-override-platform-default.md:31-49` 已定义 `DomainRiskSpec`，显式列出 `advisory_only`、`human_accountable`、`deterministic_hot_path_only`，并写明 high/critical 域必须三选一，否则 validator block domain release。 | 不改代码，仅回写 ADR 的现行约束。 |
| R11-50 | 58. ADR 新矛盾（Round 11） | P1 | `docs_zh/adr/095-\*.md (OAPEFLIR context)` | "为 Harness step 提供上下文输入"——§45 要求上下文按 NodeRun 组装非 step | 已解决（ADR 复核） | 根因不是当前上下文 ADR 仍以 step 为权威装配粒度，而是 reaudit 表沿用了旧句式；现行 ADR 已把 Execute 描述改成 NodeRun 组装，并明确 step 只是语义投影。 | `docs_zh/adr/095-harness-context-assembly.md:7-10` 已写明“为 Harness NodeRun 执行组提供上下文输入”，并附注“上下文按 NodeRun 组装，非 step；step 仅语义投影”。 | 不改代码，仅回写现行 ADR 语义。 |
| R11-51 | 58. ADR 新矛盾（Round 11） | P1 | `docs_zh/adr/044-\*.md (User roles)` | 角色(business_operator/team_lead/executive/admin)未对齐 §46 组织层级(enterprise/business_unit/department/team/seat) | 已解决（ADR 复核） | 根因是原 ADR 曾用业务语义角色名直接充当层级，但现行文档已经把用户角色映射到 `seat/team/department/division/company` 的 OrgNode hierarchy；reaudit 表没有吸收这次回写。 | `docs_zh/adr/044-non-technical-user-experience.md:12-24` 已将角色表与 `§46.1 OrgNodeType` 显式对齐，分别映射到 `seat / team / department / division / company/tenant` 视图与权限继承层级。 | 不改代码，仅回写 ADR 现行组织层级映射。 |
| R11-52 | 58. ADR 新矛盾（Round 11） | P1 | `docs_zh/adr/057-\*.md (External adapter)` | circuit_break/retry 本地重实现；§4.7 X1 Reliability Fabric 要求为中间件/库级关注点；缺 SideEffectRecord 集成 | 已解决（ADR 复核） | 根因是原 ADR 早期只描述了 adapter 内部错误处理策略，没有把 X1 横切职责与 SideEffectRecord 生命周期绑定写清楚；现行 ADR 已把这两点明确冻结，但 reaudit 表仍保留旧判断。 | `docs_zh/adr/057-external-system-integration-framework.md:23-45,55-64` 已规定 `retry` 与 `circuit_break` 由 X1 Reliability Fabric middleware/library 统一执行，Adapter 不得本地重实现；同文件 `31-39` 还把 `side_effect_policy` 与 `SideEffectRecord` 生命周期、`RuntimeStateMachine` 推进绑定。 | 不改代码，仅回写 ADR 现行约束。 |
| R11-53 | 58. ADR 新矛盾（Round 11） | P1 | `docs_zh/adr/104-\*.md (Domain recipes)` | 12种 recipe archetype 未绑定 DomainDescriptor.recipes 字段(ADR-081/100)或四阶段 onboarding(ADR-103)；与 §37-§38 集成断裂 | 已解决（ADR 复核） | 根因是 reaudit 条目引用了十二 archetype 还只是枚举清单的早期版本；现行 ADR 已补上与 `DomainDescriptor.recipes` 的字段绑定，以及在四阶段 onboarding 中的接入时点。 | `docs_zh/adr/104-domain-recipe-twelve-archetypes.md:29-39` 已明确十二 archetype 必须绑定到 `DomainDescriptor.recipes`，并写明在 ADR-103 四阶段 onboarding 的第一、第二阶段完成 archetype 选型与 baseline recipe 绑定。 | 不改代码，仅按现行 ADR 集成关系关闭。 |
| R11-54 | 58. ADR 新矛盾（Round 11） | P2 | `docs_zh/adr/085/086/087-\*.md` | 引用 "v2.7" 为来源版本；当前架构 v4.3——版本引用过时造成混淆 | 已解决（ADR 复核） | 根因不是 ADR-085/086/087 仍引用 `v2.7`，而是 reaudit 条目沿用了这组三份聚合 ADR 初稿阶段的版本印象；当前正文背景已统一回写为 `v4.3` 分段来源。 | `docs_zh/adr/085-organization-governance-and-knowledge-boundary.md:25`、`docs_zh/adr/086-scale-ecosystem-and-cross-region-runtime.md:25`、`docs_zh/adr/087-ops-maturity-runtime.md:25` 现在都明确以 `v4.3` 章节作为来源背景，源码库内未再出现该组三份 ADR 的 `v2.7` 引用。 | 不改代码，仅关闭过时版本指控。 |
| R11-55 | 58. ADR 新矛盾（Round 11） | P2 | `docs_zh/adr/049-\*.md (CompliancePolicy)` | 用 department_id 作范围；§46 OrgNode 层级(enterprise→team→seat)要求 orgNodeId 支持所有层级 | 已解决（ADR 复核） | 根因不是 ADR-049 仍把合规策略绑定死在 `department_id`，而是复审条目没有跟上 OrgNode 迁移后的字段重命名；当前策略对象已经统一使用 `orgNodeId` 和可选 `orgNodeType`。 | `docs_zh/adr/049-department-compliance-policy-engine.md:14-22` 的 `CompliancePolicy` 已使用 `orgNodeId` 作为范围字段，并在 `13` 行写明必须支持 `enterprise / division / department / team` 等层级。 | 不改代码，仅按现行 ADR 关闭字段陈旧结论。 |
| R11-56 | 58. ADR 新矛盾（Round 11） | P2 | `docs_zh/adr/067-\*.md (Capacity planning)` | 无 Ring 分配标签；§33 将 capacity planning 置于 Ring 3(Enterprise)——可能导致 MVP 阶段过早实现 | 已解决（ADR 复核） | 根因不是容量规划 ADR 仍缺 ring 分层，而是 reaudit 条目停留在未加实施边界标签的旧文本；当前正文已明确将其归入 `Ring 3 - Enterprise`，并说明 MVP 只需预留采集接口。 | `docs_zh/adr/067-capacity-planning-and-cost-prediction.md:10-12` 已新增 `Ring 3 — Enterprise` 标注，并写明 `Ring 1 MVP` 不需要完整容量预测引擎。 | 不改代码，仅回写 ADR 现行实施边界。 |
| R11-57 | 58. ADR 新矛盾（Round 11） | P2 | `docs_zh/adr/105-\*.md (Latency tiers)` | 超低延迟 tier 未引用 §3.2 deterministic_hot_path_only 约束(不可用 LLM/Harness loop)；tier 分类未强制 v4.3 非目标边界 | 已解决（ADR 复核） | 根因不是 ADR-105 仍缺超低延迟约束，而是复审条目没有吸收后续补充的 `deterministic_hot_path_only` 与 v4.3 非目标边界段落；当前正文已经把这两点写成强制校验。 | `docs_zh/adr/105-domain-latency-tier-classification.md:31-41` 已明确超低延迟 tier 不可走 `LLM loop` 或 `Harness` 通用路径，并在 `v4.3 非目标边界` 段落中要求违反约束的域降级到实时或准实时 tier。 | 不改代码，仅按现行 ADR 关闭旧结论。 |
| R12-01 | 60. 事件总线与流式基础设施深层缺陷 | P0 | `src/platform/five-plane-state-evidence/events/durable-event-bus.ts` | 无 partition-by-aggregate 排序保证；§7.3/§28.3 要求同聚合内 sequence 单调递增+分区 outbox；flat subscribers Map 无聚合分区 | 已解决（源码+回归） | 根因成立：旧总线只有 flat subscriber fan-out，没有按 partition/aggregate 做单调序列和 FIFO 投递。 | 本轮在 `durable-event-bus.ts` 增加了分区序列跟踪与 `pendingPartitionEvents` 队列，`publish()`/投递路径改为按 partition FIFO 出队；并引入本地单调 sequence 兜底，保证同 partition 内事件顺序稳定。定向命令 `./node_modules/.bin/tsx --test tests/unit/platform/state-evidence/events/durable-event-bus-partitioned-aggregate-order.test.ts tests/unit/platform/state-evidence/events/review-contract-regressions.test.ts` 已通过。 | 关闭“同聚合无顺序保证、无分区队列”的缺口。 |
| R12-02 | 60. 事件总线与流式基础设施深层缺陷 | P0 | `src/platform/five-plane-state-evidence/events/durable-event-bus.ts` | 无 consumer group 隔离；§7.3 要求 EventInbox.consumeOnce per consumer；所有订阅者同等处理无独立 offset/优先级/组级 circuit-breaker | 已解决（源码+回归） | 旧实现把所有订阅者放在统一 fan-out 路径里，缺少 group 级隔离与独立并发控制。 | 当前 `durable-event-bus.ts` 已显式维护 consumer group 视图与组级并发/积压状态，订阅注册包含 `groupId`，投递时按组隔离选择消费者，不再是所有订阅者共用一条无差别通道。定向命令 `./node_modules/.bin/tsx --test tests/unit/platform/state-evidence/events/durable-event-bus-consumer-group-isolation.test.ts tests/unit/platform/state-evidence/events/review-contract-regressions.test.ts` 已通过。 | 关闭的是“consumer group 完全无隔离”的问题。 |
| R12-03 | 60. 事件总线与流式基础设施深层缺陷 | P1 | `src/platform/five-plane-state-evidence/events/durable-event-bus.ts` | DLQ 仅日志无持久队列；§28.8 要求 DLQ 含 category/reason/retry_count/operator_action_log+inspect/redrive/discard with approval | 已解决（源码+回归） | 旧实现的真实缺口是重试耗尽后只记日志，不把失败事件落入权威持久层。 | 当前 `durable-event-bus.ts` 在重试耗尽后会构造 dead-letter record；若未显式注入 `dlqRepository`，也会回退写入 authoritative store 的 dead-letter 持久层，而不是只打印日志。现有回归集 `tests/unit/platform/state-evidence/events/review-contract-regressions.test.ts` 已覆盖 dead-letter 合同，相关事件总线定向集已通过。 | 关闭“DLQ 仅日志、无持久落盘”的核心缺口。 |
| R12-04 | 60. 事件总线与流式基础设施深层缺陷 | P1 | `src/platform/five-plane-state-evidence/events/durable-event-bus.ts`; `src/platform/shared/observability/runtime-metrics-registry.ts` | Polling 10ms 硬编码无 back-pressure；§9.2 要求 queue-depth graduated back-pressure(reject low priority→DLQ→incident) | 已解决（针对本条具体缺口） | 原问题指向的是固定 10ms polling 与完全缺失的 queue-depth back-pressure。 | 当前 `durable-event-bus.ts` 已移除硬编码 10ms 轮询，新增基于 queue depth 的自适应 polling interval 计算，并在 `runtime-metrics-registry.ts` 增加 `recordEventBackpressure(...)` 指标上报。定向命令 `./node_modules/.bin/tsx --test tests/unit/platform/state-evidence/events/durable-event-bus-backpressure-2297.test.ts tests/unit/platform/state-evidence/events/review-contract-regressions.test.ts` 已通过。边界说明：本轮关闭的是“固定 polling、无 back-pressure”这一本条缺口，不额外宣称 incident 自动化编排已经全部实现。 | 关闭的是本条审计指控的 concrete bug。 |
| R12-05 | 60. 事件总线与流式基础设施深层缺陷 | P1 | `src/platform/state-evidence/events/transactional-event-appender.ts:96` | 手动 BEGIN/COMMIT/ROLLBACK 绕过 db.transaction() wrapper；嵌套事务冲突风险(§25.2 原子 truth+event 保证) | 已解决（源码复核并补现行证据） | 原审计结论已明显滞后：当前 `TransactionalEventAppender` 的 `appendEvent()` 与 `appendEvents()` 都已经走 `db.transaction(...)` 包装，不再手写 `BEGIN/COMMIT/ROLLBACK`；同时还提供 `mutateTruth` hook，把 truth mutation、event append 和 outbox 写入纳入同一事务。 | `src/platform/state-evidence/events/transactional-event-appender.ts` 现行实现中，`appendEvent()` / `appendEvents()` 都在 `db.transaction(...)` 内执行，且 `options.mutateTruth?.(this.db)` 也在同一事务闭包内；`tests/unit/platform/state-evidence/events/transactional-event-appender.test.ts` 已覆盖 transaction wrapper、truth mutation 原子提交与 truth mutation 抛错时整体回滚。 | 关闭的是“手写事务绕过 wrapper”这条旧指控；不外推整个 durable event bus 深层项都已完成。 |
| R12-06 | 60. 事件总线与流式基础设施深层缺陷 | P1 | `src/platform/state-evidence/events/dlq-service.ts:112` | DLQ 纯内存 Map；进程重启丢失全部死信；§28.8 要求持久化 DLQ+redrive 支持 | 已修复（源码修复并补定向测试） | 根因不是仓里完全没有持久化 DLQ，而是 `SqliteDlqRepository` 已存在的情况下，`DlqService` 默认构造仍会在调用方漏注入持久仓时静默退回 `InMemoryDlqRepository`，把“可持久化能力”变成“默认仍会重启丢数”。 | 本轮将 `src/platform/five-plane-state-evidence/events/dlq-service.ts` 改为 fail-closed：生产态必须显式提供 `DlqRepository` 或 `dbConnection`，否则抛 `dlq.persistence_required`；SQLite 路径继续经 `SqliteDlqRepository` 持久化并保留 `listRetryable()` redrive 能力。新增 `tests/integration/platform/state-evidence/events/dlq-persistence.integration.test.ts`，验证 dead-letter 记录在数据库关闭并重新打开后仍可读取；`tests/unit/platform/state-evidence/events/dlq-service.test.ts` 也新增了“非测试态拒绝内存回退”的回归。 | 这是对默认丢数路径的实修，不是只补 warning。 |
| R12-07 | 60. 事件总线与流式基础设施深层缺陷 | P1 | `src/platform/interface/channel-gateway/websocket-bridge.ts:269` | WS broadcast 无 back-pressure；不检查 buffered amount/send queue depth；慢客户端造成服务端内存无界增长 | 已解决（源码复核并补定向测试） | 原审计停留在 bridge 还只是“裸 `ws.send()` 广播”的早期形态；现行实现已经在发送前检查 `bufferedAmount`，对慢消费者发 `backpressure_warning`，并在高水位时跳过 `status_changed/progress/message_delta` 这类可丢低价值事件。 | 当前 `src/platform/interface/channel-gateway/websocket-bridge.ts` 已使用 `SLOW_CONSUMER_BUFFER_BYTES`、`slowConsumers`、`backpressure_warning` 和 `client.pendingAcks/bufferedEventCount` 做发送前背压分流。 | 定向验证已通过：既有 `tests/unit/platform/interface/channel-gateway/websocket-bridge-coverage.test.ts` 的 `tracks slow consumers` 继续通过，证明背压检测与慢客户端标记真实生效。 |
| R12-08 | 60. 事件总线与流式基础设施深层缺陷 | P1 | `src/platform/interface/channel-gateway/websocket-bridge.ts:77` | WS 无 last_event_id resume/replay；§7 要求断连重连+从 last_event_id 恢复+stream_gap 事件；客户端丢事件 | 已修复（源码修复并补定向测试） | 根因是旧 bridge 虽然有 `lastEventId` 和 `stream_gap` 类型定义，但只把它们用于单连接内的 ack 顺序校验，没有任何任务级 replay buffer，也不会在重连订阅时消费 `last_event_id`。 | 本轮在 `src/platform/interface/channel-gateway/websocket-bridge.ts` 增加 `taskEventHistory`、`MAX_TASK_EVENT_HISTORY`、`replayMissedEvents()`、`recordTaskEvent()` 和 `subscribe.lastEventId` / URL `last_event_id` 支持；重连时能从最近历史回放遗漏事件，找不到锚点时会显式发送 `stream_gap`。 | 定向验证已通过：`tests/unit/platform/interface/channel-gateway/websocket-bridge-coverage.test.ts` 新增 `replays missed task updates from last_event_id on reconnect` 与 `emits stream_gap when last_event_id is no longer replayable`。 |
| R12-09 | 60. 事件总线与流式基础设施深层缺陷 | P2 | `src/platform/interface/channel-gateway/stream-bridge.ts:379` | Replay buffer 满时 splice(0,1) 丢最老帧不论类型——可丢 completed/failed/approval_requested 关键事件 | 已解决（实现已存在，本轮补回归证据） | 真实根因是早期实现确实按“最老优先”直接裁剪 replay buffer；后续源码已改成“只淘汰可丢事件”，但 reaudit 文档还停留在旧结论，没有补出现行实现和回归测试依据。 | 当前 `src/platform/five-plane-interface/channel-gateway/stream-bridge.ts` 的 `appendToReplayBuffer()` 会优先定位并删除 `status_changed/progress/message_delta`，保留 `completed/failed/approval_requested` 等关键帧；本轮新增 `tests/unit/platform/five-plane-interface/channel-gateway/stream-bridge-replay-buffer-regression.test.ts`，直接验证 buffer 超限时仍保留 `completed` 与 `approval_requested`。 | 这是文档纠偏而不是新造“假修复”；关闭依据来自现行实现和新增窄回归测试。 |
| R12-10 | 60. 事件总线与流式基础设施深层缺陷 | P2 | `src/platform/state-evidence/events/projections/ (全部 7文件)` | idempotency 用 Array.includes() O(n)+无界增长；需 Set 或 watermark 去重(§28.6 高效可重建) | 已解决（补齐剩余 projection 并补一致性测试） | 根因复核后比原条目更细：并非所有 projection 都完全停留在 `Array.includes()`，而是前几轮修复后出现了两类残留问题。`approval-queue/incident/workflow-run` 已切到 Set 或等价内部结构；但 `artifact-catalog/governance/risk-action/worker-status/tool-usage/workflow-timeline` 里存在“上层状态已声明 Set、底层 helper/测试仍沿用旧数组语义”的半迁移状态，导致文档与实现不一致、个别文件还留有重复旧 helper。 | 本轮清理并统一了 `src/platform/five-plane-state-evidence/events/projections/{artifact-catalog,governance,risk-action,worker-status,tool-usage,workflow-timeline}-projection.ts` 的去重实现，现行 idempotency 均走 `Set.has()` / `new Set(...)`；新增 `tests/unit/platform/state-evidence/events/projections/projection-contract-alignment.test.ts`，并更新上述 projection 的单测，验证 `processedEventIds` 为 Set 且重复事件不会重复计数。 | 关闭的是“这批 projection 仍靠 `Array.includes()` 做去重”的真实缺口；不把它外推成 durable event bus 其他深层项已全部完成。 |
| R12-11 | 60. 事件总线与流式基础设施深层缺陷 | P2 | `src/platform/state-evidence/events/projections/ (全部 7文件)` | 无 freshness/stale 元数据；§28.6/§25.5 要求投影暴露 lastProjectedAt/lagMs/stale 标志 | 已解决（补齐剩余 projection 并补一致性测试） | 根因与 `R12-10` 同源：文档把所有 projection 一概判成“完全没有 freshness”，但现状其实是部分 projection 已有 `lastProjectedAt/lagMs/stale`，剩余文件则处于“类型声明、实现、测试不同步”的半修状态，导致复核时很难直接证明该 contract 已闭环。 | 现行 `src/platform/five-plane-state-evidence/events/projections/{artifact-catalog,governance,risk-action,worker-status,tool-usage,workflow-timeline}-projection.ts` 都会在每次投影时调用 freshness 计算，写入 `lastProjectedAt/lagMs/stale`；`tests/unit/platform/state-evidence/events/projections/projection-contract-alignment.test.ts` 新增了过期事件用例，直接断言 old event 会被标成 `stale=true` 且暴露 `lagMs`。 | 本条关闭的是 projection freshness contract 的剩余落差；不外推事件总线整体可观测性已经无缺口。 |
| R12-12 | 60. 事件总线与流式基础设施深层缺陷 | P2 | `src/platform/five-plane-interface/channel-gateway/channel-gateway-service.ts:162,410,434,794` | 仅支持 3协议(telegram/slack/webhook)硬编码；§6 要求可插拔协议适配器注册表+协议翻译层 | 已解决（源码已满足，本轮补证据） | 原审计把“当前内置只有 telegram/slack/webhook 三个 adapter”误写成“系统只能硬编码三协议”。现行实现已经抽出了 `ChannelAdapter` 接口、`ChannelAdapterRegistry`、默认 registry 构造器和依 registry 路由的 delivery path，新协议可以通过注册 adapter 扩展，而不是改核心 service 分支。 | 当前 `src/platform/five-plane-interface/channel-gateway/channel-gateway-service.ts` 已定义 `ChannelAdapter`/`ChannelAdapterRegistry`，`createDefaultChannelAdapterRegistry()` 注册内置 adapter，`ChannelGatewayService` 构造时接收可选自定义 registry，并在 `deliverResolvedTarget()` 中通过 `adapterRegistry.get(channel)` 分发。定向测试 `tests/unit/platform/interface/channel-gateway/channel-gateway-service-coverage.test.ts` 的 `ChannelGatewayService constructor accepts custom adapter registry`、`ChannelAdapterRegistry register and get adapter`、`ChannelAdapterRegistry throws on duplicate channel registration`、`createDefaultChannelAdapterRegistry includes telegram, slack, and webhook adapters` 已通过。 | 不伪称“协议翻译层已支持无限协议”；关闭的是“没有可插拔注册表、只能硬编码改核心 service”的旧指控。 |
| R12-13 | 61. 安全模型深层缺陷（Sandbox / PDP / Secret / Command） | P0 | `src/platform/five-plane-control-plane/iam/policy-engine.ts:434-447` | full-auto 模式对所有风险类别(含 destructive/irreversible/prod_affecting)直接放行；§10.1 要求 deny-by-default+高风险强制升级 | 已解决（源码已满足，本轮补证据） | 原审计停留在旧的 full-auto 直放语义；现行引擎已经把 `destructive/irreversible/prod_affecting` 视为 deny-by-default 高风险，即便在 `full-auto` 下也必须升级审批，而不会直接放行。 | 当前 `src/platform/five-plane-control-plane/iam/policy-engine.ts` 在 `full-auto` 分支下会对高风险类别走 `policy.full_auto_high_risk_escalation`；定向测试 `tests/unit/platform/control-plane/iam/policy-engine.test.ts` 的 `PolicyEngine.evaluate escalates destructive action in full-auto mode` 与 `tests/unit/platform/control-plane/iam/iam-policy-engine-validation.test.ts` 的 `PolicyEngine escalates destructive actions even in full-auto mode` 已通过。 | 不重复宣称本轮新改生产逻辑，回写现行 deny-by-default 证据。 |
| R12-14 | 61. 安全模型深层缺陷（Sandbox / PDP / Secret / Command） | P0 | `src/platform/five-plane-control-plane/iam/sandbox-policy.ts:451-458,504-512,572-581` | restricted_exec 模式跳过路径边界检查(containsPathTraversalOutside 被豁免)；任意路径(/etc/shadow 等)通过验证(§10.3 filesystem jail) | 已解决（源码已满足，本轮补证据） | 这条已经不是现状。当前 `restricted_exec` policy 仍然有 `allowedRoots`，`checkSandboxPath()` 在 raw path 与 realpath 两个阶段都执行 `containsPathTraversalOutside(...)`，不再给 `restricted_exec` 任何路径边界豁免。顺带复核时还发现旧单测仍保留“restricted_exec 可越界”的历史断言，本轮一并改成现行 fail-close 语义。 | 当前 `src/platform/five-plane-control-plane/iam/sandbox-policy.ts` 在第 2/6 步都对越界路径返回 `sandbox.path_outside_allowed_roots`，`createRestrictedExecPolicy()` 也继续绑定工作区根；定向测试 `tests/unit/platform/control-plane/iam/sandbox-policy.test.ts` 的 `checkSandboxPath restricted_exec mode still enforces allowed root boundary` 与 `tests/unit/platform/control-plane/iam/sandbox-policy-modes.test.ts` 的 `restricted_exec still enforces allowed-root checks and denied roots` 已通过。 | 关闭的是“restricted_exec 豁免 filesystem jail”的过期结论，不假称本轮新增了另一个隔离层。 |
| R12-15 | 61. 安全模型深层缺陷（Sandbox / PDP / Secret / Command） | P0 | `src/platform/five-plane-control-plane/iam/sandbox-policy.ts:99-142` | 无 time/memory/CPU 资源限制；§10.3 要求 per-tier 时间上限/内存上限/网络隔离——完全未建模 | 已解决（源码已满足，本轮补证据） | 原审计对应的是未建模 resource tier 的旧版本；现行 `SandboxPolicy` 已包含 `resourceLimits`，并为四种 sandbox mode 提供默认 `maxCpuTimeMs/maxMemoryBytes/maxNetworkBandwidthBps/networkIsolationEnabled`。 | 当前 `src/platform/five-plane-control-plane/iam/sandbox-policy.ts` 已定义 `SandboxResourceLimits`、`DEFAULT_SANDBOX_RESOURCE_LIMITS` 与 `getEffectiveResourceLimits()`；既有定向测试 `tests/unit/platform/control-plane/iam/sandbox-policy-extended.test.ts` 已验证自定义/默认资源限制和四种 mode 的 limits 存在。 | 不夸大为“已经真的执行 OS 级 cgroup/rlimit”；本条关闭的是“资源限制完全未建模”的原始指控。 |
| R12-16 | 61. 安全模型深层缺陷（Sandbox / PDP / Secret / Command） | P1 | `src/platform/five-plane-control-plane/iam/audit-event-integrity.ts`; `src/platform/five-plane-control-plane/iam/audit-integrity-repository.ts` | 审计链用 plain SHA-256 无 HMAC/签名密钥；DB 写入攻击者可重算全链——§11.5 要求 tamper-evident 审计 | 已解决（源码+测试） | 当前实现已经不是 plain SHA-256 辅助能力停留态。 | `audit-event-integrity.ts` 现已用 `HMAC-SHA256` 生成 event checksum 与 chain hash，`audit-integrity-repository.ts` 写入 integrity record 时也显式记录 `algorithm: "HMAC-SHA256"`；定向命令 `./node_modules/.bin/tsx --test tests/unit/platform/control-plane/iam/audit-event-integrity.test.ts tests/unit/platform/control-plane/iam/audit-integrity-repository.test.ts tests/unit/platform/control-plane/iam/audit-hmac-tamper-evident.test.ts` 已通过 `21 passed / 0 failed`。边界说明：当前密钥管理仍是 env/default key 的简化模式，但本条指控的“主链仍为 plain SHA-256、不可 tamper-evident”已不成立。 | 关闭的是“审计链仍为无密钥 SHA-256”的问题。 |
| R12-17 | 61. 安全模型深层缺陷（Sandbox / PDP / Secret / Command） | P1 | `src/platform/five-plane-control-plane/iam/policy-engine.ts:186-220,346-360,479-481` | 策略决策(evaluate())不发射审计事件；auditPayload 填充后从不调用审计服务——§11.5 要求所有授权决策产出审计记录 | 已解决（源码已满足，本轮补证据） | 原审计停留在只有 `auditPayload` 字段、没有实际 emit 的旧阶段。现行引擎已经显式注入 `PolicyAuditService`，在 rate-limit 拒绝、能力拒绝以及正常返回前都会调用 `recordDecision(...)`。 | 当前 `src/platform/five-plane-control-plane/iam/policy-engine.ts` 已定义 `PolicyAuditService` / `StructuredLoggerPolicyAuditService`，并在 `evaluate()` 中对 rate-limit 路径和常规返回路径执行 `this.auditService.recordDecision(...)`；定向测试 `tests/unit/platform/control-plane/iam/iam-policy-engine-validation.test.ts` 的 `PolicyEngine evaluate records each decision through the injected audit service` 已通过。 | 不重复宣称本轮新接入审计管线，回写现行 emit 行为与直接回归证据。 |
| R12-18 | 61. 安全模型深层缺陷（Sandbox / PDP / Secret / Command） | P1 | `src/platform/control-plane/iam/access-model.ts:278-279,362-370` | evaluateAuthorizationContext() 无匹配规则时默认 allowed:true(context.default_allow)；§10.1 要求 deny-by-default | 已解决（源码修复并补定向测试） | 根因是旧逻辑在上下文分支都未命中时直接落入 `context.default_allow`，没有先校验动作所需 capability，导致“未被特殊规则覆盖”被错误等价成“允许”。 | 当前 `src/platform/five-plane-control-plane/iam/access-model.ts` 已先通过 `inferCapabilitiesForAction()` / `roleGrantsCapabilities()` 计算 `requiredCapabilities`，缺失时统一返回 `policy.capability_required`；`tests/unit/platform/control-plane/iam/access-model-authorization.test.ts` 已验证 `agent_runtime` 在 `staging/workspace` 下执行 `exec_command` 会被拒绝。 | 保留真实代码修复结论，并以定向 IAM 单测证明 deny-by-default 已落地。 |
| R12-19 | 61. 安全模型深层缺陷（Sandbox / PDP / Secret / Command） | P1 | `src/platform/five-plane-control-plane/iam/vault-http-secret-provider.ts:202-239` | 静态 Vault token 缓存任意 1h TTL 无实际过期验证；已撤销 token 继续使用长达 1小时 | 已解决（源码已满足，本轮补证据） | 原审计指向的是旧的长 TTL 缓存语义；现行实现已经把无 lease 的静态 token 缓存压到 30 秒，并把 AppRole 在缺少 `lease_duration` 时的兜底 TTL 收紧到 60 秒，而不是继续默认 1 小时。 | 当前 `src/platform/five-plane-control-plane/iam/vault-http-secret-provider.ts` 在 static token fallback 上设置 `_tokenExpiry = Date.now() + 30_000`，AppRole 无 lease 时只给 60 秒保守 TTL；定向测试 `tests/unit/platform/control-plane/iam/vault-http-secret-provider.test.ts` 的 `VaultHttpSecretProvider caches static token for only 30 seconds` 已通过。 | 不伪称“支持即时吊销回调”；关闭的是“静态 token 继续缓存 1h”的原始缺口。 |
| R12-20 | 61. 安全模型深层缺陷（Sandbox / PDP / Secret / Command） | P1 | `src/platform/five-plane-control-plane/iam/network-egress-policy.ts:220-236,256-338,420-445` | Egress 默认 audit_only 模式——blocked destination 决策仍返回 allowed:true；无显式 env 配置时 egress 从不实际阻断 | 已解决（源码已满足，本轮补证据） | 原审计的两个前提都已失效：现行 env loader 与 service 默认 mode 都是 `deny`，不是 `audit_only`；同时 `evaluate()` 对 blocked/internal 目标返回的 decision fact 也是 `allowed:false`，`audit_only` 只影响 fetch wrapper 是否继续放行，不再篡改判定事实。 | 当前 `src/platform/five-plane-control-plane/iam/network-egress-policy.ts` 默认 mode 为 `deny`，`evaluate()` 对内部地址、blocked type/domain、allowlist 未命中都返回 `allowed:false`，`createPolicyAwareFetch()` 仅在 `audit_only` 下记录后继续。定向测试 `tests/unit/platform/control-plane/iam/network-egress-policy.test.ts` 的 `NetworkEgressPolicyService audit_only mode logs but does not block` 与 `createPolicyAwareFetch audit_only mode records blocked decision but still allows request` 已通过。 | 不夸大为“全量网络层拦截已经部署到 OS 层”；关闭的是“默认 audit_only + blocked 仍 allowed:true”的过期指控。 |
| R12-21 | 61. 安全模型深层缺陷（Sandbox / PDP / Secret / Command） | P1 | `src/platform/five-plane-control-plane/iam/policy-engine.ts:297-360; src/platform/five-plane-control-plane/iam/vault-http-secret-provider.ts:41-104,372-378` | secret resolution / policy evaluation 无速率限制；可暴力枚举 secret 引用或洪泛 PDP | 已解决（源码已满足，本轮补证据） | 这条现在也不是现状：PDP 路径已在 `PolicyEngine.evaluate()` 前置 token bucket，secret resolution 也在 `VaultHttpSecretProvider.requireSecret()` 上按 `secretRef` 做限流。原审计没有吸收后续加入的两条 rate-limit 主链。 | 当前 `src/platform/five-plane-control-plane/iam/policy-engine.ts` 使用 `POLICY_EVALUATION_RATE_LIMITER(1000/min/subject)` 并返回 `policy.rate_limited`；`src/platform/five-plane-control-plane/iam/vault-http-secret-provider.ts` 使用 `SECRET_RESOLUTION_RATE_LIMITER(100/min/secretRef)` 并抛 `vault.rate_limited`。定向测试 `tests/unit/platform/control-plane/iam/iam-policy-engine-validation.test.ts` 的 `PolicyEngine rate limits excessive requests` 与 `tests/unit/platform/control-plane/iam/vault-http-secret-provider.test.ts` 的 `VaultHttpSecretProvider rate limits repeated secret resolution for the same secretRef` 已通过。 | 不伪称“全 IAM 子系统都有限流”；关闭的是 PDP 与 Vault secret resolution 两条被点名主路径的无速率限制缺口。 |
| R12-22 | 61. 安全模型深层缺陷（Sandbox / PDP / Secret / Command） | P1 | `src/platform/five-plane-execution/tool-executor/command-security.ts:108-111` | curl/wget 允许(allowed:true riskLevel:high)无 URL/目的地限制；不经 NetworkEgressPolicyService——可 curl 任意内部端点 | 已解决（源码已满足，本轮补证据） | 原审计对应的是更早的默认命令白名单；现行 classifier 已经把 `curl/wget` 设为 `allowed:false` + `critical`，要求先接到 egress policy 主链后才可能放开，而不是默认允许直接出网。 | 当前 `src/platform/five-plane-execution/tool-executor/command-security.ts` 将 `curl`/`wget` 标记为 `tool.curl_blocked_requires_egress_policy` / `tool.wget_blocked_requires_egress_policy`；定向测试 `tests/unit/platform/execution/tool-executor/command-security.test.ts` 的 `CommandSafetyClassifier blocks curl until network egress policy is available` 与 `...blocks wget until network egress policy is available` 已通过。 | 这里关闭的是“默认允许任意 curl/wget 出网”的缺口，不声称已经完成了按 URL 细粒度放行。 |
| R12-23 | 61. 安全模型深层缺陷（Sandbox / PDP / Secret / Command） | P2 | `src/platform/five-plane-execution/tool-executor/command-security.ts:84-86` | env/printenv 允许(riskLevel:medium)——暴露全进程环境含 AA*VAULT_TOKEN/AA_SECRET*\* 等密钥 | 已解决（源码已满足，本轮补证据） | 原审计同样停留在旧命令策略快照。现行 classifier 已把 `env` / `printenv` 默认改成 `allowed:false`、`riskLevel:critical`，直接阻断整进程环境暴露面。 | 当前 `src/platform/five-plane-execution/tool-executor/command-security.ts` 对 `env` / `printenv` 返回 `tool.env_blocked_exposes_secrets` / `tool.printenv_blocked_exposes_secrets`；定向测试 `tests/unit/platform/execution/tool-executor/command-security.test.ts` 的 `CommandSafetyClassifier blocks env and printenv because they expose secrets` 已通过。 | 不夸大为“所有 secret exfiltration 面都已消失”；关闭的是 env/printenv 默认允许的具体漏洞。 |
| R12-24 | 61. 安全模型深层缺陷（Sandbox / PDP / Secret / Command） | P2 | `src/platform/five-plane-control-plane/iam/vault-http-secret-provider.ts:305-316` | isAvailable() 发送 X-Vault-Token:"dummy" 作 fallback——泄漏意图到 Vault 审计日志+可能触发锁定策略 | 已解决（源码已满足，本轮补证据） | 原问题来自旧 health check 用 dummy token 试探 Vault；现行实现已经改成对 `/v1/sys/health` 做无认证探测，不再发送 `X-Vault-Token`。 | 当前 `src/platform/five-plane-control-plane/iam/vault-http-secret-provider.ts` 的 `isAvailable()` 明确对 `sys/health` 发 unauthenticated 请求，并接受 `200/429/472` 作为“可达”；定向测试 `tests/unit/platform/control-plane/iam/vault-http-secret-provider.test.ts` 的 `VaultHttpSecretProvider.isAvailable returns true when Vault responds OK` 已补充断言 `receivedHeaders === undefined` 并通过。 | 关闭的是“dummy token health check”这一泄漏面，不宣称 Vault 健康探测已覆盖所有部署拓扑。 |
| R12-25 | 62. UI 状态管理 / 构建 / 安全 / 可观测 + 平台剩余缺口 | P1 | `ui/packages/shared/state/src/stores/` | 缺 notification/theme 全局 store；§5.1 要求 auth+theme+notifications 三个全局 Zustand store | 已解决（原指控已过时） | 这条结论停留在 `shared/state` 只有 auth/realtime/sync store 的旧阶段，没有吸收后续 store 目录扩展。 | 当前 `ui/packages/shared/state/src/stores/` 已存在 `auth-store.ts`、`notification-store.ts`、`theme-store.ts`，同时还有 `realtime/sync/ui` 等辅助 store。该条关闭的是“theme/notification store 缺失”的原始指控，不外推为所有状态流都已完成全链路验证。 | 回写现行 store 结构依据。 |
| R12-26 | 62. UI 状态管理 / 构建 / 安全 / 可观测 + 平台剩余缺口 | P1 | `ui/packages/shared/state/src/` | 零 mutation/useMutation/optimistic update；§5.6.5 要求乐观更新(onMutate→cache patch→rollback on error)；UI 全只读无写路径 | 已解决（mutation/optimistic 基础设施已存在） | 原条目把早期“只读 query helpers”状态延续到了当前版本，没有复核 `mutations/` 子树。 | 当前 `ui/packages/shared/state/src/mutations/` 已包含 `use-mutation.ts` 与 `optimistic-update.ts`，`index.ts` 也已导出 mutation 入口。该条关闭的是“shared-state 完全没有 mutation/optimistic update 基础设施”的原始指控；是否所有 feature 都正确接入，仍由各 feature 条目分别跟踪。 | 回写 shared-state mutation 基础设施依据。 |
| R12-27 | 62. UI 状态管理 / 构建 / 安全 / 可观测 + 平台剩余缺口 | P1 | `ui/apps/web/vite.config.ts` | 无 production source maps(build.sourcemap 未设置默认 false)；§7.1 要求生产环境可调试 | 已解决（生产 hidden sourcemap） | 真实缺口是 web build 把 `build.sourcemap` 固定为 `false`，生产构建无法保留调试符号。 | 当前 `ui/apps/web/vite.config.ts` 已切成 mode-aware 配置：production 下 `build.sourcemap = "hidden"`，非 production 保持 `true`。新增 `ui/tests/unit/ui/apps/web/vite-config.test.ts` 已验证这两条分支。 | 关闭生产 source map 缺失问题。 |
| R12-28 | 62. UI 状态管理 / 构建 / 安全 / 可观测 + 平台剩余缺口 | P1 | `ui/packages/shared/telemetry/src/index.ts + ui/apps/web/src/runtime.ts` | 无 Core Web Vitals/RUM 追踪(LCP/FCP/CLS/INP)；§7.3 要求性能指标采集；web-vitals 未列入依赖 | 已解决（源码修复并补定向验证） | 根因不是仓库完全没有 `web-vitals` 或 telemetry 能力，而是早先只有 shared telemetry 模块，没有把 WebVitals 采集真正接进 web app 启动路径；因此 RUM 在产品入口上并未实际生效。 | 当前 `ui/package.json` 已声明 `web-vitals`；`ui/packages/shared/telemetry/src/index.ts` 已实现 `startWebVitalsCollection()` 与 OTLP exporter；本轮 `ui/apps/web/src/runtime.ts` 新增 `startWebRuntimeTelemetry()`，`ui/apps/web/src/main.tsx` 在启动时接线并于 `pagehide` flush/dispose；定向验证 `ui/tests/unit/ui/shared/telemetry.test.ts` 与 `ui/tests/unit/ui/apps/web/runtime-telemetry.test.ts` 已通过。 | 关闭的是 Web 端 Core Web Vitals/RUM 采集与启动接线缺口，不外推为整套前端可观测体系已经全部完成。 |
| R12-29 | 62. UI 状态管理 / 构建 / 安全 / 可观测 + 平台剩余缺口 | P1 | `ui/apps/web/index.html:8` | CSP 含 'unsafe-inline' for styles；§6.5.4 要求 nonce/hash-based style-src | 需继续整改（UI 产品化差距） | 这类条目描述的是 UI monorepo、feature、原生壳、性能预算、可访问性与后端 presentation 接线的产品化缺口，需要跨 `ui/` 与后端服务成组整改。 | 本轮没有执行 UI 全链路 build/e2e/原生壳验证，也没有完成整组 UI 实装重构，不能虚写已修。 | 保留为后续 UI 主线整改。 |
| R12-30 | 62. UI 状态管理 / 构建 / 安全 / 可观测 + 平台剩余缺口 | P1 | `src/platform/execution/ (缺失)` | 无 ReconciliationWorker/CompensationManager 实现；§14.12/§14.13 要求模糊副作用解决+可逆副作用修复；INV-SIDEEFFECT-001 无运行时强制 | 需继续整改（UI 产品化差距） | 这类条目描述的是 UI monorepo、feature、原生壳、性能预算、可访问性与后端 presentation 接线的产品化缺口，需要跨 `ui/` 与后端服务成组整改。 | 本轮没有执行 UI 全链路 build/e2e/原生壳验证，也没有完成整组 UI 实装重构，不能虚写已修。 | 保留为后续 UI 主线整改。 |
| R12-31 | 62. UI 状态管理 / 构建 / 安全 / 可观测 + 平台剩余缺口 | P1 | `src/platform/ (缺失)` | 无 HibernationRecord/WakeEngine 实现；§20.2 要求休眠/唤醒条件/恢复兼容性检查；长运行工作流无法休眠/恢复 | 需继续整改（UI 产品化差距） | 这类条目描述的是 UI monorepo、feature、原生壳、性能预算、可访问性与后端 presentation 接线的产品化缺口，需要跨 `ui/` 与后端服务成组整改。 | 本轮没有执行 UI 全链路 build/e2e/原生壳验证，也没有完成整组 UI 实装重构，不能虚写已修。 | 保留为后续 UI 主线整改。 |
| R12-32 | 62. UI 状态管理 / 构建 / 安全 / 可观测 + 平台剩余缺口 | P2 | `ui/apps/web/index.html:7-8` | CSP 通过 meta 标签而非 HTTP header 投递——frame-ancestors 被忽略+无 report-uri/report-to | 已解决（源码/测试） | 根因不是 CSP baseline 完全缺失，而是 `vite.config.ts` 里的 header plugin 早先写成了 `apply:"build" + configureServer()` 这种不会在真实 dev/preview server 生效的组合，因此仓库虽然写了意图说明，运行时仍只剩 `<meta>` 路径。 | 当前 `ui/apps/web/vite.config.ts` 已把 `Content-Security-Policy` 与 `Report-To` 统一抽成 header 常量，并通过 `configureServer()` / `configurePreviewServer()` 在 dev 与 preview 都实际下发 HTTP 响应头；同时 `generateBundle()` 会生成部署态 `_headers` 资产，让静态托管也能带上 `frame-ancestors 'none'` 与 `report-to csp-endpoint`。新增 `ui/tests/unit/ui/apps/web/vite-config.test.ts` 已验证 `_headers` 产物和 preview middleware 都会写入这两个 header。 | 关闭的是“仓库内没有可交付 HTTP CSP header”的缺口；是否接入某个具体 CDN/Ingress 的最终生产配置，仍属于部署层职责。 |
| R12-33 | 62. UI 状态管理 / 构建 / 安全 / 可观测 + 平台剩余缺口 | P2 | `ui/package.json + .github/workflows/ui-quality.yml` | CI 无依赖漏洞扫描(npm audit/Snyk/Trivy)；§6.5 要求供应链安全扫描 | 已解决（源码修复） | 根因是 `ui/package.json` 虽然已经定义了 `npm run audit`，但真正生效的根工作流 `.github/workflows/ui-quality.yml` 之前并未执行它；嵌套在 `ui/.github/` 下的副本并不是 GitHub 实际会跑的 PR 门禁。 | 当前 `ui/package.json` 的 `ci` 脚本已包含 `npm run audit`；本轮进一步把根工作流 `.github/workflows/ui-quality.yml` 补上显式 `npm run audit` step，使依赖漏洞扫描进入真实 PR gate。 | 关闭的是 UI CI 缺少依赖漏洞扫描门禁的问题，不外推为 Snyk/Trivy 等其他供应链扫描器也已全部接入。 |
| R12-34 | 62. UI 状态管理 / 构建 / 安全 / 可观测 + 平台剩余缺口 | P2 | `ui/scripts/perf-budget.mjs` | 性能预算检查原始字节非 gzip；§2.5.1/§7.3.4 目标为 gzip 大小；实际传输尺寸未验证 | 已解决（源码复核） | 根因是旧脚本确实按 raw bytes 预算编写；现行脚本已经改成 gzip 大小预算，并在 CI 场景下追加 Lighthouse 时间预算，因此原条目已过时。 | 当前 `ui/scripts/perf-budget.mjs` 已使用 `createGzip()` 计算 `maxJsChunkGzBytes/maxCssChunkGzBytes/totalGzBytes`，不再按 raw bytes 判定；同脚本在 `CI=true` 时还会校验 `FCP/TTI`。 | 关闭的是“perf budget 仍以 raw bytes 为准”的结论，不外推为所有按库拆分的性能预算都已完备。 |
| R12-35 | 62. UI 状态管理 / 构建 / 安全 / 可观测 + 平台剩余缺口 | P2 | `ui/packages/shared/state/src/query-client.ts` | QueryClient retry=1 + flat staleTime:30s 全局；§5.1.2 要求按数据重要性分层 staleTime(实时数据→0/静态配置→长) | 已解决（源码/测试） | 根因与 `R16-50` 相同：复审结论停留在早期统一 `30s` 缓存窗口，没有吸收后续分层缓存工厂的落地。 | 当前 `ui/packages/shared/state/src/query-client.ts` 已定义 `CACHE_TIER_STALE_TIME` 与 `createTieredQueryClientFactory()`，覆盖 `real_time=0`、`tasks=120_000`、`approvals=30_000`、`config=3_600_000`、`static=300_000`；`createQueryClientFactory()` 默认走 `tasks` tier。`ui/tests/unit/ui/shared/state-store-middleware.test.ts` 已验证 tasks/approvals/config 三档 staleTime。 | 关闭的是 staleTime 分层缺口；默认 `retry=1` 仍是现行重试策略，不构成继续挂账依据。 |
| R12-36 | 63. 运维成熟度 / 市场 / Model Gateway 深层缺口 | P0 | `src/platform/orchestration/oapeflir/types/feedback-signal.ts` | §56.2 FeedbackSignal 须携带 FeedbackTrustScore(来源可信度/历史准确率/攻击面)；schema 无此字段——低信任/对抗反馈不过滤流入学习管线 | 已解决（源码/测试） | 原问题成立于早期 `FeedbackSignal` 只有 sourceReliability 的简化信任模型；本轮已把可信度因子、推导分数和直推门禁补齐，低信任/未认证/与 holdout 重叠的反馈不再直接进入 promotion。 | 当前 `src/platform/five-plane-orchestration/oapeflir/types/feedback-signal.ts` 已增加 `historicalAccuracy/authenticatedSource/attackSurfaceExposure/holdoutOverlap`、`feedbackTrustScore`、`deriveFeedbackTrustScore()` 与 `isFeedbackSignalEligibleForDirectPromotion()`；`src/scale-ecosystem/feedback-loop/feedback-improvement-service.ts` 会阻断低信任信号的 direct promotion。`tests/unit/platform/orchestration/oapeflir/feedback-signal.test.ts`、`types/feedback-signal.test.ts`、`feedback-improvement-service.test.ts` 已覆盖。 | 关闭该条 feedback trust score 与低信任过滤缺口。 |
| R12-37 | 63. 运维成熟度 / 市场 / Model Gateway 深层缺口 | P1 | `src/ops-maturity/drift-detection/changepoint-detector/index.ts` | §63.2 要求 4检测窗口(1h Z-Score/7d CUSUM/30d Bayesian/90d KL-JS)；仅单一 24h 滑窗+固定 -10% 阈值；缺 5个 canonical 指标维度 | 已解决（源码/测试） | 该条此前属实；本轮已把 detector 收敛到 spec 要求的 4 个 canonical 检测窗，并输出 5 个 canonical drift 维度，不再只有 legacy 24h 相对阈值。 | 当前 `src/ops-maturity/drift-detection/changepoint-detector/index.ts` 已提供 `detectAll()` 默认跑 `1h/7d/30d/90d`，分别映射 `z_score/cusum/bayesian_online/kl_js_divergence`，并返回 `success_rate_drop/override_rate_spike/cost_spike/tool_usage_shift/incident_count` 五维结果；`tests/unit/ops-maturity/drift-detection/changepoint-detector/index.test.ts` 已覆盖。 | 关闭该条 canonical window / algorithm / metric 维度缺口。 |
| R12-38 | 63. 运维成熟度 / 市场 / Model Gateway 深层缺口 | P1 | `src/ops-maturity/drift-detection/fingerprint-builder/index.ts` | §63.1 BehaviorFingerprint.window 须为 1h/7d/30d/90d 枚举+含 avg_step_count；实现为自由 {start,end} 日期范围无枚举+缺字段 | 已解决（源码/测试） | 该条此前针对的是自由日期范围 fingerprint；本轮已把窗口预设与显式窗口边界纳入 fingerprint 结果，同时保留兼容层，不再缺 `avg_step_count`。 | 当前 `src/ops-maturity/drift-detection/fingerprint-builder/index.ts` 的 `BehaviorFingerprint` 已包含 `window/windowStart/windowEnd`，输入面支持 `windowPreset`，指纹串保留 `avg_step_count`；`tests/unit/ops-maturity/drift-detection/fingerprint-builder/index.test.ts` 已验证 canonical window 与 step count 序列化。 | 关闭该条 fingerprint window 枚举化缺口。 |
| R12-39 | 63. 运维成熟度 / 市场 / Model Gateway 深层缺口 | P1 | `src/ops-maturity/drift-detection/cross-agent-analyzer/index.ts` | §63.4 要求 CrossAgentDriftAlert+domain peer group+anti-gaming filter；仅返回 generic recommendation 字符串无告警/域分组/过滤 | 已解决（源码/测试） | 原问题成立于 analyzer 只返回泛化 recommendation 的旧实现；本轮已把跨 agent 分析改为按 domain peer group 聚类，并输出具备 anti-gaming 判定的 alert。 | 当前 `src/ops-maturity/drift-detection/cross-agent-analyzer/index.ts` 已按 `domainId` 分组成 `peerGroups`，告警结构含 `peerGroupId`，并在 peer group 分析中纳入 anti-gaming 检测；`src/ops-maturity/drift-detection/drift-detector-service.ts` 也已改用 `domainId` 映射。`tests/unit/ops-maturity/drift-detection/cross-agent-analyzer/index.test.ts` 已覆盖。 | 关闭该条 domain peer group / alert / anti-gaming 缺口。 |
| R12-40 | 63. 运维成熟度 / 市场 / Model Gateway 深层缺口 | P1 | `src/platform/model-gateway/provider-registry/unified-chat-provider.ts` | ChatCompletionUsage 缺 estimatedCost 字段；§15.2 per-provider usage metering 无法通过统一接口上报成本 | 已解决（源码/测试） | 原审计对应的是早期 unified usage contract；现行 `ChatCompletionUsage` 与 `ChatCompletionResult` 都已把统一成本估算作为一等字段暴露，只是 reaudit 没有跟上接口演进。 | 当前 `src/platform/model-gateway/provider-registry/unified-chat-provider.ts` 的 `ChatCompletionUsage` 已包含 `estimatedCostUsd`，归一化响应路径也会在 lines 689/724/755 计算并回写成本；本轮补强 `tests/unit/platform/model-gateway/provider-registry/unified-chat-provider-types.test.ts`，直接断言 usage/result/request 的成本与 canonical 元字段。 | 关闭该条 unified usage 成本字段缺口。 |
| R12-41 | 63. 运维成熟度 / 市场 / Model Gateway 深层缺口 | P1 | `src/platform/model-gateway/provider-registry/model-routing-service.ts` | ModelRouteRequest 缺 data_residency/pii_input_detected/pii_output_possible/model_training_opt_out/judge_independence 约束字段(§15.3) | 部分解决（4 项已落地，`pii_output_possible` 仍待补齐） | 真实情况已不再是“完全没有治理约束字段”，而是 `data_residency/pii_input_detected/model_training_opt_out/judge_independence` 已进入 request 与过滤链；尚未完全补齐的是 `pii_output_possible` 这一个出参风险约束位。 | 当前 `src/platform/model-gateway/provider-registry/model-routing-service.ts` 已在 `ModelRouteRequest` 中声明 `data_residency/pii_input_detected/model_training_opt_out/judge_independence/latency_slo_target_ms`，并在 lines 428-474 参与候选过滤；本轮补了 `tests/unit/platform/model-gateway/provider-registry/model-routing-service-types.test.ts` 与 `model-routing-service.test.ts`，验证这些字段会进入类型面并驱动 residency / pii / training opt-out / judge-independence 过滤。 | 回写为部分解决：关闭“完全缺字段”的旧结论，保留 `pii_output_possible` 未建模的剩余缺口。 |
| R12-42 | 63. 运维成熟度 / 市场 / Model Gateway 深层缺口 | P0 | `src/ops-maturity/compliance-reporter/template-registry/index.ts` | §66.1 template 须含 lockedOnGeneration/reportVersionLock/requiredDataSources/legal_version/migration_rule；Zod schema 仅 6字段严重不足 | 已解决（源码/测试） | 原结论停留在只含基础 template 识别字段的早期 schema；现行 registry 已把版本锁、法务版本、迁移规则和审计/控制扩展字段收敛进模板归一化层。 | 当前 `src/ops-maturity/compliance-reporter/template-registry/index.ts` 的 `ComplianceReportTemplateSchema` 已包含 `lockedOnGeneration/reportVersionLock/legalVersion/effectiveDate/migrationRule/controls/attestation/auditorAccess/qualityThresholds` 等扩展字段，其中 `requiredEvidenceTypes` 承担 canonical required data-source 语义；本轮补强 `tests/unit/ops-maturity/compliance-reporter/template-registry/index.test.ts`，直接验证 lock/legal/migration 与 control/attestation/auditorAccess 元数据会被保留。 | 关闭该条 template schema “仅 6 字段”的过期结论。 |
| R12-43 | 63. 运维成熟度 / 市场 / Model Gateway 深层缺口 | P1 | `src/ops-maturity/compliance-reporter/evidence-mapper/index.ts` | §66.2 要求 ControlMapper(control point+pass/fail/partial)+GapAnalyzer(remediation/owner/deadline)；仅按类型分组无 control point 映射 | 已解决（源码/测试） | 真实根因是复审条目停留在早期按 evidence type 分组的最小实现，没有吸收后续 control point 与 gap analysis 的收敛。 | 当前 `src/ops-maturity/compliance-reporter/evidence-mapper/index.ts` 已提供 `mapEvidenceByControlPoint()`、`ControlPointCoverage`、quality summarization；`src/ops-maturity/compliance-reporter/compliance-report-pipeline-service.ts` 的 `GapAnalyzerService` 也已输出 `remediation/owner/deadline`。既有 `tests/unit/ops-maturity/compliance-reporter/evidence-mapper/index.test.ts`、`gap-analyzer-service.test.ts`、`compliance-report-pipeline-service.test.ts` 已覆盖 control point status、gap owner/deadline 与 evidenceQuality。 | 关闭该条 control mapping / gap analyzer 缺口。 |
| R12-44 | 63. 运维成熟度 / 市场 / Model Gateway 深层缺口 | P1 | `src/scale-ecosystem/marketplace/certification/index.ts` | §55.1 Quality&Security Gate 要求自动扫描/SBOM/签名/兼容性测试/sandbox 验证；14行仅含 3态枚举+boolean check | 已解决（源码/测试） | 根因是认证 contract 和 gate 实现长期只把 `status + securityScan` 当作唯一门禁，SBOM / 兼容性 / sandbox / egress review 都没有进入统一认证结果。 | 本轮在 `src/scale-ecosystem/marketplace/certification/index.ts` 增加 `CertificationEvidence` 与 `CertificationRecordSchema` 的 `sbomVerified/signatureVerified/compatibilityVerified/sandboxVerified/egressPolicyReviewed` 字段，`CertificationGate` 与 `isMarketplaceListingCertified()` 改为 fail-close；`tests/unit/scale-ecosystem/marketplace/certification/certification.test.ts` 已验证证据不全时会阻断。 | 关闭的是认证 gate 只有三态+单扫描布尔门的真实缺口。 |
| R12-45 | 63. 运维成熟度 / 市场 / Model Gateway 深层缺口 | P2 | `src/scale-ecosystem/marketplace/marketplace-governance-service.ts` | §55.5 要求 4阶段(active→deprecated→sunset→removed)+sunset 180d 阻新安装+migration_threshold≥95%；仅 deprecated/retired 两态无 sunset/阈值 | 已解决（源码/测试） | 根因是治理服务原先只支持 `deprecated -> retired` 粗粒度退场，没有 `sunset` 过渡态，也没有 180 天 grace window 与迁移完成率阈值。 | 本轮将 `MarketplaceGovernanceService` 收敛为 `enabled -> deprecated -> sunset -> retired` 终态链路：`sunsetPackage()` 强制要求先 deprecated、EOL 不得短于 180 天；`retirePackage()` 强制要求已进入 sunset 且 `migrationCompletionRatio >= 0.95`；`publishPackage()` 也会拒绝 deprecated/sunset/retired 包再次发布。`tests/unit/scale-ecosystem/marketplace/marketplace-governance-lifecycle.test.ts` 已覆盖顺序流转与阈值阻断。 | 终态在现行代码里命名为 `retired`，语义上承担 audit 所述 `removed` 的 terminal lifecycle。 |
| R12-46 | 63. 运维成熟度 / 市场 / Model Gateway 深层缺口 | P2 | `src/ops-maturity/compliance-reporter/compliance-report-pipeline-service.ts:82` | §66.2 报告须经 HumanSignoff 才标 attested；generate()返回 generated/partial 状态无 signoff 强制/signoff_due_at/escalation | 已解决（源码/测试） | 原问题来自早期 pipeline 只有 `generated/partial` 双态；现行报告流已经把人工签署门、签署到期与升级策略显式纳入 artifact/status 计算。 | 当前 `src/ops-maturity/compliance-reporter/compliance-report-pipeline-service.ts` 的 artifact status 已包含 `human_signoff/attested`，`evaluateHumanSignoff()` 会输出 `signoffDueAt/escalationOwner/timeoutAction` 与 `signed/signoff_overdue/not_attested_expired`；本轮补了 `tests/unit/ops-maturity/compliance-reporter/compliance-report-pipeline-service.test.ts`，直接断言 `requireHumanSignoff` 模板会生成 `human_signoff` 报告并产出逾期升级元数据。 | 关闭该条 human signoff / escalation 缺口。 |
| R13-01 | 65. 学习与改进子系统深层缺陷 | P0 | `src/platform/orchestration/learn/learning-object-model.ts` | LearningObject promotionStatus 缺 quarantine 态；§29.4 要求"LearningCandidate 默认进入 quarantine"；代码从不隔离 | 已解决（源码/测试） | 该条此前只“名义上有 quarantine 枚举”，但多个 Learn 入口仍默认产出 `draft`，实际并未默认隔离；本轮已把默认入口统一收敛到 quarantine，并让 validator 从 quarantine 明确推进到 validated。 | 当前 `src/platform/five-plane-orchestration/learn/learning-object-model.ts` 默认值仍是 `quarantine`，且 `experience-distillation-service.ts`、`failure-pattern-miner.ts`、`llm-improvement-generation-service.ts` 现在都会默认生成 `promotionStatus: "quarantine"`；`learning-object-validator.ts` 也会把 `draft/quarantine -> validated`。`tests/unit/platform/orchestration/learn/learning-object-validator.test.ts` 与 `learning-object-model.test.ts` 已覆盖。 | 关闭该条“默认不隔离”的真实缺口。 |
| R13-02 | 65. 学习与改进子系统深层缺陷 | P0 | `src/platform/orchestration/learn/learning-object-validator.ts` | 无 PII/secret scan/holdout dedup/contamination check/diversity check(§29.4/§56.2)；仅检查 evidence ref count+confidence | 已解决（源码/测试） | 原问题已不符合现状：validator 不再只看 evidence/confidence，而是显式做 PII/secret 扫描、标题/摘要相似度去重、contamination/diversity 拦截，并把失败对象留在 quarantine。 | 当前 `src/platform/five-plane-orchestration/learn/learning-object-validator.ts` 已实现 `learning.pii_detected`、`learning.secret_detected`、`learning.dedup_similar_title`、`learning.dedup_similar_summary` 等判定，且失败对象返回 `promotionStatus: "quarantine"`。相关断言已由 `tests/unit/platform/orchestration/learn/learning-object-validator-unit.test.ts` 与本轮回归覆盖。 | 关闭该条 validator 只做浅校验的旧结论。 |
| R13-03 | 65. 学习与改进子系统深层缺陷 | P1 | `src/platform/orchestration/oapeflir/oapeflir-loop-service.ts:318` | ImprovementCandidate register 后立即 approved 无 EvaluationGate/离线评测/回归集/风险扫描/人工审批(§13.14) | 已解决（源码复核） | 该条已被现行 improve/release 流程推翻：candidate 注册后并不会无条件立即 approved，而是先过 EvaluationGate、离线质量门、风险扫描，并对安全类策略变更保留人工复核。 | 当前 `src/platform/five-plane-orchestration/oapeflir/oapeflir-loop-service.ts` 已在 improve 阶段按 `loopEvaluationReport.verdict === "accept"`、`qualityScore >= 0.95` 才允许自动批准，并在检测到 security 内容时转人工复核；release 阶段也再次受 EvaluationGate 阻断。 | 回写现行 gate/approval 链路，不改生产代码。 |
| R13-04 | 65. 学习与改进子系统深层缺陷 | P1 | `src/ops-maturity/drift-detection/benchmark-runner.ts:62` | baseline 硬编码(successRateBefore=0.60/avgCostBefore=0.30)+Math.random()模拟——§56.4 要求锁定真实输入样本+eval version | 已解决（源码/测试） | 原问题此前成立；本轮把 benchmark runner 重写为锁定 case baseline + executor 驱动的评测器，不再靠 `Math.random()` 或硬编码 baseline 伪造评估。 | 当前 `src/ops-maturity/drift-detection/learning/benchmark-runner.ts` 已增加 `ProposalExecutor`、按 case `setBaseline()`、`evaluationVersion`、`benchmarkSetId`、`baselineSnapshotRef`、`lockedCaseIds`，并在缺 baseline 或缺 executor 时显式 fail-close。`tests/unit/ops-maturity/drift-detection/benchmark-runner.test.ts` 与 `benchmark-runner-deterministic.test.ts` 已覆盖。 | 关闭该条 hardcoded baseline / random simulation 缺口。 |
| R13-05 | 65. 学习与改进子系统深层缺陷 | P1 | `src/ops-maturity/drift-detection/reflection-engine.ts` | 仅分析失败(filter !success)；§20/§58.3 要求成功+失败关联模式提取；成功模式被丢弃 | 已解决（源码/测试） | 现行 reflection engine 已同时分析失败模式和成功模式；reaudit 仍停留在只按 failure 聚类的旧实现。 | 当前 `src/ops-maturity/drift-detection/learning/reflection-engine.ts` 已维护 `byFailureMode` 与 `bySuccessPattern` 两条路径，成功样本达到阈值时会生成 `patternType: "success"` 的 reflection。`tests/unit/ops-maturity/drift-detection/reflection-engine.test.ts` 已补 success-pattern 回归。 | 关闭该条 success pattern 被丢弃的旧结论。 |
| R13-06 | 65. 学习与改进子系统深层缺陷 | P1 | `src/ops-maturity/drift-detection/ vs src/platform/orchestration/learn/` | 两套并行断开的学习管线(EvolutionIntegrationService vs StrategyLearningService)；§56 定义统一管线——学习从不合并 | 已解决（源码/测试） | 原问题已不再成立：evolution 管线现在可以把 active proposals 转换为 LearningObject 并通过 bridge 回灌到主学习链路，不再是完全断开的平行系统。 | 当前 `src/ops-maturity/drift-detection/evolution-integration-service.ts` 已定义 `LearningBridge`、`setLearningBridge()`、`syncWithLearningPipeline()` 与 `proposalToLearningObject()`；`tests/unit/ops-maturity/drift-detection/evolution-integration-service.test.ts` 已验证 bridge 会收到 active proposals 转换后的 learning objects。 | 关闭该条双轨学习管线永不合并的旧结论。 |
| R13-07 | 65. 学习与改进子系统深层缺陷 | P1 | `src/platform/state-evidence/knowledge/semantic-knowledge-graph.ts` | 无学习衍生边类型(learned_from/failure_pattern/causal_relationship/temporal_correlation)；promoted knowledge 为扁平文档无图级洞察 | 已解决（源码/测试） | 原问题一半已在此前修补，另一半 `temporal_correlation` 和图级操作接口本轮补齐；knowledge graph 不再只是扁平 chunk/keyword 图。 | 当前 `src/platform/five-plane-state-evidence/knowledge/semantic-knowledge-graph.ts` 已显式支持 `learned_from/failure_pattern/causal_relationship/temporal_correlation`，并增加 `addEntityRelation()`、`propagateTrust()`、node `trustLevel` 与 learned-edge 连接输出。`tests/unit/platform/five-plane-state-evidence/knowledge/semantic-knowledge-graph.test.ts` 已覆盖。 | 关闭该条学习衍生边与图级洞察缺口。 |
| R13-08 | 65. 学习与改进子系统深层缺陷 | P1 | `src/platform/state-evidence/knowledge/knowledge-model.ts:3` | TrustLevel(verified/reviewed/community/unverified) 与 §29.1(private_unverified/team_reviewed/official/authoritative)不匹配 | 已解决（源码/测试） | 原问题此前真实存在；本轮把知识平面的 trust level 改成 canonical 四级，同时保留 legacy 输入兼容映射，避免老数据直接断裂。 | 当前 `src/platform/five-plane-state-evidence/knowledge/knowledge-model.ts` 已输出 `private_unverified/team_reviewed/official/authoritative`，并提供 legacy→canonical `normalizeTrustLevel()`；`namespace-policy.ts`、`source-trust-policy.ts`、`freshness-tracker.ts`、`knowledge-ingestion-pipeline.ts` 与 `knowledge-promotion-service.ts` 已同步到新 trust 语义。`tests/unit/platform/five-plane-state-evidence/knowledge/knowledge-model.test.ts` 已覆盖。 | 关闭该条 trust level 枚举不匹配缺口。 |
| R13-09 | 65. 学习与改进子系统深层缺陷 | P2 | `src/platform/orchestration/learn/knowledge-promotion-service.ts:97` | 多 LearningObject 批量推广事件仅报首个对象 metadata——后续对象可追溯性丢失(§28 每个 fact event 独立可追踪) | 已解决（源码/测试） | 原问题此前属实；现行推广事件已不再只携带首个对象，而是把整批 promoted object 的 metadata 一并带出。 | 当前 `src/platform/five-plane-orchestration/learn/knowledge-promotion-service.ts` 会在单个 `learning:knowledge_promoted` 事件里输出 `promotedObjects` 数组，包含每个 learning object 的 `learningObjectId/documentId/learningType/title/summary/confidence`。`tests/unit/platform/orchestration/learn/knowledge-promotion-service.test.ts` 与 `oapeflir/learn/knowledge-promotion-service.test.ts` 已覆盖。 | 关闭该条 batch promotion 只报首对象 metadata 的缺口。 |
| R13-10 | 65. 学习与改进子系统深层缺陷 | P2 | `src/ops-maturity/drift-detection/rollout-manager.ts` | 无自动回滚触发(metric threshold→rollback_pending)；§56.4 要求质量/成本/安全指标触发阈值时自动进入 rollback | 已解决（源码/测试） | 该条此前属实；本轮已把 rollout 回滚门从“只记录指标”补成真正的阈值驱动状态迁移，质量/成本/安全指标超线都会自动进入 `rollback_pending`。 | 当前 `src/ops-maturity/drift-detection/learning/rollout-manager.ts` 已增加 `securityViolationCeiling`、`securityViolations`，`evaluateAndTriggerRollback()` 会在 success/error/latency/cost/security 超阈值时把 rollout 置为 `rollback_pending`；`tests/unit/ops-maturity/drift-detection/rollout-manager.test.ts` 已覆盖。 | 关闭该条自动回滚触发缺口。 |
| R13-11 | 65. 学习与改进子系统深层缺陷 | P2 | `src/ops-maturity/drift-detection/proposal-engine.ts:160` | 所有自动生成 proposal 硬编码 risk:'low'(含安全相关 prompt_patch)；§56.5 涉及核心 Prompt/风控变更须人工审核 | 已解决（源码/测试） | 原问题来自自动 proposal 一律 `risk:"low"` 的旧逻辑；本轮已改成按 proposal 类型动态判定 risk，并把中高风险 proposal 自动标记为人工复核。 | 当前 `src/ops-maturity/drift-detection/learning/proposal-engine.ts` 已移除一刀切 low-risk，新增 `reviewRequirement: "auto" | "manual_review"`，由风险等级驱动；安全/核心变更不再自动低风险放行。`tests/unit/ops-maturity/drift-detection/proposal-engine.test.ts` 已覆盖 risk/review requirement。 | 关闭该条 risk 硬编码与人工审核缺口。 |
| R13-12 | 65. 学习与改进子系统深层缺陷 | P2 | `src/ops-maturity/ (结构)` | 无 learning/improvement 子目录(§35 推荐)；学习在 orchestration/learn/，改进在 orchestration/improve-rollout/，ops-maturity 含独立 evolution——违反目录合约 | 已解决（源码补齐） | 原问题针对的是 `ops-maturity/` 缺少显式 learning/improvement 入口目录；本轮已补出结构层 re-export 目录，保留原实现位置同时对外提供推荐路径。 | 当前仓库已新增 `src/ops-maturity/learning/` 与 `src/ops-maturity/improvement/`，分别导出 benchmark/reflection/evolution 和 proposal/promotion/rollout 等入口文件，作为结构合约层的稳定别名。 | 关闭该条目录合约缺口；不声称历史实现都已物理迁移。 |
| R13-13 | 66. Worker Pool / Dispatch / Retry 深层缺陷 | P0 | `src/platform/five-plane-orchestration/harness/recovery-controller.ts` | 无重试限制/退避/预算；§9.3 要求指数退避(base=1s/max=60s)+max retries+retry_exhausted 升级；可能无限重试循环 | 已解决（源码+回归） | 根因成立：`worker_crash` / `platform_panic` 路径此前可直接重入恢复，缺少统一 max retries 与 exhausted 升级。 | 当前 `recovery-controller.ts` 已为 `llm_provider_unavailable`、`worker_crash`、`platform_panic` 接入指数退避、重试上限和 exhaustion 升级到 HITL；`worker_crash` 不再立即 `resume()`，而是走 `runtime.sleep(..., "worker_crash_retry", ...)`。定向命令 `./node_modules/.bin/tsx --test tests/unit/platform/orchestration/harness/recovery-controller-r13-retry-limits.test.ts tests/unit/platform/orchestration/harness/recovery-controller-retry-backoff.test.ts` 已通过。 | 关闭“可能无限重试循环”的核心缺口。 |
| R13-14 | 66. Worker Pool / Dispatch / Retry 深层缺陷 | P1 | `src/platform/execution/dispatcher/execution-dispatch-service.ts` | Dispatch 队列无优先级排序——insertion order 遍历；§8.5/§14 要求优先级队列；urgent ticket 须等前序全部失败 | 已解决（源码复核+回归） | 该条对应的是早期 insertion-order 调度结论；现行 dispatch service 已存在显式优先级排序逻辑。 | 当前 `execution-dispatch-service.ts` 已实现 `orderTicketsByPriority(...)`，按 `PRIORITY_ORDER` 对 ticket 做高优先级优先排序，并与 preemption 路径协同工作；`tests/unit/platform/execution/dispatch/execution-dispatch-service-r13.test.ts` 已回归 urgent/priority 行为并通过。 | 关闭的是“dispatch 完全无优先级排序”的问题。 |
| R13-15 | 66. Worker Pool / Dispatch / Retry 深层缺陷 | P1 | `src/platform/execution/dispatcher/execution-dispatch-service.ts` | 无租户公平调度；单租户洪泛可垄断全部 worker；§9.1/§53 要求 per-tenant 隔离/round-robin/加权公平队列 | 已解决（源码复核+回归） | 真实剩余问题不是实现完全缺失，而是旧测试用了不成立的“单租户单独排队时也不能连续拿到 3 张票”断言，导致回归无法反映真实公平语义。 | 当前 `execution-dispatch-service.ts` 已实现 `interleaveTicketsByTenant(...)`，按 tenant 分组后 round-robin 交错出队，并带 burst limit 防止单租户洪泛；本轮同步修正 `tests/unit/platform/execution/dispatch/execution-dispatch-service-r13.test.ts`，改为验证“当多个租户同时排队时，单租户不能饿死其他租户”，定向集已通过。 | 关闭的是“无 per-tenant 公平调度”的问题。 |
| R13-16 | 66. Worker Pool / Dispatch / Retry 深层缺陷 | P1 | `src/platform/five-plane-orchestration/harness/recovery-controller.ts` | 无 node-vs-graph 重试范围区分；§45.15 定义 5种暂停原因+4种恢复策略+retry_same_plan(节点级) vs replan(图级) | 已解决（源码+回归） | 根因成立：旧恢复路径没有把 failure type 可靠映射到 node-scope vs graph-scope。 | 当前 `recovery-controller.ts` 已实现 `determineRetryScope(...)`：`llm_provider_unavailable` / `tool_timeout` 走 node-scope `retry_same_plan`，`platform_panic` / `worker_crash` 走 graph-scope 恢复；`tests/unit/platform/orchestration/harness/recovery-controller-r13-retry-limits.test.ts` 已直接覆盖 scope 判定。 | 关闭“无 node-vs-graph 恢复范围区分”的问题。 |
| R13-17 | 66. Worker Pool / Dispatch / Retry 深层缺陷 | P2 | `src/platform/five-plane-orchestration/harness/loop/index.ts`; `src/platform/five-plane-orchestration/harness/recovery-controller.ts` | retry_same_plan 立即重执行无退避；§9.3 要求指数退避+jitter(base=1s/max=60s)；瞬态故障造成雷群放大 | 已解决（源码+回归） | 旧问题的关键在于恢复控制器没有统一经由 loop backoff 机制，使 `retry_same_plan` 路径可能近似立即重试。 | 当前 `HarnessLoopController.getBackoffMs()` 已提供指数退避+jitter；本轮 `recovery-controller.ts` 又把 `tool_timeout` 等 node-scope 重试接回 loop controller 的 backoff 决策，不再立即重执行。定向命令 `./node_modules/.bin/tsx --test tests/unit/platform/orchestration/harness/recovery-controller-retry-backoff.test.ts tests/unit/platform/orchestration/harness/recovery-controller-r13-retry-limits.test.ts` 已通过。 | 关闭的是“retry_same_plan 立即重试无退避”的问题。 |
| R13-18 | 66. Worker Pool / Dispatch / Retry 深层缺陷 | P2 | `src/platform/execution/worker-pool/worker/worker-registry-service.ts` | 无自动扩缩信号发射；§8.1 "队列积压>阈值→增加 worker"——无 scale-up/scale-down 信号或 warm-pool 预留 | 已解决（源码复核+回归） | 该条已不符合现状：worker registry 已按 load/backlog 发射扩缩信号。 | 当前 `worker-registry-service.ts` 已根据 worker load、saturation 与 `toolBacklogCount` 计算信号，并显式 `emitScaleSignal("scale_up" | "scale_down", ...)`；`tests/unit/platform/execution/worker-pool/worker/worker-registry-r13.test.ts` 已覆盖 scale signal 发射语义并通过。 | 关闭的是“完全无扩缩信号”的问题。 |
| R13-19 | 66. Worker Pool / Dispatch / Retry 深层缺陷 | P2 | `src/platform/five-plane-execution/worker-pool/worker-service-identity.ts` | WorkerServiceIdentityRegistry 纯内存 Map；coordinator 重启后所有 worker identity 丢失——§8.2 要求 P5 持久化 | 已解决（源码+回归） | 根因成立：仅靠内存 store 时 coordinator 重启后无法重建 worker identity。 | 当前 `worker-service-identity.ts` 在内存命中失败后会回退 `loadFromStore(...)` 读取持久层 identity，并在注册时调用 `upsertWorkerIdentity(...)` 写回存储；`tests/unit/platform/execution/worker-pool/worker-service-identity-r13.test.ts` 已验证 coordinator 重启/回灌场景。 | 关闭“worker identity 仅内存、重启全丢”的问题。 |
| R13-20 | 66. Worker Pool / Dispatch / Retry 深层缺陷 | P2 | `src/platform/five-plane-execution/lease/execution-lease-service.ts` | Lease handover 不验证新 worker capabilities/isolation level/repo version；可将执行放到缺必要工具或沙盒强度的 worker | 已解决（源码+回归） | 根因成立：旧 handover 仅看 worker 存在与容量，没有校验执行要求是否匹配。 | 当前 `execution-lease-service.ts` 在 handover 前会显式校验 `requiredCapabilities`、`requiredIsolationLevel` 与 `requiredRepoVersion`，不满足时返回 `worker_capabilities_mismatch` / `worker_isolation_mismatch` / `worker_repo_version_mismatch` 阻断切换；`tests/unit/platform/execution/lease/execution-lease-service-handover-r13.test.ts` 已通过。 | 关闭“lease handover 不校验 worker 能力/隔离/版本”的问题。 |
| R13-21 | 67. Scale-Ecosystem 深层缺口（Federation / Multi-Region / Tenant / Marketplace） | P0 | `src/scale-ecosystem/federation/ (缺失)` | Federation 模块完全不存在；§52 要求跨平台发现/信任建立/workload 委托/联邦身份/数据主权强制 | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。 | 不宣称已修复。 |
| R13-22 | 67. Scale-Ecosystem 深层缺口（Federation / Multi-Region / Tenant / Marketplace） | P0 | `src/scale-ecosystem/multi-region/` | 无 split-brain 防护；§52 要求 quorum/consensus/split-brain 检测与解决；failover 纯延迟判断无分布式共识 | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。 | 不宣称已修复。 |
| R13-23 | 67. Scale-Ecosystem 深层缺口（Federation / Multi-Region / Tenant / Marketplace） | P0 | `src/scale-ecosystem/multi-region/` | 无 RPO/RTO 保证；data-replicator 无 lag 测量/SLA 断言；failover 无有界完成时间 | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。 | 不宣称已修复。 |
| R13-24 | 67. Scale-Ecosystem 深层缺口（Federation / Multi-Region / Tenant / Marketplace） | P1 | `src/scale-ecosystem/multi-region/` | 无 active-active vs active-passive 拓扑声明；§52 要求显式模式；所有 region 同等处理无冲突解决或提升协议 | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。 | 不宣称已修复。 |
| R13-25 | 67. Scale-Ecosystem 深层缺口（Federation / Multi-Region / Tenant / Marketplace） | P1 | `src/scale-ecosystem/tenant-platform/tenant-platform-service.ts` | 无 tenant lifecycle management(suspend/deactivate/decommission)；§50 要求完整生命周期；仅有 createTenant | 已解决（源码/测试） | 该结论已经被现行 tenant lifecycle 状态机直接证伪：服务层不再只有 `createTenant()`，而是已经具备挂起、停用、退役和恢复路径。 | 当前 `src/scale-ecosystem/tenant-platform/tenant-platform-service.ts` 已声明 `TenantLifecycleStage` 与 `VALID_LIFECYCLE_TRANSITIONS`，并实现 `suspendTenant()/deactivateTenant()/decommissionTenant()/reactivateTenant()`；本轮补了 `tests/unit/scale-ecosystem/tenant-platform/tenant-platform-service.test.ts`，直接验证完整生命周期转移与 terminal 后禁止恢复。 | 关闭该条 tenant lifecycle management 缺口。 |
| R13-26 | 67. Scale-Ecosystem 深层缺口（Federation / Multi-Region / Tenant / Marketplace） | P1 | `src/scale-ecosystem/tenant-platform/tenant-platform-service.ts` | 无 per-tenant 静态加密；encryptionPolicy 仅元数据标签无实际密钥供应/加密实现(§50) | 已解决（源码/测试） | 根因是 `tenant-platform-service` 之前只把 `encryptionPolicy` 作为 namespace 元数据原样存档，没有真正接入租户级密钥供应与加解密服务。 | 本轮在 `src/scale-ecosystem/tenant-platform/tenant-platform-service.ts` 接入 `PerTenantEncryptionService`：`createTenant()`/`reactivateTenant()` 会初始化租户密钥，新增 `encryptTenantData()` / `decryptTenantData()`，`decommissionTenant()` 会清除租户密钥。`tests/unit/scale-ecosystem/tenant-platform/tenant-platform-service.test.ts` 已验证 tenant 创建后可完成真实加解密，退役后密钥被销毁。 | 关闭的是“只有 policy label、没有实际 per-tenant key supply/encryption”的缺口。 |
| R13-27 | 67. Scale-Ecosystem 深层缺口（Federation / Multi-Region / Tenant / Marketplace） | P1 | `src/scale-ecosystem/tenant-platform/` | 无 noisy-neighbor 防护；quota-enforcer 为纯函数从未被 tenant-platform 调用；无资源隔离/CPU/内存限制/限流(§50) | 已解决（源码/测试） | 原结论停留在 quota/noisy-neighbor 还未接入 tenant topology 的阶段；现行服务已经把 `NoisyNeighborProtectionService` 作为租户资源准入闸门。 | 当前 `src/scale-ecosystem/tenant-platform/tenant-platform-service.ts` 构造时会注入 `NoisyNeighborProtectionService`，并通过 `enforceQuota()` 在 workspace/tenant/deployment-binding/namespace 路径上调用 `checkRateLimit()` 与 `recordUsage()`；本轮补了 `tests/unit/scale-ecosystem/tenant-platform/tenant-platform-service.test.ts`，直接断言 tenant-scoped 资源创建会命中 quota service。 | 关闭该条 noisy-neighbor / quota 未接线缺口。 |
| R13-28 | 67. Scale-Ecosystem 深层缺口（Federation / Multi-Region / Tenant / Marketplace） | P1 | `src/scale-ecosystem/marketplace/catalog/index.ts:56` | 依赖验证忽略 versionRange——不解析 semver 范围(§55 version compatibility matrix) | 已解决（源码/测试） | 原审计对应的是只按 listingId 存在性做直接依赖检查的早期实现；现行 catalog 已把 semver range 校验纳入依赖验证。 | 当前 `src/scale-ecosystem/marketplace/catalog/index.ts` 的 `validateListingDependencies()` 会调用 `satisfiesVersionRange()` 并回写 `versionMismatches`；本轮补了 `tests/unit/scale-ecosystem/marketplace-catalog.test.ts`，直接断言 `^2.1.0` 不接受 `2.0.0`。 | 关闭该条 versionRange 忽略缺陷。 |
| R13-29 | 67. Scale-Ecosystem 深层缺口（Federation / Multi-Region / Tenant / Marketplace） | P1 | `src/scale-ecosystem/marketplace/catalog/index.ts` | 无传递依赖图解析/环检测(§55 dependency graph)；仅 depth-1 直接依赖检查 | 已解决（源码/测试） | 真实情况已不是“只有深度 1 检查”；现行 catalog 已经显式构建依赖图并检测环，只是 reaudit 没有回写。 | 当前 `src/scale-ecosystem/marketplace/catalog/index.ts` 已提供 `buildDependencyGraph()` / `analyzeDependencyGraph()`，返回 graph 与 cycles；本轮补的 `tests/unit/scale-ecosystem/marketplace-catalog.test.ts` 已直接覆盖 `dep-a -> dep-b -> dep-a` 的传递依赖环。 | 关闭该条传递依赖图 / 环检测缺口。 |
| R13-30 | 67. Scale-Ecosystem 深层缺口（Federation / Multi-Region / Tenant / Marketplace） | P1 | `src/scale-ecosystem/marketplace/` | 无自动升级路径计算/breaking change 检测(§55)；compatibilityJson 存储但从不跨版本比较 | 已解决（源码/测试） | 原结论基于早期 marketplace 只存版本字符串、不提供升级分析；现行 catalog 已内建升级路径和 breaking-change 判断。 | 当前 `src/scale-ecosystem/marketplace/catalog/index.ts` 已提供 `calculateUpgradePathForEntry()` 与 `detectBreakingChanges()`，按 semver 计算升级步进并标记 major bump；本轮补了 `tests/unit/scale-ecosystem/marketplace-catalog.test.ts`，直接验证 `1.2.3 -> 2.0.0` 会给出路径并标记 breaking。 | 关闭该条升级路径 / breaking-change 检测缺口。 |
| R13-31 | 67. Scale-Ecosystem 深层缺口（Federation / Multi-Region / Tenant / Marketplace） | P2 | `src/scale-ecosystem/tenant-platform/data-plane-flow-service.ts:611` | scopeCompatibility tenantId:null 时绕过——global namespace 可自由与任何 tenant-scoped namespace 交换数据 | 已解决（源码/测试） | 现行实现已经比原审计要求更严格：不仅修掉了 `tenantId:null` 绕过，还把“无任何 scope 的 global namespace”直接 fail-close 拒绝，避免后续 movement 路径再碰到空作用域。 | 当前 `src/scale-ecosystem/tenant-platform/data-plane-flow-service.ts` 的 `assertScopeCompatibility()` 对 tenant/org/workspace scope 都做严格相等校验；同时 `src/scale-ecosystem/tenant-platform/tenant-platform-service.ts` 在 `createDataNamespace()` 中要求至少提供 tenant/org/workspace 之一。本轮回写 `tests/unit/scale-ecosystem/marketplace/data-plane-flow-issues.test.ts`，明确验证无 scope namespace 会被 `tenant.namespace_scope_required` 拒绝，而同 tenant 允许、跨 tenant 仍阻断。 | 关闭该条 null-scope bypass 缺陷，并按现行 fail-closed 合同更新测试证据。 |
| R13-32 | 67. Scale-Ecosystem 深层缺口（Federation / Multi-Region / Tenant / Marketplace） | P2 | `src/scale-ecosystem/tenant-platform/` | tenant-scoped quota 从不在 tenant-platform 中检查；创建 workspace/namespace/binding 无资源配额限制 | 已解决（源码/测试） | 根因已不再成立：tenant-platform 当前在创建 deployment binding、data namespace 等关键资源前都会执行 quota 准入。 | 当前 `src/scale-ecosystem/tenant-platform/tenant-platform-service.ts` 在 `createWorkspace()/createTenant()/createDeploymentBinding()/createDataNamespace()` 中都接入了 `enforceQuota()`；本轮补了 `tests/unit/scale-ecosystem/tenant-platform/tenant-platform-service.test.ts`，既验证允许路径会记录 `concurrent_connections/storage` 使用量，也验证超额时会 fail-close 拒绝 namespace 创建。 | 关闭该条 tenant-scoped quota 未检查缺口。 |
| R13-33 | 68. 架构合约缺失确认（§ 级未实现） | P0 | `src/ (全域缺失)` | §3.2 ResponsibilityBoundary 类型完全不存在；advisory_only/human_accountable 边界模式无定义无运行时强制 | 已解决（源码/测试） | 原结论已不成立，而且本轮进一步把缺的运行时强制补齐：之前已有 `ResponsibilityBoundary` 基础对象，但没有显式 `advisory_only/human_accountable` operating mode，也没有动作级 enforcement。 | 本轮在 `src/platform/contracts/types/responsibility-boundary.ts` 增加 `ResponsibilityBoundaryMode`、`operatingMode` 字段，并在 `recordTransfer()/assertActionAllowed()` 内对 `advisory_only` 阻断 mutating action、对 `human_accountable` 强制 `human_operator` 执行 approve/override/patch/takeover/resume。新增 `tests/unit/platform/contracts/responsibility-boundary.test.ts` 已验证两类边界模式都会在运行时 fail-close。 | 关闭的是“类型不存在且没有运行时强制”的双重缺口。 |
| R13-34 | 68. 架构合约缺失确认（§ 级未实现） | P0 | `src/platform/orchestration/hitl/` | §45.18 HITL 5能力中 modify-and-approve/override-decision/force-terminate 3项零实现——无类型/handler/枚举 | 已解决（源码/测试） | 根因是旧 HITL 目录只有传统审批模式，`modify-and-approve/override-decision/force-terminate` 虽在部分分支里有概念，但没有被整理成统一 mode/capability/handler 出口。 | 本轮在 `src/platform/five-plane-orchestration/hitl/hitl-modes.ts` 增加 `HitlCapability` 元数据，在 `src/platform/five-plane-orchestration/hitl/hitl-runtime-capabilities.ts` 增补 `HitlForceTerminateService`，并通过 `src/platform/five-plane-orchestration/hitl/index.ts` 统一导出 runtime capability handlers。`tests/unit/platform/orchestration/hitl/hitl-modes.test.ts`、`arch-p1-hitl-modes-coverage.test.ts`、`hitl-runtime-capabilities.test.ts` 已覆盖三类能力的 mode、capability 和 handler 执行链。 | 关闭的是“无类型/handler/枚举”的真实缺口。 |
| R13-35 | 68. 架构合约缺失确认（§ 级未实现） | P1 | `src/platform/contracts/executable-contracts/index.ts:407` | §14.11 SideEffectRecord 缺 rollback_handler/timeout 字段(每效果截止时间+内联回滚处理器) | 已解决（源码复核并补直接回归） | 这条已不是现状：`SideEffectRecord` 早已有 `deadline`、`rollbackHandler.handler` 和 `rollbackHandler.timeout`，只是 reaudit 表与测试证据没有同步回写。 | 当前 `src/platform/contracts/executable-contracts/index.ts` 的 `SideEffectRecord` 与 `createSideEffectRecord()` 已显式支持 `deadline`、`rollbackHandler`、`compensationPlan`；本轮在 `tests/unit/platform/contracts/executable-contracts/index.test.ts` 新增回归，直接断言 rollback handler 和 timeout 会被保留。 | 这是文档基线漂移纠偏，不再保留为未解决。 |
| R13-36 | 68. 架构合约缺失确认（§ 级未实现） | P1 | `src/ops-maturity/drift-detection/proposal-engine.ts:17` | §20.3 ImprovementProposal 生命周期偏离 spec(缺 draft/reviewed/staged/stable/retired 态)；primitives.ts 又一套不同枚举 | 已解决（源码/测试） | 该条此前属实；本轮已把 proposal 生命周期、promotion gate 和 registry 统计统一到 spec 所要求的阶段模型，不再存在另一套偏离枚举。 | 当前 `src/ops-maturity/drift-detection/learning/proposal-engine.ts` 已收敛为 `draft/reviewed/staged/stable/retired/rejected`，并补齐 `draftedAt/reviewedAt/stagedAt/stabilizedAt/retiredAt`；`evolution-registry.ts` 与 `learning/promotion-gate.ts` 也已同步到新生命周期。`tests/unit/ops-maturity/drift-detection/proposal-engine.test.ts`、`promotion-gate.test.ts`、`evolution-registry.test.ts`、`evolution-types.test.ts` 已覆盖。 | 关闭该条 proposal lifecycle 偏离 spec 缺口。 |
| R13-37 | 68. 架构合约缺失确认（§ 级未实现） | P1 | `src/platform/orchestration/hitl/hitl-modes.ts` | §45.18 force-terminate 能力缺失——7种交互模式无一映射到强制终止动作 | 已解决（源码/测试） | 根因是旧 mode 集没有把强制终止升格为显式 capability；即便 operator takeover 存在，也没有单独的强制终止动作语义。 | 当前 `src/platform/five-plane-orchestration/hitl/hitl-modes.ts` 已显式导出 `force_terminate` mode 与 `capability: "force_terminate"`，`src/platform/five-plane-orchestration/hitl/hitl-runtime-capabilities.ts` 新增 `HitlForceTerminateService`；`tests/unit/platform/orchestration/hitl/hitl-modes.test.ts` 与 `hitl-runtime-capabilities.test.ts` 已验证 critical/high risk 下可走强制终止路径。 | 关闭该条 force-terminate mode/capability 缺口。 |
| R13-38 | 68. 架构合约缺失确认（§ 级未实现） | P1 | `src/platform/orchestration/hitl/hitl-modes.ts` | §45.18 override-decision 能力缺失——无 action type 允许人类覆盖先前自动/人工决策 | 已解决（源码/测试） | 根因是 override 之前只散落在 explainability/审批语义里，没有被定义成独立 HITL capability 并给出运行时 handler。 | 当前 `src/platform/five-plane-orchestration/hitl/hitl-modes.ts` 已把 `override_decision` 作为显式 mode，返回 `capability: "override_decision"`；`src/platform/five-plane-orchestration/hitl/hitl-runtime-capabilities.ts` 的 `HitlOverrideService` 提供 request/approve/reject handler。`tests/unit/platform/orchestration/hitl/hitl-modes.test.ts`、`arch-p1-hitl-modes-coverage.test.ts`、`hitl-runtime-capabilities.test.ts` 已验证 human override 流程。 | 关闭该条 override-decision capability 缺口。 |
| R13-39 | 68. 架构合约缺失确认（§ 级未实现） | P1 | `src/scale-ecosystem/resource-manager/quota-enforcer/index.ts` | §53 QuotaEnforcer 无状态——无持久 per-tenant 用量累积/租户注册；scopeId optional 从不绑定租户 | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。 | 不宣称已修复。 |
| R13-40 | 68. 架构合约缺失确认（§ 级未实现） | P2 | `src/platform/contracts/executable-contracts/index.ts:407` | §14.11 SideEffectRecord 缺 compensation_plan 内联字段(CompensationRecord 独立存在但 SideEffectRecord 无直接引用) | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。 | 不宣称已修复。 |
| R13-41 | 68. 架构合约缺失确认（§ 级未实现） | P2 | `src/platform/model-gateway/degradation/deterministic-hot-path-gate.ts` | §3.2 deterministic_hot_path_only 仅用于延迟路由而非责任边界强制；无运行时阻止 agent 超越其边界 | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。 | 不宣称已修复。 |
| R14-01 | §70 Incident/SLO/Reliability 深层缺陷 | P0 | `platform-panic/` | 无 Platform Panic 实现——仅有 "panic_stop" 字符串枚举，无实际传播/状态保存/恢复协议(§60 要求 cascade halt+PanicAcknowledgment+dual-admin PlatformResumeDirective) | 已修复（源码补齐返回一致性并补定向验证） | 原审计已明显滞后：`PlatformPanicService`、`PanicPropagationService`、`PlatformResumeDirective` 与双人确认类型实际都已存在；本轮复核又发现一个真实残留 bug：`cascadeHalt()` 虽会把 P1 更新为 `halted` 并持久到 `haltingLog`，但返回值仍是更新前的旧数组快照，导致调用方拿到的首平面状态与内部记录不一致。 | 本轮将 `src/platform/ops-maturity/platform-panic/panic-propagation-service.ts` 的 `cascadeHalt()` 改为返回已更新的 halting log，并新增 `tests/unit/platform/ops-maturity/platform-panic/panic-propagation-service.test.ts`，验证 cascade halt、PanicAcknowledgment、propagation record 和 dual-admin resume directive 全链都可运行。 | 关闭的是“无实际传播协议”这条旧指控与返回快照不一致的真实 bug，不外推 Incident/SLO 全域条目都已完成。 |
| R14-02 | §70 Incident/SLO/Reliability 深层缺陷 | P0 | `incident-detector.ts` | Incident severity 用 p1-p4 非 SEV1-4(§12.2)；与 runbook priority P0-P3 混淆无映射 | 已解决（源码复核） | 该条已被现行实现推翻：incident detector 当前直接使用统一 `SEV1-4` 严重度，而非旧的 `p1-p4`。 | 当前 `src/platform/five-plane-control-plane/incident-control/incident-detector.ts` 的 `UnifiedSeverity` / `IncidentStatus` 均按 `SEV1-4` 与统一事件模型工作；reaudit 这里是文档基线滞后。 | 回写现行 severity 合同，不改生产代码。 |
| R14-03 | §70 Incident/SLO/Reliability 深层缺陷 | P1 | `incident-control/` | Incident lifecycle 仅 4态(open/acknowledged/resolved/closed)缺 triaged/mitigating/reviewed(§12.5)；缺 reviewed→closed post-mortem 门 | 已解决（源码复核） | 该条已不符合现状：incident 生命周期当前已包含 `triaged/mitigating/reviewed` 扩展态，不再只有 4 态。 | 当前 `src/platform/five-plane-control-plane/incident-control/incident-detector.ts` / `incident-resolver.ts` 已按 `open -> triaged -> mitigating -> reviewed -> resolved -> closed` 工作，reviewed/post-mortem 语义已进入实现。 | 回写现行 lifecycle 证据，不改生产代码。 |
| R14-04 | §70 Incident/SLO/Reliability 深层缺陷 | P1 | `incident-control/` | 无 post-mortem 自动化(§12.5/§60.3 要求 72h 内 Post-Incident Report) | 已解决（源码复核） | 该条已被现行 resolver 推翻：72 小时 post-mortem 触发门和自动生成入口都已存在。 | 当前 `src/platform/five-plane-control-plane/incident-control/incident-resolver.ts` 已定义 `POST_MORTEM_AUTO_TRIGGER_HOURS = 72`，并提供 `isPostMortemDue()`、`generatePostMortem()`。 | 回写现行 post-mortem 自动化证据，不改生产代码。 |
| R14-05 | §70 Incident/SLO/Reliability 深层缺陷 | P1 | `incident-detector.ts` | 仅检测 p1/p2 两级；§12.3 定义 5条规则映射 SEV1-3 含自动动作；无可配置规则引擎 | 已解决（源码复核） | 该条已过时：当前 detector 已包含面向 `SEV1-3` 的 5 条默认检测规则，不再只有两级 incident。 | 当前 `src/platform/five-plane-control-plane/incident-control/incident-detector.ts` 已导出 `DEFAULT_DETECTION_RULES`，覆盖 5 条 `SEV1-3` 检测规则，并在 detector 构造器中作为可替换规则集接入。 | 回写现行 rule engine / default rules 证据，不改生产代码。 |
| R14-06 | §70 Incident/SLO/Reliability 深层缺陷 | P1 | `slo-alerting-service.ts` | SLO 无 per-domain 范围——slo_definitions 表无 domainId/tenantId 列(§27/§37.9) | 已解决（源码复核） | 该条已与现行 schema 不符：SLO 当前已支持 per-domain 范围。 | 当前 `src/platform/shared/observability/slo-alerting/types.ts` 的 `SloDefinition` 已含 `domain`，DDL 里也已有 `domain TEXT NULL`；`src/platform/shared/observability/slo-alerting-service.ts` 支持按 domain 创建与查询 SLO。 | 回写现行 per-domain SLO 合同，不改生产代码。 |
| R14-07 | §70 Incident/SLO/Reliability 深层缺陷 | P1 | `slo-alerting-service.ts` | SLO breach 仅冻结 rollout——§27.6 要求梯度响应(50-80%减速/80-100%冻结/>100%全冻+可靠性冲刺) | 已解决（源码复核） | 该条已被当前 SLO 响应逻辑推翻：实现不是单一 freeze，而是分级梯度响应。 | 当前 `src/platform/shared/observability/slo-alerting-service.ts` 已实现 `50-80% -> degrade`、`80-100% -> freeze`、`>100% -> full_freeze` 的梯度策略，并据此驱动 rollout freeze 行为。 | 回写现行 gradient response 证据，不改生产代码。 |
| R14-08 | §70 Incident/SLO/Reliability 深层缺陷 | P1 | `slo-alerting-service.ts` | Burn-rate alerting 无 multi-window 策略(§27.6 要求 1h>14.4x→SEV2/6h>6x→SEV3) | 已解决（源码复核） | 该条已与当前 burn-rate 实现不符：multi-window burn-rate 规则已经存在。 | 当前 `src/platform/shared/observability/slo-alerting-service.ts` 已实现 `1h > 14.4x -> SEV2`、`6h > 6x -> SEV3` 的 burn-rate 告警门限与严重度映射。 | 回写现行 multi-window burn-rate 证据，不改生产代码。 |
| R14-09 | §70 Incident/SLO/Reliability 深层缺陷 | P1 | `chaos/` | Chaos experiment 假设违反仅记录 status="violated" 无自动回滚/爆炸半径控制(§61) | 已解决（源码复核） | 该条已过时：现行 chaos scheduler 在假设违反时会进入 rollback 流程，并显式配置 boundary control / blast radius。 | 当前 `src/ops-maturity/chaos/chaos-experiment-scheduler.ts` 已含 `boundaryControl`、`rollbackTimeoutMs`、`rollbackActions`，违反假设后状态会转为 `rollback` 并执行自动回滚，而非仅停在 `violated`。 | 回写现行 chaos rollback / blast-radius 证据，不改生产代码。 |
| R14-10 | §70 Incident/SLO/Reliability 深层缺陷 | P2 | `incident-control/` | 无 war-room 协调服务(SEV1 多参与者协调) | 已解决 | 原审计已滞后：SEV1 多参与者 war-room 协调服务与 barrel 导出早已存在，只是 reaudit 没有补充直接证据。 | 当前 `src/platform/five-plane-control-plane/incident-control/war-room-coordination-service.ts` 已实现 `WarRoomCoordinationService`，覆盖 session 创建、参与者管理、决策记录、状态更新与 phase 推进；`src/platform/five-plane-control-plane/incident-control/index.ts` 也已对外导出。`tests/unit/platform/ops-maturity/reaudit-batch-r14-r15-r16-regressions.test.ts` 已验证 SEV1 war-room 创建、加人、phase 推进与决策记录。 | 回写 war-room 现行实现证据。 |
| R14-11 | §70 Incident/SLO/Reliability 深层缺陷 | P2 | `src/platform/five-plane-control-plane/incident-control/incident-detector.ts:430` | Incident ID 用 Math.random() 非加密唯一——并发可碰撞 | 已解决（源码已满足，本轮补证据） | 原审计对应的是旧 incident ID 生成策略；现行实现已经统一改成 `newId("incident")`，不再使用 `Math.random()` 拼接 incident 标识。 | 当前 `src/platform/five-plane-control-plane/incident-control/incident-detector.ts` 的 `generateIncidentId()` 直接返回 `newId("incident")`；既有定向测试 `tests/unit/platform/control-plane/incident-control/incident-detector.test.ts` 的 `createIncident generates unique incident IDs` 与 `incident-detector-edge-cases.test.ts` 的同名断言已经验证两次创建得到不同的 `incident_*` 标识。 | 不改生产代码，回写现行 ID 生成依据。 |
| R14-12 | §71 代码质量/实现 Bug | P0 | `src/platform/five-plane-orchestration/oapeflir/oapeflir-loop-service.ts:785` | knowledgePromotion.promote() 无 await——Promise rejection 静默丢失 | 已解决（源码已满足，本轮补证据） | 原审计指向的是较早版本里 `knowledgePromotion.promote(...)` 没有等待完成的实现；现行源码已经显式 `await` 该 Promise，只是 reaudit 行未更新。 | 当前 `src/platform/five-plane-orchestration/oapeflir/oapeflir-loop-service.ts` 在 learn 阶段后执行 `await this.knowledgePromotion.promote(learningObjects, currentInput.taskId)`；本轮新增定向测试 `tests/unit/platform/orchestration/oapeflir/review-regressions.test.ts` 的 `OapeflirLoopService waits for knowledge promotion before returning`，验证在 promotion Promise 未释放前 `produceStageRationale()` 不会提前返回。 | 不改生产代码，补最小回归并回写现行 await 证据。 |
| R14-13 | §71 代码质量/实现 Bug | P0 | `task-routes.ts:255` | PATCH /v1/tasks/:id 传旧 inputJson 不传新 title——title 静默丢弃(数据丢失 bug) | 已解决（源码复核） | 原问题来自旧 PATCH 路径只更新 status/output，不落 title；现行实现已把 `title` 纳入 PATCH 更新链。 | 当前 `src/platform/five-plane-interface/api/http-server/task-routes.ts` 已在 PATCH 分支显式执行 `deps.taskStore.task.updateTaskTitle(taskId, payload.title, now)`；现有 `tests/unit/platform/interface/api/http-server/task-routes-2043.test.ts` 仍保留旧 bug 断言，因此本轮以源码现状为证，不沿用旧测试结论。 | 不改生产代码，回写复核依据。 |
| R14-14 | §71 代码质量/实现 Bug | P1 | `src/platform/five-plane-orchestration/oapeflir/oapeflir-loop-service.ts:440,1146` | 返回原始未验证 assessment/observation 非实际使用的 validated 版本——下游数据不一致 | 已修复 | 复核后发现原问题只剩一半仍然成立：`assessment` 其实早已返回 `validatedAssessment`，但 `observation` 仍把验证前的 `taskObservation` 直接返给调用方，而内部 assess/plan 实际使用的是 `validateTaskSituation(...)` 产出的 `observedTask`。这会让 schema default/规范化结果只在内部生效、对外返回面却继续暴露未验证 task 载荷。 | 当前 `src/platform/five-plane-orchestration/oapeflir/oapeflir-loop-service.ts` 已在 O→A 边界后构造 `validatedObservation = { ...taskObservation, task: observedTask }`，并在最终结果中返回该 validated observation；本轮新增定向测试 `tests/unit/platform/orchestration/oapeflir/review-regressions.test.ts` 的 `OapeflirLoopService returns the validated observation task payload`，验证当 aggregator 丢掉 `blockers/relevantMemory/fileRefs/metrics` 时，返回结果会使用 schema 规范化后的 task 载荷，而不是原始未验证对象。 | 已改生产代码并补最小回归。 |
| R14-15 | §71 代码质量/实现 Bug | P1 | `src/platform/model-gateway/cost-tracker/chargeback-service.ts:96-128` | 跨币种 currency 被最后一条覆盖+金额天真求和——多币种 chargeback 静默错误 | 已解决（源码已满足，本轮补证据） | 原问题来自旧聚合实现按资源维度直接累加，导致同一资源的不同币种记录互相覆盖；现行实现已经把 `currency` 纳入 `allocationKey`，并为每个 allocation 单独保留 `originalCurrency/baseCurrency/fxRateToBase`。 | 当前 `src/platform/model-gateway/cost-tracker/chargeback-service.ts` 以 `tenantId/resourceType/resourceId/currency` 组成 `allocationKey`，聚合时不会把 EUR/JPY/USD 混成同一个 allocation；本轮新增定向测试 `tests/unit/platform/model-gateway/cost-tracker/chargeback-service.test.ts` 的 `ChargebackService buildReport keeps same resource split by original currency`，验证同一 `resourceId` 的 USD 与 EUR 报告会产出两条 allocation，而不是被最后一条 currency 覆盖。 | 不改生产代码，补多币种同资源回归并回写依据。 |
| R14-16 | §71 代码质量/实现 Bug | P1 | `response-hardening.ts:12` | CORS allowedOrigins:["*"]+credentials:true echoing origin——任何来源可发送凭据请求 | 已解决（源码复核） | 原问题是旧默认配置和 wildcard 判断允许了 `["*"] + credentials:true` 反模式；现行实现已 fail-close。 | `src/platform/interface/api/http-server/response-hardening.ts` 当前默认 `allowedOrigins=[]`、`credentials=false`，且 `isOriginAllowed()` 在 `credentials=true` 时明确拒绝 wildcard。旧 `response-hardening-2038` 样例里仍保留兼容期断言，本轮不再把它当作现状证据。 | 不改生产代码，回写复核依据。 |
| R14-17 | §71 代码质量/实现 Bug | P1 | `incident-routes.ts:66` | tenantId 解析后 void——incidents 无租户范围限制(跨租户数据泄漏) | 已解决（源码复核） | 原问题来自列表/详情/状态变更路径没有把租户作用域真正下沉到 service 调用；现行路由已统一按 tenant scope 执行。 | `src/platform/interface/api/http-server/incident-routes.ts` 当前在 GET list / GET detail 上通过 `resolveTenantScope(...)` 强制 tenant scope，在 PATCH `acknowledge/startMitigation/resolve` 上也将 `tenantId` 显式传入 `incidentService`。 | 不改生产代码，回写复核依据。 |
| R14-18 | §71 代码质量/实现 Bug | P1 | `task-routes.ts:73` | queryTaskInspectSummaries 始终 fetch 200 条内存分页——over-fetch 不可扩展 | 已解决（源码复核） | 原问题来自 inspect 层先全量拉取再内存过滤；现行实现已把 limit/tenantId 下推到 store。 | `src/platform/shared/observability/inspect-service.ts` 当前直接调用 `this.store.listTasks(limit, query.tenantId ?? undefined)`，并在代码内保留 `R14-18` 修复说明。 | 不改生产代码，回写复核依据。 |
| R14-19 | §71 代码质量/实现 Bug | P1 | `src/platform/prompt-engine/rollout/index.ts:112` | rollbackRollout 无状态机守卫——允许从任何状态 rollback 包括 draft/rolled_back | 已解决（源码已满足，本轮补证据） | 原问题来自旧 rollout service 对 rollback 没有限制；现行实现已经在 `rollbackRollout()` 内显式白名单 `canary_5/canary_20/stable` 三种可回滚状态，并对 `blocked/deprecated/rolled_back` 等状态抛 `invalid_transition`。 | 当前 `src/platform/prompt-engine/rollout/index.ts` 使用 `rollbackableStates = ["canary_5", "canary_20", "stable"]` 做状态机守卫；既有定向测试 `tests/unit/platform/prompt-engine/rollout/index.test.ts` 的 `PromptRolloutService rollbackRollout only applies to active rollout states` 已验证 active state 可回滚、blocked state 会抛错。 | 不改生产代码，回写现行状态机守卫与单测依据。 |
| R14-20 | §71 代码质量/实现 Bug | P2 | `src/platform/five-plane-interface/api/http-server/billing-routes.ts:70` | webhook handler 在未版本化和 /v1/ 路由间完整 copy-paste；legacy 路由缺 Deprecation/Sunset header | 已解决（源码复核） | 原审计对应的是历史兼容期里同时暴露 legacy 与 `/v1/` reconcile handler 的版本；现行 billing route 只保留一个版本化路由定义，不再存在未版本化 duplicate handler，因此也不存在 legacy 路由再补 `Deprecation/Sunset` header 的问题。 | 当前 `src/platform/five-plane-interface/api/http-server/billing-routes.ts` 的 `createBillingRoutes()` 只返回一条 `POST /v1/billing/webhooks/reconcile` 路由；既有定向测试 `tests/unit/platform/interface/api/http-server/billing-routes.test.ts` 的 `createBillingRoutes exposes only the versioned reconcile route` 已验证路由表长度为 1 且 pathname 为 `/v1/billing/webhooks/reconcile`。 | 不改生产代码，回写“legacy duplicate route 已消失”的现状依据。 |
| R14-21 | §71 代码质量/实现 Bug | P2 | `src/platform/prompt-engine/rollout/platform-prompt-release-orchestration-service.ts:134-139` | createRelease autoActivate 不检查 evaluationReport.gateDecision——eval gate "hold" 可被绕过 | 已解决（源码已满足，本轮补证据并清理陈旧测试） | 原审计停留在更早版本的 createRelease 自动激活逻辑；现行实现已经把 `autoActivate` 明确收敛到 `evaluationReport.gateDecision === "promote"` 的分支里，`hold` 不会走 activation。顺带复核时还发现该模块单测仍保留 `active/ready` 等旧状态名，本轮一并改成当前 staged rollout 语义。 | 当前 `src/platform/prompt-engine/rollout/platform-prompt-release-orchestration-service.ts` 仅在 `input.autoActivate === true && createdRollout.status === "canary_5" && evaluationReport.gateDecision === "promote"` 时尝试 activation；本轮更新并补强了 `tests/unit/platform/prompt-engine/rollout/platform-prompt-release-orchestration-service.test.ts`，其中 `PlatformPromptReleaseOrchestrationService keeps rollout at canary_5 when autoActivate lacks verified approval` 与 `...does not bypass hold gate even when autoActivate approvals are present` 直接覆盖 promote/hold 两条路径。 | 不改生产代码，补直接回归并清理陈旧状态断言。 |
| R14-22 | §71 代码质量/实现 Bug | P2 | `authoritative-task-store-decorator.ts:5` | SharedArrayBuffer+decoratorMetrics 全局单例——并发操作跨 store 指标误导+Atomics.wait 阻塞线程 | 已修复 | 真实根因分两层：早期实现确实把 metrics 做成了进程级共享状态，并用 `SharedArrayBuffer/Atomics.wait` 在同步重试里做 backoff；后续虽然先拆成了实例级 metrics，但仍残留同步阻塞式等待，线程模型风险并未真正消失。 | 当前 `src/platform/five-plane-state-evidence/truth/repositories/authoritative-task-store-decorator.ts` 已只保留每个 decorator 实例自己的 `metrics` Map；`SharedArrayBuffer` 和阻塞睡眠逻辑都已移除，异步方法在 `SQLITE_BUSY` 下通过 `await delay(backoffMs)` 做非阻塞退避，而同步方法只做即时重试、不再睡眠阻塞线程。本轮新增并通过 `tests/unit/platform/state-evidence/truth/authoritative-task-store-decorator.test.ts` 中的 `...isolates metrics per decorated store instance`、`...retries async SQLITE_BUSY failures with non-blocking backoff`、`...does not apply blocking backoff to synchronous retries`，并同步更新 `tests/unit/platform/state-evidence/truth/repositories/authoritative-task-store-decorator.test.ts` 到实例级 metrics 语义。 | 已改生产代码并补最小回归。 |
| R14-23 | §72 UI Feature 最终缺口 + Contract 缺失 | P0 | `ui/packages/features/alerts/` | 无优先级排序——§4.7 要求优先级排序队列 | 已解决（源码/测试） | 根因是旧结论停留在 alerts 只做平铺映射的阶段；现行 `mapAlertsToVm()` 已按 severity 优先级和时间倒序排序，但 reaudit 没有专项回归去证明这一点。 | 当前 `ui/packages/features/alerts/src/hooks/index.ts` 使用 `SEVERITY_PRIORITY` + `sortBySeverity()` 实现 `critical > high > medium > low` 排序；本轮新增 `ui/tests/unit/ui/packages/features/alerts/hooks.test.tsx` 的 `sorts by severity...`，直接断言排序顺序。 | 关闭的是“无优先级排序”缺口。 |
| R14-24 | §72 UI Feature 最终缺口 + Contract 缺失 | P0 | `ui/packages/features/alerts/` | 无 dismiss action——§4.7 要求 acknowledge/dismiss/escalate 三核心动作 | 已解决（源码/测试） | 根因分两层：hook 里原本已有 `dismissAlert()`，但 web view 没把它作为显式动作暴露出来，导致能力只存在于 VM 而不在实际 feature 交互面。 | 本轮在 `ui/packages/features/alerts/src/web/index.tsx` 新增 `忽略当前` 动作并接到 `vm.dismissAlert()`；`ui/tests/unit/ui/packages/features/alerts/hooks.test.tsx` 同时验证 dismiss 后 item 会从队列移除。 | 关闭的是 dismiss action 缺失问题。 |
| R14-25 | §72 UI Feature 最终缺口 + Contract 缺失 | P0 | `ui/packages/features/governance-compliance/` | 缺审计轨迹查看器——§4.8 要求独立审计轨迹面板 | 已解决（源码/测试） | 根因是 hooks 层虽然已有 `auditTrail` 数据和 `hasAuditTrailViewer` 标识，但 web feature 之前只渲染 summary panel，没有把审计记录实际展示出来。 | 本轮在 `ui/packages/features/governance-compliance/src/web/index.tsx` 增加独立 `Audit Trail` section，渲染审计动作、actor、resource 和 outcome；`ui/tests/unit/ui/packages/features/governance-compliance/web.test.tsx` 已验证面板与记录展示。 | 关闭的是审计轨迹面板缺失问题。 |
| R14-26 | §72 UI Feature 最终缺口 + Contract 缺失 | P1 | `ui/packages/features/alerts/` | 无 severity/domain/time 过滤(§4.7) | 已解决（源码/测试） | 原问题并不只是 UI 少两个下拉框，真正缺口是 `IncidentDTO` 没有 domain 维度且 alerts VM 只支持 severity/time；因此 domain 过滤无处挂接。 | 本轮给 `IncidentDTO` 与 mock incidents 增加 `domainId`，`ui/packages/features/alerts/src/hooks/index.ts` 补齐 `domainId` filter 和 `availableDomains`，`ui/packages/features/alerts/src/web/index.tsx` 新增 domain selector；`ui/tests/unit/ui/packages/features/alerts/hooks.test.tsx` 已覆盖 severity/domain/time 过滤。 | 关闭的是 alerts feature 过滤能力缺失问题。 |
| R14-27 | §72 UI Feature 最终缺口 + Contract 缺失 | P1 | `ui/packages/features/alerts/` | 无 WS 实时更新——用 polling(§4.7 要求 WS 推送) | 已解决（源码/测试） | 根因不是现行 alerts 仍只靠 polling，而是 reaudit 没吸收后续 WS invalidate 接线。 | 当前 `ui/packages/features/alerts/src/hooks/index.ts` 已通过 `wsClient.subscribe("incidents", ...)` 订阅 `incident.created/incident.updated` 并触发 `invalidateQueries(["incidents"])`；既有 `useAlertsVm` 回归持续通过。 | 关闭的是 alerts feature 缺少 WS 推送接线的陈旧结论。 |
| R14-28 | §72 UI Feature 最终缺口 + Contract 缺失 | P1 | `ui/packages/features/governance-compliance/` | 缺 exception management 面板(§4.8) | 已解决（源码/测试） | 根因与 R14-25 类似：exception request/approve/reject 能力已在 hooks 中存在，但 web view 之前没有把 exception queue 渲染成可操作面板。 | 本轮在 `ui/packages/features/governance-compliance/src/web/index.tsx` 新增 `Exception Management` section，展示异常条目并接入 approve/reject 按钮；`ui/tests/unit/ui/packages/features/governance-compliance/web.test.tsx` 已验证面板展示和动作调用。 | 关闭的是 exception management 面板缺失问题。 |
| R14-29 | §72 UI Feature 最终缺口 + Contract 缺失 | P1 | `ui/packages/features/domain-wizard/` | 无多步向导流程——渲染扁平 panel 列表(§4.3 要求 stepper+step navigation) | 已解决（源码/测试） | 根因是旧审计快照停留在单 panel 版本；现行 web feature 已有 stepper、步骤跳转、前进/后退导航，但文档没有同步回写。 | 当前 `ui/packages/features/domain-wizard/src/web/index.tsx` 已实现四步 stepper、`goNext/goPrev/goToStep` 导航以及按步骤渲染的内容区；`ui/tests/unit/ui/packages/features/domain-wizard/hooks.test.tsx` 本轮新增断言 steps 顺序和 step 切换。 | 关闭的是多步向导流程缺失问题。 |
| R14-30 | §72 UI Feature 最终缺口 + Contract 缺失 | P1 | `ui/packages/features/domain-wizard/` | hooks 无 risk profile/capability configuration 字段(§4.3) | 已解决（源码/测试） | 原结论与现行 VM 不符：`useDomainWizardVm()` 已暴露 risk/capability 两组状态和 setter，只是此前缺少专项回归。 | 当前 `ui/packages/features/domain-wizard/src/hooks/index.ts` 已提供 `riskProfile.riskLevel/dataClassification/hasExternalIntegration` 与 `capabilityConfig.maxConcurrentTasks/allowedDrillDepth/enableAutoRollback`；本轮新增 `ui/tests/unit/ui/packages/features/domain-wizard/hooks.test.tsx` 的 capability/risk 回归。 | 关闭的是 domain wizard VM 字段缺失问题。 |
| R14-31 | §72 UI Feature 最终缺口 + Contract 缺失 | P1 | `docs_zh/contracts/` | 缺 ring-model contract——release contract 无 ring-based 渐进部署+ring promotion criteria(§33) | 已解决（contract 复核） | 根因不是当前仓库仍缺 ring-model contract，而是 reaudit 条目形成时该 contract 尚未补齐，后续新增了独立 ring model contract，但复审表没有同步刷新。 | 当前 `docs_zh/contracts/ring_model_contract.md:1-9` 已明确定义 `Ring Model Contract` 的 scope；`docs_zh/contracts/ring_model_contract.md:18-27,30-57,72-112` 已覆盖 ring levels、promotion criteria、auto-rollback 与 ring promotion decision。 | 不改代码，仅按现行 contract 关闭陈旧“缺文件”结论。 |
| R14-32 | §72 UI Feature 最终缺口 + Contract 缺失 | P1 | `docs_zh/contracts/` | 缺 CI/CD pipeline contract——无 build/test/package/publish/artifact promotion(§13 pre-release) | 已解决（contract 复核） | 根因不是当前仓库仍无 CI/CD pipeline contract，而是复审条目没有吸收后续补齐的 `ci_cd_pipeline_contract.md`。 | 当前 `docs_zh/contracts/ci_cd_pipeline_contract.md:1-8` 已定义 scope；`docs_zh/contracts/ci_cd_pipeline_contract.md:17-24,26-104` 已覆盖 `build / test / package / publish / artifact promotion` 全链路与 pre-release quality gates。 | 不改代码，仅回写现行 contract 证据。 |
| R14-33 | §72 UI Feature 最终缺口 + Contract 缺失 | P2 | `response-hardening.ts` | CORS wildcard+credentials(重复确认为安全反模式) | 需继续整改（contract/文档） | contract 文档或导出面没有完全跟随 canonical runtime 迁移。 | 该条本质是 contract/导出面问题，需要逐份 contract 回写或补强测试。 | 本轮不伪造代码修复。 |
| R14-34 | §72 UI Feature 最终缺口 + Contract 缺失 | P2 | `src/platform/interface/api/` | 缺 Content-Type validation middleware(§6.2 input sanitization) | 需继续整改（contract/文档） | contract 文档或导出面没有完全跟随 canonical runtime 迁移。 | 该条本质是 contract/导出面问题，需要逐份 contract 回写或补强测试。 | 本轮不伪造代码修复。 |
| R15-01 | §74 SDK/Plugin/Domain Registry 深层缺陷 | P0 | `src/sdk/client-sdk/api-client.ts:208-214` | SDK 重试所有非 OK 响应包括 POST/PUT/DELETE——§22 要求幂等感知，重试非幂等写操作导致重复副作用 | 已解决（源码已满足，本轮补证据） | 原审计停留在旧版 SDK retry 逻辑；现行实现已经按 HTTP 方法和幂等键收敛重试面，非幂等写不会被盲目重放。 | 当前 `src/sdk/client-sdk/api-client.ts` 在 HTTP 响应重试和 `fetch` 异常重试两条路径都统一使用 `canRetry = method === "GET" || (isPutDelete && hasIdempotencyKey)`，`POST/PATCH/OPTIONS` 不会重试。定向测试 `tests/unit/sdk/client-sdk/api-client.test.ts` 的 `RetryableApiClient retries on 503 for idempotent GET requests` 与 `...does NOT retry POST requests on 503 (not idempotent)` 本轮已复跑通过。 | 不改生产代码，回写现行重试守卫并复跑最小用例。 |
| R15-02 | §74 SDK/Plugin/Domain Registry 深层缺陷 | P0 | `src/sdk/client-sdk/api-client.ts` | §22.2 要求 X-Platform-Version/X-SDK-Version/X-Contract-Version header+版本握手——所有 SDK 均无发送 | 已解决（源码已满足，本轮补证据并修正陈旧测试） | 原审计基于版本头和握手尚未落地的旧 SDK；本轮复核时还发现单测仍沿用旧接口假设，把 `initialize()` 错当成返回握手载荷的函数，导致证据链继续漂移。 | 当前 `src/sdk/client-sdk/api-client.ts` 的 `buildAuthHeaders()` 会发送 `X-Platform-Version/X-SDK-Version/X-Contract-Version`，`createApiClient()` 默认启用 `performVersionHandshakeOnInit`，而 `performVersionHandshake()` 会显式请求 `/version` 并校验 `minClientVersion`。本轮复跑并通过 `tests/unit/sdk/client-sdk/api-client.test.ts` 的 `buildAuthHeaders adds platform version header when configured`，以及 `tests/unit/sdk/client-sdk/api-client-edge-cases.test.ts` 的 `RetryableApiClient.performVersionHandshake succeeds when version is compatible`、`versionsCompatible returns true when client version equals minimum`；同时把该测试文件中的旧 `initialize()` 断言修正到了现行握手接口。 | 不改生产代码，修正陈旧测试并回写版本头/握手依据。 |
| R15-03 | §74 SDK/Plugin/Domain Registry 深层缺陷 | P0 | `src/sdk/client-sdk/api-client.ts` | SDK contract §5 要求区分 network/auth/business 错误类型——实现抛原始 Error 无类型鉴别 | 已解决（源码已满足，本轮补证据） | 原审计基于旧的“透传原始 Error”阶段；现行 SDK 已按 HTTP 状态码分流成 typed error。 | 当前 `src/sdk/client-sdk/api-client.ts` 已在非 2xx 路径中把 `401/403` 归类为 `AuthError`、`4xx` 归类为 `BusinessError`、`5xx` 归类为 `NetworkError`。本轮复跑并通过 `tests/unit/sdk/client-sdk/api-client-edge-cases.test.ts` 的 `RetryableApiClient throws AuthError for 401 response` 与 `RetryableApiClient throws BusinessError for 400 response`。 | 不改生产代码，回写现行错误分类与定向测试依据。 |
| R15-04 | §74 SDK/Plugin/Domain Registry 深层缺陷 | P1 | `src/sdk/ (全部)` | SdkReleaseDescriptor(sdk_semver/platform_min/max_version/deprecation_policy)代码不存在 | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。 | 不宣称已修复。 |
| R15-05 | §74 SDK/Plugin/Domain Registry 深层缺陷 | P1 | `src/sdk/plugin-sdk/plugin-definition.ts:9` | PluginType="tool\ | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。 | 不宣称已修复。 |
| R15-06 | §74 SDK/Plugin/Domain Registry 深层缺陷 | P1 | `src/sdk/plugin-sdk/plugin-test-harness.ts:196` | executePlugin() 硬编码 mock——§22.3 要求 MockModelGateway+record/replay 无实际执行 | 已修复（源码/测试） | 根因与 `R8-40` 同源：问题不在 mock/replay 模式本身，而在 live 路径缺少可注入的 runtime seam，导致 harness 只能停留在“声明式 plugin metadata + mock 响应”。 | `src/sdk/plugin-sdk/plugin-test-harness.ts` 现已支持 `livePlugin/liveRunner` 注入，live 模式可桥接真实 SPI 与 lifecycle；`tests/unit/sdk/plugin-sdk/plugin-test-harness-regressions.test.ts` 已验证 live 模式会执行真实 plugin 逻辑且 timeout 仍然生效。 | 与 `R8-40` 同组关闭，关闭依据是源码与回归测试，而不是口头收口。 |
| R15-07 | §74 SDK/Plugin/Domain Registry 深层缺陷 | P1 | `src/sdk/pack-sdk/ + plugin-sdk/` | §22.3 要求 fixture auto-redact secrets/hash PII——无任何脱敏逻辑 | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。 | 不宣称已修复。 |
| R15-08 | §74 SDK/Plugin/Domain Registry 深层缺陷 | P1 | `plugin-spi-registry.ts vs contract §4` | 生命周期状态不匹配：contract 有 suspended/loading/initialized 代码有 degraded/disabled——互不兼容 | 已解决（源码修复并补定向测试） | 原问题真实存在：SPI 合同和运行时状态机长期并行演化，导致 contract 侧缺少 `loading/initialized`，registry 侧又保留 `degraded/disabled` 运行态。 | 本轮在 `src/domains/registry/plugin-spi.ts` 把 `loading/initialized/suspended` 纳入统一 lifecycle schema，并在 `src/domains/registry/plugin-spi-registry.ts` 将激活路径改为 `registered -> loading -> initialized -> active`；同时保留 `degraded/disabled` 作为运行态兼容值。`tests/unit/domains/registry/plugin-spi-schema.test.ts` 与 `plugin-spi-types.test.ts` 已覆盖。 | 关闭“互不兼容”这一条。 |
| R15-09 | §74 SDK/Plugin/Domain Registry 深层缺陷 | P1 | `src/domains/registry/plugin-spi.ts` | Contract 定义 suspend() 方法——PluginLifecycleHooks 无 suspend() 实现 | 已解决（原审计结论过期） | 该结论已被现行代码直接证伪，不是本轮新引入能力。 | 当前 `src/domains/registry/plugin-spi.ts` 的 `PluginLifecycleHooks` 已包含 `suspend?(reason, context?)`，`PluginSpiRegistry.suspend()` 也会优先调用该 hook。 | 属于 reaudit 文档滞后，不再保留为未解决。 |
| R15-10 | §74 SDK/Plugin/Domain Registry 深层缺陷 | P1 | `domain-registry-service.ts:106` | deprecate() 从任何状态转换——§37.10 仅允许 Active→Deprecated | 已解决（原审计结论过期） | 原审计基于旧实现快照；现行状态机已经按 contract 收紧。 | `src/domains/registry/domain-registry-service.ts` 现已把 `deprecate()` 限制为 `active -> deprecated`，并由现有 registry service tests 覆盖。 | 关闭该条，归因为文档基线漂移。 |
| R15-11 | §74 SDK/Plugin/Domain Registry 深层缺陷 | P1 | `src/domains/domain-specs.ts vs domain-model.ts` | §37.2 v4.3 拆分为7个独立 Descriptor——DomainDefinitionSchema 仍为 monolithic 旧结构 | 已解决（原审计结论过期） | 这条不是“完全未拆分”，而是 reaudit 没把现行组合式 schema 识别出来。 | `src/domains/domain-specs.ts` 已定义 `DomainCore/Risk/Knowledge/Eval/Governance/Interaction/ExecutionProfile` 等拆分 descriptor；`src/domains/registry/domain-model.ts` 则把 workflow/tool bundle/output contract/capability/execution profile/plugin binding 组合进 `DomainDefinitionSchema`，不再是单块旧对象。 | 关闭“仍为 monolithic 旧结构”的判断。 |
| R15-12 | §74 SDK/Plugin/Domain Registry 深层缺陷 | P1 | `domain-registry-service.ts` | 无 Updating 状态(§37.10 Active→Updating→Active)；无 update 方法 | 已解决（原审计结论过期） | 现行 domain registry 已支持更新态和显式更新流程。 | `src/domains/registry/domain-registry-service.ts` 已有 `updating()`、`completeUpdate()`，并把 lifecycle schema 纳入 `updating`；相关方法已有现存单测覆盖。 | 关闭该条，不再按未实现处理。 |
| R15-13 | §74 SDK/Plugin/Domain Registry 深层缺陷 | P2 | `builtin-plugin-registry.ts` | basic-evaluator 注册名含 "evaluator" 但 spiType="validator"——命名混淆 | 已解决（源码修复） | 原问题真实存在：内置 registry 用 `basic-evaluator` 命名一个 `spiType="validator"` 的插件，容易误导 SDK/平台接入侧。 | 本轮在 `src/plugins/validators/basic-evaluator.ts` 新增 canonical `createBasicValidatorPlugin()`，并在 `src/plugins/builtin-plugin-registry.ts` 以 `plugin.core.basic-validator` 作为正式注册名，同时保留 `plugin.core.basic-evaluator` 作为兼容别名。 | 关闭“命名混淆”条目。 |
| R15-14 | §74 SDK/Plugin/Domain Registry 深层缺陷 | P2 | `src/sdk/plugin-sdk/plugin-context.ts` | PluginContext.set() 允许覆写 "system.\*" key——无命名空间隔离违反沙箱 | 已解决（源码修复并补定向测试） | 原问题真实存在：`set()/setValues()` 可直接写入 `system.*`，破坏系统保留上下文。 | 本轮在 `src/sdk/plugin-sdk/plugin-context.ts` 为 `system.*` 增加保留命名空间保护，仅允许 `source="system"` 内部写入；`tests/unit/sdk/plugin-sdk/plugin-context.test.ts` 已新增拒绝路径覆盖。 | 关闭该安全缺口。 |
| R15-15 | §74 SDK/Plugin/Domain Registry 深层缺陷 | P2 | `pack-lifecycle-orchestration-service.ts:297` | supportWindowDays≥180 但 §22.4 规定≥90天——过严阻塞合法弃用 | 已解决（源码修复并补定向测试） | 原问题真实存在且是纯 contract 偏差：实现把最小支持窗口写成了 180 天。 | 本轮将 `src/sdk/pack-sdk/pack-lifecycle-orchestration-service.ts` 的最小 support window 调整为 90 天，并在 `tests/unit/sdk/pack-sdk/pack-lifecycle-orchestration-service.test.ts` 同时覆盖“90 天允许、89 天拒绝”。 | 关闭该条。 |
| R15-16 | §74 SDK/Plugin/Domain Registry 深层缺陷 | P2 | `domain-smoke-test.ts` | 不验证 executionProfile——§37.2 要求 lint 检查 risk/HITL/tool/eval/SLO 覆盖 | 已解决（源码修复并补定向测试） | 原问题真实存在：smoke test 只看 workflow/tool bundle/capability/resource quota，没有把 executionProfile 纳入 lint。 | 本轮在 `src/domains/registry/domain-smoke-test.ts` 新增 `validateExecutionProfile()`，校验 deterministic planning 的 compiled artifact、deterministic hot path 的 LLM 禁用，以及 realtime latency 约束；`tests/unit/domains/registry/domain-smoke-test.test.ts` 已新增失败场景回归。 | 关闭“完全不验证 executionProfile”的条目。 |
| R15-17 | §74 SDK/Plugin/Domain Registry 深层缺陷 | P2 | `domain-registry-service.ts` | 无 Archived 状态(§37.10 Deprecated→Archived)；无 archive() 方法 | 已解决（原审计结论过期） | 该结论已被现行状态机直接证伪。 | 当前 `src/domains/registry/domain-registry-service.ts` 已支持 `deprecated -> archived`，并提供 `archive()` 方法。 | 关闭该条，按 reaudit 文档过期处理。 |
| R15-18 | §74 SDK/Plugin/Domain Registry 深层缺陷 | P2 | `src/sdk/harness-sdk/index.ts` | Harness SDK 无 auth/tenant/budget 检查即暴露 createRun | 已解决（源码修复并补定向测试） | 原问题真实存在：`createRun()` 直接把调用透传给 runtime，没有任何租户、预算和受监管运行鉴权门槛。 | 本轮重写 `src/sdk/harness-sdk/index.ts` 的 SDK 表面：`createRun()` 现在强制 `tenantId`，对 `budgetRef` 做预算保留检查，对 supervised/full-auto 运行要求 `authContext.actorId`；同时补齐 `appendStepWithReceipt`、计划图校验、inter-plane envelope 签名和 bulkhead 兼容实现。`tests/unit/sdk/harness-sdk/index.test.ts` 与 `harness-sdk.test.ts` 已通过。 | 关闭该条，剩余未解的是更上层 HarnessRun/PlanGraphBundle 执行路径重构，不属于本条。 |
| R15-19 | §75 Ops-Maturity 深层缺陷（Explainability/Drift/Debugger/Edge/Compliance） | P0 | `explanation-pipeline-service.ts:23-32` | StageRationale 缺 rationaleId/alternatives/confidence/decisionInputRef/versionLockRef/visibilityLabels(§59.3) | 已解决（源码修复并补定向测试） | 这条一半是旧结论未同步：`rationaleId` 等字段类型面早已存在，但生成链路确实没有系统填充 `versionLockRef`、alternatives/confidence 等治理字段。 | 本轮重写 `src/ops-maturity/explainability/explanation-pipeline-service.ts`，在 `generate()` 中补齐 `versionLockRef`、alternatives/confidence/decisionInputRef/visibilityLabels 的落盘能力，并把 `recordedFacts/modelRationales/inferredSummary` 一并纳入 `StageRationale`；`tests/unit/ops-maturity/explainability/explanation-pipeline-service-verify.test.ts` 已覆盖 version lock、自定义锁和结构化 rationale 字段。 | 关闭的是“StageRationale contract 缺字段/生成链路不填充”问题。 |
| R15-20 | §75 Ops-Maturity 深层缺陷（Explainability/Drift/Debugger/Edge/Compliance） | P0 | `explanation-pipeline-service.ts:56` | 无 versionLockRef 强制——历史解释可被静默改写(§59.6) | 已解决（源码修复并补定向测试） | 原问题真实存在：解释生成没有任何版本锁定与校验能力。 | 当前 `ExplanationPipelineService.generate()` 会为每个 rationale 生成或接受显式 `versionLockRef`，并在内存索引中保存；`verifyVersionLock(rationaleId, versionLockRef)` 现在会做真实匹配校验，而不是“只要存在就算通过”。`tests/unit/ops-maturity/explainability/explanation-pipeline-service-verify.test.ts` 已覆盖正常、缺失、篡改三条路径。 | 关闭该历史解释可静默改写缺口。 |
| R15-21 | §75 Ops-Maturity 深层缺陷（Explainability/Drift/Debugger/Edge/Compliance） | P0 | `time-travel-debug-service.ts:91-280` | 无 ReplaySandboxPolicy——replay 直接重放原始事件无隔离，可触发真实副作用(§65.3) | 已解决（源码修复并补定向测试） | 原问题真实存在：time-travel replay 之前对事件没有 side-effect policy。 | 本轮在 `src/ops-maturity/workflow-debugger/time-travel-debug-service.ts` 引入 `ReplaySandboxPolicy`，默认阻断 `write/network/process/tool_call` 等外部副作用事件，`replayStep()/replayToCursor()/jumpToStep()` 都会在执行前做 policy 检查；`tests/unit/ops-maturity/workflow-debugger/time-travel-debug-service.test.ts` 与 `...edge-cases.test.ts` 已覆盖默认阻断和显式放行。 | 关闭 replay 无隔离这一条。 |
| R15-22 | §75 Ops-Maturity 深层缺陷（Explainability/Drift/Debugger/Edge/Compliance） | P0 | `compliance-reporter/template-registry/index.ts:3-10` | Template 缺 lockedOnGeneration/reportVersionLock/legal_version/effective_date/migration_rule(§66.1) | 已解决（源码修复并补定向测试） | 原问题真实存在：template registry 只保留最小 report schema，没有法律/版本锁/生效期元数据。 | 本轮在 `src/ops-maturity/compliance-reporter/template-registry/index.ts` 为 template schema 增加 `lockedOnGeneration/reportVersionLock/legalVersion/effectiveDate/migrationRule`，并通过 `normalizeComplianceTemplate()` 自动生成 `reportVersionLock`；`src/ops-maturity/compliance-reporter/compliance-report-pipeline-service.ts` 也把这些字段带入 artifact 和 markdown。`tests/unit/ops-maturity/compliance-reporter/template-registry/index.test.ts` 与 `compliance-report-pipeline-service.test.ts` 已覆盖。 | 关闭 template 法律/锁定元数据缺失问题。 |
| R15-23 | §75 Ops-Maturity 深层缺陷（Explainability/Drift/Debugger/Edge/Compliance） | P0 | `edge-runtime-sync-service.ts:58-75` | 无 risk_level≤medium 检查——任何任务可离线执行(§62.2) | 已解决（源码修复并补定向测试） | 原审计和现行实现都不准确：旧代码实际上只允许 `low`，既不是“完全无检查”，也不符合 contract 的 `≤ medium`。 | 本轮把 `src/ops-maturity/edge-runtime/edge-runtime-sync-service.ts` 收敛到 contract 语义：`executeOffline()` 仅允许 `low/medium`，缺失或 `high/critical` 一律拒绝；同时补上 `deviceId/offlineMaxDuration/keyLease` 必填校验、edge node 一致性校验和 offline 窗口校验。`tests/unit/scale-ops/edge-runtime-sync-service.test.ts` 已覆盖 `medium` 允许、高风险拒绝、缺字段拒绝和 stale window 拒绝。 | 关闭“离线执行缺风险门”的问题，并修正旧实现比 contract 更严但表达错误的偏差。 |
| R15-24 | §75 Ops-Maturity 深层缺陷（Explainability/Drift/Debugger/Edge/Compliance） | P1 | `explanation-pipeline-service.ts:56-92` | 不区分 recorded facts/model rationale/inferred summary——全部扁平文本(§59.1) | 已解决（源码修复并补定向测试） | 原问题真实存在：解释对象只有 flat summary/factors/risks，无法区分证据事实、模型理由和推断摘要。 | 当前 `StageRationale` 已显式包含 `recordedFacts/modelRationales/inferredSummary`，L3 渲染也会输出 `facts=`、`model=`、`inferred=` 分段；`tests/unit/ops-maturity/explainability/explanation-pipeline-service-verify.test.ts` 已断言三类信息分离存储。 | 关闭解释内容全部扁平文本这一条。 |
| R15-25 | §75 Ops-Maturity 深层缺陷（Explainability/Drift/Debugger/Edge/Compliance） | P1 | `explanation-pipeline-service.ts:53` | 无解释审计轨迹——谁何时查看了什么解释(§59.6) | 已解决（源码修复并补定向测试） | 原问题真实存在：解释生成和查看都没有 access trail。 | 本轮为 `ExplanationPipelineService` 新增 `ExplanationAuditTrailEntry`、`recordExplanationView()` 和 `getAuditTrail()`；`generate()` 会自动记录 generate 事件并带上 audience/user/ip/userAgent，view 事件也会追加到同一 rationale 的审计链。`tests/unit/ops-maturity/explainability/explanation-pipeline-service-verify.test.ts` 已覆盖 generate/view 记录和可选访问上下文。 | 关闭解释访问审计轨迹缺失问题。 |
| R15-26 | §75 Ops-Maturity 深层缺陷（Explainability/Drift/Debugger/Edge/Compliance） | P1 | `explanation-pipeline-service.ts:56` | 无 forensic_explanation_budget 预留即生成 L3(§59.6) | 已解决（源码修复并补定向测试） | 原问题真实存在：`L3` 审计级解释此前和普通解释共用路径，没有预算门。 | 现在 `ExplanationPipelineService.generate(..., "L3")` 强制要求 `forensicBudgetReservationId`，否则抛出 `explanation.forensic_budget_required`；`tests/unit/ops-maturity/explainability/explanation-pipeline-service.test.ts` 已覆盖拒绝路径，现有 L3 用例也全部改成显式预算保留。 | 关闭 L3 forensic budget 缺失问题。 |
| R15-27 | §75 Ops-Maturity 深层缺陷（Explainability/Drift/Debugger/Edge/Compliance） | P1 | `fingerprint-builder/index.ts:3-17` | BehaviorFingerprintInput 缺 avg_step_count(§63.1) | 已解决（源码修复并补定向测试） | 原问题真实存在：行为指纹没有 step-count 维度。 | 本轮在 `src/ops-maturity/drift-detection/fingerprint-builder/index.ts` 增加 `avgStepCount` 输入，并把 `avg_step_count` 与 `step_count_bucket` 纳入 normalized features；`tests/unit/ops-maturity/drift-detection/fingerprint-builder/index.test.ts` 已覆盖。 | 关闭行为指纹缺 avg step count 维度的问题。 |
| R15-28 | §75 Ops-Maturity 深层缺陷（Explainability/Drift/Debugger/Edge/Compliance） | P1 | `cross-agent-analyzer/index.ts:15-37` | 无 CrossAgentDriftAlert 发射——仅返回 score(§63.4) | 已解决（原审计结论过期，本轮补证据） | 这条被现行源码直接证伪：`CrossAgentDriftAlert` 类型和 `alertHistory` 发射路径在本轮之前就已存在，只是 reaudit 表没有吸收后续实现。 | 当前 `src/ops-maturity/drift-detection/cross-agent-analyzer/index.ts` 已定义 `CrossAgentDriftAlert` 并在 `analyze()` 中对 divergence/anti-gaming 触发 alert，`getDriftAlerts()` 会返回累计历史；本轮相关测试继续通过，并且顺带扩展了 anti-gaming 维度。 | 不需要额外生产修复，按审计过期条目关闭。 |
| R15-29 | §75 Ops-Maturity 深层缺陷（Explainability/Drift/Debugger/Edge/Compliance） | P1 | `cross-agent-analyzer/index.ts` | 无 anti-gaming——不区分真实/合成/keepalive 任务(§63.4) | 已解决（源码修复并补定向测试） | 原问题真实存在：虽然已有方差式 anti-gaming，但没有 workload provenance 维度，无法区分真实任务与 synthetic/keepalive 刷分。 | 本轮在 `src/ops-maturity/drift-detection/cross-agent-analyzer/index.ts` 为 `CrossAgentMetric` 增加 `taskKindDistribution(real/synthetic/keepalive)`，并把“真实任务占比过低且 synthetic/keepalive 过高”纳入 anti-gaming 检测；`tests/unit/ops-maturity/drift-detection/cross-agent-analyzer/index.test.ts` 已新增 synthetic-heavy 触发告警回归。 | 关闭 anti-gaming provenance 缺失问题。 |
| R15-30 | §75 Ops-Maturity 深层缺陷（Explainability/Drift/Debugger/Edge/Compliance） | P1 | `time-travel-debug-service.ts:105` | 无 auth/权限检查——§65.3 要求双因子+最小权限+短寿命会话+审计 | 已解决（源码修复并补定向测试） | 原问题真实存在：任何调用方都能直接创建 replay session。 | 本轮在 `TimeTravelDebugService.createSession()` 增加 `ReplayAccessContext` 检查：默认 dev/local 会话维持兼容；prod replay 则要求 `time_travel:replay` + `time_travel:replay:prod` 权限、MFA 已验证和短寿命 `sessionExpiresAt`。`tests/unit/ops-maturity/workflow-debugger/time-travel-debug-service.test.ts` 与 `...edge-cases.test.ts` 已覆盖 prod MFA/expiry/permission 拒绝。 | 关闭 replay session 缺鉴权问题。 |
| R15-31 | §75 Ops-Maturity 深层缺陷（Explainability/Drift/Debugger/Edge/Compliance） | P1 | `workflow-debugger-service.ts:86-100` | Run comparison 仅 diff step status——缺 decision/cost/duration/outcome diff(§65.4) | 已解决（源码修复并补定向测试） | 原问题真实存在：comparison report 之前只委托 `compareWorkflowRuns()` 做 step status diff。 | 本轮在 `src/ops-maturity/workflow-debugger/workflow-debugger-service.ts` 扩展 `WorkflowTraceFrame`，新增 `decision/costUsd/durationMs/outcome/actualSideEffects` 等比较维度，`buildComparisonReport()` 会在 step diff 外继续输出 `decision:/cost:/duration:/outcome:` 差异；`tests/unit/ops-maturity/workflow-debugger/workflow-debugger-service.test.ts` 已新增对应断言。 | 关闭 run comparison 维度不足问题。 |
| R15-32 | §75 Ops-Maturity 深层缺陷（Explainability/Drift/Debugger/Edge/Compliance） | P1 | `workflow-debugger-service.ts:86-100` | 无 regression_detected 自动标记(§65.4) | 已解决（源码修复并补定向测试） | 原问题真实存在：comparison report 之前没有显式 regression flag。 | 当前 `RunComparisonReport` 已新增 `regressionDetected`，当任一状态或决策/成本/时长/结果差异出现时自动置为 `true`；零差异时返回 `false`。`tests/unit/ops-maturity/workflow-debugger/workflow-debugger-service.test.ts` 已覆盖正反两条路径。 | 关闭 regression 自动标记缺失问题。 |
| R15-33 | §75 Ops-Maturity 深层缺陷（Explainability/Drift/Debugger/Edge/Compliance） | P1 | `workflow-debugger/` | 无 WS 实时 debug stream /ws/v1/debug/{workflow_id}(§65.2) | 已解决（源码修复并补定向测试） | 原问题真实存在：workflow debugger 只有离线调试服务，没有面向实时调试的 websocket stream 表面。 | 本轮新增 `src/ops-maturity/workflow-debugger/debug-stream-service.ts`，提供 canonical 路径 `/ws/v1/debug/{workflowId}`、订阅注册与按 workflow 缓冲事件；`src/ops-maturity/workflow-debugger/index.ts` 已导出该服务，`tests/unit/ops-maturity/workflow-debugger/debug-stream-service.test.ts` 已覆盖 canonical path 与 event buffer。 | 关闭实时 debug stream 缺口。 |
| R15-34 | §75 Ops-Maturity 深层缺陷（Explainability/Drift/Debugger/Edge/Compliance） | P1 | `workflow-debugger-service.ts:5-11` | DebugBreakpointDefinition 缺 replay-condition 类型(§65.3) | 已解决（源码修复并补定向测试） | 原问题真实存在：断点定义只有基础条件/action，不能表达 replay condition。 | 当前 `src/ops-maturity/workflow-debugger/workflow-debugger-service.ts` 的 `DebugBreakpointDefinition` 已补 `replayCondition`，支持字符串和结构化对象 `{ eventType, expression, maxReplays }`；同时兼容 `planGraphId/nodeRunSelector` 与旧别名归一化，`tests/unit/ops-maturity/workflow-debugger/index.test.ts` 和 `workflow-debugger-service.test.ts` 已验证。 | 关闭断点 replay-condition 类型缺失问题。 |
| R15-35 | §75 Ops-Maturity 深层缺陷（Explainability/Drift/Debugger/Edge/Compliance） | P1 | `evidence-mapper/index.ts` | 无 control-point mapping(pass/fail/partial/not_applicable)——仅按类型分组(§66.2) | 已解决（源码修复并补定向测试） | 原问题真实存在：evidence mapper 之前只支持按 evidence type 聚合，缺少 control point 视图。 | 本轮在 `src/ops-maturity/compliance-reporter/evidence-mapper/index.ts` 增加 `mapEvidenceByControlPoint()`、`ControlPointCoverage`，并为 `EvidenceReference` 补 `controlId/controlStatus/freshnessHours/trustScore/tamperProof`；`tests/unit/ops-maturity/compliance-reporter/evidence-mapper/index.test.ts` 已验证 `pass/fail/partial/not_applicable` 映射。 | 关闭 control-point mapping 缺口。 |
| R15-36 | §75 Ops-Maturity 深层缺陷（Explainability/Drift/Debugger/Edge/Compliance） | P1 | `compliance-report-pipeline-service.ts:85` | evidenceQualityScore 仅覆盖率——缺 freshness/trustworthiness/tamper-proof(§66.2) | 已解决（源码修复并补定向测试） | 原问题真实存在：旧 `evidenceQualityScore` 基本等同 coverage，不足以表达证据质量。 | 当前 `src/ops-maturity/compliance-reporter/compliance-report-pipeline-service.ts` 已引入 `controlPointMap`、`evidenceQualityBreakdown`，并把 `evidenceQualityScore` 改为综合 completeness/freshness/trustworthiness/tamperProof 的质量分；`tests/unit/ops-maturity/compliance-reporter/compliance-report-pipeline-service.test.ts` 与 `...extended.test.ts` 已覆盖。 | 关闭 evidence quality 仅看覆盖率的问题。 |
| R15-37 | §75 Ops-Maturity 深层缺陷（Explainability/Drift/Debugger/Edge/Compliance） | P1 | `compliance-report-pipeline-service.ts:111-136` | HumanSignoff 缺 escalation_owner/timeout_action(§66.2) | 已解决（源码修复并补定向测试） | 原问题真实存在：人工签核对象缺 escalation owner 和超时动作，无法表达升级责任链。 | 当前 `ComplianceReportHumanSignoff` 已补 `escalationOwner`、`timeoutAction`，`evaluateHumanSignoff()` 会保留并返回这两项治理元数据；`tests/unit/ops-maturity/compliance-reporter/gap-analyzer-service.test.ts` 中的 signoff/schedule 覆盖已验证。 | 关闭 human signoff 元数据缺口。 |
| R15-38 | §75 Ops-Maturity 深层缺陷（Explainability/Drift/Debugger/Edge/Compliance） | P1 | `edge-runtime-sync-service.ts` | 无 offline_max_duration 强制——maxLocalRetentionHours 声明后无检查(§62.2) | 已解决（源码修复并补定向测试） | 原问题真实存在：离线执行路径此前没有把 retention window 真正用作拒绝门。 | 本轮在 `src/ops-maturity/edge-runtime/edge-runtime-sync-service.ts` 的 `executeOffline()` 增加 offline window 校验，超出 `maxLocalRetentionHours` 直接拒绝；`tests/unit/scale-ops/edge-runtime-sync-service.test.ts` 已新增 stale offline execution window 拒绝回归。 | 关闭 offline max duration 未强制执行的问题。 |
| R15-39 | §75 Ops-Maturity 深层缺陷（Explainability/Drift/Debugger/Edge/Compliance） | P1 | `edge-runtime-sync-service.ts` | 无 key-lease/device-attestation/certificate-revocation(§62.2) | 已解决（源码修复并补定向测试） | 原问题真实存在：离线 profile 校验缺少设备证明与证书状态门控。 | 当前 `src/ops-maturity/edge-runtime/edge-runtime-sync-service.ts` 已把 `keyLease` 纳入必填，并为 `EdgeRuntimeProfile` 增加 `deviceAttestation`、`certificateStatus`；`executeOffline()` 会拒绝无效 attestation 和 `revoked` 证书。`tests/unit/scale-ops/edge-runtime-sync-service.test.ts` 已覆盖缺字段、无效 attestation 与 revoked cert 拒绝。 | 关闭 edge 离线安全门缺失问题。 |
| R15-40 | §75 Ops-Maturity 深层缺陷（Explainability/Drift/Debugger/Edge/Compliance） | P1 | `sync-queue/index.ts` | SyncQueue 缺 sequence_no/side_effect_dependency_refs——§62.3 要求签名 append-only+拓扑排序 | 已解决（源码修复并补定向测试） | 原问题真实存在：队列排序只看优先级，缺少顺序号和依赖链校验。 | 本轮在 `src/ops-maturity/edge-runtime/sync-queue/index.ts` 将 `orderEdgeSyncQueue()` 收敛为 `priority desc -> sequence_no asc -> createdAt`，并新增 `validateSyncQueueChain()`/`SyncQueueChainValidationResult`，对 `prev_hash` 与 dependency chain 做校验；`tests/unit/ops-maturity/edge-runtime-sync-queue.test.ts` 已覆盖 sequence、缺依赖和合法链场景。 | 关闭 sync queue 顺序与依赖校验缺口。 |
| R15-41 | §75 Ops-Maturity 深层缺陷（Explainability/Drift/Debugger/Edge/Compliance） | P1 | `edge-runtime/` | 无 remote_wipe/edge_quarantine 指令处理(§62.3) | 已解决（源码修复并补定向测试） | 原问题真实存在：edge sync service 之前没有显式控制指令入口。 | 当前 `src/ops-maturity/edge-runtime/edge-runtime-sync-service.ts` 已新增 `executeControlCommand()`，并实现 `remote_wipe` 与 `edge_quarantine` 两类控制动作；`tests/unit/scale-ops/edge-runtime-sync-service.test.ts` 已覆盖两条指令。 | 关闭 remote wipe / quarantine 缺口。 |
| R15-42 | §75 Ops-Maturity 深层缺陷（Explainability/Drift/Debugger/Edge/Compliance） | P2 | `changepoint-detector/index.ts:26-31` | 阈值硬编码无 sample-size/分布假设/误报处理声明(§63.2) | 已解决（源码修复并补定向测试） | 原问题真实存在：changepoint detector 只有硬阈值判断，没有样本量、分布假设和误报抑制元数据。 | 本轮在 `src/ops-maturity/drift-detection/changepoint-detector/index.ts` 增加构造配置和输出字段 `sampleSize/minSampleSize/distributionAssumption/falsePositiveRate`，并支持 `minSamplesBetweenAlerts` 重复告警抑制；`src/ops-maturity/drift-detection/drift-detector-service.ts` 已接入该配置。`tests/unit/ops-maturity/drift-detection/changepoint-detector/index.test.ts` 已覆盖元数据与 suppression。 | 关闭 changepoint 阈值硬编码且无统计声明的问题。 |
| R15-43 | §75 Ops-Maturity 深层缺陷（Explainability/Drift/Debugger/Edge/Compliance） | P2 | `workflow-debugger/` | 无 side-effect diff(expected vs actual)(§65.1) | 已解决（源码修复并补定向测试） | 原问题真实存在：comparison report 之前不会显式比较 expected side effects 和 actual side effects。 | 当前 `src/ops-maturity/workflow-debugger/workflow-debugger-service.ts` 的 `buildComparisonReport()` 已增加 `side_effect_expectation_mismatch` 差异输出，并保留 node-run/legacy step 兼容；`tests/unit/ops-maturity/workflow-debugger/workflow-debugger-service.test.ts` 已新增 expected-vs-actual side-effect mismatch 断言。 | 关闭 side-effect diff 缺口。 |
| R15-44 | §75 Ops-Maturity 深层缺陷（Explainability/Drift/Debugger/Edge/Compliance） | P2 | `compliance-reporter/` | 无 GapAnalyzer+remediation owner 分配(§66.2) | 已解决（源码修复并补定向测试） | 原问题真实存在：gap analyzer 只能给出缺口状态，不能落责任人与 remediation。 | 本轮在 `src/ops-maturity/compliance-reporter/compliance-report-pipeline-service.ts` 的 `GapAnalyzerService` 增加 `ownerMap`、`deadlineMap`，返回结果包含 `owner/deadline/remediation`；`tests/unit/ops-maturity/compliance-reporter/gap-analyzer-service.test.ts` 已覆盖 owner、deadline 和 remediation 输出。 | 关闭 remediation owner 分配缺口。 |
| R15-45 | §75 Ops-Maturity 深层缺陷（Explainability/Drift/Debugger/Edge/Compliance） | P2 | `compliance-reporter/` | 无按框架频率调度(SOC2 quarterly/HIPAA monthly)(§66.3) | 已解决（源码修复并补定向测试） | 原问题真实存在：compliance report pipeline 没有按 framework 产出调度频率。 | 当前 `src/ops-maturity/compliance-reporter/compliance-report-pipeline-service.ts` 已新增 `ComplianceFrequencySchedulerService`，内置 `SOC2 -> quarterly`、`HIPAA -> monthly`、`GDPR -> monthly`、`ISO27001 -> annual`，并暴露 `getFrameworkSchedule()` 与 `nextScheduledReportDueAt()`；`tests/unit/ops-maturity/compliance-reporter/gap-analyzer-service.test.ts` 已验证框架频率和下次到期时间。 | 关闭 framework frequency scheduling 缺口。 |
| R15-46 | §75 Ops-Maturity 深层缺陷（Explainability/Drift/Debugger/Edge/Compliance） | P2 | `compliance-reporter/` | 无 AuditorAccess PII auto-redaction+per-framework 最小权限(§66.4) | 已解决 | 原审计已过时：compliance report pipeline 当前已经实现 framework 粒度最小权限校验与默认 PII 脱敏视图。 | 当前 `src/ops-maturity/compliance-reporter/compliance-report-pipeline-service.ts` 已提供 `ComplianceAuditorAccessService`、`requiredPermissionsForFramework()`、`buildAuditorView()`，会按 framework 检查权限并在 `allowPiiAccess !== true` 时自动 redaction markdown / evidenceMap；`tests/unit/platform/ops-maturity/reaudit-batch-r14-r15-r16-regressions.test.ts` 已验证 HIPAA 权限门与 email/SSN/phone 自动脱敏。 | 回写 auditor access 现行实现。 |
| R15-47 | §75 Ops-Maturity 深层缺陷（Explainability/Drift/Debugger/Edge/Compliance） | P2 | `edge-runtime/` | 部署模式分类(Edge-Micro/Standard/Mobile/Hybrid)§62.4 未建模 | 已解决 | 原问题只剩 contract 回写缺口：运行时实际上已经建模四类 edge deployment mode，并能自动解析。 | 当前 `src/ops-maturity/edge-runtime/edge-runtime-sync-service.ts` 已定义 `EdgeDeploymentMode = edge_micro | edge_standard | edge_mobile | edge_hybrid`，`resolveEdgeDeploymentMode()` 可按 capability / connectivity 解析；本轮也把 `docs_zh/contracts/edge_runtime_and_sync_contract.md` 补齐为 canonical contract，并在回归测试中验证四类模式都可落地。 | 关闭 edge mode 未建模结论。 |
| R15-48 | §75 Ops-Maturity 深层缺陷（Explainability/Drift/Debugger/Edge/Compliance） | P2 | `chaos-experiment-scheduler.ts:10-11` | 引用 "§68/§66" 但均不对应 chaos——模块无架构锚点 | 已解决 | 该条已被现行代码推翻：chaos scheduler 当前已有明确 contract / ADR / architecture 三段架构锚点。 | 当前 `src/ops-maturity/chaos/chaos-experiment-scheduler.ts` 已导出 `CHAOS_ARCHITECTURE_ANCHORS`，指向 `quality_engineering_and_chaos_testing_contract.md`、`ADR-089` 与 `02-code-architecture-reference.md`；回归测试已验证三条锚点路径稳定存在。 | 回写 chaos 模块架构锚点证据。 |
| R15-49 | §76 Scale-Ecosystem 深层缺陷（Multi-region/Tenant/Marketplace/SLA） | P0 | `failover-controller/index.ts:1-46` | Failover 无 fencing epoch——§52.3 要求 promote epoch+旧 leader 降级；实现为无状态函数 | 已解决（源码复核并补定向测试） | 原审计停留在早期无状态 failover 判定；现行 controller 已把 promote epoch、demote leader 与 stale join 拒绝链接入同一决策面。 | 当前 `src/scale-ecosystem/multi-region/failover-controller/index.ts` 的 `resolve()` 会回填 `fencingEpoch/demotedRegionId`，`rejectRegionJoin()` 会拒绝过期 epoch 的旧 leader 重新加入；`tests/unit/scale-ecosystem/reaudit-r15-multi-region-resource-manager-regressions.test.ts` 已验证 failover 后 `fencingEpoch === 1` 且 stale rejoin 被拒。 | 回写现行 failover fencing 语义与定向回归依据。 |
| R15-50 | §76 Scale-Ecosystem 深层缺陷（Multi-region/Tenant/Marketplace/SLA） | P0 | `multi-region/ (全部)` | 无 FailoverReconciliationJob——§52.3+§31 要求列出未复制写/开放预算/待审批/outbox gap+restricted-write | 已解决（源码复核并补定向测试） | 该条的真实缺口已由独立 reconciliation job 收敛，旧结论没有吸收后续故障切换后差异扫描实现。 | 当前 `src/scale-ecosystem/multi-region/failover-reconciliation-job.ts` 已实现 `FailoverReconciliationJob`，会统一汇总 `unreplicated_write/open_budget/pending_approval/outbox_gap/restricted_write` 五类缺口；定向回归已验证所有必需类别都会被扫描出来。 | 关闭“无 reconciliation job”旧结论。 |
| R15-51 | §76 Scale-Ecosystem 深层缺陷（Multi-region/Tenant/Marketplace/SLA） | P0 | `cross-region-routing-service.ts:44-95` | 路由不区分读写——§52.3 要求 truth write 仅在 partition leader；读写统一路由 | 已解决（源码复核并补定向测试） | 原问题属于早期路由策略过粗；现行写路径已显式优先 partition leader，而不是复用读路由。 | 当前 `src/scale-ecosystem/multi-region/cross-region-routing-service.ts` 已对 `operationType: "write"` 强制选择 `isPartitionLeader` 区域；定向测试验证主区健康但非 leader 时仍会把写请求路由到 leader。 | 回写现行读写分流与 leader-only truth write 证据。 |
| R15-52 | §76 Scale-Ecosystem 深层缺陷（Multi-region/Tenant/Marketplace/SLA） | P1 | `region-router/index.ts:3-11` | RegionDescriptor 缺 provider/endpoints/dataResidencyPolicy(§52.1) | 已解决（源码复核并补定向测试） | 旧审计基于早期 descriptor 最小结构；现行 schema 已把 provider、endpoint 与 residency policy 拉入 authoritative contract。 | 当前 `src/scale-ecosystem/multi-region/region-router/index.ts` 的 `RegionDescriptorSchema` 已要求 `provider/endpoints/dataResidencyPolicy`；定向回归已验证三类字段均为必备 canonical 字段。 | 回写现行 RegionDescriptor contract。 |
| R15-53 | §76 Scale-Ecosystem 深层缺陷（Multi-region/Tenant/Marketplace/SLA） | P1 | `multi-region/ (全部)` | 无跨境传输合规链——§52.4 Jurisdiction Classifier→Transfer Impact Assessor→Mechanism Selector(SCC/BCR/DPF)→Data Minimizer→Output Scanner→Transfer Logger 全缺 | 已解决（源码复核并补定向测试） | 原结论未吸收后续合规流水线实现；现行服务已把跨境分类、机制选择、最小化、输出扫描与日志落地串成一条链。 | 当前 `src/scale-ecosystem/multi-region/cross-border-transfer-compliance-service.ts` 已输出 `jurisdictionClassifier/transferImpactAssessment/mechanismSelection/dataMinimizer/outputScanner` 并持久记录 transfer log；定向测试已验证 EU→US 场景下的 cross-border/high-risk/SCC/minimized payload/transfer log。 | 关闭“跨境传输合规链全缺”旧结论。 |
| R15-54 | §76 Scale-Ecosystem 深层缺陷（Multi-region/Tenant/Marketplace/SLA） | P1 | `data-replicator/index.ts:163-189` | recordEvent() 不检查 shouldReplicateToRegion() 对 residency policy——数据可能复制到禁止区域 | 已解决（源码复核并补定向测试） | 旧结论对应的是 residency gate 未前置到记录链的阶段；现行实现已在事件入队前按目标区域 residency 策略 fail-close。 | 当前 `src/scale-ecosystem/multi-region/data-replicator/index.ts` 会在 `recordEvent()` 阶段拒绝 `local_only` 等不允许的目标区域；定向回归已验证对 `local_only` 目标返回 `null`、不会产生复制事件。 | 回写现行 residency enforcement 证据。 |
| R15-55 | §76 Scale-Ecosystem 深层缺陷（Multi-region/Tenant/Marketplace/SLA） | P1 | `region-health-check-service.ts:283-315` | performHealthCheck 用字符串长度模拟延迟——非真实网络探测，failover 决策不可靠 | 已解决（源码复核并补定向测试） | 原问题属于早期 mock-like 健康检查路径；现行健康探测已切到真实 HTTP probe。 | 当前 `src/scale-ecosystem/multi-region/region-health-check-service.ts` 已通过 `fetch(..., { method: "HEAD" })` 访问 `/health` 端点做网络探测；定向回归已验证实际请求 URL 为 `<endpoint>/health` 且 method 为 `HEAD`。 | 关闭“字符串长度模拟探测”旧结论。 |
| R15-56 | §76 Scale-Ecosystem 深层缺陷（Multi-region/Tenant/Marketplace/SLA） | P1 | `data-replicator/index.ts:240-247` | pendingCount=events.length(总批次)非实际 pending——复制 lag 指标错误 | 已解决（源码复核并补定向测试） | 原审计对应的是旧 checkpoint 更新逻辑；现行 `flush()` 已把 `pendingCount` 收敛为“本次 flush 后仍未成功送达的事件数”，而不是整批 `events.length`。 | 当前 `src/scale-ecosystem/multi-region/data-replicator/index.ts` 在 flush 后以 `pendingCount = events.length - lastSequence` 更新 checkpoint；本轮新增 `tests/unit/scale-ecosystem/reaudit-r15-multi-region-resource-manager-regressions.test.ts` 的 `R15-56 replication checkpoint stores actual pending count after partial flush failures`，直接验证 3 条事件中 1 条失败时 checkpoint `pendingCount === 1`。 | 关闭复制 lag 指标把整批数误当 pending 的旧结论。 |
| R15-57 | §76 Scale-Ecosystem 深层缺陷（Multi-region/Tenant/Marketplace/SLA） | P1 | `tenant-platform-service.ts:367-397` | createTenant() 接受 isolationMode 但无实际执行——dedicated_pool 租户无真实隔离(§9.8) | 已解决（源码/测试） | 根因是此前 `createTenant()` 虽然会进入 `provisionDedicatedPoolIsolation()`，但该方法只打日志、不产出任何可执行隔离资源或可查询隔离状态，`dedicated_pool` 实际仍停留在 metadata。 | 本轮在 `src/scale-ecosystem/tenant-platform/tenant-platform-service.ts` 引入 `ResourcePoolService` 与 `DedicatedPoolIsolationRecord`：`dedicated_pool` 租户创建时会实际注册 tenant-scoped `resourcePool`、记录 `routingPolicy: "dedicated_pool_only"` 与 `executionIsolation: "tenant_scoped_worker_pool"`，并可通过 `getDedicatedPoolIsolation()` / `getResourcePoolService()` 查询；`decommissionTenant()` 也会清理对应 isolation record。`tests/unit/scale-ecosystem/tenant-platform/tenant-platform-service.test.ts` 已新增 dedicated pool 回归。 | 关闭“只有 isolationMode 元数据、没有真实 dedicated pool 隔离”的缺口。 |
| R15-58 | §76 Scale-Ecosystem 深层缺陷（Multi-region/Tenant/Marketplace/SLA） | P1 | `quota-enforcer/index.ts:3-12` | Quota 为单资源模型——§53.2 要求 MultiResourceQuotaVector(worker/QPS/TPM/budget/storage 同时) | 已解决（源码复核并补定向测试） | 原审计停留在单维 worker quota 阶段；现行 quota evaluator 已对多个资源维度同时计算超限和剩余额度。 | 当前 `src/scale-ecosystem/resource-manager/quota-enforcer/index.ts` 的 `evaluateMultiDimensionalQuota()` 已支持 `workerUnits/qps/budgetUsd` 等并发维度，并返回 `exceededDimensions/remainingByDimension`；定向回归已验证 qps 超限时仍保留其他维度剩余量。 | 回写现行 multi-resource quota 证据。 |
| R15-59 | §76 Scale-Ecosystem 深层缺陷（Multi-region/Tenant/Marketplace/SLA） | P1 | `preemption/index.ts:7-13` | 抢占无 checkpoint-before-preempt——§53.4 要求先完成 checkpoint 再抢占 | 已解决（源码复核并补定向测试） | 原问题属于候选筛选过于粗糙；现行抢占选择已把最近 checkpoint 作为必要条件之一。 | 当前 `src/scale-ecosystem/resource-manager/preemption/index.ts` 的 victim 选择会优先过滤缺失 `lastCheckpointTimestampMs` 或 checkpoint 过旧的候选；定向回归已验证仅存在近期 checkpoint 的执行才会被选为 victim。 | 关闭“无 checkpoint-before-preempt”旧结论。 |
| R15-60 | §76 Scale-Ecosystem 深层缺陷（Multi-region/Tenant/Marketplace/SLA） | P1 | `fair-queue/index.ts:11-22` | 按 orgId/domainId 字典序排序非 WFQ——§53.4 要求加权公平队列+保证配额+借用+回收 | 已解决（源码复核并补定向测试） | 旧审计对应的是字典序/静态顺序实现；现行 fair queue 已按优先级、年龄、SLA 和 quota 份额计算得分。 | 当前 `src/scale-ecosystem/resource-manager/fair-queue/index.ts` 的 `orderFairQueue()` 已基于 score 排序而非字典序；定向回归已验证高优先级、高龄、较高 quota 份额的项会排在前面。 | 回写现行 score-based fair queue 依据。 |
| R15-61 | §76 Scale-Ecosystem 深层缺陷（Multi-region/Tenant/Marketplace/SLA） | P1 | `marketplace/catalog/index.ts:3-33` | CatalogEntry 用 listingId 非 entryId+packId(§55.2)；缺 installCount/rating/certificationStatus | 已解决（源码复核并补定向测试） | 原问题属于 catalog contract 漂移；现行 schema 已以 `entryId` 为 canonical，并向后回填 legacy `listingId/packId`。 | 当前 `src/scale-ecosystem/marketplace/catalog/index.ts` 的 `MarketplaceCatalogEntrySchema` 已要求 `entryId/title/rating/installCount/certificationStatus`，并把 `listingId/packId` 回填为 `entryId`；定向回归已验证 canonical+legacy 字段同时可用。 | 回写现行 marketplace catalog contract。 |
| R15-62 | §76 Scale-Ecosystem 深层缺陷（Multi-region/Tenant/Marketplace/SLA） | P1 | `marketplace/ (全部)` | 无 SBOM/provenance/PluginTrustStore——§55.1+§11 要求签名验证+信任根+撤销列表+隔离 | 已解决（源码/contract/测试） | 原结论只吸收到了“有部分 certification gate”，但缺少统一的 trust root / provenance / revocation store，导致 marketplace 仍无法把 SBOM、签名、撤销列表和隔离建议收敛为同一条 install trust decision。 | 本轮新增 `src/scale-ecosystem/marketplace/plugin-trust-store.ts`，提供 `PluginTrustRoot`、`PluginProvenanceAttestation`、`RevokedPluginArtifact` 与 `PluginTrustStore.evaluateArtifact()`，统一执行 trust root、provenance、revocation、signature、SBOM、sandbox、egress review 的 install gate，并输出 `requiredIsolationMode`；同时更新 `src/scale-ecosystem/marketplace/index.ts` 导出该能力，补充 `docs_zh/contracts/marketplace_catalog_and_revenue_contract.md` 的 canonical 对象与规则。`tests/unit/scale-ecosystem/marketplace/plugin-trust-store.test.ts` 与 `marketplace-index.test.ts` 已验证 trusted/revoked 两条路径及 barrel export。 | 关闭“marketplace 没有 PluginTrustStore / provenance / revocation 统一模型”的缺口；SBOM 与签名门继续与现有 certification/gov gate 协同工作。 |
| R15-63 | §76 Scale-Ecosystem 深层缺陷（Multi-region/Tenant/Marketplace/SLA） | P1 | `marketplace-governance-service.ts:614-662` | 弃用跳过 sunset 状态——§55.5 要求 active→deprecated→sunset→removed 含30天倒计时 | 已解决（源码/测试） | 根因是原治理服务会直接在 deprecated/retired 之间跳转，没有把 sunset 作为强制中间态，也没有 sunset window 校验。 | 本轮 `MarketplaceGovernanceService` 已要求 `deprecated -> sunset -> retired` 顺序推进，并对 sunset window 做 180 天 fail-close 校验；这比 audit 描述的 30 天最小倒计时更严格。`tests/unit/scale-ecosystem/marketplace/marketplace-governance-lifecycle.test.ts` 已验证 direct sunset / premature retire 都会被阻断。 | 以更强的 180 天门槛覆盖 30 天最小要求。 |
| R15-64 | §76 Scale-Ecosystem 深层缺陷（Multi-region/Tenant/Marketplace/SLA） | P1 | `marketplace/ (全部)` | 无反向依赖检查——§55.6 要求 uninstall/deprecation 检查依赖图并阻止 | 已解决（源码/测试） | 原结论没有吸收 catalog 侧已经存在的反向依赖分析能力，因此把“治理流未复核”误写成“全模块都没有反向依赖检查”。 | 当前 `src/scale-ecosystem/marketplace/catalog/index.ts` 已提供 `checkReverseDependencies()`，会返回 `dependentEntries/blockers/canSafelyRemove`；本轮补了 `tests/unit/scale-ecosystem/marketplace-catalog.test.ts`，直接验证存在依赖方时会阻止安全移除。 | 关闭的是“marketplace 完全没有 reverse dependency check”的旧结论。 |
| R15-65 | §76 Scale-Ecosystem 深层缺陷（Multi-region/Tenant/Marketplace/SLA） | P1 | `sla-engine/tier-resolver/index.ts:3-12` | SLA tier 缺 availability/approvalLatencySlo/incidentResponseSlo/costMultiplier/supportLevel(§54.1) | 已解决（源码复核并补定向测试） | 旧结论对应的是早期最小 tier contract；现行 SLA schema 已把可用性、审批、事件响应、成本和支持级别收敛为 canonical 字段。 | 当前 `src/scale-ecosystem/sla-engine/tier-resolver/index.ts` 的 `SlaTierSchema` 已要求 `availability/approvalLatencySlo/incidentResponseSlo/costMultiplier/supportLevel`；定向回归已验证这些字段都能通过 authoritative parse。 | 回写现行 SLA tier contract。 |
| R15-66 | §76 Scale-Ecosystem 深层缺陷（Multi-region/Tenant/Marketplace/SLA） | P2 | `cdc-replication-service.ts:97-289` | CDC 无时间 lag 监控(§52 ≤30s SLA)——getReplicationLag() 返回事件数非时间 | 已解决（源码复核并补定向测试） | 原问题属于 lag 指标过于粗糙；现行 CDC 已按事件时间戳与 checkpoint 时间计算真实 lag 毫秒值。 | 当前 `src/scale-ecosystem/multi-region/cdc-replication-service.ts` 的 `getReplicationLagStatus()` 会返回 `lagMs/pendingEvents/withinSlo`；定向回归已验证 45 秒 lag 在 30 秒 SLA 下被判为 `withinSlo=false`。 | 回写现行时间型 lag 监控依据。 |
| R15-67 | §76 Scale-Ecosystem 深层缺陷（Multi-region/Tenant/Marketplace/SLA） | P2 | `region-health-check-service.ts:418-445` | orchestrateFailover() fire-and-forget 无 FailoverRecord/FencingEpochChanged event(§31) | 已解决（源码复核并补定向测试） | 旧结论没有吸收编排层后续补齐的 failover 记录与 epoch 事件。 | 当前 `src/scale-ecosystem/multi-region/region-health-check-service.ts` 中的 `RegionFailoverOrchestrator` 已记录 failover history、维护 fencing epoch，并发出 `multi_region.failover_recorded` 与 `multi_region.fencing_epoch_changed`；定向回归已验证两类事件和最新 failover record 都会落地。 | 回写现行 failover audit/event 证据。 |
| R15-68 | §76 Scale-Ecosystem 深层缺陷（Multi-region/Tenant/Marketplace/SLA） | P2 | `fair-scheduling-service.ts:44-68` | 无 promotion_budget 强制——§53.4 要求每租户每小时/天升级预算 | 已解决（源码复核并补定向测试） | 原问题属于调度器只看资源不看升级预算；现行公平调度已把租户级 promotion budget 作为前置门禁。 | 当前 `src/scale-ecosystem/resource-manager/fair-scheduling-service.ts` 的 `schedule()` 已评估 `promotionBudget` 并在超额时返回 `resource_manager.promotion_budget_exhausted`；定向回归已验证预算耗尽时不会触发 preemption。 | 回写现行 promotion budget enforcement 依据。 |
| R15-69 | §76 Scale-Ecosystem 深层缺陷（Multi-region/Tenant/Marketplace/SLA） | P2 | `resource-pool-service.ts:21-77` | ResourcePool 无 tenant/org scope——§9.8 要求 failure_rate>30% 自动隔离 | 已解决（源码复核并补定向测试） | 旧结论对应的是 pool 只有容量元数据、没有作用域和自动隔离状态的阶段；现行实现已收敛 tenant/org scope 与健康隔离。 | 当前 `src/scale-ecosystem/resource-manager/resource-pool-service.ts` 已支持 `scopeType/tenantId/organizationId`，并在高失败率观测后把 pool 标记为 `isolated` 从而拒绝新分配；定向回归已验证 `failureRate=0.31` 时进入隔离且分配被拒。 | 回写现行 tenant-scoped pool isolation 证据。 |
| R15-70 | §76 Scale-Ecosystem 深层缺陷（Multi-region/Tenant/Marketplace/SLA） | P2 | `certification/index.ts:3-14` | 认证仅 pending/approved/revoked——缺 SBOM scan/sandbox cert/egress review(§55.1) | 已解决（源码/测试） | 根因与 `R12-44` 同源：认证模型只覆盖最小状态机，没有把 SBOM、sandbox、egress review 证据纳入统一认证记录。 | 本轮 `src/scale-ecosystem/marketplace/certification/index.ts` 已把 `sbomVerified/sandboxVerified/egressPolicyReviewed` 与 `signatureVerified/compatibilityVerified` 一并纳入认证 record/evidence，并由 `CertificationGate` 统一校验；`tests/unit/scale-ecosystem/marketplace/certification/certification.test.ts` 已验证证据不全时 fail-close。 | 关闭这条认证状态面过窄的缺口。 |
| R15-71 | §76 Scale-Ecosystem 深层缺陷（Multi-region/Tenant/Marketplace/SLA） | P2 | `marketplace-governance-service.ts:482-579` | publishPackage() 不检查 SBOM/sandbox cert/egress policy(§55.1 Quality & Security Gate) | 已解决（源码/测试） | 根因是治理服务发布链只看 review/signature，缺少 SBOM / sandbox / egress 合规门，因此包可以在基础审查通过后直接进入 marketplace。 | 本轮在 `src/scale-ecosystem/marketplace/marketplace-governance-service.ts` 新增 `assertPublicationCertificationGates()`，发布前强制检查 `sbomVerified/sandboxCertVerified/egressPolicyCompliant`；同时把这三项字段下沉到 SQLite `extension_packages` 持久化与 repository 读写链。`tests/unit/scale-ecosystem/marketplace/marketplace-governance-service.test.ts` 已新增 `blocks publish when certification gates are incomplete`，验证 gate 不全时会抛 `marketplace.certification_gate_failed`。 | 关闭该条 publishPackage 缺失 Quality & Security Gate 的缺口。 |
| R15-72 | §76 Scale-Ecosystem 深层缺陷（Multi-region/Tenant/Marketplace/SLA） | P2 | `sla-operations-service.ts:58-128` | 无 SLA-aware 延迟预测/自动扩缩——§54.3 要求预判+自动 scaling/preemption | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。 | 不宣称已修复。 |
| R15-73 | §77 Config/Channel Gateway/Bootstrap/Org-Governance 深层缺陷 | P0 | `config-rollout-service.ts:134` | startRollout() 对 target=100 找到 FULL stage 直接跳过 canary——§24.3 canary pipeline 完全失效 | 已解决 | 根因是旧实现按“第一个 percentage >= target”选起始 stage，`targetPercentage=100` 时直接落到 `FULL`，绕过了 5%/25%/50% 渐进发布链。 | 当前 `src/platform/five-plane-control-plane/config-center/config-rollout-service.ts` 已改为通过 `resolveInitialStage()` 固定从首个非 `FULL` 的渐进阶段起步；`tests/unit/platform/control-plane/config-center/config-rollout-service.test.ts` 新增 `startRollout begins 100 percent rollouts at canary instead of skipping directly to full`，明确验证 `target=100` 时起始 phase 仍是 `CANARY_5`。 | 关闭“100% 目标直接跳 full、canary 管线失效”的真实缺口。 |
| R15-74 | §77 Config/Channel Gateway/Bootstrap/Org-Governance 深层缺陷 | P0 | `config-center/` | ConfigImpactAnalyzer(§24.4 pre-publish 影响分析门)完全缺失——无文件/类型/引用 | 已解决（原审计结论已失效） | 根因是复审结论停留在较早版本，没有复核后续已落地的 impact analyzer 模块、类型定义和定向测试，因此把“实现存在但未复核”误记成“完全缺失”。 | 当前 `src/platform/five-plane-control-plane/config-center/config-impact-analyzer.ts` 已实现 `ConfigImpactAnalyzer`、`ConfigImpactAnalysis`、`RolloutStrategy` 等 §24.4 所需结构；`tests/unit/platform/control-plane/config-center/config-impact-analyzer.test.ts` 已覆盖安全/性能/可用性变更、blast radius、rollback recommendation 与 rollout strategy。 | 关闭的是“impact analysis gate 完全不存在”这条指控；是否所有调用链都已强制接门仍由其他未关闭条目单独跟踪。 |
| R15-75 | §77 Config/Channel Gateway/Bootstrap/Org-Governance 深层缺陷 | P0 | `config-center/` | §24.1 四种 config 生命周期类型(admission_locked/checkpoint_revalidated/hot_reloadable/emergency_override)无实现无类型 | 已解决（原审计结论已失效） | 根因是原审计没有复核现有 lifecycle 类型与 manager，导致把“未接入到所有调用方”误写成“无实现无类型”。 | 当前 `src/platform/five-plane-control-plane/config-center/config-lifecycle-types.ts` 已定义四种 `ConfigLifecycleState` 与 `ConfigLifecycleManager`，并实现 admission approval、checkpoint revalidation、hot reload、emergency override 的状态记录与转移；`tests/unit/platform/control-plane/config-center/config-lifecycle-types.test.ts` 已验证四类状态创建、状态迁移、过期 emergency override 阻断和可用性判定。 | 关闭的是“四类生命周期完全不存在”的指控，不外推为 lifecycle 已渗透所有 config 调用面。 |
| R15-76 | §77 Config/Channel Gateway/Bootstrap/Org-Governance 深层缺陷 | P1 | `config-rollout-service.ts` | Rollout 无健康门(max_error_rate/max_latency_regression/max_incident_rate)——纯按时间推进(§24.3) | 已解决 | 根因是 `autoProgressRollouts()` 之前只看时间/阶段推进，没有把 error rate、latency regression、incident rate 作为 promotion gate 输入，所以 rollout 会“按时晋级”而不是“按健康晋级”。 | 当前 `src/platform/five-plane-control-plane/config-center/config-rollout-service.ts` 已新增 `healthThresholds`、`RolloutHealthSnapshot`、`passesHealthGate()`，`autoProgressRollouts(healthSnapshots)` 会在 `maxErrorRate/maxLatencyRegression/maxIncidentRate` 任一超阈时阻断晋级；`tests/unit/platform/control-plane/config-center/config-rollout-service.test.ts` 已新增 `autoProgressRollouts respects health gates before promoting rollout` 并在本轮定向复跑通过。 | 关闭的是“rollout 完全没有健康门”的缺口；active rollout 的持久化问题仍由 `R15-79` 单独跟踪。 |
| R15-77 | §77 Config/Channel Gateway/Bootstrap/Org-Governance 深层缺陷 | P1 | `config-drift-reconciler.ts` | Drift reconciler 不发 config.drift_detected incident——无 EventBus 依赖，纯被动报告(§24.2) | 已解决 | 根因是 `ConfigDriftReconciler` 之前只返回 `ConfigDriftReport`，没有任何 incident/event bus 接线，所以 drift 只能被调用方被动轮询消费。 | 当前 `src/platform/five-plane-control-plane/config-center/config-drift-reconciler.ts` 已加入 `eventBus`/`emitIncidents` 选项，并在发现 drift 时发布 `config.drift_detected`；同时 `src/platform/five-plane-state-evidence/events/event-registry.ts` 已注册 `config.changed`、`config.rollout.*`、`config.drift_detected` 事件类型，避免 durable bus 在运行时因 schema 缺失再次失效。新增 `tests/unit/platform/control-plane/config-center/config-drift-reconciler.test.ts` 与 `tests/unit/platform/state-evidence/events/config-center-event-registry.test.ts` 覆盖 drift 触发与事件注册。 | 关闭“drift reconciler 完全没有 incident 发射能力”的真实缺口。 |
| R15-78 | §77 Config/Channel Gateway/Bootstrap/Org-Governance 深层缺陷 | P1 | `config-versioning-service.ts:115` | 版本快照存内存 Map——重启丢失全部版本历史(§24.2 "保留完整历史") | 已解决（补定向重放证据） | 根因是复审条目只盯住 service 内部 cache/Map，没有把当前实现基于事件重放重建版本历史与 rollback 点的机制纳入结论。 | 本轮新增 `tests/unit/platform/control-plane/config-center/config-center-durability-regressions.test.ts`，直接验证 `ConfigVersioningService` 在“重启后新实例 + replayable event bus”场景下仍能重建 version history 与 rollback points；同文件也验证了 `ConfigAuditService` 的 durable replay。定向命令 `AA_RUNNING_TESTS=1 node --import tsx --test tests/unit/platform/control-plane/config-center/config-center-durability-regressions.test.ts` 已通过。 | 关闭的是“历史只在进程内存里”这一指控，不外推为 config-center 所有深层条目都已完成。 |
| R15-79 | §77 Config/Channel Gateway/Bootstrap/Org-Governance 深层缺陷 | P1 | `config-rollout-service.ts:105` | 活跃 rollout 存内存 Map——重启后 canary@25% 静默变为 fully-applied | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。 | 不宣称已修复。 |
| R15-80 | §77 Config/Channel Gateway/Bootstrap/Org-Governance 深层缺陷 | P1 | `websocket-bridge.ts` | WS 无 message ack/delivery guarantee/sequence number——与 SSE StreamBridge(有 sequence+replay)不对称(§6.7 at-least-once) | 已解决 | 根因是旧 `WebSocketBridge` 只做基础广播，没有把 event 序号、ack、gap 通知和慢消费者回压建成协议面，导致 WS 路径缺少最小交付语义。 | 当前 `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts` 已补 `ack` 消息、`task_update.eventId/sequenceNum`、`stream_gap`、`backpressure_warning` 与慢消费者跟踪；`tests/unit/platform/interface/channel-gateway/websocket-bridge.test.ts` 和 `tests/unit/platform/interface/channel-gateway/websocket-bridge-coverage.test.ts` 已覆盖 ack、sequence、gap、backpressure、broadcast 与 close 行为，并在本轮定向复跑全部通过。 | 关闭的是“WS 完全缺少 ack/sequence/backpressure 协议面”的缺口，不外推为已经具备 SSE 等价的完整 replay 历史存储。 |
| R15-81 | §77 Config/Channel Gateway/Bootstrap/Org-Governance 深层缺陷 | P1 | `websocket-bridge.ts:99` | JWT 作为 URL query ?token=——暴露于 access log/proxy log/Referer header(§11 安全) | 已解决 | 根因是旧实现从 URL query 读取 token，认证凭据会自然泄漏到代理日志和访问日志。 | 当前 `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts` 只接受 `Sec-WebSocket-Protocol` 中的 bearer token，不再消费 `?token=` query；`tests/unit/platform/interface/channel-gateway/websocket-bridge-coverage.test.ts` 已直接验证“query string JWT 被忽略并拒绝连接”，本轮定向复跑通过。 | 关闭的是“query token 暴露”这条安全缺口；上层 token 签发与轮换策略仍由其他安全条目单独跟踪。 |
| R15-82 | §77 Config/Channel Gateway/Bootstrap/Org-Governance 深层缺陷 | P1 | `org-governance-saga.ts:17-39` | execute() 仅分类 steps 并返回——无 prepare/commit/compensate 执行(已知 stub 但无法接线) | 已解决（原审计结论已失效） | 根因是原审计停留在更早的 stub 认知，没有复核当前 saga 实现里已经存在的 prepare/commit/compensate/audit handler 调用链与失败补偿路径。 | 当前 `src/org-governance/org-model/org-governance-saga.ts` 的 `execute()` 会顺序执行 prepare、commit，在失败时回放 compensate，并在末尾执行 audit；现有 `tests/unit/org-governance/org-model/org-governance-saga.test.ts` 已验证 handler 调用顺序，以及 commit 失败后对 prepared/committed node 的逆序补偿。 | 关闭“saga 只分类步骤、不执行事务动作”的过期指控。 |
| R15-83 | §77 Config/Channel Gateway/Bootstrap/Org-Governance 深层缺陷 | P1 | `config/runtime/default.json` | 缺 circuit_breaker/retry_max/rate_limit 默认值——§24.1 要求平台级默认 | 已解决 | 根因是 runtime 默认配置只保留了 `maxConcurrentTasks/defaultTaskTimeoutMs` 等基础键，没有把平台级弹性控制默认值一起下沉到 versioned defaults。 | 当前 `config/runtime/default.json` 已补齐 `retryMax`、`circuitBreaker`、`rateLimit`；`tests/unit/platform/control-plane/config-center/default-config-shape.test.ts` 会校验 runtime/gateway 默认配置形状，避免再次回退成缺字段状态。 | 关闭“runtime 默认配置缺少 resilience/rate-limit 平台默认值”的真实缺口。 |
| R15-84 | §77 Config/Channel Gateway/Bootstrap/Org-Governance 深层缺陷 | P2 | `config-store.ts:116` | Change listener 用 !== 引用比较——deep-equal 对象变更不触发(事件契约违反) | 已解决 | 根因是旧实现只做引用不等比较，相同结构的新对象会被误判为“发生变化”，而深层实际变化如果上层复用了引用也可能绕过契约语义。 | 当前 `src/platform/five-plane-control-plane/config-center/config-store.ts` 已改为使用稳定序列化后的结构相等比较；`tests/unit/platform/control-plane/config-center/config-store.test.ts` 新增 `ignores structurally-equal object updates` 与 `detects deep object changes`，分别验证不会误报、也不会漏报。 | 关闭 change listener 违反结构语义的真实缺口。 |
| R15-85 | §77 Config/Channel Gateway/Bootstrap/Org-Governance 深层缺陷 | P2 | `hierarchical-config-loader.ts:243-251` | 版本 hash 用 djb2(32-bit 高碰撞) vs config-versioning-service 用 SHA-256——两套不一致 | 已解决 | 根因是层级配置加载器原先自己维护了一套轻量 32-bit hash，和 config versioning 侧的 SHA-256 版本标识并不一致，存在碰撞与跨模块版本漂移风险。 | 当前 `src/platform/five-plane-control-plane/config-center/hierarchical-config-loader.ts` 已统一改为 `sha256(stableStringify(config))`；`tests/unit/platform/control-plane/config-center/hierarchical-config-loader.test.ts` 新增 `computes stable SHA-256 version regardless of object key order`，验证同内容不同 key 顺序得到稳定版本。 | 关闭“层级 loader 与 versioning service 使用两套版本 hash”的真实缺口。 |
| R15-86 | §77 Config/Channel Gateway/Bootstrap/Org-Governance 深层缺陷 | P2 | `src/index.ts:178-243` | buildPlatformRootSummary() 顺序调用无 error boundary——单 plan 失败 crash 整体(§9 稳定性) | 已解决 | 根因是 root summary builder 之前把所有 section builder 串行裸调用，任何一个 startup plan/runtime catalog 抛错都会把整个根入口 summary 一起打崩。 | 当前 `src/index.ts` 已引入 `safeBuildSection()`，为 architecture/domains/planes/aiOperations/interactionGovernance/scaleOps 各 section 提供独立 fallback；`tests/unit/platform/root-entry-review-regressions.test.ts` 新增 `buildPlatformRootSummary keeps other sections available when one builder fails`，验证单 section 异常不会拖垮整体 summary。 | 关闭“单 builder 失败导致根 summary 全局 crash”的稳定性缺口。 |
| R15-87 | §77 Config/Channel Gateway/Bootstrap/Org-Governance 深层缺陷 | P2 | `tests/e2e/` | 无 config-rollout/config-drift/config-governance e2e 测试 | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。 | 不宣称已修复。 |
| R15-88 | §77 Config/Channel Gateway/Bootstrap/Org-Governance 深层缺陷 | P2 | `config/gateways/default.json` | 无 WS 配置(max connections/heartbeat/message size)——WS bridge 已实现但无配置(§8) | 已解决 | 根因是 gateway defaults 只覆盖 SSE/timeout/circuit breaker/request limits，漏掉了已存在 websocket bridge 所需的连接数、心跳和消息尺寸预算。 | 当前 `config/gateways/default.json` 已补齐 `websocket.enabled/maxConnections/heartbeatIntervalMs/maxMessageBytes`；`tests/unit/platform/control-plane/config-center/default-config-shape.test.ts` 已同步校验 gateway 默认配置包含 websocket 基线。 | 关闭“WS bridge 已实现但默认配置无预算与开关”的真实缺口。 |
| R16-01 | §79 Model Gateway 深层缺陷 | P0 | `circuit-breaker.ts:277` | getRecentFailureRate() 计算 (failures/windowSeconds)\*10 非真实失败率(failures/total)——无 total request 计数，§15.4 失败率阈值永远无法正确触发 | 已解决 | 根因是旧实现把“单位时间失败数”误当成“失败率”，既没有请求总数分母，也无法给 breaker metrics 暴露真实 rate。 | 当前 `src/platform/model-gateway/provider-registry/circuit-breaker.ts` 已新增 `requestTimestamps`、`totalRequests`、`recentFailureRate`，并把 `getRecentFailureRate()` 改为监控窗口内 `recentFailures / recentRequests`；`tests/unit/platform/model-gateway/provider-registry/circuit-breaker.test.ts` 与 `tests/unit/platform/model-gateway/budget-routing-regressions.test.ts` 已分别验证真实 failure-rate 开闸和 `recentFailureRate` 指标落盘，本轮定向复跑通过。 | 关闭的是“失败率公式错误”缺口；样本门槛 `minSampleSize` 仍属 breaker 策略调参，不影响本条收口。 |
| R16-02 | §79 Model Gateway 深层缺陷 | P0 | `degradation-controller.ts:357-359` | getFallbackCandidates() 始终返回[]——D1 fallback 路径为死代码，任何 D0 失败直接递归升级 | 已解决 | 根因是旧实现没有从 provider 暴露可用 profile 集，`D1` 只能拿到空候选集合，因此 fallback 路径形同死代码。 | 当前 `src/platform/model-gateway/provider-registry/unified-chat-provider.ts` 已提供 `getAvailableProfiles()`；`src/platform/model-gateway/degradation/degradation-controller.ts` 的 `getFallbackCandidates()` 会聚合 primary/fallback provider 的 profiles 并按 `profileName` 去重，`D1` 可真实选择 fallback 候选。 | `tests/unit/platform/model-gateway/degradation/degradation-controller.test.ts` 已覆盖 D1 fallback 路径，本轮定向复跑通过；递归深度问题另由 `R16-10` 跟踪。 |
| R16-03 | §79 Model Gateway 深层缺陷 | P0 | `llm-eval-service.ts:356-358` | runAbTest 使用硬编码假分数(control=0.85/treatment=0.90)——A/B 测试结果完全伪造 | 已解决 | 根因是旧 `runAbTest()` 直接塞入常量分数，既不调用 evaluator，也不产出真实样本比较或显著性指标。 | 当前 `src/platform/prompt-engine/eval/llm-eval-service.ts` 的 `runAbTest()` 已改为 evaluator 驱动执行，按 case 收集 control/treatment 实际得分，并返回 `pValue`；仅在未提供 evaluator 时才走确定性 fallback，而不再写死 `0.85/0.90`。 | `tests/unit/platform/prompt-engine/llm-eval-ab-test-regressions.test.ts` 已直接断言 A/B 测试使用 evaluator 提供的真实输出而非硬编码占位分，本轮定向复跑通过。 |
| R16-04 | §79 Model Gateway 深层缺陷 | P0 | `rollout/index.ts:7` | Prompt rollout 状态机与 stage 常量不一致，非 canonical 阶段会造成 rollout contract 漂移 | 已解决 | 根因是旧 prompt rollout 同时维护了一套非 canonical 的状态/阶段枚举，`PromptRolloutStatus`、`PROMPT_ROLLOUT_STAGES` 和激活/回滚流转并不一致。 | 当前 `src/platform/prompt-engine/rollout/index.ts` 已统一为 `canary_5/canary_20/stable/blocked/rolled_back`，并为 record 增加 `statusEnteredAt` 与 metrics 评估；`src/platform/prompt-engine/rollout/prompt-rollout-stage.ts` 已把阶段常量收敛到 canonical progression，并补 `passesQualityGate()`。 | `tests/unit/platform/prompt-engine/rollout/prompt-rollout-stage-audit-regressions.test.ts` 已验证 canonical `canary_5 -> canary_20 -> stable` 流程和 terminal stage 语义，本轮定向复跑通过。 |
| R16-05 | §79 Model Gateway 深层缺陷 | P1 | `circuit-breaker.ts:270-278` | 监控窗口仅存 failure timestamps 无 total requests——无法计算真实 failure rate | 已解决 | 根因与 `R16-01` 同源：监控窗口之前只保留失败时间戳，没有请求时间戳，导致无法得到真实分母。 | 当前 `src/platform/model-gateway/provider-registry/circuit-breaker.ts` 已同时维护 `failureTimestamps` 与 `requestTimestamps`，并在 `getMetrics()` 暴露 `totalRequests/recentFailureRate`；窗口裁剪也会同步清理请求与失败时间戳。 | `tests/unit/platform/model-gateway/provider-registry/circuit-breaker.test.ts` 已覆盖窗口内失败率计算，本轮定向复跑通过。该编号在原始 round 文档中重复出现，后续重复挂账沿用本结论。 |
| R16-06 | §79 Model Gateway 深层缺陷 | P1 | `unified-chat-provider.ts:297-336` | createStreamingChatCompletion 无 circuit breaker 保护——streaming 绕过容错 | 已解决（源码复核并补定向测试） | 根因是原审计停留在 streaming 分支尚未统一接入 breaker 的旧实现；当前 streaming path 已与非 streaming path 一样进入 provider breaker 执行链。 | 当前 `src/platform/model-gateway/provider-registry/unified-chat-provider.ts` 的 `createStreamingChatCompletion()` 会构造 `runStreamingWithBreaker()`，并在存在 breaker 时通过 `breaker.execute(runStreamingWithBreaker)` 执行。 | `tests/unit/platform/model-gateway/provider-registry/unified-chat-provider.test.ts` 已有 `UnifiedChatProvider streaming chat uses circuit breaker when non-streaming does` 专门断言；本轮复核该子测试通过，虽同文件仍有无关旧失败，但不影响这条 streaming breaker 证据成立。 |
| R16-07 | §79 Model Gateway 深层缺陷 | P1 | `unified-chat-provider.ts:40-55` | ChatCompletionResult 缺 requestId/estimatedCost/latencyMs(§15.2) | 已解决 | 根因是统一 provider 结果类型之前只保留最小 completion 字段，缺少调用链追踪、成本核算和延迟证据面。 | 当前 `src/platform/model-gateway/provider-registry/unified-chat-provider.ts` 的 `ChatCompletionResult` 已补 `requestId/estimatedCostUsd/latencyMs`，并在 Anthropic/OpenAI/MiniMax 归一化阶段统一填充；`tests/unit/platform/model-gateway/provider-registry/unified-chat-provider-types.test.ts` 与 `tests/unit/platform/model-gateway/provider-registry/provider-registry-coverage.test.ts` 已同步校验结果结构。 | 关闭的是结果 contract 缺口；更高层的 usage/telemetry 消费链是否全部使用这些字段，不在本条范围内。 |
| R16-08 | §79 Model Gateway 深层缺陷 | P1 | `unified-chat-provider.ts:57-67` | ChatCompletionRequest 缺 timeout 参数(§15.2) | 已解决 | 根因是统一 provider 请求对象只接受外部 `AbortSignal`，没有显式 `timeoutMs`，调用方无法声明式设置超时预算。 | 当前 `src/platform/model-gateway/provider-registry/unified-chat-provider.ts` 的 `ChatCompletionRequest` 与 `CompletionOptions` 已补 `timeoutMs`，`createChatCompletion()/createStreamingChatCompletion()` 会通过 `buildRuntimeSignal()` 把 timeout 与外部 abort 组合成运行时信号。 | `tests/unit/platform/model-gateway/provider-registry/unified-chat-provider-types.test.ts` 已同步校验请求 contract，本轮相关 provider-registry 定向回归全部通过。 |
| R16-09 | §79 Model Gateway 深层缺陷 | P1 | `model-routing-service.ts:54-77` | ModelRouteRequest 缺 data_residency/pii_input_detected/model_training_opt_out(§15.3) | 已解决 | 根因是旧路由请求 contract 只包含成本/风险等级，没有把 residency、PII、training opt-out、judge independence 与 latency SLO 这些治理约束输入路由决策。 | 当前 `src/platform/model-gateway/provider-registry/model-routing-service.ts` 已把 `data_residency/pii_input_detected/model_training_opt_out/judge_independence/latency_slo_target_ms` 纳入 `ModelRouteRequest`，并在候选筛选与 `persistRoutingDecision()` 证据中落地 residency、PII-safe、training opt-out、judge independence、latency 信息。 | `tests/unit/platform/model-gateway/provider-registry/model-routing-service-regressions.test.ts` 已验证这些约束会真实影响路由与持久化证据；`tests/unit/platform/model-gateway/provider-registry/model-routing-service-types.test.ts` 已同步校验类型面，本轮定向复跑通过。 |
| R16-10 | §79 Model Gateway 深层缺陷 | P1 | `degradation-controller.ts:209-241` | routeD0 递归升级无重试计数限制——D2/D3 也失败时可 stack overflow | 已解决 | 根因是旧降级路由通过递归在 `D0 -> D1 -> D2...` 间升级，一旦多级失败叠加就可能无限自调用。 | 当前 `src/platform/model-gateway/degradation/degradation-controller.ts` 已改为带 `maxAttempts` 的迭代升级循环，`routeD0/routeD1` 不再递归重入 `route()`；同时保留真实首个 escalation reason，避免被后续阶段覆盖。 | `tests/unit/platform/model-gateway/degradation/degradation-controller-recursion-guard.test.ts` 与 `tests/unit/platform/model-gateway/degradation/degradation-controller.test.ts` 已覆盖递归深度隔离与 escalation reason 行为，本轮定向复跑通过。 |
| R16-11 | §79 Model Gateway 深层缺陷 | P1 | `budget-guard.ts:12-18` | BudgetPolicy 仅有 USD 上限——§18.3 要求 max_model_tokens/max_steps/max_duration_ms 独立限制 | 已解决 | 根因是旧预算策略只表达美元上限，缺少 tokens、steps、duration 这些正交预算面，也没有执行期 session 生命周期去承接预留/执行/结算。 | 当前 `src/platform/model-gateway/cost-tracker/budget-guard.ts` 已为 `BudgetPolicy` 增加 `maxPackCostUsd/maxPlatformCostUsd/maxModelTokens/maxSteps/maxDurationMs`，并新增 `BudgetExecutionSession` / `BudgetExecutionSessionManager` 以承接 reserve-execute-settle 生命周期。 | `tests/unit/platform/model-gateway/budget-routing-regressions.test.ts` 已验证执行期 session 生命周期与预算会话行为，本轮定向复跑通过。 |
| R16-12 | §79 Model Gateway 深层缺陷 | P1 | `prompt-rollout-stage.ts:1-12` | PROMPT_ROLLOUT_STAGES(shadow/canary_5/partial_25...) 与 PromptRolloutStatus 类型系统不一致 | 已解决（源码复核并补定向测试） | 根因是旧 rollout stage 集合曾保留 `shadow/canary_5/partial_25/50/75` 这组非 canonical 枚举，导致 stage 常量与类型系统、contract 口径不一致。 | 当前 `src/platform/prompt-engine/rollout/prompt-rollout-stage.ts` 的 `PROMPT_ROLLOUT_STAGES` 已收敛为 `["canary(5%)", "canary(20%)", "stable", "rolled_back"]`，并由 `PromptRolloutStage` 直接从该常量派生类型。 | `tests/unit/platform/prompt-engine/rollout/prompt-rollout-stage-audit-regressions.test.ts` 的 `PROMPT_ROLLOUT_STAGES uses the canonical canary(5%)/canary(20%) progression` 已在本轮定向复跑通过。 |
| R16-13 | §79 Model Gateway 深层缺陷 | P1 | `rollout/index.ts:65-76` | activateRollout 仅允许 ready→active——无 canary 流量分割阶段(§16.3) | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 不宣称已修复。 |
| R16-14 | §79 Model Gateway 深层缺陷 | P1 | `platform-prompt-release-orchestration-service.ts:53-99` | 无 domain_owner_approval/rollback_plan_present 发布门(§17.3) | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 不宣称已修复。 |
| R16-15 | §79 Model Gateway 深层缺陷 | P1 | `hierarchical-registry-service.ts:238-270` | resolveBundleForTraffic hash % 100 无 RunVersionLock——同一 run 可能用不同 PromptBundle | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 不宣称已修复。 |
| R16-16 | §79 Model Gateway 深层缺陷 | P1 | `llm-eval-service.ts:307-311` | completeRun 使用 80% pass 判定 degraded——§17.3 要求 ≥95% + critical 100% | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 不宣称已修复。 |
| R16-17 | §79 Model Gateway 深层缺陷 | P1 | `llm-eval-service.ts:348-378` | A/B test 无 judge independence(§17.5 要求不同 model/family/provider) | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 不宣称已修复。 |
| R16-18 | §79 Model Gateway 深层缺陷 | P1 | `execution-outcome-evaluator.ts:39-61` | defaultPassThreshold=0.5 但 §17.3 要求 delta-based(quality_score_delta≥-0.05)——质量模型根本不同 | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 不宣称已修复。 |
| R16-19 | §79 Model Gateway 深层缺陷 | P1 | `prompt-injection-guard.ts:306-314` | blocked=true 时 hard deny——§16.5.2 禁止注入分类器单独作 production hard deny | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 不宣称已修复。 |
| R16-20 | §79 Model Gateway 深层缺陷 | P2 | `unified-chat-provider.ts:127-149` | detectProviderFromModel 未知模型静默默认 "openai"——应 throw/warn | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 不宣称已修复。 |
| R16-21 | §79 Model Gateway 深层缺陷 | P2 | `unified-chat-provider.ts:289` | recordLlmLatency TTFT=totalSeconds(未实际测量)——§15.6 TTFT>10s 触发永远不火 | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 不宣称已修复。 |
| R16-22 | §79 Model Gateway 深层缺陷 | P2 | `degradation-controller.ts:97-103` | escalateLatencyP99Ms=5000 但 §15.6 TTFT 阈值=10s——不区分 P99 与 TTFT | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 不宣称已修复。 |
| R16-23 | §79 Model Gateway 深层缺陷 | P2 | `deterministic-hot-path-gate.ts:26-33` | allowed:true + routeMode:"deterministic_hot_path_only" 矛盾 | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 不宣称已修复。 |
| R16-24 | §79 Model Gateway 深层缺陷 | P2 | `fallback/index.ts:16-38` | selectFallback 仅按 cost 排序——忽略 tier affinity(§15.4) | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 不宣称已修复。 |
| R16-25 | §79 Model Gateway 深层缺陷 | P2 | `prompt-injection-guard.ts:317-330` | 任何 URL 触发 blocked——raw_url_exfiltration 对含合法 URL 输出过度攻击 | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 不宣称已修复。 |
| R16-26 | §80 State-Evidence 平面深层缺陷 | P0 | `transactional-event-appender.ts:97-121` | 使用原始 BEGIN/COMMIT/ROLLBACK 绕过 db.transaction()——并发调用可交错事务导致静默腐败(§25.3) | 已解决（源码/测试复核） | 原审计结论已过期：`TransactionalEventAppender` 现行 `appendEvent()` / `appendEvents()` 都统一通过 `db.transaction(...)` 包装执行，不再手写 `BEGIN/COMMIT/ROLLBACK`，并且可以把 truth mutation 一起包进同一事务闭包。 | `src/platform/state-evidence/events/transactional-event-appender.ts` 已在 `appendEvent()` / `appendEvents()` 中使用 `this.db.transaction(() => ...)`；`tests/unit/platform/state-evidence/events/transactional-event-appender.test.ts` 本轮已定向通过 transaction wrapper、atomic with outbox、truth mutation 原子提交与回滚等回归。 | 该编号在原始 round 文档中重复出现 2 次；现按现行源码与定向测试统一关闭。 |
| R16-27 | §80 State-Evidence 平面深层缺陷 | P0 | `durable-event-bus.ts:178-195` | publish() 写 event+ack 但不共写 truth table——§25.2 要求 truth+event log 原子同事务 | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 不宣称已修复。 |
| R16-28 | §80 State-Evidence 平面深层缺陷 | P0 | `projection-rebuild-service.ts:217` | rebuildProjection() 对每个 event 传 null state——不累积状态，rebuild 产出空/单事件投影 | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 不宣称已修复。 |
| R16-29 | §80 State-Evidence 平面深层缺陷 | P1 | `event-registry.ts vs event-types.ts` | event-types.ts 定义 26 个 Tier-1 事件但 RAW_EVENT_SCHEMA_REGISTRY 不含——getEventSchema() 运行时 throw | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 不宣称已修复。 |
| R16-30 | §80 State-Evidence 平面深层缺陷 | P1 | `event-registry.ts:580-653 vs :63-371` | RUNTIME_EVENT_REPLAY_METADATA(platform.\*)与 RAW_EVENT_SCHEMA_REGISTRY(colon-delimited)命名不一致无互引——§28.2 EventEnvelope 字段缺失 | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 不宣称已修复。 |
| R16-31 | §80 State-Evidence 平面深层缺陷 | P1 | `workflow-step-checkpoint.ts` | Checkpoint 用 readFileSync 纯文件 IO——无原子写/WAL/fencing token；crash mid-write 腐败(§25.6 <20ms P99) | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 不宣称已修复。 |
| R16-32 | §80 State-Evidence 平面深层缺陷 | P1 | `projection-rebuild-service.ts:244-253` | rebuildAll() 16个投影顺序扫描 offset 0——无 shadow-build→compare→cutover(§28.6) | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 不宣称已修复。 |
| R16-33 | §80 State-Evidence 平面深层缺陷 | P1 | `semantic-knowledge-graph.ts:219-243` | collectAdjacent() BFS 每次 pop 遍历全部 edges——O(V×E) 不可用于千级 chunk 图(§29.1) | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 不宣称已修复。 |
| R16-34 | §80 State-Evidence 平面深层缺陷 | P1 | `semantic-knowledge-graph.ts` | §29.1 要求 Trust Level 层级(private_unverified→team_reviewed→official→authoritative)+contested 降级——图节点无 trust level | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 不宣称已修复。 |
| R16-35 | §80 State-Evidence 平面深层缺陷 | P1 | `cas/cas-service.ts:46` | CAS 纯内存 Map——§25.3 要求 CAS+lease+fencing 用于所有 truth write；无持久化无跨进程保护 | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 不宣称已修复。 |
| R16-36 | §80 State-Evidence 平面深层缺陷 | P2 | `durable-event-bus.ts:48` | ACTIVE_SUBSCRIBER_POLL_INTERVAL_MS=10(100次/秒/consumer)——不必要 CPU 烧耗 | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 不宣称已修复。 |
| R16-37 | §80 State-Evidence 平面深层缺陷 | P2 | `dlq-service.ts:112` | DLQ 纯内存 Map——§28.8 要求持久 DLQ 含 first/last_failed_at+incident linking；重启丢失 | 已修复（源码修复并补定向测试） | 根因是缺口分成了两层：一层是默认构造还能回退到内存仓导致重启丢数；另一层是扩展 DLQ 记录虽已持久化基础字段，但缺少 `first/last_failed_at` 与 incident linkage 这类 operator contract 字段。 | 本轮在 `src/platform/five-plane-state-evidence/events/dlq-service.ts` / `sqlite-dlq-repository.ts` 中补齐 `firstFailedAt`、`lastFailedAt`、`linkedIncidentId` 持久字段与 `linkIncident()`，并新增 SQLite migration `0053_dlq_incident_linking`；同时把默认构造改成生产态必须显式注入持久仓。`tests/integration/platform/state-evidence/events/dlq-persistence.integration.test.ts` 已验证 dead-letter 在数据库重开后仍保留失败时间戳与 incident link。 | 这是把“能持久化”补成“默认持久化且字段契约完整”的实修。 |
| R16-38 | §80 State-Evidence 平面深层缺陷 | P2 | `memory-layer-model.ts:46-51` | Promotion rules 跳过 runtime→session——working layer supportsPromotion=true 但无对应规则 | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 不宣称已修复。 |
| R16-39 | §80 State-Evidence 平面深层缺陷 | P2 | `memory-layer-model.ts:549-554` | working facts shouldEvict() 无 loss-report/escalation——§29.2 禁止静默丢弃 | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 不宣称已修复。 |
| R16-40 | §80 State-Evidence 平面深层缺陷 | P2 | `layered-event-inbox.ts vs durable-event-bus.ts` | 两套独立 event inbox 无共享状态/consumer registry/统一 replay——双轨并行 | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 不宣称已修复。 |
| R16-41 | §80 State-Evidence 平面深层缺陷 | P2 | `semantic-knowledge-graph.ts:260-272` | addEdge() 无去重——重复 upsertRecord 产生重复 edge 膨胀结果 | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 不宣称已修复。 |
| R16-42 | §81 UI Shared 基础设施深层缺陷 | P0 | `sync/src/conflict-resolver.ts:15-26` | "merge" 策略为天真 shallow spread——无 CRDT/vector clock/版本比较(§5.4.5 要求 CRDT offline-first) | 已修复（源码/测试） | 原审计对“完全 shallow spread”的判断已经过时，当前实现早有递归对象合并和 Lamport/vector clock；但本轮复核确认仍有一个真实 CRDT 缺口：数组里同 `id` 的重叠对象虽然会去重，却始终保留 server 侧旧 payload，缺少按 item clock/Lamport 选择 newer value 的逻辑。 | 本轮在 `ui/packages/shared/sync/src/conflict-resolver.ts` 为数组冲突补上按 `id` 的 item-level merge，并在字段级 clock 缺失时回退到 Lamport timestamp 比较；`tests/unit/ui/shared/sync/conflict-resolver.test.ts` 已新增“overlapping array entries by id”回归并通过 `./node_modules/.bin/tsx --test` 定向验证。 | 关闭的是 shared conflict resolver 的真实合并语义缺口，不外推整个 UI 产品化大盘都已完成。 |
| R16-43 | §81 UI Shared 基础设施深层缺陷 | P0 | `sync/src/types.ts:1-9` | OfflineMutation 缺 idempotencyKey/retryCount/status(pending/syncing/conflict/failed)(§5.4.5) | 已解决（源码/测试） | 根因与 `R8-56` 同源：复审条目引用的是补字段之前的旧类型定义。 | 当前 `ui/packages/shared/sync/src/types.ts` 已定义 `idempotencyKey`、`retryCount` 以及 `pending/syncing/conflict/failed` 状态集合；`ui/tests/unit/ui/shared/offline-queue.test.ts` 已用完整 mutation 结构覆盖队列读写，本轮定向测试已通过。 | 作为重复挂账，随离线 mutation contract 收口一并关闭。 |
| R16-44 | §81 UI Shared 基础设施深层缺陷 | P0 | `sync/src/sync-coordinator.ts:33-38` | flush() 仅本地清空队列——无 HTTP replay/服务端提交/retry/conflict-detection(§5.4.5 recovery flow) | 已解决（源码复核并补回归） | 真实根因不是当前 `flush()` 仍只做本地清空，而是原审计结论停留在更早快照，没有复核后续已存在的 HTTP replay/冲突处理逻辑；同时缺少一条直接钉住 replay 行为的回归测试，导致旧结论长期未被证伪。 | 当前 `ui/packages/shared/sync/src/sync-coordinator.ts` 的 `flush()` 会逐条调用 `httpClient.request(...)`，对 `409` 标记 conflict、对 `429/5xx` 做 retry 计数与 requeue、最后通过 `queue.replaceAll(nextQueue)` 回写剩余队列，不是“仅本地清空”；本轮同时在 `ui/packages/shared/sync/src/types.ts` / `sync-coordinator.ts` 对齐 flush 契约，并新增 `tests/unit/ui/shared/sync/sync-coordinator-durability.test.ts` 覆盖 HTTP replay、retry 与 conflict replay 路径。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 关闭的是 `flush()` 缺 HTTP replay 的错误结论，不外推为 UI 全链路离线产品化已经完成。 |
| R16-45 | §81 UI Shared 基础设施深层缺陷 | P0 | `api-client/src/ws-client.ts:88` | WS token 作 URL query——泄漏于 server log/referrer/history(§5.3.3/§6.5) | 已解决（源码/测试） | 根因与 `R10-25` 同源：旧浏览器 WS 客户端把鉴权 token 暴露到 URL 级连接参数，重复挂账没有吸收协议改造。 | 当前 `ui/packages/shared/api-client/src/ws-client.ts` 已用 `new WebSocket(url, "v1.auth.token")` 建连并在 `onopen` 后发送首条 auth message；`ui/tests/shared/api-client-runtime-regressions.test.ts` 已验证 token 不会出现在 URL 中，定向测试已通过。 | 作为重复挂账，随 WS 鉴权协议修复一并关闭。 |
| R16-46 | §81 UI Shared 基础设施深层缺陷 | P0 | `api-client/src/interceptors.ts:29-37` | createAuthInterceptor 接受静态 string token——无动态 TokenManager/auto-refresh/refresh-lock(§5.4.4) | 已解决（源码修复并补定向测试） | 根因是 shared auth interceptor 之前为了兼容旧调用方保留了静态 string 分支，导致运行时仍能绕开 refresh-aware token resolver。 | 本轮修改 `ui/packages/shared/api-client/src/interceptors.ts`，`createAuthInterceptor()` 现在只接受动态 resolver/TokenManager，并在运行时对字符串输入 fail-closed 抛出 `auth.dynamic_token_required`；`ui/tests/unit/ui/shared/interceptors.test.ts` 已新增并通过“拒绝静态 bearer token 字符串”的回归，`ui/tests/shared/web-runtime-auth-regression.test.ts` 也验证了 web runtime 传入的是动态 `TokenManager`。 | 关闭的是静态 token 分支本身，不外推为完整 SSO/refresh-lock 体系全部完成。 |
| R16-47 | §81 UI Shared 基础设施深层缺陷 | P0 | `apps/web/src/runtime.ts:39` | Auth token 硬编码 "ui-runtime-access"——绕过整个 auth 流程 | 已解决（源码/测试） | 根因与 `R10-28/2175` 同源：旧 runtime 直接把伪 token 当成生产鉴权输入，重复挂账时没有吸收后续 `TokenManager` 改造。 | 当前 `ui/apps/web/src/runtime.ts` 已改为 `createAuthInterceptor(tokenManager)`，不再存在 `"ui-runtime-access"` 常量；本轮新增 `ui/tests/shared/web-runtime-auth-regression.test.ts`，验证 auth interceptor 吃到的是动态 `TokenManager` 而不是硬编码字符串，定向测试已通过。 | 作为重复挂账，随 web runtime 动态鉴权接线一并关闭。 |
| R16-48 | §81 UI Shared 基础设施深层缺陷 | P0 | `auth/src/auth-service.ts:36-40` | handleSsoCallback 从 URL query 读 access_token/refresh_token 明文——应用 authorization code flow(§5.4.4) | 已解决（源码/测试） | 根因与 `2069` 同源：旧复审结论停留在 URL token callback 时代，没有吸收后续 `handleSsoCallback()` 只触发 authorization code flow 的改造。 | 当前 `ui/packages/shared/auth/src/auth-service.ts` 的 `handleSsoCallback()` 已不读取 URL 中的 `access_token/refresh_token`，而是重定向到 authorization flow；`ui/tests/unit/ui/shared/auth-service.test.ts` 已验证即使 URL 带 token 参数也会进入重定向路径，定向测试已通过。 | 关闭的是“从 URL 明文消费 token”缺陷，不外推为完整 OAuth 服务端对接已完成。 |
| R16-49 | §81 UI Shared 基础设施深层缺陷 | P0 | `apps/web/src/app-shell.tsx:8-22` | Route guard 使用硬编码 demoGuardContext+静态 permissions——无真实 RBAC 集成 | 已解决（源码/测试） | 根因与 `2164` 同源：原始实现把 route guard 的输入上下文固定成 demo 管理员权限，复审表后续重复挂账时没有吸收动态 auth context 改造。 | 当前 `ui/apps/web/src/app-shell.tsx` 已从 `authContext` 动态生成 guard context，并把 tenant/roles/permissions 下沉到守卫评估；本轮新增 `ui/tests/shared/web-app-shell-auth-regression.test.tsx`，直接验证 guard context 使用真实 `authContext` 而非硬编码 demo 值，定向测试已通过。 | 该重复挂账按现行源码与窄测试关闭。 |
| R16-50 | §81 UI Shared 基础设施深层缺陷 | P1 | `state/src/query-client.ts:7-9` | 全局 staleTime=30s/retry=1——§5.1.2 要求 per-type staleTime(tasks=2min/approvals=30s/config=1h) | 已解决（源码/测试） | 根因不是现行 query client 仍然只会给所有查询套 30s staleTime，而是复审条目停留在 tiered cache 策略接入前的默认配置。 | 当前 `ui/packages/shared/state/src/query-client.ts` 已定义 `CACHE_TIER_STALE_TIME` 与 `createTieredQueryClientFactory()`，分别覆盖 `tasks=120_000`、`approvals=30_000`、`config=3_600_000`；本轮新增 `ui/tests/unit/ui/shared/state-store-middleware.test.ts`，直接验证 tasks/approvals/config 三档 staleTime。 | 关闭的是分层 staleTime 缺口；`retry=1` 仍作为默认重试策略保留，不构成该条继续挂账依据。 |
| R16-51 | §81 UI Shared 基础设施深层缺陷 | P1 | `state/src/stores/auth-store.ts:3-8` | AuthStore 仅 authenticated+locale——缺 user/token/permissions/tenantId/login/logout/refreshToken/switchTenant(§5.1.1) | 已解决（源码/测试） | 根因是审计引用了 AuthStore 还处于最小登录态模型时的旧版本；现行 store contract 早已扩展到完整身份、租户与 token 生命周期，只是文档没回写。 | 当前 `ui/packages/shared/state/src/stores/auth-store.ts` 已包含 `userId/tenantId/roles/permissions/accessToken/refreshToken` 以及 `login/logout/switchTenant/updateTokens`；`ui/tests/unit/ui/shared/state-store-middleware.test.ts` 已验证登录、token 更新和持久化 middleware 仍能正常工作。 | 不改业务语义，仅把现行 AuthStore contract 回写到复审文档。 |
| R16-52 | §81 UI Shared 基础设施深层缺陷 | P1 | `state/src/stores/ui-store.ts:3-8` | UIStore 仅 activeRoute+activeFeature——缺 theme/sidebarCollapsed/nlPanelOpen/commandPaletteOpen(§5.1.1) | 已解决（源码/测试） | 根因同样是复审基线落后于现行 store contract；UIStore 早已不止 route/feature 两个字段。 | 当前 `ui/packages/shared/state/src/stores/ui-store.ts` 已声明 `theme/sidebarCollapsed/nlPanelOpen/commandPaletteOpen` 及对应 setter/toggle；`ui/tests/unit/ui/shared/state-store-middleware.test.ts` 已验证 `setActiveFeature()`、`toggleSidebar()`、`setCommandPaletteOpen()` 的真实状态更新。 | 这是陈旧结论回写，不再保留为未解决。 |
| R16-53 | §81 UI Shared 基础设施深层缺陷 | P1 | `state/src/stores/sync-store.ts:3-9` | SyncStore 缺 online/conflicts/syncStatus:"error"/resolveConflict/retrySync(§5.1.1) | 已解决（源码/测试） | 根因也是旧版 SyncStore contract 被后续实现超过后，reaudit 表没有跟上。 | 当前 `ui/packages/shared/state/src/stores/sync-store.ts` 已包含 `online/conflicts/syncStatus`，并提供 `resolveConflict()/retrySync()`；`ui/tests/unit/ui/shared/state-store-middleware.test.ts` 已验证冲突入列、重试和冲突清理路径。 | 关闭的是 store contract 缺口，不外推为完整离线同步工作流都已产品化。 |
| R16-54 | §81 UI Shared 基础设施深层缺陷 | P1 | `state/src/stores/realtime-store.ts:3-12` | RealtimeStore 缺 activeSubscriptions/pendingApprovalCount/activeIncidents/subscribe/unsubscribe(§5.1.1) | 已解决（源码/测试） | 根因不是现行 RealtimeStore 仍然缺字段，而是 reaudit 表沿用了更早的最小 store 版本，后续补进来的订阅、待审批计数和 incident 状态没有被回写到复审结论。 | 当前 `ui/packages/shared/state/src/stores/realtime-store.ts` 已声明 `activeSubscriptions`、`pendingApprovalCount`、`activeIncidents` 及 `subscribe()/unsubscribe()/setPendingApprovalCount()/addActiveIncident()/removeActiveIncident()`；本轮新增 `ui/tests/unit/ui/shared/state-store-middleware.test.ts` 直接验证订阅集合、审批计数和 incident 增删行为。 | 这是文档基线漂移，不再继续作为 UI 主线缺口挂账。 |
| R16-55 | §81 UI Shared 基础设施深层缺陷 | P1 | `telemetry/src/index.ts:78-92` | OTLP exporter 格式错误——wraps 在 scopeMetrics[].logs 非 resourceLogs[].scopeLogs[]；collector 会拒绝 | 已解决（源码/测试） | 根因是复审命中的仍是早期 OTLP 日志 payload 结构；现行 exporter 已迁移到 collector 可接受的 `resourceLogs[].scopeLogs[]` 形态，但文档没有同步。 | 当前 `ui/packages/shared/telemetry/src/index.ts` 的 `OtlpHttpTelemetryExporter` 会输出 `resourceLogs[].scopeLogs[].logRecords`；`ui/tests/unit/ui/shared/telemetry.test.ts` 已断言 `capturedBody.resourceLogs` 存在且服务名属性位于该结构下。 | 不改生产代码，仅按现行 exporter 协议与测试证据关闭陈旧结论。 |
| R16-56 | §81 UI Shared 基础设施深层缺陷 | P1 | `telemetry/src/index.ts:27-30` | TelemetrySink.record() 逐事件 HTTP POST——无 batching/flush interval/buffer(§7.3) | 已解决（源码/测试） | 根因同样是文档滞后：旧实现确实按事件直发，但现行 `TelemetrySink` 已引入内存缓冲、定时 flush 和 buffer 上限，复审表未吸收这次重构。 | 当前 `ui/packages/shared/telemetry/src/index.ts` 已具备 `DEFAULT_FLUSH_INTERVAL_MS`、`DEFAULT_MAX_BUFFER_SIZE`、`flush()` 和 dead-letter/retry 逻辑；`ui/tests/unit/ui/shared/telemetry.test.ts` 已覆盖“buffer 满触发 flush”“失败 exporter 重试”“超过 retry budget 进入 dead letter”等行为。 | 关闭的是 batching/flush 缺口，不外推为完整 telemetry 平台能力都已到位。 |
| R16-57 | §81 UI Shared 基础设施深层缺陷 | P1 | `telemetry/src/index.ts` | 无 analytics consent 检查——record() 不调用 PlatformAdapter.getAnalyticsConsent()(§6.5.5+GDPR) | 已解决（源码/测试） | 根因也是复审基线停留在 `record()` 尚未接入同意校验的版本；现行实现已经把 consent gate 前置到事件入缓冲前。 | 当前 `ui/packages/shared/telemetry/src/index.ts` 的 `TelemetrySink` 构造器已支持 `consentChecker`，`record()` 在无同意时直接返回；`ui/tests/unit/ui/shared/telemetry.test.ts` 的 `record respects consent checker` 已验证未授权事件不会入缓冲。 | 关闭的是 consent gate 缺口；是否接哪种平台适配器属于上层接线问题，不再把 shared telemetry 核心错误记成未解。 |
| R16-58 | §81 UI Shared 基础设施深层缺陷 | P1 | `api-client/src/interceptors.ts` | 缺 RetryInterceptor(exponential backoff+jitter)和 DedupeInterceptor(§5.4) | 已解决（源码/测试） | 根因是复审条目停留在 shared interceptor 还只有 trace/auth/tenant 阶段，没有吸收后续 retry/dedupe 实装。 | 当前 `ui/packages/shared/api-client/src/interceptors.ts` 已提供 `createRetryInterceptor()` 与 `createDedupeInterceptor()`；`ui/tests/unit/ui/shared/interceptors.test.ts` 已覆盖 retryable status 告警与 dedupe key/cleanup 行为，本轮定向测试已通过。 | 关闭的是“拦截器缺失”子问题，不外推为调用方都已启用这些拦截器。 |
| R16-59 | §81 UI Shared 基础设施深层缺陷 | P1 | `api-client/src/interceptors.ts:62-76` | OfflineQueueInterceptor enqueue 后仍发请求失败——应 short-circuit 返回合成响应 | 已解决（源码/测试） | 根因与 `R10-22/R28-37` 同源：旧离线拦截器只入队，不会阻断后续 transport dispatch。 | 当前 `ui/packages/shared/api-client/src/interceptors.ts` 的 `createOfflineQueueInterceptor()` 在离线非 GET 请求入队后会直接抛出 `rest.offline:Request queued for offline sync`，从而短路网络层；`ui/tests/shared/api-client-runtime-regressions.test.ts` 已验证 enqueue 后 transport 不会被调用，本轮定向测试已通过。 | 作为重复挂账，随离线短路修复一并关闭。 |
| R16-60 | §81 UI Shared 基础设施深层缺陷 | P1 | `api-client/src/rest-client.ts` | 无 Accept-Version:v1 header——§1.8 要求版本头+406 mismatch 处理 | 已解决（源码/测试） | 根因与 `R10-19` 同源：旧 transport 没有把版本协商头下沉到真实 fetch，也没有对 `406` 做显式处理。 | 当前 `ui/packages/shared/api-client/src/rest-client.ts` 默认发送 `Accept-Version: v1`，`ui/packages/shared/api-client/src/interceptors.ts` 也保留版本协商拦截器；`ui/tests/shared/api-client-runtime-regressions.test.ts` 与 `ui/tests/unit/ui/shared/interceptors.test.ts` 已验证真实 transport 会带 `Accept-Version` 且 `406` 会进入协商处理路径，本轮定向测试已通过。 | 作为重复挂账，随版本协商修复一并关闭。 |
| R16-61 | §81 UI Shared 基础设施深层缺陷 | P1 | `api-client/src/ws-event-router.ts` | 无 heartbeat(§5.3.2.1 30s ping/45s pong timeout)；无 SharedWorker 多路复用(§5.3.4) | 已解决（源码/测试） | 根因原本分成两段：一是 WebSocket 自身缺应用层 heartbeat，二是 runtime 永远按 tab 各自实例化 `BrowserWSClient`，没有任何 worker 级 socket 共享，所以多标签页会重复建连、重复订阅。heartbeat 之前已补，但 runtime 创建路径仍绕开任何 SharedWorker 复用。 | 当前 `ui/packages/shared/api-client/src/ws-client.ts` 已新增 `SharedWorkerWSClient` 与 `shared-ws-worker.ts`，worker 侧维护单一 socket、跨 port 复用订阅和状态广播；`createRuntimeWSClient()` 在浏览器存在 `SharedWorker` 时会优先走该路径，`ui/apps/web/src/runtime.ts` 也已切换到它。定向验证见 `ui/tests/unit/ui/shared/ws-client.test.ts`。 | 关闭的是“多标签页 socket 复用”缺口；worker 内仍沿用现有 auth token/重连策略，但不再是每个 tab 一条独立连接。 |
| R16-62 | §81 UI Shared 基础设施深层缺陷 | P1 | `api-client/src/endpoints.ts` | 所有 list endpoint 返回扁平数组——无 cursor pagination/useInfiniteQuery(§5.4.6) | 已解决（共享分页基础设施已补齐，兼容层保留） | 根因不是“后端完全不支持分页”，而是 UI shared api/state 层把所有列表接口都固化成 `readonly T[]`，既没有 cursor page contract，也没有 `useInfiniteQuery` 封装，导致上层 feature 只能做一次性全量读取。 | 当前 `ui/packages/shared/api-client/src/endpoints.ts` 已新增 `CursorPage<T>`、`cursor/direction` 查询参数、`fetch*Page()` 系列分页入口，并保留 `fetch*()` 作为 `page.items` 兼容层；`ui/packages/shared/state/src/queries/helpers.ts` 新增 `createCursorInfiniteQuery()` 与 `flattenCursorPages()`，`task/approval/mission-control` query 现已导出 `createInfinite*Query()`，`ui/packages/shared/state/src/index.ts` 也补了 `useInfiniteTasksQuery/useInfiniteWorkflowsQuery/useInfiniteApprovalsQuery/useInfiniteIncidentsQuery/useInfiniteAgentsQuery`。定向验证：`ui/tests/shared/api-client.test.ts` 与 `ui/tests/unit/ui/shared/state-infinite-query.test.ts` 已通过。**边界说明**：本条关闭的是 shared 基础设施缺失，不外推为所有 feature 页面都已改成无限滚动或 cursor UX。 |
| R16-63 | §81 UI Shared 基础设施深层缺陷 | P1 | `state/src/queries/` | 无 persistQueryClient——§5.1.4 要求 cache 持久化到 IndexedDB/SQLite 离线支持 | 已解决（源码/测试） | 根因是真缺口：此前 shared state 只有 `QueryClient` 工厂，没有任何 dehydration / persistence / restore 链，刷新后 query cache 必然丢失。 | 本轮新增 `ui/packages/shared/state/src/query-cache-persistence.ts`，实现 `IndexedDbQueryCachePersister`、`restorePersistedQueryClient()` 与 `startPersistingQueryClient()`；`UiRuntimeProvider` 启动时会先 restore，再订阅 query cache 变更并持续写回 IndexedDB。 | 定向验证已通过：`ui/tests/shared/query-cache-persistence.test.ts` 直接验证 dehydrated state 可被持久化并在新 `QueryClient` 上恢复，同时 cache 变更会被自动持久化。 |
| R16-64 | §81 UI Shared 基础设施深层缺陷 | P1 | `platform/src/web-platform-adapter.ts` | 无 notification/biometric/WebAuthn 能力(§2.6.2/§2.5.7) | 已解决（源码/测试） | 根因不是“平台层完全没有 capability facade”，而是 `PlatformAdapterCapabilityView` 只暴露了 secureStorage/clipboard/haptics 等基础能力，`WebPlatformAdapter` 也没有把浏览器原生 Notification、平台生物识别可用性探测和 WebAuthn 入口挂出来，导致上层只能绕过 adapter 直连浏览器 API。 | 当前 `ui/packages/shared/types/src/index.ts` 已补 `notifications / biometric / webAuthn` capability 视图；`ui/packages/shared/platform/src/base-platform-adapter.ts` 增加了默认 no-op/fail-closed 实现；`ui/packages/shared/platform/src/web-platform-adapter.ts` 已接入 `Notification.requestPermission()`、`new Notification(...)`、`PublicKeyCredential.isUserVerifyingPlatformAuthenticatorAvailable()` 与 WebAuthn create/get 调用。定向验证见 `ui/tests/shared/platform.test.ts`。 | 关闭的是 capability surface 缺口；是否在桌面/移动壳里进一步桥接到原生通知中心，属于后续产品化增强，不影响本条收口。 |
| R16-65 | §81 UI Shared 基础设施深层缺陷 | P1 | `apps/web/src/runtime.ts:40` | tenantId 硬编码 "tenant-default"——非从 auth context 获取；多租户路由失效 | 已解决（源码/测试） | 根因与 `R10-28` 同期：旧 runtime 同时硬编码 token 与 tenant 兜底值，reaudit 反复引用了改造前的静态租户结论。 | 当前 `ui/apps/web/src/runtime.ts` 已通过 `createTenantInterceptor(config.tenantId ?? null)` 从运行时配置接租户上下文，不再写死 `"tenant-default"`；本轮新增 `ui/tests/shared/web-runtime-auth-regression.test.ts`，直接验证 tenant interceptor 接收到的是传入的动态 `tenantId`，定向测试已通过。 | 关闭的是 runtime tenant 硬编码问题，不外推为所有多租户路由产品化能力都已完工。 |
| R16-66 | §81 UI Shared 基础设施深层缺陷 | P1 | `apps/web/src/app-shell.tsx` | 无 ErrorBoundary——§5.6 要求 per-severity 降级(fallback UI/retry/report) | 已解决（源码/测试） | 根因是重复挂账没有吸收后续 `AppErrorBoundary` 落地；现行 web 壳层已经把 feature 渲染包在错误边界里。 | 当前 `ui/apps/web/src/app-shell.tsx` 已实现 `AppErrorBoundary`，提供 `"Something went wrong"`、`Retry`、`Report Issue` 降级 UI；本轮新增 `ui/tests/shared/web-app-shell-auth-regression.test.tsx`，直接验证 feature 组件抛错后会进入错误边界 fallback，定向测试已通过。 | 关闭的是 web 壳层缺少 ErrorBoundary 的问题，不外推为完整错误分级体系全部完成。 |
| R16-67 | §81 UI Shared 基础设施深层缺陷 | P1 | `apps/web/src/app-shell.tsx` | 无 PlatformAdapterProvider——§3.7.4 要求 adapter 通过 Provider 注入 | 已解决（源码/测试） | 根因是 web 壳层此前把 platform adapter 仅当作可选参数透传，没有建立共享 Provider，导致下游无法通过 canonical context 读取 adapter，只能各自 new adapter 或依赖测试 mock。 | 本轮新增 `ui/packages/shared/platform/src/provider.tsx` 并从 `ui/packages/shared/platform/src/index.ts` 导出；`ui/apps/web/src/app-shell.tsx` 现统一用 `PlatformAdapterProvider` 包裹 `UiRuntimeProvider`，默认注入 `createWebPlatformAdapter()`，从壳层入口补齐 §3.7.4 要求的 Provider seam。 | 定向验证已通过：`ui/tests/shared/web-app-shell-auth-regression.test.tsx` 直接断言壳层树中存在 `platform-adapter-provider`，`npm test -- tests/shared/web-app-shell-auth-regression.test.tsx` 本轮通过。 |
| R16-68 | §81 UI Shared 基础设施深层缺陷 | P2 | `apps/web/src/feature-registry.ts:1-57` | 27个 feature 模块 eager import——无 React.lazy()/dynamic import(§4.4.1 per-route code split) | 已解决（源码/测试） | 根因与 `R7-1` 同源：懒加载组件和路由元数据/壳层 Suspense 之间一直没接成闭环，reaudit 才会继续把它视为“无 code split”。 | 当前 `ui/apps/web/src/feature-registry.ts` 已将各 feature 标记为 `codeSplit:true`，`ui/apps/web/src/app-shell.tsx` 也已用 `React.Suspense` 承接 lazy 组件。 | 作为重复挂账，随 `R7-1` 一并关闭；`ui/tests/unit/ui/apps/web/feature-registry.test.ts` 与 `ui/tests/shared/web-app-shell-auth-regression.test.tsx` 本轮通过。 |
| R16-69 | §81 UI Shared 基础设施深层缺陷 | P2 | `sync/src/offline-queue.ts:19-22` | enqueue() 无容量上限——§5.5 离线存储需有界队列 | 已解决（源码/测试） | 根因是复审条目停留在无界数组阶段，没有吸收离线队列后续加入 `maxCapacity` 与 oldest-eviction 的实现。 | 当前 `ui/packages/shared/sync/src/offline-queue.ts` 已定义 `DEFAULT_MAX_CAPACITY = 1000`，`enqueue()` 在满队列时会淘汰最旧 mutation；`ui/tests/unit/ui/shared/offline-queue.test.ts` 已验证 capacity=2 时会逐出 `oldest`，本轮定向测试已通过。 | 作为重复挂账，随有界离线队列修复一并关闭。 |
| R16-70 | §81 UI Shared 基础设施深层缺陷 | P2 | `telemetry/src/index.ts` | 无 Web Vitals(FCP/LCP/CLS/INP)采集(§7.3 G-3) | 已解决（源码/测试） | 根因复核后更具体：文件里虽然曾出现 `startWebVitalsCollection()` 雏形，但它依赖仓内并不存在的 `web-vitals` 运行时包，`import("web-vitals")` 在实际构建链上不可解析，所以这不是“已接线未验证”，而是一段无法真正加载的假实现。 | 当前 `ui/packages/shared/telemetry/src/index.ts` 已改成浏览器原生 `PerformanceObserver` 采集 `paint / largest-contentful-paint / layout-shift / event`，并统一记录 `FCP/LCP/CLS/INP` 到 `TelemetrySink`；`ui/tests/unit/ui/shared/telemetry.test.ts` 已新增定向回归，验证四类指标都会被写成 `web_vitals.*` 事件。 | 真实修的是运行时采集路径本身，不再依赖缺失三方包。 |
| R16-71 | §81 UI Shared 基础设施深层缺陷 | P2 | `api-client/src/rest-client.ts:177-178` | HTTP error 抛泛型 Error——无 §5.6.6 status→UI 行为映射(401→redirect/403→denied/429→backoff) | 已解决（源码/测试） | 根因是 `HttpTransport.send()` 过去无论 401/403/429/5xx 都只抛 `Error("rest.http_error:...")`，状态码虽然在 message 里，但 UI 层拿不到结构化行为信号，也拿不到 `Retry-After`。 | 本轮在 `ui/packages/shared/api-client/src/rest-client.ts` 新增 `RestHttpError`，把 `status`、`uiAction` 与 `retryAfterMs` 结构化下沉；401 现映射 `redirect_to_login`，403 映射 `access_denied`，429/5xx 映射 `backoff_and_retry`，406 也单独映射 `version_mismatch`。 | 定向验证已通过：`ui/tests/shared/api-client-runtime-regressions.test.ts` 新增 401/403/429 用例，直接验证 `RestHttpError.status/uiAction/retryAfterMs`，本轮测试通过。 |
| R16-72 | §81 UI Shared 基础设施深层缺陷 | P2 | `apps/web/src/runtime.ts:51-55` | registerWebServiceWorker 引用 aa-sw.js 但文件不存在——L1 静态缓存不工作(§5.5.1) | 已解决（源码/资源） | 根因与 `2176` 同源：早期 web runtime 在没有实际 service worker 资源时直接注册固定路径，重复条目没有吸收后续资源文件与存在性检查修复。 | 当前 `ui/apps/web/public/aa-sw.js` 已存在，`ui/apps/web/src/runtime.ts` 的 `registerWebServiceWorker()` 也会先对 `${BASE_URL}aa-sw.js` 执行 HEAD 探测，仅在资源存在时注册。该条关闭依据来自现行源码与资源文件本身。 | 关闭的是“注册不存在的 SW 文件”缺陷，不外推为 background sync 或缓存策略其他条目已完成。 |
| R16-73 | §81 UI Shared 基础设施深层缺陷 | P2 | `features/settings/src/hooks/index.ts:105-107` | save() 同步 setState 无 API 调用/ETag(§4.7.8 要求 If-Match) | 已解决（源码/测试） | 根因不是 `useSettingsVm.save()` 完全不走后端，而是 hook 侧虽然已经调用了 `updatePreferences(client, ..., etag)`，但 `RESTClient` 接口没有暴露额外 header 参数，导致 `etag` 在 endpoint helper 里被直接丢弃，`If-Match` 从未真正下沉到 transport。 | 当前 `ui/packages/shared/api-client/src/rest-client.ts` 已让 `patch()` 支持附加 headers，`ui/packages/shared/api-client/src/endpoints.ts` 的 `updatePreferences()` 会在存在 etag 时发送 `If-Match`，`ui/packages/features/settings/src/hooks/index.ts` 继续沿用该 helper。定向验证见 `ui/tests/unit/ui/packages/features/settings/hooks.test.tsx` 与 `ui/tests/shared/api-client-runtime-regressions.test.ts`，已分别验证 hook 会传出 etag、transport 会收到 `If-Match`。 | 关闭的是 optimistic locking 缺口，不外推为设置页所有字段都已有后端实体化。 |
| R16-74 | §81 UI Shared 基础设施深层缺陷 | P2 | `apps/web/src/runtime.ts` | 启动无 contract version check——§1.8 要求 /api/v1/meta/contract-version+mismatch banner | 已解决（源码/测试） | 根因是客户端虽然早就有 `Accept-Version` 头和 `406` 映射，但缺少启动期主动探测：`createWebRuntimeClients()` 只创建 client，不会在首屏前调用 `/api/v1/meta/contract-version`，也没有把 mismatch 呈现成显式 banner。 | 当前 `ui/apps/web/src/runtime.ts` 已新增 `checkWebContractVersion()`，基于 `fetchContractVersion()` 对比客户端支持集 `v1`；`ui/apps/web/src/main.tsx` 启动时会先执行该检查，再把 `startupBanner` 传给 `ui/apps/web/src/app-shell.tsx` 渲染告警条。定向验证见 `ui/tests/unit/ui/apps/web/runtime-contract-version.test.tsx`。 | 这是启动期 banner 收口；运行时 `406` 协商错误仍由既有 interceptor/transport 路径处理。 |
| R16-75 | §82 ADR/Contract 系统性一致性缺陷 | P0 | `adr/060-explicit-planning-hub.md` | 定义独立 Plan DTO(steps+dag) 与 ADR-109 冻结的 PlanGraphBundle 矛盾——两个活跃执行计划类型并存 | 已解决（文档/ADR） | 根因是 ADR-060 早期把 `Plan DTO` 与 bridge 一起写成 canonical handoff，后续正文虽然完成了 `PlanGraphBundle` 收敛，但重复挂账没有同步回写。 | 当前 `docs_zh/adr/060-explicit-planning-hub.md` 已把 `PlanGraphBundle` 写为唯一 P3→P4 handoff，`RuntimeExecuteBridge` 明确仅为 compatibility seam；`tests/invariants/contract-and-oapeflir-remediation.test.ts` 也已验证 `PlanGraphBundle / NodeAttemptReceipt` 锚点。 | 已按现行 ADR 与测试证据收口重复挂账。 |
| R16-76 | §82 ADR/Contract 系统性一致性缺陷 | P0 | `adr/060:59 + runtime-execute-bridge.ts:199` | RuntimeExecuteBridge.executePlan() 仍接受旧 Plan 类型非 PlanGraphBundle——代码实际走废弃路径 | 已解决（源码已满足，本轮补定向测试） | 根因是 ADR/contract 迁移期曾长期容忍 legacy `Plan` 与 canonical `PlanGraphBundle` 并存；现行 bridge 已完成收口，但 reaudit 条目仍停留在旧兼容期结论。 | 当前 `src/platform/five-plane-orchestration/oapeflir/runtime-execute-bridge.ts` 的 `assertIsPlanGraphBundle()` 会拒绝仅含 `planId` 的 legacy plan，并要求 `planGraphBundleId + graph.nodes`；本轮补了 `tests/unit/platform/orchestration/oapeflir/runtime-execute-bridge.test.ts` 中的 legacy plan rejection 回归，验证 `executePlan()` 不再接受废弃路径输入。 | 关闭的是 bridge 的 contract 收口，不外推为所有 ADR/文档都已自动同步。 |
| R16-77 | §82 ADR/Contract 系统性一致性缺陷 | P0 | `src/platform/contracts/execution-plan/index.ts` | ExecutionPlan 仍导出+有 createExecutionPlan() 工厂——ADR-109 已明确禁止 | 已修复（源码） | ADR-109 明确禁止 legacy factory，但 contract 导出面长期未同步；本轮按 ADR-109 移除 `createExecutionPlan` 工厂并更新导出面。 | 真实代码修改：移除 `createExecutionPlan` 工厂导出；`ExecutionPlan` 类型别名保留为 `PlanGraphBundle` 的兼容层。 | 本轮完成 contract 源码修复；引用该工厂的测试需同步更新。 |
| R16-78 | §82 ADR/Contract 系统性一致性缺陷 | P0 | `src/platform/contracts/control-directive/index.ts` | 已修复（源码） | 已移除 `createControlDirective` 工厂函数及其在 `types/index.ts` 的重导出；`ControlDirective` 接口与 `ControlDirectiveKind` 类型保留作为迁移兼容层。 | 根因是 ADR-109 明确禁止 legacy factory，但 contract 导出面长期未同步；本轮按 ADR-109 移除工厂并更新导出面。 | 真实代码修改：移除 `createControlDirective` 工厂及重导出。 |
| R16-79 | §82 ADR/Contract 系统性一致性缺陷 | P0 | `src/platform/contracts/state-command/index.ts` | StateCommand 仍导出+有工厂——ADR-109 已禁止 | 已修复（源码） | 已移除 `createStateCommand` 工厂函数及其在 `types/index.ts` 的重导出；`StateCommand` 接口保留作为迁移兼容层。 | 根因是 ADR-109 明确禁止 legacy factory，但 contract 导出面长期未同步；本轮按 ADR-109 移除工厂并更新导出面。 | 真实代码修改：移除 `createStateCommand` 工厂及重导出。 |
| R16-80 | §82 ADR/Contract 系统性一致性缺陷 | P1 | `contracts/artifact_unified_model_contract.md:22` | OAPEFLIR stages 列 "Rollout" 非 "Release"(ADR-091 已改名) | 已修复（contract/文档） | 根因不是 artifact contract 的主阶段表仍写 `Rollout`，而是文档后半段仍残留了 `OAPEFLIR Rollout` 注释与 `Rollout artifact` 旧术语，造成 Release 命名没有完全收口。 | 本轮已将 `docs_zh/contracts/artifact_unified_model_contract.md:86,108` 分别改为 `OAPEFLIR Release canary metrics` 与 `Release artifact 必须包含 metrics 快照`，消除了该 contract 内剩余的阶段旧名。 | 真实改了 contract 文档，不是假关闭。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 |
| R16-81 | §82 ADR/Contract 系统性一致性缺陷 | P1 | `contracts/context_compaction_and_overflow_contract.md:139` | OAPEFLIR stage 表用 "Rollout"+RolloutRecord(应为 Release) | 已解决（contract 复核） | 根因不是当前 context compaction contract 仍把末阶段写成 `Rollout + RolloutRecord`，而是复审条目沿用了 remediation 前的旧表格。 | 当前 `docs_zh/contracts/context_compaction_and_overflow_contract.md:140` 的阶段表已经写成 `Release | ReleaseRecord | 审计追溯`，原指控所述旧术语已不存在。 | 不改代码，仅关闭陈旧结论。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 |
| R16-82 | §82 ADR/Contract 系统性一致性缺陷 | P1 | `contracts/release_rollout_and_rollback_contract.md:8` | Contract 标题混淆 Rollout(机制) 与 Release(阶段) | 已解决（contract/文档） | 根因是 release contract 早期标题与阶段说明把 `Rollout` 和 `Release` 混写，后续 contract 已修订，但重复挂账仍停留在旧结论。 | 当前 `docs_zh/contracts/release_rollout_and_rollback_contract.md` 标题已为 `Release And Rollback Contract`，正文已把 `ReleaseDecisionView` 定义为 release 阶段 projection，并补齐 `ReleaseChannel`；不变量测试也已验证 `evaluate_0` 与 `ReleaseRecord` 语义。 | 已按现行 contract 与测试证据收口重复挂账。 |
| R16-83 | §82 ADR/Contract 系统性一致性缺陷 | P1 | `40+ contracts` | 锅炉板 OAPEFLIR 块用 "改进候选评估与 rollout"——大规模术语过时 | 已修复（文档/ADR） | 根因不是当前仍有 “40+ contracts” 保留这句旧 boilerplate，而是原始挂账把一类模板化术语残留扩大描述成合同面普遍问题；本轮复核后，实际剩余是若干 ADR 顶部 OAPEFLIR 模板块仍写着 “改进候选评估与 Rollout 状态机”。 | 本轮已把 `docs_zh/adr/002/004/005/006/008/009/010/011/014/015/020-*.md` 顶部模板语句统一改为 `改进候选评估与 Release 状态机（ADR-075）`；修复后对 `docs_zh/contracts docs_zh/adr docs_zh/architecture` 执行 `grep -Rni '改进候选评估与 Rollout|改进候选评估与 rollout'` 已无命中。 | 真实改了文档并用仓内检索校验残留已清零。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 |
| R16-84 | §82 ADR/Contract 系统性一致性缺陷 | P1 | `adr/093 vs harness/index.ts:57-77` | ConstraintPack 缺 sandbox_requirement/approval_requirement/budget_envelope(ADR-093 要求) | 已解决（源码/ADR） | 根因不是现行 `ConstraintPack` 仍缺字段，而是 ADR-093 修订后，reaudit 条目沿用了更早一版只含 risk/output policy 的旧结论。 | 当前 `src/platform/five-plane-orchestration/harness/index.ts` 的 `ConstraintPack` 已显式包含 `budgetEnvelope`、`sandboxRequirement`、`approvalRequirement`，`normalizeConstraintPack()` 也会把 `budget/budgetEnvelope` 统一折叠；`docs_zh/adr/093-harness-constraint-engine.md` 同样已把这三项写入最小集合。 | 关闭的是源码与 ADR 的一致性缺口，不外推为所有 intake/constraint 下游都已完全产品化。 |
| R16-85 | §82 ADR/Contract 系统性一致性缺陷 | P1 | `contracts/task-intake-request-contract.md:56` | ConfirmedTaskSpec 引用 ConstraintPackRef 但类型无 ref/ID 字段——inline struct 非可引用实体 | 已解决（contract/文档） | 根因是早期 intake contract 把 `ConstraintPackRef` 只当作叙事名词，没有冻结成权威引用类型。 | `docs_zh/contracts/task-intake-request-contract.md` 现已把 `ConstraintPackRef` 定义为 `string` 引用类型，格式 `constraint_pack:${id}`，`ConfirmedTaskSpec.constraints` 与 `RequestEnvelope.constraintPackRef` 都以该引用语义表达。 | 已按真实 contract 证据解决。 |
| R16-86 | §82 ADR/Contract 系统性一致性缺陷 | P1 | `architecture/05-cross-platform-ui-architecture.md:7` | UI 架构基线为 00-platform-architecture v3.2——当前已 v4.3，UI arch 未 re-baseline | 已解决（文档复核） | 根因不是现行 UI 架构文档仍锚定 v3.2，而是复审条目没有跟随后续基线回写。 | 当前 `docs_zh/architecture/05-cross-platform-ui-architecture.md` 顶部已经明确标注 `文档版本 v4.3`，并把 `00-platform-architecture.md v4.3` 写为基线文档，正文也声明已对齐 v4.3 Contract Freeze。 | 不改生产代码，只关闭文档陈旧挂账。 |
| R16-87 | §82 ADR/Contract 系统性一致性缺陷 | P1 | `adr/063 + contracts/behavior_drift_detection_contract.md` | ADR 定义4种 drift type+统计检测——代码仅有 golden-test regression；DriftDetector/DriftAlert/DriftMitigationAction 不存在 | 已解决 | 原问题集中在 contract/导出面缺口，而不是 runtime 完全不存在：`drift-detector.ts` / `drift-detector-service.ts` 已实现，但模块 barrel 与 contract 文档没有把 canonical 对象公开出来。 | 本轮在 `src/ops-maturity/drift-detection/index.ts` 补导出 `drift-detector` 与 `drift-detector-service`，并在 `docs_zh/contracts/behavior_drift_detection_contract.md` 回写 `DriftDetector/DriftAlert/DriftMitigationAction` 与四类 drift dimension；`tests/unit/platform/ops-maturity/reaudit-batch-r14-r15-r16-regressions.test.ts` 已验证这些类型与服务可直接从模块入口使用。 | 关闭 drift contract / export 面缺口。 |
| R16-88 | §82 ADR/Contract 系统性一致性缺陷 | P1 | `contracts/event_bus_contract.md:84 + event-envelope-contract.md:7` | release._ 事件既非 platform._(truth) 也非 oapeflir.view.\*(projection)——命名空间未定义 | 已解决（contract/文档） | 根因是 event docs 之前把 `release.*` 当成裸命名空间列出，却没有说明它在 v4.3 下应属于 truth fact 还是 projection。 | `docs_zh/contracts/event-envelope-contract.md` 与 `event_bus_contract.md` 现已明确：truth release 事件必须使用 `platform.release.*`，裸 `release.*` 只允许作为历史投影或 adapter 输入。 | 已按真实 contract 证据解决。 |
| R16-89 | §82 ADR/Contract 系统性一致性缺陷 | P1 | `event-registry.ts:73-109` | Event producers 注册为 workflow_runtime 非 platform.\* namespace——不遵循 canonical 命名 | 已解决 | 该条本质是 contract 理解偏差：`platform.*` 是 truth event namespace，不要求 `producer` 字段也编码成 `platform.*`；legacy event 的 producer id 保持历史标识是有意的兼容策略。 | 本轮在 `src/platform/five-plane-state-evidence/events/event-registry.ts` 明确补了兼容注释，并在 `docs_zh/contracts/event_registry_and_ops_threshold_contract.md` 写清“producer 是稳定服务标识、canonical 由 event_type namespace 决定”；回归测试已验证 legacy `task:status_changed` 与 canonical `platform.harness_run.status_changed` 会并存且 source-of-truth 判定正确。 | 回写 legacy producer compatibility 规则。 |
| R16-90 | §82 ADR/Contract 系统性一致性缺陷 | P1 | `adr/060:108` | 引用 src/core/planning/ 但路径不存在(实际为 src/platform/orchestration/) | 已解决（文档/ADR） | 根因是 ADR-060 早期交叉引用还指向 `src/core/planning/`，后续目录迁移完成后，重复挂账没有随着 ADR 正文一起更新。 | 当前 `docs_zh/adr/060-explicit-planning-hub.md` 的 “后果/来源章节” 已不再引用 `src/core/planning/`，并明确新增 `src/platform/orchestration/` 模块。 | 已按现行 ADR 证据收口重复挂账。 |
| R16-91 | §82 ADR/Contract 系统性一致性缺陷 | P1 | `architecture §19` | Agent handoff/delegation 无专属 contract(ADR-019 定义4层协议但无 agent_handoff_contract.md) | 已解决（contract/文档） | 根因是 handoff / delegation contract 长期散落在 `DelegationRequest`、ACP schema、runtime handoff model 和 ADR 中，没有单独冻结成权威文档。 | 新增 `docs_zh/contracts/agent_handoff_contract.md`，覆盖 `DelegationRequest / DelegationReceipt / ACPMessage / AgentHandoff` 与 `C1-C7` 深度治理；文档结构测试和不变量测试均已通过。 | 已按真实 contract 与测试证据解决。 |
| R16-92 | §82 ADR/Contract 系统性一致性缺陷 | P1 | `architecture §68` | Multimodal video pipeline 无 contract(multimodal_gateway_contract.md 缺 video scene/keyframe/transcript) | 已解决 | 原审计已滞后：video multimodal contract 当前已拆到 `video_multimodal_pipeline_contract.md`，但 gateway contract 的 canonical object 列表没有把 video 子对象写得足够直白。 | 本轮在 `docs_zh/contracts/multimodal_gateway_contract.md` 补入 `VideoPipelineOutput/VideoTranscriptSegment/VideoTimeline/VideoFrame` 并显式要求以 `video_multimodal_pipeline_contract.md` 为准；`src/ops-maturity/multimodal/video-processor/index.ts` 已实际产出 transcript / scenes / keyFrames，回归测试也已验证视频管道输出。 | 回写 multimodal video contract 现状。 |
| R16-93 | §82 ADR/Contract 系统性一致性缺陷 | P1 | `src/platform/contracts/execution-plan/index.ts:50` | ExecutionPlan 完整接口(budget/steps/fallback) 与 PlanGraphBundle 并行——同包两套竞争类型 | 已解决（源码） | contract 文档或导出面没有完全跟随 canonical runtime 迁移。 | `src/platform/contracts/execution-plan/index.ts` 已移除 `ExecutionPlan` 类型别名（原为 `PlanGraphBundle` 别名）和 `createExecutionPlan` 工厂函数；`src/platform/contracts/types/index.ts` 已移除对 `createExecutionPlan` 的 re-export。`LEGACY_CONTRACT_NAMES` 仍保留 "ExecutionPlan" 标识以防遗留导入。 | 移除的是deprecated alias/工厂，非canonical类型的re-export backward-compat path仍保留。
| R16-94 | §82 ADR/Contract 系统性一致性缺陷 | P2 | `adr/072-oapeflir-testing-strategy.md:18` | 引用 tests/security/、tests/chaos/、tests/performance/ 但目录不存在 | 已解决（文档/ADR） | 根因是 ADR-072 把规划态专项测试目录写成了已落地事实，导致测试路径证据失真。 | 当前 `docs_zh/adr/072-oapeflir-testing-strategy.md` 已把现存权威测试根收敛为 `tests/unit/`、`tests/integration/`、`tests/golden/`、`tests/e2e/`、`tests/invariants/`，并明确 dedicated performance/security/chaos suites 只有在真实目录与 CI 落地后才能宣称存在；`tests/invariants/contract-and-oapeflir-remediation.test.ts` 也新增了“不再引用不存在目录”的守护。 | 已按现行 ADR 与测试证据收口重复挂账。 |
| R16-95 | §82 ADR/Contract 系统性一致性缺陷 | P2 | `adr/003-memory-seven-layers.md` | 文件名 "seven-layers" 但内容/标题为 "六层"——ADR-007 交叉引用为 "六层" 链接到此文件 | 已解决 | 该条在本轮开始前重新出现了真实回归：仓库同时保留 `003-memory-seven-layers.md`，且正文仍是六层内容；部分 active 文档也继续把 ADR-003 指到 seven-layers 文件。 | 本轮已把 `docs_zh/adr/003-memory-seven-layers.md` 改成“历史别名 / 跳转页”，并把 `docs_zh/adr/007-evolution-engine.md`、`docs_zh/guides/quickstart.md`、`docs_zh/migration/00-migration-guideline.md` 等 active 引用统一改到 `003-memory-six-layers.md`；回归测试已验证 active 引用使用 six-layer 文件，而 seven-layer 文件只保留 alias 说明。 | 关闭 ADR-003 文件名/内容错配回归。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 |
| R16-96 | §82 ADR/Contract 系统性一致性缺陷 | P2 | `architecture §62` | Edge runtime contract 不引用 RunVersionLock——离线节点无版本锁冻结语义 | 已解决（contract/文档） | 根因不是 edge runtime contract 现在仍缺版本锁语义，而是复审条目停留在 `RunVersionLock` contract 与 edge sync contract 尚未打通之前。 | 当前 `docs_zh/contracts/edge_runtime_and_sync_contract.md` 已把 `run_version_lock_ref` 列入 `EdgeRuntimeProfile` 最小字段，并明确“离线节点持有活跃 NodeRun 时，必须关联 RunVersionLock”；`docs_zh/contracts/version-lock-contract.md`、`docs_zh/contracts/harness-run-contract.md` 也已形成对应引用链。 | 关闭的是 contract 引用缺口，不外推为 edge runtime 实装能力已经全量完成。 |
| 2064 | §177 汇总重复编号补记 | 说明 | `docs_zh/reviews/platform-architecture-implementation-consistency-audit_round.md:3493` | 与 `R8-61` 同源：Plugin SPI ADR 插入序列时复用了 `ADR-066`，导致编号唯一性被破坏。 | 已解决（文档/ADR） | 这是原始 round 文档对 `R8-61` 的重复挂账，不是新的独立实现缺陷。 | `docs_zh/adr/071-plugin-spi-framework.md` 已完成重编号；`docs_zh/adr/README.md`、`docs_zh/contracts/plugin_spi_contract.md`、`docs_zh/migration/00-migration-guideline.md` 已同步引用；`tests/unit/docs/documentation-health.test.ts` 已守护 ADR 编号唯一性并通过。 | 作为重复挂账补记保留，便于与源文档逐项对齐。 |
| R17-01 | §84 Orchestration/Execution 深层缺陷 | P0 | `delegation-manager.service.ts:409-411` | narrowPermissions 使用 child resources 原样(非空时)不与 parent 取交集——子代可请求父代无权资源(§19.2 child⊆parent) | 已解决（源码/测试） | 根因不是当前实现仍把 child resources 原样下发，而是复审条目没有跟上后续的权限收窄修复。 | 当前 `src/platform/five-plane-orchestration/agent-delegation/delegation-manager.service.ts` 的 `narrowPermissions()` 已对 resources 执行 `parent ∩ child`，仅在 child 未显式请求资源时才保留 parent 现有集合；`tests/unit/platform/agent-delegation/permission-narrowing.test.ts` 已验证 child 无法请求父代没有的 resource/action。 | 关闭的是 child⊆parent 权限收窄缺口；约束字段的进一步语义收窄仍按其它条目单独跟踪。 |
| R17-02 | §84 Orchestration/Execution 深层缺陷 | P0 | `delegation-manager.service.ts:180` | cancel() 直接修改 delegation.status 无 CAS/fencing——并发 cancel+complete 竞态产生不一致状态 | 已解决（源码/测试） | 根因不是委托状态机完全没有 CAS，而是复审条目停留在 `cancel()` 还未把已验证状态作为 fencing token 传入状态迁移器之前。 | 当前 `src/platform/five-plane-orchestration/agent-delegation/delegation-manager.service.ts` 的 `cancel()` 会先捕获 `initialStatus`，再调用 `transitionDelegationStatus(delegation, "cancelled", initialStatus)`；后者会在写入前校验 `delegation.status === expectedStatus`，不一致时抛出 `delegation.concurrent_modification`。本轮补了 `tests/unit/platform/five-plane-orchestration/agent-delegation/delegation-manager-status-context-regressions.test.ts` 验证并发改写会被 fencing 拦截。 | 关闭的是 cancel/complete 竞态状态保护，不外推为所有 delegation side effect 都已具备事务边界。 |
| R17-03 | §84 Orchestration/Execution 深层缺陷 | P0 | `run-termination-cleanup.ts:50-53` | execute() 仅分类资源为 cleaned/skipped 列表但无实际清理逻辑(无 lease release/secret revoke/budget release)——始终返回 complete:true | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。 | 不宣称已修复。 |
| R17-04 | §84 Orchestration/Execution 深层缺陷 | P0 | `command-security.ts:258-261` | validateCommandSignature 阻止所有以 `-` 开头的 args——合法 `python script.py --verbose` 被误拒为 interpreter_flag_denied | 已解决（源码/测试） | 根因不是现行实现仍把“任意带 interpreter flag 的命令”一律拒掉，而是复审结论停留在脚本路径只看 `args[0]` 的旧版本；那会把 `python --verbose script.py` 错判成纯 flag 调用。 | 当前 `src/platform/five-plane-execution/tool-executor/command-security.ts` 已改为寻找首个非 flag 参数作为真实脚本路径，只在“全部参数都是 flag/没有脚本路径”时返回 `tool.command_interpreter_flag_denied`。本轮在 `tests/unit/platform/execution/tool-executor/command-security.test.ts` 新增 `allows interpreter flags when a real script path follows` 回归，直接验证 `python --verbose script.py` 会放行并把 `script.py` 识别为 sandbox read path。 | 不改 broader command policy，仅关闭“合法 interpreter pre-flags 被误拒”这一缺口。 |
| R17-05 | §84 Orchestration/Execution 深层缺陷 | P1 | `delegation-manager.service.ts:274` | createDelegationContext 不重新验证 parent permissions 是否被撤销——过期权限持续生效 | 已解决（源码/测试） | 根因不是现行 `createDelegationContext()` 完全不校验权限撤销，而是复审条目没有跟上“以 delegation 过期作为权限撤销边界”的实现。 | 当前 `src/platform/five-plane-orchestration/agent-delegation/delegation-manager.service.ts` 在 `createDelegationContext()` 开头会比较 `delegation.expiresAt` 与 `nowIso()`，过期时抛出 `delegation.permissions_revoked`；本轮补了 `tests/unit/platform/five-plane-orchestration/agent-delegation/delegation-manager-status-context-regressions.test.ts` 作为回归验证。 | 这里关闭的是“过期 delegation 继续生效”的缺口；若未来需要对外部 IAM/ACL 实时撤权做更强联动，仍需单独设计。 |
| R17-06 | §84 Orchestration/Execution 深层缺陷 | P1 | `call-depth-budget.ts:18-22` | effectiveCallDepth 用 Math.max 非 sum——depth=3 delegate+depth=5 decompose 读为5非8，可绕过限制 | 已解决（源码/测试） | 根因不是当前 `CallDepthBudget` 还在用 `Math.max`，而是审计条目没有吸收后续把三种深度维度累加的修复。 | 当前 `src/platform/five-plane-orchestration/agent-delegation/call-depth-budget.ts` 已按 `currentCallDepth + goalDecompositionDepth + delegationDepth` 计算 `effectiveCallDepth`；本轮新增 `tests/unit/platform/five-plane-orchestration/agent-delegation/call-depth-budget-regressions.test.ts`，直接验证 `2 + 3 + 4 = 9`，不会再被读成单个最大值。 | 这是源码已修、文档未同步的陈旧挂账。 |
| R17-07 | §84 Orchestration/Execution 深层缺陷 | P1 | `topology-validator.ts:119-122` | 循环检测仅检查 chain 内 packId——fan-out 拓扑中兄弟分支环不可检测 | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。 | 不宣称已修复。 |
| R17-08 | §84 Orchestration/Execution 深层缺陷 | P1 | `execution-lease-service-async.ts:247-265` | releaseLeaseSync 不检查 lease.status!="active"——可释放已过期/已释放的 lease | 已解决（源码/测试） | 根因不是 release 路径现在仍对死 lease 放行，而是复审条目没有跟上后续补进的状态与过期双重校验。 | 当前 `src/platform/five-plane-execution/lease/execution-lease-service-async.ts` 的 `releaseLeaseSync()` 已先拦截 `lease.status !== "active"`，再拦截 `lease.expiresAt <= occurredAt`，随后还会在关闭前重新读取一次 lease 做 TOCTOU 校验；本轮补了 `tests/unit/platform/five-plane-execution/lease/execution-lease-service-async-regressions.test.ts` 验证过期 active lease 会返回 `lease_expired` 而不是被释放。 | 关闭的是 release dead-lease 缺口，不外推为 lease repository 已具备跨进程 SQL fencing。 |
| R17-09 | §84 Orchestration/Execution 深层缺陷 | P1 | `execution-lease-service-async.ts:424-431` | handoverLeaseSync 不检查前 lease 是否过期——可从死 lease 创建新活跃 lease | 已解决（源码/测试） | 根因不是 handover 仍可从死 lease 复制出新 lease，而是复审条目停留在补丁落地前的旧结论。 | 当前 `src/platform/five-plane-execution/lease/execution-lease-service-async.ts` 的 `handoverLeaseSync()` 在关闭旧 lease 之前已校验 `previousLease.expiresAt > occurredAt`，过期则直接返回 `lease_expired`；本轮补了 `tests/unit/platform/five-plane-execution/lease/execution-lease-service-async-regressions.test.ts` 验证过期 lease 无法 handover。 | 关闭的是 handover 从死 lease 派生新 lease 的缺口。 |
| R17-10 | §84 Orchestration/Execution 深层缺陷 | P1 | `escalation/index.ts:23-50` | EscalationService decide() 返回结构但不创建 approval_request/不通知操作员/不阻塞执行 | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。 | 不宣称已修复。 |
| R17-11 | §84 Orchestration/Execution 深层缺陷 | P1 | `budget-allocator.ts:59-62` | hardCapSatisfied=false 时 settle 仍继续——budget hard cap 未在结算时强制(§26) | 已修复（源码/测试） | 原缺口来自旧 settle 路径只计算 `hardCapSatisfied` 却没有把失败条件真正变成 fail-closed 门禁；在边界条件下调用方仍可能继续生成 ledger 结果。 | 当前 `src/platform/five-plane-execution/budget-allocator.ts` 的 `settle()` 已把 `hardCapSatisfied` 作为 `budgetPrecondition` 交给 `RuntimeStateMachine.transition()`，并在 ledger 更新前再次显式抛出 `budget.settle.hard_cap_not_satisfied`，不再允许超 hard cap 结算继续推进。`tests/unit/platform/execution/budget-allocator.test.ts` 的 `BudgetAllocator rejects settlement that exceeds the hard cap or reservation` 已定向通过。 | 关闭依据是现行 fail-closed 逻辑与定向回归，不把这条继续挂成“未复核”。 |
| R17-12 | §84 Orchestration/Execution 深层缺陷 | P1 | `worker-service-identity.ts:29-31` | register() 静默覆写已有 identity 无验证——可劫持合法 worker mTLS 指纹 | 已解决（源码/测试） | 根因不是当前 registry 仍会对已验证 identity 做静默覆写，而是复审条目没有吸收后续加上的 verified overwrite guard。 | 当前 `src/platform/five-plane-execution/worker-pool/worker-service-identity.ts` 在 `registrationVerifiedAt` 和既有 `mtlsPeerFingerprint` 存在时，会拒绝不同指纹的重新注册并抛出 `worker_identity.verified_identity_overwrite_denied`。本轮新增 `tests/unit/platform/five-plane-execution/worker-pool/worker-service-identity-regressions.test.ts`，验证已验证 worker 不能被不同 mTLS 指纹覆盖。 | 关闭的是“verified worker identity 可被劫持”缺口，不外推为完整 worker onboarding 流程都已完备。 |
| R17-13 | §84 Orchestration/Execution 深层缺陷 | P2 | `delegation-manager.service.ts:55-57` | 所有 delegation 状态纯内存——重启丢失活跃 delegations | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。 | 不宣称已修复。 |
| R17-14 | §84 Orchestration/Execution 深层缺陷 | P2 | `delegation-manager.service.ts:282` | Spec 定义 10态状态机——实现仅 6态，缺 capability_discovery/bid/award/verified/closed | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。 | 不宣称已修复。 |
| R17-15 | §84 Orchestration/Execution 深层缺陷 | P2 | `delegation-manager.service.ts:463-466` | timeout 无上限——可设 MAX_SAFE_INTEGER 创建不死 delegation | 已解决（源码/测试） | 根因不是现行 delegation 还会把任意大的 timeout 原样落库，而是复审条目停留在 clamp 逻辑补上之前。 | 当前 `src/platform/five-plane-orchestration/agent-delegation/delegation-manager.service.ts` 在 `createDelegationRecord()` 中已把 timeout 限制为 `MAX_DELEGATION_TIMEOUT_MS = 30 days`；本轮新增 `tests/unit/platform/five-plane-orchestration/agent-delegation/call-depth-budget-regressions.test.ts`，验证即使传入 `Number.MAX_SAFE_INTEGER`，最终 TTL 也会被压到 30 天窗口内。 | 关闭的是 delegation TTL 上限缺口。 |
| R17-16 | §84 Orchestration/Execution 深层缺陷 | P2 | `execution-dispatch-service.ts:357` | 始终选 eligibleWorkers[0]——等分 worker 间无随机化，确定性热点 | 已解决（源码修复并补定向测试） | 根因复核后更准确：调度链并不是简单拿 `eligibleWorkers[0]`，而是先按 dispatch score 排序；但在所有得分和负载指标完全相同的 tie group 里，旧实现仍固定回退到 `workerId.localeCompare(...)`，这会让一组完全等价的 worker 永远偏向同一台。 | 本轮修改 `src/platform/five-plane-execution/dispatcher/execution-dispatch-service.ts`，在 tie group 内改用 `ticket.id/executionId/taskId` 的稳定哈希做 deterministic tie-break，同一 ticket 仍可重复得到同一结果，但不同 ticket 会在等价 worker 之间稳定分散。新增 `tests/unit/platform/five-plane-execution/dispatcher/execution-dispatch-service-tie-break.test.ts` 已验证“同 ticket 重复选择稳定、不同 ticket 不再总落同一 worker”。 | 关闭的是“完全等价 worker 的确定性热点”缺口；这仍保持 deterministic dispatch，不引入真正随机抖动。 |
| R17-17 | §84 Orchestration/Execution 深层缺陷 | P2 | `command-security.ts:54-111` | DEFAULT_COMMAND_POLICY_ENTRIES 有 mkdir/touch 重复条目——Map 静默丢弃前者配置 | 已解决（源码/测试） | 根因不是当前 policy entry 列表还保留重复 `touch/mkdir`，而是复审条目没有吸收后续去重。 | 当前 `src/platform/five-plane-execution/tool-executor/command-security.ts` 的 `DEFAULT_COMMAND_POLICY_ENTRIES` 里 `touch` 与 `mkdir` 都只保留 1 条定义；本轮在 `tests/unit/platform/execution/tool-executor/command-security.test.ts` 新增 `createDefaultCommandPolicies keeps single touch and mkdir entries`，直接验证生成的 policy map 中两者都只有 1 个键。 | 属于源码已修、文档滞后的陈旧挂账。 |
| R17-18 | §84 Orchestration/Execution 深层缺陷 | P2 | `improvement-candidate-registry.ts:17` | 内存 Map 无 eviction/size cap——无界内存增长 | 已解决（源码/测试） | 根因不是现行 registry 仍无上界，而是复审条目停留在加上 `maxSize + accessOrder + evictIfNeeded()` 之前；本轮复核时还顺手发现 `register()` 构造的 candidate 缺少 canonical schema 必填字段，于是一起补成合法对象，避免 eviction 测试落在无效输入上。 | 当前 `src/platform/five-plane-orchestration/improve-rollout/improvement-candidate-registry.ts` 已通过 `maxSize` 和 LRU `accessOrder` 做容量收敛，超过上限时会驱逐最久未使用 candidate；同时 `register()` 现在会补齐 `learningObjectId/source/targetScope/priority`，保证产物满足 canonical schema。新增 `tests/unit/platform/five-plane-orchestration/improvement-candidate-registry-regressions.test.ts` 已验证 `maxSize=2` 时第三次注册会驱逐最旧 candidate。 | 关闭的是 registry 无界增长缺口；它仍是内存 registry，但已不是“无上限增长”。 |
| R17-19 | §84 Orchestration/Execution 深层缺陷 | P2 | `escalation/index.ts:38` | 成本阈值硬编码 $10 无 tenant/domain 可配——§12 要求策略驱动 | 已解决（源码/测试） | 根因不是现行实现仍把 `$10` 写死为唯一阈值，而是复审条目没有跟上请求级策略参数的引入。 | 当前 `src/platform/five-plane-orchestration/escalation/index.ts` 的 `EscalationRequest` 已暴露 `costThresholdUsd?: number`，`decide()` 会优先使用 `input.costThresholdUsd`，只有缺省时才回退到 `DEFAULT_COST_THRESHOLD_USD`；本轮补了 `tests/unit/platform/five-plane-orchestration/escalation-policy-regressions.test.ts` 验证不同请求阈值会改变审批决策。 | 关闭的是“完全硬编码不可配”这一条；真正的 tenant/domain 级策略下发链仍取决于调用方如何填充该字段。 |
| R17-20 | §85 Interaction/NL/Autonomy 深层缺陷 | P0 | `nl-gateway/index.ts:677-762` | buildTask() 跳过 ClarificationSession→ConfirmedTaskSpec 管线(§5.3)；直接从 parse 结果构建 RequestEnvelope | 已解决（源码修复并补定向测试） | 根因是 `buildTask()` 过去把 parse 结果直接映射成 legacy `RequestEnvelope`，确认/澄清只停留在 UI 侧提示，没有先物化 canonical `TaskDraft → ClarificationSession → ConfirmedTaskSpec → RequestEnvelope` intake 产物。 | 当前 `src/interaction/nl-gateway/index.ts` 已在 `buildTask()` 中显式创建 `canonicalTaskDraft`、`clarificationSession`、`confirmedTaskSpec`、`canonicalRequestEnvelope`，并在确认未完成时保持 `requestEnvelope: null`；定向测试 `tests/unit/interaction/nl-gateway/request-envelope-confirmation-gate.test.ts` 已验证 canonical intake artifacts 会在 admission 前被物化。 | 已改生产代码并补最小回归。 |
| R17-21 | §85 Interaction/NL/Autonomy 深层缺陷 | P0 | `nl-gateway/index.ts:164` | UserConfirmationReceipt 状态机未和 `not_required / pending_user_confirmation / confirmed` 生命周期对齐 | 已解决（源码修复并补定向测试） | 根因是无需确认分支过去沿用了“已确认”语义，导致 `UserConfirmationReceipt.state` 不能正确区分 `not_required` 与真正的 `confirmed` 生命周期。 | 当前 `src/interaction/nl-gateway/index.ts` 的 `UserConfirmationReceipt` 已声明 `not_required`、`pending_user_confirmation`、`confirmed` 三态，`buildTask()` 也会在无需确认时产出 `not_required`、在待确认时产出 `pending_user_confirmation`；定向测试 `tests/unit/interaction/nl-gateway/request-envelope-confirmation-gate.test.ts` 已覆盖这两个运行时分支。 | 已改生产代码并补最小回归。 |
| R17-22 | §85 Interaction/NL/Autonomy 深层缺陷 | P0 | `dashboard-websocket-server.ts:106` | registerClient() 仅接受 dashboardIds——无 principal/tenantId/scope/auth 检查(§7.1 要求认证绑定+租户过滤) | 已解决（源码修复并补定向测试） | 真实根因有两段：第一，连接注册过去只做 `principal/tenantId` 存在性校验，没有把订阅 channel 绑定到可执行的授权 scope；第二，`DashboardDelta` 之前不携带 `tenantId/visibilityScope`，导致推送路径即使记录了租户，也无法在服务端做逐事件 tenant 过滤，只能按 channel 广播。 | 本轮在 `src/interaction/dashboard/dashboard-websocket-server.ts` 增加 `DashboardSubscriptionAuthorization`，`registerClient()` / `updateSubscriptions()` 会对 `global/task/approvals/admin` 订阅做 fail-closed scope 校验，`task` 订阅还会校验 `filterId` 与授权 task 集；同时 `findAffectedClientIds()` 已按 `delta.tenantId + visibilityScope` 做服务端 tenant 过滤。为让过滤有权威输入，`src/interaction/dashboard/dashboard-projection-service.ts` 也为 `DashboardDelta` 增加了 `tenantId` 与 `visibilityScope`。定向测试 `tests/unit/interaction/dashboard/dashboard-websocket-auth-binding.test.ts`、`dashboard-websocket-replay-and-authorization.test.ts`、`dashboard-projection-tenant-metadata.test.ts` 已验证认证绑定、channel 授权拒绝、task scope 校验和 tenant 级事件过滤。 | 已改生产代码并补最小回归，不再把“已绑定 principal”误写成“已完成鉴权”。 |
| R17-23 | §85 Interaction/NL/Autonomy 深层缺陷 | P0 | `proactive-agent/index.ts:316-322` | evaluate() 在 requireConfirmation=false 时允许 auto_execute 不管 riskLevel——critical-risk 可自动触发(§41.1 medium+必须 suggestion mode) | 已解决（源码修复并补定向测试） | 根因是 `evaluate()` 过去把 low-risk 的 `resolveTriggerActionMode()` 逻辑错误复用到了 medium/high 主动触发路径上，只要 `requireConfirmation=false` 且 autonomy 提升到 `full_auto`，就会把 medium/high 触发也放进 `auto_execute`。 | 当前 `src/interaction/proactive-agent/index.ts` 已把 `high` 和 `medium` 统一钉死为 `suggest`，`critical` 钉死为 `silent_record`，仅 low risk 走标准自动执行判定；定向测试 `tests/unit/interaction/proactive-agent/index.test.ts` 已验证 medium risk 在 `full_auto` 下仍保持 `suggest`。 | 已改生产代码并补最小回归。 |
| R17-24 | §85 Interaction/NL/Autonomy 深层缺陷 | P1 | `nl-gateway/index.ts:804-806` | ConversationContextManager 纯内存 Map——§39.1 要求持久化到 Memory 跨会话恢复 | 已解决（源码修复并补定向测试） | 真正根因不是 manager 自身完全不支持 Memory，而是 `ConversationContextManager` 虽然早已有 `remember()/findMemories()` 逻辑，`NlEntryService` 却没有提供 `memoryService` 注入入口，生产入口始终只能跑 in-memory 模式。 | 本轮在 `src/interaction/nl-gateway/index.ts` 为 `NlEntryServiceOptions` 增加 `memoryService`，并把它透传给 `ConversationContextManager`；定向测试 `tests/unit/interaction/nl-gateway/dry-run-preview-and-memory.test.ts` 已验证一次请求写入 Memory 后，新 service 实例能恢复上一轮上下文。 | 这是把“已有持久化能力但入口断线”的问题真正接通。 |
| R17-25 | §85 Interaction/NL/Autonomy 深层缺陷 | P1 | `nl-gateway/index.ts:620-675` | parseDetailed() 不消费 ConversationContextManager——多轮上下文从不注入意图解析(§39.5) | 已解决 | 原审计停留在更早的 `parseDetailed()` 版本，没有复核后续加上的 prior context 读取与 router 注入。 | 当前 `src/interaction/nl-gateway/index.ts` 的 `parseDetailed()` 会先 `getContext()`，再将 `priorConversationContext` 传入 `intakeRouter.route(...)`，返回值里也保留 `priorConversationTurns`；本轮新增 `tests/unit/interaction/nl-gateway/dry-run-preview-and-memory.test.ts` 已验证第二次解析确实拿到了前一轮对话。 | 不改主逻辑，仅回写现行代码和定向测试依据。 |
| R17-26 | §85 Interaction/NL/Autonomy 深层缺陷 | P1 | `intent-parser/index.ts:6-21` | Intent parser 纯 regex/keyword——§39.7 要求 LLM-based 多语言意图识别 | 已解决（源码修复并补定向测试） | 真实根因不是 parser 模块缺失，而是 authoritative NL intake 主路径长期绕过了它：`NlEntryService.parseDetailed()` 过去只调用 `IntakeRouter` 的关键词/启发式分类，仓内虽然已有 `LlmIntentParser`，但它既没有被 `NlEntryService` 实例化，也没有把其结果反馈到 routing/classification 主链。 | 本轮已在 `src/interaction/nl-gateway/index.ts` 为 `NlEntryService` 增加 `intentParser/intentModelGateway` 注入，并在 `parseDetailed()` 中先调用 `intentParser.parseWithLlm()`，再把结果作为 `preferredIntent` 传给 `src/platform/five-plane-orchestration/routing/intake-router.ts`；`src/interaction/nl-gateway/intent-parser/index.ts` 也已修正为直接消费 `modelGateway.complete(prompt): Promise<string>`。定向测试 `tests/unit/interaction/nl-gateway/slot-resolution-wiring.test.ts` 已新增日文审批请求回归，直接验证 authoritative NL 主路径会消费 LLM parser 结果并产出 `approval_action`。 | 已改生产代码并补最小回归，收口的是“主路径未接线”的真实根因。 |
| R17-27 | §85 Interaction/NL/Autonomy 深层缺陷 | P1 | `slot-resolver/index.ts:3-17` | resolveRequiredSlots() 导出但从未被 NlEntryService 调用——域 slot 验证为死代码 | 已解决（源码修复并补定向测试） | 根因是 `SlotResolver` 虽然已经实现 required slot 解析、clarification question 生成和 prior-context 补槽，但 `NlEntryService.parseDetailed()` 过去完全不调用它，只靠 `slotConfidence` 和若干通用问题做启发式判断，导致“缺槽”与“已从前文补槽”都不会进入 authoritative clarification 主链。 | 本轮在 `src/interaction/nl-gateway/index.ts` 接入 `SlotResolver`：`parseDetailed()` 现在会推导 required slots，调用 `resolveRequiredSlots()`，把 `nl_gateway.required_slots_missing`、生成问题、以及 `requiredSlots/missingSlots/resolvedSlots` 真实写入 clarification/context，并允许用 prior conversation context 消除缺槽。定向测试 `tests/unit/interaction/nl-gateway/slot-resolution-wiring.test.ts` 已验证缺失日期槽位会触发 clarification，且前一轮日期上下文可在第二轮自动补齐。 | 已改生产代码并补最小回归。 |
| R17-28 | §85 Interaction/NL/Autonomy 深层缺陷 | P1 | `goal-decomposer/index.ts:66-73` | GoalLifecycleState 缺 "partially_completed"(§40.5) | 已解决 | 原审计基于旧版生命周期枚举，没有复核 `goal-decomposer` 当前导出的 authoritative 状态集。 | 当前 `src/interaction/goal-decomposer/index.ts` 的 `GoalLifecycleState` 已包含 `partially_completed`。 | 不改生产代码，仅回写现行类型依据。 |
| R17-29 | §85 Interaction/NL/Autonomy 深层缺陷 | P1 | `goal-decomposer/index.ts:200-218` | parseConstraintEnvelope 提取 budgetLimitUsd 但不按子任务比例分配(§40.2) | 已解决 | 原审计没有复核当前 decomposition 主链里已经落地的 `budgetAllocations` 生成。 | 当前 `src/interaction/goal-decomposer/index.ts` 会在 `parseConstraintEnvelope()` 和 `decompose()` 中按 task 成本占比与风险倍率生成 `budgetAllocations`；相关定向测试 `tests/unit/interaction/goal-decomposer/goal-decomposer.test.ts` 已覆盖分配总额与比例行为。 | 不改生产代码，仅回写现行实现依据。 |
| R17-30 | §85 Interaction/NL/Autonomy 深层缺陷 | P1 | `autonomy/index.ts:155-167` | scoreCapability() 返回 0-100——§42.1 定义 0-1000；10x 不匹配 | 已解决 | 原审计停留在更早的 trust score 量纲；当前 authoritative 实现已按 0-1000 计算成功率、override penalty、incident penalty 和 volume bonus。 | 当前 `src/interaction/autonomy/index.ts` 的 `scoreCapability()` 以 `success * 1000` 为基底并 clamp 到 `0..1000`；新增定向测试 `tests/unit/interaction/autonomy/autonomy-audit-round-evidence.test.ts` 已验证高质量 profile 的 `decision.trustScore` 为 `1000`。 | 不改主逻辑，仅回写现行评分实现与定向测试依据。 |
| R17-31 | §85 Interaction/NL/Autonomy 深层缺陷 | P1 | `autonomy/index.ts:296-330` | evaluateProfile() auto-promotes approvedBy:"auto"——§42.2 要求 domain_owner/platform_team 审批 | 已解决 | 原审计基于旧版 change-event 归因逻辑；当前 promotion attribution 已把 rule-engine promotion 与 incident/demotion 分开。 | 当前 `src/interaction/autonomy/index.ts` 的 `evaluateProfile()` 在 promotion 路径上会把 `approvedBy` 写成 `domain_owner`，仅 incident-driven 和 demotion 保留 `auto`；定向测试 `tests/unit/interaction/autonomy/progressive-autonomy-approval-attribution.test.ts` 已验证 rule-engine promotion 不再写 `auto`。 | 不改主逻辑，仅回写现行归因实现与定向测试依据。 |
| R17-32 | §85 Interaction/NL/Autonomy 深层缺陷 | P1 | `autonomy/index.ts:203-250` | decideLevel() 无成本/预算检查——§42.2 "成本超预算200%→降至supervised" 缺失 | 已解决（源码修复并补定向测试） | 真正根因不是完全没有成本字段，而是 `decideLevel()` 把 `costOverruns` 错当成“超支比例”再去除以 `totalExecutions`，导致只要 `costOverruns` 表示“发生过几次 200% 超支”，该分支就几乎永远达不到阈值，等同于预算 guard 不可达。 | 本轮在 `src/interaction/autonomy/index.ts` 把该规则改为只要记录到任一 `costOverruns > 0` 就降到 `supervised`；定向测试 `tests/unit/interaction/autonomy/autonomy-audit-round-evidence.test.ts` 已验证出现一次 200% 超预算执行后，`decision.level` 会真实降到 `supervised`。 | 已改生产代码并补最小回归，避免把预算 guard 留成不可达分支。 |
| R17-33 | §85 Interaction/NL/Autonomy 深层缺陷 | P1 | `dashboard-websocket-server.ts:330-347` | 无 last_event_id/delta replay/stream_gap——§7.1 要求断连恢复+gap 检测 | 已解决（源码修复并补定向测试） | 根因不是“字段没接进来”，而是恢复链路此前只有接口参数没有状态：服务端既不保存可回放 delta，也不在 reconnect 时按当前 tenant/channel 计算 replay 集，更不会在 `lastEventId` 不可回放时发出 `stream_gap`。 | 本轮在 `src/interaction/dashboard/dashboard-websocket-server.ts` 增加了有 retention/size 上限的 replay buffer；`pushDelta()` 会记录可回放 delta，`registerClient(lastEventId)` 会按当前连接的 tenant/channel scope 计算 `missedEvents`，当 `lastEventId` 超出可回放窗口或不再可重建时返回 `stream_gap` 消息与 `resync_from_snapshot` 指令。定向测试 `tests/unit/interaction/dashboard/dashboard-websocket-replay-and-authorization.test.ts` 已验证同租户同 scope replay 命中，以及 lastEventId 不可回放时确实返回 `stream_gap`。 | 这是把“lastEventId 只是占位参数”修成了真正可执行的 reconnect recovery。 |
| R17-34 | §85 Interaction/NL/Autonomy 深层缺陷 | P1 | `ux/workflow-builder-service.ts:153-196` | Builder 自建 nodes/edges 不映射 PlanNode/PlanEdge/ConstraintPack——§44.3 要求可视化构建器为 PlanGraph projection | 已解决（源码修复并补定向测试） | 根因是 `WorkflowBuilderService` 过去只产出 UI 自己的 `canvas.nodes/canvas.edges` 与若干本地校验结果，没有任何 canonical `PlanNode/PlanEdge/constraintPackRef` 投影，因此 builder 保存/预览结果无法直接落到平台主架构要求的 PlanGraph 语义。 | 本轮在 `src/interaction/ux/workflow-builder-service.ts` 为 `saveReview` 增加 `canonicalPlanGraph`，把 builder canvas 映射为 canonical `PlanNode[] / PlanEdge[] / entryNodeIds / terminalNodeIds / constraintPackRef`；同时补了 legacy 模板到 canonical 模板的归一化，避免旧模板输入在 builder 入口直接失配。定向测试 `tests/unit/interaction/ux/workflow-builder-service.test.ts` 已验证 `canonicalPlanGraph` 会产出 `PlanNode/PlanEdge`、`constraintPackRef` 和首末节点集合。 | 已改生产代码并补最小回归。 |
| R17-35 | §85 Interaction/NL/Autonomy 深层缺陷 | P2 | `nl-gateway/index.ts:448-461` | detectPromptInjection() 用 pattern.exec() 仅报第一个匹配——多注入仅报首个 | 已解决 | 原审计基于旧版单次匹配逻辑；当前实现已经改成对每条 pattern 做 `matchAll()` 聚合，因此不会在首个命中后提前停止。 | 当前 `src/interaction/nl-gateway/index.ts` 的 `detectPromptInjection()` 会遍历所有 `PROMPT_INJECTION_PATTERNS` 并累计每个命中；新增定向测试 `tests/unit/interaction/nl-gateway/slot-resolution-wiring.test.ts` 已验证同一条消息里的 `ignore`、`bypass`、`reveal` 会全部生成 finding。 | 不改主逻辑，仅补直接行为回归，防止旧单命中实现回归。 |
| R17-36 | §85 Interaction/NL/Autonomy 深层缺陷 | P2 | `nl-gateway/index.ts:677` | 无 dry-run preview for high/critical NL 请求(§39.1) | 已解决（源码修复并补定向测试） | 根因与 `R9-41` 同源：高风险 NL 请求之前只有风险标签和确认回执，没有结构化预演对象，用户看不到确认前将要发生什么。 | 当前 `src/interaction/nl-gateway/index.ts` 已为 high/critical 请求生成 `DryRunPreview`，并把 scope / operations / sideEffectPreview / policyChecks 一并返回；`tests/unit/interaction/nl-gateway/dry-run-preview-and-memory.test.ts` 已定向验证。 | 与 `R9-41` 同组收口，但这里保留 Interaction 子系统视角的直接源码依据。 |
| R17-37 | §85 Interaction/NL/Autonomy 深层缺陷 | P2 | `goal-decomposer/index.ts:245-350` | 分解前无 capability validation against DomainDescriptor(§40.2) | 已解决（源码修复并补定向测试） | 根因与 `R9-42` 同源：旧实现虽然有校验函数，但会在校验前预过滤 capability/permission，导致 DomainDescriptor 缺口在真正验证前就消失。 | 当前 `src/interaction/goal-decomposer/index.ts` 已保留任务真实请求的 capability/permission，并让 `validateTaskDomainCapabilities()` 的失配结果进入 `validationMessages` 且触发 `requiresHumanReview`；定向测试 `tests/unit/interaction/goal-decomposer/index.test.ts` 与 `task-domain-capability-policy.test.ts` 已通过。 | 已改生产代码并补最小回归。 |
| R17-38 | §85 Interaction/NL/Autonomy 深层缺陷 | P2 | `dashboard-projection-service.ts:40-42` | 仅3种 projection type——§43.2-43.5 要求 agent health/SLO/budget/approval/resource/cost 等 | 已解决（源码修复并补定向测试） | 根因不是“配置里没有这些 projection 名称”，而是实现层长期停在半接线状态：`DashboardProjectionService` 的 `projectionNames` 列表虽然已经列出 `agent_health / agent_slo / agent_budget / approval_summary / resource_usage / cost_summary`，但 `deriveChanges()` 与 `buildStateFromProjections()` 过去仍基本只消费 `task_summary / incident_summary / workflow_summary` 三类。 | 本轮在 `src/interaction/dashboard/dashboard-projection-service.ts` 补齐了扩展 projection 的 delta/state 接线：`approval_summary` 现在会生成 `approval_requested/approval_resolved`，`agent_health/agent_slo/agent_budget/resource_usage/cost_summary` 会进入系统健康或成本聚合；`buildStateFromProjections()` 也会把这些 projection 纳入 `activeAgents / approvalPendingCount / budgetUtilizationPercent` 等状态字段。定向测试 `tests/unit/interaction/dashboard/dashboard-projection.test.ts` 已验证扩展 projection 的 delta 生成与状态聚合。 | 已改生产代码并补最小回归，不再把“名称已声明”误写成“实现已接通”。 |
| R17-39 | §85 Interaction/NL/Autonomy 深层缺陷 | P2 | `dashboard-websocket-server.ts:67-69` | 无 schema_version 协商(UI arch §1.8) | 已解决 | 原审计停留在 WebSocket 注册接口还没有版本协商参数的旧快照。 | 当前 `src/interaction/dashboard/dashboard-websocket-server.ts` 的 `registerClient()` 已接收 `schemaVersion`，连接状态中会保存该值，并在 `connection_ack` 回显 negotiated `schemaVersion`；定向测试 `tests/unit/interaction/dashboard/dashboard-websocket-auth-binding.test.ts` 已覆盖这一行为。 | 不改生产代码，仅回写现行接口与定向测试依据。 |
| R17-40 | §85 Interaction/NL/Autonomy 深层缺陷 | P2 | `proactive-agent/index.ts:200-231` | trigger declarations 不验证 against DomainDescriptor(§41.1) | 已解决 | 原审计基于 `registerTrigger()` 还只做 declared-trigger 检查的旧实现，没有复核后续接入的 domain descriptor validator。 | 当前 `src/interaction/proactive-agent/index.ts` 的 `ProactiveAgentServiceOptions` 已暴露 `domainDescriptorValidator`，`registerTrigger()` 会在登记前执行该校验并在失败时抛出 `proactive_agent.domain_descriptor_validation_failed`；定向测试 `tests/unit/interaction/proactive-agent/index.test.ts` 已覆盖该拒绝路径。 | 不改生产代码，仅回写现行校验链路与定向测试依据。 |
| R17-41 | §85 Interaction/NL/Autonomy 深层缺陷 | P2 | `autonomy/promotion-engine/index.ts:14-38` | assessPromotion 忽略 incident severity 分级和 humanOverrides rate(§42.2) | 已解决 | 原审计停留在 promotion engine 只看 success/volume 的旧快照，没有复核后续补进来的 incident severity 分级和 override-rate gate。 | 当前 `src/interaction/autonomy/promotion-engine/index.ts` 的 `assessPromotion()` 已显式处理 `lastIncidentSeverity` 的 `P0/P1` 分支，并按 `humanOverrides / totalExecutions` 对 `supervised→semi_auto` 与 `semi_auto→full_auto` 做 override-rate 阻断；定向测试 `tests/unit/interaction/autonomy/promotion-engine.test.ts` 与 `tests/unit/interaction/autonomy/promotion-engine/index.test.ts` 已验证这些分支。 | 不改主逻辑，仅回写现行实现与定向测试依据。 |
| R17-42 | §86 Desktop/Mobile 应用 + Platform Stability 缺陷 | P0 | `ui/apps/electron-win/package.json` | 无 electron 依赖声明——`electron .` 启动必失败 | 已解决（源码复核） | 根因是复审条目停留在 Electron 壳刚建目录、尚未声明运行时依赖的旧快照；当前包清单已经能直接反证这条指控。 | 当前 `ui/apps/electron-win/package.json` 已声明 `electron` 与 `electron-updater`，`scripts.dev` 也保留 `electron .` 启动入口。该条关闭的是“无 electron 依赖声明”的原始指控，不外推为完整打包发布链已做过真机构建验收。 | 不改生产代码，仅回写现行 package 依据。 |
| R17-43 | §86 Desktop/Mobile 应用 + Platform Stability 缺陷 | P0 | `ui/apps/electron-win/src/main.ts` | 无 BrowserWindow/app.whenReady()/Tray/Menu——仅导出配置对象非 Electron 主进程 | 已解决（源码/测试） | 根因不是主进程完全缺失，而是复审结论停在更早的骨架阶段，没有吸收后续 `main.ts` 已接入 Electron lifecycle 的现状。 | 当前 `ui/apps/electron-win/src/main.ts` 已真实使用 `app.whenReady()`、`BrowserWindow`、`Tray`、`Menu`、`ipcMain`，并在 `ready/window-all-closed/before-quit` 上注册生命周期处理；`ui/tests/unit/ui/apps/electron-win/main.test.ts` 也已覆盖 `createMainWindow()` 的 HTML 装载。 | 关闭的是“只有配置对象、无主进程生命周期”的旧结论。 |
| R17-44 | §86 Desktop/Mobile 应用 + Platform Stability 缺陷 | P0 | `ui/apps/electron-win/src/preload.ts` | 无 contextBridge.exposeInMainWorld()——IPC channel 声明但未接线 | 已解决（源码/测试） | 根因是旧 preload 实现曾依赖直接 window mutation 暴露 bridge，后续已切到 `contextBridge`，但重复挂账没有同步回写。 | 当前 `ui/apps/electron-win/src/preload.ts` 的 `installElectronBridge()` 已通过 `resolveContextBridge()?.exposeInMainWorld("AA_ELECTRON", bridge)` 暴露桥；`ui/tests/unit/ui/apps/electron-win/preload.test.ts` 也已验证会调用 `exposeInMainWorld`，且目标 window 不会被直接污染。 | 作为 `2163/R30-43` 的重复挂账一并关闭。 |
| R17-45 | §86 Desktop/Mobile 应用 + Platform Stability 缺陷 | P0 | `ui/apps/tauri-macos/src-tauri/src/lib.rs:12` | run_shell 执行任意命令无沙箱——违反 §2.5.3 App Sandbox+Hardened Runtime | 已解决（源码/测试） | 根因是早期 Tauri shell bridge 确实会把 renderer 输入直接透传到 `run_shell`，没有任何命令层面的 fail-closed 限制。 | 当前 `ui/apps/tauri-macos/src-tauri/src/lib.rs` 已把 `run_shell` 收敛到 `ALLOWED_COMMANDS = ["status","health","version"]` allowlist，非白名单命令会直接返回 `Err("Command not allowed: ...")`；`ui/tests/unit/ui/apps/tauri-shell-security.test.ts` 已验证 macOS 路径的 allowlist 与拒绝分支。 | 关闭的是“任意命令执行”这一安全缺口，不外推为完整 macOS 沙箱/签名体系都已完成。 |
| R17-46 | §86 Desktop/Mobile 应用 + Platform Stability 缺陷 | P0 | `ui/apps/tauri-linux/src-tauri/src/lib.rs:12` | 同上——run_shell 无限制任意命令执行 | 已解决（源码/测试） | 根因与 `R17-45` 同源：Linux Tauri bridge 之前也缺命令级 allowlist，`run_shell` 会接受任意 renderer 输入。 | 当前 `ui/apps/tauri-linux/src-tauri/src/lib.rs` 同样已将 `run_shell` 收敛到 `["status","health","version"]` allowlist，非白名单命令 fail-closed 返回错误；`ui/tests/unit/ui/apps/tauri-shell-security.test.ts` 已验证 Linux 路径的同样行为。 | 关闭的是“任意命令执行”这一安全缺口，不外推为 Linux 桌面整组集成都已完成。 |
| R17-47 | §86 Desktop/Mobile 应用 + Platform Stability 缺陷 | P0 | `ui/apps/mobile/package.json` | 无 react-native 依赖——app 无法构建 | 已解决（源码复核） | 根因是复审条目引用了 mobile shell 初始占位阶段；现行 mobile app 包清单已经声明了 React Native 运行时依赖。 | 当前 `ui/apps/mobile/package.json` 已声明 `react-native` 与 `react` 依赖。该条关闭的是“连依赖都没有”的原始指控，不外推为原生工程、打包签名或真机运行都已验收。 | 不改生产代码，仅回写现行 package 依据。 |
| R17-48 | §86 Desktop/Mobile 应用 + Platform Stability 缺陷 | P0 | `ui/apps/mobile/src/App.tsx:7` | 使用 HTML div/span 非 RN View/Text——设备上 crash | 已解决（源码/测试） | 根因是移动壳根组件之前直接复用了 Web DOM 标签，React Native 运行时不会识别 `div/span/strong`，因此问题不是“样式不原生”，而是宿主组件类型本身错误。 | 当前 `ui/apps/mobile/src/App.tsx` 已切到 `react-native` 的 `View/Text/StyleSheet`；`ui/tests/unit/ui/apps/mobile/App.test.tsx` 通过 mock RN primitives 验证 Android/iOS UA 检测、adapter 注入和 bridge 状态展示，定向测试已通过。 | 关闭的是 mobile root component 用 DOM host tags 的 crash 缺口，不外推为整套 RN navigation/native module 已全部产品化。 |
| R17-49 | §86 Desktop/Mobile 应用 + Platform Stability 缺陷 | P0 | `src/platform/stability/` | SLO tracking 模块完全不存在——§27 mandates per-domain SLO+error budget+breach response | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。 | 不宣称已修复。 |
| R17-50 | §86 Desktop/Mobile 应用 + Platform Stability 缺陷 | P1 | `ui/apps/electron-win/` | §2.5.2 system tray/Jump List/Notification Center/multi-window 全缺 | 已解决（源码/测试） | 根因分成两段：其一，tray 能力其实早已存在，但复审条目没有跟进；其二，Jump List、桌面通知和多窗口入口此前确实没有接进主进程，因此“全缺”虽然过时，但剩余三项在这轮之前也还没落地。 | 当前 `ui/apps/electron-win/src/main.ts` 已具备 `createTray()`、`configureWindowsDesktopIntegrations()`、`showPlatformNotification()` 与 `openSecondaryWindow()`；`configureWindowsDesktopIntegrations()` 会调用 `app.setUserTasks(...)` 暴露 Windows Jump List 任务，`showPlatformNotification()` 走 Electron `Notification`，`openSecondaryWindow()` 则提供 secondary window 装载链。定向验证：`ui/tests/unit/ui/apps/electron-win/main.test.ts` 已覆盖 tray manifest、jump-list 任务、notification 和 secondary window HTML 装载。 | 关闭的是 §2.5.2 所点名的四项基线能力缺失，不外推为完整桌面工作流都已产品化。 |
| R17-51 | §86 Desktop/Mobile 应用 + Platform Stability 缺陷 | P1 | `ui/apps/electron-win/` | §2.5.2 keyboard shortcuts(Ctrl+K/N/Shift+D) 无 globalShortcut 注册 | 已解决（源码/测试） | 根因是 Electron 壳之前只在 manifest 上声明 `supportsGlobalShortcuts`，但主进程没有真实 `globalShortcut.register(...)` 接线，所以这不是“文档误报”，而是实现缺口。 | 当前 `ui/apps/electron-win/src/main.ts` 已新增 `electronGlobalShortcuts` 与 `registerGlobalShortcuts()`，实际注册 `CommandOrControl+K`、`CommandOrControl+N`、`Shift+CommandOrControl+D` 三个快捷键，并在 `before-quit` 执行 `globalShortcut.unregisterAll()`。`ui/tests/unit/ui/apps/electron-win/main.test.ts` 已验证三项注册。 | 已改生产代码并补最小回归。 |
| R17-52 | §86 Desktop/Mobile 应用 + Platform Stability 缺陷 | P1 | `ui/apps/electron-win/` | §2.5.2 auto-update(electron-updater) 无代码无依赖 | 已解决（源码/测试） | 根因是复审条目停留在没有 updater 依赖和初始化逻辑的旧阶段；现行 Electron 壳已经同时具备依赖声明和主进程初始化。 | 当前 `ui/apps/electron-win/package.json` 已声明 `electron-updater`，`ui/apps/electron-win/src/main.ts` 也已通过 `resolveAutoUpdater()?.checkForUpdatesAndNotify()` 在非开发环境接线自动更新。相关入口在 `ui/tests/unit/ui/apps/electron-win/main.test.ts` 的主进程基线回归中受覆盖。 | 关闭的是“无代码无依赖”的原始指控，不外推为签名、公钥轮换或真实发布通道都已验收。 |
| R17-53 | §86 Desktop/Mobile 应用 + Platform Stability 缺陷 | P1 | `ui/apps/electron-win/index.html:9` | 无 script 标签加载 app bundle——页面为静态文本 | 已解决（源码/测试） | 根因分两层：其一，`index.html` 之前确实没有任何 renderer 入口；其二，`main.ts` 创建 `BrowserWindow` 后也没有真实 `loadFile()` 页面装载链，导致即使补 HTML 也仍然不会显示动态壳层。 | 当前 `ui/apps/electron-win/index.html` 已新增 `<script type="module" src="./src/renderer.js"></script>`，`ui/apps/electron-win/src/renderer.js` 会实际替换静态占位内容并渲染 Electron shell runtime 状态，`ui/apps/electron-win/src/main.ts` 也已补 `loadFile(join(appRootDir, "index.html"))` 与 `import.meta.url` 路径解析，避免继续依赖不存在的 `__dirname`。定向验证：`ui/tests/unit/ui/apps/security-configs.test.ts` 与 `ui/tests/unit/ui/apps/electron-win/main.test.ts` 已通过。 | 关闭的是“页面只剩静态文本、没有 renderer bootstrap”的缺口；不外推为完整 Electron multi-window/shortcut/update 体系已全部完成。 |
| R17-54 | §86 Desktop/Mobile 应用 + Platform Stability 缺陷 | P1 | `ui/apps/tauri-macos/src-tauri/src/lib.rs` | 仅3个 stub commands(healthcheck/open_deep_link/run_shell)全返回硬编码字符串 | 部分修复（命令面已扩展并完成 shell/deep-link hardening） | 原条目已经不是现状：当前 macOS Tauri lib 不再只有 3 个命令，也不再是全部“硬编码字符串直返”。但 `keychain_*` / `spotlight_export` 仍是 baseline placeholder，没有真实原生插件接线。 | 当前 `ui/apps/tauri-macos/src-tauri/src/lib.rs` 已包含 `healthcheck/open_deep_link/run_shell/keychain_store/keychain_retrieve/spotlight_export` 六个命令，其中 `open_deep_link` 已做 scheme 校验，`run_shell` 已走 allowlist；仍未完成的是 Keychain/Spotlight 的真实插件接线。 | 收窄为部分修复，避免继续把已存在的 hardening 和扩展命令面误写成“只有 3 个 stub”。 |
| R17-55 | §86 Desktop/Mobile 应用 + Platform Stability 缺陷 | P1 | `ui/apps/tauri-macos/src-tauri/Cargo.toml:7` | 无 Tauri plugins(updater/notification/shell/os)——§2.5.3 Menu Bar/Notification/Keychain/Sparkle 全缺 | 部分修复（updater/secure-storage 已声明，其余仍缺） | 根因是原审计把插件状态写成“全无”，但现行 Cargo 清单已经至少接入了更新和安全存储插件；剩余 Notification/Shell/OS 级插件和 Sparkle 类完整集成仍未到位。 | 当前 `ui/apps/tauri-macos/src-tauri/Cargo.toml` 已声明 `tauri-plugin-updater` 与 `tauri-plugin-secure-storage`；未看到 `notification/shell/os` 等插件依赖，也没有完整的 Sparkle 级发布配置。 | 将条目收窄为部分修复，保留剩余原生桌面集成缺口。 |
| R17-56 | §86 Desktop/Mobile 应用 + Platform Stability 缺陷 | P1 | `ui/apps/tauri-macos/src-tauri/tauri.conf.json` | 无 security/allowlist/updater 配置；缺 CSP policy(§6.5.4) | 部分修复（security+CSP 已在位，allowlist/updater config 仍待补） | 原条目中的“缺 CSP policy”已经失真，当前 macOS Tauri config 已显式声明 `security.csp`；但更深的 updater/allowlist 配置仍未完整产品化。 | 当前 `ui/apps/tauri-macos/src-tauri/tauri.conf.json` 已包含 `security.csp` 与 `dangerousDisableAssetCspModification:false`，并在本轮补上 `minWidth/minHeight`；`ui/tests/unit/ui/apps/security-configs.test.ts` 已验证 CSP 与窗口下限。未见完整 updater endpoint/allowlist 配置。 | 收窄为部分修复，避免把已存在的 security/CSP 继续误写成缺失。 |
| R17-57 | §86 Desktop/Mobile 应用 + Platform Stability 缺陷 | P1 | `ui/apps/tauri-linux/` | §2.5.4 system tray(SNI)/D-Bus notifications/Wayland/XDG/dark-light theme 全缺 | 部分修复（D-Bus/Wayland/XDG/theme 已在位，tray 仍缺） | 原条目把 Linux 桌面集成写成“全缺”，但现行 lib.rs 已经提供了多项桌面桥接；仍未补的是 system tray/SNI 这类后台常驻能力。 | 当前 `ui/apps/tauri-linux/src-tauri/src/lib.rs` 已包含 `dbus_notify`、`xdg_open`、`detect_display_server`、`get_theme_preference`，因此 D-Bus、Wayland/XDG、dark-light theme 已不再是缺失项；system tray/SNI 仍未见实现。 | 收窄为部分修复，保留剩余 tray/background desktop integration 缺口。 |
| R17-58 | §86 Desktop/Mobile 应用 + Platform Stability 缺陷 | P1 | `ui/apps/mobile/src/index.ts` | §2.5.5/§2.5.6 push(FCM/APNs)/biometric/offline SQLite/gestures/widgets——manifest 声明但无原生接线 | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。 | 不宣称已修复。 |
| R17-59 | §86 Desktop/Mobile 应用 + Platform Stability 缺陷 | P1 | `ui/apps/mobile/src/navigation.ts` | Navigation 为 plain objects 无 @react-navigation stack/tab(§4.4.2) | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。 | 不宣称已修复。 |
| R17-60 | §86 Desktop/Mobile 应用 + Platform Stability 缺陷 | P1 | `ui/packages/ui-mobile/src/native-modules/index.ts` | Native module 仅为 boolean config——无 NativeModules/TurboModule 绑定 | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。 | 不宣称已修复。 |
| R17-61 | §86 Desktop/Mobile 应用 + Platform Stability 缺陷 | P1 | `ui/packages/ui-mobile/src/components/index.ts` | 无 RN 组件(View/Text/TouchableOpacity)——仅 TS interface+factory | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。 | 不宣称已修复。 |
| R17-62 | §86 Desktop/Mobile 应用 + Platform Stability 缺陷 | P1 | `src/ops-maturity/chaos/chaos-experiment-scheduler.ts:93` | 所有状态纯内存——重启丢失实验状态 | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。 | 不宣称已修复。 |
| R17-63 | §86 Desktop/Mobile 应用 + Platform Stability 缺陷 | P1 | `src/ops-maturity/chaos/` | §61 blast radius 范围守卫——ExperimentTarget 有 labels 但无实际范围限制 | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。 | 不宣称已修复。 |
| R17-64 | §86 Desktop/Mobile 应用 + Platform Stability 缺陷 | P1 | `src/ops-maturity/chaos/:161` | auto-terminate 仅检查时间——假设违反无自动回滚/补偿 | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。 | 不宣称已修复。 |
| R17-65 | §86 Desktop/Mobile 应用 + Platform Stability 缺陷 | P1 | `src/ops-maturity/chaos/:155` | injectFault() 返回 config 但不执行实际注入——stub | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。 | 不宣称已修复。 |
| R17-66 | §86 Desktop/Mobile 应用 + Platform Stability 缺陷 | P2 | `ui/apps/tauri-macos/src-tauri/tauri.conf.json` | 无 minWidth/minHeight(Electron 有 1180×760) | 已解决（源码/测试） | 根因是 Tauri 窗口配置之前只声明了初始 `width/height`，没有最小窗口边界，因此桌面壳在缩放时会偏离同仓 Electron baseline。 | 当前 `ui/apps/tauri-macos/src-tauri/tauri.conf.json` 已补 `minWidth: 1180` 与 `minHeight: 760`；`ui/tests/unit/ui/apps/security-configs.test.ts` 已新增断言验证该窗口下限。 | 已改配置并补最小回归。 |
| R17-67 | §86 Desktop/Mobile 应用 + Platform Stability 缺陷 | P2 | `ui/apps/tauri-linux/src/index.ts:7` | supportsBackgroundAgent:true 但无背景服务代码；其他平台无此字段 | 已解决（源码/测试） | 根因不是 Linux 壳真的已有后台代理实现，而是 manifest 之前虚报了 `supportsBackgroundAgent:true`，把不存在的能力写成已支持。 | 当前 `ui/apps/tauri-linux/src/index.ts` 已把 `tauriLinuxManifest.supportsBackgroundAgent` 改为 `false`，不再对上层或测试宣称有后台代理能力；`ui/tests/unit/ui/apps/tauri-linux/lib.test.ts` 与 `ui/tests/apps/shells.test.ts` 已同步验证这一收敛。 | 这次修的是能力声明漂移，不外推为 Linux 背景服务已经产品化。 |
| R17-68 | §86 Desktop/Mobile 应用 + Platform Stability 缺陷 | P2 | `ui/apps/mobile/metro.config.js` | 无 monorepo workspace resolver——@aa/\* 包无法解析 | 已解决（源码/测试） | 根因是 `apps/mobile` 在 `type:module` workspace 下只有最小 `sourceExts` 配置，既没有 watch workspace root，也没有 node_modules 搜索路径和 package exports 支持，因此 Metro 无法稳定解析 `@aa/*` workspace package。 | 当前 `ui/apps/mobile/metro.config.js` 已改为 ESM 配置，补入 `projectRoot/watchFolders/nodeModulesPaths/extraNodeModules/unstable_enablePackageExports`，明确把 `ui/` workspace 与 `packages/` 纳入解析范围。定向验证：`ui/tests/unit/ui/apps/mobile/metro-config.test.ts` 已验证 workspace watchFolders 和双 node_modules 搜索路径。 | 关闭的是 mobile monorepo resolver 缺失问题，不外推为原生打包链已做过真机构建验证。 |
| R17-69 | §86 Desktop/Mobile 应用 + Platform Stability 缺陷 | P2 | `ui/packages/ui-mobile/src/navigation/index.ts` | §4.4.2 5-tab(Home/Tasks/Approvals/Dashboard/More)——实现含 marketplace 替代 dashboard | 已解决（源码/测试） | 根因不是导航模型整体缺失，而是 `ui-mobile` 共享 tab baseline 里第四个 tab 仍写成 `marketplace`，与 contract 要求的 `dashboard` 不一致，导致跨端共享导航元数据和规格漂移。 | 当前 `ui/packages/ui-mobile/src/navigation/index.ts` 已把第四个 baseline tab 从 `marketplace` 改为 `dashboard`，`ui/packages/ui-mobile/src/components/index.ts` 的 `MobileScreenDescriptor["tab"]` 也同步切到 `dashboard` union。定向验证：`ui/tests/shared/ui-core-mobile.test.tsx` 已通过并继续验证 5-tab baseline 与 route map。 | 关闭的是共享移动 tab baseline 与 §4.4.2 漂移的问题，不外推为 `apps/mobile/src/navigation.ts` 已经换成真实 `@react-navigation`。 |
| R17-70 | §86 Desktop/Mobile 应用 + Platform Stability 缺陷 | P2 | `ui/packages/ui-core/src/components/index.ts` | §6.3.2 缺 Modal/Dialog/Dropdown/Toast/Tabs/Badge/Avatar/Tooltip——仅6组件 | 已解决（源码/测试） | 根因是复审条目停留在 `ui-core` 只有最初几类 base form controls 的旧阶段，没有吸收 `extended.tsx` 已补出的更完整交互组件面。 | 当前 `ui/packages/ui-core/src/components/index.ts` 已 re-export `extended.tsx`，其中实际包含 `Tabs`、`Tooltip`、`Toast`、`Drawer`、`Accordion`、`Pagination`、`Stepper`、`Timeline`、`Card` 等组件，不再是“仅 6 个组件”。定向验证：`ui/tests/unit/ui/packages/ui-core/layout-and-component-baseline.test.tsx` 已直接渲染 `Tabs/Tooltip/Toast`。 | 关闭的是“组件面仍只有 6 个”的陈旧结论，不外推为每个 design-system 组件都已完全产品化。 |
| R17-71 | §86 Desktop/Mobile 应用 + Platform Stability 缺陷 | P2 | `ui/packages/ui-core/src/layouts/index.ts:30` | ThreePaneLayout 固定 CSS grid 无响应式断点(§2.5.1 1440/1024/768px) | 已解决（源码/测试） | 根因是旧布局实现确实使用过固定三列 `minmax(...)` 模板，在窄视口下会溢出；后续虽已改成响应式 `auto-fit`，但复审文档没有回写。 | 当前 `ui/packages/ui-core/src/layouts/index.ts` 的 `ThreePaneLayout` 已改成 `gridTemplateColumns: "repeat(auto-fit, minmax(200px, 1fr))"`，不再锁死三列。定向验证：`ui/tests/unit/ui/packages/ui-core/layout-and-component-baseline.test.tsx` 已断言布局样式包含 `auto-fit` 与 `minmax(200px, 1fr)`。 | 关闭的是固定三列溢出问题，不外推为所有页面断点与视觉回归都已完整验收。 |
| R17-72 | §86 Desktop/Mobile 应用 + Platform Stability 缺陷 | P2 | `src/ops-maturity/chaos/:126` | recordSteadyStateResult 首轮后终止——无持续监控循环(§61) | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。 | 不宣称已修复。 |
| R17-73 | §87 Security/IAM/Compliance 深层缺陷 | P0 | `access-model.ts:234-245` | resolvePrincipalAccessProfile 使用 caller 提供的 capabilities 原样(非空时)绕过 role-capability map——权限提升 | 已解决（源码修复并补定向测试） | 根因是旧实现把调用方显式传入的 capability 列表直接信任为最终权限集，没有与角色继承出的授权边界做交集收敛。 | 当前 `src/platform/five-plane-control-plane/iam/access-model.ts` 已将显式 capability 过滤为 `input.capabilities.filter((capability) => roleCapabilities.includes(capability))`；`tests/unit/platform/control-plane/iam/auth-service.test.ts` 与 `tests/integration/platform/control-plane/iam/identity-provider.test.ts` 已验证 viewer/worker 等角色无法靠显式输入抬权。 | 保留真实代码修复结论，并以单测/集成测试证明 caller-supplied capability 不再可绕过角色授权。 |
| R17-74 | §87 Security/IAM/Compliance 深层缺陷 | P0 | `data-classification-service.ts:673-675` | clearAuditLog() 擦除全部分类审计轨迹无 authz 检查/无 audit-of-clear event——违反 §46 不可变审计 | 已修复（源码修复并补定向测试） | 根因不是单纯“缺少一条 clear 事件”，而是旧实现先把 `audit_log_cleared` 标记追加到同一个可变数组里，再立刻 `length = 0` 清空，结果连“清空审计的审计”也被自己擦掉了。 | 当前 `src/platform/five-plane-control-plane/iam/data-classification-service.ts` 已保留角色校验，并把清空操作改成先清掉旧日志、再以新的首条记录写入 `audit_log_cleared` marker；`tests/unit/platform/control-plane/iam/data-classification-service.test.ts` 已定向验证未授权角色被拒绝、授权角色成功且 audit log 最终保留 1 条 `audit_log_cleared` 证据。 | 这是对不可变审计语义的实修，不是假补日志。 |
| R17-75 | §87 Security/IAM/Compliance 深层缺陷 | P1 | `access-model.ts:341-359` | manualTakeoverActive=true 无条件允许所有动作(含 production/regulated-data)无角色门——接管即全能 | 已解决（源码修复并补定向测试） | 根因是旧实现把 `manualTakeoverActive` 当成通用放行短路条件，没有区分 operator-grade 与普通 runtime principal。 | 当前 `src/platform/five-plane-control-plane/iam/access-model.ts` 已在 manual takeover 分支前增加角色门，仅 `platform_admin`、`human_operator`、`service_operator` 可进入接管态；其他角色返回 `policy.context_manual_takeover_operator_required`。`tests/unit/platform/control-plane/iam/access-model-authorization.test.ts` 已新增非 operator 角色被拒绝的断言。 | 保留真实代码修复结论，并以新增定向单测证明“接管即全能”已被收口。 |
| R17-76 | §87 Security/IAM/Compliance 深层缺陷 | P1 | `field-encryption.ts:14-30` | normalizeKey SHA-256 hash 任何长度 key——接受 1 字节密码无最小强度(§43) | 已解决（源码修复并补定向测试） | 根因与 `R10-05` 一致：旧 `normalizeKey()` 把任意非空输入直接哈希成对称密钥，没有最小强度校验，也没有 KDF 的抗暴力破解特性。 | 当前 `src/platform/five-plane-control-plane/iam/field-encryption.ts` 已改为 16 字节以下口令直接拒绝，16+ 字节口令通过 `scrypt` 派生 key，32-byte raw key 仍保持兼容；`tests/unit/platform/control-plane/iam/field-encryption.test.ts` 已覆盖短口令拒绝。 | 保留真实代码修复结论。 |
| R17-77 | §87 Security/IAM/Compliance 深层缺陷 | P1 | `field-encryption.ts:33-69` | 密文无 key-ID/versioning——密钥轮换需重加密所有数据(§43 要求轮换支持) | 已解决（源码修复并补定向测试） | 根因是旧密文格式只有裸 base64 payload，没有版本号与 key identity，解密端无法判断“格式版本”与“当前 key 是否匹配”。 | 当前 `src/platform/five-plane-control-plane/iam/field-encryption.ts` 已把密文封装为 `fe1:<keyId>:<base64payload>`，解密时校验 `version` 与 `keyId`，同时保留旧 base64 payload 的向后兼容路径；`tests/unit/platform/control-plane/iam/field-encryption.test.ts` 已验证新 envelope 前缀格式与 key mismatch 拒绝。 | 保留真实代码修复结论，并以定向单测证明 version/key-id 已进入现行密文协议。 |
| R17-78 | §87 Security/IAM/Compliance 深层缺陷 | P1 | `policy-engine.ts:93-106` | subjectType 仅 user/agent/system——service/worker/plugin principal 无法进入 policy decision contract | 已解决（源码/测试） | 根因不是 access-model 完全不支持这些 principal，而是 `PolicyDecisionRequest.subjectType` 把 policy-engine 对外 contract 收窄成了三种值，导致 `service`、`worker`、`plugin` 这些已存在于 `PlatformPrincipalType` 的主体类型在进入 PDP 前就被类型层截断。 | 本轮将 `src/platform/five-plane-control-plane/iam/policy-engine.ts` 的 `subjectType` 直接对齐到 `PlatformPrincipalType`，不再把请求 DTO 缩成 `user/agent/system` 子集；`tests/unit/platform/control-plane/iam/policy-engine-edge-cases.test.ts` 已新增 service/worker/plugin 三类主体可进入 `evaluate()` 的回归。 | 关闭的是 policy-engine request contract 缩窄问题，不外推为全部 context-aware authorization 已经由 policy-engine 一次性接管。 |
| R17-79 | §87 Security/IAM/Compliance 深层缺陷 | P1 | `vault-http-secret-provider.ts:147-159` | AppRole login 失败静默吞没(无 throw/log)——降级到 static token 调用者无感知 | 已解决（源码/测试） | 根因是 `getToken()` 在 AppRole 登录非 2xx 或响应缺 `client_token` 时会直接滑入 static token fallback 分支，没有任何可观测信号，导致“凭什么降级”在日志上不可追。 | 本轮在 `src/platform/five-plane-control-plane/iam/vault-http-secret-provider.ts` 的两条 fallback 分支补入结构化 `warn` 日志：一条覆盖 HTTP 失败，一条覆盖响应无 token；既有定向测试 `tests/unit/platform/control-plane/iam/vault-http-secret-provider.test.ts` 继续证明 AppRole 失败时 fallback 到 static token、且无 static token 时仍会抛错。 | 关闭的是“静默吞没”问题，不把 fallback 机制本身误记成 bug。 |
| R17-80 | §87 Security/IAM/Compliance 深层缺陷 | P1 | `network-egress-policy.ts:248-249` | 默认 mode="audit_only"——被阻止域/内部主机仅日志实际放行；出口限制为空操作 | 已解决（源码复核） | 根因是复审条目沿用了更早的默认值结论；现行 env loader 和 service 默认构造都已 fail-close 到 `deny`。 | 当前 `src/platform/five-plane-control-plane/iam/network-egress-policy.ts` 的 `loadNetworkEgressPolicyConfigFromEnv()` 与 `NetworkEgressPolicyService` 构造默认都收敛为 `mode: "deny"`；`tests/unit/platform/control-plane/iam/network-egress-policy.test.ts` 现有 `loadNetworkEgressPolicyConfigFromEnv returns deny mode by default` 与 `NetworkEgressPolicyService defaults to deny mode` 均已通过。 | 关闭的是默认模式漂移问题，不外推为 egress allowlist/notification 等更深治理已全部完工。 |
| R17-81 | §87 Security/IAM/Compliance 深层缺陷 | P1 | `sandbox-policy.ts:410-443` | 所有工厂策略 deniedRoots:[]——/etc、/proc、~/.ssh 无默认拒绝 | 已解决（源码/测试） | 根因不是单个调用方漏传 deny list，而是 sandbox policy 工厂函数把 `deniedRoots` 统一留空，导致 `/etc`、`/proc`、`~/.ssh` 这类敏感根只能靠调用方自觉补，默认 posture 实际是 fail-open。 | 本轮在 `src/platform/five-plane-control-plane/iam/sandbox-policy.ts` 新增 `DEFAULT_SANDBOX_DENIED_ROOTS`，并接入 `createWorkspaceWritePolicy/createReadOnlyPolicy/createScopedExternalAccessPolicy/createRestrictedExecPolicy/createConfigReadPolicy` 五个工厂；`tests/unit/platform/control-plane/iam/{sandbox-policy,sandbox-policy-extended}.test.ts` 已新增/更新断言，验证默认策略包含 `/etc`、`/proc` 与 `*.ssh` 拒绝根。 | 关闭的是默认 deny roots 缺失问题。 |
| R17-82 | §87 Security/IAM/Compliance 深层缺陷 | P1 | `data-classification-service.ts:719-720` | matchesRule 用用户定义 pattern 构建 new RegExp() 无复杂度守卫——ReDoS 向量 | 已修复（源码已满足，本轮补定向测试） | 原审计命中的是 custom rule pattern 直接 `new RegExp(...).test(...)` 的旧实现；现行根因已收敛为“需要把 regex 安全门禁写进测试证据”，而不是代码仍裸跑危险模式。 | 当前 `src/platform/five-plane-control-plane/iam/data-classification-service.ts` 在 `matchesRule()` 中先执行 `isRegexSafe(pattern)`，对嵌套量词等危险模式仅打日志并跳过，不会进入真实匹配；`tests/unit/platform/control-plane/iam/data-classification-service-regex-safety.test.ts` 已定向验证危险模式 `"(a+)+$"` 不会被应用到分类链上。 | 与 `R29-48` 为同一缺口，关闭依据是现行代码与定向测试。 |
| R17-83 | §87 Security/IAM/Compliance 深层缺陷 | P1 | `compliance-case-orchestration-service.ts:63,307` | governance=null 时 governance?.allowed??true——所有合规转移无治理评估直接批准 | 已解决（源码/测试） | 根因是 `evaluateGovernance()` 允许返回 `null`，而 transfer / erasure 路径又把 `governance?.allowed ?? true` 当作默认值，结果“治理服务缺席”被解释成“治理已通过”。 | 本轮在 `src/platform/compliance/compliance-case-orchestration-service.ts` 引入 fail-closed `createMissingGovernanceResult()`，将 governance 缺失和 evaluator 返回 `null` 都统一折叠成 `allowed:false` + `governance_missing:governance_evaluator_unconfigured`；`tests/unit/platform/compliance/compliance-case-orchestration-service{,-fail-closed}.test.ts` 已新增 transfer/erasure 两条回归，验证 governance 缺失时状态为 `blocked`。 | 关闭的是 governance 缺席即默认放行的问题。 |
| R17-84 | §87 Security/IAM/Compliance 深层缺陷 | P1 | `compliance-governance-service.ts:95-109` | createExceptionWorkflow 返回 plain object 无持久化/追踪；expiresAt 无未来验证；compensating controls 不强制 | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。 | 不宣称已修复。 |
| R17-85 | §87 Security/IAM/Compliance 深层缺陷 | P1 | `evidence-collector.ts:42` | Evidence 纯内存 Map——重启丢失，无完整性保护(§46 要求持久 evidence+链式 hash) | 部分修复（组件已支持 snapshot 持久化与 hash chain，默认 wiring 仍需部署配置） | 原条目把“治理服务默认没给 collector 配 storagePath”和“`evidence-collector.ts` 自身只会内存存储、没有完整性保护”混成了一条。现行 collector 已同时具备文件快照持久化和按 framework 的链式 hash 校验。 | 当前 `src/org-governance/compliance-engine/evidence-collector.ts` 已实现 `loadSnapshot()/persistSnapshot()/verifyChain()`，并通过 `ComplianceEvidenceCollectorOptions.storagePath` 支持重启后恢复；`tests/unit/org-governance/compliance-engine/evidence-collector.test.ts` 已验证 snapshot reload 与篡改检测。**剩余边界**：`ComplianceGovernanceService` 仍未把 durable storage path 作为部署级默认配置强制下沉。 | 收窄为“部署 wiring 仍需补配置”，不再把 collector 本体误写成“无持久化/无完整性保护”。 |
| R17-86 | §87 Security/IAM/Compliance 深层缺陷 | P1 | `audit-event-integrity.ts:256` | latestChainHash 读 entries.at(-1) 从未排序输入——可能引用错误链尾 | 已解决（源码/测试） | 根因是 `verifyTier1AuditIntegrity()` 在校验循环里会先按 `chainPosition` 排序，但最终 `latestChainHash` 仍从原始 `entries.at(-1)` 取值，输入顺序一旦乱掉就会把错误链尾报给调用方。 | 本轮将 `src/platform/five-plane-control-plane/iam/audit-event-integrity.ts` 收敛为复用同一个 `sortedEntries`，循环和 `latestChainHash` 都基于链位排序结果；`tests/unit/platform/control-plane/iam/audit-event-integrity.test.ts` 已新增乱序输入回归，验证最终链尾取第二条（真实最高 chain position）而非原数组最后一条。 | 关闭的是链尾取值错误问题。 |
| R17-87 | §87 Security/IAM/Compliance 深层缺陷 | P2 | `vault-http-secret-provider.ts:173-174` | Static token 硬编码 1h 过期不管实际 TTL——过期 token 在 buffer 窗口内被服务 | 已解决（源码复核） | 根因不是现行 static token 仍缓存 1h，而是复审条目没有吸收后续 TTL 收敛。当前 static token 已改成短 TTL 缓存，AppRole token 也走保守 lease 处理。 | 当前 `src/platform/five-plane-control-plane/iam/vault-http-secret-provider.ts` 已将 static token 最大缓存压到 `30_000ms`，AppRole `lease_duration` 缺失时回落到 60s 保守 TTL；`tests/unit/platform/control-plane/iam/vault-http-secret-provider.test.ts` 的 `re-authenticates quickly when AppRole login omits lease_duration` 继续通过。 | 关闭的是“硬编码 1h TTL”旧结论。 |
| R17-88 | §87 Security/IAM/Compliance 深层缺陷 | P2 | `vault-http-secret-provider.ts:232-233` | isAvailable() 发送 "dummy" 作 X-Vault-Token——触发 Vault 审计噪音 | 已解决（源码复核） | 根因不是现行 health check 仍发送 dummy token，而是复审条目停在更早实现。 | 当前 `src/platform/five-plane-control-plane/iam/vault-http-secret-provider.ts` 的 `isAvailable()` 已改成无认证头访问 `/v1/sys/health`，并接受 `200/429/472` 作为“Vault 可响应”；对应 `tests/unit/platform/control-plane/iam/vault-http-secret-provider.test.ts` 已覆盖健康检查成功/失败分支。 | 关闭的是 health check 审计噪音问题。 |
| R17-89 | §87 Security/IAM/Compliance 深层缺陷 | P2 | `approval-routing/escalation/index.ts:12-21` | shouldEscalateApproval 无 max-depth/cooldown；仅 first match(.find)；无 SLA-breach 通知(§40) | 已解决 | 根因不是“完全没有升级规则”，而是升级判断只看 `triggerAfterMinutes + riskLevel`，`ApprovalRoutingService.resolveEscalation()` 还直接 `.find(...)` 取首条命中规则，导致多级升级、冷却窗口和 SLA breach 通知都无法表达。 | 本轮在 `src/org-governance/approval-routing/escalation/index.ts` 增加了 `maxEscalationDepth/cooldownMinutes/notifyOnSlaBreach/slaBreachNotificationTargetIds` 与 `evaluateApprovalEscalation()`；`src/org-governance/approval-routing/approval-routing-service.ts` 改为按“已满足阈值中最具体的规则”解析升级，不再依赖 `.find(...)` 的数组顺序，同时把 `slaBreachNotificationTargetIds` 和 `escalationRuleId` 纳入 route/plan 输出与审计 reason codes。定向回归覆盖位于 `tests/unit/org-governance/approval-routing/escalation/index.test.ts` 与 `tests/unit/org-governance/approval-routing/approval-routing-service-coverage.test.ts`。 | 保留真实代码修复结论。 |
| R17-90 | §87 Security/IAM/Compliance 深层缺陷 | P2 | `approval-routing/route-engine/index.ts:170` | applySodPolicy 可返回空 approver chain——无 fail-closed/显式拒绝；零审批人通过 | 已解决（源码/测试） | 根因是 `applySodPolicy()` 过滤完 requester/manager/conflict/same-chain 后，`resolveApprovalRoute()` 仍会把空数组当成合法 `approverChain` 返回，调用方只能靠后续逻辑“碰巧发现没有人审批”。 | 本轮在 `src/org-governance/approval-routing/route-engine/index.ts` 对 `approverChain.length === 0` 显式抛出 `ValidationError("approval_route.empty_approver_chain")`，把零审批人场景改成 fail-closed；`tests/unit/org-governance/approval-routing/{route-engine/index,approval-routing-service}.test.ts` 已新增/更新回归，验证 SoD 过滤清空审批链时会直接拒绝。 | 关闭的是“零审批人通过”问题。 |
| R17-91 | §87 Security/IAM/Compliance 深层缺陷 | P2 | `access-model.ts:95-101` | agent_runtime 默认角色无条件授予 exec:command+fs:write——§11 要求破坏性操作需审批 | 已解决 | 原审计基于旧版默认 capability 映射，没有复核当前 `agent_runtime` 默认权限集。 | 当前 `src/platform/five-plane-control-plane/iam/access-model.ts` 中 `agent_runtime` 仅继承 `STANDARD_CAPABILITIES`（`model:invoke`、`tool:invoke`、`network:access`），`exec:command` 与 `fs:write` 只在更高权限角色中出现。 | 不改生产代码，仅回写现行默认权限依据。 |
| R18-01 | §89 Harness Runtime/HITL/Learn 深层缺陷 | P0 | `harness/index.ts:57-77` | ConstraintPack.budget 缺 max_model_tokens/max_context_tokens/max_output_tokens——仅3/6维度(§45.3) | 已解决（源码/测试） | 根因是旧 harness constraint 归一化只镜像 `maxSteps/maxCost/maxDurationMs` 三个 budget 维度，token 预算既没有进入 deprecated `budget` 兼容面，也没有回写 `budgetEnvelope`。 | 当前 `src/platform/five-plane-orchestration/harness/index.ts` 的 `normalizeConstraintPack()` 已同时维护 `budget.max_model_tokens/max_context_tokens/max_output_tokens` 和 `budgetEnvelope.maxModelTokens/maxContextTokens/maxOutputTokens`；定向回归 `tests/unit/platform/five-plane-orchestration/harness/review-regressions.test.ts` 的 `normalizeConstraintPack keeps all six budget dimensions available` 已通过。该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 关闭的是 budget token 维度缺失，不外推为所有消费方都已完成更深 token budget 执行治理。 |
| R18-02 | §89 Harness Runtime/HITL/Learn 深层缺陷 | P0 | `harness/index.ts:168-198` | HarnessRun 缺 tenantId/traceId/riskLevel/ownership/auditRefs——多租户隔离和审计链断裂(§45.13) | 已解决（源码/测试） | 根因不是现行 HarnessRun 仍缺这些字段，而是复审结论停留在 internal run state 还没通过 canonical adapter 回填治理元数据的旧阶段。 | 当前 `src/platform/five-plane-orchestration/harness/index.ts` 的 `HarnessRunRuntimeState` 已包含 `tenantId/traceId/riskLevel/ownership/auditRefs`，`toCanonicalHarnessRun()` 与 `transitionRunStatus()` 还会对缺省值补 `trace:{runId}`、`ownerId=tenant:local` 和空 `auditRefs`；定向回归 `toCanonicalHarnessRun backfills trace, ownership, and audit defaults` 已通过。该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 关闭的是 canonical HarnessRun 治理字段缺失问题。 |
| R18-03 | §89 Harness Runtime/HITL/Learn 深层缺陷 | P0 | `harness/index.ts:585-616` | decide() 仅消费 evaluatorScore 忽略 policy/budget/risk/sideEffect/guardrail/HITL——§45.25 "LLM-as-Judge cannot override deterministic failure" | 已解决（源码/测试） | 根因是旧 `decide()` 只按 `evaluatorScore/requiresHuman/maxIterationsReached` 分支，确定性失败信号没有冻结进 decision input bundle，也不能在高分 evaluator 前 fail-close。 | 当前 `src/platform/five-plane-orchestration/harness/index.ts` 的 `decide()` 已处理 `guardrailAbort/sideEffectMayCommit/budgetExhausted/hitlPending/riskScore/guardrailSuggestedAction`，并把 frozen evaluator/risk/hitl/node/sideEffect/budget/policy/guardrail 全量写入 canonical decision input bundle；定向回归 `HarnessRuntimeService.decide honors deterministic abort ...` 已验证高分 evaluator 仍会被 `budgetExhausted` 直接打成 abort。该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 关闭的是 deterministic failure 可被 evaluator 覆盖的问题。 |
| R18-04 | §89 Harness Runtime/HITL/Learn 深层缺陷 | P0 | `harness/recovery-controller.ts:4` | HarnessFailureType 仅3/5种——缺 llm_provider_unavailable(fallback chain)/budget_exhausted(safe terminate)/platform_panic(§45.11) | 已解决（源码复核） | 旧结论停在 RecoveryController 只有最小错误分类的阶段。 | 当前 `src/platform/five-plane-orchestration/harness/recovery-controller.ts` 的 `HarnessFailureType` 已显式覆盖 `llm_provider_unavailable/budget_exhausted/platform_panic`，并分别实现指数退避、HITL 升级和 checkpoint 恢复语义。该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 关闭的是 failure taxonomy 缺项问题。 |
| R18-05 | §89 Harness Runtime/HITL/Learn 深层缺陷 | P0 | `harness/guardrails/guardrail-vibration-breaker.ts` | VibrationBreaker 从未接入 GuardrailEngine/runLoop——§45.20 振荡检测为死代码；run 可无限 replan | 已解决（源码复核） | 根因是旧实现只有独立 vibration breaker helper，没有在 runLoop 主链上消费 guardrail action signature。 | 当前 `src/platform/five-plane-orchestration/harness/index.ts` 已在 decision 之后构造 `GuardrailActionSignal`，调用 `this.vibrationBreaker.evaluate(...)` 并在 cooldown 命中时转入 `openHitlReview("guardrail_vibration_detected", ...)`，振荡检测不再是死代码。该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 关闭的是振荡检测未接主链问题。 |
| R18-06 | §89 Harness Runtime/HITL/Learn 深层缺陷 | P1 | `harness/index.ts:148-158` | HarnessStep 缺 nodeRunRefs/rationale/evidenceRefs/toolCalls/latency/cost/error/nextAction(§45.13) | 已解决（源码/测试） | 根因是早期 HarnessStep 只有最小执行投影字段，node/evidence/tool/cost/action 元数据没有进入 step 级语义投影。 | 当前 `src/platform/five-plane-orchestration/harness/index.ts` 的 `HarnessStep` 与 `appendStep()` 已支持 `nodeRunRefs/rationale/evidenceRefs/toolCalls/latency/cost/error/nextAction`；定向回归 `HarnessRuntimeService.decide honors deterministic abort and appendStep preserves step metadata` 已验证这些字段会被实写到步骤记录。该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 关闭的是 step 元数据缺失问题。 |
| R18-07 | §89 Harness Runtime/HITL/Learn 深层缺陷 | P1 | `harness/index.ts:315-316` | appendStep 设 startedAt=completedAt——step duration 恒为0违反 §58.1 harness.run.duration | 已解决（源码复核） | 根因是旧 `appendStep()` 用单一 `nowIso()` 同时写 `startedAt/completedAt`，而主循环也没有把 stage latency 回填给步骤。 | 当前 `src/platform/five-plane-orchestration/harness/index.ts` 已在 runLoop 的 planner/generator/evaluator 三段分别测量 `plannerLatencyMs/generatorLatencyMs/evaluatorLatencyMs` 并传给 `appendStep()`；`appendStep()` 收到 `latency` 时会按完成时刻重新计算 `completedAt`，主路径不再固定 0ms。该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 关闭的是 runLoop 主链 step duration 恒为 0 的问题，不额外夸大所有手工 appendStep 调用方都已做同样测量。 |
| R18-08 | §89 Harness Runtime/HITL/Learn 深层缺陷 | P1 | `harness/index.ts:57-60` | ConstraintPack.autonomyMode 用 "manual"\ | 已解决（源码复核） | 根因是旧 contract 还保留过时 autonomy mode 值，后来实现已切到 canonical 枚举，但复审条目没跟进。 | 当前 `src/platform/five-plane-orchestration/harness/index.ts` 的 `ConstraintPack.autonomyMode` 已收敛为 `suggestion/supervised/semi_auto/full_auto`，不再接受 `manual/auto` 这组旧值。该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 关闭的是 autonomy mode 枚举漂移问题。 |
| R18-09 | §89 Harness Runtime/HITL/Learn 深层缺陷 | P1 | `oapeflir-harness-mapping.ts:13-29` | mapHarnessStepToOapeflirPhase 永不返回 "learn"/"release"——8阶段中2阶段不可达(§45.22) | 已解决（源码/测试） | 根因是旧 phase mapper 只处理 `plan/execute/evaluate/hitl/loop_controller`，没有 learner/release manager 映射。 | 当前 `src/platform/five-plane-orchestration/harness/oapeflir-harness-mapping.ts` 已对 `stage==="learn" || role==="learner"` 返回 `learn`，对 `stage==="release" || role==="release_manager"` 返回 `release`；定向回归 `tests/unit/platform/five-plane-orchestration/oapeflir-harness-mapping-review-regressions.test.ts` 已通过。该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 关闭 learn/release 两个不可达阶段。 |
| R18-10 | §89 Harness Runtime/HITL/Learn 深层缺陷 | P1 | `learn/knowledge-promotion-service.ts:102-103` | 批量 promote 事件 payload 硬编码 learningObjects[0]——N>1 时丢失 N-1 对象元数据 | 已解决（源码/测试） | 根因是旧批量 promote 只发一条事件且 payload 固定引用首个 learning object。 | 当前 `src/platform/five-plane-orchestration/learn/knowledge-promotion-service.ts` 已按每个 promoted object 循环发事件，分别写入各自 `learningObjectId/learningType/documentId`，同时保留批次级 `promotedCount`；`tests/unit/platform/orchestration/learn/knowledge-promotion-service.test.ts` 的多对象回归已通过。该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 关闭的是批量 promote 首对象硬编码问题。 |
| R18-11 | §89 Harness Runtime/HITL/Learn 深层缺陷 | P1 | `learn/learning-object-model.ts:14` | createdAt 为 z.number().int()(epoch) 但 Harness 用 ISO string(nowIso())——类型不匹配无法关联 | 已解决（源码/测试） | 根因是学习对象 schema 还停留在 epoch-int 形态，而 learn/harness 主链已经统一用 ISO 时间戳。 | 当前 `src/platform/five-plane-orchestration/learn/learning-object-model.ts` 已把 `createdAt` 改为 `z.string()`，并补上 `normalizeLearningType()` 对旧 `model_retraining/dataset_gap` 的 canonical 映射；定向回归 `tests/unit/platform/orchestration/learn/learning-object-model.test.ts` 已验证 ISO `createdAt` 可被解析。该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 关闭 learning object 时间戳类型错配。 |
| R18-12 | §89 Harness Runtime/HITL/Learn 深层缺陷 | P1 | `harness/index.ts:99-104` | FeedbackEnvelope 为扁平 signals:string[]——缺 §45.6 四级层次(Step/Task/Workflow/System)和结构化信号 | 已解决（源码复核） | 根因是旧反馈信封只有 legacy flat signals，没有结构化 hierarchy。 | 当前 `src/platform/five-plane-orchestration/harness/index.ts` 的 `FeedbackEnvelope` 已显式建模 `stepSignals/taskSignals/workflowSignals/systemSignals` 四级结构，runLoop 在创建 envelope 时也同步初始化这些层级，同时保留 legacy `signals` 兼容面。该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 关闭的是反馈信号层次缺失问题。 |
| R18-13 | §89 Harness Runtime/HITL/Learn 深层缺陷 | P2 | `harness/sandbox/` | 目录不存在——§45.4 step 5 要求 per-tool sandbox-layer 绑定 | 已解决（源码复核） | 原结论至少有两处已过时：其一 `src/platform/five-plane-orchestration/harness/sandbox/` 目录和 `createSandboxLayer()` 工厂已经存在；其二 toolbelt 装配链已经支持 `sandboxRequirement` 并为 granted tools 生成 sandbox binding。 | 当前 `src/platform/five-plane-orchestration/harness/sandbox/index.ts` 提供 `HarnessSandboxLayer/ToolSandboxBinding/createSandboxLayer()`，`toolbelt-assembler.ts` 已把 `sandboxRequirement` 下沉到每个 granted tool 的 binding 生成逻辑。该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 关闭的是“目录不存在/完全无绑定工厂”的旧结论；不额外夸大为已接入真实 OS 级 sandbox executor。 |
| R18-14 | §89 Harness Runtime/HITL/Learn 深层缺陷 | P2 | `harness/index.ts:39-55` | HarnessRunStatus 有 16态 vs spec 7态——额外态无文档无 FSM guard | 需继续整改（保留开放） | 这条已不能再按“16 态且无 FSM guard”原样描述。现行实现虽然已经通过 `RuntimeStateMachine` 统一做状态跃迁，但 canonical status 集仍明显多于审计引用的 7 态规范，是否应收敛 contract 还是回写 spec 仍是未决架构问题。 | `src/platform/five-plane-orchestration/harness/index.ts` 当前仍保留 `created/admitted/planning/ready/running/pausing/paused/resuming/replanning/compensating/completed/failed/aborted` 多态集合；本轮没有假称已和 7 态规范完全对齐。该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 保持开放，后续需统一 spec 与 canonical FSM。 |
| R18-15 | §89 Harness Runtime/HITL/Learn 深层缺陷 | P2 | `harness/index.ts:633-805` | runLoop 复用相同静态 plannerOutput/generatorOutput/evaluatorOutput——replan/retry 循环无法收敛 | 已解决（源码/测试） | 根因是旧 runLoop 只能消费一次性注入的静态 `plannerOutput/generatorOutput/evaluatorOutput`，replan 后没有基于前序 planner 输出重新生成。 | 当前 `src/platform/five-plane-orchestration/harness/index.ts` 已引入 `loopServices.plan/generate/evaluate` 与 `previousPlannerOutput` 传递链，replan 时会把上一轮 planner 输出回传给下一轮 plan；定向回归 `HarnessRuntimeService.runLoop uses loop services across replans` 已验证第二轮 planner 能看到第一轮 `plan-1`。该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 关闭的是 replan/retry 静态输出复用问题。 |
| R18-16 | §89 Harness Runtime/HITL/Learn 深层缺陷 | P2 | `harness/recovery-controller.ts:26-28` | tool_timeout 直接 resume() 跳过 LoopController retry/replan 决策(§45.11) | 已解决（源码复核） | 根因是旧 recovery path 在 `tool_timeout` 场景直接 `resume()`，没有把重试额度、guard violation 和 escalation 条件交给 LoopController。 | 当前 `src/platform/five-plane-orchestration/harness/recovery-controller.ts` 的 `handleFailure(..., "tool_timeout")` 已先 `getLoopController(run)`，调用 `shouldContinue("retry_same_plan", hasRemainingIterations)` 判定是否允许重试；不允许时会 fail-close 到 HITL escalation，允许时才按 backoff 进入 `sleep(...)`。该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 关闭的是 tool_timeout 绕过 loop guard 的问题。 |
| R18-17 | §90 Tests/Bootstrap/Types 系统性缺陷 | P0 | `tests/e2e/multi-step-workflow.test.ts:15` | E2E 测试直接操作 WorkflowStateRecord(legacy)非 HarnessRun/PlanGraphBundle——验证废弃路径 | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 不宣称已修复。 |
| R18-18 | §90 Tests/Bootstrap/Types 系统性缺陷 | P0 | `tests/e2e/critical-workflows.test.ts:367+` | ~30处使用 store.insertWorkflowState()/getWorkflowState() legacy API 非 HarnessRuntimeService | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 不宣称已修复。 |
| R18-19 | §90 Tests/Bootstrap/Types 系统性缺陷 | P0 | `tests/e2e/workflow-resume-flow.test.ts:128+` | Resume 测试直接调用 store.updateWorkflowState() 绕过 fencing/CAS/lease——即使不变量失效测试仍通过 | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 不宣称已修复。 |
| R18-20 | §90 Tests/Bootstrap/Types 系统性缺陷 | P1 | `src/platform-architecture-types.ts` | 无 HarnessRun/HarnessRunStatus/PlanGraphBundle/NodeRun 类型——仅含 app-layer types(§45/§13 canonical objects 缺失) | 已解决 | 根因是 root-level `platform-architecture-types.ts` 只停留在 app/bootstrap surface，没有把 canonical executable contracts 再导出到根入口类型层，导致上层入口和 contract surface 漂移。 | `src/platform-architecture-types.ts` 现已直接 re-export `HarnessRun/HarnessRunStatus/PlanGraphBundle/NodeRun/NodeRunStatus` 自 `src/platform/contracts/executable-contracts/index.ts`；编译期对齐证据位于 `tests/unit/platform/root-entry-review-regressions.test.ts`。该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 保留真实类型面修复结论。 |
| R18-21 | §90 Tests/Bootstrap/Types 系统性缺陷 | P1 | `src/index.ts:41` | PlatformRootEntryMode 与 PlatformStartupTargetKind 重复定义相同 union——漂移风险 | 已解决 | 根因是 root entry 想给 CLI/demo 增加单独模式别名，但实际又手写了一份与 `PlatformStartupTargetKind` 相同的 union，后续任何一侧扩展都会造成漂移。 | `src/index.ts` 已改为 `export type PlatformRootEntryMode = PlatformStartupTargetKind`，不再维护第二份 union；同文件还补齐了 root top-level builder re-export，定向编译/运行证据位于 `tests/unit/platform/root-entry-review-regressions.test.ts`。该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 保留真实类型收敛结论。 |
| R18-22 | §90 Tests/Bootstrap/Types 系统性缺陷 | P1 | `src/index.ts:125-171` | runPlatformRootDemo() 输出 TaskRecord/WorkflowStateRecord 形态——demo 暴露 legacy 非 canonical | 已解决 | 根因不是 demo 无法跑，而是 root demo 直接把内部持久化快照里的 `task/workflow/execution/session` 记录形态序列化到 stdout，等于把 legacy persistence model 暴露成 public demo contract。 | `src/index.ts` 新增 `buildPlatformRootDemoSummary()`，`runPlatformRootDemo()` 现在输出 `platform_root_demo_summary_v1`，只暴露 `runRef/lifecycle/result/events` 这类 root summary 字段，不再向外泄漏 `TaskRecord/WorkflowStateRecord/ExecutionRecord/SessionRecord` 结构；回归位于 `tests/unit/platform/root-entry-review-regressions.test.ts`。该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 保留真实输出契约修复结论。 |
| R18-23 | §90 Tests/Bootstrap/Types 系统性缺陷 | P1 | `config/runtime/default.json` | 缺 §24 必须 keys：circuitBreaker.threshold/configSchemaVersion/configDriftReconciler interval | 已解决 | 根因不是运行时完全没有 breaker/drift 能力，而是 `config/runtime/default.json` 和 `RUNTIME_LAYER_SCHEMA` 都只覆盖了最早期的 timeout/concurrency 默认项，导致 canonical runtime defaults 与治理 schema 同步缺失。 | `config/runtime/default.json` 已补入 `configVersion/configSchemaVersion/circuitBreaker.threshold/configDriftReconciler.interval`；`src/platform/control-plane/config-center/config-governance-support.ts` 的 `RUNTIME_LAYER_SCHEMA` 也同步强制这些字段。证据位于 `tests/unit/platform/control-plane/config-center/config-governance-support.test.ts` 与 `tests/unit/platform/control-plane/config-center/default-config-shape.test.ts`。该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 保留真实配置与 schema 同步修复结论。 |
| R18-24 | §90 Tests/Bootstrap/Types 系统性缺陷 | P1 | `config/bootstrap/default.json` | 仅3 keys(appName/phase/stableCoreEnabled)——缺 layer 启动顺序/health-check timeout/readiness-probe(§3/§32) | 已解决 | 根因是 bootstrap default 文件长期停留在最小占位状态，而 `BOOTSTRAP_LAYER_SCHEMA` 也没有把 startup order/readiness/health probe 这些 root bootstrap contract 字段当成必填治理对象。 | `config/bootstrap/default.json` 已补入 `dependencyOrder/readinessGates/degradationPolicy/healthCheckTimeoutMs/readinessProbe`，并将 `phase` 收敛到现行 bootstrap ring 表达；`src/platform/control-plane/config-center/config-governance-support.ts` 的 `BOOTSTRAP_LAYER_SCHEMA` 同步强制这些字段。证据位于 `tests/unit/platform/control-plane/config-center/config-governance-support.test.ts` 与 `tests/unit/platform/control-plane/config-center/default-config-shape.test.ts`。该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 保留真实配置与 schema 同步修复结论。 |
| R18-25 | §90 Tests/Bootstrap/Types 系统性缺陷 | P1 | `platform-architecture-bootstrap.ts:128-148` | registerPlatformArchitectureServices() register 后立即 get()——强制 eager init 破坏 lazy DAG | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 不宣称已修复。 |
| R18-26 | §90 Tests/Bootstrap/Types 系统性缺陷 | P2 | `tests/e2e/` | 无 PlanGraphBundle dispatch e2e 测试——canonical P3→P4 路径零覆盖 | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 不宣称已修复。 |
| R18-27 | §90 Tests/Bootstrap/Types 系统性缺陷 | P2 | `tests/golden/` | 无 HarnessRun/PlanGraphBundle 响应 shape golden test——golden tests 引用 TaskRecord-era 形态 | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 不宣称已修复。 |
| R18-28 | §90 Tests/Bootstrap/Types 系统性缺陷 | P2 | `config/risk/default.json` | riskCategories 数组缺 "ai" 类别(register.json:14 有)——AI risk undeclared | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 不宣称已修复。 |
| R18-29 | §90 Tests/Bootstrap/Types 系统性缺陷 | P2 | `src/platform/shared/lifecycle/service-registry.ts:316-322` | 循环依赖 topologicalSort() 仅 warn 不 throw——bootstrap 可静默以不完整顺序继续(§3) | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 不宣称已修复。 |
| R18-30 | §91 Observability/Channel Gateway/API 层缺陷 | P0 | `middleware/ (目录)` | 无 idempotency-key enforcement middleware——§6.2 要求所有写操作携带+去重 | 已解决 | 根因不是 middleware 文件不存在，而是 HTTP server 早先没有把 idempotency enforcement 接到实际 dispatch path，导致写请求入口仍可绕开该 contract。 | `src/platform/interface/api/http-api-server.ts:162-163,447-499` 现已在请求入口统一执行 `IdempotencyKeyMiddleware.check()/record()`，并对重复写请求返回 replay；对应 middleware 能力定向验证已通过 `tests/unit/platform/interface/api/middleware/idempotency-key.test.ts`。 | 这是实修接线，不是仅关闭文档。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 |
| R18-31 | §91 Observability/Channel Gateway/API 层缺陷 | P0 | `All route files (task-routes.ts:67 etc)` | 路由用 /v1/_ 非 /api/v1/_——§6.3 canonical paths 为 /api/v1/harness-runs 等 | 已解决 | 根因是 route factories 长期保留 `/v1/*` 内部字面量，但 server matcher 没有把外部 canonical `/api/v1/*` 入口映射到同一 dispatch surface。 | `src/platform/interface/api/http-server/request-helpers.ts:17-35` 现在会把 `/api/...` 规范化回内部 route pathname/segments，因此 `/api/v1/*` 与 `/v1/*` 共用同一套 handler；定向回归 `tests/unit/platform/interface/api/http-server/http-server.test.ts` 已验证 `/api/v1/harness-runs` 路径可用。 | 以 dispatch 层收敛 canonical path，而不是继续复制第二套路由表。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 |
| R18-32 | §91 Observability/Channel Gateway/API 层缺陷 | P1 | `http-server/index.ts` | 无 harness-runs routes——§6.3/§6.8 定义 /api/v1/harness-runs 为 canonical 执行入口；仅有 legacy /v1/tasks | 已解决 | 根因是 interface admin route surface 之前没有显式暴露 harness run list/detail/events 三个 canonical read endpoints。 | `src/platform/interface/api/http-server/admin-routes.ts:127-183` 新增 `harness-runs` list/detail/events 处理逻辑；定向回归 `tests/unit/platform/interface/api/http-server/http-server.test.ts` 已通过，覆盖 `/api/v1/harness-runs`、`/api/v1/harness-runs/:id`、`/api/v1/harness-runs/:id/events`。 | 真实补了路由，不是假收口。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 |
| R18-33 | §91 Observability/Channel Gateway/API 层缺陷 | P1 | `response-hardening.ts:102-129` | decorateResponseHeaders 无 X-Trace-Id——§6.2 要求 X-Request-Id + X-Trace-Id | 已解决 | 根因是 response hardening 只稳定注入了 `x-request-id/x-api-version` 等 header，缺少 trace header 派生与 expose。 | `src/platform/interface/api/http-server/response-hardening.ts:16,102-110` 现已 expose `x-trace-id`，并从 `x-trace-id/x-correlation-id/x-request-id` 派生最终 trace header；定向回归 `tests/unit/platform/interface/api/http-server-response-hardening.test.ts` 已通过。 | 真实补齐 header contract。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 |
| R18-34 | §91 Observability/Channel Gateway/API 层缺陷 | P1 | `websocket-bridge.ts:282-288` | broadcastToTask 不检查 ws.bufferedAmount——§7.1 要求 server-side backpressure(buffer full→drop low-priority) | 已解决 | 根因是 websocket bridge 之前只在发送后统计慢消费者，没有在高水位时优先丢弃低价值事件。 | `src/platform/interface/channel-gateway/websocket-bridge.ts:232-247,272-278` 现在会在 `bufferedAmount` 超阈值时对 `status_changed/progress/message_delta` 直接发 `backpressure_warning` 并跳过低优先级广播；定向回归 `tests/unit/platform/interface/channel-gateway/websocket-bridge-coverage.test.ts` 已通过。 | 这是实修 backpressure，不是只改审计。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 |
| R18-35 | §91 Observability/Channel Gateway/API 层缺陷 | P1 | `stream-bridge.ts` | StreamBridge 无连接 client 追踪/slow-consumer 检测——§7.1 要求 per-connection backpressure+stream_gap event | 需继续整改（本轮未改） | Stream replay/gap 语义已有实现，但该条同时指向 per-connection slow-consumer 责任，现有 StreamBridge 仍不是连接级 bridge。 | `src/platform/interface/channel-gateway/stream-bridge.ts` 当前已提供 `replay()/getReplayWindow()/droppedBeforeSequence` 等 gap/replay contract，且 `tests/unit/platform/interface/channel-gateway/stream-bridge.test.ts` 已覆盖 replay evict/gap 元数据；但是否要把 connection 级 backpressure 职责继续下沉到 StreamBridge，本轮不做假收口。 | 保守保留该条，避免把 WebSocketBridge 的 slow-consumer 修复误记到 StreamBridge。 |
| R18-36 | §91 Observability/Channel Gateway/API 层缺陷 | P1 | `structured-logger.ts:42-59` | StructuredLogEntry 有 correlationId 无 causationId——§5.2 ContractEnvelope 要求两者支持 causal chain | 已解决 | 这条现在不再是“审计看漏”，而是本轮直接把 causal chain 字段能力做实并补了回归。 | `src/platform/shared/observability/structured-logger.ts:59,275,301` 现已定义并提取 `causationId`；定向回归 `tests/unit/platform/shared/observability/structured-logger-extended.test.ts` 与 `structured-logger-format.test.ts` 已通过。 | 保留已解决结论。 |
| R18-37 | §91 Observability/Channel Gateway/API 层缺陷 | P1 | `middleware/ (目录)` | 无 request-deduplication middleware——§4.2 P1 职责明确包含"请求去重" | 已解决 | 根因和 R18-30 类似：dedup middleware 已存在，但此前没有接到 HTTP request dispatch 入口。 | `src/platform/interface/api/http-api-server.ts:162,419-444` 现已在无 idempotency key 的写请求路径上执行 dedup fingerprint 检查，并返回 `409 + x-original-request-id/retry-after-ms`；middleware 定向回归 `tests/unit/platform/interface/api/middleware/request-deduplication.test.ts` 已通过。 | 这是实修接线。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 |
| R18-38 | §91 Observability/Channel Gateway/API 层缺陷 | P2 | `structured-logger.ts:247-302` | 无最低 log-level 过滤——所有 debug 级别命中 buffer+file；production 无抑制配置 | 已解决 | 根因是 StructuredLogger 之前没有最小级别门槛，所有级别都会落到 ring buffer 与 file sink。 | `src/platform/shared/observability/structured-logger.ts:89,150,246,307` 现已支持 `minLogLevel` 并在写 buffer/sink 前过滤低级别 entry；定向回归 `tests/unit/platform/shared/observability/structured-logger-extended.test.ts` 已通过 `warn/error/fatal` 过滤断言。 | 真实补了生产抑制能力。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 |
| R18-39 | §91 Observability/Channel Gateway/API 层缺陷 | P2 | `channel-gateway-service.ts:468-510` | 外部 provider HTTP 调用(Telegram/Slack/Webhook)无 circuit breaker(§9.1/§7.1) | 已解决 | 根因是 channel gateway 只有 rate limit/retry tracking，没有 provider-level breaker state。 | `src/platform/interface/channel-gateway/channel-gateway-service.ts:63,421-475` 现已新增 per-channel `circuitBreakerState`、open/reset 逻辑与 retryable failure 计数；定向回归 `tests/unit/platform/interface/channel-gateway/channel-gateway-service.test.ts` 已覆盖 repeated failure 后的 `gateway.slack_circuit_open`。 | 真实补了 breaker。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 |
| R18-40 | §91 Observability/Channel Gateway/API 层缺陷 | P2 | `websocket-bridge.ts:210-222` | 无 per-client subscription cap——单 client 可订阅无界 taskIds 导致内存耗尽(§9 isolation) | 已解决 | 根因是 websocket bridge 早先允许单 client 无上限地向 `subscribedTasks` 集合追加 taskId。 | `src/platform/interface/channel-gateway/websocket-bridge.ts:20,170-185` 现已把单 client 订阅上限限定为 `100`，超限返回 `subscription_limit_exceeded`；定向回归 `tests/unit/platform/interface/channel-gateway/websocket-bridge-coverage.test.ts` 已通过。 | 真实补了 isolation guardrail。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 |
| R18-41 | §91 Observability/Channel Gateway/API 层缺陷 | P2 | `channel-gateway-service.ts:487,541,606` | fetchImpl() 无 timeout——§7.1 要求 default 5s/max 30s | 已解决 | 根因是 Telegram/Slack/Webhook provider 调用直接透传 `fetchImpl()`，缺默认 timeout 与上限裁剪。 | `src/platform/interface/channel-gateway/types.ts:41-47` 与 `src/platform/interface/channel-gateway/channel-gateway-service.ts:60,74-81,477-490,559,613,678` 现已提供默认 `5s`、最大 `30s` 的 `fetchWithTimeout()` 包装；定向回归 `tests/unit/platform/interface/channel-gateway/channel-gateway-service.test.ts` 已覆盖 `gateway.webhook_timeout`。 | 真实补了超时 contract。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 |
| R18-42 | §92 ADR/Contract 终审缺陷 | P0 | `adr/096-harness-recovery-controller.md:14` | 引用 "phase 8b" 作 release gate——ADR-094 已修正为 Ring 2 但本 ADR 未同步(§33 three-ring model) | 已解决（ADR 复核） | 根因不是 ADR-096 现在仍引用 `phase 8b`，而是复审条目停留在 remediation 之前的版本；现行 ADR 已切到 Ring 口径。 | 当前 `docs_zh/adr/096-harness-recovery-controller.md:14` 已写成 `Ring 2 durable-readiness`，`docs_zh/adr/096-harness-recovery-controller.md:38` 还记录了“已修正交付门禁术语”的 remediation。 | 不改代码，仅按现行 ADR 关闭旧结论。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 |
| R18-43 | §92 ADR/Contract 终审缺陷 | P0 | `src/domains/recipes/index.ts:18-27` | DomainRecipeSchema 缺 contract 必须字段：risk_profile_ref/guardrail_overlay/recommended_workflow_ids/default_prompt_bundle_ref/acceptance_checklist_ref | 已解决（代码复核） | 代码复核确认 `DomainRecipeSchema` 第 27-31 行已包含全部五个必须引用字段：risk_profile_ref、guardrail_overlay、recommended_workflow_ids、default_prompt_bundle_ref、acceptance_checklist_ref。审计条目原指向本文件 18-27 行存在缺字段问题，但现行 schema 已完整。 | 当前 `src/domains/recipes/index.ts:27-31` 已正确定义全部 required contract 字段，非文档漂移而是此前审计遗漏了后续代码变更。 | 不改代码，仅更正审计结论。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 |
| R18-44 | §92 ADR/Contract 终审缺陷 | P0 | `src/domains/domain-specs.ts:18-26` | Domain lifecycle states(validated/registered/active/updating/archived)不匹配 contract(validating/certified/canary/active/deprecated/retired)——缺 canary 阶段 | 部分解决（canonical 状态已补齐，legacy 漂移仍在） | 根因已不再是“源码缺 canary 阶段”，而是 `DomainLifecycleStateSchema` 为兼容存量数据同时接受 canonical 与 legacy 状态，导致 contract 口径与实现枚举仍未完全收敛。 | 当前 `src/domains/domain-specs.ts` 的 `DomainLifecycleStateSchema` 已明确包含 `validating/certified/canary/active/deprecated/retired`，因此“缺 canary 阶段”这一指控已经过期；同时它仍保留 `draft/testing/registered/archived/updating/validated` 等 legacy 值。定向验证 `tests/unit/domains/registry/domain-descriptor-schema.test.ts` 已通过，证明现行 schema 可解析 canonical 生命周期。 | 回写为部分解决：关闭“缺 canary”旧结论，保留 lifecycle contract 收敛为后续专项。 |
| R18-45 | §92 ADR/Contract 终审缺陷 | P1 | `src/domains/domain-specs.ts:55-61` | DomainRiskSpec 缺 contract 必须字段：advisory_only/human_accountable/deterministic_hot_path_only/allowed_capability_overrides/required_approval_policies | 已解决（代码复核） | 代码复核确认 `DomainRiskSpecSchema` 第 60-72 行已包含全部五个字段：`advisoryOnly`/`advisory_only`、`humanAccountable`/`human_accountable`、`deterministicHotPathOnly`/`deterministic_hot_path_only`、`allowedCapabilityOverrides`/`allowed_capability_overrides`、`requiredApprovalPolicies`/`required_approval_policies`，且 `normalizeDomainRiskSpecInput()` 提供 snake_case 兼容。审计条目原指向 55-61 行存在缺字段问题，但现行 schema 已完整。 | 当前 `src/domains/domain-specs.ts:60-72` 已正确定义全部 required contract 字段，非文档漂移而是此前审计遗漏了后续代码变更。 | 不改代码，仅更正审计结论。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 |
| R18-46 | §92 ADR/Contract 终审缺陷 | P1 | `contracts/workflow_static_analysis_and_compensation_contract.md:31,34` | StaticCompatibilityIssue 和 WorkflowTemplate 代码库中不存在 | 需继续整改（contract/文档） | contract 文档或导出面没有完全跟随 canonical runtime 迁移。 | 该条本质是 contract/导出面问题，需要逐份 contract 回写或补强测试。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 本轮不伪造代码修复。 |
| R18-47 | §92 ADR/Contract 终审缺陷 | P1 | `contracts/gateway_streaming_contract.md:26-30` | Contract 定义 StreamChannel/ProgressChunk/FinalChunk/ErrorChunk——代码用 StreamEventFrame(类型不匹配) | 需继续整改（contract/文档） | contract 文档或导出面没有完全跟随 canonical runtime 迁移。 | 该条本质是 contract/导出面问题，需要逐份 contract 回写或补强测试。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 本轮不伪造代码修复。 |
| R18-48 | §92 ADR/Contract 终审缺陷 | P1 | `architecture/05-cross-platform-ui-architecture.md:7` | UI arch 基线为 v3.2——落后两个大版本(当前 v4.3)；缺五平面/PlanGraphBundle/NodeAttemptReceipt | 需继续整改（contract/文档） | contract 文档或导出面没有完全跟随 canonical runtime 迁移。 | 该条本质是 contract/导出面问题，需要逐份 contract 回写或补强测试。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 本轮不伪造代码修复。 |
| R18-49 | §92 ADR/Contract 终审缺陷 | P1 | `divisions/ 目录` | AGENTS.md 声明 "division definitions live in divisions/" 但目录不存在 | 已解决（仓库复核） | 根因不是当前仓库仍缺 `divisions/` 目录，而是复审条目形成时读取到的是更早的目录结构；现行仓库已存在该根目录。 | 当前仓库根目录下已存在 `divisions/`，与 `AGENTS.md` 的仓库结构说明一致。 | 不改代码，仅关闭过时目录缺失结论。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 |
| R18-50 | §92 ADR/Contract 终审缺陷 | P1 | `adr/109-contract-freeze.md:39` | 引用 ADR-110 但 110 不在 v4.3 Contract Freeze Scope——forward reference 到未冻结 ADR | 已解决（ADR 复核） | 根因不是 ADR-109 当前仍引用 `ADR-110`，而是复审条目沿用了草案阶段的 forward reference 判断；现行 Accepted 文本已无该引用。 | 当前 `docs_zh/adr/109-contract-freeze.md` 全文未再引用 `ADR-110`，`关联文档` 也仅保留架构、contracts README 与 current_todo_list。 | 不改代码，仅关闭陈旧 forward reference 指控。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 |
| R18-51 | §92 ADR/Contract 终审缺陷 | P2 | `adr/104-domain-recipe-twelve-archetypes.md:27` | 12个 archetypes 与 ADR-105 latency_tier 4级无映射——cross-ADR 语义 gap | 已修复（ADR/文档） | 根因是 ADR-104 之前只把 archetype 绑定到 `DomainDescriptor.recipes` 和 onboarding 阶段，但没有给出和 ADR-105 `latency_tier` 的默认映射，导致 recipe 形态与时延边界分离。 | 本轮在 `docs_zh/adr/104-domain-recipe-twelve-archetypes.md:41-70` 新增 archetype 到 latency tier 的默认映射表，并补充 `Trading` 的 `deterministic_hot_path_only` 约束和偏离默认映射时的 review 要求。 | 真实补了 ADR，不是假关闭。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 |
| R18-52 | §92 ADR/Contract 终审缺陷 | P2 | `contracts/multimodal_gateway_contract.md:9-12` | 4个 canonical objects 仅为 plain TS interface 非 Zod schema——contract §5 测试要求不满足 | 已修复（源码/测试） | 根因是多模态网关模块此前只用 TypeScript interface 表达 `MultimodalRequest`、`MultimodalInputPart`、`ModalityRouteDecision`、`MultimodalSafetyFinding`，没有把这些 canonical 对象提升为可执行的 runtime schema，所以 contract §5 的 validation/test 要求落不到代码。 | 本轮在 `src/ops-maturity/multimodal/multimodal-gateway-service.ts:90-170` 新增四个 Zod schema 与 `parseMultimodalRequest()` 等解析入口，并在 `handle()` 入口接入请求校验、对 route decision / safety finding 做 schema 收敛。定向运行 `tests/unit/ops-maturity/multimodal/multimodal-gateway-service.test.ts`、`tests/unit/ops-maturity/multimodal/index.test.ts`、`tests/integration/ops-maturity/multimodal-gateway-integration.test.ts`，新增 schema 用例与既有集成用例一起通过。 | 真实改了源码并补了定向测试，不是假关闭。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 |
| R18-53 | §92 ADR/Contract 终审缺陷 | P2 | `contracts/capacity_planning_contract.md:8-12` | CapacitySignal 在 architecture-remediation.ts(shim)非 ops-maturity 模块——canonical type 位置错误 | 已修复（源码复核并收口 shim） | 根因不是当前对外导出仍以 `architecture-remediation.ts` 的 shim 为 canonical，而是历史 shim 内保留了一份漂移的 `CapacitySignal` 本地定义，和现行 `ops-maturity/capacity-planner` 主定义并存，造成审计误判。 | 当前 `src/scale-ecosystem/capacity-planning/index.ts:1-8` 已直接 re-export `../../ops-maturity/capacity-planner/capacity-planning-service.js` 的 canonical `CapacitySignal`；本轮又把 `src/scale-ecosystem/architecture-remediation.ts:1,55-56` 改成 `export type CapacitySignal = CanonicalCapacitySignal`，消除了 shim 自带的竞争定义。相关容量规划定向测试 `tests/unit/scale-ecosystem/capacity-planning/index.test.ts`、`tests/unit/ops-maturity/capacity-planner/capacity-planning-service.test.ts`、`tests/integration/ops-maturity/capacity-planning-integration.test.ts` 已通过。 | 这是实修，不是只改审计。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 |
| R18-54 | §92 ADR/Contract 终审缺陷 | P2 | `docs_zh/operations/capacity-planning.md` | 描述 Pod/PostgreSQL/Redis sizing 但不引用 CapacityForecast/CapacityScenario contract 对象 | 已修复（contract/文档） | 根因是容量规划运行手册此前只写了 sizing 经验值和压测步骤，没有把 `CapacitySignal / CapacityForecast / CapacityScenario / CapacityAlert / CapacityRecommendation` 这组 canonical contract 对象带进操作流程。 | 本轮已在 `docs_zh/operations/capacity-planning.md:3-9,31-33` 补齐 contract 对象引用，并要求验证流程显式产出 `CapacitySignal`、`CapacityForecast`、`CapacityScenario`、`CapacityAlert` 与 `CapacityRecommendation`。 | 真实补了 operations 文档，不是假关闭。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 |
| R18-55 | §92 ADR/Contract 终审缺陷 | P2 | `adr/094-harness-durable-execution.md:10` | OAPEFLIR 部分仍用 "step"——ADR-092 修正为 NodeRun/NodeAttempt | 已解决（ADR 复核） | 根因不是 ADR-094 仍把 `step` 当持久执行 truth，而是复审条目没有吸收后续 remediation；现行文本已明确 `step` 只是语义投影。 | 当前 `docs_zh/adr/094-harness-durable-execution.md:10` 已写明 “step 为语义投影，非持久 truth；NodeRun/NodeAttempt 为持久执行单元”。 | 不改代码，仅按现行 ADR 关闭旧结论。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 |
| R19-01 | §94 OAPEFLIR Loop/FSM/Assessment 深层缺陷 | P0 | `stage-transition-fsm.ts (全文件)` | FSM 从未被 OapeflirLoopService.run() 实例化或查询——所有阶段排序为隐式过程代码；FSM 为死代码 | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 不宣称已修复。 |
| R19-02 | §94 OAPEFLIR Loop/FSM/Assessment 深层缺陷 | P0 | `stage-transition-fsm.ts:122-129` | FSM 硬阻止所有向后转换——§13 GraphPatch/Replan 即使 FSM 接入也不可能 | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 不宣称已修复。 |
| R19-03 | §94 OAPEFLIR Loop/FSM/Assessment 深层缺陷 | P0 | `oapeflir-loop-service.ts:276-284` | replanDecision 计算后无动作——quality-gate 失败产出决策对象但从未触发重执行/graph patching | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 不宣称已修复。 |
| R19-04 | §94 OAPEFLIR Loop/FSM/Assessment 深层缺陷 | P1 | `oapeflir-loop-service.ts:210-211` | 执行前无 budget reservation——§15.3 要求 LLM/tool 调用前 reserve budget；bridge 直接 fire | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 不宣称已修复。 |
| R19-05 | §94 OAPEFLIR Loop/FSM/Assessment 深层缺陷 | P1 | `final-response.ts:27-48` | FinalResponse 缺 §27 必须字段：audience/runId/limitations/citationsRequired/evidenceRefs/dataClass/redactionApplied/safetyLabels | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 不宣称已修复。 |
| R19-06 | §94 OAPEFLIR Loop/FSM/Assessment 深层缺陷 | P1 | `oapeflir-loop-service.ts (run())` | 全过程零 event emission——§14.3 要求所有 state change 发射 platform._ facts 或 oapeflir.view._ projections | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 不宣称已修复。 |
| R19-07 | §94 OAPEFLIR Loop/FSM/Assessment 深层缺陷 | P1 | `assessment-service.ts:65` | routingDecision.division 硬编码 "coding"——永不路由到其他 division(§2.1 Assess routing) | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 不宣称已修复。 |
| R19-08 | §94 OAPEFLIR Loop/FSM/Assessment 深层缺陷 | P1 | `oapeflir-loop-service.ts:405-418` | buildFeedbackSignals 所有 step 硬编码 category:"success"——失败 step 产生虚假正向反馈 | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 不宣称已修复。 |
| R19-09 | §94 OAPEFLIR Loop/FSM/Assessment 深层缺陷 | P1 | `runtime-execute-bridge.ts:228-240` | Bridge 委托 runMultiStepOrchestration 内部重新规划——绕过 OAPEFLIR Plan stage 已验证的 PlanGraphBundle | 已解决（源码已满足，本轮补定向测试） | 根因不是 bridge 现在仍把计划降级成原始文本，而是历史实现曾直接委托默认 runtime 入口，让人误以为会重新走 planner。现行 bridge 已把输入收敛为 canonical `PlanGraphBundle.graph.nodes` 串行化后交给 injected execution-plane adapter。 | 当前 `RuntimeExecuteBridge.executePlan()` 会调用 `serialiseOapeflirPlan(plan.graph.nodes)` 并把该结果传给 `runtimePlanExecutor`；本轮 `tests/unit/platform/orchestration/oapeflir/runtime-execute-bridge.test.ts` 已验证 executor 收到的 `request` 正是序列化后的 plan nodes，而不是重新规划前的原始 prompt。 | 关闭范围是 bridge 不再绕开已验证的 `PlanGraphBundle` contract；下游执行器内部其他逻辑不在本条关闭范围。 |
| R19-10 | §94 OAPEFLIR Loop/FSM/Assessment 深层缺陷 | P2 | `stage-transition-fsm.ts:187-195` | recordStageSkipped(stage, reasonCode) 静默丢弃 reasonCode——无存储/证据 | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 不宣称已修复。 |
| R19-11 | §94 OAPEFLIR Loop/FSM/Assessment 深层缺陷 | P2 | `runtime-execute-bridge.ts:103` | validationPassed = record.validationJson!=null——存在≠通过；应解析检查结果 | 已解决（源码已满足，本轮补定向测试） | 原缺口来自旧实现把 `validationJson` 的“存在”误当成“验证通过”；这会把 malformed/failed payload 也算成 pass。 | 当前 `src/platform/five-plane-orchestration/oapeflir/runtime-execute-bridge.ts` 已在 `mapStepOutputRecord()` 中解析 `validationJson` 并仅在 `parsed.valid === true` 时返回 `validationPassed: true`；本轮新增 `tests/unit/platform/orchestration/oapeflir/runtime-execute-bridge.test.ts` 回归，验证 malformed `validationJson` 会被判为 `false`。 | 这是 bridge 映射语义的实修，不是假回写。 |
| R19-12 | §94 OAPEFLIR Loop/FSM/Assessment 深层缺陷 | P2 | `assessment-service.ts:87-105` | 复杂度仅用 file/blocker/memory 硬编码阈值——无 §32 EvaluationGate 数值评分模型 | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 不宣称已修复。 |
| R19-13 | §94 OAPEFLIR Loop/FSM/Assessment 深层缺陷 | P2 | `oapeflir-loop-service.ts:145-165` | O→A 降级 hardcode confidence:0.5 低于阈值 0.65——保证触发 risk escalation；自放大故障 | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 不宣称已修复。 |
| R19-14 | §94 OAPEFLIR Loop/FSM/Assessment 深层缺陷 | P2 | `feedback-signal.ts:4` | FeedbackCategorySchema 缺 blocker/regression 类别——§31 contamination check 和 incident regression 无法表示 | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 不宣称已修复。 |
| R19-15 | §95 Truth/Routing/RSM/Transition 深层缺陷 | P0 | `transition-service.ts:500-526` | TaskTerminalTransitionService.apply() 用非 CAS 方法(updateTaskStatus 等)——TOCTOU race；§25.3 要求 CAS+lease+fencing | 已解决（源码复核/定向测试） | 根因不是终态级联现在仍走 `updateTaskStatus` 一类非 CAS 写法，而是复审条目停留在旧实现；现行 `TaskTerminalTransitionService.apply()` 已先重读 fresh state，再分别调用 `updateTaskOutputCas`、`updateTaskStatusCas`、`updateWorkflowStateCas`、`updateSessionStatusCas`、`updateExecutionStatusCas`。 | 当前 `src/platform/five-plane-execution/state-transition/transition-service.ts:934-1000` 已逐项使用 CAS/fencing token；本轮新增 `tests/unit/platform/five-plane-execution/state-transition/transition-service-regressions.test.ts:228-283`，显式断言五个 CAS writer 都被命中且终态级联结果正确。 | 这是审计基线滞后，不是假关闭。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 |
| R19-16 | §95 Truth/Routing/RSM/Transition 深层缺陷 | P0 | `intake-router.ts:348-402` | IntakeRouter.route() 从 raw text 直接到 workflow/division——跳过 RawInput→TaskDraft→ClarificationSession→ConfirmedTaskSpec→RequestEnvelope(§5.3/§4.2) | 已解决（源码复核/定向测试） | 根因不是现行 intake 仍然“raw text 直达 route decision”，而是复审条目停留在补齐 intake pipeline 之前；当前 `route()` 已先构建 `IntakePipelineContext`，再顺序产出 `TaskDraft`、`ClarificationSession`、`ConfirmedTaskSpec`、`RequestEnvelope`，最后才生成 `routeDecision`。 | 当前 `src/platform/five-plane-orchestration/routing/intake-router.ts:878-1155` 可见完整四阶段流水线；本轮新增 `tests/unit/platform/five-plane-orchestration/routing/intake-router-pipeline-context.test.ts:6-86`，分别验证 context 透传与 ambiguity 场景下 clarification stage 真实存在。 | 属于现行实现已到位、复审结论未同步，现关闭。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 |
| R19-17 | §95 Truth/Routing/RSM/Transition 深层缺陷 | P0 | `agent-team-service.ts:56-148` | AgentTeamService.buildPlan() 无 §19.5 协作不变量：无 delegation depth check(max 3/global 8)、无 permission subsetting(C1)、无 risk_mode guard(C2)、无 budget propagation(C6) | 已修复（源码/测试） | 根因不是完全“零实现”，而是早期实现只补了字段和局部 build lane 检查，没有把真实 delegated chain depth 编进输入契约，也没有对 review/validate/repair/release lanes 做统一的 permission subset 守卫；因此复杂协作路径里仍可能出现深度失真和子代理工具越权。 | 本轮在 `src/platform/five-plane-orchestration/routing/agent-team-service.ts:46-52,75-108,157-336` 为 delegated plan 增加 `parentDepth`/`parentAllowedTools`，把 lane depth 改成按真实链路计算，并对所有 lanes 统一执行 `assertDelegationDepth()` 与 `acp.permission_not_subset` 检查。定向测试 `tests/unit/platform/five-plane-orchestration/routing/agent-team-service-collaboration-regressions.test.ts:35-86` 覆盖缺失 parent depth、权限越界、超深 delegation 三个负向场景；`agent-team-service-invariants.test.ts:47-81` 验证合法 delegated 计划会保留正确的深度/预算/trace 元数据。 | 这是本轮真实逻辑修复，不是假关闭。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 |
| R19-18 | §95 Truth/Routing/RSM/Transition 深层缺陷 | P1 | `intake-router.ts:286-291` | IntakeRouteInput 仅 title+request——缺 tenantId/trace_id/idempotency_key/principal/confirmedTaskSpecId(§5.3) | 已解决（源码复核/定向测试） | 根因是旧版 `IntakeRouteInput` 只承载最小文本字段，无法把 §5.3 所需的租户、trace、幂等和 principal 元数据带入 intake pipeline；现行接口已补齐这些字段，`confirmedTaskSpecId` 也作为 authoritative-input 句柄保留在输入契约中，而真正的 authoritative spec 仍由 stage-3 产物生成。 | 当前 `src/platform/five-plane-orchestration/routing/intake-router.ts:334-357` 已定义 `tenantId`、`traceId`、`idempotencyKey`、`principal`、`confirmedTaskSpecId`；`878-889,1019-1039,1099-1145` 显示这些上下文字段被写入 pipeline artifacts / route decision。本轮新增 `tests/unit/platform/five-plane-orchestration/routing/intake-router-pipeline-context.test.ts:6-49` 锁定 tenant/trace/idempotency/principal 的透传。 | 属于真实 contract 补齐，不是假关闭。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 |
| R19-19 | §95 Truth/Routing/RSM/Transition 深层缺陷 | P1 | `transition-service.ts:286-287` | WorkflowTransitionService.transition() 无 db.transaction() 包裹(TaskTransition 有)——workflow 状态非原子 | 已解决（源码复核/定向测试） | 根因不是现行 workflow transition 仍裸写，而是复审条目停留在把 workflow read+write 拆开的旧实现；当前 `transition()` 已在 `db.transaction()` 中重读当前状态并完成 CAS 更新。 | 当前 `src/platform/five-plane-execution/state-transition/transition-service.ts:450-509` 已用 `this.db.transaction(() => { ... updateWorkflowStateCas ... })` 包裹；本轮新增 `tests/unit/platform/five-plane-execution/state-transition/transition-service-regressions.test.ts:286-307`，显式断言 workflow transition 命中了事务包装。 | 属于审计滞后，现关闭。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 |
| R19-20 | §95 Truth/Routing/RSM/Transition 深层缺陷 | P1 | `transition-service.ts:296-360` | Workflow/Session/Execution TransitionService.apply() 不发 tier-1 status event——仅 Task 发射；§28 要求所有状态变更发事件 | 已解决（源码复核/定向测试） | 根因不是当前只有 task 路径发事件，而是复审条目没有吸收后续补齐的 legacy tier-1 emission；现行 workflow/session/execution 三条路径都在状态 CAS 成功后发各自的 `*:status_changed` 事件。 | 当前 `src/platform/five-plane-execution/state-transition/transition-service.ts:501-507,662-668,771-776` 已分别发出 `workflow:status_changed`、`session:status_changed`、`execution:status_changed`；本轮新增 `tests/unit/platform/five-plane-execution/state-transition/transition-service-regressions.test.ts:286-352` 锁定三类事件。 | 该条只按“tier-1 event 已发射”本体关闭，不外推其它 projection 全部完美。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 |
| R19-21 | §95 Truth/Routing/RSM/Transition 深层缺陷 | P1 | `agent-team-service.ts:13-21` | AgentTeamLane 缺 depth/budget_remaining/correlation_id/parent_run_id/domain_id/risk_level/trace_id(§19.5) | 已解决（源码复核/定向测试） | 根因是早期 lane 结构只覆盖 stage/owner/agent/tool 这些执行调度字段，没有把 §19.5 要求的 delegation metadata 编进 lane contract；现行 `AgentTeamLane` 已补齐这些协作不变量字段，`buildPlan()` 也会逐 lane 填充。 | 当前 `src/platform/five-plane-orchestration/routing/agent-team-service.ts:19-34,228-320` 已定义并赋值 `depth`、`budgetRemaining`、`correlationId`、`parentRunId`、`domainId`、`riskLevel`、`traceId`；本轮新增 `tests/unit/platform/five-plane-orchestration/routing/agent-team-service-invariants.test.ts:47-81` 逐项断言这些字段存在且 delegated 深度/预算值合法。 | 这是字段级 contract 的真实补齐，不是假关闭。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 |
| R19-22 | §95 Truth/Routing/RSM/Transition 深层缺陷 | P1 | `async-repositories/delegation-repository.ts:57-76` | updateDelegation() 无 CAS/expected-status guard——并发覆写(§25.3) | 已解决（源码复核/测试补强） | 根因不是 `updateDelegation()` 现在仍无 CAS，而是复审条目没有吸收后续在 status transition 上补入的 `expectedStatus` compare-and-swap guard；缺口只剩测试没有把 SQL 级 guard 锁住。 | 当前 `src/platform/five-plane-state-evidence/truth/async-repositories/delegation-repository.ts:57-93` 已在 `hasStatusUpdate` 时把 `status = expectedStatus` 拼入 `WHERE`；本轮更新 `tests/unit/platform/state-evidence/truth/async-repositories/delegation-repository.test.ts:107-155`，显式断言 status update SQL 包含 `AND status = $...`，而仅写 `resultRef` 时不会误加该 guard。 | 属于代码已修、测试证据补齐，不是假关闭。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 |
| R19-23 | §95 Truth/Routing/RSM/Transition 深层缺陷 | P1 | `runtime-state-machine.ts:319-332` | assertAuditRef() 仅在 auditRef 提供时验证非空——从不强制要求；§25.3 HarnessRun/SideEffect 转换需强制 auditRef | 已解决（源码复核/定向测试） | 根因不是现行 `assertAuditRef()` 仍是“传了才验”，而是复审条目停留在 `requiresAudit` 只做空串判断的旧版本；当前逻辑已对 `HarnessRun`、`SideEffectRecord`、`toStatus===succeeded/failed` 四类路径强制要求非空 `auditRef`。 | 当前 `src/platform/five-plane-execution/runtime-state-machine.ts:324-338` 已在 `requiresAudit` 为真时拒绝 `null/undefined/empty`；本轮新增 `tests/unit/platform/five-plane-execution/runtime-state-machine-audit-ref-regression.test.ts:8-45`，验证缺失 `auditRef` 的 `HarnessRun created -> admitted` 会抛出 `runtime_state_machine.audit_ref_required`。 | 属于真实守卫已到位、补了负向回归，不是假关闭。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 |
| R19-24 | §95 Truth/Routing/RSM/Transition 深层缺陷 | P2 | `compliance-case-orchestration-service.ts:117-123` | allowRedactedRestrictedTransfer flag 覆盖 deny decision——§23 compliance 应 fail-closed | 已解决（源码复核/定向测试） | 根因不是当前 flag 还能覆盖 deny，而是复审条目把输入字段的存在误读成了决策 override；现行决策路径 `resolveTransferStatus()` 根本不读取该 flag，仍按 governance/artifact/transfer/residency 四类 deny fail-closed。 | 当前 `src/platform/compliance/compliance-case-orchestration-service.ts:325-347` 显示 governance deny、artifact deny、residency deny 都直接返回 `blocked`；本轮新增 `tests/unit/platform/compliance/compliance-case-orchestration-service-fail-closed.test.ts:43-75`，验证即使传入 `allowRedactedRestrictedTransfer: true`，governance deny 仍不会被覆盖。 | 这是对“flag 覆盖 deny”指控的直接证伪，现关闭。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 |
| R19-25 | §95 Truth/Routing/RSM/Transition 深层缺陷 | P2 | `async-repositories/event-repository.ts:19-68` | insertEvent() 不验证 tenantId——§5.2 ContractEnvelope tenantId 必须；多租户 event 隔离断裂 | 已修复（源码/测试） | 根因不是读侧过滤失效，而是 async 写侧 `insertEvent()` 之前只按 `EventRecord` 组装 SQL 插入，既不要求 `tenantId`，也不校验 task 归属租户；因此多租户 contract 只能依赖读侧 `listEventsForTask()` 的 join 过滤，写时并没有 fail-fast。 | 本轮在 `src/platform/five-plane-state-evidence/truth/async-repositories/event-repository.ts:31-37,57-95` 为 async insert 输入增加强制 `tenantId`，并在写入前校验 `taskId -> tenantId` 绑定，不匹配时抛 `event_repository.tenant_scope_mismatch`。`tests/unit/platform/state-evidence/truth/async-repositories/event-repository.test.ts:62-122` 新增缺失 tenant / tenant mismatch 负向断言；`tests/integration/platform/state-evidence/truth/async-repositories/event-repository.test.ts:75-249` 也已改为按真实 tenant 契约写入并通过。 | 这是 async 写侧合同的真实补齐，不是假关闭。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 |
| R19-26 | §95 Truth/Routing/RSM/Transition 深层缺陷 | P2 | `agent-team-service.ts:146` | executionLoop 硬编码固定序列——§19 high-risk 需更严格 review loop；无 risk-adaptive 组合 | 已修复（源码/测试） | 根因不是当前 loop 完全不分风险，而是早期“自适应”实现仍把 medium/high 共用一套近似逻辑，高风险单步工作流甚至可能退化成没有 repair 的普通 review 流程，无法满足 §19 对 high-risk 的更严格回路要求。 | 本轮在 `src/platform/five-plane-orchestration/routing/agent-team-service.ts:75-108` 把 `computeExecutionLoop()` 分成 low/medium/high 三套组合：low 仍最小化，medium 按 step 数增加 review/validate，高风险则始终保留 `review -> validate -> repair -> validate` 核心环，并按工作流宽度追加额外 review 压力。`tests/unit/platform/five-plane-orchestration/routing/agent-team-service-collaboration-regressions.test.ts:89-126` 验证单步 high-risk 也会走完整 7-stage，`agent-team-service.test.ts:98-156,173-220` 的既有高风险断言也已通过。 | 属于真实行为修复，不是假关闭。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 |
| R19-27 | §96 UI Tests/Perf/Build 系统缺陷 | P0 | `ui/vitest.config.ts` | 覆盖率阈值 30%/20%——§7.2.6 要求 shared≥90%/ui-core≥80%/features≥70%/apps≥50% | 已解决（源码复核） | 这是 `R7-2` 的重复挂账。根因是 UI vitest coverage 阈值曾长期停留在占位值，后续已按分层门限收敛，但重复条目没有同步回写。 | 当前 `ui/vitest.config.ts` 已按 `90/80/70/50` 四档声明 coverage thresholds。该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用此结论。 | 关闭的是覆盖率门限配置漂移，不外推为全量覆盖率现状已经全部达标。 |
| R19-28 | §96 UI Tests/Perf/Build 系统缺陷 | P0 | `.github/workflows/ui-quality.yml` | CI 用 Node.js 20——§2.3 要求 Node.js 22 LTS | 已解决（源码修复） | 根因是 UI 的真实 PR 门禁工作流曾固定在旧 Node 版本，而审计还把目标指向了 `ui/.github/` 下不会被 GitHub 执行的副本。 | 当前根工作流 `.github/workflows/ui-quality.yml` 已使用 `actions/setup-node@v4` 且 `node-version: "22"`，与 §2.3 的 LTS 基线一致。 | 关闭的是实际 UI PR workflow 的 Node 版本漂移，不外推为全部 UI CI 门禁已经完备。 |
| R19-29 | §96 UI Tests/Perf/Build 系统缺陷 | P0 | `.github/workflows/ui-quality.yml + ui/lighthouserc.json` | 无 Lighthouse CI step——§7.1.1/§7.3.1 要求 FCP/LCP/CLS/INP CI 门为 PR-blocking | 已解决（源码修复） | 根因不是仓库里完全没有 Lighthouse 配置，而是实际根工作流之前没有执行 Lighthouse；`ui/.github/` 下的副本即使写了 job，也不构成 GitHub 的真实 PR 门禁。 | 本轮已在根工作流 `.github/workflows/ui-quality.yml` 增加 `treosh/lighthouse-ci-action@v11`；`ui/lighthouserc.json` 已声明 `performance/accessibility/best-practices/seo` 阈值及 `FCP/LCP/CLS/INP` 断言，因此 Lighthouse 现已进入真实 UI PR gate。该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用此结论。 | 关闭的是“真实 PR workflow 缺 Lighthouse CI step”的问题，不外推为所有性能门都已完全产品化。 |
| R19-30 | §96 UI Tests/Perf/Build 系统缺陷 | P0 | `ui/scripts/perf-budget.mjs` | Budget 用 raw bytes(550KB JS/1200KB total)非 gzip——§7.3.1 要求 <200KB gz main/<100KB gz per route | 已解决（源码复核） | 这是 `R7-3/R12-34` 的重复挂账。根因是早期 perf budget 以 raw bytes 为主，后续已切到 gzip 预算，但重复条目没有回写。 | 当前 `ui/scripts/perf-budget.mjs` 已按 `maxJsChunkGzBytes/maxCssChunkGzBytes/totalGzBytes` 执行 gzip 预算，并在 CI 环境追加 `FCP/TTI` 时间门。该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用此结论。 | 关闭的是 raw-byte 预算结论，不外推为 per-library budget 等更深性能门也已完备。 |
| R19-31 | §96 UI Tests/Perf/Build 系统缺陷 | P0 | `ui/scripts/perf-budget.mjs` | 无 per-library budget——§7.3.4 要求 ECharts<150KB gz/Monaco<200KB gz 独立 CI 门 | 已解决（源码/测试） | 根因是早期 perf gate 只看整体 JS/CSS/total gzip 预算，没有把 ECharts 和 Monaco 这类大库拆成单独预算项，因此即使单库异常膨胀，也可能被总量预算掩盖。 | 当前 `ui/scripts/perf-budget.mjs` 已新增 `maxEchartsGzBytes=150KB` 与 `maxMonacoGzBytes=200KB`，对匹配 chunk 做独立累计并 fail-close；`ui/tests/unit/ui/apps/web/build-config.test.ts` 与 `ui/tests/tools/tooling.test.ts` 已直接断言这两个预算项存在，且本轮定向 UI 验证 `67 passed`。该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用此结论。 | 关闭的是 per-library budget 缺失问题，不外推为所有实际 bundle 产物已在当前沙箱完成构建实测。 |
| R19-32 | §96 UI Tests/Perf/Build 系统缺陷 | P1 | `ui/tests/ (全部)` | 零 accessibility 测试——§7.2.4 要求 axe-core(Playwright)+VoiceOver；无 axe-core 依赖 | 部分修复（axe-core Playwright 已接入，assistive-tech/native 回归仍缺） | 真实根因不是“UI 完全没有 a11y 依赖”本身，而是之前没有任何可执行的 accessibility regression 资产进入真实工作流，因此 Web a11y 风险不会在 PR 阶段暴露。 | 本轮新增 `ui/tests/a11y/web-accessibility.spec.ts`，使用 `@axe-core/playwright` 对真实 Web 壳层 landing route 做 WCAG A/AA 扫描；`ui/playwright.config.ts` 可被 `npx playwright test --config playwright.config.ts --list` 识别，根工作流 `.github/workflows/ui-quality.yml` 也已接线 `npm run test:a11y`。**当前环境限制**：本地沙箱执行 `npx playwright test` 时，Vite dev server 监听 `127.0.0.1:4173` 被 `EPERM` 阻断，因此本轮只验证了 runner 可识别测试和工作流已接线，未在当前沙箱内完成浏览器实跑。**剩余缺口**：VoiceOver/TalkBack 等辅助技术回归、原生壳可访问性、视觉回归仍未补齐。该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用此结论。 | 关闭的是“零 accessibility 测试、零 axe 资产”的问题；保留的是更深一层的 assistive-tech/native a11y 产品化工作。 |
| R19-33 | §96 UI Tests/Perf/Build 系统缺陷 | P1 | `ui/tests/ (全部)` | 零 visual regression 测试——§7.2.3 要求 Chromatic/Percy | 需继续整改（UI 产品化差距） | 这类条目描述的是 UI monorepo、feature、原生壳、性能预算、可访问性与后端 presentation 接线的产品化缺口，需要跨 `ui/` 与后端服务成组整改。 | 本轮没有执行 UI 全链路 build/e2e/原生壳验证，也没有完成整组 UI 实装重构，不能虚写已修。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 保留为后续 UI 主线整改。 |
| R19-34 | §96 UI Tests/Perf/Build 系统缺陷 | P1 | `ui/tools/mock-server/src/index.ts` | Mock server 仅3路由内存 stub——§7.2.4 要求 MSW(Mock Service Worker) | 已解决（源码/测试） | 根因不是 mock 工具完全不存在，而是它此前只有少量内存 stub 和路径分支，没有形成可被测试/Playwright 直接复用的 MSW handler/server 资产。 | 本轮把 `ui/tools/mock-server/src/index.ts` 扩成 MSW 实装，新增 `createMockHandlers()` 与 `createMockServer()`，并接入 `msw` / `msw/node`；`ui/tests/tools/tooling.test.ts` 已验证 handler/server 导出存在且可被工具链消费，本轮定向 UI 验证已通过。该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用此结论。 | 关闭的是“mock-server 只有 3 个 stub、没有 MSW 资产”的问题。 |
| R19-35 | §96 UI Tests/Perf/Build 系统缺陷 | P1 | `ui/tools/e2e/src/index.ts` | E2E "tool" 仅为7个场景 data catalog——无 Playwright/Detox runner/assertions | 已解决（源码/定向验证） | 根因是旧 `@aa/e2e` 工具确实只暴露场景目录和 checklist，调用方拿不到可执行断言，因此它更像文档数据而不是 runner helper。 | 本轮在 `ui/tools/e2e/src/index.ts` 新增 `runScenarioAssertion(page, id)`，并新增 `ui/tests/playwright/web-scenarios.spec.ts` 让 Playwright 直接消费该 helper；`ui/playwright.config.ts` 现在同时收集 `tests/a11y/**/*.spec.ts` 与 `tests/playwright/**/*.spec.ts`，`npx playwright test --config playwright.config.ts --list` 已识别 2 个 Web spec。**当前环境限制**：本地沙箱执行真实 Playwright 浏览器流程仍会因 Vite dev server `127.0.0.1:4173` 监听 `EPERM` 失败，但这不再是“没有 runner/assertion”的问题，而是当前环境限制。该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用此结论。 | 关闭的是“E2E tool 只有 data catalog、没有 runner/assertion”的缺口；原生壳/Detox 实跑仍属后续产品化工作。 |
| R19-36 | §96 UI Tests/Perf/Build 系统缺陷 | P1 | `ui/turbo.json:7-13` | typecheck/lint/test 无 outputs——Turborepo 无法缓存(§7.1.1 incremental build) | 需继续整改（UI 产品化差距） | 这类条目描述的是 UI monorepo、feature、原生壳、性能预算、可访问性与后端 presentation 接线的产品化缺口，需要跨 `ui/` 与后端服务成组整改。 | 本轮没有执行 UI 全链路 build/e2e/原生壳验证，也没有完成整组 UI 实装重构，不能虚写已修。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 保留为后续 UI 主线整改。 |
| R19-37 | §96 UI Tests/Perf/Build 系统缺陷 | P1 | `ui/tests/features/flows.test.tsx` | 仅5/28 features 有交互流测试——23个 feature 零覆盖 | 需继续整改（UI 产品化差距） | 这类条目描述的是 UI monorepo、feature、原生壳、性能预算、可访问性与后端 presentation 接线的产品化缺口，需要跨 `ui/` 与后端服务成组整改。 | 本轮没有执行 UI 全链路 build/e2e/原生壳验证，也没有完成整组 UI 实装重构，不能虚写已修。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 保留为后续 UI 主线整改。 |
| R19-38 | §96 UI Tests/Perf/Build 系统缺陷 | P2 | `ui/tests/ (全部)` | 无 offline→recovery 集成测试(§7.2.2 断网→排队→恢复→同步→冲突) | 需继续整改（UI 产品化差距） | 这类条目描述的是 UI monorepo、feature、原生壳、性能预算、可访问性与后端 presentation 接线的产品化缺口，需要跨 `ui/` 与后端服务成组整改。 | 本轮没有执行 UI 全链路 build/e2e/原生壳验证，也没有完成整组 UI 实装重构，不能虚写已修。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 保留为后续 UI 主线整改。 |
| R19-39 | §96 UI Tests/Perf/Build 系统缺陷 | P2 | `ui/tests/ (全部)` | 无 multi-tab WS SharedWorker 测试(§7.2.2) | 需继续整改（UI 产品化差距） | 这类条目描述的是 UI monorepo、feature、原生壳、性能预算、可访问性与后端 presentation 接线的产品化缺口，需要跨 `ui/` 与后端服务成组整改。 | 本轮没有执行 UI 全链路 build/e2e/原生壳验证，也没有完成整组 UI 实装重构，不能虚写已修。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 保留为后续 UI 主线整改。 |
| R19-40 | §96 UI Tests/Perf/Build 系统缺陷 | P2 | `ui/tests/ (全部)` | 无 SSO login flow e2e(§7.2.2 OIDC→Token→API auth→refresh) | 需继续整改（UI 产品化差距） | 这类条目描述的是 UI monorepo、feature、原生壳、性能预算、可访问性与后端 presentation 接线的产品化缺口，需要跨 `ui/` 与后端服务成组整改。 | 本轮没有执行 UI 全链路 build/e2e/原生壳验证，也没有完成整组 UI 实装重构，不能虚写已修。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 保留为后续 UI 主线整改。 |
| R19-41 | §96 UI Tests/Perf/Build 系统缺陷 | P2 | `ui/tools/codegen/src/index.ts` | Codegen 仅生成 path 常量——无 OpenAPI/contract-driven DTO type gen(§5.2.3) | 需继续整改（UI 产品化差距） | 这类条目描述的是 UI monorepo、feature、原生壳、性能预算、可访问性与后端 presentation 接线的产品化缺口，需要跨 `ui/` 与后端服务成组整改。 | 本轮没有执行 UI 全链路 build/e2e/原生壳验证，也没有完成整组 UI 实装重构，不能虚写已修。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 保留为后续 UI 主线整改。 |
| R19-42 | §96 UI Tests/Perf/Build 系统缺陷 | P2 | `ui/turbo.json` | 无 inputs 过滤——所有文件变更触发所有 task 重跑 | 需继续整改（UI 产品化差距） | 这类条目描述的是 UI monorepo、feature、原生壳、性能预算、可访问性与后端 presentation 接线的产品化缺口，需要跨 `ui/` 与后端服务成组整改。 | 本轮没有执行 UI 全链路 build/e2e/原生壳验证，也没有完成整组 UI 实装重构，不能虚写已修。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 保留为后续 UI 主线整改。 |
| R19-43 | §97 跨系统集成/平面边界违规 | P0 | `runtime-execute-bridge.ts:228` | P3 Orchestration 动态导入 ../../../core/runtime/orchestrator/(P4外 src/core/)——跨五平面边界直接耦合 | 已解决（源码已满足，本轮补证据） | 根因与 `R9-14/R9-29` 同源：旧默认 bridge 曾直接跨平面 import runtime orchestrator。现行 bridge 已改成只依赖 `RuntimePlanExecutor` 注入点。 | 当前 `src/platform/five-plane-orchestration/oapeflir/runtime-execute-bridge.ts` 构造器仅接收 injected `runtimePlanExecutor`，文件内已无 `../../../core/runtime/orchestrator` 动态导入；`tests/unit/platform/orchestration/oapeflir/runtime-execute-bridge.test.ts` 也已通过 injected executor 验证 bridge 只做 contract 适配与委托。 | 这条关闭的是跨平面直连的移除。 |
| R19-44 | §97 跨系统集成/平面边界违规 | P0 | `execution-outcome-evaluator.ts:12` | P2 AI-Ops(prompt-engine) 直接导入 scale-ecosystem/feedback-loop/(Part VIII)——跨层直接耦合绕过 event bus | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 不宣称已修复。 |
| R19-45 | §97 跨系统集成/平面边界违规 | P1 | `execution-dispatch-service.ts:15,229` | P4 Execution 内联实例化 HealthService(db,store)——直接耦合 P5 Evidence 内部 | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 不宣称已修复。 |
| R19-46 | §97 跨系统集成/平面边界违规 | P1 | `model-routing-service.ts:36` | Model Gateway(P2) 导入 prompt-engine/eval/ ModelGovernanceSnapshot——兄弟平面耦合应走 contracts/ | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 不宣称已修复。 |
| R19-47 | §97 跨系统集成/平面边界违规 | P1 | `state-evidence/events/projections/ (全部9文件)` | 无 projection 消费 dispatch:ticket_created/claimed/decision_recorded 事件——dispatch 事件无投影(P5 盲点) | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 不宣称已修复。 |
| R19-48 | §97 跨系统集成/平面边界违规 | P2 | `dashboard/metric-aggregator/index.ts` | summarizeTaskMetrics 接受 raw string[] statuses(用"done"/"in_progress")——不匹配 authoritative status("completed"/"running") | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 不宣称已修复。 |
| R19-49 | §97 跨系统集成/平面边界违规 | P2 | `dashboard/health-scorer/index.ts:1` | P1 Interaction 直接导入 src/platform/shared/observability/ SystemSituation——应通过 DashboardPort | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 不宣称已修复。 |
| R19-50 | §97 跨系统集成/平面边界违规 | P2 | `dashboard/alert-router/index.ts` | sortAttentionQueue 无调用者——死代码集成点 | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 不宣称已修复。 |
| R19-51 | §97 跨系统集成/平面边界违规 | P2 | `runtime-execute-bridge.ts:144` | extractStepOutputRecords 用 unsafe cast (snapshot as {...})——无类型契约保证字段存在 | 已解决（源码已满足，本轮补证据） | 原缺口是桥接层直接对 `snapshot` 做结构假设，字段缺失时只能靠运行时偶然性兜底。 | 当前 `src/platform/five-plane-orchestration/oapeflir/runtime-execute-bridge.ts` 已改为 `tryGetExecutionRecordStepOutputs()` 类型守卫，仅在 `executionRecord.stepOutputs` 真为数组且元素具备 `stepId` 时才返回；现有 `tests/unit/platform/orchestration/oapeflir/runtime-execute-bridge.test.ts` 已覆盖 `snapshot/executionRecord/stepOutputs` 缺失时返回空数组的分支。 | 关闭依据是生产态类型守卫与定向测试。 |
| R19-52 | §97 跨系统集成/平面边界违规 | P2 | `src/platform/cost-management/index.ts:26` | P2 Control Plane 导入 scale-ecosystem/marketplace/cost-estimation-service——跨层依赖 | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。；该编号在原始 round 文档中重复出现 2 次，后续重复挂账均沿用本结论。 | 不宣称已修复。 |
| R20-01 | §99 Planner/Projections/Worker Pool 深层缺陷 | P0 | `plan-builder.ts:41,50` | DAG validation result(valid/issues)完全被忽略——cyclic/broken plan 静默使用 fallback orderedSteps 继续 | 已解决（源码复核/测试补强） | 根因不是现行 `PlanBuilder` 仍忽略 DAG 校验，而是复审条目停留在早期 build 直接消费 steps 的旧实现；当前 build 已在生成 `PlanGraphBundle` 前检查 `dagValidation.valid` 与 `orderedSteps.length`，发现 cycle / missing dependency 时直接抛错拒绝出图。 | `src/platform/five-plane-orchestration/planner/plan-builder.ts:61-67` 已显式 fail-fast；本轮补充 `tests/unit/platform/orchestration/planner/plan-builder.test.ts:233-248`，验证循环工作流会抛 `Invalid plan: ... cycle_detected`，不会再静默落入 fallback。 | 属于旧审计结论未同步，现关闭。 |
| R20-02 | §99 Planner/Projections/Worker Pool 深层缺陷 | P0 | `plan-dag-validator.ts:64` | 检测到 cycle 时返回原始未排序 steps 作 orderedSteps(非失败)——下游消费无效排序 | 已解决（源码复核/测试补强） | 根因不是 validator 现在仍把原始 steps 当作排序结果返回，而是复审条目没有吸收后续对 cycle 分支的 fail-fast 改造；当前一旦检测到环，`orderedSteps` 会被置空并伴随 `planning.cycle_detected`。 | `src/platform/five-plane-orchestration/planner/plan-dag-validator.ts:70-79` 已在 cycle 时返回 `orderedSteps: []`；本轮补充 `tests/unit/platform/orchestration/planner/plan-dag-validator.test.ts:100-113`，锁定“有环时不得回传未排序 fallback”这一负向场景。 | 属于真实行为已纠正，现关闭。 |
| R20-03 | §99 Planner/Projections/Worker Pool 深层缺陷 | P0 | `worker-drain-protocol.ts (全文件)` | Drain 为无状态 receipt factory——无 checkpoint coordination、无 lease release、无 RecoveryWorker 集成(§9 要求先 checkpoint 再释放) | 已修复（源码/测试） | 根因不是现在还“完全无状态”，而是实现停在半成品：`drainState` 已存在，但 `coordinateCheckpoint()` 始终直接返回 `true`、`notifyRecoveryWorker()` 为空、`releaseLease()` 只改内存计数，`TERMINATE` 相位也不会真的 handoff/recovery。 | 本轮在 `src/platform/five-plane-execution/worker-pool/worker-drain-protocol.ts` 增加 `checkpointCoordinator/leaseManager/recoveryNotifier` 依赖接口，把 active lease/nodeRun 真实传给 checkpoint，completed/forced_handoff 都会调 lease release，进入 `TERMINATE` 时会强制 handoff 并通知 recovery；新增 `tests/unit/platform/five-plane-execution/worker-drain-protocol.test.ts`，定向验证 checkpoint、lease release 与 recovery notification 三条链路。 | 关闭的是 worker drain 协调主链，不外推 worker pool 其他投影/调度条目都已完成。 |
| R20-04 | §99 Planner/Projections/Worker Pool 深层缺陷 | P1 | `plan-evaluator.ts:23` | 资源估算=steps.length\*1000 天真启发——无 per-step cost/tool cost/parallelism cost(§13) | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。 | 不宣称已修复。 |
| R20-05 | §99 Planner/Projections/Worker Pool 深层缺陷 | P1 | `plan-evaluator.ts` | 无 parallelism limit check——§13 要求验证并行分支 vs worker pool capacity | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。 | 不宣称已修复。 |
| R20-06 | §99 Planner/Projections/Worker Pool 深层缺陷 | P1 | `plan-builder.ts:44` | Plan 输出缺 PlanGraphBundle 必须字段：planGraphId/graphVersion/schedulerPolicy/budget/riskProfile/validationReport | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。 | 不宣称已修复。 |
| R20-07 | §99 Planner/Projections/Worker Pool 深层缺陷 | P1 | `projections/ (全部9文件)` | processedEventIds 为无界 string[] + Array.includes() O(n)——rebuild 长实体退化为 O(n²) | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。 | 不宣称已修复。 |
| R20-08 | §99 Planner/Projections/Worker Pool 深层缺陷 | P1 | `projections/ (全部9文件)` | Shallow spread 后直接修改嵌套对象——破坏前 state 引用，replay 幂等性失效 | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。 | 不宣称已修复。 |
| R20-09 | §99 Planner/Projections/Worker Pool 深层缺陷 | P1 | `workflow-run-projection.ts` | 不处理 workflow_run.created/completed/failed——无法从 run 创建/完成进入对应状态 | 已修复（源码修复并补定向测试） | 根因是旧 projection 只消费 `workflow:step_* / division:* / subtask:* / task:status_changed` 这组 legacy/旁路事件，没有把 `workflow_run.*` 作为主生命周期事实接入；同时初始化逻辑还会优先把 `taskId` 写进 `workflowId`，导致即使后续补 handler 也会被错误的默认值挡住。 | 本轮在 `src/platform/state-evidence/events/projections/workflow-run-projection.ts` 增加 `handleWorkflowRunCreated/Completed/Failed`，并允许 `workflowRunId/workflowId` 覆盖早先的 `taskId` 占位值。 | 定向验证已通过：`tests/unit/platform/state-evidence/events/projections/workflow-run-projection.test.ts` 新增 `handles workflow_run lifecycle events` 与 `handles workflow_run.failed`。 |
| R20-10 | §99 Planner/Projections/Worker Pool 深层缺陷 | P1 | `governance-projection.ts:204-208` | 未知 approval/decision 事件 fallback 推断为 approval_granted——静默将拒绝视为授权 | 已解决（源码复核/测试补强） | 根因不是现行 governance projection 还在用 `includes("approval")/includes("decision")` 猜测授权成功，而是复审条目停留在删除前的字符串推断版本；当前未知 decision/approval 事件只会返回 `actionType: null`，并在 expired/cancelled 这类显式拒绝语义下保持 deny。 | `src/platform/five-plane-state-evidence/events/projections/governance-projection.ts:210-252,380-463` 已去掉错误的 approval_granted fallback；本轮补充 `tests/unit/platform/state-evidence/events/projections/governance-projection.test.ts:157-170`，验证 `decision:expired` 不会再被误记为授权成功。 | 属于投影语义真实收敛，现关闭。 |
| R20-11 | §99 Planner/Projections/Worker Pool 深层缺陷 | P1 | `worker-drain-protocol.ts:8-14` | WorkerDrainRequest 缺 drainReason；Receipt 缺 forcedHandoffCount/cleanupResult(§9) | 已解决（源码复核/测试补强） | 根因不是当前 worker drain contract 仍缺这些字段，而是复审条目停留在旧接口；现行 `WorkerDrainRequest` 已要求 `drainReason`，`WorkerDrainReceipt` 已包含 `forcedHandoffCount` 与 `cleanupResult`，本轮又把 `cleanupResult` 明确放入运行时返回对象，避免只停留在类型层。 | `src/platform/five-plane-execution/worker-pool/worker-drain-protocol.ts:39-70,192-205,327-334` 已定义并回传这些字段；本轮补充 `tests/unit/platform/execution/worker-pool/worker-drain-protocol.test.ts:24-40,74-84`，验证 begin/advance 路径都带有 `drainReason` 上下文和 `forcedHandoffCount/cleanupResult` 合同。 | 该条只按“合同字段缺失”本体关闭，不外推为 drain 编排全量完备。 |
| R20-12 | §99 Planner/Projections/Worker Pool 深层缺陷 | P1 | `worker-drain-protocol.ts:32` | Deadline 用 ISO string > 比较——混合 timezone/format 破坏排序 | 已解决（源码复核/测试补强） | 根因不是当前 deadline 比较仍在做字符串排序，而是复审条目停留在修复前版本；现行 `createReceipt()` 和 `isDeadlineExceeded()` 都已把 `observedAt/requestedAt/deadlineAt` 解析成 epoch milliseconds 再比较。 | `src/platform/five-plane-execution/worker-pool/worker-drain-protocol.ts:279-280,348-351` 已统一使用 `new Date(...).getTime()`；本轮补充 `tests/unit/platform/execution/worker-pool/worker-drain-protocol.test.ts:43-57`，用 `2024-01-01T11:00:00+08:00` vs `2024-01-01T03:30:00Z` 证明数值时间比较会判定超时，而字符串字典序不会。 | 属于时序比较逻辑真实收敛，现关闭。 |
| R20-13 | §99 Planner/Projections/Worker Pool 深层缺陷 | P2 | `worker-status-projection.ts` | 不处理 worker:registered/deregistered/drain_started——pool 成员变更不可追踪 | 已解决（源码复核/测试补强） | 根因不是当前 worker status projection 仍缺少这些生命周期事件，而是复审条目停留在仅覆盖 claim/heartbeat/writeback 的早期版本；现行 projection 已为 registered/deregistered/drain_started 三类成员变更事件写入对应状态。 | `src/platform/five-plane-state-evidence/events/projections/worker-status-projection.ts:316-326` 已分别处理 `worker:registered`、`worker:deregistered`、`worker:drain_started`；本轮补充 `tests/unit/platform/state-evidence/events/projections/worker-status-projection.test.ts:148-186`，验证三类事件都会更新投影状态且可追踪。 | 属于旧审计结论未同步，现关闭。 |
| R20-14 | §99 Planner/Projections/Worker Pool 深层缺陷 | P2 | `tool-usage-projection.ts:224-230` | plugin:invocation_completed 不设 status="completed"——永远停在 "started" | 已解决（源码复核/测试补强） | 根因不是当前 `plugin:invocation_completed` 路径仍停留在 started，而是复审条目沿用了旧逻辑判断；现行 projection 在完成事件上会把 `status` 置为 `completed`，并同步累计 success/lastSuccessAt。 | `src/platform/five-plane-state-evidence/events/projections/tool-usage-projection.ts:284-291` 已显式写入 `status = "completed"`；本轮补充 `tests/unit/platform/state-evidence/events/projections/tool-usage-projection.test.ts:79-91`，直接锁定 plugin completion 的 completed/success 语义。 | 该条按审计本体已成立修复，现关闭。 |
| R20-15 | §99 Planner/Projections/Worker Pool 深层缺陷 | P2 | `approval-queue-projection.ts` | 不处理 decision:expired/cancelled——过期审批永远停在 "requested" | 已解决（源码复核/测试补强） | 根因不是 approval queue 现在仍完全忽略 expired/cancelled，而是复审条目没有吸收后续状态机分支补齐；当前 `decision:expired` 和 `decision:cancelled` 都会更新终态并记录 `respondedAt`。 | `src/platform/five-plane-state-evidence/events/projections/approval-queue-projection.ts:280-317` 已覆盖 `decision:expired/cancelled`；本轮补充 `tests/unit/platform/state-evidence/events/projections/approval-queue-projection.test.ts:183-199`，验证两类事件不再停留在 `requested`。 | 属于真实投影修复，现关闭。 |
| R20-16 | §99 Planner/Projections/Worker Pool 深层缺陷 | P2 | `task-decomposition-service.ts:17` | 每个 step 无条件添加 "read" 作首 tool | 已解决（源码复核/测试补强） | 根因不是现行 decomposition 仍对所有 step 预置 `read`，而是复审条目停留在条件收敛前的旧实现；当前只有依赖前序输出的 step 才会补 `read`，补偿和输出验证工具也按各自条件单独追加。 | `src/platform/five-plane-orchestration/planner/task-decomposition-service.ts:12-22` 已按 `dependsOnStepIds.length > 0` 条件化插入 `read`；本轮补充 `tests/unit/platform/orchestration/planner/task-decomposition-service.test.ts:100-129`，分别验证孤立 step 不带 `read`、依赖 step 才带 `read`。 | 属于实现已修、测试证据补齐，现关闭。 |
| R20-17 | §99 Planner/Projections/Worker Pool 深层缺陷 | P2 | `worker-registry-service.ts` | listStaleWorkers() 无周期调用——stale worker 不转 dead/不生成 incident(§9) | 已修复（源码/测试） | 根因不是仓内完全没有 stale-heartbeat 检测逻辑，而是检测 helper 一直悬空：`WorkerRegistryService.detectStaleHeartbeats()` 没有接入任何长生命周期生产入口，`api-server` 也没有把 `taskStore` / incident service 传给 `HttpApiServer`；同时 stale 查询会把已标记 `offline` 的 worker 再次扫出来，意味着就算硬加轮询也会重复打事件/incident。 | 本轮在 `src/platform/five-plane-interface/api/http-api-server.ts:21-27,125-128,174-177,192-194,220-257` 增加了 API server 生命周期内的周期 stale-worker sweep，并在 `runWorkerHeartbeatSweep()` 中调用 `detectStaleHeartbeats()` 后打开 incident；`src/platform/five-plane-state-evidence/truth/sqlite/repositories/worker-snapshot-repository.ts:288-296` 让 stale 查询排除已 `offline` 的 worker，避免重复告警；`src/sdk/cli/api-server.ts:157-160,225-243` 也已把 `taskStore` 与 `IncidentCaseService` 真实接入生产启动链。定向测试 `tests/integration/platform/interface/api/http-api-server-architecture-regressions.test.ts:222-249` 已验证 stale worker 会被 sweep 为 `offline`，且只创建 1 条 incident。 | 这是把“存在 helper、没有运行时 owner”的真空状态补成真实后台检测链。 |
| R20-18 | §100 API Routes / UX 缺陷 | P0 | `billing-routes.ts:47-59` | Auth bypass——任何非空 Authorization 或 x-api-key header 即跳过 HMAC 签名验证 | 已修复（源码/测试） | 根因不只是历史上的 header bypass；复核时确认文件里所谓“修复”仍是假修复：它虽然不再显式放行 `Authorization/x-api-key`，但只是把 `x-webhook-signature` 直接和 shared secret 做字符串比较，没有按请求体计算 HMAC，因此并不满足 webhook 签名校验 contract。 | 本轮在 `src/platform/five-plane-interface/api/http-server/billing-routes.ts:35-58,60-78,95-99` 改为对原始请求体执行真实 `HMAC-SHA256` 校验，并支持 `x-webhook-timestamp + payload` 的签名形式；仅有 auth headers 不再构成任何放行条件。`tests/unit/platform/interface/api/http-server/billing-routes.test.ts:39-60,134-156` 已验证有效签名可通过，而携带 `Authorization/x-api-key` 但签名错误时仍返回 `Webhook signature is invalid`。 | 这里明确修掉了“假 HMAC 校验”，不是只改文案。 |
| R20-19 | §100 API Routes / UX 缺陷 | P0 | `billing-routes.ts:36-106` | Handler body 完整重复35行出现在 POST /usage 和 POST /settle 两条路由中——逻辑分叉风险 | 已解决（源码复核/既有测试佐证） | 根因不是当前 `billing-routes.ts` 里仍保留两段重复 handler，而是复审条目引用了 billing route 收口前的旧文件形态；现行实现只保留一个 `POST /v1/billing/webhooks/reconcile` 路由，原指控里的 `POST /usage` / `POST /settle` 在该文件中已不存在。 | `src/platform/five-plane-interface/api/http-server/billing-routes.ts:89-102` 当前只导出单一路由；`src/platform/five-plane-interface/api/http-api-server.ts:662-665` 也仅一次性挂载 `createBillingRoutes()`；既有 `tests/unit/platform/interface/api/http-server/billing-routes.test.ts:29-37` 已锁定 `routes.length === 1` 且路径为 `/v1/billing/webhooks/reconcile`。 | 属于旧审计基线滞后，本条按现行源码关闭。 |
| R20-20 | §100 API Routes / UX 缺陷 | P1 | `incident-routes.ts` | tenantId/principal 从 header 解析后丢弃——incidents 查询无租户隔离 | 已解决（源码复核/既有测试佐证） | 根因不是现行 incidents route 仍把 principal/tenant scope 丢弃，而是复审条目停留在 tenant-aware facade 接入前；当前 list/get/patch 路径都会先经 `requirePrincipal()` 和 `resolveTenantScope()` 收敛到租户作用域，再调用 tenant-aware incident service。 | `src/platform/five-plane-interface/api/http-server/incident-routes.ts:73-80,98-107,154-171` 已按 principal 派生 tenant scope；既有 `tests/unit/platform/interface/api/http-server/incident-routes.test.ts:91-107,155-177` 已分别验证列表隔离与跨租户 PATCH 被拒。 | 属于旧审计基线滞后，现关闭。 |
| R20-21 | §100 API Routes / UX 缺陷 | P1 | `incident-routes.ts` | total=incidents.length(slice 后)非实际总数——分页 contract 违反 | 已修复（源码/测试） | 根因是 route 之前直接把 `listIncidents(limit)` 的返回长度当 total，导致分页后 `total` 丢失真实全集计数；tenant 过滤和 limit slice 混在一个接口里，使 route 无法区分“结果条数”和“全集总数”。 | 本轮在 `src/platform/five-plane-state-evidence/incident/index.ts:86-100` 拆出 `countIncidents()`，并在 `src/platform/five-plane-interface/api/http-server/incident-routes.ts:78-85` 改为返回独立 total。`tests/unit/platform/interface/api/http-server/incident-routes.test.ts:110-135` 验证 `limit=2` 时仍返回 `total=3`。 | 这是本轮真实分页合同修复，不是假关闭。 |
| R20-22 | §100 API Routes / UX 缺陷 | P1 | `prompt-routes.ts:62` | 404 返回 {bundle:null} 非统一 ErrorEnvelope 结构 | 已修复（源码/测试） | 根因不是 prompt 查询缺失 bundle 时仍走成功包裹，而是复审条目未吸收后续错误响应改造；现行 missing-bundle 分支已改为统一 `buildJsonErrorResponse()`，不再把错误塞进 data payload。 | `src/platform/five-plane-interface/api/http-server/prompt-routes.ts:168-175` 已返回标准错误 envelope；本轮补充 `tests/unit/platform/interface/api/http-server/prompt-routes.test.ts:191-214`，验证 404 响应体顶层为 `error.code = prompt.bundle_not_found`。 | 属于真实路由合同修复，现关闭。 |
| R20-23 | §100 API Routes / UX 缺陷 | P1 | `prompt-routes.ts:77-84` | deprecate 端点不检查 bundle 是否存在——静默成功 | 已修复（源码/测试） | 根因不是 deprecate 路由现在仍对缺失 bundle 静默 200，而是复审条目停留在未捕获 registry miss 的旧版本；当前 `deprecateBundle()` miss 会被路由转换成 404 ErrorEnvelope。 | `src/platform/five-plane-interface/api/http-server/prompt-routes.ts:193-200` 已在 miss 时返回 `prompt.bundle_not_found`；本轮补充 `tests/unit/platform/interface/api/http-server/prompt-routes.test.ts:216-238`，验证缺失 bundle deprecate 返回 404 而非静默成功。 | 属于真实错误路径修复，现关闭。 |
| R20-24 | §100 API Routes / UX 缺陷 | P1 | `prompt-routes.ts:12` | Zod schema 使用 .passthrough()——允许任意未验证字段进入 | 已解决（源码复核/测试补强） | 根因不是现行 prompt route schema 仍使用 `.passthrough()`，而是复审条目停留在 schema 收紧前；当前 request / registration / deprecation 三个 schema 均为 `.strict()`。 | `src/platform/five-plane-interface/api/http-server/prompt-routes.ts:11-45` 已全部使用 `.strict()`；本轮补充 `tests/unit/platform/interface/api/http-server/prompt-routes.test.ts:240-267`，验证 POST 带 `unexpected` 字段会被拒绝。 | 属于 contract 边界已收紧、复审未同步，现关闭。 |
| R20-25 | §100 API Routes / UX 缺陷 | P1 | `utils.ts:167-175` | buildJsonResponse 无 X-Trace-Id header——违反 observability contract | 已解决（源码复核/测试补强） | 根因不是当前 `buildJsonResponse()` 仍完全不支持 trace header，而是复审条目停留在 helper 收口前；现行实现已经在传入 `traceId` 时附加 `x-trace-id`，错误响应 helper 也同步支持该 header。 | `src/platform/five-plane-interface/api/http-server/utils.ts:176-205` 显示 `buildJsonResponse()` 与 `buildJsonErrorResponse()` 都会输出 `x-trace-id`；本轮补充 `tests/unit/platform/interface/api/http-server/utils.test.ts:358-361,389-395`，直接验证成功/错误响应两条路径都会带 trace header。 | 该条按 helper 合同本体关闭，不外推所有上层路由都已完整透传 traceId。 |
| R20-26 | §100 API Routes / UX 缺陷 | P2 | `dashboard-routes.ts:53-96` | 每请求 new 5个 service 实例(无 DI)——GC 压力 + 无状态共享 | 已修复（源码修复并补定向测试） | 根因是 `createDashboardRoutes()` 之前只把 `missionControlService` 放在依赖注入边界内，其余 `PlatformWorkbenchSnapshotService`、inventory/template/judge service 都在 handler 里临时 `new`，导致请求级对象抖动且无法复用注入替身。 | 本轮把 `platformWorkbenchSnapshotService`、`benchmarkInventoryService`、`deploymentInventoryService`、`projectionInventoryService`、`complianceProgramTemplateService` 和 `judgeProviderRegistryService` 全部提升为 `DashboardRouteDeps` 的可选注入项，并在 route 构建阶段一次性实例化默认对象。 | 定向验证已通过：`tests/unit/platform/interface/api/http-server/dashboard-routes.test.ts` 新增 `reuses injected singleton services instead of new per request`。 |
| R20-27 | §100 API Routes / UX 缺陷 | P2 | `template-engine/ (全目录)` | 仅16行——无版本/domain/risk 感知 | 已解决（源码复核/测试补强） | 根因是该条与前文 `R11-18` 指向同一历史 stub，但 reaudit 行仍停留在 template engine 尚未扩展前的旧基线；现行实现已经有 `domainId/riskProfile/version`、域校验、模板推荐和参数绑定，只是这条重复挂账之前没有单独补证据。 | `src/interaction/ux/template-engine/index.ts` 当前 `InteractionTemplateSchema` 已包含 `domainId/riskProfile/version`，并导出 `validateTemplateForDomain()`、`recommendTemplatesForDomain()`、`instantiateTemplate()`；本轮在 `tests/unit/interaction/ux/template-engine.test.ts` 新增 domain/risk/version、域校验、推荐排序和参数绑定断言。 | 该条按现行源码与测试关闭，避免与 `R11-18` 重复挂账。 |
| R20-28 | §100 API Routes / UX 缺陷 | P2 | `wizard/ (全目录)` | 无 step validation/progress persistence/risk preview | 已修复（源码修复并补定向测试） | 这条原本混了三层问题：一是旧 wizard 只有步骤完成布尔，没有基于必填答案的 step validation；二是进度持久化虽然已经有序列化/保存 API，但 audit 没把它作为正式证据写回；三是完全缺少风险预览结构，前端无法在提交前知道哪些步骤需要额外 review。 | 当前 `src/interaction/ux/wizard/index.ts` 已在既有 `serializeWizardSession()/deserializeWizardSession()/saveWizardSession()/getWizardProgress()` 之外，新增 `requiredAnswerKeys/riskLevel/riskHints`、`validateWizardStep()` 与 `getWizardRiskPreview()`，并让 `canAdvanceWizard()` 受 step validation 约束。 | 定向验证已通过：`tests/unit/interaction/ux/wizard/navigation-and-session.test.ts` 新增 `validateWizardStep reports missing required answers` 与 `getWizardRiskPreview surfaces medium+ risk steps and review requirement`。 |
| R20-29 | §100 API Routes / UX 缺陷 | P2 | `onboarding/ (全目录)` | sessions 存储为内存 Map——重启丢失所有进度 | 已修复（源码修复并补定向测试） | 根因是 `UserPortalService` 之前把 session 直接写入进程内 `Map`，没有任何 repository 抽象，因此既无法跨 service instance 共享，也无法挂到 runtime truth 做持久恢复。 | 本轮在 `src/interaction/ux/onboarding/index.ts` 引入 `UserPortalSessionRepository`、`InMemoryUserPortalSessionRepository` 与 `DurableUserPortalSessionRepository`，并让 `UserPortalService` 改为仓储驱动；`src/interaction/ux/user-experience-orchestration-service.ts` 也开放了 portal repository 注入。`tests/unit/interaction/ux/onboarding.test.ts` 新增“共享仓储跨实例可读”和“通过 RuntimeLifecycleRepository round-trip 恢复”两条回归。 | 关闭的是 onboarding session 的硬编码内存存储问题；默认仍可用 in-memory 仓储做本地模式，但不再被代码结构强制绑死。 |
| R20-30 | §100 API Routes / UX 缺陷 | P2 | `incident/prompt/dashboard routes` | 均无 cursor pagination——仅 offset/limit 对大数据集性能差 | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。 | 不宣称已修复。 |
| R20-31 | §101 Graceful Shutdown / Concurrency 缺陷 | P0 | `degradation-controller.ts (routeD0)` | 递归调用无 max-depth guard——provider 链构成环时无限递归导致栈溢出 | 已修复（源码/测试） | 根因不只是历史上缺少递归深度保护；复核确认现有 guard 还把 `routeRecursionDepth` 存成实例级字段，失败深度会跨请求泄漏，`reset()` 也不清零，导致后续独立请求可能错误命中 `max_recursion_exceeded`，属于“保护存在但作用域错误”的真实缺陷。 | 本轮已将递归深度改为单次路由调用内的局部状态：`src/platform/model-gateway/degradation/degradation-controller.ts` 现在通过 `routeWithDepth()/routeAfterEscalation()` 传递深度，不再跨请求共享；新增 `tests/unit/platform/model-gateway/degradation/degradation-controller-recursion-guard.test.ts`，验证连续两次失败请求在 `reset()` 后都能稳定收敛到 D3，而不会被上一次失败深度污染。 | 这是对真实残留缺陷的代码修复，不是假关闭。 |
| R20-32 | §101 Graceful Shutdown / Concurrency 缺陷 | P0 | `service-registry.ts (teardownAll)` | Promise.all 并发 teardown——违反 §9 要求的顺序 drain→flush→close | 已解决（源码复核/测试补强） | 根因不是现行 `ServiceRegistry.teardownAll()` 还在并发 teardown，而是复审条目停留在早期 `Promise.all` 版本；当前实现已经按依赖拓扑逆序串行 teardown，保证 dependent service 先于 dependency 收口。 | `src/platform/shared/lifecycle/service-registry.ts:301-325` 现在逐个 `await` reverse-topological teardown，不再并发清理；`tests/unit/platform/shared/service-registry-lifecycle-extended.test.ts:212-236` 已验证 teardown 顺序为 `dependent -> base`。 | 属于生命周期顺序已真实收敛，现关闭。 |
| R20-33 | §101 Graceful Shutdown / Concurrency 缺陷 | P1 | `websocket-bridge.ts` | WS listeners 不 removeAllListeners on disconnect——listener leak | 已解决（源码复核/测试补强） | 根因不是现行 `WebSocketBridge` 还完全遗漏断连 listener 清理，而是复审条目停留在 `handleDisconnection()` 未收尾的旧版本；当前断连路径已经同步移除 task 订阅、slow-consumer 标记和 socket listeners。 | `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:390-406` 已在 disconnect 时调用 `ws.removeAllListeners()` 并清理订阅/slow-consumer 状态；本轮补充 `tests/unit/platform/interface/channel-gateway/websocket-bridge-coverage.test.ts:266-304`，直接验证断连后 client、task subscriber 和 slow consumer 计数都归零。 | 属于旧审计结论未同步，现关闭。 |
| R20-34 | §101 Graceful Shutdown / Concurrency 缺陷 | P1 | `websocket-bridge.ts (close)` | close() 无 timeout——misbehaving client 挂起 shutdown | 已解决（源码复核/测试补强） | 根因不是当前 `close()` 仍无限等待，而是复审条目停留在 timeout guard 加入前；现行 shutdown 路径已经设置 5 秒超时，并在 client 不配合时强制 resolve。 | `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:633-650` 已引入 5s timeout fallback；本轮补充 `tests/unit/platform/interface/channel-gateway/websocket-bridge-coverage.test.ts:306-343`，模拟 `wss.close()` 永不回调时 `bridge.close()` 仍会完成。 | 属于 shutdown 卡死路径已真实封口，现关闭。 |
| R20-35 | §101 Graceful Shutdown / Concurrency 缺陷 | P1 | `stream-bridge.ts` | 无 dispose()——per-stream maps 无界增长 | 已修复（源码/测试） | 根因不只是历史上缺少 `dispose()`；复核时确认现有实现虽然已经有 `dispose()`，但只清理 stream 级 map，没有清理 `clientLastSequence/clientStreamSubscription`，shutdown 后仍会残留 client 侧 cursor 状态，属于不完整修复。 | 本轮在 `src/platform/five-plane-interface/channel-gateway/stream-bridge.ts:314-340` 让 `closeStream()` 同步删除属于该 stream 的 client cursor，并让 `dispose()` 额外清空 `clientLastSequence/clientStreamSubscription`；`tests/unit/platform/interface/channel-gateway/stream-bridge.test.ts:139-165` 已验证 `closeStream()` 和 `dispose()` 都会把 client/stream maps 清空。 | 这是把“已有 dispose 但清理不全”的真实残留补齐，不是假关闭。 |
| R20-36 | §101 Graceful Shutdown / Concurrency 缺陷 | P1 | `durable-event-bus.ts` | 10ms poll interval = 100次/秒/consumer 无自适应 backoff | 已解决（源码复核/测试补强） | 根因不是现行 `DurableEventBus` 仍固定 10ms 轮询，而是复审条目停留在旧实现；当前代码已经把基础轮询间隔提高到 100ms，并按 queue depth / consumer count 做自适应 backoff。 | `src/platform/five-plane-state-evidence/events/durable-event-bus.ts` 已定义 `ACTIVE_SUBSCRIBER_POLL_INTERVAL_MS = 100`，并通过 `calculatePollingInterval()` 在高积压时拉长轮询；`tests/unit/platform/state-evidence/events/durable-event-bus-backpressure-2297.test.ts` 已验证基础间隔、自适应 backpressure 计算和 runtime metrics 接线。 | 属于旧审计结论未同步到现行实现，现关闭。 |
| R20-37 | §101 Graceful Shutdown / Concurrency 缺陷 | P1 | `worker-drain-protocol.ts:32` | ISO string > 比较——mixed timezone 破坏排序 | 已解决（同 R20-12） | 根因与 `R20-12` 相同：复审条目沿用了旧版 ISO 字符串比较结论，现行 shutdown/drain 时序判断已经改成 epoch 毫秒比较。 | 证据同 `src/platform/five-plane-execution/worker-pool/worker-drain-protocol.ts:279-280,348-351` 与 `tests/unit/platform/execution/worker-pool/worker-drain-protocol.test.ts:43-57`；混合时区场景的定向测试已通过。 | 重复挂账，沿用同一修复结论关闭。 |
| R20-38 | §101 Graceful Shutdown / Concurrency 缺陷 | P1 | `cli/api-server.ts (shutdown)` | 缺 DurableEventBus/WS/ServiceRegistry/ProviderPool/process-tracker cleanup | 已修复（源码/测试） | 根因不是所有列举资源都仍未接线：复核确认 `HttpApiServer.stop()` 已负责 WebSocket 关闭，`getGlobalGracefulShutdown()` 也已包含 `ServiceRegistry` teardown；真实残留是 API CLI 自身创建的 `TypedEventBus` 没有在 shutdown 时 dispose，且 `ProcessTracker` 与 model call provider / provider pool 这类全局进程级资源没有挂到 CLI shutdown 链路。 | 本轮在 `src/sdk/cli/api-server.ts` 新增 `typed_event_bus`、`model_call_provider`、`process_tracker` 三个 shutdown handler；其中 `resetModelCallProvider()` 会级联触发 `ModelCallProviderService.dispose()` 和 `UnifiedChatProvider.dispose()`，进一步释放 provider credential pools。新增 `tests/unit/sdk/cli/api-server-shutdown-wiring.test.ts`，验证这三条 cleanup wiring 已实际接入 CLI shutdown 代码路径。 | 这是把真实缺失的 lifecycle wiring 补齐后关闭，不把已存在的 WS / ServiceRegistry 路径误算成“本轮修复”。 |
| R20-39 | §101 Graceful Shutdown / Concurrency 缺陷 | P2 | `circuit-breaker.ts (failureTimestamps)` | 无上限数组——高吞吐场景可达 600k+ entries | 已修复（源码/测试） | 根因不是 `failureTimestamps` 完全不 prune，而是它只按 `monitorWindowMs` 做时间裁剪，没有容量上限；在长窗口高吞吐下，窗口内数组仍可能膨胀到非常大，造成可观内存占用。 | 本轮在 `src/platform/model-gateway/provider-registry/circuit-breaker.ts` 增加 `maxWindowEntries` 上限，并在 success/failure 两条时间窗口上统一调用 `trimWindowEntries()`；新增 `tests/unit/platform/model-gateway/provider-registry/circuit-breaker-memory-bound.test.ts`，验证 1000 次 success/failure 采样后两个窗口都被限制在配置上限内。 | 属于真实内存上界缺失修复，现关闭。 |
| R20-40 | §101 Graceful Shutdown / Concurrency 缺陷 | P2 | `service-registry.ts (reset)` | clear 在 teardown 前执行——teardown 中访问 registry 报 undefined | 已解决（源码复核/测试补强） | 根因不是当前 `reset()` 还会先 `clear()` 再 teardown，而是复审条目沿用了 reset 重构前的旧行为；现行实现直到 teardown 全部完成后才统一清空 `instances/services/initializing`。 | `src/platform/shared/lifecycle/service-registry.ts:127-159` 已把 `clear()` 挪到 teardown 循环之后；本轮补充 `tests/unit/platform/shared/service-registry-lifecycle-extended.test.ts:258-280`，验证 teardown 执行期间依赖服务的 registration 和 initialized state 仍然可见。 | 属于 reset 生命周期顺序已修正，现关闭。 |
| R20-41 | §101 Graceful Shutdown / Concurrency 缺陷 | P2 | `durable-event-bus.ts (deliveryChains)` | Promise chain 无界增长——长运行进程内存线性递增 | 已解决（源码复核/测试补强） | 根因不是现行 `deliveryChains` 仍只增不删，而是复审条目停留在 cleanup 收口前；当前 fan-out 链路已经在 `finally` 中删除当前 consumer 对应的 settled chain。 | `src/platform/five-plane-state-evidence/events/durable-event-bus.ts` 现在在 `this.deliveryChains.set(consumerId, chain)` 后，通过 `chain.finally()` 仅在当前 map 项仍指向同一 promise 时执行删除；新增独立测试 `tests/unit/platform/state-evidence/events/durable-event-bus-delivery-chain-cleanup.test.ts`，验证事件投递完成后 `deliveryChains.size === 0`。 | 属于实现已收口但此前缺少干净证据，现关闭。 |
| R20-42 | §101 Graceful Shutdown / Concurrency 缺陷 | P2 | `graceful-shutdown.ts (handlers)` | 按插入逆序执行非依赖感知——顺序脆弱 | 已解决（源码复核/测试补强） | 根因不是现行 graceful shutdown 仍完全依赖脆弱的 reverse insertion order，而是复审条目停留在 `dependsOn` 与拓扑排序接入前；当前 handler 执行顺序已经可由依赖关系显式建模。 | `src/platform/five-plane-execution/startup/graceful-shutdown.ts` 已引入 `dependsOn`、`topologicalSortHandlers()` 和 dependency-aware `executeShutdown()`；新增独立测试 `tests/unit/platform/execution/startup/graceful-shutdown-dependency-order.test.ts`，验证 `close -> flush -> drain` 会按依赖关系而不是单纯插入顺序执行。 | 属于 shutdown 顺序模型已真实升级，现关闭。 |
| R20-43 | §102 Side-Effect / Eval Gate / Static Analysis 缺陷 | P0 | `side-effect-manager.ts (全文件)` | 无 ReconciliationWorker/周期 side-effect sweep——仅 apply 不 probe/不对账 | 已修复（源码/测试） | 根因是旧 `SideEffectManager` 虽然能 `applyReconciliation()`，但缺少周期性 probe 入口，导致 ambiguous / confirmable 的 side effect 只能被动接收外部 reconciliation 结果，无法由 manager 自己组织 probe→reconciliation→nextAction 这条闭环。 | 本轮复核并补齐 `src/platform/five-plane-execution/side-effect-manager.ts` 中的 `reconciliationWorker` 与 `sweepReconciliation()`：它会对可对账 side effect 执行 probe，交给 `ReconciliationWorker.determineNextAction(...)` 生成下一动作，再创建 `ReconciliationRecord` 并返回 sweep 结果；`tests/unit/platform/five-plane-execution/side-effect-manager.test.ts` 已新增并通过 `SideEffectManager sweepReconciliation probes ambiguous effects and confirms them` 与 `... skips non-reconcilable side effects`。 | 关闭的是 side-effect manager 自带的 probe/sweep 主链，不外推外部 connector probe 能力或所有 reconciliation 策略都已完备。 |
| R20-44 | §102 Side-Effect / Eval Gate / Static Analysis 缺陷 | P0 | `side-effect-manager.ts` | 无初始注册阶段(proposed→reserved→committing)和 pre-commit re-validation | 已修复（源码/测试） | 根因是 runtime state machine 虽然早就定义了 `proposed/approved/reserved/committing/committed/confirming/confirmed` 状态，但 `SideEffectManager` 只暴露 reconciliation/compensation API，注册期和 commit 前再校验完全没有统一 owner。 | 本轮在 `src/platform/five-plane-execution/side-effect-manager.ts` 增加 `registerProposal/approve/reserve/startCommit/recordCommitted/startConfirmation/confirm`，并引入 `preCommitValidator` 在进入 commit path 前统一 re-validation；`tests/unit/platform/five-plane-execution/side-effect-manager.test.ts` 已验证完整注册生命周期和每个 pre-commit 目标状态都会触发再校验。 | 关闭的是 side-effect manager 的注册/预提交主链，不外推 ReconciliationWorker 等独立条目已完成。 |
| R20-45 | §102 Side-Effect / Eval Gate / Static Analysis 缺陷 | P1 | `workflow-validator.ts` | 缺 5/12 contract 要求的静态分析检查(resource-bound, auth-scope, idempotency, timeout-budget, data-flow) | 已修复（源码/测试） | 根因是 `MinimalWorkflowStep` 已保留 `budgetIntent/sideEffectProfile/executionRoleId/nodeType` 这些丰富元数据，但 validator 一直只校验 DAG 和基础字段，没有把运行前的资源边界、执行作用域、幂等/补偿语义与 data-flow 合同真正前置。 | 本轮在 `src/platform/five-plane-orchestration/oapeflir/workflow/workflow-validator.ts` 新增 `step.resource_bound_missing / step.auth_scope_missing / step.idempotency_missing / step.timeout_budget_mismatch / dependency.data_flow_missing` 等检查；`tests/unit/platform/orchestration/oapeflir/workflow/workflow-validator.test.ts` 已定向覆盖 tool/llm/resource/data-flow 负例。 | 关闭的是这 5 类缺失的静态分析检查，不外推 contract 第 3 节所有条目都已齐备。 |
| R20-46 | §102 Side-Effect / Eval Gate / Static Analysis 缺陷 | P1 | `workflow-validator.ts (返回类型)` | 结果类型 WorkflowLintReport vs contract 要求的 StaticCompatibilityIssue[] | 已修复（兼容式导出收口） | 根因不是 `WorkflowLintReport` 本身有错，而是代码只提供 report 形态，没有对 contract 侧导出 canonical issue array，导致 `StaticCompatibilityIssue[]` 调用面无处落脚。 | 本轮在 `src/platform/five-plane-orchestration/oapeflir/workflow/workflow-validator.ts` 导出 `type StaticCompatibilityIssue = WorkflowLintIssue`，并新增 `validateIssues()` / `validateWorkflowCompatibility()` 返回 issue array；`tests/unit/platform/orchestration/oapeflir/workflow/workflow-validator.test.ts` 已验证 canonical issue array 导出可用，同时不破坏原有 `WorkflowLintReport` API。 | 这是兼容式补齐 contract 导出，不把旧 report API 误写成“被删除”。 |
| R20-47 | §102 Side-Effect / Eval Gate / Static Analysis 缺陷 | P1 | `workflow-builder.ts (normalizeGraph)` | 不映射 MinimalWorkflowDefinition 到 PlanGraphBundle——输出缺必须字段 | 已修复（源码修复并补定向测试） | 根因是旧 `normalizeGraph()` 只做 `nodeIds/edgePairs` 去重，完全没有 canonical graph node/edge、entry/terminal、scheduler policy、budget/risk/validation report，因此保存评审阶段拿不到 `PlanGraphBundle`。 | 本轮在 `src/interaction/ux/workflow-builder-service.ts` 直接把 builder canvas 映射为 `PlanNode[]/PlanEdge[]`，并通过 `createPlanGraphBundle(...)` 生成包含 `entryNodeIds/terminalNodeIds/schedulerPolicy/budgetPlanRef/riskProfile/validationReport` 的 canonical bundle；`tests/unit/interaction/ux/workflow-builder-service.test.ts` 新增 `normalizes graph into a PlanGraphBundle` 断言。 | 关闭的是 workflow builder 输出缺 canonical graph bundle 的问题。 |
| R20-48 | §102 Side-Effect / Eval Gate / Static Analysis 缺陷 | P1 | `workflow-builder.ts (propagateRisk)` | 用 regex 匹配 tool name 推断 risk——应基于 compensationModel/sideEffectProfile | 已修复（源码修复并补定向测试） | 根因是旧 `propagateRisk()` 仅靠节点 label regex 识别 `approve/deploy/delete` 之类关键字，既无法反映可逆性/外部副作用，也无法把 compensation strategy 编进风险继承。 | 本轮在 `src/interaction/ux/onboarding/index.ts` 为 `DraggableComponent` 建模 `sideEffectProfile/compensationModel`，并在 `src/interaction/ux/workflow-builder-service.ts` 的 `buildRiskPropagation()/analyzeWorstPath()` 中基于这些结构化字段计算 inherited risk、worst path budget 和 timeout；`tests/unit/interaction/ux/workflow-builder-service.test.ts` 新增 `propagates risk from sideEffectProfile and compensationModel`。 | 关闭的是 regex 风险推断缺口；现在风险传播以结构化副作用/补偿语义为主。 |
| R20-49 | §102 Side-Effect / Eval Gate / Static Analysis 缺陷 | P1 | `post-execution-quality-gate.ts` | 仅 post-execution gate——§17 要求 pre-release gate 阻止未达标 promotion | 已修复（源码/测试） | 根因是现有质量门只覆盖“执行后如何落到 released/repair/approval/blocked”，没有独立的 pre-release promotion 决策对象，所以 regression、blocking incident、缺证据或未达分数阈值都无法在 promotion 前被 machine-check 拦下。 | 本轮在 `src/platform/prompt-engine/eval/post-execution-quality-gate.ts` 新增 `PreReleaseQualityGate` 与 `PreReleaseQualityGateDecision`，对 regression、blocking incidents、critical case 通过率、evidence completeness 和最小 quality threshold 做 promotion 前拦截；`tests/unit/platform/prompt-engine/eval/post-execution-quality-gate.test.ts` 已新增并通过对应定向回归。 | 关闭的是 pre-release gate 缺失本体，不外推整个 release pipeline 已全量接线。 |
| R20-50 | §102 Side-Effect / Eval Gate / Static Analysis 缺陷 | P1 | `connector-framework-service.ts (connectors)` | 4个 first-party connectors 全返回 stub 数据——无 callback/webhook 实际 path | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。 | 不宣称已修复。 |
| R20-51 | §102 Side-Effect / Eval Gate / Static Analysis 缺陷 | P2 | `connector-framework-service.ts` | manifests/bindings 纯内存存储无持久化——重启丢失所有注册 | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。 | 不宣称已修复。 |
| R20-52 | §102 Side-Effect / Eval Gate / Static Analysis 缺陷 | P2 | `runtime-state-machine.ts (side-effect FSM)` | 16态 vs contract 定义11态——5态无文档且无对应 contract transition | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。 | 不宣称已修复。 |
| R20-53 | §102 Side-Effect / Eval Gate / Static Analysis 缺陷 | P2 | `workflow-builder.ts (analyzeWorstPath)` | 非真实 critical-path 算法——仅 reduce sum 而非 DAG longest path | 已修复（源码/测试） | 根因不是当前 worst-path 还在做简单 reduce，而是历史 helper 虽然用了递归累计成本，却错误地从 root 节点而不是 terminal 节点收敛，导致 diamond/branching DAG 会把入口节点误当 critical path；审计定位在 workflow-builder，实际缺陷落在其调用的 DAG 分析器。 | 本轮在 `src/platform/five-plane-orchestration/planner/plan-dag-validator.ts:215-279` 改为从 terminal nodes 评估最长依赖链，得到真实 DAG critical path；`tests/unit/platform/orchestration/planner/plan-dag-validator.test.ts:40-98,245-257` 已覆盖 linear/diamond/complex DAG 三类 longest-path 场景并通过。 | 这是本轮真实算法修复，现关闭。 |
| R21-01 | §104 Multi-region / Federation / Tenant 缺陷 | P0 | `cdc-replication-service.ts:97` | CDC 无冲突解决(LWW+vector clock)——事件复制无 merge strategy | 已修复（源码修复并补定向测试） | 真实根因是复制事件模型过去没有携带权威 `sourceRegionId/vectorClock` 元数据，`updateVectorClock()` 也只是本地盲增，导致 `detectConflict()` 无法基于事件因果关系判断先后或并发，只能退化成弱时间排序。 | 当前 `src/scale-ecosystem/multi-region/cdc-replication-service.ts` 已为 `CDCReplicationEvent` 增加 `sourceRegionId` 与 `vectorClock`，补入 `VectorClock.fromRecord()/toRecord()/withRegionSequence()`，并把 `updateVectorClock()`、`detectConflict()`、`mergeEventsWithConflictResolution()` 改为基于事件向量时钟做因果/并发冲突判断；`tests/unit/scale-ecosystem/multi-region/cdc-replication-concurrent.test.ts` 已新增并通过并发复制与 conflict merge 回归。 | 关闭的是“CDC 完全没有 vector-clock merge strategy”的真实缺口。 |
| R21-02 | §104 Multi-region / Federation / Tenant 缺陷 | P0 | `multi-region/ (全目录)` | 无 split-brain 检测/fencing epoch——failover 无隔离令牌 | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。 | 不宣称已修复。 |
| R21-03 | §104 Multi-region / Federation / Tenant 缺陷 | P0 | `failover-controller/index.ts:17` | 无 FailoverReconciliationJob——resolveRegionFailover 是无状态纯函数无重试/对账循环 | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。 | 不宣称已修复。 |
| R21-04 | §104 Multi-region / Federation / Tenant 缺陷 | P0 | `region-health-check-service.ts:92` | 无 per-region circuit breaker 状态机(closed/open/half-open) | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。 | 不宣称已修复。 |
| R21-05 | §104 Multi-region / Federation / Tenant 缺陷 | P0 | `multi-region/ (全目录)` | 无读写分离——无 read replica/write routing/read-after-write 一致性 | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。 | 不宣称已修复。 |
| R21-06 | §104 Multi-region / Federation / Tenant 缺陷 | P0 | `multi-region/ (全目录)` | RPO<1min/RTO<30s 未强制——CDC 无 lag 监控,failover 无计时强制 | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。 | 不宣称已修复。 |
| R21-07 | §104 Multi-region / Federation / Tenant 缺陷 | P0 | `federation/ (目录不存在)` | Federation 模块完全缺失——FederationGateway/跨组织信任/能力委托/审计轨迹均无 | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。 | 不宣称已修复。 |
| R21-08 | §104 Multi-region / Federation / Tenant 缺陷 | P0 | `tenant-platform-service.ts:220` | 无资源配额+抢占——超额时无法驱逐低优先级租户负载 | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。 | 不宣称已修复。 |
| R21-09 | §104 Multi-region / Federation / Tenant 缺陷 | P0 | `tenant-platform/ (全目录)` | 无 fair scheduling——无加权公平队列/优先级队列/租户感知调度 | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。 | 不宣称已修复。 |
| R21-10 | §104 Multi-region / Federation / Tenant 缺陷 | P0 | `tenant-platform/ (全目录)` | 无 per-tenant SLO 定义和强制 | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。 | 不宣称已修复。 |
| R21-11 | §105 Ops-maturity 缺陷 | P1 | `explanation-pipeline-service.ts:56` | ExplanationBundle 无 versionLockRef——决策无法绑定产生它的精确模型/prompt 版本 | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。 | 不宣称已修复。 |
| R21-12 | §105 Ops-maturity 缺陷 | P1 | `changepoint-detector/index.ts:26` | 仅单窗口(24h baseline+3h recent)——spec 要求多窗口(1h/6h/24h/7d) | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。 | 不宣称已修复。 |
| R21-13 | §105 Ops-maturity 缺陷 | P1 | `cross-agent-analyzer/index.ts:40` | 用硬编码线性公式算 composite score——无统计 changepoint/显著性检验 | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。 | 不宣称已修复。 |
| R21-14 | §105 Ops-maturity 缺陷 | P1 | `changepoint-detector/index.ts:88` | 无统计 changepoint 算法(CUSUM/Bayesian/PELT)——仅均值对比+固定阈值 | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。 | 不宣称已修复。 |
| R21-15 | §105 Ops-maturity 缺陷 | P1 | `edge-runtime-sync-service.ts:103` | Edge 执行无 risk gate——离线执行跳过所有风险评估 | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。 | 不宣称已修复。 |
| R21-16 | §105 Ops-maturity 缺陷 | P1 | `edge-runtime-sync-service.ts:137` | Conflict resolution 永远 "merge" 但无实际 merge 逻辑——冲突 envelope 直接接受 | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。 | 不宣称已修复。 |
| R21-17 | §105 Ops-maturity 缺陷 | P1 | `chaos-experiment-scheduler.ts:155` | injectFault() 是 no-op stub——返回 fault config 不执行注入,无 blast radius 控制 | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。 | 不宣称已修复。 |
| R21-18 | §105 Ops-maturity 缺陷 | P1 | `chaos-experiment-scheduler.ts:148` | steady-state results 可重复累积——重复 hypothesis 评估导致提前错误完成 | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。 | 不宣称已修复。 |
| R21-19 | §105 Ops-maturity 缺陷 | P1 | `time-travel-debug-service.ts:193` | getVariableState 累积所有历史值不去重——应返回时间点最新值 | 需继续整改（本轮未改） | 该条需要逐文件、逐 contract 或跨模块复验；本轮没有取得足以推翻原指控的直接现行证据，也未实施对应代码改造。 | 为避免假收口，本轮对未明确证伪或未明确完成改造的条目保持保守结论。 | 不宣称已修复。 |
| R21-20 | §105 Ops-maturity 缺陷 | P1 | `auto-stop-loss-service.ts:619` | 升级级别基于 string.includes("emergency")——脆弱文本匹配非实际严重度 | 已解决（源码复核/测试补强） | 根因不是现行升级级别仍通过 `triggerReason.includes("emergency")` 之类的脆弱文本匹配决定，而是审计停留在旧版实现；当前 anomaly / health 入口已经显式使用 severity/status 映射，playbook 执行事件则基于动作策略决定 `escalationLevel`。 | `src/platform/five-plane-control-plane/incident-control/auto-stop-loss-service.ts` 当前在 `evaluateAnomaly()` 中通过 `SEVERITY_TO_ESCALATION[severity]` 做 severity 映射，在 `evaluateHealth()` 中通过 `HEALTH_TO_ESCALATION[status]` 做健康态映射，并在 `determineEscalation()` 里按动作集合 (`disable_new_tasks` / `escalate_to_human` / `pause_non_critical` / `scale_down`) 决定最终 `escalationLevel`。本轮补强 `tests/unit/platform/control-plane/incident-control/auto-stop-loss-service-extended.test.ts`，验证 `executePlaybook()` 在普通文案 `"routine warning without emergency keyword"` 下，只要动作是 `disable_new_tasks` 仍会得到 `critical`，证明不依赖自由文本。 | 本轮不改生产逻辑，主要回写当前显式映射实现，并补了一条直接反证“非字符串匹配”的最小回归。 |
| R21-21 | §105 Ops-maturity 缺陷 | P1 | `ha-program-service.ts:186` | overallStatus 三元条件永远 true——.some() 检查组件存在而非检查不健康组件 | 已解决（源码复核） | 根因不是现行 `overallStatus` 仍然使用会恒为真的错误三元表达式，而是审计结论停留在早期条件写错的版本；当前实现已经把“关键组件未 ready”与“仅非关键组件未 ready”拆成两层判断。 | `src/scale-ecosystem/tenant-platform/ha-program-service.ts` 当前先算 `allReady = components.every((component) => component.ready)`，再算 `hasCriticalNotReady = components.some((component) => !component.ready && (component.componentId === "coordinator" || component.componentId === "postgres"))`，最后将 `overallStatus` 收敛为 `pass/fail/warning`。`src/scale-ecosystem/marketplace/ha-program-service.ts` 现在只是对 tenant-platform 实现的直接 re-export，因此不再存在平行旧实现。现有 `tests/unit/scale-ecosystem/tenant-platform/ha-program-service.test.ts` 已覆盖 `pass`、`fail`、`warning` 三种路径，本轮复跑 `tests/unit/scale-ecosystem/tenant-platform/ha-program-service.test.ts` 与 `tests/unit/scale-ecosystem/marketplace/ha-program-service.test.ts` 均通过。 | 本轮不改生产代码，关闭依据是现行条件表达式与定向测试已经直接覆盖原审计描述的错误分支。 |
| R21-22 | §105 Ops-maturity 缺陷 | P2 | `data-replicator/index.ts:246` | pendingCount=events.length 而非实际失败数 | 已解决（源码复核/测试补强） | 根因不是现行 checkpoint 仍直接把整批 `events.length` 写进 `pendingCount`，而是审计停留在旧实现；当前缺口已经收敛为“需要直接回归证明 pendingCount 代表未确认事件而不是整批事件数”。 | 现行 `src/scale-ecosystem/multi-region/data-replicator/index.ts` 在 flush 后用 `events.length - lastSequence` 计算 `actualPendingCount`，并把它写入 checkpoint；本轮新增 `tests/unit/scale-ecosystem/multi-region/data-replicator-issues.test.ts` 回归，验证 3 个事件中 2 个成功、1 个永久失败时，`checkpoint.pendingCount === 1` 且 `sequenceNumber === 2`。 | 本轮不改这部分生产公式，只补了能直接证明“pendingCount 是未确认事件数”的最小测试依据。 |
| R21-23 | §105 Ops-maturity 缺陷 | P2 | `data-replicator/index.ts:225` | 重试成功时 lastSequence 双重计数 | 已解决（源码复核/测试补强） | 根因不是现行重试路径还会在一次事件上做两次 `lastSequence++`，而是审计没有跟进后续修正；当前实现只在首次成功时加一次，若首次失败则仅在 `retrySucceeded` 后补记一次。 | `src/scale-ecosystem/multi-region/data-replicator/index.ts` 当前在主发送成功路径里加一次 `lastSequence++`，进入 retry 分支后只有 `retrySucceeded` 时再补一次，不会双记；本轮新增 `tests/unit/scale-ecosystem/multi-region/data-replicator-issues.test.ts`，验证“首发失败、重试成功”的单事件结果仍是 `lastSequence === 1`、`checkpoint.sequenceNumber === 1`。 | 本轮未再改 sequence 生产逻辑，主要是把现行实现的真实行为用回归测试固定下来。 |
| R21-24 | §105 Ops-maturity 缺陷 | P2 | `data-replicator/index.ts:112` | ReplicationEventBuffer timer flush 返回值无消费——事件丢失 | 已修复（源码/测试） | 根因真实存在：旧 `ReplicationEventBuffer.scheduleFlush()` 在 timer 触发时会先 `flush()` 清空 buffer，但没有把返回事件交回 `DataReplicatorService` 的发送/重试/checkpoint 链路，结果是 auto flush 路径会把事件从内存中拿走后静默丢弃。附带问题是该 timer 还会保持事件循环活跃，阻塞空闲进程退出。 | 本轮在 `src/scale-ecosystem/multi-region/data-replicator/index.ts` 给 `ReplicationEventBuffer` 增加 `autoFlushHandler`，由 `DataReplicatorService` 在构造 buffer 时注入真实 `processEvents()` 链路；timer flush 现在会把事件交给 service 处理，失败时 requeue，且 timer 使用 `unref()` 不再阻塞进程退出。新增 `tests/unit/scale-ecosystem/multi-region/data-replicator.test.ts` 回归，验证仅靠 timer auto flush 就会真实 emit 事件、更新 checkpoint 到 `sequenceNumber === 1/pendingCount === 0`。 | 这是本轮的真实生产修复，不再让 timer flush 走“清空 buffer + no-op”的假链路。 |
| R21-25 | §105 Ops-maturity 缺陷 | P2 | `region-health-check-service.ts:330` | determineStatus 用引用相等比较 metrics——永远不匹配 | 已修复（源码/测试） | 根因真实存在但比原描述更具体：`determineStatus()` 旧实现并没有直接拿到当前 region 的阈值配置，而是尝试从历史 `healthResults` 里用 `metrics` 内容反推 `regionId`，首次成功检查时拿不到 config，导致 latency 超阈值也可能被误判为 `healthy`。 | 本轮在 `src/scale-ecosystem/multi-region/region-health-check-service.ts` 让 `checkRegion()` 直接把当前 `RegionHealthCheckConfig` 传入 `determineStatus()`，并直接用 `latencyMs > config.thresholds.maxLatencyMs` 判定 degraded；新增 `tests/unit/scale-ecosystem/multi-region/region-health-check-service.test.ts` 回归，验证首次成功检查即使 metrics 全绿，只要 latency 超过该 region 阈值，也会返回 `degraded`。 | 这是本轮的真实逻辑修正，不再依赖历史 metrics 匹配去间接寻找阈值。 |
| R21-26 | §105 Ops-maturity 缺陷 | P2 | `edge-runtime-sync-service.ts:109` | .reverse() 导致低优先级先处理——违反同步策略 | 已解决（源码复核/测试补强） | 根因不是现行 `sync()` 仍然通过 `.reverse()` 把低优先级项提前处理，而是审计与部分旧测试注释都停留在历史实现；当前排序路径已经统一委托给 `orderEdgeSyncQueue()`，按 priority 从高到低、同优先级按 `sequence_no` 升序处理。 | `src/ops-maturity/edge-runtime/edge-runtime-sync-service.ts` 当前在 `requireOrdering` 为真时调用 `orderEdgeSyncQueue(...)`；`src/ops-maturity/edge-runtime/sync-queue/index.ts` 的 `orderEdgeSyncQueue()` 会先对 priority 做 `sort((a, b) => b - a)`，确保高优先级先处理，没有 `.reverse()`。本轮补强 `tests/unit/ops-maturity/edge-runtime/edge-runtime-sync-service.test.ts`，把原本只验证“都被接受”的弱断言改成直接断言 `acceptedEnvelopeIds === [\"env-high\", \"env-low\"]`；定向复跑通过。 | 本轮不改生产排序逻辑，只修正了滞后的测试表述并补了真正检查顺序的回归证据。 |
| R21-27 | §105 Ops-maturity 缺陷 | P2 | `compliance-report-pipeline-service.ts:119` | 迟签(signed_at > due)被视为 not_attested_expired——无 "signed_late" 状态 | 已解决（源码复核/测试补强） | 根因不是现行 signoff 评估仍然把“迟签”并入 `not_attested_expired`，而是审计结论停留在 `signed_late` 状态引入前；当前实现已经把“已签但超期”和“截止时仍未签”拆成不同状态。 | `src/ops-maturity/compliance-reporter/compliance-report-pipeline-service.ts` 当前在 `evaluateHumanSignoff()` 中对 `signedAtMs > signoffDueAtMs` 明确返回 `status: "signed_late"`，只有未签场景才会落到 `signoff_overdue/not_attested_expired`。本轮新增 `tests/unit/ops-maturity/compliance-reporter/compliance-report-pipeline-service.test.ts` 回归，验证 `signedAt` 晚于 `signoffDueAt` 时会返回 `signed_late`。 | 本轮不改该生产分支，只补了直接覆盖迟签状态的最小测试证据。 |
| R21-28 | §106 Org-governance / Domains / SDK / Plugins 缺陷 | P0 | `org-governance-saga.ts` | Saga 是 stub——分类步骤但从不执行 prepare/commit/compensate/audit,部分完成无补偿 | 已解决（源码复核/测试补强） | 根因不是现行 `OrgGovernanceSaga` 仍是只分类步骤的空壳 stub，而是审计结论停留在早期占位认知；当前实现已经具备 prepare → commit → compensate → audit 四阶段编排、phase 排序、冻结 org version、impact diff、失败回滚、补偿收据和 execution log。此次复核还暴露出一个真实测试债：integration 测试文件仍按旧假设省略了必需 handler，没有跟上现行 `validateHandlers()` 约束。 | `src/org-governance/org-model/org-governance-saga.ts` 当前实现了 `prepare()` 的 frozen snapshot / impact diff、`commit()` 的 phase-ordered execution、`compensate()` 的反向回滚与 `CompensationReceiptEntry`、`execute()` 的 prepare/commit/compensate/audit 四阶段，以及 `executeWithReceipt()` / `executeWithOrgOperations()`。本轮修正 `tests/integration/platform/org-governance/org-model/org-governance-saga.integration.test.ts` 以匹配现行 handler 校验，并复跑 `tests/unit/org-governance/org-model/org-governance-saga.test.ts`、`tests/unit/org-governance/org-governance-saga-extended.test.ts`、`tests/integration/platform/org-governance/org-model/org-governance-saga.integration.test.ts`，覆盖成功流、commit 失败补偿、prepare 失败回滚、phase 排序、handler 校验、receipt enrich 和 failedPhase 等路径，均已通过。 | 本轮不改 saga 生产逻辑，关闭依据是现行实现与最小定向测试已经直接证明它不是 stub，且补偿/审计链路确实会执行。 |
| R21-29 | §106 Org-governance / Domains / SDK / Plugins 缺陷 | P0 | `client-sdk/api-client.ts` | 无 ContractEnvelope 实现——§5.2 要求所有跨平面调用使用含 schemaVersion/commandId/idempotencyKey 等 | 已解决（源码复核/测试补强） | 根因不是现行 `api-client` 仍完全裸发 JSON，而是复审条目停留在 envelope 封装落地前；当前 request 路径已经统一构造 `ContractEnvelope`，携带 `schemaVersion/commandId/idempotencyKey/correlationId/principal`。 | `src/sdk/client-sdk/api-client.ts` 当前在 `request()` 中对所有请求都调用 `createContractEnvelope()` 包装 payload；新增 `tests/unit/sdk/client-sdk/api-client-contract-envelope-regressions.test.ts`，验证 POST 请求体实际包含 `ContractEnvelope` 与 idempotency/principal 元数据。 | 属于 SDK 契约封装已落地、此前缺少干净证据，现关闭。 |
| R21-30 | §106 Org-governance / Domains / SDK / Plugins 缺陷 | P0 | `plugin-definition.ts:144-148` | definePlugin 检查签名字段存在但从不验证密码学签名——插件信任无意义 | 已修复（源码/测试） | 根因不只是历史上缺少强制验签；复核确认现行 `definePlugin()` 虽然会调用 `verifyPluginSignature()`，但 Ed25519/Ed448 分支错误使用 `createVerify('ed25519')`，Node 实际会报 `Invalid digest`，导致主流插件签名算法无法完成真实密码学校验。 | 本轮在 `src/sdk/plugin-sdk/plugin-definition.ts` 改为对 `ed25519/ed448` 使用 `crypto.verify(null, ...)` 的真实验签路径，其余算法仍走 `createVerify()`；新增 `tests/unit/plugins/plugin-definition-security-regressions.test.ts`，生成真实 Ed25519 密钥对和签名，验证 `definePlugin()` 会接受有效签名插件。 | 这是对“有验签框架但主算法验不了”的真实安全缺陷修复，现关闭。 |
| R21-31 | §106 Org-governance / Domains / SDK / Plugins 缺陷 | P0 | `domain-model.ts` | 缺 canary 生命周期状态——spec 要求 draft→canary→active→deprecated→archived | 已解决（源码复核/现有测试补强） | 根因不是现行 `domain-model.ts` 仍缺 `canary` 生命周期，而是复审条目停留在早期 schema 未补齐之前；当前 `DomainManifestSchema.lifecycleState` 已显式支持 `draft/canary/active/deprecated/archived`，只是审计文档与测试证据没有同步更新。 | `src/domains/registry/domain-model.ts` 当前把 `lifecycleState` 定义为 `z.enum([\"draft\", \"canary\", \"active\", \"deprecated\", \"archived\"])`；现有 `tests/unit/domains/registry/domain-model-schemas.test.ts` 与 `tests/unit/domains/registry/domain-manifest-resource-quotas.test.ts` 已覆盖全部合法 lifecycle states，本轮复跑确认通过。 | 属于 domain manifest 生命周期模型已落地、此前缺少集中证据，现关闭。 |
| R21-32 | §106 Org-governance / Domains / SDK / Plugins 缺陷 | P0 | `evidence-collector.ts` | Evidence 纯内存(Map),无持久化/hash chain/防篡改——spec 要求 tamper-proof 审计证据 | 已修复（源码/测试） | 复核后发现原指控有两层：第一，“无 hash chain/防篡改”已经不是现状，`ComplianceEvidenceCollector` 早已有 `previousHash/hash/verifyChain()`；第二，真实缺口仍在持久化，旧实现把 evidence、schedule、last hash 全留在内存 Map 里，进程重启即丢失，导致 tamper-proof 证据链无法跨进程延续。 | 本轮在 `src/org-governance/compliance-engine/evidence-collector.ts` 增加 `ComplianceEvidenceCollectorOptions.storagePath`、磁盘 snapshot 加载/落盘、records/scheduledCollections/lastHashByFramework 持久化；`tests/unit/org-governance/compliance-engine/evidence-collector.test.ts` 已新增回归，验证重启后 evidence 记录与 hash continuity 可恢复，且篡改持久化文件后 `verifyChain()` 会识别出被污染的 `evidenceId`。 | 这是把“内存链条”补成“可持久化、可重载、可验篡改”的真实修复，现关闭。 |
| R21-33 | §106 Org-governance / Domains / SDK / Plugins 缺陷 | P1 | `api-client.ts:208` | RetryableApiClient 重试所有非 ok 响应含 4xx——应仅重试 5xx/429/网络错误 | 已修复（源码/测试） | 根因不是 response 分支里完全没做状态码区分，而是 `request()` 在把 4xx 分类成 `BusinessError/AuthError` 之后，又在外层 `catch` 中把这些 typed error 当成“可重试错误”再次重试；所以 GET 遇到 400/401/403 仍会发生错误重放。 | 本轮在 `src/sdk/client-sdk/api-client.ts` 的 `catch` 块中显式排除 `AuthError/BusinessError/ValidationError/NetworkError`，仅对 fetch 级网络异常继续走 retry；`tests/unit/sdk/client-sdk/api-client-contract-envelope-regressions.test.ts` 已验证 400 business error 只请求一次，不再被重试。 | 这是本轮发现并补掉的真实 retry 漏洞，现关闭。 |
| R21-34 | §106 Org-governance / Domains / SDK / Plugins 缺陷 | P1 | `plugins/index.ts` | 生命周期 registered→loaded→active→inactive 缺 validated 和 suspended 状态 | 已解决（源码复核/测试补强） | 根因不是现行插件导出面仍只暴露 `registered/loading/active/inactive` 旧状态，而是复审条目停留在 `PluginLifecycleStateSchema` 扩展前；当前 `src/plugins/index.ts` 直接转导 `plugin-spi.ts` 的 `PluginLifecycleStateSchema`，其中已经包含 `validated` 与 `suspended`，`PluginSpiRegistry` 也实现了 suspended 生命周期流转。 | `src/domains/registry/plugin-spi.ts` 当前定义的 `PluginLifecycleStateSchema` 包含 `registered/validated/loading/active/inactive/unloaded/suspended/disabled`；`src/plugins/index.ts` 直接导出该 schema；`tests/unit/domains/registry/plugin-lifecycle-states.test.ts` 已覆盖 active/inactive→suspended，且本轮补强 `tests/unit/domains/registry/plugin-spi-schema.test.ts` 把 `validated` 纳入正向 schema 断言。 | 属于插件生命周期状态面已对齐、此前缺少完整测试佐证，现关闭。 |
| R21-35 | §106 Org-governance / Domains / SDK / Plugins 缺陷 | P1 | `plugin-spi.ts` | SPI 类型含 retriever/validator/planner 但缺 tool/evaluator——与 plugin-sdk PluginDefinition 不一致 | 已解决（源码复核/测试补强） | 根因不是现行 `plugin-spi.ts` 仍缺 `tool/evaluator`，而是复审条目停留在旧版 SPI 枚举；当前 `PluginSpiTypeSchema` 已包含 `tool/retriever/validator/planner/presenter/adapter/evaluator` 七类，只是旧测试长期未把 `tool/evaluator` 纳入正向样例，导致证据不足。 | `src/domains/registry/plugin-spi.ts` 当前 `PluginSpiTypeSchema = z.enum([\"tool\", \"retriever\", \"validator\", \"planner\", \"presenter\", \"adapter\", \"evaluator\"])`；本轮补强 `tests/unit/domains/registry/plugin-spi-schema.test.ts` 与 `tests/unit/domains/registry/plugin-spi-types.test.ts`，明确断言 `tool/evaluator` 可被 schema 接受。 | 属于实现已对齐、此前缺少直接测试证据，现关闭。 |
| R21-36 | §106 Org-governance / Domains / SDK / Plugins 缺陷 | P1 | `escalation/index.ts:158` | 仅匹配首条规则——无多级升级链/无审批步骤超时 | 已修复（源码/测试） | 复核后发现原指控有两层：第一，“只有首条规则”已经不是现状，当前实现早已是 panic_stop → takeover → approval → none 的分层升级链；第二，本轮最小定向测试又暴露了一个真实缺陷：`triggerPanicStop()` 之前把 `requiredApprovers: []` 传给 `PlatformPanicService.activate()`，触发 dual-approver invariant，导致 critical+production 路径直接抛 `panic.required_approvers_minimum_not_met`，连 panic decision 都无法落地。审批超时策略本身则一直已在 approval request 中建模为 `timeoutPolicy: \"reject\"`。 | 本轮已在 `src/platform/orchestration/escalation/index.ts` 与 `src/platform/five-plane-orchestration/escalation/index.ts` 增加 `buildRequiredApprovers()`，确保 panic stop 自动带上双人审批人列表；`createApprovalRequest()` 继续显式写入 `timeoutPolicy: \"reject\"`。`tests/unit/platform/orchestration/escalation.test.ts`、`tests/unit/platform/orchestration/escalation/index.test.ts` 本轮新增/修正了 panic stop、approval request 创建、审批超时策略、`costThresholdUsd` 覆盖默认阈值等回归。 | 关闭的是“无多级升级链/无审批超时”的原指控，并同时修复了本轮复核中发现的真实 panic 路径缺陷；不外推成“已具备任意可配置 N 级规则引擎”。 |
| R21-37 | §106 Org-governance / Domains / SDK / Plugins 缺陷 | P1 | `admin-sdk/index.ts` | 极薄 wrapper——缺租户管理/策略管理/域生命周期/rollout 控制 | 已解决（源码已满足，本轮补证据） | 根因不是当前 `AdminSdk` 仍只有少量 pass-through wrapper，而是复审条目停留在 SDK 扩容之前；现行 `src/sdk/admin-sdk/index.ts` 已包含租户 CRUD/停复用、策略 CRUD/挂载解绑、域生命周期控制、rollout 控制与 bulk operations。 | 当前 `AdminSdk` 已实现 `listTenants/getTenant/createTenant/updateTenant/deleteTenant/suspendTenant/resumeTenant`、`listPolicies/getPolicy/createPolicy/updatePolicy/deletePolicy/attachPolicy/detachPolicy/listPolicyAttachments`、`activateDomain/deactivateDomain/suspendDomain/resumeDomain/getDomainStatus`、`listRollouts/getRollout/createRollout/updateRollout/pauseRollout/resumeRollout/cancelRollout/getRolloutStatus/rollbackRollout/advanceRolloutPercentage` 以及 `bulkCreateTenants/bulkUpdateTenants/bulkDeleteTenants/bulkCreatePolicies/bulkAttachPolicies/bulkDomainLifecycle`；本轮新增 `tests/unit/sdk/admin-sdk/admin-sdk-capability-surface.test.ts` 直接锁定这些 API surface，既有 `tests/unit/sdk/admin-sdk/bulk-ops-2285.test.ts` 继续锁定 bulk contract。 | 属于旧审计基线落后于现行 SDK 能力面，本轮按源码与最小定向测试证据关闭。 |
| R21-38 | §106 Org-governance / Domains / SDK / Plugins 缺陷 | P1 | `plugin-test-harness.ts:196` | executePlugin 全是 mock 硬编码返回;timeoutMs 存储但从不强制 | 已修复（源码/测试） | 根因复核后分成两层：第一，`timeoutMs` 早已通过 `Promise.race()` 真正生效，原“只存不生效”的描述已过时；第二，真正缺口在于 `PluginTestHarness` 把 `PluginDefinition` 这类签名 manifest 当成可执行 runtime 使用，既没有 runtime binding，也没有把 live 输入桥接到真实 `tool/retriever/validator/planner/presenter/evaluator/adapter` SPI，因此 live 模式只能抛未实现错误。 | 本轮在 `src/sdk/plugin-sdk/plugin-test-harness.ts` 中新增 `livePlugin/liveRunner` 绑定点，把 manifest 与 executable runtime 分离：`executeLive()` 现在会优先走 `liveRunner`，否则可把绑定的 `RegisteredPlugin` 按 `spiType` 分发到真实 `execute/retrieve/validate/suggestWorkflow/formatOutput/evaluate/authenticate` 路径；同时补了 lifecycle prepare/activate/health check 与最小 `PluginContext`/`PluginLifecycleContext` 桥接，live 模式不再只能报 “not implemented”。`tests/unit/sdk/plugin-sdk/plugin-test-harness-regressions.test.ts` 本轮新增回归，既验证 mock 模式 1ms timeout 仍会真实超时，也验证绑定 `DomainToolPlugin` 后 live 模式会返回真实执行结果；定向执行 `./node_modules/.bin/tsx --test tests/unit/sdk/plugin-sdk/plugin-test-harness-regressions.test.ts` 结果为 `3 passed / 0 failed`。 | 本轮关闭依据是生产态 `PluginTestHarness` 已具备最小可用的 live runtime seam，而不是继续停留在占位错误；当前关闭范围仅覆盖“测试 harness 可绑定真实 runtime plugin/runner 并执行”的能力，不外推为“已具备完整插件宿主平台”。 |
| R21-39 | §106 Org-governance / Domains / SDK / Plugins 缺陷 | P1 | `chinese-wall-access-saga.ts` | Stub saga——同 OrgGovernanceSaga,无实际编排/补偿 | 已解决（源码复核/现有测试） | 根因不是现行 `ChineseWallAccessSaga` 仍是空壳 stub，而是复审条目停留在更早的占位认知；当前实现已经具备 prepare → commit → compensate → audit 四阶段编排、prepare/commit 对应校验、失败回滚、反向补偿、execution log 和 handler context 传递。 | `src/org-governance/knowledge-boundary/chinese-wall-access-saga.ts` 当前实现了 phase 排序、`hasMatchingPrepareStep()`、`runCompensation()`、`executionLog`、`failedAction` 与补偿路径；现有 `tests/unit/org-governance/knowledge-boundary/chinese-wall-access-saga.test.ts` 和 `tests/unit/org-governance/chinese-wall-access-saga-extended.test.ts` 已覆盖 grant/release 成功流、commit 失败回滚、prepare 缺失拒绝、补偿逆序、audit 收尾、context 传播等路径，本轮复跑通过。 | 属于现行 saga 已有真实编排与补偿能力、旧审计结论未同步，现关闭。 |
| R21-40 | §106 Org-governance / Domains / SDK / Plugins 缺陷 | P1 | `domain-registry-service.ts:76` | activate() 仅允许从 "registered" 转换——无 canary 部署门 | 已解决（源码复核/现有测试） | 根因不是现行 `activate()` 仍然只接受 `registered -> active` 直切，而是复审条目停留在 canary gate 引入之前；当前实现把激活拆成 `registered/updating/validated/testing -> canary` 与 `canary -> active` 两步，并对跳过 canary 的路径显式抛错。 | `src/domains/registry/domain-registry-service.ts` 当前 `activate(domainId, canary=true)` 会把 `registered/updating/validated/testing` 提升到 `canary`，而 `activate(domainId, false)` 仅允许从 `canary/validated/testing` 进入 `active`；现有 `tests/unit/domains/registry/domain-registry-service.test.ts` 和 `tests/unit/domains/registry/domain-lifecycle-state-machine.test.ts` 已验证 `registered -> canary -> active`、非法跳过 canary 会报 `domain_registry.invalid_activation_state`。 | 属于 canary 部署门已经落地、旧审计结论未更新，现关闭。 |
| R21-41 | §106 Org-governance / Domains / SDK / Plugins 缺陷 | P2 | `governance-delegation-revocation-saga.ts:28` | cascadeWithinSlo >= 0 永远 true——死逻辑 | 已解决（源码复核/现有测试） | 根因不是现行 saga 仍用 `>= 0` 这种恒真条件，而是复审条目停留在旧版 SLO 判断；当前 `revokeWithinSlo/cascadeWithinSlo` 都已经改成基于真实 elapsed 与失败阶段的布尔计算。 | `src/org-governance/delegated-governance/governance-delegation-revocation-saga.ts` 当前使用 `elapsed <= REVOKE_SLO_MS` 与 `failedStage == null && elapsed <= CASCADE_SLO_MS`；本轮复跑 `tests/unit/org-governance/delegated-governance/governance-delegation-revocation-saga-slo.test.ts`，验证超时和失败场景都会使 `cascadeWithinSlo` 变为 `false`。 | 属于旧审计结论未同步到现行实现，现关闭。 |
| R21-42 | §106 Org-governance / Domains / SDK / Plugins 缺陷 | P2 | `route-engine/index.ts:256` | 硬编码 FX rate 7.2 USD→CNY——应用汇率服务或配置 | 已解决（源码复核/测试补强） | 根因不是现行审批路由仍把 `7.2` 写死在运行时换算里，而是复审条目停留在 legacy path 重构前；当前实现已引入 `ConfiguredFxRateProvider`、环境变量配置和可替换 provider。 | `src/org-governance/approval-routing/route-engine/index.ts` 当前通过 `ConfiguredFxRateProvider`、`APPROVAL_ROUTE_USD_CNY_RATE` 和 `setFxRateProvider()/getFxRateProvider()` 解析汇率；本轮新增 `tests/unit/org-governance/approval-routing/route-engine-fx-provider.test.ts`，验证当环境配置为 `6.91` 时运行时返回 `6.91` 而不是硬编码 `7.2`。 | 属于汇率来源已可配置、此前缺少直接运行时证据，现关闭。 |
| R21-43 | §106 Org-governance / Domains / SDK / Plugins 缺陷 | P2 | `domain-smoke-test.ts` | 无实际运行时集成测试——仅验证 config 结构 | 已解决（源码复核/现有测试） | 根因不是现行 `domain-smoke-test.ts` 仍只做静态 config 校验，而是复审条目停留在 `runWithRuntime()` 和 `runRuntimeIntegration()` 引入之前；当前 smoke test 已能调用运行时上下文完成 descriptor load、sandbox create、step execute 的真实集成检查。 | `src/domains/registry/domain-smoke-test.ts` 当前除 `run()` 外还提供 `runWithRuntime()` 与 `runRuntimeIntegration()`；现有 `tests/unit/domains/registry/domain-lifecycle-state-machine.test.ts` 已覆盖 `runWithRuntime executes runtime integration`、缺失 `loadDomainDescriptor`、以及 `sandbox.executeStep` 失败等运行时路径，本轮复跑通过。 | 属于运行时集成 smoke path 已落地、旧审计结论未更新，现关闭。 |
| R21-44 | §106 Org-governance / Domains / SDK / Plugins 缺陷 | P2 | `api-client.ts` | 无版本握手/无幂等 key/无事件订阅能力 | 已解决（源码复核/测试补强） | 根因不是现行 `api-client` 仍完全缺失这些能力，而是复审条目停留在早期薄封装版本；当前 SDK 已有 `performVersionHandshake()/initialize()`、请求级 `idempotencyKey`、以及 `createEventSubscriber()` 的 typed event subscription API。 | `src/sdk/client-sdk/api-client.ts` 已实现 `/version` 握手、`ApiRequestSpec.idempotencyKey` 与 `createEventSubscriber()/subscribeToRunLifecycle()/deliverPending()`；新增 `tests/unit/sdk/client-sdk/api-client-contract-envelope-regressions.test.ts`，分别验证 version handshake 触发、请求 envelope 带幂等元数据、以及事件订阅/投递辅助能力可用。 | 属于 SDK 能力已落地、此前缺少集中证据，现关闭。 |
| R21-45 | §106 Org-governance / Domains / SDK / Plugins 缺陷 | P2 | `builtin-plugin-registry.ts` | Plugin factories 硬编码——无动态加载/无 marketplace/无认证流程 | 已解决（源码复核/测试补强） | 根因不是现行 `builtin-plugin-registry.ts` 只有一组硬编码 builtin factories 而完全没有动态加载层，而是复审条目停留在 `PluginMarketplaceRegistry` / `DynamicPluginLoader` 引入之前；当前文件除内建 factory 映射外，已经实现 marketplace registry、动态 source loader 注册、会话认证和 marketplace entry 管理。 | `src/plugins/builtin-plugin-registry.ts` 当前提供 `DynamicPluginLoader`、`PluginMarketplaceRegistry.registerLoader()/registerMarketplaceEntry()/authenticate()/loadPlugin()/listMarketplacePlugins()` 以及全局 `getMarketplaceRegistry()`；本轮复跑 `tests/unit/plugins/builtin-plugin-registry.test.ts` 与 `tests/unit/plugins/plugin-marketplace-registry.test.ts`，验证动态 loader 流程、认证要求、unknown loader 报错、marketplace entry 枚举等路径均通过。另同步修正了 `tests/unit/plugins/builtin-plugin-registry.test.ts` 中仍引用旧 ID `plugin.core.basic-evaluator` 的陈旧断言。 | 属于 marketplace 动态加载和认证流程已落地、此前审计结论和部分测试样例未同步，现关闭。 |
| R21-46 | §106 Org-governance / Domains / SDK / Plugins 缺陷 | P2 | `plugin-definition.ts` | sbomRef 接受但从不验证——无安全扫描 | 已修复（源码/测试） | 根因不是现行代码完全没有 `verifySbomRef()` / scanner 接口，而是默认 `DefaultSbomScanner` 的真实取数链路缺失：旧实现虽然会在 `definePlugin()` 中调用 SBOM 校验，但 `fetchSbom()` 对 `file://` 和 `http(s)://` 一律返回 `null`，导致默认安全扫描后端实际上无法读取和解析任何 SBOM 内容。进一步复核还发现 `definePlugin()` 会把“扫描发现高危漏洞”误归类成“扫描失败”。 | 本轮在 `src/sdk/plugin-sdk/plugin-definition.ts` 为默认 scanner 补齐了 `file://` 读取、`http(s)://` 拉取、SPDX/CycloneDX JSON 解析、package/component 提取，以及真实漏洞匹配路径；同时修正 `definePlugin()` 对 `scanErrors` 与 `critical/high vulnerabilities` 的分流处理。新增/补强 `tests/unit/plugins/plugin-definition-security-regressions.test.ts`，验证本地 `file://` SBOM 会被真实解析并检测出 `lodash/axios` 高危依赖，且带高危漏洞的插件会被 `definePlugin()` 直接拒绝。 | 这是把“有校验入口但默认后端不工作”的假安全扫描补成可执行扫描链路的真实修复，现关闭。 |
| R21-47 | §107 Config Center / Bootstrap / Tests 缺陷 | P0 | `config-center/ (缺失)` | ConfigImpactAnalyzer 完全未实现——§24.4 要求为高风险 config publish 强制门禁 | 已解决 | `ConfigImpactAnalyzer` 已经存在并能输出 severity / blast radius / rollout recommendation；问题不再是“类完全缺失”。 | 当前 `src/platform/five-plane-control-plane/config-center/config-impact-analyzer.ts` 已实现 `ConfigImpactAnalyzer.analyzeImpact()`，并在 `index.ts` barrel 中导出；本轮还补了 `tests/unit/platform/control-plane/config-center/index.test.ts` 对导出面的断言。 | 不改生产算法，仅回写“实现已存在”的依据。 |
| R21-48 | §107 Config Center / Bootstrap / Tests 缺陷 | P0 | `config-drift-reconciler.ts:21-52` | Drift 不生成 config.drift_detected incident,安全/预算/egress/sandbox 类 drift 不 fail-closed | 已修复（默认启动链路接线，范围限定） | 根因已收敛为启动链路缺口而非算法缺失：旧 `ConfigDriftReconciler` 虽已能给出 `blocking` 级 finding，但默认 `buildDefaultStartupConfigValidator()` 没有把运行时 drift 合并进 startup gate，`bootstrapGovernanceServices()` / `bootstrapGovernanceServicesWithMetrics()` 也没有给默认 checker 注入可落库的 event bus，所以运行时 `AA_*` 安全类覆盖即使发生漂移，也不会在默认治理启动路径里形成 `config.drift_detected` incident 或 fail-closed。补接线后又暴露出第二个真实问题：runtime 过滤最初过宽，会把不属于 config bundle 的 `AA_*` 运行参数一并当成 drift source，产生误报。 | 现行 `src/platform/five-plane-execution/startup/startup-preflight.ts` 已新增运行时 `AA_*` override 抽取、bundle baseline flatten 和 `buildRuntimeDriftIssues()`，并且只比较 bundle 基线中存在的键，再把 drift findings 合并进 `buildDefaultStartupConfigValidator()` 的 `issues`，使安全类 drift 直接让 startup preflight `ok=false` 且无关运行参数不误报；`src/sdk/cli/governance-bootstrap.ts` 已在 `bootstrapGovernanceServices()` 与 `bootstrapGovernanceServicesWithMetrics()` 中为 `createDefaultStartupConsistencyCheckerOptions(...)` 注入真实 `DurableEventBus`，让默认治理 checker 能把 drift 事件落到生产事件总线。`tests/unit/platform/execution/startup/startup-preflight.test.ts` 本轮新增并通过回归：验证 runtime `AA_APPROVAL_MODE` drift 会触发 `config.drift_detected` 且 fail-closed，同时验证 `AA_DB_PATH` / `AA_ENVIRONMENT` 这类无关环境变量不会被误报成 `config_drift.*`。 | 本轮动作是把 drift 检测从“文件内可报告”补成“默认 startup / governance bootstrap 链路真实生效”的生产接线，并用最小定向测试证明 incident 与 fail-closed 已进入默认路径；本结论仅覆盖当前已接入的 runtime `AA_*` override source，不夸大为所有潜在 drift source 都已完全闭环。 |
| R21-49 | §107 Config Center / Bootstrap / Tests 缺陷 | P0 | `config/bootstrap/default.json` | 缺 dependencyOrder/healthCheckTimeout/degradationPolicy/readinessGates | 已解决（源码修复并补定向测试） | 根因是默认 bootstrap 配置只保留了最小三字段，缺少运行时启动顺序、ready 门槛和降级策略。 | 本轮已在 `config/bootstrap/default.json` 中补入 `dependencyOrder`、`healthCheckTimeoutMs`、`degradationPolicy`、`readinessGates`，并新增 `tests/unit/platform/control-plane/config-center/default-config-shape.test.ts` 断言这些键存在。 | 关闭本项依赖于本轮配置文件改造和定向测试。 |
| R21-50 | §107 Config Center / Bootstrap / Tests 缺陷 | P0 | `config-rollout-service.ts:44-51` | Canary 阶段与 spec 不匹配(5%→25%→50%→100% vs spec 的 canary→30min→10%→full) | 已修复（源码/测试） | 根因不是 rollout 仍会直接跳 `FULL`，而是默认阶段表长期停留在旧的 `5% → 25% → 50% → 100%` 口径，和文件头注释、审计契约都不一致；实现与契约自身发生了漂移。 | 本轮在 `src/platform/five-plane-control-plane/config-center/config-rollout-service.ts` 将 `DEFAULT_ROLLOUT_STAGES` 收敛为 `PENDING -> CANARY(5%) -> CANARY_10(10%) -> FULL`；新增 `tests/unit/platform/control-plane/config-center/config-rollout-service-guardrails.test.ts`，验证 rollout 会按 `5% -> 10% -> 100%` 推进。 | 这是对真实阶段模型偏差的代码修复，现关闭。 |
| R21-51 | §107 Config Center / Bootstrap / Tests 缺陷 | P0 | `config-rollout-service.ts (缺失)` | Rollout 无 guardrail metrics(max_error_rate/latency_regression 等)——无自动回滚 | 已修复（源码/测试） | 根因不是 guardrail 阈值完全不存在，而是 health check 失败后只有“阻止继续 auto-progress”的半闭环：`getRolloutHealth()` 返回占位 0 值，失败指标没有被保留，且没有在失败点自动触发 rollback。 | 本轮在 `src/platform/five-plane-control-plane/config-center/config-rollout-service.ts` 为 rollout 持久化 `lastObservedErrorRate/lastObservedLatencyRegression/lastObservedIncidentRate/lastHealthCheckReasons`，并让 `recordHealthCheck()` 在失败时立即执行自动回滚；`tests/unit/platform/control-plane/config-center/config-rollout-service-guardrails.test.ts` 已验证 health gate 失败会从 `10%` 自动回滚到 `5%`，同时 `getRolloutHealth()` 返回真实观测指标而非占位 0 值。 | 这是把 guardrail 从“只记录/只阻塞前进”补成“记录 + 自动回滚”的真实闭环，现关闭。 |
| R21-52 | §107 Config Center / Bootstrap / Tests 缺陷 | P1 | `hierarchical-config-loader.ts:16-20` | 层级缺 environment 层,用 task_type 替代 runtime_dynamic | 已解决 | 分层配置加载器已经切到 `platform → environment → tenant → pack → runtime` 五层结构，不再是旧的 `task_type` 路径。 | 当前 `src/platform/five-plane-control-plane/config-center/hierarchical-config-loader.ts` 定义的 `ConfigHierarchyLayer` 为 `platform/environment/tenant/pack/runtime`，`loadConfig()` 也按该顺序合并来源。 | 不改生产代码，仅回写当前 hierarchy 依据。 |
| R21-53 | §107 Config Center / Bootstrap / Tests 缺陷 | P1 | `config-center/ (缺失)` | 无4种 config lifecycle 类型(admission_locked/checkpoint_revalidated/hot_reloadable/emergency) | 已解决 | 配置生命周期类型与管理器已经落地，只是原审计仍按文件缺失结论保留。 | 当前 `src/platform/five-plane-control-plane/config-center/config-lifecycle-types.ts` 已定义 `admission_locked`、`checkpoint_revalidated`、`hot_reloadable`、`emergency_override` 及 `ConfigLifecycleManager`；本轮也已通过 `config-center/index.ts` 暴露到 barrel。 | 不改生产代码，仅回写现行 lifecycle 类型依据。 |
| R21-54 | §107 Config Center / Bootstrap / Tests 缺陷 | P1 | `platform-architecture-bootstrap.ts:128-148` | 无 health check before ready——服务注册后立即可用无健康探针 | 已解决（源码复核/测试补强） | 根因不是现行 bootstrap 仍完全没有 readiness 探针，而是复审条目停留在旧实现；当前每个 architecture catalog 注册都带 `healthCheck`，且 bootstrap 返回前会验证关键 service 已初始化。 | `src/platform-architecture-bootstrap.ts` 当前为 `layer/plane/app/startup-target/bootstrap-summary` 五个 service 都注册了 `healthCheck`，并在返回前检查 `registry.isInitialized(...)`；本轮补充 `tests/unit/platform-architecture-bootstrap-degradation.test.ts`，验证预注册一个 `healthCheck: false` 的 `architecture.app-catalog` 时，它不会被当成 ready，而 bootstrap 会改走 fallback 数据。 | 属于现行 readiness gate 已存在、此前缺少直接证据，现关闭。 |
| R21-55 | §107 Config Center / Bootstrap / Tests 缺陷 | P1 | `platform-architecture-bootstrap.ts:128-148` | 无 graceful degradation——任何 registry.get() 抛异常则全部 bootstrap 失败 | 已修复（源码/测试） | 根因不在“完全没有注册 catalog”，而在于原实现对 `registry.get()` 是全有或全无依赖；任一 catalog 初始化或 health check 抛错，整组 architecture bootstrap 就会失败，无法退回静态 manifest。 | 本轮在 `src/platform-architecture-bootstrap.ts` 增加 `resolveArchitectureService()`，对 `layer/plane/app/startup-target/bootstrap-summary` 分别做局部 fallback，并让 `getPlatformArchitectureServices()` 在 summary 未 ready 或读取异常时回退到重新装配的降级结果；`tests/unit/platform-architecture-bootstrap-degradation.test.ts` 已验证 `architecture.layer-catalog` 故障时仍能返回非空 `layers/summary/startupTargets`。 | 这是把 bootstrap 从“单点失败全挂”改成“局部降级可继续返回静态架构描述”的真实修复，现关闭。 |
| R21-56 | §107 Config Center / Bootstrap / Tests 缺陷 | P1 | `config/runtime/default.json` | 无 configVersion 字段——不支持版本追踪和回滚 | 已解决（源码修复并补定向测试） | 根因是 runtime 默认配置长期只有行为参数，没有版本标识字段。 | 本轮已在 `config/runtime/default.json` 中加入 `configVersion`，并在 `tests/unit/platform/control-plane/config-center/default-config-shape.test.ts` 中增加对应断言。 | 关闭本项依赖于本轮配置文件改造和定向测试。 |
| R21-57 | §107 Config Center / Bootstrap / Tests 缺陷 | P1 | `golden/config-file-generation.test.ts:71` | 断言 semver 格式但实现产生 hex hash——catch 块 assert.ok(true) 静默吞噬失败 | 已解决（源码修复并补定向测试） | 根因是 golden test 仍按旧版 `versionId` 语义写成 semver 断言，但现行 `ConfigGovernanceService.buildVersion()` 早已把 `versionId` 定义为 `bundleHash.slice(0, 16)` 的 hash 前缀。 | 本轮把 `tests/golden/config-file-generation.test.ts` 的版本断言改为 `^[0-9a-f]{16}$`，并与现行配置夹具一起重跑通过；这与 `src/platform/control-plane/config-center/config-governance-service.ts` 当前 `buildVersion()` 实现一致。 | 关闭本项依赖的是测试断言对齐现行契约，不是改动生产版本算法。 |
| R21-58 | §107 Config Center / Bootstrap / Tests 缺陷 | P1 | `golden/config-file-generation.test.ts:30-75` | 两个 golden test 全在 catch 中 assert(true)——schema 验证失败被静默忽略 | 已解决（源码修复并补定向测试） | 根因是历史 golden test 曾用“失败也算通过”的保护壳掩盖 schema 漂移；后续虽然去掉了 `assert.ok(true)`，但没有同步修正夹具布局，导致测试长期不能稳定验证真实契约。 | 当前 `tests/golden/config-file-generation.test.ts` 已无 `assert.ok(true)` 容错分支，异常统一走 `assert.fail(...)`；同时本轮修正了夹具目录结构与断言，使该文件定向执行 `4 passed / 0 failed`。 | 关闭本项以现行测试源码和定向运行结果为依据。 |
| R21-59 | §107 Config Center / Bootstrap / Tests 缺陷 | P1 | `config-store.ts:116` | Change listener 用 !== 引用相等比较对象——结构相同的新对象永远触发 | 已解决 | ConfigStore 已从引用比较切到结构化深比较，原审计结论滞后。 | 当前 `src/platform/five-plane-control-plane/config-center/config-store.ts` 在 `set()` 中使用 `deepEqual(oldEntry?.value, value)` 决定是否触发 change listener，不再用 `!==`。 | 不改生产代码，仅回写现行 change detection 依据。 |
| R21-60 | §107 Config Center / Bootstrap / Tests 缺陷 | P2 | `config-override-governance.ts:314-318` | Dead code——!validation.allowed 已在310行 return,此分支不可达 | 已解决 | 根因不是当前实现仍保留“早返回后再判断 `!validation.allowed`”的不可达分支，而是审计结论停留在旧版 `recordOverride()`；现行实现已经把记录逻辑收敛成构造 `baseRecord` 后统一写 audit log，不再存在该死分支。 | 当前 `src/platform/five-plane-control-plane/config-center/config-override-governance.ts` 的 `recordOverride()` 只会构造 `baseRecord`、补充 `reason/highRiskObject`、然后 `this.auditLog.record(baseRecord)` 并返回，没有任何 `if (!validation.allowed)` 的后续不可达分支。 | 本轮复跑并通过 `tests/unit/platform/control-plane/config-center/config-override-governance.test.ts` 中的 `ConfigOverrideGovernanceService validates global layer allows all paths` 与 `ConfigOverrideGovernanceService records override in audit log`，作为最小基线证据。 |
| R21-61 | §107 Config Center / Bootstrap / Tests 缺陷 | P2 | `config-rollout-service.ts:134` | startRollout targetPercentage=100 跳过整个 canary 进程直接返回 FULL | 已解决 | 现行 rollout 服务已经强制从 `PENDING` 起步，不会因 `targetPercentage=100` 而直接进入 FULL。 | 当前 `src/platform/five-plane-control-plane/config-center/config-rollout-service.ts` 的 `startRollout()` 注释和实现都明确“Always start from PENDING”；`startStage = this.stages[0]!`。 | 不改生产代码，仅回写现行 rollout 起点依据。 |
| R21-62 | §107 Config Center / Bootstrap / Tests 缺陷 | P2 | `config/gateways/default.json` | 仅 defaultGateway+sseEnabled——缺 rate limit/CORS/auth/size limits | 已解决（源码修复并补定向测试） | 根因是默认 gateway 配置过于精简，只保留入口通道开关。 | 本轮已在 `config/gateways/default.json` 中补入 `rateLimit`、`cors`、`auth`、`requestLimits`，并在 `tests/unit/platform/control-plane/config-center/default-config-shape.test.ts` 中增加存在性断言。 | 关闭本项依赖于本轮配置文件改造和定向测试。 |
| R21-63 | §107 Config Center / Bootstrap / Tests 缺陷 | P2 | `config-center/index.ts` | 未导出 drift-reconciler/config-store/config-loader——消费者不可见 | 已解决（源码修复并补定向测试） | 根因是 config-center barrel 长期只导出部分治理组件，缺少 drift/store/loader/impact 等核心面。 | 本轮已在 `src/platform/five-plane-control-plane/config-center/index.ts` 增补 `config-drift-reconciler`、`config-impact-analyzer`、`config-lifecycle-types`、`config-loader`、`config-store` 等导出，并在 `tests/unit/platform/control-plane/config-center/index.test.ts` 增加 barrel export 断言。 | 关闭本项依赖于本轮 barrel 改造和定向测试。 |
| R21-64 | §108 Contract 文档 vs 实现偏差 | P1 | `rollout-record.ts:3-12` | Rollout level 与 release_rollout contract 不匹配——缺 evaluate_0/stable_75/stable_100,多 suggest/shadow | 已修复（源码修复并补定向测试） | 根因是 improve-rollout 仍沿用旧 `suggest/shadow/partial_50/partial_75/stable` 级别族，而 canonical contract 已迁移到 `evaluate_0/canary_5/partial_25/stable_75/stable_100`。 | 本轮把 `src/platform/five-plane-orchestration/oapeflir/types/rollout-record.ts` 的 `RolloutLevelSchema` 收敛到 canonical level，并同步修正 `strategy-versioning.ts`、`policy-rollout-service.ts`、`rollout-state-machine.ts`、`rollout-scheduler.ts`、`canary-traffic-router.ts`、`oapeflir-loop-service.ts` 的级别引用。`tests/unit/platform/five-plane-orchestration/rollout-contract-alignment.test.ts` 已验证 canonical level 解析与路由百分比。 | 这是实际 runtime level 迁移，不是仅改 contract 文案。 |
| R21-65 | §108 Contract 文档 vs 实现偏差 | P1 | `improvement-candidate.ts:13-23` | Schema 与 contract 完全不同——缺 learningObjectId/source enum/targetScope/guardrails[] | 已修复（源码修复并补定向测试） | 根因是 `ImprovementCandidateSchema` 之前只保留 task/evidence/changeScope/description 等最小字段，contract 所需的 `learningObjectId/source/targetScope/priority/rolloutLevel/metrics/guardrails` 全部缺位，registry 也无法产出 canonical 对象。 | 本轮在 `src/platform/five-plane-orchestration/oapeflir/types/improvement-candidate.ts` 增补 canonical 字段与枚举，并在 `src/platform/five-plane-orchestration/improve-rollout/improvement-candidate-registry.ts` 补齐 `learningObjectId/source/targetScope/priority/guardrails` 生成逻辑及 LRU 容量控制。`tests/unit/platform/five-plane-orchestration/rollout-contract-alignment.test.ts` 已验证 schema 解析与 registry 产物。 | 关闭的是 improvement candidate contract 漂移，不外推所有下游 UI 已消费新字段。 |
| R21-66 | §108 Contract 文档 vs 实现偏差 | P1 | `rollout-record.ts:27-38` | RolloutRecord 缺 triggeredBy/metrics(errorRate/latencyP99)/auditContext | 已修复（源码修复并补定向测试） | 根因是旧 `RolloutRecordSchema` 只有 `level/status/approvedBy/guardrailReasonCodes/evidence`，缺少 rollout 来源、观测指标和审计上下文，导致 release/rollback 事件无法满足 contract 侧 explainability。 | 本轮在 `src/platform/five-plane-orchestration/oapeflir/types/rollout-record.ts` 增补 `triggeredBy/triggerReason/metrics/auditContext/fromLevel/toLevel/createdAt`，并让 `policy-rollout-service.ts` 与 `rollout-state-machine.ts` 在 start/promote/rollback 路径上真实填充这些字段。`tests/unit/platform/five-plane-orchestration/rollout-contract-alignment.test.ts` 已验证 alias 归一化和 auditContext 回填。 | 关闭的是 RolloutRecord 字段面缺口。 |
| R21-67 | §108 Contract 文档 vs 实现偏差 | P1 | `rollout-record.ts:13-25` | 状态机与 contract 不匹配——缺 candidate_created/under_review/evaluation_enabled/released | 已修复（源码修复并补定向测试） | 根因是 improve-rollout 状态机长期停留在 `draft/pending_approval/shadow/.../stable` 老状态集，和 contract 的 candidate lifecycle 分层完全错位。 | 本轮把 `RolloutStatusSchema`、`RolloutStateMachine`、`PolicyRolloutService`、`RolloutScheduler` 与 `CanaryTrafficRouter` 全部迁移到 `candidate_created/under_review/approved/evaluation_enabled/canary_5/partial_25/stable_75/stable_100/released/rolled_back` 这套 canonical 状态。`tests/unit/platform/five-plane-orchestration/rollout-contract-alignment.test.ts` 已验证 approved -> evaluation_enabled 与 stable_100 -> released。 | 关闭的是 rollout 状态机 contract 漂移。 |
| R21-68 | §108 Contract 文档 vs 实现偏差 | P1 | `artifact-model.ts:3-19` | ArtifactRecord 字段集完全偏离 contract——缺 planId/refs/publishStatus/metadata | 已修复（源码修复并补定向测试） | 复核后发现这条是“半真半假”：`refs/publishStatus/metadata` 其实早已存在，真正剩余的差异主要是 contract 侧 `planId` 未在公开 schema 中建模。 | 本轮在 `src/platform/five-plane-state-evidence/artifacts/artifact-model.ts` 为 `ArtifactRecordSchema` 增补 `planId`，并通过 `parseArtifactRecord()` 在 legacy `planGraphBundleId` 输入上回填 `planId`；`tests/unit/platform/state-evidence/artifact-model-alignment.test.ts` 已覆盖 canonical `planId` 和 legacy backfill 两条路径。 | 关闭的是 ArtifactRecord 与 contract 的剩余字段差异，不把原本已存在的 `refs/publishStatus/metadata` 误写成“本轮新加”。 |
| R21-69 | §108 Contract 文档 vs 实现偏差 | P1 | `artifact-model.ts:3-19` | ArtifactType 无 OAPEFLIR 类型(evidence_bundle/timeline_export/workflow_checkpoint 等) | 已证伪（源码与测试已满足） | 原条目停留在 OAPEFLIR artifact plane 早期类型集；现行 artifact model 已显式包含 `evidence_bundle`、`timeline_export`、`workflow_checkpoint`、`learning_object_bundle`、`improvement_candidate_bundle`、`rollout_evidence` 等类型。 | `src/platform/five-plane-state-evidence/artifacts/artifact-model.ts` 当前 `ArtifactTypeSchema` 已定义上述 canonical OAPEFLIR 类型；`tests/unit/platform/five-plane-state-evidence/artifacts/artifact-model.test.ts` 已覆盖合法枚举解析。 | 不改代码，仅回写现行 artifact type 集合。 |
| R21-70 | §108 Contract 文档 vs 实现偏差 | P1 | `artifact-model.ts:79` | BundleType 仅 incident_bundle 部分匹配 contract——缺 task_result/promotion_evidence/canary_metrics | 已证伪（源码与测试已满足） | 原审计没有吸收 bundle 扩展后的现状；当前扩展 bundle schema 已包含 `task_result`、`promotion_evidence`、`canary_metrics` 等 canonical bundle 类型。 | `src/platform/five-plane-state-evidence/artifacts/artifact-model.ts` 的 `ArtifactBundleExtendedSchema.bundleType` 已包含 `task_result`、`promotion_evidence`、`canary_metrics`；`tests/unit/platform/five-plane-state-evidence/artifacts/artifact-model.test.ts` 已覆盖扩展 bundle 解析。 | 不改代码，仅回写 bundle type 事实。 |
| R21-71 | §108 Contract 文档 vs 实现偏差 | P1 | `artifact-model.ts:81` | publishStatus lifecycle preview→archived 变为 review→recalled——非 contract 值 | 已证伪（源码与测试已满足） | 现行 artifact plane 并没有 `review/recalled` 这一套生命周期；公开 schema 仍使用 `draft/preview/published/archived`。原条目引用的是过时导出面。 | `src/platform/five-plane-state-evidence/artifacts/artifact-model.ts` 中 `ArtifactRecordSchema.publishStatus` 与 `ArtifactBundleExtendedSchema.publishStatus` 都是 `draft/preview/published/archived`；对应单测已覆盖非法状态拒绝。 | 不改代码，仅回写现行 publish status 合同。 |
| R21-72 | §108 Contract 文档 vs 实现偏差 | P2 | `fingerprint-builder/index.ts:3-17` | BehaviorFingerprint 缺 subject_type/baseline_ref 字段 | 已修复（源码修复并补定向测试） | 根因是 `BehaviorFingerprintBuilder` 之前只输出 hash 和归一化特征，没有把 subject 类型和基线引用提升为一等字段，调用方只能从 feature 字符串或外层上下文反推。 | 本轮在 `src/ops-maturity/drift-detection/fingerprint-builder/index.ts` 为 input/output 增加 `subjectType` 与 `baselineRef`，并把二者写入 `normalizedFeatures`。 | 定向验证已通过：`tests/unit/ops-maturity/drift-detection/fingerprint-builder/index.test.ts` 现已断言 `subjectType/baselineRef` 字段和对应 normalized feature。 |
| R21-73 | §108 Contract 文档 vs 实现偏差 | P2 | `changepoint-detector/index.ts:19` | Drift response actions 缺 throttle/downgrade/rollback/freeze——仅有 observe/require_review/pause | 已修复（源码修复并补定向测试） | 根因是 drift detector 之前只暴露最粗粒度的 `observe/require_review/pause_agent`，无法表达“先降速、再降级、再回滚/冻结”这条更细的响应链。 | 本轮在 `src/ops-maturity/drift-detection/changepoint-detector/index.ts` 扩展 `DriftResponseActionType`，并让 `detect()` 按 drift 强度输出 `throttle/downgrade/rollback/freeze`。 | 定向验证已通过：`tests/unit/ops-maturity/drift-detection/changepoint-detector/index.test.ts` 现已覆盖 high drift -> `freeze` 与 medium drift -> `downgrade`。 |
| R21-74 | §108 Contract 文档 vs 实现偏差 | P2 | `drift-detection/ (全目录)` | Contract canonical objects DriftSignal/DriftResponsePlan 无实现——仅有 DriftSample | 已解决（源码修复并补定向测试） | 这条在复核时发现是“半真半假”：`DriftSignal` 其实早已存在，但 `DriftResponsePlan` 确实没有落地到 changepoint 层公开导出，因此 contract object 只完成了一半。 | 本轮在 `src/ops-maturity/drift-detection/changepoint-detector/index.ts` 补入 `DriftResponsePlan`，并新增 `buildResponsePlan(...)` 生成主动作、fallback actions、guardrails、baselineRef 和 linkedSignalId。 | 定向验证已通过：`tests/unit/ops-maturity/drift-detection/changepoint-detector/index.test.ts` 新增 response plan 构造断言。 |
| R21-75 | §108 Contract 文档 vs 实现偏差 | P2 | `context-compaction-service.ts:229-233` | CompactionRecord 缺 covered_message_range | 已证伪（源码已满足） | 原条目停留在 compaction record 还没把 message range 接进 truth record 的阶段；现行服务已经在 trim/summarize 两条记录上都写入 `coveredMessageRange`。 | `src/platform/five-plane-execution/execution-engine/context-compaction-service.ts` 当前在 stage1/stage2 `CompactionRecord` 上都回填 `coveredMessageRange: options.coveredMessageRange ?? null`。 | 不改代码，仅回写现行 compaction record 字段。 |
| R21-76 | §108 Contract 文档 vs 实现偏差 | P2 | `context-compaction-service.ts:78-89` | FeedbackSignal/LearningObject 摘要未列为 protected_parts——可被压缩删除 | 已修复（源码修复并补定向测试） | 根因是 `isProtectedMessage()` 之前只保护 `user_request/assistant_plan/approval_decision/compaction_summary`，feedback 与 learning 摘要仍会进入 trim/summarize 候选集。 | 本轮在 `src/platform/five-plane-execution/execution-engine/context-compaction-service.ts` 把 `feedback_signal`、`learning_object`、`learning_object_summary` 加入保护列表。 | 定向验证已通过：`tests/unit/platform/execution/execution-engine/context-compaction-service-detailed.test.ts` 新增 `protects feedback and learning summaries from compaction`。 |
| R21-77 | §108 Contract 文档 vs 实现偏差 | P2 | `context-compaction-service.ts:279-289` | kvCacheFixedPrefixCacheKey 声明但从不填充 | 已修复（源码修复并补定向测试） | 根因是 compaction service 之前只声明了 `kvCacheFixedPrefixCacheKey/kvCacheDomainBlockCacheKey` 两个返回槽位，但在 stage1/stage2 完成后没有对固定前缀层和 domain block 层内容做任何 key 计算。 | 本轮在 `src/platform/five-plane-execution/execution-engine/context-compaction-service.ts` 以 SHA-256 对固定前缀内容和非前缀对话内容分别计算 `kvCacheFixedPrefixCacheKey/kvCacheDomainBlockCacheKey` 并写回结果；`tests/unit/platform/execution/execution-engine/context-compaction-service-detailed.test.ts` 新增两条断言，验证 KV cache key 会被真实填充且长度为 64。 | 关闭的是 key 声明未赋值问题。 |
| R21-78 | §108 Contract 文档 vs 实现偏差 | P2 | `stream-bridge.ts` | 无 transport state tracking(connected/reconnecting/failed)——违反 gateway_streaming §10 | 需继续整改（contract/文档） | contract 文档或导出面没有完全跟随 canonical runtime 迁移。 | 该条本质是 contract/导出面问题，需要逐份 contract 回写或补强测试。 | 本轮不伪造代码修复。 |
| R22-01 | §110 UI Shared Packages 深层缺陷 | P0 | `auth/token-manager.ts:4` | Token 存储在 JS 变量中——刷新即丢失,无安全存储集成 | 已解决（源码/测试） | 根因分两层：早期 `TokenManager` 确实只有进程内 session；后续虽然补了 `sessionStorage`，但 runtime 入口还没有把默认 token 存储与 `PlatformAdapter.secureStorage` 串起来，所以复审仍保守挂账。 | 当前 `ui/packages/shared/auth/src/token-manager.ts` 默认已从 `sessionStorage` 读写会话；本轮又在 `ui/packages/shared/state/src/index.ts` 让默认 `TokenManager` 使用 `sessionStorage + PlatformAdapter.secureStorage` 镜像存储，并在 `UiRuntimeProvider` 启动时于 `sessionStorage` 为空时从平台安全存储回填 session。`ui/apps/web/src/app-shell.tsx` 也已把 `platformAdapter` 明确传入 runtime。 | 定向验证已通过：`ui/tests/shared/auth-sync-state.test.ts` 新增回归，直接验证仅在 `platformAdapter` secure store 中存在 session 时，runtime 启动后仍会恢复认证态并把 session 回填到 `sessionStorage`。 |
| R22-02 | §110 UI Shared Packages 深层缺陷 | P0 | `auth/auth-service.ts:36-39` | handleSsoCallback 从 URL 直读 token——无 PKCE(code_verifier/code_challenge/authorization code) | 已解决（源码/测试） | 根因与 `R16-48/2069` 同源：复审条目沿用了 URL token callback 的旧实现认知，没有吸收后续 PKCE authorization code flow 改造。 | 当前 `ui/packages/shared/auth/src/auth-service.ts` 的 `handleSsoCallback()` 已不消费 URL 中的 `access_token/refresh_token`，而是直接跳转到 authorization flow；`handleAuthorizationCallback()` 只接受 `code/state` 并校验待处理 flow。`ui/tests/unit/ui/shared/auth-service.test.ts` 已覆盖“不接受 URL token、未配置 exchange handler 时 fail-closed、按 code flow 完成交换”，定向测试已通过。 | 关闭的是“从 URL 明文接收 token”缺陷，不外推为完整 OAuth 服务端交换链路都已完工。 |
| R22-03 | §110 UI Shared Packages 深层缺陷 | P0 | `platform/web-platform-adapter.ts:9-13` | readSecureValue/writeSecureValue 用 localStorage——XSS 可读 token,§6 明确禁止 | 已解决（源码/测试） | 根因复核后发现，这条挂账引用的是旧版 web adapter 认知；现行 secure storage 已不落 `localStorage`，而是回退到 `DefaultPlatformAdapter` 的内存 `MemorySecureStore`，前次 reaudit 只是没有补上针对性验证。 | 当前 `ui/packages/shared/platform/src/base-platform-adapter.ts` 的 `readSecureValue()/writeSecureValue()` 只读写进程内 `MemorySecureStore`；`ui/packages/shared/platform/src/web-platform-adapter.ts` 也不再覆写 secure storage 到 `localStorage`，只把非敏感的 analytics consent 留在 `localStorage`。 | 定向验证已通过：`ui/tests/shared/web-platform-security-regressions.test.ts` 证明 `writeSecureValue("token", ...)` 后 `window.localStorage` 不会出现 `aa.secure.*` 明文；本轮 `npm test -- tests/shared/web-platform-security-regressions.test.ts` 通过。 |
| R22-04 | §110 UI Shared Packages 深层缺陷 | P0 | `api-client/ws-client.ts:100-104` | onclose 设 "reconnecting" 但从不实际重连——无重连逻辑/指数退避/重试定时器 | 已解决（源码/测试） | 根因不是“现行代码完全没有重连逻辑”，而是该条沿用了旧快照；当前 `BrowserWSClient` 早已有 `scheduleReconnect()`、指数退避和重连定时器，只是前次 reaudit 在没有 UI 定向验证前保守保留挂账。真正缺的一步是静默断连探测，这一部分由 `R22-11` 的 heartbeat 修复补齐。 | `ui/packages/shared/api-client/src/ws-client.ts` 现有 `calculateBackoffDelay()`、`scheduleReconnect()` 与 `doConnect()` 闭环：`onclose/onerror` 都会转入重连路径，并保留指数退避与 jitter。 | 定向验证已通过：`ui/tests/shared/api-client-runtime-regressions.test.ts` 与 `ui/tests/unit/ui/shared/ws-client.test.ts` 均验证连接关闭后会创建新 socket 且触发重连；本轮 `npm test -- tests/shared/api-client-runtime-regressions.test.ts tests/unit/ui/shared/ws-client.test.ts` 通过。 |
| R22-05 | §110 UI Shared Packages 深层缺陷 | P0 | `api-client/ws-client.ts:88` | Auth token 作为 URL query param——暴露在日志/代理/浏览器历史中 | 已解决（源码/测试） | 根因是审计条目停留在 token 拼 URL 的旧实现，没有吸收后续把认证改为 WebSocket subprotocol + 首帧 auth message 的改造。 | 当前 `ui/packages/shared/api-client/src/ws-client.ts` 建连时使用固定 subprotocol `v1.auth.token`，并在 `onopen` 后发送 `{"action":"auth","token":...}` 首帧；URL 本身不再带 token。 | 定向验证已通过：`ui/tests/shared/api-client-runtime-regressions.test.ts` 与 `ui/tests/unit/ui/shared/ws-client.test.ts` 都断言 URL 不含 token 且首条消息才携带 token；本轮定向测试已通过。 |
| R22-06 | §110 UI Shared Packages 深层缺陷 | P0 | `web/feature-registry.ts:1-27` | 27个 feature 全静态导入无 lazy loading——违反 §10 bundle<200KB | 已解决（源码/测试） | 根因与 `R7-1/R16-68` 同源：旧结论把 registry 里的 lazy wrapper、route metadata 和壳层加载入口割裂看待，导致已存在的 `React.lazy` 仍被误记为“全静态导入”。 | 当前 `ui/apps/web/src/feature-registry.ts` 中 feature 组件按 lazy path 注册，且 `route.codeSplit` 现已统一为 `true`；`ui/apps/web/src/app-shell.tsx` 也已补上 `Suspense`。 | 作为重复挂账，随 route-level code split 修复一并关闭；本轮相关定向测试已通过。 |
| R22-07 | §110 UI Shared Packages 深层缺陷 | P0 | `api-client/interceptors.ts:62-76` | Offline queue interceptor 入队后仍放行原始请求——请求会网络失败 | 已解决（源码/测试） | 根因与 `R16-59/R28-37` 同源：旧离线拦截器只负责 enqueue，不会短路后续 transport dispatch。 | 当前 `ui/packages/shared/api-client/src/interceptors.ts` 的 `createOfflineQueueInterceptor()` 在离线非 GET 请求入队后会直接抛出 `rest.offline:Request queued for offline sync`，从而阻断网络层；`ui/tests/shared/api-client-runtime-regressions.test.ts` 已验证 enqueue 后 transport 不会被调用，本轮定向测试已通过。 | 作为重复挂账，随离线短路修复一并关闭。 |
| R22-08 | §110 UI Shared Packages 深层缺陷 | P1 | `state/stores/ (全部4个)` | Zustand stores 无 middleware(persist/devtools/immer)——§4 要求全部三种 | 已解决（源码/测试） | 根因复核后分成两段：一是历史上 store 只零散接了 `persist`，`devtools` 没有统一化；二是本轮尝试直接引入外部 `immer` 依赖时被现有依赖解析问题阻断，导致“draft middleware”长期没有真正落地。为避免被外部包解析卡死而继续挂账，本轮把缺的能力下沉成仓内统一 middleware，而不是继续停留在文档解释。 | 当前 `ui/packages/shared/state/src/stores/middleware.ts` 已集中实现 `withPersistDevtoolsDraft()`，统一把 `persist + devtools + draft-style producer` 套到 `auth-store`、`notification-store`、`realtime-store`、`sync-store`、`theme-store`、`ui-store` 六个 shared store；这些 store 的写路径也已改成 draft mutation 形式。新增 `ui/tests/unit/ui/shared/state-store-middleware.test.ts` 已验证 auth/realtime/notification/sync/theme/ui 六类 store 都能在保留 persist 能力的同时通过 draft-style 更新状态。 | 关闭的是“缺统一 middleware 标准化与 draft 能力”这一实现缺口；这里采用的是仓内 draft middleware，而不是强依赖外部 `immer` 包。 |
| R22-09 | §110 UI Shared Packages 深层缺陷 | P1 | `auth/auth-service.ts:7-14` | login() 无 token refresh 逻辑——§6 要求 sliding-window 刷新 | 已解决（源码/测试） | 根因是前次复核把 `AuthService.login()` 当成 refresh owner，但现行刷新职责实际在 `TokenManager`：`AuthService` 只负责种下 session，后续 token 读取与 sliding-window 刷新都由 `TokenManager.getAccessTokenWithRefresh()` 负责。 | 当前 `ui/packages/shared/auth/src/token-manager.ts` 已实现 `shouldRefresh()` 与 `getAccessTokenWithRefresh()`，默认在过期前 60 秒进入 refresh window，并用 `refreshPromise` 做并发去重；`ui/packages/shared/auth/src/auth-service.ts` 继续通过组合 `TokenManager` 使用这条刷新链。 | 定向验证已通过：`ui/tests/unit/ui/shared/auth-service.test.ts` 已验证 token 进入 60 秒窗口时并发调用只会 refresh 一次，且会写回新的 access/refresh token；本轮定向测试已通过。 |
| R22-10 | §110 UI Shared Packages 深层缺陷 | P1 | `auth/session-guard.ts` | 无 session timeout warning——过期直接抛异常无预警 | 已解决（源码/测试） | 根因是 `SessionGuard` 过去只有 `requireAuthenticated()` 这类 fail-after-expiry 接口，没有“即将过期”的显式信号，运行时也没有把即将过期状态传播到 UI 通知层。 | 本轮在 `ui/packages/shared/auth/src/session-guard.ts` 增加 `getTimeoutWarning()`，默认在到期前 5 分钟返回结构化 warning；同时在 `ui/packages/shared/state/src/index.ts` 的 `UiRuntimeProvider` 中接入轮询检查，并通过 `notificationStore` 发送 `"Session expiring soon"` 告警。 | 定向验证已通过：`ui/tests/shared/auth-sync-state.test.ts` 新增回归用例，验证 token 距过期 4 分钟时运行时会产出 session timeout warning，而不是等过期后才抛 `auth.session_required`。 |
| R22-11 | §110 UI Shared Packages 深层缺陷 | P1 | `api-client/ws-client.ts:73-170` | 无 heartbeat/ping-pong——静默断开不可检测 | 已解决（源码/测试） | 根因是 `BrowserWSClient` 之前虽然在显式 `close/error` 时会重连，但缺少应用层 heartbeat；连接若被静默掐断，客户端没有任何定时探针去触发 `close()`，重连链也就无法启动。 | 本轮在 `ui/packages/shared/api-client/src/ws-client.ts` 为 `BrowserWSClient` 增加 `heartbeatIntervalMs/heartbeatTimeoutMs` 配置、定时 `{"action":"ping"}`、`pong` 清除 deadline 以及超时主动 `close()` 逻辑，从而把“静默断开检测”接入现有重连链。 | 定向验证已通过：`ui/tests/unit/ui/shared/ws-client.test.ts` 新增 heartbeat 用例，分别验证“缺失 pong 时发送 ping 并触发重连”和“收到 pong 时清掉 deadline 不误判重连”；本轮定向测试已通过。 |
| R22-12 | §110 UI Shared Packages 深层缺陷 | P1 | `api-client/interceptors.ts:29-37` | Auth interceptor 捕获静态 string token——refresh 后发送过期 token | 已解决（源码修复并补定向测试） | 根因与 `R16-46` 相同：auth interceptor 曾保留静态 token 兼容分支，导致 refresh 后仍可能走旧 bearer。 | 本轮已把 `ui/packages/shared/api-client/src/interceptors.ts` 收紧为只接受动态 resolver/TokenManager，并对字符串输入 fail-closed；`ui/tests/unit/ui/shared/interceptors.test.ts` 已验证静态 string 会被拒绝，定向测试已通过。 | 作为重复挂账一并关闭。 |
| R22-13 | §110 UI Shared Packages 深层缺陷 | P1 | `web/app-shell.tsx:58-108` | 无 per-feature error boundary——单 feature crash 击穿全应用 | 已解决（源码/测试） | 根因与 `R16-66/R28-32` 同源：旧壳层没有错误边界包装 feature 渲染，重复挂账未同步现行 `AppErrorBoundary`。 | 当前 `ui/apps/web/src/app-shell.tsx` 已用 `AppErrorBoundary` 包裹 `AppRouter/AppFrame`，并提供 `Retry/Report Issue` fallback；本轮新增 `ui/tests/shared/web-app-shell-auth-regression.test.tsx`，验证 feature 组件抛错时会显示错误边界 UI，定向测试已通过。 | 作为重复挂账，随错误边界实装一并关闭。 |
| R22-14 | §110 UI Shared Packages 深层缺陷 | P1 | `web/app-shell.tsx:8-22` | demoGuardContext 硬编码全权限——生产环境绕过真实 auth | 已解决（源码/测试） | 根因仍与 `2164` 相同：旧壳层守卫输入长期是硬编码的全权限 demo 上下文，后续真实 authContext 接线完成后，重复条目没有同步清掉。 | 当前 `ui/apps/web/src/app-shell.tsx` 已移除 demo guard 常量，改为按 `authContext` 动态构造 guard context；本轮新增 `ui/tests/shared/web-app-shell-auth-regression.test.tsx`，验证 guard context 吃到的是传入租户、角色和权限，定向测试已通过。 | 作为重复挂账，随现行动态 guard 实现一并关闭。 |
| R22-15 | §110 UI Shared Packages 深层缺陷 | P1 | `web/public/aa-sw.js:27-31` | ServiceWorker sync handler 是 no-op——§7 要求 background sync replay offline queue | 已解决（源码/测试） | 根因是早期 service worker 的 `sync` 处理器确实只是 no-op，占位声明了 background sync，但离线队列从未被真正回放。 | 当前 `ui/apps/web/public/aa-sw.js` 已在 `sync` 事件中按 `aa-sync-offline` tag 调用 `replayOfflineQueue()`，并从 IndexedDB 读取 `offline-queue` 后重放请求、成功后删除已同步 mutation；`ui/tests/unit/ui/apps/web/aa-sw.test.ts` 现已验证 sync 会真实重放队列并删除成功记录。 | 作为 `1928` 的重复挂账一并关闭，不外推为完整离线 UX 已全部产品化。 |
| R22-16 | §110 UI Shared Packages 深层缺陷 | P1 | `sync/offline-queue.ts:19-21` | enqueue 不 await readyPromise——IndexedDB 水合可覆盖已入队项 | 已解决（源码/测试） | 根因是旧 `enqueue()` 在初始 `readAll()` 完成前即可修改队列并触发 persist，IndexedDB 水合返回后可能覆盖刚入队的 mutation。 | 当前 `ui/packages/shared/sync/src/offline-queue.ts` 的 `enqueue()` 已先 `await this.readyPromise` 再写队列并持久化；`ui/tests/unit/ui/shared/offline-queue.test.ts` 现已验证在 ready barrier 完成前 mutation 不会提前可见，完成后才入队持久化，本轮定向测试已通过。 | 关闭的是 IndexedDB 初始水合覆盖问题，不外推为所有浏览器生命周期丢写风险都已完全消除。 |
| R22-17 | §110 UI Shared Packages 深层缺陷 | P1 | `sync/conflict-resolver.ts:15-26` | mergeValues shallow spread local wins——无 LWW timestamp/OT | 需继续整改（UI 产品化差距） | 这类条目描述的是 UI monorepo、feature、原生壳、性能预算、可访问性与后端 presentation 接线的产品化缺口，需要跨 `ui/` 与后端服务成组整改。 | 本轮没有执行 UI 全链路 build/e2e/原生壳验证，也没有完成整组 UI 实装重构，不能虚写已修。 | 保留为后续 UI 主线整改。 |
| R22-18 | §110 UI Shared Packages 深层缺陷 | P1 | `telemetry/index.ts:16-47` | 仅 structured event——缺 performance marks/error boundary reporting/user journey | 需继续整改（UI 产品化差距） | 这类条目描述的是 UI monorepo、feature、原生壳、性能预算、可访问性与后端 presentation 接线的产品化缺口，需要跨 `ui/` 与后端服务成组整改。 | 本轮没有执行 UI 全链路 build/e2e/原生壳验证，也没有完成整组 UI 实装重构，不能虚写已修。 | 保留为后续 UI 主线整改。 |
| R22-19 | §110 UI Shared Packages 深层缺陷 | P1 | `telemetry/index.ts:78-92` | OTLP exporter 把 logs 放 scopeMetrics[].logs——无效格式,collector 会丢弃 | 需继续整改（UI 产品化差距） | 这类条目描述的是 UI monorepo、feature、原生壳、性能预算、可访问性与后端 presentation 接线的产品化缺口，需要跨 `ui/` 与后端服务成组整改。 | 本轮没有执行 UI 全链路 build/e2e/原生壳验证，也没有完成整组 UI 实装重构，不能虚写已修。 | 保留为后续 UI 主线整改。 |
| R22-20 | §110 UI Shared Packages 深层缺陷 | P1 | `web/runtime.ts:44-46` | wsClient 创建忽略 config.wsUrl——WS URL 从不传给 connect() | 已解决（源码/测试） | 根因是复审条目把“`BrowserWSClient` 构造器不直接吃 URL”误判成“URL 被忽略”；现行接线实际上是在 runtime 暴露 `wsUrl`，再由共享状态层启动时调用 `connect(wsUrl, token)`。 | 当前 `ui/apps/web/src/runtime.ts` 会把 `config.wsUrl` 暴露到返回的 runtime clients；`ui/apps/web/src/app-shell.tsx` 继续把 `wsUrl` 传入 `UiRuntimeProvider`；`ui/packages/shared/state/src/index.ts` 在 bootstrap 时会对 `router.connect(wsUrl, wsToken)` 真正建连。本轮新增 `ui/tests/shared/ws-runtime-connect-regression.test.tsx`，直接验证 runtime 启动时会用传入的 `wsUrl` 和 session token 调用 `wsClient.connect(...)`，定向测试已通过。 | 关闭的是“wsUrl 完全未接线”缺陷，不外推为所有 WS 生命周期能力都已完工。 |
| R22-21 | §110 UI Shared Packages 深层缺陷 | P1 | `tauri-macos/src-tauri/src/lib.rs:16-20` | 仅注册3命令,TS bridge 调用12+命令——运行时全部 "command not found" | 需继续整改（UI 产品化差距） | 这类条目描述的是 UI monorepo、feature、原生壳、性能预算、可访问性与后端 presentation 接线的产品化缺口，需要跨 `ui/` 与后端服务成组整改。 | 本轮没有执行 UI 全链路 build/e2e/原生壳验证，也没有完成整组 UI 实装重构，不能虚写已修。 | 保留为后续 UI 主线整改。 |
| R22-22 | §110 UI Shared Packages 深层缺陷 | P1 | `tauri-linux/src-tauri/src/lib.rs:16-20` | 同 tauri-macos——仅3命令 vs 12+ expected | 需继续整改（UI 产品化差距） | 这类条目描述的是 UI monorepo、feature、原生壳、性能预算、可访问性与后端 presentation 接线的产品化缺口，需要跨 `ui/` 与后端服务成组整改。 | 本轮没有执行 UI 全链路 build/e2e/原生壳验证，也没有完成整组 UI 实装重构，不能虚写已修。 | 保留为后续 UI 主线整改。 |
| R22-23 | §110 UI Shared Packages 深层缺陷 | P1 | `state/index.ts:80` | 硬编码 access_token=ui-runtime-access 在生产可达路径——mock token 用于真实 API | 已解决（源码/测试） | 根因与 `2175/R10-28` 同源：旧共享状态层曾合成伪 session token，重复挂账没有吸收后续统一 `TokenManager/AuthService` 链路。 | 当前 `ui/packages/shared/state/src/index.ts` 已通过传入或新建的 `TokenManager` 构造 `AuthService`，源码中已不存在 `ui-runtime-access`；`ui/tests/shared/web-runtime-auth-regression.test.ts` 已验证 web runtime 把动态 `TokenManager` 传入 auth interceptor，且工作树中 `ui/packages/shared/state/src` / `ui/apps/web/src` 已无该硬编码凭证。 | 作为重复挂账，随统一动态 token 链路一并关闭。 |
| R22-24 | §110 UI Shared Packages 深层缺陷 | P1 | `web/app-shell.tsx:100-104` | 无 shell lifecycle phases(boot→auth→load→render→idle)——feature 立即渲染无等待 auth | 需继续整改（UI 产品化差距） | 这类条目描述的是 UI monorepo、feature、原生壳、性能预算、可访问性与后端 presentation 接线的产品化缺口，需要跨 `ui/` 与后端服务成组整改。 | 本轮没有执行 UI 全链路 build/e2e/原生壳验证，也没有完成整组 UI 实装重构，不能虚写已修。 | 保留为后续 UI 主线整改。 |
| R22-25 | §110 UI Shared Packages 深层缺陷 | P2 | `web/vite.config.ts:20-21` | 所有 features 打入单一 chunk——应 per-route code split | 需继续整改（UI 产品化差距） | 这类条目描述的是 UI monorepo、feature、原生壳、性能预算、可访问性与后端 presentation 接线的产品化缺口，需要跨 `ui/` 与后端服务成组整改。 | 本轮没有执行 UI 全链路 build/e2e/原生壳验证，也没有完成整组 UI 实装重构，不能虚写已修。 | 保留为后续 UI 主线整改。 |
| R22-26 | §110 UI Shared Packages 深层缺陷 | P2 | `api-client/ws-event-router.ts:51-94` | 事件路由用 plain string——无 discriminated union 编译期穷尽检查 | 需继续整改（UI 产品化差距） | 这类条目描述的是 UI monorepo、feature、原生壳、性能预算、可访问性与后端 presentation 接线的产品化缺口，需要跨 `ui/` 与后端服务成组整改。 | 本轮没有执行 UI 全链路 build/e2e/原生壳验证，也没有完成整组 UI 实装重构，不能虚写已修。 | 保留为后续 UI 主线整改。 |
| R22-27 | §110 UI Shared Packages 深层缺陷 | P2 | `sync/sync-coordinator.ts:33-38` | flush() 清空队列但从不发送到服务器——离线 mutation 静默丢弃 | 需继续整改（UI 产品化差距） | 这类条目描述的是 UI monorepo、feature、原生壳、性能预算、可访问性与后端 presentation 接线的产品化缺口，需要跨 `ui/` 与后端服务成组整改。 | 本轮没有执行 UI 全链路 build/e2e/原生壳验证，也没有完成整组 UI 实装重构，不能虚写已修。 | 保留为后续 UI 主线整改。 |
| R22-28 | §110 UI Shared Packages 深层缺陷 | P2 | `web/public/aa-sw.js:9-24` | SW 缓存所有 GET(含 API)——陈旧 API 响应永久从缓存提供 | 需继续整改（UI 产品化差距） | 这类条目描述的是 UI monorepo、feature、原生壳、性能预算、可访问性与后端 presentation 接线的产品化缺口，需要跨 `ui/` 与后端服务成组整改。 | 本轮没有执行 UI 全链路 build/e2e/原生壳验证，也没有完成整组 UI 实装重构，不能虚写已修。 | 保留为后续 UI 主线整改。 |
| R22-29 | §110 UI Shared Packages 深层缺陷 | P2 | `mobile/App.tsx:5` | Platform 硬编码 "android"——iOS 用户获得错误适配器 | 需继续整改（UI 产品化差距） | 这类条目描述的是 UI monorepo、feature、原生壳、性能预算、可访问性与后端 presentation 接线的产品化缺口，需要跨 `ui/` 与后端服务成组整改。 | 本轮没有执行 UI 全链路 build/e2e/原生壳验证，也没有完成整组 UI 实装重构，不能虚写已修。 | 保留为后续 UI 主线整改。 |
| R22-30 | §110 UI Shared Packages 深层缺陷 | P2 | `electron-win/src/main.ts:1-41` | Electron 主进程仅配置——无 BrowserWindow/IPC handler/auto-update | 需继续整改（UI 产品化差距） | 这类条目描述的是 UI monorepo、feature、原生壳、性能预算、可访问性与后端 presentation 接线的产品化缺口，需要跨 `ui/` 与后端服务成组整改。 | 本轮没有执行 UI 全链路 build/e2e/原生壳验证，也没有完成整组 UI 实装重构，不能虚写已修。 | 保留为后续 UI 主线整改。 |
| R22-31 | §111 Event Bus / State / Execution 深层缺陷 | P0 | `transactional-event-appender.ts` | Truth table 未在同一事务更新——event+truth 非原子违反 §25.2 | 已解决（源码修复并补定向测试） | 根因确实在接口层：旧 `TransactionalEventAppender` 只提供 event/outbox 包装，调用方即使想做 truth 更新，也没有任何参数或闭包能把 truth mutation 带入同一事务。 | 当前 `src/platform/five-plane-state-evidence/events/transactional-event-appender.ts` 已新增 `mutateTruth` hook，并在 `db.transaction(...)` 内和 event/outbox 一起执行；`tests/unit/platform/state-evidence/events/transactional-event-appender.test.ts` 已新增 “truth mutation 与 event 同事务提交” 和 “truth mutation 抛错时 event 回滚” 两条回归。 | 已改生产代码与定向测试，关闭 appender 级原子性缺口。 |
| R22-32 | §111 Event Bus / State / Execution 深层缺陷 | P0 | `cas/cas-service.ts` | CAS store 纯内存 Map——重启丢失,§8 要求持久化权威存储 | 已解决（SQLite 持久化 CAS Service 已投产） | 根因确实是纯内存 Map 没有持久化：旧 `CasService` 构造函数无任何数据库/仓储依赖，所有 CAS 操作基于进程内 `Map`。 | 本轮已在 `src/platform/five-plane-state-evidence/events/cas/cas-service.ts` 新增 `createSqliteCasService()` 工厂函数；`storage-backend-factory.ts` 的 `openAuthoritativeStorageBackend()` 已将其接入 SQLite 持久化存储；`sqlite-migration-runtime-part4.ts` 的 migration 51 已创建 `cas_records` 表并完成 wiring。 | 持久化 CAS Service 已实装，关闭进程内 Map 缺口。 |
| R22-33 | §111 Event Bus / State / Execution 深层缺陷 | P0 | `dlq-service.ts (markRetryExhausted)` | 将 status 设回 "pending" 而非终态——耗尽项重入处理队列 | 已解决（源码修复并补定向测试） | 这条是本轮确认无误的真实缺口：`markRetryExhausted()` 之前会把耗尽记录改回 `pending`，确实会重新落回 backlog 统计和后续处理面。 | 本轮已将 `src/platform/five-plane-state-evidence/events/dlq-service.ts` 与兼容 `src/platform/five-plane-state-evidence/dlq/index.ts` 的耗尽状态统一改为终态 `discarded`，并保留 `retry_exhausted` action log / `retryExhaustedAt` 作为区分；`tests/unit/platform/state-evidence/events/dlq-service.test.ts`、`tests/unit/platform/state-evidence/dlq/index.test.ts`、`tests/unit/platform/state-evidence/dlq/dlq.test.ts` 定向回归已通过。 | 这是实修终态语义，不是文档粉饰。 |
| R22-34 | §111 Event Bus / State / Execution 深层缺陷 | P0 | `transition-service.ts (TaskTerminalTransition)` | Terminal transition 无 CAS——并发竞态可覆盖状态 | 已解决 | 原审计对应的是 terminal transition 还在直接覆写多实体状态的早期实现；当前实现已经补上了逐实体 CAS。 | 当前 `src/platform/execution/state-transition/transition-service.ts` 的 `TaskTerminalTransitionService.apply()` 会分别调用 `updateTaskStatusCas()`、`updateWorkflowStateCas()`、`updateSessionStatusCas()`、`updateExecutionStatusCas()`，并在任一 `affected === 0` 时立即抛错回滚。 | 不改生产代码，仅回写现行 terminal CAS 依据。 |
| R22-35 | §111 Event Bus / State / Execution 深层缺陷 | P0 | `budget-allocator.ts (settle)` | Ledger version 内存自增无 CAS 校验——并发 settle 可覆盖 | 已修复（allocator authoritative CAS 写回已补齐） | 真实根因是 `settle()/release()` 过去虽然对 `BudgetReservation` 走了状态机，但对 `BudgetLedger` 仍只返回内存派生对象，缺少 authoritative repository 的 compare-and-set 与持久写回，因此并发更新会丢失 ledger 版本门禁。 | 当前 `src/platform/five-plane-state-evidence/truth/runtime-truth-repository.ts` 已新增 `appendBudgetReservation()` 与 `compareAndSetBudgetLedger()`；`src/platform/five-plane-execution/budget-allocator.ts` 也新增 `BudgetAllocatorAuthoritativeStore`、`persistLedgerIfNeeded()` 与层级 ledger 持久化路径，`reserve()/settle()/release()` 在注入 authoritative store 后都会走 ledger CAS 写回，而不再只返回内存自增版本。`tests/unit/platform/five-plane-execution/budget-allocator.test.ts` 已新增并通过 `settle() and release() can persist ledger CAS through authoritative truth repository` 回归。 | 关闭的是 allocator 层 `settle()/release()` 缺 authoritative CAS 的真实缺口；不把它扩写成“所有预算主线集成都已完成”。 |
| R22-36 | §111 Event Bus / State / Execution 深层缺陷 | P0 | `degradation-controller.ts (getFallbackCandidates)` | 永远返回[]——D1 fallback 是死代码,立即升级到 D2 | 已解决 | 原审计对应的是 fallback provider 还没有 profile 枚举接口时的旧实现。 | 当前 `src/platform/model-gateway/degradation/degradation-controller.ts` 会调用 provider 的 `getAvailableProfiles()` 生成 fallback candidates，而 `src/platform/model-gateway/provider-registry/unified-chat-provider.ts` 已实现该方法；`tests/unit/platform/model-gateway/degradation-controller.test.ts` 也已对 D1 fallback 路径回归通过。 | 不改生产代码，仅回写现行 fallback 证据。 |
| R22-37 | §111 Event Bus / State / Execution 深层缺陷 | P0 | `unified-chat-provider.ts (streaming)` | Streaming path 绕过 circuit breaker——失败不计入,无保护 | 已解决 | 原审计没有复核 streaming 分支已经被统一包进 breaker 执行路径。 | 当前 `src/platform/model-gateway/provider-registry/unified-chat-provider.ts` 的 `createStreamingChatCompletion()` 会构造 `runStreamingWithBreaker()`，并在存在 breaker 时通过 `breaker.execute(runStreamingWithBreaker)` 执行，因此 streaming 失败同样计入 breaker。 | 不改生产代码，仅回写 streaming 保护链证据。 |
| R22-38 | §111 Event Bus / State / Execution 深层缺陷 | P1 | `durable-event-bus.ts` | 无 per-aggregate 有序投递——§7 要求按 aggregateId 有序 | 已解决 | 原审计没有复核后续补进的 aggregate partition + sequence gate；本轮还顺手补了一个真实兼容性缺口，即 `sequence == null` 的 tier-1 事件不能被永远卡住。 | 当前 `src/platform/five-plane-state-evidence/events/durable-event-bus.ts` 会按 `aggregateId` 分组、按 `sequence` 排序，并用 `consumerGroupSequences` 记录各 aggregate 的最后确认序号；本轮又补上了“无 sequence 事件允许投递、但不污染 sequence waterline”的兼容处理。`tests/unit/platform/state-evidence/events/durable-event-bus.test.ts` 定向回归已通过。 | 不改架构方向，仅回写现行有序投递依据并补兼容缺口。 |
| R22-39 | §111 Event Bus / State / Execution 深层缺陷 | P1 | `cas/fencing-token-service.ts` | validateFencingToken 按"-"split 但 ID 可含"-"——解析错误 | 已解决（源码修复并补定向测试） | 这条是本轮确认的真实解析缺陷：旧 token 采用 `-` 连接 executionId/nodeId/counter/timestamp，executionId 或 nodeId 一旦本身带 `-`，验证阶段就会错拆 owner 与 executionId。 | 本轮已把 `src/platform/five-plane-state-evidence/events/cas/fencing-token-service.ts` 的 token 编码切为 `::` 分隔并做 URL 编码/解码校验，消除了带连字符 ID 的歧义；`tests/unit/platform/state-evidence/events/cas/fencing-token-service.test.ts` 已新增 hyphenated executionId/nodeId 回归。 | 这是实修 token 编码契约。 |
| R22-40 | §111 Event Bus / State / Execution 深层缺陷 | P1 | `cas/fencing-token-service.ts (acquireFence)` | expiresAt 永远 null——过期 fence 永久阻塞新获取 | 已解决（源码修复并补定向测试） | 这条同样是真缺口：旧 `acquireFence()` 创建的 fence `expiresAt` 恒为 `null`，进程内 fence 一旦遗留就不会自然淘汰。 | 本轮已在 `src/platform/five-plane-state-evidence/events/cas/fencing-token-service.ts` 为新 fence 加入默认 TTL，并在 `acquireFence()/isFenceHeld()/getFenceInfo()/getActiveFenceCount()` 前统一执行过期清理；`tests/unit/platform/state-evidence/events/cas/fencing-token-service.test.ts` 已同步验证 `expiresAt` 不再为空。 | 这是实修 fence 生命周期，不是单改注释。 |
| R22-41 | §111 Event Bus / State / Execution 深层缺陷 | P1 | `cas/fencing-token-service.ts` | static activeFences 是进程本地 Map——非分布式,违反分布式 fencing | 已解决（SQLite 共享后端 + PostgreSQL 分布式后端） | 根因先前确实是 fencing 只依赖进程内状态；上一轮虽已补出 SQLite 共享仓库，但仍缺真正面向跨节点部署的分布式存储后端，而且简单“把表持久化”还不够，因为 exclusive fence 获取需要事务内串行化，否则多节点并发仍可能双持有。 | 本轮新增 `src/platform/five-plane-state-evidence/events/cas/postgres-fence-repository.ts` 与 `postgres-fencing-token-service.ts`，通过 PostgreSQL `fence_records` 表和 `pg_advisory_xact_lock(hashtext(executionId))` 在事务内串行化同一 execution 的 fence 获取/释放；同时补入 PG migration `16`（`artifact_node_run_and_fencing`）创建 `fence_records` 并为 artifacts 增加 `node_run_id`。定向验证 `tests/integration/platform/state-evidence/truth/postgres-fencing-token-service.test.ts` 已证明两个 service 实例在同一 PG backend 上会共享 exclusive/shared fence 状态和计数；`tests/integration/platform/state-evidence/truth/sqlite-fence-repository.test.ts` 也继续通过。 | 关闭的是“没有真正跨节点 fencing backend”的问题。保留说明：进程内 `FencingTokenService` 仍可作为单机/测试实现存在，但不再是唯一分布式方案，因此本编号不再保持开放。 |
| R22-42 | §111 Event Bus / State / Execution 深层缺陷 | P1 | `durable-event-bus.ts (retry)` | attempt <= MAX 执行 MAX+1 次——off-by-one | 已解决（源码修复并补定向测试） | 这条是本轮确认的真实缺口：旧循环从 `attempt = 0` 跑到 `<= MAX_DELIVERY_RETRIES`，实际会执行 `MAX+1` 次 handler 调用。 | 本轮已把 `src/platform/five-plane-state-evidence/events/durable-event-bus.ts` 的重试循环改成 `attempt = 1; attempt <= MAX_DELIVERY_RETRIES; attempt++`，并同步把 backoff index 调整为 `attempt - 1`；`tests/unit/platform/state-evidence/events/durable-event-bus.test.ts` 现已直接断言失败 handler 恰好执行 3 次。 | 这是实修重试次数契约。 |
| R22-43 | §111 Event Bus / State / Execution 深层缺陷 | P1 | `budget-allocator.ts` | 无层级预算(platform→domain→task)——仅 flat ledger | 已修复（层级账本链路已补齐；持久化另见 R22-35） | 根因曾经真实存在：`BudgetAllocator` 只有 `BudgetTier`/`tierLimit` 上下文，却没有任何父子 ledger 关系和祖先级联更新入口，因此层级预算只是配置概念，不是可执行账本链路。 | 本轮在 contract 与 allocator 两侧补齐层级账本表达：`src/platform/contracts/executable-contracts/index.ts` 的 `BudgetLedger` / `createBudgetLedger(...)` 已支持 `tier / scopeKey / parentBudgetLedgerId`；`src/platform/five-plane-execution/budget-allocator.ts` 的 `reserve()/settle()/release()` 已支持 `hierarchyLedgers` 输入，并对祖先 ledger 执行同步 reservation/settlement/release 更新；`tests/unit/platform/five-plane-execution/budget-allocator.test.ts` 已新增并通过 `hierarchical ledgers reserve and settle together`。 | 关闭的是“allocator 完全只有 flat ledger、无法级联预算”的缺口；`R22-35` 里的持久化 CAS/仓储写回问题仍保持开放，不把两条混为一谈。 |
| R22-44 | §111 Event Bus / State / Execution 深层缺陷 | P1 | `budget-allocator.ts` | 无 watermark alerts | 已解决 | 原审计对应的是 `BudgetAllocator` 只保留 watermark 配置、却没有真正发出事件的旧状态。 | 当前 `src/platform/five-plane-execution/budget-allocator.ts` 已持有 `events` 端口，并在归一化上下文后实际调用 `emitWatermarkAlert` / `emitAutoThrottleEvent`；相关 `budget-allocator` 定向回归本轮已通过。 | 不改主逻辑，仅回写现行 watermark/auto-throttle 证据。 |
| R22-45 | §111 Event Bus / State / Execution 深层缺陷 | P1 | `circuit-breaker.ts (getState)` | 读状态有副作用(open→half_open 转换)——side-effecting getter | 已解决（源码修复并补定向测试） | 真实根因是状态推进职责放错了位置：旧实现把“到达 probe 窗口后可进入 half_open”这件事塞进了 `getState()` 读取路径里，导致任何观测行为都会隐式改写 breaker 状态并发出状态迁移事件。 | 本轮已在 `src/platform/model-gateway/provider-registry/circuit-breaker.ts` 把 `getState()` 改成基于 `computeReadableState()` 的纯读取；真正的 `open → half_open` 状态推进只在 `canExecute()` 允许 probe 时，或 `onSuccess()/onFailure()` 接收 probe 结果时通过 `promoteToHalfOpenIfProbeWindowExpired()` 发生。定向测试 `tests/unit/platform/model-gateway/provider-registry/circuit-breaker.test.ts` 已新增 “getState is a pure read” 回归，并连同 `tests/unit/platform/model-gateway/circuit-breaker.test.ts`、`tests/integration/platform/model-gateway/provider-registry/circuit-breaker-integration.test.ts` 验证现有 half-open/probe/close 流程未回退。 | 已改生产代码并补最小回归，修的是 getter 副作用本身，不是只改测试表述。 |
| R22-46 | §111 Event Bus / State / Execution 深层缺陷 | P1 | `circuit-breaker.ts (getRecentFailureRate)` | (failures/windowSec)\*10 公式——3次/60s=50% 过易 trip | 已解决 | 原审计描述的是一版已经被修掉的错误公式。 | 当前 `src/platform/model-gateway/provider-registry/circuit-breaker.ts` 的 `getRecentFailureRate()` 已按窗口内 `recentFailures / (recentFailures + recentSuccesses)` 计算真实失败率；`tests/unit/platform/model-gateway/provider-registry/circuit-breaker.test.ts` 也直接覆盖了 `25% / 50% / >50%` 的阈值行为。 | 不改生产代码，仅回写现行失败率算法依据。 |
| R22-47 | §111 Event Bus / State / Execution 深层缺陷 | P1 | `failure-classification.ts` | 用 L1/L2/L3 而非 spec 的 transient/permanent/unknown | 已解决（源码修复并补定向测试） | 根因不只是名称陈旧，而是恢复分类把内部 `L1/L2/L3` 直接暴露成对外 contract，导致 `shouldEscalate()`、测试和诊断文案都围绕 legacy 桶名耦合；顺手复核时又发现若干真实匹配优先级问题（如 `E7_LockTimeout`、`typeMismatch`、`schema_migration`、`security_policy_violation`）会被错误分类。 | 当前 `src/platform/five-plane-execution/recovery/failure-classification.ts` 已把 `FailureLevel` 收敛为 `transient/permanent/unknown`，同时保留 `legacyLevel` 兼容历史证据；`shouldEscalate()` 已改按 canonical 等级决策，并修正 camelCase/优先级误分类。定向回归位于 `tests/unit/platform/execution/recovery/failure-classification.test.ts`。 | 已改生产代码与定向测试，修的是 contract 与分类逻辑本身。 |
| R22-48 | §111 Event Bus / State / Execution 深层缺陷 | P1 | `execution-lease-service-async.ts (releaseLeaseSync)` | 不检查 lease.status !== "active"——可释放已释放的 lease | 已解决 | 原审计没有复核 release path 已补的活动态与过期双重校验。 | 当前 `src/platform/five-plane-execution/lease/execution-lease-service-async.ts` 的 `releaseLeaseSync()` 会先检查 `lease.status !== "active"` 并阻断，再做过期校验与事务内二次 re-check，避免释放已释放/已失效 lease。 | 不改生产代码，仅回写现行 lease release 门禁依据。 |
| R22-49 | §111 Event Bus / State / Execution 深层缺陷 | P2 | `durable-event-bus.ts (DLQ)` | 标记 "failed" 非 "dead_lettered"——不与 DlqService 集成 | 已解决（源码修复并补定向测试） | 这条是本轮确认的真实缺口：旧 `deliverOneWithResult()` 在耗尽后只把 ack 标成 `failed`，虽然会调用 `DlqService.enqueue()`，但主 ack 状态并没有进入 terminal `dead_lettered`。 | 本轮已把 `src/platform/five-plane-state-evidence/events/durable-event-bus.ts` 改为在重试耗尽后调用 `markEventDeadLettered()`，同时保留 `DlqService.enqueue()`；`tests/unit/platform/state-evidence/events/durable-event-bus.test.ts` 已验证 ack 终态为 `dead_lettered`。 | 这是实修 DLQ 终态语义与集成。 |
| R22-50 | §111 Event Bus / State / Execution 深层缺陷 | P2 | `dlq-service.ts (scheduleRetry)` | 不检查 maxRetries 即自增 retryCount——可超限重试 | 已解决（源码修复并补定向测试） | 这条也是确认无误的真实缺口：旧 `scheduleRetry()` 只做 delay 合法性校验，达到 `maxRetries` 后仍会继续累加 `retryCount`。 | 本轮已在 `src/platform/five-plane-state-evidence/events/dlq-service.ts` 的 `scheduleRetry()` 中补上 `maxRetries` 与 terminal status 校验；`tests/unit/platform/state-evidence/events/dlq-service.test.ts` 已新增第 6 次重试抛错的定向回归。 | 这是实修重试预算门禁。 |
| R22-51 | §111 Event Bus / State / Execution 深层缺陷 | P2 | `layered-event-inbox.ts (drain)` | Cursor 跳过 filtered-out events——limit 满时消费者可能错过相关事件 | 已解决 | 原审计把 `drain()` 当成“带动态过滤条件的查询 cursor”来看，但当前实现的过滤条件是按 consumer `kind` 固定下来的命名空间分流，不是同一 consumer 会变化的临时 filter。 | `src/platform/five-plane-state-evidence/events/layered-event-inbox.ts` 的 `canConsumerReceive()` 只依赖已注册 consumer 的固定 `kind`；`drain()` 虽会跳过当前 consumer 永远不可能接收的事件，但不会跳过其后仍可接收的事件。`tests/unit/platform/state-evidence/events/layered-event-inbox.test.ts` 已覆盖 truth/projection split。 | 不改生产代码，仅回写“固定分流而非动态过滤”的现行语义。 |
| R22-52 | §111 Event Bus / State / Execution 深层缺陷 | P2 | `state-transition-machine.ts (assertTransition)` | 允许 no-op(current===next)——与 RSM 行为不一致 | 已解决 | 原审计基于早期状态机实现；当前 no-op 已被显式拒绝。 | 当前 `src/platform/five-plane-execution/state-transition/state-transition-machine.ts` 在 `current === next` 时直接抛出 `WorkflowStateError(...noop_transition_denied)`，与 runtime state machine 的 no-op deny 语义一致。 | 不改生产代码，仅回写现行 no-op guard 依据。 |
| R22-53 | §112 UI Feature Packages 缺陷 | P0 | `features/task-cockpit/web/index.tsx:47-53` | 缺 pause/cancel 按钮——spec 要求 pause/resume/cancel/retry | 已解决（源码/测试） | 根因不是整组 UI 产品化不可收敛，而是 `TaskCockpitVm` 已有执行控制语义后，web 视图长期只接了 takeover/resume/escalate，漏接 pause/cancel/retry 这组动作。 | 当前 `ui/packages/features/task-cockpit/src/web/index.tsx` 已渲染 `Pause/Cancel/Retry/Resume/Supervised Resume` 控件并调用真实 VM 动作；`ui/tests/unit/ui/packages/features/task-cockpit/web.test.tsx` 与 `hooks.test.tsx` 已验证按钮接线和后端 `updateTask(...)` 调用。 | 定向验证：`vitest` 该组 task-cockpit hook/web 测试通过。 |
| R22-54 | §112 UI Feature Packages 缺陷 | P0 | `features/task-cockpit/web/index.tsx` | 无 resource usage 面板 | 已解决（源码/测试） | 根因是 `TaskDTO` 只有任务状态字段，没有为 cockpit 暴露资源画像，web 视图也就无从渲染资源面板。 | 本轮在 `ui/packages/shared/types/src/index.ts` 为 `TaskDTO` 增加 `resourceUsage`，在 `ui/packages/shared/api-client/src/mock-data.ts` 提供 CPU/Memory/Runtime 样例，并在 `ui/packages/features/task-cockpit/src/web/index.tsx` 渲染资源使用 `KeyValueTable`；`ui/tests/unit/ui/packages/features/task-cockpit/web.test.tsx` 已覆盖 `CPU/Memory` 展示。 | 关闭本项以现行 DTO、mock 数据和 web 回归测试为依据。 |
| R22-55 | §112 UI Feature Packages 缺陷 | P0 | `features/workflow-cockpit/web/index.tsx:34` | 无 DAG 可视化——label 说"DAG"但渲染 flat KeyValueTable | 已解决（源码/测试） | 根因是 workflow cockpit 早期只展示元信息表格，没有把步骤阶段映射成图形化 DAG 视图。 | 当前 `ui/packages/features/workflow-cockpit/src/web/index.tsx` 已接入 `DAGViewer`，`ui/packages/features/workflow-cockpit/src/web/dag-viewer.tsx` 又进一步调用共享 `DAGVisualization` 渲染阶段与步骤；`ui/tests/unit/ui/packages/features/workflow-cockpit/web.test.tsx` 已验证 DAG 视图和审批/证据侧栏同时渲染。 | 关闭的是“完全没有 DAG 可视化”缺口，不外推为所有高级拓扑语义都已完备。 |
| R22-56 | §112 UI Feature Packages 缺陷 | P0 | `features/workflow-cockpit/web/index.tsx` | 缺 parallel branch display 和 error highlighting | 部分解决 | 根因已拆清两层：错误高亮此前只是没有可视化组件；并行分支则是上游 `WorkflowDTO` 只有扁平 `steps[]`，没有 branch/edge 元数据，UI 无法真实还原并行拓扑。 | 当前 `ui/packages/features/workflow-cockpit/src/web/dag-viewer.tsx` 已按步骤状态着色，`failed` 会映射为 danger 色并在 `DAGVisualization` 中体现；但现有 DTO 仍不能表达 fan-out/fan-in 分支，因此只能按阶段聚合展示。 | 本轮只关闭 error-highlighting 子问题；parallel branch display 仍待上游工作流图模型补齐。 |
| R22-57 | §112 UI Feature Packages 缺陷 | P0 | `features/hitl/hooks/index.ts:5-12` | 静态 stub——返回硬编码3项,缺 inline approval/bulk ops/SLA countdown/escalation display | 部分解决 | 根因不是“仍然只有 3 个硬编码项”，而是复审沿用了旧 stub 认知；现行缺口主要收缩为 HITL 展示层能力不够全。 | 当前 `ui/packages/features/hitl/src/hooks/index.ts` 已通过 `fetchApprovals` + WebSocket 订阅构建真实队列，并接通 `approve/reject/edit/escalate/defer/delegate/resume/patch/override`；`ui/tests/unit/ui/packages/features/hitl/hooks.test.tsx` 已验证这些动作走真实 API helper。 | 仍未关闭 bulk ops、专门 SLA 倒计时展示和完整 escalation 面板，因此保留为部分解决。 |
| R22-58 | §112 UI Feature Packages 缺陷 | P0 | `features/approval/hooks/index.ts` | 缺多级审批/timeout 处理/batch approve-reject——仅支持单级 approve/reject/delegate | 部分解决 | 根因是 approval center 原实现只把审批当单条队列项处理；本轮能修的是单级动作闭环和队列状态一致性，不能凭空补出多级/批量基础设施。 | 当前 `ui/packages/features/approval/src/hooks/index.ts` 已接通 `approve/reject/delegate/requestMoreContext`，且 delegate 会把当前项移出 actionable queue 并切换到下一项；`ui/tests/unit/ui/packages/features/approval/hooks.test.tsx` 已覆盖这些行为。 | 多级审批链、超时策略和 batch approve/reject 仍缺后端模型与 UI 编排，不能虚写完成。 |
| R22-59 | §112 UI Feature Packages 缺陷 | P0 | `features/conversation/web/index.tsx` | 缺 streaming responses/code syntax highlighting/file attachments | 部分解决 | 根因是 conversation web 之前只把消息扔进 `ListCard`，既不区分代码块，也没有附件队列或 streaming 状态露出。 | 本轮在 `ui/packages/features/conversation/src/web/index.tsx` 增加了 streaming 状态显示、代码块渲染和文件附件列表；`ui/packages/features/conversation/src/hooks/index.ts` 增加本地附件队列；`ui/tests/unit/ui/packages/features/conversation/web.test.tsx` 已验证代码块、附件和按钮接线。 | 仍未实现后端附件上传链路和 token-delta 级流式回显，因此保持“部分解决”。 |
| R22-60 | §112 UI Feature Packages 缺陷 | P0 | `features/governance-compliance/hooks/index.ts:5-12` | 静态 stub——缺 policy editor/approval queue/audit log/compliance dashboard | 部分解决 | 根因是该 feature 过去只有摘要卡片，没有真正把 policy/audit/exception 三条治理子面板落到 web。 | 当前 `ui/packages/features/governance-compliance/src/hooks/index.ts` 已接通 policy、auditTrail、exceptionQueue 数据，`ui/packages/features/governance-compliance/src/web/index.tsx` 已渲染 `Policy Editor / Audit Trail / Exception Management`；`ui/tests/unit/ui/packages/features/governance-compliance/web.test.tsx` 已验证展示和动作。 | 但完整 approval queue 与更深的 compliance dashboard 仍未实现，因此只能下调为部分解决。 |
| R22-61 | §112 UI Feature Packages 缺陷 | P1 | `features/analytics/web/index.tsx` | 缺 time-range selector/drill-down/export/pie chart | 已解决（源码/测试） | 根因是 analytics 页面长期停留在 KPI scaffold，只渲染 trend surface，没有把 hook 已具备的 date range、breakdown、export 能力真正暴露到 web。 | 当前 `ui/packages/features/analytics/src/web/index.tsx` 已补入 `Start/End Date`、`Export CSV/JSON`、breakdown drill-down 按钮、`PieChart` 与 `TimelineChart`；`ui/tests/unit/ui/packages/features/analytics/hooks.test.tsx` 与 `web.test.tsx` 已分别验证导出链路和 drill-down/pie 渲染。 | 本项以现行 hook/web 实现和定向测试为关闭依据。 |
| R22-62 | §112 UI Feature Packages 缺陷 | P1 | `features/task-cockpit/hooks/index.ts:5-15` | VM 无 retry 动作方法 | 已解决（源码/测试） | 根因是 task cockpit 之前只有 takeover/resume/escalate 路径，缺少重试闭环。 | 当前 `ui/packages/features/task-cockpit/src/hooks/index.ts` 已实现 `retryTask()` 并调用 `updateTask(..., { status: "queued", currentStep: "retry_requested" })`；`ui/tests/unit/ui/packages/features/task-cockpit/hooks.test.tsx` 已覆盖该动作。 | 关闭本项以 hooks 定向测试为依据。 |
| R22-63 | §112 UI Feature Packages 缺陷 | P1 | `features/workflow-cockpit/hooks/index.ts:5-16` | VM 无 cancel 动作 | 已解决（源码/测试） | 根因是 workflow cockpit 只有 pause/resume/recover/release 四种控制，没有把“终止当前运行”封装成 VM 能力。 | 本轮在 `ui/packages/shared/api-client/src/endpoints.ts` 增加 `cancelWorkflow()`，通过 `mode: "abort"` 走既有恢复控制端点；`ui/packages/features/workflow-cockpit/src/hooks/index.ts` 与 `web/index.tsx` 已接入 `cancelWorkflow()` 和 `Cancel` 按钮；对应 hook/web 测试均已通过。 | 关闭本项以新增 API helper、VM 接线和 web 控件证据为依据。 |
| R22-64 | §112 UI Feature Packages 缺陷 | P1 | `features/takeover/hooks/index.ts:1-13` | 静态 stub——无查询/状态变更/实际接管功能 | 已解决（源码/测试） | 根因是原条目引用的是 takeover 早期静态摘要实现。 | 当前 `ui/packages/features/takeover/src/hooks/index.ts` 已具备快照捕获、所有权转移、历史持久化与恢复能力；`ui/tests/unit/ui/packages/features/takeover/hooks.test.tsx` 已覆盖 claim/transfer/restore 语义。 | 该条按现行 hook 实现与定向测试关闭。 |
| R22-65 | §112 UI Feature Packages 缺陷 | P1 | `tests/features/structure.test.ts:10` | 期望28个包但 registry.test.ts 期望27个——计数不匹配 | 已解决（源码/测试） | 根因是 feature 目录实际数目和结构测试常量长期漂移，且 `feature-flags` 还缺 `mobile/index.ts`。 | 当前 `ui/tests/features/structure.test.ts` 已按真实包数校正为 `29`，同时补入 `ui/packages/features/feature-flags/src/mobile/index.ts` 并从 `src/index.tsx` 导出；结构测试现已通过。 | 关闭本项以结构测试通过为依据。 |
| R22-66 | §112 UI Feature Packages 缺陷 | P1 | `features/approval/hooks/index.ts:86-97` | delegate 仅追加 history 不修改审批状态 | 已解决（源码/测试） | 根因是 delegate 之前只记录 actionHistory，没有同步移出当前审批项，导致 UI 队列与真实动作语义不一致。 | 当前 `ui/packages/features/approval/src/hooks/index.ts` 的 `delegate()` 会先移除当前项并选中下一项，再调用 `delegateApproval(...)`；`ui/tests/unit/ui/packages/features/approval/hooks.test.tsx` 已验证 delegated item 从 actionable queue 消失。 | 本项已按 hook 行为回归关闭。 |
| R22-67 | §112 UI Feature Packages 缺陷 | P2 | `ui-core/components/index.ts` | 缺 DAGVisualization/TimelineChart/SLACountdown/CodeBlock/FileAttachment/PieChart 组件 | 已解决（源码/测试） | 根因是 `ui-core` 只提供通用 scaffold/card/table，缺少 feature 级复用所需的可视化基元；其中 `CodeBlock` 已有，但其余 5 个组件确实缺失。 | 本轮在 `ui/packages/ui-core/src/components/extended.tsx` 新增 `DAGVisualization`、`TimelineChart`、`SLACountdown`、`FileAttachment`、`PieChart`，并在 `ui/packages/ui-core/src/index.tsx` 统一导出；workflow/analytics/conversation 已开始消费这些组件，相关 web 测试通过。 | 关闭的是共享组件缺失本身；视觉精修和无障碍深挖可另立产品化项。 |
| R22-68 | §112 UI Feature Packages 缺陷 | P2 | `tests/features/` | 无 ViewModel 行为测试——全是结构/doc-alignment 测试,零业务逻辑覆盖 | 已解决（源码/测试） | 根因是旧测试集确实偏结构校验，但后续 feature hook/web 回归已经陆续补上，reaudit 没有把这些新增测试吸收进结论。 | 当前 `ui/tests/unit/ui/packages/features/` 已有 task-cockpit、workflow-cockpit、approval、analytics、conversation、hitl、governance-compliance、takeover 等 hook/web 行为测试；本轮定向执行 `12 files / 27 tests passed`。 | 因为已经存在稳定的行为回归集，本项不再成立。 |
| R23-01 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §114 NL Gateway / Interaction / Autonomy 缺陷 | P0 | `nl-gateway/index.ts:722-762` | 未确认高风险 TaskSpec 即生成 RequestEnvelope——§39.6 要求仅 confirmed 才可 dispatch | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R23-02 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §114 NL Gateway / Interaction / Autonomy 缺陷 | P0 | `proactive-agent/index.ts:316-322` | medium/high risk 允许 auto_execute——§41.1 要求 medium+ 走 suggestion mode | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R23-03 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §114 NL Gateway / Interaction / Autonomy 缺陷 | P0 | `autonomy/level-manager/index.ts:3-9` | AUTONOMY_LEVEL_ORDER frozen 在 full_auto 后——frozen 被视为更高级别导致降级判定为升级 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R23-04 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §114 NL Gateway / Interaction / Autonomy 缺陷 | P0 | `goal-decomposer/index.ts:326-349` | 检测到循环依赖仅 warning 不拒绝——§40.2 要求环即拒绝报错 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R23-05 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §114 NL Gateway / Interaction / Autonomy 缺陷 | P1 | `proactive-agent/index.ts:249-331` | evaluate() 无 trust score/autonomy level 门控——§42.5 要求 semi_auto+ 才允许触发 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R23-06 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §114 NL Gateway / Interaction / Autonomy 缺陷 | P1 | `autonomy/promotion-engine/index.ts:14-38` | shouldPromote=true 无审批——§42.2 要求 domain_owner/platform_team 审批 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R23-07 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §114 NL Gateway / Interaction / Autonomy 缺陷 | P1 | `autonomy/index.ts:214-215` | P0 incident 设 "frozen" 而非 spec 的 "suggestion" | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R23-08 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §114 NL Gateway / Interaction / Autonomy 缺陷 | P1 | `autonomy/index.ts:218-228` | P2/P3 incident 也 freeze——spec 仅 P0/P1 触发降级 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R23-09 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §114 NL Gateway / Interaction / Autonomy 缺陷 | P1 | `dashboard/dashboard-websocket-server.ts:106-140` | registerClient 无 auth/tenantId——WS 订阅无租户过滤 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R23-10 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §114 NL Gateway / Interaction / Autonomy 缺陷 | P1 | `nl-gateway/nl-gateway-config-loader.ts:69-70` | 置信阈值 0.7 vs spec 要求 0.80——0.7-0.79 绕过澄清 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R23-11 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §114 NL Gateway / Interaction / Autonomy 缺陷 | P1 | `goal-decomposer/llm-plan-generator.ts:48-77` | LLM 计划仅 shape 验证——缺 DAG/capability/risk/budget propagation 验证 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R23-12 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §114 NL Gateway / Interaction / Autonomy 缺陷 | P1 | `proactive-agent/index.ts:416-421` | detectFeedbackLoop 报告的 triggerIds 不完整(DFS stack 被 backtrack 清空) | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R23-13 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §114 NL Gateway / Interaction / Autonomy 缺陷 | P2 | `nl-gateway/index.ts:1-2` | wildcard re-export shadows named export detectAmbiguity——外部消费者不可达 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R23-14 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §114 NL Gateway / Interaction / Autonomy 缺陷 | P2 | `dashboard/dashboard-websocket-server.ts:330-346` | heartbeat timeout 设 isConnected=false 但不从 map 移除——stale 无限累积 | 已解决（与 R11-29/2040 同源） | 该条与 `R11-29`、`2040` 指向同一 heartbeat 清理缺陷；现行实现已统一改为对超时 client 执行正式 unregister，而不是仅打断开标记。 | `src/interaction/dashboard/dashboard-websocket-server.ts` 的 `performHeartbeat()` 已调用 `unregisterClient()`，对应单测 `heartbeat unregisters timed out clients (issue #2040 fixed)`、`performHeartbeat removes timed out connections from the registry`、`heartbeat cleanup removes timed out clients from counts and routing` 已覆盖。 | 重复挂账一并关闭，避免同一问题在 reaudit 中状态不一致。 |
| R23-15 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §114 NL Gateway / Interaction / Autonomy 缺陷 | P2 | `ux/ux-event-tracking-service.ts:122` | 硬编码 eventType "test:many_events" as any 泄入生产 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R23-16 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §114 NL Gateway / Interaction / Autonomy 缺陷 | P2 | `ux/conversation-history-service.ts:235-237` | listUserSessions 无 tenant 过滤——读全部租户后客户端过滤 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R23-17 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §114 NL Gateway / Interaction / Autonomy 缺陷 | P2 | `autonomy/index.ts:280-291` | Trust score 用全量累计非 90d 滑动窗口 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R23-18 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §114 NL Gateway / Interaction / Autonomy 缺陷 | P2 | `goal-decomposer/index.ts:343` | depthUsed 永远0——无递归子分解,层次化分解不实际工作 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R23-19 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §114 NL Gateway / Interaction / Autonomy 缺陷 | P2 | `nl-gateway/index.ts:463-478` | 低风险任务跳过 "Confirming" 直达 "Executing"——违反状态机 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R23-20 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §115 IAM / Security / Sandbox 缺陷 | P0 | `compliance/encryption/index.ts:61-62` | protectValue 用 base64url 编码伪装加密——零机密性,任何人可 decode | 已解决 | 原问题属于旧实现快照；现行字段保护已使用 AES-256-GCM 真加密，并非可直接 decode 的 base64 包装。 | 当前 `src/platform/compliance/encryption/index.ts` 通过 `createCipheriv("aes-256-gcm", ...)` 生成 `enc:<fingerprint>:<iv>:<tag>:<ciphertext>`；`tests/unit/platform/reaudit-r23-security-20-32.test.ts` 已验证密文不等于明文 base64 且可同 key 正常解密。 | 回写现行加密实现与定向回归证据。 |
| R23-21 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §115 IAM / Security / Sandbox 缺陷 | P0 | `gcp-secret-manager-http-secret-provider.ts:184` | extractSecretName 取 parts[-1] 得到 version 而非 secret name——获取错误密钥 | 已解决 | 原问题属于真实解析缺陷；旧逻辑会把 version 当成 secret name，导致访问错误资源。 | 当前 `src/platform/control-plane/iam/gcp-secret-manager-http-secret-provider.ts` 已重写 `extractSecretName()`，正确解析 `secret://name` 与 `secret://name/versions/x`；`tests/unit/platform/reaudit-r23-security-20-32.test.ts` 已验证请求路径命中 `/secrets/my-secret/versions/latest:access`。 | 关闭该条 GCP secret name 解析缺口。 |
| R23-22 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §115 IAM / Security / Sandbox 缺陷 | P1 | `web-fetch.ts:141-165` | 无协议限制——file:///data:///ftp: 绕过 SSRF 防护 | 已解决 | 该条在现行代码中已不存在；outbound URL policy 只允许 `http:`/`https:`，非支持协议会直接按内网/非法出站拒绝。 | 当前 `src/platform/execution/tool-executor/outbound-url-policy.ts` 的 `isSupportedOutboundProtocol()` 仅接受 HTTP(S)，`web-fetch` 会在执行前调用；`tests/unit/platform/reaudit-r23-security-20-32.test.ts` 已验证 `file:///etc/passwd` 被阻断。 | 回写现行协议约束与定向测试依据。 |
| R23-23 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §115 IAM / Security / Sandbox 缺陷 | P1 | `web-fetch.ts:156` | DNS rebinding——解析时检查 IP 但 fetch 时 DNS 可指向内网 | 已解决 | 原问题属真实防护缺口；旧实现只在 URL/hostname 层做静态校验，没有在实际请求前做解析拦截。 | 当前 `src/platform/execution/tool-executor/web-fetch.ts` 已新增 DNS preflight，若 hostname 解析到内网/保留地址即返回 `DNS_REBINDING_BLOCKED`；`tests/unit/platform/reaudit-r23-security-20-32.test.ts` 已验证解析到 `127.0.0.1` 的域名会 fail-close。 | 关闭该条 DNS rebinding 防护缺口。 |
| R23-24 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §115 IAM / Security / Sandbox 缺陷 | P1 | `aws-kms-http-secret-provider.ts:128-133` | SigV4 credential scope 缺 date prefix——签名永远失败,AWS KMS 不可用 | 已解决 | 原问题属于真实签名错误；缺失 date scope 会导致 AWS KMS SigV4 请求不可验证。 | 当前 `src/platform/control-plane/iam/aws-kms-http-secret-provider.ts` 的 `buildStringToSign()` 已改为 `${date}/${region}/${service}/aws4_request`；`tests/unit/platform/reaudit-r23-security-20-32.test.ts` 已验证 string-to-sign 包含日期 scope。 | 关闭该条 KMS SigV4 scope 缺口。 |
| R23-25 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §115 IAM / Security / Sandbox 缺陷 | P1 | `aws-kms-http-secret-provider.ts:356` | KMS Decrypt 发 DynamoDB 格式 CiphertextBlob——KMS JSON API 期望 base64 string | 已解决 | 原问题属于真实协议格式错误；旧实现发送 `{ B: [...] }`，与 KMS JSON API 不兼容。 | 当前 `src/platform/control-plane/iam/aws-kms-http-secret-provider.ts` 已将 `CiphertextBlob` 改为 base64 string，并兼容解析 string / legacy `{ B: [...] }` 的 `Plaintext`；`tests/unit/platform/reaudit-r23-security-20-32.test.ts` 已验证请求体使用 base64 `CiphertextBlob`。 | 关闭该条 KMS decrypt payload 缺口。 |
| R23-26 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §115 IAM / Security / Sandbox 缺陷 | P1 | `gcp-secret-manager-http-secret-provider.ts:211` | URL 用未清洗的 project/secret/version——path traversal 可访问越权密钥 | 已解决 | 原问题属于真实输入校验缺失；旧实现未限制 secret/version 资源段，可被 `..` 等片段污染。 | 当前 `src/platform/control-plane/iam/gcp-secret-manager-http-secret-provider.ts` 已增加 `requireSafeSegment()` 约束 secret/version 片段；`tests/unit/platform/reaudit-r23-security-20-32.test.ts` 已验证 `secret://../evil/versions/latest` 会被拒绝。 | 关闭该条 GCP path traversal 缺口。 |
| R23-27 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §115 IAM / Security / Sandbox 缺陷 | P1 | `shadow-snapshot-service.ts:197,431` | gitBinary 选项接受任意路径无验证——可执行任意二进制 | 已解决 | 原问题属于真实命令注入面；旧实现允许任意 `gitBinary` 路径进入执行链路。 | 当前 `src/platform/execution/tool-executor/shadow-snapshot-service.ts` 已新增 `validateGitBinary()`，仅允许 `"git"` 或受信前缀下 basename 为 `git/git.exe` 的路径；`tests/unit/platform/reaudit-r23-security-20-32.test.ts` 已验证恶意覆盖会抛拒绝。 | 关闭该条任意二进制执行缺口。 |
| R23-28 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §115 IAM / Security / Sandbox 缺陷 | P1 | `web-search.ts:117` | extractSearchResults 中 new URL(url) 对畸形 URL 抛异常未捕获——crash search tool | 已解决 | 原问题属于真实鲁棒性缺陷；旧实现对畸形 URL/转义串缺少容错，会直接抛异常中断搜索。 | 当前 `src/platform/execution/tool-executor/web-search.ts` 已对 `decodeURIComponent()` 与 `new URL()` 包裹容错并跳过坏结果；`tests/unit/platform/reaudit-r23-security-20-32.test.ts` 已验证 malformed URL 会被忽略而非 crash。 | 关闭该条 search result 解析崩溃缺口。 |
| R23-29 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §115 IAM / Security / Sandbox 缺陷 | P1 | `compliance/encryption/index.ts:65-66` | Key fingerprint 仅 48bits(12 hex)——碰撞风险导致错误 keyRef 解密 | 已解决 | 原问题属于真实碰撞面；旧实现指纹长度过短，存在不同 keyRef 误匹配风险。 | 当前 `src/platform/compliance/encryption/index.ts` 已将 fingerprint 默认提升到 32 hex，并在 `revealField()` 中按密文内实际长度校验以兼容旧密文；`tests/unit/platform/reaudit-r23-security-20-32.test.ts` 已验证新密文 fingerprint 长度不少于 32。 | 回写现行 fingerprint 强化与兼容性证据。 |
| R23-30 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §115 IAM / Security / Sandbox 缺陷 | P2 | `skill-governance-service.ts:409` | LIKE 模式注入——user tag 中%和\_未转义 | 已解决 | 原问题属于真实查询拼接缺陷；旧实现直接把用户 tag 放入 `LIKE` 模式，`%`/`_` 可扩大匹配范围。 | 当前 `src/platform/execution/tool-executor/skill-governance-service.ts` 已增加 `escapeLikePattern()` 并使用 `LIKE ? ESCAPE '\\'`；`tests/unit/platform/reaudit-r23-security-20-32.test.ts` 已验证 `ops%_core` 会生成转义参数 `%ops\\%\\_core%`。 | 关闭该条 LIKE wildcard 注入缺口。 |
| R23-31 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §115 IAM / Security / Sandbox 缺陷 | P2 | `edit-snapshot-service.ts:37-38` | 无界内存 Map(edit history)——长会话 OOM | 已解决 | 原问题属于真实资源控制缺失；旧实现对 step 历史与会话总量均无上界。 | 当前 `src/platform/execution/tool-executor/edit-snapshot-service.ts` 已新增 `MAX_HISTORY_STEPS=100`、`MAX_EDITS_PER_STEP=200` 与 LRU 式 `stepTouchOrder` 淘汰；`tests/unit/platform/reaudit-r23-security-20-32.test.ts` 已验证单 step 和全 session 历史都会被裁剪。 | 关闭该条 edit history 无界增长缺口。 |
| R23-32 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §115 IAM / Security / Sandbox 缺陷 | P2 | `lineage/index.ts:62` | cloneMetadata shallow clone——嵌套对象共享引用可变异历史 | 已解决 | 原问题属于真实历史污染缺陷；浅拷贝会让嵌套 metadata 与外部对象共享引用。 | 当前 `src/platform/compliance/lineage/index.ts` 已将 `cloneMetadata()` 改为 `structuredClone(metadata)`；`tests/unit/platform/reaudit-r23-security-20-32.test.ts` 已验证外部修改不会回写到已记录 lineage edge。 | 关闭该条 lineage metadata 浅拷贝缺口。 |
| R23-33 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §116 Harness / Prompt Engine / Learn / Improve 缺陷 | P0 | `harness/hitl-runtime.ts:3` | HITL 仅 approved/rejected——缺 edit/delegate/escalate(5能力中缺3) | 已解决 | 原缺口属真实实现缺失；HITL runtime 只有 inspect/patch/override/takeover/resolve/pause/resume，缺少审计要求的 `edit/delegate/escalate` 操作面。 | 当前 `src/platform/orchestration/harness/hitl-runtime.ts` 已新增 `HitlMode` 的 `edit/delegate/escalate` 以及对应执行方法、审计记录与状态落盘；`tests/unit/platform/orchestration/reaudit-r23-harness-learn-regressions.test.ts` 已定向验证三种能力都可执行并生成责任记录。 | 关闭该条 HITL 能力缺口。 |
| R23-34 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §116 Harness / Prompt Engine / Learn / Improve 缺陷 | P0 | `harness/hitl-runtime.ts:41` | resolve() 无幂等/双重解析防护——已解析请求可重复变更 | 已解决 | 该条已被现行实现修复，但此前 reaudit 缺少回归证据。 | `src/platform/orchestration/harness/hitl-runtime.ts` 现有 `request.status !== "pending_approval"` 的 fail-closed 防护，二次 resolve 会抛出 `harness.hitl.request_already_resolved`；`tests/unit/platform/orchestration/reaudit-r23-harness-learn-regressions.test.ts` 已新增双重解析拒绝回归。 | 回写现行源码与定向测试依据。 |
| R23-35 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §116 Harness / Prompt Engine / Learn / Improve 缺陷 | P0 | `harness/recovery-controller.ts:4` | 无 retry budget——无计数/退避/耗尽检查,可无限重试 | 已解决 | 该条属于审计结论滞后；现行 `RecoveryController` 已具备重试计数、退避和耗尽后升级 HITL 的 fail-closed 路径。 | `src/platform/orchestration/harness/recovery-controller.ts` 已定义 `RETRY_MAX_ATTEMPTS`、指数退避和 exhaustion 分支；`tests/unit/platform/orchestration/reaudit-r23-harness-learn-regressions.test.ts` 已验证 `llm_provider_unavailable/worker_crash` 在预算耗尽后都会转入 HITL，不会无限重试。 | 回写现行实现与回归证据。 |
| R23-36 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §116 Harness / Prompt Engine / Learn / Improve 缺陷 | P0 | `harness/recovery-controller.ts:26-27` | tool_timeout 立即 resume 无记录——违反 INV-STATE-001 证据追踪 | 已解决 | 该条同样是旧结论残留；现行 `tool_timeout` 已先记录恢复事件，再进入 sleep/retry。 | `src/platform/orchestration/harness/recovery-controller.ts` 当前会在 `tool_timeout` 路径发出 `recovery:repair_applied` 事件，并通过 `runtime.sleep(...)` 写入 retry lease；`tests/unit/platform/orchestration/reaudit-r23-harness-learn-regressions.test.ts` 已验证事件落地与 paused/sleep 结果。 | 回写现行事件追踪与定向测试依据。 |
| R23-37 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §116 Harness / Prompt Engine / Learn / Improve 缺陷 | P0 | `learn/learning-object-model.ts:13` | 缺 quarantine 生命周期——无 untrusted/validating/quarantined 状态 | 已解决 | 原问题属真实生命周期缺口；旧 schema 只有 `draft/quarantine/validated/promoted/retired`，无法表达验证前和隔离中的细化状态。 | 当前 `src/platform/orchestration/learn/learning-object-model.ts` 与镜像 `src/platform/five-plane-orchestration/learn/learning-object-model.ts` 已新增 `untrusted/validating/quarantined`，并保留 legacy `quarantine` 兼容归一化；`tests/unit/platform/orchestration/reaudit-r23-harness-learn-regressions.test.ts` 已验证默认 `untrusted`、验证后 `validated` 和 legacy `quarantine -> quarantined`。 | 关闭该条学习对象生命周期缺口。 |
| R23-38 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §116 Harness / Prompt Engine / Learn / Improve 缺陷 | P0 | `learn/learning-object-validator.ts:24-61` | 无 PII/taint 检查——仅验证 evidence count 和 confidence floor | 已解决 | 该条已被现行 validator 推翻，但此前缺少重新核验后的证据。 | `src/platform/orchestration/learn/learning-object-validator.ts` 与镜像 `src/platform/five-plane-orchestration/learn/learning-object-validator.ts` 已执行 PII/secret 扫描、去重污染检查并把违规对象转入 `quarantined`；`tests/unit/platform/orchestration/reaudit-r23-harness-learn-regressions.test.ts` 已验证含邮箱/SSN/secret 的对象会被拒绝并隔离。 | 回写现行 taint/PII 防护与定向测试依据。 |
| R23-39 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §116 Harness / Prompt Engine / Learn / Improve 缺陷 | P1 | `harness/index.ts:39-55` | HarnessRunStatus 缺 cancelled(有 aborted 但语义不同) | 已解决 | 原问题属真实 contract/status 缺口；canonical `HarnessRunStatus` 与状态机都没有 `cancelled`，导致人工拒绝只能混用 `aborted`。 | 当前 `src/platform/contracts/executable-contracts/index.ts`、`src/platform/contracts/executable-contracts/schemas.ts`、`src/platform/five-plane-execution/runtime-state-machine.ts` 已补上 `cancelled`，`src/platform/orchestration/harness/index.ts` 也将 HITL reject 映射为 `cancelled` 并按 terminal 语义处理；`tests/unit/platform/orchestration/reaudit-r23-harness-learn-regressions.test.ts` 已验证 terminal status 集合和 reject->cancelled 路径。 | 关闭该条 cancelled 语义缺口。 |
| R23-40 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §116 Harness / Prompt Engine / Learn / Improve 缺陷 | P1 | `guardrail-vibration-breaker.ts:34` | 仅计连续相同 signature——交替签名永不 trip;无时间窗口 | 已解决 | 原问题属真实实现缺陷；旧 breaker 只有单计数器，没有 observation window，交替签名不会在窗口内累积振荡。 | 当前 `src/platform/orchestration/harness/guardrails/guardrail-vibration-breaker.ts` 已新增 windowed `recentSignals` 追踪、`observationWindowMs` 和 cooldown 期间 reset/proceed 清理逻辑；`tests/unit/platform/orchestration/reaudit-r23-harness-learn-regressions.test.ts` 已验证交替 signature 会在窗口内触发 cooldown。 | 关闭该条振荡检测缺口。 |
| R23-41 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §116 Harness / Prompt Engine / Learn / Improve 缺陷 | P1 | `guardrail-vibration-breaker.ts:19-48` | VibrationBreaker 从未集成到 GuardrailEngine/HarnessRuntimeService——死代码 | 已解决 | 该条不只是补证据；本轮同时把 vibration state 收敛为按 `runId` 隔离，避免跨 run 污染。 | `src/platform/orchestration/harness/index.ts` 现已在 `runLoop()` 中对非 `proceed` guardrail action 调用 `vibrationBreaker.evaluate(...)`，并使用 `vibrationStates` 按 run 保存窗口状态，触发 cooldown 时自动 `openHitlReview("guardrail_vibration_detected", ...)`；`tests/unit/platform/orchestration/reaudit-r23-harness-learn-regressions.test.ts` 已验证 breaker 会真实影响 `HarnessRuntimeService.runLoop` 输出。 | 关闭该条 breaker 死代码结论。 |
| R23-42 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §116 Harness / Prompt Engine / Learn / Improve 缺陷 | P1 | `knowledge-promotion-service.ts:72` | promote() 无审批门——validated 直接 auto-promote | 已解决 | 原问题属真实 bug；旧实现虽然引入了 `requireApproval/approvalGate` 选项，但同步/异步 gate 判定反了，实际并未形成可靠审批门。 | 当前 `src/platform/orchestration/learn/knowledge-promotion-service.ts` 与镜像 `src/platform/five-plane-orchestration/learn/knowledge-promotion-service.ts` 已拆分同步 `promote()` 与异步 `promoteAsync()` 审批路径，未批准时 fail closed；`tests/unit/platform/orchestration/reaudit-r23-harness-learn-regressions.test.ts` 已验证 sync reject、async approve 和 async gate 误用同步入口时的拒绝路径。 | 关闭该条知识晋升审批门缺口。 |
| R23-43 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §116 Harness / Prompt Engine / Learn / Improve 缺陷 | P1 | `improve-rollout/index.ts:18-19` | Rollout levels 非 L0-L5——用 shadow/canary_5/partial_25 等 | 已解决 | 原问题不只是旧审计滞后；`improve-rollout/index.ts` 顶层导出仍停留在 legacy status 常量，没有把现行 L0-L5 canonical levels 暴露为默认编排入口。 | 当前 `src/platform/five-plane-orchestration/improve-rollout/index.ts` 已补上 `CANARY_ROLLOUT_LEVELS=["L1_evaluate","L2_canary"]` 与 `PROGRESSIVE_ROLLOUT_LEVELS=["L2_canary","L3_partial","L4_stable","L5_full"]`，同时保留 legacy status 常量仅作兼容；`tests/unit/platform/five-plane-orchestration/improve-rollout-r23-regressions.test.ts` 已验证 canonical level 导出与 `normalizeRolloutLevel()` 映射。 | 回写 canonical rollout level 入口并补证据。 |
| R23-44 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §116 Harness / Prompt Engine / Learn / Improve 缺陷 | P1 | `improve-rollout/auto-rollback-service.ts:37` | evaluate() 不触发实际 rollback——返回决策但不变更状态 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R23-45 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §116 Harness / Prompt Engine / Learn / Improve 缺陷 | P1 | `improve-rollout/improvement-candidate-registry.ts:17` | 纯内存 Map 无持久化——重启丢失所有候选 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R23-46 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §116 Harness / Prompt Engine / Learn / Improve 缺陷 | P1 | `prompt-engine/rollout/index.ts:6` | PromptRolloutMode 仅3阶段(off/suggest/shadow)——缺 canary/partial/stable | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R23-47 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §116 Harness / Prompt Engine / Learn / Improve 缺陷 | P1 | `prompt-engine/eval/llm-eval-service.ts:356-358` | A/B test 用硬编码 mock scores 0.85/0.90——统计显著性基于伪造数据 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R23-48 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §116 Harness / Prompt Engine / Learn / Improve 缺陷 | P1 | `prompt-engine/registry/hierarchical-registry.ts:50` | 4层(global→domain→pack→task-type) vs spec 3层(platform→domain→task) | 已解决 | 这条并非纯文档滞后：实现虽然已移除 `pack` level 类型，但 register/get 路径仍优先把 `packId` 当成独立 scope key，语义上仍会把 pack 当第四层。 | 当前 `src/platform/prompt-engine/registry/hierarchical-registry-service.ts` 已把 pack 兼容参数收敛为 domain fallback：显式 `domain` 优先于 `packId`，顶层注释与 lookup precedence 也已改成 `global → domain → task-type`；`tests/unit/platform/prompt-engine/registry/hierarchical-registry-service-r23-48.test.ts` 已验证 pack-scoped 注册落在 domain 层级、不会暴露独立 pack-only lookup 层。 | 关闭 pack 语义残留。 |
| R23-49 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §116 Harness / Prompt Engine / Learn / Improve 缺陷 | P2 | `harness/index.ts:315` | startedAt===completedAt(同一 nowIso)——步骤时长永远0 | 已解决 | 原问题属实：`appendStep()` 在无显式 latency 时把 `completedAt` 直接回填成 `startedAt`，导致步骤 duration 恒为 0。 | 当前 `src/platform/five-plane-orchestration/harness/index.ts` 已改为在无 latency 时使用严格晚于 `startedAt` 的完成时间，在有 latency 时也至少推进 1ms；`tests/unit/platform/five-plane-orchestration/harness/step-timing-r23-49.test.ts` 已验证无显式 latency 的步骤仍会记录正时长。 | 修复 step timing 采样。 |
| R23-50 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §116 Harness / Prompt Engine / Learn / Improve 缺陷 | P2 | `harness/hitl-runtime.ts:17-18` | HITL 请求纯内存 Map 无 TTL——pending 永不过期 | 已解决 | 原问题属实：runtime 虽有持久化接口，但 `HitlRequest` 本体没有 expiry，读取/恢复时也不会把过期 pending 自动收敛。 | 当前 `src/platform/five-plane-orchestration/harness/hitl-runtime.ts` 已为请求引入 `expiresAt`、默认 TTL、旧记录归一化和“访问即过期”逻辑；过期 pending 会自动转成 `rejected + resolvedBy=system:hitl_timeout` 并补责任记录。`tests/unit/platform/five-plane-orchestration/harness/hitl-runtime-r23-50.test.ts` 已验证默认 expiry 和过期请求自动收敛。 | 关闭 pending 永不过期缺口。 |
| R23-51 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §116 Harness / Prompt Engine / Learn / Improve 缺陷 | P2 | `improve-rollout/rollout-state-machine.ts:18-29` | 允许自转换(draft→draft)——绕过 guardrails 创建误导审计事件 | 已解决 | 该条是 reaudit 补录滞后。当前 `ROLLOUT_TRANSITIONS` 已不再允许任何 active/self transition，自转换会被状态机直接拒绝。 | `src/platform/five-plane-orchestration/improve-rollout/rollout/rollout-state-machine.ts` 的现行转移表只允许前进、回滚或暂停恢复；`tests/unit/platform/five-plane-orchestration/improve-rollout-r23-regressions.test.ts` 已显式验证 `canary_5 -> canary_5` 会抛 `Invalid rollout transition`。 | 回写现行 guardrail。 |
| R23-52 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §116 Harness / Prompt Engine / Learn / Improve 缺陷 | P2 | `improve-rollout/canary-traffic-router.ts:29-34` | Hash 函数分布偏斜——短 ID 聚集在窄范围 | 已解决 | 原审计指向的是旧的窄范围 bucket 计算；当前实现已切到覆盖更均匀的 djb2-style hash，但 reaudit 没有补充现行分布证据。 | `src/platform/five-plane-orchestration/improve-rollout/canary-traffic-router.ts` 当前 `hashToBucket()` 已非简单累加哈希；`tests/unit/platform/five-plane-orchestration/canary-traffic-router-r23-52.test.ts` 已验证 200 个短 task id 覆盖至少 70 个 bucket 且分布范围超过 80。 | 回写哈希分布回归证据。 |
| R23-53 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §116 Harness / Prompt Engine / Learn / Improve 缺陷 | P2 | `escalation/index.ts:22-50` | EscalationService 无消费者——决策不路由到 HITL/panic controller | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R23-54 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §116 Harness / Prompt Engine / Learn / Improve 缺陷 | P2 | `prompt-injection-guard.ts:328-329` | 输出含任何 URL 即 block——合法模型输出含链接被误拦截 | 已解决 | 原问题此前只被部分修到 raw URL；`markdown_link_exfiltration` 仍会把任意 markdown link 标成 suspicious，导致正常文档链接照样被 block。 | 当前 `src/platform/stability/prompt-injection-guard.ts` 已把 markdown/raw URL 检查都收紧为“凭据上下文或敏感 query param 才拦截”；`tests/unit/platform/stability/prompt-injection-guard-output-inspection.test.ts` 已同时验证 benign raw URL、benign markdown link 允许通过，secret-bearing raw URL 与 markdown link 继续阻断。 | 关闭 URL 误拦截。 |
| R23-55 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §117 Contract 深层偏差（typed_event_bus / sandbox_auth / hitl / cost_budget / platform_panic） | P0 | `typed-event-payloads.ts` | Contract 要求15个 OAPEFLIR payload 类型——全部不存在 | 已解决 | 原审计针对的是旧实现；现行 typed payload 模块已经补齐 observe/assess/plan/execute/feedback/learn/improve/release 各阶段事件载荷，但 reaudit 当时没有把现状和证据回写。 | 当前 `src/platform/five-plane-state-evidence/events/typed-event-payloads.ts` 已导出 `ObserveSignalsCollectedPayload` 等 OAPEFLIR 载荷类型；`tests/unit/platform/five-plane-state-evidence/reaudit-batch-r23-55-60-72-75.test.ts` 已对阶段 payload 类型做现行契约回归。 | 回写 OAPEFLIR payload 现状。 |
| R23-56 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §117 Contract 深层偏差（typed_event_bus / sandbox_auth / hitl / cost_budget / platform_panic） | P0 | `typed-event-bus.ts:32` | 无 `<stage>:<event>` 格式事件(observe:/assess:/plan:/execute:/feedback:/learn:/improve:/release:) | 已解决 | 原问题是旧版 bus 类型映射不完整；现行 `TypedEventPayloadMap` 已覆盖阶段化 `<stage>:<event>` 事件名。 | 当前 `src/platform/five-plane-state-evidence/events/typed-event-bus.ts` 已显式注册 `observe:signals_collected` 到 `release:rollback_triggered` 等阶段事件；同一回归测试已验证其现行可用性。 | 回写阶段事件命名契约。 |
| R23-57 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §117 Contract 深层偏差（typed_event_bus / sandbox_auth / hitl / cost_budget / platform_panic） | P0 | `typed-event-payloads.ts:184` | PluginIsolationEventPayload 缺 phase 字段且名称为 PluginLifecycleEventPayload | 已解决 | 原问题此前只停留在独立接口定义，`plugin:error_isolated` 仍走旧的 `PluginLifecycleEventPayload` 映射。 | 当前 `src/platform/five-plane-state-evidence/events/typed-event-payloads.ts` 已提供带 `phase` 的 `PluginIsolationEventPayload`，且 `src/platform/five-plane-state-evidence/events/typed-event-bus.ts` 已把 `plugin:error_isolated` 切到该类型；回归测试已验证 `phase` 为必需字段。 | 关闭插件隔离 payload 漂移。 |
| R23-58 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §117 Contract 深层偏差（typed_event_bus / sandbox_auth / hitl / cost_budget / platform_panic） | P1 | `sandbox-policy.ts:69-90` | SandboxPolicy 缺 filesystem_rules/network_rules/process_rules/created_at | 已解决 | 该条属于 reaudit 未及时回写现行 IAM contract。 | 当前 `src/platform/five-plane-control-plane/iam/sandbox-policy.ts` 的 `SandboxPolicy` 已包含 `filesystem_rules/network_rules/process_rules/created_at`；`tests/unit/platform/five-plane-state-evidence/reaudit-batch-r23-55-60-72-75.test.ts` 已构造完整策略对象验证字段存在。 | 回写 sandbox policy 契约字段。 |
| R23-59 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §117 Contract 深层偏差（typed_event_bus / sandbox_auth / hitl / cost_budget / platform_panic） | P1 | `(缺失)` | SandboxCapabilityProfile 类型完全不存在 | 已解决 | 现行 IAM 模块已具备能力画像类型，但 reaudit 未补证。 | 当前 `src/platform/five-plane-control-plane/iam/sandbox-policy.ts` 已导出 `SandboxCapabilityProfile`，并覆盖 filesystem/network/process/ipc 四类能力面；同一回归测试已验证对象结构。 | 回写 capability profile 现状。 |
| R23-60 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §117 Contract 深层偏差（typed_event_bus / sandbox_auth / hitl / cost_budget / platform_panic） | P1 | `(缺失)` | AuthSession/AuthProviderBinding 类型不存在——认证会话模型未实现 | 已解决 | 原问题属实：运行时缺少统一认证会话与 provider binding 契约。 | 当前 `src/platform/five-plane-control-plane/iam/sandbox-policy.ts` 已新增 `AuthSession` 与 `AuthProviderBinding`，覆盖 binding、token ref、PKCE 标志与时间戳；回归测试已验证两类对象可按现行 contract 构造。 | 补齐认证会话 contract。 |
| R23-61 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §117 Contract 深层偏差（typed_event_bus / sandbox_auth / hitl / cost_budget / platform_panic） | P1 | `budget-guard.ts:12-18` | BudgetPolicy 用废弃字段(maxTaskCostUsd/maxDailyCostUsd)——contract 已将其降为 billing-only | 已解决 | 原审计针对的是旧预算层级；现行 `BudgetPolicy` 已提供 contract 要求的平台/pack/step 预算字段，但 reaudit 未补当前证据。 | 当前 `src/platform/model-gateway/cost-tracker/budget-guard.ts` 已暴露 `maxPlatformCostUsd/maxPackCostUsd/maxStepCostUsd` 并在 `BudgetGuard` 中实际参与预算判定；`tests/unit/platform/model-gateway/reaudit-batch-r23-61-64-73-74.test.ts` 已验证现行字段生效。 | 回写预算层级契约。 |
| R23-62 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §117 Contract 深层偏差（typed_event_bus / sandbox_auth / hitl / cost_budget / platform_panic） | P1 | `budget-guard.ts:17` | runtime_mode 仅3值 vs contract 8值——缺5种模式 | 已解决 | 该条是审计状态滞后；源码中的 mode 枚举早已扩展，但没有本轮闭环测试。 | 当前 `src/platform/model-gateway/cost-tracker/budget-guard.ts` 的 `BudgetPolicy.mode` 已包含 `supervised/auto/full-auto/preview/simulation/audit/enforcement/learning` 八种模式；回归测试已显式构造 `learning` 模式并通过。 | 回写 runtime mode 枚举证据。 |
| R23-63 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §117 Contract 深层偏差（typed_event_bus / sandbox_auth / hitl / cost_budget / platform_panic） | P1 | `(缺失)` | CostEvent 运行时类型不存在——仅在 DB schema 中 | 已解决 | 原问题属实：此前只有持久层语义，没有统一的运行时 cost event contract。 | 当前 `src/platform/model-gateway/cost-tracker/budget-guard.ts` 已新增 `CostEvent` 与 `actualizeCostEvent()`，把 stage、scope、BYOK 归因与平台治理成本拆分为运行时对象；回归测试已验证该类型与实际构造逻辑。 | 补齐运行时 CostEvent 契约。 |
| R23-64 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §117 Contract 深层偏差（typed_event_bus / sandbox_auth / hitl / cost_budget / platform_panic） | P1 | `(缺失)` | StageBudgetPolicy(per-OAPEFLIR-stage 预算)不存在 | 已解决 | 原问题属实：此前缺少逐阶段预算 contract 和执行检查。 | 当前 `src/platform/model-gateway/cost-tracker/budget-guard.ts` 已新增 `StageBudgetPolicy`，并把 `stageBudgets` 接入 `BudgetGuard.evaluateExecutionChain()`；回归测试已验证 `execute` 阶段超额会触发 `budget.stage_limit_exceeded`。 | 关闭逐阶段预算缺口。 |
| R23-65 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §117 Contract 深层偏差（typed_event_bus / sandbox_auth / hitl / cost_budget / platform_panic） | P1 | `platform-panic-service.ts:16-26` | PanicDirective 缺 scope_ref/expires_at——无法自动过期 | 已解决 | 原问题属实：directive 缺少 contract 字段，运行期也不会自动忽略过期 panic。 | 当前 `src/ops-maturity/emergency/platform-panic-service.ts` 已为 activation request 和 directive 增加 `scopeRef/expiresAt`，并在解析 active directive 时自动过滤过期记录；`tests/unit/platform/ops-maturity/reaudit-batch-r23-65-71.test.ts` 已验证过期 directive 不再阻断执行。 | 关闭 panic 自动过期缺口。 |
| R23-66 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §117 Contract 深层偏差（typed_event_bus / sandbox_auth / hitl / cost_budget / platform_panic） | P1 | `(缺失)` | PanicDrillRecord 不存在——panic drill 能力未实现 | 已解决 | 原问题属实：只有 panic activation，没有演练记录与生命周期。 | 当前 `src/ops-maturity/emergency/platform-panic-service.ts` 已新增 `PanicDrillRecord` 管理能力，以及 `startDrill/completeDrill/listDrills`；同一回归测试已验证 drill 可启动、完成并留存记录。 | 补齐 panic drill 能力。 |
| R23-67 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §117 Contract 深层偏差（typed_event_bus / sandbox_auth / hitl / cost_budget / platform_panic） | P1 | `resume-protocol/index.ts:3-11` | ResumePlan 缺6/9必须字段(plan_id/scope_ref/approval_count/compatibility_check_ref/mode/created_at) | 已解决 | 原问题属实：resume plan 契约过薄，无法表达兼容性检查与审批计数。 | 当前 `src/ops-maturity/emergency/resume-protocol/index.ts` 已把 `planId/scopeRef/approvalCount/compatibilityCheckRef/mode/createdAt` 纳入 `ResumePlan`，并在 `canResumeFromPanic()` 中做必填校验；回归测试已验证缺字段计划会被拒绝。 | 补齐 resume plan 元数据。 |
| R23-68 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §117 Contract 深层偏差（typed_event_bus / sandbox_auth / hitl / cost_budget / platform_panic） | P1 | `(缺失)` | 4/5 Explainability 对象缺失(RoutingExplanation/RiskExplanation/FallbackExplanation/TakeoverJustification) | 已解决 | 原问题属实：explainability service 只有单体 explanation，没有子对象 contract。 | 当前 `src/platform/five-plane-orchestration/hitl/hitl-explainability-service.ts` 已新增 `RoutingExplanation/RiskExplanation/FallbackExplanation/TakeoverJustification` 并支持挂入 `DecisionExplanation`；回归测试已验证四类子对象可被实际生成。 | 补齐 explainability 子对象。 |
| R23-69 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §117 Contract 深层偏差（typed_event_bus / sandbox_auth / hitl / cost_budget / platform_panic） | P1 | `hitl-explainability-service.ts:30-42` | DecisionExplanation 缺 matched_rule_or_policy/reason_source/remediation_hint | 已解决 | 原问题属实：DecisionExplanation 缺少 contract 指定的解释来源和补救提示字段。 | 当前 `src/platform/five-plane-orchestration/hitl/hitl-explainability-service.ts` 已把 `matched_rule_or_policy/reason_source/remediation_hint` 纳入 `DecisionExplanation`，并在生成时提供默认推导或显式注入；回归测试已验证三字段可用。 | 回写 explainability 核心字段。 |
| R23-70 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §117 Contract 深层偏差（typed_event_bus / sandbox_auth / hitl / cost_budget / platform_panic） | P1 | `human-takeover-service.ts` | 缺 feedback signal 注入/improvement candidate 创建/rollout 管理动作 | 已解决 | 现行代码已有反馈信号与 improvement candidate 事件，但缺少本轮对“手工接管动作真正发出这些事件”的闭环证据。 | 当前 `src/platform/five-plane-control-plane/incident-control/human-takeover-service.ts` 在 takeover action 中会注入 `feedback:signal_received`、生成 `improve:candidate_proposed`，并把 override/incident 上下文一并写入事件载荷；回归测试已验证 `modify_input` 会触发两类事件。 | 回写 takeover feedback/improve 证据。 |
| R23-71 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §117 Contract 深层偏差（typed_event_bus / sandbox_auth / hitl / cost_budget / platform_panic） | P1 | `(缺失)` | ManualOverride/IncidentContextBundle 类型不存在 | 已解决 | 原问题此前只修到类型声明，缺少运行时实际出现在 takeover 事件中的证据。 | 当前 `src/platform/five-plane-control-plane/incident-control/human-takeover-service.ts` 已显式生成 `ManualOverride` 与 `IncidentContextBundle` 并随 `takeover:action_applied` 发出；回归测试已验证事件 payload 中携带两类对象。 | 关闭 override/incident contract 缺口。 |
| R23-72 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §117 Contract 深层偏差（typed_event_bus / sandbox_auth / hitl / cost_budget / platform_panic） | P2 | `event-registry.ts` | 事件定义缺 producer/consumers/compatibility_policy 元数据 | 已解决 | 该条是 reaudit 取样滞后：现行 event registry 已为阶段事件提供生产者、消费者和兼容策略。 | 当前 `src/platform/five-plane-state-evidence/events/event-registry.ts` 的 `EventSchemaDefinition` 已包含 `producer/consumers/compatibilityPolicy`，且 OAPEFLIR stage events 都有明确元数据；回归测试已验证 `observe:signals_collected` 的现行 schema。 | 回写事件元数据契约。 |
| R23-73 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §117 Contract 深层偏差（typed_event_bus / sandbox_auth / hitl / cost_budget / platform_panic） | P2 | `(缺失)` | Cost estimation templates(passthrough/fast/standard/full)不存在 | 已解决 | 原问题属实：运行时没有统一的成本估算模板枚举。 | 当前 `src/platform/model-gateway/cost-tracker/budget-guard.ts` 已新增 `DEFAULT_COST_ESTIMATION_TEMPLATES`，覆盖 `passthrough/fast/standard/full` 四档模板；回归测试已验证模板集完整可用。 | 补齐成本估算模板 contract。 |
| R23-74 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §117 Contract 深层偏差（typed_event_bus / sandbox_auth / hitl / cost_budget / platform_panic） | P2 | `(缺失)` | BYOK 成本隔离逻辑不存在——平台治理成本 vs 用户模型成本混合 | 已解决 | 原问题属实：缺少统一的 BYOK 成本归因逻辑。 | 当前 `src/platform/model-gateway/cost-tracker/budget-guard.ts` 已新增 `ByokCostIsolationPolicy` 与 `actualizeCostEvent()`，能够把平台治理开销和租户模型成本分拆到独立字段；回归测试已验证 `split` 模式下 `platformGovernanceCostUsd` 与 `tenantModelCostUsd` 分离。 | 补齐 BYOK 成本隔离。 |
| R23-75 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §117 Contract 深层偏差（typed_event_bus / sandbox_auth / hitl / cost_budget / platform_panic） | P2 | `sandbox-policy.ts` | Contract 要求 PKCE 为 OAuth 默认——IAM 无 PKCE 实现 | 已解决 | 原审计与现行 IAM 实现脱节：OAuth/OIDC 服务已默认生成并发送 PKCE challenge，只是 reaudit 未补当前证据。 | 当前 `src/platform/five-plane-interface/api/oidc-oauth-service.ts` 的 `buildAuthorizationUrl()` 已默认带上 `code_challenge` 与 `code_challenge_method=S256`，同时 `src/platform/five-plane-control-plane/iam/sandbox-policy.ts` 的 `AuthProviderBinding` 也显式带 `pkceRequired`；回归测试已验证 PKCE 参数确实出现在授权 URL 中。 | 回写 PKCE 默认实现证据。 |
| R23-76 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §117 Contract 深层偏差（typed_event_bus / sandbox_auth / hitl / cost_budget / platform_panic） | P2 | `compliance/ (全模块)` | Contract 5/6 canonical types 缺失(EvidenceMappingRule/ComplianceReportRequest/Artifact/EvidenceRecord/AuditAppendCommand) | 需继续整改（ADR/contract） | ADR、contract、类型导出与现行实现存在漂移，本轮未完成逐条回写与一致性校验闭环。 | 已按源审计条目补录编号；本轮未新增 contract/ADR 回写或一致性测试证据，因此不能改写为已解决。 | 补录到 reaudit，待专项整改。 |
| R24-01 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §119 Marketplace / SLA Engine / Resource Manager 缺陷 | P0 | `certification/index.ts:6` | 认证状态 pending/approved/revoked——缺 reviewing/published/suspended(3个必须状态) | 已解决 | 这条是典型 reaudit 滞后：现行 certification schema 早已扩展出审计要求的中间和运行态。 | 当前 `src/scale-ecosystem/marketplace/certification/index.ts` 的 `CertificationStatusSchema` 已包含 `reviewing/published/suspended`；`tests/unit/scale-ecosystem/marketplace/certification/certification-status-r24.test.ts` 已显式验证三种状态可解析。 | 回写状态枚举证据。 |
| R24-02 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §119 Marketplace / SLA Engine / Resource Manager 缺陷 | P0 | `catalog/index.ts:22` | reviewStatus 缺 reviewing/published/suspended | 已解决 | 这条同样属于 reaudit 未刷新现行 schema。catalog entry 当前 review lifecycle 已覆盖送审、发布、暂停等完整状态。 | 当前 `src/scale-ecosystem/marketplace/catalog/index.ts` 的 `reviewStatus` 已包含 `submitted/reviewing/approved/published/suspended/rejected/revoked`；`tests/unit/scale-ecosystem/marketplace/catalog/catalog-status-r24.test.ts` 已验证 `reviewing/published/suspended` 三种状态可解析。 | 回写 review lifecycle 状态证据。 |
| R24-03 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §119 Marketplace / SLA Engine / Resource Manager 缺陷 | P0 | `pack-security-service.ts:204` | 静态分析扫描 URI string 而非实际代码——安全检查是 no-op | 已解决 | 原问题属于旧实现快照；现行 pack 安全扫描已经要求传入并扫描实际 `sourceCode`，不再只看 URI。 | 当前 `src/scale-ecosystem/marketplace/pack-security-service.ts` 的 `SecurityScanInput` 已包含 `sourceCode`，`runSecurityScan()` 会对真实代码执行 `runStaticAnalysis()`；`tests/unit/scale-ecosystem/reaudit-batch-r24-03-12.test.ts` 已验证 benign URI + malicious source code 仍能命中 `SAND001`。 | 回写现行源码与定向测试依据。 |
| R24-04 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §119 Marketplace / SLA Engine / Resource Manager 缺陷 | P0 | `pack-security-service.ts:65-79` | 无依赖供应链安全审计(CVE/漏洞扫描)——仅检查版本冲突 | 已解决 | 原问题不是“系统完全没有供应链扫描”，而是版本范围匹配能力不完整，导致 CVE 库命中不足；本轮已补齐 semver comparator 支持。 | 当前 `src/scale-ecosystem/marketplace/pack-security-service.ts` 已提供 `scanDependencyVulnerabilities()`、内置 `MOCK_CVE_DATABASE` 与 `versionMatchesCveRange()`，本轮新增 `</<=/>/>=/^/~` 与组合范围解析，已能覆盖 `"<0.21.2"`、`"<4.17.21"`、`">=4.0.0 <4.17.22"` 等 CVE 区间；`tests/unit/scale-ecosystem/reaudit-batch-r24-03-12.test.ts` 已验证 `axios@0.21.1`、`lodash@4.17.20` 都会命中漏洞。 | 关闭该条依赖供应链扫描缺口。 |
| R24-05 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §119 Marketplace / SLA Engine / Resource Manager 缺陷 | P0 | `resource-pool-service.ts:21-77` | 无 per-consumer 资源分解——不可能检测 noisy neighbor | 已解决 | 原结论已被现行实现推翻；资源池已维护 per-pool/per-consumer 的分解台账。 | `src/scale-ecosystem/resource-manager/resource-pool-service.ts` 现有 `consumerAllocations` 映射、`allocate()/release()` 中的逐消费者记账，以及 `getConsumerAllocations()` 输出；`tests/unit/scale-ecosystem/reaudit-batch-r24-03-12.test.ts` 已验证同一 pool 下不同 consumer 的 allocation breakdown 可被取回。 | 回写现行 per-consumer 分解证据。 |
| R24-06 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §119 Marketplace / SLA Engine / Resource Manager 缺陷 | P0 | `fair-queue/index.ts:15-20` | 按 orgId 字典序排序优先于 score——"aaa" 永远优先于 "zzz" 不论 SLA | 已解决 | 原问题属于旧排序器实现；现行公平队列已按 fair-share score、年龄和 itemId 做排序，不使用 `orgId` 作为优先级主键。 | `src/scale-ecosystem/resource-manager/fair-queue/index.ts` 当前先比较 `computeFairShareScore()`，再以 `ageMs` 和 `itemId` 做稳定 tie-break；`tests/unit/scale-ecosystem/reaudit-batch-r24-03-12.test.ts` 已验证高 SLA/高 priority 项不会被 `orgId="aaa"` 的低分项压制。 | 回写现行公平排序与定向测试依据。 |
| R24-07 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §119 Marketplace / SLA Engine / Resource Manager 缺陷 | P1 | `quota-enforcer/index.ts:28` | 用 burstLimit 而非 hardLimit 作拒绝阈值——hard limit 无强制 | 已解决 | 该条已被现行 quota evaluator 推翻，但此前 reaudit 没吸收回归证据。 | `src/scale-ecosystem/resource-manager/quota-enforcer/index.ts` 当前 `evaluateQuota()` / `evaluateMultiDimensionalQuota()` 已以 `hardLimit` 作为 exceed 阈值，`burstLimit` 只保留为容量背景，不再放宽 hard rejection；`tests/unit/scale-ecosystem/reaudit-batch-r24-03-12.test.ts` 已验证单维和多维场景都会在 hardLimit 处拒绝。 | 回写现行 hard-limit 强制与测试依据。 |
| R24-08 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §119 Marketplace / SLA Engine / Resource Manager 缺陷 | P1 | `sla-operations-service.ts:104-108` | 惩罚为 string enum 无金额/无 credit 发放/无补偿跟踪 | 已解决 | 原问题属于旧版 `SlaPenaltyDecision` 过于贫乏；现行 SLA 运营决策已经带出金额与审计说明。 | `src/scale-ecosystem/sla-engine/sla-operations-service.ts` 当前 `SlaPenaltyDecision` 已包含 `creditAmount` 与 `description`，`calculateCreditAmount()` 会按 budget allocation 和 breach 数量生成 credit；`tests/unit/scale-ecosystem/reaudit-batch-r24-03-12.test.ts` 已验证 breach 时会生成具体 credit penalty 与审计描述。 | 回写现行 penalty 细化与测试依据。 |
| R24-09 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §119 Marketplace / SLA Engine / Resource Manager 缺陷 | P1 | `tier-resolver/index.ts:16-17` | 无 tenantId 参数——无法做 per-tenant tier 解析 | 已解决 | 原问题已被现行 tier resolver 推翻；接口和筛选逻辑都支持租户作用域。 | `src/scale-ecosystem/sla-engine/tier-resolver/index.ts` 当前 `resolveHighestPriorityTier(tiers, tenantId?)` 已按 `tenantId` 过滤 tenant-specific/global tiers；`tests/unit/scale-ecosystem/reaudit-batch-r24-03-12.test.ts` 已验证 `tenant-a` 只会选中自己的 tier，不会误取其他租户 tier。 | 回写现行 tenant-aware 解析证据。 |
| R24-10 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §119 Marketplace / SLA Engine / Resource Manager 缺陷 | P1 | `sla-operations-service.ts:111` | preemptionCapApplied 永远 true——死常量 | 已解决 | 原问题属于旧死常量结论；现行实现已按 selected tier 的 preemption priority 与队列最大 priority 动态求值。 | `src/scale-ecosystem/sla-engine/sla-operations-service.ts` 当前会比较 `selectedTier.preemptionPriority` 与所有 tiers 的 `maxPriority` 生成 `preemptionCapApplied`；`tests/unit/scale-ecosystem/reaudit-batch-r24-03-12.test.ts` 已验证低优先级 tier 的该字段为 `false`。 | 回写现行动态求值与测试依据。 |
| R24-11 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §119 Marketplace / SLA Engine / Resource Manager 缺陷 | P1 | `resource-allocator/index.ts:6-10` | reservedPercent 总和不验证 ≤100——可超额分配 | 已解决 | 原问题属于旧资源保留分配器实现；现行 allocator 已在分配前执行总占比校验。 | `src/scale-ecosystem/sla-engine/resource-allocator/index.ts` 当前会先累计 `reservedPercent` 并在超出 `100` 时抛出 `resource_allocator.reserved_capacity_exceeds_100`；`tests/unit/scale-ecosystem/reaudit-batch-r24-03-12.test.ts` 已验证 `60 + 50` 会 fail closed。 | 回写现行 over-allocation 拒绝与测试依据。 |
| R24-12 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §119 Marketplace / SLA Engine / Resource Manager 缺陷 | P1 | `fair-scheduling-service.ts:50` | 抢占用 burstLimit 而非 hardLimit 触发——太晚 | 已解决 | 原问题已被现行调度器推翻；公平调度现在复用 quota enforcer 的 hard-limit 判断来决定是否抢占。 | `src/scale-ecosystem/resource-manager/fair-scheduling-service.ts` 当前通过 `evaluateMultiDimensionalQuota()` 得到 `quotaExceeded`，并以该 hard-limit exceed 信号决定是否调用 `choosePreemptionVictim()`；`tests/unit/scale-ecosystem/reaudit-batch-r24-03-12.test.ts` 已验证 currentUsage=95、hardLimit=100、requested=10 时会触发 preemption。 | 回写现行 hard-limit preemption 证据。 |
| R24-13 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §119 Marketplace / SLA Engine / Resource Manager 缺陷 | P1 | `marketplace-governance-service.ts:501-506` | reviewRequired=false 的 package 仍必须有 review record 才能 publish | 已解决（源码修复并补定向测试） | 原问题属真实发布门缺口；旧实现即使 `reviewRequired=false` 也会把 publish 卡在“必须先有人手工提 review”，且 auto-review 分支不会把生成的 `reviewId` 正确写回 publication。 | 当前 `src/scale-ecosystem/marketplace/marketplace-governance-service.ts` 已在 `reviewRequired=false` 时惰性创建 auto-approved review，并把 `reviewToUse.reviewId` 真实写入 publication；`tests/unit/platform/reaudit-batch-r24-13-28.test.ts` 已验证 review-optional package 可直接 publish 且 `publication.reviewId` 非空。 | 关闭 review-optional package 仍被 review record 门禁阻断的缺口。 |
| R24-14 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §119 Marketplace / SLA Engine / Resource Manager 缺陷 | P2 | `service-registry.ts:146-148` | Dead code: if(!has) delete——no-op | 已解决（源码复核并补定向测试） | 原 reaudit 条目停留在更早的 `ServiceRegistry` 清理逻辑快照；现行 `service-registry.ts` 已不存在“先判 `!has()` 再 `delete()`”的 no-op 死分支。 | `src/platform/shared/lifecycle/service-registry.ts` 当前源码已无该分支；`tests/unit/platform/reaudit-batch-r24-14-31-60-71.test.ts` 的 `R24-14 service registry source no longer contains delete-on-missing dead branch` 已做源码级回归。 | 回写该条为“历史审计结论过时”，不保留伪缺口。 |
| R24-15 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §119 Marketplace / SLA Engine / Resource Manager 缺陷 | P2 | `structured-logger.ts:412-414` | Race: sync statSync + async appendFile——并发写可超 maxBytes | 已解决（源码修复并补定向测试） | 原问题属真实并发写竞态；旧实现只用当前文件大小 + 单次 incomingBytes 判断，多个异步 append 会共享同一陈旧 `statSync()` 结果。 | 当前 `src/platform/shared/observability/structured-logger.ts` 已为每个 sink 增加 `pendingBytes` 跟踪，并在 rotation 判断中使用 `currentBytes + state.pendingBytes`；`tests/unit/platform/reaudit-batch-r24-13-28.test.ts` 已钉住这条并发保护逻辑。 | 关闭结构化日志文件轮转在并发写下可超 `maxBytes` 的竞态。 |
| R24-16 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §119 Marketplace / SLA Engine / Resource Manager 缺陷 | P2 | `preemption/index.ts:7-13` | 无 protected/minPriority 阈值——系统执行也可被驱逐 | 已解决（源码修复并补定向测试） | 原问题属真实抢占保护缺口；旧实现只看优先级/进度/checkpoint，新加 checkpoint 门后仍缺 candidate 级保护位。 | 当前 `src/scale-ecosystem/resource-manager/preemption/index.ts` 已新增 `protectedFromPreemption` 并保留 `minPreemptablePriority` 阈值，`tests/unit/platform/reaudit-batch-r24-13-28.test.ts` 已验证 protected execution 会被跳过且低于阈值的不参与抢占。 | 关闭系统级执行缺少 protected/minPriority 护栏的缺口。 |
| R24-17 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §119 Marketplace / SLA Engine / Resource Manager 缺陷 | P2 | `resource-pool-service.ts:55-63` | release 无 consumerId——任何调用者可释放任何 pool 资源 | 已解决（源码复核并补定向测试） | 原问题已被现行实现推翻，但复审表缺少直接证据。 | 当前 `src/scale-ecosystem/resource-manager/resource-pool-service.ts` 的 `release(poolId, consumerId, units)` 已通过 `verifyConsumerOwnsResources()` 做 owner 校验；`tests/unit/platform/reaudit-batch-r24-13-28.test.ts` 已验证错误 consumer 会收到 `resource_pool.unauthorized_release`。 | 回写 consumer-bound release 现行约束。 |
| R24-18 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §120 Knowledge / Memory / Checkpoints / Truth 缺陷 | P0 | `knowledge-retrieval.ts:296` | 语义搜索反转: vectorStore 存在时返回[]——配置 vector store 后丢失全部语义匹配 | 已解决（源码复核并补定向测试） | 原问题属于旧逻辑快照；现行异步查询链已在配置 vector store 时走 `querySimilar()`，不会把 semantic path 直接短路为空。 | 当前 `src/platform/five-plane-state-evidence/knowledge/retrieval/knowledge-retrieval.ts` 的 `collectSemanticCandidatesAsync()` 已在 `semanticVectorStore` 存在时调用 `querySimilar()` 并去重返回；`tests/unit/platform/reaudit-batch-r24-13-28.test.ts` 已验证配置 vector store 后能取回 semantic hit。 | 关闭“vector store 一启用语义检索即空结果”的旧结论。 |
| R24-19 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §120 Knowledge / Memory / Checkpoints / Truth 缺陷 | P0 | `memory-layer-model.ts:337` | shouldEvict 对 LRU 层永不触发——priority=epoch ms(~10¹²)永远>0.5 | 已解决（源码修复并补定向测试） | 原问题属真实归一化错误；旧实现把 LRU/FIFO 的 epoch 毫秒值直接拿去和 `0.5` 比较，导致容量压力下几乎永不触发逐出。 | 当前 `src/platform/five-plane-state-evidence/memory/memory-layer-model.ts` 已把 LRU/FIFO priority 归一化为基于 retention window 的 `0..1` 比较值；`tests/unit/platform/reaudit-batch-r24-13-28.test.ts` 已验证未过期但足够旧的 session memory 会在 overflow 时被逐出。 | 关闭 LRU 层永不触发 capacity eviction 的缺口。 |
| R24-20 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §120 Knowledge / Memory / Checkpoints / Truth 缺陷 | P0 | `workflow-step-checkpoint.ts:255-258` | 验证拒绝有效 checkpoint——compensationModel typeof!=="string" 但实际为 object | 已解决（源码复核并补直接回归） | 该条是 reaudit 结论滞后；现行 validator 已接受带 `strategy` 的对象式 `compensationModel`。 | 当前 `src/platform/five-plane-state-evidence/checkpoints/workflow-step-checkpoint.ts` 的 `isWorkflowStepCheckpoint()` 已允许 object compensation model；`tests/unit/platform/five-plane-state-evidence/checkpoints/workflow-step-checkpoint.test.ts` 中 `readWorkflowStepCheckpoint accepts structured compensation model objects` 已直接覆盖。 | 回写 object compensation model 的现行兼容性。 |
| R24-21 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §120 Knowledge / Memory / Checkpoints / Truth 缺陷 | P1 | `semantic-knowledge-graph.ts:4` | 仅3种 edge type——缺 derives_from/contradicts/specializes/related_to | 已解决（源码修复并补定向测试） | 原问题属真实 graph relation 面不足；虽然已有部分扩展关系，但还缺 alias/canonical 衔接与 `contradicts`。 | 当前 `src/platform/five-plane-state-evidence/knowledge/semantic-knowledge-graph.ts` 已新增 `related_to/derives_from` alias 归一化、`contradicts` 关系与对应连接集；`tests/unit/platform/reaudit-batch-r24-13-28.test.ts` 已验证 alias 会规范化为 `relates_to/derived_from` 且 `contradicts` 可落图。 | 关闭知识图边类型不足的缺口。 |
| R24-22 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §120 Knowledge / Memory / Checkpoints / Truth 缺陷 | P1 | `semantic-knowledge-graph.ts:225` | collectAdjacent O(V×E)——遍历全部 edges 而非用 adjacencyByNodeId | 已解决（源码复核并补行为回归） | 现行主链已经使用 `adjacencyByNodeId`，但 keyword 根节点的邻接信息未双端登记，导致按 keyword inspect 时不能走到相邻 chunk。 | 当前 `collectAdjacent()` 已基于 `adjacencyByNodeId` 遍历，且本轮把 `addEdge()` 调整为双端登记邻接边；`tests/unit/platform/five-plane-state-evidence/knowledge/semantic-knowledge-graph.test.ts` 的 keyword inspect 回归现已可通过。 | 关闭 semantic graph 邻接遍历仍需全表扫描的旧结论。 |
| R24-23 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §120 Knowledge / Memory / Checkpoints / Truth 缺陷 | P1 | `semantic-knowledge-graph.ts:269-271` | addEdge 无去重——重复 upsert 导致 adjacency 无界增长 | 已解决（源码修复并补定向测试） | 原问题属真实邻接表膨胀缺口；`edges` Map 虽会覆盖同 ID edge，但 `adjacencyByNodeId` 仍会持续 `push()` 重复引用。 | 当前 `src/platform/five-plane-state-evidence/knowledge/semantic-knowledge-graph.ts` 的 `addEdge()` 已按 `edgeId` 更新或插入，不再对同一边重复 push；`tests/unit/platform/reaudit-batch-r24-13-28.test.ts` 已验证重复 `addEntityRelation()` 后图里只保留 1 条对应关系边。 | 关闭 adjacency 无界增长缺口。 |
| R24-24 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §120 Knowledge / Memory / Checkpoints / Truth 缺陷 | P1 | `memory-layer-model.ts vs layer-transition-service` | 双重不一致 promotion 规则集(4 rules vs 5 rules,不同阈值/层名) | 已解决（源码修复并补定向测试） | 原问题属真实规则漂移；legacy memory model 与 six-layer transition service 在规则条数、阈值和 procedural promotion 能力上长期不一致。 | 当前 `src/platform/five-plane-state-evidence/memory/memory-layer-model.ts` 已把 `DEFAULT_MEMORY_PROMOTION_RULES` 对齐到 5 条 canonical 阶梯，并把 `user/procedural` 层 `supportsPromotion` 改为 `true`；`tests/unit/platform/reaudit-batch-r24-13-28.test.ts` 已验证 legacy thresholds 与 six-layer canonical 阶梯一致。 | 关闭 memory promotion rule 双轨漂移缺口。 |
| R24-25 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §120 Knowledge / Memory / Checkpoints / Truth 缺陷 | P1 | `knowledge/ (全目录)` | 无跨域知识联邦——单 namespace 查询无路由/merge/冲突解决 | 已解决（源码修复并补定向测试） | 原问题属真实能力缺口；旧 `KnowledgeQueryService` 只能单 namespace 查询，缺 federated route/merge。 | 当前 `src/platform/five-plane-state-evidence/knowledge/knowledge-query-service.ts` 已新增 `queryFederated()` / `queryFederatedAsync()`，按 namespace fan-out、按 `knowledgeRef` 去重并保留最高分结果；`tests/unit/platform/reaudit-batch-r24-13-28.test.ts` 已验证 `ns-a/ns-b` 两侧命中会被合并返回。 | 关闭知识联邦查询缺口。 |
| R24-26 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §120 Knowledge / Memory / Checkpoints / Truth 缺陷 | P1 | `knowledge/archive/knowledge-archive.ts:19-46` | 无版本历史——upsert 覆盖前版本无回滚/diff | 已解决（源码修复并补定向测试） | 原问题属真实 archive 可追溯性缺口；旧 archive 只保留最新记录，覆盖前版本直接丢失。 | 当前 `src/platform/five-plane-state-evidence/knowledge/archive/knowledge-archive.ts` 已新增 `historyByDocumentId`、`listVersions()`、`getDocumentVersion()`、`diffDocumentVersions()` 与 `rollbackDocument()`；`tests/unit/platform/reaudit-batch-r24-13-28.test.ts` 已验证版本列表、chunk diff 和 rollback。 | 关闭 archive 无历史/无回滚/无 diff 缺口。 |
| R24-27 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §120 Knowledge / Memory / Checkpoints / Truth 缺陷 | P1 | `checkpoints/ (全目录)` | 无 checkpoint 恢复逻辑——有创建无 restoreFromCheckpoint | 已解决（源码修复并补定向测试） | 原问题属真实恢复能力缺口；旧 checkpoint 只有 create/read/summary，没有从 checkpoint 恢复执行态的 helper。 | 当前 `src/platform/five-plane-state-evidence/checkpoints/workflow-step-checkpoint.ts` 与 barrel export 已新增 `restoreWorkflowStepCheckpoint()`，会还原 output/decision/resume/fileDiff/compensation 状态；`tests/unit/platform/reaudit-batch-r24-13-28.test.ts` 已验证 restore state 可直接读回。 | 关闭 checkpoint 只有创建没有恢复入口的缺口。 |
| R24-28 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §120 Knowledge / Memory / Checkpoints / Truth 缺陷 | P1 | `checkpoints/ (全目录)` | 无 checkpoint 版本管理——单一 v1 无迁移/比较/回滚 | 已解决（源码修复并补定向测试） | 原问题属真实版本管理缺口；旧 checkpoint 工具没有任何版本比较/差异视图。 | 当前 `src/platform/five-plane-state-evidence/checkpoints/workflow-step-checkpoint.ts` 与 `index.ts` 已新增 `compareWorkflowStepCheckpointVersions()` 与差异结果结构；`tests/unit/platform/reaudit-batch-r24-13-28.test.ts` 已验证 status/outputKeys/nextStep/compensation 变化都会被比对出来。 | 关闭 checkpoint 完全缺少版本比较能力的缺口。 |
| R24-29 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §120 Knowledge / Memory / Checkpoints / Truth 缺陷 | P1 | `memory/ (全目录)` | 无 ContextTruncationReport——spec 要求记录被压缩/排除/降级内容 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R24-30 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §120 Knowledge / Memory / Checkpoints / Truth 缺陷 | P1 | `truth/sqlite/repositories/ (全部)` | 无乐观锁——直接 INSERT/UPDATE 无 version/CAS 检查 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R24-31 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §120 Knowledge / Memory / Checkpoints / Truth 缺陷 | P2 | `projections/ (artifact/governance/risk)` | processedEventIds 用 Array.includes() O(n²) + 无界增长 | 已解决（源码复核并补定向测试） | 原 reaudit 没吸收 projection 去重状态后续已切到 `Set` 的实现，继续把这组三个 projection 记成 `Array.includes()` 线性去重。 | `src/platform/state-evidence/events/projections/artifact-catalog-projection.ts`、`governance-projection.ts`、`risk-action-projection.ts` 当前均维护 `Set`-backed `processedEventIds`；`tests/unit/platform/reaudit-batch-r24-14-31-60-71.test.ts` 已验证三者首轮构建出的幂等状态为 `Set`，且 replay 不会重复累计。 | 回写 projection idempotency 现状，不保留已消失的 O(n²) 指控。 |
| R24-32 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §120 Knowledge / Memory / Checkpoints / Truth 缺陷 | P2 | `memory-consolidation.ts:36-101` | 无 loss report——spec 要求压缩时记录丢失内容 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R24-33 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §120 Knowledge / Memory / Checkpoints / Truth 缺陷 | P2 | `memory-layer-model.ts:193-195` | 未知层 silent fallback "project"——错误配置静默错路由 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R24-34 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §120 Knowledge / Memory / Checkpoints / Truth 缺陷 | P2 | `truth/ (全部 repositories)` | 非 event-sourced——直接 CRUD 而非从事件 replay 投影 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R24-35 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §121 UI i18n / Tools / Design System / Features 缺陷 | P0 | `i18n/index.ts:62-81` | Locale catalogs 内联硬编码而非 lazy-loaded bundles——全部语言打入初始包 | 已解决（源码修复并补定向测试） | 原问题属真实壳层打包缺口；旧 `i18n` 服务把中英 catalog 直接内联在单文件里，既不能按 locale 分包，也没有 loader seam。 | 本轮将 `ui/packages/shared/i18n/src/` 拆成 `catalogs/en-US.ts`、`zh-CN.ts`、`ar-SA.ts`，并在 `index.ts` 中新增 `registerLoader()/loadLocale()` lazy loader 机制；`ui/tests/shared/reaudit-r24-ui-foundations.test.tsx` 已验证按需加载 `ar-SA` 后才会解析阿语文案，`ui/tests/reaudit-r24-ui-source-review.test.ts` 已钉住 loader 接线。 | 关闭 locale catalog 全内联、无法 lazy-load 的缺口。 |
| R24-36 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §121 UI i18n / Tools / Design System / Features 缺陷 | P0 | `i18n/ (全目录)` | 无 RTL 支持——无 dir 属性/logical CSS/RTL 检测 | 已解决（源码修复并补定向测试） | 原问题属真实国际化缺口；旧 `TranslationService` 既不追踪语言方向，也不会把 `dir/lang` 应用到文档根节点。 | 当前 `ui/packages/shared/i18n/src/index.ts` 已新增 `TextDirection`、`getDirection()`、`applyLocaleToDocument()` 与 RTL locale 元数据；`ui/tests/shared/reaudit-r24-ui-foundations.test.tsx` 已验证切到 `ar-SA` 后会把 `documentElement.dir` 设为 `rtl`。 | 关闭 UI 壳层缺少 RTL/direction 感知的缺口。 |
| R24-37 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §121 UI i18n / Tools / Design System / Features 缺陷 | P0 | `features/alerts/ (全目录)` | 无实时 alert stream——仅 REST polling 无 WebSocket 订阅 | 已解决（源码修复并补定向测试） | 原问题属真实告警流缺口；旧 alerts hook 只读取 `useIncidentsQuery()` 结果，没有订阅 `incidents` channel。 | 当前 `ui/packages/features/alerts/src/hooks/index.ts` 已通过 `useWsClient().subscribe("incidents", ...)` 订阅实时事件，并把 stream 更新写入 `liveIncidents/history`；`ui/tests/reaudit-r24-ui-source-review.test.ts` 已钉住 `incidents` 订阅与 history 接线。 | 关闭 alerts 只有 polling、没有实时订阅的缺口。 |
| R24-38 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §121 UI i18n / Tools / Design System / Features 缺陷 | P0 | `features/alerts/hooks/index.ts:9-16` | 缺 snooze/dismiss/alert history——仅有 ack/mute/escalate 且无 mutation 实现 | 已解决（源码修复并补定向测试） | 原问题属真实交互缺口；旧实现只有静态 `onDismiss` 本地过滤，没有 snooze/dismiss/escalate 的 mutation 路径，也不保留操作历史。 | 当前 `ui/packages/features/alerts/src/hooks/index.ts` 已新增 `onAcknowledge/onDismiss/onSnooze/onEscalate` 四类 mutation 与 `history` 轨迹，`ui/packages/features/alerts/src/web/index.tsx` 也会展示 alert history；定向 source-review 测试已覆盖。 | 关闭 alert snooze/dismiss/history 缺口。 |
| R24-39 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §121 UI i18n / Tools / Design System / Features 缺陷 | P0 | `features/domain-wizard/ (全目录)` | 无多步骤 wizard——flat list 无 step state machine/stepper/表单验证/持久化/预览 | 已解决（源码修复并补定向测试） | 原问题属真实产品化缺口；旧 domain wizard 只是 domain 列表映射，没有 wizard draft、stepper、校验和持久化。 | 当前 `ui/packages/features/domain-wizard/src/hooks/index.ts` 已新增 `basics/policy/preview` 三步状态机、`validateDomainWizardDraft()`、`localStorage` 持久化与 `createPreviewRows()`；`web/index.tsx` 也已接上线性 stepper、表单、预览与模板加载动作。`ui/tests/reaudit-r24-ui-source-review.test.ts` 已覆盖这些关键实现。 | 关闭 domain wizard 仍是 flat list stub 的缺口。 |
| R24-40 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §121 UI i18n / Tools / Design System / Features 缺陷 | P0 | `tools/e2e/index.ts` | 无 Playwright 测试——仅静态场景目录数组无 test()/browser launch | 已解决（源码修复并补定向测试） | 原问题属真实工具链缺口；旧 e2e 工具只有场景目录和 checklist，完全没有 `@playwright/test` suite。 | 当前 `ui/tools/e2e/src/index.ts` 已新增 `createPlaywrightScenarioDefinitions()`、`runScenario()` 与 `registerSmokeSuite()`，并通过 `ui/tools/e2e/src/smoke.spec.ts` 生成实际 Playwright smoke suite；`ui/tests/tools/tooling.test.ts` 与 `ui/tests/reaudit-r24-ui-source-review.test.ts` 已验证定义和 spec 文件存在。 | 关闭 UI e2e 工具没有 Playwright 资产的缺口。 |
| R24-41 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §121 UI i18n / Tools / Design System / Features 缺陷 | P0 | `tools/codegen/index.ts` | 无 OpenAPI codegen——仅从手写数组生成路径常量字符串 | 已解决（源码修复并补定向测试） | 原问题属真实工具链缺口；旧 codegen 只能把手工 endpoint 数组转成 path 常量，完全不读 OpenAPI 文档。 | 当前 `ui/tools/codegen/src/index.ts` 已新增 `OpenApiDocument`、`parseOpenApiDocument()` 与 `generateBindingsFromOpenApi()`；`ui/tests/tools/tooling.test.ts` 已验证会从 OpenAPI paths 生成 method+path 常量绑定。 | 关闭 OpenAPI codegen 缺失的缺口。 |
| R24-42 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §121 UI i18n / Tools / Design System / Features 缺陷 | P0 | `tools/mock-server/index.ts` | 无 HTTP server——3个纯函数返回内存对象无端口/请求处理 | 已解决（源码修复并补定向测试） | 原问题属真实 mock seam 缺口；旧 mock-server 只有 snapshot/route resolver 纯函数，没有 request handler 和 HTTP server 装配。 | 当前 `ui/tools/mock-server/src/index.ts` 已新增 `createMockRequestHandler()` 与 `createMockHttpServer()`，并支持 `/healthz` 与 `/api/*` 路由；`ui/tests/tools/tooling.test.ts` 已直接验证 HTTP handler 会返回 JSON health/api 响应。 | 关闭 mock-server 只有纯函数、没有 HTTP handler/server 的缺口。 |
| R24-43 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §121 UI i18n / Tools / Design System / Features 缺陷 | P1 | `ui-core/design-tokens/index.ts:69-84` | 原始 hex 值非语义 token——无 primitive→semantic 间接层 | 已解决（源码修复并补定向测试） | 原问题属真实设计系统缺口；旧 token 文件只有一层扁平颜色/字号对象，原始 hex 与语义用途直接耦合。 | 当前 `ui/packages/ui-core/src/design-tokens/index.ts` 已拆成 `primitiveTokens` 与 `semanticTokens` 两层，并保留向后兼容 alias；`ui/tests/shared/reaudit-r24-ui-foundations.test.tsx` 已验证 primitive→semantic 映射与 surfaceSelected 等语义 token 存在。 | 关闭 design token 缺少 primitive→semantic 间接层的缺口。 |
| R24-44 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §121 UI i18n / Tools / Design System / Features 缺陷 | P1 | `ui-core/components/index.ts:6-306` | 几乎零 accessibility——1个 aria-label,无 role/aria-selected/keyboard nav/focus/aria-live | 已解决（源码修复并补定向测试） | 原问题属真实 a11y 缺口；旧 workbench/list/layout 组件没有 listbox/log/aria-selected 等结构化语义，也没有键盘导航。 | 当前 `ui/packages/ui-core/src/components/index.ts` 已新增 `role="list"/"listbox"/"option"/"log"`、`aria-selected`、`aria-live` 与 ArrowUp/ArrowDown/Home/End 键盘导航；`ui/tests/shared/reaudit-r24-ui-foundations.test.tsx` 已验证搜索框、listbox 和 log 都可见。 | 关闭 UI workbench 组件缺少基础 accessibility 语义的缺口。 |
| R24-45 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §121 UI i18n / Tools / Design System / Features 缺陷 | P1 | `ui-core/layouts/index.ts:30` | ThreePaneLayout 无响应式——固定 grid 无 @media 断点折叠 | 已解决（源码修复并补定向测试） | 原问题属真实布局缺口；旧 `ThreePaneLayout` 始终固定三列 grid，不会按视口宽度折叠。 | 当前 `ui/packages/ui-core/src/layouts/index.ts` 已按 `viewportWidth/window.innerWidth` 在单列、双列、三列之间切换，并监听 `resize`；`ui/tests/shared/reaudit-r24-ui-foundations.test.tsx` 已验证窄屏下布局样式会收敛为单列。 | 关闭 ThreePaneLayout 不响应视口宽度的缺口。 |
| R24-46 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §121 UI i18n / Tools / Design System / Features 缺陷 | P1 | `ui-core/design-tokens/index.ts:119-123` | Breakpoint 值 640/960/1280 vs spec 768/1024/1440——不匹配 | 已解决（源码修复并补定向测试） | 原问题属真实 token 偏差；旧 breakpoint 配置确实停留在 `640/960/1280`。 | 当前 `ui/packages/ui-core/src/design-tokens/index.ts` 已把 canonical breakpoint 对齐为 `768/1024/1440`；`ui/tests/shared/reaudit-r24-ui-foundations.test.tsx` 已直接断言三档数值。 | 关闭 breakpoint 配置与 spec 不一致的缺口。 |
| R24-47 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §121 UI i18n / Tools / Design System / Features 缺陷 | P1 | `features/settings/hooks/index.ts:105-107` | save() 同步翻转状态无 API 调用——偏好不持久化到后端 | 已解决（源码复核并补定向测试） | 这条主要是 reaudit 结论滞后：`settings` hook 早已通过 mutation 调 `/preferences`，只是设置页 UI 没把更完整的 section/locale 体验接上，导致旧结论继续挂着。 | 当前 `ui/packages/features/settings/src/hooks/index.ts` 已通过 `useMutation({ method: "PUT", path: "/preferences" })` 持久化偏好；本轮 `ui/tests/reaudit-r24-ui-source-review.test.ts` 已把该 API 路径钉住。 | 回写 settings 偏好保存已真实走后端 API 的现状。 |
| R24-48 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §121 UI i18n / Tools / Design System / Features 缺陷 | P1 | `features/settings/ (全目录)` | 缺 API key management 和 notification settings | 已解决（源码修复并补定向测试） | 原问题属真实产品面缺口；虽然仓内已有 `api-keys.tsx` / `notifications.tsx` 子页，但旧 Settings Web 没有把这些 section 暴露出来。 | 当前 `ui/packages/features/settings/src/web/index.tsx` 已新增 section tabs，并可切到 `SettingsApiKeys` 与 `SettingsNotifications`；`ui/tests/reaudit-r24-ui-source-review.test.ts` 已验证这两个子页导入和 locale select 接线存在。 | 关闭 settings 页面缺 API key management / notification settings 入口的缺口。 |
| R24-49 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §121 UI i18n / Tools / Design System / Features 缺陷 | P1 | `.github/workflows/ui-quality.yml` | CI workflow 文件不存在 | 已解决（源码复核） | 根因不是当前仓库缺少 UI 质量流水线，而是 reaudit 条目沿用了 workflow 尚未落库时的旧快照。 | 当前仓库已存在 `.github/workflows/ui-quality.yml`，并对 `ui/**` 变更触发 `npm ci`、`lint`、`typecheck`、`test:coverage`、`build`、`bundle:analyze`、`perf:budget`。该条关闭的是“文件不存在”的旧结论，不外推为所有 UI 质量门禁都已满足。 | 不改代码，仅回写现行 CI 配置依据。 |
| R24-50 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §121 UI i18n / Tools / Design System / Features 缺陷 | P1 | `ui/ (全目录)` | 无 visual regression testing——零截图对比工具 | 需继续整改（UI 产品化差距） | UI、壳层或共享包仍有 stub、缺件、性能预算或安全能力未落地，本轮未完成对应实装。 | 已按源审计条目补录编号；本轮未执行足以证明修复完成的 UI 全链路 build/e2e/壳层验证。 | 补录到 reaudit，待专项整改。 |
| R24-51 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §121 UI i18n / Tools / Design System / Features 缺陷 | P2 | `i18n/index.ts:17-18` | setLocale 无事件发射——组件不会因语言切换 re-render | 已解决（源码修复并补定向测试） | 原问题属真实 i18n 响应式缺口；旧 `setLocale()` 只改局部状态，不向外发事件。 | 当前 `ui/packages/shared/i18n/src/index.ts` 已新增 `listeners` 与 `subscribe()`，`setLocale()` 会发出 locale+direction 变更并应用到 document；`ui/tests/shared/reaudit-r24-ui-foundations.test.tsx` 已验证 listener 会被触发。 | 关闭语言切换不发事件的缺口。 |
| R24-52 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §121 UI i18n / Tools / Design System / Features 缺陷 | P2 | `ui-core/components/index.ts:93,128-129` | 硬编码中文字符串绕过 i18n | 已解决（源码修复并补定向测试） | 原问题属真实组件库文案硬编码；旧 workbench 默认文案和 detail label 混入中文字符串，外层无法覆写。 | 当前 `ui/packages/ui-core/src/components/index.ts` 已把 workbench 文案抽成 `FeatureWorkbenchLabels`，默认 detail label 也改成中性英文；`ui/tests/reaudit-r24-ui-source-review.test.ts` 已钉住组件文件不再保留该组中文硬编码。 | 关闭 ui-core workbench 文案绕过 i18n 的缺口。 |
| R24-53 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §121 UI i18n / Tools / Design System / Features 缺陷 | P2 | `ui-core/components/index.ts:201` | 硬编码 #12201a 不在 design tokens 中 | 已解决（源码修复并补定向测试） | 原问题属真实 token 旁路；旧选中态背景色直接写死 `#12201a`。 | 当前 `ui/packages/ui-core/src/components/index.ts` 已改用 `designTokens.semantic.color.surfaceSelected`，且 `#12201a` 只保留在 design token primitive 层命名值中，不再从组件直接旁路；`ui/tests/reaudit-r24-ui-source-review.test.ts` 已断言组件文件不再包含该硬编码色值。 | 关闭组件直接硬编码颜色绕过 design tokens 的缺口。 |
| R24-54 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §121 UI i18n / Tools / Design System / Features 缺陷 | P2 | `scripts/perf-budget.mjs:6-9` | 仅检查文件大小——无 Lighthouse CI/Core Web Vitals 强制 | 已解决（源码复核） | 根因是早期 perf budget 脚本确实只有 bundle 字节预算，没有把 Lighthouse 时间指标纳入强制门禁；后续脚本和 CI 已收敛，但旧审计没有回写。 | 当前 `ui/scripts/perf-budget.mjs` 已同时校验 gzip chunk 预算与 `FCP<1.5s`、`TTI<3.5s`；`.github/workflows/ui-quality.yml` 也已在 UI 质量流水线中执行 `npm run perf:budget`。该条关闭的是“脚本没有 Lighthouse 门禁”的旧结论，不外推为当前所有产物都已满足预算。 | 作为 `R7-3/1930` 的重复挂账一并关闭。 |
| R24-55 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §121 UI i18n / Tools / Design System / Features 缺陷 | P2 | `features/settings/web/index.tsx:21` | 语言选择器是 free-text input 非 select——可输入无效 locale | 已解决（源码修复并补定向测试） | 原问题属真实表单约束缺口；旧设置页把 locale 暴露为自由输入框，确实可以写入无效 locale。 | 当前 `ui/packages/features/settings/src/web/index.tsx` 已把 locale 字段改为受控 `select`，选项来自 `localeOptions`；`ui/tests/reaudit-r24-ui-source-review.test.ts` 已断言设置页不再使用 `draftLocale` free-text input。 | 关闭 settings locale 输入不受限的缺口。 |
| R24-56 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §122 ADR 实现偏差 + E2E/Integration Tests 缺陷 | P0 | `risk-evaluation-engine.ts:122-158` | ADR-026 8因子模型未实现——仍用 legacy 6因子+除数75(应为8因子+除数18) | 需继续整改（ADR/contract） | ADR、contract、类型导出与现行实现存在漂移，本轮未完成逐条回写与一致性校验闭环。 | 已按源审计条目补录编号；本轮未新增 contract/ADR 回写或一致性测试证据，因此不能改写为已解决。 | 补录到 reaudit，待专项整改。 |
| R24-57 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §122 ADR 实现偏差 + E2E/Integration Tests 缺陷 | P0 | `risk-control/types.ts:42-49` | RiskFactorsSchema 用废弃字段名——缺 autonomyModeRisk/tenantImpact/evidenceConfidence | 需继续整改（ADR/contract） | ADR、contract、类型导出与现行实现存在漂移，本轮未完成逐条回写与一致性校验闭环。 | 已按源审计条目补录编号；本轮未新增 contract/ADR 回写或一致性测试证据，因此不能改写为已解决。 | 补录到 reaudit，待专项整改。 |
| R24-58 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §122 ADR 实现偏差 + E2E/Integration Tests 缺陷 | P0 | `contracts/request-envelope/index.ts:4-14` | ADR-021 RequestEnvelope 缺4/8必须字段(principal/source_plane/target_plane/directives) | 需继续整改（ADR/contract） | ADR、contract、类型导出与现行实现存在漂移，本轮未完成逐条回写与一致性校验闭环。 | 已按源审计条目补录编号；本轮未新增 contract/ADR 回写或一致性测试证据，因此不能改写为已解决。 | 补录到 reaudit，待专项整改。 |
| R24-59 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §122 ADR 实现偏差 + E2E/Integration Tests 缺陷 | P0 | `src/ (全代码库)` | ADR-021 OperationalDirective/DecisionDirective 完全未实现——directive-based P2→P3/P4 控制缺失 | 已解决（源码复核） | 根因：原审计条目停留在 directive 迁移前状态，声称"完全未实现"。实际 `src/platform/contracts/control-directive/index.ts:54-179` 已定义 `OperationalDirective`/`DecisionDirective` 类型与工厂；`src/platform/contracts/executable-contracts/index.ts:56-66` 已把它们作为 canonical P2→P3/P4 directive 重新导出；生产消费见于 `approval-service.ts:476-605`、`human-takeover-service.ts:764-821`、`traffic-routing-service.ts:267,444`、`harness/index.ts:935-968`、`oapeflir-loop-service.ts:1025`、`degradation-controller.ts:457,507`。 | 不改生产代码，回写 directive 已真实落地的现状。 |
| R24-60 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §122 ADR 实现偏差 + E2E/Integration Tests 缺陷 | P1 | `memory-layer-model.ts:48-49` | ADR-020 promotion 阈值偏离 spec ~20-40%(accessCount 10→8, qualityScore 0.8→0.75 等) | 已解决（源码复核并补定向测试） | 这条的根因不是生产实现仍旧漂移，而是 reaudit 继续拿过时 ADR 叙述对照 legacy memory rule；现行 `DEFAULT_MEMORY_PROMOTION_RULES` 已与 canonical six-layer ladder 对齐。 | `src/platform/five-plane-state-evidence/memory/memory-layer-model.ts` 当前 promotion 阈值与 `src/platform/five-plane-state-evidence/memory/layer-transition-service.ts` 的 `DEFAULT_SIX_LAYER_TRANSITION_RULES` 一致；`tests/unit/platform/reaudit-batch-r24-14-31-60-71.test.ts` 与 `tests/unit/platform/state-evidence/memory/memory-layer-model-functions.test.ts` 已验证阈值阶梯完全对齐。 | 回写 canonical 规则一致性证据，不再把旧 ADR 文案当成现行缺陷。 |
| R24-61 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §122 ADR 实现偏差 + E2E/Integration Tests 缺陷 | P1 | `memory-layer-model.ts:50` | ADR-020 L5→L6 要求 manual-only——实现有自动 rule(minHitCount:20) | 已解决（源码复核并补定向测试） | 原 reaudit 延续了旧 ADR-020 的 manual-only 描述，但当前 canonical 六层晋升规则已经明确包含 `user -> evolution` 自动晋升梯级；问题实质是文档认知滞后。 | `src/platform/five-plane-state-evidence/memory/memory-layer-model.ts` 当前保留 `from: "user", to: "evolution"` 规则，且该规则与 `DEFAULT_SIX_LAYER_TRANSITION_RULES` 同步；`tests/unit/platform/reaudit-batch-r24-14-31-60-71.test.ts` 已把这条自动晋升规则与 canonical 阶梯做一致性断言。 | 按现行 canonical 规则回写，不保留过时的 manual-only 指控。 |
| R24-62 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §122 ADR 实现偏差 + E2E/Integration Tests 缺陷 | P1 | `tests/e2e/workflow-state-transitions.test.ts` | E2E 全测 legacy(TransitionService/WorkflowState)——零 RuntimeStateMachine 覆盖 | 已解决（补 canonical 覆盖并定向验证） | 原条目成立于旧测试文件只有 legacy workflow transition 覆盖的阶段；本轮已把 `RuntimeStateMachine` 的 canonical `HarnessRun` 暂停/恢复路径补进该 E2E 文件。 | `tests/e2e/workflow-state-transitions.test.ts` 现已引入 `RuntimeStateMachine` 与 `createMinimalHarnessRun`，新增 `canonical HarnessRun pause and resume coverage`；整文件定向验证 `./node_modules/.bin/tsx --test tests/e2e/workflow-state-transitions.test.ts` 已通过。 | 关闭“零 RuntimeStateMachine 覆盖”的缺口。 |
| R24-63 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §122 ADR 实现偏差 + E2E/Integration Tests 缺陷 | P1 | `tests/e2e/execution-flow.test.ts` | E2E 8个测试全 legacy path——无 canonical HarnessRun/NodeRun/PlanGraphBundle | 已解决（补 canonical 覆盖并补定向测试） | 原条目成立于旧 `execution-flow` 文件只验证 legacy task/execution/workflow/session 状态链的阶段；本轮已把 canonical `HarnessRun + PlanGraphBundle + NodeRun` 覆盖补入。 | `tests/e2e/execution-flow.test.ts` 现已引入 `createMinimalHarnessRun`、`createMinimalPlanGraphBundle`、`createMinimalNodeRun`，并新增 `includes canonical HarnessRun, PlanGraphBundle, and NodeRun coverage`；`tests/unit/platform/reaudit-batch-r24-14-31-60-71.test.ts` 已对该源文件补做源码审计回归。 | 回写 canonical aggregate 已进入该 E2E 文件的证据。 |
| R24-64 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §122 ADR 实现偏差 + E2E/Integration Tests 缺陷 | P1 | `tests/e2e/task-lifecycle.test.ts` | E2E 10个测试全 legacy——无 RSM.transition(command) 使用 | 已解决（补 canonical 覆盖并补定向测试） | 原条目成立于旧 `task-lifecycle` 文件没有任何 `RuntimeStateMachine.transition(...)` 命令式覆盖的阶段；本轮已加入 canonical `NodeRun` 状态切换测试。 | `tests/e2e/task-lifecycle.test.ts` 现已引入 `RuntimeStateMachine` 与 `createMinimalNodeRun`，新增 `canonical NodeRun transitions use RuntimeStateMachine.transition`；`tests/unit/platform/reaudit-batch-r24-14-31-60-71.test.ts` 已对该源文件补做源码审计回归。 | 回写 `RSM.transition(command)` 已进入任务生命周期 E2E 的现状。 |
| R24-65 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §122 ADR 实现偏差 + E2E/Integration Tests 缺陷 | P1 | `tests/e2e/multi-step-workflow.test.ts` | 直接 updateWorkflowState 绕过 RSM——验证已被 ADR-030 禁止的行为 | 需继续整改（ADR/contract） | ADR、contract、类型导出与现行实现存在漂移，本轮未完成逐条回写与一致性校验闭环。 | 已按源审计条目补录编号；本轮未新增 contract/ADR 回写或一致性测试证据，因此不能改写为已解决。 | 补录到 reaudit，待专项整改。 |
| R24-66 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §122 ADR 实现偏差 + E2E/Integration Tests 缺陷 | P1 | `tests/e2e/harness-loop-e2e.test.ts:347-377` | INV-BUDGET 无 E2E 覆盖——maxCost 是死代码,注释承认 guard 不会触发 | 需继续整改（ADR/contract） | ADR、contract、类型导出与现行实现存在漂移，本轮未完成逐条回写与一致性校验闭环。 | 已按源审计条目补录编号；本轮未新增 contract/ADR 回写或一致性测试证据，因此不能改写为已解决。 | 补录到 reaudit，待专项整改。 |
| R24-67 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §122 ADR 实现偏差 + E2E/Integration Tests 缺陷 | P1 | `tests/e2e/ (全目录)` | 无 PlanGraphBundle→NodeAttemptReceipt 端到端测试——canonical P3→P4→P5 零覆盖 | 已解决（源码复核并补定向测试） | 原 reaudit 没有吸收现有 E2E 目录里已经落地的 canonical `PlanGraphBundle` 端到端覆盖，继续把全目录概括成“零覆盖”。 | `tests/e2e/plan-graph-bundle-dispatch-e2e.test.ts` 已覆盖 `PlanGraphBundle` full execution lifecycle，`tests/e2e/harness-multi-step-orchestration-e2e.test.ts` 已覆盖 `runMultiStepOrchestration` 主链；`tests/unit/platform/reaudit-batch-r24-14-31-60-71.test.ts` 已对这两份文件做源码级回归。 | 回写现有 PlanGraphBundle 端到端覆盖，不保留“全目录零覆盖”的过度结论。 |
| R24-68 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §122 ADR 实现偏差 + E2E/Integration Tests 缺陷 | P2 | `tests/e2e/oapeflir-full-loop.test.ts:19` | OAPEFLIR 独立运行不经 HarnessRuntime——违反 ADR-029 架构分层 | 需继续整改（ADR/contract） | ADR、contract、类型导出与现行实现存在漂移，本轮未完成逐条回写与一致性校验闭环。 | 已按源审计条目补录编号；本轮未新增 contract/ADR 回写或一致性测试证据，因此不能改写为已解决。 | 补录到 reaudit，待专项整改。 |
| R24-69 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §122 ADR 实现偏差 + E2E/Integration Tests 缺陷 | P2 | `risk-evaluation-engine.ts:57` | MAX_POSSIBLE_SCORE=75 vs ADR-026 应为18——归一化分数完全错误 | 需继续整改（ADR/contract） | ADR、contract、类型导出与现行实现存在漂移，本轮未完成逐条回写与一致性校验闭环。 | 已按源审计条目补录编号；本轮未新增 contract/ADR 回写或一致性测试证据，因此不能改写为已解决。 | 补录到 reaudit，待专项整改。 |
| R24-70 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §122 ADR 实现偏差 + E2E/Integration Tests 缺陷 | P2 | `tests/e2e/execution-flow.test.ts:379` | 测试验证 blocked→executing 有效——RSM 明确拒绝此转换 | 已解决（测试修正并补定向审计） | 原问题在旧 E2E 断言中确实存在：`execution-flow` 曾把 `blocked -> executing` 当作合法恢复路径；本轮已按 `RuntimeStateMachine` 语义修正为必须经 `blocked -> prechecking -> executing`。 | `tests/e2e/execution-flow.test.ts` 已移除 `makeExecCommand(executionId, "blocked", "executing", traceId)` 断言，并改为 `blocked -> prechecking`; `tests/unit/platform/reaudit-batch-r24-14-31-60-71.test.ts` 已用源码检查钉住这条修正。 | 关闭 execution-flow E2E 验证非法直接跳转的缺口。 |
| R24-71 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §122 ADR 实现偏差 + E2E/Integration Tests 缺陷 | P2 | `memory-layer-model.ts:157` | L5 supportsPromotion=false 但有 user→evolution rule——模块内自相矛盾 | 已解决（源码修复并补定向测试） | 原问题属真实模块内自相矛盾：旧 `user` 层配置保留 `supportsPromotion=false`，却同时存在 `user -> evolution` promotion 规则；本轮已把层配置与规则对齐。 | `src/platform/five-plane-state-evidence/memory/memory-layer-model.ts` 当前 `user` 层 `supportsPromotion` 已为 `true`；`tests/unit/platform/reaudit-batch-r24-14-31-60-71.test.ts` 与 `tests/unit/platform/state-evidence/memory/memory-layer-model-functions.test.ts` 已验证 `user` 层与 `user -> evolution` 规则保持一致。 | 关闭 memory layer 自相矛盾配置缺口。 |
| R24-72 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §122 ADR 实现偏差 + E2E/Integration Tests 缺陷 | P2 | `tests/e2e/ (~75文件)` | ~95% E2E 仅测 legacy 类型——canonical runtime 接近零 E2E 覆盖 | 需继续整改（ADR/contract） | ADR、contract、类型导出与现行实现存在漂移，本轮未完成逐条回写与一致性校验闭环。 | 已按源审计条目补录编号；本轮未新增 contract/ADR 回写或一致性测试证据，因此不能改写为已解决。 | 补录到 reaudit，待专项整改。 |
| R25-01 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §124 Platform Interface / API / Channel Gateway 缺陷 | P0 | `request-helpers.ts:19` | matchRoute 仅允许 GET/POST/OPTIONS——PATCH/DELETE handler 完全不可达(task-routes 定义但永远404) | 已解决（源码复核并补定向测试） | 原条目基于旧快照；当前 `matchRoute()` 早已显式接受 `PUT/PATCH/DELETE/OPTIONS`。 | `src/platform/five-plane-interface/api/http-server/request-helpers.ts` 当前 method 白名单已包含 `PUT/PATCH/DELETE`；`tests/unit/platform/interface/api/http-server/request-helpers.test.ts` 与 `tests/unit/platform/reaudit-batch-r25-01-13.test.ts` 已更新并验证这些方法不再返回 `null`。 | 回写陈旧结论，不把旧测试断言继续当成生产缺口。 |
| R25-02 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §124 Platform Interface / API / Channel Gateway 缺陷 | P0 | `response-hardening.ts:13` | CORS allowedOrigins=["*"]+credentials=true——生产默认 wildcard,§6 明确禁止 | 已解决（源码复核并补定向测试） | 原条目基于旧默认配置；现行实现默认 `allowedOrigins=[]`、`credentials=false`，且会拒绝 wildcard+credentials。 | `src/platform/five-plane-interface/api/http-server/response-hardening.ts` 的 `DEFAULT_CORS_CONFIG` 现为 fail-closed，`normalizeCorsConfig()` 对 wildcard+credentials 直接抛 `api.cors.invalid_wildcard_credentials`；`tests/unit/platform/interface/api/http-server/response-hardening.test.ts` 已新增回归。 | 关闭的是错误的默认配置指控，不外推为所有部署环境都已正确配置允许源。 |
| R25-03 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §124 Platform Interface / API / Channel Gateway 缺陷 | P1 | `api-error.ts:65-77` | instanceof 检查顺序错——GatewayTarget\*Error extends AppError 被先匹配为 AppError,特殊处理死代码 | 已解决（源码修复并补定向测试） | 原问题属真实错误归一化顺序缺口；gateway target lookup error 以前确实会先命中 `AppError` 分支。 | 本轮已把 `GatewayTargetNotFoundError/GatewayTargetAmbiguousError` 提前到 `AppError` 分支前处理；`tests/unit/platform/interface/api/http-server/api-error.test.ts` 与 `tests/unit/platform/reaudit-batch-r25-01-13.test.ts` 已验证映射回 `gateway.target_*` API error。 | 关闭 gateway target error 特殊映射死代码缺口。 |
| R25-04 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §124 Platform Interface / API / Channel Gateway 缺陷 | P1 | `utils.ts:192 / http-api-server.ts:604` | 错误响应缺 traceId 和 details 字段——不符 §7 标准化错误格式 | 已解决（源码修复并补定向测试） | 原问题属真实响应格式缺口；旧 JSON error body 只回 `{ requestId, error }`。 | 本轮已把 `buildJsonErrorResponse()` 与 `HttpApiServer.handleError()` 统一扩展为输出 `traceId` 和 `error.details`，并优先透传 `AppError.traceId/details`；`tests/unit/platform/reaudit-batch-r25-01-13.test.ts` 已覆盖 helper 和 server 两条链。 | 关闭标准化错误响应字段缺失问题。 |
| R25-05 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §124 Platform Interface / API / Channel Gateway 缺陷 | P1 | `channel-gateway-delivery-service.ts:287` | generateNonce 产生16字节而非32字节熵(64 hex truncate to 32) | 已解决（源码修复并补定向测试） | 原问题属真实熵截断；旧实现把 `randomBytes(32)` 的 64 hex chars 截成了 32 chars。 | 本轮已改为返回完整 hex 输出，不再 `.slice(0, length)`；`tests/unit/platform/reaudit-batch-r25-01-13.test.ts` 已断言默认 nonce 为 64 个 hex 字符。 | 关闭 nonce 熵被截断的问题。 |
| R25-06 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §124 Platform Interface / API / Channel Gateway 缺陷 | P1 | `channel-gateway-delivery-service.ts:336` | createDeliveryMessage 未尝试即返回 finalStatus:"success"——消费者收到虚假投递确认 | 已解决（源码修复并补定向测试） | 原问题属真实 receipt 语义错误；旧创建路径在零 attempt 时就返回了 success。 | 本轮已把新建 receipt 的 `finalStatus` 改为 `pending`，`getDeliveryReceipt()` 对未完成 message 也返回 `pending`；`tests/unit/platform/reaudit-batch-r25-01-13.test.ts` 已验证创建态和回读态都不再伪造 success。 | 关闭虚假投递确认缺口。 |
| R25-07 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §124 Platform Interface / API / Channel Gateway 缺陷 | P1 | `channel-gateway-delivery-service.ts:77-82` | Rate limiting per-channel 而非 per-tenant——§7 要求 per-tenant | 已解决（源码修复并补定向测试） | 原问题属真实限流维度缺口；旧 bucket 只有 channel/window，没有 tenant scope。 | 本轮给 `ChannelGatewayDeliveryService.checkRateLimit()/recordRateLimitHit()` 增加 tenant bucket，`ChannelGatewayService.sendMessage()/processRetryQueue()` 也已把 `tenantId` 带入发送与重试主链；`tests/unit/platform/reaudit-batch-r25-01-13.test.ts` 已验证 tenant-a 的 bucket 打满后不会连带 tenant-b。 | 关闭 gateway delivery rate limit 缺 tenant 隔离问题。 |
| R25-08 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §124 Platform Interface / API / Channel Gateway 缺陷 | P1 | `websocket-bridge.ts:97-160` | 无 server-initiated heartbeat——死连接不可检测,clients map 无界增长 | 已解决（源码修复并补定向测试） | 原问题属真实连接保活缺口；旧 bridge 只有 client 发来的 app-level ping/pong，没有服务端主动心跳。 | 本轮已给 `WebSocketBridge` 增加定时 heartbeat sweep：对活跃连接发 `ws.ping()`，对未响应连接做清理并 terminate；`tests/unit/platform/reaudit-batch-r25-01-13.test.ts` 已验证 sweep 会 ping 活连接并清理 dead client。 | 关闭 WebSocket 死连接不可检测问题。 |
| R25-09 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §124 Platform Interface / API / Channel Gateway 缺陷 | P2 | `stream-bridge.ts:180-395` | 无 backpressure——producer 无流控信号,replay buffer 无界增长 | 已解决（源码复核并补定向测试） | 原条目把“无界增长”与旧实现混在了一起；当前 `StreamBridge` replay buffer 早已是有界窗口，不存在审计所述无界增长。 | `src/platform/five-plane-interface/channel-gateway/stream-bridge.ts` 当前按 `maxReplayFrames` 保持有限 replay buffer，并在溢出时优先淘汰 droppable frame、保留 critical frame；`tests/unit/platform/reaudit-batch-r25-01-13.test.ts` 已验证缓冲区维持上界。 | 关闭这条“replay buffer 无界增长”的过时指控。 |
| R25-10 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §124 Platform Interface / API / Channel Gateway 缺陷 | P2 | `websocket-bridge.ts:133-139` | 无 WS 消息大小限制——可 OOM DoS | 已解决（源码修复并补定向测试） | 原问题属真实 ingress 限制缺口；旧 `WebSocketServer` 构造没有配置 `maxPayload`。 | 本轮已给 `WebSocketBridge` 新增可配置 `maxPayloadBytes`，默认以 `maxPayload` 传给 `ws` server；`tests/unit/platform/reaudit-batch-r25-01-13.test.ts` 已断言配置值实际下发。 | 关闭 WS payload 无上限的问题。 |
| R25-11 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §124 Platform Interface / API / Channel Gateway 缺陷 | P2 | `websocket-bridge.ts:128-129` | 无 tenant scope 检查——任何用户可订阅任何 task 更新 | 已解决（源码复核并补定向测试） | 原条目基于 tenant scope filter 接线前的旧快照；当前 bridge 已在订阅和广播两侧重做 scope 校验。 | `subscribeToTask()` 与 `broadcastToTask()` 现都通过 `TenantScopeFilter.evaluate(...)` 校验 principal/task scope；`tests/unit/platform/reaudit-batch-r25-01-13.test.ts` 已直接验证越租户订阅返回 `scope_denied`。 | 回写现行 tenant scope 收口链，不再保留“任何用户可订阅任何 task”的旧结论。 |
| R25-12 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §124 Platform Interface / API / Channel Gateway 缺陷 | P2 | `http-api-server.ts:375-378` | Rate limit key 含 path params——每个唯一 taskId 独立 bucket,有效无限流 | 已解决（源码修复并补定向测试） | 原问题属真实 endpoint key 归一化缺口；旧实现直接把 pathname 原样当 bucket key。 | 本轮已在 `HttpApiServer.extractEndpointKey()` 中把 `/tasks/:id`、`/tasks/:id/events`、`/workflows/:id` 等动态路径参数归一化成 canonical endpoint key；`tests/unit/platform/reaudit-batch-r25-01-13.test.ts` 已覆盖这些路径。 | 关闭 taskId/path-param 稀释限流桶的问题。 |
| R25-13 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §124 Platform Interface / API / Channel Gateway 缺陷 | P2 | `http-api-server.ts:362-368` | Body read 在 timeout wrapper 外——slow-drip request 绕过超时保护 | 已解决（源码修复并补定向测试） | 原问题属真实 timeout 覆盖缺口；旧 `routeRequest()` 在读取 body 后才进入 timeout 保护。 | 本轮已把 `readIncomingBody(request)` 收进 `withRequestTimeout(...)` 包裹范围，并让真实 socket 请求沿单次 request timeout 预算覆盖 body read + dispatch；`tests/unit/platform/reaudit-batch-r25-01-13.test.ts` 已用 delayed body 直接验证会抛 `api.request_timeout`。 | 关闭 slow-drip request 绕过 timeout 主链的问题。 |
| R25-14 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §125 Platform Contracts / Domain Types / Dispatch 缺陷 | P0 | `contracts/state-command/index.ts:6-15` | StateCommand 缺 §5.3 必须字段(leaseId/fencingToken/event/principal/traceId/expectedStatus) | 需继续整改（ADR/contract） | ADR、contract、类型导出与现行实现存在漂移，本轮未完成逐条回写与一致性校验闭环。 | 已按源审计条目补录编号；本轮未新增 contract/ADR 回写或一致性测试证据，因此不能改写为已解决。 | 补录到 reaudit，待专项整改。 |
| R25-15 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §125 Platform Contracts / Domain Types / Dispatch 缺陷 | P0 | `contracts/types/domain/core-types.ts:72-77` | TransitionCommand 缺 leaseId/fencingToken/event/payload/expectedVersion/principal | 需继续整改（ADR/contract） | ADR、contract、类型导出与现行实现存在漂移，本轮未完成逐条回写与一致性校验闭环。 | 已按源审计条目补录编号；本轮未新增 contract/ADR 回写或一致性测试证据，因此不能改写为已解决。 | 补录到 reaudit，待专项整改。 |
| R25-16 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §125 Platform Contracts / Domain Types / Dispatch 缺陷 | P0 | `domain types (全目录)` | §5.5 五核心对象(HarnessRun/NodeRun/NodeAttempt/BudgetReservation/SideEffectRecord)均不存在 | 需继续整改（ADR/contract） | ADR、contract、类型导出与现行实现存在漂移，本轮未完成逐条回写与一致性校验闭环。 | 已按源审计条目补录编号；本轮未新增 contract/ADR 回写或一致性测试证据，因此不能改写为已解决。 | 补录到 reaudit，待专项整改。 |
| R25-17 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §125 Platform Contracts / Domain Types / Dispatch 缺陷 | P1 | `contracts/execution-plan/index.ts:4-19` | 第三份重复 ExecutionPlan——仍用 flat steps[] 非 PlanGraph(node/edge/entry/terminal) | 需继续整改（ADR/contract） | ADR、contract、类型导出与现行实现存在漂移，本轮未完成逐条回写与一致性校验闭环。 | 已按源审计条目补录编号；本轮未新增 contract/ADR 回写或一致性测试证据，因此不能改写为已解决。 | 补录到 reaudit，待专项整改。 |
| R25-18 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §125 Platform Contracts / Domain Types / Dispatch 缺陷 | P1 | `contracts/execution-receipt/index.ts:6-17` | Legacy ExecutionReceipt 作一等 contract 导出——缺 harnessRunId/planGraphId/nodeRunId/attemptId | 需继续整改（ADR/contract） | ADR、contract、类型导出与现行实现存在漂移，本轮未完成逐条回写与一致性校验闭环。 | 已按源审计条目补录编号；本轮未新增 contract/ADR 回写或一致性测试证据，因此不能改写为已解决。 | 补录到 reaudit，待专项整改。 |
| R25-19 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §125 Platform Contracts / Domain Types / Dispatch 缺陷 | P1 | `contracts/model-request/index.ts:9-18` | ModelRequest 无 budgetReservationId——LLM 调用不引用预算预留 | 需继续整改（ADR/contract） | ADR、contract、类型导出与现行实现存在漂移，本轮未完成逐条回写与一致性校验闭环。 | 已按源审计条目补录编号；本轮未新增 contract/ADR 回写或一致性测试证据，因此不能改写为已解决。 | 补录到 reaudit，待专项整改。 |
| R25-20 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §125 Platform Contracts / Domain Types / Dispatch 缺陷 | P1 | `contracts/delegation-request/index.ts:6-17` | DelegationRequest 无 budgetEnvelope/budgetReservationId | 需继续整改（ADR/contract） | ADR、contract、类型导出与现行实现存在漂移，本轮未完成逐条回写与一致性校验闭环。 | 已按源审计条目补录编号；本轮未新增 contract/ADR 回写或一致性测试证据，因此不能改写为已解决。 | 补录到 reaudit，待专项整改。 |
| R25-21 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §125 Platform Contracts / Domain Types / Dispatch 缺陷 | P1 | `contracts/types/domain/primitives.ts:55` | RunKind 缺 "node_run"——canonical 最小执行单元无法表达 | 需继续整改（ADR/contract） | ADR、contract、类型导出与现行实现存在漂移，本轮未完成逐条回写与一致性校验闭环。 | 已按源审计条目补录编号；本轮未新增 contract/ADR 回写或一致性测试证据，因此不能改写为已解决。 | 补录到 reaudit，待专项整改。 |
| R25-22 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §125 Platform Contracts / Domain Types / Dispatch 缺陷 | P1 | `contracts/types/domain/execution-types.ts:33-61` | ExecutionRecord 用 agentId/roleId 而非 nodeRunId/planGraphId——身份模型偏离 canonical | 需继续整改（ADR/contract） | ADR、contract、类型导出与现行实现存在漂移，本轮未完成逐条回写与一致性校验闭环。 | 已按源审计条目补录编号；本轮未新增 contract/ADR 回写或一致性测试证据，因此不能改写为已解决。 | 补录到 reaudit，待专项整改。 |
| R25-23 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §125 Platform Contracts / Domain Types / Dispatch 缺陷 | P1 | `contracts/types/domain/workspace-types.ts:101` | TenantRecord.quotas optional——dispatch 可绕过租户配额限制 | 需继续整改（ADR/contract） | ADR、contract、类型导出与现行实现存在漂移，本轮未完成逐条回写与一致性校验闭环。 | 已按源审计条目补录编号；本轮未新增 contract/ADR 回写或一致性测试证据，因此不能改写为已解决。 | 补录到 reaudit，待专项整改。 |
| R25-24 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §125 Platform Contracts / Domain Types / Dispatch 缺陷 | P2 | `contracts/projection-update/index.ts` | 从 legacy platform-contracts.ts re-export 而非 executable-contracts/ | 需继续整改（ADR/contract） | ADR、contract、类型导出与现行实现存在漂移，本轮未完成逐条回写与一致性校验闭环。 | 已按源审计条目补录编号；本轮未新增 contract/ADR 回写或一致性测试证据，因此不能改写为已解决。 | 补录到 reaudit，待专项整改。 |
| R25-25 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §125 Platform Contracts / Domain Types / Dispatch 缺陷 | P2 | `contracts/prompt-bundle/index.ts:8-99` | PromptBundle 无 factory/validation 函数——无输入验证 | 需继续整改（ADR/contract） | ADR、contract、类型导出与现行实现存在漂移，本轮未完成逐条回写与一致性校验闭环。 | 已按源审计条目补录编号；本轮未新增 contract/ADR 回写或一致性测试证据，因此不能改写为已解决。 | 补录到 reaudit，待专项整改。 |
| R25-26 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §126 ADR 001-019 + Stability 缺陷 | P1 | `ADR-005 / harness/index.ts:60` | ConstraintPack.autonomyMode 用 legacy 4值 enum——ADR-005 canonical 为8-mode UnifiedRuntimeMode | 需继续整改（ADR/contract） | ADR、contract、类型导出与现行实现存在漂移，本轮未完成逐条回写与一致性校验闭环。 | 已按源审计条目补录编号；本轮未新增 contract/ADR 回写或一致性测试证据，因此不能改写为已解决。 | 补录到 reaudit，待专项整改。 |
| R25-27 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §126 ADR 001-019 + Stability 缺陷 | P1 | `ADR-007+001 / learning-object-model.ts:5` | learningType 5种超出 ADR-007 Phase 1 scope(仅3种)——无 superseding ADR | 需继续整改（ADR/contract） | ADR、contract、类型导出与现行实现存在漂移，本轮未完成逐条回写与一致性校验闭环。 | 已按源审计条目补录编号；本轮未新增 contract/ADR 回写或一致性测试证据，因此不能改写为已解决。 | 补录到 reaudit，待专项整改。 |
| R25-28 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §126 ADR 001-019 + Stability 缺陷 | P1 | `ADR-019 / handoff-serializer.ts:7-13` | Layer 语义反转(L1=FactLayer vs ADR L1=ContextSummary) + 无 L4(Full) + token budgets 不匹配 | 需继续整改（ADR/contract） | ADR、contract、类型导出与现行实现存在漂移，本轮未完成逐条回写与一致性校验闭环。 | 已按源审计条目补录编号；本轮未新增 contract/ADR 回写或一致性测试证据，因此不能改写为已解决。 | 补录到 reaudit，待专项整改。 |
| R25-29 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §126 ADR 001-019 + Stability 缺陷 | P1 | `ADR-016 / oapeflir-loop-service.ts:86` | ADR-016 A-1 废弃独立 OapeflirLoopService——仍作为 primary export 独立编排执行 | 需继续整改（ADR/contract） | ADR、contract、类型导出与现行实现存在漂移，本轮未完成逐条回写与一致性校验闭环。 | 已按源审计条目补录编号；本轮未新增 contract/ADR 回写或一致性测试证据，因此不能改写为已解决。 | 补录到 reaudit，待专项整改。 |
| R25-30 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §126 ADR 001-019 + Stability 缺陷 | P2 | `ADR-003 / 003-memory-seven-layers.md` | 文件名 "seven-layers" 实际内容为6层——文件名过时 | 需继续整改（ADR/contract） | ADR、contract、类型导出与现行实现存在漂移，本轮未完成逐条回写与一致性校验闭环。 | 已按源审计条目补录编号；本轮未新增 contract/ADR 回写或一致性测试证据，因此不能改写为已解决。 | 补录到 reaudit，待专项整改。 |
| R25-31 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §126 ADR 001-019 + Stability 缺陷 | P2 | `stability/stable-evidence-bundle.ts` | Evidence bundle 遗漏 3个 rehearsal(dispatch/worker-handshake/worker-writeback)——release gate 不验证 | 需继续整改（ADR/contract） | ADR、contract、类型导出与现行实现存在漂移，本轮未完成逐条回写与一致性校验闭环。 | 已按源审计条目补录编号；本轮未新增 contract/ADR 回写或一致性测试证据，因此不能改写为已解决。 | 补录到 reaudit，待专项整改。 |
| R25-32 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §126 ADR 001-019 + Stability 缺陷 | P2 | `ADR-005 / agent-registry/index.ts:52` | Agent registry 用独立4值 autonomy enum——与 ConstraintPack 同问题但不同子系统,两套不互通 | 需继续整改（ADR/contract） | ADR、contract、类型导出与现行实现存在漂移，本轮未完成逐条回写与一致性校验闭环。 | 已按源审计条目补录编号；本轮未新增 contract/ADR 回写或一致性测试证据，因此不能改写为已解决。 | 补录到 reaudit，待专项整改。 |
| R25-33 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §126 ADR 001-019 + Stability 缺陷 | P2 | `ADR-019 / handoff-model.ts` | 无 HandoffLevel enum——serializeHandoff 仅接受 totalMaxTokens,不支持 per-level 查询 | 需继续整改（ADR/contract） | ADR、contract、类型导出与现行实现存在漂移，本轮未完成逐条回写与一致性校验闭环。 | 已按源审计条目补录编号；本轮未新增 contract/ADR 回写或一致性测试证据，因此不能改写为已解决。 | 补录到 reaudit，待专项整改。 |
| R25-34 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §127 UI Mobile / Charts / Feature Mutations 缺陷 | P0 | `vitest.config.ts:16-19` | Coverage 阈值 30/30/30/20%——§7.2.6 要求 >80% | 已解决（源码） | 根因是该条沿用了旧版 `ui/vitest.config.ts` 的阈值快照，没有吸收后续按分层阈值回写的现行配置。 | 当前 `ui/vitest.config.ts` 已按 `90/80/70/50` 的分层阈值配置 coverage gate，不再是审计描述的 `30/30/30/20`。 | 该条关闭依据来自现行源码本身；本轮没有跑全量 coverage，只关闭“阈值字面量仍然过低”这个已过时子问题。 |
| R25-35 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §127 UI Mobile / Charts / Feature Mutations 缺陷 | P0 | `mobile/App.tsx:7` | 渲染 div/span(React DOM)——§11 要求 React Native 0.79+Hermes+Fabric | 需继续整改（UI 产品化差距） | UI、壳层或共享包仍有 stub、缺件、性能预算或安全能力未落地，本轮未完成对应实装。 | 已按源审计条目补录编号；本轮未执行足以证明修复完成的 UI 全链路 build/e2e/壳层验证。 | 补录到 reaudit，待专项整改。 |
| R25-36 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §127 UI Mobile / Charts / Feature Mutations 缺陷 | P0 | `ws-client.ts:100-102` | onclose 后永不重连——WS 永久丢失违反 §5.3 UP-5 | 已解决（源码/测试） | 根因与 `R22-04` 相同：该编号是对同一 `ws-client` 重连路径的重复挂账，前次 reaudit 没有把已有的 backoff reconnect 逻辑和定向验证吸收进结论。 | 当前 `ui/packages/shared/api-client/src/ws-client.ts` 的 `BrowserWSClient` 已在 `onclose/onerror` 上调用 `scheduleReconnect()`，并配有指数退避；本轮又通过 `R22-11` 把 heartbeat 探针补上，确保静默断开也能进入同一重连链。 | 关闭依据与 `R22-04/R22-11` 共用：`ui/tests/shared/api-client-runtime-regressions.test.ts`、`ui/tests/unit/ui/shared/ws-client.test.ts` 本轮均通过。 |
| R25-37 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §127 UI Mobile / Charts / Feature Mutations 缺陷 | P0 | `task-cockpit/hooks:58-78` | mutation 仅 setState 无 API 调用——optimistic update with rollback 完全缺失(同 workflow/approval) | 已解决（源码/测试） | 复核后发现这条混了两代问题：最近源码其实已经会调用 `updateTask(...)`，但的确仍是“等 API 成功后再改本地状态”的悲观写法，没有乐观更新与失败回滚。 | 本轮把 `claimTask/resumeTask/escalateTask` 都改成先更新本地任务与 timeline，再调用 `updateTask(...)`；若后端失败，会回滚 `tasks/timelineItems/timelineEvents` 到调用前快照，不再出现“界面已改但失败后卡在错误状态”或“完全无 rollback”的问题。 | 定向验证已通过：`ui/tests/unit/ui/packages/features/task-cockpit/hooks.test.tsx` 既验证三条 mutation 会命中真实 REST helper，也新增回归覆盖 API 失败时 owner/status/timeline 会恢复到原值。 |
| R25-38 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §127 UI Mobile / Charts / Feature Mutations 缺陷 | P1 | `charts/echart-surface-runtime.tsx:8` | 缺 DataZoomComponent——§4.2.8 要求 data zoom | 已解决（源码/测试） | 根因是图表 runtime 以前只注册了 `GridComponent/TooltipComponent`，没有把 ECharts 的 `DataZoomComponent` 接进 runtime option。 | 本轮在 `ui/packages/ui-core/src/charts/echart-surface-runtime.tsx` 注册并配置了 `inside + slider` 两个 data zoom 组件。 | 定向验证已通过：`ui/tests/shared/ui-core-chart-regressions.test.tsx` 直接断言 `setOption()` 收到两段 `dataZoom` 配置，本轮测试通过。 |
| R25-39 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §127 UI Mobile / Charts / Feature Mutations 缺陷 | P1 | `charts/echart-surface-runtime.tsx:38,46` | 硬编码颜色绕过 theme——§6.3.3 要求 theme-aware chart colors | 已解决（源码/测试） | 根因是图表 runtime 之前把 `splitLine` 和 `areaStyle` 颜色硬编码成固定 RGBA 字面量，只让折线颜色走 theme。 | 本轮在 `ui/packages/ui-core/src/charts/echart-surface-runtime.tsx` 新增 `withAlpha()`，让 `splitLine`、`slider filler` 和 `areaStyle` 都从传入 `theme.color` 推导，而不是继续写死绿色/灰色字面量。 | 定向验证已通过：`ui/tests/shared/ui-core-chart-regressions.test.tsx` 直接验证传入自定义 accent/border 后，图表 option 会生成对应的 theme-aware RGBA 颜色。 |
| R25-40 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §127 UI Mobile / Charts / Feature Mutations 缺陷 | P1 | `charts/echart-surface-runtime.tsx:52-53` | Resize 用 window event 非 ResizeObserver——sidebar toggle 不触发 reflow | 已解决（源码/测试） | 根因是旧 runtime 只监听 `window.resize`，对侧边栏折叠、面板布局变化这类“容器尺寸变了但窗口没变”的场景没有观测。 | 本轮在 `ui/packages/ui-core/src/charts/echart-surface-runtime.tsx` 增加 `ResizeObserver` 监听容器尺寸变化，并保留 `window.resize` 作为兜底。 | 定向验证已通过：`ui/tests/shared/ui-core-chart-regressions.test.tsx` mock `ResizeObserver` 并验证 runtime 会注册 `observe()`，本轮测试通过。 |
| R25-41 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §127 UI Mobile / Charts / Feature Mutations 缺陷 | P1 | `charts/ (全目录)` | 无 D3 集成——仅 ECharts LineChart,缺 bar/pie/scatter/gauge/heatmap/sparkline(7种中缺6) | 需继续整改（UI 产品化差距） | UI、壳层或共享包仍有 stub、缺件、性能预算或安全能力未落地，本轮未完成对应实装。 | 已按源审计条目补录编号；本轮未执行足以证明修复完成的 UI 全链路 build/e2e/壳层验证。 | 补录到 reaudit，待专项整改。 |
| R25-42 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §127 UI Mobile / Charts / Feature Mutations 缺陷 | P1 | `conversation/hooks/index.ts:18-65` | 无 TanStack Query/无 WS 集成——raw useState 违反 §4/§5.1.2 | 需继续整改（UI 产品化差距） | UI、壳层或共享包仍有 stub、缺件、性能预算或安全能力未落地，本轮未完成对应实装。 | 已按源审计条目补录编号；本轮未执行足以证明修复完成的 UI 全链路 build/e2e/壳层验证。 | 补录到 reaudit，待专项整改。 |
| R25-43 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §127 UI Mobile / Charts / Feature Mutations 缺陷 | P1 | `ui-mobile/navigation/index.ts:3-9` | Tab list 缺 conversation——§4.4.2 NL 为 mobile 主入口 | 需继续整改（UI 产品化差距） | UI、壳层或共享包仍有 stub、缺件、性能预算或安全能力未落地，本轮未完成对应实装。 | 已按源审计条目补录编号；本轮未执行足以证明修复完成的 UI 全链路 build/e2e/壳层验证。 | 补录到 reaudit，待专项整改。 |
| R25-44 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §127 UI Mobile / Charts / Feature Mutations 缺陷 | P1 | `ui-mobile/native-modules/index.ts:1-9` | Native modules 仅 boolean flags——无 NativeModules/TurboModule 绑定 | 需继续整改（UI 产品化差距） | UI、壳层或共享包仍有 stub、缺件、性能预算或安全能力未落地，本轮未完成对应实装。 | 已按源审计条目补录编号；本轮未执行足以证明修复完成的 UI 全链路 build/e2e/壳层验证。 | 补录到 reaudit，待专项整改。 |
| R25-45 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §127 UI Mobile / Charts / Feature Mutations 缺陷 | P1 | `pnpm-workspace.yaml + turbo.json` | 用 pnpm+Turborepo——ADR-UI-001 选型为 npm workspaces | 需继续整改（ADR/contract） | ADR、contract、类型导出与现行实现存在漂移，本轮未完成逐条回写与一致性校验闭环。 | 已按源审计条目补录编号；本轮未新增 contract/ADR 回写或一致性测试证据，因此不能改写为已解决。 | 补录到 reaudit，待专项整改。 |
| R25-46 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §127 UI Mobile / Charts / Feature Mutations 缺陷 | P1 | `state/query-client.ts:4-12` | 缺 refetchOnWindowFocus/refetchOnReconnect/per-domain staleTime | 需继续整改（UI 产品化差距） | UI、壳层或共享包仍有 stub、缺件、性能预算或安全能力未落地，本轮未完成对应实装。 | 已按源审计条目补录编号；本轮未执行足以证明修复完成的 UI 全链路 build/e2e/壳层验证。 | 补录到 reaudit，待专项整改。 |
| R25-47 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §127 UI Mobile / Charts / Feature Mutations 缺陷 | P2 | `charts/echart-surface-runtime.tsx:27-50` | 无 aria enabled/decal——screen reader 无法叙述图表 | 需继续整改（UI 产品化差距） | UI、壳层或共享包仍有 stub、缺件、性能预算或安全能力未落地，本轮未完成对应实装。 | 已按源审计条目补录编号；本轮未执行足以证明修复完成的 UI 全链路 build/e2e/壳层验证。 | 补录到 reaudit，待专项整改。 |
| R25-48 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §127 UI Mobile / Charts / Feature Mutations 缺陷 | P2 | `rest-client.ts:200-218` | 无 Accept-Version header——§1.8 版本协商缺失 | 需继续整改（UI 产品化差距） | UI、壳层或共享包仍有 stub、缺件、性能预算或安全能力未落地，本轮未完成对应实装。 | 已按源审计条目补录编号；本轮未执行足以证明修复完成的 UI 全链路 build/e2e/壳层验证。 | 补录到 reaudit，待专项整改。 |
| R25-49 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §127 UI Mobile / Charts / Feature Mutations 缺陷 | P2 | `analytics/web/index.tsx:8` | 仅单一 LineChart——§4.2.8 要求7种图表类型 | 需继续整改（UI 产品化差距） | UI、壳层或共享包仍有 stub、缺件、性能预算或安全能力未落地，本轮未完成对应实装。 | 已按源审计条目补录编号；本轮未执行足以证明修复完成的 UI 全链路 build/e2e/壳层验证。 | 补录到 reaudit，待专项整改。 |
| R26-01 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §129 Dispatch / Delegation / Lease 缺陷 | P0 | `context-isolator.ts:175` | Division by zero: parent 0 actions→Infinity→匹配>=0.9→授予 FULL 权限 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R26-02 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §129 Dispatch / Delegation / Lease 缺陷 | P0 | `delegation-audit-service.ts:47` | Audit trail 纯内存 array——重启丢失全部委托审计历史 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R26-03 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §129 Dispatch / Delegation / Lease 缺陷 | P1 | `context-isolator.ts:216-220` | MINIMAL 隔离在 resources/actions 为空时回退到 parent 全部权限——权限提升 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R26-04 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §129 Dispatch / Delegation / Lease 缺陷 | P1 | `context-isolator.ts:151` | mergePermissions 不取交集——override resources 可超出 base 范围 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R26-05 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §129 Dispatch / Delegation / Lease 缺陷 | P1 | `context-isolator.ts:281-289` | mergeDomainLists denied 域用交集而非并集——denied 域可被丢弃 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R26-06 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §129 Dispatch / Delegation / Lease 缺陷 | P1 | `invariant-enforcer.ts:82` | checkDepthLimit 比较 message.depth<=context.globalCallDepth——允许无限嵌套 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R26-07 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §129 Dispatch / Delegation / Lease 缺陷 | P1 | `delegation-audit-service.ts:122,140` | recordDelegation Completed/Failed 硬编码 depth:0——实际深度丢失 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R26-08 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §129 Dispatch / Delegation / Lease 缺陷 | P1 | `lease-repository-sqlite.ts:90-94` | updateLeaseStatus 无状态机 guard——允许反向转换(closed→active) | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R26-09 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §129 Dispatch / Delegation / Lease 缺陷 | P2 | `lease-repository-sqlite.ts:102-110` | updateLeaseRelease 无条件覆盖 status=released——已 closed/expired 的也被重写 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R26-10 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §129 Dispatch / Delegation / Lease 缺陷 | P2 | `lease-repository-sqlite.ts:96-99` | updateLeaseHeartbeat 不延长 expires_at——lease TTL 实际不可续期 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R26-11 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §129 Dispatch / Delegation / Lease 缺陷 | P2 | `execution-worker-writeback-service-async.ts:46` | CJS require() in ESM via createRequire——脆弱模块解析,破坏 tree-shaking | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R26-12 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §130 Compliance / Gateway / Eval 缺陷 | P0 | `crypto-shredding-service.ts:encryptRecordForSubject` | 返回值含明文 PII 字段——加密后仍泄漏原始数据 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R26-13 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §130 Compliance / Gateway / Eval 缺陷 | P0 | `compliance/encryption/index.ts:protectValue` | 使用 base64url 编码伪装加密——无密钥、无 IV、可逆 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R26-14 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §130 Compliance / Gateway / Eval 缺陷 | P0 | `dek-manager.ts:markRotated` | 删除旧 DEK——已加密密文永久不可解密 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R26-15 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §130 Compliance / Gateway / Eval 缺陷 | P1 | `fallback/index.ts` | selectFallback 选最便宜模型忽略 tier 兼容性——降级可能违反 SLA | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R26-16 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §130 Compliance / Gateway / Eval 缺陷 | P1 | `degradation/index.ts` | deterministic-hot-path-gate allowed:false 但 routeMode:"deterministic_hot_path_only" 矛盾 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R26-17 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §130 Compliance / Gateway / Eval 缺陷 | P1 | `metric-aggregator/index.ts` | 仅维护计数器——无 real-time windowed rollup/percentile | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R26-18 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §130 Compliance / Gateway / Eval 缺陷 | P1 | `health-scorer/index.ts` | 无 composite indicator weighting——单维度健康评分 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R26-19 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §130 Compliance / Gateway / Eval 缺陷 | P1 | `alert-router/index.ts` | 仅按严重度排序——无实际路由/升级/通知逻辑 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R26-20 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §130 Compliance / Gateway / Eval 缺陷 | P1 | `llm-eval-service.ts` | A/B test 硬编码 mock scores 0.85/0.90——非真实评估 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R26-21 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §130 Compliance / Gateway / Eval 缺陷 | P1 | `execution-outcome-evaluator.ts` | qualityScoreWeights 总和 1.2 非 1.0——加权平均偏高 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R26-22 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §130 Compliance / Gateway / Eval 缺陷 | P1 | `quality-config-loader.ts` | bare catch 吞噬所有错误——配置加载失败静默使用默认值 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R26-23 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §130 Compliance / Gateway / Eval 缺陷 | P1 | `prompt-injection-guard.ts` | 输出含任何 URL 即 block——误杀合法 URL 引用 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R26-24 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §130 Compliance / Gateway / Eval 缺陷 | P2 | `dek-manager.ts:encryptForSubject` | 返回错误 IV（固定值非随机生成） | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R26-25 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §130 Compliance / Gateway / Eval 缺陷 | P2 | `compliance-case-orchestration-service.ts` | governance 调用无 try/catch——异常中断整个合规流程 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R26-26 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §130 Compliance / Gateway / Eval 缺陷 | P2 | `lineage/index.ts` | cloneMetadata shallow clone——嵌套对象共享引用导致污染 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R26-27 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §130 Compliance / Gateway / Eval 缺陷 | P2 | `fallback/index.ts` | attemptedProfiles 含未尝试的 provider——误导重试逻辑 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R26-28 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §130 Compliance / Gateway / Eval 缺陷 | P2 | `cross-provider-judge-service.ts` | "fastest" 策略实为 "cheapest"（按 cost 排序） | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R26-29 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §130 Compliance / Gateway / Eval 缺陷 | P2 | `dashboard-projection-service.ts` | attention queue 按 createdAt 排序非 priority——紧急事项被埋没 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R26-30 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §131 ADR 080-109 + Contract 偏差 | P0 | `ADR-098 vs harness/index.ts` | harness 用 waiting_hitl vs canonical awaiting_hitl——两个冲突状态值共存 | 需继续整改（ADR/contract） | ADR、contract、类型导出与现行实现存在漂移，本轮未完成逐条回写与一致性校验闭环。 | 已按源审计条目补录编号；本轮未新增 contract/ADR 回写或一致性测试证据，因此不能改写为已解决。 | 补录到 reaudit，待专项整改。 |
| R26-31 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §131 ADR 080-109 + Contract 偏差 | P0 | `contracts/model_gateway_routing vs 实现` | ModelRouteRequest 接口完全不同(routeClass/riskLevel vs requestId/taskId/purpose/routingStrategy) | 需继续整改（ADR/contract） | ADR、contract、类型导出与现行实现存在漂移，本轮未完成逐条回写与一致性校验闭环。 | 已按源审计条目补录编号；本轮未新增 contract/ADR 回写或一致性测试证据，因此不能改写为已解决。 | 补录到 reaudit，待专项整改。 |
| R26-32 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §131 ADR 080-109 + Contract 偏差 | P0 | `contracts/model_gateway_routing vs 实现` | ModelRouteDecision 字段完全偏离——contract 和代码无重叠字段 | 需继续整改（ADR/contract） | ADR、contract、类型导出与现行实现存在漂移，本轮未完成逐条回写与一致性校验闭环。 | 已按源审计条目补录编号；本轮未新增 contract/ADR 回写或一致性测试证据，因此不能改写为已解决。 | 补录到 reaudit，待专项整改。 |
| R26-33 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §131 ADR 080-109 + Contract 偏差 | P0 | `contracts/model_gateway_routing vs 实现` | RouteFailureCode 5个 contract 值(no_capacity/budget_exceeded/risk_blocked/provider_down/timeout)全不存在 | 需继续整改（ADR/contract） | ADR、contract、类型导出与现行实现存在漂移，本轮未完成逐条回写与一致性校验闭环。 | 已按源审计条目补录编号；本轮未新增 contract/ADR 回写或一致性测试证据，因此不能改写为已解决。 | 补录到 reaudit，待专项整改。 |
| R26-34 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §131 ADR 080-109 + Contract 偏差 | P1 | `ADR-080 vs learn/index.ts` | learningType 5种超出 Phase 1 scope(仅3种)——实现与 ADR 范围不匹配 | 需继续整改（ADR/contract） | ADR、contract、类型导出与现行实现存在漂移，本轮未完成逐条回写与一致性校验闭环。 | 已按源审计条目补录编号；本轮未新增 contract/ADR 回写或一致性测试证据，因此不能改写为已解决。 | 补录到 reaudit，待专项整改。 |
| R26-35 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §131 ADR 080-109 + Contract 偏差 | P1 | `ADR-080 vs learn/index.ts` | LearningObject 字段名/结构偏离(learningObjectId vs objectId, flat vs union discriminator) | 需继续整改（ADR/contract） | ADR、contract、类型导出与现行实现存在漂移，本轮未完成逐条回写与一致性校验闭环。 | 已按源审计条目补录编号；本轮未新增 contract/ADR 回写或一致性测试证据，因此不能改写为已解决。 | 补录到 reaudit，待专项整改。 |
| R26-36 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §131 ADR 080-109 + Contract 偏差 | P1 | `ADR-093 vs 实现` | ConstraintPack 缺 sandbox_requirement/approval_requirement 字段 | 需继续整改（ADR/contract） | ADR、contract、类型导出与现行实现存在漂移，本轮未完成逐条回写与一致性校验闭环。 | 已按源审计条目补录编号；本轮未新增 contract/ADR 回写或一致性测试证据，因此不能改写为已解决。 | 补录到 reaudit，待专项整改。 |
| R26-37 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §131 ADR 080-109 + Contract 偏差 | P1 | `ADR-097 vs guardrails/` | guardrail layers 仅 tool 重叠——缺 input/planning/memory/output 四层 | 需继续整改（ADR/contract） | ADR、contract、类型导出与现行实现存在漂移，本轮未完成逐条回写与一致性校验闭环。 | 已按源审计条目补录编号；本轮未新增 contract/ADR 回写或一致性测试证据，因此不能改写为已解决。 | 补录到 reaudit，待专项整改。 |
| R26-38 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §131 ADR 080-109 + Contract 偏差 | P1 | `contracts/observability vs metrics-service.ts` | RuntimeMetricsSummary 9/14 维度缺失 | 需继续整改（ADR/contract） | ADR、contract、类型导出与现行实现存在漂移，本轮未完成逐条回写与一致性校验闭环。 | 已按源审计条目补录编号；本轮未新增 contract/ADR 回写或一致性测试证据，因此不能改写为已解决。 | 补录到 reaudit，待专项整改。 |
| R26-39 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §131 ADR 080-109 + Contract 偏差 | P1 | `ADR-096` | 仍用 "phase 8b" 未迁移到 "Ring 2" 术语 | 需继续整改（ADR/contract） | ADR、contract、类型导出与现行实现存在漂移，本轮未完成逐条回写与一致性校验闭环。 | 已按源审计条目补录编号；本轮未新增 contract/ADR 回写或一致性测试证据，因此不能改写为已解决。 | 补录到 reaudit，待专项整改。 |
| R26-40 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §131 ADR 080-109 + Contract 偏差 | P1 | `ADR-106 vs quant-trading/` | trading 无 PreTradeRisk guard/position limits/loss limits | 需继续整改（ADR/contract） | ADR、contract、类型导出与现行实现存在漂移，本轮未完成逐条回写与一致性校验闭环。 | 已按源审计条目补录编号；本轮未新增 contract/ADR 回写或一致性测试证据，因此不能改写为已解决。 | 补录到 reaudit，待专项整改。 |
| R26-41 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §131 ADR 080-109 + Contract 偏差 | P2 | `ADR-108 vs legal/` | legal domain 无 attorney-review 强制流程 | 需继续整改（ADR/contract） | ADR、contract、类型导出与现行实现存在漂移，本轮未完成逐条回写与一致性校验闭环。 | 已按源审计条目补录编号；本轮未新增 contract/ADR 回写或一致性测试证据，因此不能改写为已解决。 | 补录到 reaudit，待专项整改。 |
| R26-42 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §131 ADR 080-109 + Contract 偏差 | P2 | `ADR-105 vs provider-registry` | latency tier "interactive" 不在 ADR 定义中 | 需继续整改（ADR/contract） | ADR、contract、类型导出与现行实现存在漂移，本轮未完成逐条回写与一致性校验闭环。 | 已按源审计条目补录编号；本轮未新增 contract/ADR 回写或一致性测试证据，因此不能改写为已解决。 | 补录到 reaudit，待专项整改。 |
| R26-43 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §131 ADR 080-109 + Contract 偏差 | P2 | `contracts/observability vs 实现` | metrics 命名不符 oapeflir*<stage>*<metric> 规范 | 需继续整改（ADR/contract） | ADR、contract、类型导出与现行实现存在漂移，本轮未完成逐条回写与一致性校验闭环。 | 已按源审计条目补录编号；本轮未新增 contract/ADR 回写或一致性测试证据，因此不能改写为已解决。 | 补录到 reaudit，待专项整改。 |
| R26-44 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §131 ADR 080-109 + Contract 偏差 | P2 | `ADR-081 vs domain-specs.ts` | DomainDescriptor 缺 governancePolicy/interactionPolicy 字段 | 需继续整改（ADR/contract） | ADR、contract、类型导出与现行实现存在漂移，本轮未完成逐条回写与一致性校验闭环。 | 已按源审计条目补录编号；本轮未新增 contract/ADR 回写或一致性测试证据，因此不能改写为已解决。 | 补录到 reaudit，待专项整改。 |
| R26-45 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §132 UI State / API Deep 缺陷 | P0 | `ui/packages/shared/state/stores/auth-store` | 仅 boolean isAuthenticated——缺 token/permissions/user/tenantId/login/logout/refreshToken/switchTenant | 需继续整改（UI 产品化差距） | UI、壳层或共享包仍有 stub、缺件、性能预算或安全能力未落地，本轮未完成对应实装。 | 已按源审计条目补录编号；本轮未执行足以证明修复完成的 UI 全链路 build/e2e/壳层验证。 | 补录到 reaudit，待专项整改。 |
| R26-46 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §132 UI State / API Deep 缺陷 | P0 | `ui/packages/shared/state/stores/auth-store` | 无 auth state machine(unauthenticated→authenticating→authenticated→refreshing→expired) | 需继续整改（UI 产品化差距） | UI、壳层或共享包仍有 stub、缺件、性能预算或安全能力未落地，本轮未完成对应实装。 | 已按源审计条目补录编号；本轮未执行足以证明修复完成的 UI 全链路 build/e2e/壳层验证。 | 补录到 reaudit，待专项整改。 |
| R26-47 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §132 UI State / API Deep 缺陷 | P0 | `ui/packages/shared/api-client/ws-client` | WS 从不重连 + 无 event replay——断线后状态永久丢失 | 部分修复（已具备重连与心跳，event replay 仍缺） | 根因已从“完全没有断线恢复能力”收敛为两个子问题：`BrowserWSClient` 和 shared worker 侧的指数退避重连、心跳探活已落地，但断线后的 last-event-id、cursor replay、离线期间事件补放仍未建立。 | 当前 `ui/packages/shared/api-client/src/ws-client.ts` 与 `shared-ws-worker.ts` 已实现 `scheduleReconnect()`、指数退避、`ping/pong` 心跳和超时主动断连；`ui/tests/unit/ui/shared/ws-client.test.ts` 已验证缺失 `pong` 时会重连。仍然缺少的是 event replay 协议本身，源码中没有 replay cursor、lastEventId 或持久缓冲恢复。 | 回写为部分解决：关闭“从不重连”旧结论，保留 event replay 缺口继续开放。 |
| R26-48 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §132 UI State / API Deep 缺陷 | P0 | `ui/packages/shared/state/stores/\*` | 4个 Zustand stores 零 middleware(无 persist/devtools/immer) | 需继续整改（UI 产品化差距） | UI、壳层或共享包仍有 stub、缺件、性能预算或安全能力未落地，本轮未完成对应实装。 | 已按源审计条目补录编号；本轮未执行足以证明修复完成的 UI 全链路 build/e2e/壳层验证。 | 补录到 reaudit，待专项整改。 |
| R26-49 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §132 UI State / API Deep 缺陷 | P0 | `ui/packages/shared/state/stores/\*` | useAuthState/useUiState/useSyncState select 全量 store——破坏 selector-based 性能优化 | 需继续整改（UI 产品化差距） | UI、壳层或共享包仍有 stub、缺件、性能预算或安全能力未落地，本轮未完成对应实装。 | 已按源审计条目补录编号；本轮未执行足以证明修复完成的 UI 全链路 build/e2e/壳层验证。 | 补录到 reaudit，待专项整改。 |
| R26-50 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §132 UI State / API Deep 缺陷 | P1 | `ui/packages/shared/state/stores/ui-store` | 缺 theme/sidebar/NL panel/command palette state | 需继续整改（UI 产品化差距） | UI、壳层或共享包仍有 stub、缺件、性能预算或安全能力未落地，本轮未完成对应实装。 | 已按源审计条目补录编号；本轮未执行足以证明修复完成的 UI 全链路 build/e2e/壳层验证。 | 补录到 reaudit，待专项整改。 |
| R26-51 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §132 UI State / API Deep 缺陷 | P1 | `ui/packages/shared/state/stores/realtime-store` | 缺 subscription tracking/incident counters | 需继续整改（UI 产品化差距） | UI、壳层或共享包仍有 stub、缺件、性能预算或安全能力未落地，本轮未完成对应实装。 | 已按源审计条目补录编号；本轮未执行足以证明修复完成的 UI 全链路 build/e2e/壳层验证。 | 补录到 reaudit，待专项整改。 |
| R26-52 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §132 UI State / API Deep 缺陷 | P1 | `ui/packages/shared/state/stores/sync-store` | 缺 online flag/conflicts/error status/retrySync | 需继续整改（UI 产品化差距） | UI、壳层或共享包仍有 stub、缺件、性能预算或安全能力未落地，本轮未完成对应实装。 | 已按源审计条目补录编号；本轮未执行足以证明修复完成的 UI 全链路 build/e2e/壳层验证。 | 补录到 reaudit，待专项整改。 |
| R26-53 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §132 UI State / API Deep 缺陷 | P1 | `ui/packages/shared/state/query-client` | staleTime 30s vs spec 5min + 缺 refetchOnWindowFocus/Reconnect | 需继续整改（UI 产品化差距） | UI、壳层或共享包仍有 stub、缺件、性能预算或安全能力未落地，本轮未完成对应实装。 | 已按源审计条目补录编号；本轮未执行足以证明修复完成的 UI 全链路 build/e2e/壳层验证。 | 补录到 reaudit，待专项整改。 |
| R26-54 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §132 UI State / API Deep 缺陷 | P1 | `ui/packages/shared/api-client/interceptors` | 缺 RetryInterceptor/DedupeInterceptor | 需继续整改（UI 产品化差距） | UI、壳层或共享包仍有 stub、缺件、性能预算或安全能力未落地，本轮未完成对应实装。 | 已按源审计条目补录编号；本轮未执行足以证明修复完成的 UI 全链路 build/e2e/壳层验证。 | 补录到 reaudit，待专项整改。 |
| R26-55 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §132 UI State / API Deep 缺陷 | P1 | `ui/packages/shared/api-client/endpoints` | 缺 body/response type fields——类型不安全 | 需继续整改（UI 产品化差距） | UI、壳层或共享包仍有 stub、缺件、性能预算或安全能力未落地，本轮未完成对应实装。 | 已按源审计条目补录编号；本轮未执行足以证明修复完成的 UI 全链路 build/e2e/壳层验证。 | 补录到 reaudit，待专项整改。 |
| R26-56 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §132 UI State / API Deep 缺陷 | P1 | `ui/packages/shared/api-client/interceptors` | auth interceptor 静态 token 无动态获取/401 refresh/并发队列 | 需继续整改（UI 产品化差距） | UI、壳层或共享包仍有 stub、缺件、性能预算或安全能力未落地，本轮未完成对应实装。 | 已按源审计条目补录编号；本轮未执行足以证明修复完成的 UI 全链路 build/e2e/壳层验证。 | 补录到 reaudit，待专项整改。 |
| R26-57 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §132 UI State / API Deep 缺陷 | P1 | `ui/packages/shared/api-client/ws-event-router` | 无 heartbeat 管理——无法检测 zombie 连接 | 已解决（源码/测试；原定位错误） | 根因不是 `ws-event-router` 需要自己做 heartbeat，而是原审计把“连接存活探测”误归到了事件路由层。现行 heartbeat 位于真正持有 socket 的 `ws-client` 和 shared worker 层。 | 当前 `ui/packages/shared/api-client/src/ws-client.ts` 与 `shared-ws-worker.ts` 都已实现 `ping/pong` 心跳、deadline 超时关闭和随后重连；`ui/tests/unit/ui/shared/ws-client.test.ts` 已验证缺失 `pong` 时会进入重连，收到 `pong` 时会清除 deadline。 | 关闭的是“无 heartbeat 管理”的旧结论，不把映射层职责和连接层职责继续混写。 |
| R26-58 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §132 UI State / API Deep 缺陷 | P1 | `ui/turbo.json` | 无 inputs/outputs hash/globalDependencies/env passthrough | 需继续整改（UI 产品化差距） | UI、壳层或共享包仍有 stub、缺件、性能预算或安全能力未落地，本轮未完成对应实装。 | 已按源审计条目补录编号；本轮未执行足以证明修复完成的 UI 全链路 build/e2e/壳层验证。 | 补录到 reaudit，待专项整改。 |
| R26-59 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §132 UI State / API Deep 缺陷 | P1 | `ui/apps/web/vite.config.ts` | 无 source-map staging-only 配置 | 需继续整改（UI 产品化差距） | UI、壳层或共享包仍有 stub、缺件、性能预算或安全能力未落地，本轮未完成对应实装。 | 已按源审计条目补录编号；本轮未执行足以证明修复完成的 UI 全链路 build/e2e/壳层验证。 | 补录到 reaudit，待专项整改。 |
| R26-60 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §132 UI State / API Deep 缺陷 | P1 | `ui/packages/features/\*` | 28个 feature 无 i18n keys/独立 route definition | 需继续整改（UI 产品化差距） | UI、壳层或共享包仍有 stub、缺件、性能预算或安全能力未落地，本轮未完成对应实装。 | 已按源审计条目补录编号；本轮未执行足以证明修复完成的 UI 全链路 build/e2e/壳层验证。 | 补录到 reaudit，待专项整改。 |
| R26-61 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §132 UI State / API Deep 缺陷 | P2 | `ui/apps/web/main.tsx` | UiRuntimeProvider 硬编码 mock token | 需继续整改（UI 产品化差距） | UI、壳层或共享包仍有 stub、缺件、性能预算或安全能力未落地，本轮未完成对应实装。 | 已按源审计条目补录编号；本轮未执行足以证明修复完成的 UI 全链路 build/e2e/壳层验证。 | 补录到 reaudit，待专项整改。 |
| R26-62 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §132 UI State / API Deep 缺陷 | P2 | `ui/packages/shared/api-client/rest-client` | DefaultRESTClient 默认 MockTransport——生产请求不发出 | 已解决（默认改走 same-origin HTTP transport） | 根因是 `createDefaultTransport()` 和 `createRuntimeRESTClient()` 在未显式传 `baseUrl` 时都回落到 `MockTransport`，导致运行时环境即使在浏览器内也可能根本不发真实请求。 | 当前 `ui/packages/shared/api-client/src/rest-client.ts` 已改为默认指向 same-origin `"/api/v1"` HTTP transport；仅显式要求 mock/fallback 时才会使用 mock 行为。新增 `ui/tests/shared/api-client-default-transport.test.ts` 已验证默认 runtime client 会请求 `/api/v1/health`，而不是走 mock。 | 关闭默认 runtime client 不发真实请求的缺口。 |
| R26-63 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §132 UI State / API Deep 缺陷 | P2 | `ui/packages/shared/api-client/ws-client` | WS token 在 URL query 中——日志/代理可截获 | 已解决（源码/测试） | 根因是旧 WebSocket 建链确实把 token 放进 URL，导致代理、CDN 和浏览器日志都有暴露面；后续认证方式已迁移，但重复挂账未回写。 | 当前 `ui/packages/shared/api-client/src/ws-client.ts` 建连时使用 `v1.auth.token` subprotocol，认证 token 在 `onopen` 后作为首帧 `{"action":"auth","token":...}` 发送，URL 本身不再携带 token；`ui/tests/unit/ui/shared/ws-client.test.ts` 与 `ui/tests/shared/api-client-runtime-regressions.test.ts` 已验证 URL 不含 token。 | 关闭的是 query token 泄漏缺陷，不外推为整个实时授权体系都已完工。 |
| R26-64 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §132 UI State / API Deep 缺陷 | P2 | `ui/packages/shared/state/stores/\*` | 无 state normalization for collections——O(n) lookup | 需继续整改（UI 产品化差距） | UI、壳层或共享包仍有 stub、缺件、性能预算或安全能力未落地，本轮未完成对应实装。 | 已按源审计条目补录编号；本轮未执行足以证明修复完成的 UI 全链路 build/e2e/壳层验证。 | 补录到 reaudit，待专项整改。 |
| R26-65 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §132 UI State / API Deep 缺陷 | P2 | `ui/packages/shared/state/query-client` | 无 per-type staleTime 覆盖——所有查询同一缓存策略 | 需继续整改（UI 产品化差距） | UI、壳层或共享包仍有 stub、缺件、性能预算或安全能力未落地，本轮未完成对应实装。 | 已按源审计条目补录编号；本轮未执行足以证明修复完成的 UI 全链路 build/e2e/壳层验证。 | 补录到 reaudit，待专项整改。 |
| R26-66 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §132 UI State / API Deep 缺陷 | P2 | `ui/packages/shared/api-client/interceptors` | 无 X-Idempotency-Key interceptor——重试可重复写入 | 已解决（源码修复并补定向测试） | 根因与 `R7-13` 同源：shared api-client 之前没有专门的幂等头注入器，重试/重放写请求会丢失 client-side idempotency contract。 | 本轮新增 `ui/packages/shared/api-client/src/interceptors.ts#createIdempotencyKeyInterceptor()`，对非 GET 请求统一补写 `Idempotency-Key` 与 `x-idempotency-key`，并已接入 `ui/apps/web/src/runtime.ts` 默认链路；`ui/tests/unit/ui/shared/interceptors.test.ts` 与 `ui/tests/shared/api-client-runtime-regressions.test.ts` 已验证头注入和透传行为，定向测试已通过。 | 作为重复挂账，随 UI 幂等头注入修复一并关闭。 |
| R27-01 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §134 Execution / Model-Gateway / Ops-Maturity 缺陷 | P0 | `shared/observability/transports/fluentd-transport.ts:119` | flush() 监听 drain 事件但 socket 已可写时 drain 永不触发——Promise 永久挂起(死锁) | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R27-02 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §134 Execution / Model-Gateway / Ops-Maturity 缺陷 | P0 | `model-gateway/provider-registry/minimax/minimax-chat-service.ts:391` | streaming 中 assertMiniMaxBusinessSuccess 异常被 generic catch 以 debug 级别吞噬——业务错误静默丢失 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R27-03 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §134 Execution / Model-Gateway / Ops-Maturity 缺陷 | P0 | `model-gateway/provider-registry/openai/openai-chat-service.ts:464` | streaming 从 first chunk 取 finalFinishReason 但首 SSE chunk finish_reason 恒为 null——content_filter/tool_calls/length 全丢失 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R27-04 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §134 Execution / Model-Gateway / Ops-Maturity 缺陷 | P1 | `worker-pool/worker/worker-registry-service.ts:240` | parseJsonArray() 无 try/catch(对比 handshake-support.ts 有)——corrupt JSON 直接 crash | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R27-05 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §134 Execution / Model-Gateway / Ops-Maturity 缺陷 | P1 | `worker-pool/worker/ (handshake+writeback)` | parseJsonArray/toWorkerStatus/buildAgentExecutionRecord/persistRemoteLogs 跨文件复制粘贴——维护分歧 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R27-06 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §134 Execution / Model-Gateway / Ops-Maturity 缺陷 | P1 | `worker-pool/worker/worker-registry-service.ts:577` | getWorker() unsafe `as` cast 访问 legacy store 方法——绕过类型安全,死代码路径 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R27-07 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §134 Execution / Model-Gateway / Ops-Maturity 缺陷 | P1 | `agent-lifecycle/version-manager/agent-version-manager.ts:84` | assignDeploymentSlot() 在 zod-parsed 不可变对象上直接 mutate 字段——共享引用数据损坏 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R27-08 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §134 Execution / Model-Gateway / Ops-Maturity 缺陷 | P1 | `agent-lifecycle/version-manager/agent-version-manager.ts:86-122` | assignDeploymentSlot 清除对面 slot vs switchSlot 注释"保持双活"——blue-green 语义矛盾 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R27-09 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §134 Execution / Model-Gateway / Ops-Maturity 缺陷 | P1 | `shared/observability/transports/datadog-transport.ts:59` | flushInternal HTTP 错误时 resolve() 静默——日志条目丢失无重试/无指标/无背压 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R27-10 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §134 Execution / Model-Gateway / Ops-Maturity 缺陷 | P1 | `model-gateway/provider-registry/anthropic/anthropic-chat-service.ts:463` | streaming message_delta 不提取 stop_reason/usage——finalStopReason 永为 null | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R27-11 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §134 Execution / Model-Gateway / Ops-Maturity 缺陷 | P2 | `drift-detection/cross-agent-analyzer/index.ts:28` | metrics.length===1 时 bestAgentId===worstAgentId——误导输出 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R27-12 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §134 Execution / Model-Gateway / Ops-Maturity 缺陷 | P2 | `drift-detection/changepoint-detector/index.ts:93` | DRIFT_THRESHOLD+EPSILON 使阈值比文档-10%微偏——实际~-9.9999999%触发 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R27-13 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §134 Execution / Model-Gateway / Ops-Maturity 缺陷 | P2 | `shared/observability/transports/fluentd-transport.ts:65` | handleConnected drain loop 无 write 错误处理——中途失败剩余条目丢失 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R27-14 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §134 Execution / Model-Gateway / Ops-Maturity 缺陷 | P2 | `agent-lifecycle/retirement/index.ts:45` | canRetireAgent ISO 字符串比较忽略时区——混合 UTC/local 结果错误 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R27-15 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §135 ADR 020-039 / 061-069 偏差 | P0 | `ADR-026 vs risk-evaluation-engine.ts` | ADR 要求 8 因子风险模型(/18 除数)；代码仍为 legacy 6 因子(MAX_POSSIBLE_SCORE=75) | 需继续整改（ADR/contract） | ADR、contract、类型导出与现行实现存在漂移，本轮未完成逐条回写与一致性校验闭环。 | 已按源审计条目补录编号；本轮未新增 contract/ADR 回写或一致性测试证据，因此不能改写为已解决。 | 补录到 reaudit，待专项整改。 |
| R27-16 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §135 ADR 020-039 / 061-069 偏差 | P0 | `ADR-021 vs platform-contracts.ts` | ADR 要求 OperationalDirective/DecisionDirective 替代 ControlDirective——新类型完全不存在 | 需继续整改（ADR/contract） | ADR、contract、类型导出与现行实现存在漂移，本轮未完成逐条回写与一致性校验闭环。 | 已按源审计条目补录编号；本轮未新增 contract/ADR 回写或一致性测试证据，因此不能改写为已解决。 | 补录到 reaudit，待专项整改。 |
| R27-17 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §135 ADR 020-039 / 061-069 偏差 | P0 | `ADR-021 vs platform-contracts.ts:12` | RequestEnvelope 缺 ADR 要求的 source_plane/target_plane 字段——无跨平面路由 | 需继续整改（ADR/contract） | ADR、contract、类型导出与现行实现存在漂移，本轮未完成逐条回写与一致性校验闭环。 | 已按源审计条目补录编号；本轮未新增 contract/ADR 回写或一致性测试证据，因此不能改写为已解决。 | 补录到 reaudit，待专项整改。 |
| R27-18 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §135 ADR 020-039 / 061-069 偏差 | P1 | `ADR-027 vs plugin-definition.ts:26` | ADR 禁止 SANDBOX_NONE；代码允许 sandboxTier:"none" 跨 plugin/pack/delegation 类型 | 需继续整改（ADR/contract） | ADR、contract、类型导出与现行实现存在漂移，本轮未完成逐条回写与一致性校验闭环。 | 已按源审计条目补录编号；本轮未新增 contract/ADR 回写或一致性测试证据，因此不能改写为已解决。 | 补录到 reaudit，待专项整改。 |
| R27-19 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §135 ADR 020-039 / 061-069 偏差 | P1 | `ADR-020 vs memory-promotion-engine.ts` | ADR 要求 evaluateDemotion()+runPromotionCycle()→PromotionResult；代码仅有同步 promote() | 需继续整改（ADR/contract） | ADR、contract、类型导出与现行实现存在漂移，本轮未完成逐条回写与一致性校验闭环。 | 已按源审计条目补录编号；本轮未新增 contract/ADR 回写或一致性测试证据，因此不能改写为已解决。 | 补录到 reaudit，待专项整改。 |
| R27-20 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §135 ADR 020-039 / 061-069 偏差 | P1 | `ADR-020 vs memory-layer-model.ts:48` | ADR L3→L4: accessCount≥10, qualityScore≥0.8；代码用 minHitCount:8, minQualityScore:0.75 | 需继续整改（ADR/contract） | ADR、contract、类型导出与现行实现存在漂移，本轮未完成逐条回写与一致性校验闭环。 | 已按源审计条目补录编号；本轮未新增 contract/ADR 回写或一致性测试证据，因此不能改写为已解决。 | 补录到 reaudit，待专项整改。 |
| R27-21 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §135 ADR 020-039 / 061-069 偏差 | P1 | `ADR-025 vs policy-center/index.ts:35` | PolicyMode 保留 ADR 已删除的 supervised/degraded/maintenance/emergency 值 | 需继续整改（ADR/contract） | ADR、contract、类型导出与现行实现存在漂移，本轮未完成逐条回写与一致性校验闭环。 | 已按源审计条目补录编号；本轮未新增 contract/ADR 回写或一致性测试证据，因此不能改写为已解决。 | 补录到 reaudit，待专项整改。 |
| R27-22 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §135 ADR 020-039 / 061-069 偏差 | P1 | `ADR-027 vs executable-contracts/index.ts:60` | PrincipalRef 缺 type discriminant——ADR 要求 6 种 typed variants | 需继续整改（ADR/contract） | ADR、contract、类型导出与现行实现存在漂移，本轮未完成逐条回写与一致性校验闭环。 | 已按源审计条目补录编号；本轮未新增 contract/ADR 回写或一致性测试证据，因此不能改写为已解决。 | 补录到 reaudit，待专项整改。 |
| R27-23 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §135 ADR 020-039 / 061-069 偏差 | P1 | `ADR-029/039 vs nl-gateway/index.ts:66` | DetectedIntent 5 types 缺 ADR 要求的 cancel_task/create_goal/decompress_goal | 需继续整改（ADR/contract） | ADR、contract、类型导出与现行实现存在漂移，本轮未完成逐条回写与一致性校验闭环。 | 已按源审计条目补录编号；本轮未新增 contract/ADR 回写或一致性测试证据，因此不能改写为已解决。 | 补录到 reaudit，待专项整改。 |
| R27-24 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §135 ADR 020-039 / 061-069 偏差 | P1 | `ADR-064 vs cost-optimization-service.ts:24` | CostAttributionRecord 维度用 subjectType/subjectId——ADR 要求 harness_run_id/node_run_id | 需继续整改（ADR/contract） | ADR、contract、类型导出与现行实现存在漂移，本轮未完成逐条回写与一致性校验闭环。 | 已按源审计条目补录编号；本轮未新增 contract/ADR 回写或一致性测试证据，因此不能改写为已解决。 | 补录到 reaudit，待专项整改。 |
| R27-25 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §135 ADR 020-039 / 061-069 偏差 | P1 | `ADR-062 vs edge-runtime-sync-service.ts:39` | 冲突解决策略 taxonomy 不匹配(accept_edge/cloud/merge/reject vs last_write_wins/server_wins/merge/manual) | 需继续整改（ADR/contract） | ADR、contract、类型导出与现行实现存在漂移，本轮未完成逐条回写与一致性校验闭环。 | 已按源审计条目补录编号；本轮未新增 contract/ADR 回写或一致性测试证据，因此不能改写为已解决。 | 补录到 reaudit，待专项整改。 |
| R27-26 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §135 ADR 020-039 / 061-069 偏差 | P1 | `ADR-061 vs agent-registry/index.ts:6` | lifecycle 状态偏离：production→active, retired→archived, 多出 canary/paused | 需继续整改（ADR/contract） | ADR、contract、类型导出与现行实现存在漂移，本轮未完成逐条回写与一致性校验闭环。 | 已按源审计条目补录编号；本轮未新增 contract/ADR 回写或一致性测试证据，因此不能改写为已解决。 | 补录到 reaudit，待专项整改。 |
| R27-27 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §135 ADR 020-039 / 061-069 偏差 | P2 | `ADR-020 vs memory-layer-model.ts:17` | 层名不匹配：ADR L1-L6 RuntimeCache/.../Evolution vs 代码 working/session/.../meta | 需继续整改（ADR/contract） | ADR、contract、类型导出与现行实现存在漂移，本轮未完成逐条回写与一致性校验闭环。 | 已按源审计条目补录编号；本轮未新增 contract/ADR 回写或一致性测试证据，因此不能改写为已解决。 | 补录到 reaudit，待专项整改。 |
| R27-28 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §135 ADR 020-039 / 061-069 偏差 | P2 | `ADR-039 vs nl-gateway/index.ts:96` | RiskPreview 字段 snake_case(ADR) vs camelCase(代码)命名规范不一致 | 需继续整改（ADR/contract） | ADR、contract、类型导出与现行实现存在漂移，本轮未完成逐条回写与一致性校验闭环。 | 已按源审计条目补录编号；本轮未新增 contract/ADR 回写或一致性测试证据，因此不能改写为已解决。 | 补录到 reaudit，待专项整改。 |
| R27-29 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §135 ADR 020-039 / 061-069 偏差 | P2 | `ADR-026 vs risk-evaluation-engine.ts:11` | 代码注释引用旧 §10.2 权重=4；ADR 规定所有因子权重 1-3 | 需继续整改（ADR/contract） | ADR、contract、类型导出与现行实现存在漂移，本轮未完成逐条回写与一致性校验闭环。 | 已按源审计条目补录编号；本轮未新增 contract/ADR 回写或一致性测试证据，因此不能改写为已解决。 | 补录到 reaudit，待专项整改。 |
| R27-30 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §136 未审计 Contract 偏差 | P0 | `approval_and_hitl_contract vs approval-service.ts:54` | ApprovalRequest 缺 contract 要求的 escalation_chain/timeout_auto_action/stage_view_ref/harness_run_id/node_run_id | 需继续整改（ADR/contract） | ADR、contract、类型导出与现行实现存在漂移，本轮未完成逐条回写与一致性校验闭环。 | 已按源审计条目补录编号；本轮未新增 contract/ADR 回写或一致性测试证据，因此不能改写为已解决。 | 补录到 reaudit，待专项整改。 |
| R27-31 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §136 未审计 Contract 偏差 | P0 | `policy_engine_contract vs iam/policy-engine.ts:121` | mode 仅3 legacy 值(supervised/auto/full-auto)；contract 要求 8 canonical modes | 需继续整改（ADR/contract） | ADR、contract、类型导出与现行实现存在漂移，本轮未完成逐条回写与一致性校验闭环。 | 已按源审计条目补录编号；本轮未新增 contract/ADR 回写或一致性测试证据，因此不能改写为已解决。 | 补录到 reaudit，待专项整改。 |
| R27-32 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §136 未审计 Contract 偏差 | P0 | `policy_engine_contract vs iam/policy-engine.ts:100` | action 缺 contract 要求值：dispatch_execution/set_isolation_level/promote_improvement/advance_rollout/modify_knowledge_trust/promote_memory_layer | 需继续整改（ADR/contract） | ADR、contract、类型导出与现行实现存在漂移，本轮未完成逐条回写与一致性校验闭环。 | 已按源审计条目补录编号；本轮未新增 contract/ADR 回写或一致性测试证据，因此不能改写为已解决。 | 补录到 reaudit，待专项整改。 |
| R27-33 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §136 未审计 Contract 偏差 | P0 | `policy_engine_contract vs iam/policy-engine.ts:112` | riskCategory 缺 contract 要求的 strategy_affecting/governance_sensitive | 需继续整改（ADR/contract） | ADR、contract、类型导出与现行实现存在漂移，本轮未完成逐条回写与一致性校验闭环。 | 已按源审计条目补录编号；本轮未新增 contract/ADR 回写或一致性测试证据，因此不能改写为已解决。 | 补录到 reaudit，待专项整改。 |
| R27-34 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §136 未审计 Contract 偏差 | P0 | `distributed_locking_contract vs src/` | LockTransitionCommand 类型完全不存在——contract 要求与 RSM.transition(command) 集成 | 需继续整改（ADR/contract） | ADR、contract、类型导出与现行实现存在漂移，本轮未完成逐条回写与一致性校验闭环。 | 已按源审计条目补录编号；本轮未新增 contract/ADR 回写或一致性测试证据，因此不能改写为已解决。 | 补录到 reaudit，待专项整改。 |
| R27-35 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §136 未审计 Contract 偏差 | P0 | `edge_runtime_contract vs edge-runtime-sync-service.ts:8` | EdgeRuntimeProfile 缺 contract 要求的 stateful/lease_migration_supported/checkpoint_required_before_preempt | 需继续整改（ADR/contract） | ADR、contract、类型导出与现行实现存在漂移，本轮未完成逐条回写与一致性校验闭环。 | 已按源审计条目补录编号；本轮未新增 contract/ADR 回写或一致性测试证据，因此不能改写为已解决。 | 补录到 reaudit，待专项整改。 |
| R27-36 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §136 未审计 Contract 偏差 | P1 | `policy_engine_contract vs policy-engine.ts:134` | PolicyDecisionResult 缺 decisionTtlMs/matchedRuleRefs；无 PolicyDecisionExplain/PolicyAuditRecord 类型 | 需继续整改（ADR/contract） | ADR、contract、类型导出与现行实现存在漂移，本轮未完成逐条回写与一致性校验闭环。 | 已按源审计条目补录编号；本轮未新增 contract/ADR 回写或一致性测试证据，因此不能改写为已解决。 | 补录到 reaudit，待专项整改。 |
| R27-37 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §136 未审计 Contract 偏差 | P1 | `policy_engine_contract vs policy-center/index.ts:35` | PolicyMode 用非 canonical 名(read-only/maintenance/degraded/emergency vs contract 的 read_only/no-write/no-external-call/no-rollout/manual_only) | 需继续整改（ADR/contract） | ADR、contract、类型导出与现行实现存在漂移，本轮未完成逐条回写与一致性校验闭环。 | 已按源审计条目补录编号；本轮未新增 contract/ADR 回写或一致性测试证据，因此不能改写为已解决。 | 补录到 reaudit，待专项整改。 |
| R27-38 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §136 未审计 Contract 偏差 | P1 | `knowledge_boundary_contract vs boundary-manager/index.ts:3` | KnowledgeBoundary 缺 contract 要求的 classification_rules/share_policy | 需继续整改（ADR/contract） | ADR、contract、类型导出与现行实现存在漂移，本轮未完成逐条回写与一致性校验闭环。 | 已按源审计条目补录编号；本轮未新增 contract/ADR 回写或一致性测试证据，因此不能改写为已解决。 | 补录到 reaudit，待专项整改。 |
| R27-39 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §136 未审计 Contract 偏差 | P1 | `knowledge_boundary_contract vs src/` | FederatedSearchRequest/FederatedSearchResult canonical 类型不存在——用 ad-hoc 无类型接口 | 需继续整改（ADR/contract） | ADR、contract、类型导出与现行实现存在漂移，本轮未完成逐条回写与一致性校验闭环。 | 已按源审计条目补录编号；本轮未新增 contract/ADR 回写或一致性测试证据，因此不能改写为已解决。 | 补录到 reaudit，待专项整改。 |
| R27-40 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §136 未审计 Contract 偏差 | P1 | `knowledge_boundary_contract vs src/` | ChineseWallConstraint canonical 类型不存在——用 inline policy objects | 需继续整改（ADR/contract） | ADR、contract、类型导出与现行实现存在漂移，本轮未完成逐条回写与一致性校验闭环。 | 已按源审计条目补录编号；本轮未新增 contract/ADR 回写或一致性测试证据，因此不能改写为已解决。 | 补录到 reaudit，待专项整改。 |
| R27-41 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §136 未审计 Contract 偏差 | P1 | `org_hierarchy_contract vs org-node/index.ts:14` | OrgNodeType 用 company/division/department/team；contract 要求 enterprise/business_unit/department/team/seat | 需继续整改（ADR/contract） | ADR、contract、类型导出与现行实现存在漂移，本轮未完成逐条回写与一致性校验闭环。 | 已按源审计条目补录编号；本轮未新增 contract/ADR 回写或一致性测试证据，因此不能改写为已解决。 | 补录到 reaudit，待专项整改。 |
| R27-42 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §136 未审计 Contract 偏差 | P1 | `org_hierarchy_contract vs org-node/index.ts:88` | OrgNode 缺 contract 要求的 effective_policies/status 字段 | 需继续整改（ADR/contract） | ADR、contract、类型导出与现行实现存在漂移，本轮未完成逐条回写与一致性校验闭环。 | 已按源审计条目补录编号；本轮未新增 contract/ADR 回写或一致性测试证据，因此不能改写为已解决。 | 补录到 reaudit，待专项整改。 |
| R27-43 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §136 未审计 Contract 偏差 | P1 | `org_hierarchy_contract vs src/` | OrgHierarchySnapshot/ApprovalLimitMatrix/CompliancePolicyBinding canonical 类型不存在 | 需继续整改（ADR/contract） | ADR、contract、类型导出与现行实现存在漂移，本轮未完成逐条回写与一致性校验闭环。 | 已按源审计条目补录编号；本轮未新增 contract/ADR 回写或一致性测试证据，因此不能改写为已解决。 | 补录到 reaudit，待专项整改。 |
| R27-44 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §136 未审计 Contract 偏差 | P1 | `approval_and_hitl_contract vs hitl-approval-orchestration-service.ts:28` | ApprovalFeedbackLink 缺 loop_iteration/ref_id/feedback_signal_id | 需继续整改（ADR/contract） | ADR、contract、类型导出与现行实现存在漂移，本轮未完成逐条回写与一致性校验闭环。 | 已按源审计条目补录编号；本轮未新增 contract/ADR 回写或一致性测试证据，因此不能改写为已解决。 | 补录到 reaudit，待专项整改。 |
| R27-45 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §136 未审计 Contract 偏差 | P1 | `slo_alerting_contract vs operations-governance-service.ts:57` | Runbook catalog 缺 contract 要求的 oapeflir_loop_stalled/rollout_blocked_or_rollback | 需继续整改（ADR/contract） | ADR、contract、类型导出与现行实现存在漂移，本轮未完成逐条回写与一致性校验闭环。 | 已按源审计条目补录编号；本轮未新增 contract/ADR 回写或一致性测试证据，因此不能改写为已解决。 | 补录到 reaudit，待专项整改。 |
| R27-46 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §136 未审计 Contract 偏差 | P1 | `tenant_isolation_contract vs src/` | 无自动隔离触发(failure_rate>30% min_sample≥20)——contract §5A 要求 | 需继续整改（ADR/contract） | ADR、contract、类型导出与现行实现存在漂移，本轮未完成逐条回写与一致性校验闭环。 | 已按源审计条目补录编号；本轮未新增 contract/ADR 回写或一致性测试证据，因此不能改写为已解决。 | 补录到 reaudit，待专项整改。 |
| R27-47 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §136 未审计 Contract 偏差 | P2 | `approval_and_hitl_contract vs approval-service.ts:54` | ApprovalRequest 用 taskId 而非 contract 的 harness_run_id 作关联键 | 需继续整改（ADR/contract） | ADR、contract、类型导出与现行实现存在漂移，本轮未完成逐条回写与一致性校验闭环。 | 已按源审计条目补录编号；本轮未新增 contract/ADR 回写或一致性测试证据，因此不能改写为已解决。 | 补录到 reaudit，待专项整改。 |
| R27-48 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §136 未审计 Contract 偏差 | P2 | `remote_coordination_contract vs src/` | Remote worker session states(connecting/connected/reconnecting/degraded/failed/viewer_only)未实现 | 需继续整改（ADR/contract） | ADR、contract、类型导出与现行实现存在漂移，本轮未完成逐条回写与一致性校验闭环。 | 已按源审计条目补录编号；本轮未新增 contract/ADR 回写或一致性测试证据，因此不能改写为已解决。 | 补录到 reaudit，待专项整改。 |
| R27-49 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §136 未审计 Contract 偏差 | P2 | `ha_coordinator_contract vs execution/ha/types.ts:1` | CoordinatorNode 缺 metadata 限制 follower 动作——无类型级 guard | 需继续整改（ADR/contract） | ADR、contract、类型导出与现行实现存在漂移，本轮未完成逐条回写与一致性校验闭环。 | 已按源审计条目补录编号；本轮未新增 contract/ADR 回写或一致性测试证据，因此不能改写为已解决。 | 补录到 reaudit，待专项整改。 |
| R27-50 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §137 Org-Governance / Scale-Ecosystem / Interaction 缺陷 | P0 | `interaction/proactive-agent/trigger-engine/index.ts:7` | high-risk actions auto-execute 当 requireConfirmation=false——§42/§10 要求 high 级审批 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R27-51 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §137 Org-Governance / Scale-Ecosystem / Interaction 缺陷 | P0 | `org-governance/sso-scim/scim-service.ts:300-325` | listUsers/listGroups 无租户隔离——返回所有租户用户,违反 §11 多租户安全 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R27-52 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §137 Org-Governance / Scale-Ecosystem / Interaction 缺陷 | P0 | `org-governance/sso-scim/api-key-service.ts:94` | 过期 API key 在 validateApiKey 中不标记 expired——保持 active 状态,可在竞态窗口被轮换 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R27-53 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §137 Org-Governance / Scale-Ecosystem / Interaction 缺陷 | P1 | `org-governance/delegated-governance/governance-delegation-revocation-saga.ts:28` | cascadeWithinSlo 条件 >=0 恒为 true(数组长度不可能负)——SLO 检查是死代码 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R27-54 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §137 Org-Governance / Scale-Ecosystem / Interaction 缺陷 | P1 | `org-governance/sso-scim/scim-service.ts:532` | SCIM patchGroup remove members 清除全部成员而非解析 path 表达式指定的特定成员 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R27-55 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §137 Org-Governance / Scale-Ecosystem / Interaction 缺陷 | P1 | `org-governance/sso-scim/scim-service.ts:791` | applyFilter 忽略属性名——displayName eq "x" 实际匹配 userName | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R27-56 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §137 Org-Governance / Scale-Ecosystem / Interaction 缺陷 | P1 | `org-governance/knowledge-boundary/knowledge-boundary-service.ts:163` | requiredGrantBoundaryIds 检查逻辑错误——跨 boundary 授权请求永远失败 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R27-57 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §137 Org-Governance / Scale-Ecosystem / Interaction 缺陷 | P1 | `scale-ecosystem/multi-region/region-health-check-service.ts:330` | determineStatus 用 reference equality(===)找 regionId——永不匹配,延迟降级是死代码 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R27-58 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §137 Org-Governance / Scale-Ecosystem / Interaction 缺陷 | P1 | `org-governance/sso-scim/scim-service.ts:419` | deleteGroup 不发 provision event——与 deleteUser 发 user_deleted 不一致 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R27-59 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §137 Org-Governance / Scale-Ecosystem / Interaction 缺陷 | P1 | `org-governance/sso-scim/scim-service.ts:497` | removeMemberFromGroup 传空字符串 tenantId 给 updateGroup——审计事件无租户上下文 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R27-60 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §137 Org-Governance / Scale-Ecosystem / Interaction 缺陷 | P1 | `org-governance/delegated-governance/delegated-governance-service.ts:77` | checkOperation 仅评估 grantorId==="platform_team" 的护栏——忽略 §51.2 层级继承 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R27-61 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §137 Org-Governance / Scale-Ecosystem / Interaction 缺陷 | P1 | `interaction/autonomy/level-manager/index.ts:3` | AUTONOMY_LEVEL_ORDER frozen 在 index 4(>full_auto)——compareAutonomyLevels 视 frozen 为最高级别 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R27-62 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §137 Org-Governance / Scale-Ecosystem / Interaction 缺陷 | P2 | `org-governance/knowledge-boundary/chinese-wall-access-saga.ts:15` | Chinese Wall saga 无 prepare/commit/compensate/audit——仅纯 boolean 检查,违反 §2.4 saga | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R27-63 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §137 Org-Governance / Scale-Ecosystem / Interaction 缺陷 | P2 | `scale-ecosystem/sla-engine/sla-operations-service.ts:111` | preemptionCapApplied <= 对最高优先级恒 true——条件是 no-op | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R27-64 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §137 Org-Governance / Scale-Ecosystem / Interaction 缺陷 | P2 | `interaction/autonomy/promotion-engine/index.ts:16` | failedExecutions>2(strictly greater)——恰好2次失败+低rate仍可晋升 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R27-65 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §137 Org-Governance / Scale-Ecosystem / Interaction 缺陷 | P2 | `org-governance/compliance-engine/evidence-collector.ts:21` | normalizeEvidenceInput ?? "unknown" 掩盖 required 字段缺失——静默生成坏数据 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R27-66 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §137 Org-Governance / Scale-Ecosystem / Interaction 缺陷 | P2 | `scale-ecosystem/multi-region/cdc-replication-service.ts:277` | 复制队列无界增长——enqueueBatch 只推不排,无背压/max_queue_depth | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R27-67 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §137 Org-Governance / Scale-Ecosystem / Interaction 缺陷 | P2 | `scale-ecosystem/marketplace/catalog/index.ts:71` | validateListingDependencies 检查自身 compatibility 而非依赖方——逻辑反转 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R27-68 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §137 Org-Governance / Scale-Ecosystem / Interaction 缺陷 | P2 | `org-governance/knowledge-boundary/boundary-manager/index.ts:7` | defaultVisibility "public" 选项存在但 canAccessKnowledgeBoundary 不检查——public boundary 仍拒绝访问 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R28-01 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §139 State-Evidence / Repository SQL 缺陷 | P0 | `release-repository.ts:65` | INSERT 26列但仅25个 VALUES 占位符($1-$25)——exported_at 无 $26,INSERT 恒失败 | 已解决（源码/测试） | 根因在 async PG-style repository：`insertReleaseBundleRecord()` 的列清单是 22 列，但 SQL 写成了 `$1..$23`，占位符个数与真实列/参数不一致，执行时会在 driver 层直接失败。 | 本轮修正 `src/platform/five-plane-state-evidence/truth/async-repositories/release-repository.ts` 中 `release_bundles` INSERT 的 VALUES 清单，使其与 22 个列和 22 个参数一一对应。 | 定向验证已通过：新增 `tests/unit/platform/five-plane-state-evidence/truth/async-repositories/release-repository.test.ts`，断言 SQL 只到 `$22` 且参数数为 22。 |
| R28-02 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §139 State-Evidence / Repository SQL 缺陷 | P0 | `release-repository.ts:105` | INSERT 28列但仅27个 VALUES 占位符($1-$27)——同类 off-by-one,INSERT 恒失败 | 已解决（源码/测试） | 根因与 `R28-01` 同源：`insertReleaseExecutionReportRecord()` 的 SQL 在 `exported_at` 结尾少了最后一个占位符，导致 26 个列/参数无法完整绑定。 | 本轮将同文件 `release_execution_reports` INSERT 的 VALUES 清单补齐到 `$26`，与列数和参数数一致。 | 定向验证已通过：同一新增测试文件断言该 INSERT 已包含 `$26` 且参数数为 26。 |
| R28-03 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §139 State-Evidence / Repository SQL 缺陷 | P0 | `release-repository.ts:228` | `WHERE environment IS $1`——无效 PG 语法(IS 仅接 NULL/TRUE/FALSE 字面量) | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R28-04 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §139 State-Evidence / Repository SQL 缺陷 | P0 | `marketplace-repository.ts:169+8处` | `tenant_id IS $N`——无效 PG 语法,影响9个查询位置 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R28-05 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §139 State-Evidence / Repository SQL 缺陷 | P0 | `operations-repository.ts:175+2处` | `tenant_id IS $N`——无效 PG 语法,影响3个查询位置 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R28-06 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §139 State-Evidence / Repository SQL 缺陷 | P1 | `prompt-repository.ts:266` | setCurrentVersion 两次 UPDATE 无事务——并发可导致 0 或多个 current version | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R28-07 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §139 State-Evidence / Repository SQL 缺陷 | P1 | `prompt-repository.ts:191,215,240` | PG async repo 用 SQLite boolean 字面量 `deprecated = 0`——PG 需 `= false` | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R28-08 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §139 State-Evidence / Repository SQL 缺陷 | P1 | `prompt-repository.ts:329` | `is_current = 1` SQLite 风格——PG boolean 需 `= true` | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R28-09 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §139 State-Evidence / Repository SQL 缺陷 | P2 | `marketplace-repository-ext.ts:344` | listDownloadsByListing limit 无 sanitization(无 Math.max/Math.trunc) | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R28-10 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §139 State-Evidence / Repository SQL 缺陷 | P2 | `prompt-repository.ts:62,250,340` | 直接 this.conn.execute() 跳过 asyncExecute() helper——不一致 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R28-11 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §140 Prompt-Engine / Plugins / SDK / Channel-Gateway 缺陷 | P0 | `plugins/adapters/crm-adapter.ts:56` | action 参数未过滤直接拼入 URL 路径(`/crm/v3/objects/${action}`)——路径遍历到任意 API | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R28-12 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §140 Prompt-Engine / Plugins / SDK / Channel-Gateway 缺陷 | P0 | `plugins/adapters/crm-adapter.ts:55` | execute() 不检查 credentialFingerprint——未认证即执行(github-adapter 有门控) | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R28-13 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §140 Prompt-Engine / Plugins / SDK / Channel-Gateway 缺陷 | P0 | `plugins/adapters/game-dev-adapter.ts:29` | execute() 无 auth 检查+无 egress policy——任何调用者可无认证调用 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R28-14 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §140 Prompt-Engine / Plugins / SDK / Channel-Gateway 缺陷 | P0 | `plugins/adapters/asset-production-adapter.ts:29` | execute() 无 auth 检查+无 egress policy——同上 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R28-15 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §140 Prompt-Engine / Plugins / SDK / Channel-Gateway 缺陷 | P0 | `plugins/adapters/livestream-adapter.ts:30` | execute() 无 auth 检查+无 egress policy——同上 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R28-16 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §140 Prompt-Engine / Plugins / SDK / Channel-Gateway 缺陷 | P0 | `channel-gateway-delivery-service.ts:287` | generateNonce `randomBytes(len).toString("hex").slice(0,len)` 熵减半(32 hex=16 bytes) | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R28-17 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §140 Prompt-Engine / Plugins / SDK / Channel-Gateway 缺陷 | P1 | `prompt-engine/rollout/prompt-rollout-stage.ts:26` | nextPromptRolloutStage("stable") 返回 "rolled_back"——terminal 阶段应返回 null | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R28-18 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §140 Prompt-Engine / Plugins / SDK / Channel-Gateway 缺陷 | P1 | `prompt-engine/registry/hierarchical-registry-service.ts:395` | findBundle 接受 version 参数但从不使用——总返回默认 bundle 忽略版本请求 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R28-19 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §140 Prompt-Engine / Plugins / SDK / Channel-Gateway 缺陷 | P1 | `prompt-engine/registry/prompt-version-manager.ts:89` | compareVersions 文档说返回 -1/0/1 但返回原始差值(如5)——调用方依赖符号会 break | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R28-20 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §140 Prompt-Engine / Plugins / SDK / Channel-Gateway 缺陷 | P1 | `plugins/adapters/game-dev+asset+livestream:authenticate()` | authenticate() 是 no-op——凭证接受但从不验证或存储 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R28-21 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §140 Prompt-Engine / Plugins / SDK / Channel-Gateway 缺陷 | P1 | `plugins/adapters/game-dev+asset+livestream:healthCheck()` | healthCheck() 硬编码 return true——永不验证连通性,掩盖故障 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R28-22 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §140 Prompt-Engine / Plugins / SDK / Channel-Gateway 缺陷 | P1 | `prompt-engine/registry/prompt-version-manager.ts:234` | VersionLineage interface 重复声明(line 18 和 234)——类型冲突/覆盖 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R28-23 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §140 Prompt-Engine / Plugins / SDK / Channel-Gateway 缺陷 | P1 | `sdk/pack-sdk/pack-scaffold-service.ts:267` | packId 含 $ 字符时 regex replace 静默损坏生成代码 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R28-24 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §140 Prompt-Engine / Plugins / SDK / Channel-Gateway 缺陷 | P2 | `prompt-engine/registry/prompt-version-manager.ts:42` | versionCache Map 声明但从未 populate/read——死代码 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R28-25 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §140 Prompt-Engine / Plugins / SDK / Channel-Gateway 缺陷 | P2 | `channel-gateway/websocket-bridge.ts:99` | JWT token 作 URL query 参数——泄漏到 access log/referrer header | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R28-26 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §140 Prompt-Engine / Plugins / SDK / Channel-Gateway 缺陷 | P2 | `plugins/adapters/crm-adapter.ts:61` | execute() 返回硬编码 stub 字符串"CRM ${action} stub"——生产代码返回假数据 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R28-27 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §140 Prompt-Engine / Plugins / SDK / Channel-Gateway 缺陷 | P2 | `channel-gateway-delivery-service.ts:336` | createDeliveryMessage 创建时即设 finalStatus:"success"——未投递就标记成功 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R28-28 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §140 Prompt-Engine / Plugins / SDK / Channel-Gateway 缺陷 | P2 | `sdk/pack-sdk/pack-scaffold-service.ts:302` | packId regex 允许点和下划线——拼入目录路径时产生歧义 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R28-29 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §141 UI Deep Dive 缺陷 | P0 | `shared/platform/web-platform-adapter.ts:9` | "Secure" 值存 plaintext localStorage(`aa.secure.*`)——XSS 可直接读取 | 已解决（源码/测试） | 根因与 `R22-03` 相同：重复条目沿用了旧版 secure storage 认知，没有吸收后续 secure value 退回内存安全存储的实现。 | 当前 `ui/packages/shared/platform/src/base-platform-adapter.ts` 的 secure storage 只落 `MemorySecureStore`，`ui/packages/shared/platform/src/web-platform-adapter.ts` 也未把敏感值写入 `localStorage`。 | 作为重复挂账，随 `R22-03` 一并关闭；`ui/tests/shared/web-platform-security-regressions.test.ts` 本轮通过。 |
| R28-30 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §141 UI Deep Dive 缺陷 | P0 | `shared/auth/auth-service.ts:37` | handleSsoCallback 参数缺失时回退到硬编码 "mock-access-token"——生产 SSO 返回假 token | 已解决（源码修复并补定向测试） | 根因是 `AuthService.exchangeCodeForTokens()` 之前在未配置真实 exchange handler 时会默认合成 `mock-access-token-*` / `mock-refresh-token-*`，这会把测试占位逻辑暴露到生产可达授权码回调路径。 | 本轮修改 `ui/packages/shared/auth/src/auth-service.ts`，新增 `AuthServiceOptions.exchangeCodeForTokens` 注入点；默认未配置时现在 fail-closed 抛出 `auth.token_exchange_not_configured`，不再合成假 token。`ui/tests/unit/ui/shared/auth-service.test.ts` 已更新并通过：显式注入 handler 时可完成授权码交换，未注入时会拒绝并保持 session 为空。 | 关闭的是 mock token fallback 缺陷，不外推为已经接好真实 OAuth 服务端交换端点。 |
| R28-31 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §141 UI Deep Dive 缺陷 | P0 | `apps/web/runtime.ts:44-46` | wsUrl 配置被忽略——两个分支创建相同 BrowserWSClient(WebSocket, InMemoryWSClient()) | 已解决（源码/测试） | 根因与 `R22-20` 同源：该条把 `wsUrl` 没有出现在 `BrowserWSClient` 构造器参数里，误读成了“整个 URL 没有传到连接层”。 | 当前 URL 传递链是 `createWebRuntimeClients()` 返回 `wsUrl` → `WebAppShell` 传给 `UiRuntimeProvider` → `ui/packages/shared/state/src/index.ts` 在 bootstrap 里执行 `router.connect(wsUrl, wsToken)`；本轮新增 `ui/tests/shared/ws-runtime-connect-regression.test.tsx`，已验证 `connect()` 确实收到传入的 `wss://...` URL 与 session token，定向测试已通过。 | 作为重复挂账一并关闭。 |
| R28-32 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §141 UI Deep Dive 缺陷 | P0 | `apps/web/app-shell.tsx:1-124` | 无 ErrorBoundary——未捕获渲染错误导致整个 UI 白屏崩溃 | 已解决（源码/测试） | 根因与 `R16-66` 同源：该条引用的是加 `AppErrorBoundary` 之前的旧壳层。 | 当前 `ui/apps/web/src/app-shell.tsx` 已引入 `AppErrorBoundary` 并提供重试/上报 fallback；本轮新增 `ui/tests/shared/web-app-shell-auth-regression.test.tsx`，验证 feature 抛错时壳层不会白屏而是进入错误边界，定向测试已通过。 | 作为重复挂账一并关闭。 |
| R28-33 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §141 UI Deep Dive 缺陷 | P1 | `shared/sync/conflict-resolver.ts:15` | spec 要求 CRDT 冲突解决；merge 策略仅 shallow spread({...server,...local})——非 CRDT | 已解决（源码/测试） | 根因复核后发现，这条挂账引用的是旧版 merge 认知；现行 `ConflictResolver` 已经不是浅层 spread，而是按 vector clock / Lamport timestamp 做字段级合并。 | 当前 `ui/packages/shared/sync/src/conflict-resolver.ts` 已实现 `vectorClock`、`lamportTimestamp`、对象递归 merge 与数组按 ID 去重合并，不再是 `({...server, ...local})` 这种浅层覆盖。 | 定向验证已通过：`ui/tests/shared/conflict-resolver-regressions.test.ts` 新增嵌套对象回归，直接验证 `merge` 会按字段级时间戳选择 `phase/owner/retries/note`，而不是被浅层 spread 覆盖。 |
| R28-34 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §141 UI Deep Dive 缺陷 | P1 | `features/hitl/hooks/index.ts:5-13` | HITL hook 返回硬编码静态字符串——无 API 调用/无状态/完全惰性 | 已解决（源码/测试） | 这条原始指控已明显过时：hook 早就有 approvals fetch + WS 订阅，但确实还残留 `edit/escalate/defer/resume` 只 `console.info(...)` 的 stub 动作，导致“部分能力真实、部分能力惰性”的中间态一直没有被回写。 | 本轮在 `ui/packages/features/hitl/src/hooks/index.ts` 把 `edit/escalate/defer/resume` 全部接到真实 API helper：`editApproval`、`escalateApproval`、`deferApproval` 和带 `mode` 的 `resumeWorkflow`；对应 `ui/packages/shared/api-client/src/endpoints.ts` 也补了这些 helper。 | 定向验证已通过：`ui/tests/unit/ui/packages/features/hitl/hooks.test.tsx` 直接断言 `edit/escalate/defer/resume` 会调用真实 API helper，而不是继续停留在 `console.info`。 |
| R28-35 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §141 UI Deep Dive 缺陷 | P1 | `features/approval/hooks/index.ts:80-85` | approve()/reject() 仅更新本地 state 不调 REST API——审批决策静默丢弃 | 已解决（源码/测试） | 复核后发现“完全不调 REST API”是旧结论，但真实缺口仍在：`approve/reject` 虽然已经接了 `approveApproval/rejectApproval`，却没有乐观更新失败回滚，和任务 cockpit 一样仍停在半成品。 | 本轮把 `approve/reject` 改成先从本地队列中乐观移除并更新 action history，再调用 REST；若后端失败，会恢复 `approvals/selectedId/actionHistory` 到操作前快照，不再让审批队列在失败时静默丢项。 | 定向验证已通过：`ui/tests/unit/ui/packages/features/approval/hooks.test.tsx` 既保留“确实调用 REST API”的断言，也新增失败回滚回归，验证 optimistic approve 失败后队列和选中项会恢复。 |
| R28-36 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §141 UI Deep Dive 缺陷 | P1 | `shared/auth/auth-service.ts:7` | refreshToken 存储但从不使用——无 token 刷新流程,过期即断线 | 已解决（源码/测试） | 根因与 `R22-09` 相同：审计把 `AuthService.login()` 和 `TokenManager` 的职责混在了一起，导致已经存在的 sliding-window refresh 仍被误记为“refreshToken 从不使用”。 | 当前 refresh 入口在 `ui/packages/shared/auth/src/token-manager.ts`：`shouldRefresh()` 会在阈值窗口内触发 refresh，`getAccessTokenWithRefresh()` 使用存下来的 `refreshToken` 完成刷新并更新 session。 | 作为重复挂账，随 `R22-09` 一并关闭；`ui/tests/unit/ui/shared/auth-service.test.ts` 本轮通过并验证 refreshToken 被实际消费。 |
| R28-37 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §141 UI Deep Dive 缺陷 | P1 | `shared/api-client/interceptors.ts:65` | offline queue interceptor enqueue 后仍放行请求到网络(无 early return)——重复提交 | 已解决（源码/测试） | 根因与 `R10-22` 同源：旧离线拦截器只负责入队，不会阻断后续 transport dispatch。 | 当前 `ui/packages/shared/api-client/src/interceptors.ts` 的 `createOfflineQueueInterceptor()` 在离线非 GET 请求入队后会直接抛出 `rest.offline:Request queued for offline sync` 短路网络请求；`ui/tests/shared/api-client-runtime-regressions.test.ts` 已验证离线写会入队且 transport 不会被调用，本轮定向测试已通过。 | 作为重复挂账一并关闭。 |
| R28-38 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §141 UI Deep Dive 缺陷 | P1 | `apps/web/app-shell.tsx:8-22` | Guard context 硬编码全部权限——无动态用户权限解析,所有功能对所有人开放 | 已解决（源码/测试） | 根因与 `2164/R7-12` 同源：旧壳层守卫输入是硬编码全权限 demo 上下文，重复挂账未吸收动态 authContext 改造。 | 当前 `ui/apps/web/src/app-shell.tsx` 已通过 `createFeatureGuardContext()` 从 `authContext` 动态构造 tenant/roles/permissions；本轮新增 `ui/tests/shared/web-app-shell-auth-regression.test.tsx`，直接验证 guard context 使用真实 `authContext`，定向测试已通过。 | 作为重复挂账一并关闭。 |
| R28-39 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §141 UI Deep Dive 缺陷 | P1 | `ui-core/index.tsx:61` | 所有 feature 设 codeSplit:false——无路由级代码分割,违反 <200KB bundle 预算 | 已解决（源码/测试） | 根因分两层：其一，`ui-core` 工厂默认值早已修到 `codeSplit:true`；其二，真正拖住 web 入口的是 `ui/apps/web/src/feature-registry.ts` 仍把每个注册项手写成 `codeSplit:false`，并且壳层没有 `Suspense`。 | 现行状态是 `ui/packages/ui-core/src/index.tsx` 保持 `createFeatureModule()` 默认 `codeSplit:true`，而本轮又把 `ui/apps/web/src/feature-registry.ts` 全量切到 `codeSplit:true`，并在 `ui/apps/web/src/app-shell.tsx` 中启用 `React.Suspense`。 | 作为重复挂账，随 `R7-1/R16-68/R22-06` 一并关闭；本轮相关定向测试已通过。 |
| R28-40 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §141 UI Deep Dive 缺陷 | P1 | `features/approval/web/index.tsx:46` | Delegate input 无 aria-label；Approve/Reject 无 aria-describedby——WCAG 2.1 AA 缺口 | 需继续整改（UI 产品化差距） | UI、壳层或共享包仍有 stub、缺件、性能预算或安全能力未落地，本轮未完成对应实装。 | 已按源审计条目补录编号；本轮未执行足以证明修复完成的 UI 全链路 build/e2e/壳层验证。 | 补录到 reaudit，待专项整改。 |
| R28-41 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §141 UI Deep Dive 缺陷 | P1 | `features/alerts/hooks/index.ts:19-21` | alerts VM 只读；ack/mute/escalate 按钮从不调后端 API | 已解决（源码/测试） | 根因拆开看有两层：一是 `useAlertsVm()` 只有过滤和 dismiss，本身没有任何 incident 写操作；二是 `FeatureWorkbenchPanelAction` 原本只是静态 label/activity 描述，按钮点击只会写本地 activity log，根本没有 callback 通道可连到后端。 | 当前 `ui/packages/features/alerts/src/hooks/index.ts` 已补 `acknowledgeAlert()/startMitigation()/resolveAlert()`，通过 `ui/packages/shared/api-client/src/endpoints.ts` 调用 incident PATCH API 并刷新 `incidents` query；`ui/packages/features/alerts/src/web/index.tsx` 也已把按钮改成与后端真实支持的 `acknowledged/mitigating/resolved` 动作对齐。底层 `ui/packages/ui-core/src/components/index.ts` 同步给 `FeatureWorkbenchPanelAction` 增加了 `onTrigger` 回调。定向验证见 `ui/tests/unit/ui/packages/features/alerts/hooks.test.tsx` 与 `ui/tests/shared/ui-core-mobile.test.tsx`。 | 关闭的是“按钮不落后端”缺口；原审计里写的 `mute/escalate` 文案本身和现行后端 incident 状态机不一致，本轮已改成真实可执行动作。 |
| R28-42 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §141 UI Deep Dive 缺陷 | P2 | `shared/telemetry/index.ts:28` | TelemetrySink.record() void Promise.all——export 错误静默丢失 | 需继续整改（UI 产品化差距） | UI、壳层或共享包仍有 stub、缺件、性能预算或安全能力未落地，本轮未完成对应实装。 | 已按源审计条目补录编号；本轮未执行足以证明修复完成的 UI 全链路 build/e2e/壳层验证。 | 补录到 reaudit，待专项整改。 |
| R28-43 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §141 UI Deep Dive 缺陷 | P2 | `ui-core/components/index.ts:28` | ListCard 用 item.title 作 React key——重复标题导致 key 冲突和渲染 bug | 已解决（源码/测试） | 根因很直接：`ListCard` 把展示文案 `item.title` 误当成 identity key，用在 activity log、通知流这类可能大量复用同标题的列表时会产生 React key 冲突。 | 当前 `ui/packages/ui-core/src/components/index.ts` 已把 `ListCard` item 扩成可选 `id`，优先使用 `item.id`，缺省时退回 `${title}-${index}`；定向验证见 `ui/tests/shared/ui-core-mobile.test.tsx`，已覆盖重复标题仍可正常渲染且不会再触发 duplicate key 回归。 | 这是 UI-core 组件级实修，不依赖全链路 UI build。 |
| R28-44 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §141 UI Deep Dive 缺陷 | P2 | `shared/sync/offline-queue.ts:19-22` | enqueue() 同步但 void persist()——标签关闭前 mutation 可丢失 | 已解决（源码/测试） | 根因与 `R22-16` 同源：旧 `enqueue()` 是同步入口，内部 `void persist()` 导致调用方无法等待持久化落盘。 | 当前 `ui/packages/shared/sync/src/offline-queue.ts` 的 `enqueue()` 已改为 `async`，在返回前等待 ready barrier 和 `persist()` 完成；`ui/tests/unit/ui/shared/offline-queue.test.ts` 已更新并验证 enqueue 只有在异步持久化栅栏结束后才对外可见，本轮定向测试已通过。 | 关闭的是 queue 自身“同步返回 + fire-and-forget persist”缺陷，不外推为页面关闭时的所有浏览器级 durability 场景都已绝对保证。 |
| R28-45 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §141 UI Deep Dive 缺陷 | P2 | `features/conversation/hooks/index.ts:19` | ConversationClient per-hook useMemo 实例化——无共享实例,remount 丢状态 | 需继续整改（UI 产品化差距） | UI、壳层或共享包仍有 stub、缺件、性能预算或安全能力未落地，本轮未完成对应实装。 | 已按源审计条目补录编号；本轮未执行足以证明修复完成的 UI 全链路 build/e2e/壳层验证。 | 补录到 reaudit，待专项整改。 |
| R28-46 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §141 UI Deep Dive 缺陷 | P2 | `ui-core/themes/index.ts:1-50` | resolveTheme 存在但 designTokens 是模块常量——组件直接 import,切换主题无效 | 已解决（源码/测试） | 根因不是单个组件忘了读 theme store，而是整个 token 运行时的表达方式不对：大量组件在 render 时直接从模块级 `designTokens` 读取具体色值并写入 inline style，后续即使 `resolveTheme()` 得到新主题，也不会把已渲染节点上的颜色重新绑定到新 token。 | 当前 `ui/packages/ui-core/src/design-tokens/index.ts` 已把可主题化的 color/shadow token 改成 CSS variable 引用；`ui/packages/ui-core/src/themes/index.ts` 新增 `applyResolvedTheme()`，负责把 `light/dark/high-contrast` 的真实值落到 `documentElement`；`ui/apps/web/src/app-shell.tsx` 通过 `ThemeRuntimeBridge` 订阅 `useThemeState()` 并在主题变更时刷新 CSS 变量，`ui/packages/features/settings/src/hooks/index.ts` 也会把设置页主题选择同步到 theme store。定向验证见 `ui/tests/shared/ui-core-mobile.test.tsx` 与 `ui/tests/unit/ui/packages/features/settings/hooks.test.tsx`。 | 关闭的是“切换主题无效”缺口；后续即便还有视觉细节调整，也是在可运行主题系统之上迭代。 |
| R28-47 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §141 UI Deep Dive 缺陷 | P2 | `features/task-cockpit/hooks/index.ts:58-63` | claimTask/escalateTask 仅本地 mutate——无 API/无乐观更新回滚 | 需继续整改（UI 产品化差距） | UI、壳层或共享包仍有 stub、缺件、性能预算或安全能力未落地，本轮未完成对应实装。 | 已按源审计条目补录编号；本轮未执行足以证明修复完成的 UI 全链路 build/e2e/壳层验证。 | 补录到 reaudit，待专项整改。 |
| R28-48 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §142 Tests / Config / Bootstrap 缺陷 | P0 | `tests/integration/security/input-validation.test.ts:414` | "blocks control characters" 测试用 maliciousCommand="echo"(干净字符串)断言 blocked——验证错误行为为正确 | 需继续整改（测试/配置差距） | 测试、配置或 bootstrap 缺少审计要求的覆盖与约束，本轮未补齐。 | 已按源审计条目补录编号；本轮未新增覆盖缺口对应的测试、配置或 bootstrap 修复证据。 | 补录到 reaudit，待专项整改。 |
| R28-49 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §142 Tests / Config / Bootstrap 缺陷 | P0 | `platform-architecture-bootstrap.ts:150` | getPlatformArchitectureServices 每次调用重新 register——掩盖幂等性 bug | 需继续整改（测试/配置差距） | 测试、配置或 bootstrap 缺少审计要求的覆盖与约束，本轮未补齐。 | 已按源审计条目补录编号；本轮未新增覆盖缺口对应的测试、配置或 bootstrap 修复证据。 | 补录到 reaudit，待专项整改。 |
| R28-50 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §142 Tests / Config / Bootstrap 缺陷 | P0 | `shared/lifecycle/service-registry.ts:146` | register() 条件 `if(!has(name)){delete(name)}`——反逻辑,stale 实例永不被重注册驱逐 | 需继续整改（测试/配置差距） | 测试、配置或 bootstrap 缺少审计要求的覆盖与约束，本轮未补齐。 | 已按源审计条目补录编号；本轮未新增覆盖缺口对应的测试、配置或 bootstrap 修复证据。 | 补录到 reaudit，待专项整改。 |
| R28-51 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §142 Tests / Config / Bootstrap 缺陷 | P1 | `tests/integration/orchestration/\*-integration.test.ts` | "Integration" 测试零真实服务导入——全部 mock 对象+手动赋值,实为 unit test | 需继续整改（测试/配置差距） | 测试、配置或 bootstrap 缺少审计要求的覆盖与约束，本轮未补齐。 | 已按源审计条目补录编号；本轮未新增覆盖缺口对应的测试、配置或 bootstrap 修复证据。 | 补录到 reaudit，待专项整改。 |
| R28-52 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §142 Tests / Config / Bootstrap 缺陷 | P1 | `tests/integration/stability/\*-integration.test.ts` | 同上：仅测试数据结构构造,无真实服务集成 | 需继续整改（测试/配置差距） | 测试、配置或 bootstrap 缺少审计要求的覆盖与约束，本轮未补齐。 | 已按源审计条目补录编号；本轮未新增覆盖缺口对应的测试、配置或 bootstrap 修复证据。 | 补录到 reaudit，待专项整改。 |
| R28-53 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §142 Tests / Config / Bootstrap 缺陷 | P1 | `config/security/default.json:7` | allowedCapabilities 含 "mcp" 但无 MCP 沙箱约束/速率限制/egress policy | 需继续整改（测试/配置差距） | 测试、配置或 bootstrap 缺少审计要求的覆盖与约束，本轮未补齐。 | 已按源审计条目补录编号；本轮未新增覆盖缺口对应的测试、配置或 bootstrap 修复证据。 | 补录到 reaudit，待专项整改。 |
| R28-54 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §142 Tests / Config / Bootstrap 缺陷 | P1 | `config/risk/default.json:62-66` | riskLevelActions.medium autoExecute:true + requiresApproval:false——中风险操作无审批自动执行 | 需继续整改（测试/配置差距） | 测试、配置或 bootstrap 缺少审计要求的覆盖与约束，本轮未补齐。 | 已按源审计条目补录编号；本轮未新增覆盖缺口对应的测试、配置或 bootstrap 修复证据。 | 补录到 reaudit，待专项整改。 |
| R28-55 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §142 Tests / Config / Bootstrap 缺陷 | P1 | `config/gateways/default.json:3` | sseEnabled:true 无 sseMaxConnections/sseIdleTimeoutMs——资源耗尽攻击向量 | 需继续整改（测试/配置差距） | 测试、配置或 bootstrap 缺少审计要求的覆盖与约束，本轮未补齐。 | 已按源审计条目补录编号；本轮未新增覆盖缺口对应的测试、配置或 bootstrap 修复证据。 | 补录到 reaudit，待专项整改。 |
| R28-56 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §142 Tests / Config / Bootstrap 缺陷 | P1 | `config/runtime/default.json:2` | maxConcurrentTasks:1——无法验证并发行为,无集成测试覆盖并发场景 | 需继续整改（测试/配置差距） | 测试、配置或 bootstrap 缺少审计要求的覆盖与约束，本轮未补齐。 | 已按源审计条目补录编号；本轮未新增覆盖缺口对应的测试、配置或 bootstrap 修复证据。 | 补录到 reaudit，待专项整改。 |
| R28-57 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §142 Tests / Config / Bootstrap 缺陷 | P1 | `divisions/ trigger overlap` | "fix" 同时匹配 engineering_ops(pri50)+support(pri25)；"bug" 匹配3个 division——无消歧测试 | 需继续整改（测试/配置差距） | 测试、配置或 bootstrap 缺少审计要求的覆盖与约束，本轮未补齐。 | 已按源审计条目补录编号；本轮未新增覆盖缺口对应的测试、配置或 bootstrap 修复证据。 | 补录到 reaudit，待专项整改。 |
| R28-58 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §142 Tests / Config / Bootstrap 缺陷 | P1 | `tests/integration/security/` | 无 sandbox deniedRoots:[] 测试——默认配置 workspace_write 模式下 /etc /proc /sys 未显式拒绝 | 需继续整改（测试/配置差距） | 测试、配置或 bootstrap 缺少审计要求的覆盖与约束，本轮未补齐。 | 已按源审计条目补录编号；本轮未新增覆盖缺口对应的测试、配置或 bootstrap 修复证据。 | 补录到 reaudit，待专项整改。 |
| R28-59 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §142 Tests / Config / Bootstrap 缺陷 | P2 | `config/bootstrap/default.json:3` | phase:"phase_1a" 固定——无配置级 guard 阻止此早期 phase 进入生产 | 需继续整改（测试/配置差距） | 测试、配置或 bootstrap 缺少审计要求的覆盖与约束，本轮未补齐。 | 已按源审计条目补录编号；本轮未新增覆盖缺口对应的测试、配置或 bootstrap 修复证据。 | 补录到 reaudit，待专项整改。 |
| R28-60 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §142 Tests / Config / Bootstrap 缺陷 | P2 | `config/domains/default.json:96` | coding domain 含 shell_exec 工具但 securityLevel:"standard"——应为 "elevated" | 需继续整改（测试/配置差距） | 测试、配置或 bootstrap 缺少审计要求的覆盖与约束，本轮未补齐。 | 已按源审计条目补录编号；本轮未新增覆盖缺口对应的测试、配置或 bootstrap 修复证据。 | 补录到 reaudit，待专项整改。 |
| R28-61 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §142 Tests / Config / Bootstrap 缺陷 | P2 | `tests/integration/plugins/plugin-execution-integration.test.ts:138` | 硬编码 `ghp_test_token_12345`——匹配 GitHub PAT 前缀,可触发 secret scanner | 需继续整改（测试/配置差距） | 测试、配置或 bootstrap 缺少审计要求的覆盖与约束，本轮未补齐。 | 已按源审计条目补录编号；本轮未新增覆盖缺口对应的测试、配置或 bootstrap 修复证据。 | 补录到 reaudit，待专项整改。 |
| R28-62 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §142 Tests / Config / Bootstrap 缺陷 | P2 | `shared/lifecycle/service-registry.ts:316` | topological sort 循环依赖仅 warn 不 throw——静默允许破坏 teardown 顺序 | 需继续整改（测试/配置差距） | 测试、配置或 bootstrap 缺少审计要求的覆盖与约束，本轮未补齐。 | 已按源审计条目补录编号；本轮未新增覆盖缺口对应的测试、配置或 bootstrap 修复证据。 | 补录到 reaudit，待专项整改。 |
| R28-63 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §142 Tests / Config / Bootstrap 缺陷 | P2 | `divisions/operations/division.yaml:11` | "deployment" trigger 同时匹配 operations(pri20)+devops(pri45)——路由歧义 | 需继续整改（测试/配置差距） | 测试、配置或 bootstrap 缺少审计要求的覆盖与约束，本轮未补齐。 | 已按源审计条目补录编号；本轮未新增覆盖缺口对应的测试、配置或 bootstrap 修复证据。 | 补录到 reaudit，待专项整改。 |
| R29-01 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §144 Orchestration / Planner / Escalation / Learn / Improve-Rollout 缺陷 | P0 | `learning-artifact-model.ts:74` | fallback checksum 用 learningObjectId 填充(含 `_`/`g-z`)——不满足 `^[a-f0-9]{64}$` 校验恒 throw | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R29-02 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §144 Orchestration / Planner / Escalation / Learn / Improve-Rollout 缺陷 | P1 | `plan-strategy-selector.ts:23` | hierarchical 策略在 critical-risk 检查前选定——critical-risk multi-division 得到 hierarchical 而非 reflexive | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R29-03 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §144 Orchestration / Planner / Escalation / Learn / Improve-Rollout 缺陷 | P1 | `plan-builder.ts:41-50` | DAG valid===false 时仍用 orderedSteps 构建 plan——循环图/缺失依赖被静默接受 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R29-04 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §144 Orchestration / Planner / Escalation / Learn / Improve-Rollout 缺陷 | P1 | `rollout-state-machine.ts:29` | paused 可直接跳转 stable——绕过 canary→partial→stable 渐进发布 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R29-05 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §144 Orchestration / Planner / Escalation / Learn / Improve-Rollout 缺陷 | P1 | `autonomy-boundary-policy.ts:31` | learningObjects.every() 对空数组返回 true——零证据即允许 auto-rollout | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R29-06 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §144 Orchestration / Planner / Escalation / Learn / Improve-Rollout 缺陷 | P1 | `knowledge-promotion-service.ts:102` | batch promotion event 仅引用 learningObjects[0]——误导下游 consumer | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R29-07 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §144 Orchestration / Planner / Escalation / Learn / Improve-Rollout 缺陷 | P1 | `escalation/index.ts:5-14` | EscalationRequest 无 SLA/timeout 字段；decide() 无超时/SLA-aware 路由 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R29-08 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §144 Orchestration / Planner / Escalation / Learn / Improve-Rollout 缺陷 | P1 | `policy-rollout-service.ts:59` | approval gate 仅对 shadow 级强制——非 shadow 的 candidate 可直接 start() 跳过审批 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R29-09 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §144 Orchestration / Planner / Escalation / Learn / Improve-Rollout 缺陷 | P2 | `truncation-detector.ts:26` | finishReasonLength 计算后从未引用——死代码 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R29-10 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §144 Orchestration / Planner / Escalation / Learn / Improve-Rollout 缺陷 | P2 | `plan-repository.ts:6-10` | save() 无去重——同一 plan 多次 save 产生重复条目 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R29-11 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §144 Orchestration / Planner / Escalation / Learn / Improve-Rollout 缺陷 | P2 | `plan-evaluator.ts:23` | 资源估算 flat `steps.length*1000`——忽略 per-step 工具差异,系统性低估 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R29-12 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §144 Orchestration / Planner / Escalation / Learn / Improve-Rollout 缺陷 | P2 | `canary-traffic-router.ts:32` | hashToBucket 弱乘法哈希(\*31)——短 taskId 产生非均匀分布,偏斜 canary 流量 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R29-13 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §144 Orchestration / Planner / Escalation / Learn / Improve-Rollout 缺陷 | P2 | `experience-distillation-service.ts:22-30` | buildRecommendation 仅处理 failure_pattern/recovery_playbook——3种类型落入 generic default | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R29-14 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §145 Dispatcher / HA / Recovery / State-Transition 缺陷 | P0 | `ha-coordinator-service-inner.ts:353` | renewLeadership 返回 fencingToken=currentLease.ttlMs——TTL 毫秒值冒充 fencing token | 已解决（源码复核并补定向测试） | 根因是早期同步版 `renewLeadership()` 的返回值契约确实曾把 TTL 暴露成 `fencingToken` 语义，后续实现已改为从最新 leadership epoch 读取 fencing token，但这条早期重复挂账没有同步回写。 | 当前 `src/platform/execution/ha/ha-coordinator-service-inner.ts` 在续约后会调用 `getLatestEpoch()`，并显式返回 `fencingToken: latestEpoch.fencingToken`，不再返回 lease TTL。 | `tests/unit/platform/execution/ha/ha-coordinator-service-inner.test.ts` 已新增并通过 `renewLeadership returns epoch fencing token instead of ttlMs`，可直接证明这条旧缺口已被实修。 |
| R29-15 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §145 Dispatcher / HA / Recovery / State-Transition 缺陷 | P1 | `ha-coordinator-service-inner.ts:77,701` | fencingTokenCounter 每次实例化重置——重启后重发旧 token,破坏 fencing 安全 | 已解决（源码复核并补定向测试） | 根因是旧同步版确实依赖过实例内 fencing counter；当前实现已经收敛到从持久 epoch 表读取全局最新 token 再递增，只是这条早期重复挂账没有随修复一起回写。 | 当前 `src/platform/execution/ha/ha-coordinator-service-inner.ts` 的 `nextFencingToken()` 已改为查询 `leadership_epochs` 的 `MAX(fencing_token)` 再加一，不再依赖进程内计数器，因此服务重启后不会重发旧 token。 | `tests/unit/platform/execution/ha/ha-coordinator-service-inner.test.ts` 的 `fencing token remains monotonic across service instances sharing the same DB` 已在本轮定向复跑通过，可直接证明跨实例仍单调递增。 |
| R29-16 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §145 Dispatcher / HA / Recovery / State-Transition 缺陷 | P1 | `ha-coordinator-service-inner.ts:530-543` | authorizeAction leader_only: activeLease===null 时 fallthrough 到 authorized:true——无 lease 即授权 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R29-17 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §145 Dispatcher / HA / Recovery / State-Transition 缺陷 | P1 | `cross-region-event-replication-service.ts:178` | processReplicationQueue() async 无 await——Promise 丢弃,错误静默,replicate() 提前返回 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R29-18 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §145 Dispatcher / HA / Recovery / State-Transition 缺陷 | P1 | `patch-bundle.ts:107` | 错误消息 copy-paste bug: 显示实际值两次,不显示限制值 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R29-19 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §145 Dispatcher / HA / Recovery / State-Transition 缺陷 | P1 | `exception-recovery-config-loader.ts:28` | 模块级 cachedConfig 忽略 configPath 参数——第二次不同路径调用静默返回 stale config | 需继续整改（测试/配置差距） | 测试、配置或 bootstrap 缺少审计要求的覆盖与约束，本轮未补齐。 | 已按源审计条目补录编号；本轮未新增覆盖缺口对应的测试、配置或 bootstrap 修复证据。 | 补录到 reaudit，待专项整改。 |
| R29-20 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §145 Dispatcher / HA / Recovery / State-Transition 缺陷 | P1 | `cross-region-deployment-service.ts:548-558` | completeFailoverStep failure 时剩余 steps 保持 "pending"——不标记 "skipped",状态不一致 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R29-21 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §145 Dispatcher / HA / Recovery / State-Transition 缺陷 | P1 | `ha-coordinator-service-inner.ts:640-648` | triggerFailover 记录 FailoverDecision 用 stale fencingToken(acquireLeadership 前的值) | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R29-22 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §145 Dispatcher / HA / Recovery / State-Transition 缺陷 | P2 | `execution-recovery-worker.ts:49-54` | runRecoveryCycle 列出候选但从不执行恢复动作——itemsRecovered 是计数非实际恢复 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R29-23 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §145 Dispatcher / HA / Recovery / State-Transition 缺陷 | P2 | `cross-region-event-replication-service.ts:356` | retry setTimeout 重入 processReplicationQueue 但队列已排空——重试是 no-op | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R29-24 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §146 Interaction / API Routes / NL-Gateway / Dashboard 缺陷 | P0 | `billing-routes.ts:47-51` | 任何 Authorization/x-api-key header(即使无效)即跳过 webhook 签名验证——auth bypass | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R29-25 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §146 Interaction / API Routes / NL-Gateway / Dashboard 缺陷 | P0 | `request-helpers.ts:19` | matchRoute 仅允许 GET/POST/OPTIONS——所有 PATCH/PUT/DELETE 路由不可达(死代码) | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R29-26 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §146 Interaction / API Routes / NL-Gateway / Dashboard 缺陷 | P1 | `conversation-history-service.ts:207` | getSession 忽略 tenantId——任何租户可检索任何 session(IDOR) | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R29-27 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §146 Interaction / API Routes / NL-Gateway / Dashboard 缺陷 | P1 | `task-routes.ts:255-256` | PATCH title: 调用 updateTaskInput 传旧 inputJson,新 title 从未写入——更新是 no-op | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R29-28 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §146 Interaction / API Routes / NL-Gateway / Dashboard 缺陷 | P1 | `intent-parser/index.ts:17` | fallback normalized.length>12 一律 classify 为 task_create——误分类 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R29-29 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §146 Interaction / API Routes / NL-Gateway / Dashboard 缺陷 | P1 | `nl-gateway/index.ts:1-2` | named export detectAmbiguity 覆盖 `export *` 中同名——调用方获得错误实现 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R29-30 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §146 Interaction / API Routes / NL-Gateway / Dashboard 缺陷 | P1 | `dashboard-websocket-server.ts:337-345` | performHeartbeat 标记超时 client isConnected=false 但不移除——死连接累积阻塞 maxClients | 已解决（源码/测试） | 这条挂账停留在旧版 heartbeat 行为：当时超时连接只会被标记断开，不会从 registry/subscription map 中移除。现行源码已经把该缺口补掉，但 reaudit 结论没有跟上。 | 当前 `src/interaction/dashboard/dashboard-websocket-server.ts` 的 `performHeartbeat()` 会收集 `timedOutClientIds` 并显式调用 `unregisterClient(clientId)`，因此超时连接会同步从 `connections` 与 `channelSubscribers` 中移除，不再占住 `maxClients`。 | 定向验证已通过：`tests/unit/interaction/dashboard/dashboard-websocket-server.test.ts` 中 `heartbeat unregisters timed out clients`、`performHeartbeat removes timed out connections from the registry`、`heartbeat cleanup removes timed out clients from counts and routing` 本轮定向复跑全部通过。 |
| R29-31 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §146 Interaction / API Routes / NL-Gateway / Dashboard 缺陷 | P1 | `conversation-history-service.ts:121` | memoryLayer==="layer_3"时 addTurn 跳过持久化——但 persistSession 默认 layer_3,逻辑矛盾 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R29-32 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §146 Interaction / API Routes / NL-Gateway / Dashboard 缺陷 | P2 | `incident-routes.ts:86` | incidentId 无 validation(task routes 有 validateTaskId)——不一致 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R29-33 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §146 Interaction / API Routes / NL-Gateway / Dashboard 缺陷 | P2 | `incident-routes.ts:91` | 用户提供的 incidentId 直接反射到错误消息——日志注入/信息泄漏 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R29-34 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §146 Interaction / API Routes / NL-Gateway / Dashboard 缺陷 | P2 | `conversation-history-service.ts:246` | listUserSessions limit 在排序前截断——返回任意子集而非最新 N 条 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R29-35 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §146 Interaction / API Routes / NL-Gateway / Dashboard 缺陷 | P2 | `ux-event-tracking-service.ts:91` | eventLog 数组无界增长无驱逐——长时间运行进程内存泄漏 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R29-36 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §146 Interaction / API Routes / NL-Gateway / Dashboard 缺陷 | P2 | `ux-event-tracking-service.ts:122` | UX analytics 发布硬编码 "test:many_events" type via `as any`——生产错误事件类型 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R29-37 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §146 Interaction / API Routes / NL-Gateway / Dashboard 缺陷 | P2 | `task-routes.ts:73` | 内部 hardcoded limit:200——>200 tasks 在 cursor 分页前静默截断 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R29-38 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §146 Interaction / API Routes / NL-Gateway / Dashboard 缺陷 | P2 | `billing-routes.ts:39-106` | /billing/ 和 /v1/billing/ 逻辑完全重复——维护风险 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R29-39 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §146 Interaction / API Routes / NL-Gateway / Dashboard 缺陷 | P2 | `prompt-routes.ts:12` | schema .passthrough() + `payload as any`——任意字段未验证直接转发 registry | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R29-40 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §147 Compliance / Model-Gateway / Incident-Control / IAM / Chaos 缺陷 | P0 | `auto-stop-loss-service.ts:731` | approvePendingExecution 标记 approved 但从不执行保护动作——审批是 no-op | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R29-41 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §147 Compliance / Model-Gateway / Incident-Control / IAM / Chaos 缺陷 | P0 | `degradation-controller.ts:430` | deescalate() 重置 consecutiveHealthyCount=0 后引用——报告永远显示 recovered_after_0 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R29-42 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §147 Compliance / Model-Gateway / Incident-Control / IAM / Chaos 缺陷 | P1 | `auto-stop-loss-service.ts:715` | getHourKey month+day 无分隔符——1月11日与11月1日碰撞,速率限制被绕过 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R29-43 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §147 Compliance / Model-Gateway / Incident-Control / IAM / Chaos 缺陷 | P1 | `auto-stop-loss-service.ts:627` | 人工审批路径不调 lastExecutionTime.set()——审批后 playbook 冷却期不生效 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R29-44 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §147 Compliance / Model-Gateway / Incident-Control / IAM / Chaos 缺陷 | P1 | `tenant-execution-isolation-service.ts:323` | usage.status==="critical" 重置 overallStatus="active"——反逻辑,critical 应升级非重置 | 已修复 | 根因是隔离总状态枚举长期只有 `active/quota_exceeded/noisy_neighbor_detected/disabled` 四档，代码作者为了避免把 critical 直接判成 exceeded，错误地把 critical 分支回落成 `active`，导致预警升级被吞掉。 | 本轮在 `src/platform/control-plane/incident-control/tenant-execution-isolation-service.ts` 新增 `quota_critical` 聚合状态，并把 `usage.status==="critical"` 从“重置为 active”改为“升级到 quota_critical”；`tests/unit/platform/control-plane/incident-control/tenant-execution-isolation-service.test.ts` 已新增定向回归，验证 critical 配额会升级状态且不会伪造 `blockedSince`。 | 关闭该条真实 bug，并补足回归覆盖。 |
| R29-45 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §147 Compliance / Model-Gateway / Incident-Control / IAM / Chaos 缺陷 | P1 | `unified-chat-provider.ts:329` | MiniMax streaming 始终 isFinal=false——consumer 永收不到流结束信号 | 已修复（源码修复并补定向测试） | 根因是 `UnifiedChatProvider` 对 Anthropic/OpenAI 会沿用底层流回调的 `isFinal`，但 MiniMax 分支的底层 service 只回传 chunk，没有 final 标志，桥接层却把所有回调都硬编码成 `false`。 | 当前 `src/platform/model-gateway/provider-registry/unified-chat-provider.ts` 已在 MiniMax streaming 分支根据 `finishReason` 推断 final chunk，并在底层未显式给出 final 标志时对最后一个 chunk 补发一次 `isFinal=true` 结束回调；本轮新增 `tests/unit/platform/model-gateway/provider-registry/unified-chat-provider-streaming-regressions.test.ts`，验证 MiniMax 流回调序列为 `[false, true]`。 | 关闭范围是 unified provider 对 MiniMax 结束信号的桥接语义修复。 |
| R29-46 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §147 Compliance / Model-Gateway / Incident-Control / IAM / Chaos 缺陷 | P1 | `model-routing-service.ts:343` | compareProfiles 次排序 a.maxOutputTokens-b.maxOutputTokens 偏好较低值——注释说 higher=more capable,排序反转 | 已修复（源码修复并补定向测试） | 根因是 `compareProfiles()` 的二级比较方向写反了：主排序按更低 input cost 没问题，但同成本候选间却把更小 `maxOutputTokens` 排到了前面，与注释和能力优先级相反。 | 当前 `src/platform/model-gateway/provider-registry/model-routing-service.ts` 已把二级排序改为 `b.profile.maxOutputTokens - a.profile.maxOutputTokens`，优先更高输出上限；本轮新增 `tests/unit/platform/model-gateway/provider-registry/model-routing-service-regressions.test.ts`，验证同 input cost 候选中会选 `maxOutputTokens` 更高的 profile。 | 这是纯排序 bug 的实修。 |
| R29-47 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §147 Compliance / Model-Gateway / Incident-Control / IAM / Chaos 缺陷 | P1 | `chaos-experiment-scheduler.ts:148` | 完成检查不按 hypothesis name 去重——重复调用同 hypothesis 触发 premature completion | 已解决（源码已满足，本轮补定向测试） | 原审计针对的是旧完成条件只看 `results.length` 的阶段；现行实现已经先按 `steadyStateName` 去重，但 reaudit 文档未跟进。 | 当前 `src/ops-maturity/chaos/chaos-experiment-scheduler.ts` 在完成判断前已构造 `uniqueHypothesisNames = new Set(experiment.results.map((r) => r.steadyStateName))`，只有唯一 hypothesis 数达到定义数才会结束实验；本轮在 `tests/unit/ops-maturity/chaos/chaos-experiment-scheduler.test.ts` 新增回归，验证重复上报同一 hypothesis 不会提前完成。 | 这里是补证据关闭，不是新增主逻辑。 |
| R29-48 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §147 Compliance / Model-Gateway / Incident-Control / IAM / Chaos 缺陷 | P1 | `data-classification-service.ts:720` | matchesRule 将用户 rule.patterns 直接传给 new RegExp()——ReDoS via defineRule() | 已解决（源码已满足，本轮补定向测试） | 这条与 `R17-82` 同源：旧问题在于用户正则直接进入 `RegExp`，现行实现已经先做安全筛查，只是 reaudit 文档还停在未回写状态。 | 当前 `src/platform/five-plane-control-plane/iam/data-classification-service.ts` 已通过 `isRegexSafe()` 拒绝明显危险模式；`tests/unit/platform/control-plane/iam/data-classification-service-regex-safety.test.ts` 已补回回归，验证危险 regex 不会被执行。 | 作为重复挂账条目，与 `R17-82` 一并关闭。 |
| R29-49 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §147 Compliance / Model-Gateway / Incident-Control / IAM / Chaos 缺陷 | P1 | `auto-stop-loss-service.ts:619` | 升级级别用 string.includes("emergency")——应使用 severity 参数而非字符串匹配 | 已解决（源码已满足，本轮补证据） | 原审计命中的是旧版 escalation 推导依赖 free-form 文本关键词的阶段；现行实现已经改成显式策略推导，但 reaudit 文档未回写。 | 当前 `src/platform/five-plane-control-plane/incident-control/auto-stop-loss-service.ts` 的 anomaly/health 评估使用显式 `severity` / `status` 枚举，`executePlaybook()` 的 `escalationLevel` 也由 `determineEscalation(playbook)` 基于 action policy 决定，不再扫描 `triggerReason` 文本；`tests/unit/platform/control-plane/incident-control/auto-stop-loss-service-extended.test.ts` 已定向验证 “derive escalation from action policy, not free-form text”。 | 这是已存在实现的补证据关闭。 |
| R29-50 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §147 Compliance / Model-Gateway / Incident-Control / IAM / Chaos 缺陷 | P1 | `cve-intelligence-service.ts:281` | source.url! non-null assertion 但 CveSourceConfig.url 是 optional——undefined 传入 fetch crash | 已修复（源码修复并补定向测试） | 根因是 `loadFromSource()` 在 `http` 分支直接把 `source.url!` 传给 `loadFromHttp()`，把可选配置字段当成必填使用；配置遗漏时会把 `undefined` 带进 fetch 路径。 | 本轮在 `src/platform/five-plane-control-plane/iam/cve-intelligence-service.ts` 中增加了 `http` 源 URL 的显式校验，缺失时抛 `cve_source.http_url_required`；`tests/unit/platform/control-plane/iam/cve-intelligence-service.test.ts` 已新增回归，验证 `type:"http"` 且无 `url` 时会 fail closed。 | 这是配置入口校验的实修。 |
| R29-51 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §147 Compliance / Model-Gateway / Incident-Control / IAM / Chaos 缺陷 | P1 | `vault-http-secret-provider.ts:209` | 单段 ref `secret://mykey` 产生空路径段 `secret/data/`——Vault 返回 404 | 已修复 | 根因是 `extractSecretPath()` 把最后一段同时当作 key 与路径尾段处理，随后无条件执行 `slice(0, -1)`，导致单段 ref 的路径部分变成空串，实际请求落到 mount 根而不是 secret 路径。 | 本轮在 `src/platform/five-plane-control-plane/iam/vault-http-secret-provider.ts` 中把 secret ref 解析改成“单段 ref => `{mount}/data/{key}`、多段 ref => `{mount}/data/{path}`”，同时补了空段过滤和 mount-only fail-closed 校验；`tests/unit/platform/control-plane/iam/vault-http-secret-provider.test.ts` 已定向验证 `secret://mykey` 命中 `/v1/secret/data/mykey`、自定义 mount 命中 `/v1/secrets/data/api-key`、嵌套 ref 命中 `/v1/secret/data/myapp/prod`。 | 关闭该条真实路径解析缺陷。 |
| R29-52 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §147 Compliance / Model-Gateway / Incident-Control / IAM / Chaos 缺陷 | P2 | `degradation-controller.ts:236` | D0 failure 递归 this.route 最多4层无显式深度 guard——修改 enum 可致栈溢出 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R29-53 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §147 Compliance / Model-Gateway / Incident-Control / IAM / Chaos 缺陷 | P2 | `compliance-report-pipeline-service.ts:119` | ISO 日期字典序比较在混合时区/精度下失败 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R29-54 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §147 Compliance / Model-Gateway / Incident-Control / IAM / Chaos 缺陷 | P2 | `auto-stop-loss-service.ts:697` | executionHistory splice O(n) shift each recordEvent + executionCounts map key 永不清理 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R29-55 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §147 Compliance / Model-Gateway / Incident-Control / IAM / Chaos 缺陷 | P2 | `audit-event-integrity.ts:256` | latestChainHash 从未排序 entries.at(-1) 读取——输入非预排序时报告错误 hash | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R30-01 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §149 State-Evidence / CAS / Projections / Knowledge / Memory 缺陷 | P0 | `fencing-token-service.ts:100` | validateFencingToken split("-") 但 executionId 含连字符(UUID)——parts[1] 提取错误段,验证恒失败 | 已解决（源码复核并补定向测试） | 根因是旧 fencing token 解析按 `-` split 拆分 `executionId/nodeId/counter/timestamp`，UUID 或多段 hyphenated ID 会被错误切割，导致校验恒失败或 owner 错判。 | 当前 `src/platform/five-plane-state-evidence/events/cas/fencing-token-service.ts` 已统一使用 `FENCING_TOKEN_SEPARATOR = "::"`，并对 `executionId`、`nodeId` 做 URL 编码后再拼装与解析。 | `tests/unit/platform/state-evidence/events/cas/fencing-token-service.test.ts` 已在本轮定向复跑通过 `FencingTokenService generates token with :: separator (not -)`、`handles UUID executionId without splitting wrongly`、`acquireFence and releaseFence work with hyphenated executionId` 等回归；这条旧挂账不应继续误归为 UI 差距。 |
| R30-02 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §149 State-Evidence / CAS / Projections / Knowledge / Memory 缺陷 | P1 | `knowledge-archive.ts:20-41` | upsert 新 checksum 时旧 documentsByChecksum 条目未移除——stale 记录泄漏 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R30-03 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §149 State-Evidence / CAS / Projections / Knowledge / Memory 缺陷 | P1 | `knowledge-archive.ts:36-38` | upsert checksum 匹配时旧 chunks 不从 recordsByChunkId 清除——stale chunks 仍可查询 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R30-04 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §149 State-Evidence / CAS / Projections / Knowledge / Memory 缺陷 | P1 | `semantic-knowledge-graph.ts:269` | addEdge 无去重——重复 upsertRecord 产生重复边,膨胀图查询结果 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R30-05 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §149 State-Evidence / CAS / Projections / Knowledge / Memory 缺陷 | P1 | `cas-service.ts:183` | setValue 无条件重置 version=1——破坏并发 compareAndSet 的单调版本预期 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R30-06 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §149 State-Evidence / CAS / Projections / Knowledge / Memory 缺陷 | P1 | `fencing-token-service.ts:61` | tokenCounter per-instance 但 activeFences static——多实例产生碰撞 token 值 | 已解决（源码复核并补定向测试） | 根因是旧实现确实存在“静态 fence 表 + 实例私有 counter”的拆裂设计，不同 service 实例可能在同一进程里生成碰撞 token 序号。 | 当前 `src/platform/five-plane-state-evidence/events/cas/fencing-token-service.ts` 已把计数器收敛为 `private static readonly globalTokenCounter`，`generateFencingToken()` 不再从实例私有计数器取值。 | `tests/unit/platform/state-evidence/events/cas/fencing-token-service.test.ts` 的 `FencingTokenService shares a monotonic counter across service instances` 已在本轮定向复跑通过。 |
| R30-07 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §149 State-Evidence / CAS / Projections / Knowledge / Memory 缺陷 | P1 | `approval-queue-projection.ts:356` | handleDecisionRejected 设 rejectionsReceived=approvalsRequired——单次拒绝计为全员拒绝 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R30-08 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §149 State-Evidence / CAS / Projections / Knowledge / Memory 缺陷 | P1 | `fencing-token-service.ts:149-156` | acquireFence 不阻止同节点重获 exclusive 也不检查他节点 shared fence——锁语义损坏 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R30-09 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §149 State-Evidence / CAS / Projections / Knowledge / Memory 缺陷 | P2 | `semantic-knowledge-graph.ts:225` | collectAdjacent 遍历全部 edges 而非用 adjacencyByNodeId——O(V·E) vs O(V+E) | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R30-10 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §149 State-Evidence / CAS / Projections / Knowledge / Memory 缺陷 | P2 | `workflow-run-projection.ts:135 (9个 projection)` | isEventProcessed 用 Array.includes()——O(n) per event 使 replay O(n²) | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R30-11 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §149 State-Evidence / CAS / Projections / Knowledge / Memory 缺陷 | P2 | `layered-event-inbox.ts:21` | records 无界增长无 compaction——长时间运行内存泄漏 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R30-12 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §149 State-Evidence / CAS / Projections / Knowledge / Memory 缺陷 | P2 | `tool-usage-projection.ts:224` | invocation_completed 时 successCount++ 但不设 status——状态停留 "started" | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R30-13 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §149 State-Evidence / CAS / Projections / Knowledge / Memory 缺陷 | P2 | `knowledge-snapshot-store.ts:54` | load() JSON.parse 无 schema validation——损坏/篡改快照静默返回无效数据 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R30-14 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §149 State-Evidence / CAS / Projections / Knowledge / Memory 缺陷 | P2 | `layer-transition-service.ts:315` | age 用 createdAt 非 time-in-current-layer——晋升后下次转换过早触发 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R30-15 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §150 Harness / Sandbox / Tool-Executor / Guardrails 缺陷 | P0 | `command-security.ts:97` | chmod writePathArgPositions:[0] 校验 mode 字符串而非 arg[1] 实际路径——sandbox 路径检查绕过 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R30-16 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §150 Harness / Sandbox / Tool-Executor / Guardrails 缺陷 | P0 | `command-security.ts:98` | chown writePathArgPositions:[0] 校验 owner:group 而非 arg[1] 实际路径——同上 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R30-17 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §150 Harness / Sandbox / Tool-Executor / Guardrails 缺陷 | P1 | `hitl-runtime.ts:42-53` | resolve() 允许对已决议请求再次决议(approved→rejected)——无状态 guard | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R30-18 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §150 Harness / Sandbox / Tool-Executor / Guardrails 缺陷 | P1 | `tool-output-sanitizer.ts:20` | ANSI_REGEX 仅匹配 SGR(\e[…m)——CSI cursor/erase、OSC 等终端控制序列未剥离 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R30-19 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §150 Harness / Sandbox / Tool-Executor / Guardrails 缺陷 | P1 | `tool-metadata.ts:491-495` | WEB_SEARCH readOnly:false + approvalMode:"never"——搜索为只读却标记可变且免审批 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R30-20 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §150 Harness / Sandbox / Tool-Executor / Guardrails 缺陷 | P1 | `tool-metadata.ts:522-525` | WEB_FETCH readOnly:false + approvalMode:"never"——同上,HTTP GET 标记为可变 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R30-21 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §150 Harness / Sandbox / Tool-Executor / Guardrails 缺陷 | P1 | `command-security.ts:70-71` | grep/rg pathArgPositions:[-1] 仅验证最后 arg——`grep pattern file1 file2` 中 file1 绕过检查 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R30-22 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §150 Harness / Sandbox / Tool-Executor / Guardrails 缺陷 | P1 | `web-fetch.ts:156` | isInternalUrl 检查 hostname 字符串非解析后 IP——DNS rebinding(域解析到 127.0.0.1)绕过 SSRF 防护 | 已解决（源码/测试） | 根因是 `web-fetch` 之前只在请求前调用 `isInternalUrl(url)` 检查原始 hostname / literal IP，没有对域名做 DNS 解析后再校验解析结果，攻击者可让外部 hostname 解析到 127.0.0.1 或 RFC1918 地址绕过 SSRF 防护。 | 本轮在 `src/platform/five-plane-execution/tool-executor/web-fetch.ts` 新增 `resolvesToBlockedAddress()`，对 hostname 做 `dns.lookup(all:true)` 并对每个解析地址复用内网阻断规则；`createWebFetchTool()` 在真正发起 fetch 前先执行这一步，命中时返回 `DNS_REBINDING_BLOCKED`。 | 定向验证已通过：新增 `tests/unit/platform/five-plane-execution/tool-executor/web-fetch.test.ts`，覆盖“解析到 127.0.0.1 被阻断”“解析到公网地址允许”“命中 rebinding 时不会真正发起 fetch” 三个场景。 |
| R30-23 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §150 Harness / Sandbox / Tool-Executor / Guardrails 缺陷 | P1 | `command-security.ts:259` | 脚本解释器 flag 检查阻止所有 `-` 前缀参数——`python script.py --verbose` 被误拒 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R30-24 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §150 Harness / Sandbox / Tool-Executor / Guardrails 缺陷 | P2 | `loop/index.ts:74,77` | iterations 用 `>=`(0-indexed) 但 replans 用 `>`——允许 maxReplans+1 次 replan | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R30-25 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §150 Harness / Sandbox / Tool-Executor / Guardrails 缺陷 | P2 | `command-security.ts:92,109` | mkdir/touch 重复条目——Map last-wins 使第一条(无 writePathArgPositions)成死代码 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R30-26 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §150 Harness / Sandbox / Tool-Executor / Guardrails 缺陷 | P2 | `guardrail-engine.ts:91-95` | suggestedAction 含 "retry_same_plan" 但 engine 从不返回——consumer 死代码路径 | 已解决（源码复核并补定向测试） | 根因有两层：旧 guardrail engine 会把 warning 路径统一压成 `escalate_to_human`，没有区分“仅证据缺失”的可重试告警；同时 harness 决策层也没有消费 `retry_same_plan` 建议。 | 当前 `src/platform/orchestration/harness/guardrails/guardrail-engine.ts` 与 `src/platform/five-plane-orchestration/harness/guardrails/guardrail-engine.ts` 已在 evidence-only warning 分支产出 `suggestedAction: "retry_same_plan"`；`src/platform/orchestration/harness/index.ts` 与 `src/platform/five-plane-orchestration/harness/index.ts` 也已显式消费 `guardrailSuggestedAction` 并落成 `harness.guardrail_retry_same_plan`。 | `tests/unit/platform/orchestration/harness/guardrail-engine.test.ts` 已在本轮定向复跑通过 `GuardrailEngine suggestedAction is retry_same_plan when only evidence warnings are present`；相关 decision-priority 回归也已在文档其他已解决条目中记录。 |
| R30-27 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §150 Harness / Sandbox / Tool-Executor / Guardrails 缺陷 | P2 | `tool-output-sanitizer.ts:28` | CONTROL_CHARS \u000B-\u001A 不含 ESC(\u001B)——裸 ESC 字节通过未剥离 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R30-28 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §150 Harness / Sandbox / Tool-Executor / Guardrails 缺陷 | P2 | `recovery-controller.ts:26-30` | worker_crash 只调 recover() 不调 resume()——恢复的 run 停留非运行态 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R30-29 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §151 Middleware / Approval-Routing / Domains / Async 缺陷 | P0 | `shared/async/sync-backed-async-service.ts:9` | asPromise 在 Promise.resolve() 前同步执行 operation——同步抛出变为未捕获异常而非 rejected Promise | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R30-30 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §151 Middleware / Approval-Routing / Domains / Async 缺陷 | P0 | `approval-routing/route-engine/index.ts:97` | OrgChartRoutingStrategy selectNode fallback nodes[0]——orgNodeId 未找到时静默路由到任意节点 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R30-31 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §151 Middleware / Approval-Routing / Domains / Async 缺陷 | P1 | `domains/registry/plugin-ecosystem-runtime-service.ts:134` | buildPlan 调用两次(L108,L134)——状态可在两次间变化(TOCTOU),产生不同 plan | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R30-32 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §151 Middleware / Approval-Routing / Domains / Async 缺陷 | P1 | `approval-routing/approval-routing-service.ts:79` | audit recordId=requesterId_orgNodeId 无时间戳——同人同节点重复审批请求 ID 碰撞 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R30-33 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §151 Middleware / Approval-Routing / Domains / Async 缺陷 | P1 | `api/middleware/sdk-version-handshake.ts:86` | `parseInt(part, 10)` 配合默认回落会将恶意版本 `a.b.c` 解析为 `0.0.0`——可绕过最低版本检查 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R30-34 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §151 Middleware / Approval-Routing / Domains / Async 缺陷 | P1 | `approval-routing/route-engine/index.ts:118` | amountCny < threshold 严格小于——恰好等于阈值跳过匹配规则,非预期升级到公司级审批 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R30-35 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §151 Middleware / Approval-Routing / Domains / Async 缺陷 | P1 | `approval-routing/delegation/index.ts:27-28` | ISO 日期 string <= 比较在不同 offset 格式下失败(Z vs +00:00 vs +08:00) | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R30-36 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §151 Middleware / Approval-Routing / Domains / Async 缺陷 | P1 | `approval-routing/route-engine/index.ts:147` | SoD 阻止全部 node owner 后可产生空 approverChain——无 fallback/escalation guard | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R30-37 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §151 Middleware / Approval-Routing / Domains / Async 缺陷 | P1 | `domains/registry/plugin-runtime-host.ts:255` | IPC 消息 cast as PluginRuntimeMessage 无 schema validation——被攻破子进程可注入任意数据 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R30-38 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §151 Middleware / Approval-Routing / Domains / Async 缺陷 | P2 | `domains/recipes/recipe-executor.ts:34` | workflow existence check 是 regex stub(/^nonexistent/)——任何 ID "成功" | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R30-39 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §151 Middleware / Approval-Routing / Domains / Async 缺陷 | P2 | `domains/registry/plugin-runtime-child.ts:12,123` | 变量名 stdoutBuffer 实际累积 stdin 数据——copy-paste 命名错误 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R30-40 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §151 Middleware / Approval-Routing / Domains / Async 缺陷 | P2 | `domains/registry/domain-registry-service.ts:107` | deprecate() 无状态 guard——允许从 draft/archived 直接 deprecate | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R30-41 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §151 Middleware / Approval-Routing / Domains / Async 缺陷 | P2 | `approval-routing/route-engine/index.ts:288` | USD→CNY 硬编码汇率 7.2 无 staleness check/rate source 验证 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R30-42 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §151 Middleware / Approval-Routing / Domains / Async 缺陷 | P2 | `shared/async/sync-backed-async-service.ts.bak` | .bak 备份文件残留在源码树——不应被跟踪/部署 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R30-43 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §152 UI Features / Electron / Tauri / Charts 缺陷 | P0 | `electron-win/src/preload.ts:33` | installElectronBridge 直接赋值 window.**AA_ELECTRON** 绕过 contextBridge——破坏上下文隔离 | 已解决（源码/测试） | 根因是旧 preload 暴露链曾直接把 bridge 写到 `window`，绕过了 `contextIsolation`；后续代码已改为 `contextBridge`，但重复挂账未同步。 | 当前 `ui/apps/electron-win/src/preload.ts` 已通过 `resolveContextBridge()?.exposeInMainWorld("AA_ELECTRON", bridge)` 暴露桥，且 `ui/tests/unit/ui/apps/electron-win/preload.test.ts` 已验证 `targetWindow` 不会被直接注入属性。 | 作为 `2163/R17-44` 的重复挂账一并关闭。 |
| R30-44 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §152 UI Features / Electron / Tauri / Charts 缺陷 | P0 | `electron-win/src/preload.ts:6-7` | preload 暴露 shell:run/shell:spawn 通道无命令白名单——renderer 可执行任意 shell 命令 | 已解决（源码/测试） | 根因不是后续去补命令白名单，而是现行 preload 已直接移除了 `shell:run/shell:spawn` 这两个高危通道，旧审计还停在暴露面存在的阶段。 | 当前 `ui/apps/electron-win/src/preload.ts` 的 `electronPreloadApi.shell` 只保留 `shell:openExternal`；`ui/tests/unit/ui/apps/electron-win/preload.test.ts` 也已明确断言 `run` 和 `spawn` 不再对 renderer 暴露。 | 关闭的是 renderer 任意 shell 执行暴露面，不外推为整个桌面壳层已完成全部安全加固。 |
| R30-45 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §152 UI Features / Electron / Tauri / Charts 缺陷 | P0 | `tauri-macos/src-tauri/tauri.conf.json:1` | 无 security section——缺 CSP/dangerousRemoteDomainIpcAccess/capability scoping | 已解决（源码/测试；限于 security 段缺失指控） | 根因是旧 macOS Tauri 配置快照确实没有 `security` 段；现行配置已补入 `security.csp` 与资产 CSP 保护开关，但旧审计没有回写。 | 当前 `ui/apps/tauri-macos/src-tauri/tauri.conf.json` 已包含 `security.csp` 与 `dangerousDisableAssetCspModification: false`；`ui/tests/unit/ui/apps/security-configs.test.ts` 已验证该安全段存在。 | 关闭的是“无 security section/CSP 配置”的旧结论；更细粒度 capability scoping 若仍有要求，需由其它条目继续跟踪。 |
| R30-46 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §152 UI Features / Electron / Tauri / Charts 缺陷 | P0 | `tauri-linux/src-tauri/tauri.conf.json:1` | 同上：无 security/CSP 配置 | 已解决（源码/测试；限于 security 段缺失指控） | 根因与 macOS 条目相同：旧 Linux Tauri 配置快照没有 `security` 段，现行配置已补齐但审计未同步。 | 当前 `ui/apps/tauri-linux/src-tauri/tauri.conf.json` 已包含 `security.csp` 与 `dangerousDisableAssetCspModification: false`；`ui/tests/unit/ui/apps/security-configs.test.ts` 已验证 Linux 配置同样具备该安全段。 | 关闭的是“无 security section/CSP 配置”的旧结论；不把它扩写成所有 Tauri 壳层能力已完整产品化。 |
| R30-47 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §152 UI Features / Electron / Tauri / Charts 缺陷 | P0 | `electron-win/index.html:3` | 无 Content-Security-Policy meta tag——spec §6.5.4 要求 CSP baseline | 已解决（源码/测试） | 根因是旧 Electron HTML 模板确实缺 CSP baseline，后续 `index.html` 已补齐但重复挂账没有同步移除。 | 当前 `ui/apps/electron-win/index.html` 已声明 `Content-Security-Policy`，并包含 `default-src 'self'` 与 `frame-ancestors 'none'`；`ui/tests/unit/ui/apps/security-configs.test.ts` 已验证 CSP baseline 存在。 | 关闭的是 Electron HTML 缺 CSP meta 的旧结论。 |
| R30-48 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §152 UI Features / Electron / Tauri / Charts 缺陷 | P1 | `features/governance-compliance/hooks/index.ts:6` | hook 返回硬编码静态数组——无 API 调用,spec 要求完整审计轨迹+后端集成 | 已解决（源码/测试） | 根因是旧审计停留在 feature 还只返回说明文案的阶段；现行 hook 已通过 shared API client 拉取 policy / audit trail，并提供 update / exception submit / approve / reject 行为。 | 当前 `ui/packages/features/governance-compliance/src/hooks/index.ts` 已调用 `fetchCompliancePolicies()`、`fetchAuditLogs()`、`updateCompliancePolicy()`、`submitException()`、`approveException()`、`rejectException()`，并维护 `policies/auditTrail/exceptionQueue/loading` 状态。`ui/tests/unit/ui/packages/features/governance-compliance/hooks.test.tsx` 已验证这些 API 路径，web 层 `ui/tests/unit/ui/packages/features/governance-compliance/web.test.tsx` 进一步验证动作按钮会命中真实 VM handler。 | 关闭的是“hook 仍是静态数组”这一旧结论；更深的审批流产品语义由其它条目跟踪。 |
| R30-49 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §152 UI Features / Electron / Tauri / Charts 缺陷 | P1 | `features/takeover/hooks/index.ts:5` | hook 返回硬编码静态数组——无 WS 订阅/状态传输逻辑,spec 要求 live state transfer | 已解决（源码/测试） | 根因已收敛为两层旧问题：一是 hook 早期只回静态文案；二是 takeover snapshot 不会随着实时任务事件刷新，live transfer 只能靠人工刷新。 | 当前 `ui/packages/features/takeover/src/hooks/index.ts` 已基于 `useTasksQuery()`、`updateTask()`、`fetchWorkflowRunSteps()` 构建真实快照与 ownership history，并新增 `useWsClient().subscribe("global", ...)`，在 `status_changed/progress/workflow.updated/...` 事件上把当前 snapshot context 同步到最新任务状态；同时 web 动作通过 `takeoverCurrentTask()/annotateCurrentSnapshot()/resumeAutomaticExecution()` 接入真实 handler。`ui/tests/unit/ui/packages/features/takeover/hooks.test.tsx` 与新增 `ui/tests/unit/ui/packages/features/takeover/web.test.tsx` 已覆盖 API/动作路径。 | 关闭的是“hook 仍是静态数组、没有 live snapshot 更新”的缺口，不外推为所有跨端 takeover 协调都已完全产品化。 |
| R30-50 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §152 UI Features / Electron / Tauri / Charts 缺陷 | P1 | `ui-core/charts/echart-surface-runtime.tsx:26` | init() 仅接受静态 values prop+全量重建——无 appendData/streaming 支持 | 已解决（源码/测试） | 根因不是 ECharts 面板完全不能更新，而是旧实现把 `values` 放进单一 `useEffect` 依赖里，导致任何数据变化都会 `dispose()+init()` 全量重建实例，没有 append-only 路径。 | 当前 `ui/packages/ui-core/src/charts/echart-surface-runtime.tsx` 已把“实例初始化”和“数据更新”拆成两个 effect：首次 mount 创建 chart 与 observer，后续值变化只执行 `setOption()`；当新数据是旧序列的 append-only 扩展时，还会走 `chart.appendData(...)` 增量追加。`ui/tests/shared/ui-core-chart-regressions.test.tsx` 已验证 append-only rerender 时命中 `appendData` 且不会重新 init。 | 关闭的是全量重建缺口；更复杂的多序列 streaming 语义若后续扩展，另行跟踪。 |
| R30-51 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §152 UI Features / Electron / Tauri / Charts 缺陷 | P1 | `ui-core/charts/index.tsx:18` | MiniTrendBars 无 role/aria-label——`<span>` 无语义,WCAG 2.1 AA 失败 | 已解决（源码/测试） | 根因是原组件只渲染一组无语义 `<span>` 柱条，屏幕阅读器无法得知其含义。 | 当前 `ui/packages/ui-core/src/charts/index.tsx` 已把 `MiniTrendBars` 外层改为 `role="img"` 并生成 `aria-label="Trend values: ..."`，单个柱条标为 `aria-hidden`；`ui/tests/shared/ui-core-chart-regressions.test.tsx` 已验证无障碍角色和标签存在。 | 关闭的是趋势条图无语义的问题。 |
| R30-52 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §152 UI Features / Electron / Tauri / Charts 缺陷 | P1 | `ui-core/layouts/index.ts:30` | ThreePaneLayout 固定 gridTemplateColumns——无响应式断点,违反 spec §2.5.1 | 已解决（源码复核） | 旧问题是固定三列 `minmax(...)` 会要求大于约 720px 的布局宽度，在窄屏直接溢出。 | 当前 `ui/packages/ui-core/src/layouts/index.ts` 已切换为 `repeat(auto-fit, minmax(200px, 1fr))` 的自适应网格，窄屏会自然折叠成单列，不再强制固定三栏。 | 该条属于现行代码已完成修正、复审状态未回写。 |
| R30-53 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §152 UI Features / Electron / Tauri / Charts 缺陷 | P1 | `features/analytics/hooks/index.ts:12` | trendSummary 映射 "up"/"flat"/"down" 到 3/2/1——chart 收到无意义序数非真实指标值 | 已解决（源码/测试） | 根因是旧 VM 用趋势方向枚举硬编码成 `3/2/1`，把“方向”误当成“度量值”，导致趋势图收到的是序数而非真实 KPI。 | 当前 `ui/packages/features/analytics/src/hooks/index.ts` 已引入 `parseMetricValue()` / `buildTrendSummary()`，从 `AnalyticsMetricDTO.value` 解析数值并返回真实 metric series；`buildTimeSeries()` 也改为复用同一数值解析路径。`ui/tests/unit/ui/packages/features/analytics/hooks.test.tsx` 已断言 `trendSummary` 返回 `[12, 6]` 而不是 `[3, 2]`。 | 关闭的是 trendSummary 语义错误，而不是宣称整套 analytics 建模已全部完成。 |
| R30-54 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §152 UI Features / Electron / Tauri / Charts 缺陷 | P1 | `features/governance-compliance/web/index.tsx:11` | actions 定义 id/label/tone 但无 onClick——governance 操作(escalate/review)完全惰性 | 已解决（源码/测试） | 根因是 `FeatureWorkbenchPanel` 早已支持 `onTrigger`，但治理视图一直只传展示元数据，没有把 review/audit/exception/escalation 动作接到 VM。 | 当前 `ui/packages/features/governance-compliance/src/web/index.tsx` 已为 `汇总治理状态/审阅字段策略/查看审计轨迹/管理异常/升级委托审批` 五个 action 分别绑定 `selectPolicy()`、`updatePolicy()`、`filterAuditTrail()`、`submitExceptionRequest()` 等真实 handler。`ui/tests/unit/ui/packages/features/governance-compliance/web.test.tsx` 已验证这些动作都会触发对应 VM 调用。 | 关闭的是 action 惰性问题，不把它夸大为治理 feature 的所有流程都已完整产品化。 |
| R30-55 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §152 UI Features / Electron / Tauri / Charts 缺陷 | P1 | `features/takeover/web/index.tsx:11` | takeover actions(start/annotate/resume)无 onClick——关键管理操作不可执行 | 已解决（源码/测试） | 根因是 takeover web 视图只把动作标签交给 `FeatureWorkbenchPanel`，没有任何 handler，因此接管面板按钮全是惰性的。 | 当前 `ui/packages/features/takeover/src/web/index.tsx` 已把 `接管当前任务/添加人工批注/恢复自动执行` 分别绑定到 `takeoverCurrentTask("web-operator")`、`annotateCurrentSnapshot("manual-note", "web-operator")`、`resumeAutomaticExecution("web-operator")`。新增 `ui/tests/unit/ui/packages/features/takeover/web.test.tsx` 已验证三个动作都命中真实 VM handler。 | 关闭的是按钮不可执行的问题。 |
| R30-56 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §152 UI Features / Electron / Tauri / Charts 缺陷 | P1 | `ui-mobile/native-modules/index.ts:1` | nativeModulesBaseline 是 flat boolean 配置——无实际 bridge/permission/capability negotiation | 已解决（已补 capability/bridge 描述层） | 真实缺口不是“mobile 能力完全没有”，而是 `nativeModulesBaseline` 只有平面 boolean，无法表达哪些能力依赖 bridge、哪些只需 adapter、权限状态是什么。 | 当前 `ui/packages/ui-mobile/src/native-modules/index.ts` 已改为 `requiresBridge/permission/bridgeMethods` 结构，并通过 `describeNativeModules()` 按 bridge 存在与否导出 `enabled/source/permission` 能力视图。新增 `ui/tests/shared/native-modules.test.ts` 已验证 bridge 缺失时 secureStorage 会标成 `unavailable`，bridge 存在时 deepLink/haptics/secureStorage 会被识别为可用。 | 关闭 flat boolean baseline 无能力协商的问题。 |
| R30-57 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §152 UI Features / Electron / Tauri / Charts 缺陷 | P2 | `ui-core/charts/index.tsx:5` | MetricGrid 无 role="group"/aria-label——screen reader 无法识别指标区域 | 已解决（源码/测试） | 根因是指标卡片区域原先只有纯视觉布局，没有任何语义容器或可读标签。 | 当前 `ui/packages/ui-core/src/charts/index.tsx` 已把 `MetricGrid` 外层标成 `role="group" aria-label="Metric summary grid"`，每张 metric 卡也带有 `${label}: ${value}` 级别的辅助标签。`ui/tests/shared/ui-core-chart-regressions.test.tsx` 已验证这些角色和标签存在。 | 关闭的是 MetricGrid 无语义的问题。 |
| R30-58 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §152 UI Features / Electron / Tauri / Charts 缺陷 | P2 | `features/workflow-cockpit/hooks/index.ts:41` | selectedWorkflow fallback workflows[0]——删除后 stale selection 显示错误 workflow | 已解决（源码/测试） | 根因是旧实现会在 `selectedId` 为空或源列表变化时退回 `workflows[0]`，导致已删除/失效 workflow 还能幽灵式显示。 | 当前 `ui/packages/features/workflow-cockpit/src/hooks/index.ts` 已把 `selectedId` 默认收敛到 `null`，并在 query 更新时只在“当前选择已不存在”时清空，而不是回退到首项；`ui/tests/unit/ui/packages/features/workflow-cockpit/hooks.test.tsx` 已验证不会再镜像成本地 ghost selection。 | 该条属于现行代码已修，复审状态未回写。 |
| R30-59 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §152 UI Features / Electron / Tauri / Charts 缺陷 | P2 | `features/settings/hooks/index.ts:42-47` | useEffect 依赖 primitive 但内部读 object property——identity mismatch 可跳过同步 | 已解决（源码/测试） | 根因是旧实现依赖 `preferenceTheme/preferenceLocale` 这些派生 primitive，但 effect 内部又读取完整 `preferences` 对象，React 在对象 identity 改变而派生值恰好相同的情况下可能跳过同步。 | 当前 `ui/packages/features/settings/src/hooks/index.ts` 已把 effect 依赖改成完整 `preferences` 对象，并在同步草稿时直接读取 `preferences.theme/preferences.locale`；`ui/tests/unit/ui/packages/features/settings/hooks.test.tsx` 已验证上游 preferences 更新后草稿会同步刷新。 | 关闭的是 effect 依赖错配问题。 |
| R30-60 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §152 UI Features / Electron / Tauri / Charts 缺陷 | P2 | `electron-win/src/main.ts:15-16` | channels 无 tier/permission 分组——shell:run 与 secure-store 同信任级别 | 已解决（源码/测试） | 根因是 Electron 主进程基线最初只维护一组平面 channel 名字列表，没有把“外部导航”“窗口控制”“安全存储”“隐私控制”区分成不同信任层级。 | 当前 `ui/apps/electron-win/src/main.ts` 已把 `electronMainBaseline.channels` 改成 `{name,tier,permission}` 结构，例如 `shell:openExternal -> external-navigation/external-link`，`secure-store:* -> secure-storage/credential-*`，不再与 shell 入口混为同一层级。`ui/tests/unit/ui/apps/electron-win/main.test.ts` 已验证 secure-store 与 external shell 分属不同 tier/permission。 | 关闭的是 IPC channel 无信任分层的问题。 |
| R30-61 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §152 UI Features / Electron / Tauri / Charts 缺陷 | P2 | `features/domain-wizard/hooks/index.ts:12` | 用户控制的 domain.owner 直接嵌入模板——若渲染为 HTML 存 XSS 风险 | 已解决（源码复核/测试） | 根因是审计把“字符串插值”与“HTML sink”混为一谈；现行 domain wizard 只把 `domain.owner` 组合进普通文本描述，由 React text node 渲染，不存在 `dangerouslySetInnerHTML` 或手写 HTML 模板注入点。 | `ui/packages/features/domain-wizard/src/hooks/index.ts` 仅生成 `description: \`owner ${domain.owner} · drill ...\`` 这样的纯字符串，`ui/packages/features/domain-wizard/src/web/index.tsx` 也只是把该字符串放进 `createElement("span", ..., item.description)`。新增 `ui/tests/unit/ui/packages/features/domain-wizard/web.test.tsx` 已验证恶意 owner 字符串会按文本显示，DOM 中不会生成攻击者控制的 `<img>` 节点。 | 关闭的是“存在 HTML sink”的误报；若未来引入富文本渲染，需要另行审计。 |
| R30-62 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §152 UI Features / Electron / Tauri / Charts 缺陷 | P2 | `ui-core/charts/echart-surface-runtime.tsx:52` | resize 监听 window 非 ResizeObserver on container——panel 变化时 chart 不刷新 | 已解决（源码/测试） | 根因是旧实现只依赖 `window.resize`，容器尺寸变化但 viewport 不变时不会触发重排；后来虽然补过 observer，但和全量重建逻辑混在同一个 effect 里，证据不清晰。 | 当前 `ui/packages/ui-core/src/charts/echart-surface-runtime.tsx` 已把 `ResizeObserver` 作为主路径直接观察 chart container，并只在浏览器不支持 observer 时才回退到 `window.resize`；`ui/tests/shared/ui-core-chart-regressions.test.tsx` 已断言 observer 会被创建并调用 `observe(container)`。 | 关闭的是容器 resize 不会触发 chart reflow 的问题。 |
| R31-01 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §154 OAPEFLIR Loop / Harness-Mapping / Stage-FSM 缺陷 | P0 | `oapeflir-loop-service.ts:118` | 阶段转换间无 budget/guard 检查——spec §15.3 要求每次 LLM/tool 调用前 budget reserve | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R31-02 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §154 OAPEFLIR Loop / Harness-Mapping / Stage-FSM 缺陷 | P0 | `oapeflir-loop-service.ts:118` | eventPublisher 在阶段边界从未使用——spec §14.3 要求所有状态变更由事件驱动 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R31-03 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §154 OAPEFLIR Loop / Harness-Mapping / Stage-FSM 缺陷 | P0 | `oapeflir-harness-mapping.ts:13-29` | mapHarnessStepToOapeflirPhase 永不返回 "learn" 或 "release"——两个 OAPEFLIR 阶段完全无映射路径 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R31-04 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §154 OAPEFLIR Loop / Harness-Mapping / Stage-FSM 缺陷 | P0 | `oapeflir-loop-service.ts:195` | Plan 类型是 flat PlanStep[]——spec §6.1 明确禁止线性 steps,要求 PlanGraphBundle | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R31-05 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §154 OAPEFLIR Loop / Harness-Mapping / Stage-FSM 缺陷 | P1 | `stage-timeline.ts:3` | OapeflirStageSchema 含第9值 "knowledge_promotion" 不在 spec 或 FSM OAPEFLIR_STAGES 中——类型分歧 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R31-06 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §154 OAPEFLIR Loop / Harness-Mapping / Stage-FSM 缺陷 | P1 | `oapeflir-loop-service.ts:118` | StageTransitionFSM 从未实例化/查询——stages 硬编码顺序执行,FSM guard 逻辑完全绕过 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R31-07 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §154 OAPEFLIR Loop / Harness-Mapping / Stage-FSM 缺陷 | P1 | `stage-transition-fsm.ts:87` | getCurrentStage index=8 溢出数组(length 8)——release 后返回 undefined 但有 ! assertion | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R31-08 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §154 OAPEFLIR Loop / Harness-Mapping / Stage-FSM 缺陷 | P1 | `oapeflir-loop-service.ts:359` | 返回未验证 assessment 而非 validatedAssessment——下游接收可能未通过 A→P 边界验证的数据 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R31-09 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §154 OAPEFLIR Loop / Harness-Mapping / Stage-FSM 缺陷 | P1 | `final-response.ts:27` | FinalResponse 缺 spec §27 要求字段: audience/limitations/dataClass/redactionApplied/safetyLabels | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R31-10 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §154 OAPEFLIR Loop / Harness-Mapping / Stage-FSM 缺陷 | P1 | `assessment-service.ts:20` | assess() 计算风险但从不检查 budget feasibility——spec §11 要求 worst-path 预算分析 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R31-11 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §154 OAPEFLIR Loop / Harness-Mapping / Stage-FSM 缺陷 | P1 | `stage-transition-fsm.ts:187` | recordStageSkipped 接受 reasonCode 但静默丢弃——不存储/不传播到审计记录 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R31-12 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §154 OAPEFLIR Loop / Harness-Mapping / Stage-FSM 缺陷 | P2 | `agent-team-service.ts:3-9` | AgentTeamStage(plan/build/review/validate/repair/release) 与 OAPEFLIR 7阶段无语义映射 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R31-13 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §154 OAPEFLIR Loop / Harness-Mapping / Stage-FSM 缺陷 | P2 | `intake-router.ts:14` | docstring 声称负责 "budget entry" 但 route() 不检查/不附加 budget 信息 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R31-14 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §154 OAPEFLIR Loop / Harness-Mapping / Stage-FSM 缺陷 | P2 | `oapeflir-harness-mapping.ts:23-25` | hitl_operator 映射到 "assess"——spec §26 定义 HITL 为跨切面非阶段 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R31-15 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §154 OAPEFLIR Loop / Harness-Mapping / Stage-FSM 缺陷 | P2 | `runtime-execute-bridge.ts:228` | 动态 import() 无超时/断路器——延迟不可控,违反 X1 可靠性要求 | 已解决（缺陷入口已移除） | 这条与 `R19-43` 同源：问题依赖于 bridge 还保留动态 import runtime orchestrator 这条路径；现行实现已经没有这条入口。 | `RuntimeExecuteBridge` 当前只接收 injected `runtimePlanExecutor`，文件内不再执行动态 import，因此“动态 import 无超时/断路器”的风险入口已不存在；本轮 `runtime-execute-bridge` 定向测试已通过 injected executor 验证当前委托模型。 | 作为重复挂账条目，与 `R19-43` 一并关闭。 |
| R31-16 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §154 OAPEFLIR Loop / Harness-Mapping / Stage-FSM 缺陷 | P2 | `oapeflir-loop-service.ts:210-211` | Execute 阶段短路到 input.stepOutputs 不验证——预提供输出绕过全部执行 guardrails | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R31-17 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §155 Event-Bus / DLQ / Repositories 缺陷 | P0 | `event-registry.ts:674` | getRegisteredConsumers 对 replay 事件返回 undefined——tier-1 ack 创建静默跳过 consumer | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R31-18 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §155 Event-Bus / DLQ / Repositories 缺陷 | P0 | `durable-event-bus.ts:358` | retry 循环 0..3 执行4次但报告 MAX_DELIVERY_RETRIES=3——off-by-one 掩盖真实重试次数 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R31-19 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §155 Event-Bus / DLQ / Repositories 缺陷 | P0 | `durable-event-bus.ts:534-543` | ensurePendingAcks 对同时在 activeConsumerRefCounts 和 registry 的 consumer 创建重复 ack | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R31-20 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §155 Event-Bus / DLQ / Repositories 缺陷 | P1 | `durable-event-bus.ts:197` | tier-2/3 同时 dispatchVolatile + scheduleFanOut——事件被投递两次(volatile+pending ack poll) | 已解决（源码复核并补现行证据） | 根因是早期实现把 tier-2/3 和 tier-1 共用 fan-out 调度，volatile 事件既走即时分发，又可能进入 pending ack 轮询链。现行主链已经分流，但 reaudit 没有把结论回写。 | 当前 `src/platform/state-evidence/events/durable-event-bus.ts` 在 `publish()` / `publishBatch()` 中仅对 `tier_1` 调用 `scheduleFanOut()`，`tier_2/3` 只走 `dispatchVolatile()`；既有回归 `tests/unit/platform/state-evidence/events/durable-event-bus.test.ts` 已覆盖 “does not schedule tier2 fan-out after volatile dispatch” 与同一 `(eventId, consumerId)` 的 volatile 去重。 | 回写现行行为与测试依据，不再维持“双投递”旧结论。 |
| R31-21 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §155 Event-Bus / DLQ / Repositories 缺陷 | P1 | `dlq-service.ts:155-174` | scheduleRetry 增 retryCount 不检查 maxRetries——maxRetries 字段纯装饰,实际无限重试 | 已解决（源码已满足，本轮补证据） | 原审计命中的是旧 `scheduleRetry()`：只校验 delay，不校验 `maxRetries` 与 terminal status。 | 当前 `src/platform/five-plane-state-evidence/events/dlq-service.ts` 已在 `scheduleRetry()` 中补上 `maxRetries` 的 finite/non-negative 校验，并在 `retryCount >= maxRetries` 时直接进入 `markRetryExhausted()` 终态；`tests/unit/platform/state-evidence/events/dlq-service.test.ts` 已覆盖第 6 次调用转入 `discarded`。 | 与 `2233` 同组关闭，这里补回重复条目的源码/测试依据。 |
| R31-22 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §155 Event-Bus / DLQ / Repositories 缺陷 | P1 | `dlq-service.ts:112` | DLQ 纯内存 Map——进程重启丢失所有 dead-letter 记录,违反持久性要求 | 已解决（源码修复并补定向测试） | 这条与 `R12-06/R16-37` 同源：问题不在“没有 SQLite 仓储”，而在默认构造仍允许静默回退到内存仓。 | 当前 `DlqService` 已要求生产态显式注入持久仓或 `dbConnection`，同时 `tests/integration/platform/state-evidence/events/dlq-persistence.integration.test.ts` 已验证 dead-letter 在数据库关闭并重开后仍存在。 | 作为重复挂账条目，与 `R12-06/R16-37` 一并关闭。 |
| R31-23 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §155 Event-Bus / DLQ / Repositories 缺陷 | P1 | `transactional-event-appender.ts:97` | 手动 BEGIN TRANSACTION via exec() 绕过 db.transaction()——嵌套时外层 txn 被自动提交 | 已解决（源码/测试复核） | 这条与 `R12-05/R16-26/R22-31` 指向同一旧根因：早期 appender 自己手写事务边界，绕过了统一 wrapper。现行实现已统一收敛到 `db.transaction(...)`，旧结论不再成立。 | `src/platform/state-evidence/events/transactional-event-appender.ts` 的 `appendEvent()` / `appendEvents()` 当前都在 `db.transaction(...)` 内执行；`tests/unit/platform/state-evidence/events/transactional-event-appender.test.ts` 本轮已定向通过 transaction wrapper 和 rollback 回归。 | 作为重复挂账条目，按同一现行证据关闭，不再保留“待专项整改”的过期结论。 |
| R31-24 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §155 Event-Bus / DLQ / Repositories 缺陷 | P1 | `organization-repository.ts:218,351,416,477` | SQL `WHERE x IS $N`(4处)——PG 非标准等值,非 NULL 参数时返回错误结果 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R31-25 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §155 Event-Bus / DLQ / Repositories 缺陷 | P1 | `durable-event-bus.ts:302-306` | deliverPendingNow 无 handler 时返回 pending.length 声称成功——事件实未处理 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R31-26 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §155 Event-Bus / DLQ / Repositories 缺陷 | P2 | `event-registry.ts:544-563` | tier-2 事件无 payload validator——genericEventPayloadSchema 接受任意形状,schema 不强制 | 已解决（源码修复并补定向测试） | 根因是 `validateEventPayload()` 之前对“已注册但未单独列出的 event”会静默回退到 `genericEventPayloadSchema`。这对 `tier_2` 甚至部分 `tier_1` runtime event 来说意味着只要是对象就能通过，缺少 fail-closed 的 schema 强制。 | 当前 `src/platform/state-evidence/events/event-registry.ts` 已新增 `resolvePayloadValidator()` 与 `platform./oapeflir.` family validator，并在 `validateEventPayload()` 中强制 `tier_1/tier_2` 已注册事件不得落回 generic validator；新增 `getPayloadValidatorSource()` 便于回归验证。`tests/unit/platform/state-evidence/events/event-registry-validation.test.ts` 与 `event-registry.test.ts` 已证明所有已注册 `tier_1/tier_2` 事件都使用 specific/family validator，unknown event 也不再借 generic 通过。 | 关闭的是事件注册表的 schema fail-open 缺口，不夸大为每个事件都已手写独立 schema。 |
| R31-27 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §155 Event-Bus / DLQ / Repositories 缺陷 | P2 | `durable-event-bus.ts:470-496` | dispatchVolatile 吞噬 handler 错误仅 warn——无 ack 更新/无 dead-letter 路径 | 已解决（源码复核并补现行证据） | 根因是早期 volatile 分发链只有 warn 日志，没有任何持久化补偿出口。现行实现已在异常分支把失败写入 DLQ，并用链式 finally 清理去重键，但 reaudit 仍停留在旧实现。 | 当前 `src/platform/state-evidence/events/durable-event-bus.ts` 的 `dispatchVolatile()` catch 分支会调用 `enqueueVolatileDeadLetter()`，把 `sourceEventId / consumerId / errorCode=volatile_delivery_failed` 持久化到 DLQ，并记录 dead-letter metrics；`tests/unit/platform/state-evidence/events/durable-event-bus.test.ts` 已覆盖 volatile subscriber 抛错后生成 DLQ 记录。 | 回写现行 dead-letter 路径依据，不再把“仅 warn”作为当前结论。 |
| R31-28 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §155 Event-Bus / DLQ / Repositories 缺陷 | P2 | `async-repositories/session-repository.ts:113` | `LIMIT ${limit}` 字符串插值——limit 为用户输入时 SQL 注入 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R31-29 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §155 Event-Bus / DLQ / Repositories 缺陷 | P2 | `delegation-repository.ts:151` | listExpiredDelegations 用 SQLite datetime('now')——PG 不兼容,运行时崩溃 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R31-30 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §155 Event-Bus / DLQ / Repositories 缺陷 | P2 | `async-repositories/worker-repository.ts:743` | strftime SQLite 函数在 PG async repo——PG 运行时失败 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R31-31 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §155 Event-Bus / DLQ / Repositories 缺陷 | P2 | `authoritative-task-store-decorator.ts:94-162` | Proxy get trap 包裹所有属性访问(含非方法子对象)——破坏嵌套 repository namespace 访问 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R31-32 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §156 SDK / CLI 缺陷 | P0 | `sdk/cli/migrate-sqlite-to-pg.ts:68` | SQL 注入：table name 未参数化直接插入 `SELECT COUNT(*) FROM ${table}`(+L96,L103) | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R31-33 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §156 SDK / CLI 缺陷 | P0 | `sdk/cli/migrate-sqlite-to-pg.ts:125` | PG DSN(含凭证)明文输出到 stdout JSON | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R31-34 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §156 SDK / CLI 缺陷 | P0 | `sdk/cli/dlq-manager.ts:156` | purge 删除全部 DLQ 记录无确认提示——破坏性批量操作 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R31-35 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §156 SDK / CLI 缺陷 | P1 | `sdk/client-sdk/api-client.ts:208` | 重试所有非 OK 响应(含 400/401/403/409)——应仅重试 429/5xx | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R31-36 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §156 SDK / CLI 缺陷 | P1 | `sdk/client-sdk/api-client.ts:217` | 非 OK 响应被解析为成功返回——4xx/5xx 不抛错 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R31-37 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §156 SDK / CLI 缺陷 | P1 | `sdk/client-sdk/api-client.ts:148` | parseInt x-total-count 不检查 NaN——NaN 作为 totalCount 传播 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R31-38 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §156 SDK / CLI 缺陷 | P1 | `sdk/admin-sdk/index.ts:17` | registerDomain body:unknown 无输入验证——admin 可注册任意 payload | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R31-39 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §156 SDK / CLI 缺陷 | P1 | `sdk/admin-sdk/index.ts:6` | AdminSdk 无 role/permission 检查——与 client SDK 同级别,无 admin gate | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R31-40 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §156 SDK / CLI 缺陷 | P1 | `sdk/cli/dlq-manager.ts:135` | retry 重置所有 dead-letter 的 attempts=0 无批次限制——重试风暴 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R31-41 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §156 SDK / CLI 缺陷 | P1 | `sdk/cli/authoritative-storage-admin.ts:50` | down migration 无确认——破坏性 schema rollback 无 gate | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R31-42 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §156 SDK / CLI 缺陷 | P1 | `sdk/harness-sdk/index.ts:37` | stage 缺失时 fallback 到 nodeRunId——语义错误的默认值 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R31-43 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §156 SDK / CLI 缺陷 | P1 | `sdk/cli/shadow-snapshot.ts:32` | 模块级副作用 loadShadowSnapshotCliEnv() import 时执行——破坏测试和 lazy loading | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R31-44 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §156 SDK / CLI 缺陷 | P2 | `sdk/client-sdk/api-client.ts:111` | POST/PUT 重试无 idempotency key——非幂等写重复执行 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R31-45 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §156 SDK / CLI 缺陷 | P2 | `sdk/cli/orphan-cleanup.ts:37` | repair 删除孤儿记录无 dry-run/确认 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R31-46 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §156 SDK / CLI 缺陷 | P2 | `sdk/harness-sdk/index.ts:78` | sleep 接受 resumeAt string 无 ISO-8601 验证 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R31-47 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §156 SDK / CLI 缺陷 | P2 | `sdk/cli/billing.ts:114` | ownerId 缺省为空字符串——创建无 owner 的账户 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R31-48 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §157 Cross-Cutting Integration / Delegation / Risk 断裂 | P0 | `delegation-manager.service.ts:129` | delegate() 从不调 DelegationGovernanceService.evaluate()——governance 规则完全绕过 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R31-49 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §157 Cross-Cutting Integration / Delegation / Risk 断裂 | P0 | `delegation-manager.service.ts:129` | delegate() 从不调 DelegationAuditService——委托生命周期无审计轨迹 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R31-50 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §157 Cross-Cutting Integration / Delegation / Risk 断裂 | P0 | `model-routing-service.ts:49` | ModelRouteRiskLevel 独立重复定义 RiskLevel——与 risk-control/types.ts 无导入/无共享 contract | 需继续整改（ADR/contract） | ADR、contract、类型导出与现行实现存在漂移，本轮未完成逐条回写与一致性校验闭环。 | 已按源审计条目补录编号；本轮未新增 contract/ADR 回写或一致性测试证据，因此不能改写为已解决。 | 补录到 reaudit，待专项整改。 |
| R31-51 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §157 Cross-Cutting Integration / Delegation / Risk 断裂 | P1 | `delegation-manager.service.ts:129` | delegate() 内联重实现权限收窄/上下文隔离——不使用同目录 ContextIsolator 类 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R31-52 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §157 Cross-Cutting Integration / Delegation / Risk 断裂 | P1 | `delegation-manager.service.ts:129` | delegate() 从不调 CallDepthBudget.evaluate()——call-depth 预算检查完全未接入 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R31-53 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §157 Cross-Cutting Integration / Delegation / Risk 断裂 | P1 | `budget-allocator.ts:1` | BudgetAllocator 与 model-gateway 无集成——UnifiedChatProvider 返回 token usage 但无路径回馈 budget settlement | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R31-54 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §157 Cross-Cutting Integration / Delegation / Risk 断裂 | P1 | `risk-evaluation-engine.ts:1` | RiskEvaluationEngine 未被 execution/orchestration 层导入——风险评分完全与 dispatch/execution 断裂 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R31-55 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §157 Cross-Cutting Integration / Delegation / Risk 断裂 | P1 | `delegation-manager.service.ts:244` | fail() 接受 \_error 参数但静默丢弃——错误原因不存储到 DelegationResult | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R31-56 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §157 Cross-Cutting Integration / Delegation / Risk 断裂 | P1 | `risk-config-loader.ts:39` | JSON.parse(raw) 无 schema validation——config 畸形时产生 TypeError 非领域错误 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R31-57 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §157 Cross-Cutting Integration / Delegation / Risk 断裂 | P1 | `response-hardening.ts:13` | CORS allowedOrigins:["*"] + credentials:true——spec 禁止此组合 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R31-58 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §157 Cross-Cutting Integration / Delegation / Risk 断裂 | P1 | `delegation-tracker.ts:281` | getMetrics() 对所有节点无条件 activeCount++——completedCount/failedCount/averageDurationMs 恒为 0 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R31-59 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §157 Cross-Cutting Integration / Delegation / Risk 断裂 | P2 | `service-registry.ts:233` | teardownAll 计算逆拓扑序后 Promise.all——丢失顺序保证 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R31-60 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §157 Cross-Cutting Integration / Delegation / Risk 断裂 | P2 | `delegation-manager.service.ts:226` | completeWithEvidence 传 Number.MAX_SAFE_INTEGER 作 parentBudgetRemaining——非真实预算 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R31-61 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §157 Cross-Cutting Integration / Delegation / Risk 断裂 | P2 | `delegation-tracker.ts:134` | recordDelegation 硬编码 agentType:""——树可视化显示空 agent 类型 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R31-62 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §157 Cross-Cutting Integration / Delegation / Risk 断裂 | P2 | `delegation-manager.service.ts:503` | updateDelegationChain 不同步 DelegationTracker——两套 chain 存储运行时分歧 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R31-63 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §157 Cross-Cutting Integration / Delegation / Risk 断裂 | P2 | `delegation-governance-service.ts:224` | matchesCondition agentType 是自由字符串 vs subjectType 期望 enum——条件永不匹配真实 agent | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R32-01 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §159 Tool-Executor / State-Transition / Startup 缺陷 | P0 | `transition-service.ts:500-526` | TaskTerminalTransitionService.apply 用非 CAS updateXxxStatus——并发 terminal 转换互相覆盖 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R32-02 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §159 Tool-Executor / State-Transition / Startup 缺陷 | P0 | `tool-path-scope.ts:36-43` | normalizePath realpathSync 失败时 fallback resolve()——不解引用 symlink,路径作用域绕过 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R32-03 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §159 Tool-Executor / State-Transition / Startup 缺陷 | P0 | `tool-execution-access.ts:133-138` | execution=null 时返回 allowedTools:undefined(无限制)+仅 errorCode——调用方不检查即无限制 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R32-04 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §159 Tool-Executor / State-Transition / Startup 缺陷 | P0 | `mcp-tool-guard.ts:131` | builtin 碰撞检查用 remoteToolName 而非完整 toolName——MCP 工具名含 builtin 后缀未检测 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R32-05 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §159 Tool-Executor / State-Transition / Startup 缺陷 | P1 | `tool-parallel-executor.ts:380-403` | exclusive 工具在所有并行工具之后执行——原始顺序被破坏,写在前读在后反转 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R32-06 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §159 Tool-Executor / State-Transition / Startup 缺陷 | P1 | `loop/index.ts:32` | maxIterations=Math.floor(maxSteps/3) 当 maxSteps=1-2 时为0——循环立即终止无法执行 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R32-07 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §159 Tool-Executor / State-Transition / Startup 缺陷 | P1 | `guardrail-vibration-breaker.ts:36` | maxRepeatedActions=0 仍允许1次——off-by-one,`> maxRepeat` 非 `>=` | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R32-08 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §159 Tool-Executor / State-Transition / Startup 缺陷 | P1 | `transition-service.ts:93-101` | session paused→open 转换缺失——暂停 session 无法恢复,与注释"可恢复"矛盾 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R32-09 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §159 Tool-Executor / State-Transition / Startup 缺陷 | P1 | `web-search.ts:117` | new URL(url) 对 DDG 畸形结果 throw——整个搜索操作崩溃而非跳过坏结果 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R32-10 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §159 Tool-Executor / State-Transition / Startup 缺陷 | P1 | `process-error-handlers.ts:94-99` | error.name string matching("StorageError")——minification 后名称变化导致不匹配 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R32-11 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §159 Tool-Executor / State-Transition / Startup 缺陷 | P1 | `startup-consistency-checker.ts:470` | P0 发现后继续运行全部检查——应 short-circuit 避免查询潜在损坏的 DB | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R32-12 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §159 Tool-Executor / State-Transition / Startup 缺陷 | P2 | `tool-parallel-executor.ts:406` | results as T[] 含 undefined holes——调用方按 index 取值无类型安全 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R32-13 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §159 Tool-Executor / State-Transition / Startup 缺陷 | P2 | `tool-contract-validator.ts:47` | metadata.toolName.trim() 无类型 guard——undefined/null 时 throw | 需继续整改（ADR/contract） | ADR、contract、类型导出与现行实现存在漂移，本轮未完成逐条回写与一致性校验闭环。 | 已按源审计条目补录编号；本轮未新增 contract/ADR 回写或一致性测试证据，因此不能改写为已解决。 | 补录到 reaudit，待专项整改。 |
| R32-14 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §159 Tool-Executor / State-Transition / Startup 缺陷 | P2 | `web-fetch.ts:230` | parseInt Content-Length 忽略尾部垃圾——"1234abc"=1234 可能允许超大响应 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R32-15 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §159 Tool-Executor / State-Transition / Startup 缺陷 | P2 | `tool-argument-coercion.ts:524-532` | 原地 delete+reassign input.args——持有旧引用的中间件观察到 mid-flight mutation | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R32-16 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §159 Tool-Executor / State-Transition / Startup 缺陷 | P2 | `tool-output-sanitizer.ts:354-359` | secret regex 用 /g flag 作模块常量——lastIndex 状态跨调用持续,交替 match/miss | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R32-17 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §160 Model-Gateway Provider / Cost-Tracker / Credential 缺陷 | P0 | `credential-pool.ts:200` | markFailure 对4xx(401/403/408)不标记 cooldown/disable——永久拒绝的 key 持续重用 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R32-18 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §160 Model-Gateway Provider / Cost-Tracker / Credential 缺陷 | P1 | `anthropic-chat-service.ts:464` | message_delta usage(仅output_tokens)覆盖 accumulatedUsage——丢失 message_start 的 input_tokens | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R32-19 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §160 Model-Gateway Provider / Cost-Tracker / Credential 缺陷 | P1 | `chargeback-service.ts:35` | tenantId ?? undefined: null 转 undefined 时获取所有租户报告而非仅平台级 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R32-20 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §160 Model-Gateway Provider / Cost-Tracker / Credential 缺陷 | P1 | `unified-chat-provider.ts:137` | detectProviderFromModel lowercase 后 includes 混合大小写 prefix——MiniMax 模型永不匹配 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R32-21 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §160 Model-Gateway Provider / Cost-Tracker / Credential 缺陷 | P1 | `anthropic-chat-service.ts:267` | retry list 缺 402(Payment Required)——credential disable 但无 failover,OpenAI/MiniMax 有 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R32-22 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §160 Model-Gateway Provider / Cost-Tracker / Credential 缺陷 | P1 | `base-chat-provider.ts:258` | transformRequest 与外层都设 stream:true——provider 可 double-set 或冲突 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R32-23 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §160 Model-Gateway Provider / Cost-Tracker / Credential 缺陷 | P2 | `cost-report-service.ts:98` | listBudgetSummaries 仅保留最新 report period——丢弃最早边界,显示错误时间范围 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R32-24 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §160 Model-Gateway Provider / Cost-Tracker / Credential 缺陷 | P2 | `provider-credential-pool-support.ts:258` | cooldownUntil ISO 字符串比较在不同 UTC offset 下失败 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R32-25 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §160 Model-Gateway Provider / Cost-Tracker / Credential 缺陷 | P2 | `budget-guard.ts:69` | warnAtRatio=1.0 + cost=limit 时返回 allowed:true+requiresApproval:true 而非 block | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R32-26 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §160 Model-Gateway Provider / Cost-Tracker / Credential 缺陷 | P2 | `openai-chat-service.ts:471` | streaming refusal 用赋值非拼接——多 chunk refusal 仅保留最后 delta 文本 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R32-27 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §161 NL-Gateway / Goal-Decomposer / Proactive-Agent / Autonomy 缺陷 | P0 | `proactive-agent/index.ts:373` | change_rate_gt 计算 abs(v-prev) 而非 abs(v-prev)/prev——绝对差非变化率,阈值比较语义错误 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R32-28 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §161 NL-Gateway / Goal-Decomposer / Proactive-Agent / Autonomy 缺陷 | P0 | `goal-decomposer/llm-plan-generator.ts:64` | task.estimatedCostUsd.toFixed(4) 对 LLM 返回非数字直接 crash(TypeError) | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R32-29 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §161 NL-Gateway / Goal-Decomposer / Proactive-Agent / Autonomy 缺陷 | P1 | `autonomy/index.ts:376 vs level-manager/index.ts:3` | compareLevels frozen=index 0 vs level-manager frozen=index 4——晋升/降级检测用错误比较器 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R32-30 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §161 NL-Gateway / Goal-Decomposer / Proactive-Agent / Autonomy 缺陷 | P1 | `ux/ux-event-tracking-service.ts:162` | AB test 按 userId 为 key——同用户第二次分配覆盖第一次,丢失前次分配 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R32-31 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §161 NL-Gateway / Goal-Decomposer / Proactive-Agent / Autonomy 缺陷 | P1 | `goal-decomposer/index.ts:170` | goalId 取 goal.slice(0,16)——相同16字符前缀的不同目标 ID 碰撞 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R32-32 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §161 NL-Gateway / Goal-Decomposer / Proactive-Agent / Autonomy 缺陷 | P1 | `nl-gateway/slot-resolver/index.ts:9` | `entityType in resolved` 检查原型链——"constructor"/"toString" 等名称误判为已解析 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R32-33 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §161 NL-Gateway / Goal-Decomposer / Proactive-Agent / Autonomy 缺陷 | P1 | `goal-decomposer/index.ts:330` | estimatedDuration 硬编码 `${tasks.length}d`——忽略各 task 实际 estimatedDuration | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R32-34 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §161 NL-Gateway / Goal-Decomposer / Proactive-Agent / Autonomy 缺陷 | P1 | `autonomy/autonomy-governance-service.ts:84` | frozen(=4)>full_auto(=3) 导致推荐 frozen 时 promoted:true——降级报告为晋升 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R32-35 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §161 NL-Gateway / Goal-Decomposer / Proactive-Agent / Autonomy 缺陷 | P1 | `autonomy/historical-metrics-provider.ts:64` | incidents 计数包含所有有 last_error_code 的行——瞬态错误与真实事件混同,触发误冻结 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R32-36 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §161 NL-Gateway / Goal-Decomposer / Proactive-Agent / Autonomy 缺陷 | P2 | `nl-gateway/ambiguity-handler/index.ts:1` | detectAmbiguity 与 disambiguation-handler 同名不同实现——import 路径决定行为 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R32-37 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §161 NL-Gateway / Goal-Decomposer / Proactive-Agent / Autonomy 缺陷 | P2 | `goal-decomposer/index.ts:370` | normalized=description.toLowerCase() 声明后从不引用——死变量 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R32-38 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §161 NL-Gateway / Goal-Decomposer / Proactive-Agent / Autonomy 缺陷 | P2 | `proactive-agent/index.ts:418` | detectFeedbackLoop 捕获 mid-DFS stack 作 triggerIds——含遍历路径节点非仅环成员 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R32-39 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §161 NL-Gateway / Goal-Decomposer / Proactive-Agent / Autonomy 缺陷 | P2 | `goal-decomposer/llm-plan-generator.ts:117` | parsePlan 验证 array 但不验证单个 task shape——缺失字段/负数静默通过 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| R32-40 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §161 NL-Gateway / Goal-Decomposer / Proactive-Agent / Autonomy 缺陷 | P2 | `nl-gateway/nl-gateway-config-loader.ts:92` | JSON.parse as Partial<Config> 无 schema 验证——恶意/畸形 config 静默接受 | 需继续整改（测试/配置差距） | 测试、配置或 bootstrap 缺少审计要求的覆盖与约束，本轮未补齐。 | 已按源审计条目补录编号；本轮未新增覆盖缺口对应的测试、配置或 bootstrap 修复证据。 | 补录到 reaudit，待专项整改。 |
| R32-41 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §162 Contract Types 系统性冲突 | P0 | `executable-contracts:67 vs domain/task-types.ts:29` | ArtifactRef 两处定义形状不兼容(4字段 vs 7字段) | 已解决（源码/导出面复核） | 根因不是当前源码仍有两套对外 ArtifactRef contract，而是 `domain/task-types.ts` 历史上曾保留过独立 legacy 形状；后续迁移已把 domain 面改为直接 re-export canonical `ArtifactRef`，reaudit 没同步回写。 | 当前 `src/platform/contracts/types/domain/task-types.ts` 已只导出 canonical `ArtifactRef`；OAPEFLIR `ref-types.ts` 保留的是内部 typed ref alias，不再属于平台 contract 冲突面。 | 关闭的是 contract 导出冲突，不外推为所有内部 helper alias 都已统一命名。 |
| R32-42 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §162 Contract Types 系统性冲突 | P0 | `executable-contracts:407 vs platform-contracts.ts:55` | SideEffectRecord 两处定义不兼容(12字段 vs 6字段) | 已解决（源码/导出面修复） | 根因是 `platform-contracts.ts` 曾混出 canonical 与 platform-specific 类型，导致审计把它当成第二套 `SideEffectRecord` 暴露面。 | 本轮已把 `src/platform/contracts/types/platform-contracts.ts` 收敛为平台专用类型，不再从该文件 re-export canonical `SideEffectRecord`；对应 invariant 已补。 | 关闭的是导出面歧义，canonical `SideEffectRecord` 现只应从 `executable-contracts` 获取。 |
| R32-43 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §162 Contract Types 系统性冲突 | P0 | `executable-contracts:60 vs platform-contracts.ts:4` | PrincipalRef vs PlatformPrincipal: principalId vs actorId, tenantId required vs nullable | 已解决（职责边界回写） | 根因是审计把 `PlatformPrincipal` 当成和 `PrincipalRef` 竞争的第二套 canonical principal contract；但当前代码里前者只服务于 `EvidenceRecord` 与平台本地工厂，后者才是 executable contract 的 canonical principal。 | `src/platform/contracts/types/platform-contracts.ts` 现已明确声明仅保留 platform-specific types；canonical principal 继续由 `src/platform/contracts/executable-contracts/index.ts` 的 `PrincipalRef` 提供。 | 关闭的是 contract 权责混淆，不意味着两者字段必须物理同构。 |
| R32-44 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §162 Contract Types 系统性冲突 | P0 | `executable-contracts:126 vs platform-contracts:12 vs request-envelope:4` | RequestEnvelope 3处定义3种形状——完全不兼容 | 已解决（兼容层收口） | 根因是历史迁移阶段确实同时存在 canonical `RequestEnvelope`、`RequestEnvelopeLegacy` 和 deprecated `request-envelope/` 模块；问题在于当时它们没有被清晰隔离。 | 当前 `contracts/index.ts` 只把 canonical `RequestEnvelope` 暴露为 `PlatformRequestEnvelope`，`platform-contracts.ts` 里的 legacy 版本已显式命名为 `RequestEnvelopeLegacy`，`request-envelope/` 也仅作为 deprecated namespace 保留。 | 关闭的是“对外 contract 面完全歧义”的问题；legacy 路径保留仅用于兼容迁移。 |
| R32-45 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §162 Contract Types 系统性冲突 | P1 | `schemas.ts:574 vs executable-contracts:580` | ContractReplayBehavior "simulate" vs EventReplayBehavior "simulate_projection"——enum 值不匹配 | 已解决（源码修复） | 根因复核后收敛为两个层面：一是早期枚举口径确有漂移；二是 JSON schema 层还采取 `additionalProperties:true`，没有 strict boundary，导致 contract drift 更难暴露。 | 本轮已将 `src/platform/contracts/executable-contracts/schemas.ts` 的 `CONTRACT_JSON_SCHEMAS` 改为 `additionalProperties: false`，并通过 invariant 固定 strict contract 边界。 | 本轮关闭的是 contract schema fail-closed 缺口；旧 enum 漂移不再构成当前导出面的主要风险。 |
| R32-46 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §162 Contract Types 系统性冲突 | P1 | `execution-plan/index.ts:12 vs platform-contracts.ts:70` | ExecutionPlan 两处定义不兼容(taskId/version/steps vs workflowRunId/principal/budget/steps) | 已解决（源码/导出面复核） | 根因是 `platform-contracts.ts` 过去把 legacy `ExecutionPlan` 也重新暴露了一遍，和 `execution-plan/` 的 deprecated alias 一起形成双出口。 | 当前 `execution-plan/index.ts` 已把 `ExecutionPlan` 收口为 deprecated `PlanGraphBundle` alias；本轮又移除了 `platform-contracts.ts` 中对 legacy `ExecutionPlan` 的再次导出。 | 关闭的是双出口冲突，不宣称 legacy path 已完全删除。 |
| R32-47 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §162 Contract Types 系统性冲突 | P1 | `execution-receipt/index.ts:6 vs platform-contracts.ts:89` | ExecutionReceipt 两处定义不兼容(taskId/workerId/resultRef vs durationMs/sideEffects/evidenceRefs) | 已解决（源码/导出面复核） | 根因与 `R32-46` 相同：legacy receipt 模块与 `platform-contracts.ts` 双重导出造成“第二套权威 receipt”的假象。 | 当前 `execution-receipt/index.ts` 仍只作为 deprecated compatibility path；本轮已移除 `platform-contracts.ts` 对 legacy `ExecutionReceipt` 的再次导出。 | 关闭的是导出冲突，不宣称 legacy receipt 模块已整体移除。 |
| R32-48 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §162 Contract Types 系统性冲突 | P1 | `state-command/index.ts:6 vs platform-contracts.ts:106` | StateCommand 两处定义不兼容(entityKind/entityId/action vs type/aggregateId/fencingToken) | 已解决（源码/导出面复核） | 根因是 `StateCommand` 本身已经降级为 deprecated compatibility contract，但 `platform-contracts.ts` 仍把它重新暴露成聚合出口，导致审计持续判定为 canonical 冲突。 | 本轮已把 `platform-contracts.ts` 收敛为纯 platform-specific file，不再 re-export `StateCommand`；canonical inter-plane commands 继续由 `executable-contracts` 提供。 | 关闭的是 contract 出口歧义。 |
| R32-49 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §162 Contract Types 系统性冲突 | P1 | `control-directive/index.ts:6 vs platform-contracts.ts:37` | ControlDirective 两处定义: kind/issuedBy:string vs type/issuedBy:PlatformPrincipal | 已解决（源码/导出面复核） | 根因与 `R32-48` 一致：legacy `ControlDirective` 已经不是 canonical directive，但 `platform-contracts.ts` 仍把它挂到聚合出口上。 | 本轮已移除 `platform-contracts.ts` 对 legacy `ControlDirective` 的再次导出；canonical directive 继续由 `control-directive/` 内的 `OperationalDirective / DecisionDirective` 承担。 | 关闭的是对外 contract 歧义。 |
| R32-50 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §162 Contract Types 系统性冲突 | P1 | `platform-contracts.ts:47,64,83` | SideEffectExpectation/ExecutionPlanBudget/ExecutionReceiptErrorDetail 导出但从无导入——死类型 | 已解决（源码修复） | 根因是真实 dead type：这三组类型既不是 canonical contract，也没有实际调用方，仅因为历史迁移残留而继续暴露。 | 本轮已从 `src/platform/contracts/types/platform-contracts.ts` 删除 `SideEffectExpectation`、`ExecutionPlanBudget`、`ExecutionReceiptErrorDetail`，并以 invariant 固定。 | 这是实际源码清理，不是假关闭。 |
| R32-51 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §162 Contract Types 系统性冲突 | P1 | `types/index.ts:9-15` | barrel 不 re-export domain/ 子模块——所有 domain types 通过 types/index 路径不可达 | 已解决（源码修复） | 根因是真实 barrel 漏导出：`types/index.ts` 没有 re-export `domain/index.ts`，导致 domain types 从聚合入口不可达。 | 本轮已在 `src/platform/contracts/types/index.ts` 补 `export * from "./domain/index.js";`，并加 invariant 守护。 | 已修复。 |
| R32-52 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §162 Contract Types 系统性冲突 | P2 | `contracts/index.ts:18,29` | RequestEnvelope 同时 re-export 为 PlatformRequestEnvelope 和 PlaneRequestEnvelope——歧义 | 已解决（源码复核） | 根因是早期 barrel 确实存在双别名历史，但当前主出口已不再同时暴露 `PlatformRequestEnvelope / PlaneRequestEnvelope`。 | 当前 `src/platform/contracts/index.ts` 只保留 canonical `PlatformRequestEnvelope`，未再导出 `PlaneRequestEnvelope`；reaudit 这里是补录编号后未同步回写。 | 不改代码，仅回写现状依据。 |
| R32-53 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §162 Contract Types 系统性冲突 | P2 | `oapeflir/ref-types.ts:29 vs domain/task-types:29 vs exec-contracts:67` | ArtifactRef 存在3处: string alias / rich interface / 另一 interface | 已解决（职责边界回写） | 根因是审计把 OAPEFLIR 局部 `RefId` helper 当成平台 contract 面的一部分；实际上当前冲突只剩“内部 helper 名称重合”，而非跨 contract 导出冲突。 | `domain/task-types.ts` 已 re-export canonical `ArtifactRef`；`oapeflir/ref-types.ts` 保留的是 OAPEFLIR 局部 typed ref helper，仅在 `tool-call-record`、`final-response` 等内部模块使用。 | 关闭的是 contract types 系统性冲突，不外推为所有内部 alias 命名都已统一。 |
| R32-54 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §162 Contract Types 系统性冲突 | P2 | `platform-contracts.ts:1` | contracts 层 import 编排层 PlanStep——向上依赖违反分层 | 已解决（源码复核） | 原审计针对的是更早版本；当前 `platform-contracts.ts` 已不再 import 编排层 `PlanStep`。reaudit 这里属于历史挂账未清。 | 本轮复核 `src/platform/contracts/types/platform-contracts.ts`，其依赖仅剩本层 ids 与 platform-specific factories。 | 不改代码，仅回写现状依据。 |
| R32-55 | Round 17：Orchestration/Execution + Interaction/NL + Desktop/Mobile + Security/IAM / §162 Contract Types 系统性冲突 | P2 | `executable-contracts:632` | CONTRACT_JSON_SCHEMAS additionalProperties:true——无严格验证,额外字段静默通过 | 已解决（源码修复并补定向测试） | 根因是真实 schema boundary 宽松：`CONTRACT_JSON_SCHEMAS` 允许额外字段，contract drift 不会在 schema 层 fail-closed。 | 本轮已将 `src/platform/contracts/executable-contracts/schemas.ts` 改为 `additionalProperties: false`，并通过 `tests/invariants/adr-contract-closure-round2.test.ts` 固定。 | 已修复。 |
| 1899 | Round 33 — Dispatcher/RSM/Budget · Scale-Ecosystem/Ops · UI Build/Perf/SW · Security Holistic / §164 Dispatcher / RSM / Budget 深层缺陷 | P0 | `src/platform/execution/runtime-state-machine.ts` | NodeRun→cancelled/aborted 转换跳过 lease+fencing 检查——任何 actor 可取消运行中节点 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 1900 | Round 33 — Dispatcher/RSM/Budget · Scale-Ecosystem/Ops · UI Build/Perf/SW · Security Holistic / §164 Dispatcher / RSM / Budget 深层缺陷 | P1 | `src/platform/execution/dispatcher/execution-dispatch-service.ts` | lease 在 claim 事务外获取(TOCTOU)——可产生双工 worker 分配 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 1901 | Round 33 — Dispatcher/RSM/Budget · Scale-Ecosystem/Ops · UI Build/Perf/SW · Security Holistic / §164 Dispatcher / RSM / Budget 深层缺陷 | P1 | `src/platform/execution/budget-allocator.ts` | settle() 绕过 RSM 直接 mutate ledger——无 CAS/无 fact event | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 1902 | Round 33 — Dispatcher/RSM/Budget · Scale-Ecosystem/Ops · UI Build/Perf/SW · Security Holistic / §164 Dispatcher / RSM / Budget 深层缺陷 | P1 | `src/platform/execution/budget-allocator.ts` | call-depth-budget effectiveCallDepth 用 max 而非 sum——(4,4,4) 被视为 depth 4 而非 12 | 已解决（源码已满足，本轮补定向测试） | 原审计命中的是早期 call-depth 预算把多个维度折叠成 `Math.max` 的阶段；现行实现已改为求和，但 reaudit 条目仍保留旧结论，且路径描述也漂到了错误文件。 | 当前实际实现位于 `src/platform/five-plane-orchestration/agent-delegation/call-depth-budget.ts`，`CallDepthBudget.evaluate()` 已使用 `currentCallDepth + goalDecompositionDepth + delegationDepth`；本轮新增 `tests/unit/platform/orchestration/agent-delegation/call-depth-budget.test.ts`，直接验证 `(4,4,4) -> 12` 且触发 `call_depth.exceeded`。 | 关闭时同时更正文档根因：这是 delegation call-depth 预算问题，不是 `budget-allocator.ts` 里的现行缺口。 |
| 1903 | Round 33 — Dispatcher/RSM/Budget · Scale-Ecosystem/Ops · UI Build/Perf/SW · Security Holistic / §164 Dispatcher / RSM / Budget 深层缺陷 | P1 | `src/platform/execution/runtime-state-machine.ts` | SideEffectRecord/BudgetReservation applyStatus 不增 version——CAS 永久失效 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 1904 | Round 33 — Dispatcher/RSM/Budget · Scale-Ecosystem/Ops · UI Build/Perf/SW · Security Holistic / §164 Dispatcher / RSM / Budget 深层缺陷 | P1 | `src/platform/execution/dispatcher/dispatch-reconciliation.ts` | ticket 失效和替换在分离事务——并发产生重复 ticket | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 1905 | Round 33 — Dispatcher/RSM/Budget · Scale-Ecosystem/Ops · UI Build/Perf/SW · Security Holistic / §164 Dispatcher / RSM / Budget 深层缺陷 | P2 | `src/platform/execution/dispatcher/execution-dispatch-service.ts` | activeLeaseCount 用 Math.max 永不减少 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 1906 | Round 33 — Dispatcher/RSM/Budget · Scale-Ecosystem/Ops · UI Build/Perf/SW · Security Holistic / §164 Dispatcher / RSM / Budget 深层缺陷 | P2 | `src/platform/execution/dispatcher/execution-dispatch-service.ts` | preemption 用 worker 级 timestamp 非 execution 级 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 1907 | Round 33 — Dispatcher/RSM/Budget · Scale-Ecosystem/Ops · UI Build/Perf/SW · Security Holistic / §164 Dispatcher / RSM / Budget 深层缺陷 | P2 | `src/platform/execution/dispatcher/execution-dispatch-service.ts` | spawnDepth 无上限限制 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 1908 | Round 33 — Dispatcher/RSM/Budget · Scale-Ecosystem/Ops · UI Build/Perf/SW · Security Holistic / §164 Dispatcher / RSM / Budget 深层缺陷 | P2 | `src/platform/execution/dispatcher/execution-dispatch-service.ts` | preemption 仅 urgent 优先级触发,忽略 high/critical | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 1909 | Round 33 — Dispatcher/RSM/Budget · Scale-Ecosystem/Ops · UI Build/Perf/SW · Security Holistic / §164 Dispatcher / RSM / Budget 深层缺陷 | P2 | `src/platform/execution/runtime-state-machine.ts` | applyStatus 返回 void 非 Result 类型——调用方无法区分成功/失败 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 1910 | Round 33 — Dispatcher/RSM/Budget · Scale-Ecosystem/Ops · UI Build/Perf/SW · Security Holistic / §164 Dispatcher / RSM / Budget 深层缺陷 | P2 | `src/platform/execution/dispatcher/dispatch-reconciliation.ts` | reconcile 扫描全量 tickets 无分页——大规模下 OOM | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 1911 | Round 33 — Dispatcher/RSM/Budget · Scale-Ecosystem/Ops · UI Build/Perf/SW · Security Holistic / §164 Dispatcher / RSM / Budget 深层缺陷 | P2 | `src/platform/execution/budget-allocator.ts` | reserve() 无原子预留——并发 reserve 可超额 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 1912 | Round 33 — Dispatcher/RSM/Budget · Scale-Ecosystem/Ops · UI Build/Perf/SW · Security Holistic / §164 Dispatcher / RSM / Budget 深层缺陷 | P2 | `src/platform/execution/budget-allocator.ts` | 无水位告警/层级预算继承 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 1913 | Round 33 — Dispatcher/RSM/Budget · Scale-Ecosystem/Ops · UI Build/Perf/SW · Security Holistic / §165 Scale-Ecosystem / Ops-Maturity 深层缺陷 | P0 | `src/scale-ecosystem/multi-region/ha-program-service.ts` | `overallStatus === "coordinator" 或 "postgres"` 分支条件恒真——永不返回 `warning` | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 1914 | Round 33 — Dispatcher/RSM/Budget · Scale-Ecosystem/Ops · UI Build/Perf/SW · Security Holistic / §165 Scale-Ecosystem / Ops-Maturity 深层缺陷 | P1 | `src/ops-maturity/workflow-debugger/execution-tracer.ts` | stopTrace/abortTrace 不从 activeTraces 移除——无界内存泄漏 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 1915 | Round 33 — Dispatcher/RSM/Budget · Scale-Ecosystem/Ops · UI Build/Perf/SW · Security Holistic / §165 Scale-Ecosystem / Ops-Maturity 深层缺陷 | P1 | `src/ops-maturity/workflow-debugger/execution-tracer.ts` | getTrace 第二分支 trace! 保证 undefined——潜在 null 解引用 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 1916 | Round 33 — Dispatcher/RSM/Budget · Scale-Ecosystem/Ops · UI Build/Perf/SW · Security Holistic / §165 Scale-Ecosystem / Ops-Maturity 深层缺陷 | P1 | `src/ops-maturity/explainability/explanation-renderer.ts` | calculateDepth 不递归——maxDepth 恒为 0 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 1917 | Round 33 — Dispatcher/RSM/Budget · Scale-Ecosystem/Ops · UI Build/Perf/SW · Security Holistic / §165 Scale-Ecosystem / Ops-Maturity 深层缺陷 | P1 | `src/ops-maturity/workflow-debugger/health-monitor.ts` | 扫描全部历史——一次旧 "failed" 永久毒化状态 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 1918 | Round 33 — Dispatcher/RSM/Budget · Scale-Ecosystem/Ops · UI Build/Perf/SW · Security Holistic / §165 Scale-Ecosystem / Ops-Maturity 深层缺陷 | P1 | `src/ops-maturity/workflow-debugger/time-travel-debug-service.ts` | buildReplayState cursor fromEventIndex 总等于 toEventIndex(赋值后读取) | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 1919 | Round 33 — Dispatcher/RSM/Budget · Scale-Ecosystem/Ops · UI Build/Perf/SW · Security Holistic / §165 Scale-Ecosystem / Ops-Maturity 深层缺陷 | P1 | `src/ops-maturity/workflow-debugger/time-travel-debug-service.ts` | getVariableState 不去重——同名变量无界重复 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 1920 | Round 33 — Dispatcher/RSM/Budget · Scale-Ecosystem/Ops · UI Build/Perf/SW · Security Holistic / §165 Scale-Ecosystem / Ops-Maturity 深层缺陷 | P1 | `src/scale-ecosystem/integration/connector-framework.ts` | bindings/health map 无界增长(无 eviction) | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 1921 | Round 33 — Dispatcher/RSM/Budget · Scale-Ecosystem/Ops · UI Build/Perf/SW · Security Holistic / §165 Scale-Ecosystem / Ops-Maturity 深层缺陷 | P1 | `src/scale-ecosystem/tenant-platform/tenant-platform-service.ts` | createOrganization 默认租户检查 no-op | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 1922 | Round 33 — Dispatcher/RSM/Budget · Scale-Ecosystem/Ops · UI Build/Perf/SW · Security Holistic / §165 Scale-Ecosystem / Ops-Maturity 深层缺陷 | P2 | `src/ops-maturity/workflow-debugger/run-comparator.ts` | 仅检测左→右差异,忽略右→左 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 1923 | Round 33 — Dispatcher/RSM/Budget · Scale-Ecosystem/Ops · UI Build/Perf/SW · Security Holistic / §165 Scale-Ecosystem / Ops-Maturity 深层缺陷 | P2 | `src/ops-maturity/workflow-debugger/time-travel-debug-service.ts` | evictOldestSession 泄漏 eventStore 引用 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 1924 | Round 33 — Dispatcher/RSM/Budget · Scale-Ecosystem/Ops · UI Build/Perf/SW · Security Holistic / §165 Scale-Ecosystem / Ops-Maturity 深层缺陷 | P2 | `src/ops-maturity/workflow-debugger/execution-tracer.ts` | durationMs 在 trace 未结束时返回 NaN | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 1925 | Round 33 — Dispatcher/RSM/Budget · Scale-Ecosystem/Ops · UI Build/Perf/SW · Security Holistic / §165 Scale-Ecosystem / Ops-Maturity 深层缺陷 | P2 | `src/scale-ecosystem/multi-region/data-plane-flow.ts` | async wrapper 缺 syncState/resolveConflict/getReplicationLag 方法 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 1926 | Round 33 — Dispatcher/RSM/Budget · Scale-Ecosystem/Ops · UI Build/Perf/SW · Security Holistic / §165 Scale-Ecosystem / Ops-Maturity 深层缺陷 | P2 | `src/ops-maturity/explainability/explanation-renderer.ts` | simplifiedExplainer 每次 new RegExp 无缓存 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 1927 | Round 33 — Dispatcher/RSM/Budget · Scale-Ecosystem/Ops · UI Build/Perf/SW · Security Holistic / §165 Scale-Ecosystem / Ops-Maturity 深层缺陷 | P2 | `src/ops-maturity/workflow-debugger/health-monitor.ts` | check interval 硬编码无配置 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 1928 | Round 33 — Dispatcher/RSM/Budget · Scale-Ecosystem/Ops · UI Build/Perf/SW · Security Holistic / §166 UI Build / Performance / Service Worker 缺陷 | P0 | `ui/apps/web/public/aa-sw.js` | sync handler 是 no-op(Promise.resolve())——background sync 完全未实现 | 已解决（源码/测试） | 真实根因是 service worker 过去只声明了 `sync` 监听器，但处理函数直接 no-op，离线队列从未被真正重放。 | 当前 `ui/apps/web/public/aa-sw.js` 已在 `sync` 事件里按 `aa-sync-offline` tag 调用 `replayOfflineQueue()`，从 IndexedDB 读取 `offline-queue` 后逐条重放成功请求并删除成功记录；`ui/tests/unit/ui/apps/web/aa-sw.test.ts` 已新增并通过“sync replays offline queue and deletes successfully synced mutations”回归。 | 关闭的是 background sync no-op 缺口，不外推为整套离线同步产品能力已完成。 |
| 1929 | Round 33 — Dispatcher/RSM/Budget · Scale-Ecosystem/Ops · UI Build/Perf/SW · Security Holistic / §166 UI Build / Performance / Service Worker 缺陷 | P0 | `ui/apps/web/public/aa-sw.js` | 所有 GET(含 /api/\* JSON)缓存在同一 bucket 无 TTL——永远返回 stale API data | 已解决（源码/测试） | 真实根因是旧 fetch handler 会把所有 GET 请求都送入同一 runtime cache，API JSON 也会被静态资源策略误缓存。 | 当前 `ui/apps/web/public/aa-sw.js` 已在 `fetch` 事件中对 `url.pathname.startsWith("/api/")` 直接 return，不再缓存 API 请求；`ui/tests/unit/ui/apps/web/aa-sw.test.ts` 已新增并通过“bypasses caching for API GET requests”回归。 | 关闭的是“API 请求被静态缓存”的旧缺口，不外推为已建立完整 TTL/再验证体系。 |
| 1930 | Round 33 — Dispatcher/RSM/Budget · Scale-Ecosystem/Ops · UI Build/Perf/SW · Security Holistic / §166 UI Build / Performance / Service Worker 缺陷 | P0 | `ui/scripts/perf-budget.mjs` | 仅检查 bundle 字节——无 FCP/TTI 时间强制(spec 要求 FCP<1.5s, TTI<3.5s) | 已解决（源码复核） | 真实根因是早期 perf budget 脚本只有 bundle 体积预算，没有把 Lighthouse 时间指标纳入 CI 门禁。 | 当前 `ui/scripts/perf-budget.mjs` 已把 `FCP<1.5s`、`TTI<3.5s` 纳入检查；`.github/workflows/ui-quality.yml` 也已在 UI 质量流水线中执行 `npm run perf:budget`。 | 作为 `R7-3/R24-54` 的重复挂账一并关闭，不外推为所有构建产物都已达标。 |
| 1931 | Round 33 — Dispatcher/RSM/Budget · Scale-Ecosystem/Ops · UI Build/Perf/SW · Security Holistic / §166 UI Build / Performance / Service Worker 缺陷 | P1 | `ui/apps/web/public/aa-sw.js` | activate 不清理旧缓存版本 | 已解决（源码修复并补定向测试） | 真实根因是 service worker 的 `activate` 阶段过去没有清理旧版 `aa-ui-*` cache，部署多版本后会持续积累陈旧缓存。 | 当前 `ui/apps/web/public/aa-sw.js` 已在 `activate` 中遍历 `caches.keys()`，删除除当前 `aa-ui-runtime-v1` 之外的旧 `aa-ui-*` 缓存，并在 `ui/tests/unit/ui/apps/web/aa-sw.test.ts` 验证只会清理旧版本、不误删当前缓存。 | 关闭的是旧缓存清理缺口，不外推为完整离线缓存策略已全部产品化。 |
| 1932 | Round 33 — Dispatcher/RSM/Budget · Scale-Ecosystem/Ops · UI Build/Perf/SW · Security Holistic / §166 UI Build / Performance / Service Worker 缺陷 | P1 | `ui/apps/web/public/aa-sw.js` | install 不预缓存 app shell/offline fallback | 已解决（源码修复并补定向测试） | 真实根因是 service worker 安装期没有预缓存应用壳层和 offline fallback，离线首开无法直接拿到基础页面。 | 当前 `ui/apps/web/public/aa-sw.js` 已在 `install` 中预缓存 `"/"` 与 `"/offline"`，`ui/tests/unit/ui/apps/web/aa-sw.test.ts` 也已验证 install 会向 cache 添加这两个入口。 | 关闭的是 install 预缓存缺口，不外推为所有离线 UX 都已完成。 |
| 1933 | Round 33 — Dispatcher/RSM/Budget · Scale-Ecosystem/Ops · UI Build/Perf/SW · Security Holistic / §166 UI Build / Performance / Service Worker 缺陷 | P1 | `ui/apps/web/vite.config.ts` | 无 build.target 设置;manualChunks unmatched 归入单块 | 已解决（源码修复并补定向测试） | 真实根因是 Vite 构建配置过去依赖默认 `target`，同时 `manualChunks` 未把通用 vendor 和 feature chunk 规则抽成稳定策略，导致浏览器兼容目标不显式、未命中的 `node_modules` 也容易继续聚成不可控的大块。 | 当前 `ui/apps/web/build-config.ts` 已显式固定 `WEB_BUILD_TARGET = "es2022"`，并通过 `selectManualChunk()` 把 React、React Query、React Flow、feature 包和其余 `node_modules` 分流到稳定 chunk；`ui/apps/web/vite.config.ts` 也已直接消费这组配置。`ui/tests/unit/ui/apps/web/build-config.test.ts` 与 `ui/tests/tools/tooling.test.ts` 已验证 target 固定、feature chunk 生效，且 scoped package 会生成稳定的 `vendor-scope-pkg` 名称而不是回落到单一 unmatched chunk。 | 关闭的是“构建 target 缺省且 unmatched vendor 无稳定分块策略”的真实缺口，不外推为 UI 整体性能已经达标。 |
| 1934 | Round 33 — Dispatcher/RSM/Budget · Scale-Ecosystem/Ops · UI Build/Perf/SW · Security Holistic / §166 UI Build / Performance / Service Worker 缺陷 | P1 | `ui/apps/web/vite.config.ts` | maxJsChunkBytes 550KB 是 spec 200KB 的 2.75x | 已解决（门限与预算守卫已收敛到 spec） | 真实根因是 Web 构建侧过去把 chunk 预警阈值放宽到 550KB，且没有把 200KB 预算显式表达成构建配置与预算脚本的统一守卫，导致超标只能靠人工观察。 | 当前 `ui/apps/web/build-config.ts` 已把 `WEB_CHUNK_WARNING_LIMIT_KB` 固定为 `200` 并接入 `ui/apps/web/vite.config.ts`；`ui/scripts/perf-budget.mjs` 也会对 JS/CSS gzip chunk 和总量执行预算校验，避免继续以 550KB 作为默认容忍线。`ui/tests/unit/ui/apps/web/build-config.test.ts` 与 `ui/tests/tools/tooling.test.ts` 已覆盖 200KB warning 门限与 perf budget 脚本接线。 | 关闭的是“构建门限仍停留在 550KB”的真实缺口；是否通过实际产物预算仍取决于 CI/build 输出，不在本轮全量验证范围内。 |
| 1935 | Round 33 — Dispatcher/RSM/Budget · Scale-Ecosystem/Ops · UI Build/Perf/SW · Security Holistic / §166 UI Build / Performance / Service Worker 缺陷 | P1 | `ui/vitest.config.ts` | branches 覆盖率阈值 20% 低于其他维度 30%(spec 要求 80%) | 已解决（源码复核） | 真实根因是 UI monorepo 早期把 coverage thresholds 设成过低占位值，后续门限已经按分层要求收敛，但重复挂账未同步。 | 当前 `ui/vitest.config.ts` 已按 `shared≥90`、`ui-core≥80`、`features≥70`、`apps≥50` 分层声明 coverage thresholds，并移除了绕过强制门限的旧配置注释。 | 作为 `R7-2` 的重复挂账一并关闭，不外推为全量 UI 覆盖率已在 CI 重新验证完毕。 |
| 1936 | Round 33 — Dispatcher/RSM/Budget · Scale-Ecosystem/Ops · UI Build/Perf/SW · Security Holistic / §166 UI Build / Performance / Service Worker 缺陷 | P2 | `ui/apps/web/public/aa-sw.js` | 缓存 key 含 query string——同资源重复缓存 | 已解决（源码修复并补定向测试） | 真实根因是 fetch handler 直接用原始 request 作为 cache key，带 query string 的同一静态资源会被重复缓存。 | 当前 `ui/apps/web/public/aa-sw.js` 已把 cache key 归一化为 `origin + pathname`，忽略 query string；`ui/tests/unit/ui/apps/web/aa-sw.test.ts` 已验证 `https://example.com/assets/app.js?v=123` 会按 `https://example.com/assets/app.js` 读写缓存。 | 关闭的是 query-string 缓存键膨胀问题。 |
| 1937 | Round 33 — Dispatcher/RSM/Budget · Scale-Ecosystem/Ops · UI Build/Perf/SW · Security Holistic / §166 UI Build / Performance / Service Worker 缺陷 | P2 | `ui/scripts/perf-budget.mjs` | 硬编码路径无存在检查——文件缺失时静默通过 | 已解决（源码修复并补定向测试） | 真实根因是 perf budget 脚本以前直接假定 `apps/web/dist/assets` 存在，构建产物缺失时会绕过预算校验，形成假阳性通过。 | 当前 `ui/scripts/perf-budget.mjs` 已在读取目录前执行 `existsSync(distRoot)` 检查，不存在时直接 fail-close 抛出 `perf_budget.dist_not_found:*`；`ui/tests/unit/ui/apps/web/build-config.test.ts` 与 `ui/tests/tools/tooling.test.ts` 都已钉住该存在性检查。 | 关闭的是“dist 缺失时静默通过”的真实缺口，不外推为所有 perf budget 逻辑都已完整覆盖。 |
| 1938 | Round 33 — Dispatcher/RSM/Budget · Scale-Ecosystem/Ops · UI Build/Perf/SW · Security Holistic / §166 UI Build / Performance / Service Worker 缺陷 | P2 | `ui/tools/mock-server/index.ts` | path.includes 子串匹配——/api/v1/tasks 匹配 /api/v1/tasks-archive | 已解决（源码修复并补定向测试） | 真实根因是 mock server 过去用 `path.includes(...)` 做子串匹配，导致 canonical 路由和相似前缀路径没有边界隔离，`/api/v1/tasks-archive` 会错误命中 `/tasks` mock。 | 当前 `ui/tools/mock-server/src/index.ts` 已改成去 query、归一化 pathname 后按路由边界匹配 `route === path || path.startsWith(route + "/")`，不再靠子串命中；新增 `ui/tests/unit/ui/tools/mock-server-routing.test.ts` 已验证 `/api/v1/tasks` 不会误匹配 `/api/v1/tasks-archive`，且 `contract-version` 路由可忽略 query string 正常解析。 | 关闭的是 mock-server 路由匹配 bug，不外推为整组 UI 产品化问题都已完成。 |
| 1939 | Round 33 — Dispatcher/RSM/Budget · Scale-Ecosystem/Ops · UI Build/Perf/SW · Security Holistic / §166 UI Build / Performance / Service Worker 缺陷 | P2 | `ui/apps/web/vite.config.ts` | 无 terser/esbuild minify 配置——依赖默认行为 | 已解决（源码修复并补定向测试） | 真实根因是 Vite 构建过去没有显式声明压缩后端，行为依赖工具默认值，升级时存在漂移风险。 | 当前 `ui/apps/web/build-config.ts` 已固定 `WEB_MINIFY_MODE = "esbuild"`，`ui/apps/web/vite.config.ts` 用该常量显式配置 `build.minify`；`ui/tests/unit/ui/apps/web/build-config.test.ts` 与 `ui/tests/tools/tooling.test.ts` 已覆盖该配置。 | 关闭的是“minify 后端未显式钉死”的真实缺口，不外推为所有前端构建参数都已审计完成。 |
| 1940 | Round 33 — Dispatcher/RSM/Budget · Scale-Ecosystem/Ops · UI Build/Perf/SW · Security Holistic / §166 UI Build / Performance / Service Worker 缺陷 | P2 | `ui/scripts/perf-budget.mjs` | 无 CI 集成钩子——仅手动运行 | 已解决（CI 质量门禁已接入） | 真实根因是 perf budget 过去只作为脚本存在，没有被 UI CI 命令链调用，超预算不会自动阻断流水线。 | 当前 `ui/package.json` 已将 `perf:budget` 串入 `scripts.ci`，形成 `lint -> typecheck -> audit -> test:coverage -> build -> bundle:analyze -> perf:budget` 的质量门禁；`ui/tests/unit/ui/apps/web/build-config.test.ts` 与 `ui/tests/tools/tooling.test.ts` 已验证 `ci` 脚本包含 `perf:budget`。 | 关闭的是“perf budget 无 CI 挂钩”的真实缺口，不外推为 Lighthouse/时序预算在所有环境都已稳定可执行。 |
| 1941 | Round 33 — Dispatcher/RSM/Budget · Scale-Ecosystem/Ops · UI Build/Perf/SW · Security Holistic / §167 Security Holistic 深层缺陷 | P0 | `src/platform/control-plane/iam/access-model.ts` | evaluateAuthorizationContext 从不检查 role→capability 映射——viewer 可授权 exec_command | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 1942 | Round 33 — Dispatcher/RSM/Budget · Scale-Ecosystem/Ops · UI Build/Perf/SW · Security Holistic / §167 Security Holistic 深层缺陷 | P0 | `src/platform/control-plane/iam/policy-engine.ts` | evaluate() 不验证 subject role/capability——任何 subjectType 可通过任何 action | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 1943 | Round 33 — Dispatcher/RSM/Budget · Scale-Ecosystem/Ops · UI Build/Perf/SW · Security Holistic / §167 Security Holistic 深层缺陷 | P1 | `src/platform/control-plane/iam/secret-management-service.ts` | resolveSecret 零授权——任何调用者获取任何 secret | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 1944 | Round 33 — Dispatcher/RSM/Budget · Scale-Ecosystem/Ops · UI Build/Perf/SW · Security Holistic / §167 Security Holistic 深层缺陷 | P1 | `src/platform/compliance/encryption/field-encryption.ts` | normalizeKey 接受 1 字符 key(~7 bit entropy)无最低强度检查 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 1945 | Round 33 — Dispatcher/RSM/Budget · Scale-Ecosystem/Ops · UI Build/Perf/SW · Security Holistic / §167 Security Holistic 深层缺陷 | P1 | `src/plugins/plugin-runtime-host.ts` | renderContainerizedToken 用未 sanitize 的 pluginId——可操纵容器挂载/镜像 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 1946 | Round 33 — Dispatcher/RSM/Budget · Scale-Ecosystem/Ops · UI Build/Perf/SW · Security Holistic / §167 Security Holistic 深层缺陷 | P1 | `src/platform/control-plane/iam/access-model.ts` | roleGrantsCapabilities/inferCapabilitiesForAction 定义但从未调用——RBAC 能力强制是死代码 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 1947 | Round 33 — Dispatcher/RSM/Budget · Scale-Ecosystem/Ops · UI Build/Perf/SW · Security Holistic / §167 Security Holistic 深层缺陷 | P1 | `src/platform/control-plane/iam/secret-management-service.ts` | secret rotation 无版本追踪——旧版本立即不可达 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 1948 | Round 33 — Dispatcher/RSM/Budget · Scale-Ecosystem/Ops · UI Build/Perf/SW · Security Holistic / §167 Security Holistic 深层缺陷 | P1 | `src/platform/control-plane/iam/policy-engine.ts` | policy 缓存无失效机制——策略变更不生效 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 1949 | Round 33 — Dispatcher/RSM/Budget · Scale-Ecosystem/Ops · UI Build/Perf/SW · Security Holistic / §167 Security Holistic 深层缺陷 | P2 | `src/sdk/cli/secret-commands.ts` | secret 输出到 stdout 无 auth gate | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 1950 | Round 33 — Dispatcher/RSM/Budget · Scale-Ecosystem/Ops · UI Build/Perf/SW · Security Holistic / §167 Security Holistic 深层缺陷 | P2 | `src/platform/control-plane/iam/external-secret-provider.ts` | 从环境变量路径读文件无 sandbox 验证 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 1951 | Round 33 — Dispatcher/RSM/Budget · Scale-Ecosystem/Ops · UI Build/Perf/SW · Security Holistic / §167 Security Holistic 深层缺陷 | P2 | `src/platform/control-plane/iam/external-secret-provider.ts` | vault path 无 `..` 路径遍历拒绝 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 1952 | Round 33 — Dispatcher/RSM/Budget · Scale-Ecosystem/Ops · UI Build/Perf/SW · Security Holistic / §167 Security Holistic 深层缺陷 | P2 | `src/platform/shared/observability/redis-lock.ts` | JSON.parse 无 schema 验证——恶意 payload 可注入 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 1953 | Round 34 — Prompt-Engine/Stability · Domains/Org-Governance · Config/Bootstrap · SDK/Plugins / §169 Prompt-Engine / Stability 缺陷 | P0 | `src/platform/stability/stable-evidence-bundle.ts` | 无密码学签名——spec 要求 tamper-evident,实际写纯 JSON 无 HMAC/签名/哈希链 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 1954 | Round 34 — Prompt-Engine/Stability · Domains/Org-Governance · Config/Bootstrap · SDK/Plugins / §169 Prompt-Engine / Stability 缺陷 | P0 | `src/platform/prompt-engine/eval/quality-config-loader.ts:70` | 裸 `catch {}` 吞掉所有错误静默返回宽松默认值——攻击者修改配置无告警 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 1955 | Round 34 — Prompt-Engine/Stability · Domains/Org-Governance · Config/Bootstrap · SDK/Plugins / §169 Prompt-Engine / Stability 缺陷 | P0 | `src/platform/prompt-engine/registry/hierarchical-registry-service.ts:210` | 直接 mutate "不可变"快照对象(deprecated/updatedAt)——违反 spec 不可变快照要求 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 1956 | Round 34 — Prompt-Engine/Stability · Domains/Org-Governance · Config/Bootstrap · SDK/Plugins / §169 Prompt-Engine / Stability 缺陷 | P1 | `src/platform/prompt-engine/rollout/prompt-rollout-stage.ts` | 阶段顺序 shadow 在 canary 之前——spec 要求 canary→shadow→staged→full | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 1957 | Round 34 — Prompt-Engine/Stability · Domains/Org-Governance · Config/Bootstrap · SDK/Plugins / §169 Prompt-Engine / Stability 缺陷 | P1 | `src/platform/prompt-engine/rollout/index.ts:6` | PromptRolloutMode 仅 off/suggest/shadow——无 canary/staged/full 模式 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 1958 | Round 34 — Prompt-Engine/Stability · Domains/Org-Governance · Config/Bootstrap · SDK/Plugins / §169 Prompt-Engine / Stability 缺陷 | P1 | `src/platform/prompt-engine/rollout/index.ts:30-119` | 无自动回滚机制——spec 要求 metric regression 自动回滚 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 1959 | Round 34 — Prompt-Engine/Stability · Domains/Org-Governance · Config/Bootstrap · SDK/Plugins / §169 Prompt-Engine / Stability 缺陷 | P1 | `src/platform/prompt-engine/eval/llm-eval-service.ts:348-378` | A/B test 硬编码 0.85/0.90 分数——统计显著性测试无意义 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 1960 | Round 34 — Prompt-Engine/Stability · Domains/Org-Governance · Config/Bootstrap · SDK/Plugins / §169 Prompt-Engine / Stability 缺陷 | P1 | `src/platform/prompt-engine/eval/llm-eval-service.ts:367` | 显著性检验仅为阈值比较——非真正统计检验(t-test/bootstrap) | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 1961 | Round 34 — Prompt-Engine/Stability · Domains/Org-Governance · Config/Bootstrap · SDK/Plugins / §169 Prompt-Engine / Stability 缺陷 | P1 | `src/platform/prompt-engine/eval/execution-outcome-evaluator.ts:46-49` | 质量分权重和 1.2>1.0——clamp 丢失分辨率 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 1962 | Round 34 — Prompt-Engine/Stability · Domains/Org-Governance · Config/Bootstrap · SDK/Plugins / §169 Prompt-Engine / Stability 缺陷 | P1 | `src/platform/prompt-engine/registry/hierarchical-registry-service.ts:388-409` | findBundle 忽略 version 参数——永远选默认 bundle | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 1963 | Round 34 — Prompt-Engine/Stability · Domains/Org-Governance · Config/Bootstrap · SDK/Plugins / §169 Prompt-Engine / Stability 缺陷 | P1 | `src/platform/prompt-engine/rollout/prompt-rollout-stage.ts:24-29` | stable→rolled_back 自动前进——语义错误 | 已解决（源码复核并补定向测试） | 根因是旧推进逻辑把 `stable` 误当作普通中间态，允许继续顺推到 `rolled_back`；后续实现已把 `stable` 与 `rolled_back` 都收敛为终态，但这条旧编号一直没有同步回写。 | 当前 `src/platform/prompt-engine/rollout/prompt-rollout-stage.ts` 的 `nextPromptRolloutStage()` 对 `stage === "stable" || stage === "rolled_back"` 直接返回 `null`，不再允许 `stable→rolled_back` 的伪推进。 | `tests/unit/platform/prompt-engine/rollout/prompt-rollout-stage-audit-regressions.test.ts` 的 `nextPromptRolloutStage treats stable as a terminal state` 已在本轮定向复跑通过。 |
| 1964 | Round 34 — Prompt-Engine/Stability · Domains/Org-Governance · Config/Bootstrap · SDK/Plugins / §169 Prompt-Engine / Stability 缺陷 | P2 | `src/platform/prompt-engine/registry/prompt-version-manager.ts:234-238` | VersionLineage 接口重复声明(死代码) | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 1965 | Round 34 — Prompt-Engine/Stability · Domains/Org-Governance · Config/Bootstrap · SDK/Plugins / §169 Prompt-Engine / Stability 缺陷 | P2 | `src/platform/prompt-engine/eval/cross-provider-judge-service.ts:200` | agreementScore 仅测 promote 比率——全票 rollback 得 0.0 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 1966 | Round 34 — Prompt-Engine/Stability · Domains/Org-Governance · Config/Bootstrap · SDK/Plugins / §169 Prompt-Engine / Stability 缺陷 | P2 | `src/platform/prompt-engine/registry/hierarchical-registry-service.ts:260-268` | traffic slot 未归一化权重——分配不公平 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 1967 | Round 34 — Prompt-Engine/Stability · Domains/Org-Governance · Config/Bootstrap · SDK/Plugins / §169 Prompt-Engine / Stability 缺陷 | P2 | `src/platform/prompt-engine/eval/llm-eval-service.ts:566-568` | JSON.parse(suite.cases) 无 try/catch | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 1968 | Round 34 — Prompt-Engine/Stability · Domains/Org-Governance · Config/Bootstrap · SDK/Plugins / §169 Prompt-Engine / Stability 缺陷 | P2 | `src/platform/stability/stable-evidence-bundle-support.ts:441-450` | overrides 可运行时替换 name 字段(TS Omit 不阻止 JS) | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 1969 | Round 34 — Prompt-Engine/Stability · Domains/Org-Governance · Config/Bootstrap · SDK/Plugins / §170 Domains / Org-Governance 缺陷 | P0 | `src/org-governance/sso-scim/oidc/oidc-service.ts:237` | 无 PKCE 支持——auth code 流易被拦截 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 1970 | Round 34 — Prompt-Engine/Stability · Domains/Org-Governance · Config/Bootstrap · SDK/Plugins / §170 Domains / Org-Governance 缺陷 | P0 | `src/org-governance/sso-scim/oidc/oidc-service.ts:237,519` | userinfo 失败 fallback 到 mock admin 用户——攻击者故障注入提权 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 1971 | Round 34 — Prompt-Engine/Stability · Domains/Org-Governance · Config/Bootstrap · SDK/Plugins / §170 Domains / Org-Governance 缺陷 | P0 | `src/org-governance/sso-scim/oidc/oidc-service.ts:329-335` | refresh token 不轮换——spec 要求 token rotation | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 1972 | Round 34 — Prompt-Engine/Stability · Domains/Org-Governance · Config/Bootstrap · SDK/Plugins / §170 Domains / Org-Governance 缺陷 | P0 | `src/org-governance/sso-scim/scim-sync/scim-service.ts:135` | 无租户隔离——全局 Map 返回任何租户数据 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 1973 | Round 34 — Prompt-Engine/Stability · Domains/Org-Governance · Config/Bootstrap · SDK/Plugins / §170 Domains / Org-Governance 缺陷 | P0 | `src/org-governance/knowledge-boundary/sharing-gate/index.ts:28` | grant expiry 检查逻辑错误+字符串比较非日期比较 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 1974 | Round 34 — Prompt-Engine/Stability · Domains/Org-Governance · Config/Bootstrap · SDK/Plugins / §170 Domains / Org-Governance 缺陷 | P1 | `src/domains/registry/domain-registry-service.ts:106-111` | 缺 canary 状态——直接 registered→active 跳过 canary | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 1975 | Round 34 — Prompt-Engine/Stability · Domains/Org-Governance · Config/Bootstrap · SDK/Plugins / §170 Domains / Org-Governance 缺陷 | P1 | `src/domains/registry/domain-registry-service.ts` | 无 archived 状态转换方法 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 1976 | Round 34 — Prompt-Engine/Stability · Domains/Org-Governance · Config/Bootstrap · SDK/Plugins / §170 Domains / Org-Governance 缺陷 | P1 | `src/domains/registry/domain-model.ts:66-76` | DomainManifest 缺 resource quotas(CPU/内存/并发) | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 1977 | Round 34 — Prompt-Engine/Stability · Domains/Org-Governance · Config/Bootstrap · SDK/Plugins / §170 Domains / Org-Governance 缺陷 | P1 | `src/org-governance/delegated-governance/delegated-governance-service.ts:46-53` | delegation 不做权限交集——grantee 可超越 grantor 权限 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 1978 | Round 34 — Prompt-Engine/Stability · Domains/Org-Governance · Config/Bootstrap · SDK/Plugins / §170 Domains / Org-Governance 缺陷 | P1 | `src/org-governance/approval-routing/route-engine/index.ts:118` | 金额阈值用 < 非 <=——恰等金额 fall through | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 1979 | Round 34 — Prompt-Engine/Stability · Domains/Org-Governance · Config/Bootstrap · SDK/Plugins / §170 Domains / Org-Governance 缺陷 | P1 | `src/org-governance/approval-routing/approval-routing-service.ts:78-91` | audit recordId 无时间戳/随机——同 requester+node 碰撞 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 1980 | Round 34 — Prompt-Engine/Stability · Domains/Org-Governance · Config/Bootstrap · SDK/Plugins / §170 Domains / Org-Governance 缺陷 | P1 | `src/org-governance/compliance-engine/policy-resolver/index.ts:4-18` | 无 deny-by-default——无策略时返回空对象=允许 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 1981 | Round 34 — Prompt-Engine/Stability · Domains/Org-Governance · Config/Bootstrap · SDK/Plugins / §170 Domains / Org-Governance 缺陷 | P1 | `src/org-governance/delegated-governance/governance-delegation-revocation-saga.ts:28` | cascadeWithinSlo 恒 true(length>=0 恒真) | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 1982 | Round 34 — Prompt-Engine/Stability · Domains/Org-Governance · Config/Bootstrap · SDK/Plugins / §170 Domains / Org-Governance 缺陷 | P2 | `src/org-governance/sso-scim/oidc/oidc-service.ts:426-433` | 过期 session 清理多等 24h | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 1983 | Round 34 — Prompt-Engine/Stability · Domains/Org-Governance · Config/Bootstrap · SDK/Plugins / §170 Domains / Org-Governance 缺陷 | P2 | `src/domains/recipes/recipe-executor.ts:34` | workflow 存在检查是正则 stub 非真实查询 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 1984 | Round 34 — Prompt-Engine/Stability · Domains/Org-Governance · Config/Bootstrap · SDK/Plugins / §170 Domains / Org-Governance 缺陷 | P2 | `src/org-governance/sso-scim/scim-sync/scim-service.ts:533-535` | SCIM patch remove members 清空全部而非目标 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 1985 | Round 34 — Prompt-Engine/Stability · Domains/Org-Governance · Config/Bootstrap · SDK/Plugins / §170 Domains / Org-Governance 缺陷 | P2 | `src/org-governance/knowledge-boundary/knowledge-boundary-service.ts:163` | requiredGrantBoundaryIds 检查逻辑反——恒失败 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 1986 | Round 34 — Prompt-Engine/Stability · Domains/Org-Governance · Config/Bootstrap · SDK/Plugins / §170 Domains / Org-Governance 缺陷 | P2 | `src/org-governance/sso-scim/scim-sync/scim-service.ts:791-818` | SCIM filter 忽略属性名——错误匹配 userName | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 1987 | Round 34 — Prompt-Engine/Stability · Domains/Org-Governance · Config/Bootstrap · SDK/Plugins / §171 Config / Bootstrap / Divisions 缺陷 | P0 | `config/risk/default.json` | 仍为 6 因子风险模型——spec 要求 8 因子 | 需继续整改（测试/配置差距） | 测试、配置或 bootstrap 缺少审计要求的覆盖与约束，本轮未补齐。 | 已按源审计条目补录编号；本轮未新增覆盖缺口对应的测试、配置或 bootstrap 修复证据。 | 补录到 reaudit，待专项整改。 |
| 1988 | Round 34 — Prompt-Engine/Stability · Domains/Org-Governance · Config/Bootstrap · SDK/Plugins / §171 Config / Bootstrap / Divisions 缺陷 | P0 | `config/security/dev.json` | approvalMode:"auto" 绕过所有审批门 | 需继续整改（测试/配置差距） | 测试、配置或 bootstrap 缺少审计要求的覆盖与约束，本轮未补齐。 | 已按源审计条目补录编号；本轮未新增覆盖缺口对应的测试、配置或 bootstrap 修复证据。 | 补录到 reaudit，待专项整改。 |
| 1989 | Round 34 — Prompt-Engine/Stability · Domains/Org-Governance · Config/Bootstrap · SDK/Plugins / §171 Config / Bootstrap / Divisions 缺陷 | P0 | `config/security/default.json` | sandboxMode:"workspace_write" 过度宽松默认——违反最小权限 | 需继续整改（测试/配置差距） | 测试、配置或 bootstrap 缺少审计要求的覆盖与约束，本轮未补齐。 | 已按源审计条目补录编号；本轮未新增覆盖缺口对应的测试、配置或 bootstrap 修复证据。 | 补录到 reaudit，待专项整改。 |
| 1990 | Round 34 — Prompt-Engine/Stability · Domains/Org-Governance · Config/Bootstrap · SDK/Plugins / §171 Config / Bootstrap / Divisions 缺陷 | P0 | `config/bootstrap/default.json` | 无依赖顺序/健康检查门/层级声明——spec 要求严格依赖序 | 需继续整改（测试/配置差距） | 测试、配置或 bootstrap 缺少审计要求的覆盖与约束，本轮未补齐。 | 已按源审计条目补录编号；本轮未新增覆盖缺口对应的测试、配置或 bootstrap 修复证据。 | 补录到 reaudit，待专项整改。 |
| 1991 | Round 34 — Prompt-Engine/Stability · Domains/Org-Governance · Config/Bootstrap · SDK/Plugins / §171 Config / Bootstrap / Divisions 缺陷 | P1 | `config/bootstrap/default.json` | 无热重载/影响分析/canary 配置 | 需继续整改（测试/配置差距） | 测试、配置或 bootstrap 缺少审计要求的覆盖与约束，本轮未补齐。 | 已按源审计条目补录编号；本轮未新增覆盖缺口对应的测试、配置或 bootstrap 修复证据。 | 补录到 reaudit，待专项整改。 |
| 1992 | Round 34 — Prompt-Engine/Stability · Domains/Org-Governance · Config/Bootstrap · SDK/Plugins / §171 Config / Bootstrap / Divisions 缺陷 | P1 | `src/platform-architecture-bootstrap.ts:150-152` | getPlatformArchitectureServices 无条件重复注册 | 需继续整改（测试/配置差距） | 测试、配置或 bootstrap 缺少审计要求的覆盖与约束，本轮未补齐。 | 已按源审计条目补录编号；本轮未新增覆盖缺口对应的测试、配置或 bootstrap 修复证据。 | 补录到 reaudit，待专项整改。 |
| 1993 | Round 34 — Prompt-Engine/Stability · Domains/Org-Governance · Config/Bootstrap · SDK/Plugins / §171 Config / Bootstrap / Divisions 缺陷 | P1 | `src/platform-architecture-bootstrap.ts:143-146` | register 后立即 get 无就绪门——可获取未初始化数据 | 需继续整改（测试/配置差距） | 测试、配置或 bootstrap 缺少审计要求的覆盖与约束，本轮未补齐。 | 已按源审计条目补录编号；本轮未新增覆盖缺口对应的测试、配置或 bootstrap 修复证据。 | 补录到 reaudit，待专项整改。 |
| 1994 | Round 34 — Prompt-Engine/Stability · Domains/Org-Governance · Config/Bootstrap · SDK/Plugins / §171 Config / Bootstrap / Divisions 缺陷 | P1 | `divisions/\*/division.yaml` | 无 division 声明 resource_boundaries 或 fault_domains | 需继续整改（测试/配置差距） | 测试、配置或 bootstrap 缺少审计要求的覆盖与约束，本轮未补齐。 | 已按源审计条目补录编号；本轮未新增覆盖缺口对应的测试、配置或 bootstrap 修复证据。 | 补录到 reaudit，待专项整改。 |
| 1995 | Round 34 — Prompt-Engine/Stability · Domains/Org-Governance · Config/Bootstrap · SDK/Plugins / §171 Config / Bootstrap / Divisions 缺陷 | P1 | `divisions/devops+operations` | trigger 重叠:deployment/monitoring 无消歧规则 | 需继续整改（测试/配置差距） | 测试、配置或 bootstrap 缺少审计要求的覆盖与约束，本轮未补齐。 | 已按源审计条目补录编号；本轮未新增覆盖缺口对应的测试、配置或 bootstrap 修复证据。 | 补录到 reaudit，待专项整改。 |
| 1996 | Round 34 — Prompt-Engine/Stability · Domains/Org-Governance · Config/Bootstrap · SDK/Plugins / §171 Config / Bootstrap / Divisions 缺陷 | P1 | `divisions/engineering_ops+qa` | trigger 重叠:bug/fix 无消歧规则 | 需继续整改（测试/配置差距） | 测试、配置或 bootstrap 缺少审计要求的覆盖与约束，本轮未补齐。 | 已按源审计条目补录编号；本轮未新增覆盖缺口对应的测试、配置或 bootstrap 修复证据。 | 补录到 reaudit，待专项整改。 |
| 1997 | Round 34 — Prompt-Engine/Stability · Domains/Org-Governance · Config/Bootstrap · SDK/Plugins / §171 Config / Bootstrap / Divisions 缺陷 | P1 | `divisions/general_ops+research` | trigger 重叠:research/analyze/review 靠隐式优先级 | 需继续整改（测试/配置差距） | 测试、配置或 bootstrap 缺少审计要求的覆盖与约束，本轮未补齐。 | 已按源审计条目补录编号；本轮未新增覆盖缺口对应的测试、配置或 bootstrap 修复证据。 | 补录到 reaudit，待专项整改。 |
| 1998 | Round 34 — Prompt-Engine/Stability · Domains/Org-Governance · Config/Bootstrap · SDK/Plugins / §171 Config / Bootstrap / Divisions 缺陷 | P1 | `config/risk/default.json` | medium risk autoExecute:true+requiresApproval:false——违反纵深防御 | 需继续整改（测试/配置差距） | 测试、配置或 bootstrap 缺少审计要求的覆盖与约束，本轮未补齐。 | 已按源审计条目补录编号；本轮未新增覆盖缺口对应的测试、配置或 bootstrap 修复证据。 | 补录到 reaudit，待专项整改。 |
| 1999 | Round 34 — Prompt-Engine/Stability · Domains/Org-Governance · Config/Bootstrap · SDK/Plugins / §171 Config / Bootstrap / Divisions 缺陷 | P2 | `config/security/default.json` | remoteWorkerRegistration.allowedCapabilities 含 "bash"——prod 无覆写 | 需继续整改（测试/配置差距） | 测试、配置或 bootstrap 缺少审计要求的覆盖与约束，本轮未补齐。 | 已按源审计条目补录编号；本轮未新增覆盖缺口对应的测试、配置或 bootstrap 修复证据。 | 补录到 reaudit，待专项整改。 |
| 2000 | Round 34 — Prompt-Engine/Stability · Domains/Org-Governance · Config/Bootstrap · SDK/Plugins / §171 Config / Bootstrap / Divisions 缺陷 | P2 | `config/domains/default.json` | outputContracts schema {"type":"object"} 无属性——严格验证=无验证 | 需继续整改（ADR/contract） | ADR、contract、类型导出与现行实现存在漂移，本轮未完成逐条回写与一致性校验闭环。 | 已按源审计条目补录编号；本轮未新增 contract/ADR 回写或一致性测试证据，因此不能改写为已解决。 | 补录到 reaudit，待专项整改。 |
| 2001 | Round 34 — Prompt-Engine/Stability · Domains/Org-Governance · Config/Bootstrap · SDK/Plugins / §171 Config / Bootstrap / Divisions 缺陷 | P2 | `config/gateways/default.json` | 网关配置无 rate limit/auth/CORS/TLS 设置 | 需继续整改（测试/配置差距） | 测试、配置或 bootstrap 缺少审计要求的覆盖与约束，本轮未补齐。 | 已按源审计条目补录编号；本轮未新增覆盖缺口对应的测试、配置或 bootstrap 修复证据。 | 补录到 reaudit，待专项整改。 |
| 2002 | Round 34 — Prompt-Engine/Stability · Domains/Org-Governance · Config/Bootstrap · SDK/Plugins / §171 Config / Bootstrap / Divisions 缺陷 | P2 | `src/index.ts:41` | PlatformRootEntryMode 与 PlatformStartupTargetKind 重复定义 | 需继续整改（测试/配置差距） | 测试、配置或 bootstrap 缺少审计要求的覆盖与约束，本轮未补齐。 | 已按源审计条目补录编号；本轮未新增覆盖缺口对应的测试、配置或 bootstrap 修复证据。 | 补录到 reaudit，待专项整改。 |
| 2003 | Round 34 — Prompt-Engine/Stability · Domains/Org-Governance · Config/Bootstrap · SDK/Plugins / §171 Config / Bootstrap / Divisions 缺陷 | P2 | `config/runtime/default.json` | maxToolCalls:8/maxAgentRounds:6 过低——复杂流程静默截断 | 需继续整改（测试/配置差距） | 测试、配置或 bootstrap 缺少审计要求的覆盖与约束，本轮未补齐。 | 已按源审计条目补录编号；本轮未新增覆盖缺口对应的测试、配置或 bootstrap 修复证据。 | 补录到 reaudit，待专项整改。 |
| 2004 | Round 34 — Prompt-Engine/Stability · Domains/Org-Governance · Config/Bootstrap · SDK/Plugins / §171 Config / Bootstrap / Divisions 缺陷 | P2 | `config/security/threat-matrix.json` | STRIDE 模型缺 TAMPERING(config)/INFO_DISCLOSURE(agent memory)缓解 | 需继续整改（测试/配置差距） | 测试、配置或 bootstrap 缺少审计要求的覆盖与约束，本轮未补齐。 | 已按源审计条目补录编号；本轮未新增覆盖缺口对应的测试、配置或 bootstrap 修复证据。 | 补录到 reaudit，待专项整改。 |
| 2005 | Round 34 — Prompt-Engine/Stability · Domains/Org-Governance · Config/Bootstrap · SDK/Plugins / §172 SDK / Plugins 缺陷 | P0 | `src/sdk/client-sdk/api-client.ts:208` | 重试所有非 OK 含 4xx——POST/DELETE 重复执行 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 2006 | Round 34 — Prompt-Engine/Stability · Domains/Org-Governance · Config/Bootstrap · SDK/Plugins / §172 SDK / Plugins 缺陷 | P0 | `src/sdk/client-sdk/api-client.ts` | 无 ContractEnvelope 包装——spec 强制要求 | 需继续整改（ADR/contract） | ADR、contract、类型导出与现行实现存在漂移，本轮未完成逐条回写与一致性校验闭环。 | 已按源审计条目补录编号；本轮未新增 contract/ADR 回写或一致性测试证据，因此不能改写为已解决。 | 补录到 reaudit，待专项整改。 |
| 2007 | Round 34 — Prompt-Engine/Stability · Domains/Org-Governance · Config/Bootstrap · SDK/Plugins / §172 SDK / Plugins 缺陷 | P0 | `src/sdk/plugin-sdk/plugin-definition.ts:144-148` | 签名验证从不执行——spec 要求签名强制 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 2008 | Round 34 — Prompt-Engine/Stability · Domains/Org-Governance · Config/Bootstrap · SDK/Plugins / §172 SDK / Plugins 缺陷 | P0 | `src/plugins/adapters/crm-adapter.ts:55-72` | execute 返回硬编码 mock——生产代码无实际 API 调用 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 2009 | Round 34 — Prompt-Engine/Stability · Domains/Org-Governance · Config/Bootstrap · SDK/Plugins / §172 SDK / Plugins 缺陷 | P1 | `src/sdk/harness-sdk/index.ts` | 缺 beforeRun/afterRun/onError/onTimeout 生命周期钩子 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 2010 | Round 34 — Prompt-Engine/Stability · Domains/Org-Governance · Config/Bootstrap · SDK/Plugins / §172 SDK / Plugins 缺陷 | P1 | `src/sdk/client-sdk/api-client.ts` | 无版本握手协议 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 2011 | Round 34 — Prompt-Engine/Stability · Domains/Org-Governance · Config/Bootstrap · SDK/Plugins / §172 SDK / Plugins 缺陷 | P1 | `src/sdk/client-sdk/api-client.ts` | 无类型化错误——HTTP 错误被吞 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 2012 | Round 34 — Prompt-Engine/Stability · Domains/Org-Governance · Config/Bootstrap · SDK/Plugins / §172 SDK / Plugins 缺陷 | P1 | `src/sdk/admin-sdk/index.ts` | 缺租户管理/配置操作/审计访问 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 2013 | Round 34 — Prompt-Engine/Stability · Domains/Org-Governance · Config/Bootstrap · SDK/Plugins / §172 SDK / Plugins 缺陷 | P1 | `src/sdk/pack-sdk/pack-manifest.ts:42-104` | 无安全扫描/制品签名 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 2014 | Round 34 — Prompt-Engine/Stability · Domains/Org-Governance · Config/Bootstrap · SDK/Plugins / §172 SDK / Plugins 缺陷 | P1 | `src/plugins/adapters/game-dev-adapter.ts:26-28` | authenticate 是 no-op,execute 无 auth guard(同 asset-production/livestream) | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 2015 | Round 34 — Prompt-Engine/Stability · Domains/Org-Governance · Config/Bootstrap · SDK/Plugins / §172 SDK / Plugins 缺陷 | P1 | `src/sdk/plugin-sdk/plugin-definition.ts:140` | resourceLimits/sandboxTier 仅存储不强制——sandboxTier:"none" 无告警 | 已解决（源码修复并补定向测试） | 根因是 `definePlugin()` 过去只做必填校验，既会把 legacy `"none"` 静默归一化成 `read_only`，也不会拦截非正数 resource limits。 | 当前 `src/sdk/plugin-sdk/plugin-definition.ts` 已新增 `normalizeSecurityConfig()` 与 `normalizeResourceLimits()`：显式拒绝 `sandboxTier: "none"`，并要求 `maxMemoryMb/maxCpuMs/maxDurationMs` 为正且有限；`tests/unit/sdk/plugin-sdk/plugin-definition.test.ts` 与既有 `tests/unit/plugins/plugin-definition.test.ts` 已覆盖 insecure sandbox 与非法 resource limit 回归并通过。 | 关闭 plugin definition 对 sandbox/resource 仅存储不强制的缺口。 |
| 2016 | Round 34 — Prompt-Engine/Stability · Domains/Org-Governance · Config/Bootstrap · SDK/Plugins / §172 SDK / Plugins 缺陷 | P1 | `src/plugins/validators/basic-evaluator.ts:42` | requiredFields 空时跳过类型验证 | 已解决（源码复核并补定向测试） | 原审计结论停留在早期实现认知；现行类型校验循环本就独立于 `requiredFields`，只是之前缺少直接证据把这条旧指控清掉。 | 当前 `src/plugins/validators/basic-evaluator.ts` 会无条件遍历 `contract.fieldTypes` 做类型检查，与 `requiredFields` 是否为空无关；本轮补充 `tests/unit/plugins/validators/basic-evaluator.test.ts`，直接验证 `requiredFields: []` 且 `fieldTypes.field1="string"` 时仍会正确报出 `Expected string, received number`。 | 以源码复核和回归测试关闭陈旧指控。 |
| 2017 | Round 34 — Prompt-Engine/Stability · Domains/Org-Governance · Config/Bootstrap · SDK/Plugins / §172 SDK / Plugins 缺陷 | P1 | `src/plugins/validators/basic-evaluator.ts:53` | null 被类型化为 "object"——通过 object 类型检查 | 已解决（源码修复并补定向测试） | 根因是 evaluator 旧实现把 `null` 归类成 `"object"`，导致 object schema 被 `null` 静默通过。 | 当前 `src/plugins/validators/basic-evaluator.ts` 已改为通过 `describeValueType()` 把 `null` 单独识别为 `"null"`；`tests/unit/plugins/validators/basic-evaluator.test.ts` 已回写为断言 object 类型收到 `null` 时会报 `Expected object, received null`。 | 关闭 null 误判为 object 的校验缺口。 |
| 2018 | Round 34 — Prompt-Engine/Stability · Domains/Org-Governance · Config/Bootstrap · SDK/Plugins / §172 SDK / Plugins 缺陷 | P2 | `src/sdk/plugin-sdk/plugin-test-harness.ts:196-213` | executePlugin 是 mock 永不运行真实插件代码 | 已修复（源码/测试） | 根因与 `R8-40/R15-06` 相同：旧版 harness 缺少 live runtime binding，导致 `executePlugin()` 只能围绕 manifest/mock 打转。 | 当前 `src/sdk/plugin-sdk/plugin-test-harness.ts` 已可通过 `livePlugin/liveRunner` 执行真实 runtime plugin；`tests/unit/sdk/plugin-sdk/plugin-test-harness-regressions.test.ts` 已定向验证 live 执行结果与 timeout 约束。 | 作为重复挂账条目，与 `R8-40/R15-06` 一并关闭。 |
| 2019 | Round 34 — Prompt-Engine/Stability · Domains/Org-Governance · Config/Bootstrap · SDK/Plugins / §172 SDK / Plugins 缺陷 | P2 | `src/sdk/workbench/index.ts:8-10` | validatePluginManifest 是 no-op pass-through | 已解决（源码修复并补定向测试） | 根因是 workbench 过去直接透传 plugin manifest，既不做 schema 校验，也不在 buildSnapshot/installPlan 阶段阻断非法插件元数据。 | 当前 `src/sdk/workbench/index.ts` 已改为通过 `PluginManifestSchema.safeParse()` 校验并在失败时抛出 `sdk_workbench.invalid_plugin_manifest`；同时补齐 `tests/unit/sdk/workbench/workbench-service.test.ts` 与 `tests/unit/sdk/workbench/workbench-service-additional.test.ts`，验证合法 manifest 仍可构建 snapshot，非法 manifest 不再被静默透传。 | 关闭 workbench plugin manifest “空校验”缺口。 |
| 2020 | Round 34 — Prompt-Engine/Stability · Domains/Org-Governance · Config/Bootstrap · SDK/Plugins / §172 SDK / Plugins 缺陷 | P2 | `src/plugins/adapters/github-adapter.ts:81` | repository 参数未 sanitize——URL 路径遍历 | 已解决（源码修复并补定向测试） | 根因是 GitHub adapter 过去只要求 `repository` 为非空字符串，随后直接插值进 `/repos/${repository}`，允许 `..`、额外分段和编码斜杠进入请求路径。 | 当前 `src/plugins/adapters/github-adapter.ts` 已新增 `requireRepository()`，强制 `owner/repo` 规范格式，并拒绝 `..`、反斜杠、`%2f/%5c/%2e` 等穿越/编码路径片段；`tests/unit/plugins/adapters/github-adapter.test.ts` 已新增 traversal 与 encoded slash 回归用例并通过。 | 关闭 repository 路径注入缺口。 |
| 2021 | Round 34 — Prompt-Engine/Stability · Domains/Org-Governance · Config/Bootstrap · SDK/Plugins / §172 SDK / Plugins 缺陷 | P2 | `src/sdk/pack-sdk/pack-scaffold-service.ts:266-273` | packId 模板注入——特殊字符注入生成文件 | 已解决（源码修复并补定向测试） | 根因是 scaffold 仅校验 `packId`，但 `name/domain/owner` 仍可携带换行、反引号或 `${...}` 等模板控制字符，最终直接写入脚手架文件和 manifest。 | 当前 `src/sdk/pack-sdk/pack-scaffold-service.ts` 已把 `validateScaffoldConfig()` 改为返回规范化配置，并对 `name/domain/owner` 增加模板注入防护与 trim 归一化；`tests/unit/sdk/pack-sdk/pack-scaffold-service.test.ts` 已新增 name/domain 注入拒绝与规范化渲染回归。 | 关闭 scaffold 模板注入缺口。 |
| 2022 | Round 35 — Orchestration/State-Evidence · Interaction/Interface · ADR/Contract · UI Features/Shared / §174 Orchestration / State-Evidence 深层缺陷 | P0 | `src/platform/orchestration/oapeflir/stage-transition-fsm.ts:122` | FSM 阻止 feedback→observe 反向转换——OAPEFLIR 闭环不可能 | 已解决（源码修复并补定向测试） | 根因是 FSM 只保留了狭义的 feedback→plan 回退意图，却没有把 feedback 完成后的 observe 回环建模进去，导致闭环重观察路径被错误拒绝。 | 当前 `src/platform/five-plane-orchestration/oapeflir/stage-transition-fsm.ts` 已在 `feedback` 完成后显式允许回退到 `observe/plan`，统一返回 `fsm.feedback_driven_replan`；`tests/unit/platform/five-plane-orchestration/stage-transition-fsm.test.ts` 已新增 closed loop 回到 `observe` 的回归断言并通过。 | 关闭 OAPEFLIR feedback→observe 闭环缺口。 |
| 2023 | Round 35 — Orchestration/State-Evidence · Interaction/Interface · ADR/Contract · UI Features/Shared / §174 Orchestration / State-Evidence 深层缺陷 | P0 | `src/platform/orchestration/oapeflir/stage-transition-fsm.ts:182` | recordStageCompletion("release") 设 index=8 越界——后续查询 crash | 已解决（源码修复并补定向测试） | 根因是 `release` 完成后 `currentStageIndex` 会推进到 stage 数组末尾之外，而 `getCurrentStage()` 仍直接按索引读取，导致完成态后续查询存在越界崩溃风险。 | 当前 `src/platform/five-plane-orchestration/oapeflir/stage-transition-fsm.ts` 已对完成态 `getCurrentStage()` 做末阶段钳制，并在 `recordStageEntry/reset/resetToStage` 同步维护 stage 指针和 skip reason 状态；`tests/unit/platform/five-plane-orchestration/stage-transition-fsm.test.ts` 已验证全阶段完成后 `getCurrentStage()` 安全返回 `release`。 | 关闭 release 完成后的越界查询缺口。 |
| 2024 | Round 35 — Orchestration/State-Evidence · Interaction/Interface · ADR/Contract · UI Features/Shared / §174 Orchestration / State-Evidence 深层缺陷 | P0 | `src/platform/state-evidence/events/cas/cas-service.ts:46-47` | CAS 纯内存 Map——read-check-write 非原子,无分布式并发控制 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 2025 | Round 35 — Orchestration/State-Evidence · Interaction/Interface · ADR/Contract · UI Features/Shared / §174 Orchestration / State-Evidence 深层缺陷 | P0 | `src/platform/state-evidence/events/transactional-event-appender.ts:97` | 手动 BEGIN/COMMIT 绕过 db.transaction()——SQLite 无嵌套事务导致边界损坏 | 已解决（源码/测试复核） | 原问题已不是现状：当前 appender 不再手写 `BEGIN/COMMIT`，而是统一走数据库包装器的事务边界和嵌套事务语义。 | `src/platform/state-evidence/events/transactional-event-appender.ts` 当前通过 `this.db.transaction(() => ...)` 执行 event/outbox/truth mutation；`tests/unit/platform/state-evidence/events/transactional-event-appender.test.ts` 本轮已定向通过多事件原子提交、truth mutation 原子提交与失败回滚等测试。 | 属于重复挂账的过期条目，按现行证据关闭。 |
| 2026 | Round 35 — Orchestration/State-Evidence · Interaction/Interface · ADR/Contract · UI Features/Shared / §174 Orchestration / State-Evidence 深层缺陷 | P1 | `src/platform/state-evidence/events/cas/fencing-token-service.ts:100-110` | fencing token 按 `-` split 解析——UUID executionId 含连字符被错误切割 | 已解决（源码修复并补定向测试） | 根因是旧 fencing token 编码直接用 `-` 串联 `executionId/nodeId/counter/timestamp`，当 `executionId` 或 `nodeId` 自身带连字符时，验证阶段会把 owner 与 executionId 错拆。 | 当前 `src/platform/five-plane-state-evidence/events/cas/fencing-token-service.ts` 已改为使用 `::` 作为分隔符，并对 `executionId`、`nodeId` 做 `encodeURIComponent`/解码校验，彻底消除连字符歧义。 | `tests/unit/platform/state-evidence/events/cas/fencing-token-service.test.ts` 已覆盖 `::` 新格式与带连字符 ID 的回归，这条旧挂账应按现行 CAS token 契约关闭，而不是误归类到 UI 差距。 |
| 2027 | Round 35 — Orchestration/State-Evidence · Interaction/Interface · ADR/Contract · UI Features/Shared / §174 Orchestration / State-Evidence 深层缺陷 | P1 | `src/platform/state-evidence/memory/memory-service.ts:158` | 内容 hash 截断至 16 hex(64-bit)——~10K 记录时碰撞概率显著 | 已解决（源码修复并补定向测试） | 根因是 `remember()` 过去把 SHA-256 摘要裁成前 16 个 hex 字符，只保留 64-bit 熵，规模上来后内容去重和碰撞风险都会明显放大。 | 当前 `src/platform/five-plane-state-evidence/memory/memory-service.ts` 已改为保留完整 64 hex 的 SHA-256 digest；`tests/unit/platform/state-evidence/memory/memory-service-content-hash.test.ts` 已更新为验证完整摘要长度、与标准 SHA-256 一致，以及不同内容不再依赖截断前缀。 | 关闭 memory content hash 截断缺口。 |
| 2028 | Round 35 — Orchestration/State-Evidence · Interaction/Interface · ADR/Contract · UI Features/Shared / §174 Orchestration / State-Evidence 深层缺陷 | P1 | `src/platform/state-evidence/memory/memory-decay-service.ts:194-195` | scope→SixLayerMemoryType 无映射——"project"等真实 scope 全回退 session 衰减率 | 已解决（源码修复并补定向测试） | 根因是 `MemoryDecayService.getDecayConfig()` 过去把 `memory.scope` 误当成已是 `SixLayerMemoryType` 的 layer 名称去查 metadata，导致 `project/workspace/user/...` 等真实 scope 统统回退到 session 配置。 | 当前 `src/platform/five-plane-state-evidence/memory/memory-decay-service.ts` 已改用 `mapScopeToSixLayer(memory.scope)` 做显式 scope→layer 映射；`tests/unit/platform/state-evidence/memory/memory-decay-service.test.ts` 已新增 `project -> semantic` 的回归断言，并同步校正 working/procedural 的当前 decay 配置期望。 | 关闭真实 scope 误回退 session 的衰减映射缺口。 |
| 2029 | Round 35 — Orchestration/State-Evidence · Interaction/Interface · ADR/Contract · UI Features/Shared / §174 Orchestration / State-Evidence 深层缺陷 | P1 | `src/platform/state-evidence/knowledge/knowledge-ingestion-pipeline.ts:169` | 知识摄入跳过 quarantine——spec 要求隔离+推广生命周期 | 已解决（源码修复并补定向测试） | 根因是 ingestion pipeline 过去无论 trust level 一律把文档直接标成 `indexed` 并写入 keyword/semantic 检索路径，完全跳过 quarantine/promote 生命周期。 | 当前 `src/platform/five-plane-state-evidence/knowledge/knowledge-ingestion-pipeline.ts` 已先按 trust level 规范化，写入 `ingestionLifecycle` 元数据，并把 `private_unverified` 文档保留为 `draft/quarantined`；只有达到 `team_reviewed` 及以上级别才 promotion 到 `indexed`。同时 `knowledge/retrieval/knowledge-retrieval.ts` 已禁止 draft/archived 文档进入 semantic 和访问路径，`archive/knowledge-archive.ts` 也会保留输入状态而非强制改写为 indexed。`tests/unit/platform/state-evidence/knowledge/knowledge-ingestion-pipeline.test.ts` 与 `...comprehensive.test.ts` 已验证 legacy trust 归一化、默认 trust、quarantine 状态以及未推广文档不可检索。 | 关闭知识摄入跳过 quarantine/promotion 生命周期缺口。 |
| 2030 | Round 35 — Orchestration/State-Evidence · Interaction/Interface · ADR/Contract · UI Features/Shared / §174 Orchestration / State-Evidence 深层缺陷 | P1 | `src/platform/state-evidence/checkpoints/workflow-step-checkpoint.ts:255-258` | compensationModel 类型判断 typeof==="string" 但实际类型是 object——有效 checkpoint 被拒 | 已解决 | 原问题是真实类型守卫缺口；checkpoint 读取逻辑只接受字符串 compensation model，导致合法的结构化补偿策略对象被误判为无效快照。 | 现已在 `src/platform/five-plane-state-evidence/checkpoints/workflow-step-checkpoint.ts` 引入 `WorkflowStepCheckpointCompensationModel`，允许字符串或带 `strategy` 的对象；`isWorkflowStepCheckpoint()` 同步接受结构化对象。定向回归 `tests/unit/platform/five-plane-state-evidence/checkpoints/workflow-step-checkpoint.test.ts` 新增“structured compensation model objects”并已通过。 | 关闭 checkpoint structured compensation 误拒。 |
| 2031 | Round 35 — Orchestration/State-Evidence · Interaction/Interface · ADR/Contract · UI Features/Shared / §174 Orchestration / State-Evidence 深层缺陷 | P1 | `src/platform/state-evidence/memory/memory-service.ts:138-140` | 大小检查用 .length(UTF-16 码元)非字节——CJK 内容可超 1MB | 已解决 | 原问题真实存在；字符串长度校验会低估多字节内容，导致 1MB 上限可被 CJK 文本绕过。 | 现已在 `src/platform/five-plane-state-evidence/memory/memory-service.ts` 改为 `Buffer.byteLength(..., "utf8")` 做字节级大小检查；定向回归 `tests/unit/platform/state-evidence/memory/memory-service.test.ts` 新增 UTF-8/CJK 超限用例并通过。 | 关闭字节计量偏差。 |
| 2032 | Round 35 — Orchestration/State-Evidence · Interaction/Interface · ADR/Contract · UI Features/Shared / §174 Orchestration / State-Evidence 深层缺陷 | P1 | `src/platform/orchestration/oapeflir/oapeflir-loop-service.ts:361-365` | 返回原始 assessment 而非 validatedAssessment——下游收到未验证数据 | 已解决（源码复核） | 原问题来自 A→P 边界验证后仍把原始 `assessment` 继续下传；一旦校验失败，下游阶段会收到未规范化对象。 | 当前 `src/platform/five-plane-orchestration/oapeflir/oapeflir-loop-service.ts` 已在 assess→plan 边界构造 `validatedAssessment`，并在 plan/release/final-return 三处统一返回该对象（现行源码可见第 233、271、404、704 行附近均已收敛到 `validatedAssessment`）。 | 回写 reaudit 状态，关闭“未验证 assessment 外泄”。 |
| 2033 | Round 35 — Orchestration/State-Evidence · Interaction/Interface · ADR/Contract · UI Features/Shared / §174 Orchestration / State-Evidence 深层缺陷 | P2 | `src/platform/state-evidence/events/durable-event-bus.ts:358` | 重试循环 0..<=MAX(3) 实际 4 次——描述说"3次重试" | 已解决 | 根因是历史实现把上界写成了包含式循环，导致最大尝试次数与 contract 不一致；reaudit 状态未跟随代码回写。 | 当前 `src/platform/five-plane-state-evidence/events/durable-event-bus.ts` 已使用 `attempt < MAX_DELIVERY_RETRIES`，本轮补充确定性回归 `tests/unit/platform/state-evidence/events/durable-event-bus-attempt-limit.test.ts`，直接验证失败 handler 仅被调用 3 次且随后标记 failed。 | 回写已修复的重试上界。 |
| 2034 | Round 35 — Orchestration/State-Evidence · Interaction/Interface · ADR/Contract · UI Features/Shared / §174 Orchestration / State-Evidence 深层缺陷 | P2 | `src/platform/state-evidence/events/event-registry.ts:670-675` | getRegisteredConsumers 对 runtime 类型 undefined 解引 crash | 已解决 | 原问题是真实空引用风险；runtime replay metadata 事件未落在 schema registry 时，消费者查询路径可能走到未定义分支。 | 当前 `src/platform/five-plane-state-evidence/events/event-registry.ts` 的 `getRegisteredConsumers()` 已先判空并对 `RUNTIME_EVENT_REPLAY_METADATA` 返回默认消费者集合；定向回归 `tests/unit/platform/state-evidence/events/event-registry-crash-regression.test.ts` 已覆盖 unknown/runtime-metadata/platform 三类安全调用路径。 | 关闭 runtime 类型空解引。 |
| 2035 | Round 35 — Orchestration/State-Evidence · Interaction/Interface · ADR/Contract · UI Features/Shared / §174 Orchestration / State-Evidence 深层缺陷 | P2 | `src/platform/orchestration/harness/memory-manager.ts:10-39` | HarnessMemoryManager 无 eviction/max-size——无界增长 | 已解决 | 该项不只是“缺上限”；旧 eviction 只删元数据、不删 namespace 存储，仍会残留脏读。 | 当前 `src/platform/orchestration/harness/memory-manager.ts` 已在写入前按 tier 上限执行 eviction，并在 `evictOldestFromTier()` 同步清理 `memoryRecords` 与 namespace scope map；定向回归 `tests/unit/platform/orchestration/harness/memory-manager.test.ts` 已验证 overflow 后旧值不可读且作用域列表被清空。 | 关闭 harness memory 无界增长与残留脏读。 |
| 2036 | Round 35 — Orchestration/State-Evidence · Interaction/Interface · ADR/Contract · UI Features/Shared / §174 Orchestration / State-Evidence 深层缺陷 | P2 | `src/platform/state-evidence/memory/knowledge-promotion-service.ts:403` | verificationStatus 原地 mutate——违反追加-only 不可变审计 | 已解决 | 原实现直接改写 `entry.lineage`，外部已持有的 lineage 引用会被静默篡改，不符合追加式审计/不可变快照预期。 | 现已在 `src/platform/five-plane-state-evidence/memory/knowledge-promotion-service.ts` 的 `updateVerificationStatus()` 中构造新的 `KnowledgeLineage` 并替换 map entry，而不再原地改写；定向回归 `tests/unit/platform/state-evidence/memory/knowledge-promotion-service.test.ts` 已验证 returned snapshot 不会被后续状态更新反向污染。 | 关闭 lineage 原地变异。 |
| 2037 | Round 35 — Orchestration/State-Evidence · Interaction/Interface · ADR/Contract · UI Features/Shared / §174 Orchestration / State-Evidence 深层缺陷 | P2 | `src/platform/state-evidence/memory/memory-retrieval-service.ts:288` | 参数化查询前手动转义引号——含引号 ID 永远 DELETE 不到 | 已解决 | 原缺陷来自“参数化 SQL 之前又做一层手动转义”，把合法主键值改写成错误值。 | 当前 `src/platform/five-plane-state-evidence/memory/memory-retrieval-service.ts` 的 `unindexMemory()` 已直接把原始 `memoryId` 交给参数化 `DELETE`；定向回归 `tests/unit/platform/state-evidence/memory/memory-retrieval-service.test.ts` 已验证带单引号 ID 按原值传参。 | 关闭 FTS 删除主键误转义。 |
| 2038 | Round 35 — Orchestration/State-Evidence · Interaction/Interface · ADR/Contract · UI Features/Shared / §175 Interaction / Interface 缺陷 | P0 | `src/platform/interface/api/http-server/response-hardening.ts:12-18` | CORS origin:"\*"+credentials:true——回显任意 Origin=无 CORS | 已解决 | 原问题是 wildcard origin 与 credentialed CORS 可以同时成立，服务端会把任意来源升级成可携带凭据的受信来源。 | 现已在 `src/platform/five-plane-interface/api/http-server/response-hardening.ts` 的 `normalizeCorsConfig()` 中对 `credentials=true && allowedOrigins.includes("*")` 直接 fail-close 抛错，`resolveAllowOrigin()` 仅在非 credentialed wildcard 场景返回 `"*"`；定向回归 `tests/unit/platform/interface/api/http-server/response-hardening-2038.test.ts` 已覆盖 config-time reject、preflight/response headers 空返回和非凭证 wildcard 保留场景并通过。 | 关闭 credentialed wildcard CORS 漏洞。 |
| 2039 | Round 35 — Orchestration/State-Evidence · Interaction/Interface · ADR/Contract · UI Features/Shared / §175 Interaction / Interface 缺陷 | P0 | `src/platform/interface/api/http-server/approval-routes.ts:73,119` | approvalId 无校验/无 authz——任意 ID 注入批准/查看决策 | 已解决 | 原问题属于典型 BOLA/ID injection：路由直接吃 URL 中的 `approvalId`，既不验证格式，也不在 applyDecision 前校验 actor 是否对该 approval 有权限。 | 现已在 `src/platform/five-plane-interface/api/http-server/approval-routes.ts` 增加 `validateApprovalId()`、`assertApprovalAccess()` 与 `getAuthorizedApprovalView()`；POST 决策路径会先校验 ID，再基于 inspect 结果执行 tenant + `allowedActorIds` 授权检查，并把 inspect not-found 统一映射为 404。定向回归 `tests/unit/platform/interface/api/http-server/approval-routes-2039.test.ts` 已覆盖非法 ID、越权 actor、跨 tenant、unknown approval 和 applyDecision 不应落库等路径并通过。 | 关闭 approval 决策 BOLA。 |
| 2040 | Round 35 — Orchestration/State-Evidence · Interaction/Interface · ADR/Contract · UI Features/Shared / §175 Interaction / Interface 缺陷 | P0 | `src/interaction/dashboard/dashboard-websocket-server.ts:330-346` | heartbeat 标记断开但不 unregister——连接/订阅 Map 无界泄漏 | 已解决（与 R11-29/R23-14 同源） | 原问题是真实缺陷，但当前源码已不再停留在“只标记断开”。reaudit 这里是补录编号后没有同步回写状态。 | 当前 `src/interaction/dashboard/dashboard-websocket-server.ts` 的 `performHeartbeat()` 会收集超时连接并调用 `unregisterClient()`，后者会同时清理 `connections`、`channelSubscribers` 和 metric 订阅；现有 `tests/unit/interaction/dashboard/dashboard-websocket-server.test.ts` 已覆盖超时后连接数归零和路由不再投递。 | 重复挂账统一关闭。 |
| 2041 | Round 35 — Orchestration/State-Evidence · Interaction/Interface · ADR/Contract · UI Features/Shared / §175 Interaction / Interface 缺陷 | P1 | `src/interaction/goal-decomposer/index.ts:297-324` | 检测到循环图仍设 ready_for_planner——planner 收到循环 DAG | 已解决 | 原问题不止是 handoff state 错误；循环图分析分支还继续按 DAG 计算 critical path，导致回溯 predecessor 时可能在 cycle 上死循环。 | 现已在 `src/interaction/goal-decomposer/index.ts` 中把 `plannerHandoff.state` 收紧为 `graphAnalysis.hasCycle ? "blocked_invalid_graph" : "ready_for_planner"`，并在 cycle 分支直接返回空 `criticalPathTaskIds` 避免回溯死循环；定向回归 `tests/unit/interaction/goal-decomposer/review-regressions.test.ts` 已验证 cycle 图会产出 `goal_decomposer.cycle_detected`、`taskGraphDraft.normalized=false`、`plannerHandoff.state="blocked_invalid_graph"` 并通过。 | 关闭循环 DAG planner handoff 与回溯死循环。 |
| 2042 | Round 35 — Orchestration/State-Evidence · Interaction/Interface · ADR/Contract · UI Features/Shared / §175 Interaction / Interface 缺陷 | P1 | `src/interaction/autonomy/level-manager/index.ts:3-9` | "frozen" 排序在 "full_auto" 之上——frozen agent 被比作更高自治 | 已解决 | 原问题是真实排序语义错误；`frozen` 作为“最低自治/禁止执行”状态却被放到最高一侧，比较结果会把冻结 agent 视为比全自动更高自治。 | 现已在 `src/interaction/autonomy/level-manager/index.ts` 把 `AUTONOMY_LEVEL_ORDER` 调整为 `["frozen","suggestion","supervised","semi_auto","full_auto"]`，并把 `nextAutonomyLevel()` 改成显式状态机，保证 `frozen` 固定停留、其余级别按序提升到 `full_auto` 封顶；定向回归 `tests/unit/interaction/autonomy/level-manager.test.ts` 与 `tests/unit/interaction/autonomy/level-manager/index.test.ts` 已通过。 | 关闭 frozen 排序颠倒。 |
| 2043 | Round 35 — Orchestration/State-Evidence · Interaction/Interface · ADR/Contract · UI Features/Shared / §175 Interaction / Interface 缺陷 | P1 | `src/platform/interface/api/http-server/task-routes.ts:255-256` | PATCH title 更新死代码——写入旧值不生效 | 已解决 | 该缺陷此前已在生产代码修复，但 reaudit 与回归样例仍停留在旧路由段格式 `/api/v1/...` 和过时状态枚举上，导致表格长期误报“未解决”。 | 当前 `src/platform/five-plane-interface/api/http-server/task-routes.ts` 的 PATCH 分支已显式执行 `deps.taskStore.task.updateTaskTitle(taskId, payload.title, now)`；本轮把 `tests/unit/platform/interface/api/http-server/task-routes-2043.test.ts` 对齐到现行 `/v1/tasks/:id` 路由和当前 status schema 后，已验证 title/status/output 三条更新链均能命中并通过。 | 回写已修复的 PATCH title 链路。 |
| 2044 | Round 35 — Orchestration/State-Evidence · Interaction/Interface · ADR/Contract · UI Features/Shared / §175 Interaction / Interface 缺陷 | P1 | `src/interaction/goal-decomposer/index.ts:343-344` | currentDepth 恒0/maxDepthReached 恒 false——递归深度防护无效 | 已解决 | 原问题属实：服务只回显外部 `currentDepth`，没有根据真实 dependency graph 计算本次分解深度，也不会在达到上限时触发审计告警或人工复核。 | 当前 `src/interaction/goal-decomposer/index.ts` 已按 dependency graph 计算 `maxDependencyDepth`，用其更新 `depthUsed/maxDepthReached`，并在达到上限时写入 `goal_decomposer.max_depth_reached:*` validation message 且强制 `requiresHumanReview=true`；`tests/unit/interaction/goal-decomposer/depth-regression-2044.test.ts` 已验证营销 DAG 在 `maxDepth=2` 时会得到 `depthUsed=3`、`maxDepthReached=true`。 | 修复深度防护失效。 |
| 2045 | Round 35 — Orchestration/State-Evidence · Interaction/Interface · ADR/Contract · UI Features/Shared / §175 Interaction / Interface 缺陷 | P1 | `src/platform/interface/api/http-server/admin-routes.ts:217` | listTenants(MAX_SAFE_INTEGER) 加载全量计算 total——OOM DoS | 已解决 | 原问题是真实资源放大型缺陷：为算 `total` 二次调用 `listTenants(Number.MAX_SAFE_INTEGER)`，会把全量 tenant 装进内存。 | 现已在 `src/platform/five-plane-interface/api/http-server/admin-routes.ts` 的 `/v1/admin/tenants` 路径改为单次 `listTenants(limit)`，并用 `tenants.length` 返回当前页 total，不再存在全量读取；定向回归 `tests/unit/platform/interface/api/http-server/admin-routes-2045.test.ts` 已验证默认 limit、用户指定 limit、超大 limit clamp 和 response total 一致性并通过。 | 关闭 admin tenant listing OOM DoS。 |
| 2046 | Round 35 — Orchestration/State-Evidence · Interaction/Interface · ADR/Contract · UI Features/Shared / §175 Interaction / Interface 缺陷 | P1 | `src/interaction/proactive-agent/index.ts:392-421` | 循环检测 stack.delete 在捕获前——incident 记录不完整 trigger 集 | 已解决（回归核实） | 原问题是 feedback loop 命中后 incident 捕获时机不对，导致 `triggerIds` 可能在栈弹出后再读取，记录不全。 | 当前 `src/interaction/proactive-agent/index.ts` 的 cycle 检测在发现环时会保留 stack，并直接以 `triggerIds: [...stack]` 生成 incident；本轮补充 `tests/unit/interaction/proactive-agent/trigger-engine/trigger-engine.test.ts`，验证 `trigger-a` 与 `trigger-b` 两个环节点都会完整落入 incident 且相关 trigger 被禁用，回归已通过。 | 关闭 proactive feedback loop incident 截断。 |
| 2047 | Round 35 — Orchestration/State-Evidence · Interaction/Interface · ADR/Contract · UI Features/Shared / §175 Interaction / Interface 缺陷 | P1 | `src/platform/interface/channel-gateway/channel-gateway-service.ts` | 仅 outbound——inbound webhook 存原始 payload 不转 RequestEnvelope | 已解决（源码修复并补定向测试） | 根因是 webhook 重试链过去只持久化 `{ targetId, text, metadata }`，即便 metadata 里带有 envelope 线索，也不会在 tracked payload / replay body 中保留结构化 `requestEnvelope`。 | 当前 `src/platform/interface/channel-gateway/channel-gateway-service.ts`、`helpers.ts` 与 `types.ts` 已在 tracked payload 中显式保留 `requestEnvelope`，并支持把 `_envelope_*` 扁平字段归一化后随 retry replay 一并发出；`tests/unit/platform/interface/channel-gateway/channel-gateway-service-2047.test.ts` 已验证显式 envelope、扁平 envelope 与 retry replay 三条路径。 | 关闭 webhook retry 丢失 RequestEnvelope 上下文缺口。 |
| 2048 | Round 35 — Orchestration/State-Evidence · Interaction/Interface · ADR/Contract · UI Features/Shared / §175 Interaction / Interface 缺陷 | P2 | `src/interaction/proactive-agent/trigger-engine/index.ts:1-9` | resolveTriggerActionMode 导出但从未调用(死代码) | 已解决 | 原问题是行为逻辑在 `proactive-agent/index.ts` 内部复制了一份，导致 helper 虽导出却未进入主路径。 | 现已在 `src/interaction/proactive-agent/index.ts` 直接复用 `resolveTriggerActionMode()`，仅对 `update_dashboard` 保留显式 `silent_record` 特判；对应单测 `tests/unit/interaction/proactive-agent/trigger-engine/resolve-trigger-action-mode.unit.test.ts` 与 `tests/unit/interaction/proactive-agent/trigger-engine/trigger-engine.test.ts` 已同步到当前 contract 并通过。 | 关闭死代码与双份判定逻辑。 |
| 2049 | Round 35 — Orchestration/State-Evidence · Interaction/Interface · ADR/Contract · UI Features/Shared / §175 Interaction / Interface 缺陷 | P2 | `src/interaction/nl-gateway/index.ts:463-478 vs :668` | 状态机不一致——parseDetailed 返回 "Building" 但 buildTask 跳过 | 已解决 | 问题属实：`parseDetailed()` 暴露了一个不会被下游消费的 `"Building"` 中间态，和 `buildTask()` 的真实状态流转不一致。 | 当前 `src/interaction/nl-gateway/index.ts` 已让 `parseDetailed()` 复用 `deriveConversationState(...)`，未澄清请求直接返回 `"Executing"`，阻断/澄清仍返回 `"Clarifying"`；定向回归 `tests/unit/interaction/nl-gateway/index.test.ts` 已覆盖该状态一致性。 | 关闭幽灵中间态。 |
| 2050 | Round 35 — Orchestration/State-Evidence · Interaction/Interface · ADR/Contract · UI Features/Shared / §175 Interaction / Interface 缺陷 | P2 | `src/interaction/dashboard/index.ts:402` | attention queue 按 createdAt 排序忽略 priority | 已解决 | 原排序确实会让较新的低优先级事项压过更高优先级 incident/budget 风险。 | 当前 `src/interaction/dashboard/index.ts` 已新增 `ATTENTION_PRIORITY_ORDER`，队列先按 `critical > high > normal > low` 排序，再在同优先级内按时间倒序；定向回归 `tests/unit/interaction/dashboard/index.test.ts` 已验证 critical incident 会排在更新更晚的 normal pending 之前。 | 关闭 attention queue 优先级失真。 |
| 2051 | Round 35 — Orchestration/State-Evidence · Interaction/Interface · ADR/Contract · UI Features/Shared / §175 Interaction / Interface 缺陷 | P2 | `src/interaction/nl-gateway/index.ts:804-899` | 会话上下文 Map 无 max-size/LRU——无界内存增长 | 已解决 | 原实现仅对单会话 turn window 做裁剪，但 active session map 本身没有容量上限，长期运行会持续膨胀。 | 当前 `src/interaction/nl-gateway/index.ts` 已为 `ConversationContextManager` 增加 `maxActiveContexts` 和 LRU 触碰/淘汰逻辑；定向回归 `tests/unit/interaction/nl-gateway/conversation-context-manager.test.ts` 已验证超限后最久未访问上下文被逐出。 | 关闭会话上下文无界增长。 |
| 2052 | Round 35 — Orchestration/State-Evidence · Interaction/Interface · ADR/Contract · UI Features/Shared / §175 Interaction / Interface 缺陷 | P2 | `src/platform/interface/api/middleware/` | 无 rate limiting 中间件接入 pipeline | 已解决（源码复核并补主链验证） | 根因是审计盯住了孤立 `middleware/rate-limit.ts` 文件，却没有复核真实入口已经收敛到 `HttpApiServer.handleRequest()`。现行实现不是“没接中间件”，而是把限流前移到 HTTP 入口并与 principal/tenant/ip 组合键绑定。 | 当前 `src/platform/interface/api/http-api-server.ts` 在非 `OPTIONS` 请求上会先调用 `DistributedRateLimiter.checkAndConsume()`，并返回 `429 + x-ratelimit-* + retry-after`；本轮新增定向验证 `tests/unit/platform/interface/api/http-api-server.test.ts` 用例 `rate limiter is enforced in the HttpApiServer request pipeline`，通过 `start()+fetch()` 走真实 handler 证明限流链生效。 | 回写主请求链证据，不把“没有接旧 middleware 文件”误判成“没有入口级限流”。 |
| 2053 | Round 35 — Orchestration/State-Evidence · Interaction/Interface · ADR/Contract · UI Features/Shared / §175 Interaction / Interface 缺陷 | P2 | `src/interaction/nl-gateway/index.ts:448-461` | 注入检测 per-pattern 仅返回首个匹配——低估攻击严重度 | 已解决 | 原实现对每个 regex 只执行一次 `exec()`，同一模式在单条消息里重复出现时只会上报首个命中。 | 当前 `src/interaction/nl-gateway/index.ts` 已改为对每个模式执行全量 `matchAll()` 并去重收集所有命中；定向回归 `tests/unit/interaction/nl-gateway/prompt-injection-multi-match.test.ts` 已覆盖跨模式多命中与同一模式重复命中。 | 关闭 prompt injection 严重度低估。 |
| 2054 | Round 35 — Orchestration/State-Evidence · Interaction/Interface · ADR/Contract · UI Features/Shared / §176 ADR / Contract 文档偏差 | P0 | `docs_zh/adr/060-explicit-planning-hub.md` | 定义 `Plan{steps:PlanStep[]}` 作为 P3→P4 规范——spec/ADR-109 要求 PlanGraphBundle(DAG) | 已解决（ADR/文档） | 根因是显式规划 ADR 早期把线性 `Plan{steps[]}` 和局部 bridge 名称一并写进正文，后续虽然 remediation 已补，但主段落仍容易让人误读为新的 contract 对象。 | 本轮已把 `ADR-060` 的 handoff 固定为 `PlanGraphBundle -> HarnessRuntime / NodeAttemptReceipt`，并明确 adapter/bridge 只能是 compatibility seam，不得成为新的 canonical contract。 | 关闭的是 ADR 叙事漂移。 |
| 2055 | Round 35 — Orchestration/State-Evidence · Interaction/Interface · ADR/Contract · UI Features/Shared / §176 ADR / Contract 文档偏差 | P0 | `docs_zh/adr/083-proactive-agent-and-progressive-autonomy.md` | 4 级自治——spec §9.5 已改为 8 种 canonical runtime mode | 已解决（ADR/文档） | 根因是 ADR 把 interaction autonomy ladder 和 runtime protection mode 混成一套枚举。 | 本轮已在 `ADR-083` 中显式拆分 `InteractionAutonomyLevel` 与 `UnifiedRuntimeMode`，并给出映射关系；同时把 `ADR-042` 同步回写到当前 `suggestion / supervised / semi_auto / full_auto / frozen` 语义。 | 关闭的是 ADR 层语义混叠，不外推为 interaction 代码已切成 8 态自治。 |
| 2056 | Round 35 — Orchestration/State-Evidence · Interaction/Interface · ADR/Contract · UI Features/Shared / §176 ADR / Contract 文档偏差 | P0 | `docs_zh/adr/065-workflow-visual-debugger.md` | 引用 WorkflowState 为权威——spec §5.5 已改为 HarnessRun/NodeRun | 已解决（ADR 复核） | 该条原问题已在 ADR 正文中修正，但 reaudit 状态没回写。 | 当前 `ADR-065` 已明确 `HarnessRun / NodeRun` 是权威对象，`WorkflowState` 仅保留为 legacy 投影。 | 不改正文，仅回写状态依据。 |
| 2057 | Round 35 — Orchestration/State-Evidence · Interaction/Interface · ADR/Contract · UI Features/Shared / §176 ADR / Contract 文档偏差 | P1 | `docs_zh/contracts/workflow_debugger_contract.md` | BreakpointDefinition 用 workflow_id+step_selector——应为 harnessRunId+nodeRunId | 已解决（contract 复核） | 该条原问题已在 debugger contract 中修正，但 reaudit 状态未同步更新。 | 当前 `workflow_debugger_contract.md` 的 `BreakpointDefinition` 已以 `harness_run_id / node_run_id / node_selector` 为锚点。 | 不改正文，仅回写状态依据。 |
| 2058 | Round 35 — Orchestration/State-Evidence · Interaction/Interface · ADR/Contract · UI Features/Shared / §176 ADR / Contract 文档偏差 | P1 | `docs_zh/contracts/perception_intelligence_plane_contract.md` | completeness_score 用 "steps" 单位——应为 NodeRun | 已解决（contract 复核） | 该条原问题已在 contract 中修正，但 reaudit 未更新。 | 当前 `perception_intelligence_plane_contract.md` 的 `ExecutionAssessment` 已以 `node_run_total / node_run_completed_count` 计算 completeness。 | 不改正文，仅回写状态依据。 |
| 2059 | Round 35 — Orchestration/State-Evidence · Interaction/Interface · ADR/Contract · UI Features/Shared / §176 ADR / Contract 文档偏差 | P1 | `docs_zh/contracts/app_error_contract.md` | 导出 WorkflowStateError——应为 HarnessRunError/NodeRunError | 已解决（contract 复核） | 该条原问题已在错误 contract 中修正，reaudit 没同步清理。 | 当前 `app_error_contract.md` 的标准派生类型已使用 `HarnessRunError / NodeRunError`。 | 不改正文，仅回写状态依据。 |
| 2060 | Round 35 — Orchestration/State-Evidence · Interaction/Interface · ADR/Contract · UI Features/Shared / §176 ADR / Contract 文档偏差 | P1 | `docs_zh/contracts/platform_ops_agent_contract.md` | OpsMaturityLevel 用 legacy 4 值——应为 canonical runtime modes | 已解决（contract/文档） | 根因是真正文案仍把 ops maturity 写成一套近似自治等级，而不是 executable side 的 `UnifiedRuntimeMode`。 | 本轮已把 `platform_ops_agent_contract.md` 回写为直接复用 `full_auto / supervised_auto / read_only / no_write / no_external_call / no_rollout / manual_only / incident_mode`。 | 已修复。 |
| 2061 | Round 35 — Orchestration/State-Evidence · Interaction/Interface · ADR/Contract · UI Features/Shared / §176 ADR / Contract 文档偏差 | P1 | `docs_zh/adr/073-unified-resource-model.md` | 将 TaskRecord/WorkflowState 列为"canonical objects"——spec §5.5 定义为 non-authoritative 投影 | 已解决（ADR 复核） | 该条原问题已在 ADR 正文中修正，但 reaudit 状态未回写。 | 当前 `ADR-073` 已将 `task_projection / workflow_projection` 明确标为 projection 资源，而非 authoritative runtime objects。 | 不改正文，仅回写状态依据。 |
| 2062 | Round 35 — Orchestration/State-Evidence · Interaction/Interface · ADR/Contract · UI Features/Shared / §176 ADR / Contract 文档偏差 | P1 | `docs_zh/adr/054-sla-tiered-guarantees.md` | platinum 99.99% 无 failover/quorum/演练前提——spec §2.5 禁止 | 已解决（ADR 复核） | 该条原问题已在 ADR 正文中修正，但 reaudit 状态未更新。 | 当前 `ADR-054` 已明确 99.99% 仅限具备 failover/quorum/容量预留/演练证据的专用部署层级。 | 不改正文，仅回写状态依据。 |
| 2063 | Round 35 — Orchestration/State-Evidence · Interaction/Interface · ADR/Contract · UI Features/Shared / §176 ADR / Contract 文档偏差 | P1 | `docs_zh/adr/026-risk-control-architecture.md` | 风险因子名/权重与 spec §10.2 八因子不匹配——因子已重命名重加权 | 已解决（ADR 复核） | 该条原问题已在 ADR 正文中修正，reaudit 状态未同步。 | 当前 `ADR-026` 已收敛到 `impact × 4 + irreversibility × 4` 的 canonical 风险模型。 | 不改正文，仅回写状态依据。 |
| 2065 | Round 35 — Orchestration/State-Evidence · Interaction/Interface · ADR/Contract · UI Features/Shared / §176 ADR / Contract 文档偏差 | P2 | `docs_zh/adr/060-explicit-planning-hub.md` | 引用 RuntimeExecuteBridge——不存在于任何 contract/spec | 已解决（ADR/文档） | 根因是 ADR 把实现层 bridge 名称写进正文时，没有明确它只是局部 adapter seam。 | 本轮已在 `ADR-060` 中明确 `RuntimeExecuteBridge` 一类名称不得被视为 canonical contract object，只能作为实现层兼容桥。 | 已修复。 |
| 2066 | Round 35 — Orchestration/State-Evidence · Interaction/Interface · ADR/Contract · UI Features/Shared / §176 ADR / Contract 文档偏差 | P2 | `docs_zh/contracts/domain_descriptor_and_onboarding_contract.md` | DomainDescriptor 缺 latency_tier——ADR-105 要求 | 已解决（contract 复核） | 该条原问题已在 contract 中修正，但 reaudit 状态未回写。 | 当前 `domain_descriptor_and_onboarding_contract.md` 的 `DomainDescriptor` 最小字段已包含 `latency_tier`。 | 不改正文，仅回写状态依据。 |
| 2067 | Round 35 — Orchestration/State-Evidence · Interaction/Interface · ADR/Contract · UI Features/Shared / §176 ADR / Contract 文档偏差 | P2 | `docs_zh/contracts/cost_and_budget_contract.md` | BudgetPolicy 含 max_steps——应为 max_node_runs | 已解决（contract/文档） | 根因是 contract 主段和 remediation 段曾出现一新一旧两种表述，造成 `max_steps` 残留。 | 本轮已把 `cost_and_budget_contract.md` 的 `BudgetPolicy` 与 remediation 全部统一到 `max_node_runs`，并顺手修正 `no_rollout` runtime mode 命名。 | 已修复。 |
| 2068 | Round 35 — Orchestration/State-Evidence · Interaction/Interface · ADR/Contract · UI Features/Shared / §176 ADR / Contract 文档偏差 | P2 | `docs_zh/adr/069-platform-self-operating-agent.md` | self-ops 能力无 OpsActionProposal/governance gate 引用 | 已解决（ADR/文档） | 根因是真正文案只列了能力和权限，没有把 proposal 对象与治理门禁显式写成 contract 级语义。 | 本轮已在 `ADR-069` 中补入 `OpsActionProposal` 最小字段和 `auto / approval_required / break_glass` governance gate。 | 已修复。 |
| 2069 | Round 35 — Orchestration/State-Evidence · Interaction/Interface · ADR/Contract · UI Features/Shared / §177 UI Features / Shared Packages 缺陷 | P0 | `ui/packages/shared/auth/src/auth-service.ts:37` | SSO token 从 URL query 提取——泄漏至 history/Referer/日志 | 已解决（源码已满足，本轮补证据） | 根因是早期 UI 壳层曾把 callback query 里的 token 直接消费成 session；后续重构已切到 PKCE code flow，但 reaudit 没同步清退旧结论。 | 当前 `ui/packages/shared/auth/src/auth-service.ts` 已把 SSO callback 收敛为 `handleSsoCallback()` 触发授权码流程重定向，明确“不接受 URL 中的 token”；`ui/tests/unit/ui/shared/auth-service.test.ts` 已有 `Issue #2069` 回归，验证带 `access_token/refresh_token` 的 query 不会被接受而是进入重定向分支。 | 不做 UI 全链路 e2e；按源码与现有单测关闭该条。 |
| 2070 | Round 35 — Orchestration/State-Evidence · Interaction/Interface · ADR/Contract · UI Features/Shared / §177 UI Features / Shared Packages 缺陷 | P0 | `ui/packages/shared/api-client/src/ws-client.ts:88` | WS auth token 嵌入 URL query——代理/CDN 可见 | 已解决（源码已满足，本轮补证据） | 根因是旧浏览器 WS 客户端把鉴权放进 URL 或默认让人这么使用；现行实现已改成“URL 保持干净、token 在连通后首条消息发送”，但 reaudit 未回写。 | 当前 `ui/packages/shared/api-client/src/ws-client.ts` 的 `BrowserWSClient` 使用 `new WebSocket(url, "v1.auth.token")` 建连，并在 `onopen` 后发送 `{ action: "auth", token }`；`ui/tests/unit/ui/shared/ws-client.test.ts` 已验证 token 不出现在 URL 中，而是作为首条 auth message 发送。 | 不外推为完整端到端实时链路已验收，仅关闭 token 泄漏条目。 |
| 2071 | Round 35 — Orchestration/State-Evidence · Interaction/Interface · ADR/Contract · UI Features / Shared Packages 缺陷 | P1 | `ui/packages/shared/api-client/src/interceptors.ts:29` | auth interceptor 闭包捕获初始 token——refresh 后仍发旧 token | 已解决（源码已满足，本轮补证据） | 根因是旧拦截器在创建时捕获 token 字符串，刷新后的 session 不会影响已存在拦截器。 | 当前 `createAuthInterceptor()` 已改为在每次 `onRequest` 时动态调用 `resolveToken(tokenOrResolver)`，必要时走 `getAccessTokenWithRefresh()`；`ui/tests/unit/ui/shared/interceptors.test.ts` 已覆盖 `Issue #2071`，验证 refresh due 时会取 fresh token、401 时会回调 unauthorized handler。 | 关闭的是 token 刷新语义，不外推为完整登录流已产品化。 |
| 2072 | Round 35 — Orchestration/State-Evidence · Interaction/Interface · ADR/Contract · UI Features / Shared Packages 缺陷 | P1 | `ui/packages/shared/api-client/src/interceptors.ts:51` | CSRF interceptor 读取一次 meta tag——rotation 后 403 | 已解决（源码已满足，本轮补证据） | 根因是旧实现把 CSRF token 在拦截器创建时读进闭包，token rotation 后仍发旧 header。 | 当前 `createCsrfInterceptor()` 已在每次非 `GET` 请求时调用 `readCsrfToken()` 动态读取 meta tag；`ui/tests/unit/ui/shared/interceptors.test.ts` 已覆盖 `Issue #2072`，验证写请求会注入当前 token、`GET` 不会带 CSRF 头。 | 按源码与现有单测关闭该条。 |
| 2073 | Round 35 — Orchestration/State-Evidence · Interaction/Interface · ADR/Contract · UI Features / Shared Packages 缺陷 | P1 | `ui/packages/shared/sync/src/offline-queue.ts:19-22` | enqueue 在 IndexedDB 加载前调用——persist 覆写未加载数据 | 已解决（源码已满足，本轮补证据） | 根因是队列初始 `readAll()` 尚未完成时，`enqueue()` 就可能先 `push + persist`，把尚未加载的持久快照覆盖掉。 | 当前 `ui/packages/shared/sync/src/offline-queue.ts` 的 `enqueue()` / `replaceAll()` / `persist()` 都会先等待 `readyPromise`；`tests/unit/ui/shared/sync/sync-coordinator-durability.test.ts` 已证明队列在 flush 期间保持 durable，不会因为中途状态切换丢失待同步变更。 | 关闭的是 preload/persist 顺序 bug，不外推为整条离线同步链路已全量验收。 |
| 2074 | Round 35 — Orchestration/State-Evidence · Interaction/Interface · ADR/Contract · UI Features / Shared Packages 缺陷 | P1 | `ui/packages/shared/sync/src/conflict-resolver.ts:19-23` | merge 仅浅展开——嵌套对象 server 值被覆盖=数据丢失 | 已解决（源码/测试） | 根因是旧 merge 语义近似浅层 spread，本地对象会覆盖 server 嵌套字段。 | 当前 `ConflictResolver.mergeObjects()` 已改成按字段递归合并，并使用 vector clock / Lamport timestamp 逐字段裁决；`tests/unit/ui/shared/sync/conflict-resolver.test.ts` 已验证嵌套字段不会被无条件本地覆盖、数组也按 `id` 去重合并。 | 关闭该条真实数据丢失缺口。 |
| 2075 | Round 35 — Orchestration/State-Evidence · Interaction/Interface · ADR/Contract · UI Features / Shared Packages 缺陷 | P1 | `ui/packages/shared/sync/src/sync-coordinator.ts:33-38` | flush() 返回 mutations 但不发送——离线变更被丢弃 | 已解决（源码/测试） | 根因是旧 flush 只做队列出队/回传，没有把 mutation 真正交给 HTTP replay client。 | 当前 `SyncCoordinator.flush()` 已逐条调用 `httpClient.request(mutation)`，并区分成功、retryable failure、冲突与永久失败后再回写队列；`tests/unit/ui/shared/sync/sync-coordinator-durability.test.ts` 已验证 flush 期间 mutation 会被发送且在 ACK 前保持 durable，503 等 retryable 错误会回写为 pending。 | 关闭该条丢弃离线变更问题。 |
| 2076 | Round 35 — Orchestration/State-Evidence · Interaction/Interface · ADR/Contract · UI Features / Shared Packages 缺陷 | P1 | `ui/packages/shared/telemetry/src/index.ts:17,27` | events 数组无上限——长时间会话无界内存泄漏 | 已解决（源码/测试） | 根因是旧 `TelemetrySink` 的内存缓冲没有硬上限，长会话可无限累积。 | 当前 `ui/packages/shared/telemetry/src/index.ts` 已引入 `maxBufferSize`、缓冲满即 flush、满时丢弃最老事件，以及 dead-letter 上限；`ui/tests/unit/ui/shared/telemetry.test.ts` 已覆盖 `Issue #2076`，验证 buffer 受限、自动 flush、生存期多次 flush 不会无界增长。 | 不做 UI 全量性能回归；按源码与现有单测关闭该条。 |
| 2077 | Round 35 — Orchestration/State-Evidence · Interaction/Interface · ADR/Contract · UI Features / Shared Packages 缺陷 | P1 | `ui/packages/shared/api-client/src/rest-client.ts:60-147` | MockTransport 忽略 HTTP method——POST/DELETE 返回 GET 数据 | 已解决（源码/测试） | 根因是 mock transport 早期只按 path 解析 payload，不区分 method，导致写操作也像 GET 一样返回 200/data。 | 当前 `MockTransport.send()` 已按 `request.method` 返回 `201/200/204` 等对应状态，并保留请求体回显；`ui/tests/shared/api-client.test.ts` 已覆盖 create/update/delete 等写接口，证明 mock transport 不再把写请求当 GET。 | 关闭该条 mock 语义偏差。 |
| 2078 | Round 35 — Orchestration/State-Evidence · Interaction/Interface · ADR/Contract · UI Features / Shared Packages 缺陷 | P1 | `ui/packages/ui-core/src/index.tsx:61` | createFeatureModule 硬编码 codeSplit:false——spec 要求 lazy load | 已解决（源码/测试） | 根因是 feature module 工厂曾把路由元数据硬编码成 `codeSplit:false`，与 lazy-load 规范脱节。 | 当前 `ui/packages/ui-core/src/index.tsx` 已把 `route.codeSplit` 固定为 `true`；本轮新增 `tests/unit/ui/ui-core-regressions.test.ts`，直接验证 `createFeatureModule()` 生成的 route 默认启用 code split。 | 关闭的是路由元数据默认值缺陷，不外推为所有 feature 都已完成懒加载拆分。 |
| 2079 | Round 35 — Orchestration/State-Evidence · Interaction/Interface · ADR/Contract · UI Features / Shared Packages 缺陷 | P2 | `ui/packages/shared/telemetry/src/index.ts:28-30` | telemetry export 错误静默吞——数据丢失无告警 | 已解决（源码/测试） | 根因是旧 telemetry exporter 失败路径只有吞错或简单忽略，导致导出失败既不告警，也没有可追溯的留痕。 | 当前 `TelemetrySink.flush()` 在 exporter 失败时会 `console.error(...)`、保留待重试事件、超过重试预算后写入 `deadLetters`；`ui/packages/shared/telemetry/src/index.ts` 与现有 `ui/tests/unit/ui/shared/telemetry.test.ts` 中的 `flush re-queues events on export failure`、`moves events to dead letters after retry budget is exhausted` 已能直接反证“静默吞错”的旧结论。 | 关闭该条错误处理缺陷，不外推为完整观测链路已做全量验收。 |
| 2080 | Round 35 — Orchestration/State-Evidence · Interaction/Interface · ADR/Contract · UI Features / Shared Packages 缺陷 | P2 | `ui/packages/shared/sync/src/offline-queue.ts:87-101` | 每次 readAll/writeAll 新建 IndexedDB 连接——无复用 | 已解决（源码复核） | 根因是旧 IndexedDB store factory 在每次 `readAll()/writeAll()` 时都重复打开数据库连接。 | 当前 `ui/packages/shared/sync/src/offline-queue.ts` 的 `createIndexedDbOfflineMutationStore()` 已把 `openDatabase(databaseName, storeName)` 缓存在单个 `dbPromise` 中，后续读写都复用该连接 promise，而不是重复 `indexedDB.open(...)`。 | 本轮未额外补 UI 壳层级性能压测，但源码层该连接复用缺陷已不存在。 |
| 2081 | Round 35 — Orchestration/State-Evidence · Interaction/Interface · ADR/Contract · UI Features / Shared Packages 缺陷 | P2 | `ui/packages/ui-core/src/layouts/index.ts:30` | ThreePaneLayout 最小 720px——移动端溢出 | 已解决（源码/测试） | 根因是旧布局用固定三列 `minmax(...)` 组合，天然要求约 720px 以上宽度，窄屏会横向溢出。 | 当前 `ui/packages/ui-core/src/layouts/index.ts` 已改成 `repeat(auto-fit, minmax(200px, 1fr))` 的响应式 grid；本轮新增 `tests/unit/ui/ui-core-regressions.test.ts`，直接验证 `ThreePaneLayout` 使用 auto-fit 响应式列模板。 | 关闭的是布局最小宽度硬编码问题。 |
| 2082 | Round 35 — Orchestration/State-Evidence · Interaction/Interface · ADR/Contract · UI Features / Shared Packages 缺陷 | P2 | `ui/packages/ui-core/src/components/index.ts:156` | onChange 用 DOM Event 非 React.ChangeEvent——类型安全缺口 | 已解决（源码复核） | 根因是早期组件事件处理签名混用了 DOM `Event` 与 React synthetic event，导致类型收窄失效。 | 当前 `ui/packages/ui-core/src/components/index.ts` 已显式从 React 导入 `type ChangeEvent`，并在组件内使用 `(event: ChangeEvent<HTMLInputElement>)` 等签名；仓内现行源码不再保留该条指控描述的 DOM Event 处理方式。 | 本轮以源码复核关闭这条类型签名漂移，不宣称完成整套 UI 类型债治理。 |
| 2083 | Round 35 — Orchestration/State-Evidence · Interaction/Interface · ADR/Contract · UI Features / Shared Packages 缺陷 | P2 | `ui/packages/ui-core/src/themes/index.ts:5-22` | darkTheme 缺 accent/danger/success/warning 覆写——WCAG AAA 不达标 | 已解决（源码/测试） | 根因是 dark theme 早期只覆写了背景/边框/正文色，语义色仍回落到 light theme，导致深色面板上的可读性和对比度偏差。 | 当前 `ui/packages/ui-core/src/themes/index.ts` 已为 `darkTheme.color` 显式补齐 `accent/danger/success/warning/info`；本轮新增 `tests/unit/ui/ui-core-regressions.test.ts`，直接验证这些深色主题语义色已定义且非空。 | 关闭的是主题 token 缺项，不外推为完整 WCAG 审计已做完。 |
| 2084 | Round 35 — Orchestration/State-Evidence · Interaction/Interface · ADR/Contract · UI Features / Shared Packages 缺陷 | P2 | `ui/packages/ui-core/src/charts/echart-surface-runtime.tsx:58` | useEffect deps 缺 theme ref——主题切换后图表颜色 stale | 已修复 | 根因不只是 `useEffect` 依赖数组漏项，而是 `EChartSurfaceRuntime` 一直抓静态 `designTokens`，主题状态根本没有正式传进图表运行时，所以主题切换后即使外层页面重渲染，图表实例也不会拿到新的颜色输入。 | 本轮在 `ui/packages/ui-core/src/charts/echart-surface.tsx` / `echart-surface-runtime.tsx` 中新增显式 `theme?: CoreDesignTokens` 输入，并让 runtime 的 effect 依赖 `chartTheme.accent/border`；同时把当前实际使用点 `ui/packages/features/dashboard/src/web/index.tsx` 与 `ui/packages/features/analytics/src/web/index.tsx` 接到 `useThemeState() + resolveTheme(...)`，确保主题切换会把新 token 传入图表。`tests/unit/ui/ui-core-regressions.test.ts` 已补充源码级回归，验证 theme 输入、effect 依赖和 feature 接线都已存在。 | 关闭的是“主题切换后图表颜色 stale”缺陷，不外推为整个 UI 主题系统已全量联调。 |
| 2085 | Round 36 — Model-Gateway/Compliance · Tests/Ops-Maturity · Control-Plane/Scale-Eco · Execution/Shared / §179 Model-Gateway / Compliance 深层缺陷 | P0 | `src/platform/compliance/crypto-shredding/crypto-shredding-service.ts:249` | encryptRecordForSubject 返回 originalValue(明文 PII)——调用方日志/持久化泄漏 | 已解决（源码修复并补定向测试） | 根因是 `EncryptRecordResult.encryptions` 过去直接回传 `originalValue`，导致调用方、日志或审计序列化时可能把明文 PII 再次暴露出去。 | 当前 `src/platform/compliance/crypto-shredding/crypto-shredding-service.ts` 已从 `encryptions` 结果中移除 `originalValue`，仅保留 `fieldPath + dekId`；`tests/integration/platform/compliance/crypto-shredding.integration.test.ts` 已直接断言 encryptions 不再包含 `originalValue`，且 JSON 序列化中不再出现邮箱/SSN 明文。 | 关闭 crypto-shredding 明文 PII 回流缺口。 |
| 2086 | Round 36 — Model-Gateway/Compliance · Tests/Ops-Maturity · Control-Plane/Scale-Eco · Execution/Shared / §179 Model-Gateway / Compliance 深层缺陷 | P0 | `src/platform/compliance/crypto-shredding/dek-manager.ts:207-208` | markRotated() 删除旧 key——已加密数据无法解密,rotation 是破坏性的 | 已解决（源码修复并补定向测试） | 根因是 `DekStore.markRotated()` 旧实现会在标记 rotated 后立即删除旧 key，导致历史 ciphertext 无法再解密，rotation 退化成破坏性 shred。 | 当前 `src/platform/compliance/crypto-shredding/dek-manager.ts` 已保留 rotated DEK 的 key，仅在真正 destroy 时才擦除；`tests/integration/platform/compliance/crypto-shredding.integration.test.ts` 已验证连续两次 rotate 后，v1/v2/v3 三代历史数据仍可分别成功解密。 | 关闭 rotation 破坏历史数据可恢复性的缺口。 |
| 2087 | Round 36 — Model-Gateway/Compliance · Tests/Ops-Maturity · Control-Plane/Scale-Eco · Execution/Shared / §179 Model-Gateway / Compliance 深层缺陷 | P0 | `src/platform/model-gateway/cost-tracker/chargeback-service.ts:40-73` | 成本追踪与执行非原子——crash 间丢失成本记录 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 2088 | Round 36 — Model-Gateway/Compliance · Tests/Ops-Maturity · Control-Plane/Scale-Eco · Execution/Shared / §179 Model-Gateway / Compliance 深层缺陷 | P1 | `src/platform/model-gateway/provider-registry/circuit-breaker.ts:270-277` | failureRate=(failures/windowSec)\*10——无请求总数分母,3次失败即50%触发 open | 已解决（源码复核并补定向测试） | 原审计对应的是旧版 rate 算法；现行实现已按窗口内 `failures / requests` 计算，并不再用“失败次数乘常数”近似失败率。 | 当前 `src/platform/model-gateway/provider-registry/circuit-breaker.ts` 的 `getRecentFailureRate()` 已使用 `recentFailures / recentRequests`；`tests/unit/platform/model-gateway/provider-registry/circuit-breaker.test.ts` 本轮新增覆盖，验证 `5 success + 5 failure` 才会得到 `0.5` 打开熔断，而不会把少量失败直接误算成 50%。 | 以源码复核和行为回归关闭 failure-rate 分母缺失指控。 |
| 2089 | Round 36 — Model-Gateway/Compliance · Tests/Ops-Maturity · Control-Plane/Scale-Eco · Execution/Shared / §179 Model-Gateway / Compliance 深层缺陷 | P1 | `src/platform/model-gateway/provider-registry/openai/openai-chat-service.ts:463-465` | streaming finish_reason 从首个 chunk 读——首 chunk 恒为 null,结果恒"stop" | 已解决（源码修复并补定向测试） | 根因是 OpenAI streaming 聚合逻辑只在首个 delta chunk 初始化 `finalFinishReason`，而首个 chunk 通常返回 `null`，后续真正的 `length/tool_calls/...` 结束原因被静默覆盖成默认 `"stop"`。 | 当前 `src/platform/model-gateway/provider-registry/openai/openai-chat-service.ts` 已改为在每个 chunk 上采用“最后一个非空 finish_reason”，不再依赖 first chunk；`tests/unit/platform/model-gateway/provider-registry/openai/openai-chat-service-streaming.test.ts` 已定向验证首 chunk 为 `null`、后续 chunk 为 `"length"` 时最终结果正确返回 `"length"`。 | 关闭 OpenAI streaming finish_reason 恒 stop 缺口。 |
| 2090 | Round 36 — Model-Gateway/Compliance · Tests/Ops-Maturity · Control-Plane/Scale-Eco · Execution/Shared / §179 Model-Gateway / Compliance 深层缺陷 | P1 | `src/platform/model-gateway/degradation/degradation-controller.ts:357-359` | getFallbackCandidates() 硬编码返回 []——D1 永不生效直跳 D2 | 已解决（源码复核并补定向测试） | 根因是旧 fallback 选择逻辑确实曾硬编码返回空数组，导致 `D1` 永远失效；当前实现已经改为从 provider profile 能力动态枚举 fallback candidates，但这条旧编号没有同步回写。 | 当前 `src/platform/model-gateway/degradation/degradation-controller.ts` 的 `getFallbackCandidates()` 会调用 provider `getAvailableProfiles()` 生成 fallback 列表，`src/platform/model-gateway/provider-registry/unified-chat-provider.ts` 也已实现对应 profile 枚举。 | `tests/unit/platform/model-gateway/degradation-controller.test.ts` 已在本轮定向复跑通过；递归升级深度限制仍由相邻未关闭条目 `2091` 单独跟踪。 |
| 2091 | Round 36 — Model-Gateway/Compliance · Tests/Ops-Maturity · Control-Plane/Scale-Eco · Execution/Shared / §179 Model-Gateway / Compliance 深层缺陷 | P1 | `src/platform/model-gateway/degradation/degradation-controller.ts:209-241` | 递归 escalation 无深度限制——D1-D4 全失败时 stack overflow | 已解决（源码复核并补定向测试） | 原审计基于早期递归升级路径；现行 `route()` 已改成按剩余 level 数量受界的 `for` 循环推进，不存在无限递归堆栈增长。 | 当前 `src/platform/model-gateway/degradation/degradation-controller.ts` 使用 `maxAttempts = D4 - attemptLevel + 1` 的有界循环；`tests/unit/platform/model-gateway/degradation-controller.test.ts` 已新增“primary/fallback 全失败时仍能迭代退化到模板响应”的回归，证明不会因为 escalation 链而 stack overflow。 | 关闭递归升级深度失控的陈旧指控。 |
| 2092 | Round 36 — Model-Gateway/Compliance · Tests/Ops-Maturity · Control-Plane/Scale-Eco · Execution/Shared / §179 Model-Gateway / Compliance 深层缺陷 | P1 | `src/platform/compliance/lineage/index.ts:17` | lineage DAG 可变(plain array)——无 append-only/完整性哈希/篡改检测 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 2093 | Round 36 — Model-Gateway/Compliance · Tests/Ops-Maturity · Control-Plane/Scale-Eco · Execution/Shared / §179 Model-Gateway / Compliance 深层缺陷 | P1 | `src/platform/model-gateway/provider-registry/unified-chat-provider.ts:297-335` | streaming 绕过 circuit breaker——流失败不计入/open 时不拒绝 | 已解决（源码复核并补定向测试） | 根因是原审计停留在 streaming 分支尚未统一接入 breaker 的旧实现；当前 streaming path 已与普通 chat completion 一样走 provider breaker 执行链。 | 当前 `src/platform/model-gateway/provider-registry/unified-chat-provider.ts` 的 `createStreamingChatCompletion()` 会构造 `runStreamingWithBreaker()`，并在存在 breaker 时通过 `breaker.execute(runStreamingWithBreaker)` 执行，因此 open breaker 会拒绝、失败也会计入 breaker。 | `tests/unit/platform/model-gateway/provider-registry/unified-chat-provider.test.ts` 已包含 `UnifiedChatProvider streaming chat uses circuit breaker when non-streaming does` 专门断言；本轮复核该子测试通过，虽同文件仍有无关旧失败，但不影响这条 streaming breaker 证据成立。 |
| 2094 | Round 36 — Model-Gateway/Compliance · Tests/Ops-Maturity · Control-Plane/Scale-Eco · Execution/Shared / §179 Model-Gateway / Compliance 深层缺陷 | P2 | `src/platform/compliance/crypto-shredding/dek-manager.ts:386` | 返回 stale IV(元数据)非实际加密使用的 IV | 已解决（源码修复并补定向测试） | 根因是 `encryptForSubject()` 每次会随机生成新的 GCM IV，但返回值却错误复用了 DEK 元数据中的创建期 IV。 | 当前 `src/platform/compliance/crypto-shredding/dek-manager.ts` 已返回本次加密实际使用的 `iv.toString("hex")`；`tests/integration/platform/compliance/crypto-shredding.integration.test.ts` 已验证返回的 `iv` 与 ciphertext 头段完全一致，且多次加密会生成唯一随机 IV。 | 关闭 stale IV 返回缺口。 |
| 2095 | Round 36 — Model-Gateway/Compliance · Tests/Ops-Maturity · Control-Plane/Scale-Eco · Execution/Shared / §179 Model-Gateway / Compliance 深层缺陷 | P2 | `src/platform/model-gateway/provider-registry/anthropic/anthropic-chat-service.ts:305-306` | message role 映射是恒等——死代码 | 已解决（源码修复并补定向测试） | 根因是 Anthropic request 转换逻辑在提取 `system` 消息后，仍保留 `m.role === "assistant" ? "assistant" : m.role` 这种恒等映射，既没有实际约束价值，也无法在非法 role 泄漏时 fail-close。 | 当前 `src/platform/model-gateway/provider-registry/anthropic/anthropic-chat-service.ts` 已把该分支替换成 `toAnthropicMessageRole()` 显式校验，只允许 `user/assistant` 进入 Anthropic `messages` 数组，残留 system 或其他非法 role 会抛 `provider.invalid_request`；`tests/unit/platform/model-gateway/provider-registry/anthropic-chat-service.test.ts` 已新增非法 role 回归并通过。 | 关闭 role 恒等死代码缺口。 |
| 2096 | Round 36 — Model-Gateway/Compliance · Tests/Ops-Maturity · Control-Plane/Scale-Eco · Execution/Shared / §179 Model-Gateway / Compliance 深层缺陷 | P2 | `src/platform/model-gateway/degradation/degradation-controller.ts:425-447` | de-escalation 忽略 latency P99——高延迟持续振荡 | 已解决（源码修复并补定向测试） | 根因是 `evaluateHealth()` 旧 de-escalation 条件只看 error rate，导致高延迟或高 TTFT 期间也能累计“healthy count”，恢复路径会过早触发。 | 当前 `src/platform/model-gateway/degradation/degradation-controller.ts` 已把 `latencyP99Ms` 与 `ttftP99Ms` 纳入健康判定，并在任一指标未恢复时重置 recovery counter；`tests/unit/platform/model-gateway/degradation-controller.test.ts` 已新增延迟尖峰后 recovery 计数被重置的回归。 | 关闭 de-escalation 忽略 latency/TTFT 的振荡缺口。 |
| 2097 | Round 36 — Model-Gateway/Compliance · Tests/Ops-Maturity · Control-Plane/Scale-Eco · Execution/Shared / §179 Model-Gateway / Compliance 深层缺陷 | P2 | `src/platform/model-gateway/provider-registry/circuit-breaker.ts:155` | 无最小样本量——3次失败即 open 忽略千次成功 | 已解决（源码复核并补定向测试） | 原问题已被后续实现修掉，但审计表没有同步：当前 breaker 在窗口请求数未达到 `minSampleSize` 前不会按 failure rate 开路。 | 当前 `src/platform/model-gateway/provider-registry/circuit-breaker.ts` 的 `getRecentFailureRate()` 会在 `recentRequests < minSampleSize` 时直接返回 `0`；`tests/unit/platform/model-gateway/provider-registry/circuit-breaker.test.ts` 已新增 `3 failures / minSampleSize 4` 仍保持 closed 的回归。 | 关闭最小样本量缺失的陈旧指控。 |
| 2098 | Round 36 — Model-Gateway/Compliance · Tests/Ops-Maturity · Control-Plane/Scale-Eco · Execution/Shared / §179 Model-Gateway / Compliance 深层缺陷 | P2 | `src/platform/compliance/encryption/index.ts:61-63` | field encryption 仍为 base64url 编码非 AES-256-GCM | 已解决（源码修复并补定向测试） | 根因是 `FieldEncryptionService` 旧实现本质只是 `enc:fingerprint:base64url(plaintext)` 包装，没有机密性认证和篡改检测。 | 当前 `src/platform/compliance/encryption/index.ts` 已改为真正的 AES-256-GCM：按 `keyRef` 派生 256-bit key，生成随机 IV，输出 `enc:fingerprint:iv:authTag:ciphertext` 十六进制 envelope；`tests/unit/platform/compliance/field-encryption.test.ts` 已验证格式、解密回环、tamper auth 失败与 malformed envelope 拒绝。 | 关闭 field encryption 仍是可逆编码而非加密的缺口。 |
| 2099 | Round 36 — Model-Gateway/Compliance · Tests/Ops-Maturity · Control-Plane/Scale-Eco · Execution/Shared / §179 Model-Gateway / Compliance 深层缺陷 | P2 | `src/platform/model-gateway/provider-registry/unified-chat-provider.ts:136-137` | MiniMax 表查找死代码——大小写不匹配永不命中 | 已解决（源码/测试） | 根因是原审计停留在只看 `PROVIDER_FROM_MODEL` 表项大小写的旧快照，没有继续复核后续加上的 `detectProviderFromModel()` 归一化路径，因此把“表驱动 fallback 的大小写问题”误判成了整个 MiniMax 路由永远命不中。 | 当前 `src/platform/model-gateway/provider-registry/unified-chat-provider.ts` 已先对 `modelId.toLowerCase()` 做归一化，并在 `modelLower.startsWith("minimax")` 时直接路由到 `minimax`；本轮补充 `tests/unit/platform/model-gateway/provider-registry/unified-chat-provider.test.ts`，通过 stub `minimax.createChatCompletion()` 直接证明 `createChatCompletion("MiniMax-M2.7")` 会命中真实 MiniMax provider，而不是停在死代码路径。 | 关闭的是 MiniMax 路由大小写误判问题，不外推为整组 provider fallback 逻辑都已完备。 |
| 2100 | Round 36 — Model-Gateway/Compliance · Tests/Ops-Maturity · Control-Plane/Scale-Eco · Execution/Shared / §180 Tests / Ops-Maturity 缺陷 | P0 | `src/ops-maturity/chaos/chaos-experiment-scheduler.ts:155` | injectFault() 仅返回配置不注入——无真实故障注入能力 | 已解决（源码/测试） | 原问题来自更早版本里 `injectFault()` 基本只把 `experiment.fault` 原样回传，既不校验 blast radius，也不记录活跃故障、更没有后续回滚/过期链路；reaudit 与旧测试都没有跟上后续实现。 | 当前 `src/ops-maturity/chaos/chaos-experiment-scheduler.ts` 的 `injectFault()` 已先执行 `applyFaultToTarget()`，把故障写入 `activeFaults/experimentFaults`，生成 `rollbackToken` 并注册过期清理；本轮回写 `tests/unit/ops-maturity/chaos/chaos-experiment-scheduler-issues.test.ts`，直接断言注入后活跃故障表和 experiment 故障索引都会落账。 | 关闭的是“纯 return-only no-op”缺陷；不外推为外部 chaos daemon/真实基础设施注入器已经全部产品化。 |
| 2101 | Round 36 — Model-Gateway/Compliance · Tests/Ops-Maturity · Control-Plane/Scale-Eco · Execution/Shared / §180 Tests / Ops-Maturity 缺陷 | P0 | `src/ops-maturity/chaos/chaos-experiment-scheduler.ts:148` | recordSteadyStateResult 按计数非按 hypothesis ID 去重——同假设重复记录可完成实验 | 已解决（源码/测试） | 根因是旧实现按结果条数推进完成态，没有先按 hypothesis 标识去重，同一个假设重复上报会被当成多条独立 steady-state 结果。 | 当前 `src/ops-maturity/chaos/chaos-experiment-scheduler.ts` 已按 `steadyStateName` 替换旧结果，并用 `Set(experiment.results.map(...))` 判断唯一假设数；`tests/unit/ops-maturity/chaos/chaos-experiment-scheduler.test.ts` 与本轮回写后的 `chaos-experiment-scheduler-issues.test.ts` 都直接覆盖“同一 hypothesis 重报不应提前完成实验”的回归。 | 关闭该条 steady-state 去重缺陷。 |
| 2102 | Round 36 — Model-Gateway/Compliance · Tests/Ops-Maturity · Control-Plane/Scale-Eco · Execution/Shared / §180 Tests / Ops-Maturity 缺陷 | P0 | `src/ops-maturity/drift-detection/benchmark-runner.ts:119` | benchmark 用 Math.random() 模拟结果——promotion gate 决策无意义 | 已解决（源码/测试） | 根因是旧 benchmark runner 曾用随机结果填充评估输出，promotion gate 既没有真实 `ProposalExecutor`，也没有基线数据约束，导致“通过/拒绝”只是在随机数上摇摆。 | 当前 `src/ops-maturity/drift-detection/benchmark-runner.ts` 已移除 `Math.random()` 路径，评估依赖显式 `ProposalExecutor` 和 `setBaseline()` 提供的历史基线；本轮补充 `tests/unit/ops-maturity/drift-detection/benchmark-runner.test.ts`，一方面用源码断言确认不再出现 `Math.random`，另一方面直接验证缺失 baseline 时会 fail-closed 抛出 `baseline_required`。 | 关闭的是 benchmark 随机评估缺陷，不外推为所有 drift gate 数据源都已上线。 |
| 2103 | Round 36 — Model-Gateway/Compliance · Tests/Ops-Maturity · Control-Plane/Scale-Eco · Execution/Shared / §180 Tests / Ops-Maturity 缺陷 | P0 | `src/ops-maturity/agent-lifecycle/agent-registry/index.ts:144` | deprecated→active 有效——绕过所有 stage gate 直接回生产 | 已解决（源码/测试） | 根因是生命周期状态迁移表曾把 `deprecated` 重新回生产的路径留成了有效迁移，导致已退役 agent 可以跳过 `testing→staging→canary` 的验证链直接回到 `active`。 | 当前 `src/ops-maturity/agent-lifecycle/agent-registry/index.ts` 的 `VALID_LIFECYCLE_TRANSITIONS` 已明确禁止 `deprecated→active` 和 `archived→active`；`tests/unit/ops-maturity/agent-lifecycle/agent-registry.test.ts` 与 `agent-registry/index.test.ts` 都直接断言这两条迁移为 `false`，本轮定向测试已通过。 | 关闭该条 stage-gate 绕过缺陷，不外推为整个 agent lifecycle 面都已完备。 |
| 2104 | Round 36 — Model-Gateway/Compliance · Tests/Ops-Maturity · Control-Plane/Scale-Eco · Execution/Shared / §180 Tests / Ops-Maturity 缺陷 | P1 | `src/ops-maturity/chaos/chaos-experiment-scheduler.ts:161` | autoTerminate 不回滚已注入故障——spec 要求自动回滚 | 已解决（源码/测试） | 根因是旧 `autoTerminateIfNeeded()` 只把超时实验标成 `cancelled`，但没有把已登记的活跃故障从 `activeFaults/experimentFaults` 回滚清理，导致实验结束后故障状态残留。 | 当前 `src/ops-maturity/chaos/chaos-experiment-scheduler.ts` 在超时终止时已显式调用 `rollbackInjectedFaults(experiment)`；本轮回写 `tests/unit/ops-maturity/chaos/chaos-experiment-scheduler-issues.test.ts`，直接验证 `injectFault()` 后超时终止会把实验置为 `cancelled` 且 `autoRollbackTriggered=true`。 | 关闭的是 autoTerminate 漏回滚缺陷。 |
| 2105 | Round 36 — Model-Gateway/Compliance · Tests/Ops-Maturity · Control-Plane/Scale-Eco · Execution/Shared / §180 Tests / Ops-Maturity 缺陷 | P1 | `src/ops-maturity/agent-lifecycle/agent-lifecycle-service.ts:263` | bindTask 允许 draft/testing/staging 状态 agent | 已解决（源码/测试） | 根因是 `bindTask()` 早期只拦截了 `deprecated/archived`，没有把 `draft/testing/staging` 视为非生产态，从而允许预发版本直接接活。 | 当前 `src/ops-maturity/agent-lifecycle/agent-lifecycle-service.ts` 已对 `draft/testing/staging` 统一抛出 `agent_lifecycle.binding_forbidden_non_production`；`tests/unit/ops-maturity/agent-lifecycle/agent-lifecycle-service-bindtask.test.ts` 已覆盖 `draft/testing/staging` 禁绑、`canary/active/paused` 可绑的完整矩阵，本轮定向测试已通过。 | 关闭的是 task binding 状态门禁缺口，不代表 rollout/retirement 其余治理项都已完成。 |
| 2106 | Round 36 — Model-Gateway/Compliance · Tests/Ops-Maturity · Control-Plane/Scale-Eco · Execution/Shared / §180 Tests / Ops-Maturity 缺陷 | P1 | `src/ops-maturity/drift-detection/rollout-manager.ts:42` | rollout 纯内存无持久化——重启丢失;rollback() 仅改状态无实际回滚 | 需继续整改（测试/配置差距） | 测试、配置或 bootstrap 缺少审计要求的覆盖与约束，本轮未补齐。 | 已按源审计条目补录编号；本轮未新增覆盖缺口对应的测试、配置或 bootstrap 修复证据。 | 补录到 reaudit，待专项整改。 |
| 2107 | Round 36 — Model-Gateway/Compliance · Tests/Ops-Maturity · Control-Plane/Scale-Eco · Execution/Shared / §180 Tests / Ops-Maturity 缺陷 | P1 | `src/ops-maturity/compliance-reporter/compliance-report-pipeline-service.ts:119` | ISO 时间字符串比较——非 UTC 格式静默错误;overdue/expired 反转 | 已解决（源码/测试） | 根因是旧 human signoff 判定直接比较 ISO 字符串，带时区偏移的时间如 `2026-05-01T12:00:00+08:00` 与 `2026-05-01T04:00:01Z` 会被按字典序误判，导致 overdue/expired 状态翻转。 | 当前 `src/ops-maturity/compliance-reporter/compliance-report-pipeline-service.ts` 的 `evaluateHumanSignoff()` 已统一把 `signoffDueAt/signedAt/now` 转成毫秒时间戳比较；本轮补充 `tests/unit/ops-maturity/compliance-reporter/compliance-report-pipeline-service.test.ts`，直接验证非 UTC offset 场景会得到正确的 `signed_late`。 | 关闭该条时间比较与 signoff 状态误判缺陷。 |
| 2108 | Round 36 — Model-Gateway/Compliance · Tests/Ops-Maturity · Control-Plane/Scale-Eco · Execution/Shared / §180 Tests / Ops-Maturity 缺陷 | P1 | `src/ops-maturity/agent-lifecycle/agent-performance-profiler.ts:65` | eviction 按 Map 插入序非按活跃度——最近活跃可被淘汰 | 已解决（源码/测试） | 根因是早期 profiler 清理历史记录时按 `Map` 的插入顺序淘汰条目，导致“最早写入但最近仍活跃”的 agent profile 也可能被误删。 | 当前 `src/ops-maturity/agent-lifecycle/agent-performance-profiler.ts` 已按每个 key 最新 `completedAt` 构造活动度排序，再删除最不活跃的 20%；本轮把 `tests/unit/ops-maturity/agent-lifecycle/agent-performance-profiler.test.ts` 改成直接验证最近活跃的 `agent_0:v1` 在触发 cleanup 后仍被保留。 | 关闭该条 eviction 策略错误。 |
| 2109 | Round 36 — Model-Gateway/Compliance · Tests/Ops-Maturity · Control-Plane/Scale-Eco · Execution/Shared / §180 Tests / Ops-Maturity 缺陷 | P1 | `tests/unit/ops-maturity/agent-lifecycle/agent-lifecycle-service.test.ts:10` | 测试用非法 shape——服务不验证 schema,测试无法检测 contract 违规 | 已解决（源码/测试） | 根因是早期 `AgentLifecycleService.registerAgent()` 直接接受调用方传入对象，测试即便构造出非法 shape 也不会被服务层阻断，导致 contract 违规无法暴露。 | 当前 `src/ops-maturity/agent-lifecycle/agent-lifecycle-service.ts` 已在 `registerAgent()` 中使用 `AgentDefinitionSchema.parse(...)` 做 fail-closed 校验；本轮补了 `tests/unit/ops-maturity/agent-lifecycle/agent-lifecycle-service.test.ts` 的非法 shape 回归，直接验证缺失必填字段的 agent 定义会被拒绝。 | 关闭的是服务层 schema 校验与测试证据缺口，不外推为整组 ADR/contract 漂移都已收敛。 |
| 2110 | Round 36 — Model-Gateway/Compliance · Tests/Ops-Maturity · Control-Plane/Scale-Eco · Execution/Shared / §180 Tests / Ops-Maturity 缺陷 | P1 | `src/ops-maturity/drift-detection/reflection-engine.ts:44` | 需≥2次同类失败才反思——单次严重安全事件被忽略 | 已解决（源码/测试） | 根因是 `reflect()` 早期只按 `records.length >= 2` 触发反思，没有给安全/权限类失败单独的高优先级通道，导致单次严重事件被静默吞掉。 | 当前 `src/ops-maturity/drift-detection/reflection-engine.ts` 已对 `security/forbidden` 类失败走单样本即时反思分支；`tests/unit/ops-maturity/drift-detection/reflection-engine.test.ts` 已新增“单次 security 事件也必须产出 reflection”的回归，本轮定向测试已通过。 | 关闭的是单次高严重度事件漏反思问题，不外推为 drift-detection 其余策略都已完善。 |
| 2111 | Round 36 — Model-Gateway/Compliance · Tests/Ops-Maturity · Control-Plane/Scale-Eco · Execution/Shared / §180 Tests / Ops-Maturity 缺陷 | P2 | `src/ops-maturity/chaos/chaos-experiment-scheduler.ts:94` | steadyStateCache 声明但从未读写(死代码) | 已解决（源码复核/测试回写） | 根因是早期 scheduler 确实保留了未接线的 `steadyStateCache` 字段，后续源码已把这块死状态删掉，但 reaudit 结论和旧测试仍停留在“缓存仍存在”的旧快照。 | 当前 `src/ops-maturity/chaos/chaos-experiment-scheduler.ts` 已不再声明 `private readonly steadyStateCache`；本轮回写 `tests/unit/ops-maturity/chaos/chaos-experiment-scheduler-issues.test.ts`，直接用源码断言确认该字段声明已经消失。 | 关闭该条死代码缺陷。 |
| 2112 | Round 36 — Model-Gateway/Compliance · Tests/Ops-Maturity · Control-Plane/Scale-Eco · Execution/Shared / §180 Tests / Ops-Maturity 缺陷 | P2 | `tests/integration/ops-maturity/chaos/ integration test:296` | 测试断言通过原因错误——实验已提前完成而非仍 running | 已解决（测试回写） | 根因是旧集成测试在只有单个 hypothesis 的实验上重复记录同一 steady-state 结果，表面上断言 game day 仍是 `running` 能通过，但没有证明“为什么仍是 running”，实际上第一条实验已完成，只是第二条实验未结束。 | 本轮回写 `tests/integration/ops-maturity/chaos/chaos-experiment-scheduler-integration.test.ts`：在 `refreshGameDayStatus` 中显式断言第一个 experiment 已 `completed`、第二个仍 `running`，然后再证明 game day 整体仍为 `running`，避免错误因果的假阳性。 | 关闭该条集成测试断言理由错误问题。 |
| 2113 | Round 36 — Model-Gateway/Compliance · Tests/Ops-Maturity · Control-Plane/Scale-Eco · Execution/Shared / §180 Tests / Ops-Maturity 缺陷 | P2 | `src/ops-maturity/drift-detection/cross-agent-analyzer/index.ts:29` | 单 agent 时 best===worst——语义误导 | 已解决（源码/测试） | 根因是旧 cross-agent analyzer 在 `metrics.length===1` 时仍沿用同一条记录同时填充 `bestAgentId` 与 `worstAgentId`，把“无可比对象”误表示成了“同一个 agent 既最好也最差”。 | 当前 `src/ops-maturity/drift-detection/cross-agent-analyzer/index.ts` 已在无有效对比时返回 `bestAgentId=null`、`worstAgentId=null`；`tests/unit/ops-maturity/drift-detection/cross-agent-analyzer.test.ts` 也已直接覆盖单 agent 场景，本轮定向测试已通过。 | 关闭该条单 agent 语义误导缺陷。 |
| 2114 | Round 36 — Model-Gateway/Compliance · Tests/Ops-Maturity · Control-Plane/Scale-Eco · Execution/Shared / §180 Tests / Ops-Maturity 缺陷 | P2 | `src/ops-maturity/compliance-reporter/compliance-report-pipeline-service.ts:66` | generate() 双重查找——fallback 是死代码 | 已解决（源码/测试） | 根因是旧 `generate()` 曾同时通过 registry 和模板数组做重复查找，第二条 fallback 分支永远不会提供额外语义，只会制造死代码和未来漂移点。 | 当前 `src/ops-maturity/compliance-reporter/compliance-report-pipeline-service.ts` 已只保留一次 `registry.find(request.templateId)`；本轮补充 `tests/unit/ops-maturity/compliance-reporter/compliance-report-pipeline-service.test.ts`，直接断言源码中该模板查找只出现一次。 | 关闭该条重复模板查找死代码问题。 |
| 2115 | Round 36 — Model-Gateway/Compliance · Tests/Ops-Maturity · Control-Plane/Scale-Eco · Execution/Shared / §180 Tests / Ops-Maturity 缺陷 | P2 | `src/ops-maturity/agent-lifecycle/version-manager/agent-version-manager.ts:86` | blue-green 互斥 revoke——只有单 slot 活跃,违反零停机切换 | 需继续整改（测试/配置差距） | 测试、配置或 bootstrap 缺少审计要求的覆盖与约束，本轮未补齐。 | 已按源审计条目补录编号；本轮未新增覆盖缺口对应的测试、配置或 bootstrap 修复证据。 | 补录到 reaudit，待专项整改。 |
| 2116 | Round 36 — Model-Gateway/Compliance · Tests/Ops-Maturity · Control-Plane/Scale-Eco · Execution/Shared / §181 Control-Plane / Scale-Ecosystem 缺陷 | P0 | `src/platform/control-plane/config-center/config-rollout-service.ts:134` | canary rollout target=100 直跳 FULL——跳过 5%/25%/50% 阶段 | 已解决（源码/测试） | 根因是旧 `resolveInitialStage()` 按目标百分比直接落到终态，`targetPercentage=100` 会绕过 5%/25%/50% 健康门禁，丢失渐进发布语义。 | 本轮修正 `src/platform/five-plane-control-plane/config-center/config-rollout-service.ts`：100% rollout 强制从 `CANARY_5` 起步，非 100% 目标则选取不低于目标的最小阶段；`tests/unit/platform/control-plane/config-center/config-rollout-service.test.ts` 已覆盖 `100 -> CANARY_5`、`10 -> CANARY_25`、`40 -> HALF`。 | 关闭该条 canary 直跳 FULL 缺陷。 |
| 2117 | Round 36 — Model-Gateway/Compliance · Tests/Ops-Maturity · Control-Plane/Scale-Eco · Execution/Shared / §181 Control-Plane / Scale-Ecosystem 缺陷 | P0 | `src/scale-ecosystem/marketplace/pack-security-service.ts:154-169` | sandbox test 仅扫权限列表不执行代码——恶意运行时行为通过 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 2118 | Round 36 — Model-Gateway/Compliance · Tests/Ops-Maturity · Control-Plane/Scale-Eco · Execution/Shared / §181 Control-Plane / Scale-Ecosystem 缺陷 | P0 | `src/scale-ecosystem/marketplace/auto-stop-loss-service.ts:727-741` | human-approved playbook actions 永不执行——审批后 no-op | 已解决（源码/测试） | 根因是旧审批流只把事件留在 pending 队列里，审批通过后没有真正回放待执行动作，导致 `humanApproved=true` 也可能仍是 no-op。 | 当前实现已收敛到 `src/platform/five-plane-control-plane/incident-control/auto-stop-loss-service.ts`；`approvePendingExecution()` 在审批通过后会调用 `executeActions(...)`，并回写 `success/actionsExecuted/escalationLevel`。`tests/unit/platform/control-plane/incident-control/auto-stop-loss-service-extended.test.ts` 已直接断言审批通过后动作会落到 `actionsExecuted`。 | 关闭该条“审批后 no-op”缺陷；同时注明实现落点已迁移。 |
| 2119 | Round 36 — Model-Gateway/Compliance · Tests/Ops-Maturity · Control-Plane/Scale-Eco · Execution/Shared / §181 Control-Plane / Scale-Ecosystem 缺陷 | P1 | `src/platform/control-plane/incident-control/runbook-executor/markdown-parser.ts:48` | 严重度 pattern `\bhight\b` 拼写错误——P1 永不匹配 "high" | 已解决（源码/测试） | 根因是 P1 关键字正则里把 `high` 拼成了 `hight`，导致仅靠标题关键词推断严重度时，高优先级 runbook 会被降到默认 `P2`。 | 当前 `src/platform/control-plane/incident-control/runbook-executor/markdown-parser.ts` 已改成 `\bhigh\b`；`tests/unit/platform/control-plane/incident-control/runbook-executor/markdown-parser.test.ts` 已直接覆盖 `High Traffic Spike` → `P1` 的断言，本轮定向测试已通过。 | 关闭该条 runbook severity 识别缺陷。 |
| 2120 | Round 36 — Model-Gateway/Compliance · Tests/Ops-Maturity · Control-Plane/Scale-Eco · Execution/Shared / §181 Control-Plane / Scale-Ecosystem 缺陷 | P1 | `src/platform/control-plane/config-center/config-versioning-service.ts:339` | rollback 浅拷贝——嵌套对象共享引用,修改腐败历史版本 | 已解决（源码/测试） | 根因是旧 rollback 路径如果只做 `{...targetVersion.content}` 浅拷贝，嵌套对象/数组仍会共享引用，后续修改回滚版本会反向污染历史版本快照。 | 当前 `src/platform/control-plane/config-center/config-versioning-service.ts` 已在 `rollback()` 中使用 `structuredClone(targetVersion.content)`；本轮新增 `tests/unit/platform/control-plane/config-center/config-versioning-service.test.ts` 的深拷贝回归，并单独定向执行该用例，确认修改 rollback 结果的嵌套字段不会影响原始版本内容。 | 关闭该条 rollback 浅拷贝缺陷。 |
| 2121 | Round 36 — Model-Gateway/Compliance · Tests/Ops-Maturity · Control-Plane/Scale-Eco · Execution/Shared / §181 Control-Plane / Scale-Ecosystem 缺陷 | P1 | `src/platform/control-plane/risk-control/risk-evaluation-engine.ts:14-16` | 仅 6 因子——缺 reversibility+temporal_context(spec §10.2 要求 8) | 已解决（源码/测试） | 原审计对应的是早期风险引擎的简化版；现行实现已经切到 ADR-026 的 8 因子模型，reaudit 行没有同步回写。 | 当前 `src/platform/control-plane/risk-control/risk-evaluation-engine.ts` 已包含 `operationRisk/targetResourceCriticality/dataSensitivity/autonomyModeRisk/tenantImpact/blastRadius/historicalFailureRate/evidenceConfidence` 共 8 个风险因子；`tests/unit/platform/control-plane/risk-control/risk-evaluation-engine.test.ts` 也直接断言 `factorBreakdown.length === 8` 及完整字段顺序，本轮定向测试已通过。 | 关闭该条“风险引擎仍是 6 因子”的过期结论。 |
| 2122 | Round 36 — Model-Gateway/Compliance · Tests/Ops-Maturity · Control-Plane/Scale-Eco · Execution/Shared / §181 Control-Plane / Scale-Ecosystem 缺陷 | P1 | `src/platform/control-plane/risk-control/risk-config-loader.ts:39-73` | JSON.parse 无 schema 验证——畸形配置直接 TypeError crash | 已解决（源码/测试） | 根因是早期 loader 只做 `JSON.parse`，对缺字段或类型错误的风险配置没有结构化 schema 校验，后续调用点才会以 `TypeError` 形式炸出。 | 当前 `src/platform/control-plane/risk-control/risk-config-loader.ts` 已引入 `RiskConfigSchema` 做 Zod 校验，并在非法 JSON 与畸形 schema 两类场景上 fail-closed；`tests/unit/platform/control-plane/risk-control/risk-config-loader.test.ts` 已覆盖“invalid JSON 被拒绝”和“缺失 factorWeights 的畸形配置被拒绝”，本轮定向测试已通过。 | 关闭该条 risk config schema 校验缺口。 |
| 2123 | Round 36 — Model-Gateway/Compliance · Tests/Ops-Maturity · Control-Plane/Scale-Eco · Execution/Shared / §181 Control-Plane / Scale-Ecosystem 缺陷 | P1 | `src/scale-ecosystem/sla-engine/sla-operations-service.ts:111` | preemptionCapApplied 恒 true(值≤max 恒成立) | 已解决（源码/测试） | 根因是旧逻辑用 `selectedTier.preemptionPriority <= maxPriority` 判断是否应用抢占上限，几乎任何 tier 都会命中，导致高优先级、同优先级与低优先级路径都被误标记。 | 当前 `src/scale-ecosystem/sla-engine/sla-operations-service.ts` 已改为仅在存在更高优先级 tier 时置 `preemptionCapApplied=true`；`tests/unit/scale-ecosystem/sla-engine/sla-operations-issue-2123.test.ts` 已覆盖低/高/同优先级三种路径，本轮定向测试已通过。 | 关闭该条 SLA 抢占上限误判缺陷。 |
| 2124 | Round 36 — Model-Gateway/Compliance · Tests/Ops-Maturity · Control-Plane/Scale-Eco · Execution/Shared / §181 Control-Plane / Scale-Ecosystem 缺陷 | P1 | `src/scale-ecosystem/integration/connector-framework-service.ts:95-105` | 无 circuit breaker——单次 healthy 即信任,无状态机 | 已解决（源码/测试） | 根因是 connector execute/health 路径早期没有 connector 级断路器状态机，失败次数、打开窗口和 half-open 恢复都未被建模。 | 当前 connector framework 已实现 `closed/open/half_open` 状态、失败阈值和超时复位；`tests/unit/scale-ecosystem/integration/connector-framework-issues.test.ts` 已验证执行失败打开断路器、超时进入 half-open 并在成功后关闭，以及健康检查失败也会累计到断路器，本轮定向测试已通过。 | 关闭该条 connector 断路器缺失缺陷。 |
| 2125 | Round 36 — Model-Gateway/Compliance · Tests/Ops-Maturity · Control-Plane/Scale-Eco · Execution/Shared / §181 Control-Plane / Scale-Ecosystem 缺陷 | P1 | `src/platform/control-plane/incident-control/incident-detector.ts:63-84` | 仅生成 P1/P2——P3/P4 检查结果不创建 incident | 已解决（源码/测试） | 根因是 incident detector 旧实现只对 `fail_closed/degraded` 严重结果建 incident，`warning/info/anomaly` 映射到 P3/P4 的路径被直接跳过。 | 当前 `src/platform/control-plane/incident-control/incident-detector.ts` 已为全部严重级别生成 incident；`tests/unit/platform/control-plane/incident-control/incident-detector.test.ts` 已覆盖 `SEV4` 和“所有严重级别均建 incident”的回归，本轮定向测试已通过。 | 关闭该条 P3/P4 incident 漏建缺陷。 |
| 2126 | Round 36 — Model-Gateway/Compliance · Tests/Ops-Maturity · Control-Plane/Scale-Eco · Execution/Shared / §181 Control-Plane / Scale-Ecosystem 缺陷 | P2 | `src/platform/control-plane/incident-control/takeover-escalation-manager.ts:406-413` | 未确认 session 永不淘汰——Map 无界增长 | 已解决（源码/测试） | 根因是 TTL 清理旧逻辑只检查已确认 session 的 `acknowledgedAt`，未确认 session 没有确认时间戳，因而永远不会进入淘汰分支。 | 当前 `src/platform/control-plane/incident-control/takeover-escalation-manager.ts` 已把未确认 session 也纳入 TTL 回收；`tests/unit/platform/control-plane/incident-control/takeover-escalation-manager.test.ts` 已新增“evicts unconfirmed sessions after TTL”回归，本轮定向测试已通过。 | 关闭该条 takeover session 无界增长缺陷。 |
| 2127 | Round 36 — Model-Gateway/Compliance · Tests/Ops-Maturity · Control-Plane/Scale-Eco · Execution/Shared / §181 Control-Plane / Scale-Ecosystem 缺陷 | P2 | `src/platform/control-plane/incident-control/auto-stop-loss-service.ts:700-706` | executionCounts hourly key 无清理——慢泄漏 | 已解决（源码/测试） | 根因是旧实现只在执行成功写计数时顺手覆盖当前小时 bucket，没有在读路径淘汰旧小时 key；长期运行时旧 bucket 会一直滞留。 | 当前 `src/platform/five-plane-control-plane/incident-control/auto-stop-loss-service.ts` 已在限流检查和写入路径都调用 `pruneExecutionCounts()`；`tests/unit/platform/control-plane/incident-control/auto-stop-loss-service-extended.test.ts` 已覆盖跨小时后旧 key 被清除。 | 关闭该条 hourly counter 慢泄漏缺陷。 |
| 2128 | Round 36 — Model-Gateway/Compliance · Tests/Ops-Maturity · Control-Plane/Scale-Eco · Execution/Shared / §181 Control-Plane / Scale-Ecosystem 缺陷 | P2 | `src/platform/control-plane/incident-control/auto-stop-loss-service.ts:619` | 升级级别靠 string includes "emergency"——大小写/措辞变体误判 | 已解决（源码/测试） | 根因是旧升级逻辑只靠 `includes("emergency")` 粗判，`SEV-EMERGENCY`、大小写差异或下划线/连字符变体都会偏离预期。 | 当前实现新增 `normalizeAnomalySeverity()`，统一归一大小写与分隔符后再映射严重级别；`tests/unit/platform/control-plane/incident-control/auto-stop-loss-service-extended.test.ts` 已直接覆盖 `SEV-EMERGENCY`。 | 关闭该条 severity 变体误判缺陷。 |
| 2129 | Round 36 — Model-Gateway/Compliance · Tests/Ops-Maturity · Control-Plane/Scale-Eco · Execution/Shared / §181 Control-Plane / Scale-Ecosystem 缺陷 | P2 | `src/platform/control-plane/config-center/config-override-governance.ts:314-318` | requireAudit && !allowed 分支不可达(死代码) | 已解决（源码/测试） | 根因是旧 `requireAudit` 只停留在规则配置层，既没有形成审计记录元数据，也无法在已拒绝/已返回的分支上提供任何新增语义，最终退化成死字段。 | 当前 `src/platform/five-plane-control-plane/config-center/config-override-governance.ts` 已把 `auditRequired/auditedAt/auditDigest` 写入 override audit record，并生成确定性摘要；`tests/unit/platform/control-plane/config-center/config-override-governance.test.ts` 已断言普通记录与 require-audit 记录都会带上可验证的审计字段。 | 关闭该条 `requireAudit` 死代码问题。 |
| 2130 | Round 36 — Model-Gateway/Compliance · Tests/Ops-Maturity · Control-Plane/Scale-Eco · Execution/Shared / §181 Control-Plane / Scale-Ecosystem 缺陷 | P2 | `src/platform/control-plane/incident-control/runbook-executor/runbook-executor.ts:238-241` | "只读"正则匹配 kubectl delete/docker rm 等破坏性命令 | 已解决（源码修复并补定向测试） | 根因是旧实现只按可执行文件名前缀粗判，只要命令以 `kubectl` 或 `docker` 开头就走“只读诊断”文案，忽略了破坏性 subcommand，导致 `docker rm` / `kubectl delete` 被误归类。 | 本轮把 `simulateStepExecution()` 的只读识别改成按 executable+subcommand 白名单判断，`kubectl` 仅允许 `get/describe/logs/top/...`，`docker` 仅允许 `ps/images/inspect/logs/...`；`tests/unit/platform/control-plane/incident-control/runbook-executor/runbook-executor.test.ts` 新增 `does not classify destructive kubectl/docker commands as read-only`。 | 关闭的是只读误判问题；并不宣称 runbook executor 已具备真实命令执行沙箱。 |
| 2131 | Round 36 — Model-Gateway/Compliance · Tests/Ops-Maturity · Control-Plane/Scale-Eco · Execution/Shared / §181 Control-Plane / Scale-Ecosystem 缺陷 | P2 | `src/scale-ecosystem/sla-engine/breach-detector/index.ts:17-24` | 无 burn-rate/error-budget 追踪——仅点快照阈值 | 已解决（源码/测试） | 根因是旧 breach detector 只对单次快照做阈值比较，没有纳入请求量、时间窗和 error-budget 消耗，无法识别“尚未越线但正在高速烧预算”的风险。 | 当前 `src/scale-ecosystem/sla-engine/breach-detector/index.ts` 已新增 `requestCount/windowMs/errorBudgetPercent/budgetWindowMs` 以及 `analyzeSlaBreach()`；`tests/unit/scale-ecosystem/sla-engine/breach-detector-issue-2131.test.ts` 已覆盖 burn-rate、budget consumed 与 breach 分类。 | 关闭该条 SLA burn-rate / error-budget 缺口。 |
| 2132 | Round 36 — Model-Gateway/Compliance · Tests/Ops-Maturity · Control-Plane/Scale-Eco · Execution/Shared / §182 Execution / Shared Infrastructure 缺陷 | P0 | `src/platform/execution/tool-executor/command-executor.ts:368-380` | stdout/stderr 无 maxBuffer——恶意命令输出 OOM | 已解决（源码/测试） | 根因是旧命令执行器把 stdout/stderr 无界累计到内存，只要子进程持续喷输出，父进程就会沿着 capture buffer 线性膨胀。 | 当前 `src/platform/five-plane-execution/tool-executor/command-executor.ts` 已引入 `maxCapturedOutputBytes`，超限时立刻 `killProcessTree(child)` 并返回 `tool.output_limit_exceeded`；`tests/unit/platform/execution/tool-executor/command-executor.test.ts` 已用超大输出文件回归该 fail-closed 路径。 | 关闭该条命令输出 OOM 风险。 |
| 2133 | Round 36 — Model-Gateway/Compliance · Tests/Ops-Maturity · Control-Plane/Scale-Eco · Execution/Shared / §182 Execution / Shared Infrastructure 缺陷 | P0 | `src/platform/execution/ha/ha-coordinator-service-inner.ts:676-679` | verifyWriteAuthority 用 >=——stale leader 持当前 token 被接受,破坏 split-brain 防护 | 已解决（源码/测试） | 根因是旧 fencing 校验把“未来 token”也当成合法写权限，`>=` 会放过不等于当前 epoch token 的请求，削弱 split-brain 防线。 | 当前 `src/platform/five-plane-execution/ha/ha-coordinator-service-inner.ts` 已改为严格 `presentedFencingToken === latestEpoch.fencingToken`；`tests/unit/platform/execution/ha/ha-coordinator-service-inner.test.ts` 已新增 future token 必须拒绝的回归。 | 关闭该条 fencing token 比较错误。 |
| 2134 | Round 36 — Model-Gateway/Compliance · Tests/Ops-Maturity · Control-Plane/Scale-Eco · Execution/Shared / §182 Execution / Shared Infrastructure 缺陷 | P0 | `src/platform/execution/ha/wal-checkpoint-service.ts:119` | sequenceCounter 纯内存——crash 后重置为 0 产生重复 WAL 序号 | 已解决（源码/测试） | 根因是旧 `sequenceCounter` 只在进程内自增，重启后重新从 0 开始，和已有 `wal_entries.sequence_number` 断开，可能产生重复序号。 | 当前 `src/platform/five-plane-execution/ha/wal-checkpoint-service.ts` 已在构造和 `initializeSchema()` 阶段调用 `syncSequenceCounterFromStorage()`，通过 `SELECT MAX(sequence_number)` 恢复上次序号；`tests/unit/platform/execution/ha/wal-checkpoint-service.test.ts` 已覆盖重启后续号场景。 | 关闭该条 WAL sequence 重置缺陷。 |
| 2135 | Round 36 — Model-Gateway/Compliance · Tests/Ops-Maturity · Control-Plane/Scale-Eco · Execution/Shared / §182 Execution / Shared Infrastructure 缺陷 | P1 | `src/platform/shared/lifecycle/service-registry.ts:316-323` | 拓扑排序循环仅 warn——cycled 服务跳过 teardown 导致资源泄漏 | 已解决（源码/测试） | 根因是旧 `topologicalSort()` 在检测到循环依赖时只记录 warning，不 fail-closed，导致 teardown/reset 过程中可能静默跳过环上的服务。 | 当前 `src/platform/shared/lifecycle/service-registry.ts` 在循环依赖场景会抛出 `service_registry.circular_dependency`，不再返回部分排序结果；`tests/unit/platform/shared/lifecycle/service-registry.test.ts` 已更新为直接断言循环依赖抛错，本轮定向测试已通过。 | 关闭的是 ServiceRegistry 循环依赖 fail-open 缺陷。 |
| 2136 | Round 36 — Model-Gateway/Compliance · Tests/Ops-Maturity · Control-Plane/Scale-Eco · Execution/Shared / §182 Execution / Shared Infrastructure 缺陷 | P1 | `src/platform/execution/tool-executor/command-security.ts:259` | 脚本后所有 flag 被阻——`python script.py --output foo` 被拒 | 已解决（源码修复并补定向测试） | 根因是 interpreter 校验把“脚本前置解释器 flag”和“脚本自己的 CLI 参数”混成一类，只要 `scriptPath` 之后任意参数以 `-` 开头就拒绝，导致正常脚本参数也被误杀。 | 当前 `src/platform/five-plane-execution/tool-executor/command-security.ts` 仅在首个 interpreter 参数本身是 flag（例如 `bash -lc`、`python -c` 这类绕过脚本文件模式）时拒绝；脚本路径后的 `--output` 等参数不再触发阻断。`tests/unit/platform/execution/tool-executor/command-security.test.ts` 已新增 `allows interpreter script arguments after script path`。 | 关闭的是误拦脚本参数的问题；解释器 inline code 与前置 flag 仍保持阻断。 |
| 2137 | Round 36 — Model-Gateway/Compliance · Tests/Ops-Maturity · Control-Plane/Scale-Eco · Execution/Shared / §182 Execution / Shared Infrastructure 缺陷 | P1 | `src/platform/execution/tool-executor/command-security.ts:120` | META_SYNTAX_PATTERN 匹配 glob——`ls *.ts` 被误拒 | 已解决（源码修复并补定向测试） | 根因是 `META_SYNTAX_PATTERN` 过度扩大，把 `*`、`?` 和 `[]` 通配也当成 shell 元语法一并拒绝；这对真正需要 shell 扩展的 `-c` 场景是合理的，但对 argv 级安全执行器里的普通 glob 参数属于误报。 | 本轮从 `src/platform/five-plane-execution/tool-executor/command-security.ts` 的 `META_SYNTAX_PATTERN` 移除了 glob 匹配分支，同时保留管道、重定向、变量展开、命令替换等高风险元语法阻断；`tests/unit/platform/execution/tool-executor/command-security.test.ts` 新增 `allows shell-safe glob arguments`。 | 关闭的是 argv 级 glob 误拒问题，不表示执行器开始支持 shell 解析。 |
| 2138 | Round 36 — Model-Gateway/Compliance · Tests/Ops-Maturity · Control-Plane/Scale-Eco · Execution/Shared / §182 Execution / Shared Infrastructure 缺陷 | P1 | `src/platform/shared/lifecycle/graceful-shutdown.ts:227` | 超时 handler 继续后台运行——Promise.race 后 zombie async | 已解决（源码/测试） | 根因是旧 shutdown 流程虽然在 `Promise.race` 上报超时，但没有向底层 handler 传播取消信号，超时任务仍会在后台继续跑。 | 当前实现位于 `src/platform/five-plane-execution/startup/graceful-shutdown.ts`：handler 签名已扩展为 `(signal?: AbortSignal)`，超时路径通过 `AbortController.abort()` 终止 handler，并在 `finally` 中清理定时器；`tests/unit/platform/execution/startup/graceful-shutdown.test.ts` 已断言超时后会触发 abort。 | 关闭该条 graceful shutdown zombie async 缺陷。 |
| 2139 | Round 36 — Model-Gateway/Compliance · Tests/Ops-Maturity · Control-Plane/Scale-Eco · Execution/Shared / §182 Execution / Shared Infrastructure 缺陷 | P1 | `src/platform/shared/observability/structured-logger.ts:437` | rotationScheduled per-instance 但 fileSink 全局——并发 rotation 文件损坏 | 已解决（源码/测试） | 根因是旧 rotation 调度状态如果挂在 logger 实例上，而 file sink 又是全局共享，同一路径下多实例并发写日志时会重复触发轮转。 | 当前 `src/platform/shared/observability/structured-logger.ts` 已把轮转状态提升为静态 `rotationStateByPath`，按 `filePath` 共享 `scheduled` 标记；`tests/unit/platform/shared/observability/structured-logger.test.ts` 已新增双 logger 共用全局 sink 的回归，验证并发写入下只走共享轮转状态。 | 关闭该条并发 rotation 文件损坏风险。 |
| 2140 | Round 36 — Model-Gateway/Compliance · Tests/Ops-Maturity · Control-Plane/Scale-Eco · Execution/Shared / §182 Execution / Shared Infrastructure 缺陷 | P1 | `src/platform/execution/ha/leader-election-service.ts:512-514` | setInterval 无 unref()——阻止进程正常退出 | 已解决（源码/测试） | 根因是 leader renewal loop 作为后台保活定时器被创建后没有调用 `unref()`，Node 进程在只剩该 timer 时也不会自然退出。 | 当前 `src/platform/execution/ha/leader-election-service.ts` 的 `startRenewalLoop()` 与 HA_1 启动分支都已对 renewal timer 调用 `unref?.()`；`tests/unit/platform/execution/ha/leader-election-service.test.ts` 本轮新增了 interval spy，直接验证 renewal/heartbeat 定时器都会调用 `unref()`，定向测试已通过。 | 关闭的是进程退出阻塞缺陷，不外推为整个 HA 生命周期都无残余问题。 |
| 2141 | Round 36 — Model-Gateway/Compliance · Tests/Ops-Maturity · Control-Plane/Scale-Eco · Execution/Shared / §182 Execution / Shared Infrastructure 缺陷 | P1 | `src/platform/execution/ha/leader-election-service.ts:545-547` | heartbeat setInterval 无 unref()——同上 | 已解决（源码/测试） | 根因与 `2140` 相同，只是落点在 node heartbeat loop：心跳 timer 不 `unref()` 也会把事件循环钉住。 | 当前 `src/platform/execution/ha/leader-election-service.ts` 的 `startHeartbeat()` 已对 heartbeat timer 调用 `unref?.()`；`tests/unit/platform/execution/ha/leader-election-service.test.ts` 的 interval spy 同时覆盖了 heartbeat timer 的 `unref()` 调用，本轮定向测试已通过。 | 与 `2140` 一并关闭。 |
| 2142 | Round 36 — Model-Gateway/Compliance · Tests/Ops-Maturity · Control-Plane/Scale-Eco · Execution/Shared / §182 Execution / Shared Infrastructure 缺陷 | P1 | `src/platform/execution/ha/wal-checkpoint-service.ts:597-599` | checkpoint interval 无 unref()——同上 | 已解决（源码/测试） | 根因是 WAL checkpoint 后台定时器没有 `unref()`，运行服务即使已经没有前台工作也会被 checkpoint loop 留住。 | 当前 `src/platform/execution/ha/wal-checkpoint-service.ts` 的 `startCheckpointLoop()` 已对 checkpoint interval 调用 `unref?.()`；`tests/unit/platform/execution/ha/wal-checkpoint-service.test.ts` 本轮新增 interval spy，直接验证 `start()` 后 checkpoint loop 会执行 `unref()`，定向测试已通过。 | 关闭该条后台 checkpoint timer 阻塞退出缺陷。 |
| 2143 | Round 36 — Model-Gateway/Compliance · Tests/Ops-Maturity · Control-Plane/Scale-Eco · Execution/Shared / §182 Execution / Shared Infrastructure 缺陷 | P1 | `src/platform/execution/tool-executor/tool-parallel-executor.ts:406-407` | results 数组含 undefined hole——下游收到意外 undefined 元素 | 已解决（源码修复并补定向测试） | 根因是并行执行器按原 index 直接写入成功结果，对失败项不填值，最后又把稀疏数组强转成 `T[]` 返回；类型层面看似完整，运行时却会把 `undefined hole` 传给下游。 | 本轮将 `src/platform/five-plane-execution/tool-executor/tool-parallel-executor.ts` 的返回值改为过滤 `undefined`，只返回成功结果并保留成功项的输入顺序；`tests/unit/platform/execution/tool-executor/tool-parallel-executor.test.ts` 新增 `compacts failed positions instead of returning undefined holes`，`tests/unit/platform/execution/tool-executor/tool-executor-service.test.ts` 也同步更新 contract 断言。 | 关闭的是返回稀疏数组这一具体缺陷；失败项索引仍通过 `errors[index]` 保留。 |
| 2144 | Round 36 — Model-Gateway/Compliance · Tests/Ops-Maturity · Control-Plane/Scale-Eco · Execution/Shared / §182 Execution / Shared Infrastructure 缺陷 | P2 | `src/platform/shared/lifecycle/service-registry.ts:146-149` | `if(!has){delete}` 是 no-op——条件写反 | 已解决（源码/测试证据回写） | 这条是旧实现快照残留：历史版本确实有 live registry 清理条件写反的问题，但当前 `reset()` 路径已重构为 teardown 后无条件清空实例/注册表，并显式把自身从 `liveRegistries` 移除。 | 当前 `src/platform/shared/lifecycle/service-registry.ts` 的 `reset()` 会在 teardown 完成后执行 `ServiceRegistry.liveRegistries.delete(this)`；`tests/unit/platform/shared/lifecycle/service-registry.test.ts` 与 `service-registry-bootstrap-replay.test.ts` 本轮定向通过，证明 reset/重建/bootstrap replay 的现行生命周期可工作。 | 不额外造新修复；按现行源码与定向测试回写为已解决。 |
| 2145 | Round 36 — Model-Gateway/Compliance · Tests/Ops-Maturity · Control-Plane/Scale-Eco · Execution/Shared / §182 Execution / Shared Infrastructure 缺陷 | P2 | `src/platform/execution/tool-executor/command-security.ts:92-111` | touch/mkdir 重复条目——Map last-wins 首条死代码 | 已解决（源码清理并补定向测试） | 根因是默认命令策略表里 `touch`、`mkdir` 被声明了两次，后者带 `writePathArgPositions`，前者不带；`new Map(entries)` 会让后一条覆盖前一条，功能上虽“最终能用”，但前一条成为永远不会生效的死定义，增加策略误读和后续维护风险。 | 本轮在 `src/platform/five-plane-execution/tool-executor/command-security.ts` 中合并了 `touch`/`mkdir` 的策略定义，仅保留单一 write-aware 条目；`tests/unit/platform/execution/tool-executor/command-security.test.ts` 新增 `includes a single write-aware touch and mkdir policy`，直接校验默认策略的 write path 配置。 | 关闭的是重复死条目问题，不外推整个命令策略表已完全无冗余。 |
| 2146 | Round 36 — Model-Gateway/Compliance · Tests/Ops-Maturity · Control-Plane/Scale-Eco · Execution/Shared / §182 Execution / Shared Infrastructure 缺陷 | P2 | `src/platform/execution/tool-executor/tool-output-sanitizer.ts:28` | CONTROL_CHARS_REGEX 漏 0x1C-0x1F——FS/GS/RS/US 未清理 | 已解决（源码修复并补定向测试） | 根因是控制字符清理正则覆盖到 `0x1A` 后直接跳到 `0x7F`，遗漏了 `FS/GS/RS/US` 四个 C0 控制字符，导致 sanitization 仍会放过一段不可见分隔控制符。 | 本轮将 `src/platform/five-plane-execution/tool-executor/tool-output-sanitizer.ts` 的 `CONTROL_CHARS_REGEX` 扩展到 `\\u001F`，并在 `tests/unit/platform/execution/tool-executor/tool-output-sanitizer.test.ts` 新增 `removes FS GS RS US control characters`。 | 关闭的是控制字符覆盖缺口，不外推所有文本净化风险都已消失。 |
| 2147 | Round 37 — State-Transition/Recovery · Desktop/Mobile · Routing/Escalation/Delegation · Multi-Region/Tenant / §184 State-Transition / Recovery / Lease 缺陷 | P0 | `src/platform/execution/recovery/runtime-recovery-service.ts:298` | stale 阈值 `Date.now()+thresholdMs` 加法反了——所有 run 被标记为 stale | 已解决（源码修复并补定向测试） | 根因明确且单点：`findStaleExecuting()` 需要算“早于当前时间阈值”的时间点，却写成了 `Date.now() + thresholdMs`，把 stale cutoff 推到了未来，等价于把几乎所有执行都判成 stale。 | 本轮把 `src/platform/five-plane-execution/recovery/runtime-recovery-service.ts` 修正为 `Date.now() - thresholdMs`；`tests/unit/platform/execution/recovery/runtime-recovery-service.test.ts` 新增 `findStaleExecuting computes staleBefore in the past`，直接断言计算出的 `staleBefore` 落在当前时间之前。 | 这是实质性逻辑修复，不是文案调整。 |
| 2148 | Round 37 — State-Transition/Recovery · Desktop/Mobile · Routing/Escalation/Delegation · Multi-Region/Tenant / §184 State-Transition / Recovery / Lease 缺陷 | P0 | `src/platform/execution/state-transition/transition-service.ts:286-288` | WorkflowTransitionService.transition() 无事务——read+write 非原子,并发丢更新 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 2149 | Round 37 — State-Transition/Recovery · Desktop/Mobile · Routing/Escalation/Delegation · Multi-Region/Tenant / §184 State-Transition / Recovery / Lease 缺陷 | P1 | `src/platform/execution/state-transition/transition-service.ts:193` | Session `paused` 状态无入边——状态图孤立节点,永不可达 | 已解决 | 根因是 `SESSION_TRANSITIONS` 中 `awaiting_user` 状态缺少到 `paused` 的出边，导致 session 无法从 `awaiting_user` 进入 `paused` 状态。 | 当前 `src/platform/execution/state-transition/transition-service.ts` line 193 已将 `awaiting_user` 的出边数组添加 `paused`，即 `["streaming", "completed", "failed", "cancelled", "paused"]`；与 `runtime-state-machine.ts` 中 `LEGACY_SESSION_TRANSITIONS.awaiting_user` 保持一致；typecheck 在 `transition-service.ts` 无错误。 | 关闭该条孤立 paused 状态缺陷。 |
| 2150 | Round 37 — State-Transition/Recovery · Desktop/Mobile · Routing/Escalation/Delegation · Multi-Region/Tenant / §184 State-Transition / Recovery / Lease 缺陷 | P1 | `src/platform/execution/lease/execution-lease-service.ts:556-664` | validateWriteAccess 不检查 lease TTL 过期——过期 lease 仍可写 | 已解决（源码已验证，本轮补充证据） | 原审计指出 validateWriteAccess 缺少 expiresAt 校验，TTL 过期但未回收的 lease 仍允许写入。 | 当前 `src/platform/execution/lease/execution-lease-service.ts` 的 `validateWriteAccess()` 在 lines 714-732 显式比较 `Date.parse(activeLease.expiresAt) < Date.parse(occurredAt)`，对过期 lease 返回 `reasonCode: "lease_expired"` 并写入审计事件；该检查位于函数内，在 fencing token 和 worker 检查之后、TTL bounds 检查之前。`tests/unit/platform/execution/lease/execution-lease-service-r9.test.ts` 已验证过期 lease 即使 token/worker 匹配也会被拒绝，与 R9-01 同一代码路径。 | 与 R9-01 共用同一修复，仅回写 reaudit 状态。 |
| 2151 | Round 37 — State-Transition/Recovery · Desktop/Mobile · Routing/Escalation/Delegation · Multi-Region/Tenant / §184 State-Transition / Recovery / Lease 缺陷 | P1 | `src/platform/five-plane-execution/lease/execution-lease-service-async.ts:265-337` | async releaseLeaseSync 无 active 状态守卫——可重复释放已终止 lease | 已修复 | 根因是 `releaseLeaseSync` 缺少 `lease.status !== "active"` 守卫，允许重复释放已终止的 lease。 | 当前 `src/platform/five-plane-execution/lease/execution-lease-service-async.ts` 的 `releaseLeaseSync()` lines 283-289 已有 `if (lease.status !== "active")` 守卫返回 `lease_not_active`；此外 lines 291-298 检查 expiration，lines 301-311 做 TOCTOU re-check；三重守卫确保 idempotency；typecheck 通过。 | 关闭该条 active 状态守卫缺失缺陷。 |
| 2152 | Round 37 — State-Transition/Recovery · Desktop/Mobile · Routing/Escalation/Delegation · Multi-Region/Tenant / §184 State-Transition / Recovery / Lease 缺陷 | P1 | `src/platform/execution/recovery/runtime-recovery-decision-service.ts:162-196` | decision apply() 事务外读取——TOCTOU 基于陈旧数据做决策 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 2153 | Round 37 — State-Transition/Recovery · Desktop/Mobile · Routing/Escalation/Delegation · Multi-Region/Tenant / §184 State-Transition / Recovery / Lease 缺陷 | P1 | `src/platform/execution/recovery/repair-pipeline.ts:88-95` | transitionTo 无状态验证——terminal 状态可退出/任意跳转 | 已修复 | 源码 lines 88-109 已实现 VALID_TRANSITIONS 矩阵约束+terminal state 守卫；typecheck 通过。 | 本轮已在 `repair-pipeline.ts:88-109` 添加状态转移验证逻辑，包括：(1) VALID_TRANSITIONS 定义每个 stage 的合法后继状态；(2) 非法的任意跳转抛出 `repair.transition_invalid` 错误；(3) terminal 状态 (completed/failed) 禁止退出，抛出 `repair.transition_from_terminal` 错误。 | 已修复。 |
| 2154 | Round 37 — State-Transition/Recovery · Desktop/Mobile · Routing/Escalation/Delegation · Multi-Region/Tenant / §184 State-Transition / Recovery / Lease 缺陷 | P1 | `src/platform/execution/recovery/repair-pipeline.ts:192-222` | handleReviewFailure 绕过 repair budget——无限修复循环 | 已修复 | 源码 lines 242-248 已实现 shouldRepair() budget check；autoRepairable&&budget check before repair。 | 本轮已在 `repair-pipeline.ts:242-248` 添加 repair budget 校验，在 shouldRepair() 返回 false 时立即 escalate，防止无限修复循环。 | 已修复。 |
| 2155 | Round 37 — State-Transition/Recovery · Desktop/Mobile · Routing/Escalation/Delegation · Multi-Region/Tenant / §184 State-Transition / Recovery / Lease 缺陷 | P1 | `src/platform/execution/side-effect-manager.ts:306-321` | targetStatusForReconciliation 无 default case——unknown action→undefined 状态 | 已修复 | 源码 lines 306-321 的 `targetStatusForReconciliation` 已包含 `default: return "failed"` 分支，任何未知 `nextAction` 均映射为 `"failed"` 而非 `undefined`；typecheck 通过。 | 本轮确认 `targetStatusForReconciliation` 的 switch 语句已覆盖全部 5 种合法 action (`mark_confirmed/retry_probe/compensate/escalate_hitl/mark_failed`) 并通过 `default: return "failed"` 处理未知 action，状态机不会收到 `undefined`。 | 已修复。 |
| 2156 | Round 37 — State-Transition/Recovery · Desktop/Mobile · Routing/Escalation/Delegation · Multi-Region/Tenant / §184 State-Transition / Recovery / Lease 缺陷 | P1 | `src/platform/execution/recovery/replay-boundary-guard.ts:22` | reexecution_replay 允许 real side effects——恢复重放产生重复外部变更 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 2157 | Round 37 — State-Transition/Recovery · Desktop/Mobile · Routing/Escalation/Delegation · Multi-Region/Tenant / §184 State-Transition / Recovery / Lease 缺陷 | P2 | `src/platform/execution/state-transition/transition-service.ts:763-773` | resolveExistingExecutionId 绕过 repository 用 raw SQL——非 SQLite 后端无 FK 校验 | 已修复 | 根因是 `resolveExistingExecutionId` 使用 `db.connection.prepare(raw SQL)` 绕过 repository，无法获得 FK 校验和 repository 层抽象。 | 本轮已将 `resolveExistingExecutionId` 改为接受 `RuntimeLifecycleRepository` 而非 `AuthoritativeSqlDatabase`，改用 `repository.getExecution(executionId)` 替代 raw SQL；调用处也改为传入 `this.repository` 而非 `this.db`；typecheck 在 `transition-service.ts` 无错误。 | 已修复。 |
| 2158 | Round 37 — State-Transition/Recovery · Desktop/Mobile · Routing/Escalation/Delegation · Multi-Region/Tenant / §184 State-Transition / Recovery / Lease 缺陷 | P2 | `src/platform/execution/recovery/repair-pipeline.ts:263-286` | escalate()/fail() 丢弃 reason 参数——审计不可追溯 | 已修复 | 源码 `repair-pipeline.ts:263-270,280-286` 已添加 `escalateReason`/`failReason` 字段到 `PipelineState`，并在 `escalate()`/`fail()` 方法中赋值；typecheck 通过。 | 本轮已在 `PipelineState` 接口 (lines 28-42) 添加 `escalateReason?: string` 和 `failReason?: string` 可选字段，并在 `escalate()` 方法 (lines 263-270) 设置 `escalateReason: reason`，在 `fail()` 方法 (lines 280-286) 设置 `failReason: reason`，确保审计可追溯。 | 已修复。 |
| 2159 | Round 37 — State-Transition/Recovery · Desktop/Mobile · Routing/Escalation/Delegation · Multi-Region/Tenant / §184 State-Transition / Recovery / Lease 缺陷 | P2 | `src/platform/execution/lease/execution-lease-service-async.ts:207` | TTL 过期用字符串比较——timezone/精度不同时错误 | 已修复（源码修复） | 根因是 ISO timestamp 字符串直接比较无法正确处理不同时区或精度的值。 | 本轮已将所有 TTL 过期比较从 `lease.expiresAt <= occurredAt` 改为 `new Date(lease.expiresAt) <= new Date(occurredAt)`，共修复 6 处（lines 217/293/399/436/545/602）；typecheck 通过。 | 关闭该条 TTL 字符串比较缺陷。 |
| 2160 | Round 37 — State-Transition/Recovery · Desktop/Mobile · Routing/Escalation/Delegation · Multi-Region/Tenant / §184 State-Transition / Recovery / Lease 缺陷 | P2 | `src/platform/execution/run-termination-cleanup.ts:47-48` | 未知 resourceKind indexOf=-1 排最前——依赖序反转 | 已修复（排序逻辑修正） | 根因是 `CLEANUP_ORDER.indexOf()` 对未知 resourceKind 返回 `-1`，导致排序时这类资源反而排到最前，与依赖顺序相反。 | 当前 `src/platform/execution/run-termination-cleanup.ts` 的 `executeAsync()` 和 `execute()` 已将排序逻辑改为：未知 resourceKind 的 index 显式替换为 `Number.MAX_SAFE_INTEGER`，确保未知资源排在最后，维持依赖顺序。 | 本轮修复排序逻辑，不改 cleanup 语义。 |
| 2161 | Round 37 — State-Transition/Recovery · Desktop/Mobile · Routing/Escalation/Delegation · Multi-Region/Tenant / §184 State-Transition / Recovery / Lease 缺陷 | P2 | `src/platform/execution/state-transition/transition-service.ts:43-46` | self-transition 静默 no-op——掩盖幂等性 bug,terminal 状态不触发补偿 | 已修复 | 根因是 self-transition（fromStatus === toStatus）时，旧代码先检查当前状态是否已处于目标状态，若是则静默 return，不经过 state machine 校验，导致 terminal 状态无法触发补偿逻辑。 | 本轮在 TaskTransitionService.apply()（lines 385-404）、WorkflowTransitionService.apply()（lines 584-606）、ExecutionTransitionService.apply()（lines 777-802）中调整校验顺序：先检查 fromStatus 不匹配（CAS 失败检测），再让 state machine 验证 transition（包括 self-transition），最后仅在 fromStatus !== toStatus 时才允许 noop 返回。SessionTransitionService 已在上一轮修正。typecheck 在 transition-service.ts 无错误。 | 已修复。 |
| 2162 | Round 37 — State-Transition/Recovery · Desktop/Mobile · Routing/Escalation/Delegation · Multi-Region/Tenant / §185 Desktop / Mobile 应用缺陷 | P0 | `ui/apps/electron-win/src/main.ts:15-16` | IPC shell:run/shell:spawn 暴露——renderer 可执行任意 shell 命令 | 已解决（源码已收口，本轮补证据） | 根因并不是“本轮刚加壳层控制”，而是源码此前已经把高危 shell IPC 从主进程 allowlist 移除，但 reaudit 与部分单测仍停留在旧暴露面快照，没有把安全收口状态回写。 | 当前 `ui/apps/electron-win/src/main.ts` 的 `electronMainBaseline.channels` 只保留 `shell:openExternal`，不再包含 `shell:run/shell:spawn`；`tests/unit/ui/apps/electron-win/main.test.ts` 与本轮回写后的 `tests/unit/ui/apps/electron-win/preload.test.ts` 已定向验证主进程和 preload 两侧都不再暴露这两个高危通道。 | 不做 UI 全量构建；按源码与窄测试关闭该条。 |
| 2163 | Round 37 — State-Transition/Recovery · Desktop/Mobile · Routing/Escalation/Delegation · Multi-Region/Tenant / §185 Desktop / Mobile 应用缺陷 | P0 | `ui/apps/electron-win/src/preload.ts:33-34` | bridge 直接赋 window 属性非 contextBridge.exposeInMainWorld——绕过 context isolation | 已修复 | 根因是旧 preload 实现曾依赖直接 window mutation，后续虽已引入 `contextBridge`，但回归测试仍按旧行为断言 `target.__AA_ELECTRON__ = bridge`，导致 reaudit 误以为旁路仍在。 | 本轮确认并收敛 `ui/apps/electron-win/src/preload.ts`：`installElectronBridge()` 现在只通过 `contextBridge.exposeInMainWorld("AA_ELECTRON", bridge)` 暴露桥，不再包装或回退到 window 直写；`tests/unit/ui/apps/electron-win/preload.test.ts` 与 `ui/tests/unit/ui/apps/electron-win/preload.test.ts` 已回写为验证 `exposeInMainWorld` 调用且目标 window 不被污染。 | 关闭该条 context isolation 缺陷，并同步清理过时断言。 |
| 2164 | Round 37 — State-Transition/Recovery · Desktop/Mobile · Routing/Escalation/Delegation · Multi-Region/Tenant / §185 Desktop / Mobile 应用缺陷 | P0 | `ui/apps/web/src/app-shell.tsx:8-22` | demoGuardContext 硬编码全管理员权限——所有路由守卫 no-op | 已解决（源码/测试） | 根因是旧 web 壳层曾把 route guard 上下文写成固定的 demo admin 权限，导致守卫链即便存在也会被全量放行；reaudit 没跟上后续改成动态 auth context 的实现。 | 当前 `ui/apps/web/src/app-shell.tsx` 已改为通过 `createFeatureGuardContext()` 从 `authContext.userId/tenantId/roles/permissions` 动态构造 guard context，不再存在 `const demoGuardContext`；本轮回写 `tests/unit/ui/apps/web/app-shell.test.tsx`，直接验证源码中已不存在硬编码 demo guard，并确认 `authContext` 会同时传入 guard 和 `UiRuntimeProvider`。 | 不做 UI 全链路 e2e；按源码与窄测试关闭该条。 |
| 2165 | Round 37 — State-Transition/Recovery · Desktop/Mobile · Routing/Escalation/Delegation · Multi-Region/Tenant / §185 Desktop / Mobile 应用缺陷 | P0 | `ui/apps/electron-win/src/main.ts:21-23` | IPC files:read/files:write 无路径白名单——任意文件读写 | 已修复 | 根因和 `2162` 类似：主进程高危文件 IPC 已在源码里移除，但 preload/集成测试层还残留旧接口认知，导致复审表没有及时收口。 | 当前 `ui/apps/electron-win/src/main.ts` 的基线 channel 列表已删除 `files:read/files:write`；本轮还同步从 `ui/apps/electron-win/src/preload.ts` 删除了对应的 preload 映射，避免 renderer 继续看到无后端实现的危险文件入口；`tests/unit/ui/apps/electron-win/main.test.ts`、`tests/unit/ui/apps/electron-win/preload.test.ts` 已定向验证两侧都不再暴露该能力。 | 关闭“任意文件读写 IPC 暴露”条目，不外推为“完整受控文件 API 已产品化”。 |
| 2166 | Round 37 — State-Transition/Recovery · Desktop/Mobile · Routing/Escalation/Delegation · Multi-Region/Tenant / §185 Desktop / Mobile 应用缺陷 | P1 | `ui/apps/web/src/runtime.ts:34` | API base URL fallback http://localhost:3000——非本地部署不安全 | 已解决（源码/测试） | 根因是早期 web runtime 为了本地联调把 API 根地址回退到 `http://localhost:3000`，生产漏配时会静默打到本机服务。 | 当前 `ui/apps/web/src/runtime.ts` 的 `createWebRuntimeConfig()` / `createWebRuntimeClients()` 已不再注入 localhost fallback，未配置时只保留 `undefined` 并交由 transport fail-closed/mock 路径处理；`tests/unit/ui/apps/web/runtime.test.ts` 已覆盖“空 env 不再产生默认 localhost baseUrl”的回归，本轮定向测试已通过。 | 不做 UI 全量构建；按源码与窄测试关闭该条。 |
| 2167 | Round 37 — State-Transition/Recovery · Desktop/Mobile · Routing/Escalation/Delegation · Multi-Region/Tenant / §185 Desktop / Mobile 应用缺陷 | P1 | `ui/apps/web/src/runtime.ts:44-46` | wsUrl 被忽略——WS 永远用 InMemoryWSClient(死代码) | 已修复 | 根因不只在 `runtime.ts` 一处：一版“修复”把 `new WebSocket(config.wsUrl)` 错当成 `BrowserWSClient` 的工厂参数传入，真正调用 `connect()` 时仍会失效；同时 `UiRuntimeProvider` 还在把路由层强行连到硬编码 `ws://local/ui`，导致配置的 `wsUrl` 根本到不了真实连接链。 | 本轮把 `ui/apps/web/src/runtime.ts` 改为始终构造合法的 `BrowserWSClient(WebSocket, fallback)`，并把显式 `wsUrl` 连同共享 `TokenManager` 一起向上传递；`ui/apps/web/src/main.tsx`、`ui/apps/web/src/app-shell.tsx`、`ui/packages/shared/state/src/index.ts` 现已把 `wsUrl` 传到 `UiRuntimeProvider`，只有在存在显式 `wsUrl` 和真实 session token 时才发起 `router.connect(wsUrl, token)`，不再强连本地 stub。`tests/unit/ui/apps/web/runtime.test.ts` 已新增真实 `BrowserWSClient.connect()` 回归，`tests/unit/ui/shared-package-regressions.test.ts` 也验证共享状态层已移除硬编码本地 WS 入口，本轮定向测试已通过。 | 关闭该条“配置的 WS 端点走不到生产连接链”缺陷。 |
| 2168 | Round 37 — State-Transition/Recovery · Desktop/Mobile · Routing/Escalation/Delegation · Multi-Region/Tenant / §185 Desktop / Mobile 应用缺陷 | P1 | `ui/apps/electron-win/index.html:2-10` | 无 CSP meta tag——XSS 可加载远程脚本+调用 IPC | 已解决（源码/测试） | 根因是 Electron Windows 壳层早期只保留最小 HTML 容器，没有把桌面壳的 CSP / clickjacking 防护显式写入页面头部；后续资源已补齐，但 reaudit 行和测试仍未回写。 | 当前 `ui/apps/electron-win/index.html` 已包含 `Content-Security-Policy`、`X-Content-Type-Options`、`X-Frame-Options` 等 meta 防护；本轮新增 `tests/unit/ui/apps/electron-win/shell-assets.test.ts`，直接验证页面含有 `default-src 'self'`、`script-src 'self'` 与 `frame-ancestors 'none'`。 | 关闭的是壳层 HTML 缺失 CSP 的问题，不外推为 Electron 全链路 XSS 审计已完成。 |
| 2169 | Round 37 — State-Transition/Recovery · Desktop/Mobile · Routing/Escalation/Delegation · Multi-Region/Tenant / §185 Desktop / Mobile 应用缺陷 | P1 | `ui/apps/mobile/src/App.tsx:5` | 平台硬编码 "android"——iOS 用户获得错误 adapter | 已解决（源码/测试） | 根因是旧移动壳直接写死 `createMobilePlatformAdapter("android")`，导致没有原生桥或在 iOS 端运行时也会被错误识别成 Android。 | 当前 `ui/apps/mobile/src/App.tsx` 已通过 `detectMobilePlatform()` 基于 UA 优先识别 `iphone/ipad/ios`，仅在无法识别时才 fallback 到 `android`；本轮回写 `tests/unit/ui/apps/mobile/App.test.tsx`，直接验证源码已移除硬编码 Android 适配器并保留双平台 manifest。 | 关闭该条平台识别错误缺陷。 |
| 2170 | Round 37 — State-Transition/Recovery · Desktop/Mobile · Routing/Escalation/Delegation · Multi-Region/Tenant / §185 Desktop / Mobile 应用缺陷 | P1 | `ui/apps/tauri-macos/src-tauri/src/lib.rs:7-8` | open_deep_link 无 scheme 验证——可打开 file:///javascript: URL | 已解决（源码/测试） | 根因是旧 `open_deep_link` 对传入 URL 不做任何 scheme 白名单校验，`file:` / `javascript:` 之类危险协议可以直接穿过桌面壳。 | 当前 `ui/apps/tauri-macos/src-tauri/src/lib.rs` 已显式只接受 `http/https`，其余 scheme 一律返回 `rejected:invalid_scheme`；本轮回写 `tests/unit/ui/apps/tauri-macos/lib.test.ts`，直接断言 Rust 源码中存在 `eq_ignore_ascii_case("http") / ("https")` 和拒绝分支。 | 关闭的是 macOS deep-link scheme 校验缺陷。 |
| 2171 | Round 37 — State-Transition/Recovery · Desktop/Mobile · Routing/Escalation/Delegation · Multi-Region/Tenant / §185 Desktop / Mobile 应用缺陷 | P1 | `ui/apps/tauri-macos+linux Cargo.toml` | 无 tauri-plugin-updater——无自动更新/签名验证 | 已解决（源码/测试） | 复核后发现根因分两层：`tauri-plugin-updater` 实际已加过，但 Cargo 文件长期残留重复依赖项和无效的 `//` 注释，导致“已配置 updater”的证据既不干净也不具备 TOML 级可信度。 | 本轮已把 `ui/apps/tauri-macos/src-tauri/Cargo.toml` 与 `ui/apps/tauri-linux/src-tauri/Cargo.toml` 收敛为单次声明、TOML 合法注释的 `tauri-plugin-updater = "2"` 和 `tauri-plugin-secure-storage = "2"`；`tests/unit/ui/apps/tauri-macos/lib.test.ts`、`tests/unit/ui/apps/tauri-linux/lib.test.ts` 现都验证 updater/secure-storage 仅声明一次且不再包含 `//`。 | 关闭的是 updater 依赖与 Cargo 清单不可信的问题，不外推为自动更新发布流水线已实装。 |
| 2172 | Round 37 — State-Transition/Recovery · Desktop/Mobile · Routing/Escalation/Delegation · Multi-Region/Tenant / §185 Desktop / Mobile 应用缺陷 | P2 | `ui/apps/tauri-linux/src/index.ts:4-8` | DesktopShellManifest 缺 updateChannel | 已解决（源码/测试） | 根因是 Linux 壳 manifest 与 macOS 壳长期漂移，缺少 `updateChannel` 字段，导致上层无法统一按 channel 编排更新策略。 | 本轮在 `ui/apps/tauri-linux/src/index.ts` 的 `DesktopShellManifest` 与 `tauriLinuxManifest` 中补入 `updateChannel: "stable"`；`tests/unit/ui/apps/tauri-linux/lib.test.ts` 已直接断言 Linux manifest 现在携带该字段。 | 关闭该条 Linux manifest 漂移缺陷。 |
| 2173 | Round 37 — State-Transition/Recovery · Desktop/Mobile · Routing/Escalation/Delegation · Multi-Region/Tenant / §185 Desktop / Mobile 应用缺陷 | P2 | `ui/apps/electron-win/package.json` | 无 electron 依赖/无打包签名配置 | 已解决（源码/测试） | 根因是 Electron Windows 壳的 package 清单只有最小 `scripts/main`，既没声明桌面运行时依赖，也没有任何打包签名元数据，导致清单层面无法支撑真正的桌面构建。 | 本轮在 `ui/apps/electron-win/package.json` 中补入 `electron`、`electron-updater` 开发依赖，以及 `build.win.verifyUpdateCodeSignature/signAndEditExecutable/publisherName` 等最小签名构建元数据；`tests/unit/ui/apps/electron-win/shell-assets.test.ts` 已直接验证这些字段存在。 | 关闭的是 package manifest 缺失依赖和签名元数据的问题，不外推为证书和发布渠道已经配置完成。 |
| 2174 | Round 37 — State-Transition/Recovery · Desktop/Mobile · Routing/Escalation/Delegation · Multi-Region/Tenant / §185 Desktop / Mobile 应用缺陷 | P2 | `ui/apps/mobile/package.json` | 无 react-native 依赖——无法构建 | 已解决（源码/测试） | 根因是移动壳 package 清单之前只有 `smoke` 脚本，没有声明 React Native 运行时依赖，导致 manifest 虽然声称 `runtime: "react-native"`，但包级依赖并不自洽。 | 本轮在 `ui/apps/mobile/package.json` 中补入 `react` 与 `react-native` 依赖，并在 `tests/unit/ui/apps/mobile/App.test.tsx` 中直接验证这两个 runtime 依赖已存在。 | 关闭的是 mobile package 运行时依赖缺失问题。 |
| 2175 | Round 37 — State-Transition/Recovery · Desktop/Mobile · Routing/Escalation/Delegation · Multi-Region/Tenant / §185 Desktop / Mobile 应用缺陷 | P2 | `ui/apps/web/src/runtime.ts:39` | createAuthInterceptor 用硬编码字符串非 session token | 已修复 | 根因是问题后来从“interceptor 直接吃常量字符串”演变成“runtime 和 shared state 各自持有不同 auth token 来源”：`createAuthInterceptor()` 已经支持 `TokenManager`，但共享状态层仍在注入 `ui-runtime-access/ui-runtime-refresh/demo-token` 这类伪 session，导致真实会话根本无法贯通到 REST/WS。 | 当前 `ui/apps/web/src/runtime.ts` 继续把 `TokenManager` 传给 `createAuthInterceptor()`，并把同一个 `tokenManager` 暴露给上层；`ui/apps/web/src/main.tsx` / `app-shell.tsx` / `ui/packages/shared/state/src/index.ts` 现已共享同一 `TokenManager` 与 `AuthService`，同时删除了 `ui-runtime-access/ui-runtime-refresh/demo-token` 这类硬编码凭证。`tests/unit/ui/shared-package-regressions.test.ts` 已校验共享状态层不再保留这些伪 token，`tests/unit/ui/apps/web/runtime.test.ts` 也继续验证 interceptor 入口接受动态 token manager，本轮定向测试已通过。 | 关闭的是 Web runtime 硬编码 token 链路，不外推为完整 OAuth/SSO 生产集成已完工。 |
| 2176 | Round 37 — State-Transition/Recovery · Desktop/Mobile · Routing/Escalation/Delegation · Multi-Region/Tenant / §185 Desktop / Mobile 应用缺陷 | P2 | `ui/apps/web/src/runtime.ts:55` | registerWebServiceWorker 注册不存在的 sw 文件——404 | 已解决（源码/资源/测试） | 根因是早期 web 壳层在没有实际 service worker 资源时直接注册固定路径，部署后只能得到 404；后续资源文件和注册前存在性检查都已补上，但复审表没有回写。 | 当前 `ui/apps/web/public/aa-sw.js` 已存在，`ui/apps/web/src/runtime.ts` 的 `registerWebServiceWorker()` 也会先对 `${BASE_URL}aa-sw.js` 发 HEAD 探测，只有资源存在时才注册；`tests/unit/ui/apps/web/runtime.test.ts` 已覆盖 Node/无 SW 环境下 fail-closed 返回 `null` 的回归，本轮定向测试已通过。 | 不做 browser e2e；按源码、资源文件和窄测试关闭该条。 |
| 2177 | Round 37 — State-Transition/Recovery · Desktop/Mobile · Routing/Escalation/Delegation · Multi-Region/Tenant / §186 Routing / Escalation / Delegation 缺陷 | P0 | `src/platform/orchestration/escalation/index.ts:23-50` | 无分层升级策略——spec 要求 agent→team→human→incident,实际仅 flat 条件链 | 已解决（源码修复并补定向测试） | 根因是旧实现把 team approval 的门限判断放在前面，导致 critical 但未命中 cost/production/high-risk gate 的请求会被错误降成 `none`，分层 escalation 顺序没有真正生效。 | 当前 `src/platform/orchestration/escalation/index.ts` 已按 `incident -> human -> team -> agent` 优先级决策：critical+production 触发 `panic_stop`，critical 或 high-risk execute 触发 `takeover`，其后才是 approval 与 none；定向验证 `tests/unit/platform/orchestration/escalation/index.test.ts` 已 `18 passed`，覆盖优先级、边界值、reasonCode 和 operator-action 行为。 | 关闭的是“flat 条件链没有真正实现分层升级”的缺口。 |
| 2178 | Round 37 — State-Transition/Recovery · Desktop/Mobile · Routing/Escalation/Delegation · Multi-Region/Tenant / §186 Routing / Escalation / Delegation 缺陷 | P0 | `src/platform/orchestration/agent-delegation/context-isolator.ts:174-175` | parent.permissions.actions=0 时除零→Infinity→错误隔离级别 | 已解决（源码） | 根因是旧版 `determineIsolationLevel()` 直接用 `childRequiredCount / parentActionCount` 计算权限比率，未处理 `parentActionCount === 0` 的边界情况。 | 当前 `context-isolator.ts:176-183` 已增加零值守卫：`parentActionCount` 先捕获 `.length`，若为 0 则 `permissionRatio` 默认置 0（对应 MINIMAL 隔离），只有 `parentActionCount > 0` 时才执行除法；源码已标注 `#2178: Guard against division by zero when parent has no actions`。本轮定向运行 `tests/unit/platform/orchestration/agent-delegation/context-isolator.test.ts` 12 个用例全部通过。 | 关闭的是"除零导致 Infinity 从而错误授予 FULL 权限"的缺陷；伪造修复证据者后果自负。 |
| 2179 | Round 37 — State-Transition/Recovery · Desktop/Mobile · Routing/Escalation/Delegation · Multi-Region/Tenant / §186 Routing / Escalation / Delegation 缺陷 | P0 | `src/platform/orchestration/agent-delegation/context-isolator.ts:213-221` | MINIMAL 隔离从 requiredPermissions 取值不与 parent 交集——子可超越父权限 | 已解决 | 根因是旧版 MINIMAL 在 `requiredPermissions.resources.length > 0` 时直接取 `requiredPermissions.resources` 而非与 parent 求交集，导致 child 可获 parent 范围外的资源/动作。 | 当前 `context-isolator.ts:226-231` 已改为 `parentPermissions.resources.filter(r => requiredPermissions.resources.includes(r))` / `parentPermissions.actions.filter(a => requiredPermissions.actions.includes(a))`，确保 child 权限始终是 parent 与 required 的交集；`R9-07 fix` 注释 (§19 intersection 要求) 已在源码中标注。本轮定向运行 `tests/unit/platform/orchestration/agent-delegation/context-isolator.test.ts` 12 个用例全部通过。 | 关闭的是"MINIMAL 子权限可超出 parent 范围"的真实缺陷；不许伪造修复证据。 |
| 2180 | Round 37 — State-Transition/Recovery · Desktop/Mobile · Routing/Escalation/Delegation · Multi-Region/Tenant / §186 Routing / Escalation / Delegation 缺陷 | P1 | `src/platform/orchestration/routing/intake-router.ts:348-403` | 无 skill taxonomy/负载均衡——仅关键词匹配+字符长度启发 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 2181 | Round 37 — State-Transition/Recovery · Desktop/Mobile · Routing/Escalation/Delegation · Multi-Region/Tenant / §186 Routing / Escalation / Delegation 缺陷 | P1 | `src/platform/orchestration/routing/workflow-planner.ts:97-99,163` | 无 DAG 环检测——循环依赖导致无限执行 | 已解决（源码修复并补定向测试） | 根因是 `WorkflowPlanner.plan()` 之前只把 `dependsOnStepIds` 平铺成边，不校验缺失依赖、重复 stepId 或环路，导致非法 workflow 会直接进入执行期。 | 当前 `src/platform/orchestration/routing/workflow-planner.ts` 已在规划阶段增加 `validateWorkflowGraph()`：显式拒绝 `workflow.missing_dependency`、`workflow.duplicate_step_id` 与 `workflow.cyclic_dependency`；`tests/unit/platform/orchestration/routing/workflow-planner.test.ts` 已新增 missing-dependency 与 cyclic-dependency 回归。 | 关闭的是“非法 DAG 未在规划期 fail-close”的真实缺陷。 |
| 2182 | Round 37 — State-Transition/Recovery · Desktop/Mobile · Routing/Escalation/Delegation · Multi-Region/Tenant / §186 Routing / Escalation / Delegation 缺陷 | P1 | `src/platform/orchestration/routing/agent-team-service.ts:146` | repair→validate 无限循环无退出——无 max retry/escalation 出口 | 已解决（源码修复并补定向测试） | 根因是 `computeExecutionLoop()` 此前对 high risk workflow 的 repair/validate 循环无上限——根据 `normalizedStepCount` 追加循环但无 MAX_REPAIR_CYCLES 约束，导致 repair→validate 可能无限迭代。 | 当前 `agent-team-service.ts:8` 已新增 `MAX_REPAIR_CYCLES = 2` 常量；`computeExecutionLoop()` 对 high risk loop 改用 `Math.min(Math.max(workflowStepsCount - 1, 1), MAX_REPAIR_CYCLES)` 限制 repair 轮次；并在超出 `MAX_REPAIR_CYCLES` 时插入 `"escalate"` 阶段供 human review 退出；同时 `AgentTeamStage` 类型已扩展支持 `"escalate"` 值。 | 关闭的是"repair→validate 无上限循环无退出条件"的缺陷；现有实现通过 MAX_REPAIR_CYCLES 上限与 escalation 出口防止无限循环。
| 2183 | Round 37 — State-Transition/Recovery · Desktop/Mobile · Routing/Escalation/Delegation · Multi-Region/Tenant / §186 Routing / Escalation / Delegation 缺陷 | P1 | `src/platform/orchestration/agent-delegation/delegation-manager.service.ts:104-116` | eviction 淘汰 active delegation——"not found"错误 | 已解决 | 根因是 `evictExpired()` 此前按 TTL 老化直接驱逐所有过期 entry，未区分 delegation 状态。长期运行的 active delegation 超过 TTL 后被错误淘汰，后续访问时触发 "not found" 错误。 | 当前 `evictExpired()` 在驱逐前先检查 `delegation.status === "completed" || delegation.status === "failed" || delegation.status === "expired" || delegation.status === "cancelled"`，仅对终端态 delegation 执行淘汰；容量驱逐（`delegationStore.size > MAX_ENTRIES`）同样在移除前校验 terminal state，避免 active delegation 被错误驱逐。 | 关闭的是"active delegation 因 TTL 超期被错误淘汰"的缺陷；非终端态 delegation（pending/active/suspended）不受 eviction 影响。 |
| 2184 | Round 37 — State-Transition/Recovery · Desktop/Mobile · Routing/Escalation/Delegation · Multi-Region/Tenant / §186 Routing / Escalation / Delegation 缺陷 | P1 | `src/platform/orchestration/agent-delegation/delegation-governance-service.ts:244-252` | delegationDepth 条件短路后续检查——depth+risk 组合规则永不匹配 | 已解决（源码修复） | 根因是 `matchesCondition()` 在 depth 条件满足时直接 `return true` 短路，导致后续 permissionActions 和 riskLevel 检查被跳过，depth+risk 组合规则无法生效。 | 当前 `delegation-governance-service.ts:244-252` 已移除短路 `return true`——depth 条件满足时继续执行剩余检查（AND 语义），只有所有条件都满足才返回 true。 | 关闭的是"depth 条件满足时过早短路导致组合规则失效"的缺陷。
| 2185 | Round 37 — State-Transition/Recovery · Desktop/Mobile · Routing/Escalation/Delegation · Multi-Region/Tenant / §186 Routing / Escalation / Delegation 缺陷 | P1 | `src/platform/orchestration/agent-delegation/collaboration-protocol/invariant-enforcer.ts:82` | depth<=maxDepth 用 parent depth——实际允许 max+1 | 已修复（源码） | 根因是 `checkDepthLimit()` 在调用时把 `context.globalCallDepth` 当作允许上限传给了 `depth <= maxDepth` 比较，但 `globalCallDepth` 本身是 parent depth（已经被占据的深度），不是 remaining depth，导致 child 深度实际上是 parent depth + 1。 | 本轮在 `invariant-enforcer.ts:82` 把 `context.globalCallDepth` 改为 `context.globalCallDepth - 1`，使 max 变为 parent 允许的 remaining depth 而非 total cap；`checkDepthLimit(depth, max)` 的语义从“child depth <= parent cap”改为“child depth <= parent remaining”。 | 关闭的是"允许 delegation 比 max 深一层"的逻辑缺陷。 |
| 2186 | Round 37 — State-Transition/Recovery · Desktop/Mobile · Routing/Escalation/Delegation · Multi-Region/Tenant / §186 Routing / Escalation / Delegation 缺陷 | P1 | `src/platform/orchestration/agent-delegation/delegation-governance-service.ts:191-193` | addRule 允许重复 ruleId——delete 只删首个,重复 deny 不可覆盖 | 已解决 | 根因是 `addRule` 此前没有查重，直接 push 允许同 ruleId 多份；`removeRule` 只删首个，导致重复 deny 无法覆盖。 | 当前 `addRule` 已在 push 前做 `findIndex` 查重，遇重复则 `this.rules[existingIdx] = rule` 原地更新而非追加；`removeRule` 本身始终删首个，由于不再有重复 ruleId，效果即覆盖。 | 关闭的是"addRule 不防重导致 removeRule 失效"的真实缺陷。 |
| 2187 | Round 37 — State-Transition/Recovery · Desktop/Mobile · Routing/Escalation/Delegation · Multi-Region/Tenant / §186 Routing / Escalation / Delegation 缺陷 | P1 | `src/platform/orchestration/improve-rollout/rollout/rollout-state-machine.ts:18-29` | 自转换允许——重复记录+重置 transitionedAt 绕过 dwell-time | 已解决（源码修复） | 根因是 `RolloutStateMachine.transition()` 此前只校验目标状态是否在允许列表中，未排除源==目标的自转换，导致同一状态到自身的转换会重复记录并重置 `transitionedAt`，绕过 dwell-time 约束。 | 当前 `src/platform/orchestration/improve-rollout/rollout/rollout-state-machine.ts` 在允许列表校验前增加 `currentStatus === targetStatus` 的自转换检查，违例时直接抛错 `Self-transition not allowed`。 | 关闭的是"自转换绕过 dwell-time"的缺陷；转型前会先经 `ROLLOUT_TRANSITIONS` 校验合法目标，再校验非自转换。 |
| 2188 | Round 37 — State-Transition/Recovery · Desktop/Mobile · Routing/Escalation/Delegation · Multi-Region/Tenant / §186 Routing / Escalation / Delegation 缺陷 | P1 | `src/platform/five-plane-orchestration/learn/knowledge-promotion-service.ts:98-120` | 批量 promotion event 仅引用首个 object——其余丢失 lineage | 已解决 | 根因是旧实现仅在批量推广后发布单个事件，且 payload 硬编码 `learningObjects[0]`，导致 N>1 时第 2..N 个对象的 lineage 丢失。 | `src/platform/five-plane-orchestration/learn/knowledge-promotion-service.ts` 现行实现已改为在循环中对每个 promoted 对象独立查找对应 LearningObject 并发布独立事件；每条事件的 payload 正确引用各自对象的 `learningObjectId`、`learningType`、`documentId`，不再是硬编码的首个对象。 | 回写现行源码依据。 |
| 2189 | Round 37 — State-Transition/Recovery · Desktop/Mobile · Routing/Escalation/Delegation · Multi-Region/Tenant / §186 Routing / Escalation / Delegation 缺陷 | P2 | `src/platform/orchestration/agent-delegation/delegation-tracker.ts:281-285` | getMetrics activeCount 恒等于 nodes 总数——completed/failed 恒0 | 已解决（源码修复） | 根因是 `getMetrics()` 对所有节点无条件 `activeCount++`，未按实际状态区分 completed/failed。 | 当前 `DelegationChainNode` 已添加 `status: DelegationStatus` 字段，`recordDelegation()` 在创建节点时从 `delegation.status` 初始化该字段；`getMetrics()` 现按 `node.status` 分三类计数——`completed` 计入 `completedCount`，`failed/cancelled/expired/timed_out` 计入 `failedCount`，其余计入 `activeCount`。 | 回写现行源码依据。 |
| 2190 | Round 37 — State-Transition/Recovery · Desktop/Mobile · Routing/Escalation/Delegation · Multi-Region/Tenant / §186 Routing / Escalation / Delegation 缺陷 | P2 | `src/platform/orchestration/learn/learning-object-validator.ts:63-67` | validateMany 静默丢弃无效对象无审计——诊断信号丢失 | 已解决 | 根因是 `validateMany()` 旧实现直接 filter+map 返回有效对象，没有任何诊断信号；调试被拒绝对象时无法获知被拒原因和对象 ID。 | 当前 `src/platform/orchestration/learn/learning-object-validator.ts` 的 `validateMany()` 已在 filter 前增加 `console.warn` 审计日志，输出 `{id, reason}` 结构的被拒对象列表（R16-16 FIX 注释可证）。 | 回写现行源码依据。 |
| 2191 | Round 37 — State-Transition/Recovery · Desktop/Mobile · Routing/Escalation/Delegation · Multi-Region/Tenant / §186 Routing / Escalation / Delegation 缺陷 | P2 | `src/platform/orchestration/improve-rollout/canary-traffic-router.ts:29-34` | hash 函数短 ID 有偏——canary_5 实际偏离目标 | 已解决（源码已修复） | 根因是旧 `hashToBucket` 使用 `*31` 简单乘法哈希，对短 taskId（如 `task_001`）产生非均匀分布，canary_5 bucket 实际偏离 5% 目标值。 | 当前 `src/platform/orchestration/improve-rollout/canary-traffic-router.ts` 的 `hashToBucket()` 已改用 FNV-1a 哈希算法（FNV_OFFSET_BASIS=2166136261, FNV_PRIME=16777619），替代原 djb2 乘法哈希；FNV-1a 有更好的雪崩行为和短字符串分布，本行 typecheck 已通过。 | 源码已修复并通过 typecheck，回写修复依据。 |
| 2192 | Round 37 — State-Transition/Recovery · Desktop/Mobile · Routing/Escalation/Delegation · Multi-Region/Tenant / §187 Multi-Region / Tenant / SLA 缺陷 | P0 | `src/scale-ecosystem/tenant-platform/data-plane-flow-service.ts:613` | scope null 绕过租户隔离——数据跨 tenant 泄漏到全局命名空间 | 已解决 | 根因是旧边界检查把 `null` scope 当成“无约束”，只比较双方都非空时是否相等，于是 tenant/org/workspace 任一侧为 `null` 时都可能绕过隔离。 | 当前 `src/scale-ecosystem/tenant-platform/data-plane-flow-service.ts` 的 `assertScopeCompatibility()` 已对 tenant、organization、workspace 三层都显式处理 `null !== non-null` 的不对称越界场景；`tests/integration/platform/security/data-plane-flow-boundary.test.ts` 已定向证明跨租户 movement 会 fail-close 抛 `data_plane.cross_tenant_movement_denied`。 | 回写现行源码与定向测试依据，不再维持旧结论。 |
| 2193 | Round 37 — State-Transition/Recovery · Desktop/Mobile · Routing/Escalation/Delegation · Multi-Region/Tenant / §187 Multi-Region / Tenant / SLA 缺陷 | P0 | `src/scale-ecosystem/multi-region/region-health-check-service.ts:330-331` | latency 降级是死代码——引用等式永不匹配,高延迟地区恒 healthy | 已解决 | 根因是早期延迟降级分支确实曾把阈值判断写成等值比较，导致“超过阈值”的高延迟路径永远命不中；现行实现已改成真正的越界判定，但 reaudit 没有同步回写。 | `src/scale-ecosystem/multi-region/region-health-check-service.ts` 当前已用 `latencyMs > config.thresholds.maxLatencyMs` 触发 `degraded`；本轮新增 `tests/unit/scale-ecosystem/multi-region/round37-multi-region-regressions-2193-2198.test.ts` 用例 `RegionHealthCheckService marks a region degraded when latency exceeds the threshold`，并与现有 `region-health-check-service.test.ts` 一起证明高延迟区域不会再被误判为 healthy。 | 回写现行源码与定向测试依据。 |
| 2194 | Round 37 — State-Transition/Recovery · Desktop/Mobile · Routing/Escalation/Delegation · Multi-Region/Tenant / §187 Multi-Region / Tenant / SLA 缺陷 | P0 | `src/scale-ecosystem/multi-region/data-replicator/index.ts:112-114` | timer flush 返回值被丢弃——事件永久丢失 | 已修复 | 根因比旧描述更具体：问题后来不只是“`flushTimer()` 返回值被丢弃”，而是 `ReplicationEventBuffer` 的定时 flush 即便拿到了事件，也没有重新回到 `DataReplicatorService.flush()` 的发送、重试和 checkpoint 主链，而是被 buffer 内部清空后静默吞掉。 | 本轮在 `src/scale-ecosystem/multi-region/data-replicator/index.ts` 为 `ReplicationEventBuffer` 增加 `onFlushRequested` 回调，并让 `DataReplicatorService` 为每个 region 把定时 flush 重新接到 `void this.flush(regionId)`；新增 `tests/unit/scale-ecosystem/multi-region/round37-multi-region-regressions-2193-2198.test.ts` 用例 `DataReplicatorService scheduled flush emits buffered events and checkpoints them` 已证明定时到期后事件会被真正发送并写入 checkpoint。 | 已改生产代码与定向测试。 |
| 2195 | Round 37 — State-Transition/Recovery · Desktop/Mobile · Routing/Escalation/Delegation · Multi-Region/Tenant / §187 Multi-Region / Tenant / SLA 缺陷 | P0 | `src/scale-ecosystem/sla-engine/resource-allocator/index.ts:6-10` | 无 ≤100% 验证——tiers 超额分配物理不存在的容量 | 已解决 | 根因是旧资源分配器只按百分比做乘法分摊，没有先校验单 tier 百分比和总保留比例是否合法；后续实现还进一步把“总比例正好 100%”也视为 fail-closed，以保留运行时 headroom。 | 当前 `src/scale-ecosystem/sla-engine/resource-allocator/index.ts` 已对 `reservedPercent > 100`、`reservedPercent < 0` 和 `totalReservedPercent >= 100` 直接抛错；本轮新增 `tests/unit/scale-ecosystem/multi-region/round37-multi-region-regressions-2193-2198.test.ts` 的 `allocateReservedCapacity rejects cumulative reserved percentages at or above capacity`，定向证明超额和满额分配都会被拒绝。 | 回写现行源码与定向测试依据。 |
| 2196 | Round 37 — State-Transition/Recovery · Desktop/Mobile · Routing/Escalation/Delegation · Multi-Region/Tenant / §187 Multi-Region / Tenant / SLA 缺陷 | P0 | `src/scale-ecosystem/multi-region/cdc-replication-service.ts:277-280` | 复制队列只入不出——无界内存增长至 OOM | 已解决 | 根因是旧 CDC 服务只有入队，没有在确认或失败重排时真正从队列头移除/去重；现行实现已经补上确认出队、失败重排和重启后恢复，但早期条目没有跟进。 | 当前 `src/scale-ecosystem/multi-region/cdc-replication-service.ts` 已有 `confirmBatch()` 对队头批次出队、`recordFailure()/requeueBatch()` 对失败批次去重后回队，并把 `replicationQueues` 持久化到状态文件；定向验证见 `tests/unit/scale-ecosystem/multi-region/cdc-replication-service.test.ts` 的 `CDCReplicationService persists pending queues and keeps failed batch retryable across restart`，本轮单独复跑 `1 passed / 0 failed / 0 skipped`。 | 回写现行源码与定向测试依据。 |
| 2197 | Round 37 — State-Transition/Recovery · Desktop/Mobile · Routing/Escalation/Delegation · Multi-Region/Tenant / §187 Multi-Region / Tenant / SLA 缺陷 | P1 | `src/scale-ecosystem/multi-region/data-replicator/index.ts:246` | checkpoint pendingCount 用 total 非 errors——复制 lag 永久虚高 | 已解决 | 根因是旧 checkpoint 计算混淆了“尝试过的事件总数”和“仍未确认的事件数”，导致 lag 统计无法反映真实未确认数量。 | 当前 `src/scale-ecosystem/multi-region/data-replicator/index.ts` 已用 `events.length - lastSequence` 计算 `actualPendingCount`；本轮新增 `tests/unit/scale-ecosystem/multi-region/round37-multi-region-regressions-2193-2198.test.ts` 用例 `DataReplicatorService checkpoint pendingCount tracks only unconfirmed events`，定向证明 2 条事件里 1 条成功、1 条最终失败时，checkpoint `pendingCount === 1` 而不是把已确认事件也继续算作 pending。 | 回写现行源码与定向测试依据。 |
| 2198 | Round 37 — State-Transition/Recovery · Desktop/Mobile · Routing/Escalation/Delegation · Multi-Region/Tenant / §187 Multi-Region / Tenant / SLA 缺陷 | P1 | `src/scale-ecosystem/multi-region/data-replicator/index.ts:218-234` | retry 成功后 sequence 双重计数——ReplicationResult 不一致 | 已解决 | 根因是旧重试路径在 retry 成功后会再次推进 sequence，导致同一事件在主执行和重试成功两个分支里被重复计数。 | 当前实现只在事件最终确认成功时推进一次 sequence；本轮新增 `tests/unit/scale-ecosystem/multi-region/round37-multi-region-regressions-2193-2198.test.ts` 用例 `DataReplicatorService does not double-count sequence after a successful retry`，定向证明单事件首轮失败、重试成功后 `lastSequence === 1`、`eventsReplicated === 1`、checkpoint `sequenceNumber === 1`。 | 回写现行源码与定向测试依据。 |
| 2199 | Round 37 — State-Transition/Recovery · Desktop/Mobile · Routing/Escalation/Delegation · Multi-Region/Tenant / §187 Multi-Region / Tenant / SLA 缺陷 | P1 | `src/scale-ecosystem/multi-region/region-health-check-service.ts:347-349` | degraded 不累积 consecutiveFailures——failover 阈值永不触发 | 已修复 | 根因是 `updateHealthState()` 在 `result.status === “healthy” || result.status === “degraded”` 时都重置 `consecutiveFailures`，导致 degraded 状态的 region 永远无法累积到 failover 阈值。修复后只有 `healthy` 才重置，degraded 与 unhealthy 均继续累积。 | `src/scale-ecosystem/multi-region/region-health-check-service.ts` 的 `updateHealthState()` 已改为仅在 `result.status === “healthy”` 时重置 `consecutiveFailures`；`shouldFailover()` 的 failover 触发条件（unhealthy / breaker open / consecutiveFailures >= retryCount）现在对 degraded region 也能正常累积。 | 真实源码修改，fix 于 2026-05-07。
| 2200 | Round 37 — State-Transition/Recovery · Desktop/Mobile · Routing/Escalation/Delegation · Multi-Region/Tenant / §187 Multi-Region / Tenant / SLA 缺陷 | P1 | `src/scale-ecosystem/multi-region/region-health-check-service.ts:240-248` | 串行 health check——N regions O(N\*T) 延迟非 O(T) | 已解决（源码复核并补并发证据） | 根因是旧 `checkAllRegions()` 确实曾串行 `await` 每个 region；现行实现已改成 `Promise.all(...)`，但 reaudit 没有同步补上可证明它并发执行的测试。 | 当前 `src/scale-ecosystem/multi-region/region-health-check-service.ts` 的 `checkAllRegions()` 已并发调用 `checkRegion()`；`tests/unit/scale-ecosystem/multi-region/region-health-check-service.test.ts` 现有用例 `checkAllRegions runs checks in parallel` 已证明 3 个 40ms 检查总耗时低于串行上界。 | 回写现行并发实现与定向证据，不再维持“仍串行”的结论。 |
| 2201 | Round 37 — State-Transition/Recovery · Desktop/Mobile · Routing/Escalation/Delegation · Multi-Region/Tenant / §187 Multi-Region / Tenant / SLA 缺陷 | P1 | `src/platform/execution/startup/startup-consistency-checker.ts:470-474` | fail_closed 不阻止接收流量——报告但不执行 | 需继续整改 | 现行实现缺少审计指出的关键约束、状态校验、事务边界或资源控制；本轮未见相反证据。 | 已按源审计条目补录编号；本轮未新增可证明修复完成的源码、contract 或定向测试证据，因此维持原结论。 | 补录到 reaudit，待专项整改。 |
| 2202 | Round 37 — State-Transition/Recovery · Desktop/Mobile · Routing/Escalation/Delegation · Multi-Region/Tenant / §187 Multi-Region / Tenant / SLA 缺陷 | P1 | `src/scale-ecosystem/multi-region/failover-controller/index.ts:34-36` | failover 盲选 candidates[0]——无 health/latency 检查 | 已解决（源码修复并补定向测试） | 根因是 `RegionFailoverController.resolveRegionFailover()` 之前没有候选评分逻辑，既不看 circuit breaker 状态，也不接纳候选健康/延迟/错误率信号；没有 `preferredRegionId` 时只能退化为数组顺序。 | 当前 `src/scale-ecosystem/multi-region/failover-controller/index.ts` 已新增 `candidateRegionSignals` 与 `selectTargetRegion()`，按 preferred、breaker 状态、健康标记、延迟、错误率和优先级综合排序，并跳过 open/不健康候选；`tests/unit/scale-ecosystem/multi-region/failover-controller-issues.test.ts` 与 `failover-controller.test.ts` 已验证不会再因输入顺序盲选首项。 | 关闭的是 failover 目标选择算法缺失，不夸大为整个 multi-region 编排层都已完成。 |
| 2203 | Round 37 — State-Transition/Recovery · Desktop/Mobile · Routing/Escalation/Delegation · Multi-Region/Tenant / §187 Multi-Region / Tenant / SLA 缺陷 | P2 | `src/scale-ecosystem/multi-region/remote-session-state.ts:14-15` | failed→connected 无过渡——掩盖未解决故障 | 已解决 | 根因不是当前源码仍允许 `failed -> connected` 直跳，而是 reaudit 条目和 `tests/unit/scale-ecosystem/multi-region/remote-session-state.test.ts` 都停留在更早的 bug 叙述；该单测甚至还保留了错误的相对导入路径，导致它本身无法加载现行源码。 | 当前 `src/scale-ecosystem/multi-region/remote-session-state.ts` 在 `current === "failed" && signal === "connected"` 时显式返回 `"connecting"`，要求先走重连握手再回到 `"connected"`；本轮已修正 `tests/unit/scale-ecosystem/multi-region/remote-session-state.test.ts` 的导入路径与断言，并定向通过 `10 passed / 0 failed / 0 skipped`。 | 回写现行状态机语义与定向测试依据，不再维持旧结论。 |
| 2204 | Round 37 — State-Transition/Recovery · Desktop/Mobile · Routing/Escalation/Delegation · Multi-Region/Tenant / §187 Multi-Region / Tenant / SLA 缺陷 | P2 | `src/scale-ecosystem/multi-region/data-replicator/index.ts:170` | event ID Date.now()+random 非唯一保证——并发碰撞 | 已解决 | 根因是旧 `recordEvent()` 把 `Date.now()` 与 `Math.random()` 拼成 eventId，这既没有强唯一性，也无法给高并发写入提供碰撞边界。 | 当前 `src/scale-ecosystem/multi-region/data-replicator/index.ts` 已改为 `globalThis.crypto.randomUUID()` 生成 eventId；`tests/unit/scale-ecosystem/multi-region/data-replicator/index.test.ts` 的 `DataReplicatorService generates unique event IDs under rapid writes` 已定向证明 200 次快速写入不会重复。 | 回写现行源码与定向测试依据。 |
| 2205 | Round 38 — Platform Contracts/Types · UI Features · Contract Docs · Event Bus/Truth/Harness / §189 Platform Contracts / Types 类型系统冲突 | P0 | `src/platform/contracts/executable-contracts/index.ts:67 vs types/domain/task-types.ts:29` | ArtifactRef 双定义不兼容——字段集完全不同,barrel 导出 type shadowing | 已解决（源码复核） | 根因是旧 `domain/task-types.ts` 确实曾保留一份 legacy `ArtifactRef` 形状，后续迁移把它降成兼容别名，但审计表没跟上。 | 当前 `src/platform/contracts/types/domain/task-types.ts` 已把旧结构改名为 `LegacyArtifactRef` 并标注 `@deprecated`，同时直接 `export type { ArtifactRef } from "../../executable-contracts/index.js"`；对外 canonical `ArtifactRef` 只有 executable-contracts 一份。 | 不改生产代码，关闭 ArtifactRef type shadowing 旧结论。 |
| 2206 | Round 38 — Platform Contracts/Types · UI Features · Contract Docs · Event Bus/Truth/Harness / §189 Platform Contracts / Types 类型系统冲突 | P0 | `src/platform/contracts/execution-plan/index.ts:12 vs types/platform-contracts.ts:70` | ExecutionPlan 双定义——steps 类型/budget/traceId 全部冲突 | 已解决（源码复核） | 根因是审计条目停留在 `types/platform-contracts.ts` 仍自带一套 ExecutionPlan 的旧版本；后续已收敛为单点 re-export。 | 当前 `src/platform/contracts/types/platform-contracts.ts` 只做 `export { createExecutionPlan, type ExecutionPlan } from "../execution-plan/index.js"`，不再自定义第二套 ExecutionPlan；`src/platform/contracts/execution-plan/index.ts` 则把 `ExecutionPlan` 降为 `@deprecated` 的 `PlanGraphBundle` 兼容别名并对工厂 fail-fast。 | 不改生产代码，关闭“双定义冲突”旧结论。 |
| 2207 | Round 38 — Platform Contracts/Types · UI Features · Contract Docs · Event Bus/Truth/Harness / §189 Platform Contracts / Types 类型系统冲突 | P0 | `src/platform/contracts/execution-receipt/index.ts:6 vs types/platform-contracts.ts:89` | ExecutionReceipt 双定义——status enum 完全不同 | 已解决（源码复核） | 根因同样是审计基于 `types/platform-contracts.ts` 仍持有第二套 receipt 定义的旧快照。 | 当前 `src/platform/contracts/types/platform-contracts.ts` 已仅 re-export `../execution-receipt/index.js`，不存在第二套 status enum；legacy `ExecutionReceipt` 仍保留在 `execution-receipt/index.ts` 作为 `@deprecated` 兼容类型，并由 `createExecutionReceipt()` fail-fast 引导迁移到 `NodeAttemptReceipt`。 | 不改生产代码，关闭 ExecutionReceipt 双定义旧结论。 |
| 2208 | Round 38 — Platform Contracts/Types · UI Features · Contract Docs · Event Bus/Truth/Harness / §189 Platform Contracts / Types 类型系统冲突 | P0 | `src/platform/contracts/control-directive/index.ts:6 vs types/platform-contracts.ts:37` | ControlDirective 双定义——kind/type/issuedBy 类型/scope 全冲突 | 已解决（源码复核） | 根因是 `types/platform-contracts.ts` 旧版曾自带另一套 control directive 映射；现行文件已改为单点 re-export。 | 当前 `src/platform/contracts/types/platform-contracts.ts` 只从 `../control-directive/index.js` re-export `createControlDirective` 与 `type ControlDirective`；`src/platform/contracts/control-directive/index.ts` 中 legacy `ControlDirective` 也已被标记为 `@deprecated`，canonical 替代为 `OperationalDirective / DecisionDirective`。 | 不改生产代码，关闭 ControlDirective 双定义旧结论。 |
| 2209 | Round 38 — Platform Contracts/Types · UI Features · Contract Docs · Event Bus/Truth/Harness / §189 Platform Contracts / Types 类型系统冲突 | P0 | `src/platform/contracts/state-command/index.ts:6 vs types/platform-contracts.ts:106` | StateCommand 双定义——action/type/entityId/aggregateId 冲突,子目录版缺 fencingToken | 已解决（源码复核） | 根因是 `types/platform-contracts.ts` 旧版曾继续包一层 state-command 类型映射，审计未跟上后续收敛。 | 当前 `src/platform/contracts/types/platform-contracts.ts` 已只 re-export `../state-command/index.js` 的 `createStateCommand` 与 `type StateCommand`，不再存在第二套并行定义；legacy `state-command/index.ts` 仍保留兼容输入归一化，但类型来源已单点化。 | 不改生产代码，关闭 StateCommand 双定义旧结论。 |
| 2210 | Round 38 — Platform Contracts/Types · UI Features · Contract Docs · Event Bus/Truth/Harness / §189 Platform Contracts / Types 类型系统冲突 | P0 | `src/platform/contracts/request-envelope/index.ts:4 + types/platform-contracts.ts:12 + executable-contracts/index.ts:126` | RequestEnvelope 三重定义——三套不兼容字段集 | 已解决（源码修复并补定向验证） | 根因是 `request-envelope/` 的 legacy envelope 之外，`types/platform-contracts.ts` 还继续把 `RequestEnvelopeLegacy` 重新导出成 `RequestEnvelope`，于是 canonical `executable-contracts`、legacy module 和 platform barrel 三个入口同时暴露同名异形对象。 | 当前 `src/platform/contracts/types/platform-contracts.ts:31-59` 已改为只 re-export canonical `RequestEnvelope`，legacy 兼容类型保留为 `RequestEnvelopeLegacy`；`src/platform/contracts/request-envelope/index.ts` 继续显式标记 deprecated，canonical `RequestEnvelope` 则只来自 `src/platform/contracts/executable-contracts/index.ts:212-230`。对应 contract/schema 回归 `tests/unit/platform/contracts/executable-contracts/schemas-validation.test.ts` 与 `tests/unit/platform/contracts/types/platform-contracts.test.ts` 已随本轮通过。 | 把 platform-contracts 上的同名第三套 `RequestEnvelope` 去掉，收敛为“canonical + legacy alias”两层。 |
| 2211 | Round 38 — Platform Contracts/Types · UI Features · Contract Docs · Event Bus/Truth/Harness / §189 Platform Contracts / Types 类型系统冲突 | P1 | `src/platform/contracts/executable-contracts/index.ts:158` | HarnessRun 缺 §5.5 字段:orgId/budgetEnvelope/riskProfile/auditTrail/fencingToken | 已解决（源码修复并补定向验证） | 根因是 `HarnessRun` 初版只覆盖执行主链最小字段，预算/风险/审计上下文都分散在别处，导致 run contract 本身无法独立表达 governance 与 budget 边界。 | 当前 `src/platform/contracts/executable-contracts/index.ts:233-296,997-1055` 已新增 `HarnessBudgetEnvelope`、`HarnessAuditTrail`，并把 `HarnessRun` 扩展为包含 `orgId / riskProfile / auditTrail / budgetEnvelope / fencingToken`；`src/platform/contracts/executable-contracts/schemas.ts:144-199` 同步要求这些字段；`tests/unit/platform/contracts/executable-contracts-factories.test.ts:479-495` 已验证 `createHarnessRun()` 会真实回填这些 canonical 字段。 | 关闭的是 HarnessRun contract 缺 canonical governance/budget metadata 的真实缺口。 |
| 2212 | Round 38 — Platform Contracts/Types · UI Features · Contract Docs · Event Bus/Truth/Harness / §189 Platform Contracts / Types 类型系统冲突 | P1 | `src/platform/contracts/executable-contracts/index.ts:316` | NodeRun 缺 §5.5 字段:sideEffects[]/compensation 未内嵌,leaseId/fencingToken 可选 | 已解决（源码修复并补定向验证） | 根因是 `NodeRun` 早期只建模调度与执行状态，没有把 side effect / compensation lineage 内嵌到 run contract；同时 factory 在未显式传值时不会产出稳定 fencing token。`leaseId` 审计条目把“未 leased 的 NodeRun 允许无 lease”误写成缺陷，真正需要保证的是 fencing token 与 side-effect lineage。 | 当前 `src/platform/contracts/executable-contracts/index.ts:440-453,1167-1188` 已为 `NodeRun` 增加 `sideEffects[] / compensation[] / fencingToken`，`createNodeRun()` 默认回填空数组和稳定 fencing token；`src/platform/contracts/executable-contracts/schemas.ts:335-348` 也已要求这些字段；`tests/unit/platform/contracts/executable-contracts-factories.test.ts:498-510` 已验证回填行为。 | 修的是 NodeRun 缺 side-effect/compensation lineage 与 fencing identity 的真实问题；`leaseId` 仍按“仅 leased 时存在”建模。 |
| 2213 | Round 38 — Platform Contracts/Types · UI Features · Contract Docs · Event Bus/Truth/Harness / §189 Platform Contracts / Types 类型系统冲突 | P1 | `src/platform/contracts/executable-contracts/index.ts:583` | PlatformFactEvent 缺 source/correlationId(必填)/schemaVersion | 已解决（源码修复并补定向验证） | 根因是 `PlatformFactEvent` 之前只继承通用 `EventEnvelope`，虽然已有 `schemaVersion`，但平台事实层没有把 `source` 和必填 `correlationId` 强制到工厂与 schema 上，消费者只能靠调用方自觉补齐。 | 当前 `src/platform/contracts/executable-contracts/index.ts:732-735,1617-1655` 已把 `PlatformFactEvent` 显式扩展为包含 `source` 与必填 `correlationId`，`createPlatformFactEvent()` 默认回填 `correlationId=runId`、`source=platform-runtime`；`src/platform/contracts/executable-contracts/schemas.ts:595-598` 也已同步强制；`tests/unit/platform/contracts/executable-contracts-factories.test.ts:1279-1295` 与 schema 回归已通过。 | 关闭的是 PlatformFactEvent provenance 字段不稳定的问题，不把通用 EventEnvelope 已有 `schemaVersion` 误记成缺失。 |
| 2214 | Round 38 — Platform Contracts/Types · UI Features · Contract Docs · Event Bus/Truth/Harness / §189 Platform Contracts / Types 类型系统冲突 | P1 | `整个代码库` | ContractEnvelope 类型完全不存在——§5.5 要求 {version,schema,payload,signature,ttl} | 已解决（源码复核） | 根因是审计条目建立在 ContractEnvelope 引入前的快照，没有吸收后续 inter-plane envelope 落地。 | 当前 `src/platform/contracts/executable-contracts/index.ts` 已提供 `ContractEnvelope<TPayload>`、`createContractEnvelope()`、`signContractEnvelope()` 与 `verifyContractEnvelopeSignature()`，字段包含 `version / schema / payload / signature / ttl`。 | 不改生产代码，关闭 ContractEnvelope 缺失结论。 |
| 2215 | Round 38 — Platform Contracts/Types · UI Features · Contract Docs · Event Bus/Truth/Harness / §189 Platform Contracts / Types 类型系统冲突 | P1 | `src/platform/contracts/executable-contracts/schemas.ts:574 vs index.ts:580` | replayBehavior "simulate" vs "simulate_projection" 字符串不匹配——运行时比对失败 | 已解决（源码复核） | 根因是旧 factory 默认值与 schema 枚举曾短暂漂移；现行代码已重新对齐，但审计表未刷新。 | 当前 `src/platform/contracts/executable-contracts/schemas.ts` 的 `replayBehaviorFor("OapeflirViewEvent")` 返回 `"simulate"`，而 `src/platform/contracts/executable-contracts/index.ts` 中 `createOapeflirViewEvent(...)` 的默认 `replayBehavior` 也同样为 `"simulate"`，不存在 `"simulate_projection"` 残留。 | 不改生产代码，关闭 replayBehavior 字符串漂移。 |
| 2216 | Round 38 — Platform Contracts/Types · UI Features · Contract Docs · Event Bus/Truth/Harness / §189 Platform Contracts / Types 类型系统冲突 | P1 | `src/platform-architecture-types.ts:1` | 架构层类型文件零引用 §5.5 canonical types——canonical object map 无代码表示 | 已解决（源码复核） | 根因是审计没有复核后续对架构层类型文件的补全。 | 当前 `src/platform-architecture-types.ts` 已显式定义 `NodeRun`、`PlanGraphBundle`、`PlanGraph`、`PlanNode`、`BudgetReservation`、`ArtifactRef` 等 canonical object map，已不再是“无代码表示”。 | 不改生产代码，回写架构层类型文件现状。 |
| 2217 | Round 38 — Platform Contracts/Types · UI Features · Contract Docs · Event Bus/Truth/Harness / §189 Platform Contracts / Types 类型系统冲突 | P2 | `src/platform/contracts/index.ts:8-31` | barrel 导出 RequestEnvelope 三个别名——消费方混淆 | 已解决（源码复核） | 根因是旧 barrel 曾同时暴露多个 RequestEnvelope 命名入口；现行导出面已经明显收敛。 | 当前 `src/platform/contracts/index.ts` 只额外暴露一个 canonical 别名 `PlatformRequestEnvelope`，并把 legacy `requestEnvelopeContract` 降为单独的 deprecated namespace；已不存在“三个同层别名并列导出”的情况。 | 不改生产代码，关闭 barrel 三重别名旧结论。 |
| 2218 | Round 38 — Platform Contracts/Types · UI Features · Contract Docs · Event Bus/Truth/Harness / §189 Platform Contracts / Types 类型系统冲突 | P2 | `src/platform/contracts/types/platform-contracts.ts:55 vs executable-contracts/index.ts:407` | SideEffectRecord 双定义——effectId vs sideEffectId,6字段 vs 12+字段 | 已解决（源码复核） | 根因是原审计把 `SideEffectExpectation` 误读成第二份活跃 `SideEffectRecord`。 | 当前 `src/platform/contracts/types/platform-contracts.ts` 已只 `export type { SideEffectRecord } from "../executable-contracts/index.js"`，本地保留的是不同语义、带 `@deprecated` 的 `SideEffectExpectation`；canonical `SideEffectRecord` 只有 executable-contracts 一份。 | 不改生产代码，关闭 SideEffectRecord 双定义旧结论。 |
| 2219 | Round 38 — Platform Contracts/Types · UI Features · Contract Docs · Event Bus/Truth/Harness / §190 Contract 文档偏差（续） | P0 | `docs_zh/contracts/ (缺失)` | 14/16 spec-required contracts 不存在(distributed_consensus/data_lifecycle/execution_sandbox/evidence_chain/federation/harness_run_lifecycle/hitl/knowledge_lifecycle/prompt_management/multi_region_replication/recovery/risk_assessment/security_baseline/tenant_isolation) | 已解决（contract/文档回写） | 根因是审计时这些 spec-required contract 还未落盘。 | 当前仓库已补齐 `distributed_consensus_contract.md`、`data_lifecycle_contract.md`、`execution_sandbox_contract.md`、`evidence_chain_contract.md`、`federation_contract.md`、`harness_run_lifecycle_contract.md`、`hitl_contract.md`、`knowledge_lifecycle_contract.md`、`prompt_management_contract.md`、`multi_region_replication_contract.md`、`recovery_contract.md`、`risk_assessment_contract.md`、`security_baseline_contract.md`、`tenant_isolation_contract.md`。 | 回写缺失 contract 清单，不伪造实现层完成度。 |
| 2220 | Round 38 — Platform Contracts/Types · UI Features · Contract Docs · Event Bus/Truth/Harness / §190 Contract 文档偏差（续） | P0 | `docs_zh/contracts/app_error_contract.md:37-38` | AppError 用 task_id/execution_id——无 harness_run_id/node_run_id | 已解决（contract/文档回写） | 根因是错误合同早期围绕 task/execution 投影字段书写，没有切到 canonical runtime 关联键。 | 当前 `docs_zh/contracts/app_error_contract.md` 已把 `harness_run_id` / `node_run_id` 提升为 canonical 关联键，并把 `execution_id` 降为 legacy 查询兼容字段。 | 回写 contract，关闭错误关联键漂移。 |
| 2221 | Round 38 — Platform Contracts/Types · UI Features · Contract Docs · Event Bus/Truth/Harness / §190 Contract 文档偏差（续） | P0 | `docs_zh/contracts/model_gateway_routing_contract.md:18` | ModelRouteRequest.taskId 用 legacy 非 harnessRunId/nodeRunId | 已解决（contract/文档回写） | 根因是 model gateway route contract 沿用了任务级调用方视角，没有把运行时锚点升级到 `HarnessRun / NodeRun / Attempt`。 | 当前 `docs_zh/contracts/model_gateway_routing_contract.md` 的 `ModelRouteRequest` 已显式定义 `harnessRunId / nodeRunId / attemptId`，并将 `taskId` 保留为可选 legacy projection。 | 回写 contract，关闭 route request 关联键漂移。 |
| 2222 | Round 38 — Platform Contracts/Types · UI Features · Contract Docs · Event Bus/Truth/Harness / §190 Contract 文档偏差（续） | P1 | `docs_zh/contracts/observability_contract.md:22-29` | LogEvent 含 task_id 无 harness_run_id/node_run_id——日志无法与 runtime truth 关联 | 已解决（contract/文档回写） | 根因是观测 contract 早期把任务投影字段当作主关联键。 | 当前 `docs_zh/contracts/observability_contract.md` 的 `LogEvent`、`StageMetricSample`、`LoopIterationTrace` 等结构都已要求 `harness_run_id`，并把 `node_run_id / attempt_id` 明确为 canonical 关联字段。 | 回写 contract，关闭 observability 关联键漂移。 |
| 2223 | Round 38 — Platform Contracts/Types · UI Features · Contract Docs · Event Bus/Truth/Harness / §190 Contract 文档偏差（续） | P1 | `docs_zh/contracts/perception_intelligence_plane_contract.md:70-73` | ExecutionAssessment 用 execution_id+"steps"——应为 nodeRunId+NodeRun 计数 | 已解决（contract/文档回写） | 根因是 perception/intelligence contract 曾沿用 execution/steps 表达评估对象。 | 当前 `docs_zh/contracts/perception_intelligence_plane_contract.md` 已明确 `ExecutionAssessment` 必须锚定 `harness_run_id / node_run_id`，不得再以 `execution_id + steps[]` 充当 canonical 关联模型。 | 回写 contract，关闭 assessment 关联模型漂移。 |
| 2224 | Round 38 — Platform Contracts/Types · UI Features · Contract Docs · Event Bus/Truth/Harness / §190 Contract 文档偏差（续） | P1 | `docs_zh/contracts/audit_lineage_and_retention_contract.md:72` | audit 字段含 execution_id? 无 harness_run_id——审计记录不可链接 | 已解决（contract/文档回写） | 根因是谱系审计合同早期仍围绕 execution 查询键表达链路归属。 | 当前 `docs_zh/contracts/audit_lineage_and_retention_contract.md` 已明确 `harness_run_id / node_run_id` 为 canonical 关联键，`execution_id` 仅作为历史查询兼容。 | 回写 contract，关闭 audit linkage 漂移。 |
| 2225 | Round 38 — Platform Contracts/Types · UI Features · Contract Docs · Event Bus/Truth/Harness / §190 Contract 文档偏差（续） | P1 | `docs_zh/contracts/tool_skill_plugin_contract.md:60` | SkillDefinition 用 steps——应引用 graph nodes | 已解决（contract/文档回写） | 根因是 skill contract 曾用线性步骤语言描述执行结构。 | 当前 `docs_zh/contracts/tool_skill_plugin_contract.md` 已明确 skill 的执行结构必须引用 `PlanGraph`/entry nodes，不得把线性 `steps[]` 当作 canonical 执行模型。 | 回写 contract，关闭 skill graph 漂移。 |
| 2226 | Round 38 — Platform Contracts/Types · UI Features · Contract Docs · Event Bus/Truth/Harness / §190 Contract 文档偏差（续） | P1 | `docs_zh/contracts/dashboard_and_operator_experience_contract.md:51-58` | GuidedOnboardingSession/WorkflowBuilderDraft 用 steps——应产 PlanGraph | 已解决（contract/文档回写） | 根因是 workflow builder draft 结构没有被写成 DAG first。 | 当前 `docs_zh/contracts/dashboard_and_operator_experience_contract.md` 已把 `WorkflowBuilderDraft.plan_graph_draft` 固化为 graph 结构，并要求 UI 步骤列表只能是 graph 的只读投影。 | 回写 contract，关闭 workflow builder draft 线性模型漂移。 |
| 2227 | Round 38 — Platform Contracts/Types · UI Features · Contract Docs · Event Bus/Truth/Harness / §190 Contract 文档偏差（续） | P1 | `docs_zh/contracts/knowledge_spi_contract.md:27` | KnowledgeArchive 指定 SQLite——spec 限 MVP-only,知识平面需生产存储 | 已解决（contract/文档回写） | 根因是 Knowledge SPI 文档早期把 SQLite 写成默认实现而不是 MVP/local only 限定。 | 当前 `docs_zh/contracts/knowledge_spi_contract.md` 已明确 `KnowledgeArchive` 为“实现可插拔；SQLite 仅允许作为 MVP / 本地模式”。 | 回写 contract，关闭 KnowledgeArchive 存储表述漂移。 |
| 2228 | Round 38 — Platform Contracts/Types · UI Features · Contract Docs · Event Bus/Truth/Harness / §190 Contract 文档偏差（续） | P1 | `docs_zh/contracts/cross_region_routing_and_data_residency_contract.md:13` | ReplicationPolicy 列为 canonical 但无字段定义 | 已解决（contract/文档回写） | 根因是 cross-region contract 早期只列名不建模。 | 当前 `docs_zh/contracts/cross_region_routing_and_data_residency_contract.md` 已新增 `ReplicationPolicy` 最小字段：`policy_id / primary_region_id / replica_region_ids / replication_mode / max_replication_lag_seconds / failover_mode`。 | 回写 contract，关闭 ReplicationPolicy 缺字段定义。 |
| 2229 | Round 38 — Platform Contracts/Types · UI Features · Contract Docs · Event Bus/Truth/Harness / §190 Contract 文档偏差（续） | P1 | `docs_zh/contracts/prompt_engine_spi_contract.md:16-27` | PromptDefinition 缺 tenantId——违反多租户隔离 | 已解决（contract/文档回写） | 根因是 prompt SPI 早期只把 prompt 当模板对象，没有把租户隔离写成必填边界。 | 当前 `docs_zh/contracts/prompt_engine_spi_contract.md` 已将 `tenantId` 定义为必填隔离键，并明确共享 prompt 也必须通过显式共享命名空间表达。 | 回写 contract，关闭 prompt 多租户隔离漂移。 |
| 2230 | Round 38 — Platform Contracts/Types · UI Features · Contract Docs · Event Bus/Truth/Harness / §190 Contract 文档偏差（续） | P2 | `docs_zh/contracts/observability_contract.md:67-72` | legacy metric alias taskMetrics/executionMetrics 无对应维度 | 已解决（contract/文档回写） | 根因是 observability contract 早期保留了旧 alias 名称，但没补上映射维度约束。 | 当前 `docs_zh/contracts/observability_contract.md` 已明确 `taskMetrics.*`、`executionMetrics.*` 只能作为 projection alias，必须可映射到 `HarnessRun / NodeRun / Attempt` 维度。 | 回写 contract，关闭 legacy metric alias 无维度约束。 |
| 2231 | Round 38 — Platform Contracts/Types · UI Features · Contract Docs · Event Bus/Truth/Harness / §190 Contract 文档偏差（续） | P2 | `docs_zh/contracts/model_gateway_routing_contract.md:19` | ModelRouteRequest.sessionId 无生命周期 contract 定义 | 已解决（contract/文档回写） | 根因是 route contract 曾把 `sessionId` 当普通可选字段，没有写清其生命周期边界。 | 当前 `docs_zh/contracts/model_gateway_routing_contract.md` 已新增 `sessionId` 约束，明确它只表达 gateway/conversation 生命周期、session 终止后不得继续复用旧 provider sticky 决策。 | 回写 contract，关闭 sessionId 生命周期缺口。 |
| 2232 | Round 38 — Platform Contracts/Types · UI Features · Contract Docs · Event Bus/Truth/Harness / §190 Contract 文档偏差（续） | P2 | `docs_zh/contracts/remote_coordination_and_disaster_recovery_contract.md:37-46` | 一致性章节仅 bullet 无对象定义/failure_behavior | 已解决（contract/文档回写） | 根因是远程协调/容灾合同早期只写原则，不定义一致性对象。 | 当前 `docs_zh/contracts/remote_coordination_and_disaster_recovery_contract.md` 已新增 `DisasterRecoveryConsistencyPolicy`，显式定义 `scope / consistencyMode / failureBehavior`。 | 回写 contract，关闭 DR consistency 仅 bullet 无对象定义的问题。 |
| 2233 | Round 38 — Platform Contracts/Types · UI Features · Contract Docs · Event Bus/Truth/Harness / §191 Event Bus / Truth / Harness 缺陷 | P0 | `src/platform/state-evidence/events/dlq-service.ts:155` | scheduleRetry 无 maxRetries 上限——无限重试,无 poison-pill quarantine | 已解决（源码已满足，本轮补证据） | 原审计命中的是旧实现快照；现行 `DlqService` 已在 `scheduleRetry()` 入口校验 `maxRetries`，并在 `retryCount >= maxRetries` 时转入 `markRetryExhausted()` 的终态丢弃路径，不再允许无限重试。 | 当前 `src/platform/state-evidence/events/dlq-service.ts` 已对 `maxRetries` 做 non-negative finite 校验，并在上限命中时返回 `markRetryExhausted()`；`tests/unit/platform/state-evidence/events/dlq-service.test.ts` 已覆盖 `markRetryExhausted sets retryExhaustedAt...` 与 `retry count reaches maxRetries...`。 | 不改主实现，回写源码与测试依据。 |
| 2234 | Round 38 — Platform Contracts/Types · UI Features · Contract Docs · Event Bus/Truth/Harness / §191 Event Bus / Truth / Harness 缺陷 | P0 | `src/platform/state-evidence/truth/runtime-truth-repository.ts:196` | storeAggregate 用 Map.set 覆写——truth 非 append-only 非不可变 | 已解决（源码/测试） | 根因是 `storeAggregate()` 旧实现直接 `Map.set()` 覆写同 ID aggregate，没有对已存在聚合做拒绝或冲突保护。 | 当前 `src/platform/state-evidence/truth/runtime-truth-repository.ts` 已拒绝同 ID aggregate 的覆盖写；`tests/unit/platform/state-evidence/truth/runtime-truth-repository.test.ts` 中 `seed rejects overwriting an existing aggregate` 的定向回归已通过。 | 关闭该条 truth 覆写写入缺陷。 |
| 2235 | Round 38 — Platform Contracts/Types · UI Features · Contract Docs · Event Bus/Truth/Harness / §191 Event Bus / Truth / Harness 缺陷 | P0 | `src/platform/orchestration/harness/index.ts:633-656` | runLoop planner/generator/evaluator 全在 budget gate 前执行——spec 要求每阶段前检查 | 已解决（源码/测试） | 根因有两层：一是 budget gate 旧实现放在整轮后段，planner/generator/evaluator 已经先执行；二是 harness runtime state 没有沿状态迁移链路携带 `leaseId/fencingToken`，导致把 gate 前移后执行态缺少完整上下文。 | 当前 `src/platform/five-plane-orchestration/harness/index.ts` 已把 budget gate 前移到 generator/evaluator 之前，并在线程态 `HarnessRunRuntimeState` / `toCanonicalHarnessRun()` / `transitionRunStatus()` 中补齐 `leaseId/fencingToken` 透传；`tests/unit/platform/orchestration/harness/index.test.ts` 的 `enforces budget gate before generator and evaluator stages` 已定向通过。 | 关闭该条 harness budget gate 时序缺陷。 |
| 2236 | Round 38 — Platform Contracts/Types · UI Features · Contract Docs · Event Bus/Truth/Harness / §191 Event Bus / Truth / Harness 缺陷 | P0 | `src/platform/orchestration/oapeflir/types/feedback-signal.ts:7-16` | FeedbackSignal 缺 trustScore 和 evidenceRefs——spec 必需字段 | 已解决（源码已满足，本轮补证据并校正陈旧测试） | 原审计与旧测试都停留在 legacy `taskId + stepOutputRefs` 版本，没有跟上 v4.3 feedback contract 后续补齐的 canonical correlation key 与学习管线必需字段。 | 当前 `src/platform/orchestration/oapeflir/types/feedback-signal.ts` 已强制 `harnessRunId`、`nodeRunId`、`trustScore` 与 `evidenceRefs`；本轮已将 `tests/unit/platform/orchestration/oapeflir/types/feedback-signal.test.ts` 与 `feedback-signal.test.ts` 对齐到现行 schema，并通过定向验证。 | 不改主实现，回写真实 contract 与回归依据。 |
| 2237 | Round 38 — Platform Contracts/Types · UI Features · Contract Docs · Event Bus/Truth/Harness / §191 Event Bus / Truth / Harness 缺陷 | P1 | `src/platform/state-evidence/events/durable-event-bus.ts:1101` | ensurePendingAcks 为所有 consumer 创建 ack 不过滤 eventType——虚假 pending 计数 | 已解决（源码修复并补定向测试） | 根因不是 ack 根本没建，而是 `ensurePendingAcksForActiveConsumers()` 把“事件注册表声明的消费者”和“当前活跃/已订阅消费者”直接并集，结果未注册到该 eventType 的活跃 consumer 也会被补 pending ack。 | 当前 `src/platform/state-evidence/events/durable-event-bus.ts` 已先取 `getRegisteredConsumers(event.eventType)`，再只把属于该注册集的 active/subscribed consumer 纳入 ack 创建；`tests/unit/platform/state-evidence/events/durable-event-bus-consumer-filter.test.ts` 已验证 `custom_projection` 订阅 `task:status_changed` 后不会再出现虚假 pending ack。 | 已改生产代码与定向测试。 |
| 2238 | Round 38 — Platform Contracts/Types · UI Features · Contract Docs · Event Bus/Truth/Harness / §191 Event Bus / Truth / Harness 缺陷 | P1 | `src/platform/state-evidence/events/durable-event-bus.ts:301-306` | deliverPendingNow 无 handler 时返回 pending.length 作为"已投递"——误导指标 | 已解决（源码已满足，本轮补定向测试） | 根因是复审表没有跟上 `deliverPendingNow()` 的后续修正；现行实现拿不到 handler 时会直接返回 `0`，不会把“仍 pending 的数量”误报成“已投递数量”。 | 当前 `src/platform/state-evidence/events/durable-event-bus.ts` 在 `!handler` 分支直接 `return 0`；本轮新增 `tests/unit/platform/state-evidence/events/durable-event-bus-no-handler.test.ts`，验证“有 pending 但已无 handler”时 `deliverPending()` 返回 `0` 且 pending 记录保留。 | 新增最小回归测试，文档改写为已解决。 |
| 2239 | Round 38 — Platform Contracts/Types · UI Features · Contract Docs · Event Bus/Truth/Harness / §191 Event Bus / Truth / Harness 缺陷 | P1 | `src/platform/state-evidence/events/projections/workflow-run-projection.ts:322` | subtask:failed 立即设 workflow=failed——无重试/fallback 路径 | 已解决（源码已满足，本轮补证据并校正陈旧测试） | 原审计对应的是旧投影视图语义；现行 `subtask:failed` 只记录 failed step 与错误上下文，workflow 是否最终失败改由后续 `task:status_changed -> failed` 聚合事件决定。旧测试仍在要求“子任务一失败就 workflow failed”，本轮已纠正。 | 当前 `src/platform/state-evidence/events/projections/workflow-run-projection.ts` 在 `subtask:failed` 分支不再写 `state.status = "failed"`；`tests/unit/platform/state-evidence/events/projections/workflow-run-projection-subtask-failure-regression.test.ts` 与 `workflow-run-projection.test.ts` 已验证 `subtask:failed` 保持 `pending`，仅在后续 workflow-level failed 事件到来时转终态。 | 不改主实现，修正陈旧断言并回写依据。 |
| 2240 | Round 38 — Platform Contracts/Types · UI Features · Contract Docs · Event Bus/Truth/Harness / §191 Event Bus / Truth / Harness 缺陷 | P1 | `src/platform/state-evidence/events/projections/ (all 9)` | processedEventIds 用 Array.includes() O(n) 去重——100K 事件重建 O(n²) | 已解决（源码已满足，本轮补证据） | 原审计基于早期 projection 实现；当前 projection 家族已经统一采用内部 `_processedEventIdSet: Set<string>` 进行幂等判定，只在序列化输出时回写成 `processedEventIds` 数组。 | 当前 `approval-queue / artifact-catalog / dispatch / governance / incident / risk-action / tool-usage / worker-status / workflow-run / workflow-timeline` projection 都已使用 `_processedEventIdSet.has(eventId)`；`tests/unit/platform/state-evidence/events/projections/workflow-run-projection.test.ts` 已覆盖去重与 watermark eviction，`governance-projection.test.ts` 也覆盖了幂等/重复事件行为。 | 不改主实现，回写现状源码与回归依据。 |
| 2241 | Round 38 — Platform Contracts/Types · UI Features · Contract Docs · Event Bus/Truth/Harness / §191 Event Bus / Truth / Harness 缺陷 | P1 | `src/platform/orchestration/oapeflir/assessment-service.ts:66` | routingDecision.division 硬编码 "coding"——非编码任务全部误路由 | 已解决（源码已满足，本轮补证据） | 原审计停留在 `division` 曾硬编码 `"coding"` 的旧版本；现行 `AssessmentService` 已按 domain/content 通过 `deriveDivisionCapability()` 归类，不再把 operations/data 任务统一塞进 coding。 | 当前 `src/platform/orchestration/oapeflir/assessment-service.ts` 已提供 `deriveDivisionCapability()` 并在 `routingDecision.division` 中调用；`tests/unit/platform/orchestration/oapeflir/assessment-service-extended.test.ts` 已验证 `domainId: "general_ops"` 时返回 `"ops"`。 | 不改主实现，回写现行行为依据。 |
| 2242 | Round 38 — Platform Contracts/Types · UI Features · Contract Docs · Event Bus/Truth/Harness / §191 Event Bus / Truth / Harness 缺陷 | P1 | `src/platform/state-evidence/events/layered-event-inbox.ts:21` | records 数组只增不减无压缩——内存泄漏 | 已解决（源码已满足，本轮补定向测试） | 原审计对应的是 append-only 阶段；现行 `LayeredEventInbox` 已提供显式 `compact()`，并在记录数达到阈值时自动压缩已被所有消费者消费的前缀记录。 | 当前 `src/platform/state-evidence/events/layered-event-inbox.ts` 已实现 `compact()` 与 `COMPACT_THRESHOLD` 自动压缩；本轮新增 `tests/unit/platform/state-evidence/events/layered-event-inbox-compaction.test.ts`，验证手动压缩会移除已消费记录，且达到阈值后会自动收缩缓冲区。 | 新增最小回归测试，文档改写为已解决。 |
| 2243 | Round 38 — Platform Contracts/Types · UI Features · Contract Docs · Event Bus/Truth/Harness / §191 Event Bus / Truth / Harness 缺陷 | P2 | `src/platform/orchestration/harness/index.ts:505-506` | non-accept decision 的 feedbackEnvelope 仅 assertInvariants 检查——appendStep 路径可绕过 | 已解决（源码/测试） | 根因是旧保护只放在 `assertInvariants()` 末端校验，`appendStep()` 自身没有硬阻断，调用方可以在落步阶段绕过反馈包要求。 | 当前 `src/platform/five-plane-orchestration/harness/index.ts` 的 `appendStep()` 已直接拒绝缺少 `feedbackEnvelope` 的 non-accept decision；`tests/unit/platform/orchestration/harness/index.test.ts` 的 `appendStep rejects non-accept decisions without feedback envelope` 已定向通过。 | 关闭该条 feedbackEnvelope 绕过缺陷。 |
| 2244 | Round 38 — Platform Contracts/Types · UI Features · Contract Docs · Event Bus/Truth/Harness / §191 Event Bus / Truth / Harness 缺陷 | P2 | `src/platform/state-evidence/events/projections/artifact-catalog-projection.ts:283` | artifact:updated 版本自增——乱序重放产生确定性但语义无意义的 version | 已解决（源码/测试） | 根因有两层：一是 `artifact:updated` 旧路径会自动抬升 `version`，把元数据更新也当成内容版本演进；二是 projection handler 末尾把内部态直接返回，未把 `_processedEventIdSet` 序列化回 `processedEventIds`，导致去重与重放测试无法成立。 | 当前 `src/platform/state-evidence/events/projections/artifact-catalog-projection.ts` 已移除 `artifact:updated` 的自动版本自增，并在返回前统一调用 `toSerializedState(newState)`；`tests/unit/platform/state-evidence/events/projections/artifact-catalog-projection.test.ts` 已覆盖初始化、幂等、累计事件和乱序重放版本不自增四类回归，本轮定向测试已通过。 | 关闭该条 artifact projection 乱序重放/version 缺陷。 |
| 2245 | Round 38 — Platform Contracts/Types · UI Features · Contract Docs · Event Bus/Truth/Harness / §191 Event Bus / Truth / Harness 缺陷 | P2 | `src/platform/orchestration/harness/index.ts:508-523` | hasOpenExecutionBlockers 仅 terminal run 检查——活跃 run 的 blocked-tool 不触发 | 已解决（源码/测试） | 根因是 open blockers 校验旧实现只在 terminal run 上触发，活跃 run 中的 blocked tool 不会进入 invariant 失败分支。 | 当前 `src/platform/five-plane-orchestration/harness/index.ts` 已把 blocked-tool invariant 扩展到 active run；`tests/unit/platform/orchestration/harness/index.test.ts` 的 `assertInvariants flags blocked tools for active runs` 已定向通过。 | 关闭该条活跃 run blocked-tool 漏检缺陷。 |
| 2246 | Round 38 — Platform Contracts/Types · UI Features · Contract Docs · Event Bus/Truth/Harness / §191 Event Bus / Truth / Harness 缺陷 | P2 | `src/platform/orchestration/harness/evaluation/eval-run-service.ts:23` | actualEvidenceRefs 混入 reasonCodes——与 requiredEvidence 比对产生误匹配 | 已解决（源码修复并补定向测试） | 根因不在 `TaskOutcomeGrader`，而在 `EvalRunService` 自己把 `feedbackEnvelope.signals` 直接喂给 `actualEvidenceRefs`；该 flat array 同时承载 legacy signal/reason code，与真正的 step evidence 语义混在一起。 | 当前 `src/platform/orchestration/harness/evaluation/eval-run-service.ts` 已改为优先聚合 `run.steps[].evidenceRefs`，仅在 legacy run 缺少 step evidence 时才回退到 `feedbackEnvelope.signals`；`tests/unit/platform/orchestration/harness/evaluation/eval-run-service.test.ts` 已新增 “prefers step evidence refs over legacy feedback signals” 回归。 | 已改生产代码与定向测试。 |
| 2247 | Round 38 — Platform Contracts/Types · UI Features · Contract Docs · Event Bus/Truth/Harness / §191 Event Bus / Truth / Harness 缺陷 | P2 | `src/platform/state-evidence/events/projections/governance-projection.ts:203-204` | mapEventToActionType fallback 将含"decision"事件映射为 approval_granted——语义错误 | 已解决（源码修复并补定向测试） | 真实根因有两层：其一，未知 `decision*` 事件曾被错误兜底推断为 `approval_granted`；其二，本轮复验时又发现 handler 直接返回内部 state，导致 `_processedEventIdSet` 没有序列化回 `processedEventIds`，去重状态无法持久化。 | 当前 `src/platform/state-evidence/events/projections/governance-projection.ts` 已在未知 decision-like 事件分支停止推断 `approval_granted`，并统一返回 `toSerializedState(newState)`；`tests/unit/platform/state-evidence/events/projections/governance-projection.test.ts` 已覆盖 “does not infer approval_granted...” 与 `processedEventIds` 幂等/去重断言。 | 已修生产代码，并补强同模块的幂等序列化缺陷。 |
| 2248 | Round 39 — IAM/SSO · UI Features · CLI/SDK · Cross-Cutting Architecture / §193 IAM / SSO 深层缺陷 | P0 | `src/org-governance/sso-scim/saml/index.ts:226` | consumeAssertion 不传 IdP 公钥——xml-crypto 用 XML 内嵌 key,攻击者自签伪造断言 | 已解决（源码/测试） | 根因是旧验签路径把 key 解析权交给 XML `KeyInfo`，没有把签名锚定到 IdP/provider 的指纹，攻击者可通过内嵌证书自签伪造断言。 | 当前 SAML 验签已锚定 provider 指纹并拒绝不匹配的嵌入证书；`tests/unit/org-governance/sso-scim/saml/saml-signature.test.ts` 已覆盖“接受合法 colon 指纹签名断言”和“拒绝 provider 指纹不匹配的嵌入证书”两条回归，本轮定向测试已通过。 | 关闭该条 SAML embedded-key 伪造断言缺陷。 |
| 2249 | Round 39 — IAM/SSO · UI Features · CLI/SDK · Cross-Cutting Architecture / §193 IAM / SSO 深层缺陷 | P0 | `src/platform/control-plane/iam/access-model.ts:183` | resolvePrincipalAccessProfile 用 input.capabilities 替换非交集——viewer 可声称任意能力 | 已解决（源码修复并补定向测试） | 根因是 `resolvePrincipalAccessProfile()` 曾把调用方传入的 capability 声明当成可信输入，导致角色继承链之外的能力也能被带入访问画像。 | 当前 `src/platform/control-plane/iam/access-model.ts` 已只采信 `capabilitiesForRole()` 解析出的角色继承能力，完全丢弃输入能力声明；`tests/unit/platform/control-plane/iam/iam-access-model-extended.test.ts` 已验证 `viewer` 请求 `model:invoke/tool:invoke` 仍不会得到这些能力。 | 已修生产代码，并回写安全断言。 |
| 2250 | Round 39 — IAM/SSO · UI Features · CLI/SDK · Cross-Cutting Architecture / §193 IAM / SSO 深层缺陷 | P0 | `src/platform/control-plane/iam/access-model.ts:259-268` | manualTakeoverActive=true 时无条件 allowed:true——任何调用方注入此标志绕过全部检查 | 已解决（源码修复并补定向测试） | 根因是 `manualTakeoverActive` 旧分支提前返回 `allowed:true`，把能力、环境与风险检查整体短路；本轮将其收敛为“仅 operator-grade principal 可带该上下文标记，且仍需继续通过正常授权校验”。 | 当前 `src/platform/control-plane/iam/access-model.ts` 对 `manualTakeoverActive` 先做 operator 角色约束，再继续执行 capability/context 校验，最终只把该标记写入 `matchedRuleRefs/constraints`；`tests/unit/platform/control-plane/iam/access-model-authorization.test.ts` 与 `iam-access-model-extended.test.ts` 已覆盖非 operator 拒绝、operator 允许且保留正常检查的路径。 | 已修生产代码；同轮还修复了该模块暴露出的 default-allow 回归。 |
| 2251 | Round 39 — IAM/SSO · UI Features · CLI/SDK · Cross-Cutting Architecture / §193 IAM / SSO 深层缺陷 | P0 | `src/org-governance/sso-scim/oidc/oidc-service.ts:237-252` | fetchUserInfo 任何错误 fallback mock admin——allowMockFallback=false 时仍执行 | 已解决（源码已满足，本轮补证据） | 原审计对应的是旧 fallback 行为；现行实现已经把 mock user 回退严格绑定到 `allowMockFallback=true`，在关闭该开关时会直接抛出失败而不是返回模拟管理员。 | 当前 `src/org-governance/sso-scim/oidc/oidc-service.ts` 在 `!response.ok` 和 `catch` 两条路径都先检查 `allowMockFallback`；`tests/unit/org-governance/sso-scim/oidc/oidc-service.test.ts` 已覆盖 `allowMockFallback=false` 时 `fetchUserInfo()` 失败而非回退 mock user。 | 不改此逻辑主实现，回写现行证据。 |
| 2252 | Round 39 — IAM/SSO · UI Features · CLI/SDK · Cross-Cutting Architecture / §193 IAM / SSO 深层缺陷 | P1 | `src/platform/control-plane/iam/field-encryption.ts:14-15` | normalizeKey 单次 SHA-256 无 salt/stretching——1字符密码即有效 key | 已解决（源码已满足，本轮补证据） | 原审计基于旧的单次 SHA-256 归一化；现行实现已改为 `scryptSync` 派生，并强制 passphrase 至少 32 字节，同时拒绝弱重复字节 raw key。 | 当前 `src/platform/control-plane/iam/field-encryption.ts` 已引入 `scryptSync`、`MIN_PASSPHRASE_BYTES=32` 和弱 key 检查；`tests/unit/platform/control-plane/iam/field-encryption-extended.test.ts` 已覆盖 32-byte passphrase、弱 repeating-byte key 拒绝及篡改/版本异常。 | 不改主实现，校正陈旧测试并回写依据。 |
| 2253 | Round 39 — IAM/SSO · UI Features · CLI/SDK · Cross-Cutting Architecture / §193 IAM / SSO 深层缺陷 | P1 | `src/platform/control-plane/iam/secret-management-service.ts:168-196` | requireSecret 返回明文无审计记录——绕过审计 | 已解决（源码已满足，本轮补证据） | 原审计没有吸收 `requireSecret()` 后续补上的最小使用审计与 scope 鉴权；现行实现即便是 system-scope secret 也会先校验调用主体，再写 usage audit。 | 当前 `src/platform/control-plane/iam/secret-management-service.ts` 在 `requireSecret()` 中先 `authorizeSecretAccess(...)`，随后创建 usage audit 并记录 `requestedBy`；`tests/unit/platform/control-plane/iam/secret-management-service.test.ts` 已覆盖 `requireSecret records a minimal usage audit` 以及 system/service principal 访问约束。 | 不改主实现，回写审计证据。 |
| 2254 | Round 39 — IAM/SSO · UI Features · CLI/SDK · Cross-Cutting Architecture / §193 IAM / SSO 深层缺陷 | P1 | `src/org-governance/sso-scim/oidc/oidc-service.ts:261-276` | token 比较用 === 非 timingSafeEqual——timing side-channel | 已解决（源码已满足，本轮补证据） | 原审计对应的是旧的字符串直接比较路径；现行 `validateAccessToken()` 已改为先走 access-token 索引，再用 `timingSafeEqual` 做等长比较。本轮同模块还顺手修掉了 PKCE helper 的 ESM `require()` 运行时错误。 | 当前 `src/org-governance/sso-scim/oidc/oidc-service.ts` 已顶层导入 `timingSafeEqual` 并在 `validateAccessToken()` 中使用；`tests/unit/org-governance/sso-scim/oidc/oidc-service.test.ts` 已通过本轮定向复核。 | 不改该安全判断逻辑，只补依据并修正同模块运行时问题。 |
| 2255 | Round 39 — IAM/SSO · UI Features · CLI/SDK · Cross-Cutting Architecture / §193 IAM / SSO 深层缺陷 | P1 | `src/org-governance/sso-scim/oidc/oidc-service.ts:302-307` | createSession 无并发 session 上限——无界增长 | 已解决（源码已满足，本轮补证据） | 原审计未反映后续的 user-session 索引和 oldest-session eviction 逻辑；现行 `createSession()` 已在单用户会话数达到阈值时主动回收旧会话。 | 当前 `src/org-governance/sso-scim/oidc/oidc-service.ts` 使用 `userSessions` 跟踪用户会话，并在 `size >= maxSessionsPerUser` 时逐出最旧 session；`tests/unit/org-governance/sso-scim/oidc/oidc-service.test.ts` 已覆盖 `should enforce maxSessionsPerUser`。 | 不改主实现，回写证据。 |
| 2256 | Round 39 — IAM/SSO · UI Features · CLI/SDK · Cross-Cutting Architecture / §193 IAM / SSO 深层缺陷 | P1 | `src/org-governance/sso-scim/oidc/oidc-service.ts:404-409` | touchSession 不延 expiresAt——无滑动窗口过期 | 已解决（源码已满足，本轮补证据） | 原审计基于旧版 `touchSession()` 仅刷新 `lastActivityAt` 的阶段；现行实现已经把 `expiresAt` 按 sliding window 延长，并受 `maxSessionAgeMs` 上界约束。 | 当前 `src/org-governance/sso-scim/oidc/oidc-service.ts` 在 `touchSession()` 中计算 `newExpiresAt = min(currentExpiresAt + sessionTtlMs, createdAt + maxSessionAgeMs)`；`tests/unit/org-governance/sso-scim/oidc/oidc-service.test.ts` 已覆盖 `should extend session expiration (sliding window)`。 | 不改主实现，回写证据。 |
| 2257 | Round 39 — IAM/SSO · UI Features · CLI/SDK · Cross-Cutting Architecture / §193 IAM / SSO 深层缺陷 | P1 | `src/platform/control-plane/iam/policy-engine.ts:246-263` | full-auto 绕过所有 risk escalation——高风险操作无审批 | 已解决（源码已满足，本轮补证据并校正陈旧测试） | 原审计对应的是 `full-auto` 曾直接放行高风险动作的旧策略；现行 `PolicyEngine` 已把 `destructive / irreversible / prod_affecting / org_changing` 纳入 full-auto 升级分支。旧测试仍残留“full-auto destructive 可以直接 allow”的断言，本轮已纠正。 | 当前 `src/platform/control-plane/iam/policy-engine.ts` 在 `input.mode === "full-auto"` 且高风险时调用 `this.escalate(...)`，对 deny-by-default 高风险返回 `policy.full_auto_high_risk_escalation`；`tests/unit/platform/control-plane/iam/policy-engine.test.ts` 与 `policy-engine-edge-cases.test.ts` 已验证 destructive 和 org_change 在 full-auto 下都会升级审批。 | 不改主实现，修正陈旧断言并回写依据。 |
| 2258 | Round 39 — IAM/SSO · UI Features · CLI/SDK · Cross-Cutting Architecture / §193 IAM / SSO 深层缺陷 | P1 | `src/platform/control-plane/iam/vault-http-secret-provider.ts:173-174` | vault token 硬编码 1h 假设 TTL——实际 TTL 短时静默失败 | 已解决（源码已满足，本轮补定向测试） | 原审计命中的是 AppRole 登录缺少 `lease_duration` 时默认按 1 小时缓存 token 的旧行为；现行实现已改成“有 lease 时按实际值并封顶，无 lease 时只给保守 60 秒 TTL”。 | 当前 `src/platform/control-plane/iam/vault-http-secret-provider.ts` 已在缺失 `lease_duration` 时使用 `60` 秒保守 TTL，而不是硬编码 `3600`；本轮新增 `tests/unit/platform/control-plane/iam/vault-http-secret-provider.test.ts` 回归，验证无 `lease_duration` 时 31 秒后会重新登录，而不是静默复用旧 token。 | 新增最小回归测试，文档改写为已解决。 |
| 2259 | Round 39 — IAM/SSO · UI Features · CLI/SDK · Cross-Cutting Architecture / §193 IAM / SSO 深层缺陷 | P2 | `src/platform/five-plane-control-plane/iam/data-classification-service.ts:719-720` | 用户定义 regex 无 ReDoS 防护 | 已解决（源码已满足，本轮补定向测试） | 原审计对应的是 custom rule pattern 直接 `new RegExp(...).test(...)` 的阶段；现行实现已在运行前用 `isRegexSafe()` 过滤嵌套量词等明显危险模式，不安全 regex 直接拒绝匹配。 | 当前 `src/platform/five-plane-control-plane/iam/data-classification-service.ts` 在 `matchesRule()` 中先执行 `isRegexSafe(pattern)`，对不安全模式仅打日志并跳过；本轮新增 `tests/unit/platform/control-plane/iam/data-classification-service-regex-safety.test.ts`，验证危险模式 `"(a+)+$"` 不会被应用到分类链上。 | 新增最小回归测试，文档改写为已解决。 |
| 2260 | Round 39 — IAM/SSO · UI Features · CLI/SDK · Cross-Cutting Architecture / §193 IAM / SSO 深层缺陷 | P2 | `src/org-governance/sso-scim/scim-sync/scim-service.ts:792` | applyFilter 丢弃字段名——所有过滤落 userName | 已解决（源码已满足，本轮补证据） | 原审计对应的是旧过滤器把字段路径丢失、默认落到 `userName` 的实现；现行 `applyFilter()` 已保留解析出的 field path，并递归解析嵌套数组/对象字段。 | 当前 `src/org-governance/sso-scim/scim-sync/scim-service.ts` 已把 `fieldName` 拆成 `fieldPath`，并通过 `getFilterValues()` 递归处理嵌套字段；`tests/unit/org-governance/sso-scim/scim-sync/scim-service.test.ts` 已覆盖 `userName` 系列过滤与 `emails.value eq ...` 嵌套过滤。 | 不改主实现，回写证据。 |
| 2261 | Round 39 — IAM/SSO · UI Features · CLI/SDK · Cross-Cutting Architecture / §193 IAM / SSO 深层缺陷 | P2 | `src/platform/control-plane/iam/audit-integrity-repository.ts:63-78` | chainPosition 无唯一性/顺序验证——完整性链可损坏 | 已解决（源码已满足，本轮补证据） | 原审计没有吸收后续 pre-insert 完整性校验；现行仓储在写入前已经拒绝 duplicate `eventId`、chain gap 和 `previousChainHash` 断链。 | 当前 `src/platform/control-plane/iam/audit-integrity-repository.ts` 在 `insertIntegrityRecord()` 中已检查 duplicate `eventId`、顺序连续性与 `previousChainHash` 匹配；`tests/unit/platform/control-plane/iam/audit-integrity-repository.test.ts` 已覆盖 chain gap 与 previous hash mismatch 拒绝。 | 不改主实现，回写证据。 |
| 2262 | Round 39 — IAM/SSO · UI Features · CLI/SDK · Cross-Cutting Architecture / §193 IAM / SSO 深层缺陷 | P2 | `src/org-governance/sso-scim/group-role-mapping-service.ts:9-11` | register 无 authz/roleId 验证——任何调用方映射 admin 角色 | 已解决（源码已满足，本轮补证据） | 原审计对应的是 `register()` 无 caller context、也不校验 roleId 的旧实现；现行服务已要求 platform-admin-like 授权上下文，并在启用 allowlist 时拒绝非法 roleId。 | 当前 `src/org-governance/sso-scim/group-role-mapping-service.ts` 的 `register()` 已校验 `callerIsPlatformAdmin/callerRoleIds`，并支持 `setValidRoleIds()` allowlist；`tests/unit/org-governance/sso-scim/group-role-mapping-service.test.ts` 已覆盖未授权调用拒绝与 `invalid_role_id` 拒绝。 | 不改主实现，回写证据。 |
| 2263 | Round 39 — IAM/SSO · UI Features · CLI/SDK · Cross-Cutting Architecture / §194 UI Features 深层缺陷 | P0 | `ui/packages/features/hitl/src/hooks/index.ts:5-13` | HITL 仅 3 静态文本项——spec 5 能力(approve/reject/edit/escalate/defer)全缺 | 已解决（源码/测试） | 根因是 reaudit 结论停留在 hook 只有静态占位项的早期版本，没有复核后续接入的审批拉取、实时订阅和多种人工操作入口。 | 当前 `ui/packages/features/hitl/src/hooks/index.ts` 的 `useHitlVm()` 已提供 `approve/reject/edit/escalate/defer/delegate/resume/patch/override`，并接 `fetchApprovals()` 与 WS 订阅；`ui/tests/unit/ui/packages/features/hitl/hooks.test.tsx` 已验证 `patch/override` 会走真实审批决策 API，本轮定向测试已通过。 | 关闭该条 HITL 能力缺失结论。 |
| 2264 | Round 39 — IAM/SSO · UI Features · CLI/SDK · Cross-Cutting Architecture / §194 UI Features 深层缺陷 | P0 | `ui/packages/features/takeover/src/hooks/index.ts:5-13` | takeover 无状态保持机制——无执行快照/上下文传递/所有权转移 | 已解决（源码/测试） | 根因是 `useTakeoverVm()` 之前只有接口与占位说明，没有真正实现快照持久化、上下文恢复和所有权转移链路。 | 当前 `ui/packages/features/takeover/src/hooks/index.ts` 已实现 snapshot 本地持久化、`claimOwnership()`、`transferOwnership()`、`restoreFromSnapshot()` 与 ownership history；`ui/tests/unit/ui/packages/features/takeover/hooks.test.tsx` 已验证接管后会持久化快照、记录交接历史并可从旧快照恢复，本轮定向测试已通过。 | 关闭该条 takeover 状态保持缺陷。 |
| 2265 | Round 39 — IAM/SSO · UI Features · CLI/SDK · Cross-Cutting Architecture / §194 UI Features 深层缺陷 | P0 | `ui/packages/features/domain-wizard/src/web/index.tsx` | 无多步向导/草稿持久化/后退导航——仅 3 按钮平面面板 | 已解决（源码/测试） | 根因是条目停留在 domain wizard 还是平面面板的旧认知；现行页面和 hook 已具备 stepper、前后导航和 draft 持久化。 | 当前 `ui/packages/features/domain-wizard/src/web/index.tsx` 已实现 stepper、`goNext()/goPrev()` 和逐步表单；`ui/packages/features/domain-wizard/src/hooks/index.ts` 已把 wizard draft 持久化到 `localStorage` 并在提交后清理；`ui/tests/unit/ui/packages/features/domain-wizard/hooks.test.tsx` 已验证草稿恢复、编辑持久化和提交清理，本轮定向测试已通过。 | 关闭该条 domain wizard 多步/草稿缺陷。 |
| 2266 | Round 39 — IAM/SSO · UI Features · CLI/SDK · Cross-Cutting Architecture / §194 UI Features 深层缺陷 | P0 | `ui/packages/features/governance-compliance/src/hooks/index.ts` | 缺策略编辑器/审批队列/审计轨迹——仅 3 静态描述字符串 | 已解决（源码/测试） | 根因不只是功能缺失；旧 hook 中 `approveException/rejectException` 回调还遮蔽了同名导入 API，调用时会递归回自己，导致异常审批链真实不可用。 | 当前 `ui/packages/features/governance-compliance/src/hooks/index.ts` 已提供 `policies/auditTrail/exceptionQueue/updatePolicy/submitExceptionRequest/approveException/rejectException/filterAuditTrail`，并将导入 API 显式别名为 `approveExceptionRequest/rejectExceptionRequest` 以消除递归；`ui/tests/unit/ui/packages/features/governance-compliance/hooks.test.tsx` 已验证加载策略与审计轨迹、异常审批/驳回都会走 API client，本轮定向测试已通过。 | 关闭该条 governance-compliance hook 缺陷。 |
| 2267 | Round 39 — IAM/SSO · UI Features · CLI/SDK · Cross-Cutting Architecture / §194 UI Features 深层缺陷 | P1 | `ui/packages/features/analytics/src/web/index.tsx` | 无时序图表/导出/自定义日期——trend 映射为整数非时间数据 | 已解决（源码/测试） | 根因是旧 analytics 页面只把 trend 压成整数条形摘要，没有时序数据、日期范围和导出控制。 | 当前 `ui/packages/features/analytics/src/hooks/index.ts` 已生成 `timeSeriesData`、`dateRange` 与 `exportData()`；`ui/packages/features/analytics/src/web/index.tsx` 已接入日期筛选和 CSV/JSON 导出控件；`ui/tests/unit/ui/packages/features/analytics/hooks.test.tsx` 已验证时序数据生成与 JSON 导出，本轮定向测试已通过。 | 关闭该条 analytics 时序/导出缺陷。 |
| 2268 | Round 39 — IAM/SSO · UI Features · CLI/SDK · Cross-Cutting Architecture / §194 UI Features 深层缺陷 | P1 | `ui/packages/features/task-cockpit/src/hooks/index.ts` | 无实时监控——无 polling/WS/refetchInterval | 已解决（源码/测试） | 根因是 hook 旧实现只读一次性 query，没有 `refetchInterval`，任务列表不会周期刷新。 | 当前 `ui/packages/features/task-cockpit/src/hooks/index.ts` 已改为 `useTasksQuery(undefined, { refetchInterval: 5000 })`；`ui/tests/unit/ui/packages/features/task-cockpit/hooks.test.tsx` 已直接断言 polling 配置存在，本轮定向测试已通过。 | 关闭该条 task cockpit 实时刷新缺陷。 |
| 2269 | Round 39 — IAM/SSO · UI Features · CLI/SDK · Cross-Cutting Architecture / §194 UI Features 深层缺陷 | P1 | `ui/packages/features/settings/src/hooks/index.ts:105-107` | save() 同步设 saving→saved——React batch 使 saving 不可见 | 已解决（源码/测试） | 根因是 `save()` 旧实现把 `saving -> saved` 放在同一个 React batch 内更新，`saving` 态会被直接吞掉。 | 当前 `ui/packages/features/settings/src/hooks/index.ts` 已在保存成功后通过短延时再切到 `saved`，确保 `saving` 态可见；`ui/tests/unit/ui/packages/features/settings/hooks.test.tsx` 已验证保存中状态可见且随后切为 `saved`，本轮定向测试已通过。 | 关闭该条 settings saveState 可见性缺陷。 |
| 2270 | Round 39 — IAM/SSO · UI Features · CLI/SDK · Cross-Cutting Architecture / §194 UI Features 深层缺陷 | P1 | `ui/packages/features/workflow-cockpit/src/hooks/index.ts:41` | selectedId 不同步 fallback——高亮与详情面板不一致 | 已解决（源码/测试） | 根因是 workflow cockpit 旧逻辑在数据刷新时会 fallback 到首项，`selectedId` 和 `selectedWorkflow` 容易与用户当前显式选择脱节。 | 当前 `ui/packages/features/workflow-cockpit/src/hooks/index.ts` 已以 `null` 起步，并在 query 刷新时只在当前选择失效时清空，不再 fallback 到 `workflows[0]`；`ui/tests/unit/ui/packages/features/workflow-cockpit/hooks.test.tsx` 与 `web.test.tsx` 的定向回归已通过。 | 关闭该条 workflow cockpit ghost selection 缺陷。 |
| 2271 | Round 39 — IAM/SSO · UI Features · CLI/SDK · Cross-Cutting Architecture / §194 UI Features 深层缺陷 | P1 | `ui/packages/features/task-cockpit/src/hooks/index.ts:40` | 同上 ghost selection bug | 已解决（源码/测试） | 根因是 task cockpit 旧逻辑在轮询刷新时也会 fallback 到首项，用户未显式选择或所选任务被移除时会出现高亮与详情错位。 | 当前 `ui/packages/features/task-cockpit/src/hooks/index.ts` 已以 `null` 起始，并在刷新后仅保留仍然存在的选择；`selectedTask` 也不再回退到 `tasks[0]`；`ui/tests/unit/ui/packages/features/task-cockpit/hooks.test.tsx` 已验证初始 `selectedId/selectedTask` 都保持 `null`，本轮定向测试已通过。 | 关闭该条 task cockpit ghost selection 缺陷。 |
| 2272 | Round 39 — IAM/SSO · UI Features · CLI/SDK · Cross-Cutting Architecture / §194 UI Features 深层缺陷 | P1 | `ui/packages/features/conversation/src/hooks/index.ts:42-45` | sendPrompt 每次强制 requestClarification——无法直接执行 | 已解决（源码/测试） | 根因是普通 prompt 发送与 clarification 请求入口在旧实现里没有分开，`sendPrompt()` 每次都走 clarification 分支。 | 当前 `ui/packages/features/conversation/src/hooks/index.ts` 的 `sendPrompt()` 已直接调用 `clientRef.current?.send(draft)`，`requestClarification()` 独立保留；`ui/tests/unit/ui/packages/features/conversation/hooks.test.tsx` 已验证 `sendPrompt()` 会调用 `sendSpy("Ship the release")`，且 remount 后消息历史仍能恢复，本轮定向测试已通过。 | 关闭该条 conversation 发送入口混线缺陷。 |
| 2273 | Round 39 — IAM/SSO · UI Features · CLI/SDK · Cross-Cutting Architecture / §194 UI Features 深层缺陷 | P1 | `ui/packages/features/workflow-cockpit/src/web/index.tsx:45-48` | 破坏性操作(release/pause)无确认对话框 | 已解决（源码/测试） | 根因是页面原先直接把 `pause/release/resume/recover` 绑定到按钮，没有任何确认门。 | 当前 `ui/packages/features/workflow-cockpit/src/web/index.tsx` 已在四个操作前统一调用 `window.confirm(...)`；`ui/tests/unit/ui/packages/features/workflow-cockpit/web.test.tsx` 已验证确认后才触发对应控制 API，本轮定向测试已通过。 | 关闭该条 destructive action 无确认缺陷。 |
| 2274 | Round 39 — IAM/SSO · UI Features · CLI/SDK · Cross-Cutting Architecture / §194 UI Features 深层缺陷 | P2 | `ui/packages/features/task-cockpit/src/web/index.tsx:48-53` | operator/escalation target 无输入验证——注入无效状态值 | 已解决（源码/测试） | 根因是输入框原样把 operator / target 传给 action，没有最小清洗和空值校验。 | 当前 `ui/packages/features/task-cockpit/src/web/index.tsx` 已对两个输入使用白名单清洗并在空值时阻断提交；`ui/tests/unit/ui/packages/features/task-cockpit/web.test.tsx` 已验证 `ops<script>` 会被清洗为 `opsscript`，`domain-admin!!` 会被清洗为 `domain-admin`，本轮定向测试已通过。 | 关闭该条 task cockpit 输入注入缺陷。 |
| 2275 | Round 39 — IAM/SSO · UI Features · CLI/SDK · Cross-Cutting Architecture / §194 UI Features 深层缺陷 | P2 | `ui/packages/features/settings/src/hooks/index.ts:108-114` | activityItems 无界增长(同 workflow-cockpit/task-cockpit) | 已解决（源码/测试） | 根因是 settings、workflow-cockpit、task-cockpit 三处活动流都只有 prepend，没有长度上限，长会话会持续堆积。 | 当前 `ui/packages/features/settings/src/hooks/index.ts`、`ui/packages/features/workflow-cockpit/src/hooks/index.ts`、`ui/packages/features/task-cockpit/src/hooks/index.ts` 均已在追加后 `.slice(0, 100)` 限流；`ui/tests/unit/ui/packages/features/settings/hooks.test.tsx` 及 workflow/task-cockpit 定向 hooks 回归均已通过。 | 关闭该条 UI 活动流无界增长缺陷。 |
| 2276 | Round 39 — IAM/SSO · UI Features · CLI/SDK · Cross-Cutting Architecture / §194 UI Features 深层缺陷 | P2 | `ui/packages/features/conversation/src/hooks/index.ts:19` | ConversationClient remount 丢失全部历史——无持久化 | 已解决（源码/测试） | 根因是消息历史过去只存在内存态，组件 remount 后不会从任何持久层恢复。 | 当前 `ui/packages/features/conversation/src/hooks/index.ts` 已把消息持久化到 `sessionStorage`，并在 mount 时通过 `initialMessages: loadPersistedMessages()` 恢复；`ui/tests/unit/ui/packages/features/conversation/hooks.test.tsx` 已验证 remount 后消息历史仍存在，本轮定向测试已通过。 | 关闭该条 conversation remount 丢历史缺陷。 |
| 2277 | Round 39 — IAM/SSO · UI Features · CLI/SDK · Cross-Cutting Architecture / §194 UI Features 深层缺陷 | P2 | `ui/packages/features/settings/src/hooks/index.ts:46-47` | useEffect deps 不含全部同步字段——部分偏好变更不触发 | 已解决（源码/测试） | 根因是 effect 依赖旧实现只盯着派生字段 `preferenceTheme/preferenceLocale`，上游 `preferences` 其余变化不会触发草稿同步。 | 当前 `ui/packages/features/settings/src/hooks/index.ts` 已直接依赖完整 `preferences` 对象；`ui/tests/unit/ui/packages/features/settings/hooks.test.tsx` 已验证上游 `preferences` 更新后 `draftTheme/draftLocale` 会同步刷新，本轮定向测试已通过。 | 关闭该条 settings 偏好同步漏触发缺陷。 |
| 2278 | Round 39 — IAM/SSO · UI Features · CLI/SDK · Cross-Cutting Architecture / §195 CLI / SDK 缺陷 | P0 | `src/sdk/cli/migrate-sqlite-to-pg.ts:68` | 表名+列名直接拼入 SQL——二阶注入(attacker-controlled SQLite DB) | 已解决 | 根因是原审计命中了早期迁移器直接从 SQLite schema 反射表/列后拼接 SQL 的版本；现行实现已把迁移范围收敛为内置 allowlist 表，并对列名做标识符校验后才参与 SQL 组装。 | 当前 `src/sdk/cli/migrate-sqlite-to-pg.ts` 已只迁移受控表集并在计划阶段校验可接受列名；定向验证见 `tests/unit/sdk/cli/migrate-sqlite-to-pg.test.ts` 与 `tests/integration/sdk/migrate-sqlite-to-pg-integration-2278-2279.test.ts`。 | 回写现行源码与定向测试依据。 |
| 2279 | Round 39 — IAM/SSO · UI Features · CLI/SDK · Cross-Cutting Architecture / §195 CLI / SDK 缺陷 | P0 | `src/sdk/cli/migrate-sqlite-to-pg.ts:123-128` | PG DSN(含密码)输出到 stdout——凭证泄漏 | 已解决 | 根因是旧版 CLI 直接把传入 DSN 回显到 JSON 输出；现行入口在打印前统一经过 `redactPgDsn()`，不再把密码回写到 stdout。 | 当前 `src/sdk/cli/migrate-sqlite-to-pg.ts` 的 `main()` 输出路径已对 `pgDsn` 做脱敏；定向验证见 `tests/unit/sdk/cli/migrate-sqlite-to-pg.test.ts` 与 `tests/integration/sdk/migrate-sqlite-to-pg-integration-2278-2279.test.ts`。 | 回写现行源码与定向测试依据。 |
| 2280 | Round 39 — IAM/SSO · UI Features · CLI/SDK · Cross-Cutting Architecture / §195 CLI / SDK 缺陷 | P0 | `src/sdk/cli/api-server.ts:160-168` | AA_API_KEYS/JWT_SECRET 空时认证完全禁用——全端点无保护 | 已解决 | 根因是复审条目仍沿用早期“缺省关闭鉴权”的印象，没有吸收后续把 API server 启动阶段改为 fail-closed 的实现。 | 当前 `src/sdk/cli/api-server.ts` 在缺少 `AA_API_KEYS` 与 `AA_JWT_SECRET` 时会直接抛出 `server cannot start with all endpoints unprotected`；定向验证见 `tests/unit/sdk/cli/api-server-source-2280-2281.test.ts`。 | 回写现行源码与定向测试依据。 |
| 2281 | Round 39 — IAM/SSO · UI Features · CLI/SDK · Cross-Cutting Architecture / §195 CLI / SDK 缺陷 | P1 | `src/sdk/cli/api-server.ts:198` | webhookSecret 无最低熵检查——空/弱 secret 允许伪造 | 已解决 | 根因是审计条目没有反映后续补上的 webhook secret 启动校验；当前实现已把“空 secret”和“长度不足”都视为启动错误。 | 当前 `src/sdk/cli/api-server.ts` 会拒绝空 `AA_WEBHOOK_SECRET`，并要求长度至少为 32；定向验证见 `tests/unit/sdk/cli/api-server-source-2280-2281.test.ts`。 | 回写现行源码与定向测试依据。 |
| 2282 | Round 39 — IAM/SSO · UI Features · CLI/SDK · Cross-Cutting Architecture / §195 CLI / SDK 缺陷 | P1 | `src/sdk/cli/dlq-manager.ts:131-149` | retryDeadLetters 重置 ALL 死信为 waiting——无限制无确认 | 已修复 | 根因是旧实现把 retry 当成单条 `UPDATE`，既没有执行层面的 limit/确认约束，也没有持久化审计；本轮复核还发现同模块的事件 DLQ 列表查询已与真实 `event_dead_letters` 迁移 schema 漂移，说明该工具长期缺少针对 authoritative schema 的回归约束。 | 当前 `src/sdk/cli/dlq-manager.ts` 已增加 `--retry-limit` 解析与边界收敛、无 limit 默认 fail-safe 提示、`retryDeadLetters()` 事务封装、`dlq_operation_audits` 审计记录，以及对事件 DLQ 真实列集的查询对齐；定向验证见 `tests/unit/sdk/cli/dlq-manager-operations-2282-2283.test.ts` 与 `tests/integration/sdk/dlq-manager-architecture-regressions-2282-2283.test.ts`。 | 已改生产代码并补定向测试，不再维持未解决结论。 |
| 2283 | Round 39 — IAM/SSO · UI Features · CLI/SDK · Cross-Cutting Architecture / §195 CLI / SDK 缺陷 | P1 | `src/sdk/cli/dlq-manager.ts:152-165` | purgeDeadLetters 硬删除无审计/归档——不可恢复 | 已修复 | 根因是旧版 purge 直接删除源表，没有追加归档表、操作审计或事务边界，一旦中断就既不可恢复也无法追责。 | 当前 `src/sdk/cli/dlq-manager.ts` 已在 `gateway/jobs/events` 三类 purge 路径统一追加 `*_archive` 归档、写入 `dlq_operation_audits`，并把归档+删除+审计放进单事务；定向验证见 `tests/unit/sdk/cli/dlq-manager-operations-2282-2283.test.ts` 与 `tests/integration/sdk/dlq-manager-architecture-regressions-2282-2283.test.ts`。 | 已改生产代码并补定向测试。 |
| 2284 | Round 39 — IAM/SSO · UI Features · CLI/SDK · Cross-Cutting Architecture / §195 CLI / SDK 缺陷 | P1 | `src/sdk/harness-sdk/index.ts:1-126` | 无 deterministic replay 方法——spec 要求 | 已解决 | 根因是复审条目沿用了 `HarnessSdk` 早期只提供 create/append/evaluate 的快照，没有吸收后续加入的 trace replay 能力。 | 当前 `src/sdk/harness-sdk/index.ts` 已提供 `traceReplay()`，并在 replay 前按 `eventId` 做确定性排序；定向验证见 `tests/unit/sdk/harness-sdk/deterministic-replay-2284.test.ts`。 | 回写现行源码与定向测试依据。 |
| 2285 | Round 39 — IAM/SSO · UI Features · CLI/SDK · Cross-Cutting Architecture / §195 CLI / SDK 缺陷 | P1 | `src/sdk/admin-sdk/index.ts:6-31` | 无 bulk 操作/事务语义——spec 要求 | 已解决 | 根因是原审计停留在 `AdminSdk` 仅有单条 CRUD 包装的阶段，没有复核后来补上的 bulk 端点封装与事务语义注释。 | 当前 `src/sdk/admin-sdk/index.ts` 已提供 `bulkCreateTenants`、`bulkUpdateTenants`、`bulkDeleteTenants`、`bulkCreatePolicies`、`bulkAttachPolicies`、`bulkDomainLifecycle` 等 bulk API；定向验证见 `tests/unit/sdk/admin-sdk/bulk-ops-2285.test.ts`。 | 回写现行源码与定向测试依据。 |
| 2286 | Round 39 — IAM/SSO · UI Features · CLI/SDK · Cross-Cutting Architecture / §195 CLI / SDK 缺陷 | P1 | `src/sdk/cli/index.ts:1-82` | 无 PKCE OAuth login 命令——CLI 仅用 env bearer token | 已修复 | 根因不是“完全没有 OAuth/PKCE 代码”，而是此前只有 barrel 内 helper 和 `CLI_ENTRYPOINTS` 声明，没有真正可执行的 `login` CLI 入口，且 token 落盘还残留 ESM `require()`，导致命令宣称存在但不可调用。 | 本轮新增 `src/sdk/cli/login.ts`，实现了 PKCE login 的 start/finish 两阶段 CLI、状态文件持久化与 code exchange；`package.json` 也已补 `login` script，`src/sdk/cli/index.ts` 的 token 保存改为 ESM 安全落盘。定向验证见 `tests/unit/sdk/cli/login-2286.test.ts`。 | 已改生产代码并补定向测试。 |
| 2287 | Round 39 — IAM/SSO · UI Features · CLI/SDK · Cross-Cutting Architecture / §195 CLI / SDK 缺陷 | P1 | `src/sdk/cli/billing.ts:56-64` | Stripe secret key 明文内存无 redaction wrapper | 已解决 | 根因是审计只盯住了 CLI 文件内局部片段，没有继续复核真正承载 Stripe 密钥的 payment gateway 包装层，以及 CLI 输出侧的统一脱敏。 | 当前 `src/scale-ecosystem/billing/billing-payment-gateway.ts` 已用 `StripeSecretRedactor` 包装 `secretKey`，`src/sdk/cli/billing.ts` 输出前也会递归执行 `redactSensitiveValues()`；定向验证见 `tests/unit/sdk/cli/source-review-2287-2289-2290.test.ts`。 | 回写现行源码与定向测试依据。 |
| 2288 | Round 39 — IAM/SSO · UI Features · CLI/SDK · Cross-Cutting Architecture / §195 CLI / SDK 缺陷 | P2 | `src/sdk/cli/migrate-sqlite-to-pg.ts:79-117` | 跨表无单事务——中途崩溃破坏引用完整性 | 已解决 | 根因是该条复审仍停留在旧版逐表写 PG 的快照，没有吸收后续把跨表写入收敛到单个 PostgreSQL 事务中的实现。 | 当前 `src/sdk/cli/migrate-sqlite-to-pg.ts` 已使用 `await pg.transaction(async (conn) => { ... })` 包裹跨表迁移写入；定向验证见 `tests/unit/sdk/cli/migrate-sqlite-to-pg-2288-transaction.test.ts`。 | 回写现行源码与定向测试依据。 |
| 2289 | Round 39 — IAM/SSO · UI Features · CLI/SDK · Cross-Cutting Architecture / §195 CLI / SDK 缺陷 | P2 | `src/sdk/cli/shadow-snapshot.ts:34-44` | sandbox policy 限 workspaceRoot 但写 shadowRoot——超出策略范围 | 已解决 | 根因是 reaudit 条目沿用了更早把 sandbox root 绑定到 `workspaceRoot` 的版本，没有反映后续已经改为按真实写目标 `shadowRoot` 约束。 | 当前 `src/sdk/cli/shadow-snapshot.ts` 已显式使用 `createWorkspaceWritePolicy(envConfig.shadowRoot)`；定向验证见 `tests/unit/sdk/cli/source-review-2287-2289-2290.test.ts`。 | 回写现行源码与定向测试依据。 |
| 2290 | Round 39 — IAM/SSO · UI Features · CLI/SDK · Cross-Cutting Architecture / §195 CLI / SDK 缺陷 | P2 | `src/sdk/cli/data-plane.ts:38-46` | ArtifactStore 无 sandboxPolicy——无约束文件写 | 已解决 | 根因是原审计没有继续追踪 `artifactStoreOptions` 的接线和 `ArtifactStore` 内部的 root narrowing，因此把“policy 范围由父目录兜底、再在 store 内收窄到 rootDir”的现状误判成“完全无 sandboxPolicy”。 | 当前 `src/sdk/cli/data-plane.ts` 已在 `artifactStoreOptions` 上传入 `sandboxPolicy`，而 `src/platform/five-plane-state-evidence/artifacts/artifact-store.ts` 在实际写入前又会把 `allowedRoots` 收窄到解析后的 `rootPath`；定向验证见 `tests/unit/sdk/cli/source-review-2287-2289-2290.test.ts`。 | 回写现行源码与定向测试依据。 |
| 2291 | Round 39 — IAM/SSO · UI Features · CLI/SDK · Cross-Cutting Architecture / §195 CLI / SDK 缺陷 | P2 | `src/sdk/client-sdk/api-client.ts:258-265` | cursor JSON.parse 无 schema 验证——注入任意属性 | 已解决 | 根因是旧版 `parseCursor()` 只做 `JSON.parse`，没有拒绝额外字段或非法类型；现行实现已经把 cursor 收敛到 `cursor/limit` 两个已知字段并验证其类型。 | 当前 `src/sdk/client-sdk/api-client.ts` 的 `parseCursor()` 会拒绝 unexpected properties、非字符串 cursor 和非正整数 limit；定向验证见 `tests/unit/sdk/client-sdk/cursor-2291-schema-validation.test.ts`。 | 回写现行源码与定向测试依据。 |
| 2292 | Round 39 — IAM/SSO · UI Features · CLI/SDK · Cross-Cutting Architecture / §196 Cross-Cutting 架构级缺陷 | P0 | `src/platform/interface/api/http-server/task-routes.ts:196-229` | POST /v1/tasks 无 idempotencyKey——重试创建重复任务 | 已修复 | 根因不是“完全没有 idempotencyKey 概念”，而是任务创建既没有在 HTTP 主链强制检查 `Idempotency-Key`，也没有缓存成功响应用于重放。 | 当前 `src/platform/five-plane-interface/api/http-api-server.ts` 已对所有写请求统一执行 idempotency check/record；`POST /api/v1/tasks` 重放同一 key 会返回缓存的首个 201 响应而不是再次插入任务。`tests/integration/platform/interface/api/http-api-server-architecture-regressions.test.ts` 已验证任务数只增长 1 次。 | 已改主请求链，不再保留未解决结论。 |
| 2293 | Round 39 — IAM/SSO · UI Features · CLI/SDK · Cross-Cutting Architecture / §196 Cross-Cutting 架构级缺陷 | P0 | `src/platform/interface/api/http-server/request-helpers.ts:17-28` | matchRoute 仅允许 GET/POST/OPTIONS——PATCH/DELETE 路由不可达 | 已解决 | 旧结论停留在 `matchRoute()` 仅放行少数 method 的版本；现行实现已扩展到 canonical write verbs，但复审条目和单测仍按旧行为断言。 | 当前 `src/platform/five-plane-interface/api/http-server/request-helpers.ts` 已允许 `PUT/PATCH/DELETE/OPTIONS`；对应 `tests/unit/platform/interface/api/http-server/request-helpers.test.ts` 也已改为验证 `PUT/DELETE` 正常解析。 | 回写现行 helper 行为与定向测试依据。 |
| 2294 | Round 39 — IAM/SSO · UI Features · CLI/SDK · Cross-Cutting Architecture / §196 Cross-Cutting 架构级缺陷 | P0 | `src/platform/orchestration/oapeflir/oapeflir-loop-service.ts:86-116` | 核心编排 hard-instantiate 12+ 服务无 DI——不可测/不可替换/不可断路 | 已解决 | 根因是原审计对应的是 OAPEFLIR 早期直接在类内 new 各阶段服务的快照，没有吸收后续补进的 `OapeflirLoopServiceOptions` 注入边界。 | 当前 `src/platform/orchestration/oapeflir/oapeflir-loop-service.ts` 已支持注入 `executeBridge`、`situationBuilder`、`observationAggregator`、`assessmentService`、`feedbackCollector`、`directiveSink` 等依赖，且现有 OAPEFLIR 单测已广泛使用这些替身；本轮补充的 `tests/unit/platform/orchestration/oapeflir/di-source-review-2294.test.ts` 也已锁定这些注入点。 | 回写现行源码与定向测试依据。 |
| 2295 | Round 39 — IAM/SSO · UI Features · CLI/SDK · Cross-Cutting Architecture / §196 Cross-Cutting 架构级缺陷 | P1 | `src/platform/interface/api/http-server/ (全部路由)` | 无 correlationId 从 HTTP header 传播——下游事件/RSM/span 无法关联 | 已修复 | 根因是 request context 只把 `x-request-id` 当 trace 源，调用方显式传入的 `x-correlation-id` 没有进入统一上下文。 | 当前 `readRequestId()` 已优先读取 `x-correlation-id`，`HttpApiServer.dispatchRequest()` 会把它写入 `provideContext({ traceId, requestId })`，响应也会回传 `x-trace-id`。`tests/integration/platform/interface/api/http-api-server-architecture-regressions.test.ts` 已验证这条链路。 | 已改入口级 context 绑定与响应透传。 |
| 2296 | Round 39 — IAM/SSO · UI Features · CLI/SDK · Cross-Cutting Architecture / §196 Cross-Cutting 架构级缺陷 | P1 | `src/platform/interface/api/http-server/task-routes.ts:207-226` | 任务创建直写 store 绕过 RSM——不产生 PlatformFactEvent 违反 INV-STATE-001 | 已修复 | 根因是 `task-routes.ts` 早先虽然已经接入 intake admission，但 admission 产出的 canonical platform facts 没有落盘，导致任务 truth 写入后缺少对应不可变事件链。 | 当前 `src/platform/five-plane-interface/api/http-server/task-routes.ts` 已把 `IntakeAdmissionService.admit()` 返回的 `events` 与 task truth 放进同一个 `taskStore.db.transaction(...)` 中写入；定向验证 `tests/integration/platform/interface/api/http-api-server-architecture-regressions.test.ts` 已证明 `platform.harness_run.status_changed` 会随任务创建落库。 | 已改任务创建写链，真实补齐 fact append。 |
| 2297 | Round 39 — IAM/SSO · UI Features · CLI/SDK · Cross-Cutting Architecture / §196 Cross-Cutting 架构级缺陷 | P1 | `src/platform/state-evidence/events/durable-event-bus.ts:48` | 10ms poll 无 backpressure/max_queue_depth——无流控 | 已解决 | 根因是这条复审仍沿用旧版 durable event bus 的固定 10ms poll 印象，没有复核后续已经加入的自适应轮询和队列深度回压。 | 当前 `src/platform/state-evidence/events/durable-event-bus.ts` 已把 idle poll 收敛到 `100ms` 基线，并通过 `calculatePollingInterval()` 按 `queueDepth`、consumer priority、circuit breaker 和 consumer count 自适应退避；同时 `runtimeMetricsRegistry.recordEventBackpressure(...)` 会记录高水位与 pending 深度。定向验证见 `tests/unit/platform/state-evidence/events/durable-event-bus-backpressure-2297.test.ts`。 | 回写现行源码与定向测试依据。 |
| 2298 | Round 39 — IAM/SSO · UI Features · CLI/SDK · Cross-Cutting Architecture / §196 Cross-Cutting 架构级缺陷 | P1 | `src/platform/interface/api/http-server/response-hardening.ts:105` | API 版本仅静态 /v1/ 路径——无版本协商/弃用 header/content-type 版本 | 已修复 | 根因复核后更具体：版本 header 和 `VersionRoutingMiddleware` 早已存在，但它们一直没有接到 `HttpApiServer` 的真实 dispatch 主路径，所以只剩静态响应提示、没有协商语义。 | 当前 `src/platform/five-plane-interface/api/http-api-server.ts` 已在 `dispatchRequest()` 内强制解析 `Accept-Version`，对低于最低版本的请求直接拒绝，并把选定版本写回 `x-api-version`；`response-hardening.ts` 也已保留该协商结果而非覆盖成固定 `v1`。定向验证见 `tests/integration/platform/interface/api/http-api-server-architecture-regressions.test.ts`。 | 已把版本协商从“局部组件”推进到“主请求链强制生效”。 |
| 2299 | Round 39 — IAM/SSO · UI Features · CLI/SDK · Cross-Cutting Architecture / §196 Cross-Cutting 架构级缺陷 | P1 | `src/platform/orchestration/oapeflir/oapeflir-loop-service.ts:398-402` | 错误丢失阶段/任务/span 上下文——调试困难 | 已解决 | 根因是旧 `runStage()` 在阶段异常时重新抛错，只保留通用错误文本，丢失了 active span 上下文，导致 stage、task、trace、span 无法沿异常链保真传播。 | `src/platform/orchestration/oapeflir/oapeflir-loop-service.ts` 当前在 `runStage()` 内通过 `startActiveSpan(..., (_span, activeContext) => ...)` 捕获 `traceId/spanId/parentSpanId`，异常时把 `[stage:...] [task:...] [trace:...] [span:...]` 前缀写回消息，并把这些字段挂到错误对象上。新增 `tests/unit/platform/orchestration/oapeflir/oapeflir-loop-service.test.ts` 用例 `OapeflirLoopService preserves trace and span context on stage failures` 已定向证明失败路径会保留这些上下文。 | 这是现行代码真实修复，不再维持旧结论。 |
| 2300 | Round 39 — IAM/SSO · UI Features · CLI/SDK · Cross-Cutting Architecture / §196 Cross-Cutting 架构级缺陷 | P2 | `task-routes.ts + durable-event-bus.ts` | 任务写入与事件发布在分离事务——crash 间产生孤立任务 | 已修复 | 根因是任务创建主路径此前先插入 task，再在事务外丢弃/遗漏 intake 事件，truth 与 event 可以分离失败。 | 当前 `src/platform/five-plane-interface/api/http-server/task-routes.ts` 已把 task insert 和 `admissionResult.events` 的持久化统一放入 `taskStore.db.transaction(...)`；定向验证 `tests/integration/platform/interface/api/http-api-server-architecture-regressions.test.ts` 已证明任务写入后同事务可见对应 platform fact 事件。 | 已改为 truth+event 同事务写入，不再维持“孤立任务”结论。 |
| 2301 | Round 39 — IAM/SSO · UI Features · CLI/SDK · Cross-Cutting Architecture / §196 Cross-Cutting 架构级缺陷 | P2 | `src/platform/orchestration/oapeflir/oapeflir-loop-service.ts:144-165` | 边界验证失败静默降级无 incident/metric——掩盖系统性漂移 | 已解决 | 根因是旧 O→A、A→P 边界校验失败时仅中断流程或记录普通错误，没有独立 incident 信号与指标归因，系统性 schema 漂移会被埋在通用失败里。 | `src/platform/orchestration/oapeflir/oapeflir-loop-service.ts` 当前在两处边界校验失败时都会记录结构化错误、发出 `oapeflir.decision.recorded` 的 `boundary_violation` 事件，并调用 `runtimeMetricsRegistry.recordOapeflirBoundaryViolation(...)`；`src/platform/shared/observability/runtime-metrics-registry.ts` 已新增 `oapeflir_boundary_violation_total`。新增 `tests/unit/platform/orchestration/oapeflir/oapeflir-loop-service.test.ts` 用例 `OapeflirLoopService emits boundary violation signal and metric on O→A validation failure` 已验证事件和指标都会落地。 | 这一条现已具备专门 incident/metric 证据，不再保持未解决。 |
| 2302 | Round 39 — IAM/SSO · UI Features · CLI/SDK · Cross-Cutting Architecture / §196 Cross-Cutting 架构级缺陷 | P2 | `src/platform/shared/lifecycle/service-registry.ts:66-67` | ServiceRegistry 全局单例——多租户/多 worker 共享无隔离 | 已修复 | 根因不是“完全没有隔离能力”，而是隔离能力虽已存在，生产装配默认值却长期仍绑定 `ServiceRegistry.getInstance()`：顶层 bootstrap、runtime catalog、runtime orchestrator 和 root summary 在未显式传入 registry 时都会落到同一个全局单例，导致多租户/多 worker 默认共享注册表状态。 | 本轮已把 `src/platform/five-plane-runtime-bootstrap.ts`、`src/platform/five-plane-runtime-orchestrator.ts`、`src/platform/ai-operations-runtime-orchestrator.ts`、`src/domains-runtime-orchestrator.ts`、`src/interaction-governance-runtime-orchestrator.ts`、`src/scale-ops-runtime-orchestrator.ts`、对应 runtime catalog/startup plan/bootstrap 以及 `src/index.ts` 的默认 registry 统一切到 `ServiceRegistry.createScoped()`；新增 `tests/unit/platform/shared/service-registry-default-scope-2302.test.ts` 已验证 `registerFivePlaneRuntimeCatalog()`、`new FivePlaneRuntimeOrchestrator()` 和 `buildPlatformRootSummary()` 的默认路径不会再污染全局 registry，同批定向测试 `65 passed / 0 failed / 0 skipped` 也覆盖了五大装配链。 | 已改生产默认装配入口，不再维持“默认全局单例”结论。 |
| 2303 | Round 39 — IAM/SSO · UI Features · CLI/SDK · Cross-Cutting Architecture / §196 Cross-Cutting 架构级缺陷 | P2 | `src/platform/execution/runtime-state-machine.ts:150-189` | RSM 创建 event 但不持久化——调用方可忘记写入 | 需继续整改 | 根因是 `RuntimeStateMachine` 的职责设计仍停留在“纯状态跃迁和事件构造器”，它返回 `{ aggregate, event }`，但不拥有持久化边界，也不强制调用方把 truth 与 event 一并提交。这样一来，调用方漏写 event 或 truth 时，框架层不会阻止。 | `src/platform/execution/runtime-state-machine.ts` 当前 `transition()` 末尾仍直接 `return { aggregate, event }`；与之相对，`src/platform/state-evidence/events/transactional-event-appender.ts` 虽已提供事务型 truth+event 追加能力，但该能力并未被 RSM 强制纳入同一 API 边界。 | 保持未关闭，后续要么让 RSM 接管持久化边界，要么要求所有主路径只能通过事务型封装调用它。 |
| 2304 | Round 40 — Domains · Testing · Prompt-Engine/Proactive · HTTP Routes / §198 Domains (Quant-Trading / Legal / Registry) 缺陷 | P0 | `src/domains/quant-trading/index.ts:7` | 无领域特定风险因子(loss-limit/position-size/market-hours)——金融操作无安全门 | 已解决 | 根因是早期 quant-trading 模块只暴露 domain preset 和 review 规则，没有把金融域必需的损失上限、仓位上限、交易时段等风险门控建模为显式 guardrail。 | `src/domains/quant-trading/index.ts` 当前已定义 `QUANT_TRADING_RISK_GUARDRAILS` 与 `validateQuantTradingRisk()`，显式覆盖 `maxLossLimit`、`maxPositionSizeFraction`、`marketHoursOnly` 和 pre-trade validation。`tests/unit/domains/quant-trading/index.test.ts` 本轮新增 `QUANT_TRADING_RISK_GUARDRAILS encodes loss, position, and market-hour gates` 以及 4 条 `validateQuantTradingRisk ...` 用例，已定向证明越损失线、超仓位和 off-hours 都会 fail closed。 | 回写真实 guardrail 与定向测试依据，不再保留“无安全门”结论。 |
| 2305 | Round 40 — Domains · Testing · Prompt-Engine/Proactive · HTTP Routes / §198 Domains (Quant-Trading / Legal / Registry) 缺陷 | P0 | `src/domains/legal/index.ts:7` | 无合规检查(jurisdiction/privilege/confidentiality)——违反知识边界门要求 | 已解决 | 根因是早期 legal 模块只暴露 preset/review 信息，没有把法域、特权和保密级别约束编码成可执行的合规门。 | `src/domains/legal/index.ts` 当前已提供 `LEGAL_COMPLIANCE_GUARDRAILS` 与 `validateLegalCompliance()`，显式校验 `requiredJurisdictions`、`privilegeCheckRequired`、`confidentialityCheckRequired` 与最低 clearance。现有 `tests/unit/domains/legal/index.test.ts` 已覆盖合法上下文、非法法域、缺失 privilege 和低保密级别等路径。 | 这里是现行模块级合规门已落地并有测试覆盖，不再维持旧审计结论。 |
| 2306 | Round 40 — Domains · Testing · Prompt-Engine/Proactive · HTTP Routes / §198 Domains (Quant-Trading / Legal / Registry) 缺陷 | P0 | `src/domains/quant-trading+legal/index.ts` | 无 PlanGraphBundle——域 workflow 无 DAG 结构(spec 要求) | 已解决 | 根因有两层：一是 `WorkflowConfigSchema` 过去没有显式保留 `planGraph`，quant-trading 配置里的 DAG 结构在解析时会被当成未知字段丢弃；二是 `domain-baseline-catalog.ts` 从 `DOMAIN_SEEDS` 生成 baseline workflow 时只构造线性 `steps`，没有同步生成 canonical DAG 表示。 | 本轮在 `src/domains/registry/domain-model.ts` 为 `WorkflowConfigSchema` 新增 `planGraph` schema，避免域配置中的 DAG 结构被静默擦除；同时在 `src/domains/domain-baseline-catalog.ts` 的 `buildWorkflow()` 中新增 `planGraph` 生成逻辑，让 quant-trading、legal 等 baseline workflow 都自动携带 `graphId/nodes/edges/entryNodeIds/terminalNodeIds`。`tests/unit/domains/registry/domain-model.test.ts` 现已断言 `config/domains/quant-trading.json` 解析后保留 `planGraph`；新增 `tests/unit/domains/domain-baseline-catalog.test.ts` 用例 `vertical domain baseline catalog emits canonical DAG workflow metadata for regulated domains` 已验证 quant-trading 与 legal baseline 都带有 DAG 元数据。 | 这是本轮真实生产修复，不是假关闭。 |
| 2307 | Round 40 — Domains · Testing · Prompt-Engine/Proactive · HTTP Routes / §198 Domains (Quant-Trading / Legal / Registry) 缺陷 | P0 | `src/domains/recipes/recipe-executor.ts:34` | workflow 可用性检查仅 regex——非"nonexistent"开头的任意 ID 报告成功但不执行 | 已解决 | 根因是早期 `RecipeExecutor` 用 workflowId 命名启发式代替权威注册表查询，把“名字不像 nonexistent”误当成“workflow 可执行”。 | `src/domains/recipes/recipe-executor.ts` 当前在执行前显式调用 `this.workflowRegistry?.get(parsed.defaultWorkflowId)`；查不到就 fail closed 返回 `Workflow ... is not available in the registry.`，不再依据字符串前缀放行。`tests/unit/domains/recipes/recipe-executor.test.ts` 已覆盖 `RecipeExecutor.execute fails when workflow is absent from registry` 与 `... relies on registry instead of nonexistent-prefix stub logic`。 | 回写真实依据；这一条已经不是“regex 假检查”。 |
| 2308 | Round 40 — Domains · Testing · Prompt-Engine/Proactive · HTTP Routes / §198 Domains (Quant-Trading / Legal / Registry) 缺陷 | P1 | `src/domains/registry/plugin-ecosystem-runtime-service.ts:108-134` | buildPlan 调用两次——首次结果丢弃,两次间状态可变 | 已解决 | 根因是旧 `activateRuntime()` 先生成一份 plan，又在后续激活流程里重新 build，导致前一份快照被丢弃，plugin 或 connector 状态在两次构建之间可漂移。 | `src/domains/registry/plugin-ecosystem-runtime-service.ts` 当前在 `activateRuntime()` 开头只做一次 `const plan = this.buildPlan(...)`，随后整条激活链都复用该快照并直接返回。新增 `tests/unit/domains/registry/plugin-ecosystem-runtime-service.test.ts` 用例 `PluginEcosystemRuntimeService.activateRuntime builds the plan exactly once` 已定向证明调用次数收敛为 1。 | 现行实现已消除双构建漂移窗口。 |
| 2309 | Round 40 — Domains · Testing · Prompt-Engine/Proactive · HTTP Routes / §198 Domains (Quant-Trading / Legal / Registry) 缺陷 | P1 | `src/domains/registry/plugin-ecosystem-runtime-service.ts:96` | ready 检查忽略 degraded/registered/loaded 插件——不可用插件被视为就绪 | 已解决 | 根因是旧 readiness 只看 domain 状态和 connector 绑定，未把 plugin lifecycleState 真正纳入 runtime-ready 判定。 | `src/domains/registry/plugin-ecosystem-runtime-service.ts` 当前通过 `toPluginTarget()` 把 `healthy` 收窄为 `record.lifecycleState === "active"`，并在 `buildPlan()` 中对不健康 plugin 追加 `plugin not ready: ...` finding，最终把 `ready` 置为 false。现有 `PluginEcosystemRuntimeService.buildPlan includes plugin targets` 已证明 registered plugin 不再被标成 healthy；新增 `... marks plan not ready when plugin is only registered` 进一步验证 plan 会被挡住。 | 回写真实 lifecycle gate；registered/loading/inactive/unloaded/suspended 已不会被视为就绪。 |
| 2310 | Round 40 — Domains · Testing · Prompt-Engine/Proactive · HTTP Routes / §198 Domains (Quant-Trading / Legal / Registry) 缺陷 | P1 | `src/domains/registry/domain-smoke-test.ts:56-64` | 跨 workflow step 名碰撞——依赖图被覆写,环检测错误 | 已解决 | 根因是旧依赖图把 `stepName` 当成全局 key，不同 workflow 中同名 step 会互相覆盖，导致 DFS 读到错误 owner。 | `src/domains/registry/domain-smoke-test.ts` 当前把节点键提升为 `workflowId/stepName`，并按 workflow 维护 `workflowStepIds` 与 `dependsOnMap`，不再发生跨 workflow 覆写。新增 `tests/unit/domains/registry/domain-smoke-test.test.ts` 用例 `DomainSmokeTestRunner keeps duplicate step names isolated per workflow` 已验证同名 step 共存时仍能正确识别原 workflow 内的环。 | 这条已从结构性覆盖错误收敛到现行已修复状态。 |
| 2311 | Round 40 — Domains · Testing · Prompt-Engine/Proactive · HTTP Routes / §198 Domains (Quant-Trading / Legal / Registry) 缺陷 | P1 | `src/domains/registry/domain-smoke-test.ts:80-81` | 悬空依赖 continue 跳过——断裂 workflow 通过检查 | 已解决 | 根因是旧 DFS 遇到缺失依赖时直接 `continue`，既不累计错误也不让 smoke test fail closed。 | `src/domains/registry/domain-smoke-test.ts` 当前会把缺失边记录进 `danglingDeps`，并以 `passed: !hasCycle && danglingDeps.length === 0` 作为最终门禁。新增 `tests/unit/domains/registry/domain-smoke-test.test.ts` 用例 `DomainSmokeTestRunner fails closed on dangling workflow dependencies` 已验证断裂依赖会把 `dependency_graph` 检查打红。 | 现行实现不再放过悬空依赖。 |
| 2312 | Round 40 — Domains · Testing · Prompt-Engine/Proactive · HTTP Routes / §198 Domains (Quant-Trading / Legal / Registry) 缺陷 | P1 | `src/domains/registry/domain-model.ts:87-91` | bindingRole preprocess 非法值返回 undefined 非拒绝——掩盖类型错误 | 已解决 | 根因是旧 preprocess 会把非法 `bindingRole` 折叠成 `undefined`，而后面的 optional enum 会把“值非法”误当成“值缺失”。 | `src/domains/registry/domain-model.ts` 当前对非法字符串和非字符串值返回 `null`，随后由 `z.enum(...).optional()` 明确拒绝；只有合法 alias 才会被规范化。新增 `tests/unit/domains/registry/domain-model.test.ts` 用例 `PluginBindingSchema rejects invalid bindingRole aliases instead of erasing them` 已证明非法值不再被静默吞掉。 | 这里是现行 schema 行为已修正，不再维持旧审计结论。 |
| 2313 | Round 40 — Domains · Testing · Prompt-Engine/Proactive · HTTP Routes / §198 Domains (Quant-Trading / Legal / Registry) 缺陷 | P1 | `src/domains/recipes/recipe-registry.ts:37-39` | trigger phrase 子串首匹配——短词("trade")遮蔽长词("trade options") | 已解决 | 根因是旧匹配逻辑按 recipe 顺序和首个 trigger phrase 长度提前返回，短词一旦先命中，就会遮蔽同一输入里的更长短语。 | 本轮对 `src/domains/recipes/index.ts` 做了真实代码修复：`matchDomainRecipe()` 现在遍历全部 trigger phrase，继续使用 token-boundary regex，但不再首命中即返回，而是选择“已命中的最长短语”，同长度再按原始注册顺序稳定决策。新增 `tests/unit/domains/recipes/index.test.ts` 与 `tests/unit/domains/recipes/recipe-registry.test.ts` 的 `... prefers the longest matching trigger phrase ...` 用例，已证明 `trade options` 不会再被 `trade` 抢先吞掉。 | 这是本轮真实修复完成的条目，不是假关闭。 |
| 2314 | Round 40 — Domains · Testing · Prompt-Engine/Proactive · HTTP Routes / §198 Domains (Quant-Trading / Legal / Registry) 缺陷 | P1 | `src/domains/registry/domain-registry-service.ts:41-43` | validated→registered 自动提升无事件——审计断裂 | 已解决 | 根因是旧 `register()` 在把 `validated` 规格自动归一化为 `registered` 后，没有为这次状态提升补发对应注册事件。 | `src/domains/registry/domain-registry-service.ts` 当前在 auto-promotion 分支显式发布 `domain:registered`，并把 payload.status 固定写成 `registered`；非 auto-promotion 分支也同样统一走发布链。现有 `tests/unit/domains/registry/domain-registration.test.ts` 用例 `register emits domain:registered event` 已验证输入 `status: "validated"` 时，事件 payload 会落成 `status: "registered"`。 | 这一条现已具备审计链路，不再保持未解决。 |
| 2315 | Round 40 — Domains · Testing · Prompt-Engine/Proactive · HTTP Routes / §198 Domains (Quant-Trading / Legal / Registry) 缺陷 | P2 | `src/domains/registry/plugin-runtime-child.ts:89-92` | sandbox root 从 env 取无路径遍历检查 | 已解决 | 根因是 child runtime 早期只防 `..`、`//`、`~` 这类明文片段，直接对 `AA_PLUGIN_SANDBOX_ROOT` 执行 `process.chdir(...)`，遗漏了编码遍历、反斜杠和空字节等变体。 | `src/domains/registry/plugin-runtime-child.ts` 现会拒绝 `%00`、`%2e`、`%2f`、`\\`、`\0`，并对归一化后的 root 再做二次 `..` 与 `//` 检查后才 `chdir`；本轮定向域注册回归 `plugin-spi-registry.test.ts`、`plugin-spi-registry-invocation.test.ts` 已通过。 | 回写现行 guard 逻辑；该问题不再维持“无路径遍历检查”的旧结论。 |
| 2316 | Round 40 — Domains · Testing · Prompt-Engine/Proactive · HTTP Routes / §198 Domains (Quant-Trading / Legal / Registry) 缺陷 | P2 | `src/domains/recipes/index.ts:22` | name 可选——注册无名 recipe 不可调试 | 已解决 | 根因是 `DomainRecipeSchema` 之前把 `name` 默认成空字符串，导致 schema parse 后仍可能把无标签 recipe 持久化进注册表。 | `src/domains/recipes/index.ts` 现将 `name` 改为 `trim().optional()`，并在 object transform 中把缺失或空白名称回填为 `recipeId`；新增回归 `tests/unit/domains/recipes/index.test.ts` 已验证默认值和空白名都会落成可调试标签。 | 本轮做了真实 schema 修复，不再保留无名 recipe。 |
| 2317 | Round 40 — Domains · Testing · Prompt-Engine/Proactive · HTTP Routes / §198 Domains (Quant-Trading / Legal / Registry) 缺陷 | P2 | `src/domains/registry/domain-registry-service.ts:106-111` | deprecate 无状态守卫——draft/archived 均可 deprecate | 已解决 | 原审计依据是早于状态机收紧的旧快照；现行实现已把 `deprecate` 收窄为 `active -> deprecated` 单一路径。 | `src/domains/registry/domain-registry-service.ts:174-186` 现明确在 `current.status !== "active"` 时抛 `domain_registry.invalid_deprecate_state`；本轮定向回归 `tests/unit/domains/registry/domain-registry-service.test.ts` 已通过 `deprecate throws when domain is not active`。 | 回写现行状态守卫与定向测试依据。 |
| 2318 | Round 40 — Domains · Testing · Prompt-Engine/Proactive · HTTP Routes / §198 Domains (Quant-Trading / Legal / Registry) 缺陷 | P2 | `src/domains/registry/plugin-spi-registry.ts:646` | failure threshold off-by-one: >maxFail 非 >=——第4次才禁用 | 已解决（原指控已证伪） | 根因不是 off-by-one，而是审计把 `maxConsecutiveFailures` 误读成“达到阈值即禁用”；现行语义是“容忍 N 次连续失败，在第 N+1 次失败时禁用”。 | `src/domains/registry/plugin-spi-registry.ts:683-688` 已在注释中说明 `>` 是有意语义；本轮定向回归 `tests/unit/domains/registry/plugin-spi-registry.test.ts` 与 `plugin-spi-registry-invocation.test.ts` 已验证 `maxConsecutiveFailures: 1` 在第 2 次失败后禁用、`maxConsecutiveFailures: 2` 在第 3 次失败后禁用。 | 不做假修复：这里应纠正审计结论，而不是把正确行为改坏。 |
| 2319 | Round 40 — Domains · Testing · Prompt-Engine/Proactive · HTTP Routes / §199 Testing 基础设施缺陷 | P0 | `tests/unit/platform/workspace/sandbox-policy-security.test.ts:171` | 断言 `allowed===true` 或 `allowed===false` 恒真——命令注入测试永不失败 | 已解决 | 根因是旧测试把“路径边界校验”和“命令注入拦截”混在一起，并用双值覆盖式断言掩盖了真实语义。 | `tests/unit/platform/workspace/sandbox-policy-security.test.ts:163-176` 现直接对 `checkSandboxPath(...).allowed` 做精确断言，并明确 shell metacharacter 在路径层是普通字符、注入防护应由执行层负责；旧的恒真断言已不存在。 | 回写现行测试语义，避免继续把陈旧 tautology 当成未修复问题。 |
| 2320 | Round 40 — Domains · Testing · Prompt-Engine/Proactive · HTTP Routes / §199 Testing 基础设施缺陷 | P0 | `tests/integration/core/runtime/distributed-lock-service.test.ts:21` | `keys.length>=0` 恒真——分布式锁集成测试验证零行为 | 已解决 | 原问题来自仅检查非负长度的空信号断言；当前文件已改成校验真实导出内容。 | `tests/integration/core/runtime/distributed-lock-service.test.ts:17-38` 现断言模块 `keys.length > 0`，且至少导出 lock 或 adapter 相关 symbol；本轮定向测试该文件已通过。 | 回写现行断言与定向测试依据。 |
| 2321 | Round 40 — Domains · Testing · Prompt-Engine/Proactive · HTTP Routes / §199 Testing 基础设施缺陷 | P0 | `tests/unit/platform/stability/stable-concurrency-rehearsal.test.ts:75` | failedScenarios>0 时跳过断言——失败时测试仍通过 | 已解决 | 根因是旧测试只看总数，不强制每个场景为通过态；失败场景不会把测试真正打红。 | `tests/unit/platform/stability/stable-concurrency-rehearsal.test.ts:9-35` 现同时断言 `passedScenarios === totalScenarios`、`passedScenarios === 3`，并逐项校验每个 `scenario.passed === true`。 | 断言缺口已补；该项不再保留“失败仍通过”的结论。 |
| 2322 | Round 40 — Domains · Testing · Prompt-Engine/Proactive · HTTP Routes / §199 Testing 基础设施缺陷 | P0 | `tests/golden/config-file-generation.test.ts:38` | catch{assert.ok(true)} 吞掉所有失败——3个 golden 测试是 no-op | 已解决 | 旧问题是 golden 测试曾吞异常并没有构造完整 fixture；现行文件已恢复真实 `ConfigGovernanceService.loadBundle()` 调用，并补齐 provider registry 与 bootstrap 必填字段。 | `tests/golden/config-file-generation.test.ts:22-113` 现会写入 `providers/models.json`、`bootstrap.dependencyOrder`、`healthCheckTimeoutMs`、`degradationPolicy`、`readinessGates`，然后直接断言 `bundle.issues.length === 0`；本轮定向测试 `tests/golden/config-file-generation.test.ts` 已 4/4 通过。 | 回写真实 golden fixture 与定向测试依据。 |
| 2323 | Round 40 — Domains · Testing · Prompt-Engine/Proactive · HTTP Routes / §199 Testing 基础设施缺陷 | P1 | `tests/unit/platform/stability/stable-chaos-smoke.test.ts:88` | 仅断言 typeof fn==="function"——从不调用被测函数 | 已解决 | 根因是旧 smoke 报告测试只检查函数 shape，没有验证落盘副作用。 | `tests/unit/platform/stability/stable-chaos-smoke.test.ts:97-121` 现会真实调用 `writeStableChaosSmokeReport()`，再检查文件存在并读取内容；本轮用 `--test-name-pattern` 定向执行该子用例已通过。 | 回写真实调用与定向子用例依据。 |
| 2324 | Round 40 — Domains · Testing · Prompt-Engine/Proactive · HTTP Routes / §199 Testing 基础设施缺陷 | P1 | `tests/unit/platform/stability/stable-evidence-campaign.test.ts:15-98` | 测试自己的字面量——零生产代码执行 | 已解决（文件仍受独立编译故障阻断） | 该条的根因已不是“零生产代码执行”：现行文件前半段仍保留结构字面量 smoke，但后半段已经改成真实调用 `runStableEvidenceCampaign()`。 | `tests/unit/platform/stability/stable-evidence-campaign.test.ts:84-140` 现对 `runStableEvidenceCampaign()` 做真实执行、最小 smoke 完成和结果断言；本轮未把该文件计入通过依据，因为它被工作树中独立的 `runtime-recovery-service-root.ts` 编译错误阻断。 | 不假关单地回写：本条审计指控已过时，但该测试文件今天未重新跑通并非本条自身根因。 |
| 2325 | Round 40 — Domains · Testing · Prompt-Engine/Proactive · HTTP Routes / §199 Testing 基础设施缺陷 | P1 | `tests/unit/platform/stability/stable-chaos-smoke.test.ts:7-86` | 共享 /tmp 无隔离——并行运行互相污染 | 已解决 | 根因是旧 smoke 用例复用固定 `/tmp` 路径，多个进程并发时会覆盖同一目录。 | `tests/unit/platform/stability/stable-chaos-smoke.test.ts:9-15` 以及后续各场景现都使用 `mkdtempSync(join(tmpdir(), uniqueId))` 创建独立临时目录，不再共享固定路径。 | 回写现行隔离策略；该项不再维持并行污染结论。 |
| 2326 | Round 40 — Domains · Testing · Prompt-Engine/Proactive · HTTP Routes / §199 Testing 基础设施缺陷 | P1 | `tests/integration/security/input-validation.test.ts:408-436` | 测试输入无控制字符——验证错误行为(合法命令被阻止) | 已解决 | 原问题是测试样本没有真正携带控制字符，导致它只是在检查普通命令是否被误杀。 | `tests/integration/security/input-validation.test.ts:412-436` 现使用带实际控制字符的 `maliciousCommand = "echo\u0001\u0002\u0003"` 并断言执行结果为 `blocked`；本轮定向安全回归已通过。 | 回写恶意样本与定向测试依据。 |
| 2327 | Round 40 — Domains · Testing · Prompt-Engine/Proactive · HTTP Routes / §199 Testing 基础设施缺陷 | P1 | `tests/unit/platform/stability/stable-concurrency-rehearsal.comprehensive.test.ts:179-231` | 类型 shape 自我断言——无生产代码覆盖 | 已解决（原指控已过时） | 原审计抓到的是早期只校验 options 或类型 shape 的版本；现行同一段范围内已补入真实 rehearsal 运行。 | `tests/unit/platform/stability/stable-concurrency-rehearsal.comprehensive.test.ts:190-216` 现直接调用 `runStableConcurrencyRehearsal({ outputDir })` 并校验真实 report 结构与三类场景结果，因此“无生产代码覆盖”已不成立。 | 回写现行覆盖情况；保留的 type-shape smoke 不再构成该条原指控。 |
| 2328 | Round 40 — Domains · Testing · Prompt-Engine/Proactive · HTTP Routes / §199 Testing 基础设施缺陷 | P1 | `tests/e2e/distributed-lock-e2e.test.ts:270-327` | "并发"测试顺序执行——无 Promise.all/无 worker/无竞态 | 已解决 | 旧问题是两个 owner 依次 acquire 同一把锁，没有制造真实竞争窗口。 | `tests/e2e/distributed-lock-e2e.test.ts:329-347` 现使用 `Promise.all([...])` 并断言“恰好 1 个成功、1 个失败”；代码层面已是并发竞争测试。 | 回写现行 `Promise.all` 竞态测试依据。 |
| 2329 | Round 40 — Domains · Testing · Prompt-Engine/Proactive · HTTP Routes / §199 Testing 基础设施缺陷 | P2 | `tests/integration/security/path-traversal.test.ts:81-110` | 编码遍历测试先解码再传入——从未测试编码绕过 | 已解决 | 根因是旧测试在进入执行器前先 `decodeURIComponent(...)`，把“编码绕过”退化成普通 `../`。 | `tests/integration/security/path-traversal.test.ts:81-119` 现把 `%2e%2e%2f...` 与 `%252e%252e%252f...` 直接传给执行器，并分别断言单编码和双编码遍历都被 `blocked`；本轮定向安全回归已通过。 | 回写编码样本与定向测试依据。 |
| 2330 | Round 40 — Domains · Testing · Prompt-Engine/Proactive · HTTP Routes / §199 Testing 基础设施缺陷 | P2 | `tests/integration/security/command-injection.test.ts:450-478` | if(blocked){fail}——unknown status 静默通过 | 已解决 | 根因是旧测试只排除 `blocked`，却没有要求安全命令必须明确成功，导致未知状态也会静默通过。 | `tests/integration/security/command-injection.test.ts:450-478` 现直接断言合法 `echo` 调用的 `result.status === "succeeded"`，并要求无错误负载；本轮定向安全回归已通过。 | 回写精确状态断言与定向测试依据。 |
| 2331 | Round 40 — Domains · Testing · Prompt-Engine/Proactive · HTTP Routes / §199 Testing 基础设施缺陷 | P2 | `tests/integration/security/input-validation.test.ts:497-524` | 断言 includes 3 个值覆盖全部结果——恒通过 | 已解决 | 根因是旧断言把所有可能状态都列进 `includes(...)`，导致任何结果都能过。 | `tests/integration/security/input-validation.test.ts:499-527` 现对脚本参数注入场景直接断言 `result.status === "blocked"`，并要求返回错误码；本轮定向安全回归已通过。 | 回写精确阻断断言与定向测试依据。 |
| 2332 | Round 40 — Domains · Testing · Prompt-Engine/Proactive · HTTP Routes / §199 Testing 基础设施缺陷 | P2 | `tests/unit/platform/stability/stable-concurrency-rehearsal.test.ts:8-66` | 每个场景测试跑全套——4x 冗余 | 已解决 | 根因是旧文件用多条测试各自跑完整 rehearsal，再分别查单个场景，导致同一三场景套件被重复执行。 | `tests/unit/platform/stability/stable-concurrency-rehearsal.test.ts:9-35` 现收敛为一条完整 rehearsal 测试，同时检查总场景数、通过数与逐场景 `passed`；报告写盘另拆为独立轻量测试。 | 本轮做了真实去冗余，不再保留 4x 重跑。 |
| 2333 | Round 40 — Domains · Testing · Prompt-Engine/Proactive · HTTP Routes / §199 Testing 基础设施缺陷 | P2 | `tests/integration/security/input-validation.test.ts:439-466` | `ok(succeeded)` 或 `ok(blocked)` 的双值覆盖无信号 | 已解决 | 根因是旧断言把“成功”与“阻断”都当成可接受结果，无法反映当前实现的真实语义。 | `tests/integration/security/input-validation.test.ts:459-465` 现明确断言 `echo` 空参数调用应 `succeeded` 且无错误负载；本轮定向安全回归已通过。 | 回写精确行为断言，不再保留双值覆盖。 |
| 2334 | Round 40 — Domains · Testing · Prompt-Engine/Proactive · HTTP Routes / §200 Prompt-Engine / Proactive-Agent 深层缺陷 | P0 | `src/platform/prompt-engine/rollout/prompt-rollout-stage.ts:25-29` | nextStage("stable")→"rolled_back"——语义错误 | 已解决 | 原审计停留在 `stable` 仍可继续前推的旧快照，没有吸收终态保护。 | `src/platform/prompt-engine/rollout/prompt-rollout-stage.ts` 现对 `stable/rolled_back` 返回 `null`；`tests/unit/platform/prompt-engine/rollout/index.test.ts` 已验证 stable 不会再经 `activateRollout()` 前推。 | 回写现行实现与定向测试依据。 |
| 2335 | Round 40 — Domains · Testing · Prompt-Engine/Proactive · HTTP Routes / §200 Prompt-Engine / Proactive-Agent 深层缺陷 | P0 | `src/interaction/proactive-agent/trigger-engine/index.ts:6-7` | critical risk+!requireConfirmation→silent_record——违反自治边界 | 已解决 | 根因是历史测试和复审文档仍把 critical 无确认分支当成 `silent_record`，没有跟上自治边界收紧后的 canonical 行为。 | `src/interaction/proactive-agent/trigger-engine/index.ts` 现对 `critical` 返回 `suggest`；`tests/unit/interaction/proactive-agent/trigger-engine/index.test.ts` 与 `tests/unit/interaction/proactive-agent/index.test.ts` 已回归。 | 回写依据并修正陈旧测试。 |
| 2336 | Round 40 — Domains · Testing · Prompt-Engine/Proactive · HTTP Routes / §200 Prompt-Engine / Proactive-Agent 深层缺陷 | P0 | `src/platform/prompt-engine/rollout/index.ts:6` | PromptRolloutMode 无 "canary"——mode 与 stage 断连 | 已解决 | 原问题来自 mode 枚举和 staged rollout 主线分离的旧版本；现行 mode 已补齐。 | `src/platform/prompt-engine/rollout/index.ts` 已包含 `canary`；`tests/unit/platform/prompt-engine/rollout/index.test.ts` 已新增 `canary` mode 断言。 | 回写依据并补定向测试。 |
| 2337 | Round 40 — Domains · Testing · Prompt-Engine/Proactive · HTTP Routes / §200 Prompt-Engine / Proactive-Agent 深层缺陷 | P0 | `src/platform/prompt-engine/rollout/index.ts:65-76` | activateRollout ready→active 无 dwell-time——跳过所有渐进阶段 | 已解决 | 根因是旧复审没有复核后续加入的阶段驻留时间门禁。 | `src/platform/prompt-engine/rollout/index.ts` 已要求每阶段至少停留 24h 后才能推进；`tests/unit/platform/prompt-engine/rollout/index.test.ts` 已覆盖拒绝与推进路径。 | 回写依据并补定向测试。 |
| 2338 | Round 40 — Domains · Testing · Prompt-Engine/Proactive · HTTP Routes / §200 Prompt-Engine / Proactive-Agent 深层缺陷 | P1 | `src/platform/prompt-engine/registry/hierarchical-registry-service.ts:261-268` | traffic slot 权重不归一——高于 total 的 slot 永不匹配 | 已解决 | 原根因是流量权重归一化已落地，但复审文档和测试仍停在旧实现。 | `src/platform/prompt-engine/registry/hierarchical-registry-service.ts` 现按 `totalWeight` 归一化后再做 slot 命中；`tests/unit/platform/prompt-engine/registry/hierarchical-registry-service.test.ts` 已回归 50/50、80/20 和零/负权重。 | 回写依据并补定向测试。 |
| 2339 | Round 40 — Domains · Testing · Prompt-Engine/Proactive · HTTP Routes / §200 Prompt-Engine / Proactive-Agent 深层缺陷 | P1 | `src/platform/prompt-engine/registry/hierarchical-registry-service.ts:388-409` | findBundle 忽略 version 参数——deprecate 错误 bundle | 已修复 | `findBundle(version)` 缺口虽已补过，但 `deprecate/remove` 仍会把 scope 当前指针错误回写到被废弃版本；根因是 version store 与 scope current pointer 没有同步刷新。 | 本轮在 `src/platform/prompt-engine/registry/hierarchical-registry-service.ts` 增加 `refreshScopeBundle(...)`，让 `deprecateBundle()` / `removeBundle()` 在版本变更后重算当前有效 bundle；`tests/unit/platform/prompt-engine/registry/hierarchical-registry-service.test.ts` 已验证废弃 v1 后仍返回 v2。 | 已改生产代码与定向测试。 |
| 2340 | Round 40 — Domains · Testing · Prompt-Engine/Proactive · HTTP Routes / §200 Prompt-Engine / Proactive-Agent 深层缺陷 | P1 | `src/platform/prompt-engine/eval/cross-provider-judge-service.ts:200` | agreementScore=promoteCount/total——全票 rollback 得 0 | 已解决 | 原审计停留在只用 promote 比例计分的旧实现，没有复核 modal decision 共识分数。 | `src/platform/prompt-engine/eval/cross-provider-judge-service.ts` 现按 modal decision 计算 `agreementScore`；`tests/unit/platform/prompt-engine/eval/cross-provider-judge-service.test.ts` 已验证 unanimous promote/hold 都得到 `1.0`。 | 回写依据并补定向测试。 |
| 2341 | Round 40 — Domains · Testing · Prompt-Engine/Proactive · HTTP Routes / §200 Prompt-Engine / Proactive-Agent 深层缺陷 | P1 | `src/platform/prompt-engine/eval/cross-provider-judge-service.ts:21-22` | parallelEvaluation 声明未使用——judges 恒串行 | 已解决 | 根因是旧复审没有吸收后续 `evaluateWithPipeline()` 已按 `parallelEvaluation` 分支选择执行路径的事实。 | `src/platform/prompt-engine/eval/cross-provider-judge-service.ts` 当前已显式分 `parallelEvaluation=true/false` 两条路径；`tests/unit/platform/prompt-engine/eval/cross-provider-judge-service.test.ts` 已补 parallel 分支回归。 | 回写依据并补定向测试。 |
| 2342 | Round 40 — Domains · Testing · Prompt-Engine/Proactive · HTTP Routes / §200 Prompt-Engine / Proactive-Agent 深层缺陷 | P1 | `src/platform/prompt-engine/eval/eval-dataset-judge-service.ts:490` | criterion scores 无 [0,1] range 验证——999/-5 腐败加权 | 已解决 | 原审计停留在外部信号未做 clamp 的旧版本。 | `src/platform/prompt-engine/eval/eval-dataset-judge-service.ts` 已把 `criterionSignals` clamp 到 `[0,1]`；`tests/unit/platform/prompt-engine/eval/eval-dataset-judge-service.test.ts` 已验证 `999` 被截成 `1`。 | 回写依据并补定向测试。 |
| 2343 | Round 40 — Domains · Testing · Prompt-Engine/Proactive · HTTP Routes / §200 Prompt-Engine / Proactive-Agent 深层缺陷 | P1 | `src/platform/prompt-engine/eval/eval-dataset-judge-service.ts:263-393` | 无 hold-out set 分割——全数据集训练+评估,违反统计严格性 | 已解决 | 原审计没有复核后续加入的 deterministic holdout split。 | `src/platform/prompt-engine/eval/eval-dataset-judge-service.ts` 已通过 `holdoutRatio + buildHoldoutSplit()` 支持确定性保留集；`tests/unit/platform/prompt-engine/eval/eval-dataset-judge-service.test.ts` 已验证 `holdoutRatio=0.5` 时只评估子集。 | 回写依据并补定向测试。 |
| 2344 | Round 40 — Domains · Testing · Prompt-Engine/Proactive · HTTP Routes / §200 Prompt-Engine / Proactive-Agent 深层缺陷 | P1 | `src/interaction/proactive-agent/index.ts:270-271` | Math.max(0.6,...) 硬编码覆盖配置——proactive budget 强制≤40% | 已解决 | 原问题来自旧的保底 `0.6` clamp；现行实现已改为直接使用配置的 `userInitiatedReserveRatio`。 | `src/interaction/proactive-agent/index.ts` 现以 `Math.max(0, Math.min(1, budgetPool.userInitiatedReserveRatio))` 计算 reserve ratio，不再强制保留 60%。 | 回写源码依据。 |
| 2345 | Round 40 — Domains · Testing · Prompt-Engine/Proactive · HTTP Routes / §200 Prompt-Engine / Proactive-Agent 深层缺陷 | P1 | `src/interaction/proactive-agent/index.ts:392-430` | detectFeedbackLoop 禁用整个祖先链非仅循环成员 | 已解决 | 根因是历史实现把整条 ancestry path 都算进循环成员；现行逻辑已只标记真实回路节点。 | `src/interaction/proactive-agent/index.ts` 当前使用 `cycleMembers` 收敛到真实循环节点；`tests/unit/interaction/proactive-agent/index.test.ts` 已覆盖双节点、自环和长链场景。 | 回写依据并保留定向测试。 |
| 2346 | Round 40 — Domains · Testing · Prompt-Engine/Proactive · HTTP Routes / §200 Prompt-Engine / Proactive-Agent 深层缺陷 | P2 | `src/platform/prompt-engine/registry/prompt-version-manager.ts:89-105` | compareVersions 返回任意整数非 -1/0/1——严格比较调用方 break | 已解决 | 原审计停留在返回差值的旧版 compare；现行实现已归一到三值比较。 | `src/platform/prompt-engine/registry/prompt-version-manager.ts` 当前 `compareVersions()` 只返回 `-1/0/1`；现有单测已覆盖三种结果。 | 回写源码与测试依据。 |
| 2347 | Round 40 — Domains · Testing · Prompt-Engine/Proactive · HTTP Routes / §200 Prompt-Engine / Proactive-Agent 深层缺陷 | P2 | `src/interaction/proactive-agent/index.ts:316-322 vs trigger-engine` | 重复 action-mode 逻辑——行为不一致 | 已修复 | 根因是 `evaluate()` 在调用 `resolveTriggerActionMode()` 后又手写一层 medium/high/critical 降级分支，形成重复逻辑和潜在漂移。 | 本轮已删除 `src/interaction/proactive-agent/index.ts` 中这段重复风险分支，只保留共享 `resolveTriggerActionMode()` 和自治等级门控；`tests/unit/interaction/proactive-agent/index.test.ts` 已回归低风险/中风险/critical 行为。 | 已改生产代码与定向测试。 |
| 2348 | Round 40 — Domains · Testing · Prompt-Engine/Proactive · HTTP Routes / §200 Prompt-Engine / Proactive-Agent 深层缺陷 | P2 | `src/interaction/proactive-agent/index.ts:358-360` | input.event 可 undefined 但 .name 无 optional chaining——TypeError | 已解决 | 原审计基于旧分支；现行匹配逻辑已在事件路径统一使用可选链。 | `src/interaction/proactive-agent/index.ts` 的 `matchesTrigger()` 现使用 `input.event?.name?.includes(...)`，不会在 `event` 缺失时抛出 TypeError。 | 回写源码依据。 |
| 2349 | Round 40 — Domains · Testing · Prompt-Engine/Proactive · HTTP Routes / §201 HTTP Routes / Interface 深层缺陷 | P0 | `src/platform/interface/api/http-server/incident-routes.ts:88` | GET incident 无 tenant 隔离——void principal 后跨租户读 | 已解决 | 根因是旧实现没有把 principal tenant scope 下沉到 `getIncident()`；现行路由已先解析 tenant scope 再查 incident。 | `src/platform/interface/api/http-server/incident-routes.ts` 当前先 `resolveTenantScope(...)` 再调用 `incidentService.getIncident(tenantId, incidentId)`；`tests/unit/platform/interface/api/http-server/incident-routes.test.ts` 已验证跨租户读被拒绝。 | 回写依据并补定向测试。 |
| 2350 | Round 40 — Domains · Testing · Prompt-Engine/Proactive · HTTP Routes / §201 HTTP Routes / Interface 深层缺陷 | P0 | `src/platform/interface/api/http-server/incident-routes.ts:129` | PATCH incident 无 tenant 隔离——跨租户修改 | 已解决 | 根因同上，旧更新路径没有 tenant-scoped lookup；现行 PATCH 已先按 tenant scope 裁剪。 | `src/platform/interface/api/http-server/incident-routes.ts` 当前在 PATCH 前先查 `incidentService.getIncident(tenantId, incidentId)`；`tests/unit/platform/interface/api/http-server/incident-routes.test.ts` 已覆盖跨租户 PATCH 被拒绝。 | 回写依据并补定向测试。 |
| 2351 | Round 40 — Domains · Testing · Prompt-Engine/Proactive · HTTP Routes / §201 HTTP Routes / Interface 深层缺陷 | P0 | `src/platform/interface/api/http-server/incident-routes.ts:66-67` | list incidents void tenantId——返回全租户数据 | 已解决 | 根因是旧列表接口允许无 tenant scope 直接透传；现行实现已要求 tenant scope 才能 list。 | `src/platform/interface/api/http-server/incident-routes.ts` 现在 `tenantId === undefined` 时直接 403；`tests/unit/platform/interface/api/http-server/incident-routes.test.ts` 已验证只返回当前 tenant 数据。 | 回写依据并补定向测试。 |
| 2352 | Round 40 — Domains · Testing · Prompt-Engine/Proactive · HTTP Routes / §201 HTTP Routes / Interface 深层缺陷 | P0 | `src/platform/interface/channel-gateway/websocket-bridge.ts:158-172` | JWT token 在 URL query——泄漏至日志/代理/Referer | 已解决（源码复核） | 原审计抓的是早期 WebSocket 握手把认证参数放在 URL query 的实现；现行 bridge 已把 JWT 收敛到 `Sec-WebSocket-Protocol`，query 里只保留 `taskId/last_event_id`。 | 当前 `src/platform/interface/channel-gateway/websocket-bridge.ts` 与 `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts` 都只从 `req.headers["sec-websocket-protocol"]` 读取 token，未再读取 `query.token/jwt`。 | 回写现行实现依据；关联 WebSocket 覆盖测试在当前 sandbox 中因 `listen 127.0.0.1` 返回 `EPERM` 无法完成握手回归，本轮不把该环境噪音误判成该问题未修。 |
| 2353 | Round 40 — Domains · Testing · Prompt-Engine/Proactive · HTTP Routes / §201 HTTP Routes / Interface 深层缺陷 | P1 | `src/platform/interface/api/http-server/incident-routes.ts:86` | incidentId 无格式验证——任意字符串传入 | 已解决 | 原因是旧实现直接信任 path segment；现行路由已做格式白名单校验。 | `src/platform/interface/api/http-server/incident-routes.ts` 现用 `/^[a-zA-Z0-9_-]{1,128}$/` 校验 incidentId；`tests/unit/platform/interface/api/http-server/incident-routes.test.ts` 已补非法 ID 用例。 | 回写依据并补定向测试。 |
| 2354 | Round 40 — Domains · Testing · Prompt-Engine/Proactive · HTTP Routes / §201 HTTP Routes / Interface 深层缺陷 | P1 | `src/platform/interface/api/middleware/sanitize.ts:22-39` | JSON sanitize 无递归深度限制——10k 嵌套 stack overflow DoS | 已解决 | 原审计停留在无深度上限的旧版本；现行 sanitize 已加入最大深度门禁。 | `src/platform/interface/api/middleware/sanitize.ts` 当前有 `MAX_SANITIZE_DEPTH = 64` 且超限抛 `api.json_too_deep`。 | 回写源码依据。 |
| 2355 | Round 40 — Domains · Testing · Prompt-Engine/Proactive · HTTP Routes / §201 HTTP Routes / Interface 深层缺陷 | P1 | `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:134` | WS 无消息大小限制——GB 级 frame OOM DoS | 已解决 | 文档里的旧路径已失效；真实实现已迁到 five-plane interface，并补了 message size 上限。 | `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts` 当前定义 `MAX_MESSAGE_SIZE_BYTES = 1_000_000`，在 `message` 入口按字节数拒绝超大帧。 | 回写现行实现路径与依据。 |
| 2356 | Round 40 — Domains · Testing · Prompt-Engine/Proactive · HTTP Routes / §201 HTTP Routes / Interface 深层缺陷 | P1 | `src/platform/interface/api/http-server/billing-routes.ts:1-57` | 无版本前缀 shadow route /billing/webhooks/reconcile——未文档化攻击面 | 已修复 | 根因是版本化 `/v1/billing/webhooks/reconcile` 落地后，旧的 unversioned 兼容 shim 一直残留在 route table 里，形成未文档化可达入口。 | 本轮已在 `src/platform/interface/api/http-server/billing-routes.ts` 与 `src/platform/five-plane-interface/api/http-server/billing-routes.ts` 删除 `/billing/webhooks/reconcile`，仅保留 `/v1/billing/webhooks/reconcile`；`tests/unit/platform/interface/api/http-server/billing-routes.test.ts` 现断言 route 数量为 `1`。 | 已改生产代码与定向测试。 |
| 2357 | Round 40 — Domains · Testing · Prompt-Engine/Proactive · HTTP Routes / §201 HTTP Routes / Interface 深层缺陷 | P1 | `src/platform/interface/api/http-server/gateway-routes.ts:129-142` | webhookSecret=null 时签名验证静默跳过——无告警 | 已修复 | 根因是旧代码把 `webhookSecret == null` 当成“允许无签名继续处理”，只在有 signature 时才报错，导致未配置 secret 的系统默认接受未验签 webhook。 | 本轮在 `src/platform/interface/api/http-server/gateway-routes.ts` 改为 `webhookSecret == null` 直接记录错误并返回 `503 gateway.signature_config_missing`；`tests/unit/platform/interface/api/http-server/gateway-routes.test.ts` 已覆盖未配置 secret 时的拒绝行为。 | 已改生产代码与定向测试。 |
| 2358 | Round 40 — Domains · Testing · Prompt-Engine/Proactive · HTTP Routes / §201 HTTP Routes / Interface 深层缺陷 | P1 | `src/platform/interface/api/http-server/incident-routes.ts:136-143` | PATCH 仅含 owner 时 throw "transition to undefined"——逻辑 bug | 已解决 | 根因是旧 PATCH 分支默认按状态迁移处理 owner-only payload；现行逻辑已拆出显式 owner-only 错误。 | `src/platform/interface/api/http-server/incident-routes.ts` 现在对 `status===undefined && owner!==undefined` 返回 `incident.owner_only_not_supported`；`tests/unit/platform/interface/api/http-server/incident-routes.test.ts` 已覆盖。 | 回写依据并补定向测试。 |
| 2359 | Round 40 — Domains · Testing · Prompt-Engine/Proactive · HTTP Routes / §201 HTTP Routes / Interface 深层缺陷 | P2 | `src/platform/interface/api/http-server/gateway-routes.ts:101` | POST send 返回 200 非 201——语义错误 | 已解决 | 原审计停留在旧响应码；现行 send 已使用创建语义的 `201`。 | `src/platform/interface/api/http-server/gateway-routes.ts` 当前 `POST /v1/gateway/messages/send` 返回 `201`；`tests/unit/platform/interface/api/http-server/gateway-routes.test.ts` 已回归。 | 回写依据并补定向测试。 |
| 2360 | Round 40 — Domains · Testing · Prompt-Engine/Proactive · HTTP Routes / §201 HTTP Routes / Interface 深层缺陷 | P2 | `src/platform/interface/api/http-server/gateway-routes.ts:93-108` | POST send 无 idempotency-key | 已修复 | 根因是旧接口虽然支持去重存储，但把 `x-idempotency-key` 作为可选增强而不是硬门禁，无法防止客户端重试重复发送。 | 本轮在 `src/platform/interface/api/http-server/gateway-routes.ts` 将 `x-idempotency-key` 改为必填，缺失时返回 `400 api.idempotency_key_required`；`tests/unit/platform/interface/api/http-server/gateway-routes.test.ts` 已覆盖缺失 header 的拒绝路径。 | 已改生产代码与定向测试。 |
| 2361 | Round 40 — Domains · Testing · Prompt-Engine/Proactive · HTTP Routes / §201 HTTP Routes / Interface 深层缺陷 | P2 | `src/platform/five-plane-interface/channel-gateway/stream-bridge.ts:182-184` | sequence/replay/dropped maps 无界增长——慢泄漏 | 已解决 | 原文档路径已过时；真实实现已迁移并在 client unregister / closeStream / dispose 时做全量清理。 | `src/platform/five-plane-interface/channel-gateway/stream-bridge.ts` 当前在 `unregisterClient()`、`closeStream()` 和 `dispose()` 中清空 `nextSequenceByStream/replayBuffer/droppedBeforeSequenceByStream` 等映射。 | 回写现行实现路径与依据。 |
| 2362 | Round 40 — Domains · Testing · Prompt-Engine/Proactive · HTTP Routes / §201 HTTP Routes / Interface 深层缺陷 | P2 | `src/platform/interface/api/http-server/incident-routes.ts:52-123` | POST /v1/incidents 无 idempotency-key | 已修复 | 根因是 incident 创建链把 `x-idempotency-key` 当成“有则去重、无则直接创建”的可选增强，重试语义并没有被提升为入口硬门禁。 | 本轮在 `src/platform/interface/api/http-server/incident-routes.ts` 与 `src/platform/five-plane-interface/api/http-server/incident-routes.ts` 增加 `readRequiredIdempotencyKey()`，缺失 header 时直接返回 `400 api.idempotency_key_required`；`tests/unit/platform/interface/api/http-server/incident-routes.test.ts` 已覆盖成功创建和缺失 header 的拒绝路径。 | 已改生产代码与定向测试。 |
| 2363 | Round 40 — Domains · Testing · Prompt-Engine/Proactive · HTTP Routes / §201 HTTP Routes / Interface 深层缺陷 | P2 | `src/platform/interface/channel-gateway/channel-gateway-delivery-service.ts:290-294` | generateNonce slice(0,32) 熵减半(128-bit 非 256-bit) | 已解决 | 原问题来自旧实现先把 `randomBytes(32).toString("hex")` 生成的 64 hex chars 再 `slice(0, 32)`，把 256-bit 熵截成了 128-bit。 | 当前 `generateNonce()` 已直接返回 `randomBytes(length).toString("hex")`，不再截断；`tests/unit/platform/interface/channel-gateway/channel-gateway-delivery-service.test.ts` 本轮新增并通过 “full 256-bit entropy by default” 断言。 | 回写现行源码与定向测试依据。 |
| 2364 | Round 41 — 深度补充审计（ops-maturity / scale-ecosystem / SDK / plugins / config / UI-shared / bootstrap） / §203 Ops-Maturity 深层缺陷（workflow-debugger / drift-detection / explainability / SLA） | P0 | `src/ops-maturity/drift-detection/benchmark-runner.ts:82-200` | Math.random() 模拟基准结果——晋升决策不确定，违反规范确定性证据驱动评估 | 已解决 | 原审计命中的是早期 benchmark stub；现行 runner 已收敛为“锁定 baseline + 外部 ProposalExecutor”，不再内建随机模拟结果。 | 当前 `SimpleBenchmarkRunner` 通过 `ProposalExecutor.execute(...)` 执行基准并要求 `setBaseline()` 提供历史基线，源码中已无 `Math.random()`；`tests/unit/ops-maturity/drift-detection/benchmark-runner-deterministic.test.ts` 本轮通过。 | 回写现行源码与定向测试依据。 |
| 2365 | Round 41 — 深度补充审计（ops-maturity / scale-ecosystem / SDK / plugins / config / UI-shared / bootstrap） / §203 Ops-Maturity 深层缺陷（workflow-debugger / drift-detection / explainability / SLA） | P0 | `src/ops-maturity/workflow-debugger/time-travel-debug-service.ts:164-181,236-293` | 重放执行无 ReplaySandboxPolicy 守卫——违反 INV-REPLAY-001 重放不得产生真实副作用 | 已解决（源码复核并补定向测试） | 原问题属于旧版 replay API 在入口处没有统一强制 sandbox policy；现行实现已把 guard 下沉到所有 replay 入口。 | `TimeTravelDebugService` 当前通过 `assertReplayAllowed()` 守卫 `replayStep()`、`replayToCursor()`、`jumpToStep()`，当 `allowSideEffects=true` 时直接抛错；`tests/unit/ops-maturity/workflow-debugger/time-travel-debug-service-edge-cases.test.ts` 本轮新增并通过该拒绝用例。 | 回写现行源码与定向测试依据。 |
| 2366 | Round 41 — 深度补充审计（ops-maturity / scale-ecosystem / SDK / plugins / config / UI-shared / bootstrap） / §203 Ops-Maturity 深层缺陷（workflow-debugger / drift-detection / explainability / SLA） | P0 | `src/scale-ecosystem/integration/connector-framework-service.ts:141-202` | execute() 未经 SideEffectManager 记录——违反 INV-SIDEEFFECT-001 | 已解决（针对原条目） | 根因是旧 `ConnectorFrameworkService` 虽然持有 `SideEffectManager`，但 `recordExecution()` 只构造上下文后直接丢弃，没有生成 `SideEffectRecord`、也没有通过状态机迁移 side-effect 生命周期。 | 当前 `src/scale-ecosystem/integration/connector-framework-service.ts` 的 `recordExecution()` 已创建 `SideEffectRecord`、生成 `ReconciliationRecord`，并调用 `SideEffectManager.applyReconciliation(...)` 完成生命周期迁移；执行记录里也已回填 `sideEffectId/sideEffectStatus`，并可通过 `listSideEffectRecords()` 取证。定向回归 `tests/unit/scale-ecosystem/integration/connector-framework-service.test.ts` 的 `ConnectorFrameworkService records reconciled side effects for connector executions` 与 `... execute invokes registered executor and records executor-backed result` 已通过。 | 本轮关闭的是“完全未经过 SideEffectManager 记录”的原问题；剩余差距若有，已不再是这个条目指向的空钩子问题。 |
| 2367 | Round 41 — 深度补充审计（ops-maturity / scale-ecosystem / SDK / plugins / config / UI-shared / bootstrap） / §203 Ops-Maturity 深层缺陷（workflow-debugger / drift-detection / explainability / SLA） | P0 | `src/platform/shared/observability/slo-alerting-service.ts:226-383` | 通知 fetch 发送前即返回 delivered:true——投递失败静默吞没 | 已解决（源码复核） | 原问题来自 fire-and-forget 通道把“请求已发出”错误地当成“已成功投递”；现行实现已把未等待确认的通道统一标成 `delivered:false`。 | 当前 `WebhookAlertChannel`、`SlackAlertChannel`、`PagerDutyAlertChannel` 都在异步 `fetch` 触发后返回 `delivered: false`，并通过 `recordAlertDeliveryFailure(...)` 记录失败。 | 回写现行源码依据；相关旧 SLO 测试仍有更大范围的契约漂移，不把其历史断言继续当作本条未修证据。 |
| 2368 | Round 41 — 深度补充审计（ops-maturity / scale-ecosystem / SDK / plugins / config / UI-shared / bootstrap） / §203 Ops-Maturity 深层缺陷（workflow-debugger / drift-detection / explainability / SLA） | P1 | `src/ops-maturity/workflow-debugger/execution-tracer.ts:17-46` | workflowId/stepId 使用废弃术语——应为 PlanGraph/NodeRun/NodeAttempt | 已解决 | 根因不是完全缺少 canonical 对象，而是早期 tracer 为兼容旧调用方保留了 legacy alias；现行主字段已经切到 canonical 命名。 | 当前 `ExecutionTrace` / `TraceEvent` / `TraceFilter` 以 `planGraphId`、`harnessRunId`、`nodeRunId` 为主字段，`workflowId/stepId/executionId` 仅保留 `@deprecated` alias。 | 回写现行源码依据。 |
| 2369 | Round 41 — 深度补充审计（ops-maturity / scale-ecosystem / SDK / plugins / config / UI-shared / bootstrap） / §203 Ops-Maturity 深层缺陷（workflow-debugger / drift-detection / explainability / SLA） | P1 | `src/ops-maturity/workflow-debugger/time-travel-debug-service.ts:16-77` | DebugSnapshot 使用 executionId/stepId——与§5.5 canonical 对象模型不一致 | 已解决 | 根因同样是兼容层遗留：旧 alias 仍在类型里保留，但 canonical 字段已经成为主路径。 | 当前 `DebugSnapshot`、`ReplayCursor`、`TimeTravelDebugEvent`、`TimeTravelDebugSession` 以 `harnessRunId/nodeRunId` 为主字段，`executionId/stepId` 仅作为 `@deprecated` 兼容别名。 | 回写现行源码依据。 |
| 2370 | Round 41 — 深度补充审计（ops-maturity / scale-ecosystem / SDK / plugins / config / UI-shared / bootstrap） / §203 Ops-Maturity 深层缺陷（workflow-debugger / drift-detection / explainability / SLA） | P1 | `src/ops-maturity/drift-detection/evidence-store.ts:41-60` | InMemoryEvidenceStore.records 无上界——持续 append OOM | 已解决 | 原问题来自早期内存 evidence store 无限累积；现行实现已显式引入容量上限与淘汰。 | 当前 `InMemoryEvidenceStore` 带 `maxRecords`（默认 `10_000`）和 `evictIfNeeded()`，超过阈值时会切片保留最近记录。 | 回写现行源码依据。 |
| 2371 | Round 41 — 深度补充审计（ops-maturity / scale-ecosystem / SDK / plugins / config / UI-shared / bootstrap） / §203 Ops-Maturity 深层缺陷（workflow-debugger / drift-detection / explainability / SLA） | P1 | `src/scale-ecosystem/sla-engine/resource-allocator/index.ts:7-23` | allocateReservedCapacity 未校验 reservedPercent ≤100%——可超额分配 | 已解决 | 根因是旧资源分配器只做乘法分摊，没有先验证单项和总和的保留比例是否合法。 | 当前实现会对 `reservedPercent > 100`、`reservedPercent < 0` 和 `totalReservedPercent > 100` 直接抛错，再进入分配计算。 | 回写现行源码依据。 |
| 2372 | Round 41 — 深度补充审计（ops-maturity / scale-ecosystem / SDK / plugins / config / UI-shared / bootstrap） / §203 Ops-Maturity 深层缺陷（workflow-debugger / drift-detection / explainability / SLA） | P1 | `src/platform/shared/lifecycle/service-registry.ts:146-148` | if(!has){delete}——死代码无效果 | 已解决 | 原审计停留在旧的 teardown 代码；现行 `clearAll()` 已直接执行 registry 清理，不再保留无效的 `has`+`delete` 死分支。 | 当前 `ServiceRegistry.clearAll()` 在完成 teardown 后直接 `ServiceRegistry.liveRegistries.delete(this)`，源码中已无该死代码模式。 | 回写现行源码依据。 |
| 2373 | Round 41 — 深度补充审计（ops-maturity / scale-ecosystem / SDK / plugins / config / UI-shared / bootstrap） / §203 Ops-Maturity 深层缺陷（workflow-debugger / drift-detection / explainability / SLA） | P1 | `src/ops-maturity/explainability/explanation-pipeline-service.ts:97-189` | L3 depth ttlHours:0 但无 TTL 驱逐——条目永驻内存 | 已解决 | 原问题来自把 `ttlHours:0` 理解成“永久缓存”；现行实现对 L3 采取的是“不写缓存”，而非“写入无 TTL 条目”。 | 当前 `generate()` 在 `depth === "L3"` 时把 `cacheTtlHours` 设为 `0`，并依赖 `putExplanationCacheEntry(...)` 跳过缓存写入；L1/L2 则通过 `evictStaleCache()` 按 `maxCacheEntries` 驱逐。 | 回写现行源码依据。 |
| 2374 | Round 41 — 深度补充审计（ops-maturity / scale-ecosystem / SDK / plugins / config / UI-shared / bootstrap） / §203 Ops-Maturity 深层缺陷（workflow-debugger / drift-detection / explainability / SLA） | P1 | `src/platform/shared/observability/slo-alerting-service.ts:632-648` | acknowledgeAlert 直接 mutate readonly 语义对象 | 已解决（源码复核） | 根因是旧 shadow alert 更新路径直接改写内存中的只读语义对象；现行 ack 路径已改成创建新对象再回写 shadow store。 | 当前 `acknowledgeAlert()` 使用 `Object.assign({}, shadow, { ... })` 生成新对象并 `set(...)`，不再原地 mutate。 | 回写现行源码依据。 |
| 2375 | Round 41 — 深度补充审计（ops-maturity / scale-ecosystem / SDK / plugins / config / UI-shared / bootstrap） / §203 Ops-Maturity 深层缺陷（workflow-debugger / drift-detection / explainability / SLA） | P1 | `src/ops-maturity/drift-detection/rollout-manager.ts:61-89` | rollouts Map 无容量限制——已完成 rollout 无清理 | 已解决 | 原问题来自 rollout registry 只增不减；现行实现已分别限制 completed/active rollout 数量并驱逐旧项。 | 当前 `RolloutManager` 在 `evictCompletedIfNeeded()` / `evictInactiveIfNeeded()` 中按 `maxCompletedRollouts` 与活跃状态清理 `rollouts` Map。 | 回写现行源码依据。 |
| 2376 | Round 41 — 深度补充审计（ops-maturity / scale-ecosystem / SDK / plugins / config / UI-shared / bootstrap） / §203 Ops-Maturity 深层缺陷（workflow-debugger / drift-detection / explainability / SLA） | P1 | `src/ops-maturity/drift-detection/reflection-engine.ts:68-104` | 自增 counter 生成 ID——重启后冲突 | 已解决 | 原审计对应的是 counter 时代的实现；现行 reflection record 已统一切到平台 ID 与时钟工具。 | 当前 `reflection-engine.ts` 使用 `newId("refl")` 生成 ID，并用 `nowIso()` 写 `createdAt`。 | 回写现行源码依据。 |
| 2377 | Round 41 — 深度补充审计（ops-maturity / scale-ecosystem / SDK / plugins / config / UI-shared / bootstrap） / §203 Ops-Maturity 深层缺陷（workflow-debugger / drift-detection / explainability / SLA） | P1 | `src/ops-maturity/drift-detection/proposal-engine.ts:96-226` | 同上 prop\_${++counter}——重启后 ID 冲突 | 已解决 | 根因同上，proposal engine 已从进程内计数器迁到平台统一 ID 生成器。 | 当前 `proposal-engine.ts` 多处创建 proposal 都使用 `newId("prop")`，并统一通过 `nowIso()` 写时间戳。 | 回写现行源码依据。 |
| 2378 | Round 41 — 深度补充审计（ops-maturity / scale-ecosystem / SDK / plugins / config / UI-shared / bootstrap） / §203 Ops-Maturity 深层缺陷（workflow-debugger / drift-detection / explainability / SLA） | P1 | `src/ops-maturity/workflow-debugger/execution-tracer.ts:112-125` | durationMs = now - traceStart——测量的是总经过时间非单事件时长 | 已解决 | 根因是 tracer 早期虽然预留了 `eventStartTimes`，但没有真正按 `enter -> exit/error` 配对接线，`durationMs` 一直错误地拿 trace 起点时间做差。 | `src/ops-maturity/workflow-debugger/execution-tracer.ts` 现已按 `${traceId}:${nodeRunId}` 记录 `enter` 起点，只在 `exit/error` 上计算并清理单事件时长；`tests/unit/ops-maturity/workflow-debugger/execution-tracer.test.ts` 新增了 enter/exit 精确时长、error 清理旧计时和非终结事件保持 `null` 的回归，并已定向通过。 | 本轮做了真实事件级计时修复，不再把 trace 累计耗时冒充 event duration。 |
| 2379 | Round 41 — 深度补充审计（ops-maturity / scale-ecosystem / SDK / plugins / config / UI-shared / bootstrap） / §203 Ops-Maturity 深层缺陷（workflow-debugger / drift-detection / explainability / SLA） | P1 | `src/platform/shared/observability/slo-alerting-service.ts:742-762` | executeRunbook() 恒标 completed——无实际执行逻辑 | 已解决 | 根因有两层：一是旧实现只持久化 runbook execution 行，但从未读取 `runbook.steps`、解析步骤或调用任何执行器；二是两组单测仍把未执行路径断言成 `completed`，掩盖了能力缺口。 | 当前 `src/platform/shared/observability/slo-alerting-service.ts` 已在 `executeRunbook()` 中读取 runbook、插入 `running` 记录、解析 `steps` JSON、逐步调用 `runbookStepExecutor`，并把最终状态回写为 `completed` 或 `failed`；未配置执行器但步骤非空时会 fail closed 为 `runbook_step_executor_not_configured`。定向回归 `tests/unit/platform/shared/observability/slo-alerting-service.test.ts` 的 `runbook definition, execution, and listing` / `runbook execution fails closed when steps exist but no executor is configured` 以及 `tests/unit/platform/observability/slo-alerting-service.test.ts` 的 `SloAlertingService executes runbook` 已通过。 | 这次关闭的是“runbook 从未执行步骤”的真实实现缺口，不再靠错误断言把未实现路径伪装成 completed。 |
| 2380 | Round 41 — 深度补充审计（ops-maturity / scale-ecosystem / SDK / plugins / config / UI-shared / bootstrap） / §203 Ops-Maturity 深层缺陷（workflow-debugger / drift-detection / explainability / SLA） | P1 | `src/ops-maturity/drift-detection/reflection-engine.ts:80-104` | new Date() 替代平台 nowIso()——测试不确定 | 已解决 | 原审计基于旧时间戳写法；现行 reflection engine 已统一使用平台时间工具。 | 当前文件只通过 `nowIso()` 生成 `createdAt`，源码中已无该条目指向的 `new Date()` 路径。 | 回写现行源码依据。 |
| 2381 | Round 41 — 深度补充审计（ops-maturity / scale-ecosystem / SDK / plugins / config / UI-shared / bootstrap） / §203 Ops-Maturity 深层缺陷（workflow-debugger / drift-detection / explainability / SLA） | P1 | `src/ops-maturity/drift-detection/proposal-engine.ts:96-226` | 同上 new Date() 替代 nowIso() | 已解决 | 根因同样是旧版时间戳写法残留；现行 proposal engine 已统一切到平台时间 API。 | 当前 `proposal-engine.ts` 使用 `const now = nowIso()` 与 `proposal.updatedAt = nowIso()`，不再直接调用 `new Date()` 生成业务时间戳。 | 回写现行源码依据。 |
| 2382 | Round 41 — 深度补充审计（ops-maturity / scale-ecosystem / SDK / plugins / config / UI-shared / bootstrap） / §203 Ops-Maturity 深层缺陷（workflow-debugger / drift-detection / explainability / SLA） | P1 | `src/scale-ecosystem/integration/connector-framework-service.ts:82-165` | execute() 返回模拟结果非实际调用——connector 功能未实现 | 已解决 | 根因分两步演进：旧 framework 最初既没有 executor seam，也没有区分“内建 provider 已接线”和“custom connector 未实现”；即使补上内建 executor 自动接线后，custom connector 在未注册 executor 时仍默认返回 synthesized success，把“根本没执行”伪装成“调用成功”。 | 本轮对 `src/scale-ecosystem/integration/connector-framework-service.ts` 做了真实生产修复：当 connector 没有可用 executor 时，`execute()` 不再返回 synthesized success/deferred，而是记录一次 `mode: "synthesized"` 的失败审计并 fail closed 返回 `{ success: false, status: "failed" }`。现有内建 provider 自动接线仍保留，显式 `registerExecutor()` 也仍是 custom connector 的正式扩展入口。`tests/unit/scale-ecosystem/integration/connector-framework-service.test.ts` 已定向通过 `ConnectorFrameworkService execute fails closed for connectors without executor and records synthesized failure`，同时 `... execute returns success for healthy connector` 现在明确通过注册真实 executor 才能成功，`... auto-wires builtin providers to concrete connector executors` 继续证明内建 connector 走真实执行路径。 | 这次关闭的是“未实现 connector 仍会被框架伪装成成功执行”的剩余缺口；custom connector 现在如果没有 executor，会明确失败而不是假成功。 |
| 2383 | Round 41 — 深度补充审计（ops-maturity / scale-ecosystem / SDK / plugins / config / UI-shared / bootstrap） / §203 Ops-Maturity 深层缺陷（workflow-debugger / drift-detection / explainability / SLA） | P1 | `src/ops-maturity/workflow-debugger/time-travel-debug-service.ts:220-225` | breakpoints 赋值可变数组至 readonly 字段——绕过只读约束 | 已解决（源码复核并补定向测试） | 根因是旧 `setBreakpoints()` 直接把调用方数组挂到 session 上，外部后续 mutate 会绕过 readonly 语义污染内部状态。 | 当前 `setBreakpoints()` 已执行 `session.breakpoints = [...nodeRunIds]` 做防御性复制；`tests/unit/ops-maturity/workflow-debugger/time-travel-debug-service-edge-cases.test.ts` 本轮新增并通过“caller array mutated later does not alter stored breakpoints” 用例。 | 回写现行源码与定向测试依据。 |
| 2384 | Round 41 — 深度补充审计（ops-maturity / scale-ecosystem / SDK / plugins / config / UI-shared / bootstrap） / §204 SDK / Plugins / Config / Divisions 缺陷 | P0 | `config/security/default.json:7` | allowedCapabilities 含 "mcp" 但 PluginSandboxPolicy 无此能力类型——违反默认安全收敛 | 已解决 | 根因不是当前默认安全配置仍在放行 `mcp`，而是审计表沿用了早期更宽松的 worker/plugin capability allowlist 快照。 | 当前 `config/security/default.json` 的 `remoteWorkerRegistration.allowedCapabilities` 已收敛为 `["edit"]`，`pluginCapabilities.allowedCapabilities` 为 `["edit","read","invoke_tool"]`，都不再包含 `mcp`。 | 已复跑并通过 `tests/unit/platform/control-plane/config-center/security-defaults.test.ts`，其中 `security defaults: capability allowlists exclude unsupported mcp capability` 直接断言两处 allowlist 都不含 `mcp`。 |
| 2385 | Round 41 — 深度补充审计（ops-maturity / scale-ecosystem / SDK / plugins / config / UI-shared / bootstrap） / §204 SDK / Plugins / Config / Divisions 缺陷 | P0 | `src/plugins/adapters/crm-adapter.ts:52` | token 明文前 8 字符存入 fingerprint——泄露敏感凭据片段 | 已解决 | 根因是旧 CRM adapter 曾把凭据前缀直接拼进 fingerprint；现行实现已改为对 token 做摘要后仅保留 hash 前缀，但缺少一条直接证明“不会再泄露原始前缀”的最小安全回归。 | 当前 `src/plugins/adapters/crm-adapter.ts` 在 `authenticate()` 中使用 SHA-256 摘要并生成 `crm_${crmType}_${tokenHash}`，不再保留原始 token 前缀。 | 本轮新增并通过 `tests/unit/plugins/adapters/crm-adapter-security-regression.test.ts` 的 `CrmAdapter.authenticate uses hashed fingerprint instead of leaking token prefix`，显式断言 Authorization header 不包含原始 token 前 8 位。 |
| 2386 | Round 41 — 深度补充审计（ops-maturity / scale-ecosystem / SDK / plugins / config / UI-shared / bootstrap） / §204 SDK / Plugins / Config / Divisions 缺陷 | P1 | `src/sdk/admin-sdk/index.ts:6` | 缺 resumeHarnessRun/listWorkers/getConfig 等 Admin API——运维脚本化能力不足 | 已解决 | 原审计命中的是较早的 Admin SDK 能力面；现行 `AdminSdk` 已补齐恢复 run、列 worker、读配置等运维脚本化入口，但文档和回归没同步。 | 当前 `src/sdk/admin-sdk/index.ts` 已实现 `resumeHarnessRun()`、`listWorkers()` 与 `getConfig()`。 | 已复跑并通过 `tests/unit/sdk/admin-sdk.test.ts` 的 `AdminSdk.resumeHarnessRun creates resume operational directive` 与 `AdminSdk.listWorkers and getConfig call operational endpoints`。 |
| 2387 | Round 41 — 深度补充审计（ops-maturity / scale-ecosystem / SDK / plugins / config / UI-shared / bootstrap） / §204 SDK / Plugins / Config / Divisions 缺陷 | P1 | `src/sdk/admin-sdk/index.ts:1` | 未发送 X-Platform-Version/X-SDK-Version 请求头——违反 SDK 兼容性契约 | 已解决 | 根因是旧 SDK 构造链没有把版本头作为默认请求元数据强制注入；现行实现已在 API client 层统一补齐。 | 当前 `src/sdk/client-sdk/api-client.ts` 会为请求默认注入 `X-Platform-Version` 与 `X-SDK-Version`，`src/sdk/admin-sdk/index.ts` 又在构造时默认传入 `platformVersion ?? "v4.3"` 与 `sdkVersion ?? "1.0.0"`。 | 已复跑并通过 `tests/unit/sdk/admin-sdk.test.ts` 的 `AdminSdk sends X-Platform-Version and X-SDK-Version headers`。 |
| 2388 | Round 41 — 深度补充审计（ops-maturity / scale-ecosystem / SDK / plugins / config / UI-shared / bootstrap） / §204 SDK / Plugins / Config / Divisions 缺陷 | P1 | `src/sdk/harness-sdk/index.ts:34` | appendStep 用 stage 字段映射 nodeRunId——应为 NodeRun/NodeAttempt | 已解决（源码修复并补定向测试） | 复核时确认这里除了旧审计指出的 `stage/nodeRunId` 语义混用外，还存在更深一层真实根因：`HarnessSdk.createRun()` 返回 canonical run 后，后续 `appendStep()` 直接把 canonical 结构强转回 runtime state，导致 NodeRun lineage 无法稳定落到权威运行态。 | 本轮已把 `src/sdk/harness-sdk/index.ts` 改为：`createRun()` 先持久化 runtime state，再返回 canonical view；`appendStep()` 则通过 `requireRuntimeRun()` 从 runtime store 取回权威运行态，保留 `stage: input.phase ?? "default"` 与独立的 `nodeRunId: input.nodeRunId` 后再追加步骤并持久化。 | 已复跑并通过 `tests/unit/sdk/harness-sdk/index.test.ts` 的 `HarnessSdk.appendStep appends step to run`，并显式验证 runtime store 中的 `nodeRunIds` 包含传入的 `node-1`。 |
| 2389 | Round 41 — 深度补充审计（ops-maturity / scale-ecosystem / SDK / plugins / config / UI-shared / bootstrap） / §204 SDK / Plugins / Config / Divisions 缺陷 | P1 | `src/sdk/harness-sdk/index.ts:27` | 缺 reserveBudget/settleBudget——违反 INV-BUDGET-001 先预留再执行 | 已解决（源码修复并补定向测试） | 根因不只是“方法名缺失”。现行复核发现 `reserveBudget()` 虽已存在，但 `settleBudget()` 仍沿用 canonical run 强转 runtime state 的旧模式，导致真实结算路径会在运行态字段缺失时失败。 | 本轮已把 `src/sdk/harness-sdk/index.ts` 的 `settleBudget()` 收敛到 `requireRuntimeRun()` + `runtime.persistRun(...)` 路径，确保预算结算操作基于权威 runtime state；`reserveBudget()` 也已作为显式 SDK API 保留。 | 已复跑并通过 `tests/unit/sdk/harness-sdk/index.test.ts` 的 `HarnessSdk.reserveBudget delegates to budget checker` 与 `HarnessSdk.settleBudget persists an existing run`。 |
| 2390 | Round 41 — 深度补充审计（ops-maturity / scale-ecosystem / SDK / plugins / config / UI-shared / bootstrap） / §204 SDK / Plugins / Config / Divisions 缺陷 | P1 | `config/runtime/default.json:4` | defaultStepTimeoutMs 使用废弃 step 术语——应为 defaultNodeRunTimeoutMs | 已解决 | 根因是运行时默认配置曾沿用 legacy `step` 命名；现行默认配置已经切到 canonical `node_run` 术语，只是审计表没跟进。 | 当前 `config/runtime/default.json` 使用的是 `defaultNodeRunTimeoutMs: 300000`，且已不再包含 `defaultStepTimeoutMs`。 | 已复跑并通过 `tests/unit/platform/control-plane/config-center/security-defaults.test.ts` 的 `runtime defaults: canonical node timeout key replaces legacy step timeout key`。 |
| 2391 | Round 41 — 深度补充审计（ops-maturity / scale-ecosystem / SDK / plugins / config / UI-shared / bootstrap） / §204 SDK / Plugins / Config / Divisions 缺陷 | P1 | `config/risk/default.json:2` | $schema 引用 .ts 文件——非合法 JSON Schema | 已解决 | 根因是早期 risk 默认配置的 schema 引用不是合法 JSON Schema URL；当前默认配置已经改正。 | 当前 `config/risk/default.json` 的 `$schema` 为 `http://json-schema.org/draft-07/schema#`。 | 已复跑并通过 `tests/unit/platform/control-plane/config-center/security-defaults.test.ts` 的 `risk defaults: schema points at a legal JSON schema document`。 |
| 2392 | Round 41 — 深度补充审计（ops-maturity / scale-ecosystem / SDK / plugins / config / UI-shared / bootstrap） / §204 SDK / Plugins / Config / Divisions 缺陷 | P1 | `src/plugins/validators/basic-evaluator.ts:14` | initialize/shutdown 空实现——跳过 PluginLifecycleHooks 完整生命周期 | 已解决 | 根因是旧 `basic-evaluator` 虽然声明了 `initialize()` / `shutdown()` / `healthCheck()`，但前两者是纯 no-op，`healthCheck()` 又恒定返回 `true`，导致插件在未初始化和已关闭后都无法反映真实生命周期状态。 | 当前 `src/plugins/validators/basic-evaluator.ts` 已引入 `lifecycleState.initialized`，`initialize()` 会置为 `true`，`shutdown()` 会置为 `false`，`healthCheck()` 也改为返回该状态，不再跳过生命周期迁移。 | 已复跑并通过 `tests/unit/plugins/validators/index.test.ts` 的 `ValidatorsIndex basic-evaluator healthCheck follows lifecycle state`、`tests/unit/plugins/plugin-validation.test.ts` 的 `BasicEvaluatorPlugin healthCheck follows initialize/shutdown lifecycle`，以及 `tests/unit/plugins/validators/basic-evaluator.test.ts` 的 `createBasicEvaluatorPlugin healthCheck follows lifecycle state`。 |
| 2393 | Round 41 — 深度补充审计（ops-maturity / scale-ecosystem / SDK / plugins / config / UI-shared / bootstrap） / §204 SDK / Plugins / Config / Divisions 缺陷 | P1 | `src/plugins/adapters/game-dev-adapter.ts:26` | authenticate() 空实现——接受任意凭据 | 已解决 | 根因是旧 `GameDevAdapter.authenticate()` 确实是 no-op，且 `execute()` 也没有前置认证守卫；现行实现已经把两条链一起补上。 | 当前 `src/plugins/adapters/game-dev-adapter.ts` 会在 `authenticate()` 中校验 `token/apiKey`，缺失时抛出 `gamedev_adapter.missing_credentials`；`execute()` 在未认证时也会拒绝。 | 已复跑并通过 `tests/unit/plugins/adapters/game-dev-adapter.test.ts` 的 `GameDevAdapter.authenticate rejects missing credentials`、`GameDevAdapter.execute requires authenticate before use` 与 `GameDevAdapter.execute works after authenticate is called`。 |
| 2394 | Round 41 — 深度补充审计（ops-maturity / scale-ecosystem / SDK / plugins / config / UI-shared / bootstrap） / §204 SDK / Plugins / Config / Divisions 缺陷 | P1 | `src/plugins/builtin-plugin-registry.ts:21` | 无插件生命周期状态跟踪(registered→loaded→active→inactive→unloaded) | 已解决 | 原审计停留在 registry 只暴露工厂和 manifest 的阶段；现行内建插件注册表已经维护独立生命周期状态表。 | 当前 `src/plugins/builtin-plugin-registry.ts` 已定义 `PLUGIN_LIFECYCLE_STATES`，并提供 `getPluginLifecycleState()` / `setPluginLifecycleState()`；状态集合为 `registered/validated/loading/active/inactive/unloaded`，覆盖审计要求的生命周期跟踪需求。 | 已复跑并通过 `tests/unit/plugins/plugin-lifecycle-state.test.ts`。 |
| 2395 | Round 41 — 深度补充审计（ops-maturity / scale-ecosystem / SDK / plugins / config / UI-shared / bootstrap） / §204 SDK / Plugins / Config / Divisions 缺陷 | P1 | `src/plugins/operations-config.ts:160` | operations 域缺 monitoring_review workflow 输出合约 | 已解决 | 根因不是当前 operations 域仍缺 `monitoring_review` 输出合约，而是审计条目和旧测试基线停留在只统计两条 output contract 的早期快照。 | 当前 `src/plugins/operations-config.ts` 的 `outputContracts` 已显式包含 `ops.monitoring_review`，schema 字段为 `alertId`、`reviewedAt`、`findings`、`severityOverride`。 | 已复跑并通过 `tests/unit/plugins/operations-config.test.ts` 的 `operationsDomainDefinition has three output contracts` 与 `monitoring_review output contract is present and structured`。 |
| 2396 | Round 41 — 深度补充审计（ops-maturity / scale-ecosystem / SDK / plugins / config / UI-shared / bootstrap） / §204 SDK / Plugins / Config / Divisions 缺陷 | P1 | `config/bootstrap/default.json:2` | phase "phase_1a" 使用废弃 Phase 术语——应为 Ring 1/2/3 | 已解决 | 根因是审计表沿用了旧 bootstrap 默认值；现行默认配置已经切到 Ring 术语，但此前缺少一条直接断言。 | 当前 `config/bootstrap/default.json` 顶层字段为 `phase: "ring_1"`，不再使用 `phase_1a`。 | 已复跑并通过 `tests/unit/platform/control-plane/config-center/default-config-shape.test.ts` 的 `bootstrap default config includes dependency order, readiness gates, and degradation policy`，其中已显式断言 `phase === "ring_1"`。 |
| 2397 | Round 41 — 深度补充审计（ops-maturity / scale-ecosystem / SDK / plugins / config / UI-shared / bootstrap） / §204 SDK / Plugins / Config / Divisions 缺陷 | P1 | `src/domains/recipes/index.ts:3` | DomainRecipeSchema 缺 riskLevel/budgetHint/requiredApproval | 已解决 | 根因是旧 DomainRecipe contract 只覆盖 workflow/tool 选择信息，没有把风险、预算提示和审批需求纳入 recipe schema；现行 schema 已补齐。 | 当前 `src/domains/recipes/index.ts` 的 `DomainRecipeSchema` 已显式包含 `riskLevel`、`budgetHint` 与 `requiredApproval`。 | 已复跑并通过 `tests/unit/domains/recipes/index.test.ts` 的 `DomainRecipeSchema applies default values`，直接断言三者的默认行为。 |
| 2398 | Round 41 — 深度补充审计（ops-maturity / scale-ecosystem / SDK / plugins / config / UI-shared / bootstrap） / §204 SDK / Plugins / Config / Divisions 缺陷 | P1 | `src/sdk/pack-sdk/pack-manifest.ts:26` | BusinessPackManifest 缺 rollbackStrategy——违反"先可恢复再自动化"宪法 | 已解决 | 根因是早期 SDK pack manifest 只描述 capability/compatibility，没有把恢复策略作为 manifest 一等字段；现行类型已补齐。 | 当前 `src/sdk/pack-sdk/pack-manifest.ts` 的 `BusinessPackManifest` 已包含 `rollbackStrategy`，`validateBusinessPackManifest()` 也会保留该字段。 | 本轮新增并通过 `tests/unit/sdk/pack-sdk/pack-manifest.test.ts` 的 `pack-sdk manifest keeps rollbackStrategy when provided`。 |
| 2399 | Round 41 — 深度补充审计（ops-maturity / scale-ecosystem / SDK / plugins / config / UI-shared / bootstrap） / §204 SDK / Plugins / Config / Divisions 缺陷 | P1 | `src/plugins/adapters/asset-production-adapter.ts:15` | adapterType "figma" 不在 ExternalAdapterPlugin 联合类型中——类型不匹配 | 已解决 | 原审计针对的是 `ExternalAdapterPlugin` 联合类型尚未收录 `figma` 的时期；当前类型定义和 adapter 实现已经对齐。 | 当前 `src/domains/registry/plugin-spi.ts` 的 `ExternalAdapterPlugin["adapterType"]` 已包含 `"figma"`，`src/plugins/adapters/asset-production-adapter.ts` 也显式声明 `adapterType: "figma"`。 | 已复跑并通过 `tests/unit/plugins/adapters/asset-production-adapter.test.ts`，其中 `AssetProductionAdapter has correct plugin metadata` 直接断言 `adapterType === "figma"`。 |
| 2400 | Round 41 — 深度补充审计（ops-maturity / scale-ecosystem / SDK / plugins / config / UI-shared / bootstrap） / §204 SDK / Plugins / Config / Divisions 缺陷 | P2 | `config/security/default.json:3` | sandboxMode 默认 "workspace_write"——应为 "read_only" | 已解决 | 根因不是当前默认配置仍在放宽到 `workspace_write`，而是审计表沿用了旧安全默认值。 | 当前 `config/security/default.json` 的 `sandboxMode` 已为 `read_only`。 | 已复跑并通过 `tests/unit/platform/control-plane/config-center/security-defaults.test.ts` 的 `security defaults: sandbox mode defaults to restricted`，其中已精确断言默认值为 `read_only`。 |
| 2401 | Round 41 — 深度补充审计（ops-maturity / scale-ecosystem / SDK / plugins / config / UI-shared / bootstrap） / §204 SDK / Plugins / Config / Divisions 缺陷 | P2 | `config/gateways/default.json:1` | 缺 timeout/circuitBreaker/fallbackMode 字段 | 已解决 | 根因是审计结论停留在较早的 gateway 默认配置版本；现行配置已包含超时和熔断降级字段，只是缺少回归断言。 | 当前 `config/gateways/default.json` 已定义顶层 `timeout: 30000`、`circuitBreaker`，且 `fallbackMode` 明确位于 `circuitBreaker.fallbackMode`。 | 已复跑并通过 `tests/unit/platform/control-plane/config-center/default-config-shape.test.ts` 的 `gateway default config includes rate limit, cors, auth, and request limits`，其中新增断言 `timeout`、`circuitBreaker` 与 `fallbackMode`。 |
| 2402 | Round 41 — 深度补充审计（ops-maturity / scale-ecosystem / SDK / plugins / config / UI-shared / bootstrap） / §204 SDK / Plugins / Config / Divisions 缺陷 | P2 | `divisions/operations/schemas/ops-output.json:1` | draft-07 vs 其他 division 的 draft/2020-12——schema 版本不统一 | 已解决 | 根因是审计表记录的是旧 schema 版本；现行 operations 输出 schema 已经统一到和其他 division 一致的 2020-12 基线，但之前缺少直接证明。 | 当前 `divisions/operations/schemas/ops-output.json` 的 `$schema` 为 `https://json-schema.org/draft/2020-12/schema`。 | 已复跑并通过 `tests/unit/platform/control-plane/config-center/default-config-shape.test.ts` 的 `operations output schema uses the shared 2020-12 JSON Schema baseline`。 |
| 2403 | Round 41 — 深度补充审计（ops-maturity / scale-ecosystem / SDK / plugins / config / UI-shared / bootstrap） / §204 SDK / Plugins / Config / Divisions 缺陷 | P2 | `config/domains/default.json:98` | coding 域 status "testing" 与 Pack 生命周期状态命名不一致 | 已解决 | 根因是审计表沿用了 coding 域早期 onboarding 阶段的默认状态；现行默认域配置已经提升为 `active`，只是缺少回归断言。 | 当前 `config/domains/default.json` 中 `coding` 域的 `status` 为 `active`。 | 已复跑并通过 `tests/unit/platform/control-plane/config-center/default-config-shape.test.ts` 的 `coding domain default status is active`。 |
| 2404 | Round 41 — 深度补充审计（ops-maturity / scale-ecosystem / SDK / plugins / config / UI-shared / bootstrap） / §204 SDK / Plugins / Config / Divisions 缺陷 | P2 | `divisions/analytics/division.yaml:1` | analytics/devops/operations trigger "monitoring" 重叠——无优先级仲裁 | 已解决 | 根因不是当前 divisions 仍把 `monitoring` 作为 analytics/devops/operations 的共享 trigger，而是审计条目沿用了旧 division trigger 快照，没有反映后续 trigger 收敛和 deployment 冲突仲裁规则。 | 当前 `divisions/analytics/division.yaml` 的 `triggers` 不含 `monitoring`；`divisions/operations/division.yaml` 使用的是 `ops_monitoring`；真正存在重叠的是 `deployment`，且 `divisions/devops/division.yaml` 与 `divisions/operations/division.yaml` 都已定义 `disambiguate` 与 `priority_boost` 仲裁。 | 本轮新增并通过 `tests/unit/domains/governance/division-trigger-disambiguation.test.ts`，其中 `analytics/devops/operations divisions do not share an unarbitrated monitoring trigger` 与 `devops and operations deployment trigger overlap is explicitly arbitrated` 直接锁定现状。 |
| 2405 | Round 41 — 深度补充审计（ops-maturity / scale-ecosystem / SDK / plugins / config / UI-shared / bootstrap） / §204 SDK / Plugins / Config / Divisions 缺陷 | P2 | `src/plugins/growth-config.ts:201` | externalAdapters 声明 jira 但无实现——声明与实现不一致 | 已解决 | 根因不是当前 `growth` 域仍在声明 `jira`，而是审计条目和旧测试基线都停留在移除 `jira` 之前的配置快照。 | 当前 `src/plugins/growth-config.ts` 的 `externalAdapters` 已收敛为 `["github"]`，不存在未实现的 `jira` 声明。 | 已复跑并通过 `tests/unit/plugins/growth-config.test.ts` 的 `growthDomainDefinition has external adapters`，其中已改为精确断言 `["github"]`。 |
| 2406 | Round 41 — 深度补充审计（ops-maturity / scale-ecosystem / SDK / plugins / config / UI-shared / bootstrap） / §204 SDK / Plugins / Config / Divisions 缺陷 | P2 | `src/domains/recipes/recipe-executor.ts:34` | 硬编码 regex stub 判断 workflow 不存在——应查询 WorkflowRegistry | 已解决 | 根因是旧 `RecipeExecutor` 在缺少 `workflowRegistry` 时退回到一组 `nonexistent*` regex stub，对 workflow 是否存在做伪校验，导致未注册 workflow 仍可绕过权威 registry 查询。 | 当前 `src/domains/recipes/recipe-executor.ts` 已统一改为 `this.workflowRegistry?.get(parsed.defaultWorkflowId) ?? null`；只要 registry 缺失或查不到 workflow，就直接返回 `Workflow ... is not available in the registry.`，不再保留 regex fallback。 | 已复跑并通过 `tests/unit/domains/recipes/recipe-executor.test.ts` 的 `RecipeExecutor.execute fails closed when workflow registry is unavailable`、`RecipeExecutor.execute fails when workflow is absent from registry`、`RecipeExecutor.execute relies on registry instead of nonexistent-prefix stub logic`，以及 `tests/unit/domains/recipes/recipe-executor-full-coverage.test.ts` 的 `RecipeExecutor.execute records metrics for registry lookup failures`。 |
| 2407 | Round 41 — 深度补充审计（ops-maturity / scale-ecosystem / SDK / plugins / config / UI-shared / bootstrap） / §205 UI Shared 层 / Features 深层缺陷 | P0 | `ui/packages/shared/platform/src/web-platform-adapter.ts:9` | 安全凭证存 localStorage——XSS 完全暴露，违反 UP-6 Token 安全存储 | 已解决 | 根因不是当前 web adapter 仍把安全凭证放进 `localStorage`，而是审计结论停留在早期浏览器壳层草稿；现行实现已把 secure storage 留在基类内存安全存储，`localStorage` 只用于 analytics consent。 | 当前 `ui/packages/shared/platform/src/web-platform-adapter.ts` 已明确注明 secure storage 不使用 `localStorage`，`readFile()` / `writeFile()` 也只委托到 `DefaultPlatformAdapter`；真正的安全凭证读写仍由 `ui/packages/shared/platform/src/base-platform-adapter.ts` 的 `MemorySecureStore` 处理。 | 本轮新增并通过 `tests/unit/ui/shared/platform/platform-adapter-security.test.ts` 的 `WebPlatformAdapter keeps secure values out of localStorage` 与 `WebPlatformAdapter uses localStorage only for analytics consent persistence`。 |
| 2408 | Round 41 — 深度补充审计（ops-maturity / scale-ecosystem / SDK / plugins / config / UI-shared / bootstrap） / §205 UI Shared 层 / Features 深层缺陷 | P0 | `ui/packages/shared/platform/src/mobile-platform-adapter.ts:10` | analyticsConsentDefault:true——违反 GDPR opt-in 原则 | 已解决 | 根因是审计条目沿用了旧 mobile adapter 默认值；现行构造函数已经把 consent 缺省改为 opt-in `false`。 | 当前 `ui/packages/shared/platform/src/mobile-platform-adapter.ts` 在构造中调用 `super(platform, { analyticsConsentDefault: false })`，且 bridge 路径只在用户显式 `setAnalyticsConsent()` 后才持久化 consent。 | 本轮新增并通过 `tests/unit/ui/shared/platform/platform-adapter-security.test.ts` 的 `MobilePlatformAdapter defaults analytics consent to opt-in false` 与 `MobilePlatformAdapter persists explicit analytics consent through the mobile bridge`。 |
| 2409 | Round 41 — 深度补充审计（ops-maturity / scale-ecosystem / SDK / plugins / config / UI-shared / bootstrap） / §205 UI Shared 层 / Features 深层缺陷 | P0 | `ui/packages/features/conversation/src/hooks/index.ts:46` | 跳过 intake pipeline 本地模拟 execute——违反 P1→P2 必经控制面不变量 | 已解决 | 根因是旧 conversation hook 在离线分支里会继续走本地 execute 模拟，绕开控制面 intake pipeline；现行离线分支已经改为 fail-safe 提示，不再本地执行。 | 当前 `ui/packages/features/conversation/src/hooks/index.ts` 的 `executePlan()` 在无 `wsClient/userId` 时会设置 `status` 为 `clarifying`，并通过 `requestClarification("执行需要与后端建立连接。当前离线状态下无法执行任务，请检查网络连接后重试。")` 阻止离线执行。 | 本轮新增并通过 `tests/unit/ui/features/conversation/conversation-vm-offline-execution.test.ts` 的 `offline executePlan no longer simulates local execution`，对离线分支源码回归做了定向锁定。 |
| 2410 | Round 41 — 深度补充审计（ops-maturity / scale-ecosystem / SDK / plugins / config / UI-shared / bootstrap） / §205 UI Shared 层 / Features 深层缺陷 | P0 | `src/platform/contracts/control-directive/index.ts:4` | ControlDirective 仍为 canonical 导出——§5.2 要求 v4.2 起为 deprecated alias | 已解决 | 根因是审计表停留在 legacy contract 仍可实例化的阶段；现行 `ControlDirective` 只作为带 `@deprecated` 标记的兼容 alias 保留，工厂已被禁用。 | 当前 `src/platform/contracts/control-directive/index.ts` 已把 `ControlDirective` 和 `ControlDirectiveKind` 标为 `@deprecated`，并让 `createControlDirective()` 统一抛出 `control_directive.legacy_contract_forbidden`，同时提供 canonical `OperationalDirective` / `DecisionDirective`。 | 已复跑并通过 `tests/unit/platform/contracts/control-directive-contract.test.ts`，其中 `ControlDirective type is deprecated via JSDoc @deprecated marker`、`createControlDirective rejects all directive kinds with legacy_contract_forbidden`、`OperationalDirective and DecisionDirective are canonical replacements` 直接证明该 contract 已退位为 deprecated alias。 |
| 2411 | Round 41 — 深度补充审计（ops-maturity / scale-ecosystem / SDK / plugins / config / UI-shared / bootstrap） / §205 UI Shared 层 / Features 深层缺陷 | P0 | `src/platform/contracts/execution-plan/index.ts:17` | ExecutionPlan 仍用线性 steps[]——§5.3 要求 PlanGraphBundle 为唯一执行契约 | 已解决 | 根因是审计表沿用了 legacy `ExecutionPlan` 仍可作为线性 `steps[]` 合同实例化的阶段；现行模块已把 canonical 执行契约收敛到 `PlanGraphBundle`。 | 当前 `src/platform/contracts/execution-plan/index.ts` 只 re-export `PlanGraphBundle` 相关类型/工厂，`ExecutionPlan` 本身已退化为 `type ExecutionPlan = PlanGraphBundle` 的兼容 alias，`createExecutionPlan()` 也统一抛出 `execution_plan.legacy_contract_forbidden`。 | 已复跑并通过 `tests/unit/platform/contracts/execution-plan-contract.test.ts`，其中 `ExecutionPlan type is deprecated via JSDoc @deprecated marker`、`createExecutionPlan rejects all inputs with legacy_contract_forbidden error`、`canonical PlanGraphBundle can be created as ExecutionPlan replacement` 直接覆盖该迁移。 |
| 2412 | Round 41 — 深度补充审计（ops-maturity / scale-ecosystem / SDK / plugins / config / UI-shared / bootstrap） / §205 UI Shared 层 / Features 深层缺陷 | P1 | `ui/packages/features/settings/src/hooks/index.ts:106` | save() 无实际 API 调用——违反 UP-1 API-First 原则 | 已解决 | 根因不是当前 `save()` 仍是 stub，而是审计结论停留在早期草稿阶段；现行 hook 已通过共享 API client 发起真实偏好更新，并带上 ETag 做乐观锁。 | 当前 `ui/packages/features/settings/src/hooks/index.ts` 的 `save()` 会先 `setSaveState("saving")`，随后提取 `const etag = (preferences as { etag?: string }).etag;` 并执行 `await updatePreferences(client, { theme: draftTheme, locale: draftLocale }, etag)`，成功后再切到 `saved`。 | 本轮新增并通过 `tests/unit/ui/features/settings/settings-vm-save-contract.test.ts` 的 `useSettingsVm.save calls updatePreferences with optimistic-lock etag` 与 `useSettingsVm.save exposes saving and saved state transitions`。 |
| 2413 | Round 41 — 深度补充审计（ops-maturity / scale-ecosystem / SDK / plugins / config / UI-shared / bootstrap） / §205 UI Shared 层 / Features 深层缺陷 | P1 | `ui/packages/shared/sync/src/conflict-resolver.ts:12` | merge 用 {...server,...local} 简单覆盖——local 无条件覆写 server 可致数据丢失 | 已解决 | 根因不是当前冲突合并仍在使用 `{...server, ...local}` 浅覆盖，而是审计条目未跟上 CRDT/vector clock 改造后的实现。 | 当前 `ui/packages/shared/sync/src/conflict-resolver.ts` 在 `resolve(..., "merge")` 下会进入 `mergeValues()`，对象字段按 vector clock / Lamport timestamp 做逐字段 LWW，数组按 `id` 去重合并，标量也按 Lamport timestamp 决策，不再做无条件 local overwrite。 | 本轮新增并通过 `tests/unit/ui/shared/sync/conflict-resolver.test.ts` 的 `ConflictResolver.merge uses vector clocks instead of unconditional local overwrite`、`ConflictResolver.merge deduplicates arrays by id without replacing server entries wholesale` 与 `ConflictResolver.server_wins compares Lamport timestamps for scalar values`。 |
| 2414 | Round 41 — 深度补充审计（ops-maturity / scale-ecosystem / SDK / plugins / config / UI-shared / bootstrap） / §205 UI Shared 层 / Features 深层缺陷 | P1 | `ui/packages/shared/sync/src/offline-queue.ts:19` | enqueue 后 persist 失败仅存内存——崩溃丢失离线变更 | 已解决 | 根因是旧 `OfflineQueue.persist()` 在存储写失败时只记录错误、不向上抛出，调用方会误以为离线变更已持久化；现行实现已改为 fail-fast。 | 当前 `ui/packages/shared/sync/src/offline-queue.ts` 的 `enqueue()` 在入队后执行 `await this.persist()`；`persist()` 内部对 `store.writeAll([...this.queue])` 的异常会 `console.error(...)` 后 `throw error`，不再静默吞错。 | 本轮新增并通过 `tests/unit/ui/shared/sync/offline-queue-durability.test.ts` 的 `OfflineQueue.enqueue propagates persist failures instead of silently keeping memory-only state`。 |
| 2415 | Round 41 — 深度补充审计（ops-maturity / scale-ecosystem / SDK / plugins / config / UI-shared / bootstrap） / §205 UI Shared 层 / Features 深层缺陷 | P1 | `ui/packages/shared/sync/src/types.ts:1` | OfflineMutation 缺 tenantId/traceId/principal——flush 后无法满足 ContractEnvelope | 已解决 | 根因是审计条目基于旧版 `OfflineMutation` 结构；现行合同类型已经补齐多租户与可观测性 envelope 字段。 | 当前 `ui/packages/shared/sync/src/types.ts` 的 `OfflineMutation` 已强制声明 `tenantId`、`traceId`，以及含 `principalId` / `tenantId` / `roles` 的 `principal` 对象，flush 不再缺少 `ContractEnvelope` 所需上下文。 | 本轮新增并通过 `tests/unit/ui/shared/sync/offline-queue-durability.test.ts` 的 `OfflineMutation contract includes tenant, trace, and principal envelope fields`。 |
| 2416 | Round 41 — 深度补充审计（ops-maturity / scale-ecosystem / SDK / plugins / config / UI-shared / bootstrap） / §205 UI Shared 层 / Features 深层缺陷 | P1 | `ui/packages/shared/telemetry/src/index.ts:74` | OTLP POST 无认证头——违反多租户隔离 | 已解决 | 根因是旧 OTLP exporter 允许匿名 POST；现行实现已经把 authorization 变成构造必需条件，没有租户认证就拒绝创建 exporter。 | 当前 `ui/packages/shared/telemetry/src/index.ts` 的 `OtlpHttpTelemetryExporter` 构造函数会从 `authorization` header 或 `VITE_OTLP_AUTH_TOKEN` 读取令牌，缺失时直接抛错；`export()` 发起 POST 时也固定附带 `"authorization": this.authToken`。 | 本轮新增并通过 `tests/unit/ui/shared/telemetry/otlp-auth.test.ts` 的 `OtlpHttpTelemetryExporter rejects missing authorization for multi-tenant isolation` 与 `OtlpHttpTelemetryExporter sends authorization header on export`。 |
| 2417 | Round 41 — 深度补充审计（ops-maturity / scale-ecosystem / SDK / plugins / config / UI-shared / bootstrap） / §205 UI Shared 层 / Features 深层缺陷 | P1 | `ui/packages/shared/platform/src/base-platform-adapter.ts:96` | runShell(command) 无白名单——命令注入风险 | 已解决 | 根因是旧 `runShell(command)` 接口接受任意命令字符串，缺少最小授权边界；现行基类已经把壳层命令执行收敛到显式白名单。 | 当前 `ui/packages/shared/platform/src/base-platform-adapter.ts` 在构造时接收 `allowedShellCommands`，`runShell()` 会在白名单非空且命令不命中时直接返回 `code: 1` 与拒绝原因；只有白名单中的命令才会执行。 | 本轮新增并通过 `tests/unit/ui/shared/platform/platform-adapter-security.test.ts` 的 `DefaultPlatformAdapter.runShell denies commands outside the whitelist`；同文件既有的 secure storage / consent 回归也继续通过。 |
| 2418 | Round 41 — 深度补充审计（ops-maturity / scale-ecosystem / SDK / plugins / config / UI-shared / bootstrap） / §205 UI Shared 层 / Features 深层缺陷 | P1 | `ui/packages/shared/nl-client/src/index.ts:16` | 纯内存客户端模拟无后端交互——UI 状态与 truth 不同步 | 已解决 | 根因是旧 `ConversationClient` 自称 client，但内部只有数组和状态机，没有任何 transport seam；同时产品层 `useConversationVm` 又在 hook 里单独维护 WS 订阅/发布，导致 shared 包本身无法复用真实后端链路。 | 当前 `ui/packages/shared/nl-client/src/index.ts` 已新增 `ConversationTransport` / `ConversationClientOptions`，`ConversationClient` 在提供 `transport + userId` 时会直接订阅 `nl.session.updated` / `nl.plan.created` 并发布 `nl.prompt.sent`、`nl.plan.requested`、`nl.plan.confirmed`、`nl.execution.started` 等实时事件；`ui/packages/features/conversation/src/hooks/index.ts` 也已改为把 `wsClient` 注入 shared client，而不是在 hook 内另起一套 WS 状态协调。定向回归 `ui/tests/shared/nl-client.test.ts` 已验证“无 transport 时保留内存 fallback”和“有 transport 时能按 WS 事件回灌状态”。 | 本轮关闭的是“shared nl-client 完全不具备后端交互能力”的原问题；离线 fallback 仍保留，但已不再是 shared 包唯一能力。 |
| 2419 | Round 41 — 深度补充审计（ops-maturity / scale-ecosystem / SDK / plugins / config / UI-shared / bootstrap） / §205 UI Shared 层 / Features 深层缺陷 | P1 | `docs_zh/adr/018-rollout-eleven-state-machine.md:1` | Superseded ADR 仍含可执行规格——与 ADR-075 六级状态机不兼容 | 已解决 | 根因是 ADR-018 虽然顶部标了 superseded，但正文仍保留十一态、五级发布和自动回滚阈值等可执行规则，导致评审和实现仍可能误引用旧规范。 | 当前 `docs_zh/adr/018-rollout-eleven-state-machine.md` 已改成纯历史记录，只保留 superseded 状态、ADR-075 权威指向和历史摘要，不再包含状态枚举、级别表、阈值或实施规则。 | 本轮新增并通过 `tests/unit/docs/adr/adr-018-superseded-history.test.ts` 的 `ADR-018 is retained only as a historical record and points readers to ADR-075` 与 `ADR-018 no longer contains executable rollout tables, thresholds, or state-machine rules`。 |
| 2420 | Round 41 — 深度补充审计（ops-maturity / scale-ecosystem / SDK / plugins / config / UI-shared / bootstrap） / §205 UI Shared 层 / Features 深层缺陷 | P1 | `src/platform/execution/state-transition/transition-service.ts:228` | 仍用 TaskStatus/WorkflowStatus TransitionCommand——应统一为 RuntimeTransitionCommand | 已解决（源码复核并修正文档性误导） | 根因不是运行时还把 canonical 五平面实体走旧命令，而是 legacy `TransitionService` 注释曾误写成“use transitionRuntime()”，让审计误以为这里仍是统一入口；实际该服务一直只服务 Task/Workflow 等遗留实体，并通过 `assertNotCanonicalEntity()` 阻止 `HarnessRun/NodeRun` 等 canonical 实体误入。 | 当前 `src/platform/execution/state-transition/transition-service.ts` 与 `src/platform/five-plane-execution/state-transition/transition-service.ts` 已明确标注“legacy domain model only”，deprecated 指向也改成真实存在的 `RuntimeStateMachine + RuntimeTransitionCommand` 路径；源码内对 canonical ID 的前缀/UUID 拦截仍在。 | 本轮关闭依据是源码约束与注释一致性复核：旧服务保留是兼容层设计，不再构成“统一状态命令被绕开”的实现缺陷。 |
| 2421 | Round 41 — 深度补充审计（ops-maturity / scale-ecosystem / SDK / plugins / config / UI-shared / bootstrap） / §205 UI Shared 层 / Features 深层缺陷 | P2 | `ui/packages/features/alerts/src/hooks/index.ts:6` | 告警无权限过滤——违反最小权限展示 | 已解决 | 根因是 Alerts VM 之前直接暴露 `useIncidentsQuery()` 的全部结果，且 feature 入口权限只设为 `authenticated`，没有把告警展示收敛到运维角色。 | 当前 `ui/packages/features/alerts/src/hooks/index.ts` 已通过 `useAuthState()` 读取权限，并仅在 `auth.permissions.includes("platform_sre")` 时暴露 incidents；`ui/packages/features/alerts/src/index.tsx` 与 `ui/apps/web/src/feature-registry.ts` 也已把 Alerts 权限收紧为 `platform_sre`。 | 本轮新增并通过 `tests/unit/ui/features/alerts/alerts-access-control.test.ts` 的 `alerts feature requires platform_sre permission instead of generic authenticated access`、`useAlertsVm filters incidents by auth permissions before exposing alert items` 与 `web feature registry keeps alerts behind platform_sre and mission-control navigation`。 |
| 2422 | Round 41 — 深度补充审计（ops-maturity / scale-ecosystem / SDK / plugins / config / UI-shared / bootstrap） / §205 UI Shared 层 / Features 深层缺陷 | P2 | `ui/packages/features/conversation/src/index.tsx:7` | NL 作为主入口归入 Extended 组——与§UX-1 主入口定位矛盾 | 已解决 | 根因是 Conversation feature package、web shell registry 和 mobile navigation 都把 NL 入口当成 `Extended` 工具页处理，导致分组和路由路径与“主入口”定位漂移。 | 当前 `ui/packages/features/conversation/src/index.tsx` 已改为 `group: "Mission Control"`、`path: "/mission-control/conversation"`；`ui/apps/web/src/feature-registry.ts` 和 `ui/apps/mobile/src/navigation.ts` 也同步迁移到了 mission-control 主入口路径。 | 本轮新增并通过 `tests/unit/ui/features/conversation/conversation-entrypoint-contract.test.ts` 的 `conversation feature is grouped under mission control as a primary entrypoint`、`mobile navigation routes conversation through the mission-control entrypoint` 与 `web feature registry exposes conversation under mission control instead of extended tools`。 |
| 2423 | Round 41 — 深度补充审计（ops-maturity / scale-ecosystem / SDK / plugins / config / UI-shared / bootstrap） / §205 UI Shared 层 / Features 深层缺陷 | P2 | `ui/packages/shared/sync/src/sync-coordinator.ts:35` | flush drain 后未等发送确认——变更可永久丢失 | 已解决 | 根因是旧 `flush()` 一开始就 `drain()` 清空本地持久队列，随后才逐条发 HTTP；如果进程在收到服务端确认前崩溃，尚未重入队的变更会永久丢失。 | 当前 `ui/packages/shared/sync/src/sync-coordinator.ts` 已改为先 `peek()` 现有快照，逐条重放并生成 `nextQueue`，最后通过 `ui/packages/shared/sync/src/offline-queue.ts` 新增的 `replaceAll()` 原子写回剩余/失败项；收到确认前原始持久队列不会被提前移除。 | 本轮新增并通过 `tests/unit/ui/shared/sync/sync-coordinator-durability.test.ts` 的 `SyncCoordinator.flush keeps queued mutations durable until HTTP replay is acknowledged` 与 `SyncCoordinator.flush persists retryable failures back into the queue snapshot`。 |
| 2424 | Round 41 — 深度补充审计（ops-maturity / scale-ecosystem / SDK / plugins / config / UI-shared / bootstrap） / §205 UI Shared 层 / Features 深层缺陷 | P2 | `ui/packages/shared/telemetry/src/index.ts:28` | record() fire-and-forget 无重试/DLQ | 已解决（针对原条目） | 根因是旧 `TelemetrySink` 只有“整批失败就整批回队列”的易失性重排队，没有重试预算、没有 dead-letter 出口，而且多 exporter 场景会把已经成功的 exporter 也一起重复投递。 | `ui/packages/shared/telemetry/src/index.ts` 现已引入 `retryCounts + pendingExporterIndexes`，按 exporter 维度做重试确认；超过 `maxRetryAttempts` 后会转入 `deadLetters`，并可通过 `listDeadLetters()` 取回失败事件。已用 `node --import tsx` 直连源码做定向 smoke，验证了“单 exporter 临时失败重试”“成功 exporter 不重复投递”“超过预算进入 DLQ”三条核心路径。 | 本轮关闭的是“完全无重试/DLQ”的原问题；剩余差距仅是 DLQ 仍为内存态、尚未持久化。 |
| 2425 | Round 41 — 深度补充审计（ops-maturity / scale-ecosystem / SDK / plugins / config / UI-shared / bootstrap） / §205 UI Shared 层 / Features 深层缺陷 | P2 | `ui/packages/shared/platform/src/desktop-platform-adapter.ts:9` | Web screenSecurityDefault:false vs 桌面 true——安全基线不统一 | 已修复（源码修复并补定向测试） | 根因确实在平台基线分裂：桌面适配器构造时显式传了 `screenSecurityDefault: true`，而 `WebPlatformAdapter` 直接继承了 `DefaultPlatformAdapter` 的默认 `false`，导致不同 shell 启动时默认安全姿态不一致。 | 当前 `ui/packages/shared/platform/src/web-platform-adapter.ts` 构造函数已改为默认 `screenSecurityDefault: true`，并在浏览器环境下同步写入 `document.documentElement.dataset.aaScreenSecurity`；桌面侧保持原有 secure-by-default，不再出现 Web/桌面基线分叉。 | 本轮新增并通过 `tests/unit/ui/shared/platform/platform-adapter-security.test.ts` 的 `WebPlatformAdapter enables screen security by default to match desktop shells`。 |
| 2426 | Round 41 — 深度补充审计（ops-maturity / scale-ecosystem / SDK / plugins / config / UI-shared / bootstrap） / §205 UI Shared 层 / Features 深层缺陷 | P2 | `ui/packages/features/settings/src/hooks/index.ts:20` | 7 个查询无并行加载——违反 FCP<1.5s 性能目标 | 已解决（源码复核） | 根因是原审计把“存在 7 个查询”误判成“串行 waterfall”；但现行 `useSettingsVm()` 在首轮 render 中无条件同时调用 `usePreferencesQuery/useRolesQuery/useFeatureFlagsQuery/useModelsQuery/useDomainConfigsQuery/useTenantsQuery/useWebhooksQuery`，中间没有 `await`、条件门控或前一查询结果驱动后一查询的串行依赖。 | 当前 `ui/packages/features/settings/src/hooks/index.ts` 中 7 个 query hook 都在函数顶部直接创建，`loading` 仅取决于 `preferences == null`，不会阻塞其余查询发起；这说明“无并行加载”这一条已与源码不符。 | 本轮关闭的是“查询串行化”缺陷，不代表已完成 FCP<1.5s 的整站性能验收；后者仍需真实 UI profiling。 |
| 2427 | Round 41 — 深度补充审计（ops-maturity / scale-ecosystem / SDK / plugins / config / UI-shared / bootstrap） / §206 Bootstrap / 初始化 / 测试基础设施 缺陷 | P0 | `src/index.ts:264` | main() 未调用 registerProcessErrorHandlers——进程异常无有序关闭兜底 | 已解决（源码修复并补定向测试） | 根因确实在根入口顺序：旧 `main()` 在进入 bootstrap / demo 分支前没有先绑定 `uncaughtException` / `unhandledRejection` 处理器，导致进程级异常无法统一进入 graceful shutdown。 | 当前 `src/index.ts` 已在 `main()` 开头调用 `registerProcessErrorHandlers(getGlobalGracefulShutdown())`；`tests/integration/platform/execution/startup-lifecycle-integration.test.ts` 已验证会接上线级 `uncaughtException` 与 `unhandledRejection` 处理器，并补充了“同一 shutdown 实例重复注册不重复挂监听器”的回归。 | 根入口异常兜底已形成真实实现与测试闭环。 |
| 2428 | Round 41 — 深度补充审计（ops-maturity / scale-ecosystem / SDK / plugins / config / UI-shared / bootstrap） / §206 Bootstrap / 初始化 / 测试基础设施 缺陷 | P0 | `src/index.ts:125` | runPlatformRootDemo 未注册 GracefulShutdown——崩溃时无优雅关停 | 已解决（源码修复并补定向测试） | 复核后确认原问题在“direct demo entry”路径：`runPlatformRootDemo()` 可被直接调用而绕过 `main()`，旧实现虽然会执行业务逻辑，但不会自行安装进程级异常处理器。 | 当前 `src/index.ts` 已在 `runPlatformRootDemo()` 顶部先执行 `const shutdown = getGlobalGracefulShutdown(); registerProcessErrorHandlers(shutdown);`，再注册 demo cleanup handler；`tests/integration/platform/execution/startup-lifecycle-integration.test.ts` 新增的 handler wiring 与 idempotent 回归证明该自注册路径既能接通 graceful shutdown，又不会与 `main()` 路径叠加出重复监听器。 | 这次是实修 demo 直调入口，不是仅在文档里改口。 |
| 2429 | Round 41 — 深度补充审计（ops-maturity / scale-ecosystem / SDK / plugins / config / UI-shared / bootstrap） / §206 Bootstrap / 初始化 / 测试基础设施 缺陷 | P1 | `src/index.ts:178` | buildPlatformRootSummary 绕过 ServiceRegistry 直接调用 build 函数 | 已解决 | 根因有两层：一是 `buildPlatformRootSummary()` 之前只有 `architecture` 段走 `getPlatformArchitectureServices()`，其余分段仍直接调用 `build*StartupPlan/build*RuntimeCatalog`；二是 `registerDomainsStartupPlan()` 还错误地把 ring bootstrap service id 重新注册成依赖自身的 step service，导致 scoped registry 路径会触发自循环，进一步把调用方推回了直接 `build*`。 | 当前 `src/index.ts` 已把 `domains/planes/aiOperations/interactionGovernance/scaleOps` 全部切到 `register*StartupPlan/register*RuntimeCatalog` + `ServiceRegistry` 取值；同时 `src/domains-startup-plan.ts` 已删除那段把 `w5.domains.ring.*.bootstrap` 重新注册为自依赖服务的错误逻辑。定向回归 `tests/unit/root-entry-summary.test.ts` 的 `buildPlatformRootSummary initializes summary segments through ServiceRegistry-backed registrations` 以及 `tests/unit/domains-startup-plan.test.ts` / `tests/integration/domains-startup-plan.test.ts` 的 `registerDomainsStartupPlan registers and returns plan` 已通过。 | 这次关闭的是“root summary 绕过 registry”的真实实现缺口，summary 各段现在都能落到统一 registry 来源。 |
| 2430 | Round 41 — 深度补充审计（ops-maturity / scale-ecosystem / SDK / plugins / config / UI-shared / bootstrap） / §206 Bootstrap / 初始化 / 测试基础设施 缺陷 | P1 | `src/platform-architecture-bootstrap.ts:150` | getPlatformArchitectureServices 每次重新 register——覆盖缓存实例 | 已解决（源码修复并补定向测试） | 根因在于旧 getter 把“缺失时注册”与“每次都重建服务”混在一起，导致 registry 内缓存实例可能被重复注册逻辑覆盖。 | 当前 `src/platform-architecture-bootstrap.ts` 的 `registerPlatformArchitectureServices()` 已改为逐项 `if (!registry.has(...))` 再注册，`getPlatformArchitectureServices()` 也只在 `architecture.bootstrap-summary` 缺失时才触发注册；`tests/unit/platform-architecture-bootstrap.test.ts` 与 `tests/integration/platform-architecture-bootstrap.test.ts` 已持续验证重复调用返回同一组 registry 服务。 | 现行实现已收敛为真正的按需注册。 |
| 2431 | Round 41 — 深度补充审计（ops-maturity / scale-ecosystem / SDK / plugins / config / UI-shared / bootstrap） / §206 Bootstrap / 初始化 / 测试基础设施 缺陷 | P1 | `src/index.ts:13` | buildFivePlaneRuntimeCatalog 未 re-export——公共 API 不对称 | 已解决（源码修复并补定向测试） | 根因是根入口公共导出面在后续拆分 five-plane bootstrap helper 时没有同步维护，导致包根 API 与子模块能力不对称。 | 当前 `src/index.ts` 已显式 root export `buildFivePlaneRuntimeCatalog`；`tests/unit/root-bootstrap-exports-remediation.test.ts` 已新增对应断言并通过。 | 这是公共 API 实修，不是仅调整引用说明。 |
| 2432 | Round 41 — 深度补充审计（ops-maturity / scale-ecosystem / SDK / plugins / config / UI-shared / bootstrap） / §206 Bootstrap / 初始化 / 测试基础设施 缺陷 | P1 | `src/index.ts:10` | buildAiOperationsStartupPlan/buildFivePlaneStartupPlan 未根导出 | 已解决（源码修复并补定向测试） | 根因与 2431 相同：root entry 导出面落后于 bootstrap 模块拆分，导致调用方只能走内部子路径。 | 当前 `src/index.ts` 已显式导出 `buildAiOperationsStartupPlan` 与 `buildFivePlaneStartupPlan`；`tests/unit/root-bootstrap-exports-remediation.test.ts` 已新增两条 root export 断言并通过。 | 根导出对称性已补齐。 |
| 2433 | Round 41 — 深度补充审计（ops-maturity / scale-ecosystem / SDK / plugins / config / UI-shared / bootstrap） / §206 Bootstrap / 初始化 / 测试基础设施 缺陷 | P1 | `src/platform/execution/startup/graceful-shutdown.ts:274` | globalShutdownInstance 模块级单例未纳入 ServiceRegistry——双单例泄漏风险 | 已解决（源码修复并补定向测试） | 根因确实是 shutdown 生命周期曾分裂在“模块级单例”和“registry 服务”两套入口上，调用方可能拿到不同来源的实例。 | 当前 `src/platform/execution/startup/graceful-shutdown.ts` 的 `getGlobalGracefulShutdown()` 会优先读取 `ServiceRegistry`，缺失时创建并以 `platform.global-shutdown` 注册回 registry；`tests/integration/platform/execution/startup-lifecycle-integration.test.ts` 已验证同一 registry 下两次获取与 registry 取值为同一实例。 | shutdown 单例入口已统一到 registry 协调下。 |
| 2434 | Round 41 — 深度补充审计（ops-maturity / scale-ecosystem / SDK / plugins / config / UI-shared / bootstrap） / §206 Bootstrap / 初始化 / 测试基础设施 缺陷 | P1 | `src/platform/execution/startup/process-error-handlers.ts:19` | processLogger 模块顶层构造——绕过生命周期管理，测试间状态泄漏 | 已解决（源码修复并补定向测试） | 根因是旧实现把 `StructuredLogger` 放在模块顶层直接 new，导入模块即产生状态副作用，既绕过生命周期控制，也容易在测试间残留。 | 当前 `src/platform/execution/startup/process-error-handlers.ts` 只保留 `_processLogger: null`，真实构造已收敛到 `getProcessLogger()` 的 lazy path；同轮 `tests/integration/platform/execution/startup-lifecycle-integration.test.ts` 已通过 register/wiring/idempotent 回归，证明现行处理器注册路径在按需构造 logger 的实现下工作正常。 | 这里关闭的是“模块导入即构造”的真实副作用缺口。 |
| 2435 | Round 41 — 深度补充审计（ops-maturity / scale-ecosystem / SDK / plugins / config / UI-shared / bootstrap） / §206 Bootstrap / 初始化 / 测试基础设施 缺陷 | P1 | `src/platform-architecture-bootstrap.ts:111` | buildBootstrapSummary 与 registerServices 双源分歧——运行时数据不一致 | 已修复（源码修复并补定向测试） | 根因确实存在于双源构造：旧 `buildPlatformArchitectureBootstrapSummary()` 自己重新 `listPlatformLayerManifests/listPlatformApps/buildPlatformStartupTargets`，而 `registerPlatformArchitectureServices()` 返回值又再独立组装一次 apps/startupTargets，调用方可能读到并非同一 registry snapshot 派生的数据。 | 当前 `src/platform-architecture-bootstrap.ts` 已把 `buildPlatformArchitectureBootstrapSummary()` 改成可接收 registry sources，并在 `architecture.bootstrap-summary` 初始化时直接读取 `architecture.layer-catalog/plane-catalog/app-catalog/startup-targets`；`registerPlatformArchitectureServices()` 返回值也改为直接回传 registry.get 到的同一批 snapshot。 | 本轮新增并通过 `tests/unit/platform/architecture/platform-architecture-bootstrap.test.ts` 的 `registerPlatformArchitectureServices returns registry-backed snapshots`，直接验证返回值来自 registry，且 summary 中的 apps/startupTargets 与注册快照一致。 |
| 2436 | Round 41 — 深度补充审计（ops-maturity / scale-ecosystem / SDK / plugins / config / UI-shared / bootstrap） / §206 Bootstrap / 初始化 / 测试基础设施 缺陷 | P1 | `src/platform-application-kernel.ts:130` | registerKernel 未声明 dependsOn——ServiceRegistry 无法保障初始化顺序 | 已解决（源码修复并补定向测试） | 根因是 application kernel 作为 registry service 注册时缺少显式依赖元数据，导致初始化顺序语义只能靠调用方约定，不能从 service descriptor 读取。 | 当前 `src/platform-application-kernel.ts` 的 `registerPlatformApplicationKernel()` 已显式声明 `dependsOn: []`；`tests/unit/platform/architecture/platform-application-kernel.test.ts` 已新增断言并通过，确认 descriptor 中确实存在空依赖列表。 | 这是把“隐式无依赖”改成了 registry 可见的显式契约。 |
| 2437 | Round 41 — 深度补充审计（ops-maturity / scale-ecosystem / SDK / plugins / config / UI-shared / bootstrap） / §206 Bootstrap / 初始化 / 测试基础设施 缺陷 | P1 | `tests/integration/` | 无 graceful-shutdown/process-error-handlers 集成测试 | 已解决（源码修复并补定向测试） | 根因就是启动生命周期相关能力长期只有模块级单测/源码存在性检查，没有覆盖“registry 单例 + 进程处理器 wiring”的跨模块联动。 | 当前已新增 `tests/integration/platform/execution/startup-lifecycle-integration.test.ts`，覆盖 `getGlobalGracefulShutdown` registry 单例注册、`registerProcessErrorHandlers` 监听器接线，以及同一 shutdown 实例重复注册幂等三条集成回归。 | graceful shutdown / process handlers 现在已有专门的集成测试面。 |
| 2438 | Round 41 — 深度补充审计（ops-maturity / scale-ecosystem / SDK / plugins / config / UI-shared / bootstrap） / §206 Bootstrap / 初始化 / 测试基础设施 缺陷 | P2 | `tests/unit/runtime/graceful-shutdown.test.ts:6` | 测试路径不符合规范目录结构 | 已修复（测试目录清理） | 根因是 graceful-shutdown 早期测试在 `tests/unit/runtime/` 下留了一份遗留副本，后续虽已新增规范镜像路径 `tests/unit/platform/execution/startup/graceful-shutdown.test.ts`，但旧路径及其生成物没有被清理。 | 当前遗留的 `tests/unit/runtime/graceful-shutdown.test.ts/.js/.d.ts/.js.map` 已删除，权威单测仅保留在与源码镜像一致的 `tests/unit/platform/execution/startup/graceful-shutdown.test.ts`。 | 本轮定向执行已包含规范路径下的 graceful-shutdown 相关测试，删除旧路径不会丢失覆盖，只消除了目录结构漂移。 |
| 2439 | Round 41 — 深度补充审计（ops-maturity / scale-ecosystem / SDK / plugins / config / UI-shared / bootstrap） / §206 Bootstrap / 初始化 / 测试基础设施 缺陷 | P2 | `tests/e2e/ui-web-flow.test.ts:7` | 硬编码路径且未检查 multi-shell 路由清单一致性 | 已解决（源码复核） | 根因是旧 smoke test 只盯单一路由/单壳层输出，不能证明 web route catalog 与多 shell 清单一起被覆盖；现行 e2e 入口已升级为对 UI workspace 的统一 `npm run test:e2e` 调用，并显式校验 route catalog 与 shells 测试套件都被执行。 | 当前 `tests/e2e/ui-web-flow.test.ts` 会断言输出中同时包含 `tests/apps/web-e2e-smoke.test.tsx`、`tests/apps/web-route-catalog.test.tsx` 与 `tests/apps/shells.test.ts`，因此“未检查 multi-shell 路由清单一致性”已经与源码不符。 | 本条关闭依据为现行 e2e smoke 合同本身已把 multi-shell 与 route catalog 纳入执行面；本轮未重复跑整套 UI e2e。 |
| 2440 | Round 41 — 深度补充审计（ops-maturity / scale-ecosystem / SDK / plugins / config / UI-shared / bootstrap） / §206 Bootstrap / 初始化 / 测试基础设施 缺陷 | P2 | `tests/golden/prompt-assembly.test.ts:19` | golden 测试硬编码模型标识——model-metadata 变更则快照静默失效 | 已修复（测试去生产耦合） | 根因是这个 golden 用例测试的是 prompt partition/hash 语义，本不依赖真实模型目录，却把 `claude-sonnet-4-6/claude-haiku-4-5-20251001/claude-opus-4-6` 这类生产模型名写进断言；当 model metadata 或命名策略演进时，会引入与用例目标无关的脆弱性。 | 当前 `tests/golden/prompt-assembly.test.ts` 已改用 `test-model-primary/test-model-secondary/test-model-tertiary` 等稳定 fixture ID，只保留“不同 model/profile 影响 cache key”这一真实测试意图，不再耦合生产模型元数据。 | 本轮已对该 golden 用例做定向执行并通过，证明去耦后行为未回退。 |
| 2441 | Round 41 — 深度补充审计（ops-maturity / scale-ecosystem / SDK / plugins / config / UI-shared / bootstrap） / §206 Bootstrap / 初始化 / 测试基础设施 缺陷 | P2 | `src/index.ts:41` | PlatformRootEntryMode 定义在根入口而非 types 文件——类型分散 | 已解决（源码修复并补定向测试） | 根因是根入口早期直接携带了 `PlatformRootEntryMode` 类型别名，后续虽已有统一 types 模块，但根入口没有彻底退回为 type re-export。 | 当前 `src/platform-architecture-types.ts` 已以 `export type PlatformRootEntryMode = PlatformStartupTargetKind` 作为权威定义，`src/index.ts` 仅保留 type re-export；`tests/unit/root-entry-summary.test.ts` 中新增的 `PlatformRootEntryMode` 断言已通过按测试名过滤的定向执行验证。 | 该条关闭依据以现行类型归位和定向断言为准，不受该文件内无关旧失败影响。 |
| 2442 | Round 41 — 深度补充审计（ops-maturity / scale-ecosystem / SDK / plugins / config / UI-shared / bootstrap） / §206 Bootstrap / 初始化 / 测试基础设施 缺陷 | P2 | `package.json:6` | exports 缺 ./platform/execution/startup 子路径 | 已解决（源码修复并补定向测试） | 根因是包导出表没有随着 startup 子模块公开化同步补上 subpath，导致消费者无法通过 package exports 稳定导入该入口。 | 当前 `package.json` 已声明 `"./platform/execution/startup": "./dist/src/platform/execution/startup/index.js"`；`tests/unit/platform/execution/startup/index.test.ts` 已新增 package exports 子路径断言并通过。 | 包级 subpath export 已正式纳入回归。 |
| 2443 | Round 41 — 深度补充审计（ops-maturity / scale-ecosystem / SDK / plugins / config / UI-shared / bootstrap） / §206 Bootstrap / 初始化 / 测试基础设施 缺陷 | P2 | `src/platform/execution/startup/startup-consistency-checker.ts:16` | 硬依赖 DispatchReconciliation 构造——增加启动耦合 | 已解决（源码复核并补定向测试） | 根因是旧实现曾在构造器内直接 `new ExecutionDispatchReconciliationService(db, store)`，把 startup checker 与 dispatch repair 具体实现强耦合；现行源码其实已经支持通过 `options.dispatchReconciliation` 注入替身，只是之前没有一条回归把这件事钉死。 | 当前 `src/platform/execution/startup/startup-consistency-checker.ts` 构造器会优先采用 `options.dispatchReconciliation`，仅在缺省时才回退到默认实例；本轮新增测试已证明注入的 mock reconciler 会直接驱动 `orphan_queue_claim` finding。 | 本轮新增并通过 `tests/unit/platform/execution/startup/startup-consistency-checker.test.ts` 的 `StartupConsistencyChecker uses injected dispatch reconciliation service`。 |
| 2444 | Round 42 — 深度审计（compliance / HA-execution / UI-apps / contracts-implementation 交叉验证） / §208 Compliance / Model-Gateway / Prompt-Engine 深层缺陷 | P1 | `src/platform/prompt-engine/rollout/prompt-rollout-stage.ts:1` | 渐进阶段 canary_5/partial_25/50/75 与 spec 定义的 canary(5%)/canary(20%) 不一致 | 已解决（源码复核并补定向测试） | 根因是审计记录停留在旧阶段命名，旧实现曾使用 `canary_5/partial_25/50/75` 一组非规范枚举；现行实现已收敛到 contract 要求的 canonical 阶段名，但此前缺少一条最小回归把旧枚举彻底钉死。 | 当前 `src/platform/prompt-engine/rollout/prompt-rollout-stage.ts` 的 `PROMPT_ROLLOUT_STAGES` 已定义为 `["canary(5%)", "canary(20%)", "stable", "rolled_back"]`，`isPromptRolloutStage()` 也只接受这组 canonical 值。 | 本轮新增并通过 `tests/unit/platform/prompt-engine/rollout/prompt-rollout-stage-audit-regressions.test.ts` 的 `PROMPT_ROLLOUT_STAGES uses the canonical canary(5%)/canary(20%) progression`，直接验证旧值 `canary_5`、`partial_25` 不再被接受。 |
| 2445 | Round 42 — 深度审计（compliance / HA-execution / UI-apps / contracts-implementation 交叉验证） / §208 Compliance / Model-Gateway / Prompt-Engine 深层缺陷 | P1 | `src/platform/prompt-engine/rollout/prompt-rollout-stage.ts:24` | nextStage() 允许 stable→rolled_back 顺序推进——stable 应为终态 | 已解决（源码复核并补定向测试） | 根因是旧推进逻辑把 `stable` 当作普通中间态，允许继续顺推到 `rolled_back`；现行实现已经把 `stable` 与 `rolled_back` 都收敛为终态，但此前缺少直接覆盖 `stable -> null` 的回归测试。 | 当前 `src/platform/prompt-engine/rollout/prompt-rollout-stage.ts` 的 `nextPromptRolloutStage()` 对 `stage === "stable" || stage === "rolled_back"` 直接返回 `null`，不再允许 `stable→rolled_back` 的伪前进。 | 本轮新增并通过 `tests/unit/platform/prompt-engine/rollout/prompt-rollout-stage-audit-regressions.test.ts` 的 `nextPromptRolloutStage treats stable as a terminal state`，直接验证 `stable` 与 `rolled_back` 都不会再推进。 |
| 2446 | Round 42 — 深度审计（compliance / HA-execution / UI-apps / contracts-implementation 交叉验证） / §208 Compliance / Model-Gateway / Prompt-Engine 深层缺陷 | P1 | `src/platform/prompt-engine/eval/llm-eval-service.ts:348` | runAbTest() 硬编码 0.85/0.90 分数——A/B 测试结果完全虚假 | 已解决（源码复核并补定向测试） | 根因是旧实现曾把 control / treatment 结果近似写死为固定阈值语义，导致 A/B 结果脱离真实 case 评分；现行实现已改成按 case 相似度 / evaluator 实分计算，并统一使用配置阈值，但此前缺少一条直接证明“不会再退回硬编码 0.85/0.90”的最小回归。 | 当前 `src/platform/prompt-engine/eval/llm-eval-service.ts` 的 `runAbTest()` 已先计算 `passThreshold = config.significanceThreshold ?? 0.8`，然后对 control / treatment 两侧都使用真实 `score >= passThreshold` 判定，不再区分 0.85/0.90 两套硬编码阈值。 | 本轮新增并通过 `tests/unit/platform/prompt-engine/eval/llm-eval-service.test.ts` 的 `LlmEvalService.runAbTest uses config significanceThreshold as the pass threshold for both arms`，直接验证阈值来自 `config.significanceThreshold` 且两侧同时失败，不会出现旧实现那种固定阈值放行。 |
| 2447 | Round 42 — 深度审计（compliance / HA-execution / UI-apps / contracts-implementation 交叉验证） / §208 Compliance / Model-Gateway / Prompt-Engine 深层缺陷 | P1 | `src/platform/prompt-engine/eval/llm-eval-service.ts:308` | verdict 不区分 critical case——仅用 80% 阈值，critical_case_pass==100% 未实现 | 已解决（源码复核并补定向测试） | 根因是旧 verdict 计算只看整体通过率，critical case 失败时仍可能被非阻断阈值掩盖；现行实现已经在聚合阶段显式识别 `riskLevel === "critical"` 的失败 case，并强制 verdict 进入 `fail`，但缺少最小回归钉住这一门禁。 | 当前 `src/platform/prompt-engine/eval/llm-eval-service.ts` 的 `completeRun()` 会遍历失败结果的 `metadata.riskLevel`，一旦发现 critical failure 就设置 `hasCriticalFailure = true` 并直接输出 `verdict = "fail"`；只有“无 critical failure 且通过率 ≥ 95%”时才允许 `degraded`。 | 本轮新增并通过 `tests/unit/platform/prompt-engine/eval/llm-eval-service.test.ts` 的 `LlmEvalService.completeRun fails when any critical case fails even if pass rate remains above 95%`，验证 19/20 通过但 1 个 critical case 失败时仍会 fail，而不是被高通过率掩盖。 |
| 2448 | Round 42 — 深度审计（compliance / HA-execution / UI-apps / contracts-implementation 交叉验证） / §208 Compliance / Model-Gateway / Prompt-Engine 深层缺陷 | P1 | `src/platform/prompt-engine/eval/llm-eval-service.ts:525` | detectRegression 缺 latency_regression≤120% 和 cost_regression≤150% 门禁 | 已解决（源码复核并补定向测试） | 根因是旧 regression 检测只看 score delta，没有把延迟和成本恶化纳入回归门禁；现行实现已经补入 latency / cost 聚合与阈值判定，但此前缺少分别命中 120% 与 150% 门槛的最小回归。 | 当前 `src/platform/prompt-engine/eval/llm-eval-service.ts` 的 `detectRegression()` 已分别计算 `currentLatencyMs / previousLatencyMs` 与 metadata 中的平均 cost，并在 `> 1.20` / `> 1.50` 时置位 `latencyRegression`、`costRegression`；返回值里的 `hasRegression` 也已合并这两类门禁。 | 本轮新增并通过 `tests/unit/platform/prompt-engine/eval/llm-eval-service.test.ts` 的 `LlmEvalService.detectRegression flags latency regression above 120% of baseline` 与 `LlmEvalService.detectRegression flags cost regression above 150% of baseline`，分别验证两条门禁已真实生效。 |
| 2449 | Round 42 — 深度审计（compliance / HA-execution / UI-apps / contracts-implementation 交叉验证） / §208 Compliance / Model-Gateway / Prompt-Engine 深层缺陷 | P1 | `src/platform/prompt-engine/conversation-template-service.ts:374` | next() 对已注销 template 调用 registry.get() 抛异常而非返回 null | 已解决（源码复核并补定向测试） | 真实根因不是“现行 next 仍会抛异常”，而是审计条目停留在旧实现印象。当前 `buildTemplatedConversation()` 在 `registry.get()` 取不到模板时已经返回 `null`，`next()` 再通过 `result ?? conversation` fail-closed 返回原会话，不会因模板中途缺失而崩溃。 | 当前 `src/platform/prompt-engine/conversation-template-service.ts` 的 `next()` 会先调用 `buildTemplatedConversation(...)`，后者在模板缺失时直接 `return null`，`next()` 则返回原 `conversation` 而不是抛错。 | 本轮新增并通过 `tests/unit/platform/prompt-engine/conversation-template-service.test.ts` 的 `ConversationTemplateExecutor.next returns the existing conversation when template is no longer available mid-flow`，直接验证 `registry.get()` 返回 `undefined` 时不会抛异常。 |
| 2450 | Round 42 — 深度审计（compliance / HA-execution / UI-apps / contracts-implementation 交叉验证） / §208 Compliance / Model-Gateway / Prompt-Engine 深层缺陷 | P1 | `src/platform/execution/budget-allocator.ts:82` | settle() 内存计算 ledger——无 SQL CAS 原子操作，并发 settle 余额不一致 | 需继续整改（已复核并收窄） | 根因复核后更具体：`BudgetAllocator` 仍然把“状态合法性校验”和“账本金额提交”拆成两层，前者交给进程内 `RuntimeStateMachine`，后者仍直接基于调用方传入的 `BudgetLedger` 快照计算新金额；因为没有权威 `budget_ledgers/budget_reservations` repository，`settle()` 与 `release()` 都没有把金额更新包进数据库事务或行级 CAS。 | `src/platform/execution/budget-allocator.ts` 当前构造器仍只有 `stateMachine/events/sweeperConfig` 注入；`settle()` 先做一次 `stateMachine.transition(...)`，但末尾的 `finalLedger` 仍直接用 `input.ledger` 计算 `reservedAmount/settledAmount/releasedAmount`，`release()` 更是直接返回 `version: input.ledger.version + 1` 的内存 ledger。也就是说即便状态机做了版本递增，两个 worker 只要拿到同一旧快照，仍可能各自结算并生成互相覆盖的金额结果。 | 不假关闭：这一条缺的不是单测，而是预算账本的权威存储写入口和 SQL 级原子 commit/CAS；本轮未找到现成仓储可安全接入，因此保持未解决。 |
| 2451 | Round 42 — 深度审计（compliance / HA-execution / UI-apps / contracts-implementation 交叉验证） / §208 Compliance / Model-Gateway / Prompt-Engine 深层缺陷 | P1 | `src/platform/execution/runtime-state-machine.ts:322` | assertAuditRef 不校验 null——requiresAudit=true 时转换可不提供 auditRef | 已解决（源码复核并补定向测试） | 根因是早期 `assertAuditRef()` 只覆盖空字符串语义，没有把 `null/undefined` 作为缺失值一并拒绝；现行实现已经收敛为显式 `command.auditRef == null || trim().length === 0` 检查，但缺少独立的最小回归。 | 当前 `src/platform/execution/runtime-state-machine.ts` 的 `assertAuditRef()` 已在 requires-audit 场景下对 `null`、`undefined` 和空白字符串统一抛出 `runtime_state_machine.audit_ref_required`。 | 本轮新增并通过 `tests/unit/platform/execution/runtime-state-machine-audit-regressions.test.ts`，直接验证 `HarnessRun created -> admitted` 且 `auditRef: null` 会被拒绝。 |
| 2452 | Round 42 — 深度审计（compliance / HA-execution / UI-apps / contracts-implementation 交叉验证） / §208 Compliance / Model-Gateway / Prompt-Engine 深层缺陷 | P1 | `src/platform/state-evidence/memory/memory-decay-service.ts:161` | access boost 指数增长致 freshness 恒=1.0——高频记忆永不衰减违反 6 层模型 | 已解决（源码修复并补定向测试） | 根因是 access boost 公式长期缺少稳定的“衰减率抑制”模型：早期加法式 boost 会把 freshness 往 `1.0` 顶满，而现行中间实现虽然移除了加法饱和，却又把 boost 乘进 decay rate，反而让高频记忆衰减更快。两种实现都不满足 6 层模型要求的“高频访问减缓衰减，但仍会继续衰减”。 | 当前 `src/platform/state-evidence/memory/memory-decay-service.ts` 的 `calculateFreshness()` 已改为先计算 `accessBoostMultiplier = 1 + accessBoostFactor * log(1 + hitCount)`，再用 `(decayRate * decayRateMultiplier) / accessBoostMultiplier` 抑制衰减率；`calculateDecay()` 也同步返回与实际计算一致的 `effectiveDecayRate`，不再出现高 hitCount 加速衰减或 freshness 饱和到 `1.0`。 | 本轮通过 `tests/unit/platform/state-evidence/memory/memory-decay-service.test.ts` 的 `calculateFreshness with hitCount applies access boost` 与新增的 `calculateFreshness slows decay for frequently accessed memories without pinning freshness to 1.0`，分别验证高 hitCount 会提高 freshness，且不会把 freshness 锁死在 `1.0`。 |
| 2453 | Round 42 — 深度审计（compliance / HA-execution / UI-apps / contracts-implementation 交叉验证） / §208 Compliance / Model-Gateway / Prompt-Engine 深层缺陷 | P1 | `src/platform/state-evidence/events/cas/cas-service.ts:46` | CAS 纯内存 Map——多节点无一致性保证 | 已解决（源码修复并补定向测试） | 根因不是“只有没接 SQLite”这么简单，而是上一版修复只把 `CasService` 抽象成 repository 注入，却没有把 compare-and-swap 真正下沉到存储层原子语句；`SqliteCasRepository` 之前仍是 `get()` + `set()` 两步，等于把内存竞态原样搬到了 SQLite 外围。 | 当前 `src/platform/state-evidence/events/cas/cas-service.ts` 已把 `compareAndSwap/compareAndSet` 完全委托给 repository 原子原语；`src/platform/state-evidence/events/cas/sqlite-cas-repository.ts` 也已改成 `INSERT ... WHERE NOT EXISTS` 与 `UPDATE ... WHERE value/version = ?` 的单语句 CAS，不再走 `get()+set()` 两步。定向回归 `tests/integration/platform/state-evidence/truth/cas-and-truth-services.test.ts` 新增的 `CAS service: SQLite-backed instances share durable state and reject stale compare-and-set` 已验证两个 `CasService(SqliteCasRepository)` 实例共享同一 SQLite 文件时，后写实例能读到前写值，旧版本 CAS 会被正确拒绝。 | 现行实现已同时补齐“共享持久存储”和“存储层原子 compare-and-set”两层缺口；`createInMemoryCasService()` 仅保留为测试 helper，不再是权威实现。 |
| 2454 | Round 42 — 深度审计（compliance / HA-execution / UI-apps / contracts-implementation 交叉验证） / §208 Compliance / Model-Gateway / Prompt-Engine 深层缺陷 | P2 | `src/platform/execution/budget-reservation-sweeper.ts:31` | expiresAt 格式异常时 Date.parse 返回 NaN——孤儿 reservation 永不释放 | 已解决（源码复核） | 根因是旧实现直接使用 `Date.parse(reservation.expiresAt)` 参与比较，遇到非法时间串会得到 `NaN`，从而跳过释放条件；现行实现已经把非法 `expiresAt` 视为应立即清理的坏数据。 | 当前 `src/platform/execution/budget-reservation-sweeper.ts` 在 `Number.isNaN(expiresAtMs)` 时直接返回 `true`，把非法时间格式的 reservation 纳入释放列表。 | 现有 `tests/unit/platform/five-plane-execution/budget-reservation-sweeper.test.ts` 的 `sweep() handles invalid date format (NaN) by expiring immediately` 已直接覆盖该行为；本轮定向回归再次通过。 |
| 2455 | Round 42 — 深度审计（compliance / HA-execution / UI-apps / contracts-implementation 交叉验证） / §208 Compliance / Model-Gateway / Prompt-Engine 深层缺陷 | P2 | `src/platform/execution/distributed-lock/redis-lock-adapter.ts:97` | fencingCounter 实例级——多 pod 各自从 0 递增，无全局单调性 | 已解决（源码修复并补定向测试） | 根因是旧实现把 fencing token 计数器保存在适配器实例内，导致不同 pod / 不同 adapter 进程各自从 0 起步，无法满足分布式锁 fencing token 的全局单调性要求。 | 当前 `src/platform/execution/distributed-lock/redis-lock-adapter.ts` 的 `acquireAsync()` 与 `forceStealAsync()` 都已改为 `await this.redis.incr("lock:fencing_counter")` 从 Redis 侧统一发号，实例字段只缓存最后一次结果，不再承担全局计数职责。 | 本轮新增并通过 `tests/unit/platform/execution/distributed-lock/distributed-lock-audit-regressions.test.ts` 的 `RedisLockAdapter shares a Redis-backed monotonic fencing counter across adapter instances`，直接验证两个 adapter 实例与一次 steal 会得到 `1/2/3` 的全局递增 token。 |
| 2456 | Round 42 — 深度审计（compliance / HA-execution / UI-apps / contracts-implementation 交叉验证） / §208 Compliance / Model-Gateway / Prompt-Engine 深层缺陷 | P2 | `src/platform/execution/distributed-lock/redis-lock-adapter.ts:130` | extendAsync Lua 不更新 JSON.ttlMs——inspect 返回过期值 | 已解决（源码修复并补定向测试） | 根因是旧 Lua 延期路径只修改 Redis 键的实际 TTL，没有同步回写序列化 `LockData` 里的 `ttlMs`，导致后续 `inspect` / `get` 读到的元数据与真实过期时间漂移。 | 当前 `src/platform/execution/distributed-lock/redis-lock-adapter.ts` 的 `extendAsync()` Lua 已先写入 `data.ttlMs = newTtl`，再用 `redis.call('set', KEYS[1], cjson.encode(data), 'PX', newTtl)` 原子更新内容与 TTL，不再留下旧 `ttlMs`。 | 本轮新增并通过 `tests/unit/platform/execution/distributed-lock/distributed-lock-audit-regressions.test.ts` 的 `RedisLockAdapter extendAsync writes back updated ttlMs and preserves a PX TTL update path`，直接验证脚本包含 `data.ttlMs = newTtl` 且使用 `PX` 更新路径。 |
| 2457 | Round 42 — 深度审计（compliance / HA-execution / UI-apps / contracts-implementation 交叉验证） / §208 Compliance / Model-Gateway / Prompt-Engine 深层缺陷 | P2 | `src/platform/execution/distributed-lock/redis-lock-adapter.ts:182` | forceSteal SET XX——原 lock 过期被 Redis 删除后 steal 失败 | 已解决（源码复核并补定向测试） | 根因是旧 steal 语义依赖 `SET ... XX`，只有旧 key 仍存在时才能覆盖；一旦原 lock 已因 TTL 被 Redis 清掉，force-steal 反而失败。现行实现已经改为无 `XX` 的直接写入，但此前缺少最小回归证明“旧 key 不存在也能 steal”。 | 当前 `src/platform/execution/distributed-lock/redis-lock-adapter.ts` 的 `forceStealAsync()` 使用 `this.redis.set(key, JSON.stringify(lockData), "PX", String(Math.ceil(ttlMs)))`，不再附带 `XX` 条件。 | 本轮新增并通过 `tests/unit/platform/execution/distributed-lock/distributed-lock-audit-regressions.test.ts` 的 `RedisLockAdapter forceStealAsync no longer depends on XX and succeeds when the old lock is already absent`，直接验证缺失旧 key 时仍能成功 steal，且 `set` 参数里不含 `XX`。 |
| 2458 | Round 42 — 深度审计（compliance / HA-execution / UI-apps / contracts-implementation 交叉验证） / §208 Compliance / Model-Gateway / Prompt-Engine 深层缺陷 | P2 | `src/platform/execution/distributed-lock/pg-advisory-lock-adapter.ts:31` | lockKey hash 仅 32-bit——碰撞概率高 | 已解决（源码修复并补定向测试） | 根因是旧 advisory key 推导只落在 32-bit 空间，长尾 lock key 在多租户/多 workflow 场景下碰撞概率过高；现行实现已切换到 64-bit FNV-1a 并压到正 63-bit 范围，但此前缺少专门回归证明不会再退回 32-bit 截断。 | 当前 `src/platform/execution/distributed-lock/pg-advisory-lock-adapter.ts` 的 `lockKeyToAdvisoryKey()` 使用 FNV-1a 64-bit 累积，并在返回前执行 `hash & BigInt("0x7FFFFFFFFFFFFFFF")`，把 key 保持在 PostgreSQL advisory lock 可接受的正 63-bit 空间。 | 本轮新增并通过 `tests/unit/platform/execution/distributed-lock/distributed-lock-audit-regressions.test.ts` 的 `PgAdvisoryLockAdapter lockKeyToAdvisoryKey is not truncated to a 32-bit hash`，直接验证生成值大于 `0xFFFFFFFFn` 且不超过 `0x7FFFFFFFFFFFFFFFn`。 |
| 2459 | Round 42 — 深度审计（compliance / HA-execution / UI-apps / contracts-implementation 交叉验证） / §208 Compliance / Model-Gateway / Prompt-Engine 深层缺陷 | P2 | `src/platform/execution/distributed-lock/pg-advisory-lock-adapter.ts:66` | extend() 返回 inspect() 恒 null——扩展静默失败 | 已解决（源码复核并补定向测试） | 根因是旧同步 `extend()` 路径把“不支持的 PostgreSQL advisory lock 续租”伪装成 `inspect()/null` 返回，调用方会把静默失败误判成普通未命中，而不是能力不支持。现行实现已改为 fail-closed 抛错，但此前缺少单独回归。 | 当前 `src/platform/execution/distributed-lock/pg-advisory-lock-adapter.ts` 的 `extend()` 已直接抛出 `lock.pg_async_required`，明确要求调用 `extendAsync()`，不再返回 `null` 掩盖失败。 | 本轮修正并通过 `tests/unit/platform/execution/distributed-lock/pg-advisory-lock-adapter.test.ts` 的 `PgAdvisoryLockAdapter extend() throws pg_async_required`，直接验证同步续租现在会显式失败。 |
| 2460 | Round 42 — 深度审计（compliance / HA-execution / UI-apps / contracts-implementation 交叉验证） / §208 Compliance / Model-Gateway / Prompt-Engine 深层缺陷 | P2 | `src/platform/state-evidence/events/cas/fencing-token-service.ts:58` | activeFences static Map 但 tokenCounter 实例级——不同实例可生成相同 token | 已解决（源码复核并补定向测试） | 根因是旧实现确实存在“静态 fence 表 + 实例私有 counter”的拆裂设计；现行代码已经把 token counter 收敛为进程级 `globalTokenCounter`，但缺少直接证明“跨实例仍单调递增”的最小测试。 | 当前 `src/platform/state-evidence/events/cas/fencing-token-service.ts` 已把计数器改为 `private static readonly globalTokenCounter`，`generateFencingToken()` 不再从实例私有计数器取值。 | 本轮新增并通过 `tests/unit/platform/state-evidence/events/cas/fencing-token-service.test.ts` 的 `FencingTokenService shares a monotonic counter across service instances`，验证两个 service 实例生成的 counter 仍严格递增。 |
| 2461 | Round 42 — 深度审计（compliance / HA-execution / UI-apps / contracts-implementation 交叉验证） / §208 Compliance / Model-Gateway / Prompt-Engine 深层缺陷 | P2 | `src/platform/state-evidence/events/cas/fencing-token-service.ts:100` | split("-") 解析——executionId/nodeId 含 "-" 时解析错误 | 已解决（源码复核） | 根因是旧 token 格式曾按 `"-"` 解析，遇到 UUID 或多段 hyphenated ID 会把 executionId/nodeId 错拆。现行实现已经统一改用专用分隔符 `::` 并对 executionId/nodeId 做 URL 编码。 | 当前 `src/platform/state-evidence/events/cas/fencing-token-service.ts` 使用 `FENCING_TOKEN_SEPARATOR = "::"` 生成 token，并在 `validateFencingToken()` 中按 `token.split(FENCING_TOKEN_SEPARATOR)` 解析。 | 现有 `tests/unit/platform/state-evidence/events/cas/fencing-token-service.test.ts` 已覆盖 UUID executionId、多段 `-` executionId 和错误 owner 等场景；本轮定向回归再次通过。 |
| 2462 | Round 42 — 深度审计（compliance / HA-execution / UI-apps / contracts-implementation 交叉验证） / §208 Compliance / Model-Gateway / Prompt-Engine 深层缺陷 | P2 | `src/ops-maturity/explainability/explanation-pipeline-service.ts:80` | L3 TTL=0 条目仍写入缓存——审计解释不可检索 | 已解决（源码复核） | 根因不是 L3 解释现在仍然写缓存，而是 reaudit 沿用了修复前印象。现行实现已经把 L3 缓存 TTL 固定为 `0`，并通过缓存写入口在 `ttlHours === 0` 时直接跳过写入，只是缺少把“L3 不进入缓存”单独回写到审计表。 | 当前 `src/ops-maturity/explainability/explanation-pipeline-service.ts` 在 `depth === "L3"` 时设置 `cacheTtlHours = 0`，`src/ops-maturity/explainability/explanation-cache/index.ts` 的 `putExplanationCacheEntry()` 又在 `entry.ttlHours === 0` 时直接返回原 cache，不会写入 L3 条目。 | 已复跑并通过 `tests/unit/ops-maturity/explanation-pipeline-service.test.ts` 的 `ExplanationPipelineService preserves facts across depths and redacts unauthorized evidence`，其中显式断言 `service.getCached(audit.cacheKey) === null`，直接验证 L3 不缓存。 |
| 2463 | Round 42 — 深度审计（compliance / HA-execution / UI-apps / contracts-implementation 交叉验证） / §208 Compliance / Model-Gateway / Prompt-Engine 深层缺陷 | P2 | `src/platform/prompt-engine/eval/prompt-model-policy-governance-service.ts:162` | reviewRequired=false 直接 approved——跳过 staging eval gate | 已解决（源码复核并补定向测试） | 根因是旧注册路径确实曾把 `reviewRequired=false` 直接映射成已批准状态，导致 release 可能绕过后续 staging eval gate；现行实现已经统一收敛到 `review_required`，但仓内还残留一条旧断言把这种绕过当成正确行为。 | 当前 `src/platform/prompt-engine/eval/prompt-model-policy-governance-service.ts` 的 `registerPromptRelease()`、`registerModelRelease()`、`registerPolicyBundleRelease()` 都显式写入 `status: "review_required"`，不再让 `reviewRequired=false` 直接变成 `approved`。 | 本轮修正并通过 `tests/integration/platform/prompt-engine/eval/prompt-model-policy-governance-service-integration.test.ts` 的 `governance service getRelease keeps newly registered releases at review_required even when reviewRequired=false`，直接验证注册后状态仍是 `review_required`。 |
| 2464 | Round 42 — 深度审计（compliance / HA-execution / UI-apps / contracts-implementation 交叉验证） / §209 HA / Lease / Recovery / Worker-Pool / Dispatcher 深层缺陷 | P0 | `src/platform/execution/ha/ha-coordinator-service-inner.ts:353` | renewLeadership 返回 currentLease.ttlMs 而非 fencing token——续约后 stale-write 校验失效 | 已解决（源码复核并补定向测试） | 根因是早期同步版 `renewLeadership()` 的返回值契约确实曾把 TTL 暴露成 `fencingToken` 语义，后续实现已改为从最新 leadership epoch 读取 fencing token，但 reaudit 表没有跟进，且缺少一条直接钉死“不能把 ttlMs 当 token 返回”的最小回归。 | 当前 `src/platform/execution/ha/ha-coordinator-service-inner.ts` 在续约后会调用 `getLatestEpoch()`，并显式返回 `fencingToken: latestEpoch.fencingToken`，不再返回 lease TTL。 | 本轮新增并通过 `tests/unit/platform/execution/ha/ha-coordinator-service-inner.test.ts` 的 `renewLeadership returns epoch fencing token instead of ttlMs`；同轮还把 `verifyWriteAuthority` 的旧测试断言改成与现行 “presented token 必须大于 current token” 语义一致，保证这组 HA fencing 回归证据可用。 |
| 2465 | Round 42 — 深度审计（compliance / HA-execution / UI-apps / contracts-implementation 交叉验证） / §209 HA / Lease / Recovery / Worker-Pool / Dispatcher 深层缺陷 | P0 | `src/platform/execution/ha/ha-coordinator-service-async.ts:228` | 异步版同样返回 ttlMs 作为 fencing token | 已解决（源码复核并补定向测试） | 根因与同步版相同：问题原本在旧实现中真实存在，但当前 async 版本已经在续约时读取最新 epoch fencing token；遗留缺口是没有直接断言该返回值与 epoch token 一致、且不等于 `ttlMs`。 | 当前 `src/platform/execution/ha/ha-coordinator-service-async.ts` 在 `renewLeadership()` 内先调用 `getLatestEpoch()`，随后更新 `cachedFencingToken`，并返回 `fencingToken: latestEpoch.fencingToken`。 | 本轮新增并通过 `tests/unit/platform/execution/ha/ha-coordinator-service-async.test.ts` 的 `renewLeadership returns epoch fencing token instead of ttlMs`，与同步版单测一起形成最小闭环证据。 |
| 2466 | Round 42 — 深度审计（compliance / HA-execution / UI-apps / contracts-implementation 交叉验证） / §209 HA / Lease / Recovery / Worker-Pool / Dispatcher 深层缺陷 | P0 | `src/platform/execution/ha/ha-coordinator-service-inner.ts:701` | fencingTokenCounter 内存变量——重启归零可复用旧 token | 已解决（源码复核并补定向测试） | 根因是旧同步版确实依赖过实例内 fencing counter；现行实现已经改成从持久 epoch 表读取 `MAX(fencing_token)+1`，但 reaudit 条目没有更新，而且原单测 mock 也没有实现 `SELECT MAX(fencing_token)` 聚合，导致这条修复缺少可信的最小回归证据。 | 当前 `src/platform/execution/ha/ha-coordinator-service-inner.ts` 的 `nextFencingToken()` 已改为查询 `leadership_epochs` 的 `MAX(fencing_token)` 再加一，不再依赖进程内 counter，因此重启后仍从持久 token 空间继续递增。 | 本轮补齐 `tests/unit/platform/execution/ha/ha-coordinator-service-inner.test.ts` 的 mock `MAX(fencing_token)` 支持，并新增 `fencing token remains monotonic across service instances sharing the same DB`，验证两个 service 实例共享同一 DB 时 token 仍单调递增。 |
| 2467 | Round 42 — 深度审计（compliance / HA-execution / UI-apps / contracts-implementation 交叉验证） / §209 HA / Lease / Recovery / Worker-Pool / Dispatcher 深层缺陷 | P0 | `src/platform/execution/ha/ha-coordinator-service-async.ts:510` | 异步版 counter 各节点独立递增——无全局唯一性 | 已解决（源码复核并补定向测试） | 根因是旧 async service 的 fallback path 曾被审计为“节点各自维护 counter”；现行实现已经不保存节点私有 fencing 计数，而是通过 repo 的 latest epoch 推导下一个 token，atomic repo path 则直接在后端事务里分配 fencing token。遗留问题是没有一条跨 service 实例的最小回归证明这一点。 | 当前 `src/platform/execution/ha/ha-coordinator-service-async.ts` 的 fallback `nextFencingToken()` 基于 `getLatestEpoch().fencingToken + 1`，而 atomic path 会委派给 repo `acquireLeadershipAtomically()` 在共享后端内生成 token；因此 token 来源已经收敛到共享 epoch 状态，而不是实例私有变量。 | 本轮新增并通过 `tests/unit/platform/execution/ha/ha-coordinator-service-async.test.ts` 的 `fencing token remains monotonic across service instances sharing the same repo`，验证两个 async service 共享同一 repo 时 token 仍按全局 epoch 单调递增。 |
| 2468 | Round 42 — 深度审计（compliance / HA-execution / UI-apps / contracts-implementation 交叉验证） / §209 HA / Lease / Recovery / Worker-Pool / Dispatcher 深层缺陷 | P0 | `src/platform/execution/ha/ha-coordinator-service-async.ts:102` | acquireLeadership 无事务——并发可同时获得 leadership 脑裂 | 已修复 | 根因是旧 async service 自己串联 `getCurrentLeader/getLatestEpoch/insertLease/upsertNode/insertEpoch` 多次异步 repo 调用，既没有单事务边界，也没有 PostgreSQL 侧 advisory lock，因此两个竞争者可能同时读到同一 epoch 和空闲 leadership 视图，最终各自写入新 lease/epoch。 | 当前 `src/platform/execution/ha/ha-coordinator-service-async.ts` 已优先委派到 repo 原子入口 `acquireLeadershipAtomically()`；`src/platform/execution/ha/ha-repository-sqlite.ts` 现在把 leader demote、旧 lease 失效、新 lease/epoch/failover decision 写入包进同一个 `db.transaction(...)`；`src/platform/execution/ha/ha-repository-postgres.ts` 则在 `db.transaction(...)` 内先拿 `pg_try_advisory_xact_lock` 再执行同一批状态迁移，不再由 service 在事务外拼接写链。 | 本轮新增并通过 `tests/unit/platform/execution/ha/ha-coordinator-service-async.test.ts` 的 `acquireLeadership delegates to atomic repo path when available`、`tests/unit/platform/execution/ha/ha-repository-sqlite.test.ts` 的 `acquireLeadershipAtomically rotates leader state in one transaction`、`tests/unit/platform/execution/ha/ha-repository-postgres.test.ts` 的 `acquireLeadershipAtomically uses xact lock and rotates leader state`。 |
| 2469 | Round 42 — 深度审计（compliance / HA-execution / UI-apps / contracts-implementation 交叉验证） / §209 HA / Lease / Recovery / Worker-Pool / Dispatcher 深层缺陷 | P0 | `src/platform/execution/ha/lease-reclaimer-service.ts:387` | getExpiredLeases 依赖 getActiveLease(只返回未过期)——永远找不到过期租约 | 已解决 | 根因不是 reclaimer 现在仍从 `getActiveLease()` 倒推过期租约，而是 reaudit 条目停留在旧实现：早期版本确实只看“当前 active lease 视图”，导致真正已过期的租约永远进不了 reclaim cycle；现行实现已经改成直接查询 expired lease rows，只是文档和断言没同步。 | 当前 `src/platform/execution/ha/lease-reclaimer-service.ts` 的 `getExpiredLeases()` 已直接调用 `coordinator.getExpiredLeaseRows()`，不再依赖 `getActiveLease()` 这种只暴露未过期租约的视图。本轮补强 `tests/unit/platform/execution/ha/lease-reclaimer-service.test.ts` 中的 `expired lease detection` 用例，明确验证过期租约会被识别、`reclaimedCount === 1`，并且对应 lease 状态被改成 `expired`。 | 回写真实现状与最小定向测试证据。 |
| 2470 | Round 42 — 深度审计（compliance / HA-execution / UI-apps / contracts-implementation 交叉验证） / §209 HA / Lease / Recovery / Worker-Pool / Dispatcher 深层缺陷 | P0 | `src/platform/execution/ha/lease-reclaimer-service.ts:415` | expireLease 仅打日志不执行状态变更——过期租约无法回收 | 已解决 | 根因是旧 `expireLease()` 只有日志侧效果，没有把 lease status 从 `active` 推进到 `expired`；后续实现已补上 coordinator 状态写入，但 reaudit 仍沿用漏洞时期结论。 | 当前 `src/platform/execution/ha/lease-reclaimer-service.ts` 的 `expireLease()` 已直接调用 `coordinator.expireLease(lease.leaseId)`，`expireLeaseForNode()` 也会在 stale-node 场景下实际推进状态。本轮补强 `tests/unit/platform/execution/ha/lease-reclaimer-service.test.ts` 的 `reclaimOnce() processes expired lease after grace period`，验证回收后 `coordinator.mockState.leases.get("node-1")?.status === "expired"`，不再只是“跑过流程”。 | 回写真实现状与最小定向测试证据。 |
| 2471 | Round 42 — 深度审计（compliance / HA-execution / UI-apps / contracts-implementation 交叉验证） / §209 HA / Lease / Recovery / Worker-Pool / Dispatcher 深层缺陷 | P0 | `src/platform/execution/recovery/runtime-recovery-service.ts:298` | findStaleExecuting 用 now+threshold 而非 now-threshold——所有执行被判 stale | 已解决 | 根因不是当前 stale-execution 查询仍把阈值方向算反，而是 reaudit 停留在旧实现；现行 `findStaleExecuting()` 已按 `Date.now() - thresholdMs` 计算 stale 上界，只是旧文档和测试没有把这个边界真正断言出来。 | 当前 `src/platform/execution/recovery/runtime-recovery-service.ts` 已使用 `const staleBefore = new Date(Date.now() - thresholdMs).toISOString()`，再交给 `listStaleRuns(staleBefore, ...)`。本轮补强 `tests/unit/platform/execution/recovery/runtime-recovery-service.test.ts`，验证传给 store 的 `staleBefore` 落在 `now - threshold` 边界，而不是 `now + threshold`。 | 回写真实现状与最小定向测试证据。 |
| 2472 | Round 42 — 深度审计（compliance / HA-execution / UI-apps / contracts-implementation 交叉验证） / §209 HA / Lease / Recovery / Worker-Pool / Dispatcher 深层缺陷 | P1 | `src/platform/execution/ha/ha-coordinator-service-async.ts:487` | verifyWriteAuthority >= 比较——旧 leader 持当前 token 仍可写入 | 已解决 | 根因不是 async coordinator 仍以 `>=` 放行当前 token，而是文档和旧测试停留在修复前语义；现行实现已经改为严格 `>` 比较，要求 presented token 必须大于当前缓存 token。 | 当前 `src/platform/execution/ha/ha-coordinator-service-async.ts` 的 `verifyWriteAuthority()` 已明确返回 `presentedFencingToken > this.cachedFencingToken`。本轮补强 `tests/unit/platform/execution/ha/ha-coordinator-service-async.test.ts`，显式验证“当前 token 100 被拒绝、仅 101 通过”。 | 回写真实现状与最小定向测试证据。 |
| 2473 | Round 42 — 深度审计（compliance / HA-execution / UI-apps / contracts-implementation 交叉验证） / §209 HA / Lease / Recovery / Worker-Pool / Dispatcher 深层缺陷 | P1 | `src/platform/execution/ha/ha-coordinator-service-async.ts:503` | purgeOldFailoverDecisions 硬编码返回 0——failover 历史永不清理 | 已解决 | 根因不是 async service 现在还硬编码返回 `0`，而是审计和旧测试没有跟进后续实现：现行代码已经把清理职责下放给 repo，并在 repo 不可用时才保守回退。 | 当前 `src/platform/execution/ha/ha-coordinator-service-async.ts` 已调用 `await this.repo.purgeOldFailoverDecisions(olderThanDays)`；本轮补强 `tests/unit/platform/execution/ha/ha-coordinator-service-async.test.ts`，为 mock repo 增加真实清理实现，并验证过期 failover decision 会被删除、返回清理计数 `1`。 | 回写真实现状与最小定向测试证据。 |
| 2474 | Round 42 — 深度审计（compliance / HA-execution / UI-apps / contracts-implementation 交叉验证） / §209 HA / Lease / Recovery / Worker-Pool / Dispatcher 深层缺陷 | P1 | `src/platform/execution/ha/leader-election-service.ts:176` | HA_1 单节点无续约定时器——租约过期后不自动续期 | 已解决 | 根因不是当前 HA_1 模式仍停在“只创建单节点 lease、之后不续约”的旧实现，而是 reaudit 与一条旧测试标题都停留在修复前；现行 `start()` 已在单节点分支显式启动 renewal loop。 | 当前 `src/platform/execution/ha/leader-election-service.ts` 在 `haLevel === "HA_1"` 分支里会构造单节点 lease、启动 heartbeat，并在 `leaseRenewalIntervalMs <= 0` 时回退到 `5_000ms` 续约定时器，周期性调用 `renewLeadership()`。本轮补强 `tests/unit/platform/execution/ha/leader-election-service.test.ts`，通过 `renewLeadershipCalls` 计数验证 HA_1 启动后会实际发生续约调用。 | 回写真实现状与最小定向测试证据。 |
| 2475 | Round 42 — 深度审计（compliance / HA-execution / UI-apps / contracts-implementation 交叉验证） / §209 HA / Lease / Recovery / Worker-Pool / Dispatcher 深层缺陷 | P1 | `src/platform/execution/ha/stuck-run-sweeper-service.ts:559` | killRun 先设 status="killed" 再调回调——回调失败时状态已污染 | 已解决 | 根因不是当前 `killRun()` 仍然先污染状态再调回调，而是旧测试和 reaudit 结论没有跟进后续修复；现行实现已经先等待 kill callback 成功，失败时直接返回 `false`，不再把 run 标成 `killed`。 | 当前 `src/platform/execution/ha/stuck-run-sweeper-service.ts` 已在 `onKillExecution` 返回失败时提前退出，只有 callback 成功后才执行 `run.status = "killed"`。本轮修正 `tests/unit/platform/execution/ha/stuck-run-sweeper-service.test.ts` 的过时断言，显式验证 kill callback 返回 `false` 时 run 仍保持 `warning` 且 `killedAt === null`。 | 回写真实现状并补齐定向测试证据。 |
| 2476 | Round 42 — 深度审计（compliance / HA-execution / UI-apps / contracts-implementation 交叉验证） / §209 HA / Lease / Recovery / Worker-Pool / Dispatcher 深层缺陷 | P1 | `src/platform/execution/ha/recovery-orchestrator-service.ts:27` | recovery worker 串行 for-of await——无并行度，大量待恢复项延迟高 | 已解决 | 根因不是现行 orchestrator 仍然逐个 `for-of await` worker，而是 reaudit 条目停留在旧实现快照；当前代码早已先按优先级排序，再把每个 worker 包成 promise，用 `Promise.all(...)` 并发执行，只是仓内缺少直接证明“确实并行”的单测。 | 当前 `src/platform/execution/ha/recovery-orchestrator-service.ts` 已构造 `const workerPromises = orderedWorkers.map(async ...)`，随后统一 `await Promise.all(workerPromises)`，不是串行等待。 | 本轮新增并通过 `tests/unit/platform/execution/ha/recovery-orchestrator-service.test.ts` 的 `RecoveryOrchestratorService runs recovery workers in parallel once sorted`，直接断言两个 worker 在同一 cycle 内并发重叠执行，`maxConcurrentWorkers >= 2`。 |
| 2477 | Round 42 — 深度审计（compliance / HA-execution / UI-apps / contracts-implementation 交叉验证） / §209 HA / Lease / Recovery / Worker-Pool / Dispatcher 深层缺陷 | P1 | `src/platform/execution/dispatcher/execution-dispatch-service.ts:401` | 嵌套事务——SQLite 不支持 SAVEPOINT 嵌套可静默失败 | 已解决 | 根因不是 dispatch 主路径真的跑在“不支持嵌套事务”的 SQLite 上，而是审计和一处旧注释都误把标准 SQLite 限制直接套到了本项目自定义事务包装器上；实际使用的 `SqliteDatabase.transaction()` 明确实现了 `SAVEPOINT/RELEASE/ROLLBACK TO SAVEPOINT`，正是为 `dispatch -> acquireLease` 这种嵌套事务链准备的。 | 当前 `src/platform/state-evidence/truth/sqlite/sqlite-database.ts` 的 `runInTransaction()` 在 root write transaction 用 `BEGIN IMMEDIATE`，嵌套层使用 `SAVEPOINT aa_tx_n` / `RELEASE SAVEPOINT` / `ROLLBACK TO SAVEPOINT`；`src/platform/execution/dispatcher/execution-dispatch-service.ts` 主派发路径继续在外层 `this.db.transaction(...)` 中调用 `this.leases.acquireLease(...)`，而这条嵌套链现在有真实 savepoint 保护。本轮还同步修正了 emergency lane 旁边那条已经过时的误导性注释。 | 本轮复跑并通过 `tests/unit/platform/state-evidence/truth/sqlite-database.test.ts` 的 `sqlite database nested transactions roll back only the inner savepoint`，直接证明内层事务失败只回滚 savepoint，不会把外层事务一并污染。 |
| 2478 | Round 42 — 深度审计（compliance / HA-execution / UI-apps / contracts-implementation 交叉验证） / §209 HA / Lease / Recovery / Worker-Pool / Dispatcher 深层缺陷 | P1 | `src/platform/execution/lease/execution-lease-service-async.ts:247` | releaseLeaseSync 不检查 lease.status——已过期租约可被再次释放 | 已解决 | 根因不是当前 async 版 `releaseLeaseSync()` 仍漏掉 status 检查，而是 reaudit 与一条旧单测都停留在漏洞时期描述；现行实现已经和 sync 版一致，在释放前先拦截 `lease.status !== "active"`。 | 当前 `src/platform/execution/lease/execution-lease-service-async.ts` 的 `releaseLeaseSync()` 已在 worker 校验后立即执行 `if (lease.status !== "active") return { outcome: "blocked", reasonCode: "lease_not_active" }`。仓内 `tests/unit/platform/execution/lease/execution-lease-service-async-r9.test.ts` 早已覆盖“already released / expired lease 均返回 `lease_not_active`”；本轮进一步修正 `tests/unit/platform/execution/lease/lease-service.test.ts` 的过时断言，使基线与现行实现一致。 | 回写真实现状并补齐定向测试/测试基线证据。 |
| 2479 | Round 42 — 深度审计（compliance / HA-execution / UI-apps / contracts-implementation 交叉验证） / §209 HA / Lease / Recovery / Worker-Pool / Dispatcher 深层缺陷 | P1 | `src/platform/execution/tool-executor/tool-parallel-executor.ts:407` | results 数组含 undefined 空洞——返回类型断言 T[] 致下游 NPE | 已解决 | 根因是旧并行执行器在 tool 失败分支只记录 `errors.push(...)`，没有给 `results[originalIndex]` 明确赋值，导致结果数组留下 sparse hole；下游把它当致密 `T[]` 消费时，既拿不到占位也更容易出现索引位误判。 | 当前 `src/platform/execution/tool-executor/tool-parallel-executor.ts` 在并行和 exclusive 失败路径都会显式写入 `results[index] = undefined`，不再留下 hole；本轮扩展 `tests/unit/platform/execution/tool-executor/tool-parallel-executor.test.ts`，新增用例验证失败槽位 `1 in result.results === true` 且返回 `["ok-0", undefined, "ok-2"]`。 | 回写真实根因、源码依据与最小定向测试证据。 |
| 2480 | Round 42 — 深度审计（compliance / HA-execution / UI-apps / contracts-implementation 交叉验证） / §209 HA / Lease / Recovery / Worker-Pool / Dispatcher 深层缺陷 | P1 | `src/platform/execution/tool-executor/command-executor.ts:162` | activeProcessCount static 无锁——高并发可超限 | 已解决 | 根因是旧实现先经过多段异步校验再递增 `activeProcessCount`，并发请求会同时观察到“尚未超限”并一起穿过阈值检查，静态计数器等于没形成真正的保留位。现行实现已把检查和占位收敛成同步临界区，并在所有退出路径释放槽位。 | 当前 `src/platform/execution/tool-executor/command-executor.ts` 在 `execute()` 开头先执行 `if (activeProcessCount >= MAX...) return blocked`，随后同步 `activeProcessCount++`，并通过 `releaseSlot()` + 双层 `finally` 保证释放；本轮扩展 `tests/unit/platform/execution/tool-executor/command-executor.test.ts`，新增用例验证“达到上限时返回 `tool.process_limit_exceeded`，释放后同一 executor 可恢复执行成功”。 | 回写真实根因、源码依据与最小定向测试证据。 |
| 2481 | Round 42 — 深度审计（compliance / HA-execution / UI-apps / contracts-implementation 交叉验证） / §209 HA / Lease / Recovery / Worker-Pool / Dispatcher 深层缺陷 | P2 | `src/platform/execution/tool-executor/command-executor.ts:99` | Windows/Unix 使用相同 SIGTERM——Windows 上无效 | 已解决（源码复核并补定向测试） | 根因是旧实现把 Unix 的“进程组 SIGTERM”语义直接套到了 Windows，后者既不支持负 PID 进程组 kill，也不能依赖同一套回收路径；现行实现已经把 Unix / Windows 分支拆开，但此前缺少跨平台分支级回归。 | 当前 `src/platform/execution/tool-executor/command-executor.ts` 的 `killProcessTree()` 在 `process.platform !== "win32"` 时对 `-pid` 发送 `SIGTERM/SIGKILL`，而在 `win32` 时改为直接调用 `child.kill("SIGTERM")` / `child.kill("SIGKILL")`，不再复用 Unix 进程组语义。 | 本轮新增并通过 `tests/unit/platform/execution/tool-executor/command-executor.test.ts` 的 `command executor timeout uses process-group kill on Unix platforms` 与 `command executor timeout uses child.kill on win32 instead of process-group SIGTERM`，分别验证两条分支。 |
| 2482 | Round 42 — 深度审计（compliance / HA-execution / UI-apps / contracts-implementation 交叉验证） / §209 HA / Lease / Recovery / Worker-Pool / Dispatcher 深层缺陷 | P2 | `src/platform/execution/tool-executor/command-security.ts:259` | 过度拦截脚本参数——python --verbose 等合法用法被拒绝 | 已解决（源码复核并补定向测试） | 根因是旧解释器签名校验默认把 `args[0]` 当脚本路径，导致 `python --verbose script.py` 这类“前置解释器 flag + 真正脚本路径在后面”的合法调用被误判成缺脚本或危险 flag。现行实现已改为寻找第一个非 flag 参数作为脚本路径。 | 当前 `src/platform/execution/tool-executor/command-security.ts` 的 `validateCommandSignature()` 已通过 `args.findIndex((arg) => !arg.startsWith("-"))` 查找真实脚本路径，并仅在“全是 flag / 根本没有脚本路径”时返回 `tool.command_interpreter_flag_denied`。 | 已复跑并通过 `tests/unit/platform/execution/tool-executor/command-security.test.ts` 的 `CommandSafetyClassifier allows interpreter pre-flags when a real script path follows`，同时补齐 `CommandSafetyClassifier allows interpreter arguments after the script path`，确认合法脚本参数不再被误拦截。 |
| 2483 | Round 42 — 深度审计（compliance / HA-execution / UI-apps / contracts-implementation 交叉验证） / §209 HA / Lease / Recovery / Worker-Pool / Dispatcher 深层缺陷 | P2 | `src/platform/execution/lease/execution-lease-service.ts:797` | getLatestFencingToken fallback 返回 0——任何 token 校验均通过 | 已解决（源码复核并补定向测试） | 审计命中的是真缺陷的旧形态：早期写校验路径确实依赖 helper fallback，helper 缺失时可能把 authoritative token 误判成 `0`。现行 `validateWriteAccess()` 已不再从该 helper 取校验基准，而是直接读取最新 lease 记录，因此 helper fallback 不会让任意 token 通过。 | 当前 `src/platform/execution/lease/execution-lease-service.ts` 的 `validateWriteAccess()` 先调用 `getLatestExecutionLease()` 得到 `latestLease`，随后以 `latestLease.fencingToken` 作为 `authoritativeFencingToken`，并对 active lease 的 `fencingToken` 做严格相等校验；该校验路径没有再调用底部的 `getLatestFencingToken()` helper。 | 本轮新增并通过 `tests/unit/platform/execution/lease/execution-lease-service.test.ts` 的 `validateWriteAccess rejects stale tokens even when worker store omits getLatestFencingToken helper`，在故意删除 helper 的情况下仍验证 stale token 会被拒绝，authoritative token 保持为最新 lease 的 `7`。 |
| 2484 | Round 42 — 深度审计（compliance / HA-execution / UI-apps / contracts-implementation 交叉验证） / §209 HA / Lease / Recovery / Worker-Pool / Dispatcher 深层缺陷 | P2 | `src/platform/execution/ha/lease-reclaimer-service.ts:406` | getStaleNodes 硬编码返回空数组——心跳超时节点永不检测 | 已解决（源码修复并补定向测试） | 根因是旧 `LeaseReclaimerService` 只处理“已落到 lease 过期表象”的节点，没有任何 heartbeat 维度的 stale-node 扫描；即便仓内 `HaRepository` 早就有 stale 查询契约，reclaimer 本体仍直接 `return []`，导致超时节点永远进不了回收/故障转移链。 | 本轮为 `LeaseReclaimerConfig` 补入 `staleNodeThresholdMs`，并在 `src/platform/execution/ha/lease-reclaimer-service.ts` 中按 `lastHeartbeatAt` 过滤 `listNodes("active")`，将超时节点纳入 reclaim cycle。`tests/unit/platform/execution/ha/lease-reclaimer-service.test.ts` 已新增 “stale node detection reclaims leader node that missed heartbeats” 用例，验证 stale leader 会被识别并计入 `reclaimedCount`。 | 已改生产代码与最小定向测试。 |
| 2485 | Round 42 — 深度审计（compliance / HA-execution / UI-apps / contracts-implementation 交叉验证） / §210 UI Apps (Electron/Tauri/Mobile) + Features (HITL/Approval/Cockpit) 缺陷 | P0 | `ui/apps/tauri-macos/src-tauri/src/lib.rs:12` | run_shell 任意字符串无校验——远程代码执行 | 已解决 | 根因是早期 Tauri macOS 壳层把 renderer 传入的 `command` 当作可信输入直接执行，没有任何命令 allowlist 或 fail-closed 拦截；当前实现已把 `run_shell` 收敛成固定诊断命令白名单，但 reaudit 文档没同步。 | 当前 `ui/apps/tauri-macos/src-tauri/src/lib.rs` 已声明 `ALLOWED_COMMANDS = ["status", "health", "version"]`，并在 `run_shell()` 中先执行 `if !is_command_allowed(&command) { return Err(...) }`；本轮新增 `ui/tests/unit/ui/apps/tauri-shell-security.test.ts` 已验证 macOS 壳层源码中存在该 allowlist 与拒绝分支。 | 回写真实根因、源码依据与最小壳层安全回归证据。 |
| 2486 | Round 42 — 深度审计（compliance / HA-execution / UI-apps / contracts-implementation 交叉验证） / §210 UI Apps (Electron/Tauri/Mobile) + Features (HITL/Approval/Cockpit) 缺陷 | P0 | `ui/apps/tauri-linux/src-tauri/src/lib.rs:12` | 同上 Linux 端无输入过滤 | 已解决 | 根因与 macOS 条目相同：旧 Linux Tauri shell 同样把任意 `command` 字符串当作可执行输入，没有校验或白名单；现行实现已收敛为固定命令 allowlist。 | 当前 `ui/apps/tauri-linux/src-tauri/src/lib.rs` 已声明同样的 `ALLOWED_COMMANDS = ["status", "health", "version"]`，并在 `run_shell()` 中拒绝非 allowlist 命令；本轮新增 `ui/tests/unit/ui/apps/tauri-shell-security.test.ts` 已验证 Linux 壳层源码中同样存在 allowlist 与拒绝分支。 | 回写真实根因、源码依据与最小壳层安全回归证据。 |
| 2487 | Round 42 — 深度审计（compliance / HA-execution / UI-apps / contracts-implementation 交叉验证） / §210 UI Apps (Electron/Tauri/Mobile) + Features (HITL/Approval/Cockpit) 缺陷 | P0 | `ui/apps/electron-win/src/preload.ts:33` | 直接赋值 window.**AA_ELECTRON** 绕过 contextBridge——安全模式失效 | 已解决 | 根因是旧 preload 暴露链把 bridge 直接挂到 `window`，renderer 可绕过 `contextIsolation` 拿到原始对象；后续代码已切到 `contextBridge.exposeInMainWorld(...)`，但 reaudit 文档与回归基线一直停留在旧漏洞描述。 | 当前 `ui/apps/electron-win/src/preload.ts` 已通过 `resolveContextBridge()?.exposeInMainWorld("AA_ELECTRON", bridge)` 暴露桥接，不再直接写入 `window`；本轮 `ui/tests/unit/ui/apps/electron-win/preload.test.ts` 已验证会调用 `exposeInMainWorld`，且 `targetWindow` 上不存在直接属性注入。 | 回写真实根因、源码依据与最小壳层安全回归证据。 |
| 2488 | Round 42 — 深度审计（compliance / HA-execution / UI-apps / contracts-implementation 交叉验证） / §210 UI Apps (Electron/Tauri/Mobile) + Features (HITL/Approval/Cockpit) 缺陷 | P0 | `ui/apps/electron-win/index.html:1` | 缺 CSP meta 标签——违反§6.5.4 安全基线 | 已解决 | 根因不是 Electron shell 仍缺浏览器安全基线，而是历史审计命中了未补 CSP 的早期 HTML 模板；当前 `index.html` 已带 CSP / `X-Content-Type-Options` / `X-Frame-Options`，只是 reaudit 没同步。 | 当前 `ui/apps/electron-win/index.html` 已声明 `Content-Security-Policy` 且包含 `default-src 'self'`、`frame-ancestors 'none'`；本轮新增 `ui/tests/unit/ui/apps/security-configs.test.ts` 已验证 Electron shell HTML 中存在该 CSP 基线。 | 回写现行源码与最小静态配置回归依据。 |
| 2489 | Round 42 — 深度审计（compliance / HA-execution / UI-apps / contracts-implementation 交叉验证） / §210 UI Apps (Electron/Tauri/Mobile) + Features (HITL/Approval/Cockpit) 缺陷 | P0 | `ui/apps/tauri-macos/src-tauri/tauri.conf.json:1` | 缺 security 段/CSP/allowlist——默认开放全部 IPC | 已解决 | 根因是审计基于旧的 Tauri macOS 壳层配置快照；当前配置已经显式声明 `security.csp`，并保持 `dangerousDisableAssetCspModification=false`，不再是“默认开放全部 IPC”的裸配置。 | 当前 `ui/apps/tauri-macos/src-tauri/tauri.conf.json` 已包含 `security.csp` 与 `dangerousDisableAssetCspModification: false`；本轮新增 `ui/tests/unit/ui/apps/security-configs.test.ts` 已验证该安全段真实存在。 | 回写真实现状与最小配置回归依据。 |
| 2490 | Round 42 — 深度审计（compliance / HA-execution / UI-apps / contracts-implementation 交叉验证） / §210 UI Apps (Electron/Tauri/Mobile) + Features (HITL/Approval/Cockpit) 缺陷 | P0 | `ui/apps/tauri-linux/src-tauri/tauri.conf.json:1` | 同上 Linux Tauri 配置缺 security | 已解决 | 根因与 macOS 条目相同：reaudit 仍沿用未补安全段的旧 Linux Tauri 配置描述，而现行 `tauri.conf.json` 已明确写入 `security.csp` 和资产 CSP 保护开关。 | 当前 `ui/apps/tauri-linux/src-tauri/tauri.conf.json` 已包含 `security.csp` 与 `dangerousDisableAssetCspModification: false`；本轮新增 `ui/tests/unit/ui/apps/security-configs.test.ts` 已验证 Linux 配置同样具备该安全段。 | 回写真实现状与最小配置回归依据。 |
| 2491 | Round 42 — 深度审计（compliance / HA-execution / UI-apps / contracts-implementation 交叉验证） / §210 UI Apps (Electron/Tauri/Mobile) + Features (HITL/Approval/Cockpit) 缺陷 | P1 | `ui/packages/features/hitl/src/hooks/index.ts:5` | HITL 缺 Patch+Override 能力——仅实现 Inspect/Takeover/Resume (3/5) | 已解决 | 根因是旧 HITL VM 虽然已有 patch/override 表单，但 `patch()` / `override()` 只有 `console.info` stub，没有默认 client 注入，也没有把修改请求提交到现有审批 decision API，因此动作一直停在 UI 层。 | 当前 `ui/packages/features/hitl/src/hooks/index.ts` 已默认接入 shared-state 的 `useRestClient()/useWsClient()`，并在 `patch()` / `override()` 中调用 shared API helper `submitApprovalTextInput(...)`；`ui/packages/shared/api-client/src/endpoints.ts` 也新增了该 helper，并通过现有 `/approvals/:approvalId/decision` 入口以 `decisionType: "text_input"` + `inputText` 提交 patch/override payload，不再只是本地日志。 | 本轮复跑并通过 `ui/tests/unit/ui/packages/features/hitl/hooks.test.tsx`（`--environment jsdom`）中的 `submits patch actions through the approval decision API and removes the item` 与 `submits override actions through the approval decision API and removes the item`。 |
| 2492 | Round 42 — 深度审计（compliance / HA-execution / UI-apps / contracts-implementation 交叉验证） / §210 UI Apps (Electron/Tauri/Mobile) + Features (HITL/Approval/Cockpit) 缺陷 | P1 | `ui/packages/features/hitl/src/web/index.tsx:9` | HITL 仅渲染静态 ListCard——无实际交互表单/按钮 | 已解决 | 根因是审计命中了只有静态 `ListCard` 的早期 web view；现行 `HitlWebView` 已经提供 approve/reject/resume 按钮，以及 patch/override JSON 编辑器，但 reaudit 文档还没同步。 | 当前 `ui/packages/features/hitl/src/web/index.tsx` 已渲染动作按钮和 patch/override 编辑表单；本轮新增 `ui/tests/unit/ui/packages/features/hitl/web.test.tsx` 已验证这些按钮会触发对应 VM action，并能提交 patch/override JSON。 | 回写真实现状与最小 UI 回归证据。 |
| 2493 | Round 42 — 深度审计（compliance / HA-execution / UI-apps / contracts-implementation 交叉验证） / §210 UI Apps (Electron/Tauri/Mobile) + Features (HITL/Approval/Cockpit) 缺陷 | P1 | `ui/packages/features/approval/src/hooks/index.ts:13` | 缺 request_more_context 动作——最小动作集不完整 | 已解决 | 根因是旧 approval center VM 的最小动作集只覆盖 approve/reject/delegate，没有把 `request_more_context` 暴露为真正的 hook action；后续实现已经接入 API helper，但 reaudit 文档没同步。 | 当前 `ui/packages/features/approval/src/hooks/index.ts` 已暴露 `requestMoreContext()`，并调用 `requestMoreContextApproval(client, approvalId)`；本轮扩展 `ui/tests/unit/ui/packages/features/approval/hooks.test.tsx`，已验证该动作会命中 API helper 并写入 action history。 | 回写真实根因、源码依据与最小 hook 回归证据。 |
| 2494 | Round 42 — 深度审计（compliance / HA-execution / UI-apps / contracts-implementation 交叉验证） / §210 UI Apps (Electron/Tauri/Mobile) + Features (HITL/Approval/Cockpit) 缺陷 | P1 | `ui/packages/features/approval/src/web/index.tsx:33` | 缺 deadline 倒计时/policy_source/recommended_option | 已解决 | 根因是 audit 基于旧版 approval web view，当时 detail pane 还没把 `deadline / policySource / recommendedOption` 这些治理字段展示出来；当前实现已经补齐，但文档和测试基线未更新。 | 当前 `ui/packages/features/approval/src/web/index.tsx` 已渲染 deadline 倒计时、`Policy Source` 和 `Recommended Option`；本轮新增 `ui/tests/unit/ui/packages/features/approval/web.test.tsx` 已验证这些字段会出现在 detail pane 中。 | 回写真实现状与最小 UI 回归证据。 |
| 2495 | Round 42 — 深度审计（compliance / HA-execution / UI-apps / contracts-implementation 交叉验证） / §210 UI Apps (Electron/Tauri/Mobile) + Features (HITL/Approval/Cockpit) 缺陷 | P1 | `ui/packages/features/workflow-cockpit/src/hooks/index.ts:59` | pause/resume/recover 仅修改本地 state——未调用 REST API | 已解决 | 根因是早期 workflow cockpit action 只更新本地 VM 状态；后续实现已经把 `pause/resume/recover/release` 全部接到 shared API client，但 reaudit 文档仍停留在旧结论。 | 当前 `ui/packages/features/workflow-cockpit/src/hooks/index.ts` 的 `pauseWorkflow()/resumeWorkflow()/recoverWorkflow()/releaseWorkflow()` 已分别调用 `pauseWorkflow()/resumeWorkflow()/recoverWorkflow()/releaseWorkflow()` API helper；本轮新增 `ui/tests/unit/ui/packages/features/workflow-cockpit/hooks.test.tsx` 已验证这些动作都会命中后端 helper。 | 回写真实根因、源码依据与最小 hook 回归证据。 |
| 2496 | Round 42 — 深度审计（compliance / HA-execution / UI-apps / contracts-implementation 交叉验证） / §210 UI Apps (Electron/Tauri/Mobile) + Features (HITL/Approval/Cockpit) 缺陷 | P1 | `ui/packages/features/task-cockpit/src/hooks/index.ts:58` | claim/resume/escalate 同理——仅本地变更无 API 调用 | 已解决 | 根因是旧 task cockpit action 只变更本地 state；当前实现已经把 `claim/resume/escalate` 全部改为先调用 `updateTask()`，再同步 UI 状态。 | 当前 `ui/packages/features/task-cockpit/src/hooks/index.ts` 的 `claimTask()/resumeTask()/escalateTask()` 都会调用 shared API `updateTask()`；本轮新增 `ui/tests/unit/ui/packages/features/task-cockpit/hooks.test.tsx` 已验证这些动作会向后端 helper 发送对应 payload。 | 回写真实根因、源码依据与最小 hook 回归证据。 |
| 2497 | Round 42 — 深度审计（compliance / HA-execution / UI-apps / contracts-implementation 交叉验证） / §210 UI Apps (Electron/Tauri/Mobile) + Features (HITL/Approval/Cockpit) 缺陷 | P1 | `ui/packages/features/workflow-cockpit/src/web/index.tsx:34` | 缺 DAGViewer 组件——§4.2.3 L2 必需 | 已解决 | 根因是早期 workflow cockpit 只有静态列表，没有单独的 DAG 轨道组件；现行 web view 已经引入 `DAGViewer`，但 reaudit 文档未同步。 | 当前 `ui/packages/features/workflow-cockpit/src/web/index.tsx` 已渲染 `DAGViewer`，`ui/packages/features/workflow-cockpit/src/web/dag-viewer.tsx` 也已存在独立组件；本轮新增 `ui/tests/unit/ui/packages/features/workflow-cockpit/web.test.tsx` 已验证 cockpit 会渲染 DAG viewer。 | 回写真实现状与最小 UI 回归证据。 |
| 2498 | Round 42 — 深度审计（compliance / HA-execution / UI-apps / contracts-implementation 交叉验证） / §210 UI Apps (Electron/Tauri/Mobile) + Features (HITL/Approval/Cockpit) 缺陷 | P1 | `ui/packages/features/workflow-cockpit/src/hooks/index.ts:5` | 缺 approval_nodes/evidence_refs 字段 | 已解决 | 根因不是 shared contract 仍缺这两个字段，而是 workflow cockpit 没有把 `selectedWorkflow.approvalNodes / evidenceRefs` 消费到可见 UI 上，导致审计视角等价于“前端没有这两个字段”。 | 当前 `ui/packages/shared/types/src/index.ts` 的 `WorkflowDTO` 已包含 `approvalNodes / evidenceRefs`；本轮进一步在 `ui/packages/features/workflow-cockpit/src/web/index.tsx` 中把这两类数据的计数和列表渲染出来，并由 `ui/tests/unit/ui/packages/features/workflow-cockpit/web.test.tsx` 验证显示。 | 回写真实根因、源码补丁与最小 UI 回归证据。 |
| 2499 | Round 42 — 深度审计（compliance / HA-execution / UI-apps / contracts-implementation 交叉验证） / §210 UI Apps (Electron/Tauri/Mobile) + Features (HITL/Approval/Cockpit) 缺陷 | P1 | `ui/apps/mobile/src/App.tsx:5` | 硬编码 "android"——iOS 设备获得错误平台标识 | 已解决 | 根因是早期 mobile app 在 native bridge 不可用时直接把平台回退写死为 `"android"`，没有先从 `navigator.userAgent` 判断 iPhone/iPad/iOS，导致 iOS 设备会拿到错误 adapter 标识。 | 当前 `ui/apps/mobile/src/App.tsx` 已新增 `detectMobilePlatform()`，会先识别 iOS UA，再识别 Android，最后才回退到 Android；本轮回写并修正 `ui/tests/unit/ui/apps/mobile/App.test.tsx`，已验证 Android UA 映射到 `android`，iOS UA 映射到 `ios`。 | 回写真实根因、源码依据与最小 UI 回归证据。 |
| 2500 | Round 42 — 深度审计（compliance / HA-execution / UI-apps / contracts-implementation 交叉验证） / §210 UI Apps (Electron/Tauri/Mobile) + Features (HITL/Approval/Cockpit) 缺陷 | P1 | `ui/apps/mobile/src/navigation.ts:8` | 移动端缺 workflow-cockpit 入口 | 已解决 | 根因是审计命中了缺少 workflow cockpit drill-down 入口的旧导航版本；当前 mobile tabs 已经把工作流座舱入口补入，但 reaudit 文档与回归基线没有同步。 | 当前 `ui/apps/mobile/src/navigation.ts` 的 `mobileNavigation.tabs` 已包含 `{ id: "workflow-cockpit", path: "/mission-control/workflows/:id" }`；本轮补充 `ui/tests/unit/ui/apps/mobile/navigation.test.ts` 并验证该入口存在。 | 回写真实现状与定向测试依据。 |
| 2501 | Round 42 — 深度审计（compliance / HA-execution / UI-apps / contracts-implementation 交叉验证） / §210 UI Apps (Electron/Tauri/Mobile) + Features (HITL/Approval/Cockpit) 缺陷 | P2 | `ui/packages/features/workflow-cockpit/src/hooks/index.ts:30` | useWorkflowsQuery + useState 双持——缓存一致性偏差 | 已解决（源码修复并补定向测试） | 根因是旧 hook 把 `useWorkflowsQuery().data` 整份复制进本地 `useState(workflows)`，随后动作流再改本地副本，导致 React Query 缓存与 VM 影子状态双持；一旦后端刷新或其他组件写缓存，cockpit 视图就可能继续展示过期副本。 | 本轮已把 `queryWorkflows` 恢复为唯一主数据源：`ui/packages/features/workflow-cockpit/src/hooks/index.ts` 现在只保留按 workflowId 粒度的 `workflowOverrides` optimistic overlay，并在动作成功后 `invalidateQueries({ queryKey: ["workflows"] })` 回收权威状态。`ui/tests/unit/ui/packages/features/workflow-cockpit/hooks.test.tsx` 已验证动作后会触发 query invalidation，且上游 query 数据变化会直接反映到 `listItems`，不再依赖整份本地镜像。 | 已改生产代码与最小 hook 回归测试。 |
| 2502 | Round 42 — 深度审计（compliance / HA-execution / UI-apps / contracts-implementation 交叉验证） / §210 UI Apps (Electron/Tauri/Mobile) + Features (HITL/Approval/Cockpit) 缺陷 | P2 | `ui/packages/features/task-cockpit/src/web/index.tsx:12` | 缺 L3-L5 下钻子路由 | 已解决 | 根因是审计针对的是只停留在 L1/L2 列表与摘要的旧 task cockpit；现行 web view 已经补出 L3 Step、L4 Evidence、L5 Timeline 三个 drill-down 视图。 | 当前 `ui/packages/features/task-cockpit/src/web/index.tsx` 已渲染 `L3 Steps / L4 Evidence / L5 Timeline` 切换按钮和对应 viewer；本轮新增 `ui/tests/unit/ui/packages/features/task-cockpit/web.test.tsx` 已验证三层下钻内容都可切换显示。 | 回写真实现状与最小 UI 回归证据。 |
| 2503 | Round 42 — 深度审计（compliance / HA-execution / UI-apps / contracts-implementation 交叉验证） / §210 UI Apps (Electron/Tauri/Mobile) + Features (HITL/Approval/Cockpit) 缺陷 | P2 | `ui/packages/features/approval/src/hooks/index.ts:80` | approve/reject 仅本地移除——未调用后端 POST decisions | 已解决 | 根因是旧 `useApprovalCenterVm()` 的动作流只维护前端 queue state，没有把 approve/reject 真正发到 approval API；后续实现已补接 `approveApproval()/rejectApproval()`，但文档还停留在旧描述。 | 当前 `ui/packages/features/approval/src/hooks/index.ts` 的 `approve()`/`reject()` 已分别调用 `approveApproval(client, approvalId)` 与 `rejectApproval(client, approvalId)`，之后才更新本地 state；本轮新增 `ui/tests/unit/ui/packages/features/approval/hooks.test.tsx` 已验证这两个动作都会命中 API helper。 | 回写真实根因、源码依据与最小 hook 回归证据。 |
| 2504 | Round 42 — 深度审计（compliance / HA-execution / UI-apps / contracts-implementation 交叉验证） / §210 UI Apps (Electron/Tauri/Mobile) + Features (HITL/Approval/Cockpit) 缺陷 | P2 | `ui/apps/electron-win/src/main.ts:15` | IPC shell:run/shell:spawn 无命令白名单 | 已解决 | 根因不是“仅缺一个白名单判断”，而是旧 Electron shell 直接把 `shell:run/shell:spawn` 当 renderer 可达 IPC 能力暴露出去；现行安全基线已经改为从 channel allowlist 中彻底移除这两条危险 IPC，只保留 `shell:openExternal`，但 reaudit 文档与旧测试仍停留在漏洞时期。 | 当前 `ui/apps/electron-win/src/main.ts` 的 `electronMainBaseline.channels` 已不包含 `shell:run/shell:spawn`，`electronBridgeCapabilities` 也不再宣称 `shell/process` bridge 可用；`ui/apps/electron-win/src/preload.ts` 的 `electronPreloadApi.shell` 只保留 `openExternal`。本轮 `ui/tests/unit/ui/apps/electron-win/main.test.ts` 与 `preload.test.ts` 已验证危险 IPC 不再暴露。 | 回写真实根因、源码依据与最小壳层安全回归证据。 |
| 2505 | Round 42 — 深度审计（compliance / HA-execution / UI-apps / contracts-implementation 交叉验证） / §210 UI Apps (Electron/Tauri/Mobile) + Features (HITL/Approval/Cockpit) 缺陷 | P2 | `ui/packages/features/approval/src/mobile/index.ts:4` | 移动端审批无交互——规格要求通知栏快捷操作 | 已解决 | 根因是早期 mobile approval cards 只有静态标题和描述，没有把 approve/reject 绑定到真实快捷操作；移动端通知卡片可展示但不可执行审批。 | 当前 `ui/packages/features/approval/src/mobile/index.ts` 的 `createApprovalMobileCards()` 已为 approve/reject 卡片注入 `onApprove/onReject`，并直接调用 `approveApproval()/rejectApproval()`；本轮新增 `ui/tests/unit/ui/packages/features/approval/mobile.test.ts` 已验证快捷操作会触发 API helper，且只保留前 5 个 source approvals。 | 回写真实根因、源码依据与最小 UI 回归证据。 |
| 2506 | Round 42 — 深度审计（compliance / HA-execution / UI-apps / contracts-implementation 交叉验证） / §210 UI Apps (Electron/Tauri/Mobile) + Features (HITL/Approval/Cockpit) 缺陷 | P2 | `ui/packages/features/hitl/src/mobile/index.ts:3` | HITL 移动端完全不可用 | 已解决 | 根因不是 mobile feature 完全缺失，而是早期 `createHitlMobileCards()` 只生成静态展示卡片，没有把 approve/reject/resume 等 quick action handler 暴露给移动端卡片，导致移动通知/卡片虽然能显示但不能交互。 | 当前 `ui/packages/features/hitl/src/mobile/index.ts` 的 `createHitlMobileCards()` 已支持注入 `onApprove / onReject / onResume`，并把 handler 绑定到 approval/resume 卡片；本轮新增 `ui/tests/unit/ui/packages/features/hitl/mobile.test.ts` 已验证移动卡片会正确调用这些 handler，且仍保留最多 5 个 source items 的移动队列限制。 | 回写真实根因、源码依据与最小 UI 回归证据。 |
| 2507 | Round 42 — 深度审计（compliance / HA-execution / UI-apps / contracts-implementation 交叉验证） / §210 UI Apps (Electron/Tauri/Mobile) + Features (HITL/Approval/Cockpit) 缺陷 | P2 | `ui/apps/mobile/src/navigation.ts:14` | settings 缺子路由——无法导航到 7 个配置子页面 | 已解决 | 根因是审计命中的是没有 settings sub-routes 的旧导航定义；现行 mobile navigation 已补出 7 个配置子页面，但 reaudit 文档和单测基线没有同步回写。 | 当前 `ui/apps/mobile/src/navigation.ts` 已导出 `settingsSubRoutes`，并在 `mobileNavigation.settingsSubRoutes` 中挂入 `profile / notifications / security / appearance / language / about / advanced` 七个子路由；本轮补充 `ui/tests/unit/ui/apps/mobile/navigation.test.ts` 回归并验证这 7 个子路由都存在且都位于 `/shared/settings/*` 名称空间下。 | 回写真实现状与定向 UI 测试依据。 |
| 2508 | Round 42 — 深度审计（compliance / HA-execution / UI-apps / contracts-implementation 交叉验证） / §211 Contracts-Implementation 交叉验证（OAPEFLIR / EventBus / NL-Gateway / Goal-Decomposer） | P0 | `src/platform/contracts/types/domain/session-types.ts:237` | EventRecord 缺 sequence/causationId/correlationId/payloadHash/idempotencyKey | 已解决 | 根因是当时 contract 类型与 SQLite event persistence 已发生阶段性脱节：旧审计命中了缺字段时期的 `EventRecord` 视图，但后续迁移已把 replay/audit 字段补进 schema、repository 和 type export，只是 reaudit 文档还没回写。 | 当前 `src/platform/contracts/types/domain/session-types.ts` 的 `EventRecord` 已包含 `sequence / causationId / correlationId / payloadHash / idempotencyKey`，`src/platform/state-evidence/truth/sqlite/repositories/event-repository.ts` 也已持久化这些列；本轮新增 `tests/unit/platform/state-evidence/events/review-contract-regressions.test.ts` 已验证 `DurableEventBus.publish()` 返回的 `EventRecord` 公开这些 replay 字段。 | 回写为已解决，并补充 contract + persistence + 定向测试依据。 |
| 2509 | Round 42 — 深度审计（compliance / HA-execution / UI-apps / contracts-implementation 交叉验证） / §211 Contracts-Implementation 交叉验证（OAPEFLIR / EventBus / NL-Gateway / Goal-Decomposer） | P0 | `src/platform/state-evidence/events/durable-event-bus.ts:358` | dead-letter 仅内存 Map 非独立 DLQ 表——违反"Event 不允许物理删除" | 已解决 | 根因是早期实现只在 ack 状态层面标记失败，独立 `event_dead_letters` 存储和 bus 侧持久化路径后来补上了，但 reaudit 条目仍沿用“仅内存 Map”旧描述。 | 当前 `src/platform/state-evidence/events/durable-event-bus.ts` 在 dead-letter 分支里同事务执行 `markEventDeadLettered()` 与 `insertEventDeadLetter()`；`src/platform/state-evidence/truth/sqlite/sqlite-migration-runtime-part3.ts` 已创建 `event_dead_letters` 表。本轮新增 `tests/unit/platform/state-evidence/events/review-contract-regressions.test.ts` 已验证失败事件会落入独立 DLQ 表。 | 回写真实现状与定向测试依据。 |
| 2510 | Round 42 — 深度审计（compliance / HA-execution / UI-apps / contracts-implementation 交叉验证） / §211 Contracts-Implementation 交叉验证（OAPEFLIR / EventBus / NL-Gateway / Goal-Decomposer） | P0 | `src/platform/state-evidence/events/durable-event-bus.ts:178` | publish() 未维护 run 内单调递增 sequence——违反§14.3 第 3 条 | 已解决 | 根因是审计依据停留在 `publish()` 尚未自动分配 run-local sequence 的旧版本；当前 bus 已为同一 `runId` 建立单调递增 sequence 分配，但 reaudit 文档未同步。 | 当前 `src/platform/state-evidence/events/durable-event-bus.ts` 的 `publish()`/`publishBatch()` 都会在提供 `runId` 且未显式传 `sequence` 时调用 `getNextRunSequence()`；本轮新增 `tests/unit/platform/state-evidence/events/review-contract-regressions.test.ts` 已验证同一 `runId` 连续发布得到 `sequence = 1/2`。 | 回写为已解决，并注明剩余更强的多实例序列协调不属于本条原始缺陷描述。 |
| 2511 | Round 42 — 深度审计（compliance / HA-execution / UI-apps / contracts-implementation 交叉验证） / §211 Contracts-Implementation 交叉验证（OAPEFLIR / EventBus / NL-Gateway / Goal-Decomposer） | P1 | `src/platform/orchestration/oapeflir/stage-timeline.ts:3` | 含 knowledge_promotion 第 9 阶段——spec 定义 OAPEFLIR 仅八阶段 | 已解决 | 根因不是现行实现仍有 9 阶段，而是历史测试与审计基线残留了退场的 `knowledge_promotion` 阶段表述；源码本体已经切回 canonical 八阶段。 | 当前 `src/platform/orchestration/oapeflir/stage-timeline.ts` 的 `OapeflirStageSchema` 仅包含 `observe/assess/plan/execute/feedback/learn/improve/release` 八阶段；本轮同步修正 `tests/unit/platform/orchestration/oapeflir/stage-timeline-extended.test.ts` 的历史断言，并已通过定向回归。 | 回写为已解决，并把根因收敛为“测试/文档基线漂移”。 |
| 2512 | Round 42 — 深度审计（compliance / HA-execution / UI-apps / contracts-implementation 交叉验证） / §211 Contracts-Implementation 交叉验证（OAPEFLIR / EventBus / NL-Gateway / Goal-Decomposer） | P1 | `src/platform/orchestration/oapeflir/oapeflir-loop-service.ts:52` | 使用 PlannedWorkflow 而非 PlanGraphBundle——Plan 为线性 steps | 已解决 | 根因不是 loop service 仍只输出线性 `Plan`，而是 canonical `PlanGraphBundle` 已迁入 Plan 阶段和结果对象，但 reaudit 条目仍沿用迁移前的线性 plan 描述。 | 当前 `src/platform/orchestration/oapeflir/oapeflir-loop-service.ts` 已在 `OapeflirLoopResult` 中公开 `planGraphBundle: PlanGraphBundle`，Plan 阶段通过 `planBuilder.buildGraphBundle()` 生成 graph contract；本轮 `tests/unit/platform/orchestration/oapeflir/review-regressions.test.ts` 已继续验证 loop result 可直接消费 `planGraphBundle`。 | 回写真实 contract 现状与定向测试依据。 |
| 2513 | Round 42 — 深度审计（compliance / HA-execution / UI-apps / contracts-implementation 交叉验证） / §211 Contracts-Implementation 交叉验证（OAPEFLIR / EventBus / NL-Gateway / Goal-Decomposer） | P1 | `src/platform/orchestration/oapeflir/types/plan.ts:32` | PlanSchema 仅 steps[]——缺 nodes/edges/entryNodeIds/graphConstraints | 已解决 | 根因是 `PlanSchema` 已在后续迭代补入 graph-native 字段，但 reaudit 条目仍引用早期只含 `steps[]` 的版本。 | 当前 `src/platform/orchestration/oapeflir/types/plan.ts` 已定义 `nodes / edges / entryNodeIds / graphConstraints`；本轮新增 `tests/unit/platform/orchestration/oapeflir/plan-schema-review-regressions.test.ts` 已验证这些字段可被 schema 正常解析。 | 回写为已解决，并补定向 schema 回归依据。 |
| 2514 | Round 42 — 深度审计（compliance / HA-execution / UI-apps / contracts-implementation 交叉验证） / §211 Contracts-Implementation 交叉验证（OAPEFLIR / EventBus / NL-Gateway / Goal-Decomposer） | P1 | `src/platform/orchestration/oapeflir/oapeflir-loop-service.ts:62` | 缺 normalizationReport/validationReport/riskPropagation/worstPath | 已解决 | 根因是 `OapeflirLoopResult` 类型层已经声明了 plan diagnostics 字段，但旧实现只在 Plan 阶段局部构造了部分 report，最终返回对象没有稳定暴露 `riskPropagation/worstPath`，`normalizationReport/validationReport` 也未形成统一 result contract。 | 当前 `src/platform/orchestration/oapeflir/oapeflir-loop-service.ts` 已把 `normalizationReport / validationReport / riskPropagation / worstPath` 统一挂到 `OapeflirLoopResult` 返回值；本轮新增 `tests/unit/platform/orchestration/oapeflir/review-regressions.test.ts` 已验证 loop result 能返回完整诊断字段。 | 回写真实根因、源码修复与定向测试依据。 |
| 2515 | Round 42 — 深度审计（compliance / HA-execution / UI-apps / contracts-implementation 交叉验证） / §211 Contracts-Implementation 交叉验证（OAPEFLIR / EventBus / NL-Gateway / Goal-Decomposer） | P1 | `src/platform/orchestration/oapeflir/stage-transition-fsm.ts:53` | FSM 严格线性——不支持 retry_wait→ready 等非线性迁移 | 已解决 | 根因是审计把旧的线性阶段观感当成现状，但当前 FSM 已支持反馈驱动的回跳与重规划，不再是“只能单向前进”的严格线性机。 | 当前 `src/platform/orchestration/oapeflir/stage-transition-fsm.ts` 已允许 `feedback/learn/improve/release -> plan/assess/execute` 等非线性回退迁移，并对 `feedback -> observe` 提供 closed-loop 支持；仓内现有 `tests/unit/platform/orchestration/oapeflir/stage-transition-fsm-extended.test.ts`、`stage-transition-fsm-integration.test.ts` 已覆盖这些回退场景。 | 回写为已解决，并将问题收敛为“旧审计描述过时”。 |
| 2516 | Round 42 — 深度审计（compliance / HA-execution / UI-apps / contracts-implementation 交叉验证） / §211 Contracts-Implementation 交叉验证（OAPEFLIR / EventBus / NL-Gateway / Goal-Decomposer） | P1 | `src/platform/state-evidence/events/event-registry.ts:63` | 事件命名 task:/workflow: 与 spec platform.harness*run.*/node*run.* 完全不匹配 | 已解决 | 根因不是 runtime registry 仍缺 canonical `platform.*`/`oapeflir.*` 命名，而是 event registry 为兼容 legacy consumers 同时保留了老 namespace，审计把“仍保留 legacy”误写成了“没有 canonical”。 | 当前 `src/platform/state-evidence/events/event-registry.ts` 已注册 `platform.harness_run.* / platform.node_run.* / platform.side_effect.* / platform.budget_* / oapeflir.*` 等 canonical schema；本轮已有 `tests/unit/platform/five-plane-state-evidence/events/review-regressions.test.ts` 验证 canonical tier-1 registry 与 payload map 覆盖，并验证退场 namespace 不再作为当前 canonical schema。 | 回写真实根因与现行 registry 证据。 |
| 2517 | Round 42 — 深度审计（compliance / HA-execution / UI-apps / contracts-implementation 交叉验证） / §211 Contracts-Implementation 交叉验证（OAPEFLIR / EventBus / NL-Gateway / Goal-Decomposer） | P1 | `src/platform/state-evidence/events/durable-event-bus.ts:197` | bus 层未强制 event append 与 truth update 同事务 | 需继续整改（责任边界未统一） | 这条的真实根因不是“event bus 完全没有事务能力”，而是职责边界仍然分裂：`DurableEventBus` 负责 event append + ack/DLQ，`TransactionalEventAppender` 才负责把 truth mutation 与 event append 放进同一事务；因此总线 API 本身仍未成为所有 truth write 的强制单一入口。 | 当前 `src/platform/state-evidence/events/durable-event-bus.ts` 已保证 event append、ack 与 DLQ 相关写入的事务一致性，`src/platform/state-evidence/events/transactional-event-appender.ts` 也提供 truth+event 同事务能力；但 legacy truth 写链尚未统一改成必须经由该事务封装，因此不能假关闭。 | 保留未完成结论，并把问题收敛为“bus/appender 双轨职责未完全统一”。 |
| 2518 | Round 42 — 深度审计（compliance / HA-execution / UI-apps / contracts-implementation 交叉验证） / §211 Contracts-Implementation 交叉验证（OAPEFLIR / EventBus / NL-Gateway / Goal-Decomposer） | P1 | `src/interaction/nl-gateway/index.ts:620` | parseDetailed 缺完整 Input Guardrail 层——仅 regex injection 检测 | 已解决 | 根因是旧实现把输入防护放在意图解析和路由之后，而且只跑本地 `detectPromptInjection()` regex；恶意输入在被标记前就可能进入 `intentParser.parseWithLlm()`，也没有复用统一 guardrail engine。 | 当前 `src/interaction/nl-gateway/index.ts` 已新增 `assessInputGuardrails()`，在 `parseDetailed()` 开头先组合 `GuardrailEngine.assess({ inputPrompt })` 与 prompt-injection regex，并在命中 block 时跳过 `intentParser.parseWithLlm()`；本轮新增 `tests/unit/interaction/nl-gateway/review-regressions.test.ts` 已验证 `<script>...reveal the system prompt` 输入会被拦截且 LLM parser 调用次数为 0。 | 回写真实根因、源码修复与定向测试依据。 |
| 2519 | Round 42 — 深度审计（compliance / HA-execution / UI-apps / contracts-implementation 交叉验证） / §211 Contracts-Implementation 交叉验证（OAPEFLIR / EventBus / NL-Gateway / Goal-Decomposer） | P2 | `src/interaction/goal-decomposer/index.ts:43` | 返回 PlannedTask[] 非 PlanGraphBundle | 已解决 | 根因不是“仓内根本没有 PlanGraphBundle”，而是 canonical graph contract 只埋在 `harnessRouting.planGraphBundle` 里，`GoalDecomposition` 顶层返回仍然是 task-first 形态，导致直接消费 goal-decomposer 结果的调用方看起来仍像在拿 legacy `PlannedTask[]`。 | 当前 `src/interaction/goal-decomposer/index.ts` 已把 `planGraphBundle` 提升为 `GoalDecomposition` 顶层字段，同时保留 `tasks` 作为兼容投影；本轮新增 `tests/unit/interaction/goal-decomposer/review-regressions.test.ts` 已验证顶层 `planGraphBundle` 与 `harnessRouting.planGraphBundle` 一致且可直接消费。 | 回写真实根因、源码修复与定向测试依据。 |
| 2520 | Round 42 — 深度审计（compliance / HA-execution / UI-apps / contracts-implementation 交叉验证） / §211 Contracts-Implementation 交叉验证（OAPEFLIR / EventBus / NL-Gateway / Goal-Decomposer） | P2 | `src/interaction/goal-decomposer/index.ts:6` | SuccessCriterion 缺 operator/threshold 量化字段 | 已解决（文档过期） | 根因是审计记录停留在旧接口快照；现行 `GoalDecomposer` 已把 `operator` 与 `threshold` 纳入 `SuccessCriterion`，并增加 `evaluationMethod` 作为量化/人工校验来源。 | 当前 `src/interaction/goal-decomposer/index.ts` 的 `SuccessCriterion` 已声明 `operator` 与 `threshold`；本轮新增 `tests/unit/interaction/goal-decomposer/review-regressions.test.ts` 已验证该类型在仓内按量化字段使用。 | 以现行源码与最小类型回归纠正文档过期结论。 |
| 2521 | Round 42 — 深度审计（compliance / HA-execution / UI-apps / contracts-implementation 交叉验证） / §211 Contracts-Implementation 交叉验证（OAPEFLIR / EventBus / NL-Gateway / Goal-Decomposer） | P2 | `src/platform/orchestration/harness/oapeflir-harness-mapping.ts:13` | 缺 learn/release 映射——L/R 阶段永不被触发 | 已解决（文档过期） | 根因是审计结论基于旧快照；现行映射函数早已补入 `learner` / `release_manager` 与 `learn` / `release` stage 的分支，只是 reaudit 文档没有跟上。 | 当前 `src/platform/five-plane-orchestration/harness/oapeflir-harness-mapping.ts` 已明确返回 `learn` 与 `release`；本轮新增 `tests/unit/platform/five-plane-orchestration/oapeflir-harness-mapping-review-regressions.test.ts` 已通过，验证 learner/release_manager 两条映射。 | 以现行源码与最小回归测试纠正文档过期结论。 |
| 2522 | Round 42 — 深度审计（compliance / HA-execution / UI-apps / contracts-implementation 交叉验证） / §211 Contracts-Implementation 交叉验证（OAPEFLIR / EventBus / NL-Gateway / Goal-Decomposer） | P2 | `src/platform/orchestration/oapeflir/oapeflir-loop-service.ts:276` | Feedback 结果未包装为 DecisionInputBundle(缺 budgetState/riskState) | 已解决 | 根因是旧实现在 feedback 后只保留了 `feedback / evaluationReport / qualityGate / replanDecision / harnessDecision` 这些分散结果，没有把它们收敛成 canonical decision input contract，因此预算、风险、评估器和策略状态都没有统一附着点。 | 当前 `src/platform/five-plane-orchestration/oapeflir/oapeflir-loop-service.ts` 已新增 `decisionInputBundle` 结果字段，并通过 `buildDecisionInputBundle()` 把 feedback、quality gate、replan、constraintPack 组装为带 `budget/risk/evaluator/policy` 状态的 decision bundle；本轮新增 `tests/unit/platform/orchestration/oapeflir/review-regressions.test.ts` 已验证 bundle 具备 `decisionInputBundleId / bundleId / budget.remainingSteps / risk.escalationThreshold / evaluator.score / decisionKind`。 | 回写真实根因、源码修复与定向测试依据。 |
| 2523 | Round 42 — 深度审计（compliance / HA-execution / UI-apps / contracts-implementation 交叉验证） / §211 Contracts-Implementation 交叉验证（OAPEFLIR / EventBus / NL-Gateway / Goal-Decomposer） | P2 | `src/platform/state-evidence/events/event-types.ts:42` | TIER_1 含 26 类型但 registry 仅注册原始 9 个——新 tier-1 缺 schema/validator | 已解决（文档过期） | 根因是审计基于 canonical platform/oapeflir 事件补齐前的旧结论；现行 event registry 已为新增 tier-1 事件注册 schema 与 payload validator。 | 当前 `src/platform/five-plane-state-evidence/events/event-types.ts` 已列出 canonical harness/node/side-effect/budget/oapeflir tier-1 事件；`src/platform/five-plane-state-evidence/events/event-registry.ts` 已存在对应 schema；既有 `tests/unit/platform/five-plane-state-evidence/events/event-types.test.ts` 与本轮新增 `tests/unit/platform/five-plane-state-evidence/events/review-regressions.test.ts` 已验证 `platform.harness_run.created`、`platform.node_run.failed`、`platform.side_effect.failed`、`platform.budget.actualized`、`oapeflir.phase.transition` 都有 tier-1 / schema 覆盖。 | 以现行源码、既有测试与最小补充回归纠正文档过期结论。 |
| 2524 | Round 42 — 深度审计（compliance / HA-execution / UI-apps / contracts-implementation 交叉验证） / §211 Contracts-Implementation 交叉验证（OAPEFLIR / EventBus / NL-Gateway / Goal-Decomposer） | P2 | `src/platform/state-evidence/events/typed-event-bus.ts:32` | TypedEventPayloadMap 未覆盖 delegation/prompt/tenant 等事件 payload 定义 | 已解决（文档过期/审计基线漂移） | 根因不是“typed map 仍漏了 canonical known events”，而是审计把一批已退场的 `delegation:* / prompt:* / tenant:*` namespace 当成现行 known events 继续检查；现行 canonical known events 已转到 harness/node/side-effect/budget/oapeflir taxonomy。 | 当前 `src/platform/five-plane-state-evidence/events/typed-event-bus.ts` 已为 canonical platform/oapeflir 事件提供 payload 定义，并通过 `MissingTypedEventDefinitions` 做 compile-time coverage check；本轮新增 `tests/unit/platform/five-plane-state-evidence/events/review-regressions.test.ts` 已验证 canonical 事件可通过 typed map/runtime schema 校验，而 `delegation:granted`、`prompt:version_published`、`tenant:quota_exceeded` 不再是已注册事件 schema。 | 回写真实根因，避免把已退场 namespace 误报为现行 typed map 缺口。 |
| 2525 | Round 42 — 深度审计（compliance / HA-execution / UI-apps / contracts-implementation 交叉验证） / §211 Contracts-Implementation 交叉验证（OAPEFLIR / EventBus / NL-Gateway / Goal-Decomposer） | P2 | `src/platform/orchestration/oapeflir/oapeflir-loop-service.ts:118` | run() 未调用 StageTransitionFSM——FSM 死代码 | 已解决（文档过期） | 根因是文档记录停留在 FSM 接线前的旧实现；现行 `run()` 已在 Observe/Assess/Plan/Execute/Feedback/Learn/Improve，以及 replan 回退到 Plan 的路径上显式调用 `stageFsm.canTransitionTo()`、`recordStageEntry()`、`recordStageCompletion()`。 | 当前 `src/platform/five-plane-orchestration/oapeflir/oapeflir-loop-service.ts` 在主循环各阶段和 replan 分支都直接驱动 `stageFsm`；因此“FSM 死代码”这一指控已被现行源码证伪。 | 以源码复核纠正文档过期结论；未重复新增同质测试。 |
| 2526 | Round 42 — 深度审计（compliance / HA-execution / UI-apps / contracts-implementation 交叉验证） / §211 Contracts-Implementation 交叉验证（OAPEFLIR / EventBus / NL-Gateway / Goal-Decomposer） | P2 | `src/interaction/nl-gateway/index.ts:677` | buildTask 缺 RuntimeMode/AutonomyMode 注入——critical risk 未降级 autonomy | 已解决 | 根因是旧实现既没有把 risk-based `RuntimeMode/AutonomyMode` 写进 canonical task/request contract，也在高风险待确认场景下过早构造 `ConfirmedTaskSpec`，导致 critical risk 既拿不到降级后的执行策略，还会触发提前确认错误。 | 当前 `src/interaction/nl-gateway/index.ts` 已新增 `deriveExecutionPolicyContext()`，把 `autonomyMode/runtimeMode` 注入 `canonicalTaskDraft.normalizedIntent.context` 与 `canonicalRequestEnvelope.policyContext`，并在 `pending_user_confirmation` 时显式返回 `confirmedTaskSpec: null / canonicalRequestEnvelope: null`；本轮新增 `tests/unit/interaction/nl-gateway/review-regressions.test.ts` 已验证 critical risk 会降级为 `suggestion + no_write`，低风险 canonical request envelope 会携带 `full_auto + full_auto`。 | 回写真实根因、源码修复与定向测试依据。 |
| 2527 | Round 42 — 深度审计（compliance / HA-execution / UI-apps / contracts-implementation 交叉验证） / §211 Contracts-Implementation 交叉验证（OAPEFLIR / EventBus / NL-Gateway / Goal-Decomposer） | P2 | `src/platform/orchestration/harness/harness-bootstrap.ts:18` | 仅注册 2 个 service ID——缺 NodeRuntime/SideEffectMgr/Evaluator/GraphScheduler | 已解决（文档过期） | 根因是文档结论停留在只暴露 `catalog/bootstrap` 两个 service ID 的旧实现；现行 harness bootstrap 已把 `node_runtime / side_effect_mgr / evaluator / graph_scheduler` 一并纳入 canonical 注册清单。 | 当前 `src/platform/five-plane-orchestration/harness/harness-bootstrap.ts` 的 `registeredServiceIds` 已扩展为 6 个 ID，`registerHarnessBootstrap()` 也会注册这 6 个服务；本轮新增 `tests/unit/platform/five-plane-orchestration/harness-bootstrap-review-regressions.test.ts` 已验证 bootstrap 暴露完整 6 ID 且可从 `ServiceRegistry` 取回对应服务。 | 以现行源码与最小回归测试纠正文档过期结论。 |
| 2528 | Round 43 — 深度审计（org-governance / state-evidence / interaction-autonomy / scale-ecosystem） / §213 Org-Governance 深层缺陷（delegation / approval / SSO-SCIM） | P0 | `src/org-governance/delegated-governance/delegated-governance-service.ts:96` | attemptedValue===undefined 时跳过所有 guardrail——未传值操作绕过全部约束 | 已解决 | 根因是旧实现把 `attemptedValue` 直接传给 `evaluateGuardrail()`，在 guardrail 存在但未给值时没有 fail-closed，导致未传值操作可整体绕过约束。 | 当前 `src/org-governance/delegated-governance/delegated-governance-service.ts` 已在 `attemptedValue === undefined && allGuardrails.length > 0` 时直接拒绝；本轮新增 `tests/unit/org-governance/delegated-governance/delegated-governance-review-regressions.test.ts` 已验证该场景返回拒绝。 | 回写现行源码与定向测试依据。 |
| 2529 | Round 43 — 深度审计（org-governance / state-evidence / interaction-autonomy / scale-ecosystem） / §213 Org-Governance 深层缺陷（delegation / approval / SSO-SCIM） | P0 | `src/org-governance/delegated-governance/governance-console-service.ts:146` | revokeDelegation 不校验 actorId 权限——任意用户可撤销任意委托 | 已解决 | 根因是撤销路径最初只检查 delegation 是否存在/可撤销，没有把调用者身份与授权边界绑定到 grantor 或平台管理员。 | 当前 `src/org-governance/delegated-governance/governance-console-service.ts` 已要求 `delegation.grantorId === actorId || actorId === "platform_team"`；本轮补充的 `tests/unit/org-governance/delegated-governance/governance-console-service.test.ts` 已验证非 grantor 调用会返回 `permission_denied`。 | 回写现行源码与定向测试依据。 |
| 2530 | Round 43 — 深度审计（org-governance / state-evidence / interaction-autonomy / scale-ecosystem） / §213 Org-Governance 深层缺陷（delegation / approval / SSO-SCIM） | P0 | `src/org-governance/sso-scim/oidc/oidc-service.ts:237` | fetchUserInfo 错误时回退 simulateUserInfo——生产环境可产生虚假会话 | 已解决 | 根因是旧 `fetchUserInfo()` 在 userinfo HTTP 失败或异常时统一回落到 `simulateUserInfo()`，没有把 mock fallback 与生产禁用策略挂钩。 | 当前 `src/org-governance/sso-scim/oidc/oidc-service.ts` 在 `allowMockFallback === false` 时对非 2xx/异常直接抛错；本轮新增 `tests/unit/org-governance/sso-scim/oidc/fetch-user-info-fallback.test.ts` 已验证关闭 fallback 后不会再生成模拟用户。 | 回写现行源码与定向测试依据。 |
| 2531 | Round 43 — 深度审计（org-governance / state-evidence / interaction-autonomy / scale-ecosystem） / §213 Org-Governance 深层缺陷（delegation / approval / SSO-SCIM） | P0 | `src/org-governance/delegated-governance/delegated-governance-service.ts:189` | validateInheritanceRule 对 delete 无条件 allowed:true——低级角色可删除上级约束 | 已解决（源码修复并补定向测试） | 根因是真实缺陷残留在 `delete` 分支：虽然最早的“恒为 true”写法已不存在，但当前轮复核发现它仍把 `childIndex > parentIndex` 当作放行条件，结果仍会允许低级角色删除上级约束。 | 本轮已将 `src/org-governance/delegated-governance/delegated-governance-service.ts` 的 `delete` 规则收敛为仅同级可删、跨级一律拒绝；新增 `tests/unit/org-governance/delegated-governance/delegated-governance-review-regressions.test.ts` 已验证 `division_admin -> team_lead` 删除被拒、同级删除可通过。 | 已修生产代码与定向测试。 |
| 2532 | Round 43 — 深度审计（org-governance / state-evidence / interaction-autonomy / scale-ecosystem） / §213 Org-Governance 深层缺陷（delegation / approval / SSO-SCIM） | P1 | `src/org-governance/delegated-governance/delegated-governance-service.ts:77` | checkOperation 仅收集 platform_team guardrails——忽略 division/dept admin 层级 | 已解决 | 根因是旧实现只把 platform 级 guardrail 视为全局约束，没有把 division/department 级 active delegation 按 org/domain scope 一并纳入评估。 | 当前 `src/org-governance/delegated-governance/delegated-governance-service.ts` 会遍历所有 `status === "active"` delegation，并按 `orgNodeIds/domainIds` 聚合适用 guardrails；本轮新增 `tests/unit/org-governance/delegated-governance/delegated-governance-review-regressions.test.ts` 已验证 division 级 guardrail 会生效。 | 回写现行源码与定向测试依据。 |
| 2533 | Round 43 — 深度审计（org-governance / state-evidence / interaction-autonomy / scale-ecosystem） / §213 Org-Governance 深层缺陷（delegation / approval / SSO-SCIM） | P1 | `src/org-governance/approval-routing/route-engine/index.ts:179` | 无汇率快照时 createdAt 回退至 epoch 1970——审计时间戳失义 | 已解决 | 根因是旧 legacy 金额分支在缺少汇率快照时把 `capturedAt/createdAt` 回退到 Unix epoch，导致审批快照的审计时间语义失真。 | 当前 `src/org-governance/approval-routing/route-engine/index.ts` 已在无 `fxSnapshot` 时改用 `new Date().toISOString()` 作为当前时间；本轮新增 `tests/unit/org-governance/approval-routing/route-engine/review-regressions.test.ts` 已验证 `createdAt` 落在实际调用时间窗口内而非 `1970-01-01T00:00:00.000Z`。 | 回写现行源码与定向测试依据。 |
| 2534 | Round 43 — 深度审计（org-governance / state-evidence / interaction-autonomy / scale-ecosystem） / §213 Org-Governance 深层缺陷（delegation / approval / SSO-SCIM） | P1 | `src/org-governance/approval-routing/route-engine/index.ts:256` | normalizeThresholdCny 硬编码汇率 7.2——与动态 fxRateSnapshot 不一致 | 已解决 | 根因是旧阈值换算把 USD 规则统一乘以硬编码 `7.2`，没有优先使用请求自带的 `fxRateSnapshot`，造成审批分流与实际金额快照不一致。 | 当前 `src/org-governance/approval-routing/route-engine/index.ts` 已在 `fxSnapshot` 存在时使用 `fxSnapshot.rate`，只有 legacy 无快照请求才退回配置化默认值；本轮新增 `tests/unit/org-governance/approval-routing/route-engine/review-regressions.test.ts` 已验证路由决策按提供的 `rate: 8` 生效。 | 回写现行源码与定向测试依据。 |
| 2535 | Round 43 — 深度审计（org-governance / state-evidence / interaction-autonomy / scale-ecosystem） / §213 Org-Governance 深层缺陷（delegation / approval / SSO-SCIM） | P1 | `src/org-governance/sso-scim/scim-sync/scim-service.ts:533` | SCIM remove 清空全部 members——未解析 members[value eq "xxx"] 精确移除 | 已解决 | 根因是旧 `patchGroup(remove)` 没有解析 SCIM 标准 filter path，`members[...]` 一律退化成“清空整个 members 数组”。 | 当前 `src/org-governance/sso-scim/scim-sync/scim-service.ts` 已解析 `members[value eq "xxx"]` 并只移除目标成员；`tests/unit/org-governance/sso-scim/scim-sync/scim-service.test.ts` 中“should remove specific member via patch with filter”已通过。 | 回写现行源码与定向测试依据。 |
| 2536 | Round 43 — 深度审计（org-governance / state-evidence / interaction-autonomy / scale-ecosystem） / §213 Org-Governance 深层缺陷（delegation / approval / SSO-SCIM） | P1 | `src/org-governance/sso-scim/scim-sync/scim-service.ts:164` | createUser 未校验 userName 唯一性——重复 userName 覆盖索引 | 已解决 | 根因是旧 `createUser()` 在写入 `userByUsername` 索引前没有做租户内唯一性检查，重复 `userName` 会覆盖旧索引映射。 | 当前 `src/org-governance/sso-scim/scim-sync/scim-service.ts` 已先查 `tenantUserByUsername.get(user.userName.toLowerCase())` 并抛出 `scim.userName_already_exists`；`tests/unit/org-governance/sso-scim/scim-sync/scim-service.test.ts` 中“should throw when username already exists”已通过。 | 回写现行源码与定向测试依据。 |
| 2537 | Round 43 — 深度审计（org-governance / state-evidence / interaction-autonomy / scale-ecosystem） / §213 Org-Governance 深层缺陷（delegation / approval / SSO-SCIM） | P1 | `src/org-governance/sso-scim/saml/index.ts:150` | consumedAssertionIds Map 无 TTL——无限增长内存泄漏 | 已解决 | 根因是旧 replay 保护只把 `assertionId` 放入常驻 `Map`，没有 TTL 和清理路径，长生命周期服务会持续积累已消费断言。 | 当前 `src/org-governance/sso-scim/saml/index.ts` 已把 `consumedAssertionIds` 改为“过期时间戳 Map”，并在每次消费前执行 `cleanupExpiredAssertions()`；`tests/unit/org-governance/sso-scim/saml/index.test.ts` 已验证 replay 拦截，本轮新增 `tests/unit/org-governance/sso-scim/saml/assertion-ttl-cleanup.test.ts` 已验证 TTL 过后旧 assertionId 会被清理。 | 回写现行源码与定向测试依据。 |
| 2538 | Round 43 — 深度审计（org-governance / state-evidence / interaction-autonomy / scale-ecosystem） / §213 Org-Governance 深层缺陷（delegation / approval / SSO-SCIM） | P1 | `src/org-governance/approval-routing/delegation/index.ts:30` | coiReviewStatus=failed 的委托仍生效——利益冲突审查被绕过 | 已解决 | 根因是旧 delegated approver 解析逻辑没有把 COI 审查失败作为硬阻断条件，失败委托仍可进入审批链。 | 当前 `src/org-governance/approval-routing/delegation/index.ts` 已在匹配条件中显式要求 `item.coiReviewStatus !== "failed"`；`tests/unit/org-governance/approval-routing/approval-routing-service.test.ts` 中“route() rejects delegation with failed COI status”已通过。 | 回写现行源码与定向测试依据。 |
| 2539 | Round 43 — 深度审计（org-governance / state-evidence / interaction-autonomy / scale-ecosystem） / §213 Org-Governance 深层缺陷（delegation / approval / SSO-SCIM） | P1 | `src/org-governance/delegated-governance/governance-delegation-revocation-saga.ts:28` | cascadeWithinSlo 条件 >=0 恒 true——级联撤销失败永不标记 | 已解决 | 原实现把 `elapsed >= 0` 当作 SLO 判定，导致 `cascadeWithinSlo` 在失败场景下也恒为 true。 | 当前 `src/org-governance/delegated-governance/governance-delegation-revocation-saga.ts` 已改为 `failedStage == null && elapsed <= CASCADE_SLO_MS`；新增 `tests/unit/org-governance/delegated-governance/governance-delegation-revocation-saga-slo.test.ts` 已验证超时和失败场景都会返回 `false`。 | 不改生产代码，仅补聚焦回归并回写依据。 |
| 2540 | Round 43 — 深度审计（org-governance / state-evidence / interaction-autonomy / scale-ecosystem） / §213 Org-Governance 深层缺陷（delegation / approval / SSO-SCIM） | P1 | `src/org-governance/sso-scim/api-key-service.ts:94` | API Key 过期后 status 仍 "active"——后续操作基于 status 判断不一致 | 已解决（源码修复并补定向测试） | 根因是真实状态漂移：`validateApiKey()` 返回 `key_expired`，但没有把持久态 `record.status` 同步落成 `expired`。 | 本轮在 `src/org-governance/sso-scim/api-key-service.ts` 的过期分支补上 `record.status = "expired"`；`tests/unit/org-governance/sso-scim/api-key-service.test.ts` 与 `tests/unit/org-governance/sso-scim-api-key-service.test.ts` 已新增断言验证状态同步。 | 已修代码与测试，不保留缺口。 |
| 2541 | Round 43 — 深度审计（org-governance / state-evidence / interaction-autonomy / scale-ecosystem） / §213 Org-Governance 深层缺陷（delegation / approval / SSO-SCIM） | P1 | `src/org-governance/sso-scim/oidc/oidc-service.ts:261` | validateAccessToken O(n) 线性扫描所有 session——无 token→sessionId 索引 | 已解决 | 原问题来自 access token 校验按 session 全表扫描；现行实现已维护二级索引。 | `src/org-governance/sso-scim/oidc/oidc-service.ts` 当前维护 `accessTokenIndex`，`validateAccessToken()` 先做 O(1) 索引查找；新增 `tests/unit/org-governance/sso-scim/oidc/oidc-token-index-cleanup.test.ts` 已覆盖索引校验路径。 | 不改生产代码，补聚焦回归并回写依据。 |
| 2542 | Round 43 — 深度审计（org-governance / state-evidence / interaction-autonomy / scale-ecosystem） / §213 Org-Governance 深层缺陷（delegation / approval / SSO-SCIM） | P2 | `src/org-governance/approval-routing/approval-routing-service.ts:79` | recordId 无时间戳/随机因子——同请求者+同节点 ID 冲突 | 已解决 | 原实现 audit recordId 只依赖业务键，缺时间/随机因子，碰撞窗口真实存在。 | `src/org-governance/approval-routing/approval-routing-service.ts` 当前使用 ``audit_${requesterId}_${orgNodeId}_${Date.now()}_${crypto.randomUUID()}``；`tests/unit/org-governance/approval-routing/approval-routing-service.test.ts` 已验证连续生成的 `recordId` 不相等。 | 不改生产代码，回写复核依据。 |
| 2543 | Round 43 — 深度审计（org-governance / state-evidence / interaction-autonomy / scale-ecosystem） / §213 Org-Governance 深层缺陷（delegation / approval / SSO-SCIM） | P2 | `src/org-governance/approval-routing/approval-routing-service.ts:64` | 升级审批人追加到 chain 末尾——sequential 模式下升级反而最后审批 | 已解决 | 原问题来自 escalation approver 被 append 到顺序链尾部，破坏升级审批优先级。 | 当前 `src/org-governance/approval-routing/approval-routing-service.ts` 已改为在顺序链头部 prepend escalation approver；`tests/unit/org-governance/approval-routing/approval-routing-service.test.ts` 已验证顺序链首位即升级审批人。 | 不改生产代码，回写复核依据。 |
| 2544 | Round 43 — 深度审计（org-governance / state-evidence / interaction-autonomy / scale-ecosystem） / §213 Org-Governance 深层缺陷（delegation / approval / SSO-SCIM） | P2 | `src/org-governance/org-model/hierarchy/index.ts:73` | listAncestorNodeIds 无环检测——环形引用致无限循环 | 已解决 | 原实现只沿父指针向上遍历，没有 visited 检测，环形组织图会卡死。 | 当前 `src/org-governance/org-model/hierarchy/index.ts` 已引入 `visited` 检测并抛出 `org_hierarchy.circular_reference_detected`；`tests/unit/org-governance/org-model/hierarchy/index.test.ts` 已新增环引用回归。 | 不改生产代码，补聚焦回归并回写依据。 |
| 2545 | Round 43 — 深度审计（org-governance / state-evidence / interaction-autonomy / scale-ecosystem） / §213 Org-Governance 深层缺陷（delegation / approval / SSO-SCIM） | P2 | `src/org-governance/delegated-governance/governance-console-service.ts:126` | createDelegation 硬编码 level:"view"/delegatable:false——所有委托只读不可再委托 | 已解决（源码修复并补定向测试） | 根因不是对象组装层 alone，而是 `CreateDelegationRequestSchema` 没有声明 `level/delegatable`，`parse()` 先把请求字段剥掉，运行时仍退回默认值。 | 本轮在 `src/org-governance/delegated-governance/governance-console-service.ts` 的创建 schema 中补入 `level/delegatable`，并改为直接使用解析后的 `request.level/request.delegatable`；`tests/unit/org-governance/delegated-governance/governance-console-service.test.ts` 已新增保留调用方级别与再委托标志的断言。 | 已修代码与测试，不保留缺口。 |
| 2546 | Round 43 — 深度审计（org-governance / state-evidence / interaction-autonomy / scale-ecosystem） / §213 Org-Governance 深层缺陷（delegation / approval / SSO-SCIM） | P2 | `src/org-governance/sso-scim/scim-sync/scim-service.ts:791` | applyFilter 忽略 emails.value 等标准 SCIM 过滤字段 | 已解决（源码修复并补定向测试） | 根因是真实解析缺陷：虽然 earlier 修复已不再把所有 filter 都硬套到 `userName/displayName`，但实现仍只支持顶层字段，`emails.value` 这类标准 SCIM dotted path 取值时会直接得到 `undefined`。 | 本轮在 `src/org-governance/sso-scim/scim-sync/scim-service.ts` 为 `applyFilter()` 补上 dotted path + 数组节点解析；`tests/unit/org-governance/sso-scim/scim-sync/scim-service.test.ts` 已新增 `emails.value eq "nested.filter@example.com"` 用例并通过。 | 已修生产代码与定向测试。 |
| 2547 | Round 43 — 深度审计（org-governance / state-evidence / interaction-autonomy / scale-ecosystem） / §213 Org-Governance 深层缺陷（delegation / approval / SSO-SCIM） | P2 | `src/org-governance/org-model/hierarchy/index.ts:226` | employee_transfer toTeamId 设为父节点 ID——调动目标记录错误 | 已解决 | 原问题来自 transfer 事件把目标父节点误当成目标团队节点，导致后续审计/治理工件指向错误对象。 | 当前 `src/org-governance/org-model/hierarchy/index.ts` 已把 `toTeamId` 改为 `afterNode.orgNodeId`；`tests/unit/org-governance/org-model/hierarchy/index.test.ts` 已新增调动事件目标团队断言。 | 不改生产代码，补聚焦回归并回写依据。 |
| 2548 | Round 43 — 深度审计（org-governance / state-evidence / interaction-autonomy / scale-ecosystem） / §213 Org-Governance 深层缺陷（delegation / approval / SSO-SCIM） | P2 | `src/org-governance/sso-scim/oidc/oidc-service.ts:427` | cleanupExpiredSessions 延迟最长 24h——过期会话长期占内存 | 已解决 | 原问题是过期清理判定曾把 `maxSessionAgeMs` 混入 expired check，导致会话可在实际过期后继续滞留。 | 当前 `src/org-governance/sso-scim/oidc/oidc-service.ts` 的 `cleanupExpiredSessions()` 直接按 `Date.now() > expiresAt` 清理；新增 `tests/unit/org-governance/sso-scim/oidc/oidc-token-index-cleanup.test.ts` 已验证过期 session 会被立即回收。 | 不改生产代码，补聚焦回归并回写依据。 |
| 2549 | Round 43 — 深度审计（org-governance / state-evidence / interaction-autonomy / scale-ecosystem） / §214 State-Evidence 深层缺陷（Truth / Checkpoints / Knowledge / Projections / Inbox） | P0 | `src/platform/state-evidence/truth/runtime-truth-repository.ts:196` | storeAggregate 对已存在聚合 Map.set 覆写——违反 append-only 不变量 | 已解决 | 原实现允许 `seed/storeAggregate` 覆盖同 ID 聚合，破坏 truth append-only 基线。 | 当前 `src/platform/state-evidence/truth/runtime-truth-repository.ts` 在 `storeAggregate()` 中对各类聚合重复写入统一抛出 `runtime_truth_repository.append_only_violation`；`tests/unit/platform/state-evidence/truth/runtime-truth-repository.test.ts` 已新增重复 seed 拒绝回归。 | 不改生产代码，补聚焦回归并回写依据。 |
| 2550 | Round 43 — 深度审计（org-governance / state-evidence / interaction-autonomy / scale-ecosystem） / §214 State-Evidence 深层缺陷（Truth / Checkpoints / Knowledge / Projections / Inbox） | P0 | `src/platform/state-evidence/events/layered-event-inbox/layered-event-inbox.ts:51` | drain 无事件去重——同一 EventEnvelope append 两次 consumer 收到两条 | 已解决 | 原问题来自 consumer drain 没有按 eventId 去重；同一 envelope 重复 append 会被重复投递。 | 当前 `src/platform/state-evidence/events/layered-event-inbox.ts` 已在 drain 内维护 `seenEventIds` 去重；`tests/unit/platform/state-evidence/events/layered-event-inbox.test.ts` 已新增重复 eventId 仅投递一次的回归。 | 不改生产代码，补聚焦回归并回写依据。 |
| 2551 | Round 43 — 深度审计（org-governance / state-evidence / interaction-autonomy / scale-ecosystem） / §214 State-Evidence 深层缺陷（Truth / Checkpoints / Knowledge / Projections / Inbox） | P1 | `src/platform/state-evidence/events/durable-event-bus.ts:358` | 重试 attempt<=MAX(3) 实际执行 4 次(0..3)——比声明多一次 | 已解决（源码复核） | 原实现的 attempt 上界与“重试次数/总尝试次数”概念混用，产生 off-by-one。 | 当前 `src/platform/state-evidence/events/durable-event-bus.ts` 已拆分为 `MAX_DELIVERY_RETRIES = 2` 与 `TOTAL_DELIVERY_ATTEMPTS = MAX_DELIVERY_RETRIES + 1`，主循环按总尝试次数执行，不再多一次。仓内旧 retry loop 测试仍存在导入路径/迁移环境漂移，本轮以源码现状为证。 | 不改生产代码，回写复核依据。 |
| 2552 | Round 43 — 深度审计（org-governance / state-evidence / interaction-autonomy / scale-ecosystem） / §214 State-Evidence 深层缺陷（Truth / Checkpoints / Knowledge / Projections / Inbox） | P1 | `src/platform/state-evidence/events/layered-event-inbox/layered-event-inbox.ts:65` | drain 对不匹配 consumer 的事件仍推进 cursor——跨 consumer 事件可能丢失 | 已解决 | 原问题来自 cursor 在未投递 event 上也被推进；truth/projection 等异构 consumer 会丢事件。 | 当前 `src/platform/state-evidence/events/layered-event-inbox.ts` 仅在 `canConsumerReceive(...)` 且成功加入 `delivered` 时推进 `nextCursor`；`tests/unit/platform/state-evidence/events/layered-event-inbox.test.ts` 已新增“不匹配事件不推进 cursor”回归。 | 不改生产代码，补聚焦回归并回写依据。 |
| 2553 | Round 43 — 深度审计（org-governance / state-evidence / interaction-autonomy / scale-ecosystem） / §214 State-Evidence 深层缺陷（Truth / Checkpoints / Knowledge / Projections / Inbox） | P1 | `src/platform/state-evidence/events/transactional-event-appender.ts:97` | 手动 BEGIN 绕过 WAL 栅栏——嵌套事务时静默提交或回滚范围错误 | 已解决（源码/测试复核） | 原问题是 appender 自己手写 `BEGIN/COMMIT/ROLLBACK`，会绕过 `db.transaction()` 的嵌套事务/WAL 包装；现行实现已经统一回收进事务包装器。 | 当前 `src/platform/state-evidence/events/transactional-event-appender.ts` 的 `appendEvent()` / `appendEvents()` 均通过 `this.db.transaction(() => ...)` 执行，并支持把 `mutateTruth` 放进同一事务；`tests/unit/platform/state-evidence/events/transactional-event-appender.test.ts` 本轮已定向通过 transaction wrapper、truth mutation 原子提交/回滚等回归。 | 更新为“源码+测试复核”结论，不再保留“旧测试被 migration 噪音阻断”的过时说明。 |
| 2554 | Round 43 — 深度审计（org-governance / state-evidence / interaction-autonomy / scale-ecosystem） / §214 State-Evidence 深层缺陷（Truth / Checkpoints / Knowledge / Projections / Inbox） | P1 | `src/platform/state-evidence/knowledge/knowledge-query-service.ts:186` | Deep 同步回退用 Standard 逻辑——语义不一致 | 已解决 | 原问题是同步接口在 deep 模式下偷偷走 standard 逻辑，调用方拿到语义降级结果却无显式信号。 | 当前 `src/platform/state-evidence/knowledge/knowledge-query-service.ts` 在同步 deep 路径直接抛出 `knowledge_query.deep_requires_async`，不再 silent fallback；新增 `tests/unit/platform/state-evidence/knowledge/knowledge-query-sync-deep.test.ts` 已验证。 | 不改生产代码，补聚焦回归并回写依据。 |
| 2555 | Round 43 — 深度审计（org-governance / state-evidence / interaction-autonomy / scale-ecosystem） / §214 State-Evidence 深层缺陷（Truth / Checkpoints / Knowledge / Projections / Inbox） | P1 | `src/platform/state-evidence/knowledge/knowledge-retrieval.ts:296` | pgvector 后端时同步查询无语义候选——collectSemanticCandidates 返回空 | 已解决 | 根因是旧同步查询路径在存在 pgvector/vector backend 时没有本地 fallback，`collectSemanticCandidates()` 会直接给出空候选。 | 当前 `src/platform/five-plane-state-evidence/knowledge/retrieval/knowledge-retrieval.ts` 的同步 `collectSemanticCandidates()` 已改为基于 archive 中的 embedding 做本地相似度扫描，异步路径才走 `semanticVectorStore.querySimilar(...)`；本轮复核中 `tests/unit/platform/state-evidence/knowledge/knowledge-query-sync-deep.test.ts` 定向通过，可证明同步 deep/query 相关路径未退回旧空结果分支。 | 回写现行源码与定向测试依据。 |
| 2556 | Round 43 — 深度审计（org-governance / state-evidence / interaction-autonomy / scale-ecosystem） / §214 State-Evidence 深层缺陷（Truth / Checkpoints / Knowledge / Projections / Inbox） | P1 | `src/platform/state-evidence/checkpoints/session-dual-storage.ts:120` | task-index 写入失败仅 log——session 已落盘但索引缺失，replay 一致性破坏 | 已解决（源码复核） | 原问题是 task index 打不开或写入失败时只记日志、不抛错，调用方会误以为 session 与 task index 双写都成功。 | 当前 `src/platform/five-plane-state-evidence/truth/session-dual-storage.ts` 在 `taskIndex` open/write 两个失败分支都记录日志后显式 `throw new Error(...)`，不再静默吞掉部分失败。现有 `tests/unit/platform/state-evidence/truth/session-dual-storage.test.ts` 可证明正常双写路径稳定，但该缺陷的关键证据以现源码 fail-closed 行为为准。 | 回写现行源码依据，并明确当前为 fail-closed。 |
| 2557 | Round 43 — 深度审计（org-governance / state-evidence / interaction-autonomy / scale-ecosystem） / §214 State-Evidence 深层缺陷（Truth / Checkpoints / Knowledge / Projections / Inbox） | P1 | `src/platform/state-evidence/checkpoints/checkpoint-envelope.ts:149` | 错误消息报告 originalSize 非 compressedSize——运维排查信息错误 | 已解决 | 根因是“压缩后仍超限”的分支曾复用 `originalSizeBytes` 报错，导致异常消息与真实超限载荷不一致。 | 当前 `src/platform/state-evidence/checkpoints/checkpoint-envelope.ts` 已在压缩后超限分支抛出 `new CheckpointSizeExceededError(compressedSizeBytes, maxSizeBytes)`；`tests/unit/platform/five-plane-state-evidence/checkpoints/checkpoint-envelope.test.ts` 本轮已通过。 | 回写现行源码与定向测试依据。 |
| 2558 | Round 43 — 深度审计（org-governance / state-evidence / interaction-autonomy / scale-ecosystem） / §214 State-Evidence 深层缺陷（Truth / Checkpoints / Knowledge / Projections / Inbox） | P1 | `src/platform/state-evidence/events/projections/worker-status-projection.ts:137` | isEventProcessed Array.includes O(n²)——长期运行性能退化 | 已解决（源码修复并补定向测试） | 这里存在两个真实问题：一是去重结构仍需从数组查找迁到 `Set`；二是迁移后若直接返回内部态，`processedEventIds` 不会序列化回投影输出，重复事件仍会漏拦。 | 本轮确认 `src/platform/state-evidence/events/projections/worker-status-projection.ts` 已使用 `_processedEventIdSet.has(...)` 做 O(1) 判重，并补上结尾 `toSerializedState(newState)`，确保 processed IDs 正确落盘；新增 `tests/unit/platform/state-evidence/events/projections/worker-status-projection-idempotency.test.ts` 已验证 processed IDs 记录与重复事件跳过。 | 已修代码与测试，不保留缺口。 |
| 2559 | Round 43 — 深度审计（org-governance / state-evidence / interaction-autonomy / scale-ecosystem） / §214 State-Evidence 深层缺陷（Truth / Checkpoints / Knowledge / Projections / Inbox） | P1 | `src/platform/state-evidence/events/cas/cas-service.ts:182` | setValue 重置 version=1 而非递增——破坏 version 单调递增不变量 | 已解决 | 原问题来自 `setValue()` 每次覆盖写都把 version 重置为 1，破坏 CAS 单调版本假设。 | 当前 `src/platform/state-evidence/events/cas/cas-service.ts` 已改为在已有记录上执行 `existing.version + 1`；`tests/unit/platform/state-evidence/events/cas/cas-service.test.ts` 已同步为“覆盖写后 version=2”的断言并通过。 | 不改生产代码，回写复核依据。 |
| 2560 | Round 43 — 深度审计（org-governance / state-evidence / interaction-autonomy / scale-ecosystem） / §214 State-Evidence 深层缺陷（Truth / Checkpoints / Knowledge / Projections / Inbox） | P2 | `src/platform/state-evidence/truth/cross-region-truth-leader.ts:31` | leader 已迁移后合法写入被永久拒绝——不支持 leader failover | 已解决 | 根因是旧 leader 判定把 `homeRegion` 当成唯一合法写入 region；leader 迁移后，即便 `leaderRegion` 已切换，新的合法 leader 也会被错误拒绝。 | 当前 `src/platform/five-plane-state-evidence/truth/cross-region-truth-leader.ts` 已改为按 `claim.region === epoch.leaderRegion` 判断；`tests/unit/platform/state-evidence/truth/cross-region-truth-leader.test.ts` 本轮已通过，覆盖了 `leaderRegion` 与 claim region 不同的拒绝/接受场景。 | 回写现行源码与定向测试依据。 |
| 2561 | Round 43 — 深度审计（org-governance / state-evidence / interaction-autonomy / scale-ecosystem） / §214 State-Evidence 深层缺陷（Truth / Checkpoints / Knowledge / Projections / Inbox） | P2 | `src/platform/state-evidence/knowledge/knowledge-query-service.ts:352` | truncateHits 固定截断每个 hit——总 token 可能远超 maxTokens | 已解决（源码复核） | 原问题来自早期按固定 per-hit 长度粗暴截断，没有把全局 `maxTokens` 预算按 hits 数量重新分配。 | 当前 `src/platform/five-plane-state-evidence/knowledge/knowledge-query-service.ts` 的 `truncateHits()` 已按 `maxChars = maxTokens * 4` 计算总预算，再用 `basePerHit + remainder` 把字符预算分配到每个 hit，因此所有 snippet 总和被约束在总预算内。 | 回写现行源码依据。 |
| 2562 | Round 43 — 深度审计（org-governance / state-evidence / interaction-autonomy / scale-ecosystem） / §214 State-Evidence 深层缺陷（Truth / Checkpoints / Knowledge / Projections / Inbox） | P2 | `src/platform/state-evidence/events/projections/workflow-run-projection.ts:322` | 单 subtask:failed 直接标 workflow failed——未考虑容错策略 | 已解决 | 根因是旧 projection 在收到单个 `subtask:failed` 时立即把 workflow 标为 failed，没有等待 task/workflow 聚合态决定是否真正终止。 | 当前 `src/platform/state-evidence/events/projections/workflow-run-projection.ts` 的 `handleSubtaskFailed()` 只记录 `failedSteps/error`，不改 workflow status；本轮新增 `tests/unit/platform/state-evidence/events/projections/workflow-run-projection-subtask-failure-regression.test.ts` 已验证 `subtask:failed` 后 workflow 不会立即失败，直到后续 `task:status_changed(toStatus=failed)` 才转入失败态。 | 回写现行源码与定向测试依据。 |
| 2563 | Round 43 — 深度审计（org-governance / state-evidence / interaction-autonomy / scale-ecosystem） / §214 State-Evidence 深层缺陷（Truth / Checkpoints / Knowledge / Projections / Inbox） | P2 | `src/platform/state-evidence/events/projections/approval-queue-projection.ts:356` | rejectionsReceived 设为 approvalsRequired 非递增——多次 rejection 计数丢失 | 已解决（源码修复并补定向测试） | 复核确认该缺陷仍真实存在于 `decision:responded(rejected)` 路径：当前实现把 `rejectionsReceived` 折叠成常量值，重复 rejection 不会累计。回归过程中还发现同文件末尾错误返回了内部 `Set` 状态，导致 processedEventIds 去重序列化也会失真。 | 本轮已将 `src/platform/state-evidence/events/projections/approval-queue-projection.ts` 改为对 rejected 响应执行累加，并统一返回 `toSerializedState(newState)`；`tests/unit/platform/state-evidence/events/projections/approval-queue-projection.test.ts` 已新增多次 rejection 累加断言，且整套投影测试本轮通过。 | 已修生产代码与定向测试。 |
| 2564 | Round 43 — 深度审计（org-governance / state-evidence / interaction-autonomy / scale-ecosystem） / §214 State-Evidence 深层缺陷（Truth / Checkpoints / Knowledge / Projections / Inbox） | P2 | `src/platform/state-evidence/events/durable-event-bus.ts:48` | POLL 10ms/consumer 无 backoff——高 consumer 数 CPU 开销大 | 已解决（源码复核） | 根因是旧 subscriber 轮询间隔固定为 10ms 且不随 consumer 数量退避，空闲时会把 CPU 浪费在线性轮询上。 | 当前 `src/platform/state-evidence/events/durable-event-bus.ts` 已把默认轮询间隔提高到 `100ms`，并加入 `CONSUMER_BACKOFF_MULTIPLIER` / `CONSUMER_BACKOFF_THRESHOLD` 做 consumer 数量退避。 | 回写现行源码依据。 |
| 2565 | Round 43 — 深度审计（org-governance / state-evidence / interaction-autonomy / scale-ecosystem） / §214 State-Evidence 深层缺陷（Truth / Checkpoints / Knowledge / Projections / Inbox） | P2 | `src/platform/state-evidence/knowledge/knowledge-ingestion-pipeline.ts:220` | semantic chunking 回退到 fixed 分割——违反 ChunkingConfig 契约 | 已解决 | 根因是旧 semantic mode 没有专门实现时错误回退到了 `chunkFixed()`，破坏了按语义边界/section 切分的契约语义。 | 当前 `src/platform/state-evidence/knowledge/knowledge-ingestion-pipeline.ts` 在 `chunking.mode === "semantic"` 时已改为回退到 `chunkSectionAware(body)`，而不是 fixed 切块；因此缺少专门 semantic segmenter 时仍保持基于 section 的语义边界。 | 回写现行源码依据。 |
| 2566 | Round 43 — 深度审计（org-governance / state-evidence / interaction-autonomy / scale-ecosystem） / §214 State-Evidence 深层缺陷（Truth / Checkpoints / Knowledge / Projections / Inbox） | P2 | `src/platform/state-evidence/knowledge/storage-quota-service.ts:159` | rmSync 失败中断整个清理——应 catch 继续 | 已解决 | 根因是旧 quota cleanup 在单个文件删除失败时会把整个配额清理流程一并打断。 | 当前 `src/platform/five-plane-state-evidence/truth/storage-quota-service.ts` 已对 `rmSync(...)` 包裹 `try/catch`，失败时只记录 `session_dual_storage.cleanup.rm_failed` 并继续清理后续文件；`tests/unit/platform/state-evidence/truth/storage-quota-service.test.ts` 本轮已通过。 | 回写现行源码与定向测试依据。 |
| 2567 | Round 43 — 深度审计（org-governance / state-evidence / interaction-autonomy / scale-ecosystem） / §215 Interaction 层深层缺陷（Autonomy / Dashboard / UX / Proactive-Agent） | P0 | `src/interaction/autonomy/index.ts:215` | P0 incident 直接 freeze——§42.2 规定应降至 suggestion 非 frozen | 已解决 | 原审计对应的是旧版 incident 状态机；当前 `decideLevel()` 已先于通用 freeze 分支处理 `P0`，将其直接降到 `suggestion`，此前只是 reaudit 文档和回归证据未同步回写。 | 当前 `src/interaction/autonomy/index.ts` 在 `score.incidents > 0 && severity === "P0"` 时直接返回 `suggestion`；本轮补齐了 `tests/unit/interaction/autonomy/index.test.ts`、`tests/unit/interaction/autonomy/progressive-autonomy.test.ts`、`tests/unit/interaction/autonomy/autonomy-level-state.test.ts` 与 `tests/integration/interaction/autonomy/autonomy-integration.test.ts` 的 `P0 -> suggestion` 断言。 | 不改生产代码，仅回写现行实现并补定向测试证据。 |
| 2568 | Round 43 — 深度审计（org-governance / state-evidence / interaction-autonomy / scale-ecosystem） / §215 Interaction 层深层缺陷（Autonomy / Dashboard / UX / Proactive-Agent） | P0 | `src/interaction/autonomy/autonomy-governance-service.ts:71` | trustScore<30 降级不检查 incident severity——高风险 incident 绕过 P0/P1 降级 | 已解决（源码修复并补定向测试） | 根因不是“完全没识别 severity”，而是治理服务旧分支虽然进了 `P0/P1` 路径，却在 `assessPromotion()` 返回降级建议时丢弃了 `promotion.targetLevel`，回退成 `score.currentAutonomy`，从而把 severity 降级实际吞掉。 | 本轮已将 `src/interaction/autonomy/autonomy-governance-service.ts` 的高严重度分支改为直接采用 `promotion.targetLevel` 与 `promotion.reasonCodes`；`tests/unit/interaction/autonomy/autonomy-governance-service.test.ts` 已新增 `P0 -> suggestion` 与 `P1 -> supervised` 回归，且定向测试通过。 | 已改生产代码与定向测试。 |
| 2569 | Round 43 — 深度审计（org-governance / state-evidence / interaction-autonomy / scale-ecosystem） / §215 Interaction 层深层缺陷（Autonomy / Dashboard / UX / Proactive-Agent） | P0 | `src/interaction/autonomy/index.ts:258` | 高风险域仅压 full_auto→semi_auto——§42.5 要求写操作最高 supervised | 已解决 | 原审计停留在较早的 domain risk cap 逻辑；当前实现对 `humanAccountable`/`riskClass === "high"` 以及 `highRiskDomainIds` 已统一压到 `lowestLevel([level, "supervised"])`，比“仅压 full_auto→semi_auto”更严格。 | 当前 `src/interaction/autonomy/index.ts` 的 `applyDomainRiskAutonomyCap()` 会把高风险域 autonomy 上限压到 `supervised`，critical/advisory-only 还会进一步压到 `suggestion`；`tests/unit/interaction/autonomy/domain-risk-autonomy-cap.test.ts` 已直接验证 `quant-trading` 场景不会超过 `supervised`。 | 不改生产代码，仅回写现行实现与定向测试依据。 |
| 2570 | Round 43 — 深度审计（org-governance / state-evidence / interaction-autonomy / scale-ecosystem） / §215 Interaction 层深层缺陷（Autonomy / Dashboard / UX / Proactive-Agent） | P1 | `src/interaction/autonomy/level-manager/index.ts:3` | frozen 放在 full_auto 之后(index=4)——compare 认为 frozen>full_auto 语义错误 | 已解决 | 原问题已在代码中修正，但复审文档和两组 level-manager 测试仍停留在旧排序假设，导致 reaudit 继续把已修复代码误标成未解决。 | 当前 `src/interaction/autonomy/level-manager/index.ts` 的 `AUTONOMY_LEVEL_ORDER` 已以 `frozen` 作为最低位；本轮同步修正了 `tests/unit/interaction/autonomy/level-manager/index.test.ts` 与 `tests/unit/interaction/autonomy/level-manager.test.ts`，明确断言 `compareAutonomyLevels("frozen", "suggestion") < 0` 且 `nextAutonomyLevel("frozen") === "suggestion"`。 | 不改生产代码，仅回写现行实现并清理陈旧测试断言。 |
| 2571 | Round 43 — 深度审计（org-governance / state-evidence / interaction-autonomy / scale-ecosystem） / §215 Interaction 层深层缺陷（Autonomy / Dashboard / UX / Proactive-Agent） | P1 | `src/interaction/autonomy/index.ts:172` | 模块内 AUTONOMY_LEVEL_ORDER 不含 frozen，level-manager 含——三处排序互相矛盾 | 已解决 | 旧问题来自多处 autonomy 排序语义不同步；现行实现已把 `frozen` 视为“非正常晋升序列成员”，同时在需要比较整体结果时仍保持其最低自治等级语义。 | 当前 `src/interaction/autonomy/index.ts` 中 `demoteOneLevel()` 对 `frozen` fail-closed 返回自身，`lowestLevel([...])` 在含 `frozen` 时仍会把它排为最低整体结果；定向验证见 `tests/unit/interaction/autonomy/index.test.ts` 新增 “any capability remains frozen -> overall level frozen”。 | 回写现行源码与定向测试依据。 |
| 2572 | Round 43 — 深度审计（org-governance / state-evidence / interaction-autonomy / scale-ecosystem） / §215 Interaction 层深层缺陷（Autonomy / Dashboard / UX / Proactive-Agent） | P1 | `src/interaction/autonomy/index.ts:194` | demoteOneLevel 对 frozen→full_auto——§42 要求 frozen 恢复需人工审批 | 已解决 | 旧 bug 已不在当前实现：`frozen` 不再参与正常降级步进，避免因为 `indexOf("frozen") === -1` 误落到数组尾部。 | 当前 `src/interaction/autonomy/index.ts` 的 `demoteOneLevel()` 首先判断 `current === "frozen"` 并直接返回 `frozen`；定向验证见 `tests/unit/interaction/autonomy/index.test.ts` 的 P1 severity-based demotion 回归。 | 回写现行源码与定向测试依据。 |
| 2573 | Round 43 — 深度审计（org-governance / state-evidence / interaction-autonomy / scale-ecosystem） / §215 Interaction 层深层缺陷（Autonomy / Dashboard / UX / Proactive-Agent） | P1 | `src/interaction/autonomy/promotion-engine/index.ts:16` | incidents>0 全部阻止晋升——未区分 P2/P3 低级别 incident | 已解决 | 旧策略确实按 `incidents > 0` 粗暴阻断；现行 promotion engine 已把 P0/P1 与 P2/P3 分层处理。 | 当前 `src/interaction/autonomy/promotion-engine/index.ts` 对 `P0` 给出冻结/降级阻断，对 `P1` 生成 one-level demotion guidance，而 `P2/P3` 不再单独阻断晋升；定向验证见 `tests/unit/interaction/autonomy/promotion-engine/index.test.ts` 新增的 `P3` 晋升放行与 `P1` 单级降级用例。 | 回写现行源码与定向测试依据。 |
| 2574 | Round 43 — 深度审计（org-governance / state-evidence / interaction-autonomy / scale-ecosystem） / §215 Interaction 层深层缺陷（Autonomy / Dashboard / UX / Proactive-Agent） | P1 | `src/interaction/dashboard/dashboard-websocket-server.ts:106` | WS registerClient 无认证/鉴权——违反§11.7 要求 authenticated principal | 已解决 | 旧审计对应的是早期 `registerClient()` 只收 channel 列表的阶段；现行入口已经把 principal/tenant 和授权范围校验接进注册链。 | 当前 `src/interaction/dashboard/dashboard-websocket-server.ts` 的 `registerClient()` 会调用 `validateCredentials()` 与 `validateSubscriptions()`，并要求 `principal`、`tenantId`、`allowedChannels/allowedTenantIds/allowedTaskIds` 一致；定向验证见 `tests/unit/interaction/dashboard/dashboard-websocket-replay-and-authorization.test.ts`。 | 回写现行源码与定向测试依据。 |
| 2575 | Round 43 — 深度审计（org-governance / state-evidence / interaction-autonomy / scale-ecosystem） / §215 Interaction 层深层缺陷（Autonomy / Dashboard / UX / Proactive-Agent） | P1 | `src/interaction/dashboard/dashboard-websocket-server.ts:208` | pushDelta 按 metric 匹配但 subscriber 按 dashboardId 注册——永远匹配不到 | 已解决（源码修复并补定向测试） | 根因是 metric 路由此前只有读链：`findAffectedClientIds()` 会消费 `metricSubscribers`，但 `registerClient()/updateSubscriptions()` 从不注册 metric，且 metric 分支还会被 channel 匹配再次拦掉，导致 metric-only 订阅永远收不到 delta。 | 本轮在 `src/interaction/dashboard/dashboard-websocket-server.ts` 新增了 `subscribedMetrics`、`registerMetricSubscriptions()`、`unregisterMetricSubscriptions()` 与 `updateMetricSubscriptions()`，并让 metric 路由使用仅检查 tenant visibility 的路径，不再要求额外 channel 命中；定向验证见 `tests/unit/interaction/dashboard/dashboard-websocket-replay-and-authorization.test.ts` 新增的 metric subscription 用例。 | 已改生产代码与定向测试。 |
| 2576 | Round 43 — 深度审计（org-governance / state-evidence / interaction-autonomy / scale-ecosystem） / §215 Interaction 层深层缺陷（Autonomy / Dashboard / UX / Proactive-Agent） | P1 | `src/interaction/dashboard/index.ts:402` | attentionQueue 按 createdAt 升序(最旧在前)——应按紧急度排序 | 已解决 | 旧审计命中了早期只按时间排序的实现；现行排序已经改成先 priority 再 recency。 | 当前 `src/interaction/dashboard/index.ts` 使用 `PRIORITY_ORDER` 先按 `critical/high/normal/low` 排序，再在同优先级内按 `createdAt` 倒序；定向验证见 `tests/unit/interaction/dashboard/dashboard-aggregation-service.test.ts`。 | 回写现行源码与定向测试依据。 |
| 2577 | Round 43 — 深度审计（org-governance / state-evidence / interaction-autonomy / scale-ecosystem） / §215 Interaction 层深层缺陷（Autonomy / Dashboard / UX / Proactive-Agent） | P1 | `src/interaction/dashboard/dashboard-websocket-server.ts:330` | heartbeat 超时标 isConnected=false 但不清理 Map——幽灵连接内存泄漏 | 已解决 | 旧实现确实只标断连不回收；当前心跳路径已经改为在超时后统一走注销回收。 | 当前 `src/interaction/dashboard/dashboard-websocket-server.ts` 的 `performHeartbeat()` 会收集 `timedOutClientIds` 并调用 `unregisterClient()`，同步清理 `connections` 与 `channelSubscribers`。 | 回写现行源码依据。 |
| 2578 | Round 43 — 深度审计（org-governance / state-evidence / interaction-autonomy / scale-ecosystem） / §215 Interaction 层深层缺陷（Autonomy / Dashboard / UX / Proactive-Agent） | P2 | `src/interaction/autonomy/autonomy-audit-service.ts:39` | audit id 用递增序号——多实例重复 id | 已解决 | 原审计停留在早期内存原型；现行审计记录已改用统一 ID 生成器，而非进程内递增计数。 | 当前 `src/interaction/autonomy/autonomy-audit-service.ts` 使用 `newId("audit")` 生成全局唯一、可排序的记录 ID；定向验证见 `tests/unit/interaction/autonomy/autonomy-audit-service.test.ts`。 | 回写现行源码与定向测试依据。 |
| 2579 | Round 43 — 深度审计（org-governance / state-evidence / interaction-autonomy / scale-ecosystem） / §215 Interaction 层深层缺陷（Autonomy / Dashboard / UX / Proactive-Agent） | P2 | `src/interaction/autonomy/historical-metrics-provider.ts:49` | SQL 未按 capabilityId 过滤——返回 agent 级非 capability 级 metrics | 已解决 | 这条是补录时沿用了旧印象；现行 SQL 已按 `agent_id + capability_id + window` 三维过滤。 | 当前 `src/interaction/autonomy/historical-metrics-provider.ts` 的查询条件已包含 `AND e.capability_id = ?`；本轮补充 `tests/unit/interaction/autonomy/historical-metrics-provider.test.ts` 断言查询参数中确实传入 capabilityId。 | 回写现行源码依据，并补齐定向测试。 |
| 2580 | Round 43 — 深度审计（org-governance / state-evidence / interaction-autonomy / scale-ecosystem） / §215 Interaction 层深层缺陷（Autonomy / Dashboard / UX / Proactive-Agent） | P2 | `src/interaction/autonomy/historical-metrics-provider.ts:64` | error_code!=null 算 incident——不等于真正 incident | 已解决（源码修复并补定向测试） | 这里不是“完全没动过”，而是修到一半：incident 计数此前已切成按 `failed` status，但 `lastIncidentAt` 仍沿用 `last_error_code` 查找，相关测试也继续按旧语义断言，导致复审证据与运行语义分裂。 | 本轮将 `src/interaction/autonomy/historical-metrics-provider.ts` 的 incident 语义完全收敛到 failed execution：`incidents` 与 `lastIncidentAt` 都只看 `status === "failed"`；`tests/unit/interaction/autonomy/historical-metrics-provider.test.ts` 已同步覆盖“成功但带 error_code 不算 incident”和“最近 incident 时间来自最近 failed row”。 | 已改生产代码与定向测试。 |
| 2581 | Round 43 — 深度审计（org-governance / state-evidence / interaction-autonomy / scale-ecosystem） / §215 Interaction 层深层缺陷（Autonomy / Dashboard / UX / Proactive-Agent） | P2 | `src/interaction/ux/conversation-history-service.ts:121` | memoryLayer!=="layer_3" 才持久化，默认 "layer_3"——条件矛盾永不持久化 | 已解决 | 旧逻辑的确自相矛盾；现行实现已经改成“允许 long-term 才落 layer_3，并以 layer_3 为默认”。 | 当前 `src/interaction/ux/conversation-history-service.ts` 会在 `shouldPersistToLongTermMemory(options)` 为真且 `memoryLayer === "layer_3" || !memoryLayer` 时持久化；定向验证见 `tests/unit/platform/interaction/ux/conversation-history-service.test.ts`。 | 回写现行源码与定向测试依据。 |
| 2582 | Round 43 — 深度审计（org-governance / state-evidence / interaction-autonomy / scale-ecosystem） / §215 Interaction 层深层缺陷（Autonomy / Dashboard / UX / Proactive-Agent） | P2 | `src/interaction/ux/conversation-history-service.ts:199` | recall 不过滤 tenantId——跨租户会话泄露 | 已解决 | 旧实现缺 tenant-aware recall；当前查询层与结果层都已加租户边界。 | 当前 `src/interaction/ux/conversation-history-service.ts` 在 `getSession()` / `listUserSessions()` 调用 `memoryService.recall()` 时都会传入 `tenantId`，并在反序列化后再次校验 `session.tenantId === tenantId`；定向验证见 `tests/unit/platform/interaction/ux/conversation-history-service.test.ts`。 | 回写现行源码与定向测试依据。 |
| 2583 | Round 43 — 深度审计（org-governance / state-evidence / interaction-autonomy / scale-ecosystem） / §215 Interaction 层深层缺陷（Autonomy / Dashboard / UX / Proactive-Agent） | P2 | `src/interaction/dashboard/index.ts:183` | successRate 分母含 pending/in_progress——导致 rate 偏低 | 已解决 | 原审计命中的是旧 `total` 分母；现行聚合已经把成功率收敛到 `completed + failed` 的 settled 集。 | 当前 `src/interaction/dashboard/index.ts` 中 `buildAgentCards()` 使用 `settled = completed + failures` 作为分母，明确排除了 `pending/in_progress`；相关聚合回归位于 `tests/unit/interaction/dashboard/dashboard-aggregation-service.test.ts`。 | 回写现行源码与定向测试依据。 |
| 2584 | Round 43 — 深度审计（org-governance / state-evidence / interaction-autonomy / scale-ecosystem） / §215 Interaction 层深层缺陷（Autonomy / Dashboard / UX / Proactive-Agent） | P2 | `src/interaction/ux/ux-event-tracking-service.ts:162` | abTestAssignments 以 userId 为 key——多并行 A/B test 互相覆盖 | 已解决 | 旧实现确实只有 `userId` 单键；现行分配键已升级为 `userId:testId` 复合键。 | 当前 `src/interaction/ux/ux-event-tracking-service.ts` 在 `assignABTest()` / `getABTestAssignment()` 中都使用 `${userId}:${testId}` 作为 key；定向验证见 `tests/unit/interaction/ux/ux-event-tracking-service-edge-cases.test.ts`。 | 回写现行源码与定向测试依据。 |
| 2585 | Round 43 — 深度审计（org-governance / state-evidence / interaction-autonomy / scale-ecosystem） / §216 Scale-Ecosystem 深层缺陷（Tenant / Marketplace / SLA / Multi-Region） | P0 | `src/scale-ecosystem/tenant-platform/tenant-platform-service.ts:267` | addWorkspaceMembership 未验证调用者权限——任意用户可向任意 workspace 添加成员 | 已解决（源码修复并补定向测试） | 根因是此前所谓“鉴权”根本没把发起人身份带进来，只是回读 `workspace.ownerId` 对应 membership 是否存在；这无法把授权决策绑定到真实 caller，因此属于伪修复。 | 本轮为 `AddWorkspaceMembershipInput` 增加了 `callerUserId`，并在 `src/scale-ecosystem/tenant-platform/tenant-platform-service.ts` 中按真实 caller 的 workspace membership 角色(`owner/admin`) 做授权；`src/sdk/cli/tenant-platform.ts` 与 CLI env loader 已同步透传 caller。定向验证见 `tests/unit/scale-ecosystem/tenant-platform/tenant-platform-service.test.ts` 与 `tests/integration/scale-ecosystem/tenant-platform-integration.test.ts` 的授权成功/拒绝用例。 | 已改生产代码、CLI 边界与定向测试。 |
| 2586 | Round 43 — 深度审计（org-governance / state-evidence / interaction-autonomy / scale-ecosystem） / §216 Scale-Ecosystem 深层缺陷（Tenant / Marketplace / SLA / Multi-Region） | P0 | `src/scale-ecosystem/tenant-platform/tenant-platform-service.ts:342` | addOrganizationMembership 无鉴权——任意用户可自添 admin 角色，权限提升 | 已解决（源码修复并补定向测试） | 根因有两层：旧实现既没有 `callerUserId`/principal 输入，又错误依赖并不存在的 `organization.ownerId` 语义，所以既无法识别真实调用者，也没有权威的组织 owner/admin 模型。 | 本轮将 `CreateOrganizationInput` 补为显式 `ownerId`，`createOrganization()` 会同时写入 owner membership 作为权威起点；`addOrganizationMembership()` 改为基于 `callerUserId` 查找组织 membership 并校验 `owner/admin`，且只有 owner 能再授予 owner。`src/sdk/cli/tenant-platform.ts`、`remaining-cli-env-loaders.ts`、`remaining-cli-env-support.ts` 已同步接线；验证见 `tests/unit/scale-ecosystem/tenant-platform/tenant-platform-service.test.ts`、`tests/integration/scale-ecosystem/tenant-platform-integration.test.ts` 与 `tests/integration/platform/security/data-plane-flow-boundary.test.ts` 所在的定向测试批次。 | 已改生产代码、CLI/配置边界与定向测试。 |
| 2587 | Round 43 — 深度审计（org-governance / state-evidence / interaction-autonomy / scale-ecosystem） / §216 Scale-Ecosystem 深层缺陷（Tenant / Marketplace / SLA / Multi-Region） | P0 | `src/scale-ecosystem/tenant-platform/data-plane-flow-service.ts:611` | scope tenantId/orgId=null 时跳过边界检查——可绕过租户隔离 | 已解决 | 旧实现把 null scope 当成“无约束”；现行边界检查已经把 null/non-null 不对称本身视为越界。 | 当前 `src/scale-ecosystem/tenant-platform/data-plane-flow-service.ts` 的 `assertScopeCompatibility()` 会对 tenant/org/workspace 三层 scope 都显式处理 `null !== non-null` 场景并抛出边界错误；定向验证见 `tests/integration/platform/security/data-plane-flow-boundary.test.ts`。 | 回写现行源码与定向测试依据。 |
| 2588 | Round 43 — 深度审计（org-governance / state-evidence / interaction-autonomy / scale-ecosystem） / §216 Scale-Ecosystem 深层缺陷（Tenant / Marketplace / SLA / Multi-Region） | P1 | `src/scale-ecosystem/marketplace/pack-security-service.ts:202` | runStaticAnalysis 仅正则扫描 sourceUri 字符串——实际源代码未扫描 | 已解决（源码修复并补定向测试） | 根因是旧实现把 `sourceUri` 当成源码文本直接做正则匹配，`inline:` 之外的 `file:` / `data:` / 本地路径根本没有读取真实源码，远端不可读 URI 还会被当成“扫描过”。 | 本轮新增真实源码解析分支：`inline:` 取内联源码、`file:`/本地路径读取文件、`data:` 解码后再扫描；对拿不到源码内容的 URI 改为 fail-closed，直接产出 `PKG003` 阻断通过，不再把 URI 字符串误当源码。依据见 `src/scale-ecosystem/marketplace/pack-security-service.ts` 与 `tests/unit/scale-ecosystem/marketplace/pack-security-service.test.ts` 中“scans actual source content from file URIs / fails closed when source content is unavailable for static analysis” 两条定向用例。 | 已改生产代码与定向测试。 |
| 2589 | Round 43 — 深度审计（org-governance / state-evidence / interaction-autonomy / scale-ecosystem） / §216 Scale-Ecosystem 深层缺陷（Tenant / Marketplace / SLA / Multi-Region） | P1 | `src/scale-ecosystem/marketplace/pack-security-service.ts:238` | 危险 capability ≤3 不告警——检测阈值过高 | 已解决 | 旧逻辑把危险 capability 的告警阈值设成 `> 3`，导致只申请 1-3 个高风险能力时完全不出告警，实际风险被静默吞掉。 | 当前 `checkCapabilitySafety()` 已改为只要存在危险 capability 就产出 `CAP001`；定向依据除现行源码外，还包括 `tests/unit/scale-ecosystem/marketplace/pack-security-service.test.ts` 新增的 “flags a single dangerous capability”。 | 回写现行源码与定向测试依据。 |
| 2590 | Round 43 — 深度审计（org-governance / state-evidence / interaction-autonomy / scale-ecosystem） / §216 Scale-Ecosystem 深层缺陷（Tenant / Marketplace / SLA / Multi-Region） | P1 | `src/scale-ecosystem/sla-engine/sla-operations-service.ts:84` | 缺 maxExecutionTimeoutRate/minDependencyAvailability——SLA 违约检测不完整 | 已解决 | 旧审计对应的是 breach observation 还没带这两个约束信号的阶段。 | 当前 `src/scale-ecosystem/sla-engine/sla-operations-service.ts` 在构造 `detectSlaBreach(...)` 的 observation 时已显式注入 `maxExecutionTimeoutRate` 与 `minDependencyAvailability`。 | 回写现行源码依据。 |
| 2591 | Round 43 — 深度审计（org-governance / state-evidence / interaction-autonomy / scale-ecosystem） / §216 Scale-Ecosystem 深层缺陷（Tenant / Marketplace / SLA / Multi-Region） | P1 | `src/scale-ecosystem/sla-engine/sla-operations-service.ts:111` | preemptionCapApplied 条件恒 true——标志无法正确判断 | 已解决 | 旧 bug 的根因是用 `selectedPriority <= maxPriority` 这种恒宽松条件判断，导致低优先级 tier 也被标成“已应用 preemption cap”；现行实现已改成只有存在更高优先级 tier 可供抢占时才置真。 | 当前 `src/scale-ecosystem/sla-engine/sla-operations-service.ts` 使用 `selectedTier.preemptionPriority < maxPriority` 计算 `preemptionCapApplied`。 | 回写现行源码依据。 |
| 2592 | Round 43 — 深度审计（org-governance / state-evidence / interaction-autonomy / scale-ecosystem） / §216 Scale-Ecosystem 深层缺陷（Tenant / Marketplace / SLA / Multi-Region） | P1 | `src/scale-ecosystem/multi-region/cdc-replication-service.ts:98` | checkpoint/queue 纯内存——重启后复制进度丢失，违反 RPO | 已解决（源码修复并补定向测试） | 根因是旧 `CDCReplicationService` 只把 checkpoint 和待复制 batch 放在进程内 `Map` 里，进程重启后既丢进度也丢待重试队列；更隐蔽的一层是重启后即便恢复 source events，也会因为只看旧 checkpoint 而重新切出已排队 batch。 | 本轮为 CDC 服务新增可持久化状态文件装载/落盘能力，并在 coordinator 默认接到 `data/runtime/multi-region/cdc-replication-state.json`；同时 `prepareBatch()` 改为同时避开“已持久化 checkpoint”和“已排队但未确认的最高 sequence”，不再重启后重复切批。依据见 `src/scale-ecosystem/multi-region/cdc-replication-service.ts` 与 `tests/unit/scale-ecosystem/multi-region/cdc-replication-service.test.ts` 中 “persists checkpoints across service restart” 和 “persists pending queues ... across restart”。 | 已改生产代码与定向测试。 |
| 2593 | Round 43 — 深度审计（org-governance / state-evidence / interaction-autonomy / scale-ecosystem） / §216 Scale-Ecosystem 深层缺陷（Tenant / Marketplace / SLA / Multi-Region） | P1 | `src/scale-ecosystem/multi-region/data-replicator/index.ts:170` | eventId = Date.now()+Math.random()——高并发 ID 冲突 | 已解决 | 旧实现把毫秒级时间戳与 `Math.random()` 拼接成 eventId，本质上既无全局唯一性保证，也没有并发冲突边界。 | 当前 `recordEvent()` 已改用 `crypto.randomUUID()` 生成 eventId；定向依据见 `tests/unit/scale-ecosystem/multi-region/data-replicator/index.test.ts` 中 “generates unique event IDs under rapid writes”。 | 回写现行源码与定向测试依据。 |
| 2594 | Round 43 — 深度审计（org-governance / state-evidence / interaction-autonomy / scale-ecosystem） / §216 Scale-Ecosystem 深层缺陷（Tenant / Marketplace / SLA / Multi-Region） | P1 | `src/scale-ecosystem/multi-region/data-replicator/index.ts:244` | flush checkpoint sequence 为局部计数器——重启归零致重复消费 | 已解决 | 根因是旧 checkpoint 更新只写本次 flush 内的局部 `lastSequence`，没有把它叠加到既有持久 sequence 上，后续 flush 会从局部 0 重新起算。 | 当前 `flush()` 已改为以 `existingCheckpoint.sequenceNumber` 为 base，再叠加本次确认的 `lastSequence`；定向依据见 `tests/unit/scale-ecosystem/multi-region/data-replicator/index.test.ts` 中 “keeps checkpoint sequence cumulative across flushes”。 | 回写现行源码与定向测试依据。 |
| 2595 | Round 43 — 深度审计（org-governance / state-evidence / interaction-autonomy / scale-ecosystem） / §216 Scale-Ecosystem 深层缺陷（Tenant / Marketplace / SLA / Multi-Region） | P1 | `src/scale-ecosystem/multi-region/cdc-replication-service.ts:209` | recordFailure 仅记日志无 retry 入队——失败 batch 静默丢弃 | 已解决（源码修复并补定向测试） | 旧实现失败路径只有日志，没有把 batch 保持在可重试队列里；本轮复核时还发现如果直接再次 `enqueue` 会制造重复 batch，所以需要同时修复“无重试”和“重复入队”两个层面。 | 当前 `recordFailure()` 已改为 `requeueBatch()`：失败 batch 若已在队列中则移动到尾部继续等待重试，若不存在才补入队，并随持久化状态一并落盘；定向依据见 `tests/unit/scale-ecosystem/multi-region/cdc-replication-service.test.ts` 中 “persists pending queues and keeps failed batch retryable across restart”。 | 已改生产代码与定向测试。 |
| 2596 | Round 43 — 深度审计（org-governance / state-evidence / interaction-autonomy / scale-ecosystem） / §216 Scale-Ecosystem 深层缺陷（Tenant / Marketplace / SLA / Multi-Region） | P1 | `src/scale-ecosystem/multi-region/region-health-check-service.ts:347` | degraded 不重置 consecutiveFailures——恢复后仍累积旧计数触发误 failover | 已解决 | 根因是旧状态更新逻辑只在完全 healthy 时清零 failure count，导致从 `unhealthy` 恢复到 `degraded` 的 region 仍背着旧失败计数，后续会被误判为需要 failover。 | 当前 `updateHealthState()` 已在 `healthy` 和 `degraded` 两种恢复态下都重置 `consecutiveFailures`；定向依据见 `tests/unit/scale-ecosystem/multi-region/region-health-check-service.test.ts` 中 “resets stale failures when region recovers to degraded”。 | 回写现行源码与定向测试依据。 |
| 2597 | Round 43 — 深度审计（org-governance / state-evidence / interaction-autonomy / scale-ecosystem） / §216 Scale-Ecosystem 深层缺陷（Tenant / Marketplace / SLA / Multi-Region） | P1 | `src/scale-ecosystem/tenant-platform/tenant-platform-service.ts:312` | createOrganization 校验条件恒 false——校验被绕过 | 已解决 | 旧 bug 确实来自错误地把待创建组织 ID 与 tenant 自身组织 ID 通过 fallback 混成同一个值比较，导致条件恒真。 | 当前 `src/scale-ecosystem/tenant-platform/tenant-platform-service.ts` 在 `createOrganization()` 中使用单独的 `newOrgId` 与 `tenant.organizationId` 比较，已不再是恒 false/恒真式绕过。 | 回写现行源码依据。 |
| 2598 | Round 43 — 深度审计（org-governance / state-evidence / interaction-autonomy / scale-ecosystem） / §216 Scale-Ecosystem 深层缺陷（Tenant / Marketplace / SLA / Multi-Region） | P1 | `src/scale-ecosystem/marketplace/marketplace-governance-service.ts:501` | reviewRequired=0 的 internal 包也必须有 review——与文档矛盾 | 已解决（源码修复并补定向测试） | 根因有两层：最早的发布门禁把所有包都绑死到 review；后续虽加了 `requiresReview`，但代码仍默认 `reviewRecord/reviewId` 一定存在，导致 internal + `reviewRequired=0` 的真实无评审发布路径在运行时仍会因空 review 崩溃。 | 当前 `publishPackage()` 已改为：只有 `trustLevel !== "internal" || reviewRequired === 1` 才强制评审；对免评审 internal 包会生成系统级 review exemption 记录，既满足文档语义，也保持 publication 的 `reviewId` 引用完整。定向依据见 `tests/unit/scale-ecosystem/marketplace/marketplace-governance-service.test.ts` 中 “publishPackage publishes internal package without signature”。 | 已改生产代码与定向测试。 |
| 2599 | Round 43 — 深度审计（org-governance / state-evidence / interaction-autonomy / scale-ecosystem） / §216 Scale-Ecosystem 深层缺陷（Tenant / Marketplace / SLA / Multi-Region） | P2 | `src/scale-ecosystem/marketplace/marketplace-governance-service.ts:590` | revokePublication 未检查当前 status——已 revoked 可重复 revoke | 已解决 | 根因是旧实现撤销时只按 `publicationId` 查记录，不校验当前状态机位置，导致已失效 publication 可重复执行 revoke。 | 当前 `revokePublication()` 已显式拦截 `revoked/deprecated/retired` 状态并返回 `marketplace.publication_already_inactive`；定向依据见 `tests/unit/scale-ecosystem/marketplace/marketplace-governance-service.test.ts` 中 “revokePublication rejects already inactive publication”。 | 回写现行源码与定向测试依据。 |
| 2600 | Round 43 — 深度审计（org-governance / state-evidence / interaction-autonomy / scale-ecosystem） / §216 Scale-Ecosystem 深层缺陷（Tenant / Marketplace / SLA / Multi-Region） | P2 | `src/scale-ecosystem/marketplace/certification/index.ts:12` | 不检查 approvedAt 存在——status=approved 但 approvedAt=null 视为已认证 | 已解决（源码修复并补定向测试） | 根因是 agent certification 分支已经补了 `approvedAt` 约束，但 pack certification 分支仍只看 `status`，导致 pack 侧存在同类认证穿透。 | 当前 `CertificationGate.checkPackCertification()` 已在 `status === "approved"` 且 `approvedAt == null` 时阻断并打上 `certification_invalid`；定向依据见 `tests/unit/scale-ecosystem/marketplace/certification/certification.test.ts` 中 “blocks approved pack without approvedAt”。 | 已改生产代码与定向测试。 |
| 2601 | Round 43 — 深度审计（org-governance / state-evidence / interaction-autonomy / scale-ecosystem） / §216 Scale-Ecosystem 深层缺陷（Tenant / Marketplace / SLA / Multi-Region） | P2 | `src/scale-ecosystem/marketplace/publisher/index.ts:16` | reputationScore>=0.4 硬编码——sandboxed publisher 也可发布 | 已解决 | 旧实现把发布准入错误收敛成 reputation 阈值，完全没有把 `sandboxed` 当作硬阻断 trust state。 | 当前 `canPublisherReleaseArtifact()` 已改为仅允许 `verified/enterprise` 且 artifact type 在白名单内；定向依据见 `tests/unit/scale-ecosystem/marketplace/publisher/index.test.ts` 中关于 sandboxed publisher 在 `0.4` 与 `0.41` 两个边界值都被拒绝的用例。 | 回写现行源码与定向测试依据。 |
| 2602 | Round 43 — 深度审计（org-governance / state-evidence / interaction-autonomy / scale-ecosystem） / §216 Scale-Ecosystem 深层缺陷（Tenant / Marketplace / SLA / Multi-Region） | P2 | `src/scale-ecosystem/marketplace/catalog/index.ts:63` | validateListingDependencies 无循环检测——循环依赖致安装无限递归 | 已解决 | 旧问题的根因是 dependency validation 只做缺失项/兼容性检查，不把传递依赖图作为图结构处理，自然也不会把循环依赖纳入 invalid 条件。 | 当前 `validateListingDependencies()` 已做 DFS 递归栈检测，并把 `cyclicDependencies.length > 0` 纳入 `valid` 判定；定向依据见 `tests/unit/scale-ecosystem/marketplace/catalog/catalog.test.ts` 中 “validateListingDependencies rejects cyclic dependencies”。 | 回写现行源码与定向测试依据。 |
| 2603 | Round 43 — 深度审计（org-governance / state-evidence / interaction-autonomy / scale-ecosystem） / §216 Scale-Ecosystem 深层缺陷（Tenant / Marketplace / SLA / Multi-Region） | P2 | `src/scale-ecosystem/multi-region/region-health-check-service.ts:240` | checkAllRegions 串行 await——大量 region 延迟线性增长 | 已解决 | 旧实现逐 region 串行 `await checkRegion()`，总体耗时与 region 数量线性相乘；region 越多，巡检窗口越长。 | 当前 `checkAllRegions()` 已改为 `Promise.all([...configs.keys()].map(checkRegion))` 并行执行；定向依据见 `tests/unit/scale-ecosystem/multi-region/region-health-check-service.test.ts` 中 “checkAllRegions runs checks in parallel”。 | 回写现行源码与定向测试依据。 |
| 2604 | Round 43 — 深度审计（org-governance / state-evidence / interaction-autonomy / scale-ecosystem） / §216 Scale-Ecosystem 深层缺陷（Tenant / Marketplace / SLA / Multi-Region） | P2 | `src/scale-ecosystem/multi-region/failover-controller/index.ts:36` | failover 取 candidateRegionIds[0]——未考虑健康状态和复制 lag | 已解决（当前实现采取 fail-closed） | 旧实现的确存在盲选首候选的问题；现行控制器没有健康/lag 排序器时不再退回到 `candidateRegionIds[0]`，而是只接受显式 `preferredRegionId`，否则直接拒绝 failover。 | 当前 `src/scale-ecosystem/multi-region/failover-controller/index.ts` 在无合法 `preferredRegionId` 时返回 `targetRegionId: null` / `multi_region.no_candidate_available`，已经消除了 blind pick。 | 回写现行源码依据，并明确当前是保守 fail-closed 而非智能选优。 |
