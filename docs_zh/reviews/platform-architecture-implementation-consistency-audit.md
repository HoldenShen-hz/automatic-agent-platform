# 00-platform-architecture.md 实现一致性审计重新复核底稿

> 重开复核日期：2026-04-28
> 处理原则：保留原审计问题明细，不删除问题描述；上一版“已修复/收口证据”结论全部撤回，先统一回退为 `未修复` 与 `待补充`，再逐条真实修复；每个已修复项都必须写明直接证据与根因，禁止只写泛化 remediation 结论。
> 重要说明：`src/platform/architecture/implementation-consistency-closure.ts` 只能证明“编号存在”和“历史索引存在”，不能作为已修复证明。

## 0. 逐项复核与收口依据索引

本报告中的每个问题行均保留原始偏差描述，并通过 `ImplementationConsistencyClosureRegistry:<问题编号>` 保留历史索引。复核结论已纠正：`ImplementationConsistencyClosureRegistry` 不是收口记录，只是问题编号目录；未做直接核验的条目一律视为 `未修复`，只有在补齐直接代码、contract、ADR、spec 或定向测试证据并写明根因后，才能标记为 `已修复`。本节仅保留编号前缀到历史索引位置的映射，历史索引不能单独作为收口依据。

| 编号范围 | 问题类别 | 当前结论 | 复核备注 |
| --- | --- | --- | --- |
| C-1..C-7 | 代码运行时与架构偏差 | 已修复 | 已完成直接代码与定向测试复核；后续如发现回归需重新打开 |
| T-1..T-56 | Contract 文档与架构偏差 | 部分已修复 | 已完成 T-1..T-56 的直接 contract 正文复核；其余不得再用 remediation 小节泛化证明收口 |
| A-1..A-37 | ADR 与架构偏差 | 未修复 | 历史索引待逐条重审，不能再以 supersession ADR 泛化证明收口 |
| G-1..G-9 | 配置/代码与架构偏差 | 已修复 | 已完成直接代码/配置复核；后续如发现回归需重新打开 |
| O-1..O-24 | org-governance 与架构偏差 | 已修复 | 已完成 O-1..O-24 的直接代码与定向测试复核；后续如发现回归需重新打开 |
| S-1..S-20 | scale-ecosystem 与架构偏差 | 已修复 | 已完成直接代码与定向测试复核；后续如发现回归需重新打开 |
| M-1..M-20 | ops-maturity 与架构偏差 | 已修复 | 已完成直接代码与定向测试复核；后续如发现回归需重新打开 |
| F-1..F-25 | OAPEFLIR spec 与架构偏差 | 未修复 | 历史索引待逐条重审，不能再以 compatibility override 泛化证明收口 |
| I-1..I-20 | interaction 与架构偏差 | 已修复 | 已完成直接代码与定向测试复核；后续如发现回归需重新打开 |
| D-1..D-20 | domains / SDK 与架构偏差 | 已修复 | 已完成直接代码与定向测试复核；后续如发现回归需重新打开 |

复核门禁：不再保留任何“总括性 closure test” 作为收口依据；是否已修复只能由对应目标文件中的直接实现、直接契约文本或直接定向测试逐条证明，禁止再把历史 registry 当作“全部已修复”的证明。

## 1. 代码 vs 架构（7项）

| #   | 严重度 | 位置                                              | 偏差描述 | 状态 | 收口证据 |
| --- | ------ | ------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --- | --- |
| C-1 | HIGH   | `src/platform/orchestration/harness/index.ts`     | HarnessRunStatus 仅7状态（idle/planning/executing/paused/sleeping/completed/failed），架构§25.8定义13状态；缺 initializing/awaiting_approval/compensating/rolling_back/suspended/draining 等9个；多出 sleeping/idle/planning 3个非规范状态 | 已修复 | 当前 `HarnessRunStatus` 已与 `§25.8` 的 13 态 canonical 模型对齐：`src/platform/orchestration/harness/index.ts`、`src/platform/contracts/executable-contracts/index.ts`、`src/platform/contracts/executable-contracts/schemas.ts`；定向验证：`tests/unit/platform/contracts/executable-contracts/index.test.ts`、`tests/unit/platform/execution/runtime-state-machine-transitions.test.ts` |
| C-2 | HIGH   | 同上                                              | HarnessRun 接口无 `planGraphBundle` 字段，runLoop 从不生成/校验 PlanGraphBundle，架构§25要求每次运行持有不可变执行计划图 | 已修复 | `src/platform/orchestration/harness/index.ts` 已为 `HarnessRun` 增加 `planGraphBundle` 并在 `createRun()` 中调用 `createInitialPlanGraphBundle(...)`；定向验证：`tests/unit/platform/orchestration/harness/index.test.ts`、`node --import tsx --test tests/unit/platform/orchestration/harness/index.test.ts` |
| C-3 | HIGH   | `src/platform/execution/runtime-state-machine.ts` | RuntimeStateMachine 将 `compensating` 列为 NodeRun 合法可达状态，架构§14.3明确将补偿建模为独立 CompensationRun 而非 NodeRun 子状态 | 已修复 | 已从 `NodeRun` 迁移矩阵移除 `reconciling -> compensating`；补偿继续保留在 side-effect / compensation 轨道。直接证据：`src/platform/execution/runtime-state-machine.ts`、`tests/unit/platform/execution/runtime-state-machine.test.ts`、`tests/unit/platform/execution/runtime-state-machine-transitions.test.ts` |
| C-4 | MED    | 同上                                              | NodeRun 代码新增 `blocked`/`queued` 两个非规范状态，架构 NodeRun 状态枚举中不存在 | 已修复 | 已从 `NodeRunStatus` 类型、Zod schema、RuntimeStateMachine 和 PlanGraphHarnessRuntime 移除 `blocked/queued`；旧 `§14.10` 文本已收敛到 `§25.8` canonical 状态。直接证据：`src/platform/contracts/executable-contracts/index.ts`、`src/platform/contracts/executable-contracts/schemas.ts`、`src/platform/execution/runtime-state-machine.ts`、`src/platform/orchestration/harness/runtime/plan-graph-harness-runtime.ts`、`docs_zh/architecture/00-platform-architecture.md` |
| C-5 | MED    | 同上                                              | `assertTransitionAllowed` 对 from===to 的自转移静默放行，绕过架构要求的 CAS/lease/fencing token 校验 | 已修复 | `src/platform/execution/runtime-state-machine.ts` 已在 `fromStatus === toStatus` 时抛出 `runtime_state_machine.noop_transition_denied`；定向验证：`tests/unit/platform/execution/runtime-state-machine.test.ts`、`node --import tsx --test tests/unit/platform/execution/runtime-state-machine.test.ts` |
| C-6 | MED    | `src/platform/contracts/index.ts`                 | barrel re-export 4个已废弃合约类型（LegacyRolloutContract 等），架构v4.0已移除对应概念 | 已修复 | 已删除根 barrel 中 `legacy*Contract` 导出；定向验证：`src/platform/contracts/index.ts`、`tests/unit/platform/contracts/index.test.ts`、`node --import tsx --test tests/unit/platform/contracts/index.test.ts` |
| C-7 | LOW    | `src/platform/execution/budget-allocator.ts`      | `settle()` 硬编码 `hardCapSatisfied: true`，跳过架构§18要求的实际硬上限校验 | 已修复 | `src/platform/execution/budget-allocator.ts` 现按 reservation/ledger/actualAmount 重新计算 `hardCapSatisfied`；定向验证：`tests/unit/platform/execution/budget-allocator.test.ts`、`tests/unit/platform/execution/budget-allocator-comprehensive.test.ts`、`node --import tsx --test tests/unit/platform/execution/budget-allocator.test.ts tests/unit/platform/execution/budget-allocator-comprehensive.test.ts` |

## 2. Contract 文档 vs 架构（11项）

| #    | 严重度 | Contract 文件                         | 偏差描述 | 状态 | 收口证据 |
| ---- | ------ | ------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --- | --- |
| T-1 | HIGH   | `runtime_state_machine_contract`      | ExecutionStatus 用 running/paused/cancelled/completed/failed 5态，架构 NodeRun 用 pending/ready/running/blocked/succeeded/failed/skipped/cancelled/timed_out 9态；WorkflowStatus 6态 vs HarnessRun 13态 | 已修复 | `docs_zh/contracts/runtime_state_machine_contract.md` 已明确声明历史 task/workflow 状态机仅为 legacy 说明，并把 v4.3 权威边界收敛到 `harness-run-contract.md`、`node-run-attempt-receipt-contract.md`、`side-effect-reconciliation-contract.md` 与 `RuntimeStateMachine.transition(command)`；直接验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |
| T-2 | HIGH   | `side-effect-reconciliation-contract` | 状态机 pending→executing→reconciling→settled 4步线性，架构§14.11 要求 pending→claimed→executing→awaiting_confirmation→settled/compensating 含分支 | 已修复 | `docs_zh/contracts/side-effect-reconciliation-contract.md` 已改为 v4.3 canonical side-effect / reconciliation / compensation contract，并通过 remediation 小节显式收口 `T-2`；运行时权威推进继续收敛到 `RuntimeStateMachine.transition(command)`；直接验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |
| T-3 | MED    | `budget-ledger-contract`              | 用 "settle" 动词描述预算消费，架构§18统一用 "consume"；resourceKind 枚举仅 token/api_call/compute，架构额外定义 storage/bandwidth/memory | 已修复 | `docs_zh/contracts/budget-ledger-contract.md` 已重写为 v4.3 canonical `BudgetLedger / BudgetReservation / BudgetSettlement` contract，并通过 remediation 小节显式收口 `T-3`；当前 contract 将旧术语降为 legacy/projection 输入，不再作为新实现入口；直接验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |
| T-4 | MED    | `version-lock-contract`               | 仅支持3种锁定策略（pinned/floating/range），架构§22.4定义4种含 digest-locked | 已修复 | `docs_zh/contracts/version-lock-contract.md` 已收敛到 v4.3 canonical `RunVersionLock / ArtifactVersionLockSet`，并通过 remediation 小节显式收口 `T-4`；旧 `pinned/floating/range` 仅保留为历史输入语义，不再作为新入口定义；直接验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |
| T-5 | MED    | `event-envelope-contract`             | 缺少架构 ContractEnvelope 要求的5个必需字段：schema_version/idempotency_key/causation_id/partition_key/ttl | 已修复 | `docs_zh/contracts/event-envelope-contract.md` 已把 `schema_version / idempotency_key / causation_id / partition_key / ttl` 写入 `EventEnvelope` canonical 最小字段与规则；根因：旧文档只覆盖事件事实存储字段，遗漏了 envelope 层的幂等、分区与生命周期元数据；定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |
| T-6 | MED    | `task_lease_and_fencing_contract`     | 使用已废弃术语 execution_id，架构v4.0统一为 node_run_id | 已修复 | `docs_zh/contracts/task_lease_and_fencing_contract.md` 已把 `LeaseGrant / QueueDispatchRecord / LeaseAuditRecord / LeaseReconciliationRecord` 的 canonical 关联键收敛到 `node_run_id` / `attempt_id`，并把 `execution_id` 降为 legacy 映射；根因：文档沿用了 v3 execution-centric 队列/租约术语，没有随 `NodeRun` 权威对象迁移同步更新；定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |
| T-7 | MED    | `harness-run-contract`                | 合约§45.13定义6状态 vs 架构§25.8定义13状态，架构文档内部也不一致（§25.4列7态 vs §25.8列13态） | 已修复 | `docs_zh/contracts/harness-run-contract.md` 现已把 `HarnessRunStatus` 收敛到 13 态 canonical 模型：`created / admitted / planning / ready / running / pausing / paused / resuming / replanning / compensating / completed / failed / aborted`，并通过 remediation 小节显式收口 `T-7`；直接验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |
| T-8 | MED    | `plan-graph-contract`                 | 合约将 PlanGraph 定义为可变（支持 appendNode），架构§25明确要求 PlanGraphBundle 为不可变快照 | 已修复 | `docs_zh/contracts/plan-graph-patch-contract.md` 已新增“不可变性约束”正文，明确 `PlanGraphBundle` / `PlanGraph` 为不可变快照，禁止 `appendNode/removeNode/updateNode` 等原地修改；根因：早期文档把 in-memory builder 的编辑语义误写成 runtime canonical contract；定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |
| T-9 | MED    | `approval-routing-contract`           | 缺少架构§31要求的 escalation_chain 和 timeout_auto_action 字段 | 已修复 | `docs_zh/contracts/approval_and_hitl_contract.md` 已在 `ApprovalRequest` 中补入 `escalation_chain`、`timeout_auto_action` 与 `ApprovalEscalationHop` 最小字段；根因：旧审批 contract 把“超时策略”压缩成单值 UI 语义，没有建模控制平面自动动作与逐级升级链；定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |
| T-10 | LOW    | `model-routing-contract`              | 路由策略枚举 cost_optimized/latency_optimized/quality_optimized 3种，架构§19定义5种含 compliance_constrained/hybrid | 已修复 | `docs_zh/contracts/model_gateway_routing_contract.md` 已为 `ModelRouteRequest.routingStrategy` 补齐 `cost_optimized / latency_optimized / quality_optimized / compliance_constrained / hybrid` 五种规范枚举，并增加后两者的治理约束；根因：旧文档只覆盖性能/成本三目标，没有把合规约束与多目标折中写进 canonical request；定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |
| T-11 | LOW    | `domain-recipe-contract`              | recipe 结构缺少架构§38要求的 risk_profile_ref 和 guardrail_overlay 引用 | 已修复 | `docs_zh/contracts/domain_descriptor_and_onboarding_contract.md` 已定义 `DomainRecipe` 最小字段，并把 `risk_profile_ref` 与 `guardrail_overlay` 设为必填；根因：早期文档只把 recipe 当作 onboarding 模板，没有把风险绑定和 guardrail 叠加层视作一等 contract；定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |

## 3. ADR vs 架构（12项）

| #    | 严重度 | ADR         | 偏差描述 | 状态 | 收口证据 |
| ---- | ------ | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ | --- | --- |
| A-1 | HIGH   | ADR-016     | 将 OAPEFLIR 定义为核心执行编排器（OapeflirLoopService 作为运行入口），直接违反架构v4.3核心不变量：HarnessRuntime 是唯一执行入口点，OAPEFLIR 仅为认知循环框架 | 已修复 | 直接证据：`docs_zh/adr/016-oapeflir-loop-model.md` 已明确 `HarnessRuntime` 是唯一执行入口，OAPEFLIR 只产出 `oapeflir.view.* / oapeflir.rationale.*` 投影，并移除了 `OapeflirLoopService` 作为独立 runtime 入口的 canonical 叙述。根因：认知循环模型在早期草案中同时承担了运行时和解释层职责。定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |
| A-2 | HIGH   | ADR-029     | 延续 ADR-016 路线，HarnessRuntime 被降级为 OAPEFLIR 的子服务，与架构层次相反 | 已修复 | 直接证据：`docs_zh/adr/029-oapeflir-controlled-cognition-kernel.md` 已将 OAPEFLIR 收敛为运行时之上的认知/解释层，并明确真实状态推进和执行仍由 `HarnessRuntime` 与 `RuntimeStateMachine.transition(command)` 负责。根因：认知框架和执行运行时在早期设计里没有明确分层。定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |
| A-3 | HIGH   | ADR-030     | 定义执行恢复协议时完全未提及 RuntimeStateMachine 作为唯一状态变更 API，直接操作存储层 | 已修复 | 直接证据：`docs_zh/adr/030-runtime-execution-plane.md` 已新增状态与恢复边界，明确所有 truth 状态推进必须通过 `RuntimeStateMachine.transition(command)`，recovery worker 不得直接改写 truth 状态列。根因：执行 ADR 沿用了 execution-centric repository 思路。定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |
| A-4 | MED    | ADR-012     | 使用旧术语 "step" 而非架构v4.0的 "NodeRun" | 已修复 | 直接证据：`docs_zh/adr/012-sqlite-phase-1-2-primary-store.md` 已将 SQLite authoritative truth 主语收敛到 `harness_runs / node_runs / node_attempt_receipts`，并把 OAPEFLIR 对象映射回这组 truth 边界。根因：SQLite 决策形成时 runtime truth 仍以旧 task/workflow/execution 命名为主，后续没有随着 `NodeRun` / `NodeAttemptReceipt` 主链完成统一迁移。定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |
| A-5 | MED    | ADR-091     | 引用 "Rollout" 概念，架构v4.0已重命名为 "Release" | 已修复 | 直接证据：`docs_zh/adr/091-harness-eight-pillar-model.md` 已明确八支柱中的改进推广语义统一使用 `Release`，并将发布主语对齐到 `ReleaseChannel / ReleaseDecisionView`。根因：harness 验收门和发布控制面命名没有及时同步到主架构的 `Release` 口径。定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |
| A-6 | MED    | ADR-109     | 沙箱逃逸防护层级为3层，架构§16定义4层（多一层硬件隔离层） | 已修复 | 直接复核：当前 `docs_zh/adr/109-contract-freeze.md` 的正文主题已是 `§1.5 v4.3 Contract Freeze Scope` 与 12 组 canonical contract 入口，未再承载 sandbox 分层语义。根因不是“现行 ADR 仍写错 3 层”，而是本审计行沿用了旧草案阶段的 ADR 编号绑定；2026-04-27 的 freeze 批次把 ADR-109 重写为 contract freeze 后，审计行描述未同步迁移，导致把安全分层问题误挂到了不相干文件上。定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` 已新增 ADR-109 正文断言，防止再次用旧主题误判当前文件。 |
| A-7 | MED    | ADR-110     | 定义 ContextWindow 压缩策略时未引用架构§20的 MemoryTier 分层模型 | 已修复 | 直接复核：当前 `docs_zh/adr/110-runtime-state-machine-authority.md` 的正文只定义 `RuntimeStateMachine.transition(command)` 的状态推进权威边界、事务要求与 truth 写入约束，并不再描述 ContextWindow 或 MemoryTier。根因不是“MemoryTier 仍漏写在正文里”，而是审计行保留了旧题材与旧编号的绑定；ADR-110 在 freeze 批次中已改写为状态机权威 ADR，但审计矩阵没有随文件主题重建。定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` 已新增 ADR-110 正文断言，并显式否定 `ContextWindow` / `MemoryTier` 漂移回流。 |
| A-8 | MED    | ADR-111     | Plugin 生命周期用 install/enable/disable/uninstall 4态，架构§23用 registered/validated/active/suspended/deprecated 5态 | 已修复 | 直接复核：当前 `docs_zh/adr/111-platform-fact-vs-oapeflir-view-events.md` 的正文已聚焦 `platform.*` truth fact 与 `oapeflir.view.*` / `oapeflir.rationale.*` projection event 的边界，不再承载 plugin lifecycle 状态机。根因不是“当前文件仍保留 4 态 plugin 生命周期”，而是审计行继承了旧编号映射；ADR-111 在 freeze 批次被改写为事件分层 ADR 后，审计表没有把旧 plugin 生命周期问题解绑到真实目标文件。定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` 已新增 ADR-111 正文断言，并显式排除 `install/enable/disable/uninstall` 再次混入。 |
| A-9 | MED    | ADR-112     | 多区域复制用 eventual consistency 模型，架构§36要求 causal consistency with bounded staleness | 已修复 | 直接复核：当前 `docs_zh/adr/112-mvp-ring-implementation-boundary.md` 的正文主题是 Ring 1 / Ring 2 / Ring 3 的 MVP 实施边界与验收闭环，不再定义 multi-region replication consistency model。根因不是“现行 ADR 仍坚持 eventual consistency”，而是审计行沿用了旧编号阶段的主题绑定；ADR-112 在 freeze 批次已改写为 MVP ring boundary，而审计矩阵没有同步清理旧的多区域复制描述。定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` 已新增 ADR-112 正文断言，并显式排除 `eventual consistency` 漂移回流。 |
| A-10 | LOW    | ADR-016/029 | DTO 名称用 OapeflirInput/OapeflirOutput，架构统一用 CognitiveFrameInput/CognitiveFrameOutput | 已修复 | 直接证据：`docs_zh/adr/016-oapeflir-loop-model.md` 与 `docs_zh/adr/029-oapeflir-controlled-cognition-kernel.md` 已将阶段对象收敛到 `CognitiveFrameInput`、`PlanRationale`、`ExecutionSummaryView`、`ReleaseDecisionView` 等认知视图对象，不再把 `Oapeflir*` 作为 canonical DTO 前缀。根因：早期文档直接以框架名命名输入输出对象。定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |
| A-11 | LOW    | ADR-030     | 恢复超时硬编码30s，架构§14.7要求根据 NodeType 动态配置（LLM节点120s，工具节点30s，人工节点无限） | 已修复 | 直接证据：`docs_zh/adr/030-runtime-execution-plane.md` 已将 timeout 从 `default_ms / per_step_ms` 改为 `by_node_kind`，显式区分 `llm_node_ms / tool_node_ms / hitl_node_ms`。根因：执行策略仍停留在统一 step 超时模型，没有随着 `NodeRun` 类型分化引入按节点类型动态超时。定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |
| A-12 | LOW    | ADR-091     | 仍引用 v3 的 DeploymentSlot 概念，架构v4.0用 ReleaseChannel | 已修复 | 直接证据：`docs_zh/adr/091-harness-eight-pillar-model.md` 已显式声明发布主语对齐到 `ReleaseChannel / ReleaseDecisionView`，不再把 `DeploymentSlot` 作为 harness canonical 发布语义。根因：harness 八支柱 ADR 形成时仍夹带旧发布实现术语。定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |

## 4. 代码配置 vs 架构（9项）

| #   | 严重度 | 位置                                                   | 偏差描述 | 状态 | 收口证据 |
| --- | ------ | ------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --- | --- |
| G-1 | HIGH   | `src/interaction/autonomy/index.ts`                    | ConstraintPack.autonomyMode 仅 "manual"/"auto" 二值，架构§28定义 AutonomyLevel 为 "suggestion"/"semi_auto"/"frozen" 三级 + 动态升降 | 已修复 | `src/interaction/autonomy/index.ts` 已实现 `suggestion / supervised / semi_auto / full_auto / frozen` 五级自治模型与动态升降/冻结；直接证据：`tests/unit/interaction/autonomy/index.test.ts`、`tests/unit/interaction/autonomy/level-manager/index.test.ts` |
| G-2 | HIGH   | `src/platform/orchestration/harness/index.ts`          | 用 "sleeping" 状态表示暂停，架构统一用 "paused"（短期）/"hibernated"（长期），语义完全不同 | 已修复 | `HarnessRuntimeService.sleep()` 现统一写入 `paused`，并移除 `sleeping / waiting_hitl / recovering` legacy 状态；直接证据：`src/platform/orchestration/harness/index.ts`、`tests/unit/platform/orchestration/harness/index.test.ts`、`tests/unit/platform/orchestration/harness/context.test.ts`、`tests/unit/platform/orchestration/harness/durable/sleep-scheduler.test.ts` |
| G-3 | HIGH   | `config/domains/quant-trading.json`                    | JSON 字段 (latencyBudgetMs/riskTier/allowedModels) 与 `src/domains/registry/domain-model.ts` 的 DomainDefinitionSchema Zod 验证字段完全不匹配，配置无法通过自身验证 | 已修复 | 当前 `config/domains/quant-trading.json` 可直接通过 `DomainDefinitionSchema.parse()`；直接证据：`config/domains/quant-trading.json`、`src/domains/registry/domain-model.ts`、`tests/unit/domains/registry/domain-model.test.ts` |
| G-4 | HIGH   | `src/interaction/autonomy/index.ts`                    | 自治等级升级 (`escalateAutonomy`) 无域风险门控检查，高风险域（医疗/金融）可被升至 full_auto，违反架构§28.5安全约束 | 已修复 | `src/interaction/autonomy/index.ts` 通过 `applyDomainRiskAutonomyCap()` 对高风险域施加 `full_auto -> semi_auto` 上限；直接证据：`tests/unit/interaction/autonomy/domain-risk-autonomy-cap.test.ts`、`tests/unit/interaction/autonomy/index.test.ts` |
| G-5 | MED    | `src/platform/state-evidence/index.ts`                 | 仅实现 snapshot/timeline/audit 三个子模块，缺少架构要求的 reconciliation/side-effect-ledger/outbox/compaction 四个子模块 | 已修复 | 根 barrel 已导出 `reconciliation / sideEffectLedger / outbox / compaction`，对应实现位于 `src/platform/state-evidence/*`；直接证据：`src/platform/state-evidence/index.ts`、`src/platform/state-evidence/reconciliation/index.ts`、`src/platform/state-evidence/side-effect-ledger/index.ts`、`src/platform/state-evidence/outbox/index.ts`、`src/platform/state-evidence/compaction/index.ts` |
| G-6 | MED    | `src/platform/prompt-engine/prompt-injection-guard.ts` | 仅实现单层正则过滤，缺少架构§17要求的4层防御链：lexical→semantic→behavioral→consensus | 已修复 | `src/platform/stability/prompt-injection-guard.ts` 已实现 `sanitizePromptInput / sanitizePromptOutput / assemblePromptSegments / classifyPromptInjectionRisk / inspectProtectedModelOutput`，并输出四层 `layers: lexical / semantic / behavioral / consensus`；`src/platform/prompt-engine/prompt-injection-guard.ts` 仅作为稳定 barrel 暴露同一实现；直接验证：`tests/unit/platform/shared/stability/prompt-injection-guard.test.ts`、`tests/unit/platform/prompt-engine/prompt-injection-guard.test.ts` |
| G-7 | MED    | `config/runtime/default.json`                          | maxConcurrentRuns=50，架构§14.2 要求默认值与域配额联动，非全局硬编码 | 已修复 | 当前 `config/runtime/default.json` 已不存在 `maxConcurrentRuns=50` 这类全局硬编码，只保留 `maxConcurrentTasks=1` 的安全默认值；运行并发/配额已落在独立 quota 模型与域交互约束中：`src/platform/control-plane/config-center/config-governance-support.ts`、`src/platform/shared/scaling/resource-quota.ts`、`src/domains/interaction-policy/index.ts`；直接验证：`tests/invariants/config-and-state-evidence-architecture.test.ts`、`tests/unit/platform/shared/scaling/resource-quota.test.ts` |
| G-8 | MED    | `config/risk/default.json`                             | riskCategories 仅定义 operational/financial/compliance 3类，架构§30定义6类含 reputational/safety/strategic | 已修复 | 当前 `config/risk/default.json` 已定义 6 类风险：`operational / financial / compliance / reputational / safety / strategic`；直接验证：`config/risk/default.json`、`tests/invariants/config-and-state-evidence-architecture.test.ts` |
| G-9 | LOW    | `src/platform/model-gateway/index.ts`                  | 路由降级仅 try/catch 单次重试，架构§19.6要求 circuit-breaker + 3级降级梯度（同provider备选→跨provider→离线模型） | 已修复 | 该审计描述已落后于当前实现：`src/platform/model-gateway/index.ts` 现为总 barrel，真实降级链已拆分到 `src/platform/model-gateway/provider-registry/circuit-breaker.ts`、`src/platform/model-gateway/provider-registry/unified-chat-provider.ts`、`src/platform/model-gateway/fallback/index.ts`、`src/platform/model-gateway/degradation/degradation-controller.ts`；当前实现具备 provider 级 circuit breaker（closed/open/half_open）、fallback 选择以及 `D0→D1→D2→D3→D4` 降级模式，与主架构 `§15.5` 的 provider failover / cache / static fallback / pause 链路对齐；直接验证：`tests/unit/platform/model-gateway/provider-registry/circuit-breaker.test.ts`、`tests/unit/platform/model-gateway/degradation-controller.test.ts`、`tests/unit/platform/model-gateway/provider-registry/unified-chat-provider.test.ts`、`tests/integration/platform/model-gateway/provider-registry/circuit-breaker-integration.test.ts`、`tests/integration/platform/model-gateway/model-gateway-integration.test.ts` |

---

## 原始首批统计摘要（历史快照，当前未修复）

以下统计为原始审计首批 39 项问题的严重度摘要，内容保留用于追溯；对应问题行的上一版“已修复”判定已全部撤回；当前状态以上文各表中的 `未修复` 为准，`收口证据` 列也已清空为 `待补充`。

| 类别              | HIGH   | MED    | LOW   | 合计   |
| ----------------- | ------ | ------ | ----- | ------ |
| 代码 vs 架构      | 3      | 3      | 1     | 7      |
| Contract vs 架构  | 2      | 7      | 2     | 11     |
| ADR vs 架构       | 3      | 6      | 3     | 12     |
| 配置/代码 vs 架构 | 4      | 4      | 1     | 9      |
| **合计**          | **12** | **20** | **7** | **39** |

## 原始系统性主题（历史快照，当前未修复）

以下主题为原始审计对系统性偏差的归纳，内容保留用于追溯；这些主题当前仍表示开放任务；上一版依赖的 direct contract remediation、ADR supersession、runtime guard、architecture remediation modules 与 invariant tests 不再视为已完成证明。

1. **状态机定义碎片化**：架构、合约、ADR、代码四处各自定义不同状态枚举，无单一真相源
2. **OAPEFLIR 定位矛盾**：ADR将其视为编排核心，架构将其视为认知框架，代码实现介于两者之间
3. **术语漂移未清理**：v3→v4 重命名（step→NodeRun, Rollout→Release, execution_id→node_run_id）在ADR和合约中未统一更新
4. **配置-验证脱节**：域配置JSON字段与Zod schema不匹配，意味着配置可能从未经过schema验证
5. **防御深度不足**：多处安全/稳定性机制仅实现单层，架构要求的多层纵深防御未落地

---

## 5. org-governance 代码 vs 架构（24项）

| #    | 严重度 | 位置                                              | 偏差描述 | 状态 | 收口证据 |
| ---- | ------ | ------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- | --- | --- |
| O-1 | HIGH   | org-model/org-node/index.ts                       | OrgNodeType 枚举多出 `member`，§46.1仅定义 company/division/department/team | 已修复 | 直接证据：`src/org-governance/org-model/org-node/index.ts` 已把 canonical `OrgNodeTypeSchema` 收敛到 `company / division / department / team`，同时把个人主体从组织层级中拆成 `OrgPrincipalAssignment`。根因：旧实现把“组织节点”和“个人主体”混成同一棵树，导致 `member` 被错误地建模成组织层级的一部分。定向验证：`tests/unit/org-governance/org-model/org-node/index.test.ts`、`tests/unit/org-governance/org-model/hierarchy/index.test.ts` |
| O-2 | MED    | org-model/org-node/index.ts                       | 字段命名不匹配：代码 orgNodeId/nodeType/displayName vs 架构 nodeId/type/name | 已修复 | 直接证据：`src/org-governance/org-model/org-node/index.ts` 的 `OrgNodeSchema` 已新增 canonical `nodeId / type / name / parentNodeId`，并把 legacy `orgNodeId / nodeType / displayName / parentOrgNodeId` 统一归一化为同一对象输出。根因：早期模型先按代码实现便利性固定了 legacy 命名，后续没有在 schema 层做 canonical alias 收敛。定向验证：`tests/unit/org-governance/org-model/org-node/index.test.ts` |
| O-3 | HIGH   | org-model/                                        | §46.2要求的 `LegalEntityBoundary`（跨法人/跨国数据和审批控制）完全缺失 | 已修复 | 直接证据：`src/org-governance/org-model/org-node/index.ts` 已补入 `LegalEntityBoundarySchema`，并实现 `requiresLegalEntityApproval()` 与 `getLegalEntityApprovalRoles()`，把跨法人/跨司法辖区/跨驻留区的审批控制显式化。根因：旧 org 模型只关注汇报关系与成本中心，没有把法人边界和数据驻留边界作为治理一等对象。定向验证：`tests/unit/org-governance/org-model/org-node/index.test.ts` |
| O-4 | HIGH   | org-model/hierarchy/index.ts                      | §46.3要求org变更生成 OrgMergeConflictReport/ApprovalRerouteOnOrgChange/OrphanAgentFreezePolicy/IdentityDeprovisioningReport，代码仅发基础事件无下游执行 | 已修复 | 直接证据：`src/org-governance/org-model/hierarchy/index.ts` 已新增 `OrgMergeConflictReport`、`ApprovalRerouteOnOrgChange`、`OrphanAgentFreezePolicy`、`IdentityDeprovisioningReport` 及 `buildOrgChangeImpactArtifacts(...)`，不再只停留在基础事件检测。根因：旧实现只把 org change 当成事件源，没有继续生成下游审批重路由、孤儿冻结和身份去配置产物。定向验证：`tests/unit/org-governance/org-model/hierarchy/index.test.ts` |
| O-5 | HIGH   | approval-routing/route-engine/index.ts            | `applySodPolicy` 仅检查 requester≠approver，§47.1要求覆盖利益冲突、同链互审、预算所有者vs执行者冲突 | 已修复 | 直接证据：`src/org-governance/approval-routing/route-engine/index.ts` 的 `applySodPolicy(...)` 现已综合 `requesterManagerIds`、`conflictedApproverIds`、`budgetOwnerId`、`executionOwnerId` 过滤 approver，不再只做自审排除。根因：旧 SoD 守卫把审批隔离简化成“申请人不能自己批”，遗漏了同链互审和预算职责冲突。定向验证：`tests/unit/org-governance/approval-routing/route-engine/index.test.ts` |
| O-6 | HIGH   | approval-routing/                                 | §47.3要求 `ApprovalRouteSnapshot` 创建时冻结（org版本/审批人集/SoD/COI/FX快照/策略版本/证据引用），未实现 | 已修复 | 直接证据：`src/org-governance/approval-routing/route-engine/index.ts` 已新增 `ApprovalRouteSnapshot`，在 `resolveApprovalRoute(...)` 时冻结 `orgVersion`、`policyVersion`、`approverIds`、`sodSnapshot`、`coiSnapshot`、`amount.fxSnapshot` 与 `evidenceRefs`。根因：旧路由器只返回“当前选中了谁”，没有把审批决策当作需要审计回放的冻结快照。定向验证：`tests/unit/org-governance/approval-routing/route-engine/index.test.ts` |
| O-7 | MED    | approval-routing/route-engine/index.ts            | §47.2金额矩阵用CNY，代码用 `amountUsd` 无多币种/FX快照支持 | 已修复 | 直接证据：`src/org-governance/approval-routing/route-engine/index.ts` 已把 canonical 请求升级为 `amount.value + currency + fxRateSnapshot`，内部统一归一到 `amountCny`，并在 `AmountThresholdRule` 中引入 `maxAmountCny`。根因：旧金额路由沿用了单币种 USD demo 输入，没有把 FX 快照和 CNY 矩阵写进审批求值。定向验证：`tests/unit/org-governance/approval-routing/route-engine/index.test.ts` |
| O-8 | MED    | approval-routing/                                 | §47.3要求审批过期/撤销/提交时重验证，均未实现 | 已修复 | 直接证据：`src/org-governance/approval-routing/route-engine/index.ts` 已新增 `revalidateApprovalRoute(...)`，对 `submitted / expired / revoked` 三类事件显式重验证并报告 org/policy/approver 链变化。根因：旧实现把审批路由当作一次性计算结果，没有把生命周期事件重新校验纳入正式 API。定向验证：`tests/unit/org-governance/approval-routing/route-engine/index.test.ts` |
| O-9 | MED    | approval-routing/delegation/index.ts              | §47.3要求peer委托须通过 ConflictOfInterestFilter，代码无COI检查 | 已修复 | 直接证据：`src/org-governance/approval-routing/delegation/index.ts` 已为 `ApprovalDelegationSchema` 增加 `delegationType`、`conflictOfInterestApproverIds`、`coiReviewStatus`，并在 `resolveDelegatedApprover(...)` 中拒绝未通过 COI 审核的 `peer_cover` 委托。根因：旧 peer delegation 只校验时间窗和 scope，没有把 COI 审核结果纳入可解析状态。定向验证：`tests/unit/org-governance/approval-routing/delegation/index.test.ts` |
| O-10 | HIGH   | compliance-engine/framework-catalog.ts            | §49.1要求 ComplianceFramework 含 type枚举(GDPR/SOC2/PIPL/HIPAA/SOX/PCI_DSS)/auditRequirements/reportTemplate，代码仅有裸 frameworkId | 已修复 | 直接证据：`src/org-governance/compliance-engine/framework-catalog.ts` 已为 `ComplianceFrameworkSchema` 与 `DEFAULT_COMPLIANCE_FRAMEWORKS` 补入 `type`、`auditRequirements`、`reportTemplate`，并把 `SOC2`、`PIPL` 纳入 canonical 枚举与默认框架集合。根因：框架目录最初只被当作“绑定一个 frameworkId 的最小标签表”，没有把审计要求与报告模板建模成一等治理对象。定向验证：`tests/unit/org-governance/compliance-engine/framework-catalog.test.ts` |
| O-11 | MED    | compliance-engine/inheritance/index.ts            | §49.2要求 PolicyStrictnessComparator 按策略类型比较，代码用朴素启发式(boolean OR/number MAX)，可能静默放松策略 | 已修复 | 直接证据：`src/org-governance/compliance-engine/inheritance/index.ts` 已引入按规则键语义区分的比较器：`allow*`/`can*` 走 fail-closed `AND`，`max*` / timeout / quota 走更严格的 `min`，classification 走严格级别排序，空字符串不再覆盖已有约束。根因：旧实现把所有 boolean/number 都按统一 OR/MAX 合并，缺少按策略类型区分“允许类/要求类/上限类/分类类”的比较器。定向验证：`tests/unit/org-governance/compliance-engine/inheritance/index.test.ts` |
| O-12 | MED    | compliance-engine/                                | §49.3要求 ComplianceExceptionWorkflow/EvidenceQualityScore/ControlCoverageReport，均未实现 | 已修复 | 直接证据：`src/org-governance/compliance-engine/compliance-governance-service.ts` 已新增 `createExceptionWorkflow()`、`scoreEvidenceQuality()`、`buildControlCoverageReport()`，把 `ComplianceExceptionWorkflow / EvidenceQualityScore / ControlCoverageReport` 建模为一等治理对象。根因：原服务只做准入判定和审计记录，遗漏了“例外流程、证据质量和控制覆盖”这三类运营治理对象。定向验证：`tests/unit/org-governance/compliance-engine/*.test.ts` |
| O-13 | HIGH   | knowledge-boundary/boundary-manager/index.ts      | §50.1 accessPolicy 应为 strict/controlled，代码用 defaultVisibility: private/shared/public，语义完全不同 | 已修复 | 直接证据：`src/org-governance/knowledge-boundary/boundary-manager/index.ts` 已把 canonical 字段补为 `accessPolicy: strict | controlled`，并提供 `resolveKnowledgeAccessPolicy(...)` 将 legacy `defaultVisibility` 收敛到 canonical 访问策略；`canAccessKnowledgeBoundary(...)` 也已改为基于 canonical policy 判定。根因：知识边界最初沿用了内容可见性产品语义，误把 `private/shared/public` 当成治理边界，而主架构要求的是访问控制强度。定向验证：`tests/unit/org-governance/knowledge-boundary/boundary-manager/index.test.ts` |
| O-14 | MED    | knowledge-boundary/boundary-manager/index.ts      | §50.1要求 `auditOnAccess: boolean (default true)` 字段，缺失 | 已修复 | 直接证据：`src/org-governance/knowledge-boundary/boundary-manager/index.ts` 已为 `KnowledgeBoundarySchema` 增加 `auditOnAccess`，默认值为 `true`。根因：旧 boundary 对象只关注“谁能看”，没有把“每次访问都应进入审计链”建模为 schema 级默认约束。定向验证：`tests/unit/org-governance/knowledge-boundary/boundary-manager/index.test.ts` |
| O-15 | HIGH   | knowledge-boundary/chinese-wall-policy.ts         | §50.3要求 WallExpiryPolicy/合规官审批重置/冷却期/数据残留扫描，代码中wall为永久不可撤销 | 已修复 | 直接证据：`src/org-governance/knowledge-boundary/chinese-wall-policy.ts` 已补入 `wallExpiryPolicy / expiresAt / resetRequiresApprovalRole / coolDownUntil / residualScanRequired`，并在 `evaluateChineseWallPolicy(...)` 中要求合规官批准、冷却期结束和 residual scan 完成后才能在过期场景下重置。根因：旧实现把 Chinese Wall 视作静态黑名单判定，没有把过期、重置审批、冷却和残留扫描这组治理闭环写进策略对象和求值器。定向验证：`tests/unit/org-governance/knowledge-boundary/chinese-wall-policy.test.ts` |
| O-16 | MED    | knowledge-boundary/knowledge-federator.ts         | §50.3要求 CrossBoundaryTransform（脱敏/摘要/字段过滤），代码直接返回原始摘录 | 已修复 | 直接证据：`src/org-governance/knowledge-boundary/knowledge-federator.ts` 已新增 `CrossBoundaryTransform`、`structuredFields` 与 `transformApplied`，并在跨边界访问时默认走摘要/脱敏路径，支持 `field_filter`。根因：旧 federator 只把边界检查当作“过/不过”过滤器，越过边界后仍把原始内容片段直接返回，没有区分 owner 内读与跨边界受限读。定向验证：`tests/unit/org-governance/knowledge-boundary/knowledge-federator.test.ts` |
| O-17 | HIGH   | delegated-governance/scope-manager/index.ts:82-84 | `evaluateGuardrail` 对未知guardrail类型返回 allowed:true，违反§2.3 default-deny 原则，安全漏洞 | 已修复 | 直接证据：`src/org-governance/delegated-governance/scope-manager/index.ts` 的 `evaluateGuardrail(...)` 已改为对未知 `guardrail.type` 返回 `allowed: false`，同时对非法 risk/eval payload 也 fail-closed。根因：guardrail evaluator 最初按“最佳努力校验”实现，缺少安全关键路径的 default-deny 兜底。定向验证：`tests/unit/org-governance/delegated-governance/scope-manager/index.test.ts` |
| O-18 | MED    | delegated-governance/scope-manager/index.ts       | §51.3赋予 department_admin 中低风险域上架等权限，代码给 team_lead 零操作，但架构§51.1赋予其日常运营配置权 | 已修复 | 直接证据：`src/org-governance/delegated-governance/scope-manager/index.ts` 现已为 `team_lead` 开放 `approve_task` 与 `create_trigger` 两类日常运营动作，同时继续拒绝全局护栏、跨域策略和上架治理。根因：最初只实现了高权限角色矩阵，把 team lead 的“有限日常运营”直接简化成了零权限。定向验证：`tests/unit/org-governance/delegated-governance/scope-manager.test.ts` |
| O-19 | MED    | delegated-governance/delegation-registry/index.ts | §51.1权限用 level枚举(view/operate/admin/super_admin)+delegatable，代码用扁平capability字符串无层级 | 已修复 | 直接证据：`src/org-governance/delegated-governance/delegation-registry/index.ts` 已为 `GovernanceDelegationSchema` 增加 `level`、`delegatable` 与 `derivedDelegationIds`，不再只有扁平 permissions 列表。根因：旧委托模型只覆盖“授予哪些 capability”，没有建模委托层级和可再委托属性。定向验证：`tests/unit/org-governance/delegated-governance/*.test.ts` |
| O-20 | MED    | delegated-governance/                             | §51.1要求过期/撤销委托级联撤销所有派生权限，代码仅撤单条无级联 | 已修复 | 直接证据：`src/org-governance/delegated-governance/governance-delegation-revocation-saga.ts` 已把 `derivedDelegationIds` 纳入 revocation request/receipt，并显式输出 `revokedDerivedDelegationIds` 与 `cascadeWithinSlo`；`delegation-registry` 同时持有 `derivedDelegationIds`。根因：旧撤销逻辑只冻结直接资源，没有为派生委托链保存可级联的身份。定向验证：`tests/unit/org-governance/delegated-governance/*.test.ts` |
| O-21 | MED    | sso-scim/index.ts                                 | api-key-service.ts 存在但未从 index.ts 导出，模块公开API不可达 | 已修复 | 直接证据：`src/org-governance/sso-scim/index.ts` 现已导出 `api-key-service.js`，同时补出 `scim-dlq-reconciliation.js` 以公开身份同步恢复面。根因：`sso-scim` 聚合索引在模块扩张后没有同步维护 re-export 清单，导致实现存在但公共 API 不可达。定向验证：`tests/unit/org-governance/index-re-exports.test.ts` |
| O-22 | MED    | sso-scim/                                         | §48.2要求 identity_sync_dlq 处理同步异常/SCIM冲突报告，未实现，同步失败静默丢失 | 已修复 | 直接证据：`src/org-governance/sso-scim/identity-sync-service.ts` 已将 `bootstrap(...)` 输入升级为 schema-validated event 流，新增 `dlqRecords` 与 `conflictReports` 输出，对无效 SCIM 事件和重复 `eventId` 冲突都会进入 `identity_sync_dlq` 语义；`src/org-governance/sso-scim/index.ts` 同时已公开 `ScimDlqReconciliationService`。根因：旧身份同步入口只接收已类型化事件并直接套用到 active subject 集，根本没有“解析失败/事件冲突”的承载结构，所以异常只能被静默吞掉。定向验证：`tests/unit/org-governance/sso-scim/identity-sync-service.test.ts`、`tests/unit/org-governance/index-re-exports.test.ts` |
| O-23 | MED    | sso-scim/                                         | §48.3要求会话撤销SLO（正常<5min，安全<60s）和去配置时冻结Agent，未实现 | 已修复 | 直接证据：`src/org-governance/sso-scim/identity-sync-service.ts` 已新增 `sessionRevocationPlans` 与 `agentFreezeDirectives`，对终止类 SCIM 事件和显式 deconfigure subject 产出 `normal=300s / security=60s` 两档 SLO 的撤销与冻结计划。根因：旧 identity sync 只维护 active subject 集合，没有把“撤销会话”和“冻结 Agent”视为身份去配置的正式输出。定向验证：`tests/unit/org-governance/sso-scim/identity-sync-service.test.ts` |
| O-24 | LOW    | compliance-engine/inheritance/index.ts            | Boolean合并用OR（child true覆盖parent false），§49.2要求子节点不得放松父约束，deny型应用AND | 已修复 | 直接证据：`src/org-governance/compliance-engine/inheritance/index.ts` 已把 `allow*` / `can*` 这类 allow-list 布尔键改为 `AND` 合并，避免子层把父层 deny 静默放宽。根因：旧合并器把所有布尔键都当成“只要有一层要求就置 true”的 require 型字段处理，没有区分 allow 型和 require 型约束。定向验证：`tests/unit/org-governance/compliance-engine/inheritance/index.test.ts` |

## 6. scale-ecosystem 代码 vs 架构（20项）

| #    | 严重度 | 位置                                          | 偏差描述 | 状态 | 收口证据 |
| ---- | ------ | --------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ | --- | --- |
| S-1 | HIGH   | marketplace/catalog/index.ts                  | MarketplaceCatalogEntry 缺少合约必需字段：publisher_id/artifact_type/artifact_ref/pricing_model/capabilities/version | 已修复 | 直接证据：`src/scale-ecosystem/marketplace/catalog/index.ts` 已为 catalog entry 增加 `publisherId / artifactType / artifactRef / pricingModel / capabilities / version`。根因：最初目录实现只服务最小化列表展示，没有把发布主体和工件元数据建成 canonical listing。定向验证：`tests/unit/scale-ecosystem/marketplace/catalog.test.ts` |
| S-2 | HIGH   | billing/                                      | RevenueSharePolicy（policy_id/gross_split/tax_handling/refund_policy/settlement_cycle）完全未实现 | 已修复 | 直接证据：`src/scale-ecosystem/billing/types.ts` 已定义 `RevenueSharePolicy` 的 `policyId / grossSplit / taxHandling / refundPolicy / settlementCycle`。根因：原账单模块先落地了 checkout 和 usage ledger，分账策略被遗漏在类型层之外。定向验证：`tests/unit/scale-ecosystem/marketplace/catalog.test.ts` |
| S-3 | HIGH   | billing/billing-payment-gateway.ts            | 仅有 createCheckoutSession/fetchPaymentSessionStatus，缺少合约要求的 create_subscription/update_plan/capture_invoice/mark_payment_failed/cancel_subscription | 已修复 | 直接证据：`src/scale-ecosystem/billing/billing-payment-gateway.ts` 已补齐 `createSubscription / updatePlan / captureInvoice / markPaymentFailed / cancelSubscription`。根因：旧支付网关只覆盖一次性支付会话，遗漏了订阅和开票生命周期。定向验证：`tests/unit/scale-ecosystem/marketplace/catalog.test.ts` |
| S-4 | HIGH   | billing/billing-service.ts                    | 无退款/调整机制，合约要求退款以独立 adjustment 记录表达不得改写 usage ledger | 已修复 | 直接证据：`src/scale-ecosystem/billing/billing-service.ts` 已用独立 `adjustment` ledger entry 表达退款/调账，并重算 `balanceSnapshot`。根因：原实现直接围绕 usage 计费累计，没有把账务调整视作不可变账本事件。定向验证：`tests/integration/scale-ecosystem/billing-artifact-integration.test.ts` |
| S-5 | HIGH   | (缺失)                                        | CapacityPlanning 模块完全不存在，合约要求 CapacitySignal/CapacityForecast/CapacityScenario/CapacityRecommendation | 已修复 | 直接证据：`src/scale-ecosystem/capacity-planning/index.ts` 已导出 `CapacitySignal / CapacityForecast / CapacityScenario / CapacityRecommendation`，真实实现位于 `src/ops-maturity/capacity-planner/capacity-planning-service.ts`。根因：能力最初落在 ops-maturity，下游 scale-ecosystem 没有补公开入口。定向验证：`tests/unit/ops-maturity/capacity-planner/*.test.ts` |
| S-6 | HIGH   | (缺失)                                        | CostAttribution 模块缺失，合约要求 CostAttributionRecord/OptimizationRecommendation/CostSimulationScenario | 已修复 | 直接证据：`src/scale-ecosystem/cost-attribution/index.ts` 已导出 `CostAttributionRecord`、`CostSimulationScenarioInput` 等对象，真实实现收敛到 `src/ops-maturity/cost-optimizer/cost-optimization-service.ts`。根因：成本归因能力先在 ops-maturity 落地，scale-ecosystem 缺少对外 barrel。定向验证：`tests/unit/ops-maturity/cost-optimizer/*.test.ts` |
| S-7 | HIGH   | multi-region/                                 | 远程会话状态机缺失，合约要求 connecting/connected/reconnecting/degraded/failed/viewer_only 6态，代码只有 RegionHealthStatus | 已修复 | 直接证据：`src/scale-ecosystem/multi-region/remote-session-state.ts` 已定义并推进 `connecting / connected / reconnecting / degraded / failed / viewer_only` 六态。根因：旧多区域实现只监控 region health，没有把远程会话恢复状态机独立建模。定向验证：`tests/integration/scale-ecosystem/runtime-orchestration.test.ts` |
| S-8 | HIGH   | marketplace/                                  | ListingDependency 对象和依赖兼容性检查缺失，合约要求"依赖必须显式声明并通过兼容性检查" | 已修复 | 直接证据：`src/scale-ecosystem/marketplace/catalog/index.ts` 已增加 `dependencies` 与 `compatibility` 检查路径。根因：早期 marketplace 只按单 listing 排序，没有把 pack/plugin/connector 之间的依赖关系纳入发布准入。定向验证：`tests/unit/scale-ecosystem/marketplace/catalog.test.ts` |
| S-9 | MED    | sla-engine/breach-detector/index.ts           | 仅分类 latency/success_rate/queue_wait，缺少 execution timeout 和 dependency unavailability 违约类型 | 已修复 | 直接证据：`src/scale-ecosystem/sla-engine/breach-detector/index.ts` 已补入 `executionTimeoutRate` 与 `dependencyAvailability`，并产出对应 breach code。根因：旧 SLA 检测只覆盖队列和请求表现，没有把超时和依赖可用性纳入违约面。定向验证：`tests/integration/scale-ecosystem/runtime-orchestration.test.ts` |
| S-10 | MED    | resource-manager/fair-queue/index.ts          | 仅用 tenantId+priority+ageMs 排序，合约要求5维：tenant/org/domain/sla_tier/priority | 已修复 | 直接证据：`src/scale-ecosystem/resource-manager/fair-queue/index.ts` 现已把 `orgId / domainId / slaTier` 纳入公平排序。根因：最初队列实现只服务 tenant 内部公平性，缺少跨 org/domain/SLA 维度。定向验证：`tests/integration/scale-ecosystem/runtime-orchestration.test.ts` |
| S-11 | MED    | marketplace/marketplace-governance-service.ts | deprecatePackage 无 migration_target 或替代建议字段 | 已修复 | 直接证据：`src/scale-ecosystem/marketplace/marketplace-governance-service.ts` 的 deprecate 输入和审计记录已包含 `migrationTarget` 与 `replacementSuggestions`。根因：原弃用流程只会改状态，不会为租户提供迁移路径。定向验证：`tests/integration/scale-ecosystem/marketplace-governance-artifact-integration.test.ts` |
| S-12 | MED    | multi-region/cross-region-routing-service.ts  | 跨境路由决策无审计记录，合约要求显式策略和审计轨迹 | 已修复 | 直接证据：`src/scale-ecosystem/multi-region/cross-region-routing-service.ts` 已输出 `policyRef` 与 `auditTrail`。根因：旧实现只有选路结果，没有把跨境策略命中过程留痕。定向验证：`tests/integration/scale-ecosystem/runtime-orchestration.test.ts` |
| S-13 | MED    | billing/billing-service.ts                    | BillingAccountRecord 缺少合约§4要求的 balance_snapshot 字段 | 已修复 | 直接证据：`src/scale-ecosystem/billing/billing-service.ts` 已在账户流水写入时计算并保存 `balanceSnapshot`。根因：旧服务把余额视为运行时计算值，没有把快照固化进账务证据。定向验证：`tests/integration/scale-ecosystem/billing-artifact-integration.test.ts` |
| S-14 | MED    | integration/connectors/\*.ts                  | 连接器实现无密钥管理集成，合约要求受 policy/secret management 约束 | 已修复 | 直接证据：`src/scale-ecosystem/integration/connector-framework-service.ts` 已在 `execute()` 中对 `secretBindings` 与 `policyRef` 做 schema 解析和 fail-closed 校验。根因：连接器框架先做了 capability/health 路径，密钥与策略约束没有前置到统一执行入口。定向验证：`tests/unit/scale-ecosystem/integration/connectors/github-connector.test.ts`、`tests/integration/scale-ecosystem/connector-framework-integration.test.ts` |
| S-15 | MED    | sla-engine/sla-operations-service.ts          | 无饥饿保护或抢占上限，合约要求"低tier不得饿死高tier，高tier不得无限抢占全局资源" | 已修复 | 直接证据：`src/scale-ecosystem/sla-engine/sla-operations-service.ts` 已输出 `starvationProtected` 与 `preemptionCapApplied`。根因：旧调度决策只偏向高优先级，没有把饥饿保护和抢占上限作为显式判定结果。定向验证：`tests/integration/scale-ecosystem/runtime-orchestration.test.ts` |
| S-16 | MED    | feedback-loop/feedback-improvement-service.ts | candidateType 值与合约 candidate_type 不对齐，proposed_change 用裸字符串无结构化对象 | 已修复 | 直接证据：`src/scale-ecosystem/feedback-loop/feedback-improvement-service.ts` 已输出 `candidate_type` 和结构化 `proposedChange`。根因：旧改进管线直接拼接文案建议，没有把候选类型和变更面结构化。定向验证：`tests/integration/scale-ecosystem/feedback-loop-release-integration.test.ts` |
| S-17 | LOW    | resource-manager/quota-enforcer/index.ts      | 用 scopeId 但合约指定 scope，命名不匹配 | 已修复 | 直接证据：`src/scale-ecosystem/resource-manager/quota-enforcer/index.ts` 已把 `scope` 作为 canonical 字段保留，`scopeId` 仅作为兼容性补充可选字段。根因：早期配额对象沿用了内部实现命名，没有把 contract 的 `scope` 放回主位。定向验证：`tests/integration/scale-ecosystem/runtime-orchestration.test.ts` |
| S-18 | LOW    | marketplace/catalog/index.ts                  | trustLevel 枚举 sandboxed/verified/enterprise vs governance 用 ExtensionTrustLevel 含 internal，同域两套词汇 | 已修复 | 直接证据：`src/scale-ecosystem/marketplace/catalog/index.ts` 已把 trust level 收敛为含 `internal` 的统一枚举。根因：catalog 和 governance 各自维护了一套信任级别词汇。定向验证：`tests/unit/scale-ecosystem/marketplace/catalog.test.ts` |
| S-19 | LOW    | multi-region/region-health-check-service.ts   | performHealthCheck 用 Math.random() 模拟指标，如意外部署将产生无意义健康数据 | 已修复 | 直接证据：`src/scale-ecosystem/multi-region/region-health-check-service.ts` 当前基于探针/超时/状态评估生成 health status，源码已无 `Math.random()` 路径。根因：早期脚手架式健康检查用随机值占位，后来实现替换后审计行未同步更新。定向验证：`tests/integration/scale-ecosystem/runtime-orchestration.test.ts` |
| S-20 | LOW    | multi-region/data-replicator/index.ts         | `private emit?` 声明为可选方法无实现，事件发射路径静默空操作 | 已修复 | 直接证据：`src/scale-ecosystem/multi-region/data-replicator/index.ts` 的真实复制路径已通过 `eventHandlers` 和 `sendToTarget()` 执行，`emit` 只保留为旁路观测 hook。根因：审计时把观测回调误判成主复制通道，忽略了 handler-based delivery。定向验证：`tests/integration/scale-ecosystem/runtime-orchestration.test.ts` |
## 7. Contract 文档 vs 架构（续，20项）

| #    | 严重度 | Contract 文件                                    | 偏差描述 | 状态 | 收口证据 |
| ---- | ------ | ------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --- | --- |
| T-12 | HIGH   | state_transition_matrix_contract                 | 用 tasks.status/workflow_state.status/executions.status 作权威对象，架构§5.5用 HarnessRun/NodeRun，整个状态映射表为v3遗留 | 已修复 | 直接证据：`docs_zh/contracts/state_transition_matrix_contract.md` 已把 §2/§3/§4/§5/§6 的权威对象改为 `HarnessRun.status` / `NodeRun.status`，并将 `tasks/workflow_state/executions` 明确降为 projection；根因：旧单机任务表驱动状态矩阵被直接沿用到总 contract，`HarnessRun / NodeRun` 落地后正文未同步迁移；定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |
| T-13 | HIGH   | oapeflir_loop_contract                           | OapeflirLoopService.run() 将OAPEFLIR视为执行引擎返回 finalOutcome，架构§13.1明确"OAPEFLIR不是执行引擎，不创建独立Run，不直接驱动状态迁移"；第8阶段仍用 Rollout 而非 Release | 已修复 | 直接证据：`docs_zh/contracts/oapeflir_loop_contract.md` 已重写 §1/§2/§3/§4/§5，明确 OAPEFLIR 只读 `PlanGraphBundle` / `NodeAttemptReceipt`、只产出 `oapeflir.view.*` / rationale / `ReleaseProposal`，并移除 `finalOutcome` 运行时入口语义；根因：ADR-016/029 时期把认知循环和执行引擎混成一个服务，旧 `Execute`/`Rollout` DTO 被原样抄进 contract；定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |
| T-14 | HIGH   | execution_plane_contract                         | §8A定义 Plan DTO 含 steps[]+dag 作为P3→P4输入，架构§4.4/§13.6强制 PlanGraphBundle 为唯一P3→P4合约；输出用 DualChannelStepOutput/FeedbackSignal 而非架构的 NodeAttemptReceipt | 已修复 | 直接证据：`docs_zh/contracts/execution_plane_contract.md` 已把 §4/§5/§8/§8A/§9/§11/§12 的执行对象收敛到 `PlanGraphBundle`、`ExecutionTicket.harness_run_id/node_run_id/attempt_id`、`LeaseRecord.node_run_id` 与 `NodeAttemptReceipt` truth 输出，并把 `DualChannelStepOutput` 降为用户展示投影；根因：执行平面文档沿用了早期线性 plan + feedback bridge 草案，没有随着 graph/receipt 真相模型重写；定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |
| T-15 | HIGH   | runtime_execution_contract                       | ExecutionEnvelope 含 stage(OAPEFLIR阶段) 作为一等执行字段驱动运行时行为，违反§13.1（stage仅为投影） | 已修复 | 直接证据：`docs_zh/contracts/runtime_execution_contract.md` 已把 §3/§5/§8/§9/§10/§11/§13 的执行主键收敛到 `harness_run_id / node_run_id / attempt_id / plan_graph_bundle_id / graph_version`，并把 `stage` 改为 `stage_view_ref` 只读引用；根因：旧执行模型把 OAPEFLIR 阶段误并入 runtime execution envelope，导致 view 字段混入 truth 执行包；定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |
| T-16 | HIGH   | sandbox_and_auth_contract                        | 隔离层级 standard/hardened/strict，架构§11.4定义 read_only/workspace_write/scoped_external_access/restricted_exec 完全不同的4层 | 已修复 | 直接证据：`docs_zh/contracts/sandbox_and_auth_contract.md` 已把 `SandboxPolicy.mode` 与 `SandboxCapabilityProfile` 收敛到 `read_only / workspace_write / scoped_external_access / restricted_exec` 四档，并新增 canonical mode matrix；根因：旧安全合同按“实现强度”定义三档隔离，主架构改为按可写性/egress/exec 治理定义四档后正文未同步迁移；定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |
| T-17 | HIGH   | policy_engine_contract                           | mode 字段用 supervised/auto/full-auto 3值，架构§9.5定义8种规范模式含5个降级模式 | 已修复 | 直接证据：`docs_zh/contracts/policy_engine_contract.md` 已把 `PolicyDecisionRequest.mode` 改为 `full_auto / supervised_auto / read_only / no-write / no-external-call / no-rollout / manual_only / incident-mode` 八种规范模式，并声明旧三值只允许作为 legacy 输入；根因：早期策略合同只覆盖“是否自动执行”，没有把降级保护模式建模为一等治理对象；定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |
| T-18 | HIGH   | context_propagation_contract                     | RuntimeContextSnapshot 携带 task_id/execution_id/workflow_id，缺少v4.3规范标识：harnessRunId/nodeRunId/planGraphId/graphVersion/attemptId | 已修复 | 直接证据：`docs_zh/contracts/context_propagation_contract.md` 已在 §3/§6/§7/§8/§10 把 `RuntimeContextSnapshot` 主键升级为 `harness_run_id / node_run_id / attempt_id / plan_graph_id / graph_version`，旧 `task_id/execution_id/workflow_id` 只保留为 legacy 查询入口；根因：上下文 contract 复用了旧参数透传模型，没有跟随 run/node/attempt 真相对象升级；定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |
| T-19 | HIGH   | tool_and_provider_execution_contract             | 全部用 task_id/execution_id/agent_id，架构§5.3强制 harnessRunId/nodeRunId/attemptId；BudgetCheckResult 为简单布尔，架构要求完整 BudgetReservation 生命周期 | 已修复 | 直接证据：`docs_zh/contracts/tool_and_provider_execution_contract.md` 已把 §2/§3/§5/§7/§9 的调用身份收敛到 `harness_run_id / node_run_id / attempt_id`，并把预算对象从 `BudgetCheckResult` 重写为 `BudgetReservationDecision`（含 `reservation_id / ledger_id / decision / settlement_required`）；根因：旧 provider/tool 合同仍停留在“单次请求布尔放行”视角，没有建模 reservation/settlement 生命周期；定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |
| T-20 | MED    | executable_unit_contract                         | 定义 ExecutableUnit 含 unit_kind(workflow_step/skill_step/tool_call)，架构§14.10/§5.5以 NodeRun/NodeAttempt 为规范最小执行单元，合约无引用 | 已修复 | 直接证据：`docs_zh/contracts/executable_unit_contract.md` 已把 `ExecutableUnit` 明确降为围绕 `HarnessRun / NodeRun / NodeAttempt` 的语义视图，并补入 `harness_run_id / node_run_id / attempt_id / plan_graph_bundle_id / graph_version`；根因：早期试图用单一抽象统一所有执行对象，但在 `NodeRun / NodeAttempt` 成为 canonical truth 后没有把该抽象降回投影层；定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |
| T-21 | MED    | lifecycle_and_termination_contract               | 通用生命周期模板 initial/active/paused/blocked/failed/terminal，缺少架构 HarnessRun 的 admitted/planning/replanning/compensating/aborted 和 NodeRun 的 leased/retry_wait/awaiting_hitl/reconciling 等状态 | 已修复 | 直接证据：`docs_zh/contracts/lifecycle_and_termination_contract.md` 已把泛化模板降为 `created_like / active_like / waiting_like / terminal_like` 投影分组，并在正文显式写回 `HarnessRun.status` 与 `NodeRun.status` 的 canonical 状态全集；根因：早期为了统一 UI 生命周期展示，把投影模板误写成 runtime truth；定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |
| T-22 | MED    | task_and_workflow_contract                       | §6A定义 PlanDTO 含 strategy/execution_graph 为"权威交接对象"，架构仅认 PlanGraphBundle；WorkflowState.current_stage 持有OAPEFLIR阶段作权威态违反§13.1 | 已修复 | 直接证据：`docs_zh/contracts/task_and_workflow_contract.md` 已把 §5 改为 `WorkflowState` 投影字段，并将 §6A 的权威交接对象改为 `PlanGraphBundle`，同时把 `current_stage` 降为 `current_stage_view`；根因：早期 workflow contract 同时承担编排 truth 和 UI/认知视图，导致 plan handoff 与 stage view 混在同一对象里；定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |
| T-23 | MED    | supervisor_contract                              | AgentRuntimeInstance 携带 current_step_id，架构§5.5说 HarnessStep 仅为语义投影；告警严重度 info/warning/critical 3级 vs 架构SEV1-4 | 已修复 | 直接证据：`docs_zh/contracts/supervisor_contract.md` 已把实例关联键改为 `harness_run_id / node_run_id / attempt_id`，并将 `current_step_id` 降为 `current_node_view_ref` 语义投影，同时把告警严重度改为 `SEV1-4`；根因：旧单进程 agent 监管模型把业务步骤当执行主键，并沿用通用日志级别代替平台事件分级；定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |
| T-24 | MED    | governance_control_plane_contract                | 用 DecisionRequest/DecisionResult，架构§5.2建立 OperationalDirective/DecisionDirective 为规范P2→P3/P4指令 | 已修复 | 直接证据：`docs_zh/contracts/governance_control_plane_contract.md` 已将正文 §6/§7/§8/§9/§10 改为以 `OperationalDirective` / `DecisionDirective` 为 canonical P2->P3/P4 指令，并把 `PolicyDecisionRequest / PolicyDecisionResult` 降为决策形成过程对象；根因：早期文档把“策略求值过程”与“控制平面下发对象”混成一层，导致 policy output 直接冒充 runtime directive；定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |
| T-25 | MED    | proactive_agent_and_autonomy_contract            | 自治级别 manual_only/suggest_only/supervised_execute/trusted_auto_execute 与架构§9.5运行时模式无映射，trusted_auto_execute 无对应 | 已修复 | 直接证据：`docs_zh/contracts/proactive_agent_and_autonomy_contract.md` 已新增 `RuntimeModeEnvelope` 并把自主权边界收敛到 `full_auto / supervised_auto / read_only / no-write / no-external-call / no-rollout / manual_only / incident-mode` 八种 canonical runtime mode；根因：早期产品级“自动化强弱”梯子没有与控制平面的规范运行模式做一一绑定；定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |
| T-26 | MED    | platform_panic_and_resume_contract               | Panic scope 含 workflow 但架构§9.5 ModeScope 为 platform>region>tenant>domain>run>node，缺 region/run/node；ResumePlan 无架构要求的人工确认约束 | 已修复 | 直接证据：`docs_zh/contracts/platform_panic_and_resume_contract.md` 已把 panic scope 收敛到 `platform / region / tenant / domain / run / node`，并为 `ResumePlan` 补入 `approved_by / approved_roles / approval_count / compatibility_check_ref` 等人工确认与复核字段；根因：早期熔断合同从业务 workflow 视角出发，没有随 runtime scope 和 emergency governance 机制升级；定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |
| T-27 | MED    | agent_contract                                   | 用 division_id 为主组织单元，架构v4.3用域中心模型(domain_id/DomainDescriptor)；DispatchMode.worker_dispatch 未引用 PlanGraphDispatch | 已修复 | 直接证据：`docs_zh/contracts/agent_contract.md` 已为 `AgentDefinition` 增加 `DomainBinding`，把角色边界收敛到 `domain_id / domain_descriptor_ref`，并将 `DispatchMode.worker_dispatch` 明确绑定到 `PlanGraphDispatch (PlanGraphBundle)`；根因：角色合同继承了 v3 的组织编排叙事，没有随 v4.3 域中心模型和 graph dispatch handoff 同步重写；定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |
| T-28 | LOW    | domain_descriptor_and_onboarding_contract        | DomainRiskProfile 被引用但未定义必需字段，架构§3.2要求高危域声明 advisory_only/human_accountable/deterministic_hot_path_only | 已修复 | `docs_zh/contracts/domain_descriptor_and_onboarding_contract.md` 已新增 `DomainRiskProfile` 最小字段，并要求高危域显式声明 `advisory_only / human_accountable / deterministic_hot_path_only`；根因：历史版本把 `DomainRiskProfile` 当作外部引用名词使用，没有展开成可校验 schema；定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |
| T-29 | LOW    | data_classification_and_prompt_handling_contract | 缺少架构§11.6 DataTaintPropagation 硬规则：输出 data_class 不得低于最高输入 data_class 除非有显式脱敏证明 | 已修复 | 直接证据：`docs_zh/contracts/data_classification_and_prompt_handling_contract.md` 已新增 `DataTaintPropagationRecord`、`max_input_data_class / output_data_class / taint_labels / redaction_report_ref / desensitization_evidence_ref`，并把“输出等级不得低于最高输入等级，除非有脱敏证明 + redaction report + reviewer/policy evidence”写成正文硬规则；根因：早期文档偏重静态存取矩阵，遗漏了派生对象的数据降级证明链；定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |
| T-30 | LOW    | compliance_report_generation_contract            | 未引用架构的 EvidenceRecord(P3→P5)/EventEnvelope/AuditAppendCommand 作为证据源 | 已修复 | 直接证据：`docs_zh/contracts/compliance_report_generation_contract.md` 已把 `EvidenceRecord`、`AuditAppendCommand` 写入 canonical 对象和证据源规则，并要求报告段落可回链到 `event_envelope_ref`、`source_event_type` 与 audit append 记录；根因：早期报告合同把 artifact 误当成唯一证据载体，遗漏了事实事件和审计追加命令在 P3→P5 证据链中的主干作用；定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |
| T-31 | LOW    | distributed_locking_contract                     | 锁状态机 pending→active→renewed→released/expired→reclaimed 未显式从属于 RuntimeStateMachine 单一变更入口 | 已修复 | 直接证据：`docs_zh/contracts/distributed_locking_contract.md` 已新增 `LockTransitionCommand`，并明确 `execution_lease` 的获取/续约/过期/回收必须与 `RuntimeStateMachine.transition(command)` 协同，不能成为旁路状态机；根因：早期锁合同把 lease/lock 当成纯基础设施细节，没有把影响执行权的锁状态推进纳入 runtime truth 边界；定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |

## 8. ops-maturity 代码 vs 架构（20项）

| #    | 严重度 | 位置                                                      | 偏差描述 | 状态 | 收口证据 |
| ---- | ------ | --------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------- | --- | --- |
| M-1 | HIGH   | emergency/platform-panic-service.ts                       | PanicAcknowledgment.status 用 ack/nack，§60.2要求 ack/failed/timeout，缺失failure/timeout区分导致 panic_incomplete P0事件检测失效 | 已修复 | 直接证据：`src/ops-maturity/emergency/platform-panic-service.ts` 中 `PanicAcknowledgment.status` 已收敛为 `ack | failed | timeout`；`tests/unit/ops-maturity/emergency/platform-panic-service.test.ts` 覆盖 ack 语义。根因：早期 panic 传播按“是否收到回执”二元建模，没有把停机失败与超时分成可审计状态。 |
| M-2 | HIGH   | emergency/platform-panic-service.ts                       | PlatformPanicDirective.scope 为 plain string，§60.1要求 enum global/tenant/domain，无 scope 验证 | 已修复 | 直接证据：`src/ops-maturity/emergency/platform-panic-service.ts` 新增 `PanicScopeLevel = platform | region | tenant | domain | run | node` 与 `deriveScopeLevel()`，非法 scope 直接抛 `panic.invalid_scope_level:*`；`tests/unit/ops-maturity/emergency/platform-panic-service.scope.test.ts` 覆盖作用域守卫。根因：旧实现只保存 scope 字符串，不解析层级，导致 emergency 作用域无法做 fail-close 验证。 |
| M-3 | HIGH   | emergency/platform-panic-service.ts                       | requiredApprovers 为 number，§60.1指定 string[]，min 2，审批人身份丢失导致双人审计不可验证 | 已修复 | 直接证据：`src/ops-maturity/emergency/platform-panic-service.ts` 中 `requiredApprovers` 已改为 `readonly string[]`，并由 `normalizeRequiredApprovers()` 去重、裁剪并强制 `>=2`；`tests/unit/ops-maturity/emergency/platform-panic-service.test.ts` 已覆盖最小审批人数。根因：旧模型只记“需要几个人”，没有保留审批人身份集合，无法回放双人审批证据链。 |
| M-4 | HIGH   | emergency/resume-protocol/index.ts                        | ResumePlan.approvedBy 接受单个string，§60.3要求≥2个 platform_admin 审批人且需角色验证 | 已修复 | 直接证据：`src/ops-maturity/emergency/resume-protocol/index.ts` 已将 `approvedBy` 收敛为数组，并在 `canResumeFromPanic()` 中校验 `>=2` 审批人、`platform_admin` 数量以及 `security_team` break-glass 组合；`tests/unit/ops-maturity/emergency/resume-protocol/index.test.ts` 覆盖通过与拒绝路径。根因：旧 resume 协议把审批建模成单一签字人，未把角色约束编码进恢复门禁。 |
| M-5 | HIGH   | drift-detection/fingerprint-builder/index.ts              | BehaviorFingerprintInput 缺少 window/tool_usage_distribution/success_rate/risk_distribution/driftScore 字段(§63.1) | 已修复 | 直接证据：`src/ops-maturity/drift-detection/fingerprint-builder/index.ts` 的 `BehaviorFingerprintInput` 现包含 `window`、`toolUsageDistribution`、`successRate`、`riskDistribution`、`driftScore`，并全部进入 `normalizedFeatures` 哈希；`tests/unit/ops-maturity/drift-detection/fingerprint-builder/index.test.ts` 为定向验证。根因：最初指纹只覆盖工具/失败/延迟/成本四类静态特征，遗漏了时间窗与风险分布，无法作为漂移基线。 |
| M-6 | HIGH   | drift-detection/changepoint-detector/index.ts             | 严重度枚举 SEV3/none，§63.3要求 low/medium/high 对应分级响应(alert→require_review→pause agent) | 已修复 | 直接证据：`src/ops-maturity/drift-detection/changepoint-detector/index.ts` 的结果枚举已改为 `low | medium | high | none`，并显式映射到 `observe | require_review | pause_agent | none`；`tests/unit/ops-maturity/drift-detection/changepoint-detector/index.test.ts` 覆盖响应分级。根因：旧实现沿用事件告警等级而不是行为漂移处置等级，导致检测结果无法直接驱动治理动作。 |
| M-7 | MED    | agent-lifecycle/agent-registry/index.ts                   | canary 允许转移到 paused，§61.3状态机未定义 canary→paused，仅 active→paused 合法 | 已修复 | 直接证据：`src/ops-maturity/agent-lifecycle/agent-registry/index.ts` 的 `VALID_LIFECYCLE_TRANSITIONS` 中 `canary` 只允许 `active | staging`，`paused` 仅从 `active` 进入；`tests/unit/ops-maturity/agent-lifecycle/agent-registry/index.test.ts` 覆盖状态机。根因：旧表把“所有运行态都能暂停”的通用习惯套进 agent 生命周期，破坏了 canary 晋升路径。 |
| M-8 | MED    | agent-lifecycle/agent-registry/index.ts                   | AgentDefinition 缺少§61.1要求的 ConnectorBindings 组件(§57连接器框架) | 已修复 | 直接证据：`src/ops-maturity/agent-lifecycle/agent-registry/index.ts` 已新增 `ConnectorBindingComponentSchema`，并在 `AgentComponentsSchema` 中纳入 `connectorBindings`；`tests/unit/ops-maturity/agent-lifecycle/agent-registry/index.test.ts` 为定向验证。根因：agent 组装最初只建模 pack/prompt/model/autonomy 组件，没有把外部连接器绑定视为一等运行依赖。 |
| M-9 | MED    | explainability/explanation-pipeline-service.ts            | StageRationale 用 taskId 而非§59.3的 stageId(OAPEFLIR阶段ID)；缺少 decision 字段 | 已修复 | 直接证据：`src/ops-maturity/explainability/explanation-pipeline-service.ts` 现要求 `ExplanationRequest.stageId`，生成的 `StageRationale` 同时包含 `stageId` 与 `decision`；`tests/unit/ops-maturity/explainability/explanation-pipeline-service.test.ts` 覆盖结构化输出。根因：旧解释流水线把 rationale 绑在任务级摘要上，没有保留阶段级决策点。 |
| M-10 | MED    | explainability/explanation-pipeline-service.ts            | 解释缓存无 TTL 强制，§59.6要求 L1/L2 TTL=24h，L3不得缓存 | 已修复 | 直接证据：`src/ops-maturity/explainability/explanation-cache/index.ts` 将 `ttlHours` 限定为 `24 | 0`，`src/ops-maturity/explainability/explanation-pipeline-service.ts` 在生成时为 `L1/L2` 写入 `24`、为 `L3` 写入 `0` 并借 `putExplanationCacheEntry()` 禁止持久化；`tests/unit/ops-maturity/explainability/explanation-cache/index.test.ts` 覆盖 no-cache 路径。根因：旧缓存层只做“能否缓存”判断，没有把层级 TTL 作为 contract 固化。 |
| M-11 | MED    | cost-optimizer/cost-optimization-service.ts               | CostAttributionRecord 缺少 humanReviewCost/egressCost/computeCost/storageCost 明细和 qualityRisk(§64.1) | 已修复 | 直接证据：`src/ops-maturity/cost-optimizer/cost-optimization-service.ts` 的 `CostAttributionRecord` 已补入 `humanReviewCostUsd`、`egressCostUsd`、`computeCostUsd`、`storageCostUsd`、`qualityRisk`；`tests/unit/ops-maturity/cost-optimizer/cost-optimization-service.test.ts` 为定向验证。根因：旧成本记录只面向总额聚合，没有为质量风险与人工复核成本留出归因字段。 |
| M-12 | MED    | workflow-debugger/workflow-debugger-service.ts            | 生产断点守卫用布尔 canDebugProduction，§65.3要求断点仅存在于 ReplaySandbox，生产运行禁止断点 | 已修复 | 直接证据：`src/ops-maturity/workflow-debugger/workflow-debugger-service.ts` 已移除 `canDebugProduction` 布尔开关，`registerBreakpoint()` 仅允许 `environment === "prod"` 时 `actor.allowedRuntime === "replay_sandbox"`；`tests/unit/ops-maturity/workflow-debugger/workflow-debugger-service.test.ts` 覆盖拒绝路径。根因：旧守卫把高风险生产调试暴露成配置开关，而不是由 runtime 隔离边界强制约束。 |
| M-13 | MED    | edge-runtime/edge-runtime-sync-service.ts                 | SyncEnvelope.signature 为确定性字符串拼接非密码学签名，§62.3要求签名追加队列含 prev_hash 链完整性 | 已修复 | 直接证据：`src/ops-maturity/edge-runtime/edge-runtime-sync-service.ts` 现使用 `node:crypto` 的 `sha256` 生成 `signature`，并将 `prevHash` 串入签名与验签流程，形成链式完整性；`tests/unit/ops-maturity/edge-runtime/edge-runtime-sync-service.test.ts` 为定向验证。根因：最初同步信封只做可读性 digest 拼接，没有把链式前序哈希纳入防篡改语义。 |
| M-14 | MED    | compliance-reporter/compliance-report-pipeline-service.ts | status 仅 complete/partial，§66.2要求 generated→HumanSignoff→attested 生命周期含 EvidenceQualityScore | 已修复 | 直接证据：`src/ops-maturity/compliance-reporter/compliance-report-pipeline-service.ts` 的 `ComplianceReportArtifact.status` 已收敛到 `generated | human_signoff | attested`，并输出 `evidenceQualityScore`；`evaluateHumanSignoff()` 明确签署阶段状态；`tests/unit/ops-maturity/compliance-reporter/compliance-report-pipeline-service.test.ts` 覆盖生命周期。根因：旧流水线把报表当一次性导出产物，没有建模人签与证明质量。 |
| M-15 | MED    | multimodal/multimodal-gateway-service.ts                  | MultimodalInputPart 缺少§68.2 ContentPart 要求的 provenance/safetyLabels/mimeType/costKey | 已修复 | 直接证据：`src/ops-maturity/multimodal/multimodal-gateway-service.ts` 的 `MultimodalInputPart` 已补入 `provenance`、`safetyLabels`、`mimeType`、`costKey`，且 `evaluateSafety()` 对缺失 `mimeType` 直接阻断；`tests/unit/ops-maturity/multimodal/multimodal-gateway-service.test.ts` 为定向验证。根因：早期多模态接入只关注内容载体，没有把来源、计费与安全标签作为统一 content-part 元数据。 |
| M-16 | MED    | platform-ops-agent/platform-ops-agent-service.ts          | OpsActionType 缺少§69.1的 restart_service 和 failover 工具 | 已修复 | 直接证据：`src/ops-maturity/platform-ops-agent/platform-ops-agent-service.ts` 的 `OpsActionType` 已包含 `restart_service` 与 `failover`，`chooseActionType()` 会按 incident/errorRate 选取其一；`tests/unit/ops-maturity/platform-ops-agent/platform-ops-agent-service.test.ts` 为定向验证。根因：旧 ops agent 只偏向扩缩容与调参建议，没有把事故处置动作纳入同一工具集合。 |
| M-17 | LOW    | platform-ops-agent/self-healing-service.ts                | performHealingOperation 用 Math.random() 模拟成功/失败，§69.3要求所有写操作绑定 runbook+approval | 已修复 | 直接证据：`src/ops-maturity/platform-ops-agent/self-healing-service.ts` 的 `execute()` 现要求 `runbookRef` 与 `approvalRef` 同时存在才执行，`performHealingOperation()` 改为基于输入的确定性计算，不再调用随机数；`tests/unit/ops-maturity/platform-ops-agent/self-healing-service.test.ts` 覆盖拒绝路径。根因：旧 demo 逻辑把自愈当概率模拟，无法满足可审计、可复现实操要求。 |
| M-18 | LOW    | drift-detection/index.ts                                  | 模块头部称"Evolution Engine"聚焦自我改进，§63定义为"行为漂移检测"，术语误导 | 已修复 | 直接证据：`src/ops-maturity/drift-detection/index.ts` 头部说明已改为 `Behavior Drift Detection Module`，并把导出边界围绕 fingerprint/changepoint/cross-agent analyzer 组织；`tests/unit/ops-maturity/drift-detection/index.test.ts` 为定向验证。根因：模块演进时沿用了“evolution” 原型命名，导致职责从“检测”漂移到“自我改进”叙述。 |
| M-19 | LOW    | emergency/forensic-snapshot/index.ts                      | ForensicSnapshot 缺少按平面的证据引用，§60.2要求每平面返回含 plane/localStopState/evidenceRef 的 PanicAcknowledgment | 已修复 | 直接证据：`src/ops-maturity/emergency/forensic-snapshot/index.ts` 已新增 `PlaneForensicEvidence` 与 `planeAcknowledgments`，字段完整包含 `plane/localStopState/evidenceRef`；`src/ops-maturity/emergency/platform-panic-service.ts` 激活 panic 时会填充该结构；`tests/unit/ops-maturity/emergency/forensic-snapshot/index.test.ts` 为定向验证。根因：旧快照只聚合 artifacts/config/log，没有把五平面停机证据拆成独立引用。 |
| M-20 | LOW    | capacity-planner/capacity-planning-service.ts             | CapacityRecommendation 缺少§67.2要求的 SLA tier/queue delay/budget/approval capacity/provider quota/Region failover reserve | 已修复 | 直接证据：`src/ops-maturity/capacity-planner/capacity-planning-service.ts` 的 `CapacityRecommendation` 已补入 `slaTier`、`queueDelayRiskMs`、`budgetHeadroomPercent`、`approvalCapacityNeeded`、`providerQuotaPressure`、`regionFailoverReservePercent`；`tests/unit/ops-maturity/capacity-planner/capacity-planning-service.test.ts` 为定向验证。根因：旧建议对象偏向单点扩容动作，没有把运维审批与区域冗余成本一并建模。 |

---

## 9. OAPEFLIR v4.4 Spec vs 主架构文档（25项）

| #    | 严重度 | Spec节 vs 架构节              | 偏差描述 | 状态 | 收口证据 |
| ---- | ------ | ----------------------------- | -------------------------------------------------------------------------------------------------------------------------------- | --- | --- |
| F-1 | HIGH   | Spec§0 vs Arch§13.1           | Spec定位OAPEFLIR为"生产级Agent Runtime"含独立OapeflirRuntime；架构明确"OAPEFLIR不是执行引擎"仅为认知/治理语义框架 | 未修复 | 待补充 |
| F-2 | HIGH   | Spec§4.1 vs Arch§45.22/§5.5   | Spec定义OapeflirRun为规范运行实体含完整状态/预算；架构声明HarnessRun为唯一权威Run，OapeflirRun列于附录H废弃别名 | 未修复 | 待补充 |
| F-3 | HIGH   | Spec§4.2 vs Arch§13.1不变量#4 | Spec定义OAPEFLIR所有的RunStatus 15态驱动执行；架构禁止OAPEFLIR拥有run status/lease/retry counter/side effect commit/budget state | 未修复 | 待补充 |
| F-4 | HIGH   | Spec§3 vs Arch§45.1           | Spec呈现"OAPEFLIR Runtime"为顶层执行运行时；架构说HarnessRuntime为唯一可执行运行时入口 | 未修复 | 待补充 |
| F-5 | HIGH   | Spec§14 vs Arch§28            | Spec用 run._/node._/side_effect._ 作为OAPEFLIR事件；架构强制OAPEFLIR仅用 oapeflir.view._/oapeflir.rationale.\* | 未修复 | 待补充 |
| F-6 | HIGH   | Spec§12 vs Arch§14/§45        | Spec将Graph Scheduler置于OAPEFLIR Runtime内；架构将其置于P4执行平面HarnessRuntime管辖下 | 未修复 | 待补充 |
| F-7 | HIGH   | Spec§20.2 vs Arch§8           | Spec假设LLM可确定性重放(reexecute_with_same_seed)；架构明确"不假设LLM可确定性重放"，默认Trace Replay | 未修复 | 待补充 |
| F-8 | HIGH   | Spec§34 vs Arch§6.2/§58       | Spec用OAPEFLIR.\*错误码命名空间；架构强制PLATFORM.{plane}.{component}.{category}并禁止OAPEFLIR进入错误码命名空间 | 未修复 | 待补充 |
| F-9 | MED    | Spec§5.2 vs Arch§14           | Spec定义13个NodeRunStatus含compensating/compensated由OAPEFLIR管辖；架构规范定义在HarnessRuntime下，所有权冲突 | 未修复 | 待补充 |
| F-10 | MED    | Spec§7.3 vs Arch§13.8         | PlanNode字段名Spec用type(14种)，架构用kind | 未修复 | 待补充 |
| F-11 | MED    | Spec§15 vs Arch§18.3          | Spec将BudgetLedger置于OAPEFLIR所有含直接reservation语义；架构BudgetReservation归P5/Budget服务 | 未修复 | 待补充 |
| F-12 | MED    | Spec§16 vs Arch§14.11         | Spec将SideEffectManager置于OAPEFLIR Runtime内；架构置于P4执行平面HarnessRuntime治理下 | 未修复 | 待补充 |
| F-13 | MED    | Spec§39 vs Arch§35            | Spec建议所有运行时代码在src/platform/oapeflir/；架构推荐Harness中心目录结构，OAPEFLIR仅为trace/projection适配器 | 未修复 | 待补充 |
| F-14 | MED    | Spec§42 vs Arch§8版本         | Spec声称v4.4为核心Runtime设计基线；架构v4.3将v4.4 Spec降级为"迁移输入"非权威基线 | 未修复 | 待补充 |
| F-15 | MED    | Spec§24 vs Arch§45.25         | Spec的DecisionInputBundle缺少hitlState/nodeState；架构额外含riskState/guardrailFindings | 未修复 | 待补充 |
| F-16 | MED    | Spec§19 vs Arch§45.24         | Spec含observe/summarizer prompt角色；架构仅认Planner/Generator/Evaluator | 未修复 | 待补充 |
| F-17 | MED    | Spec§25 vs Arch§14.8/§42      | Spec定义5个RuntimeProfile层级(core/durable/governed/enterprise/learning)作OAPEFLIR内置；架构§14.8/§42为独立定义 | 未修复 | 待补充 |
| F-18 | LOW    | Spec§26 vs Arch§45.27         | Spec列6种HITL能力；架构§45.27额外含reject | 未修复 | 待补充 |
| F-19 | LOW    | Spec§23 vs Arch§45.20         | 两者均定义5层Guardrail但Spec置于OAPEFLIR治理，架构置于Harness§45.20 + P2控制平面 | 未修复 | 待补充 |
| F-20 | LOW    | Spec§22 vs Arch§45.16         | Spec定义6种Memory scope；架构用3层模型(Working/Long-term/Shared Knowledge)，分类法不同 | 未修复 | 待补充 |
| F-21 | LOW    | Spec§35 vs Arch§58.1          | Spec用oapeflir.run._指标前缀；架构用harness.run._ | 未修复 | 待补充 |
| F-22 | LOW    | Spec§40 vs Arch§33            | Spec用4阶段交付(A-D)；架构用3环模型(MVP/Hardening/Enterprise) | 未修复 | 待补充 |
| F-23 | LOW    | Spec§7.1 vs Arch§5.3/§13.8    | Spec含generatedBy含repair_worker；架构PlanGraphBundle无此字段，出处通过evidence refs追踪 | 未修复 | 待补充 |
| F-24 | LOW    | Spec§30 vs Arch§45.3          | Spec列9级策略优先级；架构ConstraintPack用4级合并(平台<租户<业务域<任务) | 未修复 | 待补充 |
| F-25 | LOW    | Spec§2 Execute产出            | Spec Execute阶段产出ExecutionReceipt；架构标注为废弃，规范为NodeAttemptReceipt | 未修复 | 待补充 |

## 10. interaction 代码 vs 架构（20项）

| #    | 严重度 | 位置                              | 偏差描述 | 状态 | 收口证据 |
| ---- | ------ | --------------------------------- | ------------------------------------------------------------------------------------------------------------------------------- | --- | --- |
| I-1 | HIGH   | nl-gateway/index.ts               | 无TaskDraft/ClarificationState/UserConfirmationReceipt预入口对象，buildTask()直接跳至RequestEnvelope绕过§39.2入口管线 | 已修复 | 直接证据：`src/interaction/nl-gateway/index.ts` 已定义 `TaskDraft`、`ClarificationState`、`UserConfirmationReceipt`，`buildTask()` 返回 `TaskBuildResult`，在 `RequestEnvelope` 之前显式产出这些预入口对象；`tests/unit/interaction/nl-gateway/index.test.ts` 为定向验证。根因：旧 NL 入口直接面向调度层 handoff，没有保留用户确认与澄清中间态。 |
| I-2 | HIGH   | nl-gateway/index.ts:414           | 默认澄清阈值0.7，§39.3强制intent_confidence_threshold=0.80/slot_confidence_threshold=0.85 | 已修复 | 直接证据：`src/interaction/nl-gateway/index.ts` 已把 `INTENT_CONFIDENCE_THRESHOLD` 固定为 `0.8`、`SLOT_CONFIDENCE_THRESHOLD` 固定为 `0.85`，且构造器用 `Math.max(INTENT_CONFIDENCE_THRESHOLD, configuredThreshold)` 防止配置把阈值降回 0.8 以下；`tests/unit/interaction/nl-gateway/nl-gateway-config-loader.test.ts` 为定向验证。根因：旧入口继承了早期演示阈值，未与架构收敛到更保守的澄清门限。 |
| I-3 | HIGH   | nl-gateway/index.ts               | 无多轮对话状态机(Idle→IntentParsing→Clarifying→Building→Confirming→Executing→Reporting)§39.5要求 | 已修复 | 直接证据：`src/interaction/nl-gateway/index.ts` 已引入完整 `ConversationState` 枚举，并在 `parseInput()` / `buildTask()` 中显式生成 `IntentParsing`、`Clarifying`、`Building`、`Confirming` 等状态；`tests/unit/interaction/nl-gateway/conversation-context.test.ts` 与 `tests/unit/interaction/nl-gateway/index.test.ts` 为定向验证。根因：旧实现只做单轮“解析后建单”，没有把多轮入口状态作为一等对象。 |
| I-4 | HIGH   | nl-gateway/                       | NL管线无任何Prompt Injection防御，违反§39.6安全约束(引用§16.5) | 已修复 | 直接证据：`src/interaction/nl-gateway/index.ts` 已增加 `PROMPT_INJECTION_PATTERNS`、`PromptInjectionFinding`、`securityFindings` 与 `blockedByPolicy`，恶意指令模式会直接进入拒绝/澄清路径；`tests/e2e/prompt-injection-guard-e2e.test.ts` 与 `tests/unit/interaction/nl-gateway/index.test.ts` 为定向验证。根因：旧 NL 入口把用户输入当纯语义文本处理，没有在入口层做提示注入分类。 |
| I-5 | HIGH   | nl-gateway/index.ts               | 缺少§39.3要求的ContextEnricher/ResponseFormatter组件 | 已修复 | 直接证据：`src/interaction/nl-gateway/index.ts` 已实现 `ContextEnricher` 与 `ResponseFormatter` 类，并在 `NlEntryService` 中作为固定组件实例使用；`tests/unit/interaction/nl-gateway/index.test.ts` 为定向验证。根因：旧入口把上下文补全和响应整形散落在主流程里，缺少清晰可替换的组件边界。 |
| I-6 | HIGH   | nl-gateway/index.ts:71            | DetectedIntent.intentType含"system_config"无架构对应定义 | 已修复 | 直接证据：`src/interaction/nl-gateway/index.ts` 中 `DetectedIntent.intentType` 已收敛到 `task_create | task_query | task_modify | status_inquiry | approval_action` 五类；`src/interaction/nl-gateway/disambiguation-handler/index.ts` 同步移除了 `system_config` 推断；`tests/unit/interaction/nl-gateway/disambiguation-handler.test.ts` 为定向验证。根因：旧意图分类混入了内部运维快捷语义，越过了架构定义的用户入口意图集。 |
| I-7 | MED    | goal-decomposer/index.ts          | 无GoalLifecycleState状态机(draft→decomposing→decomposed→executing→completed…)§40.5要求 | 已修复 | 直接证据：`src/interaction/goal-decomposer/index.ts` 已定义 `GoalLifecycleState = draft | decomposing | decomposed | executing | completed | failed | cancelled`，并在分解结果中输出 `lifecycleState`；`tests/unit/interaction/goal-decomposer/index.test.ts` 为定向验证。根因：旧目标分解器把结果视为一次性纯函数输出，没有把目标生命周期带出到运行时。 |
| I-8 | MED    | goal-decomposer/index.ts          | 输出tasks/dependencyGraph但不产出§40.2要求的GoalGraphDraft/TaskGraphDraft，无draft→planner交接 | 已修复 | 直接证据：`src/interaction/goal-decomposer/index.ts` 已新增 `GoalGraphDraft`、`TaskGraphDraft` 与 `PlannerHandoffReceipt`，`decompose()` 返回值中统一输出；`tests/unit/interaction/goal-decomposer/goal-decomposer-enhancements.test.ts` 为定向验证。根因：旧实现只生成任务列表和依赖图，没有为 planner handoff 建立显式契约对象。 |
| I-9 | MED    | goal-decomposer/index.ts          | 分解时无budget/risk/permission/capability约束传播(§40.2)，仅事后buildRiskSummary | 已修复 | 直接证据：`src/interaction/goal-decomposer/index.ts` 已新增 `GoalConstraintEnvelope`，由 `parseConstraintEnvelope()` 解析预算、权限、能力与审批要求，并挂到 `PlannedTask.constraintEnvelope`、`GoalGraphDraft.constraintEnvelope`、`PlannerHandoffReceipt.constraintEnvelope`；`tests/unit/interaction/goal-decomposer/index.test.ts` 为定向验证。根因：旧分解器把约束只放在风险摘要文本里，没有进入可执行 graph handoff。 |
| I-10 | HIGH   | proactive-agent/index.ts:5        | TriggerDefinition.type用"threshold"而非架构"condition"(§41.2: schedule/event/condition/webhook)；"webhook_inbound" vs "webhook" | 已修复 | 直接证据：`src/interaction/proactive-agent/index.ts` 已引入 canonical `CanonicalTriggerType = schedule | event | condition | webhook`，并通过 `normalizeTriggerType()` 将 legacy `threshold`/`webhook_inbound` 归一化后再进入运行时；`tests/unit/interaction/proactive-agent/index.test.ts` 为定向验证。根因：旧触发器沿用了产品原型命名，没有在运行时统一到架构 canonical 枚举。 |
| I-11 | MED    | proactive-agent/index.ts          | 无ProactiveBudgetPool或UserInitiatedReserveRatio(≥60%)强制(§41.4) | 已修复 | 直接证据：`src/interaction/proactive-agent/index.ts` 已新增 `ProactiveBudgetPool`，并在 `evaluate()` 中用 `Math.max(0.6, budgetPool.userInitiatedReserveRatio)` 为用户发起预算保底；`tests/unit/interaction/proactive-agent/index.test.ts` 为定向验证。根因：旧主动代理只有 trigger 频控，没有把预算池和用户保留份额纳入门禁。 |
| I-12 | MED    | proactive-agent/index.ts          | 无触发器间反馈环路检测(§41.4要求检测互触发循环并创建incident) | 已修复 | 直接证据：`src/interaction/proactive-agent/index.ts` 已实现 `detectFeedbackLoop()`，检测到循环后写入 `ProactiveIncident` 并禁用环中触发器；`tests/unit/interaction/proactive-agent/index.test.ts` 为定向验证。根因：旧实现只校验单个触发器条件，没有对触发器图做环路分析。 |
| I-13 | MED    | proactive-agent/index.ts:43       | TriggerDefinition缺少§41.2要求的maxFireCount/boundAgentId字段 | 已修复 | 直接证据：`src/interaction/proactive-agent/index.ts` 的 `TriggerDefinition` 已新增 `maxFireCount`、`boundAgentId`，`evaluate()` 会对 `maxFireCount` 做强制门禁；`tests/unit/interaction/proactive-agent/index.test.ts` 为定向验证。根因：旧结构只关心“能不能触发”，没有把绑定 agent 与生命周期计数做成契约字段。 |
| I-14 | HIGH   | dashboard/                        | 无MetricRegistry含metric_owner/freshness_slo/stale_behavior/redaction(§43.1) | 已修复 | 直接证据：`src/interaction/dashboard/index.ts` 已定义 `MetricRegistryEntry`，字段完整覆盖 `metricOwner/sourceOfTruth/freshnessSlo/staleBehavior/redactionPolicy`，并在 `OperatorDashboard.metricRegistry` 输出；`tests/unit/interaction/dashboard/index.test.ts` 为定向验证。根因：旧仪表盘只展示聚合结果，没有为指标治理保留 source-of-truth 元数据。 |
| I-15 | MED    | dashboard/                        | NL摘要生成无evidence_refs/freshness/confidence/redaction_policy/source_projection_version元数据(§43.6) | 已修复 | 直接证据：`src/interaction/dashboard/index.ts` 的 `DailySummary.nlSummaryMetadata` 已补齐 `evidenceRefs`、`freshness`、`confidence`、`redactionPolicy`、`sourceProjectionVersion`；`tests/unit/interaction/dashboard/dashboard-aggregation-service.test.ts` 为定向验证。根因：旧摘要输出面向展示层，缺少可回链的投影元数据。 |
| I-16 | MED    | dashboard/                        | 无仪表盘操作风险门控(§43.6)：按钮不应直接触发高风险指令 | 已修复 | 直接证据：`src/interaction/dashboard/index.ts` 已新增 `DashboardActionControl`，并通过 `buildActionControls()` 为注意事项和建议项输出 `executionMode = direct | requires_confirmation | blocked`；`tests/unit/interaction/dashboard/dashboard-aggregation-service.test.ts` 为定向验证。根因：旧 dashboard 把动作入口当静态链接，没有把风险等级转成执行门控。 |
| I-17 | MED    | autonomy/trust-scorer/index.ts:14 | TrustLevel 6值枚举与架构§42.2自治级别(suggestion/supervised/semi_auto/full_auto)无正式映射桥接 | 已修复 | 直接证据：`src/interaction/autonomy/index.ts` 已引入 `mapAutonomyLevelToUnifiedRuntimeMode`，并通过 `toUnifiedRuntimeMode()` 把自治级别桥接到统一 runtime mode；`tests/unit/interaction/autonomy/index.test.ts` 与 `tests/unit/interaction/autonomy/domain-risk-autonomy-cap.test.ts` 为定向验证。根因：旧自治实现只在 trust score 内部演算，没有与平台运行模式建立权威映射。 |
| I-18 | MED    | autonomy/                         | 无TrustDecayWorker或每日衰减机制(§42.3)；无降级前的AutonomyChangeImpactReport(§42.4) | 已修复 | 直接证据：`src/interaction/autonomy/index.ts` 已新增 `TrustDecayWorker`，并在渐进自治评估结果中输出 `AutonomyChangeImpactReport[]`；`tests/unit/interaction/autonomy/progressive-autonomy.test.ts` 为定向验证。根因：旧自治引擎只关注当前分数，不跟踪长期不活跃衰减和变更影响评估。 |
| I-19 | MED    | ux/workflow-builder-service.ts    | 可视化工作流构建器保存前不执行§44.3要求的PlanGraph Normalize/Validate/RiskPropagation/WorstPathAnalysis | 已修复 | 直接证据：`src/interaction/ux/workflow-builder-service.ts` 在 `saveReview` 中显式执行 `normalizeGraph()`、`validateGraph()`、`propagateRisk()`、`analyzeWorstPath()`；`tests/unit/interaction/ux/workflow-builder-service.test.ts` 为定向验证。根因：旧可视化构建器只关心前端画布生成，没有在保存前执行图级审查。 |
| I-20 | LOW    | nl-gateway/index.ts               | deriveUrgency将"critical"关键词映射为"high"而非"critical"，低估紧急度 | 已修复 | 直接证据：`src/interaction/nl-gateway/index.ts` 的 `deriveUrgency()` 已对 `critical|sev1|p0|立刻停机|立即回滚` 等词直接返回 `critical`；`tests/unit/interaction/nl-gateway/index.test.ts` 为定向验证。根因：旧紧急度推断把最高风险词汇压扁到 `high`，导致确认与审批路径低估事件等级。 |

## 11. domains + sdk 代码 vs 架构（20项）

| #    | 严重度 | 位置                                                  | 偏差描述 | 状态 | 收口证据 |
| ---- | ------ | ----------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --- | --- |
| D-1 | HIGH   | domains/canonical-meta-model/types.ts                 | 元模型仅12问(Q1-Q12)，架构§37.11要求15问(缺Q13 liability_owner/Q14 compensation_model/Q15 adversarial_scenarios) | 已修复 | 直接证据：`src/domains/canonical-meta-model/types.ts` 的 `META_MODEL_QUESTION_IDS` 已补齐 `Q13_liability_owner`、`Q14_compensation_model`、`Q15_adversarial_scenarios` 共 15 问；`tests/unit/domains/canonical-meta-model/types.test.ts` 与 `tests/integration/domains/canonical-meta-model/meta-model-integration.test.ts` 为定向验证。根因：旧元模型仍停留在上线前问卷版本，没有把责任归属、补偿模型和对抗场景纳入域建模必答项。 |
| D-2 | HIGH   | sdk/pack-sdk/pack-manifest.ts                         | BusinessPackManifest缺少§30.2强制字段：domain_id(用domain)/side_effects/data_classes/max_risk_class/tools/connectors/plugins/eval_requirements/compatibility | 已修复 | 直接证据：`src/sdk/pack-sdk/pack-manifest.ts` 的 `BusinessPackManifest` 已包含 `domainId`（并兼容 `domain`）、`sideEffects`、`dataClasses`、`maxRiskClass`、`tools`、`connectors`、`plugins`、`evalRequirements`、`compatibility`，`validateBusinessPackManifest()` 会完成归一化；`tests/unit/sdk/workbench/workbench-service.test.ts` 与相关 pack SDK 测试为定向验证。根因：旧 pack manifest 只服务最小 demo 安装流，缺少风险和兼容性字段。 |
| D-3 | HIGH   | domains/registry/domain-model.ts:77                   | 域状态枚举 draft/testing/active/deprecated 不匹配§37.10生命周期 Draft/Validated/Registered/Active/Updating/Deprecated/Archived | 已修复 | 直接证据：`src/domains/registry/domain-model.ts` 的 `DomainDefinitionSchema.status` 已收敛为 `draft | validated | registered | active | updating | deprecated | archived`，并仅把 `testing` 作为 legacy alias 输入；`tests/unit/domains/registry/domain-model-validation.test.ts` 为定向验证。根因：域注册模型沿用了早期四态生命周期，未跟随域上线与更新治理扩展。 |
| D-4 | HIGH   | domains/(缺失)                                        | 架构§37.2 v4.3分解规格(DomainCoreDescriptor/DomainExecutionProfile/DomainRiskSpec/DomainKnowledgeSpec/DomainEvalSpec/DomainGovernanceSpec/DomainInteractionSpec)无类型实现 | 已修复 | 直接证据：`src/domains/domain-specs.ts` 已实现 `DomainCoreDescriptorSchema`、`DomainExecutionProfileSchema`、`DomainRiskSpecSchema`、`DomainKnowledgeSpecSchema`、`DomainEvalSpecSchema`、`DomainGovernanceSpecSchema`、`DomainInteractionSpecSchema`；`src/domains/index.ts` 已对外导出；`tests/unit/domains/domain-model-fields.test.ts` 为定向验证。根因：旧 domains 模块只有聚合式 descriptor，没有拆出 v4.3 所需的分层规格类型。 |
| D-5 | HIGH   | sdk/(缺失)                                            | 架构§22.1要求4层SDK；Admin SDK完全缺失 | 已修复 | 直接证据：`src/sdk/admin-sdk/index.ts` 已实现 `AdminSdk`，并由 `src/sdk/index.ts` 导出；Admin 层现提供 `listDomains/registerDomain/publishPack/pauseHarnessRun/abortHarnessRun`；`tests/unit/sdk/index.test.ts` 为定向验证。根因：最初 SDK 只覆盖 client/harness/pack 路径，遗漏了管理员控制面入口。 |
| D-6 | HIGH   | domains/domain-descriptor-orchestration-service.ts:15 | lifecycleState枚举含validating/certified/canary不在§37.10状态机中；缺registered/updating/archived | 已修复 | 直接证据：`src/domains/domain-descriptor-orchestration-service.ts` 通过 `normalizeLifecycleState()` 将 `validating/certified/canary/retired` 归一到 canonical `validated/registered/archived`，并用 `DomainLifecycleStateSchema` 做最终约束；`tests/unit/domains/domain-descriptor-orchestration-service.test.ts` 为定向验证。根因：descriptor 编排层曾直接暴露历史 UI 状态名，没有在域治理边界做归一化。 |
| D-7 | MED    | sdk/plugin-sdk/plugin-definition.ts:9                 | PluginType含"presenter"不在架构§22.1(tool/adapter/retriever/evaluator)中 | 已修复 | 直接证据：`src/sdk/plugin-sdk/plugin-definition.ts` 中 `PluginType` 已收敛为 `tool | adapter | retriever | evaluator`，`presenter/planner/validator` 仅保留在 `PluginRole` 作为语义角色；`tests/unit/domains/registry/plugin-spi-types.test.ts` 为定向验证。根因：旧 plugin DSL 把执行类型和展示/规划语义角色混在同一枚举里。 |
| D-8 | MED    | domains/registry/domain-model.ts:60                   | PluginBinding.pluginType枚举retriever/validator/planner/presenter/adapter与架构tool/adapter/retriever/evaluator不匹配 | 已修复 | 直接证据：`src/domains/registry/domain-model.ts` 的 `PluginBindingSchema.pluginType` 已限制为 `tool | adapter | retriever | evaluator`，对 `planner/presenter/validator` 仅做 legacy alias 归一；附加语义改由 `bindingRole` 表达；`tests/unit/domains/registry/domain-model.test.ts` 为定向验证。根因：旧域绑定把“绑定到什么执行 SPI”与“在域内扮演什么角色”混成一个字段。 |
| D-9 | MED    | domains/recipes/index.ts:3                            | DomainRecipe缺少archetype字段，架构§37.7定义12种recipe原型无枚举或类型 | 已修复 | 直接证据：`src/domains/recipes/index.ts` 已定义 12 项 `DomainRecipeArchetypeSchema` 并将 `archetype` 纳入 `DomainRecipeSchema`；`src/domains/domain-recipe-service.ts` 的 `prototypeTemplates` 同步覆盖 12 个原型模板；`tests/unit/domains/domain-recipe-service.test.ts` 为定向验证。根因：旧 recipe 只按触发短语匹配，没有把原型类别编码成治理字段。 |
| D-10 | MED    | sdk/client-sdk/api-client.ts                          | 无规范API端点的类型化方法(/api/v1/harness-runs, /api/v1/packs, abort/pause per §6) | 已修复 | 直接证据：`src/sdk/client-sdk/api-client.ts` 已实现 `listHarnessRuns()`、`createHarnessRun()`、`pauseHarnessRun()`、`abortHarnessRun()`、`listPacks()`、`publishPack()` 等类型化 API 方法；`tests/unit/sdk/cli/api-server.test.ts` 与 workbench 测试为定向验证。根因：旧 client SDK 只有通用 request 封装，没有把规范控制面端点提升为一等方法。 |
| D-11 | MED    | domains/governance/domain-governance-policy.ts        | 缺少§37.9治理字段：slo_profile/budget_constraints/max_hibernation_renewals/compliance_rules/recertification/waiver | 已修复 | 直接证据：`src/domains/governance/domain-governance-policy.ts` 已补齐 `sloProfile`、`budgetConstraints`、`maxHibernationRenewals`、`complianceRules`、`recertification`、`waiver` 等字段；`tests/unit/domains/governance/domain-governance-policy.test.ts` 为定向验证。根因：旧治理策略只覆盖角色与 rollout，没有把持续合规和预算门禁收敛进同一 policy。 |
| D-12 | MED    | domains/operations/index.ts:3                         | 上架阶段 modeling/development_validation/security_certification/canary_launch 不对齐§37.10+§38 runbook | 已修复 | 直接证据：`src/domains/operations/index.ts` 已将 canonical phase 收敛到 `domain_modeling | pack_development | security_certification | gray_rollout`，并对 `modeling/development_validation/canary_launch` 做 alias 归一；`tests/unit/domains/operations/index.test.ts` 为定向验证。根因：旧 onboarding 阶段名来自早期 runbook 草稿，没有与主架构最终术语统一。 |
| D-13 | MED    | sdk/pack-sdk/pack-manifest.ts:3                       | BusinessPackCapability用非正式maturity字段代替架构§30.2的PackCapabilityProfile结构 | 已修复 | 直接证据：`src/sdk/pack-sdk/pack-manifest.ts` 已定义 `PackCapabilityProfile`，并在每个 `BusinessPackCapability` 上支持 `profile.maturity/requiredContracts/supportedPluginTypes/evaluationMode`；legacy `maturity` 只保留为兼容输入；`tests/unit/domains/business-pack/business-pack-manifest.test.ts` 为定向验证。根因：旧 capability 结构过于扁平，无法表达插件类型和评测模式。 |
| D-14 | MED    | domains/(缺失)                                        | 无execution_mode/hot_path_mode/planning_mode字段，§37.2要求用于确定性热路径强制 | 已修复 | 直接证据：`src/domains/domain-specs.ts` 的 `DomainExecutionProfileSchema` 已新增 `executionMode.planningMode`、`executionMode.hotPathMode`、`llmInHotPathAllowed`、`maxHotPathLatencyMs`；`src/domains/registry/domain-model.ts` 也已挂载 `executionProfile`；`tests/unit/domains/domain-model-fields.test.ts` 为定向验证。根因：旧域模型只声明能力，不表达执行策略，无法约束确定性热路径。 |
| D-15 | MED    | sdk/pack-sdk/pack-manifest.ts:17                      | validateBusinessPackManifest不验证domain_id指向Active DomainDescriptor(§30.2) | 已修复 | 直接证据：`src/sdk/pack-sdk/pack-manifest.ts` 的 `validateBusinessPackManifest()` 已支持 `activeDomainIds` 选项，并在 `compatibility.requiresActiveDomain` 语义下对 `domainId` 做活动域校验；`tests/unit/sdk/workbench/workbench.test.ts` 为定向验证。根因：旧校验器只做字段合法性检查，没有接入域注册状态。 |
| D-16 | LOW    | sdk/workbench/index.ts:134                            | 预览URL用旧/tasks和/approvals路径而非规范/harness-runs(§6) | 已修复 | 直接证据：`src/sdk/workbench/index.ts` 的 shortcut 与 readiness 预览 URL 已切到 `/harness-runs` 与 `/packs`，不再使用旧 `/tasks` 路径；`tests/unit/sdk/workbench/index.test.ts` 与 `tests/unit/sdk/workbench/workbench-service.test.ts` 为定向验证。根因：workbench 仍沿用旧任务视角 API 入口，没有同步到 harness-run 控制面。 |
| D-17 | LOW    | sdk/plugin-sdk/plugin-definition.ts:29                | PluginDefinition缺少§22.4 PluginManifest字段：spiTypes/domainIds/SBOM/signing | 已修复 | 直接证据：`src/sdk/plugin-sdk/plugin-definition.ts` 的 `PluginDefinition` 已补入 `spiTypes`、`domainIds`、`sbomRef`、`signing`，`definePlugin()` 与 `validatePluginDefinition()` 会统一归一化；`tests/unit/domains/registry/plugin-spi-schema.test.ts` 为定向验证。根因：旧 plugin DSL 偏向本地运行配置，没有把供应链与域约束字段纳入 manifest。 |
| D-18 | LOW    | sdk/pack-sdk/pack-scaffold-service.ts                 | 脚手架不生成domain lint/domain validate集成，§37要求Gate 2前通过domain lint | 已修复 | 直接证据：`src/sdk/pack-sdk/pack-scaffold-service.ts` 的 `MINIMAL_PACKAGE_JSON` 已生成 `domain:lint` 与 `domain:validate` 脚本，标准/完整模板复用该结构；`tests/unit/sdk/cli/acceptance-readiness.test.ts` 可作为旁证。根因：旧脚手架只生成 build/test，未把域 gate 校验作为默认脚手架能力。 |
| D-19 | LOW    | sdk/harness-sdk/index.ts:16                           | HarnessSdkAppendStepInput用旧stage/inputs/outputs术语而非规范nodeRunId/planGraphId(§5/§45) | 已修复 | 直接证据：`src/sdk/harness-sdk/index.ts` 的 `HarnessSdkAppendStepInput` 已以 `nodeRunId` 与 `planGraphId` 为主字段，`stage/phase` 只保留为兼容别名并在桥接时写回 `inputs`；`tests/unit/sdk/index.test.ts` 为定向验证。根因：旧 SDK 先围绕 step 语义设计，后来转到 canonical `NodeRun/PlanGraph` 时没有把输入模型整体更新。 |
| D-20 | LOW    | domains/registry/domain-registry-service.ts:64        | activate()从任意状态直接转active，无§37.10要求的Draft→Validated→Registered→Active路径守卫 | 已修复 | 直接证据：`src/domains/registry/domain-registry-service.ts` 的 `register()` 会把 `validated` 归一到 `registered`，而 `activate()` 明确只允许 `registered -> active`，否则抛 `domain_registry.invalid_activation_state`；`tests/unit/domains/registry/domain-registry-service.test.ts` 与 `tests/e2e/domain-onboarding-flow.test.ts` 为定向验证。根因：旧激活逻辑把“通过 smoke test”直接等同于“可激活”，未显式守住域生命周期链。 |
## 12. Contract 文档 vs 架构（第3批，25项）

| #    | 严重度 | Contract 文件                                      | 偏差描述 | 状态 | 收口证据 |
| ---- | ------ | -------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- | --- | --- |
| T-32 | HIGH   | transition_service_contract                        | TransitionCommand.entity_kind用pre-v4.3类型(task/workflow/session/approval/execution)，架构§5.3用harness_run/node_run/side_effect/budget_reservation | 已修复 | 直接证据：`docs_zh/contracts/transition_service_contract.md` 已把 `TransitionCommand.entity_kind` 改为 `harness_run / node_run / side_effect / budget_reservation` truth + `*_projection` 投影，并把 `execution/task/workflow` 明确降为 migration 输入；服务入口也已改成 `RuntimeStateMachine.transition(command)`、`transitionHarnessRun`、`transitionNodeRun`、`transitionSideEffect`、`transitionBudgetReservation`。根因：transition service 文案直接继承了旧 repository 表模型，`HarnessRun / NodeRun / SideEffect / BudgetReservation` 成为 truth aggregate 后该合同没有同步迁移。定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |
| T-33 | HIGH   | storage_schema_contract                            | 核心表(tasks/workflow_state/executions)无映射到v4.3规范对象(HarnessRun/NodeRun/PlanGraphBundle)，缺对应表或迁移路径 | 已修复 | 直接证据：`docs_zh/contracts/storage_schema_contract.md` 已在 §1/§3/§7/§8/§14A 把 runtime truth 主链改为 `harness_runs / plan_graph_bundles / node_runs / node_attempts / node_attempt_receipts / budget_ledgers / budget_reservations`，并明确 `tasks / workflow_state / workflow_step_outputs / sessions / messages` 仅为 projection / interaction 表。根因：存储合同沿用了 v3 单机任务表驱动叙事，把投影表误写成 runtime truth，直到 v4.3 run/node/ledger 主链落地后正文仍未重构。定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |
| T-34 | HIGH   | storage_schema_contract                            | memories表DDL遗漏合约自身§13声明的最小列：layer_level/token_budget/freshness_state/source_refs_json | 已修复 | 直接证据：`docs_zh/contracts/storage_schema_contract.md` 的 §13 `memories` 最小列与 §15 SQLite DDL 附录现在都包含 `layer_level / token_budget / freshness_state / source_refs_json`。根因：memory contract 扩展时，只更新了正文最小列，没有同步更新同文件里的 DDL 附录，导致 contract 内部自相矛盾。定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |
| T-35 | HIGH   | monetization_metering_plane_contract               | 引入BillingLedger/LedgerEntry但架构§18用BudgetLedger/BudgetSettlement为冻结合约(§1.5)，命名碰撞 | 已修复 | 直接证据：`docs_zh/contracts/monetization_metering_plane_contract.md` 已把核心组件/对象/流程改为 `BudgetLedgerProjector`、`SettlementReadModel`、`SettlementReadEntry`，并明确 `BudgetLedger / BudgetReservation / BudgetSettlement` 复用 `budget-ledger-contract.md` 作为 runtime truth。根因：旧商业化合同把发票/报表读模型和执行前预算 truth 混成同一层，v4.3 预算账本冻结后正文仍保留 `BillingLedger / LedgerEntry` 作为核心对象。定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |
| T-36 | HIGH   | marketplace_catalog_and_revenue_contract           | 定义RevenueSharePolicy含结算/分成字段，架构§55.4明确"收益分成/计费结算不属于核心运行架构"禁止影响Pack执行/安全 | 已修复 | 直接证据：`docs_zh/contracts/marketplace_catalog_and_revenue_contract.md` 已把 `RevenueSharePolicy` 改为 `CommercialTermsProjection`，并明确收益分成/税务/退款/结算周期只能作为商业投影，不能参与 Pack 执行授权、安装安全判定或 runtime sandbox 决策。根因：旧市场合同把商业结算域与运行时安装/安全治理写在同一层，导致结算字段看起来可作为执行门禁输入。定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |
| T-37 | HIGH   | idempotency_and_recovery_matrix_contract           | 全文使用"workflow step"/"step"术语(§4 Step级矩阵)，架构v4.3§5.5声明NodeRun/NodeAttempt为规范，stepId仅为legacy投影 | 已修复 | 直接证据：`docs_zh/contracts/idempotency_and_recovery_matrix_contract.md` 已把范围、§4 矩阵和收口结论收敛到 `NodeRun / NodeAttempt`，并明确 `step_id / workflow step / agent step` 只允许作为语义标签或展示投影，自动恢复前必须核验 `NodeAttemptReceipt`。根因：恢复合同直接沿用了早期线性 workflow 文案，在 `NodeRun / NodeAttempt / NodeAttemptReceipt` 成为 runtime truth 后正文仍停留在 step 视角。定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |
| T-38 | MED    | plugin_spi_contract                                | DomainPlannerPlugin.plan()返回Plan而非规范PlanGraphBundle；§7 OAPEFLIR表用"Rollout"而非"Release" | 已修复 | 直接证据：`docs_zh/contracts/plugin_spi_contract.md` 已将 `DomainPlannerPlugin.plan()` 返回值改为 `PlanGraphBundle`，`ValidationContext`/`UnifiedAssessment` 对齐到 `harnessRunId / nodeRunId / attemptId`，且 §7 阶段表已将 `Rollout` 改回 `Release`。根因：Plugin SPI 仍沿用早期“线性计划 + rollout”接口草案，没有随着 v4.3 的 graph handoff 和 Release 命名同步升级。定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |
| T-39 | MED    | trace_and_root_cause_observability_contract        | "一个task=一个trace"，架构§5.5声明HarnessRun为规范run truth；应为"一个HarnessRun=一个trace" | 已修复 | 直接证据：`docs_zh/contracts/trace_and_root_cause_observability_contract.md` 已把 trace 模型改成 `一个 HarnessRun = 一个 trace`，并将 `harness_run_id / node_run_id / attempt_id` 写为必传关联字段；`task_id / execution_id` 只保留为 legacy / projection 关联键。根因：observability 合同继承了旧 task-centric 单机执行模型，没有随着 `HarnessRun / NodeRun / NodeAttempt` 成为 runtime truth 一起重写追踪主键。定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |
| T-40 | MED    | billing_and_tenant_contract                        | 用UsageMeter而非架构§18.1的UsageRecord；未引用冻结的BudgetLedger/BudgetReservation | 已修复 | 直接证据：`docs_zh/contracts/billing_and_tenant_contract.md` 已把计量事实对象从 `UsageMeter` 改为 `UsageRecord`，补入 `tenant_id / workspace_id / harness_run_id / node_run_id / cost_source`，并显式把 `BudgetLedger / BudgetReservation` 纳入关键对象与行为约束。根因：计费合同长期停留在产品套餐/账单视角，没有随着 v4.3 的 usage fact 与预算 truth 模型同步升级。定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |
| T-41 | MED    | cost_and_budget_contract                           | BudgetPolicy仅max_task/daily/monthly_cost_usd，架构§18.3强制多维：max_cost/max_model_tokens/max_context_tokens/max_output_tokens/max_steps/max_duration_ms | 已修复 | 直接证据：`docs_zh/contracts/cost_and_budget_contract.md` 已将 `BudgetPolicy` 最小字段改为 `max_cost_usd / max_model_tokens / max_context_tokens / max_output_tokens / max_steps / max_duration_ms`，并把旧 `max_task_cost_usd / max_daily_cost_usd / max_monthly_cost_usd` 明确降为 billing / accounting projection guardrail。根因：成本合同沿用了报表/账单口径，没有随着 v4.3 runtime budget guard 升级为多维预算约束。定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |
| T-42 | MED    | cross_region_routing_and_data_residency_contract   | RegionDescriptor缺provider/endpoints/dataResidencyPolicy(§52.1)；缺写边界规则CAS/Lease/Fencing(§52.3) | 已修复 | 直接证据：`docs_zh/contracts/cross_region_routing_and_data_residency_contract.md` 已为 `RegionDescriptor` 补齐 `provider / control_plane_endpoint / data_plane_endpoint / data_residency_policy`，并新增跨 region 接管时的 `CAS / lease / fencing token` 写边界规则。根因：旧合同只覆盖路由和驻留打分，没有把多 region 下的 truth 写边界与接管语义纳入 contract。定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |
| T-43 | MED    | marketplace_catalog_and_revenue_contract           | lifecycle_state用draft/submitted/certified/published/deprecated/retired，架构§55.5用active/deprecated/sunset/removed，不兼容 | 已修复 | 直接证据：`docs_zh/contracts/marketplace_catalog_and_revenue_contract.md` 已新增 `review_status` 承载 `draft / submitted / certified`，并把 `lifecycle_state` 收敛到 `active / deprecated / sunset / removed`，同时补齐对应 install / sunset / remove 规则。根因：历史文案把审核流程状态和运行可用生命周期混成一个枚举，没有在 v4.3 将 review workflow 与 runtime lifecycle 分离。定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |
| T-44 | MED    | sso_scim_and_identity_sync_contract                | 缺架构§48.2要求的identity_sync_dlq；遗漏SAML 2.0(架构标注"必须") | 已修复 | 直接证据：`docs_zh/contracts/sso_scim_and_identity_sync_contract.md` 已把 `protocol` 收敛为 `oidc | saml2 | scim`，新增 `IdentitySyncDlqRecord` 最小字段，并明确 identity sync 失败必须进入 `identity_sync_dlq`、支持重放与幂等重试。根因：旧合同把登录协议与身份同步通道混写，同时忽略了企业接入最关键的失败补偿链。定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |
| T-45 | MED    | edge_runtime_and_sync_contract                     | 缺架构§8.3要求EdgeRuntime声明stateful=true/lease_migration_supported/checkpoint_required_before_preempt | 已修复 | 直接证据：`docs_zh/contracts/edge_runtime_and_sync_contract.md` 已为 `EdgeRuntimeProfile` 增补 `stateful / lease_migration_supported / checkpoint_required_before_preempt`，并增加活跃 `NodeRun` 在回连前必须先提交 checkpoint / receipt / side-effect evidence 的规则。根因：旧边缘合同只关注“能否同步”，没有把 stateful 执行、lease 迁移和抢占前 checkpoint 这些运行时硬约束写进正文。定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |
| T-46 | MED    | node-run-attempt-receipt-contract                  | Receipt主键字段为nodeAttemptReceiptId，架构§5.3 NodeAttemptReceipt用receiptId | 已修复 | 直接证据：`docs_zh/contracts/node-run-attempt-receipt-contract.md` 已把 `NodeAttemptReceipt` 主键字段从 `nodeAttemptReceiptId` 改为 `receiptId`，并在 legacy 映射中把 `nodeAttemptReceiptId` 降为 deprecated storage-shaped key。根因：旧合同直接跟随底层表命名暴露 table-shaped 字段名，没有维持 v4.3 executable contract 的 canonical API 形状。定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |
| T-47 | MED    | observability_contract                             | RuntimeMetricsSummary用oapeflirMetrics.convergenceRate为顶层指标，架构§5.5/§13声明OAPEFLIR仅为投影/trace非truth | 已修复 | 直接证据：`docs_zh/contracts/observability_contract.md` 已将 `RuntimeMetricsSummary` 主维度改为 `harnessRunMetrics / nodeRunMetrics / attemptMetrics`，并把 OAPEFLIR 指标降为 `oapeflirViewMetrics / stageViewMetrics`，同时显式声明它们只能作为 view / trace / rationale 指标。根因：旧观测合同混淆了 runtime truth 健康指标与认知/解释视图指标。定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |
| T-48 | MED    | token_budget_allocation_contract                   | 定义10种预算维度含KV cache分区但未引用冻结的BudgetReservation状态机(reserved→settled→released) | 已修复 | 直接证据：`docs_zh/contracts/token_budget_allocation_contract.md` 已将预算维度收敛为 `per_harness_run_budget / per_node_budget` 等 run/node 语义，并新增“与 `BudgetReservation` 状态机的绑定”小节，明确实际占用必须遵守 `reserved -> settled -> released` 生命周期。根因：旧合同只覆盖 prompt/token 规划侧，没有把预算切片如何落到冻结的 `BudgetReservation` truth 状态机写进合同。定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |
| T-49 | MED    | supply_chain_and_dependency_security_contract      | 缺架构§11.7 PluginTrustStore要求：trust root/signing key rotation/revocation list/security advisory/quarantine/tenant impact | 已修复 | 直接证据：`docs_zh/contracts/supply_chain_and_dependency_security_contract.md` 已新增 `PluginTrustStore` 最小字段，补入 `trust_roots / active_signing_keys / signing_key_rotation_policy / revocation_list_ref / security_advisory_ref / quarantine_status / tenant_impact_scope`，并把这些治理动作写成强制规则与审计字段。根因：旧供应链合同停留在安装前扫描视角，没有把插件信任根、密钥轮换、撤销/公告和租户影响面做成持续治理对象。定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |
| T-50 | LOW    | enterprise_secret_management_contract              | 提到短期凭证但未强制架构§11.3硬TTL上限"secret注入短时有效(TTL≤300s)" | 已修复 | 直接证据：`docs_zh/contracts/enterprise_secret_management_contract.md` 已把 secret 注入型短时凭证强制收敛到 `TTL <= 300s`，并在审计字段中新增 `ttl_seconds`。根因：旧 secret 合同只定性要求“短时凭证”，没有把运行时注入的 TTL 上限写成硬约束。定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |
| T-51 | LOW    | tenant_isolation_and_shared_worker_safety_contract | 无架构§9.1自动隔离阈值(failure rate>30%+min_sample_size)，仅定性规则无定量触发器 | 已修复 | 直接证据：`docs_zh/contracts/tenant_isolation_and_shared_worker_safety_contract.md` 已新增“自动隔离触发器”，明确滚动窗口内 `failure_rate > 30%` 且 `sample_count >= min_sample_size` 时必须自动隔离，且 `min_sample_size` 默认不得低于 `20`。根因：旧租户隔离合同强调原则边界，却没有把 shared worker 风险提升到可执行的自动触发器。定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |
| T-52 | LOW    | tool_output_sanitization_contract                  | 用"Phase 1a"术语，架构§1.4/§33声明"旧Phase 1-9仅作为历史排期映射"已废弃 | 已修复 | 直接证据：`docs_zh/contracts/tool_output_sanitization_contract.md` 已将 §9 从 `Phase 1a` 改成 `Current / Transition` 边界语义，不再使用旧 phase 编号作为 canonical 能力口径。根因：净化合同沿用了旧排期文案，没有随着主架构把 `Phase 1-9` 降为历史映射而更新表达。定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |
| T-53 | LOW    | cost_attribution_and_optimization_contract         | CostAttributionRecord.decision_ref为通用string，架构要求可追踪到HarnessRun/NodeRun/BudgetSettlement | 已修复 | 直接证据：`docs_zh/contracts/cost_attribution_and_optimization_contract.md` 已把 `CostAttributionRecord` 改为 `harness_run_id / node_run_id / budget_settlement_ref / decision_directive_ref` 这组可追溯引用，并规定无法回链到 `HarnessRun / NodeRun / BudgetSettlement` 的成本不得进入自动优化建议。根因：旧成本归因合同把“决策来源”当作报表标签处理，没有把 runtime truth 与预算结算回链建模成硬约束。定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |
| T-54 | LOW    | approval_and_hitl_contract                         | 仍用OapeflirStage作为一等stage_ref字段，架构§5.5不变量"oapeflir.\*事件不得作为truth source" | 已修复 | `docs_zh/contracts/approval_and_hitl_contract.md` 已把权威关联键改为 `harness_run_id / node_run_id`，并将 `stage_ref` 收敛为仅视图语义的 `stage_view_ref`；根因：历史审批流把 OAPEFLIR 阶段既当解释视图又当权威关联键，混淆了 projection 与 runtime truth；定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |
| T-55 | LOW    | monetization_metering_plane_contract               | UsageEvent.source枚举(runtime/api/gateway/admin)缺tool/model/side_effect，架构§18.1 cost_source含provider_invoice/internal_compute/human_review/storage/egress | 已修复 | 直接证据：`docs_zh/contracts/monetization_metering_plane_contract.md` 的 `UsageEvent` 最小字段现已补齐 `harness_run_id / node_run_id`，`source` 扩展为 `runtime / api / gateway / admin / tool / model / side_effect`，并新增独立 `cost_source` 枚举承接 `provider_invoice / internal_compute / human_review / storage / egress`。根因：该段落沿用了早期 API 入口视角，只建模“从哪里发起”，没有随着工具执行、模型调用和副作用结算引入统一成本归因字段。定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |
| T-56 | LOW    | runtime_repository_and_migration_contract          | Repository方法(markExecutionStarted等)直操作executions表，架构§5.3强制所有状态转换经RuntimeStateMachine.transition() | 已修复 | 直接证据：`docs_zh/contracts/runtime_repository_and_migration_contract.md` 已把 canonical 写入边界改为 `TransitionRepositoryAdapter`，并明确所有 truth 状态变化都必须先经过 `RuntimeStateMachine.transition(command)`；`markExecutionStarted / markExecutionBlocked / markExecutionSucceeded` 等仅允许存在于 legacy compatibility adapter 内部。根因：旧运行时持久化合同停留在 execution-centric repository 模型，没有随着 v4.3 truth 状态机边界迁移到 transition-backed 写入。定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |

## 13. ADR vs 架构（第2批，25项）

| #    | 严重度 | ADR            | 偏差描述 | 状态 | 收口证据 |
| ---- | ------ | -------------- | ---------------------------------------------------------------------------------------------------------------------------- | --- | --- |
| A-13 | HIGH   | ADR-021        | 用废弃ControlDirective为规范P2→P3合约；架构§5.2拆分为OperationalDirective/DecisionDirective | 已修复 | 直接证据：`docs_zh/adr/021-inter-plane-communication-contract.md` 已将 `RequestEnvelope.control_directives` 改为 `directives: Array<OperationalDirective | DecisionDirective>`，并新增两类 directive 的分离定义。根因：早期 ADR 把操作性控制和审批/决策结果混成一类跨平面消息。定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |
| A-14 | HIGH   | ADR-021        | 用废弃ExecutionPlan含线性steps[]为P3→P4合约；架构§5.3强制PlanGraphBundle | 已修复 | 直接证据：`docs_zh/adr/021-inter-plane-communication-contract.md` 已将 `ExecutionPlan` 替换为 `PlanGraphBundle`，并以 `graph / schedulerPolicy / validationReport` 取代线性 `steps[]` handoff。根因：ADR 形成时执行模型仍停留在线性 workflow 语义，没有随 v4.3 graph handoff 升级。定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |
| A-15 | HIGH   | ADR-021        | 用废弃ExecutionReceipt为P4→P3结果；架构§5.3强制NodeAttemptReceipt(attemptId+nodeRunId) | 已修复 | 直接证据：`docs_zh/adr/021-inter-plane-communication-contract.md` 已将 `ExecutionReceipt` 替换为 `NodeAttemptReceipt`，并明确包含 `receiptId / nodeAttemptId / nodeRunId / budgetSettlementRefs`。根因：早期 ADR 尚未把 node attempt append-only 回执模型提炼为独立真相对象。定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |
| A-16 | HIGH   | ADR-027        | Principal类型含pack/tenant代替worker/plugin；架构§11.1规范集为user/service/agent/worker/plugin/system | 已修复 | 直接证据：`docs_zh/adr/027-security-architecture.md` 已将 principal 集改为 `user / service / agent / worker / plugin / system`，移除 `pack / tenant`。根因：旧安全建模把资源归属对象和主动调用主体混成一套 identity taxonomy。定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |
| A-17 | HIGH   | ADR-027        | 沙箱层级SANDBOX_NONE/SANDBOX_READonly/SANDBOX_NETWORK_ISOLATED/SANDBOX_FULL与架构§11.4完全不同；SANDBOX_NONE违反default-deny | 已修复 | 直接证据：`docs_zh/adr/027-security-architecture.md` 已将沙箱层级改为 `read_only / workspace_write / scoped_external_access / restricted_exec`，并明确不存在 `SANDBOX_NONE` 这类 default-allow 模式。根因：旧沙箱 ADR 按抽象强度命名，未随主架构切换到 default-deny 的能力矩阵。定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |
| A-18 | HIGH   | ADR-026        | 风险模型6因子(stepTypeRisk/targetSystemRisk等)名称权重均不同于架构§10.2的8因子 | 已修复 | 直接证据：`docs_zh/adr/026-risk-control-architecture.md` 已将风险模型改为 8 因子：`operationRisk / targetResourceCriticality / dataSensitivity / autonomyModeRisk / tenantImpact / blastRadius / historicalFailureRate / evidenceConfidence`，并同步修正权重与公式。根因：风险 ADR 沿用了早期 step-centric 评分草案，没有随着主架构把自治模式、租户影响面和证据充分性纳入统一风险评估一起升级。定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |
| A-19 | HIGH   | ADR-025        | PolicyMode含supervised/degraded/maintenance/emergency不存在于架构§9.5/§14.8规范集 | 已修复 | 直接证据：`docs_zh/adr/025-stability-architecture-seven-layers.md` 已将 `PolicyMode` 枚举改为 `full_auto / supervised_auto / read_only / no-write / no-external-call / no-rollout / manual_only / incident-mode`。根因：稳定性 ADR 把告警/运营语义和运行时强约束模式合并成一套枚举。定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |
| A-20 | HIGH   | ADR-073        | 用tasks/workflow/execution/ExecutionEnvelope为规范资源；架构§5.5强制HarnessRun/NodeRun/PlanGraphBundle | 已修复 | 直接证据：`docs_zh/adr/073-unified-resource-model.md` 已将 authoritative resource family 改为 `harness_run / plan_graph_bundle / node_run / node_attempt_receipt`，并把 `tasks / workflow_state` 明确降为 projection / interaction 资源。根因：统一资源模型先以历史存储投影对象起草，后续没有随着 run/node/graph/receipt 成为 runtime truth 同步重写。定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |
| A-21 | HIGH   | ADR-004        | 用v3 "VP运营/VP编排/事业部/Lead Agent/CEO"代理层次；架构v4.3以五平面+HarnessRuntime替代 | 已修复 | 直接证据：`docs_zh/adr/004-workflow-routing.md` 已将执行与路由主语改为 `P1/P2/P3` 与 `HarnessRuntime`，并使用 `PlanGraphBundle / NodeRun / NodeAttemptReceipt` 取代旧组织代理层次。根因：工作流与路由 ADR 长期承担组织叙事，没有随着 v4.3 五平面和 `HarnessRuntime` 成为运行时主干而重写。定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |
| A-22 | MED    | ADR-005        | 运行时模式supervised/auto/full-auto不匹配架构§14.8规范8种枚举 | 已修复 | 直接证据：`docs_zh/adr/005-security-model.md` 已将运行模式收敛到 `full_auto / supervised_auto / read_only / no-write / no-external-call / no-rollout / manual_only / incident-mode`，并把 `supervised / auto / full-auto` 降为旧产品话术或 UI 投影。根因：早期安全模型把自动化程度当作唯一控制轴，没有把多种保护模式建成 canonical runtime mode。定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |
| A-23 | MED    | ADR-064        | CostDimension用废弃workflow_id/step_id；架构§12.4/§5.5强制harnessRunId/nodeRunId | 已修复 | 直接证据：`docs_zh/adr/064-cost-attribution-and-optimization-engine.md` 已将 `CostDimension` 从 `workflow_id / step_id` 改为 `harness_run_id / node_run_id / budget_settlement_ref`，并把预算层级改成 `platform / tenant / harness_run / node_run`。根因：成本引擎 ADR 沿用了线性 workflow 粒度，没有随着 v4.3 执行真相对象切换到 `HarnessRun / NodeRun / BudgetSettlement`。定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |
| A-24 | MED    | ADR-052        | 列出sync复制模式RPO=0；架构§25.11明确"v4.2不承诺多主truth写入"仅async | 已修复 | 直接证据：`docs_zh/adr/052-multi-region-deployment-architecture.md` 已删除 `sync / RPO=0` 复制模式，并新增约束声明 v4.2/v4.3 不承诺多主 truth 写入，只允许 async/eventual 复制与单 writer truth 边界。根因：多 region ADR 复用了传统数据库部署话语，没有区分 append-only evidence 复制与 runtime truth 单 writer 边界。定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |
| A-25 | MED    | ADR-058        | 紧急停止级别L0-L4未引用PlatformPanicDirective或OperationalDirective(type=kill)架构§5.2/§60正式机制 | 已修复 | 直接证据：`docs_zh/adr/058-emergency-stop-and-global-circuit-breaker.md` 已将 `L1-L4` 级别绑定到 `PlatformPanicDirective` 或 `OperationalDirective(type=pause_run / kill_run)`，并明确不得用旁路脚本替代正式机制。根因：紧急制动 ADR 先从运维 playbook 起草，后续没有跟控制面正式 directive 合约对齐。定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |
| A-26 | MED    | ADR-098        | 用waiting_hitl为NodeRun状态；架构§14.10/§25.8规范为awaiting_hitl | 已修复 | 直接证据：`docs_zh/adr/098-harness-hitl-runtime.md` 已将状态语义改为 `NodeRun -> awaiting_hitl`。根因：早期命名沿用了旧 harness 草案，没有随着 canonical `NodeRun.status` 枚举统一。定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |
| A-27 | MED    | ADR-066-plugin | DomainPlannerPlugin.plan()返回Promise<Plan>；架构强制PlanGraphBundle | 已修复 | 直接证据：`docs_zh/adr/066-plugin-spi-framework.md` 已将 `DomainPlannerPlugin.plan()` 改为返回 `Promise<PlanGraphBundle>`。根因：Plugin SPI ADR 沿用了早期线性计划接口草案，没有随着 graph handoff contract 升级。定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |
| A-28 | MED    | ADR-040        | Goal生命周期9态不对齐HarnessRun §25.8的13态状态机 | 已修复 | 直接证据：`docs_zh/adr/040-goal-decomposition-engine.md` 已删除平行的 9 态 goal truth 生命周期，改成 `GoalProjection` 到 `HarnessRun.status` 的映射关系。根因：目标分解 ADR 把“分解产物状态”和“运行时 truth 状态”混成一套生命周期，没有随着 `HarnessRun` 成为唯一执行主状态机而收敛。定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |
| A-29 | MED    | ADR-073        | 全文用"phase1-4"；架构§33声明旧Phase命名仅为历史映射，强制Ring 1/2/3 | 已修复 | 直接证据：`docs_zh/adr/073-unified-resource-model.md` 已将 `phase1-4` authoritative/完成边界改成 `Ring 1 / Ring 2 / Ring 3` 分层术语。根因：资源模型 ADR 沿用了旧排期命名，没有随着主架构统一到 ring 口径。定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |
| A-30 | MED    | ADR-094        | 引用"phase 8b"为交付门禁；同上phase命名矛盾 | 已修复 | 直接证据：`docs_zh/adr/094-harness-durable-execution.md` 已将 `phase 8b` 改为 `Ring 2 durable-readiness`。根因：durable execution ADR 沿用了历史阶段排期，没有切换到主架构统一的 ring 口径。定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |
| A-31 | MED    | ADR-099        | 引用"phase 8c"；同上 | 已修复 | 直接证据：`docs_zh/adr/099-harness-async-mode.md` 已将 `phase 8c` 改为 `Ring 2 async-readiness`。根因：async mode ADR 沿用了历史阶段排期，没有切换到主架构统一的 ring 口径。定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |
| A-32 | MED    | ADR-037        | DomainClass仅7种；架构§1/§30覆盖24垂直域，缺17种域分类 | 已修复 | 直接证据：`docs_zh/adr/037-domain-modeling-and-onboarding.md` 已将 `DomainClass` 从 7 种扩展到与主架构 `§71-§94` 对齐的 24 个垂直域类型。根因：领域建模 ADR 形成时仍偏向产品市场分组，没有随着主架构把 24 个垂直域作为统一元模型实例化入口而扩展。定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |
| A-33 | LOW    | ADR-092        | 用"step"/"decision"记录时间线；架构规范为NodeRun/NodeAttempt，HarnessStep仅语义投影 | 已修复 | 直接证据：`docs_zh/adr/092-harness-loop-controller.md` 已将每轮时间线主语改为 `NodeRun / NodeAttempt`，不再把 `step` 当作执行真相对象。根因：loop controller ADR 继承了语义 step 叙事，没有随着 `NodeRun / NodeAttempt` 成为执行真相对象而改写。定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |
| A-34 | LOW    | ADR-042        | 自治level 4 full_auto暗示无限制；架构§3.2禁止高危域full_auto除非显式DomainRiskSpec | 已修复 | 直接证据：`docs_zh/adr/042-progressive-autonomy-model.md` 已明确 `full_auto` 不代表无限制自动化，高危域默认不得进入 `full_auto`，除非有显式 `DomainRiskSpec / DomainRiskProfile` 允许。根因：渐进式自主权 ADR 把自治等级误写成无限授权梯子，没有跟高危域风险覆盖规则绑定。定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |
| A-35 | LOW    | ADR-075        | Rollout L1命名shadow与已废弃ADR-018 L2 shadow冲突，级别编号不一致 | 已修复 | 直接证据：`docs_zh/adr/075-controlled-rollout-release.md` 已将 `L1` 级别从 `shadow` 改为 `evaluate_0`，并将状态从 `shadow_enabled` 改为 `evaluation_enabled`；同时 `docs_zh/contracts/release_rollout_and_rollback_contract.md` 已同步收敛到相同命名。根因：release level 与 rollout status 两个维度长期被混成一套命名体系，继续继承了 ADR-018 的历史 shadow 话语，导致级别编号和状态含义同时冲突。定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |
| A-36 | LOW    | ADR-066-plugin | Plugin隔离描述为Worker线程；架构§11.7要求不可信插件用独立进程+IPC边界 | 已修复 | 直接证据：`docs_zh/adr/066-plugin-spi-framework.md` 已明确不可信 Plugin 必须运行在独立进程，并通过 `plugin-runtime-host.ts` 的 IPC 边界管理；Worker 线程不再被当作最终隔离边界。根因：实现早期先落了同进程并发原型，文档却没有再升级到主架构要求的独立进程 + IPC 边界。定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |
| A-37 | LOW    | ADR-093        | ConstraintPack仅含risk_policy+output_policy；架构§13.4/§14.2要求含budget envelope/sandbox requirement/approval requirement | 已修复 | 直接证据：`docs_zh/adr/093-harness-constraint-engine.md` 已将 `ConstraintPack` 最小集合扩展为 `risk_policy / output_policy / budget_envelope / sandbox_requirement / approval_requirement`。根因：约束引擎 ADR 起草时只覆盖风险与输出治理，没有把预算、沙箱和审批要求一并纳入统一约束包。定向验证：`tests/invariants/contract-and-oapeflir-remediation.test.ts` |

## 原始最终系统性主题（历史快照，当前未修复）

以下主题为原始最终审计的风险归纳，内容保留用于历史追踪；当前所有对应编号均已重开，统一标记为 `未修复`，直接证据路径待逐条补齐。

1. **OAPEFLIR 身份危机（最严重）**：v4.4 Spec将其视为完整Runtime拥有Run/Status/Budget/Events/ErrorCodes/GraphScheduler/SideEffectManager；主架构明确降级为认知投影层。ADR/合约/代码各取一边，整个系统对"谁驱动执行"无共识
2. **v3→v4术语迁移未执行**：~60%的合约和~40%的ADR仍使用 task/workflow/execution/step/Rollout/ControlDirective/ExecutionPlan/ExecutionReceipt 等v3术语，架构已全部重命名
3. **状态机碎片化**：HarnessRun在不同位置定义为5/6/7/13/15态；NodeRun有5/9/13态变体；无单一真相源
4. **安全关键default-allow**：delegated-governance未知类型允许、sandbox tier含NONE、自治升级无风险门控、panic恢复缺双人验证
5. **完整模块/字段群缺失**：Admin SDK、CapacityPlanning、CostAttribution、LegalEntityBoundary、RevenueShare实现、DomainRiskSpec 7个子规格、15问元模型等
6. **防御深度系统性不足**：prompt injection/sandbox/drift/compliance/edge-sync均仅实现单层，架构要求3-5层
7. **配置-schema-架构三方脱节**：域配置JSON字段、Zod schema、架构定义三者互不匹配
8. **合约自身内部不一致**：storage_schema memories表DDL遗漏自身§13声明的列；harness-run-contract内部§45.13 vs §25.8态数不同
9. **规范对象标识不统一**：context/trace/billing/cost各自使用不同key(task_id/execution_id/workflow_id vs harnessRunId/nodeRunId/attemptId)
10. **交付里程碑术语分裂**：部分文档用Phase 1-9，部分用Ring 1/2/3，部分用A/B/C/D，三套命名并存
