# Migrations (Legacy Alias)

`docs_zh/migrations/` no longer carries an independent source of truth.

- Canonical migration guidance lives in [../migration/README.md](../migration/README.md).
- The legacy `e2e-workflow-state-migration.md` page in this directory now redirects to the canonical singular-path document.
