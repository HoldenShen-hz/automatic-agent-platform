apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "automatic-agent.fullname" . }}-config
  labels:
    {{- include "automatic-agent.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
