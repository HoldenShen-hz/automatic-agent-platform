# Platforme Full Review A Issue Table

> The original issue list has been converted into a table, preserving the original wording for easy filtering, copying, and follow-up tracking.

| ID | Location | Issue | Issue Status | Root Cause |
| --- | --- | --- | --- | --- |
| P-0001 | src/platform/contracts/types/domain/task-types.ts:45 | `src/platform/contracts/types/domain/task-types.ts:45` `export type { ArtifactRef } from "../../executable-contracts/index.js"` : reverse cross-level re-export within the contracts subtree.   | Fixed | `contracts/types/domain` directly reverse re-exports executable contract types for compatibility, breaking the contracts layering boundary. |
| P-0002 | src/platform/contracts/types/domain/task-types.ts:45<br>package.json:14-46 | src/platform/contracts/types/domain/task-types.ts:45 `export type { ArtifactRef } from "../../executable-contracts/index.js"` (P-0001) reverse re-export; package.json:14-46 `exports` table `"./platform"` single entry exposes both `contracts/types/domain` and `executable-contracts` copies of ArtifactRef → consumer typeof comparisons see different symbols   | Fixed | The same `ArtifactRef` leaks through two publishing paths, consumers see duplicate symbol boundaries. |
| P-0003 | src/platform/contracts/types/domain/task-types.ts:45<br>package.json | src/platform/contracts/types/domain/task-types.ts:45 `export type { ArtifactRef } from "../../executable-contracts/index.js"` does a reverse cross-level re-export within the contracts subtree (P-0001), and conflicts with the package.json `exports` `"./platform"` single entry — the type is published twice under two paths   | Fixed | The type publishing surface mixes canonical definition with the compatibility exit, exposing dual-path semantics under a single entry. |
| P-0004 | src/platform/five-plane-interface/api/http-server/harness-runs-routes.ts:199 | Default value degradation: the API harness route falls back to `\|\| "default"` for domainId src/platform/five-plane-interface/api/http-server/harness-runs-routes.ts:199   | Fixed | When creating a harness run, a missing domainId falls back to "default", masking the caller's missing parameter. |
| P-0005 | src/platform/five-plane-interface/api/oidc-oauth/jwt-utils.ts:29 | Error swallowing: `} catch {` silently ignores OIDC/JWT validation exceptions src/platform/five-plane-interface/api/oidc-oauth/jwt-utils.ts:29   | Re-reviewed & Closed | The risk in the old implementation came from the `catch {}` form; current decode failures are converted to AuthError and fail-close. |
| P-0006 | src/platform/five-plane-interface/api/api-auth-service.ts:119 | JWT only allows HS256 (does not support RS/ES asymmetric); HS256 shared key is both signer and verifier src/platform/five-plane-interface/api/api-auth-service.ts:119   | Re-reviewed & Closed | The root cause is that the A table mistakenly recorded "asymmetric JWT from external IdP not supported" as a current defect; the current API auth is a self-signed self-verified service token, with `allowedAlgorithms/iss/aud/jti/revocation` already added — RS/ES is a protocol extension, not a current bypass surface. |
| P-0007 | src/platform/five-plane-interface/api/oidc-oauth-service.ts:465 | OIDC JWKS: when `kid` is missing, fallback to `keys[0]` for implicit key selection — risk of key confusion src/platform/five-plane-interface/api/oidc-oauth-service.ts:465   | Fixed | When a JWT is missing `kid`, the first JWKS key is selected by default, leaving implicit key-selection space for multi-key issuers. |
| P-0008 | src/platform/five-plane-interface/api/api-auth-service.ts:120 | Weak JWT secret check only has a minimum 8-character length, easily passing validation yet vulnerable to brute force src/platform/five-plane-interface/api/api-auth-service.ts:120   | Fixed | JWT secret strength baseline is too low — only the shortest length is checked, allowing obviously weak keys to pass. |
| P-0009 | src/platform/five-plane-interface/api/graphql-adapter-service.ts:262 | `Math.random()` used for GraphQL subscription operationId: src/platform/five-plane-interface/api/graphql-adapter-service.ts:262   | Fixed | The subscription operationId is generated by Math.random(), predictable and with weak collision control. |
| P-0010 | src/platform/five-plane-interface/webhook/index.ts:225 | Webhook HMAC validation does not read/check the timestamp or nonce; the signature can be replayed src/platform/five-plane-interface/webhook/index.ts:225   | Fixed | Webhook replay protection only validates the signature itself, without binding replay detection to request fingerprinting and idempotency semantics. |
| P-0011 | src/platform/five-plane-interface/webhook/index.ts:225 | Webhook allows `signingSecret ?? ""` as the HMAC key, and an empty secret is not blocked src/platform/five-plane-interface/webhook/index.ts:225   | Fixed | HMAC verification falls back to an empty string when the secret is missing, and configuration anomalies are not handled with fail-closed. |
| P-0012 | src/platform/five-plane-interface/api/api-auth-service.ts:121 | Weak secret blacklist only enumerates 5 values (secret/password/changeme/default/jwtsecret); other weak passwords pass src/platform/five-plane-interface/api/api-auth-service.ts:121   | Fixed | Weak secret identification relies on a small enumerated blacklist, missing common pattern-based passwords. |
| P-0013 | src/platform/five-plane-interface/api/graphql-adapter-service.ts:262 | src/platform/five-plane-interface/api/graphql-adapter-service.ts:262 `operationId = \`sub_${Date.now()}_${Math.random()...\`` same problem: subscription operationId is not cryptographically random   | Fixed | Subscription operationId again reuses Math.random(), making tracking identifiers unstable. |
| P-0014 | src/platform/five-plane-interface/api/http-api-server.ts:189 | src/platform/five-plane-interface/api/http-api-server.ts:189 `void this.handleRequest(req, res)` top-level async that does not catch exceptions, relies on the framework's default 500 — loses trace_id context binding   | Fixed | The HTTP entry point directly fire-and-forgets async processing, lacking a top-level catch wrapper and a unified error landing point. |
| P-0015 | src/platform/five-plane-interface/api/api-auth-service.ts:131 | JWT validation does only lower-case comparison: `WEAK_JWT_SECRETS.has(secret.trim().toLowerCase())`; case variants like `Secret` are not in the blacklist src/platform/five-plane-interface/api/api-auth-service.ts:131   | Re-reviewed & Closed | The current implementation does `trim().toLowerCase()` before blacklist comparison, so case variants are already covered and the original record does not hold. |
| P-0016 | src/platform/five-plane-interface/api/middleware/idempotency-key-storage.ts:211,222,231,244,260,277,287,294,299 | SQL string concatenation: dynamic `${this.tableName}` is concatenated into multiple DDL/DML statements src/platform/five-plane-interface/api/middleware/idempotency-key-storage.ts:211,222,231,244,260,277,287,294,299   | Re-reviewed & Closed | The original issue came from dynamic table-name concatenation; the current constructor is first constrained by `validateSqlIdentifier()` allowlist. |
| P-0017 | src/platform/five-plane-interface/api/oidc-oauth-service.ts:256 | OIDC validation flow allows `iss/aud/nbf` to be returned via soft error codes (e.g. `jwt.untrusted_issuer` / `not_yet_valid`) but the implementation is on the same `validateToken` path and does not independently enforce nbf skew tolerance src/platform/five-plane-interface/api/oidc-oauth-service.ts:256   | Re-reviewed & Closed | The old audit was based on a historical validation path; the current implementation explicitly validates exp/nbf/iat clock skew and fail-closes. |
| P-0018 | src/platform/five-plane-interface/api/graphql-adapter-service.ts:413,419,435,441,519 | Free-form text `throw new Error("Mutation type is not configured")` and similar in multiple places src/platform/five-plane-interface/api/graphql-adapter-service.ts:413,419,435,441,519   | Fixed | The GraphQL adapter layer directly throws raw Error, lacking unified error codes and type classification. |
| P-0019 | src/platform/five-plane-interface/api/http-api-server.ts:189 | src/platform/five-plane-interface/api/http-api-server.ts:189 `void this.handleRequest(request, response)` top-level fire-and-forget (P-0014) + no timeout wrapper (http.Server.timeout not explicitly set at the call site), slow-loris attack surface   | Fixed | The HTTP entry does not bind timeout and async exception handling explicitly at the server level, leaving slow-request protection incomplete. |
| P-0020 | src/platform/five-plane-interface/api/facade-interfaces.ts:214,217,220,223 | Free-form text `throw new Error("Incident service not configured")` repeated four places src/platform/five-plane-interface/api/facade-interfaces.ts:214,217,220,223   | Fixed | The unconfigured branch of the facade reuses raw Error, lacking a unified error type and error code. |
| P-0021 | src/platform/five-plane-interface/api/http-api-server.ts | src/platform/five-plane-interface/api/http-api-server.ts full-text grep finds no `Strict-Transport-Security/X-Content-Type-Options/Content-Security-Policy/X-Frame-Options/Referrer-Policy` security headers (without depending on a reverse proxy); the bare HTTP service has no security-header baseline   | Re-reviewed & Closed | The historical version lacked security headers; the current `decorateResponseHeaders()` uniformly injects HSTS, CSP, nosniff, and other baselines. |
| P-0022 | src/platform/five-plane-control-plane/config-center/profile-home.ts:30 | Default value degradation: `profileId?.trim() \|\| "default"` falls into a hardcoded "default" namespace src/platform/five-plane-control-plane/config-center/profile-home.ts:30   | Fixed | When profileId is not explicitly passed, it always falls into "default", and different workspaces share the same-named namespace. |
| P-0023 | src/platform/five-plane-control-plane/iam/sandbox-policy.ts:361 | Error swallowing: `} catch {` silently ignores src/platform/five-plane-control-plane/iam/sandbox-policy.ts:361   | Re-reviewed & Closed | This catch only falls back to the original string when URL decoding fails, and is not a policy-bypass path. |
| P-0024 | src/platform/five-plane-control-plane/iam/outbound-url-policy.ts:136,160 | Error swallowing: `} catch {` silently ignores src/platform/five-plane-control-plane/iam/outbound-url-policy.ts:136,160   | Re-reviewed & Closed | Both catches have been converted to ValidationError or redacted fallback, no longer silently letting policy failures through. |
| P-0025 | src/platform/five-plane-control-plane/config-center/config-impact-analyzer.ts:206 | `Math.random()` used for config impact analysis ID: src/platform/five-plane-control-plane/config-center/config-impact-analyzer.ts:206   | Fixed | The config impact analysis ID is concatenated using Math.random(), with insufficient uniqueness and unpredictability. |
| P-0026 | src/platform/five-plane-control-plane/iam/file-freshness.ts:103 | Weak algorithm optional: `takeFileSnapshot` digestAlgorithm accepts md5 src/platform/five-plane-control-plane/iam/file-freshness.ts:103   | Fixed | The file-freshness guard exposes an md5 option, bringing a weak digest algorithm into the concurrent write-protection chain. |
| P-0027 | src/platform/five-plane-control-plane/iam/secret-management-service.ts:103 | Error swallowing: `} catch {` silently ignores src/platform/five-plane-control-plane/iam/secret-management-service.ts:103   | Re-reviewed & Closed | The old audit treated the catch as swallowing; current rotation-policy parse failures throw ValidationError. |
| P-0028 | src/platform/five-plane-control-plane/cost-alert/cost-alert-service.ts:540 | src/platform/five-plane-control-plane/cost-alert/cost-alert-service.ts:540 emits `cost.threshold.exceeded`, inconsistent with the registry `cost:limit_reached` naming (dot vs colon)   | Fixed | The cost alert implementation followed the old dot-separated event name, and was not aligned with the event registry's colon style. |
| P-0029 | src/platform/five-plane-control-plane/iam/mfa-service.ts:309 | PII written to storage: MFA session identifier comment acknowledges no mask `store masked email/phone` is actually stored in plaintext src/platform/five-plane-control-plane/iam/mfa-service.ts:309   | Fixed | MFA session persistence stores the original identifier directly, and does not enforce the redaction policy declared in the comment. |
| P-0030 | src/platform/five-plane-control-plane/iam/file-freshness.ts:262 | Weak algorithm optional: `FileFreshnessTracker.snapshot` digestAlgorithm accepts md5 src/platform/five-plane-control-plane/iam/file-freshness.ts:262   | Fixed | FileFreshnessTracker also exposes md5 as an optional digest algorithm to the write-protection path. |
| P-0031 | src/platform/five-plane-control-plane/incident-control/acceptance-readiness-service.ts:119 | `JSON.parse` without try/catch: directly parses and returns the storage path src/platform/five-plane-control-plane/incident-control/acceptance-readiness-service.ts:119   | Fixed | The acceptance report reads JSON, and on parse failure silently turns it into null, disguising a corrupt state as "no data". |
| P-0032 | src/platform/five-plane-control-plane/iam/sandbox-policy.ts:361 (P-0023) and | src/platform/five-plane-control-plane/iam/sandbox-policy.ts:361 (P-0023) and outbound-url-policy.ts:136,160 (P-0024) are on the critical path of network outbound policy; swallowing errors is equivalent to policy failure   | Re-reviewed & Closed | The old issue was based on the assumption that "outbound policy critical path swallows errors"; the current implementation does not silently let through policy decisions. |
| P-0033 | src/platform/five-plane-control-plane/incident-control/auto-stop-loss-service.ts:789 | fire-and-forget Promise: `void this.executePlaybook(...)` src/platform/five-plane-control-plane/incident-control/auto-stop-loss-service.ts:789   | Re-reviewed & Closed | The call site already has rejected-promise handling and failure recording; the audit mistakenly classified the controlled async branch as unhandled fire-and-forget. |
| P-0034 | src/platform/five-plane-control-plane/iam/data-classification-service.ts:293,841-846 | `regexCache = new Map<string, RegExp>()` has no size upper bound; if classification rules are dynamically generated, this can cause memory bloat src/platform/five-plane-control-plane/iam/data-classification-service.ts:293,841-846   | Fixed | The classification-rule regex cache only grows, with no capacity upper bound. |
| P-0035 | src/platform/five-plane-control-plane/approval-center/quorum-calculator.ts:289,295 | Free-form text `throw new Error("Vote must have a valid approverId/votedAt timestamp")` src/platform/five-plane-control-plane/approval-center/quorum-calculator.ts:289,295   | Fixed | Vote-validation failures throw raw Error directly, lacking a ValidationError classification. |
| P-0036 | src/platform/five-plane-control-plane/incident-control/release-pipeline-support.ts:215 | Command construction: release-pipeline uses `spawn(request.command, request.args, ...)` driven by the request src/platform/five-plane-control-plane/incident-control/release-pipeline-support.ts:215   | Fixed | The release pipeline passes the request's command and arguments directly to `spawn`, lacking a step-level allowlist. |
| P-0037 | src/platform/five-plane-control-plane/incident-control/runbook-executor/runbook-executor.ts:263 | setTimeout not cleaned up: `setTimeout(() => {...}, 10)` async sleep is not cleared on abort src/platform/five-plane-control-plane/incident-control/runbook-executor/runbook-executor.ts:263   | Fixed | The runbook executor uses raw `setTimeout` to simulate sleep, without saving the handle and releasing event-loop references at idle. |
| P-0038 | src/platform/five-plane-control-plane/config-center/config-impact-analyzer.ts:206 | src/platform/five-plane-control-plane/config-center/config-impact-analyzer.ts:206 `analysisId = \`impact-${Date.now()}-${Math.random()...slice(2,8)\`` 6 chars → ~31 bits, weakest uniqueness guarantee   | Fixed | The impact analysis ID is still generated by timestamp plus a short random string, with large collision and prediction surface. |
| P-0039 | src/platform/five-plane-control-plane/incident-control/deployment-execution-service.ts:165 | Command construction: deployment-execution-service uses `spawn(request.command, request.args, ...)` driven by the request src/platform/five-plane-control-plane/incident-control/deployment-execution-service.ts:165   | Fixed | Deployment execution also directly executes commands from the request, lacking a whitelist closure from deployment steps to executable commands. |
| P-0040 | src/platform/five-plane-control-plane/config-center/startup-env-schema.ts:530 | src/platform/five-plane-control-plane/config-center/startup-env-schema.ts:530 `process.exit(1)` exits directly (consistent with the comment on line 5 "validation runs at process startup"), but config-center is also imported by SDK/CLI; CLI short commands risk accidentally triggering exit(1) (hard to distinguish from a ts error signal)   | Fixed | The startup-environment validation couples "library function validation failure" with "CLI entry exit" in the same function. |
| P-0041 | src/platform/five-plane-control-plane/mission/index.ts | src/platform/five-plane-control-plane/mission/index.ts ~42 KB — index.ts anti-pattern (a barrel should not carry implementation), violating the "index.ts only re-exports" convention   | Re-reviewed & Closed | The root cause is that the review treated the mission entry as a "pure barrel" requirement; the current `mission/index.ts` is itself the canonical facade/service surface for this domain, and there is no repo-wide "index.ts can only re-export" contract. |
| P-0042 | src/platform/five-plane-control-plane/iam/policy-engine.ts:83-92,116 | `decisionCache` only does a full `clear` on `invalidate`, with no independent TTL scan; TTL 5s is checked only on hit, the cache key space grows in a decisionId×action×fingerprint Cartesian product, and unhit entries become zombie entries until full clear src/platform/five-plane-control-plane/iam/policy-engine.ts:83-92,116   | Fixed | decisionCache only checks TTL on hit, and does not actively clean up expired zombie keys. |
| P-0043 | src/platform/five-plane-control-plane/iam/secret-management-service.ts | src/platform/five-plane-control-plane/iam/secret-management-service.ts ~37 KB single file holding 12+ classes (cache/rotation/audit/lease/...), violating SRP; the rotation timer (P-0389) and silent error swallowing (P-0027) are concentrated here → a risk coupling center   | Re-reviewed & Closed | The root cause is that the old snapshot directly equated "large file" with an independent defect; the current secret provider, rate limiting, supported types, and audit coordination have been split into support/provider modules, and the remainder is mostly maintainability debt, no longer listed as a runtime defect. |
| P-0044 | src/platform/five-plane-control-plane/iam/jwt-utils.ts:29 (OIDC/JWT | src/platform/five-plane-control-plane/iam/jwt-utils.ts:29 (OIDC/JWT validation exception swallowing, P-0005) is on the identity core path; the security audit needs to rewrite it as "must fail-closed"   | Re-reviewed & Closed | The old audit targeted JWT validation exception swallowing; current decode failures fail-close. |
| P-0045 | src/platform/five-plane-control-plane/config-center/config-audit-service.ts:226-251 | `SELECT * FROM config_audit_entries ORDER BY timestamp DESC` with no LIMIT/pagination; the entire table is loaded into memory and then `.map(JSON.parse(...))`; the audit table grows without bound over time src/platform/five-plane-control-plane/config-center/config-audit-service.ts:226-251   | Fixed | The config audit query reads the full table by default, lacking a return cap. |
| P-0046 | src/platform/five-plane-control-plane/cost-alert/cost-alert-service.ts:540<br>src/scale-ecosystem/federation/federation-gateway.ts:581 | src/platform/five-plane-control-plane/cost-alert/cost-alert-service.ts:540 emits `cost.threshold.exceeded` (dot), src/scale-ecosystem/federation/federation-gateway.ts:581 emits `federation-event` (hyphen), three event-naming styles coexist with the registry `cost:limit_reached` (colon) (P-0028/P-0332), lacking unified lint   | Fixed | The root cause is that cost/federation historical implementations each followed local event-naming conventions without a continuous registry-correction gate; the related emitters have been unified, and a new runtime-event audit script continuously guards the door. |
| P-0047 | src/platform/five-plane-control-plane/incident-control/release-pipeline-support.ts:215-238 | After `spawn` only `error/close` are hooked; stdout/stderr are pushed only to unbounded `stdoutChunks`/`stderrChunks` arrays; oversized output can OOM; missing output truncation/backpressure src/platform/five-plane-control-plane/incident-control/release-pipeline-support.ts:215-238 and deployment-execution-service.ts:165-194   | Fixed | The command executor accumulates stdout/stderr into unbounded arrays, with no output size upper bound. |
| P-0048 | src/platform/five-plane-control-plane/iam/secret-management-service.ts:719 | src/platform/five-plane-control-plane/iam/secret-management-service.ts:719 `logger.info(phase === "initial" ? "secret.rotation.scheduled_initial" : "secret.rotation.scheduled", {...})` and other structured log event names are not in contracts/event_registry (corroborated by P-0480, extended to the secret domain)   | Fixed | The secret rotation scheduler added event names, but the registry documentation was not updated in sync, creating contract drift. |
| P-0049 | src/platform/five-plane-control-plane/iam/vault-http-secret-provider.ts:179-180 | src/platform/five-plane-control-plane/iam/vault-http-secret-provider.ts:179-180 `if (loginData.auth?.client_token) { this._cachedToken = loginData.auth.client_token; }` caches client_token in an instance field (no TTL/re-validation), easily stale during rotation   | Fixed | The Vault AppRole post-login token cache has no minimum TTL protection, and stale credentials can linger. |
| P-0050 | src/platform/five-plane-control-plane/compliance/<br>src/platform/compliance/ | `src/platform/five-plane-control-plane/compliance/` (crypto-shredding/erasure, etc.) and the top-level `src/platform/compliance/` (`compliance-bootstrap.ts`, `lineage/`, `encryption/`) duplicate the same theme.   | Re-reviewed & Closed | The root cause is that the review mistakenly judged "same theme" as "duplicate implementation"; the top-level `platform/compliance` carries cross-plane bootstrap/capability aggregation, while `five-plane-control-plane/compliance` is the specific governance service of the control plane, with different layered responsibilities. |
| P-0051 | src/platform/five-plane-control-plane/config-center/config-versioning-service.ts:797-799 | `cloneConfigContent` uses `JSON.parse(JSON.stringify(content))` to deep-copy the full config content; the hot path triggers O(N) serialization + deserialization on every version snapshot write, and loses Date/Map/undefined semantics; other places in the project already use `structuredClone`, this one clearly lags src/platform/five-plane-control-plane/config-center/config-versioning-service.ts:797-799   | Fixed | The config version snapshot still uses JSON-serialization deep copy, with high hot-path cost and data-semantics loss. |
| P-0052 | src/platform/five-plane-control-plane/iam/secret-management-service.ts:53-82 | `SecretResolutionRateLimiter.requests: Map<string, number[]>` has no LRU/expiry cleanup; each callerId lives in the Map forever, and `.filter` inside `check` rebuilds the array and `set` it back on each call in O(window timestamps) src/platform/five-plane-control-plane/iam/secret-management-service.ts:53-82   | Fixed | The secret-resolution rate limiter only appends callerId timestamps, and does not reclaim long-unused caller state. |
| P-0053 | src/platform/five-plane-control-plane/iam/secret-management-service.ts:67-81 | read-modify-write without lock: first `requests.get(callerId)`, after filter `requests.set(...)`; while pure JS single-thread will not tear, callers interleaved across `await` boundaries are prone to TOCTOU (observed a gap between callerId count and new push) src/platform/five-plane-control-plane/iam/secret-management-service.ts:67-81   | Re-reviewed & Closed | `check()` runs entirely synchronously with no `await` boundary; the original TOCTOU description does not match the current implementation, but state encapsulation and reclamation have been added as a bonus. |
| P-0054 | src/platform/five-plane-control-plane/iam/secret-management-service.ts:749 | `setInterval(() => runRotationSweep("interval", rotationInterval), ...)` async sweep inside has no in-flight flag; a long sweep may start multiple rotation flows in parallel with the next tick, triggering secret rotate reentry src/platform/five-plane-control-plane/iam/secret-management-service.ts:749   | Fixed | The rotation scheduler is triggered on a fixed cadence but has no reentry protection, and a long sweep can overlap with the next tick. |
| P-0055 | src/platform/five-plane-orchestration/planner/plan-graph-normalizer.ts:46 | `@ts-expect-error` appears in the production path src/platform/five-plane-orchestration/planner/plan-graph-normalizer.ts:46   | Fixed | The production normalization path depends on `@ts-expect-error` to mask type issues. |
| P-0056 | src/platform/five-plane-orchestration/routing/intake-router-model.ts:657-658 | `matchRule` calls `new RegExp(...)` on every invocation; the intake router is a per-request hot path src/platform/five-plane-orchestration/routing/intake-router-model.ts:657-658   | Fixed | Intake route matching recompiles regex on the hot path, with no caching. |
| P-0057 | src/platform/five-plane-orchestration/agent-delegation/delegation-manager.service.ts<br>AGENTS.md | src/platform/five-plane-orchestration/agent-delegation/delegation-manager.service.ts ~41 KB; the `*.service.ts` naming with a dot is a rare mixed style (kebab + infix), inconsistent with the repo's "kebab-case .ts" convention (AGENTS.md)   | Re-reviewed & Closed | The root cause is that the review was too strict on the file-suffix convention; the repo currently co-contains similar namings such as `plugin-executor.service.ts`, and `*.service.ts` is already an acceptable compatibility pattern, with AGENTS only constraining base filenames to kebab-case. |
| P-0058 | src/platform/five-plane-orchestration/harness/guardrails/guardrail-vibration-breaker.ts:28 | secret comparison uses `===`: `signal.signature === "proceed"` decides pass-through src/platform/five-plane-orchestration/harness/guardrails/guardrail-vibration-breaker.ts:28   | Re-reviewed & Closed | The comparison is on a control-flow constant `proceed`, not a secret-value comparison, so constant-time comparison risk does not apply. |
| P-0059 | src/platform/five-plane-orchestration/hitl/hitl-operator-console-service.ts:65-68 | `for (const channel of channels) { await this.notifier({...}) }` serial notification across multiple channels, no Promise.all src/platform/five-plane-orchestration/hitl/hitl-operator-console-service.ts:65-68   | Fixed | Multi-channel notification is serial `await`, not parallelized. |
| P-0060 | src/platform/five-plane-orchestration/improve-rollout/rollout/rollout-scheduler.ts:126-128 | `for (const input of inputs) { decisions.push(await this.advance(input)); }` serial `await`, missing Promise.all src/platform/five-plane-orchestration/improve-rollout/rollout/rollout-scheduler.ts:126-128   | Fixed | Batch rollout advance waits serially, not executed in parallel. |
| P-0061 | src/platform/five-plane-orchestration/harness/context-assembler.ts:135-140 | `applyRedaction` does `new RegExp(...)` inside `for of redactPatterns` on every call; redaction fires on each context entry, resulting in O(entries × patterns) RegExp compilations src/platform/five-plane-orchestration/harness/context-assembler.ts:135-140   | Fixed | Context redaction recompiles regex per record, missing caching. |
| P-0062 | src/platform/five-plane-orchestration/oapeflir/handoff-model.ts:86 | `JSON.parse(JSON.stringify(handoff))` deep-copies AgentHandoff; the handoff payload usually contains a large context; O(N) serialization runs on every switch, and is inconsistent with `structuredClone` already used in `handoff-serializer.ts:16` src/platform/five-plane-orchestration/oapeflir/handoff-model.ts:86   | Fixed | Handoff deep copy still uses JSON serialization, not aligned with structuredClone. |
| P-0063 | src/platform/five-plane-orchestration/agent-delegation/delegation-manager.service.ts:826-831 | `findRepositoryRecordsByStatuses` issues 6 independent `findByStatus` queries for 6 active statuses — a typical N-query pattern; should be a single `WHERE status IN (...)`; the call sites at lines 429/607/662/809 all trigger on the hot path src/platform/five-plane-orchestration/agent-delegation/delegation-manager.service.ts:826-831   | Re-reviewed & Closed | The root cause is that the delegation repository currently exposes a status-bucketed query contract; the A table elevated the small-cardinality active-status aggregation directly into a general N+1 defect; this is a repository-capability boundary, not a correctness gap. |
| P-0064 | src/platform/five-plane-orchestration/agent-delegation/delegation-manager.service.ts:429-435,607,662,809 | After fetching each status array, `for record of records` + `record.delegationChain.includes(agentId)`, which is client-side in-memory filtering; should be changed to a SQL/JSON query with a `delegation_chain @> [agentId]` index src/platform/five-plane-orchestration/agent-delegation/delegation-manager.service.ts:429-435,607,662,809   | Re-reviewed & Closed | The root cause is that the review mistakenly recorded in-memory filtering on a bounded active set as an index defect; the current repository has no JSON-containment query contract, and the call side is simply doing a second filter on the general record set. |
| P-0065 | src/platform/five-plane-orchestration/oapeflir/oapeflir-loop-core.ts<br>src/platform/five-plane-execution/oapeflir/ (only | src/platform/five-plane-orchestration/oapeflir/oapeflir-loop-core.ts single file 1004 lines (P-0129), and src/platform/five-plane-execution/oapeflir/ (only 2-file shell, P-0103) forms a huge gap where "implementation is concentrated in orchestration, declaration in execution"   | Re-reviewed & Closed | The directory-list-style review mixed the execution/oapeflir runtime bridge with the orchestration canonical closed-loop implementation; the execution plane README has clarified that this directory only keeps the runtime bridge boundary. |
| P-0066 | src/platform/five-plane-orchestration/escalation/index.ts:15 | `src/platform/five-plane-orchestration/escalation/index.ts:15` imports `../../../ops-maturity/emergency/resume-protocol/index.js`; `five-plane-orchestration/learn/{learning-feedback-orchestration-service,llm-improvement-generation-service,strategy-learning-service,failure-pattern-miner}.ts:1-3` and `evaluator/evaluator-service.ts:16` all import `../../../scale-ecosystem/feedback-loop/collector/feedback-model.js`: plane reverse-dependency on ops-maturity and scale-ecosystem.   | Re-reviewed & Closed | The root cause is mistaking the orchestration horizontal learning/emergency dependency for an accidental reverse import; these modules consume the feedback-loop and recovery-protocol canonical horizontal capabilities, not building a second plane inside orchestration. |
| P-0067 | - | `Math.random()` used for operation/event IDs (not cryptographic, but predictable as tracking identifiers): execution-worker-handshake-service-async.ts:776   | Re-reviewed & Closed | The Math.random location referenced by the audit is no longer in the current implementation, making this a stale conclusion. |
| P-0068 | src/platform/five-plane-execution/execution-engine/multi-step-utils.ts:57 | Error swallowing: `} catch {` silently ignores input-parse exceptions src/platform/five-plane-execution/execution-engine/multi-step-utils.ts:57   | Re-reviewed & Closed | The catch branch is used as a permissive parse fallback, not silently swallowing business failures. |
| P-0069 | src/platform/five-plane-execution/compensation-manager.ts:224-256: | (N-0301) src/platform/five-plane-execution/compensation-manager.ts:224-256: `executeCompensationSteps` has no step-by-step persistence; a mid-run crash followed by retry causes non-idempotent side effects   | Re-reviewed & Closed | The root cause is that the A table continues to treat residual risk already downgraded to an idempotency enhancement in the main audit as a current correctness defect; the current compensation main chain already has human review and compensation-governance closure. |
| P-0070 | src/platform/five-plane-execution/queue-metrics/index.ts:84,93 | Retry has no upper bound/backoff: `failureReasons.push` / queue push has no explicit backoff control (only depends on the outer layer) src/platform/five-plane-execution/queue-metrics/index.ts:84,93   | Re-reviewed & Closed | The root cause is that the outer scheduling backoff policy was mistakenly required to be re-implemented inside the metrics helper; the queue metrics component is not the authoritative boundary of retry policy. |

| P-0071 | src/platform/five-plane-execution/tool-executor/web-fetch.ts:301 | src/platform/five-plane-execution/tool-executor/web-fetch.ts:301 `while (true)` stream fetch has no explicit break/max-byte guard, relying on external abort (prone to spinning against a malicious server)   | Fixed | `web-fetch` only relies on timeout and byte cap, with no independent chunk cap for high-frequency small-fragment responses. |
| P-0072 | src/platform/five-plane-execution/distributed-lock/sqlite-lock-adapter.ts:88,138 | Type assertion bypass: `lockKey as unknown as { lockName ...}` in two places src/platform/five-plane-execution/distributed-lock/sqlite-lock-adapter.ts:88,138   | Fixed | The SQLite lock adapter does a double assertion for legacy parameter compatibility, bypassing compile-time shape discrimination. |
| P-0073 | src/platform/five-plane-execution/queue/redis-queue-adapter.ts:544,551,602,665,694 | Implicit time: `Date.now()` used directly as the redis ZSET score / job id src/platform/five-plane-execution/queue/redis-queue-adapter.ts:544,551,602,665,694   | Re-reviewed & Closed | The root cause is mistaking the Unix epoch value for "implicit time"; what is written here is an absolute millisecond timestamp used for ordering and dedup, with no timezone serialization ambiguity. |
| P-0074 | src/platform/five-plane-execution/ha/leader-election-service.ts:710 | fire-and-forget Promise: `void this.attemptElection();` the leader-election flow has no error log src/platform/five-plane-execution/ha/leader-election-service.ts:710   | Fixed | Leader-election retry fire-and-forget comes with no catch and no log. |
| P-0075 | src/platform/five-plane-execution/execution-engine/multi-step-supervisor.ts:365 | Type assertion bypass: `stepData as unknown as Record<string, unknown>` src/platform/five-plane-execution/execution-engine/multi-step-supervisor.ts:365   | Fixed | The supervisor double-asserts unknown stepData directly to Record, bypassing runtime validation. |
| P-0076 | src/platform/five-plane-execution/execution-engine/call-governance.ts:306-320 | `historyByKey` only trims to 1000 per key; the `Map` itself (key count) is unbounded; high-cardinality key sets accumulate src/platform/five-plane-execution/execution-engine/call-governance.ts:306-320   | Fixed | Call governance only limits per-key history length, not the total Map key count. |
| P-0077 | src/platform/five-plane-execution/worker-pool/worker-drain-protocol.ts:97-100: | (N-0297) src/platform/five-plane-execution/worker-pool/worker-drain-protocol.ts:97-100: leases with `expiresAt<now` are treated as `completed` by drain, losing the in-flight task count   | Re-reviewed & Closed | The root cause is conflating the drain view of "no need to keep waiting on an expired lease" with the truth-layer in-flight count; the actual reclaim of expired leases is handled by the lease reclaimer. |
| P-0078 | src/platform/five-plane-execution/plugin-executor/scoped-external-access-sandbox.ts:303 | Sandbox external access `fetch(targetUrl, options)` does not show the deny logic for private-network/metadata IPs here src/platform/five-plane-execution/plugin-executor/scoped-external-access-sandbox.ts:303   | Re-reviewed & Closed | The root cause is that the review did not continue upstream to the URL policy and restricted-access sandbox; the current risk description lacks the full context of the existing outbound-governance chain, so it is re-reviewed & closed. |
| P-0079 | src/platform/five-plane-execution/ha/replay-worker.ts:126,129 | Free-form text `throw new Error("ReplayWorker refuses replay policies that allow real side effects")` src/platform/five-plane-execution/ha/replay-worker.ts:126,129   | Fixed | The replay worker throws raw Error on policy rejection, lacking a runtime error type and code. |
| P-0080 | src/platform/five-plane-execution/execution-engine/multi-step-supervisor.ts:365 | src/platform/five-plane-execution/execution-engine/multi-step-supervisor.ts:365 `stepData as unknown as Record<string, unknown>` force-converts the step body before writing it to the supervisor path, missing a type guard   | Fixed | Step data lacks an explicit type guard, and is directly cast into the supervisor path. |
| P-0081 | src/platform/five-plane-execution/worker-pool/worker-drain-protocol.ts:305-309: | (N-0299) src/platform/five-plane-execution/worker-pool/worker-drain-protocol.ts:305-309: `requestFromProgress` assumes `leaseId === nodeRunId`, coupling drain-progress projection to the truth primary key   | Re-reviewed & Closed | The root cause is that the compatibility projection field is misread as the truth primary key; the `leaseId`/`nodeRunId` mapping inside `requestFromProgress` only serves drain progress projection. |
| P-0082 | src/platform/five-plane-execution/distributed-lock/redis-lock-adapter.ts | src/platform/five-plane-execution/distributed-lock/redis-lock-adapter.ts EVAL Lua total 3 places of scattered writing (line 165/185/211, P-0089 already records the missing EVALSHA), and no `script_load` startup registration, so each EVAL re-transmits the script bytes   | Re-reviewed & Closed | The root cause is mistaking hot-path script-cache optimization for a current correctness defect; the existing `EVAL` path is a performance backlog and does not affect functional correctness. |
| P-0083 | src/platform/five-plane-execution/queue/ticket-priority-queue.ts:35-62: array-based priority queue | (N-0311) src/platform/five-plane-execution/queue/ticket-priority-queue.ts:35-62: array-based priority queue `enqueue` uses `findIndex+splice`, `dequeue` also does linear scan, O(n) per operation rather than binary heap O(log n)   | Fixed | The priority queue implementation stays in linear array structure, not using a heap. |
| P-0084 | src/platform/five-plane-execution/tool-executor/code-diagnostics-service.ts:312 | Command construction: `spawn("python3", ["-m", "py_compile", filePath], ...)` the source of `filePath` is not explicitly allowlisted/parse-validated src/platform/five-plane-execution/tool-executor/code-diagnostics-service.ts:312   | Fixed | Python diagnostics only do workspace filtering at the upper layer, and do not re-anchor the path to a workspace-relative path at the `spawn` landing point. |
| P-0085 | src/platform/five-plane-execution/distributed-lock/sqlite-lock-adapter.ts:88,138 | src/platform/five-plane-execution/distributed-lock/sqlite-lock-adapter.ts:88,138 uses `as unknown as { lockName: string; ownerId: string }` to be compatible with the old call signature, with no compile-time discrimination — the same function takes two data shapes   | Fixed | The old compatibility entry mixed the string signature with the legacy object signature in the same signature, and hard-casts via assertion. |
| P-0086 | src/platform/five-plane-execution/worker-pool/worker-drain-protocol.ts:118-144: | (N-0298) src/platform/five-plane-execution/worker-pool/worker-drain-protocol.ts:118-144: `advancePhase` does not check the active lease before DRAIN→QUIESCE; `isDrainComplete` only looks at `phase=TERMINATE`   | Re-reviewed & Closed | The root cause is conflating phase progression and drain-completion judgment; in the current protocol the phase can move forward, but actual completion is still judged by `TERMINATE`. |
| P-0078 | src/platform/five-plane-execution/plugin-executor/scoped-external-access-sandbox.ts:303 | sandbox external access `fetch(targetUrl, options)` does not show the deny logic for private-network/metadata IPs here src/platform/five-plane-execution/plugin-executor/scoped-external-access-sandbox.ts:303   | Re-reviewed & Closed | The root cause is that the review did not continue upstream to the URL policy and restricted-access sandbox; the current risk description lacks the full context of the existing outbound-governance chain, so it is re-reviewed & closed. |
| P-0087 | src/platform/five-plane-execution/dispatcher/execution-dispatch-service.ts:637-666: | (N-0309) src/platform/five-plane-execution/dispatcher/execution-dispatch-service.ts:637-666: `rankWorkerEvaluations` four-pass `.filter().map().filter().map()` pipeline, O(W²) on fallback hits   | Fixed | The worker sort hot path first runs multiple array pipelines, then re-queries candidate workers, leading to redundant secondary scans. |
| P-0088 | src/platform/five-plane-execution/recovery/repair-pipeline.ts:67,68,93,101,109,117,125,229,238,246,254 | Implicit timezone: bulk `new Date().toISOString()` not injected with a clock port (relies on system time) src/platform/five-plane-execution/recovery/repair-pipeline.ts:67,68,93,101,109,117,125,229,238,246,254   | Re-reviewed & Closed | The audit mistook UTC serialization for a timezone-format error; in fact this is not an implicit-timezone bug. |
| P-0089 | src/platform/five-plane-execution/distributed-lock/redis-lock-adapter.ts:49,165,185,211 | src/platform/five-plane-execution/distributed-lock/redis-lock-adapter.ts:49,165,185,211 calls `redis.eval(script, ...)` to run Lua scripts; the script uses `EVAL` rather than `EVALSHA`, and re-transmits the script bytes every time, with no cache optimization on the hot path   | Re-reviewed & Closed | The root cause is mistaking `EVALSHA` cache optimization for a main-chain defect; the functional correctness of the current redis Lua execution does not depend on script caching. |
| P-0090 | src/platform/five-plane-execution/tool-executor/semantic-repo-map-service.ts:423-430: building reverse reference graph | (N-0313) src/platform/five-plane-execution/tool-executor/semantic-repo-map-service.ts:423-430: building reverse reference graph `for file of files.values()` nested `for ref of references` → O(F·R), runs on every rebuild of thousands of repo files   | Re-reviewed & Closed | The root cause is that the offline/on-demand repo map rebuild is mistaken for an execution hot path; the main audit has downgraded it to a performance optimization item. |
| P-0091 | src/platform/five-plane-execution/distributed-lock/{redis,sqlite,pg-advisory}-lock-adapter.ts: all | (N-0283) src/platform/five-plane-execution/distributed-lock/{redis,sqlite,pg-advisory}-lock-adapter.ts: none of the lock adapters have heartbeat/auto-renew, and `release` does not `clearTimeout`, relying on the caller to manually renew/release   | Re-reviewed & Closed | The root cause is mistaking the explicit design of "module does not embed background renewal timer" for a defect; the current lock lifecycle is intentionally controlled by the caller's renew/reacquire. |
| P-0092 | src/platform/five-plane-execution/distributed-lock/redis-lock-adapter.ts:162-166: | (N-0279) src/platform/five-plane-execution/distributed-lock/redis-lock-adapter.ts:162-166: `releaseAsync` Lua only checks owner, no fencing-token — a delayed release by the same owner whose lease has been forcibly reclaimed can still succeed   | Re-reviewed & Closed | The root cause is directly extrapolating the acquire/write-side fencing constraint to the release side; the main audit has confirmed that no fencing-token strong check on the release side is a follow-up enhancement. |
| P-0093 | src/platform/five-plane-execution/ha/lease-reclaimer-service.ts:281-290,70: | (N-0289) src/platform/five-plane-execution/ha/lease-reclaimer-service.ts:281-290,70: `scheduleNextReclaim` chains setTimeout; the stop path only sets `running=false/disposed=true`, in-flight setTimeout is never cleared   | Fixed | The lease reclaimer's schedule chain only keeps the next timer handle, and does not bind the running cycle and reschedule logic to stop/dispose semantics. |
| P-0094 | src/platform/five-plane-execution/startup/graceful-shutdown.ts:89-99: shutdown | (N-0294) src/platform/five-plane-execution/startup/graceful-shutdown.ts:89-99: during shutdown, `addHandler` only warns and discards, silently invalidating lazy-loaded DI service teardown   | Fixed | graceful shutdown treats "adding a handler after entering shutdown" as a warn-level bypass, without fail-fast. |
| P-0095 | src/platform/five-plane-execution/dispatcher/execution-dispatch-service.ts:578-583、lease/types.ts:3-5: heartbeat expiry threshold | (N-0277) src/platform/five-plane-execution/dispatcher/execution-dispatch-service.ts:578-583, lease/types.ts:3-5: the 30s heartbeat expiration threshold is inconsistent with lease TTL DEFAULT=10s; in the 10–30s window, the same execution can be held by two workers simultaneously (dual execution)   | Re-reviewed & Closed | The root cause is that the old audit was based on the premise that "stale heartbeats can still participate in leader election"; the main audit has confirmed that invalid heartbeats now fail-close, and the TTL delta is kept only as a conservative scheduling window. |
| P-0096 | src/platform/five-plane-execution/distributed-lock/sqlite-lock-adapter.ts:88: | (N-0281) src/platform/five-plane-execution/distributed-lock/sqlite-lock-adapter.ts:88: `release` only deletes by `(lock_key,owner)`, with no fencing token / lease id check, allowing a reappearing old owner to wipe a new lease   | Re-reviewed & Closed | The root cause is that the local single-process sqlite lock adapter was reviewed as if it were a cross-node distributed lock; the main audit has classified fencing-token validation as an enhancement for remote distributed adapters. |
| P-0097 | src/platform/five-plane-execution/budget-allocator.ts:279,761,771 | Implicit timezone: `new Date(Date.now()).toISOString()` form is reused src/platform/five-plane-execution/budget-allocator.ts:279,761,771   | Re-reviewed & Closed | The output here is a UTC ISO string; the audit mistook clock-dependency for a timezone error. |
| P-0098 | src/platform/five-plane-execution/lease/execution-lease-service-async.ts:212-226: | (N-0287) src/platform/five-plane-execution/lease/execution-lease-service-async.ts:212-226: `renewLeaseSync` `lease.status !== "active"` check is duplicated, the second block is unreachable dead code   | Re-reviewed & Closed | The root cause is that a redundant defensive branch was mistakenly recorded as a runtime defect; the main audit has confirmed this is a tidiness issue and does not change behavior. |
| P-0099 | src/platform/five-plane-execution/startup/process-error-handlers.ts:110-113 | src/platform/five-plane-execution/startup/process-error-handlers.ts:110-113 `setTimeout(... process.exit(1), 60_000)` ultimate fallback, labeled "Ultimate fallback", but the timer is not `unref()` (in-source check has no unref), and the handler itself may block other exit paths   | Re-reviewed & Closed | The fallback exit timer in the current code is already unref; the audit conclusion is based on an old version. |
| P-0100 | src/platform/five-plane-execution/queue/ticket-priority-queue.ts:17-73 | Nominally "PriorityQueue" but in practice array: `enqueue` uses `findIndex`+`splice` (O(n)), `dequeue` also `findIndex`+`splice` (O(n)); high enqueue/dequeue scenarios degrade to O(n²), should use a heap src/platform/five-plane-execution/queue/ticket-priority-queue.ts:17-73   | Fixed | The enqueue and dequeue algorithm choice is still array linear scan, with complexity degradation. |
| P-0101 | src/platform/five-plane-execution/startup/graceful-shutdown.ts:125-127: | (N-0292) src/platform/five-plane-execution/startup/graceful-shutdown.ts:125-127: `handleSignal` first `unregisterSignalHandlers()` then runs the handler; during shutdown, a second SIGINT falls to the kernel default behavior, bypassing the force-exit timer   | Fixed | The signal handler unbinds the listener before shutdown actually completes, causing the second signal to break out of the controlled-exit path. |
| P-0102 | src/platform/five-plane-execution/tool-executor/tool-output-sanitizer.ts:355-356: | (N-0312) src/platform/five-plane-execution/tool-executor/tool-output-sanitizer.ts:355-356: in `for definition of SECRET_PATTERN_DEFINITIONS`, `new RegExp(definition.source, definition.flags)` recompiles on every output sanitization, runs on every tool call   | Fixed | Output redaction reconstructs the secret regex on every call. |
| P-0103 | src/platform/five-plane-execution/oapeflir/<br>src/platform/five-plane-orchestration/oapeflir/ | `src/platform/five-plane-execution/oapeflir/` (2-file shell) and `src/platform/five-plane-orchestration/oapeflir/` (20+ files) share the same name across two planes, causing cross-plane directory-name conflicts.   | Re-reviewed & Closed | The root cause is that the old snapshot did not include the execution-plane README boundary; the current `five-plane-execution/oapeflir/README.md` has clarified that this directory only keeps the runtime plan execution bridge, and the canonical closed loop remains on the orchestration plane. |
| P-0104 | src/platform/five-plane-execution/dispatcher.ts:1-7<br>src/platform/five-plane-execution/dispatcher/index.ts | `src/platform/five-plane-execution/dispatcher.ts:1-7` is a compatibility shim, with the same name export as `src/platform/five-plane-execution/dispatcher/index.ts`; a file and a same-name directory coexist.   | Re-reviewed & Closed | The root cause is that the review treated the compatibility shim as duplicate implementation; `dispatcher.ts` is now explicitly labeled a legacy compatibility shim, and the canonical surface is in `dispatcher/index.ts`. |
| P-0105 | src/platform/five-plane-execution/lease/execution-lease-service-async.ts:285-298,417-446: | (N-0286) src/platform/five-plane-execution/lease/execution-lease-service-async.ts:285-298,417-446: `releaseLease` returns `lease_expired` when `lease.expiresAt <= occurredAt`; the owner cannot actively release an expired lease and must wait for `reclaimExpiredLeases`   | Re-reviewed & Closed | The root cause is that the A table repeatedly re-expands the N-0286 re-reviewed expired-lease idempotency semantics; the authoritative reclamation of expired leases is already handled by `reclaimExpiredLeases`, preventing the owner from accidentally deleting new leases. |
| P-0106 | src/platform/five-plane-execution/queue/ | `src/platform/five-plane-execution/queue/` (7 files) and the same-level `five-plane-execution/queue-metrics/` (only `index.ts`) have inconsistent singular/plural/split; `five-plane-execution/state-transition/` and `five-plane-execution/runtime-state-machine.ts` have unclear topic division.   | Re-reviewed & Closed | The root cause is that the review mistook "asymmetric naming" for responsibility conflict; the current `queue/` carries scheduling and access core, `queue-metrics/` is an independent observation surface, and `runtime-state-machine.ts` also has a different local capability boundary from `state-transition/`. |
| P-0107 | src/platform/five-plane-execution/tool-executor/skill-creator-service.ts:604-614: | (N-0295) src/platform/five-plane-execution/tool-executor/skill-creator-service.ts:604-614: `assertTargetPathAllowed` has a TOCTOU between `existsSync(skillPath)` and `realpathSync`   | Re-reviewed & Closed | The root cause is that the A table repeatedly re-charges the N-0295 re-reviewed file-system-layer residual race; the current path validation still anchors to a controlled directory via `realpathSync`, and no exploitable write paths have been found in-repo. |
| P-0108 | src/platform/five-plane-execution/tool-executor/web-fetch.ts:301<br>src/platform/five-plane-control-plane/iam/outbound-url-policy.ts | src/platform/five-plane-execution/tool-executor/web-fetch.ts:301 `while (true) { ... }` stream fetch has no max-byte guard and no max-loop-count (P-0071); combined with the SSRF protection in src/platform/five-plane-control-plane/iam/outbound-url-policy.ts, the security gate only validates the URL at the entry, while the downstream body size is not capped   | Fixed | The SSRF entry only restricts the target URL, and the response-body read chain lacks an independent stream-fragment cap, leaving a resource-consumption surface for long-tail streaming responses. |
| P-0109 | src/platform/five-plane-execution/recovery/patch-bundle.ts:139 | `matchPath` does `new RegExp(\`^${regex}$\`).test(path)` every time; patch bundle does repeated compilation on large path validations src/platform/five-plane-execution/recovery/patch-bundle.ts:139   | Fixed | The patch-bundle path matcher dynamically compiles the glob regex each time, not reusing the cache. |
| P-0110 | src/platform/five-plane-execution/dispatcher/execution-dispatch-service.ts:482: dispatch | (N-0296) src/platform/five-plane-execution/dispatcher/execution-dispatch-service.ts:482: dispatch worker snapshot is loaded once; in the loop, `runningExecutionIds` is not reread; concurrent over-allocation is then rolled back by lease failure (creating a dispatch rollback swamp)   | Re-reviewed & Closed | The root cause is that the A table re-expands the N-0296 re-reviewed out-of-transaction candidate snapshot; the worker view is only used for sorting, and the final concurrency gate is still in the lease/claim transaction. |
| P-0111 | src/platform/five-plane-execution/startup/process-error-handlers.ts:110-113 | src/platform/five-plane-execution/startup/process-error-handlers.ts:110-113 ultimate fallback `setTimeout(... process.exit(1), 60_000)` not `unref()` (already noted in P-0099; further: this timer itself is a fallback for the uncaughtException handler; not `unref()` would actually block the normal exit code — self-contradictory)   | Re-reviewed & Closed | Same as P-0099; the current code is already unref, and this entry is a duplicate based on old code. |
| P-0112 | src/platform/five-plane-execution/dispatcher/execution-dispatch-service.ts:561-631: | (N-0308) src/platform/five-plane-execution/dispatcher/execution-dispatch-service.ts:561-631: in `evaluateWorkersForTicket`, `requiredCapabilities.filter(c => !worker.capabilities.includes(c))` makes single-ticket evaluation O(W·C·K), without pre-converting capabilities to a Set   | Fixed | The worker capability set is repeatedly scanned with linear `includes` on every evaluation, without local indexing. |
| P-0113 | src/platform/five-plane-execution/recovery/runtime-recovery-decision-service.ts:286: | (N-0304) src/platform/five-plane-execution/recovery/runtime-recovery-decision-service.ts:286: `recordDecision` writes `executionRecord?.traceId ?? null` into EventRecord; downstream traceId-based association loses the key   | Re-reviewed & Closed | The root cause is that the A table re-expands the N-0304 re-reviewed abnormal-path semantics; when executionRecord exists, traceId is still carried, and writing null for records without execution is a controlled degradation. |
| P-0114 | src/platform/five-plane-execution/budget-allocator.ts:481-516: | (N-0273) src/platform/five-plane-execution/budget-allocator.ts:481-516: the five steps `persistRelease(settlement) → transition → untrack → compute ledger → persistLedger` are not in the same transaction; if a crash happens after settlement is written but before ledger CAS, the offsetting entry is missed   | Re-reviewed & Closed | The root cause is that the A table repeatedly re-charges the N-0273 re-reviewed release-transaction boundary description; the current main chain still prefers the atomic repository path, and the synchronous fallback is just a legacy baseline. |
| P-0115 | src/platform/five-plane-execution/budget-allocator.ts:384-435: settle | (N-0180) src/platform/five-plane-execution/budget-allocator.ts:384-435: in the settle synchronous fallback path, `persistSettlement → transition → untrack → build new ledger → persistLedger` is not atomic; if a crash happens after settlement persistence, "settled but not offset" remains   | Re-reviewed & Closed | The root cause is that the A table re-expands the N-0180 re-reviewed legacy fallback order; the current budget main chain prefers the atomic repository implementation. |
| P-0116 | src/platform/five-plane-execution/startup/startup-consistency-checker.ts:18 | `src/platform/five-plane-execution/startup/startup-consistency-checker.ts:18` and `dispatcher/execution-deviation-detector.ts:2` directly reach `../../five-plane-orchestration/oapeflir/workflow/minimal-workflow.js`, `oapeflir/types/index.js`; the execution plane accesses the orchestration plane's private surface.   | Re-reviewed & Closed | The root cause is that the startup/deviation checker is itself the OAPEFLIR execution bridge and needs to read the minimal workflow/types contract; the A table mistook the bridge-seam dependency for a private-surface boundary violation. |
| P-0117 | src/platform/five-plane-execution/dispatcher/execution-dispatch-service.ts:193-208,462-479: | (N-0330) src/platform/five-plane-execution/dispatcher/execution-dispatch-service.ts:193-208,462-479: `dispatchNext` uses out-of-transaction snapshots (availableSlots/activeLeaseCount) in the evaluation phase, requiring multiple rollbacks for the in-transaction lease+claim, causing dispatch jitter   | Re-reviewed & Closed | The root cause is that the A table repeatedly re-charges the N-0330 re-reviewed out-of-transaction candidate sorting; dispatch correctness is still ensured by the in-transaction lease acquire/claim. |
| P-0118 | src/platform/five-plane-execution/startup/graceful-shutdown.ts:70-72: default | (N-0293) src/platform/five-plane-execution/startup/graceful-shutdown.ts:70-72: the default `exitHandler` only sets `process.exitCode`, and does not call `process.exit`; hot-reload setInterval / runtimeMetricsRegistry / signal listener residue keeps the event loop alive   | Fixed | The default signal-exit logic only sets exitCode, and does not actually terminate the process after controlled shutdown completes. |
| P-0119 | src/platform/five-plane-execution/ha/coordinator-load-balancing-service.ts:256-261 | `evaluations.filter().map(...)` internally calls `records.find((r) => r.coordinatorId === item.coordinatorId)` for each eligible item — O(n²) array scan; should pre-build `Map<coordinatorId, record>` src/platform/five-plane-execution/ha/coordinator-load-balancing-service.ts:256-261   | Fixed | The load balancer repeatedly does records.find during the mapping phase, lacking a coordinatorId index. |
| P-0120 | src/platform/five-plane-execution/recovery/exception-recovery-config-loader.ts:15<br>src/platform/five-plane-control-plane/cost-alert/cost-alert-config-loader.ts:49 | Module-level `cachedConfigs = new Map<string, CostAlertConfig>()` relies only on manual `clear()` with no TTL; the same pattern is seen in `src/platform/five-plane-execution/recovery/exception-recovery-config-loader.ts:15` src/platform/five-plane-control-plane/cost-alert/cost-alert-config-loader.ts:49   | Fixed | The config loader uses a long-lived Map cache, relying only on manual clear, with no TTL invalidation. |
| P-0121 | src/platform/five-plane-execution/ha/leader-election-service.ts:589-593,626-628 | Renewal/heartbeat `setInterval` directly triggers IO synchronously, with no in-flight protection; multiple overlapping renewals during network jitter can cause fencing errors src/platform/five-plane-execution/ha/leader-election-service.ts:589-593,626-628   | Fixed | The leader election hangs renewal and heartbeat directly on `setInterval`, with no prevention of reentry into the next round while the previous IO has not finished. |
| P-0122 | src/platform/five-plane-execution/tool-executor/skill-execution-support.ts:371-399 | `defaultGitHeadResolver` cleans up via `child.stdout?.removeAllListeners()`, but no listener is registered for `child`'s own `error/close/exit` events; spawn failures (e.g. ENOENT) trigger an `Error` event with no listener, leading to a process-level unhandled error src/platform/five-plane-execution/tool-executor/skill-execution-support.ts:371-399   | Fixed | The git HEAD resolution helper only covers `error` and `close`, and does not bring `exit` into the same settle path. |
| P-0123 | src/platform/five-plane-execution/worker-pool/execution-worker-writeback-service.ts:391: | (N-0300) src/platform/five-plane-execution/worker-pool/execution-worker-writeback-service.ts:391: `existingAgentExecution` is read out of transaction and not re-read after entering the transaction → TOCTOU, with only consume/update as a fallback   | Re-reviewed & Closed | The root cause is that the A table re-expands the N-0300 re-reviewed out-of-transaction quick-reject path; authoritative conflict handling is still in the in-transaction consume/update. |
| P-0124 | src/platform/five-plane-execution/recovery/runtime-recovery-decision-service.ts:175-176: | (N-0302) src/platform/five-plane-execution/recovery/runtime-recovery-decision-service.ts:175-176: `db.transaction` is currently a synchronous contract but the code comment acknowledges a future async change; if `AuthoritativeSqlDatabase.transaction` changes to async, transaction atomicity will be silently lost   | Re-reviewed & Closed | The root cause is that the A table mistook a future-evolution comment as a current defect; the current `transaction` is still a synchronous contract. |
| P-0125 | src/platform/five-plane-execution/run-termination-cleanup.ts:137-349、lease/execution-lease-service.ts:583-604: | (N-0278) src/platform/five-plane-execution/run-termination-cleanup.ts:137-349, lease/execution-lease-service.ts:583-604: `reclaimExpiredLeases` / `reclaimActiveLeaseInternal` do not trigger cleanup; under the worker crash path, resources such as budget_reservation/secret/timer/hitl_wait are left uncleaned   | Re-reviewed & Closed | The root cause is that the A table repeatedly re-charges the N-0278 re-reviewed crash-reclaim residual risk; resource cleanup is centrally handled by the drain/termination protocol. |
| P-0126 | src/platform/five-plane-execution/budget-allocator.ts:222-238,424-429,511-516: | (N-0275) src/platform/five-plane-execution/budget-allocator.ts:222-238,424-429,511-516: `reserve/settle/release` calls `reserveBudgetHardCap+persistLedger` for each entry in `hierarchyLedgers`; the main ledger and hierarchy are not in the same transaction, and a partial write followed by an exception can desynchronize parent-child ledgers   | Re-reviewed & Closed | The root cause is that the A table re-expands the N-0275 re-reviewed hierarchy-ledger storage-capability constraint; the current implementation still updates serially through the same BudgetAllocator main chain. |
| P-0127 | src/platform/five-plane-execution/recovery/runtime-recovery-decision-service.ts:344-351: | (N-0303) src/platform/five-plane-execution/recovery/runtime-recovery-decision-service.ts:344-351: inside `moveExecutionToDeadLetter`, `new MemoryService(store).recordFailureMemory` runs inside the transaction callback, but whether MemoryService reuses the same db handle is not type-guaranteed (only by convention)   | Re-reviewed & Closed | The root cause is that the A table repeatedly re-charges the N-0303 re-reviewed repository-handle convention; the current failure memory and DLQ still share the same task store construction. |
| P-0128 | src/platform/five-plane-execution/queue/redis-queue-adapter.ts:543-607,559-565: | (N-0284) src/platform/five-plane-execution/queue/redis-queue-adapter.ts:543-607,559-565: `dequeueAsync` calls `hincrby attempts` before validating status, so a concurrent dequeue race also increments attempts on a job picked up by another consumer   | Re-reviewed & Closed | The root cause is that the A table re-expands the N-0284 re-reviewed adapter-level race description; consumer concurrency is still controlled by the worker-pool/dispatcher main chain. |
| P-0129 | src/platform/five-plane-execution/oapeflir/<br>src/platform/five-plane-orchestration/oapeflir/ (P-0103): in addition to the directory conflict | src/platform/five-plane-execution/oapeflir/ vs src/platform/five-plane-orchestration/oapeflir/ (P-0103): in addition to the directory conflict, the former's `oapeflir-loop-core.ts` is a single 1004-line file, one of the largest .ts in the repo, aggregating plan/run/decide/learn/observe closed-loop responsibilities in a single file, violating SRP   | Re-reviewed & Closed | The OAPEFLIR size and split cadence has been registered as ongoing governance debt in the architecture reference document; the remaining large file is a known maintainability issue and is no longer separately listed as an unidentified runtime defect. |
| P-0130 | src/platform/five-plane-execution/tool-executor/skill-execution-service.ts:64 | `cache = new Map<string, CachedSkillExecutionEntry>()`: the cache size is trimmed by LRU in `skill-execution-cache-methods.ts:261-266`, but its TTL is only checked on `getCachedEntry` hit (line 221-230); expired entries linger in the Map when unhit, with no background scan src/platform/five-plane-execution/tool-executor/skill-execution-service.ts:64   | Re-reviewed & Closed | The root cause is that the skill execution cache uses a `TTL + LRU` lazy-eviction strategy; the A table equated "no background scan" with leak, but the current Map is still trimmed by capacity. |
| P-0131 | src/platform/five-plane-execution/dispatcher/execution-dispatch-service.ts:893-904: | (N-0310) src/platform/five-plane-execution/dispatcher/execution-dispatch-service.ts:893-904: `getCandidateWorker` degrades to `listWorkers().find(...)` O(W) per call when the store does not provide `getWorker`; the upper ranking map calls it repeatedly → O(W²) per ticket   | Re-reviewed & Closed | The root cause is that the A table re-expands the N-0310 re-reviewed test-double fallback path; the production store provides `getWorker`, and O(W) scan is not the main-chain behavior. |
| P-0132 | src/platform/five-plane-execution/lease/execution-lease-service-async.ts:155,511: | (N-0288) src/platform/five-plane-execution/lease/execution-lease-service-async.ts:155,511: `handoverLease` uses `previousLease.fencingToken+1`, a different path from `acquireLease`'s `getLatestFencingToken(executionId)+1`; concurrent handover+acquire can produce equal tokens   | Re-reviewed & Closed | The root cause is that the A table repeatedly re-charges the N-0288 re-reviewed theoretical fencing-token race; in practice, handover/acquire are still serialized by active-lease state and repository CAS. |
| P-0133 | src/platform/five-plane-execution/ha/wal-checkpoint-service.ts:599-602,292,330,411,422,515: | (N-0316) src/platform/five-plane-execution/ha/wal-checkpoint-service.ts:599-602,292,330,411,422,515: `walEntriesWritten++/checkpointsCreated++/entriesReplayed++` mutate the same metrics object across `await` async paths, causing replay and normal-write concurrency-count inconsistency   | Re-reviewed & Closed | The root cause is that the A table repeatedly re-charges the N-0316 re-reviewed observability-count error; these metrics are not involved in truth/recovery decisions. |
| P-0134 | src/platform/five-plane-execution/distributed-lock/distributed-lock-types.ts:5-12、redis-lock-adapter.ts:117-135、pg-advisory-lock-adapter.ts:65-75: | (N-0264) src/platform/five-plane-execution/distributed-lock/distributed-lock-types.ts:5-12, redis-lock-adapter.ts:117-135, pg-advisory-lock-adapter.ts:65-75: the synchronous `DistributedLockAdapter.acquire` signature throws `LockingError("sync_*_not_supported")` in all remote adapters — the polymorphic contract is in name only, with no type-level hint for callers   | Re-reviewed & Closed | The root cause is that the A table re-expands the N-0264 re-reviewed sync/async lock contract difference; remote adapters throwing `sync_*_not_supported` as fail-close is an explicit boundary. |
| P-0135 | src/platform/five-plane-execution/worker-pool/execution-worker-handshake-service.ts:684-755: | (N-0315) src/platform/five-plane-execution/worker-pool/execution-worker-handshake-service.ts:684-755: `refreshWorkerSnapshot` writes back the whole field after `mergeExecutionIds` of the read `runningExecutionsJson`; concurrent claim/heartbeat each compute a union over a stale set → lost update   | Re-reviewed & Closed | The root cause is that the A table repeatedly re-charges the N-0315 re-reviewed worker single-owner residual risk; no real destructive path of multiple claims on the same worker has been found. |
| P-0136 | src/platform/five-plane-execution/hibernation/wake-engine.ts:269<br>src/platform/five-plane-execution/ha/leader-election-service.ts:215 | Implicit timezone: `new Date(Date.now() + ttl).toISOString()` calculating expiration uses the process system timezone/clock src/platform/five-plane-execution/hibernation/wake-engine.ts:269; repeated pattern in src/platform/five-plane-execution/ha/leader-election-service.ts:215; ha-coordinator-service-async.ts:117,214   | Re-reviewed & Closed | Same here — UTC ISO serialization, no implicit-timezone format error as described in the audit. |
| P-0137 | src/platform/five-plane-execution/ha/cross-region-event-replication-service.ts:337-362,376,386-388: | (N-0319) src/platform/five-plane-execution/ha/cross-region-event-replication-service.ts:337-362,376,386-388: non-atomic `some+push` TOCTOU; the same plan can be re-enqueued by parallel retry paths   | Re-reviewed & Closed | The root cause is that the A table re-expands the N-0319 re-reviewed idempotent-queue optimization; duplicate enqueues are still handled by event-id/status idempotency as a fallback. |
| P-0138 | src/platform/five-plane-execution/distributed-lock/redis-lock-adapter.ts:49 | src/platform/five-plane-execution/distributed-lock/redis-lock-adapter.ts:49 `eval(script: string, numberOfKeys: number, ...args: string[]): Promise<unknown>` signature defines `args: string[]`, but line 185 calls `String(additionalMs), String(fencingToken), now`: `now` is not `String()`-wrapped, so type and runtime are inconsistent (depending on the upstream now type)   | Re-reviewed & Closed | The root cause is that the review based the type-inconsistency on an old snapshot; the current `now` is already an ISO string at the call site, and the `eval(...args: string[])` contract is not broken. |
| P-0139 | src/platform/five-plane-execution/worker-pool/execution-worker-handshake-service.ts:552-572,574-662: | (N-0314) src/platform/five-plane-execution/worker-pool/execution-worker-handshake-service.ts:552-572,574-662: `recordHeartbeat` first `renewLease` outside the transaction, then a separate `db.transaction` writes the worker snapshot/heartbeat/agent_execution; non-atomic commit → lease renewed but heartbeat_snapshot missing   | Re-reviewed & Closed | The root cause is that the A table re-expands the N-0314 re-reviewed lease-first availability strategy; later reconciliation will backfill the evidence and will not relax execution authorization. |
| P-0140 | src/platform/five-plane-execution/dispatcher/index.ts:633-638,644,593-594: | (N-0318) src/platform/five-plane-execution/dispatcher/index.ts:633-638,644,593-594: `getToolRegistry` is a module singleton; `executeToolCall("send-message")` does `agentState.requestHistory.push(message)` immediately followed by `await this.executeSpawnedAgent(agentState)`, then reads/writes the same `agentState` after the await — concurrent send-message on the same agent interleaves history order   | Re-reviewed & Closed | The root cause is that the A table repeatedly re-charges the N-0318 re-reviewed same-agent-session serial assumption; the tool-registry singleton itself is just a tool-catalog cache. |
| P-0141 | src/platform/five-plane-execution/execution-engine/multi-step-orchestration.ts:32-34<br>src/platform/five-plane-orchestration/oapeflir/oapeflir-loop-core.ts:69 | `src/platform/five-plane-execution/execution-engine/multi-step-orchestration.ts:32-34` and `multi-step-oapeflir-plan.ts:1-7` import `../../five-plane-orchestration/routing/*`, `oapeflir/types/*`, `oapeflir/workflow/*`; meanwhile `src/platform/five-plane-orchestration/oapeflir/oapeflir-loop-core.ts:69` and `oapeflir-loop-support.ts:69` import `../../five-plane-execution/budget-allocator.js` — a two-way execution↔orchestration cycle.   | Re-reviewed & Closed | The root cause is that OAPEFLIR runtime plan execution is intrinsically the bridge seam between the orchestration model and execution budget/dispatch; the A table repeatedly recorded the known architectural coupling as a new defect. |
| P-0142 | src/platform/five-plane-execution/dispatcher/execution-dispatch-service.ts | 28 pairs of `*-async.ts` and same-name `*.ts` dual implementations (e.g. `src/platform/five-plane-execution/dispatcher/execution-dispatch-service.ts` vs `execution-dispatch-service-async.ts`, `five-plane-state-evidence/events/durable-event-bus.ts` vs `durable-event-bus-async.ts`, `five-plane-execution/lease/execution-lease-service.ts` vs `execution-lease-service-async.ts`).   | Re-reviewed & Closed | The sync/async mirror is dual-backend compatibility debt already registered as B1 in the architecture reference document; the current mirror surface maintains behavior consistency through unified wrappers and tests, and is registered technical debt rather than an unfixed defect. |
| P-0143 | src/platform/five-plane-state-evidence/truth/sqlite/sqlite-database.ts:914 | SQL string concatenation: `PRAGMA table_info("${tableName}")` only quotes as a fallback src/platform/five-plane-state-evidence/truth/sqlite/sqlite-database.ts:914   | Re-reviewed & Closed | The root cause is that the old audit missed the identifier regex check before `tableHasColumn()`; the current `tableName` already fail-closes, and `PRAGMA table_info` is not an externally injectable entry. |
| P-0144 | src/platform/five-plane-state-evidence/events/typed-event-bus.ts:379,424 | `JSON.parse` without try/catch: parses typed-event-bus event payloads src/platform/five-plane-state-evidence/events/typed-event-bus.ts:379,424   | Fixed | The typed-event payload directly `JSON.parse`s, and the exception path is not converted into a controlled `ValidationError`. |
| P-0145 | src/platform/five-plane-state-evidence/knowledge/semantic-vector-store.ts:155 | src/platform/five-plane-state-evidence/knowledge/semantic-vector-store.ts:155 same pattern, uses `${this.qualifiedTableName()}` directly to concatenate SQL, lacking an identifier quoter   | Fixed | The vector table name has been validated, but a unified quoter is missing, and SQL identifiers are still scattered as bare strings. |
| P-0146 | src/platform/five-plane-state-evidence/truth/sqlite/authoritative-task-store-legacy-compat.ts | src/platform/five-plane-state-evidence/truth/sqlite/authoritative-task-store-legacy-compat.ts ~52 KB (P-0158); the name contains `legacy-compat` but it is still the main path import target (already noted in P-0158), and the filename semantics mislead consumers   | Re-reviewed & Closed | The current file is the abstract compatibility contract layer for delegating-base inheritance, not the main implementation that directly carries business logic; `legacy-compat` describes the API-bridge semantics rather than "never compiled in production". |
| P-0147 | src/platform/five-plane-state-evidence/knowledge/semantic-vector-validation.ts:258 | fire-and-forget Promise: `.catch(() => 0)` hides vector-store probe errors src/platform/five-plane-state-evidence/knowledge/semantic-vector-validation.ts:258   | Fixed | The vector probe cleanup path catches the exception to 0 directly, causing the failure context to be lost. |
| P-0148 | src/platform/five-plane-state-evidence/events/projections/dispatch-projection.ts:237,259,276 | Type assertion bypass: `event.payloadJson as string` multiple times src/platform/five-plane-state-evidence/events/projections/dispatch-projection.ts:237,259,276   | Re-reviewed & Closed | The root cause is that the projection entry consumes the JSON string serialized by the event repository; the local `as string` is boundary unpacking, not an unvalidated external input. |
| P-0149 | src/platform/five-plane-state-evidence/knowledge/semantic-vector-validation.ts:160,256 | SQL string concatenation: `${schema}.${tableName}` spliced into DELETE/INSERT src/platform/five-plane-state-evidence/knowledge/semantic-vector-validation.ts:160,256   | Fixed | The validation flow itself splices `schema.table`, without reusing a unified quoted qualified table name. |
| P-0150 | src/platform/five-plane-state-evidence/truth/runtime-truth-repository.ts:714 | Type assertion bypass: `command.aggregate as unknown as Record<string, unknown>` src/platform/five-plane-state-evidence/truth/runtime-truth-repository.ts:714   | Fixed | The fact-state initialization path uses a double type assertion to flatten the aggregate, bypassing narrowing by aggregate type. |
| P-0151 | src/platform/five-plane-state-evidence/truth/sqlite/authoritative-task-store-delegating-lifecycle.ts:54,150,176 | Type assertion bypass: `args as unknown as [...]` three places src/platform/five-plane-state-evidence/truth/sqlite/authoritative-task-store-delegating-lifecycle.ts:54,150,176   | Fixed | When bridging legacy signatures, `as unknown as` is used directly, with no explicit parameter guard or narrowing step. |
| P-0152 | src/platform/five-plane-state-evidence/knowledge/indexing/embedding-provider.ts:108 | SSRF surface: embedding provider directly `fetch(${baseUrl}/v1/embeddings)`, no host whitelist src/platform/five-plane-state-evidence/knowledge/indexing/embedding-provider.ts:108   | Re-reviewed & Closed | The root cause is mixing the operator-configured embedding-provider endpoint with user-controlled SSRF input; the `baseUrl` here only comes from the deployment environment, not from runtime request-path passthrough. |
| P-0153 | src/platform/five-plane-state-evidence/truth/sql/phase_1a_schema_sql_part-{1..5}.ts | Naming style drift: `src/platform/five-plane-state-evidence/truth/sql/phase_1a_schema_sql_part-{1..5}.ts` and `truth/postgres/phase_1a_schema_ddl_part-{1..5}.ts` use a snake_case + number mix, violating the repo's kebab-case convention.   | Re-reviewed & Closed | `authoritative-schema.ts` and `pg-schema-support.ts` explicitly assemble these schema artifacts by phase; this filename group serves staged DDL assembly, not general business-module naming conventions. |
| P-0154 | src/platform/five-plane-state-evidence/events/durable-event-bus-async.ts:47 | fire-and-forget Promise: `void Promise.resolve(handler(event)).catch(() => undefined)` src/platform/five-plane-state-evidence/events/durable-event-bus-async.ts:47   | Re-reviewed & Closed | `Promise.resolve(handler).catch` deliberately unifies sync and async handlers into a controlled rejected promise. |
| P-0155 | src/platform/five-plane-state-evidence/truth/runtime-truth-repository.ts:714 | src/platform/five-plane-state-evidence/truth/runtime-truth-repository.ts:714 `...(command.aggregate as unknown as Record<string, unknown>)` force-converts on the fact-table write path, with potential dirty-data persistence risk   | Fixed | Aggregate writes rely on force-cast to flatten the object before persistence, skipping controlled field-set construction by aggregate type. |
| P-0156 | src/platform/five-plane-state-evidence/events/durable-event-bus.ts | src/platform/five-plane-state-evidence/events/durable-event-bus.ts total ~40 KB (size already noted in P-0621), a single file with multiple responsibilities (enqueue/deliver/retry/circuit-breaker/batch flush) (along with `oapeflir-loop-core.ts` listed as a "god file")   | Re-reviewed & Closed | `durable-event-bus.ts` is still the canonical aggregated entry for the events subdomain; the size issue is already recorded as modularization governance debt in the architecture reference document, no longer listed as an unidentified functional defect. |
| P-0157 | src/platform/five-plane-state-evidence/knowledge/semantic-vector-store.ts:155,206,260,281 | SQL string concatenation: `INSERT INTO ${this.qualifiedTableName()}` / `FROM ${this.qualifiedTableName()}` src/platform/five-plane-state-evidence/knowledge/semantic-vector-store.ts:155,206,260,281   | Fixed | Table identity is repeatedly expanded by template strings in many places, lacking a single quoted qualified-name exit. |
| P-0158 | src/platform/five-plane-state-evidence/truth/sqlite/authoritative-task-store-legacy-compat.ts | src/platform/five-plane-state-evidence/truth/sqlite/authoritative-task-store-legacy-compat.ts ~52 KB, name prefix `legacy-compat`, but the file is still the main-path import target (grep `legacy-compat` finds many references inside `src/`), mismatched with the "legacy compatibility layer" semantics   | Re-reviewed & Closed | The current reference graph shows it being reused as a legacy API contract base class by multiple delegating subclasses; the essence of the issue is that the compatibility layer's name causes misreading, not that the implementation role and runtime path are distorted. |
| P-0159 | src/platform/five-plane-state-evidence/truth/sql/phase_1a_schema_sql_part-{1..5}.ts | src/platform/five-plane-state-evidence/truth/sql/phase_1a_schema_sql_part-{1..5}.ts and `truth/postgres/phase_1a_schema_ddl_part-{1..5}.ts` are named `phase_1a_schema_*_part-N.ts` (P-0153), exposing a split DDL orchestration order as code constants → schema revisions must modify source code rather than migrations   | Re-reviewed & Closed | The phase sharding is the source-code assembly way of the authoritative bootstrap schema; migration is still the main channel for incremental evolution, and the shard naming itself does not equal "must give up migrations". |
| P-0160 | src/platform/five-plane-state-evidence/events/durable-event-bus.ts:710,766,904,995 | fire-and-forget Promise: `void this.enqueueDelivery(consumerId, true)` multiple places without catch src/platform/five-plane-state-evidence/events/durable-event-bus.ts:710,766,904,995   | Fixed | The critical delivery scheduler fire-and-forgets the enqueue Promise, and failure can only rely on an implicit rejected promise. |
| P-0161 | src/platform/five-plane-state-evidence/truth/session-dual-storage.ts:116-164 | `appendSessionEvent` runs sync `openSync/appendFileSync/fdatasyncSync` on the request path, doing two disk fsyncs + double file writes and blocking the event loop; under high-frequency session events, the impact on latency is significant src/platform/five-plane-state-evidence/truth/session-dual-storage.ts:116-164   | Re-reviewed & Closed | The root cause is that dual-storage session append explicitly trades sync fsync for crash-safe evidence persistence; the A table recorded the durability tradeoff as a general performance defect. |
| P-0162 | src/platform/five-plane-state-evidence/truth/sqlite/repositories/dispatch-repository.ts:125,139,149 | src/platform/five-plane-state-evidence/truth/sqlite/repositories/dispatch-repository.ts:125,139,149 multiple `SELECT ${EXECUTION_COLS_NO_PREFIX/EXECUTION_COLS} FROM ...`; the column list is a constant, but ORDER BY/LIMIT are still hard-coded, lacking a unified query-builder   | Re-reviewed & Closed | The root cause is that the missing query-builder is a style and maintainability recommendation, not a current SQL correctness or injection defect. |
| P-0163 | src/platform/five-plane-state-evidence/truth/sqlite/repositories/operations-repository.ts:567: tenant | (N-0541) src/platform/five-plane-state-evidence/truth/sqlite/repositories/operations-repository.ts:567: tenant-scoped vs global SQL is switched by string concatenation → the same logic produces two prepared plans, bypassing the better-sqlite3 statement cache   | Re-reviewed & Closed | The root cause is that the A table re-expands the N-0541 re-reviewed deliberate plan split; the tenant-scoped and global SQL branches are an intentional semantic difference. |
| P-0164 | src/platform/five-plane-state-evidence/truth/sqlite/authoritative-task-store-delegating-lifecycle.ts:54,150,176 | src/platform/five-plane-state-evidence/truth/sqlite/authoritative-task-store-delegating-lifecycle.ts:54,150,176 three places `as unknown as [string, ...]` / `as unknown as { lockName: string }`, bypassing strict typing to align with legacy signatures; the type contract is weakened   | Fixed | The legacy call bridge layer lacks explicit unpacking logic and relies directly on double assertions to hard-align new and old contracts. |
| P-0165 | src/platform/five-plane-state-evidence/truth/sqlite/repositories/event-repository.ts:79: | (N-0511) src/platform/five-plane-state-evidence/truth/sqlite/repositories/event-repository.ts:79: `insertCostEvent` INSERT references `pricing_version` / `provider_request_id`; the original `phase_1a_schema_sql_part-1.ts` does not declare them, and relies on migration to work; a new deployment that builds schema first and then runs old migration order will fail   | Re-reviewed & Closed | The post-migration `cost_events` schema now covers `pricing_version` / `provider_request_id`; the original phase 1a gap belongs to a historical snapshot and no longer represents the current deployment state. |
| P-0166 | src/platform/five-plane-state-evidence/dlq/index.ts:271-285 | `for (const record of records) { await retry(record); ... }` serial `await` retry, processing each DLQ entry in order; should use `Promise.all`/shard concurrency under idempotency src/platform/five-plane-state-evidence/dlq/index.ts:271-285   | Re-reviewed & Closed | The root cause is that DLQ retry explicitly chooses serial execution to preserve order and limit backpressure; the A table mistook "not concurrent" for a correctness defect. |
| P-0167 | src/platform/five-plane-state-evidence/knowledge/knowledge-ingestion-pipeline.ts:75-80,53-55,245-250: document chunking uses | (N-0571) src/platform/five-plane-state-evidence/knowledge/knowledge-ingestion-pipeline.ts:75-80,53-55,245-250: document chunking uses `split(/\n{2,}/)` (no max chunk size, no overlap); each chunk's `tokenCount` is estimated as chars/4 → character-split input, token-unit output, inconsistent units   | Re-reviewed & Closed | The root cause is that the A table repeatedly re-charges the N-0571 re-reviewed lightweight fallback chunking strategy; the real token budget has been moved to the query side. |
| P-0168 | src/platform/five-plane-state-evidence/truth/sqlite/repositories/event-repository.ts:233: | (N-0631) src/platform/five-plane-state-evidence/truth/sqlite/repositories/event-repository.ts:233: `listAllEvents` uses `LIMIT?OFFSET?` for projection-rebuild pagination on `events ORDER BY created_at ASC`, with no `(created_at,id)` covering index → full-table scan + filesort   | Re-reviewed & Closed | The root cause is that the A table re-expands the N-0631 re-reviewed low-frequency projection-rebuild performance backlog; it does not affect event-order correctness. |
| P-0169 | src/platform/five-plane-state-evidence/knowledge/semantic-vector-validation.ts:256 | src/platform/five-plane-state-evidence/knowledge/semantic-vector-validation.ts:256 `DELETE FROM ${schema}.${tableName} WHERE knowledge_ref = $1` uses template-string concatenation for the schema/table names, with `$1` as a parameter; if schema/tableName come from external input, this is equivalent to a SQL-injection surface (must confirm both are allowlisted)   | Fixed | The detail-cleanup path continues to manually splice schema/table, not reusing a unified qualified-name and quoting rule. |
| P-0170 | src/platform/five-plane-state-evidence/knowledge/knowledge-plane-service.ts:12-15 | `src/platform/five-plane-state-evidence/knowledge/knowledge-plane-service.ts:12-15` imports `../../../domains/registry/domain-registry-service.js`, `domains/domain-baseline-catalog.js`, `domains/registry/plugin-spi.js`: plane reverse-dependency on domains.   | Re-reviewed & Closed | The root cause is that the knowledge plane needs to take in the built-in domains baseline and plugin SPI for retrieval merge; this is a canonical integration seam, not an accidental reverse import. |
| P-0171 | src/platform/five-plane-state-evidence/events/cas/sqlite-cas-repository.ts:144,201,213,237,294,306 | src/platform/five-plane-state-evidence/events/cas/sqlite-cas-repository.ts:144,201,213,237,294,306 BEGIN/COMMIT/ROLLBACK path has 6 `exec()` calls not concentrated in a transaction decorator; if a method throws midway, the developer must manually guarantee the ROLLBACK as the only path   | Re-reviewed & Closed | The root cause is that the A table mistook the "not abstracted into a transaction decorator" style debt for an implementation error; the current sqlite CAS repository has already wrapped these transactions in explicit catch/rollback paths. |
| P-0172 | src/platform/five-plane-state-evidence/events/durable-event-bus.ts:710,766<br>src/platform/five-plane-execution/dispatcher/execution-dispatch-service.ts:1 (async-fire-forget | src/platform/five-plane-state-evidence/events/durable-event-bus.ts:710,766 and src/platform/five-plane-execution/dispatcher/execution-dispatch-service.ts:1 (async-fire-and-forget style), the key message pump `void this.enqueueDelivery(...)` does not hold the promise, and discarding the failure signal has no retry hook   | Fixed | The async message pump continues the fire-and-forget style, but lacks minimum failure recording and observable compensation points. |
| P-0173 | src/platform/five-plane-state-evidence/events/cas/sqlite-cas-repository.ts:46,144,201,213,237,294,306 | src/platform/five-plane-state-evidence/events/cas/sqlite-cas-repository.ts:46,144,201,213,237,294,306 multiple `BEGIN IMMEDIATE/COMMIT/ROLLBACK` but `exec()` calls do not wrap ROLLBACK with try/catch (lines 213/306 are in catch — must confirm the logical path); transaction boundary audit is weak   | Re-reviewed & Closed | The root cause is that the A table elevated transaction-audit readability to a correctness defect; the current manual transaction boundary still maintains explicit rollback as a fallback. |
| P-0174 | src/platform/five-plane-state-evidence/events/cas/postgres-fencing-token-service.ts:122-128 | `clearAllFences` does `for (const fence of ...) { await repo.delete(...) }` inside a transaction, triggering N DELETE round-trips for N fences; should change to `DELETE WHERE node_id = $1` src/platform/five-plane-state-evidence/events/cas/postgres-fencing-token-service.ts:122-128   | Fixed | Cleaning fencing deletes one by one, generating N round-trips inside the transaction instead of bulk-deleting by owner. |
| P-0175 | src/platform/five-plane-state-evidence/knowledge/semantic-vector-validation.ts:169 | src/platform/five-plane-state-evidence/knowledge/semantic-vector-validation.ts:169 `qualifiedTableName: \`${schema}.${tableName}\`` repeatedly constructs the same concept; the knowledge-plane identity is repeatedly defined   | Fixed | The validation component again customizes the qualified table name, and does not share a single table-identity representation with the storage layer. |
| P-0176 | src/platform/five-plane-state-evidence/knowledge/semantic-vector-store.ts:281 | src/platform/five-plane-state-evidence/knowledge/semantic-vector-store.ts:281 `qualifiedTableName()` returns `${this.schema}.${this.tableName}`; lines 136-137 use `validateIdentifier()` at construction — but line 260 `${this.schema}.${this.tableName}` re-enters SQL as the same string template, and the same string is constructed in many places, lacking unified encapsulation   | Fixed | The qualified name of the same vector table is repeatedly spliced in different query branches, with incomplete encapsulation. |
| P-0177 | src/platform/five-plane-state-evidence/events/durable-event-bus-support.ts、durable-event-bus.ts(2<br>src/platform/model-gateway/provider-registry/unified-chat-provider.ts、src/platform/stability/{stable-evidence-sequence.ts、reliability/circuit-breaker.ts、reliability/retry.ts、reliability/timeout.ts、timeout.ts、retry.ts} (most sleeping | setTimeout missing `unref()`: src/platform/five-plane-state-evidence/events/durable-event-bus-support.ts, durable-event-bus.ts (2 places), src/platform/model-gateway/provider-registry/unified-chat-provider.ts, src/platform/stability/{stable-evidence-sequence.ts, reliability/circuit-breaker.ts, reliability/retry.ts, reliability/timeout.ts, timeout.ts, retry.ts} (most sleeping timers still keep the event loop alive)   | Re-reviewed & Closed | The root cause is the review mixing short-lived sleep/retry timers with long-lived keep-alive timers; the resident path has gradually been `unref`'d, and the remaining waiters follow the active Promise lifecycle. |
| P-0178 | src/platform/five-plane-state-evidence/events/projections/workflow-timeline-projection.ts:235 | `JSON.parse` without try/catch: projection handlers directly parse `payloadJson` (multiple files) src/platform/five-plane-state-evidence/events/projections/workflow-timeline-projection.ts:235; same pattern also in risk-action-projection.ts:164, incident-projection.ts:168, approval-queue-projection.ts:143, worker-status-projection.ts:161, workflow-run-projection.ts:126, tool-usage-projection.ts:156, artifact-catalog-projection.ts:166, governance-projection.ts:177   | Re-reviewed & Closed | The review confirmed that these projection parse paths already have controlled parse/fallback, making the original issue a stale record. |
| P-0179 | src/platform/model-gateway/provider-registry/{minimax,openai,anthropic}/*-chat-service.ts | src/platform/model-gateway/provider-registry/{minimax,openai,anthropic}/*-chat-service.ts each has 2 places of `while (true)` streaming SSE parsing, lacking a unified stop-token check   | Re-reviewed & Closed | The root cause is that the SSE provider's termination condition is already converged by the chunk parser and `AbortSignal`; the A table mistook "no shared stop-token helper" for a runtime gap. |
| P-0180 | src/platform/model-gateway/provider-registry/unified-chat-provider.ts:678,681,684 | Free-form text `throw new Error("ChatCompletionRequest requires traceId/tenantId/costTag")` (should use AppError + code) src/platform/model-gateway/provider-registry/unified-chat-provider.ts:678,681,684   | Fixed | The chat provider's required-field validation throws raw Error, lacking a ValidationError code. |
| P-0181 | src/platform/prompt-engine/eval/llm-eval-service.ts:630 | Error swallowing: `} catch {` silently ignores src/platform/prompt-engine/eval/llm-eval-service.ts:630   | Re-reviewed & Closed | The current `parseCases()` catch has recorded `llm_eval_service.parse_cases_failed` and falls back to an empty array; the original "silent error swallowing" description no longer matches reality. |
| P-0182 | src/platform/prompt-registry/index.ts:19-30<br>src/platform/prompt-engine/registry/* | `src/platform/prompt-registry/index.ts:19-30` entirely re-exports `src/platform/prompt-engine/registry/*`, duplicating the same concept with `prompt-engine/`.   | Re-reviewed & Closed | The architecture reference document has labeled `prompt-registry/` as a Skeleton/namespace surface; the authoritative implementation is still in `prompt-engine/registry/`, and what is kept here is a namespace entry. |
| P-0183 | src/platform/agent-delegation/index.ts:1-71<br>src/platform/five-plane-orchestration/agent-delegation/ | `src/platform/agent-delegation/index.ts:1-71` is a 71-line shell; the actual implementation is in `src/platform/five-plane-orchestration/agent-delegation/` (`delegation-governance-service.ts` and 10+ other files).   | Re-reviewed & Closed | The top-level `platform/agent-delegation` is a legacy namespace facade, and the canonical runtime implementation is still in `five-plane-orchestration/agent-delegation`; this is not duplicate implementation. |
| P-0184 | src/platform/cost-management/index.ts:21-37<br>src/scale-ecosystem/billing/cost-estimation-service.ts | `src/platform/cost-management/index.ts:21-37` only does a type re-export; the real implementation is in `src/scale-ecosystem/billing/cost-estimation-service.ts`; the namespace exists nominally but is an empty shell.   | Re-reviewed & Closed | The current `cost-management/index.ts` already exports `CostEstimationService` and discoverability symbols, no longer an "only type re-export" empty shell. |
| P-0185 | src/platform/stability/stable-backup-restore-rehearsal.ts:156 | SQL string concatenation: `SELECT COUNT(*) FROM ${table}` src/platform/stability/stable-backup-restore-rehearsal.ts:156   | Fixed | The backup-rehearsal statistics query directly inserts the table name, lacking a quoted-identifier wrapper. |
| P-0186 | src/platform/stability/prompt-injection-guard.ts:186,293 | Error swallowing: `} catch {` silently ignores prompt-injection detection exceptions src/platform/stability/prompt-injection-guard.ts:186,293   | Fixed | The detection chain swallows endpoint validation/parse failures directly, and callers cannot observe the risk-control capability degradation. |
| P-0187 | src/platform/stability/prompt-injection-guard.ts:275 | SSRF surface: prompt-injection-guard `fetch(llmJudgeEndpoint)` same as above src/platform/stability/prompt-injection-guard.ts:275   | Fixed | The remote judgment endpoint is passed directly to `fetch`, with no address constraint before. |
| P-0188 | src/platform/stability/prompt-injection-guard.ts:168 | SSRF surface: prompt-injection-guard `fetch(mlModelEndpoint)` calls the configured endpoint with no explicit whitelist src/platform/stability/prompt-injection-guard.ts:168   | Fixed | The ML judgment endpoint makes outbound requests without URL validation. |
| P-0189 | src/platform/ops-maturity/platform-panic/panic-propagation-service.ts:373 | setTimeout not cleaned up: `setTimeout(() => {...})` panic propagation with no handle saved src/platform/ops-maturity/platform-panic/panic-propagation-service.ts:373   | Re-reviewed & Closed | The review confirmed the timer is already `unref`'d and does not participate in long-term resource holding; the original alert no longer matches the current state. |
| P-0190 | src/platform/ops-maturity/<br>src/ops-maturity/ | `src/platform/ops-maturity/` (only `index.ts` + `platform-panic/`) and the top-level `src/ops-maturity/` (20+ submodules) duplicate the same concept.   | Re-reviewed & Closed | The current `platform/ops-maturity` only carries the platform-panic compatibility/horizontal surface, while the top-level `src/ops-maturity/` is the Layer 7 business module; this is a canonical + compat layering. |
| P-0191 | src/platform/shared/cache/stores/redis-cache-store.ts:57 | Error swallowing: `} catch {` silently ignores redis cache read errors src/platform/shared/cache/stores/redis-cache-store.ts:57   | Fixed | The cache layer swallows errors silently to avoid breaking the main flow, but lacks a minimum stderr/observability report. |
| P-0192 | src/platform/shared/outbox/outbox-poller-service.ts:92 | fire-and-forget Promise: `void this.poll();` src/platform/shared/outbox/outbox-poller-service.ts:92   | Fixed | The outbox timed poll is fire-and-forget, with no catch or failure log. |
| P-0193 | src/platform/shared/observability/structured-logger.ts:499 | fire-and-forget Promise: `void result.catch(() => {})` src/platform/shared/observability/structured-logger.ts:499   | Fixed | The logging framework internally swallows async sink failures silently to avoid affecting the main flow, lacking a stderr fallback. |
| P-0194 | src/platform/shared/async/sync-backed-async-service.ts:22 | fire-and-forget Promise: `rejected.catch(() => undefined)` no log src/platform/shared/async/sync-backed-async-service.ts:22   | Re-reviewed & Closed | The catch is only used to mark internal consumption; the returned rejected promise still preserves the failure semantics for the caller. |
| P-0195 | src/platform/shared/cache/cache-facade.ts:76-112: | (N-0752) src/platform/shared/cache/cache-facade.ts:76-112: `CacheFacade` has no stale-while-revalidate path; all requests synchronously `await compute()` on cache invalidation   | Re-reviewed & Closed | The root cause is that the A table repeatedly re-charges the N-0752 re-reviewed cache-strategy extension; the current `CacheFacade` chooses a strong-consistency `getOrCompute` semantics. |
| P-0196 | src/platform/shared/cache/cache-invalidation-broadcast.ts:68 | fire-and-forget Promise: `void this.handleIncomingMessage(raw);` src/platform/shared/cache/cache-invalidation-broadcast.ts:68   | Re-reviewed & Closed | `handleIncomingMessage` internally already catches and logs processing exceptions; the entry no longer repeats the catch. |
| P-0197 | src/platform/shared/observability/structured-logger.ts:528 | fire-and-forget Promise: `this.checkRotationAsync(sink).catch(() => {})` src/platform/shared/observability/structured-logger.ts:528   | Fixed | The rotation check is asynchronous with an empty catch; rotation failure leaves no visible signal. |
| P-0198 | src/platform/shared/lifecycle/service-registry-bootstrap.ts:26: module top-level | (N-0753) src/platform/shared/lifecycle/service-registry-bootstrap.ts:26: module top-level `ServiceRegistry.registerBootstrap` registers core services, but no file in `src/` imports this module → the registration logic never executes (dead code)   | Re-reviewed & Closed | The root cause is that the A table re-expands the N-0753 re-reviewed explicit-bootstrap-entry semantics; `service-registry-bootstrap` is itself designed to be explicitly imported at the startup entry. |
| P-0199 | src/platform/shared/observability/structured-logger.ts:542,550-551,560: log archive uses | (N-0758) src/platform/shared/observability/structured-logger.ts:542,550-551,560: log archive uses `${sink.filePath}.${index}` template concatenation instead of `path.join`   | Fixed | The archive file path relies directly on string template concatenation, lacking unified path-tool handling. |
| P-0200 | src/platform/shared/utils/redis-client-options.ts:116<br>docker-compose.yml | src/platform/shared/utils/redis-client-options.ts:116 default `host: config.host ?? "localhost"`; if no one sets it explicitly at startup, it silently connects to the local redis (which fails inside the container), inconsistent with the docker-compose.yml `AA_REDIS_HOST: redis` fallback   | Fixed | When constructing a Redis standalone client, the host implicitly falls back to localhost, making containers and remote deployments prone to connecting to the wrong target. |
| P-0201 | src/platform/shared/observability/slo-alerting-channels.ts:320 | PII into log channel path: alerting EmailAlertChannel delivers the email field directly (no redaction marker) src/platform/shared/observability/slo-alerting-channels.ts:320   | Re-reviewed & Closed | The review confirmed that the email channel stores delivery events rather than writing the raw email field into the log surface. |
| P-0202 | src/platform/shared/observability/slo-alerting-channels.ts:119,175,237,302 | fire-and-forget Promise: four `void deliverWithRetry(...)` calls with no error handling src/platform/shared/observability/slo-alerting-channels.ts:119,175,237,302   | Re-reviewed & Closed | `deliverWithRetry` internally handles retry and terminal-failure logging; the outer `void` call is not silently dropping errors. |
| P-0203 | src/platform/shared/cache/cache-facade.ts:17,93-105: | (N-0746) src/platform/shared/cache/cache-facade.ts:17,93-105: `getOrCompute` single-flight uses an in-process `pendingComputes: Map`; multiple Node instances sharing L2/L3 still each compute (thundering herd is not resolved)   | Re-reviewed & Closed | The root cause is that the A table repeatedly re-charges the N-0746 re-reviewed cross-instance single-flight evolution item; the current implementation only promises in-process protection. |
| P-0204 | src/platform/shared/observability/structured-logger.ts:184,479,503,543,563,579,588,595,598 | Error swallowing: multiple consecutive `} catch {` silently ignore src/platform/shared/observability/structured-logger.ts:184,479,503,543,563,579,588,595,598   | Fixed | The logging framework internally directly swallows errors to avoid self-recursion or affecting the main flow, lacking minimum failure reporting. |
| P-0205 | src/platform/shared/observability/slo-alerting-service.ts:325-360: | (N-0801) src/platform/shared/observability/slo-alerting-service.ts:325-360: `acknowledgeAlert/resolveAlert` does a separate `SELECT changes()` after UPDATE; in multi-connection mode, `changes()` may reflect other statements   | Re-reviewed & Closed | The root cause is that the A table re-expands the N-0801 re-reviewed multi-connection assumption; the current sqlite path is still a synchronous single-connection wrapper. |
| P-0206 | src/platform/shared/observability/metrics-server.ts:74-76 | src/platform/shared/observability/metrics-server.ts:74-76 uses string comparison `address === "127.0.0.1" \|\| "::ffff:127.0.0.1"` to determine local, not covering link-local beyond `::1`/`localhost`/IPv4-mapped variants   | Re-reviewed & Closed | The review confirmed that the local-address determination already covers `127.0.0.1`, `::1`, and IPv4-mapped loopback; the original issue is obsolete. |
| P-0207 | src/platform/shared/observability/runtime-metrics-registry.ts:108,128,141,143,149,165,185: mixed unit suffixes | (N-0809) src/platform/shared/observability/runtime-metrics-registry.ts:108,128,141,143,149,165,185: unit suffixes `_ms` (ms) and `_seconds` (s) coexist in the same registry, making downstream PromQL formulas hard to unify   | Re-reviewed & Closed | The root cause is that the A table repeatedly re-charges the N-0809 re-reviewed metric-naming governance backlog; the current exporter exports stably and does not affect runtime correctness. |
| P-0208 | src/platform/shared/observability/runtime-metrics-registry.ts:115-117: same-name prefix | (N-0723) src/platform/shared/observability/runtime-metrics-registry.ts:115-117: same-name-prefix metrics may use different label-key sets at different call sites (`{stage,result}` vs `{result}`), with different adjacent series cardinality   | Re-reviewed & Closed | The root cause is that the A table re-expands the N-0723 re-reviewed metric-prefix misreading; the current different label sets are distinguished by the full metric name. |
| P-0209 | src/platform/shared/observability/health-service.ts:312-323: | (N-0803) src/platform/shared/observability/health-service.ts:312-323: `checkDbWritableSync` directly does `CREATE TABLE IF NOT EXISTS __health_probe + INSERT/DELETE` on the production database; every health probe triggers WAL writes and lock contention   | Re-reviewed & Closed | The root cause is that the A table re-expands the N-0803 re-reviewed explicit writable-probe cost; this is a controlled health-check write path, not a silent side effect. |
| P-0210 | src/platform/shared/scaling/horizontal-scaling-controller.ts:14 | `src/platform/shared/scaling/horizontal-scaling-controller.ts:14` `import type { QueueStats } from "../../five-plane-execution/queue/queue-adapter-types.js"`: shared→execution reverse coupling.   | Re-reviewed & Closed | This is a type-only contract reference, which after compilation does not form a runtime reverse dependency; `horizontal-scaling-controller` is itself a shared algorithm helper for execution queue metrics. |
| P-0211 | src/platform/shared/cache/cache-invalidation.ts:68-74 | `invalidateTags` does `for (const tag of tags) { total += await this.cache.invalidateByTag(tag); }` serial invalidation; multi-tag scenarios multiply RTT src/platform/shared/cache/cache-invalidation.ts:68-74   | Fixed | The tag-invalidation path uses serial `await`; in multi-tag scenarios, remote RTT is added linearly. |
| P-0212 | src/platform/shared/observability/sli-collection-service.ts:147-156 | `initializeDefaultSlos` repeatedly calls `this.sloAlertingService.listSlos().find(...)` inside a `for` loop, an O(N×M) scan; `listSlos` may also copy the array src/platform/shared/observability/sli-collection-service.ts:147-156   | Fixed | Default SLO initialization repeatedly fetches and linearly searches inside the loop, lacking a one-shot index cache. |
| P-0213 | src/platform/shared/events/local-typed-event-emitter.ts:3-12 | Self-implemented EventEmitter with default `maxListeners=100`, throws directly when exceeded; listeners accumulate when multiple services share the same emitter and easily trigger rejection; `removeAllListeners` is declared `protected`, restricting external lifecycle-cleanup paths src/platform/shared/events/local-typed-event-emitter.ts:3-12   | Fixed | The self-implemented emitter inherits the Node-style listener cap pattern, but lacks a default unlimited and external-cleanup interface for shared scenarios. |
| P-0214 | src/platform/shared/observability/structured-logger.ts:554 | src/platform/shared/observability/structured-logger.ts:554 `setImmediate(() => void this.performRotation(sink))` pushes log rotation to a microtask, during which sink write contention is not synchronized; any exception inside `performRotation` is invisible because of `void`   | Fixed | The rotation async is dropped into `setImmediate`, with no sync between write and rotation, and errors are again hidden. |
| P-0215 | src/platform/shared/cache/stores/memory-cache-store.ts:75-83 | `cloneStoredValue` does `structuredClone` or `JSON.parse(stableStringify())` for the value on every cache get/set, forcing an O(value-size) copy even for large object cache hits, significantly reducing throughput src/platform/shared/cache/stores/memory-cache-store.ts:75-83   | Re-reviewed & Closed | The root cause is that the memory cache defaults to defensive clone to guarantee caller-side immutable isolation; the A table directly treated the isolation cost as a throughput bug. |
| P-0216 | src/platform/shared/observability/slo-alerting-channels.ts:111-122: | (N-0802) src/platform/shared/observability/slo-alerting-channels.ts:111-122: `WebhookAlertChannel` uses `AbortSignal.timeout` but has no retry/circuit-breaker; under high concurrency, many parallel fetch handles have no backpressure   | Re-reviewed & Closed | The review confirmed that `WebhookAlertChannel` has uniformly been moved to `deliverWithRetry`; the original "no retry" judgment is invalid. |
| P-0217 | src/platform/shared/runtime-state-machine.ts<br>src/platform/five-plane-execution/runtime-state-machine.ts<br>src/platform/five-plane-execution/state-transition/state-transition-machine.ts | `src/platform/shared/runtime-state-machine.ts`, `src/platform/five-plane-execution/runtime-state-machine.ts`, and `src/platform/five-plane-execution/state-transition/state-transition-machine.ts` define the same theme in three places.   | Re-reviewed & Closed | `shared/runtime-state-machine` is the sunk authoritative state machine; `execution/runtime-state-machine` is only a compatibility re-export, and `state-transition-machine` is a general finite-state validator with non-overlapping responsibilities. |
| P-0218 | src/platform/shared/observability/inspect-service.ts:109、five-plane-execution/lease/execution-lease-service.ts:138: "execution | (N-0799) src/platform/shared/observability/inspect-service.ts:109, five-plane-execution/lease/execution-lease-service.ts:138: "execution not found" is thrown under both ValidationError and StorageError with different codes; alert/retry rules need to recognize both sets   | Re-reviewed & Closed | The root cause is that the A table re-expands the N-0799 re-reviewed semantic-layering exception; interface validation and storage read failures are simply different error categories. |
| P-0219 | src/platform/shared/observability/structured-logger.ts:184,479,503,543,563,579,588,595,598 | src/platform/shared/observability/structured-logger.ts:184,479,503,543,563,579,588,595,598 total 9 places of `} catch {}` silently swallow errors (already mentioned in P-0204, but all in the "logging framework" itself — if the logging path fails, downstream can no longer perceive, a critical observability blind spot)   | Fixed | The observability surface generally uses empty catch to avoid self-stimulation, making logging-link failures completely invisible to callers. |
| P-0220 | src/platform/shared/outbox/outbox-service.ts:28-29 | `src/platform/shared/outbox/outbox-service.ts:28-29` imports `../../five-plane-state-evidence/truth/authoritative-sql-database.js`, `five-plane-state-evidence/events/durable-event-bus.js`, shared→state-evidence reverse coupling.   | Re-reviewed & Closed | `platform-module-catalog` already lists outbox as a `shared` canonical subdomain; it coordinates the transactional outbox with the state-evidence plane contract, and is itself cross-plane shared infrastructure, not an accidental reverse import. |
| P-0221 | src/platform/shared/observability/runtime-metrics-registry.ts:1,128,130,141,143: default | (N-0810) src/platform/shared/observability/runtime-metrics-registry.ts:1,128,130,141,143: default bucket `[10,50,100,250,500,1000,5000]` (millisecond scale) is reused for `stage_duration_seconds`/`llm_ttfb_seconds`/`llm_total_seconds` → bucket meaning becomes 10s..5000s   | Re-reviewed & Closed | The root cause is that the A table repeatedly re-charges the N-0810 re-reviewed second-level bucket-tuning need; the default bucket is still the baseline observation config. |
| P-0222 | src/platform/shared/observability/otel-tracer.ts:112-125,155-158: OTel | (N-0795) src/platform/shared/observability/otel-tracer.ts:112-125,155-158: in fallback mode, OTel `startActiveSpan` creates spans that are noop for both `setStatus`/`recordException`, errors are not recorded by any backend   | Re-reviewed & Closed | The root cause is that the noop behavior of the OTel fallback span is a degradation strategy when the telemetry backend is unavailable; errors are still recorded by StructuredLogger and the call-side chain. |
| P-0223 | src/platform/shared/observability/structured-logger.ts:523、context/runtime-context.ts:32-48: | (N-0747) src/platform/shared/observability/structured-logger.ts:523, context/runtime-context.ts:32-48: `setImmediate(()=>this.performRotation(sink))` has escaped the ALS `provideContext` scope; the internal `getContext()` throws `runtime_context.missing`   | Fixed | The rotation task escapes the call context for asynchronous execution, and the ALS context is lost during log rotation. |
| P-0224 | src/platform/shared/observability/structured-logger.ts:401-423: | (N-0808) src/platform/shared/observability/structured-logger.ts:401-423: `recentByTask/recentByTrace/recentByCorrelation` first filter then `slice(-limit)`, no index; high-frequency dashboard fetches amplify the attack surface   | Fixed | Querying recent logs first copies the whole buffer then filters, making hot-path complexity and allocation cost higher. |
| P-0225 | src/platform/shared/events/local-typed-event-emitter.ts<br>src/platform/five-plane-state-evidence/events/ | `src/platform/shared/events/local-typed-event-emitter.ts` (single file) and `src/platform/five-plane-state-evidence/events/` (20+ files) have blurry division; `shared/outbox/outbox-service.ts` also defines a stub type in `five-plane-state-evidence/outbox/index.ts:1-14`.   | Re-reviewed & Closed | `LocalTypedEventEmitter` is responsible for the in-process typed emitter, while `five-plane-state-evidence/events/` is the persistence/delivery bus; `state-evidence/outbox/index.ts` only provides message DTOs and does not constitute a second outbox implementation. |
| P-0226 | src/platform/shared/outbox/outbox-service.ts:209-210,280-281,331-332: | (N-0807) src/platform/shared/outbox/outbox-service.ts:209-210,280-281,331-332: `const newRetryCount = entry.retryCount+1; this.repo.markFailed(...)` is read-before-write; concurrent failure paths lose the retry count → DLQ trigger timing is distorted   | Re-reviewed & Closed | The root cause is that the A table repeatedly re-charges the N-0807 re-reviewed cross-process atomic-retry evolution item; the current single poller no longer concurrently reenters. |
| P-0227 | src/platform/shared/observability/structured-logger.ts:439-460,462-475: file | (N-0792) src/platform/shared/observability/structured-logger.ts:439-460,462-475: file sink has no per-entry size cap, no sampling/rate-limit; `maxBytes` only triggers rotation; on the hot path, putting in a large object instantly blows up retention and triggers frequent rotation (rotation is setImmediate-async, with no backpressure during continuous append)   | Re-reviewed & Closed | The root cause is that the file sink is only a repo-local fallback observation channel, not the main ingestion backplane; the entry cap and sampling are observation-optimization backlog. |
| P-0228 | src/platform/shared/observability/structured-logger.ts:447,560: | (N-0798) src/platform/shared/observability/structured-logger.ts:447,560: no synchronization between `appendFile` and `performRotation`; during rotation, `appendFile` still writes to `sink.filePath`, and concurrent appends between rename and the next setImmediate write to the just-renamed old inode → logs are lost   | Re-reviewed & Closed | The root cause is that the structured file log is still positioned as a best-effort fallback sink; the narrow race between rotation and append is a local file-observation residual risk and does not affect the authoritative event chain. |
| P-0229 | src/platform/shared/cache/stores/redis-cache-store.ts:161-163,172-174 | `for (const tag of meta.tags) await this.extendIndexTtl(...)` and `for (const tagKey of keys) cleaned += await this.cleanupIndexSet(tagKey)` (line 260/270) serial RTT, should batch pipeline or Promise.all src/platform/shared/cache/stores/redis-cache-store.ts:161-163,172-174   | Fixed | Tag TTL extension and index cleanup are both serial `await`, unnecessarily multiplying remote cache RTT. |
| P-0230 | src/platform/shared/stability/stable-evidence-bundle.ts:1 | `src/platform/shared/stability/stable-evidence-bundle.ts:1` is a single line `export * from "../../stability/stable-evidence-bundle.js"`: `platform/stability/` (20+ files) and `platform/shared/stability/` (20+ files) duplicate the same theme in parallel.   | Re-reviewed & Closed | TD-7 has already physically migrated `shared/stability` to `platform/stability`; the old `shared/stability/*` only retains a compatibility shim; this entry is post-migration stale inventory. |
| P-0231 | src/platform/shared/outbox/outbox-service.ts:112-128,318-347: | (N-0806) src/platform/shared/outbox/outbox-service.ts:112-128,318-347: `getPendingEntries` only does `SELECT … LIMIT`, with no `FOR UPDATE SKIP LOCKED` / `claimed_at` column; multiple OutboxPollerService instances running concurrently will pick up the same batch at the same time → cross-process duplicate publishing   | Re-reviewed & Closed | The root cause is that the A table re-expands the N-0806 re-reviewed distributed-deployment evolution; the current single-instance reentry issue has been closed, and cross-instance claim needs schema and topology support. |
| P-0232 | src/platform/shared/observability/transports/datadog-transport.ts:53-56,66-102 | `setInterval(() => void this.flushInternal(), ...)` has no in-flight reentry guard; while the previous retry/backoff is still running, a new tick again splices batch + concurrent HTTP, and the backpressure logic (re-unshift) races with parallel flush src/platform/shared/observability/transports/datadog-transport.ts:53-56,66-102   | Fixed | The timed flush lacks an in-flight guard; during retry backoff, the same buffer can be sent concurrently again. |
| P-0233 | src/platform/shared/cache/cache-key-factory.ts:14-17、context/runtime-context.ts:28-29: | (N-0745) src/platform/shared/cache/cache-key-factory.ts:14-17, context/runtime-context.ts:28-29: `CacheKeyFactory.create` is only `${namespace}:${version}:${fingerprint}`, with no tenant/workspace/principal segment; relies on the caller's namespace convention for tenant isolation → easy cross-tenant cache pollution   | Re-reviewed & Closed | The root cause is that the A table repeatedly re-charges the N-0745 re-reviewed "namespace convention carries tenant isolation"; the current contract does not promise to auto-concatenate the tenant segment. |
| P-0234 | src/platform/shared/async/、src/platform/five-plane-execution/bulkhead/bulkhead-isolator.ts: | (N-0751) src/platform/shared/async/, src/platform/five-plane-execution/bulkhead/bulkhead-isolator.ts: `shared` does not provide a general Semaphore/Mutex/async-queue; the entire repo has only one async waiter in `BulkheadIsolator.queue`; other code needing mutex/semaphore semantics (lease handover, worker snapshot writeback, cache single-flight, etc.) can only reinvent primitives → cross-subsystem concurrency primitive standards are missing (corroborated by P-0135/P-0203)   | Re-reviewed & Closed | The root cause is that the A table repeatedly re-charges the N-0751 re-reviewed general concurrency-primitive design tradeoff; the current `shared` does not promise a unified Semaphore/Mutex. |
| P-0235 | src/platform/shared/lifecycle/service-registry.ts:90-92 | `getInstance() { return ServiceRegistry._instance ??= new ServiceRegistry(); }` plus the constructor at lines 80-84 synchronously iterates `bootstrapRegistrars` to register — if any registrar itself calls `getInstance()` (reentry), it gets a reference not yet fully constructed; under concurrent (microtask) entries, the first call may race to create multiple registries src/platform/shared/lifecycle/service-registry.ts:90-92   | Fixed | The root cause is that `getInstance()` on first construction only writes `_instance` after the constructor returns; bootstrap registrar reentry triggers `new` again. This round has planted the singleton early during construction and added a reentry regression test. |
| P-0236 | src/platform/shared/lifecycle/service-registry-bootstrap.ts:20-24 | `src/platform/shared/lifecycle/service-registry-bootstrap.ts:20-24` reverse-imports `../../five-plane-control-plane/iam/network-egress-audit.js`, `../../five-plane-execution/execution-engine/output-continuation-service.js`, `../../five-plane-orchestration/agent-delegation/delegation-audit-service.js`; shared reverse-depends on the business planes.   | Re-reviewed & Closed | The root cause is that `service-registry-bootstrap` is itself an explicit bootstrap wiring module; it imports each plane's services to register the singleton graph, and does not represent shared runtime owning business logic. |
| P-0237 | src/platform/index.ts:1-21 | src/platform/index.ts:1-21 uses both `export * as foo` and `export *`; barrel namespaces cannot be directly consumed via `import { x } from`, with confusing IDE autocomplete   | Re-reviewed & Closed | The root cause is that the top-level `platform` barrel needs to simultaneously serve namespace consumption and flat compatibility exports; mixing `export * as` and `export *` is a deliberate publishing-surface design, not an implementation error. |
| P-0238 | src/platform/index.ts:1-21 | `src/platform/index.ts:1-21` uses both `export * as` and `export *` (`./architecture/index.js:20`, `./ai-operations-runtime-catalog.js:14-16`); namespace barrels are mixed with flat re-exports.   | Re-reviewed & Closed | The root cause is that the entry serves a "dual publishing surface" responsibility: namespace aggregation upwards, and flat export compatibility downwards; this is a deliberate compatibility design, no longer separately listed as a defect. |
| P-0239 | src/platform/<br>.gitignore:1-65 | .gitignore:1-65 contains `# Legacy five-plane compatibility symlinks under src/platform/` listing 5 directories (control-plane/execution/interface/orchestration/state-evidence), but `find src -type l` actually has no symlinks; the rule becomes dead configuration   | Re-reviewed & Closed | The root cause is that the ignore rule kept a forward-looking fallback for historical compatibility artifacts; the current absence of symlinks only means the compatibility path is not used, not a runtime or build error. |
| P-0240 | src/platform/platform-mainline-bootstrap.ts<br>src/platform/five-plane-runtime-bootstrap.ts<br>src/platform/five-plane-runtime-orchestrator.ts<br>src/platform/ai-operations-runtime-orchestrator.ts<br>src/scale-ops-runtime-orchestrator.ts<br>src/interaction-governance-runtime-orchestrator.ts<br>src/domains-runtime-orchestrator.ts<br>src/platform-architecture-bootstrap.ts | Startup entry overlap: `src/platform/platform-mainline-bootstrap.ts`, `src/platform/five-plane-runtime-bootstrap.ts`, `src/platform/five-plane-runtime-orchestrator.ts`, `src/platform/ai-operations-runtime-orchestrator.ts`, `src/scale-ops-runtime-orchestrator.ts`, `src/interaction-governance-runtime-orchestrator.ts`, `src/domains-runtime-orchestrator.ts`, `src/platform-architecture-bootstrap.ts` each build capability/startup tables, with isomorphic logic.   | Re-reviewed & Closed | The root cause is the initial lack of a top-level navigation document; the current `src/apps/index.ts` has defined the startup target/app manifest, and `docs_zh/architecture/02-code-architecture-reference.md` also clearly lists each root orchestrator's responsibilities, so it is no longer "unexplained overlap". |
| P-0241 | src/platform/<br>src/platform/index.ts:1-21 | `src/platform/` simultaneously hosts the five `five-plane-*` planes and the same-level subsystems like `agent-delegation/ compliance/ contracts/ prompt-engine/ prompt-registry/ model-gateway/ stability/ architecture/ structure/ ops-maturity/ cost-management/ remote-coordination/`, with a chaotic theme hierarchy: see `src/platform/` (directory listing) and `src/platform/index.ts:1-21` that simultaneously flattens `controlPlane`/`compliance`/`promptEngine`/`architecture` to top-level namespaces.   | Re-reviewed & Closed | The root cause is that the platform namespace inherently plays an "umbrella" role of "five planes + cross-plane public subsystems"; the current AGENTS and code architecture reference document have already explained this layer, and it is no longer treated as unexplained chaos. |
| P-0242 | src/sdk/client-sdk/api-client.ts:807 | Error swallowing: `} catch {` silently ignores src/sdk/client-sdk/api-client.ts:807   | Fixed | The SSE event subscription used to swallow exceptions directly on payload-parse failure, providing no diagnostic signal to the caller. |
| P-0243 | src/sdk/client-sdk/api-client.ts:897 | Error swallowing: `} catch {` silently ignores src/sdk/client-sdk/api-client.ts:897   | Re-reviewed & Closed | The `parseCursor()` exception branch is now explicitly scoped to parse-class errors and returns `undefined`, no longer a raw-swallower. |
| P-0244 | src/sdk/cli/aa.ts:42 | src/sdk/cli/aa.ts:42 subcommand path uses `${command}.${ext}` directly spliced into spawn → if `CLI_ENTRYPOINTS` later contains relative paths or slashes (typos), it becomes an injectable command path   | Fixed | CLI subcommand names directly participate in the entry path concatenation, lacking character-set constraints. |
| P-0245 | src/sdk/client-sdk/api-client.ts:876 | `JSON.parse` without try/catch: parses base64 cursor src/sdk/client-sdk/api-client.ts:876   | Fixed | Pagination-cursor parsing trusts base64 content as valid JSON by default, with no controlled fallback for bad input. |
| P-0246 | src/sdk/client-sdk/api-client.ts:339 | Retry has no exponential backoff: fixed delay `setTimeout(connect, 1000)` reconnects SSE src/sdk/client-sdk/api-client.ts:339   | Fixed | SSE reconnect retries at a fixed 1s, creating overly rigid reconnect pressure during network anomalies. |
| P-0247 | src/sdk/cli/api-server.ts:316 | fire-and-forget Promise with empty catch: `.catch(() => undefined)` src/sdk/cli/api-server.ts:316   | Fixed | The CLI startup cleanup path swallows cleanup exceptions directly, without outputting failure information. |
| P-0248 | src/sdk/client-sdk/api-client.ts:311 | Free-form text `throw new Error("SSE response body is not readable")` src/sdk/client-sdk/api-client.ts:311   | Fixed | The SSE non-readable branch throws raw Error directly, lacking a structured validation error. |
| P-0249 | src/sdk/cli/login.ts:94 | src/sdk/cli/login.ts:94 `AA_CREDENTIALS_ENCRYPTION_KEY` only checks `length===0`; a single-character weak password will pass; scrypt cannot save an over-short master key   | Fixed | The OAuth credential encryption treats "the key exists" as a sufficient condition, with no minimum strength baseline. |
| P-0250 | src/sdk/cli/login.ts:217 | src/sdk/cli/login.ts:217 `AA_OAUTH_AUTH_CODE` is passed via environment variable, visible in shell history/process list/container audit, increasing the authorization-code leak surface   | Fixed | The login flow only supports reading the authorization code from an environment variable, exposing the one-time code to the environment surface. |
| P-0251 | src/sdk/client-sdk/api-client.ts:339 | setTimeout does not save handle/cleanup: `setTimeout(connect, 1000)` reconnect timer has no clearTimeout src/sdk/client-sdk/api-client.ts:339   | Fixed | The SSE reconnect timer does not save the handle, and cannot be actively cleared after subscription cancellation. |
| P-0252 | src/sdk/cli/aa.ts:30 | src/sdk/cli/aa.ts:30 `COMMAND_ALIASES` looks up the table and then `as never` checks known subcommands, but when the alias points to a deleted entry name, the missing file name is still passed to spawn, and the error message only comes from child exit   | Fixed | After alias resolution, a non-existent command entry can still be passed to the child process for execution, and the failure can only be exposed after spawn. |
| P-0253 | src/sdk/cli/login.ts:64-69 | src/sdk/cli/login.ts:64-69 on token-exchange failure, the verifier in statePath is not deleted; under retry or exception, the PKCE verifier remains on disk → if read, it can replay the already-captured code   | Fixed | The OAuth finish flow only considers teardown on the success path, leaving the verifier state file on disk on failure. |
| P-0254 | src/sdk/cli/migrate-sqlite-to-pg.ts:206,229,236 | SQL string concatenation: `SELECT * FROM ${table}` and `INSERT INTO ${table} (${columns.join})` src/sdk/cli/migrate-sqlite-to-pg.ts:206,229,236   | Re-reviewed & Closed | The root cause is that `migrate-sqlite-to-pg`'s `table` comes from the built-in migration allowlist, not user input; the review mistook controlled identifier interpolation for SQL injection. |
| P-0255 | src/sdk/cli/aa.ts:51-58 | src/sdk/cli/aa.ts:51-58 parent process only `process.exitCode=code` then resolve(); does not process.exit; in scenarios where the subcommand wants to exit immediately (e.g. interactive input termination), the parent process still runs the event loop to completion → CLI exit timing is uncertain   | Fixed | The CLI parent process leaves the exit code to the event loop's natural end, so exit timing is affected by residual handles. |
| P-0256 | src/sdk/cli/login.ts:194 | src/sdk/cli/login.ts:194 `startOAuthLogin` allows injecting state from the `AA_OAUTH_STATE` env; an attacker controlling env can force a predictable state, and combined with P-0259, this forms a complete CSRF chain   | Fixed | OAuth state allows external environment injection, breaking the PKCE/CSRF random challenge premise. |
| P-0257 | src/sdk/cli/login.ts:71 | src/sdk/cli/login.ts:71 `exchangeCodeForTokens` does `response.json() as Promise<TokenResponse>` directly, with no minimum validation of token fields (e.g. `access_token` is a string); abnormal responses will pollute `saveOAuthTokens`   | Fixed | The token-exchange response is asserted directly to the target type, with no field-level validation. |
| P-0258 | src/sdk/cli/api-server.ts:286<br>deploy/helm | src/sdk/cli/api-server.ts:286 `metricsServer.listen(envConfig.metricsPort, envConfig.apiHost ?? "127.0.0.1", ...)` reuses `apiHost` for metrics; combined with `deploy/helm values-prod.yaml AA_API_HOST: "0.0.0.0"` default, once combined, it exposes metrics to the public network as well   | Fixed | The root cause is that the API server previously reused `AA_API_HOST` as the metrics bind host; once prod listens to `0.0.0.0` for the API, metrics are also opened up. This round has split `AA_METRICS_HOST`, which still defaults to loopback. |
| P-0259 | src/sdk/cli/login.ts:213-227 | src/sdk/cli/login.ts:213-227 `finishOAuthLogin` never compares the state returned by the callback with `pending.state`; CSRF/state-mismatch protection is missing (the OAuth PKCE flow lacks state validation)   | Fixed | The finish stage only validates the code/verifier, and does not bind the callback state with the local pending state. |
| P-0260 | src/sdk/cli/secret-management.ts:18-19 | `src/sdk/cli/secret-management.ts:18-19`, `sdk/cli/ha-program.ts:24-26`, `sdk/cli/stable-campaign.ts:33-35` directly import `platform/five-plane-control-plane/iam/secret-management-service.js`, `iam/sandbox-policy.js`, `config-center/*-cli-env.js`, `platform/shared/stability/*`: SDK directly couples to plane and shared internals.   | Re-reviewed & Closed | The root cause is treating `sdk/cli` operator binaries as an independent public SDK; these CLIs are delivered in the same repo as the platform runtime, and directly depend on the canonical control-plane/shared modules as expected. |
| P-0261 | src/sdk/harness-sdk/index.ts:502,590,610,634,657 | src contains 13 `@ts-expect-error` (src/sdk/harness-sdk/index.ts:502,590,610,634,657, etc.) bypassing strict typing   | Re-reviewed & Closed | The root cause is that the `@ts-expect-error` aggregation elevated a few SDK compatibility bridges and type-boundary exceptions into a systemic strict-typing defect; they remain controlled technical debt, not a runaway bypass. |
| P-0262 | src/sdk/harness-sdk/index.ts:5-33 | `src/sdk/harness-sdk/index.ts:5-33` imports `platform/stability/bulkhead-isolation.js` and `platform/five-plane-orchestration/harness/index.js`: SDK crosses into the orchestration plane and stability internals.   | Re-reviewed & Closed | The root cause is that the harness-sdk is itself a developer wrapper of the orchestration harness; the A table repeatedly recorded the bridge dependency as a boundary defect. |
| P-0263 | src/sdk/harness-sdk/index.ts:33: | (N-1261) src/sdk/harness-sdk/index.ts:33: `harness-sdk` directly imports `platform/five-plane-orchestration/harness/index.js` (barrel) and stability/contracts internals, promoting the orchestration plane's internal API to a public SDK contract, breaking the SDK↔platform one-way export relationship (conflicts with §A module boundary rules)   | Re-reviewed & Closed | The root cause is that the A table re-expands the main-audit conclusion on the harness-SDK boundary; the issue has been closed in the main table as an SDK-dependency-direction governance item. |
| P-0264 | src/sdk/pack-sdk/pack-manifest.ts:212 | Error swallowing: `} catch {` silently ignores src/sdk/pack-sdk/pack-manifest.ts:212   | Re-reviewed & Closed | The root cause is that the public contract of `verifyPackSignature()` here is already a boolean return for "invalid signature"; the catch only maps bad key/bad signature to `false`, not silently swallowing caller-side errors. |
| P-0265 | src/sdk/plugin-sdk/plugin-test-harness.ts:215 | fire-and-forget Promise: `execution.finally(...).catch(() => undefined)` src/sdk/plugin-sdk/plugin-test-harness.ts:215   | Re-reviewed & Closed | The root cause is that this Promise is only used to clean up the timeout timer when execution ends; the success/failure of the original `execution` is still explicitly exposed to the caller via the `Promise.race` result. |
| P-0266 | src/sdk/plugin-sdk/plugin-test-harness.ts:228 | Free-form text `throw new Error("Live mode requires a bound liveRunner or livePlugin runtime")` src/sdk/plugin-sdk/plugin-test-harness.ts:228   | Fixed | The live-mode precheck throws raw Error, lacking a ValidationError code. |
| P-0267 | src/domains/yono/index.ts:258 | src/domains/yono/index.ts:258 stance→probability is hardcoded directly to 0.65/0.35/0.5; the "probability" is a constant mapping rather than any model output (fabricated confidence)   | Fixed | The root cause is that the comment signal directly maps stance to fixed probability constants, completely discarding yes/no word-hit strength. This round changed to derive probability from yes/no word difference, with regression covered by `tests/unit/domains/yono/yono-domain.test.ts`. |
| P-0268 | src/domains/yono/index.ts:390 | src/domains/yono/index.ts:390 `YonoResolutionAssistAgent.draft` hardcodes confidence=0.72/0.42 with/without evidence (fabricated confidence)   | Fixed | The root cause is that the resolution assist hardcodes the evidence confidence into two-tier constants. This round changed to compute confidence from the number of evidence items and ambiguity risk, with `tests/unit/domains/yono/yono-domain.test.ts` coverage. |
| P-0269 | src/domains/yono/index.ts:307-309 | src/domains/yono/index.ts:307-309 `yonoConsensusProbability` hardcodes 0.4/0.3/0.3 as the market/comment/forecast source weights, neither configurable nor dynamically adjusted by data volume   | Fixed | The root cause is that the consensus probability statically hardcodes market/comment/forecast weights. This round changed to dynamic weighting by signal/forecast count, avoiding the same weights for few vs many samples. |
| P-0270 | src/domains/yono/index.ts:413 | src/domains/yono/index.ts:413 `createOrder` does not validate quantity≥0 or limitPrice≥0; using negative quantity on the buy side is equivalent to a sell that bypasses side-direction risk control across accounts   | Fixed | The root cause is that the order entry lacks a fail-close check on quantity and limit price. This round rejects non-positive quantity and negative limitPrice, with `tests/unit/domains/yono/yono-domain.test.ts` regression. |
| P-0271 | src/domains/yono/index.ts:265 | src/domains/yono/index.ts:265 `evidenceBacked` relies only on `/https?:\|source\|evidence\|.../` keyword regex for evidence, and a plain "source" word can pose as evidence   | Fixed | The root cause is that the evidence identification uses broad keywords as evidence anchors. This round tightened to URL, `source:`, `evidence:`, official announcement, and other markers, with regression that plain `source` does not count as evidence. |
| P-0272 | src/domains/yono/index.ts:266 | src/domains/yono/index.ts:266 `confidence` only produces "high"/"medium", never "low"; the confidence-level scale is incomplete   | Fixed | The root cause is that the comment-signal confidence tier is missing a low-confidence band. This round added a `low` branch, with `tests/unit/domains/yono/yono-domain.test.ts` covering weak-signal scenarios. |
| P-0273 | src/domains/yono/index.ts:188 | src/domains/yono/index.ts:188 `createMarket` does not validate `closeAt > openAt` and `resolutionDeadline > closeAt`; markets can be created already settled or with reversed time   | Fixed | The root cause is that market creation only accepts time strings, with no time-order validation. This round enforces `closeAt > openAt` and `resolutionDeadline > closeAt`, with illegal-time regression. |
| P-0274 | src/domains/yono/index.ts:526 | src/domains/yono/index.ts:526 `registerYonoDomain` always registers with `skipSmokeTest:true`, bypassing the smoke check for the prediction-market domain   | Re-reviewed & Closed | The root cause is that the review mistook an unused legacy option for a real go-live switch; the current `DomainRegistryService.register()` does not consume `skipSmokeTest`, and the real smoke gate is on the promote/activate path. |
| P-0275 | src/domains/quant-trading/index.ts:73 | src/domains/quant-trading/index.ts:73 `orderNotionalUsd` comparison does not take the absolute value, so a negative notional bypasses `maxOrderNotionalUsd`; lines 79, 82 also do not validate non-negative input   | Fixed | The root cause is that the pre-trade guard neither validates non-negative input nor compares notional/position via absolute value. This round added input validation and absolute-value limits, with `tests/unit/domains/quant-trading/index.test.ts` regression. |
| P-0276 | src/domains/healthcare/index.ts:7 | src/domains/healthcare/index.ts:7 `HEALTHCARE_DOMAIN_PRESET` only marks summarize/coordinate as requiring review, while triage (the highest-risk) actually bypasses review → the healthcare domain's default safety posture is wrong   | Fixed | The root cause is that the healthcare domain preset put high-risk triage in the no-review set. This round added triage to default review, with corresponding tests. |
| P-0277 | src/domains/yono/index.ts:383 | src/domains/yono/index.ts:383 `proposedOutcomeId` always takes `market.outcomes[0]`; regardless of which outcome the user evidence points to, the resolution recommendation has essentially no decision logic   | Fixed | The root cause is that the resolution assist still falls back to the default first outcome when evidence is present. This round prefers matching outcome/type from evidence refs, with regression for `no`-outcome resolution. |
| P-0278 | src/domains/risk-profile/index.ts:75 | src/domains/risk-profile/index.ts:75 `score` input is not validated for NaN/Infinity; on NaN, all `>=` comparisons are false, falling into default and returning "low"   | Fixed | The root cause is that the risk classification only compares ranges without first rejecting NaN/Infinity. This round routes illegal scores to a safe fallback, with `tests/unit/domains/risk-profile/index.test.ts` coverage. |
| P-0279 | src/domains/quant-trading/index.ts:37-42 | src/domains/quant-trading/index.ts:37-42 `DEFAULT_QUANT_TRADING_PRE_TRADE_LIMIT_POLICY` hardcodes 100K USD single order / 25K USD daily loss / 12% drawdown as the "industry default", not layered by tenant/account   | Re-reviewed & Closed | The root cause is that the review mistook the exported baseline constant for a forced tenant policy; in practice, `evaluateQuantTradingPreTradeRisk()` always consumes the `limitPolicy` passed in by the caller, and the constant is only a default sample/test baseline. |
| P-0280 | src/domains/finance-accounting/index.ts:7 | src/domains/finance-accounting/index.ts:7 `FINANCE_ACCOUNTING_DOMAIN_PRESET` excludes reconcile from review, but financial reconciliation discrepancies are core to compliance auditing, with no one reviewing by default   | Fixed | The root cause is that the finance domain preset put high-risk reconcile in the no-review set. This round added reconcile to default review, with tests. |
| P-0281 | src/domains/yono/index.ts:175-187 | src/domains/yono/index.ts:175-187 all `createMarket` risk fields (manipulationRisk/regulatoryRisk, etc.) are hardcoded to low/medium; all markets default to medium regulatory risk at creation, decoupling from the risk-profile system   | Fixed | The root cause is that the market risk profile is fully hardcoded in the create path. This round derives risk from category, tags, and time-to-close, no longer fixed backfill. |
| P-0282 | src/domains/registry/plugin-runtime-host.ts:363 | Command construction: `spawn(spec.command, spec.args, ...)` directly executes the command/args specified by the external spec src/domains/registry/plugin-runtime-host.ts:363   | Re-reviewed & Closed | The root cause is that the review only saw the `spawn()` execution point, without continuing up to the launch spec validation; the current container run spec comes from the controlled parse result of `AA_PLUGIN_RUNTIME_CONTAINER_COMMAND_JSON`, not from arbitrary command passthrough of the plugin manifest. |
| P-0283 | src/domains/risk-profile/index.ts:84 | src/domains/risk-profile/index.ts:84 `computeDomainRiskLevel` still returns "low" for low scores (<35) when `profile.defaultRiskLevel === "high"`, wrongly downgrading the high default risk (only critical has a fallback)   | Fixed | The root cause is that the default risk level only has a fallback for critical, and the high default tier is overridden by low scores. This round preserves the safe lower bound of `defaultRiskLevel === "high"`, with tests. |
| P-0284 | src/domains/yono/index.ts:336-338 | src/domains/yono/index.ts:336-338 `manipulationRisk` is judged only by "all 5 stances are the same", never outputs "medium", and does not combine time window/account reputation/IP, making it an empty-shell manipulation check   | Fixed | The root cause is that the social-prediction domain condenses manipulation risk into a binary judgment, with weak-consistency signals unable to fall into the mid tier. This round added a `medium` tier, at least promoting 3+ one-direction consistent signals from low to medium. |
| P-0285 | src/domains/legal/index.ts:11 | src/domains/legal/index.ts:11 `requiresAttorneyReview` always returns true for all LegalTaskType, making the function meaningless and forming two mutually exclusive "whether attorney review is required" judgments with line 14 `requiresLegalReview`   | Fixed | The root cause is that the legal domain maintained two parallel review-judgment logics, and `requiresAttorneyReview()` departed from the canonical rule. This round delegates it to `requiresLegalReview()`, and includes review/redline/advise in default review. |
| P-0286 | src/domains/registry/plugin-runtime-child.ts:172-176 | src/domains/registry/plugin-runtime-child.ts:172-176 globally hijacks console.*, then line 197 `process.exitCode = 0`: the plugin child process defaults to a successful exit; any runtime panic after the hijack only lands in the structured log without affecting exitCode → the parent process cannot perceive plugin failure   | Re-reviewed & Closed | The root cause is the review mixing request-level failure with process-lifecycle exit semantics; the current `process.exitCode = 0` is only set under an explicit shutdown message, and request exceptions are still returned through the runtime response channel. |
| P-0287 | src/domains/quant-trading/index.ts:51-57 | src/domains/quant-trading/index.ts:51-57 `taskType !== "trade"` directly returns `allowed=true` with empty `evaluatedGuards`; simulate tasks can bypass all risk gates (and actually reach real order chains for backtesting/shadow matching)   | Fixed | The root cause is that pre-trade risk only treats `trade` as the controlled path, while `simulate` is wrongly let through. This round makes `simulate` also go through guard validation, with tests. |
| P-0288 | src/domains/yono/index.ts:255-258 | src/domains/yono/index.ts:255-258 `scoreWords` uses `String.includes` for keyword scanning; "yes" matches "yesterday/eyes", "no" matches "knot/note", with massive misjudgment; pure English keywords fail in Chinese/Spanish markets   | Fixed | The root cause is that the comment signal uses substring matching for keywords, leading to word-boundary false hits. This round changed to post-tokenization hit detection, at least curbing the English substring false-hit surface for `yes/no`. |
| P-0289 | src/domains/academic-research/index.ts、finance-accounting、healthcare、legal | src/domains/academic-research/index.ts, finance-accounting, healthcare, legal, and 10+ other domain index.ts files only differ by changing the enum string, which is a copy-paste of the same template, lacking actual differentiated capability claims per industry (corroborated by P-0262/P-0002 as "industry-domain-scaling pseudo-differentiation")   | Re-reviewed & Closed | The root cause is the review mistaking a shared preset combination for pseudo-differentiated implementation; the current multi-domain modules intentionally reuse `domain-module-helper.ts` as a baseline template, with per-domain task/review strategies for configured differentiation. |
| P-0290 | src/domains/registry/plugin-runtime-child.ts:172-176 | src/domains/registry/plugin-runtime-child.ts:172-176 globally hijacks `console.log/warn/error = ...`, which pollutes third-party libraries' diagnostic output (including stack-trace line number loss)   | Re-reviewed & Closed | The root cause is the review mistaking the stdio redirection for diagnostic loss; the current console hijack is the established bridge layer of the plugin child process sending structured logs to the parent, and does not affect the failure-return contract. |
| P-0291 | src/domains/domain-risk-profile-service.ts:182-200 | Inside the overrides for-of, each override calls `rules.find(... matchesPattern)`, and `matchesPattern` internally does `new RegExp` on-the-fly compilation — O(n²) scan with each RegExp compilation src/domains/domain-risk-profile-service.ts:182-200   | Fixed | The override matcher repeatedly scans and on-the-fly compiles regex per rule, with high hot-path complexity. |
| P-0292 | src/domains/registry/plugin-runtime-child.ts:105-108 | src/domains/registry/plugin-runtime-child.ts:105-108 inside the ESM child process, `require("node:http"/"node:https"/"node:net"/"node:tls")` is cast as `as typeof import(...)`, bypassing the ESM static dependency graph, which is detrimental to sandbox/SBOM analysis   | Re-reviewed & Closed | The root cause is that the review ignored that the purpose here is not business dependency loading, but to first inject patches via `createRequire` and then `syncBuiltinESMExports()`, in order to synchronize the controlled rewrite of built-in network modules to ESM consumers. |
| P-0293 | src/domains/yono/index.ts:524 | src/domains/yono/index.ts:524 `registry.get(...)!` non-null assertion followed by null check, a TOCTOU race exists under concurrent registration   | Fixed | The root cause is that the registration path still kept a non-null assertion after a null check, leaving an unnecessary TOCTOU expression. This round directly returns the result of `register()` instead of `get(...)!` again. |
| P-0294 | src/domains/registry/domain-registry-service.ts:266-272 | `toolNames.filter((tool) => enabledTools.has(tool) \|\| domain.capabilities.requiredTools.includes(tool) \|\| domain.capabilities.optionalTools.includes(tool))`: each tool does linear `includes` against `requiredTools`/`optionalTools`; should pre-convert to Set src/domains/registry/domain-registry-service.ts:266-272   | Fixed | The root cause is that the tool-filter hot path repeatedly does linear `includes` inside the loop. This round pre-converts required/optional tools to `Set`, reducing repeated scan cost. |
| P-0295 | src/domains/governance/hr-role-governance-service.ts:16-24 | `src/domains/governance/hr-role-governance-service.ts:16-24` imports `platform/five-plane-control-plane/approval-center/approval-service.js` and `platform/five-plane-execution/tool-executor/tool-recommend-service.js`; `domains/governance/division-loader.ts:26-28` goes into `iam/sandbox-policy`, `config-center/config-governance-service`: domains directly enter the five-plane private internals.   | Re-reviewed & Closed | The root cause is that built-in domains/governance services are themselves runtime modules, not external extension boundaries; the A table mistook the domain layer's direct connection to canonical platform services for a private violation. |
| P-0296 | src/domains/domain-baseline-catalog.ts<br>src/interaction/interaction-baseline-catalog.ts<br>src/ops-maturity/ops-maturity-baseline-catalog.ts<br>src/org-governance/governance-baseline-catalog.ts<br>src/scale-ecosystem/scale-baseline-catalog.ts | `*-baseline-catalog.ts` is duplicated in 5 top-level directories (`src/domains/domain-baseline-catalog.ts`, `src/interaction/interaction-baseline-catalog.ts`, `src/ops-maturity/ops-maturity-baseline-catalog.ts`, `src/org-governance/governance-baseline-catalog.ts`, `src/scale-ecosystem/scale-baseline-catalog.ts`), with the same pattern scattered.   | Re-reviewed & Closed | The five baseline catalogs each carry the default governance templates and seed rules of their top-level subdomain; the consistent naming is pattern reuse, not a copy-paste of the same implementation. |
| P-0297 | src/ops-maturity/drift-detection/evolution-integration-service.ts:180 | `Math.random()` used for evolution record ID: src/ops-maturity/drift-detection/evolution-integration-service.ts:180   | Fixed | The evolution evidence record ID directly inlines `Date.now()+Math.random()`, with insufficient persistence identifier entropy and hard to harden uniformly. |
| P-0298 | src/ops-maturity/drift-detection/drift-detector-service.ts:157 | Hash comparison uses `===`: `current.hash === baseline.hash` drift detection src/ops-maturity/drift-detection/drift-detector-service.ts:157   | Fixed | The root cause is that drift detection directly does string-equality comparison, with the security-sensitive path not using constant-time comparison. This round changed to the `timingSafeEqual` helper. |
| P-0299 | src/ops-maturity/capacity-planner/forecaster/index.ts:1-13 | src/ops-maturity/capacity-planner/forecaster/index.ts:1-13 "Capacity Forecaster" only does `usage*(1+rate%)` exponential extrapolation, with no statistical model/confidence interval/input validation, misleading naming, and triggering massive array allocation when periods is huge   | Fixed | The root cause is that the capacity forecaster has no input boundary validation, and bad parameters can directly amplify to array allocation and abnormal prediction results. This round added validation for negative numbers, non-finite values, abnormal growth rate, and oversized periods. |
| P-0300 | src/ops-maturity/chaos/chaos-experiment-scheduler.ts:748 | setInterval not cleaned up: `setInterval(...)` is created but not saved to a disposable on the error path src/ops-maturity/chaos/chaos-experiment-scheduler.ts:748   | Fixed | The continuous monitoring interval is not in full lifecycle management, and the long-lived timer will continue to hold the event loop. |
| P-0301 | src/ops-maturity/workflow-debugger/health-monitor.ts:119 | src/ops-maturity/workflow-debugger/health-monitor.ts:119 uses ISO string lexicographical comparison for time windows; if any probe timestamp has a timezone suffix (e.g. +08:00), the comparison is wrong, and window filtering will miss/miscalculate   | Fixed | The time-window filter directly compares time strings rather than normalized real time points, leading to misjudgment of whether to fall into the window under mixed timezone offsets. |
| P-0302 | src/ops-maturity/drift-detection/cross-agent-analyzer/index.ts:402 | src/ops-maturity/drift-detection/cross-agent-analyzer/index.ts:402 uses `z-score>1.25` as the significance threshold when `n<3`, which is inconsistent with the alpha=0.05 path of the two paths ("significant difference" becomes an arbitrarily tunable parameter)   | Fixed | The root cause is that the small-sample significance and bootstrap paths used two inconsistent thresholds. This round converges the small-sample branch to deterministic bias-threshold logic. |
| P-0303 | src/ops-maturity/chaos/chaos-experiment-scheduler.ts:471-489 | src/ops-maturity/chaos/chaos-experiment-scheduler.ts:471-489 `simulateRollbackAction` is a placeholder (only setTimeout 100ms then logs); in production chaos experiments, "rollback" does not execute any business recovery   | Fixed | The root cause is that the chaos scheduler hard-coded rollback as a placeholder. This round introduces a pluggable `rollbackActionExecutor`, with the default path also having interruptible execution semantics. |
| P-0304 | src/ops-maturity/drift-detection/evolution-integration-service.ts:180,218 | src/ops-maturity/drift-detection/evolution-integration-service.ts:180,218 same file two times copy-paste `id: \`ev_${Date.now()}_${Math.random()...`` inline ID generation, no abstracted utility   | Fixed | The evidence ID generation logic is copy-pasted in multiple branches, making it impossible to uniformly converge the weak-random scheme and the fix strategy. |
| P-0305 | src/ops-maturity/drift-detection/learning/evidence-store.ts:121 | The evidence record is entirely cloned via `JSON.parse(JSON.stringify(record))` before being read/returned; the evidence record may be large; under high-frequency calls, it becomes a GC pressure point src/ops-maturity/drift-detection/learning/evidence-store.ts:121   | Fixed | The evidence record still uses JSON deep copy before being returned. |
| P-0306 | src/ops-maturity/workflow-debugger/health-monitor.ts:121-136 | src/ops-maturity/workflow-debugger/health-monitor.ts:121-136 when there are no probes in the window, `getSnapshot` returns `status:"healthy", healthScore:100`; components that have not reported for a long time instead appear completely healthy (should be "unknown/stale")   | Re-reviewed & Closed | The current health monitor explicitly defines an empty window as "neutral baseline with no recent samples", while exposing the no-data state via `totalProbes=0` / `oldestProbeInWindow=null`; this is a semantic choice, not an implementation error. |
| P-0307 | src/ops-maturity/drift-detection/cross-agent-analyzer/index.ts:416-417 | src/ops-maturity/drift-detection/cross-agent-analyzer/index.ts:416-417 bootstrap quantile uses `Math.floor(alpha/2*100)=2` to take the lower bound; the 2.5% quantile is anchored to the 2nd sample, and under a long tail `lower==bootstrapMeans[2]` is systematically high   | Fixed | The root cause is that the bootstrap quantile directly uses integer-bucket index truncation. This round changes to interpolation-based quantile extraction, eliminating the systematic upward bias. |
| P-0308 | src/ops-maturity/drift-detection/cross-agent-analyzer/index.ts:412 | src/ops-maturity/drift-detection/cross-agent-analyzer/index.ts:412 bootstrap significance test directly uses `Math.random`, results are not reproducible, and multiple calls of `isSignificantlyDifferent` for the same input may give inconsistent conclusions (hard to audit)   | Fixed | The root cause is that bootstrap sampling depends on the global `Math.random`. This round changed to a fixed-seed deterministic sampler, ensuring reproducibility for the same input. |
| P-0309 | src/ops-maturity/chaos/chaos-experiment-scheduler.ts:431-442 | src/ops-maturity/chaos/chaos-experiment-scheduler.ts:431-442 `executeRollbackWithTimeout` uses setTimeout to mark timeout but has no AbortController; after timeout, the underlying actions still continue to run and eventually trigger a second `completeRollbackActions("completed")` racing the timeout branch   | Fixed | The root cause is that rollback timeout only marks the state, does not interrupt the underlying actions, and has no single-completion protection. This round introduces `AbortController`, terminal-state deduplication, and runId anti-double-completion. |
| P-0310 | src/ops-maturity/chaos/chaos-experiment-scheduler.ts:748-782 | src/ops-maturity/chaos/chaos-experiment-scheduler.ts:748-782 `startContinuousMonitoring` uses `setInterval+void async` wrapping; when the evaluator is slower than `intervalMs`, concurrent evaluators appear, and `recordContinuousSteadyStateSample` is written multiple times to the same time slot   | Fixed | The root cause is that continuous monitoring uses `setInterval` superimposed with an async evaluator, and slow calls overlap. This round changed to chained `setTimeout` scheduling, avoiding concurrent reentry. |
| P-0311 | src/org-governance/org-model/org-governance-saga.ts:290,327,364 | Error swallowing: `} catch {` silently ignores in the SAGA compensation place src/org-governance/org-model/org-governance-saga.ts:290,327,364   | Fixed | The root cause is that the SAGA compensation stage swallows handler failures, breaking the fail-close semantics. This round explicitly throws `org_governance_saga.compensation_failed:*` after compensation. |
| P-0312 | src/org-governance/approval-routing/approval-routing-service.ts:82-91 | src/org-governance/approval-routing/approval-routing-service.ts:82-91 `fxRatesToCny` defaults USD=7.2 hardcoded, never refreshed; cross-border approval amount conversion uses stale exchange rates for a long time, distorting threshold judgment   | Fixed | The approval routing hardcodes the exchange-rate table in the service default value, with no explicit configuration entry or snapshot source recording. |
| P-0313 | src/org-governance/org-model/org-governance-saga.ts:290,327,364 (P-0311) three places | src/org-governance/org-model/org-governance-saga.ts:290,327,364 (P-0311) three places SAGA compensation swallows errors, violating the saga contract that requires explicit failure   | Fixed | The root cause is that compensation failure is only logged and not rethrown, making the saga look recovered. This round uniformly rethrows the failure after executing the remaining compensation, keeping saga failure observable. |
| P-0314 | src/org-governance/delegated-governance/stores/index.ts:152-156,229 | `JSON.parse` without try/catch: directly parses row fields (multiple columns) src/org-governance/delegated-governance/stores/index.ts:152-156,229   | Fixed | The root cause is that the delegated governance store directly `JSON.parse`s persisted columns, and bad data will cause uncontrolled exceptions. This round adds a safe-parse helper and returns named error codes. |
| P-0315 | src/org-governance/sso-scim/api-key-service.ts:187 | src/org-governance/sso-scim/api-key-service.ts:187 `rotateApiKey` marks the old key as `revoked` but does not write `auditLog`; key rotation is a silent revocation   | Fixed | The rotation path only changes state, without adding the revocation audit and `revokedAt/revokedBy` metadata. |
| P-0316 | src/org-governance/org-model/sync/index.ts:31 | Free-form text `throw new Error("Cannot build OrgChart: no root node found")` src/org-governance/org-model/sync/index.ts:31   | Fixed | OrgChart construction throws raw Error when missing the root node. |
| P-0317 | src/org-governance/org-model/org-governance-saga.ts:228,231,237 | Error code style inconsistent: `missing_prepare_handler` etc. has no namespace prefix, mixed with in-repo `org_hierarchy.*`, `saml.*` src/org-governance/org-model/org-governance-saga.ts:228,231,237   | Fixed | The root cause is that the org governance saga still uses bare error codes. This round unifies them under the `org_governance_saga.missing_*_handler` namespace. |
| P-0318 | src/org-governance/sso-scim/oidc/oidc-service.ts:646 | SSRF surface: oidc-service `fetch(url, ...)` for OIDC metadata/JWKS fetching, no IP/host restriction src/org-governance/sso-scim/oidc/oidc-service.ts:646   | Fixed | The root cause is that the OIDC outbound request previously passed through the provider URL directly, without going through the unified outbound-URL validation. This round has connected `parseSafeOutboundUrl()`, fail-closing for loopback/internal addresses, and added targeted regression. |
| P-0319 | src/org-governance/sso-scim/scim-dlq-reconciliation.ts:16-30 | src/org-governance/sso-scim/scim-dlq-reconciliation.ts:16-30 "Reconcile" only does three filter+map and then returns; there is no re-queue/dedup/upstream-reconciliation logic (function name severely mismatched with functionality)   | Re-reviewed & Closed | The root cause is that the review expected replay/repair semantics from the function name; the current `ScimDlqReconciliationService`'s contract and existing tests only define classification, summarization, and reporting, and do not undertake automatic replay. |
| P-0320 | src/org-governance/sso-scim/api-key-service.ts:202-203 | src/org-governance/sso-scim/api-key-service.ts:202-203 `hashKey` uses unsalted SHA-256; for low-entropy/already-leaked old keys, rainbow tables can be replayed in bulk (should use HKDF or a keyed hash with tenant salt)   | Fixed | The root cause is that the API-key index directly uses raw SHA-256, lacking keyed hash and tenant binding. This round changed to HMAC-SHA256 with pepper and tenant scope. |
| P-0321 | src/org-governance/approval-routing/approval-routing-service.ts:104-141 | src/org-governance/approval-routing/approval-routing-service.ts:104-141 after catching `empty_approver_chain`, it falls back to `fallbackApproverIds` (default empty array) and then throws at lines 148-149; the catch branch has no recovery capability, just adding complexity   | Fixed | The empty-approver-chain scenario first goes through a "pseudo-recovery" branch and then throws at the exit; the failure semantics is not directly closed at the routing boundary. |
| P-0322 | src/org-governance/sso-scim/api-key-service.ts:70 | src/org-governance/sso-scim/api-key-service.ts:70 `auditLog` only records revoke, missing critical events like generate/rotate/validate-fail/expire, not satisfying compliance audit   | Fixed | API key audit only covers revoke; the whole key lifecycle lacks a complete event chain. |
| P-0323 | src/org-governance/sso-scim/api-key-service.ts:130-133 | src/org-governance/sso-scim/api-key-service.ts:130-133 `validateApiKey` side-effect changes an expired key to `expired` but does not synchronously delete `keyHashIndex`; the next input with the same hash still hits the stale index and is repeatedly "just expired"   | Fixed | The root cause is that key expiration state and hash index cleanup are separated, with the stale index repeatedly hitting expired records. This round synchronously cleans the index on the expire/revoke/rotate path, with duplicate-expire audit regression. |
| P-0324 | src/org-governance/compliance-engine/evidence-collector.ts:318,323 | setTimeout not cleaned up: `setTimeout(runCollection, schedule.intervalMs)` does not save all handles on the error path src/org-governance/compliance-engine/evidence-collector.ts:318,323   | Fixed | The cycle-collection recursive timer lacks unified lifecycle management, and long-running runs continue to hold the event loop. |
| P-0325 | src/org-governance/approval-routing/approval-routing-service.ts:194,213-215 | src/org-governance/approval-routing/approval-routing-service.ts:194,213-215 the `planChain` conditional mode directly splices `conditionalApproverIds` to the end of `approverChain`, without validating whether these users are in the org-node permission range → can inject rule-bypassing approvers   | Fixed | The root cause is that conditional approver is only appended, without aligned validation with route/org scope. This round limits it to the allowed set and fail-closes on out-of-scope approvers. |
| P-0326 | src/org-governance/sso-scim/api-key-service.ts:121 | src/org-governance/sso-scim/api-key-service.ts:121 when `tenantId==null` and the keyHash hits across multiple tenants, it directly rejects, but lacks alert/audit → attackers can use this to enumerate cross-tenant key existence   | Fixed | Multi-tenant ambiguity validation is fail-close, but the failure branch leaves no audit event. |
| P-0327 | src/org-governance/sso-scim/scim-sync/scim-service.ts:241 | PII pattern: scim-service uses `${tenantId}:${email.toLowerCase()}` as the index key and caches it in memory src/org-governance/sso-scim/scim-sync/scim-service.ts:241   | Fixed | The root cause is that the in-memory index key directly concatenates tenant and email, with PII residing in plaintext. This round changes to a hashed email index key. |
| P-0328 | src/org-governance/sso-scim/api-key-service.ts:189-195 | src/org-governance/sso-scim/api-key-service.ts:189-195 `rotateApiKey` calls `generateApiKey` but drops `existing.tenantId`; the newly generated rotation key lands in the default `tenant:global` → moving the key from the original tenant (multi-tenant isolation breach)   | Fixed | The rotation path reuses `generateApiKey()` without carrying the old record's `tenantId` forward. |
| P-0329 | src/org-governance/approval-routing/approval-routing-service.ts:236-243 | src/org-governance/approval-routing/approval-routing-service.ts:236-243 `buildDelegationMap` only takes a one-level delegation from owners, without limiting the maximum depth of the delegation chain; when A→B→C→A forms a cycle, `resolveDelegatedApprover` behavior depends on its internal implementation → infinite loop or infinite delegation risk   | Re-reviewed & Closed | The root cause is that the review mistook a one-hop delegation map for a recursive delegation chain; the current `buildDelegationMap()` and `resolveDelegatedApprover()` do not perform recursive expansion, and the current implementation does not have a cycle-induced infinite loop. |
| P-0330 | src/org-governance/sso-scim/scim-sync/scim-service.ts:766,772,778,782,792,798,804,808,829,840,851,902,919,938,947,969 | Error code literal inconsistency: `bulk.user_not_found` / `bulk.group_not_found` / `bulk.invalid_path` / `bulk.unsupported_method` mixed with same-file `scim.*` prefix error codes src/org-governance/sso-scim/scim-sync/scim-service.ts:766,772,778,782,792,798,804,808,829,840,851,902,919,938,947,969   | Fixed | The root cause is that the bulk subpath used the historical `bulk.*` naming, not unified with `scim.*`. This round converges on the `scim.bulk.*` prefix. |
| P-0331 | src/org-governance/compliance-engine/evidence-collector.ts:317、five-plane-orchestration/agent-delegation/delegation-audit-service.ts:25-28、five-plane-execution/tool-executor/shadow-snapshot-service.ts:410：EvidenceCollector | (N-1081) src/org-governance/compliance-engine/evidence-collector.ts:317, five-plane-orchestration/agent-delegation/delegation-audit-service.ts:25-28, five-plane-execution/tool-executor/shadow-snapshot-service.ts:410: EvidenceCollector / DelegationAudit / shadow-snapshot.persistRecord all do `writeFileSync` of the whole file with no tmp+rename atomic write → crash produces half-written evidence   | Re-reviewed & Closed | The root cause is that the original record mixed the current correctness defect with subsequent durability enhancement; the evidence collection timer has added lifecycle closure, while the atomic write enhancement has been tracked under the master audit `N-0616/N-1081`. |
| P-0332 | src/scale-ecosystem/federation/federation-gateway.ts:581 | src/scale-ecosystem/federation/federation-gateway.ts:581 emits `federation-event` with a hyphen, violating the registry event-naming style   | Fixed | The federation gateway event name follows local naming conventions, and is not aligned with the global registry naming rules. |
| P-0333 | src/scale-ecosystem/integration/connector-runtime/index.ts:46 | Error swallowing: `} catch {` silently ignores src/scale-ecosystem/integration/connector-runtime/index.ts:46   | Fixed | The callback delivery path silently swallows errors to avoid blocking the main flow, but leaves no minimum failure signal. |
| P-0334 | src/scale-ecosystem/multi-region/data-replicator/index.ts:361 | `Math.random()` used for replication event ID: src/scale-ecosystem/multi-region/data-replicator/index.ts:361   | Fixed | The replication event ID relies on a timestamp plus weak random, and the persistence path lacks a stable unique identifier. |
| P-0335 | src/scale-ecosystem/runtime-services/human-takeover-service-async.ts:915 | `Math.random()` used for operation ID: src/scale-ecosystem/runtime-services/human-takeover-service-async.ts:915   | Fixed | The runtime operation ID continues the `Date.now()+Math.random()` pattern, with insufficient entropy. |
| P-0336 | src/scale-ecosystem/runtime-services/execution-dispatch-service-async.ts:622 | `Math.random()` used for operation ID: src/scale-ecosystem/runtime-services/execution-dispatch-service-async.ts:622   | Fixed | The dispatch operation ID depends on weak random concatenation, with unstable distributed tracing identifiers. |
| P-0337 | src/scale-ecosystem/integration/connector-framework-service.ts:452,471,485 | Error swallowing: three consecutive `} catch {` silently src/scale-ecosystem/integration/connector-framework-service.ts:452,471,485   | Fixed | The connector lifecycle reads the persistent file with an empty catch, and local corruption will not be observed. |
| P-0338 | src/scale-ecosystem/runtime-services/execution-worker-writeback-service-async.ts:858 | `Math.random()` used for operation ID: src/scale-ecosystem/runtime-services/execution-worker-writeback-service-async.ts:858   | Fixed | The writeback operation ID still uses the weak random scheme, making it hard to guarantee uniqueness. |
| P-0339 | src/scale-ecosystem/operations/platform-operator-service.ts:145,521 | `JSON.parse` without try/catch: parses `outputsJson` src/scale-ecosystem/operations/platform-operator-service.ts:145,521   | Fixed | The operator output parsing directly `JSON.parse`s, and corrupt data will leak as uncontrolled exceptions. |
| P-0340 | src/scale-ecosystem/multi-region/cdc-replication-service.ts:739 | `JSON.parse` without try/catch: parses cdc-replication inbound payload src/scale-ecosystem/multi-region/cdc-replication-service.ts:739   | Re-reviewed & Closed | The review confirmed that the CDC inbound payload parsing has controlled protection, and the original record is stale. |
| P-0341 | src/scale-ecosystem/tenant-platform/tenant-platform-service.ts:182-185 | src/scale-ecosystem/tenant-platform/tenant-platform-service.ts:182-185 progress percentage is hardcoded to `totalSteps=100` estimation; for real workflows with 10 or 1000 steps, the preemption decision is severely distorted   | Re-reviewed & Closed | The root cause is that the review mistook the candidate-sorting heuristic for the final quota/preemption decision; the `progressPercent` here is only used for pre-sorting, and the real quota/preemption constraints are still enforced by the scheduler and tenant-governance service. |
| P-0342 | src/scale-ecosystem/tenant-platform/tenant-platform-service.ts:193 | src/scale-ecosystem/tenant-platform/tenant-platform-service.ts:193 priority is mapped to 100/75/50 in three tiers; unrecognized values default to 25, treating "unknown" the same as "low", masking configuration errors   | Re-reviewed & Closed | The root cause is that the review mistook the heuristic ranking score for a configuration-validation boundary; the priority mapping only serves candidate sorting, and unknown values falling to the lowest priority is a conservative degradation rather than silent let-through. |
| P-0343 | src/scale-ecosystem/multi-region/data-replicator/index.ts:240 | Weak algorithm optional: `computeChecksum(payload, algorithm: "sha256"\|"md5")` default parameter is sha256, but the API exposes md5 src/scale-ecosystem/multi-region/data-replicator/index.ts:240   | Fixed | The root cause is that the checksum API historically exposed an md5 entry for compatibility. This round converged the implementation to sha256-only, fail-closing on non-sha256 algorithms. |
| P-0344 | src/scale-ecosystem/integration/connector-runtime/index.ts:36 | SSRF surface: connector-runtime `fetch(callbackUrl, ...)` receives the callback address from the requester src/scale-ecosystem/integration/connector-runtime/index.ts:36   | Fixed | The callback URL is passed directly to `fetch`, with neither address constraint nor failure observability. |
| P-0345 | src/scale-ecosystem/intelligence/perception-service.ts:256,454,488,501 | `JSON.parse` without try/catch: parses perception-service recommended actions src/scale-ecosystem/intelligence/perception-service.ts:256,454,488,501   | Fixed | The brief/proposal/export parse flows directly `JSON.parse`, without a unified safe-array parser. |
| P-0346 | src/scale-ecosystem/runtime-services/durable-event-bus-async.ts:673 | fire-and-forget Promise: `void this.flushBatch();` no catch src/scale-ecosystem/runtime-services/durable-event-bus-async.ts:673   | Fixed | The timed batch flush is directly fire-and-forget, with exceptions unable to be returned and no stderr hint. |
| P-0347 | src/scale-ecosystem/multi-region/region-health-check-service.ts:435 | SSRF surface: region-health-check directly `fetch(${url}/health)`, url comes from configuration with no whitelist src/scale-ecosystem/multi-region/region-health-check-service.ts:435   | Fixed | The regional health check endpoint makes probe requests without URL validation. |
| P-0348 | src/scale-ecosystem/marketplace/pack-security-service.ts:668-686 | src/scale-ecosystem/marketplace/pack-security-service.ts:668-686 uses regex like `/password\|secret\|token/i` to scan `capturedLogs` to post-mark sensitive data, which is a post-hoc string match with no defense against real leakage   | Re-reviewed & Closed | The root cause is that the review mistook the log-sensitive-word scan for the main defense; the current pack security main defense has converged to source-code/capability/runtime fail-close gates, and the `capturedLogs` scan is just an additional alert signal. |
| P-0349 | src/scale-ecosystem/multi-region/data-replicator/index.ts:121 | Weak algorithm optional: data-replicator allows `checksumAlgorithm: "sha256" \| "md5"`, callers can specify md5 for integrity verification src/scale-ecosystem/multi-region/data-replicator/index.ts:121   | Fixed | The root cause is that the data-replicator configuration surface previously exposed the md5 option, with weak digests able to enter the integrity chain directly. This round converges the configuration type to sha256-only. |
| P-0350 | src/scale-ecosystem/billing/budget-reallocation-service.ts:73-104 | `idempotencyKeys = new Set<string>()` never evicts, and grows without bound over the long-lived process; typical idempotency cache missing TTL/eviction strategy src/scale-ecosystem/billing/budget-reallocation-service.ts:73-104   | Fixed | The root cause is that budget reallocation uses a never-reclaimed `Set` to hold idempotency keys. This round changed to a TTL-trimmed `Map` and clears expired keys on every check. |
| P-0351 | src/scale-ecosystem/multi-region/data-replicator/index.ts:413-417 | src/scale-ecosystem/multi-region/data-replicator/index.ts:413-417 `deliverEventWithRetries` acks on success but the target side has no idempotent dedup; under network jitter, retries may cause the target region to apply the same event twice   | Fixed | The inbound replication events lack `eventId` dedup, and a duplicate arrival after a successful retry will re-apply. |
| P-0352 | src/scale-ecosystem/multi-region/region-health-check-service.ts:144: | (N-1141) src/scale-ecosystem/multi-region/region-health-check-service.ts:144: `unregisterRegion` deletes the `circuitBreakers` map reference without calling `cb.reset()` / releasing the timer → timer leak   | Fixed | Region unregistration only deletes the map reference, without actively resetting the circuit breaker's internal state. |
| P-0353 | src/scale-ecosystem/federation/federation-gateway.ts:375,378,383,431 | Free-form text `throw new Error("Source or target organization not found")` (trust governance layer error code missing) src/scale-ecosystem/federation/federation-gateway.ts:375,378,383,431   | Fixed | The federation trust governance branch lacks a structured error with code. |
| P-0354 | src/scale-ecosystem/runtime-services/durable-event-bus-async.ts:298 | Free-form text `throw new Error("Circuit breaker is open - event publishing temporarily disabled")` src/scale-ecosystem/runtime-services/durable-event-bus-async.ts:298   | Fixed | The circuit breaker throws raw Error when open, lacking unified error classification. |
| P-0355 | src/scale-ecosystem/multi-region/data-replicator/index.ts:478-483 | src/scale-ecosystem/multi-region/data-replicator/index.ts:478-483 `handleIncomingEvent` directly does `handler(event)` after receiving the event, never calling `validateEvent` to verify checksum → cross-region replication channel integrity can be bypassed   | Fixed | The inbound replication handler executes the handler directly, bypassing checksum validation. |
| P-0356 | src/scale-ecosystem/integration/connector-framework-service.ts:452,471,485 | src/scale-ecosystem/integration/connector-framework-service.ts:452,471,485 three consecutive `} catch {}` appear in the connector lifecycle critical path, already mentioned in P-0337; no compensation/alert for partial failure   | Fixed | The connector recovery chain silently swallows partial failure, lacking minimum alert and diagnostic output. |
| P-0357 | src/scale-ecosystem/marketplace/pack-security-service.ts:553-555 | src/scale-ecosystem/marketplace/pack-security-service.ts:553-555 `sandboxApi.String` exposes split/substring/trim as functions directly (not prototype methods); when called, `this` is lost and a TypeError is thrown, so even normal packs cannot run   | Fixed | The pack security service tries to expose host built-in objects via a pseudo-sandbox, which is neither secure nor breaks legitimate pack runtime semantics. |
| P-0358 | src/scale-ecosystem/federation/capability-delegation.ts:686 | For each `def of capability.constraints`, calls `overrides.find((o) => o.type === def.type)`; should be a one-shot `Map<type, override>` src/scale-ecosystem/federation/capability-delegation.ts:686   | Fixed | The constraint override repeatedly does linear find on overrides inside the loop, with unnecessary hot-path complexity amplification. |
| P-0359 | src/scale-ecosystem/marketplace/marketplace-governance-service.ts:268,356,690-692 | `JSON.parse` without try/catch: marketplace-governance-service parses `permissionsJson/compatibilityJson` multiple times src/scale-ecosystem/marketplace/marketplace-governance-service.ts:268,356,690-692   | Fixed | The persisted JSON field reads continue to use raw `JSON.parse`, with corrupt data leaking as uncontrolled exceptions. |
| P-0360 | src/scale-ecosystem/multi-region/failover-reconciliation-job.ts:96,213 | `private readonly history: ReconciliationScanResult[] = []` pushes `history.push(result)` after every scan, but no trim; long-running will OOM src/scale-ecosystem/multi-region/failover-reconciliation-job.ts:96,213   | Fixed | The reconciliation scan history results only grow, with the background task lacking a retention window upper bound. |
| P-0361 | src/scale-ecosystem/marketplace/pack-security-service.ts:597-598 | src/scale-ecosystem/marketplace/pack-security-service.ts:597-598 `__addNetworkAttempt/__addFileAccess` are callbacks injected into the context, but at the same time fetch/setTimeout are all set to undefined → no code will call these callbacks, and the "attack detection" statistics are always 0   | Fixed | Attack statistics depend on pseudo-sandbox hooks that will never fire, causing the detection surface to be detached from the actual execution path. |
| P-0362 | src/scale-ecosystem/multi-region<br>deploy/terraform/environments/multi-region | src/scale-ecosystem/multi-region and deploy/terraform/environments/multi-region have multi-region targets, but main.tf:11-19 backend "s3" key is single → multi-region terraform state shares the pool (forming a "code claims multi-region, IaC cannot be multi-region" contradiction with P-0588)   | Fixed | The multi-region overlay exists, but the backend key policy is not isolated, so primary/secondary states can share the pool. |
| P-0363 | src/scale-ecosystem/marketplace/pack-security-service.ts:119,671 | src/scale-ecosystem/marketplace/pack-security-service.ts:119,671 itself uses regex `/child_process.*shell.*true/i`, `/process\|child_process\|exec/i` to detect pack code — this is a pre-runtime heuristic, no AST, with many bypass paths   | Fixed | The pack security judgment was originally built on pseudo-runtime sandbox and overly narrow heuristics, which is both unreliable and cannot fail-close. |
| P-0364 | src/scale-ecosystem/multi-region/cdc-replication-service.ts:647-651 | `recordConflict` does `history.slice(-100)` only trimming by taskId subarray; `conflictHistory: Map<taskId, ...>` itself is unbounded; continuously generating new taskIds will leak src/scale-ecosystem/multi-region/cdc-replication-service.ts:647-651   | Fixed | CDC conflict history only trims the subarray, and the outer task map lacks a capacity upper bound. |
| P-0365 | src/scale-ecosystem/tenant-platform/tenant-platform-service.ts:131-151 | src/scale-ecosystem/tenant-platform/tenant-platform-service.ts:131-151 `evaluateSlo` only looks up globally by sloId, returning on the first matching SLO → two tenants defining the same sloId will leak another tenant's configuration (tenant isolation breach)   | Fixed | SLO evaluation looks up by global `sloId`, ignoring tenant scope. |
| P-0366 | src/scale-ecosystem/marketplace/pack-security-service.ts:531 | src/scale-ecosystem/marketplace/pack-security-service.ts:531 after exposing `JSON.parse/stringify`, a malicious pack can use the toJSON callback to grab the outer closure → sandbox allowlist is moot   | Fixed | Exposing host JSON callbacks to the vm context essentially brings closure capabilities into the so-called sandbox. |
| P-0367 | src/scale-ecosystem/runtime-services/execution-worker-writeback-service-async.ts:698,722 | setInterval handle release path is incomplete: `this.batchFlushTimer = setInterval(...)` has inconsistent conditional branches in multiple dispose paths src/scale-ecosystem/runtime-services/execution-worker-writeback-service-async.ts:698,722   | Re-reviewed & Closed | The review confirmed that the coalescing/batch two intervals have been uniformly `unref()`'d, and `dispose()` does `clearInterval()` on both handles; the original inconsistent release path record is obsolete. |
| P-0368 | src/scale-ecosystem/sla/index.ts:1<br>src/scale-ecosystem/sla-engine/ | `src/scale-ecosystem/sla/index.ts:1` is only a single line `export * from "../sla-engine/index.js"`, forming a singular/plural double directory with the same-level `src/scale-ecosystem/sla-engine/` (actual implementation).   | Re-reviewed & Closed | `scale-ecosystem/sla/index.ts` is currently just a compatibility facade, and the authoritative entry of the actual implementation is still in `sla-engine/`; this is a compatibility exit, not duplicate implementation. |
| P-0369 | src/scale-ecosystem/marketplace/pack-security-service.ts:518-524 | src/scale-ecosystem/marketplace/pack-security-service.ts:518-524 `sandboxApi.Date` is a plain object, not a Date class; `new Date()` fails in the pack, and the sandbox cannot run any legitimate code that depends on time   | Fixed | The pseudo-sandbox turns host built-in objects into incompatible fake implementations, so legitimate pack code also cannot run. |
| P-0370 | src/scale-ecosystem/multi-region/region-health-check-service.ts:183: | (N-1171) src/scale-ecosystem/multi-region/region-health-check-service.ts:183: `checkRegion` uses `circuitBreaker.execute(...)` to wrap `performHealthCheck`, but `latencyMs = Date.now()-startTime` includes the circuit-breaker wait time → the region latency metric is underestimated   | Fixed | The region latency metric incorrectly reuses the breaker wrap duration rather than the latency measured by the health probe itself. |
| P-0371 | src/scale-ecosystem/multi-region/data-replicator/index.ts:415-444 | src/scale-ecosystem/multi-region/data-replicator/index.ts:415-444 after `acknowledge`, `lastSequence++` is only the per-batch count; writing to `checkpoint.sequenceNumber` is not the source-side real sequence number, so the checkpoint rolls back after restart or concurrent flush   | Fixed | The checkpoint sequence number uses the per-batch success count rather than the real source event sequence number. |
| P-0372 | src/scale-ecosystem/multi-region/per-tenant-encryption.ts:54: | (N-1111) src/scale-ecosystem/multi-region/per-tenant-encryption.ts:54: `PerTenantEncryptionService` puts KEK/DEK directly into the in-process `Map<keyId, Buffer>`, lost on restart, no KMS/HSM backend, directly contradicting the `enforceHardwareSecurityModule` field semantics   | Re-reviewed & Closed | The root cause is the review confusing the baseline in-memory implementation with external KMS/HSM wiring; the gap has been merged into the master audit `N-1111/N-1112` as a backend-integration boundary, not a current in-repo correctness regression. |
| P-0373 | src/scale-ecosystem/marketplace/pack-security-service.ts:119,671 | src/scale-ecosystem/marketplace/pack-security-service.ts:119,671 the static scan of packs uses regex `/child_process.*shell.*true/i`, `/process\|child_process\|exec/i` (P-0363), no AST, so base64/encrypted strings and indirect access `globalThis["child_process"]` will bypass   | Fixed | The security service over-relies on narrow regex and pseudo-sandbox, lacking fail-close source-level risk judgment. |
| P-0374 | src/scale-ecosystem/runtime-services/execution-dispatch-service-async.ts:340-352 | `signal?.addEventListener("abort", () => {...})` does not include `{ once: true }` and has no corresponding `removeEventListener`; each retry/timeout call attaches a listener; when the signal is reused, listeners grow without bound + trigger Node's default maxListeners warning src/scale-ecosystem/runtime-services/execution-dispatch-service-async.ts:340-352   | Fixed | The abort listeners are stacked per request, but are not cleaned up at terminal state. |
| P-0375 | src/scale-ecosystem/cost-attribution/index.ts:1-7 | `src/scale-ecosystem/cost-attribution/index.ts:1-7` cross-region reverse re-exports `../../ops-maturity/cost-optimizer/cost-optimization-service.js`; scale-ecosystem reverse-depends on ops-maturity.   | Re-reviewed & Closed | The current `cost-attribution/index.ts` is a compatibility export surface for the canonical cost optimizer, which is a namespace bridge, not a reverse copy implementation. |
| P-0376 | src/scale-ecosystem/operations/<br>src/scale-ecosystem/runtime-services/<br>src/scale-ecosystem/runtime-governance-service.ts<br>src/scale-ecosystem/scale-bootstrap.ts | The boundary between `src/scale-ecosystem/operations/` (2 files) and `src/scale-ecosystem/runtime-services/`, `src/scale-ecosystem/runtime-governance-service.ts`, `src/scale-ecosystem/scale-bootstrap.ts` is vague, and `platform-operator-service.ts` again appears with the same name under `marketplace/`.   | Re-reviewed & Closed | This entry is mostly a directory-inventory and naming-look-and-feel issue; the scale runtime/operations/bootstrap boundary has been recorded in the architecture document, and `marketplace/platform-operator-service.ts` is now a compatibility re-export. |
| P-0377 | src/scale-ecosystem/tenant-platform/tenant-platform-service.ts | src/scale-ecosystem/tenant-platform/tenant-platform-service.ts ~41 KB single file + same-directory `tenant-platform-service-async.ts` double set (P-0142, P-0386), totaling 80 KB+ on the same theme   | Re-reviewed & Closed | This is the sync/async mirror and large-file maintenance debt already recorded in the architecture reference document, a known inventory issue, no longer repeatedly recorded as a new defect. |
| P-0378 | src/scale-ecosystem/tenant-platform/tenant-platform-service.ts:102-105 | src/scale-ecosystem/tenant-platform/tenant-platform-service.ts:102-105 fetches the global singleton via `getNoisyNeighborProtectionService()/getPerTenantEncryptionService()`, but `ResourcePoolService/FairSchedulingService` are new'd out as instances → cross-tenant shared and independent two strategies coexist, with contradictory isolation semantics   | Re-reviewed & Closed | This is the deliberate layering between the control-plane global-strategy singleton and the runtime resource-scheduling instance; the singleton carries shared governance rules, and the instance carries this service's local state, not directly forming a tenant-isolation breach. |
| P-0379 | src/scale-ecosystem/multi-region/data-replicator/index.ts:361,531 | src/scale-ecosystem/multi-region/data-replicator/index.ts:361,531 `eventId: \`repl_${Date.now()}_${Math.random().toString(36).slice(2)}\`` uses `Math.random` to generate event IDs; the collision probability under high cross-region concurrency is non-negligible; and eventId enters the persistence path   | Fixed | The replication event ID relies on a timestamp plus weak random, and the persistence path lacks a stable unique identifier. |
| P-0380 | src/scale-ecosystem/feedback-loop/analyzer/index.ts:3、src/scale-ecosystem/feedback-loop/collector/signal-preprocessor.ts:5 | src/scale-ecosystem/feedback-loop/analyzer/index.ts:3, src/scale-ecosystem/feedback-loop/collector/signal-preprocessor.ts:5 etc. `export interface` blocks are declared in index.ts when they contain only 1-2 fields; the barrel mixes types with runtime exports   | Re-reviewed & Closed | These files currently serve both tiny-surface types and runtime exports, a lightweight facade style choice, not a behavior error or duplicate implementation. |
| P-0381 | src/scale-ecosystem/runtime-services/execution-worker-writeback-service-async.ts:721-725 | `startBatchFlushTimer` is the same `setInterval(() => this.flushBatch())` with no reentrancy guard at the timer layer; relying on the internal `batchFlushPromise` in `flushBatch` for serialization, but during the promise interval enqueue continues and the interval triggers unnecessarily src/scale-ecosystem/runtime-services/execution-worker-writeback-service-async.ts:721-725   | Fixed | The flush timer does not skip existing batch processing at the entry; idle ticks continue to reenter the schedule. |
| P-0382 | src/scale-ecosystem/multi-region/data-replicator/index.ts:361 | src/scale-ecosystem/multi-region/data-replicator/index.ts:361 `eventId` uses `Date.now()+Math.random()` concatenation, with non-zero repeat probability under millisecond-level concurrency; two events in the same ms fall into the same ID → outbox dedup mistakenly drops events   | Fixed | `eventId` is generated by `Date.now()+Math.random()`, with non-zero repeat probability under millisecond-level concurrency. |
| P-0383 | src/scale-ecosystem/runtime-services/<br>src/platform/five-plane-execution/* | The entire `src/scale-ecosystem/runtime-services/` directory (`durable-event-bus.ts:1`, `execution-dispatch-service.ts:1`, etc.) only re-exports `src/platform/five-plane-execution/*` and `five-plane-state-evidence/*`; the scale layer reverse-exposes plane internals.   | Re-reviewed & Closed | `runtime-services/` is a compatibility export layer after the runtime-services extraction; the architecture document has recorded this kind of shim as a migration-compatibility surface, not a duplicate implementation. |
| P-0384 | src/scale-ecosystem/runtime-services/{execution-dispatch-service-async,human-takeover-service-async,execution-worker-handshake-service-async,execution-worker-writeback-service-async}.ts | src/scale-ecosystem/runtime-services/{execution-dispatch-service-async,human-takeover-service-async,execution-worker-handshake-service-async,execution-worker-writeback-service-async}.ts same pattern `op_${Date.now()}_${Math.random().toString(36).slice(2,11)}` 9-char base36 → only ~46 bits, far less than UUIDv4 122 bits, distributed tracing can collide   | Fixed | Multiple async runtime services reuse `Date.now()+Math.random` for operation IDs, with insufficient entropy. |
| P-0385 | src/scale-ecosystem/marketplace/pack-security-service.ts:627 | src/scale-ecosystem/marketplace/pack-security-service.ts:627 `vm.runInContext` is explicitly not a security sandbox per Node docs; a malicious package can use `this.constructor.constructor("return process")()` to escape → the entire pack security service is a performance   | Fixed | The pack security service mistakenly treats `vm.runInContext` as a security boundary. |
| P-0386 | src/scale-ecosystem/billing/billing-service-async.ts:50、tenant-platform/tenant-platform-service-async.ts:40、tenant-platform/data-plane-flow-service-async.ts:41、ops-maturity/drift-detection/evolution-mvp-service-async.ts:40、scale-ecosystem/intelligence/perception-service-async.ts:41<br>package.json:5 | src/scale-ecosystem/billing/billing-service-async.ts:50, tenant-platform/tenant-platform-service-async.ts:40, tenant-platform/data-plane-flow-service-async.ts:41, ops-maturity/drift-detection/evolution-mvp-service-async.ts:40, scale-ecosystem/intelligence/perception-service-async.ts:41 all use `const { X } = require("./X.js")` in ESM files, mixing CommonJS require, violating `package.json:5 "type": "module"`, relying on the Node compatibility layer to run   | Re-reviewed & Closed | The review confirmed these async wrappers have been changed to ESM import/SyncBackedAsyncService, and no longer mix CommonJS `require()`. |
| P-0387 | src/scale-ecosystem/runtime-services/execution-worker-handshake-service-async.ts:653-658 | `setInterval(() => this.flushBatch(), ...)` does not wait for `flushBatch` (async) to complete, with no in-flight flag; same pattern as `durable-event-bus-async.ts:681-686` (the latter also has async `flushBatch`) — may concurrently splice, causing out-of-order/duplicate processing (durable-event-bus has `batchProcessingPromise` fallback at lines 626-636, but the handshake service has no such guard) src/scale-ecosystem/runtime-services/execution-worker-handshake-service-async.ts:653-658   | Fixed | The handshake batch flush timer has no entry-reentry guard, and also lacks a failure log. |
| P-0388 | src/scale-ecosystem/marketplace/ | `src/scale-ecosystem/marketplace/` (`billing-service.ts`, `tenant-platform-service.ts`, `execution-dispatch-service.ts`, `durable-event-bus.ts`, `data-plane-flow-service.ts`, `human-takeover-service.ts` …) and `scale-ecosystem/billing/`, `scale-ecosystem/tenant-platform/`, `scale-ecosystem/runtime-services/`, `platform/five-plane-execution/`, `platform/five-plane-state-evidence/events/` largely duplicate same-name services.   | Re-reviewed & Closed | Most of the same-name files in `marketplace/` have now degraded to compatibility shim/re-export; the related split has been recorded as TD-8 in the architecture document, no longer treated as duplicate implementation defect. |
| P-0389 | src/scale-ecosystem/runtime-services/{durable-event-bus-async,execution-worker-handshake-service-async,execution-worker-writeback-service-async}.ts、src/ops-maturity/chaos/chaos-experiment-scheduler.ts:748、src/interaction/dashboard/dashboard-websocket-server.ts:123、src/platform/five-plane-execution/ha/{leader-election-service.ts:589,626、wal-checkpoint-service.ts:607、stuck-run-sweeper-service.ts:490}、src/platform/five-plane-orchestration/harness/durable/sleep-scheduler.ts、src/platform/five-plane-control-plane/iam/secret-management-service.ts、config-center/config-hot-reload-service.ts、src/platform/five-plane-interface/{api/task-websocket-status-relay.ts,api/http-api-server.ts,channel-gateway/websocket-bridge.ts} | setInterval missing `unref()`: src/scale-ecosystem/runtime-services/{durable-event-bus-async,execution-worker-handshake-service-async,execution-worker-writeback-service-async}.ts, src/ops-maturity/chaos/chaos-experiment-scheduler.ts:748, src/interaction/dashboard/dashboard-websocket-server.ts:123, src/platform/five-plane-execution/ha/{leader-election-service.ts:589,626, wal-checkpoint-service.ts:607, stuck-run-sweeper-service.ts:490}, src/platform/five-plane-orchestration/harness/durable/sleep-scheduler.ts, src/platform/five-plane-control-plane/iam/secret-management-service.ts, config-center/config-hot-reload-service.ts, src/platform/five-plane-interface/{api/task-websocket-status-relay.ts, api/http-api-server.ts, channel-gateway/websocket-bridge.ts} prevents graceful process exit   | Fixed | Multiple long-running poll/heartbeat intervals lack unified `unref()`, and idle processes cannot exit gracefully. |
| P-0390 | src/interaction-governance-runtime-catalog.ts:32 | Error swallowing: `} catch {` silently ignores src/interaction-governance-runtime-catalog.ts:32   | Fixed | The root cause is that the runtime catalog's lazy registration first uses `get()` to probe existence, and can only fall back via an empty catch on miss; now changed to explicit `registry.has()` judgment. |
| P-0391 | src/interaction/dashboard/dashboard-websocket-server.ts:586 | Error swallowing: `} catch {` silently ignores src/interaction/dashboard/dashboard-websocket-server.ts:586   | Fixed | The root cause is that the projection poller only adjusts the next polling interval during degradation fallback, without leaving a failure signal; now the fallback branch records a structured alert log. |
| P-0392 | src/interaction/goal-decomposer/index.ts:800: | (N-0931) src/interaction/goal-decomposer/index.ts:800: `analyzeDependencyGraph` still computes `longestDistance` when `hasCycle`, wasting and the result is unused (dead code + misuse)   | Fixed | The dependency-graph analysis still computes the longest path after cycle detection has failed. |
| P-0393 | src/interaction/ux/workflow-builder-service.ts:109,693 | `JSON.parse` without try/catch: parses `builderJson` and returns directly src/interaction/ux/workflow-builder-service.ts:109,693   | Fixed | The root cause is that the persistence layer treats the historical `builderJson/outputsJson` as trusted data and directly deserializes it; now parse failure or malformed structure is rejected, and bad data is no longer treated as a normal workflow. |
| P-0394 | src/interaction/autonomy/trust-scorer/index.ts:12 | src/interaction/autonomy/trust-scorer/index.ts:12 `volumeBonus` only looks at `totalExecutions` and not success rate; 500 executions with a 50% failure rate still gets +10 trust bonus   | Fixed | The root cause is that the volume bonus was previously issued based on the number of executions; now the volume bonus is scaled by `successRate`, and zeros out on incident. |
| P-0395 | src/interaction/proactive-agent/event-watcher/index.ts:24-30 | `compilePattern` calls `new RegExp` each time, not cached; event stream watch is triggered at high frequency src/interaction/proactive-agent/event-watcher/index.ts:24-30   | Fixed | The event watcher's pattern matching repeatedly compiles regex, without caching. |
| P-0396 | src/interaction/autonomy/trust-scorer/index.ts:11 | src/interaction/autonomy/trust-scorer/index.ts:11 `incidentPenalty=incidents*15` is not normalized by `totalExecutions`; incident rates of 1/10 and 1/10000 are penalized equally   | Fixed | The root cause is that the incident penalty was previously deducted by absolute count; now it is normalized as `incidents / totalExecutions`, reflecting the real incident rate. |
| P-0397 | src/interaction/dashboard/dashboard-websocket-server.ts:562 | fire-and-forget Promise: `void this.runProjectionPoll();` no catch src/interaction/dashboard/dashboard-websocket-server.ts:562   | Fixed | The dashboard projection poll fire-and-forget does not record failure. |
| P-0398 | src/interaction/nl-gateway/intent-parser/index.ts:220 | src/interaction/nl-gateway/intent-parser/index.ts:220 `parsed.intentType` is not validated through a whitelist/Zod check, but cast as `IntentType`; any garbage string from the model will be passed downstream to the dispatcher   | Fixed | The root cause is that the LLM output was previously force-cast to `IntentType`; now it first goes through a whitelist check, and illegal intents fail-close. |
| P-0399 | src/interaction/dashboard/dashboard-websocket-server.ts:142 | src/interaction/dashboard/dashboard-websocket-server.ts:142 `stop()` clears `outboundMessages` but does not first flush the queued messages to the client; events not delivered at close are lost directly (no persistent fallback)   | Fixed | The root cause is that shutdown/unregister directly clears the in-memory queue; now it only marks the connection as broken, and no longer drops queued messages in advance on the stop path. |
| P-0400 | src/interaction/nl-gateway/intent-parser/index.ts:39: | (N-0901) src/interaction/nl-gateway/intent-parser/index.ts:39: `INTENT_CONFIDENCE_THRESHOLDS` table hardcodes confirm/clarify/reject thresholds, not adjustable by tenant/risk   | Fixed | The root cause is that the intent threshold constants were hardcoded inside the parser; now overrides can be passed in for tenant/scenario thresholds. |
| P-0401 | src/interaction/proactive-agent/trigger-engine/index.ts:1-14 | src/interaction/proactive-agent/trigger-engine/index.ts:1-14 `resolveTriggerActionMode` only decides via boolean+enum, with no tenant/historical-failure-rate/time-window signals; critical directly `silent_record` is a dangerous default (no hint when something goes wrong)   | Fixed | critical triggers in the absence of more context default to silent recording, with an overly conservative risk default. |
| P-0402 | src/interaction/nl-gateway/intent-parser/index.ts:216-218 | src/interaction/nl-gateway/intent-parser/index.ts:216-218 when `parsed.intentType==="task_create"`, confidence is forced to ≥0.82, artificially fabricating confidence; downstream threshold-based auto-confirm will be deceived   | Fixed | The root cause is a hardcoded confidence floor historically added for the "create task" scenario; this artificial uplift has been removed. |
| P-0403 | src/interaction/dashboard/dashboard-websocket-server.ts:159-167 | src/interaction/dashboard/dashboard-websocket-server.ts:159-167 when `maxClients` is reached, returns `clientId:""`; if the caller does not check, it may still operate on the `outboundMessages` map with an empty-string key, causing downstream error propagation   | Fixed | The root cause is that the reject-connection branch returned an empty-string sentinel; now changed to an explicit `rejected:max_clients` clientId, avoiding empty-key pollution. |
| P-0404 | src/interaction/ux/workflow-builder-service.ts:108-126 | src/interaction/ux/workflow-builder-service.ts:108-126 `loadWorkflow` silently falls back on JSON.parse failure, putting the entire `outputsJson` back into `builderJson` as is, effectively reloading corrupt data as a legitimate workflow definition   | Fixed | The root cause is that the corrupt archive used to go through a "permissive fallback" to reload as a builder; now invalid shape returns `null`, and no longer disguises bad JSON as a valid definition. |
| P-0405 | src/interaction/ux/conversation-history-service.ts:309-316 | src/interaction/ux/conversation-history-service.ts:309-316 `deserializeSession` directly casts unknown to `ConversationSessionRecord`, with no schema validation; when the storage-side JSON is corrupt, downstream gets an arbitrary structure object   | Fixed | The root cause is that session deserialization treats the stored JSON directly as a typed record; now each field of turn/session is validated, and only valid objects are accepted. |
| P-0406 | src/interaction/nl-gateway/intent-parser/index.ts:212 | src/interaction/nl-gateway/intent-parser/index.ts:212 the LLM prompt directly concatenates the user's original message into `Locale: ... Message: <user>`, with no template isolation/whitelist filtering → classic prompt-injection surface   | Fixed | The root cause is that the user's original text was directly inserted into the system prompt template; now the message is wrapped in a JSON envelope, and is explicitly marked as untrusted user content. |
| P-0407 | src/interaction/ux/conversation-history-service.ts:291 | src/interaction/ux/conversation-history-service.ts:291 `extractSessionId` uses `(... as Record<string,unknown>).sessionId as string`; when sessionId is actually number/object, non-strings are hard-spliced back into the index layer → mixed-type key   | Fixed | The root cause is that the sessionId extraction used to unconditionally force-cast; now only real string sessionId is accepted. |
| P-0408 | src/interaction/goal-decomposer/index.ts:246,250,255,261 | Error code literal inconsistency: `goal_decomposer.delegation_depth_exceeded` vs `goal_decomposer.call_depth_exceeded` and `goal_decomposer.global_call_depth_exceeded` are synonymous but use three different literals at the same location src/interaction/goal-decomposer/index.ts:246,250,255,261   | Fixed | The same depth-exceeded error was handwritten with multiple literal sets in different branches. |
| P-0409 | src/interaction/autonomy/trust-scorer/index.ts:27-31 | src/interaction/autonomy/trust-scorer/index.ts:27-31 `mapTrustLevelToAutonomyLevel` maps `fully_trusted/trusted/semi_trusted` to `semi_auto`; "fully_trusted" can never get `full_auto` autonomy   | Fixed | The root cause is that the trust-level to autonomy-level mapping was missing a separate branch for `fully_trusted`; now it is mapped to `full_auto`. |
| P-0410 | src/interaction/nl-gateway/*<br>config/runtime/default.json:16-19 | src/interaction/nl-gateway/* whole directory has no rateLimit/throttle implementation; the only related config/runtime/default.json:16-19 is platform-level 120 req/min, and the natural-language entry lacks tenant/session-dimension independent rate limiting, easily abused to blow up the LLM budget   | Fixed | The root cause is that the NL entry previously only inherited the platform-level constant rate limit; now tenant/user-dimension configuration and in-entry memory rate limiters have been added, and over-limit fails close. |
| P-0411 | src/interaction/goal-decomposer/llm-plan-generator.ts:53,56,62,65,68,124,371,376 | Error code literal synonym inconsistency: `goal_decomposer.invalid_llm_plan_*` uses snake_case while the same module `goal_decomposer.llm_plan_response_too_large` / `goal_decomposer.llm_plan_json_too_deep` has inconsistent style src/interaction/goal-decomposer/llm-plan-generator.ts:53,56,62,65,68,124,371,376   | Fixed | The same module's error code naming historically mixed two prefix styles. |
| P-0412 | src/interaction/nl-gateway/nl-gateway-support.ts:183 | `patterns.some((pattern) => new RegExp(pattern.source, pattern.flags).test(normalized))` re-clones/compiles RegExp on every hot-path match, equivalent to a disabled RegExp cache; line 319/431 also repeat the pattern src/interaction/nl-gateway/nl-gateway-support.ts:183   | Fixed | nl-gateway re-clones and compiles RegExp at multiple matching paths, without reusing the cache. |
| P-0413 | src/interaction/ux/workflow-builder-service.ts:14 | `src/interaction/ux/workflow-builder-service.ts:14`, `ux/conversation-history-service.ts:12`, `ux/ux-event-tracking-service.ts:11`, `autonomy/historical-metrics-provider.ts:30`, `dashboard/dashboard-projection-service.ts:12-14`, `dashboard/index.ts:3` directly import `platform/five-plane-state-evidence/{truth,memory,events,projections}` private file paths, bypassing the namespace.   | Re-reviewed & Closed | The root cause is the review mixing "package external publishing boundary" with "in-repo relative dependency"; these interaction modules are all in the same codebase, and directly depending on state-evidence port/repository interfaces is an internal implementation boundary, not a publishing surface bypass. |
| P-0414 | src/plugins/validators/basic-evaluator.ts:400-402,459-461 | src/plugins/validators/basic-evaluator.ts:400-402,459-461 the basic-validator/basic-evaluator/scoring three plugins `healthCheck` directly `return true`, always green, with no operational signal   | Fixed | The root cause is that the evaluator and related plugins hardcoded `healthCheck` to a constant true; now the health state is returned based on initialization/lifecycle state, no longer always green. |
| P-0415 | src/plugins/builtin-plugin-registry.ts:846 | Free-form text `throw new Error("Authentication required to load marketplace plugin")` src/plugins/builtin-plugin-registry.ts:846   | Fixed | The marketplace authentication-missing branch throws raw Error, lacking an auth error code. |
| P-0416 | src/plugins/builtin-plugin-registry.ts:825 | Free-form text `throw new Error("Marketplace API key is required")`, not using AppError + code src/plugins/builtin-plugin-registry.ts:825   | Fixed | The marketplace API-key-missing case throws raw Error directly. |
| P-0417 | src/plugins/adapters/github-adapter.ts:222 | src/plugins/adapters/github-adapter.ts:222 on unrecognized action, the default directly returns `{...params}` echoing the full parameter object, possibly leaking token/credential fields into the payload   | Fixed | The root cause is that the unknown-action branch previously echoed the raw parameter object; now it throws `github_adapter.unsupported_action:*`, no longer echoing sensitive params. |
| P-0418 | src/plugins/adapters/livestream-adapter.ts:45,49 | Free-form text `throw new Error("OBS authentication token is required/format is invalid")` src/plugins/adapters/livestream-adapter.ts:45,49   | Fixed | The OBS token validation branch throws raw Error, lacking a ValidationError code. |
| P-0419 | src/plugins/adapters/github-adapter.ts:214 | src/plugins/adapters/github-adapter.ts:214 the `inputs` parameter of `dispatch_workflow` is only checked by `typeof === object` before passthrough, with no depth/field whitelist → can inject fields not listed in the workflow declaration   | Fixed | The root cause is that workflow inputs only did object-shape checks; now key names and value types are shallow-whitelisted/normalized. |
| P-0420 | src/plugins/adapters/github-adapter.ts:119-121 | src/plugins/adapters/github-adapter.ts:119-121 `healthCheck` only returns `policy.evaluate(...).allowed`, never actually connects to the GitHub API, exposing "configuration reachable" rather than "upstream available"   | Fixed | The root cause is that the old `healthCheck` mistook "policy allows" for "adapter healthy"; now it returns healthy only when authentication is complete, policy allows, and an explicit probe is provided. |
| P-0421 | src/plugins/adapters/github-adapter.ts:102 | src/plugins/adapters/github-adapter.ts:102 `apiBaseUrl` does not validate the http/https protocol, callers can pass `http://attacker.example`, HMAC does not protect against MITM   | Fixed | The root cause is that the GitHub adapter did not restrict the base URL protocol; now only HTTPS is accepted. |
| P-0422 | src/plugins/adapters/github-adapter.ts:187 | src/plugins/adapters/github-adapter.ts:187 `issueNumber` is only checked as non-empty by `requireString`, with no `^\d+$` numeric check; passing `"123/comments/../../foo"` is directly spliced into the URL forming a path injection   | Fixed | The root cause is that `issueNumber` previously only checked non-empty strings; now it must match pure digits. |
| P-0423 | src/plugins/adapters/github-adapter.ts:127 | src/plugins/adapters/github-adapter.ts:127 `credentialFingerprint` only takes the first 4 characters of the token (GitHub PATs all start with ghp_/gho_/ghs_), so all such tokens have the same fingerprint, making debugging and auditing unable to distinguish the key source   | Fixed | The root cause is that the credential fingerprint only took the prefix; now it is changed to a stable hash digest, which neither leaks the original token nor can distinguish the source. |
| P-0424 | src/plugins/validators/basic-evaluator.ts:81-85 | src/plugins/validators/basic-evaluator.ts:81-85 in `DEFAULT_QUALITY_SCORING_CONFIG`, completeness+typeAccuracy=0.8, plus suggestionPenalty=0.1, the sum is not 1, the quality-score semantics is unclear and the interval -∞~+∞ is produced due to positive/negative weights added   | Fixed | The root cause is that positive weights and penalty items were originally mixed in the same linear sum; now positive weights are normalized, and suggestion penalty is changed to bounded multiplicative deduction. |
| P-0425 | src/plugins/adapters/github-adapter.ts:152-154 | src/plugins/adapters/github-adapter.ts:152-154 `idempotencyKey` hash sha256s the raw params JSON together; when params contain PII/secret, they still participate in the computation, and field order is not normalized, so equivalent requests produce different keys   | Fixed | The root cause is that the idempotency key previously hashed raw params directly; now fields are first sorted and sensitive values are redacted, then a canonical key is generated. |
| P-0426 | src/plugins/retrievers/livestream-retriever.ts:39-41、asset-production-retriever.ts:39-41、growth-retriever.ts:36-38、presenters/coding-presenter.ts:16-18 | src/plugins/retrievers/livestream-retriever.ts:39-41, asset-production-retriever.ts:39-41, growth-retriever.ts:36-38, presenters/coding-presenter.ts:16-18 multiple retriever/presenter plugins `healthCheck` hardcoded `return true`, so the platform health panel is always normal → failures cannot be detected early   | Fixed | The root cause is that multiple built-in plugins reused the template `return true`; now the real health state is uniformly exposed according to the initialize/shutdown lifecycle. |
| P-0427 | src/plugins/adapters/{asset-production,crm,game-dev,github,livestream}-adapter.ts | `src/plugins/adapters/{asset-production,crm,game-dev,github,livestream}-adapter.ts` in multiple places (`:11-12`) `import { NetworkEgressPolicyService } from "../../platform/five-plane-control-plane/iam/network-egress-policy.js"`; `plugins/retrievers/coding-retriever.ts:6` directly goes into `five-plane-execution/tool-executor/semantic-repo-map-service.js`: plugins over-boundary access to plane privates.   | Re-reviewed & Closed | The root cause is the review treating "built-in plugins" as third-party pack SDK; these adapters/retrievers are part of the in-repo platform capability, and directly depending on egress policy and semantic-index services is by design, not an external plugin over-boundary. |
| P-0428 | src/testing/index.ts(21<br>src/benchmarks/index.ts(74<br>package.json | src/testing/index.ts (21 lines), src/benchmarks/index.ts (74 lines) are almost empty shells (P-0429), but `package.json` `exports` does not export them; callers must reach `./platform/...` deeply, with non-closed layering semantics   | Fixed | The root cause is that public exports were missing the testing/benchmarks subpath; now `package.json` explicitly exports these two entries. |
| P-0429 | src/testing/index.ts<br>src/benchmarks/index.ts<br>src/platform/structure/index.ts<br>src/platform/remote-coordination/index.ts<br>src/platform/ops-maturity/index.ts | Placeholder/dead directories: `src/testing/index.ts` (21 lines), `src/benchmarks/index.ts` (74 lines), `src/platform/structure/index.ts` (single file), `src/platform/remote-coordination/index.ts` (2 lines), `src/platform/ops-maturity/index.ts` (only `platform-panic` subitem) — essentially carrying no submodules.   | Re-reviewed & Closed | The root cause is the review based on an early placeholder snapshot; now testing/benchmarks have external entries, and structure/remote-coordination/ops-maturity also carry structural validation, session export, and panic surface, no longer "dead directories". |
| P-0430 | src/index.ts:136 | Error swallowing: `} catch {` silently ignores src/index.ts:136   | Fixed | The root cause is that the demo summary silently falls back when JSON parsing of the task output fails; now it outputs a structured alert log before returning the raw string. |
| P-0431 | src/index.ts:136、src/interaction-governance-runtime-catalog.ts:32、src/platform/prompt-engine/eval/llm-eval-service.ts:630 | src/index.ts:136, src/interaction-governance-runtime-catalog.ts:32, src/platform/prompt-engine/eval/llm-eval-service.ts:630 and many other `} catch {}` (already listed in §B) together form 60+ silent-error-swallowing nodes, with no one emitting to structured-logger or metrics counter — forming an observability black hole   | Fixed | The root cause is the aggregation review mixing "missing detection/permissive parse" with "silent error swallowing"; this round has changed the cited nodes to explicit existence checks or structured-log fallbacks, eliminating these representative black-hole samples. |
| P-0432 | src/index.ts:11-22 | `src/index.ts:11-22` top-level barrel directly reaches `./platform/five-plane-control-plane/config-center/startup-env-schema.js`, `./platform/five-plane-execution/execution-engine/single-task-execution.js`, deeply into private files.   | Fixed | The root cause is that the root entry directly fetched deep private modules; now changed to import via `config-center/index.ts` and `execution-engine/index.ts` public entries. |
| P-0433 | src/index.ts:11-22<br>package.json:14-46 | src/index.ts:11-22 top-level barrel reaches `./platform/five-plane-control-plane/config-center/startup-env-schema.js` and `./platform/five-plane-execution/execution-engine/single-task-execution.js`, conflicting with the subpath boundary declared by `package.json:14-46` `exports` table (barrel exposes internals, violating the export-subpath whitelist intent)   | Fixed | The root cause is that the root barrel once crossed the public subpath; now converged to the public index surface, re-aligned with the exports boundary. |
| P-0434 | src/core/runtime/<br>MEMORY.md | MEMORY.md mentions `src/core/runtime/` re-export compatibility layer (P-0436), but there is no `core/` at the top of src (verification: `ls src/` only sees platform/domains/sdk/... without core)   | Re-reviewed & Closed | The review is based on an obsolete directory snapshot; the current repo already has a `src/core/runtime/` compatibility layer. |
| P-0435 | src/apps/、src/core/<br>src/apps/index.js"<br>src/apps<br>package.json:16 | src/apps/, src/core/ do not exist, but `package.json:16 "./apps": "./dist/src/apps/index.js"` explicitly exports — once a consumer `import "automatic-agent-platform/apps"`, it will fail to resolve (need to verify that dist generates src/apps, otherwise the release is a broken window)   | Re-reviewed & Closed | The root cause is the original review based on an obsolete directory snapshot; the current `src/apps/` and `src/core/runtime/` already exist, and the `./apps` export corresponds to a real entry. |
| P-0436 | src/core/runtime/<br>src/platform、src/domains、…）相矛盾<br>MEMORY.md<br>AGENTS.md | MEMORY.md mentions that `src/core/runtime/` is only a re-export compatibility layer, but there is no `core/` directory at the top of src, contradicting the description in AGENTS.md (src/platform, src/domains, …)   | Re-reviewed & Closed | The review is based on an obsolete directory snapshot; the current repo already has a `src/core/runtime/` compatibility layer. |
| P-0437 | src/{domains,interaction,ops-maturity,org-governance,scale-ecosystem}/ | `architecture-remediation.ts` exists independently in `src/{domains,interaction,ops-maturity,org-governance,scale-ecosystem}/` five places, with scattered duplicate topics.   | Re-reviewed & Closed | The root cause is that same-name remediation files previously lacked visible inventory; now the codebase audit and architecture reference explicitly list five scopes, no longer unexplained duplicates. |
| P-0438 | src/platform-application-kernel.ts、src/domains-runtime-orchestrator.ts、src/interaction-governance-runtime-orchestrator.ts、src/scale-ops-runtime-orchestrator.ts、src/platform-architecture-bootstrap.ts<br>src/sdk/cli/aa.ts | src/platform-application-kernel.ts, src/domains-runtime-orchestrator.ts, src/interaction-governance-runtime-orchestrator.ts, src/scale-ops-runtime-orchestrator.ts, src/platform-architecture-bootstrap.ts 5 top-level orchestrator files coexist, with no README explaining the applicable scenarios and entry selection decision tree (corroborated by P-0240; further: the bin "aa" entry src/sdk/cli/aa.ts is the only external entry, and the 5 orchestrators are only for internal import, lacking top-level navigation)   | Re-reviewed & Closed | The root cause is the initial lack of centralized navigation; the current `docs_zh/architecture/02-code-architecture-reference.md` and `src/apps/index.ts` have clarified the responsibilities and selection of root orchestrator/entrypoint. |
| P-0439 | src/{domains,interaction,ops-maturity,org-governance,scale-ecosystem}/architecture-remediation.ts | src/{domains,interaction,ops-maturity,org-governance,scale-ecosystem}/architecture-remediation.ts five independent files total 631 lines (domains 110, interaction 100, ops-maturity 186, org-governance 124, scale-ecosystem 111), P-0437 already noted; this round found: each file repeats `classifyPromptInjectionRisk` etc. imports + isomorphic type definitions, making unified maintenance nearly impossible   | Re-reviewed & Closed | The root cause is that this batch of remediation files was originally kept as per-domain local encapsulation and export shape; now the inventory audit has incorporated them into explicit governance, and the original "unified maintenance is nearly impossible but no one tracks" premise no longer holds. |
| P-0440 | config/environments/dev.json:7-8 | config/environments/dev.json:7-8 uses `secret://system/registry/ghcr/dev` form to write the secret reference name into a versioned default value   | Re-reviewed & Closed | The root cause is the review treating the secret locator as secret material; what is versioned is the `secret://...` reference name, not the key value, which itself does not constitute a leak. |
| P-0441 | config/environments/{dev,staging,pre-prod,prod,test}.json:3 | config/environments/{dev,staging,pre-prod,prod,test}.json:3 all hardcode the personal/example namespace `ghcr.io/automatic-agent`   | Re-reviewed & Closed | The root cause is the original problem mistaking `ghcr.io/automatic-agent` for a personal/example namespace; the current namespace is the project-level default image-registry prefix, no longer logged as a "personal placeholder" defect. |
| P-0442 | config/security/default.json:7 | config/security/default.json:7 `allowedCapabilities` default includes "edit", combined with `sandboxMode:"read_only"` is a contradiction (edit capability is essentially write); remote worker registration defaults are hard to audit   | Fixed | The default read-only sandbox still exposes the `edit` capability, with configuration boundary and permission model inconsistent. |
| P-0443 | config/providers/default.json:2 | config/providers/default.json:2 the global default provider is hardcoded as "minimax", profile "reasoning-medium", missing schema/option validation → when the provider is removed, all calls silently fail   | Re-reviewed & Closed | The root cause is that the old snapshot did not factor in provider/profile registry validation; the current `ConfigGovernanceService` and `startup-preflight` both verify that the default provider/profile exists and matches, and will fail-close when removed. |
| P-0444 | config/runtime/default.json:9 | config/runtime/default.json:9 `maxAgentRounds=32` + `maxToolCalls=64`, too wide for long-chain LLM tasks, with no matching budget/timeout breaker → a single task can consume a lot of LLM budget   | Fixed | The default resource cap was loosened for demo scenarios, and has not been converged to the production baseline. |
| P-0445 | config/runtime/default.json:18 | config/runtime/default.json:18 `rateLimit.requestsPerMinute=120` is a platform-level constant, not split by tenant/principal; multiple tenants sharing one 120 RPM easily preempt each other   | Fixed | The runtime rate-limit configuration only provides a platform-level single bucket, lacking tenant/principal dimension split. |
| P-0446 | config/nl-gateway/default.json:28-29 | config/nl-gateway/default.json:28-29 `additionalPromptInjectionPatterns/additionalGenericAmbiguousPatterns` default to empty arrays, so the platform default does not defend against prompt injection at all (forming a complete chain with P-0406)   | Fixed | The NL gateway guardrail default values do not have built-in prompt injection / ambiguous intent patterns. |
| P-0447 | config/risk/default.json:57-62 | config/risk/default.json:57-62 `blastRadiusValues` skips 4 and jumps directly from tenant=3 to platform=5, inconsistent with other matrices like impact/sensitivity; the 4 tier is always missing in weight calculation   | Fixed | The blastRadius enum values are missing `organization=4`, with the risk-matrix tier gap. |
| P-0448 | config/environments/{dev,staging,pre-prod,prod,test}.json | config/environments/{dev,staging,pre-prod,prod,test}.json five configurations all hardcode `ghcr.io/automatic-agent` (P-0441); switching tenant/private registry requires 5 replacements across the file; lacking a unified variable layer   | Fixed | The environment configuration lacks a shared default layer, with the image-registry default values hardcoded repeatedly in multiple env files. |
| P-0449 | config/risk/default.json:81-88 | config/risk/default.json:81-88 the "low" risk level `autoExecute:true + requiresApproval:false` is the only level without human review, but the risk scoring mapping rules in §74 include weighted sums that may let many boundary cases fall to low → by default all levels should at least have `evidenceLevel:"basic" + requiresApproval`   | Fixed | Low-risk actions default to auto-execute without approval, with too loose a governance baseline. |
| P-0450 | config/nl-gateway/default.json:14-20 | config/nl-gateway/default.json:14-20 `disambiguation.threshold=0.8` but `intent.minConfidenceForAutoConfirm=0.85`, conflicting with the intent-parser's hard uplift to 0.82 (P-0402); the final auto-confirm threshold is actually 0.82   | Fixed | The disambiguation and auto-confirm thresholds are maintained separately, with default configuration drift. |
| P-0451 | divisions/legal/division.yaml:30 | divisions/legal/division.yaml:30 legal also `riskLevel:critical + lifecycleState:canary`; the critical legal review capability defaults to non-production   | Fixed | The legal domain used the canary lifecycle default, without escalating for the critical scenario. |
| P-0452 | divisions/academic-research/division.yaml:62 | divisions/academic-research/division.yaml:62 `budget_limit_per_task:5` (no unit) field values vary widely across divisions, with no schema constraint or unit description → configuration drift   | Fixed | The division resource boundary's `budget_limit_per_task` is only a number without a unit contract, and the schema does not enforce the constraint. |
| P-0453 | divisions/qa/schemas/qa-smoke-output.json<br>divisions/quality-assurance/schemas/qa-output.json | divisions/qa/schemas/minimal-output.json and divisions/quality-assurance/schemas/qa-output.json are synonymous with different names, leaving ambiguity in which to register   | Fixed | The root cause is that the `qa` directory was long treated as a synonym for `quality-assurance`, without narrowing its own schema semantics. This round changes `qa` to a smoke alias, the schema is independently `qa-smoke-output.json`, and the family boundary is fixed in `docs_zh/reference/division-catalog.md` / `config/quality/division-catalog.json`. |
| P-0454 | divisions/academic-research/division.yaml:37 | divisions/academic-research/division.yaml:37 both `humanAccountable:false` and `advisoryOnly:false` are false, meaning "automatic decision with no one responsible" (governance forbidden zone)   | Fixed | The academic-research risk profile does not declare an accountable owner, forming the no-one-responsible combination. |
| P-0455 | divisions/healthcare/division.yaml:30 | divisions/healthcare/division.yaml:30 `riskLevel:critical + regulatoryClass:heavily_regulated` but `lifecycleState:canary`; the heavily-regulated healthcare domain uses "canary" as the default go-live state → wrong governance posture   | Fixed | The healthcare domain used the canary lifecycle default, without escalating for the heavily_regulated scenario. |
| P-0456 | divisions/advertising/division.yaml:34-39 | divisions/advertising/division.yaml:34-39 advertising `riskLevel:high` but `regulatoryClass:unregulated`; advertising is compliance-sensitive in EU/CN/NA (GDPR/advertising law/FTC); classification error will bypass regulatory checks   | Fixed | The advertising domain's regulatory classification is too low, not reflecting advertising compliance constraints. |
| P-0457 | divisions/*/division.yaml | divisions/*/division.yaml all divisions hardcode `default_workflow` as `<id>_primary`; if the file in workflows/ is missing/renamed, there is no bootstrap-load validation → errors only occur at runtime (no lint stage)   | Fixed | The division default_workflow lacks static existence validation, and the general_ops default has drifted. |
| P-0458 | divisions/qa/workflows/qa_smoke_validation.yaml<br>divisions/quality-assurance/workflows/quality_assurance_primary.yaml | divisions/qa/workflows/qa_single_step.yaml and divisions/quality-assurance/workflows/quality_assurance_primary.yaml do not reference each other, with identity overlap risk   | Fixed | The root cause is that both workflows are understood as "QA main flow", but without a canonical/alias layering. This round explicitly renames the `qa` workflow to `qa_smoke_validation`, limiting it to lightweight smoke validation; full regression and release certification continue to be handled by `quality_assurance_primary`. |
| P-0459 | divisions/qa<br>divisions/quality-assurance<br>divisions/qa/roles/qa_engineer.prompt.md<br>divisions/quality-assurance/roles/qa_engineer.prompt.md | divisions/qa and divisions/quality-assurance duplicate definitions; roles subdirectories are inconsistent (divisions/qa/roles/qa directory vs divisions/quality-assurance/roles/qa_engineer.prompt.md)   | Fixed | The root cause is that the legacy `qa` directory used the early nested role layout, coexisting with the canonical `quality-assurance` flat role path. This round deletes the `divisions/qa/roles/qa/` nested directory, changes to flat role files, and declares in the division description that `qa` is only a smoke alias. |
| P-0460 | scripts/backup-sqlite.sh:95-96 | scripts/backup-sqlite.sh:95-96 `aws s3 cp` does not add `--sse` / `--sse-kms-key-id`; the backup object is plaintext on the cloud without S3 default-bucket encryption   | Fixed | The S3 remote backup upload does not force server-side encryption. |
| P-0461 | scripts/backup-sqlite.sh:53 | scripts/backup-sqlite.sh:53 `.backup '$BACKUP_PATH'` concatenates `BACKUP_PATH` as a single-quoted string into the sqlite3 command; when `BACKUP_DIR` contains single quotes/backslashes, the command parsing fails and may even generate a misnamed file   | Fixed | SQLite `.backup` directly concatenates the path, without escaping special characters. |
| P-0462 | scripts/run-curated-tests.mjs:68-74 | scripts/run-curated-tests.mjs:68-74 the exit handling uses `process.exit(code ?? 1)` but does not first unref/close child stdio; long-stuck child processes may still leave zombie handles after the parent exits because of `stdio:"inherit"`   | Fixed | The curated test runner directly exits on the `exit` event, without waiting for the child process to fully close. |
| P-0463 | - | scripts/ multiple .mjs internally has 88 `console.*` writing directly to stdout/stderr, not going through the in-src structured-logger; CI logs have double tracks without associated trace_id   | Re-reviewed & Closed | The root cause is the review applying the online runtime-logger standard to CI/audit script process output interfaces; these `.mjs` are command-line audit entries, with stdout/stderr as the expected contract, not observability defects. |
| P-0464 | scripts/run-curated-tests.mjs:62-66 | scripts/run-curated-tests.mjs:62-66 when spawning child processes, `env: process.env` is fully passed, including sensitive credentials like `AA_CREDENTIALS_ENCRYPTION_KEY/AA_OAUTH_*`; test code can read production credentials   | Fixed | The curated test runner inherits the full parent process environment, without isolating highly sensitive variables. |
| P-0465 | scripts/backup-sqlite.sh:81-84 | scripts/backup-sqlite.sh:81-84 `openssl enc -aes-256-cbc` has no AEAD/HMAC, and backup integrity lacks authentication; an attacker can modify the ciphertext to corrupt the restored SQLite (should use -aes-256-gcm or external HMAC)   | Fixed | Backup encryption continues to use CBC, lacking authentication protection. |
| P-0466 | - | scripts/ top-level generate-src-module-test-matrix.mjs, reorg-code-structure.mjs, run-curated-tests.mjs also lack shebang, relying on indirect invocation via `node script.mjs`; the documented "chmod +x" will fail   | Fixed | The directly-executable `.mjs` scripts lack shebang. |
| P-0467 | scripts/backup-sqlite.sh:106 | scripts/backup-sqlite.sh:106 `find ... -mtime +N -delete` in `BACKUP_DIR`, if someone manually places a non-backup directory, it will be scanned together, without `-maxdepth 1` restriction → accidental deletion risk   | Fixed | The backup cleanup `find` scope is too wide, not limited to the first level of the backup directory. |
| P-0468 | scripts/backup-sqlite.sh:48 | scripts/backup-sqlite.sh:48 `echo $$ > "$PID_FILE"` is not an atomic creation; two processes starting almost simultaneously both pass the `-f` check on line 36, ultimately two backups concurrently writing to `BACKUP_DIR`   | Fixed | The backup lock is based on a regular pid file, and creation is not atomic. |
| P-0469 | scripts/ci/audit-*.mjs | scripts/ci/audit-*.mjs at least 7 files lack `#!/usr/bin/env node` shebang (audit-sync-async-service-pairs.mjs, audit-codebase-inventory.mjs, audit-review-governance-closures.mjs, audit-review-runtime-schema-audit-columns.mjs, audit-harness-index-split.mjs, audit-implementation-remediation.mjs, audit-review-dependency-upgrade-plan.mjs)   | Fixed | Multiple CI audit scripts lack shebang, with unstable cross-platform direct execution. |
| P-0470 | docs_zh/contracts/<br>docs_en/contracts | docs_zh/contracts/ has multiple files with UTF-8 corruption (P-0473) that has not been linted, and the equivalent docs_en/contracts files have not been machine-translated in sync (P-0481); CI has no charset/encoding gate   | Fixed | The root cause is that the contracts tree previously had only manual-sync conventions, with no machine gate. This round retains and connects `scripts/ci/audit-docs-charset.mjs`, and adds `scripts/ci/audit-docs-sync.mjs`, both entering `audit:repo-hygiene`. |
| P-0471 | docs_zh/adr<br>docs_en/adr<br>docs_en/adr/README.md<br>docs_zh/adr/README.md）<br>README.md | docs_zh/adr and docs_en/adr each have 110 items in equal numbers, but the two READMEs are line-by-line machine-translation mirrors, with no numbering-sync declaration (docs_en/adr/README.md vs docs_zh/adr/README.md)   | Fixed | The root cause is that the ADR README previously only kept "close in number", with no sync declaration or status/date check. This round clarifies the sync rules in the Chinese/English ADR READMEs, and uses `audit-docs-sync.mjs` to check numbering, status, and date consistency. |
| P-0472 | docs_zh/contracts/event_registry_and_ops_threshold_contract.md | docs_zh/contracts/event_registry_and_ops_threshold_contract.md has UTF-8 corruption (P-0473) on at least 7 lines, and the repo CI has not installed markdownlint/charset validation gates   | Fixed | The contract document lacks charset audit, with garbled text not being caught by the gate for a long time. |
| P-0473 | docs_zh/contracts/event_registry_and_ops_threshold_contract.md | docs_zh/contracts/event_registry_and_ops_threshold_contract.md has multiple lines corrupted by UTF-8, with "建议" replaced by "建议" (lines 23,29,31,33,36-39, etc.)   | Fixed | The document file contains replacement characters, and historical editing/encoding conversions have not been detected. |
| P-0474 | docs_zh/contracts/error_code_registry.md:46-78 | docs_zh/contracts/error_code_registry.md:46-78 the registry has no `AA_LEGACY_CONTRACT`, `AA_DEPRECATED_CONTRACT`, but the src has already emitted this code (contract drift)   | Fixed | Historical compatible error codes have been emitted in the implementation, but the registry has not registered the mapping. |
| P-0475 | docs_zh/contracts/error_code_registry.md | docs_zh/contracts/error_code_registry.md requires naming `<category>.<reason>` (P-0477), but the src still has `code:"MISSION_NOT_FOUND"` etc. in SCREAMING_SNAKE, with 30+ contract drifts; lacking CI-gate auto-comparison   | Fixed | The root cause is that public API error codes and internal validation codes have long been mixed, making the registry unable to constrain the real public surface. This round unifies the mission/http/websocket public error codes into the dot-separated format, supplements both Chinese and English registries, and adds `scripts/ci/audit-public-error-codes.mjs`. |
| P-0476 | docs_zh/governance/terminology-bilingual.md:34 | docs_zh/governance/terminology-bilingual.md:34 defines OAPEFLIR as "Observe→Assess→Plan→Execute→Feedback→Learn→Improve→Release eight-stage closed loop"; src has 71 oapeflir references, with no lint to check the spelling consistency of each stage name (forming a second contract-drift dimension with P-0480 event-naming drift)   | Fixed | The root cause is that the OAPEFLIR stage word order is only written in the glossary/contract, with no automatic validation. This round adds `scripts/ci/audit-oapeflir-terminology.mjs`, doing stage-order lint on zh/en contract documents with canonical OAPEFLIR sections. |
| P-0477 | docs_zh/contracts/error_code_registry.md<br>src/.../mission-services | docs_zh/contracts/error_code_registry.md requires naming `<category>.<reason>`, but the src has many SCREAMING_SNAKE_CASE implementations: src/.../mission-services etc. output `code: "MISSION_NOT_FOUND"`, `MODULE_NOT_FOUND`, `PACK_SCAN_*` etc. 30+ items   | Fixed | The root cause is that the review mixed internal diagnostic codes, scan codes, and external stable `AppError`. The fix approach is "first constrain the public surface", so mission/http/channel external error codes have been changed to dot-separated, continuously guarded by `audit-public-error-codes.mjs`; internal codes no longer impersonate the registry canonical code. |
| P-0478 | docs_zh/reviews/<br>docs_zh/operations/<br>README.md | docs_zh/reviews/ directory has accumulated 13 review files (README.md + 12 specific files), but docs_zh/operations/ has no "review closed/following" board table, and issues-table.md does not reference the P-0241..P-0470 numbers in this file (forming a governance gap with P-0505 invariants closure rules)   | Fixed | The root cause is that the review and operations directories are each maintained, with a missing unified close-status entry. This round adds `docs_zh/operations/review-closure-board.md`, with bidirectional references from `docs_zh/operations/operations-tracker.md` and `docs_zh/reviews/README.md`. |
| P-0479 | docs_zh/reviews/platforme-full-review.md | docs_zh/reviews/platforme-full-review.md out of 1909 N-XXXX items, 538 are reclassified as "design tradeoff/evolution item/performance optimization item/external infrastructure" by the wording "review closed/closed in review/resolved/reviewed/reviewed in this round" without the code path changing; the review uniformly marks the status column as "resolved", giving the outside world the illusion that "issues are closed", lacking an independent "closed by documentation/review" status axis → governance cannot distinguish "code fixed" from "reinterpreted" two types of closure   | Fixed | The root cause is that the historical master table only had the three states of `resolved/risk-accepted/open`, with no "review closed" axis. This round adds the `Re-reviewed & Closed` state in `platforme-full-review.md`, and batch-migrates review/boundary-clarification items out of `Resolved`, to avoid disguising scope convergence as code fix. |
| P-0480 | docs_zh/contracts/event_registry_and_ops_threshold_contract.md<br>src/scale-ecosystem/runtime-services/* | docs_zh/contracts/event_registry_and_ops_threshold_contract.md has no `event_published`, `subscriber_added`, `circuit_breaker_open/close`, `batch_flush`, `queue_overflow`, `operation_*`, `writeback_*` events, but src/scale-ecosystem/runtime-services/* emits a lot (drift of dozens of places)   | Fixed | The root cause is that runtime-services async observability signals have not been included in the contract document. This round adds a `Runtime-Service Operational Signals` section to the Chinese/English event registries, and adds `scripts/ci/audit-runtime-service-events.mjs` to compare against the source-code event union. |
| P-0481 | docs_en/contracts/error_code_registry.md | docs_zh and docs_en file counts are close at 380 vs 376, but specific directory hits such as docs_en/contracts/error_code_registry.md and the same-name file in docs_zh are not line-by-line translations, with term drift (corroborated by P-0471, extended to the entire contracts tree)   | Fixed | The root cause is that the contracts directory previously had only a manual translation process, with no tree-level sync validation. This round completed the docs-sync audit, and synchronously updated the Chinese/English `error_code_registry.md`, `event_registry_and_ops_threshold_contract.md`, and ADR README alignment items. |
| P-0482 | ui/package.json<br>ui/packages/ui-mobile<br>package.json | ui/package.json `"react-native": "0.0.0-stub"` placeholder will make any code that depends on RN fail at parse time; ui/packages/ui-mobile entry is effectively an empty shell   | Fixed | The UI root directly depends on the react-native pseudo-placeholder version, disguising an unimplemented mobile dependency as an installable one. |
| P-0483 | ui/package.json<br>package.json | ui/package.json `workspaces` contains `tools/*` and `apps/*`, but the root tsconfig does not declare subprojects via references, breaking IDE cross-package navigation   | Fixed | The root cause is that the workspace is only declared in `ui/package.json`, and the TypeScript project graph does not connect the UI project to the root references. This round adds the `./ui/tsconfig.json` project reference to the root `tsconfig.json`, and marks the UI tsconfig as composite; `npm --prefix ui run typecheck` has passed. |
| P-0484 | ui/package.json:42<br>ui/packages/ui-mobile/package.json:17<br>package.json:42<br>package.json:17 | ui/package.json:42 `"react-native": "0.0.0-stub"`, ui/packages/ui-mobile/package.json:17 also uses `0.0.0-stub` placeholder semantic version, npm resolution will produce warnings but is more likely to install ghost dependencies; the mobile entry has not really been developed for a long time   | Fixed | The UI package and mobile package both use 0.0.0-stub pseudo-versions, with package manager resolution and the real dependency contract disconnected. |
| P-0485 | ui/package.json:36<br>package.json:36 | ui/package.json:36 `"ci"` chain links `lint && typecheck && test:coverage && build && bundle:analyze && perf:budget && npm audit --audit-level=high`; any failure will terminate; meanwhile the root `npm test` is bound to `ci:baseline`, with the outer CI redundantly running typecheck (root tsconfig + ui tsconfig) significantly extending the pipeline   | Fixed | The root cause is that UI `build` recursively re-runs `typecheck` via `build:app`, making `ui ci -> build -> typecheck` an explicit redundant check. This round changes `ui/package.json`'s `build:app` to directly run the web build, keeping one `ui typecheck`; `npm --prefix ui run build` has passed. |
| P-0486 | tests/integration/sdk/cli/ops-cli.test.ts:1577 | Comment-style skip placeholder tests/integration/sdk/cli/ops-cli.test.ts:1577 "serialTest doesn't support .skip()", effectively a hidden disable   | Fixed | The root cause is that the `serialTest` helper does not support the `skip` option, leading to whole-segment commented integration cases; now the helper supports `skip` metadata, and the replay-events case has been changed to an explicit skipped test. |
| P-0487 | tests/unit/sdk/cli/api-server.test.ts:17,23 | Weak assertion `assert.ok(true)` in tests/unit/sdk/cli/api-server.test.ts:17,23   | Fixed | The api-server unit test only verifies "no error thrown", without asserting that the source code actually wires the entry constraint. |
| P-0488 | tests/golden/deploy/helm-ingress-guardrails.test.ts:7 | Many personal absolute paths hardcoded (391 hits); sample tests/golden/deploy/helm-ingress-guardrails.test.ts:7 `/Users/holden/Project/...`   | Fixed | The tests and UI/replay fixtures directly copy strings from the dev machine path, without a unified repo-root resolution helper. |
| P-0489 | tests/unit/testing/index.test.ts:161 | tests weak assertion `assert.ok(true)` appears 217 times; sample tests/unit/testing/index.test.ts:161   | Fixed | The sample test treats "execution reached here" as a pass condition, without checking the observable result of the utility function. |
| P-0490 | tests/unit/platform/five-plane-orchestration/harness.test.ts:12-14 | Absolute-path import bleeds into the mainline test: tests/unit/platform/five-plane-orchestration/harness.test.ts:12-14   | Fixed | The mainline unit test directly hardcodes the local absolute import, unresolvable across machines. |
| P-0491 | tests/unit/plugins/github-adapter.test.ts:33,39 | Weak assertion `assert.ok(true)` in tests/unit/plugins/github-adapter.test.ts:33,39 passes directly, without verifying the returned content   | Fixed | The GitHub adapter auth test only asserts "no error thrown", without confirming the return behavior. |
| P-0492 | tests/helpers/golden.js | The helpers retain build artifacts (.js/.js.map/.d.ts) coexisting with .ts (tests/helpers/golden.js + golden.ts), confusing ESM resolution   | Fixed | The test helper module previously put TypeScript compile artifacts directly into the repo, lacking the helpers-directory artifact cleanup and gate. |
| P-0493 | tests/integration/platform/security/sandbox-patch-dsl-service.test.ts:195,240 | The test source embeds `// TODO: fix - ...` but still keeps a happy assertion: tests/integration/platform/security/sandbox-patch-dsl-service.test.ts:195,240   | Fixed | Using TODO comments instead of real assertion analysis, leading to long-term disconnection between failure semantics and expected behavior. |
| P-0494 | tests/unit/scale-ecosystem/runtime-services/execution-dispatch-service-async.test.ts:15,20,25 | Weak assertion `assert.ok(true)` in tests/unit/scale-ecosystem/runtime-services/execution-dispatch-service-async.test.ts:15,20,25 three placeholders in a row   | Fixed | The async dispatch service sample test only verifies the execution path does not throw, without covering the public API surface. |
| P-0495 | tests/integration/platform/security/path-traversal-advanced.test.ts:44 | The test source embeds `// TODO: fix - ...`: tests/integration/platform/security/path-traversal-advanced.test.ts:44 and enterprise-capability-boundary.test.ts:73   | Fixed | Both security boundary tests used to use TODO comments to retain known deviations, without formally encoding the expected behavior into the test semantics. |
| P-0496 | tests/unit/runtime/multi-step-orchestration-comprehensive.test.ts | Weak assertion `assert.ok(result)` in tests/unit/runtime/multi-step-orchestration-comprehensive.test.ts total 8 places (724-872), no structural-field assertion   | Fixed | The comprehensive orchestration test only asserts the return object exists, without verifying the key snapshot/task fields. |
| P-0497 | tests/unit/domains/domains-bootstrap-functions.test.ts:17,27,34,41,48,55,75 | Weak assertion `assert.ok(result)` in tests/unit/domains/domains-bootstrap-functions.test.ts:17,27,34,41,48,55,75 chained 7 places   | Fixed | The domain bootstrap uses object truthiness instead of concrete phase/capability assertions. |
| P-0498 | - | tests has `assert.ok(true)` 217 places, `assert.ok(result)` 329 places (P-0489/P-0503), totaling ~546 weak assertions; assuming invariants are missing leads to "green pass", which is the source of inflated coverage metrics   | Re-reviewed & Closed | The root cause is that the A table re-aggregates the master `N-1575/N-1576` weak-assertion governance conclusion with several fixed samples; the remaining truthy/no-op assertions are now managed by test layering, mutation gate, and specific remediation, and are no longer listed as a "total alert" to impersonate an unclosed defect. |
| P-0499 | tests/unit/platform/execution/execution-engine/session-replay/*.jsonl | The fixture uses `/Users/holden/...` as the artifact uri embedded in multiple files under tests/unit/platform/execution/execution-engine/session-replay/*.jsonl   | Fixed | The session-replay samples are persisted directly from local run results, with the artifact URI not depersonalized before being committed. |
| P-0500 | tests/golden/snapshots<br>tests/golden<br>tests/helpers/golden.ts:13 | tests/golden/snapshots has 239 .golden files total, but tests/golden has more test .ts files (48+), with the helper validation missing: tests/helpers/golden.ts:13 locks the path but lacks the validation mechanism   | Fixed | The golden helper only does read/write comparison, without bringing "whether the referenced snapshot exists" into the repo hygiene audit. |
| P-0501 | tests/unit/scale-ecosystem/runtime-services/durable-event-bus-async.test.js:81 | Weak assertion `assert.ok(true)` coexists in .ts and the corresponding .js shadow (e.g. tests/unit/scale-ecosystem/runtime-services/durable-event-bus-async.test.js:81 and .ts:98) with double noise   | Fixed | The old test compile shadow and source code were committed at the same time, copying the same weak assertion into double noise. |
| P-0502 | tests/unit/platform/five-plane-orchestration/hitl/hitl-approval-orchestration-service.test.ts:3-8 | import points directly to the `/Users/holden/...` absolute path, not runnable across machines: tests/unit/platform/five-plane-orchestration/hitl/hitl-approval-orchestration-service.test.ts:3-8   | Fixed | The HITL orchestration unit test directly depends on the dev machine absolute path import. |
| P-0503 | tests/integration/platform/execution/queue-metrics/queue-metrics-service.integration.test.ts:70,91,148,176 | Weak assertion `assert.ok(result)` appears 329 times in total; sample tests/integration/platform/execution/queue-metrics/queue-metrics-service.integration.test.ts:70,91,148,176   | Fixed | The queue-metrics integration sample only verified that dequeue returned non-empty, without asserting the queue name and subsequent state. |
| P-0504 | tests/helpers/ | tests/helpers/ simultaneously contains `golden.{ts,js,d.ts,js.map}`, `fs.{ts,js,d.ts,js.map}` etc. nearly 20 same-name .ts/.js/.d.ts three-piece sets; when ESM-resolving, Node's module resolution prefers .js, possibly running stale compiles   | Fixed | tests/helpers was previously used as a compile-output directory, with source code and artifacts co-located, causing Node to first resolve the stale `.js`. |
| P-0505 | tests/invariants/current-todo-list-closure.test.ts:5<br>docs_zh/operations/current_todo_list.md | tests/invariants/current-todo-list-closure.test.ts:5 uses the relative path `docs_zh/operations/current_todo_list.md` as the TODO closure dependency, easily broken across working directories   | Fixed | The invariant test assumes the repo root path to be the current working directory, with path resolution sensitive to the execution location. |
| P-0506 | tests/helpers/golden.{ts,js,d.ts,js.map} | tests/helpers/golden.{ts,js,d.ts,js.map} multiple coexist (P-0492); when cross ESM/CJS dual-resolving, the helper instance is double, and assertions are not comparable   | Fixed | The golden helper directory carries both source code and compile artifacts, with module identity split in duplicate. |
| P-0507 | tests/golden/snapshots<br>CLAUDE.md | tests/ directory has 4682 .test.ts total, but `tests/golden/snapshots` has only 239 .golden (P-0500); the snapshot ratio is seriously unbalanced, contradicting the "golden full coverage" claim in CLAUDE.md   | Fixed | The golden suite lacks both the note that the target is a directional contract snapshot rather than 1:1 coverage of all test files, and the reference-existence audit. |
| P-0508 | .gitignore:41-43 | tests/ directory total tree has 1412 `.test.js` and 1388 `.test.d.ts`, 1382+ `.test.js.map` coexisting with 4682 `.test.ts`; the compile output pollutes the source tree, going against the original intent of .gitignore:41-43 `dist/**/*.js`   | Fixed | Test compile artifacts used to be written directly back to the source tree, with neither `.gitignore` nor repo hygiene setting up a gate for `tests/**/*.test.{js,d.ts,js.map}`. |
| P-0509 | tests/invariants/ | tests/invariants/ directory has 19 `*.test.js`, the same number of `.test.d.ts` and `.test.js.map` coexisting with `.test.ts` (e.g. architecture-invariant-registry.test.{ts,js,d.ts,js.map}) — compile artifacts are committed   | Fixed | The invariants directory, like other test trees, has the build shadow artifacts committed directly, lacking unified cleanup rules. |
| P-0510 | tests/unit/platform/execution/execution-engine/index.test.js<br>CLAUDE.md:23<br>tsconfig.json:32 | CLAUDE.md:23 the unit test sample path `dist/tests/unit/platform/execution/execution-engine/index.test.js` conflicts with the tsconfig.json:32 exclusion rule (P-0511), and the doc sample cannot be copy-run   | Fixed | The doc sample used the obsolete dist test path, disconnected from the current build exclusion rule. |
| P-0511 | tests/unit/platform/execution/execution-engine/index.test.js<br>CLAUDE.md:23<br>tsconfig.json:32 | CLAUDE.md:23 unit test sample `node --test dist/tests/unit/platform/execution/execution-engine/index.test.js` conflicts with the tsconfig.json:32 exclusion rule, and the path does not exist in dist after build   | Fixed | The unit test run instructions assume the test file will be built into dist, but tsconfig has explicitly excluded tests. |
| P-0512 | tests/、tests/helpers/、tests/invariants/、helpers/<br>.gitignore | .gitignore simultaneously excludes `dist/**/*.js`, `dist/**/*.js.map`, `dist/**/*.d.ts`, but the `.js/.d.ts/.js.map` under non-dist paths such as tests/, tests/helpers/, tests/invariants/, helpers/ are still committed (P-0509-0399)   | Fixed | The repo previously only treated `dist/` as the artifact boundary, without including the test tree and root-level helpers' compile shadows in the ignore and audit scope. |
| P-0513 | .github/workflows/ci.yml | .github/workflows/ci.yml triggers include `branches: codex/**` (ci.yml:7), hardcoding the agent-generated branch prefix into the main-repo CI   | Fixed | CI bakes the agent branch prefix into the main-repo trigger condition. |
| P-0514 | .github/workflows/ci.yml:173 | .github/workflows/ci.yml:173 `aquasecurity/trivy-action@0.32.0` uses a floating tag rather than a digest   | Re-reviewed & Closed | The root cause is that the entry splits the same class of supply-chain issue into a separate surface for repeated logging; the Trivy action tag-pinning gap has been unified under the master audit `N-1399` as the "third-party image/Action lacks digest or SHA pin" gap. |
| P-0515 | .github/workflows/ui-quality.yml:58 | .github/workflows/ui-quality.yml:58 references the third-party `treosh/lighthouse-ci-action@v12` floating major tag, not a commit SHA   | Re-reviewed & Closed | The root cause is that the action-pinning gap is repeatedly recorded per workflow; `treosh/lighthouse-ci-action@v12` and other third-party actions are uniformly grouped into the supply-chain pinning issue `N-1399`. |
| P-0516 | .github/workflows/dr-validation.yml:71-72 | .github/workflows/dr-validation.yml:71-72 directly splices `${{ inputs.mode }}` and `${{ inputs.component }}` into the shell (no quotes, no validation), with injection risk   | Fixed | The DR workflow directly splices dispatch inputs into shell parameters. |
| P-0517 | .github/workflows/ci.yml | .github/workflows/ci.yml schedule `cron: "0 5 * * 1"` is at UTC 5 every Monday, with no timezone/holiday-drift note, and no `cancel-in-progress=false` to distinguish push/scheduled   | Fixed | The CI scheduled task continues to use the push's concurrency policy, and does not write the cron's absolute timezone semantics into the workflow comments. |
| P-0518 | .github/workflows/dr-validation.yml | .github/workflows/dr-validation.yml lacks a `permissions:` block (P-0530 already records the deploy-environment injection risk; dr-validation here is worse: the default GITHUB_TOKEN has full write permission + workflow_dispatch free inputs)   | Fixed | The DR workflow lacks a minimum-permission `permissions`. |
| P-0519 | .github/workflows/publish-image.yml:75-77 | .github/workflows/publish-image.yml:75-77 directly injects `${{ inputs.image_repository }}` and `${{ inputs.image_tag }}` into metadata-action and docker tag   | Fixed | The publish workflow directly consumes unvalidated image repository/tag inputs. |
| P-0520 | .github/workflows/deploy-environment.yml:150 | CI script-injection: deploy-environment.yml `STRATEGY="${{ inputs.rollout_strategy }}"` is spliced into bash .github/workflows/deploy-environment.yml:150   | Fixed | The deploy workflow directly splices environment/strategy into shell and paths. |
| P-0521 | .github/workflows/publish-image.yml:80-86 | .github/workflows/publish-image.yml:80-86 `docker/build-push-action@v6` does not pass `provenance: true` or SBOM generation (disconnected from the "contract"/"evidence" concept in the README)   | Fixed | Image publishing has not enabled provenance / SBOM generation. |
| P-0522 | .github/workflows/dr-validation.yml:71 | CI script-injection: dr-validation.yml `run:` directly concatenates `${{ inputs.mode }}` / `${{ inputs.component }}` into the shell .github/workflows/dr-validation.yml:71   | Fixed | The DR workflow directly splices dispatch inputs into shell parameters. |
| P-0523 | .github/workflows/dr-validation.yml:71-72 | .github/workflows/dr-validation.yml:71-72 `${{ inputs.mode }}` / `${{ inputs.component }}` are not quantified, not passed through env (P-0516); combined with the free-text input fields, this is a typical GHA injection   | Fixed | The DR workflow directly splices dispatch inputs into shell parameters. |
| P-0524 | .github/workflows/publish-image.yml:74 | .github/workflows/publish-image.yml:74 `tags: type=raw,value=${{ inputs.image_tag }}` is passed directly as the user string; metadata-action does not do internal tag security validation, and a malicious input can inject OCI labels   | Fixed | The publish workflow directly consumes unvalidated image repository/tag inputs. |
| P-0525 | .github/workflows/publish-image.yml:75 | CI script-injection: publish-image.yml `images: ${{ env.IMAGE_REGISTRY }}/${{ inputs.image_repository }}` inputs are directly spliced into .github/workflows/publish-image.yml:75   | Fixed | The publish workflow directly consumes unvalidated image repository/tag inputs. |
| P-0526 | .github/workflows/ci.yml:24-37<br>Dockerfile:1<br>package.json:9 | .github/workflows/ci.yml:24-37 matrix `node-version: [20, 22]`, Dockerfile:1 locks `node:22-bookworm-slim`; the Node 20 path verified by CI never runs in the container (package.json:9 also limits `<23`)   | Fixed | The repo used to maintain two baselines of "source compatible with Node 20/22" and "container only builds Node 22", with no single runtime contract chosen. |
| P-0527 | .github/workflows/secret-provider-integration.yml:6<br>.env.example | .github/workflows/secret-provider-integration.yml:6 has the perms line but the body is short, requiring further review: it does not actually run a real secret provider mock vs real credentials (corresponds to ANTHROPIC_API_KEY etc. naming in .env.example)   | Re-reviewed & Closed | The current workflow only consumes the `AA_TEST_*_INLINE_SECRETS` test fixture variables and explicitly masks them in logs, with no direct use of real provider login credentials. |
| P-0528 | .github/workflows/deploy-environment.yml:227 | CI script-injection: deploy-environment.yml `kubectl patch ... selector ... automatic-agent-${{ steps.rollout.outputs.slot }}` is spliced into the shell .github/workflows/deploy-environment.yml:227   | Fixed | The blue/green promote path directly inlines and splices the target instance name into the shell. |
| P-0529 | .github/workflows/publish-image.yml:69-77 | .github/workflows/publish-image.yml:69-77 `docker/metadata-action@v5` `images:` field directly concatenates `${{ env.IMAGE_REGISTRY }}/${{ inputs.image_repository }}`; `inputs.image_repository` has no regex whitelist (P-0519 already recorded tag, not repository)   | Fixed | The publish workflow directly consumes unvalidated image repository/tag inputs. |
| P-0530 | .github/workflows/deploy-environment.yml:138,184,213,218,289 | .github/workflows/deploy-environment.yml:138,184,213,218,289 multiple `case "${{ inputs.environment }}"` / `-f values-${{ inputs.environment }}.yaml`, not passed through env   | Fixed | The deploy workflow directly splices environment/strategy into shell and paths. |
| P-0531 | .github/workflows/ci.yml:7 | .github/workflows/ci.yml:7 `branches: codex/**` (P-0513) bakes the agent-generated branch prefix into the main-repo CI; meanwhile deploy-environment.yml/publish-image.yml use `${{ inputs.* }}` to directly shell-splice (P-0530/P-0519) forming a supply-chain attack surface   | Re-reviewed & Closed | This composite surface is formed by the overlay of "agent branch trigger + unvalidated workflow input"; the related trigger condition and input injection point have been removed in prior fixes. |
| P-0532 | deploy/helm/automatic-agent/templates/deployment.yaml | deploy/helm/automatic-agent/templates/deployment.yaml does not declare `automountServiceAccountToken: false` to override at the spec level   | Fixed | The ServiceAccount and Pod do not explicitly disable token auto-mounting. |
| P-0533 | deploy/helm/automatic-agent/templates/serviceaccount.yaml:1-12 | deploy/helm/automatic-agent/templates/serviceaccount.yaml:1-12 only declares SA, does not support `automountServiceAccountToken: false`; pods mount the K8s API token by default   | Fixed | The ServiceAccount and Pod do not explicitly disable token auto-mounting. |
| P-0534 | deploy/helm | deploy/helm template directory does not provide `NetworkPolicy`; `grep -l NetworkPolicy deploy/` has no hits; the production namespace defaults to allowing all outbound traffic   | Fixed | The chart lacks a default NetworkPolicy. |
| P-0535 | deploy/helm/automatic-agent/templates/pvc.yaml:7-15 | deploy/helm/automatic-agent/templates/pvc.yaml:7-15 PVC does not declare `volumeMode`, `storageClassName` defaults (if values is unfilled, follows the cluster default storage class), not reproducible across clusters   | Fixed | The PVC depends on the cluster default `storageClass` and `volumeMode`. |
| P-0536 | deploy/helm/automatic-agent/templates/deployment.yaml | deploy/helm/automatic-agent/templates/deployment.yaml full text does not declare container-level `securityContext`, only pod-level, violating Kubernetes restricted PSS   | Fixed | The Deployment only has pod-level baselines, with container-level security context missing. |
| P-0537 | deploy/helm/automatic-agent/templates/service.yaml | deploy/helm/automatic-agent/templates/service.yaml does not split `metrics` into an independent ClusterIP service + ServiceMonitor selector, inconsistent with the Prometheus Operator paradigm   | Fixed | The business Service exposes both http and metrics, with the metrics port following `service.type` outward. |
| P-0538 | deploy/helm/automatic-agent/templates/canary-ingress.yaml(28 | deploy/helm/automatic-agent/templates/canary-ingress.yaml (28 lines) and ingress.yaml (45 lines) dual ingress with the same-name host in some ingress-nginx versions will trigger canary annotation conflict, lacking lint validation   | Re-reviewed & Closed | The root cause is the review treating ingress-nginx's canary same-host mode as conflicting implementation; the current canary ingress is independently named and follows the controller's convention, and the related host rendering also has golden guardrail. |
| P-0539 | deploy/helm/automatic-agent/values-prod.yaml:32-35 | deploy/helm/automatic-agent/values-prod.yaml:32-35 `ingress.domain: ""`, `hosts: []`, `tls[0].hosts: []` default empty — if CI does not explicitly override, Helm render can still succeed but there is no available host (misaligned with ingress.yaml:2 `required` validation)   | Re-reviewed & Closed | The root cause is the original review based on the old prod values snapshot; the current `values-prod.yaml` explicitly writes domain/hosts/tls, and `tests/golden/deploy/helm-ingress-guardrails.test.ts` continuously validates. |
| P-0540 | deploy/helm/automatic-agent/templates/configmap.yaml:7-9 | deploy/helm/automatic-agent/templates/configmap.yaml:7-9 directly lays `.Values.env` all into the ConfigMap, without filtering sensitive key names (e.g. `*_TOKEN` / `*_SECRET` / `*_KEY`); values mis-entry can easily cause keys to fall into the ConfigMap   | Fixed | ConfigMap has no sensitive env-key filtering. |
| P-0541 | deploy/helm/automatic-agent/values-${{<br>.github/workflows/deploy-environment.yml:184 | CI script-injection: deploy-environment.yml `-f "deploy/helm/automatic-agent/values-${{ inputs.environment }}.yaml"` enters the shell .github/workflows/deploy-environment.yml:184   | Fixed | The deploy workflow directly splices environment/strategy into shell and paths. |
| P-0542 | deploy/helm/automatic-agent/templates/service.yaml:8 | deploy/helm/automatic-agent/templates/service.yaml:8 `type: {{ .Values.service.type }}` defaults to ClusterIP (values.yaml:17 is not explicit), but the metrics port 9090 is exposed on the same service as the http port; if values change to LoadBalancer, metrics are exposed externally   | Fixed | The business Service exposes both http and metrics, with the metrics port following `service.type` outward. |
| P-0543 | deploy/helm/automatic-agent/templates/configmap.yaml:7-9 | deploy/helm/automatic-agent/templates/configmap.yaml:7-9 `range $key, $value := .Values.env` directly serializes all env into the ConfigMap, without excluding `*_PASSWORD/*_TOKEN/*_SECRET` patterns (corroborated by P-0540: missing filter)   | Fixed | ConfigMap has no sensitive env-key filtering. |
| P-0544 | deploy/helm/automatic-agent/templates/hpa.yaml:1-36<br>src/platform/five-plane-execution/queue-metrics | deploy/helm/automatic-agent/templates/hpa.yaml:1-36 is only based on CPU/Memory, with no custom metrics (queue depth, dispatch lag, SLO error budget), disconnected from the SLI exposed by src/platform/five-plane-execution/queue-metrics   | Fixed | The Helm HPA template only exposes resource-type metric entries, without connecting custom business-metric configuration. |
| P-0545 | deploy/helm/automatic-agent | deploy/helm/automatic-agent whole directory has no `ResourceQuota`, `LimitRange`, `PodSecurityPolicy` / `PodSecurityAdmission` labels   | Fixed | The chart does not have a built-in namespace-level resource-constraint template. |
| P-0546 | deploy/helm/automatic-agent/templates/deployment.yaml:80-83 | deploy/helm/automatic-agent/templates/deployment.yaml:80-83 preStop `["sleep", "5"]` and `terminationGracePeriodSeconds` (values-controlled) have no strong constraint (when grace < 5, directly SIGKILL)   | Fixed | preStop sleep duration and termination grace period are not jointly validated. |
| P-0547 | deploy/helm/automatic-agent/values-prod.yaml:39-41 | deploy/helm/automatic-agent/values-prod.yaml:39-41 `AA_API_HOST: "0.0.0.0"`, `AA_API_PORT: "3000"`, `AA_METRICS_PORT: "9090"` in prod listen API and metrics on 0.0.0.0; container security boundary relies on K8s Service rather than the process   | Re-reviewed & Closed | The root cause is mistaking the in-Pod listen address for the exposure boundary; the current chart has split the metrics service and provides NetworkPolicy, with the real exposure surface controlled by K8s network resources rather than `0.0.0.0` inside the container. |
| P-0548 | deploy/helm/automatic-agent<br>deploy/prometheus/rules | deploy/helm/automatic-agent whole directory has no `ServiceMonitor` / `PodMonitor` (for Prometheus Operator) and `PrometheusRule`; the prometheus rules are in deploy/prometheus/rules instead of inside the chart → deployment is not consistent   | Fixed | The chart does not have built-in Prometheus Operator resources. |
| P-0549 | deploy/helm/automatic-agent/templates/secret.yaml:9-23<br>.env.example | deploy/helm/automatic-agent/templates/secret.yaml:9-23 directly renders plaintext `ANTHROPIC_API_KEY/OPENAI_API_KEY/MINIMAX_API_KEY/AA_API_JWT_SECRET` via Helm `stringData`, contradicting the "do not carry real keys" contract emphasized by README/.env.example; when `externalSecret.enabled=false`, there is no gate   | Fixed | When externalSecret is off, inline plaintext secrets are allowed to render directly, lacking an explicit confirmation. |
| P-0550 | deploy/helm/automatic-agent/templates/deployment.yaml:23-29<br>docker-compose.yml:25 | deploy/helm/automatic-agent/templates/deployment.yaml:23-29 `securityContext` only contains `runAsNonRoot/runAsUser/fsGroup`, missing `readOnlyRootFilesystem`, `allowPrivilegeEscalation: false`, `capabilities.drop: [ALL]`, `seccompProfile`, inconsistent with docker-compose.yml:25 `read_only: true` security posture   | Fixed | The container securityContext lacks read-only root filesystem, no-privilege-escalation, drop capabilities, and other baselines. |
| P-0551 | docker-compose.yml:1 | docker-compose.yml:1 still writes `version: "3.9"`, which Compose v2 ignores and warns   | Fixed | The Compose file continues to retain the ignored `version` metadata, creating a pseudo-compatibility baseline. |
| P-0552 | Dockerfile:1,16 | Dockerfile:1,16 uses floating tag `node:22-bookworm-slim` (no digest pin)   | Re-reviewed & Closed | The root cause is that the Dockerfile digest-pin issue was split from build/runtime into multiple repeated surfaces; the gap has been unified under the master audit `N-1399`. |
| P-0553 | Dockerfile:16 | The Docker runtime stage also uses a floating tag: `FROM node:22-bookworm-slim AS runtime` Dockerfile:16   | Re-reviewed & Closed | The root cause is that the same base-image pinning gap is re-listed by runtime stage; with `P-0552/P-0554` it belongs to the duplicate surface of `N-1399`. |
| P-0554 | Dockerfile:1 | The Docker base image is not pinned by digest: `FROM node:22-bookworm-slim AS build` uses floating tag Dockerfile:1   | Re-reviewed & Closed | The root cause is that the same base-image pinning gap is repeatedly recorded by build stage; merged into `N-1399` tracking. |
| P-0555 | docker-compose.yml:40-43 | docker-compose.yml:40-43 in non-swarm mode uses `cpus: "1.0"`, `mem_limit`, `mem_reservation`, `pids_limit`, silently ignored by docker compose   | Fixed | Relying on resource fields silently ignored by Compose v2, providing an illusion of resource constraints to local rehearsals. |
| P-0556 | docker-compose.yml:11-13 | docker-compose.yml:11-13 `api-server` directly `build: { context: ., target: runtime }`; local compose and CI images do not reuse the same digest; locally built images cannot be reproduced by Helm   | Fixed | Compose only does local build without explicitly binding image ref, with local run and CI/Helm image identity disconnected. |
| P-0557 | Dockerfile:53<br>docker-compose.yml | Dockerfile:53 docker-compose.yml `postgres:16-bookworm`, `redis:7-alpine`, `prom/prometheus:v2.54.1`, `prom/alertmanager:v0.27.0` are all tags, not digests   | Re-reviewed & Closed | The root cause is that the digest-pinning and Docker base-image pinning for compose/runtime-dependent images are the same supply-chain-control gap; this class of issues has been unified under `N-1399`, closed as duplicate surface here. |
| P-0558 | Dockerfile:1,16<br>package.json:9 | Dockerfile:1,16 both build and runtime `FROM node:22-bookworm-slim`, but package.json:9 `engines.node` limits `>=20 <23`; CI image and engines upper/lower bounds leave no margin, and once Node 23 is upgraded, the image pin immediately breaches the contract   | Fixed | `engines` previously kept a wide 20-22 range, but the image and CI actually only use Node 22 as the deployment baseline. |
| P-0559 | docker-compose.yml:1 | docker-compose.yml:1 `version: "3.9"` has been ignored by Compose v2, and the top of the file also says "Resource and security limits are intentionally explicit", but the following `cpus` / `mem_limit` / `pids_limit` are silently dropped in non-swarm mode (same source as P-0555 but a more obvious security-claim illusion)   | Fixed | The obsolete version and the ignored resource limits coexist, with documentation semantics inconsistent with actual execution. |
| P-0560 | deploy/prometheus/prometheus.yml | deploy/prometheus/prometheus.yml does not enable the `alerting:` section, prometheus will not actively push to alertmanager   | Fixed | The prometheus configuration lacks an alerting export, with no notification chain after rule firing. |
| P-0561 | deploy/prometheus/rules/automatic-agent.yml | deploy/prometheus/rules/automatic-agent.yml full text does not reference infrastructure metrics like `up{}` / `process_*` / `kube_pod_*`; service death (process panic) will not trigger an alert   | Fixed | The rules only focus on business thresholds, not covering scrape/process-level infrastructure liveness signals. |
| P-0562 | deploy/prometheus/alertmanager.yml:31,38 | deploy/prometheus/alertmanager.yml:31,38 `${SLACK_WEBHOOK_URL}` / `${PAGERDUTY_SERVICE_KEY}` use shell-style variables, which Alertmanager does not expand by default; this requires `--config.file` rendering or a sidecar; the bare-run writing is a literal value   | Fixed | The placeholder variables are written directly into the Alertmanager configuration, and will not be expanded by default at runtime. |
| P-0563 | deploy/prometheus/alertmanager.yml:5-15 | deploy/prometheus/alertmanager.yml:5-15 `route.routes[*].continue: true` three branches fan-out at the same time; severity=critical will page + slack + default-webhook three times simultaneously, with unclear operations RACI   | Fixed | The route branches unconditionally continue:true, turning the severity route into an undifferentiated fan-out. |
| P-0564 | deploy/prometheus/rules/automatic-agent.yml | deploy/prometheus/rules/automatic-agent.yml thresholds (error rate 1%, p95 1s/3s, queue 100/500, worker 90/95%, timeout 5%) are all hardcoded in the alert, with no SLO/SLA ConfigMap decoupling, shared across tenants/environments   | Re-reviewed & Closed | The root cause is mistaking the repo-versioned baseline alert policy for a runtime-hot configuration requirement; the current rule file is the default SLO baseline, and different environments should be overridden through overlay/independent rule sets, not dynamically rewritten in the same ConfigMap. |
| P-0565 | deploy/prometheus/rules/automatic-agent.yml:6 | deploy/prometheus/rules/automatic-agent.yml:6 `sum(rate(http_requests_total{status=~"5.."}[5m])) / clamp_min(sum(rate(http_requests_total[5m])), 1)`: the denominator `clamp_min(...,1)` treats 0 as 1 during zero traffic, so the alert expression is stable by default during quiet periods, but also masks "0 traffic anomaly"   | Fixed | Using `clamp_min(...,1)` to disguise zero traffic as denominator 1, masking the silent-no-traffic scenario. |
| P-0566 | deploy/prometheus/prometheus.yml:1-12 | deploy/prometheus/prometheus.yml:1-12 `scrape_configs.static_configs.targets` hardcodes `api-server:3000`, with no alignment guarantee with the helm Service name `{{ include "automatic-agent.fullname" . }}` output (depending on the Helm release name)   | Fixed | The monitoring configuration mixes the compose target and the k8s target into a single static address, with cross-environment naming unable to align. |
| P-0567 | deploy/prometheus/alertmanager.yml:21-29 | deploy/prometheus/alertmanager.yml:21-29 the three receivers `default-warning/slack-warning/pagerduty-critical` all include the same `webhook_configs.url: http://api-server:3000/v1/alerts/webhook`, with severe engineering coupling (when the API service fails, the alert recursively routes to itself)   | Fixed | All three receivers call back to the API's own webhook, with the alert chain self-coupled to the monitored service. |
| P-0568 | deploy/prometheus/rules/automatic-agent.yml:38,46 | deploy/prometheus/rules/automatic-agent.yml:38,46 `sum(queued_tasks)`, `sum(active_executions)` do not carry any labels (namespace/instance/job), with multi-instance aggregation conflict; no contract link to the metric-label structure emitted in src   | Fixed | The aggregation expressions do not preserve the job dimension, with multi-instance or multi-topology mixing queue and worker metrics. |
| P-0569 | deploy/chaos/approval-policy.yaml:1<br>deploy/helm | deploy/chaos/approval-policy.yaml:1 `apiVersion: automatic-agent.io/v1` custom CRD, but deploy/helm does not provide the corresponding CRD `manifests/crds/`, with chaos policy deployment directly lacking definitions on the cluster   | Fixed | The chaos approval-policy manifest first references a custom resource, but the Helm chart does not deliver the corresponding CRD. |
| P-0570 | deploy/chaos/{network-delay,pod-kill,postgres-disconnect,redis-disconnect}.yaml<br>src/ops-maturity/chaos/chaos-experiment-scheduler.ts | deploy/chaos/{network-delay,pod-kill,postgres-disconnect,redis-disconnect}.yaml four types of failure scenarios, with no contract manifest sync with the scheduler in src/ops-maturity/chaos/chaos-experiment-scheduler.ts; no fallback chaos profile when the scheduler fails to start   | Fixed | The chaos-scenario YAML and the scheduler fallback profile lack a shared directory contract, and there is no built-in fallback catalog when startup fails. |
| P-0571 | deploy/runbooks/production-alert-runbook.md | deploy/runbooks/production-alert-runbook.md has only 9 short chapter descriptions, with no 1:1 anchor with the 9 specific prometheus rules alerts (the runbook title "Worker Mass Disconnect" is inconsistent with the alert name `AutomaticAgentWorkerPoolExhaustion`)   | Fixed | The runbook is organized by colloquial chapters, without 1:1 correspondence with the alertname. |
| P-0572 | deploy/terraform/main.tf:24<br>deploy/terraform/environments/multi-region/）目标矛盾 | deploy/terraform/main.tf:24 `default = "us-east-1"` hardcodes the default region; contradicting the multi-region DR target (deploy/terraform/environments/multi-region/)   | Fixed | The root module writes aws_region as a default value, with the multi-region overlay still controlled by the implicit region. |
| P-0573 | deploy/terraform/modules/rds/main.tf:79-95 | deploy/terraform/modules/rds/main.tf:79-95 SG `egress 0.0.0.0/0 -1` opens all egress, violating least privilege   | Fixed | The RDS security group opens egress to 0.0.0.0/0, with no minimum-privilege constraint on the control-plane access. |
| P-0574 | deploy/terraform/modules/elasticache/main.tf:99-103 | deploy/terraform/modules/elasticache/main.tf:99-103 SG `egress 0.0.0.0/0 -1` fully open   | Fixed | The Redis security group also fully opens egress, with the security boundary reduced to ingress rules. |
| P-0575 | deploy/terraform/main.tf:11-19 | deploy/terraform/main.tf:11-19 backend bucket/dynamodb_table/region etc. key configurations are hardcoded into the root module; CI/different-region team onboarding requires modifying source code rather than vars   | Fixed | backend bucket, lock, key were previously hardcoded into source, with environment onboarding only able to modify source files. |
| P-0576 | deploy/terraform/modules/eks/main.tf:74 | deploy/terraform/modules/eks/main.tf:74 `endpoint_public_access = true` default exposes the K8s API server to the public network, with no `public_access_cidrs` restriction   | Fixed | EKS API is exposed to the public network by default without CIDR restriction, with the control-plane boundary too wide. |
| P-0577 | deploy/terraform/modules/eks/main.tf:140-144 | deploy/terraform/modules/eks/main.tf:140-144 `scaling_config` indentation error (4 spaces vs 2), terraform fmt drift   | Fixed | The node-group configuration has long not been organized according to the IaC baseline, with both format and structure drifting. |
| P-0578 | deploy/terraform/modules/rds/main.tf | deploy/terraform/modules/rds/main.tf does not enable `iam_database_authentication_enabled`, does not declare `enabled_cloudwatch_logs_exports`, does not force `rds.force_ssl`   | Fixed | The RDS module only creates the instance, without supplementing IAM DB auth, log export, and SSL parameter group compliance baselines. |
| P-0579 | deploy/terraform/environments/prod.tfvars:2 | deploy/terraform/environments/prod.tfvars:2 `db_instance_class = "db.t3.large"` burstable instance for prod RDS (sustained CPU credit consumption risk)   | Fixed | The production tfvars continues to use the burstable instance type, with sustained-load CPU credit risk brought into prod. |
| P-0580 | deploy/terraform/modules/eks/main.tf:134-155 | deploy/terraform/modules/eks/main.tf:134-155 `aws_eks_node_group.main` lacks `launch_template`, `disk_encrypted`, `remote_access` blocking; node disks default to non-KMS encryption   | Fixed | The node group does not force disk encryption and IMDSv2 via launch_template, with security baseline failure. |
| P-0581 | deploy/terraform/modules/elasticache/main.tf:114 | deploy/terraform/modules/elasticache/main.tf:114 `engine_version = "7.1"` pins to an exact minor version, missing patches; inconsistent with docker-compose `redis:7-alpine` floating tag   | Fixed | The Redis engine is exactly pinned to minor 7.1, with the patch-upgrade policy hardcoded in source. |
| P-0582 | deploy/terraform/environments/prod.tfvars | deploy/terraform/environments/prod.tfvars only lists 6 variables, not covering security-related variables such as `vpc_cidr`, `availability_zones`, `storage_encrypted`, `kms_key_id`; inheriting defaults `10.0.0.0/16` and us-east-1   | Fixed | The environment tfvars does not explicitly declare region, CIDR, AZ, and other basic security parameters, with default values being quietly inherited. |
| P-0583 | deploy/terraform/modules/eks/main.tf:99-132 | deploy/terraform/modules/eks/main.tf:99-132 cluster/nodes IAM roles only attach AWS managed policies, with no OIDC IRSA configuration (`identity { oidc {} }` missing), Service Account cannot go through IRSA   | Fixed | The EKS cluster lacks OIDC provider resources, with IRSA unable to land as the default service-account trust chain. |
| P-0584 | deploy/terraform/main.tf<br>deploy/terraform/backend.tf | deploy/terraform/main.tf and modules/{ecr,eks,elasticache}/main.tf exist, but environments/*.tfvars does not mark the backend/state configuration strategy in the README, missing remote state lock (no deploy/terraform/backend.tf hit)   | Fixed | The repo lacks backend/state usage notes, with the remote-state isolation policy existing only as an implicit convention. |
| P-0585 | deploy/terraform/modules/elasticache/main.tf:110-122 | deploy/terraform/modules/elasticache/main.tf:110-122 `aws_elasticache_replication_group` enables `at_rest/transit_encryption`, but lacks `auth_token` / `auth_token_update_strategy`; cross-VPC only relies on SG boundary   | Fixed | ElastiCache only enables transport and static encryption, without configuring auth_token, with a runtime authentication chain gap. |
| P-0586 | deploy/terraform/modules/eks/main.tf:66-82 | deploy/terraform/modules/eks/main.tf:66-82 `aws_eks_cluster.main` lacks `enabled_cluster_log_types`, `encryption_config`; CloudWatch audit logs and secret envelope encryption are not enabled   | Fixed | The EKS cluster does not enable control-plane logs and secrets envelope encryption, with audit and confidentiality protection missing. |
| P-0587 | deploy/terraform/modules/rds/main.tf:99-102 | deploy/terraform/modules/rds/main.tf:99-102 the commented-out `# password = aws_secretsmanager_secret_version.db_password.secret_string` is the only password source; the current module has no `password` / `manage_master_user_password` field, and apply will fail due to missing master password   | Fixed | The RDS module leaves password in the comment, with the instance actually having no legitimate master-password source. |
| P-0588 | deploy/terraform/main.tf:11-19 | deploy/terraform/main.tf:11-19 hardcodes `backend "s3"` `bucket="automatic-agent-terraform-state"`, `region="us-east-1"`, `key="infra/terraform.tfstate"`; dev/staging/prod three environments share the same state key, without workspace/prefix isolation → any environment's apply can overwrite others   | Fixed | The three environments and multi-region previously shared one backend key, with any apply potentially overwriting another environment's state. |
| P-0589 | deploy/terraform/modules/rds/main.tf:128-131 | deploy/terraform/modules/rds/main.tf:128-131 `parameters = [{ name=..., value=... }]` is written as a list-of-objects, but `aws_db_instance` does not support the `parameters` argument (should be `aws_db_parameter_group` + `parameter_group_name`); terraform plan will fail   | Fixed | Putting parameters directly into `aws_db_instance`, mixing the instance resource and parameter-group resource models. |
| P-0590 | .env.example<br>.env.example:33-40） | .env.example is committed to the repo and the file contains key names like ANTHROPIC_API_KEY/OPENAI_API_KEY/MINIMAX_API_KEY (.env.example:33-40), easily inducing real .env to be mis-committed   | Fixed | `.env.example` acted as a credential list rather than a minimal local template. |
| P-0591 | .github/workflows/deploy-environment.yml:138 | CI script-injection: deploy-environment.yml `case "${{ inputs.environment }}"` is directly spliced into bash .github/workflows/deploy-environment.yml:138   | Fixed | The deploy workflow directly splices environment/strategy into shell and paths. |
| P-0592 | .env.example | .env.example has 200+ lines (grep shows at least to line 162); per the known risk of P-0590: includes 12+ lines of `AA_STABLE_*_OUTPUT_DIR` (lines 151-162), which are purely path variables mixed in the secret table, adding cognitive noise   | Fixed | Path/tuning variables and secrets are mixed, with the template's responsibility out of focus. |
| P-0593 | .env.example<br>.env.example:21 | .env.example provides complete key placeholders in the repo (AA_API_JWT_SECRET / POSTGRES_PASSWORD / ANTHROPIC_API_KEY / OPENAI_API_KEY / MINIMAX_API_KEY); any copy-paste error will bring real credentials into git .env.example:21   | Fixed | The template centrally lists real secret names in the same checklist, easily being mistaken as a commit target. |
| P-0594 | .env.example:151-162 | .env.example:151-162 12 `AA_STABLE_*_OUTPUT_DIR` env variables (path variables) mixed in the secrets template (P-0592), and not all of them are actually read in src — documentation and code may drift   | Fixed | Stability output directory variables are stacked in the secret template, missing grouping by purpose. |
| P-0595 | .env.example | .env.example has 377 lines total (verified), of which only 5 are truly required keys (AA_API_JWT_SECRET/POSTGRES_PASSWORD/ANTHROPIC_API_KEY/OPENAI_API_KEY/MINIMAX_API_KEY), with the remaining ~370 lines being optional/path/tuning variables; newcomers find it hard to distinguish "must-fill vs can-omit"   | Fixed | The example file is too long and does not distinguish required/optional, causing excessive configuration-entry noise. |
| P-0596 | helpers/fs.ts<br>helpers/fs.{js,d.ts,js.map} | helpers/fs.ts does not exist but helpers/fs.{js,d.ts,js.map} exist, exporting `createTempWorkspace/cleanupPath/createFile/createSymlink`, including `symlinkSync` — security-sensitive tools exist in the repo without source code, a suspicious "compiled-into-repo disguise"   | Fixed | The helpers directory placed sensitive file-system tools only as compile artifacts in the repo, breaking the source-code audit chain. |
| P-0597 | - | helpers/ (top-level) contains only `fs.d.ts`, `fs.js`, `fs.js.map` (no `fs.ts` source) — P-0596 first-level directory is "compile artifacts without source code", reverse import will bypass strict checks   | Re-reviewed & Closed | The current `helpers/` already contains the `fs.ts` source file; the original record is based on the old directory state. |
| P-0598 | packs/test-pack/package.json<br>package.json<br>package.json:7 | packs/test-pack/package.json scripts `domain:lint` / `domain:validate` invoke the `agent-platform` binary, but package.json:7 `bin` only declares `aa` / `automatic-agent`, with no `agent-platform` entry   | Fixed | The test-pack script still invokes a non-existent `agent-platform` executable name, disconnected from the root bin contract. |
| P-0599 | packs/test-pack/package.json:5<br>packs/test-pack/<br>packs/test-pack<br>package.json:5 | packs/test-pack/package.json:5 `"main": "dist/index.js"` but packs/test-pack/ has no dist/; running `node packs/test-pack` locally immediately fails   | Fixed | The package entry points to a non-existent dist/index.js, with the example pack metadata not matching the real directory structure. |
| P-0600 | - | packs/ contains 9 example packs: `123-pack/`, `123test/`, `my-pack-id/`, `my.pack.id/`, `my_pack_id/`, `ops.core/`, `test-pack/`, `test.pack/`, `test_pack/` — mixed naming (kebab/dot/snake); when packId and directory have the same name, `123-pack` starting with a number violates npm naming rules   | Fixed | Example pack fixtures have long been placed in the repo's first-level directory, being mistaken for publishable packages rather than naming/validation samples. |
| P-0601 | packs/、helpers/<br>src/plugins/、src/sdk/harness-sdk/<br>AGENTS.md | packs/, helpers/ top-level directories conceptually overlap with src/plugins/, src/sdk/harness-sdk/ namespaces (pack vs plugin vs harness) but README/AGENTS.md does not provide a distinction diagram   | Fixed | Fixture assets, runtime plugins, and SDK entries previously shared mixed naming, with documents lacking clear semantic boundaries. |
| P-0602 | packs/{my-pack-id,<br>tests/fixtures） | packs/{my-pack-id, my.pack.id, my_pack_id}/manifest.json three different packIds but all `domainId: "test-domain"`, `owner: "test-owner"`; the registry cannot disambiguate (actually a case-study fixture for naming style, but placed in the repo's first-level directory rather than tests/fixtures)   | Fixed | The pack-naming cases are actually test fixtures, but were placed in the formal source root path, obscuring the "same domain same owner only for naming-normalization test" intent. |
| P-0603 | src/plugins/<br>tests/fixtures/packs<br>AGENTS.md | packs/ directory, src/plugins/ directory, scale-ecosystem/marketplace/ directory have three-layer semantics (pack vs plugin vs marketplace) with no clear division diagram in README/AGENTS.md (corroborated by P-0601, but this round found that the packs first-level directory is indeed "example fixture" semantics, should be migrated to tests/fixtures/packs or examples/)   | Fixed | The pack fixture, built-in plugin, and marketplace service previously lacked a clear layering note, causing directory semantics to overlap. |
| P-0604 | - | .husky/pre-commit is only 34 bytes (`#!/usr/bin/env sh\nnpx lint-staged`), with no typecheck/test short-circuit; pre-commit will not catch cross-file type errors   | Fixed | pre-commit only runs lint-staged, missing cross-file type validation. |
| P-0605 | - | .husky/pre-commit only contains `npx lint-staged` (2 lines without set -e, without husky v9 init shim); since husky v9 the official recommendation is an explicit entry, without an init shim it is ignored in some versions (already mentioned in P-0610, this round verified that the .husky directory permissions/content is indeed 34 bytes)   | Re-reviewed & Closed | The current hook contains `set -eu` and additional typecheck; the original "34-byte two-line script" description is obsolete; the husky initialization fragility is handled by P-0610. |
| P-0606 | package.json | stryker.config.mjs exists but package.json scripts do not include mutation testing in ci:baseline, making mutation testing a decoration   | Fixed | The root cause is that the mutation script exists, but the baseline gate does not consume it. This round incorporates `npm run test:mutation:critical` into `ci:baseline`, so that key mutation regression is no longer just a sidecar script. |
| P-0607 | package.json:255 | package.json:255 the file is 255 lines total, with the scripts block occupying ~120 lines (line 47-159 range); a single manifest has high maintenance cost; lacking `npm run-many` / Nx / Turbo etc. orchestration   | Re-reviewed & Closed | The root cause is treating centralized script orchestration as an independent defect; the current repo explicitly uses a single package + npm scripts as the control-plane entry, which is a maintenance preference, not a runtime bug. |
| P-0608 | package.json:243 | package.json:243 places `typescript` in devDependencies correctly, but the same package `tsx` is also dev, and scripts:159 `aa:dev` directly does `node --import tsx` and fails when tsx is not present after install   | Re-reviewed & Closed | `aa:dev` is already clearly marked as a development-only shortcut script in the README, and depending on devDependencies is expected, no longer treated as a production defect. |
| P-0609 | package.json:144<br>AGENTS.md | package.json:144 aliases `test` to `ci:baseline`, causing `npm test` to forcibly run typecheck+coverage gate, inconsistent with AGENTS.md's "npm test runs --test-concurrency=12" description   | Fixed | The script behavior has changed to the baseline gate, but the repo documentation still describes the old test semantics. |
| P-0610 | package.json:53 | package.json:53 declares `"prepare": "husky"`, but .husky/pre-commit only has `npx lint-staged`, without chmod / husky v9 init shim line (a single-line hook is ignored in some husky versions)   | Fixed | prepare/hook initialization is too fragile, lacking tolerance for missing husky scenarios. |
| P-0611 | package.json:53<br>Dockerfile:23）会因为 | package.json:53 `"prepare": "husky"` in the `npm ci --omit=dev` container (Dockerfile:23) will fail to execute because husky is a dev dep — the actual `ignore-scripts` behavior depends on the npm version, and CI is error-prone   | Fixed | The production install executing `prepare` assumes husky is always available. |
| P-0612 | src/sdk/cli/aa.ts"<br>package.json:159<br>Dockerfile | package.json:159 `"aa:dev": "node --import tsx src/sdk/cli/aa.ts"` cannot run in environments that install only production dependencies (such as the Dockerfile runtime stage), but the README does not label "dev only" (corroborated by P-0608)   | Fixed | aa:dev depends on tsx and other development dependencies, but the documentation does not specify that it only applies to the development environment. |
| P-0613 | scripts/ci/audit-*<br>package.json | package.json `audit:repo-hygiene` serially runs 3 audit sub-tasks (line 138), failing any of them blocks `npm test`, but scripts like `audit:test-exclusions` in scripts/ci/audit-* are .mjs without shebang (see P-0466), with cross-platform execution risk   | Fixed | The repo hygiene relies on audit scripts lacking executable entries, with unstable cross-platform execution. |
| P-0614 | docs_zh/contracts<br>CHANGELOG.md:3<br>package.json:4 | CHANGELOG.md:3 current `[0.1.0] - 2026-05-14` is the repo's only published version, and the version has not been rolled for a week from today (2026-05-21); package.json:4 `"version": "0.1.0"` is in sync, but git history/commits count far exceeds 0.1 semantics (corroborated by multiple updates in docs_zh/contracts)   | Fixed | The root cause is that the changelog only records released entries, with no position for unreleased commits, causing all subsequent changes to look like "forgot to roll the version". This round adds `Unreleased` to `CHANGELOG.md`, and clarifies the `0.x` pre-GA version policy in `docs_zh/operations/release-versioning.md`. |
| P-0615 | src/sdk/cli/aa.ts:script<br>package.json:217-228 | package.json:217-228 `dependencies` has only 10 entries (OpenTelemetry/ioredis/postgres/ws/xml-crypto/zod), but the src contains `tsx`, `stryker`, `eslint`, `prettier` etc. only referenced in dev — but src/sdk/cli/aa.ts:script `aa:dev` strongly depends on tsx (corroborated by P-0608), and the production install has no tsx, so the bin `aa` in dev mode is empty   | Re-reviewed & Closed | The production `aa` goes through the build artifacts, and `aa:dev` is only used in source-development mode; the warning is caused by mistaking the development script dependency for a runtime dependency. |
| P-0616 | package.json:144<br>AGENTS.md | package.json:144 `"test": "npm run ci:baseline"`, and `ci:baseline` (line 51) serially runs `typecheck && audit:repo-hygiene && test:raw && coverage:gate && validate:stable:compiled`; local `npm test` actually runs all ~5 steps, and AGENTS.md also writes "`npm test` runs --test-concurrency=12", with three-way documentation ↔ script ↔ behavior inconsistency (corroborated by P-0609/P-0626)   | Fixed | The script behavior has changed to the baseline gate, but the repo documentation still describes the old test semantics. |
| P-0617 | tsconfig.json:60-77 | tsconfig.json:60-77 the entire batch of integration/* paths are excluded; CI cannot typecheck them   | Re-reviewed & Closed | The root cause is treating the `tsconfig.json` editor/auxiliary config as the CI typecheck contract; CI actually runs `tsconfig.build.json` (src-only), and integration coverage is taken by the layered test gate. |
| P-0618 | tsconfig.json:23<br>tsconfig.json:24-118）大面积裁剪用例 | tsconfig.json:23 simultaneously compiles src and tests, but the exclude list has ballooned to 90+ items (tsconfig.json:24-118) heavily trimming cases   | Re-reviewed & Closed | The root cause is that the tests include in `tsconfig.json` mainly serves local IDE navigation, and the large exclude list has been explicitly audited by `scripts/ci/audit-test-exclusions.mjs`; this is compile-scope management, not runaway drift. |
| P-0619 | tests/unit/platform/execution/**/*.test.ts<br>tsconfig.json:32 | tsconfig.json:32 excludes the entire directory `tests/unit/platform/execution/**/*.test.ts`, conflicting with the README's "full regression" claim   | Re-reviewed & Closed | The root cause is confusing "TypeScript static compilation coverage" with "test execution coverage"; though this directory does not enter `tsc`, it is still run by the `node --test/tsx` layering. |
| P-0620 | tsconfig.json:23-118 | tsconfig.json:23-118 exclude list 90+ items and tests/ directory's 4682 test files have a huge trim surface (P-0618-0232); CI will not typecheck integration paths — regression coverage and name are out of sync   | Re-reviewed & Closed | The root cause is mixing compile scope, raw layered tests, and exclusion audit into a single "coverage" metric; the repo currently uses `tsconfig.build.json`, `test:raw`, and `audit:test-exclusions` as three gates. |
| P-0621 | src/scale-ecosystem/billing/billing-service-async.ts:49 | The above 5 `require` + `// eslint-disable-next-line @typescript-eslint/no-require-imports` (src/scale-ecosystem/billing/billing-service-async.ts:49 etc.) cements the anti-pattern, without migrating to `await import()`   | Fixed | These async wrappers preserved the CommonJS `require` inertia for compatibility with early sync implementations, without converging along with the repo's ESM migration. |
| P-0622 | - | eslint.config.js does not configure `eslint-plugin-security` / `eslint-plugin-no-secrets` / `eslint-plugin-import` cycle detection, forming a clear tooling gap with the project's "circular import" issues (P-0141/0021/0024/0294 etc. dozens of places)   | Fixed | The root cause is that the repo fully ties lint capability to ESLint plugins, while the current environment does not set up equivalent CI checks for cycle/secret gating. This round retains ESLint base rules, while adding `scripts/ci/audit-lint-guardrails.mjs` to do repo hygiene gate for relative import cycles and sensitive literals. |
| P-0623 | src/**/*.ts",<br>scripts/**/*.mjs"]<br>tests/、tools/、helpers/、packs/、deploy/scripts、ui/<br>deploy/、helpers/、packs/ | eslint.config.js:9 `files: ["src/**/*.ts", "scripts/**/*.mjs"]`: tests/, tools/, helpers/, packs/, deploy/scripts, ui/ are all not in the root eslint scope; ui/ has its own eslint config but deploy/, helpers/, packs/ have no lint at all   | Fixed | The root ESLint only covers src and scripts, excluding helpers, packs, tests, and other directories from static checks. |
| P-0624 | - | eslint.config.js:11-12 `"@typescript-eslint/no-explicit-any": "warn"` is not elevated to error; `no-floating-promises`, `no-misused-promises`, `require-await` are all missing, unable to catch `void this.x()` fire-and-forget in P-0172-0351   | Fixed | The root ESLint constraints on any and promise misuse are too weak, with fire-and-forget-like issues unable to enter the default review surface. |
| P-0625 | CONTRIBUTING.md<br>AGENTS.md | CONTRIBUTING.md and AGENTS.md have dual sources for commit style: AGENTS.md describes short imperatives, CONTRIBUTING.md does not reference, with a dual-track guide   | Fixed | AGENTS and CONTRIBUTING give two sets of guidance on commit conventions. |
| P-0626 | scripts/run-layered-tests.mjs:173<br>AGENTS.md:6 | AGENTS.md:6 claims `npm test` uses `--test-concurrency=12`, but scripts/run-layered-tests.mjs:173 is dynamically determined by LAYER_DEFINITIONS, not 12   | Fixed | The script behavior has changed to the baseline gate, but the repo documentation still describes the old test semantics. |
| P-0627 | src/platform/five-plane-interface/<br>src/platform<br>AGENTS.md:1-? | AGENTS.md:1-? lists the five plane directories such as `src/platform/five-plane-interface/`, some of which have spelling drift with the actual plane naming in src (src/platform contains contracts/gateway/prompt sibling directories), and are not synchronized in README   | Fixed | AGENTS only describes the five plane main paths, without covering the `src/core/` compatibility layer and the actual directory cross-reference. |
| P-0628 | src/platform<br>AGENTS.md | The plane directories listed in AGENTS.md cross-reference with the actual src/platform directory (contracts/gateway/prompt is at the same level as five-plane-*, P-0627); newcomers following AGENTS.md to find files will fail   | Fixed | AGENTS only describes the five plane main paths, without covering the `src/core/` compatibility layer and the actual directory cross-reference. |
| P-0629 | AGENTS.md | divisions directory has 67 yaml + 32 json, plus 32 first-level division directories (academic-research/...); among them P-0459 already records the qa vs quality-assurance duplicate, and further: engineering_ops vs general_ops vs operations vs it-operations have high synonymity, with no merge diagram in README/AGENTS.md   | Fixed | The root cause is that the division family has only directory names, with no machine-readable or document-readable family map. This round adds `config/quality/division-catalog.json` and `docs_zh/reference/division-catalog.md`, and explicitly explains the quality/operations family division in `README.md` and `AGENTS.md`. |
| P-0630 | docs_zh/architecture/00-platform-architecture.md<br>README.md:3 | README.md:3 references `docs_zh/architecture/00-platform-architecture.md`, but the equivalent file in docs_en is not noted in the README, with a missing bilingual portal   | Fixed | The README only exposes the Chinese architecture entry, missing the English entry. |
| P-0631 | docs_zh/architecture/00-platform-architecture.md"<br>README.md:3 | README.md:3 emphasizes "v2.7 seven-layer architecture docs_zh/architecture/00-platform-architecture.md", but the actual plane naming in src is five-plane-*, with the plane count (5) and the seven-layer architecture count (7) inconsistent   | Fixed | The README does not explain the relationship between the seven-layer architecture and the five-plane runtime. |
