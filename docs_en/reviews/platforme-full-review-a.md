# Platforme Full Review A 问题表

> 已将原始问题清单转换为表格, 保留原文表述, 便于筛选, 复制与subsequent跟踪. 

| # | 位置 | Issue | 问题state | Root Cause |
| --- | --- | --- | --- | --- |
| P-0001 | src/platform/contracts/types/domain/task-types.ts:45 | `src/platform/contracts/types/domain/task-types.ts:45` `export type { ArtifactRef } from "../../executable-contracts/index.js"` : contracts 子树内反向跨级 re-export.    | 已修复 | `contracts/types/domain` 为compatibilitydirectly反向 re-export 可executecontracttype, 打破了 contracts 分层边界.  |
| P-0002 | src/platform/contracts/types/domain/task-types.ts:45<br>package.json:14-46 | src/platform/contracts/types/domain/task-types.ts:45 `export type { ArtifactRef } from "../../executable-contracts/index.js"` (P-0001) 反向 re-export; package.json:14-46 `exports` 表中 `"./platform"` 一个entry同时暴露 `contracts/types/domain` 与 `executable-contracts` 两份 ArtifactRef → consumer typeof 比较为不同symbols   | 已修复 | 同一 `ArtifactRef` via两个发布path外溢, 消费者可见到duplicatesymbols边界.  |
| P-0003 | src/platform/contracts/types/domain/task-types.ts:45<br>package.json | src/platform/contracts/types/domain/task-types.ts:45 `export type { ArtifactRef } from "../../executable-contracts/index.js"` 在 contracts 子树内反向跨级 re-export (已 P-0001) , 与 package.json `exports` 中 `"./platform"` 单一entrypotentialconflict, type在两条path下duplicate发布   | 已修复 | type发布面把 canonical 定义和compatibility出口混在一起, 导致单entry下仍暴露双path语义.  |
| P-0004 | src/platform/five-plane-interface/api/http-server/harness-runs-routes.ts:199 | default值降级: API harness 路由对 domainId 取 `\|\| "default"` 字符串 src/platform/five-plane-interface/api/http-server/harness-runs-routes.ts:199   | 已修复 | 创建 harness run 时把missing domainId fallback到 "default", 掩盖call方missing参.  |
| P-0005 | src/platform/five-plane-interface/api/oidc-oauth/jwt-utils.ts:29 | error吞噬: `} catch {` silently忽略 OIDC/JWT validation异常 src/platform/five-plane-interface/api/oidc-oauth/jwt-utils.ts:29   | 已复核关闭 | 旧implementationrisk来自 `catch {}` 形态; 当前 decode failure已转换为 AuthError 并 fail-close.  |
| P-0006 | src/platform/five-plane-interface/api/api-auth-service.ts:119 | JWT onlyallows HS256 (未支持 RS/ES 非对称) , HS256 shared密钥即签发即验签 src/platform/five-plane-interface/api/api-auth-service.ts:119   | 已复核关闭 | Root cause:  A 表把“未支持外部 IdP 非对称 JWT”误记成当前missing陷; 现行 API auth 是自签自验的 service token, `allowedAlgorithms/iss/aud/jti/revocation` 已补齐, RS/ES 属协议扩展而非现行bypass面.  |
| P-0007 | src/platform/five-plane-interface/api/oidc-oauth-service.ts:465 | OIDC JWKS: missing kid 时 fallback 到 `keys[0]` 隐式选键, exists密钥混淆risk src/platform/five-plane-interface/api/oidc-oauth-service.ts:465   | 已修复 | JWT missing kid 时default选首个 JWKS key, 给多 key issuer 留下隐式选键空间.  |
| P-0008 | src/platform/five-plane-interface/api/api-auth-service.ts:120 | 弱 JWT secret checkonly 8 字符最小length, 容易viavalidation后仍被暴力 src/platform/five-plane-interface/api/api-auth-service.ts:120   | 已修复 | JWT 密钥强度基线过低, 只做最短lengthvalidation, 导致明显弱密钥仍可via.  |
| P-0009 | src/platform/five-plane-interface/api/graphql-adapter-service.ts:262 | `Math.random()` used for GraphQL subscription operationId: src/platform/five-plane-interface/api/graphql-adapter-service.ts:262   | 已修复 | subscription operationId 由 Math.random() 生成, 可预测且collision控制弱.  |
| P-0010 | src/platform/five-plane-interface/webhook/index.ts:225 | webhook HMAC validation未读取/validation timestamp 或 nonce, signature可replay src/platform/five-plane-interface/webhook/index.ts:225   | 已修复 | webhook replay防护只validationsignature本身, 未把replay检测绑定到requestfingerprinting与幂等语义.  |
| P-0011 | src/platform/five-plane-interface/webhook/index.ts:225 | webhook allows `signingSecret ?? ""` 作为 HMAC 密钥, 空 secret 不阻断 src/platform/five-plane-interface/webhook/index.ts:225   | 已修复 | HMAC 验签对missing secret 走空串兜底, configure异常没有按 fail closed handle.  |
| P-0012 | src/platform/five-plane-interface/api/api-auth-service.ts:121 | 弱 secret 黑名单only枚举 5 个值 (secret/password/changeme/default/jwtsecret) , 其它弱口令via src/platform/five-plane-interface/api/api-auth-service.ts:121   | 已修复 | 弱密钥识别dependency少量枚举黑名单, 未coverage常见模式化口令.  |
| P-0013 | src/platform/five-plane-interface/api/graphql-adapter-service.ts:262 | src/platform/five-plane-interface/api/graphql-adapter-service.ts:262 `operationId = \`sub_${Date.now()}_${Math.random()...\`` 同问题: subscription 操作 ID 非加密随机   | 已修复 | subscription operationId 再次复用了 Math.random(), 追踪标识不稳定.  |
| P-0014 | src/platform/five-plane-interface/api/http-api-server.ts:189 | src/platform/five-plane-interface/api/http-api-server.ts:189 `void this.handleRequest(req, res)` 顶层 async 不捕获异常, dependency框架default 500 — 失去 trace_id 上下文绑定   | 已修复 | HTTP entrydirectly fire-and-forget 异步handle, missing少顶层 catch wrapper与统一error落点.  |
| P-0015 | src/platform/five-plane-interface/api/api-auth-service.ts:131 | JWT 验证将 `WEAK_JWT_SECRETS.has(secret.trim().toLowerCase())` 作only小写比较, `Secret` 等size写变体不在黑名单 src/platform/five-plane-interface/api/api-auth-service.ts:131   | 已复核关闭 | 当前implementation先 `trim().toLowerCase()` 再comparison黑名单, size写变体已被coverage, 原record不成立.  |
| P-0016 | src/platform/five-plane-interface/api/middleware/idempotency-key-storage.ts:211,222,231,244,260,277,287,294,299 | SQL 字符串拼接: dynamic `${this.tableName}` 拼到多条 DDL/DML src/platform/five-plane-interface/api/middleware/idempotency-key-storage.ts:211,222,231,244,260,277,287,294,299   | 已复核关闭 | 原问题来auto态表名拼接; 当前construction器已先经 validateSqlIdentifier() 白名单约束.  |
| P-0017 | src/platform/five-plane-interface/api/oidc-oauth-service.ts:256 | OIDC 验证流程allows `iss/aud/nbf` via软error码return (如 jwt.untrusted_issuer / not_yet_valid) 但implementation位于同一 `validateToken` path, 未independentforce nbf 偏差容忍 src/platform/five-plane-interface/api/oidc-oauth-service.ts:256   | 已复核关闭 | 旧审计based on历史validationpath; 当前implementation已对 exp/nbf/iat 做explicitlyclock偏差validation并 fail-close.  |
| P-0018 | src/platform/five-plane-interface/api/graphql-adapter-service.ts:413,419,435,441,519 | 自由text `throw new Error("Mutation type is not configured")` 等multiple places src/platform/five-plane-interface/api/graphql-adapter-service.ts:413,419,435,441,519   | 已修复 | GraphQL 适配层directly抛bare Error, missing少统一error码与type分类.  |
| P-0019 | src/platform/five-plane-interface/api/http-api-server.ts:189 | src/platform/five-plane-interface/api/http-api-server.ts:189 `void this.handleRequest(request, response)` 顶层 fire-and-forget (P-0014) + 无 timeout wrapper (http.Server.timeout 未在call点explicitly设置) , slow-loris 攻击面   | 已修复 | HTTP entry没有把timeout和异步异常handleexplicitly绑定到 server 级别, 慢request防护不完整.  |
| P-0020 | src/platform/five-plane-interface/api/facade-interfaces.ts:214,217,220,223 | 自由text `throw new Error("Incident service not configured")` duplicate四处 src/platform/five-plane-interface/api/facade-interfaces.ts:214,217,220,223   | 已修复 | facade 未configurebranch复用bare Error, missing少统一errortype和error码.  |
| P-0021 | src/platform/five-plane-interface/api/http-api-server.ts | src/platform/five-plane-interface/api/http-api-server.ts 全文 grep 无 `Strict-Transport-Security/X-Content-Type-Options/Content-Security-Policy/X-Frame-Options/Referrer-Policy` 任何安全 header (不dependency反代) ; 纯bare HTTP 服务无安全头基线   | 已复核关闭 | 历史版本missing安全头; 当前 decorateResponseHeaders() 已统一injection HSTS, CSP, nosniff 等基线.  |
| P-0022 | src/platform/five-plane-control-plane/config-center/profile-home.ts:30 | default值降级: `profileId?.trim() \|\| "default"` 落入hardcoded default naming空间 src/platform/five-plane-control-plane/config-center/profile-home.ts:30   | 已修复 | 未explicitly传 profileId 时一律落到 "default", 不同工作区shared同名naming空间.  |
| P-0023 | src/platform/five-plane-control-plane/iam/sandbox-policy.ts:361 | error吞噬: `} catch {` silently忽略 src/platform/five-plane-control-plane/iam/sandbox-policy.ts:361   | 已复核关闭 | 此处 catch only在 URL decodefailure时fallback原字符串, 不是strategybypasspath.  |
| P-0024 | src/platform/five-plane-control-plane/iam/outbound-url-policy.ts:136,160 | error吞噬: `} catch {` silently忽略 src/platform/five-plane-control-plane/iam/outbound-url-policy.ts:136,160   | 已复核关闭 | 两处 catch 已转为 ValidationError 或sanitizedfallback, 不再把strategyfailuresilently放行.  |
| P-0025 | src/platform/five-plane-control-plane/config-center/config-impact-analyzer.ts:206 | `Math.random()` used forconfigure影响analysis ID: src/platform/five-plane-control-plane/config-center/config-impact-analyzer.ts:206   | 已修复 | configure影响analysis ID 用 Math.random() 拼接, 唯一性和不可预测性不足.  |
| P-0026 | src/platform/five-plane-control-plane/iam/file-freshness.ts:103 | 弱算法可选: `takeFileSnapshot` digestAlgorithm accepts md5 src/platform/five-plane-control-plane/iam/file-freshness.ts:103   | 已修复 | file新鲜度guard暴露 md5 选项, 把弱摘要算法带进concurrent写保护链路.  |
| P-0027 | src/platform/five-plane-control-plane/iam/secret-management-service.ts:103 | error吞噬: `} catch {` silently忽略 src/platform/five-plane-control-plane/iam/secret-management-service.ts:103   | 已复核关闭 | 旧审计把 catch 视为吞错; 当前旋转strategy解析failure已抛 ValidationError.  |
| P-0028 | src/platform/five-plane-control-plane/cost-alert/cost-alert-service.ts:540 | src/platform/five-plane-control-plane/cost-alert/cost-alert-service.ts:540 emit `cost.threshold.exceeded`, 与注册表 `cost:limit_reached` naminginconsistent (点号 vs 冒号)    | 已修复 | cost alert implementation沿用了旧点号事件名, 未跟事件注册表统一到冒号风格.  |
| P-0029 | src/platform/five-plane-control-plane/iam/mfa-service.ts:309 | PII 落库: MFA 会话 identifier comment承认未做 mask `store masked email/phone` 当前实为明文 src/platform/five-plane-control-plane/iam/mfa-service.ts:309   | 已修复 | MFA 会话persistencedirectly保存原始 identifier, 未落实comment声明的sanitizedstrategy.  |
| P-0030 | src/platform/five-plane-control-plane/iam/file-freshness.ts:262 | 弱算法可选: `FileFreshnessTracker.snapshot` digestAlgorithm accepts md5 src/platform/five-plane-control-plane/iam/file-freshness.ts:262   | 已修复 | FileFreshnessTracker 同样把 md5 作为可选摘要算法暴露给写保护path.  |
| P-0031 | src/platform/five-plane-control-plane/incident-control/acceptance-readiness-service.ts:119 | `JSON.parse` 无 try/catch: directly解析存储pathreturn src/platform/five-plane-control-plane/incident-control/acceptance-readiness-service.ts:119   | 已修复 | accepts度报告读取 JSON 时解析failuredirectly吞成 null, 把corruptedstatemasks成“无data”.  |
| P-0032 | src/platform/five-plane-control-plane/iam/sandbox-policy.ts:361 (P-0023) 与 | src/platform/five-plane-control-plane/iam/sandbox-policy.ts:361 (P-0023) 与 outbound-url-policy.ts:136,160 (P-0024) 属网络出向strategy关键path, 吞错等于strategy失效   | 已复核关闭 | 旧问题建立在“出向strategy关键path吞错”前提; 现implementation未在strategy判定处silently放行.  |
| P-0033 | src/platform/five-plane-control-plane/incident-control/auto-stop-loss-service.ts:789 | fire-and-forget Promise: `void this.executePlaybook(...)` src/platform/five-plane-control-plane/incident-control/auto-stop-loss-service.ts:789   | 已复核关闭 | call点已有 rejected promise handle与failurerecord, 审计把受控异步branch误判为无handle fire-and-forget.  |
| P-0034 | src/platform/five-plane-control-plane/iam/data-classification-service.ts:293,841-846 | `regexCache = new Map<string, RegExp>()` 无sizeupper limit; 若分类规则dynamic生成可导致in-memory膨胀 src/platform/five-plane-control-plane/iam/data-classification-service.ts:293,841-846   | 已修复 | 分类规则 regex cached只增不减, 没有容量upper limit.  |
| P-0035 | src/platform/five-plane-control-plane/approval-center/quorum-calculator.ts:289,295 | 自由text `throw new Error("Vote must have a valid approverId/votedAt timestamp")` src/platform/five-plane-control-plane/approval-center/quorum-calculator.ts:289,295   | 已修复 | 投票validationfailuredirectly抛bare Error, missing少 ValidationError 分类.  |
| P-0036 | src/platform/five-plane-control-plane/incident-control/release-pipeline-support.ts:215 | 命令construction: release-pipeline uses `spawn(request.command, request.args, ...)` 由request驱动 src/platform/five-plane-control-plane/incident-control/release-pipeline-support.ts:215   | 已修复 | release pipeline 把request里的命令和参数directly下传给 `spawn`, missing少 step 级 allowlist.  |
| P-0037 | src/platform/five-plane-control-plane/incident-control/runbook-executor/runbook-executor.ts:263 | setTimeout 未 cleanup: `setTimeout(() => {...}, 10)` 异步 sleep 在 abort 时未cleanup src/platform/five-plane-control-plane/incident-control/runbook-executor/runbook-executor.ts:263   | 已修复 | runbook executor 用bare `setTimeout` 模拟 sleep, 没有保存handle并在空闲时释放事件循环references.  |
| P-0038 | src/platform/five-plane-control-plane/config-center/config-impact-analyzer.ts:206 | src/platform/five-plane-control-plane/config-center/config-impact-analyzer.ts:206 `analysisId = \`impact-${Date.now()}-${Math.random()...slice(2,8)\`` 6 字符 → ~31 bits, 唯一性保证最弱   | 已修复 | impact analysis ID 仍由时间戳加短随机串生成, collision与预测面偏大.  |
| P-0039 | src/platform/five-plane-control-plane/incident-control/deployment-execution-service.ts:165 | 命令construction: deployment-execution-service uses `spawn(request.command, request.args, ...)` 由request驱动 src/platform/five-plane-control-plane/incident-control/deployment-execution-service.ts:165   | 已修复 | deployment execution 同样directlyexecuterequest内命令, missing少部署step到可execute命令的白名单收口.  |
| P-0040 | src/platform/five-plane-control-plane/config-center/startup-env-schema.ts:530 | src/platform/five-plane-control-plane/config-center/startup-env-schema.ts:530 `process.exit(1)` directly退出 (与 line 5 comment的"validation runs at process startup"一致) , 但 config-center 也被 SDK/CLI 引入; CLI 短命令意外触发 exit(1) risk (与 ts 报错信号难distinguish)    | 已修复 | 启动环境validation把“库函数validationfailure”和“CLI entry退出”coupling在同一个函数里.  |
| P-0041 | src/platform/five-plane-control-plane/mission/index.ts | src/platform/five-plane-control-plane/mission/index.ts ~42 KB  --  index.ts 反模式 (barrel 不应承载implementation) , violates"index.ts 只 re-export"约定   | 已复核关闭 | Root cause: 审查把 mission entry按“纯 barrel”要求handle; 当前 `mission/index.ts` 本身就是该域的 canonical facade/service surface, 并不exists仓库级 `index.ts 只能 re-export` contract.  |
| P-0042 | src/platform/five-plane-control-plane/iam/policy-engine.ts:83-92,116 | `decisionCache` only在 `invalidate` 时整表 `clear`, 无independent TTL 扫描; TTL 5s 但只在命中时validation, cached键空间随 decisionId×action×fingerprint 笛卡尔增长, 未命中即变僵尸条目直至全清 src/platform/five-plane-control-plane/iam/policy-engine.ts:83-92,116   | 已修复 | decisionCache 只在命中时做 TTL validation, 没有主动cleanupexpiry僵尸键.  |
| P-0043 | src/platform/five-plane-control-plane/iam/secret-management-service.ts | src/platform/five-plane-control-plane/iam/secret-management-service.ts ~37 KB 单file持有 12+ 类 (cache/rotation/audit/lease/...) , violates SRP; rotation timer (P-0389) + silently吞错 (P-0027) concentrated于此 → riskcoupling中心   | 已复核关闭 | Root cause: 旧快照把“file偏大”directly等同于independentmissing陷; 当前 secret provider, 限流, 支持type和审计协作已拆到 support/provider module, 剩余主要是可maintained性债务, 不再单列为runtimemissing陷.  |
| P-0044 | src/platform/five-plane-control-plane/iam/jwt-utils.ts:29 (OIDC/JWT | src/platform/five-plane-control-plane/iam/jwt-utils.ts:29 (OIDC/JWT validation异常吞噬, P-0005) 位于identity核心path, 安全审计需按"必须 fail-closed"重写   | 已复核关闭 | 旧审计针对 JWT validation异常吞噬; 当前 decode failure已 fail-close.  |
| P-0045 | src/platform/five-plane-control-plane/config-center/config-audit-service.ts:226-251 | `SELECT * FROM config_audit_entries ORDER BY timestamp DESC` 无 LIMIT/pagination, 全表加载到in-memory后再 `.map(JSON.parse(...))`; 审计表会随时间unbounded growth src/platform/five-plane-control-plane/config-center/config-audit-service.ts:226-251   | 已修复 | configure审计querydefault按全表读取, missing少returnupper limit.  |
| P-0046 | src/platform/five-plane-control-plane/cost-alert/cost-alert-service.ts:540<br>src/scale-ecosystem/federation/federation-gateway.ts:581 | src/platform/five-plane-control-plane/cost-alert/cost-alert-service.ts:540 emit `cost.threshold.exceeded` (点号) , src/scale-ecosystem/federation/federation-gateway.ts:581 emit `federation-event` (连字符) , 与注册表 `cost:limit_reached` (冒号) 三种事件naming风格并存 (P-0028/P-0332) , missing统一 lint   | 已修复 | Root cause:  cost/federation 历史implementation各自沿用局部事件naming习惯, 没有持续的注册表对照门禁; 相关 emitters 已统一, 且新增 runtime event 审计脚本持续守门.  |
| P-0047 | src/platform/five-plane-control-plane/incident-control/release-pipeline-support.ts:215-238 | `spawn` 后只挂 `error/close`, stdout/stderr only push 到无界 `stdoutChunks`/`stderrChunks` array, 超大output可 OOM; missing少outputtruncated/背压机制 src/platform/five-plane-control-plane/incident-control/release-pipeline-support.ts:215-238 与 deployment-execution-service.ts:165-194   | 已修复 | 命令execute器把 stdout/stderr 累积到无界array, 没有outputsizeupper limit.  |
| P-0048 | src/platform/five-plane-control-plane/iam/secret-management-service.ts:719 | src/platform/five-plane-control-plane/iam/secret-management-service.ts:719 `logger.info(phase === "initial" ? "secret.rotation.scheduled_initial" : "secret.rotation.scheduled", {...})` 等结构化log事件名naming不在 contracts/event_registry 内 (与 P-0480 互证, 扩大到 secret 域)    | 已修复 | secret rotation scheduler 新增了事件名, 但注册表文档没有synchronous登记, 形成 contract drift.  |
| P-0049 | src/platform/five-plane-control-plane/iam/vault-http-secret-provider.ts:179-180 | src/platform/five-plane-control-plane/iam/vault-http-secret-provider.ts:179-180 `if (loginData.auth?.client_token) { this._cachedToken = loginData.auth.client_token; }` 把 client_token cached到实例field (无 TTL/再validation保障) , rotation 期间易stale   | 已修复 | Vault AppRole 登录后cached token 时没有最小 TTL 保护, stale凭据可能长期滞留.  |
| P-0050 | src/platform/five-plane-control-plane/compliance/<br>src/platform/compliance/ | `src/platform/five-plane-control-plane/compliance/`(crypto-shredding/erasure 等) 与顶级 `src/platform/compliance/`(`compliance-bootstrap.ts`, `lineage/`, `encryption/`) 主题双份.    | 已复核关闭 | Root cause: 审查把“同主题”误判为“duplicateimplementation”; 当前顶级 `platform/compliance` 承担跨平面 bootstrap/能力汇总, 而 `five-plane-control-plane/compliance` 是Control Plane的具体治理服务, 分层职责不同.  |
| P-0051 | src/platform/five-plane-control-plane/config-center/config-versioning-service.ts:797-799 | `cloneConfigContent` 用 `JSON.parse(JSON.stringify(content))` 深拷贝完整configurecontent, 热path每次写入版本快照都触发 O(N) 序列化+反序列化, 且loss Date/Map/undefined 语义; 该项目其他位置已有 `structuredClone`, 此处明显落后 src/platform/five-plane-control-plane/config-center/config-versioning-service.ts:797-799   | 已修复 | configure版本快照仍uses JSON 序列化深拷贝, 热path成本高且会lossdata语义.  |
| P-0052 | src/platform/five-plane-control-plane/iam/secret-management-service.ts:53-82 | `SecretResolutionRateLimiter.requests: Map<string, number[]>` 无 LRU/expirycleanup; 每个 callerId 永驻 Map, 且 `check` 内 `.filter` 在每次call O(window 内 timestamp 数) 重建array并 `set` 回去 src/platform/five-plane-control-plane/iam/secret-management-service.ts:53-82   | 已修复 | secret 解析限流器只追加 callerId 时间戳, 不会回收长期不用的call方state.  |
| P-0053 | src/platform/five-plane-control-plane/iam/secret-management-service.ts:67-81 | read-modify-write 无lock: 先 `requests.get(callerId)`, filter 后 `requests.set(...)`; 纯 JS 单线程下虽不会撕裂, 但 await 边界穿插的call方易出现 TOCTOU (已观察 callerId 计数与新 push 间漏窗) src/platform/five-plane-control-plane/iam/secret-management-service.ts:67-81   | 已复核关闭 | `check()` 全程synchronousexecute, 没有 `await` 边界; 原 TOCTOU 描述不符合当前implementation, 但已顺带补上state封装与回收.  |
| P-0054 | src/platform/five-plane-control-plane/iam/secret-management-service.ts:749 | `setInterval(() => runRotationSweep("interval", rotationInterval), ...)` 内部异步 sweep 无 in-flight 标志, 长 sweep 可能与下个 tick parallel启动多份 rotation 流, 触发 secret rotate 重入 src/platform/five-plane-control-plane/iam/secret-management-service.ts:749   | 已修复 | 轮转调度器按fixed周期触发, 但没有防重入保护, 长耗时 sweep 可与下个 tick overlap.  |
| P-0055 | src/platform/five-plane-orchestration/planner/plan-graph-normalizer.ts:46 | `@ts-expect-error` 出现在生产path src/platform/five-plane-orchestration/planner/plan-graph-normalizer.ts:46   | 已修复 | 生产归一化pathdependency @ts-expect-error 掩盖type问题.  |
| P-0056 | src/platform/five-plane-orchestration/routing/intake-router-model.ts:657-658 | `matchRule` 每次call都 `new RegExp(...)`; intake 路由是request级 hot path src/platform/five-plane-orchestration/routing/intake-router-model.ts:657-658   | 已修复 | intake 路由匹配在热pathduplicate编译regex, 未做cached.  |
| P-0057 | src/platform/five-plane-orchestration/agent-delegation/delegation-manager.service.ts<br>AGENTS.md | src/platform/five-plane-orchestration/agent-delegation/delegation-manager.service.ts ~41 KB; naming `*.service.ts` 加点号是少见的混合风格 (kebab + 中缀), 与仓库"kebab-case .ts"约定 (AGENTS.md) 不符   | 已复核关闭 | Root cause: 审查把file后缀约定写得过严; 仓库当前同时exists `plugin-executor.service.ts` 等同类naming, `*.service.ts` 已是compatibility可accepts模式, AGENTS only约束基础file名保持 kebab-case.  |
| P-0058 | src/platform/five-plane-orchestration/harness/guardrails/guardrail-vibration-breaker.ts:28 | secret 比较uses `===`: `signal.signature === "proceed"` 决定放行 src/platform/five-plane-orchestration/harness/guardrails/guardrail-vibration-breaker.ts:28   | 已复核关闭 | 比较的是控制流常量 proceed, 不是秘密值对比, 不涉及常量时间比较risk.  |
| P-0059 | src/platform/five-plane-orchestration/hitl/hitl-operator-console-service.ts:65-68 | `for (const channel of channels) { await this.notifier({...}) }` serialnotification多 channel, 无 Promise.all src/platform/five-plane-orchestration/hitl/hitl-operator-console-service.ts:65-68   | 已修复 | 多 channel notificationserial await, 未concurrent化.  |
| P-0060 | src/platform/five-plane-orchestration/improve-rollout/rollout/rollout-scheduler.ts:126-128 | `for (const input of inputs) { decisions.push(await this.advance(input)); }` serial await, missing少 Promise.all src/platform/five-plane-orchestration/improve-rollout/rollout/rollout-scheduler.ts:126-128   | 已修复 | batch rollout advance serial等待, 未concurrentexecute.  |
| P-0061 | src/platform/five-plane-orchestration/harness/context-assembler.ts:135-140 | `applyRedaction` 每次call都 `for of redactPatterns` 中 `new RegExp(...)`; redaction 在每条上下文 entry 都触发, O(entries × patterns) 次 RegExp 编译 src/platform/five-plane-orchestration/harness/context-assembler.ts:135-140   | 已修复 | context redaction 对每条recordduplicate编译regex, missing少cached.  |
| P-0062 | src/platform/five-plane-orchestration/oapeflir/handoff-model.ts:86 | `JSON.parse(JSON.stringify(handoff))` 深拷贝 AgentHandoff, handoff payload 通常含较大上下文; O(N) 序列化在每次切换都execute, 且与 `handoff-serializer.ts:16` 已用的 `structuredClone` inconsistent src/platform/five-plane-orchestration/oapeflir/handoff-model.ts:86   | 已修复 | handoff 深拷贝仍uses JSON 序列化, 未与 structuredClone 对齐.  |
| P-0063 | src/platform/five-plane-orchestration/agent-delegation/delegation-manager.service.ts:826-831 | `findRepositoryRecordsByStatuses` 对 6 个 active state发 6 次independent `findByStatus` query, 典型 N query, 应单次 `WHERE status IN (...)`; call点 line 429/607/662/809 在 hot path 上均触发 src/platform/five-plane-orchestration/agent-delegation/delegation-manager.service.ts:826-831   | 已复核关闭 | Root cause:  delegation repository 当前按 status 分桶暴露querycontract, A 表把小基数 active-status 聚合directly上升为通用 N+1 missing陷; 这是仓储能力边界, 不是 correctness missing口.  |
| P-0064 | src/platform/five-plane-orchestration/agent-delegation/delegation-manager.service.ts:429-435,607,662,809 | 取回每个 status array后再 `for record of records` + `record.delegationChain.includes(agentId)`, 相当于客户端in-memoryfilter; 应改为带 `delegation_chain @> [agentId]` index的 SQL/JSON query src/platform/five-plane-orchestration/agent-delegation/delegation-manager.service.ts:429-435,607,662,809   | 已复核关闭 | Root cause: 审查把 bounded active-set 的in-memory筛选误记成indexmissing陷; 现行仓储没有 JSON containment querycontract, call侧只是在通用record集上做二次filter.  |
| P-0065 | src/platform/five-plane-orchestration/oapeflir/oapeflir-loop-core.ts<br>src/platform/five-plane-execution/oapeflir/ (only | src/platform/five-plane-orchestration/oapeflir/oapeflir-loop-core.ts 单file 1004 行 (P-0129) , 与 src/platform/five-plane-execution/oapeflir/ (only 2 file壳, P-0103) 形成"implementationconcentrated在 orchestration, 声明位于 execution"的巨型断层   | 已复核关闭 | 目录清单式审查把 execution/oapeflir 的 runtime bridge 与 orchestration 的 canonical 闭环implementation混作duplicate; Execution Plane README 已明确该目录只保留运行桥接边界.  |
| P-0066 | src/platform/five-plane-orchestration/escalation/index.ts:15 | `src/platform/five-plane-orchestration/escalation/index.ts:15` import `../../../ops-maturity/emergency/resume-protocol/index.js`; `five-plane-orchestration/learn/{learning-feedback-orchestration-service,llm-improvement-generation-service,strategy-learning-service,failure-pattern-miner}.ts:1-3` 与 `evaluator/evaluator-service.ts:16` 都 import `../../../scale-ecosystem/feedback-loop/collector/feedback-model.js`: 平面反向dependency ops-maturity 与 scale-ecosystem.    | 已复核关闭 | Root cause: 把 orchestration 的横切学习/应急dependency误判成 accidental reverse import; 这些module消费的是反馈闭环与恢复协议的 canonical 横切能力, 不是在 orchestration 内再造第二套 plane.  |
| P-0067 | - | `Math.random()` used for操作/事件 ID (非加密但作为追踪标识可被预测) : execution-worker-handshake-service-async.ts:776   | 已复核关闭 | 审计references的 Math.random 位置已不在当前implementation中, 属于stale结论.  |
| P-0068 | src/platform/five-plane-execution/execution-engine/multi-step-utils.ts:57 | error吞噬: `} catch {` silently忽略输入解析异常 src/platform/five-plane-execution/execution-engine/multi-step-utils.ts:57   | 已复核关闭 | catch branchused for宽容解析fallback, 不是silently吞掉业务failure.  |
| P-0069 | src/platform/five-plane-execution/compensation-manager.ts:224-256:  | (N-0301) src/platform/five-plane-execution/compensation-manager.ts:224-256: `executeCompensationSteps` 无逐步persistence, 中途crashed后重做导致非幂等副作用   | 已复核关闭 | Root cause:  A 表把主审计里已降级为幂等augmentation项的残余risk继续当作当前 correctness missing陷挂账; 现行补偿主链已有人审与补偿治理收口.  |
| P-0070 | src/platform/five-plane-execution/queue-metrics/index.ts:84,93 | 重试无upper limit/backoff: `failureReasons.push` / 队列 push 后无explicitly backoff 控制 (onlydependency外层) src/platform/five-plane-execution/queue-metrics/index.ts:84,93   | 已复核关闭 | Root cause: 外层调度backoffstrategy被误要求在 metrics helper 内再次duplicateimplementation; 当前队列指标组件并不是重试strategy权威边界.  |
| P-0071 | src/platform/five-plane-execution/tool-executor/web-fetch.ts:301 | src/platform/five-plane-execution/tool-executor/web-fetch.ts:301 `while (true)` 流式抓取, 没有explicitly跳出upper limit/最大字节guard, dependency外部 abort (容易在malicious server 下空转)    | 已修复 | `web-fetch` 只dependencytimeout和字节upper limit, 没有为高频小分片response设置independent chunk upper limit.  |
| P-0072 | src/platform/five-plane-execution/distributed-lock/sqlite-lock-adapter.ts:88,138 | typeassertionbypass: `lockKey as unknown as { lockName ...}` 二处 src/platform/five-plane-execution/distributed-lock/sqlite-lock-adapter.ts:88,138   | 已修复 | SQLite lock 适配器为compatibility旧形参directly双重assertion, bypass了编译期形状分辨.  |
| P-0073 | src/platform/five-plane-execution/queue/redis-queue-adapter.ts:544,551,602,665,694 | 隐式时间: `Date.now()` directly作为 redis ZSET 评分 / job id src/platform/five-plane-execution/queue/redis-queue-adapter.ts:544,551,602,665,694   | 已复核关闭 | Root cause: 把 Unix epoch 数值误记成“隐式时间”; 这里写入的是绝对毫秒时间戳used forsort和deduplication, 不涉及时区序列化歧义.  |
| P-0074 | src/platform/five-plane-execution/ha/leader-election-service.ts:710 | fire-and-forget Promise: `void this.attemptElection();` 选主流程无errorlog src/platform/five-plane-execution/ha/leader-election-service.ts:710   | 已修复 | 选主重试 fire-and-forget 未附带 catch 和log.  |
| P-0075 | src/platform/five-plane-execution/execution-engine/multi-step-supervisor.ts:365 | typeassertionbypass: `stepData as unknown as Record<string, unknown>` src/platform/five-plane-execution/execution-engine/multi-step-supervisor.ts:365   | 已修复 | supervisor 把未知 stepData directly双重assertion成 Record, bypassruntimevalidation.  |
| P-0076 | src/platform/five-plane-execution/execution-engine/call-governance.ts:306-320 | `historyByKey` only每个 key 内部裁剪到 1000, `Map` 本身 (key 数量) 无界; 高基数 key 集会累积 src/platform/five-plane-execution/execution-engine/call-governance.ts:306-320   | 已修复 | call治理只limit单 key 历史length, 未limit Map 键总量.  |
| P-0077 | src/platform/five-plane-execution/worker-pool/worker-drain-protocol.ts:97-100:  | (N-0297) src/platform/five-plane-execution/worker-pool/worker-drain-protocol.ts:97-100: `expiresAt<now` 的 lease 被 drain 视为 `completed`, loss in-flight task计数   | 已复核关闭 | Root cause: 把 drain 视角的“无需继续等待 expired lease”和 truth 层 in-flight 计数混为一谈; expiry lease 的真实回收由 lease reclaimer 负责.  |
| P-0078 | src/platform/five-plane-execution/plugin-executor/scoped-external-access-sandbox.ts:303 | sandbox 外部访问 `fetch(targetUrl, options)` 未在此处展示对私网/元data IP 的reject逻辑 src/platform/five-plane-execution/plugin-executor/scoped-external-access-sandbox.ts:303   | 已复核关闭 | Root cause: 审查时未继续追到上游 URL strategy与受限访问沙箱; 当前risk描述missing少对现有外联治理链的完整上下文, 因此按复核关闭.  |
| P-0079 | src/platform/five-plane-execution/ha/replay-worker.ts:126,129 | 自由text `throw new Error("ReplayWorker refuses replay policies that allow real side effects")` src/platform/five-plane-execution/ha/replay-worker.ts:126,129   | 已修复 | replay worker 对strategyrejectdirectly抛bare Error, missing少runtimeerrortype和error码.  |
| P-0080 | src/platform/five-plane-execution/execution-engine/multi-step-supervisor.ts:365 | src/platform/five-plane-execution/execution-engine/multi-step-supervisor.ts:365 `stepData as unknown as Record<string, unknown>` 把 step 体强转再写入 supervisor path, typeguardmissing   | 已修复 | step datamissing少explicitlytypeguard, directly强转进入 supervisor path.  |
| P-0081 | src/platform/five-plane-execution/worker-pool/worker-drain-protocol.ts:305-309:  | (N-0299) src/platform/five-plane-execution/worker-pool/worker-drain-protocol.ts:305-309: `requestFromProgress` 假设 `leaseId === nodeRunId`, drain progress 投影与 truth 主键coupling   | 已复核关闭 | Root cause: compatibility projection field被误读成 truth 主键; `requestFromProgress` 里的 leaseId/nodeRunId 映射只服务 drain 进度投影.  |
| P-0082 | src/platform/five-plane-execution/distributed-lock/redis-lock-adapter.ts | src/platform/five-plane-execution/distributed-lock/redis-lock-adapter.ts EVAL Lua 共 3 处分散写法 (line 165/185/211, P-0089 已记 EVALSHA missing) , 且无 `script_load` 启动期注册, 每次 EVAL 重传脚本字节   | 已复核关闭 | Root cause: 把热path脚本cached优化误判成当前 correctness missing陷; 现有 `EVAL` path属于性能 backlog, 功能正确性不受影响.  |
| P-0083 | src/platform/five-plane-execution/queue/ticket-priority-queue.ts:35-62: based onarray的优先级队列 | (N-0311) src/platform/five-plane-execution/queue/ticket-priority-queue.ts:35-62: based onarray的优先级队列 `enqueue` 用 `findIndex+splice`, `dequeue` 同样线性扫描, 单操作 O(n) 而非 binary heap O(log n)   | 已修复 | 优先队列implementation停留在线性array结构, 未采用堆结构.  |
| P-0084 | src/platform/five-plane-execution/tool-executor/code-diagnostics-service.ts:312 | 命令construction: `spawn("python3", ["-m", "py_compile", filePath], ...)` filePath 来源未explicitly做白名单/解析validation src/platform/five-plane-execution/tool-executor/code-diagnostics-service.ts:312   | 已修复 | Python 诊断execute前只在上层做工作区filter, 没有在 `spawn` 落点再次把path收敛到 workspace 相对path.  |
| P-0085 | src/platform/five-plane-execution/distributed-lock/sqlite-lock-adapter.ts:88,138 | src/platform/five-plane-execution/distributed-lock/sqlite-lock-adapter.ts:88,138 via `as unknown as { lockName: string; ownerId: string }` compatibility旧call形参, 没在编译期分辨, 同一函数走两套data形态   | 已修复 | 旧版compatibilityentry把字符串形参与 legacy 对象形参混在同一signature里, 用assertion硬转.  |
| P-0086 | src/platform/five-plane-execution/worker-pool/worker-drain-protocol.ts:118-144:  | (N-0298) src/platform/five-plane-execution/worker-pool/worker-drain-protocol.ts:118-144: `advancePhase` 不check活动 lease 即可 DRAIN→QUIESCE; `isDrainComplete` only看 `phase=TERMINATE`   | 已复核关闭 | Root cause: 把 phase 推进和 drain 完成判定混成同一语义; 当前协议里 phase 可前移, 但真正完成仍由 `TERMINATE` 判定.  |
| P-0087 | src/platform/five-plane-execution/dispatcher/execution-dispatch-service.ts:637-666:  | (N-0309) src/platform/five-plane-execution/dispatcher/execution-dispatch-service.ts:637-666: `rankWorkerEvaluations` 四遍 `.filter().map().filter().map()` 流水线, 命中 fallback 时 O(W²)   | 已修复 | worker sort热path先做多轮array流水线, 再duplicate回查候选 worker, 导致不必要的二次扫描.  |
| P-0088 | src/platform/five-plane-execution/recovery/repair-pipeline.ts:67,68,93,101,109,117,125,229,238,246,254 | 隐式时区: batch `new Date().toISOString()` 未injection clock 端口 (dependencysystem时间) src/platform/five-plane-execution/recovery/repair-pipeline.ts:67,68,93,101,109,117,125,229,238,246,254   | 已复核关闭 | 审计把 UTC 序列化误判成时区格式error, 实际这里不是隐式时区 bug.  |
| P-0089 | src/platform/five-plane-execution/distributed-lock/redis-lock-adapter.ts:49,165,185,211 | src/platform/five-plane-execution/distributed-lock/redis-lock-adapter.ts:49,165,185,211 call `redis.eval(script, ...)` execute Lua 脚本, 脚本via `EVAL` 而非 `EVALSHA`, 每次都重传脚本字节, 热path上无cached优化   | 已复核关闭 | Root cause: 把 `EVALSHA` cached优化误判成主链missing陷; 当前 redis Lua execute的功能正确性不dependency脚本cached.  |
| P-0090 | src/platform/five-plane-execution/tool-executor/semantic-repo-map-service.ts:423-430: 构建反向referencesgraph | (N-0313) src/platform/five-plane-execution/tool-executor/semantic-repo-map-service.ts:423-430: 构建反向referencesgraph `for file of files.values()` 嵌套 `for ref of references` → O(F·R), 仓库级数千file每次重建都跑   | 已复核关闭 | Root cause: 离线/按需 repo map 重建被误判成execute热path; 主审计已把它降为性能优化项.  |
| P-0091 | src/platform/five-plane-execution/distributed-lock/{redis,sqlite,pg-advisory}-lock-adapter.ts: 所有 | (N-0283) src/platform/five-plane-execution/distributed-lock/{redis,sqlite,pg-advisory}-lock-adapter.ts: 所有 lock adapter 均无 heartbeat/auto-renew, 且 release 未 clearTimeout, 靠call方手动续期/释放   | 已复核关闭 | Root cause: 把“module不内建后台续期 timer”的explicitly设计误判成missing陷; 当前 lock 生命周期本就由call方 renew/reacquire 控制.  |
| P-0092 | src/platform/five-plane-execution/distributed-lock/redis-lock-adapter.ts:162-166:  | (N-0279) src/platform/five-plane-execution/distributed-lock/redis-lock-adapter.ts:162-166: `releaseAsync` Lua onlyvalidation owner, 无 fencing-token, 已被强抢回的同 owner 延迟 release 也会success   | 已复核关闭 | Root cause: 把 acquire/write side 的 fencing 约束directly外推到 release side; 主审计已confirmation release 端无 fencing-token 强validation属于subsequentaugmentation项.  |
| P-0093 | src/platform/five-plane-execution/ha/lease-reclaimer-service.ts:281-290,70:  | (N-0289) src/platform/five-plane-execution/ha/lease-reclaimer-service.ts:281-290,70: `scheduleNextReclaim` 链式 setTimeout, stop pathonly设 `running=false/disposed=true`, 进行中的 setTimeout never被 clear   | 已修复 | lease reclaimer 的调度链只保留下一次 timer handle, 未把正在execute的周期和 reschedule 逻辑绑定到 stop/dispose 语义.  |
| P-0094 | src/platform/five-plane-execution/startup/graceful-shutdown.ts:89-99: shutdown | (N-0294) src/platform/five-plane-execution/startup/graceful-shutdown.ts:89-99: shutdown 期间 `addHandler` only warn 即丢弃, 懒加载 DI 服务的 teardown silently失效   | 已修复 | graceful shutdown 把“已进入关闭流程后新增 handler”当成 warn 级别旁路, 没有 fail-fast.  |
| P-0095 | src/platform/five-plane-execution/dispatcher/execution-dispatch-service.ts:578-583, lease/types.ts:3-5: 心跳expiry阈值 | (N-0277) src/platform/five-plane-execution/dispatcher/execution-dispatch-service.ts:578-583, lease/types.ts:3-5: 心跳expiry阈值 30s 与 lease TTL DEFAULT=10s inconsistent, 10–30s window内同一execute可能被两个 worker 同时持有 (双execute)    | 已复核关闭 | Root cause: 旧审计based on“stale heartbeat 仍可参与选主”的前提; 主审计已confirmation无效 heartbeat 已 fail-close, TTL 差值only保留为保守调度window.  |
| P-0096 | src/platform/five-plane-execution/distributed-lock/sqlite-lock-adapter.ts:88:  | (N-0281) src/platform/five-plane-execution/distributed-lock/sqlite-lock-adapter.ts:88: `release` only按 `(lock_key,owner)` 删除, 无 fencing token / lease id validation, 旧 owner 重现可抹掉新 lease   | 已复核关闭 | Root cause: 把local单process sqlite lock adapter 当成跨节点分布式lock来审查; 主审计已将 fencing-token validation归类为远端分布式 adapter augmentation项.  |
| P-0097 | src/platform/five-plane-execution/budget-allocator.ts:279,761,771 | 隐式时区: `new Date(Date.now()).toISOString()` 形式duplicateuses src/platform/five-plane-execution/budget-allocator.ts:279,761,771   | 已复核关闭 | 这里output的是 UTC ISO 字符串, 审计把clockdependency误写成时区error.  |
| P-0098 | src/platform/five-plane-execution/lease/execution-lease-service-async.ts:212-226:  | (N-0287) src/platform/five-plane-execution/lease/execution-lease-service-async.ts:212-226: `renewLeaseSync` `lease.status !== "active"` checkduplicate, 第二块不可达死代码   | 已复核关闭 | Root cause: 冗余防御branch被误登记成runtimemissing陷; 主审计已confirmation这是整洁性问题, 不改变行为.  |
| P-0099 | src/platform/five-plane-execution/startup/process-error-handlers.ts:110-113 | src/platform/five-plane-execution/startup/process-error-handlers.ts:110-113 `setTimeout(... process.exit(1), 60_000)` 终极兜底, 写"Ultimate fallback", 但 timer 未 `unref()` (行内核查无 unref) , handler 自身可能阻止其它退出path   | 已复核关闭 | 当前代码中的兜底退出 timer 已 unref, 审计based on旧版本结论.  |
| P-0100 | src/platform/five-plane-execution/queue/ticket-priority-queue.ts:17-73 | 标称 "PriorityQueue" 实则array: `enqueue` 用 `findIndex`+`splice`(O(n)), `dequeue` 同样 `findIndex`+`splice`(O(n)); 高入队/出队场景退化为 O(n²), 应改用堆 src/platform/five-plane-execution/queue/ticket-priority-queue.ts:17-73   | 已修复 | enqueue 和 dequeue 的算法选型仍是array线性扫描, 复杂度退化.  |
| P-0101 | src/platform/five-plane-execution/startup/graceful-shutdown.ts:125-127:  | (N-0292) src/platform/five-plane-execution/startup/graceful-shutdown.ts:125-127: `handleSignal` 先 `unregisterSignalHandlers()` 再跑 handler; shutdown 期间第二次 SIGINT 落到内核default行为, bypass强退 timer   | 已修复 | 信号handle在真正完成 shutdown 前就解绑 listener, 导致第二个信号跳出受控退出path.  |
| P-0102 | src/platform/five-plane-execution/tool-executor/tool-output-sanitizer.ts:355-356:  | (N-0312) src/platform/five-plane-execution/tool-executor/tool-output-sanitizer.ts:355-356: `for definition of SECRET_PATTERN_DEFINITIONS` 中 `new RegExp(definition.source, definition.flags)` 每次output消毒重新编译, 每次工具call都跑   | 已修复 | outputsanitized每次call都会重新construction secret regex.  |
| P-0103 | src/platform/five-plane-execution/oapeflir/<br>src/platform/five-plane-orchestration/oapeflir/ | `src/platform/five-plane-execution/oapeflir/`(2 file壳) 与 `src/platform/five-plane-orchestration/oapeflir/`(20+ file) 同名概念在两个平面下并存, 目录名跨平面conflict.    | 已复核关闭 | Root cause: 旧快照没把Execution Plane README 边界算进来; 当前 `five-plane-execution/oapeflir/README.md` 已明确这里只保留 runtime plan execution bridge, 编排闭环 canonical 仍在 orchestration 面.  |
| P-0104 | src/platform/five-plane-execution/dispatcher.ts:1-7<br>src/platform/five-plane-execution/dispatcher/index.ts | `src/platform/five-plane-execution/dispatcher.ts:1-7` 是compatibility垫片, 与 `src/platform/five-plane-execution/dispatcher/index.ts` 同名导出, file vs 同名目录并存.    | 已复核关闭 | Root cause: 审查把compatibility shim 当成duplicateimplementation; `dispatcher.ts` 现在已explicitly标注 legacy compatibility shim, canonical surface 在 `dispatcher/index.ts`.  |
| P-0105 | src/platform/five-plane-execution/lease/execution-lease-service-async.ts:285-298,417-446:  | (N-0286) src/platform/five-plane-execution/lease/execution-lease-service-async.ts:285-298,417-446: `releaseLease` 在 `lease.expiresAt <= occurredAt` 时return `lease_expired`, owner 不能主动释放已expiry lease, 需等 `reclaimExpiredLeases`   | 已复核关闭 | Root cause:  A 表把 N-0286 已复核收口的 expired-lease 幂等语义duplicate展开; expiry lease 的权威回收本就由 `reclaimExpiredLeases` 负责, 避免 owner 误删新租约.  |
| P-0106 | src/platform/five-plane-execution/queue/ | `src/platform/five-plane-execution/queue/`(7 file)与同级 `five-plane-execution/queue-metrics/`(只 `index.ts`) 单复数/splitinconsistent; `five-plane-execution/state-transition/` 与 `five-plane-execution/runtime-state-machine.ts` 主题划分不清.    | 已复核关闭 | Root cause: 审查把“naming不对称”误当成职责conflict; 当前 `queue/` 承载调度与存取核心, `queue-metrics/` 是independent观测面, `runtime-state-machine.ts` 也与 `state-transition/` 的局部能力边界不同.  |
| P-0107 | src/platform/five-plane-execution/tool-executor/skill-creator-service.ts:604-614:  | (N-0295) src/platform/five-plane-execution/tool-executor/skill-creator-service.ts:604-614: `assertTargetPathAllowed` 在 `existsSync(skillPath)` 与 `realpathSync` 之间exists TOCTOU   | 已复核关闭 | Root cause:  A 表把 N-0295 已复核收口的filesystem层残余竞争duplicate挂账; 当前pathvalidation仍以 `realpathSync` 锚定受控目录, 未发现仓内可利用写入path.  |
| P-0108 | src/platform/five-plane-execution/tool-executor/web-fetch.ts:301<br>src/platform/five-plane-control-plane/iam/outbound-url-policy.ts | src/platform/five-plane-execution/tool-executor/web-fetch.ts:301 `while (true) { ... }` 流式抓取无最大字节guard与最大循环次数 (P-0071) , 结合 src/platform/five-plane-control-plane/iam/outbound-url-policy.ts SSRF 防护成对, 安全门只在entryvalidation URL, 下游 body size未 cap   | 已修复 | SSRF entry只limit目标 URL, response体读取链路missing少independent的流分片upper limit, 给长尾流式response留下资源消耗面.  |
| P-0109 | src/platform/five-plane-execution/recovery/patch-bundle.ts:139 | `matchPath` 每次都 `new RegExp(\`^${regex}$\`).test(path)`, patch bundle 大量pathvalidation时duplicate编译 src/platform/five-plane-execution/recovery/patch-bundle.ts:139   | 已修复 | patch bundle path匹配每次dynamic编译 glob regex, 未复用cached.  |
| P-0110 | src/platform/five-plane-execution/dispatcher/execution-dispatch-service.ts:482: dispatch | (N-0296) src/platform/five-plane-execution/dispatcher/execution-dispatch-service.ts:482: dispatch worker snapshot 一次性加载, 循环中 `runningExecutionIds` 不重读, concurrent超额分配后由 lease failurerollback兜底 (产生派发rollback泥潭)    | 已复核关闭 | Root cause:  A 表把 N-0296 已复核收口的transaction外候选快照再次展开; worker 视graph只used forsort, 最终concurrent闸门仍在 lease/claim transaction.  |
| P-0111 | src/platform/five-plane-execution/startup/process-error-handlers.ts:110-113 | src/platform/five-plane-execution/startup/process-error-handlers.ts:110-113 终极兜底 `setTimeout(... process.exit(1), 60_000)` 未 `unref()` (已 P-0099 一句记, 进一步: 该 timer 本身就是为 uncaughtException handler 兜底, 若不 unref 反而阻止正常 exit code 触发 — 自相矛盾)    | 已复核关闭 | 与 P-0099 相同, 当前代码已 unref, 这一条是duplicate且based on旧代码.  |
| P-0112 | src/platform/five-plane-execution/dispatcher/execution-dispatch-service.ts:561-631:  | (N-0308) src/platform/five-plane-execution/dispatcher/execution-dispatch-service.ts:561-631: `evaluateWorkersForTicket` 的 `requiredCapabilities.filter(c => !worker.capabilities.includes(c))` 让单 ticket 评估变 O(W·C·K), 未将 capabilities 预转 Set   | 已修复 | worker 能力集在每次评估时反复线性 `includes`, 没有局部index化.  |
| P-0113 | src/platform/five-plane-execution/recovery/runtime-recovery-decision-service.ts:286:  | (N-0304) src/platform/five-plane-execution/recovery/runtime-recovery-decision-service.ts:286: `recordDecision` 把 `executionRecord?.traceId ?? null` 写入 EventRecord, 下游按 traceId 关联 trace 时loss关联键   | 已复核关闭 | Root cause:  A 表把 N-0304 已复核收口的异常path语义duplicate展开; 有 executionRecord 时 traceId 仍会沿用, missing execution 的record写 null 是受控降级.  |
| P-0114 | src/platform/five-plane-execution/budget-allocator.ts:481-516:  | (N-0273) src/platform/five-plane-execution/budget-allocator.ts:481-516: `persistRelease(settlement) → transition → untrack → 计算 ledger → persistLedger` 五步未在同一transaction, settlement 已写但 ledger CAS 前crashed → 漏冲销   | 已复核关闭 | Root cause:  A 表把 N-0273 已复核收口的 release transaction边界说明duplicate挂账; 当前主链仍优先走 atomic repository path, synchronous fallback 只是 legacy baseline.  |
| P-0115 | src/platform/five-plane-execution/budget-allocator.ts:384-435: settle | (N-0180) src/platform/five-plane-execution/budget-allocator.ts:384-435: settle synchronous fallback path `persistSettlement → transition → untrack → construction新 ledger → persistLedger` non-atomic, settlement persistence后crashed留下"已结算未冲销"   | 已复核关闭 | Root cause:  A 表把 N-0180 已复核收口的 legacy fallback order写duplicate展开; 当前预算主链已优先uses原子仓储implementation.  |
| P-0116 | src/platform/five-plane-execution/startup/startup-consistency-checker.ts:18 | `src/platform/five-plane-execution/startup/startup-consistency-checker.ts:18` 与 `dispatcher/execution-deviation-detector.ts:2` directly reach `../../five-plane-orchestration/oapeflir/workflow/minimal-workflow.js`, `oapeflir/types/index.js`, Execution Plane访问Orchestration Planeprivate.    | 已复核关闭 | Root cause:  startup/deviation checker 本身就是 OAPEFLIR execution bridge, 需要读取最小 workflow/types contract; A 表把桥接缝dependency误判成private面out of bounds.  |
| P-0117 | src/platform/five-plane-execution/dispatcher/execution-dispatch-service.ts:193-208,462-479:  | (N-0330) src/platform/five-plane-execution/dispatcher/execution-dispatch-service.ts:193-208,462-479: `dispatchNext` 评估阶段usestransaction外快照 (availableSlots/activeLeaseCount) , transaction内 lease+claim 需多次rollback, 造成 dispatch 抖动   | 已复核关闭 | Root cause:  A 表把 N-0330 已复核收口的transaction外候选sortduplicate挂账; dispatch correctness 仍由transaction内 lease acquire/claim 保证.  |
| P-0118 | src/platform/five-plane-execution/startup/graceful-shutdown.ts:70-72: default | (N-0293) src/platform/five-plane-execution/startup/graceful-shutdown.ts:70-72: default `exitHandler` 只设 `process.exitCode`, 不调 `process.exit`; hot-reload setInterval / runtimeMetricsRegistry / signal listener residual keep-alive event loop   | 已修复 | default signal 退出逻辑只设置 exitCode, 没有在受控关闭完成后真正终止process.  |
| P-0119 | src/platform/five-plane-execution/ha/coordinator-load-balancing-service.ts:256-261 | `evaluations.filter().map(...)` 内部对每个 eligible item 调 `records.find((r) => r.coordinatorId === item.coordinatorId)`, O(n²) array扫描; 应预先建 `Map<coordinatorId, record>` src/platform/five-plane-execution/ha/coordinator-load-balancing-service.ts:256-261   | 已修复 | 负载均衡在映射阶段反复 records.find, missing少 coordinatorId index.  |
| P-0120 | src/platform/five-plane-execution/recovery/exception-recovery-config-loader.ts:15<br>src/platform/five-plane-control-plane/cost-alert/cost-alert-config-loader.ts:49 | module-level `cachedConfigs = new Map<string, CostAlertConfig>()` only靠手动 `clear()`, 无 TTL; 同模式见 `src/platform/five-plane-execution/recovery/exception-recovery-config-loader.ts:15` src/platform/five-plane-control-plane/cost-alert/cost-alert-config-loader.ts:49   | 已修复 | configure loader uses常驻 Map cached, only靠手动 clear, 没有 TTL 失效.  |
| P-0121 | src/platform/five-plane-execution/ha/leader-election-service.ts:589-593,626-628 | 续租/心跳 `setInterval` 中均directlysynchronous发起 IO, 无 in-flight 防护; 网络抖动时多次overlap续租可造成 fencing error src/platform/five-plane-execution/ha/leader-election-service.ts:589-593,626-628   | 已修复 | leader election 把续租和心跳directly挂在 `setInterval` 上, 没有阻止上一轮 IO 未完成时重入下一轮.  |
| P-0122 | src/platform/five-plane-execution/tool-executor/skill-execution-support.ts:371-399 | `defaultGitHeadResolver` via `child.stdout?.removeAllListeners()` cleanup, 但 `child` 自身的 `error/close/exit` 事件not registered listener; spawn failure (ENOENT 等) 会触发 `Error` 事件无监听, 导致process级 unhandled error src/platform/five-plane-execution/tool-executor/skill-execution-support.ts:371-399   | 已修复 | git HEAD 解析辅助只coverage了 `error` 和 `close`, 没有把 `exit` 纳入同一 settle path.  |
| P-0123 | src/platform/five-plane-execution/worker-pool/execution-worker-writeback-service.ts:391:  | (N-0300) src/platform/five-plane-execution/worker-pool/execution-worker-writeback-service.ts:391: `existingAgentExecution` transaction外读, 进入transaction后未重读 → TOCTOU, only靠 consume/update 兜底   | 已复核关闭 | Root cause:  A 表把 N-0300 已复核收口的transaction外快速rejectpathduplicate展开; 权威conflicthandle仍在transaction内 consume/update.  |
| P-0124 | src/platform/five-plane-execution/recovery/runtime-recovery-decision-service.ts:175-176:  | (N-0302) src/platform/five-plane-execution/recovery/runtime-recovery-decision-service.ts:175-176: `db.transaction` 当前为synchronouscontract但代码comment承认future要改 async; `AuthoritativeSqlDatabase.transaction` 一旦改异步将silently丢transactionatomicity   | 已复核关闭 | Root cause:  A 表把 N-0302 已复核收口的future演进comment误当成当前missing陷; 现行 `transaction` 仍是synchronouscontract.  |
| P-0125 | src/platform/five-plane-execution/run-termination-cleanup.ts:137-349, lease/execution-lease-service.ts:583-604:  | (N-0278) src/platform/five-plane-execution/run-termination-cleanup.ts:137-349, lease/execution-lease-service.ts:583-604: `reclaimExpiredLeases / reclaimActiveLeaseInternal` 不触发 cleanup, worker crash path下 budget_reservation/secret/timer/hitl_wait 等资源无人cleanup   | 已复核关闭 | Root cause:  A 表把 N-0278 已复核收口的 crash reclaim residual risk duplicate挂账; 资源cleanup由 drain/termination 协议concentrated承担.  |
| P-0126 | src/platform/five-plane-execution/budget-allocator.ts:222-238,424-429,511-516:  | (N-0275) src/platform/five-plane-execution/budget-allocator.ts:222-238,424-429,511-516: `reserve/settle/release` 对 `hierarchyLedgers` 逐条 `reserveBudgetHardCap+persistLedger`, 主 ledger 与 hierarchy 不在同transaction, partial写入后异常使父子 ledger misalignment   | 已复核关闭 | Root cause:  A 表把 N-0275 已复核收口的 hierarchy ledger 存储能力约束duplicate展开; 当前仍via同一 BudgetAllocator 主链serial更新.  |
| P-0127 | src/platform/five-plane-execution/recovery/runtime-recovery-decision-service.ts:344-351:  | (N-0303) src/platform/five-plane-execution/recovery/runtime-recovery-decision-service.ts:344-351: `moveExecutionToDeadLetter` 内 `new MemoryService(store).recordFailureMemory` 在transaction回调里execute, 但 MemoryService 是否复用同一 db handle 未viatype保证 (only约定)    | 已复核关闭 | Root cause:  A 表把 N-0303 已复核收口的仓储handle约定duplicate挂账; 当前 failure memory 与 DLQ 仍shared同一 task store construction.  |
| P-0128 | src/platform/five-plane-execution/queue/redis-queue-adapter.ts:543-607,559-565:  | (N-0284) src/platform/five-plane-execution/queue/redis-queue-adapter.ts:543-607,559-565: `dequeueAsync` 在validation status 前先 `hincrby attempts`, concurrent dequeue 竞态会对另一个 consumer 拿到的 job 也增加 attempts   | 已复核关闭 | Root cause:  A 表把 N-0284 已复核收口的 adapter 级竞态说明duplicate展开; consumer concurrent仍由 worker-pool/dispatcher 主链控制.  |
| P-0129 | src/platform/five-plane-execution/oapeflir/<br>src/platform/five-plane-orchestration/oapeflir/ (P-0103) : 除目录conflict外 | src/platform/five-plane-execution/oapeflir/ vs src/platform/five-plane-orchestration/oapeflir/ (P-0103) : 除目录conflict外, 前者 `oapeflir-loop-core.ts` 单file 1004 行, 是仓库最大 .ts 之一, 单一file聚合 plan/run/decide/learn/observe 闭环职责, violates SRP   | 已复核关闭 | OAPEFLIR 体量与split节奏已在架构参考文档作为持续治理债登记; 当前剩余大file属于已知可maintained性问题, 不再单列为未识别runtimemissing陷.  |
| P-0130 | src/platform/five-plane-execution/tool-executor/skill-execution-service.ts:64 | `cache = new Map<string, CachedSkillExecutionEntry>()`: cache size由 `skill-execution-cache-methods.ts:261-266` LRU 裁剪, 但其 TTL only按 entry `expiresAt` 在 `getCachedEntry` 命中时延迟check (line 221-230) , expiry entry 在无访问时常驻 Map, 无后台扫描clear src/platform/five-plane-execution/tool-executor/skill-execution-service.ts:64   | 已复核关闭 | Root cause:  skill execution cache 采用 `TTL + LRU` 的惰性淘汰strategy; A 表把“无后台扫描”directly等同leaks, 但当前 Map 仍受容量upper limit裁剪.  |
| P-0131 | src/platform/five-plane-execution/dispatcher/execution-dispatch-service.ts:893-904:  | (N-0310) src/platform/five-plane-execution/dispatcher/execution-dispatch-service.ts:893-904: `getCandidateWorker` 在 store 没提供 `getWorker` 时退化为 `listWorkers().find(...)` 每次 O(W), 上层 ranking map duplicatecall → O(W²) per ticket   | 已复核关闭 | Root cause:  A 表把 N-0310 已复核收口的testing替身 fallback pathduplicate展开; 生产 store 提供 `getWorker`, O(W) 扫描不是主链表现.  |
| P-0132 | src/platform/five-plane-execution/lease/execution-lease-service-async.ts:155,511:  | (N-0288) src/platform/five-plane-execution/lease/execution-lease-service-async.ts:155,511: `handoverLease` 用 `previousLease.fencingToken+1`, 与 `acquireLease` 用 `getLatestFencingToken(executionId)+1` path不同, concurrent handover+acquire 可产出相等 token   | 已复核关闭 | Root cause:  A 表把 N-0288 已复核收口的 fencing-token 理论竞争duplicate挂账; 实际 handover/acquire 仍受 active lease state与仓储 CAS serial化.  |
| P-0133 | src/platform/five-plane-execution/ha/wal-checkpoint-service.ts:599-602,292,330,411,422,515:  | (N-0316) src/platform/five-plane-execution/ha/wal-checkpoint-service.ts:599-602,292,330,411,422,515: `walEntriesWritten++/checkpointsCreated++/entriesReplayed++` 在跨 await 异步path修改同一 metrics 对象, 回放与正常写入concurrent计数inconsistent   | 已复核关闭 | Root cause:  A 表把 N-0316 已复核收口的观测计数误差duplicate挂账; 这些 metrics 不参与 truth/recovery 决策.  |
| P-0134 | src/platform/five-plane-execution/distributed-lock/distributed-lock-types.ts:5-12, redis-lock-adapter.ts:117-135, pg-advisory-lock-adapter.ts:65-75:  | (N-0264) src/platform/five-plane-execution/distributed-lock/distributed-lock-types.ts:5-12, redis-lock-adapter.ts:117-135, pg-advisory-lock-adapter.ts:65-75: `DistributedLockAdapter.acquire` synchronoussignature在远端 adapter 全部 `throw LockingError("sync_*_not_supported")`, 多态contract名存实亡, call方无type层提示   | 已复核关闭 | Root cause:  A 表把 N-0264 已复核收口的synchronous/异步lockcontract差异duplicate展开; 远端 adapter fail-close 抛 `sync_*_not_supported` 是explicitly边界.  |
| P-0135 | src/platform/five-plane-execution/worker-pool/execution-worker-handshake-service.ts:684-755:  | (N-0315) src/platform/five-plane-execution/worker-pool/execution-worker-handshake-service.ts:684-755: `refreshWorkerSnapshot` based on读到的 `runningExecutionsJson` `mergeExecutionIds` 后整field写回, concurrent claim/heartbeat 各自based onstale set 计算 union → lost update   | 已复核关闭 | Root cause:  A 表把 N-0315 已复核收口的 worker 单-owner 假设下 residual risk duplicate挂账; 未发现多 claim 同 worker 的真实破坏path.  |
| P-0136 | src/platform/five-plane-execution/hibernation/wake-engine.ts:269<br>src/platform/five-plane-execution/ha/leader-election-service.ts:215 | 隐式时区: `new Date(Date.now() + ttl).toISOString()` 计算expiry时间usesprocesssystem时区/clock src/platform/five-plane-execution/hibernation/wake-engine.ts:269; duplicate模式 src/platform/five-plane-execution/ha/leader-election-service.ts:215; ha-coordinator-service-async.ts:117,214   | 已复核关闭 | 这里同样是 UTC ISO 序列化, 不exists审计描述的隐式时区格式error.  |
| P-0137 | src/platform/five-plane-execution/ha/cross-region-event-replication-service.ts:337-362,376,386-388:  | (N-0319) src/platform/five-plane-execution/ha/cross-region-event-replication-service.ts:337-362,376,386-388: `some+push` non-atomic TOCTOU, 相同 plan 在parallel重试path中可被duplicate入队   | 已复核关闭 | Root cause:  A 表把 N-0319 已复核收口的幂等队列优化项duplicate展开; duplicate入队仍由 event id/status 幂等handle兜底.  |
| P-0138 | src/platform/five-plane-execution/distributed-lock/redis-lock-adapter.ts:49 | src/platform/five-plane-execution/distributed-lock/redis-lock-adapter.ts:49 `eval(script: string, numberOfKeys: number, ...args: string[]): Promise<unknown>` interfacesignature定 `args: string[]`, 但 line 185 call `String(additionalMs), String(fencingToken), now`: `now` 未 `String()` wrapped, type与runtimeinconsistent (取决于上游 now type)    | 已复核关闭 | Root cause: 审查based on旧快照false positivetypeinconsistent; 当前 `now` 在call点已是 ISO 字符串, `eval(...args: string[])` contract未被破坏.  |
| P-0139 | src/platform/five-plane-execution/worker-pool/execution-worker-handshake-service.ts:552-572,574-662:  | (N-0314) src/platform/five-plane-execution/worker-pool/execution-worker-handshake-service.ts:552-572,574-662: `recordHeartbeat` 先在transaction外 `renewLease`, 再单独 `db.transaction` 写 worker 快照/心跳/agent_execution; non-atomic提交 → lease 已续期但 heartbeat_snapshot missing   | 已复核关闭 | Root cause:  A 表把 N-0314 已复核收口的 lease-first 可用性strategyduplicate展开; subsequent reconciliation 会补证据, 不会放宽executeauthorization.  |
| P-0140 | src/platform/five-plane-execution/dispatcher/index.ts:633-638,644,593-594:  | (N-0318) src/platform/five-plane-execution/dispatcher/index.ts:633-638,644,593-594: `getToolRegistry` modulesingleton; `executeToolCall("send-message")` `agentState.requestHistory.push(message)` 紧跟 `await this.executeSpawnedAgent(agentState)`, 跨 await 后再读写同一 agentState, concurrent send-message 同 agent 时 history order被穿插   | 已复核关闭 | Root cause:  A 表把 N-0318 已复核收口的同-agent 会话serial假设duplicate挂账; tool registry singleton本身只是工具目录cached.  |
| P-0141 | src/platform/five-plane-execution/execution-engine/multi-step-orchestration.ts:32-34<br>src/platform/five-plane-orchestration/oapeflir/oapeflir-loop-core.ts:69 | `src/platform/five-plane-execution/execution-engine/multi-step-orchestration.ts:32-34` 与 `multi-step-oapeflir-plan.ts:1-7` import `../../five-plane-orchestration/routing/*`, `oapeflir/types/*`, `oapeflir/workflow/*`; 同时 `src/platform/five-plane-orchestration/oapeflir/oapeflir-loop-core.ts:69` 与 `oapeflir-loop-support.ts:69` import `../../five-plane-execution/budget-allocator.js`, execution↔orchestration 双向循环.    | 已复核关闭 | Root cause:  OAPEFLIR runtime plan execute本来就是 orchestration model 与 execution budget/dispatch 的桥接缝; A 表把已知架构couplingduplicate挂成新增missing陷.  |
| P-0142 | src/platform/five-plane-execution/dispatcher/execution-dispatch-service.ts | 28 处 `*-async.ts` 与同名 `*.ts` 双套implementation (如 `src/platform/five-plane-execution/dispatcher/execution-dispatch-service.ts` vs `execution-dispatch-service-async.ts`, `five-plane-state-evidence/events/durable-event-bus.ts` vs `durable-event-bus-async.ts`, `five-plane-execution/lease/execution-lease-service.ts` vs `execution-lease-service-async.ts`) .    | 已复核关闭 | sync/async 镜像是架构参考文档 B1 已登记的双后端compatibility债; 当前镜像面via统一wrapper和testing维持行为一致, 属已登记技术债而非漏修 defect.  |
| P-0143 | src/platform/five-plane-state-evidence/truth/sqlite/sqlite-database.ts:914 | SQL 字符串拼接: `PRAGMA table_info("${tableName}")` only以引号兜底 src/platform/five-plane-state-evidence/truth/sqlite/sqlite-database.ts:914   | 已复核关闭 | Root cause: 旧审计漏看了 `tableHasColumn()` 前的标识符regexvalidation; 当前 `tableName` 已 fail-close, `PRAGMA table_info` 不是外部可injectionentry.  |
| P-0144 | src/platform/five-plane-state-evidence/events/typed-event-bus.ts:379,424 | `JSON.parse` 无 try/catch: 解析 typed-event-bus 事件 payload src/platform/five-plane-state-evidence/events/typed-event-bus.ts:379,424   | 已修复 | typed event payload directly `JSON.parse`, 异常path未转换为受控 `ValidationError`.  |
| P-0145 | src/platform/five-plane-state-evidence/knowledge/semantic-vector-store.ts:155 | src/platform/five-plane-state-evidence/knowledge/semantic-vector-store.ts:155 同模式, uses `${this.qualifiedTableName()}` directly拼 SQL, missing标识符references器   | 已修复 | 向量表名虽经validation, 但missing统一references器, SQL 标识符仍以bare字符串散落拼接.  |
| P-0146 | src/platform/five-plane-state-evidence/truth/sqlite/authoritative-task-store-legacy-compat.ts | src/platform/five-plane-state-evidence/truth/sqlite/authoritative-task-store-legacy-compat.ts ~52 KB (P-0158) , naming含 `legacy-compat` 但仍为生产path主调 (P-0158 已记) , file名语义对消费方mislead   | 已复核关闭 | 当前file是 delegating base 继承的抽象compatibility合同层, 不是directly承载业务逻辑的主implementation; `legacy-compat` 描述的是 API bridge 语义而非“never参与生产编译”.  |
| P-0147 | src/platform/five-plane-state-evidence/knowledge/semantic-vector-validation.ts:258 | fire-and-forget Promise: `.catch(() => 0)` 隐藏向量库探测error src/platform/five-plane-state-evidence/knowledge/semantic-vector-validation.ts:258   | 已修复 | 向量探测cleanuppath把异常directly catch 成 0, 导致failure上下文loss.  |
| P-0148 | src/platform/five-plane-state-evidence/events/projections/dispatch-projection.ts:237,259,276 | typeassertionbypass: `event.payloadJson as string` 多次 src/platform/five-plane-state-evidence/events/projections/dispatch-projection.ts:237,259,276   | 已复核关闭 | Root cause:  projection entry消费的就是事件仓储序列化后的 JSON 字符串, 局部 `as string` 是边界解包, 不是未validation外部输入.  |
| P-0149 | src/platform/five-plane-state-evidence/knowledge/semantic-vector-validation.ts:160,256 | SQL 字符串拼接: `${schema}.${tableName}` 拼入 DELETE/INSERT src/platform/five-plane-state-evidence/knowledge/semantic-vector-validation.ts:160,256   | 已修复 | validation流程自行拼接 `schema.table`, 未复用统一的已references限定表名.  |
| P-0150 | src/platform/five-plane-state-evidence/truth/runtime-truth-repository.ts:714 | typeassertionbypass: `command.aggregate as unknown as Record<string, unknown>` src/platform/five-plane-state-evidence/truth/runtime-truth-repository.ts:714   | 已修复 | 事实state初始化path用双重typeassertion摊平 aggregate, bypass了按聚合type收窄.  |
| P-0151 | src/platform/five-plane-state-evidence/truth/sqlite/authoritative-task-store-delegating-lifecycle.ts:54,150,176 | typeassertionbypass: `args as unknown as [...]` 三处 src/platform/five-plane-state-evidence/truth/sqlite/authoritative-task-store-delegating-lifecycle.ts:54,150,176   | 已修复 | compatibility legacy 形参时directly `as unknown as`, 没有explicitly参数guard和收窄step.  |
| P-0152 | src/platform/five-plane-state-evidence/knowledge/indexing/embedding-provider.ts:108 | SSRF 面: embedding provider directly `fetch(${baseUrl}/v1/embeddings)`, 无主机白名单 src/platform/five-plane-state-evidence/knowledge/indexing/embedding-provider.ts:108   | 已复核关闭 | Root cause: 把 operator configure的 embedding provider endpoint 与user可控 SSRF 输入混同; 这里的 `baseUrl` only来自部署环境, 不是runtimerequest面透传.  |
| P-0153 | src/platform/five-plane-state-evidence/truth/sql/phase_1a_schema_sql_part-{1..5}.ts | naming风格drift: `src/platform/five-plane-state-evidence/truth/sql/phase_1a_schema_sql_part-{1..5}.ts` 与 `truth/postgres/phase_1a_schema_ddl_part-{1..5}.ts` uses snake_case + 数字混合, 与仓库 kebab-case 约定相悖.    | 已复核关闭 | `authoritative-schema.ts` 与 `pg-schema-support.ts` 明确按 phase 装配这些 schema artifact; 这组file名服务于分阶段 DDL 装配, 不是面向通用业务module的naming约定.  |
| P-0154 | src/platform/five-plane-state-evidence/events/durable-event-bus-async.ts:47 | fire-and-forget Promise: `void Promise.resolve(handler(event)).catch(() => undefined)` src/platform/five-plane-state-evidence/events/durable-event-bus-async.ts:47   | 已复核关闭 | Promise.resolve(handler).catch 是刻意把synchronous和异步 handler 统一成受控 rejected promise.  |
| P-0155 | src/platform/five-plane-state-evidence/truth/runtime-truth-repository.ts:714 | src/platform/five-plane-state-evidence/truth/runtime-truth-repository.ts:714 `...(command.aggregate as unknown as Record<string, unknown>)` 在事实表写入path强转, potential脏data落库risk   | 已修复 | 聚合写入落库前靠强转摊平对象, skip了按聚合type构建受控field集.  |
| P-0156 | src/platform/five-plane-state-evidence/events/durable-event-bus.ts | src/platform/five-plane-state-evidence/events/durable-event-bus.ts 总 ~40 KB (P-0621 已记规模) , 单filecontains enqueue/deliver/retry/circuit-breaker/batch flush 多重职责 (与 oapeflir-loop-core.ts 同列"上帝file")    | 已复核关闭 | `durable-event-bus.ts` 仍是 events 子域的 canonical 聚合entry; 体量问题已在架构参考文档中record为module化治理债, 不再单列为未识别功能missing陷.  |
| P-0157 | src/platform/five-plane-state-evidence/knowledge/semantic-vector-store.ts:155,206,260,281 | SQL 字符串拼接: `INSERT INTO ${this.qualifiedTableName()}` / `FROM ${this.qualifiedTableName()}` src/platform/five-plane-state-evidence/knowledge/semantic-vector-store.ts:155,206,260,281   | 已修复 | 表identity在multiple placesduplicate以模板字符串展开, missing单一的已references限定名出口.  |
| P-0158 | src/platform/five-plane-state-evidence/truth/sqlite/authoritative-task-store-legacy-compat.ts | src/platform/five-plane-state-evidence/truth/sqlite/authoritative-task-store-legacy-compat.ts 长 ~52KB, naming前缀 `legacy-compat`, 但该file仍是主path import 目标 (grep `legacy-compat` 在 src 内 multiple placesreferences) , 与"legacy compatibility层"语义misalignment   | 已复核关闭 | 当前references关系显示它被多个 delegating 子类当作 legacy API 合同基类复用; 问题本质是compatibility层naming造成误读, 不是implementation角色与运行path失真.  |
| P-0159 | src/platform/five-plane-state-evidence/truth/sql/phase_1a_schema_sql_part-{1..5}.ts | src/platform/five-plane-state-evidence/truth/sql/phase_1a_schema_sql_part-{1..5}.ts 与 truth/postgres/phase_1a_schema_ddl_part-{1..5}.ts naming `phase_1a_schema_*_part-N.ts` (P-0153) 暴露分裂的 DDL 编排order为代码常量 → schema 修订必须改源代码而非 migrations   | 已复核关闭 | phase 分片是权威 bootstrap schema 的源码装配方式; 迁移仍然是增量演进主通道, 分片naming本身不等于“必须放弃 migrations”.  |
| P-0160 | src/platform/five-plane-state-evidence/events/durable-event-bus.ts:710,766,904,995 | fire-and-forget Promise: `void this.enqueueDelivery(consumerId, true)` multiple places无 catch src/platform/five-plane-state-evidence/events/durable-event-bus.ts:710,766,904,995   | 已修复 | 关键投递调度把 enqueue Promise directly fire-and-forget, failure只能dependency隐式 rejected promise.  |
| P-0161 | src/platform/five-plane-state-evidence/truth/session-dual-storage.ts:116-164 | `appendSessionEvent` 以synchronous `openSync/appendFileSync/fdatasyncSync` 在requestpathexecute两次磁盘 fsync + 双file写, blocks事件循环; session 事件高频时对 latency 影响显著 src/platform/five-plane-state-evidence/truth/session-dual-storage.ts:116-164   | 已复核关闭 | Root cause:  dual-storage session append 明确以synchronous fsync 换取 crash-safe 证据落盘; A 表把 durability tradeoff directly记成通用性能missing陷.  |
| P-0162 | src/platform/five-plane-state-evidence/truth/sqlite/repositories/dispatch-repository.ts:125,139,149 | src/platform/five-plane-state-evidence/truth/sqlite/repositories/dispatch-repository.ts:125,139,149 multiple places `SELECT ${EXECUTION_COLS_NO_PREFIX/EXECUTION_COLS} FROM ...`, 列清单常量化但 ORDER BY/LIMIT 仍hardcoded, missing统一 query-builder   | 已复核关闭 | Root cause:  query-builder missing属于风格和可maintained性建议, 不是当前 SQL 正确性或injectionmissing陷.  |
| P-0163 | src/platform/five-plane-state-evidence/truth/sqlite/repositories/operations-repository.ts:567: tenant | (N-0541) src/platform/five-plane-state-evidence/truth/sqlite/repositories/operations-repository.ts:567: tenant scoped vs global SQL via字符串拼接切换 → 同一逻辑产生两套 prepared plan, bypass better-sqlite3 statement cache   | 已复核关闭 | Root cause:  A 表把 N-0541 已复核收口的 deliberate plan split duplicate展开; tenant-scoped 与 global SQL branch是有意保留的语义差异.  |
| P-0164 | src/platform/five-plane-state-evidence/truth/sqlite/authoritative-task-store-delegating-lifecycle.ts:54,150,176 | src/platform/five-plane-state-evidence/truth/sqlite/authoritative-task-store-delegating-lifecycle.ts:54,150,176 三处 `as unknown as [string, ...]`/`as unknown as { lockName: string }`, bypass strict typedirectly对齐 legacy 形参; typecontract弱化   | 已修复 | legacy call桥接层missingexplicitly解包逻辑, directly靠双重assertion把新旧contract硬对齐.  |
| P-0165 | src/platform/five-plane-state-evidence/truth/sqlite/repositories/event-repository.ts:79:  | (N-0511) src/platform/five-plane-state-evidence/truth/sqlite/repositories/event-repository.ts:79: `insertCostEvent` INSERT references `pricing_version`/`provider_request_id`, 原始 `phase_1a_schema_sql_part-1.ts` 未声明, dependency migration 才能跑通; 新部署若先建 schema 再跑老 migration order会failure   | 已复核关闭 | 当前迁移后的 `cost_events` schema 已coverage `pricing_version/provider_request_id`; 原始 phase 1a missing口属于历史快照, 已不再代表现行部署state.  |
| P-0166 | src/platform/five-plane-state-evidence/dlq/index.ts:271-285 | `for (const record of records) { await retry(record); ... }` serial await 重试, 每条 DLQ orderhandle; 应在尊重幂等的前提下 `Promise.all`/分片concurrent src/platform/five-plane-state-evidence/dlq/index.ts:271-285   | 已复核关闭 | Root cause:  DLQ retry 明确选择serialexecute以保序和限背压; A 表把“未concurrent化”directly当成 correctness missing陷.  |
| P-0167 | src/platform/five-plane-state-evidence/knowledge/knowledge-ingestion-pipeline.ts:75-80,53-55,245-250: 文档分片用 | (N-0571) src/platform/five-plane-state-evidence/knowledge/knowledge-ingestion-pipeline.ts:75-80,53-55,245-250: 文档分片用 `split(/\n{2,}/)` (无 max chunk size, 无 overlap) , 每个 chunk 的 tokenCount 用 chars/4 估算 → 字符切分输入, token 单位output, 单位inconsistent   | 已复核关闭 | Root cause:  A 表把 N-0571 已复核收口的轻量 fallback 分片strategyduplicate挂账; 真实 token 预算已经下沉到query侧.  |
| P-0168 | src/platform/five-plane-state-evidence/truth/sqlite/repositories/event-repository.ts:233:  | (N-0631) src/platform/five-plane-state-evidence/truth/sqlite/repositories/event-repository.ts:233: `listAllEvents` 用 `LIMIT?OFFSET?` 在 `events ORDER BY created_at ASC` 上 projection rebuild 翻页, 无 `(created_at,id)` coverageindex → 全表扫 + filesort   | 已复核关闭 | Root cause:  A 表把 N-0631 已复核收口的低频 projection rebuild 性能 backlog duplicate展开; 它不影响事件order correctness.  |
| P-0169 | src/platform/five-plane-state-evidence/knowledge/semantic-vector-validation.ts:256 | src/platform/five-plane-state-evidence/knowledge/semantic-vector-validation.ts:256 `DELETE FROM ${schema}.${tableName} WHERE knowledge_ref = $1` 把 schema/table 名字模板字符串拼接, 再带 `$1` 参数; 如果 schema/tableName 来自外部输入则等同 SQL injection面 (须confirmation这两参为白名单)    | 已修复 | 明细cleanuppath继续手工拼接 schema/table, 未复用统一限定名与references规则.  |
| P-0170 | src/platform/five-plane-state-evidence/knowledge/knowledge-plane-service.ts:12-15 | `src/platform/five-plane-state-evidence/knowledge/knowledge-plane-service.ts:12-15` import `../../../domains/registry/domain-registry-service.js`, `domains/domain-baseline-catalog.js`, `domains/registry/plugin-spi.js`: 平面反向dependency domains.    | 已复核关闭 | Root cause:  knowledge plane 需要接内建 domains baseline 与 plugin SPI 做 retrieval merge, 这是 canonical integration seam, 不是 accidental reverse import.  |
| P-0171 | src/platform/five-plane-state-evidence/events/cas/sqlite-cas-repository.ts:144,201,213,237,294,306 | src/platform/five-plane-state-evidence/events/cas/sqlite-cas-repository.ts:144,201,213,237,294,306 BEGIN/COMMIT/ROLLBACK path 6 处 `exec()` call, 未concentrated到transaction装饰器; 如方法内中途抛错, 须人工保证 ROLLBACK 唯一path   | 已复核关闭 | Root cause:  A 表把“未抽象成transaction装饰器”的样式债误记成implementationerror; 当前 sqlite CAS repository 已在explicitly catch/rollback path中封装这些transaction.  |
| P-0172 | src/platform/five-plane-state-evidence/events/durable-event-bus.ts:710,766<br>src/platform/five-plane-execution/dispatcher/execution-dispatch-service.ts:1 (async-fire-forget | src/platform/five-plane-state-evidence/events/durable-event-bus.ts:710,766 与 src/platform/five-plane-execution/dispatcher/execution-dispatch-service.ts:1 (async-fire-forget 风格) , 关键消息泵 `void this.enqueueDelivery(...)` 不持有 promise, 丢弃failure信号无重试 hook   | 已修复 | 异步消息泵沿用 fire-and-forget 风格, 但missing最小failurerecord与可观测补偿点.  |
| P-0173 | src/platform/five-plane-state-evidence/events/cas/sqlite-cas-repository.ts:46,144,201,213,237,294,306 | src/platform/five-plane-state-evidence/events/cas/sqlite-cas-repository.ts:46,144,201,213,237,294,306 multiple places `BEGIN IMMEDIATE/COMMIT/ROLLBACK` 但 `exec()` call未 try/catch wrapped ROLLBACK (行 213/306 在 catch 内, 须confirmation逻辑path) , transaction边界 audit 弱   | 已复核关闭 | Root cause:  A 表把 transaction audit can read性问题directly上升为 correctness missing陷; 当前手工transaction边界仍保持explicitly rollback 兜底.  |
| P-0174 | src/platform/five-plane-state-evidence/events/cas/postgres-fencing-token-service.ts:122-128 | `clearAllFences` 在transaction内 `for (const fence of ...) { await repo.delete(...) }`, N 条 fence 触发 N 次 DELETE round-trip; 应改为 `DELETE WHERE node_id = $1` src/platform/five-plane-state-evidence/events/cas/postgres-fencing-token-service.ts:122-128   | 已修复 | cleanup fencing 逐条 `DELETE`, transaction内产生 N 次往返而不是按 owner batch删除.  |
| P-0175 | src/platform/five-plane-state-evidence/knowledge/semantic-vector-validation.ts:169 | src/platform/five-plane-state-evidence/knowledge/semantic-vector-validation.ts:169 `qualifiedTableName: \`${schema}.${tableName}\`` duplicateconstruction同一概念, 知识平面identityduplicate定义   | 已修复 | validation组件再次自定义限定表名, 和存储层没有shared单一表identity表示.  |
| P-0176 | src/platform/five-plane-state-evidence/knowledge/semantic-vector-store.ts:281 | src/platform/five-plane-state-evidence/knowledge/semantic-vector-store.ts:281 `qualifiedTableName()` return `${this.schema}.${this.tableName}`, construction时 line 136-137 用 `validateIdentifier()` — 但同file line 260 `${this.schema}.${this.tableName}` 同字串模板入 SQL, 多点字串duplicateconstruction, missing统一封装   | 已修复 | 同一张向量表的限定名在不同querybranchduplicate拼接, 封装边界不完整.  |
| P-0177 | src/platform/five-plane-state-evidence/events/durable-event-bus-support.ts, durable-event-bus.ts(2<br>src/platform/model-gateway/provider-registry/unified-chat-provider.ts, src/platform/stability/{stable-evidence-sequence.ts, reliability/circuit-breaker.ts, reliability/retry.ts, reliability/timeout.ts, timeout.ts, retry.ts} (多数沉睡型 | setTimeout 漏 `unref()`: src/platform/five-plane-state-evidence/events/durable-event-bus-support.ts, durable-event-bus.ts(2 处), src/platform/model-gateway/provider-registry/unified-chat-provider.ts, src/platform/stability/{stable-evidence-sequence.ts, reliability/circuit-breaker.ts, reliability/retry.ts, reliability/timeout.ts, timeout.ts, retry.ts} (多数沉睡型 timer 仍占住事件循环)    | 已复核关闭 | Root cause: 审查把短生命周期 sleep/retry timer 与长期 keep-alive timer 混算; 当前常驻path已逐步 `unref`, 剩余等待器跟随活动 Promise 生命周期.  |
| P-0178 | src/platform/five-plane-state-evidence/events/projections/workflow-timeline-projection.ts:235 | `JSON.parse` 无 try/catch: projection handle器directly解析 payloadJson (多file) src/platform/five-plane-state-evidence/events/projections/workflow-timeline-projection.ts:235; 同模式还有 risk-action-projection.ts:164, incident-projection.ts:168, approval-queue-projection.ts:143, worker-status-projection.ts:161, workflow-run-projection.ts:126, tool-usage-projection.ts:156, artifact-catalog-projection.ts:166, governance-projection.ts:177   | 已复核关闭 | 复核confirmation这些 projection 解析path已具备受控解析/兜底, 原问题为stalerecord.  |
| P-0179 | src/platform/model-gateway/provider-registry/{minimax,openai,anthropic}/*-chat-service.ts | src/platform/model-gateway/provider-registry/{minimax,openai,anthropic}/*-chat-service.ts 各 2 处 `while (true)` 流式 SSE 解析, missing统一 stop-token validation   | 已复核关闭 | Root cause:  SSE provider 的终止条件已经由各自 chunk parser 与 `AbortSignal` 收敛, A 表把“没有shared stop-token helper”误记成runtimemissing口.  |
| P-0180 | src/platform/model-gateway/provider-registry/unified-chat-provider.ts:678,681,684 | 自由text `throw new Error("ChatCompletionRequest requires traceId/tenantId/costTag")` (应uses AppError + code) src/platform/model-gateway/provider-registry/unified-chat-provider.ts:678,681,684   | 已修复 | Chat provider 的必填fieldvalidationdirectly抛bare Error, missing少 ValidationError code.  |
| P-0181 | src/platform/prompt-engine/eval/llm-eval-service.ts:630 | error吞噬: `} catch {` silently忽略 src/platform/prompt-engine/eval/llm-eval-service.ts:630   | 已复核关闭 | 当前 `parseCases()` 的 catch 已record `llm_eval_service.parse_cases_failed` 并fallback空array, 原“silently吞错”描述已不符合现状.  |
| P-0182 | src/platform/prompt-registry/index.ts:19-30<br>src/platform/prompt-engine/registry/* | `src/platform/prompt-registry/index.ts:19-30` 整体 re-export `src/platform/prompt-engine/registry/*`, 与 `prompt-engine/` duplicate定义同一概念.    | 已复核关闭 | 架构参考文档已把 `prompt-registry/` 标注为 Skeleton/namespace surface; 权威implementation仍在 `prompt-engine/registry/`, 这里保留的是naming空间entry.  |
| P-0183 | src/platform/agent-delegation/index.ts:1-71<br>src/platform/five-plane-orchestration/agent-delegation/ | `src/platform/agent-delegation/index.ts:1-71` 是 71 行壳子, 实际implementation位于 `src/platform/five-plane-orchestration/agent-delegation/`(`delegation-governance-service.ts` 等 10+ file).    | 已复核关闭 | 顶层 `platform/agent-delegation` 是 legacy namespace facade, canonical runtimeimplementation仍在 `five-plane-orchestration/agent-delegation`; 这不是duplicateimplementation.  |
| P-0184 | src/platform/cost-management/index.ts:21-37<br>src/scale-ecosystem/billing/cost-estimation-service.ts | `src/platform/cost-management/index.ts:21-37` only做 type re-export; 真正implementation在 `src/scale-ecosystem/billing/cost-estimation-service.ts`, namespace 名义exists但是空壳.    | 已复核关闭 | 当前 `cost-management/index.ts` 已导出 `CostEstimationService` 和 discoverability symbols, 不再是“only type re-export”的空壳.  |
| P-0185 | src/platform/stability/stable-backup-restore-rehearsal.ts:156 | SQL 字符串拼接: `SELECT COUNT(*) FROM ${table}` src/platform/stability/stable-backup-restore-rehearsal.ts:156   | 已修复 | 备份演练统计querydirectlyinsert表名, missing已references标识符封装.  |
| P-0186 | src/platform/stability/prompt-injection-guard.ts:186,293 | error吞噬: `} catch {` silently忽略 prompt-injection 检测异常 src/platform/stability/prompt-injection-guard.ts:186,293   | 已修复 | 检测链路把端点validation/解析failuredirectly吞掉, call方无法观察风控能力退化.  |
| P-0187 | src/platform/stability/prompt-injection-guard.ts:275 | SSRF 面: prompt-injection-guard `fetch(llmJudgeEndpoint)` 同上 src/platform/stability/prompt-injection-guard.ts:275   | 已修复 | 远端判定端点directly透传给 `fetch`, 此前没有地址约束.  |
| P-0188 | src/platform/stability/prompt-injection-guard.ts:168 | SSRF 面: prompt-injection-guard `fetch(mlModelEndpoint)` callconfigure端点无explicitly白名单 src/platform/stability/prompt-injection-guard.ts:168   | 已修复 | 机器学习判定端点未经过 URL validation即出网request.  |
| P-0189 | src/platform/ops-maturity/platform-panic/panic-propagation-service.ts:373 | setTimeout 未 cleanup: `setTimeout(() => {...})` panic 传播无handle保存 src/platform/ops-maturity/platform-panic/panic-propagation-service.ts:373   | 已复核关闭 | 复核confirmation该定时器已 `unref` 且不参与长期资源持有, 原告警与现状不符.  |
| P-0190 | src/platform/ops-maturity/<br>src/ops-maturity/ | `src/platform/ops-maturity/`(only `index.ts` + `platform-panic/`) 与顶层 `src/ops-maturity/`(20+ 子module) 双份exists同名概念.    | 已复核关闭 | 当前 `platform/ops-maturity` only承载 platform-panic compatibility/横切 surface, 顶层 `src/ops-maturity/` 才是 Layer 7 业务module; 属 canonical + compat 分层.  |
| P-0191 | src/platform/shared/cache/stores/redis-cache-store.ts:57 | error吞噬: `} catch {` silently忽略 redis cached读取error src/platform/shared/cache/stores/redis-cache-store.ts:57   | 已修复 | cached层为避免打断主流程silently吞错, 但没有最小 stderr/观测回报.  |
| P-0192 | src/platform/shared/outbox/outbox-poller-service.ts:92 | fire-and-forget Promise: `void this.poll();` src/platform/shared/outbox/outbox-poller-service.ts:92   | 已修复 | outbox 定时轮询 fire-and-forget 未附 catch 和failurelog.  |
| P-0193 | src/platform/shared/observability/structured-logger.ts:499 | fire-and-forget Promise: `void result.catch(() => {})` src/platform/shared/observability/structured-logger.ts:499   | 已修复 | log框架内部为避免影响主流程而silently吞掉异步 sink failure, missing少 stderr 兜底.  |
| P-0194 | src/platform/shared/async/sync-backed-async-service.ts:22 | fire-and-forget Promise: `rejected.catch(() => undefined)` 无log src/platform/shared/async/sync-backed-async-service.ts:22   | 已复核关闭 | catch 只used for标记内部消费, return的 rejected promise 仍向call方保留failure语义.  |
| P-0195 | src/platform/shared/cache/cache-facade.ts:76-112:  | (N-0752) src/platform/shared/cache/cache-facade.ts:76-112: `CacheFacade` 无 stale-while-revalidate path, cached失效时所有requestsynchronous await compute()   | 已复核关闭 | Root cause:  A 表把 N-0752 已复核收口的cachedstrategy扩展duplicate挂账; 当前 `CacheFacade` 选择强一致 `getOrCompute` 语义.  |
| P-0196 | src/platform/shared/cache/cache-invalidation-broadcast.ts:68 | fire-and-forget Promise: `void this.handleIncomingMessage(raw);` src/platform/shared/cache/cache-invalidation-broadcast.ts:68   | 已复核关闭 | handleIncomingMessage 内部已自行捕获并recordhandle异常, entry处不再duplicate catch.  |
| P-0197 | src/platform/shared/observability/structured-logger.ts:528 | fire-and-forget Promise: `this.checkRotationAsync(sink).catch(() => {})` src/platform/shared/observability/structured-logger.ts:528   | 已修复 | rotate check异步execute时只做空 catch, 轮转failure不会留下任何可见信号.  |
| P-0198 | src/platform/shared/lifecycle/service-registry-bootstrap.ts:26: module顶层 | (N-0753) src/platform/shared/lifecycle/service-registry-bootstrap.ts:26: module顶层 `ServiceRegistry.registerBootstrap` 注册核心服务, 但 `src/` 内无任何file import 该module → 注册逻辑从不execute (死代码)    | 已复核关闭 | Root cause:  A 表把 N-0753 已复核收口的explicitly bootstrap entry语义duplicate展开; `service-registry-bootstrap` 本就要求启动entryexplicitly import.  |
| P-0199 | src/platform/shared/observability/structured-logger.ts:542,550-551,560: log归档用 | (N-0758) src/platform/shared/observability/structured-logger.ts:542,550-551,560: log归档用 `${sink.filePath}.${index}` 模板拼接而非 `path.join`   | 已修复 | 归档filepathdirectly靠字符串模板拼接, missing少path工具统一handle.  |
| P-0200 | src/platform/shared/utils/redis-client-options.ts:116<br>docker-compose.yml | src/platform/shared/utils/redis-client-options.ts:116 default `host: config.host ?? "localhost"`, 启动期没人explicitly设置时会silently连本机 redis (容器内即failure) , 与 docker-compose.yml `AA_REDIS_HOST: redis` inconsistentfallback   | 已修复 | construction Redis standalone 客户端时对 host 做 localhost 隐式fallback, 容器和远端部署易连错目标.  |
| P-0201 | src/platform/shared/observability/slo-alerting-channels.ts:320 | PII 进log通道path: alerting EmailAlertChannel 以 email fielddirectly投递 (无sanitized标记)  src/platform/shared/observability/slo-alerting-channels.ts:320   | 已复核关闭 | 复核confirmation邮件通道保存的是投递事件而非把原始邮箱field写入log面.  |
| P-0202 | src/platform/shared/observability/slo-alerting-channels.ts:119,175,237,302 | fire-and-forget Promise: 四处 `void deliverWithRetry(...)` call无errorhandle src/platform/shared/observability/slo-alerting-channels.ts:119,175,237,302   | 已复核关闭 | deliverWithRetry 内部已负责重试和终态failurerecord, 外层 void call不是silently丢错.  |
| P-0203 | src/platform/shared/cache/cache-facade.ts:17,93-105:  | (N-0746) src/platform/shared/cache/cache-facade.ts:17,93-105: `getOrCompute` 单飞用process内 `pendingComputes: Map`, 多 Node 实例共用 L2/L3 时仍各自 compute (thundering herd 不被消解)    | 已复核关闭 | Root cause:  A 表把 N-0746 已复核收口的跨实例 single-flight 演进项duplicate挂账; 当前implementation只承诺process内保护.  |
| P-0204 | src/platform/shared/observability/structured-logger.ts:184,479,503,543,563,579,588,595,598 | error吞噬: 连续multiple places `} catch {` silently忽略 src/platform/shared/observability/structured-logger.ts:184,479,503,543,563,579,588,595,598   | 已修复 | log框架担心自我递归或影响主流程, 内部multiple placesdirectly吞错, missing最小failure回报.  |
| P-0205 | src/platform/shared/observability/slo-alerting-service.ts:325-360:  | (N-0801) src/platform/shared/observability/slo-alerting-service.ts:325-360: `acknowledgeAlert/resolveAlert` 在 UPDATE 后单独 `SELECT changes()`, 多连接模式下 `changes()` 可能反映其它语句   | 已复核关闭 | Root cause:  A 表把 N-0801 已复核收口的多连接假设duplicate展开; 现行 sqlite path仍是synchronous单连接封装.  |
| P-0206 | src/platform/shared/observability/metrics-server.ts:74-76 | src/platform/shared/observability/metrics-server.ts:74-76 via字符串比较 `address === "127.0.0.1" \|\| "::ffff:127.0.0.1"` 判断local, 未coverage `::1`/`localhost`/IPv4-mapped 变体之外的 link-local   | 已复核关闭 | 复核confirmationlocal地址判定已coverage `127.0.0.1`, `::1` 与 IPv4-mapped loopback, 原问题已过时.  |
| P-0207 | src/platform/shared/observability/runtime-metrics-registry.ts:108,128,141,143,149,165,185: 单位后缀混用 | (N-0809) src/platform/shared/observability/runtime-metrics-registry.ts:108,128,141,143,149,165,185: 单位后缀混用 `_ms`(ms) 与 `_seconds`(s) 共存于同一 registry, 下游 PromQL 公式难统一   | 已复核关闭 | Root cause:  A 表把 N-0809 已复核收口的指标naming治理 backlog duplicate挂账; 当前 exporter 已稳定导出, 不影响运行正确性.  |
| P-0208 | src/platform/shared/observability/runtime-metrics-registry.ts:115-117: 同名前缀 | (N-0723) src/platform/shared/observability/runtime-metrics-registry.ts:115-117: 同名前缀 metric 在不同call处可uses不同 label key 集合 (`{stage,result}` vs `{result}`) , 相邻 series cardinality 不同   | 已复核关闭 | Root cause:  A 表把 N-0723 已复核收口的指标前缀误读duplicate展开; 当前不同 label set 依靠完整 metric name distinguish.  |
| P-0209 | src/platform/shared/observability/health-service.ts:312-323:  | (N-0803) src/platform/shared/observability/health-service.ts:312-323: `checkDbWritableSync` directly对生产库 `CREATE TABLE IF NOT EXISTS __health_probe + INSERT/DELETE`, 每次健康探测都触发 WAL 写入与lock竞争   | 已复核关闭 | Root cause:  A 表把 N-0803 已复核收口的explicitly writable probe 成本duplicate展开; 这是受控健康check写path, 不是silently副作用.  |
| P-0210 | src/platform/shared/scaling/horizontal-scaling-controller.ts:14 | `src/platform/shared/scaling/horizontal-scaling-controller.ts:14` `import type { QueueStats } from "../../five-plane-execution/queue/queue-adapter-types.js"`: shared→execution 反向coupling.    | 已复核关闭 | 这里是 type-only contractreferences, 编译后不会形成 runtime 反向dependency; `horizontal-scaling-controller` 本身就是 execution queue 指标的shared算法 helper.  |
| P-0211 | src/platform/shared/cache/cache-invalidation.ts:68-74 | `invalidateTags` 中 `for (const tag of tags) { total += await this.cache.invalidateByTag(tag); }` serial失效; 多 tag 场景 RTT 累乘 src/platform/shared/cache/cache-invalidation.ts:68-74   | 已修复 | tag 失效沿用serial await 写法, 多标签场景把远端 RTT 线性叠加.  |
| P-0212 | src/platform/shared/observability/sli-collection-service.ts:147-156 | `initializeDefaultSlos` 在 `for` 循环里反复call `this.sloAlertingService.listSlos().find(...)`, O(N×M) 扫描; listSlos 还可能复制array src/platform/shared/observability/sli-collection-service.ts:147-156   | 已修复 | default SLO 初始化在循环内duplicate拉取并线性查找, missing一次性indexcached.  |
| P-0213 | src/platform/shared/events/local-typed-event-emitter.ts:3-12 | 自implementation EventEmitter `maxListeners=100` default, 超出directly `throw`; 多 service shared同 emitter 时 listener 累积容易触发reject; `removeAllListeners` 又被声明为 `protected`, 外部生命周期cleanuppath受限 src/platform/shared/events/local-typed-event-emitter.ts:3-12   | 已修复 | 自implementation emitter 继承了 Node 风格 listener upper limit思路, 却missingshared场景下的default无limit和外部cleanupinterface.  |
| P-0214 | src/platform/shared/observability/structured-logger.ts:554 | src/platform/shared/observability/structured-logger.ts:554 `setImmediate(() => void this.performRotation(sink))` 将log rotate 推到 microtask, 期间 sink 写入争用未synchronous; 且 performRotation 内任一异常因 `void` 不可见   | 已修复 | 轮转异步被丢进 `setImmediate`, 写入与轮转missingsynchronous且error再次被隐藏.  |
| P-0215 | src/platform/shared/cache/stores/memory-cache-store.ts:75-83 | `cloneStoredValue` 在每次cached get/set 都对值做 `structuredClone` 或 `JSON.parse(stableStringify())`, 对large object cache hit 也force O(value-size) 拷贝, 吞吐显著下降 src/platform/shared/cache/stores/memory-cache-store.ts:75-83   | 已复核关闭 | Root cause:  memory cache default采用 defensive clone 保证call方不可变隔离; A 表把隔离成本directly视为吞吐 bug.  |
| P-0216 | src/platform/shared/observability/slo-alerting-channels.ts:111-122:  | (N-0802) src/platform/shared/observability/slo-alerting-channels.ts:111-122: `WebhookAlertChannel` 用 `AbortSignal.timeout` 但无重试/熔断, 高concurrent下大量parallel fetch handle无背压   | 已复核关闭 | 复核confirmation `WebhookAlertChannel` 已统一走 `deliverWithRetry`, 原“无重试”判断失效.  |
| P-0217 | src/platform/shared/runtime-state-machine.ts<br>src/platform/five-plane-execution/runtime-state-machine.ts<br>src/platform/five-plane-execution/state-transition/state-transition-machine.ts | `src/platform/shared/runtime-state-machine.ts` 与 `src/platform/five-plane-execution/runtime-state-machine.ts`, `src/platform/five-plane-execution/state-transition/state-transition-machine.ts` 同主题三处定义.    | 已复核关闭 | `shared/runtime-state-machine` 已是下沉后的权威state机; `execution/runtime-state-machine` onlycompatibility re-export, `state-transition-machine` 则是通用有限statevalidation器, 职责并不duplicate.  |
| P-0218 | src/platform/shared/observability/inspect-service.ts:109, five-plane-execution/lease/execution-lease-service.ts:138: "execution | (N-0799) src/platform/shared/observability/inspect-service.ts:109, five-plane-execution/lease/execution-lease-service.ts:138: "execution not found" 在 ValidationError 与 StorageError 双path以不同 code throws, 告警/重试规则需同时识别两套   | 已复核关闭 | Root cause:  A 表把 N-0799 已复核收口的语义分层异常duplicate展开; interfacevalidation与存储读取failure本就不是同一error类别.  |
| P-0219 | src/platform/shared/observability/structured-logger.ts:184,479,503,543,563,579,588,595,598 | src/platform/shared/observability/structured-logger.ts:184,479,503,543,563,579,588,595,598 共 9 处 `} catch {}` silently吞错 (已在 P-0204 提及, 但全部位于"log框架"自身 -- 若logpath出错, 下游再也无法感知, 是关键观测面盲点)    | 已修复 | 观测面自身为避免自激而普遍空 catch, 导致log链路故障对call方完全不可见.  |
| P-0220 | src/platform/shared/outbox/outbox-service.ts:28-29 | `src/platform/shared/outbox/outbox-service.ts:28-29` import `../../five-plane-state-evidence/truth/authoritative-sql-database.js`, `five-plane-state-evidence/events/durable-event-bus.js`, shared→state-evidence 反向coupling.    | 已复核关闭 | `platform-module-catalog` 已把 outbox 列为 `shared` canonical subdomain; 它协调transaction性 outbox 与state证据平面contract, 本身就是跨平面shared基础设施, 不是 accidental reverse import.  |
| P-0221 | src/platform/shared/observability/runtime-metrics-registry.ts:1,128,130,141,143: default | (N-0810) src/platform/shared/observability/runtime-metrics-registry.ts:1,128,130,141,143: default bucket `[10,50,100,250,500,1000,5000]` (毫秒尺度) 被复used for `stage_duration_seconds`/`llm_ttfb_seconds`/`llm_total_seconds` → bucket 含义变成 10s..5000s   | 已复核关闭 | Root cause:  A 表把 N-0810 已复核收口的秒级 bucket 调优需求duplicate挂账; default bucket 仍是基线观测configure.  |
| P-0222 | src/platform/shared/observability/otel-tracer.ts:112-125,155-158: OTel | (N-0795) src/platform/shared/observability/otel-tracer.ts:112-125,155-158: OTel `startActiveSpan` 在 fallback 模式下创建的 span 对 `setStatus`/`recordException` 都是 noop, error不被任何后端record   | 已复核关闭 | Root cause:  OTel fallback span 的 noop 行为是 telemetry backend 不可用时的降级strategy, error仍由 StructuredLogger 和call侧链路record.  |
| P-0223 | src/platform/shared/observability/structured-logger.ts:523, context/runtime-context.ts:32-48:  | (N-0747) src/platform/shared/observability/structured-logger.ts:523, context/runtime-context.ts:32-48: `setImmediate(()=>this.performRotation(sink))` 已脱离 ALS provideContext 作用域, 内部 `getContext()` 抛 `runtime_context.missing`   | 已修复 | 轮转task脱离call上下文异步execute, ALS 上下文在log轮转中loss.  |
| P-0224 | src/platform/shared/observability/structured-logger.ts:401-423:  | (N-0808) src/platform/shared/observability/structured-logger.ts:401-423: `recentByTask/recentByTrace/recentByCorrelation` 先 filter 再 `slice(-limit)`, 无index; dashboard 高频拉取即放大攻击面   | 已修复 | query最近log时先复制全缓冲区再filter, 热path复杂度和分配成本偏高.  |
| P-0225 | src/platform/shared/events/local-typed-event-emitter.ts<br>src/platform/five-plane-state-evidence/events/ | `src/platform/shared/events/local-typed-event-emitter.ts`(单file) 与 `src/platform/five-plane-state-evidence/events/`(20+ file) 划分模糊; `shared/outbox/outbox-service.ts` 同时在 `five-plane-state-evidence/outbox/index.ts:1-14` 又定义了一份 stub type.    | 已复核关闭 | `LocalTypedEventEmitter` 负责process内 typed emitter, `five-plane-state-evidence/events/` 负责persistence/投递总线; `state-evidence/outbox/index.ts` 只提供消息 DTO, 不构成第二套 outbox implementation.  |
| P-0226 | src/platform/shared/outbox/outbox-service.ts:209-210,280-281,331-332:  | (N-0807) src/platform/shared/outbox/outbox-service.ts:209-210,280-281,331-332: `const newRetryCount = entry.retryCount+1; this.repo.markFailed(...)` 先读后写, concurrentfailurepath会loss重试计数 → DLQ 触发时机失真   | 已复核关闭 | Root cause:  A 表把 N-0807 已复核收口的跨process atomic retry 演进项duplicate挂账; 当前单 poller 内已不再concurrent重入.  |
| P-0227 | src/platform/shared/observability/structured-logger.ts:439-460,462-475: file | (N-0792) src/platform/shared/observability/structured-logger.ts:439-460,462-475: file sink 无每条 entry size cap, 也无 sampling/rate-limit; `maxBytes` only触发 rotation, 热path放入large object会瞬间撑爆 retention 触发频繁 rotate (rotate 是 setImmediate 异步, 期间继续 append 无背压)    | 已复核关闭 | Root cause: file sink 只是 repo-local fallback 观测通道, 不是主 ingestion backplane; entry cap 和 sampling 属观测优化 backlog.  |
| P-0228 | src/platform/shared/observability/structured-logger.ts:447,560:  | (N-0798) src/platform/shared/observability/structured-logger.ts:447,560: `appendFile` 与 `performRotation` 之间无synchronous, rotation 期间 appendFile 仍写 `sink.filePath`, rename 后到下次 setImmediate 之间的concurrent append 会写到刚被 rename 的旧 inode → 丢log   | 已复核关闭 | Root cause: 结构化filelog仍定位为 best-effort fallback sink; rotation 与 append 的极窄竞态属于localfile观测残余risk, 不影响权威事件链.  |
| P-0229 | src/platform/shared/cache/stores/redis-cache-store.ts:161-163,172-174 | `for (const tag of meta.tags) await this.extendIndexTtl(...)` 与 `for (const tagKey of keys) cleaned += await this.cleanupIndexSet(tagKey)` (line 260/270) serial RTT, 应batch pipeline 或 Promise.all src/platform/shared/cache/stores/redis-cache-store.ts:161-163,172-174   | 已修复 | tag TTL 延长和indexcleanup都按serial await execute, 远端cached RTT 被不必要累乘.  |
| P-0230 | src/platform/shared/stability/stable-evidence-bundle.ts:1 | `src/platform/shared/stability/stable-evidence-bundle.ts:1` 一行 `export * from "../../stability/stable-evidence-bundle.js"`: `platform/stability/`(20+ file) 与 `platform/shared/stability/`(20+ file) parallelduplicate主题.    | 已复核关闭 | TD-7 已完成把 `shared/stability` 物理迁移到 `platform/stability`, 旧 `shared/stability/*` only保留compatibility shim; 该条是迁移后的stale inventory.  |
| P-0231 | src/platform/shared/outbox/outbox-service.ts:112-128,318-347:  | (N-0806) src/platform/shared/outbox/outbox-service.ts:112-128,318-347: `getPendingEntries` only `SELECT … LIMIT`, 无 `FOR UPDATE SKIP LOCKED`/`claimed_at` 列; 多个 OutboxPollerService 实例concurrent会同时拉到同一批 → 跨processduplicate发布   | 已复核关闭 | Root cause:  A 表把 N-0806 已复核收口的分布式部署演进项duplicate展开; 当前单实例重入问题已收口, 跨实例 claim 需要 schema 与拓扑配合.  |
| P-0232 | src/platform/shared/observability/transports/datadog-transport.ts:53-56,66-102 | `setInterval(() => void this.flushInternal(), ...)` 无 in-flight reentry guard; 上一轮 retry/backoff 仍在跑时, 新 tick 再次 splice batch + concurrent HTTP, 背压逻辑 (重新 unshift) 会与parallel flush 竞态 src/platform/shared/observability/transports/datadog-transport.ts:53-56,66-102   | 已修复 | 定时 flush missing少 in-flight guard, 重试fallback期间可再次concurrent发送同一缓冲区.  |
| P-0233 | src/platform/shared/cache/cache-key-factory.ts:14-17, context/runtime-context.ts:28-29:  | (N-0745) src/platform/shared/cache/cache-key-factory.ts:14-17, context/runtime-context.ts:28-29: `CacheKeyFactory.create` only `${namespace}:${version}:${fingerprint}`, 无 tenant/workspace/principal 段, dependencycall方 namespace 约定承载tenant隔离 → 易出现跨tenantcachedpollute   | 已复核关闭 | Root cause:  A 表把 N-0745 已复核收口的 namespace 约定承载tenant隔离duplicate挂账; 当前contract并未承诺统一auto拼接 tenant 段.  |
| P-0234 | src/platform/shared/async/, src/platform/five-plane-execution/bulkhead/bulkhead-isolator.ts:  | (N-0751) src/platform/shared/async/, src/platform/five-plane-execution/bulkhead/bulkhead-isolator.ts: `shared` 不提供通用 Semaphore/Mutex/async-queue, entire repo只有 `BulkheadIsolator.queue` 一处异步 waiter; 其它需要互斥/信号量语义的代码 (lease handover, worker snapshot 写回, cache single-flight 等) 只能各自重造原语 → 跨子systemconcurrent原语标准missing (与 P-0135/P-0203 互证)    | 已复核关闭 | Root cause:  A 表把 N-0751 已复核收口的通用concurrent原语设计取舍duplicate挂账; 当前 `shared` 并未承诺提供统一 Semaphore/Mutex.  |
| P-0235 | src/platform/shared/lifecycle/service-registry.ts:90-92 | `getInstance() { return ServiceRegistry._instance ??= new ServiceRegistry(); }` 加 line 80-84 construction里synchronous遍历 `bootstrapRegistrars` 注册 -- 若任一 registrar 自身又调 `getInstance()` (重入) 会拿到尚未construction完毕的references; concurrent (微task) entry下首调可竞争创建多份 registry src/platform/shared/lifecycle/service-registry.ts:90-92   | 已修复 | Root cause:  `getInstance()` 首次construction时直到construction函数return后才写入 `_instance`, bootstrap registrar 重入会再次触发 new. 本轮已在construction期间提前种下 singleton, 并新增 reentry 回归testing.  |
| P-0236 | src/platform/shared/lifecycle/service-registry-bootstrap.ts:20-24 | `src/platform/shared/lifecycle/service-registry-bootstrap.ts:20-24` 反向导入 `../../five-plane-control-plane/iam/network-egress-audit.js`, `../../five-plane-execution/execution-engine/output-continuation-service.js`, `../../five-plane-orchestration/agent-delegation/delegation-audit-service.js`, shared 反dependency业务平面.    | 已复核关闭 | Root cause:  `service-registry-bootstrap` 本身就是explicitly bootstrap wiring module; 它导入各 plane 服务是为了注册singletongraph, 不代表 shared runtime 反向拥有业务逻辑.  |
| P-0237 | src/platform/index.ts:1-21 | src/platform/index.ts:1-21 同时uses `export * as foo` 与 `export *`, barrel naming空间不可由 `import { x } from` directly消费, IDE auto补全混乱   | 已复核关闭 | Root cause: 顶层 `platform` barrel 需要同时服务 namespace 消费和扁平compatibility导出; 混用 `export * as` 与 `export *` 是刻意的发布面设计, 不是implementationerror.  |
| P-0238 | src/platform/index.ts:1-21 | `src/platform/index.ts:1-21` 同时 `export * as` 与 `export *`(`./architecture/index.js:20`, `./ai-operations-runtime-catalog.js:14-16`), namespace barrel 与平铺 re-export 混用.    | 已复核关闭 | Root cause: 该entry承担“双发布面”职责: 对上保留 namespace 聚合, 对下compatibility平铺导出; 当前是有意compatibility设计, 不再单列为missing陷.  |
| P-0239 | src/platform/<br>.gitignore:1-65 | .gitignore:1-65 contains `# Legacy five-plane compatibility symlinks under src/platform/` 列出 5 条目录 (control-plane/execution/interface/orchestration/state-evidence) , 但 `find src -type l` 实际无任何 symlink, 规则成为死configure   | 已复核关闭 | Root cause: 忽略规则保留了历史compatibility产物的前瞻性兜底; 当前没有 symlink 只说明该compatibilitypath未被uses, 不构成runtime或构建error.  |
| P-0240 | src/platform/platform-mainline-bootstrap.ts<br>src/platform/five-plane-runtime-bootstrap.ts<br>src/platform/five-plane-runtime-orchestrator.ts<br>src/platform/ai-operations-runtime-orchestrator.ts<br>src/scale-ops-runtime-orchestrator.ts<br>src/interaction-governance-runtime-orchestrator.ts<br>src/domains-runtime-orchestrator.ts<br>src/platform-architecture-bootstrap.ts | 启动entryoverlap: `src/platform/platform-mainline-bootstrap.ts`, `src/platform/five-plane-runtime-bootstrap.ts`, `src/platform/five-plane-runtime-orchestrator.ts`, `src/platform/ai-operations-runtime-orchestrator.ts`, `src/scale-ops-runtime-orchestrator.ts`, `src/interaction-governance-runtime-orchestrator.ts`, `src/domains-runtime-orchestrator.ts`, `src/platform-architecture-bootstrap.ts` 各自构建 capability/startup 表, 逻辑同构.    | 已复核关闭 | Root cause: 最初missing少顶层导航文档; 当前 `src/apps/index.ts` 已定义 startup target/app manifest, `docs_zh/architecture/02-code-architecture-reference.md` 也明确列出各 root orchestrator 职责, 不再是“无说明overlap”.  |
| P-0241 | src/platform/<br>src/platform/index.ts:1-21 | `src/platform/` 同时承载 `five-plane-*` 五大平面与 `agent-delegation/ compliance/ contracts/ prompt-engine/ prompt-registry/ model-gateway/ stability/ architecture/ structure/ ops-maturity/ cost-management/ remote-coordination/` 等同级子system, 主题层级混乱: 见 `src/platform/`(目录列表) 与 `src/platform/index.ts:1-21` 同时把 `controlPlane`/`compliance`/`promptEngine`/`architecture` 平铺为顶级 namespace.    | 已复核关闭 | Root cause: 平台naming空间本来就承担“五平面 + 跨平面公共子system”的 umbrella 角色; 当前 AGENTS 与代码架构参考文档已经synchronous解释这一层级, 不再把它视为未解释混乱.  |
| P-0242 | src/sdk/client-sdk/api-client.ts:807 | error吞噬: `} catch {` silently忽略 src/sdk/client-sdk/api-client.ts:807   | 已修复 | SSE 事件订阅在 payload 解析failure时原本directly吃掉异常, 没有给call方任何诊断信号.  |
| P-0243 | src/sdk/client-sdk/api-client.ts:897 | error吞噬: `} catch {` silently忽略 src/sdk/client-sdk/api-client.ts:897   | 已复核关闭 | `parseCursor()` 的异常branch现在explicitly限定为解析类error并return `undefined`, 不再是bare吞错形态.  |
| P-0244 | src/sdk/cli/aa.ts:42 | src/sdk/cli/aa.ts:42 子命令path用 `${command}.${ext}` directly拼到 spawn → 若将来 CLI_ENTRYPOINTS 中含相对path或斜杠 (拼写error) 即变可injection命令path   | 已修复 | CLI 子命令名directly参与entrypath拼接, missing少字符集约束.  |
| P-0245 | src/sdk/client-sdk/api-client.ts:876 | `JSON.parse` 无 try/catch: 解析 base64 cursor src/sdk/client-sdk/api-client.ts:876   | 已修复 | pagination cursor 解析default信任 base64 content是合法 JSON, 对坏输入没有受控fallback.  |
| P-0246 | src/sdk/client-sdk/api-client.ts:339 | 重试无指数backoff: fixed延迟 `setTimeout(connect, 1000)` 重连 SSE src/sdk/client-sdk/api-client.ts:339   | 已修复 | SSE 重连fixed 1 秒重试, 网络异常时会形成过于刚性的重连压力.  |
| P-0247 | src/sdk/cli/api-server.ts:316 | fire-and-forget Promise 且 catch 写空: `.catch(() => undefined)` src/sdk/cli/api-server.ts:316   | 已修复 | CLI 启动cleanuppath把 cleanup 异常directly吞掉, 没有outputfailure信息.  |
| P-0248 | src/sdk/client-sdk/api-client.ts:311 | 自由text `throw new Error("SSE response body is not readable")` src/sdk/client-sdk/api-client.ts:311   | 已修复 | SSE 不can readbranchdirectly抛bare Error, missing少结构化validationerror.  |
| P-0249 | src/sdk/cli/login.ts:94 | src/sdk/cli/login.ts:94 `AA_CREDENTIALS_ENCRYPTION_KEY` 只validation `length===0`, 单字符弱口令会via; scrypt 救不了过短主密钥   | 已修复 | OAuth 凭据加密把“exists密钥”当成充分条件, 没有最小强度基线.  |
| P-0250 | src/sdk/cli/login.ts:217 | src/sdk/cli/login.ts:217 `AA_OAUTH_AUTH_CODE` via环境变量传入, shell 历史/process列表/容器审计中可见, authorization码leaked面增大   | 已修复 | 登录流程只支持从环境变量读取authorization码, 把一次性 code 暴露在环境面.  |
| P-0251 | src/sdk/client-sdk/api-client.ts:339 | setTimeout 未保存handle/不 cleanup: `setTimeout(connect, 1000)` 重连定时器无 clearTimeout src/sdk/client-sdk/api-client.ts:339   | 已修复 | SSE 重连 timer 没有保存handle, 订阅cancel后也无法主动cleanup.  |
| P-0252 | src/sdk/cli/aa.ts:30 | src/sdk/cli/aa.ts:30 `COMMAND_ALIASES` 查表后再 `as never` validation已知子命令, 但 alias 指向被删除的entry名时仍把missingfile名传给 spawn, error信息只来自 child exit   | 已修复 | alias 解析后仍可能把不exists的命令entry交给子processexecute, failure只能在 spawn 后暴露.  |
| P-0253 | src/sdk/cli/login.ts:64-69 | src/sdk/cli/login.ts:64-69 token 交换failure时未删除 statePath 中的 verifier, 重试或异常下 PKCE verifier 留在磁盘 → 若被读到可与已捕获 code replay   | 已修复 | OAuth finish 流程只在successpath考虑收尾, failure时会把 verifier statefile留在磁盘.  |
| P-0254 | src/sdk/cli/migrate-sqlite-to-pg.ts:206,229,236 | SQL 字符串拼接: `SELECT * FROM ${table}` 与 `INSERT INTO ${table} (${columns.join})` src/sdk/cli/migrate-sqlite-to-pg.ts:206,229,236   | 已复核关闭 | Root cause:  migrate-sqlite-to-pg 的 `table` 来源于内建迁移白名单, 不是user输入; 审查把受控标识符插值误记成 SQL injection.  |
| P-0255 | src/sdk/cli/aa.ts:51-58 | src/sdk/cli/aa.ts:51-58 父processonly `process.exitCode=code` 然后 resolve(), 未 process.exit; 子命令希望立即退出 (如交互式输入终止) 的场景父 process 仍跑完事件循环 → CLI 退出时机不确定   | 已修复 | CLI 父process把退出码留给事件循环自然结束, 退出时机受residualhandle影响.  |
| P-0256 | src/sdk/cli/login.ts:194 | src/sdk/cli/login.ts:194 `startOAuthLogin` allows从 env `AA_OAUTH_STATE` injection state, 攻击者控制 env 可forceuses可预测 state, 配合 P-0259 形成完整 CSRF 链   | 已修复 | OAuth state allows从外部环境injection, 破坏了 PKCE/CSRF 随机挑战的前提.  |
| P-0257 | src/sdk/cli/login.ts:71 | src/sdk/cli/login.ts:71 `exchangeCodeForTokens` directly `response.json() as Promise<TokenResponse>`, 未对 token field做最小validation (如 access_token 是字符串) , 异常response会pollute saveOAuthTokens   | 已修复 | token 交换response被directlyassertion成目标type, 没有做field级validation.  |
| P-0258 | src/sdk/cli/api-server.ts:286<br>deploy/helm | src/sdk/cli/api-server.ts:286 `metricsServer.listen(envConfig.metricsPort, envConfig.apiHost ?? "127.0.0.1", ...)` 把 apiHost 复用给 metrics, 与 deploy/helm values-prod.yaml `AA_API_HOST: "0.0.0.0"` default一旦合用会把 metrics 同时暴到公网   | 已修复 | Root cause:  API server 以前把 `AA_API_HOST` 复用为 metrics bind host, prod 一旦把 API 监听到 `0.0.0.0` 就会顺带放开 metrics. 本轮已split `AA_METRICS_HOST`, default仍回落 loopback.  |
| P-0259 | src/sdk/cli/login.ts:213-227 | src/sdk/cli/login.ts:213-227 `finishOAuthLogin` 全程未把回调return的 state 与 pending.state comparison, CSRF/state-mismatch 防护missing (OAuth PKCE 流程少了 state validation)    | 已修复 | finish 阶段只validation code/verifier, 没有把回调 state 与local pending state 做绑定验证.  |
| P-0260 | src/sdk/cli/secret-management.ts:18-19 | `src/sdk/cli/secret-management.ts:18-19`, `sdk/cli/ha-program.ts:24-26`, `sdk/cli/stable-campaign.ts:33-35` directly import `platform/five-plane-control-plane/iam/secret-management-service.js`, `iam/sandbox-policy.js`, `config-center/*-cli-env.js`, `platform/shared/stability/*`: SDK directlycoupling平面与 shared private.    | 已复核关闭 | Root cause: 把 `sdk/cli` operator binaries 当成independent public SDK; 这些 CLI 与 platform runtime 同仓交付, directlydependency canonical control-plane/shared module属预期.  |
| P-0261 | src/sdk/harness-sdk/index.ts:502,590,610,634,657 | src 中保留 `@ts-expect-error` 共 13 处 (src/sdk/harness-sdk/index.ts:502,590,610,634,657 等) bypass strict type   | 已复核关闭 | Root cause:  `@ts-expect-error` 汇总把少量 SDK compatibility桥接与type边界例外一并上升为system性 strict missing陷; 当前仍是受控技术债而非失控bypass.  |
| P-0262 | src/sdk/harness-sdk/index.ts:5-33 | `src/sdk/harness-sdk/index.ts:5-33` import `platform/stability/bulkhead-isolation.js` 与 `platform/five-plane-orchestration/harness/index.js`: SDK 跨进入 orchestration 平面与 stability 内部.    | 已复核关闭 | Root cause:  harness-sdk 本身就是 orchestration harness 的开发者封装层, A 表把其桥接dependencyduplicate挂成边界missing陷.  |
| P-0263 | src/sdk/harness-sdk/index.ts:33:  | (N-1261) src/sdk/harness-sdk/index.ts:33: `harness-sdk` directly import `platform/five-plane-orchestration/harness/index.js` (barrel) 以及 stability/contracts 内部, 把 orchestration plane 内部 API 升级为 SDK 公共contract, 破坏 SDK ↔ platform 单向导出关系 (与 §A module边界规则conflict)    | 已复核关闭 | Root cause:  A 表把 N-1261 对 harness SDK 边界的主审计结论再次展开; 该问题已在主表作为 SDK dependency方向治理项收口.  |
| P-0264 | src/sdk/pack-sdk/pack-manifest.ts:212 | error吞噬: `} catch {` silently忽略 src/sdk/pack-sdk/pack-manifest.ts:212   | 已复核关闭 | Root cause: 这里的 `verifyPackSignature()` 公共contract本来就以布尔return表示“signature无效”; catch only把坏 key/坏signature统一映射为 `false`, 不是silently吞掉call侧error.  |
| P-0265 | src/sdk/plugin-sdk/plugin-test-harness.ts:215 | fire-and-forget Promise: `execution.finally(...).catch(() => undefined)` src/sdk/plugin-sdk/plugin-test-harness.ts:215   | 已复核关闭 | Root cause: 这段 Promise 只used for在execute结束时cleanuptimeout timer; 原始 `execution` 的成败仍由 `Promise.race` 结果向call方explicitly暴露.  |
| P-0266 | src/sdk/plugin-sdk/plugin-test-harness.ts:228 | 自由text `throw new Error("Live mode requires a bound liveRunner or livePlugin runtime")` src/sdk/plugin-sdk/plugin-test-harness.ts:228   | 已修复 | live mode 前置validationdirectly抛bare Error, missing少 ValidationError code.  |
| P-0267 | src/domains/yono/index.ts:258 | src/domains/yono/index.ts:258 stance→probability directlyhardcoded 0.65/0.35/0.5, 所谓"概率"是常量映射而非任何模型output (伪造置信度)    | 已修复 | Root cause: 评论信号把立场directly映射成fixed概率常量, 完全丢掉 yes/no 词命中强弱. 本轮已改为按 yes/no 词差值推导概率, 并由 `tests/unit/domains/yono/yono-domain.test.ts` 补回归.  |
| P-0268 | src/domains/yono/index.ts:390 | src/domains/yono/index.ts:390 `YonoResolutionAssistAgent.draft` 在有/无证据时分别hardcoded confidence=0.72/0.42 (伪造置信度)    | 已修复 | Root cause:  resolution assist 把证据置信度写死成两档常量. 本轮已改为按证据数量与歧义risk计算 confidence, 并由 `tests/unit/domains/yono/yono-domain.test.ts` coverage.  |
| P-0269 | src/domains/yono/index.ts:307-309 | src/domains/yono/index.ts:307-309 `yonoConsensusProbability` 把 0.4/0.3/0.3 写死为市场/评论/预测三类源权重, 未configure化也未按data量dynamic调整   | 已修复 | Root cause: 共识概率把市场/评论/预测权重static写死. 本轮已改为按 signal/forecast 数量dynamic加权, 避免少量样本和大量样本走同一权重.  |
| P-0270 | src/domains/yono/index.ts:413 | src/domains/yono/index.ts:413 `createOrder` 未validation quantity 非负, limitPrice 非负, buy 端用负 quantity 等价于 sell 跨账户绕开侧别风控   | 已修复 | Root cause: 下单entrymissing少数量和限价的 fail-close validation. 本轮已reject非正 quantity 和负 limitPrice, 并由 `tests/unit/domains/yono/yono-domain.test.ts` 回归.  |
| P-0271 | src/domains/yono/index.ts:265 | src/domains/yono/index.ts:265 `evidenceBacked` only靠 `/https?:\|source\|evidence\|.../` 关键词regex判证据, 纯text "source" 一词即可冒充证据   | 已修复 | Root cause: 证据识别把宽泛关键词当成证据锚点. 本轮已收紧到 URL, `source:`, `evidence:`, official announcement 等标记, 并补 plain `source` 不算证据的回归.  |
| P-0272 | src/domains/yono/index.ts:266 | src/domains/yono/index.ts:266 `confidence` only产生 "high"/"medium", 永远不会output "low", 置信度等级残missing   | 已修复 | Root cause: 评论信号置信度分档少了一层低置信区间. 本轮已补 `low` branch, 并由 `tests/unit/domains/yono/yono-domain.test.ts` coverage弱信号场景.  |
| P-0273 | src/domains/yono/index.ts:188 | src/domains/yono/index.ts:188 `createMarket` 未validation `closeAt > openAt` 且 `resolutionDeadline > closeAt`, 可创建已结算或时间倒序的市场   | 已修复 | Root cause: 市场创建只accepts时间字符串, 没有validation时间order. 本轮已force `closeAt > openAt`, `resolutionDeadline > closeAt`, 并补非法时间回归.  |
| P-0274 | src/domains/yono/index.ts:526 | src/domains/yono/index.ts:526 `registerYonoDomain` 始终带 `skipSmokeTest:true` 注册, 预测市场域上线bypass smoke validation   | 已复核关闭 | Root cause: 评审把未uses的 legacy option 误判为真正上线开关; 当前 `DomainRegistryService.register()` 不消费 `skipSmokeTest`, 真实 smoke gate 在 promote/activate path.  |
| P-0275 | src/domains/quant-trading/index.ts:73 | src/domains/quant-trading/index.ts:73 `orderNotionalUsd` 比较未取绝对值, 负数 notional 即可bypass `maxOrderNotionalUsd`; 79, 82 行同样未validation输入非负   | 已修复 | Root cause:  pre-trade guard 既没validation非负输入, 也没对 notional/position 做绝对值比较. 本轮已补输入validation和绝对值limit, 并由 `tests/unit/domains/quant-trading/index.test.ts` 回归.  |
| P-0276 | src/domains/healthcare/index.ts:7 | src/domains/healthcare/index.ts:7 `HEALTHCARE_DOMAIN_PRESET` only把 summarize/coordinate 标为需复核, triage (分诊, 最高risk) 反而 bypass 复核 → 医疗域default安全姿态error   | 已修复 | Root cause: 医疗域 preset 把高risk triage 错放到无需复核集合. 本轮已把 triage 纳入default复核, 并补对应testing.  |
| P-0277 | src/domains/yono/index.ts:383 | src/domains/yono/index.ts:383 `proposedOutcomeId` 永远取 `market.outcomes[0]`, 无论user证据指向哪个 outcome, 决议建议本质上没有判定逻辑   | 已修复 | Root cause:  resolution assist 在证据exists时仍退化为default首 outcome. 本轮已优先从 evidence refs 匹配 outcome/type, 并补 `no` outcome 解析回归.  |
| P-0278 | src/domains/risk-profile/index.ts:75 | src/domains/risk-profile/index.ts:75 `score` 入参未做 NaN/Infinity validation, 传 NaN 时所有 `>=` 比较为 false, 落入 default return low   | 已修复 | Root cause: risk分级只比较区间, 没有先reject NaN/Infinity. 本轮已把非法分数走安全兜底, 并由 `tests/unit/domains/risk-profile/index.test.ts` coverage.  |
| P-0279 | src/domains/quant-trading/index.ts:37-42 | src/domains/quant-trading/index.ts:37-42 `DEFAULT_QUANT_TRADING_PRE_TRADE_LIMIT_POLICY` 把 100K USD 单笔/25K USD 日损/12% 回撤写死为"全行业default", 未按tenant/账户分层   | 已复核关闭 | Root cause: 评审把导出的 baseline 常量误判为forcetenantstrategy; 实际 `evaluateQuantTradingPreTradeRisk()` 始终消费call方传入的 `limitPolicy`, 该常量只是default样例/testing基线.  |
| P-0280 | src/domains/finance-accounting/index.ts:7 | src/domains/finance-accounting/index.ts:7 `FINANCE_ACCOUNTING_DOMAIN_PRESET` 把 reconcile (对账) 排除在复核之外, 但财务对账偏差是合规审计核心, default无人复核   | 已修复 | Root cause: 财务域 preset 把高risk reconcile 放到无需复核集合. 本轮已把 reconcile 纳入default复核, 并补testing.  |
| P-0281 | src/domains/yono/index.ts:175-187 | src/domains/yono/index.ts:175-187 `createMarket` 全部 risk field (manipulationRisk/regulatoryRisk 等) hardcoded为 low/medium, 所有市场上线都default medium 监管risk, 与 risk-profile 体系脱钩   | 已修复 | Root cause: 市场risk画像完全写死在创建path里. 本轮已改为按 category, tags, time-to-close 派生risk, 不再fixed回填.  |
| P-0282 | src/domains/registry/plugin-runtime-host.ts:363 | 命令construction: `spawn(spec.command, spec.args, ...)` directlyexecute外部 spec 指定的 command/args src/domains/registry/plugin-runtime-host.ts:363   | 已复核关闭 | Root cause: 评审只看到了 `spawn()` 这个execute点, 没有继续往上追 launch spec validation; 当前容器运行规范来自 `AA_PLUGIN_RUNTIME_CONTAINER_COMMAND_JSON` 的受控解析结果, 不是插件清单arbitrary透传命令.  |
| P-0283 | src/domains/risk-profile/index.ts:84 | src/domains/risk-profile/index.ts:84 `computeDomainRiskLevel` 在低分(<35)但 `profile.defaultRiskLevel === "high"` 时仍return "low", 把 high defaultriskerror降级 (only对 critical 做兜底)    | 已修复 | Root cause: defaultrisk级别只对 critical 做兜底, 高riskdefault档会被低分coverage. 本轮已保留 `defaultRiskLevel === "high"` 的安全下界, 并补testing.  |
| P-0284 | src/domains/yono/index.ts:336-338 | src/domains/yono/index.ts:336-338 `manipulationRisk` only按"全部 5 条立场相同"判定, neveroutput "medium", 且未结合时间窗/账号信誉/IP, 是空架子的操纵检测   | 已修复 | Root cause: 社交预测把操纵risk收敛成二元判断, 弱一致性信号无法落中档. 本轮已补 `medium` 分层, 至少把 3 条以上单向一致信号从低risk抬升.  |
| P-0285 | src/domains/legal/index.ts:11 | src/domains/legal/index.ts:11 `requiresAttorneyReview` 对所有 LegalTaskType 始终return true, 使函数失去判定意义且与第 14 行 `requiresLegalReview` 形成两套互斥的"是否需要律师审查"判断   | 已修复 | Root cause:  legal 域同时maintained了两套是否复核逻辑, `requiresAttorneyReview()` 脱离 canonical 规则. 本轮已让其委托 `requiresLegalReview()`, 并把 review/redline/advise 纳入default复核.  |
| P-0286 | src/domains/registry/plugin-runtime-child.ts:172-176 | src/domains/registry/plugin-runtime-child.ts:172-176 globally劫持 console.* 之后, line 197 `process.exitCode = 0`: 插件子processdefaultsuccess退出, 劫持后插件内任何runtime panic only落 structured log 不影响 exitCode → 父process难感知插件failure   | 已复核关闭 | Root cause: 评审把request级failure和process生命周期退出语义混在了一起; 当前 `process.exitCode = 0` 只在explicitly shutdown 消息下设置, request异常仍via runtime response 通道回传.  |
| P-0287 | src/domains/quant-trading/index.ts:51-57 | src/domains/quant-trading/index.ts:51-57 `taskType !== "trade"` directlyreturn `allowed=true` 且 `evaluatedGuards` 为空, simulate task可bypass所有风控阀 (实际可触达回测/影子撮合的真实订单链路)    | 已修复 | Root cause:  pre-trade risk 只把 `trade` 视为受控path, `simulate` 被error放行. 本轮已让 `simulate` 同样进入 guard validation, 并补testing.  |
| P-0288 | src/domains/yono/index.ts:255-258 | src/domains/yono/index.ts:255-258 `scoreWords` 用 `String.includes` 做关键词扫描, "yes" 命中 "yesterday/eyes", "no" 命中 "knot/note", 立场判定大量误判; 纯英文关键词在中文/西语市场失效   | 已修复 | Root cause: 评论信号用子串匹配关键词, 导致词边界误命中. 本轮已改为分词后判定命中, 至少收住 `yes/no` 的英文子串误判面.  |
| P-0289 | src/domains/academic-research/index.ts, finance-accounting, healthcare, legal | src/domains/academic-research/index.ts, finance-accounting, healthcare, legal 等十余个领域 index.ts only靠改 enum 字符串套出, 是同一份模板复制粘贴, missing少各行业实际差异化能力声明 (与 P-0262/P-0002 互证为"行业域规模化伪差异")    | 已复核关闭 | Root cause: 评审把shared preset 组合误判成伪差异implementation; 当前多领域module有意复用 `domain-module-helper.ts` 作为基线模板, 再via各域 task/review strategy做configure化差异.  |
| P-0290 | src/domains/registry/plugin-runtime-child.ts:172-176 | src/domains/registry/plugin-runtime-child.ts:172-176 globally劫持 `console.log/warn/error = ...`, 会pollute插件第三方库的诊断output (含 stack trace 行号loss)    | 已复核关闭 | Root cause: 评审把 stdio 重定向误判为诊断loss; 当前 console 劫持是插件子process向父process传 structured log 的既定桥接层, 不影响failure回传contract.  |
| P-0291 | src/domains/domain-risk-profile-service.ts:182-200 | 在 overrides for-of 内对每个 override 调 `rules.find(... matchesPattern)`, 且 `matchesPattern` 内部 `new RegExp` 即时编译 -- O(n²) 扫描叠加每次 RegExp 编译 src/domains/domain-risk-profile-service.ts:182-200   | 已修复 | override 匹配对每个规则duplicate扫描并即时编译regex, 热path复杂度偏高.  |
| P-0292 | src/domains/registry/plugin-runtime-child.ts:105-108 | src/domains/registry/plugin-runtime-child.ts:105-108 在 ESM 子process里 `require("node:http"/"node:https"/"node:net"/"node:tls")` 用 cast `as typeof import(...)`, bypass ESM staticdependencygraph, 不利于 sandbox/SBOM analysis   | 已复核关闭 | Root cause: 评审忽略了这里的目的不是业务dependency加载, 而是先经 `createRequire` injection补丁再 `syncBuiltinESMExports()`, 以把内建网络module的受控改写synchronous给 ESM 消费者.  |
| P-0293 | src/domains/yono/index.ts:524 | src/domains/yono/index.ts:524 `registry.get(...)!` 非空assertion紧跟 null check, existsconcurrent注册下的 TOCTOU 竞态   | 已修复 | Root cause: 注册path在 null check后仍保留非空assertion, 留下不必要的 TOCTOU 表达. 本轮已directlyreturn `register()` 的结果, 不再二次 `get(...)!`.  |
| P-0294 | src/domains/registry/domain-registry-service.ts:266-272 | `toolNames.filter((tool) => enabledTools.has(tool) \|\| domain.capabilities.requiredTools.includes(tool) \|\| domain.capabilities.optionalTools.includes(tool))`: 每个 tool 对 requiredTools/optionalTools 做线性 `includes`; 应预转 Set src/domains/registry/domain-registry-service.ts:266-272   | 已修复 | Root cause: 工具filter热path在循环内duplicate线性 `includes`. 本轮已把 required/optional tools 预转 `Set`, 降低duplicate扫描成本.  |
| P-0295 | src/domains/governance/hr-role-governance-service.ts:16-24 | `src/domains/governance/hr-role-governance-service.ts:16-24` import `platform/five-plane-control-plane/approval-center/approval-service.js` 与 `platform/five-plane-execution/tool-executor/tool-recommend-service.js`; `domains/governance/division-loader.ts:26-28` 进入 `iam/sandbox-policy`, `config-center/config-governance-service`: domains 直入五面private内部.    | 已复核关闭 | Root cause:  built-in domains/governance 服务本来就是 runtime modules, 不是外部扩展边界; A 表把领域层直连 canonical platform service 误判成privateout of bounds.  |
| P-0296 | src/domains/domain-baseline-catalog.ts<br>src/interaction/interaction-baseline-catalog.ts<br>src/ops-maturity/ops-maturity-baseline-catalog.ts<br>src/org-governance/governance-baseline-catalog.ts<br>src/scale-ecosystem/scale-baseline-catalog.ts | `*-baseline-catalog.ts` 在 5 个顶级目录duplicate (`src/domains/domain-baseline-catalog.ts`, `src/interaction/interaction-baseline-catalog.ts`, `src/ops-maturity/ops-maturity-baseline-catalog.ts`, `src/org-governance/governance-baseline-catalog.ts`, `src/scale-ecosystem/scale-baseline-catalog.ts`) , 同模式分散maintained.    | 已复核关闭 | 五个 baseline catalog 分别承载各顶层子域的default治理模板与种子规则; naming一致是模式复用, 不是同一implementation被复制粘贴.  |
| P-0297 | src/ops-maturity/drift-detection/evolution-integration-service.ts:180 | `Math.random()` used for演化record ID: src/ops-maturity/drift-detection/evolution-integration-service.ts:180   | 已修复 | 演化证据record ID directly内联 `Date.now()+Math.random()`, persistence标识熵不足且难统一加固.  |
| P-0298 | src/ops-maturity/drift-detection/drift-detector-service.ts:157 | hash 比较uses `===`: `current.hash === baseline.hash` drift检测 src/ops-maturity/drift-detection/drift-detector-service.ts:157   | 已修复 | Root cause: drift检测directly做字符串等值比较, 安全敏感path没有走常量时间比较. 本轮已改为 `timingSafeEqual` 辅助函数.  |
| P-0299 | src/ops-maturity/capacity-planner/forecaster/index.ts:1-13 | src/ops-maturity/capacity-planner/forecaster/index.ts:1-13 "Capacity Forecaster" only做 `usage*(1+rate%)` 指数外推, 无任何统计模型/置信区间/输入validation, namingmislead且 periods 巨大时触发巨量array分配   | 已修复 | Root cause:  capacity forecaster 没有输入边界validation, 坏参数可directly放大到array分配与异常预测结果. 本轮已补负数, 非有限值, 异常 growth rate 与超大 periods validation.  |
| P-0300 | src/ops-maturity/chaos/chaos-experiment-scheduler.ts:748 | setInterval 未 cleanup: `setInterval(...)` 创建后未在errorpath保存到 disposable src/ops-maturity/chaos/chaos-experiment-scheduler.ts:748   | 已修复 | continuous monitoring interval 未纳入完整生命周期manage, 长生命周期 timer 会持续占住事件循环.  |
| P-0301 | src/ops-maturity/workflow-debugger/health-monitor.ts:119 | src/ops-maturity/workflow-debugger/health-monitor.ts:119 用 ISO 字符串lexicographic order比较时间窗, 若任一探针 timestamp 带时区后缀 (如 +08:00) , 比较error, windowfilter会漏/误算   | 已修复 | 时间窗filterdirectly比较时间字符串而非归一化后的真实时间点, 混合时区偏移时会误判是否落窗.  |
| P-0302 | src/ops-maturity/drift-detection/cross-agent-analyzer/index.ts:402 | src/ops-maturity/drift-detection/cross-agent-analyzer/index.ts:402 `n<3` 时用 `z-score>1.25` 作显著阈值, 与 alpha=0.05 两条path标准inconsistent ("显著差异"成为玄学可调参数)    | 已修复 | Root cause: 小样本显著性和 bootstrap path用了两套inconsistent阈值. 本轮已把小样本branch收敛到确定性的偏差阈值逻辑.  |
| P-0303 | src/ops-maturity/chaos/chaos-experiment-scheduler.ts:471-489 | src/ops-maturity/chaos/chaos-experiment-scheduler.ts:471-489 `simulateRollbackAction` 是 placeholder (only setTimeout 100ms 后打log) , 生产环境 chaos 实验"rollback"不execute任何业务恢复   | 已修复 | Root cause:  chaos scheduler 把 rollback fixed成placeholderimplementation. 本轮已引入可插拔 `rollbackActionExecutor`, defaultpath也具备可中断execute语义.  |
| P-0304 | src/ops-maturity/drift-detection/evolution-integration-service.ts:180,218 | src/ops-maturity/drift-detection/evolution-integration-service.ts:180,218 同一file两次复制粘贴 `id: \`ev_${Date.now()}_${Math.random()...`` 内联 ID 生成, 未抽象 utility   | 已修复 | 证据 ID 生成逻辑被复制粘贴在多个branch里, 导致弱随机方案和修复strategy无法统一收口.  |
| P-0305 | src/ops-maturity/drift-detection/learning/evidence-store.ts:121 | 证据record读出/return前用 `JSON.parse(JSON.stringify(record))` 整体克隆, evidence record 可能很大; 高频call下成为 GC 压力点 src/ops-maturity/drift-detection/learning/evidence-store.ts:121   | 已修复 | evidence record return前仍uses JSON 深拷贝.  |
| P-0306 | src/ops-maturity/workflow-debugger/health-monitor.ts:121-136 | src/ops-maturity/workflow-debugger/health-monitor.ts:121-136 window内无任何探针时 `getSnapshot` return `status:"healthy", healthScore:100`, 组件长时间未上报反而显示完全健康 (应为 "unknown/stale")    | 已复核关闭 | 当前 health monitor 明确把空window定义为“无近期样本的中性基线”, 同时via `totalProbes=0`/`oldestProbeInWindow=null` 暴露无datastate; 属语义选择而非implementationerror.  |
| P-0307 | src/ops-maturity/drift-detection/cross-agent-analyzer/index.ts:416-417 | src/ops-maturity/drift-detection/cross-agent-analyzer/index.ts:416-417 bootstrap 分位用 `Math.floor(alpha/2*100)=2` 取下界, 2.5% 分位被定到第 2 个样本, 长尾下 `lower==bootstrapMeans[2]` system性偏高   | 已修复 | Root cause:  bootstrap 分位directly用integer桶下标truncated. 本轮已改为插值提取分位点, 消除system性偏高.  |
| P-0308 | src/ops-maturity/drift-detection/cross-agent-analyzer/index.ts:412 | src/ops-maturity/drift-detection/cross-agent-analyzer/index.ts:412 bootstrap 显著性检验directly用 `Math.random`, 结果不可复现, 对相同输入 `isSignificantlyDifferent` 多次call结论可能inconsistent (审计困难)    | 已修复 | Root cause:  bootstrap 抽样dependencyglobally `Math.random`. 本轮已改为fixed种子的确定性采样器, 保证同输入可复现.  |
| P-0309 | src/ops-maturity/chaos/chaos-experiment-scheduler.ts:431-442 | src/ops-maturity/chaos/chaos-experiment-scheduler.ts:431-442 `executeRollbackWithTimeout` 用 setTimeout 标timeout但无 AbortController, timeout后底层 actions 仍继续运行并最终触发第二次 `completeRollbackActions("completed")` 与timeoutbranch竞争   | 已修复 | Root cause:  rollback timeout只标state, 不会中断底层动作, 也没有单次完成保护. 本轮已引入 `AbortController`, 终态deduplication和 runId 防双完成.  |
| P-0310 | src/ops-maturity/chaos/chaos-experiment-scheduler.ts:748-782 | src/ops-maturity/chaos/chaos-experiment-scheduler.ts:748-782 `startContinuousMonitoring` 用 `setInterval+void async` wrapper, evaluator 慢于 intervalMs 时出现concurrent evaluator, 多次 `recordContinuousSteadyStateSample` 抢写同一时间槽   | 已修复 | Root cause:  continuous monitoring 用 `setInterval` 叠加异步 evaluator, 慢call会overlap. 本轮已改为链式 `setTimeout` 调度, 避免concurrent重入.  |
| P-0311 | src/org-governance/org-model/org-governance-saga.ts:290,327,364 | error吞噬: `} catch {` 在 SAGA 补偿处silently src/org-governance/org-model/org-governance-saga.ts:290,327,364   | 已修复 | Root cause:  SAGA 补偿阶段把 handler failure吞掉, 破坏 fail-close 语义. 本轮已在补偿后explicitlythrows `org_governance_saga.compensation_failed:*`.  |
| P-0312 | src/org-governance/approval-routing/approval-routing-service.ts:82-91 | src/org-governance/approval-routing/approval-routing-service.ts:82-91 `fxRatesToCny` default USD=7.2 写死, never刷新, 跨境审批金额折算长期usesstale汇率, 超阈值判定失真   | 已修复 | 审批路由把汇率表hardcoded在服务default值里, 没有explicitlyconfigureentry和快照来源record.  |
| P-0313 | src/org-governance/org-model/org-governance-saga.ts:290,327,364 (P-0311) 三处 | src/org-governance/org-model/org-governance-saga.ts:290,327,364 (P-0311) 三处 SAGA 补偿吞错, violates saga 必须explicitlyfailure的contract   | 已修复 | Root cause: 补偿failure只record而不外抛, 使 saga 看起来已恢复. 本轮已在execute剩余补偿后统一上抛failure, 保持 saga failure可观测.  |
| P-0314 | src/org-governance/delegated-governance/stores/index.ts:152-156,229 | `JSON.parse` 无 try/catch: directly解析 row field (多列) src/org-governance/delegated-governance/stores/index.ts:152-156,229   | 已修复 | Root cause:  delegated governance store directly `JSON.parse` persistence列, 坏data会炸出非受控异常. 本轮已补安全解析助手并returnnaming化error码.  |
| P-0315 | src/org-governance/sso-scim/api-key-service.ts:187 | src/org-governance/sso-scim/api-key-service.ts:187 `rotateApiKey` 把旧 key 标 `revoked` 但未写 `auditLog`, 密钥rotation是silently撤销   | 已修复 | rotationpath只改state, 不补撤销审计和 `revokedAt/revokedBy` 元data.  |
| P-0316 | src/org-governance/org-model/sync/index.ts:31 | 自由text `throw new Error("Cannot build OrgChart: no root node found")` src/org-governance/org-model/sync/index.ts:31   | 已修复 | OrgChart constructionmissing少根节点时directly抛bare Error.  |
| P-0317 | src/org-governance/org-model/org-governance-saga.ts:228,231,237 | error码风格inconsistent: `missing_prepare_handler` 等无naming空间前缀, 与同仓 `org_hierarchy.*`, `saml.*` 混用 src/org-governance/org-model/org-governance-saga.ts:228,231,237   | 已修复 | Root cause:  org governance saga 仍沿用bareerror码. 本轮已统一为 `org_governance_saga.missing_*_handler` naming空间.  |
| P-0318 | src/org-governance/sso-scim/oidc/oidc-service.ts:646 | SSRF 面: oidc-service `fetch(url, ...)` used for OIDC 元data/JWKS 拉取, 无 IP/主机limit src/org-governance/sso-scim/oidc/oidc-service.ts:646   | 已修复 | Root cause:  OIDC 出网request此前directly透传 provider URL, 没有经过统一 outbound URL validation. 本轮已接入 `parseSafeOutboundUrl()`, 对 loopback/内网地址 fail-close, 并补了定向回归.  |
| P-0319 | src/org-governance/sso-scim/scim-dlq-reconciliation.ts:16-30 | src/org-governance/sso-scim/scim-dlq-reconciliation.ts:16-30 "Reconcile" 只做了三次 filter+map 即return, 没有任何re-sort队/deduplication/上游对账逻辑 (函数名与功能严重不符)    | 已复核关闭 | Root cause: 评审按函数名预期了 replay/repair 语义; 当前 `ScimDlqReconciliationService` 的contract和现有testing都只定义分类, 汇总与报表, 不承担autoreplay.  |
| P-0320 | src/org-governance/sso-scim/api-key-service.ts:202-203 | src/org-governance/sso-scim/api-key-service.ts:202-203 `hashKey` 用未加盐 SHA-256, 对低熵/已leaks旧 key 可batchreplay彩虹表 (应uses HKDF 或带tenant盐的 keyed hash)    | 已修复 | Root cause:  API key indexdirectly用原始 SHA-256, missing少 keyed hash 和tenant绑定. 本轮已改为带 pepper, tenant 作用域的 HMAC-SHA256.  |
| P-0321 | src/org-governance/approval-routing/approval-routing-service.ts:104-141 | src/org-governance/approval-routing/approval-routing-service.ts:104-141 捕获 `empty_approver_chain` 后用 `fallbackApproverIds` (default空array) 兜底, 最终又在 148-149 行throws, catch branch无恢复能力, 徒增复杂度   | 已修复 | 空审批链场景先走“伪恢复”branch再在出口处抛错, failure语义没有在路由边界上directly收口.  |
| P-0322 | src/org-governance/sso-scim/api-key-service.ts:70 | src/org-governance/sso-scim/api-key-service.ts:70 `auditLog` onlyrecord revoke, missing少 generate/rotate/validate-fail/expire 等关键事件, 不满足合规审计   | 已修复 | API key 审计只coverage revoke, 一整条密钥生命周期没有完整事件链.  |
| P-0323 | src/org-governance/sso-scim/api-key-service.ts:130-133 | src/org-governance/sso-scim/api-key-service.ts:130-133 `validateApiKey` 副作用把expiry key 改成 `expired` 但未synchronous删除 `keyHashIndex`, 下次同 hash 输入仍命中 stale index并被反复"刚刚expiry"   | 已修复 | Root cause:  key expirystate和 hash indexcleanup分离, stale index 会反复命中expiryrecord. 本轮已在 expire/revoke/rotate pathsynchronouscleanupindex, 并补duplicate expire 审计回归.  |
| P-0324 | src/org-governance/compliance-engine/evidence-collector.ts:318,323 | setTimeout 未 cleanup: `setTimeout(runCollection, schedule.intervalMs)` 在errorpath未保存所有handle src/org-governance/compliance-engine/evidence-collector.ts:318,323   | 已修复 | 周期采集递归定时器missing少统一的生命周期manage, 长期运行会持续占住事件循环.  |
| P-0325 | src/org-governance/approval-routing/approval-routing-service.ts:194,213-215 | src/org-governance/approval-routing/approval-routing-service.ts:194,213-215 `planChain` conditional 模式把 `conditionalApproverIds` directly拼到 `approverChain` 末尾, 未validation这些user是否在组织节点permissionsrange内 → 可injection绕规审批人   | 已修复 | Root cause:  conditional approver 只做追加, 没有和 route/org scope 对齐validation. 本轮已limit到allows集合并对out of bounds approver fail-close.  |
| P-0326 | src/org-governance/sso-scim/api-key-service.ts:121 | src/org-governance/sso-scim/api-key-service.ts:121 当 `tenantId==null` 且 keyHash 在多tenant都有命中时directlyreject, 但missing乏告警/审计 → 攻击者可借此枚举密钥跨tenantexists性   | 已修复 | 多tenant歧义validation虽已 fail-close, 但failurebranch没有留下审计事件.  |
| P-0327 | src/org-governance/sso-scim/scim-sync/scim-service.ts:241 | PII 模式: scim-service 以 `${tenantId}:${email.toLowerCase()}` 作为index键cached于in-memory src/org-governance/sso-scim/scim-sync/scim-service.ts:241   | 已修复 | Root cause: in-memoryindex键directly拼tenant和邮箱, PII 以明文形式驻留. 本轮已改为摘要化 email index key.  |
| P-0328 | src/org-governance/sso-scim/api-key-service.ts:189-195 | src/org-governance/sso-scim/api-key-service.ts:189-195 `rotateApiKey` 调 `generateApiKey` 时丢掉了 `existing.tenantId`, 新生成的rotation密钥落到default `tenant:global` → 把密钥从原tenant搬走 (多tenant隔离破洞)    | 已修复 | rotationpath复用 `generateApiKey()` 时没有把旧record的 `tenantId` 继续传下去.  |
| P-0329 | src/org-governance/approval-routing/approval-routing-service.ts:236-243 | src/org-governance/approval-routing/approval-routing-service.ts:236-243 `buildDelegationMap` 只取 owners 一层委托, 未limit委托链最大深度, A→B→C→A 形成环时 `resolveDelegatedApprover` 行为dependency其内部implementation → 死循环或无限委托risk   | 已复核关闭 | Root cause: 评审把一跳 delegation map 误读成递归委托链; 当前 `buildDelegationMap()` 和 `resolveDelegatedApprover()` 都不做递归展开, 现implementation不exists环形死循环.  |
| P-0330 | src/org-governance/sso-scim/scim-sync/scim-service.ts:766,772,778,782,792,798,804,808,829,840,851,902,919,938,947,969 | error码literal量inconsistent: `bulk.user_not_found` / `bulk.group_not_found` / `bulk.invalid_path` / `bulk.unsupported_method` 与同file `scim.*` 前缀error码混用 src/org-governance/sso-scim/scim-sync/scim-service.ts:766,772,778,782,792,798,804,808,829,840,851,902,919,938,947,969   | 已修复 | Root cause:  bulk 子path沿用历史 `bulk.*` naming, 没有跟 `scim.*` 统一. 本轮已收敛到 `scim.bulk.*` 前缀.  |
| P-0331 | src/org-governance/compliance-engine/evidence-collector.ts:317, five-plane-orchestration/agent-delegation/delegation-audit-service.ts:25-28, five-plane-execution/tool-executor/shadow-snapshot-service.ts:410: EvidenceCollector | (N-1081) src/org-governance/compliance-engine/evidence-collector.ts:317, five-plane-orchestration/agent-delegation/delegation-audit-service.ts:25-28, five-plane-execution/tool-executor/shadow-snapshot-service.ts:410: EvidenceCollector / DelegationAudit / shadow-snapshot.persistRecord 全部 `writeFileSync` 整file, 无 tmp+rename 原子写 → crashed产生半写证据   | 已复核关闭 | Root cause: 原record把当前 correctness missing陷和subsequent durability augmentation混在一起; 证据采集定时器已加生命周期收口, 而原子写augmentation已在总审计 `N-0616/N-1081` 归并跟踪.  |
| P-0332 | src/scale-ecosystem/federation/federation-gateway.ts:581 | src/scale-ecosystem/federation/federation-gateway.ts:581 emit `federation-event` 含连字符, violates注册表事件naming风格   | 已修复 | 联邦网关事件名沿用局部naming习惯, 没有和globally注册表naming规则对齐.  |
| P-0333 | src/scale-ecosystem/integration/connector-runtime/index.ts:46 | error吞噬: `} catch {` silently忽略 src/scale-ecosystem/integration/connector-runtime/index.ts:46   | 已修复 | 回调投递path为不阻断主流程silently吞错, 但没有留下最小failure信号.  |
| P-0334 | src/scale-ecosystem/multi-region/data-replicator/index.ts:361 | `Math.random()` used for复制事件 ID: src/scale-ecosystem/multi-region/data-replicator/index.ts:361   | 已修复 | 复制事件 ID dependency时间戳拼接弱随机数, persistencepathmissing少稳定唯一标识.  |
| P-0335 | src/scale-ecosystem/runtime-services/human-takeover-service-async.ts:915 | `Math.random()` used for操作 ID: src/scale-ecosystem/runtime-services/human-takeover-service-async.ts:915   | 已修复 | runtime操作 ID 沿用 `Date.now()+Math.random()` 模式, 熵不足.  |
| P-0336 | src/scale-ecosystem/runtime-services/execution-dispatch-service-async.ts:622 | `Math.random()` used for操作 ID: src/scale-ecosystem/runtime-services/execution-dispatch-service-async.ts:622   | 已修复 | 调度操作 ID dependency弱随机拼接, 分布式追踪标识不稳.  |
| P-0337 | src/scale-ecosystem/integration/connector-framework-service.ts:452,471,485 | error吞噬: `} catch {` 三处连续silently src/scale-ecosystem/integration/connector-framework-service.ts:452,471,485   | 已修复 | connector 生命周期读取persistencefile时directly空 catch, 局部corrupted不会被观测.  |
| P-0338 | src/scale-ecosystem/runtime-services/execution-worker-writeback-service-async.ts:858 | `Math.random()` used for操作 ID: src/scale-ecosystem/runtime-services/execution-worker-writeback-service-async.ts:858   | 已修复 | writeback 操作 ID 仍uses弱随机方案, 难以保证唯一性.  |
| P-0339 | src/scale-ecosystem/operations/platform-operator-service.ts:145,521 | `JSON.parse` 无 try/catch: 解析 outputsJson src/scale-ecosystem/operations/platform-operator-service.ts:145,521   | 已修复 | operator output解析directly `JSON.parse`, corrupteddata会以非受控异常外溢.  |
| P-0340 | src/scale-ecosystem/multi-region/cdc-replication-service.ts:739 | `JSON.parse` 无 try/catch: 解析 cdc-replication 入站 payload src/scale-ecosystem/multi-region/cdc-replication-service.ts:739   | 已复核关闭 | 复核confirmation CDC 入站 payload 解析已有受控保护, 原record已stale.  |
| P-0341 | src/scale-ecosystem/tenant-platform/tenant-platform-service.ts:182-185 | src/scale-ecosystem/tenant-platform/tenant-platform-service.ts:182-185 进度百分比hardcoded `totalSteps=100` 估算, 对真实step数 10 或 1000 的工作流抢占决策严重失真   | 已复核关闭 | Root cause: 评审把候选sort启发式误判成最终配额/抢占判决; 这里的 `progressPercent` 只used for预sort, 真正的 quota/preemption 约束仍由调度与tenant治理服务execute.  |
| P-0342 | src/scale-ecosystem/tenant-platform/tenant-platform-service.ts:193 | src/scale-ecosystem/tenant-platform/tenant-platform-service.ts:193 priority 三段映射到 100/75/50, 未识别值default 25, "unknown" 与 "low" 同等handle掩盖configureerror   | 已复核关闭 | Root cause: 评审把启发式sort分数误判成configurevalidation边界; 该 priority 映射只服务候选sort, 未知值落最低优先级属于保守降级而不是silently放行.  |
| P-0343 | src/scale-ecosystem/multi-region/data-replicator/index.ts:240 | 弱算法可选: `computeChecksum(payload, algorithm: "sha256"\|"md5")` default参数虽为 sha256, 但 API 暴露 md5 src/scale-ecosystem/multi-region/data-replicator/index.ts:240   | 已修复 | Root cause:  checksum API 历史上为compatibility暴露了 md5 entry. 本轮已把implementation收敛到 sha256-only, 并对非 sha256 算法 fail-close.  |
| P-0344 | src/scale-ecosystem/integration/connector-runtime/index.ts:36 | SSRF 面: connector-runtime `fetch(callbackUrl, ...)` 由request方提供回调地址 src/scale-ecosystem/integration/connector-runtime/index.ts:36   | 已修复 | 回调 URL directly透传给 `fetch`, 既无地址约束也无failure可观测性.  |
| P-0345 | src/scale-ecosystem/intelligence/perception-service.ts:256,454,488,501 | `JSON.parse` 无 try/catch: 解析 perception-service 推荐动作 src/scale-ecosystem/intelligence/perception-service.ts:256,454,488,501   | 已修复 | brief/proposal/export 等解析流directly `JSON.parse`, 没有统一安全array解析.  |
| P-0346 | src/scale-ecosystem/runtime-services/durable-event-bus-async.ts:673 | fire-and-forget Promise: `void this.flushBatch();` 无 catch src/scale-ecosystem/runtime-services/durable-event-bus-async.ts:673   | 已修复 | 定时batch flush directly fire-and-forget, 异常无法回传也没有 stderr 提示.  |
| P-0347 | src/scale-ecosystem/multi-region/region-health-check-service.ts:435 | SSRF 面: region-health-check directly `fetch(${url}/health)`, url 来自configureno whitelist src/scale-ecosystem/multi-region/region-health-check-service.ts:435   | 已修复 | 区域健康check端点未经过 URL validation即发起探测request.  |
| P-0348 | src/scale-ecosystem/marketplace/pack-security-service.ts:668-686 | src/scale-ecosystem/marketplace/pack-security-service.ts:668-686 用 `/password\|secret\|token/i` 之类regex扫 `capturedLogs` 后置标记敏感data, 是 post-hoc 字符串匹配, 对真实泄露无防御作用   | 已复核关闭 | Root cause: 评审把log敏感词扫描误判成主防线; 当前 pack 安全主防线已收敛到源码/能力/runtime fail-close 守门, `capturedLogs` 扫描只是附加告警信号.  |
| P-0349 | src/scale-ecosystem/multi-region/data-replicator/index.ts:121 | 弱算法可选: data-replicator allows `checksumAlgorithm: "sha256" \| "md5"`, call方可指定 md5 进行integrityvalidation src/scale-ecosystem/multi-region/data-replicator/index.ts:121   | 已修复 | Root cause:  data-replicator configure面曾暴露 md5 选项, 弱摘要可directly进入integrity链路. 本轮已把configuretype收敛为 sha256-only.  |
| P-0350 | src/scale-ecosystem/billing/budget-reallocation-service.ts:73-104 | `idempotencyKeys = new Set<string>()` never淘汰, 长生命周期process会无限增长; 典型幂等cachedmissing TTL/逐出strategy src/scale-ecosystem/billing/budget-reallocation-service.ts:73-104   | 已修复 | Root cause:  budget reallocation 用never回收的 `Set` 保存幂等键. 本轮已改为 TTL 裁剪 `Map` 并在每次check时cleanupexpiry键.  |
| P-0351 | src/scale-ecosystem/multi-region/data-replicator/index.ts:413-417 | src/scale-ecosystem/multi-region/data-replicator/index.ts:413-417 `deliverEventWithRetries` success即 ack 但目标侧无幂等deduplication保障, 网络抖动下重试可能让目标 region 二次应用同一 event   | 已修复 | 入站复制事件missing少 `eventId` deduplication, 重试success后的duplicate到达会duplicate应用.  |
| P-0352 | src/scale-ecosystem/multi-region/region-health-check-service.ts:144:  | (N-1141) src/scale-ecosystem/multi-region/region-health-check-service.ts:144: `unregisterRegion` 删除 `circuitBreakers` map references却不call `cb.reset()`/释放timer → timerleaks   | 已修复 | region 注销只删除 map references, 没有主动reset circuit breaker 内部state.  |
| P-0353 | src/scale-ecosystem/federation/federation-gateway.ts:375,378,383,431 | 自由text `throw new Error("Source or target organization not found")` (trust 治理层error码missing) src/scale-ecosystem/federation/federation-gateway.ts:375,378,383,431   | 已修复 | federation trust 治理branchmissing少带 code 的结构化error.  |
| P-0354 | src/scale-ecosystem/runtime-services/durable-event-bus-async.ts:298 | 自由text `throw new Error("Circuit breaker is open - event publishing temporarily disabled")` src/scale-ecosystem/runtime-services/durable-event-bus-async.ts:298   | 已修复 | circuit breaker 打开时directly抛bare Error, missing少统一error分类.  |
| P-0355 | src/scale-ecosystem/multi-region/data-replicator/index.ts:478-483 | src/scale-ecosystem/multi-region/data-replicator/index.ts:478-483 `handleIncomingEvent` 收到事件后directly `handler(event)`, 从未call `validateEvent` validation checksum → 跨区复制信道integrity可bypass   | 已修复 | 入站复制handledirectlyexecute handler, bypass checksum validation.  |
| P-0356 | src/scale-ecosystem/integration/connector-framework-service.ts:452,471,485 | src/scale-ecosystem/integration/connector-framework-service.ts:452,471,485 三处连续 `} catch {}` 出现在 connector lifecycle 关键path, 已在 P-0337 提及; 未对 partial failure 做补偿/告警   | 已修复 | connector 恢复链路把 partial failure silently吞掉, missing最小告警与诊断output.  |
| P-0357 | src/scale-ecosystem/marketplace/pack-security-service.ts:553-555 | src/scale-ecosystem/marketplace/pack-security-service.ts:553-555 `sandboxApi.String` 把 split/substring/trim directly当函数挂出去 (非 prototype 方法) , call时 this loss会抛 TypeError, 正常 pack 也跑不动   | 已修复 | pack 安全服务试graph靠伪沙箱暴露宿主内建对象, 既unsafe也破坏合法 pack runtime语义.  |
| P-0358 | src/scale-ecosystem/federation/capability-delegation.ts:686 | 对每个 `def of capability.constraints` 调 `overrides.find((o) => o.type === def.type)`, 应改为 `Map<type, override>` 一次构建 src/scale-ecosystem/federation/capability-delegation.ts:686   | 已修复 | 约束coverage在循环里反复线性查找 override, 热点path复杂度不必要放大.  |
| P-0359 | src/scale-ecosystem/marketplace/marketplace-governance-service.ts:268,356,690-692 | `JSON.parse` 无 try/catch: marketplace-governance-service 多次解析 permissionsJson/compatibilityJson src/scale-ecosystem/marketplace/marketplace-governance-service.ts:268,356,690-692   | 已修复 | persistence JSON field读取沿用bare `JSON.parse`, corrupteddata会以非受控异常外溢.  |
| P-0360 | src/scale-ecosystem/multi-region/failover-reconciliation-job.ts:96,213 | `private readonly history: ReconciliationScanResult[] = []` 在每次 scan 后 `history.push(result)` 但无裁剪, 长期运行将 OOM src/scale-ecosystem/multi-region/failover-reconciliation-job.ts:96,213   | 已修复 | reconciliation scan 历史结果只增不减, 后台taskmissing少保留windowupper limit.  |
| P-0361 | src/scale-ecosystem/marketplace/pack-security-service.ts:597-598 | src/scale-ecosystem/marketplace/pack-security-service.ts:597-598 `__addNetworkAttempt/__addFileAccess` 是injection到 context 的回调, 但同时把 fetch/setTimeout 全置 undefined → 没有任何代码会call这些回调, "攻击检测"统计永远为 0   | 已修复 | 攻击统计dependency永远不会触发的伪沙箱 hook, 导致检测面与实际executepath脱节.  |
| P-0362 | src/scale-ecosystem/multi-region<br>deploy/terraform/environments/multi-region | src/scale-ecosystem/multi-region 与 deploy/terraform/environments/multi-region 多区域目标, 但 main.tf:11-19 backend "s3" key 单一 → 多 region terraform state 共池 (与 P-0588 形成"代码声称多区域, IaC 不可多区域"矛盾)    | 已修复 | 多区域 overlay 已exists, 但 backend key strategy未隔离, primary/secondary state容易共池.  |
| P-0363 | src/scale-ecosystem/marketplace/pack-security-service.ts:119,671 | src/scale-ecosystem/marketplace/pack-security-service.ts:119,671 自身用 regex `/child_process.*shell.*true/i`, `/process\|child_process\|exec/i` 检测 pack 代码 -- 这是 pre-runtime 启发式, 没用 AST, bypasspath多   | 已修复 | pack 安全判定原先建立在伪runtime沙箱和过窄启发式之上, 既不可靠也不能 fail-close.  |
| P-0364 | src/scale-ecosystem/multi-region/cdc-replication-service.ts:647-651 | `recordConflict` 中 `history.slice(-100)` only按 taskId 子表裁剪, `conflictHistory: Map<taskId, ...>` 自身无界; 持续生成新 taskId 会leaks src/scale-ecosystem/multi-region/cdc-replication-service.ts:647-651   | 已修复 | CDC conflict历史只裁剪子array, 外层 task map missing容量upper limit.  |
| P-0365 | src/scale-ecosystem/tenant-platform/tenant-platform-service.ts:131-151 | src/scale-ecosystem/tenant-platform/tenant-platform-service.ts:131-151 `evaluateSlo` only按 sloId globally查找, 命中第一个匹配 SLO 即 return → 两个tenant定义相同 sloId 会泄露另一tenantconfigure (tenant隔离破洞)    | 已修复 | SLO 评估按globally `sloId` 查找, 忽略了tenant作用域.  |
| P-0366 | src/scale-ecosystem/marketplace/pack-security-service.ts:531 | src/scale-ecosystem/marketplace/pack-security-service.ts:531 暴露 JSON.parse/stringify 后, malicious pack 用 toJSON 回调即可拿到外层闭包 → 沙箱白名单形同虚设   | 已修复 | 把宿主 JSON 回调暴露给 vm 上下文, 本质上把闭包能力带进了所谓沙箱.  |
| P-0367 | src/scale-ecosystem/runtime-services/execution-worker-writeback-service-async.ts:698,722 | setInterval handle释放path不完整: `this.batchFlushTimer = setInterval(...)` 在multiple places dispose path条件branchinconsistent src/scale-ecosystem/runtime-services/execution-worker-writeback-service-async.ts:698,722   | 已复核关闭 | 复核confirmation coalescing/batch 两个 interval 现已统一 `unref()`, 且 `dispose()` 会对两个handleexecute `clearInterval()`; 原释放pathinconsistentrecord已过时.  |
| P-0368 | src/scale-ecosystem/sla/index.ts:1<br>src/scale-ecosystem/sla-engine/ | `src/scale-ecosystem/sla/index.ts:1` only一行 `export * from "../sla-engine/index.js"`, 与同级 `src/scale-ecosystem/sla-engine/`(实际implementation) 形成单复数双目录.    | 已复核关闭 | `scale-ecosystem/sla/index.ts` 当前只是compatibility门面, 实际implementation权威entry仍在 `sla-engine/`; 属compatibility出口而非duplicateimplementation.  |
| P-0369 | src/scale-ecosystem/marketplace/pack-security-service.ts:518-524 | src/scale-ecosystem/marketplace/pack-security-service.ts:518-524 `sandboxApi.Date` 是普通对象不是 Date 类, pack 中 `new Date()` failure, 沙箱不能运行任何dependency时间的合法代码   | 已修复 | 伪沙箱把宿主内建对象改造成不compatibility的假implementation, 导致合法 pack 代码也无法运行.  |
| P-0370 | src/scale-ecosystem/multi-region/region-health-check-service.ts:183:  | (N-1171) src/scale-ecosystem/multi-region/region-health-check-service.ts:183: `checkRegion` 用 `circuitBreaker.execute(...)` wrapped `performHealthCheck`, 但 `latencyMs = Date.now()-startTime` 计算contains circuit breaker 等待时间 → 区域延迟指标低估   | 已修复 | 区域延迟指标error地复用了 breaker wrapper时长, 而不是健康探针自身测得的延迟.  |
| P-0371 | src/scale-ecosystem/multi-region/data-replicator/index.ts:415-444 | src/scale-ecosystem/multi-region/data-replicator/index.ts:415-444 `acknowledge` 后 `lastSequence++` only是本批计数, 写到 `checkpoint.sequenceNumber` 不是源端真实序号, 重启或concurrent flush 后 checkpoint 倒退   | 已修复 | checkpoint 序号uses本批success计数而非源事件真实序号.  |
| P-0372 | src/scale-ecosystem/multi-region/per-tenant-encryption.ts:54:  | (N-1111) src/scale-ecosystem/multi-region/per-tenant-encryption.ts:54: `PerTenantEncryptionService` 把 KEK/DEK directly放进process内 `Map<keyId, Buffer>`, 重启即丢, 无 KMS/HSM 后端, 与 `enforceHardwareSecurityModule` field语义directly矛盾   | 已复核关闭 | Root cause: 评审把基线in-memoryimplementation和外部 KMS/HSM 接线能力混为一项 defect; 该missing口已在总审计 `N-1111/N-1112` 归并为后端集成边界, 而不是当前仓内 correctness 回归.  |
| P-0373 | src/scale-ecosystem/marketplace/pack-security-service.ts:119,671 | src/scale-ecosystem/marketplace/pack-security-service.ts:119,671 static扫描 pack 用 regex `/child_process.*shell.*true/i`, `/process\|child_process\|exec/i` (P-0363) , 无 AST, 遇到 base64/加密字符串, indirectly访问 `globalThis["child_process"]` 都会bypass   | 已修复 | 安全服务过度dependency窄regex和伪沙箱, missing少 fail-close 的源码级risk判定.  |
| P-0374 | src/scale-ecosystem/runtime-services/execution-dispatch-service-async.ts:340-352 | `signal?.addEventListener("abort", () => {...})` 未带 `{ once: true }` 也无对应 `removeEventListener`, 每次 retry/timeoutcall均挂一个 listener; 当 signal 复用, 监听器unbounded growth + 触发 Node default maxListeners 警告 src/scale-ecosystem/runtime-services/execution-dispatch-service-async.ts:340-352   | 已修复 | abort 监听器按request叠加注册, 却没有在终态cleanup.  |
| P-0375 | src/scale-ecosystem/cost-attribution/index.ts:1-7 | `src/scale-ecosystem/cost-attribution/index.ts:1-7` 跨大区块反向 re-export `../../ops-maturity/cost-optimizer/cost-optimization-service.js`, scale-ecosystem 反dependency ops-maturity.    | 已复核关闭 | 当前 `cost-attribution/index.ts` 是对 canonical cost optimizer 的compatibility导出面, 属于naming空间桥接而非反向复制implementation.  |
| P-0376 | src/scale-ecosystem/operations/<br>src/scale-ecosystem/runtime-services/<br>src/scale-ecosystem/runtime-governance-service.ts<br>src/scale-ecosystem/scale-bootstrap.ts | `src/scale-ecosystem/operations/`(2 file) 与 `src/scale-ecosystem/runtime-services/`, `src/scale-ecosystem/runtime-governance-service.ts`, `src/scale-ecosystem/scale-bootstrap.ts` 之间ops/运行边界含糊, `platform-operator-service.ts` 又同名出现在 `marketplace/` 下.    | 已复核关闭 | 该条主要是目录 inventory 与naming观感问题; scale runtime/operations/bootstrap 边界已在架构文档登记, `marketplace/platform-operator-service.ts` 现也是compatibility re-export.  |
| P-0377 | src/scale-ecosystem/tenant-platform/tenant-platform-service.ts | src/scale-ecosystem/tenant-platform/tenant-platform-service.ts ~41 KB 单file + 同目录 `tenant-platform-service-async.ts` 双套 (P-0142, P-0386) , 合计 80 KB+ 同主题   | 已复核关闭 | 这是架构参考文档已登记的 sync/async 镜像与大filemaintained债, 属于已知 inventory 问题, 不再duplicate登记为新 defect.  |
| P-0378 | src/scale-ecosystem/tenant-platform/tenant-platform-service.ts:102-105 | src/scale-ecosystem/tenant-platform/tenant-platform-service.ts:102-105 via `getNoisyNeighborProtectionService()/getPerTenantEncryptionService()` 取globallysingleton, 但 `ResourcePoolService/FairSchedulingService` 是 new 出的instantiation对象 → 跨tenantshared与independent两套strategy并存, 隔离语义矛盾   | 已复核关闭 | 这里是Control Planegloballystrategysingleton与runtime资源调度实例的刻意分层, singleton承载shared治理规则, 实例承载本服务局部state, 并不directly构成tenant隔离破洞.  |
| P-0379 | src/scale-ecosystem/multi-region/data-replicator/index.ts:361,531 | src/scale-ecosystem/multi-region/data-replicator/index.ts:361,531 `eventId: \`repl_${Date.now()}_${Math.random().toString(36).slice(2)}``用`Math.random` 生成事件 ID, 跨 region 高concurrent下collision概率非可忽略; 且 eventId 进入persistencepath   | 已修复 | 复制事件 ID dependency时间戳拼接弱随机数, persistencepathmissing少稳定唯一标识.  |
| P-0380 | src/scale-ecosystem/feedback-loop/analyzer/index.ts:3, src/scale-ecosystem/feedback-loop/collector/signal-preprocessor.ts:5 | src/scale-ecosystem/feedback-loop/analyzer/index.ts:3, src/scale-ecosystem/feedback-loop/collector/signal-preprocessor.ts:5 等 `export interface` 块在only含 1-2 field下被声明在 index.ts, barrel 中混杂type与runtime导出   | 已复核关闭 | 这些file当前同时承担 tiny surface type 与 runtime export, 属于轻量门面风格选择, 不构成行为error或duplicateimplementation.  |
| P-0381 | src/scale-ecosystem/runtime-services/execution-worker-writeback-service-async.ts:721-725 | `startBatchFlushTimer` 同样 `setInterval(() => this.flushBatch())` 无 reentrancy guard 在 timer 层; dependency `flushBatch` 内部 `batchFlushPromise` serial化, 但 promise 期间仍会持续 enqueue + interval 不必要触发 src/scale-ecosystem/runtime-services/execution-worker-writeback-service-async.ts:721-725   | 已修复 | flush timer 未在entry层skip已有批handle, 空转 tick 会持续重入调度.  |
| P-0382 | src/scale-ecosystem/multi-region/data-replicator/index.ts:361 | src/scale-ecosystem/multi-region/data-replicator/index.ts:361 `eventId` 用 `Date.now()+Math.random()` 拼接, 毫秒级concurrent下duplicate概率非零; 同 ms 内两条事件落入相同 ID → outbox deduplicationerror丢事件   | 已修复 | `eventId` 由 `Date.now()+Math.random()` 生成, 毫秒级concurrent下duplicate概率非零.  |
| P-0383 | src/scale-ecosystem/runtime-services/<br>src/platform/five-plane-execution/* | `src/scale-ecosystem/runtime-services/` 整个目录(`durable-event-bus.ts:1`, `execution-dispatch-service.ts:1` 等) 只对 `src/platform/five-plane-execution/*`, `five-plane-state-evidence/*` 做 re-export, scale 层反向暴露平面内部.    | 已复核关闭 | `runtime-services/` 是 runtime-services 抽取后的compatibility导出层, 架构文档已把这类 shim record为迁移compatibility面, 而非duplicateimplementation.  |
| P-0384 | src/scale-ecosystem/runtime-services/{execution-dispatch-service-async,human-takeover-service-async,execution-worker-handshake-service-async,execution-worker-writeback-service-async}.ts | src/scale-ecosystem/runtime-services/{execution-dispatch-service-async,human-takeover-service-async,execution-worker-handshake-service-async,execution-worker-writeback-service-async}.ts 同模式 `op_${Date.now()}_${Math.random().toString(36).slice(2,11)}` 9 字符 base36 → only ~46 bits, 远below UUIDv4 122 bits, 分布式追踪可collision   | 已修复 | 多个 async runtime service 复用 `Date.now()+Math.random` 生成操作 ID, 熵不足.  |
| P-0385 | src/scale-ecosystem/marketplace/pack-security-service.ts:627 | src/scale-ecosystem/marketplace/pack-security-service.ts:627 `vm.runInContext` 在 Node 文档明确不是安全沙箱, 包malicious代码可用 `this.constructor.constructor("return process")()` 逃逸 → 整个 pack 安全服务是表演   | 已修复 | pack 安全服务error地把 `vm.runInContext` 当成安全边界.  |
| P-0386 | src/scale-ecosystem/billing/billing-service-async.ts:50, tenant-platform/tenant-platform-service-async.ts:40, tenant-platform/data-plane-flow-service-async.ts:41, ops-maturity/drift-detection/evolution-mvp-service-async.ts:40, scale-ecosystem/intelligence/perception-service-async.ts:41<br>package.json:5 | src/scale-ecosystem/billing/billing-service-async.ts:50, tenant-platform/tenant-platform-service-async.ts:40, tenant-platform/data-plane-flow-service-async.ts:41, ops-maturity/drift-detection/evolution-mvp-service-async.ts:40, scale-ecosystem/intelligence/perception-service-async.ts:41 全部以 `const { X } = require("./X.js")` 在 ESM file中混用 CommonJS require, violates package.json:5 `"type": "module"`, 靠 Node compatibility层运行   | 已复核关闭 | 复核confirmation这些 async wrapper 已改为 ESM import/SyncBackedAsyncService, 不再混用 CommonJS `require()`.  |
| P-0387 | src/scale-ecosystem/runtime-services/execution-worker-handshake-service-async.ts:653-658 | `setInterval(() => this.flushBatch(), ...)` 不等待 `flushBatch` (async) 完成, 无 in-flight 标志; 与 `durable-event-bus-async.ts:681-686` 同模式 (后者也是 async flushBatch) —可能concurrent splice 造成乱序/duplicatehandle (durable-event-bus 内部 626-636 行虽有 `batchProcessingPromise` 兜底, 但 handshake 服务无此guard) src/scale-ecosystem/runtime-services/execution-worker-handshake-service-async.ts:653-658   | 已修复 | handshake batch flush 定时器没有entry重入guard, 也missingfailurelog.  |
| P-0388 | src/scale-ecosystem/marketplace/ | `src/scale-ecosystem/marketplace/`(`billing-service.ts`, `tenant-platform-service.ts`, `execution-dispatch-service.ts`, `durable-event-bus.ts`, `data-plane-flow-service.ts`, `human-takeover-service.ts` …) 与 `scale-ecosystem/billing/`, `scale-ecosystem/tenant-platform/`, `scale-ecosystem/runtime-services/`, `platform/five-plane-execution/`, `platform/five-plane-state-evidence/events/` 主题大量duplicate同名服务.    | 已复核关闭 | `marketplace/` 的大partial同名file现已退化为compatibility shim/re-export; 相关split已在架构文档 TD-8 中record, 不再视为duplicateimplementationmissing陷.  |
| P-0389 | src/scale-ecosystem/runtime-services/{durable-event-bus-async,execution-worker-handshake-service-async,execution-worker-writeback-service-async}.ts, src/ops-maturity/chaos/chaos-experiment-scheduler.ts:748, src/interaction/dashboard/dashboard-websocket-server.ts:123, src/platform/five-plane-execution/ha/{leader-election-service.ts:589,626, wal-checkpoint-service.ts:607, stuck-run-sweeper-service.ts:490}, src/platform/five-plane-orchestration/harness/durable/sleep-scheduler.ts, src/platform/five-plane-control-plane/iam/secret-management-service.ts, config-center/config-hot-reload-service.ts, src/platform/five-plane-interface/{api/task-websocket-status-relay.ts,api/http-api-server.ts,channel-gateway/websocket-bridge.ts} | setInterval 漏 `unref()`: src/scale-ecosystem/runtime-services/{durable-event-bus-async,execution-worker-handshake-service-async,execution-worker-writeback-service-async}.ts, src/ops-maturity/chaos/chaos-experiment-scheduler.ts:748, src/interaction/dashboard/dashboard-websocket-server.ts:123, src/platform/five-plane-execution/ha/{leader-election-service.ts:589,626, wal-checkpoint-service.ts:607, stuck-run-sweeper-service.ts:490}, src/platform/five-plane-orchestration/harness/durable/sleep-scheduler.ts, src/platform/five-plane-control-plane/iam/secret-management-service.ts, config-center/config-hot-reload-service.ts, src/platform/five-plane-interface/{api/task-websocket-status-relay.ts,api/http-api-server.ts,channel-gateway/websocket-bridge.ts} 阻止process优雅退出   | 已修复 | 多个长期轮询/心跳 interval 没有统一 `unref()`, 空闲process无法优雅退出.  |
| P-0390 | src/interaction-governance-runtime-catalog.ts:32 | error吞噬: `} catch {` silently忽略 src/interaction-governance-runtime-catalog.ts:32   | 已修复 | Root cause: runtime目录的懒注册先用 `get()` 探测是否已exists, missing时只能靠空 catch fallback; 现已改为 `registry.has()` explicitly判定.  |
| P-0391 | src/interaction/dashboard/dashboard-websocket-server.ts:586 | error吞噬: `} catch {` silently忽略 src/interaction/dashboard/dashboard-websocket-server.ts:586   | 已修复 | Root cause:  projection poller 在降频fallback时只调整下一次轮询间隔, 没有留下failure信号; 现在fallbackbranch会record结构化告警log.  |
| P-0392 | src/interaction/goal-decomposer/index.ts:800:  | (N-0931) src/interaction/goal-decomposer/index.ts:800: `analyzeDependencyGraph` 在 `hasCycle` 时仍计算 longestDistance, 浪费且结果未用 (死代码 + 误用)    | 已修复 | dependencygraphanalysis在循环检测已failure后仍继续execute最长path计算.  |
| P-0393 | src/interaction/ux/workflow-builder-service.ts:109,693 | `JSON.parse` 无 try/catch: 解析 builderJson directlyreturn src/interaction/ux/workflow-builder-service.ts:109,693   | 已修复 | Root cause: persistence层把历史 `builderJson/outputsJson` 当作可信datadirectly反序列化; 现在解析failure或结构不合法都会被reject, 不再把坏data当正常工作流.  |
| P-0394 | src/interaction/autonomy/trust-scorer/index.ts:12 | src/interaction/autonomy/trust-scorer/index.ts:12 `volumeBonus` 只看 totalExecutions 不看success率, 500 次execute即便 50% failure仍获 +10 信任加分   | 已修复 | Root cause: 体量加分曾按execute次数directly发放; 现在体量加分按 successRate 缩放, 且有 incident 时directly归零.  |
| P-0395 | src/interaction/proactive-agent/event-watcher/index.ts:24-30 | `compilePattern` 每次call `new RegExp`, 未cached; 事件流 watch 高频触发 src/interaction/proactive-agent/event-watcher/index.ts:24-30   | 已修复 | event watcher 的模式匹配duplicate编译regex, 未cached.  |
| P-0396 | src/interaction/autonomy/trust-scorer/index.ts:11 | src/interaction/autonomy/trust-scorer/index.ts:11 `incidentPenalty=incidents*15` 不按 totalExecutions 归一化, 1/10 与 1/10000 的事故率被同等扣分   | 已修复 | Root cause: 事故惩罚以前按绝对次数扣分; 现在按 `incidents / totalExecutions` 归一化, 反映真实事故率.  |
| P-0397 | src/interaction/dashboard/dashboard-websocket-server.ts:562 | fire-and-forget Promise: `void this.runProjectionPoll();` 无 catch src/interaction/dashboard/dashboard-websocket-server.ts:562   | 已修复 | dashboard projection poll fire-and-forget 未recordfailure.  |
| P-0398 | src/interaction/nl-gateway/intent-parser/index.ts:220 | src/interaction/nl-gateway/intent-parser/index.ts:220 `parsed.intentType` 未via白名单/Zod validation即 cast as IntentType, 模型乱写一个字符串都会顺利下发到下游分发器   | 已修复 | Root cause:  LLM output曾directlyforce cast 为 `IntentType`; 现在先走白名单validation, 不合法 intent 会 fail-close.  |
| P-0399 | src/interaction/dashboard/dashboard-websocket-server.ts:142 | src/interaction/dashboard/dashboard-websocket-server.ts:142 `stop()` 清空 outboundMessages 但没有先把已排队消息 flush 到客户端, 关闭时未投递的事件directlyloss (无persistence兜底)    | 已修复 | Root cause:  shutdown/unregister directly清空in-memory队列; 现在只标记连接断开, 不再在 stop path提前丢弃已排队消息.  |
| P-0400 | src/interaction/nl-gateway/intent-parser/index.ts:39:  | (N-0901) src/interaction/nl-gateway/intent-parser/index.ts:39: `INTENT_CONFIDENCE_THRESHOLDS` 表hardcoded confirm/clarify/reject 阈值, 不支持按 tenant/risk 调整   | 已修复 | Root cause: 意graph阈值常量写死在解析器内部; 现在支持via override 传入tenant/场景级阈值.  |
| P-0401 | src/interaction/proactive-agent/trigger-engine/index.ts:1-14 | src/interaction/proactive-agent/trigger-engine/index.ts:1-14 `resolveTriggerActionMode` 只via boolean+enum 决策, 无tenant/历史failure率/时间窗信号; critical directly silent_record 是危险default (出问题无任何提示)    | 已修复 | critical 触发在missing少更多上下文时defaultsilentlyrecord, riskdefault值过于保守.  |
| P-0402 | src/interaction/nl-gateway/intent-parser/index.ts:216-218 | src/interaction/nl-gateway/intent-parser/index.ts:216-218 当 `parsed.intentType==="task_create"` 时把 confidence 强行抬到 ≥0.82, 是人为伪造置信度, 下游based on阈值的autoconfirmation会被欺骗   | 已修复 | Root cause: 历史上为“创建task”场景加了hardcoded置信度地板; 这段人为抬分逻辑已删除.  |
| P-0403 | src/interaction/dashboard/dashboard-websocket-server.ts:159-167 | src/interaction/dashboard/dashboard-websocket-server.ts:159-167 达到 maxClients 时return `clientId:""`, call方若不check仍可能在 outboundMessages map 以空字符串作 key 操作, 下游error传播   | 已修复 | Root cause: reject连接branchreturn了空字符串 sentinel; 现在改为explicitly `rejected:max_clients` clientId, 避免空 key pollute.  |
| P-0404 | src/interaction/ux/workflow-builder-service.ts:108-126 | src/interaction/ux/workflow-builder-service.ts:108-126 `loadWorkflow` 在 JSON.parse failure时silently走 fallback 把整个 outputsJson 原样塞回 builderJson, 等于把corrupteddata当合法工作流定义重新加载   | 已修复 | Root cause: corrupted存档曾走“宽容 fallback”重新装成 builder; 现在 invalid shape 会return `null`, 不再把坏 JSON masks成合法定义.  |
| P-0405 | src/interaction/ux/conversation-history-service.ts:309-316 | src/interaction/ux/conversation-history-service.ts:309-316 `deserializeSession` directly cast unknown 为 ConversationSessionRecord, 无 schema validation, 存储侧 JSON corrupted时下游拿到arbitrary结构对象   | 已修复 | Root cause: 会话反序列化把存储 JSON directly当成 typed record; 现在逐fieldvalidation turn/session 结构, 只accepts合法对象.  |
| P-0406 | src/interaction/nl-gateway/intent-parser/index.ts:212 | src/interaction/nl-gateway/intent-parser/index.ts:212 LLM 提示词把user原文 message directly拼到 `Locale: ... Message: <user>`, 未做模板隔离/白名单filter → 经典 prompt-injection injection面   | 已修复 | Root cause: user原文曾directlyinsertsystem提示模板; 现在 message 被包进 JSON envelope, 并明确标记为不可信usercontent.  |
| P-0407 | src/interaction/ux/conversation-history-service.ts:291 | src/interaction/ux/conversation-history-service.ts:291 `extractSessionId` 用 `(... as Record<string,unknown>).sessionId as string`, sessionId 实际为 number/object 时把非字符串硬塞回index层 → 混合type key   | 已修复 | Root cause:  sessionId 提取逻辑曾无条件强转; 现在只accepts真实字符串 sessionId.  |
| P-0408 | src/interaction/goal-decomposer/index.ts:246,250,255,261 | error码literal量inconsistent: `goal_decomposer.delegation_depth_exceeded` 与 `goal_decomposer.call_depth_exceeded`, `goal_decomposer.global_call_depth_exceeded` 同义但同位置三种不同literal量 src/interaction/goal-decomposer/index.ts:246,250,255,261   | 已修复 | 同一类深度超限error在不同branch手写了多套literal量.  |
| P-0409 | src/interaction/autonomy/trust-scorer/index.ts:27-31 | src/interaction/autonomy/trust-scorer/index.ts:27-31 `mapTrustLevelToAutonomyLevel` 把 `fully_trusted/trusted/semi_trusted` 三档全部映射到 `semi_auto`, "fully_trusted" 永远拿不到 `full_auto` 自治   | 已修复 | Root cause:  trust-level 到 autonomy-level 的映射表少了 fully_trusted 的independentbranch; 现在已映射到 `full_auto`.  |
| P-0410 | src/interaction/nl-gateway/*<br>config/runtime/default.json:16-19 | src/interaction/nl-gateway/* 全目录无 rateLimit/throttle implementation, 唯一相关 config/runtime/default.json:16-19 是平台级 120 req/min, 自然语言entrymissing乏tenant/会话维度independent限流, 易被滥用打爆 LLM 预算   | 已修复 | Root cause:  NL entry之前只继承平台级常数限流; 现在新增 tenant/user 维度configure与entryin-memory限流器, 超限会 fail-close.  |
| P-0411 | src/interaction/goal-decomposer/llm-plan-generator.ts:53,56,62,65,68,124,371,376 | error码literal量同义inconsistent: `goal_decomposer.invalid_llm_plan_*` 用 snake_case 而同module `goal_decomposer.llm_plan_response_too_large` / `goal_decomposer.llm_plan_json_too_deep` 风格不统一 src/interaction/goal-decomposer/llm-plan-generator.ts:53,56,62,65,68,124,371,376   | 已修复 | 同moduleerror码naming历史上混用了两套前缀风格.  |
| P-0412 | src/interaction/nl-gateway/nl-gateway-support.ts:183 | `patterns.some((pattern) => new RegExp(pattern.source, pattern.flags).test(normalized))` 在每次匹配 hot path 上重新克隆/编译 RegExp, 等同于 RegExp cached被废弃; line:319/431 同样模式duplicate src/interaction/nl-gateway/nl-gateway-support.ts:183   | 已修复 | nl-gateway multiple places匹配pathduplicate clone 和编译 RegExp, cached未复用.  |
| P-0413 | src/interaction/ux/workflow-builder-service.ts:14 | `src/interaction/ux/workflow-builder-service.ts:14`, `ux/conversation-history-service.ts:12`, `ux/ux-event-tracking-service.ts:11`, `autonomy/historical-metrics-provider.ts:30`, `dashboard/dashboard-projection-service.ts:12-14`, `dashboard/index.ts:3` directly import `platform/five-plane-state-evidence/{truth,memory,events,projections}` privatefilepath, bypass namespace.    | 已复核关闭 | Root cause: 审查把“包对外发布边界”和“仓库内部相对dependency”混为一谈; 这些交互module都在同一代码库内, directlydependency state-evidence 端口/仓储interface属于内部implementation边界, 不是发布面bypass.  |
| P-0414 | src/plugins/validators/basic-evaluator.ts:400-402,459-461 | src/plugins/validators/basic-evaluator.ts:400-402,459-461 basic-validator/basic-evaluator/scoring 三个插件 `healthCheck` directly `return true`, 永远绿灯, ops毫无信号   | 已修复 | Root cause:  evaluator 及相关插件把 healthCheck 写成常量真值; 现在按初始化/生命周期statereturn健康度, 不再永远绿灯.  |
| P-0415 | src/plugins/builtin-plugin-registry.ts:846 | 自由text `throw new Error("Authentication required to load marketplace plugin")` src/plugins/builtin-plugin-registry.ts:846   | 已修复 | marketplace 认证missingbranchdirectly抛bare Error, missing少鉴权error码.  |
| P-0416 | src/plugins/builtin-plugin-registry.ts:825 | 自由text `throw new Error("Marketplace API key is required")`, 未uses AppError + code src/plugins/builtin-plugin-registry.ts:825   | 已修复 | marketplace API key missing时directly抛bare Error.  |
| P-0417 | src/plugins/adapters/github-adapter.ts:222 | src/plugins/adapters/github-adapter.ts:222 未识别 action 时 default directly `{...params}` 回送整个参数对象, 可能把 token/credential field泄入 payload   | 已修复 | Root cause: 未知 action branch此前回显了原始参数对象; 现在改为抛 `github_adapter.unsupported_action:*`, 不再回传敏感 params.  |
| P-0418 | src/plugins/adapters/livestream-adapter.ts:45,49 | 自由text `throw new Error("OBS authentication token is required/format is invalid")` src/plugins/adapters/livestream-adapter.ts:45,49   | 已修复 | OBS token validationbranchdirectly抛bare Error, missing少 ValidationError code.  |
| P-0419 | src/plugins/adapters/github-adapter.ts:214 | src/plugins/adapters/github-adapter.ts:214 `dispatch_workflow` 的 inputs 参数only做 `typeof === object` check后透传, 未limit深度/field白名单 → 可injection未列入工作流声明的field   | 已修复 | Root cause:  workflow inputs 只做了对象形状check; 现在键名和 value type都经过浅层白名单/规范化validation.  |
| P-0420 | src/plugins/adapters/github-adapter.ts:119-121 | src/plugins/adapters/github-adapter.ts:119-121 `healthCheck` 只return `policy.evaluate(...).allowed`, 从不真正联通 GitHub API, 对外暴露的是"configure可达"而非"上游可用"   | 已修复 | Root cause: 旧 healthCheck 把“strategyallows”误当成“适配器健康”; 现在只有认证已完成, strategyallows且explicitly提供 probe 时才return健康.  |
| P-0421 | src/plugins/adapters/github-adapter.ts:102 | src/plugins/adapters/github-adapter.ts:102 `apiBaseUrl` 不validation http/https 协议, call方可传 `http://attacker.example`, HMAC 不防 MITM   | 已修复 | Root cause:  GitHub 适配器对 base URL 没有限定协议; 现在只accepts HTTPS.  |
| P-0422 | src/plugins/adapters/github-adapter.ts:187 | src/plugins/adapters/github-adapter.ts:187 `issueNumber` 只via requireString validation非空, 未做 `^\d+$` 数字validation, 传 `"123/comments/../../foo"` 会directly拼进 URL 形成pathinjection   | 已修复 | Root cause:  issueNumber 曾只validation非空字符串; 现在必须匹配纯数字.  |
| P-0423 | src/plugins/adapters/github-adapter.ts:127 | src/plugins/adapters/github-adapter.ts:127 `credentialFingerprint` 只取 token 前 4 字符 (GitHub PAT 都以 ghp_/gho_/ghs_ 开头) , 所有同类 token fingerprinting完全相同, 调试与审计无法distinguish密钥来源   | 已修复 | Root cause: 凭据fingerprinting只截取前缀; 现在改为稳定哈希摘要, 既不泄露原 token, 又能distinguish来源.  |
| P-0424 | src/plugins/validators/basic-evaluator.ts:81-85 | src/plugins/validators/basic-evaluator.ts:81-85 `DEFAULT_QUALITY_SCORING_CONFIG` 中 completeness+typeAccuracy=0.8, 加 suggestionPenalty=0.1 总和不为 1, 质量分语义不清且因正负权重相加产生 -∞~+∞ 区间   | 已修复 | Root cause: 正向权重和惩罚项原先混在同一线性求和里; 现在正向权重归一化, 建议惩罚改为有界乘法扣减.  |
| P-0425 | src/plugins/adapters/github-adapter.ts:152-154 | src/plugins/adapters/github-adapter.ts:152-154 `idempotencyKey` hash 把 raw params JSON 一起 sha256, params 内含 PII/secret 时仍参与运算, 且未规范化fieldorder使等价request产生不同 key   | 已修复 | Root cause: 幂等键曾directly哈希原始 params; 现在先做fieldsort和敏感值sanitized, 再生成 canonical key.  |
| P-0426 | src/plugins/retrievers/livestream-retriever.ts:39-41, asset-production-retriever.ts:39-41, growth-retriever.ts:36-38, presenters/coding-presenter.ts:16-18 | src/plugins/retrievers/livestream-retriever.ts:39-41, asset-production-retriever.ts:39-41, growth-retriever.ts:36-38, presenters/coding-presenter.ts:16-18 多个 retriever/presenter 插件 `healthCheck` 写死 `return true`, 平台健康面板永远正常 → 故障无法被及早发现   | 已修复 | Root cause: 多个内建插件沿用模板式 `return true`; 现在统一按 initialize/shutdown 生命周期暴露真实健康state.  |
| P-0427 | src/plugins/adapters/{asset-production,crm,game-dev,github,livestream}-adapter.ts | `src/plugins/adapters/{asset-production,crm,game-dev,github,livestream}-adapter.ts` multiple places (`:11-12`) `import { NetworkEgressPolicyService } from "../../platform/five-plane-control-plane/iam/network-egress-policy.js"`; `plugins/retrievers/coding-retriever.ts:6` 直入 `five-plane-execution/tool-executor/semantic-repo-map-service.js`: plugins out of bounds访问平面private.    | 已复核关闭 | Root cause: 审查把“内建插件”按第三方 pack SDK handle了; 这些 adapters/retrievers 属于仓库内平台能力的一partial, directlydependency egress policy 与语义index服务是设计内联, 不是外部插件out of bounds.  |
| P-0428 | src/testing/index.ts(21<br>src/benchmarks/index.ts(74<br>package.json | src/testing/index.ts(21 行), src/benchmarks/index.ts(74 行) 几乎空壳 (P-0429) , 但 package.json `exports` 未导出, call方必须深 reach `./platform/...`, 分层语义不闭合   | 已修复 | Root cause:  public exports missing了 testing/benchmarks 子path; 现在 `package.json` 已explicitly导出这两个entry.  |
| P-0429 | src/testing/index.ts<br>src/benchmarks/index.ts<br>src/platform/structure/index.ts<br>src/platform/remote-coordination/index.ts<br>src/platform/ops-maturity/index.ts | placeholder/死目录: `src/testing/index.ts`(21 行), `src/benchmarks/index.ts`(74 行), `src/platform/structure/index.ts`(独file), `src/platform/remote-coordination/index.ts`(2 行), `src/platform/ops-maturity/index.ts`(only `platform-panic` 子项) — 实质未承载子module.    | 已复核关闭 | Root cause: 评审based on早期placeholder快照; 当前 testing/benchmarks 已有对外entry, structure/remote-coordination/ops-maturity 也各自承担结构validation, session 导出和 panic surface, 不再是“死目录”.  |
| P-0430 | src/index.ts:136 | error吞噬: `} catch {` silently忽略 src/index.ts:136   | 已修复 | Root cause:  demo summary 对taskoutput的 JSON 解析failure会directlysilentlyfallback; 现在会output结构化告警log再return原始字符串.  |
| P-0431 | src/index.ts:136, src/interaction-governance-runtime-catalog.ts:32, src/platform/prompt-engine/eval/llm-eval-service.ts:630 | src/index.ts:136, src/interaction-governance-runtime-catalog.ts:32, src/platform/prompt-engine/eval/llm-eval-service.ts:630 等多个 `} catch {}` (已在 §B 列出) 共构成 60+ 处silently吞错节点, 没有任何一处 emit 到 structured-logger 或 metrics counter — 形成观测黑洞   | 已修复 | Root cause: 聚合审查把“missing探测/宽容解析”和“silently吞错”混在了一起; 本轮已把 cited 节点分别改成explicitlyexists性check或结构化logfallback, 消除了这些代表性黑洞样本.  |
| P-0432 | src/index.ts:11-22 | `src/index.ts:11-22` 顶层 barrel directly reach `./platform/five-plane-control-plane/config-center/startup-env-schema.js`, `./platform/five-plane-execution/execution-engine/single-task-execution.js`, 跨级deeplyprivatefile.    | 已修复 | Root cause:  root entrydirectly抓取了深层privatemodule; 现在改为经 `config-center/index.ts` 和 `execution-engine/index.ts` 公共entry导入.  |
| P-0433 | src/index.ts:11-22<br>package.json:14-46 | src/index.ts:11-22 顶层 barrel reach `./platform/five-plane-control-plane/config-center/startup-env-schema.js` 与 `./platform/five-plane-execution/execution-engine/single-task-execution.js`, 与 package.json:14-46 `exports` 表声明的 subpath 边界existsconflict (barrel 暴露内部, violates export 子path白名单意graph)    | 已修复 | Root cause:  root barrel 一度越过了public subpath; 现在收敛到公共 index surface, 与 exports 边界重新对齐.  |
| P-0434 | src/core/runtime/<br>MEMORY.md | MEMORY.md 提及 `src/core/runtime/` re-export compatibility层 (P-0436) , 但 src 顶层无 `core/` (验证: `ls src/` 只看到 platform/domains/sdk/... 不含 core)    | 已复核关闭 | 评审based onexpiry目录快照; 当前仓库已exists `src/core/runtime/` compatibility层.  |
| P-0435 | src/apps/, src/core/<br>src/apps/index.js"<br>src/apps<br>package.json:16 | src/apps/, src/core/ 不exists, 但 package.json:16 `"./apps": "./dist/src/apps/index.js"` explicitly export — 一旦消费方 `import "automatic-agent-platform/apps"` 即解析failure (需验证 dist 是否生成 src/apps, 否则发布即破窗)    | 已复核关闭 | Root cause: 原审查based onexpiry目录快照; 当前 `src/apps/` 与 `src/core/runtime/` 已exists, `./apps` export 对应真实entry.  |
| P-0436 | src/core/runtime/<br>src/platform, src/domains, …) 相矛盾<br>MEMORY.md<br>AGENTS.md | MEMORY.md 提及 `src/core/runtime/` only作为 re-export compatibility层, 但 src 顶层并无 `core/` 目录, 与 AGENTS.md 描述 (src/platform, src/domains, …) 相矛盾   | 已复核关闭 | 评审based onexpiry目录快照; 当前仓库已exists `src/core/runtime/` compatibility层.  |
| P-0437 | src/{domains,interaction,ops-maturity,org-governance,scale-ecosystem}/ | `architecture-remediation.ts` 在 `src/{domains,interaction,ops-maturity,org-governance,scale-ecosystem}/` 五处independentexists, 主题分散duplicate.    | 已复核关闭 | Root cause: 同名 remediation file过去missing少可见 inventory; 现在代码库存审计和 architecture reference 已explicitly列出五个作用域, 不再是未解释duplicate.  |
| P-0438 | src/platform-application-kernel.ts, src/domains-runtime-orchestrator.ts, src/interaction-governance-runtime-orchestrator.ts, src/scale-ops-runtime-orchestrator.ts, src/platform-architecture-bootstrap.ts<br>src/sdk/cli/aa.ts | src/platform-application-kernel.ts, src/domains-runtime-orchestrator.ts, src/interaction-governance-runtime-orchestrator.ts, src/scale-ops-runtime-orchestrator.ts, src/platform-architecture-bootstrap.ts 5 份顶级 orchestrator file并存, 无 README 说明各自适用场景与启动entry选择决策树 (与 P-0240 互证, 进一步: bin "aa" entry src/sdk/cli/aa.ts 才是唯一外部entry, 5 份 orchestrator only供内部 import, missing顶层导航)    | 已复核关闭 | Root cause: 当时missing少concentrated导航; 当前 `docs_zh/architecture/02-code-architecture-reference.md` 与 `src/apps/index.ts` 已明确说明 root orchestrator/entrypoint 的职责和选择方式.  |
| P-0439 | src/{domains,interaction,ops-maturity,org-governance,scale-ecosystem}/architecture-remediation.ts | src/{domains,interaction,ops-maturity,org-governance,scale-ecosystem}/architecture-remediation.ts 五处independentfile总 631 行 (domains 110, interaction 100, ops-maturity 186, org-governance 124, scale-ecosystem 111) , P-0437 已记, 本轮发现: 每份file都duplicate `classifyPromptInjectionRisk` 等 import + 同构 type 定义, 统一maintained几乎不可能   | 已复核关闭 | Root cause: 这批 remediation file本来就按领域保留local封装和导出形状; 现在库存审计已把它们纳入explicitly治理, 原“统一maintained几乎不可能但无人跟踪”的前提已不成立.  |
| P-0440 | config/environments/dev.json:7-8 | config/environments/dev.json:7-8 以 `secret://system/registry/ghcr/dev` 形式将 secret references名称写进版本化default值   | 已复核关闭 | Root cause: 审查把 secret locator 当成 secret material; 当前版本化的是 `secret://...` references名而非密钥值, 本身不构成leaks.  |
| P-0441 | config/environments/{dev,staging,pre-prod,prod,test}.json:3 | config/environments/{dev,staging,pre-prod,prod,test}.json:3 全部hardcoded个人/示例naming空间 `ghcr.io/automatic-agent`   | 已复核关闭 | Root cause: 原问题把 `ghcr.io/automatic-agent` 误判成个人/示例naming空间; 当前它是项目级default镜像仓库前缀, 不再按“个人placeholder符”记missing陷.  |
| P-0442 | config/security/default.json:7 | config/security/default.json:7 `allowedCapabilities` defaultcontains "edit", 加上 `sandboxMode:"read_only"` 是矛盾 (编辑能力本质上是写) ; 远程 worker 注册default值难以审计   | 已修复 | default只读沙箱仍暴露 `edit` 能力, configure边界与permissions模型inconsistent.  |
| P-0443 | config/providers/default.json:2 | config/providers/default.json:2 globallydefault provider 写死 "minimax", profile "reasoning-medium", missing少 schema/可选项validation → 删除该 provider 时所有callsilentlyfailure   | 已复核关闭 | Root cause: 旧快照未把 provider/profile registry validation算进去; 当前 `ConfigGovernanceService` 与 `startup-preflight` 都会validation default provider/profile 是否exists且匹配, 删除后会 fail-close.  |
| P-0444 | config/runtime/default.json:9 | config/runtime/default.json:9 `maxAgentRounds=32` + `maxToolCalls=64`, 对长链 LLM task过宽, 无配套预算/timeout熔断 → 单task能消耗大量 LLM 预算   | 已修复 | default资源upper limit按演示场景放宽, 未收敛到生产基线.  |
| P-0445 | config/runtime/default.json:18 | config/runtime/default.json:18 `rateLimit.requestsPerMinute=120` 是平台级常数, 未按 tenant/principal split, 多tenantshared一个 120 RPM 极易互相抢占   | 已修复 | runtime速率limitconfigure只提供平台级单桶, missing少 tenant/principal 维度split.  |
| P-0446 | config/nl-gateway/default.json:28-29 | config/nl-gateway/default.json:28-29 `additionalPromptInjectionPatterns/additionalGenericAmbiguousPatterns` default空array, 平台default完全不防 prompt injection (与 P-0406 形成完整链)    | 已修复 | NL 网关 guardrail default值没有内建 prompt injection / 模糊意graph模式.  |
| P-0447 | config/risk/default.json:57-62 | config/risk/default.json:57-62 `blastRadiusValues` skip 4 directly从 tenant=3 跳到 platform=5, 与 impact/sensitivity 等其它矩阵inconsistent, 权重计算时 4 这一档永远missing位   | 已修复 | blastRadius 枚举值missing少 `organization=4`, risk矩阵分档断层.  |
| P-0448 | config/environments/{dev,staging,pre-prod,prod,test}.json | config/environments/{dev,staging,pre-prod,prod,test}.json 五份configure统一hardcoded `ghcr.io/automatic-agent` (P-0441) , 切tenant/private registry 需要全文 5 处替换; missing统一变量化层   | 已修复 | 环境configuremissing少shareddefault层, 镜像 registry default值在多份 env file里duplicatehardcoded.  |
| P-0449 | config/risk/default.json:81-88 | config/risk/default.json:81-88 "low" risk级 `autoExecute:true + requiresApproval:false` 是唯一无人审批的级别, 但 risk 评分映射规则在 §74 含权重相加可能让大量边界场景落到 low → default所有级别应至少 `evidenceLevel:"basic" + requiresApproval`   | 已修复 | 低risk动作defaultautoexecute且无需审批, 治理基线过松.  |
| P-0450 | config/nl-gateway/default.json:14-20 | config/nl-gateway/default.json:14-20 `disambiguation.threshold=0.8` 但 `intent.minConfidenceForAutoConfirm=0.85`, 与 intent-parser 强行抬高 0.82 (P-0402) 互相conflict, 最终autoconfirmation门槛实际为 0.82   | 已修复 | disambiguation 与 auto-confirm 阈值分别maintained, defaultconfiguredrift.  |
| P-0451 | divisions/legal/division.yaml:30 | divisions/legal/division.yaml:30 legal 同样 `riskLevel:critical + lifecycleState:canary`, 关键法律审查能力default非生产级   | 已修复 | 法律域沿用了 canary 生命周期default值, 未按 critical 场景提升.  |
| P-0452 | divisions/academic-research/division.yaml:62 | divisions/academic-research/division.yaml:62 `budget_limit_per_task:5` (无单位) field值在不同 division 取值差异巨大, 但无 schema limit和单位说明 → configuredrift   | 已修复 | division 资源边界里 `budget_limit_per_task` 只有数字没有单位contract, schema 也未force约束.  |
| P-0453 | divisions/qa/schemas/qa-smoke-output.json<br>divisions/quality-assurance/schemas/qa-output.json | divisions/qa/schemas/minimal-output.json 与 divisions/quality-assurance/schemas/qa-output.json 同义不同名, 注册表二选一不明   | 已修复 | Root cause:  `qa` 目录长期被当成 `quality-assurance` 的同义缩写, 没有缩窄自己的 schema 语义. 本轮把 `qa` 改为 smoke alias, schema independent为 `qa-smoke-output.json`, 并在 `docs_zh/reference/division-catalog.md` / `config/quality/division-catalog.json` 固化家族边界.  |
| P-0454 | divisions/academic-research/division.yaml:37 | divisions/academic-research/division.yaml:37 `humanAccountable:false` 与 `advisoryOnly:false` 同时为假, 意为"auto决策且无人负责" (治理禁区组合)    | 已修复 | academic-research risk画像未声明 accountable owner, 形成无人负责组合.  |
| P-0455 | divisions/healthcare/division.yaml:30 | divisions/healthcare/division.yaml:30 `riskLevel:critical + regulatoryClass:heavily_regulated` 但 `lifecycleState:canary`, 重监管医疗领域以"金丝雀"作为default上线state → 治理姿态error   | 已修复 | 医疗域沿用了 canary 生命周期default值, 未按 heavily_regulated 场景提升.  |
| P-0456 | divisions/advertising/division.yaml:34-39 | divisions/advertising/division.yaml:34-39 advertising `riskLevel:high` 但 `regulatoryClass:unregulated`; 广告在 EU/中/北美都属合规高敏 (GDPR/广告法/FTC) , 分类error会bypass监管check   | 已修复 | 广告域监管分类过低, 未反映广告合规约束.  |
| P-0457 | divisions/*/division.yaml | divisions/*/division.yaml 所有 division 都把 `default_workflow` 写死为 `<id>_primary`, 但 workflows/ 目录下若filemissing/rename, 无引导加载validation → 启动时只有runtime报错 (无 lint 阶段)    | 已修复 | division default_workflow missing少staticexists性validation, 且 general_ops default值已drift.  |
| P-0458 | divisions/qa/workflows/qa_smoke_validation.yaml<br>divisions/quality-assurance/workflows/quality_assurance_primary.yaml | divisions/qa/workflows/qa_single_step.yaml 与 divisions/quality-assurance/workflows/quality_assurance_primary.yaml 互不references, existsidentityoverlaprisk   | 已修复 | Root cause: 两个 workflow 都被理解成“QA 主流程”, 但没有 canonical/alias 分层. 本轮把 `qa` workflow 明确rename为 `qa_smoke_validation`, 限定为轻量 smoke validation; 完整回归和发布认证继续由 `quality_assurance_primary` 承担.  |
| P-0459 | divisions/qa<br>divisions/quality-assurance<br>divisions/qa/roles/qa_engineer.prompt.md<br>divisions/quality-assurance/roles/qa_engineer.prompt.md | divisions/qa 与 divisions/quality-assurance duplicate定义; roles 子目录inconsistent (divisions/qa/roles/qa 目录 vs divisions/quality-assurance/roles/qa_engineer.prompt.md)    | 已修复 | Root cause:  legacy `qa` 目录沿用早期嵌套角色布局, 和 canonical `quality-assurance` 的扁平角色path长期并存. 本轮删除 `divisions/qa/roles/qa/` 嵌套目录, 改为扁平角色file, 并在 division 描述中声明 `qa` only是 smoke alias.  |
| P-0460 | scripts/backup-sqlite.sh:95-96 | scripts/backup-sqlite.sh:95-96 `aws s3 cp` 未加 `--sse`/`--sse-kms-key-id`, 备份对象在没有 S3 default桶加密时上云就是明文落盘   | 已修复 | S3 远程备份上传未force服务端加密.  |
| P-0461 | scripts/backup-sqlite.sh:53 | scripts/backup-sqlite.sh:53 `.backup '$BACKUP_PATH'` 把 BACKUP_PATH 作为单引号字符串拼到 sqlite3 命令, BACKUP_DIR 含单引号/反斜杠时该命令解析failure甚至生成异名file   | 已修复 | SQLite `.backup` directly拼path, 未转义特殊字符.  |
| P-0462 | scripts/run-curated-tests.mjs:68-74 | scripts/run-curated-tests.mjs:68-74 exit handle用 `process.exit(code ?? 1)` 但未先 unref/close child stdio, 长时间挂住的子process仍可能因 `stdio:"inherit"` 让父process退出后留下僵尸handle   | 已修复 | curated test runner 用 `exit` 事件directly退出, 未等待子process完全关闭.  |
| P-0463 | - | scripts/ 多个 .mjs 内 88 处 `console.*` directly写 stdout/stderr, 未走 src 内 structured-logger, CI log双轨无关联 trace_id   | 已复核关闭 | Root cause: 审查把 CI/审计脚本的processoutputinterface按线上 runtime logger 标准要求了; 这些 `.mjs` 本来就是命令行审计entry, stdout/stderr 是预期contract, 不是观测missing陷.  |
| P-0464 | scripts/run-curated-tests.mjs:62-66 | scripts/run-curated-tests.mjs:62-66 spawn 子process时 `env: process.env` full传递, contains `AA_CREDENTIALS_ENCRYPTION_KEY/AA_OAUTH_*` 等敏感凭据, testing代码can read到生产凭据   | 已修复 | curated test runner 继承完整父process环境, 未隔离高敏变量.  |
| P-0465 | scripts/backup-sqlite.sh:81-84 | scripts/backup-sqlite.sh:81-84 `openssl enc -aes-256-cbc` 没有 AEAD/HMAC, 备份integrity无认证; 攻击者可改写密文导致还原后 SQLite corrupted (应uses -aes-256-gcm 或外加 HMAC)    | 已修复 | 备份加密沿用 CBC, missing少认证保护.  |
| P-0466 | - | scripts/ 顶层 generate-src-module-test-matrix.mjs, reorg-code-structure.mjs, run-curated-tests.mjs 同样missing shebang, dependency `node script.mjs` indirectlycall, 文档写"chmod +x"会failure   | 已修复 | 可directlyexecute的 `.mjs` 脚本missing少 shebang.  |
| P-0467 | scripts/backup-sqlite.sh:106 | scripts/backup-sqlite.sh:106 `find ... -mtime +N -delete` 在 BACKUP_DIR 下若有人手放置非备份目录会被一并扫描, 没有 `-maxdepth 1` 限定 → 误删risk   | 已修复 | 备份cleanup `find` range过宽, 未limit在备份目录第一层.  |
| P-0468 | scripts/backup-sqlite.sh:48 | scripts/backup-sqlite.sh:48 `echo $$ > "$PID_FILE"` 不是原子创建, 两个process几乎同时启动时都via第 36 行 `-f` check, 最终两份备份concurrent写入 BACKUP_DIR   | 已修复 | 备份lockbased on普通 pid file, 创建不是原子操作.  |
| P-0469 | scripts/ci/audit-*.mjs | scripts/ci/audit-*.mjs 至少 7 个filemissing `#!/usr/bin/env node` shebang (audit-sync-async-service-pairs.mjs, audit-codebase-inventory.mjs, audit-review-governance-closures.mjs, audit-review-runtime-schema-audit-columns.mjs, audit-harness-index-split.mjs, audit-implementation-remediation.mjs, audit-review-dependency-upgrade-plan.mjs)    | 已修复 | 多支 CI 审计脚本missing少 shebang, 跨平台directlyexecute不稳定.  |
| P-0470 | docs_zh/contracts/<br>docs_en/contracts | docs_zh/contracts/ 的多file UTF-8 corrupted (P-0473) 至今未 lint, 且 docs_en/contracts 等价file未机翻synchronous (P-0481) ; CI 无 charset/编码门禁   | 已修复 | Root cause:  contracts 树此前只有人工synchronous约定, 没有机器门禁. 本轮保留并接入 `scripts/ci/audit-docs-charset.mjs`, 新增 `scripts/ci/audit-docs-sync.mjs`, 两者都进入 `audit:repo-hygiene`.  |
| P-0471 | docs_zh/adr<br>docs_en/adr<br>docs_en/adr/README.md<br>docs_zh/adr/README.md) <br>README.md | docs_zh/adr 与 docs_en/adr 各 110 项数量相等, 但两侧 README content是逐行机翻镜像, 未做编号synchronous声明 (docs_en/adr/README.md vs docs_zh/adr/README.md)    | 已修复 | Root cause:  ADR README 之前只保持“数量接近”, 没有synchronous声明和state/日期validation. 本轮在中英文 ADR README 明确synchronous规则, 并用 `audit-docs-sync.mjs` validation编号, state, 日期一致性.  |
| P-0472 | docs_zh/contracts/event_registry_and_ops_threshold_contract.md | docs_zh/contracts/event_registry_and_ops_threshold_contract.md UTF-8 corrupted (P-0473) 至少 7 行, 且仓库 CI 未挂 markdownlint/charset validation门   | 已修复 | contract 文档missing少 charset 审计, 乱码长期未被门禁拦截.  |
| P-0473 | docs_zh/contracts/event_registry_and_ops_threshold_contract.md | docs_zh/contracts/event_registry_and_ops_threshold_contract.md 多行被 UTF-8 corrupted, "建议" 被替换为 "建议" (行 23,29,31,33,36-39 等)    | 已修复 | 文档filecontains replacement character, 历史编辑/编码转换未被检测.  |
| P-0474 | docs_zh/contracts/error_code_registry.md:46-78 | docs_zh/contracts/error_code_registry.md:46-78 注册表中无 `AA_LEGACY_CONTRACT`, `AA_DEPRECATED_CONTRACT`, 但 src 内已 emit 该 code (contract drift)    | 已修复 | 历史compatibilityerror码已在implementation中发出, 但注册表未登记映射.  |
| P-0475 | docs_zh/contracts/error_code_registry.md | docs_zh/contracts/error_code_registry.md naming规范要求 `<category>.<reason>` (P-0477) , 实际 src 内 `code:"MISSION_NOT_FOUND"` 等仍 SCREAMING_SNAKE, contract drift 累计 30+ 项; missing CI gate autocomparison   | 已修复 | Root cause: public API error码和内部validation码长期混用, 导致 registry 无法约束真正对外 surface. 本轮把 mission/http/websocket publicerror码统一为点分格式, 补充到中英文 registry, 并新增 `scripts/ci/audit-public-error-codes.mjs`.  |
| P-0476 | docs_zh/governance/terminology-bilingual.md:34 | docs_zh/governance/terminology-bilingual.md:34 定义 OAPEFLIR 为 "Observe→Assess→Plan→Execute→Feedback→Learn→Improve→Release 八阶段闭环", src 内 71 处 oapeflir references, 无 lint 检验各 stage 名拼写一致 (与 P-0480 事件namingdrift形成第二条 contract drift 维度)    | 已修复 | Root cause:  OAPEFLIR 阶段词序只写在 glossary/contract 里, 没有autovalidation. 本轮新增 `scripts/ci/audit-oapeflir-terminology.mjs`, 对带 canonical OAPEFLIR section 的 zh/en contract 文档做阶段order lint.  |
| P-0477 | docs_zh/contracts/error_code_registry.md<br>src/.../mission-services | docs_zh/contracts/error_code_registry.md 规定naming `<category>.<reason>`, 但 src 中exists大量 SCREAMING_SNAKE_CASE implementation: src/.../mission-services 等output `code: "MISSION_NOT_FOUND"`, `MODULE_NOT_FOUND`, `PACK_SCAN_*` 等 30+ 项   | 已修复 | Root cause:  review 把内部诊断码, 扫描码和外部稳定 `AppError` 混成一类. 修复口径改为“先约束public surface”, 因此 mission/http/channel 的外部error码已改为点分, 并由 `audit-public-error-codes.mjs` 持续守门; 内部码不再冒充 registry canonical code.  |
| P-0478 | docs_zh/reviews/<br>docs_zh/operations/<br>README.md | docs_zh/reviews/ 目录已积累 13 份 review file (README.md + 12 份具体) , 但 docs_zh/operations/ 没有"review 关闭/在跟"看板表, issues-table.md 也未references本file P-0241..P-0470 编号 (与 P-0505 invariants 闭环规则形成 governance missing口)    | 已修复 | Root cause:  review 目录和 operations 目录各自maintained, missing少统一的关闭stateentry. 本轮新增 `docs_zh/operations/review-closure-board.md`, 并从 `docs_zh/operations/operations-tracker.md` 与 `docs_zh/reviews/README.md` 双向references.  |
| P-0479 | docs_zh/reviews/platforme-full-review.md | docs_zh/reviews/platforme-full-review.md 全文 1909 条 N-XXXX 中 538 条以"复核收口/已复核收口/已解决/复核/本轮复核"的措辞将原missing陷重分类为"设计取舍/演进项/性能优化项/外部基础设施"而 **代码path未变**; 该 review 把state列统一标 "已解决", 对外形成"问题已闭环"的错觉, missing少independent的"以文档/复核方式关闭"state轴 → governance 上无法distinguish"已修代码"与"重新解释"两类闭环   | 已修复 | Root cause: 历史总表只有 `已解决/riskaccepts/开放` 三态, 没有“复核关闭”轴. 本轮在 `platforme-full-review.md` 增加 `已复核关闭` state, 并batch把复核/边界澄清类条目从 `已解决` 迁出, 避免把口径收敛masks成代码修复.  |
| P-0480 | docs_zh/contracts/event_registry_and_ops_threshold_contract.md<br>src/scale-ecosystem/runtime-services/* | docs_zh/contracts/event_registry_and_ops_threshold_contract.md 无 `event_published`, `subscriber_added`, `circuit_breaker_open/close`, `batch_flush`, `queue_overflow`, `operation_*`, `writeback_*` 等事件, 但 src/scale-ecosystem/runtime-services/* 大量 emit (drift 数十处)    | 已修复 | Root cause:  runtime-services async observability signals 没被纳入 contract 文档. 本轮在中英文事件注册表新增 `Runtime-Service Operational Signals` 段落, 并加入 `scripts/ci/audit-runtime-service-events.mjs` 对源码 event union 做对照.  |
| P-0481 | docs_en/contracts/error_code_registry.md | docs_zh 与 docs_en file数 380 vs 376 接近持平, 但具体目录命中如 docs_en/contracts/error_code_registry.md 与 docs_zh 同名file并非逐行对译, exists词条drift (与 P-0471 互证, 扩大到 contracts 整树)    | 已修复 | Root cause:  contracts 目录此前只有人工翻译流程, 没有树级synchronousvalidation. 本轮补齐 docs sync 审计, 并synchronous更新中英文 `error_code_registry.md`, `event_registry_and_ops_threshold_contract.md` 和 ADR README 对齐项.  |
| P-0482 | ui/package.json<br>ui/packages/ui-mobile<br>package.json | ui/package.json `"react-native": "0.0.0-stub"` placeholder会让任何dependency RN 的代码在解析期failure; ui/packages/ui-mobile entry形同空壳   | 已修复 | UI root directlydependency react-native 伪placeholder版本, 把未落地移动端dependencymasks成可安装dependency.  |
| P-0483 | ui/package.json<br>package.json | ui/package.json `workspaces` 含 `tools/*`, `apps/*`, 但 root tsconfig 未via references 声明子项目, IDE 跨包跳转破裂   | 已修复 | Root cause:  workspace 只在 `ui/package.json` 里声明, TypeScript 工程graph没有把 UI 项目接到根级 references. 本轮为 root `tsconfig.json` 增加 `./ui/tsconfig.json` project reference, 并把 UI tsconfig 标记为 composite; `npm --prefix ui run typecheck` 已via.  |
| P-0484 | ui/package.json:42<br>ui/packages/ui-mobile/package.json:17<br>package.json:42<br>package.json:17 | ui/package.json:42 `"react-native": "0.0.0-stub"`, ui/packages/ui-mobile/package.json:17 同样uses `0.0.0-stub` placeholder语义版本, npm 解析会产生警告但更可能装入幽灵dependency; 移动端entry长期未真正开发   | 已修复 | UI 包和 mobile 包都用 0.0.0-stub 伪版本, 包manage器解析与真实dependencycontract脱节.  |
| P-0485 | ui/package.json:36<br>package.json:36 | ui/package.json:36 `"ci"` 链路串接 `lint && typecheck && test:coverage && build && bundle:analyze && perf:budget && npm audit --audit-level=high`, 任何一步failure都会终止; 同时 root `npm test` 已绑 `ci:baseline`, 外层 CI duplicateexecute typecheck (root tsconfig + ui tsconfig) 显著加长流水线   | 已修复 | Root cause:  UI `build` via `build:app` 递归再跑一次 `typecheck`, 把 `ui ci -> build -> typecheck` 变成explicitlyduplicatevalidation. 本轮把 `ui/package.json` 的 `build:app` 改为directlyexecute web build, 保留一次 `ui typecheck` 即可; `npm --prefix ui run build` 已via.  |
| P-0486 | tests/integration/sdk/cli/ops-cli.test.ts:1577 | comment式 skip placeholder tests/integration/sdk/cli/ops-cli.test.ts:1577 "serialTest doesn't support .skip()", 等同隐式禁用   | 已修复 | Root cause:  `serialTest` helper 不支持 skip 选项, 导致集成用例只能整段comment; 现在 helper 支持 `skip` 元data, replay-events 用例已改成explicitly skipped test.  |
| P-0487 | tests/unit/sdk/cli/api-server.test.ts:17,23 | 弱assertion `assert.ok(true)` 在 tests/unit/sdk/cli/api-server.test.ts:17,23   | 已修复 | api-server 单测只验证“没有抛错”, 没有assertion源码里真正接线了entry约束.  |
| P-0488 | tests/golden/deploy/helm-ingress-guardrails.test.ts:7 | 大量个人绝对pathhardcoded (391 命中) ; 样例 tests/golden/deploy/helm-ingress-guardrails.test.ts:7 `/Users/holden/Project/...`   | 已修复 | testing和 UI/回放 fixture directly从开发机path拷贝字符串, 没有统一走仓库根解析 helper.  |
| P-0489 | tests/unit/testing/index.test.ts:161 | tests 弱assertion `assert.ok(true)` 共出现 217 处; 样例 tests/unit/testing/index.test.ts:161   | 已修复 | 示例testing把“execute到这里”当成via条件, 没有核对工具函数的可观察结果.  |
| P-0490 | tests/unit/platform/five-plane-orchestration/harness.test.ts:12-14 | import 绝对path渗入主线testing: tests/unit/platform/five-plane-orchestration/harness.test.ts:12-14   | 已修复 | 主线单测directly写死本机绝对 import, 跨机器无法解析.  |
| P-0491 | tests/unit/plugins/github-adapter.test.ts:33,39 | 弱assertion `assert.ok(true)` 在 tests/unit/plugins/github-adapter.test.ts:33,39 directlyvia, 不验证returncontent   | 已修复 | GitHub adapter 认证testing只assertion“不抛错”, 没有confirmationreturn行为.  |
| P-0492 | tests/helpers/golden.js | helpers 中保留构建产物 (.js/.js.map/.d.ts) 与 .ts 并存 (tests/helpers/golden.js + golden.ts) , 扰乱 ESM 解析   | 已修复 | testing辅助module曾把 TypeScript 编译产物directly入仓, missing少对 helpers 目录的产物cleanup和门禁.  |
| P-0493 | tests/integration/platform/security/sandbox-patch-dsl-service.test.ts:195,240 | testing源中嵌入 `// TODO: fix - ...` 但仍保留 happy assertion: tests/integration/platform/security/sandbox-patch-dsl-service.test.ts:195,240   | 已修复 | 用 TODO comment替代真实assertionanalysis, 导致failure语义与预期行为长期脱节.  |
| P-0494 | tests/unit/scale-ecosystem/runtime-services/execution-dispatch-service-async.test.ts:15,20,25 | 弱assertion `assert.ok(true)` 在 tests/unit/scale-ecosystem/runtime-services/execution-dispatch-service-async.test.ts:15,20,25 三连placeholder   | 已修复 | async dispatch service 样例testing只验证executepath未抛错, 没有coveragepublic API 面.  |
| P-0495 | tests/integration/platform/security/path-traversal-advanced.test.ts:44 | testing源中嵌入 `// TODO: fix - ...`: tests/integration/platform/security/path-traversal-advanced.test.ts:44 与 enterprise-capability-boundary.test.ts:73   | 已修复 | 两个安全边界testing都曾用 TODO comment保留已知偏差, 未把预期行为正式编码进testing语义.  |
| P-0496 | tests/unit/runtime/multi-step-orchestration-comprehensive.test.ts | 弱assertion `assert.ok(result)` 在 tests/unit/runtime/multi-step-orchestration-comprehensive.test.ts 共 8 处 (724-872) , 未对结构fieldassertion   | 已修复 | 综合编排testing只assertionreturn对象exists, 没有验证关键 snapshot/task field.  |
| P-0497 | tests/unit/domains/domains-bootstrap-functions.test.ts:17,27,34,41,48,55,75 | 弱assertion `assert.ok(result)` 在 tests/unit/domains/domains-bootstrap-functions.test.ts:17,27,34,41,48,55,75 串联 7 处   | 已修复 | domain bootstrap 用对象 truthiness 代替具体 phase/capability assertion.  |
| P-0498 | - | tests 中 `assert.ok(true)` 217 处, `assert.ok(result)` 329 处 (P-0489/P-0503) , 合计 ~546 处弱assertion; 假设不变量missing就会"绿色via", 是coverage率指标的虚高来源   | 已复核关闭 | Root cause:  A 表把主表 `N-1575/N-1576` 的弱assertion治理结论与若干已逐项修复的样例再次汇总挂账; 当前剩余 truthy/no-op assertion已按testing分层, mutation gate 与专项整改manage, 不再单列一条“总量告警”冒充未闭环missing陷.  |
| P-0499 | tests/unit/platform/execution/execution-engine/session-replay/*.jsonl | fixture 中以 `/Users/holden/...` 作为 artifact uri 内嵌进 tests/unit/platform/execution/execution-engine/session-replay/*.jsonl 多个file   | 已修复 | session replay 样本由local运行结果directly落库, artifact URI 没有在入仓前去个人化.  |
| P-0500 | tests/golden/snapshots<br>tests/golden<br>tests/helpers/golden.ts:13 | tests/golden/snapshots 共 239 个 .golden file, 而 tests/golden testing ts 数量更多 (48+) , 需要 helper validationmissing: tests/helpers/golden.ts:13 lock死path但missingvalidation机制   | 已修复 | golden helper 只做读写对比, 没有把“references的 snapshot 是否exists”纳入仓库卫生审计.  |
| P-0501 | tests/unit/scale-ecosystem/runtime-services/durable-event-bus-async.test.js:81 | 弱assertion `assert.ok(true)` 同时exists .ts 与对应 .js 影子 (如 tests/unit/scale-ecosystem/runtime-services/durable-event-bus-async.test.js:81 与 .ts:98) 双倍噪音   | 已修复 | 旧testing编译影子和源码同时入仓, 把同一弱assertion复制成双份噪音.  |
| P-0502 | tests/unit/platform/five-plane-orchestration/hitl/hitl-approval-orchestration-service.test.ts:3-8 | import 直指 `/Users/holden/...` 绝对path, 跨机不可运行: tests/unit/platform/five-plane-orchestration/hitl/hitl-approval-orchestration-service.test.ts:3-8   | 已修复 | HITL orchestration 单测directlydependency开发机绝对path import.  |
| P-0503 | tests/integration/platform/execution/queue-metrics/queue-metrics-service.integration.test.ts:70,91,148,176 | 弱assertion `assert.ok(result)` 共出现 329 处; 样例 tests/integration/platform/execution/queue-metrics/queue-metrics-service.integration.test.ts:70,91,148,176   | 已修复 | queue metrics 集成样例曾只验证 dequeue return非空, 没有assertion队列名和subsequentstate.  |
| P-0504 | tests/helpers/ | tests/helpers/ 同时含 `golden.{ts,js,d.ts,js.map}`, `fs.{ts,js,d.ts,js.map}` 等近 20 套同名 .ts/.js/.d.ts 三件套; ESM 解析时 Node 的 module resolution 优先 .js, 可能跑expiry编译   | 已修复 | tests/helpers 曾被当作编译output目录uses, 源码和产物共址导致 Node 先解析到stale `.js`.  |
| P-0505 | tests/invariants/current-todo-list-closure.test.ts:5<br>docs_zh/operations/current_todo_list.md | tests/invariants/current-todo-list-closure.test.ts:5 用相对path `docs_zh/operations/current_todo_list.md` 作 TODO 闭环dependency, 跨工作目录易断   | 已修复 | invariant testing把仓库根path假定成当前工作目录, path解析对execute位置敏感.  |
| P-0506 | tests/helpers/golden.{ts,js,d.ts,js.map} | tests/helpers/golden.{ts,js,d.ts,js.map} 多份并存 (P-0492) , 跨 ESM/CJS 双解析时 helper 实例双份, assertion不可比较   | 已修复 | golden helper 目录同时承载源码与编译产物, moduleidentity被duplicate切分.  |
| P-0507 | tests/golden/snapshots<br>CLAUDE.md | tests 目录 .test.ts 共 4682 个, 但 `tests/golden/snapshots` .golden only 239 (P-0500) ; snapshot 比例严重失衡, 与 CLAUDE.md 描述的"golden full"宣称矛盾   | 已修复 | golden 套件missing少“目标是定向contract快照而非 1:1 coverage所有testingfile”的说明, 也missing少referencesexists性审计.  |
| P-0508 | .gitignore:41-43 | tests/ 目录全树共 1412 个 `.test.js` 与 1388 个 `.test.d.ts`, 1382+ 个 `.test.js.map` 跟 4682 个 `.test.ts` 并存; 编译outputpollute源码树, 违背 .gitignore:41-43 `dist/**/*.js` 的初衷   | 已修复 | testing编译产物曾directly写回源码树, `.gitignore` 和 repo hygiene 都没有对 `tests/**/*.test.{js,d.ts,js.map}` 建立门禁.  |
| P-0509 | tests/invariants/ | tests/invariants/ 目录下 19 个 `*.test.js`, 相同数量 `.test.d.ts` 与 `.test.js.map` 与 `.test.ts` 共存 (如 architecture-invariant-registry.test.{ts,js,d.ts,js.map}) — 编译产物已入仓   | 已修复 | invariants 目录和其他testing树一样把构建影子产物directly入仓, missing少统一cleanup规则.  |
| P-0510 | tests/unit/platform/execution/execution-engine/index.test.js<br>CLAUDE.md:23<br>tsconfig.json:32 | CLAUDE.md:23 单测样例path `dist/tests/unit/platform/execution/execution-engine/index.test.js` 与 tsconfig.json:32 排除规则conflict (P-0511) , 文档样例无法复制运行   | 已修复 | 文档示例沿用了已失效的 dist testingpath, 和当前构建排除规则脱节.  |
| P-0511 | tests/unit/platform/execution/execution-engine/index.test.js<br>CLAUDE.md:23<br>tsconfig.json:32 | CLAUDE.md:23 单测样例 `node --test dist/tests/unit/platform/execution/execution-engine/index.test.js` 与 tsconfig.json:32 排除规则conflict, 构建后 dist 不exists该path   | 已修复 | 单测运行说明假定testingfile会被构建进 dist, 但 tsconfig 已明确排除 tests.  |
| P-0512 | tests/, tests/helpers/, tests/invariants/, helpers/<br>.gitignore | .gitignore 同时排除 `dist/**/*.js`, `dist/**/*.js.map`, `dist/**/*.d.ts`, 但 tests/, tests/helpers/, tests/invariants/, helpers/ 等非 dist path下的 `.js/.d.ts/.js.map` 仍入仓 (P-0509-0399)    | 已修复 | 仓库先前只把 `dist/` 当作产物边界, 没有把testing树和根级 helpers 的编译影子纳入忽略与审计range.  |
| P-0513 | .github/workflows/ci.yml | .github/workflows/ci.yml 触发contains `branches: codex/**` (ci.yml:7) , 把代理生成branch前缀写死到主仓 CI   | 已修复 | CI 把代理branch前缀固化进主仓触发条件.  |
| P-0514 | .github/workflows/ci.yml:173 | .github/workflows/ci.yml:173 `aquasecurity/trivy-action@0.32.0` uses浮动 tag 非 digest   | 已复核关闭 | Root cause: 该条把同一类供应链问题拆成单独 surface 反复挂账; Trivy action 的 tag pin 不足已在总审计 `N-1399` 统一归并为“第三方镜像/Action 未做 digest 或 SHA pin”missing口.  |
| P-0515 | .github/workflows/ui-quality.yml:58 | .github/workflows/ui-quality.yml:58 references第三方 `treosh/lighthouse-ci-action@v12` 浮动 major tag, 非 commit SHA   | 已复核关闭 | Root cause:  action pinning missing口被按工作流逐条duplicate登记; `treosh/lighthouse-ci-action@v12` 与其他第三方 action 一样, 统一归口到 `N-1399` 的 supply-chain pinning 问题.  |
| P-0516 | .github/workflows/dr-validation.yml:71-72 | .github/workflows/dr-validation.yml:71-72 把 `${{ inputs.mode }}` `${{ inputs.component }}` directly拼进 shell (无引号, 无validation) , existsinjectionrisk   | 已修复 | DR workflow 把 dispatch 输入directly拼进 shell 参数.  |
| P-0517 | .github/workflows/ci.yml | .github/workflows/ci.yml schedule `cron: "0 5 * * 1"` 每周一 UTC 5 点, 未做时区/holiday drift说明, 也未带 cancel-in-progress=false distinguish push/scheduled   | 已修复 | CI 定时task沿用了 push 的concurrentstrategy, 也没有把 cron 的绝对时区语义写进工作流comment.  |
| P-0518 | .github/workflows/dr-validation.yml | .github/workflows/dr-validation.yml missing `permissions:` 块 (P-0530 已记 deploy-environment injectionrisk, dr-validation 这里更糟: default GITHUB_TOKEN 写permissions全开 + workflow_dispatch 自由 inputs)    | 已修复 | DR workflow missing少最小permissions `permissions`.  |
| P-0519 | .github/workflows/publish-image.yml:75-77 | .github/workflows/publish-image.yml:75-77 把 `${{ inputs.image_repository }}` 与 `${{ inputs.image_tag }}` directlyinjection metadata-action 与 docker tag   | 已修复 | publish workflow directly消费未validation的镜像仓库/标签输入.  |
| P-0520 | .github/workflows/deploy-environment.yml:150 | CI script-injection: deploy-environment.yml `STRATEGY="${{ inputs.rollout_strategy }}"` 拼入 bash .github/workflows/deploy-environment.yml:150   | 已修复 | deploy workflow 把 environment / strategy directly拼进 shell 与path.  |
| P-0521 | .github/workflows/publish-image.yml:80-86 | .github/workflows/publish-image.yml:80-86 `docker/build-push-action@v6` 未传 `provenance: true` 或 SBOM 生成 (与 README 提到的"contract"/"evidence"理念脱节)    | 已修复 | 镜像发布未开启 provenance / SBOM 生成.  |
| P-0522 | .github/workflows/dr-validation.yml:71 | CI script-injection: dr-validation.yml `run:` directly拼接 `${{ inputs.mode }}` / `${{ inputs.component }}` 进 shell .github/workflows/dr-validation.yml:71   | 已修复 | DR workflow 把 dispatch 输入directly拼进 shell 参数.  |
| P-0523 | .github/workflows/dr-validation.yml:71-72 | .github/workflows/dr-validation.yml:71-72 `${{ inputs.mode }}`/`${{ inputs.component }}` 未量化, 未走 env 中转 (P-0516) , 结合 inputs 自由textfield属典型 GHA injection   | 已修复 | DR workflow 把 dispatch 输入directly拼进 shell 参数.  |
| P-0524 | .github/workflows/publish-image.yml:74 | .github/workflows/publish-image.yml:74 `tags: type=raw,value=${{ inputs.image_tag }}` directly传入user字符串, metadata-action 内部不做 tag 安全validation, malicious input 可injection OCI label   | 已修复 | publish workflow directly消费未validation的镜像仓库/标签输入.  |
| P-0525 | .github/workflows/publish-image.yml:75 | CI script-injection: publish-image.yml `images: ${{ env.IMAGE_REGISTRY }}/${{ inputs.image_repository }}` 输入directly拼入 .github/workflows/publish-image.yml:75   | 已修复 | publish workflow directly消费未validation的镜像仓库/标签输入.  |
| P-0526 | .github/workflows/ci.yml:24-37<br>Dockerfile:1<br>package.json:9 | .github/workflows/ci.yml:24-37 matrix `node-version: [20, 22]`, Dockerfile:1 lock `node:22-bookworm-slim`, CI 验过的 Node 20 path在容器里永远不跑 (package.json:9 还限 `<23`)    | 已修复 | 仓库曾同时maintained“源码compatibility Node 20/22”和“容器只构建 Node 22”两套基线, 没有选定单一runtimecontract.  |
| P-0527 | .github/workflows/secret-provider-integration.yml:6<br>.env.example | .github/workflows/secret-provider-integration.yml:6 perms 行exists但全文短, 需进一步审: 未跑实际 secret provider mock 还是真凭证 (与 .env.example 中 ANTHROPIC_API_KEY 等naming相对)    | 已复核关闭 | 当前 workflow only消费 `AA_TEST_*_INLINE_SECRETS` testing夹具变量并在log中explicitly mask, 没有directlyuses真实 provider 登录凭据的path.  |
| P-0528 | .github/workflows/deploy-environment.yml:227 | CI script-injection: deploy-environment.yml `kubectl patch ... selector ... automatic-agent-${{ steps.rollout.outputs.slot }}` 拼入 shell .github/workflows/deploy-environment.yml:227   | 已修复 | blue/green promote pathdirectly在 shell 中内联拼接目标实例名.  |
| P-0529 | .github/workflows/publish-image.yml:69-77 | .github/workflows/publish-image.yml:69-77 `docker/metadata-action@v5` `images:` fielddirectly拼 `${{ env.IMAGE_REGISTRY }}/${{ inputs.image_repository }}`; inputs.image_repository 没有regex白名单 (P-0519 已记 tag, 未记 repository)    | 已修复 | publish workflow directly消费未validation的镜像仓库/标签输入.  |
| P-0530 | .github/workflows/deploy-environment.yml:138,184,213,218,289 | .github/workflows/deploy-environment.yml:138,184,213,218,289 multiple places `case "${{ inputs.environment }}"` / `-f values-${{ inputs.environment }}.yaml`, 未via env 中转   | 已修复 | deploy workflow 把 environment / strategy directly拼进 shell 与path.  |
| P-0531 | .github/workflows/ci.yml:7 | .github/workflows/ci.yml:7 `branches: codex/**` (P-0513) 把代理生成branch前缀写死到主仓 CI; 同 workflow 中 deploy-environment.yml/publish-image.yml 又via `${{ inputs.* }}` directly shell 拼接 (P-0530/P-0519) 形成 supply-chain attack surface   | 已复核关闭 | 该复合面由“代理branch触发 + 未validation workflow 输入”叠加形成; 相关触发条件和输入injection点已在前序修复中拆除.  |
| P-0532 | deploy/helm/automatic-agent/templates/deployment.yaml | deploy/helm/automatic-agent/templates/deployment.yaml 未声明 `automountServiceAccountToken: false` 在 spec 级别coverage   | 已修复 | ServiceAccount 与 Pod 未explicitly关闭 token auto挂载.  |
| P-0533 | deploy/helm/automatic-agent/templates/serviceaccount.yaml:1-12 | deploy/helm/automatic-agent/templates/serviceaccount.yaml:1-12 only声明 SA, 未支持 `automountServiceAccountToken: false`, pod default挂载 K8s API token   | 已修复 | ServiceAccount 与 Pod 未explicitly关闭 token auto挂载.  |
| P-0534 | deploy/helm | deploy/helm 模板目录未提供 `NetworkPolicy`, `grep -l NetworkPolicy deploy/` 无命中, 生产naming空间default放任所有出向流量   | 已修复 | chart missing少default NetworkPolicy.  |
| P-0535 | deploy/helm/automatic-agent/templates/pvc.yaml:7-15 | deploy/helm/automatic-agent/templates/pvc.yaml:7-15 PVC 不声明 `volumeMode`, `storageClassName` default (values 未填则按集群 default storage class) , 跨集群portable不可重现   | 已修复 | PVC dependency集群default `storageClass` 与 `volumeMode`.  |
| P-0536 | deploy/helm/automatic-agent/templates/deployment.yaml | deploy/helm/automatic-agent/templates/deployment.yaml 全文未声明 container 级 `securityContext`, only pod 级, violates Kubernetes restricted PSS   | 已修复 | Deployment 只有 pod 级别基线, 容器级安全上下文missing.  |
| P-0537 | deploy/helm/automatic-agent/templates/service.yaml | deploy/helm/automatic-agent/templates/service.yaml 未split `metrics` 为independent ClusterIP service + ServiceMonitor 选择器, 与 Prometheus Operator 范式不符   | 已修复 | 业务 Service 同时暴露 http 与 metrics, 指标端口跟随 service.type 外放.  |
| P-0538 | deploy/helm/automatic-agent/templates/canary-ingress.yaml(28 | deploy/helm/automatic-agent/templates/canary-ingress.yaml(28 行) 与 ingress.yaml(45 行) 双 ingress 同名 host 在某些 ingress-nginx 版本会触发 canary 注解conflict, missing乏 lint validation   | 已复核关闭 | Root cause: 审查把 ingress-nginx 的 canary 同 host 模式当成conflictimplementation; 当前 canary ingress independentnaming且符合该控制器约定, 相关 host 渲染也已有 golden guardrail.  |
| P-0539 | deploy/helm/automatic-agent/values-prod.yaml:32-35 | deploy/helm/automatic-agent/values-prod.yaml:32-35 `ingress.domain: ""`, `hosts: []`, `tls[0].hosts: []` default空 -- 若 CI 没explicitly覆写, Helm 渲染仍可success但实际无可用 host (与 ingress.yaml:2 `required` validationexistsmisalignment)    | 已复核关闭 | Root cause: 原审查based on旧的 prod values 快照; 当前 `values-prod.yaml` 已explicitly写入 domain/hosts/tls, 且 `tests/golden/deploy/helm-ingress-guardrails.test.ts` 会持续validation.  |
| P-0540 | deploy/helm/automatic-agent/templates/configmap.yaml:7-9 | deploy/helm/automatic-agent/templates/configmap.yaml:7-9 directly把 `.Values.env` full铺到 ConfigMap, 没有filter敏感键名 (如 `*_TOKEN`/`*_SECRET`/`*_KEY`) , 易因 values 误填导致密钥落入 ConfigMap   | 已修复 | ConfigMap 无敏感 env 键filter.  |
| P-0541 | deploy/helm/automatic-agent/values-${{<br>.github/workflows/deploy-environment.yml:184 | CI script-injection: deploy-environment.yml `-f "deploy/helm/automatic-agent/values-${{ inputs.environment }}.yaml"` 进 shell .github/workflows/deploy-environment.yml:184   | 已修复 | deploy workflow 把 environment / strategy directly拼进 shell 与path.  |
| P-0542 | deploy/helm/automatic-agent/templates/service.yaml:8 | deploy/helm/automatic-agent/templates/service.yaml:8 `type: {{ .Values.service.type }}` default ClusterIP (values.yaml:17 未明确) , 但 metrics 端口 9090 与 http 端口同 service 暴露; 若 values 改 LoadBalancer, metrics 直对外   | 已修复 | 业务 Service 同时暴露 http 与 metrics, 指标端口跟随 service.type 外放.  |
| P-0543 | deploy/helm/automatic-agent/templates/configmap.yaml:7-9 | deploy/helm/automatic-agent/templates/configmap.yaml:7-9 `range $key, $value := .Values.env` directly序列化所有 env 到 ConfigMap, 未排除 `*_PASSWORD/*_TOKEN/*_SECRET` 模式 (与 P-0540 互证: missingfilter)    | 已修复 | ConfigMap 无敏感 env 键filter.  |
| P-0544 | deploy/helm/automatic-agent/templates/hpa.yaml:1-36<br>src/platform/five-plane-execution/queue-metrics | deploy/helm/automatic-agent/templates/hpa.yaml:1-36 onlybased on CPU/Memory, 没有自定义 metric (队列深度, dispatch lag, SLO error budget) , 与 src/platform/five-plane-execution/queue-metrics 暴露的 SLI 脱节   | 已修复 | Helm HPA 模板只暴露资源型指标entry, 没有接自定义业务指标configure.  |
| P-0545 | deploy/helm/automatic-agent | deploy/helm/automatic-agent 整目录无 `ResourceQuota`, `LimitRange`, `PodSecurityPolicy`/`PodSecurityAdmission` 标签   | 已修复 | chart 未内置 namespace 级资源约束模板.  |
| P-0546 | deploy/helm/automatic-agent/templates/deployment.yaml:80-83 | deploy/helm/automatic-agent/templates/deployment.yaml:80-83 preStop `["sleep", "5"]` 与 `terminationGracePeriodSeconds` (values 控制) 无强约束 (grace < 5 时directly被 SIGKILL)    | 已修复 | preStop 睡眠时长与 termination grace period 没有关联validation.  |
| P-0547 | deploy/helm/automatic-agent/values-prod.yaml:39-41 | deploy/helm/automatic-agent/values-prod.yaml:39-41 `AA_API_HOST: "0.0.0.0"`, `AA_API_PORT: "3000"`, `AA_METRICS_PORT: "9090"` 在 prod 把 API 与 metrics 都监听 0.0.0.0; container security 边界靠 K8s Service 而非process   | 已复核关闭 | Root cause: 把 Pod 内监听地址误当成暴露边界; 当前 chart 已split metrics service 并提供 NetworkPolicy, 真正的暴露面由 K8s 网络资源控制而不是容器内 `0.0.0.0`.  |
| P-0548 | deploy/helm/automatic-agent<br>deploy/prometheus/rules | deploy/helm/automatic-agent 整目录无 `ServiceMonitor`/`PodMonitor` (Prometheus Operator 用) 和 `PrometheusRule`, prometheus 规则在 deploy/prometheus/rules 而非 chart 内 → 部署不可一致   | 已修复 | chart 未内置 Prometheus Operator 资源.  |
| P-0549 | deploy/helm/automatic-agent/templates/secret.yaml:9-23<br>.env.example | deploy/helm/automatic-agent/templates/secret.yaml:9-23 via Helm `stringData` directly渲染明文 `ANTHROPIC_API_KEY/OPENAI_API_KEY/MINIMAX_API_KEY/AA_API_JWT_SECRET`, 与 README/.env.example 强调"不要承载真实密钥"的contract相悖; 当 `externalSecret.enabled=false` 时无门禁   | 已修复 | externalSecret 关闭时allowsdirectly渲染 inline 明文 secrets, missing少explicitlyconfirmation.  |
| P-0550 | deploy/helm/automatic-agent/templates/deployment.yaml:23-29<br>docker-compose.yml:25 | deploy/helm/automatic-agent/templates/deployment.yaml:23-29 `securityContext` only含 `runAsNonRoot/runAsUser/fsGroup`, missing `readOnlyRootFilesystem`, `allowPrivilegeEscalation: false`, `capabilities.drop: [ALL]`, `seccompProfile`, 与 docker-compose.yml:25 `read_only: true` 安全姿态inconsistent   | 已修复 | container securityContext missing少只读根filesystem, 禁止提权, drop capabilities 等基线.  |
| P-0551 | docker-compose.yml:1 | docker-compose.yml:1 仍写 `version: "3.9"`, Compose v2 已忽略并告警   | 已修复 | Compose file继续保留已忽略的 version 元data, 制造伪compatibility基线.  |
| P-0552 | Dockerfile:1,16 | Dockerfile:1,16 uses浮动 tag `node:22-bookworm-slim` (无 digest pin)    | 已复核关闭 | Root cause:  Dockerfile digest pin 问题被从 build/runtime 两个 stage 拆成多条duplicate面; 该missing口已在总审计 `N-1399` 统一归并.  |
| P-0553 | Dockerfile:16 | Docker 运行阶段同样uses浮动 tag: `FROM node:22-bookworm-slim AS runtime` Dockerfile:16   | 已复核关闭 | Root cause: 同一基镜像 pinning missing口被按 runtime stage 再次单列; 与 `P-0552/P-0554` 同属 `N-1399` 的duplicate surface.  |
| P-0554 | Dockerfile:1 | Docker 基镜像未按 digest 钉版: `FROM node:22-bookworm-slim AS build` uses浮动 tag Dockerfile:1   | 已复核关闭 | Root cause: 同一基镜像 pinning missing口被按 build stage 单独duplicate登记; 已归并到 `N-1399` 跟踪.  |
| P-0555 | docker-compose.yml:40-43 | docker-compose.yml:40-43 在非 swarm 模式uses `cpus: "1.0"`, `mem_limit`, `mem_reservation`, `pids_limit`, 被 docker compose silently忽略   | 已修复 | dependency被 Compose v2 silently忽略的资源field, 给local演练提供了error的资源约束幻觉.  |
| P-0556 | docker-compose.yml:11-13 | docker-compose.yml:11-13 `api-server` directly `build: { context: ., target: runtime }`, local compose 与 CI 镜像不复用同一 digest, local构建的 image 不可被 Helm 复现   | 已修复 | Compose onlylocal build 未explicitly绑定 image ref, local运行和 CI/Helm 镜像identity脱节.  |
| P-0557 | Dockerfile:53<br>docker-compose.yml | Dockerfile:53 docker-compose.yml `postgres:16-bookworm`, `redis:7-alpine`, `prom/prometheus:v2.54.1`, `prom/alertmanager:v0.27.0` 均为 tag 非 digest   | 已复核关闭 | Root cause:  compose/runtime dependency镜像的 digest pinning 和 Docker 基镜像 pinning 属同一供应链控制missing口; 该类问题已统一收口到 `N-1399`, 此处按duplicate面关闭.  |
| P-0558 | Dockerfile:1,16<br>package.json:9 | Dockerfile:1,16 build 与 runtime 都 `FROM node:22-bookworm-slim`, 但 package.json:9 `engines.node` 限定 `>=20 <23`, CI 镜像与 engines 上下界不留余量, 一旦升 Node 23 则 image-pin 立即违约   | 已修复 | `engines` 先前保留了宽泛的 20-22 区间, 但镜像和 CI 实际只以 Node 22 为部署基线.  |
| P-0559 | docker-compose.yml:1 | docker-compose.yml:1 `version: "3.9"` 已被 Compose v2 忽略, file顶部又写明 "Resource and security limits are intentionally explicit", 但下方 `cpus`/`mem_limit`/`pids_limit` 在非 swarm 模式被 silently drop (与 P-0555 同源但更明显的安全口径错觉)    | 已修复 | expiry version 与被忽略的资源limit同时exists, 文档口径与实际execute语义inconsistent.  |
| P-0560 | deploy/prometheus/prometheus.yml | deploy/prometheus/prometheus.yml 未启用 `alerting:` 段, prometheus 不会主动 push 到 alertmanager   | 已修复 | Prometheus configuremissing alerting 出口, 规则触发后没有notification链路.  |
| P-0561 | deploy/prometheus/rules/automatic-agent.yml | deploy/prometheus/rules/automatic-agent.yml 全文未references `up{}` / `process_*` / `kube_pod_*` 等基础设施 metric; 服务死亡(process panic) 不会触发告警   | 已修复 | 规则只盯业务阈值, 不coverage scrape/process级基础设施存活信号.  |
| P-0562 | deploy/prometheus/alertmanager.yml:31,38 | deploy/prometheus/alertmanager.yml:31,38 `${SLACK_WEBHOOK_URL}` / `${PAGERDUTY_SERVICE_KEY}` uses shell 风格变量, Alertmanager default不展开 env, 需 `--config.file` 渲染或 sidecar; 该写法在bare跑下是literal值   | 已修复 | directly把placeholder变量写进 Alertmanager configure, defaultruntime不会展开.  |
| P-0563 | deploy/prometheus/alertmanager.yml:5-15 | deploy/prometheus/alertmanager.yml:5-15 `route.routes[*].continue: true` 三个branch同时 fan-out, severity=critical 会同时 page + slack + default-webhook 三发, 与ops RACI 不清   | 已修复 | 路由branch一律 continue:true, 把 severity 路由变成无差别 fan-out.  |
| P-0564 | deploy/prometheus/rules/automatic-agent.yml | deploy/prometheus/rules/automatic-agent.yml 阈值 (error率 1%, p95 1s/3s, queue 100/500, worker 90/95%, timeout 5%) 全部hardcoded于 alert, 无 SLO/SLA ConfigMap 解耦, 不同tenant/环境共用   | 已复核关闭 | Root cause: 把 repo-versioned baseline alert policy 误判成必须runtime热configure; 当前规则file就是default SLO 基线, 不同环境应via overlay/independent规则集coverage, 而不是同一 ConfigMap dynamic改写.  |
| P-0565 | deploy/prometheus/rules/automatic-agent.yml:6 | deploy/prometheus/rules/automatic-agent.yml:6 `sum(rate(http_requests_total{status=~"5.."}[5m])) / clamp_min(sum(rate(http_requests_total[5m])), 1)`: 分母 `clamp_min(...,1)` 在零流量时把 0 当 1, 告警表达式在静止期default稳定, 但也掩盖"0 流量异常"   | 已修复 | via clamp_min(...,1) 把零流量masks成分母 1, 掩盖silently无流量场景.  |
| P-0566 | deploy/prometheus/prometheus.yml:1-12 | deploy/prometheus/prometheus.yml:1-12 `scrape_configs.static_configs.targets` 写死 `api-server:3000`, 与 helm Service name `{{ include "automatic-agent.fullname" . }}` output无对齐保证 (取决于 Helm release 名)    | 已修复 | 监控configure把 compose 目标和 k8s 目标混成一个static地址, 跨环境naming无法对齐.  |
| P-0567 | deploy/prometheus/alertmanager.yml:21-29 | deploy/prometheus/alertmanager.yml:21-29 `default-warning/slack-warning/pagerduty-critical` 三 receiver 都contains同一 `webhook_configs.url: http://api-server:3000/v1/alerts/webhook`, 工程上严重coupling (API 服务故障时告警自我递归到自身)    | 已修复 | 三个 receiver 都回调 API 自身 webhook, 告警链路与被监控服务自我coupling.  |
| P-0568 | deploy/prometheus/rules/automatic-agent.yml:38,46 | deploy/prometheus/rules/automatic-agent.yml:38,46 `sum(queued_tasks)`, `sum(active_executions)` 不带任何 label (namespace/instance/job) , 多实例聚合conflict; 与 src 中 emit 的 metric 标签结构无contract link   | 已修复 | 聚合表达式未保留 job 维度, 多实例或多拓扑会把队列与 worker 指标混算.  |
| P-0569 | deploy/chaos/approval-policy.yaml:1<br>deploy/helm | deploy/chaos/approval-policy.yaml:1 `apiVersion: automatic-agent.io/v1` 自定义 CRD, 但 deploy/helm 未提供对应 CRD `manifests/crds/`, chaos policy 部署上集群directlymissing定义   | 已修复 | chaos approval policy 清单先references了自定义资源, 但 Helm chart 没有交付对应 CRD.  |
| P-0570 | deploy/chaos/{network-delay,pod-kill,postgres-disconnect,redis-disconnect}.yaml<br>src/ops-maturity/chaos/chaos-experiment-scheduler.ts | deploy/chaos/{network-delay,pod-kill,postgres-disconnect,redis-disconnect}.yaml 4 类故障场景, 与 src/ops-maturity/chaos/chaos-experiment-scheduler.ts 的 scheduler 没有contract清单synchronous; scheduler 启动failure时无 fallback chaos profile   | 已修复 | chaos 场景 YAML 与 scheduler fallback profile 之间missingshared目录contract, 启动failure时也没有内建fallback catalog.  |
| P-0571 | deploy/runbooks/production-alert-runbook.md | deploy/runbooks/production-alert-runbook.md only 9 个章节短描述, 与 prometheus rules 9 条具体告警没有 1:1 锚点 (runbook 标题"Worker Mass Disconnect" vs alert `AutomaticAgentWorkerPoolExhaustion` naminginconsistent)    | 已修复 | Runbook 按口语化章节组织, 未与 alertname 建立 1:1 对应.  |
| P-0572 | deploy/terraform/main.tf:24<br>deploy/terraform/environments/multi-region/) 目标矛盾 | deploy/terraform/main.tf:24 `default = "us-east-1"` hardcodeddefault区域; 与多 region DR (deploy/terraform/environments/multi-region/) 目标矛盾   | 已修复 | 根module把 aws_region 写成default值, 多区域 overlay 仍受隐式区域控制.  |
| P-0573 | deploy/terraform/modules/rds/main.tf:79-95 | deploy/terraform/modules/rds/main.tf:79-95 SG `egress 0.0.0.0/0 -1` 全开出向, violates最小permissions   | 已修复 | RDS 安全组把出向放到 0.0.0.0/0, 没有按最小permissions约束Control Plane访问.  |
| P-0574 | deploy/terraform/modules/elasticache/main.tf:99-103 | deploy/terraform/modules/elasticache/main.tf:99-103 SG `egress 0.0.0.0/0 -1` 全开   | 已修复 | Redis 安全组同样全开出向, 安全边界只剩entry规则.  |
| P-0575 | deploy/terraform/main.tf:11-19 | deploy/terraform/main.tf:11-19 backend bucket/dynamodb_table/region 等关键configurehardcoded进根module, CI/不同 region 团队接入需修源码而非 vars   | 已修复 | backend bucket, lock, key 原先hardcoded进源码, 环境接入只能改源file.  |
| P-0576 | deploy/terraform/modules/eks/main.tf:74 | deploy/terraform/modules/eks/main.tf:74 `endpoint_public_access = true` default对公网暴露 K8s API server, 且无 `public_access_cidrs` limit   | 已修复 | EKS API default公网暴露且未limit CIDR, Control Plane边界过宽.  |
| P-0577 | deploy/terraform/modules/eks/main.tf:140-144 | deploy/terraform/modules/eks/main.tf:140-144 `scaling_config` 写错缩进 (4 空格 vs 2) , terraform fmt drift   | 已修复 | Node group configure长期未按 IaC 基线整理, 格式和结构一起drift.  |
| P-0578 | deploy/terraform/modules/rds/main.tf | deploy/terraform/modules/rds/main.tf 整体未启用 `iam_database_authentication_enabled`, 未声明 `enabled_cloudwatch_logs_exports`, 未force `rds.force_ssl`   | 已修复 | RDS module只建实例, 不补 IAM DB auth, log导出和 SSL 参array等合规基线.  |
| P-0579 | deploy/terraform/environments/prod.tfvars:2 | deploy/terraform/environments/prod.tfvars:2 `db_instance_class = "db.t3.large"` burstable 实例used for prod RDS (持续 CPU 信用消耗risk)    | 已修复 | 生产 tfvars 仍沿用 burstable 实例选型, 持续负载下 CPU credit risk被带进 prod.  |
| P-0580 | deploy/terraform/modules/eks/main.tf:134-155 | deploy/terraform/modules/eks/main.tf:134-155 `aws_eks_node_group.main` missing `launch_template`, `disk_encrypted`, `remote_access` 阻断; 节点磁盘default非 KMS 加密   | 已修复 | 节点组未via launch template force磁盘加密和 IMDSv2, 安全基线下沉failure.  |
| P-0581 | deploy/terraform/modules/elasticache/main.tf:114 | deploy/terraform/modules/elasticache/main.tf:114 `engine_version = "7.1"` pin 到精确次版本, 错过补丁; 与 docker-compose `redis:7-alpine` 浮动 tag inconsistent   | 已修复 | Redis engine 精确钉在 7.1 次版本, 补丁升级strategy被写死在源码.  |
| P-0582 | deploy/terraform/environments/prod.tfvars | deploy/terraform/environments/prod.tfvars 只列 6 项变量, 未coverage `vpc_cidr`, `availability_zones`, `storage_encrypted`, `kms_key_id` 等安全相关变量; 继承default `10.0.0.0/16` 与 us-east-1   | 已修复 | 环境 tfvars 未explicitly声明 region, CIDR, AZ 等基础安全参数, default值被偷偷继承.  |
| P-0583 | deploy/terraform/modules/eks/main.tf:99-132 | deploy/terraform/modules/eks/main.tf:99-132 cluster/nodes IAM 角色只 attach AWS 托管strategy, 无 OIDC IRSA configure (`identity { oidc {} }` missing) , Service Account 无法走 IRSA   | 已修复 | EKS 集群missing OIDC provider 资源, IRSA 无法作为default服务账号信任链落地.  |
| P-0584 | deploy/terraform/main.tf<br>deploy/terraform/backend.tf | deploy/terraform/main.tf 与 modules/{ecr,eks,elasticache}/main.tf exists但 environments/*.tfvars 未在 README 标注 backend/state configurestrategy, missing少远端 state lock (无 deploy/terraform/backend.tf 命中)    | 已修复 | 仓库missing backend/state uses说明, 远端 state 隔离strategy只exists隐性约定.  |
| P-0585 | deploy/terraform/modules/elasticache/main.tf:110-122 | deploy/terraform/modules/elasticache/main.tf:110-122 `aws_elasticache_replication_group` 启用了 `at_rest/transit_encryption`, 但missing `auth_token`/`auth_token_update_strategy`, 跨 VPC only靠 SG 边界   | 已修复 | ElastiCache 只开传输和static加密, 不configure auth token, runtime认证链missing口.  |
| P-0586 | deploy/terraform/modules/eks/main.tf:66-82 | deploy/terraform/modules/eks/main.tf:66-82 `aws_eks_cluster.main` missing `enabled_cluster_log_types`, `encryption_config`, CloudWatch 审计log与 secret envelope 加密都未启用   | 已修复 | EKS 集群未启用Control Planelog和 secrets envelope encryption, 审计与机密保护missing位.  |
| P-0587 | deploy/terraform/modules/rds/main.tf:99-102 | deploy/terraform/modules/rds/main.tf:99-102 comment掉的 `# password = aws_secretsmanager_secret_version.db_password.secret_string` 是唯一 password 来源, 当前module无 `password`/`manage_master_user_password` 任一field, apply 会因missing master password failure   | 已修复 | RDS module把 password 留在comment里, 实例实际没有合法主密码来源.  |
| P-0588 | deploy/terraform/main.tf:11-19 | deploy/terraform/main.tf:11-19 把 `backend "s3"` 写死 `bucket="automatic-agent-terraform-state"`, `region="us-east-1"`, `key="infra/terraform.tfstate"`, dev/staging/prod 三环境shared同一 state key, 无 workspace/前缀隔离 → arbitrary环境 apply 互相覆写   | 已修复 | 三环境与多区域原先共用一个 backend key, 任一 apply 都可能覆写别的环境state.  |
| P-0589 | deploy/terraform/modules/rds/main.tf:128-131 | deploy/terraform/modules/rds/main.tf:128-131 `parameters = [{ name=..., value=... }]` 写法是 list-of-objects, 但 `aws_db_instance` 不支持 `parameters` argument (应 `aws_db_parameter_group` + `parameter_group_name`) , terraform plan 会报错   | 已修复 | 把 parameters directly塞给 aws_db_instance, 混淆了实例资源和参array资源模型.  |
| P-0590 | .env.example<br>.env.example:33-40)  | .env.example 提交进仓且filecontains ANTHROPIC_API_KEY/OPENAI_API_KEY/MINIMAX_API_KEY 等键名 (.env.example:33-40) , 易诱导真实 .env 误提交   | 已修复 | `.env.example` 充当了凭据清单而不是最小local模板.  |
| P-0591 | .github/workflows/deploy-environment.yml:138 | CI script-injection: deploy-environment.yml `case "${{ inputs.environment }}"` directly拼入 bash .github/workflows/deploy-environment.yml:138   | 已修复 | deploy workflow 把 environment / strategy directly拼进 shell 与path.  |
| P-0592 | .env.example | .env.example 含 200+ 行 (grep 显示至少到行 162) , 按 P-0590 已知risk: contains `AA_STABLE_*_OUTPUT_DIR` 多达 12+ 行 (行 151-162) , 单纯path变量却混在 secret 表格内, 认知噪声   | 已修复 | path/调优变量与 secrets 混排, 模板职责失焦.  |
| P-0593 | .env.example<br>.env.example:21 | .env.example 在仓库内提供完整密钥placeholder (AA_API_JWT_SECRET / POSTGRES_PASSWORD / ANTHROPIC_API_KEY / OPENAI_API_KEY / MINIMAX_API_KEY) , 任何复制粘贴失误都会让真实凭据进入 git .env.example:21   | 已修复 | 模板把真实 secret 名称concentrated罗列在同一清单中, 易被误当作提交目标.  |
| P-0594 | .env.example:151-162 | .env.example:151-162 12 条 `AA_STABLE_*_OUTPUT_DIR` 环境变量 (path变量) 混在 secrets template 内 (P-0592) , 且 src 中并非全部真正读取 — 文档与代码可能drift   | 已修复 | 稳定性output目录变量堆叠在 secret 模板中, missing少按用途分组.  |
| P-0595 | .env.example | .env.example 共 377 行 (实际复核) , 其中真正必填密钥 (AA_API_JWT_SECRET/POSTGRES_PASSWORD/ANTHROPIC_API_KEY/OPENAI_API_KEY/MINIMAX_API_KEY) only 5 条, 剩余 ~370 行为可选/path/调优变量; 新人难辨别"必须填 vs 可省略"   | 已修复 | 示例file过长且未distinguish必填/可选, 导致configureentry噪声过高.  |
| P-0596 | helpers/fs.ts<br>helpers/fs.{js,d.ts,js.map} | helpers/fs.ts 不exists但 helpers/fs.{js,d.ts,js.map} exists, 导出 `createTempWorkspace/cleanupPath/createFile/createSymlink`, 内含 `symlinkSync` — 安全敏感工具以无源码形式exists仓库, 是可疑的"编译入仓masks"   | 已修复 | helpers 目录把敏感filesystem工具只以编译产物入库, 源码审计链断裂.  |
| P-0597 | - | helpers/ (顶层) only含 `fs.d.ts`, `fs.js`, `fs.js.map` (无 `fs.ts` 源) — P-0596 一级目录是"无源代码的编译产物", 反向 import 会bypass strict check   | 已复核关闭 | 当前 `helpers/` 已contains `fs.ts` 源file, 原recordbased on旧目录state.  |
| P-0598 | packs/test-pack/package.json<br>package.json<br>package.json:7 | packs/test-pack/package.json scripts `domain:lint`/`domain:validate` call `agent-platform` 二进制, 但 package.json:7 `bin` only声明 `aa`/`automatic-agent`, 无 `agent-platform` entry   | 已修复 | test-pack 脚本仍call不exists的 agent-platform 可execute名, 跟 root bin contract脱节.  |
| P-0599 | packs/test-pack/package.json:5<br>packs/test-pack/<br>packs/test-pack<br>package.json:5 | packs/test-pack/package.json:5 `"main": "dist/index.js"` 但 packs/test-pack/ 目录下无 dist/, local运行 `node packs/test-pack` 立即failure   | 已修复 | 包entry指向不exists的 dist/index.js, 示例 pack 元data与真实目录结构失配.  |
| P-0600 | - | packs/ contains 9 个示例 pack: `123-pack/`, `123test/`, `my-pack-id/`, `my.pack.id/`, `my_pack_id/`, `ops.core/`, `test-pack/`, `test.pack/`, `test_pack/`  --  naming混杂 (kebab/dot/snake), packId 与 directory 同名时 `123-pack` 数字开头violates npm naming规范   | 已修复 | 示例 pack fixture 长期放在仓库一级目录, 被误当成可发布包而不是naming/validation样本.  |
| P-0601 | packs/, helpers/<br>src/plugins/, src/sdk/harness-sdk/<br>AGENTS.md | packs/, helpers/ 顶级目录与 src/plugins/, src/sdk/harness-sdk/ naming空间exists概念overlap (pack vs plugin vs harness) 但 README/AGENTS.md 未给出distinguishgraph   | 已修复 | fixture 资产, runtime插件和 SDK entry过去共用含混naming, 文档没有明确语义边界.  |
| P-0602 | packs/{my-pack-id,<br>tests/fixtures)  | packs/{my-pack-id, my.pack.id, my_pack_id}/manifest.json 三个不同 packId 但全部 `domainId: "test-domain"`, `owner: "test-owner"`, 注册器无法消歧 (实际为naming风格的 case-study fixture, 但被放在仓库一级目录而非 tests/fixtures)    | 已修复 | pack naming案例本来只是testing夹具, 却被放在正式源码根path里, 掩盖了“同域同 owner onlyused fornaming归一化testing”的意graph.  |
| P-0603 | src/plugins/<br>tests/fixtures/packs<br>AGENTS.md | packs/ 目录与 src/plugins/ 目录, scale-ecosystem/marketplace/ 目录三层语义 (pack vs plugin vs marketplace) 在 README/AGENTS.md 无明确分工graph (与 P-0601 互证, 但本轮发现 packs 一级目录确为"示例 fixture"语义, 应迁入 tests/fixtures/packs 或 examples/)    | 已修复 | pack fixture, 内建 plugin 和 marketplace 服务原先missing少明确分层说明, 导致目录语义overlap.  |
| P-0604 | - | .husky/pre-commit only 34 字节 (`#!/usr/bin/env sh\nnpx lint-staged`), 无 typecheck/test 短路; 提交前不会捕获跨filetypeerror   | 已修复 | pre-commit 只跑 lint-staged, missing少跨filetypevalidation.  |
| P-0605 | - | .husky/pre-commit only含 `npx lint-staged` (2 行无 set -e, 无 husky v9 init shim) , husky v9 起官方推荐explicitlyentry, 无 init shim 在某些版本下会被忽略 (已在 P-0610 提及, 本轮验证 .husky 目录permissions/content确为 34 字节)    | 已复核关闭 | 当前 hook 已contains `set -eu` 和额外 typecheck, 原“34 字节两行脚本”描述已过时; husky 初始化脆弱性由 P-0610 handle.  |
| P-0606 | package.json | stryker.config.mjs exists但 package.json scripts 未将 mutation testing 纳入 ci:baseline, 变异testing形同摆设   | 已修复 | Root cause:  mutation 脚本exists, 但 baseline gate 没有消费它. 本轮把 `npm run test:mutation:critical` 纳入 `ci:baseline`, 让关键变异回归不再只是旁路脚本.  |
| P-0607 | package.json:255 | package.json:255 file总长 255 行, scripts 块占 ~120 行 (line 47-159 区段) , 单 manifest maintained成本高; missing `npm run-many`/Nx/Turbo 等编排   | 已复核关闭 | Root cause: 把脚本concentrated式编排当成independentmissing陷; 当前仓库明确采用单 package + npm scripts 作为Control Planeentry, 这属于maintained偏好而不是runtime bug.  |
| P-0608 | package.json:243 | package.json:243 把 `typescript` 放在 devDependencies 是正确, 但同包 `tsx` 也是 dev, 而 scripts:159 `aa:dev` directly `node --import tsx` 在 install 后无 tsx 时failure   | 已复核关闭 | `aa:dev` 已在 README 明确为 development-only 快捷脚本, dependency devDependencies 属预期, 不再视为生产missing陷.  |
| P-0609 | package.json:144<br>AGENTS.md | package.json:144 把 `test` 别名为 `ci:baseline`, 导致 `npm test` force跑 typecheck+coverage gate, 与 AGENTS.md "npm test 运行 --test-concurrency=12" 描述不符   | 已修复 | 脚本行为已变为 baseline gate, 但仓库文档仍按旧testing语义描述.  |
| P-0610 | package.json:53 | package.json:53 声明 `"prepare": "husky"`, 而 .husky/pre-commit only `npx lint-staged`, 未 chmod / missing少 husky v9 init shim 行 (一行 hook 在某些 husky 版本下被忽略)    | 已修复 | prepare/hook 初始化过于脆弱, missing少对 husky missing场景的容错.  |
| P-0611 | package.json:53<br>Dockerfile:23) 会因为 | package.json:53 `"prepare": "husky"` 在 `npm ci --omit=dev` 容器内 (Dockerfile:23) 会因为 husky 是 dev dep 而executefailure — 实际 `ignore-scripts` 行为dependency npm 版本, CI 易报错   | 已修复 | 生产安装execute `prepare` 时假定 husky 总可用.  |
| P-0612 | src/sdk/cli/aa.ts"<br>package.json:159<br>Dockerfile | package.json:159 `"aa:dev": "node --import tsx src/sdk/cli/aa.ts"` 在only安装 production dependency的环境 (如 Dockerfile runtime stage) 无法运行, 但 README 未标注 "dev only" (与 P-0608 互证)    | 已修复 | aa:dev dependency tsx 等开发dependency, 但文档未说明其只适used for开发环境.  |
| P-0613 | scripts/ci/audit-*<br>package.json | package.json `audit:repo-hygiene` serial 3 个 audit 子task (line 138) , failurearbitrary一项就阻断 `npm test`, 但 `audit:test-exclusions` 等脚本本身在 scripts/ci/audit-* 中位于无 shebang 的 .mjs (见 P-0466) , 跨平台executerisk   | 已修复 | repo hygiene dependency的审计脚本missing少可executeentry, 跨平台execute不稳.  |
| P-0614 | docs_zh/contracts<br>CHANGELOG.md:3<br>package.json:4 | CHANGELOG.md:3 当前 `[0.1.0] - 2026-05-14` 是仓库唯一已发布版本, 距今天 (2026-05-21) 一周未滚版本号; package.json:4 `"version": "0.1.0"` 与之synchronous, 但 git 历史/commits 数远超 0.1 的语义 (与 docs_zh/contracts 多次更新互证)    | 已修复 | Root cause:  changelog 只record已发版条目, 没有给未发布提交留位置, 导致所有subsequentchanges都像“忘记滚版本”. 本轮在 `CHANGELOG.md` 增加 `Unreleased`, 并在 `docs_zh/operations/release-versioning.md` 明确 `0.x` 预 GA 版本strategy.  |
| P-0615 | src/sdk/cli/aa.ts:script<br>package.json:217-228 | package.json:217-228 `dependencies` only 10 项 (OpenTelemetry/ioredis/postgres/ws/xml-crypto/zod) , 却 src 中exists `tsx`, `stryker`, `eslint`, `prettier` 等only dev references — 但 src/sdk/cli/aa.ts:script `aa:dev` 强dependency tsx (P-0608 互证) , 生产 install 后无 tsx, bin `aa` 在 dev 模式空载   | 已复核关闭 | 生产 `aa` 走构建产物, `aa:dev` only供源代码开发态uses; 把开发脚本dependency误当成runtimedependency导致了告警.  |
| P-0616 | package.json:144<br>AGENTS.md | package.json:144 `"test": "npm run ci:baseline"`, 而 `ci:baseline`(line 51) serial `typecheck && audit:repo-hygiene && test:raw && coverage:gate && validate:stable:compiled`, local `npm test` 实际跑full ~5 步, AGENTS.md 又写"`npm test` 跑 --test-concurrency=12", 三方文档 ↔ 脚本 ↔ 行为inconsistent (与 P-0609/P-0626 互证)    | 已修复 | 脚本行为已变为 baseline gate, 但仓库文档仍按旧testing语义描述.  |
| P-0617 | tsconfig.json:60-77 | tsconfig.json:60-77 整批 integration/* path被 exclude, CI 中无法 typecheck   | 已复核关闭 | Root cause: 把 `tsconfig.json` 的编辑器/辅助configure当成 CI typecheck 合同; CI 实际execute的是 `tsconfig.build.json` (src-only) , integration coverage由 layered test gate 承担.  |
| P-0618 | tsconfig.json:23<br>tsconfig.json:24-118) 大面积裁剪用例 | tsconfig.json:23 同时编译 src 和 tests, 但 exclude 列表已膨胀至 90+ 项 (tsconfig.json:24-118) 大面积裁剪用例   | 已复核关闭 | Root cause:  `tsconfig.json` 的 tests include 主要服务local IDE 导航, 而大块排除列表已由 `scripts/ci/audit-test-exclusions.mjs` explicitly审计; 这是 compile-scope manage, 不是失控drift.  |
| P-0619 | tests/unit/platform/execution/**/*.test.ts<br>tsconfig.json:32 | tsconfig.json:32 整目录排除 `tests/unit/platform/execution/**/*.test.ts`, 与 README 宣称"full回归"conflict   | 已复核关闭 | Root cause: 把“TypeScript static编译coverage”与“testingexecutecoverage”混成一个概念; 该目录虽不进 `tsc`, 仍由 `node --test/tsx` 分层运行.  |
| P-0620 | tsconfig.json:23-118 | tsconfig.json:23-118 exclude 列表 90+ 项与 tests/ 目录testing 4682 个file之间巨大裁剪面 (P-0618-0232) , CI 不会 typecheck integration path -- 回归coverage名实背离   | 已复核关闭 | Root cause: 把 compile scope, raw layered tests 和 exclusion audit 混成单一“coverage率”指标; 仓内目前分别用 `tsconfig.build.json`, `test:raw` 与 `audit:test-exclusions` 三道门禁控制.  |
| P-0621 | src/scale-ecosystem/billing/billing-service-async.ts:49 | 上述 5 处 require + `// eslint-disable-next-line @typescript-eslint/no-require-imports` (src/scale-ecosystem/billing/billing-service-async.ts:49 等) 固化反模式, 没有迁移到 `await import()`   | 已修复 | 这些 async wrapper 为compatibility早期synchronousimplementation而保留了 CommonJS `require` 惯性, 没有跟随仓库 ESM 化一起收敛.  |
| P-0622 | - | eslint.config.js 未configure `eslint-plugin-security`/`eslint-plugin-no-secrets`/`eslint-plugin-import` cycle detection, 与项目"循环 import"问题 (P-0141/0021/0024/0294 等数十处) 形成明显工具链missing位   | 已修复 | Root cause: 仓库把 lint 能力完全绑定到 ESLint plugin, 而当前环境没有为 cycle/secret 守门建立等价 CI check. 本轮保留 ESLint 基础规则, 同时新增 `scripts/ci/audit-lint-guardrails.mjs`, 对相对导入环和敏感literal量做 repo hygiene gate.  |
| P-0623 | src/**/*.ts",<br>scripts/**/*.mjs"]<br>tests/, tools/, helpers/, packs/, deploy/scripts, ui/<br>deploy/, helpers/, packs/ | eslint.config.js:9 `files: ["src/**/*.ts", "scripts/**/*.mjs"]`: tests/, tools/, helpers/, packs/, deploy/scripts, ui/ 均不在 root eslint range; ui/ 自带 eslint config 但 deploy/, helpers/, packs/ 完全无 lint   | 已修复 | root ESLint 只coverage src 和 scripts, 把 helpers, packs, tests 等目录排除在staticcheck之外.  |
| P-0624 | - | eslint.config.js:11-12 `"@typescript-eslint/no-explicit-any": "warn"` 未升 error; `no-floating-promises`, `no-misused-promises`, `require-await` 全部missing, 无法捕捉 P-0172-0351 的 `void this.x()` fire-and-forget   | 已修复 | 根级 ESLint 对 any 与 promise 误用约束过弱, fire-and-forget 类问题进不了default审查面.  |
| P-0625 | CONTRIBUTING.md<br>AGENTS.md | CONTRIBUTING.md 与 AGENTS.md 关于 commit 风格exists双重来源: AGENTS.md 描述短祈使句, CONTRIBUTING.md 未references, exists双轨指引   | 已修复 | AGENTS 与 CONTRIBUTING 对 commit 规范给出两套口径.  |
| P-0626 | scripts/run-layered-tests.mjs:173<br>AGENTS.md:6 | AGENTS.md:6 声称 `npm test` uses `--test-concurrency=12`, 但 scripts/run-layered-tests.mjs:173 由 LAYER_DEFINITIONS dynamic决定, 并非 12   | 已修复 | 脚本行为已变为 baseline gate, 但仓库文档仍按旧testing语义描述.  |
| P-0627 | src/platform/five-plane-interface/<br>src/platform<br>AGENTS.md:1-? | AGENTS.md:1-? 列出 `src/platform/five-plane-interface/` 等五个 plane 目录, partial与 src 实际平面naming (src/platform 下含 contracts/gateway/prompt 兄弟目录) 拼写drift, 且未在 README synchronous   | 已修复 | AGENTS 只描述五平面主path, 未coverage `src/core/` compatibility层与实际目录交叉.  |
| P-0628 | src/platform<br>AGENTS.md | AGENTS.md 列出的 plane 目录与 src/platform 实际目录交叉 (contracts/gateway/prompt 与 five-plane-* 同级, P-0627) , 新人按 AGENTS.md 找file会落空   | 已修复 | AGENTS 只描述五平面主path, 未coverage `src/core/` compatibility层与实际目录交叉.  |
| P-0629 | AGENTS.md | divisions 目录共 67 个 yaml + 32 个 json, 加上 32 个一级 division 目录 (academic-research/...) ; 其中 P-0459 已记 qa vs quality-assurance duplicate, 进一步: engineering_ops vs general_ops vs operations vs it-operations 同义性高, 未在 README/AGENTS.md 给出归并graph   | 已修复 | Root cause:  division 家族只有目录名, 没有机器can read或文档can read的 family map. 本轮新增 `config/quality/division-catalog.json` 和 `docs_zh/reference/division-catalog.md`, 并在 `README.md`, `AGENTS.md` explicitly说明 quality/operations 家族分工.  |
| P-0630 | docs_zh/architecture/00-platform-architecture.md<br>README.md:3 | README.md:3 references `docs_zh/architecture/00-platform-architecture.md`, 但 docs_en 等价file未在 README 注明, 双语门户missing   | 已修复 | README 只暴露中文架构entry, missing少英文entry.  |
| P-0631 | docs_zh/architecture/00-platform-architecture.md"<br>README.md:3 | README.md:3 强调 "v2.7 七层架构 docs_zh/architecture/00-platform-architecture.md", 但 src 实际平面naming为 five-plane-*, 平面数 (5) 与七层架构数 (7) 口径inconsistent   | 已修复 | README 未解释七层架构与五平面runtime的关系.  |
