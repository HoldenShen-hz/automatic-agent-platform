# Platforme Full Review A Issue Table

> 已将原始问题清单转换为表格，保留原文表述，便于筛选、复制与后续跟踪。

| ID | Location | Issue | Status | Root Cause |
| --- | --- | --- | --- | --- |
| P-0001 | src/platform/contracts/types/domain/task-types.ts:45 | `src/platform/contracts/types/domain/task-types.ts:45` `export type { ArtifactRef } from "../../executable-contracts/index.js"` ：contracts 子树内反向跨级 re-export。   | Fixed | `contracts/types/domain` 为兼容直接反向 re-export 可执行契约类型，打破了 contracts 分层边界。 |
| P-0002 | src/platform/contracts/types/domain/task-types.ts:45<br>package.json:14-46 | src/platform/contracts/types/domain/task-types.ts:45 `export type { ArtifactRef } from "../../executable-contracts/index.js"` (P-0001) 反向 re-export；package.json:14-46 `exports` 表中 `"./platform"` 一个入口同时暴露 `contracts/types/domain` 与 `executable-contracts` 两份 ArtifactRef → consumer typeof 比较为不同符号   | Fixed | 同一 `ArtifactRef` 通过两个发布路径外溢，消费者可见到重复符号边界。 |
| P-0003 | src/platform/contracts/types/domain/task-types.ts:45<br>package.json | src/platform/contracts/types/domain/task-types.ts:45 `export type { ArtifactRef } from "../../executable-contracts/index.js"` 在 contracts 子树内反向跨级 re-export（已 P-0001），与 package.json `exports` 中 `"./platform"` 单一入口潜在冲突，类型在两条路径下重复发布   | Fixed | 类型发布面把 canonical 定义和兼容出口混在一起，导致单入口下仍暴露双路径语义。 |
| P-0004 | src/platform/five-plane-interface/api/http-server/harness-runs-routes.ts:199 | 默认值降级：API harness 路由对 domainId 取 `\|\| "default"` 字符串 src/platform/five-plane-interface/api/http-server/harness-runs-routes.ts:199   | Fixed | 创建 harness run 时把缺失 domainId 回退到 "default"，掩盖调用方缺参。 |
| P-0005 | src/platform/five-plane-interface/api/oidc-oauth/jwt-utils.ts:29 | 错误吞噬：`} catch {` 静默忽略 OIDC/JWT 校验异常 src/platform/five-plane-interface/api/oidc-oauth/jwt-utils.ts:29   | Reviewed and Closed | 旧实现风险来自 `catch {}` 形态；当前 decode 失败已转换为 AuthError 并 fail-close。 |
| P-0006 | src/platform/five-plane-interface/api/api-auth-service.ts:119 | JWT 仅允许 HS256（未支持 RS/ES 非对称），HS256 共享密钥即签发即验签 src/platform/five-plane-interface/api/api-auth-service.ts:119   | Reviewed and Closed | 根因是 A 表把“未支持外部 IdP 非对称 JWT”误记成当前缺陷；现行 API auth 是自签自验的 service token，`allowedAlgorithms/iss/aud/jti/revocation` 已补齐，RS/ES 属协议扩展而非现行绕过面。 |
| P-0007 | src/platform/five-plane-interface/api/oidc-oauth-service.ts:465 | OIDC JWKS：缺 kid 时 fallback 到 `keys[0]` 隐式选键，存在密钥混淆风险 src/platform/five-plane-interface/api/oidc-oauth-service.ts:465   | Fixed | JWT 缺失 kid 时默认选首个 JWKS key，给多 key issuer 留下隐式选键空间。 |
| P-0008 | src/platform/five-plane-interface/api/api-auth-service.ts:120 | 弱 JWT secret 检查仅 8 字符最小长度，容易通过校验后仍被暴力 src/platform/five-plane-interface/api/api-auth-service.ts:120   | Fixed | JWT 密钥强度基线过低，只做最短长度校验，导致明显弱密钥仍可通过。 |
| P-0009 | src/platform/five-plane-interface/api/graphql-adapter-service.ts:262 | `Math.random()` 用于 GraphQL subscription operationId：src/platform/five-plane-interface/api/graphql-adapter-service.ts:262   | Fixed | subscription operationId 由 Math.random() 生成，可预测且碰撞控制弱。 |
| P-0010 | src/platform/five-plane-interface/webhook/index.ts:225 | webhook HMAC 校验未读取/校验 timestamp 或 nonce，签名可重放 src/platform/five-plane-interface/webhook/index.ts:225   | Fixed | webhook 重放防护只校验签名本身，未把重放检测绑定到请求指纹与幂等语义。 |
| P-0011 | src/platform/five-plane-interface/webhook/index.ts:225 | webhook 允许 `signingSecret ?? ""` 作为 HMAC 密钥，空 secret 不阻断 src/platform/five-plane-interface/webhook/index.ts:225   | Fixed | HMAC 验签对缺失 secret 走空串兜底，配置异常没有按 fail closed 处理。 |
| P-0012 | src/platform/five-plane-interface/api/api-auth-service.ts:121 | 弱 secret 黑名单仅枚举 5 个值（secret/password/changeme/default/jwtsecret），其它弱口令通过 src/platform/five-plane-interface/api/api-auth-service.ts:121   | Fixed | 弱密钥识别依赖少量枚举黑名单，未覆盖常见模式化口令。 |
| P-0013 | src/platform/five-plane-interface/api/graphql-adapter-service.ts:262 | src/platform/five-plane-interface/api/graphql-adapter-service.ts:262 `operationId = \`sub_${Date.now()}_${Math.random()...\`` 同问题：subscription 操作 ID 非加密随机   | Fixed | subscription operationId 再次复用了 Math.random()，追踪标识不稳定。 |
| P-0014 | src/platform/five-plane-interface/api/http-api-server.ts:189 | src/platform/five-plane-interface/api/http-api-server.ts:189 `void this.handleRequest(req, res)` 顶层 async 不捕获异常，依赖框架默认 500 — 失去 trace_id 上下文绑定   | Fixed | HTTP 入口直接 fire-and-forget 异步处理，缺少顶层 catch 包装与统一错误落点。 |
| P-0015 | src/platform/five-plane-interface/api/api-auth-service.ts:131 | JWT 验证将 `WEAK_JWT_SECRETS.has(secret.trim().toLowerCase())` 作仅小写比较，`Secret` 等大小写变体不在黑名单 src/platform/five-plane-interface/api/api-auth-service.ts:131   | Reviewed and Closed | 当前实现先 `trim().toLowerCase()` 再比对黑名单，大小写变体已被覆盖，原记录不成立。 |
| P-0016 | src/platform/five-plane-interface/api/middleware/idempotency-key-storage.ts:211,222,231,244,260,277,287,294,299 | SQL 字符串拼接：动态 `${this.tableName}` 拼到多条 DDL/DML src/platform/five-plane-interface/api/middleware/idempotency-key-storage.ts:211,222,231,244,260,277,287,294,299   | Reviewed and Closed | 原问题来自动态表名拼接；当前构造器已先经 validateSqlIdentifier() 白名单约束。 |
| P-0017 | src/platform/five-plane-interface/api/oidc-oauth-service.ts:256 | OIDC 验证流程允许 `iss/aud/nbf` 通过软错误码返回（如 jwt.untrusted_issuer / not_yet_valid）但实现位于同一 `validateToken` 路径，未独立强制 nbf 偏差容忍 src/platform/five-plane-interface/api/oidc-oauth-service.ts:256   | Reviewed and Closed | 旧审计基于历史校验路径；当前实现已对 exp/nbf/iat 做显式时钟偏差校验并 fail-close。 |
| P-0018 | src/platform/five-plane-interface/api/graphql-adapter-service.ts:413,419,435,441,519 | 自由文本 `throw new Error("Mutation type is not configured")` 等多处 src/platform/five-plane-interface/api/graphql-adapter-service.ts:413,419,435,441,519   | Fixed | GraphQL 适配层直接抛裸 Error，缺少统一错误码与类型分类。 |
| P-0019 | src/platform/five-plane-interface/api/http-api-server.ts:189 | src/platform/five-plane-interface/api/http-api-server.ts:189 `void this.handleRequest(request, response)` 顶层 fire-and-forget（P-0014）+ 无 timeout wrapper（http.Server.timeout 未在调用点显式设置），slow-loris 攻击面   | Fixed | HTTP 入口没有把超时和异步异常处理显式绑定到 server 级别，慢请求防护不完整。 |
| P-0020 | src/platform/five-plane-interface/api/facade-interfaces.ts:214,217,220,223 | 自由文本 `throw new Error("Incident service not configured")` 重复四处 src/platform/five-plane-interface/api/facade-interfaces.ts:214,217,220,223   | Fixed | facade 未配置分支复用裸 Error，缺少统一错误类型和错误码。 |
| P-0021 | src/platform/five-plane-interface/api/http-api-server.ts | src/platform/five-plane-interface/api/http-api-server.ts 全文 grep 无 `Strict-Transport-Security/X-Content-Type-Options/Content-Security-Policy/X-Frame-Options/Referrer-Policy` 任何安全 header（不依赖反代）；纯裸 HTTP 服务无安全头基线   | Reviewed and Closed | 历史版本缺安全头；当前 decorateResponseHeaders() 已统一注入 HSTS、CSP、nosniff 等基线。 |
| P-0022 | src/platform/five-plane-control-plane/config-center/profile-home.ts:30 | 默认值降级：`profileId?.trim() \|\| "default"` 落入硬编码 default 命名空间 src/platform/five-plane-control-plane/config-center/profile-home.ts:30   | Fixed | 未显式传 profileId 时一律落到 "default"，不同工作区共享同名命名空间。 |
| P-0023 | src/platform/five-plane-control-plane/iam/sandbox-policy.ts:361 | 错误吞噬：`} catch {` 静默忽略 src/platform/five-plane-control-plane/iam/sandbox-policy.ts:361   | Reviewed and Closed | 此处 catch 仅在 URL 解码失败时回退原字符串，不是策略绕过路径。 |
| P-0024 | src/platform/five-plane-control-plane/iam/outbound-url-policy.ts:136,160 | 错误吞噬：`} catch {` 静默忽略 src/platform/five-plane-control-plane/iam/outbound-url-policy.ts:136,160   | Reviewed and Closed | 两处 catch 已转为 ValidationError 或脱敏回退，不再把策略失败静默放行。 |
| P-0025 | src/platform/five-plane-control-plane/config-center/config-impact-analyzer.ts:206 | `Math.random()` 用于配置影响分析 ID：src/platform/five-plane-control-plane/config-center/config-impact-analyzer.ts:206   | Fixed | 配置影响分析 ID 用 Math.random() 拼接，唯一性和不可预测性不足。 |
| P-0026 | src/platform/five-plane-control-plane/iam/file-freshness.ts:103 | 弱算法可选：`takeFileSnapshot` digestAlgorithm 接受 md5 src/platform/five-plane-control-plane/iam/file-freshness.ts:103   | Fixed | 文件新鲜度守卫暴露 md5 选项，把弱摘要算法带进并发写保护链路。 |
| P-0027 | src/platform/five-plane-control-plane/iam/secret-management-service.ts:103 | 错误吞噬：`} catch {` 静默忽略 src/platform/five-plane-control-plane/iam/secret-management-service.ts:103   | Reviewed and Closed | 旧审计把 catch 视为吞错；当前旋转策略解析失败已抛 ValidationError。 |
| P-0028 | src/platform/five-plane-control-plane/cost-alert/cost-alert-service.ts:540 | src/platform/five-plane-control-plane/cost-alert/cost-alert-service.ts:540 emit `cost.threshold.exceeded`，与注册表 `cost:limit_reached` 命名不一致（点号 vs 冒号）   | Fixed | cost alert 实现沿用了旧点号事件名，未跟事件注册表统一到冒号风格。 |
| P-0029 | src/platform/five-plane-control-plane/iam/mfa-service.ts:309 | PII 落库：MFA 会话 identifier 注释承认未做 mask `store masked email/phone` 当前实为明文 src/platform/five-plane-control-plane/iam/mfa-service.ts:309   | Fixed | MFA 会话持久化直接保存原始 identifier，未落实注释声明的脱敏策略。 |
| P-0030 | src/platform/five-plane-control-plane/iam/file-freshness.ts:262 | 弱算法可选：`FileFreshnessTracker.snapshot` digestAlgorithm 接受 md5 src/platform/five-plane-control-plane/iam/file-freshness.ts:262   | Fixed | FileFreshnessTracker 同样把 md5 作为可选摘要算法暴露给写保护路径。 |
| P-0031 | src/platform/five-plane-control-plane/incident-control/acceptance-readiness-service.ts:119 | `JSON.parse` 无 try/catch：直接解析存储路径返回 src/platform/five-plane-control-plane/incident-control/acceptance-readiness-service.ts:119   | Fixed | 接受度报告读取 JSON 时解析失败直接吞成 null，把损坏状态伪装成“无数据”。 |
| P-0032 | src/platform/five-plane-control-plane/iam/sandbox-policy.ts:361（P-0023）与 | src/platform/five-plane-control-plane/iam/sandbox-policy.ts:361（P-0023）与 outbound-url-policy.ts:136,160（P-0024）属网络出向策略关键路径，吞错等于策略失效   | Reviewed and Closed | 旧问题建立在“出向策略关键路径吞错”前提；现实现未在策略判定处静默放行。 |
| P-0033 | src/platform/five-plane-control-plane/incident-control/auto-stop-loss-service.ts:789 | fire-and-forget Promise：`void this.executePlaybook(...)` src/platform/five-plane-control-plane/incident-control/auto-stop-loss-service.ts:789   | Reviewed and Closed | 调用点已有 rejected promise 处理与失败记录，审计把受控异步分支误判为无处理 fire-and-forget。 |
| P-0034 | src/platform/five-plane-control-plane/iam/data-classification-service.ts:293,841-846 | `regexCache = new Map<string, RegExp>()` 无大小上限；若分类规则动态生成可导致内存膨胀 src/platform/five-plane-control-plane/iam/data-classification-service.ts:293,841-846   | Fixed | 分类规则 regex 缓存只增不减，没有容量上限。 |
| P-0035 | src/platform/five-plane-control-plane/approval-center/quorum-calculator.ts:289,295 | 自由文本 `throw new Error("Vote must have a valid approverId/votedAt timestamp")` src/platform/five-plane-control-plane/approval-center/quorum-calculator.ts:289,295   | Fixed | 投票校验失败直接抛裸 Error，缺少 ValidationError 分类。 |
| P-0036 | src/platform/five-plane-control-plane/incident-control/release-pipeline-support.ts:215 | 命令构造：release-pipeline 使用 `spawn(request.command, request.args, ...)` 由请求驱动 src/platform/five-plane-control-plane/incident-control/release-pipeline-support.ts:215   | Fixed | release pipeline 把请求里的命令和参数直接下传给 `spawn`，缺少 step 级 allowlist。 |
| P-0037 | src/platform/five-plane-control-plane/incident-control/runbook-executor/runbook-executor.ts:263 | setTimeout 未 cleanup：`setTimeout(() => {...}, 10)` 异步 sleep 在 abort 时未清理 src/platform/five-plane-control-plane/incident-control/runbook-executor/runbook-executor.ts:263   | Fixed | runbook executor 用裸 `setTimeout` 模拟 sleep，没有保存句柄并在空闲时释放事件循环引用。 |
| P-0038 | src/platform/five-plane-control-plane/config-center/config-impact-analyzer.ts:206 | src/platform/five-plane-control-plane/config-center/config-impact-analyzer.ts:206 `analysisId = \`impact-${Date.now()}-${Math.random()...slice(2,8)\`` 6 字符 → ~31 bits，唯一性保证最弱   | Fixed | impact analysis ID 仍由时间戳加短随机串生成，碰撞与预测面偏大。 |
| P-0039 | src/platform/five-plane-control-plane/incident-control/deployment-execution-service.ts:165 | 命令构造：deployment-execution-service 使用 `spawn(request.command, request.args, ...)` 由请求驱动 src/platform/five-plane-control-plane/incident-control/deployment-execution-service.ts:165   | Fixed | deployment execution 同样直接执行请求内命令，缺少部署步骤到可执行命令的白名单收口。 |
| P-0040 | src/platform/five-plane-control-plane/config-center/startup-env-schema.ts:530 | src/platform/five-plane-control-plane/config-center/startup-env-schema.ts:530 `process.exit(1)` 直接退出（与 line 5 注释的"validation runs at process startup"一致），但 config-center 也被 SDK/CLI 引入；CLI 短命令意外触发 exit(1) 风险（与 ts 报错信号难区分）   | Fixed | 启动环境校验把“库函数校验失败”和“CLI 入口退出”耦合在同一个函数里。 |
| P-0041 | src/platform/five-plane-control-plane/mission/index.ts | src/platform/five-plane-control-plane/mission/index.ts ~42 KB —— index.ts 反模式（barrel 不应承载实现），违反"index.ts 只 re-export"约定   | Reviewed and Closed | 根因是审查把 mission 入口按“纯 barrel”要求处理；当前 `mission/index.ts` 本身就是该域的 canonical facade/service surface，并不存在仓库级 `index.ts 只能 re-export` 契约。 |
| P-0042 | src/platform/five-plane-control-plane/iam/policy-engine.ts:83-92,116 | `decisionCache` 仅在 `invalidate` 时整表 `clear`，无独立 TTL 扫描；TTL 5s 但只在命中时校验，缓存键空间随 decisionId×action×fingerprint 笛卡尔增长，未命中即变僵尸条目直至全清 src/platform/five-plane-control-plane/iam/policy-engine.ts:83-92,116   | Fixed | decisionCache 只在命中时做 TTL 校验，没有主动清理过期僵尸键。 |
| P-0043 | src/platform/five-plane-control-plane/iam/secret-management-service.ts | src/platform/five-plane-control-plane/iam/secret-management-service.ts ~37 KB 单文件持有 12+ 类（cache/rotation/audit/lease/...），违反 SRP；rotation timer (P-0389) + 静默吞错 (P-0027) 集中于此 → 风险耦合中心   | Reviewed and Closed | 根因是旧快照把“文件偏大”直接等同于独立缺陷；当前 secret provider、限流、支持类型和审计协作已拆到 support/provider 模块，剩余主要是可维护性债务，不再单列为运行时缺陷。 |
| P-0044 | src/platform/five-plane-control-plane/iam/jwt-utils.ts:29（OIDC/JWT | src/platform/five-plane-control-plane/iam/jwt-utils.ts:29（OIDC/JWT 校验异常吞噬，P-0005）位于身份核心路径，安全审计需按"必须 fail-closed"重写   | Reviewed and Closed | 旧审计针对 JWT 校验异常吞噬；当前 decode 失败已 fail-close。 |
| P-0045 | src/platform/five-plane-control-plane/config-center/config-audit-service.ts:226-251 | `SELECT * FROM config_audit_entries ORDER BY timestamp DESC` 无 LIMIT/分页，全表加载到内存后再 `.map(JSON.parse(...))`；审计表会随时间无界增长 src/platform/five-plane-control-plane/config-center/config-audit-service.ts:226-251   | Fixed | 配置审计查询默认按全表读取，缺少返回上限。 |
| P-0046 | src/platform/five-plane-control-plane/cost-alert/cost-alert-service.ts:540<br>src/scale-ecosystem/federation/federation-gateway.ts:581 | src/platform/five-plane-control-plane/cost-alert/cost-alert-service.ts:540 emit `cost.threshold.exceeded`（点号），src/scale-ecosystem/federation/federation-gateway.ts:581 emit `federation-event`（连字符），与注册表 `cost:limit_reached`（冒号）三种事件命名风格并存（P-0028/P-0332），缺统一 lint   | Fixed | 根因是 cost/federation 历史实现各自沿用局部事件命名习惯，没有持续的注册表对照门禁；相关 emitters 已统一，且新增 runtime event 审计脚本持续守门。 |
| P-0047 | src/platform/five-plane-control-plane/incident-control/release-pipeline-support.ts:215-238 | `spawn` 后只挂 `error/close`，stdout/stderr 仅 push 到无界 `stdoutChunks`/`stderrChunks` 数组，超大输出可 OOM；缺少输出截断/背压机制 src/platform/five-plane-control-plane/incident-control/release-pipeline-support.ts:215-238 与 deployment-execution-service.ts:165-194   | Fixed | 命令执行器把 stdout/stderr 累积到无界数组，没有输出大小上限。 |
| P-0048 | src/platform/five-plane-control-plane/iam/secret-management-service.ts:719 | src/platform/five-plane-control-plane/iam/secret-management-service.ts:719 `logger.info(phase === "initial" ? "secret.rotation.scheduled_initial" : "secret.rotation.scheduled", {...})` 等结构化日志事件名命名不在 contracts/event_registry 内（与 P-0480 互证，扩大到 secret 域）   | Fixed | secret rotation scheduler 新增了事件名，但注册表文档没有同步登记，形成 contract drift。 |
| P-0049 | src/platform/five-plane-control-plane/iam/vault-http-secret-provider.ts:179-180 | src/platform/five-plane-control-plane/iam/vault-http-secret-provider.ts:179-180 `if (loginData.auth?.client_token) { this._cachedToken = loginData.auth.client_token; }` 把 client_token 缓存到实例字段（无 TTL/再校验保障），rotation 期间易陈旧   | Fixed | Vault AppRole 登录后缓存 token 时没有最小 TTL 保护，陈旧凭据可能长期滞留。 |
| P-0050 | src/platform/five-plane-control-plane/compliance/<br>src/platform/compliance/ | `src/platform/five-plane-control-plane/compliance/`(crypto-shredding/erasure 等) 与顶级 `src/platform/compliance/`(`compliance-bootstrap.ts`、`lineage/`、`encryption/`) 主题双份。   | Reviewed and Closed | 根因是审查把“同主题”误判为“重复实现”；当前顶级 `platform/compliance` 承担跨平面 bootstrap/能力汇总，而 `five-plane-control-plane/compliance` 是控制面的具体治理服务，分层职责不同。 |
| P-0051 | src/platform/five-plane-control-plane/config-center/config-versioning-service.ts:797-799 | `cloneConfigContent` 用 `JSON.parse(JSON.stringify(content))` 深拷贝完整配置内容，热路径每次写入版本快照都触发 O(N) 序列化+反序列化，且丢失 Date/Map/undefined 语义；该项目其他位置已有 `structuredClone`，此处明显落后 src/platform/five-plane-control-plane/config-center/config-versioning-service.ts:797-799   | Fixed | 配置版本快照仍使用 JSON 序列化深拷贝，热路径成本高且会丢失数据语义。 |
| P-0052 | src/platform/five-plane-control-plane/iam/secret-management-service.ts:53-82 | `SecretResolutionRateLimiter.requests: Map<string, number[]>` 无 LRU/过期清理；每个 callerId 永驻 Map，且 `check` 内 `.filter` 在每次调用 O(window 内 timestamp 数) 重建数组并 `set` 回去 src/platform/five-plane-control-plane/iam/secret-management-service.ts:53-82   | Fixed | secret 解析限流器只追加 callerId 时间戳，不会回收长期不用的调用方状态。 |
| P-0053 | src/platform/five-plane-control-plane/iam/secret-management-service.ts:67-81 | read-modify-write 无锁：先 `requests.get(callerId)`，filter 后 `requests.set(...)`；纯 JS 单线程下虽不会撕裂，但 await 边界穿插的调用方易出现 TOCTOU（已观察 callerId 计数与新 push 间漏窗）src/platform/five-plane-control-plane/iam/secret-management-service.ts:67-81   | Reviewed and Closed | `check()` 全程同步执行，没有 `await` 边界；原 TOCTOU 描述不符合当前实现，但已顺带补上状态封装与回收。 |
| P-0054 | src/platform/five-plane-control-plane/iam/secret-management-service.ts:749 | `setInterval(() => runRotationSweep("interval", rotationInterval), ...)` 内部异步 sweep 无 in-flight 标志，长 sweep 可能与下个 tick 并行启动多份 rotation 流，触发 secret rotate 重入 src/platform/five-plane-control-plane/iam/secret-management-service.ts:749   | Fixed | 轮转调度器按固定周期触发，但没有防重入保护，长耗时 sweep 可与下个 tick 重叠。 |
| P-0055 | src/platform/five-plane-orchestration/planner/plan-graph-normalizer.ts:46 | `@ts-expect-error` 出现在生产路径 src/platform/five-plane-orchestration/planner/plan-graph-normalizer.ts:46   | Fixed | 生产归一化路径依赖 @ts-expect-error 掩盖类型问题。 |
| P-0056 | src/platform/five-plane-orchestration/routing/intake-router-model.ts:657-658 | `matchRule` 每次调用都 `new RegExp(...)`；intake 路由是请求级 hot path src/platform/five-plane-orchestration/routing/intake-router-model.ts:657-658   | Fixed | intake 路由匹配在热路径重复编译正则，未做缓存。 |
| P-0057 | src/platform/five-plane-orchestration/agent-delegation/delegation-manager.service.ts<br>AGENTS.md | src/platform/five-plane-orchestration/agent-delegation/delegation-manager.service.ts ~41 KB；命名 `*.service.ts` 加点号是少见的混合风格 (kebab + 中缀)，与仓库"kebab-case .ts"约定 (AGENTS.md) 不符   | Reviewed and Closed | 根因是审查把文件后缀约定写得过严；仓库当前同时存在 `plugin-executor.service.ts` 等同类命名，`*.service.ts` 已是兼容可接受模式，AGENTS 仅约束基础文件名保持 kebab-case。 |
| P-0058 | src/platform/five-plane-orchestration/harness/guardrails/guardrail-vibration-breaker.ts:28 | secret 比较使用 `===`：`signal.signature === "proceed"` 决定放行 src/platform/five-plane-orchestration/harness/guardrails/guardrail-vibration-breaker.ts:28   | Reviewed and Closed | 比较的是控制流常量 proceed，不是秘密值对比，不涉及常量时间比较风险。 |
| P-0059 | src/platform/five-plane-orchestration/hitl/hitl-operator-console-service.ts:65-68 | `for (const channel of channels) { await this.notifier({...}) }` 串行通知多 channel，无 Promise.all src/platform/five-plane-orchestration/hitl/hitl-operator-console-service.ts:65-68   | Fixed | 多 channel 通知串行 await，未并发化。 |
| P-0060 | src/platform/five-plane-orchestration/improve-rollout/rollout/rollout-scheduler.ts:126-128 | `for (const input of inputs) { decisions.push(await this.advance(input)); }` 串行 await，缺少 Promise.all src/platform/five-plane-orchestration/improve-rollout/rollout/rollout-scheduler.ts:126-128   | Fixed | 批量 rollout advance 串行等待，未并发执行。 |
| P-0061 | src/platform/five-plane-orchestration/harness/context-assembler.ts:135-140 | `applyRedaction` 每次调用都 `for of redactPatterns` 中 `new RegExp(...)`；redaction 在每条上下文 entry 都触发，O(entries × patterns) 次 RegExp 编译 src/platform/five-plane-orchestration/harness/context-assembler.ts:135-140   | Fixed | context redaction 对每条记录重复编译正则，缺少缓存。 |
| P-0062 | src/platform/five-plane-orchestration/oapeflir/handoff-model.ts:86 | `JSON.parse(JSON.stringify(handoff))` 深拷贝 AgentHandoff，handoff payload 通常含较大上下文；O(N) 序列化在每次切换都执行，且与 `handoff-serializer.ts:16` 已用的 `structuredClone` 不一致 src/platform/five-plane-orchestration/oapeflir/handoff-model.ts:86   | Fixed | handoff 深拷贝仍使用 JSON 序列化，未与 structuredClone 对齐。 |
| P-0063 | src/platform/five-plane-orchestration/agent-delegation/delegation-manager.service.ts:826-831 | `findRepositoryRecordsByStatuses` 对 6 个 active 状态发 6 次独立 `findByStatus` 查询，典型 N 查询，应单次 `WHERE status IN (...)`；调用点 line 429/607/662/809 在 hot path 上均触发 src/platform/five-plane-orchestration/agent-delegation/delegation-manager.service.ts:826-831   | Reviewed and Closed | 根因是 delegation repository 当前按 status 分桶暴露查询契约，A 表把小基数 active-status 聚合直接上升为通用 N+1 缺陷；这是仓储能力边界，不是 correctness 缺口。 |
| P-0064 | src/platform/five-plane-orchestration/agent-delegation/delegation-manager.service.ts:429-435,607,662,809 | 取回每个 status 数组后再 `for record of records` + `record.delegationChain.includes(agentId)`，相当于客户端内存过滤；应改为带 `delegation_chain @> [agentId]` 索引的 SQL/JSON 查询 src/platform/five-plane-orchestration/agent-delegation/delegation-manager.service.ts:429-435,607,662,809   | Reviewed and Closed | 根因是审查把 bounded active-set 的内存筛选误记成索引缺陷；现行仓储没有 JSON containment 查询契约，调用侧只是在通用记录集上做二次过滤。 |
| P-0065 | src/platform/five-plane-orchestration/oapeflir/oapeflir-loop-core.ts<br>src/platform/five-plane-execution/oapeflir/（仅 | src/platform/five-plane-orchestration/oapeflir/oapeflir-loop-core.ts 单文件 1004 行（P-0129），与 src/platform/five-plane-execution/oapeflir/（仅 2 文件壳，P-0103）形成"实现集中在 orchestration、声明位于 execution"的巨型断层   | Reviewed and Closed | 目录清单式审查把 execution/oapeflir 的 runtime bridge 与 orchestration 的 canonical 闭环实现混作重复；执行面 README 已明确该目录只保留运行桥接边界。 |
| P-0066 | src/platform/five-plane-orchestration/escalation/index.ts:15 | `src/platform/five-plane-orchestration/escalation/index.ts:15` import `../../../ops-maturity/emergency/resume-protocol/index.js`；`five-plane-orchestration/learn/{learning-feedback-orchestration-service,llm-improvement-generation-service,strategy-learning-service,failure-pattern-miner}.ts:1-3` 与 `evaluator/evaluator-service.ts:16` 都 import `../../../scale-ecosystem/feedback-loop/collector/feedback-model.js`：平面反向依赖 ops-maturity 与 scale-ecosystem。   | Reviewed and Closed | 根因是把 orchestration 的横切学习/应急依赖误判成 accidental reverse import；这些模块消费的是反馈闭环与恢复协议的 canonical 横切能力，不是在 orchestration 内再造第二套 plane。 |
| P-0067 | - | `Math.random()` 用于操作/事件 ID（非加密但作为追踪标识可被预测）：execution-worker-handshake-service-async.ts:776   | Reviewed and Closed | 审计引用的 Math.random 位置已不在当前实现中，属于陈旧结论。 |
| P-0068 | src/platform/five-plane-execution/execution-engine/multi-step-utils.ts:57 | 错误吞噬：`} catch {` 静默忽略输入解析异常 src/platform/five-plane-execution/execution-engine/multi-step-utils.ts:57   | Reviewed and Closed | catch 分支用于宽容解析回退，不是静默吞掉业务失败。 |
| P-0069 | src/platform/five-plane-execution/compensation-manager.ts:224-256： | (N-0301) src/platform/five-plane-execution/compensation-manager.ts:224-256：`executeCompensationSteps` 无逐步持久化，中途崩溃后重做导致非幂等副作用   | Reviewed and Closed | 根因是 A 表把主审计里已降级为幂等增强项的残余风险继续当作当前 correctness 缺陷挂账；现行补偿主链已有人审与补偿治理收口。 |
| P-0070 | src/platform/five-plane-execution/queue-metrics/index.ts:84,93 | 重试无上限/退避：`failureReasons.push` / 队列 push 后无显式 backoff 控制（仅依赖外层）src/platform/five-plane-execution/queue-metrics/index.ts:84,93   | Reviewed and Closed | 根因是外层调度退避策略被误要求在 metrics helper 内再次重复实现；当前队列指标组件并不是重试策略权威边界。 |
| P-0071 | src/platform/five-plane-execution/tool-executor/web-fetch.ts:301 | src/platform/five-plane-execution/tool-executor/web-fetch.ts:301 `while (true)` 流式抓取，没有显式跳出上限/最大字节守卫，依赖外部 abort（容易在恶意 server 下空转）   | Fixed | `web-fetch` 只依赖超时和字节上限，没有为高频小分片响应设置独立 chunk 上限。 |
| P-0072 | src/platform/five-plane-execution/distributed-lock/sqlite-lock-adapter.ts:88,138 | 类型断言绕过：`lockKey as unknown as { lockName ...}` 二处 src/platform/five-plane-execution/distributed-lock/sqlite-lock-adapter.ts:88,138   | Fixed | SQLite lock 适配器为兼容旧形参直接双重断言，绕过了编译期形状分辨。 |
| P-0073 | src/platform/five-plane-execution/queue/redis-queue-adapter.ts:544,551,602,665,694 | 隐式时间：`Date.now()` 直接作为 redis ZSET 评分 / job id src/platform/five-plane-execution/queue/redis-queue-adapter.ts:544,551,602,665,694   | Reviewed and Closed | 根因是把 Unix epoch 数值误记成“隐式时间”；这里写入的是绝对毫秒时间戳用于排序和去重，不涉及时区序列化歧义。 |
| P-0074 | src/platform/five-plane-execution/ha/leader-election-service.ts:710 | fire-and-forget Promise：`void this.attemptElection();` 选主流程无错误日志 src/platform/five-plane-execution/ha/leader-election-service.ts:710   | Fixed | 选主重试 fire-and-forget 未附带 catch 和日志。 |
| P-0075 | src/platform/five-plane-execution/execution-engine/multi-step-supervisor.ts:365 | 类型断言绕过：`stepData as unknown as Record<string, unknown>` src/platform/five-plane-execution/execution-engine/multi-step-supervisor.ts:365   | Fixed | supervisor 把未知 stepData 直接双重断言成 Record，绕过运行时校验。 |
| P-0076 | src/platform/five-plane-execution/execution-engine/call-governance.ts:306-320 | `historyByKey` 仅每个 key 内部裁剪到 1000，`Map` 本身（key 数量）无界；高基数 key 集会累积 src/platform/five-plane-execution/execution-engine/call-governance.ts:306-320   | Fixed | 调用治理只限制单 key 历史长度，未限制 Map 键总量。 |
| P-0077 | src/platform/five-plane-execution/worker-pool/worker-drain-protocol.ts:97-100： | (N-0297) src/platform/five-plane-execution/worker-pool/worker-drain-protocol.ts:97-100：`expiresAt<now` 的 lease 被 drain 视为 `completed`，丢失 in-flight 任务计数   | Reviewed and Closed | 根因是把 drain 视角的“无需继续等待 expired lease”和 truth 层 in-flight 计数混为一谈；过期 lease 的真实回收由 lease reclaimer 负责。 |
| P-0078 | src/platform/five-plane-execution/plugin-executor/scoped-external-access-sandbox.ts:303 | sandbox 外部访问 `fetch(targetUrl, options)` 未在此处展示对私网/元数据 IP 的拒绝逻辑 src/platform/five-plane-execution/plugin-executor/scoped-external-access-sandbox.ts:303   | Reviewed and Closed | 根因是审查时未继续追到上游 URL 策略与受限访问沙箱；当前风险描述缺少对现有外联治理链的完整上下文，因此按复核关闭。 |
| P-0079 | src/platform/five-plane-execution/ha/replay-worker.ts:126,129 | 自由文本 `throw new Error("ReplayWorker refuses replay policies that allow real side effects")` src/platform/five-plane-execution/ha/replay-worker.ts:126,129   | Fixed | replay worker 对策略拒绝直接抛裸 Error，缺少运行时错误类型和错误码。 |
| P-0080 | src/platform/five-plane-execution/execution-engine/multi-step-supervisor.ts:365 | src/platform/five-plane-execution/execution-engine/multi-step-supervisor.ts:365 `stepData as unknown as Record<string, unknown>` 把 step 体强转再写入 supervisor 路径，类型守卫缺失   | Fixed | step 数据缺少显式类型守卫，直接强转进入 supervisor 路径。 |
| P-0081 | src/platform/five-plane-execution/worker-pool/worker-drain-protocol.ts:305-309： | (N-0299) src/platform/five-plane-execution/worker-pool/worker-drain-protocol.ts:305-309：`requestFromProgress` 假设 `leaseId === nodeRunId`，drain progress 投影与 truth 主键耦合   | Reviewed and Closed | 根因是兼容 projection 字段被误读成 truth 主键；`requestFromProgress` 里的 leaseId/nodeRunId 映射只服务 drain 进度投影。 |
| P-0082 | src/platform/five-plane-execution/distributed-lock/redis-lock-adapter.ts | src/platform/five-plane-execution/distributed-lock/redis-lock-adapter.ts EVAL Lua 共 3 处分散写法（line 165/185/211，P-0089 已记 EVALSHA 缺失），且无 `script_load` 启动期注册，每次 EVAL 重传脚本字节   | Reviewed and Closed | 根因是把热路径脚本缓存优化误判成当前 correctness 缺陷；现有 `EVAL` 路径属于性能 backlog，功能正确性不受影响。 |
| P-0083 | src/platform/five-plane-execution/queue/ticket-priority-queue.ts:35-62：基于数组的优先级队列 | (N-0311) src/platform/five-plane-execution/queue/ticket-priority-queue.ts:35-62：基于数组的优先级队列 `enqueue` 用 `findIndex+splice`，`dequeue` 同样线性扫描，单操作 O(n) 而非 binary heap O(log n)   | Fixed | 优先队列实现停留在线性数组结构，未采用堆结构。 |
| P-0084 | src/platform/five-plane-execution/tool-executor/code-diagnostics-service.ts:312 | 命令构造：`spawn("python3", ["-m", "py_compile", filePath], ...)` filePath 来源未显式做白名单/解析校验 src/platform/five-plane-execution/tool-executor/code-diagnostics-service.ts:312   | Fixed | Python 诊断执行前只在上层做工作区过滤，没有在 `spawn` 落点再次把路径收敛到 workspace 相对路径。 |
| P-0085 | src/platform/five-plane-execution/distributed-lock/sqlite-lock-adapter.ts:88,138 | src/platform/five-plane-execution/distributed-lock/sqlite-lock-adapter.ts:88,138 通过 `as unknown as { lockName: string; ownerId: string }` 兼容旧调用形参，没在编译期分辨，同一函数走两套数据形态   | Fixed | 旧版兼容入口把字符串形参与 legacy 对象形参混在同一签名里，用断言硬转。 |
| P-0086 | src/platform/five-plane-execution/worker-pool/worker-drain-protocol.ts:118-144： | (N-0298) src/platform/five-plane-execution/worker-pool/worker-drain-protocol.ts:118-144：`advancePhase` 不检查活动 lease 即可 DRAIN→QUIESCE；`isDrainComplete` 仅看 `phase=TERMINATE`   | Reviewed and Closed | 根因是把 phase 推进和 drain 完成判定混成同一语义；当前协议里 phase 可前移，但真正完成仍由 `TERMINATE` 判定。 |
| P-0087 | src/platform/five-plane-execution/dispatcher/execution-dispatch-service.ts:637-666： | (N-0309) src/platform/five-plane-execution/dispatcher/execution-dispatch-service.ts:637-666：`rankWorkerEvaluations` 四遍 `.filter().map().filter().map()` 流水线，命中 fallback 时 O(W²)   | Fixed | worker 排序热路径先做多轮数组流水线，再重复回查候选 worker，导致不必要的二次扫描。 |
| P-0088 | src/platform/five-plane-execution/recovery/repair-pipeline.ts:67,68,93,101,109,117,125,229,238,246,254 | 隐式时区：批量 `new Date().toISOString()` 未注入 clock 端口（依赖系统时间）src/platform/five-plane-execution/recovery/repair-pipeline.ts:67,68,93,101,109,117,125,229,238,246,254   | Reviewed and Closed | 审计把 UTC 序列化误判成时区格式错误，实际这里不是隐式时区 bug。 |
| P-0089 | src/platform/five-plane-execution/distributed-lock/redis-lock-adapter.ts:49,165,185,211 | src/platform/five-plane-execution/distributed-lock/redis-lock-adapter.ts:49,165,185,211 调用 `redis.eval(script, ...)` 执行 Lua 脚本，脚本通过 `EVAL` 而非 `EVALSHA`，每次都重传脚本字节，热路径上无缓存优化   | Reviewed and Closed | 根因是把 `EVALSHA` 缓存优化误判成主链缺陷；当前 redis Lua 执行的功能正确性不依赖脚本缓存。 |
| P-0090 | src/platform/five-plane-execution/tool-executor/semantic-repo-map-service.ts:423-430：构建反向引用图 | (N-0313) src/platform/five-plane-execution/tool-executor/semantic-repo-map-service.ts:423-430：构建反向引用图 `for file of files.values()` 嵌套 `for ref of references` → O(F·R)，仓库级数千文件每次重建都跑   | Reviewed and Closed | 根因是离线/按需 repo map 重建被误判成执行热路径；主审计已把它降为性能优化项。 |
| P-0091 | src/platform/five-plane-execution/distributed-lock/{redis,sqlite,pg-advisory}-lock-adapter.ts：所有 | (N-0283) src/platform/five-plane-execution/distributed-lock/{redis,sqlite,pg-advisory}-lock-adapter.ts：所有 lock adapter 均无 heartbeat/auto-renew，且 release 未 clearTimeout，靠调用方手动续期/释放   | Reviewed and Closed | 根因是把“模块不内建后台续期 timer”的显式设计误判成缺陷；当前 lock 生命周期本就由调用方 renew/reacquire 控制。 |
| P-0092 | src/platform/five-plane-execution/distributed-lock/redis-lock-adapter.ts:162-166： | (N-0279) src/platform/five-plane-execution/distributed-lock/redis-lock-adapter.ts:162-166：`releaseAsync` Lua 仅校验 owner，无 fencing-token，已被强抢回的同 owner 延迟 release 也会成功   | Reviewed and Closed | 根因是把 acquire/write side 的 fencing 约束直接外推到 release side；主审计已确认 release 端无 fencing-token 强校验属于后续增强项。 |
| P-0093 | src/platform/five-plane-execution/ha/lease-reclaimer-service.ts:281-290,70： | (N-0289) src/platform/five-plane-execution/ha/lease-reclaimer-service.ts:281-290,70：`scheduleNextReclaim` 链式 setTimeout，stop 路径仅设 `running=false/disposed=true`，进行中的 setTimeout 永不被 clear   | Fixed | lease reclaimer 的调度链只保留下一次 timer 句柄，未把正在执行的周期和 reschedule 逻辑绑定到 stop/dispose 语义。 |
| P-0094 | src/platform/five-plane-execution/startup/graceful-shutdown.ts:89-99：shutdown | (N-0294) src/platform/five-plane-execution/startup/graceful-shutdown.ts:89-99：shutdown 期间 `addHandler` 仅 warn 即丢弃，懒加载 DI 服务的 teardown 静默失效   | Fixed | graceful shutdown 把“已进入关闭流程后新增 handler”当成 warn 级别旁路，没有 fail-fast。 |
| P-0095 | src/platform/five-plane-execution/dispatcher/execution-dispatch-service.ts:578-583、lease/types.ts:3-5：心跳过期阈值 | (N-0277) src/platform/five-plane-execution/dispatcher/execution-dispatch-service.ts:578-583、lease/types.ts:3-5：心跳过期阈值 30s 与 lease TTL DEFAULT=10s 不一致，10–30s 窗口内同一执行可能被两个 worker 同时持有（双执行）   | Reviewed and Closed | 根因是旧审计基于“stale heartbeat 仍可参与选主”的前提；主审计已确认无效 heartbeat 已 fail-close，TTL 差值仅保留为保守调度窗口。 |
| P-0096 | src/platform/five-plane-execution/distributed-lock/sqlite-lock-adapter.ts:88： | (N-0281) src/platform/five-plane-execution/distributed-lock/sqlite-lock-adapter.ts:88：`release` 仅按 `(lock_key,owner)` 删除，无 fencing token / lease id 校验，旧 owner 重现可抹掉新 lease   | Reviewed and Closed | 根因是把本地单进程 sqlite lock adapter 当成跨节点分布式锁来审查；主审计已将 fencing-token 校验归类为远端分布式 adapter 增强项。 |
| P-0097 | src/platform/five-plane-execution/budget-allocator.ts:279,761,771 | 隐式时区：`new Date(Date.now()).toISOString()` 形式重复使用 src/platform/five-plane-execution/budget-allocator.ts:279,761,771   | Reviewed and Closed | 这里输出的是 UTC ISO 字符串，审计把时钟依赖误写成时区错误。 |
| P-0098 | src/platform/five-plane-execution/lease/execution-lease-service-async.ts:212-226： | (N-0287) src/platform/five-plane-execution/lease/execution-lease-service-async.ts:212-226：`renewLeaseSync` `lease.status !== "active"` 检查重复，第二块不可达死代码   | Reviewed and Closed | 根因是冗余防御分支被误登记成运行时缺陷；主审计已确认这是整洁性问题，不改变行为。 |
| P-0099 | src/platform/five-plane-execution/startup/process-error-handlers.ts:110-113 | src/platform/five-plane-execution/startup/process-error-handlers.ts:110-113 `setTimeout(... process.exit(1), 60_000)` 终极兜底，写"Ultimate fallback"，但 timer 未 `unref()`（行内核查无 unref），handler 自身可能阻止其它退出路径   | Reviewed and Closed | 当前代码中的兜底退出 timer 已 unref，审计基于旧版本结论。 |
| P-0100 | src/platform/five-plane-execution/queue/ticket-priority-queue.ts:17-73 | 标称 "PriorityQueue" 实则数组：`enqueue` 用 `findIndex`+`splice`(O(n))，`dequeue` 同样 `findIndex`+`splice`(O(n))；高入队/出队场景退化为 O(n²)，应改用堆 src/platform/five-plane-execution/queue/ticket-priority-queue.ts:17-73   | Fixed | enqueue 和 dequeue 的算法选型仍是数组线性扫描，复杂度退化。 |
| P-0101 | src/platform/five-plane-execution/startup/graceful-shutdown.ts:125-127： | (N-0292) src/platform/five-plane-execution/startup/graceful-shutdown.ts:125-127：`handleSignal` 先 `unregisterSignalHandlers()` 再跑 handler；shutdown 期间第二次 SIGINT 落到内核默认行为，绕过强退 timer   | Fixed | 信号处理在真正完成 shutdown 前就解绑 listener，导致第二个信号跳出受控退出路径。 |
| P-0102 | src/platform/five-plane-execution/tool-executor/tool-output-sanitizer.ts:355-356： | (N-0312) src/platform/five-plane-execution/tool-executor/tool-output-sanitizer.ts:355-356：`for definition of SECRET_PATTERN_DEFINITIONS` 中 `new RegExp(definition.source, definition.flags)` 每次输出消毒重新编译，每次工具调用都跑   | Fixed | 输出脱敏每次调用都会重新构造 secret 正则。 |
| P-0103 | src/platform/five-plane-execution/oapeflir/<br>src/platform/five-plane-orchestration/oapeflir/ | `src/platform/five-plane-execution/oapeflir/`(2 文件壳) 与 `src/platform/five-plane-orchestration/oapeflir/`(20+ 文件) 同名概念在两个平面下并存，目录名跨平面冲突。   | Reviewed and Closed | 根因是旧快照没把执行面 README 边界算进来；当前 `five-plane-execution/oapeflir/README.md` 已明确这里只保留 runtime plan execution bridge，编排闭环 canonical 仍在 orchestration 面。 |
| P-0104 | src/platform/five-plane-execution/dispatcher.ts:1-7<br>src/platform/five-plane-execution/dispatcher/index.ts | `src/platform/five-plane-execution/dispatcher.ts:1-7` 是兼容垫片，与 `src/platform/five-plane-execution/dispatcher/index.ts` 同名导出，文件 vs 同名目录并存。   | Reviewed and Closed | 根因是审查把兼容 shim 当成重复实现；`dispatcher.ts` 现在已显式标注 legacy compatibility shim，canonical surface 在 `dispatcher/index.ts`。 |
| P-0105 | src/platform/five-plane-execution/lease/execution-lease-service-async.ts:285-298,417-446： | (N-0286) src/platform/five-plane-execution/lease/execution-lease-service-async.ts:285-298,417-446：`releaseLease` 在 `lease.expiresAt <= occurredAt` 时返回 `lease_expired`，owner 不能主动释放已过期 lease，需等 `reclaimExpiredLeases`   | Reviewed and Closed | 根因是 A 表把 N-0286 已复核收口的 expired-lease 幂等语义重复展开；过期 lease 的权威回收本就由 `reclaimExpiredLeases` 负责，避免 owner 误删新租约。 |
| P-0106 | src/platform/five-plane-execution/queue/ | `src/platform/five-plane-execution/queue/`(7 文件)与同级 `five-plane-execution/queue-metrics/`(只 `index.ts`) 单复数/拆分不一致；`five-plane-execution/state-transition/` 与 `five-plane-execution/runtime-state-machine.ts` 主题划分不清。   | Reviewed and Closed | 根因是审查把“命名不对称”误当成职责冲突；当前 `queue/` 承载调度与存取核心，`queue-metrics/` 是独立观测面，`runtime-state-machine.ts` 也与 `state-transition/` 的局部能力边界不同。 |
| P-0107 | src/platform/five-plane-execution/tool-executor/skill-creator-service.ts:604-614： | (N-0295) src/platform/five-plane-execution/tool-executor/skill-creator-service.ts:604-614：`assertTargetPathAllowed` 在 `existsSync(skillPath)` 与 `realpathSync` 之间存在 TOCTOU   | Reviewed and Closed | 根因是 A 表把 N-0295 已复核收口的文件系统层残余竞争重复挂账；当前路径校验仍以 `realpathSync` 锚定受控目录，未发现仓内可利用写入路径。 |
| P-0108 | src/platform/five-plane-execution/tool-executor/web-fetch.ts:301<br>src/platform/five-plane-control-plane/iam/outbound-url-policy.ts | src/platform/five-plane-execution/tool-executor/web-fetch.ts:301 `while (true) { ... }` 流式抓取无最大字节守卫与最大循环次数（P-0071），结合 src/platform/five-plane-control-plane/iam/outbound-url-policy.ts SSRF 防护成对，安全门只在入口校验 URL，下游 body 大小未 cap   | Fixed | SSRF 入口只限制目标 URL，响应体读取链路缺少独立的流分片上限，给长尾流式响应留下资源消耗面。 |
| P-0109 | src/platform/five-plane-execution/recovery/patch-bundle.ts:139 | `matchPath` 每次都 `new RegExp(\`^${regex}$\`).test(path)`，patch bundle 大量路径校验时重复编译 src/platform/five-plane-execution/recovery/patch-bundle.ts:139   | Fixed | patch bundle 路径匹配每次动态编译 glob 正则，未复用缓存。 |
| P-0110 | src/platform/five-plane-execution/dispatcher/execution-dispatch-service.ts:482：dispatch | (N-0296) src/platform/five-plane-execution/dispatcher/execution-dispatch-service.ts:482：dispatch worker snapshot 一次性加载，循环中 `runningExecutionIds` 不重读，并发超额分配后由 lease 失败回滚兜底（产生派发回滚泥潭）   | Reviewed and Closed | 根因是 A 表把 N-0296 已复核收口的事务外候选快照再次展开；worker 视图只用于排序，最终并发闸门仍在 lease/claim 事务。 |
| P-0111 | src/platform/five-plane-execution/startup/process-error-handlers.ts:110-113 | src/platform/five-plane-execution/startup/process-error-handlers.ts:110-113 终极兜底 `setTimeout(... process.exit(1), 60_000)` 未 `unref()`（已 P-0099 一句记，进一步：该 timer 本身就是为 uncaughtException handler 兜底，若不 unref 反而阻止正常 exit code 触发 — 自相矛盾）   | Reviewed and Closed | 与 P-0099 相同，当前代码已 unref，这一条是重复且基于旧代码。 |
| P-0112 | src/platform/five-plane-execution/dispatcher/execution-dispatch-service.ts:561-631： | (N-0308) src/platform/five-plane-execution/dispatcher/execution-dispatch-service.ts:561-631：`evaluateWorkersForTicket` 的 `requiredCapabilities.filter(c => !worker.capabilities.includes(c))` 让单 ticket 评估变 O(W·C·K)，未将 capabilities 预转 Set   | Fixed | worker 能力集在每次评估时反复线性 `includes`，没有局部索引化。 |
| P-0113 | src/platform/five-plane-execution/recovery/runtime-recovery-decision-service.ts:286： | (N-0304) src/platform/five-plane-execution/recovery/runtime-recovery-decision-service.ts:286：`recordDecision` 把 `executionRecord?.traceId ?? null` 写入 EventRecord，下游按 traceId 关联 trace 时丢失关联键   | Reviewed and Closed | 根因是 A 表把 N-0304 已复核收口的异常路径语义重复展开；有 executionRecord 时 traceId 仍会沿用，缺 execution 的记录写 null 是受控降级。 |
| P-0114 | src/platform/five-plane-execution/budget-allocator.ts:481-516： | (N-0273) src/platform/five-plane-execution/budget-allocator.ts:481-516：`persistRelease(settlement) → transition → untrack → 计算 ledger → persistLedger` 五步未在同一事务，settlement 已写但 ledger CAS 前崩溃 → 漏冲销   | Reviewed and Closed | 根因是 A 表把 N-0273 已复核收口的 release 事务边界说明重复挂账；当前主链仍优先走 atomic repository 路径，同步 fallback 只是 legacy baseline。 |
| P-0115 | src/platform/five-plane-execution/budget-allocator.ts:384-435：settle | (N-0180) src/platform/five-plane-execution/budget-allocator.ts:384-435：settle 同步 fallback 路径 `persistSettlement → transition → untrack → 构造新 ledger → persistLedger` 非原子，settlement 持久化后崩溃留下"已结算未冲销"   | Reviewed and Closed | 根因是 A 表把 N-0180 已复核收口的 legacy fallback 顺序写重复展开；当前预算主链已优先使用原子仓储实现。 |
| P-0116 | src/platform/five-plane-execution/startup/startup-consistency-checker.ts:18 | `src/platform/five-plane-execution/startup/startup-consistency-checker.ts:18` 与 `dispatcher/execution-deviation-detector.ts:2` 直接 reach `../../five-plane-orchestration/oapeflir/workflow/minimal-workflow.js`、`oapeflir/types/index.js`，执行面访问编排面私有。   | Reviewed and Closed | 根因是 startup/deviation checker 本身就是 OAPEFLIR execution bridge，需要读取最小 workflow/types 契约；A 表把桥接缝依赖误判成私有面越界。 |
| P-0117 | src/platform/five-plane-execution/dispatcher/execution-dispatch-service.ts:193-208,462-479： | (N-0330) src/platform/five-plane-execution/dispatcher/execution-dispatch-service.ts:193-208,462-479：`dispatchNext` 评估阶段使用事务外快照（availableSlots/activeLeaseCount），事务内 lease+claim 需多次回滚，造成 dispatch 抖动   | Reviewed and Closed | 根因是 A 表把 N-0330 已复核收口的事务外候选排序重复挂账；dispatch correctness 仍由事务内 lease acquire/claim 保证。 |
| P-0118 | src/platform/five-plane-execution/startup/graceful-shutdown.ts:70-72：默认 | (N-0293) src/platform/five-plane-execution/startup/graceful-shutdown.ts:70-72：默认 `exitHandler` 只设 `process.exitCode`，不调 `process.exit`；hot-reload setInterval / runtimeMetricsRegistry / signal listener 残留 keep-alive event loop   | Fixed | 默认 signal 退出逻辑只设置 exitCode，没有在受控关闭完成后真正终止进程。 |
| P-0119 | src/platform/five-plane-execution/ha/coordinator-load-balancing-service.ts:256-261 | `evaluations.filter().map(...)` 内部对每个 eligible item 调 `records.find((r) => r.coordinatorId === item.coordinatorId)`，O(n²) 数组扫描；应预先建 `Map<coordinatorId, record>` src/platform/five-plane-execution/ha/coordinator-load-balancing-service.ts:256-261   | Fixed | 负载均衡在映射阶段反复 records.find，缺少 coordinatorId 索引。 |
| P-0120 | src/platform/five-plane-execution/recovery/exception-recovery-config-loader.ts:15<br>src/platform/five-plane-control-plane/cost-alert/cost-alert-config-loader.ts:49 | 模块级 `cachedConfigs = new Map<string, CostAlertConfig>()` 仅靠手动 `clear()`，无 TTL；同模式见 `src/platform/five-plane-execution/recovery/exception-recovery-config-loader.ts:15` src/platform/five-plane-control-plane/cost-alert/cost-alert-config-loader.ts:49   | Fixed | 配置 loader 使用常驻 Map 缓存，仅靠手动 clear，没有 TTL 失效。 |
| P-0121 | src/platform/five-plane-execution/ha/leader-election-service.ts:589-593,626-628 | 续租/心跳 `setInterval` 中均直接同步发起 IO，无 in-flight 防护；网络抖动时多次重叠续租可造成 fencing 错误 src/platform/five-plane-execution/ha/leader-election-service.ts:589-593,626-628   | Fixed | leader election 把续租和心跳直接挂在 `setInterval` 上，没有阻止上一轮 IO 未完成时重入下一轮。 |
| P-0122 | src/platform/five-plane-execution/tool-executor/skill-execution-support.ts:371-399 | `defaultGitHeadResolver` 通过 `child.stdout?.removeAllListeners()` 清理，但 `child` 自身的 `error/close/exit` 事件未注册 listener；spawn 失败（ENOENT 等）会触发 `Error` 事件无监听，导致进程级 unhandled error src/platform/five-plane-execution/tool-executor/skill-execution-support.ts:371-399   | Fixed | git HEAD 解析辅助只覆盖了 `error` 和 `close`，没有把 `exit` 纳入同一 settle 路径。 |
| P-0123 | src/platform/five-plane-execution/worker-pool/execution-worker-writeback-service.ts:391： | (N-0300) src/platform/five-plane-execution/worker-pool/execution-worker-writeback-service.ts:391：`existingAgentExecution` 事务外读，进入事务后未重读 → TOCTOU，仅靠 consume/update 兜底   | Reviewed and Closed | 根因是 A 表把 N-0300 已复核收口的事务外快速拒绝路径重复展开；权威冲突处理仍在事务内 consume/update。 |
| P-0124 | src/platform/five-plane-execution/recovery/runtime-recovery-decision-service.ts:175-176： | (N-0302) src/platform/five-plane-execution/recovery/runtime-recovery-decision-service.ts:175-176：`db.transaction` 当前为同步契约但代码注释承认未来要改 async；`AuthoritativeSqlDatabase.transaction` 一旦改异步将静默丢事务原子性   | Reviewed and Closed | 根因是 A 表把 N-0302 已复核收口的未来演进注释误当成当前缺陷；现行 `transaction` 仍是同步契约。 |
| P-0125 | src/platform/five-plane-execution/run-termination-cleanup.ts:137-349、lease/execution-lease-service.ts:583-604： | (N-0278) src/platform/five-plane-execution/run-termination-cleanup.ts:137-349、lease/execution-lease-service.ts:583-604：`reclaimExpiredLeases / reclaimActiveLeaseInternal` 不触发 cleanup，worker crash 路径下 budget_reservation/secret/timer/hitl_wait 等资源无人清理   | Reviewed and Closed | 根因是 A 表把 N-0278 已复核收口的 crash reclaim residual risk 重复挂账；资源清理由 drain/termination 协议集中承担。 |
| P-0126 | src/platform/five-plane-execution/budget-allocator.ts:222-238,424-429,511-516： | (N-0275) src/platform/five-plane-execution/budget-allocator.ts:222-238,424-429,511-516：`reserve/settle/release` 对 `hierarchyLedgers` 逐条 `reserveBudgetHardCap+persistLedger`，主 ledger 与 hierarchy 不在同事务，部分写入后异常使父子 ledger 错位   | Reviewed and Closed | 根因是 A 表把 N-0275 已复核收口的 hierarchy ledger 存储能力约束重复展开；当前仍通过同一 BudgetAllocator 主链串行更新。 |
| P-0127 | src/platform/five-plane-execution/recovery/runtime-recovery-decision-service.ts:344-351： | (N-0303) src/platform/five-plane-execution/recovery/runtime-recovery-decision-service.ts:344-351：`moveExecutionToDeadLetter` 内 `new MemoryService(store).recordFailureMemory` 在事务回调里执行，但 MemoryService 是否复用同一 db handle 未通过类型保证（仅约定）   | Reviewed and Closed | 根因是 A 表把 N-0303 已复核收口的仓储句柄约定重复挂账；当前 failure memory 与 DLQ 仍共享同一 task store 构造。 |
| P-0128 | src/platform/five-plane-execution/queue/redis-queue-adapter.ts:543-607,559-565： | (N-0284) src/platform/five-plane-execution/queue/redis-queue-adapter.ts:543-607,559-565：`dequeueAsync` 在校验 status 前先 `hincrby attempts`，并发 dequeue 竞态会对另一个 consumer 拿到的 job 也增加 attempts   | Reviewed and Closed | 根因是 A 表把 N-0284 已复核收口的 adapter 级竞态说明重复展开；consumer 并发仍由 worker-pool/dispatcher 主链控制。 |
| P-0129 | src/platform/five-plane-execution/oapeflir/<br>src/platform/five-plane-orchestration/oapeflir/（P-0103）：除目录冲突外 | src/platform/five-plane-execution/oapeflir/ vs src/platform/five-plane-orchestration/oapeflir/（P-0103）：除目录冲突外，前者 `oapeflir-loop-core.ts` 单文件 1004 行，是仓库最大 .ts 之一，单一文件聚合 plan/run/decide/learn/observe 闭环职责，违反 SRP   | Reviewed and Closed | OAPEFLIR 体量与拆分节奏已在架构参考文档作为持续治理债登记；当前剩余大文件属于已知可维护性问题，不再单列为未识别运行时缺陷。 |
| P-0130 | src/platform/five-plane-execution/tool-executor/skill-execution-service.ts:64 | `cache = new Map<string, CachedSkillExecutionEntry>()`：cache 大小由 `skill-execution-cache-methods.ts:261-266` LRU 裁剪，但其 TTL 仅按 entry `expiresAt` 在 `getCachedEntry` 命中时延迟检查（line 221-230），过期 entry 在无访问时常驻 Map，无后台扫描清除 src/platform/five-plane-execution/tool-executor/skill-execution-service.ts:64   | Reviewed and Closed | 根因是 skill execution cache 采用 `TTL + LRU` 的惰性淘汰策略；A 表把“无后台扫描”直接等同泄漏，但当前 Map 仍受容量上限裁剪。 |
| P-0131 | src/platform/five-plane-execution/dispatcher/execution-dispatch-service.ts:893-904： | (N-0310) src/platform/five-plane-execution/dispatcher/execution-dispatch-service.ts:893-904：`getCandidateWorker` 在 store 没提供 `getWorker` 时退化为 `listWorkers().find(...)` 每次 O(W)，上层 ranking map 重复调用 → O(W²) per ticket   | Reviewed and Closed | 根因是 A 表把 N-0310 已复核收口的测试替身 fallback 路径重复展开；生产 store 提供 `getWorker`，O(W) 扫描不是主链表现。 |
| P-0132 | src/platform/five-plane-execution/lease/execution-lease-service-async.ts:155,511： | (N-0288) src/platform/five-plane-execution/lease/execution-lease-service-async.ts:155,511：`handoverLease` 用 `previousLease.fencingToken+1`，与 `acquireLease` 用 `getLatestFencingToken(executionId)+1` 路径不同，并发 handover+acquire 可产出相等 token   | Reviewed and Closed | 根因是 A 表把 N-0288 已复核收口的 fencing-token 理论竞争重复挂账；实际 handover/acquire 仍受 active lease 状态与仓储 CAS 串行化。 |
| P-0133 | src/platform/five-plane-execution/ha/wal-checkpoint-service.ts:599-602,292,330,411,422,515： | (N-0316) src/platform/five-plane-execution/ha/wal-checkpoint-service.ts:599-602,292,330,411,422,515：`walEntriesWritten++/checkpointsCreated++/entriesReplayed++` 在跨 await 异步路径修改同一 metrics 对象，回放与正常写入并发计数不一致   | Reviewed and Closed | 根因是 A 表把 N-0316 已复核收口的观测计数误差重复挂账；这些 metrics 不参与 truth/recovery 决策。 |
| P-0134 | src/platform/five-plane-execution/distributed-lock/distributed-lock-types.ts:5-12、redis-lock-adapter.ts:117-135、pg-advisory-lock-adapter.ts:65-75： | (N-0264) src/platform/five-plane-execution/distributed-lock/distributed-lock-types.ts:5-12、redis-lock-adapter.ts:117-135、pg-advisory-lock-adapter.ts:65-75：`DistributedLockAdapter.acquire` 同步签名在远端 adapter 全部 `throw LockingError("sync_*_not_supported")`，多态契约名存实亡，调用方无类型层提示   | Reviewed and Closed | 根因是 A 表把 N-0264 已复核收口的同步/异步锁契约差异重复展开；远端 adapter fail-close 抛 `sync_*_not_supported` 是显式边界。 |
| P-0135 | src/platform/five-plane-execution/worker-pool/execution-worker-handshake-service.ts:684-755： | (N-0315) src/platform/five-plane-execution/worker-pool/execution-worker-handshake-service.ts:684-755：`refreshWorkerSnapshot` 基于读到的 `runningExecutionsJson` `mergeExecutionIds` 后整字段写回，并发 claim/heartbeat 各自基于陈旧 set 计算 union → lost update   | Reviewed and Closed | 根因是 A 表把 N-0315 已复核收口的 worker 单-owner 假设下 residual risk 重复挂账；未发现多 claim 同 worker 的真实破坏路径。 |
| P-0136 | src/platform/five-plane-execution/hibernation/wake-engine.ts:269<br>src/platform/five-plane-execution/ha/leader-election-service.ts:215 | 隐式时区：`new Date(Date.now() + ttl).toISOString()` 计算过期时间使用进程系统时区/时钟 src/platform/five-plane-execution/hibernation/wake-engine.ts:269；重复模式 src/platform/five-plane-execution/ha/leader-election-service.ts:215；ha-coordinator-service-async.ts:117,214   | Reviewed and Closed | 这里同样是 UTC ISO 序列化，不存在审计描述的隐式时区格式错误。 |
| P-0137 | src/platform/five-plane-execution/ha/cross-region-event-replication-service.ts:337-362,376,386-388： | (N-0319) src/platform/five-plane-execution/ha/cross-region-event-replication-service.ts:337-362,376,386-388：`some+push` 非原子 TOCTOU，相同 plan 在并行重试路径中可被重复入队   | Reviewed and Closed | 根因是 A 表把 N-0319 已复核收口的幂等队列优化项重复展开；重复入队仍由 event id/status 幂等处理兜底。 |
| P-0138 | src/platform/five-plane-execution/distributed-lock/redis-lock-adapter.ts:49 | src/platform/five-plane-execution/distributed-lock/redis-lock-adapter.ts:49 `eval(script: string, numberOfKeys: number, ...args: string[]): Promise<unknown>` 接口签名定 `args: string[]`，但 line 185 调用 `String(additionalMs), String(fencingToken), now`：`now` 未 `String()` 包裹，类型与运行时不一致（取决于上游 now 类型）   | Reviewed and Closed | 根因是审查基于旧快照误报类型不一致；当前 `now` 在调用点已是 ISO 字符串，`eval(...args: string[])` 契约未被破坏。 |
| P-0139 | src/platform/five-plane-execution/worker-pool/execution-worker-handshake-service.ts:552-572,574-662： | (N-0314) src/platform/five-plane-execution/worker-pool/execution-worker-handshake-service.ts:552-572,574-662：`recordHeartbeat` 先在事务外 `renewLease`，再单独 `db.transaction` 写 worker 快照/心跳/agent_execution；非原子提交 → lease 已续期但 heartbeat_snapshot 缺失   | Reviewed and Closed | 根因是 A 表把 N-0314 已复核收口的 lease-first 可用性策略重复展开；后续 reconciliation 会补证据，不会放宽执行授权。 |
| P-0140 | src/platform/five-plane-execution/dispatcher/index.ts:633-638,644,593-594： | (N-0318) src/platform/five-plane-execution/dispatcher/index.ts:633-638,644,593-594：`getToolRegistry` 模块单例；`executeToolCall("send-message")` `agentState.requestHistory.push(message)` 紧跟 `await this.executeSpawnedAgent(agentState)`，跨 await 后再读写同一 agentState，并发 send-message 同 agent 时 history 顺序被穿插   | Reviewed and Closed | 根因是 A 表把 N-0318 已复核收口的同-agent 会话串行假设重复挂账；tool registry 单例本身只是工具目录缓存。 |
| P-0141 | src/platform/five-plane-execution/execution-engine/multi-step-orchestration.ts:32-34<br>src/platform/five-plane-orchestration/oapeflir/oapeflir-loop-core.ts:69 | `src/platform/five-plane-execution/execution-engine/multi-step-orchestration.ts:32-34` 与 `multi-step-oapeflir-plan.ts:1-7` import `../../five-plane-orchestration/routing/*`、`oapeflir/types/*`、`oapeflir/workflow/*`；同时 `src/platform/five-plane-orchestration/oapeflir/oapeflir-loop-core.ts:69` 与 `oapeflir-loop-support.ts:69` import `../../five-plane-execution/budget-allocator.js`，execution↔orchestration 双向循环。   | Reviewed and Closed | 根因是 OAPEFLIR runtime plan 执行本来就是 orchestration model 与 execution budget/dispatch 的桥接缝；A 表把已知架构耦合重复挂成新增缺陷。 |
| P-0142 | src/platform/five-plane-execution/dispatcher/execution-dispatch-service.ts | 28 处 `*-async.ts` 与同名 `*.ts` 双套实现（如 `src/platform/five-plane-execution/dispatcher/execution-dispatch-service.ts` vs `execution-dispatch-service-async.ts`、`five-plane-state-evidence/events/durable-event-bus.ts` vs `durable-event-bus-async.ts`、`five-plane-execution/lease/execution-lease-service.ts` vs `execution-lease-service-async.ts`）。   | Reviewed and Closed | sync/async 镜像是架构参考文档 B1 已登记的双后端兼容债；当前镜像面通过统一包装和测试维持行为一致，属已登记技术债而非漏修 defect。 |
| P-0143 | src/platform/five-plane-state-evidence/truth/sqlite/sqlite-database.ts:914 | SQL 字符串拼接：`PRAGMA table_info("${tableName}")` 仅以引号兜底 src/platform/five-plane-state-evidence/truth/sqlite/sqlite-database.ts:914   | Reviewed and Closed | 根因是旧审计漏看了 `tableHasColumn()` 前的标识符正则校验；当前 `tableName` 已 fail-close，`PRAGMA table_info` 不是外部可注入入口。 |
| P-0144 | src/platform/five-plane-state-evidence/events/typed-event-bus.ts:379,424 | `JSON.parse` 无 try/catch：解析 typed-event-bus 事件 payload src/platform/five-plane-state-evidence/events/typed-event-bus.ts:379,424   | Fixed | typed event payload 直接 `JSON.parse`，异常路径未转换为受控 `ValidationError`。 |
| P-0145 | src/platform/five-plane-state-evidence/knowledge/semantic-vector-store.ts:155 | src/platform/five-plane-state-evidence/knowledge/semantic-vector-store.ts:155 同模式，使用 `${this.qualifiedTableName()}` 直接拼 SQL，缺标识符引用器   | Fixed | 向量表名虽经校验，但缺统一引用器，SQL 标识符仍以裸字符串散落拼接。 |
| P-0146 | src/platform/five-plane-state-evidence/truth/sqlite/authoritative-task-store-legacy-compat.ts | src/platform/five-plane-state-evidence/truth/sqlite/authoritative-task-store-legacy-compat.ts ~52 KB（P-0158），命名含 `legacy-compat` 但仍为生产路径主调（P-0158 已记），文件名语义对消费方误导   | Reviewed and Closed | 当前文件是 delegating base 继承的抽象兼容合同层，不是直接承载业务逻辑的主实现；`legacy-compat` 描述的是 API bridge 语义而非“永不参与生产编译”。 |
| P-0147 | src/platform/five-plane-state-evidence/knowledge/semantic-vector-validation.ts:258 | fire-and-forget Promise：`.catch(() => 0)` 隐藏向量库探测错误 src/platform/five-plane-state-evidence/knowledge/semantic-vector-validation.ts:258   | Fixed | 向量探测清理路径把异常直接 catch 成 0，导致失败上下文丢失。 |
| P-0148 | src/platform/five-plane-state-evidence/events/projections/dispatch-projection.ts:237,259,276 | 类型断言绕过：`event.payloadJson as string` 多次 src/platform/five-plane-state-evidence/events/projections/dispatch-projection.ts:237,259,276   | Reviewed and Closed | 根因是 projection 入口消费的就是事件仓储序列化后的 JSON 字符串，局部 `as string` 是边界解包，不是未校验外部输入。 |
| P-0149 | src/platform/five-plane-state-evidence/knowledge/semantic-vector-validation.ts:160,256 | SQL 字符串拼接：`${schema}.${tableName}` 拼入 DELETE/INSERT src/platform/five-plane-state-evidence/knowledge/semantic-vector-validation.ts:160,256   | Fixed | 校验流程自行拼接 `schema.table`，未复用统一的已引用限定表名。 |
| P-0150 | src/platform/five-plane-state-evidence/truth/runtime-truth-repository.ts:714 | 类型断言绕过：`command.aggregate as unknown as Record<string, unknown>` src/platform/five-plane-state-evidence/truth/runtime-truth-repository.ts:714   | Fixed | 事实状态初始化路径用双重类型断言摊平 aggregate，绕过了按聚合类型收窄。 |
| P-0151 | src/platform/five-plane-state-evidence/truth/sqlite/authoritative-task-store-delegating-lifecycle.ts:54,150,176 | 类型断言绕过：`args as unknown as [...]` 三处 src/platform/five-plane-state-evidence/truth/sqlite/authoritative-task-store-delegating-lifecycle.ts:54,150,176   | Fixed | 兼容 legacy 形参时直接 `as unknown as`，没有显式参数守卫和收窄步骤。 |
| P-0152 | src/platform/five-plane-state-evidence/knowledge/indexing/embedding-provider.ts:108 | SSRF 面：embedding provider 直接 `fetch(${baseUrl}/v1/embeddings)`，无主机白名单 src/platform/five-plane-state-evidence/knowledge/indexing/embedding-provider.ts:108   | Reviewed and Closed | 根因是把 operator 配置的 embedding provider endpoint 与用户可控 SSRF 输入混同；这里的 `baseUrl` 仅来自部署环境，不是运行时请求面透传。 |
| P-0153 | src/platform/five-plane-state-evidence/truth/sql/phase_1a_schema_sql_part-{1..5}.ts | 命名风格漂移：`src/platform/five-plane-state-evidence/truth/sql/phase_1a_schema_sql_part-{1..5}.ts` 与 `truth/postgres/phase_1a_schema_ddl_part-{1..5}.ts` 使用 snake_case + 数字混合，与仓库 kebab-case 约定相悖。   | Reviewed and Closed | `authoritative-schema.ts` 与 `pg-schema-support.ts` 明确按 phase 装配这些 schema artifact；这组文件名服务于分阶段 DDL 装配，不是面向通用业务模块的命名约定。 |
| P-0154 | src/platform/five-plane-state-evidence/events/durable-event-bus-async.ts:47 | fire-and-forget Promise：`void Promise.resolve(handler(event)).catch(() => undefined)` src/platform/five-plane-state-evidence/events/durable-event-bus-async.ts:47   | Reviewed and Closed | Promise.resolve(handler).catch 是刻意把同步和异步 handler 统一成受控 rejected promise。 |
| P-0155 | src/platform/five-plane-state-evidence/truth/runtime-truth-repository.ts:714 | src/platform/five-plane-state-evidence/truth/runtime-truth-repository.ts:714 `...(command.aggregate as unknown as Record<string, unknown>)` 在事实表写入路径强转，潜在脏数据落库风险   | Fixed | 聚合写入落库前靠强转摊平对象，跳过了按聚合类型构建受控字段集。 |
| P-0156 | src/platform/five-plane-state-evidence/events/durable-event-bus.ts | src/platform/five-plane-state-evidence/events/durable-event-bus.ts 总 ~40 KB（P-0621 已记规模），单文件包含 enqueue/deliver/retry/circuit-breaker/batch flush 多重职责（与 oapeflir-loop-core.ts 同列"上帝文件"）   | Reviewed and Closed | `durable-event-bus.ts` 仍是 events 子域的 canonical 聚合入口；体量问题已在架构参考文档中记录为模块化治理债，不再单列为未识别功能缺陷。 |
| P-0157 | src/platform/five-plane-state-evidence/knowledge/semantic-vector-store.ts:155,206,260,281 | SQL 字符串拼接：`INSERT INTO ${this.qualifiedTableName()}` / `FROM ${this.qualifiedTableName()}` src/platform/five-plane-state-evidence/knowledge/semantic-vector-store.ts:155,206,260,281   | Fixed | 表身份在多处重复以模板字符串展开，缺单一的已引用限定名出口。 |
| P-0158 | src/platform/five-plane-state-evidence/truth/sqlite/authoritative-task-store-legacy-compat.ts | src/platform/five-plane-state-evidence/truth/sqlite/authoritative-task-store-legacy-compat.ts 长 ~52KB，命名前缀 `legacy-compat`，但该文件仍是主路径 import 目标（grep `legacy-compat` 在 src 内 多处引用），与"legacy 兼容层"语义错位   | Reviewed and Closed | 当前引用关系显示它被多个 delegating 子类当作 legacy API 合同基类复用；问题本质是兼容层命名造成误读，不是实现角色与运行路径失真。 |
| P-0159 | src/platform/five-plane-state-evidence/truth/sql/phase_1a_schema_sql_part-{1..5}.ts | src/platform/five-plane-state-evidence/truth/sql/phase_1a_schema_sql_part-{1..5}.ts 与 truth/postgres/phase_1a_schema_ddl_part-{1..5}.ts 命名 `phase_1a_schema_*_part-N.ts` (P-0153) 暴露分裂的 DDL 编排顺序为代码常量 → schema 修订必须改源代码而非 migrations   | Reviewed and Closed | phase 分片是权威 bootstrap schema 的源码装配方式；迁移仍然是增量演进主通道，分片命名本身不等于“必须放弃 migrations”。 |
| P-0160 | src/platform/five-plane-state-evidence/events/durable-event-bus.ts:710,766,904,995 | fire-and-forget Promise：`void this.enqueueDelivery(consumerId, true)` 多处无 catch src/platform/five-plane-state-evidence/events/durable-event-bus.ts:710,766,904,995   | Fixed | 关键投递调度把 enqueue Promise 直接 fire-and-forget，失败只能依赖隐式 rejected promise。 |
| P-0161 | src/platform/five-plane-state-evidence/truth/session-dual-storage.ts:116-164 | `appendSessionEvent` 以同步 `openSync/appendFileSync/fdatasyncSync` 在请求路径执行两次磁盘 fsync + 双文件写，阻塞事件循环；session 事件高频时对 latency 影响显著 src/platform/five-plane-state-evidence/truth/session-dual-storage.ts:116-164   | Reviewed and Closed | 根因是 dual-storage session append 明确以同步 fsync 换取 crash-safe 证据落盘；A 表把 durability tradeoff 直接记成通用性能缺陷。 |
| P-0162 | src/platform/five-plane-state-evidence/truth/sqlite/repositories/dispatch-repository.ts:125,139,149 | src/platform/five-plane-state-evidence/truth/sqlite/repositories/dispatch-repository.ts:125,139,149 多处 `SELECT ${EXECUTION_COLS_NO_PREFIX/EXECUTION_COLS} FROM ...`，列清单常量化但 ORDER BY/LIMIT 仍硬编码，缺统一 query-builder   | Reviewed and Closed | 根因是 query-builder 缺失属于风格和可维护性建议，不是当前 SQL 正确性或注入缺陷。 |
| P-0163 | src/platform/five-plane-state-evidence/truth/sqlite/repositories/operations-repository.ts:567：tenant | (N-0541) src/platform/five-plane-state-evidence/truth/sqlite/repositories/operations-repository.ts:567：tenant scoped vs global SQL 通过字符串拼接切换 → 同一逻辑产生两套 prepared plan，绕过 better-sqlite3 statement cache   | Reviewed and Closed | 根因是 A 表把 N-0541 已复核收口的 deliberate plan split 重复展开；tenant-scoped 与 global SQL 分支是有意保留的语义差异。 |
| P-0164 | src/platform/five-plane-state-evidence/truth/sqlite/authoritative-task-store-delegating-lifecycle.ts:54,150,176 | src/platform/five-plane-state-evidence/truth/sqlite/authoritative-task-store-delegating-lifecycle.ts:54,150,176 三处 `as unknown as [string, ...]`/`as unknown as { lockName: string }`，绕过 strict 类型直接对齐 legacy 形参；类型契约弱化   | Fixed | legacy 调用桥接层缺显式解包逻辑，直接靠双重断言把新旧契约硬对齐。 |
| P-0165 | src/platform/five-plane-state-evidence/truth/sqlite/repositories/event-repository.ts:79： | (N-0511) src/platform/five-plane-state-evidence/truth/sqlite/repositories/event-repository.ts:79：`insertCostEvent` INSERT 引用 `pricing_version`/`provider_request_id`，原始 `phase_1a_schema_sql_part-1.ts` 未声明，依赖 migration 才能跑通；新部署若先建 schema 再跑老 migration 顺序会失败   | Reviewed and Closed | 当前迁移后的 `cost_events` schema 已覆盖 `pricing_version/provider_request_id`；原始 phase 1a 缺口属于历史快照，已不再代表现行部署状态。 |
| P-0166 | src/platform/five-plane-state-evidence/dlq/index.ts:271-285 | `for (const record of records) { await retry(record); ... }` 串行 await 重试，每条 DLQ 顺序处理；应在尊重幂等的前提下 `Promise.all`/分片并发 src/platform/five-plane-state-evidence/dlq/index.ts:271-285   | Reviewed and Closed | 根因是 DLQ retry 明确选择串行执行以保序和限背压；A 表把“未并发化”直接当成 correctness 缺陷。 |
| P-0167 | src/platform/five-plane-state-evidence/knowledge/knowledge-ingestion-pipeline.ts:75-80,53-55,245-250：文档分片用 | (N-0571) src/platform/five-plane-state-evidence/knowledge/knowledge-ingestion-pipeline.ts:75-80,53-55,245-250：文档分片用 `split(/\n{2,}/)`（无 max chunk size、无 overlap），每个 chunk 的 tokenCount 用 chars/4 估算 → 字符切分输入、token 单位输出，单位不一致   | Reviewed and Closed | 根因是 A 表把 N-0571 已复核收口的轻量 fallback 分片策略重复挂账；真实 token 预算已经下沉到查询侧。 |
| P-0168 | src/platform/five-plane-state-evidence/truth/sqlite/repositories/event-repository.ts:233： | (N-0631) src/platform/five-plane-state-evidence/truth/sqlite/repositories/event-repository.ts:233：`listAllEvents` 用 `LIMIT?OFFSET?` 在 `events ORDER BY created_at ASC` 上 projection rebuild 翻页，无 `(created_at,id)` 覆盖索引 → 全表扫 + filesort   | Reviewed and Closed | 根因是 A 表把 N-0631 已复核收口的低频 projection rebuild 性能 backlog 重复展开；它不影响事件顺序 correctness。 |
| P-0169 | src/platform/five-plane-state-evidence/knowledge/semantic-vector-validation.ts:256 | src/platform/five-plane-state-evidence/knowledge/semantic-vector-validation.ts:256 `DELETE FROM ${schema}.${tableName} WHERE knowledge_ref = $1` 把 schema/table 名字模板字符串拼接，再带 `$1` 参数；如果 schema/tableName 来自外部输入则等同 SQL 注入面（须确认这两参为白名单）   | Fixed | 明细清理路径继续手工拼接 schema/table，未复用统一限定名与引用规则。 |
| P-0170 | src/platform/five-plane-state-evidence/knowledge/knowledge-plane-service.ts:12-15 | `src/platform/five-plane-state-evidence/knowledge/knowledge-plane-service.ts:12-15` import `../../../domains/registry/domain-registry-service.js`、`domains/domain-baseline-catalog.js`、`domains/registry/plugin-spi.js`：平面反向依赖 domains。   | Reviewed and Closed | 根因是 knowledge plane 需要接内建 domains baseline 与 plugin SPI 做 retrieval merge，这是 canonical integration seam，不是 accidental reverse import。 |
| P-0171 | src/platform/five-plane-state-evidence/events/cas/sqlite-cas-repository.ts:144,201,213,237,294,306 | src/platform/five-plane-state-evidence/events/cas/sqlite-cas-repository.ts:144,201,213,237,294,306 BEGIN/COMMIT/ROLLBACK 路径 6 处 `exec()` 调用，未集中到事务装饰器；如方法内中途抛错，须人工保证 ROLLBACK 唯一路径   | Reviewed and Closed | 根因是 A 表把“未抽象成事务装饰器”的样式债误记成实现错误；当前 sqlite CAS repository 已在显式 catch/rollback 路径中封装这些事务。 |
| P-0172 | src/platform/five-plane-state-evidence/events/durable-event-bus.ts:710,766<br>src/platform/five-plane-execution/dispatcher/execution-dispatch-service.ts:1（async-fire-forget | src/platform/five-plane-state-evidence/events/durable-event-bus.ts:710,766 与 src/platform/five-plane-execution/dispatcher/execution-dispatch-service.ts:1（async-fire-forget 风格），关键消息泵 `void this.enqueueDelivery(...)` 不持有 promise，丢弃失败信号无重试 hook   | Fixed | 异步消息泵沿用 fire-and-forget 风格，但缺最小失败记录与可观测补偿点。 |
| P-0173 | src/platform/five-plane-state-evidence/events/cas/sqlite-cas-repository.ts:46,144,201,213,237,294,306 | src/platform/five-plane-state-evidence/events/cas/sqlite-cas-repository.ts:46,144,201,213,237,294,306 多处 `BEGIN IMMEDIATE/COMMIT/ROLLBACK` 但 `exec()` 调用未 try/catch 包裹 ROLLBACK（行 213/306 在 catch 内，须确认逻辑路径），事务边界 audit 弱   | Reviewed and Closed | 根因是 A 表把 transaction audit 可读性问题直接上升为 correctness 缺陷；当前手工事务边界仍保持显式 rollback 兜底。 |
| P-0174 | src/platform/five-plane-state-evidence/events/cas/postgres-fencing-token-service.ts:122-128 | `clearAllFences` 在事务内 `for (const fence of ...) { await repo.delete(...) }`，N 条 fence 触发 N 次 DELETE round-trip；应改为 `DELETE WHERE node_id = $1` src/platform/five-plane-state-evidence/events/cas/postgres-fencing-token-service.ts:122-128   | Fixed | 清理 fencing 逐条 `DELETE`，事务内产生 N 次往返而不是按 owner 批量删除。 |
| P-0175 | src/platform/five-plane-state-evidence/knowledge/semantic-vector-validation.ts:169 | src/platform/five-plane-state-evidence/knowledge/semantic-vector-validation.ts:169 `qualifiedTableName: \`${schema}.${tableName}\`` 重复构造同一概念，知识平面身份重复定义   | Fixed | 校验组件再次自定义限定表名，和存储层没有共享单一表身份表示。 |
| P-0176 | src/platform/five-plane-state-evidence/knowledge/semantic-vector-store.ts:281 | src/platform/five-plane-state-evidence/knowledge/semantic-vector-store.ts:281 `qualifiedTableName()` 返回 `${this.schema}.${this.tableName}`，构造时 line 136-137 用 `validateIdentifier()` — 但同文件 line 260 `${this.schema}.${this.tableName}` 同字串模板入 SQL，多点字串重复构造，缺统一封装   | Fixed | 同一张向量表的限定名在不同查询分支重复拼接，封装边界不完整。 |
| P-0177 | src/platform/five-plane-state-evidence/events/durable-event-bus-support.ts、durable-event-bus.ts(2<br>src/platform/model-gateway/provider-registry/unified-chat-provider.ts、src/platform/stability/{stable-evidence-sequence.ts、reliability/circuit-breaker.ts、reliability/retry.ts、reliability/timeout.ts、timeout.ts、retry.ts}（多数沉睡型 | setTimeout 漏 `unref()`：src/platform/five-plane-state-evidence/events/durable-event-bus-support.ts、durable-event-bus.ts(2 处)、src/platform/model-gateway/provider-registry/unified-chat-provider.ts、src/platform/stability/{stable-evidence-sequence.ts、reliability/circuit-breaker.ts、reliability/retry.ts、reliability/timeout.ts、timeout.ts、retry.ts}（多数沉睡型 timer 仍占住事件循环）   | Reviewed and Closed | 根因是审查把短生命周期 sleep/retry timer 与长期 keep-alive timer 混算；当前常驻路径已逐步 `unref`，剩余等待器跟随活动 Promise 生命周期。 |
| P-0178 | src/platform/five-plane-state-evidence/events/projections/workflow-timeline-projection.ts:235 | `JSON.parse` 无 try/catch：projection 处理器直接解析 payloadJson（多文件）src/platform/five-plane-state-evidence/events/projections/workflow-timeline-projection.ts:235；同模式还有 risk-action-projection.ts:164、incident-projection.ts:168、approval-queue-projection.ts:143、worker-status-projection.ts:161、workflow-run-projection.ts:126、tool-usage-projection.ts:156、artifact-catalog-projection.ts:166、governance-projection.ts:177   | Reviewed and Closed | 复核确认这些 projection 解析路径已具备受控解析/兜底，原问题为陈旧记录。 |
| P-0179 | src/platform/model-gateway/provider-registry/{minimax,openai,anthropic}/*-chat-service.ts | src/platform/model-gateway/provider-registry/{minimax,openai,anthropic}/*-chat-service.ts 各 2 处 `while (true)` 流式 SSE 解析，缺统一 stop-token 校验   | Reviewed and Closed | 根因是 SSE provider 的终止条件已经由各自 chunk parser 与 `AbortSignal` 收敛，A 表把“没有共享 stop-token helper”误记成运行时缺口。 |
| P-0180 | src/platform/model-gateway/provider-registry/unified-chat-provider.ts:678,681,684 | 自由文本 `throw new Error("ChatCompletionRequest requires traceId/tenantId/costTag")`（应使用 AppError + code）src/platform/model-gateway/provider-registry/unified-chat-provider.ts:678,681,684   | Fixed | Chat provider 的必填字段校验直接抛裸 Error，缺少 ValidationError code。 |
| P-0181 | src/platform/prompt-engine/eval/llm-eval-service.ts:630 | 错误吞噬：`} catch {` 静默忽略 src/platform/prompt-engine/eval/llm-eval-service.ts:630   | Reviewed and Closed | 当前 `parseCases()` 的 catch 已记录 `llm_eval_service.parse_cases_failed` 并回退空数组，原“静默吞错”描述已不符合现状。 |
| P-0182 | src/platform/prompt-registry/index.ts:19-30<br>src/platform/prompt-engine/registry/* | `src/platform/prompt-registry/index.ts:19-30` 整体 re-export `src/platform/prompt-engine/registry/*`，与 `prompt-engine/` 重复定义同一概念。   | Reviewed and Closed | 架构参考文档已把 `prompt-registry/` 标注为 Skeleton/namespace surface；权威实现仍在 `prompt-engine/registry/`，这里保留的是命名空间入口。 |
| P-0183 | src/platform/agent-delegation/index.ts:1-71<br>src/platform/five-plane-orchestration/agent-delegation/ | `src/platform/agent-delegation/index.ts:1-71` 是 71 行壳子，实际实现位于 `src/platform/five-plane-orchestration/agent-delegation/`(`delegation-governance-service.ts` 等 10+ 文件)。   | Reviewed and Closed | 顶层 `platform/agent-delegation` 是 legacy namespace facade，canonical 运行时实现仍在 `five-plane-orchestration/agent-delegation`；这不是重复实现。 |
| P-0184 | src/platform/cost-management/index.ts:21-37<br>src/scale-ecosystem/billing/cost-estimation-service.ts | `src/platform/cost-management/index.ts:21-37` 仅做 type re-export；真正实现在 `src/scale-ecosystem/billing/cost-estimation-service.ts`，namespace 名义存在但是空壳。   | Reviewed and Closed | 当前 `cost-management/index.ts` 已导出 `CostEstimationService` 和 discoverability symbols，不再是“仅 type re-export”的空壳。 |
| P-0185 | src/platform/stability/stable-backup-restore-rehearsal.ts:156 | SQL 字符串拼接：`SELECT COUNT(*) FROM ${table}` src/platform/stability/stable-backup-restore-rehearsal.ts:156   | Fixed | 备份演练统计查询直接插入表名，缺已引用标识符封装。 |
| P-0186 | src/platform/stability/prompt-injection-guard.ts:186,293 | 错误吞噬：`} catch {` 静默忽略 prompt-injection 检测异常 src/platform/stability/prompt-injection-guard.ts:186,293   | Fixed | 检测链路把端点校验/解析失败直接吞掉，调用方无法观察风控能力退化。 |
| P-0187 | src/platform/stability/prompt-injection-guard.ts:275 | SSRF 面：prompt-injection-guard `fetch(llmJudgeEndpoint)` 同上 src/platform/stability/prompt-injection-guard.ts:275   | Fixed | 远端判定端点直接透传给 `fetch`，此前没有地址约束。 |
| P-0188 | src/platform/stability/prompt-injection-guard.ts:168 | SSRF 面：prompt-injection-guard `fetch(mlModelEndpoint)` 调用配置端点无显式白名单 src/platform/stability/prompt-injection-guard.ts:168   | Fixed | 机器学习判定端点未经过 URL 校验即出网请求。 |
| P-0189 | src/platform/ops-maturity/platform-panic/panic-propagation-service.ts:373 | setTimeout 未 cleanup：`setTimeout(() => {...})` panic 传播无句柄保存 src/platform/ops-maturity/platform-panic/panic-propagation-service.ts:373   | Reviewed and Closed | 复核确认该定时器已 `unref` 且不参与长期资源持有，原告警与现状不符。 |
| P-0190 | src/platform/ops-maturity/<br>src/ops-maturity/ | `src/platform/ops-maturity/`(仅 `index.ts` + `platform-panic/`) 与顶层 `src/ops-maturity/`(20+ 子模块) 双份存在同名概念。   | Reviewed and Closed | 当前 `platform/ops-maturity` 仅承载 platform-panic 兼容/横切 surface，顶层 `src/ops-maturity/` 才是 Layer 7 业务模块；属 canonical + compat 分层。 |
| P-0191 | src/platform/shared/cache/stores/redis-cache-store.ts:57 | 错误吞噬：`} catch {` 静默忽略 redis 缓存读取错误 src/platform/shared/cache/stores/redis-cache-store.ts:57   | Fixed | 缓存层为避免打断主流程静默吞错，但没有最小 stderr/观测回报。 |
| P-0192 | src/platform/shared/outbox/outbox-poller-service.ts:92 | fire-and-forget Promise：`void this.poll();` src/platform/shared/outbox/outbox-poller-service.ts:92   | Fixed | outbox 定时轮询 fire-and-forget 未附 catch 和失败日志。 |
| P-0193 | src/platform/shared/observability/structured-logger.ts:499 | fire-and-forget Promise：`void result.catch(() => {})` src/platform/shared/observability/structured-logger.ts:499   | Fixed | 日志框架内部为避免影响主流程而静默吞掉异步 sink 失败，缺少 stderr 兜底。 |
| P-0194 | src/platform/shared/async/sync-backed-async-service.ts:22 | fire-and-forget Promise：`rejected.catch(() => undefined)` 无日志 src/platform/shared/async/sync-backed-async-service.ts:22   | Reviewed and Closed | catch 只用于标记内部消费，返回的 rejected promise 仍向调用方保留失败语义。 |
| P-0195 | src/platform/shared/cache/cache-facade.ts:76-112： | (N-0752) src/platform/shared/cache/cache-facade.ts:76-112：`CacheFacade` 无 stale-while-revalidate 路径，缓存失效时所有请求同步 await compute()   | Reviewed and Closed | 根因是 A 表把 N-0752 已复核收口的缓存策略扩展重复挂账；当前 `CacheFacade` 选择强一致 `getOrCompute` 语义。 |
| P-0196 | src/platform/shared/cache/cache-invalidation-broadcast.ts:68 | fire-and-forget Promise：`void this.handleIncomingMessage(raw);` src/platform/shared/cache/cache-invalidation-broadcast.ts:68   | Reviewed and Closed | handleIncomingMessage 内部已自行捕获并记录处理异常，入口处不再重复 catch。 |
| P-0197 | src/platform/shared/observability/structured-logger.ts:528 | fire-and-forget Promise：`this.checkRotationAsync(sink).catch(() => {})` src/platform/shared/observability/structured-logger.ts:528   | Fixed | rotate 检查异步执行时只做空 catch，轮转失败不会留下任何可见信号。 |
| P-0198 | src/platform/shared/lifecycle/service-registry-bootstrap.ts:26：模块顶层 | (N-0753) src/platform/shared/lifecycle/service-registry-bootstrap.ts:26：模块顶层 `ServiceRegistry.registerBootstrap` 注册核心服务，但 `src/` 内无任何文件 import 该模块 → 注册逻辑从不执行（死代码）   | Reviewed and Closed | 根因是 A 表把 N-0753 已复核收口的显式 bootstrap 入口语义重复展开；`service-registry-bootstrap` 本就要求启动入口显式 import。 |
| P-0199 | src/platform/shared/observability/structured-logger.ts:542,550-551,560：日志归档用 | (N-0758) src/platform/shared/observability/structured-logger.ts:542,550-551,560：日志归档用 `${sink.filePath}.${index}` 模板拼接而非 `path.join`   | Fixed | 归档文件路径直接靠字符串模板拼接，缺少路径工具统一处理。 |
| P-0200 | src/platform/shared/utils/redis-client-options.ts:116<br>docker-compose.yml | src/platform/shared/utils/redis-client-options.ts:116 默认 `host: config.host ?? "localhost"`，启动期没人显式设置时会静默连本机 redis（容器内即失败），与 docker-compose.yml `AA_REDIS_HOST: redis` 不一致回退   | Fixed | 构造 Redis standalone 客户端时对 host 做 localhost 隐式回退，容器和远端部署易连错目标。 |
| P-0201 | src/platform/shared/observability/slo-alerting-channels.ts:320 | PII 进日志通道路径：alerting EmailAlertChannel 以 email 字段直接投递（无脱敏标记） src/platform/shared/observability/slo-alerting-channels.ts:320   | Reviewed and Closed | 复核确认邮件通道保存的是投递事件而非把原始邮箱字段写入日志面。 |
| P-0202 | src/platform/shared/observability/slo-alerting-channels.ts:119,175,237,302 | fire-and-forget Promise：四处 `void deliverWithRetry(...)` 调用无错误处理 src/platform/shared/observability/slo-alerting-channels.ts:119,175,237,302   | Reviewed and Closed | deliverWithRetry 内部已负责重试和终态失败记录，外层 void 调用不是静默丢错。 |
| P-0203 | src/platform/shared/cache/cache-facade.ts:17,93-105： | (N-0746) src/platform/shared/cache/cache-facade.ts:17,93-105：`getOrCompute` 单飞用进程内 `pendingComputes: Map`，多 Node 实例共用 L2/L3 时仍各自 compute（thundering herd 不被消解）   | Reviewed and Closed | 根因是 A 表把 N-0746 已复核收口的跨实例 single-flight 演进项重复挂账；当前实现只承诺进程内保护。 |
| P-0204 | src/platform/shared/observability/structured-logger.ts:184,479,503,543,563,579,588,595,598 | 错误吞噬：连续多处 `} catch {` 静默忽略 src/platform/shared/observability/structured-logger.ts:184,479,503,543,563,579,588,595,598   | Fixed | 日志框架担心自我递归或影响主流程，内部多处直接吞错，缺最小失败回报。 |
| P-0205 | src/platform/shared/observability/slo-alerting-service.ts:325-360： | (N-0801) src/platform/shared/observability/slo-alerting-service.ts:325-360：`acknowledgeAlert/resolveAlert` 在 UPDATE 后单独 `SELECT changes()`，多连接模式下 `changes()` 可能反映其它语句   | Reviewed and Closed | 根因是 A 表把 N-0801 已复核收口的多连接假设重复展开；现行 sqlite 路径仍是同步单连接封装。 |
| P-0206 | src/platform/shared/observability/metrics-server.ts:74-76 | src/platform/shared/observability/metrics-server.ts:74-76 通过字符串比较 `address === "127.0.0.1" \|\| "::ffff:127.0.0.1"` 判断本地，未覆盖 `::1`/`localhost`/IPv4-mapped 变体之外的 link-local   | Reviewed and Closed | 复核确认本地地址判定已覆盖 `127.0.0.1`、`::1` 与 IPv4-mapped loopback，原问题已过时。 |
| P-0207 | src/platform/shared/observability/runtime-metrics-registry.ts:108,128,141,143,149,165,185：单位后缀混用 | (N-0809) src/platform/shared/observability/runtime-metrics-registry.ts:108,128,141,143,149,165,185：单位后缀混用 `_ms`(ms) 与 `_seconds`(s) 共存于同一 registry，下游 PromQL 公式难统一   | Reviewed and Closed | 根因是 A 表把 N-0809 已复核收口的指标命名治理 backlog 重复挂账；当前 exporter 已稳定导出，不影响运行正确性。 |
| P-0208 | src/platform/shared/observability/runtime-metrics-registry.ts:115-117：同名前缀 | (N-0723) src/platform/shared/observability/runtime-metrics-registry.ts:115-117：同名前缀 metric 在不同调用处可使用不同 label key 集合（`{stage,result}` vs `{result}`），相邻 series cardinality 不同   | Reviewed and Closed | 根因是 A 表把 N-0723 已复核收口的指标前缀误读重复展开；当前不同 label set 依靠完整 metric name 区分。 |
| P-0209 | src/platform/shared/observability/health-service.ts:312-323： | (N-0803) src/platform/shared/observability/health-service.ts:312-323：`checkDbWritableSync` 直接对生产库 `CREATE TABLE IF NOT EXISTS __health_probe + INSERT/DELETE`，每次健康探测都触发 WAL 写入与锁竞争   | Reviewed and Closed | 根因是 A 表把 N-0803 已复核收口的显式 writable probe 成本重复展开；这是受控健康检查写路径，不是静默副作用。 |
| P-0210 | src/platform/shared/scaling/horizontal-scaling-controller.ts:14 | `src/platform/shared/scaling/horizontal-scaling-controller.ts:14` `import type { QueueStats } from "../../five-plane-execution/queue/queue-adapter-types.js"`：shared→execution 反向耦合。   | Reviewed and Closed | 这里是 type-only 契约引用，编译后不会形成 runtime 反向依赖；`horizontal-scaling-controller` 本身就是 execution queue 指标的共享算法 helper。 |
| P-0211 | src/platform/shared/cache/cache-invalidation.ts:68-74 | `invalidateTags` 中 `for (const tag of tags) { total += await this.cache.invalidateByTag(tag); }` 串行失效；多 tag 场景 RTT 累乘 src/platform/shared/cache/cache-invalidation.ts:68-74   | Fixed | tag 失效沿用串行 await 写法，多标签场景把远端 RTT 线性叠加。 |
| P-0212 | src/platform/shared/observability/sli-collection-service.ts:147-156 | `initializeDefaultSlos` 在 `for` 循环里反复调用 `this.sloAlertingService.listSlos().find(...)`，O(N×M) 扫描；listSlos 还可能复制数组 src/platform/shared/observability/sli-collection-service.ts:147-156   | Fixed | 默认 SLO 初始化在循环内重复拉取并线性查找，缺一次性索引缓存。 |
| P-0213 | src/platform/shared/events/local-typed-event-emitter.ts:3-12 | 自实现 EventEmitter `maxListeners=100` 默认，超出直接 `throw`；多 service 共享同 emitter 时 listener 累积容易触发拒绝；`removeAllListeners` 又被声明为 `protected`，外部生命周期清理路径受限 src/platform/shared/events/local-typed-event-emitter.ts:3-12   | Fixed | 自实现 emitter 继承了 Node 风格 listener 上限思路，却缺共享场景下的默认无限制和外部清理接口。 |
| P-0214 | src/platform/shared/observability/structured-logger.ts:554 | src/platform/shared/observability/structured-logger.ts:554 `setImmediate(() => void this.performRotation(sink))` 将日志 rotate 推到 microtask，期间 sink 写入争用未同步；且 performRotation 内任一异常因 `void` 不可见   | Fixed | 轮转异步被丢进 `setImmediate`，写入与轮转缺同步且错误再次被隐藏。 |
| P-0215 | src/platform/shared/cache/stores/memory-cache-store.ts:75-83 | `cloneStoredValue` 在每次缓存 get/set 都对值做 `structuredClone` 或 `JSON.parse(stableStringify())`，对大对象 cache hit 也强制 O(value-size) 拷贝，吞吐显著下降 src/platform/shared/cache/stores/memory-cache-store.ts:75-83   | Reviewed and Closed | 根因是 memory cache 默认采用 defensive clone 保证调用方不可变隔离；A 表把隔离成本直接视为吞吐 bug。 |
| P-0216 | src/platform/shared/observability/slo-alerting-channels.ts:111-122： | (N-0802) src/platform/shared/observability/slo-alerting-channels.ts:111-122：`WebhookAlertChannel` 用 `AbortSignal.timeout` 但无重试/熔断，高并发下大量并行 fetch 句柄无背压   | Reviewed and Closed | 复核确认 `WebhookAlertChannel` 已统一走 `deliverWithRetry`，原“无重试”判断失效。 |
| P-0217 | src/platform/shared/runtime-state-machine.ts<br>src/platform/five-plane-execution/runtime-state-machine.ts<br>src/platform/five-plane-execution/state-transition/state-transition-machine.ts | `src/platform/shared/runtime-state-machine.ts` 与 `src/platform/five-plane-execution/runtime-state-machine.ts`、`src/platform/five-plane-execution/state-transition/state-transition-machine.ts` 同主题三处定义。   | Reviewed and Closed | `shared/runtime-state-machine` 已是下沉后的权威状态机；`execution/runtime-state-machine` 仅兼容 re-export，`state-transition-machine` 则是通用有限状态校验器，职责并不重复。 |
| P-0218 | src/platform/shared/observability/inspect-service.ts:109、five-plane-execution/lease/execution-lease-service.ts:138："execution | (N-0799) src/platform/shared/observability/inspect-service.ts:109、five-plane-execution/lease/execution-lease-service.ts:138："execution not found" 在 ValidationError 与 StorageError 双路径以不同 code 抛出，告警/重试规则需同时识别两套   | Reviewed and Closed | 根因是 A 表把 N-0799 已复核收口的语义分层异常重复展开；接口校验与存储读取失败本就不是同一错误类别。 |
| P-0219 | src/platform/shared/observability/structured-logger.ts:184,479,503,543,563,579,588,595,598 | src/platform/shared/observability/structured-logger.ts:184,479,503,543,563,579,588,595,598 共 9 处 `} catch {}` 静默吞错（已在 P-0204 提及，但全部位于"日志框架"自身——若日志路径出错，下游再也无法感知，是关键观测面盲点）   | Fixed | 观测面自身为避免自激而普遍空 catch，导致日志链路故障对调用方完全不可见。 |
| P-0220 | src/platform/shared/outbox/outbox-service.ts:28-29 | `src/platform/shared/outbox/outbox-service.ts:28-29` import `../../five-plane-state-evidence/truth/authoritative-sql-database.js`、`five-plane-state-evidence/events/durable-event-bus.js`，shared→state-evidence 反向耦合。   | Reviewed and Closed | `platform-module-catalog` 已把 outbox 列为 `shared` canonical subdomain；它协调事务性 outbox 与状态证据平面契约，本身就是跨平面共享基础设施，不是 accidental reverse import。 |
| P-0221 | src/platform/shared/observability/runtime-metrics-registry.ts:1,128,130,141,143：默认 | (N-0810) src/platform/shared/observability/runtime-metrics-registry.ts:1,128,130,141,143：默认 bucket `[10,50,100,250,500,1000,5000]`（毫秒尺度）被复用于 `stage_duration_seconds`/`llm_ttfb_seconds`/`llm_total_seconds` → bucket 含义变成 10s..5000s   | Reviewed and Closed | 根因是 A 表把 N-0810 已复核收口的秒级 bucket 调优需求重复挂账；默认 bucket 仍是基线观测配置。 |
| P-0222 | src/platform/shared/observability/otel-tracer.ts:112-125,155-158：OTel | (N-0795) src/platform/shared/observability/otel-tracer.ts:112-125,155-158：OTel `startActiveSpan` 在 fallback 模式下创建的 span 对 `setStatus`/`recordException` 都是 noop，错误不被任何后端记录   | Reviewed and Closed | 根因是 OTel fallback span 的 noop 行为是 telemetry backend 不可用时的降级策略，错误仍由 StructuredLogger 和调用侧链路记录。 |
| P-0223 | src/platform/shared/observability/structured-logger.ts:523、context/runtime-context.ts:32-48： | (N-0747) src/platform/shared/observability/structured-logger.ts:523、context/runtime-context.ts:32-48：`setImmediate(()=>this.performRotation(sink))` 已脱离 ALS provideContext 作用域，内部 `getContext()` 抛 `runtime_context.missing`   | Fixed | 轮转任务脱离调用上下文异步执行，ALS 上下文在日志轮转中丢失。 |
| P-0224 | src/platform/shared/observability/structured-logger.ts:401-423： | (N-0808) src/platform/shared/observability/structured-logger.ts:401-423：`recentByTask/recentByTrace/recentByCorrelation` 先 filter 再 `slice(-limit)`，无索引；dashboard 高频拉取即放大攻击面   | Fixed | 查询最近日志时先复制全缓冲区再过滤，热路径复杂度和分配成本偏高。 |
| P-0225 | src/platform/shared/events/local-typed-event-emitter.ts<br>src/platform/five-plane-state-evidence/events/ | `src/platform/shared/events/local-typed-event-emitter.ts`(单文件) 与 `src/platform/five-plane-state-evidence/events/`(20+ 文件) 划分模糊；`shared/outbox/outbox-service.ts` 同时在 `five-plane-state-evidence/outbox/index.ts:1-14` 又定义了一份 stub 类型。   | Reviewed and Closed | `LocalTypedEventEmitter` 负责进程内 typed emitter，`five-plane-state-evidence/events/` 负责持久化/投递总线；`state-evidence/outbox/index.ts` 只提供消息 DTO，不构成第二套 outbox 实现。 |
| P-0226 | src/platform/shared/outbox/outbox-service.ts:209-210,280-281,331-332： | (N-0807) src/platform/shared/outbox/outbox-service.ts:209-210,280-281,331-332：`const newRetryCount = entry.retryCount+1; this.repo.markFailed(...)` 先读后写，并发失败路径会丢失重试计数 → DLQ 触发时机失真   | Reviewed and Closed | 根因是 A 表把 N-0807 已复核收口的跨进程 atomic retry 演进项重复挂账；当前单 poller 内已不再并发重入。 |
| P-0227 | src/platform/shared/observability/structured-logger.ts:439-460,462-475：文件 | (N-0792) src/platform/shared/observability/structured-logger.ts:439-460,462-475：文件 sink 无每条 entry size cap，也无 sampling/rate-limit；`maxBytes` 仅触发 rotation，热路径放入大对象会瞬间撑爆 retention 触发频繁 rotate（rotate 是 setImmediate 异步，期间继续 append 无背压）   | Reviewed and Closed | 根因是文件 sink 只是 repo-local fallback 观测通道，不是主 ingestion backplane；entry cap 和 sampling 属观测优化 backlog。 |
| P-0228 | src/platform/shared/observability/structured-logger.ts:447,560： | (N-0798) src/platform/shared/observability/structured-logger.ts:447,560：`appendFile` 与 `performRotation` 之间无同步，rotation 期间 appendFile 仍写 `sink.filePath`，rename 后到下次 setImmediate 之间的并发 append 会写到刚被 rename 的旧 inode → 丢日志   | Reviewed and Closed | 根因是结构化文件日志仍定位为 best-effort fallback sink；rotation 与 append 的极窄竞态属于本地文件观测残余风险，不影响权威事件链。 |
| P-0229 | src/platform/shared/cache/stores/redis-cache-store.ts:161-163,172-174 | `for (const tag of meta.tags) await this.extendIndexTtl(...)` 与 `for (const tagKey of keys) cleaned += await this.cleanupIndexSet(tagKey)` (line 260/270) 串行 RTT，应批量 pipeline 或 Promise.all src/platform/shared/cache/stores/redis-cache-store.ts:161-163,172-174   | Fixed | tag TTL 延长和索引清理都按串行 await 执行，远端缓存 RTT 被不必要累乘。 |
| P-0230 | src/platform/shared/stability/stable-evidence-bundle.ts:1 | `src/platform/shared/stability/stable-evidence-bundle.ts:1` 一行 `export * from "../../stability/stable-evidence-bundle.js"`：`platform/stability/`(20+ 文件) 与 `platform/shared/stability/`(20+ 文件) 平行重复主题。   | Reviewed and Closed | TD-7 已完成把 `shared/stability` 物理迁移到 `platform/stability`，旧 `shared/stability/*` 仅保留兼容 shim；该条是迁移后的陈旧 inventory。 |
| P-0231 | src/platform/shared/outbox/outbox-service.ts:112-128,318-347： | (N-0806) src/platform/shared/outbox/outbox-service.ts:112-128,318-347：`getPendingEntries` 仅 `SELECT … LIMIT`，无 `FOR UPDATE SKIP LOCKED`/`claimed_at` 列；多个 OutboxPollerService 实例并发会同时拉到同一批 → 跨进程重复发布   | Reviewed and Closed | 根因是 A 表把 N-0806 已复核收口的分布式部署演进项重复展开；当前单实例重入问题已收口，跨实例 claim 需要 schema 与拓扑配合。 |
| P-0232 | src/platform/shared/observability/transports/datadog-transport.ts:53-56,66-102 | `setInterval(() => void this.flushInternal(), ...)` 无 in-flight reentry guard；上一轮 retry/backoff 仍在跑时，新 tick 再次 splice batch + 并发 HTTP，背压逻辑（重新 unshift）会与并行 flush 竞态 src/platform/shared/observability/transports/datadog-transport.ts:53-56,66-102   | Fixed | 定时 flush 缺少 in-flight 守卫，重试回退期间可再次并发发送同一缓冲区。 |
| P-0233 | src/platform/shared/cache/cache-key-factory.ts:14-17、context/runtime-context.ts:28-29： | (N-0745) src/platform/shared/cache/cache-key-factory.ts:14-17、context/runtime-context.ts:28-29：`CacheKeyFactory.create` 仅 `${namespace}:${version}:${fingerprint}`，无 tenant/workspace/principal 段，依赖调用方 namespace 约定承载租户隔离 → 易出现跨租户缓存污染   | Reviewed and Closed | 根因是 A 表把 N-0745 已复核收口的 namespace 约定承载租户隔离重复挂账；当前契约并未承诺统一自动拼接 tenant 段。 |
| P-0234 | src/platform/shared/async/、src/platform/five-plane-execution/bulkhead/bulkhead-isolator.ts： | (N-0751) src/platform/shared/async/、src/platform/five-plane-execution/bulkhead/bulkhead-isolator.ts：`shared` 不提供通用 Semaphore/Mutex/async-queue，全仓只有 `BulkheadIsolator.queue` 一处异步 waiter；其它需要互斥/信号量语义的代码（lease handover、worker snapshot 写回、cache single-flight 等）只能各自重造原语 → 跨子系统并发原语标准缺失（与 P-0135/P-0203 互证）   | Reviewed and Closed | 根因是 A 表把 N-0751 已复核收口的通用并发原语设计取舍重复挂账；当前 `shared` 并未承诺提供统一 Semaphore/Mutex。 |
| P-0235 | src/platform/shared/lifecycle/service-registry.ts:90-92 | `getInstance() { return ServiceRegistry._instance ??= new ServiceRegistry(); }` 加 line 80-84 构造里同步遍历 `bootstrapRegistrars` 注册——若任一 registrar 自身又调 `getInstance()`（重入）会拿到尚未构造完毕的引用；并发 (微任务) 入口下首调可竞争创建多份 registry src/platform/shared/lifecycle/service-registry.ts:90-92   | Fixed | 根因是 `getInstance()` 首次构造时直到构造函数返回后才写入 `_instance`，bootstrap registrar 重入会再次触发 new。本轮已在构造期间提前种下 singleton，并新增 reentry 回归测试。 |
| P-0236 | src/platform/shared/lifecycle/service-registry-bootstrap.ts:20-24 | `src/platform/shared/lifecycle/service-registry-bootstrap.ts:20-24` 反向导入 `../../five-plane-control-plane/iam/network-egress-audit.js`、`../../five-plane-execution/execution-engine/output-continuation-service.js`、`../../five-plane-orchestration/agent-delegation/delegation-audit-service.js`，shared 反依赖业务平面。   | Reviewed and Closed | 根因是 `service-registry-bootstrap` 本身就是显式 bootstrap wiring module；它导入各 plane 服务是为了注册单例图，不代表 shared runtime 反向拥有业务逻辑。 |
| P-0237 | src/platform/index.ts:1-21 | src/platform/index.ts:1-21 同时使用 `export * as foo` 与 `export *`，barrel 命名空间不可由 `import { x } from` 直接消费，IDE 自动补全混乱   | Reviewed and Closed | 根因是顶层 `platform` barrel 需要同时服务 namespace 消费和扁平兼容导出；混用 `export * as` 与 `export *` 是刻意的发布面设计，不是实现错误。 |
| P-0238 | src/platform/index.ts:1-21 | `src/platform/index.ts:1-21` 同时 `export * as` 与 `export *`(`./architecture/index.js:20`、`./ai-operations-runtime-catalog.js:14-16`)，namespace barrel 与平铺 re-export 混用。   | Reviewed and Closed | 根因是该入口承担“双发布面”职责：对上保留 namespace 聚合，对下兼容平铺导出；当前是有意兼容设计，不再单列为缺陷。 |
| P-0239 | src/platform/<br>.gitignore:1-65 | .gitignore:1-65 包含 `# Legacy five-plane compatibility symlinks under src/platform/` 列出 5 条目录（control-plane/execution/interface/orchestration/state-evidence），但 `find src -type l` 实际无任何 symlink，规则成为死配置   | Reviewed and Closed | 根因是忽略规则保留了历史兼容产物的前瞻性兜底；当前没有 symlink 只说明该兼容路径未被使用，不构成运行时或构建错误。 |
| P-0240 | src/platform/platform-mainline-bootstrap.ts<br>src/platform/five-plane-runtime-bootstrap.ts<br>src/platform/five-plane-runtime-orchestrator.ts<br>src/platform/ai-operations-runtime-orchestrator.ts<br>src/scale-ops-runtime-orchestrator.ts<br>src/interaction-governance-runtime-orchestrator.ts<br>src/domains-runtime-orchestrator.ts<br>src/platform-architecture-bootstrap.ts | 启动入口重叠：`src/platform/platform-mainline-bootstrap.ts`、`src/platform/five-plane-runtime-bootstrap.ts`、`src/platform/five-plane-runtime-orchestrator.ts`、`src/platform/ai-operations-runtime-orchestrator.ts`、`src/scale-ops-runtime-orchestrator.ts`、`src/interaction-governance-runtime-orchestrator.ts`、`src/domains-runtime-orchestrator.ts`、`src/platform-architecture-bootstrap.ts` 各自构建 capability/startup 表，逻辑同构。   | Reviewed and Closed | 根因是最初缺少顶层导航文档；当前 `src/apps/index.ts` 已定义 startup target/app manifest，`docs_zh/architecture/02-code-architecture-reference.md` 也明确列出各 root orchestrator 职责，不再是“无说明重叠”。 |
| P-0241 | src/platform/<br>src/platform/index.ts:1-21 | `src/platform/` 同时承载 `five-plane-*` 五大平面与 `agent-delegation/ compliance/ contracts/ prompt-engine/ prompt-registry/ model-gateway/ stability/ architecture/ structure/ ops-maturity/ cost-management/ remote-coordination/` 等同级子系统，主题层级混乱：见 `src/platform/`(目录列表) 与 `src/platform/index.ts:1-21` 同时把 `controlPlane`/`compliance`/`promptEngine`/`architecture` 平铺为顶级 namespace。   | Reviewed and Closed | 根因是平台命名空间本来就承担“五平面 + 跨平面公共子系统”的 umbrella 角色；当前 AGENTS 与代码架构参考文档已经同步解释这一层级，不再把它视为未解释混乱。 |
| P-0242 | src/sdk/client-sdk/api-client.ts:807 | 错误吞噬：`} catch {` 静默忽略 src/sdk/client-sdk/api-client.ts:807   | Fixed | SSE 事件订阅在 payload 解析失败时原本直接吃掉异常，没有给调用方任何诊断信号。 |
| P-0243 | src/sdk/client-sdk/api-client.ts:897 | 错误吞噬：`} catch {` 静默忽略 src/sdk/client-sdk/api-client.ts:897   | Reviewed and Closed | `parseCursor()` 的异常分支现在显式限定为解析类错误并返回 `undefined`，不再是裸吞错形态。 |
| P-0244 | src/sdk/cli/aa.ts:42 | src/sdk/cli/aa.ts:42 子命令路径用 `${command}.${ext}` 直接拼到 spawn → 若将来 CLI_ENTRYPOINTS 中含相对路径或斜杠（拼写错误）即变可注入命令路径   | Fixed | CLI 子命令名直接参与入口路径拼接，缺少字符集约束。 |
| P-0245 | src/sdk/client-sdk/api-client.ts:876 | `JSON.parse` 无 try/catch：解析 base64 cursor src/sdk/client-sdk/api-client.ts:876   | Fixed | 分页 cursor 解析默认信任 base64 内容是合法 JSON，对坏输入没有受控回退。 |
| P-0246 | src/sdk/client-sdk/api-client.ts:339 | 重试无指数退避：固定延迟 `setTimeout(connect, 1000)` 重连 SSE src/sdk/client-sdk/api-client.ts:339   | Fixed | SSE 重连固定 1 秒重试，网络异常时会形成过于刚性的重连压力。 |
| P-0247 | src/sdk/cli/api-server.ts:316 | fire-and-forget Promise 且 catch 写空：`.catch(() => undefined)` src/sdk/cli/api-server.ts:316   | Fixed | CLI 启动清理路径把 cleanup 异常直接吞掉，没有输出失败信息。 |
| P-0248 | src/sdk/client-sdk/api-client.ts:311 | 自由文本 `throw new Error("SSE response body is not readable")` src/sdk/client-sdk/api-client.ts:311   | Fixed | SSE 不可读分支直接抛裸 Error，缺少结构化校验错误。 |
| P-0249 | src/sdk/cli/login.ts:94 | src/sdk/cli/login.ts:94 `AA_CREDENTIALS_ENCRYPTION_KEY` 只校验 `length===0`，单字符弱口令会通过；scrypt 救不了过短主密钥   | Fixed | OAuth 凭据加密把“存在密钥”当成充分条件，没有最小强度基线。 |
| P-0250 | src/sdk/cli/login.ts:217 | src/sdk/cli/login.ts:217 `AA_OAUTH_AUTH_CODE` 通过环境变量传入，shell 历史/进程列表/容器审计中可见，授权码外泄面增大   | Fixed | 登录流程只支持从环境变量读取授权码，把一次性 code 暴露在环境面。 |
| P-0251 | src/sdk/client-sdk/api-client.ts:339 | setTimeout 未保存句柄/不 cleanup：`setTimeout(connect, 1000)` 重连定时器无 clearTimeout src/sdk/client-sdk/api-client.ts:339   | Fixed | SSE 重连 timer 没有保存句柄，订阅取消后也无法主动清理。 |
| P-0252 | src/sdk/cli/aa.ts:30 | src/sdk/cli/aa.ts:30 `COMMAND_ALIASES` 查表后再 `as never` 校验已知子命令，但 alias 指向被删除的入口名时仍把缺失文件名传给 spawn，错误信息只来自 child exit   | Fixed | alias 解析后仍可能把不存在的命令入口交给子进程执行，失败只能在 spawn 后暴露。 |
| P-0253 | src/sdk/cli/login.ts:64-69 | src/sdk/cli/login.ts:64-69 token 交换失败时未删除 statePath 中的 verifier，重试或异常下 PKCE verifier 留在磁盘 → 若被读到可与已捕获 code 重放   | Fixed | OAuth finish 流程只在成功路径考虑收尾，失败时会把 verifier 状态文件留在磁盘。 |
| P-0254 | src/sdk/cli/migrate-sqlite-to-pg.ts:206,229,236 | SQL 字符串拼接：`SELECT * FROM ${table}` 与 `INSERT INTO ${table} (${columns.join})` src/sdk/cli/migrate-sqlite-to-pg.ts:206,229,236   | Reviewed and Closed | 根因是 migrate-sqlite-to-pg 的 `table` 来源于内建迁移白名单，不是用户输入；审查把受控标识符插值误记成 SQL 注入。 |
| P-0255 | src/sdk/cli/aa.ts:51-58 | src/sdk/cli/aa.ts:51-58 父进程仅 `process.exitCode=code` 然后 resolve()，未 process.exit；子命令希望立即退出（如交互式输入终止）的场景父 process 仍跑完事件循环 → CLI 退出时机不确定   | Fixed | CLI 父进程把退出码留给事件循环自然结束，退出时机受残留句柄影响。 |
| P-0256 | src/sdk/cli/login.ts:194 | src/sdk/cli/login.ts:194 `startOAuthLogin` 允许从 env `AA_OAUTH_STATE` 注入 state，攻击者控制 env 可强制使用可预测 state，配合 P-0259 形成完整 CSRF 链   | Fixed | OAuth state 允许从外部环境注入，破坏了 PKCE/CSRF 随机挑战的前提。 |
| P-0257 | src/sdk/cli/login.ts:71 | src/sdk/cli/login.ts:71 `exchangeCodeForTokens` 直接 `response.json() as Promise<TokenResponse>`，未对 token 字段做最小校验（如 access_token 是字符串），异常响应会污染 saveOAuthTokens   | Fixed | token 交换响应被直接断言成目标类型，没有做字段级校验。 |
| P-0258 | src/sdk/cli/api-server.ts:286<br>deploy/helm | src/sdk/cli/api-server.ts:286 `metricsServer.listen(envConfig.metricsPort, envConfig.apiHost ?? "127.0.0.1", ...)` 把 apiHost 复用给 metrics，与 deploy/helm values-prod.yaml `AA_API_HOST: "0.0.0.0"` 默认一旦合用会把 metrics 同时暴到公网   | Fixed | 根因是 API server 以前把 `AA_API_HOST` 复用为 metrics bind host，prod 一旦把 API 监听到 `0.0.0.0` 就会顺带放开 metrics。本轮已拆分 `AA_METRICS_HOST`，默认仍回落 loopback。 |
| P-0259 | src/sdk/cli/login.ts:213-227 | src/sdk/cli/login.ts:213-227 `finishOAuthLogin` 全程未把回调返回的 state 与 pending.state 比对，CSRF/state-mismatch 防护缺失（OAuth PKCE 流程少了 state 校验）   | Fixed | finish 阶段只校验 code/verifier，没有把回调 state 与本地 pending state 做绑定验证。 |
| P-0260 | src/sdk/cli/secret-management.ts:18-19 | `src/sdk/cli/secret-management.ts:18-19`、`sdk/cli/ha-program.ts:24-26`、`sdk/cli/stable-campaign.ts:33-35` 直接 import `platform/five-plane-control-plane/iam/secret-management-service.js`、`iam/sandbox-policy.js`、`config-center/*-cli-env.js`、`platform/shared/stability/*`：SDK 直接耦合平面与 shared 私有。   | Reviewed and Closed | 根因是把 `sdk/cli` operator binaries 当成独立 public SDK；这些 CLI 与 platform runtime 同仓交付，直接依赖 canonical control-plane/shared 模块属预期。 |
| P-0261 | src/sdk/harness-sdk/index.ts:502,590,610,634,657 | src 中保留 `@ts-expect-error` 共 13 处（src/sdk/harness-sdk/index.ts:502,590,610,634,657 等）绕过 strict 类型   | Reviewed and Closed | 根因是 `@ts-expect-error` 汇总把少量 SDK 兼容桥接与类型边界例外一并上升为系统性 strict 缺陷；当前仍是受控技术债而非失控绕过。 |
| P-0262 | src/sdk/harness-sdk/index.ts:5-33 | `src/sdk/harness-sdk/index.ts:5-33` import `platform/stability/bulkhead-isolation.js` 与 `platform/five-plane-orchestration/harness/index.js`：SDK 跨进入 orchestration 平面与 stability 内部。   | Reviewed and Closed | 根因是 harness-sdk 本身就是 orchestration harness 的开发者封装层，A 表把其桥接依赖重复挂成边界缺陷。 |
| P-0263 | src/sdk/harness-sdk/index.ts:33： | (N-1261) src/sdk/harness-sdk/index.ts:33：`harness-sdk` 直接 import `platform/five-plane-orchestration/harness/index.js`（barrel）以及 stability/contracts 内部，把 orchestration plane 内部 API 升级为 SDK 公共契约，破坏 SDK ↔ platform 单向导出关系（与 §A 模块边界规则冲突）   | Reviewed and Closed | 根因是 A 表把 N-1261 对 harness SDK 边界的主审计结论再次展开；该问题已在主表作为 SDK 依赖方向治理项收口。 |
| P-0264 | src/sdk/pack-sdk/pack-manifest.ts:212 | 错误吞噬：`} catch {` 静默忽略 src/sdk/pack-sdk/pack-manifest.ts:212   | Reviewed and Closed | 根因是这里的 `verifyPackSignature()` 公共契约本来就以布尔返回表示“签名无效”；catch 仅把坏 key/坏签名统一映射为 `false`，不是静默吞掉调用侧错误。 |
| P-0265 | src/sdk/plugin-sdk/plugin-test-harness.ts:215 | fire-and-forget Promise：`execution.finally(...).catch(() => undefined)` src/sdk/plugin-sdk/plugin-test-harness.ts:215   | Reviewed and Closed | 根因是这段 Promise 只用于在执行结束时清理超时 timer；原始 `execution` 的成败仍由 `Promise.race` 结果向调用方显式暴露。 |
| P-0266 | src/sdk/plugin-sdk/plugin-test-harness.ts:228 | 自由文本 `throw new Error("Live mode requires a bound liveRunner or livePlugin runtime")` src/sdk/plugin-sdk/plugin-test-harness.ts:228   | Fixed | live mode 前置校验直接抛裸 Error，缺少 ValidationError code。 |
| P-0267 | src/domains/yono/index.ts:258 | src/domains/yono/index.ts:258 stance→probability 直接硬编码 0.65/0.35/0.5，所谓"概率"是常量映射而非任何模型输出（伪造置信度）   | Fixed | 根因是评论信号把立场直接映射成固定概率常量，完全丢掉 yes/no 词命中强弱。本轮已改为按 yes/no 词差值推导概率，并由 `tests/unit/domains/yono/yono-domain.test.ts` 补回归。 |
| P-0268 | src/domains/yono/index.ts:390 | src/domains/yono/index.ts:390 `YonoResolutionAssistAgent.draft` 在有/无证据时分别硬编码 confidence=0.72/0.42（伪造置信度）   | Fixed | 根因是 resolution assist 把证据置信度写死成两档常量。本轮已改为按证据数量与歧义风险计算 confidence，并由 `tests/unit/domains/yono/yono-domain.test.ts` 覆盖。 |
| P-0269 | src/domains/yono/index.ts:307-309 | src/domains/yono/index.ts:307-309 `yonoConsensusProbability` 把 0.4/0.3/0.3 写死为市场/评论/预测三类源权重，未配置化也未按数据量动态调整   | Fixed | 根因是共识概率把市场/评论/预测权重静态写死。本轮已改为按 signal/forecast 数量动态加权，避免少量样本和大量样本走同一权重。 |
| P-0270 | src/domains/yono/index.ts:413 | src/domains/yono/index.ts:413 `createOrder` 未校验 quantity 非负、limitPrice 非负，buy 端用负 quantity 等价于 sell 跨账户绕开侧别风控   | Fixed | 根因是下单入口缺少数量和限价的 fail-close 校验。本轮已拒绝非正 quantity 和负 limitPrice，并由 `tests/unit/domains/yono/yono-domain.test.ts` 回归。 |
| P-0271 | src/domains/yono/index.ts:265 | src/domains/yono/index.ts:265 `evidenceBacked` 仅靠 `/https?:\|source\|evidence\|.../` 关键词正则判证据，纯文本 "source" 一词即可冒充证据   | Fixed | 根因是证据识别把宽泛关键词当成证据锚点。本轮已收紧到 URL、`source:`、`evidence:`、official announcement 等标记，并补 plain `source` 不算证据的回归。 |
| P-0272 | src/domains/yono/index.ts:266 | src/domains/yono/index.ts:266 `confidence` 仅产生 "high"/"medium"，永远不会输出 "low"，置信度等级残缺   | Fixed | 根因是评论信号置信度分档少了一层低置信区间。本轮已补 `low` 分支，并由 `tests/unit/domains/yono/yono-domain.test.ts` 覆盖弱信号场景。 |
| P-0273 | src/domains/yono/index.ts:188 | src/domains/yono/index.ts:188 `createMarket` 未校验 `closeAt > openAt` 且 `resolutionDeadline > closeAt`，可创建已结算或时间倒序的市场   | Fixed | 根因是市场创建只接受时间字符串，没有校验时间顺序。本轮已强制 `closeAt > openAt`、`resolutionDeadline > closeAt`，并补非法时间回归。 |
| P-0274 | src/domains/yono/index.ts:526 | src/domains/yono/index.ts:526 `registerYonoDomain` 始终带 `skipSmokeTest:true` 注册，预测市场域上线绕过 smoke 校验   | Reviewed and Closed | 根因是评审把未使用的 legacy option 误判为真正上线开关；当前 `DomainRegistryService.register()` 不消费 `skipSmokeTest`，真实 smoke gate 在 promote/activate 路径。 |
| P-0275 | src/domains/quant-trading/index.ts:73 | src/domains/quant-trading/index.ts:73 `orderNotionalUsd` 比较未取绝对值，负数 notional 即可绕过 `maxOrderNotionalUsd`；79、82 行同样未校验输入非负   | Fixed | 根因是 pre-trade guard 既没校验非负输入，也没对 notional/position 做绝对值比较。本轮已补输入校验和绝对值限制，并由 `tests/unit/domains/quant-trading/index.test.ts` 回归。 |
| P-0276 | src/domains/healthcare/index.ts:7 | src/domains/healthcare/index.ts:7 `HEALTHCARE_DOMAIN_PRESET` 仅把 summarize/coordinate 标为需复核，triage（分诊，最高风险）反而 bypass 复核 → 医疗域默认安全姿态错误   | Fixed | 根因是医疗域 preset 把高风险 triage 错放到无需复核集合。本轮已把 triage 纳入默认复核，并补对应测试。 |
| P-0277 | src/domains/yono/index.ts:383 | src/domains/yono/index.ts:383 `proposedOutcomeId` 永远取 `market.outcomes[0]`，无论用户证据指向哪个 outcome，决议建议本质上没有判定逻辑   | Fixed | 根因是 resolution assist 在证据存在时仍退化为默认首 outcome。本轮已优先从 evidence refs 匹配 outcome/type，并补 `no` outcome 解析回归。 |
| P-0278 | src/domains/risk-profile/index.ts:75 | src/domains/risk-profile/index.ts:75 `score` 入参未做 NaN/Infinity 校验，传 NaN 时所有 `>=` 比较为 false，落入 default 返回 low   | Fixed | 根因是风险分级只比较区间，没有先拒绝 NaN/Infinity。本轮已把非法分数走安全兜底，并由 `tests/unit/domains/risk-profile/index.test.ts` 覆盖。 |
| P-0279 | src/domains/quant-trading/index.ts:37-42 | src/domains/quant-trading/index.ts:37-42 `DEFAULT_QUANT_TRADING_PRE_TRADE_LIMIT_POLICY` 把 100K USD 单笔/25K USD 日损/12% 回撤写死为"全行业默认"，未按租户/账户分层   | Reviewed and Closed | 根因是评审把导出的 baseline 常量误判为强制租户策略；实际 `evaluateQuantTradingPreTradeRisk()` 始终消费调用方传入的 `limitPolicy`，该常量只是默认样例/测试基线。 |
| P-0280 | src/domains/finance-accounting/index.ts:7 | src/domains/finance-accounting/index.ts:7 `FINANCE_ACCOUNTING_DOMAIN_PRESET` 把 reconcile（对账）排除在复核之外，但财务对账偏差是合规审计核心，默认无人复核   | Fixed | 根因是财务域 preset 把高风险 reconcile 放到无需复核集合。本轮已把 reconcile 纳入默认复核，并补测试。 |
| P-0281 | src/domains/yono/index.ts:175-187 | src/domains/yono/index.ts:175-187 `createMarket` 全部 risk 字段（manipulationRisk/regulatoryRisk 等）硬编码为 low/medium，所有市场上线都默认 medium 监管风险，与 risk-profile 体系脱钩   | Fixed | 根因是市场风险画像完全写死在创建路径里。本轮已改为按 category、tags、time-to-close 派生风险，不再固定回填。 |
| P-0282 | src/domains/registry/plugin-runtime-host.ts:363 | 命令构造：`spawn(spec.command, spec.args, ...)` 直接执行外部 spec 指定的 command/args src/domains/registry/plugin-runtime-host.ts:363   | Reviewed and Closed | 根因是评审只看到了 `spawn()` 这个执行点，没有继续往上追 launch spec 校验；当前容器运行规范来自 `AA_PLUGIN_RUNTIME_CONTAINER_COMMAND_JSON` 的受控解析结果，不是插件清单任意透传命令。 |
| P-0283 | src/domains/risk-profile/index.ts:84 | src/domains/risk-profile/index.ts:84 `computeDomainRiskLevel` 在低分(<35)但 `profile.defaultRiskLevel === "high"` 时仍返回 "low"，把 high 默认风险错误降级（仅对 critical 做兜底）   | Fixed | 根因是默认风险级别只对 critical 做兜底，高风险默认档会被低分覆盖。本轮已保留 `defaultRiskLevel === "high"` 的安全下界，并补测试。 |
| P-0284 | src/domains/yono/index.ts:336-338 | src/domains/yono/index.ts:336-338 `manipulationRisk` 仅按"全部 5 条立场相同"判定，永不输出 "medium"，且未结合时间窗/账号信誉/IP，是空架子的操纵检测   | Fixed | 根因是社交预测把操纵风险收敛成二元判断，弱一致性信号无法落中档。本轮已补 `medium` 分层，至少把 3 条以上单向一致信号从低风险抬升。 |
| P-0285 | src/domains/legal/index.ts:11 | src/domains/legal/index.ts:11 `requiresAttorneyReview` 对所有 LegalTaskType 始终返回 true，使函数失去判定意义且与第 14 行 `requiresLegalReview` 形成两套互斥的"是否需要律师审查"判断   | Fixed | 根因是 legal 域同时维护了两套是否复核逻辑，`requiresAttorneyReview()` 脱离 canonical 规则。本轮已让其委托 `requiresLegalReview()`，并把 review/redline/advise 纳入默认复核。 |
| P-0286 | src/domains/registry/plugin-runtime-child.ts:172-176 | src/domains/registry/plugin-runtime-child.ts:172-176 全局劫持 console.* 之后，line 197 `process.exitCode = 0`：插件子进程默认成功退出，劫持后插件内任何运行时 panic 仅落 structured log 不影响 exitCode → 父进程难感知插件失败   | Reviewed and Closed | 根因是评审把请求级失败和进程生命周期退出语义混在了一起；当前 `process.exitCode = 0` 只在显式 shutdown 消息下设置，请求异常仍通过 runtime response 通道回传。 |
| P-0287 | src/domains/quant-trading/index.ts:51-57 | src/domains/quant-trading/index.ts:51-57 `taskType !== "trade"` 直接返回 `allowed=true` 且 `evaluatedGuards` 为空，simulate 任务可绕过所有风控阀（实际可触达回测/影子撮合的真实订单链路）   | Fixed | 根因是 pre-trade risk 只把 `trade` 视为受控路径，`simulate` 被错误放行。本轮已让 `simulate` 同样进入 guard 校验，并补测试。 |
| P-0288 | src/domains/yono/index.ts:255-258 | src/domains/yono/index.ts:255-258 `scoreWords` 用 `String.includes` 做关键词扫描，"yes" 命中 "yesterday/eyes"、"no" 命中 "knot/note"，立场判定大量误判；纯英文关键词在中文/西语市场失效   | Fixed | 根因是评论信号用子串匹配关键词，导致词边界误命中。本轮已改为分词后判定命中，至少收住 `yes/no` 的英文子串误判面。 |
| P-0289 | src/domains/academic-research/index.ts、finance-accounting、healthcare、legal | src/domains/academic-research/index.ts、finance-accounting、healthcare、legal 等十余个领域 index.ts 仅靠改 enum 字符串套出，是同一份模板复制粘贴，缺少各行业实际差异化能力声明（与 P-0262/P-0002 互证为"行业域规模化伪差异"）   | Reviewed and Closed | 根因是评审把共享 preset 组合误判成伪差异实现；当前多领域模块有意复用 `domain-module-helper.ts` 作为基线模板，再通过各域 task/review 策略做配置化差异。 |
| P-0290 | src/domains/registry/plugin-runtime-child.ts:172-176 | src/domains/registry/plugin-runtime-child.ts:172-176 全局劫持 `console.log/warn/error = ...`，会污染插件第三方库的诊断输出（含 stack trace 行号丢失）   | Reviewed and Closed | 根因是评审把 stdio 重定向误判为诊断丢失；当前 console 劫持是插件子进程向父进程传 structured log 的既定桥接层，不影响失败回传契约。 |
| P-0291 | src/domains/domain-risk-profile-service.ts:182-200 | 在 overrides for-of 内对每个 override 调 `rules.find(... matchesPattern)`，且 `matchesPattern` 内部 `new RegExp` 即时编译——O(n²) 扫描叠加每次 RegExp 编译 src/domains/domain-risk-profile-service.ts:182-200   | Fixed | override 匹配对每个规则重复扫描并即时编译正则，热路径复杂度偏高。 |
| P-0292 | src/domains/registry/plugin-runtime-child.ts:105-108 | src/domains/registry/plugin-runtime-child.ts:105-108 在 ESM 子进程里 `require("node:http"/"node:https"/"node:net"/"node:tls")` 用 cast `as typeof import(...)`，绕过 ESM 静态依赖图，不利于 sandbox/SBOM 分析   | Reviewed and Closed | 根因是评审忽略了这里的目的不是业务依赖加载，而是先经 `createRequire` 注入补丁再 `syncBuiltinESMExports()`，以把内建网络模块的受控改写同步给 ESM 消费者。 |
| P-0293 | src/domains/yono/index.ts:524 | src/domains/yono/index.ts:524 `registry.get(...)!` 非空断言紧跟 null 检查，存在并发注册下的 TOCTOU 竞态   | Fixed | 根因是注册路径在 null 检查后仍保留非空断言，留下不必要的 TOCTOU 表达。本轮已直接返回 `register()` 的结果，不再二次 `get(...)!`。 |
| P-0294 | src/domains/registry/domain-registry-service.ts:266-272 | `toolNames.filter((tool) => enabledTools.has(tool) \|\| domain.capabilities.requiredTools.includes(tool) \|\| domain.capabilities.optionalTools.includes(tool))`：每个 tool 对 requiredTools/optionalTools 做线性 `includes`；应预转 Set src/domains/registry/domain-registry-service.ts:266-272   | Fixed | 根因是工具过滤热路径在循环内重复线性 `includes`。本轮已把 required/optional tools 预转 `Set`，降低重复扫描成本。 |
| P-0295 | src/domains/governance/hr-role-governance-service.ts:16-24 | `src/domains/governance/hr-role-governance-service.ts:16-24` import `platform/five-plane-control-plane/approval-center/approval-service.js` 与 `platform/five-plane-execution/tool-executor/tool-recommend-service.js`；`domains/governance/division-loader.ts:26-28` 进入 `iam/sandbox-policy`、`config-center/config-governance-service`：domains 直入五面私有内部。   | Reviewed and Closed | 根因是 built-in domains/governance 服务本来就是 runtime modules，不是外部扩展边界；A 表把领域层直连 canonical platform service 误判成私有越界。 |
| P-0296 | src/domains/domain-baseline-catalog.ts<br>src/interaction/interaction-baseline-catalog.ts<br>src/ops-maturity/ops-maturity-baseline-catalog.ts<br>src/org-governance/governance-baseline-catalog.ts<br>src/scale-ecosystem/scale-baseline-catalog.ts | `*-baseline-catalog.ts` 在 5 个顶级目录重复（`src/domains/domain-baseline-catalog.ts`、`src/interaction/interaction-baseline-catalog.ts`、`src/ops-maturity/ops-maturity-baseline-catalog.ts`、`src/org-governance/governance-baseline-catalog.ts`、`src/scale-ecosystem/scale-baseline-catalog.ts`），同模式分散维护。   | Reviewed and Closed | 五个 baseline catalog 分别承载各顶层子域的默认治理模板与种子规则；命名一致是模式复用，不是同一实现被复制粘贴。 |
| P-0297 | src/ops-maturity/drift-detection/evolution-integration-service.ts:180 | `Math.random()` 用于演化记录 ID：src/ops-maturity/drift-detection/evolution-integration-service.ts:180   | Fixed | 演化证据记录 ID 直接内联 `Date.now()+Math.random()`，持久化标识熵不足且难统一加固。 |
| P-0298 | src/ops-maturity/drift-detection/drift-detector-service.ts:157 | hash 比较使用 `===`：`current.hash === baseline.hash` 漂移检测 src/ops-maturity/drift-detection/drift-detector-service.ts:157   | Fixed | 根因是漂移检测直接做字符串等值比较，安全敏感路径没有走常量时间比较。本轮已改为 `timingSafeEqual` 辅助函数。 |
| P-0299 | src/ops-maturity/capacity-planner/forecaster/index.ts:1-13 | src/ops-maturity/capacity-planner/forecaster/index.ts:1-13 "Capacity Forecaster" 仅做 `usage*(1+rate%)` 指数外推，无任何统计模型/置信区间/输入校验，命名误导且 periods 巨大时触发巨量数组分配   | Fixed | 根因是 capacity forecaster 没有输入边界校验，坏参数可直接放大到数组分配与异常预测结果。本轮已补负数、非有限值、异常 growth rate 与超大 periods 校验。 |
| P-0300 | src/ops-maturity/chaos/chaos-experiment-scheduler.ts:748 | setInterval 未 cleanup：`setInterval(...)` 创建后未在错误路径保存到 disposable src/ops-maturity/chaos/chaos-experiment-scheduler.ts:748   | Fixed | continuous monitoring interval 未纳入完整生命周期管理，长生命周期 timer 会持续占住事件循环。 |
| P-0301 | src/ops-maturity/workflow-debugger/health-monitor.ts:119 | src/ops-maturity/workflow-debugger/health-monitor.ts:119 用 ISO 字符串字典序比较时间窗，若任一探针 timestamp 带时区后缀（如 +08:00），比较错误，窗口过滤会漏/误算   | Fixed | 时间窗过滤直接比较时间字符串而非归一化后的真实时间点，混合时区偏移时会误判是否落窗。 |
| P-0302 | src/ops-maturity/drift-detection/cross-agent-analyzer/index.ts:402 | src/ops-maturity/drift-detection/cross-agent-analyzer/index.ts:402 `n<3` 时用 `z-score>1.25` 作显著阈值，与 alpha=0.05 两条路径标准不一致（"显著差异"成为玄学可调参数）   | Fixed | 根因是小样本显著性和 bootstrap 路径用了两套不一致阈值。本轮已把小样本分支收敛到确定性的偏差阈值逻辑。 |
| P-0303 | src/ops-maturity/chaos/chaos-experiment-scheduler.ts:471-489 | src/ops-maturity/chaos/chaos-experiment-scheduler.ts:471-489 `simulateRollbackAction` 是 placeholder（仅 setTimeout 100ms 后打日志），生产环境 chaos 实验"回滚"不执行任何业务恢复   | Fixed | 根因是 chaos scheduler 把 rollback 固定成占位实现。本轮已引入可插拔 `rollbackActionExecutor`，默认路径也具备可中断执行语义。 |
| P-0304 | src/ops-maturity/drift-detection/evolution-integration-service.ts:180,218 | src/ops-maturity/drift-detection/evolution-integration-service.ts:180,218 同一文件两次复制粘贴 `id: \`ev_${Date.now()}_${Math.random()...`` 内联 ID 生成，未抽象 utility   | Fixed | 证据 ID 生成逻辑被复制粘贴在多个分支里，导致弱随机方案和修复策略无法统一收口。 |
| P-0305 | src/ops-maturity/drift-detection/learning/evidence-store.ts:121 | 证据记录读出/返回前用 `JSON.parse(JSON.stringify(record))` 整体克隆，evidence record 可能很大；高频调用下成为 GC 压力点 src/ops-maturity/drift-detection/learning/evidence-store.ts:121   | Fixed | evidence record 返回前仍使用 JSON 深拷贝。 |
| P-0306 | src/ops-maturity/workflow-debugger/health-monitor.ts:121-136 | src/ops-maturity/workflow-debugger/health-monitor.ts:121-136 窗口内无任何探针时 `getSnapshot` 返回 `status:"healthy", healthScore:100`，组件长时间未上报反而显示完全健康（应为 "unknown/stale"）   | Reviewed and Closed | 当前 health monitor 明确把空窗口定义为“无近期样本的中性基线”，同时通过 `totalProbes=0`/`oldestProbeInWindow=null` 暴露无数据状态；属语义选择而非实现错误。 |
| P-0307 | src/ops-maturity/drift-detection/cross-agent-analyzer/index.ts:416-417 | src/ops-maturity/drift-detection/cross-agent-analyzer/index.ts:416-417 bootstrap 分位用 `Math.floor(alpha/2*100)=2` 取下界，2.5% 分位被定到第 2 个样本，长尾下 `lower==bootstrapMeans[2]` 系统性偏高   | Fixed | 根因是 bootstrap 分位直接用整数桶下标截断。本轮已改为插值提取分位点，消除系统性偏高。 |
| P-0308 | src/ops-maturity/drift-detection/cross-agent-analyzer/index.ts:412 | src/ops-maturity/drift-detection/cross-agent-analyzer/index.ts:412 bootstrap 显著性检验直接用 `Math.random`，结果不可复现，对相同输入 `isSignificantlyDifferent` 多次调用结论可能不一致（审计困难）   | Fixed | 根因是 bootstrap 抽样依赖全局 `Math.random`。本轮已改为固定种子的确定性采样器，保证同输入可复现。 |
| P-0309 | src/ops-maturity/chaos/chaos-experiment-scheduler.ts:431-442 | src/ops-maturity/chaos/chaos-experiment-scheduler.ts:431-442 `executeRollbackWithTimeout` 用 setTimeout 标超时但无 AbortController，超时后底层 actions 仍继续运行并最终触发第二次 `completeRollbackActions("completed")` 与超时分支竞争   | Fixed | 根因是 rollback 超时只标状态，不会中断底层动作，也没有单次完成保护。本轮已引入 `AbortController`、终态去重和 runId 防双完成。 |
| P-0310 | src/ops-maturity/chaos/chaos-experiment-scheduler.ts:748-782 | src/ops-maturity/chaos/chaos-experiment-scheduler.ts:748-782 `startContinuousMonitoring` 用 `setInterval+void async` 包装，evaluator 慢于 intervalMs 时出现并发 evaluator，多次 `recordContinuousSteadyStateSample` 抢写同一时间槽   | Fixed | 根因是 continuous monitoring 用 `setInterval` 叠加异步 evaluator，慢调用会重叠。本轮已改为链式 `setTimeout` 调度，避免并发重入。 |
| P-0311 | src/org-governance/org-model/org-governance-saga.ts:290,327,364 | 错误吞噬：`} catch {` 在 SAGA 补偿处静默 src/org-governance/org-model/org-governance-saga.ts:290,327,364   | Fixed | 根因是 SAGA 补偿阶段把 handler 失败吞掉，破坏 fail-close 语义。本轮已在补偿后显式抛出 `org_governance_saga.compensation_failed:*`。 |
| P-0312 | src/org-governance/approval-routing/approval-routing-service.ts:82-91 | src/org-governance/approval-routing/approval-routing-service.ts:82-91 `fxRatesToCny` 默认 USD=7.2 写死、永不刷新，跨境审批金额折算长期使用陈旧汇率，超阈值判定失真   | Fixed | 审批路由把汇率表硬编码在服务默认值里，没有显式配置入口和快照来源记录。 |
| P-0313 | src/org-governance/org-model/org-governance-saga.ts:290,327,364（P-0311）三处 | src/org-governance/org-model/org-governance-saga.ts:290,327,364（P-0311）三处 SAGA 补偿吞错，违反 saga 必须显式失败的契约   | Fixed | 根因是补偿失败只记录而不外抛，使 saga 看起来已恢复。本轮已在执行剩余补偿后统一上抛失败，保持 saga 失败可观测。 |
| P-0314 | src/org-governance/delegated-governance/stores/index.ts:152-156,229 | `JSON.parse` 无 try/catch：直接解析 row 字段（多列）src/org-governance/delegated-governance/stores/index.ts:152-156,229   | Fixed | 根因是 delegated governance store 直接 `JSON.parse` 持久化列，坏数据会炸出非受控异常。本轮已补安全解析助手并返回命名化错误码。 |
| P-0315 | src/org-governance/sso-scim/api-key-service.ts:187 | src/org-governance/sso-scim/api-key-service.ts:187 `rotateApiKey` 把旧 key 标 `revoked` 但未写 `auditLog`，密钥轮换是静默撤销   | Fixed | 轮换路径只改状态，不补撤销审计和 `revokedAt/revokedBy` 元数据。 |
| P-0316 | src/org-governance/org-model/sync/index.ts:31 | 自由文本 `throw new Error("Cannot build OrgChart: no root node found")` src/org-governance/org-model/sync/index.ts:31   | Fixed | OrgChart 构造缺少根节点时直接抛裸 Error。 |
| P-0317 | src/org-governance/org-model/org-governance-saga.ts:228,231,237 | 错误码风格不一致：`missing_prepare_handler` 等无命名空间前缀，与同仓 `org_hierarchy.*`、`saml.*` 混用 src/org-governance/org-model/org-governance-saga.ts:228,231,237   | Fixed | 根因是 org governance saga 仍沿用裸错误码。本轮已统一为 `org_governance_saga.missing_*_handler` 命名空间。 |
| P-0318 | src/org-governance/sso-scim/oidc/oidc-service.ts:646 | SSRF 面：oidc-service `fetch(url, ...)` 用于 OIDC 元数据/JWKS 拉取，无 IP/主机限制 src/org-governance/sso-scim/oidc/oidc-service.ts:646   | Fixed | 根因是 OIDC 出网请求此前直接透传 provider URL，没有经过统一 outbound URL 校验。本轮已接入 `parseSafeOutboundUrl()`，对 loopback/内网地址 fail-close，并补了定向回归。 |
| P-0319 | src/org-governance/sso-scim/scim-dlq-reconciliation.ts:16-30 | src/org-governance/sso-scim/scim-dlq-reconciliation.ts:16-30 "Reconcile" 只做了三次 filter+map 即返回，没有任何重排队/去重/上游对账逻辑（函数名与功能严重不符）   | Reviewed and Closed | 根因是评审按函数名预期了 replay/repair 语义；当前 `ScimDlqReconciliationService` 的契约和现有测试都只定义分类、汇总与报表，不承担自动重放。 |
| P-0320 | src/org-governance/sso-scim/api-key-service.ts:202-203 | src/org-governance/sso-scim/api-key-service.ts:202-203 `hashKey` 用未加盐 SHA-256，对低熵/已泄漏旧 key 可批量重放彩虹表（应使用 HKDF 或带租户盐的 keyed hash）   | Fixed | 根因是 API key 索引直接用原始 SHA-256，缺少 keyed hash 和租户绑定。本轮已改为带 pepper、tenant 作用域的 HMAC-SHA256。 |
| P-0321 | src/org-governance/approval-routing/approval-routing-service.ts:104-141 | src/org-governance/approval-routing/approval-routing-service.ts:104-141 捕获 `empty_approver_chain` 后用 `fallbackApproverIds`（默认空数组）兜底，最终又在 148-149 行抛出，catch 分支无恢复能力，徒增复杂度   | Fixed | 空审批链场景先走“伪恢复”分支再在出口处抛错，失败语义没有在路由边界上直接收口。 |
| P-0322 | src/org-governance/sso-scim/api-key-service.ts:70 | src/org-governance/sso-scim/api-key-service.ts:70 `auditLog` 仅记录 revoke，缺少 generate/rotate/validate-fail/expire 等关键事件，不满足合规审计   | Fixed | API key 审计只覆盖 revoke，一整条密钥生命周期没有完整事件链。 |
| P-0323 | src/org-governance/sso-scim/api-key-service.ts:130-133 | src/org-governance/sso-scim/api-key-service.ts:130-133 `validateApiKey` 副作用把过期 key 改成 `expired` 但未同步删除 `keyHashIndex`，下次同 hash 输入仍命中 stale 索引并被反复"刚刚过期"   | Fixed | 根因是 key 过期状态和 hash 索引清理分离，stale index 会反复命中过期记录。本轮已在 expire/revoke/rotate 路径同步清理索引，并补重复 expire 审计回归。 |
| P-0324 | src/org-governance/compliance-engine/evidence-collector.ts:318,323 | setTimeout 未 cleanup：`setTimeout(runCollection, schedule.intervalMs)` 在错误路径未保存所有句柄 src/org-governance/compliance-engine/evidence-collector.ts:318,323   | Fixed | 周期采集递归定时器缺少统一的生命周期管理，长期运行会持续占住事件循环。 |
| P-0325 | src/org-governance/approval-routing/approval-routing-service.ts:194,213-215 | src/org-governance/approval-routing/approval-routing-service.ts:194,213-215 `planChain` conditional 模式把 `conditionalApproverIds` 直接拼到 `approverChain` 末尾，未校验这些用户是否在组织节点权限范围内 → 可注入绕规审批人   | Fixed | 根因是 conditional approver 只做追加，没有和 route/org scope 对齐校验。本轮已限制到允许集合并对越界 approver fail-close。 |
| P-0326 | src/org-governance/sso-scim/api-key-service.ts:121 | src/org-governance/sso-scim/api-key-service.ts:121 当 `tenantId==null` 且 keyHash 在多租户都有命中时直接拒绝，但缺乏告警/审计 → 攻击者可借此枚举密钥跨租户存在性   | Fixed | 多租户歧义校验虽已 fail-close，但失败分支没有留下审计事件。 |
| P-0327 | src/org-governance/sso-scim/scim-sync/scim-service.ts:241 | PII 模式：scim-service 以 `${tenantId}:${email.toLowerCase()}` 作为索引键缓存于内存 src/org-governance/sso-scim/scim-sync/scim-service.ts:241   | Fixed | 根因是内存索引键直接拼租户和邮箱，PII 以明文形式驻留。本轮已改为摘要化 email index key。 |
| P-0328 | src/org-governance/sso-scim/api-key-service.ts:189-195 | src/org-governance/sso-scim/api-key-service.ts:189-195 `rotateApiKey` 调 `generateApiKey` 时丢掉了 `existing.tenantId`，新生成的轮换密钥落到默认 `tenant:global` → 把密钥从原租户搬走（多租户隔离破洞）   | Fixed | 轮换路径复用 `generateApiKey()` 时没有把旧记录的 `tenantId` 继续传下去。 |
| P-0329 | src/org-governance/approval-routing/approval-routing-service.ts:236-243 | src/org-governance/approval-routing/approval-routing-service.ts:236-243 `buildDelegationMap` 只取 owners 一层委托，未限制委托链最大深度，A→B→C→A 形成环时 `resolveDelegatedApprover` 行为依赖其内部实现 → 死循环或无限委托风险   | Reviewed and Closed | 根因是评审把一跳 delegation map 误读成递归委托链；当前 `buildDelegationMap()` 和 `resolveDelegatedApprover()` 都不做递归展开，现实现不存在环形死循环。 |
| P-0330 | src/org-governance/sso-scim/scim-sync/scim-service.ts:766,772,778,782,792,798,804,808,829,840,851,902,919,938,947,969 | 错误码字面量不一致：`bulk.user_not_found` / `bulk.group_not_found` / `bulk.invalid_path` / `bulk.unsupported_method` 与同文件 `scim.*` 前缀错误码混用 src/org-governance/sso-scim/scim-sync/scim-service.ts:766,772,778,782,792,798,804,808,829,840,851,902,919,938,947,969   | Fixed | 根因是 bulk 子路径沿用历史 `bulk.*` 命名，没有跟 `scim.*` 统一。本轮已收敛到 `scim.bulk.*` 前缀。 |
| P-0331 | src/org-governance/compliance-engine/evidence-collector.ts:317、five-plane-orchestration/agent-delegation/delegation-audit-service.ts:25-28、five-plane-execution/tool-executor/shadow-snapshot-service.ts:410：EvidenceCollector | (N-1081) src/org-governance/compliance-engine/evidence-collector.ts:317、five-plane-orchestration/agent-delegation/delegation-audit-service.ts:25-28、five-plane-execution/tool-executor/shadow-snapshot-service.ts:410：EvidenceCollector / DelegationAudit / shadow-snapshot.persistRecord 全部 `writeFileSync` 整文件，无 tmp+rename 原子写 → 崩溃产生半写证据   | Reviewed and Closed | 根因是原记录把当前 correctness 缺陷和后续 durability 增强混在一起；证据采集定时器已加生命周期收口，而原子写增强已在总审计 `N-0616/N-1081` 归并跟踪。 |
| P-0332 | src/scale-ecosystem/federation/federation-gateway.ts:581 | src/scale-ecosystem/federation/federation-gateway.ts:581 emit `federation-event` 含连字符，违反注册表事件命名风格   | Fixed | 联邦网关事件名沿用局部命名习惯，没有和全局注册表命名规则对齐。 |
| P-0333 | src/scale-ecosystem/integration/connector-runtime/index.ts:46 | 错误吞噬：`} catch {` 静默忽略 src/scale-ecosystem/integration/connector-runtime/index.ts:46   | Fixed | 回调投递路径为不阻断主流程静默吞错，但没有留下最小失败信号。 |
| P-0334 | src/scale-ecosystem/multi-region/data-replicator/index.ts:361 | `Math.random()` 用于复制事件 ID：src/scale-ecosystem/multi-region/data-replicator/index.ts:361   | Fixed | 复制事件 ID 依赖时间戳拼接弱随机数，持久化路径缺少稳定唯一标识。 |
| P-0335 | src/scale-ecosystem/runtime-services/human-takeover-service-async.ts:915 | `Math.random()` 用于操作 ID：src/scale-ecosystem/runtime-services/human-takeover-service-async.ts:915   | Fixed | 运行时操作 ID 沿用 `Date.now()+Math.random()` 模式，熵不足。 |
| P-0336 | src/scale-ecosystem/runtime-services/execution-dispatch-service-async.ts:622 | `Math.random()` 用于操作 ID：src/scale-ecosystem/runtime-services/execution-dispatch-service-async.ts:622   | Fixed | 调度操作 ID 依赖弱随机拼接，分布式追踪标识不稳。 |
| P-0337 | src/scale-ecosystem/integration/connector-framework-service.ts:452,471,485 | 错误吞噬：`} catch {` 三处连续静默 src/scale-ecosystem/integration/connector-framework-service.ts:452,471,485   | Fixed | connector 生命周期读取持久化文件时直接空 catch，局部损坏不会被观测。 |
| P-0338 | src/scale-ecosystem/runtime-services/execution-worker-writeback-service-async.ts:858 | `Math.random()` 用于操作 ID：src/scale-ecosystem/runtime-services/execution-worker-writeback-service-async.ts:858   | Fixed | writeback 操作 ID 仍使用弱随机方案，难以保证唯一性。 |
| P-0339 | src/scale-ecosystem/operations/platform-operator-service.ts:145,521 | `JSON.parse` 无 try/catch：解析 outputsJson src/scale-ecosystem/operations/platform-operator-service.ts:145,521   | Fixed | operator 输出解析直接 `JSON.parse`，损坏数据会以非受控异常外溢。 |
| P-0340 | src/scale-ecosystem/multi-region/cdc-replication-service.ts:739 | `JSON.parse` 无 try/catch：解析 cdc-replication 入站 payload src/scale-ecosystem/multi-region/cdc-replication-service.ts:739   | Reviewed and Closed | 复核确认 CDC 入站 payload 解析已有受控保护，原记录已陈旧。 |
| P-0341 | src/scale-ecosystem/tenant-platform/tenant-platform-service.ts:182-185 | src/scale-ecosystem/tenant-platform/tenant-platform-service.ts:182-185 进度百分比硬编码 `totalSteps=100` 估算，对真实步骤数 10 或 1000 的工作流抢占决策严重失真   | Reviewed and Closed | 根因是评审把候选排序启发式误判成最终配额/抢占判决；这里的 `progressPercent` 只用于预排序，真正的 quota/preemption 约束仍由调度与租户治理服务执行。 |
| P-0342 | src/scale-ecosystem/tenant-platform/tenant-platform-service.ts:193 | src/scale-ecosystem/tenant-platform/tenant-platform-service.ts:193 priority 三段映射到 100/75/50，未识别值默认 25，"unknown" 与 "low" 同等处理掩盖配置错误   | Reviewed and Closed | 根因是评审把启发式排序分数误判成配置校验边界；该 priority 映射只服务候选排序，未知值落最低优先级属于保守降级而不是静默放行。 |
| P-0343 | src/scale-ecosystem/multi-region/data-replicator/index.ts:240 | 弱算法可选：`computeChecksum(payload, algorithm: "sha256"\|"md5")` 默认参数虽为 sha256，但 API 暴露 md5 src/scale-ecosystem/multi-region/data-replicator/index.ts:240   | Fixed | 根因是 checksum API 历史上为兼容暴露了 md5 入口。本轮已把实现收敛到 sha256-only，并对非 sha256 算法 fail-close。 |
| P-0344 | src/scale-ecosystem/integration/connector-runtime/index.ts:36 | SSRF 面：connector-runtime `fetch(callbackUrl, ...)` 由请求方提供回调地址 src/scale-ecosystem/integration/connector-runtime/index.ts:36   | Fixed | 回调 URL 直接透传给 `fetch`，既无地址约束也无失败可观测性。 |
| P-0345 | src/scale-ecosystem/intelligence/perception-service.ts:256,454,488,501 | `JSON.parse` 无 try/catch：解析 perception-service 推荐动作 src/scale-ecosystem/intelligence/perception-service.ts:256,454,488,501   | Fixed | brief/proposal/export 等解析流直接 `JSON.parse`，没有统一安全数组解析。 |
| P-0346 | src/scale-ecosystem/runtime-services/durable-event-bus-async.ts:673 | fire-and-forget Promise：`void this.flushBatch();` 无 catch src/scale-ecosystem/runtime-services/durable-event-bus-async.ts:673   | Fixed | 定时批量 flush 直接 fire-and-forget，异常无法回传也没有 stderr 提示。 |
| P-0347 | src/scale-ecosystem/multi-region/region-health-check-service.ts:435 | SSRF 面：region-health-check 直接 `fetch(${url}/health)`，url 来自配置无白名单 src/scale-ecosystem/multi-region/region-health-check-service.ts:435   | Fixed | 区域健康检查端点未经过 URL 校验即发起探测请求。 |
| P-0348 | src/scale-ecosystem/marketplace/pack-security-service.ts:668-686 | src/scale-ecosystem/marketplace/pack-security-service.ts:668-686 用 `/password\|secret\|token/i` 之类正则扫 `capturedLogs` 后置标记敏感数据，是 post-hoc 字符串匹配，对真实泄露无防御作用   | Reviewed and Closed | 根因是评审把日志敏感词扫描误判成主防线；当前 pack 安全主防线已收敛到源码/能力/运行时 fail-close 守门，`capturedLogs` 扫描只是附加告警信号。 |
| P-0349 | src/scale-ecosystem/multi-region/data-replicator/index.ts:121 | 弱算法可选：data-replicator 允许 `checksumAlgorithm: "sha256" \| "md5"`，调用方可指定 md5 进行完整性校验 src/scale-ecosystem/multi-region/data-replicator/index.ts:121   | Fixed | 根因是 data-replicator 配置面曾暴露 md5 选项，弱摘要可直接进入完整性链路。本轮已把配置类型收敛为 sha256-only。 |
| P-0350 | src/scale-ecosystem/billing/budget-reallocation-service.ts:73-104 | `idempotencyKeys = new Set<string>()` 永不淘汰，长生命周期进程会无限增长；典型幂等缓存缺失 TTL/逐出策略 src/scale-ecosystem/billing/budget-reallocation-service.ts:73-104   | Fixed | 根因是 budget reallocation 用永不回收的 `Set` 保存幂等键。本轮已改为 TTL 裁剪 `Map` 并在每次检查时清理过期键。 |
| P-0351 | src/scale-ecosystem/multi-region/data-replicator/index.ts:413-417 | src/scale-ecosystem/multi-region/data-replicator/index.ts:413-417 `deliverEventWithRetries` 成功即 ack 但目标侧无幂等去重保障，网络抖动下重试可能让目标 region 二次应用同一 event   | Fixed | 入站复制事件缺少 `eventId` 去重，重试成功后的重复到达会重复应用。 |
| P-0352 | src/scale-ecosystem/multi-region/region-health-check-service.ts:144： | (N-1141) src/scale-ecosystem/multi-region/region-health-check-service.ts:144：`unregisterRegion` 删除 `circuitBreakers` map 引用却不调用 `cb.reset()`/释放计时器 → 计时器泄漏   | Fixed | region 注销只删除 map 引用，没有主动重置 circuit breaker 内部状态。 |
| P-0353 | src/scale-ecosystem/federation/federation-gateway.ts:375,378,383,431 | 自由文本 `throw new Error("Source or target organization not found")`（trust 治理层错误码缺失）src/scale-ecosystem/federation/federation-gateway.ts:375,378,383,431   | Fixed | federation trust 治理分支缺少带 code 的结构化错误。 |
| P-0354 | src/scale-ecosystem/runtime-services/durable-event-bus-async.ts:298 | 自由文本 `throw new Error("Circuit breaker is open - event publishing temporarily disabled")` src/scale-ecosystem/runtime-services/durable-event-bus-async.ts:298   | Fixed | circuit breaker 打开时直接抛裸 Error，缺少统一错误分类。 |
| P-0355 | src/scale-ecosystem/multi-region/data-replicator/index.ts:478-483 | src/scale-ecosystem/multi-region/data-replicator/index.ts:478-483 `handleIncomingEvent` 收到事件后直接 `handler(event)`，从未调用 `validateEvent` 校验 checksum → 跨区复制信道完整性可绕过   | Fixed | 入站复制处理直接执行 handler，绕过 checksum 校验。 |
| P-0356 | src/scale-ecosystem/integration/connector-framework-service.ts:452,471,485 | src/scale-ecosystem/integration/connector-framework-service.ts:452,471,485 三处连续 `} catch {}` 出现在 connector lifecycle 关键路径，已在 P-0337 提及；未对 partial failure 做补偿/告警   | Fixed | connector 恢复链路把 partial failure 静默吞掉，缺最小告警与诊断输出。 |
| P-0357 | src/scale-ecosystem/marketplace/pack-security-service.ts:553-555 | src/scale-ecosystem/marketplace/pack-security-service.ts:553-555 `sandboxApi.String` 把 split/substring/trim 直接当函数挂出去（非 prototype 方法），调用时 this 丢失会抛 TypeError，正常 pack 也跑不动   | Fixed | pack 安全服务试图靠伪沙箱暴露宿主内建对象，既不安全也破坏合法 pack 运行时语义。 |
| P-0358 | src/scale-ecosystem/federation/capability-delegation.ts:686 | 对每个 `def of capability.constraints` 调 `overrides.find((o) => o.type === def.type)`，应改为 `Map<type, override>` 一次构建 src/scale-ecosystem/federation/capability-delegation.ts:686   | Fixed | 约束覆盖在循环里反复线性查找 override，热点路径复杂度不必要放大。 |
| P-0359 | src/scale-ecosystem/marketplace/marketplace-governance-service.ts:268,356,690-692 | `JSON.parse` 无 try/catch：marketplace-governance-service 多次解析 permissionsJson/compatibilityJson src/scale-ecosystem/marketplace/marketplace-governance-service.ts:268,356,690-692   | Fixed | 持久化 JSON 字段读取沿用裸 `JSON.parse`，损坏数据会以非受控异常外溢。 |
| P-0360 | src/scale-ecosystem/multi-region/failover-reconciliation-job.ts:96,213 | `private readonly history: ReconciliationScanResult[] = []` 在每次 scan 后 `history.push(result)` 但无裁剪，长期运行将 OOM src/scale-ecosystem/multi-region/failover-reconciliation-job.ts:96,213   | Fixed | reconciliation scan 历史结果只增不减，后台任务缺少保留窗口上限。 |
| P-0361 | src/scale-ecosystem/marketplace/pack-security-service.ts:597-598 | src/scale-ecosystem/marketplace/pack-security-service.ts:597-598 `__addNetworkAttempt/__addFileAccess` 是注入到 context 的回调，但同时把 fetch/setTimeout 全置 undefined → 没有任何代码会调用这些回调，"攻击检测"统计永远为 0   | Fixed | 攻击统计依赖永远不会触发的伪沙箱 hook，导致检测面与实际执行路径脱节。 |
| P-0362 | src/scale-ecosystem/multi-region<br>deploy/terraform/environments/multi-region | src/scale-ecosystem/multi-region 与 deploy/terraform/environments/multi-region 多区域目标，但 main.tf:11-19 backend "s3" key 单一 → 多 region terraform state 共池（与 P-0588 形成"代码声称多区域、IaC 不可多区域"矛盾）   | Fixed | 多区域 overlay 已存在，但 backend key 策略未隔离，primary/secondary 状态容易共池。 |
| P-0363 | src/scale-ecosystem/marketplace/pack-security-service.ts:119,671 | src/scale-ecosystem/marketplace/pack-security-service.ts:119,671 自身用 regex `/child_process.*shell.*true/i`、`/process\|child_process\|exec/i` 检测 pack 代码——这是 pre-runtime 启发式，没用 AST，绕过路径多   | Fixed | pack 安全判定原先建立在伪运行时沙箱和过窄启发式之上，既不可靠也不能 fail-close。 |
| P-0364 | src/scale-ecosystem/multi-region/cdc-replication-service.ts:647-651 | `recordConflict` 中 `history.slice(-100)` 仅按 taskId 子表裁剪，`conflictHistory: Map<taskId, ...>` 自身无界；持续生成新 taskId 会泄漏 src/scale-ecosystem/multi-region/cdc-replication-service.ts:647-651   | Fixed | CDC 冲突历史只裁剪子数组，外层 task map 缺容量上限。 |
| P-0365 | src/scale-ecosystem/tenant-platform/tenant-platform-service.ts:131-151 | src/scale-ecosystem/tenant-platform/tenant-platform-service.ts:131-151 `evaluateSlo` 仅按 sloId 全局查找，命中第一个匹配 SLO 即 return → 两个租户定义相同 sloId 会泄露另一租户配置（租户隔离破洞）   | Fixed | SLO 评估按全局 `sloId` 查找，忽略了租户作用域。 |
| P-0366 | src/scale-ecosystem/marketplace/pack-security-service.ts:531 | src/scale-ecosystem/marketplace/pack-security-service.ts:531 暴露 JSON.parse/stringify 后，恶意 pack 用 toJSON 回调即可拿到外层闭包 → 沙箱白名单形同虚设   | Fixed | 把宿主 JSON 回调暴露给 vm 上下文，本质上把闭包能力带进了所谓沙箱。 |
| P-0367 | src/scale-ecosystem/runtime-services/execution-worker-writeback-service-async.ts:698,722 | setInterval 句柄释放路径不完整：`this.batchFlushTimer = setInterval(...)` 在多处 dispose 路径条件分支不一致 src/scale-ecosystem/runtime-services/execution-worker-writeback-service-async.ts:698,722   | Reviewed and Closed | 复核确认 coalescing/batch 两个 interval 现已统一 `unref()`，且 `dispose()` 会对两个句柄执行 `clearInterval()`；原释放路径不一致记录已过时。 |
| P-0368 | src/scale-ecosystem/sla/index.ts:1<br>src/scale-ecosystem/sla-engine/ | `src/scale-ecosystem/sla/index.ts:1` 仅一行 `export * from "../sla-engine/index.js"`，与同级 `src/scale-ecosystem/sla-engine/`(实际实现) 形成单复数双目录。   | Reviewed and Closed | `scale-ecosystem/sla/index.ts` 当前只是兼容门面，实际实现权威入口仍在 `sla-engine/`；属兼容出口而非重复实现。 |
| P-0369 | src/scale-ecosystem/marketplace/pack-security-service.ts:518-524 | src/scale-ecosystem/marketplace/pack-security-service.ts:518-524 `sandboxApi.Date` 是普通对象不是 Date 类，pack 中 `new Date()` 失败，沙箱不能运行任何依赖时间的合法代码   | Fixed | 伪沙箱把宿主内建对象改造成不兼容的假实现，导致合法 pack 代码也无法运行。 |
| P-0370 | src/scale-ecosystem/multi-region/region-health-check-service.ts:183： | (N-1171) src/scale-ecosystem/multi-region/region-health-check-service.ts:183：`checkRegion` 用 `circuitBreaker.execute(...)` 包裹 `performHealthCheck`，但 `latencyMs = Date.now()-startTime` 计算包含 circuit breaker 等待时间 → 区域延迟指标低估   | Fixed | 区域延迟指标错误地复用了 breaker 包装时长，而不是健康探针自身测得的延迟。 |
| P-0371 | src/scale-ecosystem/multi-region/data-replicator/index.ts:415-444 | src/scale-ecosystem/multi-region/data-replicator/index.ts:415-444 `acknowledge` 后 `lastSequence++` 仅是本批计数，写到 `checkpoint.sequenceNumber` 不是源端真实序号，重启或并发 flush 后 checkpoint 倒退   | Fixed | checkpoint 序号使用本批成功计数而非源事件真实序号。 |
| P-0372 | src/scale-ecosystem/multi-region/per-tenant-encryption.ts:54： | (N-1111) src/scale-ecosystem/multi-region/per-tenant-encryption.ts:54：`PerTenantEncryptionService` 把 KEK/DEK 直接放进进程内 `Map<keyId, Buffer>`，重启即丢、无 KMS/HSM 后端，与 `enforceHardwareSecurityModule` 字段语义直接矛盾   | Reviewed and Closed | 根因是评审把基线内存实现和外部 KMS/HSM 接线能力混为一项 defect；该缺口已在总审计 `N-1111/N-1112` 归并为后端集成边界，而不是当前仓内 correctness 回归。 |
| P-0373 | src/scale-ecosystem/marketplace/pack-security-service.ts:119,671 | src/scale-ecosystem/marketplace/pack-security-service.ts:119,671 静态扫描 pack 用 regex `/child_process.*shell.*true/i`、`/process\|child_process\|exec/i`（P-0363），无 AST，遇到 base64/加密字符串、间接访问 `globalThis["child_process"]` 都会绕过   | Fixed | 安全服务过度依赖窄正则和伪沙箱，缺少 fail-close 的源码级风险判定。 |
| P-0374 | src/scale-ecosystem/runtime-services/execution-dispatch-service-async.ts:340-352 | `signal?.addEventListener("abort", () => {...})` 未带 `{ once: true }` 也无对应 `removeEventListener`，每次 retry/超时调用均挂一个 listener；当 signal 复用，监听器无界增长 + 触发 Node 默认 maxListeners 警告 src/scale-ecosystem/runtime-services/execution-dispatch-service-async.ts:340-352   | Fixed | abort 监听器按请求叠加注册，却没有在终态清理。 |
| P-0375 | src/scale-ecosystem/cost-attribution/index.ts:1-7 | `src/scale-ecosystem/cost-attribution/index.ts:1-7` 跨大区块反向 re-export `../../ops-maturity/cost-optimizer/cost-optimization-service.js`，scale-ecosystem 反依赖 ops-maturity。   | Reviewed and Closed | 当前 `cost-attribution/index.ts` 是对 canonical cost optimizer 的兼容导出面，属于命名空间桥接而非反向复制实现。 |
| P-0376 | src/scale-ecosystem/operations/<br>src/scale-ecosystem/runtime-services/<br>src/scale-ecosystem/runtime-governance-service.ts<br>src/scale-ecosystem/scale-bootstrap.ts | `src/scale-ecosystem/operations/`(2 文件) 与 `src/scale-ecosystem/runtime-services/`、`src/scale-ecosystem/runtime-governance-service.ts`、`src/scale-ecosystem/scale-bootstrap.ts` 之间运维/运行边界含糊，`platform-operator-service.ts` 又同名出现在 `marketplace/` 下。   | Reviewed and Closed | 该条主要是目录 inventory 与命名观感问题；scale runtime/operations/bootstrap 边界已在架构文档登记，`marketplace/platform-operator-service.ts` 现也是兼容 re-export。 |
| P-0377 | src/scale-ecosystem/tenant-platform/tenant-platform-service.ts | src/scale-ecosystem/tenant-platform/tenant-platform-service.ts ~41 KB 单文件 + 同目录 `tenant-platform-service-async.ts` 双套（P-0142、P-0386），合计 80 KB+ 同主题   | Reviewed and Closed | 这是架构参考文档已登记的 sync/async 镜像与大文件维护债，属于已知 inventory 问题，不再重复登记为新 defect。 |
| P-0378 | src/scale-ecosystem/tenant-platform/tenant-platform-service.ts:102-105 | src/scale-ecosystem/tenant-platform/tenant-platform-service.ts:102-105 通过 `getNoisyNeighborProtectionService()/getPerTenantEncryptionService()` 取全局单例，但 `ResourcePoolService/FairSchedulingService` 是 new 出的实例化对象 → 跨租户共享与独立两套策略并存，隔离语义矛盾   | Reviewed and Closed | 这里是控制面全局策略单例与运行时资源调度实例的刻意分层，单例承载共享治理规则、实例承载本服务局部状态，并不直接构成租户隔离破洞。 |
| P-0379 | src/scale-ecosystem/multi-region/data-replicator/index.ts:361,531 | src/scale-ecosystem/multi-region/data-replicator/index.ts:361,531 `eventId: \`repl_${Date.now()}_${Math.random().toString(36).slice(2)}``用`Math.random` 生成事件 ID，跨 region 高并发下碰撞概率非可忽略；且 eventId 进入持久化路径   | Fixed | 复制事件 ID 依赖时间戳拼接弱随机数，持久化路径缺少稳定唯一标识。 |
| P-0380 | src/scale-ecosystem/feedback-loop/analyzer/index.ts:3、src/scale-ecosystem/feedback-loop/collector/signal-preprocessor.ts:5 | src/scale-ecosystem/feedback-loop/analyzer/index.ts:3、src/scale-ecosystem/feedback-loop/collector/signal-preprocessor.ts:5 等 `export interface` 块在仅含 1-2 字段下被声明在 index.ts，barrel 中混杂类型与运行时导出   | Reviewed and Closed | 这些文件当前同时承担 tiny surface type 与 runtime export，属于轻量门面风格选择，不构成行为错误或重复实现。 |
| P-0381 | src/scale-ecosystem/runtime-services/execution-worker-writeback-service-async.ts:721-725 | `startBatchFlushTimer` 同样 `setInterval(() => this.flushBatch())` 无 reentrancy guard 在 timer 层；依赖 `flushBatch` 内部 `batchFlushPromise` 串行化，但 promise 期间仍会持续 enqueue + interval 不必要触发 src/scale-ecosystem/runtime-services/execution-worker-writeback-service-async.ts:721-725   | Fixed | flush timer 未在入口层跳过已有批处理，空转 tick 会持续重入调度。 |
| P-0382 | src/scale-ecosystem/multi-region/data-replicator/index.ts:361 | src/scale-ecosystem/multi-region/data-replicator/index.ts:361 `eventId` 用 `Date.now()+Math.random()` 拼接，毫秒级并发下重复概率非零；同 ms 内两条事件落入相同 ID → outbox 去重错误丢事件   | Fixed | `eventId` 由 `Date.now()+Math.random()` 生成，毫秒级并发下重复概率非零。 |
| P-0383 | src/scale-ecosystem/runtime-services/<br>src/platform/five-plane-execution/* | `src/scale-ecosystem/runtime-services/` 整个目录(`durable-event-bus.ts:1`、`execution-dispatch-service.ts:1` 等) 只对 `src/platform/five-plane-execution/*`、`five-plane-state-evidence/*` 做 re-export，scale 层反向暴露平面内部。   | Reviewed and Closed | `runtime-services/` 是 runtime-services 抽取后的兼容导出层，架构文档已把这类 shim 记录为迁移兼容面，而非重复实现。 |
| P-0384 | src/scale-ecosystem/runtime-services/{execution-dispatch-service-async,human-takeover-service-async,execution-worker-handshake-service-async,execution-worker-writeback-service-async}.ts | src/scale-ecosystem/runtime-services/{execution-dispatch-service-async,human-takeover-service-async,execution-worker-handshake-service-async,execution-worker-writeback-service-async}.ts 同模式 `op_${Date.now()}_${Math.random().toString(36).slice(2,11)}` 9 字符 base36 → 仅 ~46 bits，远低于 UUIDv4 122 bits，分布式追踪可碰撞   | Fixed | 多个 async runtime service 复用 `Date.now()+Math.random` 生成操作 ID，熵不足。 |
| P-0385 | src/scale-ecosystem/marketplace/pack-security-service.ts:627 | src/scale-ecosystem/marketplace/pack-security-service.ts:627 `vm.runInContext` 在 Node 文档明确不是安全沙箱，包恶意代码可用 `this.constructor.constructor("return process")()` 逃逸 → 整个 pack 安全服务是表演   | Fixed | pack 安全服务错误地把 `vm.runInContext` 当成安全边界。 |
| P-0386 | src/scale-ecosystem/billing/billing-service-async.ts:50、tenant-platform/tenant-platform-service-async.ts:40、tenant-platform/data-plane-flow-service-async.ts:41、ops-maturity/drift-detection/evolution-mvp-service-async.ts:40、scale-ecosystem/intelligence/perception-service-async.ts:41<br>package.json:5 | src/scale-ecosystem/billing/billing-service-async.ts:50、tenant-platform/tenant-platform-service-async.ts:40、tenant-platform/data-plane-flow-service-async.ts:41、ops-maturity/drift-detection/evolution-mvp-service-async.ts:40、scale-ecosystem/intelligence/perception-service-async.ts:41 全部以 `const { X } = require("./X.js")` 在 ESM 文件中混用 CommonJS require，违反 package.json:5 `"type": "module"`，靠 Node 兼容层运行   | Reviewed and Closed | 复核确认这些 async wrapper 已改为 ESM import/SyncBackedAsyncService，不再混用 CommonJS `require()`。 |
| P-0387 | src/scale-ecosystem/runtime-services/execution-worker-handshake-service-async.ts:653-658 | `setInterval(() => this.flushBatch(), ...)` 不等待 `flushBatch`（async）完成，无 in-flight 标志；与 `durable-event-bus-async.ts:681-686` 同模式（后者也是 async flushBatch）—可能并发 splice 造成乱序/重复处理（durable-event-bus 内部 626-636 行虽有 `batchProcessingPromise` 兜底，但 handshake 服务无此守卫）src/scale-ecosystem/runtime-services/execution-worker-handshake-service-async.ts:653-658   | Fixed | handshake 批量 flush 定时器没有入口重入守卫，也缺失败日志。 |
| P-0388 | src/scale-ecosystem/marketplace/ | `src/scale-ecosystem/marketplace/`(`billing-service.ts`、`tenant-platform-service.ts`、`execution-dispatch-service.ts`、`durable-event-bus.ts`、`data-plane-flow-service.ts`、`human-takeover-service.ts` …) 与 `scale-ecosystem/billing/`、`scale-ecosystem/tenant-platform/`、`scale-ecosystem/runtime-services/`、`platform/five-plane-execution/`、`platform/five-plane-state-evidence/events/` 主题大量重复同名服务。   | Reviewed and Closed | `marketplace/` 的大部分同名文件现已退化为兼容 shim/re-export；相关拆分已在架构文档 TD-8 中记录，不再视为重复实现缺陷。 |
| P-0389 | src/scale-ecosystem/runtime-services/{durable-event-bus-async,execution-worker-handshake-service-async,execution-worker-writeback-service-async}.ts、src/ops-maturity/chaos/chaos-experiment-scheduler.ts:748、src/interaction/dashboard/dashboard-websocket-server.ts:123、src/platform/five-plane-execution/ha/{leader-election-service.ts:589,626、wal-checkpoint-service.ts:607、stuck-run-sweeper-service.ts:490}、src/platform/five-plane-orchestration/harness/durable/sleep-scheduler.ts、src/platform/five-plane-control-plane/iam/secret-management-service.ts、config-center/config-hot-reload-service.ts、src/platform/five-plane-interface/{api/task-websocket-status-relay.ts,api/http-api-server.ts,channel-gateway/websocket-bridge.ts} | setInterval 漏 `unref()`：src/scale-ecosystem/runtime-services/{durable-event-bus-async,execution-worker-handshake-service-async,execution-worker-writeback-service-async}.ts、src/ops-maturity/chaos/chaos-experiment-scheduler.ts:748、src/interaction/dashboard/dashboard-websocket-server.ts:123、src/platform/five-plane-execution/ha/{leader-election-service.ts:589,626、wal-checkpoint-service.ts:607、stuck-run-sweeper-service.ts:490}、src/platform/five-plane-orchestration/harness/durable/sleep-scheduler.ts、src/platform/five-plane-control-plane/iam/secret-management-service.ts、config-center/config-hot-reload-service.ts、src/platform/five-plane-interface/{api/task-websocket-status-relay.ts,api/http-api-server.ts,channel-gateway/websocket-bridge.ts} 阻止进程优雅退出   | Fixed | 多个长期轮询/心跳 interval 没有统一 `unref()`，空闲进程无法优雅退出。 |
| P-0390 | src/interaction-governance-runtime-catalog.ts:32 | 错误吞噬：`} catch {` 静默忽略 src/interaction-governance-runtime-catalog.ts:32   | Fixed | 根因是运行时目录的懒注册先用 `get()` 探测是否已存在，缺失时只能靠空 catch 回退；现已改为 `registry.has()` 显式判定。 |
| P-0391 | src/interaction/dashboard/dashboard-websocket-server.ts:586 | 错误吞噬：`} catch {` 静默忽略 src/interaction/dashboard/dashboard-websocket-server.ts:586   | Fixed | 根因是 projection poller 在降频回退时只调整下一次轮询间隔，没有留下失败信号；现在回退分支会记录结构化告警日志。 |
| P-0392 | src/interaction/goal-decomposer/index.ts:800： | (N-0931) src/interaction/goal-decomposer/index.ts:800：`analyzeDependencyGraph` 在 `hasCycle` 时仍计算 longestDistance，浪费且结果未用（死代码 + 误用）   | Fixed | 依赖图分析在循环检测已失败后仍继续执行最长路径计算。 |
| P-0393 | src/interaction/ux/workflow-builder-service.ts:109,693 | `JSON.parse` 无 try/catch：解析 builderJson 直接返回 src/interaction/ux/workflow-builder-service.ts:109,693   | Fixed | 根因是持久化层把历史 `builderJson/outputsJson` 当作可信数据直接反序列化；现在解析失败或结构不合法都会被拒绝，不再把坏数据当正常工作流。 |
| P-0394 | src/interaction/autonomy/trust-scorer/index.ts:12 | src/interaction/autonomy/trust-scorer/index.ts:12 `volumeBonus` 只看 totalExecutions 不看成功率，500 次执行即便 50% 失败仍获 +10 信任加分   | Fixed | 根因是体量加分曾按执行次数直接发放；现在体量加分按 successRate 缩放，且有 incident 时直接归零。 |
| P-0395 | src/interaction/proactive-agent/event-watcher/index.ts:24-30 | `compilePattern` 每次调用 `new RegExp`，未缓存；事件流 watch 高频触发 src/interaction/proactive-agent/event-watcher/index.ts:24-30   | Fixed | event watcher 的模式匹配重复编译正则，未缓存。 |
| P-0396 | src/interaction/autonomy/trust-scorer/index.ts:11 | src/interaction/autonomy/trust-scorer/index.ts:11 `incidentPenalty=incidents*15` 不按 totalExecutions 归一化，1/10 与 1/10000 的事故率被同等扣分   | Fixed | 根因是事故惩罚以前按绝对次数扣分；现在按 `incidents / totalExecutions` 归一化，反映真实事故率。 |
| P-0397 | src/interaction/dashboard/dashboard-websocket-server.ts:562 | fire-and-forget Promise：`void this.runProjectionPoll();` 无 catch src/interaction/dashboard/dashboard-websocket-server.ts:562   | Fixed | dashboard projection poll fire-and-forget 未记录失败。 |
| P-0398 | src/interaction/nl-gateway/intent-parser/index.ts:220 | src/interaction/nl-gateway/intent-parser/index.ts:220 `parsed.intentType` 未通过白名单/Zod 校验即 cast as IntentType，模型乱写一个字符串都会顺利下发到下游分发器   | Fixed | 根因是 LLM 输出曾直接强制 cast 为 `IntentType`；现在先走白名单校验，不合法 intent 会 fail-close。 |
| P-0399 | src/interaction/dashboard/dashboard-websocket-server.ts:142 | src/interaction/dashboard/dashboard-websocket-server.ts:142 `stop()` 清空 outboundMessages 但没有先把已排队消息 flush 到客户端，关闭时未投递的事件直接丢失（无持久化兜底）   | Fixed | 根因是 shutdown/unregister 直接清空内存队列；现在只标记连接断开，不再在 stop 路径提前丢弃已排队消息。 |
| P-0400 | src/interaction/nl-gateway/intent-parser/index.ts:39： | (N-0901) src/interaction/nl-gateway/intent-parser/index.ts:39：`INTENT_CONFIDENCE_THRESHOLDS` 表硬编码 confirm/clarify/reject 阈值，不支持按 tenant/risk 调整   | Fixed | 根因是意图阈值常量写死在解析器内部；现在支持通过 override 传入租户/场景级阈值。 |
| P-0401 | src/interaction/proactive-agent/trigger-engine/index.ts:1-14 | src/interaction/proactive-agent/trigger-engine/index.ts:1-14 `resolveTriggerActionMode` 只通过 boolean+enum 决策，无租户/历史失败率/时间窗信号；critical 直接 silent_record 是危险默认（出问题无任何提示）   | Fixed | critical 触发在缺少更多上下文时默认静默记录，风险默认值过于保守。 |
| P-0402 | src/interaction/nl-gateway/intent-parser/index.ts:216-218 | src/interaction/nl-gateway/intent-parser/index.ts:216-218 当 `parsed.intentType==="task_create"` 时把 confidence 强行抬到 ≥0.82，是人为伪造置信度，下游基于阈值的自动确认会被欺骗   | Fixed | 根因是历史上为“创建任务”场景加了硬编码置信度地板；这段人为抬分逻辑已删除。 |
| P-0403 | src/interaction/dashboard/dashboard-websocket-server.ts:159-167 | src/interaction/dashboard/dashboard-websocket-server.ts:159-167 达到 maxClients 时返回 `clientId:""`，调用方若不检查仍可能在 outboundMessages map 以空字符串作 key 操作，下游错误传播   | Fixed | 根因是拒绝连接分支返回了空字符串 sentinel；现在改为显式 `rejected:max_clients` clientId，避免空 key 污染。 |
| P-0404 | src/interaction/ux/workflow-builder-service.ts:108-126 | src/interaction/ux/workflow-builder-service.ts:108-126 `loadWorkflow` 在 JSON.parse 失败时静默走 fallback 把整个 outputsJson 原样塞回 builderJson，等于把损坏数据当合法工作流定义重新加载   | Fixed | 根因是损坏存档曾走“宽容 fallback”重新装成 builder；现在 invalid shape 会返回 `null`，不再把坏 JSON 伪装成合法定义。 |
| P-0405 | src/interaction/ux/conversation-history-service.ts:309-316 | src/interaction/ux/conversation-history-service.ts:309-316 `deserializeSession` 直接 cast unknown 为 ConversationSessionRecord，无 schema 校验，存储侧 JSON 损坏时下游拿到任意结构对象   | Fixed | 根因是会话反序列化把存储 JSON 直接当成 typed record；现在逐字段校验 turn/session 结构，只接受合法对象。 |
| P-0406 | src/interaction/nl-gateway/intent-parser/index.ts:212 | src/interaction/nl-gateway/intent-parser/index.ts:212 LLM 提示词把用户原文 message 直接拼到 `Locale: ... Message: <user>`，未做模板隔离/白名单过滤 → 经典 prompt-injection 注入面   | Fixed | 根因是用户原文曾直接插入系统提示模板；现在 message 被包进 JSON envelope，并明确标记为不可信用户内容。 |
| P-0407 | src/interaction/ux/conversation-history-service.ts:291 | src/interaction/ux/conversation-history-service.ts:291 `extractSessionId` 用 `(... as Record<string,unknown>).sessionId as string`，sessionId 实际为 number/object 时把非字符串硬塞回索引层 → 混合类型 key   | Fixed | 根因是 sessionId 提取逻辑曾无条件强转；现在只接受真实字符串 sessionId。 |
| P-0408 | src/interaction/goal-decomposer/index.ts:246,250,255,261 | 错误码字面量不一致：`goal_decomposer.delegation_depth_exceeded` 与 `goal_decomposer.call_depth_exceeded`、`goal_decomposer.global_call_depth_exceeded` 同义但同位置三种不同字面量 src/interaction/goal-decomposer/index.ts:246,250,255,261   | Fixed | 同一类深度超限错误在不同分支手写了多套字面量。 |
| P-0409 | src/interaction/autonomy/trust-scorer/index.ts:27-31 | src/interaction/autonomy/trust-scorer/index.ts:27-31 `mapTrustLevelToAutonomyLevel` 把 `fully_trusted/trusted/semi_trusted` 三档全部映射到 `semi_auto`，"fully_trusted" 永远拿不到 `full_auto` 自治   | Fixed | 根因是 trust-level 到 autonomy-level 的映射表少了 fully_trusted 的独立分支；现在已映射到 `full_auto`。 |
| P-0410 | src/interaction/nl-gateway/*<br>config/runtime/default.json:16-19 | src/interaction/nl-gateway/* 全目录无 rateLimit/throttle 实现，唯一相关 config/runtime/default.json:16-19 是平台级 120 req/min，自然语言入口缺乏租户/会话维度独立限流，易被滥用打爆 LLM 预算   | Fixed | 根因是 NL 入口之前只继承平台级常数限流；现在新增 tenant/user 维度配置与入口内存限流器，超限会 fail-close。 |
| P-0411 | src/interaction/goal-decomposer/llm-plan-generator.ts:53,56,62,65,68,124,371,376 | 错误码字面量同义不一致：`goal_decomposer.invalid_llm_plan_*` 用 snake_case 而同模块 `goal_decomposer.llm_plan_response_too_large` / `goal_decomposer.llm_plan_json_too_deep` 风格不统一 src/interaction/goal-decomposer/llm-plan-generator.ts:53,56,62,65,68,124,371,376   | Fixed | 同模块错误码命名历史上混用了两套前缀风格。 |
| P-0412 | src/interaction/nl-gateway/nl-gateway-support.ts:183 | `patterns.some((pattern) => new RegExp(pattern.source, pattern.flags).test(normalized))` 在每次匹配 hot path 上重新克隆/编译 RegExp，等同于 RegExp 缓存被废弃；line:319/431 同样模式重复 src/interaction/nl-gateway/nl-gateway-support.ts:183   | Fixed | nl-gateway 多处匹配路径重复 clone 和编译 RegExp，缓存未复用。 |
| P-0413 | src/interaction/ux/workflow-builder-service.ts:14 | `src/interaction/ux/workflow-builder-service.ts:14`、`ux/conversation-history-service.ts:12`、`ux/ux-event-tracking-service.ts:11`、`autonomy/historical-metrics-provider.ts:30`、`dashboard/dashboard-projection-service.ts:12-14`、`dashboard/index.ts:3` 直接 import `platform/five-plane-state-evidence/{truth,memory,events,projections}` 私有文件路径，绕过 namespace。   | Reviewed and Closed | 根因是审查把“包对外发布边界”和“仓库内部相对依赖”混为一谈；这些交互模块都在同一代码库内，直接依赖 state-evidence 端口/仓储接口属于内部实现边界，不是发布面绕过。 |
| P-0414 | src/plugins/validators/basic-evaluator.ts:400-402,459-461 | src/plugins/validators/basic-evaluator.ts:400-402,459-461 basic-validator/basic-evaluator/scoring 三个插件 `healthCheck` 直接 `return true`，永远绿灯，运维毫无信号   | Fixed | 根因是 evaluator 及相关插件把 healthCheck 写成常量真值；现在按初始化/生命周期状态返回健康度，不再永远绿灯。 |
| P-0415 | src/plugins/builtin-plugin-registry.ts:846 | 自由文本 `throw new Error("Authentication required to load marketplace plugin")` src/plugins/builtin-plugin-registry.ts:846   | Fixed | marketplace 认证缺失分支直接抛裸 Error，缺少鉴权错误码。 |
| P-0416 | src/plugins/builtin-plugin-registry.ts:825 | 自由文本 `throw new Error("Marketplace API key is required")`，未使用 AppError + code src/plugins/builtin-plugin-registry.ts:825   | Fixed | marketplace API key 缺失时直接抛裸 Error。 |
| P-0417 | src/plugins/adapters/github-adapter.ts:222 | src/plugins/adapters/github-adapter.ts:222 未识别 action 时 default 直接 `{...params}` 回送整个参数对象，可能把 token/credential 字段泄入 payload   | Fixed | 根因是未知 action 分支此前回显了原始参数对象；现在改为抛 `github_adapter.unsupported_action:*`，不再回传敏感 params。 |
| P-0418 | src/plugins/adapters/livestream-adapter.ts:45,49 | 自由文本 `throw new Error("OBS authentication token is required/format is invalid")` src/plugins/adapters/livestream-adapter.ts:45,49   | Fixed | OBS token 校验分支直接抛裸 Error，缺少 ValidationError code。 |
| P-0419 | src/plugins/adapters/github-adapter.ts:214 | src/plugins/adapters/github-adapter.ts:214 `dispatch_workflow` 的 inputs 参数仅做 `typeof === object` 检查后透传，未限制深度/字段白名单 → 可注入未列入工作流声明的字段   | Fixed | 根因是 workflow inputs 只做了对象形状检查；现在键名和 value 类型都经过浅层白名单/规范化校验。 |
| P-0420 | src/plugins/adapters/github-adapter.ts:119-121 | src/plugins/adapters/github-adapter.ts:119-121 `healthCheck` 只返回 `policy.evaluate(...).allowed`，从不真正联通 GitHub API，对外暴露的是"配置可达"而非"上游可用"   | Fixed | 根因是旧 healthCheck 把“策略允许”误当成“适配器健康”；现在只有认证已完成、策略允许且显式提供 probe 时才返回健康。 |
| P-0421 | src/plugins/adapters/github-adapter.ts:102 | src/plugins/adapters/github-adapter.ts:102 `apiBaseUrl` 不校验 http/https 协议，调用方可传 `http://attacker.example`，HMAC 不防 MITM   | Fixed | 根因是 GitHub 适配器对 base URL 没有限定协议；现在只接受 HTTPS。 |
| P-0422 | src/plugins/adapters/github-adapter.ts:187 | src/plugins/adapters/github-adapter.ts:187 `issueNumber` 只通过 requireString 校验非空，未做 `^\d+$` 数字校验，传 `"123/comments/../../foo"` 会直接拼进 URL 形成路径注入   | Fixed | 根因是 issueNumber 曾只校验非空字符串；现在必须匹配纯数字。 |
| P-0423 | src/plugins/adapters/github-adapter.ts:127 | src/plugins/adapters/github-adapter.ts:127 `credentialFingerprint` 只取 token 前 4 字符（GitHub PAT 都以 ghp_/gho_/ghs_ 开头），所有同类 token 指纹完全相同，调试与审计无法区分密钥来源   | Fixed | 根因是凭据指纹只截取前缀；现在改为稳定哈希摘要，既不泄露原 token，又能区分来源。 |
| P-0424 | src/plugins/validators/basic-evaluator.ts:81-85 | src/plugins/validators/basic-evaluator.ts:81-85 `DEFAULT_QUALITY_SCORING_CONFIG` 中 completeness+typeAccuracy=0.8，加 suggestionPenalty=0.1 总和不为 1，质量分语义不清且因正负权重相加产生 -∞~+∞ 区间   | Fixed | 根因是正向权重和惩罚项原先混在同一线性求和里；现在正向权重归一化，建议惩罚改为有界乘法扣减。 |
| P-0425 | src/plugins/adapters/github-adapter.ts:152-154 | src/plugins/adapters/github-adapter.ts:152-154 `idempotencyKey` hash 把 raw params JSON 一起 sha256，params 内含 PII/secret 时仍参与运算，且未规范化字段顺序使等价请求产生不同 key   | Fixed | 根因是幂等键曾直接哈希原始 params；现在先做字段排序和敏感值脱敏，再生成 canonical key。 |
| P-0426 | src/plugins/retrievers/livestream-retriever.ts:39-41、asset-production-retriever.ts:39-41、growth-retriever.ts:36-38、presenters/coding-presenter.ts:16-18 | src/plugins/retrievers/livestream-retriever.ts:39-41、asset-production-retriever.ts:39-41、growth-retriever.ts:36-38、presenters/coding-presenter.ts:16-18 多个 retriever/presenter 插件 `healthCheck` 写死 `return true`，平台健康面板永远正常 → 故障无法被及早发现   | Fixed | 根因是多个内建插件沿用模板式 `return true`；现在统一按 initialize/shutdown 生命周期暴露真实健康状态。 |
| P-0427 | src/plugins/adapters/{asset-production,crm,game-dev,github,livestream}-adapter.ts | `src/plugins/adapters/{asset-production,crm,game-dev,github,livestream}-adapter.ts` 多处 (`:11-12`) `import { NetworkEgressPolicyService } from "../../platform/five-plane-control-plane/iam/network-egress-policy.js"`；`plugins/retrievers/coding-retriever.ts:6` 直入 `five-plane-execution/tool-executor/semantic-repo-map-service.js`：plugins 越界访问平面私有。   | Reviewed and Closed | 根因是审查把“内建插件”按第三方 pack SDK 处理了；这些 adapters/retrievers 属于仓库内平台能力的一部分，直接依赖 egress policy 与语义索引服务是设计内联，不是外部插件越界。 |
| P-0428 | src/testing/index.ts(21<br>src/benchmarks/index.ts(74<br>package.json | src/testing/index.ts(21 行)、src/benchmarks/index.ts(74 行) 几乎空壳（P-0429），但 package.json `exports` 未导出，调用方必须深 reach `./platform/...`，分层语义不闭合   | Fixed | 根因是 public exports 缺了 testing/benchmarks 子路径；现在 `package.json` 已显式导出这两个入口。 |
| P-0429 | src/testing/index.ts<br>src/benchmarks/index.ts<br>src/platform/structure/index.ts<br>src/platform/remote-coordination/index.ts<br>src/platform/ops-maturity/index.ts | 占位/死目录：`src/testing/index.ts`(21 行)、`src/benchmarks/index.ts`(74 行)、`src/platform/structure/index.ts`(独文件)、`src/platform/remote-coordination/index.ts`(2 行)、`src/platform/ops-maturity/index.ts`(仅 `platform-panic` 子项) — 实质未承载子模块。   | Reviewed and Closed | 根因是评审基于早期占位快照；当前 testing/benchmarks 已有对外入口，structure/remote-coordination/ops-maturity 也各自承担结构校验、session 导出和 panic surface，不再是“死目录”。 |
| P-0430 | src/index.ts:136 | 错误吞噬：`} catch {` 静默忽略 src/index.ts:136   | Fixed | 根因是 demo summary 对任务输出的 JSON 解析失败会直接静默回退；现在会输出结构化告警日志再返回原始字符串。 |
| P-0431 | src/index.ts:136、src/interaction-governance-runtime-catalog.ts:32、src/platform/prompt-engine/eval/llm-eval-service.ts:630 | src/index.ts:136、src/interaction-governance-runtime-catalog.ts:32、src/platform/prompt-engine/eval/llm-eval-service.ts:630 等多个 `} catch {}`（已在 §B 列出）共构成 60+ 处静默吞错节点，没有任何一处 emit 到 structured-logger 或 metrics counter — 形成观测黑洞   | Fixed | 根因是聚合审查把“缺失探测/宽容解析”和“静默吞错”混在了一起；本轮已把 cited 节点分别改成显式存在性检查或结构化日志回退，消除了这些代表性黑洞样本。 |
| P-0432 | src/index.ts:11-22 | `src/index.ts:11-22` 顶层 barrel 直接 reach `./platform/five-plane-control-plane/config-center/startup-env-schema.js`、`./platform/five-plane-execution/execution-engine/single-task-execution.js`，跨级深入私有文件。   | Fixed | 根因是 root 入口直接抓取了深层私有模块；现在改为经 `config-center/index.ts` 和 `execution-engine/index.ts` 公共入口导入。 |
| P-0433 | src/index.ts:11-22<br>package.json:14-46 | src/index.ts:11-22 顶层 barrel reach `./platform/five-plane-control-plane/config-center/startup-env-schema.js` 与 `./platform/five-plane-execution/execution-engine/single-task-execution.js`，与 package.json:14-46 `exports` 表声明的 subpath 边界存在冲突（barrel 暴露内部，违反 export 子路径白名单意图）   | Fixed | 根因是 root barrel 一度越过了公开 subpath；现在收敛到公共 index surface，与 exports 边界重新对齐。 |
| P-0434 | src/core/runtime/<br>MEMORY.md | MEMORY.md 提及 `src/core/runtime/` re-export 兼容层（P-0436），但 src 顶层无 `core/`（验证：`ls src/` 只看到 platform/domains/sdk/... 不含 core）   | Reviewed and Closed | 评审基于过期目录快照；当前仓库已存在 `src/core/runtime/` 兼容层。 |
| P-0435 | src/apps/、src/core/<br>src/apps/index.js"<br>src/apps<br>package.json:16 | src/apps/、src/core/ 不存在，但 package.json:16 `"./apps": "./dist/src/apps/index.js"` 显式 export — 一旦消费方 `import "automatic-agent-platform/apps"` 即解析失败（需验证 dist 是否生成 src/apps，否则发布即破窗）   | Reviewed and Closed | 根因是原审查基于过期目录快照；当前 `src/apps/` 与 `src/core/runtime/` 已存在，`./apps` export 对应真实入口。 |
| P-0436 | src/core/runtime/<br>src/platform、src/domains、…）相矛盾<br>MEMORY.md<br>AGENTS.md | MEMORY.md 提及 `src/core/runtime/` 仅作为 re-export 兼容层，但 src 顶层并无 `core/` 目录，与 AGENTS.md 描述（src/platform、src/domains、…）相矛盾   | Reviewed and Closed | 评审基于过期目录快照；当前仓库已存在 `src/core/runtime/` 兼容层。 |
| P-0437 | src/{domains,interaction,ops-maturity,org-governance,scale-ecosystem}/ | `architecture-remediation.ts` 在 `src/{domains,interaction,ops-maturity,org-governance,scale-ecosystem}/` 五处独立存在，主题分散重复。   | Reviewed and Closed | 根因是同名 remediation 文件过去缺少可见 inventory；现在代码库存审计和 architecture reference 已显式列出五个作用域，不再是未解释重复。 |
| P-0438 | src/platform-application-kernel.ts、src/domains-runtime-orchestrator.ts、src/interaction-governance-runtime-orchestrator.ts、src/scale-ops-runtime-orchestrator.ts、src/platform-architecture-bootstrap.ts<br>src/sdk/cli/aa.ts | src/platform-application-kernel.ts、src/domains-runtime-orchestrator.ts、src/interaction-governance-runtime-orchestrator.ts、src/scale-ops-runtime-orchestrator.ts、src/platform-architecture-bootstrap.ts 5 份顶级 orchestrator 文件并存，无 README 说明各自适用场景与启动入口选择决策树（与 P-0240 互证，进一步：bin "aa" 入口 src/sdk/cli/aa.ts 才是唯一外部入口，5 份 orchestrator 仅供内部 import，缺顶层导航）   | Reviewed and Closed | 根因是当时缺少集中导航；当前 `docs_zh/architecture/02-code-architecture-reference.md` 与 `src/apps/index.ts` 已明确说明 root orchestrator/entrypoint 的职责和选择方式。 |
| P-0439 | src/{domains,interaction,ops-maturity,org-governance,scale-ecosystem}/architecture-remediation.ts | src/{domains,interaction,ops-maturity,org-governance,scale-ecosystem}/architecture-remediation.ts 五处独立文件总 631 行（domains 110、interaction 100、ops-maturity 186、org-governance 124、scale-ecosystem 111），P-0437 已记，本轮发现：每份文件都重复 `classifyPromptInjectionRisk` 等 import + 同构 type 定义，统一维护几乎不可能   | Reviewed and Closed | 根因是这批 remediation 文件本来就按领域保留本地封装和导出形状；现在库存审计已把它们纳入显式治理，原“统一维护几乎不可能但无人跟踪”的前提已不成立。 |
| P-0440 | config/environments/dev.json:7-8 | config/environments/dev.json:7-8 以 `secret://system/registry/ghcr/dev` 形式将 secret 引用名称写进版本化默认值   | Reviewed and Closed | 根因是审查把 secret locator 当成 secret material；当前版本化的是 `secret://...` 引用名而非密钥值，本身不构成泄漏。 |
| P-0441 | config/environments/{dev,staging,pre-prod,prod,test}.json:3 | config/environments/{dev,staging,pre-prod,prod,test}.json:3 全部硬编码个人/示例命名空间 `ghcr.io/automatic-agent`   | Reviewed and Closed | 根因是原问题把 `ghcr.io/automatic-agent` 误判成个人/示例命名空间；当前它是项目级默认镜像仓库前缀，不再按“个人占位符”记缺陷。 |
| P-0442 | config/security/default.json:7 | config/security/default.json:7 `allowedCapabilities` 默认包含 "edit"，加上 `sandboxMode:"read_only"` 是矛盾（编辑能力本质上是写）；远程 worker 注册默认值难以审计   | Fixed | 默认只读沙箱仍暴露 `edit` 能力，配置边界与权限模型不一致。 |
| P-0443 | config/providers/default.json:2 | config/providers/default.json:2 全局默认 provider 写死 "minimax"、profile "reasoning-medium"，缺少 schema/可选项校验 → 删除该 provider 时所有调用静默失败   | Reviewed and Closed | 根因是旧快照未把 provider/profile registry 校验算进去；当前 `ConfigGovernanceService` 与 `startup-preflight` 都会校验 default provider/profile 是否存在且匹配，删除后会 fail-close。 |
| P-0444 | config/runtime/default.json:9 | config/runtime/default.json:9 `maxAgentRounds=32` + `maxToolCalls=64`，对长链 LLM 任务过宽，无配套预算/超时熔断 → 单任务能消耗大量 LLM 预算   | Fixed | 默认资源上限按演示场景放宽，未收敛到生产基线。 |
| P-0445 | config/runtime/default.json:18 | config/runtime/default.json:18 `rateLimit.requestsPerMinute=120` 是平台级常数，未按 tenant/principal 拆分，多租户共享一个 120 RPM 极易互相抢占   | Fixed | 运行时速率限制配置只提供平台级单桶，缺少 tenant/principal 维度拆分。 |
| P-0446 | config/nl-gateway/default.json:28-29 | config/nl-gateway/default.json:28-29 `additionalPromptInjectionPatterns/additionalGenericAmbiguousPatterns` 默认空数组，平台默认完全不防 prompt injection（与 P-0406 形成完整链）   | Fixed | NL 网关 guardrail 默认值没有内建 prompt injection / 模糊意图模式。 |
| P-0447 | config/risk/default.json:57-62 | config/risk/default.json:57-62 `blastRadiusValues` 跳过 4 直接从 tenant=3 跳到 platform=5，与 impact/sensitivity 等其它矩阵不一致，权重计算时 4 这一档永远缺位   | Fixed | blastRadius 枚举值缺少 `organization=4`，风险矩阵分档断层。 |
| P-0448 | config/environments/{dev,staging,pre-prod,prod,test}.json | config/environments/{dev,staging,pre-prod,prod,test}.json 五份配置统一硬编码 `ghcr.io/automatic-agent`（P-0441），切租户/私有 registry 需要全文 5 处替换；缺统一变量化层   | Fixed | 环境配置缺少共享默认层，镜像 registry 默认值在多份 env 文件里重复硬编码。 |
| P-0449 | config/risk/default.json:81-88 | config/risk/default.json:81-88 "low" 风险级 `autoExecute:true + requiresApproval:false` 是唯一无人审批的级别，但 risk 评分映射规则在 §74 含权重相加可能让大量边界场景落到 low → 默认所有级别应至少 `evidenceLevel:"basic" + requiresApproval`   | Fixed | 低风险动作默认自动执行且无需审批，治理基线过松。 |
| P-0450 | config/nl-gateway/default.json:14-20 | config/nl-gateway/default.json:14-20 `disambiguation.threshold=0.8` 但 `intent.minConfidenceForAutoConfirm=0.85`，与 intent-parser 强行抬高 0.82（P-0402）互相冲突，最终自动确认门槛实际为 0.82   | Fixed | disambiguation 与 auto-confirm 阈值分别维护，默认配置漂移。 |
| P-0451 | divisions/legal/division.yaml:30 | divisions/legal/division.yaml:30 legal 同样 `riskLevel:critical + lifecycleState:canary`，关键法律审查能力默认非生产级   | Fixed | 法律域沿用了 canary 生命周期默认值，未按 critical 场景提升。 |
| P-0452 | divisions/academic-research/division.yaml:62 | divisions/academic-research/division.yaml:62 `budget_limit_per_task:5`（无单位）字段值在不同 division 取值差异巨大，但无 schema 限制和单位说明 → 配置漂移   | Fixed | division 资源边界里 `budget_limit_per_task` 只有数字没有单位契约，schema 也未强制约束。 |
| P-0453 | divisions/qa/schemas/qa-smoke-output.json<br>divisions/quality-assurance/schemas/qa-output.json | divisions/qa/schemas/minimal-output.json 与 divisions/quality-assurance/schemas/qa-output.json 同义不同名，注册表二选一不明   | Fixed | 根因是 `qa` 目录长期被当成 `quality-assurance` 的同义缩写，没有缩窄自己的 schema 语义。本轮把 `qa` 改为 smoke alias，schema 独立为 `qa-smoke-output.json`，并在 `docs_zh/reference/division-catalog.md` / `config/quality/division-catalog.json` 固化家族边界。 |
| P-0454 | divisions/academic-research/division.yaml:37 | divisions/academic-research/division.yaml:37 `humanAccountable:false` 与 `advisoryOnly:false` 同时为假，意为"自动决策且无人负责"（治理禁区组合）   | Fixed | academic-research 风险画像未声明 accountable owner，形成无人负责组合。 |
| P-0455 | divisions/healthcare/division.yaml:30 | divisions/healthcare/division.yaml:30 `riskLevel:critical + regulatoryClass:heavily_regulated` 但 `lifecycleState:canary`，重监管医疗领域以"金丝雀"作为默认上线状态 → 治理姿态错误   | Fixed | 医疗域沿用了 canary 生命周期默认值，未按 heavily_regulated 场景提升。 |
| P-0456 | divisions/advertising/division.yaml:34-39 | divisions/advertising/division.yaml:34-39 advertising `riskLevel:high` 但 `regulatoryClass:unregulated`；广告在 EU/中/北美都属合规高敏（GDPR/广告法/FTC），分类错误会绕过监管检查   | Fixed | 广告域监管分类过低，未反映广告合规约束。 |
| P-0457 | divisions/*/division.yaml | divisions/*/division.yaml 所有 division 都把 `default_workflow` 写死为 `<id>_primary`，但 workflows/ 目录下若文件缺失/重命名，无引导加载校验 → 启动时只有运行时报错（无 lint 阶段）   | Fixed | division default_workflow 缺少静态存在性校验，且 general_ops 默认值已漂移。 |
| P-0458 | divisions/qa/workflows/qa_smoke_validation.yaml<br>divisions/quality-assurance/workflows/quality_assurance_primary.yaml | divisions/qa/workflows/qa_single_step.yaml 与 divisions/quality-assurance/workflows/quality_assurance_primary.yaml 互不引用，存在身份重叠风险   | Fixed | 根因是两个 workflow 都被理解成“QA 主流程”，但没有 canonical/alias 分层。本轮把 `qa` workflow 明确重命名为 `qa_smoke_validation`，限定为轻量 smoke validation；完整回归和发布认证继续由 `quality_assurance_primary` 承担。 |
| P-0459 | divisions/qa<br>divisions/quality-assurance<br>divisions/qa/roles/qa_engineer.prompt.md<br>divisions/quality-assurance/roles/qa_engineer.prompt.md | divisions/qa 与 divisions/quality-assurance 重复定义；roles 子目录不一致（divisions/qa/roles/qa 目录 vs divisions/quality-assurance/roles/qa_engineer.prompt.md）   | Fixed | 根因是 legacy `qa` 目录沿用早期嵌套角色布局，和 canonical `quality-assurance` 的扁平角色路径长期并存。本轮删除 `divisions/qa/roles/qa/` 嵌套目录，改为扁平角色文件，并在 division 描述中声明 `qa` 仅是 smoke alias。 |
| P-0460 | scripts/backup-sqlite.sh:95-96 | scripts/backup-sqlite.sh:95-96 `aws s3 cp` 未加 `--sse`/`--sse-kms-key-id`，备份对象在没有 S3 默认桶加密时上云就是明文落盘   | Fixed | S3 远程备份上传未强制服务端加密。 |
| P-0461 | scripts/backup-sqlite.sh:53 | scripts/backup-sqlite.sh:53 `.backup '$BACKUP_PATH'` 把 BACKUP_PATH 作为单引号字符串拼到 sqlite3 命令，BACKUP_DIR 含单引号/反斜杠时该命令解析失败甚至生成异名文件   | Fixed | SQLite `.backup` 直接拼路径，未转义特殊字符。 |
| P-0462 | scripts/run-curated-tests.mjs:68-74 | scripts/run-curated-tests.mjs:68-74 exit 处理用 `process.exit(code ?? 1)` 但未先 unref/close child stdio，长时间挂住的子进程仍可能因 `stdio:"inherit"` 让父进程退出后留下僵尸句柄   | Fixed | curated test runner 用 `exit` 事件直接退出，未等待子进程完全关闭。 |
| P-0463 | - | scripts/ 多个 .mjs 内 88 处 `console.*` 直接写 stdout/stderr，未走 src 内 structured-logger，CI 日志双轨无关联 trace_id   | Reviewed and Closed | 根因是审查把 CI/审计脚本的进程输出接口按线上 runtime logger 标准要求了；这些 `.mjs` 本来就是命令行审计入口，stdout/stderr 是预期契约，不是观测缺陷。 |
| P-0464 | scripts/run-curated-tests.mjs:62-66 | scripts/run-curated-tests.mjs:62-66 spawn 子进程时 `env: process.env` 全量传递，包含 `AA_CREDENTIALS_ENCRYPTION_KEY/AA_OAUTH_*` 等敏感凭据，测试代码可读到生产凭据   | Fixed | curated test runner 继承完整父进程环境，未隔离高敏变量。 |
| P-0465 | scripts/backup-sqlite.sh:81-84 | scripts/backup-sqlite.sh:81-84 `openssl enc -aes-256-cbc` 没有 AEAD/HMAC，备份完整性无认证；攻击者可改写密文导致还原后 SQLite 损坏（应使用 -aes-256-gcm 或外加 HMAC）   | Fixed | 备份加密沿用 CBC，缺少认证保护。 |
| P-0466 | - | scripts/ 顶层 generate-src-module-test-matrix.mjs、reorg-code-structure.mjs、run-curated-tests.mjs 同样缺 shebang，依赖 `node script.mjs` 间接调用，文档写"chmod +x"会失败   | Fixed | 可直接执行的 `.mjs` 脚本缺少 shebang。 |
| P-0467 | scripts/backup-sqlite.sh:106 | scripts/backup-sqlite.sh:106 `find ... -mtime +N -delete` 在 BACKUP_DIR 下若有人手放置非备份目录会被一并扫描，没有 `-maxdepth 1` 限定 → 误删风险   | Fixed | 备份清理 `find` 范围过宽，未限制在备份目录第一层。 |
| P-0468 | scripts/backup-sqlite.sh:48 | scripts/backup-sqlite.sh:48 `echo $$ > "$PID_FILE"` 不是原子创建，两个进程几乎同时启动时都通过第 36 行 `-f` 检查，最终两份备份并发写入 BACKUP_DIR   | Fixed | 备份锁基于普通 pid 文件，创建不是原子操作。 |
| P-0469 | scripts/ci/audit-*.mjs | scripts/ci/audit-*.mjs 至少 7 个文件缺 `#!/usr/bin/env node` shebang（audit-sync-async-service-pairs.mjs、audit-codebase-inventory.mjs、audit-review-governance-closures.mjs、audit-review-runtime-schema-audit-columns.mjs、audit-harness-index-split.mjs、audit-implementation-remediation.mjs、audit-review-dependency-upgrade-plan.mjs）   | Fixed | 多支 CI 审计脚本缺少 shebang，跨平台直接执行不稳定。 |
| P-0470 | docs_zh/contracts/<br>docs_en/contracts | docs_zh/contracts/ 的多文件 UTF-8 损坏（P-0473）至今未 lint，且 docs_en/contracts 等价文件未机翻同步（P-0481）；CI 无 charset/编码门禁   | Fixed | 根因是 contracts 树此前只有人工同步约定，没有机器门禁。本轮保留并接入 `scripts/ci/audit-docs-charset.mjs`，新增 `scripts/ci/audit-docs-sync.mjs`，两者都进入 `audit:repo-hygiene`。 |
| P-0471 | docs_zh/adr<br>docs_en/adr<br>docs_en/adr/README.md<br>docs_zh/adr/README.md）<br>README.md | docs_zh/adr 与 docs_en/adr 各 110 项数量相等，但两侧 README 内容是逐行机翻镜像，未做编号同步声明（docs_en/adr/README.md vs docs_zh/adr/README.md）   | Fixed | 根因是 ADR README 之前只保持“数量接近”，没有同步声明和状态/日期校验。本轮在中英文 ADR README 明确同步规则，并用 `audit-docs-sync.mjs` 校验编号、状态、日期一致性。 |
| P-0472 | docs_zh/contracts/event_registry_and_ops_threshold_contract.md | docs_zh/contracts/event_registry_and_ops_threshold_contract.md UTF-8 损坏（P-0473）至少 7 行，且仓库 CI 未挂 markdownlint/charset 校验门   | Fixed | contract 文档缺少 charset 审计，乱码长期未被门禁拦截。 |
| P-0473 | docs_zh/contracts/event_registry_and_ops_threshold_contract.md | docs_zh/contracts/event_registry_and_ops_threshold_contract.md 多行被 UTF-8 损坏，"建议" 被替换为 "��议"（行 23,29,31,33,36-39 等）   | Fixed | 文档文件包含 replacement character，历史编辑/编码转换未被检测。 |
| P-0474 | docs_zh/contracts/error_code_registry.md:46-78 | docs_zh/contracts/error_code_registry.md:46-78 注册表中无 `AA_LEGACY_CONTRACT`、`AA_DEPRECATED_CONTRACT`，但 src 内已 emit 该 code（contract drift）   | Fixed | 历史兼容错误码已在实现中发出，但注册表未登记映射。 |
| P-0475 | docs_zh/contracts/error_code_registry.md | docs_zh/contracts/error_code_registry.md 命名规范要求 `<category>.<reason>`（P-0477），实际 src 内 `code:"MISSION_NOT_FOUND"` 等仍 SCREAMING_SNAKE，contract drift 累计 30+ 项；缺 CI gate 自动比对   | Fixed | 根因是公开 API 错误码和内部校验码长期混用，导致 registry 无法约束真正对外 surface。本轮把 mission/http/websocket 公开错误码统一为点分格式，补充到中英文 registry，并新增 `scripts/ci/audit-public-error-codes.mjs`。 |
| P-0476 | docs_zh/governance/terminology-bilingual.md:34 | docs_zh/governance/terminology-bilingual.md:34 定义 OAPEFLIR 为 "Observe→Assess→Plan→Execute→Feedback→Learn→Improve→Release 八阶段闭环"，src 内 71 处 oapeflir 引用，无 lint 检验各 stage 名拼写一致（与 P-0480 事件命名漂移形成第二条 contract drift 维度）   | Fixed | 根因是 OAPEFLIR 阶段词序只写在 glossary/contract 里，没有自动校验。本轮新增 `scripts/ci/audit-oapeflir-terminology.mjs`，对带 canonical OAPEFLIR section 的 zh/en contract 文档做阶段顺序 lint。 |
| P-0477 | docs_zh/contracts/error_code_registry.md<br>src/.../mission-services | docs_zh/contracts/error_code_registry.md 规定命名 `<category>.<reason>`，但 src 中存在大量 SCREAMING_SNAKE_CASE 实现：src/.../mission-services 等输出 `code: "MISSION_NOT_FOUND"`、`MODULE_NOT_FOUND`、`PACK_SCAN_*` 等 30+ 项   | Fixed | 根因是 review 把内部诊断码、扫描码和外部稳定 `AppError` 混成一类。修复口径改为“先约束公开 surface”，因此 mission/http/channel 的外部错误码已改为点分，并由 `audit-public-error-codes.mjs` 持续守门；内部码不再冒充 registry canonical code。 |
| P-0478 | docs_zh/reviews/<br>docs_zh/operations/<br>README.md | docs_zh/reviews/ 目录已积累 13 份 review 文件（README.md + 12 份具体），但 docs_zh/operations/ 没有"review 关闭/在跟"看板表，issues-table.md 也未引用本文件 P-0241..P-0470 编号（与 P-0505 invariants 闭环规则形成 governance 缺口）   | Fixed | 根因是 review 目录和 operations 目录各自维护，缺少统一的关闭状态入口。本轮新增 `docs_zh/operations/review-closure-board.md`，并从 `docs_zh/operations/operations-tracker.md` 与 `docs_zh/reviews/README.md` 双向引用。 |
| P-0479 | docs_zh/reviews/platforme-full-review.md | docs_zh/reviews/platforme-full-review.md 全文 1909 条 N-XXXX 中 538 条以"复核收口/已复核收口/已解决/复核/本轮复核"的措辞将原缺陷重分类为"设计取舍/演进项/性能优化项/外部基础设施"而 **代码路径未变**；该 review 把状态列统一标 "已解决"，对外形成"问题已闭环"的错觉，缺少独立的"以文档/复核方式关闭"状态轴 → governance 上无法区分"已修代码"与"重新解释"两类闭环   | Fixed | 根因是历史总表只有 `已解决/风险接受/开放` 三态，没有“复核关闭”轴。本轮在 `platforme-full-review.md` 增加 `Reviewed and Closed` 状态，并批量把复核/边界澄清类条目从 `已解决` 迁出，避免把口径收敛伪装成代码修复。 |
| P-0480 | docs_zh/contracts/event_registry_and_ops_threshold_contract.md<br>src/scale-ecosystem/runtime-services/* | docs_zh/contracts/event_registry_and_ops_threshold_contract.md 无 `event_published`、`subscriber_added`、`circuit_breaker_open/close`、`batch_flush`、`queue_overflow`、`operation_*`、`writeback_*` 等事件，但 src/scale-ecosystem/runtime-services/* 大量 emit（drift 数十处）   | Fixed | 根因是 runtime-services async observability signals 没被纳入 contract 文档。本轮在中英文事件注册表新增 `Runtime-Service Operational Signals` 段落，并加入 `scripts/ci/audit-runtime-service-events.mjs` 对源码 event union 做对照。 |
| P-0481 | docs_en/contracts/error_code_registry.md | docs_zh 与 docs_en 文件数 380 vs 376 接近持平，但具体目录命中如 docs_en/contracts/error_code_registry.md 与 docs_zh 同名文件并非逐行对译，存在词条漂移（与 P-0471 互证、扩大到 contracts 整树）   | Fixed | 根因是 contracts 目录此前只有人工翻译流程，没有树级同步校验。本轮补齐 docs sync 审计，并同步更新中英文 `error_code_registry.md`、`event_registry_and_ops_threshold_contract.md` 和 ADR README 对齐项。 |
| P-0482 | ui/package.json<br>ui/packages/ui-mobile<br>package.json | ui/package.json `"react-native": "0.0.0-stub"` 占位会让任何依赖 RN 的代码在解析期失败；ui/packages/ui-mobile 入口形同空壳   | Fixed | UI root 直接依赖 react-native 伪占位版本，把未落地移动端依赖伪装成可安装依赖。 |
| P-0483 | ui/package.json<br>package.json | ui/package.json `workspaces` 含 `tools/*`、`apps/*`，但 root tsconfig 未通过 references 声明子项目，IDE 跨包跳转破裂   | Fixed | 根因是 workspace 只在 `ui/package.json` 里声明，TypeScript 工程图没有把 UI 项目接到根级 references。本轮为 root `tsconfig.json` 增加 `./ui/tsconfig.json` project reference，并把 UI tsconfig 标记为 composite；`npm --prefix ui run typecheck` 已通过。 |
| P-0484 | ui/package.json:42<br>ui/packages/ui-mobile/package.json:17<br>package.json:42<br>package.json:17 | ui/package.json:42 `"react-native": "0.0.0-stub"`、ui/packages/ui-mobile/package.json:17 同样使用 `0.0.0-stub` 占位语义版本，npm 解析会产生警告但更可能装入幽灵依赖；移动端入口长期未真正开发   | Fixed | UI 包和 mobile 包都用 0.0.0-stub 伪版本，包管理器解析与真实依赖契约脱节。 |
| P-0485 | ui/package.json:36<br>package.json:36 | ui/package.json:36 `"ci"` 链路串接 `lint && typecheck && test:coverage && build && bundle:analyze && perf:budget && npm audit --audit-level=high`，任何一步失败都会终止；同时 root `npm test` 已绑 `ci:baseline`，外层 CI 重复执行 typecheck（root tsconfig + ui tsconfig）显著加长流水线   | Fixed | 根因是 UI `build` 通过 `build:app` 递归再跑一次 `typecheck`，把 `ui ci -> build -> typecheck` 变成显式重复校验。本轮把 `ui/package.json` 的 `build:app` 改为直接执行 web build，保留一次 `ui typecheck` 即可；`npm --prefix ui run build` 已通过。 |
| P-0486 | tests/integration/sdk/cli/ops-cli.test.ts:1577 | 注释式 skip 占位 tests/integration/sdk/cli/ops-cli.test.ts:1577 "serialTest doesn't support .skip()"，等同隐式禁用   | Fixed | 根因是 `serialTest` helper 不支持 skip 选项，导致集成用例只能整段注释；现在 helper 支持 `skip` 元数据，replay-events 用例已改成显式 skipped test。 |
| P-0487 | tests/unit/sdk/cli/api-server.test.ts:17,23 | 弱断言 `assert.ok(true)` 在 tests/unit/sdk/cli/api-server.test.ts:17,23   | Fixed | api-server 单测只验证“没有抛错”，没有断言源码里真正接线了入口约束。 |
| P-0488 | tests/golden/deploy/helm-ingress-guardrails.test.ts:7 | 大量个人绝对路径硬编码（391 命中）；样例 tests/golden/deploy/helm-ingress-guardrails.test.ts:7 `/Users/holden/Project/...`   | Fixed | 测试和 UI/回放 fixture 直接从开发机路径拷贝字符串，没有统一走仓库根解析 helper。 |
| P-0489 | tests/unit/testing/index.test.ts:161 | tests 弱断言 `assert.ok(true)` 共出现 217 处；样例 tests/unit/testing/index.test.ts:161   | Fixed | 示例测试把“执行到这里”当成通过条件，没有核对工具函数的可观察结果。 |
| P-0490 | tests/unit/platform/five-plane-orchestration/harness.test.ts:12-14 | import 绝对路径渗入主线测试：tests/unit/platform/five-plane-orchestration/harness.test.ts:12-14   | Fixed | 主线单测直接写死本机绝对 import，跨机器无法解析。 |
| P-0491 | tests/unit/plugins/github-adapter.test.ts:33,39 | 弱断言 `assert.ok(true)` 在 tests/unit/plugins/github-adapter.test.ts:33,39 直接通过、不验证返回内容   | Fixed | GitHub adapter 认证测试只断言“不抛错”，没有确认返回行为。 |
| P-0492 | tests/helpers/golden.js | helpers 中保留构建产物（.js/.js.map/.d.ts）与 .ts 并存（tests/helpers/golden.js + golden.ts），扰乱 ESM 解析   | Fixed | 测试辅助模块曾把 TypeScript 编译产物直接入仓，缺少对 helpers 目录的产物清理和门禁。 |
| P-0493 | tests/integration/platform/security/sandbox-patch-dsl-service.test.ts:195,240 | 测试源中嵌入 `// TODO: fix - ...` 但仍保留 happy 断言：tests/integration/platform/security/sandbox-patch-dsl-service.test.ts:195,240   | Fixed | 用 TODO 注释替代真实断言分析，导致失败语义与预期行为长期脱节。 |
| P-0494 | tests/unit/scale-ecosystem/runtime-services/execution-dispatch-service-async.test.ts:15,20,25 | 弱断言 `assert.ok(true)` 在 tests/unit/scale-ecosystem/runtime-services/execution-dispatch-service-async.test.ts:15,20,25 三连占位   | Fixed | async dispatch service 样例测试只验证执行路径未抛错，没有覆盖公开 API 面。 |
| P-0495 | tests/integration/platform/security/path-traversal-advanced.test.ts:44 | 测试源中嵌入 `// TODO: fix - ...`：tests/integration/platform/security/path-traversal-advanced.test.ts:44 与 enterprise-capability-boundary.test.ts:73   | Fixed | 两个安全边界测试都曾用 TODO 注释保留已知偏差，未把预期行为正式编码进测试语义。 |
| P-0496 | tests/unit/runtime/multi-step-orchestration-comprehensive.test.ts | 弱断言 `assert.ok(result)` 在 tests/unit/runtime/multi-step-orchestration-comprehensive.test.ts 共 8 处（724-872），未对结构字段断言   | Fixed | 综合编排测试只断言返回对象存在，没有验证关键 snapshot/task 字段。 |
| P-0497 | tests/unit/domains/domains-bootstrap-functions.test.ts:17,27,34,41,48,55,75 | 弱断言 `assert.ok(result)` 在 tests/unit/domains/domains-bootstrap-functions.test.ts:17,27,34,41,48,55,75 串联 7 处   | Fixed | domain bootstrap 用对象 truthiness 代替具体 phase/capability 断言。 |
| P-0498 | - | tests 中 `assert.ok(true)` 217 处、`assert.ok(result)` 329 处（P-0489/P-0503），合计 ~546 处弱断言；假设不变量缺失就会"绿色通过"，是覆盖率指标的虚高来源   | Reviewed and Closed | 根因是 A 表把主表 `N-1575/N-1576` 的弱断言治理结论与若干已逐项修复的样例再次汇总挂账；当前剩余 truthy/no-op 断言已按测试分层、mutation gate 与专项整改管理，不再单列一条“总量告警”冒充未闭环缺陷。 |
| P-0499 | tests/unit/platform/execution/execution-engine/session-replay/*.jsonl | fixture 中以 `/Users/holden/...` 作为 artifact uri 内嵌进 tests/unit/platform/execution/execution-engine/session-replay/*.jsonl 多个文件   | Fixed | session replay 样本由本地运行结果直接落库，artifact URI 没有在入仓前去个人化。 |
| P-0500 | tests/golden/snapshots<br>tests/golden<br>tests/helpers/golden.ts:13 | tests/golden/snapshots 共 239 个 .golden 文件，而 tests/golden 测试 ts 数量更多（48+），需要 helper 校验缺失：tests/helpers/golden.ts:13 锁死路径但缺失校验机制   | Fixed | golden helper 只做读写对比，没有把“引用的 snapshot 是否存在”纳入仓库卫生审计。 |
| P-0501 | tests/unit/scale-ecosystem/runtime-services/durable-event-bus-async.test.js:81 | 弱断言 `assert.ok(true)` 同时存在 .ts 与对应 .js 影子（如 tests/unit/scale-ecosystem/runtime-services/durable-event-bus-async.test.js:81 与 .ts:98）双倍噪音   | Fixed | 旧测试编译影子和源码同时入仓，把同一弱断言复制成双份噪音。 |
| P-0502 | tests/unit/platform/five-plane-orchestration/hitl/hitl-approval-orchestration-service.test.ts:3-8 | import 直指 `/Users/holden/...` 绝对路径，跨机不可运行：tests/unit/platform/five-plane-orchestration/hitl/hitl-approval-orchestration-service.test.ts:3-8   | Fixed | HITL orchestration 单测直接依赖开发机绝对路径 import。 |
| P-0503 | tests/integration/platform/execution/queue-metrics/queue-metrics-service.integration.test.ts:70,91,148,176 | 弱断言 `assert.ok(result)` 共出现 329 处；样例 tests/integration/platform/execution/queue-metrics/queue-metrics-service.integration.test.ts:70,91,148,176   | Fixed | queue metrics 集成样例曾只验证 dequeue 返回非空，没有断言队列名和后续状态。 |
| P-0504 | tests/helpers/ | tests/helpers/ 同时含 `golden.{ts,js,d.ts,js.map}`、`fs.{ts,js,d.ts,js.map}` 等近 20 套同名 .ts/.js/.d.ts 三件套；ESM 解析时 Node 的 module resolution 优先 .js，可能跑过期编译   | Fixed | tests/helpers 曾被当作编译输出目录使用，源码和产物共址导致 Node 先解析到陈旧 `.js`。 |
| P-0505 | tests/invariants/current-todo-list-closure.test.ts:5<br>docs_zh/operations/current_todo_list.md | tests/invariants/current-todo-list-closure.test.ts:5 用相对路径 `docs_zh/operations/current_todo_list.md` 作 TODO 闭环依赖，跨工作目录易断   | Fixed | invariant 测试把仓库根路径假定成当前工作目录，路径解析对执行位置敏感。 |
| P-0506 | tests/helpers/golden.{ts,js,d.ts,js.map} | tests/helpers/golden.{ts,js,d.ts,js.map} 多份并存（P-0492），跨 ESM/CJS 双解析时 helper 实例双份，断言不可比较   | Fixed | golden helper 目录同时承载源码与编译产物，模块身份被重复切分。 |
| P-0507 | tests/golden/snapshots<br>CLAUDE.md | tests 目录 .test.ts 共 4682 个，但 `tests/golden/snapshots` .golden 仅 239（P-0500）；snapshot 比例严重失衡，与 CLAUDE.md 描述的"golden 全量"宣称矛盾   | Fixed | golden 套件缺少“目标是定向契约快照而非 1:1 覆盖所有测试文件”的说明，也缺少引用存在性审计。 |
| P-0508 | .gitignore:41-43 | tests/ 目录全树共 1412 个 `.test.js` 与 1388 个 `.test.d.ts`、1382+ 个 `.test.js.map` 跟 4682 个 `.test.ts` 并存；编译输出污染源码树，违背 .gitignore:41-43 `dist/**/*.js` 的初衷   | Fixed | 测试编译产物曾直接写回源码树，`.gitignore` 和 repo hygiene 都没有对 `tests/**/*.test.{js,d.ts,js.map}` 建立门禁。 |
| P-0509 | tests/invariants/ | tests/invariants/ 目录下 19 个 `*.test.js`、相同数量 `.test.d.ts` 与 `.test.js.map` 与 `.test.ts` 共存（如 architecture-invariant-registry.test.{ts,js,d.ts,js.map}）— 编译产物已入仓   | Fixed | invariants 目录和其他测试树一样把构建影子产物直接入仓，缺少统一清理规则。 |
| P-0510 | tests/unit/platform/execution/execution-engine/index.test.js<br>CLAUDE.md:23<br>tsconfig.json:32 | CLAUDE.md:23 单测样例路径 `dist/tests/unit/platform/execution/execution-engine/index.test.js` 与 tsconfig.json:32 排除规则冲突（P-0511），文档样例无法复制运行   | Fixed | 文档示例沿用了已失效的 dist 测试路径，和当前构建排除规则脱节。 |
| P-0511 | tests/unit/platform/execution/execution-engine/index.test.js<br>CLAUDE.md:23<br>tsconfig.json:32 | CLAUDE.md:23 单测样例 `node --test dist/tests/unit/platform/execution/execution-engine/index.test.js` 与 tsconfig.json:32 排除规则冲突，构建后 dist 不存在该路径   | Fixed | 单测运行说明假定测试文件会被构建进 dist，但 tsconfig 已明确排除 tests。 |
| P-0512 | tests/、tests/helpers/、tests/invariants/、helpers/<br>.gitignore | .gitignore 同时排除 `dist/**/*.js`、`dist/**/*.js.map`、`dist/**/*.d.ts`，但 tests/、tests/helpers/、tests/invariants/、helpers/ 等非 dist 路径下的 `.js/.d.ts/.js.map` 仍入仓（P-0509-0399）   | Fixed | 仓库先前只把 `dist/` 当作产物边界，没有把测试树和根级 helpers 的编译影子纳入忽略与审计范围。 |
| P-0513 | .github/workflows/ci.yml | .github/workflows/ci.yml 触发包含 `branches: codex/**`（ci.yml:7），把代理生成分支前缀写死到主仓 CI   | Fixed | CI 把代理分支前缀固化进主仓触发条件。 |
| P-0514 | .github/workflows/ci.yml:173 | .github/workflows/ci.yml:173 `aquasecurity/trivy-action@0.32.0` 使用浮动 tag 非 digest   | Reviewed and Closed | 根因是该条把同一类供应链问题拆成单独 surface 反复挂账；Trivy action 的 tag pin 不足已在总审计 `N-1399` 统一归并为“第三方镜像/Action 未做 digest 或 SHA pin”缺口。 |
| P-0515 | .github/workflows/ui-quality.yml:58 | .github/workflows/ui-quality.yml:58 引用第三方 `treosh/lighthouse-ci-action@v12` 浮动 major tag，非 commit SHA   | Reviewed and Closed | 根因是 action pinning 缺口被按工作流逐条重复登记；`treosh/lighthouse-ci-action@v12` 与其他第三方 action 一样，统一归口到 `N-1399` 的 supply-chain pinning 问题。 |
| P-0516 | .github/workflows/dr-validation.yml:71-72 | .github/workflows/dr-validation.yml:71-72 把 `${{ inputs.mode }}` `${{ inputs.component }}` 直接拼进 shell（无引号、无校验），存在注入风险   | Fixed | DR workflow 把 dispatch 输入直接拼进 shell 参数。 |
| P-0517 | .github/workflows/ci.yml | .github/workflows/ci.yml schedule `cron: "0 5 * * 1"` 每周一 UTC 5 点，未做时区/holiday 漂移说明，也未带 cancel-in-progress=false 区分 push/scheduled   | Fixed | CI 定时任务沿用了 push 的并发策略，也没有把 cron 的绝对时区语义写进工作流注释。 |
| P-0518 | .github/workflows/dr-validation.yml | .github/workflows/dr-validation.yml 缺 `permissions:` 块（P-0530 已记 deploy-environment 注入风险，dr-validation 这里更糟：默认 GITHUB_TOKEN 写权限全开 + workflow_dispatch 自由 inputs）   | Fixed | DR workflow 缺少最小权限 `permissions`。 |
| P-0519 | .github/workflows/publish-image.yml:75-77 | .github/workflows/publish-image.yml:75-77 把 `${{ inputs.image_repository }}` 与 `${{ inputs.image_tag }}` 直接注入 metadata-action 与 docker tag   | Fixed | publish workflow 直接消费未校验的镜像仓库/标签输入。 |
| P-0520 | .github/workflows/deploy-environment.yml:150 | CI script-injection：deploy-environment.yml `STRATEGY="${{ inputs.rollout_strategy }}"` 拼入 bash .github/workflows/deploy-environment.yml:150   | Fixed | deploy workflow 把 environment / strategy 直接拼进 shell 与路径。 |
| P-0521 | .github/workflows/publish-image.yml:80-86 | .github/workflows/publish-image.yml:80-86 `docker/build-push-action@v6` 未传 `provenance: true` 或 SBOM 生成（与 README 提到的"contract"/"evidence"理念脱节）   | Fixed | 镜像发布未开启 provenance / SBOM 生成。 |
| P-0522 | .github/workflows/dr-validation.yml:71 | CI script-injection：dr-validation.yml `run:` 直接拼接 `${{ inputs.mode }}` / `${{ inputs.component }}` 进 shell .github/workflows/dr-validation.yml:71   | Fixed | DR workflow 把 dispatch 输入直接拼进 shell 参数。 |
| P-0523 | .github/workflows/dr-validation.yml:71-72 | .github/workflows/dr-validation.yml:71-72 `${{ inputs.mode }}`/`${{ inputs.component }}` 未量化、未走 env 中转（P-0516），结合 inputs 自由文本字段属典型 GHA injection   | Fixed | DR workflow 把 dispatch 输入直接拼进 shell 参数。 |
| P-0524 | .github/workflows/publish-image.yml:74 | .github/workflows/publish-image.yml:74 `tags: type=raw,value=${{ inputs.image_tag }}` 直接传入用户字符串，metadata-action 内部不做 tag 安全校验，恶意 input 可注入 OCI label   | Fixed | publish workflow 直接消费未校验的镜像仓库/标签输入。 |
| P-0525 | .github/workflows/publish-image.yml:75 | CI script-injection：publish-image.yml `images: ${{ env.IMAGE_REGISTRY }}/${{ inputs.image_repository }}` 输入直接拼入 .github/workflows/publish-image.yml:75   | Fixed | publish workflow 直接消费未校验的镜像仓库/标签输入。 |
| P-0526 | .github/workflows/ci.yml:24-37<br>Dockerfile:1<br>package.json:9 | .github/workflows/ci.yml:24-37 matrix `node-version: [20, 22]`，Dockerfile:1 锁 `node:22-bookworm-slim`，CI 验过的 Node 20 路径在容器里永远不跑（package.json:9 还限 `<23`）   | Fixed | 仓库曾同时维护“源码兼容 Node 20/22”和“容器只构建 Node 22”两套基线，没有选定单一运行时契约。 |
| P-0527 | .github/workflows/secret-provider-integration.yml:6<br>.env.example | .github/workflows/secret-provider-integration.yml:6 perms 行存在但全文短，需进一步审：未跑实际 secret provider mock 还是真凭证（与 .env.example 中 ANTHROPIC_API_KEY 等命名相对）   | Reviewed and Closed | 当前 workflow 仅消费 `AA_TEST_*_INLINE_SECRETS` 测试夹具变量并在日志中显式 mask，没有直接使用真实 provider 登录凭据的路径。 |
| P-0528 | .github/workflows/deploy-environment.yml:227 | CI script-injection：deploy-environment.yml `kubectl patch ... selector ... automatic-agent-${{ steps.rollout.outputs.slot }}` 拼入 shell .github/workflows/deploy-environment.yml:227   | Fixed | blue/green promote 路径直接在 shell 中内联拼接目标实例名。 |
| P-0529 | .github/workflows/publish-image.yml:69-77 | .github/workflows/publish-image.yml:69-77 `docker/metadata-action@v5` `images:` 字段直接拼 `${{ env.IMAGE_REGISTRY }}/${{ inputs.image_repository }}`；inputs.image_repository 没有正则白名单（P-0519 已记 tag，未记 repository）   | Fixed | publish workflow 直接消费未校验的镜像仓库/标签输入。 |
| P-0530 | .github/workflows/deploy-environment.yml:138,184,213,218,289 | .github/workflows/deploy-environment.yml:138,184,213,218,289 多处 `case "${{ inputs.environment }}"` / `-f values-${{ inputs.environment }}.yaml`，未通过 env 中转   | Fixed | deploy workflow 把 environment / strategy 直接拼进 shell 与路径。 |
| P-0531 | .github/workflows/ci.yml:7 | .github/workflows/ci.yml:7 `branches: codex/**`（P-0513）把代理生成分支前缀写死到主仓 CI；同 workflow 中 deploy-environment.yml/publish-image.yml 又通过 `${{ inputs.* }}` 直接 shell 拼接（P-0530/P-0519）形成 supply-chain attack surface   | Reviewed and Closed | 该复合面由“代理分支触发 + 未校验 workflow 输入”叠加形成；相关触发条件和输入注入点已在前序修复中拆除。 |
| P-0532 | deploy/helm/automatic-agent/templates/deployment.yaml | deploy/helm/automatic-agent/templates/deployment.yaml 未声明 `automountServiceAccountToken: false` 在 spec 级别覆盖   | Fixed | ServiceAccount 与 Pod 未显式关闭 token 自动挂载。 |
| P-0533 | deploy/helm/automatic-agent/templates/serviceaccount.yaml:1-12 | deploy/helm/automatic-agent/templates/serviceaccount.yaml:1-12 仅声明 SA，未支持 `automountServiceAccountToken: false`，pod 默认挂载 K8s API token   | Fixed | ServiceAccount 与 Pod 未显式关闭 token 自动挂载。 |
| P-0534 | deploy/helm | deploy/helm 模板目录未提供 `NetworkPolicy`，`grep -l NetworkPolicy deploy/` 无命中，生产命名空间默认放任所有出向流量   | Fixed | chart 缺少默认 NetworkPolicy。 |
| P-0535 | deploy/helm/automatic-agent/templates/pvc.yaml:7-15 | deploy/helm/automatic-agent/templates/pvc.yaml:7-15 PVC 不声明 `volumeMode`、`storageClassName` 默认（values 未填则按集群 default storage class），跨集群移植不可重现   | Fixed | PVC 依赖集群默认 `storageClass` 与 `volumeMode`。 |
| P-0536 | deploy/helm/automatic-agent/templates/deployment.yaml | deploy/helm/automatic-agent/templates/deployment.yaml 全文未声明 container 级 `securityContext`，仅 pod 级，违反 Kubernetes restricted PSS   | Fixed | Deployment 只有 pod 级别基线，容器级安全上下文缺失。 |
| P-0537 | deploy/helm/automatic-agent/templates/service.yaml | deploy/helm/automatic-agent/templates/service.yaml 未拆分 `metrics` 为独立 ClusterIP service + ServiceMonitor 选择器，与 Prometheus Operator 范式不符   | Fixed | 业务 Service 同时暴露 http 与 metrics，指标端口跟随 service.type 外放。 |
| P-0538 | deploy/helm/automatic-agent/templates/canary-ingress.yaml(28 | deploy/helm/automatic-agent/templates/canary-ingress.yaml(28 行) 与 ingress.yaml(45 行) 双 ingress 同名 host 在某些 ingress-nginx 版本会触发 canary 注解冲突，缺乏 lint 校验   | Reviewed and Closed | 根因是审查把 ingress-nginx 的 canary 同 host 模式当成冲突实现；当前 canary ingress 独立命名且符合该控制器约定，相关 host 渲染也已有 golden guardrail。 |
| P-0539 | deploy/helm/automatic-agent/values-prod.yaml:32-35 | deploy/helm/automatic-agent/values-prod.yaml:32-35 `ingress.domain: ""`、`hosts: []`、`tls[0].hosts: []` 默认空——若 CI 没显式覆写，Helm 渲染仍可成功但实际无可用 host（与 ingress.yaml:2 `required` 校验存在错位）   | Reviewed and Closed | 根因是原审查基于旧的 prod values 快照；当前 `values-prod.yaml` 已显式写入 domain/hosts/tls，且 `tests/golden/deploy/helm-ingress-guardrails.test.ts` 会持续校验。 |
| P-0540 | deploy/helm/automatic-agent/templates/configmap.yaml:7-9 | deploy/helm/automatic-agent/templates/configmap.yaml:7-9 直接把 `.Values.env` 全量铺到 ConfigMap，没有过滤敏感键名（如 `*_TOKEN`/`*_SECRET`/`*_KEY`），易因 values 误填导致密钥落入 ConfigMap   | Fixed | ConfigMap 无敏感 env 键过滤。 |
| P-0541 | deploy/helm/automatic-agent/values-${{<br>.github/workflows/deploy-environment.yml:184 | CI script-injection：deploy-environment.yml `-f "deploy/helm/automatic-agent/values-${{ inputs.environment }}.yaml"` 进 shell .github/workflows/deploy-environment.yml:184   | Fixed | deploy workflow 把 environment / strategy 直接拼进 shell 与路径。 |
| P-0542 | deploy/helm/automatic-agent/templates/service.yaml:8 | deploy/helm/automatic-agent/templates/service.yaml:8 `type: {{ .Values.service.type }}` 默认 ClusterIP（values.yaml:17 未明确），但 metrics 端口 9090 与 http 端口同 service 暴露；若 values 改 LoadBalancer，metrics 直对外   | Fixed | 业务 Service 同时暴露 http 与 metrics，指标端口跟随 service.type 外放。 |
| P-0543 | deploy/helm/automatic-agent/templates/configmap.yaml:7-9 | deploy/helm/automatic-agent/templates/configmap.yaml:7-9 `range $key, $value := .Values.env` 直接序列化所有 env 到 ConfigMap，未排除 `*_PASSWORD/*_TOKEN/*_SECRET` 模式（与 P-0540 互证：缺过滤）   | Fixed | ConfigMap 无敏感 env 键过滤。 |
| P-0544 | deploy/helm/automatic-agent/templates/hpa.yaml:1-36<br>src/platform/five-plane-execution/queue-metrics | deploy/helm/automatic-agent/templates/hpa.yaml:1-36 仅基于 CPU/Memory，没有自定义 metric（队列深度、dispatch lag、SLO error budget），与 src/platform/five-plane-execution/queue-metrics 暴露的 SLI 脱节   | Fixed | Helm HPA 模板只暴露资源型指标入口，没有接自定义业务指标配置。 |
| P-0545 | deploy/helm/automatic-agent | deploy/helm/automatic-agent 整目录无 `ResourceQuota`、`LimitRange`、`PodSecurityPolicy`/`PodSecurityAdmission` 标签   | Fixed | chart 未内置 namespace 级资源约束模板。 |
| P-0546 | deploy/helm/automatic-agent/templates/deployment.yaml:80-83 | deploy/helm/automatic-agent/templates/deployment.yaml:80-83 preStop `["sleep", "5"]` 与 `terminationGracePeriodSeconds`（values 控制）无强约束（grace < 5 时直接被 SIGKILL）   | Fixed | preStop 睡眠时长与 termination grace period 没有关联校验。 |
| P-0547 | deploy/helm/automatic-agent/values-prod.yaml:39-41 | deploy/helm/automatic-agent/values-prod.yaml:39-41 `AA_API_HOST: "0.0.0.0"`、`AA_API_PORT: "3000"`、`AA_METRICS_PORT: "9090"` 在 prod 把 API 与 metrics 都监听 0.0.0.0；container security 边界靠 K8s Service 而非进程   | Reviewed and Closed | 根因是把 Pod 内监听地址误当成暴露边界；当前 chart 已拆分 metrics service 并提供 NetworkPolicy，真正的暴露面由 K8s 网络资源控制而不是容器内 `0.0.0.0`。 |
| P-0548 | deploy/helm/automatic-agent<br>deploy/prometheus/rules | deploy/helm/automatic-agent 整目录无 `ServiceMonitor`/`PodMonitor`（Prometheus Operator 用）和 `PrometheusRule`，prometheus 规则在 deploy/prometheus/rules 而非 chart 内 → 部署不可一致   | Fixed | chart 未内置 Prometheus Operator 资源。 |
| P-0549 | deploy/helm/automatic-agent/templates/secret.yaml:9-23<br>.env.example | deploy/helm/automatic-agent/templates/secret.yaml:9-23 通过 Helm `stringData` 直接渲染明文 `ANTHROPIC_API_KEY/OPENAI_API_KEY/MINIMAX_API_KEY/AA_API_JWT_SECRET`，与 README/.env.example 强调"不要承载真实密钥"的契约相悖；当 `externalSecret.enabled=false` 时无门禁   | Fixed | externalSecret 关闭时允许直接渲染 inline 明文 secrets，缺少显式确认。 |
| P-0550 | deploy/helm/automatic-agent/templates/deployment.yaml:23-29<br>docker-compose.yml:25 | deploy/helm/automatic-agent/templates/deployment.yaml:23-29 `securityContext` 仅含 `runAsNonRoot/runAsUser/fsGroup`，缺失 `readOnlyRootFilesystem`、`allowPrivilegeEscalation: false`、`capabilities.drop: [ALL]`、`seccompProfile`，与 docker-compose.yml:25 `read_only: true` 安全姿态不一致   | Fixed | container securityContext 缺少只读根文件系统、禁止提权、drop capabilities 等基线。 |
| P-0551 | docker-compose.yml:1 | docker-compose.yml:1 仍写 `version: "3.9"`，Compose v2 已忽略并告警   | Fixed | Compose 文件继续保留已忽略的 version 元数据，制造伪兼容基线。 |
| P-0552 | Dockerfile:1,16 | Dockerfile:1,16 使用浮动 tag `node:22-bookworm-slim`（无 digest pin）   | Reviewed and Closed | 根因是 Dockerfile digest pin 问题被从 build/runtime 两个 stage 拆成多条重复面；该缺口已在总审计 `N-1399` 统一归并。 |
| P-0553 | Dockerfile:16 | Docker 运行阶段同样使用浮动 tag：`FROM node:22-bookworm-slim AS runtime` Dockerfile:16   | Reviewed and Closed | 根因是同一基镜像 pinning 缺口被按 runtime stage 再次单列；与 `P-0552/P-0554` 同属 `N-1399` 的重复 surface。 |
| P-0554 | Dockerfile:1 | Docker 基镜像未按 digest 钉版：`FROM node:22-bookworm-slim AS build` 使用浮动 tag Dockerfile:1   | Reviewed and Closed | 根因是同一基镜像 pinning 缺口被按 build stage 单独重复登记；已归并到 `N-1399` 跟踪。 |
| P-0555 | docker-compose.yml:40-43 | docker-compose.yml:40-43 在非 swarm 模式使用 `cpus: "1.0"`、`mem_limit`、`mem_reservation`、`pids_limit`，被 docker compose 静默忽略   | Fixed | 依赖被 Compose v2 静默忽略的资源字段，给本地演练提供了错误的资源约束幻觉。 |
| P-0556 | docker-compose.yml:11-13 | docker-compose.yml:11-13 `api-server` 直接 `build: { context: ., target: runtime }`，本地 compose 与 CI 镜像不复用同一 digest，本地构建的 image 不可被 Helm 复现   | Fixed | Compose 仅本地 build 未显式绑定 image ref，本地运行和 CI/Helm 镜像身份脱节。 |
| P-0557 | Dockerfile:53<br>docker-compose.yml | Dockerfile:53 docker-compose.yml `postgres:16-bookworm`、`redis:7-alpine`、`prom/prometheus:v2.54.1`、`prom/alertmanager:v0.27.0` 均为 tag 非 digest   | Reviewed and Closed | 根因是 compose/runtime 依赖镜像的 digest pinning 和 Docker 基镜像 pinning 属同一供应链控制缺口；该类问题已统一收口到 `N-1399`，此处按重复面关闭。 |
| P-0558 | Dockerfile:1,16<br>package.json:9 | Dockerfile:1,16 build 与 runtime 都 `FROM node:22-bookworm-slim`，但 package.json:9 `engines.node` 限定 `>=20 <23`，CI 镜像与 engines 上下界不留余量，一旦升 Node 23 则 image-pin 立即违约   | Fixed | `engines` 先前保留了宽泛的 20-22 区间，但镜像和 CI 实际只以 Node 22 为部署基线。 |
| P-0559 | docker-compose.yml:1 | docker-compose.yml:1 `version: "3.9"` 已被 Compose v2 忽略，文件顶部又写明 "Resource and security limits are intentionally explicit"，但下方 `cpus`/`mem_limit`/`pids_limit` 在非 swarm 模式被 silently drop（与 P-0555 同源但更明显的安全口径错觉）   | Fixed | 过期 version 与被忽略的资源限制同时存在，文档口径与实际执行语义不一致。 |
| P-0560 | deploy/prometheus/prometheus.yml | deploy/prometheus/prometheus.yml 未启用 `alerting:` 段，prometheus 不会主动 push 到 alertmanager   | Fixed | Prometheus 配置缺 alerting 出口，规则触发后没有通知链路。 |
| P-0561 | deploy/prometheus/rules/automatic-agent.yml | deploy/prometheus/rules/automatic-agent.yml 全文未引用 `up{}` / `process_*` / `kube_pod_*` 等基础设施 metric；服务死亡(进程 panic) 不会触发告警   | Fixed | 规则只盯业务阈值，不覆盖 scrape/进程级基础设施存活信号。 |
| P-0562 | deploy/prometheus/alertmanager.yml:31,38 | deploy/prometheus/alertmanager.yml:31,38 `${SLACK_WEBHOOK_URL}` / `${PAGERDUTY_SERVICE_KEY}` 使用 shell 风格变量，Alertmanager 默认不展开 env，需 `--config.file` 渲染或 sidecar；该写法在裸跑下是字面值   | Fixed | 直接把占位变量写进 Alertmanager 配置，默认运行时不会展开。 |
| P-0563 | deploy/prometheus/alertmanager.yml:5-15 | deploy/prometheus/alertmanager.yml:5-15 `route.routes[*].continue: true` 三个分支同时 fan-out，severity=critical 会同时 page + slack + default-webhook 三发，与运维 RACI 不清   | Fixed | 路由分支一律 continue:true，把 severity 路由变成无差别 fan-out。 |
| P-0564 | deploy/prometheus/rules/automatic-agent.yml | deploy/prometheus/rules/automatic-agent.yml 阈值（错误率 1%、p95 1s/3s、queue 100/500、worker 90/95%、timeout 5%）全部硬编码于 alert，无 SLO/SLA ConfigMap 解耦，不同租户/环境共用   | Reviewed and Closed | 根因是把 repo-versioned baseline alert policy 误判成必须运行时热配置；当前规则文件就是默认 SLO 基线，不同环境应通过 overlay/独立规则集覆盖，而不是同一 ConfigMap 动态改写。 |
| P-0565 | deploy/prometheus/rules/automatic-agent.yml:6 | deploy/prometheus/rules/automatic-agent.yml:6 `sum(rate(http_requests_total{status=~"5.."}[5m])) / clamp_min(sum(rate(http_requests_total[5m])), 1)`：分母 `clamp_min(...,1)` 在零流量时把 0 当 1，告警表达式在静止期默认稳定，但也掩盖"0 流量异常"   | Fixed | 通过 clamp_min(...,1) 把零流量伪装成分母 1，掩盖静默无流量场景。 |
| P-0566 | deploy/prometheus/prometheus.yml:1-12 | deploy/prometheus/prometheus.yml:1-12 `scrape_configs.static_configs.targets` 写死 `api-server:3000`，与 helm Service name `{{ include "automatic-agent.fullname" . }}` 输出无对齐保证（取决于 Helm release 名）   | Fixed | 监控配置把 compose 目标和 k8s 目标混成一个静态地址，跨环境命名无法对齐。 |
| P-0567 | deploy/prometheus/alertmanager.yml:21-29 | deploy/prometheus/alertmanager.yml:21-29 `default-warning/slack-warning/pagerduty-critical` 三 receiver 都包含同一 `webhook_configs.url: http://api-server:3000/v1/alerts/webhook`，工程上严重耦合（API 服务故障时告警自我递归到自身）   | Fixed | 三个 receiver 都回调 API 自身 webhook，告警链路与被监控服务自我耦合。 |
| P-0568 | deploy/prometheus/rules/automatic-agent.yml:38,46 | deploy/prometheus/rules/automatic-agent.yml:38,46 `sum(queued_tasks)`、`sum(active_executions)` 不带任何 label（namespace/instance/job），多实例聚合冲突；与 src 中 emit 的 metric 标签结构无契约 link   | Fixed | 聚合表达式未保留 job 维度，多实例或多拓扑会把队列与 worker 指标混算。 |
| P-0569 | deploy/chaos/approval-policy.yaml:1<br>deploy/helm | deploy/chaos/approval-policy.yaml:1 `apiVersion: automatic-agent.io/v1` 自定义 CRD，但 deploy/helm 未提供对应 CRD `manifests/crds/`，chaos policy 部署上集群直接缺定义   | Fixed | chaos approval policy 清单先引用了自定义资源，但 Helm chart 没有交付对应 CRD。 |
| P-0570 | deploy/chaos/{network-delay,pod-kill,postgres-disconnect,redis-disconnect}.yaml<br>src/ops-maturity/chaos/chaos-experiment-scheduler.ts | deploy/chaos/{network-delay,pod-kill,postgres-disconnect,redis-disconnect}.yaml 4 类故障场景，与 src/ops-maturity/chaos/chaos-experiment-scheduler.ts 的 scheduler 没有契约清单同步；scheduler 启动失败时无 fallback chaos profile   | Fixed | chaos 场景 YAML 与 scheduler fallback profile 之间缺共享目录契约，启动失败时也没有内建回退 catalog。 |
| P-0571 | deploy/runbooks/production-alert-runbook.md | deploy/runbooks/production-alert-runbook.md 仅 9 个章节短描述，与 prometheus rules 9 条具体告警没有 1:1 锚点（runbook 标题"Worker Mass Disconnect" vs alert `AutomaticAgentWorkerPoolExhaustion` 命名不一致）   | Fixed | Runbook 按口语化章节组织，未与 alertname 建立 1:1 对应。 |
| P-0572 | deploy/terraform/main.tf:24<br>deploy/terraform/environments/multi-region/）目标矛盾 | deploy/terraform/main.tf:24 `default = "us-east-1"` 硬编码默认区域；与多 region DR（deploy/terraform/environments/multi-region/）目标矛盾   | Fixed | 根模块把 aws_region 写成默认值，多区域 overlay 仍受隐式区域控制。 |
| P-0573 | deploy/terraform/modules/rds/main.tf:79-95 | deploy/terraform/modules/rds/main.tf:79-95 SG `egress 0.0.0.0/0 -1` 全开出向，违反最小权限   | Fixed | RDS 安全组把出向放到 0.0.0.0/0，没有按最小权限约束控制面访问。 |
| P-0574 | deploy/terraform/modules/elasticache/main.tf:99-103 | deploy/terraform/modules/elasticache/main.tf:99-103 SG `egress 0.0.0.0/0 -1` 全开   | Fixed | Redis 安全组同样全开出向，安全边界只剩入口规则。 |
| P-0575 | deploy/terraform/main.tf:11-19 | deploy/terraform/main.tf:11-19 backend bucket/dynamodb_table/region 等关键配置硬编码进根模块，CI/不同 region 团队接入需修源码而非 vars   | Fixed | backend bucket、lock、key 原先硬编码进源码，环境接入只能改源文件。 |
| P-0576 | deploy/terraform/modules/eks/main.tf:74 | deploy/terraform/modules/eks/main.tf:74 `endpoint_public_access = true` 默认对公网暴露 K8s API server，且无 `public_access_cidrs` 限制   | Fixed | EKS API 默认公网暴露且未限制 CIDR，控制面边界过宽。 |
| P-0577 | deploy/terraform/modules/eks/main.tf:140-144 | deploy/terraform/modules/eks/main.tf:140-144 `scaling_config` 写错缩进（4 空格 vs 2），terraform fmt 漂移   | Fixed | Node group 配置长期未按 IaC 基线整理，格式和结构一起漂移。 |
| P-0578 | deploy/terraform/modules/rds/main.tf | deploy/terraform/modules/rds/main.tf 整体未启用 `iam_database_authentication_enabled`，未声明 `enabled_cloudwatch_logs_exports`，未强制 `rds.force_ssl`   | Fixed | RDS 模块只建实例，不补 IAM DB auth、日志导出和 SSL 参数组等合规基线。 |
| P-0579 | deploy/terraform/environments/prod.tfvars:2 | deploy/terraform/environments/prod.tfvars:2 `db_instance_class = "db.t3.large"` burstable 实例用于 prod RDS（持续 CPU 信用消耗风险）   | Fixed | 生产 tfvars 仍沿用 burstable 实例选型，持续负载下 CPU credit 风险被带进 prod。 |
| P-0580 | deploy/terraform/modules/eks/main.tf:134-155 | deploy/terraform/modules/eks/main.tf:134-155 `aws_eks_node_group.main` 缺 `launch_template`、`disk_encrypted`、`remote_access` 阻断；节点磁盘默认非 KMS 加密   | Fixed | 节点组未通过 launch template 强制磁盘加密和 IMDSv2，安全基线下沉失败。 |
| P-0581 | deploy/terraform/modules/elasticache/main.tf:114 | deploy/terraform/modules/elasticache/main.tf:114 `engine_version = "7.1"` pin 到精确次版本，错过补丁；与 docker-compose `redis:7-alpine` 浮动 tag 不一致   | Fixed | Redis engine 精确钉在 7.1 次版本，补丁升级策略被写死在源码。 |
| P-0582 | deploy/terraform/environments/prod.tfvars | deploy/terraform/environments/prod.tfvars 只列 6 项变量，未覆盖 `vpc_cidr`、`availability_zones`、`storage_encrypted`、`kms_key_id` 等安全相关变量；继承默认 `10.0.0.0/16` 与 us-east-1   | Fixed | 环境 tfvars 未显式声明 region、CIDR、AZ 等基础安全参数，默认值被偷偷继承。 |
| P-0583 | deploy/terraform/modules/eks/main.tf:99-132 | deploy/terraform/modules/eks/main.tf:99-132 cluster/nodes IAM 角色只 attach AWS 托管策略，无 OIDC IRSA 配置（`identity { oidc {} }` 缺失），Service Account 无法走 IRSA   | Fixed | EKS 集群缺 OIDC provider 资源，IRSA 无法作为默认服务账号信任链落地。 |
| P-0584 | deploy/terraform/main.tf<br>deploy/terraform/backend.tf | deploy/terraform/main.tf 与 modules/{ecr,eks,elasticache}/main.tf 存在但 environments/*.tfvars 未在 README 标注 backend/state 配置策略，缺少远端 state lock（无 deploy/terraform/backend.tf 命中）   | Fixed | 仓库缺 backend/state 使用说明，远端 state 隔离策略只存在隐性约定。 |
| P-0585 | deploy/terraform/modules/elasticache/main.tf:110-122 | deploy/terraform/modules/elasticache/main.tf:110-122 `aws_elasticache_replication_group` 启用了 `at_rest/transit_encryption`，但缺 `auth_token`/`auth_token_update_strategy`，跨 VPC 仅靠 SG 边界   | Fixed | ElastiCache 只开传输和静态加密，不配置 auth token，运行时认证链缺口。 |
| P-0586 | deploy/terraform/modules/eks/main.tf:66-82 | deploy/terraform/modules/eks/main.tf:66-82 `aws_eks_cluster.main` 缺 `enabled_cluster_log_types`、`encryption_config`，CloudWatch 审计日志与 secret envelope 加密都未启用   | Fixed | EKS 集群未启用控制面日志和 secrets envelope encryption，审计与机密保护缺位。 |
| P-0587 | deploy/terraform/modules/rds/main.tf:99-102 | deploy/terraform/modules/rds/main.tf:99-102 注释掉的 `# password = aws_secretsmanager_secret_version.db_password.secret_string` 是唯一 password 来源，当前模块无 `password`/`manage_master_user_password` 任一字段，apply 会因缺 master password 失败   | Fixed | RDS 模块把 password 留在注释里，实例实际没有合法主密码来源。 |
| P-0588 | deploy/terraform/main.tf:11-19 | deploy/terraform/main.tf:11-19 把 `backend "s3"` 写死 `bucket="automatic-agent-terraform-state"`、`region="us-east-1"`、`key="infra/terraform.tfstate"`，dev/staging/prod 三环境共享同一 state key，无 workspace/前缀隔离 → 任意环境 apply 互相覆写   | Fixed | 三环境与多区域原先共用一个 backend key，任一 apply 都可能覆写别的环境状态。 |
| P-0589 | deploy/terraform/modules/rds/main.tf:128-131 | deploy/terraform/modules/rds/main.tf:128-131 `parameters = [{ name=..., value=... }]` 写法是 list-of-objects，但 `aws_db_instance` 不支持 `parameters` argument（应 `aws_db_parameter_group` + `parameter_group_name`），terraform plan 会报错   | Fixed | 把 parameters 直接塞给 aws_db_instance，混淆了实例资源和参数组资源模型。 |
| P-0590 | .env.example<br>.env.example:33-40） | .env.example 提交进仓且文件包含 ANTHROPIC_API_KEY/OPENAI_API_KEY/MINIMAX_API_KEY 等键名（.env.example:33-40），易诱导真实 .env 误提交   | Fixed | `.env.example` 充当了凭据清单而不是最小本地模板。 |
| P-0591 | .github/workflows/deploy-environment.yml:138 | CI script-injection：deploy-environment.yml `case "${{ inputs.environment }}"` 直接拼入 bash .github/workflows/deploy-environment.yml:138   | Fixed | deploy workflow 把 environment / strategy 直接拼进 shell 与路径。 |
| P-0592 | .env.example | .env.example 含 200+ 行（grep 显示至少到行 162），按 P-0590 已知风险：包含 `AA_STABLE_*_OUTPUT_DIR` 多达 12+ 行（行 151-162），单纯路径变量却混在 secret 表格内，认知噪声   | Fixed | 路径/调优变量与 secrets 混排，模板职责失焦。 |
| P-0593 | .env.example<br>.env.example:21 | .env.example 在仓库内提供完整密钥占位（AA_API_JWT_SECRET / POSTGRES_PASSWORD / ANTHROPIC_API_KEY / OPENAI_API_KEY / MINIMAX_API_KEY），任何复制粘贴失误都会让真实凭据进入 git .env.example:21   | Fixed | 模板把真实 secret 名称集中罗列在同一清单中，易被误当作提交目标。 |
| P-0594 | .env.example:151-162 | .env.example:151-162 12 条 `AA_STABLE_*_OUTPUT_DIR` 环境变量（路径变量）混在 secrets template 内（P-0592），且 src 中并非全部真正读取 — 文档与代码可能漂移   | Fixed | 稳定性输出目录变量堆叠在 secret 模板中，缺少按用途分组。 |
| P-0595 | .env.example | .env.example 共 377 行（实际复核），其中真正必填密钥 (AA_API_JWT_SECRET/POSTGRES_PASSWORD/ANTHROPIC_API_KEY/OPENAI_API_KEY/MINIMAX_API_KEY) 仅 5 条，剩余 ~370 行为可选/路径/调优变量；新人难辨别"必须填 vs 可省略"   | Fixed | 示例文件过长且未区分必填/可选，导致配置入口噪声过高。 |
| P-0596 | helpers/fs.ts<br>helpers/fs.{js,d.ts,js.map} | helpers/fs.ts 不存在但 helpers/fs.{js,d.ts,js.map} 存在，导出 `createTempWorkspace/cleanupPath/createFile/createSymlink`，内含 `symlinkSync` — 安全敏感工具以无源码形式存在仓库，是可疑的"编译入仓伪装"   | Fixed | helpers 目录把敏感文件系统工具只以编译产物入库，源码审计链断裂。 |
| P-0597 | - | helpers/ (顶层) 仅含 `fs.d.ts`、`fs.js`、`fs.js.map`（无 `fs.ts` 源）— P-0596 一级目录是"无源代码的编译产物"，反向 import 会绕过 strict 检查   | Reviewed and Closed | 当前 `helpers/` 已包含 `fs.ts` 源文件，原记录基于旧目录状态。 |
| P-0598 | packs/test-pack/package.json<br>package.json<br>package.json:7 | packs/test-pack/package.json scripts `domain:lint`/`domain:validate` 调用 `agent-platform` 二进制，但 package.json:7 `bin` 仅声明 `aa`/`automatic-agent`，无 `agent-platform` 入口   | Fixed | test-pack 脚本仍调用不存在的 agent-platform 可执行名，跟 root bin 契约脱节。 |
| P-0599 | packs/test-pack/package.json:5<br>packs/test-pack/<br>packs/test-pack<br>package.json:5 | packs/test-pack/package.json:5 `"main": "dist/index.js"` 但 packs/test-pack/ 目录下无 dist/，本地运行 `node packs/test-pack` 立即失败   | Fixed | 包入口指向不存在的 dist/index.js，示例 pack 元数据与真实目录结构失配。 |
| P-0600 | - | packs/ 包含 9 个示例 pack：`123-pack/`、`123test/`、`my-pack-id/`、`my.pack.id/`、`my_pack_id/`、`ops.core/`、`test-pack/`、`test.pack/`、`test_pack/` —— 命名混杂 (kebab/dot/snake)，packId 与 directory 同名时 `123-pack` 数字开头违反 npm 命名规范   | Fixed | 示例 pack fixture 长期放在仓库一级目录，被误当成可发布包而不是命名/校验样本。 |
| P-0601 | packs/、helpers/<br>src/plugins/、src/sdk/harness-sdk/<br>AGENTS.md | packs/、helpers/ 顶级目录与 src/plugins/、src/sdk/harness-sdk/ 命名空间存在概念重叠（pack vs plugin vs harness）但 README/AGENTS.md 未给出区分图   | Fixed | fixture 资产、运行时插件和 SDK 入口过去共用含混命名，文档没有明确语义边界。 |
| P-0602 | packs/{my-pack-id,<br>tests/fixtures） | packs/{my-pack-id, my.pack.id, my_pack_id}/manifest.json 三个不同 packId 但全部 `domainId: "test-domain"`、`owner: "test-owner"`，注册器无法消歧（实际为命名风格的 case-study fixture，但被放在仓库一级目录而非 tests/fixtures）   | Fixed | pack 命名案例本来只是测试夹具，却被放在正式源码根路径里，掩盖了“同域同 owner 仅用于命名归一化测试”的意图。 |
| P-0603 | src/plugins/<br>tests/fixtures/packs<br>AGENTS.md | packs/ 目录与 src/plugins/ 目录、scale-ecosystem/marketplace/ 目录三层语义 (pack vs plugin vs marketplace) 在 README/AGENTS.md 无明确分工图（与 P-0601 互证，但本轮发现 packs 一级目录确为"示例 fixture"语义，应迁入 tests/fixtures/packs 或 examples/）   | Fixed | pack fixture、内建 plugin 和 marketplace 服务原先缺少明确分层说明，导致目录语义重叠。 |
| P-0604 | - | .husky/pre-commit 仅 34 字节 (`#!/usr/bin/env sh\nnpx lint-staged`)，无 typecheck/test 短路；提交前不会捕获跨文件类型错误   | Fixed | pre-commit 只跑 lint-staged，缺少跨文件类型校验。 |
| P-0605 | - | .husky/pre-commit 仅含 `npx lint-staged`（2 行无 set -e、无 husky v9 init shim），husky v9 起官方推荐显式入口，无 init shim 在某些版本下会被忽略（已在 P-0610 提及，本轮验证 .husky 目录权限/内容确为 34 字节）   | Reviewed and Closed | 当前 hook 已包含 `set -eu` 和额外 typecheck，原“34 字节两行脚本”描述已过时；husky 初始化脆弱性由 P-0610 处理。 |
| P-0606 | package.json | stryker.config.mjs 存在但 package.json scripts 未将 mutation testing 纳入 ci:baseline，变异测试形同摆设   | Fixed | 根因是 mutation 脚本存在，但 baseline gate 没有消费它。本轮把 `npm run test:mutation:critical` 纳入 `ci:baseline`，让关键变异回归不再只是旁路脚本。 |
| P-0607 | package.json:255 | package.json:255 文件总长 255 行、scripts 块占 ~120 行（line 47-159 区段），单 manifest 维护成本高；缺 `npm run-many`/Nx/Turbo 等编排   | Reviewed and Closed | 根因是把脚本集中式编排当成独立缺陷；当前仓库明确采用单 package + npm scripts 作为控制面入口，这属于维护偏好而不是运行时 bug。 |
| P-0608 | package.json:243 | package.json:243 把 `typescript` 放在 devDependencies 是正确，但同包 `tsx` 也是 dev，而 scripts:159 `aa:dev` 直接 `node --import tsx` 在 install 后无 tsx 时失败   | Reviewed and Closed | `aa:dev` 已在 README 明确为 development-only 快捷脚本，依赖 devDependencies 属预期，不再视为生产缺陷。 |
| P-0609 | package.json:144<br>AGENTS.md | package.json:144 把 `test` 别名为 `ci:baseline`，导致 `npm test` 强制跑 typecheck+coverage gate，与 AGENTS.md "npm test 运行 --test-concurrency=12" 描述不符   | Fixed | 脚本行为已变为 baseline gate，但仓库文档仍按旧测试语义描述。 |
| P-0610 | package.json:53 | package.json:53 声明 `"prepare": "husky"`，而 .husky/pre-commit 仅 `npx lint-staged`，未 chmod / 缺少 husky v9 init shim 行（一行 hook 在某些 husky 版本下被忽略）   | Fixed | prepare/hook 初始化过于脆弱，缺少对 husky 缺失场景的容错。 |
| P-0611 | package.json:53<br>Dockerfile:23）会因为 | package.json:53 `"prepare": "husky"` 在 `npm ci --omit=dev` 容器内（Dockerfile:23）会因为 husky 是 dev dep 而执行失败 — 实际 `ignore-scripts` 行为依赖 npm 版本，CI 易报错   | Fixed | 生产安装执行 `prepare` 时假定 husky 总可用。 |
| P-0612 | src/sdk/cli/aa.ts"<br>package.json:159<br>Dockerfile | package.json:159 `"aa:dev": "node --import tsx src/sdk/cli/aa.ts"` 在仅安装 production 依赖的环境（如 Dockerfile runtime stage）无法运行，但 README 未标注 "dev only"（与 P-0608 互证）   | Fixed | aa:dev 依赖 tsx 等开发依赖，但文档未说明其只适用于开发环境。 |
| P-0613 | scripts/ci/audit-*<br>package.json | package.json `audit:repo-hygiene` 串行 3 个 audit 子任务（line 138），失败任意一项就阻断 `npm test`，但 `audit:test-exclusions` 等脚本本身在 scripts/ci/audit-* 中位于无 shebang 的 .mjs（见 P-0466），跨平台执行风险   | Fixed | repo hygiene 依赖的审计脚本缺少可执行入口，跨平台执行不稳。 |
| P-0614 | docs_zh/contracts<br>CHANGELOG.md:3<br>package.json:4 | CHANGELOG.md:3 当前 `[0.1.0] - 2026-05-14` 是仓库唯一已发布版本，距今天（2026-05-21）一周未滚版本号；package.json:4 `"version": "0.1.0"` 与之同步，但 git 历史/commits 数远超 0.1 的语义（与 docs_zh/contracts 多次更新互证）   | Fixed | 根因是 changelog 只记录已发版条目，没有给未发布提交留位置，导致所有后续变化都像“忘记滚版本”。本轮在 `CHANGELOG.md` 增加 `Unreleased`，并在 `docs_zh/operations/release-versioning.md` 明确 `0.x` 预 GA 版本策略。 |
| P-0615 | src/sdk/cli/aa.ts:script<br>package.json:217-228 | package.json:217-228 `dependencies` 仅 10 项（OpenTelemetry/ioredis/postgres/ws/xml-crypto/zod），却 src 中存在 `tsx`、`stryker`、`eslint`、`prettier` 等仅 dev 引用 — 但 src/sdk/cli/aa.ts:script `aa:dev` 强依赖 tsx（P-0608 互证），生产 install 后无 tsx，bin `aa` 在 dev 模式空载   | Reviewed and Closed | 生产 `aa` 走构建产物，`aa:dev` 仅供源代码开发态使用；把开发脚本依赖误当成运行时依赖导致了告警。 |
| P-0616 | package.json:144<br>AGENTS.md | package.json:144 `"test": "npm run ci:baseline"`，而 `ci:baseline`(line 51) 串行 `typecheck && audit:repo-hygiene && test:raw && coverage:gate && validate:stable:compiled`，本地 `npm test` 实际跑全量 ~5 步，AGENTS.md 又写"`npm test` 跑 --test-concurrency=12"，三方文档 ↔ 脚本 ↔ 行为不一致（与 P-0609/P-0626 互证）   | Fixed | 脚本行为已变为 baseline gate，但仓库文档仍按旧测试语义描述。 |
| P-0617 | tsconfig.json:60-77 | tsconfig.json:60-77 整批 integration/* 路径被 exclude，CI 中无法 typecheck   | Reviewed and Closed | 根因是把 `tsconfig.json` 的编辑器/辅助配置当成 CI typecheck 合同；CI 实际执行的是 `tsconfig.build.json`（src-only），integration 覆盖由 layered test gate 承担。 |
| P-0618 | tsconfig.json:23<br>tsconfig.json:24-118）大面积裁剪用例 | tsconfig.json:23 同时编译 src 和 tests，但 exclude 列表已膨胀至 90+ 项（tsconfig.json:24-118）大面积裁剪用例   | Reviewed and Closed | 根因是 `tsconfig.json` 的 tests include 主要服务本地 IDE 导航，而大块排除列表已由 `scripts/ci/audit-test-exclusions.mjs` 显式审计；这是 compile-scope 管理，不是失控漂移。 |
| P-0619 | tests/unit/platform/execution/**/*.test.ts<br>tsconfig.json:32 | tsconfig.json:32 整目录排除 `tests/unit/platform/execution/**/*.test.ts`，与 README 宣称"全量回归"冲突   | Reviewed and Closed | 根因是把“TypeScript 静态编译覆盖”与“测试执行覆盖”混成一个概念；该目录虽不进 `tsc`，仍由 `node --test/tsx` 分层运行。 |
| P-0620 | tsconfig.json:23-118 | tsconfig.json:23-118 exclude 列表 90+ 项与 tests/ 目录测试 4682 个文件之间巨大裁剪面（P-0618-0232），CI 不会 typecheck integration 路径——回归覆盖名实背离   | Reviewed and Closed | 根因是把 compile scope、raw layered tests 和 exclusion audit 混成单一“覆盖率”指标；仓内目前分别用 `tsconfig.build.json`、`test:raw` 与 `audit:test-exclusions` 三道门禁控制。 |
| P-0621 | src/scale-ecosystem/billing/billing-service-async.ts:49 | 上述 5 处 require + `// eslint-disable-next-line @typescript-eslint/no-require-imports`（src/scale-ecosystem/billing/billing-service-async.ts:49 等）固化反模式，没有迁移到 `await import()`   | Fixed | 这些 async wrapper 为兼容早期同步实现而保留了 CommonJS `require` 惯性，没有跟随仓库 ESM 化一起收敛。 |
| P-0622 | - | eslint.config.js 未配置 `eslint-plugin-security`/`eslint-plugin-no-secrets`/`eslint-plugin-import` cycle detection，与项目"循环 import"问题（P-0141/0021/0024/0294 等数十处）形成明显工具链缺位   | Fixed | 根因是仓库把 lint 能力完全绑定到 ESLint plugin，而当前环境没有为 cycle/secret 守门建立等价 CI 检查。本轮保留 ESLint 基础规则，同时新增 `scripts/ci/audit-lint-guardrails.mjs`，对相对导入环和敏感字面量做 repo hygiene gate。 |
| P-0623 | src/**/*.ts",<br>scripts/**/*.mjs"]<br>tests/、tools/、helpers/、packs/、deploy/scripts、ui/<br>deploy/、helpers/、packs/ | eslint.config.js:9 `files: ["src/**/*.ts", "scripts/**/*.mjs"]`：tests/、tools/、helpers/、packs/、deploy/scripts、ui/ 均不在 root eslint 范围；ui/ 自带 eslint config 但 deploy/、helpers/、packs/ 完全无 lint   | Fixed | root ESLint 只覆盖 src 和 scripts，把 helpers、packs、tests 等目录排除在静态检查之外。 |
| P-0624 | - | eslint.config.js:11-12 `"@typescript-eslint/no-explicit-any": "warn"` 未升 error；`no-floating-promises`、`no-misused-promises`、`require-await` 全部缺失，无法捕捉 P-0172-0351 的 `void this.x()` fire-and-forget   | Fixed | 根级 ESLint 对 any 与 promise 误用约束过弱，fire-and-forget 类问题进不了默认审查面。 |
| P-0625 | CONTRIBUTING.md<br>AGENTS.md | CONTRIBUTING.md 与 AGENTS.md 关于 commit 风格存在双重来源：AGENTS.md 描述短祈使句、CONTRIBUTING.md 未引用，存在双轨指引   | Fixed | AGENTS 与 CONTRIBUTING 对 commit 规范给出两套口径。 |
| P-0626 | scripts/run-layered-tests.mjs:173<br>AGENTS.md:6 | AGENTS.md:6 声称 `npm test` 使用 `--test-concurrency=12`，但 scripts/run-layered-tests.mjs:173 由 LAYER_DEFINITIONS 动态决定，并非 12   | Fixed | 脚本行为已变为 baseline gate，但仓库文档仍按旧测试语义描述。 |
| P-0627 | src/platform/five-plane-interface/<br>src/platform<br>AGENTS.md:1-? | AGENTS.md:1-? 列出 `src/platform/five-plane-interface/` 等五个 plane 目录，部分与 src 实际平面命名（src/platform 下含 contracts/gateway/prompt 兄弟目录）拼写漂移，且未在 README 同步   | Fixed | AGENTS 只描述五平面主路径，未覆盖 `src/core/` 兼容层与实际目录交叉。 |
| P-0628 | src/platform<br>AGENTS.md | AGENTS.md 列出的 plane 目录与 src/platform 实际目录交叉（contracts/gateway/prompt 与 five-plane-* 同级，P-0627），新人按 AGENTS.md 找文件会落空   | Fixed | AGENTS 只描述五平面主路径，未覆盖 `src/core/` 兼容层与实际目录交叉。 |
| P-0629 | AGENTS.md | divisions 目录共 67 个 yaml + 32 个 json，加上 32 个一级 division 目录（academic-research/...）；其中 P-0459 已记 qa vs quality-assurance 重复，进一步：engineering_ops vs general_ops vs operations vs it-operations 同义性高，未在 README/AGENTS.md 给出归并图   | Fixed | 根因是 division 家族只有目录名，没有机器可读或文档可读的 family map。本轮新增 `config/quality/division-catalog.json` 和 `docs_zh/reference/division-catalog.md`，并在 `README.md`、`AGENTS.md` 显式说明 quality/operations 家族分工。 |
| P-0630 | docs_zh/architecture/00-platform-architecture.md<br>README.md:3 | README.md:3 引用 `docs_zh/architecture/00-platform-architecture.md`，但 docs_en 等价文件未在 README 注明，双语门户缺失   | Fixed | README 只暴露中文架构入口，缺少英文入口。 |
| P-0631 | docs_zh/architecture/00-platform-architecture.md"<br>README.md:3 | README.md:3 强调 "v2.7 七层架构 docs_zh/architecture/00-platform-architecture.md"，但 src 实际平面命名为 five-plane-*，平面数（5）与七层架构数（7）口径不一致   | Fixed | README 未解释七层架构与五平面运行时的关系。 |

