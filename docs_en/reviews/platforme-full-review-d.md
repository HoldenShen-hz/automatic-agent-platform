## src/platform/five-plane-interface

| ID | Issue | Status | Root Cause |
| --- | --- | --- | --- |
| 1 | iam/audit-event-integrity.ts:43-44, distributed-rate-limiter.ts:47, request-deduplication.ts:82, http-api-server.ts:119-122, http-server/health-routes.ts:19-21 directly read `process.env.NODE_ENV`/constants in-library, breaking DI and testing | `done` | The root cause is that the interface layer and IAM helper modules embed environment detection inside the library, lacking a clear boundary for injecting runtime configuration via options/deps. |
| 2 | stryker.config.mjs:30-33 mutate: only 9 files concentrated in http-server/, coverage far narrower than the strategy document claims | `done` | The root cause is that the mutation test configuration has long relied on a manually-maintained named list, and did not expand as the interface layer grew. |
| 3 | src/platform/five-plane-interface/api/http-server/harness-runs-routes.ts:82 user cursor `JSON.parse` without try/catch, malicious cursor → 500 | `done` | The root cause is that the pagination cursor was hand-decoded in the route, without reusing a unified error boundary. |
| 4 | src/platform/five-plane-interface/api/http-server/harness-runs-routes.ts:103-104 module-level `InMemoryHarnessRunStore` singleton, shared across requests, data lost on restart | `done` | The root cause is that the early placeholder implementation made the store a module-level singleton rather than a route dependency injected via DI. |
| 5 | src/platform/five-plane-interface/api/http-server/harness-runs-routes.ts:159-162 the `/events` interface always returns an empty array, not wired to Truth | `done` | The root cause is that the events interface was only a route placeholder, without dropping the create/update lifecycle events into a readable store. |
| 6 | src/platform/five-plane-interface/api/http-server/harness-runs-routes.ts:209,217-218,228 `body.riskLevel/status` directly cast as 强转 with no enum validation | `done` | The root cause is missing input validation, relying on TypeScript casts instead of runtime constraints. |
| 7 | src/platform/five-plane-interface/api/http-server/harness-runs-routes.ts:273-279 PATCH writes `body.status/terminalReason` directly to the store, with no whitelist | `done` | The root cause is that the PATCH logic directly maps the request body to a model update, with no field whitelist or terminal-state constraint. |
| 8 | src/platform/five-plane-interface/api/http-server/harness-runs-routes.ts:76-89 list per request `Array.from+sort` O(n log n) full re-sort | `done` | The root cause is that the list interface was written as "fetch-and-sort on the fly" temporary implementation, becoming a per-request re-sort. |
| 9 | src/platform/five-plane-interface/webhook/index.ts:73-74 `acceptedEnvelopes/failureCounts` unbounded growth | `done` | The root cause is that the webhook entry assumed a short-lived process, lacking capacity governance and reclamation strategy. |
| 10 | src/platform/five-plane-interface/webhook/index.ts:72 `envelopesByIdempotencyKey` lacks TTL/upper bound | `done` | The root cause is that the idempotency cache only implemented deduplication semantics, without TTL or capacity eviction. |
| 11 | src/platform/five-plane-interface/webhook/index.ts:111-120 event type/allow-list validation precedes signature validation, unsigned probes can enumerate allowedEventTypes | `done` | The root cause is that the validation order is organized by business fields, without first doing authentication and then authorization filtering. |
| 12 | src/platform/five-plane-interface/webhook/index.ts:207-209 failure count hardcoded at 50, no re-activation path after auto-disable | `done` | The root cause is that the failure breaker threshold is hardcoded in the implementation, and lacks an explicit recovery operation. |
| 13 | src/platform/five-plane-interface/webhook/index.ts:200-211 `recordDeliveryFailure` directly mutates the registered object's `enabled`, breaking immutability contract | `done` | The root cause is that the registered object is treated as mutable runtime state and directly written back, instead of being regenerated and replaced. |
| 14 | src/platform/five-plane-interface/webhook/index.ts:182-184 `rollbackAcceptedEnvelope` `findIndex` linear search | `done` | The root cause is that the accepted list is only represented as an array, without a reverse index supporting rollback deletion. |
| 15 | src/platform/five-plane-interface/webhook/index.ts:296-315 `parseWebhookPayload` does not limit body size, oversized JSON blocks the event loop | `done` | The root cause is that payload parsing only cares about JSON structure, not about input volume upper bound. |
| 16 | src/platform/five-plane-interface/api/middleware/idempotency-key.ts:190,250 get-before-set non-atomic, concurrent same Idempotency-Key double-write | `done` | The root cause is that the idempotency middleware only exposes abstract get/set store interface, lacking atomic reservation semantics. |
| 17 | src/platform/five-plane-interface/api/middleware/idempotency-key.ts:206-217 in-flight branch returns `allowed:true` while also attaching 409, semantic conflict | `done` | The root cause is that "duplicate request accepted" was incorrectly modeled as `allowed`, while still attaching a conflict error. |
| 18 | src/platform/five-plane-interface/api/middleware/idempotency-key.ts:201 error message echoes the user idempotencyKey/method, response injection risk | `done` | The root cause is that the error text directly concatenates user input, without minimizing the echo. |
| 19 | src/platform/five-plane-interface/api/middleware/idempotency-key.ts:222-234 cached response `JSON.parse` with no size limit | `done` | The root cause is that the idempotency replay path only validates JSON parseable, without limiting cached body size. |
| 20 | src/platform/five-plane-interface/api/http-server/approval-routes.ts:73 user `requestJson` `JSON.parse` with no size limit | `done` | The root cause is that the approval `requestJson` was treated as a trusted internal field, missing the size-upper-bound protection. |
| 21 | src/platform/five-plane-interface/api/http-server/dashboard-routes.ts:344 dashboard `requestJson` `JSON.parse` same as above | `done` | The root cause is that the dashboard reused the same "directly parse requestJson" pattern, with no volume guardrail. |
| 22 | src/platform/five-plane-interface/api/http-server/utils.ts:339,344 cursor base64url encode/decode without try/catch, no integrity signature, can be tampered | `done` | The root cause is that the generic cursor originally only did reversible encoding, without adding signing to prevent tampering. |
| 23 | src/platform/five-plane-interface/api/http-server/gateway-routes.ts:125 body non-string case is forwarded after `JSON.stringify`, losing byte-order signature failure | `done` | The root cause is that the webhook receiving route mixed "raw body" with "parsed object", silently re-serializing the input. |
| 24 | src/platform/five-plane-interface/api/http-server/task-routes.ts:340,357 `JSON.stringify(payload)` persistence does not constrain field order | `done` | The root cause is that the persistence path still uses regular `JSON.stringify`, rather than stable serialization. |
| 25 | src/platform/five-plane-interface/webhook/index.ts:255 `Buffer.from(normalizedSignature,"hex")` accepts non-hex and truncates, length comparison masks pollution | `done` | The root cause is missing pre-signature validation, defaulting to Buffer's lenient hex decode behavior. |
| 26 | src/platform/five-plane-interface/webhook/index.ts:60-61 replay cache TTL/capacity are module constants, not configurable per tenant/endpoint | `done` | The root cause is that the replay cache parameters were originally hardcoded as module constants, not exposed to the service/endpoint configuration layer. |
| 27 | tests/integration/platform/interface/api/grpc-adapter-service-integration.test.ts:24,47,145,178,207,337 6 places of `host:"0.0.0.0"` listen on all interfaces | `done` | The root cause is that integration tests took a shortcut of reusing wildcard listen addresses, instead of explicit loopback. |
| 28 | api-server-env.ts reads `AA_API_KEYS_JSON` but the documentation says `AA_API_KEYS` | `done` | The root cause is that the env loader split structured config and compatibility variable into two names, with documentation and implementation drifting long-term. |
| 29 | control plane deeply reaches state-evidence plane SQLite private paths (multiple places in approval/config/incident-control) | `done` | The root cause is that the control plane historically directly reached into the state-evidence plane's private SQLite implementation, lacking a stable truth public exit. |
| 30 | execution plane has ~40 places importing control plane IAM/config implementation details, not going through contract/policy ports | `done` | The root cause is that the execution plane long reused control plane implementation details rather than public index/contract ports, leading to cross-plane boundary leakage. |
| 31 | iam/field-encryption.ts:10,24 PBKDF2 only 100k iterations + sync `pbkdf2Sync`, below OWASP 600k and blocks the event loop | `done` | The root cause is that field encryption previously bound "password derivation" and "runtime encrypt/decrypt" to the sync path, limiting both strength and the event loop. |
| 32 | iam/session-management.ts:164-167 `hashToken` uses raw `sha256(token)`, file comment admits it should use HMAC | `done` | The root cause is that the session index was originally implemented as a plain digest, lacking a server-side keyed secret. |
| 33 | tests/integration/platform/control-plane/config-center/config-rollout-service-integration.test.ts:317,332,355 `Date.now()-90000000` annotated as "25h" but actually 25h00m00s, equals TTL causing jitter | `done` | The root cause is that the test data has long used bare millisecond literals, with poor readability and easily mistaken for boundary jitter. |
| 34 | tests/integration/platform/control-plane/incident-control/doctor.test.ts:1096 deletes `process.env.AA_EXPECTED_PROTECTED_GOVERNANCE_VERSION` with no pre-capture | `done` | The root cause is that the review was based on an old version; the current test first captures and restores the env var in `finally`. |
| 35 | src/platform/five-plane-control-plane/policy-center/index.ts:282 emergency mode `requiresApproval=subjectType!=="system"`, system principal bypasses break-glass approval | `done` | The root cause is that the break-glass logic incorrectly treats the system principal as naturally trusted, missing the unified approval gate. |
| 36 | src/platform/five-plane-control-plane/approval-center/approval-policy-engine/rule-engine.ts:120-132 `getFieldValue` walks `.` paths, without rejecting `__proto__/constructor/prototype` | `done` | The root cause is that the rule engine only considered business field navigation, without treating prototype-chain paths as untrusted input. |
| 37 | src/platform/five-plane-control-plane/config-center/config-hot-reload-service.ts:450 `JSON.stringify(config, Object.keys(config).sort())` misuse of `replacer` as a key whitelist, nested fields get trimmed | `done` | The root cause is that the version calculation took a shortcut with shallow key sorting, without implementing real recursive stable serialization. |
| 38 | src/platform/five-plane-control-plane/config-center/config-hot-reload-service.ts:451-457 32-bit non-cryptographic hash used for config checksum, with obvious collision probability | `done` | The root cause is that the hot-reload version number used a lightweight string hash, rather than a strong integrity digest for config completeness. |
| 39 | src/platform/five-plane-control-plane/iam/secret-management-service.ts:765-768 `startDailyRotationSchedulers` entry clears then adds, reentry loses the in-flight sweep | `done` | The root cause is that the rotation scheduler handled "duplicate startup" as silent rebuild, breaking the existing schedule handle and in-flight sweep. |
| 40 | src/platform/five-plane-control-plane/iam/secret-management-service.ts:770-776 `runRotationSweep("initial")` and `setInterval` start at the same time, possibly concurrent same sweep | `done` | The root cause is the review's mistaken belief that the sync initial sweep runs concurrently; the current implementation is converged to a single-instance scheduler, and the concurrent restart path no longer exists. |
| 41 | src/platform/five-plane-control-plane/iam/aws-kms-http-secret-provider.ts:358-364 double base64 decode assumes KMS always returns base64 | `done` | The root cause is that the implementation forced the KMS Plaintext field into the local ciphertext storage encoding assumption, confusing the provider protocol with the local configuration format. |
| 42 | src/platform/five-plane-control-plane/iam/gcp-secret-manager-http-secret-provider.ts:256 GCP secret return value is not validated as base64 | `done` | The root cause is that the provider defaults to trusting the cloud payload encoding, without strict base64 validation and failure branching. |
| 43 | src/platform/five-plane-control-plane/incident-control/runbook-executor/runbook-executor.ts:192-198,258-266 runbook executor only simulates, production path not wired | `done` | The root cause is that the runbook executor was originally only a drill placeholder, with no abstraction for a controlled command-execution boundary; it is now converged into an injected read-only executor that fail-closes for non-read-only commands. |
| 44 | src/platform/five-plane-control-plane/config-center/config-hot-reload-service.ts config hash only takes top-level keys, not recursive | `done` | The root cause is that the config version function was only designed around the top-level object, without stable recursive traversal for nested structures. |
| 45 | src/platform/five-plane-control-plane/iam/field-encryption.ts:46 `Buffer.from(value,"base64")` directly decodes, without rejecting pure utf-8 input | `done` | The root cause is that the ciphertext envelope parsing depends on Node's lenient base64 decode behavior, without round-trip validation. |
| 46 | src/platform/five-plane-control-plane/approval-center/approval-policy-engine/rule-engine.ts:128 arbitrary string parts index to `Function.prototype` etc., false positive/negative matches | `done` | The root cause is that the field access neither blocks dangerous fragments nor limits own-property access. |
| 47 | src/platform/five-plane-control-plane/iam/secret-management-service.ts:805-815 `requireRegistryRecord` error path's `details` repeatedly leaks raw `secretRef` | `done` | The root cause is that the storage error directly reused the original `secretRef` as message/details, without minimal disclosure. |
| 48 | tests/unit/platform/control-plane/iam/sandbox-policy-extended.test.ts:66 `/tmp/test-file-${Date.now()}` escapes the sandbox-root test matrix | `done` | The root cause is that the test previously depended on host `/tmp` semantics, bypassing the in-repo controlled temp workspace. |
| 49 | startup-env-schema.ts:376 JWT key `undefined` falls through to default-allow, can still sign without a key | `done` | The root cause is that the review was based on the old validation logic; the current schema requires a JWT secret when configuring API key auth. |
| 50 | api-client.ts `Retry-After` directly `parseInt`, doesn't recognize HTTP-date | `done` | The root cause is that the old implementation only supported delta-seconds; the current parse function has added the HTTP-date branch. |
| 51 | `test:secret-providers` path error (missing a `platform/` layer) | `done` | The root cause is that the script path was not synced after the test directory migration; the current `package.json` already points to the current integration path. |
| 52 | auto-stop-loss-service.ts:789, config-hot-reload-service.ts:268,506, cache-invalidation-broadcast.ts:68, durable-event-bus.ts:710,916,1007, call-governance.ts:609, external-secret-provider.ts:226 multiple `void` promise fire-and-forget without `.catch` | `done` | The root cause is that multiple infrastructure modules treat "background tasks" as ignorable details, missing rejection observability and cleanup. |
| 53 | aws-kms-http-secret-provider.ts:211, gcp-secret-manager-http-secret-provider.ts:103, vault-http-secret-provider.ts:132 `setTimeout(...controller.abort)` not `.unref()` and some success paths miss `clearTimeout` | `done` | The root cause is that the provider timeout helper was written as a minimal-available implementation, ignoring process-exit blocking and timer governance. |
| 54 | secret-management-service.ts:765-768 `startDailyRotationSchedulers` silently clears existing schedulers, external handle becomes invalid | `done` | The root cause is that the scheduler lifecycle does not distinguish between "first start" and "repeated call", leading to silent replacement of existing handles. |
| 55 | client-sdk/api-client.ts:188 `(result as { totalCount?: number }).totalCount = totalCount` rewrites a readonly field through cast | `done` | The root cause is that the pagination response, for compatibility with optional fields, took a type-assertion rewrite rather than reconstructing the object. |
| 56 | client-sdk/api-client.ts:368 `connect()` is fire-and-forget at SSE bootstrap, initial fetch rejection not handled | `done` | The root cause is the review was based on an old understanding; the current `connect` internally handles connection exceptions and goes through a reconnect branch, the bootstrap site only retains an explicit `void` call. |

## src/platform/five-plane-orchestration

| ID | Issue | Status | Root Cause |
| --- | --- | --- | --- |
| 57 | `src/platform/agent-delegation/index.ts` and `five-plane-orchestration/agent-delegation/*` form a dual entry | `done` | The root cause is that the legacy facade was preserved to fill in directory-structure auditing early on, and subsequent tests and structural assertions continued to consume it, so the public surface was not closed to the canonical `five-plane-orchestration` entry in time. |
| 58 | `config/quality/test-exclusion-allowlist.json` lists `tests/integration/platform/orchestration/**` (renamed to `five-plane-orchestration`), never hits | `done` | The root cause is that after the directory rename the allowlist only kept the old path, and did not update with the canonical layout. |
| 59 | oapeflir/runtime-execute-bridge.ts:223-235 when executor is null, synthesizes a fake `succeeded` + `validationPassed:true`, stub silently returns | `done` | The root cause is that the bridge defaults to returning a fake success result for early integration, hiding the real executor's absence. |
| 60 | oapeflir/runtime-execute-bridge.ts:182 `defaultModelId="MiniMax-M2.7"` hardcodes a specific vendor's model into framework code | `done` | The root cause is that the framework default directly inherited a certain integration model, rather than being abstracted as a vendor-neutral default. |
| 61 | oapeflir/runtime-execute-bridge.ts:194,264,316 `createdAt: Date.now()` numeric vs `Plan.createdAt: string` type drift, only relying on `as Plan` cast | `done` | The root cause is the review based on old DTO understanding; the current `Plan.createdAt` is still a millisecond timestamp, and has been changed to a unified ISO->ms normalization. |
| 62 | oapeflir/handoff-model.ts:55-57 `Math.ceil(JSON.stringify(value).length/4)` token estimation is distorted for CJK/multi-byte | `done` | The root cause is that handoff compression only estimates tokens by ASCII text experience, ignoring UTF-8 multi-byte differences. |
| 63 | oapeflir/handoff-model.ts:88-135 compression silently drops `historyRefs/toolCallRecords/planDelta/blockers/artifactRefs`, no drop ledger | `done` | The root cause is that handoff compaction only focuses on reducing size, without keeping audit traces for trimmed fields. |
| 64 | oapeflir/oapeflir-loop-core.ts:382, oapeflir-loop-support.ts:324, stage-transition-fsm.ts:189-223 multiple `Date.now()` self-timestamping, non-monotonic on clock rollback | `done` | The root cause is that OAPEFLIR internal event time originally took wall clock directly, without abstracting into a monotonically-increasing time source. |
| 65 | oapeflir/oapeflir-loop-core.ts:299 writes `process.{version,platform,cwd()}` into `environmentContext` to retain evidence, leaking host fingerprint | `done` | The root cause is that the fallback observation directly collects host context for debugging convenience, lacking the evidence-minimal-disclosure constraint. |
| 66 | `docs_zh/contracts/oapeflir_loop_contract.md` exists but is not listed in README; ADR-016 references OAPEFLIR but does not link | `done` | The root cause is that after the contract/ADR documents were added, README and related ADRs did not synchronously complete the reference chain. |
| 67 | `scripts/ci/mutation-critical-tests.sh:13` references `tests/unit/platform/orchestration/oapeflir/...` while the authoritative path is `five-plane-orchestration`, silently zero tests after rename | `done` | The root cause is that after the test directory was renamed, the mutation critical script still referenced the old path. |
| 68 | `src/platform/five-plane-orchestration/oapeflir/runtime-execute-bridge.ts:275` `super("mock://runtime","local-simulated")` defaults to mock runtime | `done` | The root cause is that the mock bridge directly bakes mock runtime URI/modelId into the default parent constructor, blurring the boundary between the real bridge and the test double. |
| 69 | `scripts/ci/audit-oapeflir-terminology.mjs` only scans eight-letter spelling, missing Chinese term drift | `done` | The root cause is that the terminology audit script only validates the English stage word order, without including the Chinese canonical terms in the check. |

## src/platform/five-plane-execution

| ID | Issue | Status | Root Cause |
| --- | --- | --- | --- |
| 70 | plugin-executor.service.ts:482 explicitly throws `action_not_implemented`, missing hook → 500 | `done` | The root cause is that the plugin executor treats unimplemented hooks as exception path, rather than falling back to a structured rejected result. |
| 71 | `five-plane-execution/state-transition/*` re-exported via `core/runtime/index.ts`, crossing the boundary | `done` | The root cause is that `core/runtime` continues to forward the execution-plane state-transition implementation for compatibility with old callers, expanding the cross-layer public surface. |
| 72 | `tests/unit/runtime/`, `platform/execution/`, `platform/five-plane-execution/` are parallel duplicates | `done` | The root cause is that after multiple rounds of directory renaming, the execution plane kept three parallel test trees: `runtime`, `platform/execution`, `five-plane-execution`; this batch has deleted the duplicated old trees, migrated compatible coverage, and added duplicate-path audit tests to prevent regression. |
| 73 | plugin-executor.service.ts:106 `enforceSignatures` default `false`, unsafe fail-open when env not set | `done` | The root cause is that the plugin signature verification originally prioritized integration convenience, defaulting to fail-open; secure defaults were not tightened for the external plugin scenario. |
| 74 | `src/platform/five-plane-execution/distributed-lock/sqlite-lock-adapter.ts:55-90` SELECT/DELETE/INSERT not in a transaction, TTL expiry eviction TOCTOU; concurrent lock steal can wrongly delete the just-acquirer | `done` | The root cause is that the SQLite lock adapter was originally implemented by stitching per-statement scripts, without bringing expiry cleanup and lock-steal into a single-transaction critical section. |
| 75 | `src/platform/five-plane-execution/distributed-lock/sqlite-lock-adapter.ts:34-37` `distributed_lock_fencing_tokens` only INSERT auto-increment, never cleaned up, unbounded growth | `done` | The root cause is that the fencing token was designed as an append-only counter table, but no synchronous planning for compaction or bounded storage strategy. |
| 76 | `src/platform/five-plane-execution/distributed-lock/sqlite-lock-adapter.ts:54` `ttlMs` has no lower-bound check, negative/0 written directly | `done` | The root cause is that the lock TTL only does upper-bound trimming, without establishing a minimum valid lease constraint. |
| 77 | `src/platform/five-plane-execution/distributed-lock/sqlite-lock-adapter.ts:135-148` `forceSteal` has no prior authorization/reason whitelist, any call can steal | `done` | The root cause is that `forceSteal` was exposed as an internal operations shortcut, with the implementation missing an explicit authorization-reason boundary. |
| 78 | `src/platform/five-plane-execution/distributed-lock/sqlite-lock-adapter.ts:140` `forceSteal` hardcodes `ttlMs=30000` instead of reusing the original lock config | `done` | The root cause is that the forced-steal path evolved from an example implementation, hardcoding the TTL constant in the branch without reusing the lock config strategy. |
| 79 | `src/platform/five-plane-execution/distributed-lock/sqlite-lock-adapter.ts:107-112` `release` silently returns `false` on owner mismatch with no audit event | `done` | The root cause is that release failure was long modeled only as a boolean result, without treating owner mismatch as an exception contention event requiring audit. |
| 80 | `src/platform/five-plane-execution/queue/redis-queue-adapter.ts:549-573` `dequeueAsync` has no atomicity between multiple awaits, two workers can take the same jobId | `done` | The root cause is that the Redis queue was originally implemented by stringing multiple commands for the claim flow, without using a single script to atomically take the number and switch state. |
| 81 | `src/platform/five-plane-execution/queue/redis-queue-adapter.ts:569` `hincrby attempts` before status confirmation, status reset causes count misalignment | `done` | The root cause is that the attempts budget is incremented before a successful claim, decoupling the count semantics from the real lifecycle. |
| 82 | `src/platform/five-plane-execution/queue/redis-queue-adapter.ts:566-568` status not `waiting` then `zrem` returns null, silently drops task | `done` | The root cause is that the old `dequeue` mixed contention failure and abnormal task status into the same path, missing explicit recovery or re-queue branching. |
| 83 | `src/platform/five-plane-execution/queue/redis-queue-adapter.ts:592-596` only ack path expires task keys, nack/failed tasks accumulate without TTL key | `done` | The root cause is that the task retention policy only covers the success path, without defining a unified TTL reclamation for nack, dead-letter, and failure states. |
| 84 | `src/platform/five-plane-execution/queue/redis-queue-adapter.ts:605-609` nack re-queue has no backoff, immediately re-pulled by same worker | `done` | The root cause is that the nack implementation only pursues "retry as soon as possible", without introducing a minimum backoff window to avoid hot loops. |
| 85 | `src/platform/five-plane-execution/queue/redis-queue-adapter.ts:664-672` `retryJobAsync` forces `attempts=0` bypassing maxAttempts budget | `done` | The root cause is that the manual retry path was treated as a full reset, breaking the attempts budget's consistency with the dead-letter policy. |
| 86 | `src/platform/five-plane-execution/queue/redis-queue-adapter.ts:600` nack closure references stale `jobData.priority` | `done` | The root cause is that the nack logic's closure reused an old snapshot, and the authoritative priority is not re-read when the task is re-queued. |
| 87 | `src/platform/five-plane-execution/queue/redis-queue-adapter.ts:675-695` `purgeAsync` does individual `hgetall` for each ID, N+1 RTT | `done` | The root cause is that the purge process was first implemented per-record for readability, without batched design, leading to obvious N+1 round-trips under high latency. |
| 88 | `src/platform/five-plane-execution/plugin-executor/scoped-external-access-sandbox.ts:308-317` first `arrayBuffer()` reads all, then checks size, vulnerable to OOM on oversized response | `done` | The root cause is that the external access sandbox first pursued simplicity in the fetch API usage, without moving the response-stream size guard up to the streaming-read phase. |
| 89 | `src/platform/five-plane-execution/plugin-executor/scoped-external-access-sandbox.ts:323-324` `JSON.parse` without try/catch, malicious JSON throws directly | `done` | The root cause is that the JSON response is assumed to be a trusted structure, missing parse-failure isolation and structured error conversion. |
| 90 | `src/platform/five-plane-execution/plugin-executor/scoped-external-access-sandbox.ts:296-298` `Content-Type` case-sensitive comparison, caller's `Content-type` would double-write headers | `done` | The root cause is that the HTTP header normalization convention was not enforced at the sandbox adaptation layer, with case compatibility relying on caller awareness. |
| 91 | `src/platform/five-plane-execution/plugin-executor/sub-workflow-executor.ts:672-678` `simulateStepExecution` only `setTimeout` 50ms, doesn't really run any action | `done` | The root cause is that the sub-workflow executor originally only had a placeholder delay, with no abstraction for an injectable step-execution boundary. |
| 92 | `src/platform/five-plane-execution/plugin-executor/sub-workflow-executor.ts:729-733` `simulateRollback` is no-op, rollback always succeeds | `done` | The root cause is that rollback was kept as an empty implementation for demo paths, without a real rollback executor and failure branch. |
| 93 | `src/platform/five-plane-execution/plugin-executor/sub-workflow-executor.ts:665-669` `findStepDefinition` iterates all executions O(N·M) | `done` | The root cause is that the step definition is not attached to the execution's local index, so at runtime it can only be looked up globally. |
| 94 | `src/platform/five-plane-execution/plugin-executor/sub-workflow-executor.ts:642` `step.output` hardcoded as "Step X completed successfully" overwrites real output | `done` | The root cause is that the default happy path directly backfills templated output, rather than consuming the real result of the step executor. |
| 95 | `src/platform/five-plane-execution/compensation-manager.ts:312-319` `reverseExternalEffect` directly returns `true`, external side effects not reversed | `done` | The root cause is that the compensation manager only implemented the state machine skeleton, without abstracting reverse external side effects into an executable adapter. |
| 96 | `src/platform/five-plane-execution/compensation-manager.ts:326-333` `executeCompensateAction` stub always true | `done` | The root cause is that the compensate action has remained a stub, lacking step/context-based execution decisions. |
| 97 | `src/platform/five-plane-execution/compensation-manager.ts:339-344` `sendCompensationNotification` empty body, notifications never sent | `done` | The root cause is that the notification step has no independent adapter; the old implementation treated notify as a perpetually-successful bypass. |
| 98 | `src/platform/five-plane-execution/compensation-manager.ts:350-358` `executeRollback` stub, rollback contract not implemented | `done` | The root cause is that the rollback step does not consume the rollback plan / targetRef, only keeping an example function shell. |
| 99 | `src/platform/five-plane-execution/execution-engine/phase1a-happy-path.ts:1-6` only re-export, should be removed entirely per AGENTS | `done` | The root cause is that the execution engine, after renaming, kept a phase1a compatibility shell, and the old naming was not removed from the source surface in time. |
| 100 | `src/platform/five-plane-execution/execution-engine/phase1b-orchestration.ts:1-31` compatibility file maintains redundant naming | `done` | The root cause is that after multi-step orchestration was closed, phase1b alias files remained, and the public surface was not fully switched to canonical naming. |
| 101 | `src/platform/five-plane-execution/execution-engine/phase1b-tool-definitions.ts` & `phase1b-utils.ts` phase1b compatibility residue | `done` | The root cause is that phase1b's supporting tool definitions and utility functions continued to use the old aliases, maintaining dual-file representations of the same capability. |
| 102 | `src/platform/five-plane-execution/recovery/runtime-recovery-service.ts` and `runtime-recovery-service-root.ts` and other four `*-service/*-service-root` twin pairs | `done` | The root cause is that during the recovery module migration, a root alias file was preserved for compatibility with old entry points, creating factually-duplicate implementation branches at the source level. |
| 103 | `src/platform/five-plane-execution/execution-engine/multi-step-orchestration.ts` 581 lines single file violates AGENTS small modules principle | `done` | The root cause is that the multi-step orchestration originally piled plan parsing, bootstrap, HarnessRun persistence, and final-state cleanup in the entry file; it has now been split into `plan/bootstrap/finalize` support modules, with the entry collapsed to a 202-line coordinator. |
| 104 | `src/platform/five-plane-execution/execution-engine/multi-step-supervisor.ts` 814 lines, same as above | `done` | The root cause is that the supervisor long mixed breakpoint handling, failure branches, successful commits, and the main loop in one file; it has now been split into three support modules `breakpoint/failure/success`, with the main loop collapsed to 358 lines. |
| 105 | `src/platform/five-plane-execution/dispatcher/execution-priority-preemption-service-async.ts:47` uses CJS `require` inside ESM | `done` | The root cause is that the async wrapper layer once borrowed `require` to load sibling modules for reusing the sync service, without maintaining the ESM static dependency graph. |
| 106 | `src/platform/five-plane-execution/distributed-lock/locking-support.ts:12` `require("postgres")` synchronous load, optional dependency coupling | `done` | The root cause is that the distributed-lock support layer once disguised backend dependencies as runtime `require` branches; the dependency is neither transparent nor conducive to static analysis. |
| 107 | `src/platform/five-plane-execution/distributed-lock/redis-lock-adapter.ts:67-68` construct-time `createRequire+require("ioredis")`, missing dependency crashes startup | `done` | The root cause is that the Redis lock adapter continued the `createRequire`-style dynamic assembly; dependency load failure can only blow up at construction. |
| 108 | `src/platform/five-plane-execution/distributed-lock/redis-lock-adapter.ts:167,226` lock id uses `Date.now()` string concatenation, millisecond-level collision reusable | `done` | The root cause is that the lock ID originally only concatenated timestamp and business fields, without introducing a true globally unique entropy source. |
| 109 | `src/platform/five-plane-execution/dispatcher/execution-priority-preemption-service-async.ts:25-47` dynamic `require` of sibling `.js` after `createRequire`, module graph not statically analyzable | `done` | The root cause is that the async facade once tried to pull the same-directory implementation back at runtime, making the module graph invisible to tooling. |
| 110 | `src/platform/five-plane-execution/distributed-lock/distributed-lock-manager.ts` 9 lines dead shim | `done` | The root cause is that after the distributed-lock directory was reorganized, the legacy manager thin shell was preserved, and callers were slow to be converged to a single public surface. |
| 111 | `src/platform/five-plane-execution/distributed-lock/distributed-lock-service.ts` 10 lines dead shim | `done` | The root cause is that the service file was originally just a forwarding shell, with no real public-entry responsibilities, leading to manager/service concept duplication. |
| 112 | `src/platform/five-plane-execution/distributed-lock/distributed-lock-factory.ts` 21 lines lightweight wrapper overlapping with manager/service | `done` | The root cause is that lock-adapter creation logic was scattered into multiple lightweight wrappers, and historical compatibility files accumulated. |
| 113 | `src/platform/five-plane-execution/execution-engine/runtime-context.ts` 1 line shim | `done` | The root cause is that after the directory migration, runtime context kept an empty-shell forwarding layer, without redirecting callers to the shared/context canonical path. |
| 114 | `src/platform/five-plane-execution/execution-engine/single-task-execution.ts` 7 lines re-export shim | `done` | The root cause is that the single-task happy path, after rename, continued to maintain the old filename's re-export, creating redundant entry points at the source surface. |
| 115 | `src/platform/five-plane-execution/distributed-lock/index.ts` 1 line barrel | `done` | The root cause is that the directory-level barrel continued to expose historical paths after the public surface was already converged, amplifying import branching. |
| 116 | `src/platform/five-plane-execution/queue/redis-queue-adapter.ts:560-562` takes the highest score (newest), violating FIFO intuition, undocumented | `done` | The root cause is that the waiting queue's claim path iterated Redis `ZRANGE` results in reverse, so the actual consumption order quietly degraded to "newest first". |
| 117 | `src/platform/five-plane-execution/plugin-executor/sub-workflow-executor.ts:686-693` checkpoint state directly references original `entries` array, later mutations pollute history checkpoint | `done` | The root cause is that the checkpoint record directly reuses the in-memory object reference, without doing a snapshot-level deep copy. |
| 118 | `src/platform/five-plane-execution/plugin-executor/sub-workflow-executor.ts:706-713` `performRollback` when `rollbackHistory` is empty, marks all as `rolled_back`, without distinguishing un-executed steps | `done` | The root cause is that an empty `rollbackHistory` was mistakenly treated as "all steps can be rolled back", and the state machine did not distinguish un-executed vs rolled-back. |
| 119 | `src/platform/five-plane-execution/plugin-executor/scoped-external-access-sandbox.ts:286-288` `AbortController.unref` then timeout doesn't block exit, but no `await` cleanup after abort and ESM top-level race | `done` | The root cause is the old review remained at the pre-timeout-cleanup implementation; the current request path has placed the timeout in `finally` for cleanup, and the underlying issue was that timeout lifecycle governance was never explicitly modeled. |
| 120 | `src/platform/five-plane-execution/recovery/runtime-recovery-service-root.ts` and `runtime-recovery-service.ts` only differ in import path and a few variable names, forming a factual branch | `done` | The root cause is that during the recovery service migration, the root version alias source was kept, ultimately forming a branch that only differs in path and variable name. |
| 121 | `src/platform/five-plane-execution/distributed-lock/sqlite-lock-adapter.ts:60,87,119-121` `Math.min(ttlMs,MAX_LOCK_TTL_MS)` three places duplicated, constant `600_000ms` hardcoded | `done` | The root cause is that the TTL trim rules are spread and duplicated in multiple branches, without being abstracted into a unified lease normalization function. |
| 122 | `src/platform/five-plane-execution/distributed-lock/sqlite-lock-adapter.ts:115-127` extend uses `MIN(ttl_ms+?, MAX)` to accumulate instead of resetting from now, TTL always slides to the upper bound | `done` | The root cause is that the extend semantics was originally implemented as "accumulate remaining TTL" rather than "re-lease from current moment". |
| 123 | pg-advisory-lock-adapter.ts: no try/finally after acquiring lock, lock leaks on throw path | `done` | The root cause is that advisory lock acquire only considered the happy path, leaving the "acquire succeeded but local bookkeeping failed" cleanup path missing outside session locks. |
| 124 | pg-advisory-lock-adapter.ts:34-43 custom FNV-1a truncated to 63 bits, collisions silently accepted | `done` | The root cause is that the PG advisory key generation directly used a lightweight example hash, rather than a more stable cryptographic hash mapping. |
| 125 | pg-advisory-lock-adapter.ts:71-83 `extend()` only changes in-memory map, does not refresh PG-side advisory lock TTL | `done` | The root cause is the review placed PG advisory lock into a lease-type TTL mental model; PG itself has no server TTL, and the old implementation lacks an explicit constraint on the "only refresh client-side lease metadata" semantics. |
| 126 | pg-advisory-lock-adapter.ts:107-115 catch-all disguises transient PG errors as "lock taken" | `done` | The root cause is that the adapter folded all driver exceptions into `acquire=false`, smoothing out the boundary between "lock occupied" and "backend temporarily unavailable". |
| 127 | pg-advisory-lock-adapter.ts:101 `Number(result.fencing_token)` exceeds 2^53 precision loss | `done` | The root cause is that the fencing token was read back from the database bigint and directly cast to a JS number, without first performing a safe-range check. |

## src/platform/five-plane-state-evidence

| ID | Issue | Status | Root Cause |
| --- | --- | --- | --- |
| 128 | CLAUDE.md:50 references a non-existent `state-evidence/artifacts/` directory | `done` | The root cause is the review was based on an old directory snapshot; the current artifacts module has been restored to a real directory, the original issue is obsolete. |
| 129 | Multiple contract/review entries point to a non-existent `state-evidence/artifacts/` directory | `done` | The root cause is that some review/contract conclusions remain at the time when artifacts was missing, and did not follow up with later module restoration. |
| 130 | runtime-truth-repository.ts:741, projection-rebuild-service.ts:429, memory-gateway/index.ts:248, plan-builder.ts:193 use non-normalized `JSON.stringify` for fingerprinting, key order change → false diff | `done` | The root cause is that multiple planes each hand-wrote fingerprint logic, reusing regular `JSON.stringify` without converging to a stable serialization utility. |
| 131 | tests/integration/platform/state-evidence/memory/memory-layer-model-integration.test.ts:261 uses `Date.now()-90000` for aging assertion, clock drift causes jitter | `done` | The root cause is that time-sensitive tests have long relied directly on wall-clock differences, without fixing a baseline time. |
| 132 | tests/integration/platform/state-evidence/events/durable-event-bus.integration.test.ts and `durable-event-bus-integration.test.ts` inconsistent naming suspected double-run | `done` | The root cause is that the durable-event-bus integration test, when extended, used two sets of file naming conventions, so the "basic flow" and "replay ordering flow" boundaries are only reflected in file content, not in filenames. |
| 133 | package.json:223-234 `test:receipt-store/tool-gateway/memory-gateway/sandbox-provider` has no aggregator, only operator entry | `done` | The root cause is that several low-level entry tests were split into independent scripts without adding a CI/batch-verification aggregate command. |
| 134 | five-plane-state-evidence/index.ts:1 re-exports non-existent `./artifacts/index.js`, import throws immediately | `done` | The root cause is the review was based on the old snapshot when artifacts was missing; the current `artifacts/index.ts` exists and is publicly exported. |
| 135 | truth/sqlite/repositories/operations-repository.ts:898 `listRuntimeRecoveryRecords` directly splices the caller's `whereClause` into SQL, only filtering `;\\|--\\|/*` still allows `OR 1=1`/subqueries | `done` | The root cause is that the runtime recovery query, to reuse multiple filter scenarios, exposes a free SQL fragment interface, without converging to a controlled predicate allowlist. |
| 136 | truth/sqlite/repositories/event-repository.ts:788-828 `insertEvent` and outbox INSERT are two prepared statements with no unified transaction, breaking outbox atomicity | `done` | The root cause is that the Tier-1 state event path split event append and outbox append into two independent statements, missing the same transaction or savepoint boundary. |
| 137 | truth/sqlite/repositories/task-repository.ts:96-125 `listTasks` cursor only by `updated_at`, no id tiebreaker, pagination can lose rows/dead loop | `done` | The root cause is that the task list pagination cursor only kept the timestamp, without encoding the stable primary key into a composite cursor. |
| 138 | truth/sqlite/repositories/tenant-repository.ts:203-204 `listAll` uses `[...Map.values()].slice` with no stable sorting, results re-order across pages | `done` | The root cause is that the in-memory tenant repository directly slices `Map.values()`, with the default iteration order mistakenly treated as pagination order. |
| 139 | truth/sqlite/repositories/release-repository.ts:611,632,654 `listEnterprise*` only `limit=20`, no cursor/offset/tenant filter | `done` | The root cause is that the enterprise release reports were originally only landed as a "latest N" operations view, without simultaneously abstracting stable pagination and tenant-dimension constraints. |
| 140 | truth/sqlite/repositories/intelligence-repository.ts:350 `listIntelBriefs(limit=20)` no cursor silent truncation | `done` | The root cause is that the intelligence brief list has long only served the recent-brief panel, lacking stable cursor pagination. |
| 141 | truth/sqlite/repositories/organization-repository.ts:273 `listOrganizationRecords(limit=50)` no tenant filter, cross-tenant leak | `done` | The root cause is that the organization list defaults to a platform operations view, missing the tenant view of organization visibility constraints. |
| 142 | truth/sqlite/repositories/worker-repository.ts:63 and `worker-snapshot-repository.ts:276` `listCoordinatorInstanceSnapshots` dual implementation with schema drift | `done` | The root cause is the review was based on an old structure snapshot; the current `WorkerRepository` is fully delegated to `WorkerSnapshotRepository`, and the real dual implementation is closed. |
| 143 | state-evidence/dlq/index.ts:110-113 `enqueue` uses linear `listByConsumer` to deduplicate, O(n) per insert, no index | `done` | The root cause is that the base DLQ repository interface only exposes scan by consumer; the dedup key `sourceEventId+consumerId` has neither a separate query surface nor a persistence layer index. |
| 144 | state-evidence/dlq/index.ts:282-284 `runDueRetries` empty `catch {}` swallows errors without logger/telemetry/backoff | `done` | The root cause is that the DLQ retry worker only counts failures, without sending failure context to the structured log or telemetry surface. |
| 145 | state-evidence/dlq/index.ts:99 `maxRetries=5` hardcoded, conflicts with `dlq-service.ts` retry policy | `done` | The root cause is that the base DLQ and extended DLQ each maintain their own default retry constants, without abstracting into a single policy source. |
| 146 | state-evidence/dlq/index.ts:6-23 `DeadLetterRecord` and `contracts/types/domain/session-types.ts` `EventDeadLetterRecord` schema dual sources | `done` | The root cause is that the base DLQ record independently rewrites the event dead-letter field names and field types, without reusing the event dead-letter field definitions in the domain contract. |
| 147 | state-evidence/incident/index.ts:127-161 `listIncidents/listIncidentsPaginated` full `Map.values()` + sort + `findIndex`, cursor invalid under concurrent insertion | `done` | The root cause is that the incident pagination cursor originally only carried `incidentId`, without encoding the sort basis into the cursor. |
| 148 | state-evidence/incident/index.ts:35 `linkedEvidenceRefs: input.linkedEvidenceRefs ?? []` directly stores caller reference, external mutation pollutes internals | `done` | The root cause is that the incident open path directly reuses the caller's array reference, without doing a boundary copy. |
| 149 | state-evidence/incident/index.ts:117-121 `resolve()` accepts any current state, bypassing the `triaged→mitigating→reviewed→resolved` FSM | `done` | The root cause is that the incident FSM is missing a state guard on the resolve edge, only checking existence. |
| 150 | state-evidence/incident/index.ts:22 `nextIncidentOrder` monotonic ID publicly predictable and enumerable | `done` | The root cause is that the ordering tie-breaker depends on an incrementing sequence number, so the internal order key is both predictable and tightly coupled to the pagination cursor. |
| 151 | state-evidence/audit/index.ts:21,29 `AuditTrailService.records` in-memory array no rotation/persistence, long-running process must OOM | `done` | The root cause is that the audit trail service started as an in-process array, with no capacity limit or reclamation strategy. |
| 152 | projections/projection-rebuild-service.ts:265-266,278-294 `JSON.stringify` comparison with non-normalized key order; cutover has no optimistic concurrency token | `done` | The root cause is that the projection compare/cutover originally only served single-threaded local rebuild, without building stable serialization and cutover version checks into an explicit protocol. |
| 153 | checkpoints/checkpoint-envelope.ts:226 `Buffer.from(payload,"base64")` does not throw, malicious payload silently truncated then enters gunzip | `done` | The root cause is that the envelope unpack defaults to trusting Node's lenient base64 decode behavior, without first validating encoding integrity. |
| 154 | checkpoints/checkpoint-envelope.ts:147-149 `JSON.stringify` a large object first materializes fully then checks size, OOM before the guard | `done` | The root cause is that the checkpoint size guard is placed after serialization, with no JSON-volume pre-estimate fence. |
| 155 | checkpoints/checkpoint-gc-service.ts:548-560 `acquireRunLock` doesn't record PID/host, crash-residual lock is indistinguishable from a live lock | `done` | The root cause is that the checkpoint GC lock file only records `acquiredAt`, missing process/host identity metadata. |
| 156 | knowledge/keyword-index.ts:22-30 `upsert` doesn't clear the previous keywords' reverse index, leaving stale postings | `done` | The root cause is that the keyword index's `upsert` only has an add path, without first revoking the old inverted entries. |
| 157 | knowledge/keyword-index.ts:32-47 `query` rescans `countOccurrences` every time, no cache | `done` | The root cause is that the keyword hit score is fully calculated at query time, with no caching of chunk-keyword statistics. |
| 158 | knowledge/keyword-index.ts:1-53 missing `delete(chunkId)` API, chunks live forever | `done` | The root cause is that the keyword index only designed `upsert/reset` ends, without a single-deletion semantic. |
| 159 | memory-gateway/index.ts:248-258 `projectionHash` uses `JSON.stringify([...input.memoryIds])` preserving caller order, same set different order → different hash | `done` | The root cause is that the projection hash directly serializes the caller's array, without first deduplicating and sorting for normalization. |
| 160 | memory-gateway/index.ts:280-298 in-memory layer mapping L1/L2/L4/L6 round-trip is lossy, not asserted | `done` | The root cause is that the managed-layer to runtime-layer compression mapping doesn't preserve canonical layer metadata, and can only be degraded on readback. |
| 161 | memory-gateway/index.ts:328 `Number.isFinite(Number(metadata.version))` accepts 1e308, missing integer/range checks | `done` | The root cause is that memory version parsing only did "convertible to number" judgment, without integer and upper-bound constraints. |
| 162 | state-evidence/memory/trust-level-service.ts:245-248 MAX=500/TTL=24h/EVICT=60s hardcoded no config | `done` | The root cause is that the trust-level service originally started with process-default values, writing capacity, TTL, and eviction cycle hardcoded into the class. |
| 163 | state-evidence/memory/trust-level-service.ts:280-289 each eviction `[...entries].sort` O(n log n), including non-null assertion swallowing OOB | `done` | The root cause is that the over-capacity eviction directly goes through a full sort, which is both unnecessary and leaves holes to the non-null assertion. |
| 164 | state-evidence/memory/trust-level-service.ts:384-385 `includes("TODO"/"FIXME")` literal string filter, normal text false-positive | `done` | The root cause is that content quality checks treat TODO/FIXME as arbitrary substrings, without limiting them to explicit placeholder markers. |
| 165 | truth/sqlite/repositories/prompt-bundle-repository.ts:164-332 8 places `JSON.stringify(input.*)` column writes without zod validation | `done` | The root cause is that the prompt bundle repository treats the JSON column as a "store-then-directly-stringify" thin wrapper, without putting schema validation at the persistence boundary. |
| 166 | truth/sqlite/repositories/billing-repository.ts:168 `Number(result.changes)` BigInt > 2^53 silent truncation | `done` | The root cause is that SQLite `run().changes` is treated as a regular number, without a unified bigint safety boundary conversion. |
| 167 | truth/sqlite/repositories/worker-snapshot-repository.ts:249 same query switches ORDER BY by filter, cursor fails across filters | `done` | The root cause is the review remained on the old pagination assumption; the current repository does not expose a cursor protocol for this list, the real risk is that the sort semantics are not specified, not "cursor across filter". |
| 168 | state-evidence/events/event-ops-service.ts:216-221 `setTimeout(...)` reject timer not `unref`; `Promise.race` winner doesn't `clearTimeout` | `done` | The root cause is the review remained on the old implementation; the current timeout helper has both `unref()` and `clearTimeout()` in `finally`. |
| 169 | state-evidence/events/durable-event-bus.ts:9 different instantiation points `retentionLimit:500/100` inconsistent | `done` | The root cause is that the old review mixed retention configurations of different module loggers into the durable-event-bus self-issue; the current bus logger is independently closed. |
| 170 | tests/integration/platform/state-evidence/events/transactional-event-appender and `event-repository.ts:788-828` outbox split into two prepared calls, SQLite WAL autocommit observer sees partial state | `done` | The root cause is that the Tier-1 event special path bypassed the unified transactional appender, downgrading event/outbox atomicity back to dual-statement autocommit. |
| 171 | tests/integration/platform/state-evidence/checkpoints/checkpoint-envelope.ts:178 `createdAt:new Date().toISOString()` uses local clock, different TZ replay produces non-deterministic metadata | `done` | The root cause is the old test position referenced by the review is obsolete; the current checkpoint envelope test uses fixed timestamp samples, no longer depending on local clock. |
| 172 | src/platform/five-plane-state-evidence/truth/sqlite/sqlite-database.ts:138 `PRAGMA journal_mode=WAL` does not assert return value, in NFS environments silently falls back to delete | `done` | The root cause is that the SQLite initialization previously only "requested WAL" but did not "confirm WAL", leaving whether the actual journal mode degraded as a runtime occasional failure. |
| 173 | src/platform/five-plane-state-evidence/truth/sqlite/sqlite-database.ts:134 `busy_timeout` allows 0, transient SQLITE_BUSY with concurrent conflicts | `done` | The root cause is that the busy timeout config only does numeric truncation, without establishing a minimum positive integer constraint. |
| 174 | src/platform/five-plane-state-evidence/truth/sqlite/sqlite-database.ts:283 `Object.values(row)` depends on `wal_checkpoint` column order, missing key name destructuring | `done` | The root cause is that the WAL checkpoint result reading took the lazy approach of taking the object value array directly, without fixed binding to the SQLite return column name. |
| 175 | src/platform/five-plane-state-evidence/truth/sqlite/sqlite-database.ts:347-350 `close()` doesn't check `wal_checkpoint` busy>0, frames not flushed before close | `done` | The root cause is that `close` only does best-effort checkpoint, without treating busy or incomplete checkpoint as an explicit close failure. |
| 176 | src/platform/five-plane-state-evidence/truth/sqlite/sqlite-database.ts:442-449 uses regex `database is locked\\|busy` to identify BUSY, ineffective when localization/errno change | `done` | The root cause is that SQLite write contention identification has long relied on message text matching, without prioritizing sqlite/errno level error identifiers. |
| 177 | src/platform/five-plane-state-evidence/truth/sqlite/sqlite-database.ts:323-340 `healthCheck` is `async` but only sync transactions, misleading callers | `done` | The root cause is that the SQLite health probe copied the asynchronous backend interface signature, but internally has always gone through synchronous connections and synchronous transactions. |
| 178 | src/platform/five-plane-state-evidence/truth/sqlite/sqlite-database.ts:455-465 `applyCompatibleColumnMigrationIfKnown` skips migration.sql after hit, index/constraint changes silently lost | `done` | The root cause is that the compatibility migration path previously lacked explicit regression validation, easily making people think the "add column" branch doesn't complete the rest of DDL; a regression test for migration 11 has been added, confirming the compatibility branch still creates supplementary tables/indexes, no longer leaving such concerns in an evidence-less state. |
| 179 | src/platform/five-plane-state-evidence/knowledge/indexing/embedding-provider.ts:108,233 `fetch` has no AbortController/timeout | `done` | The root cause is the review was based on an old version; the current provider uniformly uses `fetchWithTimeout()`, which has been wired with AbortController and timeout cleanup. |
| 180 | src/platform/five-plane-state-evidence/knowledge/indexing/embedding-provider.ts:121-126,246-251 error body directly concatenated to Error message, potential log injection | `done` | The root cause is that the provider's error path directly concatenates the upstream response body, without doing newline/length convergence. |
| 181 | src/platform/five-plane-state-evidence/knowledge/indexing/embedding-provider.ts:137,259+ `response.json()` without try/catch | `done` | The root cause is that the embedding provider defaults to trusting the upstream JSON structure, without isolating parse failures. |
| 182 | src/platform/five-plane-state-evidence/knowledge/indexing/embedding-provider.ts:142-144 does not validate returned index range/duplicates, post-sort mapping assumes one-to-one correspondence | `done` | The root cause is that the sort recovery results default to trusting provider index completeness and uniqueness, without boundary checks. |
| 183 | src/platform/five-plane-state-evidence/checkpoints/checkpoint-envelope.ts:226 `Buffer.from(payload,"base64")` does not validate length/MIME, corrupted payload decoded as empty buffer without error | `done` | The root cause is that the checkpoint payload decoding only relies on Buffer's lenient behavior, without base64 length and character set constraints. |
| 184 | src/platform/five-plane-state-evidence/memory/trust-level-service.ts:384-385 uses `content.includes("TODO/FIXME")` as trust-level downgrading basis, obviously misfiring | `done` | The root cause is that quality checks treat TODO/FIXME as arbitrary substrings, not explicit placeholder markers. |
| 185 | src/platform/five-plane-state-evidence/truth/sqlite/sqlite-database.ts:323-330 `healthCheck` does CREATE/DROP TEMP TABLE inside transaction, rollback leaves TEMP handle residue | `done` | The root cause is that the health probe originally used a temp table write/delete to prove writability, with side-effect validation outweighing the connection-probe itself. |
| 186 | src/platform/five-plane-state-evidence/truth/sqlite/sqlite-database.ts:283-290 `checkpointWal` doesn't distinguish busy>0 from frames=0, ops cannot identify real bottleneck | `done` | The root cause is that the checkpoint return value was previously coarsely read by position array, with busy, log frames, and checkpointed frames not stably distinguished as separate signals. |
| 187 | tests/integration/platform/state-evidence/dlq-persistence.test.ts:464 `/tmp/dlq-persistence-test-${Date.now()}.db` not portable to Windows and not cleaned up in `finally` | `done` | The root cause is that the file-based persistence test was originally drafted quickly for the local `/tmp`, without reusing the test-layer unified temp workspace and cleanup constraints. |
| 188 | tests/unit/platform/state-evidence/knowledge/knowledge-store.test.ts:17 `/tmp/aa-sandbox/ktest_${suffix}_${Date.now()}` central pollution | `done` | The root cause is that the knowledge snapshot test hardcoded Unix temp directory constants in the helper, lacking a `tmpdir()`-based platform-independent concatenation. |
| 189 | tests/unit/platform/state-evidence/knowledge/p2-defects-sys-sec-4-2.test.ts:63,113 two `/tmp/aa-sandbox/...` not cleaned up | `done` | The root cause is that the security regression tests only focused on path allow/deny semantics, without bringing artifact lifecycle into test governance. |
| 190 | tests/leaks/platform/state-evidence/events/durable-event-bus.leak.test.ts threshold 10MB same logic and doesn't distinguish RSS/heapUsed | `done` | The root cause is that the leak test originally only focused on heapUsed, without distinguishing RSS from no-`--expose-gc` runtime environments. |
| 191 | dashboard-projection-service.ts:110 `system.health.changed` not registered to TypedEventType | `done` | The root cause is the review did not absorb the subsequent addition of typed-event-bus / event-registry; the current `system.health.changed` has been registered. |
| 192 | migrate-sqlite-to-pg.ts column/table names directly spliced into SQL, no whitelist (injection risk) | `done` | The root cause is that the migration script originally defaulted to having table and column names from a trusted schema; the current implementation now does allowlist for table names and identifier validation for column names before concatenation. |
| 193 | idempotency-key-storage.ts `${this.tableName}` directly spliced into SQL, not validated at construction | `done` | The root cause is that the idempotency-key storage previously allowed unconstrained custom table names; it is now mandatory at the construction boundary to validate safe SQL identifiers and fail-close with structured errors. |
| 194 | semantic-vector-store.ts `process.env[name]` with name from config, can read any env | `done` | The root cause is that this entry is based on an old implementation snapshot; the current `semantic-vector-store.ts` only reads the fixed `AA_KNOWLEDGE_VECTOR_BACKEND` / `AA_KNOWLEDGE_SEMANTIC_BACKEND`, with no config-driven arbitrary env reads. |
| 195 | checkpoint-gc-service.ts `fs.stat→fs.unlink` TOCTOU window | `done` | The root cause is that the GC delete path first checks existence then deletes; it now uses `lstat/open(O_NOFOLLOW)/fstat/unlink` to bind object identity, and is idempotent on `ENOENT`. |
| 196 | shadow-snapshot-service.ts lstat→rename symlink swap window | `done` | The root cause is that shadow snapshot metadata previously used a temp file `rename()` to overwrite the target, with `lstat` separated from the final landing point; it now uses `O_EXCL|O_NOFOLLOW` to directly exclusively create the final file and reject duplicate snapshotId, removing this elevation window. |
| 197 | sqlite-database-wrapper.ts:94-114 savepoint name directly spliced into `exec`, future callers can inject | `done` | The root cause is that the PG-compatible wrapper previously inserted savepoint names directly into SQL; it now constrains savepoint names to a constrained generator and quotes by identifier. |
| 198 | sqlite-database.ts:143 `PRAGMA busy_timeout = ${this.busyTimeoutMs}` SQL concatenation, `busyTimeoutMs` not validated as integer | `done` | The root cause is that although the PRAGMA value comes from the configuration layer, it is not re-validated as a positive integer at the database boundary, leaving a concatenation-based config-injection surface. |
| 199 | pg-advisory-lock-adapter.ts `Number(result.fencing_token)`, sqlite-lock-adapter.ts:36 `Number(result.lastInsertRowid)` exceed 2^53 precision loss | `done` | The root cause is that the two risk points corresponding to this entry have disappeared: the PG adapter now does a safe-integer range check, and the SQLite lock adapter no longer relies on `lastInsertRowid` to generate the fencing token. |
| 200 | checkpoint-gc-service.ts:171,557, learning-object-model.ts:180,184, risk-register.ts:87,110, invariant-registry.ts:137,165,180, responsibility-boundary.ts:158-308, admin-config-service.ts:66, outbox-repository.ts:117, memory-layer-model.ts:214,549, graphql-adapter-service.ts:294, conversation-template-service.ts:408, approval-policy-engine/version-manager.ts:111, stable-evidence-bundle-support.ts:612,616,732, dlq-service.ts:238, knowledge-snapshot-store.ts:25-48, semantic-vector-validation.ts:276, tool-gateway/index.ts:150,160, idempotency-key-storage.ts:310,338,341, cors.ts:49-68, reliability/timeout.ts:45,54 multiple places throwing raw `Error` rather than structured AppError/ValidationError | `done` | The root cause is that platform submodules have long each directly thrown the native `Error`; this batch has converged the still-hit existing paths to `ValidationError` / `StorageError` / typed error, and the dead paths no longer correspond to current code. |
| 201 | `.gitignore` global `*.db-shm/*.db-wal` doesn't exist, sqlite WAL residue can be committed | `done` | The root cause is the old review was based on an obsolete `.gitignore` snapshot; the current repository already explicitly ignores `*.db-shm` and `*.db-wal`. |

## src/platform/shared

| ID | Issue | Status | Root Cause |
| --- | --- | --- | --- |
| 202 | `src/platform/stability/` and `src/platform/shared/stability/` parallel same-name directories have diverged in implementation | `done` | The root cause is that the stability capability once simultaneously preserved the authoritative implementation and the historical copy facade; the reliability sub-capability has now been uniformly reclaimed as a thin re-export of the top-level stability. |
| 203 | `src/platform/shared/reliability/`, `shared/stability/reliability/`, `stability/reliability/` three reliability implementations duplicate | `done` | The root cause is that after the reliability directory was reorganized, the old implementation was not fully retired; the current repository has only one implementation, with `shared/stability` as a facade. |
| 204 | `src/platform/shared/observability/structured-logger.ts:484-491` per-entry fsync log `openSync+appendFileSync+fsyncSync+closeSync` serial synchronous IO | `done` | The root cause is that the durable sink previously reopened the file for each log entry; it now reuses the persistent file descriptor, only closing and reopening on rotation. |
| 205 | `src/platform/shared/observability/structured-logger.ts:153,180` `sinkBaseDir=process.cwd()`, runtime semantics drift after chdir | `done` | The root cause is that the sink base directory was previously directly bound to `process.cwd()`; it is now fixed to a stable absolute base directory resolved at module initialization, with an explicit override entry preserved. |
| 206 | `src/platform/shared/observability/structured-logger.ts:194` `mkdirSync` no error handling, throws directly on insufficient permissions at configure | `done` | The root cause is that the logger directory creation previously leaked file system exceptions to the caller; it now catches and downgrades to a structured internal error, and disables that sink. |
| 207 | `src/platform/shared/observability/structured-logger.ts:262` `retentionLimit=0` makes buffer length 0, all logs silently dropped | `done` | The root cause is that this entry is based on a misjudgment; the current implementation with `retentionLimit=0` only disables memory retention, not blocking file sink and transport output. |
| 208 | `src/platform/shared/outbox/outbox-poller-service.ts:193-197` `retryCount>=maxRetries` only `failed++;continue`, never sent to DLQ | `done` | The root cause is that the outbox poller previously had no terminal-failure semantic; it now writes an explicit dead-letter marker for over-limit records and removes them from the pending set. |
| 209 | `src/platform/shared/outbox/outbox-poller-service.ts:188-217` `for-await` serial processing, no concurrent batch publishing | `done` | The root cause is that the outbox publishing loop was originally implemented in order; it now does concurrent chunk publishing by configurable chunk size, preferring batch publish. |
| 210 | `src/platform/shared/observability/otel-tracer.ts` & `otel-bootstrap.ts` each have `loadOtelApi/loadOtelModules`, two OTel loading paths | `done` | The root cause is that the OTel module detection logic was previously scattered between tracer and bootstrap; it has now been extracted to the shared `otel-module-loader.ts` single entry. |
| 211 | `src/platform/shared/observability/structured-logger.ts:153` `sinkBaseDir=process.cwd()` after multi-worker fork each holds own cwd, paths inconsistent | `done` | The root cause is that the log sink previously derived the path from each worker's own `cwd`; it is now uniformly anchored to the absolute base directory resolved at startup. |
| 212 | tests/unit/platform/shared/stability/stable-prompt-injection-red-team-additional.test.ts:82,97,111,129,145 5 places of `/tmp/...` not portable | `done` | The root cause is that the stability additional tests directly hand-wrote Unix temp directory strings, without reusing a unified test workspace helper. |
| 213 | tests/unit/platform/shared/stability/stable-runtime-validator-additional.test.ts:30 `/tmp/${caseId}.backup.db` cross-case name collision overwrites each other | `done` | The root cause is that the baseline/backup path used `caseId` directly spliced to the shared `/tmp`, lacking a platform-independent and isolation-prefixed temp path generation. |
| 214 | graceful-shutdown.ts `setImmediate(()=>process.exit())` does not flush stdio | `done` | The root cause is that the old implementation indeed only scheduled to the next event loop and exited; the current path explicitly waits for stdout/stderr flush before exiting. |
| 215 | slo-alerting-channels.ts does synchronous blocking IO inside `queueMicrotask` | `done` | The root cause is this review conclusion remained on the old implementation snapshot; the current `slo-alerting-channels.ts` has no `queueMicrotask`-wrapped synchronous blocking IO path. |
| 216 | graceful-shutdown.ts:122 `void this.handleSignal(signal)` no `.catch`; shutdown errors become unhandled rejection | `done` | The root cause is that the signal listener started async shutdown as fire-and-forget, without consuming rejection at the listener boundary. |

| 1161 | src/plugins/builtin-plugin-registry.ts normalizeManifest() only does @platform/→@automatic-agent/ string replacement, not handling nested schema/fields; other legacy naming (e.g. @aa-platform/) is not covered. EN: incomplete legacy-namespace migration. | `done` | The root cause is that manifest normalization previously only covered `@platform/`; this round has unified `@aa-platform/` as well. |
| 1162 | src/plugins/builtin-plugin-registry.ts outputDataClass field defined but all builtin manifests do not fill it, Set/Get path unused. EN: dead manifest field. | `done` | The root cause is that manifest layer did not actually persist output data classification; this round has made `outputDataClass` a schema-internal field and provided default/explicit values for builtin manifests. |
| 1163 | src/plugins/builtin-plugin-registry.ts globalMarketplaceRegistry / pluginRevocations / BundleRevocationRegistry three module-level singletons, resetBuiltinPluginRegistryStateForTests only resets one, unit tests pollute each other. EN: global singletons not all reset by test hook. | `done` | The root cause is that the reset hook previously did not cover all module-level singletons; this round has completed the cleanup. |
| 1164 | src/plugins/builtin-plugin-registry.ts allowedExternalDomains: [] and allowNetworkEgress: true appearing together, the combined semantic "allow all domains" or "no allow" is not normalized. EN: ambiguous network-egress contract. | `done` | The root cause is that external adapter manifests once had the ambiguous configuration of "open network egress without a domain allowlist"; this round has added the allowlist and disambiguated at the normalize stage. |
| 1165 | src/plugins/adapters/crm-adapter.ts:~30 default baseUrl=api.hubspot.com has nothing to do with crmType, when Salesforce config is missing baseUrl it still points to HubSpot. EN: default base URL ignores crmType discriminator. | `done` | The root cause is that the default base URL is bound to HubSpot; this round has split the default address by CRM type. |
| 1166 | src/plugins/adapters/crm-adapter.ts path hardcoded as /crm/v3/objects/, Salesforce REST path is /services/data/vXX.X/sobjects/, fundamentally not available. EN: HubSpot-specific path applied universally. | `done` | The root cause is that the path template reused the HubSpot implementation; this round has split into platform-specific path generation. |
| 1167 | src/plugins/adapters/crm-adapter.ts:136,143 crmRequest(action,…) uses the raw action rather than normalizedAction for URL/handler selection, alias invalid. EN: action alias resolution dropped before dispatch. | `done` | The root cause is that alias resolution did not thread through to request execution; this round has uniformly used `normalizedAction`. |
| 1168 | src/plugins/adapters/crm-adapter.ts ACTION_ALIASES table globally shared rather than grouped by crmType, HubSpot's alias also works on Salesforce, polluting semantics. EN: aliases are not per-CRM. | `done` | The root cause is that the alias table was previously not isolated by CRM dialect; this round has changed to per-CRM configuration. |
| 1169 | src/plugins/adapters/crm-adapter.ts fetch(...) call has no AbortSignal/timeout, no response size limit; malicious/slow backend can hang worker. EN: missing fetch timeout & response size cap. | `done` | The root cause is that the CRM request execution layer lacked timeout and response-size constraints; this round has added them. |
| 1170 | src/plugins/adapters/credential-hygiene.ts bytes.toString("utf8") writes secret into non-zeroizable JS string, breaking subsequent zeroize commitment. EN: plaintext copied into immutable string defeats zeroize. | `done` | The root cause is that the credential helper previously provided a long-lived `reveal()`; this round has changed to callback-style short-lived exposure. |
| 1171 | src/plugins/adapters/credential-hygiene.ts fingerprint truncated to 12 hex chars (~48 bit), with many credentials in the same tenant birthday-collision probability is non-negligible. EN: fingerprint truncation collision risk. | `done` | The root cause is that the default fingerprint bit count is too short; this round has expanded to 24 hex. |
| 1172 | src/plugins/adapters/livestream-adapter.ts healthCheck() returns unhealthy forever when credentialFingerprint===null; initialization order does not guarantee fingerprint is ready first. EN: health check unreachable until external init. | `done` | The root cause is that the health check coupled authentication state with endpoint health; this round has decoupled them. |
| 1173 | src/plugins/index.ts top-level `export *` exposes all internal classes of builtin-plugin-registry (such as BundleRevocationRegistry), breaking encapsulation. EN: barrel leaks internal classes. | `done` | The root cause is that the root plugins barrel lacked a controlled export boundary; this round has changed to an explicit public surface and added anti-leak tests. |
| 1174 | plugin-runtime-child.ts globally overwrites `console.*` polluting the main process | `done` | The root cause is that runtime child previously permanently overwrote global `console.*` during bootstrap; this round has changed to only temporarily redirect during direct stdio request execution and restore in `finally`. |
