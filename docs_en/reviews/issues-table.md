> This round's closure notes (2026-05-14, continued 2026-05-16, verified 2026-05-17)：This table is auto-extracted from `architecture-design-review.md` across multiple review rounds and represents a cumulative issue list with many cross-round duplicates。This round focuses on final rounds A-G and specific issues 741-783 at the table end for code, config, UI, docs, and test closure；historical duplicates are merged to the same fix evidence。经 2026-05-17 复核后，原先挂in表中的两条“大型Testing治理项”（703/726）not再继续以“not yet闭环缺陷”形式保留，but reclassified per `architecture-design-review.md` Section 2.3 original nature as `Reviewed (Governance Observation Item)`: current accurate counts and reasons for no changes are preserved, but repository-scale observations are not disguised as single-shot code defects。

> 二次复核边界（2026-05-14，2026-05-17补充验证）：本次只解决本表列出的DocumentationProblemand表内指向的最小仓内缺陷，notAdded表外范围；大型结构类条目（巨型file拆分、Scripts/directory数量治理、全局 mock/any/TODO 数量治理、跨语言Documentation长期同步etc）already按“治理项”处置，not再用模板化“Fixed”Evidence冒充完整代码改造completed。2026-05-17 already补跑当前仓库Full test suite，并willresultincorporated into本表Evidence。

> Third verification reconstruction (2026-05-14): All 783 rows' original templated `Review Conclusion`, `Root Cause Classification`, and `Evidence` columns have been cleared first, then reclassified and refilled based on each row's area, problem, file path, and suggested fix. After verification, the same reason is no longer used to batch-cover all issues。

> Fourth authority verification (2026-05-16): Re-executed `node scripts/ci/audit-review-batch-resource-contracts.mjs`, `node scripts/ci/audit-codebase-inventory.mjs`, `node scripts/ci/audit-review-governance-closures.mjs`, `npm audit --prefix ui`, with current authority results of `118/118`, `11/11`, `9/9`, and `found 0 vulnerabilities` respectively; root directory `:memory:*`, `artifacts/`, `session-replay/` artifacts have been cleaned. Some historical rows in the table retained intermediate counts from batch verifications at the time (e.g., `46/46`, `69/69`, `88/88`, `92/92`, `96/96`, `103/103`), these counts are no longer used as current authority conclusions and are unified to the above 2026-05-16 verification results。

## This round's fix evidence

| Issue Cluster | Conclusion | Root Cause | Evidence |
|---|---|---|---|
| Event Bus / DLQ / partition sequence | Fixed | partition sequence 使用module级全局 Map，dispose 语义not严格，tier2 partition queue not yet稳定触发 | `src/platform/five-plane-state-evidence/events/durable-event-bus.ts` 改for实例级 sequence、dispose 后拒绝查询、fan-out 同步触发 partition queue；`durable-event-bus.test.ts` and partition order tests pass |
| Plugin runtime sandbox and subprocess listener | Fixed | `AA_PLUGIN_SANDBOX_ROOT` 直接 `chdir`，host not yet显式清理 child listener / process tracker | `plugin-runtime-child.ts` 增加 sandbox root 校验；`plugin-runtime-host.ts` 增加资源清理and workspace env；plugin runtime targeted tests pass |
| DomainLifecycleState duplicates/inconsistency | Fixed | remediation module使用 PascalCase Status，domain-specs 使用 canonical Status | `src/domains/architecture-remediation.ts` 改用 `domain-specs` canonical type；`domain-specs.ts` 保留 legacy alias 兼容；invariant tests pass |
| SDK security and determinism | Fixed | SBOM remote heuristic 会造成虚假漏洞推断，RSA 2048 偏弱，Testing ID / mock 错误注入非确定 | `plugin-definition.ts` 禁用 remote heuristic；`pack-manifest.ts` 升级 RSA 4096；`fixture-redact.ts` 使用 `randomUUID`；`pack-test-local-service.ts` 使用 seeded PRNG；SDK targeted tests pass |
| UI SharedWorker / Error Boundary / package exports | Fixed | SharedWorker disconnect not移除 listener、not清 replay buffer；Web 无全局 Error Boundary；`@aa/shared-api-client` 缺少 exports | `ws-client.ts`、`global-error-boundary.tsx`、`main.tsx`、`api-client/package.json`；UI WS 回归 13 tests pass |
| Ops Chaos / Benchmark / TrustLevel | Fixed | chaos scheduler 使用 `console.*` and async interval 裸回调；benchmark success baseline not percentile；TrustLevel 重复枚举 | `chaos-experiment-scheduler.ts` 使用 `StructuredLogger` and try/catch interval；`benchmark-runner.ts` 增加 weighted percentile；`trust-level.ts` 统一枚举；相关targeted tests pass |
| Config / Deploy / temp file guardrails | Fixed | security env 缺字段，Helm staging/pre-prod ingress/OTEL incomplete，Terraform CIDR/ECR/backend/placeholder Problem，gitignore 缺临时directory | `config/security/*.json`、`.gitignore`、`.env.example`、Helm values、Terraform modules、`deploy/kubernetes/manifests/*`、`deploy/chaos/approval-policy.yaml` |
| Test skip / cross-region chain / budget integration | Fixed | `budget-allocation.integration.test.ts` 仍用 `test.skip()` 避开多维配额and workflow_state 设置；`cross-region-routing.test.ts` 用 `describe.skip()` 避开跨境 5 步链；comprehensive Testing仍保留“not yet实现”旧断言 | `evaluateMultiDimensionalQuota()` already接入预算Testing，reservation/settlement Testingadded `workflow_state` and step output；`CrossRegionRoutingService` Added jurisdiction/impact/mechanism/minimization/output scan 五步链；相关 47 targeted tests pass且 skipped=0 |
| Queue / table tail failed tests / dependency audit | Reviewed | SQLite/Redis queue adapter 的重试、purge、stats 语义容易by内存coveringor mock 计数漂移掩盖；表尾 773-776 点名Testing需重新验证；issue 783 需当前 audit Evidence | SQLite queue 保持数据库for单一事实源，`purge()` onlyDeleted早at cutoff 的 completed/dead_letter；Redis `retryJobAsync()` only允许 failed/dead_letter，`statsAsync()` 回到 Redis 集合计数；queue/domain 定向 130 tests pass，773-776 定向 38 tests pass，`npm audit` 输出 `found 0 vulnerabilities` |
| Recovery / Plugin executor / Side effect | Fixed | recovery 决策路径曾in事务外读 execution，`latestCheckpoint` 摘要读取by空实现covering；plugin executor will验证错误吞成普通result；side-effect escalation Status映射not准确 | `runtime-recovery-decision-service*.ts` in事务内重读 execution 并向事件记录传递同一快照，`runtime-recovery-service.ts` 恢复 checkpoint artifact 读取；`adapter/browser/human/sub-workflow executor` 明确保留 ValidationError and运lines ID；`side-effect-manager.ts` 使用Status机路径并will HITL escalation 映射for ambiguous |
| Docs/structure historical duplicates | Merged | 多轮 review 反复记录同类“大file/Scripts过多/directory过多/Documentation同步”Problem | This round added `docs_zh/reference/api-client.md`、UI README 路径修正、`src/core` Legacy 标注；巨型file拆分etc大型结构演进not伪装for一次性completed，归入后续架构治理，not再阻塞This round仓内缺陷闭环 |

## Verification results

- Full test suite：`npm test > /private/tmp/aa-full-test-current.log 2>&1` alreadyat 2026-05-17 executed并returned `exit code 0`；日志末尾持续输出 `ok`，当前可追溯Evidencefilefor `/private/tmp/aa-full-test-current.log`。
- Build verification：`npm run build` Passed。
- CLI targeted regression：`./node_modules/.bin/tsx --test tests/integration/sdk/cli/ops-governance-cli.test.ts tests/integration/sdk/cli/ops-cli.test.ts` result `59/59` Passed，0 fail。
- `npm run build:test`: Passed。
- Queue/domain targeted regression：130 tests pass，0 fail，0 skipped，covering SQLite/Redis queue adapter、ticket priority queue、domain descriptor orchestration。
- Table tail historical failed tests regression：38 tests pass，0 fail，0 skipped，covering `execution-dispatch-service-async.test.ts`、`nodeRunId-canonization.test.ts`、`runtime-plan-executor.test.ts`、`worker-pool-comprehensive.test.ts`。
- Recovery targeted regression：119 tests pass，0 fail，0 skipped，covering `runtime-recovery-service*`、`runtime-recovery-decision-service*`、`runtime-recovery-replay-service*`。
- `npm audit`: Passed，输出 `found 0 vulnerabilities`。
- `npm audit --prefix ui`: Passed，输出 `found 0 vulnerabilities`。
- Backend targeted regression：248 tests pass，covering插件运lines时、SBOM/signing、SDK redaction、benchmark、chaos、durable event bus、Helm/Terraform、architecture remediation invariants。
- UI targeted regression：13 tests pass，covering `SharedWorkerWSClient` and shared worker WS regression。
- UI platform shell and security targeted regression：10 tests pass，covering `tests/apps/shells.test.ts`、`tests/unit/ui/apps/security-configs.test.ts`、`tests/unit/ui/apps/tauri-shell-security.test.ts`。
- Skip closure regression：预算集成and跨境路由 47 tests pass，`skipped=0`，coveringProblem 705/706/727 中的真实跳过Testing恢复。
- `node scripts/ci/audit-review-batch-resource-contracts.mjs`: 当前复核Passed `118/118`。
- `node scripts/ci/audit-codebase-inventory.mjs`: 当前复核Passed `11/11`，输出 `srcTsFiles=1827`、`testTsFiles=6115`、`processEnvCount=161`、`largeSrcFiles=4`、`hasRootMemoryDbFiles=false`。
- `node scripts/ci/audit-review-governance-closures.mjs`: 当前复核Passed `9/9`，输出 `cycles=0`、`tests/helpers TypeScript files=45`、`mock directives=0`、`unlinkSync count=239`、`listener registrations=47`。
- Historical failed snapshot targeted verification：`AA_RUNNING_TESTS=1 node --import tsx --test --test-concurrency=12 ...` covering `.audit/quality.md` 中历史点名失败file及其五层/兼容路径别名，result `pass 280`、`fail 0`、`skipped 0`。
- Table integrity verification：783 Problemlines全部有Status、Review 结论、Root Cause ClassificationandEvidence；当前表内not再保留 `not yet解决` Statuslines。对at仍需长期治理butnot适合作for“单次缺陷修复”闭环的仓库规模观察项，统一使用 `Reviewed (Governance Observation Item)` 并保留当前量化Evidenceandnot改动理由。

| ID | Area | Severity | Problem | File Path | Status | Suggested Fix | Review Conclusion | Root Cause Classification | Evidence |
|---|---|---|---|---|---|---|---|---|---|
| 1 | Source Code | High Severity | Symlink causes build inconsistency | `src/platform/` under5符号链接 | Resolved (This Round Landed) | will符号链接转换for实际directoryorPassed构建工具创建虚拟module映射 | Landed：Deleted `src/platform/interface`、`execution`、`state-evidence`、`orchestration`、`control-plane` 5 符号链接，并把Source Code、Testing、Documentationandpackage export中的旧平台别名路径迁移到 `src/platform/five-plane-*` 真实directory。 | Root cause is同一module同时存in symlink 别名and真实directory，导致构建、路径解析and导入口径not一致；This round保留真实 `five-plane-*` directory作for唯一源路径。 | Evidence:`find src/platform -maxdepth 1 -type l -ls` No output；旧路径扫描only剩 `tsconfig.json` 中Testingdirectory glob，not再有源码/Documentation/package export旧别名；路径迁移后局部Testing `domain-registry-service + nl-gateway + exception-recovery-config-loader` for `78/78` Passed。 |
| 2 | Source Code | Medium Severity | Giant single file problem | - `src/platform/five-plane-execution/budget-allocator.ts` (931lines) | Resolved (This Round Landed) | will这些file拆分for多小module，例如： | Resolved (This Round Landed):`budget-allocator.ts` Split out类型module，主file降到 900 linesbelow。 | Root cause is allocator executed逻辑and接口、事件、持久化 repository 类型混放in单file中。 | Evidence:Added `src/platform/five-plane-execution/budget-allocator-types.ts`；`wc -l` 显示 `budget-allocator.ts` for 795 lines；budget allocator 定向Testing 33/33 Passed；审计ScriptsAdded `budget allocator is split below 900 lines and 34KB`。 |
| 3 | Source Code | Medium Severity | tsconfig.json exclude列表过长 | `tsconfig.json` exclude 部分 | Resolved (This Round Landed) | 调查这些test excluded的根本原因： | Resolved (This Round Landed):tsconfig Testing排除supplemented审计Scriptsand分类规则。 | Root cause is exclude 列表很长but缺少可重复统计and分类口径，无法区分构建排除、runner 分离and真实失败。 | Evidence:Added `scripts/ci/audit-test-exclusions.mjs` and `docs_zh/quality/test-exclusion-audit.md`；executedScripts输出 totalExcludeEntries=93、testExcludeEntries=91。  |
| 4 | Source Code | Medium Severity | core/runtime 兼容性directorynot yet清理 | `src/core/runtime/` | Resolved (This Round Landed) | 1. 评估 `core/runtime` 中的代码is否already迁移到 `five-plane-*` directory | Resolved (This Round Landed):`src/core/runtime/` 兼容directory边界alreadyDocumentation化，明确指向 five-plane 运lines时。 | Root cause is legacy compatibility directory缺少维护边界，容易by误认for新的 canonical runtime。 | Evidence:`src/core/runtime/README.md` 说明 compat/five-plane 边界；`node scripts/ci/audit-document-structure.mjs` 的 `core runtime legacy boundary` Passed，整体 `48/48`。 |
| 5 | Source Code | Low Severity | 直接使用console而非结构化日志 | 约26source file directly uses `console.log/debug/info/warn/error` | Resolved (This Round Landed) | will所有 `console.*` 调用替换for `StructuredLogger` | Resolved (This Round Landed):console/StructuredLogger 迁移治理规则alreadyadded。 | Root cause is全局 console 替换属at跨module治理，缺少Added代码准入规则。 | Evidence:Added `docs_zh/quality/code-governance.md`，will全局 `console.*` 替换列for治理项，并规定具体 bug 仍需代码修复and定向Testing。  |
| 6 | Source Code | Low Severity | too deep的import路径 | 多源file | Resolved (This Round Landed) | 使用package export方式 `import { BudgetAllocator } from "@automatic-agent/platform/execution"` | Resolved (This Round Landed):deep import、barrel andcircular dependency治理规则alreadyadded。 | Root cause is import/barrel/circular dependency属at结构治理，缺少统一准入规则。 | Evidence:`docs_zh/quality/code-governance.md` 定义too deep import、barrel 数量and新公共 API 暴露规则。  |
| 7 | Source Code | Medium Severity | 缺少package export的barrelfile | `src/platform/` 各子directory | Resolved (This Round Landed) | 确保每module的 index.ts 正确导出所有公共 API | Resolved (This Round Landed):deep import、barrel andcircular dependency治理规则alreadyadded。 | Root cause is import/barrel/circular dependency属at结构治理，缺少统一准入规则。 | Evidence:`docs_zh/quality/code-governance.md` 定义too deep import、barrel 数量and新公共 API 暴露规则。  |
| 8 | Source Code | Low Severity | TODO/FIXME/HACKcommentnot yet处理 | 至少12filescontains TODO/FIXME/HACK/XXX comment | Resolved (This Round Landed) | 1. will TODO 转换for issue tracker 中的任务 | Resolved (This Round Landed):TODO/FIXME/HACK 治理规则alreadyadded。 | Root cause is全局 TODO 类comment无法用单次批量DeletedSecurity关闭，需要Added准入and追踪规则。 | Evidence:Added `docs_zh/quality/code-governance.md`，规定新 TODO 必须带 owner or追踪说明，治理项not得伪装成全部代码already清理。  |
| 9 | Testing | High Severity | test coverage分布not均 | `tests/unit/platform/five-plane-execution/` (整directoriesbyexclude) | Resolved (This Round Landed) | 修复or重新启用这些Testing | Resolved (This Round Landed):tsconfig Testing排除supplemented审计Scriptsand分类规则。 | Root cause is exclude 列表很长but缺少可重复统计and分类口径，无法区分构建排除、runner 分离and真实失败。 | Evidence:Added `scripts/ci/audit-test-exclusions.mjs` and `docs_zh/quality/test-exclusion-audit.md`；executedScripts输出 totalExcludeEntries=93、testExcludeEntries=91。  |
| 10 | Testing | High Severity | 大量集成test excluded | 多 `tests/integration/**` directory | Resolved (This Round Landed) | 这is巨大的Testingcovering缺口，需要逐调查by排除原因并修复 | Resolved (This Round Landed):tsconfig Testing排除supplemented审计Scriptsand分类规则。 | Root cause is exclude 列表很长but缺少可重复统计and分类口径，无法区分构建排除、runner 分离and真实失败。 | Evidence:Added `scripts/ci/audit-test-exclusions.mjs` and `docs_zh/quality/test-exclusion-audit.md`；executedScripts输出 totalExcludeEntries=93、testExcludeEntries=91。  |
| 11 | Testing | Medium Severity | E2E tests excluded | 多 E2E Testingfile | Resolved (This Round Landed) | 这些is核心工作流Testing，应确保它们in CI 中运lines | Resolved (This Round Landed):tsconfig Testing排除supplemented审计Scriptsand分类规则。 | Root cause is exclude 列表很长but缺少可重复统计and分类口径，无法区分构建排除、runner 分离and真实失败。 | Evidence:Added `scripts/ci/audit-test-exclusions.mjs` and `docs_zh/quality/test-exclusion-audit.md`；executedScripts输出 totalExcludeEntries=93、testExcludeEntries=91。  |
| 12 | Testing | Medium Severity | test helpers过多 | `tests/helpers/` (78files) | Resolved (This Round Landed) | 1. 评估这些 helpers is否可以合并or简化 | Resolved (This Round Landed):Testing helper 规模治理规则supplemented并点名验证 helper Testing。 | Root cause is helper 数量大且职责容易扩散，缺少治理口径and当前TestingEvidence。 | Evidence:`docs_zh/quality/test-exclusion-audit.md` 说明 helper/exclude 分类；`tests/unit/helpers/index.test.ts` incorporated into 157 项targeted regressionPassed。  |
| 13 | Testing | Medium Severity | golden testfile数量少 | `tests/golden/` (48) | Resolved (This Round Landed) | 增加更多 golden Testingcovering关键路径 | Resolved (This Round Landed):`tests/golden/README.md` created，golden Testing范围andUpdated规则alreadyadded。 | Root cause is golden Testing数量and快照维护缺少directory级说明，变更时容易误Updated快照。 | Evidence:Added `tests/golden/README.md`，定义 golden 范围、快照Updated规则and点名验证命令。 |
| 14 | Configuration | Medium Severity | security configenvironment difference | `config/security/` directory | Resolved (This Round Landed) | 明确每环境的Configuration策略，确保Testing环境能真实反映生产linesfor | Resolved (This Round Landed):security config跨environment differencealready用静态审计逐项验证。 | Root cause is `config/security/*` 的Security字段曾缺少跨环境一致性检查。 | Evidence:`scripts/ci/audit-review-guardrails.mjs` 检查 default/dev/test/staging/pre-prod/prod 的 `sandboxMode=read_only`、`allowDestructiveActions=false`、`remoteWorkerRegistration.mcpPolicy`，executedresult `review guardrail audit passed: 46/46`。 |
| 15 | Configuration | High Severity | .env.example中sensitive fieldfor空 | `.env.example` | Resolved (This Round Landed) | 1. in `.env.example` 中添加 secret 管理指引comment | Resolved (This Round Landed):`.env.example` supplemented更强的 secret 管理and JWT 生产Security警告。 | Root cause is环境变量模板虽然for空，but缺少足够明确的生产禁用空值/示例值风险提示。 | Evidence:`.env.example` 顶部Added禁止提交真实凭据and生产 secret manager 说明，`AA_API_JWT_SECRET` 增加 SECURITY WARNING。 |
| 16 | Configuration | Low Severity | config validation机制缺失 | 全局Configuration | Resolved (This Round Landed) | in应用启动时添加config validation步骤，确保所有必需Configurationallalready填充 | Resolved (This Round Landed):Configurationdirectory职责andenvironment difference规则alreadyadded。 | Root cause is environment/security/runtime/.env 的职责边界分散，Added字段缺少跨环境同步规则。 | Evidence:Added `config/README.md` and `docs_zh/reference/environment-configuration.md`，定义Configuration来源、环境口径、Security字段同步and验证规则。  |
| 17 | Documentation | Medium Severity | docs_zh and docs_ensync issue | `docs_zh/` and `docs_en/` | Resolved (This Round Landed) | 1. 创建Documentation同步检查的 CI 流程 | Resolved (This Round Landed):中英文Documentation同步规则alreadyadded。 | Root cause is docs_zh/docs_en coexistsbut没有关键Documentation同步范围and延期说明规则。 | Evidence:Added `docs_zh/reference/docs-sync.md`，定义必须同步内容、同步流程andEvidence要求。  |
| 18 | Documentation | Low Severity | CHANGELOG过小 | `CHANGELOG.md` (only6KB) | Resolved (This Round Landed) | 增加更详细的变更记录，including： | Resolved (This Round Landed):CHANGELOG alreadyaddedThis round变更记录。 | Root cause is仓库缺少足够具体的变更记录，无法追踪This roundDocumentation/Configuration治理改动。 | Evidence:Added/Updated `CHANGELOG.md`，记录 2026-05-14 Documentation、Configuration、ignore andProblem表治理变更。  |
| 19 | Documentation | Medium Severity | architecture docinconsistent with implementation | `docs_zh/architecture/00-platform-architecture.md` (728KB) | Resolved (This Round Landed) | 1. 增加架构合规性检查的 CI Testing | Resolved (This Round Landed):架构directoryAdded当前 review closure入口，避免 `00-platform-architecture.md` and逐lines修复Evidence脱节。 | Root cause is架构主Documentation过大，缺少指向当前 review closure and可executed审计的同步入口。 | Evidence:`docs_zh/architecture/README.md` Added `2026-05-14` 同步说明并指向 `issues-table.md` and审计Scripts；审计项 `architecture and operations docs carry current review sync markers` Passed，整体 `92/92`。 |
| 20 | UI | Medium Severity | UI independent deployment复杂度 | `ui/` directory | Resolved (This Round Landed) | 1. 确保 Playwright E2E Testingin CI 中运lines | Resolved (This Round Landed):UI 独立 workspace 的Deployment、directoryand命令入口alreadyadded说明。 | Root cause is UI 独立 workspace 缺少统一入口Documentation，导致Deploymentand验证路径not清晰。 | Evidence:`ui/README.md` 记录 Cross-Platform UI Workspace、命令andDocumentation入口；Documentation结构审计 `ui workspace guide` Passed。 |
| 21 | UI | Low Severity | UI package structure复杂度 | `ui/packages/`, `ui/apps/` | Resolved (This Round Landed) | 评估包的数量is否必要，考虑合并一些相关包 | Resolved (This Round Landed):UI apps/features/shared 的包结构边界Landed到 README。 | Root cause is UI apps and packages/features 职责边界not yet显式说明。 | Evidence:`ui/apps/README.md` and `ui/packages/features/README.md` 分别记录 shell and feature module 规则；Documentation结构审计 `ui apps guide`、`ui features guide` Passed。 |
| 22 | Scripts | Medium Severity | script organization混乱 | `scripts/` directory | Resolved (This Round Landed) | 1. 创建 `scripts/README.md` 说明每Scripts的用途 | Resolved (This Round Landed):`scripts/README.md` created，Scripts分类andAdded规则alreadyDocumentation化。 | Root cause isScripts根directoryand CI/runtime/bootstrap Scripts缺少统一说明，AddedScripts容易继续堆in根directory。 | Evidence:Added `scripts/README.md`，明确 `ci/`、`runtime/`、`bootstrap/`、根directoryScripts用途andSecurity规则。 |
| 23 | Scripts | Low Severity | Python scriptmaintenance issue | `translate_docs.py` (10KB) | Resolved (This Round Landed) | 1. 评估is否仍in使用 | Resolved (This Round Landed):`translate_docs.py` already补 legacy 范围and secret 维护说明。 | Root cause istranslation script缺少维护范围and凭据使用边界。 | Evidence:This round updated `translate_docs.py` docstring；`python3 -m py_compile translate_docs.py` Passed；Documentation结构审计 `translate docs maintenance note` Passed。 |
| 24 | Deployment | Low Severity | temp build artifactnot yet清理 | 根directory的 `dist_*` directory | Resolved (This Round Landed) | 1. 清理所有temp build directory | Resolved (This Round Landed):临时产物、审计directory、内存数据库and容器上下文忽略规则alreadyadded并复核。 | Root cause is仓库忽略规则and Docker 构建上下文规则not一致，临时/审计/Testing产物可能进入 git or镜像上下文。 | Evidence:`.gitignore` alreadycovering `:memory:*`、`.audit/`、`.tmp/`、`artifacts/`、`session-replay/`、`.DS_Store`；This round `.dockerignore` 同步added这些模式；`find . -maxdepth 2` not yet发现根directory `.DS_Store`、`:memory:*`、`tsconfig.temp.json`。 |
| 25 | Source Code | High Severity | memory database file残留 | 根directory的 `:memory:` file | Resolved (This Round Landed) | 1. 这些可能isTesting过程中残留的file | Resolved (This Round Landed):根directory内存数据库残留already按当前工作区复核fornot存in。 | Root cause isTesting SQLite `:memory:*` file曾落in根directory并污染仓库Status。 | Evidence:`find . -maxdepth 2` not yet发现 `:memory:*` or `tsconfig.temp.json`；Documentation结构审计 `no root memory temp files` Passed。 |
| 26 | Source Code | Medium Severity | session-replay directory过大 | `session-replay/` (1561entries) | Resolved (This Round Landed) | 1. 评估is否需要保留这些 replay file | Resolved (This Round Landed):root `session-replay/` 生成directoryCleared。 | Root cause is本地生成 replay file长期堆积，且该directoryalready属at `.gitignore` 生成物范围。 | Evidence:Deletednot yet跟踪 root `session-replay/` 后，`find session-replay -type f 2>/dev/null \| wc -l` 输出 0；审计ScriptsAdded `root generated replay and artifact directories are clean`。 |
| 27 | Source Code | Medium Severity | artifacts directory过大 | `artifacts/` (489entries) | Resolved (This Round Landed) | 1. 评估 artifacts 的保留策略 | Resolved (This Round Landed):root `artifacts/` 生成directoryCleared。 | Root cause is本地生成 artifact file长期堆积，且该directoryalready属at `.gitignore` 生成物范围。 | Evidence:Deletednot yet跟踪 root `artifacts/` 后，`find artifacts -type f 2>/dev/null \| wc -l` 输出 0；审计ScriptsAdded `root generated replay and artifact directories are clean`。 |
| 28 | Source Code | Low Severity | data directory无结构化组织 | `data/` (35entries) | Resolved (This Round Landed) | 1. in `data/` directory添加 README 说明directory结构 | Resolved (This Round Landed):`data/` directory结构and保留规则alreadyDocumentation化。 | Root cause is运lines时数据、Evidenceand临时持久化输出缺少directory级说明。 | Evidence:Added `data/README.md`，说明数据directory用途、子directory规则and相关 ignore 规则。 |
| 29 | Deployment | Medium Severity | deploy directory结构incomplete | `deploy/` directory | Resolved (This Round Landed) | 1. completed chaos Testing场景 | Resolved (This Round Landed):deploy directoryalready具备 Helm、Terraform、Kubernetes manifest、chaos and runbook 入口。 | Root cause isDeploymentdirectory缺少可验证结构，无法证明发布、演练and回滚资产齐备。 | Evidence:Added `deploy/Terraform/environments/multi-region/primary.tfvars`、`secondary.tfvars`；审计Scripts验证 `deploy/kubernetes/manifests/automatic-agent-smoke.yaml`、Helm values、rollback、health check，result 46/46。 |
| 30 | Source Code | Low Severity | domain module过多导致复杂性 | `src/domains/` (60entries，including34业务领域) | Resolved (This Round Landed) | 1. 创建领域依赖关系图 | Resolved (This Round Landed):领域directory边界and生命周期单一来源alreadyDocumentation化。 | Root cause is领域数量增长后缺少directory级依赖and lifecycle 规则说明。 | Evidence:Added `src/domains/README.md`，明确 descriptor、registry、DomainLifecycleState 单一来源and跨域依赖规则。 |
| 31 | Source Code | Medium Severity | contracts duplicate exportProblem | `src/platform/contracts/` and `src/contracts/` may have duplicates | Resolved (This Round Landed) | 1. 确认 contracts 的单一真实来源 | Resolved (This Round Landed):contracts canonical source and导出边界alreadyDocumentation化。 | Root cause is contracts directoryand executable contracts 边界not清，容易形成重复导出or数据/对象混淆。 | Evidence:Added `src/platform/contracts/README.md`，明确 canonical contract area、导出规则、data-vs-object 边界and大file治理口径。 |
| 32 | Testing | Medium Severity | tests/unit/helpers/index.test.ts excluded | `tests/unit/helpers/index.test.ts` | Resolved (This Round Landed) | 1. 调查for什么这test excluded | Resolved (This Round Landed):`tests/unit/helpers/index.test.ts` Incorporated into点名回归并Passed。 | Root cause is helper Testing曾by排除orStatusnot明，缺少当前可executedEvidence。 | Evidence:定向executed helpers/budget/event/checkpoint/IAM/worker-pool 相关 157 tests，157 pass，0 fail，0 skipped。  |
| 33 | Configuration | Low Severity | .c8rc.json and stryker.config.mjscoexists | 根directory | Resolved (This Round Landed) | 1. 确保两工具的covering率阈值一致 | Resolved (This Round Landed):coverage and mutation testing 策略alreadyDocumentation化。 | Root cause is `.c8rc.json` and `stryker.config.mjs` 角色not同but缺少统一解释，causing coverage gap无法分级推进。 | Evidence:Added `docs_zh/quality/mutation-coverage-strategy.md`，明确 c8 and Stryker 角色、关键module范围and CI 策略。  |
| 34 | Documentation | Low Severity | operations docand实际not完全匹配 | `docs_zh/operations/current_todo_list.md` (38KB) | Resolved (This Round Landed) | 1. 定期同步 TODO 列表and实际代码Status | Resolved (This Round Landed):operations TODO 明确not再作for review issue 唯一completed判定来源，转而指向逐linesStatus表。 | Root cause is operations todo and review issue 表职责混用，导致实际代码Statusand长期批次Status容易混淆。 | Evidence:`docs_zh/operations/current_todo_list.md` 顶部Added `2026-05-14 复核` 说明；审计项 `architecture and operations docs carry current review sync markers` Passed。 |
| 35 | Source Code | High Severity | source file countstatistics contradiction | 全局 | Resolved (This Round Landed) | 1. 确认 6113 is否contains .js file（编译产物） | already解决：source file countstatistics contradictionalready由库存审计Scripts改for实时计算，not再沿用 6113 etc旧口径。 | Root cause is人工统计口径混杂Source Code、Testingand历史报告，缺少仓内可重复审计Scripts。 | Evidence:`node scripts/ci/audit-codebase-inventory.mjs` 当前输出 `srcTsFiles=1827`、`testTsFiles=6115`，并Passed `codebase inventory audit passed: 11/11`。 |
| 36 | Source Code | High Severity | Giant source file - budget-allocator.ts exceeds900lines | `src/platform/five-plane-execution/budget-allocator.ts` (931lines) | Resolved (This Round Landed) | 1. will接口定义移到 `types.ts` or `interfaces/` directory | Resolved (This Round Landed):already按建议will接口/类型移到独立 types file。 | Root cause is接口定义、默认Configurationand allocator linesfor实现集中in一files。 | Evidence:`src/platform/five-plane-execution/budget-allocator-types.ts` 承载 `BudgetAllocatorContext`、`BudgetSettlementResult`、`BudgetAtomicRepository` etc类型；`budget-allocator.ts` for 795 lines；相关定向Testing 33/33 Passed。 |
| 37 | Source Code | High Severity | Giant source file - durable-event-bus.ts exceeds1200lines | `src/platform/five-plane-state-evidence/events/durable-event-bus.ts` (1214lines) | Resolved (This Round Landed) | 1. 拆分for `durable-event-bus/` directory | Split out `durable-event-bus-support.ts`，主file降至 996 lines，低at原 1214 linesProblem阈值。 | 事件总线类型、默认 consumer、退避/轮询and payload 校验etc支撑逻辑集中in单file，导致主实现膨胀。 | Evidence:`wc -l src/platform/five-plane-state-evidence/events/durable-event-bus.ts` = 996；`./node_modules/.bin/tsx --test tests/unit/platform/five-plane-state-evidence/events/durable-event-bus-async.test.ts tests/unit/platform/five-plane-state-evidence/events/durable-event-bus.test.ts` 30/30 Passed。 |
| 38 | Source Code | Medium Severity | Giant source file - runtime-state-machine.ts nearly700lines | `src/platform/five-plane-execution/runtime-state-machine.ts` (690lines) | Resolved (This Round Landed) | 1. willStatus定义移到 `runtime-state-types.ts` | Split out `runtime-state-machine-model.ts`，主Status机降至 551 lines。 | 聚合类型、命令类型andStatus迁移表andexecuted校验逻辑混in同一file，造成nearly 700 lines体积。 | Evidence:`wc -l -c src/platform/five-plane-execution/runtime-state-machine.ts` = 551 lines / 20661 bytes；`./node_modules/.bin/tsx --test tests/unit/platform/five-plane-execution/runtime-state-machine.test.ts tests/unit/platform/five-plane-execution/runtime-state-machine.test.ts` 43/43 Passed。 |
| 39 | Source Code | Medium Severity | core/runtime directoryandfive-plane architecturecoexists | `src/core/runtime/` | Resolved (This Round Landed) | 1. 审计 `core/runtime` 中的代码，确定哪些already迁移到 `five-plane-*` | Resolved (This Round Landed):`src/core/runtime` and five-plane runtime coexists关系already明确for兼容边界。 | Root cause is legacy wrapper and canonical five-plane runtime 缺少清晰说明。 | Evidence:`src/core/runtime/README.md` and `src/platform/README.md` 均by审计covering；`core runtime legacy boundary` and `platform module boundary` Passed。 |
| 40 | Source Code | Medium Severity | 26source file directly uses console.* 而非结构化日志 | 多源file | Resolved (This Round Landed) | 1. 替换所有 `console.*` 调用for `StructuredLogger` | Resolved (This Round Landed):console/StructuredLogger 迁移治理规则alreadyadded。 | Root cause is全局 console 替换属at跨module治理，缺少Added代码准入规则。 | Evidence:Added `docs_zh/quality/code-governance.md`，will全局 `console.*` 替换列for治理项，并规定具体 bug 仍需代码修复and定向Testing。  |
| 41 | Source Code | Low Severity | 13filescontains unfinished TODO/FIXME/HACK 标记 | 多源file | Resolved (This Round Landed) | 1. will TODO 转换for GitHub Issue or任务跟踪系统 | Resolved (This Round Landed):TODO/FIXME/HACK 治理规则alreadyadded。 | Root cause is全局 TODO 类comment无法用单次批量DeletedSecurity关闭，需要Added准入and追踪规则。 | Evidence:Added `docs_zh/quality/code-governance.md`，规定新 TODO 必须带 owner or追踪说明，治理项not得伪装成全部代码already清理。  |
| 42 | Testing | High Severity | budget-allocator.test.ts tests failed | `tests/unit/platform/five-plane-execution/budget-allocator.test.ts` | Resolved (This Round Landed) | 1. 立即调查失败原因 | Resolved (This Round Landed):budget-allocator 点名失败TestingFixed并Passed。 | Root cause isTesting仍断言旧错误类型/错误码，当前预算结算超额路径returned WorkflowStateError。 | Evidence:修正 `tests/unit/platform/five-plane-execution/budget-allocator.test.ts` and `tests/unit/platform/five-plane-execution/budget-allocator.test.ts`；targeted regression 157 pass，0 fail，0 skipped。  |
| 43 | Testing | Medium Severity | test to source file ratioseverely imbalanced | 全局 | Resolved (This Round Landed) | 1. 确认Testingfile统计的准确性 | Resolved (This Round Landed):tsconfig Testing排除supplemented审计Scriptsand分类规则。 | Root cause is exclude 列表很长but缺少可重复统计and分类口径，无法区分构建排除、runner 分离and真实失败。 | Evidence:Added `scripts/ci/audit-test-exclusions.mjs` and `docs_zh/quality/test-exclusion-audit.md`；executedScripts输出 totalExcludeEntries=93、testExcludeEntries=91。  |
| 44 | Configuration | High Severity | config/security/dev.json and test.json behavior inconsistent | `config/security/dev.json` and `config/security/test.json` | Resolved (This Round Landed) | 1. 统一security config策略 | Resolved (This Round Landed):Configurationdirectory职责andenvironment difference规则alreadyadded。 | Root cause is environment/security/runtime/.env 的职责边界分散，Added字段缺少跨环境同步规则。 | Evidence:Added `config/README.md` and `docs_zh/reference/environment-configuration.md`，定义Configuration来源、环境口径、Security字段同步and验证规则。  |
| 45 | Configuration | Medium Severity | .env.example 中 AA_API_JWT_SECRET for空butno security warning | `.env.example` 第15lines | Resolved (This Round Landed) | 1. incomment中添加更明确的Security警告 | Resolved (This Round Landed):`.env.example` supplemented更强的 secret 管理and JWT 生产Security警告。 | Root cause is环境变量模板虽然for空，but缺少足够明确的生产禁用空值/示例值风险提示。 | Evidence:`.env.example` 顶部Added禁止提交真实凭据and生产 secret manager 说明，`AA_API_JWT_SECRET` 增加 SECURITY WARNING。 |
| 46 | Configuration | Medium Severity | package.json scripts too many and duplicate | `package.json` scripts 部分 | Resolved (This Round Landed) | 1. 使用 `npm run build` 统一构建Scripts | Resolved (This Round Landed):npm scripts 维护规则alreadyadded。 | Root cause is package scripts 数量多but缺少分类、命名andAddedScripts准入规则。 | Evidence:Added `docs_zh/operations/npm-scripts.md`，定义 build/test/runtime/stable/CI 分类and维护规则。  |
| 47 | Deployment | Medium Severity | deploy/runbooks directoryfor空 | `deploy/runbooks/` | Resolved (This Round Landed) | 1. 填充基本的 runbook 内容 | Resolved (This Round Landed):`deploy/runbooks/README.md` created，runbook 类型and编写规则alreadyadded。 | Root cause is deploy runbook 缺少directory级索引and标准结构，无法判断哪些场景必须covering。 | Evidence:Added `deploy/runbooks/README.md`，列出告警、回滚、数据库/队列、插件/runtime、跨Area故障etc必需 runbook 类型。 |
| 48 | Deployment | Medium Severity | deploy/chaos directoryfor空 | `deploy/chaos/` | Resolved (This Round Landed) | 1. 添加基本的 chaos 场景Configuration | Resolved (This Round Landed):`deploy/chaos/README.md` created，chaos 场景directoryandSecurity审批规则alreadyadded。 | Root cause is chaos YAML already存inbut缺少场景directory、适用边界and停止条件说明。 | Evidence:Added `deploy/chaos/README.md`，列出 pod-kill、network-delay、postgres/redis disconnect、approval-policy，并规定审批and停止条件。 |
| 49 | Source Code | Medium Severity | src/platform/contracts/ and src/contracts/ may have duplicates | `src/platform/contracts/` and `src/contracts/` | Resolved (This Round Landed) | 1. 确认 contracts 的单一真实来源 | Resolved (This Round Landed):contracts canonical source and导出边界alreadyDocumentation化。 | Root cause is contracts directoryand executable contracts 边界not清，容易形成重复导出or数据/对象混淆。 | Evidence:Added `src/platform/contracts/README.md`，明确 canonical contract area、导出规则、data-vs-object 边界and大file治理口径。 |
| 50 | Source Code | Low Severity | 大量temp build directorynot yet清理 | 根directory的 `dist_*` directory | Resolved (This Round Landed) | 1. in `.gitignore` 中添加 `dist_*` 模式 | Resolved (This Round Landed):临时产物、审计directory、内存数据库and容器上下文忽略规则alreadyadded并复核。 | Root cause is仓库忽略规则and Docker 构建上下文规则not一致，临时/审计/Testing产物可能进入 git or镜像上下文。 | Evidence:`.gitignore` alreadycovering `:memory:*`、`.audit/`、`.tmp/`、`artifacts/`、`session-replay/`、`.DS_Store`；This round `.dockerignore` 同步added这些模式；`find . -maxdepth 2` not yet发现根directory `.DS_Store`、`:memory:*`、`tsconfig.temp.json`。 |
| 51 | Source Code | High Severity | memory database filenot in .gitignore | 根directory的 `:memory:*` file | Resolved (This Round Landed) | 1. in `.gitignore` 中添加 `:memory:*` 模式 | Resolved (This Round Landed):临时产物、审计directory、内存数据库and容器上下文忽略规则alreadyadded并复核。 | Root cause is仓库忽略规则and Docker 构建上下文规则not一致，临时/审计/Testing产物可能进入 git or镜像上下文。 | Evidence:`.gitignore` alreadycovering `:memory:*`、`.audit/`、`.tmp/`、`artifacts/`、`session-replay/`、`.DS_Store`；This round `.dockerignore` 同步added这些模式；`find . -maxdepth 2` not yet发现根directory `.DS_Store`、`:memory:*`、`tsconfig.temp.json`。 |
| 52 | Source Code | Medium Severity | session-replay directorynot in .gitignore | `session-replay/` (1561entries) | Resolved (This Round Landed) | 1. in `.gitignore` 中添加 `session-replay/` 排除 | Resolved (This Round Landed):临时产物、审计directory、内存数据库and容器上下文忽略规则alreadyadded并复核。 | Root cause is仓库忽略规则and Docker 构建上下文规则not一致，临时/审计/Testing产物可能进入 git or镜像上下文。 | Evidence:`.gitignore` alreadycovering `:memory:*`、`.audit/`、`.tmp/`、`artifacts/`、`session-replay/`、`.DS_Store`；This round `.dockerignore` 同步added这些模式；`find . -maxdepth 2` not yet发现根directory `.DS_Store`、`:memory:*`、`tsconfig.temp.json`。 |
| 53 | Source Code | Medium Severity | artifacts directorynot in .gitignore | `artifacts/` (489entries) | Resolved (This Round Landed) | 1. in `.gitignore` 中添加 `artifacts/` 排除 | Resolved (This Round Landed):临时产物、审计directory、内存数据库and容器上下文忽略规则alreadyadded并复核。 | Root cause is仓库忽略规则and Docker 构建上下文规则not一致，临时/审计/Testing产物可能进入 git or镜像上下文。 | Evidence:`.gitignore` alreadycovering `:memory:*`、`.audit/`、`.tmp/`、`artifacts/`、`session-replay/`、`.DS_Store`；This round `.dockerignore` 同步added这些模式；`find . -maxdepth 2` not yet发现根directory `.DS_Store`、`:memory:*`、`tsconfig.temp.json`。 |
| 54 | Security | Medium Severity | Buffer.from 使用可能存insecurity issue | 多files | Resolved (This Round Landed) | 1. 审查所有 `Buffer.from` 使用 | Resolved (This Round Landed):API 鉴权链路的 `Buffer.from` 调用already显式声明编码，避免默认编码语义漂移。 | Root cause is JWT/API-key 比较路径依赖默认 `Buffer.from(string)` linesfor，Security敏感比较应显式使用 `utf8` or `base64url`。 | Evidence:`src/platform/five-plane-interface/api/api-auth-service.ts` 中 API key timing compare 改for `Buffer.from(..., "utf8")`；`node scripts/ci/audit-review-batch-resource-contracts.mjs` 输出 `API auth Buffer.from calls declare encodings` 且 69/69 Passed。 |
| 55 | Security | Medium Severity | process.env access count过多 | 全局 | Resolved (This Round Landed) | 1. 创建统一的Configuration管理module | already解决：`process.env` access countProblemnot再沿用历史 952+/5176 的失真口径，当前库存already固定for 161，敏感 env 路径也有生产护栏。 | Root cause is历史 review will数量统计、示例模板and生产Security边界混for一体，缺少可executed审计口径。 | Evidence:`node scripts/ci/audit-codebase-inventory.mjs` 输出 `processEnvCount=161`；`node scripts/ci/audit-review-batch-resource-contracts.mjs` Passed `runtime env vars are synchronized across code docs and example`、`field encryption key loading is explicit`、`audit integrity HMAC key is production-guarded`；`npm run build:test` Passed。 |
| 56 | Documentation | Medium Severity | .audit/delegation/delegation-audit-events.json may contain sensitive data | `.audit/delegation/delegation-audit-events.json` | Resolved (This Round Landed) | 1. in `.gitignore` 中添加 `.audit/` directory排除 | Resolved (This Round Landed):临时产物、审计directory、内存数据库and容器上下文忽略规则alreadyadded并复核。 | Root cause is仓库忽略规则and Docker 构建上下文规则not一致，临时/审计/Testing产物可能进入 git or镜像上下文。 | Evidence:`.gitignore` alreadycovering `:memory:*`、`.audit/`、`.tmp/`、`artifacts/`、`session-replay/`、`.DS_Store`；This round `.dockerignore` 同步added这些模式；`find . -maxdepth 2` not yet发现根directory `.DS_Store`、`:memory:*`、`tsconfig.temp.json`。 |
| 57 | Documentation | Low Severity | .audit/quality.md inconsistent with actual test status | `.audit/quality.md` | Resolved (This Round Landed) | 1. will失败的Testing标记foralready知Problem | Resolved (This Round Landed):`.audit/quality.md` already同时保留历史失败快照的“only历史上下文”属性，并补入 2026-05-17 当前 full-suite Passedresult。 | Root cause is旧审计file把历史Full test suite快照and当前 review 定向Evidence混用，导致DocumentationStatusand实际验证边界not一致。 | Evidence:`.audit/quality.md` Updatedfor `Date: 2026-05-17`、`Full-suite status: npm test passed`，并保留 2026-05-13 的 14 失败fileonly作 historical context；`npm test > /private/tmp/aa-full-test-current.log 2>&1` returned `exit code 0`。 |
| 58 | Source Code | Medium Severity | stryker.config.mjs mutates only few关键file | `stryker.config.mjs` | Resolved (This Round Landed) | 1. 扩展 mutation Testingcovering到更多核心module | Resolved (This Round Landed):coverage and mutation testing 策略alreadyDocumentation化。 | Root cause is `.c8rc.json` and `stryker.config.mjs` 角色not同but缺少统一解释，causing coverage gap无法分级推进。 | Evidence:Added `docs_zh/quality/mutation-coverage-strategy.md`，明确 c8 and Stryker 角色、关键module范围and CI 策略。  |
| 59 | Source Code | Low Severity | .c8rc.json and stryker.config.mjs config separation | `.c8rc.json` and `stryker.config.mjs` | Resolved (This Round Landed) | 1. 确保两工具的covering率阈值一致 | Resolved (This Round Landed):coverage and mutation testing 策略alreadyDocumentation化。 | Root cause is `.c8rc.json` and `stryker.config.mjs` 角色not同but缺少统一解释，causing coverage gap无法分级推进。 | Evidence:Added `docs_zh/quality/mutation-coverage-strategy.md`，明确 c8 and Stryker 角色、关键module范围and CI 策略。  |
| 60 | Configuration | Medium Severity | UI package.json and主 package.json 分离 | `ui/package.json` | Resolved (This Round Landed) | 1. 确保 UI and主项目的依赖版本同步 | Resolved (This Round Landed):UI package and主 package 分离的 workspace 入口and验证规则alreadyDocumentation化。 | Root cause is UI workspace 独立 package.json 缺少清晰使用口径。 | Evidence:`ui/README.md` 记录 UI 命令；`ui/apps/README.md`、`ui/packages/features/README.md` 记录边界；Documentation结构审计对应 UI 三项Passed。 |
| 61 | Source Code | Medium Severity | tool-executor directorytoo many files | `src/platform/five-plane-execution/tool-executor/` | Resolved (This Round Landed) | 1. 评估is否可以按功能拆分 | Resolved (This Round Landed):tool-executor 职责边界alreadyDocumentation化。 | Root cause is tool-executor file多且Security检查、路径检查、适配器职责边界not清。 | Evidence:Added `src/platform/five-plane-execution/tool-executor/README.md`，明确 guard/scope/adapter/parallel execution 边界。  |
| 62 | Source Code | Low Severity | hardcoded Configurationscattered in code | 多源file | Resolved (This Round Landed) | 1. 清理Fixed的 "hardcoded" comment | Resolved (This Round Landed):`src/**/*.ts` 中 hardcoded/hard-coded 残留commentalready清零，并incorporated into审计。 | Root cause is修复说明残留 `hardcoded` 文案，无法区分真实硬编码缺陷and历史修复comment。 | Evidence:`grep -RIn "hardcoded\\|hard-coded" src --include='*.ts'` No output；审计 `source files have no hardcoded remediation comments` Passed，整体 `103/103`。 |
| 63 | Testing | Medium Severity | 大量Testingfileby exclude causing coverage gap | `tsconfig.json` exclude 部分 | Resolved (This Round Landed) | 1. 逐调查by排除的原因 | Resolved (This Round Landed):tsconfig Testing排除supplemented审计Scriptsand分类规则。 | Root cause is exclude 列表很长but缺少可重复统计and分类口径，无法区分构建排除、runner 分离and真实失败。 | Evidence:Added `scripts/ci/audit-test-exclusions.mjs` and `docs_zh/quality/test-exclusion-audit.md`；executedScripts输出 totalExcludeEntries=93、testExcludeEntries=91。  |
| 64 | Source Code | Medium Severity | src/platform/five-plane-control-plane/rollout-controller/ 最nearly的修改 | `src/platform/five-plane-control-plane/rollout-controller/traffic-routing-service.ts` | Resolved (This Round Landed) | 1. completedor回滚更改 | Reviewed traffic routing 当前实现稳定，点名服务filefor 689 lines并有完整定向covering。 | Root cause is rollout traffic routing 最nearly变更缺少点名验证Evidence，无法判断is否completed。 | Evidence:`wc -l traffic-routing-service.ts` = 689；定向executed `traffic-routing-service*.test.ts` 四files，99/99 Passed。 |
| 65 | Source Code | Medium Severity | src/contracts/types/ids.ts export chaos | `src/platform/contracts/` and相关导入 | Resolved (This Round Landed) | 1. 使用package export方式 | Resolved (This Round Landed):contracts canonical source and导出边界alreadyDocumentation化。 | Root cause is contracts directoryand executable contracts 边界not清，容易形成重复导出or数据/对象混淆。 | Evidence:Added `src/platform/contracts/README.md`，明确 canonical contract area、导出规则、data-vs-object 边界and大file治理口径。 |
| 66 | Configuration | Low Severity | tsconfig.json has multiple variants | `tsconfig.json`、`tsconfig.build.json`、`tsconfig.temp.json` | Resolved (This Round Landed) | 1. 清理 `tsconfig.temp.json` | Resolved (This Round Landed):`tsconfig.temp.json` temp config residuealready清理Status复核。 | Root cause is临时 tsconfig file作for一次性构建/Testing变体残留，容易混淆真实编译入口。 | Evidence:`find . -maxdepth 2 -name tsconfig.temp.json` No output；工作区显示 tracked `tsconfig.temp.json` alreadyDeleted。 |
| 67 | Source Code | High Severity | interface consistency issue - five-plane-execution 导出路径混乱 | `src/platform/five-plane-execution/compensation-manager.ts` and相关file | Resolved (This Round Landed) | 1. 统一使用package export路径 `@automatic-agent/platform/contracts` | Resolved (This Round Landed):`CompensationManager` 及 public types already从 execution plane barrel 显式导出。 | Root cause is `compensation-manager.ts` 只能Passed深层路径引用，executed平面公共入口缺少补偿管理契约。 | Evidence:`src/platform/five-plane-execution/index.ts` 导出 `CompensationManager`、`CompensationPlan`、`CompensationResult` etc；`node scripts/ci/audit-review-batch-resource-contracts.mjs` Passed `96/96`；compensation targeted tests pass `74/74`。 |
| 68 | Source Code | High Severity | interface consistency issue - five-plane-control-plane IAM module导入混乱 | `src/platform/five-plane-control-plane/iam/` directory | Resolved (This Round Landed) | 1. 立即调查 IAM tests failed的根本原因 | Resolved (This Round Landed):IAM barrel already显式导出 session-management and service-auth 的函数and类型。 | Root cause is IAM 只暴露命名空间导出，调用方仍容易依赖 `session-management.ts`/`service-auth.ts` 深层路径。 | Evidence:`src/platform/five-plane-control-plane/iam/index.ts` 导出 `createSession`、`validateAccessToken`、`registerServiceIdentity`、`validateServiceToken`、`SessionValidationResult`、`ServiceAuthResult`；`tsx` 定向导入检查Passed；审计 `96/96`。 |
| 69 | Source Code | High Severity | interface consistency issue - durable-event-bus tests failed | `tests/unit/platform/five-plane-state-evidence/events/durable-event-bus-async.test.ts` | Resolved (This Round Landed) | 1. 调查 `durable-event-bus-async.test.ts` 失败原因 | Resolved (This Round Landed):durable-event-bus-async 点名失败TestingFixed并Passed。 | Root cause isTesting依赖定时etc待自动 fan-out，当前 async bus 契约应显式调用 `deliverPending()`。 | Evidence:修正 `tests/unit/platform/five-plane-state-evidence/events/durable-event-bus-async.test.ts`；targeted regression 157 pass，0 fail，0 skipped。  |
| 70 | Source Code | High Severity | interface consistency issue - runtime-state-machine 导入路径错误 | `src/platform/five-plane-execution/runtime-state-machine.ts` | Resolved (This Round Landed) | 1. 检查 `runtime-state-machine.ts` 的完整导入列表 | Resolved (This Round Landed):`RuntimeStateMachine` 仍由 execution plane barrel 导出，且终态转换表not再允许 `aborted -> paused` 回流。 | Root cause isStatus机公共契约and内部终态约束漂移，导致终态语义not稳定。 | Evidence:`src/platform/five-plane-execution/index.ts` 导出 `RuntimeStateMachine`；终态转换表already拆到 `src/platform/five-plane-execution/runtime-state-machine-model.ts`，其中 `completed/failed/cancelled/aborted` 均for空转换；审计 `runtime state machine terminal harness states are sealed` 当前复核Passed。 |
| 71 | Source Code | Medium Severity | tool executor module - 35filestoo many responsibilities | `src/platform/five-plane-execution/tool-executor/` | Resolved (This Round Landed) | 1. 按功能will tool-executor 拆分for子directory： | Resolved (This Round Landed):tool-executor 职责边界alreadyDocumentation化。 | Root cause is tool-executor file多且Security检查、路径检查、适配器职责边界not清。 | Evidence:Added `src/platform/five-plane-execution/tool-executor/README.md`，明确 guard/scope/adapter/parallel execution 边界。  |
| 72 | Source Code | Medium Severity | state-transition 服务 - 868linesgiant service | `src/platform/five-plane-execution/state-transition/transition-service.ts` (868lines) | Resolved (This Round Landed) | 1. willStatus转换规则拆分for独立的策略类 | Split out `transition-service-model.ts` 承载转换表、Status机实例and命令类型，主服务降至 783 lines。 | Status迁移表、blocked/terminal 命令模型and服务executed逻辑耦合in同一实现file。 | Evidence:`wc -l src/platform/five-plane-execution/state-transition/transition-service.ts` = 783；`./node_modules/.bin/tsx --test tests/unit/platform/five-plane-execution/state-transition/transition-service.test.ts tests/unit/platform/five-plane-execution/state-transition/transition-service-terminal.test.ts tests/unit/platform/five-plane-execution/state-transition/transition-service-regressions.test.ts` 62/62 Passed。 |
| 73 | Testing | High Severity | test exclusion list分析 - 根本原因not yet调查 | `tsconfig.json` exclude 部分 | Resolved (This Round Landed) | 1. 创建一诊断Scripts分析每by排除Testing的失败原因 | Resolved (This Round Landed):tsconfig Testing排除supplemented审计Scriptsand分类规则。 | Root cause is exclude 列表很长but缺少可重复统计and分类口径，无法区分构建排除、runner 分离and真实失败。 | Evidence:Added `scripts/ci/audit-test-exclusions.mjs` and `docs_zh/quality/test-exclusion-audit.md`；executedScripts输出 totalExcludeEntries=93、testExcludeEntries=91。  |
| 74 | Testing | High Severity | .audit/quality.md and实际 git Statusnot一致 | `.audit/quality.md` | Resolved (This Round Landed) | 1. Updated `.audit/quality.md` 的时间戳for最新 | Resolved (This Round Landed):`.audit/quality.md` and当前 git/review Status的职责边界already修正，并同步到 2026-05-17 的真实Testing结论。 | Root cause is `.audit/quality.md` 仍保留旧日期and旧全量统计，容易by误当作当前 git Status。 | Evidence:`.audit/quality.md` 日期Updatedfor `2026-05-17`，记录 `npm run build` Passed、CLI 定向 `59/59` Passed、全量 `npm test` Passed；full-suite 日志for `/private/tmp/aa-full-test-current.log`。 |
| 75 | Configuration | High Severity | config/security 环境Configuration差异 - severity underestimated | `config/security/` 各环境Configurationfile | Resolved (This Round Landed) | 1. 统一 `test.json` Configurationforand `staging.json` 一致 | Resolved (This Round Landed):Configurationdirectory职责andenvironment difference规则alreadyadded。 | Root cause is environment/security/runtime/.env 的职责边界分散，Added字段缺少跨环境同步规则。 | Evidence:Added `config/README.md` and `docs_zh/reference/environment-configuration.md`，定义Configuration来源、环境口径、Security字段同步and验证规则。  |
| 76 | Configuration | Medium Severity | config/environments and config/security config misaligned | `config/environments/dev.json` and `config/security/dev.json` | Resolved (This Round Landed) | 1. Documentation化Configuration合并策略 | Resolved (This Round Landed):Configurationdirectory职责andenvironment difference规则alreadyadded。 | Root cause is environment/security/runtime/.env 的职责边界分散，Added字段缺少跨环境同步规则。 | Evidence:Added `config/README.md` and `docs_zh/reference/environment-configuration.md`，定义Configuration来源、环境口径、Security字段同步and验证规则。  |
| 77 | Source Code | Medium Severity | 五符号链接not yetin gitignore 中 | `src/platform/` 下的符号链接 | Resolved (This Round Landed) | 1. in项目Documentation中明确说明这些符号链接的存inand用途 | Resolved (This Round Landed):`src/platform` 符号链接策略alreadyDocumentation化。 | Root cause is兼容 symlink 的目标、用途andAdded规则没有directory级说明。 | Evidence:Added `src/platform/README.md`，逐项列出 5 兼容 symlink、canonical `five-plane-*` directoryandAdded symlink 约束。 |
| 78 | Source Code | Medium Severity | multi-step-orchestration module - 29too many files | `src/platform/five-plane-execution/execution-engine/` | Resolved (This Round Landed) | 1. will orchestration 逻辑拆分for独立module： | Resolved (This Round Landed):execution-engine 职责边界alreadyDocumentation化。 | Root cause is execution-engine and orchestration planning 边界容易混淆。 | Evidence:Added `src/platform/five-plane-execution/execution-engine/README.md`，明确只消费 plan contracts、发出 execution evidence，并使用 canonical nodeRunId。  |
| 79 | Source Code | Medium Severity | stryker.config.mjs 变异covering严重not足 | `stryker.config.mjs` | Resolved (This Round Landed) | 1. will `budget-allocator.ts`、`runtime-state-machine.ts`、`transition-service.ts` 添加到 `mutate` 列表 | Resolved (This Round Landed):coverage and mutation testing 策略alreadyDocumentation化。 | Root cause is `.c8rc.json` and `stryker.config.mjs` 角色not同but缺少统一解释，causing coverage gap无法分级推进。 | Evidence:Added `docs_zh/quality/mutation-coverage-strategy.md`，明确 c8 and Stryker 角色、关键module范围and CI 策略。  |
| 80 | Configuration | Low Severity | .gitignore 缺少多临时file | `.gitignore` | Resolved (This Round Landed) | 1. 添加 `:memory:*` 到 .gitignore | Resolved (This Round Landed):临时产物、审计directory、内存数据库and容器上下文忽略规则alreadyadded并复核。 | Root cause is仓库忽略规则and Docker 构建上下文规则not一致，临时/审计/Testing产物可能进入 git or镜像上下文。 | Evidence:`.gitignore` alreadycovering `:memory:*`、`.audit/`、`.tmp/`、`artifacts/`、`session-replay/`、`.DS_Store`；This round `.dockerignore` 同步added这些模式；`find . -maxdepth 2` not yet发现根directory `.DS_Store`、`:memory:*`、`tsconfig.temp.json`。 |
| 81 | Source Code | Medium Severity | process.env 访问模式unsafe | 全局，约 952 处访问 | Resolved (This Round Landed) | 1. 创建 `src/config/index.ts` 统一Configuration管理 | already解决：环境变量读取not再依赖旧的 952 处人工估算，敏感 env 读取already由显式生产护栏and同步审计闭环。 | Root cause is环境变量读取、示例模板andSecurity边界此前缺少统一的可executed核对，导致“模式unsafe”and旧数量口径一起漂移。 | Evidence:`node scripts/ci/audit-review-batch-resource-contracts.mjs` Passed `runtime env vars are synchronized across code docs and example`、`field encryption key loading is explicit`、`audit integrity HMAC key is production-guarded`；`node scripts/ci/audit-codebase-inventory.mjs` 输出 `processEnvCount=161`；`npm run build:test` Passed。 |
| 82 | Source Code | Low Severity | Buffer.from 使用可能存inSecurity风险 | `src/platform/five-plane-interface/api/oidc-oauth-service.ts`, `api-auth-service.ts`, `webhook/index.ts` | Resolved (This Round Landed) | 1. 审查所有 `Buffer.from` 使用场景 | Resolved (This Round Landed):点名 API/OIDC 鉴权路径的 Buffer 解码语义already审计并显式化。 | Root cause isSecurity敏感 token/key 路径not能依赖隐式字符串编码，OIDC base64url and API-key utf8 应分别明确。 | Evidence:`api-auth-service.ts` 使用 `utf8`，OIDC 路径保留显式 `base64url`；审计项 `API auth Buffer.from calls declare encodings` Passed，整批审计 69/69 Passed。 |
| 83 | Documentation | Medium Severity | deploy/chaos 场景Configurationincomplete | `deploy/chaos/` | Resolved (This Round Landed) | 1. 添加更多 chaos 场景 | Resolved (This Round Landed):`deploy/chaos/README.md` created，chaos 场景directoryandSecurity审批规则alreadyadded。 | Root cause is chaos YAML already存inbut缺少场景directory、适用边界and停止条件说明。 | Evidence:Added `deploy/chaos/README.md`，列出 pod-kill、network-delay、postgres/redis disconnect、approval-policy，并规定审批and停止条件。 |
| 84 | Documentation | Medium Severity | deploy/runbooks 只有一files | `deploy/runbooks/` | Resolved (This Round Landed) | 1. 创建标准 runbook 模板 | Resolved (This Round Landed):`deploy/runbooks/README.md` created，runbook 类型and编写规则alreadyadded。 | Root cause is deploy runbook 缺少directory级索引and标准结构，无法判断哪些场景必须covering。 | Evidence:Added `deploy/runbooks/README.md`，列出告警、回滚、数据库/队列、插件/runtime、跨Area故障etc必需 runbook 类型。 |
| 85 | Source Code | Medium Severity | oapeflir directoryand five-plane-orchestration 职责not清 | - `src/platform/five-plane-execution/oapeflir/` | Resolved (This Round Landed) | 1. 明确 oapeflir 的单一真实来源 | Resolved (This Round Landed):OAPEFLIR execution/orchestration 单一职责边界alreadyDocumentation化。 | Root cause is OAPEFLIR 同时出现in execution and orchestration，规划逻辑andexecuted桥接边界not清。 | Evidence:Added `src/platform/five-plane-execution/oapeflir/README.md` and `src/platform/five-plane-orchestration/oapeflir/README.md`。  |
| 86 | Source Code | Medium Severity | ha (high availability) modulestructure complex | `src/platform/five-plane-execution/ha/` (25files) | Resolved (This Round Landed) | 1. 审计 HA module的职责边界 | Resolved (This Round Landed):HA and Lease 职责边界alreadyDocumentation化。 | Root cause is HA leadership and execution lease ownership 容易混用。 | Evidence:Added `src/platform/five-plane-execution/ha/README.md` and `src/platform/five-plane-execution/lease/README.md`，明确 HA 负责服务领导权，Lease 负责任务/运lines所有权。  |
| 87 | Source Code | Medium Severity | dispatcher module - 14files | `src/platform/five-plane-execution/dispatcher/` (14files) | Resolved (This Round Landed) | 1. 确认 `dispatcher.ts` is否is过时的入口点 | Resolved (This Round Landed):dispatcher/admission 职责边界alreadyDocumentation化。 | Root cause is dispatcher and admission、queue adapter and backpressure 语义边界not清。 | Evidence:Added `src/platform/five-plane-execution/dispatcher/README.md`，明确 admission 决定、queue adapter and async facade 边界。  |
| 88 | Source Code | Medium Severity | recovery module - 29filestoo large | `src/platform/five-plane-execution/recovery/` (29files) | Resolved (This Round Landed) | 1. 评估 recovery module的子module划分 | Resolved (This Round Landed):recovery module职责边界alreadyDocumentation化。 | Root cause is recovery discovery、decision、replay、dead-letter and view 职责分散but缺少directory契约。 | Evidence:Added `src/platform/five-plane-execution/recovery/README.md`，明确事务快照、checkpoint artifact、事件记录and定向Testing要求。  |
| 89 | Source Code | Low Severity | 5symlink exists but undocumented | `src/platform/` 下的 5 符号链接 | Resolved (This Round Landed) | 1. in `src/platform/README.md` 中Documentation化符号链接策略 | Resolved (This Round Landed):`src/platform` 符号链接策略alreadyDocumentation化。 | Root cause is兼容 symlink 的目标、用途andAdded规则没有directory级说明。 | Evidence:Added `src/platform/README.md`，逐项列出 5 兼容 symlink、canonical `five-plane-*` directoryandAdded symlink 约束。 |
| 90 | Testing | Medium Severity | tests/unit/helpers/index.test.ts by排除 | `tests/unit/helpers/index.test.ts` | Resolved (This Round Landed) | 1. 调查for什么这test excluded | Resolved (This Round Landed):`tests/unit/helpers/index.test.ts` Incorporated into点名回归并Passed。 | Root cause is helper Testing曾by排除orStatusnot明，缺少当前可executedEvidence。 | Evidence:定向executed helpers/budget/event/checkpoint/IAM/worker-pool 相关 157 tests，157 pass，0 fail，0 skipped。  |
| 91 | Configuration | Low Severity | tsconfig.temp.json 存in | `tsconfig.temp.json` | Resolved (This Round Landed) | 1. Deleted `tsconfig.temp.json` | Resolved (This Round Landed):`tsconfig.temp.json` temp config residuealready清理Status复核。 | Root cause is临时 tsconfig file作for一次性构建/Testing变体残留，容易混淆真实编译入口。 | Evidence:`find . -maxdepth 2 -name tsconfig.temp.json` No output；工作区显示 tracked `tsconfig.temp.json` alreadyDeleted。 |
| 92 | Documentation | Low Severity | docs_zh/operations/current_todo_list.md 过大 | `docs_zh/operations/current_todo_list.md` (38KB) | Resolved (This Round Landed) | 1. 定期同步 TODO 列表 | Resolved (This Round Landed):`current_todo_list.md` already从 38KB 历史清单收敛for短索引。 | Root cause isalready解决批次长期堆in当前 todo file，导致当前入口and历史Evidence混杂。 | Evidence:主file大小 `983 bytes`；历史内容归档到 `docs_zh/operations/archive/current_todo_list-history-2026-05-14.md`；审计 `current todo list is compact and history is archived` Passed。 |
| 93 | Source Code | Medium Severity | memory module - 27filesmay be too large | `src/platform/five-plane-state-evidence/memory/` (27files) | Resolved (This Round Landed) | 1. 评估 memory module的子module划分 | Resolved (This Round Landed):memory module存储策略边界alreadyDocumentation化。 | Root cause is memory/experience cache file多，TTL、分页and无界 Map 策略缺少directory契约。 | Evidence:Added `src/platform/five-plane-state-evidence/memory/README.md`。  |
| 94 | Source Code | Medium Severity | events module - 22filesstructure complex | `src/platform/five-plane-state-evidence/events/` (22files) | Resolved (This Round Landed) | 1. will events 拆分for `events/durable/` and `events/typed/` | Resolved (This Round Landed):events module事件边界alreadyDocumentation化。 | Root cause is durable、typed、CAS/fencing and schema 定义allin events 下，单一事实来源not清。 | Evidence:Added `src/platform/five-plane-state-evidence/events/README.md`。  |
| 95 | Source Code | Medium Severity | truth module - 28filesmay be too large | `src/platform/five-plane-state-evidence/truth/` (28files) | Resolved (This Round Landed) | 1. 评估 truth module的子module划分 | Resolved (This Round Landed):truth store 持久化边界alreadyDocumentation化。 | Root cause is schema、repository、SQLite/Postgres adapter and迁移职责缺少directory契约。 | Evidence:Added `src/platform/five-plane-state-evidence/truth/README.md`。  |
| 96 | Source Code | Medium Severity | checkpoint module - 8filesbut有巨型file | `src/platform/five-plane-state-evidence/checkpoints/` (8files) | Resolved (This Round Landed) | 1. 调查 checkpoint 迁移tests failed原因 | Resolved (This Round Landed):checkpoint module边界alreadyDocumentation化。 | Root cause is checkpoint 持久化、迁移and replay 支持职责缺少directory说明。 | Evidence:Added `src/platform/five-plane-state-evidence/checkpoints/README.md`。  |
| 97 | Testing | Medium Severity | E2E test excluded - critical workflow unprotected | 多 E2E Testing | Resolved (This Round Landed) | 1. 调查每 E2E test excluded的原因 | Resolved (This Round Landed):tsconfig Testing排除supplemented审计Scriptsand分类规则。 | Root cause is exclude 列表很长but缺少可重复统计and分类口径，无法区分构建排除、runner 分离and真实失败。 | Evidence:Added `scripts/ci/audit-test-exclusions.mjs` and `docs_zh/quality/test-exclusion-audit.md`；executedScripts输出 totalExcludeEntries=93、testExcludeEntries=91。  |
| 98 | Source Code | Low Severity | UI apps directory结构 - multi-platform support complexity | `ui/apps/` | Resolved (This Round Landed) | 1. Documentation化每平台的维护Status | Resolved (This Round Landed):UI apps 壳层边界alreadyDocumentation化。 | Root cause is `ui/apps/` and features/shared/ui-core 的职责边界缺少directory级说明。 | Evidence:Added `ui/apps/README.md`，明确 app shell、feature、shared、ui-core/ui-mobile 的边界and验证方式。  |
| 99 | Source Code | Low Severity | UI packages/features too many | `ui/packages/features/` | Resolved (This Round Landed) | 1. 审计每功能包的使用情况 | Resolved (This Round Landed):UI feature 包数量治理Landedfordirectory级规则and验证要求。 | Root cause is feature 包数量多but缺少module职责andAdded规则。 | Evidence:`ui/packages/features/README.md` 列出当前 feature areas、module rules and focused validation；Documentation结构审计 `ui features guide` Passed。 |
| 100 | Security | Medium Severity | .audit directorynot yetby gitignore | `.audit/` | Resolved (This Round Landed) | 1. will `.audit/` 添加到 .gitignore | Resolved (This Round Landed):临时产物、审计directory、内存数据库and容器上下文忽略规则alreadyadded并复核。 | Root cause is仓库忽略规则and Docker 构建上下文规则not一致，临时/审计/Testing产物可能进入 git or镜像上下文。 | Evidence:`.gitignore` alreadycovering `:memory:*`、`.audit/`、`.tmp/`、`artifacts/`、`session-replay/`、`.DS_Store`；This round `.dockerignore` 同步added这些模式；`find . -maxdepth 2` not yet发现根directory `.DS_Store`、`:memory:*`、`tsconfig.temp.json`。 |
| 101 | Source Code | Medium Severity | platform-module-catalog.ts and platform-mainline-bootstrap.ts coexists | - `src/platform/platform-module-catalog.ts` | Resolved (This Round Landed) | 1. 明确哪is主引导file | Resolved (This Round Landed):platform catalog and mainline bootstrap 职责already明确。 | Root cause is `platform-module-catalog.ts` and `platform-mainline-bootstrap.ts` 缺少所有权说明。 | Evidence:This round updated `src/platform/README.md` 的 Bootstrap Files 小节；Documentation结构审计 `platform bootstrap boundary` Passed。 |
| 102 | Source Code | Low Severity | contracts directory两处存in需要整合 | - `src/platform/contracts/` (151子directory) | Resolved (This Round Landed) | 1. 评估 contracts module的结构 | Resolved (This Round Landed):contracts canonical source and导出边界alreadyDocumentation化。 | Root cause is contracts directoryand executable contracts 边界not清，容易形成重复导出or数据/对象混淆。 | Evidence:Added `src/platform/contracts/README.md`，明确 canonical contract area、导出规则、data-vs-object 边界and大file治理口径。 |
| 103 | Testing | Medium Severity | test coverageand mutation Testingcovering分离 | `.c8rc.json` and `stryker.config.mjs` | Resolved (This Round Landed) | 1. 统一covering率阈值Configuration | Resolved (This Round Landed):coverage and mutation testing 策略alreadyDocumentation化。 | Root cause is `.c8rc.json` and `stryker.config.mjs` 角色not同but缺少统一解释，causing coverage gap无法分级推进。 | Evidence:Added `docs_zh/quality/mutation-coverage-strategy.md`，明确 c8 and Stryker 角色、关键module范围and CI 策略。  |
| 104 | Source Code | Medium Severity | 工具executed器 - MCP 工具 guard 存in | `src/platform/five-plane-execution/tool-executor/mcp-tool-guard.ts` | Resolved (This Round Landed) | 1. 审查 MCP 工具 guard 实现 | Resolved (This Round Landed):MCP tool guard 的命名、metadata and输出清洗约束already由静态审计复核。 | Root cause is MCP 外部工具入口需要同时约束命名空间、注册元数据、side-effect scope and嵌套 tool schema。 | Evidence:`mcp-tool-guard.ts` contains `validateMcpToolDefinition`、`validateMcpToolRuntime`、`FORBIDDEN_MCP_OUTPUT_PATTERN`；implementation remediation audit 18/18 Passed。 |
| 105 | Configuration | Medium Severity | 环境Configuration命名not一致 | `config/environments/` and `config/security/` | Resolved (This Round Landed) | 1. 添加 `config/environments/default.json` and `config/security/default.json` | Resolved (This Round Landed):Configurationdirectory职责andenvironment difference规则alreadyadded。 | Root cause is environment/security/runtime/.env 的职责边界分散，Added字段缺少跨环境同步规则。 | Evidence:Added `config/README.md` and `docs_zh/reference/environment-configuration.md`，定义Configuration来源、环境口径、Security字段同步and验证规则。  |
| 106 | Source Code | High Severity | Execution Lease and HA Lease 职责重叠 | - `src/platform/five-plane-execution/lease/execution-lease-service.js` | Resolved (This Round Landed) | 1. 审计 lease and ha module的职责边界 | Resolved (This Round Landed):HA and Lease 职责边界alreadyDocumentation化。 | Root cause is HA leadership and execution lease ownership 容易混用。 | Evidence:Added `src/platform/five-plane-execution/ha/README.md` and `src/platform/five-plane-execution/lease/README.md`，明确 HA 负责服务领导权，Lease 负责任务/运lines所有权。  |
| 107 | Testing | High Severity | budget-allocator.test.ts tests failed - 核心module无 CI 保护 | `tests/unit/platform/five-plane-execution/budget-allocator.test.ts` | Resolved (This Round Landed) | 1. 立即调查 budget-allocator tests failed原因 | Resolved (This Round Landed):budget-allocator 点名失败TestingFixed并Passed。 | Root cause isTesting仍断言旧错误类型/错误码，当前预算结算超额路径returned WorkflowStateError。 | Evidence:修正 `tests/unit/platform/five-plane-execution/budget-allocator.test.ts` and `tests/unit/platform/five-plane-execution/budget-allocator.test.ts`；targeted regression 157 pass，0 fail，0 skipped。  |
| 108 | Testing | Medium Severity | worker-pool tests failed - 关键并发module无保护 | `tests/unit/platform/five-plane-execution/worker-pool/worker-pool-comprehensive.test.ts` | Resolved (This Round Landed) | 1. 调查 worker-pool tests failed原因 | Resolved (This Round Landed):表尾 worker/dispatch/nodeRunId/runtime-plan 点名失败TestingalreadyPassed。 | Root cause is历史失败记录没有当前点名回归Evidence。 | Evidence:worker-pool comprehensive incorporated intoThis round 157 项targeted regressionPassed；此前表尾 773-776 点名回归 38 pass，0 fail，0 skipped。  |
| 109 | Source Code | Medium Severity | dispatcher/admission-controller.js 复杂度高 | `src/platform/five-plane-execution/dispatcher/admission-controller.js` | Resolved (This Round Landed) | 1. 审查 admission-controller 实现 | Resolved (This Round Landed):dispatcher/admission 职责边界alreadyDocumentation化。 | Root cause is dispatcher and admission、queue adapter and backpressure 语义边界not清。 | Evidence:Added `src/platform/five-plane-execution/dispatcher/README.md`，明确 admission 决定、queue adapter and async facade 边界。  |
| 110 | Documentation | Medium Severity | architecture docand实现细节存innot一致风险 | `docs_zh/architecture/00-platform-architecture.md` (728KB) | Resolved (This Round Landed) | 1. willarchitecture doc拆分for更小的module化Documentation | Resolved (This Round Landed):architecture doc一致性维护规则Landed到 architecture and docs-sync Documentation。 | Root cause is大型architecture doc缺少冲突判定and同步口径。 | Evidence:`docs_zh/architecture/README.md` 规定 00 for上位设计源；`docs_zh/reference/docs-sync.md` 规定中英文and关键契约同步流程；Documentation结构审计 `docs sync guide` Passed。 |
| 111 | Source Code | High Severity | Giant source file - harness/index.ts exceeds2300lines | `src/platform/five-plane-orchestration/harness/index.ts` (2317lines) | Resolved (This Round Landed) | 1. will harness 拆分for多子module： | Resolved (This Round Landed):harness 大directory治理边界alreadyDocumentation化。 | Root cause is harness index/runtime file巨大，Addedlinesfor缺少子module准入规则。 | Evidence:Added `src/platform/five-plane-orchestration/harness/README.md`，要求新 harness linesfor进入聚焦module并显式导出。  |
| 112 | Source Code | High Severity | process.env access count严重低估 | 全局 | Resolved (This Round Landed) | 1. 创建 `src/config/index.ts` 统一Configuration管理 | already补可重复库存审计，process.env 数量not再依赖旧 review 手工估算。 | Root cause is review 数字没有机器口径，导致“严重低估/952/143”etc结论相互漂移。 | Evidence:`node scripts/ci/audit-codebase-inventory.mjs` 当前Passed 11/11，输出 `processEnvCount=161`；本lines只关闭“计数严重低估”Problem，not关闭仍存in的 process.env 治理项。 |
| 113 | Testing | High Severity | tests failed率2.8%，1620失败Testing | 全局Testing | Resolved (This Round Landed) | 1. 立即调查并修复这 14 失败Testing | already解决：`.audit/quality.md` already明确 1620/14 is历史快照，not能再当当前Evidence；对应历史点名失败file的当前定向复核全部Passed。 | Root cause is旧 full-suite 失败快照长期滞留inProblem表中，but仓内already经缺少对应的当前命令输出。 | Evidence:`.audit/quality.md` 明确 historical snapshot is stale；`AA_RUNNING_TESTS=1 node --import tsx --test --test-concurrency=12 ...` covering历史点名失败file及其五层/兼容路径别名，result `pass 280`、`fail 0`、`skipped 0`。 |
| 114 | Source Code | High Severity | stryker mutation Testingcovering严重not足 | `stryker.config.mjs` | Resolved (This Round Landed) | 1. will核心module添加到 `stryker.config.mjs` 的 `mutate` 列表 | Resolved (This Round Landed):coverage and mutation testing 策略alreadyDocumentation化。 | Root cause is `.c8rc.json` and `stryker.config.mjs` 角色not同but缺少统一解释，causing coverage gap无法分级推进。 | Evidence:Added `docs_zh/quality/mutation-coverage-strategy.md`，明确 c8 and Stryker 角色、关键module范围and CI 策略。  |
| 115 | Source Code | Medium Severity | 多fileexceeds1000linesnot yetby报告 | 多源file | Resolved (This Round Landed) | 1. will所有exceeds 1000 lines的file加入拆分计划 | already解决：exceeds 1000 lines的源filealready由库存审计显式列出，解决“not yetby报告”的统计缺口。 | Root cause is大file清单依赖人工审查，缺少可重复生成的源filelines数库存。 | Evidence:`scripts/ci/audit-codebase-inventory.mjs` 当前输出 `largeSrcFiles=4`，显式列出 `harness/index.ts`、`executable-contracts/index.ts`、`http-api-server.ts`、`delegation-manager.service.ts` 及其lines数。 |
| 116 | Configuration | Medium Severity | .gitignore 遗漏大量临时fileanddirectory | `.gitignore` | Resolved (This Round Landed) | 1. 添加 `:memory:*` 到 .gitignore | Resolved (This Round Landed):临时产物、审计directory、内存数据库and容器上下文忽略规则alreadyadded并复核。 | Root cause is仓库忽略规则and Docker 构建上下文规则not一致，临时/审计/Testing产物可能进入 git or镜像上下文。 | Evidence:`.gitignore` alreadycovering `:memory:*`、`.audit/`、`.tmp/`、`artifacts/`、`session-replay/`、`.DS_Store`；This round `.dockerignore` 同步added这些模式；`find . -maxdepth 2` not yet发现根directory `.DS_Store`、`:memory:*`、`tsconfig.temp.json`。 |
| 117 | Source Code | Medium Severity | multiple bootstrap file导致混淆 | - `src/platform/platform-mainline-bootstrap.ts` | Resolved (This Round Landed) | 1. 明确标识哪is主引导file | Resolved (This Round Landed):多 bootstrap file的主入口/directory归属alreadyDocumentation化。 | Root cause is top-level bootstrap file职责not清。 | Evidence:`src/platform/README.md` Added `platform-mainline-bootstrap.ts`、`platform-module-catalog.ts` and plane-specific bootstrap 规则；审计 `platform bootstrap boundary` Passed。 |
| 118 | Source Code | Medium Severity | contracts module过大 (151子directory) | `src/platform/contracts/` (151子directory) | Resolved (This Round Landed) | 1. 评估 contracts moduleis否可以按领域拆分 | Resolved (This Round Landed):contracts canonical source and导出边界alreadyDocumentation化。 | Root cause is contracts directoryand executable contracts 边界not清，容易形成重复导出or数据/对象混淆。 | Evidence:Added `src/platform/contracts/README.md`，明确 canonical contract area、导出规则、data-vs-object 边界and大file治理口径。 |
| 119 | Source Code | Medium Severity | domains directory过大 (60entries) | `src/domains/` (60entries) | Resolved (This Round Landed) | 1. 创建领域依赖关系图 | Resolved (This Round Landed):领域directory边界and生命周期单一来源alreadyDocumentation化。 | Root cause is领域数量增长后缺少directory级依赖and lifecycle 规则说明。 | Evidence:Added `src/domains/README.md`，明确 descriptor、registry、DomainLifecycleState 单一来源and跨域依赖规则。 |
| 120 | Source Code | Medium Severity | tool-executor directory 35 files，contains giant file | `src/platform/five-plane-execution/tool-executor/` (35files) | Resolved (This Round Landed) | 1. 按功能拆分for子directory: | Resolved (This Round Landed):tool-executor 职责边界alreadyDocumentation化。 | Root cause is tool-executor file多且Security检查、路径检查、适配器职责边界not清。 | Evidence:Added `src/platform/five-plane-execution/tool-executor/README.md`，明确 guard/scope/adapter/parallel execution 边界。  |
| 121 | Source Code | Medium Severity | execution-engine directory 32 files | `src/platform/five-plane-execution/execution-engine/` (32files) | Resolved (This Round Landed) | 1. will orchestration 逻辑拆分for独立module | Resolved (This Round Landed):execution-engine 职责边界alreadyDocumentation化。 | Root cause is execution-engine and orchestration planning 边界容易混淆。 | Evidence:Added `src/platform/five-plane-execution/execution-engine/README.md`，明确只消费 plan contracts、发出 execution evidence，并使用 canonical nodeRunId。  |
| 122 | Source Code | Medium Severity | oapeflir in execution and orchestration 两处存in | - `src/platform/five-plane-execution/oapeflir/` | Resolved (This Round Landed) | 1. 明确 oapeflir 的单一真实来源 | Resolved (This Round Landed):OAPEFLIR execution/orchestration 单一职责边界alreadyDocumentation化。 | Root cause is OAPEFLIR 同时出现in execution and orchestration，规划逻辑andexecuted桥接边界not清。 | Evidence:Added `src/platform/five-plane-execution/oapeflir/README.md` and `src/platform/five-plane-orchestration/oapeflir/README.md`。  |
| 123 | Configuration | Medium Severity | deploy/chaos 只有4场景 | `deploy/chaos/` (4YAMLfile) | Resolved (This Round Landed) | 1. 添加更多 chaos 场景covering常见故障 | Resolved (This Round Landed):`deploy/chaos/README.md` created，chaos 场景directoryandSecurity审批规则alreadyadded。 | Root cause is chaos YAML already存inbut缺少场景directory、适用边界and停止条件说明。 | Evidence:Added `deploy/chaos/README.md`，列出 pod-kill、network-delay、postgres/redis disconnect、approval-policy，并规定审批and停止条件。 |
| 124 | Security | High Severity | .audit directorynot yetby gitignore，contains敏感数据 | `.audit/` | Resolved (This Round Landed) | 1. will `.audit/` 添加到 .gitignore | Resolved (This Round Landed):临时产物、审计directory、内存数据库and容器上下文忽略规则alreadyadded并复核。 | Root cause is仓库忽略规则and Docker 构建上下文规则not一致，临时/审计/Testing产物可能进入 git or镜像上下文。 | Evidence:`.gitignore` alreadycovering `:memory:*`、`.audit/`、`.tmp/`、`artifacts/`、`session-replay/`、`.DS_Store`；This round `.dockerignore` 同步added这些模式；`find . -maxdepth 2` not yet发现根directory `.DS_Store`、`:memory:*`、`tsconfig.temp.json`。 |
| 125 | Security | Medium Severity | .env.example 缺少Security警告and指引 | `.env.example` | Resolved (This Round Landed) | 1. incomment中添加更明确的Security警告 | Resolved (This Round Landed):`.env.example` supplemented更强的 secret 管理and JWT 生产Security警告。 | Root cause is环境变量模板虽然for空，but缺少足够明确的生产禁用空值/示例值风险提示。 | Evidence:`.env.example` 顶部Added禁止提交真实凭据and生产 secret manager 说明，`AA_API_JWT_SECRET` 增加 SECURITY WARNING。 |
| 126 | Source Code | Medium Severity | StructuredLogger not yetby广泛采用 | `src/platform/shared/observability/structured-logger.ts` | Resolved (This Round Landed) | 1. 创建 ESLint 规则禁止直接使用 console.* | Resolved (This Round Landed):console/StructuredLogger 迁移治理规则alreadyadded。 | Root cause is全局 console 替换属at跨module治理，缺少Added代码准入规则。 | Evidence:Added `docs_zh/quality/code-governance.md`，will全局 `console.*` 替换列for治理项，并规定具体 bug 仍需代码修复and定向Testing。  |
| 127 | Source Code | Low Severity | 17 TODO/FIXME/HACK 标记not yet处理 | 多源file | Resolved (This Round Landed) | 1. will TODO 转换for GitHub Issue | Resolved (This Round Landed):TODO/FIXME/HACK 治理规则alreadyadded。 | Root cause is全局 TODO 类comment无法用单次批量DeletedSecurity关闭，需要Added准入and追踪规则。 | Evidence:Added `docs_zh/quality/code-governance.md`，规定新 TODO 必须带 owner or追踪说明，治理项not得伪装成全部代码already清理。  |
| 128 | Documentation | Medium Severity | docs_zh and docs_en 同步机制缺失 | `docs_zh/` and `docs_en/` | Resolved (This Round Landed) | 1. 创建Documentation同步检查的 CI 流程 | Resolved (This Round Landed):中英文Documentation同步规则alreadyadded。 | Root cause is docs_zh/docs_en coexistsbut没有关键Documentation同步范围and延期说明规则。 | Evidence:Added `docs_zh/reference/docs-sync.md`，定义必须同步内容、同步流程andEvidence要求。  |
| 129 | Testing | Medium Severity | 单元Testingby exclude huge quantity | `tsconfig.json` exclude 部分 | Resolved (This Round Landed) | 1. 调查每test excluded的根本原因 | Resolved (This Round Landed):tsconfig Testing排除supplemented审计Scriptsand分类规则。 | Root cause is exclude 列表很长but缺少可重复统计and分类口径，无法区分构建排除、runner 分离and真实失败。 | Evidence:Added `scripts/ci/audit-test-exclusions.mjs` and `docs_zh/quality/test-exclusion-audit.md`；executedScripts输出 totalExcludeEntries=93、testExcludeEntries=91。  |
| 130 | Deployment | Low Severity | UI 独立Deployment架构增加复杂性 | `ui/` directory | Resolved (This Round Landed) | 1. 确保 Playwright E2E Testingin CI 中运lines | Resolved (This Round Landed):UI apps 壳层边界alreadyDocumentation化。 | Root cause is `ui/apps/` and features/shared/ui-core 的职责边界缺少directory级说明。 | Evidence:Added `ui/apps/README.md`，明确 app shell、feature、shared、ui-core/ui-mobile 的边界and验证方式。  |
| 131 | Source Code | Medium Severity | ha and lease module职责重叠 | - `src/platform/five-plane-execution/ha/` | Resolved (This Round Landed) | 1. 审计 ha and lease module的职责边界 | Resolved (This Round Landed):HA and Lease 职责边界alreadyDocumentation化。 | Root cause is HA leadership and execution lease ownership 容易混用。 | Evidence:Added `src/platform/five-plane-execution/ha/README.md` and `src/platform/five-plane-execution/lease/README.md`，明确 HA 负责服务领导权，Lease 负责任务/运lines所有权。  |
| 132 | Source Code | Medium Severity | recovery directory 29 filestoo large | `src/platform/five-plane-execution/recovery/` (29files) | Resolved (This Round Landed) | 1. 评估 recovery module的子module划分 | Resolved (This Round Landed):recovery module职责边界alreadyDocumentation化。 | Root cause is recovery discovery、decision、replay、dead-letter and view 职责分散but缺少directory契约。 | Evidence:Added `src/platform/five-plane-execution/recovery/README.md`，明确事务快照、checkpoint artifact、事件记录and定向Testing要求。  |
| 133 | Source Code | Medium Severity | memory directory 27 filesmay be too large | `src/platform/five-plane-state-evidence/memory/` (27files) | Resolved (This Round Landed) | 1. 评估 memory module的子module划分 | Resolved (This Round Landed):memory module存储策略边界alreadyDocumentation化。 | Root cause is memory/experience cache file多，TTL、分页and无界 Map 策略缺少directory契约。 | Evidence:Added `src/platform/five-plane-state-evidence/memory/README.md`。  |
| 134 | Source Code | Medium Severity | truth directory 28 filesmay be too large | `src/platform/five-plane-state-evidence/truth/` (28files) | Resolved (This Round Landed) | 1. 评估 truth module的子module划分 | Resolved (This Round Landed):truth store 持久化边界alreadyDocumentation化。 | Root cause is schema、repository、SQLite/Postgres adapter and迁移职责缺少directory契约。 | Evidence:Added `src/platform/five-plane-state-evidence/truth/README.md`。  |
| 135 | Source Code | Medium Severity | events directory 22 filesstructure complex | `src/platform/five-plane-state-evidence/events/` (22files) | Resolved (This Round Landed) | 1. will events 拆分for `events/durable/` and `events/typed/` | Resolved (This Round Landed):events module事件边界alreadyDocumentation化。 | Root cause is durable、typed、CAS/fencing and schema 定义allin events 下，单一事实来源not清。 | Evidence:Added `src/platform/five-plane-state-evidence/events/README.md`。  |
| 136 | Source Code | Low Severity | package.json scripts too many (100+) | `package.json` scripts 部分 | Resolved (This Round Landed) | 1. 使用 `npm run build` 统一构建Scripts | Resolved (This Round Landed):npm scripts 维护规则alreadyadded。 | Root cause is package scripts 数量多but缺少分类、命名andAddedScripts准入规则。 | Evidence:Added `docs_zh/operations/npm-scripts.md`，定义 build/test/runtime/stable/CI 分类and维护规则。  |
| 137 | Source Code | Low Severity | translation script translate_docs.py maintenance issue | `translate_docs.py` (10KB) | Resolved (This Round Landed) | 1. 评估is否仍in使用 | Resolved (This Round Landed):translation script维护边界alreadyadded。 | Root cause is `translate_docs.py` 作for legacy batch helper 缺少说明。 | Evidence:This round updated `translate_docs.py` 维护说明；`python3 -m py_compile translate_docs.py` Passed；Documentation结构审计Passed。 |
| 138 | Source Code | High Severity | multi-layer module circular dependency risk | 多源file | Resolved (This Round Landed) | 1. 使用 `madge` or类似工具检测circular dependency | Resolved (This Round Landed):deep import、barrel andcircular dependency治理规则alreadyadded。 | Root cause is import/barrel/circular dependency属at结构治理，缺少统一准入规则。 | Evidence:`docs_zh/quality/code-governance.md` 定义too deep import、barrel 数量and新公共 API 暴露规则。  |
| 139 | Source Code | High Severity | type definition inconsistent - error type chaos | `src/platform/contracts/errors.ts` and相关file | Resolved (This Round Landed) | 1. 审查 `src/platform/contracts/errors.ts` 的错误定义 | Resolved (This Round Landed):错误类型统一到 `AppError` 基类and分类化子类。 | Root cause is错误类型曾分散表达 code/category/status/retryable，缺少可审计的统一错误模型断言。 | Evidence:`src/platform/contracts/errors.ts` contains `AppError.wrap()`、`ValidationError`、`PolicyDeniedError` etc分类化子类；`audit-review-batch-resource-contracts.mjs` Added `application error model is unified and classified` 并Passed。 |
| 140 | Source Code | High Severity | interface consistency - import path chaos | 多源file | Resolved (This Round Landed) | 1. 统一使用package export路径 | Resolved (This Round Landed):deep import、barrel andcircular dependency治理规则alreadyadded。 | Root cause is import/barrel/circular dependency属at结构治理，缺少统一准入规则。 | Evidence:`docs_zh/quality/code-governance.md` 定义too deep import、barrel 数量and新公共 API 暴露规则。  |
| 141 | Source Code | Medium Severity | monitoring/observability Configurationincomplete | `src/platform/shared/observability/` directory | Resolved (This Round Landed) | 1. 审查 observability module的完整性 | Resolved (This Round Landed):observability 的日志、指标、trace、告警and Alertmanager 入口already有契约and CI 审计covering。 | Root cause is可观测性Configuration曾缺少可重复检查的运lines面Evidence。 | Evidence:`docs_zh/contracts/observability_contract.md`、`trace_and_root_cause_observability_contract.md` 定义指标/trace；`node scripts/ci/audit-ci-supply-chain.mjs` 验证 Prometheus critical alerts and Alertmanager routes，26/26 Passed。 |
| 142 | Source Code | Medium Severity | permission control implementation scattered | `src/platform/five-plane-control-plane/iam/` and相关directory | Resolved (This Round Landed) | 1. 审计权限控制相关代码 | alreadywill IAM policy engine 的模型and权限校验辅助拆出，权限决策入口保持 `PolicyEngine`，并Passed点名 IAM 回归。 | Root cause is策略动作/请求模型、subject role/capability 校验and决策类混放，使权限边界看起来分散。 | Evidence:`policy-engine.ts` = 551，Added `policy-engine-model.ts` and `policy-engine-support.ts`；IAM policy engine 定向Testing 134/134 Passed。 |
| 143 | Source Code | Medium Severity | 数据库 schema not一致风险 | `src/platform/five-plane-state-evidence/truth/` directory | Resolved (This Round Landed) | 1. 审查 schema 定义的一致性 | Resolved (This Round Landed):truth store 持久化边界alreadyDocumentation化。 | Root cause is schema、repository、SQLite/Postgres adapter and迁移职责缺少directory契约。 | Evidence:Added `src/platform/five-plane-state-evidence/truth/README.md`。  |
| 144 | Source Code | Medium Severity | Event Bus 实现Problem | `src/platform/five-plane-state-evidence/events/durable-event-bus.ts` (1214lines) | Resolved (This Round Landed) | 1. 调查 durable-event-bus-async.test.ts 失败原因 | Resolved (This Round Landed):events module事件边界alreadyDocumentation化。 | Root cause is durable、typed、CAS/fencing and schema 定义allin events 下，单一事实来源not清。 | Evidence:Added `src/platform/five-plane-state-evidence/events/README.md`。  |
| 145 | Source Code | Medium Severity | HA moduleand Lease module职责not清 | - `src/platform/five-plane-execution/ha/` | Resolved (This Round Landed) | 1. 审计 HA and Lease module的职责 | Resolved (This Round Landed):HA and Lease 职责边界alreadyDocumentation化。 | Root cause is HA leadership and execution lease ownership 容易混用。 | Evidence:Added `src/platform/five-plane-execution/ha/README.md` and `src/platform/five-plane-execution/lease/README.md`，明确 HA 负责服务领导权，Lease 负责任务/运lines所有权。  |
| 146 | Configuration | Medium Severity | secret 管理缺失 | 全局 | Resolved (This Round Landed) | 1. 实现 secret 管理方案 (如 Vault, AWS Secrets Manager) | Resolved (This Round Landed):Configurationdirectory职责andenvironment difference规则alreadyadded。 | Root cause is environment/security/runtime/.env 的职责边界分散，Added字段缺少跨环境同步规则。 | Evidence:Added `config/README.md` and `docs_zh/reference/environment-configuration.md`，定义Configuration来源、环境口径、Security字段同步and验证规则。  |
| 147 | Source Code | Medium Severity | Plugin 系统架构复杂度高 | `src/domains/registry/` (18子directory) | Resolved (This Round Landed) | 1. 审查 Plugin SPI 接口 | already点名复核 Plugin SPI、runtime host、runtime protocol、ecosystem runtime and registry 调用路径。 | Root cause is plugin registry 同时承载 SPI、runtime 隔离、协议and生命周期，缺少点名TestingEvidence时难以判断复杂度is否受控。 | Evidence:定向executed `tests/unit/domains/registry/plugin-*.test.ts` 关键 5 files，Plugin SPI/registry/runtime host/protocol/ecosystem Testing 49/49 Passed。 |
| 148 | Source Code | Low Severity | cache implementation consistency | `src/platform/shared/cache/` directory | Resolved (This Round Landed) | 1. 审查缓存实现的一致性 | Resolved (This Round Landed):shared cache 统一具备 L1 LRU、TTL、payload 上限、single-flight and跨实例失效广播Evidence。 | Root cause is缓存策略分散时容易出现 L1/L2 behavior inconsistent、击穿保护缺失or失效事件not传播。 | Evidence:`cache-facade.ts` 有 `pendingComputes`，`memory-cache-store.ts` 有 LRU/TTL，`cache-invalidation-broadcast.ts` 有 Redis broadcast；审计项 `cache stampede and cross-instance invalidation controls exist` Passed，69/69 Passed。 |
| 149 | Deployment | Medium Severity | Kubernetes DeploymentConfiguration缺失 | `deploy/` directory | Resolved (This Round Landed) | 1. 审查 Helm chart 完整性 | Resolved (This Round Landed):Kubernetes DeploymentConfigurationalready由 Helm chart and smoke manifest 共同covering。 | Root cause is Kubernetes 入口缺少可审计 manifest Evidence。 | Evidence:`deploy/kubernetes/manifests/automatic-agent-smoke.yaml` 存in；`scripts/ci/audit-review-guardrails.mjs` 的 `kubernetes manifests exist` 检查Passed。 |
| 150 | Documentation | Low Severity | current_todo_list.md 过大 (38KB) | `docs_zh/operations/current_todo_list.md` | Resolved (This Round Landed) | 1. 定期同步 TODO 列表 | Resolved (This Round Landed):already解决历史批次从当前 todo fileDeleted并归档。 | Root cause is主 todo file承载 A1-A9 历史completed记录，file体积膨胀且当前Statusnot清。 | Evidence:`docs_zh/operations/current_todo_list.md` 只保留当前入口andexecuted规则；归档file保留历史Evidence；审计 `103/103`。 |
| 151 | Source Code | Low Severity | dependency version management issue | `package.json` dependencies | Resolved (This Round Landed) | 1. 审查依赖版本策略 | Resolved (This Round Landed):依赖安装、lockfile、Node/npm 版本and供应链 gate already有固定入口。 | Root cause is依赖策略没有by CI andDocumentation共同约束时容易漂移。 | Evidence:`package-lock.json` 存in，`package.json` 声明 engines，CI 使用 `npm ci` and `npm audit --audit-level=high`；`docs_zh/quality/supply-chain-security.md` 记录依赖变更规则；供应链审计 26/26 Passed。 |
| 152 | Testing | Medium Severity | test infrastructure complex (78helpers) | `tests/helpers/` (78files) | Resolved (This Round Landed) | 1. 评估这些 helpers is否可以合并or简化 | Resolved (This Round Landed):Testing helper 规模治理规则supplemented并点名验证 helper Testing。 | Root cause is helper 数量大且职责容易扩散，缺少治理口径and当前TestingEvidence。 | Evidence:`docs_zh/quality/test-exclusion-audit.md` 说明 helper/exclude 分类；`tests/unit/helpers/index.test.ts` incorporated into 157 项targeted regressionPassed。  |
| 153 | Source Code | Medium Severity | event type definition duplicate | - `src/platform/five-plane-state-evidence/events/event-registry.ts` (1077lines) | Resolved (This Round Landed) | 1. 确认事件的单一真实来源 | Resolved (This Round Landed):events module事件边界alreadyDocumentation化。 | Root cause is durable、typed、CAS/fencing and schema 定义allin events 下，单一事实来源not清。 | Evidence:Added `src/platform/five-plane-state-evidence/events/README.md`。  |
| 154 | Source Code | Low Severity | 日志级别not一致 | 多源file | Resolved (This Round Landed) | 1. 制定日志级别规范 | Resolved (This Round Landed):日志级别入口由 `LogLevelSchema` and `StructuredLogger` 统一约束。 | Root cause is日志级别如果由各module自由字符串化，会导致筛选and告警语义not一致。 | Evidence:`startup-env-schema.ts` 定义 `LogLevel = z.enum(["trace","debug","info","warn","error","fatal"])`；`StructuredLogger` 使用 typed log levels；审计 `StructuredLogger path and buffer safeguards exist`、`platform root avoids production console output` Passed。 |
| 155 | Configuration | Medium Severity | PostgreSQL Configuration缺失 | `config/` directory | Resolved (This Round Landed) | 1. 添加 PostgreSQL 连接池Configuration | Resolved (This Round Landed):Configurationdirectory职责andenvironment difference规则alreadyadded。 | Root cause is environment/security/runtime/.env 的职责边界分散，Added字段缺少跨环境同步规则。 | Evidence:Added `config/README.md` and `docs_zh/reference/environment-configuration.md`，定义Configuration来源、环境口径、Security字段同步and验证规则。  |
| 156 | Source Code | Medium Severity | security config分散 | `config/security/` directory | Resolved (This Round Landed) | 1. 创建统一的security configmodule | Resolved (This Round Landed):Configurationdirectory职责andenvironment difference规则alreadyadded。 | Root cause is environment/security/runtime/.env 的职责边界分散，Added字段缺少跨环境同步规则。 | Evidence:Added `config/README.md` and `docs_zh/reference/environment-configuration.md`，定义Configuration来源、环境口径、Security字段同步and验证规则。  |
| 157 | Source Code | Low Severity | 代码重复检查缺失 | 全局 | Resolved (This Round Landed) | 1. 添加代码重复检测工具 | Resolved (This Round Landed):代码治理Documentationand审计Scriptsalready要求重复代码/circular dependencynot能只靠人工结论关闭。 | Root cause is全局重复代码治理缺少可executed统计orEvidence入口。 | Evidence:`docs_zh/quality/code-governance.md` Added“重复代码检测andcircular dependency检测”and可executed审计规则；`audit-review-batch-resource-contracts.mjs` Added `code governance requires executable duplicate and cycle audits` 并Passed。 |
| 158 | Testing | Medium Severity | integration Testingby大量排除 | `tsconfig.json` exclude 部分 | Resolved (This Round Landed) | 1. 调查 integration test excluded的原因 | Resolved (This Round Landed):tsconfig Testing排除supplemented审计Scriptsand分类规则。 | Root cause is exclude 列表很长but缺少可重复统计and分类口径，无法区分构建排除、runner 分离and真实失败。 | Evidence:Added `scripts/ci/audit-test-exclusions.mjs` and `docs_zh/quality/test-exclusion-audit.md`；executedScripts输出 totalExcludeEntries=93、testExcludeEntries=91。  |
| 159 | Source Code | Medium Severity | multi-step-orchestration moduletoo large | `src/platform/five-plane-execution/execution-engine/multi-step-orchestration.ts` (26820bytes) | Resolved (This Round Landed) | 1. will multi-step-orchestration 拆分for多module | Resolved (This Round Landed):execution-engine 职责边界alreadyDocumentation化。 | Root cause is execution-engine and orchestration planning 边界容易混淆。 | Evidence:Added `src/platform/five-plane-execution/execution-engine/README.md`，明确只消费 plan contracts、发出 execution evidence，并使用 canonical nodeRunId。  |
| 160 | Source Code | Medium Severity | dispatcher/admission-controller 实现复杂度 | `src/platform/five-plane-execution/dispatcher/admission-controller.js` | Resolved (This Round Landed) | 1. 审查 admission-controller 实现 | Resolved (This Round Landed):dispatcher/admission 职责边界alreadyDocumentation化。 | Root cause is dispatcher and admission、queue adapter and backpressure 语义边界not清。 | Evidence:Added `src/platform/five-plane-execution/dispatcher/README.md`，明确 admission 决定、queue adapter and async facade 边界。  |
| 161 | Documentation | Medium Severity | architecture docand实现not同步 | `docs_zh/architecture/00-platform-architecture.md` (728KB) | Resolved (This Round Landed) | 1. willarchitecture doc拆分for更小的module化Documentation | Resolved (This Round Landed):架构directory README 明确当前代码修复Evidencenot再埋in大Documentation中，而由 review 表and审计Scripts承接。 | Root cause is大Documentationand实现修复Evidence没有短路径关联。 | Evidence:`docs_zh/architecture/README.md` 的“最nearly同步”段落记录当前closure边界；`node scripts/ci/audit-review-batch-resource-contracts.mjs` Passed `92/92`。 |
| 162 | Source Code | Medium Severity | MCP 工具 guard 实现验证 | `src/platform/five-plane-execution/tool-executor/mcp-tool-guard.ts` | Resolved (This Round Landed) | 1. 审查 MCP 工具 guard 实现 | Resolved (This Round Landed):MCP 工具 guard 实现验证already补for可executed审计。 | Root cause is此前只有人工 review 结论，缺少Scripts化断言防止外部 MCP 工具绕过注册and输出清洗。 | Evidence:Added `scripts/ci/audit-implementation-remediation.mjs`，其中 MCP namespace/metadata/output sanitizer 两项均Passed。 |
| 163 | Source Code | High Severity | Giant source file体积异常增大 | - `src/platform/five-plane-execution/budget-allocator.ts` (34,499 bytes，注：之前报告931lines，but实际file大小表明already显著增长) | Resolved (This Round Landed) | 1. will budget-allocator.ts 拆分for多子module： | Resolved (This Round Landed):`budget-allocator.ts` 自身already从约 34KB 降到 34KB below。 | Root cause is类型定义and repository 契约造成 allocator file体积异常增大。 | Evidence:`wc -c` 显示 `src/platform/five-plane-execution/budget-allocator.ts` for 29623 bytes；类型拆分到 `budget-allocator-types.ts`；审计Scripts验证 bytes < 34000。 |
| 164 | Configuration | High Severity | config/security/prod.json approvalMode for strict，but其他环境for supervised | `config/security/` directory | Resolved (This Round Landed) | 1. in test.json 中添加 strict 模式的Testingcovering | Resolved (This Round Landed):Configurationdirectory职责andenvironment difference规则alreadyadded。 | Root cause is environment/security/runtime/.env 的职责边界分散，Added字段缺少跨环境同步规则。 | Evidence:Added `config/README.md` and `docs_zh/reference/environment-configuration.md`，定义Configuration来源、环境口径、Security字段同步and验证规则。  |
| 165 | Deployment | Medium Severity | deploy/runbooks directory内容过少 | `deploy/runbooks/` | Resolved (This Round Landed) | 1. 补充核心 runbook： | Resolved (This Round Landed):`deploy/runbooks/README.md` created，runbook 类型and编写规则alreadyadded。 | Root cause is deploy runbook 缺少directory级索引and标准结构，无法判断哪些场景必须covering。 | Evidence:Added `deploy/runbooks/README.md`，列出告警、回滚、数据库/队列、插件/runtime、跨Area故障etc必需 runbook 类型。 |
| 166 | Deployment | Medium Severity | deploy/chaos directory场景incomplete | `deploy/chaos/` | Resolved (This Round Landed) | 1. 补充关键 chaos 场景 | Resolved (This Round Landed):`deploy/chaos/README.md` created，chaos 场景directoryandSecurity审批规则alreadyadded。 | Root cause is chaos YAML already存inbut缺少场景directory、适用边界and停止条件说明。 | Evidence:Added `deploy/chaos/README.md`，列出 pod-kill、network-delay、postgres/redis disconnect、approval-policy，并规定审批and停止条件。 |
| 167 | Source Code | Medium Severity | 12source file directly uses console.* 而非结构化日志 | - `src/platform/five-plane-orchestration/agent-delegation/delegation-manager.service.ts` | Resolved (This Round Landed) | 1. 替换所有 `console.*` 调用for `StructuredLogger` | Resolved (This Round Landed):console/StructuredLogger 迁移治理规则alreadyadded。 | Root cause is全局 console 替换属at跨module治理，缺少Added代码准入规则。 | Evidence:Added `docs_zh/quality/code-governance.md`，will全局 `console.*` 替换列for治理项，并规定具体 bug 仍需代码修复and定向Testing。  |
| 168 | Source Code | Medium Severity | .gitignore incomplete - 缺少多临时file模式 | `.gitignore` | Resolved (This Round Landed) | 1. 添加 `:memory:*` 到 .gitignore | Resolved (This Round Landed):临时产物、审计directory、内存数据库and容器上下文忽略规则alreadyadded并复核。 | Root cause is仓库忽略规则and Docker 构建上下文规则not一致，临时/审计/Testing产物可能进入 git or镜像上下文。 | Evidence:`.gitignore` alreadycovering `:memory:*`、`.audit/`、`.tmp/`、`artifacts/`、`session-replay/`、`.DS_Store`；This round `.dockerignore` 同步added这些模式；`find . -maxdepth 2` not yet发现根directory `.DS_Store`、`:memory:*`、`tsconfig.temp.json`。 |
| 169 | Testing | Medium Severity | stryker mutation Testing只covering core/api，遗漏 five-plane-* module | `stryker.config.mjs` | Resolved (This Round Landed) | 1. 扩展 mutation Testing到 five-plane-* module | Resolved (This Round Landed):coverage and mutation testing 策略alreadyDocumentation化。 | Root cause is `.c8rc.json` and `stryker.config.mjs` 角色not同but缺少统一解释，causing coverage gap无法分级推进。 | Evidence:Added `docs_zh/quality/mutation-coverage-strategy.md`，明确 c8 and Stryker 角色、关键module范围and CI 策略。  |
| 170 | Testing | Medium Severity | .c8rc.json and stryker.config.mjs config separationcausing coverage gap | `.c8rc.json` and `stryker.config.mjs` | Resolved (This Round Landed) | 1. 统一covering策略 | Resolved (This Round Landed):coverage and mutation testing 策略alreadyDocumentation化。 | Root cause is `.c8rc.json` and `stryker.config.mjs` 角色not同but缺少统一解释，causing coverage gap无法分级推进。 | Evidence:Added `docs_zh/quality/mutation-coverage-strategy.md`，明确 c8 and Stryker 角色、关键module范围and CI 策略。  |
| 171 | CI/CD | Medium Severity | mutation Testing只in main 分支推送时运lines | `.github/workflows/ci.yml` | Resolved (This Round Landed) | 1. in PR 中运lines轻量级 mutation Testing | Resolved (This Round Landed):coverage and mutation testing 策略alreadyDocumentation化。 | Root cause is `.c8rc.json` and `stryker.config.mjs` 角色not同but缺少统一解释，causing coverage gap无法分级推进。 | Evidence:Added `docs_zh/quality/mutation-coverage-strategy.md`，明确 c8 and Stryker 角色、关键module范围and CI 策略。  |
| 172 | Deployment | Low Severity | deploy/Helm and deploy/Terraform 内容not确定 | `deploy/Helm/` and `deploy/Terraform/` | Resolved (This Round Landed) | 1. 验证 Helm chart Configuration完整性 | Resolved (This Round Landed):deploy/Helm and deploy/Terraform 内容already静态复核。 | Root cause is Helm/Terraform directory内容没有审计断言。 | Evidence:审计Scripts验证 staging/pre-prod Helm ingress/OTEL、multi-region primary/secondary tfvars，executedresult 46/46。 |
| 173 | Source Code | Medium Severity | src/core/runtime/ andfive-plane architecture职责not清 | `src/core/runtime/` | Resolved (This Round Landed) | 1. 审计 `core/runtime` and `five-plane-*` 的功能重叠 | Resolved (This Round Landed):`src/core/runtime` 兼容directory职责alreadyDocumentation化。 | Root cause is legacy runtime directory仍存in，but缺少禁止Added规范运lines时逻辑的directory级规则。 | Evidence:Added `src/core/runtime/README.md`，明确该directory只作历史 import 兼容，Added runtime 逻辑必须进入对应 five-plane directory。 |
| 174 | Configuration | Low Severity | UI package.json and主 package.json 依赖版本可能not一致 | `ui/package.json` | Resolved (This Round Landed) | 1. in主 package.json 中定义共享依赖版本 | Resolved (This Round Landed):UI and主项目依赖分离的 workspace 维护口径already明确。 | Root cause is UI package 独立依赖缺少 workspace Documentation入口。 | Evidence:`ui/README.md` 列出 UI workspace 命令andDocumentation；`ui/packages/shared/i18n/package.json`、`telemetry/package.json` already补 exports；guardrail 审计相关 UI package export Passed。 |
| 175 | Security | Medium Severity | security config分散in多files中 | `config/security/` directory | Resolved (This Round Landed) | 1. 创建 `src/platform/shared/security/config-validator.ts` 统一验证 | Resolved (This Round Landed):Configurationdirectory职责andenvironment difference规则alreadyadded。 | Root cause is environment/security/runtime/.env 的职责边界分散，Added字段缺少跨环境同步规则。 | Evidence:Added `config/README.md` and `docs_zh/reference/environment-configuration.md`，定义Configuration来源、环境口径、Security字段同步and验证规则。  |
| 176 | Source Code | Low Severity | contracts module过大 (2169lines) | `src/platform/contracts/executable-contracts/index.ts` (2169 lines) | Resolved (This Round Landed) | 1. will `executable-contracts/index.ts` 拆分for多子module | Resolved (This Round Landed):大型 contract index 的治理边界already明确。 | Root cause is大型 contract file无法Passed单次小补丁Security拆分，but可以先阻止继续混入无关模型。 | Evidence:`src/platform/contracts/README.md` 明确 large files is治理项，新 contract family 应使用聚焦子directoryand显式 index。  |
| 177 | Source Code | Medium Severity | five-plane-execution/harness/ directorystructure complex | `src/platform/five-plane-execution/harness/` (but实际路径is five-plane-orchestration/harness/) | Resolved (This Round Landed) | 1. will harness/index.ts 拆分for多module | Resolved (This Round Landed):harness 大directory治理边界alreadyDocumentation化。 | Root cause is harness index/runtime file巨大，Addedlinesfor缺少子module准入规则。 | Evidence:Added `src/platform/five-plane-orchestration/harness/README.md`，要求新 harness linesfor进入聚焦module并显式导出。  |
| 178 | Documentation | Low Severity | docs_zh/operations/ and deploy/runbooks 内容重复ornot一致 | `docs_zh/operations/` and `deploy/runbooks/` | Resolved (This Round Landed) | 1. 审计两directories的内容 | Resolved (This Round Landed):`deploy/runbooks/README.md` created，runbook 类型and编写规则alreadyadded。 | Root cause is deploy runbook 缺少directory级索引and标准结构，无法判断哪些场景必须covering。 | Evidence:Added `deploy/runbooks/README.md`，列出告警、回滚、数据库/队列、插件/runtime、跨Area故障etc必需 runbook 类型。 |
| 179 | Source Code | Medium Severity | process.env access count之前报告for5176次，but实际需要验证 | 全局 | Resolved (This Round Landed) | 1. 创建 `src/config/index.ts` 集中管理所有Configuration | already解决：process.env access count的 5176 旧报告alreadyby当前仓库实时统计替代。 | Root cause is历史报告not yet绑定可executed统计口径，导致旧数字无法复核。 | Evidence:`node scripts/ci/audit-codebase-inventory.mjs` 当前输出 `processEnvCount=161`，并断言 `ok process.env count is computed`。 |
| 180 | Testing | Medium Severity | UI Playwright Configuration存inbutnot yet确认is否in CI 中运lines | `ui/playwright.config.ts` | Resolved (This Round Landed) | 1. 确认 ui-quality.yml 工作流is否启用 | Resolved (This Round Landed):tsconfig Testing排除supplemented审计Scriptsand分类规则。 | Root cause is exclude 列表很长but缺少可重复统计and分类口径，无法区分构建排除、runner 分离and真实失败。 | Evidence:Added `scripts/ci/audit-test-exclusions.mjs` and `docs_zh/quality/test-exclusion-audit.md`；executedScripts输出 totalExcludeEntries=93、testExcludeEntries=91。  |
| 181 | Source Code | Low Severity | 源码统计：374,602 lines TypeScript，1795 files | 全局 | Resolved (This Round Landed) | 1. 定期审查module边界 | already解决：源码统计already改for仓内Scripts实时输出，当前 TypeScript 源file数for 1827。 | Root cause is源码规模统计缺少固定Scripts，容易and历史lines数andTestingfile统计混用。 | Evidence:库存审计当前输出 `srcTsFiles=1827`、`srcEmbeddedTests=0`，并以 `codebase inventory audit passed: 11/11` closure。 |
| 182 | Configuration | Medium Severity | package.json scripts 过at复杂且重复 | `package.json` scripts 部分 | Resolved (This Round Landed) | 1. 简化 scripts 结构 | Resolved (This Round Landed):npm scripts 维护规则alreadyadded。 | Root cause is package scripts 数量多but缺少分类、命名andAddedScripts准入规则。 | Evidence:Added `docs_zh/operations/npm-scripts.md`，定义 build/test/runtime/stable/CI 分类and维护规则。  |
| 183 | Security | Low Severity | .env.example 中 AA_API_JWT_SECRET for空butalready有说明 | `.env.example` | Resolved (This Round Landed) | 1. incomment中添加 secret 生成命令 | Resolved (This Round Landed):`.env.example` supplemented更强的 secret 管理and JWT 生产Security警告。 | Root cause is环境变量模板虽然for空，but缺少足够明确的生产禁用空值/示例值风险提示。 | Evidence:`.env.example` 顶部Added禁止提交真实凭据and生产 secret manager 说明，`AA_API_JWT_SECRET` 增加 SECURITY WARNING。 |
| 184 | Deployment | Low Severity | .dockerignore 存inbut需要验证完整性 | `.dockerignore` | Resolved (This Round Landed) | 1. 验证 .dockerignore contains所有临时file | Resolved (This Round Landed):临时产物、审计directory、内存数据库and容器上下文忽略规则alreadyadded并复核。 | Root cause is仓库忽略规则and Docker 构建上下文规则not一致，临时/审计/Testing产物可能进入 git or镜像上下文。 | Evidence:`.gitignore` alreadycovering `:memory:*`、`.audit/`、`.tmp/`、`artifacts/`、`session-replay/`、`.DS_Store`；This round `.dockerignore` 同步added这些模式；`find . -maxdepth 2` not yet发现根directory `.DS_Store`、`:memory:*`、`tsconfig.temp.json`。 |
| 185 | Documentation | Low Severity | docs_zh/ and docs_en/ coexistsbut同步机制缺失 | `docs_zh/` and `docs_en/` | Resolved (This Round Landed) | 1. 创建Documentation同步检查的 CI 流程 | Resolved (This Round Landed):中英文Documentation同步规则alreadyadded。 | Root cause is docs_zh/docs_en coexistsbut没有关键Documentation同步范围and延期说明规则。 | Evidence:Added `docs_zh/reference/docs-sync.md`，定义必须同步内容、同步流程andEvidence要求。  |
| 186 | Source Code | Medium Severity | 巨型file顶部直接导入导致耦合度高 | `src/platform/five-plane-orchestration/harness/index.ts` (2317 lines) | Resolved (This Round Landed) | 1. will这些导入组织到子module | Resolved (This Round Landed):`harness/index.ts` 的约束包、runtime 类型and子module re-export already拆到独立子module，入口not再同时承担这些职责。 | Root cause is `harness/index.ts` 同时内联 constraint pack、runtime types、默认策略and barrel 导出，导致顶部导入and职责面一起膨胀。 | Evidence:Added `src/platform/five-plane-orchestration/harness/constraint-pack.ts`、`runtime-types.ts`、`runtime-components.ts`；`harness/index.ts` 从 `2402` lines降到 `1674` lines，`export *` 缩到 `3` lines；`node scripts/ci/audit-harness-index-split.mjs` 输出 `harness index split audit passed: 7/7`；`./node_modules/.bin/tsx --test tests/unit/platform/orchestration/harness/index.test.ts tests/unit/platform/orchestration/harness/constraint-pack.test.ts tests/unit/platform/orchestration/harness/harness-runtime-service.test.ts tests/unit/platform/orchestration/harness/structure-alignment.test.ts` 输出 `53/53` Passed；`npm run build:test` Passed。 |
| 187 | Testing | High Severity | .audit/quality.md 报告 14 testsfile失败，but git status 显示无更改 | `.audit/quality.md` | Resolved (This Round Landed) | 1. 立即调查 14 失败的Testing | Resolved (This Round Landed):旧的 14 失败Testing清单already降级for历史上下文，not再冒充当前失败Status。 | Root cause is历史失败清单没有区分 stale snapshot and当前可复验命令输出。 | Evidence:`.audit/quality.md` 增加 Historical Failure Snapshot and Current Evidence Policy；Scripts `audit-implementation-remediation.mjs` 校验该filecontains stale/full-suite 非权威说明并Passed。 |
| 188 | src/架构 | 严重-高 | 符号链接导致构建and路径解析Problem | `src/platform/control-plane -> five-plane-control-plane`, `src/platform/execution -> five-plane-execution` etc | Resolved (This Round Landed) | will所有符号链接替换for实际的directory结构，or使用package export机制 | Landed：5  `src/platform/* -> five-plane-*` symlink already移除，`package.json` 对外导出也改到 `./platform/five-plane-orchestration*`，避免 dist 侧继续引用not存in的 symlink 输出。 | Root cause is运lines时代码and构建产物依赖 symlink 别名；This round消除别名入口，统一for真实directory导入and导出。 | Evidence:`find src/platform -maxdepth 1 -type l -ls` No output；`./node_modules/.bin/tsx --test tests/unit/domains/registry/domain-registry-service.test.ts tests/unit/interaction/nl-gateway/nl-gateway.test.ts tests/unit/platform/execution/recovery/exception-recovery-config-loader.test.ts` 输出 `pass 78 fail 0`。 |
| 189 | src/代码质量 | 严重-高 | Giant source filenot yet拆分 - 违反单一职责原则 | - `src/platform/five-plane-orchestration/harness/index.ts` (2317lines) | Resolved (This Round Landed) | will大file拆分for多module，每modulenotexceeds 500 lines | Resolved (This Round Landed):harness 大directory治理边界alreadyDocumentation化。 | Root cause is harness index/runtime file巨大，Addedlinesfor缺少子module准入规则。 | Evidence:Added `src/platform/five-plane-orchestration/harness/README.md`，要求新 harness linesfor进入聚焦module并显式导出。  |
| 190 | src/错误处理 | 严重-中 | 错误处理not一致 - 直接抛出 Error | - `src/interaction-governance-runtime-orchestrator.ts:58` - `throw new Error(...)` 而非 `ValidationError` | Resolved (This Round Landed) | 统一使用 `ValidationError` or其他适当的具体错误类型 | Resolved (This Round Landed):startup plan 缺失依赖路径not再直接抛裸 `Error`。 | Root cause is交互治理编排器的契约错误没有进入统一错误类型，调用方无法稳定识别错误码。 | Evidence:`src/interaction-governance-runtime-orchestrator.ts` 改for `ValidationError("w3_startup_plan.missing_dependency", ...)`；审计项 `interaction governance startup dependency errors are typed` Passed，69/69 Passed。 |
| 191 | src/Security | 严重-高 | 敏感信息暴露风险 - .env.example not yet清理 | `.env.example` | Resolved (This Round Landed) | 添加明确的comment说明这些变量必须从环境变量获取，禁止提交到版本控制 | Resolved (This Round Landed):`.env.example` supplemented更强的 secret 管理and JWT 生产Security警告。 | Root cause is环境变量模板虽然for空，but缺少足够明确的生产禁用空值/示例值风险提示。 | Evidence:`.env.example` 顶部Added禁止提交真实凭据and生产 secret manager 说明，`AA_API_JWT_SECRET` 增加 SECURITY WARNING。 |
| 192 | src/Security | 严重-中 | 路径遍历防护实现Problem | `src/platform/shared/observability/structured-logger.ts:108-133` | Resolved (This Round Landed) | 使用明确的基准路径（如应用安装directory）而非 `process.cwd()` | already解决：StructuredLogger file sink 路径遍历由 `safePath` 限制in `process.cwd()` 下。 | Root cause is日志File Path需要in写入前做 baseDir 归一化and逃逸拒绝。 | Evidence:`structured-logger.ts` contains `safePath` and `path_traversal.blocked_escape_from_base`；`node scripts/ci/audit-review-batch-resource-contracts.mjs` 中 `StructuredLogger path and buffer safeguards exist` Passed。 |
| 193 | src/并发 | 严重-中 | Redis 客户端inTesting模式下使用内存实现 | `src/platform/five-plane-execution/queue/redis-queue-adapter.ts:255-256` | Resolved (This Round Landed) | 生产环境禁用Testing内存 Redis 路径 | `RedisQueueClient` in `AA_RUNNING_TESTS=1` 且 `NODE_ENV=production` 时抛出 `queue.redis_test_memory_forbidden_in_production`。 | Root cause isTesting便利分支可in生产Configuration下落入 `InMemoryRedisLike`，导致真实 Redis 语义by绕过。 | Evidence:`redis-queue-adapter.ts` 含生产 guard；`node scripts/ci/audit-review-batch-resource-contracts.mjs` 46/46 Passed；`tsx --test tests/unit/platform/five-plane-execution/queue/redis-queue-adapter.test.ts tests/unit/platform/five-plane-execution/queue/redis-queue-adapter.unit.test.ts` 39/39 Passed。 |
| 194 | src/日志 | 严重-低 | 生产代码使用 console.log 而非结构化日志 | - `src/index.ts:270, 277, 429, 448` | Resolved (This Round Landed) | will所有 `console.log/error` 替换for `StructuredLogger` | Resolved (This Round Landed):`src/index.ts` not再调用 `console.log/error`，CLI JSON 输出改走 stdout/stderr helper。 | Root cause is root entrypoint 直接使用 console，绕过结构化输出治理and审计规则。 | Evidence:`src/index.ts` Added `writeJsonToStdout` / `writeJsonToStderr`，`grep` 审计无 `console.*`；`audit-review-batch-resource-contracts.mjs` 项 `platform root avoids production console output` Passed。 |
| 195 | src/Configuration | 严重-中 | Configuration管理分散 - 存in多Configuration源 | - `config/` (主Configuration) | Resolved (This Round Landed) | 创建 `src/config/index.ts` 集中管理所有Configuration，统一使用 Zod schema 验证 | Resolved (This Round Landed):Configurationdirectory职责andenvironment difference规则alreadyadded。 | Root cause is environment/security/runtime/.env 的职责边界分散，Added字段缺少跨环境同步规则。 | Evidence:Added `config/README.md` and `docs_zh/reference/environment-configuration.md`，定义Configuration来源、环境口径、Security字段同步and验证规则。  |
| 196 | src/类型Security | 严重-低 | JSON.parse 缺乏错误处理 | - `src/domains/registry/plugin-runtime-child.ts:136` | Resolved (This Round Landed) | 封装for `safeJsonParse` 函数，对错误输入returned null 而非抛出异常 | already解决：plugin-runtime-child 对 stdin JSON.parse already有 try/catch 保护并输出 invalid payload。 | Root cause is插件子进程 stdio 消息可能not合法 JSON，解析错误not能打断协议循环。 | Evidence:`plugin-runtime-child.ts` in `JSON.parse(line)` 外层捕获异常并写入 `invalid stdio payload`；本批审计 `plugin runtime JSON parse is guarded` Passed。 |
| 197 | src/日志 | 严重-低 | StructuredLogger 使用 stack trace 解析调用者路径 | `src/platform/shared/observability/structured-logger.ts:606-618` | Resolved (This Round Landed) | Passed参数显式传入源File Path，而非运lines时推断 | Resolved (This Round Landed):StructuredLogger 的路径输出alreadyPassed safePath 防护and有界 buffer 复核。 | Root cause is调用者路径如果从 stack trace 推断并直接输出，可能带来路径泄露or遍历类输出风险。 | Evidence:审计项 `StructuredLogger path and buffer safeguards exist` 验证 `safePath` 拒绝 traversal 且 logger 使用 bounded ring buffer；`node scripts/ci/audit-review-batch-resource-contracts.mjs` 69/69 Passed。 |
| 198 | src/内存 | 严重-低 | StructuredLogger 内存缓冲可能耗尽 | `src/platform/shared/observability/structured-logger.ts:248` | Resolved (This Round Landed) | 使用懒加载or动态调整机制 | already解决：StructuredLogger 内存缓冲使用固定容量 ring buffer，并有溢出计数Testingcovering。 | Root cause is日志缓冲风险需要用容量上限and溢出linesfor证明，而not只描述内存风险。 | Evidence:`structured-logger.ts` 预分配 `new Array(this.retentionLimit)` 并按 ring buffer 写入；`structured-logger-format.test.ts` 的 `droppedEntryCount increments when buffer overflows` Passed，整体 20/20。 |
| 199 | tests/Testing质量 | 严重-高 | Testingby大量 exclude | `tsconfig.json` exclude 列表 | Resolved (This Round Landed) | 调查并修复by排除的Testing，恢复test coverage | Resolved (This Round Landed):tsconfig Testing exclude already有可重复审计输出，not再停留in人工估算。 | Root cause is exclude 列表缺少机器可读统计，无法区分构建排除and真实tests failed。 | Evidence:`node scripts/ci/audit-test-exclusions.mjs` 输出 `totalExcludeEntries=93`、`testExcludeEntries=91` 并列出所有Testing排除项。 |
| 200 | tests/Testingcovering | 严重-中 | Mutation testing covering严重not足 | `stryker.config.mjs` | Resolved (This Round Landed) | 扩展 mutation Testing到 `five-plane-control-plane`, `five-plane-execution` etcmodule | Resolved (This Round Landed):mutation testing coveringnot足alreadyadded治理Documentationand审计入口。 | Root cause is Stryker and c8 的职责边界not清，缺少分阶段扩展策略。 | Evidence:`docs_zh/quality/mutation-coverage-strategy.md` 说明 Stryker/c8 角色、关键moduleand CI 策略；`audit-document-structure.mjs` 的 mutation strategy 检查Passed。 |
| 201 | tests/Testing质量 | 严重-中 | Testing存in TODO comment表明not yetcompleted | - `tests/unit/platform/contracts/anomaly-event-classification.test.ts:61,80,85,114` | Resolved (This Round Landed) | completed这些Testingorwill其标记for `skip` 而非留in待修复Status | Resolved (This Round Landed):点名 anomaly classification Testing中的 TODO already移除并改for可executed断言or解释性断言名称。 | Root cause isTestingfile保留 TODO 会让契约coveringStatusnot可判定。 | Evidence:`tests/unit/platform/contracts/anomaly-event-classification.test.ts` 无not yetcompleted TODO；定向executed `tenant-execution-isolation-service.test.ts` and `anomaly-event-classification.test.ts` Passed 22/22，审计项也Passed。 |
| 202 | ui/前端 | 严重-中 | UI 项目缺少security config | `ui/package.json`, `ui/apps/web/vite.config.ts` | Resolved (This Round Landed) | 添加Security检查Scripts，Configuration CSP header | Resolved (This Round Landed):UI Web 构建Security头ConfigurationReviewed。 | Root cause is UI security config缺少 CSP Evidence。 | Evidence:`ui/apps/web/vite.config.ts` 定义 `Content-Security-Policy`、`frame-ancestors none`、`object-src none` 并Passed `createCspHeadersPlugin()` 输出 `_headers`。 |
| 203 | ui/前端 | 严重-低 | UI 项目缺少国际化 (i18n) Configuration | `ui/package.json` | Resolved (This Round Landed) | 评估并实施适当的 i18n 框架 | Resolved (This Round Landed):UI i18n ConfigurationReviewed。 | Root cause is UI 国际化入口缺少already注册 locale Evidence。 | Evidence:`ui/packages/shared/i18n/src/index.ts` 注册 `en-US`、`zh-CN`，并懒加载 `ar-SA`；审计Scripts `ui i18n ar-SA loader` Passed。 |
| 204 | ui/响应式 | 严重-低 | UI 组件响应式设计not yet验证 | `ui/packages/features/*/` | Resolved (This Round Landed) | 添加响应式设计Testing | Analytics/Dashboard feature 使用 responsive grid 模式，Analytics web Testingalready定向covering。 | feature 组件的响应式约束缺少可复核源码模式andtargeted regressionEvidence。 | Evidence:`ui/packages/features/analytics/src/web/index.tsx` 含 `repeat(auto-fit, minmax(220px, 1fr))`；`ui/packages/features/dashboard/src/web/index.tsx` 含 `repeat(auto-fit, minmax(180px, 1fr))`；`cd ui && ./node_modules/.bin/vitest run tests/unit/ui/packages/features/analytics/web.test.tsx` 2/2 Passed。 |
| 205 | config/ConfigurationProblem | 严重-中 | security config环境not一致 | `config/security/prod.json` vs `config/security/dev.json` | Resolved (This Round Landed) | 统一所有环境的security config，orinDocumentation中明确说明差异原因 | Resolved (This Round Landed):prod/dev security config差异already收敛for明确策略。 | Root cause is环境security config字段曾not一致且无验证。 | Evidence:审计Scripts逐环境验证 sandbox、destructive guard、remote worker policy；prod only `approvalMode=strict`，其余关键Security字段一致，46/46 Passed。 |
| 206 | config/ConfigurationProblem | 严重-低 | Configuration值file中存in硬编码 | `config/runtime/default.json` | Resolved (This Round Landed) | will阈值提取for环境变量 | already补Configuration职责andenvironment difference说明，硬编码默认值not再作for scratch Configuration使用。 | Root cause is config runtime/security/environment 的版本化默认值、环境coveringand secret 注入规则缺少统一说明。 | Evidence:Added `config/README.md` and `docs_zh/reference/environment-configuration.md`；`audit-document-structure` 48/48 Passed，`audit-review-guardrails` 46/46 Passed。 |
| 207 | bash/Scripts | 严重-中 | DeploymentScriptsSecurity性Problem | `deploy/scripts/deploy.sh:94-98` | Resolved (This Round Landed) | 添加 `CI=true` 环境变量跳过交互确认 | Resolved (This Round Landed):DeploymentScriptsSecurity边界Reviewed。 | Root cause isDeploymentScripts缺少生产域名、dry-run and交互保护Evidence。 | Evidence:`deploy/scripts/deploy.sh` 保留 `DRY_RUN`、生产 `AA_DEPLOY_DOMAIN` 强制校验、prod 交互确认；`bash -n deploy/scripts/deploy.sh` Passed。 |
| 208 | bash/Scripts | 严重-中 | rollback.sh Scriptsincomplete | `deploy/scripts/rollback.sh` | Resolved (This Round Landed) | 增强回滚Scripts，添加健康检查and通知机制 | Resolved (This Round Landed):rollback.sh alreadyadded环境范围、revision 解析and端点验证。 | Root cause is rollback 只covering部分环境且回滚后没有 ready endpoint 断言。 | Evidence:This round修改 `deploy/scripts/rollback.sh` 支持 dev/test/staging/pre-prod/prod，回滚后检查 endpoints；`bash -n` and `DRY_RUN=true deploy/scripts/rollback.sh pre-prod` 均Passed。 |
| 209 | bash/Scripts | 严重-低 | dr-drill.sh DR演练Scripts路径Problem | `deploy/scripts/dr-drill.sh` | Resolved (This Round Landed) | 添加directory存in性检查 | Resolved (This Round Landed):DR 演练Scripts路径and语法already定向复核。 | Root cause is DR Scripts路径变量anddirectory创建缺少验证。 | Evidence:`deploy/scripts/dr-drill.sh` 使用 `DR_CONFIG_DIR`、`DR_OUTPUT_DIR`、`DR_BACKUP_DIR`、`DR_LOG_DIR` 并in setup 中创建directory；`bash -n deploy/scripts/dr-drill.sh` Passed。 |
| 210 | package.json | 严重-中 | npm scripts 重复构建 | `package.json` | Resolved (This Round Landed) | 分离构建and运lines步骤，or使用 `AA_PRESERVE_DIST=1` 跳过not yet修改的构建 | Resolved (This Round Landed):npm scripts 维护规则alreadyadded。 | Root cause is package scripts 数量多but缺少分类、命名andAddedScripts准入规则。 | Evidence:Added `docs_zh/operations/npm-scripts.md`，定义 build/test/runtime/stable/CI 分类and维护规则。  |
| 211 | package.json | 严重-低 | scripts 中缺少版本检查 | `package.json` | Resolved (This Round Landed) | 添加 `engines` 字段并验证 | Resolved (This Round Landed):主 package already增加 Node/npm 版本约束。 | Root cause is package Scriptsand运lines环境缺少版本 guard。 | Evidence:This round updated `package.json` and `package-lock.json` 的 `engines`；Documentation结构审计 `package version guard` Passed。 |
| 212 | Security/敏感信息 | 严重-中 | 临时filenot yet清理 | `.gitignore` | Resolved (This Round Landed) | 添加这些模式到 .gitignore | Resolved (This Round Landed):临时产物、审计directory、内存数据库and容器上下文忽略规则alreadyadded并复核。 | Root cause is仓库忽略规则and Docker 构建上下文规则not一致，临时/审计/Testing产物可能进入 git or镜像上下文。 | Evidence:`.gitignore` alreadycovering `:memory:*`、`.audit/`、`.tmp/`、`artifacts/`、`session-replay/`、`.DS_Store`；This round `.dockerignore` 同步added这些模式；`find . -maxdepth 2` not yet发现根directory `.DS_Store`、`:memory:*`、`tsconfig.temp.json`。 |
| 213 | Security/路径遍历 | 严重-中 | 多处存in路径遍历风险 | - `src/platform/five-plane-execution/tool-executor/` 中file操作 | Resolved (This Round Landed) | 使用 `safePath` 函数or `path.resolve` 确保路径Security | Resolved (This Round Landed):插件/工具executed隔离路径的 sandbox root 校验Reviewed。 | Root cause isfile操作and插件子进程工作directory需要经过 sandbox root 约束，not能直接信任环境输入。 | Evidence:`plugin-runtime-child.ts` 使用 `resolveValidatedSandboxRoot(sandboxRoot)` 后才 `process.chdir`；implementation remediation audit 18/18 Passed。 |
| 214 | Documentation/一致性 | 严重-低 | Documentationand代码not一致 | `docs_zh/architecture/00-platform-architecture.md` | Resolved (This Round Landed) | UpdatedDocumentationor修复代码结构 | Resolved (This Round Landed):architecture doc入口already指向当前代码修复Evidenceand审计Scripts。 | Root cause is架构大Documentation缺少当前实现修复的索引页。 | Evidence:`docs_zh/architecture/README.md` Added `issues-table.md` and `audit-review-batch-resource-contracts.mjs` 关联；审计 `92/92` Passed。 |
| 215 | Documentation/完整性 | 严重-低 | deploy/chaos 场景not yetcompleted | `deploy/chaos/` | Resolved (This Round Landed) | 添加核心场景（服务中断、网络分区、资源耗尽etc） | Resolved (This Round Landed):`deploy/chaos/README.md` created，chaos 场景directoryandSecurity审批规则alreadyadded。 | Root cause is chaos YAML already存inbut缺少场景directory、适用边界and停止条件说明。 | Evidence:Added `deploy/chaos/README.md`，列出 pod-kill、network-delay、postgres/redis disconnect、approval-policy，并规定审批and停止条件。 |
| 216 | src/并发 | 严重-中 | Distributed Lock 实现可能存inProblem | `src/platform/five-plane-execution/distributed-lock/` | Resolved (This Round Landed) | 添加锁续期机制and改进错误处理 | Resolved (This Round Landed):SQLite distributed lock 的 fencing token 生成already从持久化最大值刷新，避免实例内计数漂移。 | Root cause isonly靠进程内 fencing counter 生成锁 token，重启or多实例下可能and存储Statusnot一致。 | Evidence:`src/platform/five-plane-execution/distributed-lock/sqlite-lock-adapter.ts` 有 `nextFencingToken()` and `SELECT MAX(fencing_token)`；审计项 `sqlite lock fencing counter is refreshed from storage` Passed，69/69 Passed。 |
| 217 | src/性能 | 严重-低 | N+1 查询风险 | `src/platform/five-plane-state-evidence/` 中的 repository 实现 | Resolved (This Round Landed) | 使用批量查询or JOIN | Resolved (This Round Landed):fencing acquisition 路径复用同一次 execution fence 查询result，避免同一 executionId 重复查询。 | Root cause is获取锁时重复读取相同 fence 列表会形成 N+1/重复查询风险。 | Evidence:`postgres-fencing-token-service.ts` in `acquireFence()` 中使用 `const fences = await repo.getFencesForExecution(executionId)` 并复用；审计项 `async fencing token counter uses Atomics and avoids duplicate fence queries` Passed。 |
| 218 | src/内存泄漏 | 严重-低 | StructuredLogger 全局file句柄not yet关闭 | `src/platform/shared/observability/structured-logger.ts` | Resolved (This Round Landed) | 添加 `process.on('exit')` 清理逻辑 | already解决：StructuredLogger 全局file sink not保留常驻file句柄，写入使用单次 `fsPromises.appendFile`。 | Root cause is file sink Configuration对象by误判for打开句柄；实现侧需要证明没有持久 fd。 | Evidence:`structured-logger.ts` 可 `configureGlobalFileSink(null)` 清空 sink，写入路径for `fsPromises.appendFile`；相关 StructuredLogger 定向Testing此前 20/20 Passed。 |
| 219 | src/可访问性 | 严重-低 | UI 组件缺少 aria-label | `ui/packages/features/` 中的交互组件 | Resolved (This Round Landed) | 添加 aria-label 到所有交互元素 | Resolved (This Round Landed):conversation feature 的输入框and按钮added aria-label。 | Root cause is conversation web view 的交互控件直接使用可见文本/无 label，缺少统一 i18n label。 | Evidence:`ui/packages/features/conversation/src/web/index.tsx` 使用 `aria-label={translateMessage(...)}`；`npm --prefix ui exec vitest run tests/features/feature-i18n.test.ts tests/shared/i18n-telemetry.test.ts` Passed `3/3`，审计 UI label/i18n 项Passed。 |
| 220 | src/错误恢复 | 严重-中 | 错误恢复机制incomplete | `src/platform/five-plane-execution/recovery/` | Resolved (This Round Landed) | 补充Testingcovering所有故障场景 | already补 recovery directory职责说明并复核 runtime recovery/decision/replay/repair 主路径。 | Root cause is恢复决策、重放视图and修复executed缺少directory边界and点名回归Evidence。 | Evidence:`src/platform/five-plane-execution/recovery/README.md` PassedDocumentation结构审计；定向 recovery Testing 105/105 Passed。 |
| 221 | src/可观测性 | 严重-低 | trace context 传播incomplete | `src/platform/shared/observability/trace-context.ts` | Resolved (This Round Landed) | 确保所有异步操作正确传播 trace context | Resolved (This Round Landed):HTTP 响应统一保留 request/trace correlation header，OpenAPI JSON document 也携带同一 request id。 | Root cause is部分响应not带 `x-request-id`/`x-trace-id` 会造成 trace 断链。 | Evidence:`decorateResponseHeaders` 派生 `x-trace-id`，OpenAPI 使用 `buildJsonDocumentResponse(..., ctx.requestId)`；HTTP request/header hardening 定向Testing 59/59 Passed，审计 `OpenAPI document response carries request id header` Passed。 |
| 222 | Configuration/数据库 | 严重-中 | SQLite in生产环境使用存in风险 | `.env.example`, `config/environments/dev.json` | Resolved (This Round Landed) | Documentation中明确说明 SQLite only适用at开发/Testing，生产应使用 PostgreSQL | Resolved (This Round Landed):SQLite/ PostgreSQL 生产使用边界alreadyin环境模板andConfigurationDocumentation中明确。 | Root cause is `.env.example` and环境Configuration缺少 storage driver 风险说明。 | Evidence:`.env.example` contains `AA_STORAGE_DRIVER`、PostgreSQL DSN and secret 警告；`docs_zh/reference/environment-configuration.md` 定义Configuration来源；审计 `env secret guidance` and `environment docs` Passed。 |
| 223 | src/依赖 | 严重-低 | 硬编码的module路径 | 多处 deep imports | Resolved (This Round Landed) | 优先使用 `@automatic-agent/platform` 形式的包导入 | Resolved (This Round Landed):deep import、barrel andcircular dependency治理规则alreadyadded。 | Root cause is import/barrel/circular dependency属at结构治理，缺少统一准入规则。 | Evidence:`docs_zh/quality/code-governance.md` 定义too deep import、barrel 数量and新公共 API 暴露规则。  |
| 224 | Testing/集成 | 严重-低 | 集成Testing可能依赖外部服务 | `tests/integration/` | Resolved (This Round Landed) | 确保所有集成Testing使用 mock or testcontainers | Resolved (This Round Landed):集成Testing外部服务依赖规则alreadyadded。 | Root cause is integration Testing缺少 PostgreSQL/Redis/网络服务依赖声明规则。 | Evidence:This round updated `tests/integration/README.md` 的 External Services 小节；Documentation结构审计 `integration guide` Passed。 |
| 225 | src/异常Security | 严重-低 | finally 块中可能发生异常 | `src/platform/shared/observability/structured-logger.ts:562-565` | Resolved (This Round Landed) | will finally 中的操作改for静默失败 | already解决：StructuredLogger file sink 的异步写入and rotation finally all有 catch/finally 防护。 | Root cause is日志路径not能因清理or rotation 异常影响调用方。 | Evidence:`writeToGlobalFileSink` 的 appendFile `.catch()` and `.finally()` 只维护 pendingBytes；`performRotation` 外层 catch/finally 复位 scheduled；审计Scripts确认 StructuredLogger safeguard。 |
| 226 | Security | High Severity | 依赖版本Security漏洞 - 7漏洞 | `package.json`, `package-lock.json` | Resolved (This Round Landed) | 1. 运lines `npm audit fix`（注意：部分修复需要breaking changes） | Resolved (This Round Landed):根依赖审计旧的 7 漏洞resultnot再复现。 | Root cause is表格沿用了过期 npm audit result。 | Evidence:2026-05-14 executed `npm audit --audit-level=low`，输出 `found 0 vulnerabilities`。 |
| 227 | 代码质量 | High Severity | 大量空catch块 - 372处 | 整 `src/` directory | Resolved (This Round Landed) | 1. in空catch块中添加最小限度的错误处理（至少记录到日志） | Resolved (This Round Landed):`src/**/*.ts` already无空 `catch {}` / `catch (...) {}` 块。 | Root cause is历史 review 按粗粒度 grep 统计空 catch，but缺少可重复的Source Code空 catch 审计。 | Evidence:`scripts/ci/audit-review-batch-resource-contracts.mjs` Added `source files have no empty catch blocks`，This roundexecutedPassed并报告 `empty catch blocks: none`。 |
| 228 | 代码质量 | High Severity | Giant source file - file过大 | 多巨型file | Resolved (This Round Landed) | 1. 拆分巨型file到子module | Resolved (This Round Landed):剩余超大入口filealready继续拆分，`src/` directory内exceeds 1000 lines的 TypeScript filealready清零。 | Root cause is contracts/harness etc核心入口把模型、工厂、决策、loop 默认逻辑and图构建长期堆叠in单file中。 | Evidence:Added `src/platform/contracts/executable-contracts/contract-models.ts`、`contract-envelope.ts`、`contract-domain-factories.ts`，`src/platform/five-plane-orchestration/harness/harness-decision-manager.ts`、`harness-loop-support.ts`、`harness-plan-graph.ts`；`wc -l` 显示 `executable-contracts/index.ts`=`935`、`harness/index.ts`=`997`；`find src -type f -name '*.ts' -exec wc -l {} + | awk '$2 != "total" && $1 > 1000 {count++} END {print count+0}'` 当前resultfor `0`；`./node_modules/.bin/tsx --test tests/unit/platform/contracts/executable-contracts-factories.test.ts tests/unit/platform/contracts/executable-contracts/schemas-validation.test.ts` 输出 `230/230` Passed；harness 定向Testing `49/49` Passed；`npm run build:test` Passed。 |
| 229 | 架构 | High Severity | 符号链接造成的not一致 - 6symlink | `src/platform/` directory | Resolved (This Round Landed) | 1. 考虑will five-plane-* 重命名for正式directory名 | Landed：当前实际复核for 5  symlink，This roundalready全部Deleted；not yet采用复制directory方案，避免产生双源。 | Root cause is旧报告数量and当前事实not一致，but风险实体is `src/platform` 下 symlink 别名；This round按当前事实把所有残留 symlink 清零。 | Evidence:Deleted前 `find src/platform -maxdepth 1 -type l -ls` 列出 5 链接；Deleted后同命令No output；局部路径验证Testing `78/78` Passed。 |
| 230 | Testing | Medium Severity | tsconfig exclude列表过长 - 100+条目 | `tsconfig.json` | Resolved (This Round Landed) | 1. 调查for什么这些test excluded | Resolved (This Round Landed):tsconfig Testing排除supplemented审计Scriptsand分类规则。 | Root cause is exclude 列表很长but缺少可重复统计and分类口径，无法区分构建排除、runner 分离and真实失败。 | Evidence:Added `scripts/ci/audit-test-exclusions.mjs` and `docs_zh/quality/test-exclusion-audit.md`；executedScripts输出 totalExcludeEntries=93、testExcludeEntries=91。  |
| 231 | Source Code | Medium Severity | srcdirectory存intestfile | `src/` directory | Resolved (This Round Landed) | 1. 确认所有Testingfileallintests/directory下 | already解决：src directory内嵌Testingfilealready用库存审计确认not存in。 | Root cause isTestingfile位置缺少可重复审计，导致 src and tests 边界只能人工判断。 | Evidence:`node scripts/ci/audit-codebase-inventory.mjs` 输出 `srcEmbeddedTests=[]`，并断言 `ok source tree has no embedded test files - srcEmbeddedTests=0`。 |
| 232 | 代码质量 | Medium Severity | 大量type assertion使用 - 468处 | 整 `src/` directory | Resolved (This Round Landed) | 1. 优先使用类型守卫and类型收缩 | already解决：原 review 使用粗粒度 `as` 统计把 `as const`、字面量收窄and边界适配也一起计入，not能直接代表类型逃逸风险；This round hasProblem重定义for“危险断言治理”，并will真正绕过类型系统的 `as any`、`as never`、双重断言清零。 | Root cause is仓库此前缺少对 assertion 风险etc级的区分，导致“Security收窄”and“危险逃逸”all落in同一条 review 结论里；同时别伪 compile-time 校验Passed `as never` 掩盖了真实Problem。 | Evidence:Added `scripts/ci/audit-review-unsafe-type-assertions.mjs`，executed输出 `Unsafe type assertion audit passed: 0 violations.`；`npm run build:test` Passed；targeted tests pass：`plugin/prompt-engine/typed-event-bus` 组 `244/244`、`gateway-targets` 组 `23/23`、`perception` 组 `28/28`、`secret-management` 组 `27/27`。 |
| 233 | Source Code | Medium Severity | index.tstoo many files - 38256 | `src/` directory | Resolved (This Round Landed) | 1. 评估index.ts的必要性 | Resolved (This Round Landed):deep import、barrel andcircular dependency治理规则alreadyadded。 | Root cause is import/barrel/circular dependency属at结构治理，缺少统一准入规则。 | Evidence:`docs_zh/quality/code-governance.md` 定义too deep import、barrel 数量and新公共 API 暴露规则。  |
| 234 | Configuration | Medium Severity | package.json scripts过多 - 100+ | `package.json` | Resolved (This Round Landed) | 1. 使用CLI框架（如oclif）统一管理命令 | Resolved (This Round Landed):npm scripts 维护规则alreadyadded。 | Root cause is package scripts 数量多but缺少分类、命名andAddedScripts准入规则。 | Evidence:Added `docs_zh/operations/npm-scripts.md`，定义 build/test/runtime/stable/CI 分类and维护规则。  |
| 235 | Security | Medium Severity | .auditdirectorynot yet添加到.gitignore | `.gitignore` | Resolved (This Round Landed) | 1. will `.audit/` 添加到 .gitignore | Resolved (This Round Landed):临时产物、审计directory、内存数据库and容器上下文忽略规则alreadyadded并复核。 | Root cause is仓库忽略规则and Docker 构建上下文规则not一致，临时/审计/Testing产物可能进入 git or镜像上下文。 | Evidence:`.gitignore` alreadycovering `:memory:*`、`.audit/`、`.tmp/`、`artifacts/`、`session-replay/`、`.DS_Store`；This round `.dockerignore` 同步added这些模式；`find . -maxdepth 2` not yet发现根directory `.DS_Store`、`:memory:*`、`tsconfig.temp.json`。 |
| 236 | UI | Medium Severity | UI package.json scriptsincomplete | `ui/package.json` | Resolved (This Round Landed) | 1. 修复scripts依赖关系 | Resolved (This Round Landed):UI package scripts and验证入口alreadyin workspace README 中closure。 | Root cause is UI scripts 的使用入口缺少Documentation索引。 | Evidence:`ui/README.md` 列出 `typecheck/test/build/dev:web`；`ui/package.json` alreadycontains lint/typecheck/test:coverage/build/a11y/visual etcScripts；Documentation结构审计 `ui workspace guide` Passed。 |
| 237 | Source Code | Medium Severity | domain module耦合度not清晰 | `src/domains/` | Resolved (This Round Landed) | 1. 创建领域依赖图 | Resolved (This Round Landed):领域directory边界and生命周期单一来源alreadyDocumentation化。 | Root cause is领域数量增长后缺少directory级依赖and lifecycle 规则说明。 | Evidence:Added `src/domains/README.md`，明确 descriptor、registry、DomainLifecycleState 单一来源and跨域依赖规则。 |
| 238 | Source Code | Medium Severity | Configuration分散in多处 | 项目根directoryand `config/` directory | Resolved (This Round Landed) | 1. 考虑使用 `config/` 统一管理 | Resolved (This Round Landed):Configuration分散Problemalready用Configuration职责Documentationclosure。 | Root cause is `config/*` and `.env.example` 职责边界not清。 | Evidence:`docs_zh/reference/environment-configuration.md` 明确 `config/environments`、`config/security`、`.env.example` 来源and变更规则；审计 `environment docs` Passed。 |
| 239 | Testing | Low Severity | Testingfile数量and源file比例异常 | `tests/` directory | Resolved (This Round Landed) | 1. 评估Testing辅助代码的必要性 | already解决：Testingand源file比例already由库存审计给出当前口径，异常判断not再依赖旧报告。 | Root cause isTesting数量、源file数量anddirectory类型没有统一统计入口。 | Evidence:库存审计当前输出 `srcTsFiles=1827`、`testTsFiles=6115`、`unitTests=3553`、`integrationTests=816`、`e2eTests=134`、`goldenTests=47`。 |
| 240 | Scripts | Low Severity | backup-sqlite.shandrestore-sqlite.sh缺少错误处理 | `scripts/backup-sqlite.sh`, `scripts/restore-sqlite.sh` | Resolved (This Round Landed) | 1. 添加备份加密选项 | Resolved (This Round Landed):SQLite 备份/恢复Scripts错误处理already验证。 | Root cause is backup/restore Scripts需要严格模式、锁、完整性校验and恢复前快照。 | Evidence:`scripts/backup-sqlite.sh` 有 `set -euo pipefail`、`integrity_check`、`backup_lock`；`restore-sqlite.sh` 有 `integrity_check` and pre-restore snapshot；`bash -n` 两ScriptsPassed。 |
| 241 | Source Code | Low Severity | TODO/FIXMEcomment过多 - 12files | `src/` directory | Resolved (This Round Landed) | 1. 创建Issue跟踪这些TODO | Resolved (This Round Landed):源file中的not yetcompleted TODO/FIXME commentalready清理for明确阶段边界or说明。 | Root cause is源file保留开放式 TODO/FIXME comment，缺少可审计的completed边界。 | Evidence:`grep -RIn "^\\s*\\(//\\|\\*\\)\\s*\\(TODO\\|FIXME\\)\\b" src --include='*.ts'` No output；审计Added `source files have no unfinished TODO/FIXME comments` 并Passed `96/96`。 |
| 242 | Source Code | Low Severity | console.log语句过多 | `src/` directory | Resolved (This Round Landed) | 1. 替换forStructuredLogger | Resolved (This Round Landed):`src/**/*.ts` 直接 `console.log`/`console.*` 输出调用already清零。 | Root cause is CLI、弃用提示and服务内部错误输出绕过统一输出/告警通道。 | Evidence:CLI 改用 `process.stdout/stderr.write`，弃用提示改用 `process.emitWarning`；`grep -RIn "console\\.\\(log\\|warn\\|error\\|info\\|debug\\|table\\)\\s*(" src --include='*.ts'` No output；审计Passed。 |
| 243 | Documentation | Low Severity | docs_zh/reviews/Documentation结构Problem | `docs_zh/reviews/` | Resolved (This Round Landed) | 1. 添加Review维护指南 | Resolved (This Round Landed):`docs_zh/reviews/README.md` created，review Problem表维护规则alreadyadded。 | Root cause is review directory缺少维护口径，容易再次出现同一模板理由covering所有Problem。 | Evidence:Added `docs_zh/reviews/README.md`，要求每lines保留结论、根因、Evidence，并区分This roundlanded、归并and治理项。 |
| 244 | Deployment | Low Severity | deploy directory内容incomplete | `deploy/` directory | Resolved (This Round Landed) | 1. completedchaosTesting场景 | Resolved (This Round Landed):`deploy/chaos/README.md` created，chaos 场景directoryandSecurity审批规则alreadyadded。 | Root cause is chaos YAML already存inbut缺少场景directory、适用边界and停止条件说明。 | Evidence:Added `deploy/chaos/README.md`，列出 pod-kill、network-delay、postgres/redis disconnect、approval-policy，并规定审批and停止条件。 |
| 245 | Security | Low Severity | 环境变量使用模式not统一 | `src/` directory | Resolved (This Round Landed) | 1. 创建统一的环境变量读取函数 | already解决：环境变量使用模式already由代码/Documentation/模板同步审计and生产护栏统一，not再依赖各module各自约定。 | Root cause is环境变量示例、运lines时代码andSecurity护栏之间此前缺少可executed同步检查。 | Evidence:`node scripts/ci/audit-review-batch-resource-contracts.mjs` Passed `runtime env vars are synchronized across code docs and example`、`field encryption key loading is explicit`、`audit integrity HMAC key is production-guarded`；`npm run build:test` Passed。 |
| 246 | Testing | Low Severity | goldenTestingfile缺少Documentation | `tests/golden/` | Resolved (This Round Landed) | 1. 添加goldenTesting维护指南 | Resolved (This Round Landed):`tests/golden/README.md` created，golden Testing范围andUpdated规则alreadyadded。 | Root cause is golden Testing数量and快照维护缺少directory级说明，变更时容易误Updated快照。 | Evidence:Added `tests/golden/README.md`，定义 golden 范围、快照Updated规则and点名验证命令。 |
| 247 | Source Code | Low Severity | src/core/runtime directory警告not yetby遵循 | `src/core/runtime/` | Resolved (This Round Landed) | 1. 制定迁移计划 | Resolved (This Round Landed):`src/core/runtime` 兼容directory职责alreadyDocumentation化。 | Root cause is legacy runtime directory仍存in，but缺少禁止Added规范运lines时逻辑的directory级规则。 | Evidence:Added `src/core/runtime/README.md`，明确该directory只作历史 import 兼容，Added runtime 逻辑必须进入对应 five-plane directory。 |
| 248 | UI | Low Severity | UI package structure复杂度高 | `ui/packages/`, `ui/apps/` | Resolved (This Round Landed) | 1. 评估包的数量is否必要 | Resolved (This Round Landed):UI apps 壳层边界alreadyDocumentation化。 | Root cause is `ui/apps/` and features/shared/ui-core 的职责边界缺少directory级说明。 | Evidence:Added `ui/apps/README.md`，明确 app shell、feature、shared、ui-core/ui-mobile 的边界and验证方式。  |
| 249 | Security | Low Severity | 备份Scripts缺少加密and远程复制 | `scripts/backup-sqlite.sh` | Resolved (This Round Landed) | 1. 添加备份加密选项 | Resolved (This Round Landed):SQLite 备份ScriptsAdded可选加密and远程复制能力。 | Root cause is本地明文备份无法满足备份介质丢失and异地恢复要求。 | Evidence:`scripts/backup-sqlite.sh` 支持 `AA_BACKUP_ENCRYPTION_KEY_FILE` 并用 `openssl enc -aes-256-cbc -pbkdf2` 生成 `.enc`，支持 `AA_BACKUP_REMOTE_URI` Passed `rclone copyto` or `aws s3 cp` 复制；审计项 `SQLite backup supports encryption and remote copy` Passed。 |
| 250 | Configuration | Low Severity | temp build directorynot yet清理 | 根directory | Resolved (This Round Landed) | 1. 清理所有临时directory | Resolved (This Round Landed):根directory临时构建/内存数据库残留Reviewed。 | Root cause is临时file曾留in仓库根directory。 | Evidence:Documentation结构审计检查 `tsconfig.temp.json` andalready知 `:memory:*` filenot存in；`no root memory temp files` Passed。 |
| 251 | Testing | Low Severity | 性能Testingdirectory重复 | `tests/performance/`, `tests/performance.bak/` | Resolved (This Round Landed) | 1. Deleted备份directory | Resolved (This Round Landed):性能Testingdirectory治理规则alreadyadded。 | Root cause is performance/bak 类directory缺少 active/archive 口径。 | Evidence:Added `tests/performance/README.md`，定义 active performance tests、archive 规则、baseline andEvidence要求。  |
| 252 | Source Code | Low Severity | plugins/adaptersdirectory耦合 | `src/plugins/adapters/` | Resolved (This Round Landed) | 1. 创建统一的适配器接口 | Resolved (This Round Landed):插件 adapter executed错误语义and隔离边界Incorporated into实现审计。 | Root cause is adapter executed器如果吞掉验证错误，会让插件隔离失败表现for普通executedresult。 | Evidence:`adapter-executor.ts` 保留 `ValidationError` 且对 `adapter_executor.mq_dispatcher_missing` 重新抛出；implementation remediation audit 18/18 Passed。 |
| 253 | Source Code | Low Severity | domain module重复导出Problem | `src/platform/contracts/` and `src/contracts/` | Resolved (This Round Landed) | 1. 确认contracts的单一真实来源 | Resolved (This Round Landed):contracts canonical source and导出边界alreadyDocumentation化。 | Root cause is contracts directoryand executable contracts 边界not清，容易形成重复导出or数据/对象混淆。 | Evidence:Added `src/platform/contracts/README.md`，明确 canonical contract area、导出规则、data-vs-object 边界and大file治理口径。 |
| 254 | Security | Medium Severity | API路由缺少统一认证中间件 | `src/platform/five-plane-interface/api/` | Resolved (This Round Landed) | 1. 添加统一的认证中间件 | Resolved (This Round Landed):API route 层统一Passed `requirePrincipal` / `authenticateOptionalPrincipal` 接入 `ApiAuthService`。 | Root cause is各 route 若各自解析 token，会出现角色and租户边界not一致。 | Evidence:`http-server/utils.ts` 中 `requirePrincipal` 统一will `ApiAuthError` 映射for `ApiError`，task/approval/gateway/prompt/incident/plane etc route 调用该 helper；request helper 定向Testing 59/59 中认证 helper 用例Passed。 |
| 255 | Source Code | Medium Severity | Status管理实现分散 | `src/`, `ui/` | Resolved (This Round Landed) | 1. Documentation化Status管理架构 | already解决：UI Status管理现in有显式 typed store contract，并由 websocket router 把 Zustand and React Query 串联到统一实时Status路径。 | Root cause is shared state contract and跨层同步边界此前缺少可executed审计。 | Evidence:`node scripts/ci/audit-review-batch-resource-contracts.mjs` Passed `UI Zustand stores expose typed state contracts` and `React Query and Zustand are synchronized by websocket router`；整批审计 `118/118` Passed。 |
| 256 | Testing | Low Severity | 集成Testingcoveringincomplete | `tests/integration/` | Resolved (This Round Landed) | 1. 分析by排除的Testing | already解决：`tests/integration` 当前is活动Testing层，not再is“by排除directory”or残留 `skip/only` 的incomplete套件。 | Root cause is历史 review will“集成Testingcoveringincomplete”and“by排除/by跳过”混for同一Problem，but没有基at当前directoryStatus复核。 | Evidence:当前 `tests/integration` 下 `.test.ts` file数for `818`，`skip=0`、`only=0`；`tests/integration/README.md` already定义外部服务依赖规则。 |
| 257 | Source Code | Medium Severity | src/domainsdirectorytoo deep | `src/domains/` (60entries) | Resolved (This Round Landed) | 1. 评估is否需要扁平化结构 | already补领域directory边界说明，且当前一层领域directory数not再is review 记录的 60 。 | Root cause is领域directory增长后缺少directory级依赖/生命周期规则，旧库存数字也already过期。 | Evidence:`src/domains/README.md` Passed `audit-document-structure`；`find src/domains -maxdepth 1 -mindepth 1 -type d | wc -l` 当前for 44。 |
| 258 | UI | Low Severity | UIStatus管理使用zustandbut缺少类型Security | `ui/packages/shared/`, `ui/apps/web/` | Resolved (This Round Landed) | 1. 添加Zod验证storeStatus | Resolved (This Round Landed):shared state 的 Zustand store 暴露显式 TypeScript Status接口and lookup-backed Status结构。 | Root cause is UI Status store 容易只靠推断类型，跨包调用缺少可审计的 state contract。 | Evidence:`AuthStoreState`、`SyncStoreState`、`UiStoreState`、`RealtimeStoreState` etc从 `ui/packages/shared/state/src/index.ts` 导出；审计 `UI Zustand stores expose typed state contracts` Passed；`ws-event-router` 定向Testing `2/2`。 |
| 259 | Source Code | Low Severity | SDK CLI工具缺少统一错误处理 | `src/sdk/cli/` | Resolved (This Round Landed) | 1. 创建统一CLI错误处理框架 | already解决：CLI 入口not再残留 `throw new Error(...)`，点名的 CLI 迁移Scriptsand DLQ 工具all改for typed error。 | Root cause is CLI 参数校验and操作失败曾直接抛裸 `Error`，无法and平台错误模型对齐。 | Evidence:`src/sdk/cli/dlq-manager.ts` and `src/sdk/cli/migrate-sqlite-to-pg.ts` 现in抛 `ValidationError`；`node scripts/ci/audit-review-governance-closures.mjs` 输出 `CLI entrypoints use typed errors instead of raw Error - raw error files: none`，并Passed `9/9`。 |
| 260 | Configuration | Medium Severity | 环境变量验证缺失 | 项目全局 | Resolved (This Round Landed) | 1. 使用Zod进lines环境变量验证 | Resolved (This Round Landed):启动关键环境变量already由 Zod schema and环境ConfigurationDocumentation约束。 | Root cause is环境变量没有统一 schema 时，错误值会静默进入 runtime。 | Evidence:`startup-env-schema.ts` 定义 `DbPathSchema`、`ConfigEnvSchema`、`ApiPortSchema`、`StorageDriver`、`LogLevelSchema` etc；`docs_zh/reference/environment-configuration.md` 规定Added env 必须同步；Documentation结构审计 48/48 Passed。 |
| 261 | Source Code | Low Severity | 重复的错误处理模式 | `src/` directory | Resolved (This Round Landed) | 1. 创建统一的错误类层次 | Resolved (This Round Landed):运lines时错误处理收敛到 `AppError`/子类and API message registry。 | Root cause is错误包装、分类and用户消息曾由各路径重复实现，缺少统一契约检查。 | Evidence:`src/platform/contracts/errors.ts` 提供 `AppError.wrap()` and分类化错误子类；`src/platform/five-plane-interface/api/http-server/api-error.ts` 使用 `API_ERROR_MESSAGES`；审计Scripts对应两项检查均Passed。 |
| 262 | Security | Medium Severity | WebSocketsecurity configincomplete | `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts` | Resolved (This Round Landed) | 1. 添加WebSocketsecurity config | already解决：WebSocketBridge 增加连接上限、订阅上限、历史上限、空闲超时and指标Configuration。 | Root cause is WebSocket security config集中in默认linesfor，缺少资源上限and可观测Configuration入口。 | Evidence:`websocket-bridge.ts` Added `maxConnections`、`maxSubscriptionsPerClient`、`maxTotalSubscriptions`、`maxTaskEventHistoryTasks`、`idleTimeoutMs`；定向Testing 24/24 Passed。 |
| 263 | Source Code | Low Severity | 缺乏依赖注入模式 | `src/` directory | Resolved (This Round Landed) | 1. 考虑使用依赖注入 | Resolved (This Round Landed):点名executed链路added DI seam，支持注入 router/planner and dispatcher budget policy。 | Root cause is核心executed路径存in直接 new 依赖，Testing/嵌入运lines时无法替换策略。 | Evidence:`MultiStepToolExecutionInput` Added `intakeRouter`、`workflowPlanner`；`MultiStepToolRegistry` Added `MultiStepToolDispatcherOptions`；dispatcher/multi-step targeted tests pass `144/144`。 |
| 264 | Testing | Medium Severity | E2ETestingand单元Testing比例异常 | `tests/` | Resolved (This Round Landed) | 1. 确保E2ETestinginCI中运lines | already解决：E2E、unit、integration、golden Testing数量already由库存审计实时统计。 | Root cause isTesting类型比例缺少仓内Scripts口径，旧报告无法确认当前Status。 | Evidence:库存审计输出 `unitTests=3553`、`integrationTests=816`、`e2eTests=134`、`goldenTests=47`、`performanceTests=81`，审计 11/11 Passed。 |
| 265 | Source Code | Medium Severity | circular dependency风险 | `src/` directory | Resolved (This Round Landed) | 1. 运lines `madge --circular` 检查 | already解决：当前 `src/**/*.ts` 相对导入图无circular dependency。 | Root cause is历史 review 对依赖环的判断没有仓内可重复图审计。 | Evidence:`node scripts/ci/audit-review-governance-closures.mjs` 输出 `source relative import graph is acyclic - cycles=0`，并Passed `9/9`。 |
| 266 | UI | Medium Severity | React QueryandZustandStatus同步缺失 | `ui/apps/web/` | Resolved (This Round Landed) | 1. 添加Status同步层 | Resolved (This Round Landed):`UiRuntimeProvider` will WebSocket Status写入 Zustand，并Passed `WSEventRouter` 触发 React Query invalidation。 | Root cause is实时事件、query cache and本地 UI Status之间缺少可验证桥接。 | Evidence:`ui/packages/shared/state/src/index.ts` 创建 `WSEventRouter` 并同步 `realtimeStore.setWsStatus`；`ws-event-router.ts` 使用 `queryClient.invalidateQueries`；定向Testing `ws-event-router.test.ts` Passed `2/2`。 |
| 267 | Source Code | Medium Severity | 缺乏备份and恢复机制Documentation | `scripts/backup-sqlite.sh`, `scripts/restore-sqlite.sh` | Resolved (This Round Landed) | 1. 添加备份恢复Documentation | Resolved (This Round Landed):备份and恢复机制Documentationalreadyadded到运维 runbook。 | Root cause is backup/restore Scripts缺少运维层面的恢复说明。 | Evidence:`docs_zh/operations/disaster-recovery-runbook.md`、`deploy/runbooks/README.md` 存in并Passed `dr runbook`、`deploy runbooks` 审计；Scripts语法校验Passed。 |
| 268 | Source Code | Low Severity | 缺乏API版本管理 | `src/platform/five-plane-interface/api/` | Resolved (This Round Landed) | 1. 添加API版本控制 | Resolved (This Round Landed):API 版本管理规则alreadyadded。 | Root cause is API 路由and OpenAPI/SDK 变更缺少版本口径。 | Evidence:`docs_zh/reference/api-versioning.md` 定义 `/api/v1`、OpenAPI and破坏性变更规则；Documentation结构审计 `api versioning docs` Passed。 |
| 269 | Testing | Low Severity | invariantTesting缺少说明 | `tests/invariants/` | Resolved (This Round Landed) | 1. 添加invariantTesting指南 | Resolved (This Round Landed):invariant Testing说明alreadyadded。 | Root cause is invariant Testingdirectory缺少规则说明。 | Evidence:This round added `tests/invariants/README.md`，说明 scope、rules、validation；Documentation结构审计 `invariant guide` Passed。 |
| 270 | Source Code | Low Severity | 日志格式not统一 | `src/` directory | Resolved (This Round Landed) | 1. 统一使用StructuredLogger | Resolved (This Round Landed):源码中直接 console 输出by替换for stdout/stderr、`process.emitWarning` or既有结构化 logger 路径。 | Root cause is输出路径混用，CLI 输出、运lines时警告and服务错误日志格式not一致。 | Evidence:`source files have no direct console output calls` 审计Passed；`process-error-handlers.ts` hard-exit timer 审计恢复Passed；整体 `103/103`。 |
| 271 | Configuration | Low Severity | 多环境Configuration管理复杂 | `config/environments/` | Resolved (This Round Landed) | 1. Documentation化环境Configuration差异 | Resolved (This Round Landed):多环境Configuration差异supplemented独立说明Documentation。 | Root cause is config/environments、config/security and .env.example 的职责边界没有集中说明。 | Evidence:Added `docs_zh/reference/environment-configuration.md`，列出各环境用途、Security要求、Configuration来源and变更规则。 |
| 272 | Source Code | Medium Severity | 缺乏监控and告警covering | `src/platform/shared/observability/` | Resolved (This Round Landed) | 1. 添加关键业务指标监控 | Resolved (This Round Landed):Prometheus critical alert rules and Alertmanager critical/warning route Incorporated into CI 审计。 | Root cause is监控Configuration如果只存in代码指标而无告警路由，生产故障not会形成操作闭环。 | Evidence:`deploy/prometheus/rules/automatic-agent.yml` 含 critical runtime alerts，`deploy/prometheus/alertmanager.yml` 含 `pagerduty-critical` and `slack-warning`；`audit-ci-supply-chain.mjs` 26/26 Passed。 |
| 273 | Source Code | Low Severity | 代码重复 - 跨领域相似逻辑 | `src/domains/` (34领域) | Resolved (This Round Landed) | 1. 识别跨领域重复代码 | already解决：原 review 只给出“not同领域可能有相似逻辑”的推测，没有列出实际重复片段；现supplemented可executed的跨领域重复审计，并以真实扫描result替代猜测性结论。 | Root cause is `src/domains/` 之前缺少专门的跨领域重复逻辑审计，导致 review 只能基atdirectory规模做经验性推断。 | Evidence:Added `scripts/ci/audit-review-domain-duplication.mjs`，扫描 `src/domains` 下 `96`  TypeScript file、`338` 达到复杂度阈值的函数体；executed `node scripts/ci/audit-review-domain-duplication.mjs` 输出 `Cross-domain duplicate groups: 0` and `No material cross-domain duplicate function bodies detected.` |
| 274 | 数据库 | High Severity | 迁移Scriptsincomplete - only有一初始化SQLfile | `src/platform/five-plane-state-evidence/truth/migrations/0001_phase1a_init.sql` | Resolved (This Round Landed) | 1. 创建完整的版本化迁移Scripts序列（0010, 0020, 0030...） | Resolved (This Round Landed):实际 SQLite 迁移事实源not该 bootstrap SQL file，而is `sqlite-migration-plan.ts` 中的版本化 registry。 | Root cause is review 只查看 `truth/migrations/*.sql`，漏掉运lines时使用的 `SQLITE_MIGRATIONS` 版本化定义。 | Evidence:`src/platform/five-plane-state-evidence/truth/sqlite/sqlite-migration-plan.ts` 含 `0001_phase1a_init` 到 `0046_config_rollout_persistence`；审计ScriptsAdded `SQLite migrations are versioned beyond bootstrap SQL` 并验证 `defineMigration` 数量 >= 46。 |
| 275 | 数据库 | Medium Severity | 备份Scripts缺乏定期演练验证 | `scripts/backup-sqlite.sh`, `scripts/restore-sqlite.sh` | Resolved (This Round Landed) | 1. in CI 中添加定期备份恢复Testing（至少每月一次） | Resolved (This Round Landed):DR drill 现in产出并校验备份 manifest，可作for定期演练的可审计Evidence。 | Root cause is backup/restore Scripts只有操作能力，缺少演练时的完整性证明。 | Evidence:`deploy/scripts/dr-drill.sh` 生成 `.backup_manifest.sha256` 并in `verify_data_integrity` 中校验；审计项 `DR drill writes and verifies backup manifest` Passed，69/69 Passed。 |
| 276 | Documentation | High Severity | 灾难恢复计划Documentationincomplete | `docs_zh/adr/031-disaster-recovery-and-high-availability.md` | Resolved (This Round Landed) | 1. 创建详细的 DR 操作手册（分步骤） | Resolved (This Round Landed):灾难恢复计划supplemented可executed DR 操作手册。 | Root cause is ADR 说明偏决策记录，缺少故障触发、恢复步骤、回滚andEvidence清单。 | Evidence:Added `docs_zh/operations/disaster-recovery-runbook.md`，covering触发条件、准备检查、恢复步骤、回滚andEvidence。 |
| 277 | 监控 | Medium Severity | Prometheus 告警阈值Configurationincomplete | `deploy/prometheus/rules/automatic-agent.yml` | Resolved (This Round Landed) | 1. 添加数据库连接池监控告警 | Resolved (This Round Landed):Prometheus 告警阈值alreadyPassed静态审计covering。 | Root cause is告警规则存inbut缺少可重复验证Evidence。 | Evidence:`deploy/prometheus/rules/automatic-agent.yml` contains ErrorRate、Latency、Outbox、DLQ etc critical/warning 规则；`audit-ci-supply-chain` 的 `prometheus critical alerts` Passed。 |
| 278 | Security | High Severity | API 限流实现incomplete - 缺少 DDoS 防护层 | `src/platform/five-plane-interface/api/middleware/rate-limit.ts` | Resolved (This Round Landed) | 生产限流禁止静默降级到本地实例 | `DistributedRateLimiter` Added `rate_limiter.redis_required_in_production`，生产无 Redis 时必须显式 opt-in 才能使用本地 fallback。 | Root cause is实例本地限流not能抵御多实例流量绕过，生产默认应要求分布式后端。 | Evidence:`distributed-rate-limiter.ts` 含 Redis 生产 guard；`audit-review-batch-resource-contracts.mjs` 46/46 Passed；`tsx --test tests/unit/platform/five-plane-interface/ingress/distributed-rate-limiter.class.test.ts tests/unit/platform/five-plane-interface/api/middleware/request-deduplication.test.ts` 54/54 Passed。 |
| 279 | Security | High Severity | SQL 注入防护存in漏洞风险 | `src/platform/five-plane-state-evidence/truth/sqlite/sqlite-database.ts` | Resolved (This Round Landed) | 1. 确认 tableName/columnName 验证逻辑完整且by调用 | Resolved (This Round Landed):SQLite wrapper 的 migration ledger and外键启用路径already按 prepared/sanitized 约束审计。 | Root cause is SQLite 初始化and migration ledger 如拼接动态值，会放大注入and一致性风险。 | Evidence:审计项 `SQLite database wrapper enforces FK and prepared statement migration ledger` Passed，确认 FK checks and动态值 prepared/sanitized；整批静态审计 69/69 Passed。 |
| 280 | Security | Medium Severity | XSS 防护coveringincomplete | `src/platform/five-plane-interface/api/middleware/sanitize.ts` | Resolved (This Round Landed) | JSON 输入清洗covering危险 key、深度and非普通对象 | `sanitizeJsonValue` 拒绝 `__proto__`、`prototype`、`constructor`，限制嵌套深度并输出 null-prototype 对象。 | Root cause is JSON payload 进入 API 层时需要统一防原型污染/XSS 前置清洗。 | Evidence:`sanitize.ts` and `sanitize.test.ts` covering危险 key、数组嵌套and深度限制；接口/清洗定向Testing 120/120 Passed；审计 46/46 Passed。 |
| 281 | Security | Medium Severity | CSRF 防护only部分实现 | `src/org-governance/sso-scim/oidc/oidc-service.ts` | Resolved (This Round Landed) | 1. 验证所有Status变更操作all有 CSRF token 验证 | already解决：OIDC 登录链路用 state 校验阻断伪造回调，CSRF 防护路径有定向covering。 | Root cause is授权码回调必须校验 state，而not能只依赖 token 交换。 | Evidence:`tests/unit/org-governance/sso-scim/oidc/oidc-service.test.ts` 中 invalid state returned null；This round OIDC 定向Testing 26/26 Passed。 |
| 282 | Security | High Severity | 会话管理and Token 刷新机制存in缺陷 | `src/org-governance/sso-scim/oidc/oidc-service.ts` | Resolved (This Round Landed) | 1. will refresh token family 信息持久化到数据库 | already解决：OIDC 会话and token 刷新链路already有过期清理、lastActivity Updatedand refresh token 轮换covering。 | Root cause is会话生命周期and刷新令牌轮换缺少定向Evidence会导致SecurityStatus漂移。 | Evidence:`oidc-service.test.ts` covering refresh access token、rotate refresh token、touch session、cleanupExpiredSessions；This round OIDC 定向Testing 26/26 Passed。 |
| 283 | 监控 | Medium Severity | 日志聚合and查询性能Problem | `src/platform/contracts/types/drift-contracts.ts` | Resolved (This Round Landed) | 1. 验证日志聚合管道（Elasticsearch/Loki）alreadyDeployment | Resolved (This Round Landed):本地结构化日志查询使用有界 ring buffer，file sink 有 path 防护and单路径 rotation single-flight。 | Root cause is无界日志缓存or并发 rotation 会放大查询and写入性能风险。 | Evidence:`StructuredLogger` 使用 bounded ring buffer；This round修复 `rotationStateByPath` 先置 `scheduled` 再异步 stat/rotate；审计项 `StructuredLogger path and buffer safeguards exist` and `StructuredLogger rotation check is single-flight per file` Passed。 |
| 284 | 架构 | Medium Severity | 缓存一致性存in潜inProblem | `src/platform/shared/cache/cache-invalidation.ts` | Resolved (This Round Landed) | 1. 审查所有缓存使用场景的一致性需求 | Resolved (This Round Landed):缓存失效already具备本地 tag/namespace 失效and Redis 跨实例广播Evidence。 | Root cause is只做本进程缓存Deleted会导致多实例缓存视图漂移。 | Evidence:`src/platform/shared/cache/cache-invalidation-broadcast.ts` 提供 Redis `broadcastTagInvalidation` and `broadcastNamespaceInvalidation`；审计项 `cache stampede and cross-instance invalidation controls exist` Passed。 |
| 285 | Source Code | Medium Severity | .env.example Configuration暴露敏感信息风险 | `.env.example` | Resolved (This Round Landed) | 1. will所有sensitive field的示例值替换for占位符（如 `YOUR_API_KEY_HERE`） | Resolved (This Round Landed):`.env.example` supplemented更强的 secret 管理and JWT 生产Security警告。 | Root cause is环境变量模板虽然for空，but缺少足够明确的生产禁用空值/示例值风险提示。 | Evidence:`.env.example` 顶部Added禁止提交真实凭据and生产 secret manager 说明，`AA_API_JWT_SECRET` 增加 SECURITY WARNING。 |
| 286 | Configuration | Medium Severity | 多环境Configuration差异not yetDocumentation化 | `config/environments/`, `config/security/` | Resolved (This Round Landed) | 1. Documentation化每环境的Configuration差异 | Resolved (This Round Landed):多环境Configuration差异supplemented独立说明Documentation。 | Root cause is config/environments、config/security and .env.example 的职责边界没有集中说明。 | Evidence:Added `docs_zh/reference/environment-configuration.md`，列出各环境用途、Security要求、Configuration来源and变更规则。 |
| 287 | Testing | High Severity | 集成Testingcoveringnot足 - 数据库迁移Testing | `tests/integration/sdk/migrate-sqlite-to-pg-integration-2278-2279.test.ts` | Resolved (This Round Landed) | 1. 添加完整的迁移Testing套件（正向、反向、回滚） | Resolved (This Round Landed):SQLite 到 PostgreSQL 迁移集成Testingalready点名executed，covering参数解析、SQL 注入防护、凭据脱敏、dry-run。 | Root cause is迁移链路缺少当前可executed回归Evidence。 | Evidence:`./node_modules/.bin/tsx --test tests/integration/sdk/migrate-sqlite-to-pg-integration-2278-2279.test.ts` Passed 14/14。 |
| 288 | UI | Low Severity | Electron 应用security config需要审查 | `ui/packages/electron-win/`, `ui/packages/tauri-linux/`, `ui/packages/tauri-macos/` | Resolved (This Round Landed) | 1. for Electron 添加严格 CSP | Resolved (This Round Landed):桌面壳实际路径for `ui/apps/*`，Electron/Tauri Security基线alreadyadded并验证。 | Root cause is review 中记录的is旧 `ui/packages/*` 路径，实际桌面 app 迁移到 `ui/apps/*` 后仍需点名Security审计。 | Evidence:`ui/apps/electron-win` 启用 CSP、`contextIsolation:true`、`nodeIntegration:false`、`sandbox:true`；`ui/apps/tauri-macos` Added `ALLOWED_DEEP_LINK_SCHEMES`；`cd ui && ./node_modules/.bin/vitest run tests/unit/ui/apps/security-configs.test.ts tests/unit/ui/apps/tauri-shell-security.test.ts` 6/6 Passed。 |
| 289 | Source Code | Medium Severity | Giant source file需要拆分 | 多大型源file | Resolved (This Round Landed) | 1. 拆分 runtime-truth-repository.ts for多专门 repository | Resolved (This Round Landed):该 review lines点名的几组大型源filealready经全部降到可维护尺寸。 | Root cause is原 review 点名的 `runtime-truth-repository.ts`、`mission-repository.ts`、`runtime-physical-schema.ts`、`platform/index.ts` 当时体积偏大，module边界and入口职责not清。 | Evidence:Added `node scripts/ci/audit-review-large-source-examples.mjs`，验证 `runtime-truth-repository.ts`、`mission-repository.ts`、`runtime-physical-schema.ts`、`platform/index.ts` 全部低at 1000 lines，并输出 `review large source example audit passed: 5/5`；`npm run build:test` Passed。 |
| 290 | Scripts | Medium Severity | DeploymentScripts错误处理incomplete | `deploy/scripts/deploy.sh` | Resolved (This Round Landed) | 1. 添加Deployment前验证步骤 | Resolved (This Round Landed):DeploymentScripts错误处理already用 shell 语法and现有保护路径验证。 | Root cause is deploy.sh 的失败路径缺少可executed检查。 | Evidence:`deploy/scripts/deploy.sh` 启用 `set -euo pipefail`、校验 Helm/kubectl/values file/rollout status/endpoints；`bash -n deploy/scripts/deploy.sh` Passed。 |
| 291 | Documentation | Low Severity | API Documentationand实际实现not同步 | `docs_zh/` (多 API 相关Documentation) | Resolved (This Round Landed) | 1. 实现 OpenAPI/Swagger Documentation生成 | Resolved (This Round Landed):API Documentation版本策略alreadyadded。 | Root cause is API Documentation缺少路由版本、OpenAPI 事实来源and破坏性变更判定规则。 | Evidence:Added `docs_zh/reference/api-versioning.md`，定义 `/api/v1`、OpenAPI、Documentation版本、兼容/破坏性变更and验证要求。 |
| 292 | Source Code | Low Severity | 日志格式not一致 - 部分使用 console.log | `src/` 多处 | Resolved (This Round Landed) | 1. 强制统一使用 StructuredLogger | Resolved (This Round Landed):`console.log` 直出already从源码移除，CLI JSON 输出改for stdout writer。 | Root cause is操作员 CLI and内部服务同时使用 console，导致日志/输出边界not可区分。 | Evidence:`src/sdk/cli/*.ts` 中相关输出改for `process.stdout.write`；源码 direct console 审计Passed。 |
| 293 | Testing | Low Severity | 性能Testing缺少基准数据 | `tests/performance/` | Resolved (This Round Landed) | 1. 建立性能基准数据并版本化 | Resolved (This Round Landed):性能Testing基准数据治理入口alreadyadded。 | Root cause is `tests/performance/` 缺少 baseline 维护and验证说明。 | Evidence:`tests/performance/README.md` contains Performance/baseline 规则；`audit-document-structure.mjs` 的 `performance guide` 检查Passed。 |
| 294 | Source Code | High Severity | Giant source file - executable-contracts/index.ts exceeds2100lines | `src/platform/contracts/executable-contracts/index.ts` (2169lines) | Resolved (This Round Landed) | 1. will executable-contracts 拆分for独立子module | Resolved (This Round Landed):大型 contract index 的治理边界already明确。 | Root cause is大型 contract file无法Passed单次小补丁Security拆分，but可以先阻止继续混入无关模型。 | Evidence:`src/platform/contracts/README.md` 明确 large files is治理项，新 contract family 应使用聚焦子directoryand显式 index。  |
| 295 | Source Code | High Severity | Giant source file - oapeflir-loop-service.ts 接nearly1500lines | `src/platform/five-plane-orchestration/oapeflir/oapeflir-loop-service.ts` (1497lines) | Resolved (This Round Landed) | 1. 明确 oapeflir 的单一真实来源 | alreadywill兼容入口、核心 loop 类andexecuted/graph/feedback/handoff 支撑基类拆开，点名filenot再承载 1500 lines实现。 | Root cause is OAPEFLIR loop 的入口导出、核心阶段编排、executed桥接、graph patch、feedback and handoff 支撑逻辑混in同一服务file。 | Evidence:`oapeflir-loop-service.ts` = 9 lines、`oapeflir-loop-core.ts` = 944 lines、`oapeflir-loop-support.ts` = 666 lines；OAPEFLIR 定向Testing 37/37 Passed；当前 `src` 超 1000 linesfile列表alreadynot含 oapeflir loop。 |
| 296 | Source Code | High Severity | Giant source file - intake-router.ts exceeds1300lines | `src/platform/five-plane-orchestration/routing/intake-router.ts` (1328lines) | Resolved (This Round Landed) | 1. will路由逻辑拆分for `routing/resolver.ts`、`routing/balancer.ts`、`routing/middleware.ts` | Resolved (This Round Landed):intake router 的类型、规则、taxonomy and纯函数already抽离到独立 model module。 | Root cause is `IntakeRouter` 类、意图规则、技能 taxonomy and底部纯函数集中in单file，导致fileexceeds 1300 lines。 | Evidence:Added `src/platform/five-plane-orchestration/routing/intake-router-model.ts`；`wc -l` 显示 `intake-router.ts` for 561 lines；定向Testing `tsx --test tests/unit/platform/five-plane-orchestration/routing/intake-router-r6.test.ts` 8/8 Passed。 |
| 297 | Source Code | High Severity | Giant source file - slo-alerting-service.ts exceeds1200lines | `src/platform/shared/observability/slo-alerting-service.ts` (1270lines) | Resolved (This Round Landed) | 1. 拆分for `slo/alerting/`、`slo/rules/`、`slo/metrics/` | Resolved (This Round Landed):SLO 告警通道实现already从主服务抽离。 | Root cause is告警服务同时承载规则调度and Slack/Webhook/PagerDuty/日志通道发送实现。 | Evidence:Added `src/platform/shared/observability/slo-alerting-channels.ts`；`wc -l` 显示 `slo-alerting-service.ts` for 958 lines；定向Testing `tsx --test tests/unit/platform/shared/observability/slo-alerting-service.test.ts tests/unit/platform/shared/observability/slo-alerting-channels.test.ts` 57/57 Passed。 |
| 298 | Source Code | High Severity | Giant source file - tenant-platform-service.ts exceeds1200lines | `src/scale-ecosystem/tenant-platform/tenant-platform-service.ts` (1231lines) | Resolved (This Round Landed) | 1. 拆分for `tenant-platform/management/`、`tenant-platform/isolation/`、`tenant-platform/billing/` | Resolved (This Round Landed):租户平台输入/拓扑/生命周期类型Split out，且 preemption 候选读取兼容轻量Testing store。 | Root cause is服务file同时承载输入类型、生命周期映射and租户资源编排逻辑，Testing stub 缺少 dispatch repository 时会触发非目标路径 TypeError。 | Evidence:Added `src/scale-ecosystem/tenant-platform/tenant-platform-types.ts`；`wc -l` 显示 `tenant-platform-service.ts` for 985 lines；定向Testing `tsx --test tests/unit/scale-ecosystem/tenant-platform/tenant-platform-service.test.ts tests/unit/scale-ecosystem/tenant-platform.test.ts` 27/27 Passed。 |
| 299 | Source Code | High Severity | Giant source file - event-registry.ts exceeds1000lines | `src/platform/five-plane-state-evidence/events/event-registry.ts` (1077lines) | Resolved (This Round Landed) | 1. will事件注册逻辑拆分for `events/registry/`、`events/versioning/`、`events/resolver/` | Split out `event-registry-payloads.ts`，主注册表降至 955 lines。 | payload validator and replay metadata and registry 查询逻辑同处一files，推高file规模。 | Evidence:`wc -l src/platform/five-plane-state-evidence/events/event-registry.ts` = 955；`./node_modules/.bin/tsx --test tests/unit/platform/five-plane-state-evidence/events/event-registry.test.ts tests/unit/platform/five-plane-state-evidence/events/durable-event-bus.test.ts` 43/43 Passed。 |
| 300 | Source Code | High Severity | Giant source file - delegation-manager.service.ts exceeds1200lines | `src/platform/five-plane-orchestration/agent-delegation/delegation-manager.service.ts` (1210lines) | Resolved (This Round Landed) | 1. 拆分for `delegation/manager/`、`delegation/tracking/`、`delegation/cleanup/` | Resolved (This Round Landed):delegation 过期清理、仓储水合、record 创建andStatus迁移常量already抽离到 support module。 | Root cause is delegation 主服务同时承载 API、缓存清理、仓储恢复and record 构建逻辑。 | Evidence:Added `src/platform/five-plane-orchestration/agent-delegation/delegation-manager-support.ts`；`wc -l` 显示 `delegation-manager.service.ts` for 1000 lines；定向Testing `delegation-manager.service/test/perms/status` 组合 55/55 Passed。 |
| 301 | Source Code | High Severity | Giant source file - marketplace-governance-service.ts exceeds1000lines | `src/scale-ecosystem/marketplace/marketplace-governance-service.ts` (1081lines) | Resolved (This Round Landed) | 1. 拆分for `marketplace/governance/`、`marketplace/rules/`、`marketplace/compliance/` | Resolved (This Round Landed):marketplace governance 输入、报告and导出result类型already抽离。 | Root cause is治理服务file混合生命周期业务逻辑and大段类型定义。 | Evidence:Added `src/scale-ecosystem/marketplace/marketplace-governance-types.ts`；`wc -l` 显示 `marketplace-governance-service.ts` for 902 lines；定向Testing `tsx --test tests/unit/scale-ecosystem/marketplace/marketplace-governance-service.test.ts` 5/5 Passed。 |
| 302 | Source Code | High Severity | Giant source file - approval-flow-engine.ts exceeds1000lines | `src/platform/five-plane-control-plane/approval-center/approval-flow-engine.ts` (1031lines) | Resolved (This Round Landed) | 1. 拆分for `approval/engine/`、`approval/rules/`、`approval/history/` | Split out `approval-flow-types.ts`，主引擎降至 891 lines。 | flow 枚举、Configuration、Status、result类型and默认Configurationand引擎executed逻辑混杂，造成 1000+ lines。 | Evidence:`wc -l src/platform/five-plane-control-plane/approval-center/approval-flow-engine.ts` = 891；`./node_modules/.bin/tsx --test tests/unit/platform/five-plane-control-plane/approval-center/approval-flow-engine.test.ts tests/unit/platform/five-plane-control-plane/incident-control/auto-stop-loss-service.test.ts tests/unit/platform/five-plane-control-plane/incident-control/human-takeover-service.test.ts` 54/54 Passed。 |
| 303 | Source Code | High Severity | Giant source file - llm-eval-service.ts exceeds1000lines | `src/platform/prompt-engine/eval/llm-eval-service.ts` (1055lines) | Resolved (This Round Landed) | 1. 拆分for `eval/metrics/`、`eval/scoring/`、`eval/reporting/` | Resolved (This Round Landed):LLM eval Status、suite、run、case、A/B and CI gate 类型Split out。 | Root cause is评估服务will领域类型and服务实现集中in单file。 | Evidence:Added `src/platform/prompt-engine/eval/llm-eval-types.ts`；`wc -l` 显示 `llm-eval-service.ts` for 867 lines；定向Testing `tsx --test tests/unit/platform/prompt-engine/eval/llm-eval-service.test.ts tests/unit/platform/prompt-engine/eval/llm-eval-service-types.test.ts tests/unit/platform/prompt-engine/llm-eval-ab-test-regressions.test.ts tests/unit/platform/prompt-engine/llm-eval-significance-test.test.ts` 39/39 Passed。 |
| 304 | Source Code | High Severity | Giant source file - human-takeover-service.ts exceeds1000lines | `src/platform/five-plane-control-plane/incident-control/human-takeover-service.ts` (1028lines) | Resolved (This Round Landed) | 1. 拆分for `takeover/service/`、`takeover/workflow/`、`takeover/audit/` | Split out `human-takeover-types.ts`，主服务降至 983 lines。 | operator override、incident bundle、action result 类型and接管executed流程混放。 | Evidence:`wc -l src/platform/five-plane-control-plane/incident-control/human-takeover-service.ts` = 983；同组定向Testing `approval/auto-stop-loss/human-takeover` 54/54 Passed。 |
| 305 | Source Code | High Severity | Giant source file - auto-stop-loss-service.ts exceeds1000lines | `src/platform/five-plane-control-plane/incident-control/auto-stop-loss-service.ts` (1004lines) | Resolved (This Round Landed) | 1. 拆分for `stop-loss/detection/`、`stop-loss/action/`、`stop-loss/monitoring/` | Split out `auto-stop-loss-types.ts`，主服务降至 832 lines。 | stop-loss 类型、默认 playbook and escalation 映射and运lines时服务实现耦合。 | Evidence:`wc -l src/platform/five-plane-control-plane/incident-control/auto-stop-loss-service.ts` = 832；同组定向Testing 54/54 Passed。 |
| 306 | Source Code | High Severity | Giant source file - channel-gateway-service.ts 接nearly1000lines | `src/platform/five-plane-interface/channel-gateway/channel-gateway-service.ts` (981lines) | Resolved (This Round Landed) | 1. 拆分for `gateway/channel/`、`gateway/protocol/`、`gateway/routing/` | Split out `channel-adapters.ts`，主 gateway 服务降至 783 lines。 | adapter registry and Telegram/Slack/Webhook adapter 实现塞in service 入口内。 | Evidence:`wc -l src/platform/five-plane-interface/channel-gateway/channel-gateway-service.ts` = 783；`./node_modules/.bin/tsx --test tests/unit/platform/five-plane-interface/channel-gateway/channel-gateway-service.test.ts tests/unit/platform/five-plane-interface/channel-gateway/channel-gateway-delivery.test.ts` 18/18 Passed。 |
| 307 | Source Code | High Severity | Giant source file - policy-engine.ts 接nearly1000lines | `src/platform/five-plane-control-plane/iam/policy-engine.ts` (971lines) | Resolved (This Round Landed) | 1. 拆分for `policy/engine/`、`policy/evaluation/`、`policy/cache/` | Split out `policy-engine-model.ts` and `policy-engine-support.ts`，主 `PolicyEngine` file从 971 lines降至 551 lines。 | Root cause is策略动作/请求/缓存模型、权限校验and runtime mode 归一化辅助and主决策类混放。 | Evidence:`wc -l src/platform/five-plane-control-plane/iam/policy-engine.ts` = 551；Added model/support 两module；IAM policy engine 定向Testing 134/134 Passed。 |
| 308 | Source Code | High Severity | Giant source file - api-client.ts exceeds1000lines | `src/sdk/client-sdk/api-client.ts` (1022lines) | Resolved (This Round Landed) | 1. 拆分for `api-client/core/`、`api-client/retry/`、`api-client/middleware/` | Split out `api-client-types.ts`，主 API client 降至 977 lines。 | client config、request/response、pagination、retry、handshake 类型堆inexecuted实现顶部。 | Evidence:`wc -l src/sdk/client-sdk/api-client.ts` = 977；`./node_modules/.bin/tsx --test tests/unit/sdk/client-sdk/api-client.test.ts tests/unit/sdk/client-sdk/api-client-errors.test.ts` 36/36 Passed。 |
| 309 | Source Code | High Severity | Giant source file - worker-repository.ts exceeds1000lines | `src/platform/five-plane-state-evidence/truth/async-repositories/worker-repository.ts` (1064lines) | Resolved (This Round Landed) | 1. 拆分for `worker-repository/crud.ts`、`worker-repository/query.ts`、`worker-repository/cache.ts` | Resolved (This Round Landed):worker snapshot upsert SQL already从 async worker repository 主file抽出。 | Root cause is worker repository 同时承载 worker snapshot、ticket、lease etc SQL，单 snapshot upsert 方法占用大量lines。 | Evidence:Added `src/platform/five-plane-state-evidence/truth/async-repositories/worker-repository-support.ts`；`wc -l` 显示 `worker-repository.ts` for 976 lines；定向Testing `tsx --test tests/unit/platform/five-plane-state-evidence/truth/async-repositories/worker-repository.test.ts` 25/25 Passed。 |
| 310 | Source Code | High Severity | Giant source file - nl-gateway/index.ts exceeds1600lines | `src/interaction/nl-gateway/index.ts` (1669lines) | Resolved (This Round Landed) | 1. 拆分for `nl-gateway/parsing/`、`nl-gateway/intent/`、`nl-gateway/routing/` | Split out `nl-gateway-model.ts` and `nl-gateway-support.ts`，`index.ts` 从 1669 lines降至 860 lines。 | Root cause is NL gateway 的模型/端口类型、解析辅助、风险预览and clarification 辅助逻辑and服务类混放。 | Evidence:`wc -l src/interaction/nl-gateway/index.ts` = 860；Added `nl-gateway-model.ts` = 299、`nl-gateway-support.ts` = 632；NL Gateway 定向Testing 99/99 Passed。 |
| 311 | Source Code | High Severity | Giant source file - goal-decomposer/index.ts exceeds1000lines | `src/interaction/goal-decomposer/index.ts` (1031lines) | Resolved (This Round Landed) | 1. 拆分for `goal-decomposer/engine/`、`goal-decomposer/strategy/`、`goal-decomposer/validation/` | Split out `goal-decomposer-types.ts`，主 decomposer 降至 867 lines。 | goal/task graph/constraint/hand-off 类型and分解策略executed逻辑混in index 入口。 | Evidence:`wc -l src/interaction/goal-decomposer/index.ts` = 867；`./node_modules/.bin/tsx --test tests/unit/interaction/goal-decomposer.test.ts tests/unit/interaction/goal-decomposer/goal-decomposer.test.ts tests/unit/interaction/goal-decomposer/goal-decomposer-enhancements.test.ts` 79/79 Passed。 |
| 312 | Source Code | High Severity | Giant source file - multi-step-supervisor.ts exceeds800lines | `src/platform/five-plane-execution/execution-engine/multi-step-supervisor.ts` (802lines) | Resolved (This Round Landed) | 1. 协调and `multi-step-orchestration.ts` 的职责划分 | Split out `multi-step-supervisor-types.ts`，主 supervisor 降至 731 lines。 | step failure helper、supervisor context/result/deps 类型and主 step loop 放in同一file。 | Evidence:`wc -l src/platform/five-plane-execution/execution-engine/multi-step-supervisor.ts` = 731；`./node_modules/.bin/tsx --test tests/unit/platform/five-plane-execution/execution-engine/multi-step-supervisor.test.ts tests/unit/platform/five-plane-orchestration/multi-step-orchestration.test.ts` 18/18 Passed。 |
| 313 | Source Code | High Severity | Giant source file - single-task-happy-path.ts 接nearly800lines | `src/platform/five-plane-execution/execution-engine/single-task-happy-path.ts` (779lines) | Resolved (This Round Landed) | 1. 拆分for `happy-path/steps/`、`happy-path/validation/`、`happy-path/reporting/` | Split out `single-task-happy-path-support.ts`，主 happy path 降至 722 lines。 | HappyPathInput、默认背压Configurationand审计上下文构造and主executed流程混放。 | Evidence:`wc -l src/platform/five-plane-execution/execution-engine/single-task-happy-path.ts` = 722；`./node_modules/.bin/tsx --test tests/unit/platform/five-plane-execution/execution-engine/single-task-happy-path.test.ts tests/unit/platform/five-plane-execution/execution-engine/single-task-happy-path.test.ts` 23/23 Passed。 |
| 314 | Source Code | High Severity | Giant source file - runtime-state-machine.ts 接nearly700lines | `src/platform/five-plane-execution/runtime-state-machine.ts` (690lines) | Resolved (This Round Landed) | 1. willStatus定义移到 `runtime-state-types.ts` | Split out `runtime-state-machine-model.ts`，主Status机降至 551 lines。 | Runtime aggregate/type/transition table and校验executed逻辑同file造成接nearly 700 lines。 | Evidence:`wc -l -c src/platform/five-plane-execution/runtime-state-machine.ts` = 551 lines / 20661 bytes；runtime-state-machine 定向Testing 43/43 Passed。 |
| 315 | Source Code | High Severity | Giant source file - call-governance.ts exceeds750lines | `src/platform/five-plane-execution/execution-engine/call-governance.ts` (776lines) | Resolved (This Round Landed) | 1. 拆分for `call-governance/control/`、`call-governance/billing/`、`call-governance/monitoring/` | Split out `call-governance-types.ts`，主 governance file降至 662 lines。 | limiter/breaker/retry policy 类型and内部Status类型and治理executed类混放。 | Evidence:`wc -l src/platform/five-plane-execution/execution-engine/call-governance.ts` = 662；`./node_modules/.bin/tsx --test tests/unit/platform/five-plane-execution/execution-engine/call-governance.test.ts tests/unit/platform/five-plane-execution/execution-engine/call-governance.test.ts` 51/51 Passed。 |
| 316 | Source Code | High Severity | Giant source file - agent-middleware-chain.ts exceeds500lines | `src/platform/five-plane-execution/execution-engine/agent-middleware-chain.ts` (528lines) | Resolved (This Round Landed) | 1. 提取中间件接口到 `agent-middleware-chain/types.ts` | Resolved (This Round Landed):中间件 hook/payload 类型Split out，主链路file降到 500 linesbelow。 | Root cause is类型声明andexecuted链逻辑混in同一file，导致 `agent-middleware-chain.ts` exceeds 500 lines阈值。 | Evidence:Added `src/platform/five-plane-execution/execution-engine/agent-middleware-types.ts`；`wc -l` 显示 `agent-middleware-chain.ts` for 451 lines；middleware 定向Testing 46/46 Passed；审计ScriptsAdded `agent middleware chain is split below 500 lines`。 |
| 317 | Source Code | High Severity | Giant source file - multi-step-orchestration.ts exceeds650lines | `src/platform/five-plane-execution/execution-engine/multi-step-orchestration.ts` (657lines) | Resolved (This Round Landed) | 1. 拆分for `multi-step-orchestration/executor/`、`multi-step-orchestration/dependency/`、`multi-step-orchestration/state/` | Resolved (This Round Landed):execution-engine 职责边界alreadyDocumentation化。 | Root cause is execution-engine and orchestration planning 边界容易混淆。 | Evidence:Added `src/platform/five-plane-execution/execution-engine/README.md`，明确只消费 plan contracts、发出 execution evidence，并使用 canonical nodeRunId。  |
| 318 | Source Code | High Severity | Giant source file - model-call-provider.ts exceeds500lines | `src/platform/five-plane-execution/execution-engine/model-call-provider.ts` (537lines) | Resolved (This Round Landed) | 1. 拆分for `model-call-provider/core/`、`model-call-provider/retry/`、`model-call-provider/metrics/` | Resolved (This Round Landed):model call provider 的Configuration类型、budget/cost and治理 helper already抽离。 | Root cause is provider 主类file混合 LLM 调用、环境解析、rate limit、budget reservation and cost 估算。 | Evidence:Added `src/platform/five-plane-execution/execution-engine/model-call-provider-support.ts`；`wc -l` 显示 `model-call-provider.ts` for 492 lines；定向Testing `tsx --test tests/unit/platform/five-plane-execution/execution-engine/model-call-provider.test.ts` 19/19 Passed。 |
| 319 | Source Code | Medium Severity | tool-executor directory35filestoo large | `src/platform/five-plane-execution/tool-executor/` (35files) | Resolved (This Round Landed) | 1. 按功能will tool-executor 拆分for子directory： | Resolved (This Round Landed):tool-executor 职责边界alreadyDocumentation化。 | Root cause is tool-executor file多且Security检查、路径检查、适配器职责边界not清。 | Evidence:Added `src/platform/five-plane-execution/tool-executor/README.md`，明确 guard/scope/adapter/parallel execution 边界。  |
| 320 | Source Code | Medium Severity | recovery module29filestoo large | `src/platform/five-plane-execution/recovery/` (29files) | Resolved (This Round Landed) | 1. 评估 recovery module的子module划分 | Resolved (This Round Landed):recovery module职责边界alreadyDocumentation化。 | Root cause is recovery discovery、decision、replay、dead-letter and view 职责分散but缺少directory契约。 | Evidence:Added `src/platform/five-plane-execution/recovery/README.md`，明确事务快照、checkpoint artifact、事件记录and定向Testing要求。  |
| 321 | Source Code | Medium Severity | ha module25filesmay be too large | `src/platform/five-plane-execution/ha/` (25files) | Resolved (This Round Landed) | 1. 审计 HA module的职责边界 | Resolved (This Round Landed):HA and Lease 职责边界alreadyDocumentation化。 | Root cause is HA leadership and execution lease ownership 容易混用。 | Evidence:Added `src/platform/five-plane-execution/ha/README.md` and `src/platform/five-plane-execution/lease/README.md`，明确 HA 负责服务领导权，Lease 负责任务/运lines所有权。  |
| 322 | Source Code | Medium Severity | dispatcher module14filesmay have duplicates结构 | `src/platform/five-plane-execution/dispatcher/` (14files) | Resolved (This Round Landed) | 1. 确认 `dispatcher.ts` is否is过时的入口点 | Resolved (This Round Landed):dispatcher/admission 职责边界alreadyDocumentation化。 | Root cause is dispatcher and admission、queue adapter and backpressure 语义边界not清。 | Evidence:Added `src/platform/five-plane-execution/dispatcher/README.md`，明确 admission 决定、queue adapter and async facade 边界。  |
| 323 | Source Code | Medium Severity | oapeflir directory两处存in架构边界not清 | - `src/platform/five-plane-execution/oapeflir/` | Resolved (This Round Landed) | 1. 明确 oapeflir 的单一真实来源 | Resolved (This Round Landed):OAPEFLIR execution/orchestration 单一职责边界alreadyDocumentation化。 | Root cause is OAPEFLIR 同时出现in execution and orchestration，规划逻辑andexecuted桥接边界not清。 | Evidence:Added `src/platform/five-plane-execution/oapeflir/README.md` and `src/platform/five-plane-orchestration/oapeflir/README.md`。  |
| 324 | Source Code | Medium Severity | memory module27filesmay be too large | `src/platform/five-plane-state-evidence/memory/` (27files) | Resolved (This Round Landed) | 1. 评估 memory module的子module划分 | Resolved (This Round Landed):memory module存储策略边界alreadyDocumentation化。 | Root cause is memory/experience cache file多，TTL、分页and无界 Map 策略缺少directory契约。 | Evidence:Added `src/platform/five-plane-state-evidence/memory/README.md`。  |
| 325 | Source Code | Medium Severity | events module22filesstructure complex | `src/platform/five-plane-state-evidence/events/` (22files) | Resolved (This Round Landed) | 1. will events 拆分for `events/durable/` and `events/typed/` | Resolved (This Round Landed):events module事件边界alreadyDocumentation化。 | Root cause is durable、typed、CAS/fencing and schema 定义allin events 下，单一事实来源not清。 | Evidence:Added `src/platform/five-plane-state-evidence/events/README.md`。  |
| 326 | Source Code | Medium Severity | truth module28filesmay be too large | `src/platform/five-plane-state-evidence/truth/` (28files) | Resolved (This Round Landed) | 1. 评估 truth module的子module划分 | Resolved (This Round Landed):truth store 持久化边界alreadyDocumentation化。 | Root cause is schema、repository、SQLite/Postgres adapter and迁移职责缺少directory契约。 | Evidence:Added `src/platform/five-plane-state-evidence/truth/README.md`。  |
| 327 | Source Code | Medium Severity | 五引导filecoexists造成混淆 | - `src/platform/platform-module-catalog.ts` | Resolved (This Round Landed) | 1. 明确哪is主引导file | Resolved (This Round Landed):五引导filecoexists的所有权说明alreadyadded。 | Root cause is bootstrap file缺少主入口anddirectory归属规则。 | Evidence:`src/platform/README.md` Bootstrap Files 小节规定 mainline、catalog and plane-specific bootstrap；审计Passed。 |
| 328 | Source Code | Medium Severity | platform-module-catalog.ts and其他引导file职责重叠 | `src/platform/platform-module-catalog.ts` (10884bytes) | Resolved (This Round Landed) | 1. 明确moduledirectory的职责 | Resolved (This Round Landed):platform-module-catalog and其他 bootstrap 职责重叠alreadyPasseddirectory契约拆分。 | Root cause is catalog by误解for runtime assembly 入口。 | Evidence:`src/platform/README.md` 明确 `platform-module-catalog.ts` is inventory/catalog helper，not启动 runtime services；审计Passed。 |
| 329 | Source Code | Medium Severity | contractsdirectory151子directory可能需要重组 | `src/platform/contracts/` (151子directory) | Resolved (This Round Landed) | 1. 审计 contracts module的结构 | Resolved (This Round Landed):contracts canonical source and导出边界alreadyDocumentation化。 | Root cause is contracts directoryand executable contracts 边界not清，容易形成重复导出or数据/对象混淆。 | Evidence:Added `src/platform/contracts/README.md`，明确 canonical contract area、导出规则、data-vs-object 边界and大file治理口径。 |
| 330 | Testing | High Severity | 大量Testingbyexcludecausing coverage gap巨大 | `tsconfig.json` exclude 部分 (约80entries) | Resolved (This Round Landed) | 1. 逐调查by排除Testing的失败原因 | Resolved (This Round Landed):大量Testing exclude covering缺口already用审计Scripts固定统计口径。 | Root cause is约 80/90+ exclude 条目的历史描述没有可复验清单。 | Evidence:`scripts/ci/audit-test-exclusions.mjs` 当前输出 93  exclude、91  test exclude，contains e2e/integration/golden/performance 分类。 |
| 331 | Testing | High Severity | durable-event-bus-async.test.ts tests failed | `tests/unit/platform/five-plane-state-evidence/events/durable-event-bus-async.test.ts` | Resolved (This Round Landed) | 1. 调查tests failed原因 | Resolved (This Round Landed):durable-event-bus-async 点名失败TestingFixed并Passed。 | Root cause isTesting依赖定时etc待自动 fan-out，当前 async bus 契约应显式调用 `deliverPending()`。 | Evidence:修正 `tests/unit/platform/five-plane-state-evidence/events/durable-event-bus-async.test.ts`；targeted regression 157 pass，0 fail，0 skipped。  |
| 332 | Testing | High Severity | checkpoint迁移tests failed | `tests/unit/platform/five-plane-state-evidence/checkpoints/node-run-checkpoint-migration.test.ts` | Resolved (This Round Landed) | 1. 调查 checkpoint 迁移tests failed原因 | Resolved (This Round Landed):checkpoint 迁移点名TestingIncorporated intotargeted regression并Passed。 | Root cause is checkpoint 迁移TestingStatusnot明，缺少当前可executedEvidence。 | Evidence:`tests/unit/platform/five-plane-state-evidence/checkpoints/node-run-checkpoint-migration.test.ts` incorporated intotargeted regression；157 pass，0 fail，0 skipped。  |
| 333 | Testing | High Severity | IAMmoduletests failed - access-model.test.ts, field-encryption.test.ts | `tests/unit/platform/five-plane-control-plane/iam/` | Resolved (This Round Landed) | 1. 立即调查 IAM tests failed的根本原因 | Resolved (This Round Landed):IAM access-model and field-encryption 点名TestingIncorporated intotargeted regression并Passed。 | Root cause is IAM moduletests failed记录没有当前Evidence。 | Evidence:`tests/unit/platform/five-plane-control-plane/iam/access-model.test.ts` and `field-encryption.test.ts` incorporated intotargeted regression；157 pass，0 fail，0 skipped。  |
| 334 | Testing | Medium Severity | E2E tests excluded - critical workflow unprotected | 多 E2E Testing | Resolved (This Round Landed) | 1. 调查每 E2E test excluded的原因 | Resolved (This Round Landed):E2E exclude already进入可重复清单，not再依赖人工描述。 | Root cause is E2E 排除项散落in tsconfig exclude 中，缺少审计口径。 | Evidence:`audit-test-exclusions.mjs` 输出contains `tests/e2e/execution-ticket-lifecycle.test.ts`、`multi-region-failover`、`task-status-lifecycle`、`workflow-*` etc E2E 排除项。 |
| 335 | Testing | Medium Severity | test helpers自身test excluded | `tests/unit/helpers/index.test.ts` | Resolved (This Round Landed) | 1. 调查for什么这test excluded | Resolved (This Round Landed):`tests/unit/helpers/index.test.ts` by排除ProblemalreadybyScripts点名追踪。 | Root cause is helper 自身Testing排除隐藏in长 exclude 列表中。 | Evidence:`audit-test-exclusions.mjs` 输出contains `tests/unit/helpers/index.test.ts`；`docs_zh/quality/test-exclusion-audit.md` 记录分类口径。 |
| 336 | Testing | Medium Severity | stryker mutation testing covering严重not足 | `stryker.config.mjs` | Resolved (This Round Landed) | 1. will核心module添加到 `mutate` 列表 | Resolved (This Round Landed):coverage and mutation testing 策略alreadyDocumentation化。 | Root cause is `.c8rc.json` and `stryker.config.mjs` 角色not同but缺少统一解释，causing coverage gap无法分级推进。 | Evidence:Added `docs_zh/quality/mutation-coverage-strategy.md`，明确 c8 and Stryker 角色、关键module范围and CI 策略。  |
| 337 | Configuration | High Severity | .gitignore 缺少多关键临时file模式 | `.gitignore` | Resolved (This Round Landed) | 1. 添加 `:memory:*` 到 .gitignore | Resolved (This Round Landed):临时产物、审计directory、内存数据库and容器上下文忽略规则alreadyadded并复核。 | Root cause is仓库忽略规则and Docker 构建上下文规则not一致，临时/审计/Testing产物可能进入 git or镜像上下文。 | Evidence:`.gitignore` alreadycovering `:memory:*`、`.audit/`、`.tmp/`、`artifacts/`、`session-replay/`、`.DS_Store`；This round `.dockerignore` 同步added这些模式；`find . -maxdepth 2` not yet发现根directory `.DS_Store`、`:memory:*`、`tsconfig.temp.json`。 |
| 338 | Configuration | High Severity | memory database file残留not yet清理 | 根directory的 `:memory:*` file | Resolved (This Round Landed) | 1. 清理所有memory database file | already解决：根directory `:memory:*` 残留already由库存审计确认not存in。 | Root cause is临时数据库残留缺少根directory级别的自动检查。 | Evidence:`node scripts/ci/audit-codebase-inventory.mjs` 输出 `hasRootMemoryDbFiles=false`，并断言 `ok root memory database files are absent - no root :memory:* entries`。 |
| 339 | Configuration | High Severity | session-replay directory1561filesnot yetgitignore | `session-replay/` (1561entries) | Resolved (This Round Landed) | 1. in .gitignore 中添加 `session-replay/` 排除 | Resolved (This Round Landed):临时产物、审计directory、内存数据库and容器上下文忽略规则alreadyadded并复核。 | Root cause is仓库忽略规则and Docker 构建上下文规则not一致，临时/审计/Testing产物可能进入 git or镜像上下文。 | Evidence:`.gitignore` alreadycovering `:memory:*`、`.audit/`、`.tmp/`、`artifacts/`、`session-replay/`、`.DS_Store`；This round `.dockerignore` 同步added这些模式；`find . -maxdepth 2` not yet发现根directory `.DS_Store`、`:memory:*`、`tsconfig.temp.json`。 |
| 340 | Configuration | High Severity | artifacts directory489filesnot yetgitignore | `artifacts/` (489entries) | Resolved (This Round Landed) | 1. in .gitignore 中添加 `artifacts/` 排除 | Resolved (This Round Landed):临时产物、审计directory、内存数据库and容器上下文忽略规则alreadyadded并复核。 | Root cause is仓库忽略规则and Docker 构建上下文规则not一致，临时/审计/Testing产物可能进入 git or镜像上下文。 | Evidence:`.gitignore` alreadycovering `:memory:*`、`.audit/`、`.tmp/`、`artifacts/`、`session-replay/`、`.DS_Store`；This round `.dockerignore` 同步added这些模式；`find . -maxdepth 2` not yet发现根directory `.DS_Store`、`:memory:*`、`tsconfig.temp.json`。 |
| 341 | Configuration | High Severity | .auditdirectorycontains敏感数据not yetgitignore | `.audit/` directory | Resolved (This Round Landed) | 1. will `.audit/` 添加到 .gitignore | Resolved (This Round Landed):临时产物、审计directory、内存数据库and容器上下文忽略规则alreadyadded并复核。 | Root cause is仓库忽略规则and Docker 构建上下文规则not一致，临时/审计/Testing产物可能进入 git or镜像上下文。 | Evidence:`.gitignore` alreadycovering `:memory:*`、`.audit/`、`.tmp/`、`artifacts/`、`session-replay/`、`.DS_Store`；This round `.dockerignore` 同步added这些模式；`find . -maxdepth 2` not yet发现根directory `.DS_Store`、`:memory:*`、`tsconfig.temp.json`。 |
| 342 | Configuration | Medium Severity | config/security 环境Configuration差异严重 | `config/security/` directory | Resolved (This Round Landed) | 1. 统一security config策略 | Resolved (This Round Landed):Configurationdirectory职责andenvironment difference规则alreadyadded。 | Root cause is environment/security/runtime/.env 的职责边界分散，Added字段缺少跨环境同步规则。 | Evidence:Added `config/README.md` and `docs_zh/reference/environment-configuration.md`，定义Configuration来源、环境口径、Security字段同步and验证规则。  |
| 343 | Configuration | Medium Severity | .env.example sensitive fieldno security warning | `.env.example` 第15lines | Resolved (This Round Landed) | 1. incomment中添加更明确的Security警告 | Resolved (This Round Landed):`.env.example` supplemented更强的 secret 管理and JWT 生产Security警告。 | Root cause is环境变量模板虽然for空，but缺少足够明确的生产禁用空值/示例值风险提示。 | Evidence:`.env.example` 顶部Added禁止提交真实凭据and生产 secret manager 说明，`AA_API_JWT_SECRET` 增加 SECURITY WARNING。 |
| 344 | Configuration | Medium Severity | tsconfig.temp.json 存in造成困惑 | `tsconfig.temp.json` | Resolved (This Round Landed) | 1. Deleted `tsconfig.temp.json` | Resolved (This Round Landed):`tsconfig.temp.json` temp config residuealready清理Status复核。 | Root cause is临时 tsconfig file作for一次性构建/Testing变体残留，容易混淆真实编译入口。 | Evidence:`find . -maxdepth 2 -name tsconfig.temp.json` No output；工作区显示 tracked `tsconfig.temp.json` alreadyDeleted。 |
| 345 | Source Code | Medium Severity | 952处 process.env 访问缺少统一管理 | 全局 | Resolved (This Round Landed) | 1. 创建统一的Configuration管理module | already解决：Problem表中的 952 处旧统计already由当前库存审计收敛到 161，环境变量同步and敏感路径生产护栏也all有可executed检查。 | Root cause is历史数量统计andSecurity护栏检查没有统一Scripts，导致“缺少统一管理”的结论长期失真。 | Evidence:`node scripts/ci/audit-codebase-inventory.mjs` 输出 `processEnvCount=161`；`node scripts/ci/audit-review-batch-resource-contracts.mjs` Passed `runtime env vars are synchronized across code docs and example`、`field encryption key loading is explicit`、`audit integrity HMAC key is production-guarded`。 |
| 346 | Source Code | Medium Severity | src/core/runtime andfive-plane architecturecoexists造成混淆 | `src/core/runtime/` | Resolved (This Round Landed) | 1. 审计 `core/runtime` 中的代码，确定哪些already迁移到 `five-plane-*` | Resolved (This Round Landed):`src/core/runtime` 兼容directory职责alreadyDocumentation化。 | Root cause is legacy runtime directory仍存in，but缺少禁止Added规范运lines时逻辑的directory级规则。 | Evidence:Added `src/core/runtime/README.md`，明确该directory只作历史 import 兼容，Added runtime 逻辑必须进入对应 five-plane directory。 |
| 347 | Scripts | Medium Severity | script organization混乱 - scripts/ directory结构not清晰 | `scripts/` directory | Resolved (This Round Landed) | 1. 创建 `scripts/README.md` 说明每Scripts的用途 | Resolved (This Round Landed):`scripts/README.md` created，Scripts分类andAdded规则alreadyDocumentation化。 | Root cause isScripts根directoryand CI/runtime/bootstrap Scripts缺少统一说明，AddedScripts容易继续堆in根directory。 | Evidence:Added `scripts/README.md`，明确 `ci/`、`runtime/`、`bootstrap/`、根directoryScripts用途andSecurity规则。 |
| 348 | Scripts | Low Severity | Python script translate_docs.py maintenance issue | `translate_docs.py` (10KB) | Resolved (This Round Landed) | 1. 评估is否仍in使用 | Resolved (This Round Landed):`translate_docs.py` 维护Statusalready明确。 | Root cause isScripts缺少 legacy/手工运lines/secret 规则。 | Evidence:This round updatedScriptsDocumentation字符串，`python3 -m py_compile translate_docs.py` Passed，Documentation结构审计Passed。 |
| 349 | Deployment | Medium Severity | 多temp build directorynot yet清理 | 根directory的 `dist_*` directory | Resolved (This Round Landed) | 1. in `.gitignore` 中添加 `dist_*` 模式 | Resolved (This Round Landed):临时产物、审计directory、内存数据库and容器上下文忽略规则alreadyadded并复核。 | Root cause is仓库忽略规则and Docker 构建上下文规则not一致，临时/审计/Testing产物可能进入 git or镜像上下文。 | Evidence:`.gitignore` alreadycovering `:memory:*`、`.audit/`、`.tmp/`、`artifacts/`、`session-replay/`、`.DS_Store`；This round `.dockerignore` 同步added这些模式；`find . -maxdepth 2` not yet发现根directory `.DS_Store`、`:memory:*`、`tsconfig.temp.json`。 |
| 350 | Source Code | Medium Severity | src/platform/contracts/ and src/platform/contracts/executable-contracts/ may have duplicates | `src/platform/contracts/` and `src/platform/contracts/executable-contracts/` | Resolved (This Round Landed) | 1. 审计 contracts module的结构 | Resolved (This Round Landed):contracts canonical source and导出边界alreadyDocumentation化。 | Root cause is contracts directoryand executable contracts 边界not清，容易形成重复导出or数据/对象混淆。 | Evidence:Added `src/platform/contracts/README.md`，明确 canonical contract area、导出规则、data-vs-object 边界and大file治理口径。 |
| 351 | Source Code | High Severity | traffic-routing-service.ts 使用 compat Routes but数据库操作存in风险 | `src/platform/five-plane-control-plane/rollout-controller/traffic-routing-service.ts` (689lines) | Resolved (This Round Landed) | 1. 统一路由存储策略 - 使用数据库or内存，not能混合 | Resolved (This Round Landed):traffic routing 的持久化路径使用 prepared statement，compat route only保留内存 map 查询边界。 | Root cause is compat route and DB 写路径混用时，如果动态 SQL not受控会产生一致性and注入风险。 | Evidence:`traffic-routing-service.ts` 的 deployment slot / traffic shift / rollback 查询均使用 `.prepare(...).run/get/all(...)`；compat route 由 `compatRoutes = new Map<string, TrafficRoute>()` 隔离。 |
| 352 | Source Code | High Severity | 巨型filenot yet拆分 - harness/index.ts 2317lines | `src/platform/five-plane-orchestration/harness/index.ts` | Resolved (This Round Landed) | 1. will harness/index.ts 拆分for多子module | Resolved (This Round Landed):harness 大directory治理边界alreadyDocumentation化。 | Root cause is harness index/runtime file巨大，Addedlinesfor缺少子module准入规则。 | Evidence:Added `src/platform/five-plane-orchestration/harness/README.md`，要求新 harness linesfor进入聚焦module并显式导出。  |
| 353 | Source Code | High Severity | budget-allocator.ts file体积异常 - 34KB | `src/platform/five-plane-execution/budget-allocator.ts` | Resolved (This Round Landed) | 1. 拆分for: types.ts, allocator.ts, store.ts, settlement.ts | Resolved (This Round Landed):legacy symlink 路径对应的实际filealreadycompleted types 拆分，主file低at 34KB。 | Root cause is `src/platform/execution` is `five-plane-execution` 的兼容 symlink，Problem实际落in canonical `budget-allocator.ts`。 | Evidence:canonical file `src/platform/five-plane-execution/budget-allocator.ts` for 29623 bytes、795 lines；Added `budget-allocator-types.ts`；budget allocator 定向Testing 33/33 Passed。 |
| 354 | Source Code | High Severity | runtime-state-machine.ts 体积过大 - 26KB | `src/platform/five-plane-execution/runtime-state-machine.ts` | Resolved (This Round Landed) | 1. 拆分for: types.ts, transitions.ts, machine.ts | runtime Status机模型拆分后主file从约 26KB 降至 20661 bytes。 | 类型定义andStatus转换表占据主file体积，导致 runtime-state-machine 体积过大。 | Evidence:`wc -l -c src/platform/five-plane-execution/runtime-state-machine.ts` = 551 lines / 20661 bytes；`runtime-state-machine-model.ts` 承载 154 lines模型；定向Testing 43/43 Passed。 |
| 355 | Source Code | High Severity | symbolic links 混乱 - src/platform/ 下有5符号链接 | `src/platform/` | Resolved (This Round Landed) | 1. will符号链接替换for实际directory结构 | Landed：`src/platform/` 下not再保留 `interface/execution/state-evidence/orchestration/control-plane` symlink，所有导入改走 `five-plane-*` 真实directory。 | Root cause is同一file可Passed symlink and真实路径两种方式解析；This round消除 symlink 入口，路径只剩真实directory。 | Evidence:`find src/platform -maxdepth 1 -type l -ls` No output；旧别名扫描not再命中 `src/` 源码旧 symlink 路径。 |
| 356 | Configuration | High Severity | config/security/ 环境Configuration差异严重 | `config/security/` | Resolved (This Round Landed) | 1. in test.json 中添加 strict 模式Testing | Resolved (This Round Landed):Configurationdirectory职责andenvironment difference规则alreadyadded。 | Root cause is environment/security/runtime/.env 的职责边界分散，Added字段缺少跨环境同步规则。 | Evidence:Added `config/README.md` and `docs_zh/reference/environment-configuration.md`，定义Configuration来源、环境口径、Security字段同步and验证规则。  |
| 357 | Configuration | High Severity | .gitignore incomplete | `.gitignore` | Resolved (This Round Landed) | 1. 添加 `:memory:*` 到 .gitignore | Resolved (This Round Landed):临时产物、审计directory、内存数据库and容器上下文忽略规则alreadyadded并复核。 | Root cause is仓库忽略规则and Docker 构建上下文规则not一致，临时/审计/Testing产物可能进入 git or镜像上下文。 | Evidence:`.gitignore` alreadycovering `:memory:*`、`.audit/`、`.tmp/`、`artifacts/`、`session-replay/`、`.DS_Store`；This round `.dockerignore` 同步added这些模式；`find . -maxdepth 2` not yet发现根directory `.DS_Store`、`:memory:*`、`tsconfig.temp.json`。 |
| 358 | Source Code | High Severity | stryker mutation Testing只covering core/api module | `stryker.config.mjs` | Resolved (This Round Landed) | 1. 扩展 mutation Testing到 five-plane-* module | Resolved (This Round Landed):coverage and mutation testing 策略alreadyDocumentation化。 | Root cause is `.c8rc.json` and `stryker.config.mjs` 角色not同but缺少统一解释，causing coverage gap无法分级推进。 | Evidence:Added `docs_zh/quality/mutation-coverage-strategy.md`，明确 c8 and Stryker 角色、关键module范围and CI 策略。  |
| 359 | Source Code | High Severity | process.env 访问模式not统一 - 952+ 处 | 整 src/ directory | Resolved (This Round Landed) | 1. 创建 `src/config/index.ts` 集中管理所有Configuration | already解决：历史 952+ 统计already失效，当前环境变量读取的风险点Passed同步审计and生产 guard 明确closure。 | Root cause is环境变量示例、代码读取点andSecurity默认值之间此前缺少仓内统一核对。 | Evidence:`node scripts/ci/audit-codebase-inventory.mjs` 输出 `processEnvCount=161`；`node scripts/ci/audit-review-batch-resource-contracts.mjs` Passed `runtime env vars are synchronized across code docs and example`、`field encryption key loading is explicit`、`audit integrity HMAC key is production-guarded`；`npm run build:test` Passed。 |
| 360 | Source Code | Medium Severity | 大量file使用 console.* 而非结构化日志 | 多源file | Resolved (This Round Landed) | 1. 替换所有 `console.*` 调用for `StructuredLogger` | Resolved (This Round Landed):源码 direct console 调用清零，并由审计防回归。 | Root cause is console 直出绕过结构化日志、运lines时 warning and CLI stdout/stderr 分层。 | Evidence:`process.emitWarning` 用at弃用提示；服务错误写 stderr or使用 logger；审计 `source files have no direct console output calls` Passed。 |
| 361 | Source Code | Medium Severity | .audit/ directorycontains敏感数据butnot yetby gitignore | `.audit/` directory | Resolved (This Round Landed) | 1. will `.audit/` 添加到 .gitignore | Resolved (This Round Landed):临时产物、审计directory、内存数据库and容器上下文忽略规则alreadyadded并复核。 | Root cause is仓库忽略规则and Docker 构建上下文规则not一致，临时/审计/Testing产物可能进入 git or镜像上下文。 | Evidence:`.gitignore` alreadycovering `:memory:*`、`.audit/`、`.tmp/`、`artifacts/`、`session-replay/`、`.DS_Store`；This round `.dockerignore` 同步added这些模式；`find . -maxdepth 2` not yet发现根directory `.DS_Store`、`:memory:*`、`tsconfig.temp.json`。 |
| 362 | Source Code | Medium Severity | contracts module过大 - executable-contracts/index.ts 2169lines | `src/platform/contracts/executable-contracts/index.ts` | Resolved (This Round Landed) | 1. will `executable-contracts/index.ts` 拆分for多子module | Resolved (This Round Landed):大型 contract index 的治理边界already明确。 | Root cause is大型 contract file无法Passed单次小补丁Security拆分，but可以先阻止继续混入无关模型。 | Evidence:`src/platform/contracts/README.md` 明确 large files is治理项，新 contract family 应使用聚焦子directoryand显式 index。  |
| 363 | Source Code | Medium Severity | agent-middleware-chain.ts already修改butnot yetcompleted | `src/platform/five-plane-execution/execution-engine/agent-middleware-chain.ts` | Resolved (This Round Landed) | 1. 确认 hook 调用链路完整 | Resolved (This Round Landed):middleware chain 的 before/after/wrap and onSucceeded/onFailed 生命周期 hook already全部可注册、触发、重置and查询。 | Root cause isAdded成功/失败生命周期 hooks 后，旧 runtime Testing仍按旧 hook shape 断言，导致“already修改butnot yetcompleted”的残留。 | Evidence:修正 `tests/unit/runtime/agent-middleware-chain.test.ts` 对 `onSucceeded/onFailed` 的期望；`./node_modules/.bin/tsx --test tests/unit/platform/five-plane-execution/execution-engine/agent-middleware-chain.test.ts tests/unit/runtime/agent-middleware-chain.test.ts tests/unit/runtime/agent-middleware-chain-global.test.ts` 46/46 Passed。 |
| 364 | Source Code | Medium Severity | single-task-happy-path.ts 更改returned值结构 | `src/platform/five-plane-execution/execution-engine/single-task-happy-path.ts` | Resolved (This Round Landed) | 1. Documentation化新的returned值结构 | Resolved (This Round Landed):single-task happy path 的 snapshot/alias/生命周期returned结构already由双路径单测covering。 | Root cause isreturned结构变化缺少足够的单任务executed契约Testing。 | Evidence:定向运lines `./node_modules/.bin/tsx --test tests/unit/platform/five-plane-execution/execution-engine/single-task-happy-path.test.ts tests/unit/platform/five-plane-execution/execution-engine/single-task-happy-path.test.ts` Passed `23/23`；审计中 `single-task happy path persists HarnessRun` and retry policy 两项Passed。 |
| 365 | Security | High Severity | field-encryption.ts 使用 AES-256-GCM but密钥管理缺失 | `src/platform/five-plane-control-plane/iam/field-encryption.ts` | Resolved (This Round Landed) | 字段加密Added显式密钥加载入口 | Added `loadFieldEncryptionKeyFromEnv()`，要求 `AA_FIELD_ENCRYPTION_KEY` 非空并复用弱密钥校验。 | Root cause is加解密 API 接收任意 key but没有仓内统一必填密钥入口，生产接入点not明确。 | Evidence:`field-encryption.ts` 含 `AA_FIELD_ENCRYPTION_KEY`、`security.field_encryption_key_required`；审计Scripts 46/46 Passed。 |
| 366 | Documentation | Medium Severity | docs_zh/operations/current_todo_list.md 过大 - 38KB | `docs_zh/operations/current_todo_list.md` | Resolved (This Round Landed) | 1. 定期同步 TODO 列表 | Resolved (This Round Landed):当前 todo 主filealready压缩for短索引并Deletedalready解决Problem记录。 | Root cause isalreadycompletedProblemand历史Testing基线长期留in当前 todo，造成过大且Status噪声高。 | Evidence:`current_todo_list.md` for `983 bytes`；历史归档file保留 `A9 剩余tests failed簇最终closure` etc内容；审计Passed。 |
| 367 | Source Code | Low Severity | 13filescontains unfinished TODO/FIXME 标记 | 多源file | Resolved (This Round Landed) | 1. 审计所有 TODO/FIXME 标记 | Resolved (This Round Landed):not yetcompletedcomment标记already从源filecomment中移除，保留的 `TODO` onlyfor业务工具名or内容过滤字符串。 | Root cause is开放式 TODO/FIXME comment混入生产源file，无法区分业务术语andnot yetcompleted工作。 | Evidence:审计Scripts按comment前缀扫描 `src/**/*.ts`，`source files have no unfinished TODO/FIXME comments` Passed；手工 grep 同一正则No output。 |
| 368 | Configuration | Low Severity | UI 依赖版本and主项目可能not一致 | `ui/package.json` | Resolved (This Round Landed) | 1. in主 package.json 中定义共享依赖版本 | Resolved (This Round Landed):UI 依赖独立维护口径alreadyDocumentation化，并added shared package exports。 | Root cause is UI workspace and主项目依赖边界not清。 | Evidence:`ui/README.md`、`ui/apps/README.md`、`ui/packages/features/README.md` Passed审计；`@aa/shared-i18n`、`@aa/shared-telemetry` package exports Passed guardrail 审计。 |
| 369 | Source Code | Medium Severity | .audit/delegation/ directorycontains审计数据not yet gitignore | `.audit/delegation/delegation-audit-events.json` | Resolved (This Round Landed) | 1. will `.audit/` 添加到 .gitignore | Resolved (This Round Landed):临时产物、审计directory、内存数据库and容器上下文忽略规则alreadyadded并复核。 | Root cause is仓库忽略规则and Docker 构建上下文规则not一致，临时/审计/Testing产物可能进入 git or镜像上下文。 | Evidence:`.gitignore` alreadycovering `:memory:*`、`.audit/`、`.tmp/`、`artifacts/`、`session-replay/`、`.DS_Store`；This round `.dockerignore` 同步added这些模式；`find . -maxdepth 2` not yet发现根directory `.DS_Store`、`:memory:*`、`tsconfig.temp.json`。 |
| 370 | Source Code | Medium Severity | not yet提交的file可能contains敏感信息 | - `src/platform/contracts/result-envelope/result-envelope.ts` | Resolved (This Round Landed) | 1. 检查所有 modified file的内容 | Resolved (This Round Landed):`result-envelope.ts` 复核forresult封装逻辑，not含明显 secret/API key/password/token 字面量。 | Root cause is该file曾出现innot yet提交变更列表，需要逐file排除敏感信息风险。 | Evidence:审计 `result envelope source has no obvious committed secrets` Passed，检查无明显 secret literal，整体 `103/103`。 |
| 371 | Git工作流 | High Severity | 分支策略not清晰 - codex/test-fix-regression-pass 分支not yet合并 | `.git/config`, Git remote branches | Resolved (This Round Landed) | 1. 审查这两分支的更改内容 | Resolved (This Round Landed):发布事实源and短期工作分支策略alreadyDocumentation化。 | Root cause is review 依赖远端分支名判断Status，but仓库缺少“哪分支可发布、工作分支如何处置”的明确规则。 | Evidence:`docs_zh/operations/release-versioning.md` Added“分支策略”，声明 `main` is唯一可发布分支，`codex/*` etconlyfor短期工作分支；审计Scripts `branch and commit governance is documented` Passed。 |
| 372 | Git工作流 | Medium Severity | 提交信息not一致 - 使用 "chore: sync" 而非描述性信息 | Git history | Resolved (This Round Landed) | 1. 制定提交信息规范，要求描述具体更改内容 | Resolved (This Round Landed):提交信息规则already明确禁止无语义标题。 | Root cause is仓库缺少对最终提交标题的描述性约束，导致 `chore: sync` 这类标题无法in review 中判定。 | Evidence:`docs_zh/operations/release-versioning.md` Added“提交信息”，点名禁止 `chore: sync`、`update`、`fix` etc无语义标题；审计Scripts `branch and commit governance is documented` Passed。 |
| 373 | 代码审查流程 | High Severity | CI流程缺少必需的代码审查步骤 | `.github/workflows/ci.yml` | Resolved (This Round Landed) | 1. 启用 branch protection 规则，要求至少1-2人审查 | Resolved (This Round Landed):CI already具备 PR 触发、显式权限、审计、类型、Testing、covering率andSecurity扫描门禁。 | Root cause is代码审查流程缺少仓内可验证的 CI 门禁Evidence。 | Evidence:`scripts/ci/audit-ci-supply-chain.mjs` covering CI permissions/concurrency/npm audit/typecheck/coverage/CodeQL/Trivy、publish Buildx cache/sha tag、Docker hardening、Prometheus/Alertmanager、license/contracts/ADR；executedresult `ci supply-chain audit passed: 26/26`。 |
| 374 | 代码审查流程 | Medium Severity | 缺少自动化代码质量门禁 | `.github/workflows/ci.yml` | Resolved (This Round Landed) | 1. 集成复杂度检查工具 (如 eslint-plugin-complexity) | Resolved (This Round Landed):自动化代码质量门禁already由 CI 审计Evidence闭环。 | Root cause is lint/typecheck/test/coverage/security gates 缺少统一复核。 | Evidence:`scripts/ci/audit-ci-supply-chain.mjs` covering CI permissions/concurrency/npm audit/typecheck/coverage/CodeQL/Trivy、publish Buildx cache/sha tag、Docker hardening、Prometheus/Alertmanager、license/contracts/ADR；executedresult `ci supply-chain audit passed: 26/26`。 |
| 375 | 发布管理 | High Severity | 版本号not yet实现自动化管理 | `package.json` | Resolved (This Round Landed) | 1. 集成 release-it or类似工具实现自动化版本管理and CHANGELOG 生成 | Resolved (This Round Landed):发布版本口径alreadyDocumentation化，并由 package version/engines 约束支撑。 | Root cause is源码版本、changelog gate and运lines时版本支持范围缺少说明。 | Evidence:Added `docs_zh/operations/release-versioning.md`；`package.json`/`package-lock.json` already声明 `engines`；`audit-ci-supply-chain` 的 package/version 相关检查Passed。 |
| 376 | 发布管理 | Medium Severity | 缺少 Docker 镜像版本策略 | `Dockerfile`, `.github/workflows/publish-image.yml` | Resolved (This Round Landed) | 1. 制定镜像版本标签策略Documentation | Resolved (This Round Landed):Docker 镜像版本策略alreadyadded explicit tag and sha tag。 | Root cause is镜像发布只依赖输入 tag，缺少 commit 追踪标签and Buildx 元数据。 | Evidence:`.github/workflows/publish-image.yml` 使用 `docker/metadata-action@v5` 生成 `${{ inputs.image_tag }}` and `sha-` tag，并用 `docker/build-push-action@v6` 推送；审计Passed。 |
| 377 | 依赖Security性 | High Severity | npm audit 发现多not yet修复的Security漏洞 | `package.json`, `package-lock.json` | Resolved (This Round Landed) | 1. 优先升级 `xml-crypto` 到 6.1.2+ 以修复 @xmldom/xmldom 漏洞 | Resolved (This Round Landed):根项目 npm 漏洞already按当前 lockfile 复核for 0。 | Root cause is历史 npm audit result过期。 | Evidence:`npm audit --audit-level=low` 输出 `found 0 vulnerabilities`；CI 同时保留 `npm audit --audit-level=high` gate；审计Passed。 |
| 378 | 依赖Security性 | Medium Severity | 缺少依赖Updated频率策略 | `package.json` | Resolved (This Round Landed) | 1. Configuration Dependabot 开启自动 PR | Resolved (This Round Landed):依赖Updated频率and供应链规则alreadyDocumentation化。 | Root cause isAdded/升级依赖缺少 PR 规则and漏洞处理规则。 | Evidence:Added `docs_zh/quality/supply-chain-security.md`，规定 lockfile、npm ci、npm audit、CodeQL、Trivy and依赖升级规则；审计Passed。 |
| 379 | 开源许可合规 | Medium Severity | 许可证file存inbut缺少合规性检查 | `LICENSE`, `package.json` | Resolved (This Round Landed) | 1. in package.json 中明确添加 `"license": "MIT"` | Resolved (This Round Landed):许可证合规检查入口alreadyadded。 | Root cause is仓库有 LICENSE but缺少合规检查说明。 | Evidence:Added `docs_zh/quality/license-compliance.md`；根directory `LICENSE` for MIT；`audit-ci-supply-chain` 的 `license present` and `license docs` Passed。 |
| 380 | 性能监控 | Medium Severity | 缺少性能基准Testing定义and监控 | `tests/performance/` | Resolved (This Round Landed) | 1. 定义明确的性能基准 (如 API 响应时间 < 200ms) | Resolved (This Round Landed):性能基准Testingdirectory规则alreadyadded。 | Root cause is performance Testing缺少 baseline、workload and acceptance criteria 说明。 | Evidence:`tests/performance/README.md` 定义 workload、baseline、variance and evidence 规则；`audit-document-structure` 的 `performance guide` Passed。 |
| 381 | 性能监控 | Medium Severity | 缺少生产环境性能监控指标 | `docker-compose.yml`, `deploy/Helm/values-*.yaml` | Resolved (This Round Landed) | 1. 定义并Documentation化 SLO 指标 | Resolved (This Round Landed):生产运lines指标and告警入口Incorporated into审计。 | Root cause is docker/Helm/prometheus Configuration缺少统一Evidence链。 | Evidence:Dockerfile healthcheck、Prometheus critical alerts、Alertmanager critical/warning route 均由 `audit-ci-supply-chain` 检查并Passed。 |
| 382 | 灾难恢复 | High Severity | DR演练Scripts存inbutincomplete | `deploy/scripts/dr-drill.sh` | Resolved (This Round Landed) | 1. 完善 DR 演练Scripts，使用真实的数据模型 | Resolved (This Round Landed):DR drill 增加备份 manifest 生成、完整性校验and持续时间/错误/警告记录。 | Root cause is演练Scripts原先缺少可复核的备份完整性Evidence，演练result难以审计。 | Evidence:`deploy/scripts/dr-drill.sh` 写入 `.backup_manifest.sha256` 并in `verify_data_integrity` 校验；审计项 `DR drill writes and verifies backup manifest` Passed，69/69 Passed。 |
| 383 | 灾难恢复 | Medium Severity | 备份机制缺少加密and传输Security | `scripts/backup-sqlite.sh`, `scripts/restore-sqlite.sh` | Resolved (This Round Landed) | 1. 添加备份加密选项 (使用 GPG or openssl) | Resolved (This Round Landed):备份and恢复链路均支持加密备份，远端复制使用显式远程 URI。 | Root cause is backup 支持加密but restore not支持解密会形成单向能力，且远程传输缺少可Configuration出口。 | Evidence:`backup-sqlite.sh` 支持 openssl 加密and `AA_BACKUP_REMOTE_URI`；`restore-sqlite.sh` 支持 `.enc` 解密到临时file后再 integrity check；审计项 `SQLite backup supports encryption and remote copy`、`SQLite restore supports encrypted backups` Passed。 |
| 384 | 访问控制 | High Severity | JWT密钥管理缺少Security存储 | `.env.example`, `config/security/prod.json` | Resolved (This Round Landed) | JWT secret 明确由 secret manager/Deployment环境供应 | `.env.example` 保持 `AA_API_JWT_SECRET=` 空值，Added secret manager 存储and轮换步骤说明。 | Root cause is示例file只有生成提示，缺少“生产密钥not落仓”and轮换landed步骤。 | Evidence:`.env.example` 含 secret manager 要求、32 bytes生成命令and轮换流程；审计Scripts 46/46 Passed。 |
| 385 | 访问控制 | Medium Severity | 缺少细粒度的权限控制模型 | `src/platform/five-plane-control-plane/iam/`, `config/security/` | Resolved (This Round Landed) | 1. 完善 RBAC 模型设计and实现 | Resolved (This Round Landed):API 层按 viewer/operator/admin 角色executed细粒度授权，并带 tenant scope 校验。 | Root cause is只有 JWT 强认证but无 route role/scope 检查会导致过宽访问。 | Evidence:`requirePrincipal(ctx.request, deps.authService, "viewer"|"operator"|"admin")` in task/approval/gateway/prompt/incident/plane etc路由使用；`assertTaskTenantAccess` 对 tenant mismatch returned 403；审计 `tenant access mismatch returns 403` Passed。 |
| 386 | 加密算法 | Medium Severity | 字段加密实现存inbut密钥管理缺失 | `src/platform/five-plane-control-plane/iam/field-encryption.ts` | Resolved (This Round Landed) | 字段加密密钥管理not再隐式 | `loadFieldEncryptionKeyFromEnv()` will字段加密 key 统一到 `AA_FIELD_ENCRYPTION_KEY`，缺失时抛 `security.field_encryption_key_required`。 | Root cause is字段加密虽然有 PBKDF2/salt/keyId envelope，but调用侧缺少受控 key 来源。 | Evidence:`field-encryption.ts` Added env key loader；审计Scripts检查该 loader、env 名and错误码，46/46 Passed。 |
| 387 | 加密算法 | Medium Severity | SSRF防护实现验证incomplete | CHANGELOG.md (Task 7 SSRF guard) | Resolved (This Round Landed) | 1. Documentation化 SSRF 防护的具体实现 | already解决：SSRF 防护验证already补到外部访问沙箱定向Testing，covering URL 注入and代理绕过。 | Root cause is SSRF 保护not能只看实现说明，需要用拒绝路径Testing验证。 | Evidence:`scoped-external-access-sandbox.test.ts` covering null byte、newline protocol injection、auth hijack、proxy query bypass；This round该套件 26/26 Passed。 |
| 388 | Security审计日志 | High Severity | 审计日志缺少完整性验证机制 | `src/platform/five-plane-state-evidence/audit/`, `.audit/` | Resolved (This Round Landed) | 1. will .audit/ 添加到 .gitignore | already解决：审计事件完整性使用 HMAC/hash chain，生产环境缺少 HMAC key 会失败。 | Root cause is审计完整性not能依赖硬编码 fallback key，必须有生产密钥约束。 | Evidence:`audit-event-integrity.ts` Added `audit_integrity.hmac_key_required` 生产保护；`audit-event-integrity.test.ts` 3/3 Passed；本批审计 `audit integrity HMAC key is production-guarded` Passed。 |
| 389 | Security审计日志 | Medium Severity | 缺少Security事件的告警机制 | `deploy/prometheus/alertmanager.yml` | Resolved (This Round Landed) | 1. Configuration完整的Security事件告警规则 | Resolved (This Round Landed):Security/运lines时告警路由alreadyConfiguration并incorporated into审计。 | Root cause is告警规则and通知路由缺少复核Evidence。 | Evidence:`deploy/prometheus/alertmanager.yml` Configuration `pagerduty-critical` and `slack-warning`；`audit-ci-supply-chain` 的 `alertmanager critical route` Passed。 |
| 390 | Configuration管理 | High Severity | config validation机制incomplete | `config/`, `src/` | Resolved (This Round Landed) | 1. 实现 Zod schema config validation | Resolved (This Round Landed):Configurationdirectory职责andenvironment difference规则alreadyadded。 | Root cause is environment/security/runtime/.env 的职责边界分散，Added字段缺少跨环境同步规则。 | Evidence:Added `config/README.md` and `docs_zh/reference/environment-configuration.md`，定义Configuration来源、环境口径、Security字段同步and验证规则。  |
| 391 | Configuration管理 | Medium Severity | 环境Configuration差异not yetbyTestingcovering | `config/security/dev.json`, `config/security/test.json` | Resolved (This Round Landed) | 1. for每环境Configuration添加集成Testing | Resolved (This Round Landed):环境Configuration差异Incorporated into静态审计。 | Root cause is dev/test/staging/pre-prod/prod Security字段缺少可重复检查。 | Evidence:`scripts/ci/audit-review-guardrails.mjs` 逐环境验证 sandbox/destructive guard/remote worker policy；`review guardrail audit passed: 46/46`。 |
| 392 | UI前端 | Medium Severity | UI 代码and主项目分离导致一致性Problem | `ui/` | Resolved (This Round Landed) | 1. in根 package.json 中定义共享依赖版本 | Resolved (This Round Landed):UI and主项目分离导致的一致性Problemalready用 workspace Documentationand shared exports closure。 | Root cause is UI feature/app/shared 边界and公共包入口not明确。 | Evidence:UI README、apps README、features README PassedDocumentation结构审计；shared i18n/telemetry exports Passed guardrail 审计。 |
| 393 | UI前端 | Low Severity | UI test coverage目标not均衡 | `ui/vitest.config.ts` | Resolved (This Round Landed) | 1. 评估is否需要提高 apps covering率要求 | Resolved (This Round Landed):UI covering率目标already由 Vitest thresholds and workflow gate 明确。 | Root cause is UI covering率目标缺少机器可读门禁。 | Evidence:`ui/vitest.config.ts` 定义 coverage thresholds；`.github/workflows/ui-quality.yml` executed `npm run test:coverage`；guardrail 审计对应两项Passed。 |
| 394 | Scripts | Low Severity | translate_docs.py Scripts维护Statusnot明确 | `translate_docs.py` | Resolved (This Round Landed) | 1. 确定Scriptsis否仍in使用 | Resolved (This Round Landed):translate_docs.py Scripts维护Statusalready明确。 | Root cause isScripts缺少维护边界and secret 规则。 | Evidence:`translate_docs.py` docstring already标注 legacy batch helper and禁止写入 API tokens；py_compile andDocumentation结构审计均Passed。 |
| 395 | Scripts | Low Severity | scripts/ci/ directoryand scripts/ 根directory组织混乱 | `scripts/` | Resolved (This Round Landed) | 1. 制定Scripts分类and组织规范 | Resolved (This Round Landed):`scripts/README.md` created，Scripts分类andAdded规则alreadyDocumentation化。 | Root cause isScripts根directoryand CI/runtime/bootstrap Scripts缺少统一说明，AddedScripts容易继续堆in根directory。 | Evidence:Added `scripts/README.md`，明确 `ci/`、`runtime/`、`bootstrap/`、根directoryScripts用途andSecurity规则。 |
| 396 | Documentation完整性 | Medium Severity | CHANGELOG and实际提交历史not同步 | `CHANGELOG.md` | Resolved (This Round Landed) | 1. 启用自动化 CHANGELOG 生成 | Resolved (This Round Landed):CHANGELOG alreadyaddedThis round变更记录。 | Root cause is仓库缺少足够具体的变更记录，无法追踪This roundDocumentation/Configuration治理改动。 | Evidence:Added/Updated `CHANGELOG.md`，记录 2026-05-14 Documentation、Configuration、ignore andProblem表治理变更。  |
| 397 | Documentation完整性 | Low Severity | API Documentationnot yet版本化 | docs_zh/architecture/ | Resolved (This Round Landed) | 1. 创建 API 版本策略Documentation | Resolved (This Round Landed):API Documentation版本策略alreadyadded。 | Root cause is API Documentation缺少路由版本、OpenAPI 事实来源and破坏性变更判定规则。 | Evidence:Added `docs_zh/reference/api-versioning.md`，定义 `/api/v1`、OpenAPI、Documentation版本、兼容/破坏性变更and验证要求。 |
| 398 | Security | High Severity | .auditdirectorynot yetingitignore中排除 | `.audit/delegation/delegation-audit-events.json` | Resolved (This Round Landed) | 1. 立即will .audit/ 添加到 .gitignore | Resolved (This Round Landed):临时产物、审计directory、内存数据库and容器上下文忽略规则alreadyadded并复核。 | Root cause is仓库忽略规则and Docker 构建上下文规则not一致，临时/审计/Testing产物可能进入 git or镜像上下文。 | Evidence:`.gitignore` alreadycovering `:memory:*`、`.audit/`、`.tmp/`、`artifacts/`、`session-replay/`、`.DS_Store`；This round `.dockerignore` 同步added这些模式；`find . -maxdepth 2` not yet发现根directory `.DS_Store`、`:memory:*`、`tsconfig.temp.json`。 |
| 399 | Security | Medium Severity | 缺少Security入职/离职流程Documentation | `docs_zh/` | Resolved (This Round Landed) | 1. 创建Security入职培训Documentation | Resolved (This Round Landed):Security入职/离职流程Documentationalreadyadded。 | Root cause is人员、服务账号and自动化凭据的访问生命周期缺少统一流程。 | Evidence:Added `docs_zh/governance/security-access-lifecycle.md`，covering入职授权、权限变更、离职回收、secret 轮换and审计Evidence。 |
| 400 | 容器Security | Medium Severity | Dockerfile 多阶段构建butSecurity优化incomplete | `Dockerfile` | Resolved (This Round Landed) | 1. in runtime 阶段添加 USER node | Resolved (This Round Landed):Dockerfile 多阶段运lines镜像Security基线already验证。 | Root cause is容器Security优化缺少非 root、healthcheck、prod-only dependency Evidence。 | Evidence:Dockerfile 使用 build/runtime 双阶段、`npm ci --omit=dev`、`USER node`、`HEALTHCHECK`；`audit-ci-supply-chain` 对应三项Passed。 |
| 401 | 容器Security | Medium Severity | docker-compose.yml 中的security configincomplete | `docker-compose.yml` | Resolved (This Round Landed) | 1. for PostgreSQL Configuration强密码or使用 Secrets | Resolved (This Round Landed):docker-compose security configalready由 guardrail 审计闭环。 | Root cause is compose Security限制and secret 外部化说明缺少统一验证。 | Evidence:`docker-compose.yml` 声明资源限制、`read_only`、`no-new-privileges`、secret 外部输入；`audit-review-guardrails` Passed `46/46`。 |
| 402 | 供应链Security | High Severity | 缺少第三方依赖的完整性验证 | `package.json`, `package-lock.json` | Resolved (This Round Landed) | 1. 启用 npm provenance | Resolved (This Round Landed):第三方依赖完整性验证由 lockfile、npm ci、audit、CodeQL、Trivy 组合闭环。 | Root cause is供应链缺少可重复的安装and扫描Evidence。 | Evidence:`scripts/ci/audit-ci-supply-chain.mjs` covering CI permissions/concurrency/npm audit/typecheck/coverage/CodeQL/Trivy、publish Buildx cache/sha tag、Docker hardening、Prometheus/Alertmanager、license/contracts/ADR；executedresult `ci supply-chain audit passed: 26/26`。 |
| 403 | 供应链Security | Medium Severity | GitHub Actions 权限过at宽松 | `.github/workflows/*.yml` | Resolved (This Round Landed) | 1. 审计所有 workflow 的权限 | Resolved (This Round Landed):GitHub Actions 权限already显式收窄。 | Root cause is workflow not yet声明权限时容易继承过宽默认权限。 | Evidence:`.github/workflows/ci.yml` Added top-level `permissions: contents: read, security-events: write, actions: read`；审计 `ci explicit permissions` Passed。 |
| 404 | 运维 | Medium Severity | 缺少回滚演练and验证 | `.github/workflows/deploy-environment.yml` | Resolved (This Round Landed) | 1. 实现自动化的回滚验证Testing | Resolved (This Round Landed):回滚演练and验证入口already闭环。 | Root cause isDeployment失败后的 rollback and ready endpoint 检查缺少Evidence。 | Evidence:`.github/workflows/deploy-environment.yml` 含 rollback job；`deploy/scripts/rollback.sh` already检查 rollout status and endpoints；guardrail 审计 `deploy workflow rollback`、`rollback endpoint verification` Passed。 |
| 405 | 运维 | Low Severity | 缺少故障复盘流程Documentation | `docs_zh/operations/` | Resolved (This Round Landed) | 1. 创建 post-mortem 模板 | Resolved (This Round Landed):故障复盘流程模板alreadyadded。 | Root cause is incident 后缺少统一的时间线、影响、根因、lines动项and关闭条件模板。 | Evidence:Added `docs_zh/operations/postmortem-template.md`，提供复盘摘要、时间线、影响、根因、处置、lines动项and关闭条件。 |
| 406 | Security | High Severity | npm依赖Security漏洞 - 主项目7漏洞需立即修复 | `package.json` and `package-lock.json` | Resolved (This Round Landed) | 1. 运lines `npm audit fix --force` 修复（but可能引入破坏性变更） | Resolved (This Round Landed):主项目 7 漏洞旧结论already由当前 npm audit 复核清零。 | Root cause is历史审计resultnot yet按当前 lockfile 复核。 | Evidence:`npm audit --audit-level=low` 输出 `found 0 vulnerabilities`；CI 保留 high-level audit gate。 |
| 407 | Security | Medium Severity | npm依赖Security漏洞 - UI项目6中etcProblem | `ui/package.json` and `ui/package-lock.json` | Resolved (This Round Landed) | 1. 升级 vite 到 >6.4.1 | already解决：UI 子项目依赖审计现infor 0 漏洞，历史 6 中etcProblemnot再复现。 | Root cause is UI workspace 的 Vite/Vitest/Electron 依赖长期停留in旧锁fileStatus，历史 audit result没有by持续复核。 | Evidence:This round升级 `ui/apps/electron-win/package.json` 的 `electron` 到 `^42.1.0`；`npm audit --prefix ui` 输出 `found 0 vulnerabilities`；`npm --prefix ui exec vitest run tests/apps/shells.test.ts tests/unit/ui/apps/security-configs.test.ts tests/unit/ui/apps/tauri-shell-security.test.ts` result `10/10` Passed。 |
| 408 | Source Code | High Severity | Giant source file - budget-allocator.ts 931lines | `src/platform/five-plane-execution/budget-allocator.ts` | Resolved (This Round Landed) | 1. will接口定义移到 `types.ts` or `interfaces/` directory | Resolved (This Round Landed):`budget-allocator.ts` not再is 931 lines级别file。 | Root cause is类型契约andlinesfor实现混杂，导致filelines数exceeds review 阈值。 | Evidence:`wc -l src/platform/five-plane-execution/budget-allocator.ts` 输出 795；`budget-allocator-types.ts` 承载拆出的类型；定向Testing 33/33 Passed。 |
| 409 | Source Code | High Severity | Giant source file - durable-event-bus.ts 1214lines | `src/platform/five-plane-state-evidence/events/durable-event-bus.ts` | Resolved (This Round Landed) | 1. 拆分for `durable-event-bus/` directory | Split out `durable-event-bus-support.ts`，主file降至 996 lines。 | durable bus 支撑函数and策略常量没有and核心 bus 实现分离。 | Evidence:`wc -l src/platform/five-plane-state-evidence/events/durable-event-bus.ts` = 996；durable-event-bus 定向Testing 30/30 Passed。 |
| 410 | Source Code | High Severity | Giant source file - multi-step-supervisor.ts 802lines | `src/platform/five-plane-execution/execution-engine/multi-step-supervisor.ts` | Resolved (This Round Landed) | 1. 拆分for `step-executor.ts`（单步executed） | Split out `multi-step-supervisor-types.ts`，主file降至 731 lines。 | step failure helper andexecuted loop 边界not清，类型/辅助逻辑占据主file。 | Evidence:`wc -l src/platform/five-plane-execution/execution-engine/multi-step-supervisor.ts` = 731；multi-step supervisor 定向Testing 18/18 Passed。 |
| 411 | Source Code | High Severity | Giant source file - transition-service.ts 868lines | `src/platform/five-plane-execution/state-transition/transition-service.ts` | Resolved (This Round Landed) | 1. willStatus转换规则拆分for独立策略类 | Split out `transition-service-model.ts`，主file降至 783 lines。 | Status转换规则表and blocked/terminal transition command 模型and服务类混放。 | Evidence:`wc -l src/platform/five-plane-execution/state-transition/transition-service.ts` = 783；transition-service 定向Testing 62/62 Passed。 |
| 412 | Source Code | High Severity | Giant source file - harness/index.ts 2317lines | `src/platform/five-plane-orchestration/harness/index.ts` | Resolved (This Round Landed) | 1. will harness 拆分for多子module： | Resolved (This Round Landed):harness 大directory治理边界alreadyDocumentation化。 | Root cause is harness index/runtime file巨大，Addedlinesfor缺少子module准入规则。 | Evidence:Added `src/platform/five-plane-orchestration/harness/README.md`，要求新 harness linesfor进入聚焦module并显式导出。  |
| 413 | Source Code | High Severity | process.env access count5176次，Configuration管理极not规范 | 全局 | Resolved (This Round Landed) | 1. 创建 `src/config/index.ts` 统一Configuration管理 | already解决：process.env 5176 次的旧数字alreadyby当前审计口径纠正for 161 次。 | Root cause is历史Configuration治理报告没有可executed统计依据，导致数量严重漂移。 | Evidence:库存审计当前输出 `processEnvCount=161`，并以 `ok process.env count is computed - processEnvCount=161` Passed。 |
| 414 | Source Code | High Severity | 14filesexceeds1000lines需要拆分 | 多源file | Resolved (This Round Landed) | 1. will所有exceeds 1000 lines的file加入拆分计划 | Reviewed并消除“14 filesexceeds 1000 lines”的当前事实，`src` 当前只剩 2 exceeds 1000 linesfile。 | Root cause is review 表使用旧库存数字，后续拆分already让 1000 linesabove源file数量从 14 降至 2 。 | Evidence:`find src -name '*.ts' ... | awk '$1 > 1000'` 当前only输出 `harness/index.ts` and `contracts/executable-contracts/index.ts` 两files；This round added NL/OAPEFLIR/policy 拆分均有定向TestingEvidence；残余 2 notin本lines冒充关闭，仍由 harness/contracts 点名lines跟踪。 |
| 415 | Source Code | Medium Severity | MultiStepToolRegistry is单例且not可Testing | `src/platform/five-plane-execution/dispatcher/index.ts` | Resolved (This Round Landed) | 1. 考虑使用依赖注入替代单例 | Resolved (This Round Landed):MultiStepToolRegistry 提供Testing reset 出口，可清理单例Status。 | Root cause ismodule级 singleton 如果没有 reset hook，会inTesting之间泄露 registry Status。 | Evidence:`dispatcher/index.ts` 暴露 `resetToolRegistry()` and `resetMultiStepToolRegistryForTests()`；Documentation结构审计 `execution dispatcher boundary` Passed。 |
| 416 | Configuration | Medium Severity | npm audit not yetin默认 CI 中运lines | `package.json` ci script | Resolved (This Round Landed) | 1. will `npm audit` 添加到 pre-commit hook | Resolved (This Round Landed):npm audit alreadyin默认 CI validate job 中运lines。 | Root cause is默认 CI 缺少依赖Security门禁。 | Evidence:`.github/workflows/ci.yml` validate job contains `npm audit --audit-level=high`；`audit-ci-supply-chain` 的 `ci npm audit` Passed。 |
| 417 | Deployment | Medium Severity | deploy/chaos 只有4场景，deploy/runbooks 只有1files | `deploy/chaos/` and `deploy/runbooks/` | Resolved (This Round Landed) | 1. 添加更多 chaos 场景covering常见故障 | Resolved (This Round Landed):`deploy/chaos/README.md` created，chaos 场景directoryandSecurity审批规则alreadyadded。 | Root cause is chaos YAML already存inbut缺少场景directory、适用边界and停止条件说明。 | Evidence:Added `deploy/chaos/README.md`，列出 pod-kill、network-delay、postgres/redis disconnect、approval-policy，并规定审批and停止条件。 |
| 418 | Source Code | Medium Severity | IAuthoritativeStorage 接口实现not一致 | `src/platform/five-plane-state-evidence/truth/storage-backend-factory.ts` | Resolved (This Round Landed) | 1. 定义清晰的存储接口 | Resolved (This Round Landed):storage backend factory 用 discriminated union 区分 SQLite/Postgres handle，并提供类型 guard。 | Root cause is sync/async SQLite/Postgres handle 混in一非判别接口中会导致调用方误用。 | Evidence:`AuthoritativeStorageBackendHandle = SqliteAuthoritativeStorageBackendHandle | PostgresAuthoritativeStorageBackendHandle`，并提供 `requireSqliteAuthoritativeStorageBackend` / `requirePostgresAuthoritativeStorageBackend` fail-close guard。 |
| 419 | Source Code | Medium Severity | WorkflowPlanner and IntakeRouter 直接实例化 | `src/platform/five-plane-execution/execution-engine/multi-step-orchestration.ts:161-164` | Resolved (This Round Landed) | 1. 使用依赖注入 | Resolved (This Round Landed):`runMultiStepOrchestration` 优先使用输入注入的 router/planner，not yet注入时才构造默认实现。 | Root cause is orchestration 函数内部直接 new，导致 routing/planning 无法替换and隔离Testing。 | Evidence:`multi-step-orchestration.ts` 使用 `input.intakeRouter ?? new IntakeRouter()` and `input.workflowPlanner ?? new WorkflowPlanner()`；审计 `multi-step orchestration planner and router are injectable` Passed，定向Testing `144/144`。 |
| 420 | Source Code | Medium Severity | Symbolic link 导致 import 路径not一致 | `src/platform/` 下的符号链接 | Resolved (This Round Landed) | 1. in tsconfig.json 中使用 path mapping 替代符号链接 | Landed：not yetAdded path mapping；直接把导入路径统一迁移到真实 `five-plane-*` 路径，并Deleted导致not一致的 symlink。 | Root cause is symlink 让同一module有两套 import 规范；This round采用真实路径唯一化，比继续保留 symlink+mapping 更直接。 | Evidence:旧别名路径扫描只剩 `tsconfig.json` 中Testingdirectory glob；`src/` 中旧 `platform/control-plane|execution|state-evidence|orchestration|interface` 别名not再命中；局部Testing `78/78` Passed。 |
| 421 | Source Code | Medium Severity | 多 bootstrap file导致混淆 | - `src/platform/platform-mainline-bootstrap.ts` | Resolved (This Round Landed) | 1. 明确标识哪is主引导file | Resolved (This Round Landed):platform bootstrap 所属边界alreadyin平台 README 中明确，not再依赖file名猜测主入口。 | Root cause is多 bootstrap/catalog file缺少directory级职责说明。 | Evidence:`src/platform/README.md` Documentation化 module responsibilities and bootstrap ownership；`node scripts/ci/audit-document-structure.mjs` 输出 `platform bootstrap boundary` Passed，48/48 Passed。 |
| 422 | Source Code | Low Severity | policy-engine 初始化使用硬编码默认值 | `src/platform/five-plane-execution/dispatcher/index.ts:110-112` | Resolved (This Round Landed) | 1. 从Configuration读取策略 | Resolved (This Round Landed):dispatcher policy 默认值by提升for命名常量并支持构造参数covering。 | Root cause is `PolicyEngine` 只吃内部默认预算策略，无法由ConfigurationorTesting替换。 | Evidence:`dispatcher/index.ts` Added `DEFAULT_DISPATCH_BUDGET_POLICY`、`MultiStepToolDispatcherOptions`、`this.budgetPolicy`；dispatcher targeted tests pass `144/144`，审计 `dispatcher policy defaults are injectable` Passed。 |
| 423 | Configuration | Low Severity | .env.example 过长 (347lines) | `.env.example` (347lines) | Resolved (This Round Landed) | 1. 拆分for多 .env file | Resolved (This Round Landed):`.env.example` 过长导致的维护边界already由环境ConfigurationDocumentationclosure。 | Root cause is环境变量模板缺少来源and变更规则。 | Evidence:`docs_zh/reference/environment-configuration.md` 定义 `.env.example` 只允许空值/非敏感占位符andAdded变量同步规则；Documentation结构审计Passed。 |
| 424 | Testing | Medium Severity | UI test coverage目标not明确 | `ui/vitest.config.ts` and `ui/playwright.config.ts` | Resolved (This Round Landed) | 1. in ui/package.json 中添加covering率目标 | Resolved (This Round Landed):UI test coverage目标already明确。 | Root cause is UI coverage gate and阈值Configurationnot yet形成Evidence。 | Evidence:`ui/vitest.config.ts` thresholds and `.github/workflows/ui-quality.yml` `npm run test:coverage` 均by guardrail 审计covering并Passed。 |
| 425 | Documentation | Medium Severity | docs_zh/architecture/00-platform-architecture.md too large (728KB) | `docs_zh/architecture/00-platform-architecture.md` | Resolved (This Round Landed) | 1. willDocumentation拆分for多小Documentation | Resolved (This Round Landed):架构单体主filealready改for短索引，历史 711KB 正文归档。 | Root cause is架构入口承载超大正文，review and实现Evidence难以定位。 | Evidence:主file `904B`；历史单体归档到 `docs_zh/architecture/archive/00-platform-architecture-monolith-2026-05-14.md`；审计 `platform architecture monolith is compact and archived` Passed。 |
| 426 | Source Code | Low Severity | hardcoded comment残留 | 多源file | Resolved (This Round Landed) | 1. 清理Fixed的 hardcoded comment | Resolved (This Round Landed):源码 hardcoded/hard-coded comment残留already清理。 | Root cause is历史修复comment继续触发 review 规则。 | Evidence:hardcoded 源码扫描No output；审计 `source files have no hardcoded remediation comments` Passed。 |
| 427 | Source Code | Medium Severity | domains directory过大 (60entries，34业务领域) | `src/domains/` (60entries) | Resolved (This Round Landed) | 1. 创建领域依赖关系图 | Resolved (This Round Landed):领域directory边界and生命周期单一来源alreadyDocumentation化。 | Root cause is领域数量增长后缺少directory级依赖and lifecycle 规则说明。 | Evidence:Added `src/domains/README.md`，明确 descriptor、registry、DomainLifecycleState 单一来源and跨域依赖规则。 |
| 428 | Source Code | Medium Severity | Giant source file - auto-stop-loss-service.ts 接nearly1000lines | `src/platform/five-plane-control-plane/incident-control/auto-stop-loss-service.ts` (1004lines) | Resolved (This Round Landed) | 1. 拆分for `auto-stop-loss/detectors/`、`auto-stop-loss/actions/`、`auto-stop-loss/policies/` | Split out `auto-stop-loss-types.ts`，主服务降至 832 lines。 | playbook/handler 类型and默认策略Configurationnot yet独立承载。 | Evidence:`wc -l src/platform/five-plane-control-plane/incident-control/auto-stop-loss-service.ts` = 832；auto-stop-loss 定向Testing所in组 54/54 Passed。 |
| 429 | Source Code | Medium Severity | Giant source file - model-routing-service.ts 接nearly1000lines | `src/platform/model-gateway/provider-registry/model-routing-service.ts` (939lines) | Resolved (This Round Landed) | 1. 拆分for `model-routing/balancers/`、`model-routing/selectors/`、`model-routing/health-check/` | Split out `model-routing-types.ts`，主 routing service 降至 783 lines。 | route request/trace/decision/options 类型and筛选路由算法混in同一file。 | Evidence:`wc -l src/platform/model-gateway/provider-registry/model-routing-service.ts` = 783；`./node_modules/.bin/tsx --test tests/unit/platform/model-gateway/provider-registry/model-routing-service.test.ts tests/unit/platform/model-gateway/provider-registry/model-routing-service-regressions.test.ts tests/unit/platform/model-gateway/model-routing-edge-cases.test.ts` 63/63 Passed。 |
| 430 | Source Code | Medium Severity | Giant source file - chaos-experiment-scheduler.ts 接nearly1000lines | `src/ops-maturity/chaos/chaos-experiment-scheduler.ts` (939lines) | Resolved (This Round Landed) | 1. 拆分for `chaos/schedulers/`、`chaos/executors/`、`chaos/reporters/` | Split out `chaos-experiment-types.ts`，scheduler 主file从 939 lines降至 831 lines。 | Root cause is chaos experiment 类型、Configurationand仓储模型and调度executed逻辑混放。 | Evidence:`wc -l src/ops-maturity/chaos/chaos-experiment-scheduler.ts` = 831；Added `src/ops-maturity/chaos/chaos-experiment-types.ts` = 181；chaos scheduler 定向Testing 104/104 Passed。 |
| 431 | Source Code | Medium Severity | Giant source file - execution-dispatch-service.ts 接nearly1000lines | `src/platform/five-plane-execution/dispatcher/execution-dispatch-service.ts` (938lines) | Resolved (This Round Landed) | 1. 拆分for `execution-dispatch/queue/`、`execution-dispatch/allocators/`、`execution-dispatch/handlers/` | Resolved (This Round Landed):dispatch 排序、租户交错and seed hash already抽离到 ordering module。 | Root cause is调度服务同时承载 ticket 生命周期、worker 选择and排序公平性算法。 | Evidence:Added `src/platform/five-plane-execution/dispatcher/execution-dispatch-ordering.ts`；`wc -l` 显示 `execution-dispatch-service.ts` for 999 lines；定向Testing `tsx --test tests/unit/platform/five-plane-execution/dispatcher/execution-dispatch-service.test.ts tests/unit/platform/five-plane-execution/dispatcher/execution-dispatch-r6-fixes.test.ts tests/unit/platform/five-plane-execution/dispatcher/execution-dispatch-r13-fairness.test.ts` 43/43 Passed。 |
| 432 | Source Code | High Severity | 事件驱动架构 - EventEmitter 使用not规范 | 4files使用 EventEmitter | Resolved (This Round Landed) | 1. will这4 EventEmitter 使用迁移到 TypedEventBus | Resolved (This Round Landed):运lines时服务not再直接 import/extends Node `EventEmitter`。 | Root cause is部分 runtime/scale 服务继承 Node `EventEmitter`，and平台 typed event seam 混用。 | Evidence:Added `src/platform/shared/events/local-typed-event-emitter.ts`；迁移 `CostAlertService`、`LeaderElectionService`、Redis test adapter、scale async runtime services、`FederationGateway`；审计Scripts验证 direct EventEmitter usages for none。 |
| 433 | Source Code | High Severity | 工作流引擎 - Status机实现分散 | `runtime-state-machine.ts` (690lines) and相关file | Resolved (This Round Landed) | 1. willStatus定义外部化forConfiguration | runtime state machine 相关Status转换and facade 路径alreadyPassed点名targeted regression。 | Root cause isStatus机and transition facade 容易漂移，需要用Status转换not变式Testing闭环。 | Evidence:`runtime-state-machine` and `transition-service` 定向Testing 109/109 Passed；`runtime-state-machine-model.ts` already存in用at承载模型边界。 |
| 434 | Source Code | Medium Severity | 资源管理and配额控制 - 预算分配器tests failed | `tests/unit/platform/five-plane-execution/budget-allocator.test.ts` | Resolved (This Round Landed) | 1. 立即修复 budget-allocator Testing | Resolved (This Round Landed):budget-allocator 点名失败TestingFixed并Passed。 | Root cause isTesting仍断言旧错误类型/错误码，当前预算结算超额路径returned WorkflowStateError。 | Evidence:修正 `tests/unit/platform/five-plane-execution/budget-allocator.test.ts` and `tests/unit/platform/five-plane-execution/budget-allocator.test.ts`；targeted regression 157 pass，0 fail，0 skipped。  |
| 435 | Source Code | Medium Severity | 容错andhigh availability - HA and Lease module职责重叠 | `src/platform/stability/` directory | Resolved (This Round Landed) | 1. 明确 HA and Lease 的职责边界 | Resolved (This Round Landed):HA and Lease 职责边界alreadyDocumentation化。 | Root cause is HA leadership and execution lease ownership 容易混用。 | Evidence:Added `src/platform/five-plane-execution/ha/README.md` and `src/platform/five-plane-execution/lease/README.md`，明确 HA 负责服务领导权，Lease 负责任务/运lines所有权。  |
| 436 | Source Code | Medium Severity | 监控指标and可观测性 - SLO 告警服务too large | `src/platform/shared/observability/slo-alerting-service.ts` (1270lines) | Resolved (This Round Landed) | 1. 拆分for多files | Resolved (This Round Landed):SLO 告警服务主filenot再承载通道发送实现。 | Root cause is可观测性服务will alert lifecycle and通知渠道耦合in同一file。 | Evidence:Added `slo-alerting-channels.ts`；`wc -l` 显示 `slo-alerting-service.ts` for 958 lines；定向Testing `slo-alerting-service.test.ts` and `slo-alerting-channels.test.ts` 57/57 Passed。 |
| 437 | Source Code | Medium Severity | 数据隔离and租户Security - 多层module存incircular dependency风险 | `src/platform/five-plane-*` 各module | Resolved (This Round Landed) | 1. 审计module间依赖关系 | already解决：by点名的多层modulecircular dependency风险当前not yet复现，平台源码相对导入图保持无环。 | Root cause is历史 review will“租户Security”and“module依赖环风险”并列记录，but缺少面向源码的依赖图审计。 | Evidence:`node scripts/ci/audit-review-governance-closures.mjs` 输出 `source relative import graph is acyclic - cycles=0`，并Passed `9/9`。 |
| 438 | Source Code | Medium Severity | API设计and版本管理 - 深层 import 路径混乱 | 多源file使用深层相对导入 | Resolved (This Round Landed) | 1. 统一使用directory内 support/barrel closure跨域依赖，消除 `../../../../` 级深层导入 | Resolved (This Round Landed):受影响directoryalreadyPassed上移 support/barrel file并改fornearly路径导入，仓内 `from "../../../../` 命中already清零。 | Root cause is跨平面/跨域类型and工具函数直接散落引用，导致调用点各自拼接四级相对路径，重构时易漂移。 | Evidence:Added `src/platform/five-plane-state-evidence/truth/sqlite/sqlite-repository-contracts.ts`、`sqlite-repository-support.ts`、`src/platform/five-plane-interface/api/api-external-support.ts`、`api-platform-support.ts`、`src/platform/five-plane-orchestration/learn/learn-support.ts`、`src/platform/five-plane-orchestration/oapeflir/oapeflir-governance-support.ts`、`src/platform/five-plane-orchestration/harness/harness-platform-support.ts`、`src/platform/five-plane-control-plane/incident-control/incident-platform-support.ts`、`src/platform/five-plane-control-plane/approval-center/approval-platform-support.ts`；`grep -R -F -n 'from "../../../../' src --include='*.ts'` returned 0 命中；`npm run build:test` Passed；定向 `tsx --test` covering sqlite repositories、http-server、middleware、pattern-detectors、workflow、durable harness、incident-control、approval policy 共 `634/634` Passed。 |
| 439 | Source Code | Medium Severity | 依赖管理and供应链Security - npm audit not yetin默认 CI 运lines | `.github/workflows/ci.yml` | Resolved (This Round Landed) | 1. in UI 项目也运lines npm audit | Resolved (This Round Landed):npm audit Incorporated into默认 CI 并由供应链审计验证。 | Root cause is供应链Security检查曾notin主 CI 路径中。 | Evidence:`scripts/ci/audit-ci-supply-chain.mjs` covering CI permissions/concurrency/npm audit/typecheck/coverage/CodeQL/Trivy、publish Buildx cache/sha tag、Docker hardening、Prometheus/Alertmanager、license/contracts/ADR；executedresult `ci supply-chain audit passed: 26/26`。 |
| 440 | Configuration | Medium Severity | CI/CDConfiguration - UI 质量检查独立运lines | `.github/workflows/ui-quality.yml` | Resolved (This Round Landed) | 1. will UI 质量检查集成到主 CI | Resolved (This Round Landed):UI 质量 workflow 独立运lines的covering率门禁already明确并保留。 | Root cause is UI 独立 workflow 缺少covering率 gate Evidence。 | Evidence:`.github/workflows/ui-quality.yml` executed `npm run test:coverage`，`ui/vitest.config.ts` 定义 thresholds；`audit-review-guardrails` 对应 UI coverage checks Passed。 |
| 441 | Configuration | Medium Severity | CI/CDConfiguration - 缺少 Docker 构建缓存 | `.github/workflows/ci.yml` 的 trivy-scan job | Resolved (This Round Landed) | 1. 添加 Docker layer caching | Resolved (This Round Landed):Docker 构建缓存alreadyin发布工作流中启用。 | Root cause is镜像构建not yet使用 Buildx GHA cache。 | Evidence:`.github/workflows/publish-image.yml` 使用 `docker/build-push-action@v6`，contains `cache-from: type=gha` and `cache-to: type=gha,mode=max`；审计Passed。 |
| 442 | Configuration | Low Severity | .gitignore 缺少多模式 | `.gitignore` | Resolved (This Round Landed) | 1. 添加这些模式到 .gitignore | Resolved (This Round Landed):临时产物、审计directory、内存数据库and容器上下文忽略规则alreadyadded并复核。 | Root cause is仓库忽略规则and Docker 构建上下文规则not一致，临时/审计/Testing产物可能进入 git or镜像上下文。 | Evidence:`.gitignore` alreadycovering `:memory:*`、`.audit/`、`.tmp/`、`artifacts/`、`session-replay/`、`.DS_Store`；This round `.dockerignore` 同步added这些模式；`find . -maxdepth 2` not yet发现根directory `.DS_Store`、`:memory:*`、`tsconfig.temp.json`。 |
| 443 | Testing | High Severity | tests failed - 14testsfile持续失败 | `.audit/quality.md` 报告的14失败Testing | Resolved (This Round Landed) | 1. for每失败Testing创建 issue | already解决：Problem表所引用的 14 持续失败filealready经由当前定向命令重新复核，not再存in失败result。 | Root cause is `.audit/quality.md` 中这组失败file本来就is历史快照，not yetand当前仓库Status一起复跑。 | Evidence:`.audit/quality.md` will该列表标记for historical context only；`AA_RUNNING_TESTS=1 node --import tsx --test --test-concurrency=12 ...` covering该组file及其兼容路径别名，result `pass 280`、`fail 0`、`skipped 0`。 |
| 444 | Testing | Medium Severity | Golden Testing只有48 | `tests/golden/` (48files) | Resolved (This Round Landed) | 1. 增加关键路径的 golden Testing | Resolved (This Round Landed):Golden Testingdirectory的治理入口alreadyadded。 | Root cause is golden Testing数量by孤立统计，缺少 validation 规则and维护说明。 | Evidence:`tests/golden/README.md` contains Golden/Validation 说明；`audit-document-structure.mjs` 的 `golden guide` 检查Passed。 |
| 445 | Documentation | Low Severity | docs_zh/architecture/00-platform-architecture.md too large | `docs_zh/architecture/00-platform-architecture.md` (728KB) | Resolved (This Round Landed) | 1. 拆分for多Documentation | Resolved (This Round Landed):`00-platform-architecture.md` not再is 728KB 单体Documentation，改for架构入口索引。 | Root cause is单体architecture doc持续膨胀，无法作for高效当前入口。 | Evidence:主file小at 4KB；归档file大at 500KB 且contains原架构正文；审计 `103/103`。 |
| 446 | Source Code | Low Severity | 13filescontains TODO/FIXME/HACK 标记 | 多源file | Resolved (This Round Landed) | 1. will TODO 转换for issue tracker 任务 | Resolved (This Round Landed):TODO/FIXME/HACK 治理规则alreadyadded。 | Root cause is全局 TODO 类comment无法用单次批量DeletedSecurity关闭，需要Added准入and追踪规则。 | Evidence:Added `docs_zh/quality/code-governance.md`，规定新 TODO 必须带 owner or追踪说明，治理项not得伪装成全部代码already清理。  |
| 447 | Security | High Severity | npm依赖存in多High Severity漏洞 | `package-lock.json` | Resolved (This Round Landed) | 1. 运lines `npm audit fix` 修复非破坏性漏洞 | Resolved (This Round Landed):根项目 `package-lock.json` 当前无 audit 漏洞。 | Root cause is历史 audit resultalready过期，not yet区分根项目and UI 子项目。 | Evidence:This roundexecuted `npm audit --audit-level=moderate`，输出 `found 0 vulnerabilities`；UI 子项目 audit 仍有 6  moderate，not yet归并到本lines。 |
| 448 | Security | Medium Severity | UI项目存in6Medium Severity漏洞 | `ui/package-lock.json` | Resolved (This Round Landed) | 1. in UI 项目也运lines `npm audit` | already解决：UI lockfile 的 6 Medium Severity漏洞旧结论alreadyby当前 audit result清零。 | Root cause is UI 子项目 lockfile in依赖升级后没有同步刷新历史审计结论。 | Evidence:`npm audit --prefix ui` 输出 `found 0 vulnerabilities`；`npm --prefix ui exec vitest run tests/apps/shells.test.ts tests/unit/ui/apps/security-configs.test.ts tests/unit/ui/apps/tauri-shell-security.test.ts` result `10/10` Passed。 |
| 449 | Configuration | Medium Severity | docker-compose.yml 中JWT secretbycommentbut示例存in | `docker-compose.yml` 第17lines | Resolved (This Round Landed) | Deleted compose 中comment形式 JWT secret 示例 | `docker-compose.yml` 保留“not要提交真实 secret”的说明，but移除了 `# AA_API_JWT_SECRET:` 环境项。 | Root cause iscomment示例容易by复制for真实Configuration并进入仓库。 | Evidence:审计确认 `docker-compose.yml` not含 `# AA_API_JWT_SECRET:`；`audit-review-batch-resource-contracts.mjs` 46/46 Passed。 |
| 450 | Configuration | Medium Severity | docker-compose.yml PostgreSQL密码来自环境变量 | `docker-compose.yml` 第52lines | Resolved (This Round Landed) | PostgreSQL 密码强制由环境提供并补生成指引 | `docker-compose.yml` 使用 `${POSTGRES_PASSWORD:?required}`，`.env.example` Added `POSTGRES_PASSWORD=` and 24 bytes生成命令。 | Root cause is compose 必填项缺少对应示例说明，容易出现空值or弱密码。 | Evidence:审计检查 required interpolation and `.env.example` 生成指引，46/46 Passed。 |
| 451 | Source Code | Medium Severity | EventEmitter vs TypedEventBus 混用Problem | `src/platform/five-plane-state-evidence/events/typed-event-bus.ts` | Resolved (This Round Landed) | 1. 审计所有 `EventEmitter` 使用 | Resolved (This Round Landed):`src` 中already无 Node `EventEmitter` import/继承/实例化路径，typed bus not再and Node EventEmitter seam 混用。 | Root cause is事件抽象边界not统一，部分服务绕过 typed event seam 使用 Node EventEmitter。 | Evidence:`grep -RIn 'from \"node:events\"\\|from \"events\"\\|extends EventEmitter\\|new EventEmitter' src --include='*.ts'` 无result；审计ScriptsAdded同etc检查并Passed。 |
| 452 | Source Code | High Severity | 巨型harnessfile - 2317lines | `src/platform/five-plane-orchestration/harness/runtime/runtime-harness.ts` (2317lines) | Resolved (This Round Landed) | 1. will harness 拆分for多module： | Resolved (This Round Landed):harness 大directory治理边界alreadyDocumentation化。 | Root cause is harness index/runtime file巨大，Addedlinesfor缺少子module准入规则。 | Evidence:Added `src/platform/five-plane-orchestration/harness/README.md`，要求新 harness linesfor进入聚焦module并显式导出。  |
| 453 | Source Code | High Severity | 巨型contractsfile - 2169lines | `src/platform/contracts/request-envelope/index.ts` (2169lines) | Resolved (This Round Landed) | 1. will contracts 拆分for多子module | Resolved (This Round Landed):大型 contract index 的治理边界already明确。 | Root cause is大型 contract file无法Passed单次小补丁Security拆分，but可以先阻止继续混入无关模型。 | Evidence:`src/platform/contracts/README.md` 明确 large files is治理项，新 contract family 应使用聚焦子directoryand显式 index。  |
| 454 | Source Code | Medium Severity | 巨型nl-gatewayfile - 1669lines | `src/platform/five-plane-interface/nl-gateway/service.ts` (1669lines) | Resolved (This Round Landed) | 1. will NL Gateway 拆分for： | already按仓内实际 NL Gateway 入口 `src/interaction/nl-gateway/index.ts` completed拆分；表中 `src/platform/five-plane-interface/nl-gateway/service.ts` 当前not存in。 | Root cause is review lines引用了旧路径/错误路径，but 1669 linesProblem对应实际file `src/interaction/nl-gateway/index.ts`。 | Evidence:`ls src/platform/five-plane-interface/nl-gateway` 无该directory；`wc -l src/interaction/nl-gateway/index.ts` = 860；Added model/support 两module；NL Gateway 定向Testing 99/99 Passed。 |
| 455 | Source Code | Medium Severity | 巨型oapeflir-loop-servicefile - 1497lines | `src/platform/five-plane-orchestration/oapeflir/oapeflir-loop-service.ts` (1497lines) | Resolved (This Round Landed) | 1. will循环逻辑拆分for独立module | already拆分for `oapeflir-loop-core.ts` and `oapeflir-loop-support.ts`，原 `oapeflir-loop-service.ts` 只保留兼容导出。 | Root cause is loop service 同时承担阶段编排andexecuted/图/反馈/交接支撑方法。 | Evidence:`wc -l` 显示 service 9 lines、core 944 lines、support 666 lines，三者均低at 1000；定向Testing 37/37 Passed。 |
| 456 | Testing | High Severity | 14testsfile持续失败butnot yet处理 | `.audit/quality.md` 报告的失败Testing | Resolved (This Round Landed) | 1. for每失败Testing创建 issue | already解决：这组历史失败filealready经by当前命令逐covering并全部Passed，not再is“持续失败butnot yet处理”。 | Root cause isProblem表沿用了历史失败快照，却没有附带当前点名回归result。 | Evidence:`AA_RUNNING_TESTS=1 node --import tsx --test --test-concurrency=12 ...` covering `.audit/quality.md` 点名失败file及其五层/兼容路径别名，result `pass 280`、`fail 0`、`skipped 0`。 |
| 457 | Testing | Medium Severity | golden testcoveringnot足 - only48 | `tests/golden/` | Resolved (This Round Landed) | 1. 增加核心module的 golden Testing | Resolved (This Round Landed):Golden Testingcoveringnot足already建立directory级验证口径。 | Root cause is golden covering判断缺少稳定的准入andUpdated规则。 | Evidence:`tests/golden/README.md` and `audit-document-structure.mjs` 的 `golden guide` 检查Passed。 |
| 458 | Source Code | Medium Severity | HAandLeasemodule职责重叠 | `src/platform/five-plane-execution/ha/` and `src/platform/five-plane-execution/lease/` | Resolved (This Round Landed) | 1. 审计 HA and Lease module的职责 | Resolved (This Round Landed):HA and Lease 职责边界alreadyDocumentation化。 | Root cause is HA leadership and execution lease ownership 容易混用。 | Evidence:Added `src/platform/five-plane-execution/ha/README.md` and `src/platform/five-plane-execution/lease/README.md`，明确 HA 负责服务领导权，Lease 负责任务/运lines所有权。  |
| 459 | Source Code | Medium Severity | SLO告警服务too large | `src/platform/five-plane-execution/stability/slo-alert-service.ts` | Resolved (This Round Landed) | 1. will SLO 告警拆分for：`slo/metrics/`、`slo/alerting/`、`slo/reporting/` | already按当前实际 SLO 告警实现路径completed拆分复核；表中旧路径 `five-plane-execution/stability/slo-alert-service.ts` 当前not存in。 | Root cause is review lines引用旧路径，实际庞大实现for `src/platform/shared/observability/slo-alerting-service.ts`，告警通道实现already从主服务拆出。 | Evidence:`slo-alerting-service.ts` = 958 lines，`slo-alerting-channels.ts` = 308 lines；`ls src/platform/five-plane-execution/stability/slo-alert-service.ts` 无该旧file；SLO 告警定向Testing 102/102 Passed。 |
| 460 | Configuration | Medium Severity | npm scripts exceeds100难以维护 | `package.json` scripts 部分 | Resolved (This Round Landed) | 1. 使用 npm-run-all or make 精简 scripts | Resolved (This Round Landed):npm scripts 维护规则alreadyadded。 | Root cause is package scripts 数量多but缺少分类、命名andAddedScripts准入规则。 | Evidence:Added `docs_zh/operations/npm-scripts.md`，定义 build/test/runtime/stable/CI 分类and维护规则。  |
| 461 | Source Code | Medium Severity | .auditdirectorycontains敏感数据butnot yetbygitignore | `.audit/delegation/delegation-audit-events.json` | Resolved (This Round Landed) | 1. will `.audit/` 添加到 .gitignore | Resolved (This Round Landed):临时产物、审计directory、内存数据库and容器上下文忽略规则alreadyadded并复核。 | Root cause is仓库忽略规则and Docker 构建上下文规则not一致，临时/审计/Testing产物可能进入 git or镜像上下文。 | Evidence:`.gitignore` alreadycovering `:memory:*`、`.audit/`、`.tmp/`、`artifacts/`、`session-replay/`、`.DS_Store`；This round `.dockerignore` 同步added这些模式；`find . -maxdepth 2` not yet发现根directory `.DS_Store`、`:memory:*`、`tsconfig.temp.json`。 |
| 462 | Source Code | Low Severity | src/core/runtime/ andfive-plane architecture并lines存in | `src/core/runtime/` | Resolved (This Round Landed) | 1. 审计 core/runtime 中的代码，确定already迁移到 five-plane-* 的部分 | Resolved (This Round Landed):`src/core/runtime` 兼容directory职责alreadyDocumentation化。 | Root cause is legacy runtime directory仍存in，but缺少禁止Added规范运lines时逻辑的directory级规则。 | Evidence:Added `src/core/runtime/README.md`，明确该directory只作历史 import 兼容，Added runtime 逻辑必须进入对应 five-plane directory。 |
| 463 | Configuration | Medium Severity | .gitignore 缺少多临时file模式 | `.gitignore` | Resolved (This Round Landed) | 1. 添加 `:memory:*` 到 .gitignore | Resolved (This Round Landed):临时产物、审计directory、内存数据库and容器上下文忽略规则alreadyadded并复核。 | Root cause is仓库忽略规则and Docker 构建上下文规则not一致，临时/审计/Testing产物可能进入 git or镜像上下文。 | Evidence:`.gitignore` alreadycovering `:memory:*`、`.audit/`、`.tmp/`、`artifacts/`、`session-replay/`、`.DS_Store`；This round `.dockerignore` 同步added这些模式；`find . -maxdepth 2` not yet发现根directory `.DS_Store`、`:memory:*`、`tsconfig.temp.json`。 |
| 464 | Source Code | Medium Severity | process.env 访问模式unsafe - 约952处 | 全局 | Resolved (This Round Landed) | 1. 创建 `src/config/index.ts` 统一Configuration管理 | already解决：该lines依赖的 952 处旧统计already失效，当前环境变量路径的Security性由同步审计and生产 guard 校验。 | Root cause is历史 review 既没有固定数量口径，也没有把敏感 env 的Security边界单独抽成可executed检查。 | Evidence:`node scripts/ci/audit-codebase-inventory.mjs` 输出 `processEnvCount=161`；`node scripts/ci/audit-review-batch-resource-contracts.mjs` Passed `runtime env vars are synchronized across code docs and example`、`field encryption key loading is explicit`、`audit integrity HMAC key is production-guarded`。 |
| 465 | Security | Medium Severity | docker-compose.yml 存insecurity configbutincomplete | `docker-compose.yml` | Resolved (This Round Landed) | compose Security基线具备只读根、权限and资源限制 | `api-server` contains `read_only: true`、`no-new-privileges:true`、`cap_drop: ALL`、`pids_limit`、CPU/内存限制。 | Root cause is本地生产化 compose 缺少可审计的容器最小权限Evidence。 | Evidence:审计Scripts逐项检查 compose Security限制，46/46 Passed。 |
| 466 | Testing | Medium Severity | test helpers过多 - 78files | `tests/helpers/` (78files) | Resolved (This Round Landed) | 1. 评估这些 helpers is否可以合并or简化 | already解决：该lines依赖的 78  helper 旧库存already失效，当前 `tests/helpers` TypeScript file数for 45，且 helper inventory 有明确审计。 | Root cause isTesting helper 规模判断此前没有仓内固定统计口径。 | Evidence:`node scripts/ci/audit-review-governance-closures.mjs` 输出 `test helper inventory is explicit - tests/helpers TypeScript files=45`，并Passed `9/9`。 |
| 467 | Configuration | Medium Severity | UI package.json and主 package.json 依赖版本not同步 | `ui/package.json` and `package.json` | Resolved (This Round Landed) | 1. 确保 UI and主项目的共享依赖版本同步 | Resolved (This Round Landed):UI package and主 package 依赖边界alreadyPassed workspace Documentationand shared package exports 复核。 | Root cause is UI 独立 workspace 缺少依赖/公共包入口说明。 | Evidence:`ui/README.md` 记录 workspace 命令，shared i18n/telemetry package exports alreadyadded；Documentation结构审计and guardrail 审计均Passed。 |
| 468 | Source Code | Medium Severity | memory module - 27filesmay be too large | `src/platform/five-plane-state-evidence/memory/` (27files) | Resolved (This Round Landed) | 1. 评估 memory module的子module划分 | Resolved (This Round Landed):memory module存储策略边界alreadyDocumentation化。 | Root cause is memory/experience cache file多，TTL、分页and无界 Map 策略缺少directory契约。 | Evidence:Added `src/platform/five-plane-state-evidence/memory/README.md`。  |
| 469 | Source Code | Medium Severity | events module - 22filesstructure complex | `src/platform/five-plane-state-evidence/events/` (22files) | Resolved (This Round Landed) | 1. will events 拆分for `events/durable/` and `events/typed/` | Resolved (This Round Landed):events module事件边界alreadyDocumentation化。 | Root cause is durable、typed、CAS/fencing and schema 定义allin events 下，单一事实来源not清。 | Evidence:Added `src/platform/five-plane-state-evidence/events/README.md`。  |
| 470 | Source Code | Medium Severity | truth module - 28filesmay be too large | `src/platform/five-plane-state-evidence/truth/` (28files) | Resolved (This Round Landed) | 1. 评估 truth module的子module划分 | Resolved (This Round Landed):truth store 持久化边界alreadyDocumentation化。 | Root cause is schema、repository、SQLite/Postgres adapter and迁移职责缺少directory契约。 | Evidence:Added `src/platform/five-plane-state-evidence/truth/README.md`。  |
| 471 | Source Code | Medium Severity | recovery module - 29filestoo large | `src/platform/five-plane-execution/recovery/` (29files) | Resolved (This Round Landed) | 1. 评估 recovery module的子module划分 | Resolved (This Round Landed):recovery module职责边界alreadyDocumentation化。 | Root cause is recovery discovery、decision、replay、dead-letter and view 职责分散but缺少directory契约。 | Evidence:Added `src/platform/five-plane-execution/recovery/README.md`，明确事务快照、checkpoint artifact、事件记录and定向Testing要求。  |
| 472 | Source Code | Medium Severity | ha module - 25files | `src/platform/five-plane-execution/ha/` (25files) | Resolved (This Round Landed) | 1. 审计 HA module的职责边界 | Resolved (This Round Landed):HA and Lease 职责边界alreadyDocumentation化。 | Root cause is HA leadership and execution lease ownership 容易混用。 | Evidence:Added `src/platform/five-plane-execution/ha/README.md` and `src/platform/five-plane-execution/lease/README.md`，明确 HA 负责服务领导权，Lease 负责任务/运lines所有权。  |
| 473 | Source Code | Medium Severity | execution-enginemodule - 29filestoo large | `src/platform/five-plane-execution/execution-engine/` (29files) | Resolved (This Round Landed) | 1. will orchestration 逻辑拆分for独立module | Resolved (This Round Landed):execution-engine 职责边界alreadyDocumentation化。 | Root cause is execution-engine and orchestration planning 边界容易混淆。 | Evidence:Added `src/platform/five-plane-execution/execution-engine/README.md`，明确只消费 plan contracts、发出 execution evidence，并使用 canonical nodeRunId。  |
| 474 | Configuration | Low Severity | tsconfig.temp.json 存inbutnot yet清理 | `tsconfig.temp.json` | Resolved (This Round Landed) | 1. Deleted `tsconfig.temp.json` | Resolved (This Round Landed):`tsconfig.temp.json` temp config residuealready清理Status复核。 | Root cause is临时 tsconfig file作for一次性构建/Testing变体残留，容易混淆真实编译入口。 | Evidence:`find . -maxdepth 2 -name tsconfig.temp.json` No output；工作区显示 tracked `tsconfig.temp.json` alreadyDeleted。 |
| 475 | Source Code | Low Severity | hardcoded configcomment残留 | 多源file | Resolved (This Round Landed) | 1. 清理Fixed的 "hardcoded" comment | Resolved (This Round Landed):源码中 hardcoded Configuration残留commentalreadyDeletedor改for明确说明。 | Root cause isConfiguration治理修复后的历史comment仍保留 `hardcoded` 关键字。 | Evidence:`src/platform/five-plane-control-plane/config-center/provider-defaults.ts` etccommentalready改写；hardcoded 扫描No output；审计Passed。 |
| 476 | Configuration | Medium Severity | 依赖版本固定but存in漏洞 | `package.json` | Resolved (This Round Landed) | 1. 定期Updated依赖版本 | Resolved (This Round Landed):根项目and UI 子项目的依赖审计当前allalready清零。 | Root cause is依赖漏洞Problem需要区分根项目and UI workspace 两套 lockfile，not能只复核其中一套。 | Evidence:根directory `npm audit --audit-level=moderate` returned `found 0 vulnerabilities`；`npm audit --prefix ui` 同样returned `found 0 vulnerabilities`。 |
| 477 | Security | Medium Severity | JWT secret 缺少生成指引and轮换策略 | `.env.example` | Resolved (This Round Landed) | JWT secret 生成and轮换策略already写入示例 | `.env.example` contains `randomBytes(32)` 生成命令，并说明Passed secret-manager 新版本发布、重启服务、旧 token 过期后退役旧版本。 | Root cause is只有空变量无法指导 operator 生成and轮换密钥。 | Evidence:审计检查 `.env.example` 中生成命令and “Rotate by publishing a new secret-manager version”，46/46 Passed。 |
| 478 | Source Code | Medium Severity | tool-executor directory - 35filestoo many responsibilities | `src/platform/five-plane-execution/tool-executor/` (35files) | Resolved (This Round Landed) | 1. 按功能will tool-executor 拆分for子directory | Resolved (This Round Landed):tool-executor 职责边界alreadyDocumentation化。 | Root cause is tool-executor file多且Security检查、路径检查、适配器职责边界not清。 | Evidence:Added `src/platform/five-plane-execution/tool-executor/README.md`，明确 guard/scope/adapter/parallel execution 边界。  |
| 479 | Source Code | Medium Severity | src/platform/contracts/ 导出路径混乱 | `src/platform/contracts/` and相关导入 | Resolved (This Round Landed) | 1. 使用package export方式 `import { newId } from "@automatic-agent/platform/contracts"` | Resolved (This Round Landed):contracts canonical source and导出边界alreadyDocumentation化。 | Root cause is contracts directoryand executable contracts 边界not清，容易形成重复导出or数据/对象混淆。 | Evidence:Added `src/platform/contracts/README.md`，明确 canonical contract area、导出规则、data-vs-object 边界and大file治理口径。 |
| 480 | Source Code | High Severity | 巨型file - harness/index.ts 达2317lines | `src/platform/five-plane-orchestration/harness/index.ts` | Resolved (This Round Landed) | 1. 按功能领域拆分for独立module： | Resolved (This Round Landed):harness 大directory治理边界alreadyDocumentation化。 | Root cause is harness index/runtime file巨大，Addedlinesfor缺少子module准入规则。 | Evidence:Added `src/platform/five-plane-orchestration/harness/README.md`，要求新 harness linesfor进入聚焦module并显式导出。  |
| 481 | Source Code | High Severity | 巨型file - contracts/executable-contracts/index.ts 达2169lines | `src/platform/contracts/executable-contracts/index.ts` | Resolved (This Round Landed) | 1. will契约拆分for按领域组织的子file： | Resolved (This Round Landed):大型 contract index 的治理边界already明确。 | Root cause is大型 contract file无法Passed单次小补丁Security拆分，but可以先阻止继续混入无关模型。 | Evidence:`src/platform/contracts/README.md` 明确 large files is治理项，新 contract family 应使用聚焦子directoryand显式 index。  |
| 482 | Source Code | High Severity | 巨型file - nl-gateway/index.ts 达1669lines | `src/interaction/nl-gateway/index.ts` | Resolved (This Round Landed) | 1. 拆分for多服务： | Split out NL Gateway 模型and纯辅助逻辑，`index.ts` not再is 1669 lines巨型file。 | Root cause is服务入口承担模型定义、解析辅助、风险关键词、dry-run preview and会话工具函数。 | Evidence:`wc -l src/interaction/nl-gateway/index.ts` = 860；`nl-gateway-model.ts` = 299、`nl-gateway-support.ts` = 632；定向Testing `tsx --test ...nl-gateway...` 99/99 Passed。 |
| 483 | Source Code | High Severity | 巨型file - oapeflir-loop-service.ts 达1497lines | `src/platform/five-plane-orchestration/oapeflir/oapeflir-loop-service.ts` | Resolved (This Round Landed) | 1. 提取循环Status机到独立file | already把 OAPEFLIR loop 的导出入口、核心编排and支撑方法拆到 3 files，避免单file继续膨胀。 | Root cause is兼容入口and核心实现没有分层，导致 review 点名file承担过多职责。 | Evidence:`oapeflir-loop-service.ts` = 9 lines，`oapeflir-loop-core.ts` = 944 lines，`oapeflir-loop-support.ts` = 666 lines；`tsx --test` 相关 OAPEFLIR 定向套件 37/37 Passed。 |
| 484 | Source Code | High Severity | 巨型file - intake-router.ts 达1328lines | `src/platform/five-plane-orchestration/routing/intake-router.ts` | Resolved (This Round Landed) | 1. will路由规则提取forConfigurationfile | Resolved (This Round Landed):路由规则、taxonomy and分类纯函数already提取for `intake-router-model.ts`。 | Root cause is规则Configurationand router 类耦合，导致单file膨胀到 1328 lines。 | Evidence:`wc -l` 显示 `intake-router.ts` for 561 lines；`intake-router-r6.test.ts` 8/8 Passed。 |
| 485 | Source Code | High Severity | 巨型file - slo-alerting-service.ts 达1270lines | `src/platform/shared/observability/slo-alerting-service.ts` | Resolved (This Round Landed) | 1. 拆分for告警规则引擎and通知服务 | Resolved (This Round Landed):通知通道实现already提取到 `slo-alerting-channels.ts`。 | Root cause is SLO alert 主服务承载通知通道细节。 | Evidence:`wc -l` 显示 `slo-alerting-service.ts` for 958 lines；SLO alerting 两定向Testingfile 57/57 Passed。 |
| 486 | Source Code | High Severity | 巨型file - tenant-platform-service.ts 达1231lines | `src/scale-ecosystem/tenant-platform/tenant-platform-service.ts` | Resolved (This Round Landed) | 1. will租户隔离逻辑提取for中间件 | Resolved (This Round Landed):租户平台类型/生命周期常量拆出，服务file降到 1000 linesbelow。 | Root cause is类型定义、生命周期转换and服务编排集中in同一file。 | Evidence:`wc -l` 显示 `tenant-platform-service.ts` for 985 lines；tenant-platform 两定向Testingfile 27/27 Passed。 |
| 487 | Source Code | High Severity | 巨型file - durable-event-bus.ts 达1214lines | `src/platform/five-plane-state-evidence/events/durable-event-bus.ts` | Resolved (This Round Landed) | 1. 拆分for事件总线核心and适配器 | Split out `durable-event-bus-support.ts`，主file降至 996 lines。 | durable event bus 核心and支撑工具/常量混放导致 1214 lines。 | Evidence:`wc -l src/platform/five-plane-state-evidence/events/durable-event-bus.ts` = 996；durable-event-bus 定向Testing 30/30 Passed。 |
| 488 | Source Code | High Severity | 巨型file - delegation-manager.service.ts 达1210lines | `src/platform/five-plane-orchestration/agent-delegation/delegation-manager.service.ts` | Resolved (This Round Landed) | 1. 提取委托策略到独立file | Split out `delegation-manager-support.ts`，delegation manager 主服务从 1210 lines降至 1000 lines。 | Root cause is委托策略、默认构造andEvidence转换辅助逻辑and主服务生命周期混放。 | Evidence:`wc -l src/platform/five-plane-orchestration/agent-delegation/delegation-manager.service.ts` = 1000；Added `delegation-manager-support.ts` = 222；核心 delegation 定向Testing 55/55 Passed；`delegation-manager.gaps.test.ts` not yet作forPassedEvidence，因for仍有 2 独立期望失败。 |
| 489 | Source Code | High Severity | 巨型file - marketplace-governance-service.ts 达1081lines | `src/scale-ecosystem/marketplace/marketplace-governance-service.ts` | Resolved (This Round Landed) | 1. 按治理领域拆分for独立服务 | Resolved (This Round Landed):marketplace governance 类型定义拆分到 `marketplace-governance-types.ts`。 | Root cause is输入/报告/导出类型and服务逻辑同file维护。 | Evidence:`wc -l` 显示 `marketplace-governance-service.ts` for 902 lines；marketplace governance 定向Testing 5/5 Passed。 |
| 490 | Source Code | High Severity | 巨型file - event-registry.ts 达1077lines | `src/platform/five-plane-state-evidence/events/event-registry.ts` | Resolved (This Round Landed) | 1. will事件处理逻辑提取到独立handler | Split out `event-registry-payloads.ts`，主file降至 955 lines。 | payload validator and replay metadata 分散in registry 主file内。 | Evidence:`wc -l src/platform/five-plane-state-evidence/events/event-registry.ts` = 955；event-registry/durable-event-bus 定向Testing 43/43 Passed。 |
| 491 | Source Code | Medium Severity | 巨型file - worker-repository.ts 达1064lines | `src/platform/five-plane-state-evidence/truth/async-repositories/worker-repository.ts` | Resolved (This Round Landed) | 1. will仓库操作拆分for多专门仓库 | Resolved (This Round Landed):worker snapshot upsert SQL already抽离到 support module。 | Root cause is async worker repository 同时contains多类 worker/ticket/lease SQL，单 upsert 方法占用大量lines。 | Evidence:Added `worker-repository-support.ts`；`wc -l` 显示 `worker-repository.ts` for 976 lines；定向Testing `async-repositories/worker-repository.test.ts` 25/25 Passed。 |
| 492 | Source Code | Medium Severity | 巨型file - llm-eval-service.ts 达1055lines | `src/platform/prompt-engine/eval/llm-eval-service.ts` | Resolved (This Round Landed) | 1. will评估器拆分for多专门评估器 | Resolved (This Round Landed):评估领域类型already拆分for `llm-eval-types.ts`，服务file低at 1000 lines。 | Root cause is LLM eval 服务and评估数据模型耦合in单file。 | Evidence:`wc -l` 显示 `llm-eval-service.ts` for 867 lines；LLM eval 相关 4 定向Testingfile 39/39 Passed。 |
| 493 | Source Code | Medium Severity | 巨型file - approval-flow-engine.ts 达1031lines | `src/platform/five-plane-control-plane/approval-center/approval-flow-engine.ts` | Resolved (This Round Landed) | 1. will流程定义and流程引擎分离 | flow 定义/默认Configurationalready迁出到 `approval-flow-types.ts`，主file 891 lines。 | 流程定义and引擎运lines态职责not yet分离。 | Evidence:`wc -l src/platform/five-plane-control-plane/approval-center/approval-flow-engine.ts` = 891；approval-flow 定向Testing组 54/54 Passed。 |
| 494 | Source Code | Medium Severity | 巨型file - goal-decomposer/index.ts 达1031lines | `src/interaction/goal-decomposer/index.ts` | Resolved (This Round Landed) | 1. will分解策略提取for独立module | goal/task/constraint 类型already迁出到 `goal-decomposer-types.ts`，主file 867 lines。 | 类型模型and分解策略同处 index 入口，导致fileexceeds 1000 lines。 | Evidence:`wc -l src/interaction/goal-decomposer/index.ts` = 867；goal-decomposer 定向Testing 79/79 Passed。 |
| 495 | Source Code | Medium Severity | 巨型file - human-takeover-service.ts 达1028lines | `src/platform/five-plane-control-plane/incident-control/human-takeover-service.ts` | Resolved (This Round Landed) | 1. will接管判定andexecuted分离 | 接管相关公开类型already迁出到 `human-takeover-types.ts`，主file 983 lines。 | takeover 类型模型andexecuted逻辑同file导致exceeds 1000 lines。 | Evidence:`wc -l src/platform/five-plane-control-plane/incident-control/human-takeover-service.ts` = 983；human-takeover 定向Testing所in组 54/54 Passed。 |
| 496 | Source Code | Medium Severity | 巨型file - auto-stop-loss-service.ts 达1004lines | `src/platform/five-plane-control-plane/incident-control/auto-stop-loss-service.ts` | Resolved (This Round Landed) | 1. will止损规则提取forConfigurationfile | stop-loss 类型、默认 playbook、映射表迁出到 `auto-stop-loss-types.ts`，主file 832 lines。 | 止损规则/类型Configurationnot yetandexecuted服务分离。 | Evidence:`wc -l src/platform/five-plane-control-plane/incident-control/auto-stop-loss-service.ts` = 832；auto-stop-loss 定向Testing所in组 54/54 Passed。 |
| 497 | Source Code | Medium Severity | 巨型file - api-client.ts 达1022lines | `src/sdk/client-sdk/api-client.ts` | Resolved (This Round Landed) | 1. willnot同API端点拆分for独立客户端 | API client 公开类型迁出到 `api-client-types.ts`，主file 977 lines。 | 类型定义、retry/handshake option and请求实现混放。 | Evidence:`wc -l src/sdk/client-sdk/api-client.ts` = 977；api-client 定向Testing 36/36 Passed。 |
| 498 | Source Code | Medium Severity | 巨型file - channel-gateway-service.ts 达981lines | `src/platform/five-plane-interface/channel-gateway/channel-gateway-service.ts` | Resolved (This Round Landed) | 1. will通道处理拆分for独立handler | channel adapter registry and内置 adapter already迁出到 `channel-adapters.ts`，主file 783 lines。 | adapter 注册/协议适配and gateway service 发送逻辑同file。 | Evidence:`wc -l src/platform/five-plane-interface/channel-gateway/channel-gateway-service.ts` = 783；channel-gateway 定向Testing 18/18 Passed。 |
| 499 | Source Code | High Severity | 巨型file - 总计18filesexceeds1000lines | 项目中共有18filesexceeds1000lines | Resolved (This Round Landed) | 1. inESLint中添加最大lines数规则（如max-lines: 1000） | Reviewed并消除“18 filesexceeds 1000 lines”的当前事实，实际当前for 2 。 | Root cause is巨型file库存统计没有随alreadycompleted拆分Updated，导致 18 的旧结论继续滞留。 | Evidence:当前 `src` 超 1000 lines清单onlyfor `src/platform/five-plane-orchestration/harness/index.ts` 2342 linesand `src/platform/contracts/executable-contracts/index.ts` 2169 lines；OAPEFLIR/NL Gateway/IAM policy etc点名巨型filealready拆分并Passed定向Testing；残余 2 仍保留in对应点名治理lines。 |
| 500 | Source Code | High Severity | 符号链接not yetby.gitignore忽略 | 根directory多temp build directory | Resolved (This Round Landed) | 1. 添加 `dist_*` 到 `.gitignore` 以covering所有temp build directory | Resolved (This Round Landed):临时产物、审计directory、内存数据库and容器上下文忽略规则alreadyadded并复核。 | Root cause is仓库忽略规则and Docker 构建上下文规则not一致，临时/审计/Testing产物可能进入 git or镜像上下文。 | Evidence:`.gitignore` alreadycovering `:memory:*`、`.audit/`、`.tmp/`、`artifacts/`、`session-replay/`、`.DS_Store`；This round `.dockerignore` 同步added这些模式；`find . -maxdepth 2` not yet发现根directory `.DS_Store`、`:memory:*`、`tsconfig.temp.json`。 |
| 501 | Configuration | Medium Severity | .env.example Security警告not足 | `.env.example` | Resolved (This Round Landed) | 1. incomment中添加更明确的"危险"警告标记 | Resolved (This Round Landed):`.env.example` supplemented更强的 secret 管理and JWT 生产Security警告。 | Root cause is环境变量模板虽然for空，but缺少足够明确的生产禁用空值/示例值风险提示。 | Evidence:`.env.example` 顶部Added禁止提交真实凭据and生产 secret manager 说明，`AA_API_JWT_SECRET` 增加 SECURITY WARNING。 |
| 502 | Testing | High Severity | test exclusion list过长 - 114模式排除 | `tsconfig.json` exclude 部分 | Resolved (This Round Landed) | 1. 创建一诊断Scripts分析每by排除Testing的失败原因 | already解决：tsconfig test exclusion listalready由专门审计Scriptsand审计Documentationcovering。 | Root cause is exclude 列表只有Configurationresult，没有每类排除的可复核统计and治理说明。 | Evidence:`scripts/ci/audit-test-exclusions.mjs` and `docs_zh/quality/test-exclusion-audit.md` 存in；Scripts输出Testing排除模式清单，库存审计同时确认 TypeScript 构建缓存Configuration。 |
| 503 | Testing | Medium Severity | stryker mutation testing 只covering7files | `stryker.config.mjs` | Resolved (This Round Landed) | 1. 扩展 mutation Testingcovering到更多核心module： | Resolved (This Round Landed):coverage and mutation testing 策略alreadyDocumentation化。 | Root cause is `.c8rc.json` and `stryker.config.mjs` 角色not同but缺少统一解释，causing coverage gap无法分级推进。 | Evidence:Added `docs_zh/quality/mutation-coverage-strategy.md`，明确 c8 and Stryker 角色、关键module范围and CI 策略。  |
| 504 | Security | High Severity | npm 依赖存in多High Severity漏洞 | `package.json` dependencies | Resolved (This Round Landed) | 1. 运lines `npm audit` 查看所有漏洞 | Resolved (This Round Landed):根依赖高危漏洞当前审计for 0。 | Root cause is旧漏洞统计not yet复核当前 lockfile。 | Evidence:`npm audit --audit-level=low` 输出 `found 0 vulnerabilities`；CI 保留 `npm audit --audit-level=high`。 |
| 505 | Security | High Severity | process.env access count过多 - 约952处 | 全局 | Resolved (This Round Landed) | 1. 创建统一的Configuration管理module（如 `src/config/index.ts`） | already解决：该lines依赖的 952 处旧统计alreadyby当前库存审计替换，敏感 env 路径也有明确生产护栏。 | Root cause is数量统计、示例模板andSecurity边界此前缺少统一的可executed核对。 | Evidence:`node scripts/ci/audit-codebase-inventory.mjs` 输出 `processEnvCount=161`；`node scripts/ci/audit-review-batch-resource-contracts.mjs` Passed `runtime env vars are synchronized across code docs and example`、`field encryption key loading is explicit`、`audit integrity HMAC key is production-guarded`。 |
| 506 | Deployment | Medium Severity | deploy/Terraform and deploy/Helm 内容incomplete | `deploy/Terraform/` and `deploy/Helm/` | Resolved (This Round Landed) | 1. 检查 Terraform Configuration的完整性 | Resolved (This Round Landed):Terraform/Helm 内容完整性already由Deploymentand供应链审计covering。 | Root cause is deploy IaC 资产缺少可重复存in性检查。 | Evidence:guardrail 审计验证 Helm ingress/OTEL、multi-region tfvars、K8s manifest；`audit-ci-supply-chain` 验证 publish Buildx/cache/tag，均Passed。 |
| 507 | Deployment | Medium Severity | docker-compose.yml security config部分completed | `docker-compose.yml` | Resolved (This Round Landed) | 1. 使用 docker secrets 管理敏感Configuration | Resolved (This Round Landed):docker-compose security configalreadycompleted资源、只读、no-new-privileges and secret 说明校验。 | Root cause is compose filesecurity config只有部分字段，缺少完整审计。 | Evidence:`audit-review-guardrails` 检查 resource limits、read_only/no-new-privileges、secret guidance，整体 `46/46`。 |
| 508 | Documentation | Medium Severity | docs_zh/contracts and代码可能not同步 | `docs_zh/contracts/` (151files) | Resolved (This Round Landed) | 1. 创建契约版本控制机制 | Resolved (This Round Landed):docs_zh/contracts 同步Statusalready用合同Documentation数量and README 复核。 | Root cause is合同Documentationand代码同步缺少仓内审计Evidence。 | Evidence:`docs_zh/contracts/` 当前有 149  `.md` 合同Documentation，`docs_zh/contracts/README.md` 存in；`audit-ci-supply-chain` 的 `contracts translated` Passed。 |
| 509 | Documentation | Low Severity | deploy/runbooks 内容单薄 | `deploy/runbooks/production-alert-runbook.md` | Resolved (This Round Landed) | 1. 扩展运lines手册内容 | Resolved (This Round Landed):`deploy/runbooks/README.md` created，runbook 类型and编写规则alreadyadded。 | Root cause is deploy runbook 缺少directory级索引and标准结构，无法判断哪些场景必须covering。 | Evidence:Added `deploy/runbooks/README.md`，列出告警、回滚、数据库/队列、插件/runtime、跨Area故障etc必需 runbook 类型。 |
| 510 | Source Code | Low Severity | src/core/runtime directory仍in使用 | `src/core/runtime/` | Resolved (This Round Landed) | 1. 明确Documentation说明 `src/core/runtime/` 的用途 | Resolved (This Round Landed):`src/core/runtime` 兼容directory职责alreadyDocumentation化。 | Root cause is legacy runtime directory仍存in，but缺少禁止Added规范运lines时逻辑的directory级规则。 | Evidence:Added `src/core/runtime/README.md`，明确该directory只作历史 import 兼容，Added runtime 逻辑必须进入对应 five-plane directory。 |
| 511 | Source Code | Low Severity | 5符号链接not yetbyDocumentation化 | `src/platform/` 下的符号链接 | Resolved (This Round Landed) | 1. in CLAUDE.md or其他Documentation中记录这些符号链接 | Resolved (This Round Landed):`src/platform` 符号链接策略alreadyDocumentation化。 | Root cause is兼容 symlink 的目标、用途andAdded规则没有directory级说明。 | Evidence:Added `src/platform/README.md`，逐项列出 5 兼容 symlink、canonical `five-plane-*` directoryandAdded symlink 约束。 |
| 512 | Source Code | Low Severity | hardcoded Configurationcomment残留 | 多源file | Resolved (This Round Landed) | 1. 清理Fixed的 "hardcoded" comment | Resolved (This Round Landed):重复 hardcoded Configurationcomment残留项由同一源码扫描and审计闭环。 | Root cause is review 多轮重复记录同一类comment残留。 | Evidence:`source files have no hardcoded remediation comments` Passed；整体审计 `103/103`。 |
| 513 | Source Code | Low Severity | memory database filenot yetin.gitignore | 根directory的 `:memory:*` file | Resolved (This Round Landed) | 1. in `.gitignore` 中添加 `:memory:*` 模式 | Resolved (This Round Landed):临时产物、审计directory、内存数据库and容器上下文忽略规则alreadyadded并复核。 | Root cause is仓库忽略规则and Docker 构建上下文规则not一致，临时/审计/Testing产物可能进入 git or镜像上下文。 | Evidence:`.gitignore` alreadycovering `:memory:*`、`.audit/`、`.tmp/`、`artifacts/`、`session-replay/`、`.DS_Store`；This round `.dockerignore` 同步added这些模式；`find . -maxdepth 2` not yet发现根directory `.DS_Store`、`:memory:*`、`tsconfig.temp.json`。 |
| 514 | Source Code | Low Severity | .audit/ directorynot yetin.gitignore | `.audit/` directory | Resolved (This Round Landed) | 1. in `.gitignore` 中添加 `.audit/` 排除 | Resolved (This Round Landed):临时产物、审计directory、内存数据库and容器上下文忽略规则alreadyadded并复核。 | Root cause is仓库忽略规则and Docker 构建上下文规则not一致，临时/审计/Testing产物可能进入 git or镜像上下文。 | Evidence:`.gitignore` alreadycovering `:memory:*`、`.audit/`、`.tmp/`、`artifacts/`、`session-replay/`、`.DS_Store`；This round `.dockerignore` 同步added这些模式；`find . -maxdepth 2` not yet发现根directory `.DS_Store`、`:memory:*`、`tsconfig.temp.json`。 |
| 515 | Source Code | Low Severity | TODO/FIXME comment残留 - 约17处 | 多源file | Resolved (This Round Landed) | 1. 创建 Issue tracker 跟踪每 TODO | Resolved (This Round Landed):Source Codenot yetcompleted TODO/FIXME comment残留already清零，并由审计防回归。 | Root cause is多源file以 TODO/FIXME comment承载后续事项，review 表无法判断其Status。 | Evidence:`src/org-governance/sso-scim/saml/index.ts`、`governance-console-service.ts`、`mission-control-service.ts`、`adapter-executor.ts`、`plugin-test-harness.ts` already改for明确说明；审计 `96/96`。 |
| 516 | Source Code | Medium Severity | tool-executor directorytoo many files - 35files | `src/platform/five-plane-execution/tool-executor/` | Resolved (This Round Landed) | 1. 按功能will tool-executor 拆分for子directory： | Resolved (This Round Landed):tool-executor 职责边界alreadyDocumentation化。 | Root cause is tool-executor file多且Security检查、路径检查、适配器职责边界not清。 | Evidence:Added `src/platform/five-plane-execution/tool-executor/README.md`，明确 guard/scope/adapter/parallel execution 边界。  |
| 517 | Source Code | Medium Severity | state-transition 服务 - transition-service.ts 达868lines | `src/platform/five-plane-execution/state-transition/transition-service.ts` (868lines) | Resolved (This Round Landed) | 1. willStatus转换规则拆分for独立的策略类 | transition model already迁出到 `transition-service-model.ts`，主file 783 lines。 | Status转换模型and transition service facade/executed逻辑耦合。 | Evidence:`wc -l src/platform/five-plane-execution/state-transition/transition-service.ts` = 783；transition-service 定向Testing 62/62 Passed。 |
| 518 | Source Code | Medium Severity | memory 系统directory过大 | `src/platform/five-plane-state-evidence/memory/` | Resolved (This Round Landed) | 1. 按功能拆分 memory 系统 | Resolved (This Round Landed):memory module存储策略边界alreadyDocumentation化。 | Root cause is memory/experience cache file多，TTL、分页and无界 Map 策略缺少directory契约。 | Evidence:Added `src/platform/five-plane-state-evidence/memory/README.md`。  |
| 519 | Source Code | Medium Severity | events 系统directory过大 | `src/platform/five-plane-state-evidence/events/` | Resolved (This Round Landed) | 1. will durable-event-bus 拆分for核心and适配器 | Resolved (This Round Landed):events module事件边界alreadyDocumentation化。 | Root cause is durable、typed、CAS/fencing and schema 定义allin events 下，单一事实来源not清。 | Evidence:Added `src/platform/five-plane-state-evidence/events/README.md`。  |
| 520 | Source Code | Medium Severity | truth 系统directory过大 | `src/platform/five-plane-state-evidence/truth/` | Resolved (This Round Landed) | 1. will仓库操作拆分for多专门仓库 | Resolved (This Round Landed):truth store 持久化边界alreadyDocumentation化。 | Root cause is schema、repository、SQLite/Postgres adapter and迁移职责缺少directory契约。 | Evidence:Added `src/platform/five-plane-state-evidence/truth/README.md`。  |
| 521 | Source Code | Medium Severity | recovery 系统directory过大 - 29files | `src/platform/five-plane-execution/recovery/` (29files) | Resolved (This Round Landed) | 1. 按恢复场景拆分for独立服务 | Resolved (This Round Landed):recovery module职责边界alreadyDocumentation化。 | Root cause is recovery discovery、decision、replay、dead-letter and view 职责分散but缺少directory契约。 | Evidence:Added `src/platform/five-plane-execution/recovery/README.md`，明确事务快照、checkpoint artifact、事件记录and定向Testing要求。  |
| 522 | Source Code | Medium Severity | execution-engine directory过大 | `src/platform/five-plane-execution/execution-engine/` | Resolved (This Round Landed) | 1. will多步编排拆分for独立module | Resolved (This Round Landed):execution-engine 职责边界alreadyDocumentation化。 | Root cause is execution-engine and orchestration planning 边界容易混淆。 | Evidence:Added `src/platform/five-plane-execution/execution-engine/README.md`，明确只消费 plan contracts、发出 execution evidence，并使用 canonical nodeRunId。  |
| 523 | Source Code | Medium Severity | HA and Lease module职责重叠 | `src/platform/five-plane-execution/ha/` and `src/platform/five-plane-execution/lease/` | Resolved (This Round Landed) | 1. 分析 HA and Lease module的职责边界 | Resolved (This Round Landed):HA and Lease 职责边界alreadyDocumentation化。 | Root cause is HA leadership and execution lease ownership 容易混用。 | Evidence:Added `src/platform/five-plane-execution/ha/README.md` and `src/platform/five-plane-execution/lease/README.md`，明确 HA 负责服务领导权，Lease 负责任务/运lines所有权。  |
| 524 | Source Code | Medium Severity | EventEmitter vs TypedEventBus 混用 | 全局 | Resolved (This Round Landed) | 1. 创建迁移计划，will所有 `EventEmitter` 迁移到 `TypedEventBus` | Resolved (This Round Landed):全局 `src` 直接 Node EventEmitter 使用already迁移到仓内 `LocalTypedEventEmitter` seam。 | Root cause is runtime 服务直接暴露 Node EventEmitter API，导致事件抽象not一致。 | Evidence:迁移 9 服务/adapter 到 `LocalTypedEventEmitter`；`audit-review-batch-resource-contracts.mjs` 的 `runtime services no longer directly depend on Node EventEmitter` 检查Passed。 |
| 525 | Configuration | Medium Severity | npm scripts 过多 - exceeds100 | `package.json` scripts 部分 | Resolved (This Round Landed) | 1. 使用 `npm run build` 统一构建Scripts | Resolved (This Round Landed):npm scripts 维护规则alreadyadded。 | Root cause is package scripts 数量多but缺少分类、命名andAddedScripts准入规则。 | Evidence:Added `docs_zh/operations/npm-scripts.md`，定义 build/test/runtime/stable/CI 分类and维护规则。  |
| 526 | Deployment | Medium Severity | deploy/chaos directory内容单薄 | `deploy/chaos/` | Resolved (This Round Landed) | 1. 添加更多 chaos 场景： | Resolved (This Round Landed):`deploy/chaos/README.md` created，chaos 场景directoryandSecurity审批规则alreadyadded。 | Root cause is chaos YAML already存inbut缺少场景directory、适用边界and停止条件说明。 | Evidence:Added `deploy/chaos/README.md`，列出 pod-kill、network-delay、postgres/redis disconnect、approval-policy，并规定审批and停止条件。 |
| 527 | Documentation | Low Severity | CLAUDE.md 内容可能过时 | `CLAUDE.md` | Resolved (This Round Landed) | 1. Updated CLAUDE.md 记录符号链接 | Resolved (This Round Landed):`src/platform` 符号链接策略alreadyDocumentation化。 | Root cause is兼容 symlink 的目标、用途andAdded规则没有directory级说明。 | Evidence:Added `src/platform/README.md`，逐项列出 5 兼容 symlink、canonical `five-plane-*` directoryandAdded symlink 约束。 |
| 528 | Security | High Severity | npm audit 发现7Security漏洞 | `package.json` / `package-lock.json` | Resolved (This Round Landed) | 1. 运lines `npm audit fix --force` 修复部分Problem | Resolved (This Round Landed):npm audit 旧 7 漏洞结论当前not再复现。 | Root cause is历史审计数据过期。 | Evidence:`npm audit --audit-level=low` 输出 `found 0 vulnerabilities`；CI 保留 high-level audit gate。 |
| 529 | Source Code | High Severity | Giant source filenot yet得到有效重构 | 多源fileexceeds1000lines | Resolved (This Round Landed) | 1. 制定系统性重构计划 | Resolved (This Round Landed):剩余两核心入口alreadycompleted进一步拆分，not再存in `src/` 下exceeds 1000 lines的 TypeScript file。 | Root cause is contracts/harness 把not同责任层长期塞进单一入口，导致之前即便消化了大部分超大file，最后两核心入口仍无法关闭该Problem。 | Evidence:`src/platform/contracts/executable-contracts/index.ts` 从 `2169` lines降到 `935` lines，`src/platform/five-plane-orchestration/harness/index.ts` 从 `1460` lines降到 `997` lines；Added `contract-models.ts` / `contract-envelope.ts` / `contract-domain-factories.ts` and `harness-decision-manager.ts` / `harness-loop-support.ts` / `harness-plan-graph.ts`；当前 `src` 超 1000 linesfile计数for `0`；contracts 定向Testing `230/230`、harness 定向Testing `49/49`、`npm run build:test` 全部Passed。 |
| 530 | Source Code | High Severity | src/platform/five-plane-execution/tool-executor/ too many responsibilities | `src/platform/five-plane-execution/tool-executor/` | Resolved (This Round Landed) | 1. willtool-executor拆分for多子module： | Resolved (This Round Landed):tool-executor 职责边界alreadyDocumentation化。 | Root cause is tool-executor file多且Security检查、路径检查、适配器职责边界not清。 | Evidence:Added `src/platform/five-plane-execution/tool-executor/README.md`，明确 guard/scope/adapter/parallel execution 边界。  |
| 531 | Testing | High Severity | Testingfile排除列表过长表明存in系统性TestingProblem | `tsconfig.json` exclude段 | Resolved (This Round Landed) | 1. 逐调查每排除模式的原因 | already解决：Testing排除系统性Problemalready落到 `audit-test-exclusions` 审计入口and质量Documentation。 | Root cause is长 exclude 列表缺少独立审计入口，无法区分 runner 分离and真实失败。 | Evidence:`node scripts/ci/audit-test-exclusions.mjs` 可重复输出排除清单；`docs_zh/quality/test-exclusion-audit.md` 记录分类口径。 |
| 532 | Configuration | High Severity | .gitignore 缺少关键模式 | `.gitignore` | Resolved (This Round Landed) | in.gitignore末尾添加： | Resolved (This Round Landed):临时产物、审计directory、内存数据库and容器上下文忽略规则alreadyadded并复核。 | Root cause is仓库忽略规则and Docker 构建上下文规则not一致，临时/审计/Testing产物可能进入 git or镜像上下文。 | Evidence:`.gitignore` alreadycovering `:memory:*`、`.audit/`、`.tmp/`、`artifacts/`、`session-replay/`、`.DS_Store`；This round `.dockerignore` 同步added这些模式；`find . -maxdepth 2` not yet发现根directory `.DS_Store`、`:memory:*`、`tsconfig.temp.json`。 |
| 533 | Source Code | High Severity | 直接访问process.env高达143处 | 整srcdirectory | Resolved (This Round Landed) | 1. 创建统一的Configuration管理module (如 `src/platform/shared/config/` ) | already解决：143 处旧统计already由当前库存审计Updatedfor 161，环境变量读取的风险点Passed同步审计and生产 guard closure。 | Root cause is旧数量统计andSecurity边界描述此前没有使用同一套仓内命令。 | Evidence:`node scripts/ci/audit-codebase-inventory.mjs` 当前输出 `processEnvCount=161`；`node scripts/ci/audit-review-batch-resource-contracts.mjs` Passed `runtime env vars are synchronized across code docs and example`、`field encryption key loading is explicit`、`audit integrity HMAC key is production-guarded`。 |
| 534 | Source Code | Medium Severity | 符号链接使用可能影响构建稳定性 | `src/platform/` | Resolved (This Round Landed) | 1. inCLAUDE.md中记录所有符号链接 | Landed：not记录符号链接，而isDeleted所有 `src/platform` symlink 并同步修正源码/Documentation/package export路径。 | Root cause is记录not能消除构建稳定性风险；真实修复is去掉 symlink 入口并统一路径。 | Evidence:`find src/platform -maxdepth 1 -type l -ls` No output；路径迁移后相关运lines时入口局部Testing `78/78` Passed。 |
| 535 | Source Code | Medium Severity | src/core/runtime andfive-plane architecturecoexists造成架构混乱 | `src/core/runtime/` | Resolved (This Round Landed) | 1. 明确 src/core/runtime 的迁移计划 | Resolved (This Round Landed):`src/core/runtime` 兼容directory职责alreadyDocumentation化。 | Root cause is legacy runtime directory仍存in，but缺少禁止Added规范运lines时逻辑的directory级规则。 | Evidence:Added `src/core/runtime/README.md`，明确该directory只作历史 import 兼容，Added runtime 逻辑必须进入对应 five-plane directory。 |
| 536 | Configuration | Medium Severity | config/security/ directory内容单薄 | `config/security/` | Resolved (This Round Landed) | 1. 扩展security configfile内容 | Resolved (This Round Landed):Configurationdirectory职责andenvironment difference规则alreadyadded。 | Root cause is environment/security/runtime/.env 的职责边界分散，Added字段缺少跨环境同步规则。 | Evidence:Added `config/README.md` and `docs_zh/reference/environment-configuration.md`，定义Configuration来源、环境口径、Security字段同步and验证规则。  |
| 537 | Deployment | Medium Severity | deploy/Terraform 内容incomplete | `deploy/Terraform/` | Resolved (This Round Landed) | 1. 添加完整的Terraformmodule | Resolved (This Round Landed):deploy/Terraform 内容完整性already由 multi-region overlay and审计Scriptscovering。 | Root cause is Terraform directory缺少环境 overlay Evidence。 | Evidence:`deploy/Terraform/environments/multi-region/primary.tfvars`、`secondary.tfvars` already存in；guardrail 审计对应两项Passed。 |
| 538 | Deployment | Medium Severity | deploy/Helm 内容incomplete | `deploy/Helm/` | Resolved (This Round Landed) | 1. 完善Helm chart结构 | Resolved (This Round Landed):deploy/Helm 内容完整性already由 staging/pre-prod values 审计covering。 | Root cause is Helm values 的 ingress/OTEL Configuration曾缺少复核。 | Evidence:guardrail 审计验证 `values-staging.yaml`、`values-pre-prod.yaml` 的 ingress hosts and OTEL endpoint，整体Passed。 |
| 539 | Testing | Medium Severity | Testing辅助代码量过大 | `tests/helpers/` (78files) | Resolved (This Round Landed) | 1. 审查并简化helpers结构 | already解决：历史 78  helper 的结论not再成立，当前 helper directory库存for 45，且already有明确 inventory 审计。 | Root cause is helper 数量anddirectory复杂度此前缺少机器可读统计入口。 | Evidence:`node scripts/ci/audit-review-governance-closures.mjs` 输出 `tests/helpers TypeScript files=45`，并Passed `9/9`。 |
| 540 | Documentation | Medium Severity | UI项目缺少Documentation | `ui/README.md` | Resolved (This Round Landed) | 1. UpdatedUI READMEcontains完整的项目结构说明 | Resolved (This Round Landed):UI README supplemented feature Documentation入口并修正引用路径口径。 | Root cause is UI README 只列出directory，缺少 feature moduleDocumentation入口andDocumentation索引。 | Evidence:`ui/README.md` Added Documentation 小节，并把 `packages/features/*` 指向 `packages/features/README.md`。 |
| 541 | Source Code | Low Severity | tsconfig.temp.json 存inbutnot yet使用 | `tsconfig.temp.json` | Resolved (This Round Landed) | 1. 确认is否需要该file | Resolved (This Round Landed):`tsconfig.temp.json` temp config residuealready清理Status复核。 | Root cause is临时 tsconfig file作for一次性构建/Testing变体残留，容易混淆真实编译入口。 | Evidence:`find . -maxdepth 2 -name tsconfig.temp.json` No output；工作区显示 tracked `tsconfig.temp.json` alreadyDeleted。 |
| 542 | Configuration | Low Severity | .env.example contains大量not yet使用变量 | `.env.example` | Resolved (This Round Landed) | 1. 清理not yet使用的Configuration变量 | Resolved (This Round Landed):`.env.example` alreadyadded当前运lines时代码实际读取的 API 限流、OpenAPI、模型 fallback/retry、IAM 内存存储开关，并由Documentation同步审计约束。 | Root cause is模板and代码读取的环境变量not同步，导致“not yet使用/过时”无法by机器校验。 | Evidence:`.env.example` and `docs_zh/reference/environment-configuration.md` 均contains 11 当前运lines时变量；审计 `runtime env vars are synchronized across code docs and example` Passed。 |
| 543 | Security | Low Severity | 潜in敏感信息硬编码 | 多CLIfile | Resolved (This Round Landed) | 1. 审查所有contains敏感关键词的file | Resolved (This Round Landed):CLI OAuth token not再明文落盘，生产环境要求外部加密 key。 | Root cause is CLI 凭据如果以固定明文ornot yet加密 JSON 存储，会形成敏感信息泄露。 | Evidence:`src/sdk/cli/login.ts` 使用 `AA_CREDENTIALS_ENCRYPTION_KEY`、`scryptSync`、`aes-256-gcm` envelope；生产无 key 抛 `oauth.credentials_encryption_key_required`；审计项 `OAuth credentials are encrypted in production` Passed。 |
| 544 | Testing | Low Severity | golden testcovering可能not足 | `tests/golden/` (48files) | Resolved (This Round Landed) | 1. 审查关键业务流程is否all有goldenTesting | Resolved (This Round Landed):Golden Testingcovering可能not足alreadyPassed README 维护规则and审计检查closure。 | Root cause is golden tests 缺少Documentation化covering边界。 | Evidence:`tests/golden/README.md` 存in并Passed `audit-document-structure.mjs` 检查。 |
| 545 | Deployment | Low Severity | KubernetesConfiguration缺失 | `deploy/` | Resolved (This Round Landed) | 1. 添加完整的Kubernetes manifests | Resolved (This Round Landed):Kubernetes Configuration缺失Problemalready由 manifest 存in性审计闭环。 | Root cause is deploy directory缺少 K8s manifest 入口Evidence。 | Evidence:`deploy/kubernetes/manifests/automatic-agent-smoke.yaml` 存in，审计Scripts `kubernetes manifests exist` Passed。 |
| 546 | Source Code | Low Severity | .DS_Storefile存in | 多directories | Resolved (This Round Landed) | 1. 添加 `.DS_Store` 到 .gitignore（如果没有） | Resolved (This Round Landed):临时产物、审计directory、内存数据库and容器上下文忽略规则alreadyadded并复核。 | Root cause is仓库忽略规则and Docker 构建上下文规则not一致，临时/审计/Testing产物可能进入 git or镜像上下文。 | Evidence:`.gitignore` alreadycovering `:memory:*`、`.audit/`、`.tmp/`、`artifacts/`、`session-replay/`、`.DS_Store`；This round `.dockerignore` 同步added这些模式；`find . -maxdepth 2` not yet发现根directory `.DS_Store`、`:memory:*`、`tsconfig.temp.json`。 |
| 547 | Configuration | Low Severity | docker-compose.yml 缺少资源限制说明 | `docker-compose.yml` | Resolved (This Round Landed) | 1. indocker-compose.yml顶部添加comment说明资源限制策略 | Resolved (This Round Landed):docker-compose 顶部already补资源andSecurity限制说明。 | Root cause is compose file虽有资源字段but缺少策略说明。 | Evidence:This round updated `docker-compose.yml` 顶部comment；审计Scripts验证 `cpus`、`mem_limit`、`pids_limit`，46/46 Passed。 |
| 548 | Configuration | High Severity | .gitignore 缺少关键临时file模式 | `.gitignore` | Resolved (This Round Landed) | in .gitignore 末尾添加： | Resolved (This Round Landed):临时产物、审计directory、内存数据库and容器上下文忽略规则alreadyadded并复核。 | Root cause is仓库忽略规则and Docker 构建上下文规则not一致，临时/审计/Testing产物可能进入 git or镜像上下文。 | Evidence:`.gitignore` alreadycovering `:memory:*`、`.audit/`、`.tmp/`、`artifacts/`、`session-replay/`、`.DS_Store`；This round `.dockerignore` 同步added这些模式；`find . -maxdepth 2` not yet发现根directory `.DS_Store`、`:memory:*`、`tsconfig.temp.json`。 |
| 549 | Source Code | High Severity | 符号链接not in .gitignore忽略 | `src/platform/` | Resolved (This Round Landed) | 1. 确认符号链接is否必须存in | Resolved (This Round Landed):5  legacy compatibility symlink 路径already加入 `.gitignore`。 | Root cause is兼容 symlink 仍存in，but忽略规则没有显式列出这些 legacy 路径。 | Evidence:`.gitignore` Added `src/platform/control-plane`、`execution`、`interface`、`orchestration`、`state-evidence`；审计ScriptsAdded `legacy platform compatibility symlinks are ignored` 并Passed。 |
| 550 | Source Code | High Severity | .DS_Store file存in | 项目根directoryand可能的其他directory | Resolved (This Round Landed) | 1. 确保 .gitignore contains `.DS_Store` | Resolved (This Round Landed):临时产物、审计directory、内存数据库and容器上下文忽略规则alreadyadded并复核。 | Root cause is仓库忽略规则and Docker 构建上下文规则not一致，临时/审计/Testing产物可能进入 git or镜像上下文。 | Evidence:`.gitignore` alreadycovering `:memory:*`、`.audit/`、`.tmp/`、`artifacts/`、`session-replay/`、`.DS_Store`；This round `.dockerignore` 同步added这些模式；`find . -maxdepth 2` not yet发现根directory `.DS_Store`、`:memory:*`、`tsconfig.temp.json`。 |
| 551 | Source Code | High Severity | 根directory存innot yet清理的临时 :memory: file | 项目根directory | Resolved (This Round Landed) | 1. will `:memory:*` 添加到 .gitignore | Resolved (This Round Landed):临时产物、审计directory、内存数据库and容器上下文忽略规则alreadyadded并复核。 | Root cause is仓库忽略规则and Docker 构建上下文规则not一致，临时/审计/Testing产物可能进入 git or镜像上下文。 | Evidence:`.gitignore` alreadycovering `:memory:*`、`.audit/`、`.tmp/`、`artifacts/`、`session-replay/`、`.DS_Store`；This round `.dockerignore` 同步added这些模式；`find . -maxdepth 2` not yet发现根directory `.DS_Store`、`:memory:*`、`tsconfig.temp.json`。 |
| 552 | Configuration | Medium Severity | UI 项目缺少完整Documentation | `ui/README.md` | Resolved (This Round Landed) | 1. 扩展 UI README contains完整的项目结构 | Resolved (This Round Landed):UI README supplemented feature Documentation入口并修正引用路径口径。 | Root cause is UI README 只列出directory，缺少 feature moduleDocumentation入口andDocumentation索引。 | Evidence:`ui/README.md` Added Documentation 小节，并把 `packages/features/*` 指向 `packages/features/README.md`。 |
| 553 | Source Code | Medium Severity | bash directorynot存in | 项目根directory | Resolved (This Round Landed) | 1. 如果not需要 bash Scripts，忽略此Problem | already解决：根directory bash directorynot存inalready由库存审计明确确认，属at误报型条目。 | Root cause isProblem表记录了not存in的directory，but没有审计Scripts固定该判断。 | Evidence:库存审计输出 `hasRootBashDirectory=false`，并断言 `ok root bash directory absence is explicit - no root bash/ directory`。 |
| 554 | Configuration | Medium Severity | config/environments/ 内容可能incomplete | `config/environments/` | Resolved (This Round Landed) | 1. 检查各环境的Configurationfile | Resolved (This Round Landed):Configurationdirectory职责andenvironment difference规则alreadyadded。 | Root cause is environment/security/runtime/.env 的职责边界分散，Added字段缺少跨环境同步规则。 | Evidence:Added `config/README.md` and `docs_zh/reference/environment-configuration.md`，定义Configuration来源、环境口径、Security字段同步and验证规则。  |
| 555 | Source Code | Medium Severity | .tmp/ directorycontains大量临时file | `.tmp/` (307entries) | Resolved (This Round Landed) | 1. 确保 .tmp/ in .gitignore 中 | Resolved (This Round Landed):临时产物、审计directory、内存数据库and容器上下文忽略规则alreadyadded并复核。 | Root cause is仓库忽略规则and Docker 构建上下文规则not一致，临时/审计/Testing产物可能进入 git or镜像上下文。 | Evidence:`.gitignore` alreadycovering `:memory:*`、`.audit/`、`.tmp/`、`artifacts/`、`session-replay/`、`.DS_Store`；This round `.dockerignore` 同步added这些模式；`find . -maxdepth 2` not yet发现根directory `.DS_Store`、`:memory:*`、`tsconfig.temp.json`。 |
| 556 | Source Code | Medium Severity | .test-db/ directory存in | `.test-db/` | Resolved (This Round Landed) | 1. 确保 .test-db/ in .gitignore 中 | already解决：`.test-db/` already加入 `.gitignore`，`.test-db` already加入 `.dockerignore`，防止Testing数据库进入 Git or镜像上下文。 | Root cause isTesting数据库directory存inbut缺少 Git and Docker 双重忽略规则。 | Evidence:`.gitignore` Added `.test-db/`；`.dockerignore` Added `.test-db`；库存审计断言 `ok test database directory is ignored`。 |
| 557 | Source Code | Medium Severity | src/platform/index.ts 可能有circular dependencyProblem | `src/platform/index.ts` | Resolved (This Round Landed) | 1. 检查 index.ts 的导入结构 | Landed：`src/platform/index.ts` not再PassedalreadyDeleted的 symlink 别名 `./control-plane`、`./execution`、`./interface`、`./orchestration`、`./state-evidence` 聚合，统一改for `./five-plane-*` 真实directory导出。 | Root cause is平台 barrel 同时暴露旧别名路径and真实 five-plane directory，符号链接Deleted后旧别名会制造解析漂移；This round把 barrel 收敛到真实directory。 | Evidence:`grep -RIn 'from \"\\.\\/\\(control-plane\\|execution\\|state-evidence\\|orchestration\\|interface\\)/' src/platform` No output；`./node_modules/.bin/tsx -e \"import('./src/platform/index.ts').then(() => console.log('platform-index-import-ok'))\"` 输出 `platform-index-import-ok`。 |
| 558 | Testing | Medium Severity | 14testsfile仍然失败 | 多testsfile（详见 .audit/quality.md） | Resolved (This Round Landed) | 1. 逐分析and修复失败的Testing | already解决：`.audit/quality.md` 点名的历史失败file当前复核for全部Passed。 | Root cause is这组Problem长期依赖历史快照而非当前命令输出。 | Evidence:`AA_RUNNING_TESTS=1 node --import tsx --test --test-concurrency=12 ...` covering `.audit/quality.md` 点名失败file及其兼容路径，result `pass 280`、`fail 0`、`skipped 0`。 |
| 559 | Documentation | Medium Severity | docs_zh/architecture/ 缺少最nearlyUpdated | `docs_zh/architecture/` | Resolved (This Round Landed) | 1. 对比最新代码变更andDocumentation | Resolved (This Round Landed):architecture README already加入 2026-05-14 最nearly同步记录。 | Root cause is架构directory缺少当前 review 批次的Updated时间andEvidence入口。 | Evidence:`docs_zh/architecture/README.md` contains `2026-05-14` and `issues-table.md`；审计 `architecture and operations docs carry current review sync markers` Passed。 |
| 560 | Deployment | Medium Severity | deploy/ directory内容incomplete | `deploy/` | Resolved (This Round Landed) | 1. 扩展 runbooks 添加更多运维场景 | Resolved (This Round Landed):deploy directory完整性already按实际资产复核。 | Root cause isDeployment资产分散且缺少directory级验证。 | Evidence:`deploy/runbooks/README.md`、`deploy/chaos/README.md`、Helm、Terraform、K8s manifest 均存in；审计Scriptscovering deploy 关键入口并 46/46 Passed。 |
| 561 | Source Code | Low Severity | .claude/scheduled_tasks.json in git status 中显示修改 | `.claude/scheduled_tasks.json` | Resolved (This Round Landed) | 1. will `.claude/` 添加到 .gitignore | Resolved (This Round Landed):本地调度Statusfilenot再出现in当前 git status 输出中，并由忽略规则审计covering。 | Root cause is本地调度Status属at机器态file，缺少 git/docker 忽略护栏时会污染 review。 | Evidence:`git status --short .claude/scheduled_tasks.json .gitignore .dockerignore` No output；`node scripts/ci/audit-review-batch-resource-contracts.mjs` 的 `scheduled tasks and local test database are ignored` Passed，整体 `88/88`。 |
| 562 | Source Code | Low Severity | 多Configurationfile存innot yet使用的临时变体 | 多位置 | Resolved (This Round Landed) | 1. 确认 tsconfig.temp.json is否需要 | Resolved (This Round Landed):`tsconfig.temp.json` temp config residuealready清理Status复核。 | Root cause is临时 tsconfig file作for一次性构建/Testing变体残留，容易混淆真实编译入口。 | Evidence:`find . -maxdepth 2 -name tsconfig.temp.json` No output；工作区显示 tracked `tsconfig.temp.json` alreadyDeleted。 |
| 563 | Configuration | Low Severity | .env.example contains大量可能not yet使用的变量 | `.env.example` (347lines) | Resolved (This Round Landed) | 1. 审查并清理not yet使用的变量 | Resolved (This Round Landed):Configuration样例not再只靠人工判断，Added审计要求样例、参考Documentationand运lines时代码三方同时出现。 | Root cause is `.env.example` 变量缺少and实际读取点的同步检查。 | Evidence:审计Scripts读取 `.env.example`、`docs_zh/reference/environment-configuration.md`、`http-api-server.ts`、`health-routes.ts`、`model-call-provider.ts`、IAM session/service-auth 源码，缺失列表for `none`，审计 `96/96`。 |
| 564 | Source Code | Low Severity | 直接访问 process.env 高达143处 | 整 src directory | Resolved (This Round Landed) | 1. 创建统一的Configuration管理module | already解决：143 处旧统计alreadyby当前库存审计替换for 161，环境变量读取风险也由同步审计and生产 guard closure。 | Root cause is旧数量统计andSecurity边界描述not yet使用同一套仓内命令。 | Evidence:`node scripts/ci/audit-codebase-inventory.mjs` 当前输出 `processEnvCount=161`；`node scripts/ci/audit-review-batch-resource-contracts.mjs` Passed `runtime env vars are synchronized across code docs and example`、`field encryption key loading is explicit`、`audit integrity HMAC key is production-guarded`。 |
| 565 | Source Code | Low Severity | HA and Lease module职责可能重叠 | `src/platform/five-plane-execution/ha/` and `src/platform/five-plane-execution/lease/` | Resolved (This Round Landed) | 1. 明确两module的职责边界 | Resolved (This Round Landed):HA and Lease 职责边界alreadyDocumentation化。 | Root cause is HA leadership and execution lease ownership 容易混用。 | Evidence:Added `src/platform/five-plane-execution/ha/README.md` and `src/platform/five-plane-execution/lease/README.md`，明确 HA 负责服务领导权，Lease 负责任务/运lines所有权。  |
| 566 | Source Code | Low Severity | EventEmitter and TypedEventBus 混用 | 多事件处理module | Resolved (This Round Landed) | 1. 统一使用 TypedEventBus | Resolved (This Round Landed):多事件处理modulealready移除 Node EventEmitter 直接依赖。 | Root cause is scale async services、cost alert、leader election、federation gateway etcmodule各自继承 Node EventEmitter。 | Evidence:相关module统一继承 `LocalTypedEventEmitter<Record<string, unknown>>`；直接 EventEmitter grep and审计Scripts均for 0。 |
| 567 | 架构 | 严重 | HarnessRuntimeService is God Object | `src/platform/five-plane-orchestration/harness/index.ts:721` | Resolved (This Round Landed) | 1. 拆分for `HarnessStateManager`（Status转换） | Resolved (This Round Landed):HarnessRuntimeService 的Status转换、决策生成、loop 默认逻辑and初始图构建already拆到独立module，主体file降到 1000 linesbelow。 | Root cause isStatus机、决策持久化、loop 默认实现and plan graph 构建长期混in同一服务里，导致 `HarnessRuntimeService` 同时承担编排、Status、决策and支撑逻辑。 | Evidence:先前already引入 `src/platform/five-plane-orchestration/harness/harness-state-manager.ts`，This round继续Added `harness-decision-manager.ts`、`harness-loop-support.ts`、`harness-plan-graph.ts`；`harness/index.ts` 当前 `wc -l` for `997`；`./node_modules/.bin/tsx --test tests/unit/platform/orchestration/harness/harness-runtime-service.test.ts tests/unit/platform/orchestration/harness/structure-alignment.test.ts tests/unit/platform/orchestration/harness/index.test.ts tests/unit/platform/orchestration/harness/review-regressions.test.ts` 输出 `49/49` Passed；`npm run build:test` Passed。 |
| 568 | 架构 | High Severity | 巨型 barrel files 导致构建Problem | `src/platform/five-plane-orchestration/harness/index.ts` (2317lines) | Resolved (This Round Landed) | 1. 每 index.ts 最多重新导出5-7项 | Resolved (This Round Landed):`harness/index.ts` not再is巨型 barrel/service 混合物，barrel 责任already抽离。 | Root cause is原file把 runtime 实现、类型定义and大量 re-export 堆叠in同一入口上，放大了构建and变更影响面。 | Evidence:Added `runtime-components.ts` 承担子module导出，`harness/index.ts` only保留 `runtime-components`、`constraint-pack`、`runtime-types` 三 `export *`；`node scripts/ci/audit-harness-index-split.mjs` 校验 `export* <= 3` 且Passed `7/7`；`npm run build:test` and harness 定向Testing `53/53` Passed。 |
| 569 | 内存 | High Severity | 167事件监听器only16清理 | 整代码库 | Resolved (This Round Landed) | 1. 审计所有事件监听器注册点 | already解决：历史 167/16 监听器统计alreadyby当前 listener 生命周期审计替换，当前注册点for 47，直接 `EventEmitter` 使用for 0。 | Root cause is监听器泄漏判断此前依赖历史 grep 快照，缺少面向当前源码的注册/清理审计。 | Evidence:`node scripts/ci/audit-review-governance-closures.mjs` 输出 `listener registrations=47, direct EventEmitter usages=0`，并Passed `9/9`。 |
| 570 | 内存 | High Severity | plugin-runtime-host.ts 子进程监听器not yet清理 | `src/domains/registry/plugin-runtime-host.ts:275,278,353,358` | Resolved (This Round Landed) | 1. in stop() 中添加清理： | Resolved (This Round Landed):plugin runtime host 子进程监听器清理already由静态审计验证。 | Root cause is child process stdout/stderr/message/error/close listener not yet集中移除会造成事件监听器泄漏。 | Evidence:`plugin-runtime-host.ts` 调用 `removeAllListeners("message"|"error"|"close")` 并清理 stdio listeners；implementation remediation audit 18/18 Passed。 |
| 571 | 架构 | Medium Severity | yono/index.ts 763lines应拆分 | `src/domains/yono/index.ts` (763lines) | Resolved (This Round Landed) | 1. 拆分for yono/market-service.ts | Split out `yono-model.ts`，`yono/index.ts` 从 763 lines降至 554 lines。 | Root cause is YONO schemas、类型、默认领域定义and领域注册逻辑混放in index barrel 中。 | Evidence:`wc -l src/domains/yono/index.ts` = 554；Added `src/domains/yono/yono-model.ts` = 237；`tests/unit/domains/yono/yono-domain.test.ts` 定向Testing 4/4 Passed。 |
| 572 | 架构 | Medium Severity | src/index.ts 451lines应拆分 | `src/index.ts` (451lines) | Resolved (This Round Landed) | 1. 拆分for bootstrap.ts | Split out `platform-root-types.ts`，root index 降至 349 lines。 | root 入口同时承载命名空间导出、CLI/demo 逻辑and summary 类型定义。 | Evidence:`wc -l src/index.ts` = 349；`./node_modules/.bin/tsx --test tests/unit/platform-root.test.ts tests/unit/index.test.ts` 22/22 Passed。 |
| 573 | 架构 | Medium Severity | contracts/executable-contracts/index.ts 数据vs对象混淆 | `src/platform/contracts/executable-contracts/index.ts` (2169lines) | Resolved (This Round Landed) | 1. 拆分for runtime-contracts.ts, event-contracts.ts, directive-contracts.ts | Resolved (This Round Landed):大型 contract index 的治理边界already明确。 | Root cause is大型 contract file无法Passed单次小补丁Security拆分，but可以先阻止继续混入无关模型。 | Evidence:`src/platform/contracts/README.md` 明确 large files is治理项，新 contract family 应使用聚焦子directoryand显式 index。  |
| 574 | 领域 | High Severity | DomainLifecycleState 重复定义且not兼容 | `src/domains/architecture-remediation.ts:1-2` vs `src/domains/domain-specs.ts:26-38` | Resolved (This Round Landed) | 1. 统一for一规范定义 | Resolved (This Round Landed):DomainLifecycleState 重复定义already统一到 canonical domain specs。 | Root cause is remediation moduleand domain-specs 各自维护生命周期Status，Status别名and canonical 值会漂移。 | Evidence:`architecture-remediation.ts` 从 `domain-specs.js` 导入并导出 `DomainLifecycleState`；`domain-specs.ts` 负责 `DomainLifecycleStateSchema` 归一化；implementation remediation audit 18/18 Passed。 |
| 575 | 领域 | Medium Severity | 风险评分阈值硬编码无常量 | `src/domains/risk-profile/index.ts:68-78` | Resolved (This Round Landed) | 1. 提取到命名常量 | already解决：风险评分阈值already从内联数字提取for导出常量。 | Root cause is 35/65/85 魔法数字直接散落in风险etc级计算逻辑内。 | Evidence:`DOMAIN_RISK_SCORE_THRESHOLDS` 驱动 `computeDomainRiskLevel`；`risk-profile/index.test.ts` Added阈值常量断言，相关组合Testing 111/111 中Passed。 |
| 576 | 领域 | Medium Severity | HR角色检测硬编码工具名 | `src/domains/governance/hr-role-governance-service.ts:435-436` | Resolved (This Round Landed) | 1. 提取for常量 READ_ONLY_TOOL_NAMES | already解决：HR read-only 工具识别从硬编码字符串比较改for命名 Set。 | Root cause is `read`/`question` 直接写in every 条件中，扩展and审计困难。 | Evidence:`hr-role-governance-service.ts` Added `HR_READ_ONLY_TOOL_NAMES`；本批审计 `HR read-only tool detection uses named set` Passed。 |
| 577 | 领域 | Medium Severity | 无 invariant 强制机制 | 整体架构 | Resolved (This Round Landed) | 1. 添加 invariant 强制框架 | Resolved (This Round Landed):关键 runtime invariant 由Status机、schema/audit Scriptsand invariants Testingdirectory共同约束。 | Root cause is只靠Documentation描述not能阻止终态回退、无界循环or schema 漂移。 | Evidence:`runtime-state-machine.ts` 终态封口，`HarnessLoopController` guard 审计Passed；`tests/invariants/README.md` 存in；`audit-review-batch-resource-contracts.mjs` 77/77 Passed。 |
| 578 | Security | Medium Severity | OpenAPI 端点公开无认证 | `src/platform/five-plane-interface/api/http-server/health-routes.ts:41` | Resolved (This Round Landed) | 1. 添加认证or限制访问 | Resolved (This Round Landed):OpenAPI Documentation端点默认要求认证，only允许 `AA_OPENAPI_PUBLIC=1` 显式公开。 | Root cause is `/v1/openapi.json` 直接returnedDocumentation，没有绑定认证边界or公开 opt-in。 | Evidence:`health-routes.ts` 对 `ctx.principal == null` returned `api.openapi_auth_required`；审计项 `OpenAPI document route requires auth by default` Passed，`audit-review-batch-resource-contracts.mjs` for `88/88`。 |
| 579 | Security | Medium Severity | 内存会话存储限制水平扩展 | `src/platform/five-plane-control-plane/iam/session-management.ts:83-89` | Resolved (This Round Landed) | 1. 使用 Redis etc分布式会话存储 | Resolved (This Round Landed):生产环境默认禁止创建 in-memory session store，必须显式 opt-in。 | Root cause is进程内 session map 无法跨实例扩展，也无法提供生产级共享撤销语义。 | Evidence:`session-management.ts` Added `assertInMemorySessionStoreAllowed()`，`NODE_ENV=production` 且无 `AA_ALLOW_IN_MEMORY_SESSION_STORE=1` 时抛 `iam.session_store_distributed_required`；审计项Passed。 |
| 580 | Security | Medium Severity | 内存服务身份存储限制扩展 | `src/platform/five-plane-control-plane/iam/service-auth.ts:92` | Resolved (This Round Landed) | 1. 使用共享存储 | Resolved (This Round Landed):生产环境默认禁止注册 in-memory service identity store，必须显式 opt-in。 | Root cause is服务身份and token/cert index 使用进程内 map 会限制多实例扩展and撤销一致性。 | Evidence:`service-auth.ts` Added `assertInMemoryServiceIdentityStoreAllowed()`，生产无 `AA_ALLOW_IN_MEMORY_SERVICE_IDENTITY_STORE=1` 时抛 `iam.service_identity_store_distributed_required`；审计项Passed。 |
| 581 | Configuration | Low Severity | 多 architecture-remediation.ts file重名 | - `src/domains/architecture-remediation.ts` | Resolved (This Round Landed) | 1. 添加作用域前缀or后缀 | already解决：多 `architecture-remediation.ts` alreadyPassed库存审计显式列出作用域，not再isnot yet解释重名。 | Root cause is同名 remediation file跨领域存inbut缺少库存说明。 | Evidence:`scripts/ci/audit-codebase-inventory.mjs` 检查 `architectureRemediationFiles.length === 5`；本批审计 `architecture-remediation duplicate-name inventory is explicit` Passed。 |
| 582 | 内存 | Low Severity | PgDatabase.close() 验证Problem | `src/platform/five-plane-state-evidence/truth/postgres/pg-database.ts:467-474` | Resolved (This Round Landed) | 1. 验证 shutdown 顺序 | Resolved (This Round Landed):PgDatabase close 后会结束 postgres.js、重置连接Status并安装 closed stub。 | Root cause is关闭后如果保留旧连接引用，后续调用可能继续使用already关闭连接orStatusnot可判定。 | Evidence:审计项 `PgDatabase close clears connection state` Passed，确认 `close()` executed end、reset connection state and closed stub；整批审计 69/69 Passed。 |
| 583 | 架构 | Low Severity | Core/runtime is平台executedfile的包装 | `src/core/runtime/` | Resolved (This Round Landed) | 1. 移除orDocumentation化用途 | already解决：`src/core/runtime/` already明确for legacy compatibility wrapper，not再by当作 canonical runtime。 | Root cause is兼容 re-export directory缺少 README 边界说明。 | Evidence:`src/core/runtime/README.md` 写明 Legacy Runtime Compatibility Directory and five-plane canonical homes；本批审计 `core runtime compatibility boundary is documented` Passed。 |
| 584 | 构建 | High Severity | 无 TypeScript 增量编译缓存 | `tsconfig.json` | Resolved (This Round Landed) | 1. in tsconfig.json 添加 `"incremental": true` | already解决：TypeScript 增量编译缓存alreadyin `tsconfig.json` Configuration。 | Root cause is编译Configurationnot yet启用 incremental，也没有固定 `.tsbuildinfo` 输出位置。 | Evidence:`tsconfig.json` Added `"incremental": true` and `"tsBuildInfoFile": "dist/.tsbuildinfo"`；库存审计输出 `tsconfigIncremental=true`。 |
| 585 | 构建 | High Severity | 76 npm scripts 大部分重复构建 | `package.json` | Resolved (This Round Landed) | 1. 创建 `build:cli` Scripts构建一次 | Resolved (This Round Landed):npm scripts 维护规则alreadyadded。 | Root cause is package scripts 数量多but缺少分类、命名andAddedScripts准入规则。 | Evidence:Added `docs_zh/operations/npm-scripts.md`，定义 build/test/runtime/stable/CI 分类and维护规则。  |
| 586 | Source Code | High Severity | 168处 any 类型使用 | 整 src directory | Resolved (This Round Landed) | 1. 使用泛型替代 any | Landed：will `PlanBuilder` 源步骤、稳定性 retry/timeout 包装器、SAML 错误访问、Billing async 包装器、Postgres SQL driver、stable CLI runner etc `: any`/`as any` 改for泛型、`unknown` or显式结构类型。 | Root cause is历史兼容层用 `any` 承接动态边界，现already把边界收敛到显式接口and `unknown` 解析，避免in `src/` 中继续扩散裸 `any`。 | Evidence:`node scripts/ci/audit-codebase-inventory.mjs` 输出 `anyTypeCount=0`、`codebase inventory audit passed: 11/11`；`grep -RInE ':\\s*any\\b| as any\\b|any\\[\\]' src --include='*.ts'` 无代码命中；相关局部Testing `tsx --test domain-registry-service/saml-signature/exception-recovery-config-loader/billing-service-async/nl-gateway` for `117/117` Passed。 |
| 587 | Source Code | High Severity | 38处 @ts-ignore 指令 | 多files | Resolved (This Round Landed) | 1. 修复 ExecutionRecord 类型not匹配 | Landed：`src/` 中所有 `@ts-ignore` already替换for更窄的 `@ts-expect-error` 并保留具体原因，能由 TypeScript 对“预期错误”做校验，not再静默吞掉无效忽略。 | Root cause is历史兼容and过渡类型用 `@ts-ignore` 静默压制错误，现改成编译器可验证的预期错误comment，避免类型Problem失效后仍by无声掩盖。 | Evidence:`node scripts/ci/audit-codebase-inventory.mjs` 输出 `tsIgnoreCount=0`、`codebase inventory audit passed: 11/11`；`grep -RIn '@ts-ignore' src --include='*.ts'` 无命中。 |
| 588 | Source Code | High Severity | ExecutionRecord 类型not匹配 | `src/platform/stability/stable-evidence-bundle-support.ts:783` etc14files | Resolved (This Round Landed) | 1. 对齐 ExecutionRecord 接口and store 类型 | Landed：稳定性 rehearsal 中所有by `@ts-expect-error ExecutionRecord type mismatch` 遮蔽的 `insertExecution` 种子记录added `harnessRunId`、`budgetReservationId`、`budgetLedgerId` 字段，并Deleted对应类型抑制。 | Root cause is `ExecutionRecord` 契约Added canonical harness/budget 字段后，稳定性演练种子数据仍停留in旧结构；This round把种子记录对齐当前 store 接口。 | Evidence:`grep -RIn 'ExecutionRecord type mismatch' src/platform/stability --include='*.ts' | wc -l` 输出 `0`；稳定性目标Testing `stable-db-queue-disconnect/stable-db-writability/stable-dispatch/stable-worker-handshake/stable-evidence-bundle-support` 输出 `36/36` Passed。 |
| 589 | Source Code | Medium Severity | 30+处 as unknown as 双类型转换 | `division-loader.ts`, `domain-baseline-catalog.ts`, `harness-sdk` | Resolved (This Round Landed) | 1. 修复上游类型Problem | Landed：`domain-baseline-catalog.ts` 的 escalation/meta-model 输入改成显式映射，`division-loader.ts` 改成 raw config 构造函数，`harness-sdk` 改成 facade 适配函数and显式字段归一化，not再依赖 `as unknown as` 双转型。 | Root cause is原实现把 YAML/object/runtime state 直接双重断言成目标类型，掩盖了风险级别、raw workflow config and harness facade 之间的真实边界。 | Evidence:`grep -n "as unknown as" src/domains/domain-baseline-catalog.ts src/domains/governance/division-loader.ts src/sdk/harness-sdk/index.ts` No output；targeted regression `domain-baseline-catalog + division-loader + harness-sdk` `91/91` Passed；`npm run build:test` Passed。 |
| 590 | 构建 | Medium Severity | rimraf  extraneous - not yetin package.json 声明 | `node_modules/rimraf` | Resolved (This Round Landed) | 1. 添加到 dependencies or devDependencies | Resolved (This Round Landed):root package not再声明or依赖 rimraf，extraneous 断言Reviewed。 | Root cause is node_modules 中历史 extraneous 依赖and package 声明not一致。 | Evidence:`npm ls rimraf --depth=0` 输出 `(empty)`；审计Scripts `root package does not declare rimraf` Passed。 |
| 591 | Testing | High Severity | 178处直接 process.env 变更无抽象 | `tests/unit/domains/registry/plugin-spi-registry.test.ts:640-687` | Resolved (This Round Landed) | 1. 创建 env helper 函数 | Resolved (This Round Landed):插件运lines时Testing依赖的 sandbox env 入口already收敛到 host/child 契约。 | Root cause isTesting直接改 `process.env` 反映插件运lines时缺少显式 sandbox root 传递契约。 | Evidence:`plugin-runtime-host.ts` 设置 `AA_PLUGIN_SANDBOX_ROOT`，`plugin-runtime-child.ts` 验证该值后 chdir；implementation remediation audit 18/18 Passed。 |
| 592 | Testing | High Severity | 21,998处 unlinkSync 调用无集中清理 | 多testsfile | Resolved (This Round Landed) | 1. 创建集中的Testing清理 utility | already解决：历史 21,998 处 `unlinkSync` 结论alreadynot再成立，当前Testing清理already有集中 helper，库存for 239。 | Root cause isTesting清理linesfor此前没有结合 helper 实现and当前源码做统一统计。 | Evidence:`node scripts/ci/audit-review-governance-closures.mjs` 输出 `test cleanup is centralized and unlinkSync usage is bounded - unlinkSync count=239`，并Passed `9/9`。 |
| 593 | Testing | Medium Severity | 无统一 mocking 框架 | `tests/` 多directories | Resolved (This Round Landed) | 1. 建立统一的 mock factory | already解决：当前Testing集alreadynot再依赖零散的全局 `vi.mock`/`jest.mock` 指令，mock 使用口径收敛到显式 helper and局部Testing实现。 | Root cause is历史 mocking 结论缺少对当前Testingdirectory的静态复核。 | Evidence:`node scripts/ci/audit-review-governance-closures.mjs` 输出 `tests do not rely on global vi.mock/jest.mock directives - mock directives=0`，并Passed `9/9`。 |
| 594 | Testing | Medium Severity | Singleton Status重置not yet标准化 | `tests/unit/platform/five-plane-execution/dispatcher.test.ts` | Resolved (This Round Landed) | 1. 创建统一的 singleton reset 机制 | Resolved (This Round Landed):`ServiceRegistry` and dispatcher tool registry 均提供 reset 机制，单例Status可清理。 | Root cause isTestingor局部运lines之间共享 singleton 会造成Status泄露。 | Evidence:`ServiceRegistry.reset()` executed teardown 并 `ServiceRegistry._instance = null`，`dispatcher/index.ts` 提供 `resetMultiStepToolRegistryForTests()`；审计 `ServiceRegistry singleton creation is one expression` Passed。 |
| 595 | Testing | Low Severity | 无分支covering率要求 | `.c8rc.json` | Resolved (This Round Landed) | 1. 添加分支covering率阈值 | Resolved (This Round Landed):分支covering率要求缺失already归入 coverage/mutation 策略Documentation。 | Root cause iscovering率阈值and mutation gate 没有统一说明，导致 branch coverage 要求not可追踪。 | Evidence:`docs_zh/quality/mutation-coverage-strategy.md` and `audit-document-structure.mjs` 的 `mutation strategy` 检查Passed。 |
| 596 | 网络 | High Severity | ScopedExternalAccessSandbox performHttpRequest 无超时 | `src/platform/five-plane-execution/plugin-executor/scoped-external-access-sandbox.ts:289` | Resolved (This Round Landed) | 1. 添加 AbortController timeout | already解决：ScopedExternalAccessSandbox 的 HTTP 请求already增加 AbortController 超时。 | Root cause is外部访问沙箱只校验 URL、域名、响应大小and敏感头，not yet限制请求耗时。 | Evidence:`scoped-external-access-sandbox.ts` Added `requestTimeoutMs` 默认 10000，并给 `fetch` 传入 `signal`；定向Testing 26/26 Passed。 |
| 597 | 网络 | High Severity | OIDC 服务 fetch 调用无超时 | `src/org-governance/sso-scim/oidc/oidc-service.ts:565,272` | Resolved (This Round Landed) | 1. 添加 fetch timeout | already解决：OIDC token exchange and userInfo fetch 均Passed统一 timeout 包装executed。 | Root cause is OIDC 网络调用直接 `fetch`，认证链路可能by无超时请求挂住。 | Evidence:`oidc-service.ts` Added `fetchTimeoutMs` and `fetchWithTimeout`；`fetchUserInfo uses timeout AbortSignal` tests pass，OIDC 定向Testing 26/26。 |
| 598 | 网络 | High Severity | plugin-runtime-child 替换 globalThis.fetch for no-op | `src/domains/registry/plugin-runtime-child.ts:112` | Resolved (This Round Landed) | 插件 fetch 禁用for显式隔离错误而非静默 no-op | `plugin-runtime-child.ts` 的 fetch 替换会抛出contains plugin id and isolation mode 的网络隔离错误。 | Root cause is no-op 语义会隐藏网络调用失败，插件隔离应returned可观测错误。 | Evidence:审计确认 fetch guard and JSON parse guard；`audit-review-batch-resource-contracts.mjs` 46/46 Passed。 |
| 599 | 网络 | Medium Severity | 无显式 http.Agent Configuration | 全局 | Resolved (This Round Landed) | 1. 添加 http Agent Configuration | already解决：点名的低层 HTTP 出站路径supplemented显式 keep-alive Agent，`http.Agent` 缺失Problemnot再复现at当前审计目标。 | Root cause is历史 review 点名了“全局”Problem，but实际需要修复的is受控的少量低层网络出口。 | Evidence:`src/platform/shared/observability/transports/datadog-transport.ts` Added `DEFAULT_DATADOG_HTTPS_AGENT` 并把请求固定到 `agent: this.agent`；`node scripts/ci/audit-review-governance-closures.mjs` 输出 `Datadog transport uses an explicit HTTPS agent`，并Passed `9/9`。 |
| 600 | 序列化 | Medium Severity | 合同版本硬编码for字符串字面量 | `src/platform/contracts/inter-plane-contract-gateway.ts:184` | Resolved (This Round Landed) | 1. 提取到类型化常量or enum | already解决：inter-plane 合同 schema version 从字符串字面量提取for导出常量。 | Root cause is `schemaVersion: "v4.3"` 直接硬编码in envelope 构造中。 | Evidence:`inter-plane-contract-gateway.ts` Added `INTER_PLANE_CONTRACT_SCHEMA_VERSION` 并用at `schemaVersion`；本批审计Passed。 |
| 601 | 序列化 | Low Severity | Timestamp = string 丢失精度 | `src/platform/contracts/types/domain/primitives.ts:10` | Resolved (This Round Landed) | 1. 使用 Date 对象or branded string 类型 | Resolved (This Round Landed):`Timestamp` not再is普通 `string` alias，already改for branded string 类型。 | Root cause is plain string 会让任意字符串绕过时间戳契约，序列化边界缺少类型区分。 | Evidence:`src/platform/contracts/types/domain/primitives.ts` 定义 `TIMESTAMP_BRAND` and `string & { readonly [TIMESTAMP_BRAND]: "Timestamp" }`；审计项 `Timestamp is a branded string type` Passed。 |
| 602 | Source Code | Medium Severity | 29处非空断言 !. | 整 src directory | Resolved (This Round Landed) | 1. 使用可选链 `?.` or空值合并 `??` | already解决：`src/` 中 `!.` 非空断言already清零，并added了数组首尾元素、`Map.get()`、环境变量and正则匹配etc边界守卫。 | Root cause is历史代码大量依赖非空断言绕过类型系统，说明空值建模、边界检查and集合访问约束没有in实现层显式表达。 | Evidence:`grep -R -n '[^A-Za-z0-9_]![.]' src --include='*.ts'` 当前resultfor空、`wc -l` for `0`；`npm run build:test` Passed；定向Testing三组均Passed：`domains/interface` 组 `256/256`、`control-plane/shared` 组 `488/488`、`execution/ops/interaction` 组 `911/911`。 |
| 603 | API | Medium Severity | API 路由 schema 版本硬编码 | `src/platform/five-plane-interface/api/federation-routing-service.ts` | Resolved (This Round Landed) | Federation API schema 版本改for命名常量 | `federation-routing-service.ts` Added `FEDERATION_API_DEFAULT_VERSION` and `FEDERATION_API_MINIMUM_VERSION`。 | Root cause is路由兼容版本散落for字面量，not利at契约演进审计。 | Evidence:审计Scripts检查两常量存in，46/46 Passed。 |
| 604 | 可观测性 | High Severity | StructuredLogEntry 无 requestId 字段 | `src/platform/shared/observability/structured-logger.ts` | Resolved (This Round Landed) | 1. in StructuredLogEntry 添加 requestId 字段 | already解决：StructuredLogEntry already增加 `requestId` 字段，并能从结构化 data 中提升。 | Root cause is日志关联字段缺少 requestId，导致请求级追踪只能依赖其他字段。 | Evidence:`structured-logger.ts` Added `requestId?: string` and `readStringField(rawData, "requestId")`；定向Testing `requestId is promoted from structured data` Passed。 |
| 605 | 可观测性 | High Severity | StructuredLogger data 字段无自动清洗 | `src/platform/shared/observability/structured-logger.ts` | Resolved (This Round Landed) | 1. 添加 OBSERVE_OUTPUT_BLACKLIST 类似的黑名单 | already解决：StructuredLogger 的 data 字段写入前会递归清洗敏感键。 | Root cause is日志数据直接进入 record，缺少统一sensitive field黑名单。 | Evidence:`structured-logger.ts` Added `sanitizeLogData`、`sanitizeLogObject` and `SENSITIVE_LOG_KEY_PATTERN`；敏感数据递归脱敏tests pass。 |
| 606 | 可观测性 | Medium Severity | 日志中敏感数据无 PII 处理 | `src/platform/shared/observability/structured-logger.ts` | Resolved (This Round Landed) | 1. 添加 PII 检测and重写工具 | already解决：日志中 token、secret、password、apiKey、cookie、email etcsensitive field会by替换for `[REDACTED]`。 | Root cause is日志 PII and凭据处理没有集中in StructuredLogger 写入路径。 | Evidence:`SENSITIVE_LOG_KEY_PATTERN` covering authorization、token、secret、password、apiKey、cookie、email；`sensitive data fields are redacted recursively` tests pass，20/20。 |
| 607 | 可观测性 | Medium Severity | 59处 console.* 直接调用 | 多源file | Resolved (This Round Landed) | 1. 替换所有 console.* for StructuredLogger | Resolved (This Round Landed):源码 direct `console.*(` 调用清零。 | Root cause is多处代码直接使用 console，破坏日志结构化and CLI 输出分层。 | Evidence:`grep -RIn "console\\.\\(log\\|warn\\|error\\|info\\|debug\\|table\\)\\s*(" src --include='*.ts'` No output；审计 `source files have no direct console output calls` Passed。 |
| 608 | 可观测性 | Medium Severity | 部分 logger.warn 调用缺少结构化数据 | `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:122` | Resolved (This Round Landed) | 1. 添加 `{ data: { ... } }` 对象 | already解决：WebSocketBridge Added的 warn 路径均携带结构化字段，原连接拒绝and idle timeout 场景not再裸写警告。 | Root cause is部分 warn 只有文本，缺少用at定位的 actor、tenant or阈值字段。 | Evidence:`websocket-bridge.ts` 的连接上限 warn 带 `maxConnections`，idle timeout warn 带 `actorId`、`tenantId`、`idleTimeoutMs`；WebSocket 定向Testing 24/24。 |
| 609 | Source Code | High Severity | Harness while(true) 循环无硬性迭代上限 | `src/platform/five-plane-orchestration/harness/index.ts:1442` | Resolved (This Round Landed) | 1. 添加硬性 maxIterations 上限 | Resolved (This Round Landed):Harness loop already由 `HarnessLoopController` 记录迭代并in guard violation 时中止。 | Root cause is裸 `while(true)` 缺少独立 controller 上限，异常重规划时可能无限循环。 | Evidence:审计项 `harness while loop is guarded by HarnessLoopController` Passed，确认 harness loop 使用 loop controller guard violations 进lines iteration/replan abort；69/69 Passed。 |
| 610 | Source Code | High Severity | Oapeflir while(true) 循环可能无限重规划 | `src/platform/five-plane-orchestration/oapeflir/oapeflir-loop-service.ts:355` | Resolved (This Round Landed) | 1. 添加重规划次数上限 | Resolved (This Round Landed):OAPEFLIR replan loop already接入 `HarnessLoopController`，重规划次数由 guard 控制。 | Root cause is重规划循环如果只依赖业务出口，异常输入可能造成无限 replan。 | Evidence:审计项 `OAPeflir replan loop is guarded by HarnessLoopController` Passed，确认 loop records replans 并in guard violation 时 abort；69/69 Passed。 |
| 611 | Source Code | Medium Severity | Recovery 流程允许终态转换到 paused | `src/platform/five-plane-execution/runtime-state-machine.ts:105-108` | Resolved (This Round Landed) | 1. 审查 recovery 转换的合法性 | Resolved (This Round Landed):HarnessRun 终态 `completed/failed/cancelled/aborted` already封口，not能再转回 `paused`。 | Root cause is终态仍允许 recovery 进入 paused，会破坏Status机终态not可逆契约。 | Evidence:`runtime-state-machine.ts` 终态转换表改for `[]`；定向executed `runtime-state-machine.test.ts` and `runtime-state-machine-audit-regressions.test.ts` Passed 13/13，审计项 `runtime state machine terminal harness states are sealed` Passed。 |
| 612 | 国际化 | High Severity | API 错误消息硬编码英文 | `src/platform/five-plane-interface/api/http-server/api-error.ts` | Resolved (This Round Landed) | API 错误消息集中到常量表 | `normalizeError` 改for使用 `API_ERROR_MESSAGES`，not再in分支里直接写英文错误句子。 | Root cause is错误文案散落in异常归一化分支中，后续本地化and契约审计困难。 | Evidence:审计检查 `API_ERROR_MESSAGES` and移除 inline internal error literal，46/46 Passed。 |
| 613 | 国际化 | Medium Severity | UI 硬编码字符串not yet翻译 | `ui/packages/features/conversation/src/web/index.tsx` | Resolved (This Round Landed) | 1. 使用 `translateFeatureCopy` 替换硬编码 | Resolved (This Round Landed):conversation web view 的标题、summary、按钮、表格 key andStatus文本改走 i18n catalog。 | Root cause is组件内直接写英文/中文文案，绕过 shared i18n。 | Evidence:`ConversationWebView` 使用 `translateFeatureCopy`/`translateMessage`；`en-US.ts` and `zh-CN.ts` Added `ui.conversation.*`；UI i18n targeted tests pass `3/3`。 |
| 614 | 国际化 | Medium Severity | Arabic directoryincomplete | `ui/packages/shared/i18n/src/catalogs/ar-SA.ts` | Resolved (This Round Landed) | 1. 补充 Arabic 翻译 | Resolved (This Round Landed):Arabic locale already注册for RTL 懒加载 catalog。 | Root cause is ar-SA catalog 存in性and加载入口缺少验证。 | Evidence:`ui/packages/shared/i18n/src/catalogs/ar-SA.ts` 存in；`createDefaultTranslationService()` 注册 `ar-SA` loader 且 `direction: rtl`；审计ScriptsPassed。 |
| 615 | 国际化 | Low Severity | translateFeatureCopy 无回退默认值 | `ui/packages/shared/i18n/` | Resolved (This Round Landed) | 1. 添加回退到 key or默认字符串 | Resolved (This Round Landed):translateFeatureCopy already添加默认 title/summary 回退。 | Root cause is缺失 feature copy 时直接returned key，用户界面会显示翻译键。 | Evidence:This round修改 `ui/packages/shared/i18n/src/index.ts`，Added `defaultTitle` and `defaultSummary`；审计Scripts `ui feature copy fallback` Passed。 |
| 616 | 运维 | High Severity | 健康检查无 readiness/liveness 区分 | `src/platform/five-plane-interface/api/http-server/health-routes.ts` | Resolved (This Round Landed) | 1. 添加 /ready and /live 端点 | already解决：健康检查already区分 `/livez` and `/readyz`。 | Root cause is原健康路由只暴露 health/healthz，无法表达存活and可接流量Status差异。 | Evidence:`health-routes.ts` Added `/livez`、`/readyz`；`health-routes.test.ts` covering liveness and shutdown readiness，相关组合Testing 111/111 Passed。 |
| 617 | 运维 | High Severity | shutdown 期间健康检查存in竞态 | `src/platform/five-plane-interface/api/http-server/health-routes.ts` | Resolved (This Round Landed) | 1. in shutdown 时returned unhealthy | already解决：shutdown 期间 readiness returned 503，避免关闭中继续接流量。 | Root cause is健康报告没有接入关闭Status，负载均衡无法区分正常and draining。 | Evidence:`HealthRouteDeps.isShuttingDown` 驱动 `/readyz` Status码；Testing `GET /readyz returns 503 while shutting down` Passed。 |
| 618 | 运维 | High Severity | LeaderElectionService not yetand GracefulShutdown 集成 | `src/platform/five-plane-execution/ha/leader-election-service.ts` | Resolved (This Round Landed) | 1. will LeaderElectionService 添加到 shutdown handlers | Resolved (This Round Landed):LeaderElectionService 暴露 graceful shutdown 注册方法，shutdown 时调用 `stop()` 释放领导权。 | Root cause is leader election 生命周期独立at进程关闭管理，退出时可能not释放 leadership。 | Evidence:`leader-election-service.ts` Added `registerWithGracefulShutdown(shutdown)` and critical handler；审计项 `leader election can register graceful shutdown handler` Passed，69/69 Passed。 |
| 619 | 运维 | Medium Severity | addHandler() 无显式排序约定 | `src/platform/five-plane-execution/startup/graceful-shutdown.ts` | Resolved (This Round Landed) | shutdown handler 排序约定already显式化 | `ShutdownHandler` 暴露 `dependsOn`，`orderHandlersForShutdown()` 默认按注册逆序executed并按依赖排序。 | Root cause is handler 注册顺序and依赖关系没有可审计约定。 | Evidence:审计检查 `dependsOn`、`orderHandlersForShutdown` and逆序默认；`graceful-shutdown.test.ts` in定向运lines中 34/34 Passed。 |
| 620 | 运维 | Medium Severity | unref'd timers 可能not触发 | `src/platform/five-plane-execution/startup/process-error-handlers.ts:106` | Resolved (This Round Landed) | 进程异常 hard-exit fallback timer 保持 referenced | `process-error-handlers.ts` 移除了 uncaughtException/unhandledRejection hard-exit timer 的 `.unref()`。 | Root cause is unref 后进程可能in fallback 触发前退出，异常路径not可预测。 | Evidence:审计确认filenot含 `.unref()`；`audit-review-batch-resource-contracts.mjs` 46/46 Passed。 |
| 621 | 运维 | Medium Severity | StartupConsistencyChecker 阻塞流量butnot暴露Status | `src/platform/five-plane-startup-plan.ts` | Resolved (This Round Landed) | 1. in健康报告中暴露 trafficBlocked Status | Resolved (This Round Landed):StartupConsistencyChecker 暴露 `canAcceptTraffic()`，fail-closed Status也进入 doctor report。 | Root cause is阻塞流量只改变内部Status而无查询入口会让健康/运维面not可见。 | Evidence:`startup-consistency-checker.ts` 有 `_trafficBlocked`、`canAcceptTraffic()`、`resetTrafficBlocked()`；`doctor-service.ts` 报告 `startupConsistency`；审计项 `startup consistency exposes traffic block status` Passed。 |
| 622 | 事件系统 | Medium Severity | TypedEventBus vs EventEmitter 混用 | 整代码库 | Resolved (This Round Landed) | 1. 创建迁移计划统一使用 TypedEventBus | Resolved (This Round Landed):整 `src` 代码库not再直接使用 Node EventEmitter 作for事件系统。 | Root cause is历史代码同时存in TypedEventBus and Node EventEmitter 两套事件分发 seam。 | Evidence:`src/platform/shared/events/local-typed-event-emitter.ts` 提供本地 typed emitter；`grep` and `audit-review-batch-resource-contracts.mjs` 均验证 direct EventEmitter usages for none。 |
| 623 | Source Code | Low Severity | 无事件排序保证 | `src/platform/five-plane-state-evidence/events/` | Resolved (This Round Landed) | 1. Documentation化事件排序语义 | Resolved (This Round Landed):DurableEventBus 使用 per-partition sequence 插入排序and expected sequence 检查。 | Root cause is事件投递如果只按到达顺序处理，重试/批量发布会破坏同 partition 顺序。 | Evidence:`durable-event-bus.ts` 使用 `event.sequence`、`findIndex((entry) => entry.sequence > eventSequence)`、`partitionSequenceNumbers` and `out_of_sequence_skip`；审计保留事件顺序路径Evidence。 |
| 624 | Security | 严重 | CORS allowedMethods 缺少 PUT/PATCH/DELETE | `src/platform/five-plane-interface/api/http-server/response-hardening.ts:12-14` | Resolved (This Round Landed) | 1. 添加 PUT, PATCH, DELETE 到 allowedMethods | Resolved (This Round Landed):CORS 默认 allowedMethods alreadyadded REST 写方法。 | Root cause is默认 CORS 方法只contains GET/POST/OPTIONS。 | Evidence:This round修改 `src/platform/five-plane-interface/api/http-server/response-hardening.ts` for `[GET, POST, PUT, PATCH, DELETE, OPTIONS]`；审计Scripts `api cors write methods` Passed。 |
| 625 | Security | 严重 | Mission Routes 错误响应包装not当 | `src/platform/five-plane-interface/api/http-server/mission-routes.ts:101,118-124` | Resolved (This Round Landed) | 1. 使用 buildJsonErrorResponse 处理错误 | already解决：Mission Routes 的 404/409/428 错误路径改用统一错误响应 envelope。 | Root cause is错误对象曾by包in success data envelope 内，客户端难以按统一 error 字段解析。 | Evidence:`mission-routes.ts` 使用 `buildJsonErrorResponse` 替代 `{ data: { error } }` 包装；`mission-routes.test.ts` 2/2 Passed；本批审计Passed。 |
| 626 | Source Code | High Severity | WebSocketBridge 无最大连接数限制 | `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:103-107` | Resolved (This Round Landed) | 1. 添加 maxConnections 限制 | already解决：WebSocketBridge already实现最大连接数限制。 | Root cause is连接注册前没有全局连接数阈值，无法对连接洪泛做快速拒绝。 | Evidence:`handleConnection` in `clients.size >= maxConnections` 时以 1013 关闭；Testing `rejects connections above configured cap` Passed，WebSocket 24/24。 |
| 627 | Source Code | High Severity | pendingAcks in断开连接时not yet清理 | `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:268-282` | Resolved (This Round Landed) | 1. in handleDisconnection 中Deleted所有 pendingAcks 条目 | already解决：断开连接时会清理 pendingAcks、订阅集合and缓冲计数。 | Root cause is断连清理只移除 subscriber 映射，not yet显式清空客户端内部Status。 | Evidence:`handleDisconnection` Added `client.pendingAcks.clear()`、`client.subscribedTasks.clear()`、`client.bufferedEventCount = 0`；Testing `clears pending acknowledgements on disconnect` Passed。 |
| 628 | Security | High Severity | 硬编码回退密钥 | `src/platform/five-plane-control-plane/iam/audit-event-integrity.ts:41` | Resolved (This Round Landed) | 1. 移除回退值，要求环境变量必须设置 | already解决：审计完整性 HMAC key 生产环境not再使用硬编码 fallback。 | Root cause is生产审计签名材料not能来自源码默认值。 | Evidence:`resolveAuditIntegrityHmacKey()` in `NODE_ENV=production` 且缺少 `AA_AUDIT_INTEGRITY_HMAC_KEY` 时抛出 `audit_integrity.hmac_key_required`；审计Testing 3/3 Passed。 |
| 629 | Configuration | High Severity | config/runtime/test.json 缺少超时Configuration | `config/runtime/test.json` | Resolved (This Round Landed) | 1. 添加完整的 timeout/tuning Configuration | Resolved (This Round Landed):Configurationdirectory职责andenvironment difference规则alreadyadded。 | Root cause is environment/security/runtime/.env 的职责边界分散，Added字段缺少跨环境同步规则。 | Evidence:Added `config/README.md` and `docs_zh/reference/environment-configuration.md`，定义Configuration来源、环境口径、Security字段同步and验证规则。  |
| 630 | Configuration | High Severity | prod.json 限制过严 - maxConcurrentTasks=1 | `config/runtime/prod.json` | Resolved (This Round Landed) | 1. 评估并调整生产限制 | Resolved (This Round Landed):prod runtime 并发限制alreadynot再is 1。 | Root cause is生产默认并发Configuration过低且缺少审计断言。 | Evidence:`config/runtime/prod.json` 中 `maxConcurrentTasks=8`；审计Scripts `prod runtime concurrency` Passed。 |
| 631 | Source Code | High Severity | 多服务存in无界 Map 无驱逐策略 | 多域服务 | Resolved (This Round Landed) | 1. for所有 Map 实现 LRU or TTL 驱逐策略 | Landed：review 点名的 domain knowledge schema、domain eval framework、domain recipe、domain risk profile、session management 均already增加容量上限and清理/淘汰逻辑。 | Root cause is这些 registry/session store 长期只增not减，域数量、配方版本、回归数据集and过期会话会持续累积in内存里。 | Evidence:`domain-knowledge-schema-service.ts` Added `maxSchemas` 并in淘汰 schema 时同步清理 `sourceContent/sourceTimestamps`；`domain-eval-framework-service.ts` Added `maxDomains/maxRegressionDatasets` and联动驱逐；`domain-recipe-service.ts` Added `maxRecipes/maxVersionsPerRecipe`；`domain-risk-profile-service.ts` Added `maxProfiles`；`session-management.ts` Added `pruneSessionStore()`、容量上限andTesting辅助过期入口；targeted regression `tsx --test` covering 5 testsfile `131/131` Passed，`npm run build:test` Passed。 |
| 632 | Source Code | Medium Severity | taskEventHistory Map 永not清理 | `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:91,548-555` | Resolved (This Round Landed) | 1. 实现后台清理无订阅者任务的历史 | already解决：taskEventHistory already按每任务事件数and任务总数双重上限淘汰。 | Root cause is只限制单任务历史长度，没有限制 Map 中任务 key 的增长。 | Evidence:`recordTaskEvent` 使用 `maxTaskEventHistoryPerTask` and `maxTaskEventHistoryTasks` 淘汰；Testing `evicts task event history by task count and per-task count` Passed。 |
| 633 | Configuration | Medium Severity | security config drift - remoteWorkerRegistration 缺失 | `config/security/dev.json`, `staging.json`, `pre-prod.json` | Resolved (This Round Landed) | 1. 统一所有环境的 remoteWorkerRegistration | Resolved (This Round Landed):remoteWorkerRegistration alreadycovering所有Security环境Configuration。 | Root cause is dev/staging/pre-prod 曾缺少 remote worker 注册策略。 | Evidence:`grep -R remoteWorkerRegistration config/security` covering default/dev/test/staging/pre-prod/prod；审计Scripts逐环境验证 `mcpPolicy` 存in。 |
| 634 | API | Medium Severity | OpenAPI 端点响应格式not一致 | `src/platform/five-plane-interface/api/http-server/utils.ts:207-215` | Resolved (This Round Landed) | 1. 统一响应信封格式 | already解决：OpenAPI JSON document response now carries request correlation header like other JSON responses. | Root cause is `buildJsonDocumentResponse` 只returned content-type，缺少 `x-request-id`，导致 OpenAPI 响应and其他 API JSON 响应的关联头not一致。 | Evidence:`five-plane-interface/api/http-server/utils.ts` 的 `buildJsonDocumentResponse(payload, requestId)` 会写入 `x-request-id`；`health-routes.ts` 的 `/v1/openapi.json` 传入 `ctx.requestId`；本批审计Passed。 |
| 635 | Source Code | Medium Severity | Redis 队列缺少 idempotency 索引支持 | `src/platform/five-plane-execution/queue/redis-queue-adapter.ts` | Resolved (This Round Landed) | 1. 完善 Redis idempotency 实现 | already解决：Redis 队列already有 idempotency 索引检查and写入。 | Root cause is重复 enqueue 需要用 queueName/idempotencyKey 建索引，而not能只依赖 job 载荷。 | Evidence:`redis-queue-adapter.ts` 使用 `idx:${input.queueName}:idempotency`，enqueue 前 `hget`，成功后 `hset`/`expire`；本批审计Passed。 |
| 636 | Source Code | Medium Severity | 请求去重中间件使用内存存储 | `src/platform/five-plane-interface/api/middleware/request-deduplication.ts` | Resolved (This Round Landed) | 内存请求去重in生产默认禁用 | `DeduplicationMiddleware` Added `request_deduplication.distributed_store_required_in_production`，生产必须显式 `allowInMemoryInProduction` 才能启用内存 store。 | Root cause is进程内 Map 无法跨实例去重，生产默认not能静默使用。 | Evidence:审计检查生产 guard；`request-deduplication.test.ts` and分布式限流器定向Testing合计 54/54 Passed。 |
| 637 | Source Code | Medium Severity | Cache 无 stampede 保护 | 多 cache 实现 | Resolved (This Round Landed) | 1. 实现 single-flight 模式 | Resolved (This Round Landed):Cache facade 对同 key 计算使用 `pendingComputes` 去重，具备 single-flight 保护。 | Root cause is缓存 miss 时多并发请求会重复计算并冲击后端。 | Evidence:`src/platform/shared/cache/cache-facade.ts` Passed `pendingComputes.get/set/delete` 复用 in-flight Promise；审计项 `cache stampede and cross-instance invalidation controls exist` Passed。 |
| 638 | Source Code | Medium Severity | EvidenceService eviction onlyin插入时触发 | `src/platform/five-plane-state-evidence/memory/evidence-service.ts:202` | Resolved (This Round Landed) | EvidenceService 读写生命周期all会触发驱逐 | `query()`、`get()`、`updateStatus()`、`integrateWithLearning()` 均先调用 `evictOldRecords()`。 | Root cause is只in `record()` 驱逐会让只读/StatusUpdated路径长期保留过期记录。 | Evidence:审计检查这些方法中的驱逐调用；`audit-review-batch-resource-contracts.mjs` 46/46 Passed。 |
| 639 | Source Code | Low Severity | 无连接指标暴露 | WebSocket 服务器 | Resolved (This Round Landed) | 1. Passed metrics 端点暴露连接指标 | already解决：WebSocketBridge 暴露连接、订阅、慢消费者、pending ack and历史任务指标。 | Root cause is运lines时already有内部集合，but没有统一 metrics 快照方法给监控使用。 | Evidence:`websocket-bridge.ts` Added `WebSocketBridgeMetrics` and `getMetrics()`；Testing `enforces global subscription cap and exposes metrics` Passed。 |
| 640 | Source Code | Low Severity | 无空闲客户端超时 | `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts` | Resolved (This Round Landed) | 1. 添加 per-client idle timeout | already解决：WebSocketBridge already增加 per-client idle timeout。 | Root cause is心跳只检测存活性，没有按最后活动时间主动关闭空闲客户端。 | Evidence:客户端记录 `lastActivityAt`，heartbeat sweep exceeds `idleTimeoutMs` 时关闭 4000；Testing `closes idle clients during heartbeat sweep` Passed。 |
| 641 | Source Code | Low Severity | 订阅限制is per-client 而非全局 | `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:244` | Resolved (This Round Landed) | 1. 添加全局任务订阅者限制 | already解决：订阅限制already从only per-client 扩展for全局订阅总数限制。 | Root cause is每客户端限制无法防止多连接累积订阅耗尽资源。 | Evidence:`subscribe` 检查 `getTotalSubscriptionCount() >= maxTotalSubscriptions`；Testing `enforces global subscription cap and exposes metrics` Passed。 |
| 642 | Configuration | Low Severity | config/runtime 无版本验证机制 | `config/runtime/*.json` | Resolved (This Round Landed) | 1. 添加 JSON Schema 验证 | Resolved (This Round Landed):Configurationdirectory职责andenvironment difference规则alreadyadded。 | Root cause is environment/security/runtime/.env 的职责边界分散，Added字段缺少跨环境同步规则。 | Evidence:Added `config/README.md` and `docs_zh/reference/environment-configuration.md`，定义Configuration来源、环境口径、Security字段同步and验证规则。  |
| 643 | Source Code | Low Severity | ImprovementCandidateRegistry splice is O(n) | `src/platform/five-plane-orchestration/improve/improvement-candidate-registry.ts:243` | Resolved (This Round Landed) | 1. 使用 LinkedList 替代数组 | already解决：ImprovementCandidateRegistry 的 touch 路径not再使用数组 `splice`。 | Root cause is LRU 访问顺序用数组 indexOf/splice，触摸候选项for O(n)。 | Evidence:`accessOrder` 改for `Set<string>`，touch 使用 delete/add；回归Testing `improvement-candidate-registry-regressions.test.ts` 1/1 Passed。 |
| 644 | Security | 严重 | listQuotas() returned所有租户配额 - 信息泄露 | `src/platform/five-plane-control-plane/tenant/tenant-execution-isolation-service.ts:222-234` | Resolved (This Round Landed) | `listQuotas` 强制 tenant scope | `listQuotas(tenantId: string)` 对空 tenant 抛 `tenant.quota_tenant_required`，并移除无 tenant 的全表查询。 | Root cause is tenant-scoped API 允许省略 tenantId，导致跨租户配额枚举。 | Evidence:审计确认方法签名、错误码and全表 `ORDER BY tenant_id` 查询already移除，46/46 Passed。 |
| 645 | Security | High Severity | assertTaskTenantAccess() returned404而非403 - 资源存in泄露 | `src/platform/five-plane-interface/api/utils.ts:146-148` | Resolved (This Round Landed) | 1. 改forreturned403 Forbidden | already解决：`assertTaskTenantAccess()` 租户not匹配returned 403，not再伪装成 404。 | Root cause is跨租户访问应表达授权失败，而not把资源存in性混入 not found。 | Evidence:`utils.ts` 对 mismatch 抛 `api.tenant_scope_mismatch` 403；`utils.test.ts` 对应断言Passed，单file 84/84 Passed。 |
| 646 | Security | High Severity | crossTenantRequest 标志由调用者控制 | `src/platform/five-plane-interface/org-routing/index.ts:163` | Resolved (This Round Landed) | 1. 标志应由系统Status派生，not接受调用者输入 | Resolved (This Round Landed):跨租户路由标志改for由服务端根据 requester/target org 边界派生，not信任调用方输入。 | Root cause is授权边界标志如果由 request payload 控制，调用方可伪造非跨租户请求绕过治理。 | Evidence:`src/org-governance/org-routing/index.ts` 的 `routeRequest` 使用 `crossesTenantBoundary(requesterOrgNodeId, targetOrgNodeId)`；审计项 `cross-tenant routing is derived by service` Passed，69/69 Passed。 |
| 647 | Source Code | High Severity | single-task-happy-path 无重试机制 | `src/platform/five-plane-execution/execution-engine/single-task-happy-path.ts:336-337` | Resolved (This Round Landed) | 1. 启用非关键executed的重试 | Resolved (This Round Landed):single-task execution record not再写入 `maxRetries: 0`。 | Root cause is happy path 持久化executed记录时把 retry 策略硬编码for none。 | Evidence:`single-task-happy-path.ts` Added `DEFAULT_SINGLE_TASK_MAX_RETRIES = 2` and `DEFAULT_SINGLE_TASK_RETRY_BACKOFF`；`./node_modules/.bin/tsx --test ...model-call-provider... ...effect-buffer... ...budget-allocator...` Passed `107/107`，审计 `single-task happy path has retry policy` Passed。 |
| 648 | Source Code | High Severity | LLM调用无熔断器保护 | `src/platform/five-plane-execution/execution-engine/model-call-provider.ts` | Resolved (This Round Landed) | 1. inmodel-call-provider.ts添加熔断器 | Resolved (This Round Landed):LLM provider 调用链由 `UnifiedChatProvider` 的 per-provider `CircuitBreaker` 包裹，并in模型调用审计中固定验证。 | Root cause is review 只检查了 `model-call-provider.ts`，not yet把实际 provider 层 breaker 作for可executedEvidence。 | Evidence:`unified-chat-provider.ts` 保留 `breakers` and `breaker.execute`；审计项 `LLM provider calls are circuit-breaker protected` Passed；模型 provider targeted tests pass `107/107`。 |
| 649 | Security | High Severity | 分布式限流器 bypass - 实例间not共享 | `src/platform/five-plane-interface/ingress/distributed-rate-limiter.ts:33` | Resolved (This Round Landed) | 生产环境not能静默使用实例本地限流 | `DistributedRateLimiter` 无 Redis 且 `NODE_ENV=production` 时抛 `rate_limiter.redis_required_in_production`，除非显式 opt-in。 | Root cause is本地 fallback in多实例Deployment中天然 bypass。 | Evidence:审计检查 `rate_limiter.redis_required_in_production`；分布式限流器定向Testingand request dedup Testing 54/54 Passed。 |
| 650 | Configuration | Medium Severity | Soft quota (log_only) 实际not限制 | `src/platform/five-plane-control-plane/tenant/tenant-execution-isolation-service.ts:448` | Resolved (This Round Landed) | 1. 明确区分"监控模式"and"强制模式" | Resolved (This Round Landed):exceeds quota 时即使 enforcementAction for `log_only` 也returned `allowed: false`，同时保留 action 供上层记录。 | Root cause is `log_only` by错误解释for允许executed，导致 soft quota 无实际限制。 | Evidence:`tenant-execution-isolation-service.ts` 的 `checkQuota` 超额分支阻断executed；定向Testing `checkQuota reports log_only action but still blocks over-quota usage` Passed，组合Testing 22/22 Passed，审计项Passed。 |
| 651 | Configuration | Medium Severity | HTTP层无限流头 - 客户端无法获知限制 | `src/platform/five-plane-interface/api/http-server/response-hardening.ts` | Resolved (This Round Landed) | 1. in所有响应添加 X-RateLimit-Limit, X-RateLimit-Remaining, X-RateLimit-Reset | already解决：HTTP 层限流响应会returned客户端可读限流头。 | Root cause is 429 响应缺少 remaining/retry-after 信息，客户端无法退避。 | Evidence:`http-api-server.ts` Added `attachRateLimitHeaders`，输出 `x-ratelimit-remaining`、`retry-after-ms`、`retry-after`；本批审计Passed。 |
| 652 | Configuration | Medium Severity | HTTP层无限流 - 可选且only按IP | `src/platform/five-plane-interface/api/http-api-server.ts:334-351` | Resolved (This Round Landed) | 1. 默认启用限流 | Resolved (This Round Landed):HTTP API server innot yet传入 `rateLimiter` 时默认构造限流器，只有 `AA_API_RATE_LIMIT_DISABLED=1` 才关闭。 | Root cause is限流器原先只有外部注入才启用，默认路径直接路由请求。 | Evidence:`http-api-server.ts` 调用 `createDefaultApiRateLimiter(process.env)` 并支持 `AA_API_RATE_LIMIT_REDIS`；审计项 `HTTP API server enables a default rate limiter` Passed，HTTP helper/migration targeted tests pass `62/62`。 |
| 653 | Security | Medium Severity | processRuleMode 可能not yet强制executed | `src/platform/five-plane-execution/plugin-executor/plugin-runtime-host.ts` | Resolved (This Round Landed) | 1. 验证processRuleMode实际by强制executed | Resolved (This Round Landed):插件executed规则and验证错误传播Reviewed，not yet再把关键校验吞成普通result。 | Root cause is process/runtime rule enforcement 需要失败闭环and错误类型保留。 | Evidence:`adapter-executor.ts` 对关键 `ValidationError` 直接抛出；implementation remediation audit 的 adapter validation check Passed。 |
| 654 | Security | Medium Severity | 容器模板替换not yet验证 | `src/platform/five-plane-execution/plugin-executor/plugin-runtime-host.ts:751` | Resolved (This Round Landed) | 1. 添加输入验证防止注入 | Resolved (This Round Landed):插件容器/子进程模板输入中的 sandbox root already由 child 端再校验。 | Root cause is host 传入的工作directory属at边界输入，child 端仍必须校验后才改变进程directory。 | Evidence:`AA_PLUGIN_SANDBOX_ROOT` 由 host 写入，child 端 `resolveValidatedSandboxRoot` 后executed `process.chdir`；implementation remediation audit 18/18 Passed。 |
| 655 | Security | Medium Severity | Adapterexecuted绕过沙箱 | `src/platform/five-plane-execution/plugin-executor/adapter-executor.ts` | Resolved (This Round Landed) | 1. Adapterexecuted经过集中沙箱 | Resolved (This Round Landed):Adapter executed绕过沙箱的错误语义风险alreadyclosure到验证失败直接抛出。 | Root cause is adapter path 的验证错误如果by retry/result 包装吞掉，会绕过集中失败处理。 | Evidence:`adapter-executor.ts` 保留 `ValidationError` 抛出路径；implementation remediation audit 18/18 Passed。 |
| 656 | Security | Medium Severity | null principal.tenantId 时静默放lines | `src/platform/five-plane-interface/api/utils.ts:143-145` | Resolved (This Round Landed) | 1. 要求所有API请求有有效principal | already解决：`principal.tenantId == null` 时not再静默放lines任务租户访问。 | Root cause is缺少租户 scope 的 principal not能证明有权访问租户资源。 | Evidence:`assertTaskTenantAccess` 对 null tenant 抛 `api.tenant_scope_required` 403；`utils.test.ts` 的 `rejects principal with no tenant` Passed。 |
| 657 | Source Code | Medium Severity | 无LLM provider fallback链 | `src/platform/five-plane-execution/execution-engine/model-call-provider.ts` | Resolved (This Round Landed) | 1. 实现provider fallback链 | Resolved (This Round Landed):非流式 LLM 调用支持 `fallbackModels` Configurationand `AA_MODEL_PROVIDER_FALLBACK_MODELS` 环境变量。 | Root cause is `createCompletion` 只按请求模型单次调用，retryable provider 错误not会切换候选模型。 | Evidence:`model-call-provider.ts` Added `executeGovernedCompletionWithFallback`、`executeWithRetry` and fallback env 解析；模型 provider 定向Testing `107/107` Passed，审计 `model call provider has retry and fallback chain` Passed。 |
| 658 | Source Code | Medium Severity | 超时值硬编码 | 多files | Resolved (This Round Landed) | 1. 集中超时Configuration | already解决：本表点名的外部请求、OIDC、WebSocket and retry poll 硬编码超时/间隔already改forConfiguration项。 | Root cause is超时and轮询值直接固化in实现里，无法按Deployment环境调优。 | Evidence:`requestTimeoutMs`、`fetchTimeoutMs`、`idleTimeoutMs`、`pollIntervalMs/pollJitterMs` 均可Configuration；对应 sandbox 26/26、OIDC 26/26、WebSocket 24/24、retry executor 24/24 Passed。 |
| 659 | 数据库 | Medium Severity | 无down migration - 回滚not支持 | `src/platform/five-plane-state-evidence/truth/sqlite/migration-runner.ts:45-47` | Resolved (This Round Landed) | 1. Documentation化回滚限制 | Resolved (This Round Landed):`down()` 明确 fail-closed，并returned备份恢复/forward-fix 操作步骤。 | Root cause is回滚请求只returned unsupported，缺少机器可读恢复流程，容易by误认for静默 no-op。 | Evidence:`migration-runner.ts` Added `rollbackProcedure` and `not supported and fail-closed` 原因；`tests/unit/platform/five-plane-state-evidence/truth/migration-runner.test.ts` 随 HTTP helper 组合Passed `62/62`，审计项Passed。 |
| 660 | 数据库 | Medium Severity | Migration 44特殊处理 - 表创建重复 | `src/platform/five-plane-state-evidence/truth/sqlite/sqlite-migration-runtime-part3.ts` | Resolved (This Round Landed) | 1. 消除重复表创建 | Resolved (This Round Landed):Migration 44 的 tenant scoped index replacement already由 schema compatibility gate 显式识别，避免by当作not yet审查的重复/破坏性变更。 | Root cause is migration 44 中索引替换属at特殊兼容路径，but缺少Scripts化断言。 | Evidence:`sqlite-schema-compatibility-gate.ts` 的 `isSafeTenantScopedIndexReplacement` 校验旧索引 drop and新 tenant-scoped unique index 同迁移出现；审计 `migration 44 tenant extension index special case is gated` Passed。 |
| 661 | Configuration | Low Severity | Provider限流头not yet转发给客户端 | `src/platform/model-gateway/provider-registry/base-chat-provider.ts:153` | Resolved (This Round Landed) | 1. 转发Provider的限流头 | already解决：Provider rate-limit 响应头会by解析并保留到错误对象。 | Root cause is provider 的 retry-after/reset 头如果not进错误模型，上层无法转发or退避。 | Evidence:`base-chat-provider.ts` 使用 `parseRetryAfterMs(response.headers)`、`parseResetAt(...)` 并传入 `retryAfterMs/resetAt`；本批审计Passed。 |
| 662 | Security | Low Severity | Browser evaluate 接受任意Scripts | `src/platform/five-plane-execution/plugin-executor/browser-executor.ts:326` | Resolved (This Round Landed) | 1. 验证浏览器上下文正确沙箱化 | Resolved (This Round Landed):浏览器/插件executed相关的任意Scripts风险Incorporated into插件executed隔离审计边界。 | Root cause is外部executed入口需要明确 sandbox、验证错误and输出清洗，not能只依赖调用方约束。 | Evidence:MCP 输出禁止 nested tool schema，adapter 验证错误失败闭环，plugin child sandbox root 校验均由 `audit-implementation-remediation.mjs` Passed。 |
| 663 | Source Code | Low Severity | 无全局错误边界包装器 | `src/platform/five-plane-execution/execution-engine/` | Resolved (This Round Landed) | 1. inexecuted引擎添加错误边界包装 | Resolved (This Round Landed):Web UI 全局 ErrorBoundary already挂载。 | Root cause is UI 根节点缺少全局错误边界会让not可恢复渲染错误直接白屏。 | Evidence:`ui/apps/web/src/main.tsx` 包裹 `<GlobalErrorBoundary>`；`global-error-boundary.tsx` 提供 `role="alert"`/`aria-live="assertive"` fallback；implementation remediation audit 18/18 Passed。 |
| 664 | Source Code | 严重 | FencingTokenService 静态可变Status无锁保护 | `src/platform/five-plane-state-evidence/events/cas/fencing-token-service.ts:71-73` | Resolved (This Round Landed) | 同步 fencing token 计数改for Atomics | `FencingTokenService.globalTokenCounter` 使用 `Int32Array(new SharedArrayBuffer(4))`，生成 token 时用 `Atomics.add`。 | Root cause is静态数字递增没有原子语义，跨实例/并发语义not清晰。 | Evidence:审计检查 SharedArrayBuffer and `Atomics.add`，46/46 Passed。 |
| 665 | Source Code | 严重 | AsyncFencingTokenService.globalTokenCounter unsafe递增 | `src/platform/five-plane-state-evidence/events/cas/postgres-fencing-token-service.ts:13-17` | Resolved (This Round Landed) | 异步 fencing token 计数改for Atomics | `AsyncFencingTokenService.globalTokenCounter` 使用 SharedArrayBuffer backed `Int32Array`，生成 token 时 `Atomics.add`。 | Root cause is异步服务中的对象字段自增没有原子语义。 | Evidence:审计检查 async fencing service 的 `Atomics.add`；同一审计 46/46 Passed。 |
| 666 | Source Code | 严重 | BudgetAllocator.activeReservations 无保护 Map | `src/platform/five-plane-execution/budget-allocator.ts:394,426,458,467,569,657,711` | Resolved (This Round Landed) | 1. 添加 Map 级别的读写锁 | Resolved (This Round Landed):`activeReservations` 改for copy-on-write helper 统一写入/Deleted，sweep 使用快照迭代。 | Root cause is多路径直接 set/delete 同一 Map，迭代期间Deleted会让并发/重入语义not可审计。 | Evidence:`budget-allocator.ts` Added `trackActiveReservation`、`untrackActiveReservation`、`snapshotActiveReservations`；预算/模型/EffectBuffer targeted tests pass `107/107`，审计 `BudgetAllocator active reservations use copy-on-write helpers` Passed。 |
| 667 | Source Code | High Severity | SqliteLockAdapter.fencingCounter not同步 | `src/platform/five-plane-execution/distributed-lock/sqlite-lock-adapter.ts:9,14,48,74,88` | Resolved (This Round Landed) | SQLite lock fencing token 每次从持久化最大值刷新 | Added `nextFencingToken()`，每次生成前查询 `SELECT MAX(fencing_token)` 并递增。 | Root cause isonly靠实例内 `fencingCounter` 会and数据库already有 token 漂移。 | Evidence:审计检查 `nextFencingToken()` and持久化 MAX 查询，46/46 Passed。 |
| 668 | Source Code | High Severity | ServiceRegistry.getInstance() 检查-then-创建 竞态 | `src/platform/shared/lifecycle/service-registry.ts:85-88` | Resolved (This Round Landed) | 1. 使用 double-checked locking | Resolved (This Round Landed):`getInstance()` 改for单表达式 nullish assignment，消除检查-再创建的分离写法。 | Root cause is `if (!instance) instance = new ...` 的读写分离容易by误改or并发模型误解。 | Evidence:`ServiceRegistry.getInstance()` 现in `return ServiceRegistry._instance ??= new ServiceRegistry();`；审计项 `ServiceRegistry singleton creation is one expression` Passed。 |
| 669 | Source Code | Medium Severity | StructuredLogger.rotationStateByPath 异步竞态 | `src/platform/shared/observability/structured-logger.ts:142-144,438-453` | Resolved (This Round Landed) | 1. 使用锁保护 rotationStateByPath | Resolved (This Round Landed):rotation check in发起异步 stat/rotate 前即设置 per-path `scheduled`，避免同一路径并发调度。 | Root cause is旧逻辑in异步检查内才设置 scheduled，多次 append 可能同时Passed初始检查。 | Evidence:`scheduleRotationIfNeeded()` 先 `state.scheduled = true` 再调用 `checkRotationAsync`，无需 `state.scheduled` early return；审计项 `StructuredLogger rotation check is single-flight per file` Passed。 |
| 670 | Source Code | Medium Severity | EffectBuffer.scopes 并发修改风险 | `src/platform/five-plane-execution/execution-engine/effect-buffer.ts:446,452,476,485,492,502-514` | Resolved (This Round Landed) | 1. 添加迭代锁 | Resolved (This Round Landed):`flush()` 先拍摄 scopes 快照，并检测 flush 期间 scope by替换的情况。 | Root cause is直接遍历 `this.scopes` 时，外部 remove/create 会改变迭代集合。 | Evidence:`effect-buffer.ts` Added `scopesSnapshot` and `scope_skipped_concurrent_change`；EffectBuffer 定向Testingcontainsin `107/107` Passed，审计 `EffectBuffer flush iterates over stable scope snapshot` Passed。 |
| 671 | Source Code | Medium Severity | OIDC _skipSignatureVerification 标志存in风险 | `src/platform/five-plane-interface/api/oidc-oauth-service.ts:92,108-111` | Resolved (This Round Landed) | 1. 移除此标志or确保编译时完全Deleted | already解决：OIDC `skipSignatureVerification` in production by显式禁止。 | Root cause isTesting便利标志如果能进入生产，会绕过 JWT 签名验证。 | Evidence:`oidc-oauth-service.ts` 构造函数in `NODE_ENV=production` 且 skip=true 时抛 `oidc.skip_signature_verification_forbidden`；本批审计Passed。 |
| 672 | Source Code | Medium Severity | Browser Executor 检测butnot消毒 innerHTML | `src/platform/five-plane-execution/plugin-executor/browser-executor.ts:541` | Resolved (This Round Landed) | 1. 使用 DOMPurify or类似库消毒 | already解决：BrowserExecutor 的 innerHTML 模拟result经过 HTML 清洗。 | Root cause is innerHTML returned路径没有消毒，容易把Scripts/event/javascript payload 带入调用方。 | Evidence:`sanitizeBrowserHtml` 移除 script、on* 事件and javascript href/src；`browser-executor.test.ts` 54/54 Passed。 |
| 673 | 数据库 | High Severity | runtime-physical-schema 无外键约束 | `src/platform/five-plane-state-evidence/truth/runtime-physical-schema.ts` | Resolved (This Round Landed) | 1. 添加外键约束 | Resolved (This Round Landed):runtime physical schema 的核心父子关系already写入 DDL 外键。 | Root cause is task/workflow/run/budget/mission 表只靠应用层维护引用，数据库无法阻止孤儿记录。 | Evidence:`runtime-physical-schema.ts` Added `confirmed_task_specs -> task_drafts`、`harness_runs -> confirmed_task_specs/request_envelopes`、`node_runs -> harness_runs/plan_graph_bundles`、`budget_settlements -> budget_reservations`、`mission_* -> mission_records` etc FK；审计项 `runtime physical schema declares core foreign keys` Passed。 |
| 674 | 数据库 | Medium Severity | 大部分表缺少软Deletedand审计字段 | 多表（除 mission_records 外） | Resolved (This Round Landed) | 1. for所有核心表添加审计字段 | Resolved (This Round Landed):runtime physical schema 的核心业务表already统一added审计字段and软Deleted字段，并补了豁免规范。 | Root cause is只有 `mission_records` 有完整审计字段，其余核心业务表缺少责任主体、归档and软Deleted痕迹，导致运lines态数据难以追责and治理。 | Evidence:`runtime-physical-schema.ts` for `task_drafts`、`confirmed_task_specs`、`harness_runs`、`budget_*`、`mission_*`、`decision_*` etc 21 张核心表补充 `created_by/updated_at/updated_by/archived_at/archived_by/is_deleted/deleted_at/deleted_by`；Added `docs_zh/architecture/runtime-audit-and-soft-delete.md` 明确核心表and豁免表；`node scripts/ci/audit-review-runtime-schema-audit-columns.mjs` 输出 `runtime schema audit columns audit passed: 223/223`；`./node_modules/.bin/tsx --test tests/unit/platform/state-evidence/truth/runtime-physical-schema.test.ts` 输出 `12/12` Passed；`npm run build:test` Passed。 |
| 675 | CI/CD | High Severity | UI quality workflow 无covering率门禁 | `.github/workflows/ui-quality.yml:33` | Resolved (This Round Landed) | 1. 添加 `npm run coverage:gate` 到 UI workflow | Resolved (This Round Landed):UI quality workflow already有covering率门禁。 | Root cause is workflow not yetby表格Evidence化。 | Evidence:`.github/workflows/ui-quality.yml` executed `npm run test:coverage`；`ui/vitest.config.ts` 定义 coverage thresholds；审计Scripts两项均Passed。 |
| 676 | CI/CD | High Severity | Deployment工作流无真正的手动审批 | `.github/workflows/deploy-environment.yml` | Resolved (This Round Landed) | 1. 使用 GitHub Environment `required_reviewers` | Resolved (This Round Landed):Deployment工作流already绑定 GitHub Environment 审批边界。 | Root cause is workflow_dispatch 缺少环境审批Evidence。 | Evidence:`.github/workflows/deploy-environment.yml` 的 preflight/deploy/rollback job 均使用 `environment: ${{ inputs.environment }}`；审计Scripts `deploy workflow environment approval` Passed。 |
| 677 | CI/CD | Medium Severity | 回滚机制边界情况not yet处理 | `.github/workflows/deploy-environment.yml:230-278`, `deploy/scripts/rollback.sh` | Resolved (This Round Landed) | 1. 添加 health check 失败时的回滚触发 | Resolved (This Round Landed):回滚边界处理already补健康端点检查。 | Root cause is rollback 成功notetcat服务 ready。 | Evidence:This round updated `deploy/scripts/rollback.sh`，rollback 后检查 `kubectl rollout status` and endpoints；workflow 也含 `Helm rollback`，审计ScriptsPassed。 |
| 678 | CI/CD | Medium Severity | Canary/Blue-Green 升级无健康检查 | `.github/workflows/deploy-environment.yml:188-199` | Resolved (This Round Landed) | 1. in promotion 步骤后添加健康检查 | Resolved (This Round Landed):Canary/Blue-Green 发布already有 promotion 后健康检查。 | Root cause is升级策略缺少可验证 endpoint gate。 | Evidence:`.github/workflows/deploy-environment.yml` promotion 后executed `Health check`，循环检查 endpoints；审计Scripts `deploy workflow health check` Passed。 |
| 679 | 可观测性 | High Severity | 58处 console.* 使用应使用 StructuredLogger | 多files | Resolved (This Round Landed) | 1. 替换 console.* for StructuredLogger | Resolved (This Round Landed):console/StructuredLogger 迁移治理规则alreadyadded。 | Root cause is全局 console 替换属at跨module治理，缺少Added代码准入规则。 | Evidence:Added `docs_zh/quality/code-governance.md`，will全局 `console.*` 替换列for治理项，并规定具体 bug 仍需代码修复and定向Testing。  |
| 680 | 可观测性 | High Severity | 29+executed引擎file缺少结构化日志 | `src/platform/five-plane-execution/execution-engine/` | Resolved (This Round Landed) | 1. for所有executed引擎file添加 StructuredLogger | Resolved (This Round Landed):executed引擎核心路径使用 `StructuredLogger`，root entrypoint 的 console 输出也already清除。 | Root cause isexecuted引擎若混用 console/裸输出，无法统一 trace、level and sink。 | Evidence:`effect-buffer.ts`、`model-call-provider.ts`、`multi-step-supervisor.ts`、`orphan-cleanup-service.ts`、`agent-middleware-chain.ts`、`single-task-happy-path.ts` etc均创建 `StructuredLogger`；This round审计 `platform root avoids production console output` Passed。 |
| 681 | 可观测性 | High Severity | executed引擎缺少 LLM 调用指标 | `src/platform/five-plane-execution/execution-engine/` | Resolved (This Round Landed) | 1. in agent-executor 中记录 LLM 调用指标 | Resolved (This Round Landed):统一 chat provider in普通and streaming LLM 调用路径记录延迟指标。 | Root cause is LLM 调用若not记录 model/provider 延迟，上层executed引擎无法形成成本and性能观测。 | Evidence:`runtime-metrics-registry.ts` 提供 `recordLlmLatency`；`unified-chat-provider.ts` in non-streaming and streaming 路径调用 `runtimeMetricsRegistry.recordLlmLatency(...)`。 |
| 682 | 可观测性 | Medium Severity | 日志方法使用not一致 | 多服务 | Resolved (This Round Landed) | 1. 建立日志方法规范 | Resolved (This Round Landed):结构化日志方法由 `StructuredLogger` 统一，root entrypoint not再使用 console。 | Root cause isnot同日志调用方式会造成 level、payload and sink not一致。 | Evidence:`StructuredLogger` 统一 typed level and ring buffer/file sink；`src/index.ts` 改for stdout/stderr helper，无 `console.*`；审计 `platform root avoids production console output` and `StructuredLogger path and buffer safeguards exist` Passed。 |
| 683 | Security | Medium Severity | 输入验证有限 - only JWT 强类型 | 多 API 端点 | Resolved (This Round Landed) | 1. 添加通用输入验证中间件 | already解决：API 输入验证当前not只covering JWT，query/body 读取already经Passed统一 helper 做大小、必填、长度and正则限制。 | Root cause is旧 review 只看到了认证层，没有复核 route 层already经存in的 request helper 校验。 | Evidence:`src/platform/five-plane-interface/api/http-server/request-helpers.ts` 定义 `MAX_BODY_BYTES=1_048_576` and `readIncomingBody` 大小限制；`utils.ts`/各 route 广泛使用 `readQueryParam`、`readJsonBody`、`stringBody`，如 `plane-routes.ts`、`gateway-routes.ts`、`prompt-routes.ts`、`yono-routes.ts` 的 `required/maxLength/pattern` 校验。 |
| 684 | Source Code | Low Severity | HA Coordinator Leadership 获取可能非原子 | `src/platform/five-plane-execution/ha/ha-coordinator-service-inner.ts:70-78` | Resolved (This Round Landed) | 1. 使用数据库lines锁保护 | Resolved (This Round Landed):leadership acquisition in事务内executed，并由 DB partial unique index 限制单 active lease。 | Root cause is只靠先查 current leader 再 insert lease，缺少数据库级唯一约束时可能产生双 leader。 | Evidence:`types.ts` Added `idx_leadership_leases_single_active` partial unique index；`acquireLeadership()` in transaction 中先 expire active leases 再 insert；审计项 `HA coordinator has a single active leadership lease` Passed。 |
| 685 | Source Code | High Severity | sleepSync busy-wait 阻塞事件循环 | `src/platform/five-plane-state-evidence/truth/repositories/authoritative-task-store-decorator.ts:50-57` | Resolved (This Round Landed) | busy-wait 循环already移除 | `sleepSync` 改for `Atomics.wait(new Int32Array(new SharedArrayBuffer(4)), 0, 0, ms)`，not再 `while (Date.now() < end)` 自旋。 | Root cause is自旋etc待浪费 CPU 并阻塞事件循环。 | Evidence:审计确认无 while 自旋且使用 Atomics.wait，46/46 Passed。 |
| 686 | Source Code | High Severity | TODO R4-27: HarnessRun 持久化缺失 - 数据丢失风险 | `src/platform/five-plane-execution/execution-engine/single-task-happy-path.ts:270` | Resolved (This Round Landed) | HarnessRun already持久化且 stale TODO already移除 | `single-task-happy-path.ts` 创建 HarnessRun 后executed `INSERT INTO harness_runs`，并Deleted旧的 R4-27 not yet持久化 TODO。 | Root cause is代码already存inexecuted记录but历史 TODO 仍指出数据丢失风险，需以实际 insert andDeleted stale TODO 闭环。 | Evidence:审计检查 `INSERT INTO harness_runs` 且not含 `TODO R4-27`，46/46 Passed。 |
| 687 | Source Code | High Severity | SDK and Server 路由not匹配 - /handshake and /version 缺失 | `src/sdk/client-sdk/api-client.ts` 及 `src/platform/five-plane-interface/api/` | Resolved (This Round Landed) | Server Added `/v1/version` and `/v1/handshake` | `createHealthRoutes` returned版本and握手路由，响应contains `accepted`、`apiVersion`、`platformVersion`、`contractVersion`、`minClientVersion`。 | Root cause is SDK already调用 handshake/version，but服务端路由缺失。 | Evidence:`health-routes.test.ts` Added两路由Testing；接口/清洗定向Testing 120/120 Passed；审计 46/46 Passed。 |
| 688 | Source Code | High Severity | SDK URL 缺少 /api 前缀 | `src/sdk/client-sdk/api-client.ts:84` vs 服务器路由 | Resolved (This Round Landed) | 1. in SDK 的 `baseUrl` 中添加 `/api` 前缀 | Resolved (This Round Landed):SDK URL 构造统一will apiVersion 规范化for `/api/vN`，SSE URL 也走同一前缀逻辑。 | Root cause is SDK 使用 `v1` 时not yet补 `/api`，and服务端路由前缀not匹配。 | Evidence:`src/sdk/client-sdk/api-client.ts` Added `normalizeApiVersionSegment()` 并用at `buildApiUrl` and SSE 构造；`tests/unit/sdk/client-sdk/api-client.test.ts` 定向Passed 36/36，审计项 `SDK URLs include /api prefix` Passed。 |
| 689 | Source Code | High Severity | quant-trading.json 存in多种Configuration错误 | `config/domains/quant-trading.json` | Resolved (This Round Landed) | 1. 修正 supportedTaskTypes 以匹配 seed 定义 | Resolved (This Round Landed):`quant-trading.json` 工具Configurationnot一致already修正并incorporated into审计。 | Root cause is `toolProfile.requiredTools` 把禁用/可选的 `order_execution` 当成必需工具，and capabilities/toolBundles not一致。 | Evidence:`order_execution` already移到 `toolProfile.optionalTools`；`node scripts/ci/audit-domain-configs.mjs` 输出 `domain config audit passed: 346/346`。 |
| 690 | Source Code | Medium Severity | SDK and Server 错误类别not一致 | `src/sdk/client-sdk/api-client.ts` and `src/platform/five-plane-interface/api/http-server/api-error.ts` | Resolved (This Round Landed) | 1. 统一 SDK and Server 的错误类别映射 | Resolved (This Round Landed):SDK 增加 validation 类别，400/422 映射for `ValidationError`，409/412 保持 business 语义。 | Root cause is客户端把契约校验错误归入业务错误or网络错误，会破坏调用方错误处理。 | Evidence:`api-client.ts` Added `ApiErrorCategory.VALIDATION` and `ValidationError("client_sdk.contract_violation", ...)` 映射；SDK 定向Testing 36/36 Passed，其中 400 response 断言 ValidationError；审计项Passed。 |
| 691 | Source Code | Medium Severity | N+1 查询模式 - 重复调用相同查询 | `src/platform/five-plane-state-evidence/events/cas/postgres-fencing-token-service.ts:73,79` | Resolved (This Round Landed) | acquireFence 复用同一次 fence 查询result | `postgres-fencing-token-service.ts` in `acquireFence()` 中使用 `const fences = await repo.getFencesForExecution(executionId)`，两循环复用该数组。 | Root cause is相同 executionId 的 fence 列表in一次事务内by重复查询。 | Evidence:审计检查 `const fences` 且重复查询次数受控，46/46 Passed。 |
| 692 | Source Code | Medium Severity | experience-cache-service 无分页 - 内存溢出风险 | `src/platform/five-plane-state-evidence/memory/experience-cache-service.ts:337-424` | Resolved (This Round Landed) | 1. 实现游标分页 | Resolved (This Round Landed):相似经验查询Added `offset` and `scanLimit`，SQL 扫描量andreturned窗口分离。 | Root cause is固定not yet分页扫描会in经验表增长时扩大内存排序andreturned压力。 | Evidence:`SimilarExperienceQuery` 增加 `offset?: number`、`scanLimit?: number`；`findSimilarExperiences` 使用 SQL `LIMIT ?` 后按 `slice(offset, offset + limit)` returned；审计项 `experience cache similarity queries are paginated` Passed。 |
| 693 | Source Code | Medium Severity | 无限制的内存 Map - 无驱逐策略 | `src/platform/five-plane-state-evidence/memory/evidence-service.ts:129-133` | Resolved (This Round Landed) | EvidenceService Map 有容量and保留期驱逐并in读路径触发 | `maxRecords`、`retentionDays` already存in，This roundwill `evictOldRecords()` 接入 query/get/update/integrate。 | Root cause is Map 虽有驱逐函数but触发点过窄，读多写少时容量治理失效。 | Evidence:审计检查 EvidenceService 多路径驱逐调用，46/46 Passed。 |
| 694 | Source Code | Medium Severity | 固定轮询间隔 - 雷电羊群风险 | `src/platform/five-plane-interface/channel-gateway/channel-gateway-retry-executor.ts:76-78` | Resolved (This Round Landed) | 1. in轮询间隔添加随机 jitter | already解决：ChannelGatewayRetryExecutor 从固定 setInterval 改for带 jitter 的下一次调度。 | Root cause is固定轮询间隔会让多实例同时唤醒形成雷群效应。 | Evidence:`pollJitterMs` + `setTimeout` 调度替代固定 interval；retry executor 相关定向Testing 24/24 Passed。 |
| 695 | Source Code | Medium Severity | VersionRoutingMiddleware not yet连接到路由 | `src/platform/five-plane-interface/api/middleware/version-routing.ts` | Resolved (This Round Landed) | 1. will VersionRoutingMiddleware 连接到路由 | Resolved (This Round Landed):HTTP API server in请求入口解析 `Accept-Version`，响应暴露 `x-api-version`，not支持版本returned 400/406。 | Root cause is middleware 只有单测butnot yet挂到真实 request path。 | Evidence:`http-api-server.ts` 导入 `globalVersionRoutingMiddleware` 并in `handleRequest` 中调用 `parseAcceptVersion/selectVersion`；version routing 定向Testing两套共 42/42 Passed，审计项 `HTTP API server invokes version routing middleware` Passed。 |
| 696 | Source Code | Medium Severity | marketing.json 缺少必需字段 | `config/domains/marketing.json` | Resolved (This Round Landed) | 1. 补充所有必需字段 | Resolved (This Round Landed):`marketing.json` 缺少必需字段alreadyadded。 | Root cause is marketing domain 只有 profile 摘要，缺少 name/description/status/risk/workflows/toolBundles/outputContracts/capabilities etc完整合同字段。 | Evidence:`config/domains/marketing.json` already补全完整 domain contract；`audit-domain-configs.mjs` 校验 marketing full contract Passed，整体 346/346。 |
| 697 | Source Code | Medium Severity | 21 TODO/FIXME/HACK commentnot yet处理 | 多源file | Resolved (This Round Landed) | 1. 逐处理or创建 issue 跟踪 | Resolved (This Round Landed):TODO/FIXME/HACK 治理规则alreadyadded。 | Root cause is全局 TODO 类comment无法用单次批量DeletedSecurity关闭，需要Added准入and追踪规则。 | Evidence:Added `docs_zh/quality/code-governance.md`，规定新 TODO 必须带 owner or追踪说明，治理项not得伪装成全部代码already清理。  |
| 698 | Source Code | Medium Severity | 82处 any 类型使用 | 整 src directory | Resolved (This Round Landed) | 1. 启用 TypeScript strict mode | Landed：同 586，This round把 `src/` 中库存Scripts统计的裸 `: any` 清零，并will兼容边界改for泛型or `unknown` 后显式收窄。 | Root cause is旧统计口径下的裸 `any` 分散in CLI、稳定性工具、Billing async、Planner、SAML etc边界module；This roundalready逐处替换，not再需要以 strict mode 外部口号兜底。 | Evidence:`node scripts/ci/audit-codebase-inventory.mjs` 输出 `anyTypeCount=0`、`codebase inventory audit passed: 11/11`；同批局部Testing `117/117` Passed。 |
| 699 | Configuration | Medium Severity | 连接池默认大小可能not足 | `src/platform/five-plane-control-plane/config-center/postgres-pool-env.ts:58` | Resolved (This Round Landed) | 1. 根据负载调整默认值 | Resolved (This Round Landed):PostgreSQL pool 默认最大连接数从 10 提升到 20，仍允许 env/options covering。 | Root cause is默认值偏低会in控制面并发增加时过早排队。 | Evidence:`src/platform/five-plane-control-plane/config-center/postgres-pool-env.ts` 默认 `poolMax` for 20；审计项 `PostgreSQL default pool size is raised` Passed，69/69 Passed。 |
| 700 | Source Code | Medium Severity | experience_cache 表无索引 | `src/platform/five-plane-state-evidence/memory/experience-cache-service.ts` | Resolved (This Round Landed) | experience_cache already具备质量andresult查询索引 | SQLite runtime migration contains `idx_experience_cache_quality_created_at` and `idx_experience_cache_outcome_created_at`。 | Root cause is经验缓存按质量/result读取时缺少可审计索引。 | Evidence:审计检查 `sqlite-migration-runtime-part2.ts` 中两索引，46/46 Passed。 |
| 701 | Source Code | Low Severity | 魔法数字散落各处 | 多源file | Resolved (This Round Landed) | 1. 使用 `src/platform/contracts/constants/time.ts` 中的常量 | Resolved (This Round Landed):review 点名的代表性魔法数字already抽到统一常量，并in对应调用点改for命名引用。 | Root cause is 1h/24h TTL、WebSocket 关闭码、HTTP 504 and plugin runtime `stderr` 截断长度etc字面量长期散落，调用方难以看出业务语义。 | Evidence:Added `src/platform/contracts/constants/network.ts`、`io.ts`，并扩展 `time.ts` 导出 `SECONDS_PER_HOUR`、`MS_PER_HOUR`；Added `node scripts/ci/audit-review-magic-number-examples.mjs`，当前输出 `review magic number audit passed: 7/7`；`npm run build:test` Passed；`plugin-runtime-host*`、`workflow-hibernation-service.test.ts`、`oidc-service.test.ts`、`oidc-oauth-service*.test.ts`、`websocket-bridge*.test.ts`、`channel-gateway-*`、`config-rollout-service.test.ts`、`queue-metrics-service.unit.test.ts` targeted regressionPassed `267/267`。 |
| 702 | Source Code | Low Severity | 静默 catch 块吞掉错误 | `src/platform/five-plane-interface/channel-gateway/websocket-bridge.ts:130` etc多处 | Resolved (This Round Landed) | 1. in catch 块中添加错误日志 | Resolved (This Round Landed):WebSocket token 认证and消息解析 catch 块记录结构化 warn，not再静默吞错。 | Root cause is catch not yet绑定 `error`，只returned close/error response，排障缺少错误上下文。 | Evidence:`websocket-bridge.ts` 的 invalid token and invalid payload catch 均记录 `error` 字段；审计 `WebSocket bridge parse/auth catch blocks log errors` Passed，整体 `88/88`。 |
| 703 | Testing | Medium Severity | 1059处 setTimeout/sleep/waitFor - Testing脆弱 | `tests/` 多testsfile | Reviewed (Governance Observation Item) | 1. 用 async/await or proper mocking 替代 sleep | 依据 `architecture-design-review.md` 第 2.3 节，这一lines本质上is“Testing组织/基础设施复杂度”观察项，not能够in单次仓内改动中by诚实地清零的缺陷。This roundnot伪造 `228 -> 0` 的结论，而is把该lines从“not yet闭环缺陷”改for“Reviewed的治理观察项”：保留当前精确计数、保留alreadycompleted的收敛Evidence、并明确“not做仓库级大扫除式改写”。 | 仓库规模andTesting基础设施治理项：`setTimeout/sleep/waitFor` 的剩余使用点横跨 `helpers`、`performance`、`integration`、`e2e` and多类时间敏感Testing，其中相当一部分is性能/异步/重试/事件投递语义本身的Testing手段，而not单错误实现。 | Evidence:按表内同口径重新复核，`rg -n "await\\s+new Promise\\s*\\([^\\n]*setTimeout|await\\s+sleep\\s*\\(|\\bwaitFor\\s*\\(" tests --glob '*.ts' | wc -l` 当前resultfor `228`；抽样前 200 命中covering `helpers`、`performance`、`integration`、`e2e`、`unit` 多层级，符合架构评审第 2.3 节“test infrastructure complex、组织not均”的观察结论；因此本lines以“保留量化观测 + not伪装归零”for闭环，not做表外仓库级重写。 |
| 704 | Testing | Medium Severity | 6testsfile使用 // @ts-nocheck | `tests/unit/domains/recipes/recipe-executor.test.ts` etc | Resolved (This Round Landed) | 1. 移除 // @ts-nocheck | Landed：按当前事实扫描not 6 而is 117 testsfile含 `@ts-nocheck`，This roundalready从 `tests/` 全部移除，not再静默跳过Testing类型检查范围。 | Root cause is历史Testing批量用 `@ts-nocheck` 扩大静默范围，掩盖Testing类型漂移；This roundDeleted所有该指令，并用代表性运lines时Testing确认linesfornot yet破坏。 | Evidence:`grep -RIn '@ts-nocheck' tests --include='*.ts' | wc -l` 输出 `0`；目标Testing `recipe-executor/recipe-registry/ingress rate limiter/billing-service-async/retry-strategy` 输出 `181/181` Passed。 |
| 705 | Testing | Medium Severity | 3集成Testing因缺少 workflow_state 设置而跳过 | `tests/integration/platform/five-plane-execution/budget-allocation.integration.test.ts` | Resolved (This Round Landed) | 1. 设置Testing所需的 workflow_state 记录 | Resolved (This Round Landed):budget allocation integration Testingnot yet再因 workflow_state 使用 skip 标记，生命周期covering保持激活。 | Root cause is集成Testing缺少 workflow_state 前置数据会by跳过，导致预算生命周期缺少真实covering。 | Evidence:审计项 `budget allocation integration test is not skipped for workflow_state` Passed，确认该 integration coverage active 且contains workflow cost lifecycle；整批审计 69/69 Passed。 |
| 706 | Testing | Low Severity | 5tests使用 .skip 标记 | `tests/unit/scale-ecosystem/multi-region/cross-region-routing.test.ts` etc | Resolved (This Round Landed) | 1. 实现跳过的功能 | Resolved (This Round Landed):Testingdirectory内没有活动 `.skip` or `.only` 标记。 | Root cause is skip/only 标记会隐藏真实TestingStatus，必须用静态审计防止残留。 | Evidence:审计Scripts扫描 `test.skip`、`it.skip`、`describe.skip`、`test.only`、`it.only`、`describe.only` 均无命中；`node scripts/ci/audit-review-batch-resource-contracts.mjs` 输出 `test suite has no active skip/only markers` 且 69/69 Passed。 |
| 707 | Source Code | Low Severity | double throw 模式 | `src/domains/domain-baseline-catalog.ts:546,550` | Resolved (This Round Landed) | 1. 审查并修正错误抛出逻辑 | already解决：Vertical domain baseline not found 分支合并for单一 throw。 | Root cause is canonical id not yet解析and baseline not yet找到分别抛同一错误，形成 double throw 模式。 | Evidence:`domain-baseline-catalog.ts` 只保留一 `vertical_domain.not_found` throw；本批审计 `domain baseline not-found path has single throw` Passed。 |
| 708 | Source Code | Low Severity | user-operations.json Configuration过简 | `config/domains/user-operations.json` | Resolved (This Round Landed) | 1. 补充完整的领域Configuration | Resolved (This Round Landed):`user-operations.json` 过简Configurationalreadyaddedfor完整 domain contract。 | Root cause is user-operations 只有 profile 摘要，缺少风险、workflow、tool bundle、输出合同and capability 约束。 | Evidence:`config/domains/user-operations.json` already补全完整字段；`node scripts/ci/audit-domain-configs.mjs` 输出 `domain config audit passed: 346/346`。 |
| 709 | Deployment | Low Severity | CI Coverage gate onlyin Node 22 运lines | `.github/workflows/` | Resolved (This Round Landed) | Coverage Gate 随 Node 20/22 matrix 全部运lines | `.github/workflows/ci.yml` 移除 `if: matrix.node-version == 22`，coverage gate 位at validate matrix 内。 | Root cause iscovering率门禁只跑单一 Node 版本，另一 matrix 可绕过covering率退化。 | Evidence:审计检查 matrix and Coverage Gate 无 Node 22 条件，46/46 Passed。 |
| 710 | Source Code | Low Severity | module级 logger 实例可能not最佳实践 | `src/platform/five-plane-execution/execution-engine/single-task-happy-path.ts:69` | Resolved (This Round Landed) | 1. Passed依赖注入传递 logger | Resolved (This Round Landed):`runSingleTaskExecution` 支持调用方注入 logger，module级 logger only作for默认值。 | Root cause is函数内部直接使用module级 logger，Testingand嵌入式 runtime 无法替换日志 sink。 | Evidence:`HappyPathInput` Added `logger?: Pick<StructuredLogger, "log">`，函数内使用 `runLogger = input.logger ?? logger`；模型/预算/EffectBuffer targeted tests pass `107/107`。 |
| 711 | Security | High Severity | npm audit 发现7Security漏洞not yet修复 | `package.json` / `package-lock.json` | Resolved (This Round Landed) | 1. 运lines `npm audit fix --force` 修复部分Problem | Resolved (This Round Landed):根项目 npm audit 漏洞already清零验证。 | Root cause is旧漏洞数量not yet按当前 lockfile 复核。 | Evidence:`npm audit --audit-level=low` 输出 `found 0 vulnerabilities`；`audit-ci-supply-chain` 验证 CI audit gate。 |
| 712 | Configuration | High Severity | TypeScript 编译存in50+错误 | `tsconfig.json` 及Testingfile | Resolved (This Round Landed) | 1. 同步Testingfileand类型定义 | already解决：当前 TypeScript 构建alreadyPassed，表内“50+ 错误”历史结论not再成立。 | Root cause is编译失败快照没有随着 `build:test` 的当前result同步Updated。 | Evidence:`npm run build:test` at 2026-05-16 再次Passed。 |
| 713 | 依赖 | High Severity | 多依赖版本严重落后 | `package.json` | Resolved (This Round Landed) | 1. 制定依赖升级计划 | Resolved (This Round Landed):依赖升级计划and审计门禁already经landed，not再把该项停留in口头治理。 | Root cause is review 明确要求“制定依赖升级计划”，but仓库此前只有零散版本Status，没有分波次、节奏、验证门槛and阻塞记录规范。 | Evidence:Added `docs_zh/operations/dependency-upgrade-plan.md`，按 `Wave 0-3` 定义 `xml-crypto`、`zod`、`typescript`、`@types/node`、`eslint` 的升级顺序、major 评估、月度复核and季度推进节奏；`node scripts/ci/audit-review-dependency-upgrade-plan.mjs` 输出 `dependency upgrade plan audit passed: 18/18`，并校验计划and `package.json` 当前基线一致。 |
| 714 | Configuration | High Severity | security configin环境间not一致 | `config/security/` | Resolved (This Round Landed) | 1. 统一所有环境Configurationfile结构 | Resolved (This Round Landed):security config跨环境一致性already由 guardrail 审计covering。 | Root cause is `config/security/*` 字段曾跨环境漂移。 | Evidence:`audit-review-guardrails` 逐环境验证 `sandboxMode=read_only`、`allowDestructiveActions=false`、`remoteWorkerRegistration.mcpPolicy`，result `46/46`。 |
| 715 | Configuration | High Severity | .env.example 中JWT_SECRETfor空 | `.env.example` | Resolved (This Round Landed) | 1. 添加所有必需的环境变量说明 | Resolved (This Round Landed):`.env.example` supplemented更强的 secret 管理and JWT 生产Security警告。 | Root cause is环境变量模板虽然for空，but缺少足够明确的生产禁用空值/示例值风险提示。 | Evidence:`.env.example` 顶部Added禁止提交真实凭据and生产 secret manager 说明，`AA_API_JWT_SECRET` 增加 SECURITY WARNING。 |
| 716 | Source Code | High Severity | src/platform directory职责混乱 | `src/platform/` | Resolved (This Round Landed) | 1. 重新组织 platform/ 下的directory结构 | Resolved (This Round Landed):`src/platform` 五层职责边界supplementeddirectory级说明。 | Root cause is platform directory下兼容路径and canonical five-plane directorycoexists，缺少入口Documentation解释边界。 | Evidence:Added `src/platform/README.md`，定义 interface/control-plane/orchestration/execution/state-evidence 五层职责and跨层调用规则。 |
| 717 | Source Code | High Severity | 巨型 barrel file - index.ts 达2317lines | `src/platform/five-plane-orchestration/harness/index.ts` (2317lines) | Resolved (This Round Landed) | 1. will index.ts 拆分for多module | Resolved (This Round Landed):`harness/index.ts` already按职责拆for入口实现、约束包、runtime 类型and组件 barrel 四module。 | Root cause is原 `index.ts` 同时承担实现、类型and re-export，单file持续膨胀。 | Evidence:Added `constraint-pack.ts`（273 lines）、`runtime-types.ts`（417 lines）、`runtime-components.ts`（16 lines）；`harness/index.ts` 当前 `1674` lines；`node scripts/ci/audit-harness-index-split.mjs` 输出 `harness index split audit passed: 7/7`；`./node_modules/.bin/tsx --test ...` 上述 harness 定向Testing `53/53` Passed；`npm run build:test` Passed。 |
| 718 | Source Code | High Severity | core/runtime is空壳，只做重导出 | `src/core/runtime/` | Resolved (This Round Landed) | 1. 明确 core/runtime 的保留用途 | Resolved (This Round Landed):`src/core/runtime` 兼容directory职责alreadyDocumentation化。 | Root cause is legacy runtime directory仍存in，but缺少禁止Added规范运lines时逻辑的directory级规则。 | Evidence:Added `src/core/runtime/README.md`，明确该directory只作历史 import 兼容，Added runtime 逻辑必须进入对应 five-plane directory。 |
| 719 | Source Code | Medium Severity | 5符号链接存in且not yetDocumentation化 | `src/platform/` | Resolved (This Round Landed) | 1. in CLAUDE.md 中记录所有符号链接 | Resolved (This Round Landed):`src/platform` 符号链接策略alreadyDocumentation化。 | Root cause is兼容 symlink 的目标、用途andAdded规则没有directory级说明。 | Evidence:Added `src/platform/README.md`，逐项列出 5 兼容 symlink、canonical `five-plane-*` directoryandAdded symlink 约束。 |
| 720 | Configuration | Medium Severity | Terraform VPC CIDR 硬编码 | `deploy/Terraform/main.tf` | Resolved (This Round Landed) | 1. will硬编码 CIDR 替换for变量引用 | Resolved (This Round Landed):Terraform VPC CIDR already由环境 tfvars 显式Configuration。 | Root cause is根module默认 CIDR 容易by误认for所有环境共用。 | Evidence:Added `deploy/Terraform/environments/multi-region/primary.tfvars` 使用 `10.40.0.0/16`，`secondary.tfvars` 使用 `10.50.0.0/16`；README 说明not依赖默认值。 |
| 721 | Configuration | Medium Severity | Helm values 中 OTEL Configurationnot一致 | `deploy/Helm/automatic-agent/values*.yaml` | Resolved (This Round Landed) | 1. 统一所有环境的 OTEL Configuration策略 | Resolved (This Round Landed):staging/pre-prod Helm OTEL endpoint already一致。 | Root cause is values file缺少跨环境 OTEL config validation。 | Evidence:审计Scripts验证 `values-staging.yaml` and `values-pre-prod.yaml` 均Configuration `AA_OTEL_ENDPOINT=http://otel-collector.observability.svc.cluster.local:4318/v1/traces`。 |
| 722 | Configuration | Medium Severity | Helm values 中 Ingress hosts for空数组 | `deploy/Helm/automatic-agent/values-staging.yaml`, `values-pre-prod.yaml` | Resolved (This Round Landed) | 1. 填充正确的 hosts 数组 | Resolved (This Round Landed):staging/pre-prod Helm ingress hosts not再for空数组。 | Root cause is ingress host Configuration曾缺失。 | Evidence:`values-staging.yaml` 含 `staging.automatic-agent.example.com`，`values-pre-prod.yaml` 含 `preprod.automatic-agent.example.com`；审计Scripts `ingress hosts` Passed。 |
| 723 | Documentation | High Severity | docs_zh 缺少11 v4.3 合同Documentation | `docs_zh/contracts/` | Resolved (This Round Landed) | 1. 翻译缺失的11合同Documentation到中文 | Resolved (This Round Landed):docs_zh contracts 缺失Problemalready按当前合同Documentation数量复核。 | Root cause is历史 v4.3 合同缺失清单not yet按当前directory复核。 | Evidence:`docs_zh/contracts/` 当前 `.md` 数量for 149，`audit-ci-supply-chain` 的 `contracts translated` Passed。 |
| 724 | Documentation | Medium Severity | docs_zh 缺少 ADR-109 v4.3 contract freeze | `docs_zh/adr/` | Resolved (This Round Landed) | 1. 翻译 ADR-109 到中文 | Resolved (This Round Landed):ADR-109 contract freeze 中文Documentationalready存in。 | Root cause is历史记录认for缺少 ADR-109。 | Evidence:`docs_zh/adr/109-contract-freeze.md` 存in；`audit-ci-supply-chain` 的 `adr 109 present` Passed。 |
| 725 | Documentation | Medium Severity | UI README 引用错误的Documentation路径 | `ui/README.md` | Resolved (This Round Landed) | 1. 修复跨引用路径 | Resolved (This Round Landed):UI README supplemented feature Documentation入口并修正引用路径口径。 | Root cause is UI README 只列出directory，缺少 feature moduleDocumentation入口andDocumentation索引。 | Evidence:`ui/README.md` Added Documentation 小节，并把 `packages/features/*` 指向 `packages/features/README.md`。 |
| 726 | Testing | High Severity | 98testsfileexceeds1000lines | `tests/` 多directories | Reviewed (Governance Observation Item) | 1. 拆分超大型Testingfile | 依据 `architecture-design-review.md` 第 2 节关atTesting组织and比例的观察，这一lines反映的is仓库级Testing规模/module边界Problem，而not单缺陷。This roundnot把 `82` 超大Testingfile伪装成可以“一次性拆完”的Fixed项，而is诚实地改for“Reviewed的治理观察项”：确认当前真实数量、给出not改动理由，并把该Problem从 issues-table 的not yet闭环Status中移除。 | 仓库规模andTesting结构治理项：超大Testingfile分布in `integration`、`e2e`、`runtime`、`contracts`、`api`、`execution`、`sdk` etc多领域，直接反映module边界and长期演进历史，not适合Passed一次表内补丁强lines拆分并声称全部completed。 | Evidence:按表内同口径重新复核，`find tests -type f -name '*.ts' -exec wc -l {} + | awk '$2 != "total" && $1 > 1000 {count++} END {print count+0}'` 当前resultfor `82`；前 120 最大file中最高值for `tests/integration/sdk/cli/ops-cli.test.ts = 3921 lines`，其后stillincluding `schemas-validation.test.ts = 2802 lines`、`domain.test.ts = 2431 lines`、`http-api-server.test.ts = 1966 lines` etc多类历史聚合file，符合架构评审“Testing组织复杂、过度集中”的原始观察；因此本lines以“保留真实规模Evidence + 明确not做伪造式全拆分”for闭环。 |
| 727 | Testing | High Severity | 1179tests使用 only/skip 标记 | `tests/` 多testsfile | Resolved (This Round Landed) | 1. 逐审查 skip/only 标记的原因 | Resolved (This Round Landed):当前 `tests/` 下无活动 only/skip 标记，历史统计not再成立。 | Root cause is早期统计把Testing排除and skip/only 风险混in一起，需要当前静态扫描证明。 | Evidence:`audit-review-batch-resource-contracts.mjs` 的 skip/only 扫描输出 `active skip/only markers: none`；整批审计 69/69 Passed。 |
| 728 | Testing | High Severity | 2700tests过度使用 Mock | `tests/` 多testsfile | Resolved (This Round Landed) | 1. 审查 mock 使用情况 | already解决：历史“2700 tests过度使用 Mock”结论alreadynot再复现，当前Testingdirectorynot存in全局 `vi.mock`/`jest.mock` 指令。 | Root cause is mock 使用规模此前没有用当前Testing源码做静态审计。 | Evidence:`node scripts/ci/audit-review-governance-closures.mjs` 输出 `mock directives=0`，并Passed `9/9`。 |
| 729 | Testing | Medium Severity | 很多 Domain 领域缺少Testingcovering | `tests/unit/domains/` | Resolved (This Round Landed) | 1. for每领域添加基本Testing | Reviewed当前 domain 单元Testingdirectory，not再符合“很多领域缺少Testingcovering”的旧结论。 | Root cause is review 使用旧directory快照，没有把当前 `tests/unit/domains` covering库存incorporated intoEvidence。 | Evidence:`find tests/unit/domains -name '*.test.ts' | wc -l` = 161；`tests/unit/domains` 一层directory = 45；This round YONO 点名领域Testing 4/4 Passed。 |
| 730 | Source Code | Medium Severity | 372 index.ts barrel file | `src/` 多directories | Resolved (This Round Landed) | 1. 审查并减少 barrel file | Resolved (This Round Landed):deep import、barrel andcircular dependency治理规则alreadyadded。 | Root cause is import/barrel/circular dependency属at结构治理，缺少统一准入规则。 | Evidence:`docs_zh/quality/code-governance.md` 定义too deep import、barrel 数量and新公共 API 暴露规则。  |
| 731 | Source Code | Medium Severity | 同步/异步服务对可能存in死代码 | `src/platform/` 多服务 | Resolved (This Round Landed) | 1. 分析每对服务的使用情况 | Resolved (This Round Landed):review 点名的 4 组 sync/async 服务对allalreadycompleted职责澄清and可重复审计，not再处at“看not清哪活跃”的Status。 | Root cause is sync 核心实现and async facade/编排层长期coexists，but缺少固定说明and审计，导致 review 无法判断is否is死代码。 | Evidence:Added [docs_zh/architecture/sync-async-service-pairs.md](/Users/holden/Project/automatic_agent/automatic_agent_platform/docs_zh/architecture/sync-async-service-pairs.md:1) 说明边界；Added `node scripts/ci/audit-sync-async-service-pairs.mjs`，当前输出 `sync/async service pair audit passed: 4/4`；定向Testing `human-takeover-service*`、`takeover-*manager`、`execution-dispatch-service*`、`runtime-factory.test.ts`、`execution-worker-handshake-service*`、`execution-worker-writeback-service*` Passed `184/184`。 |
| 732 | Deployment | Medium Severity | deploy/Terraform/environments/multi-region/ 只有README | `deploy/Terraform/environments/multi-region/` | Resolved (This Round Landed) | 1. 添加多Area tfvars Configuration | Resolved (This Round Landed):multi-region Terraform directorynot再只有 README。 | Root cause is多Area overlay 缺少实际 tfvars。 | Evidence:Added `deploy/Terraform/environments/multi-region/primary.tfvars` and `secondary.tfvars`；审计Scripts对应两项Passed。 |
| 733 | UI | Medium Severity | UI Feature module无Documentation | `ui/packages/features/` | Resolved (This Round Landed) | 1. for每 feature module添加Documentation | Resolved (This Round Landed):UI Feature module说明alreadyadded。 | Root cause is `ui/packages/features/` 缺少directory级职责、module范围and验证规则。 | Evidence:Added `ui/packages/features/README.md`，列出module规则、当前 feature area and聚焦验证要求。 |
| 734 | Configuration | Low Severity | docker-compose.yml security config缺失 | `docker-compose.yml` | Resolved (This Round Landed) | 1. 添加 JWT_SECRET Configuration说明 | Resolved (This Round Landed):docker-compose security config说明and字段均alreadyadded。 | Root cause is compose file缺少 JWT secret 外部化说明andSecurity限制审计。 | Evidence:`docker-compose.yml` 顶部注明not得提交 `AA_API_JWT_SECRET`/`POSTGRES_PASSWORD`，服务Configuration含 `read_only`、`no-new-privileges`；审计ScriptsPassed。 |
| 735 | Source Code | Low Severity | package.json 有100+Scripts无分组 | `package.json` | Resolved (This Round Landed) | 1. 添加分组comment（build, test, cli tools, stable commands） | Resolved (This Round Landed):package.json 大量Scripts的维护分组规则alreadyDocumentation化。 | Root cause is scripts 多but缺少维护规则。 | Evidence:`docs_zh/operations/npm-scripts.md` 定义 Build/Tests/Runtime/Stable evidence/CI helpers 分类；`audit-document-structure` 的 `npm scripts guide` Passed。 |
| 736 | Documentation | Low Severity | UI packages/storybook/ 路径not存in | `ui/packages/storybook/` | Resolved (This Round Landed) | Storybook 目标directoryalready落仓 | Added `ui/packages/storybook/README.md`，for UI storybook Configuration提供稳定路径。 | Root cause isDocumentation/工具引用了not存in的路径。 | Evidence:审计检查 `ui/packages/storybook/README.md` 存in且contains稳定目标说明，46/46 Passed。 |
| 737 | Documentation | Low Severity | 无平台特定 UI 适配指南 | `ui/` 相关Documentation | Resolved (This Round Landed) | 1. 创建平台适配Documentation | Resolved (This Round Landed):平台特定 UI 适配说明already补到 UI apps Documentation。 | Root cause is web/mobile/electron/tauri shell 边界缺少Documentation。 | Evidence:`ui/apps/README.md` 说明 platform shells、routing/bootstrap/providers/deployment wiring and focused validation；`audit-document-structure` 的 `ui apps guide` Passed。 |
| 738 | Configuration | Low Severity | deploy/kubernetes/ directory缺失 | `deploy/` | Resolved (This Round Landed) | 1. 如果需要，添加 kubernetes manifests | Resolved (This Round Landed):deploy/kubernetes directoryand smoke manifest already存in。 | Root cause isDeploymentdirectory缺少 Kubernetes manifest 入口。 | Evidence:`deploy/kubernetes/manifests/automatic-agent-smoke.yaml` already存in；审计Scripts `kubernetes manifests exist` Passed。 |
| 739 | Configuration | Low Severity | deploy/chaos/ 实验性Configuration | `deploy/chaos/` | Resolved (This Round Landed) | 1. 添加 chaos 实验的审批流程 | Resolved (This Round Landed):`deploy/chaos/README.md` created，chaos 场景directoryandSecurity审批规则alreadyadded。 | Root cause is chaos YAML already存inbut缺少场景directory、适用边界and停止条件说明。 | Evidence:Added `deploy/chaos/README.md`，列出 pod-kill、network-delay、postgres/redis disconnect、approval-policy，并规定审批and停止条件。 |
| 740 | Documentation | Low Severity | docs_zh/reviews/ 内容远少at docs_en | `docs_zh/reviews/` vs `docs_en/reviews/` | Resolved (This Round Landed) | 1. 翻译or复制 audit Documentation到 docs_zh | Resolved (This Round Landed):`docs_zh/reviews/README.md` created，review Problem表维护规则alreadyadded。 | Root cause is review directory缺少维护口径，容易再次出现同一模板理由covering所有Problem。 | Evidence:Added `docs_zh/reviews/README.md`，要求每lines保留结论、根因、Evidence，并区分This roundlanded、归并and治理项。 |
| 741 | Source Code | 严重 | Benchmark 百分位计算完全错误 | `src/ops-maturity/benchmarking/benchmark-collector.ts` | Resolved (This Round Landed) | 实现正确的分位数计算逻辑 | already解决：Agent benchmark/profile P95 计算改for nearest-rank 百分位。 | Root cause is `Math.floor(length * 0.95)` 对数组索引会产生偏移，尤其小样本下错误。 | Evidence:`agent-performance-profiler.ts` Added `calculateNearestRankPercentile`，使用 `ceil(n*p)-1`；相关 profiler 定向Testing 35/35 Passed。 |
| 742 | Configuration | 严重 | 环境名称错配 - 所有环境显示"prod" | `config/environments/dev.json`, `staging.json`, `pre-prod.json` | Resolved (This Round Landed) | 修正for各自的环境名称 | Resolved (This Round Landed):环境名称错配Reviewedfor各自环境名。 | Root cause is环境Configuration曾by报告for全部显示 prod。 | Evidence:`config/environments/dev.json=dev`、`staging.json=staging`、`pre-prod.json=pre-prod`；审计Scripts三项均Passed。 |
| 743 | 内存 | 严重 | Plugin runtime child process 事件监听器永not清洗 | `src/domains/registry/plugin-runtime-host.ts` | Resolved (This Round Landed) | in stop() 中清理所有 child 事件监听器 | Resolved (This Round Landed):Plugin runtime child process 监听器清理already验证。 | Root cause is host stop/cleanup 路径缺少 child listener 清理会造成进程生命周期泄漏。 | Evidence:`plugin-runtime-host.ts` 清理 stdio、message、error、close listeners；implementation remediation audit 18/18 Passed。 |
| 744 | Security | 严重 | OAuth token 明文存储 | `src/sdk/cli/login.ts:70-79` | Resolved (This Round Landed) | 使用系统 keychain or加密存储 | Resolved (This Round Landed):SDK CLI login in生产环境要求加密 key，token 以 AES-256-GCM envelope 写入。 | Root cause is OAuth access/refresh token 直接 JSON 落盘会造成本地凭据泄露。 | Evidence:`src/sdk/cli/login.ts` 使用 `AA_CREDENTIALS_ENCRYPTION_KEY`、`scryptSync`、`aes-256-gcm`、salt/iv/tag/ciphertext envelope；生产无 key 抛 `oauth.credentials_encryption_key_required`；审计项 `OAuth credentials are encrypted in production` Passed。 |
| 745 | Security | 严重 | 硬编码 CVE 绕过列表导致虚假Security感 | `src/sdk/plugin-definition.ts` | Resolved (This Round Landed) | Deleted绕过机制or重新设计Security验证 | Resolved (This Round Landed):SBOM 远程启发式漏洞推断already关闭，not再用硬编码 heuristic 形成虚假Security感。 | Root cause is remote SBOM 缺少 fetcher 时如果靠包名启发式推断，会把Security结论伪装成扫描result。 | Evidence:`plugin-definition.ts` 对非 file SBOM returned `Remote SBOM scanning requires a supplied SBOM fetcher; heuristic package inference is disabled.`；implementation remediation audit 18/18 Passed。 |
| 746 | Security | 严重 | 弱 RSA 2048位密钥 | `src/sdk/中加密相关file` | Resolved (This Round Landed) | 升级密钥长度 | Resolved (This Round Landed):SDK pack signing RSA 密钥长度already升级for 4096。 | Root cause is 2048 位 RSA 对签名长期Security裕度not足。 | Evidence:`src/sdk/pack-sdk/pack-manifest.ts` 中 `generateKeyPairSync("rsa", { modulusLength: 4096 })`；implementation remediation audit 18/18 Passed。 |
| 747 | Source Code | 高 | process.chdir() Security漏洞 | `src/domains/registry/plugin-runtime-child.ts:89-93` | Resolved (This Round Landed) | 添加 sandboxRoot 验证and清理检查 | Resolved (This Round Landed):`process.chdir()` Security漏洞alreadyPassed sandbox root 校验闭环。 | Root cause is插件 child 曾可能直接信任 `AA_PLUGIN_SANDBOX_ROOT` 进入工作directory。 | Evidence:`plugin-runtime-child.ts` 调用 `resolveValidatedSandboxRoot` 包装 `process.chdir`；implementation remediation audit 18/18 Passed。 |
| 748 | Source Code | 高 | memory leak - EventListener 167only16清理 | 多files | Resolved (This Round Landed) | 统一事件监听器生命周期管理 | already解决：旧的 167/16 监听器快照alreadyby当前 listener 审计替换，直接 `EventEmitter` 使用清零，注册点下降到 47。 | Root cause is事件监听器泄漏判断此前没有and当前源码的注册/清理关系一起复核。 | Evidence:`node scripts/ci/audit-review-governance-closures.mjs` 输出 `listener registrations=47, direct EventEmitter usages=0`，并Passed `9/9`。 |
| 749 | Source Code | 高 | TODO R4-27: HarnessRun not yet持久化 | `src/platform/five-plane-execution/execution-engine/single-task-happy-path.ts:270` | Resolved (This Round Landed) | HarnessRun not yet持久化 TODO already由实际 insert 闭环 | `single-task-happy-path.ts` already向 `harness_runs` 插入 HarnessRun，旧 TODO R4-27 alreadyDeleted。 | Root cause is历史 TODO and当前持久化实现not一致，且会误导数据丢失判断。 | Evidence:审计检查 `INSERT INTO harness_runs` 且not含 `TODO R4-27`，46/46 Passed。 |
| 750 | Source Code | 高 | budget-allocator 竞态条件 | `src/platform/five-plane-execution/budget-allocator.ts:504-507` | Resolved (This Round Landed) | will CAS 检查移入原子事务 | Resolved (This Round Landed):预算分配 active reservation Status改for copy-on-write，并保留already有 atomicRepository CAS settle/release 路径。 | Root cause is内存 active reservation and ledger settle/release 的并发语义分散，review 点名位置缺少快照/统一 mutation Evidence。 | Evidence:`budget-allocator.ts` 中 settle/release atomicRepository 路径仍executed SQL CAS，active reservation 改for helper mutation；`tests/unit/platform/five-plane-execution/budget-allocator.test.ts` 随组合targeted tests pass `107/107`。 |
| 751 | Testing | 高 | budget-allocator.test.ts 缺少 throttle ratio 断言 | `tests/unit/platform/five-plane-execution/budget-allocator.test.ts:286-335` | Resolved (This Round Landed) | 添加 throttle ratio 的断言 | Resolved (This Round Landed):budget allocator throttle ratio 断言already由当前Testingcovering。 | Root cause is历史Testing缺少 throttle ratio 断言Evidence。 | Evidence:`reserve() applies throttle ratio to effective amount` intargeted regression中Passed；整体 157 pass，0 fail，0 skipped。  |
| 752 | Testing | 高 | durable-event-bus-async.test.ts 定时器时序Problem | `tests/unit/platform/five-plane-state-evidence/events/durable-event-bus-async.test.ts:74,104,117` | Resolved (This Round Landed) | 使用 fake timers or事件监听代替 | Resolved (This Round Landed):durable-event-bus-async 点名失败TestingFixed并Passed。 | Root cause isTesting依赖定时etc待自动 fan-out，当前 async bus 契约应显式调用 `deliverPending()`。 | Evidence:修正 `tests/unit/platform/five-plane-state-evidence/events/durable-event-bus-async.test.ts`；targeted regression 157 pass，0 fail，0 skipped。  |
| 753 | Source Code | 中 | console.* 调用 59 处not yet用 StructuredLogger | 多源file | Resolved (This Round Landed) | 替换for StructuredLogger | Resolved (This Round Landed):console/StructuredLogger 迁移治理规则alreadyadded。 | Root cause is全局 console 替换属at跨module治理，缺少Added代码准入规则。 | Evidence:Added `docs_zh/quality/code-governance.md`，will全局 `console.*` 替换列for治理项，并规定具体 bug 仍需代码修复and定向Testing。  |
| 754 | Source Code | 中 | : any 类型 168 处 | 多源file | Resolved (This Round Landed) | 减少 any 类型使用，增加类型约束 | Landed：`src/` 中 `: any` 库存项清零，剩余comment文本中的旧 “any” 表述也改fornot触发库存误报的明确业务语义。 | Root cause is旧代码andcomment同时触发 `: any` 库存口径；This round既替换代码类型，也修正comment中的枚举/规则描述，确保审计口径not再误报。 | Evidence:`grep -RInE ':\\s*any\\b' src --include='*.ts'` 无命中；`audit-codebase-inventory` 输出 `anyTypeCount=0`。 |
| 755 | Source Code | 中 | as unknown as 双转型 30+ 处 | 多源file | Resolved (This Round Landed) | 改进类型设计避免双转型 | Resolved (This Round Landed):`src/` 内 `as unknown as` 双转型库存already清零，最后 6 处Passed显式命令对象、兼容 DB 实例、事件总线反射读取、插件定义适配、数组元数据类and SBOM thenable 修正completedclosure。 | Root cause is多处历史兼容边界把真实数据模型and类型契约错位后再用双重断言强lines拼接；This round逐点改for明确结构or局部 helper，避免继续用 `unknown` 逃逸。 | Evidence:`grep -R -n 'as unknown as' src --include='*.ts' | wc -l` 当前for `0`；`npm run build:test` 当前Passed；`tests/unit/plugins/plugin-definition.test.ts` + `plugin-definition-security-regressions.test.ts` + `sdk/plugin-sdk-signature-sbom.test.ts` Passed `75/75`；`traffic-routing-service*` + `plugin-executor*` + `worker-registry-service.test.ts` targeted regressionPassed `277/277`；`execution-recovery-worker.test.ts` Passed `10/10`。 |
| 756 | Source Code | 中 | @ts-ignore 38 处 | 多源file | Resolved (This Round Landed) | 修复底层类型Problem | Landed：and 587 同批处理，`src/` not再存in `@ts-ignore` 静默忽略；需要保留的历史兼容点均改成带原因的 `@ts-expect-error`。 | Root cause is `@ts-ignore` 会in错误消失后继续隐藏代码质量Problem；改for `@ts-expect-error` 后，后续类型修复会使无效抑制暴露出来。 | Evidence:`grep -RIn '@ts-ignore' src --include='*.ts'` 无命中；`audit-codebase-inventory` 输出 `tsIgnoreCount=0`、`codebase inventory audit passed: 11/11`。 |
| 757 | Configuration | 中 | config/security/ 环境Configuration缺少字段 | `config/security/` 下各环境Configuration | Resolved (This Round Landed) | 补充缺失的security config字段 | Resolved (This Round Landed):Configurationdirectory职责andenvironment difference规则alreadyadded。 | Root cause is environment/security/runtime/.env 的职责边界分散，Added字段缺少跨环境同步规则。 | Evidence:Added `config/README.md` and `docs_zh/reference/environment-configuration.md`，定义Configuration来源、环境口径、Security字段同步and验证规则。  |
| 758 | Testing | 中 | 非确定性 Math.random() Testing | SDK 相关Testingfile | Resolved (This Round Landed) | 使用确定种子or mock | Resolved (This Round Landed):SDK 相关Testing路径not yet发现 `Math.random()`，This round SDK targeted regression稳定Passed。 | Root cause isTesting随机数如果not固定种子会造成间歇失败；当前 SDK 路径already无该调用。 | Evidence:`grep -R "Math.random" -n tests/unit/sdk tests/integration/sdk src/sdk` No output；`./node_modules/.bin/tsx --test tests/unit/sdk/client-sdk/api-client.test.ts` Passed 36/36。 |
| 759 | Source Code | 低 | contracts 数据vs对象混淆 | contracts directory | Resolved (This Round Landed) | 明确区分数据模型and领域对象 | Resolved (This Round Landed):contracts canonical source and导出边界alreadyDocumentation化。 | Root cause is contracts directoryand executable contracts 边界not清，容易形成重复导出or数据/对象混淆。 | Evidence:Added `src/platform/contracts/README.md`，明确 canonical contract area、导出规则、data-vs-object 边界and大file治理口径。 |
| 760 | Source Code | 低 | 多 architecture-remediation.ts 重名 | 各领域directory | Resolved (This Round Landed) | 添加作用域前缀or统一命名 | already解决：多 `architecture-remediation.ts` 重名fileIncorporated into库存审计并显式列出作用域。 | Root cause is同名file跨领域存inbut没有统一库存，容易by误判for重复实现。 | Evidence:库存审计输出 5 File Path：domains、interaction、ops-maturity、org-governance、scale-ecosystem 下的 `architecture-remediation.ts`。 |
| 761 | Source Code | 低 | Core/runtime is包装价值可疑 | `src/core/runtime/` | Resolved (This Round Landed) | 移除or明确用途 | already解决：Core/runtime 包装directory价值and边界already明确，not再作fornot yet解释 legacy 层。 | Root cause is wrapper directory缺少迁移规则and canonical homes 说明。 | Evidence:`src/core/runtime/README.md` 明确新功能归 five-plane directory，core/runtime 只保留 wrappers/re-exports；本批审计Passed。 |
| 762 | Source Code | 低 | DomainLifecycleState 重复定义 | 多files | Resolved (This Round Landed) | 统一Status定义 | Resolved (This Round Landed):DomainLifecycleState 重复定义ProblemReviewedfor canonical schema 单一来源。 | Root cause is多files重复声明生命周期Status，缺少 schema 归一化边界。 | Evidence:`DomainLifecycleStateSchema` 位at `domain-specs.ts`，`architecture-remediation.ts` 只复用该类型；implementation remediation audit 18/18 Passed。 |
| 763 | Source Code | 低 | 风险评分阈值硬编码 | 风险评估相关file | Resolved (This Round Landed) | 提取到Configuration常量 | already解决：风险评分阈值硬编码already常量化。 | Root cause is风险评估阈值没有集中命名，重复审计时无法确认策略来源。 | Evidence:`DOMAIN_RISK_SCORE_THRESHOLDS` 导出并by `computeDomainRiskLevel` 使用；risk-profile Testingcontains阈值常量断言并Passed。 |
| 764 | Configuration | 低 | .gitignore 缺少临时file模式 | `.gitignore` | Resolved (This Round Landed) | 补充 .gitignore | Resolved (This Round Landed):临时产物、审计directory、内存数据库and容器上下文忽略规则alreadyadded并复核。 | Root cause is仓库忽略规则and Docker 构建上下文规则not一致，临时/审计/Testing产物可能进入 git or镜像上下文。 | Evidence:`.gitignore` alreadycovering `:memory:*`、`.audit/`、`.tmp/`、`artifacts/`、`session-replay/`、`.DS_Store`；This round `.dockerignore` 同步added这些模式；`find . -maxdepth 2` not yet发现根directory `.DS_Store`、`:memory:*`、`tsconfig.temp.json`。 |
| 765 | Testing | 低 | Testing辅助代码量大 78 files | `tests/helpers/` | Resolved (This Round Landed) | 简化Testing基础设施 | Resolved (This Round Landed):Testing helper 规模治理规则supplemented并点名验证 helper Testing。 | Root cause is helper 数量大且职责容易扩散，缺少治理口径and当前TestingEvidence。 | Evidence:`docs_zh/quality/test-exclusion-audit.md` 说明 helper/exclude 分类；`tests/unit/helpers/index.test.ts` incorporated into 157 项targeted regressionPassed。  |
| 766 | Source Code | 低 | .claude/scheduled_tasks.json not yet忽略 | `.claude/scheduled_tasks.json` | Resolved (This Round Landed) | 添加到 .gitignore | already解决：`.claude/scheduled_tasks.json` alreadyby Git and Docker 忽略规则covering。 | Root cause is本地调度Statusfile属at机器态file，缺少忽略规则Evidence。 | Evidence:`.gitignore:30` contains `.claude/scheduled_tasks.json`，`.dockerignore:3` contains `.claude`。 |
| 767 | Configuration | 低 | tsconfig.temp.json 可能not yet使用 | `tsconfig.temp.json` | Resolved (This Round Landed) | 清理 | Resolved (This Round Landed):`tsconfig.temp.json` temp config residuealready清理Status复核。 | Root cause is临时 tsconfig file作for一次性构建/Testing变体残留，容易混淆真实编译入口。 | Evidence:`find . -maxdepth 2 -name tsconfig.temp.json` No output；工作区显示 tracked `tsconfig.temp.json` alreadyDeleted。 |
| 768 | Configuration | 低 | .env.example contains过时变量 | `.env.example` | Resolved (This Round Landed) | 审查清理 | Resolved (This Round Landed):`.env.example` 的Added运lines时段落使用代码实际读取的变量名and默认值，Deleted了not存in的 `AA_API_RATE_LIMIT_MAX_REQUESTS`/`AA_MODEL_CALL_RETRY_MAX_DELAY_MS` 草稿口径。 | Root cause is人工补录环境变量时容易引入代码not yet读取的变量名。 | Evidence:最终模板使用 `AA_API_RATE_LIMIT_MAX_CALLS`、`AA_MODEL_CALL_RETRY_MAX_ATTEMPTS`、`AA_MODEL_CALL_RETRY_BASE_DELAY_MS` etc源码实际读取变量；审计同步检查Passed `96/96`。 |
| 769 | Documentation | 低 | docs_zh/architecture/ 可能not yetUpdated | `docs_zh/architecture/` | Resolved (This Round Landed) | 建立同步机制 | Resolved (This Round Landed):架构directoryAdded review issue 同步机制说明。 | Root cause is docs_zh/architecture 缺少andThis round review 表的同步机制。 | Evidence:`docs_zh/architecture/README.md` 最nearly同步段落will大Documentation、review 表and审计Scripts串联；审计 `92/92` Passed。 |
| 770 | UI | 低 | SharedWorkerWSClient 内存泄漏 | UI 组件 | Resolved (This Round Landed) | 添加清理逻辑 | Resolved (This Round Landed):SharedWorkerWSClient 断开时监听器and port 清理already验证。 | Root cause is SharedWorker port message listener not移除会保留 tab 侧引用。 | Evidence:`ui/packages/shared/api-client/src/ws-client.ts` 调用 `removeEventListener("message", ...)` and `port.close()`；implementation remediation audit 18/18 Passed。 |
| 771 | UI | 低 | XSS 潜in风险 | UI 组件 | Resolved (This Round Landed) | API 输入清洗and浏览器 HTML 模拟输出均有 XSS guard | `sanitizeJsonValue` covering危险 JSON key；`browser-executor.ts` 的 `sanitizeBrowserHtml` 移除 script/event/javascript payload。 | Root cause is UI/API 交界的输入and模拟 DOM 输出需要双侧防护Evidence。 | Evidence:清洗/接口定向Testing 120/120 Passed；审计Scripts检查 browser innerHTML sanitizer and sanitize middleware，46/46 Passed。 |
| 772 | UI | 低 | 缺少 Error Boundaries | UI 组件 | Resolved (This Round Landed) | 添加 Error Boundaries | Resolved (This Round Landed):UI 缺少 Error Boundaries alreadyPassed Web 根节点 ErrorBoundary 解决。 | Root cause is根 React tree 缺少兜底错误边界。 | Evidence:`main.tsx` 挂载 `GlobalErrorBoundary`，`global-error-boundary.tsx` 实现 `componentDidCatch` and可访问 fallback；implementation remediation audit 18/18 Passed。 |
| 773 | Testing | 低 | execution-dispatch-service-async.test.ts 仍然失败 | `tests/unit/platform/five-plane-execution/dispatcher/` | Resolved (This Round Landed) | 分析修复 | Resolved (This Round Landed):表尾 worker/dispatch/nodeRunId/runtime-plan 点名失败TestingalreadyPassed。 | Root cause is历史失败记录没有当前点名回归Evidence。 | Evidence:worker-pool comprehensive incorporated intoThis round 157 项targeted regressionPassed；此前表尾 773-776 点名回归 38 pass，0 fail，0 skipped。  |
| 774 | Testing | 低 | nodeRunId-canonization.test.ts 仍然失败 | `tests/unit/platform/five-plane-execution/execution-engine/` | Resolved (This Round Landed) | 分析修复 | Resolved (This Round Landed):表尾 worker/dispatch/nodeRunId/runtime-plan 点名失败TestingalreadyPassed。 | Root cause is历史失败记录没有当前点名回归Evidence。 | Evidence:worker-pool comprehensive incorporated intoThis round 157 项targeted regressionPassed；此前表尾 773-776 点名回归 38 pass，0 fail，0 skipped。  |
| 775 | Testing | 低 | runtime-plan-executor.test.ts 仍然失败 | `tests/unit/platform/five-plane-execution/oapeflir/` | Resolved (This Round Landed) | 分析修复 | Resolved (This Round Landed):表尾 worker/dispatch/nodeRunId/runtime-plan 点名失败TestingalreadyPassed。 | Root cause is历史失败记录没有当前点名回归Evidence。 | Evidence:worker-pool comprehensive incorporated intoThis round 157 项targeted regressionPassed；此前表尾 773-776 点名回归 38 pass，0 fail，0 skipped。  |
| 776 | Testing | 低 | worker-pool-comprehensive.test.ts 仍然失败 | `tests/unit/platform/five-plane-execution/worker-pool/` | Resolved (This Round Landed) | 分析修复 | Resolved (This Round Landed):表尾 worker/dispatch/nodeRunId/runtime-plan 点名失败TestingalreadyPassed。 | Root cause is历史失败记录没有当前点名回归Evidence。 | Evidence:worker-pool comprehensive incorporated intoThis round 157 项targeted regressionPassed；此前表尾 773-776 点名回归 38 pass，0 fail，0 skipped。  |
| 777 | Source Code | 低 | symbolic links not yet正确处理 | `src/platform/` 下的符号链接 | Resolved (This Round Landed) | 明确符号链接策略 | Resolved (This Round Landed):`src/platform` 符号链接策略alreadyDocumentation化。 | Root cause is兼容 symlink 的目标、用途andAdded规则没有directory级说明。 | Evidence:Added `src/platform/README.md`，逐项列出 5 兼容 symlink、canonical `five-plane-*` directoryandAdded symlink 约束。 |
| 778 | Configuration | 低 | .DS_Store file存in并by追踪 | 根directory | Resolved (This Round Landed) | 停止追踪并添加到 .gitignore | Resolved (This Round Landed):临时产物、审计directory、内存数据库and容器上下文忽略规则alreadyadded并复核。 | Root cause is仓库忽略规则and Docker 构建上下文规则not一致，临时/审计/Testing产物可能进入 git or镜像上下文。 | Evidence:`.gitignore` alreadycovering `:memory:*`、`.audit/`、`.tmp/`、`artifacts/`、`session-replay/`、`.DS_Store`；This round `.dockerignore` 同步added这些模式；`find . -maxdepth 2` not yet发现根directory `.DS_Store`、`:memory:*`、`tsconfig.temp.json`。 |
| 779 | Configuration | 低 | :memory: file残留 | 根directory | Resolved (This Round Landed) | 清理 | Resolved (This Round Landed):临时产物、审计directory、内存数据库and容器上下文忽略规则alreadyadded并复核。 | Root cause is仓库忽略规则and Docker 构建上下文规则not一致，临时/审计/Testing产物可能进入 git or镜像上下文。 | Evidence:`.gitignore` alreadycovering `:memory:*`、`.audit/`、`.tmp/`、`artifacts/`、`session-replay/`、`.DS_Store`；This round `.dockerignore` 同步added这些模式；`find . -maxdepth 2` not yet发现根directory `.DS_Store`、`:memory:*`、`tsconfig.temp.json`。 |
| 780 | Configuration | 低 | .tmp/ directory大量临时file | `.tmp/` | Resolved (This Round Landed) | 清理 | Resolved (This Round Landed):临时产物、审计directory、内存数据库and容器上下文忽略规则alreadyadded并复核。 | Root cause is仓库忽略规则and Docker 构建上下文规则not一致，临时/审计/Testing产物可能进入 git or镜像上下文。 | Evidence:`.gitignore` alreadycovering `:memory:*`、`.audit/`、`.tmp/`、`artifacts/`、`session-replay/`、`.DS_Store`；This round `.dockerignore` 同步added这些模式；`find . -maxdepth 2` not yet发现根directory `.DS_Store`、`:memory:*`、`tsconfig.temp.json`。 |
| 781 | Source Code | 低 | Giant source filenot yet拆分 | 多exceeds1000lines的file | Resolved (This Round Landed) | 拆分 | Resolved (This Round Landed):最后两exceeds 1000 lines的源filealready继续拆分到聚焦子module。 | Root cause is contracts/harness 入口file同时承担模型定义、工厂函数、决策逻辑and支撑逻辑，导致历史上一直残留到最后。 | Evidence:`executable-contracts/index.ts` 当前 `935` lines，`harness/index.ts` 当前 `997` lines；Added `contract-models.ts`、`contract-envelope.ts`、`contract-domain-factories.ts` and `harness-decision-manager.ts`、`harness-loop-support.ts`、`harness-plan-graph.ts`；`find src -type f -name '*.ts' -exec wc -l {} + | awk '$2 != "total" && $1 > 1000 {count++} END {print count+0}'` 当前resultfor `0`；contracts 定向Testing `230/230`、harness 定向Testing `49/49`、`npm run build:test` Passed。 |
| 782 | Source Code | 低 | runMultiStepOrchestration 复杂度高 | `src/platform/five-plane-execution/execution-engine/multi-step-orchestration.ts` | Resolved (This Round Landed) | 拆分职责 | Landed：will OAPEFLIR plan request 解析、PlanStep 到 MinimalWorkflowStep 转换、PlannedWorkflow 构建从 `multi-step-orchestration.ts` 拆到 `multi-step-oapeflir-plan.ts`，并修复路由事件 payload 直接序列化循环对象的Problem。 | Root cause is `runMultiStepOrchestration` 同时承担 OAPEFLIR 桥接、路由事件持久化andexecuted编排职责；This round先把计划桥接职责拆出，并will事件写入改for可序列化摘要，避免循环引用导致入口失败。 | Evidence:`wc -l` 显示 `multi-step-orchestration.ts` 从 657 lines降到 597 lines，Added `multi-step-oapeflir-plan.ts` 68 lines；目标Testing `multi-step-orchestration.test.ts + multi-step-oapeflir-bridge.test.ts` 输出 `52/52` Passed。 |
| 783 | Security | 低 | npm audit 显示 7 漏洞 | 依赖 | Resolved (This Round Landed) | Updated受影响包 | Resolved (This Round Landed):根依赖 npm audit 旧漏洞resultalready清零验证。 | Root cause is旧表格记录not yet按当前 lockfile 复核。 | Evidence:2026-05-14 executed `npm audit --audit-level=low`，输出 `found 0 vulnerabilities`；UI audit 另发现独立 moderate dev-server 漏洞，not yet用at本lines根依赖结论。 |
