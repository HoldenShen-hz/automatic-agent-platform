# 2026-04-30 Platform Architecture Implementation Consistency Audit Round - Per-Number Re-audit Results

This document provides per-number review results for `docs_zh/reviews/platform-architecture-implementation-consistency-audit_round.md`.

Principles:

- Do not directly inherit historical long-table conclusions.
- Only write "fixed/resolved" when there is real source code, contract, ADR, configuration, or targeted test evidence.
- For items that don't require code changes, explicitly state the evidence; do not create false fixes.
- Do not perform full testing.

Scale notes:

- Original line items: 3490
- Unique numbers: 2640
- Duplicate numbers: 850
- Number range up to `2604`, plus round numbers like `R2-1` to `R32-55`
- New unique numbers added in this supplement: 1334

Currently directly fixed and targeted-verified real issues:

- `R2-6`: `BudgetGuard`'s pack/platform result types vs legacy normalization gap
- `R4-37`: `network-egress-policy` default `deny` mode not actually blocking, and `audit_only` environment mode parsing drift
- Autonomy/proactive trigger family stale test assertions: medium risk old assertion still expects `auto_execute`; TrustScorer old assertion still压低 low-risk `fully_trusted` to `semi_auto`
- `R5-49`: `KnowledgeQueryService` already has scope input but doesn't bind `accessPrincipal` into service layer boundary validation
- `R22-31`: `TransactionalEventAppender` only wraps event/outbox, no truth mutation hook, cannot guarantee truth+event same transaction
- `R22-47`: Recovery classification still exposes `L1/L2/L3` externally, not aligned with canonical `transient/permanent/unknown`; also fixed several real misclassification priority issues
- `R5-59`: `ADR-002` still has残留 `VP 编排层/事业部` result expression, mixed with migrated DomainDescriptor/BusinessPack main line
- `R5-63`: In ADR source section batch, `ADR-002/004/028` still have old section numbers or old narrative残留, need to supplement current canonical references
- `2580`: `SqlExecutionMetricsProvider` changed incident count to by failed status, but `lastIncidentAt` and old tests still use `last_error_code` semantics, causing reaudit evidence to continue drifting
- `2575`: `DashboardWebSocketServer` metric routing previously only had consumer side, no subscription registration endpoint; this round supplemented registration/update/recovery chain
- `2568`: `AutonomyGovernanceService` recognizes `P0/P1`, but old severity branch discards `promotion.targetLevel`, high severity incidents actually don't trigger demotion suggestion; fixed this round
- `2585`: `addWorkspaceMembership()` old "fix" has no real caller context, just reads owner membership; this round connected `callerUserId` into service/CLI boundary and does real authorization
- `2586`: `addOrganizationMembership()` old implementation has neither caller principal nor relies on non-existent `organization.ownerId`; this round changed to `createOrganization()` explicitly establishing owner membership, then using `callerUserId` for member management authorization
- `R5-28`: `GoalDecompositionService` previously only exposed capability/permission violations, but task envelope doesn't produce final permission set narrowed by domain; this round supplemented real task-level permission narrowing, with mismatch alerts retained
- `R14-22`: `authoritative-task-store-decorator` old implementation no longer uses global metrics Map, but sync retry still relies on blocking wait as fallback; this round removed `SharedArrayBuffer`/blocking sleep, changed to instance-level metrics + async non-blocking backoff + sync immediate retry
- `R15-02`: SDK version handshake test still uses old interface treating `initialize()` as returning handshake result; this round rewrote to explicitly call `performVersionHandshake()`, and supplemented evidence for `R15-01/02/03`
- `2236`: `DurableEventBus.ensurePendingAcksForActiveConsumers()` previously created pending ack for active consumers not registered to that eventType, causing false backlog count; this round filtered by event registry
- `2246`: `EvalRunService` previously directly used `feedbackEnvelope.signals` as `actualEvidenceRefs`, mixing legacy reason/signal with evidence; this round changed to prioritize reading step `evidenceRefs`, only falling back to flat signals when legacy run has no step evidence
- `2247`: `governance-projection` removed unknown `decision*` events to `approval_granted` erroneous inference; this round also found projection handler directly returning internal `_processedEventIdSet`, not serialized back to `processedEventIds`, causing deduplication state unable to persist; this real implementation issue also fixed
- `2249/2250`: `access-model` now only trusts capabilities obtained from role inheritance, and converged `manualTakeoverActive` to "record only and only available to operator-grade principal" context marker; this round also fixed the default-allow regression exposed during validation, restored normal path of "allow after capability and context check pass"
- `2251/2254/2255/2256`: OIDC path now forbids mock user fallback when `allowMockFallback=false`, uses `timingSafeEqual` to validate access token, limits single user concurrent session count and implements `touchSession()` sliding expiration; supplemented targeted verification, and also fixed PKCE helper's ESM `require()` runtime error
- `2236/2239/2240`: `FeedbackSignal` now forces `trustScore/evidenceRefs` with canonical `harnessRunId/nodeRunId`, `workflow-run-projection` changed `subtask:failed` to only record failed step, not immediately mark workflow as failed, and projection deduplication main chain now uniformly switched to internal `Set` instead of `Array.includes()`
- `2257/2258/2259`: `PolicyEngine`'s `full-auto` no longer bypasses high-risk escalation; `VaultHttpSecretProvider` no longer caches AppRole token missing `lease_duration` for 1 hour by default; `DataClassificationService` now rejects unsafe regex before execution, avoiding user-defined patterns triggering ReDoS
- `R7-42/R7-43/R7-47` and `2292/2293/2295/2296/2298/2300`: HTTP API main chain previously scattered idempotency, version negotiation, trace propagation and task creation fact append across local components, not truly connected to request entry; this round converged to `HttpApiServer` main chain and supplemented task creation truth+event same transaction
- `2278-2291`: Round 39 CLI/SDK consecutive entries now all re-reviewed; among them `migrate-sqlite-to-pg`, `api-server`, `harness-sdk`, `admin-sdk`, `billing`, `shadow-snapshot`, `data-plane`, `client-sdk` mostly belong to reaudit conclusion lag, `dlq-manager` and `login` have real gaps, this round respectively supplemented transaction/audit/archive/schema alignment, and truly executable PKCE OAuth CLI entry with ESM secure persistence
- `2302`: `ServiceRegistry` already has `createScoped()`, but top-level bootstrap/runtime catalog/runtime orchestrator/root summary default entry still implicitly bound `getInstance()`; this round cut all these production assembly defaults to `createScoped()`, only sharing when explicitly passed registry
- `2203`: `remote-session-state` real implementation already converged failed received `connected` signal to `connecting`, but reaudit entry and corresponding unit test still停留在 old bug description; this round rewrote to "must reconnect handshake first" current semantics, and fixed invalid test import path
- `2192/2193/2195/2196/2197/2198/2199/2204`: In Round 37 multi-tenant/multi-region early entries, some have actually been really fixed by subsequent implementation but not written back,另有 `2194` still has real timed flush bypass, this round closed timed flush into `DataReplicatorService.flush()` main chain, and supplemented boundary/delay/capacity/queue/sequence evidence
- `R11-35`: `prompt-routes` `POST` path long ago went through strong-type schema, but same file `PUT /deprecate` still残留 `readJsonBody(...) as Record<string, unknown>` old branch; this round supplemented independent deprecation schema, and also cleaned up `R11-32/33/34/36` this group of "boundary still bare parse/as any" stale conclusions rewritten according to current state
- `R11-41/R11-44`: This round continued cleaning Round 11 residual real type erasure points, respectively removed implicit unsafe assertions for `elementId/interactionType` in `UxEventTrackingService`, and `stable-runner-factory` erasure of runner signature `(runner as any)(runnerArgs as any)`; also rewrote `R11-37/38/39/40/42/43` according to current source code as "original charges outdated"
- `R6-45`: 5 short contract documents previously indeed lacked remediation / canonical mandatory rules, and several less than 60 lines; this round directly supplemented `sla_tier`, `quota_preemption`, `multimodal_gateway`, `org_hierarchy`, `feedback_improvement` remediation, export/compatibility constraints and v4.3 mandatory rules

Targeted verification results:

- `node --test --import tsx tests/unit/platform/control-plane/iam/network-egress-policy.test.ts tests/unit/platform/model-gateway/cost-tracker/budget-guard.test.ts tests/unit/platform/model-gateway/cost-tracker/budget-guard-types.test.ts tests/unit/interaction/autonomy/trust-scorer/index.test.ts tests/unit/interaction/autonomy/promotion-engine/index.test.ts tests/unit/interaction/proactive-agent/trigger-engine/index.test.ts tests/unit/interaction/proactive-agent/trigger-engine/trigger-engine.test.ts tests/unit/interaction/proactive-agent/trigger-engine/resolve-trigger-action-mode.unit.test.ts tests/unit/interaction/proactive-agent/trigger-engine.test.ts tests/unit/platform/interaction/proactive-agent/trigger-engine.test.ts`
- Result: `152 passed / 0 failed / 0 skipped`
- Additional targeted tests: `62 passed / 0 failed / 0 skipped`
- Additional targeted tests: `10 passed / 0 failed / 0 skipped`
- Additional targeted tests: `395 passed / 0 failed / 0 skipped`
- Additional targeted tests: `3 passed / 0 failed / 0 skipped`
- Additional targeted tests: `149 passed / 0 failed / 0 skipped`
- Additional targeted tests: `47 passed / 0 failed / 0 skipped`
- Additional targeted tests: `30 passed / 0 failed / 0 skipped`
- Additional targeted tests: `9 passed / 0 failed / 0 skipped`
- Additional targeted tests: `2 passed / 0 failed / 0 skipped`
- Additional targeted tests: `65 passed / 0 failed / 0 skipped`
- Additional targeted tests: `10 passed / 0 failed / 0 skipped`
- Additional targeted tests: `44 passed / 0 failed / 0 skipped`
- Additional targeted tests: `1 passed / 0 failed / 0 skipped`
- Additional targeted tests: `63 passed / 0 failed / 0 skipped`
- Additional targeted tests: `94 passed / 0 failed / 0 skipped`
- `npm run build` - Result: Passed; simultaneously exposed and converged `stable-gate.ts`, `stable-package.ts` signature drift under `exactOptionalPropertyTypes` writing `undefined` to optional fields explicitly

## Per-Number Conclusion Table (by unique number)

| Number | Original Section | Severity | Location | Original Issue | Current Conclusion | Root Cause | Evidence | This Round Action |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| R2-1 | 7. AI Operations Layer Code vs Architecture (§15-§23) | CRITICAL | `model-gateway/unified-chat-provider.ts` | ChatCompletionRequest missing required fields traceId/tenantId/costTag, architecture §15.2 explicitly requires | Resolved | Original audit停留在 old model gateway request and streaming interface snapshot, didn't reflect subsequent context, budget and audit propagation | Current `src/platform/model-gateway/provider-registry/unified-chat-provider.ts` already includes `traceId`, `tenantId`, `costTag`, `abortSignal`, `validatePartialChunk`, `streamingBudgetTrack`, `policyOutcome`, and backfills trace | No code change, just wrote evidence |
| R2-2 | 7. AI Operations Layer Code vs Architecture (§15-§23) | CRITICAL | `model-gateway/unified-chat-provider.ts stream()` | No AbortSignal / incremental budget deduction / partial response validation, architecture §15.4 requires streaming budget real-time control | Resolved | Original audit停留在 old model gateway request and streaming interface snapshot | Current code already has streaming budget control | No code change, just wrote evidence |
| R2-3 | 7. AI Operations Layer Code vs Architecture (§15-§23) | CRITICAL | `prompt-engine/prompt-injection-guard.ts` | PromptInjectionDefenseChain is single-layer regex, architecture §20.3 requires multi-layer chain orchestrator | Resolved | Prompt/Eval audit based on early implementation, subsequent multi-layer defense, graded sample gates and independent judge already in place | Current `prompt-injection-guard.ts` already not single-layer regex | No code change, just wrote evidence |
| R2-6 | 7. AI Operations Layer Code vs Architecture (§15-§23) | HIGH | `model-gateway/cost-tracker/budget-guard.ts` | BudgetPolicy only supports task-level budget, missing architecture §18 required platform/pack/step three-level budget hierarchy | Fixed | Budget hierarchy migrated from task-only to task/pack/platform, result types and legacy normalization not upgraded together, causing new scope to be swallowed at type boundary | Current `budget-guard.ts` already has pack/platform cascading budget, warning/violation scope and default normalization | Code fix retained |
| R2-7 | 7. AI Operations Layer Code vs Architecture (§15-§23) | HIGH | `model-gateway/cost-tracker/chargeback-service.ts` | ChargebackAllocation missing fx_rate/cost_source fields, architecture §18.7 requires multi-currency attribution | Resolved | Old audit停留在 chargeback contract initial migration stage, didn't absorb subsequent `fxRateToBase/costSource` implementation and multi-currency regression tests | Current `chargeback-service.ts` already includes `originalCurrency/baseCurrency/fxRateToBase/costSource` | No production code change, supplemented targeted test and wrote evidence |
| R2-13 | 8. Remaining Contract vs Architecture | HIGH | `runtime_state_machine_contract.md` | §6 ExecutionStatus 8-state machine conflicts with architecture §25.8 NodeRun 14-state lifecycle | Fixed | Contract docs previously followed execution/workflow/step/status legacy canonical, subsequently written back according to HarnessRun/NodeRun/PlanGraph main chain | Current contracts under `docs_zh/contracts/` switched to canonical naming | No code change, treated as contract rewrite issue |
| R3-1 | 11. Harness Runtime Deep Implementation Gaps (§45) | CRITICAL | `orchestration/harness/guardrail-engine.ts` | Guardrails only have policy/risk/tool/evidence/budget 5 layers; §45.20 requires Input/Planning/Tool/Memory/Output five layers | Resolved | Harness runtime entries from early snapshot, didn't reflect subsequent HITL, responsibility records, decision bundle, toolbelt six-step assembly, five-type recovery, memory governance and invariants closure | Current `src/platform/five-plane-orchestration/harness/` already includes inspect/patch/override/takeover/resume, HumanResponsibilityRecord, PromptExecutionRecord, DecisionInputBundle, downgrade_mode, six-step ToolbeltAssembler, five-type RecoveryController, memory namespace promotion/demotion and assertInvariants() 10-item verification | No code change, just wrote evidence |
| R3-25 | 12. Organization Governance + Scale Ecosystem Deep Gaps (§46-§57) | HIGH | `org-governance/knowledge-boundary/chinese-wall-access-saga.ts` | Missing §50.3 two-phase commit; only does simple pass/fail classification | Fixed (source code fix + targeted test) | Original implementation had real gap: `prepare_release` wrongly went to `prepareGrant` handler, prepare/commit handler exceptions didn't enter saga failure chain, rollback only generated based on committed commit action, causing prepare-only failures to lack failure reconciliation | This round supplemented prepare/commit try-catch, compatible with multiple prepare/commit stepId associations, generated compensating actions inversely from successful actions, converged `committedActions` to successful 2PC trace | Fixed production code, tests and documentary evidence |
| R3-35 | 13. Operations Maturity + SDK Gaps (§51-§69) | HIGH | `ops-maturity/agent-lifecycle/agent-registry/` | AgentLifecycleState missing removed state (§61.3 requires 9 states) | Fixed (source code fix + targeted test) | Main state machine actually already migrated to 9 states, but auxiliary semantics and batch of old tests still understood as 8-state/`archived` terminal state | `src/ops-maturity/agent-lifecycle/agent-registry/index.ts` confirmed 9 states with `archived→removed` and `paused→canary`; further corrected `isTerminalState(...)` to only `removed` as terminal state | Fixed code and tests, no gap retained |
| R3-37 | 13. Operations Maturity + SDK Gaps (§51-§69) | HIGH | `ops-maturity/edge-runtime/sync-queue/` | EdgeSyncEnvelope missing device_id/sequence_no/prev_hash/side_effect_dependency_refs/signature/local_time_offset | Fixed (source code fix + targeted test) | Queue envelope contract maintained minimal compatible form, key provenance fields停留在 optional/unenforced wiring state | This round converged `EdgeSyncEnvelope` to include canonical contract with `device_id/sequence_no/prev_hash/side_effect_dependency_refs/signature/local_time_offset/createdAt`, supplemented mapping in ordered sync path | Fixed code and tests, no gap retained |
| R3-47 | 13. Operations Maturity + SDK Gaps (§51-§69) | MEDIUM | `ops-maturity/platform-ops-agent/` | OpsAgentDefinition missing §69.1 ops_data_boundary declaration | Fixed (source code fix + targeted test) | Real gap existed: `ops_data_boundary` long停留在 optional field, no hard declaration, no runtime boundary validation | This round made `OpsAgentDefinition.ops_data_boundary` required, added `DEFAULT_OPS_DATA_BOUNDARY` and construction-time validation, prohibited `businessPayloadAllowed=true` | Fixed code and tests, no gap retained |
| R3-51 | 14. ADR and Architecture Conflicts | HIGH | `ADR-060` | Defines Plan DTO + RuntimeExecuteBridge as P3→P4 contract; §5.3 requires PlanGraphBundle as sole canonical P3→P4 handover | Fixed (document/ADR) | ADR-060 still残留 early "Plan DTO / Bridge 即 handoff" expression, not fully consistent with current executable contract | This round rewrote `docs_zh/adr/060-explicit-planning-hub.md`, clearly converged P3→P4 sole handover to `PlanGraphBundle`, downgraded `RuntimeExecuteBridge` to compatibility seam | Fixed ADR, no gap retained |
| R3-64 | 14. ADR and Architecture Conflicts | LOW | `ADR-019` | Claims source section §12 "Agent Handoff"; actually §12 is "Exception Event Handling Architecture" | Fixed (document/ADR) | ADR-019 source section indeed残留 erroneous section number reference | This round rewrote `docs_zh/adr/019-agent-handoff-four-layer-protocol.md`, removed erroneous `§12 Agent Handoff` reference, changed to handoff/explainability related canonical section description | Fixed ADR, no gap retained |
| R3-68 | 15. Remaining Contract Deep Gaps | HIGH | `explainability_and_stage_rationale_contract.md` | StageRationale only 7 fields; §59.3 requires 11 fields | Fixed (contract/document) | Contract doc partially migrated, still missing `rendered_explanation` and depth/governance description | This round rewrote `docs_zh/contracts/explainability_and_stage_rationale_contract.md`, supplemented authoritative field descriptions including `rationale_id/decision_input_ref/version_lock_ref/visibility_labels/confidence/alternatives/rendered_explanation` | Fixed contract document, no gap retained |
| R3-70 | 15. Remaining Contract Deep Gaps | HIGH | `startup_consistency_and_recovery_drill_contract.md` | Consistency matrix uses current_step_index/workflow_state; should be HarnessRun.status/NodeRun.status/PlanGraph | Fixed (contract/document) | Contract still残留 old workflow/step terminology, not fully switched to HarnessRun/NodeRun/PlanGraph consistency model | This round rewrote `docs_zh/contracts/startup_consistency_and_recovery_drill_contract.md`, changed inspection matrix to HarnessRun/NodeRun/PlanGraph semantics | Fixed contract document, no gap retained |
| R3-71 | 15. Remaining Contract Deep Gaps | MEDIUM | `budget-ledger-contract.md` | BudgetReservation.resourceKind enum missing storage/bandwidth/memory | Fixed (contract/document) | Budget contract migrated to ledger model, but `resourceKind` enum still missing 3 canonical resource categories | This round rewrote `docs_zh/contracts/budget-ledger-contract.md`, supplemented `storage / bandwidth / memory` | Fixed contract document, no gap retained |
| R3-72 | 15. Remaining Contract Deep Gaps | MEDIUM | `naming_and_engineering_boundary_contract.md` | §2 lists WorkflowExecutor as canonical engineering name; §5 canonical entry is HarnessRuntime | Fixed (contract/document) | Naming boundary contract still retained `WorkflowExecutor` as engineering canonical name | This round rewrote `docs_zh/contracts/naming_and_engineering_boundary_contract.md`, corrected canonical entry to `HarnessRuntime` | Fixed contract document, no gap retained |
| R3-74 | 15. Remaining Contract Deep Gaps | MEDIUM | `nl_entry_and_goal_decomposition_contract.md` | IntentParseResult contains suggested_workflow_id; §5 all execution is HarnessRun, NL should suggest domain/pack/recipe | Fixed (contract/document) | NL contract still残留 `suggested_workflow_id`, and doesn't clarify Goal only as upper-layer projection | This round rewrote `docs_zh/contracts/nl_entry_and_goal_decomposition_contract.md`, changed suggestion output to `division/domain/pack/recipe`, noted execution truth converges to `HarnessRun.status` | Fixed contract document, no gap retained |
| R6-45 | 32. Remaining Contract Batch Gaps | MEDIUM | `sla_tier_contract.md / quota_preemption / multimodal_gateway / org_hierarchy / feedback_improvement` | All less than 60 lines, missing ContractEnvelope compliance + remediation section | Fixed | 5 short contract documents indeed lacked remediation/canonical mandatory rules | This round directly supplemented `sla_tier`, `quota_preemption`, `multimodal_gateway`, `org_hierarchy`, `feedback_improvement` remediation, export/compatibility constraints and v4.3 mandatory rules | Fixed contract documents, no gap retained |

This document continues with the per-number conclusion table covering all unique numbers. Due to the document size (1.7MB), the complete table is available in the Chinese version at `docs_zh/reviews/platform-architecture-implementation-consistency-audit_round_reaudit.md`.

The reaudit confirmed that the majority of items marked as "RESOLVED" or "Fixed" in the original audit have been properly addressed through actual code fixes or documentary updates. Key findings:

1. **Contract Migration Complete**: Most contract documents have been migrated from legacy execution/workflow/step terminology to canonical HarnessRun/NodeRun/PlanGraph terminology
2. **Test Infrastructure Strengthened**: Multiple test suites now pass with proper assertions reflecting current implementation
3. **Real Implementation Gaps Fixed**: Several genuine implementation gaps were identified and fixed in areas like:
   - BudgetGuard multi-level hierarchy
   - Chinese-wall two-phase commit
   - Agent lifecycle 9-state machine
   - Edge sync envelope canonical fields
   - OpsAgent data boundary declaration
4. **ADR Documentation Aligned**: ADR documents with conflicts against architecture have been updated to reflect v4.3 canonical definitions
