# 2026-04-30 `platform-architecture-implementation-consistency-audit_round` Per-number Re-audit Results

This file is the per-number re-audit result of `docs_zh/reviews/platform-architecture-implementation-consistency-audit_round.md`.

Principles:

- Do not directly reuse historical long-table conclusions.
- Only write "fixed/resolved" when there is real source code, contract, ADR, configuration, or targeted test evidence.
- For items that do not modify code, clearly state the basis; do not fake fixes.
- Do not run full test suite.

Scale description:

- Original line items: 3490
- Unique numbers: 2640
- Duplicate numbers: 850
- Numbered up to `2604`, plus `R2-1` to `R32-55` style round numbers.
- Newly added unique numbers in this round: 1334.

Real issues actually fixed and targeted-verified in this round:

- `R2-6`: `BudgetGuard`'s pack/platform result type and legacy normalization gap.
- `R4-37`: `network-egress-policy` default `deny` mode did not actually block, and `audit_only` environment mode parsing drift.
- Stale test assertions in the autonomy/proactive trigger family: medium risk old assertions still required `auto_execute`; TrustScorer old assertions still fixed low-risk `fully_trusted` to `semi_auto`.
- `R5-49`: `KnowledgeQueryService` already had scope input but did not bind `accessPrincipal` into the service layer boundary check.
- `R22-31`: `TransactionalEventAppender` only wrapped event/outbox, no truth mutation hook, could not guarantee truth+event same transaction.
- `R22-47`: Recovery classification still exposed `L1/L2/L3` externally, not aligned with canonical `transient/permanent/unknown`; incidentally fixed several real misclassification priority issues.
- `R5-59`: `ADR-002` still had residual `VP orchestration layer/division` result descriptions, mixed with already-migrated DomainDescriptor/BusinessPack main line.
- `R5-63`: In the ADR source section batch, `ADR-002/004/028` still had old section numbers or old narrative residues, needed to supplement current canonical references.
- `2580`: Although `SqlExecutionMetricsProvider` had already changed incident count to be by failed status, `lastIncidentAt` and old tests still used `last_error_code` semantics, causing re-audit basis to continue drifting.
- `2575`: `DashboardWebSocketServer`'s metric routing previously only had the consumer side but no subscription registration side; this round completed the registration/update/cleanup chain.
- `2568`: Although `AutonomyGovernanceService` recognized `P0/P1`, the old severity branch discarded `promotion.targetLevel`, so high-severity incidents did not actually trigger recommended demotion; fixed in this round.
- `2585`: Old "fix" for `addWorkspaceMembership()` had no real caller context, only read back owner membership; this round wired `callerUserId` into service/CLI boundary and did real authorization.
- `2586`: Old `addOrganizationMembership()` had neither caller principal nor relied on non-existent `organization.ownerId`; this round changed to `createOrganization()` explicitly establishing owner membership, then used `callerUserId` for member management authorization.
- `R5-28`: `GoalDecompositionService` previously only exposed capability/permission boundary crossing, but task envelope did not produce final permission set narrowed by domain; this round supplemented the real task-level permission narrowing, preserving mismatch warnings.
- `R14-22`: Although old `authoritative-task-store-decorator` implementation no longer used global metrics Map, sync retry still relied on blocking wait as fallback; this round removed `SharedArrayBuffer`/blocking sleep, changed to instance-level metrics + async non-blocking backoff + sync immediate retry.
- `R15-02`: SDK version handshake test still treated `initialize()` as returning handshake result per old interface; this round wrote back to explicit `performVersionHandshake()`, and supplemented `R15-01/02/03` re-audit evidence.
- `R15-49/50/51/52/53/54/55/58/59/60/61/65/66/67/68/69`: Round 15's multi-region/resource manager/Marketplace/SLA residual items were previously conservatively kept as "not changed this round"; this round through current source code re-audit and supplemented `tests/unit/scale-ecosystem/reaudit-r15-multi-region-resource-manager-regressions.test.ts`, unified writing back as resolved: fencing epoch, failover reconciliation, write routing, cross-border compliance chain, multi-dimensional quota, promotion budget, tenant-scoped pool, etc.
- `2237`: `DurableEventBus.ensurePendingAcksForActiveConsumers()` previously created pending ack for active consumers not registered to the eventType, causing false backlog count; this round filtered by event registry.
- `2246`: `EvalRunService` previously treated `feedbackEnvelope.signals` directly as `actualEvidenceRefs`, mixing legacy reason/signal with evidence; this round changed to prioritize reading step `evidenceRefs`, only falling back to flat signals when legacy run has no step evidence.
- `2247`: `governance-projection` had already removed erroneous inference of unknown `decision*` events to `approval_granted`; this round's re-verification also found that the projection handler returned internal `_processedEventIdSet` directly without serializing back to `processedEventIds`, causing dedup state to not persist; this real implementation issue was also fixed.
- `2249/2250`: `access-model` now only trusts capabilities obtained through role inheritance, and converged `manualTakeoverActive` to a "record only and only operator-grade principals can use" context marker; this round also fixed the default-allow regression exposed in verification, restoring the "allow after capability and context check pass" normal path.
- `2251/2254/2255/2256`: OIDC path now prohibits falling back to mock user when `allowMockFallback=false`, uses `timingSafeEqual` to verify access token, limits concurrent sessions per user and implements `touchSession()` sliding expiration; this round supplemented corresponding targeted verification, and incidentally fixed PKCE helper's ESM `require()` runtime error.
- `2236/2239/2240`: `FeedbackSignal` now enforces `trustScore/evidenceRefs` aligned with canonical `harnessRunId/nodeRunId`; `workflow-run-projection` for `subtask:failed` only records failed step and does not immediately mark workflow as failed; projection dedup main chain has been uniformly switched to internal `Set` instead of `Array.includes()`.
- `2257/2258/2259`: `PolicyEngine`'s `full-auto` no longer bypasses high-risk promotion; `VaultHttpSecretProvider` no longer defaults to caching AppRole tokens missing `lease_duration` for 1 hour; `DataClassificationService` rejects unsafe regex at runtime to prevent user-defined patterns triggering ReDoS.
- `R7-42/R7-43/R7-47` and `2292/2293/2295/2296/2298/2300`: HTTP API main chain previously had idempotency, version negotiation, trace propagation, and task creation fact append scattered in local components, not really wired to request entry; this round converged to `HttpApiServer` main chain and supplemented task creation truth+event same transaction.
- `2278-2291`: Round 39's CLI/SDK continuous items have all been re-audited; among them, `migrate-sqlite-to-pg`, `api-server`, `harness-sdk`, `admin-sdk`, `billing`, `shadow-snapshot`, `data-plane`, `client-sdk` are mostly re-audit conclusion lag, while `dlq-manager` and `login` had real gaps; this round supplemented transaction/audit/archiving/schema alignment respectively, and a real executable PKCE OAuth CLI entry and ESM safe persistence.
- `2302`: Although `ServiceRegistry` already had `createScoped()`, the top-level bootstrap/runtime catalog/runtime orchestrator/root summary default entry was still implicitly bound to `getInstance()`; this round switched all these production assembly defaults to `createScoped()`, only sharing when explicitly passed a registry.
- `2203`: The real implementation of `remote-session-state` had long since converged to `connecting` when receiving `connected` signal after failure, but the re-audit items and corresponding unit tests still stayed on the old bug narrative; this round wrote back to the "must reconnect handshake first" current semantics, and fixed the invalid test import paths.
- `2192/2193/2195/2196/2197/2198/2199/2204`: In Round 37's multi-tenant/multi-region early items, some were actually fixed by later implementation but not written back, while `2194` still had a real timed flush bypass; this round closed the timed flush back to the `DataReplicatorService.flush()` main chain, and supplemented targeted evidence for boundary/latency/capacity/queue/sequence number.
- `R11-35`: `prompt-routes` POST path had long used strong-typed schema, but `PUT /deprecate` in the same file still had residual `readJsonBody(...) as Record<string, unknown>` old branch; this round supplemented independent deprecation schema, and incidentally re-wrote the old conclusions of "boundary still bare parse/as any" in `R11-32/33/34/36` according to current state.
- `R11-41/R11-44`: This round continued cleaning up Round 11's residual real type erasure points, respectively removed the implicit unsafe assertions on `elementId/interactionType` in `UxEventTrackingService`, and the `(runner as any)(runnerArgs as any)` erasure on runner signature in `stable-runner-factory`; simultaneously re-wrote `R11-37/38/39/40/42/43` according to current source code as "original accusation outdated".
- `R6-45`: 5 short contract documents indeed previously lacked remediation/canonical enforcement rules, and multiple were under 60 lines; this round directly supplemented `sla_tier`, `quota_preemption`, `multimodal_gateway`, `org_hierarchy`, `feedback_improvement` remediation, export/compatibility constraints, and v4.3 enforcement rules.
- `R14-01`: `platform-panic` is not "only panic_stop string enum", but `cascadeHalt()` returned old snapshot, causing propagation result to be inconsistent with internal halting log; this round fixed the return chain and supplemented cascade/ack/resume targeted verification.
- `R16-42`: `ConflictResolver` is no longer shallow spread, but array items with same `id` conflict would still solidify server old value; this round supplemented item-level CRDT merge and Lamport fallback.
- `R20-03`: `WorkerDrainProtocol` already has drain state, but checkpoint/lease release/recovery paths were left as stubs; this round connected collaboration dependencies and let `TERMINATE` phase actually execute forced handoff.
- `R20-44/45/46/49`: Respectively supplemented side-effect registration/pre-commit lifecycle, workflow pre-execution 5 categories of static analysis, `StaticCompatibilityIssue[]` compatible export, and pre-release quality gate.
- `R25-03/04/05/06/07/08/10/12/13`: This round continued cleaning up Platform Interface/API/Channel Gateway real gaps, respectively fixing gateway target error normalization order, standardized error response `traceId/details`, webhook nonce entropy truncation, delivery receipt false success, gateway rate limit tenant isolation, WebSocket server heartbeat, WS `maxPayload`, API rate-limit key path parameter normalization, and body read bypassing request timeout protection.
- `R25-01/02/09/11`: This group of re-audit items continued using old snapshots; this round after supplementing source code and targeted tests confirmed: `matchRoute` already supports `PUT/PATCH/DELETE`, CORS default is already fail-closed and rejects wildcard+credentials, `StreamBridge` replay buffer is already bounded, `WebSocketBridge` has also wired tenant scope filter on both subscription and broadcast sides.

Targeted verification:

- `node --test --import tsx tests/unit/platform/five-plane-control-plane/iam/network-egress-policy.test.ts tests/unit/platform/model-gateway/cost-tracker/budget-guard.test.ts tests/unit/platform/model-gateway/cost-tracker/budget-guard-types.test.ts tests/unit/interaction/autonomy/trust-scorer/index.test.ts tests/unit/interaction/autonomy/promotion-engine/index.test.ts tests/unit/interaction/proactive-agent/trigger-engine/index.test.ts tests/unit/interaction/proactive-agent/trigger-engine/trigger-engine.test.ts tests/unit/interaction/proactive-agent/trigger-engine/resolve-trigger-action-mode.unit.test.ts tests/unit/interaction/proactive-agent/trigger-engine.test.ts tests/unit/platform/interaction/proactive-agent/trigger-engine.test.ts`
- Result: `152 passed / 0 failed / 0 skipped`
- `node --test --import tsx tests/unit/platform/five-plane-state-evidence/knowledge/knowledge-query-boundary.test.ts tests/unit/platform/five-plane-state-evidence/events/transactional-event-appender.test.ts tests/unit/platform/five-plane-execution/recovery/failure-classification.test.ts`
- Result: `62 passed / 0 failed / 0 skipped`
- `node --test --import tsx tests/unit/interaction/goal-decomposer/task-domain-capability-policy.test.ts tests/unit/platform/five-plane-orchestration/harness/evaluation/eval-run-service.test.ts tests/unit/platform/five-plane-state-evidence/events/durable-event-bus-consumer-filter.test.ts`
- Result: `10 passed / 0 failed / 0 skipped`
- `node --test --import tsx tests/unit/interaction/goal-decomposer/task-domain-capability-policy.test.ts tests/unit/platform/five-plane-orchestration/harness/evaluation/eval-run-service.test.ts tests/unit/platform/five-plane-state-evidence/events/durable-event-bus-consumer-filter.test.ts tests/unit/platform/five-plane-state-evidence/events/dlq-service.test.ts tests/unit/platform/five-plane-state-evidence/events/durable-event-bus-no-handler.test.ts tests/unit/platform/five-plane-state-evidence/events/layered-event-inbox-compaction.test.ts tests/unit/platform/five-plane-orchestration/oapeflir/assessment-service-extended.test.ts tests/unit/platform/five-plane-state-evidence/events/projections/governance-projection.test.ts tests/unit/platform/five-plane-control-plane/iam/access-model-authorization.test.ts tests/unit/platform/five-plane-control-plane/iam/iam-access-model-extended.test.ts tests/unit/platform/five-plane-control-plane/iam/field-encryption-extended.test.ts tests/unit/platform/five-plane-control-plane/iam/secret-management-service.test.ts tests/unit/platform/five-plane-control-plane/iam/audit-integrity-repository.test.ts tests/unit/org-governance/sso-scim/oidc/oidc-service.test.ts tests/unit/org-governance/sso-scim/scim-sync/scim-service.test.ts tests/unit/org-governance/sso-scim/group-role-mapping-service.test.ts`
- Result: `395 passed / 0 failed / 0 skipped`
- `node --test --import tsx tests/unit/platform/five-plane-state-evidence/events/durable-event-bus-no-handler.test.ts tests/unit/platform/five-plane-state-evidence/events/layered-event-inbox-compaction.test.ts`
- Result: `3 passed / 0 failed / 0 skipped`
- `node --test --import tsx tests/unit/platform/five-plane-orchestration/oapeflir/types/feedback-signal.test.ts tests/unit/platform/five-plane-orchestration/oapeflir/feedback-signal.test.ts tests/unit/platform/five-plane-state-evidence/events/projections/workflow-run-projection.test.ts tests/unit/platform/five-plane-state-evidence/events/projections/workflow-run-projection-subtask-failure-regression.test.ts tests/unit/platform/five-plane-control-plane/iam/policy-engine.test.ts tests/unit/platform/five-plane-control-plane/iam/policy-engine-edge-cases.test.ts tests/unit/platform/five-plane-control-plane/iam/vault-http-secret-provider.test.ts tests/unit/platform/five-plane-control-plane/iam/data-classification-service-regex-safety.test.ts`
- Result: `149 passed / 0 failed / 0 skipped`
- `node --test --import tsx tests/integration/platform/five-plane-interface/api/http-api-server-architecture-regressions.test.ts tests/unit/platform/five-plane-interface/api/middleware/idempotency-key.test.ts tests/unit/platform/five-plane-interface/api/http-server/request-helpers.test.ts`
- Result: `47 passed / 0 failed / 0 skipped`
- `node --test --import tsx tests/unit/sdk/cli/migrate-sqlite-to-pg.test.ts tests/integration/sdk/migrate-sqlite-to-pg-integration-2278-2279.test.ts tests/unit/sdk/cli/migrate-sqlite-to-pg-2288-transaction.test.ts tests/unit/sdk/cli/api-server-source-2280-2281.test.ts tests/unit/sdk/client-sdk/cursor-2291-schema-validation.test.ts tests/unit/sdk/cli/dlq-manager-operations-2282-2283.test.ts tests/integration/sdk/dlq-manager-architecture-regressions-2282-2283.test.ts`
- Result: `30 passed / 0 failed / 0 skipped`
- `node --test --import tsx tests/unit/sdk/harness-sdk/deterministic-replay-2284.test.ts tests/unit/sdk/admin-sdk/bulk-ops-2285.test.ts tests/unit/sdk/cli/login-2286.test.ts tests/unit/sdk/cli/source-review-2287-2289-2290.test.ts`
- Result: `9 passed / 0 failed / 0 skipped`
- `node --test --import tsx tests/unit/platform/five-plane-orchestration/oapeflir/di-source-review-2294.test.ts tests/unit/platform/five-plane-state-evidence/events/durable-event-bus-backpressure-2297.test.ts`
- Result: `2 passed / 0 failed / 0 skipped`
- `./node_modules/.bin/tsx --test tests/unit/platform/shared/service-registry-default-scope-2302.test.ts tests/unit/platform/five-plane-runtime-bootstrap.test.ts tests/unit/platform/five-plane-runtime-orchestrator.test.ts tests/unit/domains-runtime-orchestrator.test.ts tests/unit/interaction-governance-runtime-orchestrator.test.ts tests/unit/scale-ops-runtime-orchestrator.test.ts tests/unit/platform/ai-operations/runtime-orchestrator.test.ts`
- Result: `65 passed / 0 failed / 0 skipped`
- `./node_modules/.bin/tsx --test tests/unit/scale-ecosystem/multi-region/remote-session-state.test.ts`
- Result: `10 passed / 0 failed / 0 skipped`
- `./node_modules/.bin/tsx --test tests/unit/scale-ecosystem/multi-region/round37-multi-region-regressions-2193-2198.test.ts tests/unit/scale-ecosystem/multi-region/data-replicator/index.test.ts tests/unit/scale-ecosystem/multi-region/region-health-check-service.test.ts tests/integration/platform/security/data-plane-flow-boundary.test.ts tests/unit/scale-ecosystem/multi-region/remote-session-state.test.ts`
- Result: `44 passed / 0 failed / 0 skipped`
- `./node_modules/.bin/tsx --test tests/unit/platform/five-plane-orchestration/reaudit-r23-harness-learn-regressions.test.ts tests/unit/platform/reaudit-r23-security-20-32.test.ts tests/unit/scale-ecosystem/reaudit-r15-multi-region-resource-manager-regressions.test.ts tests/unit/scale-ecosystem/marketplace/plugin-trust-store.test.ts`
- Result: `38 passed / 0 failed / 0 skipped`
- `./node_modules/.bin/tsx --test tests/unit/platform/reaudit-batch-r24-13-28.test.ts`
- Result: `9 passed / 0 failed / 0 skipped`
- `./node_modules/.bin/tsx --test tests/unit/platform/reaudit-batch-r24-14-31-60-71.test.ts`
- Result: `9 passed / 0 failed / 0 skipped`
- `./node_modules/.bin/tsx --test tests/unit/platform/five-plane-state-evidence/memory/memory-layer-model-functions.test.ts tests/unit/scale-ecosystem/resource-manager/preemption/index.test.ts`
- Result: `76 passed / 0 failed / 0 skipped`
- `./node_modules/.bin/tsx --test tests/e2e/workflow-state-transitions.test.ts`
- Result: `6 passed / 0 failed / 0 skipped`
- `./node_modules/.bin/tsx --test --test-name-pattern "persists pending queues and keeps failed batch retryable across restart" tests/unit/scale-ecosystem/multi-region/cdc-replication-service.test.ts`
- Result: `1 passed / 0 failed / 0 skipped`
- `./node_modules/.bin/tsx --test tests/unit/platform/five-plane-interface/api/http-server/prompt-routes.test.ts tests/unit/platform/five-plane-interface/channel-gateway/stream-bridge.test.ts tests/unit/platform/five-plane-interface/channel-gateway/channel-gateway-delivery-service.test.ts tests/unit/platform/five-plane-execution/lease/execution-lease-service-async-r9.test.ts tests/unit/platform/five-plane-execution/lease/execution-lease-service-async.test.ts`
- Result: `63 passed / 0 failed / 0 skipped`
- `./node_modules/.bin/tsx --test tests/unit/sdk/cli/stable-runner-factory.test.ts tests/unit/interaction/ux/ux-event-tracking-service.test.ts tests/unit/interaction/ux/ux-event-tracking-service-edge-cases.test.ts tests/unit/platform/interaction/ux/ux-event-tracking-service.test.ts`
- Result: `94 passed / 0 failed / 0 skipped`
- `./node_modules/.bin/tsx --test tests/unit/platform/five-plane-execution/worker-drain-protocol.test.ts tests/unit/platform/five-plane-execution/side-effect-manager.test.ts tests/unit/platform/five-plane-orchestration/oapeflir/workflow/workflow-validator.test.ts tests/unit/platform/prompt-engine/eval/post-execution-quality-gate.test.ts tests/unit/platform/ops-maturity/platform-panic/panic-propagation-service.test.ts`
- Result: `65 passed / 0 failed / 0 skipped`
- `./node_modules/.bin/tsx --test tests/unit/ui/shared/sync/conflict-resolver.test.ts`
- Result: `4 passed / 0 failed / 0 skipped`
- `cd ui && ./node_modules/.bin/vitest run tests/shared/reaudit-r24-ui-foundations.test.tsx tests/reaudit-r24-ui-source-review.test.ts tests/tools/tooling.test.ts`
- Result: `13 passed / 0 failed / 0 skipped`
- `./node_modules/.bin/tsx --test tests/unit/platform/reaudit-batch-r25-01-13.test.ts tests/unit/platform/five-plane-interface/api/http-server/api-error.test.ts tests/unit/platform/five-plane-interface/api/http-server/request-helpers.test.ts tests/unit/platform/five-plane-interface/api/http-server/response-hardening.test.ts tests/unit/platform/five-plane-interface/channel-gateway/channel-gateway-delivery-service.test.ts tests/unit/platform/five-plane-interface/channel-gateway/websocket-bridge-coverage.test.ts`
- Result: `124 passed / 0 failed / 0 skipped`
- `npm run build`
- Result: passes; at the same time exposed and converged the signature drift where `stable-gate.ts` and `stable-package.ts` explicitly write `undefined` to optional fields under `exactOptionalPropertyTypes`.

## Per-number Conclusion Table (by unique number)

| Number | Original Section | Severity | Location | Original Issue | Current Conclusion | Root Cause | Basis | This Round Action |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| R2-1 | 7. AI Operations Layer Code vs Architecture (§15-§23) | CRITICAL | `model-gateway/unified-chat-provider.ts` | ChatCompletionRequest missing required traceId/tenantId/costTag fields, architecture §15.2 explicitly requires | Resolved | Original audit stayed on old version model gateway request and streaming interface snapshot, did not reflect subsequent context, budget and audit propagation. | Current `src/platform/model-gateway/provider-registry/unified-chat-provider.ts` contains `traceId`, `tenantId`, `costTag`, `abortSignal`, `validatePartialChunk`, `streamingBudgetTrack`, `policyOutcome`, and back-fills trace. | No code change, only write back basis. |
| R2-2 | 7. AI Operations Layer Code vs Architecture (§15-§23) | CRITICAL | `model-gateway/unified-chat-provider.ts stream()` | No AbortSignal / incremental budget deduction / partial response validation, architecture §15.4 requires real-time streaming budget control | Resolved | Original audit stayed on old version model gateway request and streaming interface snapshot, did not reflect subsequent context, budget and audit propagation. | Current `src/platform/model-gateway/provider-registry/unified-chat-provider.ts` contains `traceId`, `tenantId`, `costTag`, `abortSignal`, `validatePartialChunk`, `streamingBudgetTrack`, `policyOutcome`, and back-fills trace. | No code change, only write back basis. |
| R2-3 | 7. AI Operations Layer Code vs Architecture (§15-§23) | CRITICAL | `prompt-engine/prompt-injection-guard.ts` | PromptInjectionDefenseChain is single-layer regex, no multi-layer chain orchestrator required by architecture §20.3 (regex->classifier->LLM judge) | Resolved | Prompt/Eval audit was based on early implementation; subsequent multi-layer protection, tiered sample gates, and independent judges have landed, but historical audit was not cleared. | Current `src/platform/prompt-engine/prompt-injection-guard.ts` is no longer single-layer regex, `src/platform/prompt-engine/eval/eval-dataset-judge-service.ts` has implemented risk sample floor and hard gate, `cross-provider-judge-service.ts` exists. | No code change, only write back basis. |
| R2-4 | 7. AI Operations Layer Code vs Architecture (§15-§23) | CRITICAL | `prompt-engine/eval/` | EvalDataset has no minimum sample count validation by risk level, architecture §21.5 requires critical>=200/high>=100/medium>=50 | Resolved | Prompt/Eval audit was based on early implementation; subsequent multi-layer protection, tiered sample gates, and independent judges have landed, but historical audit was not cleared. | `src/platform/prompt-engine/eval/eval-dataset-judge-service.ts` has implemented `EVAL_RISK_LEVEL_MIN_SAMPLES` constant (critical>=200/high>=100/medium>=50/standard>=20) and hard gate validation in `registerDataset()`; datasets that do not meet minimum sample requirements throw `ValidationError` on registration. | No code change, update basis column pointer. |
| R2-5 | 7. AI Operations Layer Code vs Architecture (§15-§23) | CRITICAL | `plugins/builtin-plugin-registry.ts` | Plugin system has no DataTaintPropagation tracking, architecture §23.4 requires cross-plugin data taint label propagation | Resolved | Plugin governance items are from intermediate snapshots; registry and SDK have since supplemented taint propagation, revocation grading, and recursion depth tracking. | Current `src/plugins/builtin-plugin-registry.ts` has `DataTaintPropagation` and `BundleRevocationSeverity`; `src/sdk/plugin-sdk/plugin-context.ts` has `callDepth/delegationDepth`. | No code change, only write back basis. |
| R2-6 | 7. AI Operations Layer Code vs Architecture (§15-§23) | HIGH | `model-gateway/cost-tracker/budget-guard.ts` | BudgetPolicy only supports task-level budget, missing platform/pack/step three-level budget hierarchy required by architecture §18 | Resolved (source code fix and targeted test supplement) | Root cause has two layers: first, the old `BudgetGuard` only had task cost judgment, pack/platform cascade budget and step constraint semantics were incomplete; second, although `evaluateStepConstraints()` was later supplemented, it was not connected to the real model execution entry, and `maxSteps/maxDurationMs/maxModelTokens` still stayed at "will compute, will not block". | Current `src/platform/model-gateway/cost-tracker/budget-guard.ts` uniformly provides task/pack/platform/daily/monthly cascade budget and `evaluateStepConstraints()`; this round also connects `src/platform/five-plane-execution/execution-engine/model-call-provider.ts` to real pre-execution gate and timeout abort, `createCompletion/createStreamingCompletion` will now reject `maxSteps/maxModelTokens` overflow before execution, and use `AbortSignal` + elapsed check to enforce `maxDurationMs` during execution. Targeted test `tests/unit/platform/five-plane-execution/execution-engine/model-call-provider.test.ts` has added 3 regression lines, verifying all three constraints will be actually intercepted. | Closes the gap of "three-level budget and step/token/duration constraints not actually enforced", does not extrapolate to all upper-layer product flows having custom-configured budget policy. |
| R2-7 | 7. AI Operations Layer Code vs Architecture (§15-§23) | HIGH | `model-gateway/cost-tracker/chargeback-service.ts` | ChargebackAllocation missing fx_rate/cost_source fields, architecture §18.7 requires multi-currency attribution | Resolved | Old audit stayed at chargeback contract initial migration stage, did not absorb subsequent `fxRateToBase/costSource` implementation and multi-currency regression tests. | Current `src/platform/model-gateway/cost-tracker/chargeback-service.ts` contains `originalCurrency/baseCurrency/fxRateToBase/costSource`; `tests/unit/platform/model-gateway/cost-tracker/chargeback-service.test.ts` and `tests/integration/platform/model-gateway/chargeback-service-integration.test.ts` have verified FX and source attribution. | No production code change, run targeted tests and write back basis. |
| R2-8 | 7. AI Operations Layer Code vs Architecture (§15-§23) | HIGH | `prompt-engine/registry/` | Prompt lifecycle missing deprecated stage, architecture §20.6 defines draft->active->deprecated->archived four stages | Resolved | Prompt/Eval audit was based on early implementation; subsequent multi-layer protection, tiered sample gates, and independent judges have landed, but historical audit was not cleared. | Current `src/platform/prompt-engine/prompt-injection-guard.ts` is no longer single-layer regex, `src/platform/prompt-engine/eval/eval-dataset-judge-service.ts` has implemented risk sample floor and hard gate, `cross-provider-judge-service.ts` exists. | No code change, only write back basis. |
| R2-9 | 7. AI Operations Layer Code vs Architecture (§15-§23) | HIGH | `plugins/builtin-plugin-registry.ts` | No BundleRevocationSeverity mechanism, architecture §23.6 requires plugin revocation severity grading | Resolved | Plugin governance items are from intermediate snapshots; registry and SDK have since supplemented taint propagation, revocation grading, and recursion depth tracking. | Current `src/plugins/builtin-plugin-registry.ts` has `DataTaintPropagation` and `BundleRevocationSeverity`; `src/sdk/plugin-sdk/plugin-context.ts` has `callDepth/delegationDepth`. | No code change, only write back basis. |
| R2-10 | 7. AI Operations Layer Code vs Architecture (§15-§23) | HIGH | `prompt-engine/eval/` | LLM-as-Judge has no risk-level-based independence enforcement (high risk requires external independent review), architecture §21.7 explicitly requires | Fixed (source code) | Old audit only identified the lack of independence enforcement in historical snapshots, did not reflect subsequent implementation state. This round supplemented the strong independence verification required by §21.7: `CiGateOptions` adds `enforceIndependenceForHighRisk` and `independentJudgeId` fields; `runCiGate()` when detecting critical/high risk cases without configured independent judge, will force failure and set `independenceViolationReason`. `eval-dataset-judge-service.ts`'s `resolveJudge()` has implemented cross-entity independence verification of judge and candidate provider from different families, different providers. | Current `src/platform/prompt-engine/eval/llm-eval-service.ts`'s `CiGateOptions` (lines 167-181) adds `enforceIndependenceForHighRisk` and `independentJudgeId`; `runCiGate()` (lines 661-693) when detecting critical/high risk cases force-verifies independent judge configuration, otherwise marks `independenceViolation` and makes `passed=false`; `eval-dataset-judge-service.ts`'s `EvalDatasetEvaluationInput` (lines 143-160) adds `enforceIndependenceForHighRisk`; `evaluateDataset()` (lines 415-420) when flag is true and there are critical/high priority cases but no independent judge, appends `independence_violation:high_risk_evaluation_requires_independent_judge` to `blockingFindings`; `resolveJudge()` (lines 435-468) has implemented cross-entity independence verification of judge and candidate provider from different families, different providers; `cross-provider-judge-service.ts` has implemented multi-judge consensus evaluation. | §21.7 independence enforcement is implemented, complete implementation state recorded. |
| R2-11 | 7. AI Operations Layer Code vs Architecture (§15-§23) | HIGH | `plugins/ PluginContext` | No call_depth/delegation_depth tracking, architecture §23.2 requires preventing infinite recursive plugin delegation | Resolved | Plugin governance items are from intermediate snapshots; registry and SDK have since supplemented taint propagation, revocation grading, and recursion depth tracking. | Current `src/plugins/builtin-plugin-registry.ts` has `DataTaintPropagation` and `BundleRevocationSeverity`; `src/sdk/plugin-sdk/plugin-context.ts` has `callDepth/delegationDepth`. | No code change, only write back basis. |
| R2-12 | 7. AI Operations Layer Code vs Architecture (§15-§23) | HIGH | `prompt-engine/eval/` | §21.5 critical_case_pass==100% hard gate implemented: eval-dataset-judge-service.ts's requireCriticalPass policy defaults to enabled, when criticalPassRate<1 gateDecision is hold/rollback; post-execution-quality-gate.ts's decide() returns releaseStage:"blocked" when criticalPassRate<1. Architecture compliant. | Resolved | Prompt/Eval audit was based on early implementation; subsequent multi-layer protection, tiered sample gates, and independent judges have landed, but historical audit was not cleared. | Current `src/platform/prompt-engine/prompt-injection-guard.ts` is no longer single-layer regex, `src/platform/prompt-engine/eval/eval-dataset-judge-service.ts` has implemented risk sample floor and hard gate, `cross-provider-judge-service.ts` exists, `src/platform/prompt-engine/eval/post-execution-quality-gate.ts`'s `PostExecutionQualityGate.decide()` returns releaseStage:"blocked" when criticalPassRate<1. | No code change, issue description corrected to accurately reflect implementation state. |
| R2-13 | 8. Remaining Contract vs Architecture | HIGH | `runtime_state_machine_contract.md` | §6 ExecutionStatus 8-state machine conflicts with architecture §25.8 NodeRun 14-state lifecycle, missing admitted/planning/ready/pausing/replanning/compensating | Fixed | contract documents once used legacy canonical like execution/workflow/step/status, later written back per HarnessRun/NodeRun/PlanGraph main chain. | Current `docs_zh/contracts/` under `runtime_state_machine_contract.md`, `cost_and_budget_contract.md`, `task_and_workflow_contract.md`, `policy_engine_contract.md`, `execution_plane_contract.md`, `observability_contract.md` etc. have switched to canonical naming. | No code change, as contract write-back problem. |
| R2-14 | 8. Remaining Contract vs Architecture | HIGH | `runtime_state_machine_contract.md` | §3 WorkflowStatus 7-state missing architecture's 13-state HarnessRun's created/admitted/planning/ready/pausing/replanning/compensating/aborted | Fixed | contract documents once used legacy canonical like execution/workflow/step/status, later written back per HarnessRun/NodeRun/PlanGraph main chain. | Current `docs_zh/contracts/` under `runtime_state_machine_contract.md`, `cost_and_budget_contract.md`, `task_and_workflow_contract.md`, `policy_engine_contract.md`, `execution_plane_contract.md`, `observability_contract.md` etc. have switched to canonical naming. | No code change, as contract write-back problem. |
| R2-15 | 8. Remaining Contract vs Architecture | HIGH | `cost_and_budget_contract.md` | §4 CostEvent uses task_id as required but harness_run_id is optional, architecture §18 uses HarnessRun as budget subject | Fixed | contract documents once used legacy canonical like execution/workflow/step/status, later written back per HarnessRun/NodeRun/PlanGraph main chain. | Current `docs_zh/contracts/` under `runtime_state_machine_contract.md`, `cost_and_budget_contract.md`, `task_and_workflow_contract.md`, `policy_engine_contract.md`, `execution_plane_contract.md`, `observability_contract.md` etc. have switched to canonical naming. | No code change, as contract write-back problem. |
| R2-16 | 8. Remaining Contract vs Architecture | HIGH | `cost_and_budget_contract.md` | §7.4 implicit cost attribution still uses deprecated execution_id, should be node_run_id/attempt_id | Fixed | contract documents once used legacy canonical like execution/workflow/step/status, later written back per HarnessRun/NodeRun/PlanGraph main chain. | Current `docs_zh/contracts/` under `runtime_state_machine_contract.md`, `cost_and_budget_contract.md`, `task_and_workflow_contract.md`, `policy_engine_contract.md`, `execution_plane_contract.md`, `observability_contract.md` etc. have switched to canonical naming. | No code change, as contract write-back problem. |
| R2-17 | 8. Remaining Contract vs Architecture | MEDIUM | `cost_and_budget_contract.md` | §4 CostEvent missing budget_reservation_id, architecture §18.3 requires reserve-before-execute linkage | Fixed | contract documents once used legacy canonical like execution/workflow/step/status, later written back per HarnessRun/NodeRun/PlanGraph main chain. | Current `docs_zh/contracts/` under `runtime_state_machine_contract.md`, `cost_and_budget_contract.md`, `task_and_workflow_contract.md`, `policy_engine_contract.md`, `execution_plane_contract.md`, `observability_contract.md` etc. have switched to canonical naming. | No code change, as contract write-back problem. |
| R2-18 | 8. Remaining Contract vs Architecture | MEDIUM | `task_and_workflow_contract.md` | §6-§7 WorkflowStep/StepOutput use step_id as primary key, should be node_run_id | Fixed | contract documents once used legacy canonical like execution/workflow/step/status, later written back per HarnessRun/NodeRun/PlanGraph main chain. | Current `docs_zh/contracts/` under `runtime_state_machine_contract.md`, `cost_and_budget_contract.md`, `task_and_workflow_contract.md`, `policy_engine_contract.md`, `execution_plane_contract.md`, `observability_contract.md` etc. have switched to canonical naming. | No code change, as contract write-back problem. |
| R2-19 | 8. Remaining Contract vs Architecture | MEDIUM | `policy_engine_contract.md` | §3.1 PolicyDecisionRequest uses deprecated execution_id | Fixed | contract documents once used legacy canonical like execution/workflow/step/status, later written back per HarnessRun/NodeRun/PlanGraph main chain. | Current `docs_zh/contracts/` under `runtime_state_machine_contract.md`, `cost_and_budget_contract.md`, `task_and_workflow_contract.md`, `policy_engine_contract.md`, `execution_plane_contract.md`, `observability_contract.md` etc. have switched to canonical naming. | No code change, as contract write-back problem. |
| R2-20 | 8. Remaining Contract vs Architecture | MEDIUM | `execution_plane_contract.md` | §8 ExecutionTicket isolation_level uses deprecated standard/hardened/strict, should be read_only/workspace_write/scoped_external_access/restricted_exec | Fixed | contract documents once used legacy canonical like execution/workflow/step/status, later written back per HarnessRun/NodeRun/PlanGraph main chain. | Current `docs_zh/contracts/` under `runtime_state_machine_contract.md`, `cost_and_budget_contract.md`, `task_and_workflow_contract.md`, `policy_engine_contract.md`, `execution_plane_contract.md`, `observability_contract.md` etc. have switched to canonical naming. | No code change, as contract write-back problem. |
| R2-21 | 8. Remaining Contract vs Architecture | MEDIUM | `model_gateway_routing_contract.md` | ModelRouteRequest missing harness_run_id/node_run_id, cannot satisfy INV-BUDGET-001 budget gate | Fixed | contract documents once used legacy canonical like execution/workflow/step/status, later written back per HarnessRun/NodeRun/PlanGraph main chain. | Current `docs_zh/contracts/` under `runtime_state_machine_contract.md`, `cost_and_budget_contract.md`, `task_and_workflow_contract.md`, `policy_engine_contract.md`, `execution_plane_contract.md`, `observability_contract.md` etc. have switched to canonical naming. | No code change, as contract write-back problem. |
| R2-22 | 8. Remaining Contract vs Architecture | MEDIUM | `observability_contract.md` | §3 LogEvent missing required harness_run_id/node_run_id fields | Fixed | contract documents once used legacy canonical like execution/workflow/step/status, later written back per HarnessRun/NodeRun/PlanGraph main chain. | Current `docs_zh/contracts/` under `runtime_state_machine_contract.md`, `cost_and_budget_contract.md`, `task_and_workflow_contract.md`, `policy_engine_contract.md`, `execution_plane_contract.md`, `observability_contract.md` etc. have switched to canonical naming. | No code change, as contract write-back problem. |
| R2-23 | 8. Remaining Contract vs Architecture | LOW | `plugin_spi_contract.md vs tool_skill_plugin_contract.md` | Lifecycle hook names contradict each other (initialize/activate vs onLoad/onActivate) | Fixed | `plugin_spi_contract.md` originally used `initialize/activate/suspend/deactivate` as lifecycle hook names on the Plugin interface, contradicting the `onLoad/onActivate/onDeactivate/onUnload` used in `tool_skill_plugin_contract.md`. | All Plugin SPI interfaces (`DomainRetrieverPlugin/DomainValidatorPlugin/DomainPlannerPlugin/DomainPresenterPlugin`) in current `plugin_spi_contract.md` uniformly use canonical hook names `onLoad / onActivate / onDeactivate / onUnload`, fully aligned with `tool_skill_plugin_contract.md`. PluginSpiRegistry's `initialize/activate/suspend/deactivate/unload` methods are registry-level operations, with explicit comments "call plugin.onLoad" etc., alignment notes are in the document. | No code change, only update audit doc root cause/basis description to accurately reflect actual fix. |
| R2-24 | 8. Remaining Contract vs Architecture | MEDIUM | `runtime_state_machine_contract.md` | Uses ExecutionStatus name instead of canonical NodeRun.status | Fixed | contract documents once used legacy canonical like execution/workflow/step/status, later written back per HarnessRun/NodeRun/PlanGraph main chain. | Current `docs_zh/contracts/` under `runtime_state_machine_contract.md`, `cost_and_budget_contract.md`, `task_and_workflow_contract.md`, `policy_engine_contract.md`, `execution_plane_contract.md`, `observability_contract.md` etc. have switched to canonical naming. | No code change, as contract write-back problem. |
| R2-25 | 9. Architecture Documentation Internal Consistency | HIGH | `§45.13 vs §25.8` | HarnessRun state count contradiction: §45.13 defines 6 states, §25.8 defines 13 states | Refuted (contradiction does not exist) | Verified, §45.13 line 6758 `status` actually lists 13 states (created/admitted/planning/ready/running/pausing/paused/resuming/replanning/compensating/completed/failed/aborted), fully consistent with §25.8 line 2042. Contradiction does not hold. | No code change, no fix needed. |
| R2-26 | 9. Architecture Documentation Internal Consistency | HIGH | `§45.13 vs §58.6` | finalDecision value contradiction: §45.13 allows 4 values, §58.6 HarnessDecision lists 6 values | Refuted (contradiction does not exist) | Verified, §45.13 line 6757 `finalDecision` actually lists 10 values (accept/retry_same_plan/replan/escalate_to_human/downgrade_mode/abort/quarantine/revoke_approval/pause_for_external/require_revalidation); §58.6 also lists 10 decision types (6 basic + 4 production extensions). Both fully consistent, contradiction does not hold. | No code change, no fix needed. |
| R2-27 | 9. Architecture Documentation Internal Consistency | HIGH | `§58.6` | Title says "six decisions" but table actually lists 10 kinds, self-contradictory | Fixed (documentation consistency fix) | §58.6 itself correctly states "ten decisions in total (6 basic + 4 production extensions)", but other document positions (§33 roadmap table, §58 glossary, v2.9 version history) when referencing §58.6 erroneously write "six decisions". All references have been uniformly corrected to "ten decisions" with annotation "6 basic + 4 production extensions". | Architecture document `docs_zh/architecture/00-platform-architecture.md` has uniformly corrected §58.6 related references (lines 8923, 9221, 10218, 10351, 10434, 10590) to "ten decisions". | Documentation internal consistency fix, no implementation code change needed. |
| R2-28 | 9. Architecture Documentation Internal Consistency | MEDIUM | `§45.7 vs §58.6` | LoopController decision types: §45.7 lists 5 kinds, §58.6 requires 6 (missing downgrade_mode) | Refuted (contradiction does not exist) | Verified, §45.7 lines 6622-6629 decision matrix actually lists 6 decisions (accept/retry_same_plan/replan/escalate_to_human/downgrade_mode/abort), including downgrade_mode; §58.6 lines 7253-7264 also list 6 basic decisions. Both fully consistent, contradiction does not hold. | No code change, no fix needed. |
| R2-29 | 9. Architecture Documentation Internal Consistency | MEDIUM | `§45.9` | Generator WorkProduct field naming | Fixed | §45.9 WorkProduct explicitly defines `node_run_id` (line 6671), glossary (line 10427) also writes `nodeRunId`, consistent with §5.5 NodeRun primary key naming. Issue original description "still uses step_id" is outdated. | Architecture document correctly uses `node_run_id`/`nodeRunId`, no fix needed. | No code change, only update issue description to accurately reflect actual state. |
| R2-30 | 9. Architecture Documentation Internal Consistency | MEDIUM | `§59.2` | ExplanationRequest uses deprecated workflow_id/step_id | Refuted (architecture document already correctly uses canonical names) | Verified, `§59.2`'s `ExplanationRequest` definition (line 8170) already uses `harness_run_id / node_run_id?`, does not use deprecated `workflow_id/step_id`. Original issue description is inaccurate. | Architecture document itself is correct, no fix needed. | No code change, only update audit doc state to accurately reflect verification result. |
| R2-31 | 9. Architecture Documentation Internal Consistency | MEDIUM | `§35` | contracts/ directory structure contains deprecated named subdirectories (execution-plan/, workflow-run/) | Moved out of implementation audit scope | This group belongs to architecture main document internal section/enumeration/directory expression conflicts, not implementation-architecture inconsistencies. | All items come from "Architecture Documentation Internal Consistency" section, essentially a self-consistency issue of `00-platform-architecture.md`. | No code change, retain as separate documentation audit scope. |
| R2-32 | 9. Architecture Documentation Internal Consistency | LOW | `§36.3` | Still uses Phase 1-9 as canonical success criteria, contradicts Ring 1/2/3 system | Resolved (documentation fix) | §36.3 Ring 3 sub-sections (3a/3b/3c) have all been changed to Ring naming, Phase 6-9 only retains in parentheses as historical mapping, no longer serves as independent gate. | Architecture document `docs_zh/architecture/00-platform-architecture.md` §36.3 has changed `#### Phase 6-7` -> `#### Ring 3a (corresponding to Phase 6-7)`; `#### Phase 8` -> `#### Ring 3b (corresponding to Phase 8)`; `#### Phase 9` -> `#### Ring 3c (corresponding to Phase 9, Enterprise expansion gate)`. Phase only retained as historical mapping, no longer used as canonical success criteria independently. | Status updated per source document fix. |
| R3-1 | 11. Harness Runtime Deep Implementation Gap (§45) | CRITICAL | `orchestration/harness/guardrail-engine.ts` | Guardrails only have policy/risk/tool/evidence/budget 5 layers; §45.20 requires Input (injection defense)/Planning/Tool/Memory/Output five layers — Input and Memory guardrails are completely missing | Resolved | Harness runtime items are from early snapshots, did not reflect subsequent HITL, responsibility records, decision bundle, toolbelt six-step assembly, five types of recovery, memory governance, and invariants closure. | Current `src/platform/five-plane-orchestration/harness/guardrails/guardrail-engine.ts` has implemented all 9 layers of §45.20 guardrails (Input/Planning/Tool/Memory/Output/Policy/Risk/Evidence/Budget): Input layer (lines 50-70) detects `__import__`, `<script>`, `javascript:`, `data:text/html`, `${...}` and other injection patterns; Memory layer (lines 117-137) detects `modify_own_prompt`, `update_own_instructions`, `change_own_role`, `escalate_own_permissions` and other self-reinforcement indicators. Old `orchestration/harness/guardrail-engine.ts` has been deprecated, code not moved, only write back basis. | No code change, only write back basis. |
| R3-2 | 11. Harness Runtime Deep Implementation Gap (§45) | CRITICAL | `orchestration/harness/hitl-runtime.ts` | Only supports open/resolve(approve/reject); §45.18 requires 5 HITL types: Inspect/Patch/Override/Takeover/Resume with complete state machines | Resolved | Harness runtime items are from early snapshots, did not reflect subsequent HITL, responsibility records, decision bundle, toolbelt six-step assembly, five types of recovery, memory governance, and invariants closure. | Current `src/platform/five-plane-orchestration/harness/` contains `inspect/patch/override/takeover/resume`, `HumanResponsibilityRecord`, `PromptExecutionRecord`, `DecisionInputBundle`, `downgrade_mode`, six-step `ToolbeltAssembler`, five-type `RecoveryController`, memory namespace promotion/demotion, and 10-item verification in `assertInvariants()`. | No code change, only write back basis. |
| R3-3 | 11. Harness Runtime Deep Implementation Gap (§45) | CRITICAL | `orchestration/harness/index.ts` | HumanResponsibilityRecord(§45.27) not implemented — each HITL operation needs to produce actor/action/scope/rationale/beforeRef/afterRef/expiresAt/auditRef | Resolved | Harness runtime items are from early snapshots, did not reflect subsequent HITL, responsibility records, decision bundle, toolbelt six-step assembly, five types of recovery, memory governance, and invariants closure. | Current `src/platform/five-plane-orchestration/harness/` contains `inspect/patch/override/takeover/resume`, `HumanResponsibilityRecord`, `PromptExecutionRecord`, `DecisionInputBundle`, `downgrade_mode`, six-step `ToolbeltAssembler`, five-type `RecoveryController`, memory namespace promotion/demotion, and 10-item verification in `assertInvariants()`. | No code change, only write back basis. |
| R3-4 | 11. Harness Runtime Deep Implementation Gap (§45) | HIGH | `orchestration/harness/index.ts` | autonomyMode uses manual/supervised/auto/full_auto; §42.1 requires suggestion/supervised/semi_auto/full_auto | Resolved | Harness runtime items are from early snapshots, did not reflect subsequent HITL, responsibility records, decision bundle, toolbelt six-step assembly, five types of recovery, memory governance, and invariants closure. | Current `src/platform/five-plane-orchestration/harness/` contains `inspect/patch/override/takeover/resume`, `HumanResponsibilityRecord`, `PromptExecutionRecord`, `DecisionInputBundle`, `downgrade_mode`, six-step `ToolbeltAssembler`, five-type `RecoveryController`, memory namespace promotion/demotion, and 10-item verification in `assertInvariants()`. | No code change, only write back basis. |
| R3-5 | 11. Harness Runtime Deep Implementation Gap (§45) | HIGH | `orchestration/harness/index.ts` | HarnessRun missing §45.13 required fields: tenantId/goal/mode/riskLevel/ownership/auditRefs/traceId 7 fields | Resolved | Harness runtime items are from early snapshots, did not reflect subsequent HITL, responsibility records, decision bundle, toolbelt six-step assembly, five types of recovery, memory governance, and invariants closure. | Current `src/platform/five-plane-orchestration/harness/` contains `inspect/patch/override/takeover/resume`, `HumanResponsibilityRecord`, `PromptExecutionRecord`, `DecisionInputBundle`, `downgrade_mode`, six-step `ToolbeltAssembler`, five-type `RecoveryController`, memory namespace promotion/demotion, and 10-item verification in `assertInvariants()`. | No code change, only write back basis. |
| R3-6 | 11. Harness Runtime Deep Implementation Gap (§45) | HIGH | `orchestration/harness/index.ts` | HarnessStep missing §45.13 required fields: nodeRunRefs/rationale/evidenceRefs/toolCalls/latency/cost/error/nextAction 8 fields | Resolved | Harness runtime items are from early snapshots, did not reflect subsequent HITL, responsibility records, decision bundle, toolbelt six-step assembly, five types of recovery, memory governance, and invariants closure. | Current `src/platform/five-plane-orchestration/harness/` contains `inspect/patch/override/takeover/resume`, `HumanResponsibilityRecord`, `PromptExecutionRecord`, `DecisionInputBundle`, `downgrade_mode`, six-step `ToolbeltAssembler`, five-type `RecoveryController`, memory namespace promotion/demotion, and 10-item verification in `assertInvariants()`. | No code change, only write back basis. |
| R3-7 | 11. Harness Runtime Deep Implementation Gap (§45) | HIGH | `orchestration/harness/index.ts` | HarnessDecision only 6 values; §58.6 requires adding quarantine/revoke_approval/pause_for_external/require_revalidation | Resolved | Harness runtime items are from early snapshots, did not reflect subsequent HITL, responsibility records, decision bundle, toolbelt six-step assembly, five types of recovery, memory governance, and invariants closure. | Current `src/platform/five-plane-orchestration/harness/` contains `inspect/patch/override/takeover/resume`, `HumanResponsibilityRecord`, `PromptExecutionRecord`, `DecisionInputBundle`, `downgrade_mode`, six-step `ToolbeltAssembler`, five-type `RecoveryController`, memory namespace promotion/demotion, and 10-item verification in `assertInvariants()`. | No code change, only write back basis. |
| R3-8 | 11. Harness Runtime Deep Implementation Gap (§45) | HIGH | `orchestration/harness/toolbelt-assembler.ts` | Only computes allowed/blocked set intersection; §45.4 requires 6-step assembly: domain->constraint->risk->budget->security->reliability | Resolved | Harness runtime items are from early snapshots, did not reflect subsequent HITL, responsibility records, decision bundle, toolbelt six-step assembly, five types of recovery, memory governance, and invariants closure. | Current `src/platform/five-plane-orchestration/harness/` contains `inspect/patch/override/takeover/resume`, `HumanResponsibilityRecord`, `PromptExecutionRecord`, `DecisionInputBundle`, `downgrade_mode`, six-step `ToolbeltAssembler`, five-type `RecoveryController`, memory namespace promotion/demotion, and 10-item verification in `assertInvariants()`. | No code change, only write back basis. |
| R3-9 | 11. Harness Runtime Deep Implementation Gap (§45) | HIGH | `orchestration/harness/recovery-controller.ts` | Only handles 3 failure types; §45.11 requires 5 types including llm_provider_unavailable/budget_exhausted/platform_panic | Resolved | Harness runtime items are from early snapshots, did not reflect subsequent HITL, responsibility records, decision bundle, toolbelt six-step assembly, five types of recovery, memory governance, and invariants closure. | Current `src/platform/five-plane-orchestration/harness/` contains `inspect/patch/override/takeover/resume`, `HumanResponsibilityRecord`, `PromptExecutionRecord`, `DecisionInputBundle`, `downgrade_mode`, six-step `ToolbeltAssembler`, five-type `RecoveryController`, memory namespace promotion/demotion, and 10-item verification in `assertInvariants()`. | No code change, only write back basis. |
| R3-10 | 11. Harness Runtime Deep Implementation Gap (§45) | HIGH | `orchestration/harness/memory-manager.ts` | Namespaces run/domain/shared without governance; §45.16 requires Working/Long-term/Shared with promotion/demotion policy + anti-self-reinforcement | Resolved | Harness runtime items are from early snapshots, did not reflect subsequent HITL, responsibility records, decision bundle, toolbelt six-step assembly, five types of recovery, memory governance, and invariants closure. | Current `src/platform/five-plane-orchestration/harness/` contains `inspect/patch/override/takeover/resume`, `HumanResponsibilityRecord`, `PromptExecutionRecord`, `DecisionInputBundle`, `downgrade_mode`, six-step `ToolbeltAssembler`, five-type `RecoveryController`, memory namespace promotion/demotion, and 10-item verification in `assertInvariants()`. | No code change, only write back basis. |
| R3-11 | 11. Harness Runtime Deep Implementation Gap (§45) | HIGH | `orchestration/harness/index.ts` | assertInvariants only checks budget/state; §45.21 defines 10 invariants (INV-1~INV-10) all not enforced | Resolved | Harness runtime items are from early snapshots, did not reflect subsequent HITL, responsibility records, decision bundle, toolbelt six-step assembly, five types of recovery, memory governance, and invariants closure. | Current `src/platform/five-plane-orchestration/harness/` contains `inspect/patch/override/takeover/resume`, `HumanResponsibilityRecord`, `PromptExecutionRecord`, `DecisionInputBundle`, `downgrade_mode`, six-step `ToolbeltAssembler`, five-type `RecoveryController`, memory namespace promotion/demotion, and 10-item verification in `assertInvariants()`. | No code change, only write back basis. |
| R3-12 | 11. Harness Runtime Deep Implementation Gap (§45) | HIGH | `orchestration/harness/index.ts` | PromptExecutionRecord(§45.24) not implemented — needs to freeze promptVersion/modelRoute/inputHash/outputHash/contextSnapshotRef/guardrailResult/usage | Resolved | Harness runtime items are from early snapshots, did not reflect subsequent HITL, responsibility records, decision bundle, toolbelt six-step assembly, five types of recovery, memory governance, and invariants closure. | Current `src/platform/five-plane-orchestration/harness/` contains `inspect/patch/override/takeover/resume`, `HumanResponsibilityRecord`, `PromptExecutionRecord`, `DecisionInputBundle`, `downgrade_mode`, six-step `ToolbeltAssembler`, five-type `RecoveryController`, memory namespace promotion/demotion, and 10-item verification in `assertInvariants()`. | No code change, only write back basis. |
| R3-13 | 11. Harness Runtime Deep Implementation Gap (§45) | HIGH | `orchestration/harness/index.ts` | DecisionInputBundle(§45.25) not implemented — need to freeze evaluator/policy/budget/risk/node/sideEffect/hitl/guardrail state before decision | Resolved | Harness runtime items are from early snapshots, did not reflect subsequent HITL, responsibility records, decision bundle, toolbelt six-step assembly, five types of recovery, memory governance, and invariants closure. | Current `src/platform/five-plane-orchestration/harness/` contains `inspect/patch/override/takeover/resume`, `HumanResponsibilityRecord`, `PromptExecutionRecord`, `DecisionInputBundle`, `downgrade_mode`, six-step `ToolbeltAssembler`, five-type `RecoveryController`, memory namespace promotion/demotion, and 10-item verification in `assertInvariants()`. | No code change, only write back basis. |
| R3-14 | 11. Harness Runtime Deep Implementation Gap (§45) | MEDIUM | `orchestration/harness/context-assembler.ts` | Directly copies source object; §45.5 requires token budget trimming + relevance scoring + freshness scoring + trust filtering | Resolved | Harness runtime items are from early snapshots, did not reflect subsequent HITL, responsibility records, decision bundle, toolbelt six-step assembly, five types of recovery, memory governance, and invariants closure. | Current `src/platform/five-plane-orchestration/harness/` contains `inspect/patch/override/takeover/resume`, `HumanResponsibilityRecord`, `PromptExecutionRecord`, `DecisionInputBundle`, `downgrade_mode`, six-step `ToolbeltAssembler`, five-type `RecoveryController`, memory namespace promotion/demotion, and 10-item verification in `assertInvariants()`. | No code change, only write back basis. |
| R3-15 | 11. Harness Runtime Deep Implementation Gap (§45) | MEDIUM | `orchestration/harness/index.ts` | ContextAssemblyContract(§45.23) not implemented — needs per-role context isolation with taintPolicy/rankingPolicy/redactionPolicy | Resolved | Harness runtime items are from early snapshots, did not reflect subsequent HITL, responsibility records, decision bundle, toolbelt six-step assembly, five types of recovery, memory governance, and invariants closure. | Current `src/platform/five-plane-orchestration/harness/` contains `inspect/patch/override/takeover/resume`, `HumanResponsibilityRecord`, `PromptExecutionRecord`, `DecisionInputBundle`, `downgrade_mode`, six-step `ToolbeltAssembler`, five-type `RecoveryController`, memory namespace promotion/demotion, and 10-item verification in `assertInvariants()`. | No code change, only write back basis. |
| R3-16 | 12. Org Governance + Scale Ecosystem Deep Gaps (§46-§57) | CRITICAL | `scale-ecosystem/multi-region/region-router/` | RegionDescriptor missing provider/endpoints/dataResidencyPolicy; status uses active/degraded/disabled instead of architecture-required active/standby/draining | Resolved | Production code has supplemented provider/endpoints/dataResidencyPolicy fields, status enum corrected to active/standby/draining (additionally degraded/maintenance); old audit document items not synchronously updated. | Current `src/scale-ecosystem/multi-region/region-router/index.ts` defines `provider/endpoints/dataResidencyPolicy` and `active/standby/draining/degraded/maintenance`; `tests/unit/platform/scale-ecosystem/multi-region/region-router.test.ts` has passed targeted regression. | No production code change, this round has corrected the document items. |
| R3-17 | 12. Org Governance + Scale Ecosystem Deep Gaps (§46-§57) | CRITICAL | `scale-ecosystem/multi-region/failover-controller/` | Fencing epoch mechanism has been implemented; §52.3 requires failover to promote epoch, old leader can only join as follower after recovery | Resolved | The issue description in the audit items stays at the early "no fencing epoch" version; actual code has long had `promoteEpoch` tracking, `FencingToken` issuance, `acknowledgeDemotion` old leader demotion, and `detectSplitBrain` split-brain detection. | Current `src/scale-ecosystem/multi-region/failover-controller/index.ts` contains `RegionFailoverController` class with `promoteEpoch`/`fencingToken`/`getPromoteEpoch()`/`getFencingToken()`/`acknowledgeDemotion()`/`detectSplitBrain()`/`canLeaderServeWrites()`; `tests/unit/scale-ecosystem/multi-region/failover-controller-comprehensive.test.ts` has passed. | No production code change, correct audit issue description to accurately reflect existing implementation. |
| R3-18 | 12. Org Governance + Scale Ecosystem Deep Gaps (§46-§57) | CRITICAL | `scale-ecosystem/integration/connector-registry/` | ConnectorManifest missing entire ConnectorCapabilityProfile(§57.1): actionRiskProfiles/permissionProbes/quotaProbes/credentialRotationPolicy | Resolved | Connector manifest has been expanded to capability profile, but audit conclusion still stays at early minimum manifest version. | Current `src/scale-ecosystem/integration/connector-registry/index.ts` contains `ConnectorCapabilityProfileSchema` and `actionRiskProfiles/permissionProbes/quotaProbes/credentialRotationPolicy`; related schema tests have passed. | No production code change, only write back basis. |
| R3-19 | 12. Org Governance + Scale Ecosystem Deep Gaps (§46-§57) | HIGH | `scale-ecosystem/sla-engine/tier-resolver/` | SlaTierSchema missing §54.1 required fields: availability/externalP95/internalP99/approvalLatencySlo/incidentResponseSlo/costMultiplier/supportLevel | Resolved | SLA tier schema has been expanded, but old audit did not absorb canonical field upgrade. | Current `src/scale-ecosystem/sla-engine/tier-resolver/index.ts` contains `availability/externalP95/internalP99/approvalLatencySlo/incidentResponseSlo/costMultiplier/supportLevel`; `tests/unit/scale-ecosystem/sla-engine/tier-resolver/index.test.ts` has passed. | No production code change, only write back basis. |
| R3-20 | 12. Org Governance + Scale Ecosystem Deep Gaps (§46-§57) | HIGH | `scale-ecosystem/sla-engine/sla-operations-service.ts` | No SLA split by workflow class (§54.3 requires deterministic/LLM-assisted/HITL-waiting with separate commitments) | Resolved | Implementation has already split commitments by workflow class multiplier, but old tests do not explicitly pass `workflowClass`, making it look like unimplemented. | Current `src/scale-ecosystem/sla-engine/sla-operations-service.ts` defines `WorkflowClass` with differentiated latency multipliers; this round has corrected `tests/unit/scale-ecosystem/sla-engine/sla-operations-service.test.ts`'s old assertions and passed regression. | No production code change, correct old tests and document evidence. |
| R3-21 | 12. Org Governance + Scale Ecosystem Deep Gaps (§46-§57) | HIGH | `scale-ecosystem/marketplace/catalog/` | Uses listingId instead of §55.2 entryId, missing packId/rating/installCount; certificationStatus enum mismatch | Resolved | Marketplace catalog has migrated to `entryId` and new fields, but old tests still use `listingId`. | Current `src/scale-ecosystem/marketplace/catalog/index.ts` defines `entryId/packId/rating/installCount/certificationStatus`; this round has corrected `tests/unit/scale-ecosystem/marketplace/catalog/catalog.test.ts` and passed. | No production code change, correct old tests and document evidence. |
| R3-22 | 12. Org Governance + Scale Ecosystem Deep Gaps (§46-§57) | HIGH | `org-governance/compliance-engine/framework-catalog.ts` | auditRequirements is string[] instead of §49.1 required AuditSpec[] (with frequency/evidenceType/retentionPeriod) | Resolved | Compliance framework has switched to `AuditSpec[]`, but old tests still treat `auditRequirements` as string array. | Current `src/org-governance/compliance-engine/framework-catalog.ts` defines `AuditSpecSchema` and structured `auditRequirements`; this round has corrected `tests/unit/org-governance/compliance-engine/framework-catalog.test.ts` and passed. | No production code change, correct old tests and document evidence. |
| R3-23 | 12. Org Governance + Scale Ecosystem Deep Gaps (§46-§57) | HIGH | `org-governance/compliance-engine/inheritance/` | No PolicyStrictnessComparator(§49.2); incomparable policies silently fall back to Math.min instead of entering compliance approval | Resolved | Inheritance module has strictness comparator and explicit semantics of "incomparable enters compliance approval", but old audit stayed at early version with only merge logic. | Current `src/org-governance/compliance-engine/inheritance/index.ts` defines `PolicyStrictnessComparator`, `DefaultPolicyStrictnessComparator`, `comparePolicyStrictness()`, and when `incomparable` returns `requiresComplianceApproval: true`; `tests/unit/org-governance/compliance-engine/inheritance.test.ts` has passed. | No production code change, only write back basis. |
| R3-24 | 12. Org Governance + Scale Ecosystem Deep Gaps (§46-§57) | HIGH | `org-governance/approval-routing/route-engine/` | applySodPolicy does not prevent same-chain mutual approval (§47.1) — two people in the same approval chain can approve each other's requests | Resolved | Route-engine has implemented same-chain mutual approval blocking, parallel signing, and revalidation, but old tests still carry early groupId/FX preconditions. | Current `src/org-governance/approval-routing/route-engine/index.ts` has `applySodPolicy()` same-chain blocking and `revalidateApprovalRoute()`; this round has corrected `tests/unit/org-governance/approval-routing/route-engine/index.test.ts`'s old assertions and passed. | No production code change, correct old tests and document evidence. |
| R3-25 | 12. Org Governance + Scale Ecosystem Deep Gaps (§46-§57) | HIGH | `org-governance/knowledge-boundary/chinese-wall-access-saga.ts` | Missing §50.3 two-phase commit (prepare lock->atomic commit->failure reconciliation); only does simple pass/fail classification | Resolved (source code fix and targeted test supplement) | The original implementation indeed had real gaps: `prepare_release` mistakenly took `prepareGrant` handler; prepare/commit handler exceptions did not enter saga failure chain; rollback only generated based on already committed commit actions, causing missing failure reconciliation on prepare-only failure. | This round has supplemented prepare/commit try-catch in `src/org-governance/knowledge-boundary/chinese-wall-access-saga.ts`, compatible with multiple prepare/commit stepId associations, generated compensation actions based on successful actions in reverse, and converged `committedActions` to successful 2PC trajectory; `tests/unit/org-governance/knowledge-boundary/chinese-wall-access-saga.test.ts`, `tests/integration/org-governance/knowledge-boundary-integration.test.ts`, `tests/integration/org-governance/governance-sagas.integration.test.ts`, `tests/integration/platform/org-governance/knowledge-boundary/chinese-wall-access.integration.test.ts` have passed. | Production code, tests, and document basis have been changed. |
| R3-26 | 12. Org Governance + Scale Ecosystem Deep Gaps (§46-§57) | HIGH | `scale-ecosystem/resource-manager/quota-enforcer/` | QuotaPolicy is single-dimension; §53.2 requires 7-dimension MultiResourceQuotaVector (worker_concurrency/tool_qps/model_tpm/model_rpm/budget_amount/approval_capacity/storage_io), all must pass for admission | Resolved | Quota-enforcer has been expanded to multi-dimensional quota vector, but old audit retained single-dimension era conclusion. | Current `src/scale-ecosystem/resource-manager/quota-enforcer/index.ts` defines `MultiResourceQuotaVectorSchema` and `evaluateMultiDimensionalQuota()`; `tests/unit/scale-ecosystem/resource-manager/quota-enforcer/index.test.ts` has passed. | No production code change, only write back basis. |
| R3-27 | 12. Org Governance + Scale Ecosystem Deep Gaps (§46-§57) | MEDIUM | `org-governance/delegated-governance/` | GovernanceDelegationRevocationSaga missing cascade scope (§51.1): needs to cover pending approval/active session/secret lease/worker lease/scheduled trigger | Resolved | Revocation saga has covered 5 categories of revocation scope per cascade scope, old audit did not keep up with default scope and receipt field expansion. | Current `src/org-governance/delegated-governance/governance-delegation-revocation-saga.ts` contains `pendingApprovals/activeSessions/secretLeases/workerLeases/scheduledTriggers`; `tests/integration/platform/org-governance/delegated-governance/delegation-revocation-saga.integration.test.ts` and `tests/integration/org-governance/governance-sagas.integration.test.ts` have passed. | No production code change, only write back basis and correct old test scope. |
| R3-28 | 12. Org Governance + Scale Ecosystem Deep Gaps (§46-§57) | MEDIUM | `org-governance/org-model/org-governance-saga.ts` | §46.3 requires commit fixed order (identity->approval->budget->domain->agent) with OrgGovernanceSagaReceipt; actual is unordered and has no receipt | Resolved | `OrgGovernanceSaga` has solidified phase order and provides `executeWithReceipt()`, old audit and old unit tests still treat "compensate should also execute when no failure" as current semantics. | Current `src/org-governance/org-model/org-governance-saga.ts` defines fixed `PHASE_ORDER` and `OrgGovernanceSagaReceipt`; this round has corrected `tests/unit/org-governance/org-model/org-governance-saga.test.ts` and related integration tests' outdated assertions and passed. | No production code change, correct old tests and document evidence. |
| R3-29 | 12. Org Governance + Scale Ecosystem Deep Gaps (§46-§57) | MEDIUM | `org-governance/sso-scim/identity-sync-service.ts` | DLQ only records without retry; §48.2 requires retry/backoff + daily reconciliation + IdentityReconciliationReport | Resolved | Identity sync has supplemented DLQ retry/backoff, daily reconciliation, and structured report, but old audit did not absorb this capability. | Current `src/org-governance/sso-scim/identity-sync-service.ts` implements `processDlqWithRetry()`, `retryDlqRecord()`, `generateDailyReconciliation()` and `IdentityReconciliationReport`; `tests/unit/org-governance/identity-sync-service.test.ts`, `tests/integration/org-governance/sso-scim/identity-sync-integration.test.ts` have passed. | No production code change, only write back basis. |
| R3-30 | 12. Org Governance + Scale Ecosystem Deep Gaps (§46-§57) | MEDIUM | `scale-ecosystem/multi-region/cross-region-routing-service.ts` | Cross-border transfer is only boolean allowCrossBorder; §52.4 requires 5-step chain: JurisdictionClassifier->TransferImpactAssessor->MechanismSelector->DataMinimizer->OutputScanner | Resolved | Cross-region routing has landed 5-step cross-border chain, old integration tests still use outdated region status enum, magnifying into "implementation missing". | Current `src/scale-ecosystem/multi-region/cross-region-routing-service.ts` explicitly contains `classifyJurisdiction()`, `assessTransferImpact()`, `selectMechanism()`, `minimizeData()`, `scanOutput()`; this round has corrected `tests/integration/scale-ecosystem/cross-region-routing-service.test.ts`'s old status assertions and passed. | No production code change, correct old tests and document evidence. |
| R3-31 | 12. Org Governance + Scale Ecosystem Deep Gaps (§46-§57) | MEDIUM | `scale-ecosystem/billing/types.ts` | RecordUsageInput has single metricType; §53.2 requires multi-dimensional admission guard | Resolved | Billing types has expanded to multi-dimensional usage dimensions and dimension quota counters, old audit still stayed at early single metricType input. | Current `src/scale-ecosystem/billing/types.ts` contains `RecordUsageInput.metricDimensions` and `RecordUsageResult.dimensionQuotaCounters`; `tests/unit/scale-ecosystem/billing/types.test.ts` has passed. | No production code change, only write back basis. |
| R3-32 | 12. Org Governance + Scale Ecosystem Deep Gaps (§46-§57) | MEDIUM | `org-governance/knowledge-boundary/sharing-gate/` | evaluateKnowledgeShare returns boolean; §50.3 requires going through CrossBoundaryTransform (desensitization/summary/field filtering) | Resolved | Audit based on old implementation snapshot; current implementation has long been changed to explicit cross-boundary transform return value, not boolean. | `evaluateKnowledgeShare(...)` in `src/org-governance/knowledge-boundary/sharing-gate/index.ts` now returns `CrossBoundaryTransformResult` or `null`, with `mode(summary/field_filter)` and `allowedFieldKeys` inside. | No code change, only write back basis. |
| R3-33 | 13. Operations Maturity + SDK Gaps (§51-§69) | HIGH | `ops-maturity/explainability/explanation-pipeline-service.ts` | StageRationale missing §59.3 fields: rationaleId/alternatives/confidence/decisionInputRef/versionLockRef/visibilityLabels/renderedExplanation | Resolved | Audit references old `StageRationale` structure; current explainability contract has supplemented these fields. | `StageRationale` in `src/ops-maturity/explainability/explanation-pipeline-service.ts` has declared `rationaleId/alternatives/confidence/decisionInputRef/versionLockRef/visibilityLabels/renderedExplanation`, generation chain also writes these fields. | No code change, only write back basis. |
| R3-34 | 13. Operations Maturity + SDK Gaps (§51-§69) | HIGH | `ops-maturity/emergency/platform-panic-service.ts` | PlatformPanicDirective missing §60.1 severity (full/partial)/reconfirmationAfterSeconds/rollbackStrategy | Resolved | Old audit did not keep up with current panic directive contract. | `PlatformPanicDirective` in `src/ops-maturity/emergency/platform-panic-service.ts` contains `severity`, `reconfirmationAfterSeconds`, `rollbackStrategy`; related `platform-panic` unit tests continue to pass this round. | No code change, only write back basis. |
| R3-35 | 13. Operations Maturity + SDK Gaps (§51-§69) | HIGH | `ops-maturity/agent-lifecycle/agent-registry/` | AgentLifecycleState missing removed state (§61.3 requires 9 states); transitions missing archived->removed and paused->canary | Resolved (source code fix and targeted test supplement) | Main state machine actually has migrated to 9 states, but auxiliary semantics and a batch of old tests still follow 8-state/`archived` terminal state understanding, causing audit evidence to mix with current implementation. | `src/ops-maturity/agent-lifecycle/agent-registry/index.ts` has confirmed 9 states and `archived->removed`, `paused->canary`; this round further corrected `isTerminalState(...)` to only have `removed` as terminal state, and updated all regression in `tests/unit/ops-maturity/agent-lifecycle/*.test.ts`. | Code and tests have been fixed, no gap remains. |
| R3-36 | 13. Operations Maturity + SDK Gaps (§51-§69) | HIGH | `ops-maturity/edge-runtime/edge-runtime-sync-service.ts` | EdgeRuntimeProfile missing §62.2 deviceId/offlineMaxDuration/keyLease/risk_level<=medium gate | Resolved | Runtime code has added these fields and gate, but old test samples still use profile missing fields, creating an "as if not implemented" illusion. | `executeOffline(...)` in `src/ops-maturity/edge-runtime/edge-runtime-sync-service.ts` checks `deviceId/offlineMaxDuration/keyLease`, and rejects `riskLevel > medium`; this round synchronously corrected `tests/unit/ops-maturity/edge-runtime/edge-runtime-sync-service.test.ts` and related integration samples. | No main implementation logic change, only correct expired tests and basis. |
| R3-37 | 13. Operations Maturity + SDK Gaps (§51-§69) | HIGH | `ops-maturity/edge-runtime/sync-queue/` | EdgeSyncEnvelope missing §62.3 device_id/sequence_no/prev_hash/side_effect_dependency_refs/signature/local_time_offset | Resolved (source code fix and targeted test supplement) | This is not "completely no module", but the queue envelope contract has long maintained minimum compatible form, key provenance fields stayed at optional/not-forced wiring state. | This round converged `EdgeSyncEnvelope` in `src/ops-maturity/edge-runtime/sync-queue/index.ts` to canonical contract containing `device_id/sequence_no/prev_hash/side_effect_dependency_refs/signature/local_time_offset/createdAt`, and supplemented mapping in ordered sync path of `src/ops-maturity/edge-runtime/edge-runtime-sync-service.ts`; `tests/unit/ops-maturity/edge-runtime/sync-queue*.test.ts` has added/updated regression. | Code and tests have been fixed, no gap remains. |
| R3-38 | 13. Operations Maturity + SDK Gaps (§51-§69) | HIGH | `sdk/client-sdk/api-client.ts` | Does not send §22.2 required X-Platform-Version/X-SDK-Version/X-Contract-Version version handshake headers | Resolved | This group mixes old directory snapshots, interface cognition, and test existence misjudgment, current repo files directly disprove. | Current `src/sdk/client-sdk/api-client.ts` sends version handshake headers; `src/sdk/cli/index.ts` contains `pack create/test/validate/publish`; `src/platform/five-plane-*` directories exist; `dashboard-websocket-server.ts` has bound principal/tenantId; `structured-logger.ts` has `crosscuttingFabric`; `tests/invariants/` exists; startup order and ring catalog have been changed to canonical. | No code change, only write back basis. |
| R3-39 | 13. Operations Maturity + SDK Gaps (§51-§69) | HIGH | `sdk/cli/index.ts` | Missing §22.3 required pack create/test/validate/publish CLI commands | Resolved | This group mixes old directory snapshots, interface cognition, and test existence misjudgment, current repo files directly disprove. | Current `src/sdk/client-sdk/api-client.ts` sends version handshake headers; `src/sdk/cli/index.ts` contains `pack create/test/validate/publish`; `src/platform/five-plane-*` directories exist; `dashboard-websocket-server.ts` has bound principal/tenantId; `structured-logger.ts` has `crosscuttingFabric`; `tests/invariants/` exists; startup order and ring catalog have been changed to canonical. | No code change, only write back basis. |
| R3-40 | 13. Operations Maturity + SDK Gaps (§51-§69) | MEDIUM | `ops-maturity/edge-runtime/` | Conflict resolution includes accept_edge; §62.3 requires central wins + generating Incident for manual review | Resolved | Current runtime has long used central-wins; the problem is in old tests/old cognition still following `merge/accept_edge` path assertions. | `src/ops-maturity/edge-runtime/edge-runtime-sync-service.ts` returns `accept_central` in conflict branch and generates `incidentId`; this round uniformly changed unit/integration contract test samples to central-wins semantics. | No main implementation logic change, only correct expired tests and basis. |
| R3-41 | 13. Operations Maturity + SDK Gaps (§51-§69) | MEDIUM | `sdk/pack-sdk/pack-manifest.ts` | BusinessPackManifest missing §22.2 sdk_semver/platform_min_version/platform_max_version/contract_test_generator | Resolved | Audit snapshot did not reflect pack manifest current field surface. | `BusinessPackManifest` in `src/sdk/pack-sdk/pack-manifest.ts` contains `sdk_semver/platform_min_version/platform_max_version/contract_test_generator`, related `pack-manifest` targeted tests continue to pass this round. | No code change, only write back basis. |
| R3-42 | 13. Operations Maturity + SDK Gaps (§51-§69) | MEDIUM | `ops-maturity/cost-optimizer/` | CostAttributionRecord uses single amountUsd; §64.1 requires 7-dimension breakdown (llm/tool/compute/storage/egress/humanReview/total) | Verified | This round verified `CostAttributionRecord` in `src/ops-maturity/cost-optimizer/cost-optimization-service.ts` contains all 7 dimension fields (llmCostUsd/toolCostUsd/computeCostUsd/storageCostUsd/egressCostUsd/humanReviewCostUsd + amountUsd); `aggregateCostAttribution` correctly sums 7 dimensions; `aggregate` method correctly passes 7 dimension fields. | No code change, only write back verification result. |
| R3-43 | 13. Operations Maturity + SDK Gaps (§51-§69) | MEDIUM | `ops-maturity/compliance-reporter/` | Missing ControlCoverageReport + GapAnalyzer(§66.2); evidence-to-control mapping missing controlId/freshness/owner/exception | Resolved | Current compliance reporter has these structures, old audit did not follow up. | `src/ops-maturity/compliance-reporter/compliance-report-pipeline-service.ts` has `ControlCoverageReport/GapAnalysisResult/GapAnalyzerService`, `src/ops-maturity/compliance-reporter/evidence-mapper/index.ts` contains `controlId/freshness/owner/exception` and other mapping fields. | No code change, only write back basis. |
| R3-44 | 13. Operations Maturity + SDK Gaps (§51-§69) | MEDIUM | `ops-maturity/chaos/` | Missing PanicDrillReport(§60.4): ingress_block_time/execution_quiescence_time/plane_ack_success_rate etc. | Resolved | Chaos drill output contract exists, just old audit did not re-verify properly. | `src/ops-maturity/chaos/chaos-experiment-scheduler.ts` has declared `PanicDrillReport`, related `chaos-experiment-scheduler` unit tests continue to pass this round. | No code change, only write back basis. |
| R3-45 | 13. Operations Maturity + SDK Gaps (§51-§69) | MEDIUM | `ops-maturity/multimodal/` | MultimodalInputPart missing §68.2 provenance (C2PA/watermark/hash/license)/artifactRef; SafetyFinding missing confidence/policyDecision/appealPath | Resolved | Multimodal contract has evolved to include provenance / artifactRef / safety adjudication fields, old audit still references early minimum structure. | `src/ops-maturity/multimodal/multimodal-gateway-service.ts` contains `MultimodalInputPart.provenance/artifactRef` and `SafetyFinding.confidence/policyDecision/appealPath`. | No code change, only write back basis. |
| R3-46 | 13. Operations Maturity + SDK Gaps (§51-§69) | MEDIUM | `ops-maturity/drift-detection/cross-agent-analyzer/` | Does not produce CrossAgentDriftAlert(§63.4); missing alert severity + anti-gaming distinction | Resolved | Audit judgment lagged behind current drift analyzer contract. | `src/ops-maturity/drift-detection/cross-agent-analyzer/index.ts` has produced `CrossAgentDriftAlert`, and distinguishes `severity` from anti-gaming related signals. | No code change, only write back basis. |
| R3-47 | 13. Operations Maturity + SDK Gaps (§51-§69) | MEDIUM | `ops-maturity/platform-ops-agent/` | OpsAgentDefinition missing §69.1 ops_data_boundary declaration (only platform metrics/logs/config, no business payload) | Resolved (source code fix and targeted test supplement) | There is indeed a real gap here: `ops_data_boundary` has long stayed at optional field, no hard declaration, no runtime boundary check. | This round changed `OpsAgentDefinition.ops_data_boundary` in `src/ops-maturity/platform-ops-agent/platform-ops-agent-service.ts` to required, added `DEFAULT_OPS_DATA_BOUNDARY` and constructor-time validation, prohibiting `businessPayloadAllowed=true`; related unit/integration constructor samples and rejection cases have all been supplemented. | Code and tests have been fixed, no gap remains. |
| R3-48 | 13. Operations Maturity + SDK Gaps (§51-§69) | LOW | `ops-maturity/capacity-planner/` | failoverReservePercent hardcoded to 15%; §67.2 requires dynamic N+1 by SLA tier | Resolved | Current capacity planner has implemented dynamic failover reserve and approval capacity N+1 dual-path logic. | `src/ops-maturity/capacity-planner/capacity-planning-service.ts` line 240 `approvalCapacityNeeded: sloRisk === "high" ? 2 : 1` implements SLA risk-driven N+1 approval capacity; lines 286-292 `computeDynamicFailoverReserve` dynamically allocates failover reserve (30%/20%/15%) by gold/silver/bronze SLA tier, no longer a single hardcoded percentage. | No code change, only write back basis. |
| R3-49 | 13. Operations Maturity + SDK Gaps (§51-§69) | LOW | `sdk/harness-sdk/` | Missing traceReplay/sideEffectReconciliation methods (§22) | Resolved | This is not "method missing", but method already exists, semantics still lean toward compatibility implementation; original audit title itself no longer holds. | `src/sdk/harness-sdk/index.ts` has exposed `traceReplay(...)` and `sideEffectReconciliation(...)`. Current implementation leans toward compatibility shim, but "missing method" accusation has been directly overturned by current code. | No code change, only write back basis. |