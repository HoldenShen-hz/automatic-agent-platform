import { createMobileFeatureCard } from "@aa/ui-mobile";
export function createWorkflowBuilderMobileCards() {
    return [
        createMobileFeatureCard("Palette", "OAPEFLIR node palette"),
        createMobileFeatureCard("Canvas", "Drag and connect workflow graph"),
        createMobileFeatureCard("Validation", "Schema and policy checks"),
    ];
}
