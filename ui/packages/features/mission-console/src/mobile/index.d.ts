import type { MissionConsoleVm } from "../hooks";
export declare function createMissionConsoleMobileCards(vm: MissionConsoleVm): {
    id: string;
    title: string;
    subtitle: string;
    evidenceCount: number;
}[];
