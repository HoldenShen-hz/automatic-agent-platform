# UI Storybook Package

This directory is intentionally present so UI package documentation and tooling
references have a stable target. Storybook configuration should live here when
UI component stories are promoted from package-local examples into the shared
design review surface.
