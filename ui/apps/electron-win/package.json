{
  "name": "@aa/electron-win",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "license": "UNLICENSED",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json && node ./scripts/prepare-shell-assets.mjs",
    "dev": "npm run build && electron .",
    "smoke": "node --import tsx ./src/index.ts"
  },
  "devDependencies": {
    "electron": "^31.0.0",
    "electron-updater": "^6.6.2"
  },
  "build": {
    "win": {
      "verifyUpdateCodeSignature": true,
      "signAndEditExecutable": false
    }
  }
}
