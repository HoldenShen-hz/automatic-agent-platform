export const PLATFORM_ROOT_BASELINE_COUNTS = Object.freeze({
  domains: Object.freeze({
    totalCapabilityCount: 31,
    capabilityCounts: Object.freeze({
      ring1: 8,
      ring2: 11,
      ring3: 12,
    }),
  }),
});
