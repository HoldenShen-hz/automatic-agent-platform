These directories are pack fixtures for validation, naming, and registry tests.

- `tests/fixtures/packs/` is not a publishable marketplace root.
- Mixed names such as `123-pack`, `my.pack.id`, and `test_pack` are intentional fixture cases.
- Duplicate `domainId` and `owner` values are intentional to exercise pack-id normalization and disambiguation logic.
