{
  "name": "test-pack",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "test": "node --test tests/",
    "domain:lint": "automatic-agent pack-validate --domain-id=test-domain --lint-only",
    "domain:validate": "automatic-agent pack-validate --domain-id=test-domain"
  }
}
