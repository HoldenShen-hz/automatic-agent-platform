# Release Claims Negative Enum Seed

> The audit:release-claims scanner MUST NOT report enum-style mode labels
> that mention `production-ready` as a gate tier rather than a release claim.

- 新增 warning-only / P0 / production-ready 三档 coverage audit 入口
